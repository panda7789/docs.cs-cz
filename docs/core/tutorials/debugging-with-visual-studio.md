---
title: Ladění aplikace Hello World .NET Core pomocí sady Visual Studio 2017
description: Naučte se ladit aplikaci Hello World napsanou v C# nebo Visual Basic se sadou Visual Studio 2017.
ms.date: 12/15/2017
ms.custom: vs-dotnet, seodec18
ms.openlocfilehash: 7239ca52c0b90c4cfacd68581f569b9ac7d70eae
ms.sourcegitcommit: 7b1ce327e8c84f115f007be4728d29a89efe11ef
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/13/2019
ms.locfileid: "70969395"
---
# <a name="debug-your-c-or-visual-basic-net-core-hello-world-application-using-visual-studio-2017"></a><span data-ttu-id="674cb-103">Ladění aplikace Hello World v jazyce C# nebo Visual Basic .NET Core pomocí sady Visual Studio 2017</span><span class="sxs-lookup"><span data-stu-id="674cb-103">Debug your C# or Visual Basic .NET Core Hello World application using Visual Studio 2017</span></span>

<span data-ttu-id="674cb-104">Zatím jste postupovali podle kroků v části [vytvoření C# Hello World aplikace pomocí .NET Core v aplikaci Visual Studio 2017](with-visual-studio.md) nebo [sestavení Visual Basic Hello World aplikaci pomocí .NET core v aplikaci Visual Studio 2017](vb-with-visual-studio.md) pro vytvoření a spuštění jednoduché konzolové aplikace.</span><span class="sxs-lookup"><span data-stu-id="674cb-104">So far, you've followed the steps in [Build a C# Hello World Application with .NET Core in Visual Studio 2017](with-visual-studio.md) or [Build a Visual Basic Hello World Application with .NET Core in Visual Studio 2017](vb-with-visual-studio.md) to create and run a simple console application.</span></span> <span data-ttu-id="674cb-105">Jakmile napíšete a zkompilujete aplikaci, můžete ji začít testovat.</span><span class="sxs-lookup"><span data-stu-id="674cb-105">Once you've written and compiled your application, you can begin testing it.</span></span> <span data-ttu-id="674cb-106">Sada Visual Studio obsahuje komplexní sadu ladicích nástrojů, které můžete použít při testování a odstraňování potíží s aplikací.</span><span class="sxs-lookup"><span data-stu-id="674cb-106">Visual Studio includes a comprehensive set of debugging tools that you can use when testing and troubleshooting your application.</span></span> 

## <a name="debugging-in-debug-mode"></a><span data-ttu-id="674cb-107">Ladění v režimu ladění</span><span class="sxs-lookup"><span data-stu-id="674cb-107">Debugging in Debug mode</span></span>

<span data-ttu-id="674cb-108">*Ladění* a *vydání* jsou dvě z výchozích konfigurací sestavení sady Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="674cb-108">*Debug* and *Release* are two of Visual Studio's default build configurations.</span></span> <span data-ttu-id="674cb-109">Aktuální konfigurace sestavení se zobrazí na panelu nástrojů.</span><span class="sxs-lookup"><span data-stu-id="674cb-109">The current build configuration is shown on the toolbar.</span></span> <span data-ttu-id="674cb-110">Následující obrázek na panelu nástrojů ukazuje, že je aplikace Visual Studio nakonfigurována pro kompilaci aplikace v režimu **ladění** .</span><span class="sxs-lookup"><span data-stu-id="674cb-110">The following toolbar image shows that Visual Studio is configured to compile your application in **Debug** mode.</span></span>

   ![výchozí panel nástrojů sady Visual Studio se zvýrazněným laděním](./media/debugging-with-visual-studio/visual-studio-toolbar-debug.png)

<span data-ttu-id="674cb-112">Vždy byste měli začít testováním programu v režimu ladění.</span><span class="sxs-lookup"><span data-stu-id="674cb-112">You should always begin by testing your program in Debug mode.</span></span> <span data-ttu-id="674cb-113">Režim ladění vypne většinu optimalizací kompilátoru a poskytuje bohatší informace během procesu sestavení.</span><span class="sxs-lookup"><span data-stu-id="674cb-113">Debug mode turns off most compiler optimizations and provides richer information during the build process.</span></span>

## <a name="setting-a-breakpoint"></a><span data-ttu-id="674cb-114">Nastavením zarážky</span><span class="sxs-lookup"><span data-stu-id="674cb-114">Setting a breakpoint</span></span>

<span data-ttu-id="674cb-115">Spusťte program v režimu ladění a vyzkoušejte několik funkcí ladění:</span><span class="sxs-lookup"><span data-stu-id="674cb-115">Run your program in Debug mode and try a few debugging features:</span></span>

<!-- markdownlint-disable MD025 -->

# <a name="ctabcsharp"></a>[<span data-ttu-id="674cb-116">C#</span><span class="sxs-lookup"><span data-stu-id="674cb-116">C#</span></span>](#tab/csharp)

1. <span data-ttu-id="674cb-117">*Zarážka* dočasně přerušuje provádění aplikace *před* provedením řádku se zarážkou.</span><span class="sxs-lookup"><span data-stu-id="674cb-117">A *breakpoint* temporarily interrupts the execution of the application *before* the line with the breakpoint is executed.</span></span> 

   <span data-ttu-id="674cb-118">Nastavte zarážku na řádku, který čte `Console.WriteLine($"\nHello, {name}, on {date:d} at {date:t}!");` kliknutím na levý okraj okna kódu na daném řádku, nebo výběrem položky nabídky **ladit** > **přepínací zarážku** s vybraným řádkem.</span><span class="sxs-lookup"><span data-stu-id="674cb-118">Set a breakpoint on the line that reads `Console.WriteLine($"\nHello, {name}, on {date:d} at {date:t}!");` by clicking in the left margin of the code window on that line or by choosing the **Debug** > **Toggle Breakpoint** menu item with the line selected.</span></span> <span data-ttu-id="674cb-119">Jak ukazuje následující obrázek, sada Visual Studio označuje řádek, na kterém je zarážka nastavena, zvýrazněním a zobrazením červeného kruhu v jeho levém okraji.</span><span class="sxs-lookup"><span data-stu-id="674cb-119">As the following figure shows, Visual Studio indicates the line on which the breakpoint is set by highlighting it and displaying a red circle in its left margin.</span></span>

   ![Okno programu Visual Studio se sadou zarážek](./media/debugging-with-visual-studio/set-breakpoint-in-editor.png)

1. <span data-ttu-id="674cb-121">Spusťte program v režimu ladění tak, že vyberete tlačítko **HelloWorld** se zelenou šipkou na panelu nástrojů, stisknete klávesu F5 nebo zvolíte **ladění** > **Spustit ladění**.</span><span class="sxs-lookup"><span data-stu-id="674cb-121">Run the program in Debug mode by selecting the **HelloWorld** button with the green arrow on the toolbar, pressing F5, or choosing **Debug** > **Start Debugging**.</span></span>

1. <span data-ttu-id="674cb-122">Pokud se program vyzve k zadání názvu a stisknete klávesu ENTER, zadejte v okně konzoly řetězec.</span><span class="sxs-lookup"><span data-stu-id="674cb-122">Enter a string in the console window when the program prompts for a name and press Enter.</span></span>

1. <span data-ttu-id="674cb-123">Spuštění programu se zastaví, když dosáhne zarážky a před `Console.WriteLine` tím, než se metoda spustí.</span><span class="sxs-lookup"><span data-stu-id="674cb-123">Program execution stops when it reaches the breakpoint and before the `Console.WriteLine` method executes.</span></span> <span data-ttu-id="674cb-124">Okno **Automatické** hodnoty zobrazuje hodnoty proměnných, které se používají kolem aktuálního řádku.</span><span class="sxs-lookup"><span data-stu-id="674cb-124">The **Autos** window displays the values of variables that are used around the current line.</span></span> <span data-ttu-id="674cb-125">Okno **místních** hodnot (které můžete zobrazit kliknutím na kartu **místní** hodnoty) zobrazí hodnoty proměnných, které jsou definovány v aktuálně vykonávané metodě.</span><span class="sxs-lookup"><span data-stu-id="674cb-125">The **Locals** window (which you can view by clicking the **Locals** tab) displays the values of variables that are defined in the currently executing method.</span></span>

   ![Snímek obrazovky se zarážkou v aplikaci Visual Studio.](./media/debugging-with-visual-studio/breakpoint-console-window.png)

1. <span data-ttu-id="674cb-127">Můžete změnit hodnotu proměnných, abyste viděli, jak ovlivní váš program.</span><span class="sxs-lookup"><span data-stu-id="674cb-127">You can change the value of the variables to see how it affects your program.</span></span> <span data-ttu-id="674cb-128">Pokud **okno okamžité** není viditelné, zobrazte ho výběrem**položky nabídky** **ladit** > **Windows** > hned.</span><span class="sxs-lookup"><span data-stu-id="674cb-128">If the **Immediate Window** is not visible, display it by choosing the **Debug** > **Windows** > **Immediate** menu item.</span></span> <span data-ttu-id="674cb-129">**Okamžité okno** vám umožní pracovat s aplikací, kterou ladíte.</span><span class="sxs-lookup"><span data-stu-id="674cb-129">The **Immediate Window** lets you interact with the application you're debugging.</span></span>

1. <span data-ttu-id="674cb-130">Hodnoty proměnných můžete interaktivně měnit.</span><span class="sxs-lookup"><span data-stu-id="674cb-130">You can interactively change the values of variables.</span></span> <span data-ttu-id="674cb-131">Do `name = "Gracie"` příkazového **podokna** zadejte a stiskněte klávesu ENTER.</span><span class="sxs-lookup"><span data-stu-id="674cb-131">Enter `name = "Gracie"` in the **Immediate Window** and press the Enter key.</span></span>

1. <span data-ttu-id="674cb-132">Do `date = new DateTime(2016,11,01,11,59,00)` příkazového **podokna** zadejte a stiskněte klávesu ENTER.</span><span class="sxs-lookup"><span data-stu-id="674cb-132">Enter `date = new DateTime(2016,11,01,11,59,00)` in the **Immediate Window** and press the Enter key.</span></span>

   <span data-ttu-id="674cb-133">V **příkazovém okně** se zobrazí hodnota proměnné řetězce a vlastnosti <xref:System.DateTime> hodnoty.</span><span class="sxs-lookup"><span data-stu-id="674cb-133">The **Immediate Window** displays the value of the string variable and the properties of the <xref:System.DateTime> value.</span></span> <span data-ttu-id="674cb-134">Kromě toho je hodnota proměnných aktualizována v oknech **Automatické** hodnoty a **místní** hodnoty.</span><span class="sxs-lookup"><span data-stu-id="674cb-134">In addition, the value of the variables is updated in the **Autos** and **Locals** windows.</span></span>

   ![Okno Automatické hodnoty a příkazové okno](./media/debugging-with-visual-studio/autos-immediate-window.png)

1. <span data-ttu-id="674cb-136">Pokračujte v provádění programu tak, že na panelu nástrojů kliknete na tlačítko **pokračovat** nebo vyberete položku nabídky **ladit** > **pokračovat** .</span><span class="sxs-lookup"><span data-stu-id="674cb-136">Continue program execution by selecting the **Continue** button in the toolbar or by selecting the **Debug** > **Continue** menu item.</span></span> <span data-ttu-id="674cb-137">Hodnoty zobrazené v okně konzoly odpovídají změnám, které jste provedli v **příkazovém okně**.</span><span class="sxs-lookup"><span data-stu-id="674cb-137">The values displayed in the console window correspond to the changes you made in the **Immediate Window**.</span></span>

   ![Okno konzoly zobrazující hodnotu zdířka v poli jaký je vaše jméno? výzva následovaná Gracie Hello](./media/debugging-with-visual-studio/debug-changed-value.png)

1. <span data-ttu-id="674cb-139">Stisknutím libovolné klávesy ukončete aplikaci a ukončete režim ladění.</span><span class="sxs-lookup"><span data-stu-id="674cb-139">Press any key to exit the application and end Debug mode.</span></span>

# <a name="visual-basictabvb"></a>[<span data-ttu-id="674cb-140">Visual Basic</span><span class="sxs-lookup"><span data-stu-id="674cb-140">Visual Basic</span></span>](#tab/vb)

1. <span data-ttu-id="674cb-141">*Zarážka* dočasně přerušuje provádění aplikace *před* provedením řádku se zarážkou.</span><span class="sxs-lookup"><span data-stu-id="674cb-141">A *breakpoint* temporarily interrupts the execution of the application *before* the line with the breakpoint is executed.</span></span> 

   <span data-ttu-id="674cb-142">Nastavte zarážku na řádku, který čte `Console.WriteLine(vbCrLf + $"Hello, {name}, on {currentDate:d} at {currentDate:t}!")` kliknutím na levý okraj okna kódu na daném řádku, nebo výběrem položky nabídky **ladit** > **přepínací zarážku** s vybraným řádkem.</span><span class="sxs-lookup"><span data-stu-id="674cb-142">Set a breakpoint on the line that reads `Console.WriteLine(vbCrLf + $"Hello, {name}, on {currentDate:d} at {currentDate:t}!")` by clicking in the left margin of the code window on that line or by choosing the **Debug** > **Toggle Breakpoint** menu item with the line selected.</span></span> <span data-ttu-id="674cb-143">Jak ukazuje následující obrázek, sada Visual Studio označuje řádek, na kterém je zarážka nastavena, zvýrazněním a zobrazením červeného kruhu v jeho levém okraji.</span><span class="sxs-lookup"><span data-stu-id="674cb-143">As the following figure shows, Visual Studio indicates the line on which the breakpoint is set by highlighting it and displaying a red circle in its left margin.</span></span>

   ![Okno programu Visual Studio se sadou zarážek](./media/debugging-with-visual-studio/vb-set-breakpoint-in-editor.png)

1. <span data-ttu-id="674cb-145">Spusťte program v režimu ladění tak, že vyberete tlačítko **HelloWorld** se zelenou šipkou na panelu nástrojů, stisknete klávesu F5 nebo zvolíte **ladění** > **Spustit ladění**.</span><span class="sxs-lookup"><span data-stu-id="674cb-145">Run the program in Debug mode by selecting the **HelloWorld** button with the green arrow on the toolbar, pressing F5, or choosing **Debug** > **Start Debugging**.</span></span>

1. <span data-ttu-id="674cb-146">Pokud se program vyzve k zadání názvu a stisknete klávesu ENTER, zadejte v okně konzoly řetězec.</span><span class="sxs-lookup"><span data-stu-id="674cb-146">Enter a string in the console window when the program prompts for a name and press Enter.</span></span>

1. <span data-ttu-id="674cb-147">Spuštění programu se zastaví, když dosáhne zarážky a před `Console.WriteLine` tím, než se metoda spustí.</span><span class="sxs-lookup"><span data-stu-id="674cb-147">Program execution stops when it reaches the breakpoint and before the `Console.WriteLine` method executes.</span></span> <span data-ttu-id="674cb-148">Okno **Automatické** hodnoty zobrazuje hodnoty proměnných, které se používají kolem aktuálního řádku.</span><span class="sxs-lookup"><span data-stu-id="674cb-148">The **Autos** window displays the values of variables that are used around the current line.</span></span> <span data-ttu-id="674cb-149">Okno **místních** hodnot (které můžete zobrazit kliknutím na kartu **místní** hodnoty) zobrazí hodnoty proměnných, které jsou definovány v aktuálně vykonávané metodě.</span><span class="sxs-lookup"><span data-stu-id="674cb-149">The **Locals** window (which you can view by clicking the **Locals** tab) displays the values of variables that are defined in the currently executing method.</span></span>

   ![Okno aplikace Visual Studio na zarážce](./media/debugging-with-visual-studio/vb-stop-at-breakpoint.png)

1. <span data-ttu-id="674cb-151">Můžete změnit hodnotu proměnných, abyste viděli, jak ovlivní váš program.</span><span class="sxs-lookup"><span data-stu-id="674cb-151">You can change the value of the variables to see how it affects your program.</span></span> <span data-ttu-id="674cb-152">Pokud **okno okamžité** není viditelné, zobrazte ho výběrem**položky nabídky** **ladit** > **Windows** > hned.</span><span class="sxs-lookup"><span data-stu-id="674cb-152">If the **Immediate Window** is not visible, display it by choosing the **Debug** > **Windows** > **Immediate** menu item.</span></span> <span data-ttu-id="674cb-153">**Okamžité okno** vám umožní pracovat s aplikací, kterou ladíte.</span><span class="sxs-lookup"><span data-stu-id="674cb-153">The **Immediate Window** lets you interact with the application you're debugging.</span></span>

1. <span data-ttu-id="674cb-154">Hodnoty proměnných můžete interaktivně měnit.</span><span class="sxs-lookup"><span data-stu-id="674cb-154">You can interactively change the values of variables.</span></span> <span data-ttu-id="674cb-155">Do `name = "Gracie"` příkazového **podokna** zadejte a stiskněte klávesu ENTER.</span><span class="sxs-lookup"><span data-stu-id="674cb-155">Enter `name = "Gracie"` in the **Immediate Window** and press the Enter key.</span></span>

1. <span data-ttu-id="674cb-156">Do `currentDate = new DateTime(2016,11,01,11,59,00)` příkazového **podokna** zadejte a stiskněte klávesu ENTER.</span><span class="sxs-lookup"><span data-stu-id="674cb-156">Enter `currentDate = new DateTime(2016,11,01,11,59,00)` in the **Immediate Window** and press the Enter key.</span></span>

1. <span data-ttu-id="674cb-157">Pokračujte v provádění programu tak, že na panelu nástrojů kliknete na tlačítko **pokračovat** nebo vyberete položku nabídky **ladit** > **pokračovat** .</span><span class="sxs-lookup"><span data-stu-id="674cb-157">Continue program execution by selecting the **Continue** button in the toolbar or by selecting the **Debug** > **Continue** menu item.</span></span> <span data-ttu-id="674cb-158">Hodnoty zobrazené v okně konzoly odpovídají změnám, které jste provedli v **příkazovém okně**.</span><span class="sxs-lookup"><span data-stu-id="674cb-158">The values displayed in the console window correspond to the changes you made in the **Immediate Window**.</span></span>

   ![Okno konzoly zobrazující změněné hodnoty zadané v příkazovém okně](./media/debugging-with-visual-studio/debug-changed-value.png)

1. <span data-ttu-id="674cb-160">Stisknutím libovolné klávesy ukončete aplikaci a ukončete režim ladění.</span><span class="sxs-lookup"><span data-stu-id="674cb-160">Press any key to exit the application and end Debug mode.</span></span>

---

## <a name="setting-a-conditional-breakpoint"></a><span data-ttu-id="674cb-161">Nastavení podmíněné zarážky</span><span class="sxs-lookup"><span data-stu-id="674cb-161">Setting a conditional breakpoint</span></span>

<span data-ttu-id="674cb-162">Program zobrazí řetězec, který uživatel zadá.</span><span class="sxs-lookup"><span data-stu-id="674cb-162">Your program displays the string that the user enters.</span></span> <span data-ttu-id="674cb-163">Co se stane, když uživatel nezadá něco?</span><span class="sxs-lookup"><span data-stu-id="674cb-163">What happens if the user doesn't enter anything?</span></span> <span data-ttu-id="674cb-164">Můžete ji otestovat s užitečnou funkcí ladění, *podmíněná zarážka*, která přerušuje provádění programu, pokud je splněna jedna nebo více podmínek.</span><span class="sxs-lookup"><span data-stu-id="674cb-164">You can test this with a useful debugging feature, the *conditional breakpoint*, which breaks program execution when one or more conditions are met.</span></span>

<span data-ttu-id="674cb-165">Chcete-li nastavit podmíněný bod přerušení a otestovat, co se stane, když uživatel nezadá řetězec, udělejte toto:</span><span class="sxs-lookup"><span data-stu-id="674cb-165">To set a conditional breakpoint and test what happens when the user fails to enter a string, do the following:</span></span>

# <a name="ctabcsharp"></a>[<span data-ttu-id="674cb-166">C#</span><span class="sxs-lookup"><span data-stu-id="674cb-166">C#</span></span>](#tab/csharp)

1. <span data-ttu-id="674cb-167">Pravým tlačítkem myši klikněte na červenou tečku, která představuje zarážku.</span><span class="sxs-lookup"><span data-stu-id="674cb-167">Right-click on the red dot that represents the breakpoint.</span></span> <span data-ttu-id="674cb-168">V místní nabídce vyberte **podmínky** a otevřete tak dialogové okno **Nastavení zarážky** .</span><span class="sxs-lookup"><span data-stu-id="674cb-168">On the context menu, select **Conditions** to open the **Breakpoint Settings** dialog.</span></span> <span data-ttu-id="674cb-169">Zaškrtněte políčko pro **podmínky**.</span><span class="sxs-lookup"><span data-stu-id="674cb-169">Check the box for **Conditions**.</span></span>

   ![Editor znázorňující panel nastavení zarážky –C#](./media/debugging-with-visual-studio/breakpoint-settings.png)

1. <span data-ttu-id="674cb-171">Pro **podmíněný výraz** nahraďte "např. x = = 5" následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="674cb-171">For the **Conditional Expression** replace "e.g. x == 5" with the following:</span></span>

   ```csharp
   String.IsNullOrEmpty(name)
   ```

   <span data-ttu-id="674cb-172">Testujete podmínku kódu, že `String.IsNullOrEmpty(name)` volání metody je `true` buď na to, že *název* není přiřazený nebo že má hodnotu prázdný řetězec ("").</span><span class="sxs-lookup"><span data-stu-id="674cb-172">You're testing for a code condition, that the `String.IsNullOrEmpty(name)` method call is `true` either because *name* has not been assigned a value or because its value is an empty string ("").</span></span> <span data-ttu-id="674cb-173">Můžete také zadat *počet*průchodů, který přerušuje spuštění programu před provedením určitého příkazu, nebo *podmínku filtru*, která přerušuje spuštění programu na základě takových atributů jako identifikátor vlákna, proces název nebo název vlákna.</span><span class="sxs-lookup"><span data-stu-id="674cb-173">You can also specify a *hit count*, which interrupts program execution before a statement is executed a specified number of times, or a *filter condition*, which interrupts program execution based on such attributes as a thread identifier, process name, or thread name.</span></span>

1. <span data-ttu-id="674cb-174">Kliknutím na tlačítko **Zavřít** zavřete dialogové okno.</span><span class="sxs-lookup"><span data-stu-id="674cb-174">Select the **Close** button to close the dialog.</span></span>

1. <span data-ttu-id="674cb-175">Spusťte program v režimu ladění.</span><span class="sxs-lookup"><span data-stu-id="674cb-175">Run the program in Debug mode.</span></span>

1. <span data-ttu-id="674cb-176">Po zobrazení výzvy k zadání názvu v okně konzoly stiskněte klávesu ENTER.</span><span class="sxs-lookup"><span data-stu-id="674cb-176">In the console window, press the Enter key when prompted to enter your name.</span></span>

1. <span data-ttu-id="674cb-177">Vzhledem k tomu, že podmínka `name` , kterou `null` jsme <xref:System.String.Empty?displayProperty=nameWithType>určili, je nebo, byla splněna, spuštění programu se zastaví, když `Console.WriteLine` dosáhne zarážky a předtím, než se metoda spustí.</span><span class="sxs-lookup"><span data-stu-id="674cb-177">Because the condition we specified, `name` is either `null` or <xref:System.String.Empty?displayProperty=nameWithType>, has been satisfied, program execution stops when it reaches the breakpoint and before the `Console.WriteLine` method executes.</span></span>

1. <span data-ttu-id="674cb-178">Vyberte okno **místních** hodnot, které zobrazuje hodnoty proměnných, které jsou lokální pro právě spuštěnou metodu, což je `Main` metoda v programu.</span><span class="sxs-lookup"><span data-stu-id="674cb-178">Select the **Locals** window, which shows the values of variables that are local to the currently executing method, which is the `Main` method in your program.</span></span> <span data-ttu-id="674cb-179">Všimněte si, že hodnota `name` proměnné je `""`nebo <xref:System.String.Empty?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="674cb-179">Observe that the value of the `name` variable is `""`, or <xref:System.String.Empty?displayProperty=nameWithType>.</span></span>

1. <span data-ttu-id="674cb-180">Potvrďte, že hodnota je prázdný řetězec zadáním následujícího příkazu v **příkazovém okně**.</span><span class="sxs-lookup"><span data-stu-id="674cb-180">Confirm the value is an empty string by entering the following statement in the **Immediate Window**.</span></span> <span data-ttu-id="674cb-181">Výsledek je `true`.</span><span class="sxs-lookup"><span data-stu-id="674cb-181">The result is `true`.</span></span>

   ```csharp
   ? name == String.Empty
   ```

   ![Okamžité okno vracející hodnotu true po provedení příkazu –C#](./media/debugging-with-visual-studio/immediate-window-output.png)

1. <span data-ttu-id="674cb-183">Kliknutím na tlačítko **pokračovat** na panelu nástrojů pokračujte v provádění programu.</span><span class="sxs-lookup"><span data-stu-id="674cb-183">Select the **Continue** button on the toolbar to continue program execution.</span></span>

1. <span data-ttu-id="674cb-184">Stisknutím libovolné klávesy zavřete okno konzoly a ukončete režim ladění.</span><span class="sxs-lookup"><span data-stu-id="674cb-184">Press any key to close the console window and exit Debug mode.</span></span>

1. <span data-ttu-id="674cb-185">Vymažte zarážku kliknutím na tečka v levém okraji okna kódu nebo výběrem položky nabídky **ladění > Přepnout zarážku** s vybraným řádkem.</span><span class="sxs-lookup"><span data-stu-id="674cb-185">Clear the breakpoint by clicking on the dot in the left margin of the code window or by choosing the **Debug > Toggle Breakpoint** menu item with the row selected.</span></span>

# <a name="visual-basictabvb"></a>[<span data-ttu-id="674cb-186">Visual Basic</span><span class="sxs-lookup"><span data-stu-id="674cb-186">Visual Basic</span></span>](#tab/vb)

1. <span data-ttu-id="674cb-187">Pravým tlačítkem myši klikněte na červenou tečku, která představuje zarážku.</span><span class="sxs-lookup"><span data-stu-id="674cb-187">Right-click on the red dot that represents the breakpoint.</span></span> <span data-ttu-id="674cb-188">V místní nabídce vyberte **podmínky** a otevřete tak dialogové okno **Nastavení zarážky** .</span><span class="sxs-lookup"><span data-stu-id="674cb-188">On the context menu, select **Conditions** to open the **Breakpoint Settings** dialog.</span></span> <span data-ttu-id="674cb-189">Zaškrtněte políčko pro **podmínky**.</span><span class="sxs-lookup"><span data-stu-id="674cb-189">Check the box for **Conditions**.</span></span>

   ![Editor znázorňující panel nastavení zarážky – Visual Basic](./media/debugging-with-visual-studio/vb-breakpointsettings.png)

1. <span data-ttu-id="674cb-191">Pro **podmíněný výraz** nahraďte "např. x = 5" následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="674cb-191">For the **Conditional Expression** replace "e.g. x = 5" with the following:</span></span>

   ```vb
   String.IsNullOrEmpty(name)
   ```

   <span data-ttu-id="674cb-192">Testujete podmínku kódu, že `String.IsNullOrEmpty(name)` volání metody je `True` buď na to, že *název* není přiřazený nebo že má hodnotu prázdný řetězec ("").</span><span class="sxs-lookup"><span data-stu-id="674cb-192">You're testing for a code condition, that the `String.IsNullOrEmpty(name)` method call is `True` either because *name* has not been assigned a value or because its value is an empty string ("").</span></span> <span data-ttu-id="674cb-193">Můžete také zadat *počet*průchodů, který přerušuje spuštění programu před provedením určitého příkazu, nebo *podmínku filtru*, která přerušuje spuštění programu na základě takových atributů jako identifikátor vlákna, proces název nebo název vlákna.</span><span class="sxs-lookup"><span data-stu-id="674cb-193">You can also specify a *hit count*, which interrupts program execution before a statement is executed a specified number of times, or a *filter condition*, which interrupts program execution based on such attributes as a thread identifier, process name, or thread name.</span></span>

1. <span data-ttu-id="674cb-194">Kliknutím na tlačítko **Zavřít** zavřete dialogové okno.</span><span class="sxs-lookup"><span data-stu-id="674cb-194">Select the **Close** button to close the dialog.</span></span>

1. <span data-ttu-id="674cb-195">Spusťte program v režimu ladění.</span><span class="sxs-lookup"><span data-stu-id="674cb-195">Run the program in Debug mode.</span></span>

1. <span data-ttu-id="674cb-196">Po zobrazení výzvy k zadání názvu v okně konzoly stiskněte klávesu ENTER.</span><span class="sxs-lookup"><span data-stu-id="674cb-196">In the console window, press the Enter key when prompted to enter your name.</span></span>

1. <span data-ttu-id="674cb-197">Vzhledem k tomu, že podmínka `name` , kterou `null` jsme <xref:System.String.Empty?displayProperty=nameWithType>určili, je nebo, byla splněna, spuštění programu se zastaví, když `Console.WriteLine` dosáhne zarážky a předtím, než se metoda spustí.</span><span class="sxs-lookup"><span data-stu-id="674cb-197">Because the condition we specified, `name` is either `null` or <xref:System.String.Empty?displayProperty=nameWithType>, has been satisfied, program execution stops when it reaches the breakpoint and before the `Console.WriteLine` method executes.</span></span>

1. <span data-ttu-id="674cb-198">Vyberte okno **místních** hodnot, které zobrazuje hodnoty proměnných, které jsou lokální pro právě spuštěnou metodu, což je `Main` metoda v programu.</span><span class="sxs-lookup"><span data-stu-id="674cb-198">Select the **Locals** window, which shows the values of variables that are local to the currently executing method, which is the `Main` method in your program.</span></span> <span data-ttu-id="674cb-199">Všimněte si, že hodnota `name` proměnné je `""`nebo <xref:System.String.Empty?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="674cb-199">Observe that the value of the `name` variable is `""`, or <xref:System.String.Empty?displayProperty=nameWithType>.</span></span>

1. <span data-ttu-id="674cb-200">Potvrďte, že hodnota je prázdný řetězec zadáním následujícího příkazu v **příkazovém okně**.</span><span class="sxs-lookup"><span data-stu-id="674cb-200">Confirm the value is an empty string by entering the following statement in the **Immediate Window**.</span></span> <span data-ttu-id="674cb-201">Výsledek je `true`.</span><span class="sxs-lookup"><span data-stu-id="674cb-201">The result is `true`.</span></span>

   ```vb
   ? String.IsNullOrEmpty(name)
   ```

  ![Okamžité okno vracející hodnotu true po provedení příkazu – Visual Basic](./media/debugging-with-visual-studio/vb-immediate-window-output.png)

1. <span data-ttu-id="674cb-203">Kliknutím na tlačítko **pokračovat** na panelu nástrojů pokračujte v provádění programu.</span><span class="sxs-lookup"><span data-stu-id="674cb-203">Select the **Continue** button on the toolbar to continue program execution.</span></span>

1. <span data-ttu-id="674cb-204">Stisknutím libovolné klávesy zavřete okno konzoly a ukončete režim ladění.</span><span class="sxs-lookup"><span data-stu-id="674cb-204">Press any key to close the console window and exit Debug mode.</span></span>

1. <span data-ttu-id="674cb-205">Vymažte zarážku kliknutím na tečka v levém okraji okna kódu nebo výběrem položky nabídky **ladění > Přepnout zarážku** s vybraným řádkem.</span><span class="sxs-lookup"><span data-stu-id="674cb-205">Clear the breakpoint by clicking on the dot in the left margin of the code window or by choosing the **Debug > Toggle Breakpoint** menu item with the row selected.</span></span>

---
## <a name="stepping-through-a-program"></a><span data-ttu-id="674cb-206">Krokování přes program</span><span class="sxs-lookup"><span data-stu-id="674cb-206">Stepping through a program</span></span>

<span data-ttu-id="674cb-207">Visual Studio také umožňuje krokovat řádek po řádku programu a monitorovat jeho spuštění.</span><span class="sxs-lookup"><span data-stu-id="674cb-207">Visual Studio also allows you to step line by line through a program and monitor its execution.</span></span> <span data-ttu-id="674cb-208">Obvykle byste nastavili zarážku a pomocí této funkce mohli sledovat tok programu přes malou část kódu programu.</span><span class="sxs-lookup"><span data-stu-id="674cb-208">Ordinarily, you'd set a breakpoint and use this feature to follow program flow through a small part of your program code.</span></span> <span data-ttu-id="674cb-209">Vzhledem k tomu, že je váš program malý, můžete procházet celý program pomocí následujícího postupu:</span><span class="sxs-lookup"><span data-stu-id="674cb-209">Since your program is small, you can step through the entire program by doing the following:</span></span>

# <a name="ctabcsharp"></a>[<span data-ttu-id="674cb-210">C#</span><span class="sxs-lookup"><span data-stu-id="674cb-210">C#</span></span>](#tab/csharp)

1. <span data-ttu-id="674cb-211">Na panelu nabídek zvolte možnost **ladit** > **Krok do** nebo stiskněte klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-211">On the menu bar, choose **Debug** > **Step Into** or press the F11 key.</span></span> <span data-ttu-id="674cb-212">Visual Studio zvýrazní a zobrazí šipku vedle dalšího řádku provádění.</span><span class="sxs-lookup"><span data-stu-id="674cb-212">Visual Studio highlights and displays an arrow beside the next line of execution.</span></span>

   ![Krok v aplikaci Visual Studio – metoda –C#](./media/debugging-with-visual-studio/step-into-method.png)

   <span data-ttu-id="674cb-214">V tomto okamžiku okno **Automatické** hodnoty zobrazí, že program definoval pouze jednu proměnnou `args`.</span><span class="sxs-lookup"><span data-stu-id="674cb-214">At this point, the **Autos** window shows that your program has defined only one variable, `args`.</span></span> <span data-ttu-id="674cb-215">Vzhledem k tomu, že jste do programu neprošli žádné argumenty příkazového řádku, je jeho hodnota prázdným polem řetězce.</span><span class="sxs-lookup"><span data-stu-id="674cb-215">Because you haven't passed any command-line arguments to the program, its value is an empty string array.</span></span> <span data-ttu-id="674cb-216">Kromě toho Visual Studio otevřelo prázdné okno konzoly.</span><span class="sxs-lookup"><span data-stu-id="674cb-216">In addition, Visual Studio has opened a blank console window.</span></span>

1. <span data-ttu-id="674cb-217">Vyberte možnost **ladit** > **Krok do** nebo stiskněte klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-217">Select **Debug** > **Step Into** or press the F11 key.</span></span> <span data-ttu-id="674cb-218">Visual Studio teď zvýrazní další řádek provádění.</span><span class="sxs-lookup"><span data-stu-id="674cb-218">Visual Studio now highlights the next line of execution.</span></span> <span data-ttu-id="674cb-219">Jak ukazuje obrázek, převzal méně než jeden milisekund, aby spustil kód mezi posledním příkazem a tímto.</span><span class="sxs-lookup"><span data-stu-id="674cb-219">As the figure shows, it has taken less than one millisecond to execute the code between the last statement and this one.</span></span> <span data-ttu-id="674cb-220">`args`zůstane jedinou deklarovanou proměnnou a okno konzoly zůstane prázdné.</span><span class="sxs-lookup"><span data-stu-id="674cb-220">`args` remains the only declared variable, and the console window remains blank.</span></span>

   ![Krok sady Visual Studio ve zdroji metody –C#](./media/debugging-with-visual-studio/step-into-source-method.png)

1. <span data-ttu-id="674cb-222">Vyberte možnost **ladit** > **Krok do** nebo stiskněte klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-222">Select **Debug** > **Step Into** or press the F11 key.</span></span> <span data-ttu-id="674cb-223">Visual Studio zvýrazní příkaz, který obsahuje `name` přiřazení proměnné.</span><span class="sxs-lookup"><span data-stu-id="674cb-223">Visual Studio highlights the statement that includes the `name` variable assignment.</span></span> <span data-ttu-id="674cb-224">Okno **Automatické** hodnoty ukazuje, `name` `null`a okno konzoly zobrazuje řetězec "Co je vaše jméno?".</span><span class="sxs-lookup"><span data-stu-id="674cb-224">The **Autos** window shows that `name` is `null`, and the console window displays the string "What is your name?".</span></span>

1. <span data-ttu-id="674cb-225">Zadáním řetězce v okně konzoly a stisknutím klávesy ENTER odpovězte na výzvu.</span><span class="sxs-lookup"><span data-stu-id="674cb-225">Respond to the prompt by entering a string in the console window and pressing Enter.</span></span> <span data-ttu-id="674cb-226">Konzola nereaguje a řetězec, který zadáte, se v okně konzoly nezobrazí, ale <xref:System.Console.ReadLine%2A?displayProperty=nameWithType> metoda přesto zaznamená vstup.</span><span class="sxs-lookup"><span data-stu-id="674cb-226">The console is unresponsive, and the string you enter isn't displayed in the console window, but the <xref:System.Console.ReadLine%2A?displayProperty=nameWithType> method will nevertheless capture your input.</span></span>

1. <span data-ttu-id="674cb-227">Vyberte možnost **ladit** > **Krok do** nebo stiskněte klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-227">Select **Debug** > **Step Into** or press the F11 key.</span></span> <span data-ttu-id="674cb-228">Visual Studio zvýrazní příkaz, který obsahuje `date` přiřazení proměnné (in C#) `currentDate` nebo (in Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="674cb-228">Visual Studio highlights the statement that includes the `date` (in C#) or `currentDate` (in Visual Basic) variable assignment.</span></span> <span data-ttu-id="674cb-229">Okno **Automatické** hodnoty zobrazuje <xref:System.DateTime.Now?displayProperty=nameWithType> hodnotu vlastnosti a hodnotu vrácenou voláním <xref:System.Console.ReadLine%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="674cb-229">The **Autos** window shows the <xref:System.DateTime.Now?displayProperty=nameWithType> property value and the value returned by the call to the <xref:System.Console.ReadLine%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="674cb-230">V okně konzoly se zobrazí také řetězec zadaný při zobrazení výzvy ke vstupu v konzole nástroje.</span><span class="sxs-lookup"><span data-stu-id="674cb-230">The console window also displays the string entered when the console prompted for input.</span></span>

1. <span data-ttu-id="674cb-231">Vyberte možnost **ladit** > **Krok do** nebo stiskněte klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-231">Select **Debug** > **Step Into** or press the F11 key.</span></span> <span data-ttu-id="674cb-232">Okno **Automatické** hodnoty zobrazuje hodnotu `date` proměnné po přiřazení z <xref:System.DateTime.Now?displayProperty=nameWithType> vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="674cb-232">The **Autos** window shows the value of the `date` variable after the assignment from the <xref:System.DateTime.Now?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="674cb-233">Okno konzoly se nezměnilo.</span><span class="sxs-lookup"><span data-stu-id="674cb-233">The console window is unchanged.</span></span>

1. <span data-ttu-id="674cb-234">Vyberte možnost **ladit** > **Krok do** nebo stiskněte klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-234">Select **Debug** > **Step Into** or press the F11 key.</span></span> <span data-ttu-id="674cb-235">Visual Studio volá <xref:System.Console.WriteLine(System.String,System.Object,System.Object)?displayProperty=nameWithType> metodu.</span><span class="sxs-lookup"><span data-stu-id="674cb-235">Visual Studio calls the <xref:System.Console.WriteLine(System.String,System.Object,System.Object)?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="674cb-236">Hodnoty `date` (nebo `currentDate`) a `name` proměnné se zobrazí v okně **Automatické** hodnoty a v okně konzoly se zobrazí formátovaný řetězec.</span><span class="sxs-lookup"><span data-stu-id="674cb-236">The values of the `date` (or `currentDate`) and `name` variables appear in the **Autos** window, and the console window displays the formatted string.</span></span>

1. <span data-ttu-id="674cb-237">Vyberte **ladit** > **Krok ven** nebo stiskněte Shift a klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-237">Select **Debug** > **Step Out** or press Shift and the F11 key.</span></span> <span data-ttu-id="674cb-238">Tím se zastaví krok za krokem.</span><span class="sxs-lookup"><span data-stu-id="674cb-238">This stops step-by-step execution.</span></span> <span data-ttu-id="674cb-239">V okně konzoly se zobrazí zpráva a čeká na stisknutí klávesy.</span><span class="sxs-lookup"><span data-stu-id="674cb-239">The console window displays a message and waits for you to press a key.</span></span>

1. <span data-ttu-id="674cb-240">Stisknutím libovolné klávesy zavřete okno konzoly a ukončete režim ladění.</span><span class="sxs-lookup"><span data-stu-id="674cb-240">Press any key to close the console window and exit Debug mode.</span></span>

# <a name="visual-basictabvb"></a>[<span data-ttu-id="674cb-241">Visual Basic</span><span class="sxs-lookup"><span data-stu-id="674cb-241">Visual Basic</span></span>](#tab/vb)

1. <span data-ttu-id="674cb-242">Na panelu nabídek zvolte možnost **ladit** > **Krok do** nebo stiskněte klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-242">On the menu bar, choose **Debug** > **Step Into** or press the F11 key.</span></span> <span data-ttu-id="674cb-243">Visual Studio zvýrazní a zobrazí šipku vedle dalšího řádku provádění.</span><span class="sxs-lookup"><span data-stu-id="674cb-243">Visual Studio highlights and displays an arrow beside the next line of execution.</span></span>

   ![Metoda Visual Studio Step Into – Visual Basic](./media/debugging-with-visual-studio/vb-step-into-method.png)

   <span data-ttu-id="674cb-245">V tomto okamžiku, protože jste do programu neprošli žádné argumenty příkazového řádku, zobrazí okno **Automatické** hodnoty, že hodnota `args` proměnné je prázdné pole řetězce.</span><span class="sxs-lookup"><span data-stu-id="674cb-245">At this point, because you haven't passed any command-line arguments to the program, the **Autos** window shows that the value of the `args` variable is an empty string array.</span></span> <span data-ttu-id="674cb-246">Kromě toho Visual Studio otevřelo prázdné okno konzoly.</span><span class="sxs-lookup"><span data-stu-id="674cb-246">In addition, Visual Studio has opened a blank console window.</span></span>

1. <span data-ttu-id="674cb-247">Vyberte možnost **ladit** > **Krok do** nebo stiskněte klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-247">Select **Debug** > **Step Into** or press the F11 key.</span></span> <span data-ttu-id="674cb-248">Visual Studio teď zvýrazní další řádek provádění.</span><span class="sxs-lookup"><span data-stu-id="674cb-248">Visual Studio now highlights the next line of execution.</span></span> <span data-ttu-id="674cb-249">Jak ukazuje obrázek, převzal méně než jeden milisekund, aby spustil kód mezi posledním příkazem a tímto.</span><span class="sxs-lookup"><span data-stu-id="674cb-249">As the figure shows, it has taken less than one millisecond to execute the code between the last statement and this one.</span></span> <span data-ttu-id="674cb-250">`args`zůstane jedinou deklarovanou proměnnou a okno konzoly zůstane prázdné.</span><span class="sxs-lookup"><span data-stu-id="674cb-250">`args` remains the only declared variable, and the console window remains blank.</span></span>

   ![Krok do zdroje metody v aplikaci Visual Studio – Visual Basic](./media/debugging-with-visual-studio/vb-step-into-source-method.png)

1. <span data-ttu-id="674cb-252">Vyberte možnost **ladit** > **Krok do** nebo stiskněte klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-252">Select **Debug** > **Step Into** or press the F11 key.</span></span> <span data-ttu-id="674cb-253">Visual Studio zvýrazní příkaz, který obsahuje `name` přiřazení proměnné.</span><span class="sxs-lookup"><span data-stu-id="674cb-253">Visual Studio highlights the statement that includes the `name` variable assignment.</span></span> <span data-ttu-id="674cb-254">Okno **Automatické** hodnoty ukazuje, `name` `Nothing`a okno konzoly zobrazuje řetězec "Co je vaše jméno?".</span><span class="sxs-lookup"><span data-stu-id="674cb-254">The **Autos** window shows that `name` is `Nothing`, and the console window displays the string "What is your name?".</span></span>

1. <span data-ttu-id="674cb-255">Zadáním řetězce v okně konzoly a stisknutím klávesy ENTER odpovězte na výzvu.</span><span class="sxs-lookup"><span data-stu-id="674cb-255">Respond to the prompt by entering a string in the console window and pressing Enter.</span></span> <span data-ttu-id="674cb-256">Konzola nereaguje a řetězec, který zadáte, se v okně konzoly nezobrazí, ale <xref:System.Console.ReadLine%2A?displayProperty=nameWithType> metoda přesto zaznamená vstup.</span><span class="sxs-lookup"><span data-stu-id="674cb-256">The console is unresponsive, and the string you enter isn't displayed in the console window, but the <xref:System.Console.ReadLine%2A?displayProperty=nameWithType> method will nevertheless capture your input.</span></span>

1. <span data-ttu-id="674cb-257">Vyberte možnost **ladit** > **Krok do** nebo stiskněte klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-257">Select **Debug** > **Step Into** or press the F11 key.</span></span> <span data-ttu-id="674cb-258">Visual Studio zvýrazní příkaz, který obsahuje `date` přiřazení proměnné (in C#) `currentDate` nebo (in Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="674cb-258">Visual Studio highlights the statement that includes the `date` (in C#) or `currentDate` (in Visual Basic) variable assignment.</span></span> <span data-ttu-id="674cb-259">Okno **Automatické** hodnoty zobrazuje <xref:System.DateTime.Now?displayProperty=nameWithType> hodnotu vlastnosti a hodnotu vrácenou voláním <xref:System.Console.ReadLine%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="674cb-259">The **Autos** window shows the <xref:System.DateTime.Now?displayProperty=nameWithType> property value and the value returned by the call to the <xref:System.Console.ReadLine%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="674cb-260">V okně konzoly se zobrazí také řetězec zadaný při zobrazení výzvy ke vstupu v konzole nástroje.</span><span class="sxs-lookup"><span data-stu-id="674cb-260">The console window also displays the string entered when the console prompted for input.</span></span>

1. <span data-ttu-id="674cb-261">Vyberte možnost **ladit** > **Krok do** nebo stiskněte klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-261">Select **Debug** > **Step Into** or press the F11 key.</span></span> <span data-ttu-id="674cb-262">Okno **Automatické** hodnoty zobrazuje hodnotu `date` proměnné po přiřazení z <xref:System.DateTime.Now?displayProperty=nameWithType> vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="674cb-262">The **Autos** window shows the value of the `date` variable after the assignment from the <xref:System.DateTime.Now?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="674cb-263">Okno konzoly se nezměnilo.</span><span class="sxs-lookup"><span data-stu-id="674cb-263">The console window is unchanged.</span></span>

1. <span data-ttu-id="674cb-264">Vyberte možnost **ladit** > **Krok do** nebo stiskněte klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-264">Select **Debug** > **Step Into** or press the F11 key.</span></span> <span data-ttu-id="674cb-265">Visual Studio volá <xref:System.Console.WriteLine(System.String,System.Object,System.Object)?displayProperty=nameWithType> metodu.</span><span class="sxs-lookup"><span data-stu-id="674cb-265">Visual Studio calls the <xref:System.Console.WriteLine(System.String,System.Object,System.Object)?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="674cb-266">Hodnoty `date` (nebo `currentDate`) a `name` proměnné se zobrazí v okně **Automatické** hodnoty a v okně konzoly se zobrazí formátovaný řetězec.</span><span class="sxs-lookup"><span data-stu-id="674cb-266">The values of the `date` (or `currentDate`) and `name` variables appear in the **Autos** window, and the console window displays the formatted string.</span></span>

1. <span data-ttu-id="674cb-267">Vyberte **ladit** > **Krok ven** nebo stiskněte Shift a klávesu F11.</span><span class="sxs-lookup"><span data-stu-id="674cb-267">Select **Debug** > **Step Out** or press Shift and the F11 key.</span></span> <span data-ttu-id="674cb-268">Tím se zastaví krok za krokem.</span><span class="sxs-lookup"><span data-stu-id="674cb-268">This stops step-by-step execution.</span></span> <span data-ttu-id="674cb-269">V okně konzoly se zobrazí zpráva a čeká na stisknutí klávesy.</span><span class="sxs-lookup"><span data-stu-id="674cb-269">The console window displays a message and waits for you to press a key.</span></span>

1. <span data-ttu-id="674cb-270">Stisknutím libovolné klávesy zavřete okno konzoly a ukončete režim ladění.</span><span class="sxs-lookup"><span data-stu-id="674cb-270">Press any key to close the console window and exit Debug mode.</span></span>

---

## <a name="building-a-release-version"></a><span data-ttu-id="674cb-271">Sestavení verze pro vydání</span><span class="sxs-lookup"><span data-stu-id="674cb-271">Building a Release version</span></span>

<span data-ttu-id="674cb-272">Po otestování sestavení ladění aplikace byste měli také kompilovat a testovat verzi vydání.</span><span class="sxs-lookup"><span data-stu-id="674cb-272">Once you've tested the Debug build of your application, you should also compile and test the Release version.</span></span> <span data-ttu-id="674cb-273">Verze Release zahrnuje optimalizace kompilátoru, které mohou být někdy negativně ovlivněny chování aplikace.</span><span class="sxs-lookup"><span data-stu-id="674cb-273">The Release version incorporates compiler optimizations that can sometimes negatively affect the behavior of an application.</span></span> <span data-ttu-id="674cb-274">Například optimalizace kompilátoru, které jsou navrženy pro zlepšení výkonu, mohou vytvářet konflikty časování v asynchronních nebo vícevláknových aplikacích.</span><span class="sxs-lookup"><span data-stu-id="674cb-274">For example, compiler optimizations that are designed to improve performance can create race conditions in asynchronous or multithreaded applications.</span></span>

<span data-ttu-id="674cb-275">Chcete-li vytvořit a otestovat verzi pro vydání konzolové aplikace, změňte konfiguraci sestavení na panelu nástrojů z části **ladit** na **verzi**.</span><span class="sxs-lookup"><span data-stu-id="674cb-275">To build and test the Release version of your console application, change the build configuration on the toolbar from **Debug** to **Release**.</span></span>

![výchozí panel nástrojů sady Visual Studio se zvýrazněným laděním](./media/debugging-with-visual-studio/visual-studio-toolbar-release.png)

<span data-ttu-id="674cb-277">Když stisknete klávesu F5 nebo zvolíte **Sestavit řešení** z nabídky **sestavení** , Visual Studio zkompiluje verzi vydanou konzolovou aplikací.</span><span class="sxs-lookup"><span data-stu-id="674cb-277">When you press F5 or choose **Build Solution** from the **Build** menu, Visual Studio compiles the Release version of your console application.</span></span> <span data-ttu-id="674cb-278">Můžete ho otestovat stejným způsobem jako ladicí verze aplikace.</span><span class="sxs-lookup"><span data-stu-id="674cb-278">You can test it as you did the Debug version of the application.</span></span>

<span data-ttu-id="674cb-279">Po dokončení ladění aplikace je dalším krokem publikování nasaditelné verze vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="674cb-279">Once you've finished debugging your application, the next step is to publish a deployable version of your application.</span></span> <span data-ttu-id="674cb-280">Informace o tom, jak to provést, najdete v tématu [publikování Hello World aplikace pomocí sady Visual Studio 2017](publishing-with-visual-studio.md).</span><span class="sxs-lookup"><span data-stu-id="674cb-280">For information on how to do this, see [Publish the Hello World application with Visual Studio 2017](publishing-with-visual-studio.md).</span></span>
