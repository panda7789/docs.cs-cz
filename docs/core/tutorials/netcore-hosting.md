---
title: Zápis vlastního hostitele modulu runtime .NET Core
description: Naučte se hostovat modul runtime .NET Core z nativního kódu pro podporu pokročilých scénářů, které vyžadují řízení fungování modulu runtime .NET Core.
author: mjrousos
ms.date: 12/21/2018
ms.custom: seodec18
ms.openlocfilehash: b4d36d1ded3af1c6f1181712080e9fcd18a5a468
ms.sourcegitcommit: 30a558d23e3ac5a52071121a52c305c85fe15726
ms.translationtype: HT
ms.contentlocale: cs-CZ
ms.lasthandoff: 12/25/2019
ms.locfileid: "75339725"
---
# <a name="write-a-custom-net-core-host-to-control-the-net-runtime-from-your-native-code"></a><span data-ttu-id="fdde8-103">Zápis vlastního hostitele .NET Core pro řízení modulu .NET runtime z vašeho nativního kódu</span><span class="sxs-lookup"><span data-stu-id="fdde8-103">Write a custom .NET Core host to control the .NET runtime from your native code</span></span>

<span data-ttu-id="fdde8-104">Stejně jako všechny spravované kódy jsou aplikace .NET Core spouštěny hostitelem.</span><span class="sxs-lookup"><span data-stu-id="fdde8-104">Like all managed code, .NET Core applications are executed by a host.</span></span> <span data-ttu-id="fdde8-105">Hostitel zodpovídá za spuštění modulu runtime (včetně součástí, jako je například JIT a systém uvolňování paměti) a vyvolává spravované vstupní body.</span><span class="sxs-lookup"><span data-stu-id="fdde8-105">The host is responsible for starting the runtime (including components like the JIT and garbage collector) and invoking managed entry points.</span></span>

<span data-ttu-id="fdde8-106">Hostování modulu runtime .NET Core je pokročilý scénář a ve většině případů se vývojáři .NET Core nemusejí starat o hostování, protože procesy sestavení .NET Core poskytují výchozímu hostiteli spouštění aplikací .NET Core.</span><span class="sxs-lookup"><span data-stu-id="fdde8-106">Hosting the .NET Core runtime is an advanced scenario and, in most cases, .NET Core developers don't need to worry about hosting because .NET Core build processes provide a default host to run .NET Core applications.</span></span> <span data-ttu-id="fdde8-107">V některých specializovaných případech může být užitečné explicitně hostovat modul runtime .NET Core, a to buď jako způsob vyvolání spravovaného kódu v nativním procesu, nebo aby bylo možné získat větší kontrolu nad tím, jak funguje modul runtime.</span><span class="sxs-lookup"><span data-stu-id="fdde8-107">In some specialized circumstances, though, it can be useful to explicitly host the .NET Core runtime, either as a means of invoking managed code in a native process or in order to gain more control over how the runtime works.</span></span>

<span data-ttu-id="fdde8-108">Tento článek obsahuje přehled kroků nezbytných ke spuštění modulu runtime .NET Core z nativního kódu a spuštění spravovaného kódu.</span><span class="sxs-lookup"><span data-stu-id="fdde8-108">This article gives an overview of the steps necessary to start the .NET Core runtime from native code and execute managed code in it.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="fdde8-109">Požadavky</span><span class="sxs-lookup"><span data-stu-id="fdde8-109">Prerequisites</span></span>

<span data-ttu-id="fdde8-110">Vzhledem k tomu, že hostitelé jsou nativní aplikace, tento C++ kurz popisuje vytvoření aplikace pro hostování .NET Core.</span><span class="sxs-lookup"><span data-stu-id="fdde8-110">Because hosts are native applications, this tutorial covers constructing a C++ application to host .NET Core.</span></span> <span data-ttu-id="fdde8-111">Budete potřebovat C++ vývojové prostředí (například to, které poskytuje [Visual Studio](https://aka.ms/vsdownload?utm_source=mscom&utm_campaign=msdocs)).</span><span class="sxs-lookup"><span data-stu-id="fdde8-111">You will need a C++ development environment (such as that provided by [Visual Studio](https://aka.ms/vsdownload?utm_source=mscom&utm_campaign=msdocs)).</span></span>

<span data-ttu-id="fdde8-112">Budete také chtít, aby hostitel v rámci aplikace .NET Core otestoval hostitele. proto byste měli nainstalovat [.NET Core SDK](https://dotnet.microsoft.com/download) a [vytvořit malou testovací aplikaci .NET Core](with-visual-studio.md) (například aplikaci Hello World).</span><span class="sxs-lookup"><span data-stu-id="fdde8-112">You will also want a simple .NET Core application to test the host with, so you should install the [.NET Core SDK](https://dotnet.microsoft.com/download) and [build a small .NET Core test app](with-visual-studio.md) (such as a 'Hello World' app).</span></span> <span data-ttu-id="fdde8-113">Je dostačující aplikace Hello World vytvořená novou šablonou projektu konzoly .NET Core.</span><span class="sxs-lookup"><span data-stu-id="fdde8-113">The 'Hello World' app created by the new .NET Core console project template is sufficient.</span></span>

## <a name="hosting-apis"></a><span data-ttu-id="fdde8-114">Hostování rozhraní API</span><span class="sxs-lookup"><span data-stu-id="fdde8-114">Hosting APIs</span></span>
<span data-ttu-id="fdde8-115">Existují tři různá rozhraní API, která lze použít k hostování .NET Core.</span><span class="sxs-lookup"><span data-stu-id="fdde8-115">There are three different APIs that can be used to host .NET Core.</span></span> <span data-ttu-id="fdde8-116">Tento článek (a jeho přidružené [ukázky](https://github.com/dotnet/samples/tree/master/core/hosting)) se zabývá všemi možnostmi.</span><span class="sxs-lookup"><span data-stu-id="fdde8-116">This article (and its associated [samples](https://github.com/dotnet/samples/tree/master/core/hosting)) covers all options.</span></span>

* <span data-ttu-id="fdde8-117">Upřednostňovanou metodou hostování modulu runtime .NET Core v rozhraní .NET Core 3,0 a vyšším je rozhraní API `nethost` a `hostfxr` knihovny.</span><span class="sxs-lookup"><span data-stu-id="fdde8-117">The preferred method of hosting the .NET Core runtime in .NET Core 3.0 and above is with the `nethost` and `hostfxr` libraries' APIs.</span></span> <span data-ttu-id="fdde8-118">Tyto vstupní body zpracovávají složitost hledání a nastavení modulu runtime pro inicializaci a umožňují spouštění spravované aplikace a volání do statické spravované metody.</span><span class="sxs-lookup"><span data-stu-id="fdde8-118">These entry points handle the complexity of finding and setting up the runtime for initialization and allow both launching a managed application and calling into a static managed method.</span></span>
* <span data-ttu-id="fdde8-119">Upřednostňovanou metodou hostování modulu runtime .NET Core před rozhraním .NET Core 3,0 je rozhraní API [CoreClrHost. h](https://github.com/dotnet/coreclr/blob/master/src/coreclr/hosts/inc/coreclrhost.h) .</span><span class="sxs-lookup"><span data-stu-id="fdde8-119">The preferred method of hosting the .NET Core runtime prior to .NET Core 3.0 is with the [CoreClrHost.h](https://github.com/dotnet/coreclr/blob/master/src/coreclr/hosts/inc/coreclrhost.h) API.</span></span> <span data-ttu-id="fdde8-120">Toto rozhraní API zpřístupňuje funkce pro snadné spouštění a zastavování modulu runtime a vyvolání spravovaného kódu (buď spuštěním spravovaného souboru exe, nebo voláním statických spravovaných metod).</span><span class="sxs-lookup"><span data-stu-id="fdde8-120">This API exposes functions for easily starting and stopping the runtime and invoking managed code (either by launching a managed exe or by calling static managed methods).</span></span>
* <span data-ttu-id="fdde8-121">Rozhraní .NET Core lze také hostovat s rozhraním `ICLRRuntimeHost4` v [knihovně Mscoree. h](https://github.com/dotnet/coreclr/blob/master/src/pal/prebuilt/inc/mscoree.h).</span><span class="sxs-lookup"><span data-stu-id="fdde8-121">.NET Core can also be hosted with the `ICLRRuntimeHost4` interface in [mscoree.h](https://github.com/dotnet/coreclr/blob/master/src/pal/prebuilt/inc/mscoree.h).</span></span> <span data-ttu-id="fdde8-122">Toto rozhraní API bylo asi delší než CoreClrHost. h, takže jste se mohli setkat s jeho staršími hostiteli.</span><span class="sxs-lookup"><span data-stu-id="fdde8-122">This API has been around longer than CoreClrHost.h, so you may have seen older hosts using it.</span></span> <span data-ttu-id="fdde8-123">Pořád funguje a umožňuje nad hostitelským procesem větší kontrolu než CoreClrHost.</span><span class="sxs-lookup"><span data-stu-id="fdde8-123">It still works and allows a bit more control over the hosting process than CoreClrHost.</span></span> <span data-ttu-id="fdde8-124">U většiny scénářů se ale CoreClrHost. h upřednostňuje teď z důvodu jednodušších rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="fdde8-124">For most scenarios, though, CoreClrHost.h is preferred now because of its simpler APIs.</span></span>

## <a name="sample-hosts"></a><span data-ttu-id="fdde8-125">Ukázkové hostitele</span><span class="sxs-lookup"><span data-stu-id="fdde8-125">Sample Hosts</span></span>
<span data-ttu-id="fdde8-126">[Příklady hostitelů](https://github.com/dotnet/samples/tree/master/core/hosting) , které demonstrují kroky popsané v následujících kurzech, jsou k dispozici v úložišti GitHub/Samples GitHub.</span><span class="sxs-lookup"><span data-stu-id="fdde8-126">[Sample hosts](https://github.com/dotnet/samples/tree/master/core/hosting) demonstrating the steps outlined in the tutorials below are available in the dotnet/samples GitHub repository.</span></span> <span data-ttu-id="fdde8-127">Komentáře v ukázkách jasně přiřazují očíslované kroky z těchto kurzů s tím, kde jsou provedeny v ukázce.</span><span class="sxs-lookup"><span data-stu-id="fdde8-127">Comments in the samples clearly associate the numbered steps from these tutorials with where they're performed in the sample.</span></span> <span data-ttu-id="fdde8-128">Pokyny ke stažení najdete v tématu [ukázky a kurzy](../../samples-and-tutorials/index.md#viewing-and-downloading-samples).</span><span class="sxs-lookup"><span data-stu-id="fdde8-128">For download instructions, see [Samples and Tutorials](../../samples-and-tutorials/index.md#viewing-and-downloading-samples).</span></span>

<span data-ttu-id="fdde8-129">Mějte na paměti, že Ukázkoví hostitelé se mají použít ke studijním účelům, takže mají světlou kontrolu chyb a jsou navržené pro zdůraznění čitelnosti v průběhu efektivity.</span><span class="sxs-lookup"><span data-stu-id="fdde8-129">Keep in mind that the sample hosts are meant to be used for learning purposes, so they are light on error checking and are designed to emphasize readability over efficiency.</span></span>

## <a name="create-a-host-using-nethosth-and-hostfxrh"></a><span data-ttu-id="fdde8-130">Vytvoření hostitele pomocí NetHost. h a HostFxr. h</span><span class="sxs-lookup"><span data-stu-id="fdde8-130">Create a host using NetHost.h and HostFxr.h</span></span>

<span data-ttu-id="fdde8-131">Následující kroky podrobně popisují, jak pomocí knihoven `nethost` a `hostfxr` spustit modul runtime .NET Core v nativní aplikaci a volat do spravované statické metody.</span><span class="sxs-lookup"><span data-stu-id="fdde8-131">The following steps detail how to use the `nethost` and `hostfxr` libraries to start the .NET Core runtime in a native application and call into a managed static method.</span></span> <span data-ttu-id="fdde8-132">[Ukázka](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithHostFxr) používá hlavičku `nethost` a knihovnu nainstalovanou se sadou .NET SDK a kopie [`coreclr_delegates.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/coreclr_delegates.h) a [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) souborů z úložiště [dotnet/Core-Setup](https://github.com/dotnet/core-setup) .</span><span class="sxs-lookup"><span data-stu-id="fdde8-132">The [sample](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithHostFxr) uses the `nethost` header and library installed with the .NET SDK and copies of the [`coreclr_delegates.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/coreclr_delegates.h) and [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) files from the [dotnet/core-setup](https://github.com/dotnet/core-setup) repository.</span></span>

### <a name="step-1---load-hostfxr-and-get-exported-hosting-functions"></a><span data-ttu-id="fdde8-133">Krok 1 – načtení HostFxr a získání exportovaných funkcí hostování</span><span class="sxs-lookup"><span data-stu-id="fdde8-133">Step 1 - Load HostFxr and get exported hosting functions</span></span>

<span data-ttu-id="fdde8-134">Knihovna `nethost` poskytuje funkci `get_hostfxr_path` pro vyhledání knihovny `hostfxr`.</span><span class="sxs-lookup"><span data-stu-id="fdde8-134">The `nethost` library provides the `get_hostfxr_path` function for locating the `hostfxr` library.</span></span> <span data-ttu-id="fdde8-135">Knihovna `hostfxr` zpřístupňuje funkce pro hostování modulu runtime .NET Core.</span><span class="sxs-lookup"><span data-stu-id="fdde8-135">The `hostfxr` library exposes functions for hosting the .NET Core runtime.</span></span> <span data-ttu-id="fdde8-136">Úplný seznam funkcí najdete v [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) a v [nativním dokumentu návrhu hostování](https://github.com/dotnet/core-setup/blob/master/Documentation/design-docs/native-hosting.md).</span><span class="sxs-lookup"><span data-stu-id="fdde8-136">The full list of functions can be found in [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) and the [native hosting design document](https://github.com/dotnet/core-setup/blob/master/Documentation/design-docs/native-hosting.md).</span></span> <span data-ttu-id="fdde8-137">Ukázka a tento kurz používá následující:</span><span class="sxs-lookup"><span data-stu-id="fdde8-137">The sample and this tutorial use the following:</span></span>

* <span data-ttu-id="fdde8-138">`hostfxr_initialize_for_runtime_config`: inicializuje hostitelský kontext a připraví inicializaci modulu runtime .NET Core pomocí zadané konfigurace modulu runtime.</span><span class="sxs-lookup"><span data-stu-id="fdde8-138">`hostfxr_initialize_for_runtime_config`: Initializes a host context and prepares for initialization of the .NET Core runtime using the specified runtime configuration.</span></span>
* <span data-ttu-id="fdde8-139">`hostfxr_get_runtime_delegate`: Získá delegáta pro běhovou funkčnost.</span><span class="sxs-lookup"><span data-stu-id="fdde8-139">`hostfxr_get_runtime_delegate`: Gets a delegate for runtime functionality.</span></span>
* <span data-ttu-id="fdde8-140">`hostfxr_close`: zavře hostitelský kontext.</span><span class="sxs-lookup"><span data-stu-id="fdde8-140">`hostfxr_close`: Closes a host context.</span></span>

<span data-ttu-id="fdde8-141">Knihovna `hostfxr` se našla pomocí `get_hostfxr_path`.</span><span class="sxs-lookup"><span data-stu-id="fdde8-141">The `hostfxr` library is found using `get_hostfxr_path`.</span></span> <span data-ttu-id="fdde8-142">Pak se načte a jeho exporty se načtou.</span><span class="sxs-lookup"><span data-stu-id="fdde8-142">It is then loaded and its exports are retrieved.</span></span>

[!code-cpp[HostFxrHost#LoadHostFxr](~/samples/core/hosting/HostWithHostFxr/src/NativeHost/nativehost.cpp#LoadHostFxr)]

### <a name="step-2---initialize-and-start-the-net-core-runtime"></a><span data-ttu-id="fdde8-143">Krok 2 – inicializace a spuštění modulu runtime .NET Core</span><span class="sxs-lookup"><span data-stu-id="fdde8-143">Step 2 - Initialize and start the .NET Core runtime</span></span>

<span data-ttu-id="fdde8-144">Funkce `hostfxr_initialize_for_runtime_config` a `hostfxr_get_runtime_delegate` Inicializuje a spustí modul runtime .NET Core pomocí konfigurace modulu runtime pro spravovanou komponentu, která bude načtena.</span><span class="sxs-lookup"><span data-stu-id="fdde8-144">The `hostfxr_initialize_for_runtime_config` and `hostfxr_get_runtime_delegate` functions initialize and start the .NET Core runtime using the runtime configuration for the managed component that will be loaded.</span></span> <span data-ttu-id="fdde8-145">Funkce `hostfxr_get_runtime_delegate` slouží k získání delegáta modulu runtime, který umožňuje načíst spravované sestavení a získat ukazatel funkce na statickou metodu v tomto sestavení.</span><span class="sxs-lookup"><span data-stu-id="fdde8-145">The `hostfxr_get_runtime_delegate` function is used to get a runtime delegate that allows loading a managed assembly and getting a function pointer to a static method in that assembly.</span></span>

[!code-cpp[HostFxrHost#Initialize](~/samples/core/hosting/HostWithHostFxr/src/NativeHost/nativehost.cpp#Initialize)]

### <a name="step-3---load-managed-assembly-and-get-function-pointer-to-a-managed-method"></a><span data-ttu-id="fdde8-146">Krok 3 – načtení spravovaného sestavení a získání ukazatele na spravovanou metodu</span><span class="sxs-lookup"><span data-stu-id="fdde8-146">Step 3 - Load managed assembly and get function pointer to a managed method</span></span>

<span data-ttu-id="fdde8-147">Delegát modulu runtime je volán pro načtení spravovaného sestavení a získání ukazatele na funkci spravované metody.</span><span class="sxs-lookup"><span data-stu-id="fdde8-147">The runtime delegate is called to load the managed assembly and get a function pointer to a managed method.</span></span> <span data-ttu-id="fdde8-148">Delegát vyžaduje cestu sestavení, název typu a název metody jako vstupy a vrátí ukazatel na funkci, který lze použít k vyvolání spravované metody.</span><span class="sxs-lookup"><span data-stu-id="fdde8-148">The delegate requires the assembly path, type name, and method name as inputs and returns a function pointer that can be used to invoke the managed method.</span></span>

[!code-cpp[HostFxrHost#LoadAndGet](~/samples/core/hosting/HostWithHostFxr/src/NativeHost/nativehost.cpp#LoadAndGet)]

<span data-ttu-id="fdde8-149">Předáním `nullptr` jako názvu typu delegáta při volání delegáta modulu runtime používá ukázka výchozí signaturu pro spravovanou metodu:</span><span class="sxs-lookup"><span data-stu-id="fdde8-149">By passing `nullptr` as the delegate type name when calling the runtime delegate, the sample uses a default signature for the managed method:</span></span>

```csharp
public delegate int ComponentEntryPoint(IntPtr args, int sizeBytes);
```

<span data-ttu-id="fdde8-150">Jiný podpis lze použít zadáním názvu typu delegáta při volání delegáta modulu runtime.</span><span class="sxs-lookup"><span data-stu-id="fdde8-150">A different signature can be used by specifying the delegate type name when calling the runtime delegate.</span></span>

### <a name="step-4---run-managed-code"></a><span data-ttu-id="fdde8-151">Krok 4 – spuštění spravovaného kódu</span><span class="sxs-lookup"><span data-stu-id="fdde8-151">Step 4 - Run managed code!</span></span>

<span data-ttu-id="fdde8-152">Nativní hostitel teď může zavolat spravovanou metodu a předat jí požadované parametry.</span><span class="sxs-lookup"><span data-stu-id="fdde8-152">The native host can now call the managed method and pass it the desired parameters.</span></span>

[!code-cpp[HostFxrHost#CallManaged](~/samples/core/hosting/HostWithHostFxr/src/NativeHost/nativehost.cpp#CallManaged)]

## <a name="create-a-host-using-coreclrhosth"></a><span data-ttu-id="fdde8-153">Vytvoření hostitele pomocí CoreClrHost. h</span><span class="sxs-lookup"><span data-stu-id="fdde8-153">Create a host using CoreClrHost.h</span></span>

<span data-ttu-id="fdde8-154">Následující kroky podrobně popisují, jak pomocí rozhraní API CoreClrHost. h spustit modul runtime .NET Core v nativní aplikaci a volat do spravované statické metody.</span><span class="sxs-lookup"><span data-stu-id="fdde8-154">The following steps detail how to use the CoreClrHost.h API to start the .NET Core runtime in a native application and call into a managed static method.</span></span> <span data-ttu-id="fdde8-155">Fragmenty kódu v tomto dokumentu používají některá rozhraní API specifická pro systém Windows, ale [úplný Ukázkový hostitel](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithCoreClrHost) zobrazuje cesty kódu pro Windows i Linux.</span><span class="sxs-lookup"><span data-stu-id="fdde8-155">The code snippets in this document use some Windows-specific APIs, but the [full sample host](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithCoreClrHost) shows both Windows and Linux code paths.</span></span>

<span data-ttu-id="fdde8-156">Hostitel spolupracujícího s platformou [UNIX](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts/unixcorerun) obsahuje složitější příklad hostování v reálném čase pomocí coreclrhost. h.</span><span class="sxs-lookup"><span data-stu-id="fdde8-156">The [Unix CoreRun host](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts/unixcorerun) shows a more complex, real-world example of hosting using coreclrhost.h.</span></span>

### <a name="step-1---find-and-load-coreclr"></a><span data-ttu-id="fdde8-157">Krok 1 – vyhledání a načtení CoreCLR</span><span class="sxs-lookup"><span data-stu-id="fdde8-157">Step 1 - Find and load CoreCLR</span></span>

<span data-ttu-id="fdde8-158">Rozhraní API modulu runtime .NET Core jsou v *CoreCLR. dll* (ve Windows), v *Libcoreclr.so* (na platformě Linux) nebo v *Libcoreclr. DYLIB* (na MacOS).</span><span class="sxs-lookup"><span data-stu-id="fdde8-158">The .NET Core runtime APIs are in *coreclr.dll* (on Windows), in *libcoreclr.so* (on Linux), or in *libcoreclr.dylib* (on macOS).</span></span> <span data-ttu-id="fdde8-159">Prvním krokem pro hostování .NET Core je načtení knihovny CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="fdde8-159">The first step to hosting .NET Core is to load the CoreCLR library.</span></span> <span data-ttu-id="fdde8-160">Někteří hostitelé prohledají různé cesty nebo používají vstupní parametry k nalezení knihovny, zatímco jiné vědí, že ji načítají z určité cesty (vedle hostitele, například nebo z umístění v rámci počítače).</span><span class="sxs-lookup"><span data-stu-id="fdde8-160">Some hosts probe different paths or use input parameters to find the library while others know to load it from a certain path (next to the host, for example, or from a machine-wide location).</span></span>

<span data-ttu-id="fdde8-161">Po nalezení se knihovna načte s `LoadLibraryEx` (ve Windows) nebo `dlopen` (na platformě Linux/macOS).</span><span class="sxs-lookup"><span data-stu-id="fdde8-161">Once found, the library is loaded with `LoadLibraryEx` (on Windows) or `dlopen` (on Linux/macOS).</span></span>

[!code-cpp[CoreClrHost#1](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#1)]

### <a name="step-2---get-net-core-hosting-functions"></a><span data-ttu-id="fdde8-162">Krok 2 – získání hostujících funkcí .NET Core</span><span class="sxs-lookup"><span data-stu-id="fdde8-162">Step 2 - Get .NET Core hosting functions</span></span>

<span data-ttu-id="fdde8-163">CoreClrHost má několik důležitých metod, které jsou užitečné pro hostování .NET Core:</span><span class="sxs-lookup"><span data-stu-id="fdde8-163">CoreClrHost has several important methods useful for hosting .NET Core:</span></span>

* <span data-ttu-id="fdde8-164">`coreclr_initialize`: spustí modul runtime .NET Core a nastaví výchozí (a pouze) doménu AppDomain.</span><span class="sxs-lookup"><span data-stu-id="fdde8-164">`coreclr_initialize`: Starts the .NET Core runtime and sets up the default (and only) AppDomain.</span></span>
* <span data-ttu-id="fdde8-165">`coreclr_execute_assembly`: spustí spravované sestavení.</span><span class="sxs-lookup"><span data-stu-id="fdde8-165">`coreclr_execute_assembly`: Executes a managed assembly.</span></span>
* <span data-ttu-id="fdde8-166">`coreclr_create_delegate`: Vytvoří ukazatel na funkci spravované metody.</span><span class="sxs-lookup"><span data-stu-id="fdde8-166">`coreclr_create_delegate`: Creates a function pointer to a managed method.</span></span>
* <span data-ttu-id="fdde8-167">`coreclr_shutdown`: ukončí modul runtime .NET Core.</span><span class="sxs-lookup"><span data-stu-id="fdde8-167">`coreclr_shutdown`: Shuts down the .NET Core runtime.</span></span>
* <span data-ttu-id="fdde8-168">`coreclr_shutdown_2`: jako `coreclr_shutdown`, ale také načte ukončovací kód spravovaného kódu.</span><span class="sxs-lookup"><span data-stu-id="fdde8-168">`coreclr_shutdown_2`: Like `coreclr_shutdown`, but also retrieves the managed code's exit code.</span></span>

<span data-ttu-id="fdde8-169">Po načtení knihovny CoreCLR je dalším krokem získat odkazy na tyto funkce pomocí `GetProcAddress` (ve Windows) nebo `dlsym` (na platformě Linux/macOS).</span><span class="sxs-lookup"><span data-stu-id="fdde8-169">After loading the CoreCLR library, the next step is to get references to these functions using `GetProcAddress` (on Windows) or `dlsym` (on Linux/macOS).</span></span>

[!code-cpp[CoreClrHost#2](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#2)]

### <a name="step-3---prepare-runtime-properties"></a><span data-ttu-id="fdde8-170">Krok 3 – Příprava vlastností modulu runtime</span><span class="sxs-lookup"><span data-stu-id="fdde8-170">Step 3 - Prepare runtime properties</span></span>

<span data-ttu-id="fdde8-171">Než začnete s modulem runtime, je nutné připravit některé vlastnosti k určení chování (zejména týkající se zavaděče sestavení).</span><span class="sxs-lookup"><span data-stu-id="fdde8-171">Before starting the runtime, it is necessary to prepare some properties to specify behavior (especially concerning the assembly loader).</span></span>

<span data-ttu-id="fdde8-172">Mezi běžné vlastnosti patří:</span><span class="sxs-lookup"><span data-stu-id="fdde8-172">Common properties include:</span></span>

* <span data-ttu-id="fdde8-173">`TRUSTED_PLATFORM_ASSEMBLIES` Toto je seznam cest sestavení (oddělený znakem '; ' ve Windows a ': ' v systému Linux), který bude ve výchozím nastavení schopný vyřešit.</span><span class="sxs-lookup"><span data-stu-id="fdde8-173">`TRUSTED_PLATFORM_ASSEMBLIES` This is a list of assembly paths (delimited by ';' on Windows and ':' on Linux) which the runtime will be able to resolve by default.</span></span> <span data-ttu-id="fdde8-174">Někteří hostitelé mají pevně kódované manifesty se seznamem sestavení, která lze načíst.</span><span class="sxs-lookup"><span data-stu-id="fdde8-174">Some hosts have hard-coded manifests listing assemblies they can load.</span></span> <span data-ttu-id="fdde8-175">Ostatní budou do tohoto seznamu vkládat všechny knihovny v určitých umístěních (například vedle *CoreCLR. dll*).</span><span class="sxs-lookup"><span data-stu-id="fdde8-175">Others will put any library in certain locations (next to *coreclr.dll*, for example) on this list.</span></span>
* <span data-ttu-id="fdde8-176">`APP_PATHS` se jedná o seznam cest k testování pro sestavení, pokud se nenalezne v seznamu důvěryhodných platforem sestavení (TPA).</span><span class="sxs-lookup"><span data-stu-id="fdde8-176">`APP_PATHS` This is a list of paths to probe in for an assembly if it can't be found in the trusted platform assemblies (TPA) list.</span></span> <span data-ttu-id="fdde8-177">Vzhledem k tomu, že hostitel má větší kontrolu nad tím, která sestavení jsou načtena pomocí seznamu TPA, je osvědčeným postupem pro hostitele k určení, která sestavení chtějí načíst a jejich seznam explicitně.</span><span class="sxs-lookup"><span data-stu-id="fdde8-177">Because the host has more control over which assemblies are loaded using the TPA list, it is a best practice for hosts to determine which assemblies they expect to load and list them explicitly.</span></span> <span data-ttu-id="fdde8-178">Pokud je tato vlastnost potřebná ke zjišťování za běhu, může tento scénář povolit.</span><span class="sxs-lookup"><span data-stu-id="fdde8-178">If probing at runtime is needed, however, this property can enable that scenario.</span></span>
* <span data-ttu-id="fdde8-179">`APP_NI_PATHS` tento seznam se podobá APP_PATHS s tím rozdílem, že se jedná o cesty, které budou zjišťovány pro nativní bitové kopie.</span><span class="sxs-lookup"><span data-stu-id="fdde8-179">`APP_NI_PATHS` This list is similar to APP_PATHS except that it's meant to be paths that will be probed for native images.</span></span>
* <span data-ttu-id="fdde8-180">`NATIVE_DLL_SEARCH_DIRECTORIES` Tato vlastnost je seznam cest, které by měl zavaděč při hledání nativních knihoven volaných prostřednictvím volání nespravovaného testu otestovat.</span><span class="sxs-lookup"><span data-stu-id="fdde8-180">`NATIVE_DLL_SEARCH_DIRECTORIES` This property is a list of paths the loader should probe when looking for native libraries called via p/invoke.</span></span>
* <span data-ttu-id="fdde8-181">`PLATFORM_RESOURCE_ROOTS` tento seznam obsahuje cesty ke sondám pro satelitní sestavení prostředků (v podadresářích závislých na jazykové verzi).</span><span class="sxs-lookup"><span data-stu-id="fdde8-181">`PLATFORM_RESOURCE_ROOTS` This list includes paths to probe in for resource satellite assemblies (in culture-specific subdirectories).</span></span>

<span data-ttu-id="fdde8-182">V tomto ukázkovém hostiteli je seznam TPA vytvořený pouhým seznamem všech knihoven v aktuálním adresáři:</span><span class="sxs-lookup"><span data-stu-id="fdde8-182">In this sample host, the TPA list is constructed by simply listing all libraries in the current directory:</span></span>

[!code-cpp[CoreClrHost#7](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#7)]

<span data-ttu-id="fdde8-183">Vzhledem k tomu, že je ukázka jednoduchá, potřebuje pouze vlastnost `TRUSTED_PLATFORM_ASSEMBLIES`:</span><span class="sxs-lookup"><span data-stu-id="fdde8-183">Because the sample is simple, it only needs the `TRUSTED_PLATFORM_ASSEMBLIES` property:</span></span>

[!code-cpp[CoreClrHost#3](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#3)]

### <a name="step-4---start-the-runtime"></a><span data-ttu-id="fdde8-184">Krok 4 – spuštění modulu runtime</span><span class="sxs-lookup"><span data-stu-id="fdde8-184">Step 4 - Start the runtime</span></span>

<span data-ttu-id="fdde8-185">Na rozdíl od rozhraní API pro hostování aplikace Mscoree. h (popsané níže), rozhraní API CoreCLRHost. h spustí modul runtime a vytvoří výchozí doménu AppDomain s jediným voláním.</span><span class="sxs-lookup"><span data-stu-id="fdde8-185">Unlike the mscoree.h hosting API (described below), CoreCLRHost.h APIs start the runtime and create the default AppDomain all with a single call.</span></span> <span data-ttu-id="fdde8-186">Funkce `coreclr_initialize` přebírá základní cestu, název a vlastnosti popsané dříve a vrací zpětnou obslužnou rutinu na hostitele prostřednictvím parametru `hostHandle`.</span><span class="sxs-lookup"><span data-stu-id="fdde8-186">The `coreclr_initialize` function takes a base path, name, and the properties described earlier and returns back a handle to the host via the `hostHandle` parameter.</span></span>

[!code-cpp[CoreClrHost#4](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#4)]

### <a name="step-5---run-managed-code"></a><span data-ttu-id="fdde8-187">Krok 5 – spuštění spravovaného kódu</span><span class="sxs-lookup"><span data-stu-id="fdde8-187">Step 5 - Run managed code!</span></span>

<span data-ttu-id="fdde8-188">Při spuštění modulu runtime může hostitel volat spravovaný kód.</span><span class="sxs-lookup"><span data-stu-id="fdde8-188">With the runtime started, the host can call managed code.</span></span> <span data-ttu-id="fdde8-189">To lze provést několika různými způsoby.</span><span class="sxs-lookup"><span data-stu-id="fdde8-189">This can be done in a couple of different ways.</span></span> <span data-ttu-id="fdde8-190">Vzorový kód spojený s tímto kurzem používá funkci `coreclr_create_delegate` k vytvoření delegáta staticky spravované metody.</span><span class="sxs-lookup"><span data-stu-id="fdde8-190">The sample code linked to this tutorial uses the `coreclr_create_delegate` function to create a delegate to a static managed method.</span></span> <span data-ttu-id="fdde8-191">Toto rozhraní API přebírá [název sestavení](../../standard/assembly/names.md), kvalifikovaný název oboru názvů a název metody jako vstupy a vrací delegáta, který může být použit k vyvolání metody.</span><span class="sxs-lookup"><span data-stu-id="fdde8-191">This API takes the [assembly name](../../standard/assembly/names.md), namespace-qualified type name, and method name as inputs and returns a delegate that can be used to invoke the method.</span></span>

[!code-cpp[CoreClrHost#5](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#5)]

<span data-ttu-id="fdde8-192">V této ukázce může hostitel nyní volat `managedDelegate` ke spuštění metody `ManagedWorker.DoWork`.</span><span class="sxs-lookup"><span data-stu-id="fdde8-192">In this sample, the host can now call `managedDelegate` to run the `ManagedWorker.DoWork` method.</span></span>

<span data-ttu-id="fdde8-193">Alternativně lze funkci `coreclr_execute_assembly` použít ke spuštění spravovaného spustitelného souboru.</span><span class="sxs-lookup"><span data-stu-id="fdde8-193">Alternatively, the `coreclr_execute_assembly` function can be used to launch a managed executable.</span></span> <span data-ttu-id="fdde8-194">Toto rozhraní API přebírá cestu sestavení a pole argumentů jako vstupní parametry.</span><span class="sxs-lookup"><span data-stu-id="fdde8-194">This API takes an assembly path and array of arguments as input parameters.</span></span> <span data-ttu-id="fdde8-195">Načte sestavení na této cestě a vyvolá jeho metodu Main.</span><span class="sxs-lookup"><span data-stu-id="fdde8-195">It loads the assembly at that path and invokes its main method.</span></span>

```C++
int hr = executeAssembly(
        hostHandle,
        domainId,
        argumentCount,
        arguments,
        "HelloWorld.exe",
        (unsigned int*)&exitCode);
```

### <a name="step-6---shutdown-and-clean-up"></a><span data-ttu-id="fdde8-196">Krok 6 – vypnutí a vyčištění</span><span class="sxs-lookup"><span data-stu-id="fdde8-196">Step 6 - Shutdown and clean up</span></span>

<span data-ttu-id="fdde8-197">Nakonec, když se hostitel spustí se spravovaným kódem, modul runtime .NET Core se vypne s `coreclr_shutdown` nebo `coreclr_shutdown_2`.</span><span class="sxs-lookup"><span data-stu-id="fdde8-197">Finally, when the host is done running managed code, the .NET Core runtime is shut down with `coreclr_shutdown` or `coreclr_shutdown_2`.</span></span>

[!code-cpp[CoreClrHost#6](~/samples/core/hosting/HostWithCoreClrHost/src/SampleHost.cpp#6)]

<span data-ttu-id="fdde8-198">CoreCLR nepodporuje opětovnou inicializaci ani uvolnění.</span><span class="sxs-lookup"><span data-stu-id="fdde8-198">CoreCLR does not support reinitialization or unloading.</span></span> <span data-ttu-id="fdde8-199">Nevolejte `coreclr_initialize` znovu ani uvolněte knihovnu CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="fdde8-199">Do not call `coreclr_initialize` again or unload the CoreCLR library.</span></span>

## <a name="create-a-host-using-mscoreeh"></a><span data-ttu-id="fdde8-200">Vytvoření hostitele pomocí knihovny MSCOREE. h</span><span class="sxs-lookup"><span data-stu-id="fdde8-200">Create a host using Mscoree.h</span></span>

<span data-ttu-id="fdde8-201">Jak bylo zmíněno dříve, CoreClrHost. h je nyní upřednostňovanou metodou hostování modulu runtime .NET Core.</span><span class="sxs-lookup"><span data-stu-id="fdde8-201">As mentioned previously, CoreClrHost.h is now the preferred method of hosting the .NET Core runtime.</span></span> <span data-ttu-id="fdde8-202">Rozhraní `ICLRRuntimeHost4` lze přesto použít, i když rozhraní CoreClrHost. h nejsou dostatečná (pokud jsou třeba nestandardní spouštěcí příznaky, například nebo pokud je AppDomainManager nutné ve výchozí doméně).</span><span class="sxs-lookup"><span data-stu-id="fdde8-202">The `ICLRRuntimeHost4` interface can still be used, though, if the CoreClrHost.h interfaces aren't sufficient (if non-standard startup flags are needed, for example, or if an AppDomainManager is needed on the default domain).</span></span> <span data-ttu-id="fdde8-203">Tyto pokyny vás provede hostováním .NET Core pomocí Mscoree. h.</span><span class="sxs-lookup"><span data-stu-id="fdde8-203">These instructions will guide you through hosting .NET Core using mscoree.h.</span></span>

<span data-ttu-id="fdde8-204">Hostitel spoluopětovného [spuštění](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts/corerun) zobrazuje složitější, reálný příklad hostování pomocí Mscoree. h.</span><span class="sxs-lookup"><span data-stu-id="fdde8-204">The [CoreRun host](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts/corerun) shows a more complex, real-world example of hosting using mscoree.h.</span></span>

### <a name="a-note-about-mscoreeh"></a><span data-ttu-id="fdde8-205">Poznámka o knihovně Mscoree. h</span><span class="sxs-lookup"><span data-stu-id="fdde8-205">A note about mscoree.h</span></span>
<span data-ttu-id="fdde8-206">Rozhraní pro hostování `ICLRRuntimeHost4` .NET Core je definováno v [knihovně Mscoree. IDL](https://github.com/dotnet/coreclr/blob/master/src/inc/MSCOREE.IDL).</span><span class="sxs-lookup"><span data-stu-id="fdde8-206">The `ICLRRuntimeHost4` .NET Core hosting interface is defined in [MSCOREE.IDL](https://github.com/dotnet/coreclr/blob/master/src/inc/MSCOREE.IDL).</span></span> <span data-ttu-id="fdde8-207">Verze hlavičky tohoto souboru (mscoree. h), kterou váš hostitel bude potřebovat odkazovat, je vytvořena prostřednictvím MIDL při sestavení [modulu runtime .NET Core](https://github.com/dotnet/coreclr/) .</span><span class="sxs-lookup"><span data-stu-id="fdde8-207">A header version of this file (mscoree.h), which your host will need to reference, is produced via MIDL when the [.NET Core runtime](https://github.com/dotnet/coreclr/) is built.</span></span> <span data-ttu-id="fdde8-208">Pokud nechcete sestavit modul runtime .NET Core, Mscoree. h je také k dispozici jako [předem sestavená hlavička](https://github.com/dotnet/coreclr/tree/master/src/pal/prebuilt/inc) v úložišti dotnet/CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="fdde8-208">If you do not want to build the .NET Core runtime, mscoree.h is also available as a [pre-built header](https://github.com/dotnet/coreclr/tree/master/src/pal/prebuilt/inc) in the dotnet/coreclr repository.</span></span> <span data-ttu-id="fdde8-209">[Pokyny k vytvoření modulu runtime .NET Core](https://github.com/dotnet/coreclr#building-the-repository) najdete v úložišti GitHub.</span><span class="sxs-lookup"><span data-stu-id="fdde8-209">[Instructions on building the .NET Core runtime](https://github.com/dotnet/coreclr#building-the-repository) can be found in its GitHub repository.</span></span>

### <a name="step-1---identify-the-managed-entry-point"></a><span data-ttu-id="fdde8-210">Krok 1 – určení spravovaného vstupního bodu</span><span class="sxs-lookup"><span data-stu-id="fdde8-210">Step 1 - Identify the managed entry point</span></span>
<span data-ttu-id="fdde8-211">Po odkazování na potřebné hlavičky (například[Mscoree. h](https://github.com/dotnet/coreclr/tree/master/src/pal/prebuilt/inc/mscoree.h) a stdio. h) jedna z prvních věcí, kterou musí hostitel .NET Core udělat, je najít spravovaný vstupní bod, který bude používat.</span><span class="sxs-lookup"><span data-stu-id="fdde8-211">After referencing necessary headers ([mscoree.h](https://github.com/dotnet/coreclr/tree/master/src/pal/prebuilt/inc/mscoree.h) and stdio.h, for example), one of the first things a .NET Core host must do is locate the managed entry point it will be using.</span></span> <span data-ttu-id="fdde8-212">V našem ukázkovém hostiteli je to provedeno pouhým prvním argumentem příkazového řádku pro hostitele jako cestu ke spravovanému binárnímu souboru, jehož metoda `main` bude provedena.</span><span class="sxs-lookup"><span data-stu-id="fdde8-212">In our sample host, this is done by just taking the first command-line argument to our host as the path to a managed binary whose `main` method will be executed.</span></span>

[!code-cpp[NetCoreHost#1](~/samples/core/hosting/HostWithMscoree/host.cpp#1)]

### <a name="step-2---find-and-load-coreclr"></a><span data-ttu-id="fdde8-213">Krok 2 – vyhledání a načtení CoreCLR</span><span class="sxs-lookup"><span data-stu-id="fdde8-213">Step 2 - Find and load CoreCLR</span></span>
<span data-ttu-id="fdde8-214">Rozhraní API modulu runtime .NET Core jsou v *CoreCLR. dll* (ve Windows).</span><span class="sxs-lookup"><span data-stu-id="fdde8-214">The .NET Core runtime APIs are in *CoreCLR.dll* (on Windows).</span></span> <span data-ttu-id="fdde8-215">Chcete-li získat naše hostující rozhraní (`ICLRRuntimeHost4`), je nutné najít a načíst *CoreCLR. dll*.</span><span class="sxs-lookup"><span data-stu-id="fdde8-215">To get our hosting interface (`ICLRRuntimeHost4`), it's necessary to find and load *CoreCLR.dll*.</span></span> <span data-ttu-id="fdde8-216">Pro definování konvence, jak bude vyhledána *Knihovna CoreCLR. dll*, je k dis na hostitele.</span><span class="sxs-lookup"><span data-stu-id="fdde8-216">It is up to the host to define a convention for how it will locate *CoreCLR.dll*.</span></span> <span data-ttu-id="fdde8-217">Někteří hostitelé očekávají, že se soubor nachází v dobře známém umístění v rámci počítače (například *%ProgramFiles%\dotnet\shared\Microsoft.NETCore.App\2.1.6*).</span><span class="sxs-lookup"><span data-stu-id="fdde8-217">Some hosts expect the file to be present in a well-known machine-wide location (such as *%programfiles%\dotnet\shared\Microsoft.NETCore.App\2.1.6*).</span></span> <span data-ttu-id="fdde8-218">Ostatní očekávají, že *CoreCLR. dll* se načte z umístění vedle samotného hostitele nebo aplikace, která se má hostovat.</span><span class="sxs-lookup"><span data-stu-id="fdde8-218">Others expect that *CoreCLR.dll* will be loaded from a location next to either the host itself or the app to be hosted.</span></span> <span data-ttu-id="fdde8-219">I když ostatní můžou najít knihovnu v proměnné prostředí.</span><span class="sxs-lookup"><span data-stu-id="fdde8-219">Still others might consult an environment variable to find the library.</span></span>

<span data-ttu-id="fdde8-220">V systému Linux nebo macOS je základní běhová knihovna *libcoreclr.so* nebo *libcoreclr. DYLIB*, v uvedeném pořadí.</span><span class="sxs-lookup"><span data-stu-id="fdde8-220">On Linux or macOS, the core runtime library is *libcoreclr.so* or *libcoreclr.dylib*, respectively.</span></span>

<span data-ttu-id="fdde8-221">Náš Ukázkový hostitel sonduje několik běžných umístění pro *CoreCLR. dll*.</span><span class="sxs-lookup"><span data-stu-id="fdde8-221">Our sample host probes a few common locations for *CoreCLR.dll*.</span></span> <span data-ttu-id="fdde8-222">Po nalezení je nutné ji načíst prostřednictvím `LoadLibrary` (nebo `dlopen` v systému Linux/macOS).</span><span class="sxs-lookup"><span data-stu-id="fdde8-222">Once found, it must be loaded via `LoadLibrary` (or `dlopen` on Linux/macOS).</span></span>

[!code-cpp[NetCoreHost#2](~/samples/core/hosting/HostWithMscoree/host.cpp#2)]

### <a name="step-3---get-an-iclrruntimehost4-instance"></a><span data-ttu-id="fdde8-223">Krok 3 – získání instance ICLRRuntimeHost4</span><span class="sxs-lookup"><span data-stu-id="fdde8-223">Step 3 - Get an ICLRRuntimeHost4 Instance</span></span>
<span data-ttu-id="fdde8-224">Rozhraní `ICLRRuntimeHost4` hostování je načteno voláním `GetProcAddress` (nebo `dlsym` na Linux/macOS) na `GetCLRRuntimeHost`a následným vyvoláním této funkce.</span><span class="sxs-lookup"><span data-stu-id="fdde8-224">The `ICLRRuntimeHost4` hosting interface is retrieved by calling `GetProcAddress` (or `dlsym` on Linux/macOS) on `GetCLRRuntimeHost`, and then invoking that function.</span></span>

[!code-cpp[NetCoreHost#3](~/samples/core/hosting/HostWithMscoree/host.cpp#3)]

### <a name="step-4---set-startup-flags-and-start-the-runtime"></a><span data-ttu-id="fdde8-225">Krok 4 – nastavení příznaků spuštění a spuštění modulu runtime</span><span class="sxs-lookup"><span data-stu-id="fdde8-225">Step 4 - Set startup flags and start the runtime</span></span>
<span data-ttu-id="fdde8-226">S `ICLRRuntimeHost4` teď můžeme zadat spouštěcí příznaky pro celé prostředí a spustit modul runtime.</span><span class="sxs-lookup"><span data-stu-id="fdde8-226">With an `ICLRRuntimeHost4` in-hand, we can now specify runtime-wide startup flags and start the runtime.</span></span> <span data-ttu-id="fdde8-227">Příznaky při spuštění určují, který systém uvolňování paměti (GC) se má použít (souběžný nebo Server), jestli budeme používat jednu doménu AppDomain nebo více doménových sad a jaké zásady optimalizace zavaděče použít (pro doménové nezávislé načítání sestavení).</span><span class="sxs-lookup"><span data-stu-id="fdde8-227">Startup flags determine which garbage collector (GC) to use (concurrent or server), whether we will use a single AppDomain or multiple AppDomains, and what loader optimization policy to use (for domain-neutral loading of assemblies).</span></span>

[!code-cpp[NetCoreHost#4](~/samples/core/hosting/HostWithMscoree/host.cpp#4)]

<span data-ttu-id="fdde8-228">Modul runtime je spuštěn s voláním funkce `Start`.</span><span class="sxs-lookup"><span data-stu-id="fdde8-228">The runtime is started with a call to the `Start` function.</span></span>

```C++
hr = runtimeHost->Start();
```

### <a name="step-5---preparing-appdomain-settings"></a><span data-ttu-id="fdde8-229">Krok 5 – Příprava nastavení AppDomain</span><span class="sxs-lookup"><span data-stu-id="fdde8-229">Step 5 - Preparing AppDomain settings</span></span>
<span data-ttu-id="fdde8-230">Po spuštění modulu runtime budeme chtít nastavit doménu AppDomain.</span><span class="sxs-lookup"><span data-stu-id="fdde8-230">Once the runtime is started, we will want to set up an AppDomain.</span></span> <span data-ttu-id="fdde8-231">K dispozici je několik možností, které je třeba zadat při vytváření domény aplikace .NET, takže je nutné je nejprve připravit.</span><span class="sxs-lookup"><span data-stu-id="fdde8-231">There are a number of options that must be specified when creating a .NET AppDomain, however, so it's necessary to prepare those first.</span></span>

<span data-ttu-id="fdde8-232">Příznaky AppDomain určují chování AppDomain související se zabezpečením a interoperabilitou.</span><span class="sxs-lookup"><span data-stu-id="fdde8-232">AppDomain flags specify AppDomain behaviors related to security and interop.</span></span> <span data-ttu-id="fdde8-233">Starší hostitelé technologie Silverlight používali tato nastavení pro uživatelský kód izolovaného prostoru, ale většina moderních hostitelů .NET Core spouští uživatelský kód jako úplný vztah důvěryhodnosti a povoluje spolupráci.</span><span class="sxs-lookup"><span data-stu-id="fdde8-233">Older Silverlight hosts used these settings to sandbox user code, but most modern .NET Core hosts run user code as full trust and enable interop.</span></span>

[!code-cpp[NetCoreHost#5](~/samples/core/hosting/HostWithMscoree/host.cpp#5)]

<span data-ttu-id="fdde8-234">Až se rozhodnete, které příznaky AppDomain se mají použít, musí být definované vlastnosti AppDomain.</span><span class="sxs-lookup"><span data-stu-id="fdde8-234">After deciding which AppDomain flags to use, AppDomain properties must be defined.</span></span> <span data-ttu-id="fdde8-235">Vlastnosti jsou páry klíč/hodnota řetězců.</span><span class="sxs-lookup"><span data-stu-id="fdde8-235">The properties are key/value pairs of strings.</span></span> <span data-ttu-id="fdde8-236">Mnohé z vlastností se týkají způsobu, jakým aplikace AppDomain načte sestavení.</span><span class="sxs-lookup"><span data-stu-id="fdde8-236">Many of the properties relate to how the AppDomain will load assemblies.</span></span>

<span data-ttu-id="fdde8-237">Mezi běžné vlastnosti AppDomain patří:</span><span class="sxs-lookup"><span data-stu-id="fdde8-237">Common AppDomain properties include:</span></span>

* <span data-ttu-id="fdde8-238">`TRUSTED_PLATFORM_ASSEMBLIES` se jedná o seznam cest sestavení (oddělený `;` ve Windows a `:` na Linux/macOS), které by měly mít v doméně přednost při načítání a udělení úplné důvěryhodnosti (i v částečně důvěryhodných doménách).</span><span class="sxs-lookup"><span data-stu-id="fdde8-238">`TRUSTED_PLATFORM_ASSEMBLIES` This is a list of assembly paths (delimited by `;` on Windows and `:` on Linux/macOS) which the AppDomain should prioritize loading and give full trust to (even in partially trusted domains).</span></span> <span data-ttu-id="fdde8-239">Tento seznam má obsahovat sestavení architektury a jiné důvěryhodné moduly, podobně jako globální mezipaměť sestavení (GAC) v .NET Frameworkch scénářích.</span><span class="sxs-lookup"><span data-stu-id="fdde8-239">This list is meant to contain 'Framework' assemblies and other trusted modules, similar to the GAC in .NET Framework scenarios.</span></span> <span data-ttu-id="fdde8-240">Někteří hostitelé vloží do tohoto seznamu všechny knihovny v *CoreCLR. dll* , ostatní mají pevně kódované manifesty, které uvádějí důvěryhodná sestavení pro jejich účely.</span><span class="sxs-lookup"><span data-stu-id="fdde8-240">Some hosts will put any library next to *coreclr.dll* on this list, others have hard-coded manifests listing trusted assemblies for their purposes.</span></span>
* <span data-ttu-id="fdde8-241">`APP_PATHS` se jedná o seznam cest k testování pro sestavení, pokud se nenalezne v seznamu důvěryhodných platforem sestavení (TPA).</span><span class="sxs-lookup"><span data-stu-id="fdde8-241">`APP_PATHS` This is a list of paths to probe in for an assembly if it can't be found in the trusted platform assemblies (TPA) list.</span></span> <span data-ttu-id="fdde8-242">Vzhledem k tomu, že hostitel má větší kontrolu nad tím, která sestavení jsou načtena pomocí seznamu TPA, je osvědčeným postupem pro hostitele k určení, která sestavení chtějí načíst a jejich seznam explicitně.</span><span class="sxs-lookup"><span data-stu-id="fdde8-242">Because the host has more control over which assemblies are loaded using the TPA list, it is a best practice for hosts to determine which assemblies they expect to load and list them explicitly.</span></span> <span data-ttu-id="fdde8-243">Pokud je tato vlastnost potřebná ke zjišťování za běhu, může tento scénář povolit.</span><span class="sxs-lookup"><span data-stu-id="fdde8-243">If probing at runtime is needed, however, this property can enable that scenario.</span></span>
* <span data-ttu-id="fdde8-244">`APP_NI_PATHS` tento seznam je velmi podobný APP_PATHS s tím rozdílem, že se jedná o cesty, které budou zjišťovány pro nativní bitové kopie.</span><span class="sxs-lookup"><span data-stu-id="fdde8-244">`APP_NI_PATHS` This list is very similar to APP_PATHS except that it's meant to be paths that will be probed for native images.</span></span>
* <span data-ttu-id="fdde8-245">`NATIVE_DLL_SEARCH_DIRECTORIES` Tato vlastnost je seznam cest, které by měl zavaděč při hledání nativních knihoven DLL volaných prostřednictvím volání nespravovaného testu otestovat.</span><span class="sxs-lookup"><span data-stu-id="fdde8-245">`NATIVE_DLL_SEARCH_DIRECTORIES` This property is a list of paths the loader should probe when looking for native DLLs called via p/invoke.</span></span>
* <span data-ttu-id="fdde8-246">`PLATFORM_RESOURCE_ROOTS` tento seznam obsahuje cesty ke sondám pro satelitní sestavení prostředků (v podadresářích závislých na jazykové verzi).</span><span class="sxs-lookup"><span data-stu-id="fdde8-246">`PLATFORM_RESOURCE_ROOTS` This list includes paths to probe in for resource satellite assemblies (in culture-specific subdirectories).</span></span>

<span data-ttu-id="fdde8-247">V našem [jednoduchém ukázkovém hostiteli](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithMscoree)se tyto vlastnosti nastavují takto:</span><span class="sxs-lookup"><span data-stu-id="fdde8-247">In our [simple sample host](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithMscoree), these properties are set up as follows:</span></span>

[!code-cpp[NetCoreHost#6](~/samples/core/hosting/HostWithMscoree/host.cpp#6)]

### <a name="step-6---create-the-appdomain"></a><span data-ttu-id="fdde8-248">Krok 6 – vytvoření domény AppDomain</span><span class="sxs-lookup"><span data-stu-id="fdde8-248">Step 6 - Create the AppDomain</span></span>
<span data-ttu-id="fdde8-249">Po přípravě všech příznaků a vlastností domény aplikace `ICLRRuntimeHost4::CreateAppDomainWithManager` lze použít k nastavení domény AppDomain.</span><span class="sxs-lookup"><span data-stu-id="fdde8-249">Once all AppDomain flags and properties are prepared, `ICLRRuntimeHost4::CreateAppDomainWithManager` can be used to set up the AppDomain.</span></span> <span data-ttu-id="fdde8-250">Tato funkce volitelně přijímá plně kvalifikovaný název sestavení a název typu, který se použije jako správce domény AppDomain.</span><span class="sxs-lookup"><span data-stu-id="fdde8-250">This function optionally takes a fully qualified assembly name and type name to use as the domain's AppDomain manager.</span></span> <span data-ttu-id="fdde8-251">Správce AppDomain může hostiteli umožnit řídit některé aspekty chování AppDomain a může poskytovat vstupní body pro spuštění spravovaného kódu, pokud hostitel nemá v úmyslu vyvolat kód uživatele přímo.</span><span class="sxs-lookup"><span data-stu-id="fdde8-251">An AppDomain manager can allow a host to control some aspects of AppDomain behavior and may provide entry points for launching managed code if the host doesn't intend to invoke user code directly.</span></span>

[!code-cpp[NetCoreHost#7](~/samples/core/hosting/HostWithMscoree/host.cpp#7)]

### <a name="step-7---run-managed-code"></a><span data-ttu-id="fdde8-252">Krok 7 – spuštění spravovaného kódu</span><span class="sxs-lookup"><span data-stu-id="fdde8-252">Step 7 - Run managed code!</span></span>
<span data-ttu-id="fdde8-253">V případě, že je doména AppDomain spuštěná a spuštěná, může nyní hostitel spouštět spravovaný kód.</span><span class="sxs-lookup"><span data-stu-id="fdde8-253">With an AppDomain up and running, the host can now start executing managed code.</span></span> <span data-ttu-id="fdde8-254">Nejjednodušší způsob, jak to provést, je použít `ICLRRuntimeHost4::ExecuteAssembly` k vyvolání metody vstupního bodu spravovaného sestavení.</span><span class="sxs-lookup"><span data-stu-id="fdde8-254">The easiest way to do this is to use `ICLRRuntimeHost4::ExecuteAssembly` to invoke a managed assembly's entry point method.</span></span> <span data-ttu-id="fdde8-255">Všimněte si, že tato funkce funguje pouze ve scénářích s jednou doménou.</span><span class="sxs-lookup"><span data-stu-id="fdde8-255">Note that this function only works in single-domain scenarios.</span></span>

[!code-cpp[NetCoreHost#8](~/samples/core/hosting/HostWithMscoree/host.cpp#8)]

<span data-ttu-id="fdde8-256">Další možnost, pokud `ExecuteAssembly` nesplňuje potřeby hostitele, je použití `CreateDelegate` k vytvoření ukazatele funkce na statickou spravovanou metodu.</span><span class="sxs-lookup"><span data-stu-id="fdde8-256">Another option, if `ExecuteAssembly` doesn't meet your host's needs, is to use `CreateDelegate` to create a function pointer to a static managed method.</span></span> <span data-ttu-id="fdde8-257">To vyžaduje, aby hostitel znal podpis metody, do které volá (aby mohl vytvořit typ ukazatele na funkci), ale umožňuje hostitelům flexibility vyvolat kód jiný než vstupní bod sestavení.</span><span class="sxs-lookup"><span data-stu-id="fdde8-257">This requires the host to know the signature of the method it is calling into (in order to create the function pointer type) but allows hosts the flexibility to invoke code other than an assembly's entry point.</span></span> <span data-ttu-id="fdde8-258">Název sestavení zadaný ve druhém parametru je [úplný název spravovaného sestavení](../../standard/assembly/names.md) knihovny, která se má načíst.</span><span class="sxs-lookup"><span data-stu-id="fdde8-258">The assembly name provided in the second parameter is the [full managed assembly name](../../standard/assembly/names.md) of the library to load.</span></span>

```C++
void *pfnDelegate = NULL;
hr = runtimeHost->CreateDelegate(
    domainId,
    L"HW, Version=1.0.0.0, Culture=neutral", // Target managed assembly
    L"ConsoleApplication.Program",           // Target managed type
    L"Main",                                 // Target entry point (static method)
    (INT_PTR*)&pfnDelegate);

((MainMethodFp*)pfnDelegate)(NULL);
```

### <a name="step-8---clean-up"></a><span data-ttu-id="fdde8-259">Krok 8 – vyčištění</span><span class="sxs-lookup"><span data-stu-id="fdde8-259">Step 8 - Clean up</span></span>
<span data-ttu-id="fdde8-260">Nakonec by se měl hostitel vyčistit po samotném uvolněním AppDomains, zastavením modulu runtime a uvolněním `ICLRRuntimeHost4` odkazem.</span><span class="sxs-lookup"><span data-stu-id="fdde8-260">Finally, the host should clean up after itself by unloading AppDomains, stopping the runtime, and releasing the `ICLRRuntimeHost4` reference.</span></span>

[!code-cpp[NetCoreHost#9](~/samples/core/hosting/HostWithMscoree/host.cpp#9)]

<span data-ttu-id="fdde8-261">CoreCLR nepodporuje vykládku.</span><span class="sxs-lookup"><span data-stu-id="fdde8-261">CoreCLR does not support unloading.</span></span> <span data-ttu-id="fdde8-262">Uvolněte knihovnu CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="fdde8-262">Do not unload the CoreCLR library.</span></span>

## <a name="conclusion"></a><span data-ttu-id="fdde8-263">Závěr</span><span class="sxs-lookup"><span data-stu-id="fdde8-263">Conclusion</span></span>
<span data-ttu-id="fdde8-264">Po sestavení hostitele je možné ho otestovat spuštěním z příkazového řádku a předávat všechny argumenty, které hostitel očekává (jako je spravovaná aplikace pro spuštění ukázkového hostitele mscoree).</span><span class="sxs-lookup"><span data-stu-id="fdde8-264">Once your host is built, it can be tested by running it from the command line and passing any arguments the host expects (like the managed app to run for the mscoree example host).</span></span> <span data-ttu-id="fdde8-265">Při zadávání aplikace .NET Core pro spuštění hostitele nezapomeňte použít knihovnu DLL, která je vytvořena nástrojem `dotnet build`.</span><span class="sxs-lookup"><span data-stu-id="fdde8-265">When specifying the .NET Core app for the host to run, be sure to use the .dll that is produced by `dotnet build`.</span></span> <span data-ttu-id="fdde8-266">Spustitelné soubory (soubory. exe) vytvořené `dotnet publish` pro samostatné aplikace jsou ve skutečnosti výchozím hostitelem .NET Core (aby bylo možné aplikaci spustit přímo z příkazového řádku ve scénářích hlavní); uživatelský kód je zkompilován do knihovny DLL se stejným názvem.</span><span class="sxs-lookup"><span data-stu-id="fdde8-266">Executables (.exe files) produced by `dotnet publish` for self-contained applications are actually the default .NET Core host (so that the app can be launched directly from the command line in mainline scenarios); user code is compiled into a dll of the same name.</span></span>

<span data-ttu-id="fdde8-267">Pokud co nejdříve nefunguje, poklikejte na to, že *CoreCLR. dll* je k dispozici v umístění očekávaném hostitelem, že všechny nezbytné knihovny rozhraní jsou v seznamu TPA a že CoreCLR bitová verze (32-bit nebo 64) odpovídá způsobu sestavení hostitele.</span><span class="sxs-lookup"><span data-stu-id="fdde8-267">If things don't work initially, double-check that *coreclr.dll* is available in the location expected by the host, that all necessary Framework libraries are in the TPA list, and that CoreCLR's bitness (32-bit or 64-bit) matches how the host was built.</span></span>

<span data-ttu-id="fdde8-268">Hostování modulu runtime .NET Core je pokročilý scénář, který nevyžaduje mnoho vývojářů, ale pro uživatele, kteří potřebují spustit spravovaný kód z nativního procesu nebo kteří potřebují větší kontrolu nad chováním modulu runtime .NET Core, může být velmi užitečné.</span><span class="sxs-lookup"><span data-stu-id="fdde8-268">Hosting the .NET Core runtime is an advanced scenario that many developers won't require, but for those who need to launch managed code from a native process, or who need more control over the .NET Core runtime's behavior, it can be very useful.</span></span>
