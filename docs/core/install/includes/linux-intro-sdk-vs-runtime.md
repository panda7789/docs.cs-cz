---
ms.openlocfilehash: 81ddf0ccdc0ff3269a68fcb6c7c43ad9b39c8128
ms.sourcegitcommit: cdb295dd1db589ce5169ac9ff096f01fd0c2da9d
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/09/2020
ms.locfileid: "84602969"
---

<span data-ttu-id="365ef-101">Pokud chcete vyvíjet aplikace .NET, nainstalujte sadu SDK (která obsahuje modul runtime).</span><span class="sxs-lookup"><span data-stu-id="365ef-101">Install the SDK (which includes the runtime) if you want to develop .NET apps.</span></span> <span data-ttu-id="365ef-102">Nebo, pokud potřebujete pouze spustit aplikace, nainstalujte modul runtime.</span><span class="sxs-lookup"><span data-stu-id="365ef-102">Or, if you only need to run apps, install the runtime.</span></span> <span data-ttu-id="365ef-103">Pokud instalujete modul runtime, doporučujeme nainstalovat modul **runtime ASP.NET Core** , protože obsahuje modul runtime .NET Core i ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="365ef-103">If you're installing the runtime, we suggest you install the **ASP.NET Core runtime** as it includes both .NET Core and ASP.NET Core runtimes.</span></span>

<span data-ttu-id="365ef-104">Pokud jste již nainstalovali sadu SDK nebo modul runtime, použijte `dotnet --list-sdks` `dotnet --list-runtimes` k zobrazení nainstalovaných verzí příkazy a.</span><span class="sxs-lookup"><span data-stu-id="365ef-104">If you've already installed the SDK or runtime, use the `dotnet --list-sdks` and `dotnet --list-runtimes` commands to see which versions are installed.</span></span> <span data-ttu-id="365ef-105">Další informace najdete v tématu [Jak ověřit, zda je rozhraní .NET Core již nainstalováno](../how-to-detect-installed-versions.md).</span><span class="sxs-lookup"><span data-stu-id="365ef-105">For more information, see [How to check that .NET Core is already installed](../how-to-detect-installed-versions.md).</span></span>
