---
title: Práce s macOS Catalina notarization
description: Jak zvládnout notarization a problémy s certifikáty pomocí macOS při instalaci modulu runtime .NET Core, sady SDK a aplikací vytvořených pomocí .NET Core
author: adegeo
ms.author: adegeo
ms.date: 02/14/2020
ms.openlocfilehash: a7741727ad46216ebd9936515d8af29b6d7049c2
ms.sourcegitcommit: c4a15c6c4ecbb8a46ad4e67d9b3ab9b8b031d849
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/20/2020
ms.locfileid: "88656524"
---
# <a name="macos-catalina-notarization-and-the-impact-on-net-core-downloads-and-projects"></a><span data-ttu-id="e080b-103">macOS Catalina notarization a dopad na stažení a projekty .NET Core</span><span class="sxs-lookup"><span data-stu-id="e080b-103">macOS Catalina Notarization and the impact on .NET Core downloads and projects</span></span>

<span data-ttu-id="e080b-104">Od macOS Catalina (verze 10,15) je nutné, aby byl veškerý software sestavený od 1. června 2019 a distribuován s ID vývojáře notarized.</span><span class="sxs-lookup"><span data-stu-id="e080b-104">Beginning with macOS Catalina (version 10.15), all software built after June 1, 2019, and distributed with Developer ID, must be notarized.</span></span> <span data-ttu-id="e080b-105">Tento požadavek se vztahuje na modul runtime .NET Core, .NET Core SDK a software vytvořený pomocí .NET Core.</span><span class="sxs-lookup"><span data-stu-id="e080b-105">This requirement applies to the .NET Core runtime, .NET Core SDK, and software created with .NET Core.</span></span> <span data-ttu-id="e080b-106">Tento článek popisuje běžné scénáře, se kterými se můžete setkat s .NET Core a macOS Notarization.</span><span class="sxs-lookup"><span data-stu-id="e080b-106">This article describes the common scenarios you may face with .NET Core and macOS notarization.</span></span>

## <a name="installing-net-core"></a><span data-ttu-id="e080b-107">Instalace .NET Core</span><span class="sxs-lookup"><span data-stu-id="e080b-107">Installing .NET Core</span></span>

<span data-ttu-id="e080b-108">Instalační programy pro .NET Core (běhové i sady SDK) verze 3,1, 3,0 a 2,1 byly notarized od 18. února 2020.</span><span class="sxs-lookup"><span data-stu-id="e080b-108">The installers for .NET Core (both runtime and SDK) versions 3.1, 3.0, and 2.1, have been notarized since February 18, 2020.</span></span> <span data-ttu-id="e080b-109">Předchozí vydané verze nejsou notarized.</span><span class="sxs-lookup"><span data-stu-id="e080b-109">Prior released versions aren't notarized.</span></span> <span data-ttu-id="e080b-110">Nenotarized verzi rozhraní .NET Core můžete nainstalovat ručně, a to tak, že nejprve stáhnete instalační program a potom použijete `sudo installer` příkaz.</span><span class="sxs-lookup"><span data-stu-id="e080b-110">You can manually install a non-notarized version of .NET Core by first downloading the installer, and then using the `sudo installer` command.</span></span> <span data-ttu-id="e080b-111">Další informace najdete v tématu [Stažení a ruční instalace pro MacOS](sdk.md?pivots=os-macos#download-and-manually-install).</span><span class="sxs-lookup"><span data-stu-id="e080b-111">For more information, see [Download and manually install for macOS](sdk.md?pivots=os-macos#download-and-manually-install).</span></span>

<span data-ttu-id="e080b-112">Od následujících verzí jsou instalační programy .NET Core notarized:</span><span class="sxs-lookup"><span data-stu-id="e080b-112">Beginning with the following versions, the .NET Core installers are notarized:</span></span>

- <span data-ttu-id="e080b-113">.NET Core Runtime</span><span class="sxs-lookup"><span data-stu-id="e080b-113">.NET Core Runtime</span></span>
  - <span data-ttu-id="e080b-114">2.1.16</span><span class="sxs-lookup"><span data-stu-id="e080b-114">2.1.16</span></span>
  - <span data-ttu-id="e080b-115">3.0.3</span><span class="sxs-lookup"><span data-stu-id="e080b-115">3.0.3</span></span>
  - <span data-ttu-id="e080b-116">3.1.2</span><span class="sxs-lookup"><span data-stu-id="e080b-116">3.1.2</span></span>

- <span data-ttu-id="e080b-117">Sada .NET Core SDK</span><span class="sxs-lookup"><span data-stu-id="e080b-117">.NET Core SDK</span></span>
  - <span data-ttu-id="e080b-118">2.1.512</span><span class="sxs-lookup"><span data-stu-id="e080b-118">2.1.512</span></span>
  - <span data-ttu-id="e080b-119">3.0.103</span><span class="sxs-lookup"><span data-stu-id="e080b-119">3.0.103</span></span>
  - <span data-ttu-id="e080b-120">3.1.102</span><span class="sxs-lookup"><span data-stu-id="e080b-120">3.1.102</span></span>

## <a name="apphost-is-disabled-by-default"></a><span data-ttu-id="e080b-121">appHost je ve výchozím nastavení zakázaná.</span><span class="sxs-lookup"><span data-stu-id="e080b-121">appHost is disabled by default</span></span>

<span data-ttu-id="e080b-122">Ve výchozím nastavení nenotarized verze .NET Core SDK 3,0 a vyšší mají za následek, že se při kompilaci projektu, publikování nebo spuštění spustí nativní spustitelný soubor strojového souboru (označovaný jako **appHost**).</span><span class="sxs-lookup"><span data-stu-id="e080b-122">By default, non-notarized versions of the .NET Core SDK 3.0 and above produce a native Mach-O executable (known as the **appHost**) when your project compiles, publishes, or is run.</span></span> <span data-ttu-id="e080b-123">Tento spustitelný soubor je pohodlný způsob, jak aplikaci spustit.</span><span class="sxs-lookup"><span data-stu-id="e080b-123">This executable is a convenient way to run your app.</span></span> <span data-ttu-id="e080b-124">V opačném případě musí být aplikace spuštěná `dotnet <filename.dll>` .</span><span class="sxs-lookup"><span data-stu-id="e080b-124">Otherwise, your app must be started by running `dotnet <filename.dll>`.</span></span> <span data-ttu-id="e080b-125">Když je povolený appHost, `dotnet run` příkaz se vyvolá v kontextu appHost.</span><span class="sxs-lookup"><span data-stu-id="e080b-125">When the appHost is enabled, the `dotnet run` command is invoked in the context of the appHost.</span></span> <span data-ttu-id="e080b-126">Další informace najdete v tématu [kontext appHost](#context-of-the-apphost).</span><span class="sxs-lookup"><span data-stu-id="e080b-126">For more information, see [Context of the appHost](#context-of-the-apphost).</span></span>

<span data-ttu-id="e080b-127">Počínaje verzí notarized .NET Core SDK 3,0 a vyšší se ve výchozím nastavení negeneruje spustitelný soubor appHost.</span><span class="sxs-lookup"><span data-stu-id="e080b-127">Starting with the notarized versions of the .NET Core SDK 3.0 and above, the appHost executable isn't generated by default.</span></span> <span data-ttu-id="e080b-128">Můžete zapnout generování appHost s [`UseAppHost`](../project-sdk/msbuild-props.md#useapphost) nastavením Boolean v souboru projektu.</span><span class="sxs-lookup"><span data-stu-id="e080b-128">You can turn on appHost generation with the [`UseAppHost`](../project-sdk/msbuild-props.md#useapphost) boolean setting in the project file.</span></span> <span data-ttu-id="e080b-129">Můžete také přepnout appHost s `-p:UseAppHost` parametrem na příkazovém řádku pro konkrétní `dotnet` příkaz, který spustíte:</span><span class="sxs-lookup"><span data-stu-id="e080b-129">You can also toggle the appHost with the `-p:UseAppHost` parameter on the command line for the specific `dotnet` command you run:</span></span>

- <span data-ttu-id="e080b-130">Soubor projektu</span><span class="sxs-lookup"><span data-stu-id="e080b-130">Project file</span></span>

  ```xml
  <PropertyGroup>
    <UseAppHost>true</UseAppHost>
  </PropertyGroup>
  ```

- <span data-ttu-id="e080b-131">Parametr příkazového řádku</span><span class="sxs-lookup"><span data-stu-id="e080b-131">Command-line parameter</span></span>

  ```dotnetcli
  dotnet run -p:UseAppHost=true
  ```

<span data-ttu-id="e080b-132">AppHost se vždy vytvoří při publikování [vlastní](../deploying/index.md#publish-self-contained)aplikace.</span><span class="sxs-lookup"><span data-stu-id="e080b-132">An appHost is always created when you publish your app [self-contained](../deploying/index.md#publish-self-contained).</span></span>

<span data-ttu-id="e080b-133">Další informace o tomto `UseAppHost` nastavení najdete v tématu [vlastnosti MSBuild pro Microsoft. NET. SDK](../project-sdk/msbuild-props.md#useapphost).</span><span class="sxs-lookup"><span data-stu-id="e080b-133">For more information about the `UseAppHost` setting, see [MSBuild properties for Microsoft.NET.Sdk](../project-sdk/msbuild-props.md#useapphost).</span></span>

### <a name="context-of-the-apphost"></a><span data-ttu-id="e080b-134">Kontext appHost</span><span class="sxs-lookup"><span data-stu-id="e080b-134">Context of the appHost</span></span>

<span data-ttu-id="e080b-135">Když je v projektu povolený appHost a použijete `dotnet run` příkaz ke spuštění vaší aplikace, vyvolá se aplikace v kontextu appHost a ne jako výchozí hostitel (výchozí hostitel je `dotnet` příkaz).</span><span class="sxs-lookup"><span data-stu-id="e080b-135">When the appHost is enabled in your project, and you use the `dotnet run` command to run your app, the app is invoked in the context of the appHost and not the default host (the default host is the `dotnet` command).</span></span> <span data-ttu-id="e080b-136">Pokud je appHost v projektu zakázaný, `dotnet run` příkaz spustí vaši aplikaci v kontextu výchozího hostitele.</span><span class="sxs-lookup"><span data-stu-id="e080b-136">If the appHost is disabled in your project, the `dotnet run` command runs your app in the context of the default host.</span></span> <span data-ttu-id="e080b-137">I když je appHost zakázaný, publikování aplikace jako samostatné vygeneruje spustitelný soubor appHost a uživatelé tento spustitelný soubor použijí ke spuštění vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="e080b-137">Even if the appHost is disabled, publishing your app as self-contained generates an appHost executable, and users use that executable to run your app.</span></span> <span data-ttu-id="e080b-138">Spuštění vaší aplikace pomocí `dotnet <filename.dll>` vyvolá aplikaci s výchozím hostitelem, sdíleným modulem runtime.</span><span class="sxs-lookup"><span data-stu-id="e080b-138">Running your app with `dotnet <filename.dll>` invokes the app with the default host, the shared runtime.</span></span>

<span data-ttu-id="e080b-139">Po vyvolání aplikace používající appHost se oddíl certifikátu, ke kterému se aplikace přistupovala, liší od výchozího hostitele notarized.</span><span class="sxs-lookup"><span data-stu-id="e080b-139">When an app using the appHost is invoked, the certificate partition accessed by the app is different from the notarized default host.</span></span> <span data-ttu-id="e080b-140">Pokud vaše aplikace musí přistupovat k certifikátům nainstalovaným prostřednictvím výchozího hostitele, použijte `dotnet run` příkaz ke spuštění aplikace ze souboru projektu nebo použijte `dotnet <filename.dll>` příkaz ke spuštění aplikace přímo.</span><span class="sxs-lookup"><span data-stu-id="e080b-140">If your app must access the certificates installed through the default host, use the `dotnet run` command to run your app from its project file, or use the `dotnet <filename.dll>` command to start the app directly.</span></span>

<span data-ttu-id="e080b-141">Další informace o tomto scénáři najdete v části [ASP.NET Core a MacOS a certifikáty](#aspnet-core-and-macos-and-certificates) .</span><span class="sxs-lookup"><span data-stu-id="e080b-141">More information about this scenario is provided in the [ASP.NET Core and macOS and certificates](#aspnet-core-and-macos-and-certificates) section.</span></span>

## <a name="aspnet-core-and-macos-and-certificates"></a><span data-ttu-id="e080b-142">ASP.NET Core a macOS a certifikáty</span><span class="sxs-lookup"><span data-stu-id="e080b-142">ASP.NET Core and macOS and certificates</span></span>

<span data-ttu-id="e080b-143">.NET Core poskytuje možnost spravovat certifikáty v řetězci klíčů macOS s <xref:System.Security.Cryptography.X509Certificates> třídou.</span><span class="sxs-lookup"><span data-stu-id="e080b-143">.NET Core provides the ability to manage certificates in the macOS Keychain with the <xref:System.Security.Cryptography.X509Certificates> class.</span></span> <span data-ttu-id="e080b-144">Přístup k řetězci macOS používá při rozhodování, který oddíl má být považován za primární klíč, identitu aplikace.</span><span class="sxs-lookup"><span data-stu-id="e080b-144">Access to the macOS Keychain uses the applications identity as the primary key when deciding which partition to consider.</span></span> <span data-ttu-id="e080b-145">Nepodepsané aplikace například ukládají tajné klíče do nepodepsaného oddílu, ale podepsané aplikace ukládají jejich tajné klíče pouze do oddílů, ke kterým mají přístup.</span><span class="sxs-lookup"><span data-stu-id="e080b-145">For example, unsigned applications store secrets in the unsigned partition, but signed applications store their secrets in partitions only they can access.</span></span> <span data-ttu-id="e080b-146">Zdroj spuštění, který vyvolá aplikaci, rozhoduje o tom, který oddíl se má použít.</span><span class="sxs-lookup"><span data-stu-id="e080b-146">The source of execution that invokes your app decides which partition to use.</span></span>

<span data-ttu-id="e080b-147">.NET Core poskytuje tři zdroje spuštění: [appHost](#apphost-is-disabled-by-default), výchozí hostitel ( `dotnet` příkaz) a vlastní hostitel.</span><span class="sxs-lookup"><span data-stu-id="e080b-147">.NET Core provides three sources of execution: [appHost](#apphost-is-disabled-by-default), default host (the `dotnet` command), and a custom host.</span></span> <span data-ttu-id="e080b-148">Každý model spuštění může mít různé identity, buď podepsané, nebo bez znaménka, a má přístup k různým oddílům v řetězci klíčů.</span><span class="sxs-lookup"><span data-stu-id="e080b-148">Each execution model may have different identities, either signed or unsigned, and has access to different partitions within the Keychain.</span></span> <span data-ttu-id="e080b-149">Certifikáty naimportované v jednom režimu nemusí být dostupné z jiné.</span><span class="sxs-lookup"><span data-stu-id="e080b-149">Certificates imported by one mode may not be accessible from another.</span></span> <span data-ttu-id="e080b-150">Například verze notarized .NET Core mají výchozího hostitele, který je podepsaný.</span><span class="sxs-lookup"><span data-stu-id="e080b-150">For example, the notarized versions of .NET Core have a default host that is signed.</span></span> <span data-ttu-id="e080b-151">Certifikáty se importují do zabezpečeného oddílu na základě jeho identity.</span><span class="sxs-lookup"><span data-stu-id="e080b-151">Certificates are imported into a secure partition based on its identity.</span></span> <span data-ttu-id="e080b-152">Tyto certifikáty nejsou přístupné z vygenerovaných appHost, protože appHost je bez znaménka.</span><span class="sxs-lookup"><span data-stu-id="e080b-152">These certificates aren't accessible from a generated appHost, as the appHost is unsigned.</span></span>

<span data-ttu-id="e080b-153">Ve výchozím nastavení například ASP.NET Core importuje výchozí certifikát SSL prostřednictvím výchozího hostitele.</span><span class="sxs-lookup"><span data-stu-id="e080b-153">Another example, by default, ASP.NET Core imports a default SSL certificate through the default host.</span></span> <span data-ttu-id="e080b-154">ASP.NET Core aplikace, které používají appHost, nebudou mít přístup k tomuto certifikátu a zobrazí se chyba, když .NET Core zjistí, že certifikát není přístupný.</span><span class="sxs-lookup"><span data-stu-id="e080b-154">ASP.NET Core applications that use an appHost won't have access to this certificate and will receive an error when .NET Core detects the certificate isn't accessible.</span></span> <span data-ttu-id="e080b-155">Chybová zpráva poskytuje pokyny k vyřešení tohoto problému.</span><span class="sxs-lookup"><span data-stu-id="e080b-155">The error message provides instructions on how to fix this problem.</span></span>

<span data-ttu-id="e080b-156">Pokud je vyžadováno sdílení certifikátů, macOS poskytuje možnosti konfigurace pomocí `security` nástroje.</span><span class="sxs-lookup"><span data-stu-id="e080b-156">If certificate sharing is required, macOS provides configuration options with the `security` utility.</span></span>

<span data-ttu-id="e080b-157">Další informace o řešení problémů s certifikátem ASP.NET Core najdete v tématu vystavení [protokolu HTTPS v ASP.NET Core](/aspnet/core/security/enforcing-ssl?view=aspnetcore-3.1&tabs=visual-studio#troubleshoot-certificate-problems).</span><span class="sxs-lookup"><span data-stu-id="e080b-157">For more information on how to troubleshoot ASP.NET Core certificate issues, see [Enforce HTTPS in ASP.NET Core](/aspnet/core/security/enforcing-ssl?view=aspnetcore-3.1&tabs=visual-studio#troubleshoot-certificate-problems).</span></span>

## <a name="default-entitlements"></a><span data-ttu-id="e080b-158">Výchozí nároky</span><span class="sxs-lookup"><span data-stu-id="e080b-158">Default entitlements</span></span>

<span data-ttu-id="e080b-159">Výchozí hostitel .NET Core ( `dotnet` příkaz) má sadu výchozích nároků.</span><span class="sxs-lookup"><span data-stu-id="e080b-159">.NET Core's default host (the `dotnet` command) has a set of default entitlements.</span></span> <span data-ttu-id="e080b-160">Tato oprávnění se vyžadují ke správnému fungování .NET Core.</span><span class="sxs-lookup"><span data-stu-id="e080b-160">These entitlements are required for proper operation of .NET Core.</span></span> <span data-ttu-id="e080b-161">Je možné, že vaše aplikace bude potřebovat další nároky. v takovém případě budete muset vygenerovat a používat [appHost](#apphost-is-disabled-by-default) a potom přidat potřebná oprávnění místně.</span><span class="sxs-lookup"><span data-stu-id="e080b-161">It's possible that your application may need additional entitlements, in which case you'll need to generate and use an [appHost](#apphost-is-disabled-by-default) and then add the necessary entitlements locally.</span></span>

<span data-ttu-id="e080b-162">Výchozí sada nároků pro .NET Core:</span><span class="sxs-lookup"><span data-stu-id="e080b-162">Default set of entitlements for .NET Core:</span></span>

- `com.apple.security.cs.allow-jit`
- `com.apple.security.cs.allow-unsigned-executable-memory`
- `com.apple.security.cs.allow-dyld-environment-variables`
- `com.apple.security.cs.disable-library-validation`

## <a name="notarize-a-net-core-app"></a><span data-ttu-id="e080b-163">Notarize aplikace .NET Core</span><span class="sxs-lookup"><span data-stu-id="e080b-163">Notarize a .NET Core app</span></span>

<span data-ttu-id="e080b-164">Pokud chcete, aby vaše aplikace běžela na macOS Catalina (verze 10,15) nebo vyšší, budete chtít notarize aplikaci.</span><span class="sxs-lookup"><span data-stu-id="e080b-164">If you want your application to run on macOS Catalina (version 10.15) or higher, you'll want to notarize your app.</span></span> <span data-ttu-id="e080b-165">AppHost, které odesíláte s vaší aplikací pro notarization, by se měla použít s alespoň stejnými [výchozími nároky](#default-entitlements) pro .NET Core.</span><span class="sxs-lookup"><span data-stu-id="e080b-165">The appHost you submit with your application for notarization should be used with at least the same [default entitlements](#default-entitlements) for .NET Core.</span></span>

## <a name="next-steps"></a><span data-ttu-id="e080b-166">Další kroky</span><span class="sxs-lookup"><span data-stu-id="e080b-166">Next steps</span></span>

- <span data-ttu-id="e080b-167">[Závislosti a požadavky rozhraní .NET Core](macos.md#dependencies).</span><span class="sxs-lookup"><span data-stu-id="e080b-167">[.NET Core dependencies and requirements](macos.md#dependencies).</span></span>
- <span data-ttu-id="e080b-168">[Nainstalujte modul runtime .NET Core a sadu SDK](macos.md).</span><span class="sxs-lookup"><span data-stu-id="e080b-168">[Install the .NET Core Runtime and SDK](macos.md).</span></span>
