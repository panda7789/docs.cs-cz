---
title: Publikování aplikací .NET Core pomocí rozhraní příkazového řádku
description: Naučte se publikovat aplikaci .NET Core pomocí nástrojů rozhraní příkazového řádku .NET Core SDK (CLI).
author: thraka
ms.author: adegeo
ms.date: 01/16/2019
dev_langs:
- csharp
- vb
ms.custom: seodec18
ms.openlocfilehash: 41af1c708a264833f1f7217529b5c0206d405449
ms.sourcegitcommit: 9a39f2a06f110c9c7ca54ba216900d038aa14ef3
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 11/23/2019
ms.locfileid: "74428910"
---
# <a name="publish-net-core-apps-with-the-cli"></a><span data-ttu-id="3f4dd-103">Publikování aplikací .NET Core pomocí rozhraní příkazového řádku</span><span class="sxs-lookup"><span data-stu-id="3f4dd-103">Publish .NET Core apps with the CLI</span></span>

<span data-ttu-id="3f4dd-104">Tento článek ukazuje, jak můžete publikovat aplikaci .NET Core z příkazového řádku.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-104">This article demonstrates how you can publish your .NET Core application from the command line.</span></span> <span data-ttu-id="3f4dd-105">.NET Core nabízí tři způsoby, jak publikovat aplikace.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-105">.NET Core provides three ways to publish your applications.</span></span> <span data-ttu-id="3f4dd-106">Nasazení závislé na rozhraní vytvoří soubor. dll pro více platforem, který používá místně instalovaný modul runtime .NET Core.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-106">Framework-dependent deployment produces a cross-platform .dll file that uses the locally installed .NET Core runtime.</span></span> <span data-ttu-id="3f4dd-107">Spustitelný soubor závislý na rozhraní vytvoří spustitelný soubor specifický pro platformu, který používá místně instalovaný modul runtime .NET Core.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-107">Framework-dependent executable produces a platform-specific executable that uses the locally installed .NET Core runtime.</span></span> <span data-ttu-id="3f4dd-108">Samostatně obsažený spustitelný soubor vytvoří spustitelný soubor specifický pro platformu a obsahuje místní kopii modulu runtime .NET Core.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-108">Self-contained executable produces a platform-specific executable and includes a local copy of the .NET Core runtime.</span></span>

<span data-ttu-id="3f4dd-109">Přehled těchto režimů publikování najdete v tématu [nasazení aplikace .NET Core](index.md).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-109">For an overview of these publishing modes, see [.NET Core Application Deployment](index.md).</span></span>

<span data-ttu-id="3f4dd-110">Hledáte nějakou rychlou nápovědu k používání rozhraní příkazového řádku?</span><span class="sxs-lookup"><span data-stu-id="3f4dd-110">Looking for some quick help on using the CLI?</span></span> <span data-ttu-id="3f4dd-111">V následující tabulce jsou uvedeny některé příklady publikování aplikace.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-111">The following table shows some examples of how to publish your app.</span></span> <span data-ttu-id="3f4dd-112">Můžete zadat cílovou architekturu s parametrem `-f <TFM>` nebo úpravou souboru projektu.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-112">You can specify the target framework with the `-f <TFM>` parameter or by editing the project file.</span></span> <span data-ttu-id="3f4dd-113">Další informace najdete v tématu [základy publikování](#publishing-basics).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-113">For more information, see [Publishing basics](#publishing-basics).</span></span>

| <span data-ttu-id="3f4dd-114">Režim publikování</span><span class="sxs-lookup"><span data-stu-id="3f4dd-114">Publish Mode</span></span> | <span data-ttu-id="3f4dd-115">Verze sady SDK</span><span class="sxs-lookup"><span data-stu-id="3f4dd-115">SDK Version</span></span> | <span data-ttu-id="3f4dd-116">Příkaz</span><span class="sxs-lookup"><span data-stu-id="3f4dd-116">Command</span></span> |
| ------------ | ----------- | ------- |
| <span data-ttu-id="3f4dd-117">Nasazení závisí na architektuře</span><span class="sxs-lookup"><span data-stu-id="3f4dd-117">Framework-dependent deployment</span></span> | <span data-ttu-id="3f4dd-118">2.x</span><span class="sxs-lookup"><span data-stu-id="3f4dd-118">2.x</span></span> | `dotnet publish -c Release` |
| <span data-ttu-id="3f4dd-119">Spustitelný soubor závislý na rozhraní</span><span class="sxs-lookup"><span data-stu-id="3f4dd-119">Framework-dependent executable</span></span> | <span data-ttu-id="3f4dd-120">2.2</span><span class="sxs-lookup"><span data-stu-id="3f4dd-120">2.2</span></span> | `dotnet publish -c Release -r <RID> --self-contained false` |
|                                | <span data-ttu-id="3f4dd-121">3.0</span><span class="sxs-lookup"><span data-stu-id="3f4dd-121">3.0</span></span> | `dotnet publish -c Release -r <RID> --self-contained false` |
|                                | <span data-ttu-id="3f4dd-122">3,0 \*</span><span class="sxs-lookup"><span data-stu-id="3f4dd-122">3.0\*</span></span> | `dotnet publish -c Release` |
| <span data-ttu-id="3f4dd-123">Samostatná nasazení</span><span class="sxs-lookup"><span data-stu-id="3f4dd-123">Self-contained deployment</span></span>      | <span data-ttu-id="3f4dd-124">2.1</span><span class="sxs-lookup"><span data-stu-id="3f4dd-124">2.1</span></span> | `dotnet publish -c Release -r <RID> --self-contained true` |
|                                | <span data-ttu-id="3f4dd-125">2.2</span><span class="sxs-lookup"><span data-stu-id="3f4dd-125">2.2</span></span> | `dotnet publish -c Release -r <RID> --self-contained true` |
|                                | <span data-ttu-id="3f4dd-126">3.0</span><span class="sxs-lookup"><span data-stu-id="3f4dd-126">3.0</span></span> | `dotnet publish -c Release -r <RID> --self-contained true` |

<span data-ttu-id="3f4dd-127">\* při použití sady SDK verze 3,0 je spustitelný soubor závislý na architektuře výchozím režimem publikování při spuštění příkazu Basic `dotnet publish`.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-127">\* When using SDK version 3.0, framework-dependent executable is the default publishing mode when running the basic `dotnet publish` command.</span></span> <span data-ttu-id="3f4dd-128">To platí pouze v případě, že projekt cílí na **rozhraní .NET core 2,1** nebo **.NET Core 3,0**.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-128">This only applies when the project targets either **.NET Core 2.1** or **.NET Core 3.0**.</span></span>

## <a name="publishing-basics"></a><span data-ttu-id="3f4dd-129">Základy publikování</span><span class="sxs-lookup"><span data-stu-id="3f4dd-129">Publishing basics</span></span>

<span data-ttu-id="3f4dd-130">Nastavení `<TargetFramework>` souboru projektu určuje výchozí cílovou architekturu při publikování aplikace.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-130">The `<TargetFramework>` setting of the project file specifies the default target framework when you publish your app.</span></span> <span data-ttu-id="3f4dd-131">Můžete změnit cílovou architekturu na libovolný platný [cílový moniker rozhraní (TFM)](../../standard/frameworks.md).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-131">You can change the target framework to any valid [Target Framework Moniker (TFM)](../../standard/frameworks.md).</span></span> <span data-ttu-id="3f4dd-132">Například pokud váš projekt používá `<TargetFramework>netcoreapp2.2</TargetFramework>`, je vytvořen binární soubor, který cílí na .NET Core 2,2.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-132">For example, if your project uses `<TargetFramework>netcoreapp2.2</TargetFramework>`, a binary that targets .NET Core 2.2 is created.</span></span> <span data-ttu-id="3f4dd-133">TFM zadané v tomto nastavení je výchozí cíl, který používá příkaz [`dotnet publish`](../tools/dotnet-publish.md) .</span><span class="sxs-lookup"><span data-stu-id="3f4dd-133">The TFM specified in this setting is the default target used by the [`dotnet publish`](../tools/dotnet-publish.md) command.</span></span>

<span data-ttu-id="3f4dd-134">Pokud chcete cílit na více než jednu architekturu, můžete nastavit `<TargetFrameworks>` nastavení na více než jednu hodnotu TFM oddělenou středníkem.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-134">If you want to target more than one framework, you can set the `<TargetFrameworks>` setting to more than one TFM value separated by a semicolon.</span></span> <span data-ttu-id="3f4dd-135">Můžete publikovat jednu z architektur pomocí příkazu `dotnet publish -f <TFM>`.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-135">You can publish one of the frameworks with the `dotnet publish -f <TFM>` command.</span></span> <span data-ttu-id="3f4dd-136">Například pokud máte `<TargetFrameworks>netcoreapp2.1;netcoreapp2.2</TargetFrameworks>` a spustíte `dotnet publish -f netcoreapp2.1`, vytvoří se binární soubor, který cílí na .NET Core 2,1.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-136">For example, if you have `<TargetFrameworks>netcoreapp2.1;netcoreapp2.2</TargetFrameworks>` and run `dotnet publish -f netcoreapp2.1`, a binary that targets .NET Core 2.1 is created.</span></span>

<span data-ttu-id="3f4dd-137">Pokud není nastavena jinak, je výstupní adresář příkazu [`dotnet publish`](../tools/dotnet-publish.md) `./bin/<BUILD-CONFIGURATION>/<TFM>/publish/`.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-137">Unless otherwise set, the output directory of the [`dotnet publish`](../tools/dotnet-publish.md) command is `./bin/<BUILD-CONFIGURATION>/<TFM>/publish/`.</span></span> <span data-ttu-id="3f4dd-138">Výchozí režim **konfigurace sestavení** je **ladění** , pokud se nezmění pomocí parametru `-c`.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-138">The default **BUILD-CONFIGURATION** mode is **Debug** unless changed with the `-c` parameter.</span></span> <span data-ttu-id="3f4dd-139">Například `dotnet publish -c Release -f netcoreapp2.1` publikovat do `myfolder/bin/Release/netcoreapp2.1/publish/`.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-139">For example, `dotnet publish -c Release -f netcoreapp2.1` publishes to `myfolder/bin/Release/netcoreapp2.1/publish/`.</span></span>

<span data-ttu-id="3f4dd-140">Pokud používáte .NET Core SDK 3,0, výchozím režimem publikování pro aplikace, které cílí na .NET Core verze 2,1, 2,2 nebo 3,0, je spustitelný soubor závislý na rozhraní.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-140">If you use .NET Core SDK 3.0, the default publish mode for apps that target .NET Core versions 2.1, 2.2, or 3.0 is framework-dependent executable.</span></span>

<span data-ttu-id="3f4dd-141">Pokud používáte .NET Core SDK 2,1, výchozím režimem publikování pro aplikace, které cílí na .NET Core verze 2,1, 2,2 je nasazení závislé na rozhraní.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-141">If you use .NET Core SDK 2.1, the default publish mode for apps that target .NET Core versions 2.1, 2.2 is framework-dependent deployment.</span></span>

### <a name="native-dependencies"></a><span data-ttu-id="3f4dd-142">Nativní závislosti</span><span class="sxs-lookup"><span data-stu-id="3f4dd-142">Native dependencies</span></span>

<span data-ttu-id="3f4dd-143">Pokud má vaše aplikace nativní závislosti, nemusí běžet v jiném operačním systému.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-143">If your app has native dependencies, it may not run on a different operating system.</span></span> <span data-ttu-id="3f4dd-144">Pokud vaše aplikace například používá nativní rozhraní API systému Windows, nebude spuštěna na macOS nebo Linux.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-144">For example, if your app uses the native Windows API, it won't run on macOS or Linux.</span></span> <span data-ttu-id="3f4dd-145">Je nutné zadat kód specifický pro platformu a zkompilovat spustitelný soubor pro každou platformu.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-145">You would need to provide platform-specific code and compile an executable for each platform.</span></span>

<span data-ttu-id="3f4dd-146">V případě, že knihovna, na kterou jste odkazovali, má nativní závislost, vaše aplikace nemusí běžet na všech platformách.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-146">Consider also, if a library you referenced has a native dependency, your app may not run on every platform.</span></span> <span data-ttu-id="3f4dd-147">Je však možné, že balíček NuGet, na který odkazujete, obsahuje verze specifické pro platformu, které pro vás pořídí požadované nativní závislosti.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-147">However, it's possible a NuGet package you're referencing has included platform-specific versions to handle the required native dependencies for you.</span></span>

<span data-ttu-id="3f4dd-148">Při distribuci aplikace s nativními závislostmi možná budete muset použít přepínač `dotnet publish -r <RID>` k určení cílové platformy, pro kterou chcete publikovat.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-148">When distributing an app with native dependencies, you may need to use the `dotnet publish -r <RID>` switch to specify the target platform you want to publish for.</span></span> <span data-ttu-id="3f4dd-149">Seznam identifikátorů modulu runtime naleznete v [katalogu identifikátorů modulu runtime (RID)](../rid-catalog.md).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-149">For a list of runtime identifiers, see [Runtime Identifier (RID) catalog](../rid-catalog.md).</span></span>

<span data-ttu-id="3f4dd-150">Další informace o binárních souborech specifických pro konkrétní platformu jsou pokryté [spustitelnými soubory závislými na rozhraní](#framework-dependent-executable) a oddíly [pro nasazení samostatného nasazení](#self-contained-deployment) .</span><span class="sxs-lookup"><span data-stu-id="3f4dd-150">More information about platform-specific binaries is covered in the [Framework-dependent executable](#framework-dependent-executable) and [Self-contained deployment](#self-contained-deployment) sections.</span></span>

## <a name="sample-app"></a><span data-ttu-id="3f4dd-151">Ukázková aplikace</span><span class="sxs-lookup"><span data-stu-id="3f4dd-151">Sample app</span></span>

<span data-ttu-id="3f4dd-152">Pomocí následující aplikace můžete prozkoumat příkazy pro publikování.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-152">You can use the following app to explore the publishing commands.</span></span> <span data-ttu-id="3f4dd-153">Aplikaci vytvoříte spuštěním následujících příkazů v terminálu:</span><span class="sxs-lookup"><span data-stu-id="3f4dd-153">The app is created by running the following commands in your terminal:</span></span>

```dotnetcli
mkdir apptest1
cd apptest1
dotnet new console
dotnet add package Figgle
```

<span data-ttu-id="3f4dd-154">Soubor `Program.cs` nebo `Program.vb` generovaný šablonou konzoly musí být změněn na následující:</span><span class="sxs-lookup"><span data-stu-id="3f4dd-154">The `Program.cs` or `Program.vb` file that is generated by the console template needs to be changed to the following:</span></span>

```csharp
using System;

namespace apptest1
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine(Figgle.FiggleFonts.Standard.Render("Hello, World!"));
        }
    }
}
```

```vb
Module Program
    Sub Main(args As String())
        Console.WriteLine(Figgle.FiggleFonts.Standard.Render("Hello, World!"))
    End Sub
End Module
```

<span data-ttu-id="3f4dd-155">Při spuštění aplikace ([`dotnet run`](../tools/dotnet-run.md)) se zobrazí následující výstup:</span><span class="sxs-lookup"><span data-stu-id="3f4dd-155">When you run the app ([`dotnet run`](../tools/dotnet-run.md)), the following output is displayed:</span></span>

```terminal
  _   _      _ _         __        __         _     _ _
 | | | | ___| | | ___    \ \      / /__  _ __| | __| | |
 | |_| |/ _ \ | |/ _ \    \ \ /\ / / _ \| '__| |/ _` | |
 |  _  |  __/ | | (_) |    \ V  V / (_) | |  | | (_| |_|
 |_| |_|\___|_|_|\___( )    \_/\_/ \___/|_|  |_|\__,_(_)
                     |/
```

## <a name="framework-dependent-deployment"></a><span data-ttu-id="3f4dd-156">Nasazení závisí na architektuře</span><span class="sxs-lookup"><span data-stu-id="3f4dd-156">Framework-dependent deployment</span></span>

<span data-ttu-id="3f4dd-157">Pro .NET Core SDK 2. x CLI, nasazení závislé na rozhraní (FDD) je výchozím režimem pro příkaz Basic `dotnet publish`.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-157">For the .NET Core SDK 2.x CLI, framework-dependent deployment (FDD) is the default mode for the basic `dotnet publish` command.</span></span>

<span data-ttu-id="3f4dd-158">Když aplikaci publikujete jako FDD, vytvoří se soubor `<PROJECT-NAME>.dll` ve složce `./bin/<BUILD-CONFIGURATION>/<TFM>/publish/`.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-158">When you publish your app as an FDD, a `<PROJECT-NAME>.dll` file is created in the `./bin/<BUILD-CONFIGURATION>/<TFM>/publish/` folder.</span></span> <span data-ttu-id="3f4dd-159">Chcete-li spustit aplikaci, přejděte do výstupní složky a použijte příkaz `dotnet <PROJECT-NAME>.dll`.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-159">To run your app, navigate to the output folder and use the `dotnet <PROJECT-NAME>.dll` command.</span></span>

<span data-ttu-id="3f4dd-160">Vaše aplikace je nakonfigurovaná tak, aby byla cílena na konkrétní verzi .NET Core.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-160">Your app is configured to target a specific version of .NET Core.</span></span> <span data-ttu-id="3f4dd-161">Cílový modul runtime .NET Core musí být v počítači, na kterém chcete aplikaci spustit.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-161">That targeted .NET Core runtime is required to be on the machine where you want to run your app.</span></span> <span data-ttu-id="3f4dd-162">Například pokud vaše aplikace cílí na .NET Core 2,2, musí mít každý počítač, na kterém je aplikace spuštěná, nainstalovaný modul .NET Core 2,2 Runtime.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-162">For example, if your app targets .NET Core 2.2, any machine that your app runs on must have the .NET Core 2.2 runtime installed.</span></span> <span data-ttu-id="3f4dd-163">Jak je uvedeno v části [základy publikování](#publishing-basics) , můžete upravit soubor projektu pro změnu výchozí cílové architektury nebo pro cílení na více než jedno rozhraní.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-163">As stated in the [Publishing basics](#publishing-basics) section, you can edit your project file to change the default target framework or to target more than one framework.</span></span>

<span data-ttu-id="3f4dd-164">Publikováním FDD se vytvoří aplikace, která se automaticky přenese do nejnovější opravy zabezpečení .NET Core, která je dostupná v systému, ve kterém je spuštěná aplikace.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-164">Publishing an FDD creates an app that automatically rolls-forward to the latest .NET Core security patch available on the system that runs the app.</span></span> <span data-ttu-id="3f4dd-165">Další informace o vazbách verzí v době kompilace naleznete v tématu [Vyberte verzi rozhraní .NET Core, která se má použít](../versions/selection.md#framework-dependent-apps-roll-forward).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-165">For more information on version binding at compile time, see [Select the .NET Core version to use](../versions/selection.md#framework-dependent-apps-roll-forward).</span></span>

## <a name="framework-dependent-executable"></a><span data-ttu-id="3f4dd-166">Spustitelný soubor závislý na rozhraní</span><span class="sxs-lookup"><span data-stu-id="3f4dd-166">Framework-dependent executable</span></span>

<span data-ttu-id="3f4dd-167">Pro .NET Core SDK 3. x CLI, spustitelný soubor závislý na rozhraní (FDE) je výchozím režimem pro základní `dotnet publish` příkaz.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-167">For the .NET Core SDK 3.x CLI, framework-dependent executable (FDE) is the default mode for the basic `dotnet publish` command.</span></span> <span data-ttu-id="3f4dd-168">Není nutné zadávat žádné další parametry, pokud chcete cílit na aktuální operační systém.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-168">You don't need to specify any other parameters as long as you want to target the current operating system.</span></span>

<span data-ttu-id="3f4dd-169">V tomto režimu se pro hostování aplikace pro různé platformy vytvoří spustitelný hostitel specifický pro platformu.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-169">In this mode, a platform-specific executable host is created to host your cross-platform app.</span></span> <span data-ttu-id="3f4dd-170">Tento režim je podobný jako FDD, protože FDD vyžaduje hostitele ve formě `dotnet`ho příkazu.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-170">This mode is similar to FDD as FDD requires a host in the form of the `dotnet` command.</span></span> <span data-ttu-id="3f4dd-171">Název spustitelného souboru hostitele se liší podle platformy a má název podobný `<PROJECT-FILE>.exe`.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-171">The host executable filename varies per platform, and is named something similar to `<PROJECT-FILE>.exe`.</span></span> <span data-ttu-id="3f4dd-172">Tento spustitelný soubor můžete spustit přímo místo volání `dotnet <PROJECT-FILE>.dll`, což je stále přijatelný způsob, jak aplikaci spustit.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-172">You can run this executable directly instead of calling `dotnet <PROJECT-FILE>.dll` which is still an acceptable way to run the app.</span></span>

<span data-ttu-id="3f4dd-173">Vaše aplikace je nakonfigurovaná tak, aby byla cílena na konkrétní verzi .NET Core.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-173">Your app is configured to target a specific version of .NET Core.</span></span> <span data-ttu-id="3f4dd-174">Cílový modul runtime .NET Core musí být v počítači, na kterém chcete aplikaci spustit.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-174">That targeted .NET Core runtime is required to be on the machine where you want to run your app.</span></span> <span data-ttu-id="3f4dd-175">Například pokud vaše aplikace cílí na .NET Core 2,2, musí mít každý počítač, na kterém je aplikace spuštěná, nainstalovaný modul .NET Core 2,2 Runtime.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-175">For example, if your app targets .NET Core 2.2, any machine that your app runs on must have the .NET Core 2.2 runtime installed.</span></span> <span data-ttu-id="3f4dd-176">Jak je uvedeno v části [základy publikování](#publishing-basics) , můžete upravit soubor projektu pro změnu výchozí cílové architektury nebo pro cílení na více než jedno rozhraní.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-176">As stated in the [Publishing basics](#publishing-basics) section, you can edit your project file to change the default target framework or to target more than one framework.</span></span>

<span data-ttu-id="3f4dd-177">Publikováním FDE se vytvoří aplikace, která se automaticky přenese na nejnovější aktualizaci zabezpečení .NET Core dostupnou v systému, ve kterém je aplikace spuštěná.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-177">Publishing an FDE creates an app that automatically rolls-forward to the latest .NET Core security patch available on the system that runs the app.</span></span> <span data-ttu-id="3f4dd-178">Další informace o vazbách verzí v době kompilace naleznete v tématu [Vyberte verzi rozhraní .NET Core, která se má použít](../versions/selection.md#framework-dependent-apps-roll-forward).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-178">For more information on version binding at compile time, see [Select the .NET Core version to use](../versions/selection.md#framework-dependent-apps-roll-forward).</span></span>

<span data-ttu-id="3f4dd-179">Je nutné (kromě .NET Core 3. x při cílení na aktuální platformu) použít následující přepínače s příkazem `dotnet publish` k publikování FDE:</span><span class="sxs-lookup"><span data-stu-id="3f4dd-179">You must (except for .NET Core 3.x when you target the current platform) use the following switches with the `dotnet publish` command to publish an FDE:</span></span>

- <span data-ttu-id="3f4dd-180">`-r <RID>` tento přepínač pomocí identifikátoru (RID) určí cílovou platformu.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-180">`-r <RID>` This switch uses an identifier (RID) to specify the target platform.</span></span> <span data-ttu-id="3f4dd-181">Seznam identifikátorů modulu runtime naleznete v [katalogu identifikátorů modulu runtime (RID)](../rid-catalog.md).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-181">For a list of runtime identifiers, see [Runtime Identifier (RID) catalog](../rid-catalog.md).</span></span>

- <span data-ttu-id="3f4dd-182">`--self-contained false` tento přepínač instruuje .NET Core SDK, aby vytvořil spustitelný soubor jako FDE.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-182">`--self-contained false` This switch tells the .NET Core SDK to create an executable as an FDE.</span></span>

<span data-ttu-id="3f4dd-183">Vždy, když použijete přepínač `-r`, cesta ke výstupní složce se změní na: `./bin/<BUILD-CONFIGURATION>/<TFM>/<RID>/publish/`</span><span class="sxs-lookup"><span data-stu-id="3f4dd-183">Whenever you use the `-r` switch, the output folder path changes to: `./bin/<BUILD-CONFIGURATION>/<TFM>/<RID>/publish/`</span></span>

<span data-ttu-id="3f4dd-184">Pokud používáte [ukázkovou aplikaci](#sample-app), spusťte `dotnet publish -f netcoreapp2.2 -r win10-x64 --self-contained false`.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-184">If you use the [example app](#sample-app), run `dotnet publish -f netcoreapp2.2 -r win10-x64 --self-contained false`.</span></span> <span data-ttu-id="3f4dd-185">Tento příkaz vytvoří následující spustitelný soubor: `./bin/Debug/netcoreapp2.2/win10-x64/publish/apptest1.exe`</span><span class="sxs-lookup"><span data-stu-id="3f4dd-185">This command creates the following executable: `./bin/Debug/netcoreapp2.2/win10-x64/publish/apptest1.exe`</span></span>

> [!NOTE]
> <span data-ttu-id="3f4dd-186">Celkovou velikost nasazení můžete snížit povolením **režimu invariantování globalizace**.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-186">You can reduce the total size of your deployment by enabling **globalization invariant mode**.</span></span> <span data-ttu-id="3f4dd-187">Tento režim je vhodný pro aplikace, které nejsou globálně závislé a které mohou použít konvence formátování, konvence velikosti písmen a pořadí řazení [neutrální jazykové verze](xref:System.Globalization.CultureInfo.InvariantCulture).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-187">This mode is useful for applications that are not globally aware and that can use the formatting conventions, casing conventions, and string comparison and sort order of the [invariant culture](xref:System.Globalization.CultureInfo.InvariantCulture).</span></span> <span data-ttu-id="3f4dd-188">Další informace o **invariantní režimu globalizace** a o tom, jak ho povolit, najdete v tématu [Nevariantní režim globalizace .NET Core](https://github.com/dotnet/corefx/blob/master/Documentation/architecture/globalization-invariant-mode.md).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-188">For more information about **globalization invariant mode** and how to enable it, see [.NET Core Globalization Invariant Mode](https://github.com/dotnet/corefx/blob/master/Documentation/architecture/globalization-invariant-mode.md).</span></span>

## <a name="self-contained-deployment"></a><span data-ttu-id="3f4dd-189">Samostatná nasazení</span><span class="sxs-lookup"><span data-stu-id="3f4dd-189">Self-contained deployment</span></span>

<span data-ttu-id="3f4dd-190">Při publikování samostatného nasazení (SCD) vytvoří .NET Core SDK spustitelný soubor specifický pro platformu.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-190">When you publish a self-contained deployment (SCD), the .NET Core SDK creates a platform-specific executable.</span></span> <span data-ttu-id="3f4dd-191">Publikování SCD zahrnuje všechny požadované soubory .NET Core ke spuštění vaší aplikace, ale nezahrnuje [nativní závislosti rozhraní .NET Core](https://github.com/dotnet/core/blob/master/Documentation/prereqs.md).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-191">Publishing an SCD includes all required .NET Core files to run your app but it doesn't include the [native dependencies of .NET Core](https://github.com/dotnet/core/blob/master/Documentation/prereqs.md).</span></span> <span data-ttu-id="3f4dd-192">Tyto závislosti musí být v systému před spuštěním aplikace přítomny.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-192">These dependencies must be present on the system before the app runs.</span></span>

<span data-ttu-id="3f4dd-193">Publikování SCD vytvoří aplikaci, která nepřevádí zpět na nejnovější dostupnou opravu zabezpečení .NET Core.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-193">Publishing an SCD creates an app that doesn't roll-forward to the latest available .NET Core security patch.</span></span> <span data-ttu-id="3f4dd-194">Další informace o vazbách verzí v době kompilace naleznete v tématu [Vyberte verzi rozhraní .NET Core, která se má použít](../versions/selection.md#self-contained-deployments-include-the-selected-runtime).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-194">For more information on version binding at compile time, see [Select the .NET Core version to use](../versions/selection.md#self-contained-deployments-include-the-selected-runtime).</span></span>

<span data-ttu-id="3f4dd-195">K publikování SCD je nutné použít následující přepínače s příkazem `dotnet publish`:</span><span class="sxs-lookup"><span data-stu-id="3f4dd-195">You must use the following switches with the `dotnet publish` command to publish an SCD:</span></span>

- <span data-ttu-id="3f4dd-196">`-r <RID>` tento přepínač pomocí identifikátoru (RID) určí cílovou platformu.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-196">`-r <RID>` This switch uses an identifier (RID) to specify the target platform.</span></span> <span data-ttu-id="3f4dd-197">Seznam identifikátorů modulu runtime naleznete v [katalogu identifikátorů modulu runtime (RID)](../rid-catalog.md).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-197">For a list of runtime identifiers, see [Runtime Identifier (RID) catalog](../rid-catalog.md).</span></span>

- <span data-ttu-id="3f4dd-198">`--self-contained true` tento přepínač instruuje .NET Core SDK, aby vytvořil spustitelný soubor jako SCD.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-198">`--self-contained true` This switch tells the .NET Core SDK to create an executable as an SCD.</span></span>

> [!NOTE]
> <span data-ttu-id="3f4dd-199">Celkovou velikost nasazení můžete snížit povolením **režimu invariantování globalizace**.</span><span class="sxs-lookup"><span data-stu-id="3f4dd-199">You can reduce the total size of your deployment by enabling **globalization invariant mode**.</span></span> <span data-ttu-id="3f4dd-200">Tento režim je vhodný pro aplikace, které nejsou globálně závislé a které mohou použít konvence formátování, konvence velikosti písmen a pořadí řazení [neutrální jazykové verze](xref:System.Globalization.CultureInfo.InvariantCulture).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-200">This mode is useful for applications that are not globally aware and that can use the formatting conventions, casing conventions, and string comparison and sort order of the [invariant culture](xref:System.Globalization.CultureInfo.InvariantCulture).</span></span> <span data-ttu-id="3f4dd-201">Další informace o **invariantní režimu globalizace** a o tom, jak ho povolit, najdete v tématu [Nevariantní režim globalizace .NET Core](https://github.com/dotnet/corefx/blob/master/Documentation/architecture/globalization-invariant-mode.md).</span><span class="sxs-lookup"><span data-stu-id="3f4dd-201">For more information about **globalization invariant mode** and how to enable it, see [.NET Core Globalization Invariant Mode](https://github.com/dotnet/corefx/blob/master/Documentation/architecture/globalization-invariant-mode.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="3f4dd-202">Viz také:</span><span class="sxs-lookup"><span data-stu-id="3f4dd-202">See also</span></span>

- [<span data-ttu-id="3f4dd-203">Přehled nasazení aplikace .NET Core</span><span class="sxs-lookup"><span data-stu-id="3f4dd-203">.NET Core Application Deployment Overview</span></span>](index.md)
- [<span data-ttu-id="3f4dd-204">Katalog identifikátorů runtime .NET Core (RID)</span><span class="sxs-lookup"><span data-stu-id="3f4dd-204">.NET Core Runtime IDentifier (RID) catalog</span></span>](../rid-catalog.md)
