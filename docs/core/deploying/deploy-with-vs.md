---
title: Nasazení aplikace .NET core pomocí sady Visual Studio
description: Další nasazení aplikace .NET Core pomocí sady Visual Studio
author: rpetrusha
ms.author: ronpet
ms.date: 04/18/2017
ms.topic: conceptual
ms.prod: dotnet-core
ms.devlang: dotnet
ms.workload:
- dotnetcore
ms.openlocfilehash: 238e43149eebb59ecbb25dfc3976f912e0ae8b01
ms.sourcegitcommit: 03ee570f6f528a7d23a4221dcb26a9498edbdf8c
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/28/2018
---
# <a name="deploying-net-core-apps-with-visual-studio"></a><span data-ttu-id="a5c2c-103">Nasazení .NET základní aplikace pomocí sady Visual Studio</span><span class="sxs-lookup"><span data-stu-id="a5c2c-103">Deploying .NET Core apps with Visual Studio</span></span>

<span data-ttu-id="a5c2c-104">Můžete nasadit aplikaci .NET Core buď jako *nasazení závislé na framework*, který obsahuje binární soubory vaší aplikace, ale závisí na přítomnost .NET Core v cílovém systému, nebo jako *samostatný nasazení*, což zahrnuje aplikace a binární soubory .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-104">You can deploy a .NET Core application either as a *framework-dependent deployment*, which includes your application binaries but depends on the presence of .NET Core on the target system, or as a *self-contained deployment*, which includes both your application and .NET Core binaries.</span></span> <span data-ttu-id="a5c2c-105">Přehled nasazení aplikace .NET Core najdete v tématu [nasazení aplikace .NET Core](index.md).</span><span class="sxs-lookup"><span data-stu-id="a5c2c-105">For an overview of .NET Core application deployment, see [.NET Core Application Deployment](index.md).</span></span>

<span data-ttu-id="a5c2c-106">Následující části vysvětlují, jak pomocí sady Microsoft Visual Studio vytvořit následující typy nasazení:</span><span class="sxs-lookup"><span data-stu-id="a5c2c-106">The following sections show how to use Microsoft Visual Studio to create the following kinds of deployments:</span></span>

- <span data-ttu-id="a5c2c-107">Nasazení závislé na Framework</span><span class="sxs-lookup"><span data-stu-id="a5c2c-107">Framework-dependent deployment</span></span>
- <span data-ttu-id="a5c2c-108">Nasazení závislé na Framework se závislostmi třetích stran</span><span class="sxs-lookup"><span data-stu-id="a5c2c-108">Framework-dependent deployment with third-party dependencies</span></span>
- <span data-ttu-id="a5c2c-109">Samostatná nasazení</span><span class="sxs-lookup"><span data-stu-id="a5c2c-109">Self-contained deployment</span></span>
- <span data-ttu-id="a5c2c-110">Samostatná nasazení se závislostmi třetích stran</span><span class="sxs-lookup"><span data-stu-id="a5c2c-110">Self-contained deployment with third-party dependencies</span></span>

<span data-ttu-id="a5c2c-111">Informace o vývoji aplikací .NET Core pomocí sady Visual Studio najdete v tématu [požadavky pro .NET Core v systému Windows](../windows-prerequisites.md#prerequisites-with-visual-studio-2017).</span><span class="sxs-lookup"><span data-stu-id="a5c2c-111">For information on using Visual Studio to develop .NET Core applications, see [Prerequisites for .NET Core on Windows](../windows-prerequisites.md#prerequisites-with-visual-studio-2017).</span></span>

## <a name="framework-dependent-deployment"></a><span data-ttu-id="a5c2c-112">Nasazení závislé na Framework</span><span class="sxs-lookup"><span data-stu-id="a5c2c-112">Framework-dependent deployment</span></span>

<span data-ttu-id="a5c2c-113">Nasazení závislé na framework nasazení nemá žádné závislosti třetích stran zahrnuje vytváření, testování a publikování aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-113">Deploying a framework-dependent deployment with no third-party dependencies simply involves building, testing, and publishing the app.</span></span> <span data-ttu-id="a5c2c-114">Jednoduchý příklad napsané v C# znázorňuje proces.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-114">A simple example written in C# illustrates the process.</span></span>  

1. <span data-ttu-id="a5c2c-115">Vytvoření projektu.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-115">Create the project.</span></span>

   <span data-ttu-id="a5c2c-116">Select **File** > **New** > **Project**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-116">Select **File** > **New** > **Project**.</span></span> <span data-ttu-id="a5c2c-117">V **nový projekt** dialogovém okně, vyberte **.NET Core** v **nainstalovaná** typy podokně projektu a vyberte **konzolové aplikace (.NET Core)** Šablona v prostředním podokně.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-117">In the **New Project** dialog, select **.NET Core** in the **Installed** project types pane, and select the **Console App (.NET Core)** template in the center pane.</span></span> <span data-ttu-id="a5c2c-118">Zadejte název projektu, například "Chyba" v **název** textové pole.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-118">Enter a project name, such as "FDD", in the **Name** text box.</span></span> <span data-ttu-id="a5c2c-119">Vyberte **OK** tlačítko.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-119">Select the **OK** button.</span></span>

1. <span data-ttu-id="a5c2c-120">Přidejte zdrojový kód aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-120">Add the application's source code.</span></span>

   <span data-ttu-id="a5c2c-121">Otevřete *Program.cs* souboru v editoru a automaticky vygenerovaný kód nahraďte následujícím kódem.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-121">Open the *Program.cs* file in the editor and replace the auto-generated code with the following code.</span></span> <span data-ttu-id="a5c2c-122">Se zobrazí výzvu k zadání textu a zobrazí jednotlivých slov zadané uživatelem.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-122">It prompts the user to enter text and displays the individual words entered by the user.</span></span> <span data-ttu-id="a5c2c-123">Používá regulární výraz `\w+` slov v vstupního textu.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-123">It uses the regular expression `\w+` to separate the words in the input text.</span></span>

   [!code-csharp[deployment#1](../../../samples/snippets/core/deploying/deployment-example.cs)]

1. <span data-ttu-id="a5c2c-124">Vytvořte sestavení ladicí verze vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-124">Create a Debug build of your app.</span></span>

   <span data-ttu-id="a5c2c-125">Vyberte **sestavení** > **sestavení řešení**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-125">Select **Build** > **Build Solution**.</span></span> <span data-ttu-id="a5c2c-126">Můžete také zkompilování a spuštění ladění sestavení aplikace tak, že vyberete **ladění** > **spustit ladění**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-126">You can also compile and run the Debug build of your application by selecting **Debug** > **Start Debugging**.</span></span>

1. <span data-ttu-id="a5c2c-127">Nasazení aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-127">Deploy your app.</span></span>

   <span data-ttu-id="a5c2c-128">Po ladit a testovat program, vytvoření souborů k nasazení s vaší aplikací.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-128">After you've debugged and tested the program, create the files to be deployed with your app.</span></span> <span data-ttu-id="a5c2c-129">Pokud chcete publikovat ze sady Visual Studio, postupujte takto:</span><span class="sxs-lookup"><span data-stu-id="a5c2c-129">To publish from Visual Studio, do the following:</span></span>

      1. <span data-ttu-id="a5c2c-130">Změňte konfiguraci řešení z **ladění** k **verze** na panelu nástrojů na verzi sestavení a verze (nikoli a ladění) vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-130">Change the solution configuration from **Debug** to **Release** on the toolbar to build a Release (rather than a Debug) version of your app.</span></span>

      1. <span data-ttu-id="a5c2c-131">Klikněte pravým tlačítkem na projekt (ne řešení) v **Průzkumníku řešení**a vyberte **publikovat**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-131">Right-click on the project (not the solution) in **Solution Explorer**, and select **Publish**.</span></span>

      1. <span data-ttu-id="a5c2c-132">V **publikovat** vyberte **publikovat**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-132">In the **Publish** tab, select **Publish**.</span></span> <span data-ttu-id="a5c2c-133">Visual Studio zapíše soubory, které tvoří vaši aplikaci do místního systému souborů.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-133">Visual Studio writes the files that comprise your application to the local file system.</span></span>

      1. <span data-ttu-id="a5c2c-134">**Publikovat** kartě se teď zobrazují jediného profilu, **FolderProfile**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-134">The **Publish** tab now shows a single profile, **FolderProfile**.</span></span> <span data-ttu-id="a5c2c-135">Nastavení profilu konfigurace se zobrazují v **Souhrn** části karty.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-135">The profile's configuration settings are shown in the **Summary** section of the tab.</span></span>

   <span data-ttu-id="a5c2c-136">Výsledné soubory jsou umístěny v adresáři s názvem `PublishOutput` , je v podadresáři vašeho projektu *.\bin\release* podadresáři.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-136">The resulting files are placed in a directory named `PublishOutput` that is in a subdirectory of your project's *.\bin\release* subdirectory.</span></span>

<span data-ttu-id="a5c2c-137">Proces publikování společně se soubory aplikace vysílá soubor databáze (.pdb) program, který obsahuje ladicí informace o vaší aplikaci.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-137">Along with your application's files, the publishing process emits a program database (.pdb) file that contains debugging information about your app.</span></span> <span data-ttu-id="a5c2c-138">Soubor je užitečné především pro ladění výjimek.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-138">The file is useful primarily for debugging exceptions.</span></span> <span data-ttu-id="a5c2c-139">Je možné, není-li vytvořit balíček s soubory aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-139">You can choose not to package it with your application's files.</span></span> <span data-ttu-id="a5c2c-140">Ale uložte jej v případě, že chcete ladit sestavení pro vydání vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-140">You should, however, save it in the event that you want to debug the Release build of your app.</span></span>

<span data-ttu-id="a5c2c-141">Kompletní sadu souborů aplikace žádným způsobem, který chcete nasaďte.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-141">Deploy the complete set of application files in any way you like.</span></span> <span data-ttu-id="a5c2c-142">Například můžete balíček je v souboru Zip, pomocí jednoduché `copy` příkaz nebo nasadit pomocí jakékoli instalačního balíčku podle svého výběru.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-142">For example, you can package them in a Zip file, use a simple `copy` command, or deploy them with any installation package of your choice.</span></span> <span data-ttu-id="a5c2c-143">Po instalaci uživatelů můžete pak spouštět aplikace pomocí `dotnet` příkaz a poskytnutí názvu souboru, jako například `dotnet fdd.dll`.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-143">Once installed, users can then execute your application by using the `dotnet` command and providing the application filename, such as `dotnet fdd.dll`.</span></span>

<span data-ttu-id="a5c2c-144">Kromě binární soubory aplikace instalačním programem vaší by také buď vytvořit balíček instalačního programu sdílený framework nebo kontrolovat předpokladem je jako součást instalace aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-144">In addition to the application binaries, your installer should also either bundle the shared framework installer or check for it as a prerequisite as part of the application installation.</span></span>  <span data-ttu-id="a5c2c-145">Instalace sdílený framework vyžaduje přístup správce nebo nejnižší vzhledem k tomu, že je celého systému.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-145">Installation of the shared framework requires Administrator/root access since it is machine-wide.</span></span>

## <a name="framework-dependent-deployment-with-third-party-dependencies"></a><span data-ttu-id="a5c2c-146">Nasazení závislé na Framework se závislostmi třetích stran</span><span class="sxs-lookup"><span data-stu-id="a5c2c-146">Framework-dependent deployment with third-party dependencies</span></span>

<span data-ttu-id="a5c2c-147">Nasazení nasazení framework závislé na jeden nebo více třetích stran závislosti vyžaduje, aby všechny závislosti projektu k dispozici.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-147">Deploying a framework-dependent deployment with one or more third-party dependencies requires that any dependencies be available to your project.</span></span> <span data-ttu-id="a5c2c-148">Následující kroky jsou požadovány, než můžete sestavit aplikaci:</span><span class="sxs-lookup"><span data-stu-id="a5c2c-148">The following additional steps are required before you can build your app:</span></span>

1. <span data-ttu-id="a5c2c-149">Použití **Správce balíčků NuGet** přidat odkaz na balíček NuGet do projektu; a pokud balíček již není k dispozici v systému, nainstalujte ji.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-149">Use the **NuGet Package Manager** to add a reference to a NuGet package to your project; and if the package is not already available on your system, install it.</span></span> <span data-ttu-id="a5c2c-150">Chcete-li otevřít správce balíčku, vyberte **nástroje** > **Správce balíčků NuGet** > **spravovat balíčky NuGet pro řešení**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-150">To open the package manager, select **Tools** > **NuGet Package Manager** > **Manage NuGet Packages for Solution**.</span></span>

1. <span data-ttu-id="a5c2c-151">Potvrďte, že `Newtonsoft.Json` na vašem systému je nainstalovaná a pokud není, nainstalujte ji.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-151">Confirm that `Newtonsoft.Json` is installed on your system and, if it is not, install it.</span></span> <span data-ttu-id="a5c2c-152">**Nainstalovaná** karta Vypíše seznam balíčků NuGet v systému nainstalována.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-152">The **Installed** tab lists NuGet packages installed on your system.</span></span> <span data-ttu-id="a5c2c-153">Pokud `Newtonsoft.Json` není uveden, vyberte **Procházet** kartě a do vyhledávacího pole zadejte "Newtonsoft.Json".</span><span class="sxs-lookup"><span data-stu-id="a5c2c-153">If `Newtonsoft.Json` is not listed there, select the **Browse** tab and enter "Newtonsoft.Json" in the search box.</span></span> <span data-ttu-id="a5c2c-154">Vyberte `Newtonsoft.Json` a v pravém podokně vyberte projektu před výběrem **nainstalovat**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-154">Select `Newtonsoft.Json` and, in the right pane, select your project before selecting **Install**.</span></span>

1. <span data-ttu-id="a5c2c-155">Pokud `Newtonsoft.Json` je již nainstalován ve vašem systému, přidejte ji do projektu výběrem projektu v pravém podokně **spravovat balíčky pro řešení** kartě.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-155">If `Newtonsoft.Json` is already installed on your system, add it to your project by selecting your project in the right pane of hte **Manage Packages for Solution** tab.</span></span>

<span data-ttu-id="a5c2c-156">Upozorňujeme, že nasazení závislé na framework se závislostmi třetí strany se pouze jako přenosný jeho závislé součásti třetích stran.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-156">Note that a framework-dependent deployment with third-party dependencies is only as portable as its third-party dependencies.</span></span> <span data-ttu-id="a5c2c-157">Například pokud knihovnu třetích stran podporuje pouze systému macOS, aplikace není přenosné pro systémy Windows.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-157">For example, if a third-party library only supports macOS, the app isn't portable to Windows systems.</span></span> <span data-ttu-id="a5c2c-158">To se stane, když závislost třetích stran, samotné závisí na nativního kódu.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-158">This happens if the third-party dependency itself depends on native code.</span></span> <span data-ttu-id="a5c2c-159">Dobrým příkladem tohoto objektu je [Kestrel server](http://docs.microsoft.com/aspnet/core/fundamentals/servers/kestrel), který vyžaduje nativní závislost na [libuv](https://github.com/libuv/libuv).</span><span class="sxs-lookup"><span data-stu-id="a5c2c-159">A good example of this is [Kestrel server](http://docs.microsoft.com/aspnet/core/fundamentals/servers/kestrel), which requires a native dependency on [libuv](https://github.com/libuv/libuv).</span></span> <span data-ttu-id="a5c2c-160">Když disketové jednotky se vytvoří pro aplikace s tímto typem závislosti třetích stran, publikované výstup obsahuje složku pro každý [identifikátor Runtime (RID)](../rid-catalog.md) podporující nativní závislost (a která existuje v jeho balíčku NuGet).</span><span class="sxs-lookup"><span data-stu-id="a5c2c-160">When an FDD is created for an application with this kind of third-party dependency, the published output contains a folder for each [Runtime Identifier (RID)](../rid-catalog.md) that the native dependency supports (and that exists in its NuGet package).</span></span>

## <a name="simpleSelf"></a> <span data-ttu-id="a5c2c-161">Samostatná nasazení bez závislosti na třetích stran</span><span class="sxs-lookup"><span data-stu-id="a5c2c-161">Self-contained deployment without third-party dependencies</span></span>

<span data-ttu-id="a5c2c-162">Nasazení samostatná nasazení nemá žádné závislosti třetích stran zahrnuje vytvoření projektu, úprava *csproj* souborů, vytváření, testování a publikování aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-162">Deploying a self-contained deployment with no third-party dependencies involves creating the project, modifying the *csproj* file, building, testing, and publishing the app.</span></span> <span data-ttu-id="a5c2c-163">Jednoduchý příklad napsané v C# znázorňuje proces.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-163">A simple example written in C# illustrates the process.</span></span> 

1. <span data-ttu-id="a5c2c-164">Vytvoření projektu.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-164">Create the project.</span></span>

   <span data-ttu-id="a5c2c-165">Select **File** > **New** > **Project**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-165">Select **File** > **New** > **Project**.</span></span> <span data-ttu-id="a5c2c-166">V **přidat nový projekt** dialogovém okně, vyberte **.NET Core** v **nainstalovaná** typy podokně projektu a vyberte **konzolové aplikace (.NET Core)** Šablona v prostředním podokně.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-166">In the **Add New Project** dialog, select **.NET Core** in the **Installed** project types pane, and select the **Console App (.NET Core)** template in the center pane.</span></span> <span data-ttu-id="a5c2c-167">Zadejte název projektu, například "SCD" **název** textového pole a vyberte **OK** tlačítko.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-167">Enter a project name, such as "SCD", in the **Name** text box, and select the **OK** button.</span></span>

1. <span data-ttu-id="a5c2c-168">Přidejte zdrojový kód aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-168">Add the application's source code.</span></span>

   <span data-ttu-id="a5c2c-169">Otevřete *Program.cs* souboru ve svém editoru a automaticky vygenerovaný kód nahraďte následujícím kódem.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-169">Open the *Program.cs* file in your editor, and replace the auto-generated code with the following code.</span></span> <span data-ttu-id="a5c2c-170">Se zobrazí výzvu k zadání textu a zobrazí jednotlivých slov zadané uživatelem.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-170">It prompts the user to enter text and displays the individual words entered by the user.</span></span> <span data-ttu-id="a5c2c-171">Používá regulární výraz `\w+` slov v vstupního textu.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-171">It uses the regular expression `\w+` to separate the words in the input text.</span></span>

   [!code-csharp[deployment#1](../../../samples/snippets/core/deploying/deployment-example.cs)]

1. <span data-ttu-id="a5c2c-172">Definujte platformy, které se zaměří na vaši aplikaci.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-172">Define the platforms that your app will target.</span></span>

   1. <span data-ttu-id="a5c2c-173">Klikněte pravým tlačítkem na projekt (ne řešení) v **Průzkumníku řešení**a vyberte **upravit SCD.csproj**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-173">Right-click on your project (not the solution) In **Solution Explorer**, and select **Edit SCD.csproj**.</span></span>

   1. <span data-ttu-id="a5c2c-174">Vytvoření `<RuntimeIdentifiers>` značky v `<PropertyGroup>` část vaší *csproj* soubor, který definuje platformy cíle vaší aplikace, a zadejte runtime identifikátorů (RID) pro každou platformu, která cílí.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-174">Create a `<RuntimeIdentifiers>` tag in the `<PropertyGroup>` section of your *csproj* file that defines the platforms your app targets, and specify the runtime identifier (RID) of each platform that you target.</span></span> <span data-ttu-id="a5c2c-175">Všimněte si, že je také nutné přidat středníkem k oddělení identifikátorů RID.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-175">Note that you also need to add a semicolon to separate the RIDs.</span></span> <span data-ttu-id="a5c2c-176">V tématu [Runtime identifikátor katalogu](../rid-catalog.md) seznam identifikátorů modulu runtime.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-176">See [Runtime IDentifier catalog](../rid-catalog.md) for a list of runtime identifiers.</span></span> 

   <span data-ttu-id="a5c2c-177">Následující příklad například označuje, že aplikace běží na 64bitové verze Windows 10 operační systémy a operační systém 10.11 verze OS X 64-bit.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-177">For example, the following example indicates that the app runs on 64-bit Windows 10 operating systems and the 64-bit OS X Version 10.11 operating system.</span></span>

```xml
<PropertyGroup>
    <RuntimeIdentifiers>win10-x64;osx.10.11-x64</RuntimeIdentifiers>
</PropertyGroup>
```
   <span data-ttu-id="a5c2c-178">Všimněte si, že `<RuntimeIdentifiers>` element můžete přejít do jakéhokoli `<PropertyGroup>` , ke které máte vaší *csproj* souboru.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-178">Note that the `<RuntimeIdentifiers>` element can go into any `<PropertyGroup>` that you have in your *csproj* file.</span></span> <span data-ttu-id="a5c2c-179">Ucelenou ukázku *csproj* souboru se zobrazí později v této části.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-179">A complete sample *csproj* file appears later in this section.</span></span>

1. <span data-ttu-id="a5c2c-180">Vytvořte sestavení ladicí verze vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-180">Create a Debug build of your app.</span></span>

   <span data-ttu-id="a5c2c-181">Vyberte **sestavení** > **sestavení řešení**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-181">Select **Build** > **Build Solution**.</span></span> <span data-ttu-id="a5c2c-182">Můžete také zkompilování a spuštění ladění sestavení aplikace tak, že vyberete **ladění** > **spustit ladění**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-182">You can also compile and run the Debug build of your application by selecting **Debug** > **Start Debugging**.</span></span>

1. <span data-ttu-id="a5c2c-183">Publikování aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-183">Publish your app.</span></span>

   <span data-ttu-id="a5c2c-184">Po ladit a testovat program, vytvoření souborů k nasazení s vaší aplikací pro každou platformu, cílem.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-184">After you've debugged and tested the program, create the files to be deployed with your app for each platform that it targets.</span></span>

   <span data-ttu-id="a5c2c-185">K publikování aplikace ze sady Visual Studio, postupujte takto:</span><span class="sxs-lookup"><span data-stu-id="a5c2c-185">To publish your app from Visual Studio, do the following:</span></span>

      1. <span data-ttu-id="a5c2c-186">Změňte konfiguraci řešení z **ladění** k **verze** na panelu nástrojů na verzi sestavení a verze (nikoli a ladění) vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-186">Change the solution configuration from **Debug** to **Release** on the toolbar to build a Release (rather than a Debug) version of your app.</span></span>

      1. <span data-ttu-id="a5c2c-187">Klikněte pravým tlačítkem na projekt (ne řešení) v **Průzkumníku řešení** a vyberte **publikovat**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-187">Right-click on the project (not the solution) in **Solution Explorer** and select **Publish**.</span></span> 

      1. <span data-ttu-id="a5c2c-188">V **publikovat** vyberte **publikovat**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-188">In the **Publish** tab, select **Publish**.</span></span> <span data-ttu-id="a5c2c-189">Visual Studio zapíše soubory, které tvoří vaši aplikaci do místního systému souborů.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-189">Visual Studio writes the files that comprise your application to the local file system.</span></span>

      1. <span data-ttu-id="a5c2c-190">**Publikovat** kartě se teď zobrazují jediného profilu, **FolderProfile**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-190">The **Publish** tab now shows a single profile, **FolderProfile**.</span></span> <span data-ttu-id="a5c2c-191">Nastavení profilu konfigurace se zobrazují v **Souhrn** části karty. **Cíl Runtime** identifikuje publikovali které runtime a **cílové umístění** identifikuje, kde byly napsány souborů pro samostatný nasazení.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-191">The profile's configuration settings are shown in the **Summary** section of the tab. **Target Runtime** identifies which runtime has been published, and **Target Location** identifies where the files for the self-contained deployment were written.</span></span>

      1. <span data-ttu-id="a5c2c-192">Visual Studio ve výchozím nastavení zapíše že všechny publikované soubory do jednoho adresáře.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-192">Visual Studio by default writes all published files to a single directory.</span></span> <span data-ttu-id="a5c2c-193">Pro usnadnění práce je nejvhodnější vytvořit samostatný profil pro každý cílový modul runtime a publikovaná soubory umístit do adresáře specifické pro platformu.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-193">For convenience, it's best to create separate profiles for each target runtime and to place published files in a platform-specific directory.</span></span> <span data-ttu-id="a5c2c-194">To zahrnuje vytváření samostatný profil publikování pro každou cílovou platformu.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-194">This involves creating a separate publishing profile for each target platform.</span></span> <span data-ttu-id="a5c2c-195">Proto nyní znovu sestavte aplikaci pro každou platformu následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="a5c2c-195">So now rebuild the application for each platform by doing the following:</span></span>

         1. <span data-ttu-id="a5c2c-196">Vyberte **vytvořit nový profil** v **publikovat** dialogové okno.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-196">Select **Create new profile** in the **Publish** dialog.</span></span>

         1. <span data-ttu-id="a5c2c-197">V **vyberte cíl publikování** dialogové okno, změny **vyberte složku,** umístění pro *bin\Release\PublishOutput\win10 x64*.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-197">In the **Pick a publish target** dialog, change the **Choose a folder** location to *bin\Release\PublishOutput\win10-x64*.</span></span> <span data-ttu-id="a5c2c-198">Vyberte **OK**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-198">Select **OK**.</span></span>

         1. <span data-ttu-id="a5c2c-199">Vyberte profil, nové (**FolderProfile1**) v seznamu profilů a ujistěte se, že **cílový modul Runtime** je `win10-x64`.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-199">Select the new profile (**FolderProfile1**) in the list of profiles, and make sure that the **Target Runtime** is `win10-x64`.</span></span> <span data-ttu-id="a5c2c-200">Pokud tomu tak není, vyberte **nastavení**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-200">If it isn't, select **Settings**.</span></span> <span data-ttu-id="a5c2c-201">V **nastavení profilu** dialogové okno, změny **cílový modul Runtime** k `win10-x64` a vyberte **Uložit**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-201">In the **Profile Settings** dialog, change the **Target Runtime** to `win10-x64` and select **Save**.</span></span> <span data-ttu-id="a5c2c-202">Jinak vyberte možnost **zrušit**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-202">Otherwise, select **Cancel**.</span></span>

         1. <span data-ttu-id="a5c2c-203">Vyberte **publikovat** k publikování aplikace pro 64bitové platformy Windows 10.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-203">Select **Publish** to publish your app for 64-bit Windows 10 platforms.</span></span>

         1. <span data-ttu-id="a5c2c-204">Postupujte podle předchozích kroků znovu a vytvořit profil pro `osx.10.11-x64` platformy.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-204">Follow the previous steps again to create a profile for the `osx.10.11-x64` platform.</span></span> <span data-ttu-id="a5c2c-205">**Cílové umístění** je *bin\Release\PublishOutput\osx.10.11-x64*a **cílový modul Runtime** je `osx.10.11-x64`.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-205">The **Target Location** is *bin\Release\PublishOutput\osx.10.11-x64*, and the **Target Runtime** is `osx.10.11-x64`.</span></span> <span data-ttu-id="a5c2c-206">Název sady Visual Studio přiřadí k tomuto profilu je **FolderProfile2**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-206">The name that Visual Studio assigns to this profile is **FolderProfile2**.</span></span>

      <span data-ttu-id="a5c2c-207">Všimněte si, že každé cílové umístění obsahuje kompletní sadu souborů (soubory aplikace a všechny soubory .NET Core) potřebné ke spuštění aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-207">Note that each target location contains the complete set of files (both your app files and all .NET Core files) needed to launch your app.</span></span>

<span data-ttu-id="a5c2c-208">Proces publikování společně se soubory aplikace vysílá soubor databáze (.pdb) program, který obsahuje ladicí informace o vaší aplikaci.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-208">Along with your application's files, the publishing process emits a program database (.pdb) file that contains debugging information about your app.</span></span> <span data-ttu-id="a5c2c-209">Soubor je užitečné především pro ladění výjimek.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-209">The file is useful primarily for debugging exceptions.</span></span> <span data-ttu-id="a5c2c-210">Je možné, není-li vytvořit balíček s soubory aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-210">You can choose not to package it with your application's files.</span></span> <span data-ttu-id="a5c2c-211">Ale uložte jej v případě, že chcete ladit sestavení pro vydání vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-211">You should, however, save it in the event that you want to debug the Release build of your app.</span></span>

<span data-ttu-id="a5c2c-212">Publikované soubory žádným způsobem, který chcete nasaďte.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-212">Deploy the published files in any way you like.</span></span> <span data-ttu-id="a5c2c-213">Například můžete balíček je v souboru Zip, pomocí jednoduché `copy` příkaz nebo nasadit pomocí jakékoli instalačního balíčku podle svého výběru.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-213">For example, you can package them in a Zip file, use a simple `copy` command, or deploy them with any installation package of your choice.</span></span>

<span data-ttu-id="a5c2c-214">Toto je kompletní *csproj* souboru pro tento projekt.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-214">The following is the complete *csproj* file for this project.</span></span>

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>netcoreapp1.1</TargetFramework>
    <RuntimeIdentifiers>win10-x64;osx.10.11-x64</RuntimeIdentifiers>
  </PropertyGroup>
</Project>
```

## <a name="self-contained-deployment-with-third-party-dependencies"></a><span data-ttu-id="a5c2c-215">Samostatná nasazení se závislostmi třetích stran</span><span class="sxs-lookup"><span data-stu-id="a5c2c-215">Self-contained deployment with third-party dependencies</span></span>

<span data-ttu-id="a5c2c-216">Nasazení samostatná nasazení s jeden nebo více závislostí třetí strany vyžaduje přidání závislosti.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-216">Deploying a self-contained deployment with one or more third-party dependencies involves adding the dependencies.</span></span> <span data-ttu-id="a5c2c-217">Následující kroky jsou požadovány, než můžete sestavit aplikaci:</span><span class="sxs-lookup"><span data-stu-id="a5c2c-217">The following additional steps are required before you can build your app:</span></span>

1. <span data-ttu-id="a5c2c-218">Použití **Správce balíčků NuGet** přidat odkaz na balíček NuGet do projektu; a pokud balíček již není k dispozici v systému, nainstalujte ji.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-218">Use the **NuGet Package Manager** to add a reference to a NuGet package to your project; and if the package is not already available on your system, install it.</span></span> <span data-ttu-id="a5c2c-219">Chcete-li otevřít správce balíčku, vyberte **nástroje** > **Správce balíčků NuGet** > **spravovat balíčky NuGet pro řešení**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-219">To open the package manager, select **Tools** > **NuGet Package Manager** > **Manage NuGet Packages for Solution**.</span></span>

1. <span data-ttu-id="a5c2c-220">Potvrďte, že `Newtonsoft.Json` na vašem systému je nainstalovaná a pokud není, nainstalujte ji.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-220">Confirm that `Newtonsoft.Json` is installed on your system and, if it is not, install it.</span></span> <span data-ttu-id="a5c2c-221">**Nainstalovaná** karta Vypíše seznam balíčků NuGet v systému nainstalována.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-221">The **Installed** tab lists NuGet packages installed on your system.</span></span> <span data-ttu-id="a5c2c-222">Pokud `Newtonsoft.Json` není uveden, vyberte **Procházet** kartě a do vyhledávacího pole zadejte "Newtonsoft.Json".</span><span class="sxs-lookup"><span data-stu-id="a5c2c-222">If `Newtonsoft.Json` is not listed there, select the **Browse** tab and enter "Newtonsoft.Json" in the search box.</span></span> <span data-ttu-id="a5c2c-223">Vyberte `Newtonsoft.Json` a v pravém podokně vyberte projektu před výběrem **nainstalovat**.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-223">Select `Newtonsoft.Json` and, in the right pane, select your project before selecting **Install**.</span></span>

1. <span data-ttu-id="a5c2c-224">Pokud `Newtonsoft.Json` je již nainstalován ve vašem systému, přidejte ji do projektu výběrem projektu v pravém podokně **spravovat balíčky pro řešení** kartě.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-224">If `Newtonsoft.Json` is already installed on your system, add it to your project by selecting your project in the right pane of the **Manage Packages for Solution** tab.</span></span>

<span data-ttu-id="a5c2c-225">Toto je kompletní *csproj* souboru pro tento projekt:</span><span class="sxs-lookup"><span data-stu-id="a5c2c-225">The following is the complete *csproj* file for this project:</span></span>

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>netcoreapp1.1</TargetFramework>
    <RuntimeIdentifiers>win10-x64;osx.10.11-x64</RuntimeIdentifiers>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="10.0.2" />
  </ItemGroup>
</Project>
```

<span data-ttu-id="a5c2c-226">Když nasadíte aplikaci, všechny závislosti třetích stran používají ve vaší aplikaci jsou také obsažena s soubory aplikace.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-226">When you deploy your application, any third-party dependencies used in your app are also contained with your application files.</span></span> <span data-ttu-id="a5c2c-227">Knihovny jiných výrobců nemusí na serveru, na kterém aplikace běží.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-227">Third-party libraries aren't required on the system on which the app is running.</span></span>

<span data-ttu-id="a5c2c-228">Všimněte si, že lze nasadit pouze samostatná nasazení s knihovnou třetích stran na platformách podporovaných aplikací této knihovny.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-228">Note that you can only deploy a self-contained deployment with a third-party library to platforms supported by that library.</span></span> <span data-ttu-id="a5c2c-229">Toto je podobná s závislosti třetích stran s nativní závislosti ve vašem nasazení závislé na framework, kde neexistují nativní závislosti na cílové platformy, pokud byly dříve nainstalovány existuje.</span><span class="sxs-lookup"><span data-stu-id="a5c2c-229">This is similar to having third-party dependencies with native dependencies in your framework-dependent deployment, where the native dependencies won't exist on the target platform unless they were previously installed there.</span></span>

# <a name="see-also"></a><span data-ttu-id="a5c2c-230">Viz také</span><span class="sxs-lookup"><span data-stu-id="a5c2c-230">See also</span></span>
<span data-ttu-id="a5c2c-231">[Nasazení aplikace .NET core](index.md) </span><span class="sxs-lookup"><span data-stu-id="a5c2c-231">[.NET Core Application Deployment](index.md) </span></span>  
[<span data-ttu-id="a5c2c-232">Katalog .NET core Runtime identifikátor (RID)</span><span class="sxs-lookup"><span data-stu-id="a5c2c-232">.NET Core Runtime IDentifier (RID) catalog</span></span>](../rid-catalog.md)   
