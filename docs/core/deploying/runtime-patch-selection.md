---
title: Runtime roll vpřed pro nasazení samostatných aplikací .NET Core.
description: Další informace o dotnet publikovat změny pro samostatná nasazení.
author: KathleenDollard
ms.date: 05/31/2018
ms.openlocfilehash: 22385c7b5d2bf87755fd51cd6268d21fe3431c74
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/14/2020
ms.locfileid: "75740793"
---
# <a name="self-contained-deployment-runtime-roll-forward"></a><span data-ttu-id="12343-103">Samostatné nasazení – dopředné posunutí modulu runtime</span><span class="sxs-lookup"><span data-stu-id="12343-103">Self-contained deployment runtime roll forward</span></span>

<span data-ttu-id="12343-104">[Samostatná nasazení aplikací](index.md) .NET Core zahrnují knihovny .NET Core i runtime .NET Core.</span><span class="sxs-lookup"><span data-stu-id="12343-104">.NET Core [self-contained application deployments](index.md) include both the .NET Core libraries and the .NET Core runtime.</span></span> <span data-ttu-id="12343-105">Počínaje .NET Core 2.1 SDK (verze 2.1.300) samostatné nasazení aplikace [publikuje nejvyšší čas běhu opravy na vašem počítači](https://github.com/dotnet/designs/pull/36).</span><span class="sxs-lookup"><span data-stu-id="12343-105">Starting in .NET Core 2.1 SDK (version 2.1.300), a self-contained application deployment [publishes the highest patch runtime on your machine](https://github.com/dotnet/designs/pull/36).</span></span> <span data-ttu-id="12343-106">Ve výchozím [`dotnet publish`](../tools/dotnet-publish.md) nastavení pro samostatné nasazení vybere nejnovější verzi nainstalovanou jako součást sady SDK v počítači pro publikování.</span><span class="sxs-lookup"><span data-stu-id="12343-106">By default, [`dotnet publish`](../tools/dotnet-publish.md) for a self-contained deployment selects the latest version installed as part of the SDK on the publishing machine.</span></span> <span data-ttu-id="12343-107">To umožňuje nasazené aplikace spustit s opravami zabezpečení `publish`(a další opravy) k dispozici během .</span><span class="sxs-lookup"><span data-stu-id="12343-107">This enables your deployed application to run with security fixes (and other fixes) available during `publish`.</span></span> <span data-ttu-id="12343-108">Aplikace musí být znovu publikována, aby bylo možné získat novou opravu.</span><span class="sxs-lookup"><span data-stu-id="12343-108">The application must be republished to obtain a new patch.</span></span> <span data-ttu-id="12343-109">Samostatné aplikace jsou vytvářeny zadáním `-r <RID>` `dotnet publish` příkazu nebo zadáním [identifikátoru runtime (RID)](../rid-catalog.md) v souboru projektu (csproj / vbproj) nebo na příkazovém řádku.</span><span class="sxs-lookup"><span data-stu-id="12343-109">Self-contained applications are created by specifying `-r <RID>` on the `dotnet publish` command or by specifying the [runtime identifier (RID)](../rid-catalog.md) in the project file (csproj / vbproj) or on the command line.</span></span>

## <a name="patch-version-roll-forward-overview"></a><span data-ttu-id="12343-110">Přehled posunu verze opravy vpřed</span><span class="sxs-lookup"><span data-stu-id="12343-110">Patch version roll forward overview</span></span>

<span data-ttu-id="12343-111">[`restore`](../tools/dotnet-restore.md)a [`build`](../tools/dotnet-build.md) [`publish`](../tools/dotnet-publish.md) jsou `dotnet` příkazy, které lze spustit samostatně.</span><span class="sxs-lookup"><span data-stu-id="12343-111">[`restore`](../tools/dotnet-restore.md), [`build`](../tools/dotnet-build.md) and [`publish`](../tools/dotnet-publish.md) are `dotnet` commands that can run separately.</span></span> <span data-ttu-id="12343-112">Volba runtime je součástí `restore` operace, `publish` `build`nikoli nebo .</span><span class="sxs-lookup"><span data-stu-id="12343-112">The runtime choice is part of the `restore` operation, not `publish` or `build`.</span></span> <span data-ttu-id="12343-113">Pokud zavoláte `publish`, bude vybrána nejnovější verze opravy.</span><span class="sxs-lookup"><span data-stu-id="12343-113">If you call `publish`, the latest patch version will be chosen.</span></span> <span data-ttu-id="12343-114">Pokud voláte `publish` `--no-restore` s argumentem, pak nemusí získat požadovanou `restore` verzi opravy, protože předchozí nemusí být provedeny s novou zásadu publikování samostatné aplikace.</span><span class="sxs-lookup"><span data-stu-id="12343-114">If you call `publish` with the `--no-restore` argument, then you may not get the desired patch version because a prior `restore` may not have been executed with the new self-contained application publishing policy.</span></span> <span data-ttu-id="12343-115">V tomto případě je generována chyba sestavení s textem podobným následujícímu:</span><span class="sxs-lookup"><span data-stu-id="12343-115">In this case, a build error is generated with text similar to the following:</span></span>

  <span data-ttu-id="12343-116">"Projekt byl obnoven pomocí Microsoft.NETCore.App verze 2.0.0, ale s aktuálním nastavením by se místo toho použila verze 2.0.6.</span><span class="sxs-lookup"><span data-stu-id="12343-116">"The project was restored using Microsoft.NETCore.App version 2.0.0, but with current settings, version 2.0.6 would be used instead.</span></span> <span data-ttu-id="12343-117">Chcete-li tento problém vyřešit, ujistěte se, že stejná nastavení se používají pro obnovení a pro následné operace, jako je například sestavení nebo publikování.</span><span class="sxs-lookup"><span data-stu-id="12343-117">To resolve this issue, make sure the same settings are used for restore and for subsequent operations such as build or publish.</span></span> <span data-ttu-id="12343-118">Obvykle k tomuto problému může dojít, pokud runtimeidentifier vlastnost je nastavena během sestavení nebo publikovat, ale ne během obnovení."</span><span class="sxs-lookup"><span data-stu-id="12343-118">Typically this issue can occur if the RuntimeIdentifier property is set during build or publish but not during restore."</span></span>

> [!NOTE]
> <span data-ttu-id="12343-119">`restore`a `build` lze jej spustit implicitně jako `publish`součást jiného příkazu, například .</span><span class="sxs-lookup"><span data-stu-id="12343-119">`restore` and `build` can be run implicitly as part of another command, like `publish`.</span></span> <span data-ttu-id="12343-120">Při spuštění implicitně jako součást jiného příkazu jsou k dispozici další kontext tak, aby byly vytvořeny správné artefakty.</span><span class="sxs-lookup"><span data-stu-id="12343-120">When run implicitly as part of another command, they are provided with additional context so that the right artifacts are produced.</span></span> <span data-ttu-id="12343-121">Když `publish` s runtime (například `dotnet publish -r linux-x64`), `restore` implicitní obnoví balíčky pro linux-x64 runtime.</span><span class="sxs-lookup"><span data-stu-id="12343-121">When you `publish` with a runtime (for example, `dotnet publish -r linux-x64`), the implicit `restore` restores packages for the linux-x64 runtime.</span></span> <span data-ttu-id="12343-122">Pokud voláte `restore` explicitně, neobnoví balíčky za běhu ve výchozím nastavení, protože nemá tento kontext.</span><span class="sxs-lookup"><span data-stu-id="12343-122">If you call `restore` explicitly, it does not restore runtime packages by default, because it doesn't have that context.</span></span>

## <a name="how-to-avoid-restore-during-publish"></a><span data-ttu-id="12343-123">Jak se vyhnout obnovení během publikování</span><span class="sxs-lookup"><span data-stu-id="12343-123">How to avoid restore during publish</span></span>

<span data-ttu-id="12343-124">Spuštění `restore` jako součást `publish` operace může být nežádoucí pro váš scénář.</span><span class="sxs-lookup"><span data-stu-id="12343-124">Running `restore` as part of the `publish` operation may be undesirable for your scenario.</span></span> <span data-ttu-id="12343-125">Chcete-li se vyhnout `restore` při `publish` vytváření samostatných aplikací, postupujte takto:</span><span class="sxs-lookup"><span data-stu-id="12343-125">To avoid `restore` during `publish` while creating self-contained applications, do the following:</span></span>

- <span data-ttu-id="12343-126">Nastavte `RuntimeIdentifiers` vlastnost na středník-oddělený seznam všech [IDs, které](../rid-catalog.md) mají být publikovány.</span><span class="sxs-lookup"><span data-stu-id="12343-126">Set the `RuntimeIdentifiers` property to a semicolon-separated list of all the [RIDs](../rid-catalog.md) to be published.</span></span>
- <span data-ttu-id="12343-127">Nastavte `TargetLatestRuntimePatch` vlastnost `true`na .</span><span class="sxs-lookup"><span data-stu-id="12343-127">Set the `TargetLatestRuntimePatch` property to `true`.</span></span>

## <a name="no-restore-argument-with-dotnet-publish-options"></a><span data-ttu-id="12343-128">Argument bez obnovení s možnostmi publikování dotnet</span><span class="sxs-lookup"><span data-stu-id="12343-128">No-restore argument with dotnet publish options</span></span>

<span data-ttu-id="12343-129">Pokud chcete vytvořit samostatné aplikace i [aplikace závislé na rámci](index.md) se stejným souborem projektu `--no-restore` a `dotnet publish`chcete použít argument s , zvolte jednu z následujících možností:</span><span class="sxs-lookup"><span data-stu-id="12343-129">If you want to create both self-contained applications and [framework-dependent applications](index.md) with the same project file, and you want to use the `--no-restore` argument with `dotnet publish`, then choose one of the following:</span></span>

1. <span data-ttu-id="12343-130">Preferují chování závislé na rámci.</span><span class="sxs-lookup"><span data-stu-id="12343-130">Prefer the framework-dependent behavior.</span></span> <span data-ttu-id="12343-131">Pokud je aplikace závislá na rámci, toto je výchozí chování.</span><span class="sxs-lookup"><span data-stu-id="12343-131">If the application is framework-dependent, this is the default behavior.</span></span> <span data-ttu-id="12343-132">Pokud je aplikace samostatná a může použít neopravený místní runtime 2.1.0, nastavte `TargetLatestRuntimePatch` do `false` v souboru projektu.</span><span class="sxs-lookup"><span data-stu-id="12343-132">If the application is self-contained, and can use an unpatched 2.1.0 local runtime, set the `TargetLatestRuntimePatch` to `false` in the project file.</span></span>

2. <span data-ttu-id="12343-133">Preferujte samostatné chování.</span><span class="sxs-lookup"><span data-stu-id="12343-133">Prefer the self-contained behavior.</span></span> <span data-ttu-id="12343-134">Pokud je aplikace samostatná, jedná se o výchozí chování.</span><span class="sxs-lookup"><span data-stu-id="12343-134">If the application is self-contained, this is the default behavior.</span></span> <span data-ttu-id="12343-135">Pokud je aplikace závislá na rozhraní a vyžaduje `TargetLatestRuntimePatch` nainstalovanou nejnovější opravu, nastavte ji `true` v souboru projektu.</span><span class="sxs-lookup"><span data-stu-id="12343-135">If the application is framework-dependent, and requires the latest patch installed, set `TargetLatestRuntimePatch` to `true` in the project file.</span></span>

3. <span data-ttu-id="12343-136">Převzít explicitní kontrolu verze rozhraní `RuntimeFrameworkVersion` runtime rozhraní nastavením na konkrétní verzi opravy v souboru projektu.</span><span class="sxs-lookup"><span data-stu-id="12343-136">Take explicit control of the runtime framework version by setting `RuntimeFrameworkVersion` to the specific patch version in the project file.</span></span>
