---
title: Kolekční sestavení pro generování dynamických typů
description: Začínáme s kolekční sestaveními pro generování dynamického typu v .NET. Přečtěte si o kolekční a platnosti sestavení a omezeních.
ms.date: 08/29/2017
helpviewer_keywords:
- reflection, dynamic assembly
- assemblies, collectible
- collectible assemblies, retrieving
ms.openlocfilehash: 4981b93dbd49a6da96740bebed0f2ed7b89036c8
ms.sourcegitcommit: cf5a800a33de64d0aad6d115ffcc935f32375164
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/20/2020
ms.locfileid: "86475122"
---
# <a name="collectible-assemblies-for-dynamic-type-generation"></a><span data-ttu-id="6f737-104">Kolekční sestavení pro generování dynamických typů</span><span class="sxs-lookup"><span data-stu-id="6f737-104">Collectible assemblies for dynamic type generation</span></span>

<span data-ttu-id="6f737-105">*Sestavení kolekční* jsou dynamická sestavení, která lze uvolnit bez uvolnění domény aplikace, ve které byly vytvořeny.</span><span class="sxs-lookup"><span data-stu-id="6f737-105">*Collectible assemblies* are dynamic assemblies that can be unloaded without unloading the application domain in which they were created.</span></span> <span data-ttu-id="6f737-106">Veškerá spravovaná a nespravovaná paměť, kterou používá kolekční sestavení, a typy, které obsahuje, mohou být uvolněny.</span><span class="sxs-lookup"><span data-stu-id="6f737-106">All managed and unmanaged memory used by a collectible assembly and the types it contains can be reclaimed.</span></span> <span data-ttu-id="6f737-107">Informace, jako je název sestavení, jsou odebrány z interní tabulky.</span><span class="sxs-lookup"><span data-stu-id="6f737-107">Information such as the assembly name is removed from internal tables.</span></span>

<span data-ttu-id="6f737-108">Chcete-li povolit uvolnění, použijte <xref:System.Reflection.Emit.AssemblyBuilderAccess.RunAndCollect?displayProperty=nameWithType> příznak při vytváření dynamického sestavení.</span><span class="sxs-lookup"><span data-stu-id="6f737-108">To enable unloading, use the <xref:System.Reflection.Emit.AssemblyBuilderAccess.RunAndCollect?displayProperty=nameWithType> flag when you create a dynamic assembly.</span></span> <span data-ttu-id="6f737-109">Sestavení je přechodné (to znamená, nemůže být uloženo) a podléhá omezením popsaným v části [omezení pro sestavení kolekční](#restrictions-on-collectible-assemblies) .</span><span class="sxs-lookup"><span data-stu-id="6f737-109">The assembly is transient (that is, it cannot be saved) and is subject to limitations described in the [Restrictions on Collectible Assemblies](#restrictions-on-collectible-assemblies) section.</span></span> <span data-ttu-id="6f737-110">Modul CLR (Common Language Runtime) uvolní sestavení kolekční automaticky při uvolnění všech objektů přidružených k sestavení.</span><span class="sxs-lookup"><span data-stu-id="6f737-110">The common language runtime (CLR) unloads a collectible assembly automatically when you release all objects associated with the assembly.</span></span> <span data-ttu-id="6f737-111">Ve všech ostatních ohledech se kolekční sestavení vytvářejí a používají stejným způsobem jako ostatní dynamická sestavení.</span><span class="sxs-lookup"><span data-stu-id="6f737-111">In all other respects, collectible assemblies are created and used in the same way as other dynamic assemblies.</span></span>

## <a name="lifetime-of-collectible-assemblies"></a><span data-ttu-id="6f737-112">Doba života kolekční sestavení</span><span class="sxs-lookup"><span data-stu-id="6f737-112">Lifetime of collectible assemblies</span></span>

<span data-ttu-id="6f737-113">Životnost sestavení kolekční je řízena existencí odkazů na typy, které obsahuje, a objekty, které jsou vytvořeny z těchto typů.</span><span class="sxs-lookup"><span data-stu-id="6f737-113">The lifetime of a collectible assembly is controlled by the existence of references to the types it contains and the objects that are created from those types.</span></span> <span data-ttu-id="6f737-114">Modul CLR (Common Language Runtime) neuvolní sestavení, pokud existuje jeden nebo více z následujících `T` typů (je libovolný typ, který je definován v sestavení):</span><span class="sxs-lookup"><span data-stu-id="6f737-114">The common language runtime does not unload an assembly as long as one or more of the following exist (`T` is any type that is defined in the assembly):</span></span>

- <span data-ttu-id="6f737-115">Instance `T` .</span><span class="sxs-lookup"><span data-stu-id="6f737-115">An instance of `T`.</span></span>

- <span data-ttu-id="6f737-116">Instance pole `T` .</span><span class="sxs-lookup"><span data-stu-id="6f737-116">An instance of an array of `T`.</span></span>

- <span data-ttu-id="6f737-117">Instance obecného typu, který má `T` jako jeden z jejích argumentů typu.</span><span class="sxs-lookup"><span data-stu-id="6f737-117">An instance of a generic type that has `T` as one of its type arguments.</span></span> <span data-ttu-id="6f737-118">To zahrnuje obecné kolekce `T` , a to i v případě, že je kolekce prázdná.</span><span class="sxs-lookup"><span data-stu-id="6f737-118">This includes generic collections of `T`, even if that collection is empty.</span></span>

- <span data-ttu-id="6f737-119">Instance <xref:System.Type> nebo <xref:System.Reflection.Emit.TypeBuilder> , která představuje `T` .</span><span class="sxs-lookup"><span data-stu-id="6f737-119">An instance of <xref:System.Type> or <xref:System.Reflection.Emit.TypeBuilder> that represents `T`.</span></span>

   > [!IMPORTANT]
   > <span data-ttu-id="6f737-120">Je nutné uvolnit všechny objekty, které reprezentují části sestavení.</span><span class="sxs-lookup"><span data-stu-id="6f737-120">You must release all objects that represent parts of the assembly.</span></span> <span data-ttu-id="6f737-121">Rozhraní <xref:System.Reflection.Emit.ModuleBuilder> , které definuje `T` odkaz na <xref:System.Reflection.Emit.TypeBuilder> , a <xref:System.Reflection.Emit.AssemblyBuilder> objekt udržuje odkaz na <xref:System.Reflection.Emit.ModuleBuilder> , takže odkazy na tyto objekty musí být uvolněny.</span><span class="sxs-lookup"><span data-stu-id="6f737-121">The <xref:System.Reflection.Emit.ModuleBuilder> that defines `T` keeps a reference to the <xref:System.Reflection.Emit.TypeBuilder>, and the <xref:System.Reflection.Emit.AssemblyBuilder> object keeps a reference to the <xref:System.Reflection.Emit.ModuleBuilder>, so references to these objects must be released.</span></span> <span data-ttu-id="6f737-122">Dokonce i existence nebo, která se <xref:System.Reflection.Emit.LocalBuilder> <xref:System.Reflection.Emit.ILGenerator> používá v rámci konstrukce `T` prevence vykládku.</span><span class="sxs-lookup"><span data-stu-id="6f737-122">Even the existence of a <xref:System.Reflection.Emit.LocalBuilder> or an <xref:System.Reflection.Emit.ILGenerator> used in the construction of `T` prevents unloading.</span></span>

- <span data-ttu-id="6f737-123">Statický odkaz na `T` jiný dynamicky definovaný typ `T1` , který je stále dosažitelný spuštěním kódu.</span><span class="sxs-lookup"><span data-stu-id="6f737-123">A static reference to `T` by another dynamically defined type `T1` that is still reachable by executing code.</span></span> <span data-ttu-id="6f737-124">Například `T1` může odvozovat z `T` nebo `T` může být typem parametru v metodě `T1` .</span><span class="sxs-lookup"><span data-stu-id="6f737-124">For example, `T1` might derive from `T`, or `T` might be the type of a parameter in a method of `T1`.</span></span>

- <span data-ttu-id="6f737-125">Typ **ByRef** ke statickému poli, které patří do `T` .</span><span class="sxs-lookup"><span data-stu-id="6f737-125">A **ByRef** to a static field that belongs to `T`.</span></span>

- <span data-ttu-id="6f737-126">A <xref:System.RuntimeTypeHandle> , <xref:System.RuntimeFieldHandle> nebo <xref:System.RuntimeMethodHandle> , který odkazuje na `T` součást prvku `T` .</span><span class="sxs-lookup"><span data-stu-id="6f737-126">A <xref:System.RuntimeTypeHandle>, <xref:System.RuntimeFieldHandle>, or <xref:System.RuntimeMethodHandle> that refers to `T` or to a component of `T`.</span></span>

- <span data-ttu-id="6f737-127">Instance libovolného objektu reflexe, který může být použit nepřímo nebo přímo pro přístup k <xref:System.Type> objektu, který představuje `T` .</span><span class="sxs-lookup"><span data-stu-id="6f737-127">An instance of any reflection object that could be used indirectly or directly to access the <xref:System.Type> object that represents `T`.</span></span> <span data-ttu-id="6f737-128">Například <xref:System.Type> objekt pro `T` lze získat z typu pole, jehož typ elementu je `T` , nebo z obecného typu, který má `T` jako argument typu.</span><span class="sxs-lookup"><span data-stu-id="6f737-128">For example, the <xref:System.Type> object for `T` can be obtained from an array type whose element type is `T`, or from a generic type that has `T` as a type argument.</span></span>

- <span data-ttu-id="6f737-129">Metoda `M` v zásobníku volání libovolného vlákna, kde `M` je metoda `T` nebo metoda na úrovni modulu, která je definována v sestavení.</span><span class="sxs-lookup"><span data-stu-id="6f737-129">A method `M` on the call stack of any thread, where `M` is a method of `T` or a module-level method that is defined in the assembly.</span></span>

- <span data-ttu-id="6f737-130">Delegát pro statickou metodu, která je definována v modulu sestavení.</span><span class="sxs-lookup"><span data-stu-id="6f737-130">A delegate to a static method that is defined in a module of the assembly.</span></span>

<span data-ttu-id="6f737-131">Pokud pouze jedna položka z tohoto seznamu existuje pouze pro jeden typ nebo jednu metodu v sestavení, modul runtime nemůže uvolnit sestavení.</span><span class="sxs-lookup"><span data-stu-id="6f737-131">If only one item from this list exists for only one type or one method in the assembly, the runtime cannot unload the assembly.</span></span>

> [!NOTE]
> <span data-ttu-id="6f737-132">Modul runtime ve skutečnosti nenačítá sestavení, dokud nejsou spuštěny finalizační metody pro všechny položky v seznamu.</span><span class="sxs-lookup"><span data-stu-id="6f737-132">The runtime does not actually unload the assembly until finalizers have run for all items in the list.</span></span>

<span data-ttu-id="6f737-133">Pro účely sledování životního cyklu se konstruovaný obecný typ, jako `List<int>` je například (v jazyce C#) nebo `List(Of Integer)` (v Visual Basic), který je vytvořen a použit při generování sestavení kolekční, je považován za definovaný buď v sestavení, které obsahuje definici obecného typu nebo v sestavení, které obsahuje definici jednoho z jejích argumentů typu.</span><span class="sxs-lookup"><span data-stu-id="6f737-133">For purposes of tracking lifetime, a constructed generic type such as `List<int>` (in C#) or `List(Of Integer)` (in Visual Basic) that is created and used in the generation of a collectible assembly is considered to have been defined either in the assembly that contains the generic type definition or in an assembly that contains the definition of one of its type arguments.</span></span> <span data-ttu-id="6f737-134">Přesné sestavení, které je použito, je podrobné informace o implementaci a může se změnit.</span><span class="sxs-lookup"><span data-stu-id="6f737-134">The exact assembly that is used is an implementation detail and subject to change.</span></span>

## <a name="restrictions-on-collectible-assemblies"></a><span data-ttu-id="6f737-135">Omezení pro kolekční sestavení</span><span class="sxs-lookup"><span data-stu-id="6f737-135">Restrictions on collectible assemblies</span></span>

<span data-ttu-id="6f737-136">Následující omezení platí pro kolekční sestavení:</span><span class="sxs-lookup"><span data-stu-id="6f737-136">The following restrictions apply to collectible assemblies:</span></span>

- <span data-ttu-id="6f737-137">**Statické odkazy** Typy v běžném dynamickém sestavení nemohou mít statické odkazy na typy, které jsou definovány v sestavení kolekční.</span><span class="sxs-lookup"><span data-stu-id="6f737-137">**Static references** Types in an ordinary dynamic assembly cannot have static references to types that are defined in a collectible assembly.</span></span> <span data-ttu-id="6f737-138">Například pokud definujete běžný typ, který dědí z typu v sestavení kolekční, <xref:System.NotSupportedException> je vyvolána výjimka.</span><span class="sxs-lookup"><span data-stu-id="6f737-138">For example, if you define an ordinary type that inherits from a type in a collectible assembly, a <xref:System.NotSupportedException> exception is thrown.</span></span> <span data-ttu-id="6f737-139">Typ v sestavení kolekční může mít statické odkazy na typ v jiném sestavení kolekční, ale rozšiřuje životnost odkazovaného sestavení na životnost odkazujícího sestavení.</span><span class="sxs-lookup"><span data-stu-id="6f737-139">A type in a collectible assembly can have static references to a type in another collectible assembly, but this extends the lifetime of the referenced assembly to the lifetime of the referencing assembly.</span></span>

- <span data-ttu-id="6f737-140">**Zprostředkovatel komunikace s objekty COM** V rámci kolekční sestavení nelze definovat žádná rozhraní modelu COM a žádné instance typů v rámci sestavení kolekční nelze převést na objekty modelu COM.</span><span class="sxs-lookup"><span data-stu-id="6f737-140">**COM interop** No COM interfaces can be defined within a collectible assembly, and no instances of types within a collectible assembly can be converted into COM objects.</span></span> <span data-ttu-id="6f737-141">Typ v sestavení kolekční nemůže sloužit jako obálka s možnou modelem COM (doleva) nebo obálka s možnou (RCW).</span><span class="sxs-lookup"><span data-stu-id="6f737-141">A type in a collectible assembly cannot serve as a COM callable wrapper (CCW) or runtime callable wrapper (RCW).</span></span> <span data-ttu-id="6f737-142">Typy v sestaveních kolekční však mohou používat objekty, které implementují rozhraní COM.</span><span class="sxs-lookup"><span data-stu-id="6f737-142">However, types in collectible assemblies can use objects that implement COM interfaces.</span></span>

- <span data-ttu-id="6f737-143">**Vyvolání platformy** Metody, které mají <xref:System.Runtime.InteropServices.DllImportAttribute> atribut, nebudou zkompilovány, pokud jsou deklarovány v sestavení kolekční.</span><span class="sxs-lookup"><span data-stu-id="6f737-143">**Platform invoke** Methods that have the <xref:System.Runtime.InteropServices.DllImportAttribute> attribute will not compile when they are declared in a collectible assembly.</span></span> <span data-ttu-id="6f737-144"><xref:System.Reflection.Emit.OpCodes.Calli?displayProperty=nameWithType>Instrukci nelze použít v implementaci typu v kolekční sestavení a takové typy nelze zařadit do nespravovaného kódu.</span><span class="sxs-lookup"><span data-stu-id="6f737-144">The <xref:System.Reflection.Emit.OpCodes.Calli?displayProperty=nameWithType> instruction cannot be used in the implementation of a type in a collectible assembly, and such types cannot be marshaled to unmanaged code.</span></span> <span data-ttu-id="6f737-145">Do nativního kódu však můžete zavolat pomocí vstupního bodu, který je deklarován v sestavení bez kolekční.</span><span class="sxs-lookup"><span data-stu-id="6f737-145">However, you can call into native code by using an entry point that is declared in a non-collectible assembly.</span></span>

- <span data-ttu-id="6f737-146">**Zařazování** Objekty (zejména Delegáti), které jsou definovány v sestaveních kolekční, nelze zařadit.</span><span class="sxs-lookup"><span data-stu-id="6f737-146">**Marshaling** Objects (in particular, delegates) that are defined in collectible assemblies cannot be marshaled.</span></span> <span data-ttu-id="6f737-147">Toto je omezení pro všechny přechodné emitované typy.</span><span class="sxs-lookup"><span data-stu-id="6f737-147">This is a restriction on all transient emitted types.</span></span>

- <span data-ttu-id="6f737-148">**Načítání sestavení** Generování reflexe je jediným mechanismem, který je podporován pro načítání sestavení kolekční.</span><span class="sxs-lookup"><span data-stu-id="6f737-148">**Assembly loading** Reflection emit is the only mechanism that is supported for loading collectible assemblies.</span></span> <span data-ttu-id="6f737-149">Sestavení, která jsou načtena pomocí jakékoliv jiné formy načítání sestavení, nelze uvolnit.</span><span class="sxs-lookup"><span data-stu-id="6f737-149">Assemblies that are loaded by using any other form of assembly loading cannot be unloaded.</span></span>

- <span data-ttu-id="6f737-150">**Kontextově vázané objekty** Kontext – statické proměnné nejsou podporovány.</span><span class="sxs-lookup"><span data-stu-id="6f737-150">**Context-bound objects** Context-static variables are not supported.</span></span> <span data-ttu-id="6f737-151">Typy v sestavení kolekční se nedají zvětšit <xref:System.ContextBoundObject> .</span><span class="sxs-lookup"><span data-stu-id="6f737-151">Types in a collectible assembly cannot extend <xref:System.ContextBoundObject>.</span></span> <span data-ttu-id="6f737-152">Nicméně kód v sestaveních kolekční může používat kontextově vázané objekty, které jsou definovány jinde.</span><span class="sxs-lookup"><span data-stu-id="6f737-152">However, code in collectible assemblies can use context-bound objects that are defined elsewhere.</span></span>

- <span data-ttu-id="6f737-153">**Vlákna – statická data** Proměnné statických vláken nejsou podporovány.</span><span class="sxs-lookup"><span data-stu-id="6f737-153">**Thread-static data** Thread-static variables are not supported.</span></span>

## <a name="see-also"></a><span data-ttu-id="6f737-154">Viz také</span><span class="sxs-lookup"><span data-stu-id="6f737-154">See also</span></span>

- [<span data-ttu-id="6f737-155">Generování dynamických metod a sestavení</span><span class="sxs-lookup"><span data-stu-id="6f737-155">Emitting Dynamic Methods and Assemblies</span></span>](emitting-dynamic-methods-and-assemblies.md)
