---
title: 'Návod: Vytváření kódu ve scénářích s částečnou důvěryhodností'
description: Podívejte se, jak vygenerovat kód ve scénářích s částečnou důvěryhodností. Vygenerování reflexe používá stejná rozhraní API, ale některé funkce vyžadují zvláštní oprávnění v částečně důvěryhodném kódu.
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- reflection emit, anonymously hosted dynamic methods
- partial trust, reflection
- partial trust, emitting dynamic methods
- reflection emit, partial trust scenarios
- anonymously hosted dynamic methods [.NET Framework]
- emitting dynamic assemblies,partial trust scenarios
- reflection emit, dynamic methods
- dynamic methods
ms.assetid: c45be261-2a9d-4c4e-9bd6-27f0931b7d25
ms.openlocfilehash: 70adb3ce67b45459b18741948092a912f6173731
ms.sourcegitcommit: 3d84eac0818099c9949035feb96bbe0346358504
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/21/2020
ms.locfileid: "86865187"
---
# <a name="walkthrough-emitting-code-in-partial-trust-scenarios"></a><span data-ttu-id="585b3-104">Návod: Vytváření kódu ve scénářích s částečnou důvěryhodností</span><span class="sxs-lookup"><span data-stu-id="585b3-104">Walkthrough: Emitting Code in Partial Trust Scenarios</span></span>

<span data-ttu-id="585b3-105">Vygenerování reflexe používá stejnou sadu rozhraní API v plném nebo částečném vztahu důvěryhodnosti, ale některé funkce vyžadují zvláštní oprávnění v částečně důvěryhodném kódu.</span><span class="sxs-lookup"><span data-stu-id="585b3-105">Reflection emit uses the same API set in full or partial trust, but some features require special permissions in partially trusted code.</span></span> <span data-ttu-id="585b3-106">Kromě toho, vygenerování reflexe má funkci, anonymně hostované dynamické metody, která je navržena pro použití s částečnou důvěryhodností a sestaveními transparentními pro zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="585b3-106">In addition, reflection emit has a feature, anonymously hosted dynamic methods, that is designed to be used with partial trust and by security-transparent assemblies.</span></span>

> [!NOTE]
> <span data-ttu-id="585b3-107">Před .NET Framework 3,5, vygeneruje se kód vyžadovaný <xref:System.Security.Permissions.ReflectionPermission> <xref:System.Security.Permissions.ReflectionPermissionFlag.ReflectionEmit?displayProperty=nameWithType> příznakem.</span><span class="sxs-lookup"><span data-stu-id="585b3-107">Before .NET Framework 3.5, emitting code required <xref:System.Security.Permissions.ReflectionPermission> with the <xref:System.Security.Permissions.ReflectionPermissionFlag.ReflectionEmit?displayProperty=nameWithType> flag.</span></span> <span data-ttu-id="585b3-108">Toto oprávnění je ve výchozím nastavení zahrnuté `FullTrust` v `Intranet` sadách a pojmenovaných oprávněních, ale ne v `Internet` sadě oprávnění.</span><span class="sxs-lookup"><span data-stu-id="585b3-108">This permission is included by default in the `FullTrust` and `Intranet` named permission sets, but not in the `Internet` permission set.</span></span> <span data-ttu-id="585b3-109">Proto by knihovna mohla být použita z částečné důvěryhodnosti pouze v případě, že má <xref:System.Security.SecurityCriticalAttribute> atribut a také spustil <xref:System.Security.PermissionSet.Assert%2A> metodu pro <xref:System.Security.Permissions.ReflectionPermissionFlag.ReflectionEmit> .</span><span class="sxs-lookup"><span data-stu-id="585b3-109">Therefore, a library could be used from partial trust only if it had the <xref:System.Security.SecurityCriticalAttribute> attribute and also executed an <xref:System.Security.PermissionSet.Assert%2A> method for <xref:System.Security.Permissions.ReflectionPermissionFlag.ReflectionEmit>.</span></span> <span data-ttu-id="585b3-110">Tyto knihovny vyžadují pečlivou kontrolu zabezpečení, protože chyby kódování můžou vést k bezpečnostním otvorům.</span><span class="sxs-lookup"><span data-stu-id="585b3-110">Such libraries require careful security review because coding errors could result in security holes.</span></span> <span data-ttu-id="585b3-111">.NET Framework 3,5 umožňuje, aby byl kód generován ve scénářích s částečnou důvěryhodností bez nutnosti vydávat požadavky na zabezpečení, protože generování kódu není podstatou privilegované operace.</span><span class="sxs-lookup"><span data-stu-id="585b3-111">The .NET Framework 3.5 allows code to be emitted in partial trust scenarios without issuing any security demands, because generating code is not inherently a privileged operation.</span></span> <span data-ttu-id="585b3-112">To znamená, že generovaný kód nemá žádná další oprávnění, než sestavení, které ho emituje.</span><span class="sxs-lookup"><span data-stu-id="585b3-112">That is, the generated code has no more permissions than the assembly that emits it.</span></span> <span data-ttu-id="585b3-113">To umožňuje, aby knihovny, které generují kód, byly transparentní pro zabezpečení a odebraly nutnost <xref:System.Security.Permissions.ReflectionPermissionFlag.ReflectionEmit> vyhodnotit, aby při psaní zabezpečené knihovny nevyžadovaly důkladnou kontrolu zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="585b3-113">This enables libraries that emit code to be security-transparent and removes the need to assert <xref:System.Security.Permissions.ReflectionPermissionFlag.ReflectionEmit>, so that writing a secure library does not require such a thorough security review.</span></span>

<span data-ttu-id="585b3-114">Tento návod znázorňuje následující úlohy:</span><span class="sxs-lookup"><span data-stu-id="585b3-114">This walkthrough illustrates the following tasks:</span></span>

- <span data-ttu-id="585b3-115">[Nastavení jednoduchého izolovaného prostoru pro testování částečně důvěryhodného kódu](#Setting_up).</span><span class="sxs-lookup"><span data-stu-id="585b3-115">[Setting up a simple sandbox for testing partially trusted code](#Setting_up).</span></span>

  > [!IMPORTANT]
  > <span data-ttu-id="585b3-116">Toto je jednoduchý způsob, jak experimentovat s kódem v částečném vztahu důvěryhodnosti.</span><span class="sxs-lookup"><span data-stu-id="585b3-116">This is a simple way to experiment with code in partial trust.</span></span> <span data-ttu-id="585b3-117">Chcete-li spustit kód, který skutečně pochází z nedůvěryhodných umístění, přečtěte si téma [How to: Run a částečně důvěryhodný kód v izolovaném prostoru](../misc/how-to-run-partially-trusted-code-in-a-sandbox.md).</span><span class="sxs-lookup"><span data-stu-id="585b3-117">To run code that actually comes from untrusted locations, see [How to: Run Partially Trusted Code in a Sandbox](../misc/how-to-run-partially-trusted-code-in-a-sandbox.md).</span></span>

- <span data-ttu-id="585b3-118">[Spouštění kódu v částečně důvěryhodných doménách aplikace](#Running_code).</span><span class="sxs-lookup"><span data-stu-id="585b3-118">[Running code in partially trusted application domains](#Running_code).</span></span>

- <span data-ttu-id="585b3-119">[Použití anonymně hostovaných dynamických metod k vygenerování a spuštění kódu v částečném vztahu důvěryhodnosti](#Using_methods).</span><span class="sxs-lookup"><span data-stu-id="585b3-119">[Using anonymously hosted dynamic methods to emit and execute code in partial trust](#Using_methods).</span></span>

<span data-ttu-id="585b3-120">Další informace o vygenerování kódu ve scénářích s částečnou důvěryhodností najdete v tématu [problémy se zabezpečením v generování reflexe](security-issues-in-reflection-emit.md).</span><span class="sxs-lookup"><span data-stu-id="585b3-120">For more information about emitting code in partial trust scenarios, see [Security Issues in Reflection Emit](security-issues-in-reflection-emit.md).</span></span>

<span data-ttu-id="585b3-121">Úplný seznam kódu zobrazeného v těchto postupech najdete v [části příklad](#Example) na konci tohoto návodu.</span><span class="sxs-lookup"><span data-stu-id="585b3-121">For a complete listing of the code shown in these procedures, see the [Example section](#Example) at the end of this walkthrough.</span></span>

<a name="Setting_up"></a>

## <a name="setting-up-partially-trusted-locations"></a><span data-ttu-id="585b3-122">Nastavení částečně důvěryhodných umístění</span><span class="sxs-lookup"><span data-stu-id="585b3-122">Setting up Partially Trusted Locations</span></span>

<span data-ttu-id="585b3-123">Následující dva postupy ukazují, jak nastavit umístění, ze kterých můžete testovat kód s částečnou důvěryhodností.</span><span class="sxs-lookup"><span data-stu-id="585b3-123">The following two procedures show how to set up locations from which you can test code with partial trust.</span></span>

- <span data-ttu-id="585b3-124">První postup ukazuje, jak vytvořit doménu aplikace v izolovaném prostoru, ve kterém je kód udělena přístupová oprávnění k Internetu.</span><span class="sxs-lookup"><span data-stu-id="585b3-124">The first procedure shows how to create a sandboxed application domain in which code is granted Internet permissions.</span></span>

- <span data-ttu-id="585b3-125">Druhý postup ukazuje, jak přidat <xref:System.Security.Permissions.ReflectionPermission> s <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess?displayProperty=nameWithType> příznakem do domény částečně důvěryhodné aplikace, aby bylo možné povolit přístup k soukromým datům v sestaveních se stejnou nebo menší důvěryhodností.</span><span class="sxs-lookup"><span data-stu-id="585b3-125">The second procedure shows how to add <xref:System.Security.Permissions.ReflectionPermission> with the <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess?displayProperty=nameWithType> flag to a partially trusted application domain, to enable access to private data in assemblies of equal or lesser trust.</span></span>

### <a name="creating-sandboxed-application-domains"></a><span data-ttu-id="585b3-126">Vytváření domén aplikací v izolovaném prostoru</span><span class="sxs-lookup"><span data-stu-id="585b3-126">Creating Sandboxed Application Domains</span></span>

<span data-ttu-id="585b3-127">Chcete-li vytvořit doménu aplikace, ve které jsou sestavení spouštěna s částečným vztahem důvěryhodnosti, je nutné zadat sadu oprávnění, která mají být udělena sestavení pomocí <xref:System.AppDomain.CreateDomain%28System.String%2CSystem.Security.Policy.Evidence%2CSystem.AppDomainSetup%2CSystem.Security.PermissionSet%2CSystem.Security.Policy.StrongName%5B%5D%29?displayProperty=nameWithType> přetížení metody k vytvoření domény aplikace.</span><span class="sxs-lookup"><span data-stu-id="585b3-127">To create an application domain in which your assemblies run with partial trust, you must specify the set of permissions to be granted to the assemblies by using the <xref:System.AppDomain.CreateDomain%28System.String%2CSystem.Security.Policy.Evidence%2CSystem.AppDomainSetup%2CSystem.Security.PermissionSet%2CSystem.Security.Policy.StrongName%5B%5D%29?displayProperty=nameWithType> method overload to create the application domain.</span></span> <span data-ttu-id="585b3-128">Nejjednodušší způsob, jak zadat sadu udělení, je načíst pojmenovanou sadu oprávnění ze zásad zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="585b3-128">The easiest way to specify the grant set is to retrieve a named permission set from security policy.</span></span>

<span data-ttu-id="585b3-129">Následující postup vytvoří doménu aplikace v izolovaném prostoru, ve které běží váš kód s částečnou důvěryhodností, na testování scénářů, ve kterých emitující kód může přistupovat pouze k veřejným členům veřejných typů.</span><span class="sxs-lookup"><span data-stu-id="585b3-129">The following procedure creates a sandboxed application domain that runs your code with partial trust, to test scenarios in which emitted code can access only public members of public types.</span></span> <span data-ttu-id="585b3-130">Následující postup ukazuje, jak přidat <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess> , do testovacích scénářů, ve kterých emitující kód může přistupovat k NonPublic typům a členům v sestaveních, kterým jsou udělena stejná nebo menší oprávnění.</span><span class="sxs-lookup"><span data-stu-id="585b3-130">A subsequent procedure shows how to add <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess>, to test scenarios in which emitted code can access nonpublic types and members in assemblies that are granted equal or lesser permissions.</span></span>

#### <a name="to-create-an-application-domain-with-partial-trust"></a><span data-ttu-id="585b3-131">Vytvoření domény aplikace s částečnou důvěryhodností</span><span class="sxs-lookup"><span data-stu-id="585b3-131">To create an application domain with partial trust</span></span>

1. <span data-ttu-id="585b3-132">Vytvořte sadu oprávnění pro udělení sestavením v doméně aplikace v izolovaném prostoru.</span><span class="sxs-lookup"><span data-stu-id="585b3-132">Create a permission set to grant to the assemblies in the sandboxed application domain.</span></span> <span data-ttu-id="585b3-133">V takovém případě se použije sada oprávnění zóny Internet.</span><span class="sxs-lookup"><span data-stu-id="585b3-133">In this case, the permission set of the Internet zone is used.</span></span>

    [!code-csharp[HowToEmitCodeInPartialTrust#2](../../../samples/snippets/csharp/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/cs/source.cs#2)]
    [!code-vb[HowToEmitCodeInPartialTrust#2](../../../samples/snippets/visualbasic/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/vb/source.vb#2)]

2. <span data-ttu-id="585b3-134">Vytvořte <xref:System.AppDomainSetup> objekt pro inicializaci aplikační domény pomocí cesty aplikace.</span><span class="sxs-lookup"><span data-stu-id="585b3-134">Create an <xref:System.AppDomainSetup> object to initialize the application domain with an application path.</span></span>

    > [!IMPORTANT]
    > <span data-ttu-id="585b3-135">Pro zjednodušení tento příklad kódu používá aktuální složku.</span><span class="sxs-lookup"><span data-stu-id="585b3-135">For simplicity, this code example uses the current folder.</span></span> <span data-ttu-id="585b3-136">Chcete-li spustit kód, který skutečně pochází z Internetu, použijte samostatnou složku pro nedůvěryhodný kód, jak je popsáno v tématu [How to: Run částečně důvěryhodný kód v izolovaném prostoru (sandboxu)](../misc/how-to-run-partially-trusted-code-in-a-sandbox.md).</span><span class="sxs-lookup"><span data-stu-id="585b3-136">To run code that actually comes from the Internet, use a separate folder for the untrusted code, as described in [How to: Run Partially Trusted Code in a Sandbox](../misc/how-to-run-partially-trusted-code-in-a-sandbox.md).</span></span>

    [!code-csharp[HowToEmitCodeInPartialTrust#3](../../../samples/snippets/csharp/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/cs/source.cs#3)]
    [!code-vb[HowToEmitCodeInPartialTrust#3](../../../samples/snippets/visualbasic/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/vb/source.vb#3)]

3. <span data-ttu-id="585b3-137">Vytvořte doménu aplikace a určete informace o nastavení domény aplikace a sadu udělení pro všechna sestavení, která jsou spouštěna v doméně aplikace.</span><span class="sxs-lookup"><span data-stu-id="585b3-137">Create the application domain, specifying the application domain setup information and the grant set for all assemblies that execute in the application domain.</span></span>

    [!code-csharp[HowToEmitCodeInPartialTrust#5](../../../samples/snippets/csharp/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/cs/source.cs#5)]
    [!code-vb[HowToEmitCodeInPartialTrust#5](../../../samples/snippets/visualbasic/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/vb/source.vb#5)]

    <span data-ttu-id="585b3-138">Poslední parametr <xref:System.AppDomain.CreateDomain%28System.String%2CSystem.Security.Policy.Evidence%2CSystem.AppDomainSetup%2CSystem.Security.PermissionSet%2CSystem.Security.Policy.StrongName%5B%5D%29?displayProperty=nameWithType> přetížení metody umožňuje zadat sadu sestavení, kterým chcete udělit úplný vztah důvěryhodnosti, namísto sady udělení domény aplikace.</span><span class="sxs-lookup"><span data-stu-id="585b3-138">The last parameter of the <xref:System.AppDomain.CreateDomain%28System.String%2CSystem.Security.Policy.Evidence%2CSystem.AppDomainSetup%2CSystem.Security.PermissionSet%2CSystem.Security.Policy.StrongName%5B%5D%29?displayProperty=nameWithType> method overload enables you to specify a set of assemblies that are to be granted full trust, instead of the grant set of the application domain.</span></span> <span data-ttu-id="585b3-139">Nemusíte určovat .NET Framework sestavení, která vaše aplikace používá, protože tato sestavení jsou v globální mezipaměti sestavení (GAC).</span><span class="sxs-lookup"><span data-stu-id="585b3-139">You do not have to specify the .NET Framework assemblies that your application uses, because those assemblies are in the global assembly cache.</span></span> <span data-ttu-id="585b3-140">Sestavení v globální mezipaměti sestavení (GAC) jsou vždy plně důvěryhodná.</span><span class="sxs-lookup"><span data-stu-id="585b3-140">Assemblies in the global assembly cache are always fully trusted.</span></span> <span data-ttu-id="585b3-141">Tento parametr můžete použít k určení sestavení se silným názvem, která nejsou v globální mezipaměti sestavení (GAC).</span><span class="sxs-lookup"><span data-stu-id="585b3-141">You can use this parameter to specify strong-named assemblies that are not in the global assembly cache.</span></span>

### <a name="adding-restrictedmemberaccess-to-sandboxed-domains"></a><span data-ttu-id="585b3-142">Přidání RestrictedMemberAccess k doménám v izolovaném prostoru</span><span class="sxs-lookup"><span data-stu-id="585b3-142">Adding RestrictedMemberAccess to Sandboxed Domains</span></span>

<span data-ttu-id="585b3-143">Hostitelské aplikace mohou umožňovat anonymně hostovaným dynamickým metodám přístup k soukromým datům v sestaveních, která mají úroveň důvěryhodnosti rovnou nebo nižší než úroveň důvěryhodnosti sestavení, které kód emituje.</span><span class="sxs-lookup"><span data-stu-id="585b3-143">Host applications can allow anonymously hosted dynamic methods to have access to private data in assemblies that have trust levels equal to or less than the trust level of the assembly that emits the code.</span></span> <span data-ttu-id="585b3-144">Chcete-li povolit tuto omezenou schopnost přeskočit kontroly viditelnosti JIT (just-in-time), hostitelská aplikace přidá <xref:System.Security.Permissions.ReflectionPermission> objekt s <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess?displayProperty=nameWithType> příznakem (RMA) do sady udělení.</span><span class="sxs-lookup"><span data-stu-id="585b3-144">To enable this restricted ability to skip just-in-time (JIT) visibility checks, the host application adds a <xref:System.Security.Permissions.ReflectionPermission> object with the <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess?displayProperty=nameWithType> (RMA) flag to the grant set.</span></span>

<span data-ttu-id="585b3-145">Hostitel může například udělit internetovým aplikacím přístupová oprávnění k Internetu a RMA, aby mohla Internetová aplikace vygenerovat kód, který přistupuje k soukromým datům ve vlastních sestaveních.</span><span class="sxs-lookup"><span data-stu-id="585b3-145">For example, a host might grant Internet applications Internet permissions plus RMA, so that an Internet application can emit code that accesses private data in its own assemblies.</span></span> <span data-ttu-id="585b3-146">Vzhledem k tomu, že přístup je omezen na sestavení se stejnou nebo menší důvěryhodností, Internetová aplikace nemůže přistupovat ke členům plně důvěryhodných sestavení, jako jsou .NET Framework sestavení.</span><span class="sxs-lookup"><span data-stu-id="585b3-146">Because the access is limited to assemblies of equal or lesser trust, an Internet application cannot access members of fully trusted assemblies such as .NET Framework assemblies.</span></span>

> [!NOTE]
> <span data-ttu-id="585b3-147">Aby se zabránilo zvýšení oprávnění, jsou informace zásobníku pro vydávané sestavení zahrnuty při vytváření anonymně hostovaných dynamických metod.</span><span class="sxs-lookup"><span data-stu-id="585b3-147">To prevent elevation of privilege, stack information for the emitting assembly is included when anonymously hosted dynamic methods are constructed.</span></span> <span data-ttu-id="585b3-148">Při vyvolání metody jsou zkontrolovány informace o zásobníku.</span><span class="sxs-lookup"><span data-stu-id="585b3-148">When the method is invoked, the stack information is checked.</span></span> <span data-ttu-id="585b3-149">Anonymně hostovaná dynamická metoda, která je vyvolána z plně důvěryhodného kódu, je tedy stále omezena na úroveň důvěryhodnosti pro vygenerování sestavení.</span><span class="sxs-lookup"><span data-stu-id="585b3-149">Thus, an anonymously hosted dynamic method that is invoked from fully trusted code is still limited to the trust level of the emitting assembly.</span></span>

#### <a name="to-create-an-application-domain-with-partial-trust-plus-rma"></a><span data-ttu-id="585b3-150">Vytvoření domény aplikace s částečnou důvěryhodností plus RMA</span><span class="sxs-lookup"><span data-stu-id="585b3-150">To create an application domain with partial trust plus RMA</span></span>

1. <span data-ttu-id="585b3-151">Vytvořte nový <xref:System.Security.Permissions.ReflectionPermission> objekt s <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess> příznakem (RMA) a použijte <xref:System.Security.PermissionSet.SetPermission%2A?displayProperty=nameWithType> metodu k přidání oprávnění do sady udělení.</span><span class="sxs-lookup"><span data-stu-id="585b3-151">Create a new <xref:System.Security.Permissions.ReflectionPermission> object with the <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess> (RMA) flag, and use the <xref:System.Security.PermissionSet.SetPermission%2A?displayProperty=nameWithType> method to add the permission to the grant set.</span></span>

    [!code-csharp[HowToEmitCodeInPartialTrust#7](../../../samples/snippets/csharp/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/cs/source.cs#7)]
    [!code-vb[HowToEmitCodeInPartialTrust#7](../../../samples/snippets/visualbasic/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/vb/source.vb#7)]

    <span data-ttu-id="585b3-152"><xref:System.Security.PermissionSet.AddPermission%2A>Metoda přidá oprávnění do sady udělení, pokud ještě není zahrnutá.</span><span class="sxs-lookup"><span data-stu-id="585b3-152">The <xref:System.Security.PermissionSet.AddPermission%2A> method adds the permission to the grant set if it is not already included.</span></span> <span data-ttu-id="585b3-153">Pokud je již oprávnění zahrnuto do sady udělení, zadané příznaky jsou přidány do stávajícího oprávnění.</span><span class="sxs-lookup"><span data-stu-id="585b3-153">If the permission is already included in the grant set, the specified flags are added to the existing permission.</span></span>

    > [!NOTE]
    > <span data-ttu-id="585b3-154">RMA je funkce anonymně hostovaných dynamických metod.</span><span class="sxs-lookup"><span data-stu-id="585b3-154">RMA is a feature of anonymously hosted dynamic methods.</span></span> <span data-ttu-id="585b3-155">Když běžné dynamické metody přeskočí kontroly viditelnosti JIT, vygenerovaný kód vyžaduje úplný vztah důvěryhodnosti.</span><span class="sxs-lookup"><span data-stu-id="585b3-155">When ordinary dynamic methods skip JIT visibility checks, the emitted code requires full trust.</span></span>

2. <span data-ttu-id="585b3-156">Vytvořte doménu aplikace a určete informace o nastavení domény aplikace a sadu udělení.</span><span class="sxs-lookup"><span data-stu-id="585b3-156">Create the application domain, specifying the application domain setup information and the grant set.</span></span>

    [!code-csharp[HowToEmitCodeInPartialTrust#8](../../../samples/snippets/csharp/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/cs/source.cs#8)]
    [!code-vb[HowToEmitCodeInPartialTrust#8](../../../samples/snippets/visualbasic/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/vb/source.vb#8)]

<a name="Running_code"></a>

## <a name="running-code-in-sandboxed-application-domains"></a><span data-ttu-id="585b3-157">Spouštění kódu v doménách aplikací v izolovaném prostoru</span><span class="sxs-lookup"><span data-stu-id="585b3-157">Running Code in Sandboxed Application Domains</span></span>

<span data-ttu-id="585b3-158">Následující postup vysvětluje, jak definovat třídu pomocí metod, které lze spustit v doméně aplikace, jak vytvořit instanci třídy v doméně a jak spustit její metody.</span><span class="sxs-lookup"><span data-stu-id="585b3-158">The following procedure explains how to define a class by using methods that can be executed in an application domain, how to create an instance of the class in the domain, and how to execute its methods.</span></span>

#### <a name="to-define-and-execute-a-method-in-an-application-domain"></a><span data-ttu-id="585b3-159">Definování a spuštění metody v doméně aplikace</span><span class="sxs-lookup"><span data-stu-id="585b3-159">To define and execute a method in an application domain</span></span>

1. <span data-ttu-id="585b3-160">Definujte třídu, která je odvozena z <xref:System.MarshalByRefObject> .</span><span class="sxs-lookup"><span data-stu-id="585b3-160">Define a class that derives from <xref:System.MarshalByRefObject>.</span></span> <span data-ttu-id="585b3-161">To umožňuje vytvářet instance třídy v jiných doménách aplikace a provádět volání metod napříč hranicemi aplikační domény.</span><span class="sxs-lookup"><span data-stu-id="585b3-161">This enables you to create instances of the class in other application domains and to make method calls across application domain boundaries.</span></span> <span data-ttu-id="585b3-162">Třída v tomto příkladu je pojmenována `Worker` .</span><span class="sxs-lookup"><span data-stu-id="585b3-162">The class in this example is named `Worker`.</span></span>

    [!code-csharp[HowToEmitCodeInPartialTrust#10](../../../samples/snippets/csharp/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/cs/source.cs#10)]
    [!code-vb[HowToEmitCodeInPartialTrust#10](../../../samples/snippets/visualbasic/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/vb/source.vb#10)]

2. <span data-ttu-id="585b3-163">Definujte veřejnou metodu, která obsahuje kód, který chcete spustit.</span><span class="sxs-lookup"><span data-stu-id="585b3-163">Define a public method that contains the code you want to execute.</span></span> <span data-ttu-id="585b3-164">V tomto příkladu kód generuje jednoduchou dynamickou metodu, vytvoří delegáta pro provedení metody a vyvolá delegáta.</span><span class="sxs-lookup"><span data-stu-id="585b3-164">In this example, the code emits a simple dynamic method, creates a delegate to execute the method, and invokes the delegate.</span></span>

    [!code-csharp[HowToEmitCodeInPartialTrust#11](../../../samples/snippets/csharp/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/cs/source.cs#11)]
    [!code-vb[HowToEmitCodeInPartialTrust#11](../../../samples/snippets/visualbasic/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/vb/source.vb#11)]

3. <span data-ttu-id="585b3-165">V hlavním programu Získejte zobrazovaný název sestavení.</span><span class="sxs-lookup"><span data-stu-id="585b3-165">In your main program, get the display name of your assembly.</span></span> <span data-ttu-id="585b3-166">Tento název se používá při vytváření instancí `Worker` třídy v doméně aplikace v izolovaném prostoru.</span><span class="sxs-lookup"><span data-stu-id="585b3-166">This name is used when you create instances of the `Worker` class in the sandboxed application domain.</span></span>

    [!code-csharp[HowToEmitCodeInPartialTrust#14](../../../samples/snippets/csharp/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/cs/source.cs#14)]
    [!code-vb[HowToEmitCodeInPartialTrust#14](../../../samples/snippets/visualbasic/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/vb/source.vb#14)]

4. <span data-ttu-id="585b3-167">V hlavním programu vytvořte doménu aplikace izolovaného prostoru (sandbox), jak je popsáno v [prvním postupu](#Setting_up) v tomto návodu.</span><span class="sxs-lookup"><span data-stu-id="585b3-167">In your main program, create a sandboxed application domain, as described in [the first procedure](#Setting_up) in this walkthrough.</span></span> <span data-ttu-id="585b3-168">Do sady oprávnění není nutné přidávat žádná oprávnění `Internet` , protože `SimpleEmitDemo` Metoda používá pouze veřejné metody.</span><span class="sxs-lookup"><span data-stu-id="585b3-168">You do not have to add any permissions to the `Internet` permission set, because the `SimpleEmitDemo` method uses only public methods.</span></span>

5. <span data-ttu-id="585b3-169">V hlavním programu vytvořte instanci `Worker` třídy v doméně aplikace v izolovaném prostoru.</span><span class="sxs-lookup"><span data-stu-id="585b3-169">In your main program, create an instance of the `Worker` class in the sandboxed application domain.</span></span>

    [!code-csharp[HowToEmitCodeInPartialTrust#12](../../../samples/snippets/csharp/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/cs/source.cs#12)]
    [!code-vb[HowToEmitCodeInPartialTrust#12](../../../samples/snippets/visualbasic/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/vb/source.vb#12)]

    <span data-ttu-id="585b3-170"><xref:System.AppDomain.CreateInstanceAndUnwrap%2A>Metoda vytvoří objekt v cílové doméně aplikace a vrátí proxy, který lze použít k volání vlastností a metod objektu.</span><span class="sxs-lookup"><span data-stu-id="585b3-170">The <xref:System.AppDomain.CreateInstanceAndUnwrap%2A> method creates the object in the target application domain and returns a proxy that can be used to call the properties and methods of the object.</span></span>

    > [!NOTE]
    > <span data-ttu-id="585b3-171">Použijete-li tento kód v aplikaci Visual Studio, je nutné změnit název třídy tak, aby zahrnovala obor názvů.</span><span class="sxs-lookup"><span data-stu-id="585b3-171">If you use this code in Visual Studio, you must change the name of the class to include the namespace.</span></span> <span data-ttu-id="585b3-172">Ve výchozím nastavení je oborem názvů název projektu.</span><span class="sxs-lookup"><span data-stu-id="585b3-172">By default, the namespace is the name of the project.</span></span> <span data-ttu-id="585b3-173">Pokud je projekt například "PartialTrust", musí být název třídy "PartialTrust. Worker".</span><span class="sxs-lookup"><span data-stu-id="585b3-173">For example, if the project is "PartialTrust", the class name must be "PartialTrust.Worker".</span></span>

6. <span data-ttu-id="585b3-174">Přidejte kód pro volání `SimpleEmitDemo` metody.</span><span class="sxs-lookup"><span data-stu-id="585b3-174">Add code to call the `SimpleEmitDemo` method.</span></span> <span data-ttu-id="585b3-175">Volání je zařazeno napříč hranicí domény aplikace a kód je spuštěn v doméně aplikace v izolovaném prostoru.</span><span class="sxs-lookup"><span data-stu-id="585b3-175">The call is marshaled across the application domain boundary, and the code is executed in the sandboxed application domain.</span></span>

    [!code-csharp[HowToEmitCodeInPartialTrust#13](../../../samples/snippets/csharp/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/cs/source.cs#13)]
    [!code-vb[HowToEmitCodeInPartialTrust#13](../../../samples/snippets/visualbasic/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/vb/source.vb#13)]

<a name="Using_methods"></a>

## <a name="using-anonymously-hosted-dynamic-methods"></a><span data-ttu-id="585b3-176">Použití anonymně hostovaných dynamických metod</span><span class="sxs-lookup"><span data-stu-id="585b3-176">Using Anonymously Hosted Dynamic Methods</span></span>

<span data-ttu-id="585b3-177">Anonymně hostované dynamické metody jsou přidruženy k transparentnímu sestavení poskytovanému systémem.</span><span class="sxs-lookup"><span data-stu-id="585b3-177">Anonymously hosted dynamic methods are associated with a transparent assembly that is provided by the system.</span></span> <span data-ttu-id="585b3-178">Proto kód, který obsahuje, je transparentní.</span><span class="sxs-lookup"><span data-stu-id="585b3-178">Therefore, the code they contain is transparent.</span></span> <span data-ttu-id="585b3-179">Běžné dynamické metody na druhé straně musí být přidružené k existujícímu modulu (ať už přímo určenému nebo odvozenému z přidruženého typu) a přebírat jejich úroveň zabezpečení z tohoto modulu.</span><span class="sxs-lookup"><span data-stu-id="585b3-179">Ordinary dynamic methods, on the other hand, must be associated with an existing module (whether directly specified or inferred from an associated type), and take their security level from that module.</span></span>

> [!NOTE]
> <span data-ttu-id="585b3-180">Jediným způsobem, jak přidružit dynamickou metodu k sestavení, které poskytuje anonymní hostování, je použít konstruktory, které jsou popsány v následujícím postupu.</span><span class="sxs-lookup"><span data-stu-id="585b3-180">The only way to associate a dynamic method with the assembly that provides anonymous hosting is to use the constructors that are described in the following procedure.</span></span> <span data-ttu-id="585b3-181">Nemůžete explicitně zadat modul v sestavení anonymního hostování.</span><span class="sxs-lookup"><span data-stu-id="585b3-181">You cannot explicitly specify a module in the anonymous hosting assembly.</span></span>

<span data-ttu-id="585b3-182">Běžné dynamické metody mají přístup k interním členům modulu, ke kterým jsou přidruženi, nebo k soukromým členům typu, ke kterému jsou přidruženy.</span><span class="sxs-lookup"><span data-stu-id="585b3-182">Ordinary dynamic methods have access to the internal members of the module they are associated with, or to the private members of the type they are associated with.</span></span> <span data-ttu-id="585b3-183">Vzhledem k tomu, že anonymně hostované dynamické metody jsou izolované od jiného kódu, nemají přístup k soukromým datům.</span><span class="sxs-lookup"><span data-stu-id="585b3-183">Because anonymously hosted dynamic methods are isolated from other code, they do not have access to private data.</span></span> <span data-ttu-id="585b3-184">Mají však omezenou schopnost přeskočit kontroly viditelnosti JIT a získat tak přístup k soukromým datům.</span><span class="sxs-lookup"><span data-stu-id="585b3-184">However, they do have a restricted ability to skip JIT visibility checks to gain access to private data.</span></span> <span data-ttu-id="585b3-185">Tato možnost je omezena na sestavení, která mají úrovně důvěryhodnosti rovna nebo nižší než úroveň důvěryhodnosti sestavení, které kód emituje.</span><span class="sxs-lookup"><span data-stu-id="585b3-185">This ability is limited to assemblies that have trust levels equal to or less than the trust level of the assembly that emits the code.</span></span>

<span data-ttu-id="585b3-186">Aby se zabránilo zvýšení oprávnění, jsou informace zásobníku pro vydávané sestavení zahrnuty při vytváření anonymně hostovaných dynamických metod.</span><span class="sxs-lookup"><span data-stu-id="585b3-186">To prevent elevation of privilege, stack information for the emitting assembly is included when anonymously hosted dynamic methods are constructed.</span></span> <span data-ttu-id="585b3-187">Při vyvolání metody jsou zkontrolovány informace o zásobníku.</span><span class="sxs-lookup"><span data-stu-id="585b3-187">When the method is invoked, the stack information is checked.</span></span> <span data-ttu-id="585b3-188">Anonymně hostovaná dynamická metoda, která je vyvolána z plně důvěryhodného kódu, je stále omezena na úroveň důvěryhodnosti sestavení, které ji vyvolalo.</span><span class="sxs-lookup"><span data-stu-id="585b3-188">An anonymously hosted dynamic method that is invoked from fully trusted code is still limited to the trust level of the assembly that emitted it.</span></span>

#### <a name="to-use-anonymously-hosted-dynamic-methods"></a><span data-ttu-id="585b3-189">Použití anonymně hostovaných dynamických metod</span><span class="sxs-lookup"><span data-stu-id="585b3-189">To use anonymously hosted dynamic methods</span></span>

- <span data-ttu-id="585b3-190">Vytvořte anonymně hostovanou dynamickou metodu pomocí konstruktoru, který neurčuje přidružený modul nebo typ.</span><span class="sxs-lookup"><span data-stu-id="585b3-190">Create an anonymously hosted dynamic method by using a constructor that does not specify an associated module or type.</span></span>

  [!code-csharp[HowToEmitCodeInPartialTrust#15](../../../samples/snippets/csharp/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/cs/source.cs#15)]
  [!code-vb[HowToEmitCodeInPartialTrust#15](../../../samples/snippets/visualbasic/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/vb/source.vb#15)]

  <span data-ttu-id="585b3-191">Pokud anonymně hostovaná dynamická metoda používá pouze veřejné typy a metody, nevyžaduje přístup k omezenému členu a není nutné přeskočí kontroly viditelnosti JIT.</span><span class="sxs-lookup"><span data-stu-id="585b3-191">If an anonymously hosted dynamic method uses only public types and methods, it does not require restricted member access and does not have to skip JIT visibility checks.</span></span>

  <span data-ttu-id="585b3-192">K vygenerování dynamické metody nejsou potřeba žádná zvláštní oprávnění, ale generovaný kód vyžaduje oprávnění, která jsou vyžadovaná typy a metodami, které používá.</span><span class="sxs-lookup"><span data-stu-id="585b3-192">No special permissions are required to emit a dynamic method, but the emitted code requires the permissions that are demanded by the types and methods it uses.</span></span> <span data-ttu-id="585b3-193">Například pokud vygenerovaný kód volá metodu, která přistupuje k souboru, vyžaduje <xref:System.Security.Permissions.FileIOPermission> .</span><span class="sxs-lookup"><span data-stu-id="585b3-193">For example, if the emitted code calls a method that accesses a file, it requires <xref:System.Security.Permissions.FileIOPermission>.</span></span> <span data-ttu-id="585b3-194">Pokud úroveň vztahu důvěryhodnosti neobsahuje toto oprávnění, je vyvolána výjimka zabezpečení při spuštění generovaného kódu.</span><span class="sxs-lookup"><span data-stu-id="585b3-194">If the trust level does not include that permission, a security exception is thrown when the emitted code is executed.</span></span> <span data-ttu-id="585b3-195">Zde zobrazený kód emituje dynamickou metodu, která používá pouze <xref:System.Console.WriteLine%2A?displayProperty=nameWithType> metodu.</span><span class="sxs-lookup"><span data-stu-id="585b3-195">The code shown here emits a dynamic method that uses only the <xref:System.Console.WriteLine%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="585b3-196">Proto lze kód spustit z částečně důvěryhodných umístění.</span><span class="sxs-lookup"><span data-stu-id="585b3-196">Therefore, the code can be executed from partially trusted locations.</span></span>

- <span data-ttu-id="585b3-197">Případně můžete vytvořit anonymně hostovanou dynamickou metodu s omezenou schopností přeskočit kontroly viditelnosti JIT pomocí <xref:System.Reflection.Emit.DynamicMethod.%23ctor%28System.String%2CSystem.Type%2CSystem.Type%5B%5D%2CSystem.Boolean%29> konstruktoru a zadáním `true` `restrictedSkipVisibility` parametru.</span><span class="sxs-lookup"><span data-stu-id="585b3-197">Alternatively, create an anonymously hosted dynamic method with restricted ability to skip JIT visibility checks, by using the <xref:System.Reflection.Emit.DynamicMethod.%23ctor%28System.String%2CSystem.Type%2CSystem.Type%5B%5D%2CSystem.Boolean%29> constructor and specifying `true` for the `restrictedSkipVisibility` parameter.</span></span>

  [!code-csharp[HowToEmitCodeInPartialTrust#16](../../../samples/snippets/csharp/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/cs/source.cs#16)]
  [!code-vb[HowToEmitCodeInPartialTrust#16](../../../samples/snippets/visualbasic/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/vb/source.vb#16)]

  <span data-ttu-id="585b3-198">Omezení je, že anonymně hostovaná dynamická metoda má přístup k soukromým datům pouze v sestaveních s úrovní důvěryhodnosti, která se rovná nebo je menší než úroveň důvěryhodnosti vygenerovaného sestavení.</span><span class="sxs-lookup"><span data-stu-id="585b3-198">The restriction is that the anonymously hosted dynamic method can access private data only in assemblies with trust levels equal to or less than the trust level of the emitting assembly.</span></span> <span data-ttu-id="585b3-199">Například pokud je dynamická metoda prováděna s důvěryhodností Internetu, může získat přístup k soukromým datům v jiných sestaveních, která jsou také vykonávána s důvěryhodností Internetu, ale nemůže získat přístup k soukromým datům .NET Framework sestavení.</span><span class="sxs-lookup"><span data-stu-id="585b3-199">For example, if the dynamic method is executing with Internet trust, it can access private data in other assemblies that are also executing with Internet trust, but it cannot access private data of .NET Framework assemblies.</span></span> <span data-ttu-id="585b3-200">.NET Framework sestavení jsou nainstalována v globální mezipaměti sestavení (GAC) a jsou vždy plně důvěryhodná.</span><span class="sxs-lookup"><span data-stu-id="585b3-200">.NET Framework assemblies are installed in the global assembly cache and are always fully trusted.</span></span>

  <span data-ttu-id="585b3-201">Anonymně hostované dynamické metody mohou použít tuto omezenou schopnost přeskočit kontroly viditelnosti JIT pouze v případě, že hostitelská aplikace udělí <xref:System.Security.Permissions.ReflectionPermission> <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess?displayProperty=nameWithType> příznak.</span><span class="sxs-lookup"><span data-stu-id="585b3-201">Anonymously hosted dynamic methods can use this restricted ability to skip JIT visibility checks only if the host application grants <xref:System.Security.Permissions.ReflectionPermission> with the <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess?displayProperty=nameWithType> flag.</span></span> <span data-ttu-id="585b3-202">Požadavek na toto oprávnění je proveden při vyvolání metody.</span><span class="sxs-lookup"><span data-stu-id="585b3-202">The demand for this permission is made when the method is invoked.</span></span>

  > [!NOTE]
  > <span data-ttu-id="585b3-203">Informace zásobníku volání pro vydávané sestavení jsou zahrnuty, pokud je vytvořena dynamická metoda.</span><span class="sxs-lookup"><span data-stu-id="585b3-203">Call stack information for the emitting assembly is included when the dynamic method is constructed.</span></span> <span data-ttu-id="585b3-204">Proto se požadavek provede proti oprávněním vysílat sestavení namísto sestavení, které vyvolá metodu.</span><span class="sxs-lookup"><span data-stu-id="585b3-204">Therefore, the demand is made against the permissions of the emitting assembly instead of the assembly that invokes the method.</span></span> <span data-ttu-id="585b3-205">Tím se zabrání spuštění vygenerovaného kódu se zvýšenými oprávněními.</span><span class="sxs-lookup"><span data-stu-id="585b3-205">This prevents the emitted code from being executed with elevated permissions.</span></span>

  <span data-ttu-id="585b3-206">[Úplný příklad kódu](#Example) na konci tohoto návodu ukazuje použití a omezení přístupu ke členům s omezeným přístupem.</span><span class="sxs-lookup"><span data-stu-id="585b3-206">The [complete code example](#Example) at the end of this walkthrough demonstrates the use and limitations of restricted member access.</span></span> <span data-ttu-id="585b3-207">Jeho `Worker` Třída obsahuje metodu, která může vytvořit anonymně hostované dynamické metody s nebo bez omezené možnosti přeskočit kontroly viditelnosti a příklad ukazuje výsledek spuštění této metody v aplikačních doménách, které mají různé úrovně důvěryhodnosti.</span><span class="sxs-lookup"><span data-stu-id="585b3-207">Its `Worker` class includes a method that can create anonymously hosted dynamic methods with or without the restricted ability to skip visibility checks, and the example shows the result of executing this method in application domains that have different trust levels.</span></span>

  > [!NOTE]
  > <span data-ttu-id="585b3-208">Omezená schopnost přeskočit kontroly viditelnosti je funkce anonymně hostovaných dynamických metod.</span><span class="sxs-lookup"><span data-stu-id="585b3-208">The restricted ability to skip visibility checks is a feature of anonymously hosted dynamic methods.</span></span> <span data-ttu-id="585b3-209">Když běžné dynamické metody přeskočí kontroly viditelnosti JIT, musí jim být udělen úplný vztah důvěryhodnosti.</span><span class="sxs-lookup"><span data-stu-id="585b3-209">When ordinary dynamic methods skip JIT visibility checks, they must be granted full trust.</span></span>

<a name="Example"></a>

## <a name="example"></a><span data-ttu-id="585b3-210">Příklad</span><span class="sxs-lookup"><span data-stu-id="585b3-210">Example</span></span>

### <a name="description"></a><span data-ttu-id="585b3-211">Popis</span><span class="sxs-lookup"><span data-stu-id="585b3-211">Description</span></span>

<span data-ttu-id="585b3-212">Následující příklad kódu ukazuje použití <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess> příznaku k umožnění anonymně hostovaným dynamickým metodám přeskočit kontroly viditelnosti JIT, ale pouze v případě, že je cílový člen na stejné nebo nižší úrovni důvěryhodnosti než sestavení, které kód emituje.</span><span class="sxs-lookup"><span data-stu-id="585b3-212">The following code example demonstrates the use of the <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess> flag to allow anonymously hosted dynamic methods to skip JIT visibility checks, but only when the target member is at an equal or lower level of trust than the assembly that emits the code.</span></span>

<span data-ttu-id="585b3-213">V příkladu je definována `Worker` třída, která může být zařazena mezi hranice aplikační domény.</span><span class="sxs-lookup"><span data-stu-id="585b3-213">The example defines a `Worker` class that can be marshaled across application domain boundaries.</span></span> <span data-ttu-id="585b3-214">Třída má dvě `AccessPrivateMethod` přetížení metod, které generují a spouštějí dynamické metody.</span><span class="sxs-lookup"><span data-stu-id="585b3-214">The class has two `AccessPrivateMethod` method overloads that emit and execute dynamic methods.</span></span> <span data-ttu-id="585b3-215">První přetížení emituje dynamickou metodu, která volá soukromou `PrivateMethod` metodu `Worker` třídy, a může vygenerovat dynamickou metodu s kontrolami viditelnosti JIT nebo bez nich.</span><span class="sxs-lookup"><span data-stu-id="585b3-215">The first overload emits a dynamic method that calls the private `PrivateMethod` method of the `Worker` class, and it can emit the dynamic method with or without JIT visibility checks.</span></span> <span data-ttu-id="585b3-216">Druhé přetížení emituje dynamickou metodu, která přistupuje k `internal` vlastnosti ( `Friend` vlastnost v Visual Basic) <xref:System.String> třídy.</span><span class="sxs-lookup"><span data-stu-id="585b3-216">The second overload emits a dynamic method that accesses an `internal` property (`Friend` property in Visual Basic) of the <xref:System.String> class.</span></span>

<span data-ttu-id="585b3-217">V příkladu se používá pomocná metoda pro vytvoření sady udělení omezené na `Internet` oprávnění a poté vytvoří doménu aplikace pomocí <xref:System.AppDomain.CreateDomain%28System.String%2CSystem.Security.Policy.Evidence%2CSystem.AppDomainSetup%2CSystem.Security.PermissionSet%2CSystem.Security.Policy.StrongName%5B%5D%29?displayProperty=nameWithType> přetížení metody k určení toho, že veškerý kód, který se spustí v doméně, používá tuto sadu udělení.</span><span class="sxs-lookup"><span data-stu-id="585b3-217">The example uses a helper method to create a grant set limited to `Internet` permissions, and then creates an application domain, using the <xref:System.AppDomain.CreateDomain%28System.String%2CSystem.Security.Policy.Evidence%2CSystem.AppDomainSetup%2CSystem.Security.PermissionSet%2CSystem.Security.Policy.StrongName%5B%5D%29?displayProperty=nameWithType> method overload to specify that all code that executes in the domain uses this grant set.</span></span> <span data-ttu-id="585b3-218">Příklad vytvoří instanci `Worker` třídy v aplikační doméně a spustí `AccessPrivateMethod` metodu dvakrát.</span><span class="sxs-lookup"><span data-stu-id="585b3-218">The example creates an instance of the `Worker` class in the application domain, and executes the `AccessPrivateMethod` method two times.</span></span>

- <span data-ttu-id="585b3-219">Při prvním `AccessPrivateMethod` spuštění metody se vynutily kontroly viditelnosti JIT.</span><span class="sxs-lookup"><span data-stu-id="585b3-219">The first time the `AccessPrivateMethod` method is executed, JIT visibility checks are enforced.</span></span> <span data-ttu-id="585b3-220">Dynamická metoda se při vyvolání nezdařila, protože kontroly viditelnosti JIT brání v přístupu k soukromé metodě.</span><span class="sxs-lookup"><span data-stu-id="585b3-220">The dynamic method fails when it is invoked, because JIT visibility checks prevent it from accessing the private method.</span></span>

- <span data-ttu-id="585b3-221">Při druhém `AccessPrivateMethod` spuštění metody se kontroly viditelnosti JIT přeskočí.</span><span class="sxs-lookup"><span data-stu-id="585b3-221">The second time the `AccessPrivateMethod` method is executed, JIT visibility checks are skipped.</span></span> <span data-ttu-id="585b3-222">Dynamická metoda je při kompilaci neúspěšná, protože `Internet` sada udělení neuděluje dostatečná oprávnění k přeskočení kontrol viditelnosti.</span><span class="sxs-lookup"><span data-stu-id="585b3-222">The dynamic method fails when it is compiled, because the `Internet` grant set does not grant sufficient permissions to skip visibility checks.</span></span>

<span data-ttu-id="585b3-223">Tento příklad přidá <xref:System.Security.Permissions.ReflectionPermission> <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess?displayProperty=nameWithType> do sady udělení.</span><span class="sxs-lookup"><span data-stu-id="585b3-223">The example adds <xref:System.Security.Permissions.ReflectionPermission> with <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess?displayProperty=nameWithType> to the grant set.</span></span> <span data-ttu-id="585b3-224">Příklad potom vytvoří druhou doménu a určí, že veškerý kód, který se spustí v doméně, má oprávnění v nové sadě udělení.</span><span class="sxs-lookup"><span data-stu-id="585b3-224">The example then creates a second domain, specifying that all code that executes in the domain is granted the permissions in the new grant set.</span></span> <span data-ttu-id="585b3-225">Příklad vytvoří instanci `Worker` třídy v nové aplikační doméně a provede obě přetížení `AccessPrivateMethod` metody.</span><span class="sxs-lookup"><span data-stu-id="585b3-225">The example creates an instance of the `Worker` class in the new application domain, and executes both overloads of the `AccessPrivateMethod` method.</span></span>

- <span data-ttu-id="585b3-226">První přetížení `AccessPrivateMethod` metody je provedeno a kontroly viditelnosti JIT se přeskočí.</span><span class="sxs-lookup"><span data-stu-id="585b3-226">The first overload of the `AccessPrivateMethod` method is executed, and JIT visibility checks are skipped.</span></span> <span data-ttu-id="585b3-227">Dynamická metoda se zkompiluje a provede úspěšně, protože sestavení, které generuje kód, je stejné jako sestavení, které obsahuje soukromou metodu.</span><span class="sxs-lookup"><span data-stu-id="585b3-227">The dynamic method compiles and executes successfully, because the assembly that emits the code is the same as the assembly that contains the private method.</span></span> <span data-ttu-id="585b3-228">Proto se úrovně důvěryhodnosti rovnají.</span><span class="sxs-lookup"><span data-stu-id="585b3-228">Therefore, the trust levels are equal.</span></span> <span data-ttu-id="585b3-229">Pokud aplikace, která obsahuje `Worker` třídu, obsahovala několik sestavení, stejný proces by byl úspěšný pro každé z těchto sestavení, protože by všechny byly na stejné úrovni vztahu důvěryhodnosti.</span><span class="sxs-lookup"><span data-stu-id="585b3-229">If the application that contains the `Worker` class had several assemblies, the same process would succeed for any one of those assemblies, because they would all be at the same trust level.</span></span>

- <span data-ttu-id="585b3-230">Druhé přetížení `AccessPrivateMethod` metody je provedeno a znovu se kontrolám viditelnosti JIT přeskočí.</span><span class="sxs-lookup"><span data-stu-id="585b3-230">The second overload of the `AccessPrivateMethod` method is executed, and again JIT visibility checks are skipped.</span></span> <span data-ttu-id="585b3-231">Tentokrát dynamická metoda při kompilaci dojde k chybě, protože se pokouší o přístup k `internal` `FirstChar` vlastnosti <xref:System.String> třídy.</span><span class="sxs-lookup"><span data-stu-id="585b3-231">This time the dynamic method fails when it is compiled, because it tries to access the `internal` `FirstChar` property of the <xref:System.String> class.</span></span> <span data-ttu-id="585b3-232">Sestavení, které obsahuje <xref:System.String> třídu, je plně důvěryhodné.</span><span class="sxs-lookup"><span data-stu-id="585b3-232">The assembly that contains the <xref:System.String> class is fully trusted.</span></span> <span data-ttu-id="585b3-233">Proto je na vyšší úrovni důvěryhodnosti než sestavení, které kód emituje.</span><span class="sxs-lookup"><span data-stu-id="585b3-233">Therefore, it is at a higher level of trust than the assembly that emits the code.</span></span>

<span data-ttu-id="585b3-234">Toto porovnání ukazuje <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess?displayProperty=nameWithType> , jak umožňuje částečně důvěryhodnému kódu přeskočit kontroly viditelnosti pro jiný částečně důvěryhodný kód bez narušení zabezpečení důvěryhodného kódu.</span><span class="sxs-lookup"><span data-stu-id="585b3-234">This comparison shows how <xref:System.Security.Permissions.ReflectionPermissionFlag.RestrictedMemberAccess?displayProperty=nameWithType> enables partially trusted code to skip visibility checks for other partially trusted code without compromising the security of trusted code.</span></span>

### <a name="code"></a><span data-ttu-id="585b3-235">Kód</span><span class="sxs-lookup"><span data-stu-id="585b3-235">Code</span></span>

[!code-csharp[HowToEmitCodeInPartialTrust#1](../../../samples/snippets/csharp/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/cs/source.cs#1)]
[!code-vb[HowToEmitCodeInPartialTrust#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/HowToEmitCodeInPartialTrust/vb/source.vb#1)]

## <a name="compiling-the-code"></a><span data-ttu-id="585b3-236">Probíhá kompilace kódu</span><span class="sxs-lookup"><span data-stu-id="585b3-236">Compiling the Code</span></span>

- <span data-ttu-id="585b3-237">Pokud tento příklad kódu sestavíte v aplikaci Visual Studio, je nutné změnit název třídy tak, aby zahrnoval obor názvů, když ho předáte do <xref:System.AppDomain.CreateInstanceAndUnwrap%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="585b3-237">If you build this code example in Visual Studio, you must change the name of the class to include the namespace when you pass it to the <xref:System.AppDomain.CreateInstanceAndUnwrap%2A> method.</span></span> <span data-ttu-id="585b3-238">Ve výchozím nastavení je oborem názvů název projektu.</span><span class="sxs-lookup"><span data-stu-id="585b3-238">By default, the namespace is the name of the project.</span></span> <span data-ttu-id="585b3-239">Pokud je projekt například "PartialTrust", musí být název třídy "PartialTrust. Worker".</span><span class="sxs-lookup"><span data-stu-id="585b3-239">For example, if the project is "PartialTrust", the class name must be "PartialTrust.Worker".</span></span>

## <a name="see-also"></a><span data-ttu-id="585b3-240">Viz také</span><span class="sxs-lookup"><span data-stu-id="585b3-240">See also</span></span>

- [<span data-ttu-id="585b3-241">Bezpečnostní problémy v generování reflexe</span><span class="sxs-lookup"><span data-stu-id="585b3-241">Security Issues in Reflection Emit</span></span>](security-issues-in-reflection-emit.md)
- [<span data-ttu-id="585b3-242">Postupy: Spuštění částečně důvěryhodného kódu v izolovaném prostoru</span><span class="sxs-lookup"><span data-stu-id="585b3-242">How to: Run Partially Trusted Code in a Sandbox</span></span>](../misc/how-to-run-partially-trusted-code-in-a-sandbox.md)
