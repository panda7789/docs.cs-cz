---
title: Naplnění datové sady z adaptéru dat
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: 3fa0ac7d-e266-4954-bfac-3fbe2f913153
ms.openlocfilehash: ecfd2c3a31b42b380c593aef0bbc23775874cc7a
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/23/2019
ms.locfileid: "61878199"
---
# <a name="populating-a-dataset-from-a-dataadapter"></a><span data-ttu-id="28aac-102">Naplnění datové sady z adaptéru dat</span><span class="sxs-lookup"><span data-stu-id="28aac-102">Populating a DataSet from a DataAdapter</span></span>
<span data-ttu-id="28aac-103">[!INCLUDE[vstecado](../../../../includes/vstecado-md.md)] <xref:System.Data.DataSet> Rezidentní reprezentace dat, které poskytuje konzistentní relační programovací model bez ohledu na zdroj dat je.</span><span class="sxs-lookup"><span data-stu-id="28aac-103">The [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)]<xref:System.Data.DataSet> is a memory-resident representation of data that provides a consistent relational programming model independent of the data source.</span></span> <span data-ttu-id="28aac-104">`DataSet` Představuje ucelenou sadu dat, která obsahuje tabulky, omezení a relace mezi tabulkami.</span><span class="sxs-lookup"><span data-stu-id="28aac-104">The `DataSet` represents a complete set of data that includes tables, constraints, and relationships among the tables.</span></span> <span data-ttu-id="28aac-105">Protože `DataSet` je nezávislý na zdroji dat `DataSet` může obsahovat místní aplikaci a data z různých zdrojů dat.</span><span class="sxs-lookup"><span data-stu-id="28aac-105">Because the `DataSet` is independent of the data source, a `DataSet` can include data local to the application, and data from multiple data sources.</span></span> <span data-ttu-id="28aac-106">Interakce s stávajících zdrojů dat je řízen pomocí `DataAdapter`.</span><span class="sxs-lookup"><span data-stu-id="28aac-106">Interaction with existing data sources is controlled through the `DataAdapter`.</span></span>  
  
 <span data-ttu-id="28aac-107">`SelectCommand` Vlastnost `DataAdapter` je `Command` objekt, který načítá data z datového zdroje.</span><span class="sxs-lookup"><span data-stu-id="28aac-107">The `SelectCommand` property of the `DataAdapter` is a `Command` object that retrieves data from the data source.</span></span> <span data-ttu-id="28aac-108">`InsertCommand`, `UpdateCommand`, A `DeleteCommand` vlastnosti `DataAdapter` jsou `Command` objekty, které spravují aktualizace dat ve zdroji dat podle změny provedené v datech v `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="28aac-108">The `InsertCommand`, `UpdateCommand`, and `DeleteCommand` properties of the `DataAdapter` are `Command` objects that manage updates to the data in the data source according to modifications made to the data in the `DataSet`.</span></span> <span data-ttu-id="28aac-109">Tyto vlastnosti se budeme věnovat jednotlivě podrobněji v [aktualizace zdroje dat pomocí adaptérů dat](../../../../docs/framework/data/adonet/updating-data-sources-with-dataadapters.md).</span><span class="sxs-lookup"><span data-stu-id="28aac-109">These properties are covered in more detail in [Updating Data Sources with DataAdapters](../../../../docs/framework/data/adonet/updating-data-sources-with-dataadapters.md).</span></span>  
  
 <span data-ttu-id="28aac-110">`Fill` Metodu `DataAdapter` slouží k naplnění `DataSet` s výsledky `SelectCommand` z `DataAdapter`.</span><span class="sxs-lookup"><span data-stu-id="28aac-110">The `Fill` method of the `DataAdapter` is used to populate a `DataSet` with the results of the `SelectCommand` of the `DataAdapter`.</span></span> <span data-ttu-id="28aac-111">`Fill` jako argumenty přijímá `DataSet` vyplní a `DataTable` objekt nebo název `DataTable` tankujeme řádků vrácených `SelectCommand`.</span><span class="sxs-lookup"><span data-stu-id="28aac-111">`Fill` takes as its arguments a `DataSet` to be populated, and a `DataTable` object, or the name of the `DataTable` to be filled with the rows returned from the `SelectCommand`.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="28aac-112">Použití `DataAdapter` chcete načíst všechny tabulky trvá určitou dobu, zejména v případě, že existují mnoho řádků v tabulce.</span><span class="sxs-lookup"><span data-stu-id="28aac-112">Using the `DataAdapter` to retrieve all of a table takes time, especially if there are many rows in the table.</span></span> <span data-ttu-id="28aac-113">Důvodem je, že přístup k databázi, lokalizuje a zpracování dat, a následný přenos dat do klienta je časově náročné.</span><span class="sxs-lookup"><span data-stu-id="28aac-113">This is because accessing the database, locating and processing the data, and then transferring the data to the client is time-consuming.</span></span> <span data-ttu-id="28aac-114">Všechny tabulky potažením prstem klienta zamkne také všechny řádky na serveru.</span><span class="sxs-lookup"><span data-stu-id="28aac-114">Pulling all of the table to the client also locks all of the rows on the server.</span></span> <span data-ttu-id="28aac-115">Chcete-li zvýšit výkon, můžete použít `WHERE` klauzule, která výrazně snížit počet řádků vrácených do klienta.</span><span class="sxs-lookup"><span data-stu-id="28aac-115">To improve performance, you can use the `WHERE` clause to greatly reduce the number of rows returned to the client.</span></span> <span data-ttu-id="28aac-116">Může také snížit množství dat vrácených do klienta pouze explicitně uvedete seznam požadovaných sloupců v `SELECT` příkazu.</span><span class="sxs-lookup"><span data-stu-id="28aac-116">You can also reduce the amount of data returned to the client by only explicitly listing required columns in the `SELECT` statement.</span></span> <span data-ttu-id="28aac-117">Další dobrá alternativním řešením je načíst řádky v dávkách (například několik stovek řádků najednou) a další dávku načíst pouze po dokončení aktuální dávkou klienta.</span><span class="sxs-lookup"><span data-stu-id="28aac-117">Another good workaround is to retrieve the rows in batches (such as several hundred rows at a time) and only retrieve the next batch when the client is finished with the current batch.</span></span>  
  
 <span data-ttu-id="28aac-118">`Fill` Metoda používá `DataReader` objekt implicitně vrátí názvy sloupců a typy, které se používají k vytváření tabulek v `DataSet`a dat k vyplnění řádků tabulky v `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="28aac-118">The `Fill` method uses the `DataReader` object implicitly to return the column names and types that are used to create the tables in the `DataSet`, and the data to populate the rows of the tables in the `DataSet`.</span></span> <span data-ttu-id="28aac-119">Tabulky a sloupce jsou vytvářeny, pouze pokud ještě splněné nejsou; v opačném případě `Fill` používá stávající `DataSet` schématu.</span><span class="sxs-lookup"><span data-stu-id="28aac-119">Tables and columns are only created if they do not already exist; otherwise `Fill` uses the existing `DataSet` schema.</span></span> <span data-ttu-id="28aac-120">Typy sloupců vytvořených jako [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] typy podle tabulky v [mapování datového typu v ADO.NET](../../../../docs/framework/data/adonet/data-type-mappings-in-ado-net.md).</span><span class="sxs-lookup"><span data-stu-id="28aac-120">Column types are created as [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] types according to the tables in [Data Type Mappings in ADO.NET](../../../../docs/framework/data/adonet/data-type-mappings-in-ado-net.md).</span></span> <span data-ttu-id="28aac-121">Primární klíče se nevytvoří, pokud existují ve zdroji dat a `DataAdapter` **.**`MissingSchemaAction`</span><span class="sxs-lookup"><span data-stu-id="28aac-121">Primary keys are not created unless they exist in the data source and `DataAdapter`**.**`MissingSchemaAction`</span></span> <span data-ttu-id="28aac-122">je nastavena na `MissingSchemaAction` **.** `AddWithKey`.</span><span class="sxs-lookup"><span data-stu-id="28aac-122">is set to `MissingSchemaAction`**.**`AddWithKey`.</span></span> <span data-ttu-id="28aac-123">Pokud `Fill` zjistí, že primární klíč pro tabulku existuje, přepíše data v `DataSet` s daty ze zdroje dat pro řádky, kde se hodnoty pro sloupec primárního klíče shodují řádku vrácená ze zdroje dat.</span><span class="sxs-lookup"><span data-stu-id="28aac-123">If `Fill` finds that a primary key exists for a table, it will overwrite data in the `DataSet` with data from the data source for rows where the primary key column values match those of the row returned from the data source.</span></span> <span data-ttu-id="28aac-124">Pokud se nenajde žádný primární klíč, data se připojí k tabulkám ve `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="28aac-124">If no primary key is found, the data is appended to the tables in the `DataSet`.</span></span> <span data-ttu-id="28aac-125">`Fill` používá všechna mapování, které mohou existovat při naplňování `DataSet` (naleznete v tématu [adaptéru dat, datové tabulky a DataColumn mapování](../../../../docs/framework/data/adonet/dataadapter-datatable-and-datacolumn-mappings.md)).</span><span class="sxs-lookup"><span data-stu-id="28aac-125">`Fill` uses any mappings that may exist when you populate the `DataSet` (see [DataAdapter DataTable and DataColumn Mappings](../../../../docs/framework/data/adonet/dataadapter-datatable-and-datacolumn-mappings.md)).</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="28aac-126">Pokud `SelectCommand` vrátí výsledky z vnějšího spojení, `DataAdapter` nenastaví `PrimaryKey` hodnoty pro výsledné `DataTable`.</span><span class="sxs-lookup"><span data-stu-id="28aac-126">If the `SelectCommand` returns the results of an OUTER JOIN, the `DataAdapter` does not set a `PrimaryKey` value for the resulting `DataTable`.</span></span> <span data-ttu-id="28aac-127">Je nutné definovat `PrimaryKey` sami, abyste měli jistotu, že jsou správně přeložit duplicitní řádky.</span><span class="sxs-lookup"><span data-stu-id="28aac-127">You must define the `PrimaryKey` yourself to make sure that duplicate rows are resolved correctly.</span></span> <span data-ttu-id="28aac-128">Další informace najdete v tématu [definování primárních klíčů](../../../../docs/framework/data/adonet/dataset-datatable-dataview/defining-primary-keys.md).</span><span class="sxs-lookup"><span data-stu-id="28aac-128">For more information, see [Defining Primary Keys](../../../../docs/framework/data/adonet/dataset-datatable-dataview/defining-primary-keys.md).</span></span>  
  
 <span data-ttu-id="28aac-129">Následující příklad kódu vytvoří instanci <xref:System.Data.SqlClient.SqlDataAdapter> , která používá <xref:System.Data.SqlClient.SqlConnection> Microsoft SQL Server `Northwind` databáze a naplní <xref:System.Data.DataTable> v `DataSet` s seznam zákazníků.</span><span class="sxs-lookup"><span data-stu-id="28aac-129">The following code example creates an instance of a <xref:System.Data.SqlClient.SqlDataAdapter> that uses a <xref:System.Data.SqlClient.SqlConnection> to the Microsoft SQL Server `Northwind` database and populates a <xref:System.Data.DataTable> in a `DataSet` with the list of customers.</span></span> <span data-ttu-id="28aac-130">Příkaz jazyka SQL a <xref:System.Data.SqlClient.SqlConnection> argumenty předané <xref:System.Data.SqlClient.SqlDataAdapter> konstruktoru se používají k vytváření <xref:System.Data.SqlClient.SqlDataAdapter.SelectCommand%2A> vlastnost <xref:System.Data.SqlClient.SqlDataAdapter>.</span><span class="sxs-lookup"><span data-stu-id="28aac-130">The SQL statement and <xref:System.Data.SqlClient.SqlConnection> arguments passed to the <xref:System.Data.SqlClient.SqlDataAdapter> constructor are used to create the <xref:System.Data.SqlClient.SqlDataAdapter.SelectCommand%2A> property of the <xref:System.Data.SqlClient.SqlDataAdapter>.</span></span>  
  
## <a name="example"></a><span data-ttu-id="28aac-131">Příklad</span><span class="sxs-lookup"><span data-stu-id="28aac-131">Example</span></span>  
  
```vb  
' Assumes that connection is a valid SqlConnection object.  
Dim queryString As String = _  
  "SELECT CustomerID, CompanyName FROM dbo.Customers"  
Dim adapter As SqlDataAdapter = New SqlDataAdapter( _  
  queryString, connection)  
  
Dim customers As DataSet = New DataSet  
adapter.Fill(customers, "Customers")  
```  
  
```csharp  
// Assumes that connection is a valid SqlConnection object.  
string queryString =   
  "SELECT CustomerID, CompanyName FROM dbo.Customers";  
SqlDataAdapter adapter = new SqlDataAdapter(queryString, connection);  
  
DataSet customers = new DataSet();  
adapter.Fill(customers, "Customers");  
```  
  
> [!NOTE]
>  <span data-ttu-id="28aac-132">Kód v tomto příkladu se neotevře explicitně a zavřít `Connection`.</span><span class="sxs-lookup"><span data-stu-id="28aac-132">The code shown in this example does not explicitly open and close the `Connection`.</span></span> <span data-ttu-id="28aac-133">`Fill` Metoda implicitně otevře `Connection` , který `DataAdapter` používá, pokud zjistí, že připojení ještě není otevřený.</span><span class="sxs-lookup"><span data-stu-id="28aac-133">The `Fill` method implicitly opens the `Connection` that the `DataAdapter` is using if it finds that the connection is not already open.</span></span> <span data-ttu-id="28aac-134">Pokud `Fill` otevření připojení, také uzavře připojení při `Fill` bylo dokončeno.</span><span class="sxs-lookup"><span data-stu-id="28aac-134">If `Fill` opened the connection, it also closes the connection when `Fill` is finished.</span></span> <span data-ttu-id="28aac-135">Při řešení pomocí jedné operace, jako to může zjednodušit kódu `Fill` nebo `Update`.</span><span class="sxs-lookup"><span data-stu-id="28aac-135">This can simplify your code when you deal with a single operation such as a `Fill` or an `Update`.</span></span> <span data-ttu-id="28aac-136">Nicméně pokud provádíte více operací, které vyžaduje otevřené připojení, lze zvýšit výkon vaší aplikace explicitně voláním `Open` metodu `Connection`, provádění operací na zdroji dat, a následným voláním `Close` metodu `Connection`.</span><span class="sxs-lookup"><span data-stu-id="28aac-136">However, if you are performing multiple operations that require an open connection, you can improve the performance of your application by explicitly calling the `Open` method of the `Connection`, performing the operations against the data source, and then calling the `Close` method of the `Connection`.</span></span> <span data-ttu-id="28aac-137">Doporučujeme nechat připojení ke zdroji dat otevřené jako stručně k uvolnění prostředků pro použití v jiné klientské aplikace.</span><span class="sxs-lookup"><span data-stu-id="28aac-137">You should try to keep connections to the data source open as briefly as possible to free resources for use by other client applications.</span></span>  
  
## <a name="multiple-result-sets"></a><span data-ttu-id="28aac-138">Více sad výsledků dotazu</span><span class="sxs-lookup"><span data-stu-id="28aac-138">Multiple Result Sets</span></span>  
 <span data-ttu-id="28aac-139">Pokud `DataAdapter` zjistí sady více výsledků, vytvoří více tabulek v `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="28aac-139">If the `DataAdapter` encounters multiple result sets, it creates multiple tables in the `DataSet`.</span></span> <span data-ttu-id="28aac-140">V tabulkách jsou uvedeny na přírůstkové výchozí název tabulky*N*počínaje "Table" pro Table0.</span><span class="sxs-lookup"><span data-stu-id="28aac-140">The tables are given an incremental default name of Table*N*, starting with "Table" for Table0.</span></span> <span data-ttu-id="28aac-141">Pokud zadáte název tabulky je předán jako argument `Fill` metody v tabulkách jsou uvedeny přírůstkové výchozí název TableName*N*počínaje "TableName" pro TableName0.</span><span class="sxs-lookup"><span data-stu-id="28aac-141">If a table name is passed as an argument to the `Fill` method, the tables are given an incremental default name of TableName*N*, starting with "TableName" for TableName0.</span></span>  
  
## <a name="populating-a-dataset-from-multiple-dataadapters"></a><span data-ttu-id="28aac-142">Naplnění datové sady z více adaptérů dat</span><span class="sxs-lookup"><span data-stu-id="28aac-142">Populating a DataSet from Multiple DataAdapters</span></span>  
 <span data-ttu-id="28aac-143">Libovolný počet `DataAdapter` objekty je možné s `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="28aac-143">Any number of `DataAdapter` objects can be used with a `DataSet`.</span></span> <span data-ttu-id="28aac-144">Každý `DataAdapter` slouží k vyplnění jeden nebo více `DataTable` objekty a vyřešit aktualizací zpět do zdroje relevantní data.</span><span class="sxs-lookup"><span data-stu-id="28aac-144">Each `DataAdapter` can be used to fill one or more `DataTable` objects and resolve updates back to the relevant data source.</span></span> <span data-ttu-id="28aac-145">`DataRelation` a `Constraint` objekty mohou být přidány do `DataSet` místně, což vám umožní se vztahují data z různorodých datových zdrojů.</span><span class="sxs-lookup"><span data-stu-id="28aac-145">`DataRelation` and `Constraint` objects can be added to the `DataSet` locally, which enables you to relate data from dissimilar data sources.</span></span> <span data-ttu-id="28aac-146">Například `DataSet` může obsahovat data z databáze Microsoft SQL Server, databázi IBM DB2 prostřednictvím OLE DB a zdroj dat, která jsou streamována XML.</span><span class="sxs-lookup"><span data-stu-id="28aac-146">For example, a `DataSet` can contain data from a Microsoft SQL Server database, an IBM DB2 database exposed through OLE DB, and a data source that streams XML.</span></span> <span data-ttu-id="28aac-147">Jeden nebo více `DataAdapter` objekty zvládne komunikace pro jednotlivé zdroje dat.</span><span class="sxs-lookup"><span data-stu-id="28aac-147">One or more `DataAdapter` objects can handle communication to each data source.</span></span>  
  
### <a name="example"></a><span data-ttu-id="28aac-148">Příklad</span><span class="sxs-lookup"><span data-stu-id="28aac-148">Example</span></span>  
 <span data-ttu-id="28aac-149">Následující příklad kódu naplní seznam zákazníků `Northwind` databáze na serveru Microsoft SQL Server a seznam objednávek z `Northwind` databáze uloženou v aplikaci Microsoft Access 2000.</span><span class="sxs-lookup"><span data-stu-id="28aac-149">The following code example populates a list of customers from the `Northwind` database on Microsoft SQL Server, and a list of orders from the `Northwind` database stored in Microsoft Access 2000.</span></span> <span data-ttu-id="28aac-150">Vyplněný tabulky souvisejí s `DataRelation`, a pak zobrazí seznam zákazníků s objednávek tohoto zákazníka.</span><span class="sxs-lookup"><span data-stu-id="28aac-150">The filled tables are related with a `DataRelation`, and the list of customers is then displayed with the orders for that customer.</span></span> <span data-ttu-id="28aac-151">Další informace o `DataRelation` objekty, najdete [přidání datových relací](../../../../docs/framework/data/adonet/dataset-datatable-dataview/adding-datarelations.md) a [procházení datových relací](../../../../docs/framework/data/adonet/dataset-datatable-dataview/navigating-datarelations.md).</span><span class="sxs-lookup"><span data-stu-id="28aac-151">For more information about `DataRelation` objects, see [Adding DataRelations](../../../../docs/framework/data/adonet/dataset-datatable-dataview/adding-datarelations.md) and [Navigating DataRelations](../../../../docs/framework/data/adonet/dataset-datatable-dataview/navigating-datarelations.md).</span></span>  
  
```vb  
' Assumes that customerConnection is a valid SqlConnection object.  
' Assumes that orderConnection is a valid OleDbConnection object.  
Dim custAdapter As SqlDataAdapter = New SqlDataAdapter( _  
  "SELECT * FROM dbo.Customers", customerConnection)  
  
Dim ordAdapter As OleDbDataAdapter = New OleDbDataAdapter( _  
  "SELECT * FROM Orders", orderConnection)  
  
Dim customerOrders As DataSet = New DataSet()  
custAdapter.Fill(customerOrders, "Customers")  
ordAdapter.Fill(customerOrders, "Orders")  
  
Dim relation As DataRelation = _  
  customerOrders.Relations.Add("CustOrders", _  
  customerOrders.Tables("Customers").Columns("CustomerID"), _   
  customerOrders.Tables("Orders").Columns("CustomerID"))  
  
Dim pRow, cRow As DataRow  
For Each pRow In customerOrders.Tables("Customers").Rows  
  Console.WriteLine(pRow("CustomerID").ToString())  
  
  For Each cRow In pRow.GetChildRows(relation)  
    Console.WriteLine(vbTab & cRow("OrderID").ToString())  
  Next  
Next  
```  
  
```csharp  
// Assumes that customerConnection is a valid SqlConnection object.  
// Assumes that orderConnection is a valid OleDbConnection object.  
SqlDataAdapter custAdapter = new SqlDataAdapter(  
  "SELECT * FROM dbo.Customers", customerConnection);  
OleDbDataAdapter ordAdapter = new OleDbDataAdapter(  
  "SELECT * FROM Orders", orderConnection);  
  
DataSet customerOrders = new DataSet();  
  
custAdapter.Fill(customerOrders, "Customers");  
ordAdapter.Fill(customerOrders, "Orders");  
  
DataRelation relation = customerOrders.Relations.Add("CustOrders",  
  customerOrders.Tables["Customers"].Columns["CustomerID"],  
  customerOrders.Tables["Orders"].Columns["CustomerID"]);  
  
foreach (DataRow pRow in customerOrders.Tables["Customers"].Rows)  
{  
  Console.WriteLine(pRow["CustomerID"]);  
   foreach (DataRow cRow in pRow.GetChildRows(relation))  
    Console.WriteLine("\t" + cRow["OrderID"]);  
}  
```  
  
## <a name="sql-server-decimal-type"></a><span data-ttu-id="28aac-152">Typ desetinné SQL serveru</span><span class="sxs-lookup"><span data-stu-id="28aac-152">SQL Server Decimal Type</span></span>  
 <span data-ttu-id="28aac-153">Ve výchozím nastavení `DataSet` ukládá data s využitím [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] datové typy.</span><span class="sxs-lookup"><span data-stu-id="28aac-153">By default, the `DataSet` stores data by using [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] data types.</span></span> <span data-ttu-id="28aac-154">Pro většinu aplikací tyto poskytují pohodlný reprezentace informace o zdroji dat.</span><span class="sxs-lookup"><span data-stu-id="28aac-154">For most applications, these provide a convenient representation of data source information.</span></span> <span data-ttu-id="28aac-155">Tento zápis však může dojít k potížím, pokud datový typ ve zdroji dat je desítkový nebo číselný datový typ SQL serveru.</span><span class="sxs-lookup"><span data-stu-id="28aac-155">However, this representation may cause a problem when the data type in the data source is a SQL Server decimal or numeric data type.</span></span> <span data-ttu-id="28aac-156">[!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] `decimal` Datový typ povoluje maximálně 28 platných číslic, zatímco SQL Server `decimal` datový typ umožňuje 38 nejvýznamnějších číslic.</span><span class="sxs-lookup"><span data-stu-id="28aac-156">The [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] `decimal` data type allows a maximum of 28 significant digits, whereas the SQL Server `decimal` data type allows 38 significant digits.</span></span> <span data-ttu-id="28aac-157">Pokud `SqlDataAdapter` určuje během `Fill` operace, která přesnost systému SQL Server `decimal` pole je větší než 28 znaků, aktuální řádek není přidán do `DataTable`.</span><span class="sxs-lookup"><span data-stu-id="28aac-157">If the `SqlDataAdapter` determines during a `Fill` operation that the precision of a SQL Server `decimal` field is larger than 28 characters, the current row is not added to the `DataTable`.</span></span> <span data-ttu-id="28aac-158">Místo toho `FillError` dojde k události, což vám umožní zjistit, jestli ke ztrátě přesnosti dojít a reagují odpovídajícím způsobem.</span><span class="sxs-lookup"><span data-stu-id="28aac-158">Instead the `FillError` event occurs, which enables you to determine whether a loss of precision will occur, and respond appropriately.</span></span> <span data-ttu-id="28aac-159">Další informace o `FillError` událostí, naleznete v tématu [zpracování událostí adaptéru dat](../../../../docs/framework/data/adonet/handling-dataadapter-events.md).</span><span class="sxs-lookup"><span data-stu-id="28aac-159">For more information about the `FillError` event, see [Handling DataAdapter Events](../../../../docs/framework/data/adonet/handling-dataadapter-events.md).</span></span> <span data-ttu-id="28aac-160">Získat SQL Server `decimal` hodnotu, můžete použít také <xref:System.Data.SqlClient.SqlDataReader> objektu a volání <xref:System.Data.SqlClient.SqlDataReader.GetSqlDecimal%2A> metoda.</span><span class="sxs-lookup"><span data-stu-id="28aac-160">To get the SQL Server `decimal` value, you can also use a <xref:System.Data.SqlClient.SqlDataReader> object and call the <xref:System.Data.SqlClient.SqlDataReader.GetSqlDecimal%2A> method.</span></span>  
  
 [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)] <span data-ttu-id="28aac-161">2.0 zavedl rozšířenou podporu <xref:System.Data.SqlTypes> v `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="28aac-161">2.0 introduced enhanced support for <xref:System.Data.SqlTypes> in the `DataSet`.</span></span> <span data-ttu-id="28aac-162">Další informace najdete v tématu [SqlTypes a datová sada](../../../../docs/framework/data/adonet/sql/sqltypes-and-the-dataset.md).</span><span class="sxs-lookup"><span data-stu-id="28aac-162">For more information, see [SqlTypes and the DataSet](../../../../docs/framework/data/adonet/sql/sqltypes-and-the-dataset.md).</span></span>  
  
## <a name="ole-db-chapters"></a><span data-ttu-id="28aac-163">OLE DB kapitol</span><span class="sxs-lookup"><span data-stu-id="28aac-163">OLE DB Chapters</span></span>  
 <span data-ttu-id="28aac-164">Hierarchické sady řádků nebo kapitol (typ OLE DB `DBTYPE_HCHAPTER`, typ ADO `adChapter`) můžete použít tak, aby vyplnil obsah `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="28aac-164">Hierarchical rowsets, or chapters (OLE DB type `DBTYPE_HCHAPTER`, ADO type `adChapter`) can be used to fill the contents of a `DataSet`.</span></span> <span data-ttu-id="28aac-165">Při <xref:System.Data.OleDb.OleDbDataAdapter> zaznamená sloupec nepoužívaly kapitoly během `Fill` operace, `DataTable` pro sloupec nepoužívaly kapitoly, se vytvoří a je tuto tabulku se sloupci a řádky v kapitole.</span><span class="sxs-lookup"><span data-stu-id="28aac-165">When the <xref:System.Data.OleDb.OleDbDataAdapter> encounters a chaptered column during a `Fill` operation, a `DataTable` is created for the chaptered column, and that table is filled with the columns and rows from the chapter.</span></span> <span data-ttu-id="28aac-166">Tabulky vytvořené pro nepoužívaly kapitoly sloupec nazýval pomocí název nadřazené tabulky a název sloupce nepoužívaly kapitoly ve formě "*ParentTableNameChapteredColumnName*".</span><span class="sxs-lookup"><span data-stu-id="28aac-166">The table created for the chaptered column is named by using both the parent table name and the chaptered column name in the form "*ParentTableNameChapteredColumnName*".</span></span> <span data-ttu-id="28aac-167">Pokud tabulka již existuje v `DataSet` , který odpovídá názvu nepoužívaly kapitoly sloupec, v aktuální tabulce je naplněný daty kapitoly.</span><span class="sxs-lookup"><span data-stu-id="28aac-167">If a table already exists in the `DataSet` that matches the name of the chaptered column, the current table is filled with the chapter data.</span></span> <span data-ttu-id="28aac-168">Pokud neexistuje žádný sloupec z existující tabulky, který odpovídá sloupci najít v kapitole, je přidán nový sloupec.</span><span class="sxs-lookup"><span data-stu-id="28aac-168">If there is no column in an existing table that matches a column found in the chapter, a new column is added.</span></span>  
  
 <span data-ttu-id="28aac-169">Před tabulky v `DataSet` jsou vyplněny s daty v nepoužívaly kapitoly sloupce, relace se vytvoří mezi nadřazenými a podřízenými tabulek v hierarchické sadě řádků tak, že přidáte sloupec celých čísel do nadřazené i podřízené tabulky, nastavení nadřazeného sloupce Automatické zvyšování čísla a vytváření `DataRelation` pomocí přidání sloupce z obou tabulek.</span><span class="sxs-lookup"><span data-stu-id="28aac-169">Before the tables in the `DataSet` are filled with the data in the chaptered columns, a relation is created between the parent and child tables of the hierarchical rowset by adding an integer column to both the parent and child table, setting the parent column to auto-increment, and creating a `DataRelation` using the added columns from both tables.</span></span> <span data-ttu-id="28aac-170">Přidání vztahu je pojmenováno pomocí nadřazeného názvy sloupců tabulky a kapitoly ve formě "*ParentTableNameChapterColumnName*".</span><span class="sxs-lookup"><span data-stu-id="28aac-170">The added relation is named by using the parent table and chapter column names in the form "*ParentTableNameChapterColumnName*".</span></span>  
  
 <span data-ttu-id="28aac-171">Všimněte si, že související sloupec existuje pouze v `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="28aac-171">Note that the related column only exists in the `DataSet`.</span></span> <span data-ttu-id="28aac-172">Následné výplně ze zdroje dat může způsobit nové řádky, které chcete přidat do tabulky místo změny do existující řádky.</span><span class="sxs-lookup"><span data-stu-id="28aac-172">Subsequent fills from the data source can cause new rows to be added to the tables instead of changes being merged into existing rows.</span></span>  
  
 <span data-ttu-id="28aac-173">Všimněte si také, že pokud použijete `DataAdapter.Fill` přetížení, které přijímá `DataTable`, bude vyplněno pouze tuto tabulku.</span><span class="sxs-lookup"><span data-stu-id="28aac-173">Note also that, if you use the `DataAdapter.Fill` overload that takes a `DataTable`, only that table will be filled.</span></span> <span data-ttu-id="28aac-174">Sloupec celých čísel automatické zvyšování bude i tak přidána k tabulce, ale žádné podřízené tabulce bude vytvořen nebo vyplněné a se nevytvoří žádný vztah.</span><span class="sxs-lookup"><span data-stu-id="28aac-174">An auto-incrementing integer column will still be added to the table, but no child table will be created or filled, and no relation will be created.</span></span>  
  
 <span data-ttu-id="28aac-175">Následující příklad používá zprostředkovatele MSDataShape ke generování objednávek pro každého zákazníka neočekávaný sloupec kapitoly v seznam zákazníků.</span><span class="sxs-lookup"><span data-stu-id="28aac-175">The following example uses the MSDataShape Provider to generate a chapter column of orders for each customer in a list of customers.</span></span> <span data-ttu-id="28aac-176">A `DataSet` pak naplní daty.</span><span class="sxs-lookup"><span data-stu-id="28aac-176">A `DataSet` is then filled with the data.</span></span>  
  
```vb  
Using connection As OleDbConnection = New OleDbConnection( _  
  "Provider=MSDataShape;Data Provider=SQLOLEDB;" & _  
  "Data Source=(local);Integrated " & _  
  "Security=SSPI;Initial Catalog=northwind")  
  
Dim adapter As OleDbDataAdapter = New OleDbDataAdapter( _  
  "SHAPE {SELECT CustomerID, CompanyName FROM Customers} " & _  
  "APPEND ({SELECT CustomerID, OrderID FROM Orders} AS Orders " & _  
  "RELATE CustomerID TO CustomerID)", connection)  
  
Dim customers As DataSet = New DataSet()  
  
adapter.Fill(customers, "Customers")  
End Using  
```  
  
```csharp  
using (OleDbConnection connection = new OleDbConnection("Provider=MSDataShape;Data Provider=SQLOLEDB;" +  
  "Data Source=(local);Integrated Security=SSPI;Initial Catalog=northwind"))  
{  
OleDbDataAdapter adapter = new OleDbDataAdapter("SHAPE {SELECT CustomerID, CompanyName FROM Customers} " +  
  "APPEND ({SELECT CustomerID, OrderID FROM Orders} AS Orders " +  
  "RELATE CustomerID TO CustomerID)", connection);  
  
DataSet customers = new DataSet();  
adapter.Fill(customers, "Customers");  
}  
```  
  
 <span data-ttu-id="28aac-177">Když `Fill` operace se dokončila, `DataSet` obsahuje dvě tabulky: `Customers` a `CustomersOrders`, kde `CustomersOrders` představuje sloupci nepoužívaly kapitoly.</span><span class="sxs-lookup"><span data-stu-id="28aac-177">When the `Fill` operation is complete, the `DataSet` contains two tables: `Customers` and `CustomersOrders`, where `CustomersOrders` represents the chaptered column.</span></span> <span data-ttu-id="28aac-178">Další sloupec s názvem `Orders` se přidá do `Customers` tabulku a sloupec s názvem `CustomersOrders` se přidá do `CustomersOrders` tabulky.</span><span class="sxs-lookup"><span data-stu-id="28aac-178">An additional column named `Orders` is added to the `Customers` table, and an additional column named `CustomersOrders` is added to the `CustomersOrders` table.</span></span> <span data-ttu-id="28aac-179">`Orders` Sloupec `Customers` tabulky je nastavena na automatické zvyšování čísla.</span><span class="sxs-lookup"><span data-stu-id="28aac-179">The `Orders` column in the `Customers` table is set to auto-increment.</span></span> <span data-ttu-id="28aac-180">A `DataRelation`, `CustomersOrders`, se vytvoří s použitím sloupce, které byly přidány do tabulky s `Customers` jako nadřazená tabulka.</span><span class="sxs-lookup"><span data-stu-id="28aac-180">A `DataRelation`, `CustomersOrders`, is created by using the columns that were added to the tables with `Customers` as the parent table.</span></span> <span data-ttu-id="28aac-181">Následující tabulky popisují některé ukázkové výsledky.</span><span class="sxs-lookup"><span data-stu-id="28aac-181">The following tables show some sample results.</span></span>  
  
### <a name="tablename-customers"></a><span data-ttu-id="28aac-182">Název tabulky: Zákazníci</span><span class="sxs-lookup"><span data-stu-id="28aac-182">TableName: Customers</span></span>  
  
|<span data-ttu-id="28aac-183">ID zákazníka</span><span class="sxs-lookup"><span data-stu-id="28aac-183">CustomerID</span></span>|<span data-ttu-id="28aac-184">CompanyName</span><span class="sxs-lookup"><span data-stu-id="28aac-184">CompanyName</span></span>|<span data-ttu-id="28aac-185">Objednávky</span><span class="sxs-lookup"><span data-stu-id="28aac-185">Orders</span></span>|  
|----------------|-----------------|------------|  
|<span data-ttu-id="28aac-186">ALFKI</span><span class="sxs-lookup"><span data-stu-id="28aac-186">ALFKI</span></span>|<span data-ttu-id="28aac-187">Alfreds Futterkiste</span><span class="sxs-lookup"><span data-stu-id="28aac-187">Alfreds Futterkiste</span></span>|<span data-ttu-id="28aac-188">0</span><span class="sxs-lookup"><span data-stu-id="28aac-188">0</span></span>|  
|<span data-ttu-id="28aac-189">ANATR</span><span class="sxs-lookup"><span data-stu-id="28aac-189">ANATR</span></span>|<span data-ttu-id="28aac-190">Helados Ana Trujillo Emparedados y</span><span class="sxs-lookup"><span data-stu-id="28aac-190">Ana Trujillo Emparedados y helados</span></span>|<span data-ttu-id="28aac-191">1</span><span class="sxs-lookup"><span data-stu-id="28aac-191">1</span></span>|  
  
### <a name="tablename-customersorders"></a><span data-ttu-id="28aac-192">Název tabulky: CustomersOrders</span><span class="sxs-lookup"><span data-stu-id="28aac-192">TableName: CustomersOrders</span></span>  
  
|<span data-ttu-id="28aac-193">ID zákazníka</span><span class="sxs-lookup"><span data-stu-id="28aac-193">CustomerID</span></span>|<span data-ttu-id="28aac-194">ID objednávky</span><span class="sxs-lookup"><span data-stu-id="28aac-194">OrderID</span></span>|<span data-ttu-id="28aac-195">CustomersOrders</span><span class="sxs-lookup"><span data-stu-id="28aac-195">CustomersOrders</span></span>|  
|----------------|-------------|---------------------|  
|<span data-ttu-id="28aac-196">ALFKI</span><span class="sxs-lookup"><span data-stu-id="28aac-196">ALFKI</span></span>|<span data-ttu-id="28aac-197">10643</span><span class="sxs-lookup"><span data-stu-id="28aac-197">10643</span></span>|<span data-ttu-id="28aac-198">0</span><span class="sxs-lookup"><span data-stu-id="28aac-198">0</span></span>|  
|<span data-ttu-id="28aac-199">ALFKI</span><span class="sxs-lookup"><span data-stu-id="28aac-199">ALFKI</span></span>|<span data-ttu-id="28aac-200">10692</span><span class="sxs-lookup"><span data-stu-id="28aac-200">10692</span></span>|<span data-ttu-id="28aac-201">0</span><span class="sxs-lookup"><span data-stu-id="28aac-201">0</span></span>|  
|<span data-ttu-id="28aac-202">ANATR</span><span class="sxs-lookup"><span data-stu-id="28aac-202">ANATR</span></span>|<span data-ttu-id="28aac-203">10308</span><span class="sxs-lookup"><span data-stu-id="28aac-203">10308</span></span>|<span data-ttu-id="28aac-204">1</span><span class="sxs-lookup"><span data-stu-id="28aac-204">1</span></span>|  
|<span data-ttu-id="28aac-205">ANATR</span><span class="sxs-lookup"><span data-stu-id="28aac-205">ANATR</span></span>|<span data-ttu-id="28aac-206">10625</span><span class="sxs-lookup"><span data-stu-id="28aac-206">10625</span></span>|<span data-ttu-id="28aac-207">1</span><span class="sxs-lookup"><span data-stu-id="28aac-207">1</span></span>|  
  
## <a name="see-also"></a><span data-ttu-id="28aac-208">Viz také:</span><span class="sxs-lookup"><span data-stu-id="28aac-208">See also</span></span>

- [<span data-ttu-id="28aac-209">Adaptéry a čtečky dat</span><span class="sxs-lookup"><span data-stu-id="28aac-209">DataAdapters and DataReaders</span></span>](../../../../docs/framework/data/adonet/dataadapters-and-datareaders.md)
- [<span data-ttu-id="28aac-210">Mapování datového typu v ADO.NET</span><span class="sxs-lookup"><span data-stu-id="28aac-210">Data Type Mappings in ADO.NET</span></span>](../../../../docs/framework/data/adonet/data-type-mappings-in-ado-net.md)
- [<span data-ttu-id="28aac-211">Úpravy dat přes DbDataAdapter</span><span class="sxs-lookup"><span data-stu-id="28aac-211">Modifying Data with a DbDataAdapter</span></span>](../../../../docs/framework/data/adonet/modifying-data-with-a-dbdataadapter.md)
- [<span data-ttu-id="28aac-212">Více aktivních sad výsledků (MARS)</span><span class="sxs-lookup"><span data-stu-id="28aac-212">Multiple Active Result Sets (MARS)</span></span>](../../../../docs/framework/data/adonet/sql/multiple-active-result-sets-mars.md)
- [<span data-ttu-id="28aac-213">ADO.NET spravovaných zprostředkovatelích a datové sady pro vývojáře</span><span class="sxs-lookup"><span data-stu-id="28aac-213">ADO.NET Managed Providers and DataSet Developer Center</span></span>](https://go.microsoft.com/fwlink/?LinkId=217917)
