---
title: Přehled LINQ to DataSet
ms.date: 03/30/2017
ms.assetid: dc20a8fb-03f6-4b68-9c2b-7f7299e3070b
ms.openlocfilehash: 20d9be052ba2567c16718b4b1c1019427c9b5df1
ms.sourcegitcommit: 7bc6887ab658550baa78f1520ea735838249345e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 01/03/2020
ms.locfileid: "75634817"
---
# <a name="linq-to-dataset-overview"></a><span data-ttu-id="e3d2e-102">Přehled LINQ to DataSet</span><span class="sxs-lookup"><span data-stu-id="e3d2e-102">LINQ to DataSet Overview</span></span>
<span data-ttu-id="e3d2e-103"><xref:System.Data.DataSet> je jednou z široce používaných komponent ADO.NET.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-103">The <xref:System.Data.DataSet> is one of the more widely used components of ADO.NET.</span></span> <span data-ttu-id="e3d2e-104">Je klíčovým prvkem odpojeného programovacího modelu, na kterém je ADO.NET založen, a umožňuje explicitně ukládat data z různých zdrojů dat do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-104">It is a key element of the disconnected programming model that ADO.NET is based on, and it enables you to explicitly cache data from different data sources.</span></span> <span data-ttu-id="e3d2e-105">Pro prezentační vrstvu je <xref:System.Data.DataSet> úzce integrovaný s ovládacími prvky GUI pro datovou vazbu.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-105">For the presentation tier, the <xref:System.Data.DataSet> is tightly integrated with GUI controls for data-binding.</span></span> <span data-ttu-id="e3d2e-106">V případě střední vrstvy poskytuje mezipaměť, která zachovává relační tvar dat, a zahrnuje rychlé jednoduché navigační služby dotazů a hierarchií.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-106">For the middle-tier, it provides a cache that preserves the relational shape of data, and includes fast simple query and hierarchy navigation services.</span></span> <span data-ttu-id="e3d2e-107">Běžnou technikou pro snížení počtu požadavků na databázi je použití <xref:System.Data.DataSet> pro ukládání do mezipaměti v prostřední vrstvě.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-107">A common technique used to lower the number of requests on a database is to use the <xref:System.Data.DataSet> for caching in the middle-tier.</span></span> <span data-ttu-id="e3d2e-108">Zvažte například webovou aplikaci ASP.NET řízená daty.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-108">For example, consider a data-driven ASP.NET Web application.</span></span> <span data-ttu-id="e3d2e-109">Často se podstatná část dat aplikace nemění často a je společná napříč relacemi nebo uživateli.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-109">Often, a significant portion of the application data does not change frequently and is common across sessions or users.</span></span> <span data-ttu-id="e3d2e-110">Tato data je možné uchovávat v paměti na webovém serveru, což snižuje počet požadavků na databázi a urychluje interakce uživatele.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-110">This data can be kept in memory on the Web server, which reduces the number of requests against the database and speeds up the user’s interactions.</span></span> <span data-ttu-id="e3d2e-111">Dalším užitečným aspektem <xref:System.Data.DataSet> je to, že aplikace umožňuje přenést podmnožiny dat z jednoho nebo více zdrojů dat do prostoru aplikace.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-111">Another useful aspect of the <xref:System.Data.DataSet> is that it allows an application to bring subsets of data from one or more data source into the application space.</span></span> <span data-ttu-id="e3d2e-112">Aplikace pak může manipulovat s daty v paměti a přitom zachovat relační tvar.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-112">The application can then manipulate the data in-memory, while retaining its relational shape.</span></span>  
  
 <span data-ttu-id="e3d2e-113"><xref:System.Data.DataSet> má bez ohledu na své význačnost možnosti dotazů.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-113">Despite its prominence, the <xref:System.Data.DataSet> has limited query capabilities.</span></span> <span data-ttu-id="e3d2e-114">Metodu <xref:System.Data.DataTable.Select%2A> lze použít pro filtrování a řazení a metody <xref:System.Data.DataRow.GetChildRows%2A> a <xref:System.Data.DataRow.GetParentRow%2A> lze použít pro navigaci v hierarchii.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-114">The <xref:System.Data.DataTable.Select%2A> method can be used for filtering and sorting, and the <xref:System.Data.DataRow.GetChildRows%2A> and <xref:System.Data.DataRow.GetParentRow%2A> methods can be used for hierarchy navigation.</span></span> <span data-ttu-id="e3d2e-115">Pro něco složitějšího, ale vývojář musí napsat vlastní dotaz.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-115">For anything more complex, however, the developer must write a custom query.</span></span> <span data-ttu-id="e3d2e-116">To může mít za následek nedostatečně náročné aplikace a jejich údržbu.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-116">This can result in applications that perform poorly and are difficult to maintain.</span></span>  
  
 <span data-ttu-id="e3d2e-117">LINQ to DataSet usnadňuje a urychlují dotaz na data uložená v mezipaměti v objektu <xref:System.Data.DataSet>.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-117">LINQ to DataSet makes it easier and faster to query over data cached in a <xref:System.Data.DataSet> object.</span></span> <span data-ttu-id="e3d2e-118">Tyto dotazy jsou vyjádřeny v samotném programovacím jazyce, nikoli jako řetězcové literály vložené v kódu aplikace.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-118">These queries are expressed in the programming language itself, rather than as string literals embedded in the application code.</span></span> <span data-ttu-id="e3d2e-119">To znamená, že vývojáři se nemusí učit o samostatném dotazovacímu jazyku.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-119">This means that developers do not have to learn a separate query language.</span></span> <span data-ttu-id="e3d2e-120">Kromě toho LINQ to DataSet umožňuje vývojářům sady Visual Studio pracovat častěji, protože integrované vývojové prostředí (IDE) sady Visual Studio poskytuje kontrolu syntaxe v době kompilace, statické typování a podporu technologie IntelliSense pro LINQ.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-120">Additionally, LINQ to DataSet enables Visual Studio developers to work more productively, because the Visual Studio IDE provides compile-time syntax checking, static typing, and IntelliSense support for LINQ.</span></span> <span data-ttu-id="e3d2e-121">LINQ to DataSet lze také použít k dotazování na data, která byla konsolidována z jednoho nebo více zdrojů dat.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-121">LINQ to DataSet can also be used to query over data that has been consolidated from one or more data sources.</span></span> <span data-ttu-id="e3d2e-122">Díky tomu je možné mnoha scénářů, které vyžadují flexibilitu při reprezentaci a zpracování dat.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-122">This enables many scenarios that require flexibility in how data is represented and handled.</span></span> <span data-ttu-id="e3d2e-123">Tato metoda manipulace vyžaduje zejména obecné vytváření sestav, analýzy a business intelligence aplikace.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-123">In particular, generic reporting, analysis, and business intelligence applications require this method of manipulation.</span></span>  
  
## <a name="querying-datasets-using-linq-to-dataset"></a><span data-ttu-id="e3d2e-124">Dotazování na datové sady pomocí LINQ to DataSet</span><span class="sxs-lookup"><span data-stu-id="e3d2e-124">Querying DataSets Using LINQ to DataSet</span></span>  
 <span data-ttu-id="e3d2e-125">Než začnete s dotazem na objekt <xref:System.Data.DataSet> pomocí LINQ to DataSet, je nutné naplnit <xref:System.Data.DataSet>.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-125">Before you can begin querying a <xref:System.Data.DataSet> object using LINQ to DataSet, you must populate the <xref:System.Data.DataSet>.</span></span> <span data-ttu-id="e3d2e-126">Existuje několik způsobů, jak načíst data do <xref:System.Data.DataSet>, jako je například použití třídy <xref:System.Data.Common.DataAdapter> nebo [LINQ to SQL](./sql/linq/index.md).</span><span class="sxs-lookup"><span data-stu-id="e3d2e-126">There are several ways to load data into a <xref:System.Data.DataSet>, such as using the <xref:System.Data.Common.DataAdapter> class or [LINQ to SQL](./sql/linq/index.md).</span></span> <span data-ttu-id="e3d2e-127">Po načtení dat do objektu <xref:System.Data.DataSet> můžete začít dotazovat.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-127">After the data has been loaded into a <xref:System.Data.DataSet> object, you can begin to query it.</span></span> <span data-ttu-id="e3d2e-128">Formulace dotazů pomocí LINQ to DataSet je podobná použití LINQ (Language-Integrated Query) pro jiné zdroje dat s podporou LINQ.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-128">Formulating queries using LINQ to DataSet is similar to using Language-Integrated Query (LINQ) against other LINQ-enabled data sources.</span></span> <span data-ttu-id="e3d2e-129">Dotazy LINQ lze provádět v rámci jednoduchých tabulek v <xref:System.Data.DataSet> nebo proti více než jedné tabulce pomocí standardních operátorů dotazu <xref:System.Linq.Enumerable.Join%2A> a <xref:System.Linq.Enumerable.GroupJoin%2A>.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-129">LINQ queries can be performed against single tables in a <xref:System.Data.DataSet> or against more than one table by using the <xref:System.Linq.Enumerable.Join%2A> and <xref:System.Linq.Enumerable.GroupJoin%2A> standard query operators.</span></span>  
  
 <span data-ttu-id="e3d2e-130">Dotazy LINQ jsou podporovány proti typovým i netypovým <xref:System.Data.DataSet>m objektům.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-130">LINQ queries are supported against both typed and untyped <xref:System.Data.DataSet> objects.</span></span> <span data-ttu-id="e3d2e-131">Pokud je schéma <xref:System.Data.DataSet> známo v době návrhu aplikace, je doporučena typová <xref:System.Data.DataSet>.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-131">If the schema of the <xref:System.Data.DataSet> is known at application design time, a typed <xref:System.Data.DataSet> is recommended.</span></span> <span data-ttu-id="e3d2e-132">V zadaném <xref:System.Data.DataSet>tabulky a řádky zadávané členy pro každý sloupec, což usnadňuje čtení dotazů a jejich snadnější čitelnost.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-132">In a typed <xref:System.Data.DataSet>, the tables and rows have typed members for each of the columns, which makes queries simpler and more readable.</span></span>  
  
 <span data-ttu-id="e3d2e-133">Kromě standardních operátorů dotazu implementovaných v System. Core. dll LINQ to DataSet přidává několik rozšíření specifických pro <xref:System.Data.DataSet>, které usnadňuje dotazování nad sadou objektů <xref:System.Data.DataRow>.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-133">In addition to the standard query operators implemented in System.Core.dll, LINQ to DataSet adds several <xref:System.Data.DataSet>-specific extensions that make it easier to query over a set of <xref:System.Data.DataRow> objects.</span></span> <span data-ttu-id="e3d2e-134">Tato rozšíření specifická pro <xref:System.Data.DataSet>zahrnují operátory pro porovnávání sekvencí řádků a také metody, které poskytují přístup k hodnotám sloupců <xref:System.Data.DataRow>.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-134">These <xref:System.Data.DataSet>-specific extensions include operators for comparing sequences of rows, as well as methods that provide access to the column values of a <xref:System.Data.DataRow>.</span></span>  
  
## <a name="n-tier-applications-and-linq-to-dataset"></a><span data-ttu-id="e3d2e-135">N-vrstvé aplikace a LINQ to DataSet</span><span class="sxs-lookup"><span data-stu-id="e3d2e-135">N-tier Applications and LINQ to DataSet</span></span>  
 <span data-ttu-id="e3d2e-136">N-vrstvé datové aplikace jsou aplikace zaměřené na data, které jsou rozdělené do několika logických vrstev (nebo vrstev).</span><span class="sxs-lookup"><span data-stu-id="e3d2e-136">N-tier data applications are data-centric applications that are separated into multiple logical layers (or tiers).</span></span> <span data-ttu-id="e3d2e-137">Typická N-vrstvá aplikace zahrnuje prezentační vrstvu, střední vrstvu a datovou vrstvu.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-137">A typical N-tier application includes a presentation tier, a middle tier, and a data tier.</span></span> <span data-ttu-id="e3d2e-138">Rozdělení komponent aplikace do oddělených vrstev zvyšuje udržovatelnost a škálovatelnost aplikace.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-138">Separating application components into separate tiers increases the maintainability and scalability of the application.</span></span> <span data-ttu-id="e3d2e-139">Další informace o N-vrstvých datových aplikacích naleznete v tématu [práce s datovými sadami v n-vrstvých aplikacích](/visualstudio/data-tools/work-with-datasets-in-n-tier-applications).</span><span class="sxs-lookup"><span data-stu-id="e3d2e-139">For more information about N-tier data applications, see [Work with datasets in n-tier applications](/visualstudio/data-tools/work-with-datasets-in-n-tier-applications).</span></span>  
  
 <span data-ttu-id="e3d2e-140">V N-vrstvých aplikacích se <xref:System.Data.DataSet> často používá k ukládání informací do mezipaměti pro webovou aplikaci v prostřední vrstvě.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-140">In N-tier applications, the <xref:System.Data.DataSet> is often used in the middle-tier to cache information for a Web application.</span></span> <span data-ttu-id="e3d2e-141">LINQ to DataSet funkce dotazování je implementována prostřednictvím rozšiřujících metod a rozšiřuje stávající <xref:System.Data.DataSet>ADO.NET 2,0.</span><span class="sxs-lookup"><span data-stu-id="e3d2e-141">The LINQ to DataSet querying functionality is implemented through extension methods and extends the existing ADO.NET 2.0 <xref:System.Data.DataSet>.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="e3d2e-142">Viz také:</span><span class="sxs-lookup"><span data-stu-id="e3d2e-142">See also</span></span>

- [<span data-ttu-id="e3d2e-143">Dotazy na datové sady</span><span class="sxs-lookup"><span data-stu-id="e3d2e-143">Querying DataSets</span></span>](querying-datasets-linq-to-dataset.md)
- [<span data-ttu-id="e3d2e-144">LINQ (Language-Integrated Query) –C#</span><span class="sxs-lookup"><span data-stu-id="e3d2e-144">Language-Integrated Query (LINQ) - C#</span></span>](../../../csharp/programming-guide/concepts/linq/index.md)
- [<span data-ttu-id="e3d2e-145">LINQ (Language-Integrated Query) – Visual Basic</span><span class="sxs-lookup"><span data-stu-id="e3d2e-145">Language-Integrated Query (LINQ) - Visual Basic</span></span>](../../../visual-basic/programming-guide/concepts/linq/index.md)
- [<span data-ttu-id="e3d2e-146">LINQ to SQL</span><span class="sxs-lookup"><span data-stu-id="e3d2e-146">LINQ to SQL</span></span>](./sql/linq/index.md)
