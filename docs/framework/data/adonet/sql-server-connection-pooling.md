---
title: Sdružování připojení serveru SQL Server
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: 7e51d44e-7c4e-4040-9332-f0190fe36f07
ms.openlocfilehash: 149511bd4e84baabf11eca014257127b587830df
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/12/2020
ms.locfileid: "79148995"
---
# <a name="sql-server-connection-pooling-adonet"></a><span data-ttu-id="d144e-102">Sdružování připojení SQL Serveru (ADO.NET)</span><span class="sxs-lookup"><span data-stu-id="d144e-102">SQL Server Connection Pooling (ADO.NET)</span></span>
<span data-ttu-id="d144e-103">Připojení k databázovému serveru se obvykle skládá z několika časově náročných kroků.</span><span class="sxs-lookup"><span data-stu-id="d144e-103">Connecting to a database server typically consists of several time-consuming steps.</span></span> <span data-ttu-id="d144e-104">Fyzický kanál, jako je soket nebo pojmenovaný kanál, musí být vytvořen, musí dojít k počátečnímu podání ruky se serverem, musí být analyzovány informace o připojovacím řetězci, připojení musí být ověřeno serverem, kontroly musí být spuštěny pro zařazení do aktuální transakce a tak dále.</span><span class="sxs-lookup"><span data-stu-id="d144e-104">A physical channel such as a socket or a named pipe must be established, the initial handshake with the server must occur, the connection string information must be parsed, the connection must be authenticated by the server, checks must be run for enlisting in the current transaction, and so on.</span></span>  
  
 <span data-ttu-id="d144e-105">V praxi většina aplikací používá pouze jednu nebo několik různých konfigurací pro připojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-105">In practice, most applications use only one or a few different configurations for connections.</span></span> <span data-ttu-id="d144e-106">To znamená, že během provádění aplikace bude mnoho identických připojení opakovaně otevřeno a zavírat.</span><span class="sxs-lookup"><span data-stu-id="d144e-106">This means that during application execution, many identical connections will be repeatedly opened and closed.</span></span> <span data-ttu-id="d144e-107">Chcete-li minimalizovat náklady na otevření připojení, ADO.NET používá optimalizační techniku nazvanou *sdružování připojení*.</span><span class="sxs-lookup"><span data-stu-id="d144e-107">To minimize the cost of opening connections, ADO.NET uses an optimization technique called *connection pooling*.</span></span>  
  
 <span data-ttu-id="d144e-108">Sdružování připojení snižuje počet, kolikrát je nutné otevřít nová připojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-108">Connection pooling reduces the number of times that new connections must be opened.</span></span> <span data-ttu-id="d144e-109">*Pooler* udržuje vlastnictví fyzicképřipojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-109">The *pooler* maintains ownership of the physical connection.</span></span> <span data-ttu-id="d144e-110">Spravuje připojení udržováním při životě sadu aktivních připojení pro každou konfiguraci daného připojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-110">It manages connections by keeping alive a set of active connections for each given connection configuration.</span></span> <span data-ttu-id="d144e-111">Kdykoli uživatel `Open` volá připojení, pooler hledá dostupné připojení ve fondu.</span><span class="sxs-lookup"><span data-stu-id="d144e-111">Whenever a user calls `Open` on a connection, the pooler looks for an available connection in the pool.</span></span> <span data-ttu-id="d144e-112">Pokud je k dispozici sdružené připojení, vrátí jej volajícímu namísto otevření nového připojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-112">If a pooled connection is available, it returns it to the caller instead of opening a new connection.</span></span> <span data-ttu-id="d144e-113">Když aplikace `Close` volá připojení, pooler vrátí do sdružené sady aktivních připojení namísto jeho zavření.</span><span class="sxs-lookup"><span data-stu-id="d144e-113">When the application calls `Close` on the connection, the pooler returns it to the pooled set of active connections instead of closing it.</span></span> <span data-ttu-id="d144e-114">Jakmile je připojení vráceno do fondu, je připraveno `Open` k opětovnému použití při dalším volání.</span><span class="sxs-lookup"><span data-stu-id="d144e-114">Once the connection is returned to the pool, it is ready to be reused on the next `Open` call.</span></span>  
  
 <span data-ttu-id="d144e-115">Sdružují se pouze připojení se stejnou konfigurací.</span><span class="sxs-lookup"><span data-stu-id="d144e-115">Only connections with the same configuration can be pooled.</span></span> <span data-ttu-id="d144e-116">ADO.NET udržuje několik fondů současně, jeden pro každou konfiguraci.</span><span class="sxs-lookup"><span data-stu-id="d144e-116">ADO.NET keeps several pools at the same time, one for each configuration.</span></span> <span data-ttu-id="d144e-117">Připojení jsou rozdělena do fondů podle připojovacího řetězce a identity systému Windows při použití integrovaného zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="d144e-117">Connections are separated into pools by connection string, and by Windows identity when integrated security is used.</span></span> <span data-ttu-id="d144e-118">Připojení jsou také sdružené na základě toho, zda jsou zapsány v transakci.</span><span class="sxs-lookup"><span data-stu-id="d144e-118">Connections are also pooled based on whether they are enlisted in a transaction.</span></span> <span data-ttu-id="d144e-119">Při <xref:System.Data.SqlClient.SqlConnection.ChangePassword%2A>použití <xref:System.Data.SqlClient.SqlCredential> instance ovlivňuje fond připojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-119">When using <xref:System.Data.SqlClient.SqlConnection.ChangePassword%2A>, the <xref:System.Data.SqlClient.SqlCredential> instance affects the connection pool.</span></span> <span data-ttu-id="d144e-120">Různé instance <xref:System.Data.SqlClient.SqlCredential> budou používat různé fondy připojení, i když id uživatele a heslo jsou stejné.</span><span class="sxs-lookup"><span data-stu-id="d144e-120">Different instances of <xref:System.Data.SqlClient.SqlCredential> will use different connection pools, even if the user ID and password are the same.</span></span>  
  
 <span data-ttu-id="d144e-121">Sdružování připojení může výrazně zvýšit výkon a škálovatelnost vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="d144e-121">Pooling connections can significantly enhance the performance and scalability of your application.</span></span> <span data-ttu-id="d144e-122">Ve výchozím nastavení je sdružování připojení povoleno v ADO.NET.</span><span class="sxs-lookup"><span data-stu-id="d144e-122">By default, connection pooling is enabled in ADO.NET.</span></span> <span data-ttu-id="d144e-123">Pokud explicitně zakázat, pooler optimalizuje připojení, jak jsou otevřeny a uzavřeny ve vaší aplikaci.</span><span class="sxs-lookup"><span data-stu-id="d144e-123">Unless you explicitly disable it, the pooler optimizes the connections as they are opened and closed in your application.</span></span> <span data-ttu-id="d144e-124">Můžete také zadat několik modifikátorů připojovacího řetězce pro řízení chování sdružování připojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-124">You can also supply several connection string modifiers to control connection pooling behavior.</span></span> <span data-ttu-id="d144e-125">Další informace naleznete v tématu "Řízení sdružování připojení s klíčovými slovy připojovacího řetězce" dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="d144e-125">For more information, see "Controlling Connection Pooling with Connection String Keywords" later in this topic.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="d144e-126">Pokud je povoleno sdružování připojení a dojde-li k chybě vypršení časového limitu nebo jiné chybě přihlášení, bude vyvolána výjimka a následné pokusy o připojení se nezdaří po dobu dalších pěti sekund, "blokovací období".</span><span class="sxs-lookup"><span data-stu-id="d144e-126">When connection pooling is enabled, and if a timeout error or other login error occurs, an exception will be thrown and subsequent connection attempts will fail for the next five seconds, the "blocking period".</span></span> <span data-ttu-id="d144e-127">Pokud se aplikace pokusí připojit v rámci období blokování, bude znovu vyvolána první výjimka.</span><span class="sxs-lookup"><span data-stu-id="d144e-127">If the application attempts to connect within the blocking period, the first exception will be thrown again.</span></span> <span data-ttu-id="d144e-128">Následné chyby po ukončení blokovacího období budou mít za následek nové blokovací období, které je dvakrát delší než předchozí blokovací období, maximálně jednu minutu.</span><span class="sxs-lookup"><span data-stu-id="d144e-128">Subsequent failures after a blocking period ends will result in a new blocking periods that is twice as long as the previous blocking period, up to a maximum of one minute.</span></span>  
  
## <a name="pool-creation-and-assignment"></a><span data-ttu-id="d144e-129">Vytvoření a přiřazení fondu</span><span class="sxs-lookup"><span data-stu-id="d144e-129">Pool Creation and Assignment</span></span>  
 <span data-ttu-id="d144e-130">Při prvním otevření připojení je vytvořen fond připojení na základě algoritmu přesného párování, který přidružuje fond k připojovacímu řetězci v připojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-130">When a connection is first opened, a connection pool is created based on an exact matching algorithm that associates the pool with the connection string in the connection.</span></span> <span data-ttu-id="d144e-131">Každý fond připojení je přidružen k odlišnému připojovacímu řetězci.</span><span class="sxs-lookup"><span data-stu-id="d144e-131">Each connection pool is associated with a distinct connection string.</span></span> <span data-ttu-id="d144e-132">Při otevření nového připojení, pokud připojovací řetězec není přesná shoda s existujícím fondem, je vytvořen nový fond.</span><span class="sxs-lookup"><span data-stu-id="d144e-132">When a new connection is opened, if the connection string is not an exact match to an existing pool, a new pool is created.</span></span> <span data-ttu-id="d144e-133">Připojení jsou sdružená podle procesu, podle domény aplikace, podle připojovacího řetězce a při použití integrovaného zabezpečení podle identity systému Windows.</span><span class="sxs-lookup"><span data-stu-id="d144e-133">Connections are pooled per process, per application domain, per connection string and when integrated security is used, per Windows identity.</span></span> <span data-ttu-id="d144e-134">Připojovací řetězce musí být také přesnou shodu; klíčová slova dodaná v jiném pořadí pro stejné připojení budou sdružena samostatně.</span><span class="sxs-lookup"><span data-stu-id="d144e-134">Connection strings must also be an exact match; keywords supplied in a different order for the same connection will be pooled separately.</span></span>  
  
 <span data-ttu-id="d144e-135">V následujícím příkladu jazyka <xref:System.Data.SqlClient.SqlConnection> C# jsou vytvořeny tři nové objekty, ale ke správě jsou vyžadovány pouze dva fondy připojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-135">In the following C# example, three new <xref:System.Data.SqlClient.SqlConnection> objects are created, but only two connection pools are required to manage them.</span></span> <span data-ttu-id="d144e-136">Všimněte si, že první a druhý připojovací řetězce se liší podle hodnoty přiřazené pro `Initial Catalog`.</span><span class="sxs-lookup"><span data-stu-id="d144e-136">Note that the first and second connection strings differ by the value assigned for `Initial Catalog`.</span></span>  
  
```csharp
using (SqlConnection connection = new SqlConnection(  
  "Integrated Security=SSPI;Initial Catalog=Northwind"))  
    {  
        connection.Open();
        // Pool A is created.  
    }  
  
using (SqlConnection connection = new SqlConnection(  
  "Integrated Security=SSPI;Initial Catalog=pubs"))  
    {  
        connection.Open();
        // Pool B is created because the connection strings differ.  
    }  
  
using (SqlConnection connection = new SqlConnection(  
  "Integrated Security=SSPI;Initial Catalog=Northwind"))  
    {  
        connection.Open();
        // The connection string matches pool A.  
    }  
```  
  
 <span data-ttu-id="d144e-137">Pokud `MinPoolSize` není zadán v připojovacím řetězci nebo je zadán jako nula, připojení ve fondu bude uzavřenpo určité době nečinnosti.</span><span class="sxs-lookup"><span data-stu-id="d144e-137">If `MinPoolSize` is either not specified in the connection string or is specified as zero, the connections in the pool will be closed after a period of inactivity.</span></span> <span data-ttu-id="d144e-138">Pokud je však zadaný `MinPoolSize` je větší než nula, fond připojení není zničen, `AppDomain` dokud je uvolněn a proces končí.</span><span class="sxs-lookup"><span data-stu-id="d144e-138">However, if the specified `MinPoolSize` is greater than zero, the connection pool is not destroyed until the `AppDomain` is unloaded and the process ends.</span></span> <span data-ttu-id="d144e-139">Údržba neaktivních nebo prázdných fondů zahrnuje minimální režii systému.</span><span class="sxs-lookup"><span data-stu-id="d144e-139">Maintenance of inactive or empty pools involves minimal system overhead.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="d144e-140">Fond je automaticky vymazána, když dojde k závažné chybě, jako je například převzetí služeb při selhání.</span><span class="sxs-lookup"><span data-stu-id="d144e-140">The pool is automatically cleared when a fatal error occurs, such as a failover.</span></span>  
  
## <a name="adding-connections"></a><span data-ttu-id="d144e-141">Přidání připojení</span><span class="sxs-lookup"><span data-stu-id="d144e-141">Adding Connections</span></span>  
 <span data-ttu-id="d144e-142">Fond připojení je vytvořen pro každý jedinečný připojovací řetězec.</span><span class="sxs-lookup"><span data-stu-id="d144e-142">A connection pool is created for each unique connection string.</span></span> <span data-ttu-id="d144e-143">Při vytvoření fondu jsou vytvořeny a přidány do fondu více objektů připojení, aby byl splněn požadavek na minimální velikost fondu.</span><span class="sxs-lookup"><span data-stu-id="d144e-143">When a pool is created, multiple connection objects are created and added to the pool so that the minimum pool size requirement is satisfied.</span></span> <span data-ttu-id="d144e-144">Připojení jsou přidány do fondu podle potřeby až do maximální velikostfondu zadaná (100 je výchozí).</span><span class="sxs-lookup"><span data-stu-id="d144e-144">Connections are added to the pool as needed, up to the maximum pool size specified (100 is the default).</span></span> <span data-ttu-id="d144e-145">Připojení jsou uvolněny zpět do fondu, když jsou uzavřeny nebo vyřazeny.</span><span class="sxs-lookup"><span data-stu-id="d144e-145">Connections are released back into the pool when they are closed or disposed.</span></span>  
  
 <span data-ttu-id="d144e-146">Pokud <xref:System.Data.SqlClient.SqlConnection> je požadován objekt, je získán z fondu, pokud je k dispozici použitelné připojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-146">When a <xref:System.Data.SqlClient.SqlConnection> object is requested, it is obtained from the pool if a usable connection is available.</span></span> <span data-ttu-id="d144e-147">Aby bylo možné připojení použít, musí být nevyužité, musí mít odpovídající kontext transakce nebo být nepřidruženo k libovolnému kontextu transakce a mít platné propojení se serverem.</span><span class="sxs-lookup"><span data-stu-id="d144e-147">To be usable, a connection must be unused, have a matching transaction context or be unassociated with any transaction context, and have a valid link to the server.</span></span>  
  
 <span data-ttu-id="d144e-148">Sdružovací služba připojení splňuje požadavky na připojení přerozdělením připojení při jejich uvolnění zpět do fondu.</span><span class="sxs-lookup"><span data-stu-id="d144e-148">The connection pooler satisfies requests for connections by reallocating connections as they are released back into the pool.</span></span> <span data-ttu-id="d144e-149">Pokud bylo dosaženo maximální velikosti fondu a není k dispozici žádné použitelné připojení, je požadavek zařazen do fronty.</span><span class="sxs-lookup"><span data-stu-id="d144e-149">If the maximum pool size has been reached and no usable connection is available, the request is queued.</span></span> <span data-ttu-id="d144e-150">Pooler se pak pokusí získat zpět všechna připojení, dokud není dosaženo časového limitu (výchozí hodnota je 15 sekund).</span><span class="sxs-lookup"><span data-stu-id="d144e-150">The pooler then tries to reclaim any connections until the time-out is reached (the default is 15 seconds).</span></span> <span data-ttu-id="d144e-151">Pokud pooler nemůže splnit požadavek před časový limit připojení, je vyvolána výjimka.</span><span class="sxs-lookup"><span data-stu-id="d144e-151">If the pooler cannot satisfy the request before the connection times out, an exception is thrown.</span></span>  
  
> [!CAUTION]
> <span data-ttu-id="d144e-152">Důrazně doporučujeme, abyste vždy zavřít připojení po dokončení jeho použití tak, aby připojení bude vrácena do fondu.</span><span class="sxs-lookup"><span data-stu-id="d144e-152">We strongly recommend that you always close the connection when you are finished using it so that the connection will be returned to the pool.</span></span> <span data-ttu-id="d144e-153">Můžete to provést pomocí `Close` `Dispose` nebo metody `Connection` objektu nebo otevřením `using` všechna připojení uvnitř příkazu v jazyce C# nebo `Using` příkaz v jazyce Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="d144e-153">You can do this using either the `Close` or `Dispose` methods of the `Connection` object, or by opening all connections inside a `using` statement in C#, or a `Using` statement in Visual Basic.</span></span> <span data-ttu-id="d144e-154">Připojení, která nejsou explicitně uzavřena, nemusí být přidána nebo vrácena do fondu.</span><span class="sxs-lookup"><span data-stu-id="d144e-154">Connections that are not explicitly closed might not be added or returned to the pool.</span></span> <span data-ttu-id="d144e-155">Další informace naleznete [v tématu using Statement](../../../csharp/language-reference/keywords/using-statement.md) or How [to: Dispose of a System Resource](../../../visual-basic/programming-guide/language-features/control-flow/how-to-dispose-of-a-system-resource.md) for Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="d144e-155">For more information, see [using Statement](../../../csharp/language-reference/keywords/using-statement.md) or [How to: Dispose of a System Resource](../../../visual-basic/programming-guide/language-features/control-flow/how-to-dispose-of-a-system-resource.md) for Visual Basic.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="d144e-156">Nevolejte `Close` nebo `Dispose` na `Connection`, `DataReader`, , nebo jakýkoli `Finalize` jiný spravovaný objekt v metodě vaší třídy.</span><span class="sxs-lookup"><span data-stu-id="d144e-156">Do not call `Close` or `Dispose` on a `Connection`, a `DataReader`, or any other managed object in the `Finalize` method of your class.</span></span> <span data-ttu-id="d144e-157">Ve finalizační metodě uvolněte pouze nespravované prostředky, které přímo vlastní vaše třída.</span><span class="sxs-lookup"><span data-stu-id="d144e-157">In a finalizer, only release unmanaged resources that your class owns directly.</span></span> <span data-ttu-id="d144e-158">Pokud vaše třída nevlastní žádné nespravované prostředky, `Finalize` nezahrnujte metodu do definice třídy.</span><span class="sxs-lookup"><span data-stu-id="d144e-158">If your class does not own any unmanaged resources, do not include a `Finalize` method in your class definition.</span></span> <span data-ttu-id="d144e-159">Další informace naleznete v [tématu Garbage Collection](../../../standard/garbage-collection/index.md).</span><span class="sxs-lookup"><span data-stu-id="d144e-159">For more information, see [Garbage Collection](../../../standard/garbage-collection/index.md).</span></span>  
  
<span data-ttu-id="d144e-160">Další informace o událostech spojených s otevíráním a zavíráním připojení naleznete v [tématu Audit přihlášení třídy událostí](/sql/relational-databases/event-classes/audit-login-event-class) a [třídy odhlášení auditu](/sql/relational-databases/event-classes/audit-logout-event-class) v dokumentaci k serveru SQL Server.</span><span class="sxs-lookup"><span data-stu-id="d144e-160">For more info about the events associated with opening and closing connections, see [Audit Login Event Class](/sql/relational-databases/event-classes/audit-login-event-class) and [Audit Logout Event Class](/sql/relational-databases/event-classes/audit-logout-event-class) in the SQL Server documentation.</span></span>  
  
## <a name="removing-connections"></a><span data-ttu-id="d144e-161">Odebrání připojení</span><span class="sxs-lookup"><span data-stu-id="d144e-161">Removing Connections</span></span>  
 <span data-ttu-id="d144e-162">Sdružovací služba připojení odebere připojení z fondu poté, co byl nečinný po dobu přibližně 4-8 minut, nebo pokud pooler zjistí, že připojení se serverem byla přerušena.</span><span class="sxs-lookup"><span data-stu-id="d144e-162">The connection pooler removes a connection from the pool after it has been idle for approximately 4-8 minutes, or if the pooler detects that the connection with the server has been severed.</span></span> <span data-ttu-id="d144e-163">Všimněte si, že přerušené připojení lze zjistit pouze po pokusu o komunikaci se serverem.</span><span class="sxs-lookup"><span data-stu-id="d144e-163">Note that a severed connection can be detected only after attempting to communicate with the server.</span></span> <span data-ttu-id="d144e-164">Pokud je nalezeno připojení, které již není připojeno k serveru, je označeno jako neplatné.</span><span class="sxs-lookup"><span data-stu-id="d144e-164">If a connection is found that is no longer connected to the server, it is marked as invalid.</span></span> <span data-ttu-id="d144e-165">Neplatná připojení jsou odebrána z fondu připojení pouze v případě, že jsou uzavřeny nebo regenerované.</span><span class="sxs-lookup"><span data-stu-id="d144e-165">Invalid connections are removed from the connection pool only when they are closed or reclaimed.</span></span>  
  
 <span data-ttu-id="d144e-166">Pokud existuje připojení k serveru, který zmizel, toto připojení lze nakreslit z fondu i v případě, že sdružovací připojení nezjistilpřerušené připojení a označil jej jako neplatné.</span><span class="sxs-lookup"><span data-stu-id="d144e-166">If a connection exists to a server that has disappeared, this connection can be drawn from the pool even if the connection pooler has not detected the severed connection and marked it as invalid.</span></span> <span data-ttu-id="d144e-167">To je případ, protože režie kontroly, že připojení je stále platný by eliminovat výhody s pooler tím, že způsobí další odezvu na server dojít.</span><span class="sxs-lookup"><span data-stu-id="d144e-167">This is the case because the overhead of checking that the connection is still valid would eliminate the benefits of having a pooler by causing another round trip to the server to occur.</span></span> <span data-ttu-id="d144e-168">V takovém případě první pokus o použití připojení zjistí, že připojení bylo přerušeno a je vyvolána výjimka.</span><span class="sxs-lookup"><span data-stu-id="d144e-168">When this occurs, the first attempt to use the connection will detect that the connection has been severed, and an exception is thrown.</span></span>  
  
## <a name="clearing-the-pool"></a><span data-ttu-id="d144e-169">Vymazání fondu</span><span class="sxs-lookup"><span data-stu-id="d144e-169">Clearing the Pool</span></span>  
 <span data-ttu-id="d144e-170">ADO.NET 2.0 zavedla dvě nové metody <xref:System.Data.SqlClient.SqlConnection.ClearAllPools%2A> <xref:System.Data.SqlClient.SqlConnection.ClearPool%2A>pro vyčištění bazénu: a .</span><span class="sxs-lookup"><span data-stu-id="d144e-170">ADO.NET 2.0 introduced two new methods to clear the pool: <xref:System.Data.SqlClient.SqlConnection.ClearAllPools%2A> and <xref:System.Data.SqlClient.SqlConnection.ClearPool%2A>.</span></span> <span data-ttu-id="d144e-171">`ClearAllPools`vymaže fondy připojení `ClearPool` pro daného zprostředkovatele a vymaže fond připojení, který je přidružen k určitému připojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-171">`ClearAllPools` clears the connection pools for a given provider, and `ClearPool` clears the connection pool that is associated with a specific connection.</span></span> <span data-ttu-id="d144e-172">Pokud jsou v době volání používána připojení, jsou odpovídajícím způsobem označena.</span><span class="sxs-lookup"><span data-stu-id="d144e-172">If there are connections being used at the time of the call, they are marked appropriately.</span></span> <span data-ttu-id="d144e-173">Když jsou uzavřeny, jsou zahozeny namísto vrácení do fondu.</span><span class="sxs-lookup"><span data-stu-id="d144e-173">When they are closed, they are discarded instead of being returned to the pool.</span></span>  
  
## <a name="transaction-support"></a><span data-ttu-id="d144e-174">Podpora transakcí</span><span class="sxs-lookup"><span data-stu-id="d144e-174">Transaction Support</span></span>  
 <span data-ttu-id="d144e-175">Připojení jsou čerpána z fondu a přiřazena na základě kontextu transakce.</span><span class="sxs-lookup"><span data-stu-id="d144e-175">Connections are drawn from the pool and assigned based on transaction context.</span></span> <span data-ttu-id="d144e-176">Pokud `Enlist=false` není zadán v připojovacím řetězci, fond připojení <xref:System.Transactions.Transaction.Current%2A> zajišťuje, že připojení je zapsán v kontextu.</span><span class="sxs-lookup"><span data-stu-id="d144e-176">Unless `Enlist=false` is specified in the connection string, the connection pool makes sure that the connection is enlisted in the <xref:System.Transactions.Transaction.Current%2A> context.</span></span> <span data-ttu-id="d144e-177">Pokud je připojení uzavřeno a vráceno do `System.Transactions` fondu s zapsanou transakcí, je odloženo tak, že `System.Transactions` další požadavek pro tento fond připojení se stejnou transakcí vrátí stejné připojení, pokud je k dispozici.</span><span class="sxs-lookup"><span data-stu-id="d144e-177">When a connection is closed and returned to the pool with an enlisted `System.Transactions` transaction, it is set aside in such a way that the next request for that connection pool with the same `System.Transactions` transaction will return the same connection if it is available.</span></span> <span data-ttu-id="d144e-178">Pokud je takový požadavek vydán a nejsou k dispozici žádná sdružená připojení, je připojení nakresleno z netransakční části fondu a zapsáno.</span><span class="sxs-lookup"><span data-stu-id="d144e-178">If such a request is issued, and there are no pooled connections available, a connection is drawn from the non-transacted part of the pool and enlisted.</span></span> <span data-ttu-id="d144e-179">Pokud v žádné oblasti fondu nejsou k dispozici žádná připojení, je vytvořeno a zapsáno nové připojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-179">If no connections are available in either area of the pool, a new connection is created and enlisted.</span></span>  
  
 <span data-ttu-id="d144e-180">Když je připojení uzavřeno, je uvolněna zpět do fondu a do příslušného dělení na základě jeho kontextu transakce.</span><span class="sxs-lookup"><span data-stu-id="d144e-180">When a connection is closed, it is released back into the pool and into the appropriate subdivision based on its transaction context.</span></span> <span data-ttu-id="d144e-181">Proto můžete zavřít připojení bez generování chyby, i když distribuovaná transakce stále čeká na vyřízení.</span><span class="sxs-lookup"><span data-stu-id="d144e-181">Therefore, you can close the connection without generating an error, even though a distributed transaction is still pending.</span></span> <span data-ttu-id="d144e-182">To umožňuje potvrdit nebo přerušit distribuované transakce později.</span><span class="sxs-lookup"><span data-stu-id="d144e-182">This allows you to commit or abort the distributed transaction later.</span></span>  
  
## <a name="controlling-connection-pooling-with-connection-string-keywords"></a><span data-ttu-id="d144e-183">Řízení sdružování připojení pomocí klíčových slov připojovacího řetězce</span><span class="sxs-lookup"><span data-stu-id="d144e-183">Controlling Connection Pooling with Connection String Keywords</span></span>  
 <span data-ttu-id="d144e-184">Vlastnost `ConnectionString` objektu <xref:System.Data.SqlClient.SqlConnection> podporuje dvojice klíčů a hodnot připojovacího řetězce, které lze použít k úpravě chování logiky sdružování připojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-184">The `ConnectionString` property of the <xref:System.Data.SqlClient.SqlConnection> object supports connection string key/value pairs that can be used to adjust the behavior of the connection pooling logic.</span></span> <span data-ttu-id="d144e-185">Další informace naleznete v tématu <xref:System.Data.SqlClient.SqlConnection.ConnectionString%2A>.</span><span class="sxs-lookup"><span data-stu-id="d144e-185">For more information, see <xref:System.Data.SqlClient.SqlConnection.ConnectionString%2A>.</span></span>  
  
## <a name="pool-fragmentation"></a><span data-ttu-id="d144e-186">Fragmentace fondu</span><span class="sxs-lookup"><span data-stu-id="d144e-186">Pool Fragmentation</span></span>  
 <span data-ttu-id="d144e-187">Fragmentace fondu je běžný problém v mnoha webových aplikacích, kde aplikace může vytvořit velký počet fondů, které nejsou uvolněny, dokud proces ukončí.</span><span class="sxs-lookup"><span data-stu-id="d144e-187">Pool fragmentation is a common problem in many Web applications where the application can create a large number of pools that are not freed until the process exits.</span></span> <span data-ttu-id="d144e-188">To ponechává velký počet připojení otevřené a náročné paměti, což má za následek nízký výkon.</span><span class="sxs-lookup"><span data-stu-id="d144e-188">This leaves a large number of connections open and consuming memory, which results in poor performance.</span></span>  
  
### <a name="pool-fragmentation-due-to-integrated-security"></a><span data-ttu-id="d144e-189">Fragmentace fondu díky integrovanému zabezpečení</span><span class="sxs-lookup"><span data-stu-id="d144e-189">Pool Fragmentation Due to Integrated Security</span></span>  
 <span data-ttu-id="d144e-190">Připojení jsou sdružena podle připojovacího řetězce a identity uživatele.</span><span class="sxs-lookup"><span data-stu-id="d144e-190">Connections are pooled according to the connection string plus the user identity.</span></span> <span data-ttu-id="d144e-191">Proto pokud používáte základní ověřování nebo ověřování systému Windows na webu a integrované přihlášení zabezpečení, získáte jeden fond na uživatele.</span><span class="sxs-lookup"><span data-stu-id="d144e-191">Therefore, if you use Basic authentication or Windows Authentication on the Web site and an integrated security login, you get one pool per user.</span></span> <span data-ttu-id="d144e-192">Přestože to zlepšuje výkon následných požadavků na databázi pro jednoho uživatele, tento uživatel nemůže využít výhod připojení jiných uživatelů.</span><span class="sxs-lookup"><span data-stu-id="d144e-192">Although this improves the performance of subsequent database requests for a single user, that user cannot take advantage of connections made by other users.</span></span> <span data-ttu-id="d144e-193">Výsledkem je také alespoň jedno připojení na uživatele k databázovému serveru.</span><span class="sxs-lookup"><span data-stu-id="d144e-193">It also results in at least one connection per user to the database server.</span></span> <span data-ttu-id="d144e-194">Jedná se o vedlejší účinek konkrétní architektury webových aplikací, které musí vývojáři zvážit s požadavky na zabezpečení a auditování.</span><span class="sxs-lookup"><span data-stu-id="d144e-194">This is a side effect of a particular Web application architecture that developers must weigh against security and auditing requirements.</span></span>  
  
### <a name="pool-fragmentation-due-to-many-databases"></a><span data-ttu-id="d144e-195">Fragmentace fondu z důvodu mnoha databází</span><span class="sxs-lookup"><span data-stu-id="d144e-195">Pool Fragmentation Due to Many Databases</span></span>  
 <span data-ttu-id="d144e-196">Mnoho poskytovatelů služeb Internetu hostuje několik webů na jednom serveru.</span><span class="sxs-lookup"><span data-stu-id="d144e-196">Many Internet service providers host several Web sites on a single server.</span></span> <span data-ttu-id="d144e-197">Mohou použít jednu databázi k potvrzení přihlášení ověřování pomocí formuláře a potom otevřít připojení k určité databázi pro tohoto uživatele nebo skupinu uživatelů.</span><span class="sxs-lookup"><span data-stu-id="d144e-197">They may use a single database to confirm a Forms authentication login and then open a connection to a specific database for that user or group of users.</span></span> <span data-ttu-id="d144e-198">Připojení k databázi ověřování je sdružené a používá každý.</span><span class="sxs-lookup"><span data-stu-id="d144e-198">The connection to the authentication database is pooled and used by everyone.</span></span> <span data-ttu-id="d144e-199">Existuje však samostatný fond připojení ke každé databázi, které zvyšují počet připojení k serveru.</span><span class="sxs-lookup"><span data-stu-id="d144e-199">However, there is a separate pool of connections to each database, which increase the number of connections to the server.</span></span>  
  
 <span data-ttu-id="d144e-200">To je také vedlejší účinek návrhu aplikace.</span><span class="sxs-lookup"><span data-stu-id="d144e-200">This is also a side-effect of the application design.</span></span> <span data-ttu-id="d144e-201">Existuje poměrně jednoduchý způsob, jak se vyhnout tento vedlejší účinek bez ohrožení zabezpečení při připojení k serveru SQL Server.</span><span class="sxs-lookup"><span data-stu-id="d144e-201">There is a relatively simple way to avoid this side effect without compromising security when you connect to SQL Server.</span></span> <span data-ttu-id="d144e-202">Namísto připojení k samostatné databázi pro každého uživatele nebo skupinu se připojte ke stejné databázi na serveru a potom spusťte příkaz Transact-SQL USE a změňte na požadovanou databázi.</span><span class="sxs-lookup"><span data-stu-id="d144e-202">Instead of connecting to a separate database for each user or group, connect to the same database on the server and then execute the Transact-SQL USE statement to change to the desired database.</span></span> <span data-ttu-id="d144e-203">Následující fragment kódu ukazuje vytvoření počátečního `master` připojení k databázi a následné `databaseName` přepnutí na požadovanou databázi zadanou v proměnné řetězce.</span><span class="sxs-lookup"><span data-stu-id="d144e-203">The following code fragment demonstrates creating an initial connection to the `master` database and then switching to the desired database specified in the `databaseName` string variable.</span></span>  
  
```vb  
' Assumes that command is a valid SqlCommand object and that  
' connectionString connects to master.  
    command.Text = "USE DatabaseName"  
Using connection As New SqlConnection(connectionString)  
    connection.Open()  
    command.ExecuteNonQuery()  
End Using  
```  
  
```csharp  
// Assumes that command is a SqlCommand object and that  
// connectionString connects to master.  
command.Text = "USE DatabaseName";  
using (SqlConnection connection = new SqlConnection(  
  connectionString))  
  {  
    connection.Open();  
    command.ExecuteNonQuery();  
  }  
```  
  
## <a name="application-roles-and-connection-pooling"></a><span data-ttu-id="d144e-204">Role aplikací a sdružování připojení</span><span class="sxs-lookup"><span data-stu-id="d144e-204">Application Roles and Connection Pooling</span></span>  
 <span data-ttu-id="d144e-205">Po aktivaci role aplikace serveru SQL `sp_setapprole` Server voláním uložené procedury systému nelze obnovit kontext zabezpečení tohoto připojení.</span><span class="sxs-lookup"><span data-stu-id="d144e-205">After a SQL Server application role has been activated by calling the `sp_setapprole` system stored procedure, the security context of that connection cannot be reset.</span></span> <span data-ttu-id="d144e-206">Pokud je však povoleno sdružování, je připojení vráceno do fondu a při opakovaném použití sdruženého připojení dojde k chybě.</span><span class="sxs-lookup"><span data-stu-id="d144e-206">However, if pooling is enabled, the connection is returned to the pool, and an error occurs when the pooled connection is reused.</span></span> <span data-ttu-id="d144e-207">Další informace naleznete v článku znalostní báze Knowledge Base "[Chyby role aplikace SQL se sdružováním prostředků technologie OLE DB](https://support.microsoft.com/default.aspx?scid=KB;EN-US;Q229564)" .</span><span class="sxs-lookup"><span data-stu-id="d144e-207">For more information, see the Knowledge Base article, "[SQL application role errors with OLE DB resource pooling](https://support.microsoft.com/default.aspx?scid=KB;EN-US;Q229564)."</span></span>  
  
### <a name="application-role-alternatives"></a><span data-ttu-id="d144e-208">Alternativy rolí aplikace</span><span class="sxs-lookup"><span data-stu-id="d144e-208">Application Role Alternatives</span></span>  
 <span data-ttu-id="d144e-209">Doporučujeme využít výhod mechanismů zabezpečení, které můžete použít namísto rolí aplikace.</span><span class="sxs-lookup"><span data-stu-id="d144e-209">We recommend that you take advantage of security mechanisms that you can use instead of application roles.</span></span> <span data-ttu-id="d144e-210">Další informace naleznete [v tématu Vytváření rolí aplikací na serveru SQL Server](./sql/creating-application-roles-in-sql-server.md).</span><span class="sxs-lookup"><span data-stu-id="d144e-210">For more information, see [Creating Application Roles in SQL Server](./sql/creating-application-roles-in-sql-server.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="d144e-211">Viz také</span><span class="sxs-lookup"><span data-stu-id="d144e-211">See also</span></span>

- [<span data-ttu-id="d144e-212">Sdružování připojení</span><span class="sxs-lookup"><span data-stu-id="d144e-212">Connection Pooling</span></span>](connection-pooling.md)
- [<span data-ttu-id="d144e-213">SQL Server a ADO.NET</span><span class="sxs-lookup"><span data-stu-id="d144e-213">SQL Server and ADO.NET</span></span>](./sql/index.md)
- [<span data-ttu-id="d144e-214">Čítače výkonu</span><span class="sxs-lookup"><span data-stu-id="d144e-214">Performance Counters</span></span>](performance-counters.md)
- [<span data-ttu-id="d144e-215">Přehled ADO.NET</span><span class="sxs-lookup"><span data-stu-id="d144e-215">ADO.NET Overview</span></span>](ado-net-overview.md)
