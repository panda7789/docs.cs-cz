---
title: Připojení k SQL serveru sdružování (ADO.NET)
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: 7e51d44e-7c4e-4040-9332-f0190fe36f07
ms.openlocfilehash: f416ae8252d9991905da7eeaf4ce6398ff0e7461
ms.sourcegitcommit: efff8f331fd9467f093f8ab8d23a203d6ecb5b60
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/01/2018
ms.locfileid: "43406493"
---
# <a name="sql-server-connection-pooling-adonet"></a><span data-ttu-id="5ad2b-102">Připojení k SQL serveru sdružování (ADO.NET)</span><span class="sxs-lookup"><span data-stu-id="5ad2b-102">SQL Server Connection Pooling (ADO.NET)</span></span>
<span data-ttu-id="5ad2b-103">Připojování k databázovému serveru, obvykle se skládá z několika kroků časově náročné.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-103">Connecting to a database server typically consists of several time-consuming steps.</span></span> <span data-ttu-id="5ad2b-104">Fyzické kanál například soket nebo pojmenovaný kanál musí navázat, počáteční metody handshake se serverem se musí vyskytovat, informace o připojovacím řetězci musí být analyzován, server musí být ověřené připojení, kontroly musí být spuštěn pro zařazování aktuální transakce a tak dále.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-104">A physical channel such as a socket or a named pipe must be established, the initial handshake with the server must occur, the connection string information must be parsed, the connection must be authenticated by the server, checks must be run for enlisting in the current transaction, and so on.</span></span>  
  
 <span data-ttu-id="5ad2b-105">V praxi používat většinu aplikací pouze jednu nebo několik různých konfigurací pro připojení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-105">In practice, most applications use only one or a few different configurations for connections.</span></span> <span data-ttu-id="5ad2b-106">To znamená, že při spuštění aplikace, mnoho stejné připojení bude možné opakovaně otevřel a uzavřel.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-106">This means that during application execution, many identical connections will be repeatedly opened and closed.</span></span> <span data-ttu-id="5ad2b-107">Chcete-li minimalizovat náklady na otevření připojení, [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)] používá optimalizaci techniky označované jako *sdružování připojení*.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-107">To minimize the cost of opening connections, [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)] uses an optimization technique called *connection pooling*.</span></span>  
  
 <span data-ttu-id="5ad2b-108">Sdružování připojení snižuje počet případů, kdy se nové připojení musí být otevřeny.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-108">Connection pooling reduces the number of times that new connections must be opened.</span></span> <span data-ttu-id="5ad2b-109">*Pro sdružování* udržuje vlastnictví fyzické připojení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-109">The *pooler* maintains ownership of the physical connection.</span></span> <span data-ttu-id="5ad2b-110">Spravuje připojení udržováním aktivní sadu aktivních připojení pro každou konfiguraci dané připojení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-110">It manages connections by keeping alive a set of active connections for each given connection configuration.</span></span> <span data-ttu-id="5ad2b-111">Vždy, když uživateli zavolá `Open` na připojení, hledá pro sdružování připojení k dispozici ve fondu.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-111">Whenever a user calls `Open` on a connection, the pooler looks for an available connection in the pool.</span></span> <span data-ttu-id="5ad2b-112">Pokud je k dispozici ve fondu připojení, se vrátí volajícímu místo otevřít nové připojení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-112">If a pooled connection is available, it returns it to the caller instead of opening a new connection.</span></span> <span data-ttu-id="5ad2b-113">Když aplikace volá `Close` připojení, pro sdružování vrátí ji do klientů ve fondu sadu aktivních připojení místo jeho uzavřením.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-113">When the application calls `Close` on the connection, the pooler returns it to the pooled set of active connections instead of closing it.</span></span> <span data-ttu-id="5ad2b-114">Po připojení se vrátí do fondu, je připraven znovu použije při dalším `Open` volání.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-114">Once the connection is returned to the pool, it is ready to be reused on the next `Open` call.</span></span>  
  
 <span data-ttu-id="5ad2b-115">Pouze připojení se stejnou konfigurací můžete ve fondu.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-115">Only connections with the same configuration can be pooled.</span></span> [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)]<span data-ttu-id="5ad2b-116"> udržuje několik fondů ve stejnou dobu, jeden pro každou konfiguraci.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-116"> keeps several pools at the same time, one for each configuration.</span></span> <span data-ttu-id="5ad2b-117">Připojení jsou rozděleny do fondů pomocí připojovacího řetězce a identita Windows při použití integrovaného zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-117">Connections are separated into pools by connection string, and by Windows identity when integrated security is used.</span></span> <span data-ttu-id="5ad2b-118">Připojení se spojují také závislosti na tom, zda je uveden v transakci.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-118">Connections are also pooled based on whether they are enlisted in a transaction.</span></span> <span data-ttu-id="5ad2b-119">Při použití <xref:System.Data.SqlClient.SqlConnection.ChangePassword%2A>, <xref:System.Data.SqlClient.SqlCredential> instance má vliv na fond připojení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-119">When using <xref:System.Data.SqlClient.SqlConnection.ChangePassword%2A>, the <xref:System.Data.SqlClient.SqlCredential> instance affects the connection pool.</span></span> <span data-ttu-id="5ad2b-120">Různé instance <xref:System.Data.SqlClient.SqlCredential> používat fondy jiné připojení, i v případě, že ID uživatele a heslo jsou stejné.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-120">Different instances of <xref:System.Data.SqlClient.SqlCredential> will use different connection pools, even if the user ID and password are the same.</span></span>  
  
 <span data-ttu-id="5ad2b-121">Sdružování připojení může výrazně zlepšit výkon a škálovatelnost aplikace.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-121">Pooling connections can significantly enhance the performance and scalability of your application.</span></span> <span data-ttu-id="5ad2b-122">Ve výchozím nastavení, je povoleno sdružování připojení v [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)].</span><span class="sxs-lookup"><span data-stu-id="5ad2b-122">By default, connection pooling is enabled in [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)].</span></span> <span data-ttu-id="5ad2b-123">Pokud zakážete explicitně ji, optimalizuje pro sdružování připojení jsou otevřené a uzavřené ve vaší aplikaci.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-123">Unless you explicitly disable it, the pooler optimizes the connections as they are opened and closed in your application.</span></span> <span data-ttu-id="5ad2b-124">Můžete také zadat několik modifikátory připojovací řetězec můžete řídit chování sdružování připojení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-124">You can also supply several connection string modifiers to control connection pooling behavior.</span></span> <span data-ttu-id="5ad2b-125">Další informace najdete v tématu "Řízení připojení sdružování s připojovací řetězec klíčová slova" dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-125">For more information, see "Controlling Connection Pooling with Connection String Keywords" later in this topic.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="5ad2b-126">Pokud je povoleno sdružování připojení, pokud dojde k vypršení časového limitu nebo jiná chyba přihlášení, bude vyvolána výjimka a následné pokusy o připojení se nezdaří pro další pět sekund, "blokování období".</span><span class="sxs-lookup"><span data-stu-id="5ad2b-126">When connection pooling is enabled, and if a timeout error or other login error occurs, an exception will be thrown and subsequent connection attempts will fail for the next five seconds, the "blocking period".</span></span> <span data-ttu-id="5ad2b-127">Pokud se aplikace pokusí o připojení v rámci doby blokování, první výjimka vyvolána znovu.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-127">If the application attempts to connect within the blocking period, the first exception will be thrown again.</span></span> <span data-ttu-id="5ad2b-128">Další chyby po skončení blokování období způsobí nové blokování období, která je dvakrát až předchozí blokování období až do maximálního počtu jednu minutu.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-128">Subsequent failures after a blocking period ends will result in a new blocking periods that is twice as long as the previous blocking period, up to a maximum of one minute.</span></span>  
  
## <a name="pool-creation-and-assignment"></a><span data-ttu-id="5ad2b-129">Vytvoření fondu a přiřazení</span><span class="sxs-lookup"><span data-stu-id="5ad2b-129">Pool Creation and Assignment</span></span>  
 <span data-ttu-id="5ad2b-130">Při prvním otevření připojení se vytvoří fond připojení podle přesně odpovídající algoritmus, který přidruží připojovací řetězec v připojení k fondu.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-130">When a connection is first opened, a connection pool is created based on an exact matching algorithm that associates the pool with the connection string in the connection.</span></span> <span data-ttu-id="5ad2b-131">Každý fond připojení souvisí s distinct připojovací řetězec.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-131">Each connection pool is associated with a distinct connection string.</span></span> <span data-ttu-id="5ad2b-132">Při otevření nové připojení připojovací řetězec není přesná shoda do existujícího fondu, je vytvořen nový fond.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-132">When a new connection is opened, if the connection string is not an exact match to an existing pool, a new pool is created.</span></span> <span data-ttu-id="5ad2b-133">Proces pro doménu aplikace, na připojovací řetězec a při použití integrovaného zabezpečení na Windows identity klientů ve fondu připojení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-133">Connections are pooled per process, per application domain, per connection string and when integrated security is used, per Windows identity.</span></span> <span data-ttu-id="5ad2b-134">Připojovací řetězce musí být také přesnou shodu. klíčová slova uvedená v jiném pořadí pro stejné připojení bude ve fondu samostatně.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-134">Connection strings must also be an exact match; keywords supplied in a different order for the same connection will be pooled separately.</span></span>  
  
 <span data-ttu-id="5ad2b-135">V následujícím příkladu C#, tři nové <xref:System.Data.SqlClient.SqlConnection> jsou vytvořeny objekty, ale pouze dva fondy připojení jsou nutné k jejich správě.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-135">In the following C# example, three new <xref:System.Data.SqlClient.SqlConnection> objects are created, but only two connection pools are required to manage them.</span></span> <span data-ttu-id="5ad2b-136">Všimněte si, že první a druhý připojovací řetězce se liší podle hodnoty přiřazené pro `Initial Catalog`.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-136">Note that the first and second connection strings differ by the value assigned for `Initial Catalog`.</span></span>  
  
```  
using (SqlConnection connection = new SqlConnection(  
  "Integrated Security=SSPI;Initial Catalog=Northwind"))  
    {  
        connection.Open();        
        // Pool A is created.  
    }  
  
using (SqlConnection connection = new SqlConnection(  
  "Integrated Security=SSPI;Initial Catalog=pubs"))  
    {  
        connection.Open();        
        // Pool B is created because the connection strings differ.  
    }  
  
using (SqlConnection connection = new SqlConnection(  
  "Integrated Security=SSPI;Initial Catalog=Northwind"))  
    {  
        connection.Open();        
        // The connection string matches pool A.  
    }  
```  
  
 <span data-ttu-id="5ad2b-137">Pokud `MinPoolSize` v připojovacím řetězci nebyl zadán nebo je zadán jako nula, připojení ve fondu se zavře po určité době nečinnosti.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-137">If `MinPoolSize` is either not specified in the connection string or is specified as zero, the connections in the pool will be closed after a period of inactivity.</span></span> <span data-ttu-id="5ad2b-138">Ale pokud zadaný `MinPoolSize` je větší než nula, fond připojení není zničen až `AppDomain` je uvolněna a proces skončí.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-138">However, if the specified `MinPoolSize` is greater than zero, the connection pool is not destroyed until the `AppDomain` is unloaded and the process ends.</span></span> <span data-ttu-id="5ad2b-139">Údržby není neaktivní nebo prázdný fondů zahrnuje minimální systém režii.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-139">Maintenance of inactive or empty pools involves minimal system overhead.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="5ad2b-140">Fondu jsou automaticky vymazány, pokud dojde k závažné chybě, jako je převzetí služeb při selhání.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-140">The pool is automatically cleared when a fatal error occurs, such as a failover.</span></span>  
  
## <a name="adding-connections"></a><span data-ttu-id="5ad2b-141">Přidání připojení</span><span class="sxs-lookup"><span data-stu-id="5ad2b-141">Adding Connections</span></span>  
 <span data-ttu-id="5ad2b-142">Fond připojení se vytvoří pro každý jedinečných připojovací řetězec.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-142">A connection pool is created for each unique connection string.</span></span> <span data-ttu-id="5ad2b-143">Při vytvoření fondu jsou více objektů připojení vytvořen a přidán do fondu tak, aby velikost požadavek na minimální fond není splněna.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-143">When a pool is created, multiple connection objects are created and added to the pool so that the minimum pool size requirement is satisfied.</span></span> <span data-ttu-id="5ad2b-144">Připojení se přidají do fondu podle potřeby až do maximální velikosti fondu zadána (výchozí hodnota je 100).</span><span class="sxs-lookup"><span data-stu-id="5ad2b-144">Connections are added to the pool as needed, up to the maximum pool size specified (100 is the default).</span></span> <span data-ttu-id="5ad2b-145">Připojení se vydávají zpět do fondu, pokud jsou ukončen nebo odstraněn.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-145">Connections are released back into the pool when they are closed or disposed.</span></span>  
  
 <span data-ttu-id="5ad2b-146">Když <xref:System.Data.SqlClient.SqlConnection> je požadován objekt, se získávají z fondu, pokud je k dispozici použitelné připojení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-146">When a <xref:System.Data.SqlClient.SqlConnection> object is requested, it is obtained from the pool if a usable connection is available.</span></span> <span data-ttu-id="5ad2b-147">Má být použitelná, připojení musí nevyužité, nemají odpovídající kontext transakce nebo nepřidružené k žádnému kontextu transakce a mít platný odkaz na serveru.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-147">To be usable, a connection must be unused, have a matching transaction context or be unassociated with any transaction context, and have a valid link to the server.</span></span>  
  
 <span data-ttu-id="5ad2b-148">Pro sdružování připojení splňuje požadavky pro připojení pomocí přerozdělení připojení při jejich vydání zpět do fondu.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-148">The connection pooler satisfies requests for connections by reallocating connections as they are released back into the pool.</span></span> <span data-ttu-id="5ad2b-149">Pokud bylo dosaženo maximální velikosti fondu a není k dispozici žádné použitelné připojení, je požadavek ve frontě.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-149">If the maximum pool size has been reached and no usable connection is available, the request is queued.</span></span> <span data-ttu-id="5ad2b-150">Pro sdružování pokusí uvolnit všechna připojení, dokud nebude dosaženo časového limitu (výchozí hodnota je 15 sekund).</span><span class="sxs-lookup"><span data-stu-id="5ad2b-150">The pooler then tries to reclaim any connections until the time-out is reached (the default is 15 seconds).</span></span> <span data-ttu-id="5ad2b-151">Pokud pro sdružování nemůže požadavek splnit, předtím, než vyprší časový limit připojení, je vyvolána výjimka.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-151">If the pooler cannot satisfy the request before the connection times out, an exception is thrown.</span></span>  
  
> [!CAUTION]
>  <span data-ttu-id="5ad2b-152">Důrazně doporučujeme po dokončení jeho použití tak, aby připojení bude vrácen do fondu vždy ukončení připojení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-152">We strongly recommend that you always close the connection when you are finished using it so that the connection will be returned to the pool.</span></span> <span data-ttu-id="5ad2b-153">Můžete provést buď pomocí `Close` nebo `Dispose` metody `Connection` objektu, nebo otevřením všechna připojení v rámci `using` příkaz v jazyce C#, nebo `Using` v sadě Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-153">You can do this using either the `Close` or `Dispose` methods of the `Connection` object, or by opening all connections inside a `using` statement in C#, or a `Using` statement in Visual Basic.</span></span> <span data-ttu-id="5ad2b-154">Připojení, které nebyly uzavřeny explicitně nemusí přidali nebo vrácen do fondu.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-154">Connections that are not explicitly closed might not be added or returned to the pool.</span></span> <span data-ttu-id="5ad2b-155">Další informace najdete v tématu [příkaz using](~/docs/csharp/language-reference/keywords/using-statement.md) nebo [postupy: odstranění systémového prostředku](~/docs/visual-basic/programming-guide/language-features/control-flow/how-to-dispose-of-a-system-resource.md) v jazyce Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-155">For more information, see [using Statement](~/docs/csharp/language-reference/keywords/using-statement.md) or [How to: Dispose of a System Resource](~/docs/visual-basic/programming-guide/language-features/control-flow/how-to-dispose-of-a-system-resource.md) for Visual Basic.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="5ad2b-156">Nevolejte `Close` nebo `Dispose` na `Connection`, `DataReader`, nebo jakýkoli jiný spravovaný objekt v `Finalize` metoda vaší třídy.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-156">Do not call `Close` or `Dispose` on a `Connection`, a `DataReader`, or any other managed object in the `Finalize` method of your class.</span></span> <span data-ttu-id="5ad2b-157">V finalizační metodu jenom uvolnění nespravovaných prostředků, které vaše třída vlastní přímo.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-157">In a finalizer, only release unmanaged resources that your class owns directly.</span></span> <span data-ttu-id="5ad2b-158">Pokud vaše třída není vlastníkem jakýchkoliv nespravovaných prostředků, nezahrnujte `Finalize` metoda v definici třídy.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-158">If your class does not own any unmanaged resources, do not include a `Finalize` method in your class definition.</span></span> <span data-ttu-id="5ad2b-159">Další informace najdete v tématu [uvolňování](../../../../docs/standard/garbage-collection/index.md).</span><span class="sxs-lookup"><span data-stu-id="5ad2b-159">For more information, see [Garbage Collection](../../../../docs/standard/garbage-collection/index.md).</span></span>  
  
<span data-ttu-id="5ad2b-160">Další informace o událostech, které jsou přidružené k otevření a zavření připojení najdete v tématu [třídy události auditování přihlášení](/sql/relational-databases/event-classes/audit-login-event-class) a [třída událostí auditu odhlášení](/sql/relational-databases/event-classes/audit-logout-event-class) v dokumentaci k SQL serveru.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-160">For more info about the events associated with opening and closing connections, see [Audit Login Event Class](/sql/relational-databases/event-classes/audit-login-event-class) and [Audit Logout Event Class](/sql/relational-databases/event-classes/audit-logout-event-class) in the SQL Server documentation.</span></span>  
  
## <a name="removing-connections"></a><span data-ttu-id="5ad2b-161">Odebrání připojení</span><span class="sxs-lookup"><span data-stu-id="5ad2b-161">Removing Connections</span></span>  
 <span data-ttu-id="5ad2b-162">Pro sdružování připojení odebere z fondu připojení, poté, co byl nečinný přibližně 4. – 8 minut, nebo pokud pro sdružování zjistí, že připojení k serveru bylo porušeno.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-162">The connection pooler removes a connection from the pool after it has been idle for approximately 4-8 minutes, or if the pooler detects that the connection with the server has been severed.</span></span> <span data-ttu-id="5ad2b-163">Všimněte si, že přerušená připojení lze zjistit pouze po pokusu o komunikaci se serverem.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-163">Note that a severed connection can be detected only after attempting to communicate with the server.</span></span> <span data-ttu-id="5ad2b-164">Pokud připojení není nalezen, který není připojený k serveru, je označena jako neplatná.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-164">If a connection is found that is no longer connected to the server, it is marked as invalid.</span></span> <span data-ttu-id="5ad2b-165">Neplatné připojení se odeberou z fondu připojení, pouze když jsou uzavřeny nebo uvolnit.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-165">Invalid connections are removed from the connection pool only when they are closed or reclaimed.</span></span>  
  
 <span data-ttu-id="5ad2b-166">Pokud připojení existuje na serveru, který chybí, lze toto připojení rozlišovat z fondu i v případě, že nebyl zjištěn přerušená připojení a označena jako neplatná pro sdružování připojení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-166">If a connection exists to a server that has disappeared, this connection can be drawn from the pool even if the connection pooler has not detected the severed connection and marked it as invalid.</span></span> <span data-ttu-id="5ad2b-167">To platí, protože režii kontrole, že připojení je stále platný by o výhodách pro sdružování způsobením učiníte serveru dojde k odstranění.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-167">This is the case because the overhead of checking that the connection is still valid would eliminate the benefits of having a pooler by causing another round trip to the server to occur.</span></span> <span data-ttu-id="5ad2b-168">V tomto případě první pokus o připojení k zjistí, že připojení bylo porušeno. a je vyvolána výjimka.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-168">When this occurs, the first attempt to use the connection will detect that the connection has been severed, and an exception is thrown.</span></span>  
  
## <a name="clearing-the-pool"></a><span data-ttu-id="5ad2b-169">Vymazává se fondu</span><span class="sxs-lookup"><span data-stu-id="5ad2b-169">Clearing the Pool</span></span>  
 [!INCLUDE[vstecado](../../../../includes/vstecado-md.md)]<span data-ttu-id="5ad2b-170"> 2.0 zavedené dvě nové metody pro vymazávání fondu: <xref:System.Data.SqlClient.SqlConnection.ClearAllPools%2A> a <xref:System.Data.SqlClient.SqlConnection.ClearPool%2A>.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-170"> 2.0 introduced two new methods to clear the pool: <xref:System.Data.SqlClient.SqlConnection.ClearAllPools%2A> and <xref:System.Data.SqlClient.SqlConnection.ClearPool%2A>.</span></span> <span data-ttu-id="5ad2b-171">`ClearAllPools` Vymaže sdružení připojení pro daného poskytovatele a `ClearPool` vymaže fondu připojení, který je spojen s konkrétním připojení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-171">`ClearAllPools` clears the connection pools for a given provider, and `ClearPool` clears the connection pool that is associated with a specific connection.</span></span> <span data-ttu-id="5ad2b-172">Pokud je připojení používaná v okamžiku volání, jsou označen odpovídajícím způsobem.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-172">If there are connections being used at the time of the call, they are marked appropriately.</span></span> <span data-ttu-id="5ad2b-173">Když jsou uzavřeny, jsou zahozeny místo se vrací do fondu.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-173">When they are closed, they are discarded instead of being returned to the pool.</span></span>  
  
## <a name="transaction-support"></a><span data-ttu-id="5ad2b-174">Podpora transakcí</span><span class="sxs-lookup"><span data-stu-id="5ad2b-174">Transaction Support</span></span>  
 <span data-ttu-id="5ad2b-175">Připojení jsou vykreslovány vedle z fondu a kontext přiřazené na základě transakce.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-175">Connections are drawn from the pool and assigned based on transaction context.</span></span> <span data-ttu-id="5ad2b-176">Není-li `Enlist=false` zadaná v připojovacím řetězci, fond připojení zajišťuje, že připojení je uveden v <xref:System.Transactions.Transaction.Current%2A> kontextu.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-176">Unless `Enlist=false` is specified in the connection string, the connection pool makes sure that the connection is enlisted in the <xref:System.Transactions.Transaction.Current%2A> context.</span></span> <span data-ttu-id="5ad2b-177">Pokud je připojení uzavřen a vrácen do fondu se zařazených `System.Transactions` transakce, to je odložena tak, že další žádosti pro tento fond připojení se stejným `System.Transactions` transakce se vrátí stejné připojení, pokud je k dispozici.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-177">When a connection is closed and returned to the pool with an enlisted `System.Transactions` transaction, it is set aside in such a way that the next request for that connection pool with the same `System.Transactions` transaction will return the same connection if it is available.</span></span> <span data-ttu-id="5ad2b-178">Pokud takový požadavek vydává a nejsou k dispozici žádné připojení ve fondu, je připojení ze součástí fondu beztransakční a zařazen.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-178">If such a request is issued, and there are no pooled connections available, a connection is drawn from the non-transacted part of the pool and enlisted.</span></span> <span data-ttu-id="5ad2b-179">Pokud v obou oblastech fondu jsou k dispozici žádné připojení, se nové připojení a zařazeny.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-179">If no connections are available in either area of the pool, a new connection is created and enlisted.</span></span>  
  
 <span data-ttu-id="5ad2b-180">Když je připojení ukončeno, uvolní se zpět do fondu a do příslušné dělení na základě jeho kontextu transakce.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-180">When a connection is closed, it is released back into the pool and into the appropriate subdivision based on its transaction context.</span></span> <span data-ttu-id="5ad2b-181">Proto můžete zavřít připojení bez dojde k chybě, i když distribuované transakce je stále čekající na vyřízení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-181">Therefore, you can close the connection without generating an error, even though a distributed transaction is still pending.</span></span> <span data-ttu-id="5ad2b-182">To umožňuje potvrzení nebo přerušení později distribuované transakce.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-182">This allows you to commit or abort the distributed transaction later.</span></span>  
  
## <a name="controlling-connection-pooling-with-connection-string-keywords"></a><span data-ttu-id="5ad2b-183">Řízení sdružování připojení s klíčovými slovy připojovací řetězec</span><span class="sxs-lookup"><span data-stu-id="5ad2b-183">Controlling Connection Pooling with Connection String Keywords</span></span>  
 <span data-ttu-id="5ad2b-184">`ConnectionString` Vlastnost <xref:System.Data.SqlClient.SqlConnection> objekt podporuje dvojice klíč/hodnota řetězce připojení, které lze použít k úpravě chování logiky sdružování připojení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-184">The `ConnectionString` property of the <xref:System.Data.SqlClient.SqlConnection> object supports connection string key/value pairs that can be used to adjust the behavior of the connection pooling logic.</span></span> <span data-ttu-id="5ad2b-185">Další informace naleznete v tématu <xref:System.Data.SqlClient.SqlConnection.ConnectionString%2A>.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-185">For more information, see <xref:System.Data.SqlClient.SqlConnection.ConnectionString%2A>.</span></span>  
  
## <a name="pool-fragmentation"></a><span data-ttu-id="5ad2b-186">Fragmentace fondu</span><span class="sxs-lookup"><span data-stu-id="5ad2b-186">Pool Fragmentation</span></span>  
 <span data-ttu-id="5ad2b-187">Fragmentace fondu je běžný problém v mnoha webových aplikací, kde aplikace může vytvářet velký počet fondů, které nejsou uvolněny, dokud se proces ukončí.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-187">Pool fragmentation is a common problem in many Web applications where the application can create a large number of pools that are not freed until the process exits.</span></span> <span data-ttu-id="5ad2b-188">Velký počet připojení tak zůstanou otevřené a využívání paměti, což má za následek nízký výkon.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-188">This leaves a large number of connections open and consuming memory, which results in poor performance.</span></span>  
  
### <a name="pool-fragmentation-due-to-integrated-security"></a><span data-ttu-id="5ad2b-189">Fragmentace fondu z důvodu integrované zabezpečení</span><span class="sxs-lookup"><span data-stu-id="5ad2b-189">Pool Fragmentation Due to Integrated Security</span></span>  
 <span data-ttu-id="5ad2b-190">Připojení se sdílejí ve fondech podle připojovací řetězec a identitu uživatele.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-190">Connections are pooled according to the connection string plus the user identity.</span></span> <span data-ttu-id="5ad2b-191">Proto pokud používáte základní ověřování nebo ověřování Windows na webovém serveru a integrované zabezpečené přihlášení, můžete získat jeden fond na uživatele.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-191">Therefore, if you use Basic authentication or Windows Authentication on the Web site and an integrated security login, you get one pool per user.</span></span> <span data-ttu-id="5ad2b-192">I když to zlepšuje výkon při následné databáze požadavků pro jednoho uživatele, tento uživatel nemůže využít připojení provedené ostatními uživateli.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-192">Although this improves the performance of subsequent database requests for a single user, that user cannot take advantage of connections made by other users.</span></span> <span data-ttu-id="5ad2b-193">Výsledkem je také alespoň jedno připojení na uživatele pro databázový server.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-193">It also results in at least one connection per user to the database server.</span></span> <span data-ttu-id="5ad2b-194">Toto je vedlejším účinkem konkrétní architektura pro webové aplikace, které musí vývojáři naváží s požadavky na auditování a zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-194">This is a side effect of a particular Web application architecture that developers must weigh against security and auditing requirements.</span></span>  
  
### <a name="pool-fragmentation-due-to-many-databases"></a><span data-ttu-id="5ad2b-195">Fragmentace fondu z důvodu velkého počtu databází</span><span class="sxs-lookup"><span data-stu-id="5ad2b-195">Pool Fragmentation Due to Many Databases</span></span>  
 <span data-ttu-id="5ad2b-196">Mnoho poskytovatelů služeb Internetu hostování několika webů na jednom serveru.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-196">Many Internet service providers host several Web sites on a single server.</span></span> <span data-ttu-id="5ad2b-197">Potvrďte účet ověřování formulářů a pak otevřete připojení ke konkrétní databázi pro uživatele nebo skupinu uživatelů mohou používat izolované databáze.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-197">They may use a single database to confirm a Forms authentication login and then open a connection to a specific database for that user or group of users.</span></span> <span data-ttu-id="5ad2b-198">Připojení k databázi ověřování je ve fondu a používání pro všechny uživatele.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-198">The connection to the authentication database is pooled and used by everyone.</span></span> <span data-ttu-id="5ad2b-199">Je však samostatný fond připojení pro každou databázi, která zvýšit počet připojení k serveru.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-199">However, there is a separate pool of connections to each database, which increase the number of connections to the server.</span></span>  
  
 <span data-ttu-id="5ad2b-200">Toto je také vedlejší efekt návrh aplikace.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-200">This is also a side-effect of the application design.</span></span> <span data-ttu-id="5ad2b-201">Je poměrně jednoduchý způsob, jak tomuto vedlejšímu efektu zabránit bez narušení zabezpečení, když se připojíte k serveru SQL Server.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-201">There is a relatively simple way to avoid this side effect without compromising security when you connect to SQL Server.</span></span> <span data-ttu-id="5ad2b-202">Místo připojování k samostatnou databázi pro jednotlivé uživatele nebo skupiny, připojení ke stejné databázi na serveru a následné provádění [!INCLUDE[tsql](../../../../includes/tsql-md.md)] příkaz USE změnit na požadované databáze.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-202">Instead of connecting to a separate database for each user or group, connect to the same database on the server and then execute the [!INCLUDE[tsql](../../../../includes/tsql-md.md)] USE statement to change to the desired database.</span></span> <span data-ttu-id="5ad2b-203">Následující fragment kódu ukazuje vytvoření počátečního připojení k `master` databáze a poté přepnete do požadovaného databázi zadané v `databaseName` proměnné řetězce.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-203">The following code fragment demonstrates creating an initial connection to the `master` database and then switching to the desired database specified in the `databaseName` string variable.</span></span>  
  
```vb  
' Assumes that command is a valid SqlCommand object and that  
' connectionString connects to master.  
    command.Text = "USE DatabaseName"  
Using connection As New SqlConnection(connectionString)  
    connection.Open()  
    command.ExecuteNonQuery()  
End Using  
```  
  
```csharp  
// Assumes that command is a SqlCommand object and that  
// connectionString connects to master.  
command.Text = "USE DatabaseName";  
using (SqlConnection connection = new SqlConnection(  
  connectionString))  
  {  
    connection.Open();  
    command.ExecuteNonQuery();  
  }  
```  
  
## <a name="application-roles-and-connection-pooling"></a><span data-ttu-id="5ad2b-204">Aplikační role a sdružování připojení</span><span class="sxs-lookup"><span data-stu-id="5ad2b-204">Application Roles and Connection Pooling</span></span>  
 <span data-ttu-id="5ad2b-205">Po SQL Server aktivoval se aplikační role voláním `sp_setapprole` systémové uložené procedury, kontext zabezpečení připojení nelze resetovat.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-205">After a SQL Server application role has been activated by calling the `sp_setapprole` system stored procedure, the security context of that connection cannot be reset.</span></span> <span data-ttu-id="5ad2b-206">Ale pokud je povoleno sdružování připojení je vrácen do fondu a dojde k chybě, když je znovu použít ve fondu připojení.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-206">However, if pooling is enabled, the connection is returned to the pool, and an error occurs when the pooled connection is reused.</span></span> <span data-ttu-id="5ad2b-207">Další informace najdete v článku znalostní báze Knowledge Base "[chyby role SQL aplikace s sdružování prostředků OLE DB](https://support.microsoft.com/default.aspx?scid=KB;EN-US;Q229564)."</span><span class="sxs-lookup"><span data-stu-id="5ad2b-207">For more information, see the Knowledge Base article, "[SQL application role errors with OLE DB resource pooling](https://support.microsoft.com/default.aspx?scid=KB;EN-US;Q229564)."</span></span>  
  
### <a name="application-role-alternatives"></a><span data-ttu-id="5ad2b-208">Alternativy Role aplikace</span><span class="sxs-lookup"><span data-stu-id="5ad2b-208">Application Role Alternatives</span></span>  
 <span data-ttu-id="5ad2b-209">Doporučujeme vám, že využijete mechanismy zabezpečení, které můžete použít namísto aplikační role.</span><span class="sxs-lookup"><span data-stu-id="5ad2b-209">We recommend that you take advantage of security mechanisms that you can use instead of application roles.</span></span> <span data-ttu-id="5ad2b-210">Další informace najdete v tématu [vytváření rolí aplikací v systému SQL Server](../../../../docs/framework/data/adonet/sql/creating-application-roles-in-sql-server.md).</span><span class="sxs-lookup"><span data-stu-id="5ad2b-210">For more information, see [Creating Application Roles in SQL Server](../../../../docs/framework/data/adonet/sql/creating-application-roles-in-sql-server.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="5ad2b-211">Viz také</span><span class="sxs-lookup"><span data-stu-id="5ad2b-211">See Also</span></span>  
 [<span data-ttu-id="5ad2b-212">Sdružování připojení</span><span class="sxs-lookup"><span data-stu-id="5ad2b-212">Connection Pooling</span></span>](../../../../docs/framework/data/adonet/connection-pooling.md)  
 [<span data-ttu-id="5ad2b-213">SQL Server a ADO.NET</span><span class="sxs-lookup"><span data-stu-id="5ad2b-213">SQL Server and ADO.NET</span></span>](../../../../docs/framework/data/adonet/sql/index.md)  
 [<span data-ttu-id="5ad2b-214">Čítače výkonu</span><span class="sxs-lookup"><span data-stu-id="5ad2b-214">Performance Counters</span></span>](../../../../docs/framework/data/adonet/performance-counters.md)  
 [<span data-ttu-id="5ad2b-215">ADO.NET spravovaných zprostředkovatelích a datové sady pro vývojáře</span><span class="sxs-lookup"><span data-stu-id="5ad2b-215">ADO.NET Managed Providers and DataSet Developer Center</span></span>](https://go.microsoft.com/fwlink/?LinkId=217917)
