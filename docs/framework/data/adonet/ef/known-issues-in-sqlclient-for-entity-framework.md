---
title: Známé problémy v SqlClient rozhraní Entity Framework
ms.date: 03/30/2017
ms.assetid: 48fe4912-4d0f-46b6-be96-3a42c54780f6
ms.openlocfilehash: 99d4c5c1cf7275936cc7a13bb545321ec8407e43
ms.sourcegitcommit: 11f11ca6cefe555972b3a5c99729d1a7523d8f50
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 05/03/2018
ms.locfileid: "32765656"
---
# <a name="known-issues-in-sqlclient-for-entity-framework"></a><span data-ttu-id="cb82c-102">Známé problémy v SqlClient rozhraní Entity Framework</span><span class="sxs-lookup"><span data-stu-id="cb82c-102">Known Issues in SqlClient for Entity Framework</span></span>
<span data-ttu-id="cb82c-103">Tato část popisuje známé problémy související s zprostředkovatele dat .NET Framework pro SQL Server (SqlClient).</span><span class="sxs-lookup"><span data-stu-id="cb82c-103">This section describes known issues related to the .NET Framework Data Provider for SQL Server (SqlClient).</span></span>  
  
## <a name="trailing-spaces-in-string-functions"></a><span data-ttu-id="cb82c-104">Koncové mezery řetězcové funkce</span><span class="sxs-lookup"><span data-stu-id="cb82c-104">Trailing Spaces in String Functions</span></span>  
 <span data-ttu-id="cb82c-105">SQL Server ignoruje koncové mezery v řetězcové hodnoty.</span><span class="sxs-lookup"><span data-stu-id="cb82c-105">SQL Server ignores trailing spaces in string values.</span></span> <span data-ttu-id="cb82c-106">Proto předávání koncové mezery v řetězci může vést k vést k neočekávaným výsledkům, i selhání.</span><span class="sxs-lookup"><span data-stu-id="cb82c-106">Therefore, passing trailing spaces in the string can lead to unpredictable results, even failures.</span></span>  
  
 <span data-ttu-id="cb82c-107">Pokud máte tak, aby měl koncové mezery ve vašem řetězci, měli byste zvážit připojování prázdný znak na konci, tak, aby SQL Server není oříznout řetězec.</span><span class="sxs-lookup"><span data-stu-id="cb82c-107">If you have to have trailing spaces in your string, you should consider appending a white space character at the end, so that SQL Server does not trim the string.</span></span> <span data-ttu-id="cb82c-108">Pokud koncové mezery nejsou vyžadovány, má být oříznut předtím, než jsou předávány dolů kanálu dotazu.</span><span class="sxs-lookup"><span data-stu-id="cb82c-108">If the trailing spaces are not required, they should be trimmed before they are passed down the query pipeline.</span></span>  
  
## <a name="right-function"></a><span data-ttu-id="cb82c-109">RIGHT – funkce</span><span class="sxs-lookup"><span data-stu-id="cb82c-109">RIGHT Function</span></span>  
 <span data-ttu-id="cb82c-110">Pokud jinou hodnotu než`null` byla předána hodnota jako první argument a 0 je předána jako druhý argument `RIGHT(nvarchar(max)`, 0`)` nebo `RIGHT(varchar(max)`, 0`)`, `NULL` bude vrácena hodnota místo `empty` řetězec.</span><span class="sxs-lookup"><span data-stu-id="cb82c-110">If a non-`null` value is passed as a first argument and 0 is passed as a second argument to `RIGHT(nvarchar(max)`, 0`)` or `RIGHT(varchar(max)`, 0`)`, a `NULL` value will be returned instead of an `empty` string.</span></span>  
  
## <a name="cross-and-outer-apply-operators"></a><span data-ttu-id="cb82c-111">MEZI a vnější použít operátory</span><span class="sxs-lookup"><span data-stu-id="cb82c-111">CROSS and OUTER APPLY Operators</span></span>  
 <span data-ttu-id="cb82c-112">MEZI a operátoru OUTER APPLY operátory byly zavedeny v [!INCLUDE[ssVersion2005](../../../../../includes/ssversion2005-md.md)].</span><span class="sxs-lookup"><span data-stu-id="cb82c-112">CROSS and OUTER APPLY operators were introduced in [!INCLUDE[ssVersion2005](../../../../../includes/ssversion2005-md.md)].</span></span> <span data-ttu-id="cb82c-113">V některých případech může vytvořit kanál dotazu příkazu Transact-SQL, který obsahuje operátory křížové použít nebo operátoru OUTER APPLY.</span><span class="sxs-lookup"><span data-stu-id="cb82c-113">In some cases the query pipeline might produce a Transact-SQL statement that contains CROSS APPLY and/or OUTER APPLY operators.</span></span> <span data-ttu-id="cb82c-114">Protože někteří poskytovatelé back-end, včetně verze systému SQL Server starších než [!INCLUDE[ssVersion2005](../../../../../includes/ssversion2005-md.md)], nepodporují tyto operátory, takové dotazy se nedá spustit na tyto zprostředkovatele back-end.</span><span class="sxs-lookup"><span data-stu-id="cb82c-114">Because some backend providers, including versions of SQL Server earlier than [!INCLUDE[ssVersion2005](../../../../../includes/ssversion2005-md.md)], do not support these operators, such queries cannot be executed on these backend providers.</span></span>  
  
 <span data-ttu-id="cb82c-115">Tady jsou některé typické scénáře, které mohou vést k přítomnost mezi použít nebo operátoru OUTER APPLY operátory v dotazu výstup:</span><span class="sxs-lookup"><span data-stu-id="cb82c-115">The following are some typical scenarios that might lead to the presence of CROSS APPLY and/or OUTER APPLY operators in the output query:</span></span>  
  
-   <span data-ttu-id="cb82c-116">Korelační poddotaz s stránkování.</span><span class="sxs-lookup"><span data-stu-id="cb82c-116">A correlated subquery with paging.</span></span>  
  
-   <span data-ttu-id="cb82c-117">`AnyElement` Přes korelační poddotazu nebo přes kolekci vyprodukované navigace.</span><span class="sxs-lookup"><span data-stu-id="cb82c-117">An `AnyElement` over a correlated sub-query, or over a collection produced by navigation.</span></span>  
  
-   <span data-ttu-id="cb82c-118">LINQ dotazy, které používají seskupování metody, které přijímají selektorem elementu.</span><span class="sxs-lookup"><span data-stu-id="cb82c-118">LINQ queries that use grouping methods that accept an element selector.</span></span>  
  
-   <span data-ttu-id="cb82c-119">Dotaz, ve kterém je explicitně zadaný křížové použít nebo operátoru OUTER APPLY</span><span class="sxs-lookup"><span data-stu-id="cb82c-119">A query in which a CROSS APPLY or an OUTER APPLY is explicitly specified</span></span>  
  
-   <span data-ttu-id="cb82c-120">Vytvořit dotaz, který má DEREF přes REF konstrukce.</span><span class="sxs-lookup"><span data-stu-id="cb82c-120">A query that has a DEREF construct over a REF construct.</span></span>  
  
## <a name="skip-operator"></a><span data-ttu-id="cb82c-121">SKIP – operátor</span><span class="sxs-lookup"><span data-stu-id="cb82c-121">SKIP Operator</span></span>  
 <span data-ttu-id="cb82c-122">Pokud používáte [!INCLUDE[ssVersion2000](../../../../../includes/ssversion2000-md.md)], pomocí přeskočit s klauzulí ORDER BY v neklíčových sloupců může vrátit nesprávné výsledky.</span><span class="sxs-lookup"><span data-stu-id="cb82c-122">If you are using [!INCLUDE[ssVersion2000](../../../../../includes/ssversion2000-md.md)], using SKIP with ORDER BY on non-key columns might return incorrect results.</span></span> <span data-ttu-id="cb82c-123">Více než zadaný počet řádků může přeskočen. Pokud neklíčový sloupec obsahuje duplicitní data v ní.</span><span class="sxs-lookup"><span data-stu-id="cb82c-123">More than the specified number of rows might be skipped if the non-key column has duplicate data in it.</span></span> <span data-ttu-id="cb82c-124">Je to z důvodu jak SKIP je přeložená pro [!INCLUDE[ssVersion2000](../../../../../includes/ssversion2000-md.md)].</span><span class="sxs-lookup"><span data-stu-id="cb82c-124">This is due to how SKIP is translated for [!INCLUDE[ssVersion2000](../../../../../includes/ssversion2000-md.md)].</span></span> <span data-ttu-id="cb82c-125">Například v následujícím dotazu, více než pět řádků může být přeskočeno `E.NonKeyColumn` obsahuje duplicitní hodnoty:</span><span class="sxs-lookup"><span data-stu-id="cb82c-125">For example, in the following query, more than five rows might be skipped if `E.NonKeyColumn` has duplicate values:</span></span>  
  
```  
SELECT [E] FROM Container.EntitySet AS [E] ORDER BY [E].[NonKeyColumn] DESC SKIP 5L  
```  
  
## <a name="targeting-the-correct-sql-server-version"></a><span data-ttu-id="cb82c-126">Cílení na verzi serveru SQL správný</span><span class="sxs-lookup"><span data-stu-id="cb82c-126">Targeting the Correct SQL Server Version</span></span>  
 <span data-ttu-id="cb82c-127">[!INCLUDE[adonet_ef](../../../../../includes/adonet-ef-md.md)] Cíle [!INCLUDE[tsql](../../../../../includes/tsql-md.md)] podle verze systému SQL Server, který je uveden v dotazu `ProviderManifestToken` atribut elementu schématu v souboru modelu (ssdl) úložiště.</span><span class="sxs-lookup"><span data-stu-id="cb82c-127">The [!INCLUDE[adonet_ef](../../../../../includes/adonet-ef-md.md)] targets the [!INCLUDE[tsql](../../../../../includes/tsql-md.md)] query based on the SQL Server version that is specified in the `ProviderManifestToken` attribute of the Schema element in the storage model (.ssdl) file.</span></span> <span data-ttu-id="cb82c-128">Tato verze může lišit od verze skutečné jste připojeni k serveru SQL.</span><span class="sxs-lookup"><span data-stu-id="cb82c-128">This version might differ from the version of the actual SQL Server you are connected to.</span></span> <span data-ttu-id="cb82c-129">Například, pokud používáte SQL Server 2005 ale vaše `ProviderManifestToken` je atribut nastaven na 2008, vygenerovaného [!INCLUDE[tsql](../../../../../includes/tsql-md.md)] dotaz nemusí spustit na serveru.</span><span class="sxs-lookup"><span data-stu-id="cb82c-129">For example, if you are using SQL Server 2005, but your `ProviderManifestToken` attribute is set to 2008, the generated [!INCLUDE[tsql](../../../../../includes/tsql-md.md)] query might not execute on the server.</span></span> <span data-ttu-id="cb82c-130">Například nebude v dřívějších verzích systému SQL Server spustit dotaz, který používá nové datum čas typy, které byly zavedeny v systému SQL Server 2008.</span><span class="sxs-lookup"><span data-stu-id="cb82c-130">For example, a query that uses the new date time types that were introduced in SQL Server 2008 will not execute on earlier versions of the SQL Server.</span></span> <span data-ttu-id="cb82c-131">Pokud používáte SQL Server 2005, ale vaše `ProviderManifestToken` je atribut nastaven na 2000 vygenerovaného [!INCLUDE[tsql](../../../../../includes/tsql-md.md)] dotazu může být menší optimalizované nebo výjimku, která uvádí, že dotaz není podporovaný, může získat.</span><span class="sxs-lookup"><span data-stu-id="cb82c-131">If you are using SQL Server 2005, but your `ProviderManifestToken` attribute is set to 2000, the generated [!INCLUDE[tsql](../../../../../includes/tsql-md.md)] query might be less optimized, or you might get an exception that says that the query is not supported.</span></span> <span data-ttu-id="cb82c-132">Další informace najdete v tématu mezi vnější použít operátory části a dříve v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="cb82c-132">For more information, see the CROSS and OUTER APPLY Operators section, earlier in this topic.</span></span>  
  
 <span data-ttu-id="cb82c-133">Určitého chování databáze závisí na úroveň kompatibility nastaveno na databázi.</span><span class="sxs-lookup"><span data-stu-id="cb82c-133">Certain database behaviors depend on the compatibility level set to the database.</span></span> <span data-ttu-id="cb82c-134">Pokud vaše `ProviderManifestToken` 2005 nastavený atribut a je vaší verze systému SQL Server 2005, ale úroveň kompatibility databáze je nastavena na "80" (SQL Server 2000), vygenerovaného [!INCLUDE[tsql](../../../../../includes/tsql-md.md)] bude cílené na SQL Server 2005, ale nemusí provést podle očekávání kvůli nastavení úrovně kompatibility.</span><span class="sxs-lookup"><span data-stu-id="cb82c-134">If your `ProviderManifestToken` attribute is set to 2005 and your SQL Server version is 2005, but the compatibility level of a database is set to "80" (SQL Server 2000), the generated [!INCLUDE[tsql](../../../../../includes/tsql-md.md)] will be targeting SQL Server 2005, but might not execute as expected due to the compatibility level setting.</span></span> <span data-ttu-id="cb82c-135">Například může ztratit řazení informace, pokud název sloupce v seznamu ORDER BY odpovídá název sloupce v modulu pro výběr.</span><span class="sxs-lookup"><span data-stu-id="cb82c-135">For example, you might lose ordering information if a column name in the ORDER BY list matches a column name in the selector.</span></span>  
  
## <a name="nested-queries-in-projection"></a><span data-ttu-id="cb82c-136">Vnořené dotazy v projekci</span><span class="sxs-lookup"><span data-stu-id="cb82c-136">Nested Queries in Projection</span></span>  
 <span data-ttu-id="cb82c-137">Vnořené dotazy v klauzuli projekce může získat přeložit na kartézský součin dotazy na serveru.</span><span class="sxs-lookup"><span data-stu-id="cb82c-137">Nested queries in a projection clause might get translated into Cartesian product queries on the server.</span></span> <span data-ttu-id="cb82c-138">Na některých serverech back-end, včetně serveru SQL Server to může způsobit v tabulce databáze TempDB získat poměrně velké.</span><span class="sxs-lookup"><span data-stu-id="cb82c-138">On some back-end servers, including SLQ Server, this can cause the TempDB table to get quite large.</span></span> <span data-ttu-id="cb82c-139">To může snížit výkon serveru.</span><span class="sxs-lookup"><span data-stu-id="cb82c-139">This can decrease server performance.</span></span>  
  
 <span data-ttu-id="cb82c-140">Následuje příklad vnořený dotaz v klauzuli projekce:</span><span class="sxs-lookup"><span data-stu-id="cb82c-140">The following is an example of  a nested query in a projection clause:</span></span>  
  
```  
SELECT c, (SELECT c, (SELECT c FROM AdventureWorksModel.Vendor AS c  ) As Inner2 FROM AdventureWorksModel.JobCandidate AS c  ) As Inner1 FROM AdventureWorksModel.EmployeeDepartmentHistory AS c  
```  
  
## <a name="server-generated-guid-identity-values"></a><span data-ttu-id="cb82c-141">Hodnoty Identity GUID generovaný serverem</span><span class="sxs-lookup"><span data-stu-id="cb82c-141">Server Generated GUID Identity Values</span></span>  
 <span data-ttu-id="cb82c-142">[!INCLUDE[adonet_ef](../../../../../includes/adonet-ef-md.md)] Podporuje hodnoty generované serverem GUID typ identity, ale zprostředkovatel musí podporovat vrácení hodnoty generované serverem identity po vložení řádku.</span><span class="sxs-lookup"><span data-stu-id="cb82c-142">The [!INCLUDE[adonet_ef](../../../../../includes/adonet-ef-md.md)] supports server-generated GUID type identity values, but the provider must support returning the server-generated identity value after a row was inserted.</span></span> <span data-ttu-id="cb82c-143">Od verze systému SQL Server 2005, můžete se vrátit na typ GUID generované serverem v databázi systému SQL Server prostřednictvím [klauzuli OUTPUT](http://go.microsoft.com/fwlink/?LinkId=169400) .</span><span class="sxs-lookup"><span data-stu-id="cb82c-143">Starting with SQL Server 2005, you can return the server-generated GUID type in a SQL Server database through the [OUTPUT clause](http://go.microsoft.com/fwlink/?LinkId=169400) .</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="cb82c-144">Viz také</span><span class="sxs-lookup"><span data-stu-id="cb82c-144">See Also</span></span>  
 [<span data-ttu-id="cb82c-145">SqlClient pro Entity Framework</span><span class="sxs-lookup"><span data-stu-id="cb82c-145">SqlClient for the Entity Framework</span></span>](../../../../../docs/framework/data/adonet/ef/sqlclient-for-the-entity-framework.md)  
 [<span data-ttu-id="cb82c-146">Známé problémy a aspekty u LINQ to Entities</span><span class="sxs-lookup"><span data-stu-id="cb82c-146">Known Issues and Considerations in LINQ to Entities</span></span>](../../../../../docs/framework/data/adonet/ef/language-reference/known-issues-and-considerations-in-linq-to-entities.md)
