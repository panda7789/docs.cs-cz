---
title: Zpracování událostí datové tabulky
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: 62f404a5-13ea-4b93-a29f-55b74a16c9d3
ms.openlocfilehash: 414be4a5bdbd1fe5d65475efcd5e72606b73685f
ms.sourcegitcommit: 0be8a279af6d8a43e03141e349d3efd5d35f8767
ms.translationtype: HT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/18/2019
ms.locfileid: "59312823"
---
# <a name="handling-datatable-events"></a><span data-ttu-id="93c28-102">Zpracování událostí datové tabulky</span><span class="sxs-lookup"><span data-stu-id="93c28-102">Handling DataTable Events</span></span>
<span data-ttu-id="93c28-103"><xref:System.Data.DataTable> Objekt, který poskytuje řadu událostí, které mohou být zpracovány aplikací.</span><span class="sxs-lookup"><span data-stu-id="93c28-103">The <xref:System.Data.DataTable> object provides a series of events that can be processed by an application.</span></span> <span data-ttu-id="93c28-104">Následující tabulka popisuje `DataTable` události.</span><span class="sxs-lookup"><span data-stu-id="93c28-104">The following table describes `DataTable` events.</span></span>  
  
|<span data-ttu-id="93c28-105">Událost</span><span class="sxs-lookup"><span data-stu-id="93c28-105">Event</span></span>|<span data-ttu-id="93c28-106">Popis</span><span class="sxs-lookup"><span data-stu-id="93c28-106">Description</span></span>|  
|-----------|-----------------|  
|<xref:System.Data.DataTable.Initialized>|<span data-ttu-id="93c28-107">Nastane po <xref:System.Data.DataTable.EndInit%2A> metodu `DataTable` je volána.</span><span class="sxs-lookup"><span data-stu-id="93c28-107">Occurs after the <xref:System.Data.DataTable.EndInit%2A> method of a `DataTable` is called.</span></span> <span data-ttu-id="93c28-108">Tato událost je určený primárně pro zajištění podpory scénářů návrhu.</span><span class="sxs-lookup"><span data-stu-id="93c28-108">This event is intended primarily to support design-time scenarios.</span></span>|  
|<xref:System.Data.DataTable.ColumnChanged>|<span data-ttu-id="93c28-109">Vyvolá se po hodnotu po úspěšném provedení změny v <xref:System.Data.DataColumn>.</span><span class="sxs-lookup"><span data-stu-id="93c28-109">Occurs after a value has been successfully changed in a <xref:System.Data.DataColumn>.</span></span>|  
|<xref:System.Data.DataTable.ColumnChanging>|<span data-ttu-id="93c28-110">Nastane, pokud hodnota byla odeslána k `DataColumn`.</span><span class="sxs-lookup"><span data-stu-id="93c28-110">Occurs when a value has been submitted for a `DataColumn`.</span></span>|  
|<xref:System.Data.DataTable.RowChanged>|<span data-ttu-id="93c28-111">Nastane po `DataColumn` hodnotu nebo <xref:System.Data.DataRow.RowState%2A> z <xref:System.Data.DataRow> v `DataTable` byl úspěšně změněn.</span><span class="sxs-lookup"><span data-stu-id="93c28-111">Occurs after a `DataColumn` value or the <xref:System.Data.DataRow.RowState%2A> of a <xref:System.Data.DataRow> in the `DataTable` has been changed successfully.</span></span>|  
|<xref:System.Data.DataTable.RowChanging>|<span data-ttu-id="93c28-112">Nastane, pokud se odeslal změnu `DataColumn` hodnotu nebo `RowState` z `DataRow` v `DataTable`.</span><span class="sxs-lookup"><span data-stu-id="93c28-112">Occurs when a change has been submitted for a `DataColumn` value or the `RowState` of a `DataRow` in the `DataTable`.</span></span>|  
|<xref:System.Data.DataTable.RowDeleted>|<span data-ttu-id="93c28-113">Nastane po `DataRow` v `DataTable` byl označen jako `Deleted`.</span><span class="sxs-lookup"><span data-stu-id="93c28-113">Occurs after a `DataRow` in the `DataTable` has been marked as `Deleted`.</span></span>|  
|<xref:System.Data.DataTable.RowDeleting>|<span data-ttu-id="93c28-114">Vyvolá se před `DataRow` v `DataTable` je označen jako `Deleted`.</span><span class="sxs-lookup"><span data-stu-id="93c28-114">Occurs before a `DataRow` in the `DataTable` is marked as `Deleted`.</span></span>|  
|<xref:System.Data.DataTable.TableCleared>|<span data-ttu-id="93c28-115">Vyvolá se po volání <xref:System.Data.DataTable.Clear%2A> metodu `DataTable` úspěšně vymazala každý `DataRow`.</span><span class="sxs-lookup"><span data-stu-id="93c28-115">Occurs after a call to the <xref:System.Data.DataTable.Clear%2A> method of the `DataTable` has successfully cleared every `DataRow`.</span></span>|  
|<xref:System.Data.DataTable.TableClearing>|<span data-ttu-id="93c28-116">Nastane po `Clear` metoda je volána, ale předtím, než `Clear` zahájení operace.</span><span class="sxs-lookup"><span data-stu-id="93c28-116">Occurs after the `Clear` method is called but before the `Clear` operation begins.</span></span>|  
|<xref:System.Data.DataTable.TableNewRow>|<span data-ttu-id="93c28-117">Dojde poté, co je nového `DataRow` je vytvořen voláním `NewRow` metodu `DataTable`.</span><span class="sxs-lookup"><span data-stu-id="93c28-117">Occurs after a new `DataRow` is created by a call to the `NewRow` method of the `DataTable`.</span></span>|  
|<xref:System.ComponentModel.MarshalByValueComponent.Disposed>|<span data-ttu-id="93c28-118">Nastane, když `DataTable` je `Disposed`.</span><span class="sxs-lookup"><span data-stu-id="93c28-118">Occurs when the `DataTable` is `Disposed`.</span></span> <span data-ttu-id="93c28-119">Zděděno z <xref:System.ComponentModel.MarshalByValueComponent>.</span><span class="sxs-lookup"><span data-stu-id="93c28-119">Inherited from <xref:System.ComponentModel.MarshalByValueComponent>.</span></span>|  
  
> [!NOTE]
>  <span data-ttu-id="93c28-120">Většinu operací, které přidávat a odstraňovat řádky nevyvolávejte `ColumnChanged` a `ColumnChanging` události.</span><span class="sxs-lookup"><span data-stu-id="93c28-120">Most operations that add or delete rows do not raise the `ColumnChanged` and `ColumnChanging` events.</span></span> <span data-ttu-id="93c28-121">Ale `ReadXml` vyvolat metodu `ColumnChanged` a `ColumnChanging` události, není-li `XmlReadMode` je nastavena na `DiffGram` nebo je nastaven na `Auto` po dokumentu XML, který je čten `DiffGram`.</span><span class="sxs-lookup"><span data-stu-id="93c28-121">However, the `ReadXml` method does raise `ColumnChanged` and `ColumnChanging` events, unless the `XmlReadMode` is set to `DiffGram` or is set to `Auto` when the XML document being read is a `DiffGram`.</span></span>  
  
> [!WARNING]
>  <span data-ttu-id="93c28-122">Poškození dat může dojít, pokud se při změně dat `DataSet` ze kterého `RowChanged` událost se vyvolá.</span><span class="sxs-lookup"><span data-stu-id="93c28-122">Data corruption can occur if data is modified in a `DataSet` from which the `RowChanged` event is raised.</span></span> <span data-ttu-id="93c28-123">Pokud dojde k poškození těchto dat, bude vyvolána žádná výjimka.</span><span class="sxs-lookup"><span data-stu-id="93c28-123">No exception will be raised if such data corruption occurs.</span></span>  
  
## <a name="additional-related-events"></a><span data-ttu-id="93c28-124">Další související události</span><span class="sxs-lookup"><span data-stu-id="93c28-124">Additional Related Events</span></span>  
 <span data-ttu-id="93c28-125"><xref:System.Data.DataTable.Constraints%2A> Obsahuje vlastnost <xref:System.Data.ConstraintCollection> instance.</span><span class="sxs-lookup"><span data-stu-id="93c28-125">The <xref:System.Data.DataTable.Constraints%2A> property holds a <xref:System.Data.ConstraintCollection> instance.</span></span> <span data-ttu-id="93c28-126"><xref:System.Data.ConstraintCollection> Třídy zpřístupňuje <xref:System.Data.ConstraintCollection.CollectionChanged> událostí.</span><span class="sxs-lookup"><span data-stu-id="93c28-126">The <xref:System.Data.ConstraintCollection> class exposes a <xref:System.Data.ConstraintCollection.CollectionChanged> event.</span></span> <span data-ttu-id="93c28-127">Tato událost se aktivuje při přidání, změnit nebo odebrat z omezení `ConstraintCollection`.</span><span class="sxs-lookup"><span data-stu-id="93c28-127">This event fires when a constraint is added, modified, or removed from the `ConstraintCollection`.</span></span>  
  
 <span data-ttu-id="93c28-128"><xref:System.Data.DataTable.Columns%2A> Obsahuje vlastnost <xref:System.Data.DataColumnCollection> instance.</span><span class="sxs-lookup"><span data-stu-id="93c28-128">The <xref:System.Data.DataTable.Columns%2A> property holds a <xref:System.Data.DataColumnCollection> instance.</span></span> <span data-ttu-id="93c28-129">`DataColumnCollection` Třídy zpřístupňuje <xref:System.Data.DataColumnCollection.CollectionChanged> událostí.</span><span class="sxs-lookup"><span data-stu-id="93c28-129">The `DataColumnCollection` class exposes a <xref:System.Data.DataColumnCollection.CollectionChanged> event.</span></span> <span data-ttu-id="93c28-130">Tato událost aktivuje, když `DataColumn` je přidat, upravit nebo odebrat z `DataColumnCollection`.</span><span class="sxs-lookup"><span data-stu-id="93c28-130">This event fires when a `DataColumn` is added, modified, or removed from the `DataColumnCollection`.</span></span> <span data-ttu-id="93c28-131">Změny, které způsobují na aktivaci události zahrnují změny názvu, typu, výraz nebo pořadí sloupce.</span><span class="sxs-lookup"><span data-stu-id="93c28-131">Modifications that cause the event to fire include changes to the name, type, expression or ordinal position of a column.</span></span>  
  
 <span data-ttu-id="93c28-132"><xref:System.Data.DataSet.Tables%2A> Vlastnost <xref:System.Data.DataSet> obsahuje <xref:System.Data.DataTableCollection> instance.</span><span class="sxs-lookup"><span data-stu-id="93c28-132">The <xref:System.Data.DataSet.Tables%2A> property of a <xref:System.Data.DataSet> holds a <xref:System.Data.DataTableCollection> instance.</span></span> <span data-ttu-id="93c28-133">`DataTableCollection` Třídy vystaví oba `CollectionChanged` a `CollectionChanging` událostí.</span><span class="sxs-lookup"><span data-stu-id="93c28-133">The `DataTableCollection` class exposes both a `CollectionChanged` and a `CollectionChanging` event.</span></span> <span data-ttu-id="93c28-134">Vyvolat tyto události, kdy `DataTable` je přidán či odebrán z `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="93c28-134">These events fire when a `DataTable` is added to or removed from the `DataSet`.</span></span>  
  
 <span data-ttu-id="93c28-135">Změny `DataRows` také mohou aktivovat události pro přidružené <xref:System.Data.DataView>.</span><span class="sxs-lookup"><span data-stu-id="93c28-135">Changes to `DataRows` can also trigger events for an associated <xref:System.Data.DataView>.</span></span> <span data-ttu-id="93c28-136">`DataView` Třídy zpřístupňuje <xref:System.Data.DataView.ListChanged> událost, která se spustí při `DataColumn` změny hodnot nebo když se změní složení nebo pořadí řazení zobrazení.</span><span class="sxs-lookup"><span data-stu-id="93c28-136">The `DataView` class exposes a <xref:System.Data.DataView.ListChanged> event that fires when a `DataColumn` value changes or when the composition or sort order of the view changes.</span></span> <span data-ttu-id="93c28-137"><xref:System.Data.DataRowView> Třídy zpřístupňuje <xref:System.Data.DataRowView.PropertyChanged> událost, která je vyvoláno, když je přiřazený `DataColumn` hodnota se mění.</span><span class="sxs-lookup"><span data-stu-id="93c28-137">The <xref:System.Data.DataRowView> class exposes a <xref:System.Data.DataRowView.PropertyChanged> event that fires when an associated `DataColumn` value changes.</span></span>  
  
## <a name="sequence-of-operations"></a><span data-ttu-id="93c28-138">Posloupnost operací</span><span class="sxs-lookup"><span data-stu-id="93c28-138">Sequence of Operations</span></span>  
 <span data-ttu-id="93c28-139">Tady je posloupnost operací, ke kterým dochází při `DataRow` je přidat, upravit nebo odstranit:</span><span class="sxs-lookup"><span data-stu-id="93c28-139">Here is the sequence of operations that occur when a `DataRow` is added, modified, or deleted:</span></span>  
  
1. <span data-ttu-id="93c28-140">Vytváření navrhovaná záznam a použity všechny změny.</span><span class="sxs-lookup"><span data-stu-id="93c28-140">Create the proposed record and apply any changes.</span></span>  
  
2. <span data-ttu-id="93c28-141">Kontrola omezení u sloupců bez výrazu.</span><span class="sxs-lookup"><span data-stu-id="93c28-141">Check constraints for non-expression columns.</span></span>  
  
3. <span data-ttu-id="93c28-142">Vyvolat `RowChanging` nebo `RowDeleting` události podle potřeby.</span><span class="sxs-lookup"><span data-stu-id="93c28-142">Raise the `RowChanging` or `RowDeleting` events as applicable.</span></span>  
  
4. <span data-ttu-id="93c28-143">Nastavte navrhovaných záznam na aktuální záznam.</span><span class="sxs-lookup"><span data-stu-id="93c28-143">Set the proposed record to be the current record.</span></span>  
  
5. <span data-ttu-id="93c28-144">Aktualizujte všechny přidružené indexy.</span><span class="sxs-lookup"><span data-stu-id="93c28-144">Update any associated indexes.</span></span>  
  
6. <span data-ttu-id="93c28-145">Vyvolat `ListChanged` související události pro `DataView` objekty a `PropertyChanged` související události pro `DataRowView` objekty.</span><span class="sxs-lookup"><span data-stu-id="93c28-145">Raise `ListChanged` events for associated `DataView` objects and `PropertyChanged` events for associated `DataRowView` objects.</span></span>  
  
7. <span data-ttu-id="93c28-146">Vyhodnoťte všechny sloupce výrazu, ale zpoždění kontroly jakákoliv omezení u těchto sloupců.</span><span class="sxs-lookup"><span data-stu-id="93c28-146">Evaluate all expression columns, but delay checking any constraints on these columns.</span></span>  
  
8. <span data-ttu-id="93c28-147">Vyvolat `ListChanged` související události pro `DataView` objekty a `PropertyChanged` související události pro `DataRowView` objekty ovlivněné vyhodnocení výrazu sloupce.</span><span class="sxs-lookup"><span data-stu-id="93c28-147">Raise `ListChanged` events for associated `DataView` objects and `PropertyChanged` events for associated `DataRowView` objects affected by the expression column evaluations.</span></span>  
  
9. <span data-ttu-id="93c28-148">Vyvolat `RowChanged` nebo `RowDeleted` události podle potřeby.</span><span class="sxs-lookup"><span data-stu-id="93c28-148">Raise `RowChanged` or `RowDeleted` events as applicable.</span></span>  
  
10. <span data-ttu-id="93c28-149">Kontrola omezení na sloupec.</span><span class="sxs-lookup"><span data-stu-id="93c28-149">Check constraints on expression columns.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="93c28-150">Změny výrazu sloupce nikdy vyvolat `DataTable` události.</span><span class="sxs-lookup"><span data-stu-id="93c28-150">Changes to expression columns never raise `DataTable` events.</span></span> <span data-ttu-id="93c28-151">Změny výrazu sloupce pouze zvýšit `DataView` a `DataRowView` události.</span><span class="sxs-lookup"><span data-stu-id="93c28-151">Changes to expression columns only raise `DataView` and `DataRowView` events.</span></span> <span data-ttu-id="93c28-152">Výraz sloupce mohou mít závislosti na několik dalších sloupců a může být vyhodnocen více než jednou během jediné `DataRow` operace.</span><span class="sxs-lookup"><span data-stu-id="93c28-152">Expression columns can have dependencies on multiple other columns, and can be evaluated multiple times during a single `DataRow` operation.</span></span> <span data-ttu-id="93c28-153">Každé vyhodnocení výrazu vyvolává události a jediného `DataRow` může operace vyvolat více `ListChanged` a `PropertyChanged` události, kdy výrazu sloupce ovlivňuje, včetně více událostí pro stejný sloupec výrazu.</span><span class="sxs-lookup"><span data-stu-id="93c28-153">Each expression evaluation raises events, and a single `DataRow` operation can raise multiple `ListChanged` and `PropertyChanged` events when expression columns are affected, possibly including multiple events for the same expression column.</span></span>  
  
> [!WARNING]
>  <span data-ttu-id="93c28-154">Nevyvolají výjimku <xref:System.NullReferenceException> v rámci `RowChanged` obslužné rutiny události.</span><span class="sxs-lookup"><span data-stu-id="93c28-154">Do not throw a <xref:System.NullReferenceException> within the `RowChanged` event handler.</span></span> <span data-ttu-id="93c28-155">Pokud <xref:System.NullReferenceException> je vyvolána v rámci `RowChanged` události `DataTable`, pak bude `DataTable` poškozen.</span><span class="sxs-lookup"><span data-stu-id="93c28-155">If a <xref:System.NullReferenceException> is thrown within the `RowChanged` event of a `DataTable`, then the `DataTable` will be corrupted.</span></span>  
  
### <a name="example"></a><span data-ttu-id="93c28-156">Příklad</span><span class="sxs-lookup"><span data-stu-id="93c28-156">Example</span></span>  
 <span data-ttu-id="93c28-157">Následující příklad ukazuje, jak vytvořit obslužné rutiny událostí pro `RowChanged`, `RowChanging`, `RowDeleted`, `RowDeleting`, `ColumnChanged`, `ColumnChanging`, `TableNewRow`, `TableCleared`, a `TableClearing` události.</span><span class="sxs-lookup"><span data-stu-id="93c28-157">The following example demonstrates how to create event handlers for the `RowChanged`, `RowChanging`, `RowDeleted`, `RowDeleting`, `ColumnChanged`, `ColumnChanging`, `TableNewRow`, `TableCleared`, and `TableClearing` events.</span></span> <span data-ttu-id="93c28-158">Když se aktivuje Každá obslužná rutina události zobrazí výstup v okně konzoly.</span><span class="sxs-lookup"><span data-stu-id="93c28-158">Each event handler displays output in the console window when it is fired.</span></span>  
  
 [!code-csharp[DataWorks DataTable.Events#1](../../../../../samples/snippets/csharp/VS_Snippets_ADO.NET/DataWorks DataTable.Events/CS/source.cs#1)]
 [!code-vb[DataWorks DataTable.Events#1](../../../../../samples/snippets/visualbasic/VS_Snippets_ADO.NET/DataWorks DataTable.Events/VB/source.vb#1)]  
  
## <a name="see-also"></a><span data-ttu-id="93c28-159">Viz také:</span><span class="sxs-lookup"><span data-stu-id="93c28-159">See also</span></span>

- [<span data-ttu-id="93c28-160">Manipulace s daty v datové tabulce</span><span class="sxs-lookup"><span data-stu-id="93c28-160">Manipulating Data in a DataTable</span></span>](../../../../../docs/framework/data/adonet/dataset-datatable-dataview/manipulating-data-in-a-datatable.md)
- [<span data-ttu-id="93c28-161">Zpracování událostí adaptéru dat</span><span class="sxs-lookup"><span data-stu-id="93c28-161">Handling DataAdapter Events</span></span>](../../../../../docs/framework/data/adonet/handling-dataadapter-events.md)
- [<span data-ttu-id="93c28-162">Zpracování událostí datové sady</span><span class="sxs-lookup"><span data-stu-id="93c28-162">Handling DataSet Events</span></span>](../../../../../docs/framework/data/adonet/dataset-datatable-dataview/handling-dataset-events.md)
- [<span data-ttu-id="93c28-163">ADO.NET spravovaných zprostředkovatelích a datové sady pro vývojáře</span><span class="sxs-lookup"><span data-stu-id="93c28-163">ADO.NET Managed Providers and DataSet Developer Center</span></span>](https://go.microsoft.com/fwlink/?LinkId=217917)
