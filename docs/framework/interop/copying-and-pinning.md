---
title: Kopírování a přichycování
ms.date: 03/30/2017
helpviewer_keywords:
- pinning, interop marshaling
- copying, interop marshaling
- interop marshaling, copying
- interop marshaling, pinning
ms.assetid: 0059f576-e460-4e70-b257-668870e420b8
ms.openlocfilehash: f6db7d37293015911c1285d39e19bf7542a7ac59
ms.sourcegitcommit: 559fcfbe4871636494870a8b716bf7325df34ac5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/30/2019
ms.locfileid: "73123643"
---
# <a name="copying-and-pinning"></a><span data-ttu-id="422fe-102">Kopírování a přichycování</span><span class="sxs-lookup"><span data-stu-id="422fe-102">Copying and Pinning</span></span>

<span data-ttu-id="422fe-103">Při zařazování dat může zařazovací modul spolupráce zkopírovat nebo připnout data, která jsou zařazování.</span><span class="sxs-lookup"><span data-stu-id="422fe-103">When marshaling data, the interop marshaler can copy or pin the data being marshaled.</span></span> <span data-ttu-id="422fe-104">Kopírování dat umístí kopii dat z jednoho umístění v paměti v jiném umístění v paměti.</span><span class="sxs-lookup"><span data-stu-id="422fe-104">Copying the data places a copy of data from one memory location in another memory location.</span></span> <span data-ttu-id="422fe-105">Následující ilustrace znázorňuje rozdíly mezi zkopírováním typu hodnoty a zkopírováním typu předaného odkazem ze spravovaného do nespravované paměti.</span><span class="sxs-lookup"><span data-stu-id="422fe-105">The following illustration shows the differences between copying a value type and copying a type passed by reference from managed to unmanaged memory.</span></span>

![Diagram, který ukazuje, jak se zkopírují typy hodnot a odkazů](./media/copying-and-pinning/interop-marshal-copy.gif)

<span data-ttu-id="422fe-107">Argumenty metody předané hodnotou jsou zařazeny do nespravovaného kódu jako hodnoty v zásobníku.</span><span class="sxs-lookup"><span data-stu-id="422fe-107">Method arguments passed by value are marshaled to unmanaged code as values on the stack.</span></span> <span data-ttu-id="422fe-108">Proces kopírování je přímý.</span><span class="sxs-lookup"><span data-stu-id="422fe-108">The copying process is direct.</span></span> <span data-ttu-id="422fe-109">Argumenty předané odkazem jsou předány jako ukazatelé na zásobníku.</span><span class="sxs-lookup"><span data-stu-id="422fe-109">Arguments passed by reference are passed as pointers on the stack.</span></span> <span data-ttu-id="422fe-110">Odkazové typy jsou také předávány hodnotou a odkazem.</span><span class="sxs-lookup"><span data-stu-id="422fe-110">Reference types are also passed by value and by reference.</span></span> <span data-ttu-id="422fe-111">Jak ukazuje následující obrázek, typy odkazů předané hodnotou jsou buď zkopírovány nebo připnuté:</span><span class="sxs-lookup"><span data-stu-id="422fe-111">As the following illustration shows, reference types passed by value are either copied or pinned:</span></span>

![Diagram znázorňující typy odkazů předané hodnotou a odkazem.](./media/copying-and-pinning/interop-marshal-reference-pin.gif)

<span data-ttu-id="422fe-113">Připnutí dočasně uzamkne data v jejich aktuálním umístění v paměti, takže tím brání jejich přemístění systémem uvolňování paměti společného jazykového modulu runtime.</span><span class="sxs-lookup"><span data-stu-id="422fe-113">Pinning temporarily locks the data in its current memory location, thus keeping it from being relocated by the common language runtime's garbage collector.</span></span> <span data-ttu-id="422fe-114">Data zařazovacího modulu připnete k omezení režijních nákladů na kopírování a zvýšení výkonu.</span><span class="sxs-lookup"><span data-stu-id="422fe-114">The marshaler pins data to reduce the overhead of copying and enhance performance.</span></span> <span data-ttu-id="422fe-115">Typ dat určuje, zda je zkopírován nebo připnuté během procesu zařazování.</span><span class="sxs-lookup"><span data-stu-id="422fe-115">The type of the data determines whether it is copied or pinned during the marshaling process.</span></span>  <span data-ttu-id="422fe-116">Připnutí se provádí automaticky během zařazování pro objekty <xref:System.String>, jako je, můžete ale také ručně připnout paměť <xref:System.Runtime.InteropServices.GCHandle> pomocí třídy.</span><span class="sxs-lookup"><span data-stu-id="422fe-116">Pinning is automatically performed during marshaling for objects such as <xref:System.String>, however you can also manually pin memory using the <xref:System.Runtime.InteropServices.GCHandle> class.</span></span>

## <a name="formatted-blittable-classes"></a><span data-ttu-id="422fe-117">Formátované přenositelné třídy</span><span class="sxs-lookup"><span data-stu-id="422fe-117">Formatted Blittable Classes</span></span>

<span data-ttu-id="422fe-118">Naformátované [přenositelné třídy mají](blittable-and-non-blittable-types.md) v spravované i nespravované paměti pevné rozložení (naformátované) a běžné reprezentace dat.</span><span class="sxs-lookup"><span data-stu-id="422fe-118">Formatted [blittable](blittable-and-non-blittable-types.md) classes have fixed layout (formatted) and common data representation in both managed and unmanaged memory.</span></span> <span data-ttu-id="422fe-119">Pokud tyto typy vyžadují zařazování, ukazatel na objekt v haldě je předán volanému přímo.</span><span class="sxs-lookup"><span data-stu-id="422fe-119">When these types require marshaling, a pointer to the object in the heap is passed to the callee directly.</span></span> <span data-ttu-id="422fe-120">Volaný může změnit obsah umístění v paměti, na které odkazuje ukazatel.</span><span class="sxs-lookup"><span data-stu-id="422fe-120">The callee can change the contents of the memory location being referenced by the pointer.</span></span>

> [!NOTE]
> <span data-ttu-id="422fe-121">Volaný může změnit obsah paměti, pokud je parametr označený jako out nebo out. Naproti tomu by volaný neměl měnit obsah, pokud je parametr nastaven na zařazování jako v, což je výchozí hodnota pro formátované přenositelné typy.</span><span class="sxs-lookup"><span data-stu-id="422fe-121">The callee can change the memory contents if the parameter is marked Out or In/Out. In contrast, the callee should avoid changing the contents when the parameter is set to marshal as In, which is the default for formatted blittable types.</span></span> <span data-ttu-id="422fe-122">Změna v objektu generuje problémy, pokud je stejná třída exportována do knihovny typů a použita k provedení volání křížového typu.</span><span class="sxs-lookup"><span data-stu-id="422fe-122">Modifying an In object generates problems when the same class is exported to a type library and used to make cross-apartment calls.</span></span>

## <a name="formatted-non-blittable-classes"></a><span data-ttu-id="422fe-123">Naformátované třídy, které nejsou přenositelné</span><span class="sxs-lookup"><span data-stu-id="422fe-123">Formatted Non-Blittable Classes</span></span>

<span data-ttu-id="422fe-124">Formátované nepřenositelné třídy mají pevně dané rozložení ( [naformátované](blittable-and-non-blittable-types.md) ), ale reprezentace dat se liší ve spravované a nespravované paměti.</span><span class="sxs-lookup"><span data-stu-id="422fe-124">Formatted [non-blittable](blittable-and-non-blittable-types.md) classes have fixed layout (formatted) but the data representation is different in managed and unmanaged memory.</span></span> <span data-ttu-id="422fe-125">Data mohou vyžadovat transformaci za následujících podmínek:</span><span class="sxs-lookup"><span data-stu-id="422fe-125">The data can require transformation under the following conditions:</span></span>

- <span data-ttu-id="422fe-126">Pokud je třída, která není přenositelná, zařazena podle hodnoty, Volaný obdrží ukazatel na kopii struktury dat.</span><span class="sxs-lookup"><span data-stu-id="422fe-126">If a non-blittable class is marshaled by value, the callee receives a pointer to a copy of the data structure.</span></span>

- <span data-ttu-id="422fe-127">Pokud je nepřenosná třída zařazená odkazem, Volaný obdrží ukazatel na ukazatel na kopii struktury dat.</span><span class="sxs-lookup"><span data-stu-id="422fe-127">If a non-blittable class is marshaled by reference, the callee receives a pointer to a pointer to a copy of the data structure.</span></span>

- <span data-ttu-id="422fe-128">Pokud je <xref:System.Runtime.InteropServices.InAttribute> atribut nastaven, bude tato kopie vždy inicializována se stavem instance, zařazování podle potřeby.</span><span class="sxs-lookup"><span data-stu-id="422fe-128">If the <xref:System.Runtime.InteropServices.InAttribute> attribute is set, this copy is always initialized with the instance's state, marshaling as necessary.</span></span>

- <span data-ttu-id="422fe-129">Pokud je <xref:System.Runtime.InteropServices.OutAttribute> atribut nastaven, stav je vždy zkopírován zpět do instance při návratu, zařazování podle potřeby.</span><span class="sxs-lookup"><span data-stu-id="422fe-129">If the <xref:System.Runtime.InteropServices.OutAttribute> attribute is set, the state is always copied back to the instance on return, marshaling as necessary.</span></span>

- <span data-ttu-id="422fe-130">Pokud jsou nastaveny atributy **InAttribute** i **subattribute** , jsou požadovány obě kopie.</span><span class="sxs-lookup"><span data-stu-id="422fe-130">If both **InAttribute** and **OutAttribute** are set, both copies are required.</span></span> <span data-ttu-id="422fe-131">Pokud je atribut vynechán, zařazovací modul může být optimalizován vyřazením buď zkopírování.</span><span class="sxs-lookup"><span data-stu-id="422fe-131">If either attribute is omitted, the marshaler can optimize by eliminating either copy.</span></span>

## <a name="reference-types"></a><span data-ttu-id="422fe-132">Typy odkazů</span><span class="sxs-lookup"><span data-stu-id="422fe-132">Reference Types</span></span>

<span data-ttu-id="422fe-133">Odkazové typy mohou být předány hodnotou nebo odkazem.</span><span class="sxs-lookup"><span data-stu-id="422fe-133">Reference types can be passed by value or by reference.</span></span> <span data-ttu-id="422fe-134">Když jsou předávány hodnotou, ukazatel na typ je předán do zásobníku.</span><span class="sxs-lookup"><span data-stu-id="422fe-134">When they are passed by value, a pointer to the type is passed on the stack.</span></span> <span data-ttu-id="422fe-135">Při předání odkazem je ukazatel na ukazatel na typ předán do zásobníku.</span><span class="sxs-lookup"><span data-stu-id="422fe-135">When passed by reference, a pointer to a pointer to the type is passed on the stack.</span></span>

<span data-ttu-id="422fe-136">Typy odkazů mají následující podmíněné chování:</span><span class="sxs-lookup"><span data-stu-id="422fe-136">Reference types have the following conditional behavior:</span></span>

- <span data-ttu-id="422fe-137">Pokud je odkazový typ předán podle hodnoty a má členy nepřenositelného typu, jsou typy převedeny dvakrát:</span><span class="sxs-lookup"><span data-stu-id="422fe-137">If a reference type is passed by value and it has members of non-blittable types, the types are converted twice:</span></span>

  - <span data-ttu-id="422fe-138">Při předání argumentu nespravované straně.</span><span class="sxs-lookup"><span data-stu-id="422fe-138">When an argument is passed to the unmanaged side.</span></span>

  - <span data-ttu-id="422fe-139">Při návratu z volání.</span><span class="sxs-lookup"><span data-stu-id="422fe-139">On return from the call.</span></span>

  <span data-ttu-id="422fe-140">Aby nedocházelo k zbytečnému kopírování a převodu, jsou tyto typy zařazeny jako v parametrech.</span><span class="sxs-lookup"><span data-stu-id="422fe-140">To avoid unnecessarily copying and conversion, these types are marshaled as In parameters.</span></span> <span data-ttu-id="422fe-141">Chcete-li zobrazit změny provedené volaným, je nutné explicitně použít atributy **InAttribute** a Attribute **atributu** na argument volajícího.</span><span class="sxs-lookup"><span data-stu-id="422fe-141">You must explicitly apply the **InAttribute** and **OutAttribute** attributes to an argument for the caller to see changes made by the callee.</span></span>

- <span data-ttu-id="422fe-142">Pokud je odkazový typ předán podle hodnoty a má pouze členy typů, lze jej připnout během zařazování a jakékoli změny provedené u členů typu volanými jsou viditelné volajícím.</span><span class="sxs-lookup"><span data-stu-id="422fe-142">If a reference type is passed by value and it has only members of blittable types, it can be pinned during marshaling and any changes made to the members of the type by the callee are seen by the caller.</span></span> <span data-ttu-id="422fe-143">Pokud chcete toto chování, použijte **atribut InAttribute** a **InAttribute** .</span><span class="sxs-lookup"><span data-stu-id="422fe-143">Apply **InAttribute** and **OutAttribute** explicitly if you want this behavior.</span></span> <span data-ttu-id="422fe-144">Bez těchto směrových atributů nezařazovací modul Interop neexportuje směrující informace do knihovny typů (exportuje se jako v, což je výchozí nastavení), což může způsobit problémy s zařazováním do více platforem modelu COM.</span><span class="sxs-lookup"><span data-stu-id="422fe-144">Without these directional attributes, the interop marshaler does not export directional information to the type library (it exports as In, which is the default) and this can cause problems with COM cross-apartment marshaling.</span></span>

- <span data-ttu-id="422fe-145">Pokud typ odkazu přechází odkazem, bude ve výchozím nastavení zařazen jako ve výchozím stavu.</span><span class="sxs-lookup"><span data-stu-id="422fe-145">If a reference type is passed by reference, it will be marshaled as In/Out by default.</span></span>

## <a name="systemstring-and-systemtextstringbuilder"></a><span data-ttu-id="422fe-146">System. String a System. text. StringBuilder</span><span class="sxs-lookup"><span data-stu-id="422fe-146">System.String and System.Text.StringBuilder</span></span>

<span data-ttu-id="422fe-147">Když jsou data zařazena do nespravovaného kódu pomocí hodnoty nebo odkazem, zařazovací objekt obvykle kopíruje data do sekundární vyrovnávací paměti (případně převádějí znakové sady během kopírování) a předá odkaz na vyrovnávací paměť volanému.</span><span class="sxs-lookup"><span data-stu-id="422fe-147">When data is marshaled to unmanaged code by value or by reference, the marshaler typically copies the data to a secondary buffer (possibly converting character sets during the copy) and passes a reference to the buffer to the callee.</span></span> <span data-ttu-id="422fe-148">Pokud odkaz není typu **BSTR** přidělený pomocí **případě**, je odkaz vždy přidělen pomocí **CoTaskMemAlloc**.</span><span class="sxs-lookup"><span data-stu-id="422fe-148">Unless the reference is a **BSTR** allocated with **SysAllocString**, the reference is always allocated with **CoTaskMemAlloc**.</span></span>

<span data-ttu-id="422fe-149">Jako optimalizace, pokud je typ řetězce zařazen podle hodnoty (například řetězce znaků Unicode), zařazovací modul před kopírováním do nové vyrovnávací paměti předává volaný přímý ukazatel na spravované řetězce v interní vyrovnávací paměti Unicode.</span><span class="sxs-lookup"><span data-stu-id="422fe-149">As an optimization when either string type is marshaled by value (such as a Unicode character string), the marshaler passes the callee a direct pointer to managed strings in the internal Unicode buffer instead of copying it to a new buffer.</span></span>

> [!CAUTION]
> <span data-ttu-id="422fe-150">Když je řetězec předán podle hodnoty, volaný nesmí nikdy změnit odkaz předaný zařazovacím objektem.</span><span class="sxs-lookup"><span data-stu-id="422fe-150">When a string is passed by value, the callee must never alter the reference passed by the marshaler.</span></span> <span data-ttu-id="422fe-151">V takovém případě může být poškozena spravovaná halda.</span><span class="sxs-lookup"><span data-stu-id="422fe-151">Doing so can corrupt the managed heap.</span></span>

<span data-ttu-id="422fe-152">Pokud <xref:System.String?displayProperty=nameWithType> je předána odkazem, zařazovací služba zkopíruje obsah řetězce do sekundární vyrovnávací paměti před provedením volání.</span><span class="sxs-lookup"><span data-stu-id="422fe-152">When a <xref:System.String?displayProperty=nameWithType> is passed by reference, the marshaler copies the contents the string to a secondary buffer before making the call.</span></span> <span data-ttu-id="422fe-153">Pak zkopíruje obsah vyrovnávací paměti do nového řetězce při návratu z volání.</span><span class="sxs-lookup"><span data-stu-id="422fe-153">It then copies the contents of the buffer into a new string on return from the call.</span></span> <span data-ttu-id="422fe-154">Tato technika zajišťuje, že neměnné spravované řetězce zůstávají beze změny.</span><span class="sxs-lookup"><span data-stu-id="422fe-154">This technique ensures that the immutable managed string remains unaltered.</span></span>

<span data-ttu-id="422fe-155">Když <xref:System.Text.StringBuilder?displayProperty=nameWithType> je předána hodnotou, zařazovací modul předává odkaz na vnitřní vyrovnávací paměť **StringBuilder** přímo volajícímu.</span><span class="sxs-lookup"><span data-stu-id="422fe-155">When a <xref:System.Text.StringBuilder?displayProperty=nameWithType> is passed by value, the marshaler passes a reference to the internal buffer of the **StringBuilder** directly to the caller.</span></span> <span data-ttu-id="422fe-156">Volající a volaný musí souhlasit s velikostí vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="422fe-156">The caller and callee must agree on the size of the buffer.</span></span> <span data-ttu-id="422fe-157">Volající je zodpovědný za vytvoření **StringBuilder** s adekvátní délkou.</span><span class="sxs-lookup"><span data-stu-id="422fe-157">The caller is responsible for creating a **StringBuilder** of adequate length.</span></span> <span data-ttu-id="422fe-158">Volaný musí podniknout nezbytná opatření, aby se zajistilo, že vyrovnávací paměť nebude přetečení.</span><span class="sxs-lookup"><span data-stu-id="422fe-158">The callee must take the necessary precautions to ensure that the buffer is not overrun.</span></span> <span data-ttu-id="422fe-159">**StringBuilder** je výjimka pro pravidlo, které odkazuje na typy předané hodnotou, ve výchozím nastavení předány jako parametry v parametrech.</span><span class="sxs-lookup"><span data-stu-id="422fe-159">**StringBuilder** is an exception to the rule that reference types passed by value are passed as In parameters by default.</span></span> <span data-ttu-id="422fe-160">Vždycky se předává jako vstupně-výstupní.</span><span class="sxs-lookup"><span data-stu-id="422fe-160">It is always passed as In/Out.</span></span>

## <a name="see-also"></a><span data-ttu-id="422fe-161">Viz také</span><span class="sxs-lookup"><span data-stu-id="422fe-161">See also</span></span>

- [<span data-ttu-id="422fe-162">Výchozí chování zařazování</span><span class="sxs-lookup"><span data-stu-id="422fe-162">Default Marshaling Behavior</span></span>](default-marshaling-behavior.md)
- <span data-ttu-id="422fe-163">[Směrové atributy](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/77e6taeh(v=vs.100))</span><span class="sxs-lookup"><span data-stu-id="422fe-163">[Directional Attributes](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/77e6taeh(v=vs.100))</span></span>
- [<span data-ttu-id="422fe-164">Zařazování spolupráce</span><span class="sxs-lookup"><span data-stu-id="422fe-164">Interop Marshaling</span></span>](interop-marshaling.md)
