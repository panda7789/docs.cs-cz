---
title: "Ověřování uživatelského vstupu ve Windows Forms"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.technology:
- dotnet-winforms
ms.tgt_pltfrm: 
ms.topic: article
helpviewer_keywords:
- Windows Forms, validating user input
- validation [Windows Forms], Windows Forms user input
- user input [Windows Forms], validating in Windows Forms
- validating user input [Windows Forms], Windows Forms
ms.assetid: 4ec07681-1dee-4bf9-be5e-718f635a33a1
caps.latest.revision: 
author: dotnet-bot
ms.author: dotnetcontent
manager: wpickett
ms.workload:
- dotnet
ms.openlocfilehash: 1d0e3ec867e44c4f01b239e8e243259d7c951d96
ms.sourcegitcommit: cf22b29db780e532e1090c6e755aa52d28273fa6
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 02/01/2018
---
# <a name="user-input-validation-in-windows-forms"></a><span data-ttu-id="cb4ad-102">Ověřování uživatelského vstupu ve Windows Forms</span><span class="sxs-lookup"><span data-stu-id="cb4ad-102">User Input Validation in Windows Forms</span></span>
<span data-ttu-id="cb4ad-103">Při zadávání dat do aplikace, můžete ověřit platnost data předtím, než je vaše aplikace používá.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-103">When users enter data into your application, you may want to verify that the data is valid before your application uses it.</span></span> <span data-ttu-id="cb4ad-104">Může vyžadovat určité textová pole nesmí být nulová délka, naformátovat pole jako telefonní číslo nebo jiný typ dat ve správném formátu, nebo že řetězec neobsahuje žádné nebezpečné znaky, které by mohly být použity k ohrožení zabezpečení databáze.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-104">You may require that certain text fields not be zero-length, that a field be formatted as a telephone number or other type of well-formed data, or that a string not contain any unsafe characters that could be used to compromise the security of a database.</span></span> <span data-ttu-id="cb4ad-105">Windows Forms poskytuje několik způsobů ověření vstupu ve vaší aplikaci.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-105">Windows Forms provides several ways for you to validate input in your application.</span></span>  
  
## <a name="validation-with-the-maskedtextbox-control"></a><span data-ttu-id="cb4ad-106">Ověření s ovládacím prvkem MaskedTextBox</span><span class="sxs-lookup"><span data-stu-id="cb4ad-106">Validation with the MaskedTextBox Control</span></span>  
 <span data-ttu-id="cb4ad-107">Pokud potřebujete po uživatelích vyžadovat zadání data v dobře definovaný formátu, například telefonní číslo nebo číslo část toho lze dosáhnout rychle a s minimálními kódu pomocí <xref:System.Windows.Forms.MaskedTextBox> ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-107">If you need to require users to enter data in a well-defined format, such as a telephone number or a part number, you can accomplish this quickly and with minimal code by using the <xref:System.Windows.Forms.MaskedTextBox> control.</span></span> <span data-ttu-id="cb4ad-108">A *maska* je řetězec znaky z maskování jazyk, který určuje, které znaky, si můžete zaznamenat na dané pozici v textovém poli.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-108">A *mask* is a string made up of characters from a masking language that specifies which characters can be entered at any given position in the text box.</span></span> <span data-ttu-id="cb4ad-109">Tento ovládací prvek zobrazí sadu výzvy pro uživatele.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-109">The control displays a set of prompts to the user.</span></span> <span data-ttu-id="cb4ad-110">Pokud uživatel zadá nesprávnou položku, například uživatel zadá písmeno, pokud je potřeba číslici, ovládacího prvku automaticky odmítnou vstup.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-110">If the user types an incorrect entry, for example, the user types a letter when a digit is required, the control will automatically reject the input.</span></span>  
  
 <span data-ttu-id="cb4ad-111">Maskování jazyk, který je používán <xref:System.Windows.Forms.MaskedTextBox> je velmi flexibilní.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-111">The masking language that is used by <xref:System.Windows.Forms.MaskedTextBox> is very flexible.</span></span> <span data-ttu-id="cb4ad-112">Umožňuje vám zadat znaků, volitelné znaků, literálů, pomlčky a kulaté závorky, měny znaků a datum oddělovačů.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-112">It allows you to specify required characters, optional characters, literal characters, such as hyphens and parentheses, currency characters, and date separators.</span></span> <span data-ttu-id="cb4ad-113">Ovládací prvek také funguje i při vázání ke zdroji dat.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-113">The control also works well when bound to a data source.</span></span> <span data-ttu-id="cb4ad-114"><xref:System.Windows.Forms.Binding.Format> Události u vazby dat slouží k opakovanému formátování příchozích dat pro dosažení souladu s maskou a <xref:System.Windows.Forms.Binding.Parse> událostí slouží k opakovanému formátování odchozích dat k zajištění souladu se specifikacemi pole data.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-114">The <xref:System.Windows.Forms.Binding.Format> event on a data binding can be used to reformat incoming data to comply with the mask, and the <xref:System.Windows.Forms.Binding.Parse> event can be used to reformat outgoing data to comply with the specifications of the data field.</span></span>  
  
 <span data-ttu-id="cb4ad-115">Další informace najdete v tématu [MaskedTextBox – ovládací prvek](../../../docs/framework/winforms/controls/maskedtextbox-control-windows-forms.md).</span><span class="sxs-lookup"><span data-stu-id="cb4ad-115">For more information, see [MaskedTextBox Control](../../../docs/framework/winforms/controls/maskedtextbox-control-windows-forms.md).</span></span>  
  
## <a name="event-driven-validation"></a><span data-ttu-id="cb4ad-116">Ověřování založeného na událostech</span><span class="sxs-lookup"><span data-stu-id="cb4ad-116">Event-Driven Validation</span></span>  
 <span data-ttu-id="cb4ad-117">Chcete-li úplné programovou kontrolu nad ověření, nebo třeba provést složité ověřovacích kontrol, měli byste použít události ověření součástí většina ovládacích prvků Windows Forms.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-117">If you want full programmatic control over validation, or need to perform complex validation checks, you should use the validation events built into most Windows Forms controls.</span></span> <span data-ttu-id="cb4ad-118">Má každý ovládací prvek, který přijímá vlastní uživatelský vstup <xref:System.Windows.Forms.Control.Validating> událost, která dojde vždy, když ověření dat vyžaduje ovládací prvek.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-118">Each control that accepts free-form user input has a <xref:System.Windows.Forms.Control.Validating> event that will occur whenever the control requires data validation.</span></span> <span data-ttu-id="cb4ad-119">V <xref:System.Windows.Forms.Control.Validating> metoda zpracování událostí, můžete ověřit uživatelský vstup několika způsoby.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-119">In the <xref:System.Windows.Forms.Control.Validating> event-handling method, you can validate user input in several ways.</span></span> <span data-ttu-id="cb4ad-120">Pokud máte textové pole, které musí obsahovat poštovní směrovací číslo, je třeba provést ověření následujícími způsoby:</span><span class="sxs-lookup"><span data-stu-id="cb4ad-120">For example, if you have a text box that must contain a postal code, you can perform the validation in the following ways:</span></span>  
  
-   <span data-ttu-id="cb4ad-121">Pokud poštovnímu směrovacímu číslu musí patřit do určité skupiny PSČ, můžete provést porovnání řetězců na vstup ověřit data zadaná uživatelem.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-121">If the postal code must belong to a specific group of zip codes, you can perform a string comparison on the input to validate the data entered by the user.</span></span> <span data-ttu-id="cb4ad-122">Například pokud v sadě {10001, 10002, 10003} musí být zadané PSČ, pak můžete porovnání řetězců ověřit data.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-122">For example, if the postal code must be in the set {10001, 10002, 10003}, then you can use a string comparison to validate the data.</span></span>  
  
-   <span data-ttu-id="cb4ad-123">Pokud poštovní směrovací číslo musí být v konkrétní formuláře můžete ověřit data zadaná uživatelem regulární výrazy.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-123">If the postal code must be in a specific form you can use regular expressions to validate the data entered by the user.</span></span> <span data-ttu-id="cb4ad-124">Například pro ověření formátu `#####` nebo `#####-####`, můžete použít s regulárním výrazem `^(\d{5})(-\d{4})?$`.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-124">For example, to validate the form `#####` or `#####-####`, you can use the regular expression `^(\d{5})(-\d{4})?$`.</span></span> <span data-ttu-id="cb4ad-125">Ověření formuláře `A#A #A#`, můžete použít s regulárním výrazem `[A-Z]\d[A-Z] \d[A-Z]\d`.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-125">To validate the form `A#A #A#`, you can use the regular expression `[A-Z]\d[A-Z] \d[A-Z]\d`.</span></span> <span data-ttu-id="cb4ad-126">Další informace o regulárních výrazech najdete v tématu [regulární výrazy rozhraní .NET Framework](../../../docs/standard/base-types/regular-expressions.md) a [Příklady regulárních výrazů](../../../docs/standard/base-types/regular-expression-examples.md).</span><span class="sxs-lookup"><span data-stu-id="cb4ad-126">For more information about regular expressions, see [.NET Framework Regular Expressions](../../../docs/standard/base-types/regular-expressions.md) and [Regular Expression Examples](../../../docs/standard/base-types/regular-expression-examples.md).</span></span>  
  
-   <span data-ttu-id="cb4ad-127">Pokud zadané PSČ musí být platné PSČ USA, může volání PSČ webové služby ověřit data zadaná uživatelem.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-127">If the postal code must be a valid United States Zip code, you could call a Zip code Web service to validate the data entered by the user.</span></span>  
  
 <span data-ttu-id="cb4ad-128"><xref:System.Windows.Forms.Control.Validating> Událostí je zadaný objekt typu <xref:System.ComponentModel.CancelEventArgs>.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-128">The <xref:System.Windows.Forms.Control.Validating> event is supplied an object of type <xref:System.ComponentModel.CancelEventArgs>.</span></span> <span data-ttu-id="cb4ad-129">Pokud zjistíte, že data ovládacího prvku není platný, můžete zrušit <xref:System.Windows.Forms.Control.Validating> událostí nastavením tohoto objektu <xref:System.ComponentModel.CancelEventArgs.Cancel%2A> vlastnost `true`.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-129">If you determine that the control's data is not valid, you can cancel the <xref:System.Windows.Forms.Control.Validating> event by setting this object's <xref:System.ComponentModel.CancelEventArgs.Cancel%2A> property to `true`.</span></span> <span data-ttu-id="cb4ad-130">Pokud není nastaveno <xref:System.ComponentModel.CancelEventArgs.Cancel%2A> vlastnost Windows Forms se předpokládá, že se ověřování bylo úspěšné pro ovládací prvek a vygenerovat <xref:System.Windows.Forms.Control.Validated> událostí.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-130">If you do not set the <xref:System.ComponentModel.CancelEventArgs.Cancel%2A> property, Windows Forms will assume that validation succeeded for that control, and raise the <xref:System.Windows.Forms.Control.Validated> event.</span></span>  
  
 <span data-ttu-id="cb4ad-131">Příklad kódu, který ověří e-mailovou adresu v <xref:System.Windows.Controls.TextBox>, najdete v části <xref:System.Windows.Forms.Control.Validating>.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-131">For a code example that validates an email address in a <xref:System.Windows.Controls.TextBox>, see <xref:System.Windows.Forms.Control.Validating>.</span></span>  
  
### <a name="data-binding-and-event-driven-validation"></a><span data-ttu-id="cb4ad-132">Datové vazby a ověřování založeného na událostech</span><span class="sxs-lookup"><span data-stu-id="cb4ad-132">Data Binding and Event-Driven Validation</span></span>  
 <span data-ttu-id="cb4ad-133">Ověření je velmi užitečné, když je ovládací prvek vázán vaše ovládací prvky ke zdroji dat, jako je například databázové tabulky.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-133">Validation is very useful when you have bound your controls to a data source, such as a database table.</span></span> <span data-ttu-id="cb4ad-134">Pomocí ověřování, můžete zkontrolovat, může nebezpečné dat ovládacího prvku splňuje formátu potřebném zdroj dat, a že není obsahovat žádné speciální znaky, jako je například uvozovek a zpět lomítka.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-134">By using validation, you can make sure that your control's data satisfies the format required by the data source, and that it does not contain any special characters such as quotation marks and back slashes that might be unsafe.</span></span>  
  
 <span data-ttu-id="cb4ad-135">Při použití datové vazby dat ve vašem ovládacím prvku je synchronizován se zdrojem dat během provádění <xref:System.Windows.Forms.Control.Validating> událostí.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-135">When you use data binding, the data in your control is synchronized with the data source during execution of the <xref:System.Windows.Forms.Control.Validating> event.</span></span> <span data-ttu-id="cb4ad-136">Pokud zrušíte <xref:System.Windows.Forms.Control.Validating> událostí, data nebudou synchronizovat se zdrojem dat.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-136">If you cancel the <xref:System.Windows.Forms.Control.Validating> event, the data will not be synchronized with the data source.</span></span>  
  
> [!IMPORTANT]
>  <span data-ttu-id="cb4ad-137">Pokud máte vlastní ověření, který probíhá po <xref:System.Windows.Forms.Control.Validating> událostí, nebude to mít vliv datové vazby.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-137">If you have custom validation that takes place after the <xref:System.Windows.Forms.Control.Validating> event, it will not affect the data binding.</span></span> <span data-ttu-id="cb4ad-138">Například, pokud máte kódu <xref:System.Windows.Forms.Control.Validated> událost, která se pokusí zrušit datové vazby, datové vazby přesto dojde.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-138">For example, if you have code in a <xref:System.Windows.Forms.Control.Validated> event that attempts to cancel the data binding, the data binding will still occur.</span></span> <span data-ttu-id="cb4ad-139">V tomto případě k provedení ověření v <xref:System.Windows.Forms.Control.Validated> událostí, změňte ovládacího prvku **režim aktualizace zdroje dat** vlastnost (**pod (datové vazby)**\\**(rozšířené)** ) z **OnValidation** k **nikdy**a přidejte *řízení*`.DataBindings["`*\<YOURFIELD >*  `"].WriteValue()` ověření kódu.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-139">In this case, to perform validation in the <xref:System.Windows.Forms.Control.Validated> event, change the control's **Data Source Update Mode** property (**under (Databindings)**\\**(Advanced)**) from **OnValidation** to **Never**, and add *Control*`.DataBindings["`*\<YOURFIELD>*`"].WriteValue()` to your validation code.</span></span>  
  
### <a name="implicit-and-explicit-validation"></a><span data-ttu-id="cb4ad-140">Implicitní a explicitní ověření</span><span class="sxs-lookup"><span data-stu-id="cb4ad-140">Implicit and Explicit Validation</span></span>  
 <span data-ttu-id="cb4ad-141">Proto když data ovládacího prvku získat ověřit?</span><span class="sxs-lookup"><span data-stu-id="cb4ad-141">So when does a control's data get validated?</span></span> <span data-ttu-id="cb4ad-142">Toto je až vás jako na vývojáři.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-142">This is up to you, the developer.</span></span> <span data-ttu-id="cb4ad-143">Můžete použít implicitním nebo explicitním ověření, v závislosti na potřebách vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-143">You can use either implicit or explicit validation, depending on the needs of your application.</span></span>  
  
#### <a name="implicit-validation"></a><span data-ttu-id="cb4ad-144">Implicitní ověření</span><span class="sxs-lookup"><span data-stu-id="cb4ad-144">Implicit Validation</span></span>  
 <span data-ttu-id="cb4ad-145">Implicitní ověření přístupu ověří data, protože uživatel zadá ho.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-145">The implicit validation approach validates data as the user enters it.</span></span> <span data-ttu-id="cb4ad-146">Data můžete ověřit, jak dat je zadán v ovládacím prvku načtením klíče, jako jsou v stisknutí tlačítka nebo více běžně vždy, když uživatel provede zaměření pro vstup od jeden ovládací prvek a přesune na další.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-146">You can validate the data as the data is entered in a control by reading the keys as they are pressed, or more commonly whenever the user takes the input focus away from one control and moves to the next.</span></span> <span data-ttu-id="cb4ad-147">Tento přístup je užitečné, když chcete udělit uživatelské okamžitou zpětnou vazbu o datech, jak pracují.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-147">This approach is useful when you want to give the user immediate feedback about the data as they are working.</span></span>  
  
 <span data-ttu-id="cb4ad-148">Pokud chcete použít implicitní ověření pro ovládací prvek, je nutné nastavit tuto kontrolu <xref:System.Windows.Forms.ContainerControl.AutoValidate%2A> vlastnost `true`.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-148">If you want to use implicit validation for a control, you must set that control's <xref:System.Windows.Forms.ContainerControl.AutoValidate%2A> property to `true`.</span></span> <span data-ttu-id="cb4ad-149">Pokud zrušíte <xref:System.Windows.Forms.Control.Validating> událostí, chování ovládacího prvku se určí na základě co hodnoty, které jste přiřadili <xref:System.Windows.Forms.ContainerControl.AutoValidate%2A>.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-149">If you cancel the <xref:System.Windows.Forms.Control.Validating> event, the behavior of the control will be determined by what value that you assigned to <xref:System.Windows.Forms.ContainerControl.AutoValidate%2A>.</span></span> <span data-ttu-id="cb4ad-150">Pokud jste přiřadili <xref:System.Windows.Forms.AutoValidate.EnablePreventFocusChange>, zrušení událost způsobí, že <xref:System.Windows.Forms.Control.Validated> není na dojde k události.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-150">If you assigned <xref:System.Windows.Forms.AutoValidate.EnablePreventFocusChange>, canceling the event will cause the <xref:System.Windows.Forms.Control.Validated> event not to occur.</span></span> <span data-ttu-id="cb4ad-151">Zaměření pro vstup zůstane na aktuální ovládací prvek, dokud uživatel nezmění data na platnou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-151">Input focus will remain on the current control until the user changes the data to a valid input.</span></span> <span data-ttu-id="cb4ad-152">Pokud jste přiřadili <xref:System.Windows.Forms.AutoValidate.EnableAllowFocusChange>, <xref:System.Windows.Forms.Control.Validated> událostí nedojde, pokud zrušíte událost, ale přesto změní fokus na další ovládací prvek.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-152">If you assigned <xref:System.Windows.Forms.AutoValidate.EnableAllowFocusChange>, the <xref:System.Windows.Forms.Control.Validated> event will not occur when you cancel the event, but focus will still change to the next control.</span></span>  
  
 <span data-ttu-id="cb4ad-153">Přiřazení <xref:System.Windows.Forms.AutoValidate.Disable> k <xref:System.Windows.Forms.ContainerControl.AutoValidate%2A> vlastnost zabraňuje implicitní ověření úplně.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-153">Assigning <xref:System.Windows.Forms.AutoValidate.Disable> to the <xref:System.Windows.Forms.ContainerControl.AutoValidate%2A> property prevents implicit validation altogether.</span></span> <span data-ttu-id="cb4ad-154">Chcete-li ověřit vaše ovládací prvky, budete muset použít explicitní ověření.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-154">To validate your controls, you will have to use explicit validation.</span></span>  
  
#### <a name="explicit-validation"></a><span data-ttu-id="cb4ad-155">Explicitní ověření</span><span class="sxs-lookup"><span data-stu-id="cb4ad-155">Explicit Validation</span></span>  
 <span data-ttu-id="cb4ad-156">Explicitní ověření přístupu ověří data najednou.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-156">The explicit validation approach validates data at one time.</span></span> <span data-ttu-id="cb4ad-157">Můžete ověřit data v reakci na akci uživatele, jako je například kliknutí na tlačítko Uložit nebo odkaz Další.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-157">You can validate the data in response to a user action, such as clicking a Save button or a Next link.</span></span> <span data-ttu-id="cb4ad-158">Když dojde k akci uživatele, můžete aktivovat explicitní ověření v jednom z následujících způsobů:</span><span class="sxs-lookup"><span data-stu-id="cb4ad-158">When the user action occurs, you can trigger explicit validation in one of the following ways:</span></span>  
  
-   <span data-ttu-id="cb4ad-159">Volání <xref:System.Windows.Forms.ContainerControl.Validate%2A> ověření ovládacího prvku poslední ztratil fokus.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-159">Call <xref:System.Windows.Forms.ContainerControl.Validate%2A> to validate the last control to have lost focus.</span></span>  
  
-   <span data-ttu-id="cb4ad-160">Volání <xref:System.Windows.Forms.ContainerControl.ValidateChildren%2A> ověření všech podřízených ovládacích prvků v ovládacím prvku formuláře nebo kontejneru.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-160">Call <xref:System.Windows.Forms.ContainerControl.ValidateChildren%2A> to validate all child controls in a form or container control.</span></span>  
  
-   <span data-ttu-id="cb4ad-161">Volání vlastní metody ověřit data v ovládacích prvcích ručně.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-161">Call a custom method to validate the data in the controls manually.</span></span>  
  
#### <a name="default-implicit-validation-behavior-for-windows-forms-controls"></a><span data-ttu-id="cb4ad-162">Výchozí chování implicitní ověřování pro Windows Forms – ovládací prvky</span><span class="sxs-lookup"><span data-stu-id="cb4ad-162">Default Implicit Validation Behavior for Windows Forms Controls</span></span>  
 <span data-ttu-id="cb4ad-163">Různé ovládací prvky Windows Forms mít různé výchozí hodnoty pro jejich <xref:System.Windows.Forms.ContainerControl.AutoValidate%2A> vlastnost.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-163">Different Windows Forms controls have different defaults for their <xref:System.Windows.Forms.ContainerControl.AutoValidate%2A> property.</span></span> <span data-ttu-id="cb4ad-164">Následující tabulka uvádí nejběžnější ovládací prvky a jejich výchozí hodnoty.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-164">The following table shows the most common controls and their defaults.</span></span>  
  
|<span data-ttu-id="cb4ad-165">Ovládací prvek</span><span class="sxs-lookup"><span data-stu-id="cb4ad-165">Control</span></span>|<span data-ttu-id="cb4ad-166">Výchozí chování při ověřování</span><span class="sxs-lookup"><span data-stu-id="cb4ad-166">Default Validation Behavior</span></span>|  
|-------------|---------------------------------|  
|<xref:System.Windows.Forms.ContainerControl>|<xref:System.Windows.Forms.AutoValidate.Inherit>|  
|<xref:System.Windows.Forms.Form>|<xref:System.Windows.Forms.AutoValidate.EnableAllowFocusChange>|  
|<xref:System.Windows.Forms.PropertyGrid>|<span data-ttu-id="cb4ad-167">Vlastnosti nejsou viditelné v sadě Visual Studio</span><span class="sxs-lookup"><span data-stu-id="cb4ad-167">Property not exposed in Visual Studio</span></span>|  
|<xref:System.Windows.Forms.ToolStripContainer>|<span data-ttu-id="cb4ad-168">Vlastnosti nejsou viditelné v sadě Visual Studio</span><span class="sxs-lookup"><span data-stu-id="cb4ad-168">Property not exposed in Visual Studio</span></span>|  
|<xref:System.Windows.Forms.SplitContainer>|<xref:System.Windows.Forms.AutoValidate.Inherit>|  
|<xref:System.Windows.Forms.UserControl>|<xref:System.Windows.Forms.AutoValidate.EnableAllowFocusChange>|  
  
## <a name="closing-the-form-and-overriding-validation"></a><span data-ttu-id="cb4ad-169">Zavřením formuláře a přepsáním ověření</span><span class="sxs-lookup"><span data-stu-id="cb4ad-169">Closing the Form and Overriding Validation</span></span>  
 <span data-ttu-id="cb4ad-170">Když ovládacího prvku udržuje zaměřuje, protože data, která obsahuje je neplatná, není možné zavřít formulář nadřazené obvyklé způsoby:</span><span class="sxs-lookup"><span data-stu-id="cb4ad-170">When a control maintains focus because the data it contains is invalid, it is impossible to close the parent form in one of the usual ways:</span></span>  
  
-   <span data-ttu-id="cb4ad-171">Kliknutím **Zavřít** tlačítko.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-171">By clicking the **Close** button.</span></span>  
  
-   <span data-ttu-id="cb4ad-172">Výběrem **Zavřít** v **systému** nabídky.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-172">By selecting **Close** in the **System** menu.</span></span>  
  
-   <span data-ttu-id="cb4ad-173">Při volání <xref:System.Windows.Forms.Form.Close%2A> metoda prostřednictvím kódu programu.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-173">By calling the <xref:System.Windows.Forms.Form.Close%2A> method programmatically.</span></span>  
  
 <span data-ttu-id="cb4ad-174">Ale v některých případech můžete chtít mohl uživatel zavřete formulář bez ohledu na to, jestli jsou platné hodnoty v ovládacích prvcích.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-174">However, in some cases, you might want to let the user close the form regardless of whether the values in the controls are valid.</span></span> <span data-ttu-id="cb4ad-175">Můžete přepsat ověření a zavřete formulář, který ještě obsahuje neplatná data tak, že vytvoříte obslužnou rutinu pro daný formulář <xref:System.Windows.Forms.Form.Closing> události.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-175">You can override validation and close a form that still contains invalid data by creating a handler for the form's <xref:System.Windows.Forms.Form.Closing> event.</span></span> <span data-ttu-id="cb4ad-176">V události, nastavte <xref:System.ComponentModel.CancelEventArgs.Cancel%2A> vlastnost `false`.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-176">In the event, set the <xref:System.ComponentModel.CancelEventArgs.Cancel%2A> property to `false`.</span></span> <span data-ttu-id="cb4ad-177">Vynutí se tak formulář zavřete.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-177">This forces the form to close.</span></span> <span data-ttu-id="cb4ad-178">Další informace a příklady naleznete v tématu <xref:System.Windows.Forms.Form.Closing?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-178">For more information and an example, see <xref:System.Windows.Forms.Form.Closing?displayProperty=nameWithType>.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="cb4ad-179">Pokud vynutíte formulář zavřete tímto způsobem, dojde ke ztrátě dat v ovládacích prvcích formuláře, který ještě nebyl uložen.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-179">If you force the form to close in this manner, any data in the form's controls that has not already been saved is lost.</span></span> <span data-ttu-id="cb4ad-180">Kromě toho modální forms neověřují obsah ovládacích prvků, když jsou uzavřeny.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-180">In addition, modal forms do not validate the contents of controls when they are closed.</span></span> <span data-ttu-id="cb4ad-181">Můžete použít ovládací prvek ověření přesto k uzamčení fokus na ovládací prvek, ale nemáte starat o chování přidružené zavřením formuláře.</span><span class="sxs-lookup"><span data-stu-id="cb4ad-181">You can still use control validation to lock focus to a control, but you do not have to be concerned about the behavior associated with closing the form.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="cb4ad-182">Viz také</span><span class="sxs-lookup"><span data-stu-id="cb4ad-182">See Also</span></span>  
 <xref:System.Windows.Forms.Control.Validating?displayProperty=nameWithType>  
 <xref:System.Windows.Forms.Form.Closing?displayProperty=nameWithType>  
 <xref:System.ComponentModel.CancelEventArgs?displayProperty=nameWithType>  
 [<span data-ttu-id="cb4ad-183">Ovládací prvek MaskedTextBox</span><span class="sxs-lookup"><span data-stu-id="cb4ad-183">MaskedTextBox Control</span></span>](../../../docs/framework/winforms/controls/maskedtextbox-control-windows-forms.md)  
 [<span data-ttu-id="cb4ad-184">Příklady regulárních výrazů</span><span class="sxs-lookup"><span data-stu-id="cb4ad-184">Regular Expression Examples</span></span>](../../../docs/standard/base-types/regular-expression-examples.md)
