---
title: Doporučené postupy pro načtení sestavení
ms.date: 03/30/2017
helpviewer_keywords:
- assemblies,binding
- LoadFrom method
- default load context
- TypeLoadException class,causes
- assembly loading errors
- load contexts
- assemblies,loading
- LoadWithPartialName method
- load-from context
ms.assetid: 68d1c539-6a47-4614-ab59-4b071c9d4b4c
ms.openlocfilehash: 7575c40edf47e977335bcc34fcd9e49debab0980
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/15/2020
ms.locfileid: "79181705"
---
# <a name="best-practices-for-assembly-loading"></a><span data-ttu-id="23e52-102">Doporučené postupy pro načtení sestavení</span><span class="sxs-lookup"><span data-stu-id="23e52-102">Best Practices for Assembly Loading</span></span>
<span data-ttu-id="23e52-103">Tento článek popisuje způsoby, jak se vyhnout <xref:System.InvalidCastException>problémům <xref:System.MissingMethodException>s identitou typu, které mohou vést k , a další chyby.</span><span class="sxs-lookup"><span data-stu-id="23e52-103">This article discusses ways to avoid problems of type identity that can lead to <xref:System.InvalidCastException>, <xref:System.MissingMethodException>, and other errors.</span></span> <span data-ttu-id="23e52-104">Článek popisuje následující doporučení:</span><span class="sxs-lookup"><span data-stu-id="23e52-104">The article discusses the following recommendations:</span></span>  
  
- [<span data-ttu-id="23e52-105">Pochopte výhody a nevýhody kontextů zatížení</span><span class="sxs-lookup"><span data-stu-id="23e52-105">Understand the advantages and disadvantages of load contexts</span></span>](#load_contexts)  
  
- [<span data-ttu-id="23e52-106">Vyhněte se vazbě na názvy částečných sestavení</span><span class="sxs-lookup"><span data-stu-id="23e52-106">Avoid binding on partial assembly names</span></span>](#avoid_partial_names)  
  
- [<span data-ttu-id="23e52-107">Vyhněte se načítání sestavy do více kontextů</span><span class="sxs-lookup"><span data-stu-id="23e52-107">Avoid loading an assembly into multiple contexts</span></span>](#avoid_loading_into_multiple_contexts)  
  
- [<span data-ttu-id="23e52-108">Vyhněte se načítání více verzí sestavení do stejného kontextu</span><span class="sxs-lookup"><span data-stu-id="23e52-108">Avoid loading multiple versions of an assembly into the same context</span></span>](#avoid_loading_multiple_versions)  
  
- [<span data-ttu-id="23e52-109">Zvažte přepnutí na výchozí kontext zatížení</span><span class="sxs-lookup"><span data-stu-id="23e52-109">Consider switching to the default load context</span></span>](#switch_to_default)  
  
 <span data-ttu-id="23e52-110">První doporučení, [pochopit výhody a nevýhody zatížení kontexty](#load_contexts), poskytuje základní informace pro další doporučení, protože všechny závisí na znalosti zatížení kontexty.</span><span class="sxs-lookup"><span data-stu-id="23e52-110">The first recommendation, [understand the advantages and disadvantages of load contexts](#load_contexts), provides background information for the other recommendations, because they all depend on a knowledge of load contexts.</span></span>  
  
<a name="load_contexts"></a>
## <a name="understand-the-advantages-and-disadvantages-of-load-contexts"></a><span data-ttu-id="23e52-111">Seznamte se s výhodami a nevýhodami kontextů zatížení</span><span class="sxs-lookup"><span data-stu-id="23e52-111">Understand the Advantages and Disadvantages of Load Contexts</span></span>  
 <span data-ttu-id="23e52-112">V rámci domény aplikace lze sestavení načíst do jednoho ze tří kontextů nebo je lze načíst bez kontextu:</span><span class="sxs-lookup"><span data-stu-id="23e52-112">Within an application domain, assemblies can be loaded into one of three contexts, or they can be loaded without context:</span></span>  
  
- <span data-ttu-id="23e52-113">Výchozí kontext zatížení obsahuje sestavení nalezená zjišťováním globální mezipaměti sestavení, úložiště sestavení hostitele, pokud je modul <xref:System.AppDomainSetup.ApplicationBase%2A> runtime hostován (například na serveru SQL Server), a doménu aplikace a a. <xref:System.AppDomainSetup.PrivateBinPath%2A></span><span class="sxs-lookup"><span data-stu-id="23e52-113">The default load context contains assemblies found by probing the global assembly cache, the host assembly store if the runtime is hosted (for example, in SQL Server), and the <xref:System.AppDomainSetup.ApplicationBase%2A> and <xref:System.AppDomainSetup.PrivateBinPath%2A> of the application domain.</span></span> <span data-ttu-id="23e52-114">Většina přetížení <xref:System.Reflection.Assembly.Load%2A> sestavení zatížení metody do tohoto kontextu.</span><span class="sxs-lookup"><span data-stu-id="23e52-114">Most overloads of the <xref:System.Reflection.Assembly.Load%2A> method load assemblies into this context.</span></span>  
  
- <span data-ttu-id="23e52-115">Kontext load-from obsahuje sestavení, která jsou načtena z umístění, která nejsou prohledána zavaděčem.</span><span class="sxs-lookup"><span data-stu-id="23e52-115">The load-from context contains assemblies that are loaded from locations that are not searched by the loader.</span></span> <span data-ttu-id="23e52-116">Doplňky mohou být například nainstalovány v adresáři, který není v cestě aplikace.</span><span class="sxs-lookup"><span data-stu-id="23e52-116">For example, add-ins might be installed in a directory that is not under the application path.</span></span> <span data-ttu-id="23e52-117"><xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>, <xref:System.AppDomain.CreateInstanceFrom%2A?displayProperty=nameWithType>a <xref:System.AppDomain.ExecuteAssembly%2A?displayProperty=nameWithType> jsou příklady metod, které se načítají podle cesty.</span><span class="sxs-lookup"><span data-stu-id="23e52-117"><xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>, <xref:System.AppDomain.CreateInstanceFrom%2A?displayProperty=nameWithType>, and <xref:System.AppDomain.ExecuteAssembly%2A?displayProperty=nameWithType> are examples of methods that load by path.</span></span>  
  
- <span data-ttu-id="23e52-118">Pouze reflexe kontextu obsahuje sestavení <xref:System.Reflection.Assembly.ReflectionOnlyLoad%2A> naloženo s a <xref:System.Reflection.Assembly.ReflectionOnlyLoadFrom%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="23e52-118">The reflection-only context contains assemblies loaded with the <xref:System.Reflection.Assembly.ReflectionOnlyLoad%2A> and <xref:System.Reflection.Assembly.ReflectionOnlyLoadFrom%2A> methods.</span></span> <span data-ttu-id="23e52-119">Kód v tomto kontextu nelze spustit, takže není popsánzde.</span><span class="sxs-lookup"><span data-stu-id="23e52-119">Code in this context cannot be executed, so it is not discussed here.</span></span> <span data-ttu-id="23e52-120">Další informace naleznete v [tématu How to: Load Assemblies into the Reflection-Only Context](../reflection-and-codedom/how-to-load-assemblies-into-the-reflection-only-context.md).</span><span class="sxs-lookup"><span data-stu-id="23e52-120">For more information, see [How to: Load Assemblies into the Reflection-Only Context](../reflection-and-codedom/how-to-load-assemblies-into-the-reflection-only-context.md).</span></span>  
  
- <span data-ttu-id="23e52-121">Pokud jste vygenerovali přechodné dynamické sestavení pomocí odrazu vyzařovat, sestavení není v žádném kontextu.</span><span class="sxs-lookup"><span data-stu-id="23e52-121">If you generated a transient dynamic assembly by using reflection emit, the assembly is not in any context.</span></span> <span data-ttu-id="23e52-122">Kromě toho většina sestavení, které <xref:System.Reflection.Assembly.LoadFile%2A> jsou načteny pomocí metody jsou načteny bez kontextu a sestavení, které jsou načteny z bajtových polí jsou načteny bez kontextu, pokud jejich identita (po použití zásady) zjistí, že jsou v globální mezipaměti sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-122">In addition, most assemblies that are loaded by using the <xref:System.Reflection.Assembly.LoadFile%2A> method are loaded without context, and assemblies that are loaded from byte arrays are loaded without context unless their identity (after policy is applied) establishes that they are in the global assembly cache.</span></span>  
  
 <span data-ttu-id="23e52-123">Spuštění kontexty mají výhody a nevýhody, jak je popsáno v následujících částech.</span><span class="sxs-lookup"><span data-stu-id="23e52-123">The execution contexts have advantages and disadvantages, as discussed in the following sections.</span></span>  
  
### <a name="default-load-context"></a><span data-ttu-id="23e52-124">Výchozí kontext načítání</span><span class="sxs-lookup"><span data-stu-id="23e52-124">Default Load Context</span></span>  
 <span data-ttu-id="23e52-125">Při načtení sestavení do výchozího kontextu zatížení jsou jejich závislosti načteny automaticky.</span><span class="sxs-lookup"><span data-stu-id="23e52-125">When assemblies are loaded into the default load context, their dependencies are loaded automatically.</span></span> <span data-ttu-id="23e52-126">Závislosti, které jsou načteny do výchozího kontextu načítání, jsou nalezeny automaticky pro sestavení ve výchozím kontextu zatížení nebo kontextu zatížení z.</span><span class="sxs-lookup"><span data-stu-id="23e52-126">Dependencies that are loaded into the default load context are found automatically for assemblies in the default load context or the load-from context.</span></span> <span data-ttu-id="23e52-127">Načítání identitou sestavení zvyšuje stabilitu aplikací tím, že zajišťuje, že se nepoužívají neznámé verze sestavení (viz část [Vyhněte se vazbě na částečné názvy sestavení).](#avoid_partial_names)</span><span class="sxs-lookup"><span data-stu-id="23e52-127">Loading by assembly identity increases the stability of applications by ensuring that unknown versions of assemblies are not used (see the [Avoid Binding on Partial Assembly Names](#avoid_partial_names) section).</span></span>  
  
 <span data-ttu-id="23e52-128">Použití výchozího kontextu zatížení má následující nevýhody:</span><span class="sxs-lookup"><span data-stu-id="23e52-128">Using the default load context has the following disadvantages:</span></span>  
  
- <span data-ttu-id="23e52-129">Závislosti, které jsou načteny do jiných kontextů, nejsou k dispozici.</span><span class="sxs-lookup"><span data-stu-id="23e52-129">Dependencies that are loaded into other contexts are not available.</span></span>  
  
- <span data-ttu-id="23e52-130">Sestavení z umístění mimo cestu zjišťování nelze načíst do výchozího kontextu načítání.</span><span class="sxs-lookup"><span data-stu-id="23e52-130">You cannot load assemblies from locations outside the probing path into the default load context.</span></span>  
  
### <a name="load-from-context"></a><span data-ttu-id="23e52-131">Načíst z kontextu</span><span class="sxs-lookup"><span data-stu-id="23e52-131">Load-From Context</span></span>  
 <span data-ttu-id="23e52-132">Kontext load-from umožňuje načíst sestavení z cesty, která není pod cestou aplikace, a proto není zahrnuta v zjišťování.</span><span class="sxs-lookup"><span data-stu-id="23e52-132">The load-from context lets you load an assembly from a path that is not under the application path, and therefore is not included in probing.</span></span> <span data-ttu-id="23e52-133">Umožňuje závislosti, které mají být umístěny a načteny z této cesty, protože informace o cestě je udržována kontextu.</span><span class="sxs-lookup"><span data-stu-id="23e52-133">It enables dependencies to be located and loaded from that path, because the path information is maintained by the context.</span></span> <span data-ttu-id="23e52-134">Kromě toho sestavení v tomto kontextu můžete použít závislosti, které jsou načteny do výchozího kontextu zatížení.</span><span class="sxs-lookup"><span data-stu-id="23e52-134">In addition, assemblies in this context can use dependencies that are loaded into the default load context.</span></span>  
  
 <span data-ttu-id="23e52-135">Načítání sestavení pomocí <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> metody nebo jedné z dalších metod, které se načítají cestou, má následující nevýhody:</span><span class="sxs-lookup"><span data-stu-id="23e52-135">Loading assemblies by using the <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> method, or one of the other methods that load by path, has the following disadvantages:</span></span>  
  
- <span data-ttu-id="23e52-136">Pokud je již načteno sestavení se <xref:System.Reflection.Assembly.LoadFrom%2A> stejnou identitou, vrátí načtené sestavení i v případě, že byla zadána jiná cesta.</span><span class="sxs-lookup"><span data-stu-id="23e52-136">If an assembly with the same identity is already loaded, <xref:System.Reflection.Assembly.LoadFrom%2A> returns the loaded assembly even if a different path was specified.</span></span>  
  
- <span data-ttu-id="23e52-137">Pokud je sestavení <xref:System.Reflection.Assembly.LoadFrom%2A>načteno s a později se sestavení ve výchozím kontextu zatížení pokusí načíst stejné sestavení podle zobrazovaného názvu, pokus o načtení se nezdaří.</span><span class="sxs-lookup"><span data-stu-id="23e52-137">If an assembly is loaded with <xref:System.Reflection.Assembly.LoadFrom%2A>, and later an assembly in the default load context tries to load the same assembly by display name, the load attempt fails.</span></span> <span data-ttu-id="23e52-138">Tato situace může nastat při rekonstruované sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-138">This can occur when an assembly is deserialized.</span></span>  
  
- <span data-ttu-id="23e52-139">Pokud je sestavení <xref:System.Reflection.Assembly.LoadFrom%2A>načteno s , a cesta zjišťování obsahuje sestavení se stejnou <xref:System.InvalidCastException>identitou, ale v jiném umístění, může dojít k , <xref:System.MissingMethodException>nebo jinému neočekávanému chování.</span><span class="sxs-lookup"><span data-stu-id="23e52-139">If an assembly is loaded with <xref:System.Reflection.Assembly.LoadFrom%2A>, and the probing path includes an assembly with the same identity but in a different location, an <xref:System.InvalidCastException>, <xref:System.MissingMethodException>, or other unexpected behavior can occur.</span></span>  
  
- <span data-ttu-id="23e52-140"><xref:System.Reflection.Assembly.LoadFrom%2A>požadavky <xref:System.Security.Permissions.FileIOPermissionAccess.Read?displayProperty=nameWithType> <xref:System.Security.Permissions.FileIOPermissionAccess.PathDiscovery?displayProperty=nameWithType>a <xref:System.Net.WebPermission>, nebo na zadanou cestu.</span><span class="sxs-lookup"><span data-stu-id="23e52-140"><xref:System.Reflection.Assembly.LoadFrom%2A> demands <xref:System.Security.Permissions.FileIOPermissionAccess.Read?displayProperty=nameWithType> and <xref:System.Security.Permissions.FileIOPermissionAccess.PathDiscovery?displayProperty=nameWithType>, or <xref:System.Net.WebPermission>, on the specified path.</span></span>  
  
- <span data-ttu-id="23e52-141">Pokud pro sestavení existuje nativní bitová kopie, nebude použita.</span><span class="sxs-lookup"><span data-stu-id="23e52-141">If a native image exists for the assembly, it is not used.</span></span>  
  
- <span data-ttu-id="23e52-142">Sestavení nelze načíst jako neutrální doménu.</span><span class="sxs-lookup"><span data-stu-id="23e52-142">The assembly cannot be loaded as domain-neutral.</span></span>  
  
- <span data-ttu-id="23e52-143">V rozhraní .NET Framework verze 1.0 a 1.1 zásady není použita.</span><span class="sxs-lookup"><span data-stu-id="23e52-143">In the .NET Framework versions 1.0 and 1.1, policy is not applied.</span></span>  
  
### <a name="no-context"></a><span data-ttu-id="23e52-144">Žádný kontext</span><span class="sxs-lookup"><span data-stu-id="23e52-144">No Context</span></span>  
 <span data-ttu-id="23e52-145">Načítání bez kontextu je jedinou možností pro přechodná sestavení, která jsou generována s odrazem emit.</span><span class="sxs-lookup"><span data-stu-id="23e52-145">Loading without context is the only option for transient assemblies that are generated with reflection emit.</span></span> <span data-ttu-id="23e52-146">Načítání bez kontextu je jediný způsob, jak načíst více sestavení, které mají stejnou identitu do jedné domény aplikace.</span><span class="sxs-lookup"><span data-stu-id="23e52-146">Loading without context is the only way to load multiple assemblies that have the same identity into one application domain.</span></span> <span data-ttu-id="23e52-147">Náklady na sondování je zabráněno.</span><span class="sxs-lookup"><span data-stu-id="23e52-147">The cost of probing is avoided.</span></span>  
  
 <span data-ttu-id="23e52-148">Sestavení, která jsou načtena z bajtových polí, jsou načtena bez kontextu, pokud identita sestavení, která je vytvořena při použití zásady, neodpovídá identitě sestavení v globální mezipaměti sestavení; v takovém případě je sestavení načteno z globální mezipaměti sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-148">Assemblies that are loaded from byte arrays are loaded without context unless the identity of the assembly, which is established when policy is applied, matches the identity of an assembly in the global assembly cache; in that case, the assembly is loaded from the global assembly cache.</span></span>  
  
 <span data-ttu-id="23e52-149">Načítání sestavení bez kontextu má následující nevýhody:</span><span class="sxs-lookup"><span data-stu-id="23e52-149">Loading assemblies without context has the following disadvantages:</span></span>  
  
- <span data-ttu-id="23e52-150">Jiná sestavení nelze vytvořit vazbu na sestavení, která <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> jsou načtena bez kontextu, pokud událost nezpracovat.</span><span class="sxs-lookup"><span data-stu-id="23e52-150">Other assemblies cannot bind to assemblies that are loaded without context, unless you handle the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event.</span></span>  
  
- <span data-ttu-id="23e52-151">Závislosti nejsou načteny automaticky.</span><span class="sxs-lookup"><span data-stu-id="23e52-151">Dependencies are not loaded automatically.</span></span> <span data-ttu-id="23e52-152">Můžete je předem načíst bez kontextu, předem je načíst do <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> výchozího kontextu zatížení nebo je načíst zpracováním události.</span><span class="sxs-lookup"><span data-stu-id="23e52-152">You can preload them without context, preload them into the default load context, or load them by handling the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event.</span></span>  
  
- <span data-ttu-id="23e52-153">Načítání více sestavení se stejnou identitou bez kontextu může způsobit problémy s identitou typu podobné těm, které jsou způsobeny načítáním sestavení se stejnou identitou do více kontextů.</span><span class="sxs-lookup"><span data-stu-id="23e52-153">Loading multiple assemblies with the same identity without context can cause type identity problems similar to those caused by loading assemblies with the same identity into multiple contexts.</span></span> <span data-ttu-id="23e52-154">Viz [Vyhněte se načítání sestavení do více kontextů](#avoid_loading_into_multiple_contexts).</span><span class="sxs-lookup"><span data-stu-id="23e52-154">See [Avoid Loading an Assembly into Multiple Contexts](#avoid_loading_into_multiple_contexts).</span></span>  
  
- <span data-ttu-id="23e52-155">Pokud pro sestavení existuje nativní bitová kopie, nebude použita.</span><span class="sxs-lookup"><span data-stu-id="23e52-155">If a native image exists for the assembly, it is not used.</span></span>  
  
- <span data-ttu-id="23e52-156">Sestavení nelze načíst jako neutrální doménu.</span><span class="sxs-lookup"><span data-stu-id="23e52-156">The assembly cannot be loaded as domain-neutral.</span></span>  
  
- <span data-ttu-id="23e52-157">V rozhraní .NET Framework verze 1.0 a 1.1 zásady není použita.</span><span class="sxs-lookup"><span data-stu-id="23e52-157">In the .NET Framework versions 1.0 and 1.1, policy is not applied.</span></span>  
  
<a name="avoid_partial_names"></a>
## <a name="avoid-binding-on-partial-assembly-names"></a><span data-ttu-id="23e52-158">Vyhněte se vazbě na částečné názvy sestavení</span><span class="sxs-lookup"><span data-stu-id="23e52-158">Avoid Binding on Partial Assembly Names</span></span>  
 <span data-ttu-id="23e52-159">Částečná vazba názvu nastane, když zadáte<xref:System.Reflection.Assembly.FullName%2A>pouze část zobrazovaného názvu sestavení ( ) při načtení sestavy.</span><span class="sxs-lookup"><span data-stu-id="23e52-159">Partial name binding occurs when you specify only part of the assembly display name (<xref:System.Reflection.Assembly.FullName%2A>) when you load an assembly.</span></span> <span data-ttu-id="23e52-160">Například můžete volat <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> metodu pouze s jednoduchým názvem sestavení, vynechání verze, jazykové verze a token veřejného klíče.</span><span class="sxs-lookup"><span data-stu-id="23e52-160">For example, you might call the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method with only the simple name of the assembly, omitting the version, culture, and public key token.</span></span> <span data-ttu-id="23e52-161">Nebo můžete volat <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> metodu, která <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> nejprve volá metodu a pokud se nepodaří najít sestavení, prohledá globální mezipaměť sestavení a načte nejnovější dostupnou verzi sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-161">Or you might call the <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> method, which first calls the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and, if that fails to locate the assembly, searches the global assembly cache and loads the latest available version of the assembly.</span></span>  
  
 <span data-ttu-id="23e52-162">Částečná vazba názvu může způsobit mnoho problémů, včetně následujících:</span><span class="sxs-lookup"><span data-stu-id="23e52-162">Partial name binding can cause many problems, including the following:</span></span>  
  
- <span data-ttu-id="23e52-163">Metoda <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> může načíst jiné sestavení se stejným jednoduchým názvem.</span><span class="sxs-lookup"><span data-stu-id="23e52-163">The <xref:System.Reflection.Assembly.LoadWithPartialName%2A?displayProperty=nameWithType> method might load a different assembly with the same simple name.</span></span> <span data-ttu-id="23e52-164">Například dvě aplikace mohou nainstalovat dvě zcela odlišná `GraphicsLibrary` sestavení, která mají jednoduchý název do globální mezipaměti sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-164">For example, two applications might install two completely different assemblies that both have the simple name `GraphicsLibrary` into the global assembly cache.</span></span>  
  
- <span data-ttu-id="23e52-165">Sestavení, které je skutečně načtennemusí být zpětně kompatibilní.</span><span class="sxs-lookup"><span data-stu-id="23e52-165">The assembly that is actually loaded might not be backward-compatible.</span></span> <span data-ttu-id="23e52-166">Například nezadání verze může mít za následek načítání mnohem novější verze, než byla původně napsána verze, kterou byl program původně napsán k použití.</span><span class="sxs-lookup"><span data-stu-id="23e52-166">For example, not specifying the version might result in the loading of a much later version than the version your program was originally written to use.</span></span> <span data-ttu-id="23e52-167">Změny v novější verzi může způsobit chyby v aplikaci.</span><span class="sxs-lookup"><span data-stu-id="23e52-167">Changes in the later version might cause errors in your application.</span></span>  
  
- <span data-ttu-id="23e52-168">Sestavení, které je skutečně načteno, nemusí být kompatibilní s forwardem.</span><span class="sxs-lookup"><span data-stu-id="23e52-168">The assembly that is actually loaded might not be forward-compatible.</span></span> <span data-ttu-id="23e52-169">Například jste vytvořili a otestovali aplikaci s nejnovější verzí sestavení, ale částečná vazba může načíst mnohem starší verzi, která postrádá funkce, které vaše aplikace používá.</span><span class="sxs-lookup"><span data-stu-id="23e52-169">For example, you might have built and tested your application with the latest version of an assembly, but partial binding might load a much earlier version that lacks features your application uses.</span></span>  
  
- <span data-ttu-id="23e52-170">Instalace nových aplikací může přerušit stávající aplikace.</span><span class="sxs-lookup"><span data-stu-id="23e52-170">Installing new applications can break existing applications.</span></span> <span data-ttu-id="23e52-171">Aplikace, která <xref:System.Reflection.Assembly.LoadWithPartialName%2A> používá metodu lze porušit instalací novější, nekompatibilní verze sdíleného sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-171">An application that uses the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method can be broken by installing a newer, incompatible version of a shared assembly.</span></span>  
  
- <span data-ttu-id="23e52-172">Může dojít k neočekávanému načítání závislostí.</span><span class="sxs-lookup"><span data-stu-id="23e52-172">Unexpected dependency loading can occur.</span></span> <span data-ttu-id="23e52-173">Načtete dvě sestavení, která sdílejí závislost, načítání je s částečnou vazbou může mít za následek jednu sestavu pomocí součásti, která nebyla vytvořena nebo testována s.</span><span class="sxs-lookup"><span data-stu-id="23e52-173">It you load two assemblies that share a dependency, loading them with partial binding might result in one assembly using a component that it was not built or tested with.</span></span>  
  
 <span data-ttu-id="23e52-174">Z důvodu problémů, které <xref:System.Reflection.Assembly.LoadWithPartialName%2A> může způsobit, byla metoda označena jako zastaralá.</span><span class="sxs-lookup"><span data-stu-id="23e52-174">Because of the problems it can cause, the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method has been marked obsolete.</span></span> <span data-ttu-id="23e52-175">Doporučujeme použít metodu <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> místo a zadejte úplné sestavení zobrazované názvy.</span><span class="sxs-lookup"><span data-stu-id="23e52-175">We recommend that you use the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method instead, and specify full assembly display names.</span></span> <span data-ttu-id="23e52-176">Viz [Principy výhod a nevýhod kontextů zatížení](#load_contexts) a [zvažte přepnutí na výchozí kontext načítání](#switch_to_default).</span><span class="sxs-lookup"><span data-stu-id="23e52-176">See [Understand the Advantages and Disadvantages of Load Contexts](#load_contexts) and [Consider Switching to the Default Load Context](#switch_to_default).</span></span>  
  
 <span data-ttu-id="23e52-177">Pokud chcete použít <xref:System.Reflection.Assembly.LoadWithPartialName%2A> metodu, protože usnadňuje načítání sestavení, zvažte, že s vaší aplikace nezdaří s chybovou zprávou, která identifikuje chybějící sestavení je pravděpodobné, že poskytne lepší uživatelské prostředí než automaticky pomocí neznámé verze sestavení, což může způsobit nepředvídatelné chování a bezpečnostní díry.</span><span class="sxs-lookup"><span data-stu-id="23e52-177">If you want to use the <xref:System.Reflection.Assembly.LoadWithPartialName%2A> method because it makes assembly loading easy, consider that having your application fail with an error message that identifies the missing assembly is likely to provide a better user experience than automatically using an unknown version of the assembly, which might cause unpredictable behavior and security holes.</span></span>  
  
<a name="avoid_loading_into_multiple_contexts"></a>
## <a name="avoid-loading-an-assembly-into-multiple-contexts"></a><span data-ttu-id="23e52-178">Vyhněte se načítání sestavení do více kontextů</span><span class="sxs-lookup"><span data-stu-id="23e52-178">Avoid Loading an Assembly into Multiple Contexts</span></span>  
 <span data-ttu-id="23e52-179">Načítání sestavení do více kontextů může způsobit problémy s identitou typu.</span><span class="sxs-lookup"><span data-stu-id="23e52-179">Loading an assembly into multiple contexts can cause type identity problems.</span></span> <span data-ttu-id="23e52-180">Pokud je stejný typ načten ze stejného sestavení do dvou různých kontextů, je to, jako kdyby byly načteny dva různé typy se stejným názvem.</span><span class="sxs-lookup"><span data-stu-id="23e52-180">If the same type is loaded from the same assembly into two different contexts, it is as if two different types with the same name had been loaded.</span></span> <span data-ttu-id="23e52-181">Je <xref:System.InvalidCastException> vyvolána, pokud se pokusíte přetypovat jeden typ `MyType` na druhý, `MyType`s matoucí zprávu, že typ nelze přetypovat na typ .</span><span class="sxs-lookup"><span data-stu-id="23e52-181">An <xref:System.InvalidCastException> is thrown if you try to cast one type to the other, with the confusing message that type `MyType` cannot be cast to type `MyType`.</span></span>  
  
 <span data-ttu-id="23e52-182">Předpokládejme například, `ICommunicate` že rozhraní je `Utility`deklarováno v sestavení s názvem , na které odkazuje program a také další sestavení, která program načte.</span><span class="sxs-lookup"><span data-stu-id="23e52-182">For example, suppose that the `ICommunicate` interface is declared in an assembly named `Utility`, which is referenced by your program and also by other assemblies that your program loads.</span></span> <span data-ttu-id="23e52-183">Tato další sestavení obsahují typy, které implementují `ICommunicate` rozhraní, což umožňuje programu je používat.</span><span class="sxs-lookup"><span data-stu-id="23e52-183">These other assemblies contain types that implement the `ICommunicate` interface, allowing your program to use them.</span></span>  
  
 <span data-ttu-id="23e52-184">Nyní zvažte, co se stane, když je váš program spuštěn.</span><span class="sxs-lookup"><span data-stu-id="23e52-184">Now consider what happens when your program is run.</span></span> <span data-ttu-id="23e52-185">Sestavení, na která program odkazuje, jsou načtena do výchozího kontextu načítání.</span><span class="sxs-lookup"><span data-stu-id="23e52-185">Assemblies that are referenced by your program are loaded into the default load context.</span></span> <span data-ttu-id="23e52-186">Pokud načtete cílové sestavení podle jeho <xref:System.Reflection.Assembly.Load%2A> identity pomocí metody, bude ve výchozím kontextu zatížení a tak bude jeho závislosti.</span><span class="sxs-lookup"><span data-stu-id="23e52-186">If you load a target assembly by its identity, using the <xref:System.Reflection.Assembly.Load%2A> method, it will be in the default load context, and so will its dependencies.</span></span> <span data-ttu-id="23e52-187">Váš program i cílové sestavení budou `Utility` používat stejné sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-187">Both your program and the target assembly will use the same `Utility` assembly.</span></span>  
  
 <span data-ttu-id="23e52-188">Předpokládejme však, že načtete cílové <xref:System.Reflection.Assembly.LoadFile%2A> sestavení podle cesty k souboru pomocí metody.</span><span class="sxs-lookup"><span data-stu-id="23e52-188">However, suppose you load the target assembly by its file path, using the <xref:System.Reflection.Assembly.LoadFile%2A> method.</span></span> <span data-ttu-id="23e52-189">Sestavení je načteno bez kontextu, takže jeho závislosti nejsou automaticky načteny.</span><span class="sxs-lookup"><span data-stu-id="23e52-189">The assembly is loaded without any context, so its dependencies are not automatically loaded.</span></span> <span data-ttu-id="23e52-190">Můžete mít obslužnou rutinu <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> pro událost k `Utility` zadání závislosti a může <xref:System.Reflection.Assembly.LoadFile%2A> načíst sestavení bez kontextu pomocí metody.</span><span class="sxs-lookup"><span data-stu-id="23e52-190">You might have a handler for the <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType> event to supply the dependency, and it might load the `Utility` assembly with no context by using the <xref:System.Reflection.Assembly.LoadFile%2A> method.</span></span> <span data-ttu-id="23e52-191">Nyní při vytváření instance typu, který je obsažen v cílovém sestavení a pokuste `ICommunicate` <xref:System.InvalidCastException> se ji přiřadit proměnné typu `ICommunicate` , je vyvolána, `Utility` protože modul runtime považuje rozhraní ve dvou kopiích sestavení za různé typy.</span><span class="sxs-lookup"><span data-stu-id="23e52-191">Now when you create an instance of a type that is contained in the target assembly and try to assign it to a variable of type `ICommunicate`, an <xref:System.InvalidCastException> is thrown because the runtime considers the `ICommunicate` interfaces in the two copies of the `Utility` assembly to be different types.</span></span>  
  
 <span data-ttu-id="23e52-192">Existuje mnoho dalších scénářů, ve kterých lze načíst sestavení do více kontextů.</span><span class="sxs-lookup"><span data-stu-id="23e52-192">There are many other scenarios in which an assembly can be loaded into multiple contexts.</span></span> <span data-ttu-id="23e52-193">Nejlepším přístupem je vyhnout se konfliktům přemístěním cílového sestavení v <xref:System.Reflection.Assembly.Load%2A> cestě aplikace a použitím metody s úplným zobrazovaným názvem.</span><span class="sxs-lookup"><span data-stu-id="23e52-193">The best approach is to avoid conflicts by relocating the target assembly in your application path and using the <xref:System.Reflection.Assembly.Load%2A> method with the full display name.</span></span> <span data-ttu-id="23e52-194">Sestava je pak načtena do výchozího kontextu zatížení `Utility` a obě sestavení používají stejné sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-194">The assembly is then loaded into the default load context, and both assemblies use the same `Utility` assembly.</span></span>  
  
 <span data-ttu-id="23e52-195">Pokud cílové sestavení musí zůstat mimo cestu aplikace, můžete použít metodu <xref:System.Reflection.Assembly.LoadFrom%2A> k načtení do kontextu load-from.</span><span class="sxs-lookup"><span data-stu-id="23e52-195">If the target assembly must remain outside your application path, you can use the <xref:System.Reflection.Assembly.LoadFrom%2A> method to load it into the load-from context.</span></span> <span data-ttu-id="23e52-196">Pokud cílové sestavení bylo zkompilován s `Utility` odkazem na sestavení `Utility` vaší aplikace, bude používat sestavení, které vaše aplikace načetla do výchozího kontextu zatížení.</span><span class="sxs-lookup"><span data-stu-id="23e52-196">If the target assembly was compiled with a reference to your application's `Utility` assembly, it will use the `Utility` assembly that your application has loaded into the default load context.</span></span> <span data-ttu-id="23e52-197">Všimněte si, že problémy mohou nastat, pokud `Utility` cílové sestavení má závislost na kopii sestavení umístěné mimo cestu aplikace.</span><span class="sxs-lookup"><span data-stu-id="23e52-197">Note that problems can occur if the target assembly has a dependency on a copy of the `Utility` assembly located outside your application path.</span></span> <span data-ttu-id="23e52-198">Pokud je toto sestavení načteno do kontextu `Utility` zatížení z před načtením sestavení, zatížení aplikace se nezdaří.</span><span class="sxs-lookup"><span data-stu-id="23e52-198">If that assembly is loaded into the load-from context before your application loads the `Utility` assembly, your application's load will fail.</span></span>  
  
 <span data-ttu-id="23e52-199">V [části Zvažte přepnutí na výchozí kontext načítání](#switch_to_default) popisuje <xref:System.Reflection.Assembly.LoadFile%2A> alternativy k použití načtení cesty k souboru, například a <xref:System.Reflection.Assembly.LoadFrom%2A>.</span><span class="sxs-lookup"><span data-stu-id="23e52-199">The [Consider Switching to the Default Load Context](#switch_to_default) section discusses alternatives to using file path loads such as <xref:System.Reflection.Assembly.LoadFile%2A> and <xref:System.Reflection.Assembly.LoadFrom%2A>.</span></span>  
  
<a name="avoid_loading_multiple_versions"></a>
## <a name="avoid-loading-multiple-versions-of-an-assembly-into-the-same-context"></a><span data-ttu-id="23e52-200">Vyhněte se načítání více verzí sestavení do stejného kontextu</span><span class="sxs-lookup"><span data-stu-id="23e52-200">Avoid Loading Multiple Versions of an Assembly into the Same Context</span></span>  
 <span data-ttu-id="23e52-201">Načítání více verzí sestavení do jednoho kontextu zatížení může způsobit problémy s identitou typu.</span><span class="sxs-lookup"><span data-stu-id="23e52-201">Loading multiple versions of an assembly into one load context can cause type identity problems.</span></span> <span data-ttu-id="23e52-202">Pokud je stejný typ načten ze dvou verzí stejného sestavení, je to, jako kdyby byly načteny dva různé typy se stejným názvem.</span><span class="sxs-lookup"><span data-stu-id="23e52-202">If the same type is loaded from two versions of the same assembly, it is as if two different types with the same name had been loaded.</span></span> <span data-ttu-id="23e52-203">Je <xref:System.InvalidCastException> vyvolána, pokud se pokusíte přetypovat jeden typ `MyType` na druhý, `MyType`s matoucí zprávu, že typ nelze přetypovat na typ .</span><span class="sxs-lookup"><span data-stu-id="23e52-203">An <xref:System.InvalidCastException> is thrown if you try to cast one type to the other, with the confusing message that type `MyType` cannot be cast to type `MyType`.</span></span>  
  
 <span data-ttu-id="23e52-204">Program může například načíst jednu verzi `Utility` sestavení přímo a později může načíst `Utility` jiné sestavení, které načte jinou verzi sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-204">For example, your program might load one version of the `Utility` assembly directly, and later it might load another assembly that loads a different version of the `Utility` assembly.</span></span> <span data-ttu-id="23e52-205">Nebo chyba kódování může způsobit dvě různé cesty kódu v aplikaci načíst různé verze sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-205">Or a coding error might cause two different code paths in your application to load different versions of an assembly.</span></span>  
  
 <span data-ttu-id="23e52-206">Ve výchozím kontextu zatížení k tomuto problému <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> může dojít při použití metody a zadat úplné sestavení zobrazované názvy, které obsahují různá čísla verzí.</span><span class="sxs-lookup"><span data-stu-id="23e52-206">In the default load context, this problem can occur when you use the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and specify complete assembly display names that include different version numbers.</span></span> <span data-ttu-id="23e52-207">U sestavení, která jsou načtena bez kontextu, <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType> může být problém způsoben použitím metody k načtení stejnésestavy z různých cest.</span><span class="sxs-lookup"><span data-stu-id="23e52-207">For assemblies that are loaded without context, the problem can be caused by using the <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType> method to load the same assembly from different paths.</span></span> <span data-ttu-id="23e52-208">Za běhu považuje dvě sestavení, které jsou načteny z různých cest, za různá sestavení, i když jsou jejich identity stejné.</span><span class="sxs-lookup"><span data-stu-id="23e52-208">The runtime considers two assemblies that are loaded from different paths to be different assemblies, even if their identities are the same.</span></span>  
  
 <span data-ttu-id="23e52-209">Kromě problémů s identitou typu může více verzí <xref:System.MissingMethodException> sestavení způsobit, pokud je typ načtený z jedné verze sestavení předán kódu, který očekává tento typ z jiné verze.</span><span class="sxs-lookup"><span data-stu-id="23e52-209">In addition to type identity problems, multiple versions of an assembly can cause a <xref:System.MissingMethodException> if a type that is loaded from one version of the assembly is passed to code that expects that type from a different version.</span></span> <span data-ttu-id="23e52-210">Kód může například očekávat metodu, která byla přidána do novější verze.</span><span class="sxs-lookup"><span data-stu-id="23e52-210">For example, the code might expect a method that was added to the later version.</span></span>  
  
 <span data-ttu-id="23e52-211">Jemnější chyby může dojít, pokud chování typu změnil mezi verzemi.</span><span class="sxs-lookup"><span data-stu-id="23e52-211">More subtle errors can occur if the behavior of the type changed between versions.</span></span> <span data-ttu-id="23e52-212">Metoda může například vyvolat neočekávanou výjimku nebo vrátit neočekávanou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="23e52-212">For example, a method might throw an unexpected exception or return an unexpected value.</span></span>  
  
 <span data-ttu-id="23e52-213">Pečlivě zkontrolujte kód a ujistěte se, že je načtena pouze jedna verze sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-213">Carefully review your code to ensure that only one version of an assembly is loaded.</span></span> <span data-ttu-id="23e52-214">Tuto metodu <xref:System.AppDomain.GetAssemblies%2A?displayProperty=nameWithType> můžete použít k určení sestavení, která jsou v daném okamžiku načtena.</span><span class="sxs-lookup"><span data-stu-id="23e52-214">You can use the <xref:System.AppDomain.GetAssemblies%2A?displayProperty=nameWithType> method to determine which assemblies are loaded at any given time.</span></span>  
  
<a name="switch_to_default"></a>
## <a name="consider-switching-to-the-default-load-context"></a><span data-ttu-id="23e52-215">Zvažte přepnutí na výchozí kontext načítání</span><span class="sxs-lookup"><span data-stu-id="23e52-215">Consider Switching to the Default Load Context</span></span>  
 <span data-ttu-id="23e52-216">Zkontrolujte vzory načítání a nasazování sestavení aplikace.</span><span class="sxs-lookup"><span data-stu-id="23e52-216">Examine your application's assembly loading and deployment patterns.</span></span> <span data-ttu-id="23e52-217">Můžete eliminovat sestavení, která jsou načtena z bajtových polí?</span><span class="sxs-lookup"><span data-stu-id="23e52-217">Can you eliminate assemblies that are loaded from byte arrays?</span></span> <span data-ttu-id="23e52-218">Můžete přesunout sestavení do cesty sondování?</span><span class="sxs-lookup"><span data-stu-id="23e52-218">Can you move assemblies into the probing path?</span></span> <span data-ttu-id="23e52-219">Pokud jsou sestavení umístěna v globální mezipaměti sestavení nebo v cestě zjišťování <xref:System.AppDomainSetup.ApplicationBase%2A> <xref:System.AppDomainSetup.PrivateBinPath%2A>domény aplikace (tj. jeho a ), můžete načíst sestavení podle jeho identity.</span><span class="sxs-lookup"><span data-stu-id="23e52-219">If assemblies are located in the global assembly cache or in the application domain's probing path (that is, its <xref:System.AppDomainSetup.ApplicationBase%2A> and <xref:System.AppDomainSetup.PrivateBinPath%2A>), you can load the assembly by its identity.</span></span>  
  
 <span data-ttu-id="23e52-220">Pokud není možné umístit všechna sestavení do cesty zjišťování, zvažte alternativy, jako je například použití modelu doplňku rozhraní .NET Framework, umístění sestavení do globální mezipaměti sestavení nebo vytvoření aplikačních domén.</span><span class="sxs-lookup"><span data-stu-id="23e52-220">If it is not possible to put all your assemblies in the probing path, consider alternatives such as using the .NET Framework add-in model, placing assemblies into the global assembly cache, or creating application domains.</span></span>  
  
### <a name="consider-using-the-net-framework-add-in-model"></a><span data-ttu-id="23e52-221">Zvažte použití modelu doplňku rozhraní .NET Framework</span><span class="sxs-lookup"><span data-stu-id="23e52-221">Consider Using the .NET Framework Add-In Model</span></span>  
 <span data-ttu-id="23e52-222">Pokud používáte kontext load-from k implementaci doplňků, které obvykle nejsou nainstalovány v základu aplikace, použijte model doplňku rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="23e52-222">If you are using the load-from context to implement add-ins, which typically are not installed in the application base, use the .NET Framework add-in model.</span></span> <span data-ttu-id="23e52-223">Tento model poskytuje izolaci na úrovni domény aplikace nebo procesu, aniž byste museli spravovat aplikační domény sami.</span><span class="sxs-lookup"><span data-stu-id="23e52-223">This model provides isolation at the application domain or process level, without requiring you to manage application domains yourself.</span></span> <span data-ttu-id="23e52-224">Informace o modelu doplňku naleznete [v tématu Doplňky a rozšiřitelnost](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/bb384200(v%3dvs.100)).</span><span class="sxs-lookup"><span data-stu-id="23e52-224">For information about the add-in model, see [Add-ins and Extensibility](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/bb384200(v%3dvs.100)).</span></span>  
  
### <a name="consider-using-the-global-assembly-cache"></a><span data-ttu-id="23e52-225">Zvažte použití globální mezipaměti sestavení</span><span class="sxs-lookup"><span data-stu-id="23e52-225">Consider Using the Global Assembly Cache</span></span>  
 <span data-ttu-id="23e52-226">Umístěte sestavení do globální mezipaměti sestavení, abyste získali výhodu sdílené cesty sestavení, která je mimo základnu aplikace, aniž byste ztratili výhody výchozího kontextu zatížení nebo převzali nevýhody jiných kontextů.</span><span class="sxs-lookup"><span data-stu-id="23e52-226">Place assemblies in the global assembly cache to get the benefit of a shared assembly path that is outside the application base, without losing the advantages of the default load context or taking on the disadvantages of the other contexts.</span></span>  
  
### <a name="consider-using-application-domains"></a><span data-ttu-id="23e52-227">Zvažte použití aplikačních domén</span><span class="sxs-lookup"><span data-stu-id="23e52-227">Consider Using Application Domains</span></span>  
 <span data-ttu-id="23e52-228">Pokud zjistíte, že některá sestavení nelze nasadit v cestě zjišťování aplikace, zvažte vytvoření nové domény aplikace pro tato sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-228">If you determine that some of your assemblies cannot be deployed in the application's probing path, consider creating a new application domain for those assemblies.</span></span> <span data-ttu-id="23e52-229">Použijte <xref:System.AppDomainSetup> an k vytvoření nové domény <xref:System.AppDomainSetup.ApplicationBase%2A?displayProperty=nameWithType> aplikace a použijte vlastnost k určení cesty, která obsahuje sestavení, která chcete načíst.</span><span class="sxs-lookup"><span data-stu-id="23e52-229">Use an <xref:System.AppDomainSetup> to create the new application domain, and use the <xref:System.AppDomainSetup.ApplicationBase%2A?displayProperty=nameWithType> property to specify the path that contains the assemblies you want to load.</span></span> <span data-ttu-id="23e52-230">Pokud máte více adresářů pro účely sondy, můžete <xref:System.AppDomainSetup.ApplicationBase%2A> <xref:System.AppDomainSetup.PrivateBinPath%2A?displayProperty=nameWithType> nastavit kořenový adresář a použít vlastnost k identifikaci podadresářů, které mají být sondovat.</span><span class="sxs-lookup"><span data-stu-id="23e52-230">If you have multiple directories to probe, you can set the <xref:System.AppDomainSetup.ApplicationBase%2A> to a root directory and use the <xref:System.AppDomainSetup.PrivateBinPath%2A?displayProperty=nameWithType> property to identify the subdirectories to probe.</span></span> <span data-ttu-id="23e52-231">Případně můžete vytvořit více aplikačních domén <xref:System.AppDomainSetup.ApplicationBase%2A> a nastavit každou doménu aplikace na příslušnou cestu pro její sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-231">Alternatively, you can create multiple application domains and set the <xref:System.AppDomainSetup.ApplicationBase%2A> of each application domain to the appropriate path for its assemblies.</span></span>  
  
 <span data-ttu-id="23e52-232">Všimněte si, <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> že můžete použít metodu k načtení těchto sestavení.</span><span class="sxs-lookup"><span data-stu-id="23e52-232">Note that you can use the <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType> method to load these assemblies.</span></span> <span data-ttu-id="23e52-233">Vzhledem k tomu, že jsou nyní v cestě zjišťování, budou načteny do výchozího kontextu zatížení namísto kontextu zatížení z.</span><span class="sxs-lookup"><span data-stu-id="23e52-233">Because they are now in the probing path, they will be loaded into the default load context instead of the load-from context.</span></span> <span data-ttu-id="23e52-234">Doporučujeme však přepnout <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> na metodu a zadat úplné sestavení zobrazované názvy, aby bylo zajištěno, že správné verze jsou vždy používány.</span><span class="sxs-lookup"><span data-stu-id="23e52-234">However, we recommend that you switch to the <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType> method and supply full assembly display names to ensure that correct versions are always used.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="23e52-235">Viz také</span><span class="sxs-lookup"><span data-stu-id="23e52-235">See also</span></span>

- <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>
- <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>
- <xref:System.Reflection.Assembly.LoadFile%2A?displayProperty=nameWithType>
- <xref:System.AppDomain.AssemblyResolve?displayProperty=nameWithType>
