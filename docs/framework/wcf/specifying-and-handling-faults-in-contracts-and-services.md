---
title: Určování a zpracování chyb v kontraktech a službách
ms.date: 03/30/2017
helpviewer_keywords:
- handling faults [WCF]
ms.assetid: a9696563-d404-4905-942d-1e0834c26dea
ms.openlocfilehash: 95797bd341aa7051107209d6cd26097458661e5c
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/22/2019
ms.locfileid: "69922947"
---
# <a name="specifying-and-handling-faults-in-contracts-and-services"></a><span data-ttu-id="bf247-102">Určování a zpracování chyb v kontraktech a službách</span><span class="sxs-lookup"><span data-stu-id="bf247-102">Specifying and Handling Faults in Contracts and Services</span></span>
<span data-ttu-id="bf247-103">Aplikace Windows Communication Foundation (WCF) zpracovávají chybové situace mapováním spravovaných objektů výjimek na objekty selhání SOAP a objekty selhání SOAP na spravované objekty výjimek.</span><span class="sxs-lookup"><span data-stu-id="bf247-103">Windows Communication Foundation (WCF) applications handle error situations by mapping managed exception objects to SOAP fault objects and SOAP fault objects to managed exception objects.</span></span> <span data-ttu-id="bf247-104">Témata v této části popisují, jak navrhovat smlouvy, aby se chybové stavy vystavoval jako vlastní chyby protokolu SOAP, jak tyto chyby vracet jako součást implementace služby a jak tyto chyby zachytí klienti.</span><span class="sxs-lookup"><span data-stu-id="bf247-104">The topics in this section discuss how to design contracts to expose error conditions as custom SOAP faults, how to return such faults as part of service implementation, and how clients catch such faults.</span></span>  
  
## <a name="error-handling-overview"></a><span data-ttu-id="bf247-105">Přehled zpracování chyb</span><span class="sxs-lookup"><span data-stu-id="bf247-105">Error Handling Overview</span></span>  
 <span data-ttu-id="bf247-106">Ve všech spravovaných aplikacích jsou chyby zpracování reprezentovány <xref:System.Exception> objekty.</span><span class="sxs-lookup"><span data-stu-id="bf247-106">In all managed applications, processing errors are represented by <xref:System.Exception> objects.</span></span> <span data-ttu-id="bf247-107">V aplikacích založených na protokolu SOAP, jako jsou aplikace WCF, metody služeb sdělují informace o chybě zpracování pomocí zpráv o chybách protokolu SOAP.</span><span class="sxs-lookup"><span data-stu-id="bf247-107">In SOAP-based applications such as WCF applications, service methods communicate processing error information using SOAP fault messages.</span></span> <span data-ttu-id="bf247-108">Chyby protokolu SOAP jsou typy zpráv, které jsou zahrnuty v metadatech pro operaci služby. proto je možné vytvořit kontrakt selhání, který mohou klienti použít k zajištění robustnější nebo interaktivní operace.</span><span class="sxs-lookup"><span data-stu-id="bf247-108">SOAP faults are message types that are included in the metadata for a service operation and therefore create a fault contract that clients can use to make their operation more robust or interactive.</span></span> <span data-ttu-id="bf247-109">Vzhledem k tomu, že chyby protokolu SOAP jsou vyjádřeny klientům ve formátu XML, je vysoce interoperabilní systém typů, který mohou klienti na libovolné platformě SOAP použít a zvyšují dosah vaší aplikace WCF.</span><span class="sxs-lookup"><span data-stu-id="bf247-109">In addition, because SOAP faults are expressed to clients in XML form, it is a highly interoperable type system that clients on any SOAP platform can use, increasing the reach of your WCF application.</span></span>  
  
 <span data-ttu-id="bf247-110">Vzhledem k tomu, že aplikace WCF běží v obou typech chybových systémů, všechny spravované informace o výjimce, které se odesílají do klienta, musí být převedeny z výjimek na chyby protokolu SOAP ve službě, odeslány a převedeny z chyb protokolu SOAP na výjimky chyb v klientech WCF.</span><span class="sxs-lookup"><span data-stu-id="bf247-110">Because WCF applications run under both types of error systems, any managed exception information that is sent to the client must be converted from exceptions into SOAP faults on the service, sent, and converted from SOAP faults to fault exceptions in WCF clients.</span></span> <span data-ttu-id="bf247-111">V případě duplexních klientů můžou kontrakty klienta také odesílat chyby protokolu SOAP zpátky do služby.</span><span class="sxs-lookup"><span data-stu-id="bf247-111">In the case of duplex clients, client contracts can also send SOAP faults back to a service.</span></span> <span data-ttu-id="bf247-112">V obou případech můžete použít výchozí chování výjimky služby nebo můžete explicitně řídit, jestli – a jak jsou výjimky namapované na chybové zprávy.</span><span class="sxs-lookup"><span data-stu-id="bf247-112">In either case, you can use the default service exception behaviors, or you can explicitly control whether—and how—exceptions are mapped to fault messages.</span></span>  
  
 <span data-ttu-id="bf247-113">Je možné odeslat dva typy chyb SOAP: *deklarovaný* a nedeklarovaný.</span><span class="sxs-lookup"><span data-stu-id="bf247-113">Two types of SOAP faults can be sent: *declared* and *undeclared*.</span></span> <span data-ttu-id="bf247-114">Deklarované chyby protokolu SOAP jsou ty, ve kterých operace má <xref:System.ServiceModel.FaultContractAttribute?displayProperty=nameWithType> atribut, který určuje vlastní typ chyby SOAP.</span><span class="sxs-lookup"><span data-stu-id="bf247-114">Declared SOAP faults are those in which an operation has a <xref:System.ServiceModel.FaultContractAttribute?displayProperty=nameWithType> attribute that specifies a custom SOAP fault type.</span></span> <span data-ttu-id="bf247-115">*Nedeklarovaný* V kontraktu pro operaci nejsou zadány chyby protokolu SOAP.</span><span class="sxs-lookup"><span data-stu-id="bf247-115">*Undeclared* SOAP faults are not specified in the contract for an operation.</span></span>  
  
 <span data-ttu-id="bf247-116">Důrazně se doporučuje, aby operace služby deklarovaly své chyby pomocí <xref:System.ServiceModel.FaultContractAttribute> atributu pro vytvoření všech chyb protokolu SOAP, které může klient očekávat při běžném průběhu operace.</span><span class="sxs-lookup"><span data-stu-id="bf247-116">It is strongly recommended that service operations declare their faults by using the <xref:System.ServiceModel.FaultContractAttribute> attribute to formally specify all SOAP faults that a client can expect to receive in the normal course of an operation.</span></span> <span data-ttu-id="bf247-117">Také se doporučuje vrátit se do chyby protokolu SOAP pouze informace, které musí klient znát, aby bylo možné minimalizovat vyzrazení informací.</span><span class="sxs-lookup"><span data-stu-id="bf247-117">It is also recommended that you return in a SOAP fault only the information that a client must know to minimize information disclosure.</span></span>  
  
 <span data-ttu-id="bf247-118">Služby (a duplexní klienti) obvykle procházejí následujícími kroky, které úspěšně integrují zpracování chyb do svých aplikací:</span><span class="sxs-lookup"><span data-stu-id="bf247-118">Typically, services (and duplex clients) take the following steps to successfully integrate error handling into their applications:</span></span>  
  
- <span data-ttu-id="bf247-119">Namapujte podmínky výjimky na vlastní chyby protokolu SOAP.</span><span class="sxs-lookup"><span data-stu-id="bf247-119">Map exception conditions to custom SOAP faults.</span></span>  
  
- <span data-ttu-id="bf247-120">Klienti a služby odesílají a přijímají chyby protokolu SOAP jako výjimky.</span><span class="sxs-lookup"><span data-stu-id="bf247-120">Clients and services send and receive SOAP faults as exceptions.</span></span>  
  
 <span data-ttu-id="bf247-121">Kromě toho mohou klienti a služby WCF používat nedeklarované chyby protokolu SOAP pro účely ladění a mohou rozšířit chování výchozí chyby.</span><span class="sxs-lookup"><span data-stu-id="bf247-121">In addition, WCF clients and services can use undeclared soap faults for debugging purposes and can extend the default error behavior.</span></span> <span data-ttu-id="bf247-122">Následující části popisují tyto úlohy a koncepty.</span><span class="sxs-lookup"><span data-stu-id="bf247-122">The following sections discuss these tasks and concepts.</span></span>  
  
## <a name="map-exceptions-to-soap-faults"></a><span data-ttu-id="bf247-123">Mapování výjimek na chyby protokolu SOAP</span><span class="sxs-lookup"><span data-stu-id="bf247-123">Map Exceptions to SOAP Faults</span></span>  
 <span data-ttu-id="bf247-124">Prvním krokem při vytváření operace, která zpracovává chybové stavy, je určit, za jakých podmínek by měla být klientská aplikace informována o chybách.</span><span class="sxs-lookup"><span data-stu-id="bf247-124">The first step in creating an operation that handles error conditions is to decide under what conditions a client application should be informed about errors.</span></span> <span data-ttu-id="bf247-125">Některé operace mají chybové podmínky specifické pro jejich funkce.</span><span class="sxs-lookup"><span data-stu-id="bf247-125">Some operations have error conditions specific to their functionality.</span></span> <span data-ttu-id="bf247-126">Například `PurchaseOrder` operace může vracet konkrétní informace zákazníkům, kteří již nejsou oprávněni iniciovat nákupní objednávku.</span><span class="sxs-lookup"><span data-stu-id="bf247-126">For example, a `PurchaseOrder` operation might return specific information to customers who are no longer permitted to initiate a purchase order.</span></span> <span data-ttu-id="bf247-127">V jiných případech, jako je `Calculator` například služba, může být `MathFault` obecnější Chyba protokolu SOAP popsána v rámci celé služby.</span><span class="sxs-lookup"><span data-stu-id="bf247-127">In other cases, such as a `Calculator` service, a more general `MathFault` SOAP fault may be able to describe all error conditions across an entire service.</span></span> <span data-ttu-id="bf247-128">Jakmile se identifikují chybové stavy klientů vaší služby, může být vytvořena vlastní chyba protokolu SOAP a operace může být označena jako vracená Chyba protokolu SOAP, když dojde k odpovídajícímu chybovému stavu.</span><span class="sxs-lookup"><span data-stu-id="bf247-128">Once the error conditions of clients of your service are identified, a custom SOAP fault can be constructed and the operation can be marked as returning that SOAP fault when its corresponding error condition arises.</span></span>  
  
 <span data-ttu-id="bf247-129">Další informace o tomto kroku vývoje služby nebo klienta najdete v tématu [Definování a určení chyb](../../../docs/framework/wcf/defining-and-specifying-faults.md).</span><span class="sxs-lookup"><span data-stu-id="bf247-129">For more information about this step of developing your service or client, see [Defining and Specifying Faults](../../../docs/framework/wcf/defining-and-specifying-faults.md).</span></span>  
  
## <a name="clients-and-services-handle-soap-faults-as-exceptions"></a><span data-ttu-id="bf247-130">Klienti a služby zpracovávají chyby protokolu SOAP jako výjimky.</span><span class="sxs-lookup"><span data-stu-id="bf247-130">Clients and Services Handle SOAP Faults as Exceptions</span></span>  
 <span data-ttu-id="bf247-131">Identifikují se chybové podmínky operace, definování vlastních chyb protokolu SOAP a označení operací, jako je vrácení těchto chyb, jsou první kroky při úspěšném zpracování chyb v aplikacích WCF.</span><span class="sxs-lookup"><span data-stu-id="bf247-131">Identifying operation error conditions, defining custom SOAP faults, and marking those operations as returning those faults are the first steps in successful error handling in WCF applications.</span></span> <span data-ttu-id="bf247-132">Dalším krokem je správné implementace odesílání a přijímání těchto chyb.</span><span class="sxs-lookup"><span data-stu-id="bf247-132">The next step is to properly implement the sending and receiving of these faults.</span></span> <span data-ttu-id="bf247-133">Služby obvykle odesílají chyby, které upozorňují klientské aplikace na chybové podmínky, ale duplexní klienti mohou také odesílat chyby protokolu SOAP službám.</span><span class="sxs-lookup"><span data-stu-id="bf247-133">Typically services send faults to inform client applications about error conditions, but duplex clients can also send SOAP faults to services.</span></span>  
  
 <span data-ttu-id="bf247-134">Další informace najdete v tématu [odesílání a příjem chyb](../../../docs/framework/wcf/sending-and-receiving-faults.md).</span><span class="sxs-lookup"><span data-stu-id="bf247-134">For more information, see [Sending and Receiving Faults](../../../docs/framework/wcf/sending-and-receiving-faults.md).</span></span>  
  
## <a name="undeclared-soap-faults-and-debugging"></a><span data-ttu-id="bf247-135">Nedeklarované chyby protokolu SOAP a ladění</span><span class="sxs-lookup"><span data-stu-id="bf247-135">Undeclared SOAP Faults and Debugging</span></span>  
 <span data-ttu-id="bf247-136">Deklarované chyby protokolu SOAP jsou velmi užitečné pro vytváření robustních, interoperabilních a distribuovaných aplikací.</span><span class="sxs-lookup"><span data-stu-id="bf247-136">Declared SOAP faults are extremely useful for building robust, interoperable, distributed applications.</span></span> <span data-ttu-id="bf247-137">V některých případech je však užitečné, aby služba (nebo duplexní klienti) odesílala nedeklarovanou chybu protokolu SOAP, kterou nezmiňuje jazyk WSDL (Web Services Description Language) pro tuto operaci.</span><span class="sxs-lookup"><span data-stu-id="bf247-137">However, in some cases it is useful for a service (or duplex client) to send an undeclared SOAP fault, one that is not mentioned in the Web Services Description Language (WSDL) for that operation.</span></span> <span data-ttu-id="bf247-138">Například při vývoji služby může dojít k neočekávaným situacím, kdy je užitečné pro účely ladění pro odeslání informací zpět klientovi.</span><span class="sxs-lookup"><span data-stu-id="bf247-138">For example, when developing a service, unexpected situations can occur in which it is useful for debugging purposes to send information back to the client.</span></span> <span data-ttu-id="bf247-139">Kromě toho můžete nastavit <xref:System.ServiceModel.ServiceBehaviorAttribute.IncludeExceptionDetailInFaults%2A?displayProperty=nameWithType> vlastnost <xref:System.ServiceModel.Description.ServiceDebugBehavior.IncludeExceptionDetailInFaults%2A?displayProperty=nameWithType> nebo vlastnost na `true` , aby klienti WCF mohli získat informace o výjimkách interní operace služby.</span><span class="sxs-lookup"><span data-stu-id="bf247-139">In addition, you can set the <xref:System.ServiceModel.ServiceBehaviorAttribute.IncludeExceptionDetailInFaults%2A?displayProperty=nameWithType> property or the <xref:System.ServiceModel.Description.ServiceDebugBehavior.IncludeExceptionDetailInFaults%2A?displayProperty=nameWithType> property to `true` to permit WCF clients to obtain information about internal service operation exceptions.</span></span> <span data-ttu-id="bf247-140">Posílání jednotlivých chyb a nastavení vlastností chování ladění jsou popsány v tématu [odesílání a příjem chyb](../../../docs/framework/wcf/sending-and-receiving-faults.md).</span><span class="sxs-lookup"><span data-stu-id="bf247-140">Both sending individual faults and setting the debugging behavior properties are described in [Sending and Receiving Faults](../../../docs/framework/wcf/sending-and-receiving-faults.md).</span></span>  
  
> [!IMPORTANT]
> <span data-ttu-id="bf247-141">Vzhledem k tomu, že spravované výjimky můžou vystavovat <xref:System.ServiceModel.Description.ServiceDebugBehavior.IncludeExceptionDetailInFaults%2A?displayProperty=nameWithType> interní `true` informace o aplikaci, nastavení <xref:System.ServiceModel.ServiceBehaviorAttribute.IncludeExceptionDetailInFaults%2A?displayProperty=nameWithType> nebo může povolit klientům WCF získávat informace o výjimkách interních operací služby, včetně osobních údajů. identifikovatelné nebo jiné citlivé informace.</span><span class="sxs-lookup"><span data-stu-id="bf247-141">Because managed exceptions can expose internal application information, setting <xref:System.ServiceModel.ServiceBehaviorAttribute.IncludeExceptionDetailInFaults%2A?displayProperty=nameWithType> or <xref:System.ServiceModel.Description.ServiceDebugBehavior.IncludeExceptionDetailInFaults%2A?displayProperty=nameWithType> to `true` can permit WCF clients to obtain information about internal service operation exceptions, including personally identifiable or other sensitive information.</span></span>  
>   
>  <span data-ttu-id="bf247-142">Proto se nastavení <xref:System.ServiceModel.ServiceBehaviorAttribute.IncludeExceptionDetailInFaults%2A?displayProperty=nameWithType> nebo <xref:System.ServiceModel.Description.ServiceDebugBehavior.IncludeExceptionDetailInFaults%2A?displayProperty=nameWithType> na `true` doporučuje jenom jako způsob, jak dočasně ladit aplikaci služby.</span><span class="sxs-lookup"><span data-stu-id="bf247-142">Therefore, setting <xref:System.ServiceModel.ServiceBehaviorAttribute.IncludeExceptionDetailInFaults%2A?displayProperty=nameWithType> or <xref:System.ServiceModel.Description.ServiceDebugBehavior.IncludeExceptionDetailInFaults%2A?displayProperty=nameWithType> to `true` is recommended only as a way to temporarily debug a service application.</span></span> <span data-ttu-id="bf247-143">Kromě toho WSDL pro metodu, která vrací neošetřené spravované výjimky tímto způsobem, neobsahuje kontrakt pro <xref:System.ServiceModel.FaultException%601> typ. <xref:System.ServiceModel.ExceptionDetail></span><span class="sxs-lookup"><span data-stu-id="bf247-143">In addition, the WSDL for a method that returns unhandled managed exceptions in this way does not contain the contract for the <xref:System.ServiceModel.FaultException%601> of type <xref:System.ServiceModel.ExceptionDetail>.</span></span> <span data-ttu-id="bf247-144">Aby klienti mohli správně získat informace o ladění, musí očekávat neznámou chybu SOAP <xref:System.ServiceModel.FaultException?displayProperty=nameWithType> (vrácený klientům WCF jako objekty).</span><span class="sxs-lookup"><span data-stu-id="bf247-144">Clients must expect the possibility of an unknown SOAP fault (returned to WCF clients as <xref:System.ServiceModel.FaultException?displayProperty=nameWithType> objects) to obtain the debugging information properly.</span></span>  
  
## <a name="customizing-error-handling-with-ierrorhandler"></a><span data-ttu-id="bf247-145">Přizpůsobení zpracování chyb pomocí IErrorHandler</span><span class="sxs-lookup"><span data-stu-id="bf247-145">Customizing Error Handling with IErrorHandler</span></span>  
 <span data-ttu-id="bf247-146">Pokud máte zvláštní požadavky pro přizpůsobení zprávy odpovědi klientovi, když dojde k výjimce na úrovni aplikace nebo provedení určitého vlastního zpracování po vrácení zprávy s odpovědí, implementujte <xref:System.ServiceModel.Dispatcher.IErrorHandler?displayProperty=nameWithType> rozhraní.</span><span class="sxs-lookup"><span data-stu-id="bf247-146">If you have special requirements to either customize the response message to the client when an application-level exception happens or perform some custom processing after the response message is returned, implement the <xref:System.ServiceModel.Dispatcher.IErrorHandler?displayProperty=nameWithType> interface.</span></span>  
  
## <a name="fault-serialization-issues"></a><span data-ttu-id="bf247-147">Problémy serializace chyby</span><span class="sxs-lookup"><span data-stu-id="bf247-147">Fault Serialization Issues</span></span>  
 <span data-ttu-id="bf247-148">Při deserializaci kontraktu selhání se WCF nejdříve pokusí vyhledat název kontraktu chyby ve zprávě SOAP s typem kontraktu selhání.</span><span class="sxs-lookup"><span data-stu-id="bf247-148">When deserializing a fault contract, WCF first attempts to match the fault contract name in the SOAP message with the fault contract type.</span></span> <span data-ttu-id="bf247-149">Pokud nemůže najít přesnou shodu, vyhledá kompatibilní typ v seznamu dostupných kontraktů selhání v abecedním pořadí.</span><span class="sxs-lookup"><span data-stu-id="bf247-149">If it cannot find an exact match it will then search the list of available fault contracts in alphabetical order for a compatible type.</span></span> <span data-ttu-id="bf247-150">Pokud jsou dvě kontrakty selhání kompatibilní typy (jedna je podtřídou jiného, například), je možné použít špatný typ k deserializaci chyby.</span><span class="sxs-lookup"><span data-stu-id="bf247-150">If two fault contracts are compatible types (one is a subclass of another, for example) the wrong type may be used to de-serialize the fault.</span></span> <span data-ttu-id="bf247-151">K tomu dochází pouze v případě, že smlouva o selhání neurčuje název, obor názvů a akci.</span><span class="sxs-lookup"><span data-stu-id="bf247-151">This only occurs if the fault contract does not specify a name, namespace, and action.</span></span> <span data-ttu-id="bf247-152">Chcete-li zabránit výskytu tohoto problému, vždy plně kvalifikované smlouvy o selhání zadáním názvu, oboru názvů a atributů akce.</span><span class="sxs-lookup"><span data-stu-id="bf247-152">To prevent this issue from occurring, always fully qualify fault contracts by specifying the name, namespace, and action attributes.</span></span> <span data-ttu-id="bf247-153">Pokud jste navíc definovali řadu souvisejících kontraktů selhání odvozených ze sdílené základní třídy, nezapomeňte označit všechny nové členy pomocí `[DataMember(IsRequired=true)]`.</span><span class="sxs-lookup"><span data-stu-id="bf247-153">Additionally if you have defined a number of related fault contracts derived from a shared base class, make sure to mark any new members with `[DataMember(IsRequired=true)]`.</span></span> <span data-ttu-id="bf247-154">Další informace o tomto `IsRequired` atributu <xref:System.Runtime.Serialization.DataMemberAttribute>naleznete v tématu.</span><span class="sxs-lookup"><span data-stu-id="bf247-154">For more information on this `IsRequired` attribute see, <xref:System.Runtime.Serialization.DataMemberAttribute>.</span></span> <span data-ttu-id="bf247-155">Tím zabráníte, aby základní třída byla kompatibilního typu, a vynutíte deserializaci chyby do správného odvozeného typu.</span><span class="sxs-lookup"><span data-stu-id="bf247-155">This will prevent a base class from being a compatible type and force the fault to be deserialized into the correct derived type.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="bf247-156">Viz také:</span><span class="sxs-lookup"><span data-stu-id="bf247-156">See also</span></span>

- <xref:System.ServiceModel.FaultException>
- <xref:System.ServiceModel.FaultContractAttribute>
- <xref:System.ServiceModel.FaultException>
- <xref:System.Xml.Serialization.XmlSerializer>
- <xref:System.ServiceModel.XmlSerializerFormatAttribute>
- <xref:System.ServiceModel.FaultContractAttribute>
- <xref:System.ServiceModel.CommunicationException>
- <xref:System.ServiceModel.FaultContractAttribute.Action%2A>
- <xref:System.ServiceModel.FaultException.Code%2A>
- <xref:System.ServiceModel.FaultException.Reason%2A>
- <xref:System.ServiceModel.FaultCode.SubCode%2A>
- <xref:System.ServiceModel.OperationContractAttribute.IsOneWay%2A>
- [<span data-ttu-id="bf247-157">Definice a určení chyb</span><span class="sxs-lookup"><span data-stu-id="bf247-157">Defining and Specifying Faults</span></span>](../../../docs/framework/wcf/defining-and-specifying-faults.md)
