---
title: Vlastní kodéry
ms.date: 03/30/2017
ms.assetid: fa0e1d7f-af36-4bf4-aac9-cd4eab95bc4f
ms.openlocfilehash: 586207af0bfbe106512dfb61ee7439d4f5ce64c6
ms.sourcegitcommit: d2e1dfa7ef2d4e9ffae3d431cf6a4ffd9c8d378f
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/07/2019
ms.locfileid: "70797215"
---
# <a name="custom-encoders"></a><span data-ttu-id="ba219-102">Vlastní kodéry</span><span class="sxs-lookup"><span data-stu-id="ba219-102">Custom Encoders</span></span>
<span data-ttu-id="ba219-103">Toto téma popisuje, jak vytvořit vlastní kodéry.</span><span class="sxs-lookup"><span data-stu-id="ba219-103">This topic discusses how to create custom encoders.</span></span>  
  
 <span data-ttu-id="ba219-104">V Windows Communication Foundation (WCF) použijete *vazbu* k určení způsobu přenosu dat mezi koncovými body v síti.</span><span class="sxs-lookup"><span data-stu-id="ba219-104">In Windows Communication Foundation (WCF), you use a *binding* to specify how to transfer data across a network between endpoints.</span></span> <span data-ttu-id="ba219-105">Vazba je tvořena sekvencí *prvků vazby*.</span><span class="sxs-lookup"><span data-stu-id="ba219-105">A binding is made up of a sequence of *binding elements*.</span></span> <span data-ttu-id="ba219-106">Vazba obsahuje volitelné prvky vazby protokolu, jako je například zabezpečení, požadovaný element vazby *kodéru zprávy* a požadovaný prvek vazby přenosu.</span><span class="sxs-lookup"><span data-stu-id="ba219-106">A binding includes optional protocol binding elements such as security, a required *Message Encoder* binding element, and a required transport binding element.</span></span> <span data-ttu-id="ba219-107">Kodér zprávy je reprezentován prvkem vazby kódování zprávy.</span><span class="sxs-lookup"><span data-stu-id="ba219-107">A message encoder is represented by a message encoding binding element.</span></span> <span data-ttu-id="ba219-108">WCF obsahuje tři kodéry zpráv: Binární, mechanizmus pro optimalizaci přenosu zpráv (MTOM) a text.</span><span class="sxs-lookup"><span data-stu-id="ba219-108">Three message encoders are included in WCF: Binary, Message Transmission Optimization Mechanism (MTOM), and Text.</span></span>  
  
 <span data-ttu-id="ba219-109">Element vazby kódování zprávy serializaci odchozí <xref:System.ServiceModel.Channels.Message> a předává do přenosu, nebo přijímá serializovanou formu zprávy z přenosu a předává ji do vrstvy protokolu, pokud je k dispozici, nebo do aplikace, pokud není k dispozici.</span><span class="sxs-lookup"><span data-stu-id="ba219-109">A message encoding binding element serializes an outgoing <xref:System.ServiceModel.Channels.Message> and passes it to the transport, or receives the serialized form of a message from the transport and passes it to the protocol layer if present, or to the application, if not present.</span></span>  
  
 <span data-ttu-id="ba219-110">Kodéry zpráv transformují <xref:System.ServiceModel.Channels.Message> instance do a z reprezentace v rámci vedení.</span><span class="sxs-lookup"><span data-stu-id="ba219-110">Message encoders transform <xref:System.ServiceModel.Channels.Message> instances to and from a wire representation.</span></span> <span data-ttu-id="ba219-111">I když jsou kodéry popsány tak, aby se nacházely nad přenosovou vrstvou v zásobníku kanálů, jsou umístěny uvnitř přenosové vrstvy.</span><span class="sxs-lookup"><span data-stu-id="ba219-111">Although encoders are described as sitting above the transport layer in the channel stack, they reside inside the transport layer.</span></span> <span data-ttu-id="ba219-112">Přenos (například HTTP) formátuje zprávu podle požadavků standardu transportu.</span><span class="sxs-lookup"><span data-stu-id="ba219-112">Transports (for example HTTP) format the message according to the requirements of the transport standard.</span></span> <span data-ttu-id="ba219-113">Kodéry (například text XML) právě zakódují zprávu.</span><span class="sxs-lookup"><span data-stu-id="ba219-113">Encoders (for example Text Xml) just encode the message.</span></span>  
  
 <span data-ttu-id="ba219-114">Pokud se připojujete k již existujícímu klientovi nebo serveru, možná nebudete mít možnost používat konkrétní kódování zprávy.</span><span class="sxs-lookup"><span data-stu-id="ba219-114">When connecting to a preexisting client or server, you may not have a choice about using a particular message encoding.</span></span> <span data-ttu-id="ba219-115">Služby WCF však lze zpřístupnit prostřednictvím více koncových bodů, z nichž každý má jiný kodér zpráv.</span><span class="sxs-lookup"><span data-stu-id="ba219-115">However, WCF services can be made accessible through multiple endpoints, each with a different message encoder.</span></span> <span data-ttu-id="ba219-116">Pokud jeden kodér nepokrývá celou cílovou skupinu pro vaši službu, zvažte zveřejnění služby přes více koncových bodů.</span><span class="sxs-lookup"><span data-stu-id="ba219-116">When a single encoder does not cover the entire audience for your service, consider exposing your service over multiple endpoints.</span></span> <span data-ttu-id="ba219-117">Klientské aplikace potom můžou zvolit koncový bod, který je pro ně nejvhodnější.</span><span class="sxs-lookup"><span data-stu-id="ba219-117">Client applications can then choose the endpoint that is best for them.</span></span> <span data-ttu-id="ba219-118">Použití více koncových bodů umožňuje kombinovat výhody různých kodérů zpráv s dalšími prvky vazby.</span><span class="sxs-lookup"><span data-stu-id="ba219-118">Using multiple endpoints allows you to combine the advantages of different message encoders with other binding elements.</span></span>  
  
## <a name="system-provided-encoders"></a><span data-ttu-id="ba219-119">Kodéry poskytované systémem</span><span class="sxs-lookup"><span data-stu-id="ba219-119">System-Provided Encoders</span></span>  
 <span data-ttu-id="ba219-120">WCF poskytuje několik vazeb poskytovaných systémem, které jsou navržené tak, aby pokrývaly nejběžnější scénáře použití aplikací.</span><span class="sxs-lookup"><span data-stu-id="ba219-120">WCF provides several system-provided bindings that are designed to cover the most common application scenarios.</span></span> <span data-ttu-id="ba219-121">Každá z těchto vazeb kombinuje přenos, kodér zpráv a další možnosti (například zabezpečení).</span><span class="sxs-lookup"><span data-stu-id="ba219-121">Each of these bindings combine a transport, message encoder, and other options (security, for example).</span></span> <span data-ttu-id="ba219-122">Toto téma popisuje, jak můžete roztáhnout `Text`kodéry `MTOM` , `Binary`, a zprávy, které jsou součástí WCF, nebo vytvořit vlastní kodér.</span><span class="sxs-lookup"><span data-stu-id="ba219-122">This topic describes how to extend the `Text`, `Binary`, and `MTOM` message encoders that are included in WCF, or create your own custom encoder.</span></span> <span data-ttu-id="ba219-123">Kodér textu zprávy podporuje jak kódování ve formátu XML, tak i kódování SOAP.</span><span class="sxs-lookup"><span data-stu-id="ba219-123">The text message encoder supports both a plain XML encoding as well as SOAP encodings.</span></span> <span data-ttu-id="ba219-124">Režim prostého kódování XML kodéru textové zprávy se nazývá kodér POX ("obyčejný starý soubor XML"), který ho odlišuje od textového kódování SOAP založeného na textu.</span><span class="sxs-lookup"><span data-stu-id="ba219-124">The plain XML encoding mode of the text message encoder is called the POX ("Plain Old XML") encoder to distinguish it from the text-based SOAP encoding.</span></span>  
  
 <span data-ttu-id="ba219-125">Další informace o kombinací prvků vazby poskytovaných pomocí vazeb poskytovaných systémem naleznete v odpovídající části [výběru přenosu](../feature-details/choosing-a-transport.md).</span><span class="sxs-lookup"><span data-stu-id="ba219-125">For more information about the combinations of binding elements provided by the system-provided bindings, see the corresponding section in [Choosing a Transport](../feature-details/choosing-a-transport.md).</span></span>  
  
## <a name="how-to-work-with-system-provided-encoders"></a><span data-ttu-id="ba219-126">Jak pracovat s kodéry poskytnutými systémem</span><span class="sxs-lookup"><span data-stu-id="ba219-126">How to Work with System-Provided Encoders</span></span>  
 <span data-ttu-id="ba219-127">Do vazby se přidá kódování pomocí třídy odvozené z <xref:System.ServiceModel.Channels.MessageEncodingBindingElement>.</span><span class="sxs-lookup"><span data-stu-id="ba219-127">An encoding is added to a binding using a class derived from <xref:System.ServiceModel.Channels.MessageEncodingBindingElement>.</span></span>  
  
 <span data-ttu-id="ba219-128">WCF poskytuje následující typy prvků vazby, které jsou odvozeny <xref:System.ServiceModel.Channels.MessageEncodingBindingElement> ze třídy, která může poskytnout pro text, binární a kódování v mechanismu optimalizace přenosu zpráv (MTOM):</span><span class="sxs-lookup"><span data-stu-id="ba219-128">WCF provides the following types of binding elements derived from the <xref:System.ServiceModel.Channels.MessageEncodingBindingElement> class that can provide for text, binary and Message Transmission Optimization Mechanism (MTOM) encoding:</span></span>  
  
- <span data-ttu-id="ba219-129"><xref:System.ServiceModel.Channels.TextMessageEncodingBindingElement>: Nejvíce interoperabilní, ale nejméně efektivní kodér pro zprávy XML.</span><span class="sxs-lookup"><span data-stu-id="ba219-129"><xref:System.ServiceModel.Channels.TextMessageEncodingBindingElement>: The most interoperable, but the least efficient encoder for XML messages.</span></span> <span data-ttu-id="ba219-130">Webová služba nebo klient webové služby může obecně pochopit text XML.</span><span class="sxs-lookup"><span data-stu-id="ba219-130">A Web service or Web service client can generally understand textual XML.</span></span> <span data-ttu-id="ba219-131">Nicméně přenos velkých bloků binárních dat jako textu není efektivní.</span><span class="sxs-lookup"><span data-stu-id="ba219-131">However, transmitting large blocks of binary data as text is not efficient.</span></span>  
  
- <span data-ttu-id="ba219-132"><xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement>: Představuje prvek vazby, který určuje kódování znaků a verze zprávy používané pro binární zprávy XML.</span><span class="sxs-lookup"><span data-stu-id="ba219-132"><xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement>: Represents the binding element that specifies the character encoding and message versioning used for binary-based XML messages.</span></span> <span data-ttu-id="ba219-133">To je nejúčinnější z možností kódování, ale nejméně interoperabilní, protože jsou podporovány pouze koncovými body WCF.</span><span class="sxs-lookup"><span data-stu-id="ba219-133">This is most efficient of the encoding options, but the least interoperable, because it is only supported by WCF endpoints.</span></span>  
  
- <span data-ttu-id="ba219-134"><xref:System.ServiceModel.Channels.MtomMessageEncodingBindingElement>: Představuje prvek vazby, který určuje kódování znaků a správu verzí zpráv používané pro zprávu pomocí kódování MTOM (Message reoptimizationing mechanism).</span><span class="sxs-lookup"><span data-stu-id="ba219-134"><xref:System.ServiceModel.Channels.MtomMessageEncodingBindingElement>: Represents the binding element that specifies the character encoding and message versioning used for a message using a Message Transmission Optimization Mechanism (MTOM) encoding.</span></span> <span data-ttu-id="ba219-135">MTOM je efektivní technologie pro přenos binárních dat ve zprávách WCF.</span><span class="sxs-lookup"><span data-stu-id="ba219-135">MTOM is an efficient technology for transmitting binary data in WCF messages.</span></span> <span data-ttu-id="ba219-136">Kodér MTOM se pokusí vyvážit efektivitu a spolupráci.</span><span class="sxs-lookup"><span data-stu-id="ba219-136">The MTOM encoder attempts to balance between efficiency and interoperability.</span></span> <span data-ttu-id="ba219-137">Kódování MTOM přenáší většinu XML v textové podobě, ale optimalizuje velké bloky binárních dat jejich přenosem tak, jak jsou, bez konverze na text.</span><span class="sxs-lookup"><span data-stu-id="ba219-137">The MTOM encoding transmits most XML in textual form, but optimizes large blocks of binary data by transmitting them as-is, without conversion to text.</span></span>  
  
 <span data-ttu-id="ba219-138">Element Binding vytvoří binární, MTOM nebo text <xref:System.ServiceModel.Channels.MessageEncoderFactory>.</span><span class="sxs-lookup"><span data-stu-id="ba219-138">The binding element creates a binary, MTOM, or text <xref:System.ServiceModel.Channels.MessageEncoderFactory>.</span></span> <span data-ttu-id="ba219-139">Objekt pro vytváření vytvoří binární, MTOM nebo textovou <xref:System.ServiceModel.Channels.MessageEncoderFactory> instanci.</span><span class="sxs-lookup"><span data-stu-id="ba219-139">The factory creates a binary, MTOM, or text <xref:System.ServiceModel.Channels.MessageEncoderFactory> instance.</span></span> <span data-ttu-id="ba219-140">Obvykle je k dispozici pouze jedna instance.</span><span class="sxs-lookup"><span data-stu-id="ba219-140">Typically, there is only a single instance.</span></span> <span data-ttu-id="ba219-141">Pokud se ale používají relace, může se každé relaci zadat jiný kodér.</span><span class="sxs-lookup"><span data-stu-id="ba219-141">However if sessions are used, a different encoder may be provided to each session.</span></span> <span data-ttu-id="ba219-142">Binární kodér využívá tuto technologii ke koordinaci dynamických slovníků (viz infrastruktura XML).</span><span class="sxs-lookup"><span data-stu-id="ba219-142">The Binary encoder makes use of this to coordinate dynamic dictionaries (see XML Infrastructure).</span></span>  
  
 <span data-ttu-id="ba219-143">Metody <xref:System.ServiceModel.Channels.MessageEncoder.ReadMessage%2A> a<xref:System.ServiceModel.Channels.MessageEncoder.WriteMessage%2A> jsou jádrem kodéru.</span><span class="sxs-lookup"><span data-stu-id="ba219-143">The <xref:System.ServiceModel.Channels.MessageEncoder.ReadMessage%2A> and <xref:System.ServiceModel.Channels.MessageEncoder.WriteMessage%2A> methods are the core of the encoders.</span></span> <span data-ttu-id="ba219-144">Metody poskytují pro čtení zprávy z datového proudu nebo z <xref:System.Byte> pole.</span><span class="sxs-lookup"><span data-stu-id="ba219-144">The methods provide for reading a message from a stream or from a <xref:System.Byte> array.</span></span> <span data-ttu-id="ba219-145">Pole bajtů se používají, když přenos pracuje v režimu vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="ba219-145">Byte arrays are used when the transport is operating in buffered mode.</span></span> <span data-ttu-id="ba219-146">Zprávy jsou vždy zapisovány do datových proudů.</span><span class="sxs-lookup"><span data-stu-id="ba219-146">Messages are always written to streams.</span></span> <span data-ttu-id="ba219-147">Pokud přenos musí zprávu ukládat do vyrovnávací paměti, poskytne datový proud, který ukládá do vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="ba219-147">If the transport must buffer the message, it provides a stream that does the buffering.</span></span>  
  
 <span data-ttu-id="ba219-148">Zbytek členů pracuje s obsahem podpory, typy médií a <xref:System.ServiceModel.Channels.MessageEncoder.MessageVersion%2A>.</span><span class="sxs-lookup"><span data-stu-id="ba219-148">The rest of the members work with support content, media types, and <xref:System.ServiceModel.Channels.MessageEncoder.MessageVersion%2A>.</span></span> <span data-ttu-id="ba219-149">Přenos volá tyto metody kodéru k otestování, zda je možné příchozí zprávu dekódovat, nebo zda je odchozí zpráva platná pro tento kodér.</span><span class="sxs-lookup"><span data-stu-id="ba219-149">The transport calls these encoder methods to test whether the incoming message can be decoded by it, or to determine if the outgoing message is valid for this encoder.</span></span>  
  
 <span data-ttu-id="ba219-150">Každé ze tří implementací kodéru přidá vlastnosti, které jsou relevantní pro konkrétní kódování, a je plně konfigurovatelné.</span><span class="sxs-lookup"><span data-stu-id="ba219-150">Each of the three encoder implementations adds properties that are relevant to the specific encodings and is fully configurable.</span></span> <span data-ttu-id="ba219-151">Kodéry také zveřejňují kvóty čtecího zařízení, které mají zabezpečené výchozí hodnoty.</span><span class="sxs-lookup"><span data-stu-id="ba219-151">The encoders also expose reader quotas that have secure defaults.</span></span> <span data-ttu-id="ba219-152">Diskuzi o kvótách najdete v tématu Infrastruktura XML.</span><span class="sxs-lookup"><span data-stu-id="ba219-152">See XML Infrastructure for a discussion of the quotas.</span></span>  
  
## <a name="features-of-system-provided-encoders"></a><span data-ttu-id="ba219-153">Funkce kodérů poskytovaných systémem</span><span class="sxs-lookup"><span data-stu-id="ba219-153">Features of System-Provided Encoders</span></span>  
 <span data-ttu-id="ba219-154">Kodéry poskytované systémem obsahují řadu funkcí.</span><span class="sxs-lookup"><span data-stu-id="ba219-154">There are a number of features provided by the system-provided encoders.</span></span>  
  
### <a name="pooling"></a><span data-ttu-id="ba219-155">Sdružování</span><span class="sxs-lookup"><span data-stu-id="ba219-155">Pooling</span></span>  
 <span data-ttu-id="ba219-156">Každé z těchto implementací kodéru se pokusí co nejvíce vyřadit z fondu.</span><span class="sxs-lookup"><span data-stu-id="ba219-156">Each of the encoder implementations tries to pool as much as possible.</span></span> <span data-ttu-id="ba219-157">Snížení počtu přidělení je klíčovým způsobem, jak vylepšit výkon spravovaného kódu.</span><span class="sxs-lookup"><span data-stu-id="ba219-157">Reducing allocations is a key way to improve the performance of managed code.</span></span> <span data-ttu-id="ba219-158">Pro dosažení tohoto sdružování implementují implementace `SynchronizedPool` třídu.</span><span class="sxs-lookup"><span data-stu-id="ba219-158">To accomplish this pooling, the implementations use the `SynchronizedPool` class.</span></span> <span data-ttu-id="ba219-159">C# Soubor obsahuje popis dalších optimalizace používaných touto třídou.</span><span class="sxs-lookup"><span data-stu-id="ba219-159">The C# file contains a description of the additional optimizations used by this class.</span></span>  
  
 <span data-ttu-id="ba219-160"><xref:System.Xml.XmlDictionaryReader>a <xref:System.Xml.XmlDictionaryWriter> instance jsou sdružené a znovu inicializované, aby nedocházelo k přidělování nových zpráv pro každou zprávu.</span><span class="sxs-lookup"><span data-stu-id="ba219-160"><xref:System.Xml.XmlDictionaryReader> and <xref:System.Xml.XmlDictionaryWriter> instances are pooled and reinitialized to prevent allocating new ones for each message.</span></span> <span data-ttu-id="ba219-161">Pro čtenáře, `OnClose` zpětné volání Redeklarace čtenáře při `Close()` volání metody.</span><span class="sxs-lookup"><span data-stu-id="ba219-161">For the readers, an `OnClose` callback reclaims the reader when `Close()` is called.</span></span> <span data-ttu-id="ba219-162">Kodér také recykluje některé objekty stavu zprávy používané při sestavování zpráv.</span><span class="sxs-lookup"><span data-stu-id="ba219-162">The encoder also recycles some message state objects used when constructing messages.</span></span> <span data-ttu-id="ba219-163">Velikosti těchto fondů lze konfigurovat pomocí `MaxReadPoolSize` vlastností a `MaxWritePoolSize` na každé ze tří tříd odvozených z <xref:System.ServiceModel.Channels.MessageEncodingBindingElement>.</span><span class="sxs-lookup"><span data-stu-id="ba219-163">The sizes of these pools are configurable by the `MaxReadPoolSize` and `MaxWritePoolSize` properties on each of the three classes derived from <xref:System.ServiceModel.Channels.MessageEncodingBindingElement>.</span></span>  
  
### <a name="binary-encoding"></a><span data-ttu-id="ba219-164">Binární kódování</span><span class="sxs-lookup"><span data-stu-id="ba219-164">Binary Encoding</span></span>  
 <span data-ttu-id="ba219-165">Když binární kódování používá relace, řetězec dynamického slovníku musí být sdělen příjemci zprávy.</span><span class="sxs-lookup"><span data-stu-id="ba219-165">When binary encoding uses sessions, the dynamic dictionary string must be communicated to the receiver of the message.</span></span> <span data-ttu-id="ba219-166">K tomu slouží předpona zprávy s dynamickými řetězci slovníku.</span><span class="sxs-lookup"><span data-stu-id="ba219-166">This is done by prefixing the message with the dynamic dictionary strings.</span></span> <span data-ttu-id="ba219-167">Přijímač odliští řetězce, přidá je do relace a zpracuje zprávu.</span><span class="sxs-lookup"><span data-stu-id="ba219-167">The receiver strips off the strings, adds them to the session, and processes the message.</span></span> <span data-ttu-id="ba219-168">Správné předání řetězců slovníku vyžaduje, aby transport byl uložen do vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="ba219-168">Correctly passing dictionary strings requires that the transport be buffered.</span></span>  
  
 <span data-ttu-id="ba219-169">Řetězce jsou připojeny k zprávě pomocí interní `AddSessionInformationToMessage` metody.</span><span class="sxs-lookup"><span data-stu-id="ba219-169">The strings are appended to the message by an internal `AddSessionInformationToMessage` method.</span></span> <span data-ttu-id="ba219-170">Přidá řetězce jako UTF-8 do přední části zprávy s předponou.</span><span class="sxs-lookup"><span data-stu-id="ba219-170">It adds the strings as UTF-8 to the front of the message prefixed with their length.</span></span> <span data-ttu-id="ba219-171">Celá hlavička slovníku je pak předpona s délkou jejich dat.</span><span class="sxs-lookup"><span data-stu-id="ba219-171">The entire dictionary header is then prefixed with the length of its data.</span></span> <span data-ttu-id="ba219-172">Reverzní operace provádí interní `ExtractSessionInformationFromMessage` metodu.</span><span class="sxs-lookup"><span data-stu-id="ba219-172">The reverse operation is performed by an internal `ExtractSessionInformationFromMessage` method.</span></span>  
  
 <span data-ttu-id="ba219-173">Kromě zpracování dynamických klíčů slovníku se zprávy relací s vyrovnávací pamětí přijímají jedinečným způsobem.</span><span class="sxs-lookup"><span data-stu-id="ba219-173">In addition to processing dynamic dictionary keys, buffered sessionful messages are received in a unique way.</span></span> <span data-ttu-id="ba219-174">Místo vytvoření čtecího modulu v dokumentu a jeho zpracování použije binární kodér interní `MessagePatterns` třídu k dekonstrukci binárního datového proudu.</span><span class="sxs-lookup"><span data-stu-id="ba219-174">Instead of creating a reader over the document and processing it, the binary encoder uses the internal `MessagePatterns` class to deconstruct the binary stream.</span></span> <span data-ttu-id="ba219-175">Výsledkem je, že většina zpráv má určitou sadu hlaviček, která se zobrazí v určitém pořadí při generování službou WCF.</span><span class="sxs-lookup"><span data-stu-id="ba219-175">The idea is that most messages have a certain set of headers that show up in a certain order when generated by WCF.</span></span> <span data-ttu-id="ba219-176">Systém vzorků rozdělí zprávy na základě toho, co očekává.</span><span class="sxs-lookup"><span data-stu-id="ba219-176">The pattern system breaks the message apart based on what it expects.</span></span> <span data-ttu-id="ba219-177">Pokud je úspěšná, inicializuje <xref:System.ServiceModel.Channels.MessageHeaders> objekt bez analýzy XML.</span><span class="sxs-lookup"><span data-stu-id="ba219-177">If it is successful, it initializes a <xref:System.ServiceModel.Channels.MessageHeaders> object without parsing the XML.</span></span> <span data-ttu-id="ba219-178">V takovém případě se vrátí zpět ke standardní metodě.</span><span class="sxs-lookup"><span data-stu-id="ba219-178">If not, it falls back to the standard method.</span></span>  
  
### <a name="mtom-encoding"></a><span data-ttu-id="ba219-179">Kódování MTOM</span><span class="sxs-lookup"><span data-stu-id="ba219-179">MTOM Encoding</span></span>  
 <span data-ttu-id="ba219-180">Třída má další vlastnost konfigurace s názvem <xref:System.ServiceModel.Channels.MtomMessageEncodingBindingElement.MaxBufferSize%2A>. <xref:System.ServiceModel.Channels.MtomMessageEncodingBindingElement></span><span class="sxs-lookup"><span data-stu-id="ba219-180">The <xref:System.ServiceModel.Channels.MtomMessageEncodingBindingElement> class has an additional configuration property called <xref:System.ServiceModel.Channels.MtomMessageEncodingBindingElement.MaxBufferSize%2A>.</span></span> <span data-ttu-id="ba219-181">Tím se umístí horní mez rozsahu dat, ke kterým může během procesu čtení zprávy docházet vyrovnávací paměť.</span><span class="sxs-lookup"><span data-stu-id="ba219-181">This places an upper bound on how much data it is allowed to buffer during the process of reading a message.</span></span> <span data-ttu-id="ba219-182">Aby bylo možné překompilovat všechny části MIME do jediné zprávy, může být nutné ukládat do vyrovnávací paměti sadu informací XML (informační sada) nebo jiné části MIME.</span><span class="sxs-lookup"><span data-stu-id="ba219-182">The XML Information Set (Infoset), or other MIME parts, may need to be buffered to reassemble all the MIME parts into a single message.</span></span>  
  
 <span data-ttu-id="ba219-183">Pro správnou práci s protokolem HTTP, interní třída kodéru zpráv MTOM poskytuje některá interní `GetContentType` rozhraní API pro (která jsou také `WriteMessage`interní) a, která je veřejná a může být přepsána.</span><span class="sxs-lookup"><span data-stu-id="ba219-183">To work correctly with HTTP, the internal MTOM message encoder class provides some internal APIs for `GetContentType` (which is also internal) and `WriteMessage`, which is public and can be overridden.</span></span> <span data-ttu-id="ba219-184">K zajištění toho, aby hodnoty hlaviček protokolu HTTP souhlasily s hodnotami v hlavičkách MIME, musí být k větší komunikace.</span><span class="sxs-lookup"><span data-stu-id="ba219-184">More communication must occur to ensure values in the HTTP headers agree with values in the MIME headers.</span></span>  
  
 <span data-ttu-id="ba219-185">Kodér zprávy MTOM interně používá čtečky textu WCF a je podobný textu kodéru.</span><span class="sxs-lookup"><span data-stu-id="ba219-185">Internally, the MTOM message encoder uses WCF's text readers, and is similar to the Text encoder.</span></span> <span data-ttu-id="ba219-186">Hlavním rozdílem je to, že optimalizuje velké bloky binárních souborů nebo "binárních velkých objektů" (blobů), protože je před vložením do bajtů zpráv nepřevádí na kódování Base-64.</span><span class="sxs-lookup"><span data-stu-id="ba219-186">The main difference is that it optimizes large chunks of binary, or "Binary Large Objects" (BLOBs), by not converting them to Base-64 encoding prior to being embedded into the message bytes.</span></span> <span data-ttu-id="ba219-187">Místo toho se tyto objekty blob uchovávají a odkazují jako přílohy MIME.</span><span class="sxs-lookup"><span data-stu-id="ba219-187">Instead, these BLOBs are kept extracted, and referenced as the MIME attachments.</span></span>  
  
## <a name="writing-your-own-encoder"></a><span data-ttu-id="ba219-188">Psaní vlastního kodéru</span><span class="sxs-lookup"><span data-stu-id="ba219-188">Writing your own Encoder</span></span>  
 <span data-ttu-id="ba219-189">Chcete-li implementovat vlastní kodér zpráv, je nutné poskytnout vlastní implementace následujících abstraktních základních tříd:</span><span class="sxs-lookup"><span data-stu-id="ba219-189">To implement your own custom message encoder, you must provide custom implementations of the following abstract base classes:</span></span>  
  
- <xref:System.ServiceModel.Channels.MessageEncoder>  
  
- <xref:System.ServiceModel.Channels.MessageEncoderFactory>  
  
- <xref:System.ServiceModel.Channels.MessageEncodingBindingElement>  
  
 <span data-ttu-id="ba219-190">Převod z reprezentace zprávy v paměti na reprezentaci, která může být zapsána do datového proudu, je zapouzdřen v rámci <xref:System.ServiceModel.Channels.MessageEncoder> třídy, která slouží jako továrna pro čtečky XML a zapisovače XML, které podporují konkrétní typy kódování XML.</span><span class="sxs-lookup"><span data-stu-id="ba219-190">Converting from the in-memory representation of a message to a representation that can be written to a stream is encapsulated within the <xref:System.ServiceModel.Channels.MessageEncoder> class, which serves as a factory for XML readers and XML writers that support specific types of XML encodings.</span></span>  
  
- <span data-ttu-id="ba219-191">Klíčové metody této třídy, které je nutné přepsat, jsou:</span><span class="sxs-lookup"><span data-stu-id="ba219-191">The key methods of this class that you must override are:</span></span>  
  
- <span data-ttu-id="ba219-192"><xref:System.ServiceModel.Channels.MessageEncoder.WriteMessage%2A>který přebírá <xref:System.ServiceModel.Channels.MessageEncodingBindingElement> objekt a zapisuje ho <xref:System.IO.Stream> do objektu.</span><span class="sxs-lookup"><span data-stu-id="ba219-192"><xref:System.ServiceModel.Channels.MessageEncoder.WriteMessage%2A> which takes a <xref:System.ServiceModel.Channels.MessageEncodingBindingElement> object and writes it into a <xref:System.IO.Stream> object.</span></span>  
  
- <span data-ttu-id="ba219-193"><xref:System.ServiceModel.Channels.MessageEncoder.ReadMessage%2A>který přebírá <xref:System.IO.Stream> objekt a maximální velikost hlavičky a <xref:System.ServiceModel.Channels.Message> vrací objekt.</span><span class="sxs-lookup"><span data-stu-id="ba219-193"><xref:System.ServiceModel.Channels.MessageEncoder.ReadMessage%2A> which takes a <xref:System.IO.Stream> object and a maximum header size and returns a <xref:System.ServiceModel.Channels.Message> object.</span></span>  
  
 <span data-ttu-id="ba219-194">Jedná se o kód, který zapisujete do těchto metod, který zpracovává převod mezi standardním transportním protokolem a vlastním kódováním.</span><span class="sxs-lookup"><span data-stu-id="ba219-194">It is the code you write in these methods that handles conversion between the standard transport protocol, and your customized encoding.</span></span>  
  
 <span data-ttu-id="ba219-195">Dále je nutné vytvořit kód třídy továrny, která vytvoří vlastní kodér.</span><span class="sxs-lookup"><span data-stu-id="ba219-195">Next you need to code a factory class that creates your custom encoder.</span></span> <span data-ttu-id="ba219-196">Přepište <xref:System.ServiceModel.Channels.MessageEncoder>a vraťte instanci svého vlastního. <xref:System.ServiceModel.Channels.MessageEncoderFactory.Encoder%2A></span><span class="sxs-lookup"><span data-stu-id="ba219-196">Override the <xref:System.ServiceModel.Channels.MessageEncoderFactory.Encoder%2A> to return an instance of your custom <xref:System.ServiceModel.Channels.MessageEncoder>.</span></span>  
  
 <span data-ttu-id="ba219-197">Pak připojte vlastní <xref:System.ServiceModel.Channels.MessageEncoderFactory> ke zásobníku prvků vazby, který se používá ke konfiguraci služby nebo klienta, <xref:System.ServiceModel.Channels.MessageEncodingBindingElement.CreateMessageEncoderFactory%2A> přepsáním metody, která vrátí instanci tohoto objektu pro vytváření.</span><span class="sxs-lookup"><span data-stu-id="ba219-197">Then connect your custom <xref:System.ServiceModel.Channels.MessageEncoderFactory> to the binding element stack used to configure the service or client by overriding the <xref:System.ServiceModel.Channels.MessageEncodingBindingElement.CreateMessageEncoderFactory%2A> method to return an instance of this factory.</span></span>  
  
 <span data-ttu-id="ba219-198">Pro WCF jsou k dispozici dvě ukázky, které ilustrují tento proces s ukázkovým kódem: [Vlastní kodér zpráv: Vlastní kodér](../samples/custom-message-encoder-custom-text-encoder.md) textu a [vlastní kodér zpráv: Kompresní kodér](../samples/custom-message-encoder-compression-encoder.md).</span><span class="sxs-lookup"><span data-stu-id="ba219-198">There are two samples provided with WCF that illustrate this process with sample code: [Custom Message Encoder: Custom Text Encoder](../samples/custom-message-encoder-custom-text-encoder.md) and [Custom Message Encoder: Compression Encoder](../samples/custom-message-encoder-compression-encoder.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="ba219-199">Viz také:</span><span class="sxs-lookup"><span data-stu-id="ba219-199">See also</span></span>

- <xref:System.ServiceModel.Channels.MessageEncodingBindingElement>
- <xref:System.ServiceModel.Channels.MessageEncoderFactory>
- <xref:System.ServiceModel.Channels.MessageEncoder>
- [<span data-ttu-id="ba219-200">Strukturální přehled přenosu dat</span><span class="sxs-lookup"><span data-stu-id="ba219-200">Data Transfer Architectural Overview</span></span>](../feature-details/data-transfer-architectural-overview.md)
- [<span data-ttu-id="ba219-201">Výběr kodéru zprávy</span><span class="sxs-lookup"><span data-stu-id="ba219-201">Choosing a Message Encoder</span></span>](../feature-details/choosing-a-message-encoder.md)
- [<span data-ttu-id="ba219-202">Volba přenosu</span><span class="sxs-lookup"><span data-stu-id="ba219-202">Choosing a Transport</span></span>](../feature-details/choosing-a-transport.md)
