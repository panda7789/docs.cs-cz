---
title: Přepsání identity služby kvůli ověřování
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: d613a22b-07d7-41a4-bada-1adc653b9b5d
ms.openlocfilehash: d1c0cc487d8deea7045ee9653d1eae9b73ec864f
ms.sourcegitcommit: 7e2128d4a4c45b4274bea3b8e5760d4694569ca1
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 01/14/2020
ms.locfileid: "75938020"
---
# <a name="overriding-the-identity-of-a-service-for-authentication"></a><span data-ttu-id="dc208-102">Přepsání identity služby kvůli ověřování</span><span class="sxs-lookup"><span data-stu-id="dc208-102">Overriding the Identity of a Service for Authentication</span></span>
<span data-ttu-id="dc208-103">Obvykle není nutné nastavovat identitu na službě, protože výběr typu přihlašovacích údajů klienta Určuje typ identity, která je vystavena v metadatech služby.</span><span class="sxs-lookup"><span data-stu-id="dc208-103">Typically, you do not have to set the identity on a service because the selection of a client credential type dictates the type of identity exposed in the service metadata.</span></span> <span data-ttu-id="dc208-104">Například následující konfigurační kód používá [> prvek\<WSHttpBinding](../../configure-apps/file-schema/wcf/wshttpbinding.md) a nastaví atribut `clientCredentialType` na Windows.</span><span class="sxs-lookup"><span data-stu-id="dc208-104">For example, the following configuration code uses the [\<wsHttpBinding>](../../configure-apps/file-schema/wcf/wshttpbinding.md) element and sets the `clientCredentialType` attribute to Windows.</span></span>  

 <span data-ttu-id="dc208-105">Následující fragment kódu jazyka WSDL (Web Services Description Language) zobrazuje identitu dříve definovaného koncového bodu.</span><span class="sxs-lookup"><span data-stu-id="dc208-105">The following Web Services Description Language (WSDL) fragment shows the identity for the endpoint previously defined.</span></span> <span data-ttu-id="dc208-106">V tomto příkladu je služba spuštěná jako Samoobslužná služba v rámci konkrétního uživatelského účtu (username@contoso.com), a proto Identita hlavního názvu uživatele (UPN) obsahuje název účtu.</span><span class="sxs-lookup"><span data-stu-id="dc208-106">In this example, the service is running as a self-hosted service under a particular user account (username@contoso.com) and therefore the user principal name (UPN) identity contains the account name.</span></span> <span data-ttu-id="dc208-107">Hlavní název uživatele (UPN) je také označován jako přihlašovací jméno uživatele v doméně systému Windows.</span><span class="sxs-lookup"><span data-stu-id="dc208-107">The UPN is also known as the user logon name in a Windows domain.</span></span>  

 <span data-ttu-id="dc208-108">Ukázkovou aplikaci, která demonstruje nastavení identity, najdete v tématu [Ukázka identity služby](../samples/service-identity-sample.md).</span><span class="sxs-lookup"><span data-stu-id="dc208-108">For a sample application that demonstrates identity setting, see [Service Identity Sample](../samples/service-identity-sample.md).</span></span> <span data-ttu-id="dc208-109">Další informace o identitě služby najdete v tématu [identita služby a ověřování](../feature-details/service-identity-and-authentication.md).</span><span class="sxs-lookup"><span data-stu-id="dc208-109">For more information about service identity, see [Service Identity and Authentication](../feature-details/service-identity-and-authentication.md).</span></span>  
  
## <a name="kerberos-authentication-and-identity"></a><span data-ttu-id="dc208-110">Ověřování a identita protokolu Kerberos</span><span class="sxs-lookup"><span data-stu-id="dc208-110">Kerberos Authentication and Identity</span></span>  
 <span data-ttu-id="dc208-111">Ve výchozím nastavení platí, že pokud je služba nakonfigurována pro použití pověření systému Windows, [\<prvek identity >](../../configure-apps/file-schema/wcf/identity.md) , který obsahuje [\<userPrincipalName >](../../configure-apps/file-schema/wcf/userprincipalname.md) nebo [\<prvek servicePrincipalName >](../../configure-apps/file-schema/wcf/serviceprincipalname.md) element je vygenerován v jazyce WSDL.</span><span class="sxs-lookup"><span data-stu-id="dc208-111">By default, when a service is configured to use a Windows credential, an [\<identity>](../../configure-apps/file-schema/wcf/identity.md) element that contains a [\<userPrincipalName>](../../configure-apps/file-schema/wcf/userprincipalname.md) or [\<servicePrincipalName>](../../configure-apps/file-schema/wcf/serviceprincipalname.md) element is generated in the WSDL.</span></span> <span data-ttu-id="dc208-112">Pokud je služba spuštěna pod účtem `LocalSystem`, `LocalService`nebo `NetworkService`, hlavní název služby (SPN) je ve výchozím nastavení generován ve formě `host/`\<*název hostitele*> protože tyto účty mají přístup k datům hlavního názvu počítače.</span><span class="sxs-lookup"><span data-stu-id="dc208-112">If the service is running under the `LocalSystem`, `LocalService`, or `NetworkService` account, a service principal name (SPN) is generated by default in the form of `host/`\<*hostname*> because those accounts have access to the computer's SPN data.</span></span> <span data-ttu-id="dc208-113">Pokud je služba spuštěná pod jiným účtem, Windows Communication Foundation (WCF) vygeneruje hlavní název uživatele (UPN) ve formátu \<*username*>@<*DomainName*`>`.</span><span class="sxs-lookup"><span data-stu-id="dc208-113">If the service is running under a different account, Windows Communication Foundation (WCF) generates a UPN in the form of \<*username*>@<*domainName*`>`.</span></span> <span data-ttu-id="dc208-114">K tomu dochází, protože ověřování protokolem Kerberos vyžaduje, aby klient zadal hlavní název uživatele (UPN) nebo hlavní název služby, aby mohl ověřit službu.</span><span class="sxs-lookup"><span data-stu-id="dc208-114">This occurs because Kerberos authentication requires that a UPN or SPN be supplied to the client to authenticate the service.</span></span>  
  
 <span data-ttu-id="dc208-115">Pomocí nástroje Setspn. exe můžete také zaregistrovat další hlavní název služby (SPN) s účtem služby v doméně.</span><span class="sxs-lookup"><span data-stu-id="dc208-115">You can also use the Setspn.exe tool to register an additional SPN with a service's account in a domain.</span></span> <span data-ttu-id="dc208-116">Pak můžete jako identitu služby použít hlavní název služby (SPN).</span><span class="sxs-lookup"><span data-stu-id="dc208-116">You can then use the SPN as the identity of the service.</span></span> <span data-ttu-id="dc208-117">Pokud si chcete nástroj stáhnout, přečtěte si téma [Nástroj Windows 2000 Resource Kit: Setspn. exe](https://go.microsoft.com/fwlink/?LinkId=91752).</span><span class="sxs-lookup"><span data-stu-id="dc208-117">To download the tool, see [Windows 2000 Resource Kit Tool : Setspn.exe](https://go.microsoft.com/fwlink/?LinkId=91752).</span></span> <span data-ttu-id="dc208-118">Další informace o tomto nástroji najdete v tématu [Přehled nástroje Setspn](https://docs.microsoft.com/previous-versions/windows/it-pro/windows-server-2003/cc773257(v=ws.10)).</span><span class="sxs-lookup"><span data-stu-id="dc208-118">For more information about the tool, see [Setspn Overview](https://docs.microsoft.com/previous-versions/windows/it-pro/windows-server-2003/cc773257(v=ws.10)).</span></span>  
  
> [!NOTE]
> <span data-ttu-id="dc208-119">Chcete-li použít typ přihlašovacích údajů systému Windows bez vyjednávání, musí mít uživatelský účet služby přístup k hlavnímu názvu služby (SPN), který je zaregistrován v doméně služby Active Directory.</span><span class="sxs-lookup"><span data-stu-id="dc208-119">To use the Windows credential type without negotiation, the service's user account must have access to the SPN that is registered with the Active Directory domain.</span></span> <span data-ttu-id="dc208-120">Můžete to udělat následujícími způsoby:</span><span class="sxs-lookup"><span data-stu-id="dc208-120">You can do this in the following ways:</span></span>  
  
- <span data-ttu-id="dc208-121">Ke spuštění služby použijte účet NetworkService nebo LocalSystem.</span><span class="sxs-lookup"><span data-stu-id="dc208-121">Use the NetworkService or LocalSystem account to run your service.</span></span> <span data-ttu-id="dc208-122">Vzhledem k tomu, že tyto účty mají přístup k hlavnímu názvu služby počítače, který se vytvoří, když se počítač připojí k doméně služby Active Directory, služba WCF automaticky vygeneruje správný element SPN v rámci koncového bodu služby v metadatech služby (WSDL).</span><span class="sxs-lookup"><span data-stu-id="dc208-122">Because those accounts have access to the machine SPN that is established when the machine joins the Active Directory domain, WCF automatically generates the proper SPN element inside the service's endpoint in the service's metadata (WSDL).</span></span>  
  
- <span data-ttu-id="dc208-123">Ke spuštění služby použijte libovolný účet domény služby Active Directory.</span><span class="sxs-lookup"><span data-stu-id="dc208-123">Use an arbitrary Active Directory domain account to run your service.</span></span> <span data-ttu-id="dc208-124">V takovém případě vytvořte hlavní název služby (SPN) pro účet domény, který můžete provést pomocí nástroje Setspn. exe Utility.</span><span class="sxs-lookup"><span data-stu-id="dc208-124">In this case, establish an SPN for that domain account, which you can do by using the Setspn.exe utility tool.</span></span> <span data-ttu-id="dc208-125">Po vytvoření hlavního názvu služby (SPN) pro účet služby nakonfigurujte WCF pro publikování tohoto hlavního názvu služby do klientů služby prostřednictvím metadat (WSDL).</span><span class="sxs-lookup"><span data-stu-id="dc208-125">Once you create the SPN for the service's account, configure WCF to publish that SPN to the service's clients through its metadata (WSDL).</span></span> <span data-ttu-id="dc208-126">To se provádí nastavením identity koncového bodu pro vystavený koncový bod, a to buď prostřednictvím konfiguračního souboru aplikace, nebo kódu.</span><span class="sxs-lookup"><span data-stu-id="dc208-126">This is done by setting the endpoint identity for the exposed endpoint, either through an application configuration file or code.</span></span>  
  
 <span data-ttu-id="dc208-127">Další informace o SPN, protokolu Kerberos a službě Active Directory najdete v tématu [Kerberos Technical dodatk pro Windows](https://docs.microsoft.com/previous-versions/msp-n-p/ff649429(v=pandp.10)).</span><span class="sxs-lookup"><span data-stu-id="dc208-127">For more information about SPNs, the Kerberos protocol, and Active Directory, see [Kerberos Technical Supplement for Windows](https://docs.microsoft.com/previous-versions/msp-n-p/ff649429(v=pandp.10)).</span></span>  
  
### <a name="when-spn-or-upn-equals-the-empty-string"></a><span data-ttu-id="dc208-128">Pokud hlavní název služby (SPN) nebo hlavní název uživatele se rovná prázdnému řetězci</span><span class="sxs-lookup"><span data-stu-id="dc208-128">When SPN or UPN Equals the Empty String</span></span>  
 <span data-ttu-id="dc208-129">Pokud nastavíte hlavní název služby (SPN) nebo hlavní název uživatele (UPN) na hodnotu prázdný řetězec, dojde v závislosti na úrovni zabezpečení a používaném režimu ověřování k různým účelům.</span><span class="sxs-lookup"><span data-stu-id="dc208-129">If you set the SPN or UPN equal to an empty string, a number of different things happen, depending on the security level and authentication mode being used:</span></span>  
  
- <span data-ttu-id="dc208-130">Pokud používáte zabezpečení na úrovni přenosu, zvolí se ověřování NT LanMan (NTLM).</span><span class="sxs-lookup"><span data-stu-id="dc208-130">If you are using transport level security, NT LanMan (NTLM) authentication is chosen.</span></span>  
  
- <span data-ttu-id="dc208-131">Pokud používáte zabezpečení na úrovni zpráv, ověřování může selhat v závislosti na režimu ověřování:</span><span class="sxs-lookup"><span data-stu-id="dc208-131">If you are using message level security, authentication may fail, depending on the authentication mode:</span></span>  
  
- <span data-ttu-id="dc208-132">Pokud používáte režim `spnego` a atribut `AllowNtlm` je nastaven na `false`, ověřování se nezdaří.</span><span class="sxs-lookup"><span data-stu-id="dc208-132">If you are using `spnego` mode and the `AllowNtlm` attribute is set to `false`, authentication fail.</span></span>  
  
- <span data-ttu-id="dc208-133">Pokud používáte režim `spnego` a atribut `AllowNtlm` je nastaven na `true`, ověřování selže, pokud je hlavní název uživatele prázdný, ale úspěch je prázdný, pokud je hlavní název služby prázdný.</span><span class="sxs-lookup"><span data-stu-id="dc208-133">If you are using `spnego` mode and the `AllowNtlm` attribute is set to `true`, authentication fails if the UPN is empty, but succeeds if the SPN is empty.</span></span>  
  
- <span data-ttu-id="dc208-134">Pokud používáte protokol Kerberos Direct (označovaný také jako "jeden snímek"), ověření se nepovede.</span><span class="sxs-lookup"><span data-stu-id="dc208-134">If you are using Kerberos direct (also known as "one-shot"), authentication fails.</span></span>  
  
### <a name="using-the-identity-element-in-configuration"></a><span data-ttu-id="dc208-135">Použití prvku \<> identity v konfiguraci</span><span class="sxs-lookup"><span data-stu-id="dc208-135">Using the \<identity> Element in Configuration</span></span>  
 <span data-ttu-id="dc208-136">Pokud změníte typ přihlašovacích údajů klienta ve vazbě, která byla dříve zobrazena na certifikát`,` pak vygenerovaný prvek WSDL obsahuje serializovaný certifikát X. 509 pro hodnotu identity, jak je znázorněno v následujícím kódu.</span><span class="sxs-lookup"><span data-stu-id="dc208-136">If you change the client credential type in the binding previously shown to Certificate`,` then the generated WSDL contains a Base64 serialized X.509 certificate for the identity value as shown in the following code.</span></span> <span data-ttu-id="dc208-137">Toto je výchozí nastavení pro všechny typy přihlašovacích údajů klienta jiné než Windows.</span><span class="sxs-lookup"><span data-stu-id="dc208-137">This is the default for all client credential types other than Windows.</span></span>  

 <span data-ttu-id="dc208-138">Můžete změnit hodnotu výchozí identity služby nebo změnit typ identity pomocí elementu <`identity`> v konfiguraci nebo nastavením identity v kódu.</span><span class="sxs-lookup"><span data-stu-id="dc208-138">You can change the value of the default service identity or change the type of the identity by using the <`identity`> element in configuration or by setting the identity in code.</span></span> <span data-ttu-id="dc208-139">Následující konfigurační kód nastaví identitu DNS (Domain Name System) s hodnotou `contoso.com`.</span><span class="sxs-lookup"><span data-stu-id="dc208-139">The following configuration code sets a domain name system (DNS) identity with the value `contoso.com`.</span></span>  

### <a name="setting-identity-programmatically"></a><span data-ttu-id="dc208-140">Nastavování identity prostřednictvím kódu programu</span><span class="sxs-lookup"><span data-stu-id="dc208-140">Setting Identity Programmatically</span></span>  
 <span data-ttu-id="dc208-141">Vaše služba nemusí explicitně určovat identitu, protože ji WCF automaticky určí.</span><span class="sxs-lookup"><span data-stu-id="dc208-141">Your service does not have to explicitly specify an identity, because WCF automatically determines it.</span></span> <span data-ttu-id="dc208-142">Služba WCF ale umožňuje v případě potřeby zadat identitu pro koncový bod.</span><span class="sxs-lookup"><span data-stu-id="dc208-142">However, WCF allows you to specify an identity on an endpoint, if required.</span></span> <span data-ttu-id="dc208-143">Následující kód přidá nový koncový bod služby s konkrétní identitou DNS.</span><span class="sxs-lookup"><span data-stu-id="dc208-143">The following code adds a new service endpoint with a specific DNS identity.</span></span>  
  
 [!code-csharp[C_Identity#5](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_identity/cs/source.cs#5)]
 [!code-vb[C_Identity#5](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_identity/vb/source.vb#5)]  
  
## <a name="see-also"></a><span data-ttu-id="dc208-144">Viz také:</span><span class="sxs-lookup"><span data-stu-id="dc208-144">See also</span></span>

- [<span data-ttu-id="dc208-145">Postupy: Vytvoření vlastního ověřovatele identity klientů</span><span class="sxs-lookup"><span data-stu-id="dc208-145">How to: Create a Custom Client Identity Verifier</span></span>](how-to-create-a-custom-client-identity-verifier.md)
- [<span data-ttu-id="dc208-146">Identita a ověřování služby</span><span class="sxs-lookup"><span data-stu-id="dc208-146">Service Identity and Authentication</span></span>](../feature-details/service-identity-and-authentication.md)
