---
title: Dávkování zpráv v transakci
ms.date: 03/30/2017
helpviewer_keywords:
- batching messages [WCF]
ms.assetid: 53305392-e82e-4e89-aedc-3efb6ebcd28c
ms.openlocfilehash: 2d820087973e689514a0a19a7adc912f49e9d0a2
ms.sourcegitcommit: 558d78d2a68acd4c95ef23231c8b4e4c7bac3902
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/09/2019
ms.locfileid: "59310522"
---
# <a name="batching-messages-in-a-transaction"></a><span data-ttu-id="975d5-102">Dávkování zpráv v transakci</span><span class="sxs-lookup"><span data-stu-id="975d5-102">Batching Messages in a Transaction</span></span>
<span data-ttu-id="975d5-103">Ve frontě aplikací používání transakcí k zajištění správnosti a spolehlivé doručování zpráv.</span><span class="sxs-lookup"><span data-stu-id="975d5-103">Queued applications use transactions to ensure correctness and reliable delivery of messages.</span></span> <span data-ttu-id="975d5-104">Transakce, ale jsou nákladný provoz a může výrazně snížit propustnost zpráv.</span><span class="sxs-lookup"><span data-stu-id="975d5-104">Transactions, however, are expensive operations and can dramatically reduce message throughput.</span></span> <span data-ttu-id="975d5-105">Jedním způsobem, jak zlepšit propustnost zpráv je, aby aplikaci číst a zpracovávat více zpráv v rámci jedné transakce.</span><span class="sxs-lookup"><span data-stu-id="975d5-105">One way to improve message throughput is to have an application read and process multiple messages within a single transaction.</span></span> <span data-ttu-id="975d5-106">Nutný kompromis je mezi výkonem a možností obnovení: jak se zvyšuje počet zpráv v dávce, postupy zločinců se množství práce obnovení, který vyžaduje, pokud jsou transakce vrácena zpět.</span><span class="sxs-lookup"><span data-stu-id="975d5-106">The trade-off is between performance and recovery: as the number of messages in a batch increases, so does the amount of recovery work that required if transactions are rolled back.</span></span> <span data-ttu-id="975d5-107">Je důležité si uvědomit rozdíl mezi dávkování zpráv v transakci a relací.</span><span class="sxs-lookup"><span data-stu-id="975d5-107">It is important to note the difference between batching messages in a transaction and sessions.</span></span> <span data-ttu-id="975d5-108">A *relace* je seskupení související zprávy, které jsou zpracovány jednu aplikaci a potvrzena jako jeden celek.</span><span class="sxs-lookup"><span data-stu-id="975d5-108">A *session* is a grouping of related messages that are processed by a single application and committed as a single unit.</span></span> <span data-ttu-id="975d5-109">Relace se běžně používají, pokud skupina související zprávy musí být zpracovány současně.</span><span class="sxs-lookup"><span data-stu-id="975d5-109">Sessions are generally used when a group of related messages must be processed together.</span></span> <span data-ttu-id="975d5-110">Příkladem je online nakupování webové stránky.</span><span class="sxs-lookup"><span data-stu-id="975d5-110">An example of this is an online shopping Web site.</span></span> <span data-ttu-id="975d5-111">*Dávky* se používají ke zpracování více, nesouvisející zprávy tak, že zpráva zvýšení propustnosti.</span><span class="sxs-lookup"><span data-stu-id="975d5-111">*Batches* are used to process multiple, unrelated messages in a way that increases message throughput.</span></span> <span data-ttu-id="975d5-112">Další informace o relacích najdete v tématu [seskupování zpráv zařazených do fronty v relaci](../../../../docs/framework/wcf/feature-details/grouping-queued-messages-in-a-session.md).</span><span class="sxs-lookup"><span data-stu-id="975d5-112">For more information about sessions, see [Grouping Queued Messages in a Session](../../../../docs/framework/wcf/feature-details/grouping-queued-messages-in-a-session.md).</span></span> <span data-ttu-id="975d5-113">Také zpracování jedné aplikace a potvrzena jako jednu jednotku zpráv v dávce, ale může být žádný vztah mezi zpráv v dávce.</span><span class="sxs-lookup"><span data-stu-id="975d5-113">Messages in a batch are also processed by a single application and committed as a single unit, but there may be no relationship between the messages in the batch.</span></span> <span data-ttu-id="975d5-114">Dávkování zpráv v transakci je optimalizace, která se nemění, jak je aplikace spuštěná.</span><span class="sxs-lookup"><span data-stu-id="975d5-114">Batching messages in a transaction is an optimization that does not change how the application runs.</span></span>  
  
## <a name="entering-batching-mode"></a><span data-ttu-id="975d5-115">Zadat režim dávkování byl</span><span class="sxs-lookup"><span data-stu-id="975d5-115">Entering Batching Mode</span></span>  
 <span data-ttu-id="975d5-116"><xref:System.ServiceModel.Description.TransactedBatchingBehavior> Dávkování řídí chování koncového bodu.</span><span class="sxs-lookup"><span data-stu-id="975d5-116">The <xref:System.ServiceModel.Description.TransactedBatchingBehavior> endpoint behavior controls batching.</span></span> <span data-ttu-id="975d5-117">Přidání tohoto chování koncového bodu na koncový bod služby říká Windows Communication Foundation (WCF) do služby batch zpráv v transakci.</span><span class="sxs-lookup"><span data-stu-id="975d5-117">Adding this endpoint behavior to a service endpoint tells Windows Communication Foundation (WCF) to batch messages in a transaction.</span></span> <span data-ttu-id="975d5-118">Ne všechny zprávy vyžadují transakci, takže pouze zprávy, které vyžadují transakce jsou umístěny v dávce, a pouze zprávy odeslané z operace označené `TransactionScopeRequired`  =  `true` a `TransactionAutoComplete`  =  `true` jsou zahrnutých do dávky.</span><span class="sxs-lookup"><span data-stu-id="975d5-118">Not all messages require a transaction, so only messages that require a transaction are placed in a batch, and only messages sent from operations marked with `TransactionScopeRequired` = `true` and `TransactionAutoComplete` = `true` are considered for a batch.</span></span> <span data-ttu-id="975d5-119">Pokud všechny operace v kontraktu služby jsou označeny `TransactionScopeRequired`  =  `false` a `TransactionAutoComplete`  =  `false`, pak režim dávkování byl se nezadal.</span><span class="sxs-lookup"><span data-stu-id="975d5-119">If all operations on the service contract are marked with `TransactionScopeRequired` = `false` and `TransactionAutoComplete` = `false`, then batching mode is never entered.</span></span>  
  
## <a name="committing-a-transaction"></a><span data-ttu-id="975d5-120">Potvrzení transakce</span><span class="sxs-lookup"><span data-stu-id="975d5-120">Committing a Transaction</span></span>  
 <span data-ttu-id="975d5-121">Dávkové transakce se potvrzeny založené na těchto faktorech:</span><span class="sxs-lookup"><span data-stu-id="975d5-121">A batched transaction is committed based on the following:</span></span>  
  
-   `MaxBatchSize`<span data-ttu-id="975d5-122">.</span><span class="sxs-lookup"><span data-stu-id="975d5-122">.</span></span> <span data-ttu-id="975d5-123">Vlastnost <xref:System.ServiceModel.Description.TransactedBatchingBehavior> chování.</span><span class="sxs-lookup"><span data-stu-id="975d5-123">A property of the <xref:System.ServiceModel.Description.TransactedBatchingBehavior> behavior.</span></span> <span data-ttu-id="975d5-124">Tato vlastnost určuje maximální počet zpráv, které se umístí do dávky.</span><span class="sxs-lookup"><span data-stu-id="975d5-124">This property determines the maximum number of messages that are placed into a batch.</span></span> <span data-ttu-id="975d5-125">Při dosažení tohoto počtu se zavazuje služby batch.</span><span class="sxs-lookup"><span data-stu-id="975d5-125">When this number is reached, the batch is committed.</span></span> <span data-ttu-id="975d5-126">Toto je hodnota není striktní limitu, je možné potvrdit dávku před přijetím tento počet zpráv.</span><span class="sxs-lookup"><span data-stu-id="975d5-126">This is value is not a strict limit, it is possible to commit a batch before receiving this number of messages.</span></span>  
  
-   `Transaction Timeout`<span data-ttu-id="975d5-127">.</span><span class="sxs-lookup"><span data-stu-id="975d5-127">.</span></span> <span data-ttu-id="975d5-128">Po uplynutí 80 procent společností z žebříčku časový limit transakce, usiluje o dávky a vytvoří novou dávku.</span><span class="sxs-lookup"><span data-stu-id="975d5-128">After 80 percent of the transaction's time-out has elapsed, the batch is committed and a new batch is created.</span></span> <span data-ttu-id="975d5-129">To znamená, že pokud 20 procent nebo méně času pro k dokončení transakce zůstává, služby batch je potvrzená.</span><span class="sxs-lookup"><span data-stu-id="975d5-129">This means that if 20 percent or less of the time given for a transaction to complete remains, the batch is committed.</span></span>  
  
-   `TransactionScopeRequired`<span data-ttu-id="975d5-130">.</span><span class="sxs-lookup"><span data-stu-id="975d5-130">.</span></span> <span data-ttu-id="975d5-131">Při zpracování dávky zprávy, pokud WCF nenajde ten, který má `TransactionScopeRequired`  =  `false`, potvrzení změn služby batch a znovu neotevře novou dávku na příjem první zprávy s `TransactionScopeRequired`  =  `true` a `TransactionAutoComplete`  = `true`.</span><span class="sxs-lookup"><span data-stu-id="975d5-131">When processing a batch of messages, if WCF finds one that has `TransactionScopeRequired` = `false`, it commits the batch and reopens a new batch on receipt of the first message with `TransactionScopeRequired` = `true` and `TransactionAutoComplete` = `true`.</span></span>  
  
-   <span data-ttu-id="975d5-132">Pokud neexistují žádné další zprávy ve frontě, je aktuální dávku potvrzené, i v případě `MaxBatchSize` nebyl dosažen nebo nebyl 80 procent společností z žebříčku transakce časový limit vypršel.</span><span class="sxs-lookup"><span data-stu-id="975d5-132">If no more messages exist in the queue, then the current batch is committed, even if the `MaxBatchSize` has not been reached or 80 percent of the transaction's time-out has not elapsed.</span></span>  
  
## <a name="leaving-batching-mode"></a><span data-ttu-id="975d5-133">Opustit režim dávkování byl</span><span class="sxs-lookup"><span data-stu-id="975d5-133">Leaving Batching Mode</span></span>  
 <span data-ttu-id="975d5-134">Pokud zpráva v dávce způsobí, že transakce pro přerušení, dojde k následujícím krokům:</span><span class="sxs-lookup"><span data-stu-id="975d5-134">If a message in a batch causes the transaction to abort, the following steps occur:</span></span>  
  
1. <span data-ttu-id="975d5-135">Celý dávku zpráv se vrátí zpět.</span><span class="sxs-lookup"><span data-stu-id="975d5-135">The entire batch of messages is rolled back.</span></span>  
  
2. <span data-ttu-id="975d5-136">Zprávy jsou jeden po druhém, dokud počet zpráv, přečtěte si, které překročí dvakrát maximální velikost dávky pro čtení.</span><span class="sxs-lookup"><span data-stu-id="975d5-136">Messages are read one at a time until the number of messages read exceeds twice the maximum batch size.</span></span>  
  
3. <span data-ttu-id="975d5-137">Režim dávky je zadat znovu.</span><span class="sxs-lookup"><span data-stu-id="975d5-137">Batch mode is re-entered.</span></span>  
  
## <a name="choosing-the-batch-size"></a><span data-ttu-id="975d5-138">Vyberete velikost dávky</span><span class="sxs-lookup"><span data-stu-id="975d5-138">Choosing the Batch Size</span></span>  
 <span data-ttu-id="975d5-139">Velikost dávky je aplikace závislá.</span><span class="sxs-lookup"><span data-stu-id="975d5-139">The size of a batch is application-dependent.</span></span> <span data-ttu-id="975d5-140">Metoda empirical je nejlepší způsob, jak dorazí na velikost optimální služby batch pro aplikaci.</span><span class="sxs-lookup"><span data-stu-id="975d5-140">The empirical method is the best way to arrive at an optimal batch size for the application.</span></span> <span data-ttu-id="975d5-141">Je důležité pamatovat při volbě velikosti dávky zvolte velikost podle vaší aplikace skutečný nasazení modelu.</span><span class="sxs-lookup"><span data-stu-id="975d5-141">It is important to remember when choosing a batch size to choose the size according to your application's actual deployment model.</span></span> <span data-ttu-id="975d5-142">Například při nasazování aplikací, pokud je třeba SQL server na vzdáleném počítači a transakce, která zahrnuje fronty a SQL server, pak velikost dávky je nejlepší určí spuštěním této přesnou konfiguraci.</span><span class="sxs-lookup"><span data-stu-id="975d5-142">For example, when deploying the application, if you need an SQL server on a remote machine and a transaction that spans the queue and the SQL server, then the batch size is best determined by running this exact configuration.</span></span>  
  
## <a name="concurrency-and-batching"></a><span data-ttu-id="975d5-143">Souběžnost a dávkové zpracování</span><span class="sxs-lookup"><span data-stu-id="975d5-143">Concurrency and Batching</span></span>  
 <span data-ttu-id="975d5-144">Pokud chcete zvýšit propustnost, můžete mít také mnoho dávky běžet souběžně.</span><span class="sxs-lookup"><span data-stu-id="975d5-144">To increase throughput, you can also have many batches run concurrently.</span></span> <span data-ttu-id="975d5-145">Nastavením `ConcurrencyMode.Multiple` v `ServiceBehaviorAttribute`, můžete povolit souběžné dávkování.</span><span class="sxs-lookup"><span data-stu-id="975d5-145">By setting `ConcurrencyMode.Multiple` in `ServiceBehaviorAttribute`, you enable concurrent batching.</span></span>  
  
 <span data-ttu-id="975d5-146">*Služba omezování* je chování služby, který se používá k označení, nelze realizovat maximální počet souběžných volání služby.</span><span class="sxs-lookup"><span data-stu-id="975d5-146">*Service throttling* is a service behavior that is used to indicate how many maximum concurrent calls can be made on the service.</span></span> <span data-ttu-id="975d5-147">Při použití s dávkové zpracování, je interpretován jako způsob, jakým lze spustit mnoho souběžných dávky.</span><span class="sxs-lookup"><span data-stu-id="975d5-147">When used with batching, this is interpreted as how many concurrent batches can be run.</span></span> <span data-ttu-id="975d5-148">Pokud omezení služby není nastavená, WCF výchozí maximální souběžných volání na 16.</span><span class="sxs-lookup"><span data-stu-id="975d5-148">If the service throttling is not set, WCF defaults the maximum concurrent calls to 16.</span></span> <span data-ttu-id="975d5-149">Proto pokud ve výchozím nastavení byly přidány do dávek chování, maximálně 16 dávek může být aktivní ve stejnou dobu.</span><span class="sxs-lookup"><span data-stu-id="975d5-149">Thus, if batching behavior were added by default, a maximum of 16 batches can be active at the same time.</span></span> <span data-ttu-id="975d5-150">Je nejvhodnější k vyladění omezení služby a dávkové zpracování na základě vaší kapacity.</span><span class="sxs-lookup"><span data-stu-id="975d5-150">It is best to tune the service throttling and batching based on your capacity.</span></span> <span data-ttu-id="975d5-151">Například pokud fronta má 100 zpráv a je požadován dávku 20, s maximální souběžných volání nastaven na hodnotu 16 není užitečné proto, že v závislosti na propustnost, může být 16 transakce aktivní, podobně jako nemít dávkování zapnuté.</span><span class="sxs-lookup"><span data-stu-id="975d5-151">For example, if the queue has 100 messages and a batch of 20 is desired, having the maximum concurrent calls set to 16 is not useful because, depending on throughput, 16 transactions could be active, similar to not having batching turned on.</span></span> <span data-ttu-id="975d5-152">Proto při optimalizaci výkonu, buď nemáte k dispozici souběžných dávkování nebo mít souběžných dávkování omezení velikosti správné služby.</span><span class="sxs-lookup"><span data-stu-id="975d5-152">Therefore, when fine-tuning for performance, either do not have concurrent batching or have concurrent batching with the correct service throttle size.</span></span>  
  
## <a name="batching-and-multiple-endpoints"></a><span data-ttu-id="975d5-153">Dávkové zpracování a několik koncových bodů</span><span class="sxs-lookup"><span data-stu-id="975d5-153">Batching and Multiple Endpoints</span></span>  
 <span data-ttu-id="975d5-154">Koncový bod se skládá z adresy a kontrakt.</span><span class="sxs-lookup"><span data-stu-id="975d5-154">An endpoint is composed of an address and a contract.</span></span> <span data-ttu-id="975d5-155">Může existovat více koncových bodů, které sdílejí stejnou vazbu.</span><span class="sxs-lookup"><span data-stu-id="975d5-155">There may be multiple endpoints that share the same binding.</span></span> <span data-ttu-id="975d5-156">Je možné pro dva koncové body sdílet stejnou vazbu a naslouchání identifikátor URI (Uniform Resource), nebo adresa fronty.</span><span class="sxs-lookup"><span data-stu-id="975d5-156">It is possible for two endpoints to share the same binding and listen Uniform Resource Identifier (URI), or queue address.</span></span> <span data-ttu-id="975d5-157">Pokud jsou dva koncové body čtení ze stejné fronty a transakční dávkování chování je přidána do oba koncové body, konfliktu ve službě batch, které by mohly nastat velikosti.</span><span class="sxs-lookup"><span data-stu-id="975d5-157">If two endpoints are reading from the same queue, and transacted batching behavior is added to both endpoints, a conflict in the batch sizes specified could arise.</span></span> <span data-ttu-id="975d5-158">Je to vyřešit implementací dávkování použitím velikosti minimální dávky určena v rozsahu mezi dvěma provedené dávkování chování.</span><span class="sxs-lookup"><span data-stu-id="975d5-158">This is resolved by implementing batching using the minimal batch size specified between the two transacted batching behaviors.</span></span> <span data-ttu-id="975d5-159">V tomto scénáři Pokud jeden z koncových bodů není určen provedené dávkování, pak oba koncové body nepoužívejte dávkování.</span><span class="sxs-lookup"><span data-stu-id="975d5-159">In this scenario, if one of the endpoints does not specify transacted batching, then both endpoints would not use batching.</span></span>  
  
## <a name="example"></a><span data-ttu-id="975d5-160">Příklad</span><span class="sxs-lookup"><span data-stu-id="975d5-160">Example</span></span>  
 <span data-ttu-id="975d5-161">Následující příklad ukazuje, jak určit `TransactedBatchingBehavior` v konfiguračním souboru.</span><span class="sxs-lookup"><span data-stu-id="975d5-161">The following example shows how to specify the `TransactedBatchingBehavior` in a configuration file.</span></span>  
  
```xml  
<behaviors>
  <endpointBehaviors>
    <behavior name="TransactedBatchingBehavior"
              maxBatchSize="100" />
  </endpointBehaviors>
</behaviors>
```  
  
 <span data-ttu-id="975d5-162">Následující příklad ukazuje, jak určit <xref:System.ServiceModel.Description.TransactedBatchingBehavior> v kódu.</span><span class="sxs-lookup"><span data-stu-id="975d5-162">The following example shows how to specify the <xref:System.ServiceModel.Description.TransactedBatchingBehavior> in code.</span></span>  
  
```csharp
using (ServiceHost serviceHost = new ServiceHost(typeof(OrderProcessorService)))
{
     ServiceEndpoint sep = ServiceHost.AddServiceEndpoint(typeof(IOrderProcessor), new NetMsmqBinding(), "net.msmq://localhost/private/ServiceModelSamplesTransacted");
     sep.Behaviors.Add(new TransactedBatchingBehavior(100));
     
     // Open the ServiceHost to create listeners and start listening for messages.
    serviceHost.Open();
  
    // The service can now be accessed.
    Console.WriteLine("The service is ready.");
    Console.WriteLine("Press <ENTER> to terminate service.");
    Console.WriteLine();
    Console.ReadLine();
  
    // Close the ServiceHostB to shut down the service.
    serviceHost.Close();
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="975d5-163">Viz také:</span><span class="sxs-lookup"><span data-stu-id="975d5-163">See also</span></span>

- [<span data-ttu-id="975d5-164">Fronty – přehled</span><span class="sxs-lookup"><span data-stu-id="975d5-164">Queues Overview</span></span>](../../../../docs/framework/wcf/feature-details/queues-overview.md)
- [<span data-ttu-id="975d5-165">Fronty ve WCF</span><span class="sxs-lookup"><span data-stu-id="975d5-165">Queuing in WCF</span></span>](../../../../docs/framework/wcf/feature-details/queuing-in-wcf.md)
