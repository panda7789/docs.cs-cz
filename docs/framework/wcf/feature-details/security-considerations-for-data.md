---
title: Důležité informace o zabezpečení pro data
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: a7eb98da-4a93-4692-8b59-9d670c79ffb2
author: BrucePerlerMS
manager: mbaldwin
ms.openlocfilehash: 077d6b3527119f00ecec3014778fecf0dd1a4bde
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 05/04/2018
ms.locfileid: "33509072"
---
# <a name="security-considerations-for-data"></a><span data-ttu-id="743a8-102">Důležité informace o zabezpečení pro data</span><span class="sxs-lookup"><span data-stu-id="743a8-102">Security Considerations for Data</span></span>
<span data-ttu-id="743a8-103">Při plánování práce s daty ve Windows Communication Foundation (WCF), musíte zvážit mnoho kategorií hrozeb.</span><span class="sxs-lookup"><span data-stu-id="743a8-103">When dealing with data in Windows Communication Foundation (WCF), you must consider a number of threat categories.</span></span> <span data-ttu-id="743a8-104">Následující tabulka uvádí nejdůležitější třídy hrozeb, které se vztahují ke zpracování dat.</span><span class="sxs-lookup"><span data-stu-id="743a8-104">The following table lists the most important threat classes that relate to data processing.</span></span> <span data-ttu-id="743a8-105">WCF poskytuje nástroje ke zmírnění tyto hrozby.</span><span class="sxs-lookup"><span data-stu-id="743a8-105">WCF provides tools to mitigate these threats.</span></span>  
  
 <span data-ttu-id="743a8-106">Odmítnutí služby</span><span class="sxs-lookup"><span data-stu-id="743a8-106">Denial of service</span></span>  
 <span data-ttu-id="743a8-107">Při přijetí nedůvěryhodných dat, může způsobit data straně příjmu pro přístup k nesoustředil příliš velký objem různé prostředky, například paměť, vláken, dostupná připojení nebo cyklů procesoru tím, že na zdlouhavé výpočty.</span><span class="sxs-lookup"><span data-stu-id="743a8-107">When receiving untrusted data, the data may cause the receiving side to access a disproportionate amount of various resources, such as memory, threads, available connections, or processor cycles by causing lengthy computations.</span></span> <span data-ttu-id="743a8-108">Odmítnutí služby útoku proti serveru může způsobit havárií, a nebude možné zpracovat zprávy od klientů, které legitimní.</span><span class="sxs-lookup"><span data-stu-id="743a8-108">A denial-of-service attack against a server may cause it to crash and be unable to process messages from other, legitimate clients.</span></span>  
  
 <span data-ttu-id="743a8-109">Spuštění škodlivého kódu</span><span class="sxs-lookup"><span data-stu-id="743a8-109">Malicious code execution</span></span>  
 <span data-ttu-id="743a8-110">Příchozí data nedůvěryhodné způsobí, že ke spuštění kód, který nechtěli na straně příjmu.</span><span class="sxs-lookup"><span data-stu-id="743a8-110">Incoming untrusted data causes the receiving side to run code it did not intend to.</span></span>  
  
 <span data-ttu-id="743a8-111">Zpřístupnění informací</span><span class="sxs-lookup"><span data-stu-id="743a8-111">Information disclosure</span></span>  
 <span data-ttu-id="743a8-112">Vzdálenému útočníkovi vynutí přijímající strany reagovat na jeho požadavky v tak, aby poskytnout další informace, než má v úmyslu.</span><span class="sxs-lookup"><span data-stu-id="743a8-112">The remote attacker forces the receiving party to respond to its requests in such a way as to disclose more information than it intends to.</span></span>  
  
## <a name="user-provided-code-and-code-access-security"></a><span data-ttu-id="743a8-113">Zadaný uživatelem kódu a zabezpečení přístupu kódu</span><span class="sxs-lookup"><span data-stu-id="743a8-113">User-Provided Code and Code Access Security</span></span>  
 <span data-ttu-id="743a8-114">Počet míst v infrastruktuře Windows Communication Foundation (WCF) spustit kód, který je zadané uživatelem.</span><span class="sxs-lookup"><span data-stu-id="743a8-114">A number of places in the Windows Communication Foundation (WCF) infrastructure run code that is provided by the user.</span></span> <span data-ttu-id="743a8-115">Například <xref:System.Runtime.Serialization.DataContractSerializer> serializace modul může volat zadaný uživatelem vlastnost `set` přístupové objekty a `get` přistupující objekty.</span><span class="sxs-lookup"><span data-stu-id="743a8-115">For example, the <xref:System.Runtime.Serialization.DataContractSerializer> serialization engine may call user-provided property `set` accessors and `get` accessors.</span></span> <span data-ttu-id="743a8-116">Infrastruktura kanálu WCF může také volání do zadaný uživatelem odvozené třídy <xref:System.ServiceModel.Channels.Message> třídy.</span><span class="sxs-lookup"><span data-stu-id="743a8-116">The WCF channel infrastructure may also call into user-provided derived classes of the <xref:System.ServiceModel.Channels.Message> class.</span></span>  
  
 <span data-ttu-id="743a8-117">Je zodpovědností kódu Autor zajistit, že neexistují žádné chyby zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="743a8-117">It is the responsibility of the code author to ensure that no security vulnerabilities exist.</span></span> <span data-ttu-id="743a8-118">Například pokud vytvoříte kontraktů dat typ s vlastností člena data typu integer a v `set` přistupujícího objektu implementace přidělit pole na základě hodnoty vlastnosti, pokud škodlivý vystavit možnosti útoku odmítnutí služby zpráva obsahuje velmi velké hodnoty pro tento člen data.</span><span class="sxs-lookup"><span data-stu-id="743a8-118">For example, if you create a data contract type with a data member property of type integer, and in the `set` accessor implementation allocate an array based on the property value, you expose the possibility of a denial-of-service attack if a malicious message contains an extremely large value for this data member.</span></span> <span data-ttu-id="743a8-119">Obecně platí vyhněte rozdělení podle příchozích dat nebo dlouhé zpracování v kódu zadaný uživatelem (Pokud zvlášť zdlouhavé zpracování může být způsobeno malé množství příchozích dat).</span><span class="sxs-lookup"><span data-stu-id="743a8-119">In general, avoid any allocations based on incoming data or lengthy processing in user-provided code (especially if lengthy processing can be caused by a small amount of incoming data).</span></span> <span data-ttu-id="743a8-120">Při provádění analýzu zabezpečení zadaný uživatelem kódu, ujistěte se, že jste také vzít v úvahu všechny případy selhání (to znamená, že všechny kód větve kde jsou výjimky vyvolány).</span><span class="sxs-lookup"><span data-stu-id="743a8-120">When performing security analysis of user-provided code, make sure to also consider all failure cases (that is, all code branches where exceptions are thrown).</span></span>  
  
 <span data-ttu-id="743a8-121">Ultimate příklad kódu zadaný uživatelem je kód uvnitř vaší implementace služby pro každou operaci.</span><span class="sxs-lookup"><span data-stu-id="743a8-121">The ultimate example of user-provided code is the code inside your service implementation for each operation.</span></span> <span data-ttu-id="743a8-122">Zabezpečení vaší implementace služby je vaší povinností.</span><span class="sxs-lookup"><span data-stu-id="743a8-122">The security of your service implementation is your responsibility.</span></span> <span data-ttu-id="743a8-123">Je snadné vytvořit nechtěně nezabezpečené operace implementací, které může vést k ohrožení zabezpečení denial of service.</span><span class="sxs-lookup"><span data-stu-id="743a8-123">It is easy to inadvertently create insecure operation implementations that may result in denial-of-service vulnerabilities.</span></span> <span data-ttu-id="743a8-124">Například operace, která přebírá řetězec a vrátí do seznamu zákazníků z databáze, jejichž název začíná tento řetězec.</span><span class="sxs-lookup"><span data-stu-id="743a8-124">For example, an operation that takes a string and returns the list of customers from a database whose name starts with that string.</span></span> <span data-ttu-id="743a8-125">Pokud pracujete s velké databáze a řetězec předávány je právě jedním písmenem, kódu pokusu o vytvoření zprávy větší než všechny dostupné paměti, způsobuje selhání celé služby.</span><span class="sxs-lookup"><span data-stu-id="743a8-125">If you are working with a large database and the string being passed is just a single letter, your code may attempt to create a message larger than all available memory, causing the entire service to fail.</span></span> <span data-ttu-id="743a8-126">( <xref:System.OutOfMemoryException> Není použitelná pro obnovení v [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] a vždy výsledkem ukončení aplikace.)</span><span class="sxs-lookup"><span data-stu-id="743a8-126">(An <xref:System.OutOfMemoryException> is not recoverable in the [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] and always results in the termination of your application.)</span></span>  
  
 <span data-ttu-id="743a8-127">Ujistěte se, že žádný škodlivý kód je připojen do různých body rozšiřitelnosti.</span><span class="sxs-lookup"><span data-stu-id="743a8-127">You should ensure that no malicious code is plugged in to the various extensibility points.</span></span> <span data-ttu-id="743a8-128">To je obzvláště důležité, při spuštění v částečné důvěryhodnosti, plánování práce s typy z částečně důvěryhodného sestavení nebo vytváření použitelné součásti částečně důvěryhodný kód.</span><span class="sxs-lookup"><span data-stu-id="743a8-128">This is especially relevant when running under partial trust, dealing with types from partially-trusted assemblies, or creating components usable by partially-trusted code.</span></span> <span data-ttu-id="743a8-129">Další informace najdete v tématu "Částečné důvěryhodnosti hrozby" v další části.</span><span class="sxs-lookup"><span data-stu-id="743a8-129">For more information, see "Partial Trust Threats" in a later section.</span></span>  
  
 <span data-ttu-id="743a8-130">Poznámka: při spuštění v částečné důvěryhodnosti, infrastruktura serializace kontraktu dat podporuje pouze omezenou podmnožinou dat kontrakt programovacího modelu – například private členy nebo typy pomocí <xref:System.SerializableAttribute> atribut nejsou podporovány.</span><span class="sxs-lookup"><span data-stu-id="743a8-130">Note that when running in partial trust, the data contract serialization infrastructure supports only a limited subset of the data contract programming model - for example, private data members or types using the <xref:System.SerializableAttribute> attribute are not supported.</span></span> <span data-ttu-id="743a8-131">Další informace najdete v tématu [částečné důvěryhodnosti](../../../../docs/framework/wcf/feature-details/partial-trust.md).</span><span class="sxs-lookup"><span data-stu-id="743a8-131">For more information, see [Partial Trust](../../../../docs/framework/wcf/feature-details/partial-trust.md).</span></span>  
  
## <a name="avoiding-unintentional-information-disclosure"></a><span data-ttu-id="743a8-132">Zamezení neúmyslnému přístup k informacím</span><span class="sxs-lookup"><span data-stu-id="743a8-132">Avoiding Unintentional Information Disclosure</span></span>  
 <span data-ttu-id="743a8-133">Při navrhování Serializovatelné typy s důrazem na bezpečnost, zpřístupnění informací je možné problémy.</span><span class="sxs-lookup"><span data-stu-id="743a8-133">When designing serializable types with security in mind, information disclosure is a possible concern.</span></span>  
  
 <span data-ttu-id="743a8-134">Mějte na paměti následující body:</span><span class="sxs-lookup"><span data-stu-id="743a8-134">Consider the following points:</span></span>  
  
-   <span data-ttu-id="743a8-135"><xref:System.Runtime.Serialization.DataContractSerializer> Programovací model umožňuje úniku privátní a interní data mimo typ nebo sestavení během serializace.</span><span class="sxs-lookup"><span data-stu-id="743a8-135">The <xref:System.Runtime.Serialization.DataContractSerializer> programming model allows the exposure of private and internal data outside of the type or assembly during serialization.</span></span> <span data-ttu-id="743a8-136">Kromě toho mohou být zpřístupněny obrazce typu během exportu schématu.</span><span class="sxs-lookup"><span data-stu-id="743a8-136">Additionally, the shape of a type can be exposed during schema export.</span></span> <span data-ttu-id="743a8-137">Ujistěte se, že pochopení vašeho typu serializaci projekce.</span><span class="sxs-lookup"><span data-stu-id="743a8-137">Be sure to understand your type's serialization projection.</span></span> <span data-ttu-id="743a8-138">Pokud nechcete, aby nic vystavený, zakázat jeho serializace (například použitím není <xref:System.Runtime.Serialization.DataMemberAttribute> atribut v případě kontraktu dat).</span><span class="sxs-lookup"><span data-stu-id="743a8-138">If you do not want anything exposed, disable serializing it (for example, by not applying the <xref:System.Runtime.Serialization.DataMemberAttribute> attribute in the case of a data contract).</span></span>  
  
-   <span data-ttu-id="743a8-139">Upozorňujeme, že stejný typ může mít několik serializaci projekce, v závislosti na serializátoru, který je používán.</span><span class="sxs-lookup"><span data-stu-id="743a8-139">Be aware that the same type may have multiple serialization projections, depending on the serializer in use.</span></span> <span data-ttu-id="743a8-140">Stejný typ vystavuje jednu sadu dat při použití s <xref:System.Runtime.Serialization.DataContractSerializer> a jinou sadu dat při použití s <xref:System.Xml.Serialization.XmlSerializer>.</span><span class="sxs-lookup"><span data-stu-id="743a8-140">The same type may expose one set of data when used with the <xref:System.Runtime.Serialization.DataContractSerializer> and another set of data when used with the <xref:System.Xml.Serialization.XmlSerializer>.</span></span> <span data-ttu-id="743a8-141">Omylem pomocí nesprávného serializátor může vést ke zpřístupnění informací.</span><span class="sxs-lookup"><span data-stu-id="743a8-141">Accidentally using the wrong serializer may lead to information disclosure.</span></span>  
  
-   <span data-ttu-id="743a8-142">Pomocí <xref:System.Xml.Serialization.XmlSerializer> v postupu starší verze vzdáleného volání (RPC) / kódovaného režimu může neúmyslně vystavit obrazec grafu objektu na straně odesílání na straně příjmu.</span><span class="sxs-lookup"><span data-stu-id="743a8-142">Using the <xref:System.Xml.Serialization.XmlSerializer> in legacy remote procedure call (RPC)/encoded mode may unintentionally expose the shape of the object graph on the sending side to the receiving side.</span></span>  
  
## <a name="preventing-denial-of-service-attacks"></a><span data-ttu-id="743a8-143">Prevence útoků odmítnutí služby</span><span class="sxs-lookup"><span data-stu-id="743a8-143">Preventing Denial-of-Service Attacks</span></span>  
  
### <a name="quotas"></a><span data-ttu-id="743a8-144">Kvóty</span><span class="sxs-lookup"><span data-stu-id="743a8-144">Quotas</span></span>  
 <span data-ttu-id="743a8-145">Způsobuje přijímající straně přidělit významné množství paměti je možný útok denial of service.</span><span class="sxs-lookup"><span data-stu-id="743a8-145">Causing the receiving side to allocate a significant amount of memory is a potential denial-of-service attack.</span></span> <span data-ttu-id="743a8-146">Při této části soustřeďuje na otázky spotřeba paměti vyplývající z velkých zpráv, může dojít, jiným útokům.</span><span class="sxs-lookup"><span data-stu-id="743a8-146">While this section concentrates on memory consumption issues arising from large messages, other attacks may occur.</span></span> <span data-ttu-id="743a8-147">Zprávy mohou například používat nesoustředil příliš velký objem doba zpracování.</span><span class="sxs-lookup"><span data-stu-id="743a8-147">For example, messages may use a disproportionate amount of processing time.</span></span>  
  
 <span data-ttu-id="743a8-148">Útoky DOS jsou obvykle omezeny pomocí kvóty.</span><span class="sxs-lookup"><span data-stu-id="743a8-148">Denial-of-service attacks are usually mitigated using quotas.</span></span> <span data-ttu-id="743a8-149">Při překročení kvóty <xref:System.ServiceModel.QuotaExceededException> normálně je vyvolána výjimka.</span><span class="sxs-lookup"><span data-stu-id="743a8-149">When a quota is exceeded, a <xref:System.ServiceModel.QuotaExceededException> exception is normally thrown.</span></span> <span data-ttu-id="743a8-150">Bez kvótu, škodlivé zprávy může způsobit všechny dostupné paměti nelze přistupovat, což vede k <xref:System.OutOfMemoryException> výjimky nebo všechny dostupné balíčky nelze přistupovat, což je <xref:System.StackOverflowException>.</span><span class="sxs-lookup"><span data-stu-id="743a8-150">Without the quota, a malicious message may cause all available memory to be accessed, resulting in an <xref:System.OutOfMemoryException> exception, or all available stacks to be accessed, resulting in a <xref:System.StackOverflowException>.</span></span>  
  
 <span data-ttu-id="743a8-151">Byla překročena kvóta scénář je obnovitelné; Jestliže ve spuštěné službě, je právě zpracovávána zprávy zahozena a službu neustále běží a zpracuje zprávy o další.</span><span class="sxs-lookup"><span data-stu-id="743a8-151">The quota exceeded scenario is recoverable; if encountered in a running service, the message currently being processed is discarded and the service keeps running and processes further messages.</span></span> <span data-ttu-id="743a8-152">Scénáře přetečení zásobníku a z důvodu nedostatku paměti, ale nejsou obnovitelné kdekoliv [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)]; služba ukončuje v případě nalezení takové výjimky.</span><span class="sxs-lookup"><span data-stu-id="743a8-152">The out-of-memory and stack overflow scenarios, however, are not recoverable anywhere in the [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)]; the service terminates if it encounters such exceptions.</span></span>  
  
 <span data-ttu-id="743a8-153">Kvóty ve WCF nezahrnují všechny předběžné přidělení.</span><span class="sxs-lookup"><span data-stu-id="743a8-153">Quotas in WCF do not involve any pre-allocation.</span></span> <span data-ttu-id="743a8-154">Například pokud <xref:System.ServiceModel.Channels.TransportBindingElement.MaxReceivedMessageSize%2A> kvóta (nachází se na různé třídy) je nastavena na 128 KB, neznamená, že je 128 KB automaticky přidělena pro každou zprávu.</span><span class="sxs-lookup"><span data-stu-id="743a8-154">For example, if the <xref:System.ServiceModel.Channels.TransportBindingElement.MaxReceivedMessageSize%2A> quota (found on various classes) is set to 128 KB, it does not mean that 128 KB is automatically allocated for each message.</span></span> <span data-ttu-id="743a8-155">Skutečná velikost přidělené závisí na skutečná velikost příchozí zprávy.</span><span class="sxs-lookup"><span data-stu-id="743a8-155">The actual amount allocated depends on the actual incoming message size.</span></span>  
  
 <span data-ttu-id="743a8-156">Mnoho kvóty jsou k dispozici v přenosové vrstvě.</span><span class="sxs-lookup"><span data-stu-id="743a8-156">Many quotas are available at the transport layer.</span></span> <span data-ttu-id="743a8-157">Jedná se o kvóty podle konkrétní přenosu kanálu používá (HTTP, TCP a tak dále).</span><span class="sxs-lookup"><span data-stu-id="743a8-157">These are quotas enforced by the specific transport channel in use (HTTP, TCP, and so on).</span></span> <span data-ttu-id="743a8-158">Když toto téma popisuje některé z těchto kvót, těchto kvót jsou podrobně popsány v [přenosové kvóty](../../../../docs/framework/wcf/feature-details/transport-quotas.md).</span><span class="sxs-lookup"><span data-stu-id="743a8-158">While this topic discusses some of these quotas, these quotas are described in detail in [Transport Quotas](../../../../docs/framework/wcf/feature-details/transport-quotas.md).</span></span>  
  
### <a name="hashtable-vulnerability"></a><span data-ttu-id="743a8-159">Zatřiďovací tabulky ohrožení zabezpečení</span><span class="sxs-lookup"><span data-stu-id="743a8-159">Hashtable Vulnerability</span></span>  
 <span data-ttu-id="743a8-160">Obsahuje chybu, pokud kontrakty dat obsahují zatřiďovacích tabulkách nebo kolekce.</span><span class="sxs-lookup"><span data-stu-id="743a8-160">A vulnerability exists when data contracts contain hashtables or collections.</span></span> <span data-ttu-id="743a8-161">K problému dochází velký počet hodnot vložit do zatřiďovací tabulky, kde velký počet tyto hodnoty generovat stejnou hodnotu hash.</span><span class="sxs-lookup"><span data-stu-id="743a8-161">The problem occurs if a large number of values are inserted into a hashtable where a large number of those values generate the same hash value.</span></span> <span data-ttu-id="743a8-162">To slouží jako útoku DOS.</span><span class="sxs-lookup"><span data-stu-id="743a8-162">This can be used as a DOS attack.</span></span>  <span data-ttu-id="743a8-163">Toto ohrožení zabezpečení lze zmírnit nastavíte kvótu MaxRecievedMessageSize vazby.</span><span class="sxs-lookup"><span data-stu-id="743a8-163">This vulnerability can be mitigated by setting the MaxRecievedMessageSize binding quota.</span></span> <span data-ttu-id="743a8-164">Musí dát pozor při nastavení tato kvóta prevence takové útoky.</span><span class="sxs-lookup"><span data-stu-id="743a8-164">Care must be taken while setting this quota in order to prevent such attacks.</span></span> <span data-ttu-id="743a8-165">Tato kvóta převádí horní limit velikosti zprávy WCF.</span><span class="sxs-lookup"><span data-stu-id="743a8-165">This quota puts an upper limit on the size of WCF message.</span></span> <span data-ttu-id="743a8-166">Kromě toho Vyhněte se používání zatřiďovacích tabulkách nebo kolekcí v kontraktech vaše data.</span><span class="sxs-lookup"><span data-stu-id="743a8-166">Additionally, avoid using hashtables or collections in your data contracts.</span></span>  
  
## <a name="limiting-memory-consumption-without-streaming"></a><span data-ttu-id="743a8-167">Omezení využití paměti bez streamování</span><span class="sxs-lookup"><span data-stu-id="743a8-167">Limiting Memory Consumption Without Streaming</span></span>  
 <span data-ttu-id="743a8-168">Model zabezpečení kolem velké zprávy závisí na tom streamování používán.</span><span class="sxs-lookup"><span data-stu-id="743a8-168">The security model around large messages depends on whether streaming is in use.</span></span> <span data-ttu-id="743a8-169">V případě základní, datového proudu zprávy se uloží do vyrovnávací paměti do paměti.</span><span class="sxs-lookup"><span data-stu-id="743a8-169">In the basic, non-streamed case, messages are buffered into memory.</span></span> <span data-ttu-id="743a8-170">V takovém případě použijte <xref:System.ServiceModel.Channels.TransportBindingElement.MaxReceivedMessageSize%2A> kvóty v <xref:System.ServiceModel.Channels.TransportBindingElement> nebo na vazby poskytované systémem pro ochranu proti velké zprávy omezením maximální velikosti zprávy pro přístup.</span><span class="sxs-lookup"><span data-stu-id="743a8-170">In this case, use the <xref:System.ServiceModel.Channels.TransportBindingElement.MaxReceivedMessageSize%2A> quota on the <xref:System.ServiceModel.Channels.TransportBindingElement> or on the system-provided bindings to protect against large messages by limiting the maximum message size to access.</span></span> <span data-ttu-id="743a8-171">Všimněte si, že služba může zpracování více zpráv ve stejnou dobu, v takovém případě jsou všechny v paměti.</span><span class="sxs-lookup"><span data-stu-id="743a8-171">Note that a service may be processing multiple messages at the same time, in which case they are all in memory.</span></span> <span data-ttu-id="743a8-172">Použijte funkci omezení pro zmírnění této hrozby.</span><span class="sxs-lookup"><span data-stu-id="743a8-172">Use the throttling feature to mitigate this threat.</span></span>  
  
 <span data-ttu-id="743a8-173">Všimněte si také, že `MaxReceivedMessageSize` neumístí horní mez na jednotlivé zprávy využití paměti, ale její omezení v rámci konstantní faktor.</span><span class="sxs-lookup"><span data-stu-id="743a8-173">Also note that `MaxReceivedMessageSize` does not place an upper bound on per-message memory consumption, but limits it to within a constant factor.</span></span> <span data-ttu-id="743a8-174">Například pokud `MaxReceivedMessageSize` je 1 MB a je přijatá zpráva 1 MB a pak je potřeba obsahovat deserializovaný objekt grafu, což vede k celkové paměti spotřeba také více než 1 MB deserializovat, další paměť.</span><span class="sxs-lookup"><span data-stu-id="743a8-174">For example, if the `MaxReceivedMessageSize` is 1 MB and a 1-MB message is received and then deserialized, additional memory is required to contain the deserialized object graph, resulting in total memory consumption well over 1 MB.</span></span> <span data-ttu-id="743a8-175">Z tohoto důvodu Vyhněte se vytváření Serializovatelné typy, které by mohly způsobit významné paměťové nároky bez množství příchozích dat.</span><span class="sxs-lookup"><span data-stu-id="743a8-175">For this reason, avoid creating serializable types that could result in significant memory consumption without much incoming data.</span></span> <span data-ttu-id="743a8-176">Například kontrakt dat "MyContract" 50 volitelnými daty člen pole a další 100 privátní pole může být vytvořena s konstrukce XML "\<MyContract / >".</span><span class="sxs-lookup"><span data-stu-id="743a8-176">For example, a data contract "MyContract" with 50 optional data member fields and an additional 100 private fields could be instantiated with the XML construction "\<MyContract/>".</span></span> <span data-ttu-id="743a8-177">Tato konfigurace XML má za následek paměti přistupuje 150 polí.</span><span class="sxs-lookup"><span data-stu-id="743a8-177">This XML results in memory being accessed for 150 fields.</span></span> <span data-ttu-id="743a8-178">Upozorňujeme, že jsou ve výchozím nastavení volitelné datových členů.</span><span class="sxs-lookup"><span data-stu-id="743a8-178">Note that data members are optional by default.</span></span> <span data-ttu-id="743a8-179">Tento problém nastane, když je takového typu součástí pole.</span><span class="sxs-lookup"><span data-stu-id="743a8-179">The problem is compounded when such a type is part of an array.</span></span>  
  
 <span data-ttu-id="743a8-180">`MaxReceivedMessageSize` samostatně není dostatečně k útokům denial of service.</span><span class="sxs-lookup"><span data-stu-id="743a8-180">`MaxReceivedMessageSize` alone is not enough to prevent all denial-of-service attacks.</span></span> <span data-ttu-id="743a8-181">Deserializátor může vynutit k deserializaci hluboko vnořený objekt graf (objekt, který obsahuje jiný objekt, který obsahuje ještě jiný a tak dále), například pomocí příchozí zprávy.</span><span class="sxs-lookup"><span data-stu-id="743a8-181">For example, the deserializer may be forced to deserialize a deeply-nested object graph (an object that contains another object that contains yet another one, and so on) by an incoming message.</span></span> <span data-ttu-id="743a8-182">Jak <xref:System.Runtime.Serialization.DataContractSerializer> a <xref:System.Xml.Serialization.XmlSerializer> volat metody vnořené způsobem k deserializaci takové grafy.</span><span class="sxs-lookup"><span data-stu-id="743a8-182">Both the <xref:System.Runtime.Serialization.DataContractSerializer> and the <xref:System.Xml.Serialization.XmlSerializer> call methods in a nested way to deserialize such graphs.</span></span> <span data-ttu-id="743a8-183">Hluboká vnoření volání metod může mít za následek k neodstranitelné <xref:System.StackOverflowException>.</span><span class="sxs-lookup"><span data-stu-id="743a8-183">Deep nesting of method calls may result in an unrecoverable <xref:System.StackOverflowException>.</span></span> <span data-ttu-id="743a8-184">Tuto hrozbu zmírnit nastavení <xref:System.ServiceModel.Configuration.XmlDictionaryReaderQuotasElement.MaxDepth%2A> kvóty pro omezení úroveň vnoření XML, jak je popsáno v části "Použití XML bezpečně" později v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="743a8-184">This threat is mitigated by setting the <xref:System.ServiceModel.Configuration.XmlDictionaryReaderQuotasElement.MaxDepth%2A> quota to limit the level of XML nesting, as discussed in the "Using XML Safely" section later in the topic.</span></span>  
  
 <span data-ttu-id="743a8-185">Nastavení dodatečných kvótách `MaxReceivedMessageSize` je zvlášť důležité při použití binárního kódování XML.</span><span class="sxs-lookup"><span data-stu-id="743a8-185">Setting additional quotas to `MaxReceivedMessageSize` is especially important when using binary XML encoding.</span></span> <span data-ttu-id="743a8-186">Použití binárního kódování odpovídá poněkud komprese: malou skupinu bajtů příchozí zpráva může představovat velké množství dat.</span><span class="sxs-lookup"><span data-stu-id="743a8-186">Using binary encoding is somewhat equivalent to compression: a small group of bytes in the incoming message may represent a lot of data.</span></span> <span data-ttu-id="743a8-187">Proto i zprávu hodí do `MaxReceivedMessageSize` limit může trvat až mnohem víc paměti ve plně rozšířené formuláře.</span><span class="sxs-lookup"><span data-stu-id="743a8-187">Thus, even a message fitting into the `MaxReceivedMessageSize` limit may take up much more memory in fully expanded form.</span></span> <span data-ttu-id="743a8-188">Zmírnit hrozby takové specifické XML, všechny kvóty čtečky XML musí být nastavena správně, jak je popsáno v části "Použití XML bezpečně" dál v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="743a8-188">To mitigate such XML-specific threats, all of the XML reader quotas must be set correctly, as discussed in the "Using XML Safely" section later in this topic.</span></span>  
  
## <a name="limiting-memory-consumption-with-streaming"></a><span data-ttu-id="743a8-189">Omezení využití paměti při streamování</span><span class="sxs-lookup"><span data-stu-id="743a8-189">Limiting Memory Consumption with Streaming</span></span>  
 <span data-ttu-id="743a8-190">Při streamování, můžete použít malá `MaxReceivedMessageSize` nastavení k ochraně před útoky DOS.</span><span class="sxs-lookup"><span data-stu-id="743a8-190">When streaming, you may use a small `MaxReceivedMessageSize` setting to protect against denial-of-service attacks.</span></span> <span data-ttu-id="743a8-191">Složitější scénáře jsou však možné pomocí vysílání datového proudu.</span><span class="sxs-lookup"><span data-stu-id="743a8-191">However, more complicated scenarios are possible with streaming.</span></span> <span data-ttu-id="743a8-192">Například službu nahrávání souborů přijímá soubory větší než všechny dostupné paměti.</span><span class="sxs-lookup"><span data-stu-id="743a8-192">For example, a file upload service accepts files larger than all available memory.</span></span> <span data-ttu-id="743a8-193">V takovém případě nastavte `MaxReceivedMessageSize` velmi velké hodnoty, očekává, že je téměř žádná data do vyrovnávací paměti v paměti a zpráva datové proudy přímo na disku.</span><span class="sxs-lookup"><span data-stu-id="743a8-193">In this case, set the `MaxReceivedMessageSize` to an extremely large value, expecting that almost no data is buffered in memory and the message streams directly to disk.</span></span> <span data-ttu-id="743a8-194">Pokud škodlivé zprávy mohou nějakým způsobem vynutit WCF na místo datových proudů v takovém případě vyrovnávací paměť dat `MaxReceivedMessageSize` už chrání před zpráva přístup k veškeré dostupné paměti.</span><span class="sxs-lookup"><span data-stu-id="743a8-194">If a malicious message can somehow force WCF to buffer data instead of streaming it in this case, `MaxReceivedMessageSize` no longer protects against the message accessing all available memory.</span></span>  
  
 <span data-ttu-id="743a8-195">Pro zmírnění této hrozby, existují určité kvótu na různé součásti WCF zpracování dat tento limit ukládání do vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="743a8-195">To mitigate this threat, specific quota settings exist on various WCF data-processing components that limit buffering.</span></span> <span data-ttu-id="743a8-196">Nejdůležitější z nich je `MaxBufferSize` vlastnost na různých elementů přenosové vazby a standardní vazby.</span><span class="sxs-lookup"><span data-stu-id="743a8-196">The most important of these is the `MaxBufferSize` property on various transport binding elements and standard bindings.</span></span> <span data-ttu-id="743a8-197">Při streamování, musí být tato kvóta nastavená vezme v úvahu maximální množství paměti, kterou chcete přidělit jednotlivých zpráv.</span><span class="sxs-lookup"><span data-stu-id="743a8-197">When streaming, this quota should be set taking into account the maximum amount of memory you are willing to allocate per message.</span></span> <span data-ttu-id="743a8-198">Stejně jako u `MaxReceivedMessageSize`, nastavení není umístí absolutní maximální využití paměti, ale pouze její omezení v rámci konstantní faktor.</span><span class="sxs-lookup"><span data-stu-id="743a8-198">As with `MaxReceivedMessageSize`, the setting does not put an absolute maximum on memory consumption but only limits it to within a constant factor.</span></span> <span data-ttu-id="743a8-199">Také jako s `MaxReceivedMessageSize`, mějte na paměti na možnost více zpráv zpracovávaný současně.</span><span class="sxs-lookup"><span data-stu-id="743a8-199">Also, as with `MaxReceivedMessageSize`, be aware of the possibility of multiple messages being processed simultaneously.</span></span>  
  
### <a name="maxbuffersize-details"></a><span data-ttu-id="743a8-200">Podrobnosti o MaxBufferSize</span><span class="sxs-lookup"><span data-stu-id="743a8-200">MaxBufferSize Details</span></span>  
 <span data-ttu-id="743a8-201">`MaxBufferSize` Vlastnost omezuje žádné hromadné nemá vyrovnávací paměti WCF.</span><span class="sxs-lookup"><span data-stu-id="743a8-201">The `MaxBufferSize` property limits any bulk buffering WCF does.</span></span> <span data-ttu-id="743a8-202">Například WCF vždy ukládá do vyrovnávací paměti hlavičky SOAP a chyb protokolu SOAP a také všechny části MIME najít tak, aby nebyl v pořadí fyzická čtení zprávy zpráva přenosu optimalizace mechanismus (MTOM).</span><span class="sxs-lookup"><span data-stu-id="743a8-202">For example, WCF always buffers SOAP headers and SOAP faults, as well as any MIME parts found to be not in the natural reading order in an Message Transmission Optimization Mechanism (MTOM) message.</span></span> <span data-ttu-id="743a8-203">Toto nastavení omezuje množství ukládání do vyrovnávací paměti ve všech těchto případech.</span><span class="sxs-lookup"><span data-stu-id="743a8-203">This setting limits the amount of buffering in all these cases.</span></span>  
  
 <span data-ttu-id="743a8-204">WCF dosahuje tak, že předávání `MaxBufferSize` hodnotu pro různé součásti, které může ukládat do vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="743a8-204">WCF accomplishes this by passing the `MaxBufferSize` value to the various components that may buffer.</span></span> <span data-ttu-id="743a8-205">Například některé <xref:System.ServiceModel.Channels.Message.CreateMessage%2A> přetížení <xref:System.ServiceModel.Channels.Message> proveďte třídy `maxSizeOfHeaders` parametr.</span><span class="sxs-lookup"><span data-stu-id="743a8-205">For example, some <xref:System.ServiceModel.Channels.Message.CreateMessage%2A> overloads of the <xref:System.ServiceModel.Channels.Message> class take a `maxSizeOfHeaders` parameter.</span></span> <span data-ttu-id="743a8-206">Předá WCF `MaxBufferSize` hodnotu tohoto parametru a omezit tak velikost hlavičky SOAP ukládání do vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="743a8-206">WCF passes the `MaxBufferSize` value to this parameter to limit the amount of SOAP header buffering.</span></span> <span data-ttu-id="743a8-207">Je důležité při použití nastavte tento parametr <xref:System.ServiceModel.Channels.Message> přímo třídu.</span><span class="sxs-lookup"><span data-stu-id="743a8-207">It is important to set this parameter when using the <xref:System.ServiceModel.Channels.Message> class directly.</span></span> <span data-ttu-id="743a8-208">Obecně platí při použití součásti WCF, který přebírá parametry kvót, je důležité pochopit bezpečnostních důsledcích tyto parametry a tyto hodnoty správně nastaveny.</span><span class="sxs-lookup"><span data-stu-id="743a8-208">In general, when using a component in WCF that takes quota parameters, it is important to understand the security implications of these parameters and set them correctly.</span></span>  
  
 <span data-ttu-id="743a8-209">Kodér zpráv MTOM má také `MaxBufferSize` nastavení.</span><span class="sxs-lookup"><span data-stu-id="743a8-209">The MTOM message encoder also has a `MaxBufferSize` setting.</span></span> <span data-ttu-id="743a8-210">Při použití standardní vazby, to je automaticky nastaven na úrovni přenosu `MaxBufferSize` hodnotu.</span><span class="sxs-lookup"><span data-stu-id="743a8-210">When using standard bindings, this is set automatically to the transport-level `MaxBufferSize` value.</span></span> <span data-ttu-id="743a8-211">Ale při použití prvku vazby MTOM zpráva encoder můžete vytvořit vlastní vazby, je důležité nastavit `MaxBufferSize` vlastnost na hodnotu bezpečné při streamování se používá.</span><span class="sxs-lookup"><span data-stu-id="743a8-211">However, when using the MTOM message encoder binding element to construct a custom binding, it is important to set the `MaxBufferSize` property to a safe value when streaming is used.</span></span>  
  
## <a name="xml-based-streaming-attacks"></a><span data-ttu-id="743a8-212">Streamování útoky založenými na XML</span><span class="sxs-lookup"><span data-stu-id="743a8-212">XML-Based Streaming Attacks</span></span>  
 <span data-ttu-id="743a8-213">`MaxBufferSize` samostatně není dostatečně zajistit, že WCF nelze vynutit do ukládání do vyrovnávací paměti při streamování se očekává.</span><span class="sxs-lookup"><span data-stu-id="743a8-213">`MaxBufferSize` alone is not enough to ensure that WCF cannot be forced into buffering when streaming is expected.</span></span> <span data-ttu-id="743a8-214">Například čtečky WCF XML vždy vyrovnávací paměti celý počáteční značky elementu XML při spouštění číst nového elementu.</span><span class="sxs-lookup"><span data-stu-id="743a8-214">For example, the WCF XML readers always buffer the entire XML element start tag when starting to read a new element.</span></span> <span data-ttu-id="743a8-215">To se provádí tak, aby správně zpracovat obory názvů a atributy.</span><span class="sxs-lookup"><span data-stu-id="743a8-215">This is done so that namespaces and attributes are properly processed.</span></span> <span data-ttu-id="743a8-216">Pokud `MaxReceivedMessageSize` je nakonfigurovaný jako velký (například povolit velký soubor přímo na disk streamování scénář), škodlivé zprávy může zkonstruovat kde text celé zprávy je velký počáteční značky elementu XML.</span><span class="sxs-lookup"><span data-stu-id="743a8-216">If `MaxReceivedMessageSize` is configured to be large (for example, to enable a direct-to-disk large file streaming scenario), a malicious message may be constructed where the entire message body is a large XML element start tag.</span></span> <span data-ttu-id="743a8-217">Pokus o čtení ho vede <xref:System.OutOfMemoryException>.</span><span class="sxs-lookup"><span data-stu-id="743a8-217">An attempt to read it results in an <xref:System.OutOfMemoryException>.</span></span> <span data-ttu-id="743a8-218">Toto je jedna z mnoha možné na základě XML denial-of-service útoků, které lze všechny zmírnit pomocí kvóty čtečky XML, které jsou popsané v části "Použití XML bezpečně" dál v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="743a8-218">This is one of many possible XML-based denial-of-service attacks that can all be mitigated using XML reader quotas, discussed in the "Using XML Safely" section later in this topic.</span></span> <span data-ttu-id="743a8-219">Při streamování, je velmi důležité nastavit všechny tyto kvóty.</span><span class="sxs-lookup"><span data-stu-id="743a8-219">When streaming, it is especially important to set all of these quotas.</span></span>  
  
### <a name="mixing-streaming-and-buffering-programming-models"></a><span data-ttu-id="743a8-220">Kombinování streamování a ukládání do vyrovnávací paměti modely programování</span><span class="sxs-lookup"><span data-stu-id="743a8-220">Mixing Streaming and Buffering Programming Models</span></span>  
 <span data-ttu-id="743a8-221">Řada možných útoků jsou vyvolány kombinování streamování a streamování programovací modely v rámci stejné služby.</span><span class="sxs-lookup"><span data-stu-id="743a8-221">Many possible attacks arise from mixing streaming and non-streaming programming models in the same service.</span></span> <span data-ttu-id="743a8-222">Předpokládejme, že je kontrakt služby s dvěma operacemi: jeden trvá <xref:System.IO.Stream> a jiné přijímá pole vlastní typu.</span><span class="sxs-lookup"><span data-stu-id="743a8-222">Suppose there is a service contract with two operations: one takes a <xref:System.IO.Stream> and another takes an array of some custom type.</span></span> <span data-ttu-id="743a8-223">Předpokládejme také který `MaxReceivedMessageSize` je nastaven na velké hodnoty, aby první operaci ke zpracování velkých datových proudů.</span><span class="sxs-lookup"><span data-stu-id="743a8-223">Suppose also that `MaxReceivedMessageSize` is set to a large value to enable the first operation to process large streams.</span></span> <span data-ttu-id="743a8-224">Bohužel to znamená, velké můžete nyní odesílání zpráv i druhá operace a data vyrovnávací paměti deserializátor v paměti jako pole předtím, než se nazývá operaci.</span><span class="sxs-lookup"><span data-stu-id="743a8-224">Unfortunately, this means that large messages can now be sent to the second operation as well, and the deserializer buffers data in memory as an array before the operation is called.</span></span> <span data-ttu-id="743a8-225">Toto je možný útok denial of service: `MaxBufferSize` kvóty neomezuje velikost těla zprávy, což je deserializátor pracuje s.</span><span class="sxs-lookup"><span data-stu-id="743a8-225">This is a potential denial-of-service attack: the `MaxBufferSize` quota does not limit the size of the message body, which is what the deserializer works with.</span></span>  
  
 <span data-ttu-id="743a8-226">Z tohoto důvodu vyhněte míchání operací na základě datového proudu a bez datového proudu ve stejné smlouvy.</span><span class="sxs-lookup"><span data-stu-id="743a8-226">For this reason, avoid mixing stream-based and non-streamed operations in the same contract.</span></span> <span data-ttu-id="743a8-227">Pokud musíte kombinovat absolutně dva programovací modely, pomocí následujících opatření:</span><span class="sxs-lookup"><span data-stu-id="743a8-227">If you absolutely must mix the two programming models, use the following precautions:</span></span>  
  
-   <span data-ttu-id="743a8-228">Vypnout <xref:System.Runtime.Serialization.IExtensibleDataObject> funkce nastavením <xref:System.ServiceModel.ServiceBehaviorAttribute.IgnoreExtensionDataObject%2A> vlastnost <xref:System.ServiceModel.ServiceBehaviorAttribute> k `true`.</span><span class="sxs-lookup"><span data-stu-id="743a8-228">Turn off the <xref:System.Runtime.Serialization.IExtensibleDataObject> feature by setting the <xref:System.ServiceModel.ServiceBehaviorAttribute.IgnoreExtensionDataObject%2A> property of the <xref:System.ServiceModel.ServiceBehaviorAttribute> to `true`.</span></span> <span data-ttu-id="743a8-229">Tím se zajistí, že jsou pouze členové, které jsou součástí kontraktu deserializovat.</span><span class="sxs-lookup"><span data-stu-id="743a8-229">This ensures that only members that are a part of the contract are deserialized.</span></span>  
  
-   <span data-ttu-id="743a8-230">Nastavte <xref:System.Runtime.Serialization.DataContractSerializer.MaxItemsInObjectGraph%2A> vlastnost <xref:System.Runtime.Serialization.DataContractSerializer> na hodnotu bezpečné.</span><span class="sxs-lookup"><span data-stu-id="743a8-230">Set the <xref:System.Runtime.Serialization.DataContractSerializer.MaxItemsInObjectGraph%2A> property of the <xref:System.Runtime.Serialization.DataContractSerializer> to a safe value.</span></span> <span data-ttu-id="743a8-231">Tato kvóta je také k dispozici na <xref:System.ServiceModel.ServiceBehaviorAttribute> atribut nebo prostřednictvím konfigurace.</span><span class="sxs-lookup"><span data-stu-id="743a8-231">This quota is also available on the <xref:System.ServiceModel.ServiceBehaviorAttribute> attribute or through configuration.</span></span> <span data-ttu-id="743a8-232">Tato kvóta omezí počet objektů, které jsou v jedné díl deserializace deserializovat.</span><span class="sxs-lookup"><span data-stu-id="743a8-232">This quota limits the number of objects that are deserialized in one deserialization episode.</span></span> <span data-ttu-id="743a8-233">Za normálních okolností je v jedné díl deserializovat každou operaci parametr nebo zprávy část textu v kontrakt zprávy.</span><span class="sxs-lookup"><span data-stu-id="743a8-233">Normally, each operation parameter or message body part in a message contract is deserialized in one episode.</span></span> <span data-ttu-id="743a8-234">Při deserializaci pole, každé položky pole se počítá jako samostatný objekt.</span><span class="sxs-lookup"><span data-stu-id="743a8-234">When deserializing arrays, each array entry is counted as a separate object.</span></span>  
  
-   <span data-ttu-id="743a8-235">Nastavte všechny kvóty čtečky XML bezpečné hodnoty.</span><span class="sxs-lookup"><span data-stu-id="743a8-235">Set all of the XML reader quotas to safe values.</span></span> <span data-ttu-id="743a8-236">Věnujte pozornost <xref:System.Xml.XmlDictionaryReaderQuotas.MaxDepth%2A>, <xref:System.Xml.XmlDictionaryReaderQuotas.MaxStringContentLength%2A>, a <xref:System.Xml.XmlDictionaryReaderQuotas.MaxArrayLength%2A> a vyhnout se řetězce v operacích streamování.</span><span class="sxs-lookup"><span data-stu-id="743a8-236">Pay attention to <xref:System.Xml.XmlDictionaryReaderQuotas.MaxDepth%2A>, <xref:System.Xml.XmlDictionaryReaderQuotas.MaxStringContentLength%2A>, and <xref:System.Xml.XmlDictionaryReaderQuotas.MaxArrayLength%2A> and avoid strings in non-streaming operations.</span></span>  
  
-   <span data-ttu-id="743a8-237">Projděte si seznam známé typy, pamatujte, že kterékoli z nich může být vytvořena instance kdykoli (viz část "Brání nezamýšleným typy z načítá" dál v tomto tématu).</span><span class="sxs-lookup"><span data-stu-id="743a8-237">Review the list of known types, keeping in mind that any one of them can be instantiated at any time (see the "Preventing Unintended Types from Being Loaded" section later in this topic).</span></span>  
  
-   <span data-ttu-id="743a8-238">Nepoužívejte všechny typy, které implementují <xref:System.Xml.Serialization.IXmlSerializable> rozhraní, které ukládat velké množství dat do vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="743a8-238">Do not use any types that implement the <xref:System.Xml.Serialization.IXmlSerializable> interface that buffer a lot of data.</span></span> <span data-ttu-id="743a8-239">Nepřidávejte tyto typy do seznamu ze známých typů.</span><span class="sxs-lookup"><span data-stu-id="743a8-239">Do not add such types to the list of known types.</span></span>  
  
-   <span data-ttu-id="743a8-240">Nepoužívejte <xref:System.Xml.XmlElement>, <xref:System.Xml.XmlNode> pole, <xref:System.Byte> maticových nebo které implementují typy <xref:System.Runtime.Serialization.ISerializable> ve smlouvě.</span><span class="sxs-lookup"><span data-stu-id="743a8-240">Do not use the <xref:System.Xml.XmlElement>, <xref:System.Xml.XmlNode> arrays, <xref:System.Byte> arrays, or types that implement <xref:System.Runtime.Serialization.ISerializable> in a contract.</span></span>  
  
-   <span data-ttu-id="743a8-241">Nepoužívejte <xref:System.Xml.XmlElement>, <xref:System.Xml.XmlNode> pole, <xref:System.Byte> maticových nebo které implementují typy <xref:System.Runtime.Serialization.ISerializable> v seznamu známých typů.</span><span class="sxs-lookup"><span data-stu-id="743a8-241">Do not use the <xref:System.Xml.XmlElement>, <xref:System.Xml.XmlNode> arrays, <xref:System.Byte> arrays, or types that implement <xref:System.Runtime.Serialization.ISerializable> in the list of known types.</span></span>  
  
 <span data-ttu-id="743a8-242">Předchozí opatření použít, když používá operaci streamování <xref:System.Runtime.Serialization.DataContractSerializer>.</span><span class="sxs-lookup"><span data-stu-id="743a8-242">The preceding precautions apply when the non-streamed operation uses the <xref:System.Runtime.Serialization.DataContractSerializer>.</span></span> <span data-ttu-id="743a8-243">Nikdy kombinovat streamování a -streamování programování modely na stejnou službu, pokud používáte <xref:System.Xml.Serialization.XmlSerializer>, protože nemá ochranu <xref:System.ServiceModel.Description.DataContractSerializerOperationBehavior.MaxItemsInObjectGraph%2A> kvóty.</span><span class="sxs-lookup"><span data-stu-id="743a8-243">Never mix streaming and non-streaming programming models on the same service if you are using the <xref:System.Xml.Serialization.XmlSerializer>, because it does not have the protection of the <xref:System.ServiceModel.Description.DataContractSerializerOperationBehavior.MaxItemsInObjectGraph%2A> quota.</span></span>  
  
### <a name="slow-stream-attacks"></a><span data-ttu-id="743a8-244">Útoky pomalé datového proudu</span><span class="sxs-lookup"><span data-stu-id="743a8-244">Slow Stream Attacks</span></span>  
 <span data-ttu-id="743a8-245">Třída útoků odmítnutí služby streamování nezahrnuje využití paměti.</span><span class="sxs-lookup"><span data-stu-id="743a8-245">A class of streaming denial-of-service attacks does not involve memory consumption.</span></span> <span data-ttu-id="743a8-246">Místo toho útoku zahrnuje pomalé odesílatel nebo příjemce data.</span><span class="sxs-lookup"><span data-stu-id="743a8-246">Instead, the attack involves a slow sender or receiver of data.</span></span> <span data-ttu-id="743a8-247">Při čekání na data, která mají být odesílané nebo přijímané, vyčerpání prostředků, jako je například vláken a dostupné připojení.</span><span class="sxs-lookup"><span data-stu-id="743a8-247">While waiting for the data to be sent or received, resources such as threads and available connections are exhausted.</span></span> <span data-ttu-id="743a8-248">Tato situace může nastat v důsledku napadením se zlými úmysly nebo z legitimní odesílatel/příjemce na pomalé síťové připojení.</span><span class="sxs-lookup"><span data-stu-id="743a8-248">This situation could arise either as a result of a malicious attack or from a legitimate sender/receiver on a slow network connection.</span></span>  
  
 <span data-ttu-id="743a8-249">Zmírnit tyto útoky, správně nastavené časové limity přenosu.</span><span class="sxs-lookup"><span data-stu-id="743a8-249">To mitigate these attacks, set the transport time-outs correctly.</span></span> <span data-ttu-id="743a8-250">Další informace najdete v tématu [přenosové kvóty](../../../../docs/framework/wcf/feature-details/transport-quotas.md).</span><span class="sxs-lookup"><span data-stu-id="743a8-250">For more information, see [Transport Quotas](../../../../docs/framework/wcf/feature-details/transport-quotas.md).</span></span> <span data-ttu-id="743a8-251">Za druhé, nikdy nepoužívejte synchronní `Read` nebo `Write` operace při práci s datovými proudy v WCF.</span><span class="sxs-lookup"><span data-stu-id="743a8-251">Secondly, never use synchronous `Read` or `Write` operations when working with streams in WCF.</span></span>  
  
## <a name="using-xml-safely"></a><span data-ttu-id="743a8-252">Bezpečně pomocí XML</span><span class="sxs-lookup"><span data-stu-id="743a8-252">Using XML Safely</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="743a8-253">I když tato část se týká XML, informace platí také pro dokumenty JavaScript Object Notation (JSON).</span><span class="sxs-lookup"><span data-stu-id="743a8-253">Although this section is about XML, the information also applies to JavaScript Object Notation (JSON) documents.</span></span> <span data-ttu-id="743a8-254">Kvóty fungují podobně, pomocí [mapování mezi JSON a XML](../../../../docs/framework/wcf/feature-details/mapping-between-json-and-xml.md).</span><span class="sxs-lookup"><span data-stu-id="743a8-254">The quotas work similarly, using [Mapping Between JSON and XML](../../../../docs/framework/wcf/feature-details/mapping-between-json-and-xml.md).</span></span>  
  
### <a name="secure-xml-readers"></a><span data-ttu-id="743a8-255">Zabezpečení čtečky XML</span><span class="sxs-lookup"><span data-stu-id="743a8-255">Secure XML Readers</span></span>  
 <span data-ttu-id="743a8-256">Informační sadu XML je základem veškeré zpracování zpráv ve WCF.</span><span class="sxs-lookup"><span data-stu-id="743a8-256">The XML Infoset forms the basis of all message processing in WCF.</span></span> <span data-ttu-id="743a8-257">Pokud existují přijímá data XML z nedůvěryhodného zdroje, počet možnosti útok s cílem služby, musí být omezeny.</span><span class="sxs-lookup"><span data-stu-id="743a8-257">When accepting XML data from an untrusted source, a number of denial-of-service attack possibilities exist that must be mitigated.</span></span> <span data-ttu-id="743a8-258">WCF poskytuje speciální, zabezpečte čtečky XML.</span><span class="sxs-lookup"><span data-stu-id="743a8-258">WCF provides special, secure XML readers.</span></span> <span data-ttu-id="743a8-259">Tyto čtečky automaticky vytvoří při použití jednu standardní kódování ve WCF (text, binary nebo MTOM).</span><span class="sxs-lookup"><span data-stu-id="743a8-259">These readers are created automatically when using one of the standard encodings in WCF (text, binary, or MTOM).</span></span>  
  
 <span data-ttu-id="743a8-260">Některé z funkcí zabezpečení na těchto čtečky jsou vždy aktivní.</span><span class="sxs-lookup"><span data-stu-id="743a8-260">Some of the security features on these readers are always active.</span></span> <span data-ttu-id="743a8-261">Například čtečky nikdy zpracovat definice typu dokumentu (DTD), které jsou potenciální zdroj útoky DOS a by se nikdy zobrazit v legitimní protokolu SOAP zprávy.</span><span class="sxs-lookup"><span data-stu-id="743a8-261">For example, the readers never process document type definitions (DTDs), which are a potential source of denial-of-service attacks and should never appear in legitimate SOAP messages.</span></span> <span data-ttu-id="743a8-262">Další funkce zabezpečení zahrnují kvóty čtečky, které musí být nakonfigurován, které jsou popsané v následující části.</span><span class="sxs-lookup"><span data-stu-id="743a8-262">Other security features include reader quotas that must be configured, which are described in the following section.</span></span>  
  
 <span data-ttu-id="743a8-263">Při práci přímo s čtečky XML (například při zápisu vlastní kodér nebo při práci přímo s <xref:System.ServiceModel.Channels.Message> – třída), vždy používali zabezpečené čtečky WCF, když je možné pracovat s nedůvěryhodné data.</span><span class="sxs-lookup"><span data-stu-id="743a8-263">When working directly with XML readers (such as when writing your own custom encoder or when working directly with the <xref:System.ServiceModel.Channels.Message> class), always use the WCF secure readers when there is a chance of working with untrusted data.</span></span> <span data-ttu-id="743a8-264">Vytvoření zabezpečeného čtečky volání jeden statický objektu pro vytváření přetížení metody <xref:System.Xml.XmlDictionaryReader.CreateTextReader%2A>, <xref:System.Xml.XmlDictionaryReader.CreateBinaryReader%2A>, nebo <xref:System.Xml.XmlDictionaryReader.CreateMtomReader%2A> na <xref:System.Xml.XmlDictionaryReader> třídy.</span><span class="sxs-lookup"><span data-stu-id="743a8-264">Create the secure readers by calling one of the static factory method overloads of <xref:System.Xml.XmlDictionaryReader.CreateTextReader%2A>, <xref:System.Xml.XmlDictionaryReader.CreateBinaryReader%2A>, or <xref:System.Xml.XmlDictionaryReader.CreateMtomReader%2A> on the <xref:System.Xml.XmlDictionaryReader> class.</span></span> <span data-ttu-id="743a8-265">Při vytváření čtečka čipových karet, předejte hodnoty zabezpečené kvóty.</span><span class="sxs-lookup"><span data-stu-id="743a8-265">When creating a reader, pass in secure quota values.</span></span> <span data-ttu-id="743a8-266">Nevolejte `Create` přetížení metody.</span><span class="sxs-lookup"><span data-stu-id="743a8-266">Do not call the `Create` method overloads.</span></span> <span data-ttu-id="743a8-267">Tyto nevytvářejte čtečku WCF.</span><span class="sxs-lookup"><span data-stu-id="743a8-267">These do not create a WCF reader.</span></span> <span data-ttu-id="743a8-268">Místo toho se vytvoří čtečka čipových karet, který není chráněný pomocí funkce zabezpečení, které jsou popsané v této části.</span><span class="sxs-lookup"><span data-stu-id="743a8-268">Instead, a reader is created that is not protected by the security features described in this section.</span></span>  
  
### <a name="reader-quotas"></a><span data-ttu-id="743a8-269">Čtečka kvóty</span><span class="sxs-lookup"><span data-stu-id="743a8-269">Reader Quotas</span></span>  
 <span data-ttu-id="743a8-270">Zabezpečené čtečky XML mít pět konfigurovat kvóty.</span><span class="sxs-lookup"><span data-stu-id="743a8-270">The secure XML readers have five configurable quotas.</span></span> <span data-ttu-id="743a8-271">To se obvykle konfiguruje pomocí `ReaderQuotas` vlastnost na kódování prvky vazeb nebo standardní vazby, nebo můžete použít <xref:System.Xml.XmlDictionaryReaderQuotas> objekt předaný při vytváření čtečka čipových karet.</span><span class="sxs-lookup"><span data-stu-id="743a8-271">These are normally configured using the `ReaderQuotas` property on the encoding binding elements or standard bindings, or by using an <xref:System.Xml.XmlDictionaryReaderQuotas> object passed when creating a reader.</span></span>  
  
#### <a name="maxbytesperread"></a><span data-ttu-id="743a8-272">MaxBytesPerRead</span><span class="sxs-lookup"><span data-stu-id="743a8-272">MaxBytesPerRead</span></span>  
 <span data-ttu-id="743a8-273">Tato kvóta omezuje počet bajtů, které se čtou v jediném `Read` operace při čtení element spustit značku a její atributy.</span><span class="sxs-lookup"><span data-stu-id="743a8-273">This quota limits the number of bytes that are read in a single `Read` operation when reading the element start tag and its attributes.</span></span> <span data-ttu-id="743a8-274">(V případech,-streamování, není počítáno samotný název elementu proti kvótu.) <xref:System.Xml.XmlDictionaryReaderQuotas.MaxBytesPerRead%2A> je důležité z následujících důvodů:</span><span class="sxs-lookup"><span data-stu-id="743a8-274">(In non-streamed cases, the element name itself is not counted against the quota.) <xref:System.Xml.XmlDictionaryReaderQuotas.MaxBytesPerRead%2A> is important for the following reasons:</span></span>  
  
-   <span data-ttu-id="743a8-275">Název elementu a jeho atributy jsou vždy do vyrovnávací paměti v paměti při jejich přečtení.</span><span class="sxs-lookup"><span data-stu-id="743a8-275">The element name and its attributes are always buffered in memory when they are being read.</span></span> <span data-ttu-id="743a8-276">Proto je důležité nastavit tuto kvótu správně při streamování režimu, aby se zabránilo nadměrnému ukládání do vyrovnávací paměti při streamování se očekává.</span><span class="sxs-lookup"><span data-stu-id="743a8-276">Therefore, it is important to set this quota correctly in streaming mode to prevent excessive buffering when streaming is expected.</span></span> <span data-ttu-id="743a8-277">Najdete v článku `MaxDepth` části kvóta informace o skutečné množství ukládání do vyrovnávací paměti, který probíhá.</span><span class="sxs-lookup"><span data-stu-id="743a8-277">See the `MaxDepth` quota section for information about the actual amount of buffering that takes place.</span></span>  
  
-   <span data-ttu-id="743a8-278">Má příliš mnoho atributů XML může použít až nesoustředil příliš velký doba zpracování, protože názvy atributů musí být vráceny jedinečnosti.</span><span class="sxs-lookup"><span data-stu-id="743a8-278">Having too many XML attributes may use up disproportionate processing time because attribute names have to be checked for uniqueness.</span></span> <span data-ttu-id="743a8-279">`MaxBytesPerRead` omezuje tuto hrozbu.</span><span class="sxs-lookup"><span data-stu-id="743a8-279">`MaxBytesPerRead` mitigates this threat.</span></span>  
  
#### <a name="maxdepth"></a><span data-ttu-id="743a8-280">MaxDepth</span><span class="sxs-lookup"><span data-stu-id="743a8-280">MaxDepth</span></span>  
 <span data-ttu-id="743a8-281">Tato kvóta omezuje maximální hloubky vnoření elementů XML.</span><span class="sxs-lookup"><span data-stu-id="743a8-281">This quota limits the maximum nesting depth of XML elements.</span></span> <span data-ttu-id="743a8-282">Například v dokumentu "\<A >\<B >\<C / >\</B >\</A >" se hloubky vnoření tří.</span><span class="sxs-lookup"><span data-stu-id="743a8-282">For example, the document "\<A>\<B>\<C/>\</B>\</A>" has a nesting depth of three.</span></span> <span data-ttu-id="743a8-283"><xref:System.Xml.XmlDictionaryReaderQuotas.MaxDepth%2A> je důležité z následujících důvodů:</span><span class="sxs-lookup"><span data-stu-id="743a8-283"><xref:System.Xml.XmlDictionaryReaderQuotas.MaxDepth%2A> is important for the following reasons:</span></span>  
  
-   <span data-ttu-id="743a8-284">`MaxDepth` komunikuje s `MaxBytesPerRead`: Čtečka vždy uchovává data v paměti pro aktuální prvek a veškeré jeho předchůdců, je spotřeba maximální paměť čtečky úměrný tato dvě nastavení.</span><span class="sxs-lookup"><span data-stu-id="743a8-284">`MaxDepth` interacts with `MaxBytesPerRead`: the reader always keeps data in memory for the current element and all of its ancestors, so the maximum memory consumption of the reader is proportional to the product of these two settings.</span></span>  
  
-   <span data-ttu-id="743a8-285">Při deserializaci hluboko vnořený objekt grafu, pro přístup k celý zásobník a throw k neodstranitelné je nucen deserializátor <xref:System.StackOverflowException>.</span><span class="sxs-lookup"><span data-stu-id="743a8-285">When deserializing a deeply-nested object graph, the deserializer is forced to access the entire stack and throw an unrecoverable <xref:System.StackOverflowException>.</span></span> <span data-ttu-id="743a8-286">Přímá korelace mezi vnoření XML a objekt vnoření obě existuje <xref:System.Runtime.Serialization.DataContractSerializer> a <xref:System.Xml.Serialization.XmlSerializer>.</span><span class="sxs-lookup"><span data-stu-id="743a8-286">A direct correlation exists between XML nesting and object nesting for both the <xref:System.Runtime.Serialization.DataContractSerializer> and the <xref:System.Xml.Serialization.XmlSerializer>.</span></span> <span data-ttu-id="743a8-287">Použití `MaxDepth` pro zmírnění této hrozby.</span><span class="sxs-lookup"><span data-stu-id="743a8-287">Use `MaxDepth` to mitigate this threat.</span></span>  
  
#### <a name="maxnametablecharcount"></a><span data-ttu-id="743a8-288">MaxNameTableCharCount</span><span class="sxs-lookup"><span data-stu-id="743a8-288">MaxNameTableCharCount</span></span>  
 <span data-ttu-id="743a8-289">Tato kvóta omezuje velikost čtenáře *tabulky názvů*.</span><span class="sxs-lookup"><span data-stu-id="743a8-289">This quota limits the size of the reader’s *nametable*.</span></span> <span data-ttu-id="743a8-290">Pracovat s tabulkou názvů obsahuje některé řetězce (například obory názvů a předpony), které jsou došlo při zpracování dokument XML.</span><span class="sxs-lookup"><span data-stu-id="743a8-290">The nametable contains certain strings (such as namespaces and prefixes) that are encountered when processing an XML document.</span></span> <span data-ttu-id="743a8-291">Tyto řetězce jsou uložená do vyrovnávací paměti v paměti, nastavte tuto kvótu, aby se zabránilo nadměrnému ukládání do vyrovnávací paměti při streamování se očekává.</span><span class="sxs-lookup"><span data-stu-id="743a8-291">As these strings are buffered in memory, set this quota to prevent excessive buffering when streaming is expected.</span></span>  
  
#### <a name="maxstringcontentlength"></a><span data-ttu-id="743a8-292">MaxStringContentLength</span><span class="sxs-lookup"><span data-stu-id="743a8-292">MaxStringContentLength</span></span>  
 <span data-ttu-id="743a8-293">Tato kvóta omezuje velikost maximální řetězec, který vrací čtecí modul XML.</span><span class="sxs-lookup"><span data-stu-id="743a8-293">This quota limits the maximum string size that the XML reader returns.</span></span> <span data-ttu-id="743a8-294">Tato kvóta neomezuje využití paměti v samotné čtecí modul XML, ale v komponentě používající čtečky.</span><span class="sxs-lookup"><span data-stu-id="743a8-294">This quota does not limit memory consumption in the XML reader itself, but in the component that is using the reader.</span></span> <span data-ttu-id="743a8-295">Například, když <xref:System.Runtime.Serialization.DataContractSerializer> používá čtečku zabezpečen <xref:System.Xml.XmlDictionaryReaderQuotas.MaxStringContentLength%2A>, ji deserializovat není větší než tato kvóta řetězce.</span><span class="sxs-lookup"><span data-stu-id="743a8-295">For example, when the <xref:System.Runtime.Serialization.DataContractSerializer> uses a reader secured with <xref:System.Xml.XmlDictionaryReaderQuotas.MaxStringContentLength%2A>, it does not deserialize strings larger than this quota.</span></span> <span data-ttu-id="743a8-296">Při použití <xref:System.Xml.XmlDictionaryReader> třídy přímo, ne všechny metody ohledem tato kvóta, ale jenom metody, které jsou vytvořené speciálně číst řetězce, například <xref:System.Xml.XmlDictionaryReader.ReadContentAsString%2A> metoda.</span><span class="sxs-lookup"><span data-stu-id="743a8-296">When using the <xref:System.Xml.XmlDictionaryReader> class directly, not all methods respect this quota, but only the methods that are specifically designed to read strings, such as the <xref:System.Xml.XmlDictionaryReader.ReadContentAsString%2A> method.</span></span> <span data-ttu-id="743a8-297"><xref:System.Xml.XmlReader.Value%2A> Vlastnost čtečky nemá vliv tato kvóta a proto by nemělo použít, pokud ochrany poskytuje tuto kvótu je nutné.</span><span class="sxs-lookup"><span data-stu-id="743a8-297">The <xref:System.Xml.XmlReader.Value%2A> property on the reader is not affected by this quota, and thus should not be used when the protection this quota provides is necessary.</span></span>  
  
#### <a name="maxarraylength"></a><span data-ttu-id="743a8-298">MaxArrayLength</span><span class="sxs-lookup"><span data-stu-id="743a8-298">MaxArrayLength</span></span>  
 <span data-ttu-id="743a8-299">Tato kvóta omezuje maximální velikost pole primitivních elementů, které vrací čtecí modul XML, včetně pole bajtů.</span><span class="sxs-lookup"><span data-stu-id="743a8-299">This quota limits the maximum size of an array of primitives that the XML reader returns, including byte arrays.</span></span> <span data-ttu-id="743a8-300">Tato kvóta neomezuje využití paměti v samotné čtecí modul XML, ale v jakémkoli součásti používající čtečky.</span><span class="sxs-lookup"><span data-stu-id="743a8-300">This quota does not limit memory consumption in the XML reader itself, but in whatever component that is using the reader.</span></span> <span data-ttu-id="743a8-301">Například, když <xref:System.Runtime.Serialization.DataContractSerializer> používá čtečku zabezpečen <xref:System.Xml.XmlDictionaryReaderQuotas.MaxArrayLength%2A>, ji deserializovat není větší než tato kvóta pole bajtů.</span><span class="sxs-lookup"><span data-stu-id="743a8-301">For example, when the <xref:System.Runtime.Serialization.DataContractSerializer> uses a reader secured with <xref:System.Xml.XmlDictionaryReaderQuotas.MaxArrayLength%2A>, it does not deserialize byte arrays larger than this quota.</span></span> <span data-ttu-id="743a8-302">Je důležité nastavit tuto kvótu při pokusu o kombinovat streamování a ve vyrovnávací paměti programovací modely v jedné smlouvy.</span><span class="sxs-lookup"><span data-stu-id="743a8-302">It is important to set this quota when attempting to mix streaming and buffered programming models in a single contract.</span></span> <span data-ttu-id="743a8-303">Mějte na paměti, že při použití <xref:System.Xml.XmlDictionaryReader> přímo, třídu pouze metody, které jsou vytvořené speciálně číst pole libovolné velikosti určité primitivní typy, jako například <xref:System.Xml.XmlDictionaryReader.ReadInt32Array%2A>, respektují tuto kvótu.</span><span class="sxs-lookup"><span data-stu-id="743a8-303">Keep in mind that when using the <xref:System.Xml.XmlDictionaryReader> class directly, only the methods that are specifically designed to read arrays of arbitrary size of certain primitive types, such as <xref:System.Xml.XmlDictionaryReader.ReadInt32Array%2A>, respect this quota.</span></span>  
  
## <a name="threats-specific-to-the-binary-encoding"></a><span data-ttu-id="743a8-304">Specifické hrozby pro binární kódování</span><span class="sxs-lookup"><span data-stu-id="743a8-304">Threats Specific to the Binary Encoding</span></span>  
 <span data-ttu-id="743a8-305">Obsahuje binární kódování WCF podporuje XML *slovníku řetězce* funkce.</span><span class="sxs-lookup"><span data-stu-id="743a8-305">The binary XML encoding WCF supports includes a *dictionary strings* feature.</span></span> <span data-ttu-id="743a8-306">Řetězec velký může kódované pomocí pouze několik bajtů.</span><span class="sxs-lookup"><span data-stu-id="743a8-306">A large string may be encoded using only a few bytes.</span></span> <span data-ttu-id="743a8-307">To umožňuje výrazné zvýšení výkonu, ale zavádí nové hrozby odmítnutí služby, které musí být omezeny.</span><span class="sxs-lookup"><span data-stu-id="743a8-307">This enables significant performance gains, but introduces new denial-of-service threats that must be mitigated.</span></span>  
  
 <span data-ttu-id="743a8-308">Existují dva druhy slovník: *statické* a *dynamické*.</span><span class="sxs-lookup"><span data-stu-id="743a8-308">There are two kinds of dictionaries: *static* and *dynamic*.</span></span> <span data-ttu-id="743a8-309">Statické slovník je předdefinovaný seznam dlouhých řetězců, které může být reprezentován pomocí krátký kód v binární kódování.</span><span class="sxs-lookup"><span data-stu-id="743a8-309">The static dictionary is a built-in list of long strings that may be represented using a short code in the binary encoding.</span></span> <span data-ttu-id="743a8-310">Tento seznam řetězců vyřešen, když čtečka je vytvořena a nelze jej změnit.</span><span class="sxs-lookup"><span data-stu-id="743a8-310">This list of strings is fixed when the reader is created and cannot be modified.</span></span> <span data-ttu-id="743a8-311">Žádný z řetězce ve slovníku statické WCF používá ve výchozím nastavení není dostatečně velký pro představovat závažné ohrožení, odmítnutí služby, přestože může být přesto použít v slovníkový útok rozšíření.</span><span class="sxs-lookup"><span data-stu-id="743a8-311">None of the strings in the static dictionary that WCF uses by default are sufficiently large to pose a serious denial-of-service threat, although they may still be used in a dictionary expansion attack.</span></span> <span data-ttu-id="743a8-312">V pokročilých scénářích, kde zadáte vlastní statické slovník buďte opatrní při představení řetězců velké slovníku.</span><span class="sxs-lookup"><span data-stu-id="743a8-312">In advanced scenarios where you supply your own static dictionary, be careful when introducing large dictionary strings.</span></span>  
  
 <span data-ttu-id="743a8-313">Funkce dynamické slovník umožňuje zprávy a pokuste se definovat vlastní řetězce a přidružte je ke krátké kódy.</span><span class="sxs-lookup"><span data-stu-id="743a8-313">The dynamic dictionaries feature allows messages to define their own strings and associate them with short codes.</span></span> <span data-ttu-id="743a8-314">Tato mapování řetězec kódu jsou uloženy v paměti během celé komunikace relace tak, aby následné zprávy není nutné znovu odeslal řetězce a může využívat kódy, které jsou již definováni.</span><span class="sxs-lookup"><span data-stu-id="743a8-314">These string-to-code mappings are kept in memory during the entire communication session, such that subsequent messages do not have to resend the strings and can utilize codes that are already defined.</span></span> <span data-ttu-id="743a8-315">Tyto řetězce mohou být o libovolné délce a proto představovat hrozbu závažnější než ty, které ve slovníku statické.</span><span class="sxs-lookup"><span data-stu-id="743a8-315">These strings may be of arbitrary length and thus pose a more serious threat than those in the static dictionary.</span></span>  
  
 <span data-ttu-id="743a8-316">První ohrožení, které musí být omezeny je možnost výskytu nekonzistentních je dynamický slovník (tabulky mapování řetězec kódu), příliš velká.</span><span class="sxs-lookup"><span data-stu-id="743a8-316">The first threat that must be mitigated is the possibility of the dynamic dictionary (the string-to-code mapping table) becoming too large.</span></span> <span data-ttu-id="743a8-317">Rozbalit tohoto slovníku během několika zpráv a proto `MaxReceivedMessageSize` kvóty nabízí žádná ochrana, protože se vztahuje pouze na každou zprávu samostatně.</span><span class="sxs-lookup"><span data-stu-id="743a8-317">This dictionary may be expanded over the course of several messages, and so the `MaxReceivedMessageSize` quota offers no protection because it applies only to each message separately.</span></span> <span data-ttu-id="743a8-318">Proto samostatné <xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement.MaxSessionSize%2A> vlastnost existuje na <xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement> , která omezuje velikost slovníku.</span><span class="sxs-lookup"><span data-stu-id="743a8-318">Therefore, a separate <xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement.MaxSessionSize%2A> property exists on the <xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement> that limits the size of the dictionary.</span></span>  
  
 <span data-ttu-id="743a8-319">Na rozdíl od většiny jiných kvóty tato kvóta platí i v případě zápis zprávy.</span><span class="sxs-lookup"><span data-stu-id="743a8-319">Unlike most other quotas, this quota also applies when writing messages.</span></span> <span data-ttu-id="743a8-320">Pokud dojde k překročení při čtení zprávy, `QuotaExceededException` je vyvolána jako obvykle.</span><span class="sxs-lookup"><span data-stu-id="743a8-320">If it is exceeded when reading a message, the `QuotaExceededException` is thrown as usual.</span></span> <span data-ttu-id="743a8-321">Pokud je překročena při zápisu zprávu, všechny řetězce, které způsobí překročení kvóty se zapisují jako-je, bez použití funkce dynamické slovník.</span><span class="sxs-lookup"><span data-stu-id="743a8-321">If it is exceeded when writing a message, any strings that cause the quota to be exceeded are written as-is, without using the dynamic dictionaries feature.</span></span>  
  
### <a name="dictionary-expansion-threats"></a><span data-ttu-id="743a8-322">Slovník rozšíření hrozeb</span><span class="sxs-lookup"><span data-stu-id="743a8-322">Dictionary Expansion Threats</span></span>  
 <span data-ttu-id="743a8-323">Třídu významné útoků specifické pro binární vyplývá z rozšíření slovníku.</span><span class="sxs-lookup"><span data-stu-id="743a8-323">A significant class of binary-specific attacks arises from dictionary expansion.</span></span> <span data-ttu-id="743a8-324">Krátké zprávy v binární podobě může zapněte do velké zprávy ve plně rozšířené textové formě Pokud využívají funkci slovník řetězec.</span><span class="sxs-lookup"><span data-stu-id="743a8-324">A small message in binary form may turn into a very large message in fully expanded textual form if it makes extensive use of the string dictionaries feature.</span></span> <span data-ttu-id="743a8-325">Faktor rozšíření pro dynamický slovník řetězce je omezena <xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement.MaxSessionSize%2A> kvótu, protože žádný dynamický slovník řetězec přesahuje maximální velikost celý slovníku.</span><span class="sxs-lookup"><span data-stu-id="743a8-325">The expansion factor for dynamic dictionary strings is limited by the <xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement.MaxSessionSize%2A> quota, because no dynamic dictionary string exceeds the maximum size of the entire dictionary.</span></span>  
  
 <span data-ttu-id="743a8-326"><xref:System.Xml.XmlDictionaryReaderQuotas.MaxNameTableCharCount%2A>, `MaxStringContentLength`, A `MaxArrayLength` vlastnosti pouze omezení využití paměti.</span><span class="sxs-lookup"><span data-stu-id="743a8-326">The <xref:System.Xml.XmlDictionaryReaderQuotas.MaxNameTableCharCount%2A>, `MaxStringContentLength`, and `MaxArrayLength` properties only limit memory consumption.</span></span> <span data-ttu-id="743a8-327">Jsou obvykle není vyžadována zmírnit všechny hrozby využití datového proudu, protože využití paměti je již omezena `MaxReceivedMessageSize`.</span><span class="sxs-lookup"><span data-stu-id="743a8-327">They are normally not required to mitigate any threats in the non-streamed usage because memory usage is already limited by `MaxReceivedMessageSize`.</span></span> <span data-ttu-id="743a8-328">Ale `MaxReceivedMessageSize` počty předběžné rozšíření bajtů.</span><span class="sxs-lookup"><span data-stu-id="743a8-328">However, `MaxReceivedMessageSize` counts pre-expansion bytes.</span></span> <span data-ttu-id="743a8-329">Když binárního kódování se používá, využití paměti může potenciálně jdou nad rámec `MaxReceivedMessageSize`omezené jenom faktorem <xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement.MaxSessionSize%2A>.</span><span class="sxs-lookup"><span data-stu-id="743a8-329">When binary encoding is in use, memory consumption could potentially go beyond `MaxReceivedMessageSize`, limited only by a factor of <xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement.MaxSessionSize%2A>.</span></span> <span data-ttu-id="743a8-330">Z tohoto důvodu je důležité vždy nastavit všechny čtečky kvóty (zejména <xref:System.Xml.XmlDictionaryReaderQuotas.MaxStringContentLength%2A>) při použití binárního kódování.</span><span class="sxs-lookup"><span data-stu-id="743a8-330">For this reason, it is important to always set all of the reader quotas (especially <xref:System.Xml.XmlDictionaryReaderQuotas.MaxStringContentLength%2A>) when using the binary encoding.</span></span>  
  
 <span data-ttu-id="743a8-331">Při použití binárního kódování společně s <xref:System.Runtime.Serialization.DataContractSerializer>, `IExtensibleDataObject` rozhraní můžete došlo ke zneužití připojit slovníkový útok rozšíření.</span><span class="sxs-lookup"><span data-stu-id="743a8-331">When using binary encoding together with the <xref:System.Runtime.Serialization.DataContractSerializer>, the `IExtensibleDataObject` interface can be misused to mount a dictionary expansion attack.</span></span> <span data-ttu-id="743a8-332">Toto rozhraní v podstatě poskytuje neomezené úložiště pro libovolná data, která není součástí kontraktu.</span><span class="sxs-lookup"><span data-stu-id="743a8-332">This interface essentially provides unlimited storage for arbitrary data that is not a part of the contract.</span></span> <span data-ttu-id="743a8-333">Pokud nelze nastavit kvóty nízké tak, aby `MaxSessionSize` násobí hodnotou `MaxReceivedMessageSize` neobsahuje představovat problém, zakažte `IExtensibleDataObject` funkce při použití binárního kódování.</span><span class="sxs-lookup"><span data-stu-id="743a8-333">If quotas cannot be set low enough such that `MaxSessionSize` multiplied by `MaxReceivedMessageSize` does not pose a problem, disable the `IExtensibleDataObject` feature when using the binary encoding.</span></span> <span data-ttu-id="743a8-334">Nastavte `IgnoreExtensionDataObject` vlastnost `true` na `ServiceBehaviorAttribute` atribut.</span><span class="sxs-lookup"><span data-stu-id="743a8-334">Set the `IgnoreExtensionDataObject` property to `true` on the `ServiceBehaviorAttribute` attribute.</span></span> <span data-ttu-id="743a8-335">Alternativně neimplementují `IExtensibleDataObject` rozhraní.</span><span class="sxs-lookup"><span data-stu-id="743a8-335">Alternatively, do not implement the `IExtensibleDataObject` interface.</span></span> <span data-ttu-id="743a8-336">Další informace najdete v tématu [kontrakty dat dopřednou](../../../../docs/framework/wcf/feature-details/forward-compatible-data-contracts.md).</span><span class="sxs-lookup"><span data-stu-id="743a8-336">For more information, see [Forward-Compatible Data Contracts](../../../../docs/framework/wcf/feature-details/forward-compatible-data-contracts.md).</span></span>  
  
### <a name="quotas-summary"></a><span data-ttu-id="743a8-337">Souhrn kvóty</span><span class="sxs-lookup"><span data-stu-id="743a8-337">Quotas Summary</span></span>  
 <span data-ttu-id="743a8-338">Následující tabulka shrnuje informace o kvóty.</span><span class="sxs-lookup"><span data-stu-id="743a8-338">The following table summarizes the guidance about quotas.</span></span>  
  
|<span data-ttu-id="743a8-339">Podmínka</span><span class="sxs-lookup"><span data-stu-id="743a8-339">Condition</span></span>|<span data-ttu-id="743a8-340">Chcete-li nastavit důležité kvóty</span><span class="sxs-lookup"><span data-stu-id="743a8-340">Important quotas to set</span></span>|  
|---------------|-----------------------------|  
|<span data-ttu-id="743a8-341">Žádné streamování nebo streamování malých zpráv, text nebo kódování MTOM</span><span class="sxs-lookup"><span data-stu-id="743a8-341">No streaming or streaming small messages, text, or MTOM encoding</span></span>|<span data-ttu-id="743a8-342">`MaxReceivedMessageSize`, `MaxBytesPerRead`, a `MaxDepth`</span><span class="sxs-lookup"><span data-stu-id="743a8-342">`MaxReceivedMessageSize`, `MaxBytesPerRead`, and `MaxDepth`</span></span>|  
|<span data-ttu-id="743a8-343">Žádné streamování nebo streamování malých zpráv, binární kódování</span><span class="sxs-lookup"><span data-stu-id="743a8-343">No streaming or streaming small messages, binary encoding</span></span>|<span data-ttu-id="743a8-344">`MaxReceivedMessageSize`, `MaxSessionSize`a všechny `ReaderQuotas`</span><span class="sxs-lookup"><span data-stu-id="743a8-344">`MaxReceivedMessageSize`, `MaxSessionSize`, and all `ReaderQuotas`</span></span>|  
|<span data-ttu-id="743a8-345">Streamování velké zprávy, text nebo kódování MTOM</span><span class="sxs-lookup"><span data-stu-id="743a8-345">Streaming large messages, text, or MTOM encoding</span></span>|<span data-ttu-id="743a8-346">`MaxBufferSize` a všechny `ReaderQuotas`</span><span class="sxs-lookup"><span data-stu-id="743a8-346">`MaxBufferSize` and all `ReaderQuotas`</span></span>|  
|<span data-ttu-id="743a8-347">Streamování velké zprávy, binární kódování</span><span class="sxs-lookup"><span data-stu-id="743a8-347">Streaming large messages, binary encoding</span></span>|<span data-ttu-id="743a8-348">`MaxBufferSize`, `MaxSessionSize`a všechny `ReaderQuotas`</span><span class="sxs-lookup"><span data-stu-id="743a8-348">`MaxBufferSize`, `MaxSessionSize`, and all `ReaderQuotas`</span></span>|  
  
-   <span data-ttu-id="743a8-349">Vypršení časových limitů transportní vrstvy musí být vždycky nastavená a nikdy nepoužívejte synchronní čtení/zápisu při streamování se používá, bez ohledu na to, jestli jsou streamování velké nebo malé zprávy.</span><span class="sxs-lookup"><span data-stu-id="743a8-349">Transport-level time-outs must always be set and never use synchronous reads/writes when streaming is in use, regardless of whether you are streaming large or small messages.</span></span>  
  
-   <span data-ttu-id="743a8-350">Pokud máte pochybnosti o kvótu, nastavte ji na hodnotu bezpečné místo ponechat otevřené.</span><span class="sxs-lookup"><span data-stu-id="743a8-350">When in doubt about a quota, set it to a safe value rather than leaving it open.</span></span>  
  
## <a name="preventing-malicious-code-execution"></a><span data-ttu-id="743a8-351">Brání spuštění škodlivého kódu</span><span class="sxs-lookup"><span data-stu-id="743a8-351">Preventing Malicious Code Execution</span></span>  
 <span data-ttu-id="743a8-352">Následující obecné třídy hrozeb můžete spustit kód a mít nežádoucí účinky:</span><span class="sxs-lookup"><span data-stu-id="743a8-352">The following general classes of threats can execute code and have unintended effects:</span></span>  
  
-   <span data-ttu-id="743a8-353">Načte deserializátor typu škodlivý, unsafe nebo citlivým z hlediska zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="743a8-353">The deserializer loads a malicious, unsafe, or security-sensitive type.</span></span>  
  
-   <span data-ttu-id="743a8-354">Příchozí zpráva způsobí, že deserializátor vytvořit instanci typu normálně bezpečné v například tak, že má nezamýšleným důsledky.</span><span class="sxs-lookup"><span data-stu-id="743a8-354">An incoming message causes the deserializer to construct an instance of a normally safe type in such a way that it has unintended consequences.</span></span>  
  
 <span data-ttu-id="743a8-355">Následující části popisují tyto třídy další hrozby.</span><span class="sxs-lookup"><span data-stu-id="743a8-355">The following sections discuss these classes of threats further.</span></span>  
  
## <a name="datacontractserializer"></a><span data-ttu-id="743a8-356">DataContractSerializer</span><span class="sxs-lookup"><span data-stu-id="743a8-356">DataContractSerializer</span></span>  
 <span data-ttu-id="743a8-357">(Zabezpečení informace na <xref:System.Xml.Serialization.XmlSerializer>, naleznete v příslušné dokumentaci.) Model zabezpečení pro <xref:System.Xml.Serialization.XmlSerializer> je podobné jako <xref:System.Runtime.Serialization.DataContractSerializer>a liší se většinou v podrobnostech.</span><span class="sxs-lookup"><span data-stu-id="743a8-357">(For security information on the <xref:System.Xml.Serialization.XmlSerializer>, see the relevant documentation.) The security model for the <xref:System.Xml.Serialization.XmlSerializer> is similar to that of the <xref:System.Runtime.Serialization.DataContractSerializer>, and differs mostly in details.</span></span> <span data-ttu-id="743a8-358">Například <xref:System.Xml.Serialization.XmlIncludeAttribute> atribut se používá pro zahrnutí typ místo <xref:System.Runtime.Serialization.KnownTypeAttribute> atribut.</span><span class="sxs-lookup"><span data-stu-id="743a8-358">For example, the <xref:System.Xml.Serialization.XmlIncludeAttribute> attribute is used for type inclusion instead of the <xref:System.Runtime.Serialization.KnownTypeAttribute> attribute.</span></span> <span data-ttu-id="743a8-359">Ale některé hrozby jedinečný <xref:System.Xml.Serialization.XmlSerializer> jsou popsány dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="743a8-359">However, some threats unique to the <xref:System.Xml.Serialization.XmlSerializer> are discussed later in this topic.</span></span>  
  
### <a name="preventing-unintended-types-from-being-loaded"></a><span data-ttu-id="743a8-360">Brání načítá nezamýšleným typy</span><span class="sxs-lookup"><span data-stu-id="743a8-360">Preventing Unintended Types from Being Loaded</span></span>  
 <span data-ttu-id="743a8-361">Načítání nezamýšleným typy může mít významné důsledky, jestli typ je škodlivý nebo právě má vedlejší účinky citlivé na zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="743a8-361">Loading unintended types may have significant consequences, whether the type is malicious or just has security-sensitive side effects.</span></span> <span data-ttu-id="743a8-362">Typ může obsahovat chyba zneužití zabezpečení, provádět akce citlivé na zabezpečení v jeho konstruktoru nebo konstruktoru třídy, mít nároků velké paměti, který usnadňuje útoky DOS, nebo může vyvolat neobnovitelná výjimky.</span><span class="sxs-lookup"><span data-stu-id="743a8-362">A type may contain exploitable security vulnerability, perform security-sensitive actions in its constructor or class constructor, have a large memory footprint that facilitates denial-of-service attacks, or may throw non-recoverable exceptions.</span></span> <span data-ttu-id="743a8-363">Typy může mít konstruktory třídy, které spustit hned, jak je načíst typ, a před vytvořením všechny instance.</span><span class="sxs-lookup"><span data-stu-id="743a8-363">Types may have class constructors that run as soon as the type is loaded and before any instances are created.</span></span> <span data-ttu-id="743a8-364">Z těchto důvodů je důležité pro řízení sadu typů, které deserializátor může načíst.</span><span class="sxs-lookup"><span data-stu-id="743a8-364">For these reasons, it is important to control the set of types that the deserializer may load.</span></span>  
  
 <span data-ttu-id="743a8-365"><xref:System.Runtime.Serialization.DataContractSerializer> Deserializuje volně párované způsobem.</span><span class="sxs-lookup"><span data-stu-id="743a8-365">The <xref:System.Runtime.Serialization.DataContractSerializer> deserializes in a loosely coupled way.</span></span> <span data-ttu-id="743a8-366">Nikdy přečte common language runtime (CLR) typu a sestavení názvy z příchozí data.</span><span class="sxs-lookup"><span data-stu-id="743a8-366">It never reads common language runtime (CLR) type and assembly names from the incoming data.</span></span> <span data-ttu-id="743a8-367">Toto je podobná chování <xref:System.Xml.Serialization.XmlSerializer>, ale se liší od chování <xref:System.Runtime.Serialization.NetDataContractSerializer>, <xref:System.Runtime.Serialization.Formatters.Binary.BinaryFormatter>a <xref:System.Runtime.Serialization.Formatters.Soap.SoapFormatter>.</span><span class="sxs-lookup"><span data-stu-id="743a8-367">This is similar to the behavior of the <xref:System.Xml.Serialization.XmlSerializer>, but differs from the behavior of the <xref:System.Runtime.Serialization.NetDataContractSerializer>, <xref:System.Runtime.Serialization.Formatters.Binary.BinaryFormatter>, and the <xref:System.Runtime.Serialization.Formatters.Soap.SoapFormatter>.</span></span> <span data-ttu-id="743a8-368">Volné párování představuje určitou úroveň zabezpečení, protože vzdálenému útočníkovi nesmí uvádět libovolný typ se načíst právě pojmenováním typu ve zprávě.</span><span class="sxs-lookup"><span data-stu-id="743a8-368">Loose coupling introduces a degree of safety, because the remote attacker cannot indicate an arbitrary type to load just by naming that type in the message.</span></span>  
  
 <span data-ttu-id="743a8-369"><xref:System.Runtime.Serialization.DataContractSerializer> Je vždycky povolená načíst typ, který je aktuálně očekáván podle kontrakt.</span><span class="sxs-lookup"><span data-stu-id="743a8-369">The <xref:System.Runtime.Serialization.DataContractSerializer> is always allowed to load a type that is currently expected according to the contract.</span></span> <span data-ttu-id="743a8-370">Například, pokud kontraktu dat obsahuje data člena typu `Customer`, <xref:System.Runtime.Serialization.DataContractSerializer> může načíst `Customer` zadat, když ho deserializuje tento člen data.</span><span class="sxs-lookup"><span data-stu-id="743a8-370">For example, if a data contract has a data member of type `Customer`, the <xref:System.Runtime.Serialization.DataContractSerializer> is allowed to load the `Customer` type when it deserializes this data member.</span></span>  
  
 <span data-ttu-id="743a8-371">Kromě toho <xref:System.Runtime.Serialization.DataContractSerializer> podporuje polymorfismus.</span><span class="sxs-lookup"><span data-stu-id="743a8-371">Additionally, the <xref:System.Runtime.Serialization.DataContractSerializer> supports polymorphism.</span></span> <span data-ttu-id="743a8-372">Člen dat může být deklarována jako <xref:System.Object>, ale může obsahovat příchozích dat `Customer` instance.</span><span class="sxs-lookup"><span data-stu-id="743a8-372">A data member may be declared as <xref:System.Object>, but the incoming data may contain a `Customer` instance.</span></span> <span data-ttu-id="743a8-373">To je možné, pouze pokud `Customer` typ byl proveden "známé" deserializátor prostřednictvím jednoho z těchto mechanismů:</span><span class="sxs-lookup"><span data-stu-id="743a8-373">This is possible only if the `Customer` type has been made "known" to the deserializer through one of these mechanisms:</span></span>  
  
-   <span data-ttu-id="743a8-374"><xref:System.Runtime.Serialization.KnownTypeAttribute> Atribut použitý k typu.</span><span class="sxs-lookup"><span data-stu-id="743a8-374"><xref:System.Runtime.Serialization.KnownTypeAttribute> attribute applied to a type.</span></span>  
  
-   <span data-ttu-id="743a8-375">`KnownTypeAttribute` atribut určení metodu, která vrátí seznam typů.</span><span class="sxs-lookup"><span data-stu-id="743a8-375">`KnownTypeAttribute` attribute specifying a method that returns a list of types.</span></span>  
  
-   <span data-ttu-id="743a8-376">`ServiceKnownTypeAttribute` atribut.</span><span class="sxs-lookup"><span data-stu-id="743a8-376">`ServiceKnownTypeAttribute` attribute.</span></span>  
  
-   <span data-ttu-id="743a8-377">`KnownTypes` Konfigurační oddíl.</span><span class="sxs-lookup"><span data-stu-id="743a8-377">The `KnownTypes` configuration section.</span></span>  
  
-   <span data-ttu-id="743a8-378">Seznam známých typů explicitně předaný <xref:System.Runtime.Serialization.DataContractSerializer> během vytváření, pokud se používá serializátor přímo.</span><span class="sxs-lookup"><span data-stu-id="743a8-378">A list of known types explicitly passed to the <xref:System.Runtime.Serialization.DataContractSerializer> during construction, if using the serializer directly.</span></span>  
  
 <span data-ttu-id="743a8-379">Každý z těchto mechanismů zvyšuje pravděpodobnost zavedením další typy, které deserializátor může načíst.</span><span class="sxs-lookup"><span data-stu-id="743a8-379">Each of these mechanisms increases the surface area by introducing more types that the deserializer can load.</span></span> <span data-ttu-id="743a8-380">Řídit každou z těchto mechanismů zajistit, že žádné škodlivého nebo nežádoucího typy jsou přidány do seznamu známé typy.</span><span class="sxs-lookup"><span data-stu-id="743a8-380">Control each of these mechanisms to ensure no malicious or unintended types are added to the known types list.</span></span>  
  
 <span data-ttu-id="743a8-381">Jakmile známý typ je v oboru, mohou být načteny kdykoli a nelze vytvořit instance typu, i když kontrakt zakazuje ve skutečnosti jeho použití.</span><span class="sxs-lookup"><span data-stu-id="743a8-381">Once a known type is in scope, it can be loaded at any time, and instances of the type can be created, even if the contract forbids actually using it.</span></span> <span data-ttu-id="743a8-382">Předpokládejme například, typ, který "MyDangerousType" je přidán do seznamu známých typů pomocí jedné z výše uvedených mechanismů.</span><span class="sxs-lookup"><span data-stu-id="743a8-382">For example, suppose the type "MyDangerousType" is added to the known types list using one of the mechanisms above.</span></span> <span data-ttu-id="743a8-383">To znamená, že:</span><span class="sxs-lookup"><span data-stu-id="743a8-383">This means that:</span></span>  
  
-   <span data-ttu-id="743a8-384">`MyDangerousType` instaluje se a jeho spuštěním konstruktoru třídy.</span><span class="sxs-lookup"><span data-stu-id="743a8-384">`MyDangerousType` is loaded and its class constructor runs.</span></span>  
  
-   <span data-ttu-id="743a8-385">I když deserializaci kontraktu dat s členem řetězec dat, škodlivé zprávy může způsobit stále instanci `MyDangerousType` k vytvoření.</span><span class="sxs-lookup"><span data-stu-id="743a8-385">Even when deserializing a data contract with a string data member, a malicious message may still cause an instance of `MyDangerousType` to create.</span></span> <span data-ttu-id="743a8-386">Kód na `MyDangerousType`, například nastavením vlastností, může spustit.</span><span class="sxs-lookup"><span data-stu-id="743a8-386">Code in `MyDangerousType`, such as property setters, may run.</span></span> <span data-ttu-id="743a8-387">Po dokončení, pokusí se deserializátor přiřadit tato instance řetězec – datový člen a selhat s výjimkou.</span><span class="sxs-lookup"><span data-stu-id="743a8-387">After this is done, the deserializer tries to assign this instance to the string data member and fail with an exception.</span></span>  
  
 <span data-ttu-id="743a8-388">Při zápisu metodu, která vrátí seznam hodnot známé typy nebo při předávání přímo na seznamu <xref:System.Runtime.Serialization.DataContractSerializer> konstruktoru, zkontrolujte, zda kód, který připraví seznamu je bezpečné a funguje pouze na důvěryhodné data.</span><span class="sxs-lookup"><span data-stu-id="743a8-388">When writing a method that returns a list of known types, or when passing a list directly to the <xref:System.Runtime.Serialization.DataContractSerializer> constructor, ensure that the code that prepares the list is secure and operates only on trusted data.</span></span>  
  
 <span data-ttu-id="743a8-389">Při zadání známé typy v konfiguraci, ujistěte se, že konfigurační soubor je bezpečné.</span><span class="sxs-lookup"><span data-stu-id="743a8-389">If specifying known types in configuration, ensure that the configuration file is secure.</span></span> <span data-ttu-id="743a8-390">Silné názvy používat v konfiguraci (zadáním veřejný klíč podepsané sestavení, které se nachází typ), ale nezadávejte žádné verzi typu, k načtení.</span><span class="sxs-lookup"><span data-stu-id="743a8-390">Always use strong names in configuration (by specifying the public key of the signed assembly where the type resides), but do not specify the version of the type to load.</span></span> <span data-ttu-id="743a8-391">Typ zavaděč automaticky vybere nejnovější verzi, pokud je to možné.</span><span class="sxs-lookup"><span data-stu-id="743a8-391">The type loader automatically picks the latest version, if possible.</span></span> <span data-ttu-id="743a8-392">Pokud zadáte v konfiguraci na konkrétní verzi, riskujete, následující: Typ může mít chyba zabezpečení, která může být stanovena v budoucí verzi, ale verze stále načte, protože je explicitně zadaná v konfiguraci.</span><span class="sxs-lookup"><span data-stu-id="743a8-392">If you specify a particular version in configuration, you run the following risk: A type may have a security vulnerability that may be fixed in a future version, but the vulnerable version still loads because it is explicitly specified in configuration.</span></span>  
  
 <span data-ttu-id="743a8-393">Příliš mnoho známé typy má jiné důsledkem: <xref:System.Runtime.Serialization.DataContractSerializer> vytvoří mezipaměť o serializaci nebo deserializaci kódu v doméně aplikace, se záznam pro každý typ musí serializaci a deserializaci.</span><span class="sxs-lookup"><span data-stu-id="743a8-393">Having too many known types has another consequence: The <xref:System.Runtime.Serialization.DataContractSerializer> creates a cache of serialization/deserialization code in the application domain, with an entry for each type it must serialize and deserialize.</span></span> <span data-ttu-id="743a8-394">Tato mezipaměť je nikdy vymazat tak dlouho, dokud doménu aplikace běží.</span><span class="sxs-lookup"><span data-stu-id="743a8-394">This cache is never cleared as long as the application domain is running.</span></span> <span data-ttu-id="743a8-395">Útočník, který si je vědoma, že aplikace používá mnoho známé typy proto může způsobit deserializace všechny tyto typy způsobuje mezipaměti využívat nepřiměřeně velké množství paměti.</span><span class="sxs-lookup"><span data-stu-id="743a8-395">Therefore, an attacker who is aware that an application uses many known types can cause the deserialization of all these types, causing the cache to consume a disproportionately large amount of memory.</span></span>  
  
### <a name="preventing-types-from-being-in-an-unintended-state"></a><span data-ttu-id="743a8-396">Typy brání používán nezamýšleným stavu</span><span class="sxs-lookup"><span data-stu-id="743a8-396">Preventing Types from Being in an Unintended State</span></span>  
 <span data-ttu-id="743a8-397">Typ může mít interní konzistence omezení, která musí být vynucená.</span><span class="sxs-lookup"><span data-stu-id="743a8-397">A type may have internal consistency constraints that must be enforced.</span></span> <span data-ttu-id="743a8-398">Musí dát pozor na-li se vyhnout narušení těchto omezení během deserializace.</span><span class="sxs-lookup"><span data-stu-id="743a8-398">Care must be taken to avoid breaking these constraints during deserialization.</span></span>  
  
 <span data-ttu-id="743a8-399">Následující příklad typu představuje stav přetlakové komory na kosmické a vynucuje omezení, že vnitřní a vnější dveře nelze otevřít ve stejnou dobu.</span><span class="sxs-lookup"><span data-stu-id="743a8-399">The following example of a type represents the state of an airlock on a spacecraft, and enforces the constraint that both the inner and the outer doors cannot be open at the same time.</span></span>  
  
 [!code-csharp[DataContractAttribute#3](../../../../samples/snippets/csharp/VS_Snippets_CFX/datacontractattribute/cs/overview.cs#3)]
 [!code-vb[DataContractAttribute#3](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/datacontractattribute/vb/overview.vb#3)]  
  
 <span data-ttu-id="743a8-400">Útočník může odeslat zprávu škodlivý tímto způsobem, získávání kolem omezení a získávání objektu v neplatném stavu, který může mít nezamýšleným a nepředvídatelným důsledky.</span><span class="sxs-lookup"><span data-stu-id="743a8-400">An attacker may send a malicious message like this, getting around the constraints and getting the object into an invalid state, which may have unintended and unpredictable consequences.</span></span>  
  
```xml  
<SpaceStationAirlock>  
    <innerDoorOpen>true</innerDoorOpen>  
    <outerDoorOpen>true</outerDoorOpen>  
</SpaceStationAirlock>  
```  
  
 <span data-ttu-id="743a8-401">Tato situace se vyhnout tím, že je vědět následující body:</span><span class="sxs-lookup"><span data-stu-id="743a8-401">This situation can be avoided by being aware of the following points:</span></span>  
  
-   <span data-ttu-id="743a8-402">Když <xref:System.Runtime.Serialization.DataContractSerializer> deserializuje Většina tříd, konstruktory se nespustí.</span><span class="sxs-lookup"><span data-stu-id="743a8-402">When the <xref:System.Runtime.Serialization.DataContractSerializer> deserializes most classes, constructors do not run.</span></span> <span data-ttu-id="743a8-403">Nespoléhejte proto na jakékoli správy stavu v konstruktoru.</span><span class="sxs-lookup"><span data-stu-id="743a8-403">Therefore, do not rely on any state management done in the constructor.</span></span>  
  
-   <span data-ttu-id="743a8-404">Zpětná volání použijte k zajištění, že objekt je v platném stavu.</span><span class="sxs-lookup"><span data-stu-id="743a8-404">Use callbacks to ensure that the object is in a valid state.</span></span> <span data-ttu-id="743a8-405">Zpětné volání označené jako <xref:System.Runtime.Serialization.OnDeserializedAttribute> atribut je obzvláště užitečná, protože se spustí po dokončení a možnost zkontrolujte a opravte celkový stav deserializace.</span><span class="sxs-lookup"><span data-stu-id="743a8-405">The callback marked with the <xref:System.Runtime.Serialization.OnDeserializedAttribute> attribute is especially useful because it runs after deserialization is complete and has a chance to examine and correct the overall state.</span></span> <span data-ttu-id="743a8-406">Další informace najdete v tématu [verze proti chybám zpětná volání serializace tolerantní](../../../../docs/framework/wcf/feature-details/version-tolerant-serialization-callbacks.md).</span><span class="sxs-lookup"><span data-stu-id="743a8-406">For more information, see [Version-Tolerant Serialization Callbacks](../../../../docs/framework/wcf/feature-details/version-tolerant-serialization-callbacks.md).</span></span>  
  
-   <span data-ttu-id="743a8-407">Design typy kontraktů dat moct spolehnout na žádné konkrétní pořadí, ve kterých se vlastnosti musí být voláno setter.</span><span class="sxs-lookup"><span data-stu-id="743a8-407">Do not design data contract types to rely on any particular order in which property setters must be called.</span></span>  
  
-   <span data-ttu-id="743a8-408">Vezměte v potaz pomocí starší verze typů, které jsou označené jako <xref:System.SerializableAttribute> atribut.</span><span class="sxs-lookup"><span data-stu-id="743a8-408">Take care using legacy types marked with the <xref:System.SerializableAttribute> attribute.</span></span> <span data-ttu-id="743a8-409">Řada z nich byly navrženy pro práci s [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] vzdálenou komunikaci pro použití s pouze důvěryhodné data.</span><span class="sxs-lookup"><span data-stu-id="743a8-409">Many of them were designed to work with [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] remoting for use with trusted data only.</span></span> <span data-ttu-id="743a8-410">Existující typy, které jsou označené jako tento atribut nemusí byly navrženy s stavu zabezpečení v paměti.</span><span class="sxs-lookup"><span data-stu-id="743a8-410">Existing types marked with this attribute may not have been designed with state safety in mind.</span></span>  
  
-   <span data-ttu-id="743a8-411">Nespoléhejte na <xref:System.Runtime.Serialization.DataMemberAttribute.IsRequired%2A> vlastnost `DataMemberAttribute` atribut zaručit přítomnosti dat jde stavu zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="743a8-411">Do not rely on the <xref:System.Runtime.Serialization.DataMemberAttribute.IsRequired%2A> property of the `DataMemberAttribute` attribute to guarantee presence of data as far as state safety is concerned.</span></span> <span data-ttu-id="743a8-412">Data mohou být vždy `null`, `zero`, nebo `invalid`.</span><span class="sxs-lookup"><span data-stu-id="743a8-412">Data could always be `null`, `zero`, or `invalid`.</span></span>  
  
-   <span data-ttu-id="743a8-413">Nikdy důvěryhodnosti grafu objektu deserializovat z nedůvěryhodného zdroje bez nejprve její ověřování.</span><span class="sxs-lookup"><span data-stu-id="743a8-413">Never trust an object graph deserialized from an untrusted data source without validating it first.</span></span> <span data-ttu-id="743a8-414">Každý jednotlivý objekt může být v konzistentním stavu, ale grafu objektů jako celek nesmí být.</span><span class="sxs-lookup"><span data-stu-id="743a8-414">Each individual object may be in a consistent state, but the object graph as a whole may not be.</span></span> <span data-ttu-id="743a8-415">Kromě toho i v případě režimu zachování grafu objektu je zakázané, deserializovat graf může mít několik odkazů na stejný objekt nebo mít cyklické odkazy.</span><span class="sxs-lookup"><span data-stu-id="743a8-415">Furthermore, even if the object graph preservation mode is disabled, the deserialized graph may have multiple references to the same object or have circular references.</span></span> <span data-ttu-id="743a8-416">Další informace najdete v tématu [serializace a deserializace](../../../../docs/framework/wcf/feature-details/serialization-and-deserialization.md).</span><span class="sxs-lookup"><span data-stu-id="743a8-416">For more information, see [Serialization and Deserialization](../../../../docs/framework/wcf/feature-details/serialization-and-deserialization.md).</span></span>  
  
### <a name="using-the-netdatacontractserializer-securely"></a><span data-ttu-id="743a8-417">Pomocí NetDataContractSerializer bezpečně</span><span class="sxs-lookup"><span data-stu-id="743a8-417">Using the NetDataContractSerializer Securely</span></span>  
 <span data-ttu-id="743a8-418"><xref:System.Runtime.Serialization.NetDataContractSerializer> Serializace modul, který používá úzkou párování na typy.</span><span class="sxs-lookup"><span data-stu-id="743a8-418">The <xref:System.Runtime.Serialization.NetDataContractSerializer> is a serialization engine that uses tight coupling to types.</span></span> <span data-ttu-id="743a8-419">Toto je podobná <xref:System.Runtime.Serialization.Formatters.Binary.BinaryFormatter> a <xref:System.Runtime.Serialization.Formatters.Soap.SoapFormatter>.</span><span class="sxs-lookup"><span data-stu-id="743a8-419">This is similar to the <xref:System.Runtime.Serialization.Formatters.Binary.BinaryFormatter> and the <xref:System.Runtime.Serialization.Formatters.Soap.SoapFormatter>.</span></span> <span data-ttu-id="743a8-420">To znamená, určí, který typ instance načtením [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] název sestavení a typu z příchozí data.</span><span class="sxs-lookup"><span data-stu-id="743a8-420">That is, it determines which type to instantiate by reading the [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] assembly and type name from the incoming data.</span></span> <span data-ttu-id="743a8-421">Přestože je součástí služby WCF, se nedá nijak zadaný vyřešily v této Serializační stroj; musí být napsané vlastní kód.</span><span class="sxs-lookup"><span data-stu-id="743a8-421">Although it is a part of WCF, there is no supplied way of plugging in this serialization engine; custom code must be written.</span></span> <span data-ttu-id="743a8-422">`NetDataContractSerializer` Zajišťuje především pro usnadnění migrace z [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] remoting do WCF.</span><span class="sxs-lookup"><span data-stu-id="743a8-422">The `NetDataContractSerializer` is provided primarily to ease migration from [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] remoting to WCF.</span></span> <span data-ttu-id="743a8-423">Další informace najdete v části relevantní v [serializace a deserializace](../../../../docs/framework/wcf/feature-details/serialization-and-deserialization.md).</span><span class="sxs-lookup"><span data-stu-id="743a8-423">For more information, see the relevant section in [Serialization and Deserialization](../../../../docs/framework/wcf/feature-details/serialization-and-deserialization.md).</span></span>  
  
 <span data-ttu-id="743a8-424">Protože samotnou zprávu může znamenat lze načíst libovolného typu, <xref:System.Runtime.Serialization.NetDataContractSerializer> mechanismus je ze své podstaty nezabezpečené a musí být použit pouze s důvěryhodné data.</span><span class="sxs-lookup"><span data-stu-id="743a8-424">Because the message itself may indicate any type can be loaded, the <xref:System.Runtime.Serialization.NetDataContractSerializer> mechanism is inherently insecure and should be used only with trusted data.</span></span> <span data-ttu-id="743a8-425">Je možné zabezpečit napsáním vazač zabezpečené, typ omezení typu, který umožňuje pouze bezpečné typy načíst (pomocí <xref:System.Runtime.Serialization.NetDataContractSerializer.Binder%2A> vlastnost).</span><span class="sxs-lookup"><span data-stu-id="743a8-425">It is possible to make it secure by writing a secure, type-limiting type binder that allows only safe types to load (using the <xref:System.Runtime.Serialization.NetDataContractSerializer.Binder%2A> property).</span></span>  
  
 <span data-ttu-id="743a8-426">I v případě, že se používá pro důvěryhodné data, příchozích dat může nedostatečně zadejte typ načíst, zvlášť pokud <xref:System.Runtime.Serialization.NetDataContractSerializer.AssemblyFormat%2A> je nastavena na <xref:System.Runtime.Serialization.Formatters.FormatterAssemblyStyle.Simple>.</span><span class="sxs-lookup"><span data-stu-id="743a8-426">Even when used with trusted data, the incoming data may insufficiently specify the type to load, especially if the <xref:System.Runtime.Serialization.NetDataContractSerializer.AssemblyFormat%2A> property is set to <xref:System.Runtime.Serialization.Formatters.FormatterAssemblyStyle.Simple>.</span></span> <span data-ttu-id="743a8-427">Každý, kdo má přístup do adresáře aplikace nebo do globální mezipaměti sestavení můžete nahradit typu škodlivý místo ten, který se má načíst.</span><span class="sxs-lookup"><span data-stu-id="743a8-427">Anyone with access to the application’s directory or to the global assembly cache can substitute a malicious type in place of the one that is supposed to load.</span></span> <span data-ttu-id="743a8-428">Vždy zajistěte zabezpečení adresáře vaší aplikace a z globální mezipaměti sestavení správně nastavení oprávnění.</span><span class="sxs-lookup"><span data-stu-id="743a8-428">Always ensure the security of your application’s directory and of the global assembly cache by correctly setting permissions.</span></span>  
  
 <span data-ttu-id="743a8-429">Obecně platí, že pokud povolíte částečně důvěryhodného kódu přístup k vaší `NetDataContractSerializer` instance nebo jinak kontrolujete selektor náhradní (<xref:System.Runtime.Serialization.ISurrogateSelector>) nebo vazače serializace (<xref:System.Runtime.Serialization.SerializationBinder>), kód může vykonávat značnou část řízení přes proces serializaci nebo deserializaci.</span><span class="sxs-lookup"><span data-stu-id="743a8-429">In general, if you allow partially trusted code access to your `NetDataContractSerializer` instance or otherwise control the surrogate selector (<xref:System.Runtime.Serialization.ISurrogateSelector>) or the serialization binder (<xref:System.Runtime.Serialization.SerializationBinder>), the code may exercise a great deal of control over the serialization/deserialization process.</span></span> <span data-ttu-id="743a8-430">Například je může vložit náhodné typy, způsobit zpřístupnění informací, manipulovat s výsledný objekt grafu nebo serializovaná data nebo přetečení výsledné serializovaném datovém proudu.</span><span class="sxs-lookup"><span data-stu-id="743a8-430">For example, it may inject arbitrary types, lead to information disclosure, tamper with the resulting object graph or serialized data, or overflow the resultant serialized stream.</span></span>  
  
 <span data-ttu-id="743a8-431">Jiné potíže se zabezpečením s `NetDataContractSerializer` je odepření služby, není hrozbu provádění škodlivý kód.</span><span class="sxs-lookup"><span data-stu-id="743a8-431">Another security concern with the `NetDataContractSerializer` is a denial of service, not a malicious code execution threat.</span></span> <span data-ttu-id="743a8-432">Při použití `NetDataContractSerializer`, vždy nastavte <xref:System.Runtime.Serialization.NetDataContractSerializer.MaxItemsInObjectGraph%2A> kvóta na hodnotu bezpečné.</span><span class="sxs-lookup"><span data-stu-id="743a8-432">When using the `NetDataContractSerializer`, always set the <xref:System.Runtime.Serialization.NetDataContractSerializer.MaxItemsInObjectGraph%2A> quota to a safe value.</span></span> <span data-ttu-id="743a8-433">Je snadné vytvořit malé škodlivý zprávu, že přiděluje pole objektů, jejíž aktuální velikost je omezena pouze tuto kvótu.</span><span class="sxs-lookup"><span data-stu-id="743a8-433">It is easy to construct a small malicious message that allocates an array of objects whose size is limited only by this quota.</span></span>  
  
### <a name="xmlserializer-specific-threats"></a><span data-ttu-id="743a8-434">XmlSerializer specifické hrozby</span><span class="sxs-lookup"><span data-stu-id="743a8-434">XmlSerializer-Specific Threats</span></span>  
 <span data-ttu-id="743a8-435"><xref:System.Xml.Serialization.XmlSerializer> Model zabezpečení je podobné jako <xref:System.Runtime.Serialization.DataContractSerializer>.</span><span class="sxs-lookup"><span data-stu-id="743a8-435">The <xref:System.Xml.Serialization.XmlSerializer> security model is similar to that of the <xref:System.Runtime.Serialization.DataContractSerializer>.</span></span> <span data-ttu-id="743a8-436">Několik hrozeb, ale jsou jedinečná pro <xref:System.Xml.Serialization.XmlSerializer>.</span><span class="sxs-lookup"><span data-stu-id="743a8-436">A few threats, however, are unique to the <xref:System.Xml.Serialization.XmlSerializer>.</span></span>  
  
 <span data-ttu-id="743a8-437"><xref:System.Xml.Serialization.XmlSerializer> Generuje *sestavení serializace* za běhu, která obsahovat kód, který je ve skutečnosti serializuje a deserializuje; jsou tyto sestavení vytvořené v adresáři dočasné soubory.</span><span class="sxs-lookup"><span data-stu-id="743a8-437">The <xref:System.Xml.Serialization.XmlSerializer> generates *serialization assemblies* at runtime that contain code that actually serializes and deserializes; these assemblies are created in a temporary files directory.</span></span> <span data-ttu-id="743a8-438">Pokud některé proces nebo uživatelský má přístupová práva k tomuto adresáři, může se kód serializaci nebo deserializaci přepsat libovolný kód.</span><span class="sxs-lookup"><span data-stu-id="743a8-438">If some other process or user has access rights to that directory, they may overwrite the serialization/deserialization code with arbitrary code.</span></span> <span data-ttu-id="743a8-439"><xref:System.Xml.Serialization.XmlSerializer> Pak spustí tento kód používá kontext zabezpečení místo kód serializaci nebo deserializaci.</span><span class="sxs-lookup"><span data-stu-id="743a8-439">The <xref:System.Xml.Serialization.XmlSerializer> then runs this code using its security context, instead of the serialization/deserialization code.</span></span> <span data-ttu-id="743a8-440">Zkontrolujte, zda že jsou oprávnění nastavena správně na adresáře s dočasnými soubory k tomu zabránili.</span><span class="sxs-lookup"><span data-stu-id="743a8-440">Make sure the permissions are set correctly on the temporary files directory to prevent this from happening.</span></span>  
  
 <span data-ttu-id="743a8-441"><xref:System.Xml.Serialization.XmlSerializer> Má také režim, ve kterém používá sestavení předem vygenerovaná serializace místo aby generovala je za běhu.</span><span class="sxs-lookup"><span data-stu-id="743a8-441">The <xref:System.Xml.Serialization.XmlSerializer> also has a mode in which it uses pre-generated serialization assemblies instead of generating them at runtime.</span></span> <span data-ttu-id="743a8-442">Tento režim se aktivuje vždy, když <xref:System.Xml.Serialization.XmlSerializer> můžete najít vhodnou serializaci sestavení.</span><span class="sxs-lookup"><span data-stu-id="743a8-442">This mode is triggered whenever the <xref:System.Xml.Serialization.XmlSerializer> can find a suitable serialization assembly.</span></span> <span data-ttu-id="743a8-443"><xref:System.Xml.Serialization.XmlSerializer> Kontroly, jestli byl sestavení serializace podepsány stejným klíčem, který se použil k podepsání sestavení, které obsahuje typy serializována.</span><span class="sxs-lookup"><span data-stu-id="743a8-443">The <xref:System.Xml.Serialization.XmlSerializer> checks whether or not the serialization assembly was signed by the same key that was used to sign the assembly that contains the types being serialized.</span></span> <span data-ttu-id="743a8-444">To nabízí ochranu z škodlivý sestavení se maskovaný jako sestavení serializace.</span><span class="sxs-lookup"><span data-stu-id="743a8-444">This offers protection from malicious assemblies being disguised as serialization assemblies.</span></span> <span data-ttu-id="743a8-445">Ale pokud je sestavení obsahující Serializovatelné typy není podepsaná, <xref:System.Xml.Serialization.XmlSerializer> nelze provést tuto kontrolu a používá žádné sestavení se správným názvem.</span><span class="sxs-lookup"><span data-stu-id="743a8-445">However, if the assembly that contains your serializable types is not signed, the <xref:System.Xml.Serialization.XmlSerializer> cannot perform this check and uses any assembly with the correct name.</span></span> <span data-ttu-id="743a8-446">To umožňuje spuštění škodlivého kódu.</span><span class="sxs-lookup"><span data-stu-id="743a8-446">This makes running malicious code possible.</span></span> <span data-ttu-id="743a8-447">Vždy podepsat sestavení, které obsahují Serializovatelné typy nebo úzce řízení přístupu k adresáři vaší aplikace a globální mezipaměti sestavení aby zavlečení škodlivého sestavení.</span><span class="sxs-lookup"><span data-stu-id="743a8-447">Always sign the assemblies that contain your serializable types, or tightly control access to your application’s directory and the global assembly cache to prevent the introduction of malicious assemblies.</span></span>  
  
 <span data-ttu-id="743a8-448"><xref:System.Xml.Serialization.XmlSerializer> Může podléhat odepření služby.</span><span class="sxs-lookup"><span data-stu-id="743a8-448">The <xref:System.Xml.Serialization.XmlSerializer> can be subject to a denial of service attack.</span></span> <span data-ttu-id="743a8-449"><xref:System.Xml.Serialization.XmlSerializer> Nemá `MaxItemsInObjectGraph` kvóty (jako je k dispozici na <xref:System.Runtime.Serialization.DataContractSerializer>).</span><span class="sxs-lookup"><span data-stu-id="743a8-449">The <xref:System.Xml.Serialization.XmlSerializer> does not have a `MaxItemsInObjectGraph` quota (as is available on the <xref:System.Runtime.Serialization.DataContractSerializer>).</span></span> <span data-ttu-id="743a8-450">Proto deserializuje libovolné množství objektů omezena pouze velikost zprávy.</span><span class="sxs-lookup"><span data-stu-id="743a8-450">Thus, it deserializes an arbitrary amount of objects, limited only by the message size.</span></span>  
  
### <a name="partial-trust-threats"></a><span data-ttu-id="743a8-451">Částečná důvěryhodnost hrozeb</span><span class="sxs-lookup"><span data-stu-id="743a8-451">Partial Trust Threats</span></span>  
 <span data-ttu-id="743a8-452">Mějte na paměti následující otázky týkající se hrozeb související s kód s částečnou důvěryhodností.</span><span class="sxs-lookup"><span data-stu-id="743a8-452">Note the following concerns regarding threats related to code running with partial trust.</span></span> <span data-ttu-id="743a8-453">Tyto hrozby zahrnují škodlivý částečně důvěryhodného kódu, a také škodlivý částečně důvěryhodného kódu v kombinaci s další scénáře útoků (například částečně důvěryhodného kódu, který vytvoří si konkrétní řetězec a pak ho deserializaci).</span><span class="sxs-lookup"><span data-stu-id="743a8-453">These threats include malicious partially-trusted code as well as malicious partially-trusted code in combination with other attack scenarios (for example, partially-trusted code that constructs a specific string and then deserializing it).</span></span>  
  
-   <span data-ttu-id="743a8-454">Při použití žádné součásti serializace, nikdy assert všechna oprávnění, než taková využití i v případě scénáře celý serializace je v rámci oboru vašeho vyhodnocení a nejsou zabývají nedůvěryhodné data ani objekty.</span><span class="sxs-lookup"><span data-stu-id="743a8-454">When using any serialization components, never assert any permissions before such usage, even if the entire serialization scenario is within the scope of your assert, and you are not dealing with any untrusted data or objects.</span></span> <span data-ttu-id="743a8-455">Takové využití může vést k ohrožení zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="743a8-455">Such usage may lead to security vulnerabilities.</span></span>  
  
-   <span data-ttu-id="743a8-456">V případech, kdy částečně důvěryhodného kódu obsahuje kontrolu nad tímto procesem serializace, buď prostřednictvím body rozšiřitelnosti (náhrady), typy serializována, nebo jinými prostředky může způsobit částečně důvěryhodného kódu serializátor výstup velké množství data do serializovaném datovém proudu, což může způsobit odepření služby (DoS) k příjemce tento datový proud.</span><span class="sxs-lookup"><span data-stu-id="743a8-456">In cases where partially-trusted code has control over the serialization process, either through extensibility points (surrogates), types being serialized, or through other means, the partially-trusted code may cause the serializer to output a large amount of data into the serialized stream, which may cause Denial of Service (DoS) to the receiver of this stream.</span></span> <span data-ttu-id="743a8-457">Pokud jsou serializaci dat určené pro cíl, který je citlivý na DoS hrozby, serializovat částečně důvěryhodné typy nebo jinak umožní částečně důvěryhodného kódu řízení serializace.</span><span class="sxs-lookup"><span data-stu-id="743a8-457">If you are serializing data intended for a target that is sensitive to DoS threats, do not serialize partially-trusted types or otherwise let partially-trusted code control serialization.</span></span>  
  
-   <span data-ttu-id="743a8-458">Pokud povolíte částečně důvěryhodného kódu, přístup k vaší <xref:System.Runtime.Serialization.DataContractSerializer> instance nebo jinak kontrolujete [náhrady kontraktů dat](../../../../docs/framework/wcf/extending/data-contract-surrogates.md), uplatnit značnou část kontrolu nad tímto procesem serializaci nebo deserializaci.</span><span class="sxs-lookup"><span data-stu-id="743a8-458">If you allow partially-trusted code access to your <xref:System.Runtime.Serialization.DataContractSerializer> instance or otherwise control the [Data Contract Surrogates](../../../../docs/framework/wcf/extending/data-contract-surrogates.md), it may exercise a great deal of control over the serialization/deserialization process.</span></span> <span data-ttu-id="743a8-459">Například je může vložit náhodné typy, způsobit zpřístupnění informací, manipulovat s výsledný objekt grafu nebo serializovaná data nebo přetečení výsledné serializovaném datovém proudu.</span><span class="sxs-lookup"><span data-stu-id="743a8-459">For example, it may inject arbitrary types, lead to information disclosure, tamper with the resulting object graph or serialized data, or overflow the resultant serialized stream.</span></span> <span data-ttu-id="743a8-460">Ekvivalentní <xref:System.Runtime.Serialization.NetDataContractSerializer> threat je popsaný v části "Použití NetDataContractSerializer bezpečně".</span><span class="sxs-lookup"><span data-stu-id="743a8-460">An equivalent <xref:System.Runtime.Serialization.NetDataContractSerializer> threat is described in the "Using the NetDataContractSerializer Securely" section.</span></span>  
  
-   <span data-ttu-id="743a8-461">Pokud <xref:System.Runtime.Serialization.DataContractAttribute> je použit atribut typu (nebo typ označen jako `[Serializable]` , ale není `ISerializable`), deserializátor můžete vytvořit instanci takového typu, i když jsou všechny konstruktory neveřejný nebo chráněný moduly požadavky.</span><span class="sxs-lookup"><span data-stu-id="743a8-461">If the <xref:System.Runtime.Serialization.DataContractAttribute> attribute is applied to a type (or the type marked as `[Serializable]` but is not `ISerializable`), the deserializer can create an instance of such a type even if all constructors are non-public or protected by demands.</span></span>  
  
-   <span data-ttu-id="743a8-462">Pokud data, která mají být deserializovat je důvěryhodný a jste si jisti, že všechny známé typy jsou typy, které důvěřujete nikdy důvěřovat výsledek deserializace.</span><span class="sxs-lookup"><span data-stu-id="743a8-462">Never trust the result of deserialization unless the data to be deserialized is trusted and you are certain that all known types are types that you trust.</span></span> <span data-ttu-id="743a8-463">Všimněte si, že známé typy nejsou načíst z konfiguračního souboru aplikace (však jsou načteny z konfiguračního souboru počítače) při spuštění v částečné důvěryhodnosti.</span><span class="sxs-lookup"><span data-stu-id="743a8-463">Note that known types are not loaded from the application configuration file, (but are loaded from the computer configuration file) when running in partial trust.</span></span>  
  
-   <span data-ttu-id="743a8-464">Pokud předáte `DataContractSerializer` přidána instance s náhradní částečně důvěryhodného kódu, kód můžete změnit nastavení změn na tomto náhradní.</span><span class="sxs-lookup"><span data-stu-id="743a8-464">If you pass a `DataContractSerializer` instance with a surrogate added to partially-trusted code, the code can change any modifiable settings on that surrogate.</span></span>  
  
-   <span data-ttu-id="743a8-465">Pro objekt deserializovaný Pokud čtecí modul XML (nebo data v nich) pochází z částečně důvěryhodného kódu považovat za výsledný objekt deserializovaný nedůvěryhodné data.</span><span class="sxs-lookup"><span data-stu-id="743a8-465">For a deserialized object, if the XML reader (or the data therein) comes from partially-trusted code, treat the resulting deserialized object as untrusted data.</span></span>  
  
-   <span data-ttu-id="743a8-466">Fakt, na který <xref:System.Runtime.Serialization.ExtensionDataObject> typ má žádné veřejné členy neznamená, že je zabezpečení dat v něm.</span><span class="sxs-lookup"><span data-stu-id="743a8-466">The fact that the <xref:System.Runtime.Serialization.ExtensionDataObject> type has no public members does not mean that data within it is secure.</span></span> <span data-ttu-id="743a8-467">Například pokud deserializovat ze zdroje dat privilegované do objektu, ve které některá data nachází, pak pracovní, tento objekt částečně důvěryhodného kódu částečně důvěryhodného kódu můžete číst data v `ExtensionDataObject` pomocí serializace objektu.</span><span class="sxs-lookup"><span data-stu-id="743a8-467">For example, if you deserialize from a privileged data source into an object in which some data resides, then hand that object to partially-trusted code, the partially-trusted code can read the data in the `ExtensionDataObject` by serializing the object.</span></span> <span data-ttu-id="743a8-468">Zvažte nastavení <xref:System.Runtime.Serialization.DataContractSerializer.IgnoreExtensionDataObject%2A> k `true` při deserializaci ze zdroje dat privilegované do objektu, který je novější předaný částečně důvěryhodného kódu.</span><span class="sxs-lookup"><span data-stu-id="743a8-468">Consider setting <xref:System.Runtime.Serialization.DataContractSerializer.IgnoreExtensionDataObject%2A> to `true` when deserializing from a privileged data source into an object that is later passed to partially-trusted code.</span></span>  
  
-   <span data-ttu-id="743a8-469"><xref:System.Runtime.Serialization.DataContractSerializer> a <xref:System.Runtime.Serialization.Json.DataContractJsonSerializer> podporu serializace privátní, chráněné, interní a veřejné členy v režimu plné důvěryhodnosti.</span><span class="sxs-lookup"><span data-stu-id="743a8-469"><xref:System.Runtime.Serialization.DataContractSerializer> and <xref:System.Runtime.Serialization.Json.DataContractJsonSerializer> support the serialization of private, protected, internal, and public members in full trust.</span></span> <span data-ttu-id="743a8-470">V částečné důvěryhodnosti, jde však serializovat pouze veřejné členy.</span><span class="sxs-lookup"><span data-stu-id="743a8-470">However, in partial trust, only public members can be serialized.</span></span> <span data-ttu-id="743a8-471">A `SecurityException` je vyvolána, pokud se aplikace pokusí serializovat neveřejný člen.</span><span class="sxs-lookup"><span data-stu-id="743a8-471">A `SecurityException` is thrown if an application attempts to serialize a non-public member.</span></span>  
  
     <span data-ttu-id="743a8-472">Umožnit vnitřního nebo chráněné vnitřní členy k serializaci v částečné důvěryhodnosti, použijte `System.Runtime.CompilerServices.InternalsVisibleTo` atributu sestavení.</span><span class="sxs-lookup"><span data-stu-id="743a8-472">To allow internal or protected internal members to be serialized in partial trust, use the `System.Runtime.CompilerServices.InternalsVisibleTo` assembly attribute.</span></span> <span data-ttu-id="743a8-473">Tento atribut umožňuje sestavení deklarovat, že její interní členové jsou viditelné pro některé jiné sestavení.</span><span class="sxs-lookup"><span data-stu-id="743a8-473">This attribute allows an assembly to declare that its internal members are visible to some other assembly.</span></span> <span data-ttu-id="743a8-474">V takovém případě sestavení, které chce mít jeho vnitřní členy serializovat prohlašuje, že jsou viditelné pro System.Runtime.Serialization.dll její vnitřní členy.</span><span class="sxs-lookup"><span data-stu-id="743a8-474">In this case, an assembly that wants to have its internal members serialized declares that its internal members are visible to System.Runtime.Serialization.dll.</span></span>  
  
     <span data-ttu-id="743a8-475">Výhodou tohoto přístupu je, nevyžaduje cestu generování kódu zvýšenými oprávněními.</span><span class="sxs-lookup"><span data-stu-id="743a8-475">The advantage of this approach is that it does not require an elevated code generation path.</span></span>  
  
     <span data-ttu-id="743a8-476">Ve stejnou dobu existují dvě hlavní nevýhody.</span><span class="sxs-lookup"><span data-stu-id="743a8-476">At the same time, there are two major disadvantages.</span></span>  
  
     <span data-ttu-id="743a8-477">První nevýhodou je, že přihlášení vlastnost `InternalsVisibleTo` atribut je celou sestavení.</span><span class="sxs-lookup"><span data-stu-id="743a8-477">The first disadvantage is that the opt-in property of the `InternalsVisibleTo` attribute is assembly-wide.</span></span> <span data-ttu-id="743a8-478">To znamená nelze zadat, pouze určité třídy můžou mít její vnitřní členy serializovat.</span><span class="sxs-lookup"><span data-stu-id="743a8-478">That is, you cannot specify that only a certain class can have its internal members serialized.</span></span> <span data-ttu-id="743a8-479">Samozřejmě, stále můžete není určená k serializaci konkrétní vnitřní člen, stačí přidat není `DataMember` atribut tohoto člena.</span><span class="sxs-lookup"><span data-stu-id="743a8-479">Of course, you can still choose not to serialize a specific internal member, by simply not adding a `DataMember` attribute to that member.</span></span> <span data-ttu-id="743a8-480">Podobně vývojář můžete také aby členem interní místo soukromé nebo chráněné s mírné viditelnost otázky.</span><span class="sxs-lookup"><span data-stu-id="743a8-480">Similarly, a developer can also choose to make a member internal rather than private or protected, with slight visibility concerns.</span></span>  
  
     <span data-ttu-id="743a8-481">Druhý nevýhodou je, že stále nejsou podporovány soukromé nebo chráněné členy.</span><span class="sxs-lookup"><span data-stu-id="743a8-481">The second disadvantage is that it still does not support private or protected members.</span></span>  
  
     <span data-ttu-id="743a8-482">Pro ilustraci použití `InternalsVisibleTo` atribut v částečné důvěryhodnosti, zvažte následující program:</span><span class="sxs-lookup"><span data-stu-id="743a8-482">To illustrate the use of the `InternalsVisibleTo` attribute in partial trust, consider the following program:</span></span>  
  
     [!code-csharp[CDF_WCF_SecurityConsiderationsForData#1](../../../../samples/snippets/csharp/VS_Snippets_CFX/cdf_wcf_securityconsiderationsfordata/cs/program.cs#1)]  
  
     <span data-ttu-id="743a8-483">V příkladu nahoře `PermissionsHelper.InternetZone` odpovídá `PermissionSet` částečného vztahu důvěryhodnosti.</span><span class="sxs-lookup"><span data-stu-id="743a8-483">In the example above, `PermissionsHelper.InternetZone` corresponds to the `PermissionSet` for partial trust.</span></span> <span data-ttu-id="743a8-484">Nyní, bez `InternalsVisibleToAttribute`, aplikace se nezdaří, vyvolání `SecurityException` indikující, že neveřejným členy nelze serializovat v částečné důvěryhodnosti.</span><span class="sxs-lookup"><span data-stu-id="743a8-484">Now, without `InternalsVisibleToAttribute`, the application will fail, throwing a `SecurityException` indicating that non-public members cannot be serialized in partial trust.</span></span>  
  
     <span data-ttu-id="743a8-485">Ale pokud jsme ke zdrojovému souboru přidejte následující řádek, program se úspěšně spustí.</span><span class="sxs-lookup"><span data-stu-id="743a8-485">However, if we add the following line to the source file, the program runs successfully.</span></span>  
  
     [!code-csharp[CDF_WCF_SecurityConsiderationsForData#2](../../../../samples/snippets/csharp/VS_Snippets_CFX/cdf_wcf_securityconsiderationsfordata/cs/program.cs#2)]  
  
## <a name="other-state-management-concerns"></a><span data-ttu-id="743a8-486">Další aspekty správy stavu</span><span class="sxs-lookup"><span data-stu-id="743a8-486">Other State Management Concerns</span></span>  
 <span data-ttu-id="743a8-487">Několik obavy týkající se správy stavu objektu jsou důležité zmínit:</span><span class="sxs-lookup"><span data-stu-id="743a8-487">A few other concerns regarding object state management are worth mentioning:</span></span>  
  
-   <span data-ttu-id="743a8-488">Pokud používáte programovací model na základě datového proudu s streamování přenosu, dojde k jako příchodu zpracování zprávy.</span><span class="sxs-lookup"><span data-stu-id="743a8-488">When using the stream-based programming model with a streaming transport, processing of the message occurs as the message arrives.</span></span> <span data-ttu-id="743a8-489">Odesílatel zprávy může přerušit operaci odeslání uprostřed datového proudu, nechat kódu v nepředvídatelném stavu, pokud byl očekávaný další obsah.</span><span class="sxs-lookup"><span data-stu-id="743a8-489">The sender of the message may abort the send operation in the middle of the stream, leaving your code in an unpredictable state if more content was expected.</span></span> <span data-ttu-id="743a8-490">Obecně platí, nespoléhejte na datový proud je úplný a neprovádět veškerou práci v operaci na základě datového proudu, která se nedá vrátit zpět v případě, že datový proud byl přerušen.</span><span class="sxs-lookup"><span data-stu-id="743a8-490">In general, do not rely on the stream being complete, and do not perform any work in a stream-based operation that cannot be rolled back in case the stream is aborted.</span></span> <span data-ttu-id="743a8-491">To platí také pro situace, kdy zprávu může být poškozený po streamování textu (například se pravděpodobně chybí koncová značka pro obálku protokolu SOAP nebo může mít druhý tělo zprávy).</span><span class="sxs-lookup"><span data-stu-id="743a8-491">This also applies to the situation where a message may be malformed after the streaming body (for example, it may be missing an end tag for the SOAP envelope or may have a second message body).</span></span>  
  
-   <span data-ttu-id="743a8-492">Pomocí `IExtensibleDataObject` funkce může způsobit, že se vygenerované citlivá data.</span><span class="sxs-lookup"><span data-stu-id="743a8-492">Using the `IExtensibleDataObject` feature may cause sensitive data to be emitted.</span></span> <span data-ttu-id="743a8-493">Pokud jsou přijetí dat z nedůvěryhodného zdroje do kontrakty dat s `IExtensibleObjectData` a později znovu ji vydat na zabezpečený kanál, kde jsou podepsané zprávy, můžete se potenciálně zaručování nic vědět o data.</span><span class="sxs-lookup"><span data-stu-id="743a8-493">If you are accepting data from an untrusted source into data contracts with `IExtensibleObjectData` and later re-emitting it on a secure channel where messages are signed, you are potentially vouching for data you know nothing about.</span></span> <span data-ttu-id="743a8-494">Kromě toho je pravděpodobně neplatný, pokud byste vzít v úvahu známé a neznámé částí dat celkového stavu, ve kterém jsou odesílání.</span><span class="sxs-lookup"><span data-stu-id="743a8-494">Moreover, the overall state you are sending may be invalid if you take both the known and unknown pieces of data into account.</span></span> <span data-ttu-id="743a8-495">Předešli této situaci nastavením buď selektivně vlastnosti rozšíření dat na `null` nebo selektivně zakázáním `IExtensibleObjectData` funkce.</span><span class="sxs-lookup"><span data-stu-id="743a8-495">Avoid this situation by either selectively setting the extension data property to `null` or by selectively disabling the `IExtensibleObjectData` feature.</span></span>  
  
## <a name="schema-import"></a><span data-ttu-id="743a8-496">Import schématu</span><span class="sxs-lookup"><span data-stu-id="743a8-496">Schema Import</span></span>  
 <span data-ttu-id="743a8-497">Za normálních okolností proces import schématu pro generování typy dochází pouze v době návrhu, například při použití [ServiceModel Metadata Utility Tool (Svcutil.exe)](../../../../docs/framework/wcf/servicemodel-metadata-utility-tool-svcutil-exe.md) na webovou službu, kterou generovat třídu klienta.</span><span class="sxs-lookup"><span data-stu-id="743a8-497">Normally, the process of importing schema to generate types happens only at design time, for example, when using the [ServiceModel Metadata Utility Tool (Svcutil.exe)](../../../../docs/framework/wcf/servicemodel-metadata-utility-tool-svcutil-exe.md) on a Web service to generate a client class.</span></span> <span data-ttu-id="743a8-498">V pokročilejších scénářích, ale může zpracovat schématu za běhu.</span><span class="sxs-lookup"><span data-stu-id="743a8-498">However, in more advanced scenarios, you may process schema at runtime.</span></span> <span data-ttu-id="743a8-499">Upozorňujeme, že díky tomu můžou zpřístupnit můžete na rizika denial of service.</span><span class="sxs-lookup"><span data-stu-id="743a8-499">Be aware that doing so can expose you to denial-of-service risks.</span></span> <span data-ttu-id="743a8-500">Některé schématu může trvat dlouhou dobu, která bude importována.</span><span class="sxs-lookup"><span data-stu-id="743a8-500">Some schema may take a long time to be imported.</span></span> <span data-ttu-id="743a8-501">Nikdy nepoužívejte <xref:System.Xml.Serialization.XmlSerializer> komponenty import schématu v takových scénářů, pokud schémata pravděpodobně pochází z nedůvěryhodného zdroje.</span><span class="sxs-lookup"><span data-stu-id="743a8-501">Never use the <xref:System.Xml.Serialization.XmlSerializer> schema import component in such scenarios if schemas are possibly coming from an untrusted source.</span></span>  
  
## <a name="threats-specific-to-aspnet-ajax-integration"></a><span data-ttu-id="743a8-502">Specifické hrozby integrace ASP.NET AJAX</span><span class="sxs-lookup"><span data-stu-id="743a8-502">Threats Specific to ASP.NET AJAX Integration</span></span>  
 <span data-ttu-id="743a8-503">Když uživatel implementuje <xref:System.ServiceModel.Description.WebScriptEnablingBehavior> nebo <xref:System.ServiceModel.Description.WebHttpBehavior>, WCF zpřístupní koncový bod, který může přijmout zprávy XML a JSON.</span><span class="sxs-lookup"><span data-stu-id="743a8-503">When the user implements <xref:System.ServiceModel.Description.WebScriptEnablingBehavior> or <xref:System.ServiceModel.Description.WebHttpBehavior>, WCF exposes an endpoint that can accept both XML and JSON messages.</span></span> <span data-ttu-id="743a8-504">Je však jen jednu sadu čtečky kvóty, používat službu čtecí modul XML a JSON čtečky.</span><span class="sxs-lookup"><span data-stu-id="743a8-504">However, there is only one set of reader quotas, used both by the XML reader and the JSON reader.</span></span> <span data-ttu-id="743a8-505">Některá nastavení kvóty může být vhodné pro jeden čtečky ale příliš velký pro druhý.</span><span class="sxs-lookup"><span data-stu-id="743a8-505">Some quota settings may be appropriate for one reader but too large for the other.</span></span>  
  
 <span data-ttu-id="743a8-506">Při implementaci `WebScriptEnablingBehavior`, uživatel má možnost vystavení proxy server JavaScript na koncový bod.</span><span class="sxs-lookup"><span data-stu-id="743a8-506">When implementing `WebScriptEnablingBehavior`, the user has the option to expose a JavaScript proxy at the endpoint.</span></span> <span data-ttu-id="743a8-507">Je třeba zvážit následující problémy se zabezpečením:</span><span class="sxs-lookup"><span data-stu-id="743a8-507">The following security issues must be considered:</span></span>  
  
-   <span data-ttu-id="743a8-508">Informace týkající se služby (názvy operace, názvy parametrů a tak dále) můžete získat tak, že prověří proxy server JavaScript.</span><span class="sxs-lookup"><span data-stu-id="743a8-508">Information about the service (operation names, parameter names, and so on) can be obtained by examining the JavaScript proxy.</span></span>  
  
-   <span data-ttu-id="743a8-509">Při použití JavaScript koncový bod, informace citlivé a privátní může uchovávají v klientovi mezipaměti webového prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="743a8-509">When using the JavaScript endpoint, sensitive and private information might be retained in the client Web browser cache.</span></span>  
  
## <a name="a-note-on-components"></a><span data-ttu-id="743a8-510">Poznámka: na komponenty</span><span class="sxs-lookup"><span data-stu-id="743a8-510">A Note on Components</span></span>  
 <span data-ttu-id="743a8-511">WCF je systém flexibilní a přizpůsobit.</span><span class="sxs-lookup"><span data-stu-id="743a8-511">WCF is a flexible and customizable system.</span></span> <span data-ttu-id="743a8-512">Většinu obsahu v tomto tématu se zaměřit na nejběžnější scénáře použití WCF.</span><span class="sxs-lookup"><span data-stu-id="743a8-512">Most of the contents of this topic focus on the most common WCF usage scenarios.</span></span> <span data-ttu-id="743a8-513">Je však možné tvoří součásti, které poskytuje WCF v mnoha různými způsoby.</span><span class="sxs-lookup"><span data-stu-id="743a8-513">However, it is possible to compose components WCF provides in many different ways.</span></span> <span data-ttu-id="743a8-514">Je důležité porozumět zabezpečení důsledky použití jednotlivých součástí.</span><span class="sxs-lookup"><span data-stu-id="743a8-514">It is important to understand the security implications of using each component.</span></span> <span data-ttu-id="743a8-515">Zejména:</span><span class="sxs-lookup"><span data-stu-id="743a8-515">In particular:</span></span>  
  
-   <span data-ttu-id="743a8-516">Pokud musíte použít čtečky XML, použijte čtečky <xref:System.Xml.XmlDictionaryReader> třída poskytuje rozdíl od jiných čtenářů.</span><span class="sxs-lookup"><span data-stu-id="743a8-516">When you must use XML readers, use the readers the <xref:System.Xml.XmlDictionaryReader> class provides as opposed to any other readers.</span></span> <span data-ttu-id="743a8-517">Bezpečné čtečky jsou vytvořené pomocí <xref:System.Xml.XmlDictionaryReader.CreateTextReader%2A>, <xref:System.Xml.XmlDictionaryReader.CreateBinaryReader%2A>, nebo <xref:System.Xml.XmlDictionaryReader.CreateMtomReader%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="743a8-517">Safe readers are created using <xref:System.Xml.XmlDictionaryReader.CreateTextReader%2A>, <xref:System.Xml.XmlDictionaryReader.CreateBinaryReader%2A>, or <xref:System.Xml.XmlDictionaryReader.CreateMtomReader%2A> methods.</span></span> <span data-ttu-id="743a8-518">Nepoužívejte <xref:System.Xml.XmlReader.Create%2A> metoda.</span><span class="sxs-lookup"><span data-stu-id="743a8-518">Do not use the <xref:System.Xml.XmlReader.Create%2A> method.</span></span> <span data-ttu-id="743a8-519">Vždy konfigurujte čtečky s bezpečné kvóty.</span><span class="sxs-lookup"><span data-stu-id="743a8-519">Always configure the readers with safe quotas.</span></span> <span data-ttu-id="743a8-520">Moduly serializace ve WCF, jsou zabezpečené jenom při použití s zabezpečené čtečky XML z WCF.</span><span class="sxs-lookup"><span data-stu-id="743a8-520">The serialization engines in WCF are secure only when used with secure XML readers from WCF.</span></span>  
  
-   <span data-ttu-id="743a8-521">Při použití <xref:System.Runtime.Serialization.DataContractSerializer> k deserializaci potenciálně nedůvěryhodných dat, vždy nastavená <xref:System.Runtime.Serialization.DataContractSerializer.MaxItemsInObjectGraph%2A> vlastnost.</span><span class="sxs-lookup"><span data-stu-id="743a8-521">When using the <xref:System.Runtime.Serialization.DataContractSerializer> to deserialize potentially untrusted data, always set the <xref:System.Runtime.Serialization.DataContractSerializer.MaxItemsInObjectGraph%2A> property.</span></span>  
  
-   <span data-ttu-id="743a8-522">Při vytváření zprávy, nastavte `maxSizeOfHeaders` parametr Pokud `MaxReceivedMessageSize` nenabízí dostatek ochrany.</span><span class="sxs-lookup"><span data-stu-id="743a8-522">When creating a message, set the `maxSizeOfHeaders` parameter if `MaxReceivedMessageSize` does not offer enough protection.</span></span>  
  
-   <span data-ttu-id="743a8-523">Při vytváření kodér, vždy nakonfigurovat relevantní kvóty, jako třeba `MaxSessionSize` a `MaxBufferSize`.</span><span class="sxs-lookup"><span data-stu-id="743a8-523">When creating an encoder, always configure the relevant quotas, such as `MaxSessionSize` and `MaxBufferSize`.</span></span>  
  
-   <span data-ttu-id="743a8-524">Pokud používáte filtr XPath zprávy, nastavte <xref:System.ServiceModel.Dispatcher.XPathMessageFilter.NodeQuota%2A> a omezit tak velikost uzlů XML navštíví filtru.</span><span class="sxs-lookup"><span data-stu-id="743a8-524">When using an XPath message filter, set the <xref:System.ServiceModel.Dispatcher.XPathMessageFilter.NodeQuota%2A> to limit the amount of XML nodes the filter visits.</span></span> <span data-ttu-id="743a8-525">Nepoužívejte XPath výrazy, které může trvat dlouhou dobu výpočetní aniž byste museli navštívit velký počet uzlů.</span><span class="sxs-lookup"><span data-stu-id="743a8-525">Do not use XPath expressions that could take a long time to compute without visiting many nodes.</span></span>  
  
-   <span data-ttu-id="743a8-526">Obecně platí při použití jakékoli součásti, která přijímá kvótu, pochopit její vliv na zabezpečení a nastavte ji na hodnotu bezpečné.</span><span class="sxs-lookup"><span data-stu-id="743a8-526">In general, when using any component that accepts a quota, understand its security implications and set it to a safe value.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="743a8-527">Viz také</span><span class="sxs-lookup"><span data-stu-id="743a8-527">See Also</span></span>  
 <xref:System.Runtime.Serialization.DataContractSerializer>  
 <xref:System.Xml.XmlDictionaryReader>  
 <xref:System.Xml.Serialization.XmlSerializer>  
 [<span data-ttu-id="743a8-528">Známé typy kontraktů dat</span><span class="sxs-lookup"><span data-stu-id="743a8-528">Data Contract Known Types</span></span>](../../../../docs/framework/wcf/feature-details/data-contract-known-types.md)
