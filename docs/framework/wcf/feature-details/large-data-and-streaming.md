---
title: Objemná data a vysílání datových proudů
ms.date: 03/30/2017
ms.assetid: ab2851f5-966b-4549-80ab-c94c5c0502d2
ms.openlocfilehash: f381df2acdb370c6e84d3a00079578f8fceb69f3
ms.sourcegitcommit: 64f4baed249341e5bf64d1385bf48e3f2e1a0211
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/07/2018
ms.locfileid: "44079757"
---
# <a name="large-data-and-streaming"></a><span data-ttu-id="6afbb-102">Objemná data a vysílání datových proudů</span><span class="sxs-lookup"><span data-stu-id="6afbb-102">Large Data and Streaming</span></span>
<span data-ttu-id="6afbb-103">Windows Communication Foundation (WCF) jsou infrastruktura komunikace založený na formátu XML.</span><span class="sxs-lookup"><span data-stu-id="6afbb-103">Windows Communication Foundation (WCF) is an XML-based communications infrastructure.</span></span> <span data-ttu-id="6afbb-104">Protože XML data je běžně zakódován do formátu standardního textu definované v [specifikace XML 1.0](https://go.microsoft.com/fwlink/?LinkId=94838), připojené systémy vývojářům a architektům jsou obvykle, které zajímá nároky na místo při přenosu (nebo velikost) zprávy odeslané přes síť a založený na textu kódování XML představuje zvláštní výzev pro efektivní přenos binární data.</span><span class="sxs-lookup"><span data-stu-id="6afbb-104">Because XML data is commonly encoded in the standard text format defined in the [XML 1.0 specification](https://go.microsoft.com/fwlink/?LinkId=94838), connected systems developers and architects are typically concerned about the wire footprint (or size) of messages sent across the network, and the text-based encoding of XML poses special challenges for the efficient transfer of binary data.</span></span>  
  
## <a name="basic-considerations"></a><span data-ttu-id="6afbb-105">Základní aspekty</span><span class="sxs-lookup"><span data-stu-id="6afbb-105">Basic Considerations</span></span>  
 <span data-ttu-id="6afbb-106">Poskytnout základní informace o následující informace pro službu WCF, tato část ukazuje některé obecné připomínky můžete vystavit a důležité informace týkající se kódování binárních dat, a streamování, které obecně platí pro připojené systémy infrastruktury.</span><span class="sxs-lookup"><span data-stu-id="6afbb-106">To provide background information about the following information for WCF, this section highlights some general concerns and considerations for encodings, binary data, and streaming that generally apply to connected systems infrastructures.</span></span>  
  
### <a name="encoding-data-text-vs-binary"></a><span data-ttu-id="6afbb-107">Kódování dat: Text vs. binární</span><span class="sxs-lookup"><span data-stu-id="6afbb-107">Encoding Data: Text vs. Binary</span></span>  
 <span data-ttu-id="6afbb-108">Obvykle vyjádřená vývojáře starostí zahrnují dojem, že XML má významné režijní náklady v porovnání s binární typy z důvodu opakující povaze počáteční značky a koncovými značkami, že kódování číselné hodnoty je považován za výrazně větší protože jsou vyjádřeny v textových hodnot a binární data nelze efektivně vyjádřit, protože musí být speciálně kódovány pro vkládání do formátu textu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-108">Commonly expressed developer concerns include the perception that XML has significant overhead when compared to binary formats due to the repetitive nature of start tags and end tags, that the encoding of numerical values is considered to be significantly larger because they are expressed in text values, and that binary data cannot be expressed efficiently because it must be specially encoded for embedding into a text format.</span></span>  
  
 <span data-ttu-id="6afbb-109">Zatímco řada z nich a podobně se týká jsou platné, skutečné rozdíl mezi text XML kódováním zpráv v prostředí XML webové služby a kódování binární zprávy ve starší verzi vzdálené volání procedur (RPC) prostředí je často mnohem méně důležité než Počáteční faktor by mohla naznačovat.</span><span class="sxs-lookup"><span data-stu-id="6afbb-109">While many of these and similar concerns are valid, the actual difference between XML-text encoded messages in an XML Web services environment and binary-encoded messages in a legacy remote procedure call (RPC) environment is often much less significant than the initial consideration might suggest.</span></span>  
  
 <span data-ttu-id="6afbb-110">Při XML text kódováním zprávy jsou transparentní a "lidsky čitelné" binární zprávy jsou často poměrně skrytého porovnání a obtížně dekódování bez nástroje.</span><span class="sxs-lookup"><span data-stu-id="6afbb-110">While XML-text encoded messages are transparent and "human readable", binary messages are often fairly obscure in comparison and difficult to decode without tools.</span></span> <span data-ttu-id="6afbb-111">Tento rozdíl v čitelnost vede z nich se má přehlédnout, že binární zprávy také často provádění vložených metadat v datové části, který přidává režie stejně jako u textových zpráv XML.</span><span class="sxs-lookup"><span data-stu-id="6afbb-111">This difference in legibility leads one to overlook that binary messages also often carry inline metadata in the payload, which adds overhead just as with XML text messages.</span></span> <span data-ttu-id="6afbb-112">To platí konkrétně pro binární formáty, které mají poskytují volné párování a dynamické vyvolání funkce.</span><span class="sxs-lookup"><span data-stu-id="6afbb-112">This is specifically true for binary formats that aim to provide loose-coupling and dynamic invocation capabilities.</span></span>  
  
 <span data-ttu-id="6afbb-113">Binární formáty však běžně nesou informaci takové popisnými metadaty v "záhlaví" které také deklaruje rozložení dat následujících datových záznamů.</span><span class="sxs-lookup"><span data-stu-id="6afbb-113">However, binary formats commonly carry such descriptive metadata information in a "header," which also declares the data layout for the following data records.</span></span> <span data-ttu-id="6afbb-114">Datová část potom následuje této společné deklarace bloku metadat s minimálními nároky na další.</span><span class="sxs-lookup"><span data-stu-id="6afbb-114">The payload then follows this common metadata block declaration with minimal further overhead.</span></span> <span data-ttu-id="6afbb-115">Naproti tomu XML obklopuje jednotlivých datových položek v elementu nebo atributu tak, aby nadřazené metadat pro každý objekt Serializovaná datová část opakovaně zahrnuty.</span><span class="sxs-lookup"><span data-stu-id="6afbb-115">In contrast, XML encloses each data item in an element or attribute so that the enclosing metadata is repetitively included for each serialized payload object.</span></span> <span data-ttu-id="6afbb-116">V důsledku toho velikost objektu jedné serializované datové části je podobný jako při porovnání text, který se binární reprezentaci jako některé popisná metadata musí být vyjádřena v obou případech ale binární formát výhody popis sdílené metadat s každou další datová část objektu, který se přenáší z důvodu nižší celkové režijní náklady.</span><span class="sxs-lookup"><span data-stu-id="6afbb-116">As a result, the size of a single serialized payload object is similar when comparing text to binary representations as some descriptive metadata must be expressed for both, but the binary format benefits from the shared metadata description with each additional payload object that is transferred due to the lower overall overhead.</span></span>  
  
 <span data-ttu-id="6afbb-117">Stále, u určitých datových typů, jako je například čísla, může být nevýhodou použití pevné velikosti, binární reprezentace číselné, jako je například typ 128bitové desetinné místo prostého textu, jako prostý text reprezentace pravděpodobně několik menších bajtů.</span><span class="sxs-lookup"><span data-stu-id="6afbb-117">Still, for certain data types, such as numbers, there might be a disadvantage to using fixed-size, binary numerical representations, such as a 128-bit decimal type instead of plain text, as the plain text representation might be several bytes smaller.</span></span> <span data-ttu-id="6afbb-118">Textová data také může mít velikost výhody obvykle flexibilnější text XML kódováním volby, zatímco může ve výchozím nastavení 16 bitů nebo dokonce 32-bit Unicode, které se nevztahují na binární formát XML .NET některé binární formáty.</span><span class="sxs-lookup"><span data-stu-id="6afbb-118">Text data also might have size benefits from the typically more flexible XML text encoding choices, while some binary formats might default to 16-bit or even 32-bit Unicode, which does not apply to the .NET Binary XML Format.</span></span>  
  
 <span data-ttu-id="6afbb-119">V důsledku toho rozhodování mezi textová nebo binární není úplně stejně jednoduše jako si za předpokladu, že binární zprávy budou vždy menší než textu XML zprávy.</span><span class="sxs-lookup"><span data-stu-id="6afbb-119">As a result, deciding between text or binary is not quite as easy as assuming that binary messages are always smaller than XML-text messages.</span></span>  
  
 <span data-ttu-id="6afbb-120">Vymazat výhod textu XML zprávy je, že jsou založené na standardech a nabízejí nejširší škálu možností možností vzájemná funkční spolupráce a platformní podpory.</span><span class="sxs-lookup"><span data-stu-id="6afbb-120">A clear advantage of XML-text messages is that they are standards-based and offer the broadest choice of interoperability options and platform support.</span></span> <span data-ttu-id="6afbb-121">Další informace najdete v části "Kódování" dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-121">For more information, see the "Encodings" section later in this topic.</span></span>  
  
### <a name="binary-content"></a><span data-ttu-id="6afbb-122">Binární obsah</span><span class="sxs-lookup"><span data-stu-id="6afbb-122">Binary Content</span></span>  
 <span data-ttu-id="6afbb-123">Jednu oblast, kde jsou lepší než založený na textu kódování z hlediska výsledná velikost zprávy binárního kódování jsou položky velkému objemu binárních dat jako jsou obrázky, videa, zvukové klipy nebo jakoukoli jinou formu neprůhledný, binární data, která musí být vyměňují mezi službami a jejich příjemci.</span><span class="sxs-lookup"><span data-stu-id="6afbb-123">One area where binary encodings are superior to text-based encodings in terms of the resulting message size are large binary data items such as pictures, videos, sound clips, or any other form of opaque, binary data that must be exchanged between services and their consumers.</span></span> <span data-ttu-id="6afbb-124">Aby tyto typy dat do textu XML běžným přístupem je zakódovat je pomocí kódování Base64.</span><span class="sxs-lookup"><span data-stu-id="6afbb-124">To fit these types of data into XML text, the common approach is to encode them using the Base64 encoding.</span></span>  
  
 <span data-ttu-id="6afbb-125">Každý znak v řetězec s kódováním Base64, představuje 6 bitů původní data 8 bitů, což vede poměr 4:3 kódování režie pro Base64, výčtu nebudou započteny velmi formátovací znaky (návrat na začátek řádku řádku), které jsou obvykle přidány na základě konvence.</span><span class="sxs-lookup"><span data-stu-id="6afbb-125">In a Base64-encoded string, each character represents 6-bits of the original 8-bit data, which results in a 4:3 encoding-overhead ratio for Base64, not counting extra formatting characters (carriage return/line feed) that are commonly added by convention.</span></span> <span data-ttu-id="6afbb-126">Přestože významné rozdíly mezi XML a binární kódování, obvykle závisí na scénáři, zvýšení velikosti více než 33 % při přenosu datovou část, která 500 MB není obvykle přijatelné.</span><span class="sxs-lookup"><span data-stu-id="6afbb-126">While the significance of the differences between XML and binary encodings typically depends on the scenario, a size gain of more than 33% when transmitting a 500-MB payload is usually not acceptable.</span></span>  
  
 <span data-ttu-id="6afbb-127">Standard, aby toto kódování režie, mechanismus pro optimalizaci přenosu zprávu (MTOM) umožňuje zajištěním externího velké datové prvky, které jsou obsaženy ve zprávě a jejich provádění se zprávou jako binární data bez jakékoli speciální kódování.</span><span class="sxs-lookup"><span data-stu-id="6afbb-127">To avoid this encoding overhead, the Message Transmission Optimization Mechanism (MTOM) standard allows for externalizing large data elements that are contained in a message and carrying them with the message as binary data without any special encoding.</span></span> <span data-ttu-id="6afbb-128">S MTOM zprávy se vyměňují podobným způsobem přenos protokolu SMTP (Simple Mail) e-mailové zprávy s přílohami nebo vložený obsah (obrázky a další obsah); Zprávy MTOM jsou dodávány jako sekvence MIME multipart/related kořenové části se skutečné zprávu protokolu SOAP.</span><span class="sxs-lookup"><span data-stu-id="6afbb-128">With MTOM, messages are exchanged in a similar fashion to Simple Mail Transfer Protocol (SMTP) email messages with attachments or embedded content (pictures and other embedded content); MTOM messages are packaged as multipart/related MIME sequences with the root part being the actual SOAP message.</span></span>  
  
 <span data-ttu-id="6afbb-129">Zpráva MTOM SOAP se liší od jeho zrušení kódovaného verzi tak, aby značky speciální elementů, které odkazují na příslušné části MIME proběhnout původní prvky ve zprávě, která obsahuje binární data.</span><span class="sxs-lookup"><span data-stu-id="6afbb-129">An MTOM SOAP message is modified from its un-encoded version so that special element tags that refer to the respective MIME parts take the place of the original elements in the message that contained binary data.</span></span> <span data-ttu-id="6afbb-130">V důsledku toho zprávu protokolu SOAP odkazuje na binární obsah tak, že označíte částí MIME odeslané s ním, ale jinak právě přenáší textová data XML.</span><span class="sxs-lookup"><span data-stu-id="6afbb-130">As a result, the SOAP message refers to binary content by pointing to the MIME parts sent with it, but otherwise just carries XML text data.</span></span> <span data-ttu-id="6afbb-131">Protože tento model je úzce v souladu s modelem zavedené SMTP, existuje široká podpora nástrojů pro kódování a dekódování zprávy MTOM na mnoha platformách, takže je mimořádně interoperabilní podle výběru.</span><span class="sxs-lookup"><span data-stu-id="6afbb-131">Because this model is closely aligned with the well-established SMTP model, there is broad tooling support to encode and decode MTOM messages on many platforms, which makes it an extremely interoperable choice.</span></span>  
  
 <span data-ttu-id="6afbb-132">Stále stejně jako Base64, MTOM také obsahuje nezbytné režijní náklady pro formát MIME tak, aby výhody používání MTOM se zobrazují pouze, pokud překročí velikost prvku binární data o velikosti 1 KB.</span><span class="sxs-lookup"><span data-stu-id="6afbb-132">Still, as with Base64, MTOM also comes with some necessary overhead for the MIME format, so that advantages of using MTOM are only seen when the size of a binary data element exceeds about 1 KB.</span></span> <span data-ttu-id="6afbb-133">Z důvodu režie může být větší než zprávy, které používají kódování Base64 pro binární data, pokud zůstane binární datové části v rámci této prahové hodnoty MTOM kódování zprávy.</span><span class="sxs-lookup"><span data-stu-id="6afbb-133">Due to the overhead, MTOM-encoded messages might be larger than messages that use Base64 encoding for binary data, if the binary payload remains under that threshold.</span></span> <span data-ttu-id="6afbb-134">Další informace najdete v části "Kódování" dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-134">For more information, see the "Encodings" section later in this topic.</span></span>  
  
### <a name="large-data-content"></a><span data-ttu-id="6afbb-135">Obsah velkých objemů dat</span><span class="sxs-lookup"><span data-stu-id="6afbb-135">Large Data Content</span></span>  
 <span data-ttu-id="6afbb-136">Při přenosu nároky jste si poznamenali, výše uvedené datové části 500 MB také představuje skvělý místní výzvu na službu a klienta.</span><span class="sxs-lookup"><span data-stu-id="6afbb-136">Wire-footprint aside, the previously mentioned 500-MB payload also poses a great local challenge at for the service and the client.</span></span> <span data-ttu-id="6afbb-137">Ve výchozím nastavení, WCF zpracovávat zprávy v *ve vyrovnávací paměti režimu*.</span><span class="sxs-lookup"><span data-stu-id="6afbb-137">By default, WCF processes messages in *buffered mode*.</span></span> <span data-ttu-id="6afbb-138">To znamená, že je k dispozici v paměti před jejím odesláním nebo po přijetí celý obsah zprávy.</span><span class="sxs-lookup"><span data-stu-id="6afbb-138">This means that the entire content of a message is present in memory before it is sent or after it is received.</span></span> <span data-ttu-id="6afbb-139">Který je dobrou strategii pro většinu scénářů a jsou nezbytná pro funkce zasílání zpráv, jako je digitální podpisy a spolehlivé doručování, velkých zpráv může vyčerpat systémové prostředky.</span><span class="sxs-lookup"><span data-stu-id="6afbb-139">While that is a good strategy for most scenarios, and necessary for messaging features such as digital signatures and reliable delivery, large messages could exhaust a system's resources.</span></span>  
  
 <span data-ttu-id="6afbb-140">Strategie se velké datové části je streamování.</span><span class="sxs-lookup"><span data-stu-id="6afbb-140">The strategy to deal with large payloads is streaming.</span></span> <span data-ttu-id="6afbb-141">Při zprávy zejména těch, které jsou vyjádřené ve formátu XML, jsou běžně považují za probíhá poměrně kompaktní data balíčky, zpráva může mít velikost několik gigabajtů a vypadat podobně jako průběžné datového proudu více než balíček dat.</span><span class="sxs-lookup"><span data-stu-id="6afbb-141">While messages, especially those expressed in XML, are commonly thought of as being relatively compact data packages, a message might be multiple gigabytes in size and resemble a continuous data stream more than a data package.</span></span> <span data-ttu-id="6afbb-142">Při přenosu dat v datových proudů režimu místo ve vyrovnávací paměti režimu odesílatel zpřístupní obsah textu zprávy příjemci ve formě datového proudu a infrastruktury zpráva průběžně předá data od odesílatele příjemce, protože je k dispozici.</span><span class="sxs-lookup"><span data-stu-id="6afbb-142">When data is transferred in streaming mode instead of buffered mode, the sender makes the contents of the message body available to the recipient in the form of a stream and the message infrastructure continuously forwards the data from sender to receiver as it becomes available.</span></span>  
  
 <span data-ttu-id="6afbb-143">Nejběžnější scénář, ve kterém takový obsah velkých objemů dat, přenosy dojít, jsou přenosy binárních dat objektů, které:</span><span class="sxs-lookup"><span data-stu-id="6afbb-143">The most common scenario in which such large data content transfers occur are transfers of binary data objects that:</span></span>  
  
-   <span data-ttu-id="6afbb-144">Nelze se rozdělit snadno do zprávy sekvence.</span><span class="sxs-lookup"><span data-stu-id="6afbb-144">Cannot be easily broken up into a message sequence.</span></span>  
  
-   <span data-ttu-id="6afbb-145">Musí doručit včas.</span><span class="sxs-lookup"><span data-stu-id="6afbb-145">Must be delivered in a timely manner.</span></span>  
  
-   <span data-ttu-id="6afbb-146">Nejsou k dispozici v plné výši při zahájení přenosu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-146">Are not available in their entirety when the transfer is initiated.</span></span>  
  
 <span data-ttu-id="6afbb-147">Pro data, která těchto omezení nemá je obvykle vhodnější odeslat pořadí zpráv v rámci oboru relace než velkých zpráv.</span><span class="sxs-lookup"><span data-stu-id="6afbb-147">For data that does not have these constraints, it is typically better to send sequences of messages within the scope of a session than one large message.</span></span> <span data-ttu-id="6afbb-148">Další informace najdete v části "Streamovaná Data" dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-148">For more information, see the "Streaming Data" section later in this topic.</span></span>  
  
 <span data-ttu-id="6afbb-149">Při odesílání velkých objemů dat je potřeba nastavit `maxAllowedContentLength` nastavení služby IIS (Další informace najdete v části [konfigurace omezení počtu požadavků služby IIS](https://go.microsoft.com/fwlink/?LinkId=253165)) a `maxReceivedMessageSize` vazby nastavení (například [ System.ServiceModel.BasicHttpBinding.MaxReceivedMessageSize](xref:System.ServiceModel.HttpBindingBase.MaxReceivedMessageSize%2A) nebo <xref:System.ServiceModel.NetTcpBinding.MaxReceivedMessageSize%2A>).</span><span class="sxs-lookup"><span data-stu-id="6afbb-149">When sending large amounts of data you will need to set the `maxAllowedContentLength` IIS setting (for more information see [Configuring IIS Request Limits](https://go.microsoft.com/fwlink/?LinkId=253165)) and the `maxReceivedMessageSize` binding setting (for example [System.ServiceModel.BasicHttpBinding.MaxReceivedMessageSize](xref:System.ServiceModel.HttpBindingBase.MaxReceivedMessageSize%2A) or <xref:System.ServiceModel.NetTcpBinding.MaxReceivedMessageSize%2A>).</span></span> <span data-ttu-id="6afbb-150">`maxAllowedContentLength` Výchozí hodnoty vlastností 28.6 m a `maxReceivedMessageSize` vlastnost Výchozí hodnota je 64 KB.</span><span class="sxs-lookup"><span data-stu-id="6afbb-150">The `maxAllowedContentLength` property defaults to 28.6 M and the `maxReceivedMessageSize` property defaults to 64KB.</span></span>  
  
## <a name="encodings"></a><span data-ttu-id="6afbb-151">Kódování</span><span class="sxs-lookup"><span data-stu-id="6afbb-151">Encodings</span></span>  
 <span data-ttu-id="6afbb-152">*Kódování* definuje sadu pravidel o tom, jak zobrazení zpráv na lince.</span><span class="sxs-lookup"><span data-stu-id="6afbb-152">An *encoding* defines a set of rules about how to present messages on the wire.</span></span> <span data-ttu-id="6afbb-153">*Kodér* implementuje tyto kódování a odpovídá na straně odesílatele pro zapnutí <xref:System.ServiceModel.Channels.Message> zprávy v paměti do datového proudu bajtů nebo bajtová vyrovnávací paměť, kterou je možné odeslat přes síť.</span><span class="sxs-lookup"><span data-stu-id="6afbb-153">An *encoder* implements such an encoding and is responsible, on the sender side, for turning a <xref:System.ServiceModel.Channels.Message> in-memory message into a byte stream or byte buffer that can be sent across the network.</span></span> <span data-ttu-id="6afbb-154">Na straně příjemce kodér změní pořadí bajtů do zprávy v paměti.</span><span class="sxs-lookup"><span data-stu-id="6afbb-154">On the receiver side, the encoder turns a sequence of bytes into an in-memory message.</span></span>  
  
 <span data-ttu-id="6afbb-155">WCF obsahuje tři kodérů a umožní vám psát a zapojte vlastní kodéry v případě potřeby.</span><span class="sxs-lookup"><span data-stu-id="6afbb-155">WCF includes three encoders and allows you to write and plug in your own encoders, if necessary.</span></span>  
  
 <span data-ttu-id="6afbb-156">Každé standardní vazeb obsahuje předem kodér, kterým vazby s předponou Net \* pomocí binárního kodéru (zahrnutím <xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement> třídy) zatímco <xref:System.ServiceModel.BasicHttpBinding> a <xref:System.ServiceModel.WSHttpBinding> třídy použít kodér textu zprávy (pomocí zprávy z <xref:System.ServiceModel.Channels.TextMessageEncodingBindingElement> třídy) ve výchozím nastavení.</span><span class="sxs-lookup"><span data-stu-id="6afbb-156">Each of the standard bindings includes a preconfigured encoder, whereby the bindings with the Net\* prefix use the binary encoder (by including the <xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement> class) while the <xref:System.ServiceModel.BasicHttpBinding> and <xref:System.ServiceModel.WSHttpBinding> classes use the text message encoder (by means of the <xref:System.ServiceModel.Channels.TextMessageEncodingBindingElement> class) by default.</span></span>  
  
|<span data-ttu-id="6afbb-157">Element vazby kodér</span><span class="sxs-lookup"><span data-stu-id="6afbb-157">Encoder binding element</span></span>|<span data-ttu-id="6afbb-158">Popis</span><span class="sxs-lookup"><span data-stu-id="6afbb-158">Description</span></span>|  
|-----------------------------|-----------------|  
|<xref:System.ServiceModel.Channels.TextMessageEncodingBindingElement>|<span data-ttu-id="6afbb-159">Kodér textu zprávy je kodéru pro všechny vazby založené na protokolu HTTP a vhodnou volbou pro všechny vlastní vazby, kde je vzájemná funkční spolupráce nejvyšší žádný problém.</span><span class="sxs-lookup"><span data-stu-id="6afbb-159">The text message encoder is the default encoder for all HTTP-based bindings and the appropriate choice for all custom bindings where interoperability is the highest concern.</span></span> <span data-ttu-id="6afbb-160">Tomto kodéru čte a zapisuje standardní protokol SOAP 1.1 a SOAP 1.2 textové zprávy s žádné speciální zpracování pro binární data.</span><span class="sxs-lookup"><span data-stu-id="6afbb-160">This encoder reads and writes standard SOAP 1.1/SOAP 1.2 text messages with no special handling for binary data.</span></span> <span data-ttu-id="6afbb-161">Pokud <xref:System.ServiceModel.Channels.MessageVersion> zprávy je nastavena na `None`obálky obálky protokolu SOAP je vynecháno z výstupu a serializuje pouze obsah textu zprávy.</span><span class="sxs-lookup"><span data-stu-id="6afbb-161">If the <xref:System.ServiceModel.Channels.MessageVersion> of a message is set to `None`, the SOAP envelope wrapper is omitted from the output and only the message body content is serialized.</span></span>|  
|<xref:System.ServiceModel.Channels.MtomMessageEncodingBindingElement>|<span data-ttu-id="6afbb-162">Kodér MTOM zpráv je kodér textu, který implementuje speciální zpracování binárních dat a není použita ve výchozím nastavení v některém z standardní vazby, protože je výhradně nástroj Optimalizace případ od případu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-162">The MTOM message encoder is a text encoder that implements special handling for binary data and is not used by default in any of the standard bindings because it is strictly a case-by-case optimization utility.</span></span> <span data-ttu-id="6afbb-163">Pokud zpráva obsahuje binární data, která překročí prahovou hodnotu, pokud kódování MTOM přínosy, se do části MIME následující obálky zprávy externalized data.</span><span class="sxs-lookup"><span data-stu-id="6afbb-163">If the message contains binary data that exceeds a threshold where MTOM encoding yields a benefit, the data is externalized into a MIME part following the message envelope.</span></span> <span data-ttu-id="6afbb-164">V tématu Povolení MTOM dál v této části.</span><span class="sxs-lookup"><span data-stu-id="6afbb-164">See Enabling MTOM later in this section.</span></span>|  
|<xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement>|<span data-ttu-id="6afbb-165">Kodér binárních zpráv je kodéru pro Net \* vazbách a vhodnou volbou pokaždé, když oba komunikujících stran jsou založené na WCF.</span><span class="sxs-lookup"><span data-stu-id="6afbb-165">The binary message encoder is the default encoder for the Net\* bindings and the appropriate choice whenever both communicating parties are based on WCF.</span></span> <span data-ttu-id="6afbb-166">Kodér binárních zpráv používá .NET binární formát XML, specifické pro společnost Microsoft binární reprezentaci XML informace sad (Infosets), které obvykle vrací menší nároky na místo než ekvivalentní reprezentaci XML 1.0 a zakóduje binární data v bajtové podobě datový proud.</span><span class="sxs-lookup"><span data-stu-id="6afbb-166">The binary message encoder uses the .NET Binary XML Format, a Microsoft-specific binary representation for XML Information Sets (Infosets) that generally yields a smaller footprint than the equivalent XML 1.0 representation and encodes binary data as a byte stream.</span></span>|  
  
 <span data-ttu-id="6afbb-167">Kódování textu zprávy je obvykle nejlepší volbou pro jakékoli komunikační trasa, která vyžaduje spolupráci při kódování binární zprávy je nejlepší volbou pro jiné cesty komunikace.</span><span class="sxs-lookup"><span data-stu-id="6afbb-167">Text message encoding is typically the best choice for any communication path that requires interoperability, while binary message encoding is the best choice for any other communication path.</span></span> <span data-ttu-id="6afbb-168">Kódování binární zprávy obvykle vrací menší zpráva, že velikosti ve srovnání s text jedné zprávy a postupně i menší velikosti za celou dobu relace komunikace.</span><span class="sxs-lookup"><span data-stu-id="6afbb-168">Binary message encoding typically yields smaller message sizes compared to text for a single message and progressively even smaller message sizes over the duration of a communication session.</span></span> <span data-ttu-id="6afbb-169">Na rozdíl od kódování textu binární kódování není nutné používat speciální zpracování pro binární data, jako je třeba použití ve formátu Base64, ale představuje bajtů na bajty.</span><span class="sxs-lookup"><span data-stu-id="6afbb-169">Unlike text encoding, binary encoding does not have to use special handling for binary data, such as using Base64, but represents bytes as bytes.</span></span>  
  
 <span data-ttu-id="6afbb-170">Pokud vaše řešení nevyžaduje, aby vzájemná funkční spolupráce, ale chcete použít přenos pomocí protokolu HTTP, můžete vytvořit <xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement> do vlastní vazby, který používá <xref:System.ServiceModel.Channels.HttpTransportBindingElement> třídy pro přenos.</span><span class="sxs-lookup"><span data-stu-id="6afbb-170">If your solution does not require interoperability, but you still want to use HTTP transport, you can compose the <xref:System.ServiceModel.Channels.BinaryMessageEncodingBindingElement> into a custom binding that uses the <xref:System.ServiceModel.Channels.HttpTransportBindingElement> class for the transport.</span></span> <span data-ttu-id="6afbb-171">Pokud počet klientů pro vaši službu, které vyžadují vzájemná funkční spolupráce, se doporučuje zpřístupníte paralelní koncových bodů, která má každá odpovídající přenos a kódování volby pro příslušné klienty povolena.</span><span class="sxs-lookup"><span data-stu-id="6afbb-171">If a number of clients on your service require interoperability, it is recommended that you expose parallel endpoints that each has the appropriate transport and encoding choices for the respective clients enabled.</span></span>  
  
### <a name="enabling-mtom"></a><span data-ttu-id="6afbb-172">Povolení funkce MTOM</span><span class="sxs-lookup"><span data-stu-id="6afbb-172">Enabling MTOM</span></span>  
 <span data-ttu-id="6afbb-173">Při zajištění lepší interoperability je povinné a velkému objemu binárních dat se musí odeslat a pak kódování zprávy MTOM je alternativou kódování strategie, kterou můžete povolit na standardu <xref:System.ServiceModel.BasicHttpBinding> nebo <xref:System.ServiceModel.WSHttpBinding> vazby nastavením funkcím `MessageEncoding` Vlastnost <xref:System.ServiceModel.WSMessageEncoding.Mtom> nebo vytváření <xref:System.ServiceModel.Channels.MtomMessageEncodingBindingElement> do <xref:System.ServiceModel.Channels.CustomBinding>.</span><span class="sxs-lookup"><span data-stu-id="6afbb-173">When interoperability is a requirement and large binary data must be sent, then MTOM message encoding is the alternative encoding strategy that you can enable on the standard <xref:System.ServiceModel.BasicHttpBinding> or <xref:System.ServiceModel.WSHttpBinding> bindings by setting the respective `MessageEncoding` property to <xref:System.ServiceModel.WSMessageEncoding.Mtom> or by composing the <xref:System.ServiceModel.Channels.MtomMessageEncodingBindingElement> into a <xref:System.ServiceModel.Channels.CustomBinding>.</span></span> <span data-ttu-id="6afbb-174">Následující příklad kódu, extrahovat z [kódování MTOM](../../../../docs/framework/wcf/samples/mtom-encoding.md) Ukázka předvádí, jak povolit MTOM v konfiguraci.</span><span class="sxs-lookup"><span data-stu-id="6afbb-174">The following example code, extracted from the [MTOM Encoding](../../../../docs/framework/wcf/samples/mtom-encoding.md) sample demonstrates how to enable MTOM in configuration.</span></span>  
  
```xml  
<system.serviceModel>  
     …  
    <bindings>  
      <wsHttpBinding>  
        <binding name="ExampleBinding" messageEncoding="Mtom"/>  
      </wsHttpBinding>  
    </bindings>  
     …  
<system.serviceModel>  
```  
  
 <span data-ttu-id="6afbb-175">Jak už bylo zmíněno dříve, rozhodnout a použít kódování MTOM závisí na objemu dat, odesílání.</span><span class="sxs-lookup"><span data-stu-id="6afbb-175">As mentioned earlier, the decision to use MTOM encoding depends on the data volume you are sending.</span></span> <span data-ttu-id="6afbb-176">Také protože MTOM je povoleno na úrovni vazby, povolení MTOM ovlivňuje všechny operace na daný koncový bod.</span><span class="sxs-lookup"><span data-stu-id="6afbb-176">Also, because MTOM is enabled at the binding level, enabling MTOM affects all operations on a given endpoint.</span></span>  
  
 <span data-ttu-id="6afbb-177">Protože kodér MTOM vydá kódování MTOM MIME/více-částečný zprávu bez ohledu na to, zda skončilo se externalized binárních dat, měli byste obecně pouze povolit MTOM pro koncové body, které výměna zpráv s využitím více než 1 KB binární data.</span><span class="sxs-lookup"><span data-stu-id="6afbb-177">Because the MTOM encoder always emits an MTOM-encoded MIME/multi-part message regardless of whether binary data ends up being externalized, you should generally only enable MTOM for endpoints that exchange messages with more than 1 KB of binary data.</span></span> <span data-ttu-id="6afbb-178">Také kontrakty služeb, které jsou navrženy pro použití s koncovými body s povoleným MTOM by měl, pokud je to možné, omezovat se zadáním těchto operace přenosu dat.</span><span class="sxs-lookup"><span data-stu-id="6afbb-178">Also, the service contracts designed for use with MTOM-enabled endpoints should, when possible, be constrained to specifying such data transfer operations.</span></span> <span data-ttu-id="6afbb-179">Funkce související ovládací prvek by měl být uložený v samostatné smlouvy.</span><span class="sxs-lookup"><span data-stu-id="6afbb-179">Related control functionality should reside on a separate contract.</span></span> <span data-ttu-id="6afbb-180">Toto pravidlo "Pouze MTOM" platí pouze pro zprávy odeslané přes koncový bod povolené MTOM; kodér MTOM může dekódovat a parsovat příchozí zprávy bez MTOM také.</span><span class="sxs-lookup"><span data-stu-id="6afbb-180">This "MTOM-only" rule applies only to messages sent through an MTOM-enabled endpoint; the MTOM-encoder can decode and parse incoming non-MTOM messages as well.</span></span>  
  
 <span data-ttu-id="6afbb-181">Pomocí kodér MTOM splňuje všechny ostatní funkce WCF.</span><span class="sxs-lookup"><span data-stu-id="6afbb-181">Using the MTOM encoder conforms with all other WCF features.</span></span> <span data-ttu-id="6afbb-182">Všimněte si, že nemusí být možné toto pravidlo ve všech případech, například když je nutné podporovat relace.</span><span class="sxs-lookup"><span data-stu-id="6afbb-182">Note that it may not be possible to observe this rule in all cases, such as when session support is required.</span></span>  
  
### <a name="programming-model"></a><span data-ttu-id="6afbb-183">Programovací model</span><span class="sxs-lookup"><span data-stu-id="6afbb-183">Programming Model</span></span>  
 <span data-ttu-id="6afbb-184">Bez ohledu na to, které tři předdefinované kodérů, které používáte ve vaší aplikaci programovací prostředí je shodné s ohledem na přenos binární data.</span><span class="sxs-lookup"><span data-stu-id="6afbb-184">Regardless of which of the three built-in encoders you use in your application, the programming experience is identical with regards to transferring binary data.</span></span> <span data-ttu-id="6afbb-185">Rozdíl je v způsob, jakým WCF zpracovává data v závislosti na jejich datové typy.</span><span class="sxs-lookup"><span data-stu-id="6afbb-185">The difference is in how WCF handles the data based on their data types.</span></span>  
  
```  
[DataContract]  
class MyData  
{  
    [DataMember]  
    byte[] binaryBuffer;  
    [DataMember]  
    string someStringData;  
}   
```  
  
 <span data-ttu-id="6afbb-186">Při použití MTOM předchozí kontraktu dat. je serializována podle následujících pravidel:</span><span class="sxs-lookup"><span data-stu-id="6afbb-186">When using MTOM, the preceding data contract is serialized according to the following rules:</span></span>  
  
-   <span data-ttu-id="6afbb-187">Pokud `binaryBuffer` není `null` a jednotlivě obsahuje dostatek dat k vysvětlení režii externalizace MTOM (hlavičky MIME a tak dále) ve srovnání s Base64 kódování, data se externalized a provádět s touto zprávou jako binární část MIME.</span><span class="sxs-lookup"><span data-stu-id="6afbb-187">If `binaryBuffer` is not `null` and individually contains enough data to justify the MTOM externalization overhead (MIME headers, and so on) when compared to Base64 encoding, the data is externalized and carried with the message as a binary MIME part.</span></span> <span data-ttu-id="6afbb-188">Pokud není překročena prahová hodnota, data kódovaná jako Base64.</span><span class="sxs-lookup"><span data-stu-id="6afbb-188">If the threshold is not exceeded, the data is encoded as Base64.</span></span>  
  
-   <span data-ttu-id="6afbb-189">Řetězec (a všechny ostatní typy, které nejsou binární) je vždy reprezentována jako řetězec v textu zprávy, bez ohledu na velikost.</span><span class="sxs-lookup"><span data-stu-id="6afbb-189">The string (and all other types that are not binary) is always represented as a string inside the message body, regardless of size.</span></span>  
  
 <span data-ttu-id="6afbb-190">Vliv na kódování MTOM je stejné, zda je používán kontrakt explicitní data, jak je znázorněno v předchozím příkladu použít seznam parametrů v operaci, mají vnořených datových kontraktů nebo převést objekt kontraktu dat v kolekci.</span><span class="sxs-lookup"><span data-stu-id="6afbb-190">The effect on the MTOM encoding is the same whether you use an explicit data contract, as shown in the preceding example, use a parameter list in an operation, have nested data contracts, or transfer a data contract object inside a collection.</span></span> <span data-ttu-id="6afbb-191">Bajtová pole jsou vždy kandidáty na optimalizaci a jsou optimalizované, pokud se dosažení prahové hodnoty optimalizace.</span><span class="sxs-lookup"><span data-stu-id="6afbb-191">Byte arrays are always candidates for optimization and are optimized if the optimization thresholds are being met.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="6afbb-192">Neměli byste používat <xref:System.IO.Stream?displayProperty=nameWithType> odvozené typy v kontraktech dat.</span><span class="sxs-lookup"><span data-stu-id="6afbb-192">You should not be using <xref:System.IO.Stream?displayProperty=nameWithType> derived types inside of data contracts.</span></span> <span data-ttu-id="6afbb-193">Stream dat by měl být oznámen pomocí streamování modelu je vysvětleno v následující části "Streamovaná Data".</span><span class="sxs-lookup"><span data-stu-id="6afbb-193">Stream data should be communicated using the streaming model, explained in the following "Streaming Data" section.</span></span>  
  
## <a name="streaming-data"></a><span data-ttu-id="6afbb-194">Streamování dat</span><span class="sxs-lookup"><span data-stu-id="6afbb-194">Streaming Data</span></span>  
 <span data-ttu-id="6afbb-195">Pokud máte velký objem přenášených dat, je režim tvorby datového proudu přenosu ve službě WCF proveditelné alternativou k výchozí chování ukládání do vyrovnávací paměti a zpracování zpráv v paměti jako celek.</span><span class="sxs-lookup"><span data-stu-id="6afbb-195">When you have a large amount of data to transfer, the streaming transfer mode in WCF is a feasible alternative to the default behavior of buffering and processing messages in memory in their entirety.</span></span>  
  
 <span data-ttu-id="6afbb-196">Jak už bylo zmíněno dříve, povolení streamování pouze u velkých zpráv (se textová nebo binární obsah), pokud data nelze segmentované, pokud zpráva musí doručit včas, nebo pokud data ještě nejsou plně k dispozici při zahájení přenosu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-196">As mentioned earlier, enable streaming only for large messages (with text or binary content) if the data cannot be segmented, if the message must be delivered in a timely fashion, or if the data is not yet fully available when the transfer is initiated.</span></span>  
  
### <a name="restrictions"></a><span data-ttu-id="6afbb-197">Omezení</span><span class="sxs-lookup"><span data-stu-id="6afbb-197">Restrictions</span></span>  
 <span data-ttu-id="6afbb-198">Velký počet funkcí WCF nelze použít, pokud je povoleno vysílání datového proudu:</span><span class="sxs-lookup"><span data-stu-id="6afbb-198">You cannot use a significant number of WCF features when streaming is enabled:</span></span>  
  
-   <span data-ttu-id="6afbb-199">Digitální podpisy pro text zprávy nejde provést, protože vyžadují výpočtu hodnoty hash přes obsah celé zprávy.</span><span class="sxs-lookup"><span data-stu-id="6afbb-199">Digital signatures for the message body cannot be performed because they require computing a hash over the entire message contents.</span></span> <span data-ttu-id="6afbb-200">Díky streamování, obsah není plně k dispozici při vytvořena a posílat záhlaví zpráv, a proto nelze vypočítat digitální podpis.</span><span class="sxs-lookup"><span data-stu-id="6afbb-200">With streaming, the content is not fully available when the message headers are constructed and sent and, therefore, a digital signature cannot be computed.</span></span>  
  
-   <span data-ttu-id="6afbb-201">Šifrování závisí na digitálních podpisů k ověření, že data byla rekonstruována správně.</span><span class="sxs-lookup"><span data-stu-id="6afbb-201">Encryption depends on digital signatures to verify that the data has been reconstructed correctly.</span></span>  
  
-   <span data-ttu-id="6afbb-202">Spolehlivé relace musí vyrovnávací paměti odeslaných zpráv na straně klienta pro opakované dodání Pokud zpráva získá ztrátě při přenosu a musí obsahovat zpráv ve službě ještě před jejich k implementaci služby pro zachování pořadí zpráv v případě, že jsou zprávy přijímány mimo pořadí.</span><span class="sxs-lookup"><span data-stu-id="6afbb-202">Reliable sessions must buffer sent messages on the client for redelivery if a message gets lost in transfer and must hold messages on the service before handing them to the service implementation to preserve message order in case messages are received out-of-sequence.</span></span>  
  
 <span data-ttu-id="6afbb-203">Kvůli těmto omezením funkční můžete použít pouze zabezpečení transportní vrstvy, které možnosti pro streamování a nelze zapnout spolehlivé relace.</span><span class="sxs-lookup"><span data-stu-id="6afbb-203">Because of these functional constraints, you can use only transport-level security options for streaming and you cannot turn on reliable sessions.</span></span> <span data-ttu-id="6afbb-204">Streamování je k dispozici pouze u následujících vazeb definovaných systémem:</span><span class="sxs-lookup"><span data-stu-id="6afbb-204">Streaming is only available with the following system-defined bindings:</span></span>  
  
-   <xref:System.ServiceModel.BasicHttpBinding>  
  
-   <xref:System.ServiceModel.NetTcpBinding>  
  
-   <xref:System.ServiceModel.NetNamedPipeBinding>  
  
-   <xref:System.ServiceModel.WebHttpBinding>  
  
 <span data-ttu-id="6afbb-205">Protože základní přenosy z <xref:System.ServiceModel.NetTcpBinding> a <xref:System.ServiceModel.NetNamedPipeBinding> přináší spolehlivé doručování a relace na základě připojení podporovat, na rozdíl od protokolu HTTP, tyto dvě vazby jsou pouze nejméně ovlivněný zahlcením těmto omezením, v praxi.</span><span class="sxs-lookup"><span data-stu-id="6afbb-205">Because the underlying transports of <xref:System.ServiceModel.NetTcpBinding> and <xref:System.ServiceModel.NetNamedPipeBinding> have inherent reliable delivery and connection-based session support, unlike HTTP, these two bindings are only minimally affected by these constraints, in practice.</span></span>  
  
 <span data-ttu-id="6afbb-206">Streamování není k dispozici přenos řízení front zpráv (MSMQ) a proto jej nelze použít s <xref:System.ServiceModel.NetMsmqBinding> nebo <xref:System.ServiceModel.MsmqIntegration.MsmqIntegrationBinding> třídy.</span><span class="sxs-lookup"><span data-stu-id="6afbb-206">Streaming is not available with the Message Queuing (MSMQ) transport and so cannot be used with the <xref:System.ServiceModel.NetMsmqBinding> or the <xref:System.ServiceModel.MsmqIntegration.MsmqIntegrationBinding> class.</span></span> <span data-ttu-id="6afbb-207">Přenos řízení front zpráv podporuje pouze uložené do vyrovnávací paměti datových přenosů s velikostí omezené zprávy, zatímco všechny ostatní přenosy nemají žádné omezení velikosti praktické zprávy pro většinu scénářů.</span><span class="sxs-lookup"><span data-stu-id="6afbb-207">The Message Queuing transport only supports buffered data transfers with a constrained message size, while all other transports do not have any practical message size limit for the vast majority of scenarios.</span></span>  
  
 <span data-ttu-id="6afbb-208">Streamování není k dispozici při pomocí přenosu protokolu Peer Channel, takže není k dispozici <xref:System.ServiceModel.NetPeerTcpBinding>.</span><span class="sxs-lookup"><span data-stu-id="6afbb-208">Streaming is also not available when using the Peer Channel transport, so is not available with the <xref:System.ServiceModel.NetPeerTcpBinding>.</span></span>  
  
#### <a name="streaming-and-sessions"></a><span data-ttu-id="6afbb-209">Streamování a relace</span><span class="sxs-lookup"><span data-stu-id="6afbb-209">Streaming and Sessions</span></span>  
 <span data-ttu-id="6afbb-210">Můžete obdržet neočekávané chování při streamování volání s vazbou na základě relace.</span><span class="sxs-lookup"><span data-stu-id="6afbb-210">You may get unexpected behavior when streaming calls with a session-based binding.</span></span> <span data-ttu-id="6afbb-211">Všechna volání streamování se provádějí přes jeden kanál (kanálu datagramu), který nepodporuje relace, i v případě, že vazba používá je nakonfigurována pro použití relací.</span><span class="sxs-lookup"><span data-stu-id="6afbb-211">All streaming calls are made through a single channel (the datagram channel) that does not support sessions even if the binding being used is configured to use sessions.</span></span> <span data-ttu-id="6afbb-212">Pokud více klientů volání datových proudů na stejný objekt služby přes vazbu na základě relace a objekt služby souběžný režim je nastavena na jednou a jeho režimu instance kontextu je nastavena na hodnotu PerSession, všechna volání musí projít kanálu datagramu a aby volání zpracovávána v čase.</span><span class="sxs-lookup"><span data-stu-id="6afbb-212">If multiple clients make streaming calls to the same service object over a session-based binding and the service object's concurrency mode is set to single and its instance context mode is set to PerSession, all calls must go through the datagram channel and so only one call is processed at a time.</span></span> <span data-ttu-id="6afbb-213">Jeden nebo více klientů může potom vypršení časového limitu. Tento problém můžete obejít tak, že buď nastavíte režim kontextu instancí objektu služby PerCall nebo souběžnosti k několika.</span><span class="sxs-lookup"><span data-stu-id="6afbb-213">One or more clients may then time out. You can work around this issue by either setting the service object's Instance Context Mode to PerCall or Concurrency to Multiple.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="6afbb-214">MaxConcurrentSessions nemá žádný vliv v tomto případě, protože není k dispozici pouze jeden "relace".</span><span class="sxs-lookup"><span data-stu-id="6afbb-214">MaxConcurrentSessions has no effect in this case because there is only one "session" available.</span></span>  
  
### <a name="enabling-streaming"></a><span data-ttu-id="6afbb-215">Povolení streamování</span><span class="sxs-lookup"><span data-stu-id="6afbb-215">Enabling Streaming</span></span>  
 <span data-ttu-id="6afbb-216">Můžete povolit datové proudy následujícími způsoby:</span><span class="sxs-lookup"><span data-stu-id="6afbb-216">You can enable streaming in the following ways:</span></span>  
  
-   <span data-ttu-id="6afbb-217">Odesílání a příjem požadavků v režim tvorby datového proudu a přijmout a vrácení odpovědi v režimu vyrovnávací paměti (<xref:System.ServiceModel.TransferMode.StreamedRequest>).</span><span class="sxs-lookup"><span data-stu-id="6afbb-217">Send and accept requests in streaming mode, and accept and return responses in buffered mode (<xref:System.ServiceModel.TransferMode.StreamedRequest>).</span></span>  
  
-   <span data-ttu-id="6afbb-218">Odesílání a příjem požadavků v režimu vyrovnávací paměti a přijímají a vrací odpovědi v režimu proudu (<xref:System.ServiceModel.TransferMode.StreamedResponse>).</span><span class="sxs-lookup"><span data-stu-id="6afbb-218">Send and accept requests in buffered mode, and accept and return responses in streamed mode (<xref:System.ServiceModel.TransferMode.StreamedResponse>).</span></span>  
  
-   <span data-ttu-id="6afbb-219">Odesílat a přijímat požadavky a odpovědi v režimu proudu v obou směrech.</span><span class="sxs-lookup"><span data-stu-id="6afbb-219">Send and receive requests and responses in streamed mode in both directions.</span></span> <span data-ttu-id="6afbb-220">(<xref:System.ServiceModel.TransferMode.Streamed>).</span><span class="sxs-lookup"><span data-stu-id="6afbb-220">(<xref:System.ServiceModel.TransferMode.Streamed>).</span></span>  
  
 <span data-ttu-id="6afbb-221">Vám může zcela vypnout streamování nastavením je režim přenosu na <xref:System.ServiceModel.TransferMode.Buffered>, což je výchozí nastavení na všechny vazby.</span><span class="sxs-lookup"><span data-stu-id="6afbb-221">You can disable streaming by setting the transfer mode to <xref:System.ServiceModel.TransferMode.Buffered>, which is the default setting on all bindings.</span></span> <span data-ttu-id="6afbb-222">Následující kód ukazuje, jak nastavit režim přenosu v konfiguraci.</span><span class="sxs-lookup"><span data-stu-id="6afbb-222">The following code shows how to set the transfer mode in configuration.</span></span>  
  
```xml  
<system.serviceModel>  
     …  
    <bindings>  
      <basicHttpBinding>  
        <binding name="ExampleBinding" transferMode="Streaming"/>  
      </basicHttpBinding>  
    </bindings>  
     …  
<system.serviceModel>  
```  
  
 <span data-ttu-id="6afbb-223">Při vytváření instance vaše vazby v kódu, musíte nastavit příslušné `TransferMode` majetku vazby (nebo element vazby přenosu, pokud vytváříte vlastní vazby) na jednu z výše uvedených hodnot.</span><span class="sxs-lookup"><span data-stu-id="6afbb-223">When you instantiate your binding in code, you must set the respective `TransferMode` property of the binding (or the transport binding element if you are composing a custom binding) to one of the previously mentioned values.</span></span>  
  
 <span data-ttu-id="6afbb-224">Můžete zapnout, aniž by to ovlivnilo funkce streamování pro zpracování požadavků a odpovědí nebo pro oba směry nezávisle na obou stranách komunikujících stran.</span><span class="sxs-lookup"><span data-stu-id="6afbb-224">You can turn on streaming for requests and replies or for both directions independently at either side of the communicating parties without affecting functionality.</span></span> <span data-ttu-id="6afbb-225">Ale byste měli vždy předpokládat, že velikost přenášených dat je důležité, že oba koncové body spoj ospravedlnit povolení streamování.</span><span class="sxs-lookup"><span data-stu-id="6afbb-225">However, you should always assume that the transferred data size is so significant that enabling streaming is justified on both endpoints of a communication link.</span></span> <span data-ttu-id="6afbb-226">Pro komunikaci napříč platformami, kde jeden z koncových bodů není implementovaná s použitím technologie WCF umožňuje používat streamování závisí na funkce streamování platformu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-226">For cross-platform communication where one of the endpoints is not implemented with WCF, the ability to use streaming depends on the platform's streaming capabilities.</span></span> <span data-ttu-id="6afbb-227">Další výjimečných výjimku, může být spotřebu paměti řízené scénáři, kde klienta nebo služby musíte minimalizovat jeho pracovní sada a můžete dovolit pouze malou vyrovnávací pamětí velikosti.</span><span class="sxs-lookup"><span data-stu-id="6afbb-227">Another rare exception might be a memory-consumption driven scenario where a client or service must minimize its working set and can only afford small buffer sizes.</span></span>  
  
### <a name="enabling-asynchronous-streaming"></a><span data-ttu-id="6afbb-228">Povolení asynchronního streamování</span><span class="sxs-lookup"><span data-stu-id="6afbb-228">Enabling Asynchronous Streaming</span></span>  
 <span data-ttu-id="6afbb-229">Pokud chcete povolit, asynchronního streamování, přidejte <xref:System.ServiceModel.Description.DispatcherSynchronizationBehavior> chování koncového bodu do hostitele služby a nastavte jeho <xref:System.ServiceModel.Description.DispatcherSynchronizationBehavior.AsynchronousSendEnabled%2A> vlastnost `true`.</span><span class="sxs-lookup"><span data-stu-id="6afbb-229">To enable asynchronous streaming, add the  <xref:System.ServiceModel.Description.DispatcherSynchronizationBehavior> endpoint behavior to the service host and set its <xref:System.ServiceModel.Description.DispatcherSynchronizationBehavior.AsynchronousSendEnabled%2A> property to `true`.</span></span> <span data-ttu-id="6afbb-230">Přidali jsme také možnost true asynchronního streamování na straně odesílání.</span><span class="sxs-lookup"><span data-stu-id="6afbb-230">We have also added the capability of true asynchronous streaming on the send side.</span></span> <span data-ttu-id="6afbb-231">Tím se zlepšuje škálovatelnost služby ve scénářích, kde je datový proud zpráv do více klientů, z nichž některé jsou pomalé v režimu čtení pravděpodobně z důvodu zahlcení sítě, nebo nejsou vůbec čtení.</span><span class="sxs-lookup"><span data-stu-id="6afbb-231">This improves scalability of the service in scenarios where it is streaming messages to multiple clients some of which are slow in reading possibly due to network congestion or are not reading at all.</span></span> <span data-ttu-id="6afbb-232">V těchto scénářích jsme teď neblokují jednotlivá vlákna ve službě za klienta.</span><span class="sxs-lookup"><span data-stu-id="6afbb-232">In these scenarios we now do not block individual threads on the service per client.</span></span> <span data-ttu-id="6afbb-233">Tím se zajistí, že služba je schopná zpracovat mnoho více klientů a zlepšuje škálovatelnost služby.</span><span class="sxs-lookup"><span data-stu-id="6afbb-233">This ensures that the service is able to process many more clients thereby improving the scalability of the service.</span></span>  
  
### <a name="programming-model-for-streamed-transfers"></a><span data-ttu-id="6afbb-234">Programovací Model pro streamovaná přenosy</span><span class="sxs-lookup"><span data-stu-id="6afbb-234">Programming Model for Streamed Transfers</span></span>  
 <span data-ttu-id="6afbb-235">Programovací model pro streamování je jednoduché.</span><span class="sxs-lookup"><span data-stu-id="6afbb-235">The programming model for streaming is straightforward.</span></span> <span data-ttu-id="6afbb-236">Pro příjem streamovaných datech, určete kontrakt operace, která má jeden <xref:System.IO.Stream> zadaný vstupní parametr.</span><span class="sxs-lookup"><span data-stu-id="6afbb-236">For receiving streamed data, specify an operation contract that has a single <xref:System.IO.Stream> typed input parameter.</span></span> <span data-ttu-id="6afbb-237">Pro vracení datového proudu, vrátí <xref:System.IO.Stream> odkaz.</span><span class="sxs-lookup"><span data-stu-id="6afbb-237">For returning streamed data, return a <xref:System.IO.Stream> reference.</span></span>  
  
```  
[ServiceContract(Namespace="http://Microsoft.ServiceModel.Samples")]  
public interface IStreamedService  
{  
    [OperationContract]  
    Stream Echo(Stream data);  
    [OperationContract]  
    Stream RequestInfo(string query);  
    [OperationContract(OneWay=true)]  
    void ProvideInfo(Stream data);  
}  
```  
  
 <span data-ttu-id="6afbb-238">Operace `Echo` v předchozím příkladu přijímá a vrací datový proud a je proto třeba použít u vazby s <xref:System.ServiceModel.TransferMode.Streamed>.</span><span class="sxs-lookup"><span data-stu-id="6afbb-238">The operation `Echo` in the preceding example receives and returns a stream and should therefore be used on a binding with <xref:System.ServiceModel.TransferMode.Streamed>.</span></span> <span data-ttu-id="6afbb-239">Pro operaci `RequestInfo`, <xref:System.ServiceModel.TransferMode.StreamedResponse> je nejvhodnější, protože se vrátí jenom <xref:System.IO.Stream>.</span><span class="sxs-lookup"><span data-stu-id="6afbb-239">For the operation `RequestInfo`, <xref:System.ServiceModel.TransferMode.StreamedResponse> is best suited, because it only returns a <xref:System.IO.Stream>.</span></span> <span data-ttu-id="6afbb-240">Jednosměrná operace je nejvhodnější pro <xref:System.ServiceModel.TransferMode.StreamedRequest>.</span><span class="sxs-lookup"><span data-stu-id="6afbb-240">The one-way operation is best suited for <xref:System.ServiceModel.TransferMode.StreamedRequest>.</span></span>  
  
 <span data-ttu-id="6afbb-241">Všimněte si, že přidání druhého parametru následující `Echo` nebo `ProvideInfo` operací způsobí, že model služby zpátky na strategii ve vyrovnávací paměti a použití za běhu serializace reprezentace datového proudu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-241">Note that adding a second parameter to the following `Echo` or `ProvideInfo` operations causes the service model to revert back to a buffered strategy and use the run-time serialization representation of the stream.</span></span> <span data-ttu-id="6afbb-242">Operace pouze s parametrem jednoho vstupního datového proudu jsou kompatibilní s začátku do konce požadavek streaming.</span><span class="sxs-lookup"><span data-stu-id="6afbb-242">Only operations with a single input stream parameter are compatible with end-to-end request streaming.</span></span>  
  
 <span data-ttu-id="6afbb-243">Toto pravidlo se vztahuje podobně kontraktů zpráv.</span><span class="sxs-lookup"><span data-stu-id="6afbb-243">This rule similarly applies to message contracts.</span></span> <span data-ttu-id="6afbb-244">Jak je znázorněno v následujícím kontraktu zprávy, může mít pouze jednoho těla členské v váš kontraktu zprávy, které je datový proud.</span><span class="sxs-lookup"><span data-stu-id="6afbb-244">As shown in the following message contract, you can have only a single body member in your message contract that is a stream.</span></span> <span data-ttu-id="6afbb-245">Pokud chcete sdělit Další informace pomocí datového proudu, musí být tyto informace provádí v záhlaví zpráv.</span><span class="sxs-lookup"><span data-stu-id="6afbb-245">If you want to communicate additional information with the stream, this information must be a carried in message headers.</span></span> <span data-ttu-id="6afbb-246">Text zprávy je rezervovaných exkluzivně pro streamování obsahu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-246">The message body is exclusively reserved for the stream content.</span></span>  
  
```  
[MessageContract]  
public class UploadStreamMessage  
{  
   [MessageHeader]  
   public string appRef;  
   [MessageBodyMember]  
   public Stream data;  
}   
```  
  
 <span data-ttu-id="6afbb-247">End streamovaná přenosů a zpráva je uzavřena dosáhne datový proud na konci souboru (EOF).</span><span class="sxs-lookup"><span data-stu-id="6afbb-247">Streamed transfers end and the message is closed when the stream reaches the end of file (EOF).</span></span> <span data-ttu-id="6afbb-248">Při odesílání zprávy (návratová hodnota nebo vyvolání operace), můžete předat <xref:System.IO.FileStream> a infrastruktura WCF následně načte všechna data z tohoto datového proudu, dokud datový proud byl zcela čtení a bylo dosaženo konce souboru.</span><span class="sxs-lookup"><span data-stu-id="6afbb-248">When sending a message (returning a value or invoking an operation), you can pass a <xref:System.IO.FileStream> and the WCF infrastructure subsequently pulls all the data from that stream until the stream has been completely read and reached EOF.</span></span> <span data-ttu-id="6afbb-249">Převést datové proudy pro zdroj, který bez těchto předdefinovaných <xref:System.IO.Stream> odvozené třídy existuje, vytvořit takové třídy, překrytí třídy přes datový proud zdroje a použít je jako argument nebo návratovou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-249">To transfer streamed data for the source that no such pre-built <xref:System.IO.Stream> derived class exists, construct such a class, overlay that class over your stream source, and use that as the argument or return value.</span></span>  
  
 <span data-ttu-id="6afbb-250">Při přijímání zprávy WCF vytvoří datový proud obsahu těla zprávy s kódováním Base64 (případně odpovídající část MIME, pokud používáte MTOM) a datový proud dosáhne EOF přečtení obsahu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-250">When receiving a message, WCF constructs a stream over the Base64-encoded message body content (or the respective MIME part if using MTOM) and the stream reaches EOF when the content has been read.</span></span>  
  
 <span data-ttu-id="6afbb-251">Streamování transportní vrstvy také funguje s jakékoli další typ kontraktu zprávy (seznamy parametrů, argumenty kontraktu dat a kontrakt explicitní zprávy), ale vzhledem k tomu, že serializace a deserializace tohoto typu zprávy vyžaduje ukládání do vyrovnávací paměti modulem pro serializaci , není vhodné používat takové varianty kontraktu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-251">Transport-level streaming also works with any other message contract type (parameter lists, data contract arguments, and explicit message contract), but because the serialization and deserialization of such typed messages requires buffering by the serializer, using such contract variants is not advisable.</span></span>  
  
### <a name="special-security-considerations-for-large-data"></a><span data-ttu-id="6afbb-252">Zvláštní bezpečnostní aspekty velkých objemů dat</span><span class="sxs-lookup"><span data-stu-id="6afbb-252">Special Security Considerations for Large Data</span></span>  
 <span data-ttu-id="6afbb-253">Všechny vazby umožňují omezit velikost příchozí zprávy, aby se zabránilo útoky s cílem odepření služeb.</span><span class="sxs-lookup"><span data-stu-id="6afbb-253">All bindings allow you to constrain the size of incoming messages to prevent denial-of-service attacks.</span></span> <span data-ttu-id="6afbb-254"><xref:System.ServiceModel.BasicHttpBinding>, Například zpřístupňuje [System.ServiceModel.BasicHttpBinding.MaxReceivedMessageSize](xref:System.ServiceModel.HttpBindingBase.MaxReceivedMessageSize%2A) vlastnost, která za rozsahem velikost příchozí zprávy a proto také za rozsahem maximální množství paměti, která se využívají Při zpracování zprávy.</span><span class="sxs-lookup"><span data-stu-id="6afbb-254">The <xref:System.ServiceModel.BasicHttpBinding>, for example, exposes a [System.ServiceModel.BasicHttpBinding.MaxReceivedMessageSize](xref:System.ServiceModel.HttpBindingBase.MaxReceivedMessageSize%2A) property that bounds the size of the incoming message, and so also bounds the maximum amount of memory that is accessed when processing the message.</span></span> <span data-ttu-id="6afbb-255">Tato jednotka je sady v bajtech s výchozí hodnotou 65 536 bajtů.</span><span class="sxs-lookup"><span data-stu-id="6afbb-255">This unit is set in bytes with a default value of 65,536 bytes.</span></span>  
  
 <span data-ttu-id="6afbb-256">Ohrožení zabezpečení, které jsou specifické pro scénář streamování velkých objemů dat vyvolá odepření služby tak, že data uložená do vyrovnávací paměti, když příjemce očekává, že odesílání.</span><span class="sxs-lookup"><span data-stu-id="6afbb-256">A security threat that is specific to the large data streaming scenario provokes a denial of service by causing data to be buffered when the receiver expects it to be streamed.</span></span> <span data-ttu-id="6afbb-257">Například WCF vždy vyrovnávacích pamětí záhlaví SOAP zprávy, a proto útočník může vytvořit velké škodlivé zprávy, který je tvořen výhradně záhlaví přinutit data, která mají být ukládány do vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="6afbb-257">For example, WCF always buffers the SOAP headers of a message, and so an attacker may construct a large malicious message that consists entirely of headers to force the data to be buffered.</span></span> <span data-ttu-id="6afbb-258">Pokud je povoleno vysílání datového proudu, `MaxReceivedMessageSize` může být nastavená na velmi velké hodnoty, protože příjemce nikdy očekává celé zprávy do vyrovnávací paměti najednou.</span><span class="sxs-lookup"><span data-stu-id="6afbb-258">When streaming is enabled, the `MaxReceivedMessageSize` may be set to an extremely large value, because the receiver never expects the entire message to be buffered in memory at once.</span></span> <span data-ttu-id="6afbb-259">Pokud je do vyrovnávací paměti zprávy WCF, dojde k přetečení paměti.</span><span class="sxs-lookup"><span data-stu-id="6afbb-259">If WCF is forced to buffer the message, a memory overflow occurs.</span></span>  
  
 <span data-ttu-id="6afbb-260">Proto omezení maximální velikost příchozí zprávy není dostatečně v tomto případě.</span><span class="sxs-lookup"><span data-stu-id="6afbb-260">Therefore, restricting the maximum incoming message size is not enough in this case.</span></span> <span data-ttu-id="6afbb-261">`MaxBufferSize` Vlastnost se vyžaduje k omezení paměti ukládaného ve vyrovnávací paměti WCF.</span><span class="sxs-lookup"><span data-stu-id="6afbb-261">The `MaxBufferSize` property is required to constrain the memory that WCF buffers.</span></span> <span data-ttu-id="6afbb-262">Je důležité si nastavíte na hodnotu bezpečné (nebo je uchovávejte na výchozí hodnotu) při streamování.</span><span class="sxs-lookup"><span data-stu-id="6afbb-262">It is important to set this to a safe value (or keep it at the default value) when streaming.</span></span> <span data-ttu-id="6afbb-263">Předpokládejme například, že vaše služba musí přijímat soubory ve velikosti až 4 GB a jejich uložení na místním disku.</span><span class="sxs-lookup"><span data-stu-id="6afbb-263">For example, suppose your service must receive files up to 4 GB in size and store them on the local disk.</span></span> <span data-ttu-id="6afbb-264">Předpokládejme také, že vaše paměti je omezená tak, že jste pouze uložit do vyrovnávací paměti 64 KB dat. současně.</span><span class="sxs-lookup"><span data-stu-id="6afbb-264">Suppose also that your memory is constrained in such a way that you can only buffer 64 KB of data at a time.</span></span> <span data-ttu-id="6afbb-265">Pak byste měli nastavit `MaxReceivedMessageSize` až 4 GB a `MaxBufferSize` 64 kB.</span><span class="sxs-lookup"><span data-stu-id="6afbb-265">Then you would set the `MaxReceivedMessageSize` to 4 GB and `MaxBufferSize` to 64 KB.</span></span> <span data-ttu-id="6afbb-266">Navíc ve vaší implementaci služby musíte zajistit, abyste si pouze z příchozího datového proudu v bloky dat 64 KB a číst další blok předtím, než se předchozí zapsané na disk a byla odstraněna z paměti.</span><span class="sxs-lookup"><span data-stu-id="6afbb-266">Also, in your service implementation, you must ensure that you read only from the incoming stream in 64-KB chunks and do not read the next chunk before the previous one has been written to disk and discarded from memory.</span></span>  
  
 <span data-ttu-id="6afbb-267">Je také důležité pochopit, že tato kvóta omezuje pouze ukládání do vyrovnávací paměti provádí WCF a nemůže chránit proti žádné ukládání do vyrovnávací paměti, který používáte ve vaší vlastní implementaci služby ani klienta.</span><span class="sxs-lookup"><span data-stu-id="6afbb-267">It is also important to understand that this quota only limits the buffering done by WCF and cannot protect you against any buffering that you do in your own service or client implementation.</span></span> <span data-ttu-id="6afbb-268">Další informace o dodatečné informace o zabezpečení najdete v tématu [aspekty zabezpečení pro Data](../../../../docs/framework/wcf/feature-details/security-considerations-for-data.md).</span><span class="sxs-lookup"><span data-stu-id="6afbb-268">For more information about additional security considerations, see [Security Considerations for Data](../../../../docs/framework/wcf/feature-details/security-considerations-for-data.md).</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="6afbb-269">Rozhodnout a použít ve vyrovnávací paměti nebo streamovaná přenosy je místní rozhodnutí koncového bodu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-269">The decision to use either buffered or streamed transfers is a local decision of the endpoint.</span></span> <span data-ttu-id="6afbb-270">Pro přenosy HTTP je režim přenosu nešíří přes připojení nebo proxy servery a jiných dodavatelů.</span><span class="sxs-lookup"><span data-stu-id="6afbb-270">For HTTP transports, the transfer mode does not propagate across a connection or to proxy servers and other intermediaries.</span></span> <span data-ttu-id="6afbb-271">Nastavení režimu převodu se neprojeví v popisu rozhraní služby.</span><span class="sxs-lookup"><span data-stu-id="6afbb-271">Setting the transfer mode is not reflected in the description of the service interface.</span></span> <span data-ttu-id="6afbb-272">Po vygenerování klienta WCF na službu, musíte upravit konfigurační soubor služby určené pro použití s datovým proudem přenosy pro nastavení režimu.</span><span class="sxs-lookup"><span data-stu-id="6afbb-272">After generating a WCF client to a service, you must edit the configuration file for services intended to be used with streamed transfers to set the mode.</span></span> <span data-ttu-id="6afbb-273">Pro protokoly TCP a přenosy pojmenovaný kanál se šíří je režim přenosu jako kontrolní výraz zásad.</span><span class="sxs-lookup"><span data-stu-id="6afbb-273">For TCP and named pipe transports, the transfer mode is propagated as a policy assertion.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="6afbb-274">Viz také</span><span class="sxs-lookup"><span data-stu-id="6afbb-274">See Also</span></span>  
 [<span data-ttu-id="6afbb-275">Postupy: Povolení streamování</span><span class="sxs-lookup"><span data-stu-id="6afbb-275">How to: Enable Streaming</span></span>](../../../../docs/framework/wcf/feature-details/how-to-enable-streaming.md)
