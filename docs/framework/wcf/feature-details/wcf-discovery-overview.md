---
title: Přehled zjišťování WCF
ms.date: 03/30/2017
ms.assetid: 84fad0e4-23b1-45b5-a2d4-c9cdf90bbb22
ms.openlocfilehash: 46092c3bce87d426f4d465367e99a9ebb6dc37fa
ms.sourcegitcommit: de17a7a0a37042f0d4406f5ae5393531caeb25ba
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 01/24/2020
ms.locfileid: "76737488"
---
# <a name="wcf-discovery-overview"></a><span data-ttu-id="ce971-102">Přehled zjišťování WCF</span><span class="sxs-lookup"><span data-stu-id="ce971-102">WCF Discovery Overview</span></span>
<span data-ttu-id="ce971-103">Rozhraní API pro zjišťování poskytují jednotný programovací model pro dynamickou publikaci a zjišťování webových služeb pomocí protokolu WS-Discovery.</span><span class="sxs-lookup"><span data-stu-id="ce971-103">The Discovery APIs provide a unified programming model for the dynamic publication and discovery of Web services using the WS-Discovery protocol.</span></span> <span data-ttu-id="ce971-104">Tato rozhraní API umožňují službám publikování a klientů, aby našli publikované služby.</span><span class="sxs-lookup"><span data-stu-id="ce971-104">These APIs allow services to publish themselves and clients to find published services.</span></span> <span data-ttu-id="ce971-105">Jakmile je služba zjistitelná, může služba posílat zprávy oznámení a také naslouchat a reagovat na požadavky na zjišťování.</span><span class="sxs-lookup"><span data-stu-id="ce971-105">Once a service is made discoverable, the service has the ability to send announcement messages as well as listen for and respond to discovery requests.</span></span> <span data-ttu-id="ce971-106">Zjistitelné služby mohou odesílat zprávy Hello, které oznamují jejich doručení v síti a bye zprávy, aby bylo možné oznámení odchodu ze sítě.</span><span class="sxs-lookup"><span data-stu-id="ce971-106">Discoverable services can send Hello messages to announce their arrival on a network and Bye messages to announce their departure from a network.</span></span> <span data-ttu-id="ce971-107">Aby klienti našli službu, odesílají `Probe` požadavek, který obsahuje konkrétní kritéria, jako je například typ kontraktu služby, klíčová slova a obor v síti.</span><span class="sxs-lookup"><span data-stu-id="ce971-107">To find a service, clients send a `Probe` request that contains specific criteria such as service contract type, keywords, and scope on the network.</span></span> <span data-ttu-id="ce971-108">Služby obdrží požadavek `Probe` a určí, zda odpovídají kritériím.</span><span class="sxs-lookup"><span data-stu-id="ce971-108">Services receive the `Probe` request and determine whether they match the criteria.</span></span> <span data-ttu-id="ce971-109">Pokud se služba shoduje, odpoví odesláním `ProbeMatch` zprávy zpátky do klienta s informacemi potřebnými ke kontaktování služby.</span><span class="sxs-lookup"><span data-stu-id="ce971-109">If a service matches, it responds by sending a `ProbeMatch` message back to the client with the information necessary to contact the service.</span></span> <span data-ttu-id="ce971-110">Klienti mohou také odesílat `Resolve` požadavky, které jim umožňují najít služby, které mohly změnit jejich adresy koncových bodů.</span><span class="sxs-lookup"><span data-stu-id="ce971-110">Clients can also send `Resolve` requests that allow them to find services that may have changed their endpoint address.</span></span> <span data-ttu-id="ce971-111">Vyhovující služby reagují na `Resolve` žádosti odesláním zprávy `ResolveMatch` zpátky klientovi.</span><span class="sxs-lookup"><span data-stu-id="ce971-111">Matching services respond to `Resolve` requests by sending a `ResolveMatch` message back to the client.</span></span>  
  
## <a name="ad-hoc-and-managed-modes"></a><span data-ttu-id="ce971-112">Ad-hoc a spravované režimy</span><span class="sxs-lookup"><span data-stu-id="ce971-112">Ad-Hoc and Managed Modes</span></span>  
 <span data-ttu-id="ce971-113">Rozhraní API pro zjišťování podporuje dva různé režimy: spravované a ad hoc.</span><span class="sxs-lookup"><span data-stu-id="ce971-113">The Discovery API supports two different modes: Managed and Ad-Hoc.</span></span> <span data-ttu-id="ce971-114">V spravovaném režimu se nachází centralizovaný Server označovaný jako proxy zjišťování, který uchovává informace o dostupných službách.</span><span class="sxs-lookup"><span data-stu-id="ce971-114">In Managed mode there is a centralized server called a discovery proxy that maintains information about available services.</span></span> <span data-ttu-id="ce971-115">Proxy zjišťování lze naplnit různými způsoby pomocí informací o službách.</span><span class="sxs-lookup"><span data-stu-id="ce971-115">The discovery proxy can be populated with information about services in a variety of ways.</span></span> <span data-ttu-id="ce971-116">Služba může například odesílat zprávy oznámení během spouštění na proxy zjišťování nebo může proxy číst data z databáze nebo konfiguračního souboru a určit, jaké služby jsou k dispozici.</span><span class="sxs-lookup"><span data-stu-id="ce971-116">For example, services can send announcement messages during start up to the discovery proxy or the proxy may read data from a database or a configuration file to determine what services are available.</span></span> <span data-ttu-id="ce971-117">Způsob, jakým je vyplněné zjišťování proxy serveru, je zcela pro vývojáře.</span><span class="sxs-lookup"><span data-stu-id="ce971-117">How the discovery proxy is populated is completely up to the developer.</span></span> <span data-ttu-id="ce971-118">Klienti používají proxy zjišťování k načítání informací o dostupných službách.</span><span class="sxs-lookup"><span data-stu-id="ce971-118">Clients use the discovery proxy to retrieve information about available services.</span></span> <span data-ttu-id="ce971-119">Když klient vyhledá službu, pošle `Probe`ovou zprávu na proxy zjišťování a proxy určí, jestli kterákoli ze služeb, které ví o shodu se službou, kterou klient hledá.</span><span class="sxs-lookup"><span data-stu-id="ce971-119">When a client searches for a service it sends a `Probe` message to the discovery proxy and the proxy determines whether any of the services it knows about match the service the client is searching for.</span></span> <span data-ttu-id="ce971-120">Pokud se shodují, proxy zjišťování pošle odpověď `ProbeMatch` zpátky klientovi.</span><span class="sxs-lookup"><span data-stu-id="ce971-120">If there are matches the discovery proxy sends a `ProbeMatch` response back to the client.</span></span> <span data-ttu-id="ce971-121">Klient pak může službu kontaktovat přímo pomocí informací o službě vrácených z proxy serveru.</span><span class="sxs-lookup"><span data-stu-id="ce971-121">The client can then contact the service directly using the service information returned from the proxy.</span></span> <span data-ttu-id="ce971-122">Klíčovým principem na spravovaném režimu je, že požadavky na zjišťování se odesílají v jednosměrovém vysílání pro jednu autoritu, proxy zjišťování.</span><span class="sxs-lookup"><span data-stu-id="ce971-122">The key principle behind Managed mode is that the discovery requests are sent in a unicast manner to one authority, the discovery proxy.</span></span> <span data-ttu-id="ce971-123">.NET Framework obsahuje klíčové komponenty, které umožňují vytvořit vlastní proxy server.</span><span class="sxs-lookup"><span data-stu-id="ce971-123">The .NET Framework contains key components that allow you to build your own proxy.</span></span> <span data-ttu-id="ce971-124">Klienti a služby můžou proxy najít několika způsoby:</span><span class="sxs-lookup"><span data-stu-id="ce971-124">Clients and services can locate the proxy by multiple methods:</span></span>  
  
- <span data-ttu-id="ce971-125">Proxy může reagovat na zprávy ad-hoc.</span><span class="sxs-lookup"><span data-stu-id="ce971-125">The proxy can respond to ad-hoc messages.</span></span>  
  
- <span data-ttu-id="ce971-126">Proxy může během spuštění odeslat zprávu s oznámením.</span><span class="sxs-lookup"><span data-stu-id="ce971-126">The proxy can send an announcement message during start up.</span></span>  
  
- <span data-ttu-id="ce971-127">Klienty a služby lze zapsat za účelem vyhledání konkrétního známého koncového bodu.</span><span class="sxs-lookup"><span data-stu-id="ce971-127">Clients and services can be written to look for a specific well-known endpoint.</span></span>  
  
 <span data-ttu-id="ce971-128">V režimu ad hoc není žádný centralizovaný Server.</span><span class="sxs-lookup"><span data-stu-id="ce971-128">In Ad-Hoc mode, there is no centralized server.</span></span> <span data-ttu-id="ce971-129">Všechny zprávy zjišťování, jako jsou například oznámení o službách a požadavky klientů, jsou odesílány vícesměrovém způsobem.</span><span class="sxs-lookup"><span data-stu-id="ce971-129">All discovery messages such as service announcements and client requests are sent in a multicast fashion.</span></span> <span data-ttu-id="ce971-130">Ve výchozím nastavení .NET Framework obsahuje podporu pro ad hoc zjišťování přes protokol UDP.</span><span class="sxs-lookup"><span data-stu-id="ce971-130">By default the .NET Framework contains support for Ad-Hoc discovery over the UDP protocol.</span></span> <span data-ttu-id="ce971-131">Pokud je třeba služba nakonfigurovaná tak, aby při spuštění odesílala oznámení Hello, pošle ji přes známou adresu vícesměrového vysílání pomocí protokolu UDP.</span><span class="sxs-lookup"><span data-stu-id="ce971-131">For example, if a service is configured to send out a Hello announcement on start up, it sends it out over a well-known, multicast address using the UDP protocol.</span></span> <span data-ttu-id="ce971-132">Klienti musí u těchto oznámení aktivně naslouchat a zpracovávat je odpovídajícím způsobem.</span><span class="sxs-lookup"><span data-stu-id="ce971-132">Clients have to actively listen for these announcements and process them accordingly.</span></span> <span data-ttu-id="ce971-133">Když klient pošle zprávu `Probe` pro službu, která se odesílá přes síť pomocí protokolu vícesměrového vysílání.</span><span class="sxs-lookup"><span data-stu-id="ce971-133">When a client sends a `Probe` message for a service it is sent over the network using a multicast protocol.</span></span> <span data-ttu-id="ce971-134">Každá služba, která požadavek obdrží, určuje, zda odpovídá kritériím ve zprávě `Probe` a reaguje přímo na klienta se zprávou `ProbeMatch`, pokud služba odpovídá kritériím uvedeným v `Probe` zprávě.</span><span class="sxs-lookup"><span data-stu-id="ce971-134">Each service that receives the request determines whether it matches the criteria in the `Probe` message and responds directly to the client with a `ProbeMatch` message if the service matches the criteria specified in the `Probe` message.</span></span>  
  
## <a name="benefits-of-using-wcf-discovery"></a><span data-ttu-id="ce971-135">Výhody použití zjišťování WCF</span><span class="sxs-lookup"><span data-stu-id="ce971-135">Benefits of Using WCF Discovery</span></span>  
 <span data-ttu-id="ce971-136">Vzhledem k tomu, že zjišťování WCF je implementováno pomocí protokolu WS-Discovery, spolupracuje s jinými klienty, službami a proxy servery, které implementují funkci WS-Discovery.</span><span class="sxs-lookup"><span data-stu-id="ce971-136">Because WCF Discovery is implemented using the WS-Discovery protocol it is interoperable with other clients, services, and proxies that implement WS-Discovery as well.</span></span> <span data-ttu-id="ce971-137">Zjišťování WCF je postavené na stávajících rozhraních API WCF, což usnadňuje přidání funkce zjišťování do stávajících služeb a klientů.</span><span class="sxs-lookup"><span data-stu-id="ce971-137">WCF Discovery is built upon the existing WCF APIs, which makes it easy to add Discovery functionality to your existing services and clients.</span></span> <span data-ttu-id="ce971-138">Zjistitelnost služby se dá snadno přidat prostřednictvím nastavení konfigurace aplikace.</span><span class="sxs-lookup"><span data-stu-id="ce971-138">Service discoverability can be easily added through the application configuration settings.</span></span> <span data-ttu-id="ce971-139">Kromě toho zjišťování WCF také podporuje použití protokolu zjišťování přes jiné přenosy, jako je například síť typu peer-to-peer, překryv názvů a HTTP.</span><span class="sxs-lookup"><span data-stu-id="ce971-139">In addition, WCF Discovery also supports using the discovery protocol over other transports such as peer net, naming overlay, and HTTP.</span></span> <span data-ttu-id="ce971-140">Zjišťování WCF poskytuje podporu pro spravovaný režim operace, kde se používá proxy zjišťování.</span><span class="sxs-lookup"><span data-stu-id="ce971-140">WCF Discovery provides support for a Managed mode of operation where a discovery proxy is used.</span></span> <span data-ttu-id="ce971-141">To může snížit zatížení sítě, protože se zprávy odesílají přímo na proxy zjišťování namísto odesílání zpráv vícesměrového vysílání do celé sítě.</span><span class="sxs-lookup"><span data-stu-id="ce971-141">This can reduce network traffic as messages are sent directly to the discovery proxy instead of sending multicast messages to the entire network.</span></span> <span data-ttu-id="ce971-142">Zjišťování WCF také umožňuje větší flexibilitu při práci s webovými službami.</span><span class="sxs-lookup"><span data-stu-id="ce971-142">WCF Discovery also allows for more flexibility when working with Web services.</span></span> <span data-ttu-id="ce971-143">Můžete například změnit adresu služby, aniž byste museli překonfigurovat klienta nebo službu.</span><span class="sxs-lookup"><span data-stu-id="ce971-143">For example, you can change the address of a service without having to reconfigure the client or the service.</span></span> <span data-ttu-id="ce971-144">Když má klient přístup ke službě, může vystavit `Probe` zprávu prostřednictvím žádosti `Find` a očekávat, že služba bude odpovídat její aktuální adrese.</span><span class="sxs-lookup"><span data-stu-id="ce971-144">When a client must access the service it can issue a `Probe` message through a `Find` request and expect the service to respond with its current address.</span></span> <span data-ttu-id="ce971-145">Zjišťování WCF umožňuje klientovi vyhledat službu na základě různých kritérií, včetně typů kontraktů, vazeb prvků, oboru názvů, oboru a klíčových slov nebo čísel verzí.</span><span class="sxs-lookup"><span data-stu-id="ce971-145">WCF Discovery allows a client to search for a service based on different criteria including contract types, binding elements, namespace, scope, and keywords or version numbers.</span></span> <span data-ttu-id="ce971-146">Zjišťování WCF umožňuje zjišťování doby běhu a návrhu.</span><span class="sxs-lookup"><span data-stu-id="ce971-146">WCF Discovery enables runtime and design time discovery.</span></span> <span data-ttu-id="ce971-147">Přidání zjišťování do aplikace lze použít k povolení jiných scénářů, jako je odolnost proti chybám a Automatická konfigurace.</span><span class="sxs-lookup"><span data-stu-id="ce971-147">Adding discovery to your application can be used to enable other scenarios such as fault tolerance and auto configuration.</span></span>  
  
## <a name="service-publication"></a><span data-ttu-id="ce971-148">Publikace služby</span><span class="sxs-lookup"><span data-stu-id="ce971-148">Service Publication</span></span>  
 <span data-ttu-id="ce971-149">Aby bylo možné službu zjistit, musí být do hostitele služby přidána <xref:System.ServiceModel.Discovery.ServiceDiscoveryBehavior> a musí být přidán koncový bod zjišťování, aby bylo možné určit, kam se mají zprávy zjišťování naslouchat.</span><span class="sxs-lookup"><span data-stu-id="ce971-149">To make a service discoverable, a <xref:System.ServiceModel.Discovery.ServiceDiscoveryBehavior> must be added to the service host and a discovery endpoint must be added to specify where to listen for discovery messages.</span></span> <span data-ttu-id="ce971-150">Následující příklad kódu ukazuje, jak lze upravit službu v místním prostředí, aby ji bylo možné zjistit.</span><span class="sxs-lookup"><span data-stu-id="ce971-150">The following code example shows how a self-hosted service can be modified to make it discoverable.</span></span>  
  
```csharp  
Uri baseAddress = new Uri($"http://{System.Net.Dns.GetHostName()}:8000/discovery/scenarios/calculatorservice/{Guid.NewGuid().ToString()}/");

// Create a ServiceHost for the CalculatorService type.
using (ServiceHost serviceHost = new ServiceHost(typeof(CalculatorService), baseAddress))
{
    // Add calculator endpoint
    serviceHost.AddServiceEndpoint(typeof(ICalculator), new WSHttpBinding(), string.Empty);

    // ** DISCOVERY ** //
    // Make the service discoverable by adding the discovery behavior
    serviceHost.Description.Behaviors.Add(new ServiceDiscoveryBehavior());

    // ** DISCOVERY ** //
    // Add the discovery endpoint that specifies where to publish the services
    serviceHost.AddServiceEndpoint(new UdpDiscoveryEndpoint());

    // Open the ServiceHost to create listeners and start listening for messages.
    serviceHost.Open();

    // The service can now be accessed.
    Console.WriteLine("Press <ENTER> to terminate service.");
    Console.ReadLine();
}
```  
  
 <span data-ttu-id="ce971-151">Aby bylo možné službu zjistit, musí být do popisu služby přidána instance <xref:System.ServiceModel.Discovery.ServiceDiscoveryBehavior>.</span><span class="sxs-lookup"><span data-stu-id="ce971-151">A <xref:System.ServiceModel.Discovery.ServiceDiscoveryBehavior> instance must be added to a service description for the service to be discoverable.</span></span> <span data-ttu-id="ce971-152">Instanci <xref:System.ServiceModel.Discovery.DiscoveryEndpoint> je třeba přidat do hostitele služby, aby mohla služba sdělit, kde má naslouchat požadavky na zjišťování.</span><span class="sxs-lookup"><span data-stu-id="ce971-152">A <xref:System.ServiceModel.Discovery.DiscoveryEndpoint> instance must be added to the service host to tell the service where to listen for discovery requests.</span></span> <span data-ttu-id="ce971-153">V tomto příkladu je přidána <xref:System.ServiceModel.Discovery.UdpDiscoveryEndpoint> (která je odvozena z <xref:System.ServiceModel.Discovery.DiscoveryEndpoint>) k určení toho, že služba by měla naslouchat požadavkům na zjišťování přes přenos vícesměrového vysílání UDP.</span><span class="sxs-lookup"><span data-stu-id="ce971-153">In this example, a <xref:System.ServiceModel.Discovery.UdpDiscoveryEndpoint> (which is derived from <xref:System.ServiceModel.Discovery.DiscoveryEndpoint>) is added to specify that the service should listen for discovery requests over the UDP multicast transport.</span></span> <span data-ttu-id="ce971-154"><xref:System.ServiceModel.Discovery.UdpDiscoveryEndpoint> se používá pro zjišťování ad-hoc, protože všechny zprávy jsou odesílány vícesměrového vysílání.</span><span class="sxs-lookup"><span data-stu-id="ce971-154">The <xref:System.ServiceModel.Discovery.UdpDiscoveryEndpoint> is used for Ad-Hoc discovery because all messages are sent in a multicast fashion.</span></span>  
  
## <a name="announcement"></a><span data-ttu-id="ce971-155">Hlášení</span><span class="sxs-lookup"><span data-stu-id="ce971-155">Announcement</span></span>  
 <span data-ttu-id="ce971-156">Ve výchozím nastavení publikace služby neodesílá zprávy s oznámením.</span><span class="sxs-lookup"><span data-stu-id="ce971-156">By default, service publication does not send out announcement messages.</span></span> <span data-ttu-id="ce971-157">Služba musí být nakonfigurovaná tak, aby odesílala zprávy s oznámením.</span><span class="sxs-lookup"><span data-stu-id="ce971-157">The service must be configured to send out announcement messages.</span></span> <span data-ttu-id="ce971-158">To poskytuje větší flexibilitu zapisovače služeb, protože může informovat službu nezávisle na naslouchání zpráv zjišťování.</span><span class="sxs-lookup"><span data-stu-id="ce971-158">This provides additional flexibility for service writers because they can announce the service separately from listening for discovery messages.</span></span> <span data-ttu-id="ce971-159">Oznámení služby je také možné použít jako mechanismus pro registraci služeb s proxy zjišťování nebo jinými registry služby.</span><span class="sxs-lookup"><span data-stu-id="ce971-159">Service announcement can also be used as a mechanism for registering services with a discovery proxy or other service registries.</span></span> <span data-ttu-id="ce971-160">Následující kód ukazuje, jak nakonfigurovat službu pro posílání zpráv oznámení přes vazbu UDP.</span><span class="sxs-lookup"><span data-stu-id="ce971-160">The following code shows how to configure a service to send announcement messages over a UDP binding.</span></span>  
  
```csharp  
Uri baseAddress = new Uri($"http://{System.Net.Dns.GetHostName()}:8000/discovery/scenarios/calculatorservice/{Guid.NewGuid().ToString()}/");

// Create a ServiceHost for the CalculatorService type.
using (ServiceHost serviceHost = new ServiceHost(typeof(CalculatorService), baseAddress))
{
    // Add calculator endpoint
    serviceHost.AddServiceEndpoint(typeof(ICalculator), new WSHttpBinding(), string.Empty);

    // ** DISCOVERY ** //
    // Make the service discoverable by adding the discovery behavior
    ServiceDiscoveryBehavior discoveryBehavior = new ServiceDiscoveryBehavior();
    serviceHost.Description.Behaviors.Add(discoveryBehavior);

    // Send announcements on UDP multicast transport
    discoveryBehavior.AnnouncementEndpoints.Add(
      new UdpAnnouncementEndpoint());

    // ** DISCOVERY ** //
    // Add the discovery endpoint that specifies where to publish the services
    serviceHost.Description.Endpoints.Add(new UdpDiscoveryEndpoint());

    // Open the ServiceHost to create listeners and start listening for messages.
    serviceHost.Open();

    // The service can now be accessed.
    Console.WriteLine("Press <ENTER> to terminate service.");
    Console.ReadLine();
}
```  
  
## <a name="service-discovery"></a><span data-ttu-id="ce971-161">Zjišťování služby</span><span class="sxs-lookup"><span data-stu-id="ce971-161">Service Discovery</span></span>  
 <span data-ttu-id="ce971-162">Klientská aplikace může použít třídu <xref:System.ServiceModel.Discovery.DiscoveryClient> k vyhledání služeb.</span><span class="sxs-lookup"><span data-stu-id="ce971-162">A client application can use the <xref:System.ServiceModel.Discovery.DiscoveryClient> class to find services.</span></span> <span data-ttu-id="ce971-163">Vývojář vytvoří instanci třídy <xref:System.ServiceModel.Discovery.DiscoveryClient>, která projde koncovým bodem zjišťování, který určuje, kam se mají posílat `Probe` nebo `Resolve` zprávy.</span><span class="sxs-lookup"><span data-stu-id="ce971-163">The developer creates an instance of the <xref:System.ServiceModel.Discovery.DiscoveryClient> class that passes in a discovery endpoint that specifies where to send `Probe` or `Resolve` messages.</span></span> <span data-ttu-id="ce971-164">Klient pak zavolá <xref:System.ServiceModel.Discovery.DiscoveryClient.Find%2A>, která určuje kritéria vyhledávání v rámci instance <xref:System.ServiceModel.Discovery.FindCriteria>.</span><span class="sxs-lookup"><span data-stu-id="ce971-164">The client then calls <xref:System.ServiceModel.Discovery.DiscoveryClient.Find%2A> that specifies search criteria within a <xref:System.ServiceModel.Discovery.FindCriteria> instance.</span></span> <span data-ttu-id="ce971-165">Pokud se najde vyhovující služby, <xref:System.ServiceModel.Discovery.DiscoveryClient.Find%2A> vrátí kolekci <xref:System.ServiceModel.Discovery.EndpointDiscoveryMetadata>.</span><span class="sxs-lookup"><span data-stu-id="ce971-165">If matching services are found, <xref:System.ServiceModel.Discovery.DiscoveryClient.Find%2A> returns a collection of <xref:System.ServiceModel.Discovery.EndpointDiscoveryMetadata>.</span></span> <span data-ttu-id="ce971-166">Následující kód ukazuje, jak zavolat metodu `Find` a pak se připojit ke zjištěné službě.</span><span class="sxs-lookup"><span data-stu-id="ce971-166">The following code shows how to call the `Find` method and then connect to a discovered service.</span></span>  
  
```csharp  
class Client
{
    static EndpointAddress serviceAddress;
  
    static void Main()
    {  
        if (FindService()) 
        {
            InvokeService();
        }
    }  
  
    // ** DISCOVERY ** //  
    static bool FindService()  
    {  
        Console.WriteLine("\nFinding Calculator Service ..");  
        DiscoveryClient discoveryClient =   
            new DiscoveryClient(new UdpDiscoveryEndpoint());  
  
        Collection<EndpointDiscoveryMetadata> calculatorServices =   
            (Collection<EndpointDiscoveryMetadata>)discoveryClient.Find(new FindCriteria(typeof(ICalculator))).Endpoints;  
  
        discoveryClient.Close();  
  
        if (calculatorServices.Count == 0)  
        {  
            Console.WriteLine("\nNo services are found.");  
            return false;  
        }  
        else  
        {  
            serviceAddress = calculatorServices[0].Address;  
            return true;  
        }  
    }  
  
    static void InvokeService()  
    {  
        Console.WriteLine("\nInvoking Calculator Service at {0}\n", serviceAddress);  
  
        // Create a client  
        CalculatorClient client = new CalculatorClient();  
        client.Endpoint.Address = serviceAddress;  
        client.Add(10,3);  
    }
}  
```  
  
## <a name="discovery-and-message-level-security"></a><span data-ttu-id="ce971-167">Zjišťování a zabezpečení na úrovni zpráv</span><span class="sxs-lookup"><span data-stu-id="ce971-167">Discovery and Message Level Security</span></span>  
 <span data-ttu-id="ce971-168">Pokud používáte zabezpečení na úrovni zpráv, je nutné zadat <xref:System.ServiceModel.EndpointIdentity> pro koncový bod zjišťování služby a odpovídajícího <xref:System.ServiceModel.EndpointIdentity> na koncovém bodu zjišťování klienta.</span><span class="sxs-lookup"><span data-stu-id="ce971-168">When using message level security it is necessary to specify an <xref:System.ServiceModel.EndpointIdentity> on the service discovery endpoint and a matching <xref:System.ServiceModel.EndpointIdentity> on the client discovery endpoint.</span></span> <span data-ttu-id="ce971-169">Další informace o zabezpečení na úrovni zpráv najdete v tématu [zabezpečení zpráv](../../../../docs/framework/wcf/feature-details/message-security-in-wcf.md).</span><span class="sxs-lookup"><span data-stu-id="ce971-169">For more information about message level security, see [Message Security](../../../../docs/framework/wcf/feature-details/message-security-in-wcf.md).</span></span>  
  
## <a name="discovery-and-web-hosted-services"></a><span data-ttu-id="ce971-170">Služby pro zjišťování a hostování webů</span><span class="sxs-lookup"><span data-stu-id="ce971-170">Discovery and Web Hosted Services</span></span>  
 <span data-ttu-id="ce971-171">Aby služby WCF bylo možné zjistit, musí být spuštěny.</span><span class="sxs-lookup"><span data-stu-id="ce971-171">In order for WCF services to be discoverable they must be running.</span></span> <span data-ttu-id="ce971-172">Služby WCF hostované v rámci služby IIS nebo se nedají spustit, dokud služba IIS/neobdrží pro službu vazbu na zprávu, takže se ve výchozím nastavení nedá zjistit.</span><span class="sxs-lookup"><span data-stu-id="ce971-172">WCF services hosted under IIS or WAS do not run until IIS/WAS receives a message bound for the service, so they cannot be discoverable by default.</span></span>  <span data-ttu-id="ce971-173">Existují dvě možnosti, jak zpřístupnit webové služby, které jsou zjistitelné:</span><span class="sxs-lookup"><span data-stu-id="ce971-173">There are two options for making Web-Hosted services discoverable:</span></span>  
  
1. <span data-ttu-id="ce971-174">Použití funkce automatického spuštění Windows serveru AppFabric</span><span class="sxs-lookup"><span data-stu-id="ce971-174">Use the Windows Server AppFabric Auto-Start feature</span></span>  
  
2. <span data-ttu-id="ce971-175">Pro komunikaci jménem služby použít proxy zjišťování</span><span class="sxs-lookup"><span data-stu-id="ce971-175">Use a discovery proxy to communicate on behalf of the service</span></span>  
  
 <span data-ttu-id="ce971-176">Windows Server AppFabric má funkci automatického spuštění, která umožní spuštění služby před přijetím jakýchkoli zpráv.</span><span class="sxs-lookup"><span data-stu-id="ce971-176">Windows Server AppFabric has an Auto-Start feature that will allow a service to be started before receiving any messages.</span></span> <span data-ttu-id="ce971-177">V této automatické sadě se dá nakonfigurovat hostovaná služba IIS/s, aby byla zjistitelná.</span><span class="sxs-lookup"><span data-stu-id="ce971-177">With this Auto-Start set, an IIS/WAS hosted service can be configured to be discoverable.</span></span> <span data-ttu-id="ce971-178">Další informace o funkci automatického spuštění najdete v tématu [funkce automatického spuštění Windows serveru AppFabric](https://docs.microsoft.com/previous-versions/appfabric/ee677260(v=azure.10)).</span><span class="sxs-lookup"><span data-stu-id="ce971-178">For more information about the Auto-Start feature see, [Windows Server AppFabric Auto-Start Feature](https://docs.microsoft.com/previous-versions/appfabric/ee677260(v=azure.10)).</span></span> <span data-ttu-id="ce971-179">Společně s zapnutím funkce automatického spuštění musíte službu nakonfigurovat pro zjišťování.</span><span class="sxs-lookup"><span data-stu-id="ce971-179">Along with turning on the Auto-Start feature, you must configure the service for discovery.</span></span> <span data-ttu-id="ce971-180">Další informace naleznete v tématu [How to: programing](../../../../docs/framework/wcf/feature-details/how-to-programmatically-add-discoverability-to-a-wcf-service-and-client.md)autoforming on a Client[Configure Discovery in a Configuration v konfiguračním souboru](../../../../docs/framework/wcf/feature-details/configuring-discovery-in-a-configuration-file.md).</span><span class="sxs-lookup"><span data-stu-id="ce971-180">For more information, see [How to: Programmatically Add Discoverability to a WCF Service and Client](../../../../docs/framework/wcf/feature-details/how-to-programmatically-add-discoverability-to-a-wcf-service-and-client.md)[Configuring Discovery in a Configuration File](../../../../docs/framework/wcf/feature-details/configuring-discovery-in-a-configuration-file.md).</span></span>  
  
 <span data-ttu-id="ce971-181">Proxy zjišťování lze použít ke komunikaci jménem služby WCF, pokud služba není spuštěna.</span><span class="sxs-lookup"><span data-stu-id="ce971-181">A discovery proxy can be used to communicate on behalf of the WCF service when the service is not running.</span></span> <span data-ttu-id="ce971-182">Proxy server může naslouchat testům nebo překládat zprávy a reagovat na klienta.</span><span class="sxs-lookup"><span data-stu-id="ce971-182">The proxy can listen for probe or resolve messages and respond to the client.</span></span> <span data-ttu-id="ce971-183">Klient pak může odesílat zprávy přímo službě.</span><span class="sxs-lookup"><span data-stu-id="ce971-183">The client can then send messages directly to the service.</span></span> <span data-ttu-id="ce971-184">Když klient pošle zprávu službě, že se vytvoří její instance, aby reagovala na zprávu.</span><span class="sxs-lookup"><span data-stu-id="ce971-184">When the client sends a message to the service it will be instantiated to respond to the message.</span></span> <span data-ttu-id="ce971-185">Další informace o implementaci proxy zjišťování najdete v tématu [implementace proxy zjišťování](../../../../docs/framework/wcf/feature-details/implementing-a-discovery-proxy.md).</span><span class="sxs-lookup"><span data-stu-id="ce971-185">For more information about implementing a discovery proxy see, [Implementing a Discovery Proxy](../../../../docs/framework/wcf/feature-details/implementing-a-discovery-proxy.md).</span></span>  
  
> [!NOTE]
> <span data-ttu-id="ce971-186">Aby zjišťování WCF fungovalo správně, musí mít všechny síťové karty (síťový adaptér) jenom 1 IP adresu.</span><span class="sxs-lookup"><span data-stu-id="ce971-186">For WCF Discovery to work correctly, all NICs (Network Interface Controller) should only have 1 IP address.</span></span>
