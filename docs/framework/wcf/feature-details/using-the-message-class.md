---
title: "Používání třídy Message"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-clr
ms.tgt_pltfrm: 
ms.topic: article
dev_langs:
- csharp
- vb
ms.assetid: d1d62bfb-2aa3-4170-b6f8-c93d3afdbbed
caps.latest.revision: "14"
author: Erikre
ms.author: erikre
manager: erikre
ms.openlocfilehash: 18cb2162712ffac74972ba20a61cd84657685af0
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/18/2017
---
# <a name="using-the-message-class"></a><span data-ttu-id="d75b2-102">Používání třídy Message</span><span class="sxs-lookup"><span data-stu-id="d75b2-102">Using the Message Class</span></span>
<span data-ttu-id="d75b2-103"><xref:System.ServiceModel.Channels.Message> Třída je nezbytné, aby [!INCLUDE[indigo1](../../../../includes/indigo1-md.md)].</span><span class="sxs-lookup"><span data-stu-id="d75b2-103">The <xref:System.ServiceModel.Channels.Message> class is fundamental to [!INCLUDE[indigo1](../../../../includes/indigo1-md.md)].</span></span> <span data-ttu-id="d75b2-104">Veškerá komunikace mezi klienty a služby ve výsledku <xref:System.ServiceModel.Channels.Message> instancí se odesílají a přijímají.</span><span class="sxs-lookup"><span data-stu-id="d75b2-104">All communication between clients and services ultimately results in <xref:System.ServiceModel.Channels.Message> instances being sent and received.</span></span>  
  
 <span data-ttu-id="d75b2-105">By obvykle interakci s <xref:System.ServiceModel.Channels.Message> přímo třídu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-105">You would not usually interact with the <xref:System.ServiceModel.Channels.Message> class directly.</span></span> <span data-ttu-id="d75b2-106">Místo toho [!INCLUDE[indigo2](../../../../includes/indigo2-md.md)] vytvoří model služby, jako je například datové kontrakty, kontrakty zpráv a operace smlouvy, se používají k popisu příchozí a odchozí zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-106">Instead, [!INCLUDE[indigo2](../../../../includes/indigo2-md.md)] service model constructs, such as data contracts, message contracts, and operation contracts, are used to describe incoming and outgoing messages.</span></span> <span data-ttu-id="d75b2-107">Ale v některých pokročilé scénáře, které můžete naprogramovat pomocí <xref:System.ServiceModel.Channels.Message> přímo třídu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-107">However, in some advanced scenarios you can program using the <xref:System.ServiceModel.Channels.Message> class directly.</span></span> <span data-ttu-id="d75b2-108">Například můžete chtít použít <xref:System.ServiceModel.Channels.Message> třídy:</span><span class="sxs-lookup"><span data-stu-id="d75b2-108">For example, you might want to use the <xref:System.ServiceModel.Channels.Message> class:</span></span>  
  
-   <span data-ttu-id="d75b2-109">Když potřebujete alternativní způsob vytváření odchozí obsah zprávy (například vytvoření zprávy přímo ze souboru na disku) namísto serializaci [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] objekty.</span><span class="sxs-lookup"><span data-stu-id="d75b2-109">When you need an alternative way of creating outgoing message contents (for example, creating a message directly from a file on disk) instead of serializing [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] objects.</span></span>  
  
-   <span data-ttu-id="d75b2-110">Když potřebujete alternativní způsob použití příchozí obsah zprávy (například pokud chcete použít transformaci XSLT na nezpracovaná obsah XML) namísto deserializaci do [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] objekty.</span><span class="sxs-lookup"><span data-stu-id="d75b2-110">When you need an alternative way of using incoming message contents (for example, when you want to apply an XSLT transformation to the raw XML contents) instead of deserializing into [!INCLUDE[dnprdnshort](../../../../includes/dnprdnshort-md.md)] objects.</span></span>  
  
-   <span data-ttu-id="d75b2-111">Pokud je potřeba řešit zprávy obecné způsobem, bez ohledu na obsah zprávy (například v případě, že směrování nebo předávat zprávy při sestavování směrovač, Vyrovnávání zatížení nebo publikování-přihlášení k odběru systému).</span><span class="sxs-lookup"><span data-stu-id="d75b2-111">When you need to deal with messages in a general way regardless of message contents (for example, when routing or forwarding messages when building a router, load-balancer, or a publish-subscribe system).</span></span>  
  
 <span data-ttu-id="d75b2-112">Před použitím <xref:System.ServiceModel.Channels.Message> třídy, seznamte se s [!INCLUDE[indigo2](../../../../includes/indigo2-md.md)] přenos dat architektura v [architektury Přehled přenosu dat](../../../../docs/framework/wcf/feature-details/data-transfer-architectural-overview.md).</span><span class="sxs-lookup"><span data-stu-id="d75b2-112">Before using the <xref:System.ServiceModel.Channels.Message> class, familiarize yourself with the [!INCLUDE[indigo2](../../../../includes/indigo2-md.md)] data transfer architecture in [Data Transfer Architectural Overview](../../../../docs/framework/wcf/feature-details/data-transfer-architectural-overview.md).</span></span>  
  
 <span data-ttu-id="d75b2-113">A <xref:System.ServiceModel.Channels.Message> je kontejner pro obecné účely pro data, ale jeho návrhu přesně dodržuje návrh zprávy v protokolu SOAP.</span><span class="sxs-lookup"><span data-stu-id="d75b2-113">A <xref:System.ServiceModel.Channels.Message> is a general-purpose container for data, but its design closely follows the design of a message in the SOAP protocol.</span></span> <span data-ttu-id="d75b2-114">Stejně jako v protokolu SOAP, zpráva má tělo zprávy a hlaviček.</span><span class="sxs-lookup"><span data-stu-id="d75b2-114">Just like in SOAP, a message has both a message body and headers.</span></span> <span data-ttu-id="d75b2-115">Tělo zprávy obsahuje skutečné datová část dat, zatímco hlavičky obsahovat další data s názvem kontejnery.</span><span class="sxs-lookup"><span data-stu-id="d75b2-115">The message body contains the actual payload data, while the headers contain additional named data containers.</span></span> <span data-ttu-id="d75b2-116">Pravidla pro čtení a zápis textu a hlavičky se liší, například hlavičky jsou vždy do vyrovnávací paměti v paměti a můžete získat přístup v žádné pořadí libovolný počet dobu, zatímco text lze číst pouze jednou a může být streamování.</span><span class="sxs-lookup"><span data-stu-id="d75b2-116">The rules for reading and writing the body and the headers are different, for example, the headers are always buffered in memory and may be accessed in any order any number of times, while the body may be read only once and may be streamed.</span></span> <span data-ttu-id="d75b2-117">Za normálních okolností při použití protokolu SOAP, tělo zprávy je namapovaná na textu protokolu SOAP a záhlaví zprávy jsou namapované na hlavičky SOAP.</span><span class="sxs-lookup"><span data-stu-id="d75b2-117">Normally, when using SOAP, the message body is mapped to the SOAP body and the message headers are mapped to the SOAP headers.</span></span>  
  
## <a name="using-the-message-class-in-operations"></a><span data-ttu-id="d75b2-118">Používání třídy Message v operacích</span><span class="sxs-lookup"><span data-stu-id="d75b2-118">Using the Message Class in Operations</span></span>  
 <span data-ttu-id="d75b2-119">Můžete použít <xref:System.ServiceModel.Channels.Message> třída jako vstupní parametr operace, návratová hodnota operace, nebo obojí.</span><span class="sxs-lookup"><span data-stu-id="d75b2-119">You can use the <xref:System.ServiceModel.Channels.Message> class as an input parameter of an operation, the return value of an operation, or both.</span></span> <span data-ttu-id="d75b2-120">Pokud <xref:System.ServiceModel.Channels.Message> slouží kdekoli v operaci, platí následující omezení:</span><span class="sxs-lookup"><span data-stu-id="d75b2-120">If <xref:System.ServiceModel.Channels.Message> is used anywhere in an operation, the following restrictions apply:</span></span>  
  
-   <span data-ttu-id="d75b2-121">Operace nemůže mít žádné `out` nebo `ref` parametry.</span><span class="sxs-lookup"><span data-stu-id="d75b2-121">The operation cannot have any `out` or `ref` parameters.</span></span>  
  
-   <span data-ttu-id="d75b2-122">Nemůže být více než jeden `input` parametr.</span><span class="sxs-lookup"><span data-stu-id="d75b2-122">There cannot be more than one `input` parameter.</span></span> <span data-ttu-id="d75b2-123">Pokud se nachází parametr, musí být zpráva nebo typ kontrakt zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-123">If the parameter is present, it must be either Message or a message contract type.</span></span>  
  
-   <span data-ttu-id="d75b2-124">Návratový typ musí být buď `void`, `Message`, nebo zprávu typ kontraktu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-124">The return type must be either `void`, `Message`, or a message contract type.</span></span>  
  
 <span data-ttu-id="d75b2-125">Následující příklad kódu obsahuje kontraktu platná operace.</span><span class="sxs-lookup"><span data-stu-id="d75b2-125">The following code example contains a valid operation contract.</span></span>  
  
 [!code-csharp[C_UsingTheMessageClass#1](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_usingthemessageclass/cs/source.cs#1)]
 [!code-vb[C_UsingTheMessageClass#1](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_usingthemessageclass/vb/source.vb#1)]  
  
## <a name="creating-basic-messages"></a><span data-ttu-id="d75b2-126">Vytváření základních zprávy</span><span class="sxs-lookup"><span data-stu-id="d75b2-126">Creating Basic Messages</span></span>  
 <span data-ttu-id="d75b2-127"><xref:System.ServiceModel.Channels.Message> Třída poskytuje statické `CreateMessage` factory metody, které můžete použít k vytvoření základní zpráv.</span><span class="sxs-lookup"><span data-stu-id="d75b2-127">The <xref:System.ServiceModel.Channels.Message> class provides static `CreateMessage` factory methods that you can use to create basic messages.</span></span>  
  
 <span data-ttu-id="d75b2-128">Všechny `CreateMessage` přetížení přebírají parametr verze typu <xref:System.ServiceModel.Channels.MessageVersion> určující verze protokolu SOAP a WS-Addressing sloužící ke zprávě.</span><span class="sxs-lookup"><span data-stu-id="d75b2-128">All `CreateMessage` overloads take a version parameter of type <xref:System.ServiceModel.Channels.MessageVersion> that indicates the SOAP and WS-Addressing versions to use for the message.</span></span> <span data-ttu-id="d75b2-129">Pokud chcete použít stejné verze protokolu jako příchozí zprávy, můžete použít <xref:System.ServiceModel.OperationContext.IncomingMessageVersion%2A> vlastnost na <xref:System.ServiceModel.OperationContext> instance získaný <xref:System.ServiceModel.OperationContext.Current%2A> vlastnost.</span><span class="sxs-lookup"><span data-stu-id="d75b2-129">If you want to use the same protocol versions as the incoming message, you can use the <xref:System.ServiceModel.OperationContext.IncomingMessageVersion%2A> property on the <xref:System.ServiceModel.OperationContext> instance obtained from the <xref:System.ServiceModel.OperationContext.Current%2A> property.</span></span> <span data-ttu-id="d75b2-130">Většina `CreateMessage` přetížení také mít parametr řetězec, který určuje použití pro zprávu protokolu SOAP akce.</span><span class="sxs-lookup"><span data-stu-id="d75b2-130">Most `CreateMessage` overloads also have a string parameter that indicates the SOAP action to use for the message.</span></span> <span data-ttu-id="d75b2-131">Verze může být nastaven na `None` zakázat generování obálky protokolu SOAP; tato zpráva se skládá pouze obsahu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-131">Version can be set to `None` to disable SOAP envelope generation; the message consists of only the body.</span></span>  
  
## <a name="creating-messages-from-objects"></a><span data-ttu-id="d75b2-132">Vytváření zprávy z objektů</span><span class="sxs-lookup"><span data-stu-id="d75b2-132">Creating Messages from Objects</span></span>  
 <span data-ttu-id="d75b2-133">Nejzákladnější `CreateMessage` přetížení, které přijímá pouze verze a akce vytvoří zprávu s prázdným textem zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-133">The most basic `CreateMessage` overload that takes only a version and an action creates a message with an empty body.</span></span> <span data-ttu-id="d75b2-134">Další přetížení trvá Další <xref:System.Object> parametr; tím se vytvoří zprávu, jejíž text je serializovaná reprezentace daného objektu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-134">Another overload takes an additional <xref:System.Object> parameter; this creates a message whose body is the serialized representation of the given object.</span></span> <span data-ttu-id="d75b2-135">Použití <xref:System.Runtime.Serialization.DataContractSerializer> s výchozím nastavením pro serializaci.</span><span class="sxs-lookup"><span data-stu-id="d75b2-135">Use the <xref:System.Runtime.Serialization.DataContractSerializer> with default settings for serialization.</span></span> <span data-ttu-id="d75b2-136">Pokud chcete použít jiný serializátor, nebo chcete `DataContractSerializer` nakonfigurována jinak než pomocí `CreateMessage` přetížení, které přijímá také `XmlObjectSerializer` parametr.</span><span class="sxs-lookup"><span data-stu-id="d75b2-136">If you want to use a different serializer, or you want the `DataContractSerializer` configured differently, use the `CreateMessage` overload that also takes an `XmlObjectSerializer` parameter.</span></span>  
  
 <span data-ttu-id="d75b2-137">Například pokud chcete vrátit objekt ve zprávě, můžete použít následující kód.</span><span class="sxs-lookup"><span data-stu-id="d75b2-137">For example, to return an object in a message, you can use the following code.</span></span>  
  
 [!code-csharp[C_UsingTheMessageClass#2](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_usingthemessageclass/cs/source.cs#2)]
 [!code-vb[C_UsingTheMessageClass#2](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_usingthemessageclass/vb/source.vb#2)]  
  
## <a name="creating-messages-from-xml-readers"></a><span data-ttu-id="d75b2-138">Vytváření zprávy z čtečky XML</span><span class="sxs-lookup"><span data-stu-id="d75b2-138">Creating Messages from XML Readers</span></span>  
 <span data-ttu-id="d75b2-139">Existují `CreateMessage` přetížení trvají <xref:System.Xml.XmlReader> nebo <xref:System.Xml.XmlDictionaryReader> pro tělo namísto objektu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-139">There are `CreateMessage` overloads that take an <xref:System.Xml.XmlReader> or an <xref:System.Xml.XmlDictionaryReader> for the body instead of an object.</span></span> <span data-ttu-id="d75b2-140">V takovém případě tělo zprávy obsahuje XML, který je výsledkem čtení z předaný čtecí modul XML.</span><span class="sxs-lookup"><span data-stu-id="d75b2-140">In this case, the body of the message contains the XML that results from reading from the passed XML reader.</span></span> <span data-ttu-id="d75b2-141">Například následující kód vrátí zprávu s textu, že obsah číst ze souboru XML.</span><span class="sxs-lookup"><span data-stu-id="d75b2-141">For example, the following code returns a message with body contents read from an XML file.</span></span>  
  
 [!code-csharp[C_UsingTheMessageClass#3](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_usingthemessageclass/cs/source.cs#3)]
 [!code-vb[C_UsingTheMessageClass#3](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_usingthemessageclass/vb/source.vb#3)]  
  
 <span data-ttu-id="d75b2-142">Kromě toho existují `CreateMessage` přetížení trvají <xref:System.Xml.XmlReader> nebo <xref:System.Xml.XmlDictionaryReader> představující celé zprávy a nikoli pouze text.</span><span class="sxs-lookup"><span data-stu-id="d75b2-142">Additionally, there are `CreateMessage` overloads that take an <xref:System.Xml.XmlReader> or an <xref:System.Xml.XmlDictionaryReader> that represents the entire message and not just the body.</span></span> <span data-ttu-id="d75b2-143">Tato přetížení také trvat celé `maxSizeOfHeaders` parametr.</span><span class="sxs-lookup"><span data-stu-id="d75b2-143">These overloads also take an integer `maxSizeOfHeaders` parameter.</span></span> <span data-ttu-id="d75b2-144">Hlavičky jsou vždy do vyrovnávací paměti do paměti Jakmile se vytvoří zprávu, a tento parametr omezuje množství ukládání do vyrovnávací paměti, který probíhá.</span><span class="sxs-lookup"><span data-stu-id="d75b2-144">Headers are always buffered into memory as soon as the message is created, and this parameter limits the amount of buffering that takes place.</span></span> <span data-ttu-id="d75b2-145">Je důležité v případě XML pochází z nedůvěryhodného zdroje zmírnit možnost útoku DOS tento parametr nastavte na hodnotu bezpečné.</span><span class="sxs-lookup"><span data-stu-id="d75b2-145">It is important to set this parameter to a safe value if the XML is coming from an untrusted source to mitigate the possibility of a denial of service attack.</span></span> <span data-ttu-id="d75b2-146">Verze protokolu SOAP a WS-Addressing zprávy, kterou představuje čtecí modul XML musí odpovídat verze uvedené, pomocí parametru verze.</span><span class="sxs-lookup"><span data-stu-id="d75b2-146">The SOAP and WS-Addressing versions of the message the XML reader represents must match the versions indicated using the version parameter.</span></span>  
  
## <a name="creating-messages-with-bodywriter"></a><span data-ttu-id="d75b2-147">Vytváření zprávy s BodyWriter</span><span class="sxs-lookup"><span data-stu-id="d75b2-147">Creating Messages with BodyWriter</span></span>  
 <span data-ttu-id="d75b2-148">Jeden `CreateMessage` přetížení trvá `BodyWriter` instance k popisu tělo zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-148">One `CreateMessage` overload takes a `BodyWriter` instance to describe the body of the message.</span></span> <span data-ttu-id="d75b2-149">A `BodyWriter` je abstraktní třída, která lze odvodit přizpůsobit vytváření zpráv.</span><span class="sxs-lookup"><span data-stu-id="d75b2-149">A `BodyWriter` is an abstract class that can be derived to customize how message bodies are created.</span></span> <span data-ttu-id="d75b2-150">Můžete vytvořit vlastní `BodyWriter` odvozené třídy k popisu těla zprávy vlastním způsobem.</span><span class="sxs-lookup"><span data-stu-id="d75b2-150">You can create your own `BodyWriter` derived class to describe message bodies in a custom way.</span></span> <span data-ttu-id="d75b2-151">Je nutné přepsat `BodyWriter.OnWriteBodyContents` metody, která použije <xref:System.Xml.XmlDictionaryWriter>; tato metoda je zodpovědné za zápis na text.</span><span class="sxs-lookup"><span data-stu-id="d75b2-151">You must override the `BodyWriter.OnWriteBodyContents` method that takes an <xref:System.Xml.XmlDictionaryWriter>; this method is responsible for writing out the body.</span></span>  
  
 <span data-ttu-id="d75b2-152">Můžete do vyrovnávací paměti zapisovače textu nebo bez vyrovnávací paměti (streamování).</span><span class="sxs-lookup"><span data-stu-id="d75b2-152">Body writers can be buffered or non-buffered (streamed).</span></span> <span data-ttu-id="d75b2-153">Zapisovače textu ve vyrovnávací paměti můžete zapsat jejich obsah libovolný počet dobu, během přenášené datovými proudy ty můžete zapsat jejich obsah pouze jednou.</span><span class="sxs-lookup"><span data-stu-id="d75b2-153">Buffered body writers can write out their contents any number of times, while streamed ones can write out their contents only once.</span></span> <span data-ttu-id="d75b2-154">`IsBuffered` Vlastnost určuje, zda je zapisovač textu do vyrovnávací paměti nebo ne.</span><span class="sxs-lookup"><span data-stu-id="d75b2-154">The `IsBuffered` property indicates whether a body writer is buffered or not.</span></span> <span data-ttu-id="d75b2-155">Můžete nastavit to pro vaše zapisovač textu voláním chráněné `BodyWriter` konstruktor, který přebírá `isBuffered` logického parametru.</span><span class="sxs-lookup"><span data-stu-id="d75b2-155">You can set this for your body writer by calling the protected `BodyWriter` constructor that takes an `isBuffered` boolean parameter.</span></span> <span data-ttu-id="d75b2-156">Zapisovače textu podporují vytváření zapisovač textu ve vyrovnávací paměti z zapisovač textu bez vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="d75b2-156">Body writers support creating a buffered body writer from a non-buffered body writer.</span></span> <span data-ttu-id="d75b2-157">Je možné přepsat `OnCreateBufferedCopy` metodu za účelem přizpůsobení tohoto procesu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-157">You can override the `OnCreateBufferedCopy` method to customize this process.</span></span> <span data-ttu-id="d75b2-158">Ve výchozím nastavení, v paměti vyrovnávací paměti, který obsahuje XML vrácený `OnWriteBodyContents` se používá.</span><span class="sxs-lookup"><span data-stu-id="d75b2-158">By default, an in-memory buffer that contains the XML returned by `OnWriteBodyContents` is used.</span></span> <span data-ttu-id="d75b2-159">`OnCreateBufferedCopy`přijímá `maxBufferSize` celé číslo parametru; Pokud tuto metodu přepíšete, nesmí vytvoříte vyrovnávací paměti větší než této maximální velikosti.</span><span class="sxs-lookup"><span data-stu-id="d75b2-159">`OnCreateBufferedCopy` takes a `maxBufferSize` integer parameter; if you override this method, you must not create buffers larger than this maximum size.</span></span>  
  
 <span data-ttu-id="d75b2-160">`BodyWriter` Třída poskytuje `WriteBodyContents` a `CreateBufferedCopy` metody, které jsou v podstatě dynamicky obálky kolem `OnWriteBodyContents` a `OnCreateBufferedCopy` metody, v uvedeném pořadí.</span><span class="sxs-lookup"><span data-stu-id="d75b2-160">The `BodyWriter` class provides the `WriteBodyContents` and `CreateBufferedCopy` methods, which are essentially thin wrappers around `OnWriteBodyContents` and `OnCreateBufferedCopy` methods, respectively.</span></span> <span data-ttu-id="d75b2-161">Tyto metody provedení stavu kontrole zajistíte zapisovač textu bez vyrovnávací paměti není přístup k více než jednou.</span><span class="sxs-lookup"><span data-stu-id="d75b2-161">These methods perform state checking to ensure that a non-buffered body writer is not accessed more than once.</span></span> <span data-ttu-id="d75b2-162">Tyto metody jsou pouze při vytváření vlastní volat přímo `Message` odvozených třídách na základě `BodyWriters`.</span><span class="sxs-lookup"><span data-stu-id="d75b2-162">These methods are called directly only when creating custom `Message` derived classes based on `BodyWriters`.</span></span>  
  
## <a name="creating-fault-messages"></a><span data-ttu-id="d75b2-163">Vytváření selhání zprávy</span><span class="sxs-lookup"><span data-stu-id="d75b2-163">Creating Fault Messages</span></span>  
 <span data-ttu-id="d75b2-164">Můžete použít určité `CreateMessage` poruch přetížení pro vytvoření protokolu SOAP zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-164">You can use certain `CreateMessage` overloads to create SOAP fault messages.</span></span> <span data-ttu-id="d75b2-165">Nejvíce basic tyto trvá <xref:System.ServiceModel.Channels.MessageFault> objekt, který popisuje chybu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-165">The most basic of these takes a <xref:System.ServiceModel.Channels.MessageFault> object that describes the fault.</span></span> <span data-ttu-id="d75b2-166">Pro usnadnění práce najdete dalšími přetíženími.</span><span class="sxs-lookup"><span data-stu-id="d75b2-166">Other overloads are provided for convenience.</span></span> <span data-ttu-id="d75b2-167">První například přetížení trvá `FaultCode` a řetězec důvod a vytvoří `MessageFault` pomocí `MessageFault.CreateFault` na základě těchto informací.</span><span class="sxs-lookup"><span data-stu-id="d75b2-167">The first such overload takes a `FaultCode` and a reason string and creates a `MessageFault` using `MessageFault.CreateFault` using this information.</span></span> <span data-ttu-id="d75b2-168">Další přetížení přebírá objekt podrobnosti a také předává jej do `CreateFault` společně s kódem chyby a z důvodu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-168">The other overload takes a detail object and also passes it to `CreateFault` together with the fault code and the reason.</span></span> <span data-ttu-id="d75b2-169">Například následující operace vrátí chybu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-169">For example, the following operation returns a fault.</span></span>  
  
 [!code-csharp[C_UsingTheMessageClass#4](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_usingthemessageclass/cs/source.cs#4)]
 [!code-vb[C_UsingTheMessageClass#4](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_usingthemessageclass/vb/source.vb#4)]  
  
## <a name="extracting-message-body-data"></a><span data-ttu-id="d75b2-170">Extrahování dat tělo zprávy</span><span class="sxs-lookup"><span data-stu-id="d75b2-170">Extracting Message Body Data</span></span>  
 <span data-ttu-id="d75b2-171">`Message` Třída podporuje více způsobů extrahování informací z jeho textu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-171">The `Message` class supports multiple ways of extracting information from its body.</span></span> <span data-ttu-id="d75b2-172">Ty můžou být klasifikované do následujících kategorií:</span><span class="sxs-lookup"><span data-stu-id="d75b2-172">These can be classified into the following categories:</span></span>  
  
-   <span data-ttu-id="d75b2-173">Načítání textu celé zprávy najednou zapsané do zapisovače XML.</span><span class="sxs-lookup"><span data-stu-id="d75b2-173">Getting the entire message body written out at once to an XML writer.</span></span> <span data-ttu-id="d75b2-174">Tento proces se označuje jako *zápis zprávy*.</span><span class="sxs-lookup"><span data-stu-id="d75b2-174">This is referred to as *writing a message*.</span></span>  
  
-   <span data-ttu-id="d75b2-175">Získávání čtecí modul XML přes text zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-175">Getting an XML reader over the message body.</span></span> <span data-ttu-id="d75b2-176">Díky tomu můžete zprávu textu část jednotlivé novější přístupu podle potřeby.</span><span class="sxs-lookup"><span data-stu-id="d75b2-176">This enables you to later access the message body piece-by-piece as required.</span></span> <span data-ttu-id="d75b2-177">Tento proces se označuje jako *čtení zprávy*.</span><span class="sxs-lookup"><span data-stu-id="d75b2-177">This is referred to as *reading a message*.</span></span>  
  
-   <span data-ttu-id="d75b2-178">Celé zprávy, včetně jeho textu je možné zkopírovat do vyrovnávací paměť v paměti z <xref:System.ServiceModel.Channels.MessageBuffer> typu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-178">The entire message, including its body, can be copied to an in-memory buffer of the <xref:System.ServiceModel.Channels.MessageBuffer> type.</span></span> <span data-ttu-id="d75b2-179">Tento proces se označuje jako *kopírování zprávu*.</span><span class="sxs-lookup"><span data-stu-id="d75b2-179">This is referred to as *copying a message*.</span></span>  
  
 <span data-ttu-id="d75b2-180">Dostanete textu `Message` pouze jednou, bez ohledu na to, jak je přístupný.</span><span class="sxs-lookup"><span data-stu-id="d75b2-180">You can access the body of a `Message` only once, regardless of how it is accessed.</span></span> <span data-ttu-id="d75b2-181">Objekt zpráva má `State` vlastnosti, která je původně nastaven na vytvořit.</span><span class="sxs-lookup"><span data-stu-id="d75b2-181">A message object has a `State` property, which is initially set to Created.</span></span> <span data-ttu-id="d75b2-182">Tři přístup metod popsaných v předchozím seznamu nastavit stav na Written, čtení a zkopírovaných, v uvedeném pořadí.</span><span class="sxs-lookup"><span data-stu-id="d75b2-182">The three access methods described in the preceding list set the state to Written, Read, and Copied, respectively.</span></span> <span data-ttu-id="d75b2-183">Kromě toho `Close` metoda můžete nastavit stav na Uzavřeno, když obsah zprávy se už nevyžadují.</span><span class="sxs-lookup"><span data-stu-id="d75b2-183">Additionally, a `Close` method can set the state to Closed when the message body contents are no longer required.</span></span> <span data-ttu-id="d75b2-184">Tělo zprávy jsou přístupné pouze ve stavu vytvořen a neexistuje žádný způsob, jak vrátit do stavu vytvořeno po změně stavu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-184">The message body can be accessed only in the Created state, and there is no way to go back to the Created state after the state has changed.</span></span>  
  
## <a name="writing-messages"></a><span data-ttu-id="d75b2-185">Zápis zpráv</span><span class="sxs-lookup"><span data-stu-id="d75b2-185">Writing Messages</span></span>  
 <span data-ttu-id="d75b2-186"><xref:System.ServiceModel.Channels.Message.WriteBodyContents%28System.Xml.XmlDictionaryWriter%29> Metoda zapíše obsah textu danou `Message` instance daného zapisovače XML.</span><span class="sxs-lookup"><span data-stu-id="d75b2-186">The <xref:System.ServiceModel.Channels.Message.WriteBodyContents%28System.Xml.XmlDictionaryWriter%29> method writes out the body contents of a given `Message` instance to a given XML writer.</span></span> <span data-ttu-id="d75b2-187"><xref:System.ServiceModel.Channels.Message.WriteBody%2A> Metoda dělá to stejné, s tím rozdílem, že se uzavře obsah textu v příslušné obálku elementu (například <`soap:body`>).</span><span class="sxs-lookup"><span data-stu-id="d75b2-187">The <xref:System.ServiceModel.Channels.Message.WriteBody%2A> method does the same, except that it encloses the body contents in the appropriate wrapper element (for example, <`soap:body`>).</span></span> <span data-ttu-id="d75b2-188">Nakonec <xref:System.ServiceModel.Channels.Message.WriteMessage%2A> zápisy celou zprávu, včetně zabalenou SOAP obálky a hlavičky.</span><span class="sxs-lookup"><span data-stu-id="d75b2-188">Finally, <xref:System.ServiceModel.Channels.Message.WriteMessage%2A> writes out the entire message, including the wrapping SOAP envelope and the headers.</span></span> <span data-ttu-id="d75b2-189">Pokud je vypnutý protokolu SOAP (verze je `MessageVersion.None`), stejnou věc udělat všechny tři metody: jejich Vypsat obsah textu zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-189">If SOAP is turned off (Version is `MessageVersion.None`), all three methods do the same thing: they write out the message body contents.</span></span>  
  
 <span data-ttu-id="d75b2-190">Například následující kód zapíše text příchozí zprávy do souboru.</span><span class="sxs-lookup"><span data-stu-id="d75b2-190">For example, the following code writes out the body of an incoming message to a file.</span></span>  
  
 [!code-csharp[C_UsingTheMessageClass#5](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_usingthemessageclass/cs/source.cs#5)]
 [!code-vb[C_UsingTheMessageClass#5](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_usingthemessageclass/vb/source.vb#5)]  
  
 <span data-ttu-id="d75b2-191">Dva další pomocné metody zapsat určité značky elementu spuštění protokolu SOAP.</span><span class="sxs-lookup"><span data-stu-id="d75b2-191">Two additional helper methods write out certain SOAP start element tags.</span></span> <span data-ttu-id="d75b2-192">Tyto metody k přístupu ke tělo zprávy a proto mohou změnit stav zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-192">These methods do not access the message body and so they do not change the message state.</span></span> <span data-ttu-id="d75b2-193">Mezi ně patří:</span><span class="sxs-lookup"><span data-stu-id="d75b2-193">These include:</span></span>  
  
-   <span data-ttu-id="d75b2-194"><xref:System.ServiceModel.Channels.Message.WriteStartBody%2A>zapíše elementu počáteční body, například `<soap:Body>`.</span><span class="sxs-lookup"><span data-stu-id="d75b2-194"><xref:System.ServiceModel.Channels.Message.WriteStartBody%2A> writes the start body element, for example, `<soap:Body>`.</span></span>  
  
-   <span data-ttu-id="d75b2-195"><xref:System.ServiceModel.Channels.Message.WriteStartEnvelope%2A>zapíše počáteční obálky prvek, například `<soap:Envelope>`.</span><span class="sxs-lookup"><span data-stu-id="d75b2-195"><xref:System.ServiceModel.Channels.Message.WriteStartEnvelope%2A> writes the start envelope element, for example, `<soap:Envelope>`.</span></span>  
  
 <span data-ttu-id="d75b2-196">Zápis odpovídající koncové značky elementu, volání `WriteEndElement` na odpovídající zapisovače XML.</span><span class="sxs-lookup"><span data-stu-id="d75b2-196">To write the corresponding end element tags, call `WriteEndElement` on the corresponding XML writer.</span></span> <span data-ttu-id="d75b2-197">Tyto metody jsou zřídka volat přímo.</span><span class="sxs-lookup"><span data-stu-id="d75b2-197">These methods are rarely called directly.</span></span>  
  
## <a name="reading-messages"></a><span data-ttu-id="d75b2-198">Čtení zpráv</span><span class="sxs-lookup"><span data-stu-id="d75b2-198">Reading Messages</span></span>  
 <span data-ttu-id="d75b2-199">Primární způsobem, jak číst text zprávy je volání <xref:System.ServiceModel.Channels.Message.GetReaderAtBodyContents%2A>.</span><span class="sxs-lookup"><span data-stu-id="d75b2-199">The primary way to read a message body is to call <xref:System.ServiceModel.Channels.Message.GetReaderAtBodyContents%2A>.</span></span> <span data-ttu-id="d75b2-200">Můžete se vrátit <xref:System.Xml.XmlDictionaryReader> , můžete použít ke čtení textu zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-200">You get back an <xref:System.Xml.XmlDictionaryReader> that you can use to read the message body.</span></span> <span data-ttu-id="d75b2-201">Všimněte si, že <xref:System.ServiceModel.Channels.Message> přechody stavu čtení co nejrychleji <xref:System.ServiceModel.Channels.Message.GetReaderAtBodyContents%2A> je volána, a není při použití vrácený čtecí modul XML.</span><span class="sxs-lookup"><span data-stu-id="d75b2-201">Note that the <xref:System.ServiceModel.Channels.Message> transitions to the Read state as soon as <xref:System.ServiceModel.Channels.Message.GetReaderAtBodyContents%2A> is called, and not when you use the returned XML reader.</span></span>  
  
 <span data-ttu-id="d75b2-202"><xref:System.ServiceModel.Channels.Message.GetBody%2A> Metoda také umožňuje přístup k tělo zprávy jako objekt typu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-202">The <xref:System.ServiceModel.Channels.Message.GetBody%2A> method also enables you to access the message body as a typed object.</span></span> <span data-ttu-id="d75b2-203">Interně tato metoda používá `GetReaderAtBodyContents`, a proto také přechází zpráva stav, který má <xref:System.ServiceModel.Channels.MessageState.Read> stavu (najdete v článku <xref:System.ServiceModel.Channels.Message.State%2A> vlastnost).</span><span class="sxs-lookup"><span data-stu-id="d75b2-203">Internally, this method uses `GetReaderAtBodyContents`, and so it also transitions the message state to the <xref:System.ServiceModel.Channels.MessageState.Read> state (see the <xref:System.ServiceModel.Channels.Message.State%2A> property).</span></span>  
  
 <span data-ttu-id="d75b2-204">Je vhodné zkontrolovat <xref:System.ServiceModel.Channels.Message.IsEmpty%2A> vlastnost, ve kterém je případ tělo zprávy prázdný a <xref:System.ServiceModel.Channels.Message.GetReaderAtBodyContents%2A> vyvolá <xref:System.InvalidOperationException>.</span><span class="sxs-lookup"><span data-stu-id="d75b2-204">It is good practice to check the <xref:System.ServiceModel.Channels.Message.IsEmpty%2A> property, in which case the message body is empty and <xref:System.ServiceModel.Channels.Message.GetReaderAtBodyContents%2A> throws an <xref:System.InvalidOperationException>.</span></span> <span data-ttu-id="d75b2-205">Navíc pokud je přijaté zprávy (například odpověď), můžete také zkontrolovat <xref:System.ServiceModel.Channels.Message.IsFault%2A>, což naznačuje, zda zpráva obsahuje chybu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-205">Also, if it is a received message (for example, the reply), you may also want to check <xref:System.ServiceModel.Channels.Message.IsFault%2A>, which indicates whether the message contains a fault.</span></span>  
  
 <span data-ttu-id="d75b2-206">Nejzákladnější přetížení <xref:System.ServiceModel.Channels.Message.GetBody%2A> deserializuje tělo zprávy do instance typu (označené obecný parametr) pomocí <xref:System.Runtime.Serialization.DataContractSerializer> nakonfigurována s výchozím nastavením a s <xref:System.Runtime.Serialization.DataContractSerializer.MaxItemsInObjectGraph%2A> kvóty zakázány.</span><span class="sxs-lookup"><span data-stu-id="d75b2-206">The most basic overload of <xref:System.ServiceModel.Channels.Message.GetBody%2A> deserializes the message body into an instance of a type (indicated by the generic parameter) using a <xref:System.Runtime.Serialization.DataContractSerializer> configured with the default settings and with the <xref:System.Runtime.Serialization.DataContractSerializer.MaxItemsInObjectGraph%2A> quota disabled.</span></span> <span data-ttu-id="d75b2-207">Pokud chcete používat jiný Serializační stroj, nebo můžete nakonfigurovat `DataContractSerializer` způsobem, jiné než výchozí, použijte <xref:System.ServiceModel.Channels.Message.GetBody%2A> přetížení, které přijímá <xref:System.Runtime.Serialization.XmlObjectSerializer>.</span><span class="sxs-lookup"><span data-stu-id="d75b2-207">If you want to use a different serialization engine, or configure the `DataContractSerializer` in a non-default way, use the <xref:System.ServiceModel.Channels.Message.GetBody%2A> overload that takes an <xref:System.Runtime.Serialization.XmlObjectSerializer>.</span></span>  
  
 <span data-ttu-id="d75b2-208">Například následující kód extrahuje data z textu zprávy, která obsahuje serializovaný seznam `Person` objektu a vytiskne na jméno uživatele.</span><span class="sxs-lookup"><span data-stu-id="d75b2-208">For example, the following code extracts data from a message body that contains a serialized `Person` object and prints out the person’s name.</span></span>  
  
 [!code-csharp[C_UsingTheMessageClass#6](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_usingthemessageclass/cs/source.cs#6)]
 [!code-vb[C_UsingTheMessageClass#6](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_usingthemessageclass/vb/source.vb#6)]  
  
## <a name="copying-a-message-into-a-buffer"></a><span data-ttu-id="d75b2-209">Kopírování zprávu do vyrovnávací paměti</span><span class="sxs-lookup"><span data-stu-id="d75b2-209">Copying a Message into a Buffer</span></span>  
 <span data-ttu-id="d75b2-210">Někdy je nezbytné pro přístup k tělo zprávy více než jednou, například, předat dál stejnou zprávu do více cílů v rámci systému vydavatele odběratele.</span><span class="sxs-lookup"><span data-stu-id="d75b2-210">Sometimes it is necessary to access the message body more than once, for example, to forward the same message to multiple destinations as part of a publisher-subscriber system.</span></span> <span data-ttu-id="d75b2-211">V takovém případě je nutná k uložení celé zprávy (včetně textu) v paměti.</span><span class="sxs-lookup"><span data-stu-id="d75b2-211">In this case, it is necessary to buffer the entire message (including the body) in memory.</span></span> <span data-ttu-id="d75b2-212">To provedete pomocí volání <xref:System.ServiceModel.Channels.Message.CreateBufferedCopy%28System.Int32%29>.</span><span class="sxs-lookup"><span data-stu-id="d75b2-212">You can do this by calling <xref:System.ServiceModel.Channels.Message.CreateBufferedCopy%28System.Int32%29>.</span></span> <span data-ttu-id="d75b2-213">Tato metoda přebírá parametr celé číslo, který představuje maximální velikost vyrovnávací paměti a vytvoří není větší než tato velikost vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="d75b2-213">This method takes an integer parameter that represents the maximum buffer size, and creates a buffer not larger than this size.</span></span> <span data-ttu-id="d75b2-214">Je důležité nastavit na hodnotu bezpečné Pokud zpráva pochází z nedůvěryhodného zdroje.</span><span class="sxs-lookup"><span data-stu-id="d75b2-214">It is important to set this to a safe value if the message is coming from an untrusted source.</span></span>  
  
 <span data-ttu-id="d75b2-215">Vyrovnávací paměť se vrátí jako <xref:System.ServiceModel.Channels.MessageBuffer> instance.</span><span class="sxs-lookup"><span data-stu-id="d75b2-215">The buffer is returned as a <xref:System.ServiceModel.Channels.MessageBuffer> instance.</span></span> <span data-ttu-id="d75b2-216">Můžete přistupovat k datům ve vyrovnávací paměti několika způsoby.</span><span class="sxs-lookup"><span data-stu-id="d75b2-216">You can access data in the buffer in several ways.</span></span> <span data-ttu-id="d75b2-217">Primární způsob je volání <xref:System.ServiceModel.Channels.Message.CreateMessage%2A> k vytvoření `Message` instancí z vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="d75b2-217">The primary way is to call <xref:System.ServiceModel.Channels.Message.CreateMessage%2A> to create `Message` instances from the buffer.</span></span>  
  
 <span data-ttu-id="d75b2-218">Jiný způsob, jak získat přístup k datům ve vyrovnávací paměti je implementovat <xref:System.Xml.XPath.IXPathNavigable> rozhraní <xref:System.ServiceModel.Channels.MessageBuffer> třída implementuje pro přístup k podkladové XML přímo.</span><span class="sxs-lookup"><span data-stu-id="d75b2-218">Another way to access the data in the buffer is to implement the <xref:System.Xml.XPath.IXPathNavigable> interface that the <xref:System.ServiceModel.Channels.MessageBuffer> class implements to access the underlying XML directly.</span></span> <span data-ttu-id="d75b2-219">Některé <xref:System.ServiceModel.Channels.MessageBuffer.CreateNavigator%2A> přetížení umožňují vytvářet <xref:System.Xml.XPath> navigátory chráněn uzlu kvótu, omezení počtu uzlů XML, které lze navštívit.</span><span class="sxs-lookup"><span data-stu-id="d75b2-219">Some <xref:System.ServiceModel.Channels.MessageBuffer.CreateNavigator%2A> overloads allow you to create <xref:System.Xml.XPath> navigators protected by a node quota, limiting the number of XML nodes that can be visited.</span></span> <span data-ttu-id="d75b2-220">To pomáhá zabránit útoku DoS založené na delší dobu zpracování.</span><span class="sxs-lookup"><span data-stu-id="d75b2-220">This helps prevent denial of service attacks based on lengthy processing time.</span></span> <span data-ttu-id="d75b2-221">Tuto nabídku ve výchozím nastavení vypnutá.</span><span class="sxs-lookup"><span data-stu-id="d75b2-221">This quote is disabled by default.</span></span> <span data-ttu-id="d75b2-222">Některé `CreateNavigator` přetížení umožňují určit způsob zpracování mezer v kódu XML pomocí <xref:System.Xml.XmlSpace> výčet, s právě výchozí `XmlSpace.None`.</span><span class="sxs-lookup"><span data-stu-id="d75b2-222">Some `CreateNavigator` overloads allow you to specify how white space should be handled in the XML using the <xref:System.Xml.XmlSpace> enumeration, with the default being `XmlSpace.None`.</span></span>  
  
 <span data-ttu-id="d75b2-223">Poslední způsob pro přístup k obsahu zprávy vyrovnávací paměť je zapsat svůj obsah rozbalí do datového proudu pomocí <xref:System.ServiceModel.Channels.Message.WriteMessage%2A>.</span><span class="sxs-lookup"><span data-stu-id="d75b2-223">A final way to access the contents of a message buffer is to write out its contents to a stream using <xref:System.ServiceModel.Channels.Message.WriteMessage%2A>.</span></span>  
  
 <span data-ttu-id="d75b2-224">Následující příklad znázorňuje proces práce `MessageBuffer`: příchozí zprávy předával několika příjemcům a poté protokolována do souboru.</span><span class="sxs-lookup"><span data-stu-id="d75b2-224">The following example demonstrates the process of working with a `MessageBuffer`: an incoming message is forwarded to multiple recipients, and then logged to a file.</span></span> <span data-ttu-id="d75b2-225">Bez ukládání do vyrovnávací paměti, to není možné, protože obsah zprávy lze přistupovat pouze jednou.</span><span class="sxs-lookup"><span data-stu-id="d75b2-225">Without buffering, this is not possible, because the message body can then be accessed only once.</span></span>  
  
 [!code-csharp[C_UsingTheMessageClass#7](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_usingthemessageclass/cs/source.cs#7)]
 [!code-vb[C_UsingTheMessageClass#7](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_usingthemessageclass/vb/source.vb#7)]  
  
 <span data-ttu-id="d75b2-226">`MessageBuffer` Třída má ostatní členové vhodné poznamenat.</span><span class="sxs-lookup"><span data-stu-id="d75b2-226">The `MessageBuffer` class has other members worth noting.</span></span> <span data-ttu-id="d75b2-227"><xref:System.ServiceModel.Channels.MessageBuffer.Close%2A> Metodu lze volat kvůli uvolnění prostředků, pokud vyrovnávací paměti obsah se už nevyžadují.</span><span class="sxs-lookup"><span data-stu-id="d75b2-227">The <xref:System.ServiceModel.Channels.MessageBuffer.Close%2A> method can be called to free resources when the buffer contents are no longer required.</span></span> <span data-ttu-id="d75b2-228"><xref:System.ServiceModel.Channels.MessageBuffer.BufferSize%2A> Vlastnost vrátí velikost přidělené vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="d75b2-228">The <xref:System.ServiceModel.Channels.MessageBuffer.BufferSize%2A> property returns the size of the allocated buffer.</span></span> <span data-ttu-id="d75b2-229"><xref:System.ServiceModel.Channels.MessageBuffer.MessageContentType%2A> Vlastnost vrátí typ MIME obsahu zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-229">The <xref:System.ServiceModel.Channels.MessageBuffer.MessageContentType%2A> property returns the MIME content type of the message.</span></span>  
  
## <a name="accessing-the-message-body-for-debugging"></a><span data-ttu-id="d75b2-230">Přístup k text zprávy pro ladění</span><span class="sxs-lookup"><span data-stu-id="d75b2-230">Accessing the Message Body for Debugging</span></span>  
 <span data-ttu-id="d75b2-231">Pro účely ladění si můžete volat <xref:System.ServiceModel.Channels.Message.ToString%2A> metodu k získání reprezentace zprávy jako řetězec.</span><span class="sxs-lookup"><span data-stu-id="d75b2-231">For debugging purposes, you can call the <xref:System.ServiceModel.Channels.Message.ToString%2A> method to get a representation of the message as a string.</span></span> <span data-ttu-id="d75b2-232">Tento zápis obecně odpovídá zprávu by vzhled přenášený Pokud byly zakódovaných pomocí kodér textu s tím rozdílem, že by lidského čitelnější lépe formátu XML.</span><span class="sxs-lookup"><span data-stu-id="d75b2-232">This representation generally matches the way a message would look on the wire if it were encoded with the text encoder, except that the XML would be better formatted for human readability.</span></span> <span data-ttu-id="d75b2-233">Jedinou výjimkou je text zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-233">The one exception to this is the message body.</span></span> <span data-ttu-id="d75b2-234">Text může číst pouze jednou a `ToString` nezmění stav zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-234">The body can be read only once, and `ToString` does not change the message state.</span></span> <span data-ttu-id="d75b2-235">Proto `ToString` metoda nemusí být schopni přistupovat text a může nahraďte zástupný symbol (například "..." nebo tři tečky) místo textu zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-235">Therefore, the `ToString` method might not be able to access the body and might substitute a placeholder (for example, "…" or three dots) instead of the message body.</span></span> <span data-ttu-id="d75b2-236">Proto nepoužívejte `ToString` k protokolování zpráv, pokud je důležité obsah textu zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-236">Therefore, do not use `ToString` to log messages if the body content of the messages is important.</span></span>  
  
## <a name="accessing-other-message-parts"></a><span data-ttu-id="d75b2-237">Přístup k další části zprávy</span><span class="sxs-lookup"><span data-stu-id="d75b2-237">Accessing Other Message Parts</span></span>  
 <span data-ttu-id="d75b2-238">Různé vlastnosti jsou k dispozici pro přístup k informacím o zprávě, než jeho obsah textu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-238">Various properties are provided to access information about the message other than its body contents.</span></span> <span data-ttu-id="d75b2-239">Ale tyto nelze volat po uzavření zpráva:</span><span class="sxs-lookup"><span data-stu-id="d75b2-239">However, these cannot be called once the message has been closed:</span></span>  
  
-   <span data-ttu-id="d75b2-240"><xref:System.ServiceModel.Channels.Message.Headers%2A> Vlastnost představuje záhlaví zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-240">The <xref:System.ServiceModel.Channels.Message.Headers%2A> property represents the message headers.</span></span> <span data-ttu-id="d75b2-241">Viz oddíl "Práce s hlavičky" dál v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-241">See the section on "Working with Headers" later in this topic.</span></span>  
  
-   <span data-ttu-id="d75b2-242"><xref:System.ServiceModel.Channels.Message.Properties%2A> Vlastnost představuje vlastnosti zprávy, které jsou částí s názvem dat připojeného ke zprávě, která není obecně získat vygenerované při odeslání zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-242">The <xref:System.ServiceModel.Channels.Message.Properties%2A> property represents the message properties, which are pieces of named data attached to the message that do not generally get emitted when the message is sent.</span></span> <span data-ttu-id="d75b2-243">Najdete v části na práci s vlastnosti"dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-243">See the section on "Working with Properties" later in this topic.</span></span>  
  
-   <span data-ttu-id="d75b2-244"><xref:System.ServiceModel.Channels.Message.Version%2A> Vlastnost označuje verzi protokolu SOAP a adresování WS spojený se zprávou, nebo `None` Pokud SOAP je zakázána.</span><span class="sxs-lookup"><span data-stu-id="d75b2-244">The <xref:System.ServiceModel.Channels.Message.Version%2A> property indicates the SOAP and WS-Addressing version associated with the message, or `None` if SOAP is disabled.</span></span>  
  
-   <span data-ttu-id="d75b2-245"><xref:System.ServiceModel.Channels.Message.IsFault%2A> Vlastnost vrátí `true` Pokud zpráva je zprávu o chybě protokolu SOAP.</span><span class="sxs-lookup"><span data-stu-id="d75b2-245">The <xref:System.ServiceModel.Channels.Message.IsFault%2A> property returns `true` if the message is a SOAP fault message.</span></span>  
  
-   <span data-ttu-id="d75b2-246"><xref:System.ServiceModel.Channels.Message.IsEmpty%2A> Vlastnost vrátí `true` Pokud zpráva je prázdná.</span><span class="sxs-lookup"><span data-stu-id="d75b2-246">The <xref:System.ServiceModel.Channels.Message.IsEmpty%2A> property returns `true` if the message is empty.</span></span>  
  
 <span data-ttu-id="d75b2-247">Můžete použít <xref:System.ServiceModel.Channels.Message.GetBodyAttribute%28System.String%2CSystem.String%29> metody přístup konkrétní atribut v prvku body obálku (například `<soap:Body>`) identifikovaný konkrétní názvem a oborem názvů.</span><span class="sxs-lookup"><span data-stu-id="d75b2-247">You can use the <xref:System.ServiceModel.Channels.Message.GetBodyAttribute%28System.String%2CSystem.String%29> method to access a particular attribute on the body wrapper element (for example, `<soap:Body>`) identified by a particular name and namespace.</span></span> <span data-ttu-id="d75b2-248">Pokud není nalezen takový atribut, `null` je vrácen.</span><span class="sxs-lookup"><span data-stu-id="d75b2-248">If such an attribute is not found, `null` is returned.</span></span> <span data-ttu-id="d75b2-249">Tuto metodu lze volat pouze tehdy, když `Message` je ve stavu vytvořen (Pokud je text zprávy nebyl ještě využívány).</span><span class="sxs-lookup"><span data-stu-id="d75b2-249">This method can be called only when the `Message` is in the Created state (when the message body has not yet been accessed).</span></span>  
  
## <a name="working-with-headers"></a><span data-ttu-id="d75b2-250">Práce s hlavičky</span><span class="sxs-lookup"><span data-stu-id="d75b2-250">Working with Headers</span></span>  
 <span data-ttu-id="d75b2-251">A `Message` může obsahovat libovolný počet pojmenované fragmenty XML názvem *hlavičky*.</span><span class="sxs-lookup"><span data-stu-id="d75b2-251">A `Message` can contain any number of named XML fragments, called *headers*.</span></span> <span data-ttu-id="d75b2-252">Každý fragment obvykle mapuje hlavičku protokolu SOAP.</span><span class="sxs-lookup"><span data-stu-id="d75b2-252">Each fragment normally maps to a SOAP header.</span></span> <span data-ttu-id="d75b2-253">Hlavičky jsou přístupné prostřednictvím `Headers` vlastnost typu <xref:System.ServiceModel.Channels.MessageHeaders>.</span><span class="sxs-lookup"><span data-stu-id="d75b2-253">Headers are accessed through the `Headers` property of type <xref:System.ServiceModel.Channels.MessageHeaders>.</span></span> <span data-ttu-id="d75b2-254"><xref:System.ServiceModel.Channels.MessageHeaders>je kolekce <xref:System.ServiceModel.Channels.MessageHeaderInfo> objekty a jednotlivých záhlaví je přístupná přes jeho <xref:System.Collections.IEnumerable> rozhraní nebo pomocí jeho indexer.</span><span class="sxs-lookup"><span data-stu-id="d75b2-254"><xref:System.ServiceModel.Channels.MessageHeaders> is a collection of <xref:System.ServiceModel.Channels.MessageHeaderInfo> objects, and individual headers can be accessed through its <xref:System.Collections.IEnumerable> interface or through its indexer.</span></span> <span data-ttu-id="d75b2-255">Například následující kód obsahuje názvy všech záhlaví v `Message`.</span><span class="sxs-lookup"><span data-stu-id="d75b2-255">For example, the following code lists the names of all the headers in a `Message`.</span></span>  
  
 [!code-csharp[C_UsingTheMessageClass#8](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_usingthemessageclass/cs/source.cs#8)]
 [!code-vb[C_UsingTheMessageClass#8](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_usingthemessageclass/vb/source.vb#8)]  
  
#### <a name="adding-removing-finding-headers"></a><span data-ttu-id="d75b2-256">Přidání, odebrání, hledání hlavičky</span><span class="sxs-lookup"><span data-stu-id="d75b2-256">Adding, Removing, Finding Headers</span></span>  
 <span data-ttu-id="d75b2-257">Můžete přidat nové hlavičku na konci všechny existující hlavičky pomocí <xref:System.ServiceModel.Channels.MessageHeaders.Add%2A> metoda.</span><span class="sxs-lookup"><span data-stu-id="d75b2-257">You can add a new header at the end of all existing headers using the <xref:System.ServiceModel.Channels.MessageHeaders.Add%2A> method.</span></span> <span data-ttu-id="d75b2-258">Můžete použít <xref:System.ServiceModel.Channels.MessageHeaders.Insert%2A> metoda vložte hlavičku na konkrétním indexem.</span><span class="sxs-lookup"><span data-stu-id="d75b2-258">You can use the <xref:System.ServiceModel.Channels.MessageHeaders.Insert%2A> method to insert a header at a particular index.</span></span> <span data-ttu-id="d75b2-259">Existující záhlaví posunuty vložené položky.</span><span class="sxs-lookup"><span data-stu-id="d75b2-259">Existing headers are shifted for the inserted item.</span></span> <span data-ttu-id="d75b2-260">Hlavičky jsou seřazené podle svého indexu a index prvního k dispozici je 0.</span><span class="sxs-lookup"><span data-stu-id="d75b2-260">Headers are ordered according to their index, and the first available index is 0.</span></span> <span data-ttu-id="d75b2-261">Můžete použít různé <xref:System.ServiceModel.Channels.MessageHeaders.CopyHeadersFrom%2A> přetížení metody pro přidání hlavičky z jiné `Message` nebo `MessageHeaders` instance.</span><span class="sxs-lookup"><span data-stu-id="d75b2-261">You can use the various <xref:System.ServiceModel.Channels.MessageHeaders.CopyHeadersFrom%2A> method overloads to add headers from a different `Message` or `MessageHeaders` instance.</span></span> <span data-ttu-id="d75b2-262">Některé přetížení zkopírovat jeden jednotlivých záhlaví, zatímco ostatní zkopírujte všechny z nich.</span><span class="sxs-lookup"><span data-stu-id="d75b2-262">Some overloads copy one individual header, while others copy all of them.</span></span> <span data-ttu-id="d75b2-263"><xref:System.ServiceModel.Channels.MessageHeaders.Clear%2A> Metoda odebere všechny hlavičky.</span><span class="sxs-lookup"><span data-stu-id="d75b2-263">The <xref:System.ServiceModel.Channels.MessageHeaders.Clear%2A> method removes all headers.</span></span> <span data-ttu-id="d75b2-264"><xref:System.ServiceModel.Channels.MessageHeaders.RemoveAt%2A> Metoda odebere hlaviček v konkrétním indexem (s posunem všechny hlavičky po jeho).</span><span class="sxs-lookup"><span data-stu-id="d75b2-264">The <xref:System.ServiceModel.Channels.MessageHeaders.RemoveAt%2A> method removes a header at a particular index (shifting all headers after it).</span></span> <span data-ttu-id="d75b2-265"><xref:System.ServiceModel.Channels.MessageHeaders.RemoveAll%2A> Metoda odebere všechny hlavičky s určitým názvem a oborem názvů.</span><span class="sxs-lookup"><span data-stu-id="d75b2-265">The <xref:System.ServiceModel.Channels.MessageHeaders.RemoveAll%2A> method removes all headers with a particular name and namespace.</span></span>  
  
 <span data-ttu-id="d75b2-266">Načíst konkrétní hlavičky pomocí <xref:System.ServiceModel.Channels.MessageHeaders.FindHeader%2A> metoda.</span><span class="sxs-lookup"><span data-stu-id="d75b2-266">Retrieve a particular header using the <xref:System.ServiceModel.Channels.MessageHeaders.FindHeader%2A> method.</span></span> <span data-ttu-id="d75b2-267">Tato metoda přebírá názvem a oborem názvů hlavičky k najít a vrátí její index.</span><span class="sxs-lookup"><span data-stu-id="d75b2-267">This method takes the name and namespace of the header to find, and returns its index.</span></span> <span data-ttu-id="d75b2-268">Pokud hlavička vyskytuje více než jednou, je vyvolána výjimka.</span><span class="sxs-lookup"><span data-stu-id="d75b2-268">If the header occurs more than once, an exception is thrown.</span></span> <span data-ttu-id="d75b2-269">Pokud hlavička nebyla nalezena, vrátí hodnotu -1.</span><span class="sxs-lookup"><span data-stu-id="d75b2-269">If the header is not found, it returns -1.</span></span>  
  
 <span data-ttu-id="d75b2-270">V modelu hlavičky SOAP, může mít hlavičky `Actor` hodnotu, která určuje zamýšlený příjemce hlavičky.</span><span class="sxs-lookup"><span data-stu-id="d75b2-270">In the SOAP header model, headers can have an `Actor` value that specifies the intended recipient of the header.</span></span> <span data-ttu-id="d75b2-271">Nejzákladnější `FindHeader` přetížení vyhledá pouze záhlaví určený pro ultimate příjemce zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-271">The most basic `FindHeader` overload searches only headers intended for the ultimate receiver of the message.</span></span> <span data-ttu-id="d75b2-272">Ale jiné přetížení umožňuje určit, které `Actor` hodnoty jsou zahrnuty v hledání.</span><span class="sxs-lookup"><span data-stu-id="d75b2-272">However, another overload enables you to specify which `Actor` values are included in the search.</span></span> [!INCLUDE[crdefault](../../../../includes/crdefault-md.md)]<span data-ttu-id="d75b2-273">Specifikace protokolu SOAP.</span><span class="sxs-lookup"><span data-stu-id="d75b2-273"> the SOAP specification.</span></span>  
  
 <span data-ttu-id="d75b2-274">A <xref:System.ServiceModel.Channels.MessageHeaders.CopyTo%28System.ServiceModel.Channels.MessageHeaderInfo%5B%5D%2CSystem.Int32%29> metoda zajišťuje zkopírujte hlaviček z <xref:System.ServiceModel.Channels.MessageHeaders> kolekci na pole <xref:System.ServiceModel.Channels.MessageHeaderInfo> objekty.</span><span class="sxs-lookup"><span data-stu-id="d75b2-274">A <xref:System.ServiceModel.Channels.MessageHeaders.CopyTo%28System.ServiceModel.Channels.MessageHeaderInfo%5B%5D%2CSystem.Int32%29> method is provided to copy headers from a <xref:System.ServiceModel.Channels.MessageHeaders> collection to an array of <xref:System.ServiceModel.Channels.MessageHeaderInfo> objects.</span></span>  
  
 <span data-ttu-id="d75b2-275">Přístup k datům XML v hlavičce můžete volat <xref:System.ServiceModel.Channels.MessageHeaders.GetReaderAtHeader%2A> a vrátit čtečky XML pro konkrétní hlavičky index.</span><span class="sxs-lookup"><span data-stu-id="d75b2-275">To access the XML data in a header, you can call <xref:System.ServiceModel.Channels.MessageHeaders.GetReaderAtHeader%2A> and return an XML reader for the specific header index.</span></span> <span data-ttu-id="d75b2-276">Pokud chcete k deserializaci obsahu hlavičky do objektu, použijte <xref:System.ServiceModel.Channels.MessageHeaders.GetHeader%60%601%28System.Int32%29> nebo jeden z dalšími přetíženími.</span><span class="sxs-lookup"><span data-stu-id="d75b2-276">If you want to deserialize the header contents into an object, use <xref:System.ServiceModel.Channels.MessageHeaders.GetHeader%60%601%28System.Int32%29> or one of the other overloads.</span></span> <span data-ttu-id="d75b2-277">Nejzákladnější přetížení deserializovat hlavičky pomocí <xref:System.Runtime.Serialization.DataContractSerializer> nakonfigurované ve výchozím způsobem.</span><span class="sxs-lookup"><span data-stu-id="d75b2-277">The most basic overloads deserialize headers using the <xref:System.Runtime.Serialization.DataContractSerializer> configured in the default way.</span></span> <span data-ttu-id="d75b2-278">Pokud chcete použít jiný serializátor nebo jiné konfigurace systému `DataContractSerializer`, použijte jednu z přetížení, které provést `XmlObjectSerializer`.</span><span class="sxs-lookup"><span data-stu-id="d75b2-278">If you want to use a different serializer or a different configuration of the `DataContractSerializer`, use one of the overloads that take an `XmlObjectSerializer`.</span></span> <span data-ttu-id="d75b2-279">Existují také přetížení, které provést název hlavičky, obor názvů a volitelně seznam `Actor` hodnoty místo index; to je kombinací `FindHeader` a `GetHeader`.</span><span class="sxs-lookup"><span data-stu-id="d75b2-279">There are also overloads that take the header name, namespace, and optionally a list of `Actor` values instead of an index; this is a combination of `FindHeader` and `GetHeader`.</span></span>  
  
## <a name="working-with-properties"></a><span data-ttu-id="d75b2-280">Práce s vlastnostmi</span><span class="sxs-lookup"><span data-stu-id="d75b2-280">Working with Properties</span></span>  
 <span data-ttu-id="d75b2-281">A `Message` instance může obsahovat libovolný počet pojmenovaných objektů náhodné typy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-281">A `Message` instance can contain an arbitrary number of named objects of arbitrary types.</span></span> <span data-ttu-id="d75b2-282">Tato kolekce je přístupné přes `Properties` vlastnost typu `MessageProperties`.</span><span class="sxs-lookup"><span data-stu-id="d75b2-282">This collection is accessed through the `Properties` property of type `MessageProperties`.</span></span> <span data-ttu-id="d75b2-283">Implementuje kolekce <xref:System.Collections.Generic.IDictionary%602> rozhraní a funguje jako mapování z <xref:System.String> k <xref:System.Object>.</span><span class="sxs-lookup"><span data-stu-id="d75b2-283">The collection implements the <xref:System.Collections.Generic.IDictionary%602> interface and acts as a mapping from <xref:System.String> to <xref:System.Object>.</span></span> <span data-ttu-id="d75b2-284">Za normálních okolností se nemapují přímo na jakékoliv části zprávy v drátové síti hodnoty vlastností, ale spíš poskytovat různé zpráva zpracování pro různé kanály v [!INCLUDE[indigo2](../../../../includes/indigo2-md.md)] kanál zásobníku nebo <xref:System.ServiceModel.Channels.MessageHeaders.CopyTo%28System.ServiceModel.Channels.MessageHeaderInfo%5B%5D%2CSystem.Int32%29> architektura služby.</span><span class="sxs-lookup"><span data-stu-id="d75b2-284">Normally, property values do not map directly to any part of the message on the wire, but rather provide various message processing hints to the various channels in the [!INCLUDE[indigo2](../../../../includes/indigo2-md.md)] channel stack or to the <xref:System.ServiceModel.Channels.MessageHeaders.CopyTo%28System.ServiceModel.Channels.MessageHeaderInfo%5B%5D%2CSystem.Int32%29> service framework.</span></span> <span data-ttu-id="d75b2-285">Příklad, naleznete v části [architektury Přehled přenosu dat](../../../../docs/framework/wcf/feature-details/data-transfer-architectural-overview.md).</span><span class="sxs-lookup"><span data-stu-id="d75b2-285">For an example, see [Data Transfer Architectural Overview](../../../../docs/framework/wcf/feature-details/data-transfer-architectural-overview.md).</span></span>  
  
## <a name="inheriting-from-the-message-class"></a><span data-ttu-id="d75b2-286">Která dědí z třídy Message</span><span class="sxs-lookup"><span data-stu-id="d75b2-286">Inheriting from the Message Class</span></span>  
 <span data-ttu-id="d75b2-287">Pokud integrované zpráv typy vytvořené pomocí `CreateMessage` není vyhovovalo vašim požadavkům, vytvořte třídu, která je odvozena z `Message` třídy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-287">If the built-in message types created using `CreateMessage` do not meet your requirements, create a class that derives from the `Message` class.</span></span>  
  
### <a name="defining-the-message-body-contents"></a><span data-ttu-id="d75b2-288">Definování obsah zprávy</span><span class="sxs-lookup"><span data-stu-id="d75b2-288">Defining the Message Body Contents</span></span>  
 <span data-ttu-id="d75b2-289">Existují tři primární techniky pro přístup k datům v rámci tělo zprávy: zápis, čtení a kopírování do vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="d75b2-289">Three primary techniques exist for accessing data within a message body: writing, reading, and copying it to a buffer.</span></span> <span data-ttu-id="d75b2-290">Tyto operace konečným výsledkem <xref:System.ServiceModel.Channels.Message.OnWriteBodyContents%2A>, <xref:System.ServiceModel.Channels.Message.OnGetReaderAtBodyContents%2A>, a <xref:System.ServiceModel.Channels.Message.OnCreateBufferedCopy%2A> metody volané, v uvedeném pořadí, v odvozené třídě z `Message`.</span><span class="sxs-lookup"><span data-stu-id="d75b2-290">These operations ultimately result in the <xref:System.ServiceModel.Channels.Message.OnWriteBodyContents%2A>, <xref:System.ServiceModel.Channels.Message.OnGetReaderAtBodyContents%2A>, and <xref:System.ServiceModel.Channels.Message.OnCreateBufferedCopy%2A> methods being called, respectively, on your derived class of `Message`.</span></span> <span data-ttu-id="d75b2-291">Základní `Message` třída zaručuje, že pouze jeden z těchto metod je volána pro každou `Message` instance, a že není volaná víc než jednou.</span><span class="sxs-lookup"><span data-stu-id="d75b2-291">The base `Message` class guarantees that only one of these methods is called for each `Message` instance, and that it is not called more than once.</span></span> <span data-ttu-id="d75b2-292">Základní třídy taky zajišťuje, že metody nejsou zavolána pro zprávu uzavřené.</span><span class="sxs-lookup"><span data-stu-id="d75b2-292">The base class also ensures that the methods are not called on a closed message.</span></span> <span data-ttu-id="d75b2-293">Není nutné sledovat stav zpráv v implementaci.</span><span class="sxs-lookup"><span data-stu-id="d75b2-293">There is no need to track the message state in your implementation.</span></span>  
  
 <span data-ttu-id="d75b2-294"><xref:System.ServiceModel.Channels.Message.OnWriteBodyContents%2A>je abstraktní metodu a musí být implementována.</span><span class="sxs-lookup"><span data-stu-id="d75b2-294"><xref:System.ServiceModel.Channels.Message.OnWriteBodyContents%2A> is an abstract method and must be implemented.</span></span> <span data-ttu-id="d75b2-295">Zápis pomocí této metody je nejzákladnější způsob, jak definovat obsah textu zprávy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-295">The most basic way to define the body contents of your message is to write using this method.</span></span> <span data-ttu-id="d75b2-296">Například následující zpráva obsahuje 100 000 náhodných čísel od 1 až 20 číslic.</span><span class="sxs-lookup"><span data-stu-id="d75b2-296">For example, the following message contains 100,000 random numbers from 1 to 20.</span></span>  
  
 [!code-csharp[C_UsingTheMessageClass#9](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_usingthemessageclass/cs/source.cs#9)]
 [!code-vb[C_UsingTheMessageClass#9](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_usingthemessageclass/vb/source.vb#9)]  
  
 <span data-ttu-id="d75b2-297">`OnGetReaderAtBodyContents` a `OnCreateBufferedCopy` mají výchozí implementaci, která pracují pro většině případů.</span><span class="sxs-lookup"><span data-stu-id="d75b2-297">The `OnGetReaderAtBodyContents` and `OnCreateBufferedCopy` methods have default implementations that work for most cases.</span></span> <span data-ttu-id="d75b2-298">Výchozí implementace volání `OnWriteBodyContents`, výsledky ukládat do vyrovnávací paměti a pracovat s výsledné vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="d75b2-298">The default implementations call `OnWriteBodyContents`, buffer the results, and work with the resulting buffer.</span></span> <span data-ttu-id="d75b2-299">Ale v některých případech to nemusí stačit.</span><span class="sxs-lookup"><span data-stu-id="d75b2-299">However, in some cases this may not be enough.</span></span> <span data-ttu-id="d75b2-300">V předchozím příkladu čtení zpráv výsledky v 100 000 elementů XML se uloží do vyrovnávací paměti, které nemusí být žádoucí.</span><span class="sxs-lookup"><span data-stu-id="d75b2-300">In the preceding example, reading the message results in 100,000 XML elements being buffered, which might not be desirable.</span></span> <span data-ttu-id="d75b2-301">Chcete přepsat `OnGetReaderAtBodyContents` vrátit vlastní `XmlDictionaryReader` odvozené třídy, která slouží až náhodných čísel.</span><span class="sxs-lookup"><span data-stu-id="d75b2-301">You might want to override `OnGetReaderAtBodyContents` to return a custom `XmlDictionaryReader` derived class that serves up random numbers.</span></span> <span data-ttu-id="d75b2-302">Potom můžete přepsat `OnWriteBodyContents` použít program pro čtení, `OnGetReaderAtBodyContents` vlastnost vrátí, jak je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-302">You can then override `OnWriteBodyContents` to use the reader that the `OnGetReaderAtBodyContents` property returns, as shown in the following example.</span></span>  
  
 [!code-csharp[C_UsingTheMessageClass#10](../../../../samples/snippets/csharp/VS_Snippets_CFX/c_usingthemessageclass/cs/source.cs#10)]
 [!code-vb[C_UsingTheMessageClass#10](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_usingthemessageclass/vb/source.vb#10)]  
  
 <span data-ttu-id="d75b2-303">Podobně můžete chtít přepsat `OnCreateBufferedCopy` vrátit vlastní `MessageBuffer` odvozené třídy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-303">Similarly, you might want to override `OnCreateBufferedCopy` to return your own `MessageBuffer` derived class.</span></span>  
  
 <span data-ttu-id="d75b2-304">Kromě obsah textu zprávy, musí vaše zpráva odvozené třídy také přepsat `Version`, `Headers`, a `Properties` vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="d75b2-304">In addition to providing message body contents, your message derived class must also override the `Version`, `Headers`, and `Properties` properties.</span></span>  
  
 <span data-ttu-id="d75b2-305">Všimněte si, že pokud vytvoříte kopii zprávy, kopie používá záhlaví zprávy z původní.</span><span class="sxs-lookup"><span data-stu-id="d75b2-305">Note that if you create a copy of a message, the copy uses the message headers from the original.</span></span>  
  
### <a name="other-members-that-can-be-overridden"></a><span data-ttu-id="d75b2-306">Jiní členové, které je možné přepsat</span><span class="sxs-lookup"><span data-stu-id="d75b2-306">Other Members that Can Be Overridden</span></span>  
 <span data-ttu-id="d75b2-307">Je možné přepsat <xref:System.ServiceModel.Channels.Message.OnWriteStartEnvelope%2A>, <xref:System.ServiceModel.Channels.Message.OnWriteStartHeaders%2A>, a <xref:System.ServiceModel.Channels.Message.OnWriteStartBody%2A> metody k určení, jak obálky protokolu SOAP, hlavičky SOAP a SOAP body element počáteční značky se zapisují. To obvykle odpovídají `<soap:Envelope>`, `<soap:Header>`, a `<soap:Body>`.</span><span class="sxs-lookup"><span data-stu-id="d75b2-307">You can override the <xref:System.ServiceModel.Channels.Message.OnWriteStartEnvelope%2A>, <xref:System.ServiceModel.Channels.Message.OnWriteStartHeaders%2A>, and <xref:System.ServiceModel.Channels.Message.OnWriteStartBody%2A> methods to specify how the SOAP envelope, SOAP headers, and SOAP body element start tags are written out. These normally correspond to `<soap:Envelope>`, `<soap:Header>`, and `<soap:Body>`.</span></span> <span data-ttu-id="d75b2-308">Tyto metody by měl obvykle není nic se v případě zápisu `Version` vlastnost vrátí `MessageVersion.None`.</span><span class="sxs-lookup"><span data-stu-id="d75b2-308">These methods should normally not write anything out if the `Version` property returns `MessageVersion.None`.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="d75b2-309">Výchozí implementaci `OnGetReaderAtBodyContents` volání `OnWriteStartEnvelope` a `OnWriteStartBody` před voláním `OnWriteBodyContents` a ukládání do vyrovnávací paměti výsledky.</span><span class="sxs-lookup"><span data-stu-id="d75b2-309">The default implementation of `OnGetReaderAtBodyContents` calls `OnWriteStartEnvelope` and `OnWriteStartBody` before calling `OnWriteBodyContents` and buffering the results.</span></span> <span data-ttu-id="d75b2-310">Hlavičky nejsou zapsána.</span><span class="sxs-lookup"><span data-stu-id="d75b2-310">Headers are not written out.</span></span>  
  
 <span data-ttu-id="d75b2-311">Přepsání <xref:System.ServiceModel.Channels.Message.OnWriteMessage%2A> metoda změnit způsob celé zprávy je vytvořený z jeho různých místech.</span><span class="sxs-lookup"><span data-stu-id="d75b2-311">Override the <xref:System.ServiceModel.Channels.Message.OnWriteMessage%2A> method to change the way the entire message is constructed from its various pieces.</span></span> <span data-ttu-id="d75b2-312">`OnWriteMessage` Metoda je volána z <xref:System.ServiceModel.Channels.Message.WriteMessage%2A> a z výchozího <xref:System.ServiceModel.Channels.Message.OnCreateBufferedCopy%2A> implementace.</span><span class="sxs-lookup"><span data-stu-id="d75b2-312">The `OnWriteMessage` method is called from <xref:System.ServiceModel.Channels.Message.WriteMessage%2A> and from the default <xref:System.ServiceModel.Channels.Message.OnCreateBufferedCopy%2A> implementation.</span></span> <span data-ttu-id="d75b2-313">Všimněte si, že přepsání <xref:System.ServiceModel.Channels.Message.WriteMessage%2A> se neshoduje osvědčenými postupy.</span><span class="sxs-lookup"><span data-stu-id="d75b2-313">Note that overriding <xref:System.ServiceModel.Channels.Message.WriteMessage%2A> is not a best practice.</span></span> <span data-ttu-id="d75b2-314">Je lepší pro přepsání odpovídající `On` metody (například <xref:System.ServiceModel.Channels.Message.OnWriteStartEnvelope%2A>, <xref:System.ServiceModel.Channels.Message.OnWriteStartHeaders%2A>, a <xref:System.ServiceModel.Channels.BodyWriter.OnWriteBodyContents%2A>.</span><span class="sxs-lookup"><span data-stu-id="d75b2-314">It is better to override the appropriate `On` methods (for example, <xref:System.ServiceModel.Channels.Message.OnWriteStartEnvelope%2A>, <xref:System.ServiceModel.Channels.Message.OnWriteStartHeaders%2A>, and <xref:System.ServiceModel.Channels.BodyWriter.OnWriteBodyContents%2A>.</span></span>  
  
 <span data-ttu-id="d75b2-315">Přepsání <xref:System.ServiceModel.Channels.Message.OnBodyToString%2A> přepsat, jak vaše text zprávy je reprezentována během ladění.</span><span class="sxs-lookup"><span data-stu-id="d75b2-315">Override <xref:System.ServiceModel.Channels.Message.OnBodyToString%2A> to override how your message body is represented during debugging.</span></span> <span data-ttu-id="d75b2-316">Ve výchozím nastavení se představují ho jako tři tečky ("...").</span><span class="sxs-lookup"><span data-stu-id="d75b2-316">The default is to represent it as three dots ("…").</span></span> <span data-ttu-id="d75b2-317">Všimněte si, že tuto metodu lze volat vícekrát Pokud zpráva stav jakoukoli jinou hodnotu než uzavřeno.</span><span class="sxs-lookup"><span data-stu-id="d75b2-317">Note that this method can be called multiple times when the message state is anything other than Closed.</span></span> <span data-ttu-id="d75b2-318">Implementace této metody by nikdy způsobit žádnou akci, kterou je nutné provést pouze jednou (např. čtení z datového proudu dopředné).</span><span class="sxs-lookup"><span data-stu-id="d75b2-318">An implementation of this method should never cause any action that must be performed only once (such as reading from a forward-only stream).</span></span>  
  
 <span data-ttu-id="d75b2-319">Přepsání <xref:System.ServiceModel.Channels.Message.OnGetBodyAttribute%2A> metoda pro povolení přístupu k atributy elementu těla protokolu SOAP.</span><span class="sxs-lookup"><span data-stu-id="d75b2-319">Override the <xref:System.ServiceModel.Channels.Message.OnGetBodyAttribute%2A> method to allow access to attributes on the SOAP body element.</span></span> <span data-ttu-id="d75b2-320">Tuto metodu lze volat libovolný počet dobu, ale `Message` základní typ zaručuje, že je pouze volána při zprávy je ve stavu vytvořen.</span><span class="sxs-lookup"><span data-stu-id="d75b2-320">This method can be called any number of times, but the `Message` base type guarantees that it is only called when the message is in the Created state.</span></span> <span data-ttu-id="d75b2-321">Není potřeba pro kontrolu stavu v implementaci.</span><span class="sxs-lookup"><span data-stu-id="d75b2-321">It is not required to check the state in an implementation.</span></span> <span data-ttu-id="d75b2-322">Výchozí implementace vždy vrátí `null`, což naznačuje, že neexistují žádné atributy v textu elementu.</span><span class="sxs-lookup"><span data-stu-id="d75b2-322">The default implementation always returns `null`, which indicates that there are no attributes on the body element.</span></span>  
  
 <span data-ttu-id="d75b2-323">Pokud vaše `Message` objekt nutné provést jakékoli speciální čištění, když obsah zprávy se už nevyžaduje, můžete přepsat <xref:System.ServiceModel.Channels.Message.OnClose%2A>.</span><span class="sxs-lookup"><span data-stu-id="d75b2-323">If your `Message` object must do any special cleanup when the message body is no longer required, you can override <xref:System.ServiceModel.Channels.Message.OnClose%2A>.</span></span> <span data-ttu-id="d75b2-324">Výchozí implementace neprovede žádnou akci.</span><span class="sxs-lookup"><span data-stu-id="d75b2-324">The default implementation does nothing.</span></span>  
  
 <span data-ttu-id="d75b2-325">`IsEmpty` a `IsFault` vlastnosti je možné přepsat.</span><span class="sxs-lookup"><span data-stu-id="d75b2-325">The `IsEmpty` and `IsFault` properties can be overridden.</span></span> <span data-ttu-id="d75b2-326">Ve výchozím nastavení, jak vrátit `false`.</span><span class="sxs-lookup"><span data-stu-id="d75b2-326">By default, both return `false`.</span></span>
