---
title: Vývoj kanálu
ms.date: 03/30/2017
helpviewer_keywords:
- add-in pipeline [.NET Framework], segments
- activation path for add-ins [.NET Framework]
- add-in pipeline [.NET Framework], activation path
- communication pipeline for add-ins [.NET Framework]
- add-in pipeline [.NET Framework], about
- add-ins [.NET Framework], pipeline development
ms.assetid: 932788f2-b87d-44cf-82f9-04492a8b2722
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 047cd7a2b8a6d315c6cadb9b535b84f744fd2d09
ms.sourcegitcommit: 2eceb05f1a5bb261291a1f6a91c5153727ac1c19
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/04/2018
ms.locfileid: "43504882"
---
# <a name="pipeline-development"></a><span data-ttu-id="cd546-102">Vývoj kanálu</span><span class="sxs-lookup"><span data-stu-id="cd546-102">Pipeline Development</span></span>
<span data-ttu-id="cd546-103">Kanál doplňku je cesta segmentů kanálu, které hostitelská aplikace a jeho doplňků musí používat ke komunikaci mezi sebou.</span><span class="sxs-lookup"><span data-stu-id="cd546-103">The add-in pipeline is the path of pipeline segments that the host application and its add-in must use to communicate with each other.</span></span>  
  
 <span data-ttu-id="cd546-104">Následující obrázek znázorňuje komunikační kanál a jeho segmentů.</span><span class="sxs-lookup"><span data-stu-id="cd546-104">The following illustration shows the communication pipeline and its segments.</span></span>  
  
 <span data-ttu-id="cd546-105">![Přidat&#45;v kanálu modelu. ](../../../docs/framework/add-ins/media/addin1.png "AddIn1")</span><span class="sxs-lookup"><span data-stu-id="cd546-105">![Add&#45;in pipeline model.](../../../docs/framework/add-ins/media/addin1.png "AddIn1")</span></span>  
<span data-ttu-id="cd546-106">Kanál doplňku</span><span class="sxs-lookup"><span data-stu-id="cd546-106">Add-in pipeline</span></span>  
  
 <span data-ttu-id="cd546-107">Hostitelská aplikace je na jednom konci kanálu a doplňku je na druhém konci.</span><span class="sxs-lookup"><span data-stu-id="cd546-107">The host application is at one end of the pipeline and the add-in is at the other end.</span></span> <span data-ttu-id="cd546-108">Počínaje každý konec a přesun směrem do středu, hostitelské aplikace i doplňku mají abstraktní základní třídu, která definuje zobrazení objektu modelu, který ale i sdílet.</span><span class="sxs-lookup"><span data-stu-id="cd546-108">Starting from each end and moving toward the middle, both the host application and the add-in have an abstract base class that defines a view of the object model that they both share.</span></span> <span data-ttu-id="cd546-109">Tyto typy (třídy) tvoří segment kanálu doplňku zobrazení a zobrazení hostitele doplňku kanálu segmentu.</span><span class="sxs-lookup"><span data-stu-id="cd546-109">These types (classes) make up the add-in view pipeline segment and the host view of the add-in pipeline segment.</span></span> <span data-ttu-id="cd546-110">Segment kanálu doplňku zobrazení často obsahuje více než jednu abstraktní třídu, ale třída, která zdědí doplněk se označuje jako základ doplňku.</span><span class="sxs-lookup"><span data-stu-id="cd546-110">The add-in view pipeline segment often contains more than one abstract class but the class that the add-in inherits from is known as the add-in base.</span></span>  
  
 <span data-ttu-id="cd546-111">Segment kanálu adaptér přidat v na straně a převést segment kanálu adaptér hostitelské tok typy mezi jejich zobrazení segmentů kanálu a segment kanálu kontraktu.</span><span class="sxs-lookup"><span data-stu-id="cd546-111">The add-in-side adapter pipeline segment and the host-side adapter pipeline segment convert the flow of types between their view pipeline segments and the contract pipeline segment.</span></span> <span data-ttu-id="cd546-112">Centrální segment kanálu je kontrakt, který je odvozen z <xref:System.AddIn.Contract.IContract> rozhraní.</span><span class="sxs-lookup"><span data-stu-id="cd546-112">The central segment of the pipeline is a contract that is derived from the <xref:System.AddIn.Contract.IContract> interface.</span></span> <span data-ttu-id="cd546-113">Tento kontrakt definuje metody, které hostitelskou aplikaci a její doplňku se používají.</span><span class="sxs-lookup"><span data-stu-id="cd546-113">This contract defines the methods that the host application and its add-in will both use.</span></span>  
  
 <span data-ttu-id="cd546-114">Pokud načtete hostitele a doplňku na samostatnou aplikační domény, je nutné oddělovací hranice, který odděluje oboru hostitelskou aplikaci z oboru add-in.</span><span class="sxs-lookup"><span data-stu-id="cd546-114">If you load the host and the add-in into separate application domains, you have an isolation boundary that separates the scope of the host application from the scope of the add-in.</span></span> <span data-ttu-id="cd546-115">Smlouva je pouze sestavení, který je načten do domény aplikace doplňku i hostitele.</span><span class="sxs-lookup"><span data-stu-id="cd546-115">The contract is the only assembly that is loaded in both the host and add-in application domains.</span></span> <span data-ttu-id="cd546-116">Hostitele a doplněk každý odkazovat pouze zobrazení svého kontraktu metody.</span><span class="sxs-lookup"><span data-stu-id="cd546-116">The host and the add-in each refer only to their view of the contract methods.</span></span> <span data-ttu-id="cd546-117">Proto jsou odděleny abstrakční ze smlouvy.</span><span class="sxs-lookup"><span data-stu-id="cd546-117">Therefore, they are separated by a layer of abstraction from the contract.</span></span>  
  
 <span data-ttu-id="cd546-118">K vývoji segmentů kanálu, musíte vytvořit adresářovou strukturu, která bude obsahovat.</span><span class="sxs-lookup"><span data-stu-id="cd546-118">To develop pipeline segments, you must create a directory structure that will contain them.</span></span> <span data-ttu-id="cd546-119">Další informace o požadavky na vývoj a pokyny pro obor, naleznete v tématu [požadavky na vývoj kanálu](https://msdn.microsoft.com/library/ef9fa986-e80b-43e1-868b-247f4c1d9da5).</span><span class="sxs-lookup"><span data-stu-id="cd546-119">For more information about development requirements and scope guidelines, see [Pipeline Development Requirements](https://msdn.microsoft.com/library/ef9fa986-e80b-43e1-868b-247f4c1d9da5).</span></span>  
  
 <span data-ttu-id="cd546-120">Následující obrázek znázorňuje typy, které tvoří segmentů kanálu.</span><span class="sxs-lookup"><span data-stu-id="cd546-120">The following illustration shows the types that make up the pipeline segments.</span></span> <span data-ttu-id="cd546-121">Názvy typů je znázorněno na obrázku jsou libovolného, ale všechny typy s výjimkou hostitele a hostitele zobrazení atributů doplněk vyžadovat, aby mohla být zjištěna metodami, které vytvořit úložiště informace.</span><span class="sxs-lookup"><span data-stu-id="cd546-121">The names of the types shown in the illustration are arbitrary, but all types except for the host and the host view of the add-in require attributes so they can be discovered by methods that construct an information store.</span></span>  
  
 <span data-ttu-id="cd546-122">![Přidat&#45;v modelu s povinné atributy u typů. ](../../../docs/framework/add-ins/media/addin-model.png "AddIn_Model")</span><span class="sxs-lookup"><span data-stu-id="cd546-122">![Add&#45;in model with required attributes on types.](../../../docs/framework/add-ins/media/addin-model.png "AddIn_Model")</span></span>  
<span data-ttu-id="cd546-123">Kanál doplňku s typy</span><span class="sxs-lookup"><span data-stu-id="cd546-123">Add-in pipeline with types</span></span>  
  
 <span data-ttu-id="cd546-124">Následující tabulka popisuje segmentů kanálu pro aktivaci doplňku.</span><span class="sxs-lookup"><span data-stu-id="cd546-124">The following table describes the pipeline segments for activating an add-in.</span></span> <span data-ttu-id="cd546-125">Další informace o těchto segmentů, naleznete v tématu [kontrakty, zobrazení a adaptéry](https://msdn.microsoft.com/library/a6460173-9507-4b87-8c07-d4ee245d715c).</span><span class="sxs-lookup"><span data-stu-id="cd546-125">For more information about these segments, see [Contracts, Views, and Adapters](https://msdn.microsoft.com/library/a6460173-9507-4b87-8c07-d4ee245d715c).</span></span>  
  
|<span data-ttu-id="cd546-126">Segment kanálu</span><span class="sxs-lookup"><span data-stu-id="cd546-126">Pipeline segment</span></span>|<span data-ttu-id="cd546-127">Popis</span><span class="sxs-lookup"><span data-stu-id="cd546-127">Description</span></span>|  
|----------------------|-----------------|  
|<span data-ttu-id="cd546-128">Hostitel</span><span class="sxs-lookup"><span data-stu-id="cd546-128">Host</span></span>|<span data-ttu-id="cd546-129">Sestavení aplikace, která vytvoří instanci doplňku.</span><span class="sxs-lookup"><span data-stu-id="cd546-129">The application assembly that creates an instance of an add-in.</span></span>|  
|<span data-ttu-id="cd546-130">Doplněk v zobrazení pro hostitele</span><span class="sxs-lookup"><span data-stu-id="cd546-130">Host view of the add-in</span></span>|<span data-ttu-id="cd546-131">Představuje zobrazení hostitelská aplikace objekt typů a metod používaných ke komunikaci s doplňku.</span><span class="sxs-lookup"><span data-stu-id="cd546-131">Represents the host application's view of the object types and methods used to communicate with the add-in.</span></span> <span data-ttu-id="cd546-132">Zobrazení hostitele je abstraktní základní třída nebo rozhraní.</span><span class="sxs-lookup"><span data-stu-id="cd546-132">The host view is an abstract base class or interface.</span></span>|  
|<span data-ttu-id="cd546-133">Adaptér na straně hostitele</span><span class="sxs-lookup"><span data-stu-id="cd546-133">Host-side adapter</span></span>|<span data-ttu-id="cd546-134">Sestavení s jednu nebo více tříd, které se přizpůsobí metody do a ze smlouvy.</span><span class="sxs-lookup"><span data-stu-id="cd546-134">An assembly with one or more classes that adapts methods to and from the contract.</span></span><br /><br /> <span data-ttu-id="cd546-135">Tento segment kanálu je identifikovaný pomocí <xref:System.AddIn.Pipeline.HostAdapterAttribute> atribut.</span><span class="sxs-lookup"><span data-stu-id="cd546-135">This pipeline segment is identified by using the <xref:System.AddIn.Pipeline.HostAdapterAttribute> attribute.</span></span><br /><br /> <span data-ttu-id="cd546-136">Vícemodulová sestavení nejsou podporována.</span><span class="sxs-lookup"><span data-stu-id="cd546-136">Multi-module assemblies are not supported.</span></span>|  
|<span data-ttu-id="cd546-137">Kontrakt</span><span class="sxs-lookup"><span data-stu-id="cd546-137">Contract</span></span>|<span data-ttu-id="cd546-138">Který je odvozen z rozhraní <xref:System.AddIn.Contract.IContract> rozhraní a, který definuje protokol pro komunikaci typů mezi hostitelem a jeho doplňku.</span><span class="sxs-lookup"><span data-stu-id="cd546-138">An interface that is derived from the <xref:System.AddIn.Contract.IContract> interface and that defines the protocol for communicating types between the host and its add-in.</span></span><br /><br /> <span data-ttu-id="cd546-139">Tento segment kanálu je identifikován podle nastavení <xref:System.AddIn.Pipeline.AddInContractAttribute> atribut.</span><span class="sxs-lookup"><span data-stu-id="cd546-139">This pipeline segment is identified by setting the <xref:System.AddIn.Pipeline.AddInContractAttribute> attribute.</span></span>|  
|<span data-ttu-id="cd546-140">Přidat v-adaptér na straně</span><span class="sxs-lookup"><span data-stu-id="cd546-140">Add-in-side adapter</span></span>|<span data-ttu-id="cd546-141">Sestavení s jednu nebo více tříd, které se přizpůsobí metody do a ze smlouvy.</span><span class="sxs-lookup"><span data-stu-id="cd546-141">An assembly with one or more classes that adapts methods to and from the contract.</span></span><br /><br /> <span data-ttu-id="cd546-142">Tento segment kanálu je identifikovaný pomocí <xref:System.AddIn.Pipeline.AddInAdapterAttribute> atribut.</span><span class="sxs-lookup"><span data-stu-id="cd546-142">This pipeline segment is identified by using the <xref:System.AddIn.Pipeline.AddInAdapterAttribute> attribute.</span></span><br /><br /> <span data-ttu-id="cd546-143">Každé sestavení v adresáři adaptér přidat v na straně, který obsahuje typ, který má <xref:System.AddIn.Pipeline.AddInAdapterAttribute> atribut je načten do doplňku na aplikační domény.</span><span class="sxs-lookup"><span data-stu-id="cd546-143">Each assembly in the add-in-side adapter directory that contains a type that has an <xref:System.AddIn.Pipeline.AddInAdapterAttribute> attribute is loaded into the add-in's application domain.</span></span><br /><br /> <span data-ttu-id="cd546-144">Každé sestavení v adresáři add-na straně je načteno do vlastní domény aplikace.</span><span class="sxs-lookup"><span data-stu-id="cd546-144">Each assembly in the add-in-side directory is loaded in its own application domain.</span></span><br /><br /> <span data-ttu-id="cd546-145">Sestavení s víc moduly se nepodporují.</span><span class="sxs-lookup"><span data-stu-id="cd546-145">Multi-module assemblies are not supported</span></span>|  
|<span data-ttu-id="cd546-146">Zobrazení doplňku</span><span class="sxs-lookup"><span data-stu-id="cd546-146">Add-in view</span></span>|<span data-ttu-id="cd546-147">Sestavení, který představuje zobrazení doplňku na typy objektů a metod, které se používají ke komunikaci s hostitelem.</span><span class="sxs-lookup"><span data-stu-id="cd546-147">An assembly that represents the add-in's view of the object types and methods that are used to communicate with the host.</span></span> <span data-ttu-id="cd546-148">Zobrazení doplňku je abstraktní základní třída nebo rozhraní.</span><span class="sxs-lookup"><span data-stu-id="cd546-148">The add-in view is an abstract base class or interface.</span></span><br /><br /> <span data-ttu-id="cd546-149">Tento segment kanálu je identifikovaný pomocí <xref:System.AddIn.Pipeline.AddInBaseAttribute> atribut.</span><span class="sxs-lookup"><span data-stu-id="cd546-149">This pipeline segment is identified by using the <xref:System.AddIn.Pipeline.AddInBaseAttribute> attribute.</span></span><br /><br /> <span data-ttu-id="cd546-150">Každé sestavení v adresáři AddInViews, který obsahuje typ, který má <xref:System.AddIn.Pipeline.AddInBaseAttribute> atribut je načten do doplňku na aplikační domény.</span><span class="sxs-lookup"><span data-stu-id="cd546-150">Each assembly in the AddInViews directory that contains a type that has an <xref:System.AddIn.Pipeline.AddInBaseAttribute> attribute is loaded into the add-in's application domain.</span></span>|  
|<span data-ttu-id="cd546-151">doplněk</span><span class="sxs-lookup"><span data-stu-id="cd546-151">Add-in</span></span>|<span data-ttu-id="cd546-152">Instance typu, který provádí služba pro hostitele.</span><span class="sxs-lookup"><span data-stu-id="cd546-152">An instantiated type that performs a service for the host.</span></span>|  
  
## <a name="pipeline-activation-path"></a><span data-ttu-id="cd546-153">Aktivační cesta kanálu</span><span class="sxs-lookup"><span data-stu-id="cd546-153">Pipeline Activation Path</span></span>  
 <span data-ttu-id="cd546-154">Následující obrázek znázorňuje aktivace typy, když se aktivuje doplněk.</span><span class="sxs-lookup"><span data-stu-id="cd546-154">The following illustration shows the activation of types when an add-in is activated.</span></span> <span data-ttu-id="cd546-155">Také ukazuje předávání objektů na hostitele, jako jsou výsledky výpočtu nebo kolekci objektů.</span><span class="sxs-lookup"><span data-stu-id="cd546-155">It also shows the passing of objects to the host, such as the results of a calculation or a collection of objects.</span></span> <span data-ttu-id="cd546-156">Toto je obvykle scénář.</span><span class="sxs-lookup"><span data-stu-id="cd546-156">This is the most typical scenario.</span></span>  
  
 <span data-ttu-id="cd546-157">![Přidat&#45;v modelu s cestou k aktivaci. ](../../../docs/framework/add-ins/media/addin6.png "AddIn6")</span><span class="sxs-lookup"><span data-stu-id="cd546-157">![Add&#45;in model with activation path.](../../../docs/framework/add-ins/media/addin6.png "AddIn6")</span></span>  
<span data-ttu-id="cd546-158">Aktivační cesta od doplňku na hostitele</span><span class="sxs-lookup"><span data-stu-id="cd546-158">Activation path from the add-in to the host</span></span>  
  
 <span data-ttu-id="cd546-159">Aktivační cesta kanálu dojde k následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="cd546-159">The activation path of the pipeline occurs as follows:</span></span>  
  
1.  <span data-ttu-id="cd546-160">Doplněk se aktivuje hostitelskou aplikaci <xref:System.AddIn.Hosting.AddInToken.Activate%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="cd546-160">The host application activates the add-in with the <xref:System.AddIn.Hosting.AddInToken.Activate%2A> method.</span></span>  
  
2.  <span data-ttu-id="cd546-161">Zobrazení doplňku, doplněk, adaptér přidat v na straně a sestavení kontraktu se načtou do doplňku na aplikační domény.</span><span class="sxs-lookup"><span data-stu-id="cd546-161">The add-in, add-in view, add-in-side adapter, and the contract assemblies are loaded into the add-in's application domain.</span></span>  
  
3.  <span data-ttu-id="cd546-162">Pomocí zobrazení doplňku je vytvořena instance adaptér přidat v na straně (pomocí třídy identifikován <xref:System.AddIn.Pipeline.AddInBaseAttribute> atribut) jako jeho konstruktoru.</span><span class="sxs-lookup"><span data-stu-id="cd546-162">An instance of the add-in-side adapter is created using the add-in view (with the class identified by the <xref:System.AddIn.Pipeline.AddInBaseAttribute> attribute) as its constructor.</span></span> <span data-ttu-id="cd546-163">Přidat v straně adaptér dědí ze smlouvy.</span><span class="sxs-lookup"><span data-stu-id="cd546-163">The add-in-side adapter inherits from the contract.</span></span>  
  
4.  <span data-ttu-id="cd546-164">Přidat v straně adaptér, který je zadán jako kontrakt, je předána hranice izolace (volitelné) konstruktor adaptér na straně hostitele.</span><span class="sxs-lookup"><span data-stu-id="cd546-164">The add-in-side adapter, which is typed as the contract, is passed across the (optional) isolation boundary to the host-side adapter's constructor.</span></span>  
  
5.  <span data-ttu-id="cd546-165">Zobrazení hostitele doplňku, hostitelské adaptéru a sestavení kontraktu jsou načtena do domény aplikací hostitele.</span><span class="sxs-lookup"><span data-stu-id="cd546-165">The host view of the add-in, host-side adapter, and the contract assemblies are loaded into the host's application domain.</span></span>  
  
6.  <span data-ttu-id="cd546-166">Pomocí kontraktu jako jeho konstruktor je vytvořena instance adaptér na straně hostitele.</span><span class="sxs-lookup"><span data-stu-id="cd546-166">An instance of the host-side adapter is created using the contract as its constructor.</span></span> <span data-ttu-id="cd546-167">Adaptér na straně hostitele dědí ze zobrazení hostitele doplňku.</span><span class="sxs-lookup"><span data-stu-id="cd546-167">The host-side adapter inherits from the host view of the add-in.</span></span>  
  
7.  <span data-ttu-id="cd546-168">Hostitel má doplněk, který je zadán jako hostitele zobrazení doplňku a můžete pokračovat v jeho metody volání.</span><span class="sxs-lookup"><span data-stu-id="cd546-168">The host has the add-in, which is typed as the host view of the add-in, and can continue calling its methods.</span></span>  
  
## <a name="walkthroughs"></a><span data-ttu-id="cd546-169">Postupy</span><span class="sxs-lookup"><span data-stu-id="cd546-169">Walkthroughs</span></span>  
 <span data-ttu-id="cd546-170">Existují tři návody, které popisují, jak vytvářet kanály pomocí sady Visual Studio:</span><span class="sxs-lookup"><span data-stu-id="cd546-170">There are three walkthrough topics that describe how to create pipelines using Visual Studio:</span></span>  
  
-   [<span data-ttu-id="cd546-171">Návod: Vytváření rozšiřitelné aplikace</span><span class="sxs-lookup"><span data-stu-id="cd546-171">Walkthrough: Creating an Extensible Application</span></span>](../../../docs/framework/add-ins/walkthrough-create-extensible-app.md)  
  
     <span data-ttu-id="cd546-172">Popisuje doplněk kalkulačky, který provádí sčítání, odčítání, násobení a dělení výpočtů pro hostitele.</span><span class="sxs-lookup"><span data-stu-id="cd546-172">Describes a calculator add-in that performs addition, subtraction, multiplication, and divsion calculations for the host.</span></span>  
  
-   [<span data-ttu-id="cd546-173">Návod: Umožnění zpětné kompatibility při změně vašeho hostitele</span><span class="sxs-lookup"><span data-stu-id="cd546-173">Walkthrough: Enabling Backward Compatibility as Your Host Changes</span></span>](https://msdn.microsoft.com/library/6fa15bb5-8f04-407d-bd7d-675dc043c848)  
  
     <span data-ttu-id="cd546-174">Popisuje doplněk Kalkulačka rozšířené výpočetní funkce a tom, jak udržovat kompatibilitu s první doplněk kalkulačky.</span><span class="sxs-lookup"><span data-stu-id="cd546-174">Describes a calculator add-in with enhanced calculation capabilities, and how to maintain compatibility with the first calculator add-in.</span></span>  
  
-   [<span data-ttu-id="cd546-175">Návod: Předávání kolekcí mezi hostiteli a doplňky</span><span class="sxs-lookup"><span data-stu-id="cd546-175">Walkthrough: Passing Collections Between Hosts and Add-Ins</span></span>](https://msdn.microsoft.com/library/b532c604-548e-4fab-b11c-377257dd0ee5)  
  
     <span data-ttu-id="cd546-176">Popisuje, jak předat kolekce dat přes kanál pomocí scénář úložiště knihy.</span><span class="sxs-lookup"><span data-stu-id="cd546-176">Describes how to pass data collections over the pipeline using a book store scenario.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="cd546-177">Viz také</span><span class="sxs-lookup"><span data-stu-id="cd546-177">See Also</span></span>  
 [<span data-ttu-id="cd546-178">Scénáře kanálu doplňku</span><span class="sxs-lookup"><span data-stu-id="cd546-178">Add-in Pipeline Scenarios</span></span>](https://msdn.microsoft.com/library/feb70e0b-8734-494c-aeaf-b567f014043e)  
 [<span data-ttu-id="cd546-179">Doplňky a rozšíření</span><span class="sxs-lookup"><span data-stu-id="cd546-179">Add-ins and Extensibility</span></span>](../../../docs/framework/add-ins/index.md)
