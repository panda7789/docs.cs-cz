---
title: WSTrustChannelFactory a WSTrustChannel
ms.date: 03/30/2017
ms.assetid: 96cec467-e963-4132-b18b-7d0b3a2e979f
author: BrucePerlerMS
ms.openlocfilehash: e00f3ae25a50c2fb3f34f4c04d02cde574b3da17
ms.sourcegitcommit: 289e06e904b72f34ac717dbcc5074239b977e707
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/17/2019
ms.locfileid: "71044919"
---
# <a name="wstrustchannelfactory-and-wstrustchannel"></a><span data-ttu-id="b57e1-102">WSTrustChannelFactory a WSTrustChannel</span><span class="sxs-lookup"><span data-stu-id="b57e1-102">WSTrustChannelFactory and WSTrustChannel</span></span>
<span data-ttu-id="b57e1-103">Pokud už jste obeznámeni se službou Windows Communication Foundation (WCF), víte, že klient WCF už ve federaci ví.</span><span class="sxs-lookup"><span data-stu-id="b57e1-103">If you are already familiar with Windows Communication Foundation (WCF), you know that a WCF client is already federation aware.</span></span> <span data-ttu-id="b57e1-104">Konfigurací klienta WCF pomocí <xref:System.ServiceModel.WSFederationHttpBinding> nebo podobné vlastní vazby můžete povolit federované ověřování pro službu.</span><span class="sxs-lookup"><span data-stu-id="b57e1-104">By configuring a WCF client with a <xref:System.ServiceModel.WSFederationHttpBinding> or similar custom binding, you can enable federated authentication to a service.</span></span>

 <span data-ttu-id="b57e1-105">Služba WCF získá token vydaný službou tokenu zabezpečení (STS) na pozadí a pomocí tohoto tokenu ověří službu.</span><span class="sxs-lookup"><span data-stu-id="b57e1-105">WCF obtains the token that is issued by the security token service (STS) behind the scenes and uses this token to authenticate to the service.</span></span> <span data-ttu-id="b57e1-106">Hlavním omezením tohoto přístupu je, že neexistuje žádná viditelnost komunikace klienta se serverem.</span><span class="sxs-lookup"><span data-stu-id="b57e1-106">The main limitation to this approach is that there is no visibility into the client’s communications with the server.</span></span> <span data-ttu-id="b57e1-107">Služba WCF automaticky vygeneruje token zabezpečení Request (RST) na službu STS na základě parametrů vydaných tokenů ve vazbě.</span><span class="sxs-lookup"><span data-stu-id="b57e1-107">WCF automatically generates the request security token (RST) to the STS based on the issued token parameters on the binding.</span></span> <span data-ttu-id="b57e1-108">To znamená, že klient nemůže měnit parametry RST na žádost, zkontrolovat odpověď tokenu zabezpečení (RSTR) žádosti o získání informací, jako jsou například deklarace identity zobrazení, nebo uložit token do mezipaměti pro budoucí použití.</span><span class="sxs-lookup"><span data-stu-id="b57e1-108">This means that the client cannot vary the RST parameters per request, inspect the request security token response (RSTR) to get information such as display claims, or cache the token for future use.</span></span>

 <span data-ttu-id="b57e1-109">V současné době je klient služby WCF vhodný pro scénáře základní federace.</span><span class="sxs-lookup"><span data-stu-id="b57e1-109">Currently, the WCF client is suitable for basic federation scenarios.</span></span> <span data-ttu-id="b57e1-110">Jeden z hlavních scénářů, které podporuje technologie Windows Identity Foundation (WIF), ale vyžaduje kontrolu nad RST na úrovni, kterou WCF snadno nepovoluje.</span><span class="sxs-lookup"><span data-stu-id="b57e1-110">However, one of the major scenarios that Windows Identity Foundation (WIF) supports requires control over the RST at a level that WCF does not easily allow.</span></span> <span data-ttu-id="b57e1-111">Proto WIF přidá funkce, které vám poskytnou větší kontrolu nad komunikací s STS.</span><span class="sxs-lookup"><span data-stu-id="b57e1-111">Therefore, WIF adds features that give you more control over communication with the STS.</span></span>

 <span data-ttu-id="b57e1-112">WIF podporuje následující scénáře federace:</span><span class="sxs-lookup"><span data-stu-id="b57e1-112">WIF supports the following federation scenarios:</span></span>

- <span data-ttu-id="b57e1-113">Použití klienta WCF bez závislostí WIF k ověření pro federované služby</span><span class="sxs-lookup"><span data-stu-id="b57e1-113">Using a WCF client without any WIF dependencies to authenticate to a federated service</span></span>

- <span data-ttu-id="b57e1-114">Povolení WIF v klientovi WCF pro vložení elementu ActAs nebo OnBehalfOf do pole RST do služby STS</span><span class="sxs-lookup"><span data-stu-id="b57e1-114">Enabling WIF on a WCF client to insert an ActAs or OnBehalfOf element into the RST to the STS</span></span>

- <span data-ttu-id="b57e1-115">Použití samotného WIF k získání tokenu ze služby STS a následnému povolení ověřování klienta WCF s tímto tokenem.</span><span class="sxs-lookup"><span data-stu-id="b57e1-115">Using WIF alone to obtain a token from the STS and then enable a WCF client to authenticate with this token.</span></span> <span data-ttu-id="b57e1-116">Další informace najdete v tématu [ClaimsAwareWebService](https://go.microsoft.com/fwlink/?LinkID=248406) Sample.</span><span class="sxs-lookup"><span data-stu-id="b57e1-116">For more information, see [ClaimsAwareWebService](https://go.microsoft.com/fwlink/?LinkID=248406) sample.</span></span>

 <span data-ttu-id="b57e1-117">První scénář je samozřejmý: Stávající klienti WCF budou dál spolupracovat s předávajícími stranami WIF a STS.</span><span class="sxs-lookup"><span data-stu-id="b57e1-117">The first scenario is self-explanatory: Existing WCF clients will continue to work with WIF relying parties and STSs.</span></span> <span data-ttu-id="b57e1-118">Toto téma pojednává o zbývajících dvou scénářích.</span><span class="sxs-lookup"><span data-stu-id="b57e1-118">This topic discusses the remaining two scenarios.</span></span>

## <a name="enhancing-an-existing-wcf-client-with-actas--onbehalfof"></a><span data-ttu-id="b57e1-119">Vylepšení stávajícího klienta WCF pomocí ActAs/OnBehalfOf</span><span class="sxs-lookup"><span data-stu-id="b57e1-119">Enhancing an Existing WCF Client with ActAs / OnBehalfOf</span></span>
<span data-ttu-id="b57e1-120">V typickém scénáři delegování identity klient volá službu střední vrstvy, která pak zavolá back-end službu.</span><span class="sxs-lookup"><span data-stu-id="b57e1-120">In a typical identity delegation scenario, a client calls a middle-tier service, which then calls a back-end service.</span></span> <span data-ttu-id="b57e1-121">Služba střední vrstvy funguje jako klient nástroje nebo funguje jako jeho jménem.</span><span class="sxs-lookup"><span data-stu-id="b57e1-121">The middle-tier service acts as, or acts on behalf of, the client.</span></span>

> [!TIP]
> <span data-ttu-id="b57e1-122">Jaký je rozdíl mezi ActAs a OnBehalfOf?</span><span class="sxs-lookup"><span data-stu-id="b57e1-122">What is the difference between ActAs and OnBehalfOf?</span></span>
>
> <span data-ttu-id="b57e1-123">Z hlediska protokolu WS-Trust:</span><span class="sxs-lookup"><span data-stu-id="b57e1-123">From the WS-Trust protocol standpoint:</span></span>
>
> 1. <span data-ttu-id="b57e1-124">Element ActAs RST indikuje, že žadatel chce token, který obsahuje deklarace identity o dvou různých entitách: žadateli a externí entitu reprezentovanou tokenem v elementu ActAs.</span><span class="sxs-lookup"><span data-stu-id="b57e1-124">An ActAs RST element indicates that the requestor wants a token that contains claims about two distinct entities: the requestor, and an external entity represented by the token in the ActAs element.</span></span>
> 2. <span data-ttu-id="b57e1-125">Element OnBehalfOf RST indikuje, že žadatel chce token, který obsahuje deklarace identity jenom o jedné entitě: Externí entita reprezentovaná tokenem v elementu OnBehalfOf.</span><span class="sxs-lookup"><span data-stu-id="b57e1-125">An OnBehalfOf RST element indicates that the requestor wants a token that contains claims only about one entity: the external entity represented by the token in the OnBehalfOf element.</span></span>
>
> <span data-ttu-id="b57e1-126">Funkce ActAs se obvykle používá ve scénářích, které vyžadují složené delegování, přičemž konečný příjemce vydaného tokenu může zkontrolovat celý řetěz delegování a zobrazit nejenom klienta, ale všechny zprostředkovatele.</span><span class="sxs-lookup"><span data-stu-id="b57e1-126">The ActAs feature is typically used in scenarios that require composite delegation, where the final recipient of the issued token can inspect the entire delegation chain and see not just the client, but all intermediaries.</span></span> <span data-ttu-id="b57e1-127">To umožňuje provádět řízení přístupu, auditování a další související aktivity na základě celého řetězce delegování identity.</span><span class="sxs-lookup"><span data-stu-id="b57e1-127">This lets it perform access control, auditing and other related activities based on the entire identity delegation chain.</span></span> <span data-ttu-id="b57e1-128">Funkce ActAs se běžně používá v vícevrstvých systémech k ověřování a předávání informací o identitách mezi vrstvami bez nutnosti předat tyto informace na vrstvě Application/Business Logic.</span><span class="sxs-lookup"><span data-stu-id="b57e1-128">The ActAs feature is commonly used in multi-tiered systems to authenticate and pass information about identities between the tiers without having to pass this information at the application/business logic layer.</span></span>
>
> <span data-ttu-id="b57e1-129">Funkce OnBehalfOf se používá ve scénářích, kdy je důležitá pouze identita původního klienta a je v podstatě stejná jako funkce zosobnění identity, která je v systému Windows k dispozici.</span><span class="sxs-lookup"><span data-stu-id="b57e1-129">The OnBehalfOf feature is used in scenarios where only the identity of the original client is important and is effectively the same as the identity impersonation feature available in Windows.</span></span> <span data-ttu-id="b57e1-130">Při použití OnBehalfOf může konečný příjemce vydaného tokenu zobrazit jenom deklarace identity o původním klientovi a informace o prostředníkech se nezachovají.</span><span class="sxs-lookup"><span data-stu-id="b57e1-130">When OnBehalfOf is used, the final recipient of the issued token can only see claims about the original client, and the information about intermediaries is not preserved.</span></span> <span data-ttu-id="b57e1-131">Jedním z běžných vzorů, ve kterých se používá funkce OnBehalfOf, je vzor proxy serveru, kde klient nemůže získat přístup k STS přímo, ale komunikuje prostřednictvím brány proxy serveru.</span><span class="sxs-lookup"><span data-stu-id="b57e1-131">One common pattern where the OnBehalfOf feature is used is the proxy pattern where the client cannot access the STS directly but instead communicates through a proxy gateway.</span></span> <span data-ttu-id="b57e1-132">Brána proxy ověří volajícího a vloží informace o volajícím do prvku OnBehalfOf zprávy RST, kterou pak pošle skutečné službě STS ke zpracování.</span><span class="sxs-lookup"><span data-stu-id="b57e1-132">The proxy gateway authenticates the caller and puts information about the caller into the OnBehalfOf element of the RST message that it then sends to the real STS for processing.</span></span> <span data-ttu-id="b57e1-133">Výsledný token obsahuje jenom deklarace, které souvisejí s klientem proxy serveru, takže proxy serveru je zcela transparentní na přijímači vydaného tokenu. Všimněte si, že WIF nepodporuje \<wsse: SecurityTokenReference > ani \<wsa: EndpointReferences \<> jako podřízenou položku wst: OnBehalfOf >.</span><span class="sxs-lookup"><span data-stu-id="b57e1-133">The resulting token contains only claims related to the client of the proxy, making the proxy completely transparent to the receiver of the issued token.Note that WIF does not support \<wsse:SecurityTokenReference> or \<wsa:EndpointReferences> as a child of \<wst:OnBehalfOf>.</span></span> <span data-ttu-id="b57e1-134">Specifikace WS-Trust umožňuje třem způsobům identifikace původního žadatele (jménem uživatele, který funguje).</span><span class="sxs-lookup"><span data-stu-id="b57e1-134">The WS-Trust specification allows for three ways to identify the original requestor (on behalf of whom the proxy is acting).</span></span> <span data-ttu-id="b57e1-135">Toto jsou:</span><span class="sxs-lookup"><span data-stu-id="b57e1-135">These are:</span></span>
>
> - <span data-ttu-id="b57e1-136">Odkaz na token zabezpečení</span><span class="sxs-lookup"><span data-stu-id="b57e1-136">Security token reference.</span></span> <span data-ttu-id="b57e1-137">Odkaz na token, buď ve zprávě, nebo může být načten mimo pásmo).</span><span class="sxs-lookup"><span data-stu-id="b57e1-137">A reference to a token, either in the message, or possibly retrieved out of band).</span></span>
> - <span data-ttu-id="b57e1-138">Odkaz na koncový bod</span><span class="sxs-lookup"><span data-stu-id="b57e1-138">Endpoint reference.</span></span> <span data-ttu-id="b57e1-139">Používá se jako klíč pro vyhledávání dat, opět mimo pásmo.</span><span class="sxs-lookup"><span data-stu-id="b57e1-139">Used as a key to look up data, again out of band.</span></span>
> - <span data-ttu-id="b57e1-140">Token zabezpečení</span><span class="sxs-lookup"><span data-stu-id="b57e1-140">Security token.</span></span> <span data-ttu-id="b57e1-141">Přímo identifikuje původního žadatele.</span><span class="sxs-lookup"><span data-stu-id="b57e1-141">Identifies the original requestor directly.</span></span>
>
> <span data-ttu-id="b57e1-142">WIF podporuje pouze tokeny zabezpečení, které jsou šifrované nebo nešifrované, jako přímý podřízený prvek \<wst: OnBehalfOf >.</span><span class="sxs-lookup"><span data-stu-id="b57e1-142">WIF supports only security tokens, either encrypted or unencrypted, as a direct child element of \<wst:OnBehalfOf>.</span></span>

 <span data-ttu-id="b57e1-143">Tyto informace jsou předány vydavateli WS-Trust pomocí prvků tokenu ActAs a OnBehalfOf v RST.</span><span class="sxs-lookup"><span data-stu-id="b57e1-143">This information is conveyed to a WS-Trust issuer using the ActAs and OnBehalfOf token elements in the RST.</span></span>

 <span data-ttu-id="b57e1-144">WCF zpřístupňuje bod rozšiřitelnosti pro vazbu, která umožňuje přidat libovolné elementy XML do RST.</span><span class="sxs-lookup"><span data-stu-id="b57e1-144">WCF exposes an extensibility point on the binding that allows arbitrary XML elements to be added to the RST.</span></span> <span data-ttu-id="b57e1-145">Vzhledem k tomu, že bod rozšiřitelnosti je svázán s vazbou, musí být scénáře, které vyžadují obsah RST v závislosti na volání, znovu vytvořit klienta pro každé volání, což snižuje výkon.</span><span class="sxs-lookup"><span data-stu-id="b57e1-145">However, because the extensibility point is tied to the binding, scenarios that require the RST contents to vary per call must re-create the client for every call, which decreases performance.</span></span> <span data-ttu-id="b57e1-146">WIF používá metody `ChannelFactory` rozšíření třídy, aby umožnila vývojářům připojit jakýkoliv token, který se získá mimo pásmo, do RST.</span><span class="sxs-lookup"><span data-stu-id="b57e1-146">WIF uses extension methods on the `ChannelFactory` class to allow developers to attach any token that is obtained out of band to the RST.</span></span> <span data-ttu-id="b57e1-147">Následující příklad kódu ukazuje, jak převzít token, který představuje klienta (například token X. 509, username nebo Security Assertion Markup Language (SAML) a připojí ho k RST, která je odeslána vystaviteli.</span><span class="sxs-lookup"><span data-stu-id="b57e1-147">The following code example shows how to take a token that represents the client (such as an X.509, username, or Security Assertion Markup Language (SAML) token) and attach it to the RST that is sent to the issuer.</span></span>

```csharp
IHelloService serviceChannel = channelFactory.CreateChannelActingAs<IHelloService>(clientSamlToken);
serviceChannel.Hello("Hi!");
```

 <span data-ttu-id="b57e1-148">WIF poskytuje následující výhody:</span><span class="sxs-lookup"><span data-stu-id="b57e1-148">WIF provides the following benefits:</span></span>

- <span data-ttu-id="b57e1-149">Možnost RST se dá upravit na kanál; Proto služby střední vrstvy nemusí znovu vytvářet továrny kanálů pro každého klienta, což zvyšuje výkon.</span><span class="sxs-lookup"><span data-stu-id="b57e1-149">The RST can be modified per channel; therefore, middle-tier services do not have to re-create the channel factory for each client, which improves performance.</span></span>

- <span data-ttu-id="b57e1-150">Tato funkce funguje se stávajícími klienty WCF, což usnadňuje cestu k snadnému upgradu pro existující služby střední vrstvy WCF, které chtějí povolit sémantiku delegování identity.</span><span class="sxs-lookup"><span data-stu-id="b57e1-150">This works with existing WCF clients, which makes an easy upgrade path possible for existing WCF middle-tier services that want to enable identity delegation semantics.</span></span>

 <span data-ttu-id="b57e1-151">Stále ale neexistuje žádná viditelnost komunikace klienta se službou STS.</span><span class="sxs-lookup"><span data-stu-id="b57e1-151">However, there is still no visibility into the client’s communication with the STS.</span></span> <span data-ttu-id="b57e1-152">Podíváme se do třetího scénáře.</span><span class="sxs-lookup"><span data-stu-id="b57e1-152">We’ll examine this in the third scenario.</span></span>

## <a name="communicating-directly-with-an-issuer-and-using-the-issued-token-to-authenticate"></a><span data-ttu-id="b57e1-153">Komunikace přímo s vystavitelem a používání vydaného tokenu k ověření</span><span class="sxs-lookup"><span data-stu-id="b57e1-153">Communicating Directly with an Issuer and Using the Issued Token to Authenticate</span></span>
<span data-ttu-id="b57e1-154">V některých pokročilých scénářích není rozšíření klienta služby WCF dostatečné.</span><span class="sxs-lookup"><span data-stu-id="b57e1-154">For some advanced scenarios, enhancing a WCF client is not enough.</span></span> <span data-ttu-id="b57e1-155">Vývojáři, kteří používají pouze WCF, obvykle používají zprávy ve smlouvách/zprávách odchozích zpráv a zpracovávají odpovědi vystavitelů na straně klienta ručně.</span><span class="sxs-lookup"><span data-stu-id="b57e1-155">Developers who use only WCF typically use Message In / Message Out contracts and handle client-side parsing of the issuer response manually.</span></span>

<span data-ttu-id="b57e1-156">WIF zavádí <xref:System.ServiceModel.Security.WSTrustChannelFactory> třídy a <xref:System.ServiceModel.Security.WSTrustChannel> , aby klient mohl komunikovat přímo s vystavitelem WS-Trust.</span><span class="sxs-lookup"><span data-stu-id="b57e1-156">WIF introduces the <xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> classes to let the client communicate directly with a WS-Trust issuer.</span></span> <span data-ttu-id="b57e1-157">Třídy <xref:System.ServiceModel.Security.WSTrustChannelFactory> a<xref:System.ServiceModel.Security.WSTrustChannel> umožňují tok silně typovaného typu RST a RSTR objektů do toku mezi klientem a vystavitelem, jak je znázorněno v následujícím příkladu kódu.</span><span class="sxs-lookup"><span data-stu-id="b57e1-157">The <xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> classes enable strongly typed RST and RSTR objects to flow between the client and issuer, as shown in the following code example.</span></span>

```csharp
WSTrustChannelFactory trustChannelFactory = new WSTrustChannelFactory(stsBinding, stsAddress);
WSTrustChannel channel = (WSTrustChannel) trustChannelFactory.CreateChannel();
RequestSecurityToken rst = new RequestSecurityToken(RequestTypes.Issue);
rst.AppliesTo = new EndpointAddress(serviceAddress);
RequestSecurityTokenResponse rstr = null;
SecurityToken token = channel.Issue(rst, out rstr);
```

<span data-ttu-id="b57e1-158">Všimněte si, `out` že parametr <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> v metodě umožňuje přístup k RSTR pro kontrolu na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="b57e1-158">Note that the `out` parameter on the <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> method allows access to the RSTR for client-side inspection.</span></span>

<span data-ttu-id="b57e1-159">Zatím jste viděli jenom, jak získat token.</span><span class="sxs-lookup"><span data-stu-id="b57e1-159">So far, you’ve only seen how to obtain a token.</span></span> <span data-ttu-id="b57e1-160">Token, který je vrácen z <xref:System.ServiceModel.Security.WSTrustChannel> objektu, `GenericXmlSecurityToken` je, který obsahuje všechny informace, které jsou nezbytné pro ověřování předávající strany.</span><span class="sxs-lookup"><span data-stu-id="b57e1-160">The token that is returned from the <xref:System.ServiceModel.Security.WSTrustChannel> object is a `GenericXmlSecurityToken` that contains all of the information that is necessary for authentication to a relying party.</span></span> <span data-ttu-id="b57e1-161">Následující příklad kódu ukazuje, jak použít tento token.</span><span class="sxs-lookup"><span data-stu-id="b57e1-161">The following code example shows how to use this token.</span></span>

```csharp
IHelloService serviceChannel = channelFactory.CreateChannelWithIssuedToken<IHelloService>( token );
serviceChannel.Hello("Hi!");
```

<span data-ttu-id="b57e1-162">Metoda <xref:System.ServiceModel.ChannelFactory%601.CreateChannelWithIssuedToken%2A> rozšíření `ChannelFactory` na objektu indikuje WIF, že jste token získali mimo pásmo, a že by měl zastavit normální volání WCF do vystavitele a místo toho použít token, který jste získali k ověřování předávající strany.</span><span class="sxs-lookup"><span data-stu-id="b57e1-162">The <xref:System.ServiceModel.ChannelFactory%601.CreateChannelWithIssuedToken%2A> extension method on the `ChannelFactory` object indicates to WIF that you have obtained a token out of band, and that it should stop the normal WCF call to the issuer and instead use the token that you obtained to authenticate to the relying party.</span></span> <span data-ttu-id="b57e1-163">To má následující výhody:</span><span class="sxs-lookup"><span data-stu-id="b57e1-163">This has the following benefits:</span></span>

- <span data-ttu-id="b57e1-164">Poskytuje plnou kontrolu nad procesem vystavení tokenu.</span><span class="sxs-lookup"><span data-stu-id="b57e1-164">It gives you complete control over the token issuance process.</span></span>

- <span data-ttu-id="b57e1-165">Podporuje scénáře ActAs/OnBehalfOf, a to přímo nastavením těchto vlastností v odchozím parametru RST.</span><span class="sxs-lookup"><span data-stu-id="b57e1-165">It supports ActAs / OnBehalfOf scenarios by directly setting these properties on the outgoing RST.</span></span>

- <span data-ttu-id="b57e1-166">Umožňuje dynamické rozhodování o důvěryhodnosti na straně klienta na základě obsahu RSTR.</span><span class="sxs-lookup"><span data-stu-id="b57e1-166">It enables dynamic client-side trust decisions to be made based on the contents of the RSTR.</span></span>

- <span data-ttu-id="b57e1-167">Umožňuje ukládat do mezipaměti a znovu použít token, který je vrácen z <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="b57e1-167">It lets you cache and reuse the token that is returned from the <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> method.</span></span>

- <span data-ttu-id="b57e1-168"><xref:System.ServiceModel.Security.WSTrustChannelFactory>a <xref:System.ServiceModel.Security.WSTrustChannel> umožňují kontrolu mezipamětí kanálu, selhání a sémantiky obnovení v souladu s osvědčenými postupy WCF.</span><span class="sxs-lookup"><span data-stu-id="b57e1-168"><xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> allow for control of channel caching, fault, and recovery semantics according to WCF best practices.</span></span>

## <a name="see-also"></a><span data-ttu-id="b57e1-169">Viz také:</span><span class="sxs-lookup"><span data-stu-id="b57e1-169">See also</span></span>

- [<span data-ttu-id="b57e1-170">Funkce technologie WIF</span><span class="sxs-lookup"><span data-stu-id="b57e1-170">WIF Features</span></span>](wif-features.md)
