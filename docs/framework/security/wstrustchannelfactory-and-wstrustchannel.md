---
title: WSTrustChannelFactory a WSTrustChannel
ms.date: 03/30/2017
ms.assetid: 96cec467-e963-4132-b18b-7d0b3a2e979f
author: BrucePerlerMS
ms.openlocfilehash: ecc62292b2b064219127c369f43141a31ffe606d
ms.sourcegitcommit: 69229651598b427c550223d3c58aba82e47b3f82
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/04/2018
ms.locfileid: "48793528"
---
# <a name="wstrustchannelfactory-and-wstrustchannel"></a><span data-ttu-id="09fc9-102">WSTrustChannelFactory a WSTrustChannel</span><span class="sxs-lookup"><span data-stu-id="09fc9-102">WSTrustChannelFactory and WSTrustChannel</span></span>
<span data-ttu-id="09fc9-103">Pokud jste už obeznámení s Windows Communication Foundation (WCF), víte, že klient WCF je již federace vědět.</span><span class="sxs-lookup"><span data-stu-id="09fc9-103">If you are already familiar with Windows Communication Foundation (WCF), you know that a WCF client is already federation aware.</span></span> <span data-ttu-id="09fc9-104">Pomocí konfigurace klienta WCF s <xref:System.ServiceModel.WSFederationHttpBinding> nebo podobné vlastní vazby, můžete povolit federované ověřování do služby.</span><span class="sxs-lookup"><span data-stu-id="09fc9-104">By configuring a WCF client with a <xref:System.ServiceModel.WSFederationHttpBinding> or similar custom binding, you can enable federated authentication to a service.</span></span>

 <span data-ttu-id="09fc9-105">WCF získá token, který je vydaný služby tokenů zabezpečení (STS) na pozadí a použije tento token k ověření ve službě.</span><span class="sxs-lookup"><span data-stu-id="09fc9-105">WCF obtains the token that is issued by the security token service (STS) behind the scenes and uses this token to authenticate to the service.</span></span> <span data-ttu-id="09fc9-106">Hlavní omezení tohoto přístupu je, že neexistuje žádná přehled o komunikaci klienta se serverem.</span><span class="sxs-lookup"><span data-stu-id="09fc9-106">The main limitation to this approach is that there is no visibility into the client’s communications with the server.</span></span> <span data-ttu-id="09fc9-107">WCF automaticky generuje token zabezpečení požadavku (RVNÍ) na službu STS, na základě vydaný token parametrů ve vazbě.</span><span class="sxs-lookup"><span data-stu-id="09fc9-107">WCF automatically generates the request security token (RST) to the STS based on the issued token parameters on the binding.</span></span> <span data-ttu-id="09fc9-108">To znamená, že klient nemůže se liší parametry RVNÍ každý požadavek, zkontrolovat odpovědi tokenu zabezpečení požadavku (RSTR) Chcete-li získat informace, jako je zobrazení deklarací identity, mezipaměti token pro budoucí použití.</span><span class="sxs-lookup"><span data-stu-id="09fc9-108">This means that the client cannot vary the RST parameters per request, inspect the request security token response (RSTR) to get information such as display claims, or cache the token for future use.</span></span>

 <span data-ttu-id="09fc9-109">V současné době je vhodný pro základní federacích klienta WCF.</span><span class="sxs-lookup"><span data-stu-id="09fc9-109">Currently, the WCF client is suitable for basic federation scenarios.</span></span> <span data-ttu-id="09fc9-110">Jedním z hlavních scénářů, které podporuje technologie Windows Identity Foundation (WIF) ale vyžaduje kontrolu nad RVNÍ na úrovni, která není povolena snadno WCF.</span><span class="sxs-lookup"><span data-stu-id="09fc9-110">However, one of the major scenarios that Windows Identity Foundation (WIF) supports requires control over the RST at a level that WCF does not easily allow.</span></span> <span data-ttu-id="09fc9-111">Technologie WIF proto přidá funkce, které poskytují větší kontrolu nad komunikace se službou tokenů zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="09fc9-111">Therefore, WIF adds features that give you more control over communication with the STS.</span></span>

 <span data-ttu-id="09fc9-112">Technologie WIF podporuje následující scénáře federace:</span><span class="sxs-lookup"><span data-stu-id="09fc9-112">WIF supports the following federation scenarios:</span></span>

- <span data-ttu-id="09fc9-113">Pomocí klienta WCF nezávisle technologie WIF za účelem ověření k federované službě</span><span class="sxs-lookup"><span data-stu-id="09fc9-113">Using a WCF client without any WIF dependencies to authenticate to a federated service</span></span>

- <span data-ttu-id="09fc9-114">Povolení technologie WIF na klienta WCF na vkládání elementů ActAs nebo OnBehalfOf do RVNÍ na službu STS</span><span class="sxs-lookup"><span data-stu-id="09fc9-114">Enabling WIF on a WCF client to insert an ActAs or OnBehalfOf element into the RST to the STS</span></span>

- <span data-ttu-id="09fc9-115">Pomocí technologie WIF samostatně, aby získat token od služby STS a pak povolte klienta WCF na ověření pomocí tohoto tokenu.</span><span class="sxs-lookup"><span data-stu-id="09fc9-115">Using WIF alone to obtain a token from the STS and then enable a WCF client to authenticate with this token.</span></span> <span data-ttu-id="09fc9-116">Další informace najdete v tématu [ClaimsAwareWebService](https://go.microsoft.com/fwlink/?LinkID=248406) vzorku.</span><span class="sxs-lookup"><span data-stu-id="09fc9-116">For more information, see [ClaimsAwareWebService](https://go.microsoft.com/fwlink/?LinkID=248406) sample.</span></span>

 <span data-ttu-id="09fc9-117">První scénář je zřejmých: WCF existující klienti budou dál fungovat s přijímajících stran, které technologie WIF a služby tokenů zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="09fc9-117">The first scenario is self-explanatory: Existing WCF clients will continue to work with WIF relying parties and STSs.</span></span> <span data-ttu-id="09fc9-118">Toto téma popisuje zbývající dva scénáře.</span><span class="sxs-lookup"><span data-stu-id="09fc9-118">This topic discusses the remaining two scenarios.</span></span>

## <a name="enhancing-an-existing-wcf-client-with-actas--onbehalfof"></a><span data-ttu-id="09fc9-119">Vylepšení stávajícího klienta WCF s ActAs nebo OnBehalfOf</span><span class="sxs-lookup"><span data-stu-id="09fc9-119">Enhancing an Existing WCF Client with ActAs / OnBehalfOf</span></span>
<span data-ttu-id="09fc9-120">V případě delegování identity typické klient volá střední vrstvy služby, která potom volá back-end služby.</span><span class="sxs-lookup"><span data-stu-id="09fc9-120">In a typical identity delegation scenario, a client calls a middle-tier service, which then calls a back-end service.</span></span> <span data-ttu-id="09fc9-121">Služba střední vrstvy funguje jako, nebo funguje jménem klienta.</span><span class="sxs-lookup"><span data-stu-id="09fc9-121">The middle-tier service acts as, or acts on behalf of, the client.</span></span>

> [!TIP]
> <span data-ttu-id="09fc9-122">Jaký je rozdíl mezi ActAs a prostřednictvím profilu OnBehalfOf?</span><span class="sxs-lookup"><span data-stu-id="09fc9-122">What is the difference between ActAs and OnBehalfOf?</span></span>
>
> <span data-ttu-id="09fc9-123">Z hlediska protokolu WS-Trust:</span><span class="sxs-lookup"><span data-stu-id="09fc9-123">From the WS-Trust protocol standpoint:</span></span>
>
> 1. <span data-ttu-id="09fc9-124">Element ActAs RVNÍ označuje, žadatel vyžaduje token, který obsahuje deklarace identity o dvou různých entit: žadatel a externí entitu reprezentovanou objektem token v elementu ActAs.</span><span class="sxs-lookup"><span data-stu-id="09fc9-124">An ActAs RST element indicates that the requestor wants a token that contains claims about two distinct entities: the requestor, and an external entity represented by the token in the ActAs element.</span></span>
> 2. <span data-ttu-id="09fc9-125">Element prostřednictvím profilu OnBehalfOf RVNÍ označuje, že žadatel vyžaduje token, který obsahuje pouze jednu entitu deklarace identity: externí entitu reprezentovanou objektem token v elementu prostřednictvím profilu OnBehalfOf.</span><span class="sxs-lookup"><span data-stu-id="09fc9-125">An OnBehalfOf RST element indicates that the requestor wants a token that contains claims only about one entity: the external entity represented by the token in the OnBehalfOf element.</span></span>
>
> <span data-ttu-id="09fc9-126">Funkce ActAs se obvykle používá ve scénářích, které vyžadují složené delegování, kde kontrolovat celou delegování řetězce a vidět nejen klienta, ale všichni zprostředkovatelé konečného příjemce vydaný token.</span><span class="sxs-lookup"><span data-stu-id="09fc9-126">The ActAs feature is typically used in scenarios that require composite delegation, where the final recipient of the issued token can inspect the entire delegation chain and see not just the client, but all intermediaries.</span></span> <span data-ttu-id="09fc9-127">To umožňuje provádět řízení přístupu, auditování a další související aktivity založené na řetězci delegování celý identity.</span><span class="sxs-lookup"><span data-stu-id="09fc9-127">This lets it perform access control, auditing and other related activities based on the entire identity delegation chain.</span></span> <span data-ttu-id="09fc9-128">ActAs funkce se běžně používá v systémech vícevrstevných k ověření a předání informací o identitách mezi vrstvami, aniž byste museli předejte tyto informace ve vrstvě aplikace a obchodní logiku.</span><span class="sxs-lookup"><span data-stu-id="09fc9-128">The ActAs feature is commonly used in multi-tiered systems to authenticate and pass information about identities between the tiers without having to pass this information at the application/business logic layer.</span></span>
>
> <span data-ttu-id="09fc9-129">Prostřednictvím profilu OnBehalfOf funkce se používá v situacích, kdy pouze identitu původního klienta je důležité a je v podstatě totéž jako identita zosobnění funkci k dispozici ve Windows.</span><span class="sxs-lookup"><span data-stu-id="09fc9-129">The OnBehalfOf feature is used in scenarios where only the identity of the original client is important and is effectively the same as the identity impersonation feature available in Windows.</span></span> <span data-ttu-id="09fc9-130">Při použití prostřednictvím profilu OnBehalfOf konečného příjemce vydaný token uvidí jenom deklarace identity o původní klienta a informace o zprostředkovateli nezachová.</span><span class="sxs-lookup"><span data-stu-id="09fc9-130">When OnBehalfOf is used, the final recipient of the issued token can only see claims about the original client, and the information about intermediaries is not preserved.</span></span> <span data-ttu-id="09fc9-131">Ten, který je běžný vzor, kde se používá funkci prostřednictvím profilu OnBehalfOf vzor proxy serveru, kde klient nemůže získat Služba tokenů zabezpečení přímo, ale místo toho komunikuje přes proxy server brány.</span><span class="sxs-lookup"><span data-stu-id="09fc9-131">One common pattern where the OnBehalfOf feature is used is the proxy pattern where the client cannot access the STS directly but instead communicates through a proxy gateway.</span></span> <span data-ttu-id="09fc9-132">Proxy server brány ověří volajícího a vloží informace o volajícím do elementu prostřednictvím profilu OnBehalfOf RVNÍ zpráva, která pak odešle do skutečných STS pro zpracování.</span><span class="sxs-lookup"><span data-stu-id="09fc9-132">The proxy gateway authenticates the caller and puts information about the caller into the OnBehalfOf element of the RST message that it then sends to the real STS for processing.</span></span> <span data-ttu-id="09fc9-133">Výsledný token obsahuje pouze nároky týkající se klienta proxy, aby zcela transparentní proxy server příjemci vydaný token. Všimněte si, že technologie WIF nepodporuje \<wsse: SecurityTokenReference > nebo \<wsa:EndpointReferences > jako podřízený objekt \<wst:OnBehalfOf >.</span><span class="sxs-lookup"><span data-stu-id="09fc9-133">The resulting token contains only claims related to the client of the proxy, making the proxy completely transparent to the receiver of the issued token.Note that WIF does not support \<wsse:SecurityTokenReference> or \<wsa:EndpointReferences> as a child of \<wst:OnBehalfOf>.</span></span> <span data-ttu-id="09fc9-134">Specifikace WS-Trust umožňuje tři způsoby, jak identifikovat ho původnímu žadateli (jménem kterého se chová proxy serveru).</span><span class="sxs-lookup"><span data-stu-id="09fc9-134">The WS-Trust specification allows for three ways to identify the original requestor (on behalf of whom the proxy is acting).</span></span> <span data-ttu-id="09fc9-135">Toto jsou:</span><span class="sxs-lookup"><span data-stu-id="09fc9-135">These are:</span></span>
>
> - <span data-ttu-id="09fc9-136">Odkaz na token zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="09fc9-136">Security token reference.</span></span> <span data-ttu-id="09fc9-137">Odkaz na token, buď ve zprávě, nebo může být načtena z obsluhy vzdálené správy).</span><span class="sxs-lookup"><span data-stu-id="09fc9-137">A reference to a token, either in the message, or possibly retrieved out of band).</span></span>
> - <span data-ttu-id="09fc9-138">Reference koncového bodu.</span><span class="sxs-lookup"><span data-stu-id="09fc9-138">Endpoint reference.</span></span> <span data-ttu-id="09fc9-139">Chcete-li vyhledat data opět vzdáleně použít jako klíč.</span><span class="sxs-lookup"><span data-stu-id="09fc9-139">Used as a key to look up data, again out of band.</span></span>
> - <span data-ttu-id="09fc9-140">Token zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="09fc9-140">Security token.</span></span> <span data-ttu-id="09fc9-141">Identifikuje původní žadatel přímo.</span><span class="sxs-lookup"><span data-stu-id="09fc9-141">Identifies the original requestor directly.</span></span>
>
> <span data-ttu-id="09fc9-142">Technologie WIF podporuje pouze zabezpečení tokeny, šifrované a nešifrované jako přímý podřízený prvek \<wst:OnBehalfOf >.</span><span class="sxs-lookup"><span data-stu-id="09fc9-142">WIF supports only security tokens, either encrypted or unencrypted, as a direct child element of \<wst:OnBehalfOf>.</span></span>

 <span data-ttu-id="09fc9-143">Tyto informace předávají do vystavitele WS-Trust ActAs a prostřednictvím profilu OnBehalfOf token prvky v RVNÍ.</span><span class="sxs-lookup"><span data-stu-id="09fc9-143">This information is conveyed to a WS-Trust issuer using the ActAs and OnBehalfOf token elements in the RST.</span></span>

 <span data-ttu-id="09fc9-144">WCF zpřístupní bod rozšiřitelnosti pro vazbu, která umožňuje libovolnými prvky XML mají být přidány do RVNÍ.</span><span class="sxs-lookup"><span data-stu-id="09fc9-144">WCF exposes an extensibility point on the binding that allows arbitrary XML elements to be added to the RST.</span></span> <span data-ttu-id="09fc9-145">Ale protože bodu rozšiřitelnosti se váže na vazby, scénáře, které vyžadují obsah RVNÍ se liší podle volání musí znovu vytvořit klienta pro všechna volání, což snižuje výkon.</span><span class="sxs-lookup"><span data-stu-id="09fc9-145">However, because the extensibility point is tied to the binding, scenarios that require the RST contents to vary per call must re-create the client for every call, which decreases performance.</span></span> <span data-ttu-id="09fc9-146">Technologie WIF používají rozšiřující metody na `ChannelFactory` třídu, která umožňuje vývojářům připojit žádný token, která se získá mimo pásmo RVNÍ.</span><span class="sxs-lookup"><span data-stu-id="09fc9-146">WIF uses extension methods on the `ChannelFactory` class to allow developers to attach any token that is obtained out of band to the RST.</span></span> <span data-ttu-id="09fc9-147">Následující příklad kódu ukazuje, jak provést token, který představuje klienta (například X.509, uživatelské jméno nebo token zabezpečení kontrolního výrazu SAML (Markup Language)) a připojit ho k RVNÍ, které je odesláno vystavitele.</span><span class="sxs-lookup"><span data-stu-id="09fc9-147">The following code example shows how to take a token that represents the client (such as an X.509, username, or Security Assertion Markup Language (SAML) token) and attach it to the RST that is sent to the issuer.</span></span>

```csharp
IHelloService serviceChannel = channelFactory.CreateChannelActingAs<IHelloService>(clientSamlToken);
serviceChannel.Hello("Hi!");
```

 <span data-ttu-id="09fc9-148">Technologie WIF nabízí následující výhody:</span><span class="sxs-lookup"><span data-stu-id="09fc9-148">WIF provides the following benefits:</span></span>

- <span data-ttu-id="09fc9-149">Je možné upravit RVNÍ jeden kanál; střední vrstvy služby, proto není potřeba znovu vytvořit objekt pro vytváření kanálů pro každého klienta, což zvyšuje výkon.</span><span class="sxs-lookup"><span data-stu-id="09fc9-149">The RST can be modified per channel; therefore, middle-tier services do not have to re-create the channel factory for each client, which improves performance.</span></span>

- <span data-ttu-id="09fc9-150">Tento postup funguje s existující klienti WCF, která umožňuje snadný způsob upgradu pro existující střední vrstvy služby WCF, které chcete povolit sémantiku delegování identity.</span><span class="sxs-lookup"><span data-stu-id="09fc9-150">This works with existing WCF clients, which makes an easy upgrade path possible for existing WCF middle-tier services that want to enable identity delegation semantics.</span></span>

 <span data-ttu-id="09fc9-151">Existuje ale stále žádné přehled o komunikaci klienta pomocí služby STS.</span><span class="sxs-lookup"><span data-stu-id="09fc9-151">However, there is still no visibility into the client’s communication with the STS.</span></span> <span data-ttu-id="09fc9-152">Prozkoumáme to v třetí scénář.</span><span class="sxs-lookup"><span data-stu-id="09fc9-152">We’ll examine this in the third scenario.</span></span>

## <a name="communicating-directly-with-an-issuer-and-using-the-issued-token-to-authenticate"></a><span data-ttu-id="09fc9-153">Komunikuje přímo s vystavitele a pomocí vydaný Token pro ověření</span><span class="sxs-lookup"><span data-stu-id="09fc9-153">Communicating Directly with an Issuer and Using the Issued Token to Authenticate</span></span>
<span data-ttu-id="09fc9-154">Pro některé pokročilé scénáře vylepšení klienta WCF nestačí.</span><span class="sxs-lookup"><span data-stu-id="09fc9-154">For some advanced scenarios, enhancing a WCF client is not enough.</span></span> <span data-ttu-id="09fc9-155">Vývojáři, kteří používají obvykle pouze WCF používat zprávy v / zprávy smlouvy a zpracování na straně klienta ruční parsování odpovědi vystavitele.</span><span class="sxs-lookup"><span data-stu-id="09fc9-155">Developers who use only WCF typically use Message In / Message Out contracts and handle client-side parsing of the issuer response manually.</span></span>

<span data-ttu-id="09fc9-156">Zavádí technologie WIF <xref:System.ServiceModel.Security.WSTrustChannelFactory> a <xref:System.ServiceModel.Security.WSTrustChannel> třídy, které umožní klient komunikovat přímo s vystavitele WS-Trust.</span><span class="sxs-lookup"><span data-stu-id="09fc9-156">WIF introduces the <xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> classes to let the client communicate directly with a WS-Trust issuer.</span></span> <span data-ttu-id="09fc9-157"><xref:System.ServiceModel.Security.WSTrustChannelFactory> a <xref:System.ServiceModel.Security.WSTrustChannel> třídy povolit RVNÍ a RSTR objektů se silným typem tok mezi klientem a vystavitele, jak je znázorněno v následujícím příkladu kódu.</span><span class="sxs-lookup"><span data-stu-id="09fc9-157">The <xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> classes enable strongly typed RST and RSTR objects to flow between the client and issuer, as shown in the following code example.</span></span>

```csharp
WSTrustChannelFactory trustChannelFactory = new WSTrustChannelFactory(stsBinding, stsAddress);
WSTrustChannel channel = (WSTrustChannel) trustChannelFactory.CreateChannel();
RequestSecurityToken rst = new RequestSecurityToken(RequestTypes.Issue);
rst.AppliesTo = new EndpointAddress(serviceAddress);
RequestSecurityTokenResponse rstr = null;
SecurityToken token = channel.Issue(rst, out rstr);
```

<span data-ttu-id="09fc9-158">Všimněte si, že `out` parametru u <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> metoda umožňuje přístup k RSTR pro kontroly na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="09fc9-158">Note that the `out` parameter on the <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> method allows access to the RSTR for client-side inspection.</span></span>

<span data-ttu-id="09fc9-159">Zatím jste pouze seznámili s postupem k získání tokenu.</span><span class="sxs-lookup"><span data-stu-id="09fc9-159">So far, you’ve only seen how to obtain a token.</span></span> <span data-ttu-id="09fc9-160">Token, který je vrácen z <xref:System.ServiceModel.Security.WSTrustChannel> je objekt `GenericXmlSecurityToken` , která obsahuje všechny informace, které jsou nezbytné pro ověřování a předávající stranou.</span><span class="sxs-lookup"><span data-stu-id="09fc9-160">The token that is returned from the <xref:System.ServiceModel.Security.WSTrustChannel> object is a `GenericXmlSecurityToken` that contains all of the information that is necessary for authentication to a relying party.</span></span> <span data-ttu-id="09fc9-161">Následující příklad kódu ukazuje, jak používat tento token.</span><span class="sxs-lookup"><span data-stu-id="09fc9-161">The following code example shows how to use this token.</span></span>

```csharp
IHelloService serviceChannel = channelFactory.CreateChannelWithIssuedToken<IHelloService>( token );
serviceChannel.Hello("Hi!");
```

<span data-ttu-id="09fc9-162"><xref:System.ServiceModel.ChannelFactory%601.CreateChannelWithIssuedToken%2A> Rozšiřující metody na `ChannelFactory` objekt znamená technologie WIF, který jste získali token mimo IP síť a měl by zastavit normální volání WCF vystavitele a místo toho použijte token, který jste získali k ověření předávající straně.</span><span class="sxs-lookup"><span data-stu-id="09fc9-162">The <xref:System.ServiceModel.ChannelFactory%601.CreateChannelWithIssuedToken%2A> extension method on the `ChannelFactory` object indicates to WIF that you have obtained a token out of band, and that it should stop the normal WCF call to the issuer and instead use the token that you obtained to authenticate to the relying party.</span></span> <span data-ttu-id="09fc9-163">To má tyto výhody:</span><span class="sxs-lookup"><span data-stu-id="09fc9-163">This has the following benefits:</span></span>

- <span data-ttu-id="09fc9-164">Poskytuje plnou kontrolu nad procesu vystavování tokenů.</span><span class="sxs-lookup"><span data-stu-id="09fc9-164">It gives you complete control over the token issuance process.</span></span>

- <span data-ttu-id="09fc9-165">Podporuje ActAs nebo OnBehalfOf scénáře podle přímo na odchozí RVNÍ nastavení těchto vlastností.</span><span class="sxs-lookup"><span data-stu-id="09fc9-165">It supports ActAs / OnBehalfOf scenarios by directly setting these properties on the outgoing RST.</span></span>

- <span data-ttu-id="09fc9-166">Umožňuje dynamického vztahu důvěryhodnosti na straně klienta na základě rozhodnutí o obsahu RSTR.</span><span class="sxs-lookup"><span data-stu-id="09fc9-166">It enables dynamic client-side trust decisions to be made based on the contents of the RSTR.</span></span>

- <span data-ttu-id="09fc9-167">To umožňuje ukládat do mezipaměti a opakovaně používat token, který je vrácen z <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="09fc9-167">It lets you cache and reuse the token that is returned from the <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> method.</span></span>

- <span data-ttu-id="09fc9-168"><xref:System.ServiceModel.Security.WSTrustChannelFactory> a <xref:System.ServiceModel.Security.WSTrustChannel> povolit pro ovládací prvek kanál sémantiky ukládání do mezipaměti, selhání a obnovení podle osvědčených postupů WCF.</span><span class="sxs-lookup"><span data-stu-id="09fc9-168"><xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> allow for control of channel caching, fault, and recovery semantics according to WCF best practices.</span></span>

## <a name="see-also"></a><span data-ttu-id="09fc9-169">Viz také:</span><span class="sxs-lookup"><span data-stu-id="09fc9-169">See also</span></span>

- [<span data-ttu-id="09fc9-170">Funkce technologie WIF</span><span class="sxs-lookup"><span data-stu-id="09fc9-170">WIF Features</span></span>](../../../docs/framework/security/wif-features.md)