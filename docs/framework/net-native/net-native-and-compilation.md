---
title: .NET Native a kompilace
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-clr
ms.tgt_pltfrm: 
ms.topic: article
ms.assetid: e38ae4f3-3e3d-42c3-a4b8-db1aa9d84f85
caps.latest.revision: "7"
author: rpetrusha
ms.author: ronpet
manager: wpickett
ms.workload: dotnet
ms.openlocfilehash: d86d8a740aa0597a21c6665ee722f4a601dec9bf
ms.sourcegitcommit: 16186c34a957fdd52e5db7294f291f7530ac9d24
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 12/22/2017
---
# <a name="net-native-and-compilation"></a><span data-ttu-id="117f9-102">.NET Native a kompilace</span><span class="sxs-lookup"><span data-stu-id="117f9-102">.NET Native and Compilation</span></span>
<span data-ttu-id="117f9-103">Aplikace Windows 8.1 a Windows Desktop aplikace, které cílí na rozhraní.NET Framework jsou napsané v konkrétní programovací jazyk a zkompilovat do převodní jazyk (IL).</span><span class="sxs-lookup"><span data-stu-id="117f9-103">Windows 8.1 applications and Windows Desktop applications that target the.NET Framework are written in a particular programming language and compiled into intermediate language (IL).</span></span> <span data-ttu-id="117f9-104">V době běhu kompilátor za běhu (JIT) zodpovídá za kompilace IL do nativního kódu pro místní počítač, těsně před je metoda spuštěna poprvé.</span><span class="sxs-lookup"><span data-stu-id="117f9-104">At runtime, a just-in-time (JIT) compiler is responsible for compiling the IL into native code for the local machine just before a method is executed for the first time.</span></span> <span data-ttu-id="117f9-105">Naproti tomu řetězu .NET Native nástroj převede zdrojového kódu do nativního kódu v době kompilace.</span><span class="sxs-lookup"><span data-stu-id="117f9-105">In contrast, the .NET Native tool chain converts source code to native code at compile time.</span></span> <span data-ttu-id="117f9-106">Toto téma porovnává .NET Native s jinými technologiemi kompilace k dispozici pro aplikace .NET Framework a také poskytuje praktické přehled o tom, jak .NET Native vytváří nativní kód, který vám může pomoct pochopit, proč výjimky, které se objeví v kódu kompilovat s .NET Nativní se nevyskytují v kompilována kódu.</span><span class="sxs-lookup"><span data-stu-id="117f9-106">This topic compares .NET Native with other compilation technologies available for .NET Framework apps, and also provides a practical overview of how .NET Native produces native code that can help you understand why exceptions that occur in code compiled with .NET Native do not occur in JIT-compiled code.</span></span>  
  
## <a name="net-native-generating-native-binaries"></a><span data-ttu-id="117f9-107">.NET native: Generování nativní binárních souborů</span><span class="sxs-lookup"><span data-stu-id="117f9-107">.NET Native: Generating native binaries</span></span>  
 <span data-ttu-id="117f9-108">Aplikace, není pomocí .NET Native řetězu nástroj zkompilovaná cílů rozhraní .NET Framework a který se skládá z sestavení aplikace, která zahrnuje následující:</span><span class="sxs-lookup"><span data-stu-id="117f9-108">An application that targets the .NET Framework and that is not compiled by using the .NET Native tool chain consists of your application assembly, which includes the following:</span></span>  
  
-   <span data-ttu-id="117f9-109">[Metadata](../../../docs/standard/metadata-and-self-describing-components.md) sestavení, jeho závislé součásti, obsahuje typy a jejich členy, který popisuje.</span><span class="sxs-lookup"><span data-stu-id="117f9-109">[Metadata](../../../docs/standard/metadata-and-self-describing-components.md) that describes the assembly, its dependencies, the types it contains, and their members.</span></span> <span data-ttu-id="117f9-110">Metadata slouží pro reflexe a pozdní vazby přístup a v některých případech i nástrojů kompilátoru a sestavení.</span><span class="sxs-lookup"><span data-stu-id="117f9-110">The metadata is used for reflection and late-bound access, and in some cases by compiler and build tools as well.</span></span>  
  
-   <span data-ttu-id="117f9-111">Implementace kód.</span><span class="sxs-lookup"><span data-stu-id="117f9-111">Implementation code.</span></span> <span data-ttu-id="117f9-112">Tento postup se skládá operačních kódů převodní jazyk (IL).</span><span class="sxs-lookup"><span data-stu-id="117f9-112">This consists of intermediate language (IL) opcodes.</span></span> <span data-ttu-id="117f9-113">V době běhu kompilátoru za běhu (JIT) převede jej do nativního kódu pro cílovou platformu.</span><span class="sxs-lookup"><span data-stu-id="117f9-113">At runtime, the just-in-time (JIT) compiler translates it into native code for the target platform.</span></span>  
  
 <span data-ttu-id="117f9-114">Kromě vaší hlavní aplikace sestavení aplikace vyžaduje, aby následující existuje:</span><span class="sxs-lookup"><span data-stu-id="117f9-114">In addition to your main application assembly, an app requires that the following be present:</span></span>  
  
-   <span data-ttu-id="117f9-115">Žádné další třídy knihovny nebo sestavení třetích stran, které jsou vyžadované vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="117f9-115">Any additional class libraries or third-party assemblies that are required by your app.</span></span> <span data-ttu-id="117f9-116">Tyto sestavení podobně obsahovat metadata, která popisuje sestavení, jeho typy a jejich členy, jakož i IL, který implementuje všechny členy typu.</span><span class="sxs-lookup"><span data-stu-id="117f9-116">These assemblies similarly include metadata that describes the assembly, its types, and their members, as well as the IL that implements all type members.</span></span>  
  
-   <span data-ttu-id="117f9-117">Knihovna tříd rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="117f9-117">The .NET Framework Class Library.</span></span> <span data-ttu-id="117f9-118">Toto je kolekce sestavení, která je nainstalována na místní systém s instalace rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="117f9-118">This is a collection of assemblies that is installed on the local system with the .NET Framework installation.</span></span> <span data-ttu-id="117f9-119">Sestavení zahrnuty v knihovně tříd rozhraní .NET Framework zahrnovat kompletní sadu metadat a provádění kódu.</span><span class="sxs-lookup"><span data-stu-id="117f9-119">The assemblies included in the .NET Framework Class Library include a complete set of metadata and implementation code.</span></span>  
  
-   <span data-ttu-id="117f9-120">Modul CLR.</span><span class="sxs-lookup"><span data-stu-id="117f9-120">The common language runtime.</span></span> <span data-ttu-id="117f9-121">Toto je kolekce dynamické knihovny, které provádějí takové služby jako načítání sestavení, správu a uvolňování paměti kolekce paměti, výjimek, kompilace za běhu, vzdálenou komunikaci a zprostředkovatel komunikace s objekty.</span><span class="sxs-lookup"><span data-stu-id="117f9-121">This is a collection of dynamic link libraries that perform such services as assembly loading, memory management and garbage collection, exception handling, just-in-time compilation, remoting, and interop.</span></span> <span data-ttu-id="117f9-122">Jako knihovny tříd je nainstalován modul runtime v místním systému jako součást instalace rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="117f9-122">Like the class library, the runtime is installed on the local system as part of the .NET Framework installation.</span></span>  
  
 <span data-ttu-id="117f9-123">Všimněte si, že celý modul common language runtime, jakož i metadata a IL pro všechny typy v sestavení specifické pro aplikaci, sestavení třetích stran a sestavení systému musí být k dispozici pro aplikaci při spuštění.</span><span class="sxs-lookup"><span data-stu-id="117f9-123">Note that the entire common language runtime, as well as metadata and IL for all types in application-specific assemblies, third-party assemblies, and system assemblies must be present for the app to execute successfully.</span></span>  
  
## <a name="net-native-and-just-in-time-compilation"></a><span data-ttu-id="117f9-124">Kompilace .NET native a za běhu</span><span class="sxs-lookup"><span data-stu-id="117f9-124">.NET Native and just-in-time compilation</span></span>  
 <span data-ttu-id="117f9-125">Vstup pro .NET Native řetězec nástroj je aplikace pro Windows store vytvořené C# nebo Visual Basic – kompilátor.</span><span class="sxs-lookup"><span data-stu-id="117f9-125">The input for the .NET Native tool chain is the Windows store app built by the C# or Visual Basic compiler.</span></span> <span data-ttu-id="117f9-126">Jinými slovy řetězu .NET Native nástroj zahájí spuštění po dokončení kompilátor jazyka kompilace aplikace pro Windows Store.</span><span class="sxs-lookup"><span data-stu-id="117f9-126">In other words, the .NET Native tool chain begins execution when the language compiler has finished compilation of a Windows Store app.</span></span>  
  
> [!TIP]
>  <span data-ttu-id="117f9-127">Protože vstup .NET Native IL a metadata zapsána do spravované sestavení, můžete přesto provést generace vlastní kód nebo další vlastní operace pomocí události před sestavením nebo po sestavení nebo změnou soubor projektu nástroje MSBuild.</span><span class="sxs-lookup"><span data-stu-id="117f9-127">Because the input to .NET Native is the IL and metadata written to managed assemblies, you can still perform custom code generation or other custom operations by using pre-build or post-build events or by modifying the MSBuild project file.</span></span>  
>   
>  <span data-ttu-id="117f9-128">Kategorie nástroje, které upravit IL a tím zabránit v řetězu .NET nástroj Analýza aplikace IL ale podporované nejsou.</span><span class="sxs-lookup"><span data-stu-id="117f9-128">However, categories of tools that modify IL and thereby prevent the .NET tool chain from analyzing an app's IL are not supported.</span></span> <span data-ttu-id="117f9-129">Obfuscators jsou nástroje nejdůležitějším tohoto typu.</span><span class="sxs-lookup"><span data-stu-id="117f9-129">Obfuscators are the most notable tools of this type.</span></span>  
  
 <span data-ttu-id="117f9-130">V průběhu převodu aplikace na nativní kód IL, řetězu .NET Native nástroj provádí operace, jako je následující:</span><span class="sxs-lookup"><span data-stu-id="117f9-130">In the course of converting an app from IL to native code, the .NET Native tool chain performs operations like the following:</span></span>  
  
-   <span data-ttu-id="117f9-131">Pro určité cesty kódu, se nahradí kód, který závisí na reflexi a metadat pomocí statické nativního kódu.</span><span class="sxs-lookup"><span data-stu-id="117f9-131">For certain code paths, it replaces code that relies on reflection and metadata with static native code.</span></span> <span data-ttu-id="117f9-132">Například, pokud typ hodnoty nepřepisuje <xref:System.ValueType.Equals%2A?displayProperty=nameWithType> metoda, výchozí testování rovnosti pomocí reflexe načíst <xref:System.Reflection.FieldInfo> objekty, které představují typ hodnoty pole, pak porovná dvě instance hodnoty polí.</span><span class="sxs-lookup"><span data-stu-id="117f9-132">For example, if a value type does not override the <xref:System.ValueType.Equals%2A?displayProperty=nameWithType> method, the default test for equality uses reflection to retrieve <xref:System.Reflection.FieldInfo> objects that represent the value type's fields, then compares the field values of two instances.</span></span> <span data-ttu-id="117f9-133">Při kompilování do nativního kódu, nahradí řetězu .NET Native nástroj reflexe kód a metadata statické porovnání hodnot polí.</span><span class="sxs-lookup"><span data-stu-id="117f9-133">When compiling to native code, the .NET Native tool chain replaces the reflection code and metadata with a static comparison of the field values.</span></span>  
  
-   <span data-ttu-id="117f9-134">Pokud je to možné, pokusí se odstranit všechna metadata.</span><span class="sxs-lookup"><span data-stu-id="117f9-134">Where possible, it attempts to eliminate all metadata.</span></span>  
  
-   <span data-ttu-id="117f9-135">Obsahuje v sestaveních konečné aplikaci pouze implementace kód, který je ve skutečnosti vyvolané aplikace.</span><span class="sxs-lookup"><span data-stu-id="117f9-135">It includes in the final app assemblies only the implementation code that is actually invoked by the app.</span></span> <span data-ttu-id="117f9-136">Tímto je ovlivněn zvlášť kód v knihovnách třetích stran a v knihovně tříd rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="117f9-136">This particularly affects code in third-party libraries and in the .NET Framework Class Library.</span></span> <span data-ttu-id="117f9-137">V důsledku toho se aplikace už závisí na knihovny třetích stran nebo úplné Framework knihovně tříd rozhraní .NET; Místo toho v jiných výrobců a knihovny tříd rozhraní .NET Framework je kód místní do aplikace.</span><span class="sxs-lookup"><span data-stu-id="117f9-137">As a result, an application no longer depends on either third-party libraries or the full .NET Framework Class Library; instead, code in third-party and .NET Framework class libraries is now local to the app.</span></span>  
  
-   <span data-ttu-id="117f9-138">Nahradí úplné CLR refactored modul runtime, který obsahuje především uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="117f9-138">It replaces the full CLR with a refactored runtime that primarily contains the garbage collector.</span></span> <span data-ttu-id="117f9-139">Refactored runtime nachází v knihovně s názvem mrt100_app.dll místní aplikace, který je pouze několik set kilobajtů velikost.</span><span class="sxs-lookup"><span data-stu-id="117f9-139">The refactored runtime is found in a library named mrt100_app.dll that is local to the app and is only a few hundred kilobytes in size.</span></span> <span data-ttu-id="117f9-140">To je možné, protože statické propojení eliminuje potřebu mnoho služeb provádí modul common language runtime.</span><span class="sxs-lookup"><span data-stu-id="117f9-140">This is possible because static linking eliminates the need for many of the services performed by the common language runtime.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="117f9-141">.NET native používá stejné uvolňování jako standardní common language runtime.</span><span class="sxs-lookup"><span data-stu-id="117f9-141">.NET Native uses the same garbage collector as the standard common language runtime.</span></span> <span data-ttu-id="117f9-142">V .NET Native uvolňování paměti je ve výchozím nastavení povolená kolekce paměti na pozadí.</span><span class="sxs-lookup"><span data-stu-id="117f9-142">In the .NET Native garbage collector, background garbage collection is enabled by default.</span></span> <span data-ttu-id="117f9-143">Další informace o uvolňování paměti najdete v tématu [základy uvolnění paměti](../../../docs/standard/garbage-collection/fundamentals.md).</span><span class="sxs-lookup"><span data-stu-id="117f9-143">For more information about garbage collection, see [Fundamentals of Garbage Collection](../../../docs/standard/garbage-collection/fundamentals.md).</span></span>  
  
> [!IMPORTANT]
>  <span data-ttu-id="117f9-144">.NET native zkompiluje celý aplikace k nativní aplikaci.</span><span class="sxs-lookup"><span data-stu-id="117f9-144">.NET Native compiles an entire application to a native application.</span></span> <span data-ttu-id="117f9-145">Neumožňuje můžete zkompilovat jednoho sestavení obsahující knihovny tříd do nativního kódu tak, aby ji můžete nezávisle volat ze spravovaného kódu.</span><span class="sxs-lookup"><span data-stu-id="117f9-145">It does not allow you to compile a single assembly that contains a class library to native code so that it can be called independently from managed code.</span></span>  
  
 <span data-ttu-id="117f9-146">Výsledný aplikaci, která je produkovaný řetězu .NET Native nástroj je zapsán do adresář s názvem ilc.out v adresáři ladění nebo verze v adresáři projektu.</span><span class="sxs-lookup"><span data-stu-id="117f9-146">The resulting app that is produced by the .NET Native tool chain is written to a directory named ilc.out in the Debug or Release directory of your project directory.</span></span> <span data-ttu-id="117f9-147">Obsahuje následující soubory:</span><span class="sxs-lookup"><span data-stu-id="117f9-147">It consists of the following files:</span></span>  
  
-   <span data-ttu-id="117f9-148">*\<appName >*.exe, se zakázaným inzerováním spustitelný soubor, který jednoduše převede ovládací prvek speciální `Main` exportovat ve  *\<appName >*.dll.</span><span class="sxs-lookup"><span data-stu-id="117f9-148">*\<appName>*.exe, a stub executable that simply transfers control to a special `Main` export in *\<appName>*.dll.</span></span>  
  
-   <span data-ttu-id="117f9-149">*\<appName >*.dll, Windows dynamické knihovny, který obsahuje všechny kódu aplikace, stejně jako kód z knihovně tříd rozhraní .NET Framework a všechny knihovny třetích stran, které jsou závislé na.</span><span class="sxs-lookup"><span data-stu-id="117f9-149">*\<appName>*.dll, a Windows dynamic link library that contains all your application code, as well as code from the .NET Framework Class Library and any third-party libraries that you have a dependency on.</span></span>  <span data-ttu-id="117f9-150">Také obsahuje kód podpory, jako je kód potřebné pro spolupráci s Windows a k serializaci objektů ve vaší aplikaci.</span><span class="sxs-lookup"><span data-stu-id="117f9-150">It also contains support code, such as the code necessary to interoperate with Windows and to serialize objects in your app.</span></span>  
  
-   <span data-ttu-id="117f9-151">mrt100_app.dll, refactored modul runtime, který poskytuje služby modulu runtime, jako je například uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="117f9-151">mrt100_app.dll, a refactored runtime that provides runtime services such as garbage collection.</span></span>  
  
 <span data-ttu-id="117f9-152">Všechny závislosti jsou zachyceny pomocí APPX manifest aplikace.</span><span class="sxs-lookup"><span data-stu-id="117f9-152">All dependencies are captured by the app's APPX manifest.</span></span>  <span data-ttu-id="117f9-153">Kromě aplikací exe, dll a mrt100_app.dll, které jsou seskupeny přímo v balíček appx to zahrnuje další dva soubory:</span><span class="sxs-lookup"><span data-stu-id="117f9-153">In addition to the application exe, dll, and mrt100_app.dll, which are bundled directly in the appx package, this includes two more files:</span></span>  
  
-   <span data-ttu-id="117f9-154">msvcr140_app.dll, používá mrt100_app.dll knihovny C Runtime (CRT).</span><span class="sxs-lookup"><span data-stu-id="117f9-154">msvcr140_app.dll, the C run-time (CRT) library used by mrt100_app.dll.</span></span> <span data-ttu-id="117f9-155">Je zahrnutý v balíčku odkaz framework.</span><span class="sxs-lookup"><span data-stu-id="117f9-155">It is included by a framework reference in the package.</span></span>  
  
-   <span data-ttu-id="117f9-156">mrt100.dll.</span><span class="sxs-lookup"><span data-stu-id="117f9-156">mrt100.dll.</span></span> <span data-ttu-id="117f9-157">Tato knihovna obsahuje funkce, které může zvýšit výkon mrt100_app.dll, i když jeho absenci nezabrání mrt100_app.dll funguje.</span><span class="sxs-lookup"><span data-stu-id="117f9-157">This library includes functions that can improve the performance of mrt100_app.dll, although its absence does not prevent mrt100_app.dll from functioning.</span></span> <span data-ttu-id="117f9-158">Je načten z adresáře system32 v místním počítači, pokud je k dispozici.</span><span class="sxs-lookup"><span data-stu-id="117f9-158">It is loaded from the system32 directory on the local machine, if it is present.</span></span>  
  
 <span data-ttu-id="117f9-159">Protože řetězu .NET Native nástroj odkazuje implementaci kódu do vaší aplikace jenom v případě, že bude vědět, že aplikace ve skutečnosti vyvolá tento kód a nemusí být součástí aplikace metadata nebo kód implementace vyžaduje v následujících scénářích:</span><span class="sxs-lookup"><span data-stu-id="117f9-159">Because the .NET Native tool chain links implementation code into your app only if it knows that your app actually invokes that code, either the metadata or the implementation code required in the following scenarios may not be included with your app:</span></span>  
  
-   <span data-ttu-id="117f9-160">Reflexe.</span><span class="sxs-lookup"><span data-stu-id="117f9-160">Reflection.</span></span>  
  
-   <span data-ttu-id="117f9-161">Dynamické nebo pozdní vazbou volání.</span><span class="sxs-lookup"><span data-stu-id="117f9-161">Dynamic or late-bound invocation.</span></span>  
  
-   <span data-ttu-id="117f9-162">Serializace a deserializace.</span><span class="sxs-lookup"><span data-stu-id="117f9-162">Serialization and deserialization.</span></span>  
  
-   <span data-ttu-id="117f9-163">Zprostředkovatel komunikace s objekty COM.</span><span class="sxs-lookup"><span data-stu-id="117f9-163">COM interop.</span></span>  
  
 <span data-ttu-id="117f9-164">Chybí-li nutné metadata nebo implementaci kódu v době běhu .NET Native runtime vyvolá výjimku.</span><span class="sxs-lookup"><span data-stu-id="117f9-164">If the necessary metadata or implementation code is absent at runtime, the .NET Native runtime throws an exception.</span></span> <span data-ttu-id="117f9-165">Můžete zakázat tyto výjimky a ujistěte se, že řetězu .NET Native nástroj zahrnuje požadovaná metadata a implementaci kódu pomocí [souboru direktivy modulu runtime](../../../docs/framework/net-native/runtime-directives-rd-xml-configuration-file-reference.md), soubor XML, který určuje elementy program, jehož metadata nebo implementace kód musí být k dispozici v době běhu a přiřadí zásady modulu runtime k nim.</span><span class="sxs-lookup"><span data-stu-id="117f9-165">You can prevent these exceptions, and ensure that the .NET Native tool chain includes the required metadata and implementation code, by using a [runtime directives file](../../../docs/framework/net-native/runtime-directives-rd-xml-configuration-file-reference.md), an XML file that designates the program elements whose metadata or implementation code must be available at runtime and assigns a runtime policy to them.</span></span> <span data-ttu-id="117f9-166">Toto je výchozí soubor direktivy modulu runtime, který je přidán do projektu Windows Store, který se zkompiluje .NET Native řetězcem nástroj:</span><span class="sxs-lookup"><span data-stu-id="117f9-166">The following is the default runtime directives file that is added to a Windows Store project that is compiled by the .NET Native tool chain:</span></span>  
  
```xml  
<Directives xmlns="http://schemas.microsoft.com/netfx/2013/01/metadata">  
  <Application>  
    <Assembly Name="*Application*" Dynamic="Required All" />  
  </Application>  
</Directives>  
```  
  
 <span data-ttu-id="117f9-167">To umožňuje všechny typy, stejně jako všechny jejich členy, ve všech sestaveních ve vašich balíček aplikace pro reflexe a dynamické volání.</span><span class="sxs-lookup"><span data-stu-id="117f9-167">This enables all the types, as well as all their members, in all the assemblies in your app package for reflection and dynamic invocation.</span></span> <span data-ttu-id="117f9-168">Však není povolit, reflexe nebo dynamické aktivace typy v sestavení rozhraní .NET Framework – knihovna tříd.</span><span class="sxs-lookup"><span data-stu-id="117f9-168">However, it does not enable reflection or dynamic activation of types in .NET Framework Class Library assemblies.</span></span> <span data-ttu-id="117f9-169">V mnoha případech je to dostačující.</span><span class="sxs-lookup"><span data-stu-id="117f9-169">In many cases, this is adequate.</span></span>  
  
## <a name="net-native-and-ngen"></a><span data-ttu-id="117f9-170">.NET native a NGEN</span><span class="sxs-lookup"><span data-stu-id="117f9-170">.NET Native and NGEN</span></span>  
 <span data-ttu-id="117f9-171">[(Generátor](../../../docs/framework/tools/ngen-exe-native-image-generator.md) (NGEN) kompiluje sestavení do nativního kódu a nainstaluje je do mezipaměti nativních bitových kopií v místním počítači.</span><span class="sxs-lookup"><span data-stu-id="117f9-171">The [(Native Image Generator](../../../docs/framework/tools/ngen-exe-native-image-generator.md) (NGEN) compiles assemblies to native code and installs them in the native image cache on the local computer.</span></span> <span data-ttu-id="117f9-172">Nicméně i když NGEN, jako jsou .NET Native vytváří nativního kódu, se liší od .NET Native některé důležité způsoby:</span><span class="sxs-lookup"><span data-stu-id="117f9-172">However, although NGEN, like .NET Native, produces native code, it differs from .NET Native in some significant ways:</span></span>  
  
-   <span data-ttu-id="117f9-173">Pokud je k dispozici pro konkrétní metody žádné nativních bitových kopií, NGEN spadne zpět na JITing kódu.</span><span class="sxs-lookup"><span data-stu-id="117f9-173">If no native image is available for a particular method, NGEN falls back to JITing code.</span></span> <span data-ttu-id="117f9-174">To znamená, že nativní bitové kopie musí dál zahrnovat metadata a IL v případě, že NGEN musí vrátit zpět k JIT – kompilace.</span><span class="sxs-lookup"><span data-stu-id="117f9-174">This means that native images must continue to include metadata and IL in the event that NGEN needs to fall back to JIT compilation.</span></span> <span data-ttu-id="117f9-175">Naproti tomu .NET Native vytváří nativní bitové kopie a nepřecházely k JIT – kompilace.</span><span class="sxs-lookup"><span data-stu-id="117f9-175">In contrast, .NET Native produces only native images and does not fall back to JIT compilation.</span></span> <span data-ttu-id="117f9-176">V důsledku toho musí být zachováno pouze metadata jsou požadovaná pro některé úvahy, serializace a spolupráce scénáře.</span><span class="sxs-lookup"><span data-stu-id="117f9-176">As a result, only metadata required for some reflection, serialization, and interop scenarios must be preserved.</span></span>  
  
-   <span data-ttu-id="117f9-177">NGEN nadále spoléhají na úplné common language runtime služby, například sestavení načítání, vzdálenou komunikaci, zprostředkovatel komunikace s objekty, správa paměti, uvolňování paměti a v případě potřeby JIT – kompilace.</span><span class="sxs-lookup"><span data-stu-id="117f9-177">NGEN continues to rely on the full common language runtime for services such as assembly loading, remoting, interop, memory management, garbage collection, and, if necessary, JIT compilation.</span></span> <span data-ttu-id="117f9-178">V rozhraní .NET nativní mnoho z těchto služeb jsou buď nepotřebné (JIT – kompilace) nebo jsou přeložit v čase vytvoření buildu a začlenit i metodu v sestavení aplikace.</span><span class="sxs-lookup"><span data-stu-id="117f9-178">In .NET Native, many of these services are either unnecessary (JIT compilation) or are resolved at build-time and incorporated in the app assembly.</span></span> <span data-ttu-id="117f9-179">Zbývající služby, které nejdůležitější je uvolňování paměti, jsou součástí mnohem menší, rozdělili runtime s názvem mrt100_app.dll.</span><span class="sxs-lookup"><span data-stu-id="117f9-179">The remaining services, the most important of which is garbage collection, are included in a much smaller, refactored runtime named mrt100_app.dll.</span></span>  
  
-   <span data-ttu-id="117f9-180">Obrázky NGEN jsou obvykle křehké.</span><span class="sxs-lookup"><span data-stu-id="117f9-180">NGEN images tend to be fragile.</span></span> <span data-ttu-id="117f9-181">Například opravy nebo změně závislost obvykle vyžaduje, aby sestavení, které ho používají také NGENed znovu.</span><span class="sxs-lookup"><span data-stu-id="117f9-181">For example, a patch or change to a dependency typically requires that the assemblies that use it also be re-NGENed.</span></span> <span data-ttu-id="117f9-182">To platí hlavně systému sestavení v knihovně tříd rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="117f9-182">This is particularly true of system assemblies in the .NET Framework Class Library.</span></span> <span data-ttu-id="117f9-183">.NET Native naopak umožňuje aplikacím zpracovat nezávisle na sobě.</span><span class="sxs-lookup"><span data-stu-id="117f9-183">In contrast, .NET Native allows applications to be served independently of one another.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="117f9-184">Viz také</span><span class="sxs-lookup"><span data-stu-id="117f9-184">See Also</span></span>  
 [<span data-ttu-id="117f9-185">Metadata a komponenty popisující samy sebe</span><span class="sxs-lookup"><span data-stu-id="117f9-185">Metadata and Self-Describing Components</span></span>](../../../docs/standard/metadata-and-self-describing-components.md)  
 [<span data-ttu-id="117f9-186">Uvnitř .NET Native (Video na kanálu 9)</span><span class="sxs-lookup"><span data-stu-id="117f9-186">Inside .NET Native (Channel 9 Video)</span></span>](http://channel9.msdn.com/Shows/Going+Deep/Inside-NET-Native)  
 [<span data-ttu-id="117f9-187">Reflexe a .NET Native</span><span class="sxs-lookup"><span data-stu-id="117f9-187">Reflection and .NET Native</span></span>](../../../docs/framework/net-native/reflection-and-net-native.md)  
 [<span data-ttu-id="117f9-188">Obecné řešení potíží s .NET Native</span><span class="sxs-lookup"><span data-stu-id="117f9-188">.NET Native General Troubleshooting</span></span>](../../../docs/framework/net-native/net-native-general-troubleshooting.md)
