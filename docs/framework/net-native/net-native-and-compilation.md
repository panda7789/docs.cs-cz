---
title: .NET Native a kompilace
ms.date: 03/30/2017
ms.assetid: e38ae4f3-3e3d-42c3-a4b8-db1aa9d84f85
ms.openlocfilehash: cf5c9f05b2f2cb4ca15e4add5b53bc9bdca757a3
ms.sourcegitcommit: 559fcfbe4871636494870a8b716bf7325df34ac5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/30/2019
ms.locfileid: "73128249"
---
# <a name="net-native-and-compilation"></a><span data-ttu-id="0e045-102">.NET Native a kompilace</span><span class="sxs-lookup"><span data-stu-id="0e045-102">.NET Native and Compilation</span></span>

<span data-ttu-id="0e045-103">Windows 8.1 aplikací a desktopových aplikací pro Windows, které cílí na the.NET Framework, se zapisují do konkrétního programovacího jazyka a zkompiluje se do jazyka IL (Intermediate Language).</span><span class="sxs-lookup"><span data-stu-id="0e045-103">Windows 8.1 applications and Windows Desktop applications that target the.NET Framework are written in a particular programming language and compiled into intermediate language (IL).</span></span> <span data-ttu-id="0e045-104">Za běhu zodpovídá kompilátor JIT (just-in-time) za kompilace IL do nativního kódu pro místní počítač těsně před prvním provedením metody.</span><span class="sxs-lookup"><span data-stu-id="0e045-104">At runtime, a just-in-time (JIT) compiler is responsible for compiling the IL into native code for the local machine just before a method is executed for the first time.</span></span> <span data-ttu-id="0e045-105">Naproti tomu řetěz nástrojů .NET Native převádí zdrojový kód do nativního kódu v době kompilace.</span><span class="sxs-lookup"><span data-stu-id="0e045-105">In contrast, the .NET Native tool chain converts source code to native code at compile time.</span></span> <span data-ttu-id="0e045-106">Toto téma porovnává .NET Native s dalšími technologiemi kompilace dostupnými pro .NET Framework aplikace a také poskytuje praktický přehled o tom, jak .NET Native vytváří nativní kód, který vám pomůže pochopit, proč výjimky, ke kterým dochází v kódu zkompilovaném pomocí .NET. Nativní neproběhne v kódu kompilovaném JIT.</span><span class="sxs-lookup"><span data-stu-id="0e045-106">This topic compares .NET Native with other compilation technologies available for .NET Framework apps, and also provides a practical overview of how .NET Native produces native code that can help you understand why exceptions that occur in code compiled with .NET Native do not occur in JIT-compiled code.</span></span>

## <a name="net-native-generating-native-binaries"></a><span data-ttu-id="0e045-107">.NET Native: Generování nativních binárních souborů</span><span class="sxs-lookup"><span data-stu-id="0e045-107">.NET Native: Generating native binaries</span></span>

<span data-ttu-id="0e045-108">Aplikace, která cílí na .NET Framework a která není zkompilována pomocí .NET Nativeho řetězu nástroje, se skládá ze sestavení vaší aplikace, které zahrnuje následující:</span><span class="sxs-lookup"><span data-stu-id="0e045-108">An application that targets the .NET Framework and that is not compiled by using the .NET Native tool chain consists of your application assembly, which includes the following:</span></span>

- <span data-ttu-id="0e045-109">[Metadata](../../standard/metadata-and-self-describing-components.md) , která popisují sestavení, jeho závislosti, typy, které obsahuje, a jejich členy.</span><span class="sxs-lookup"><span data-stu-id="0e045-109">[Metadata](../../standard/metadata-and-self-describing-components.md) that describes the assembly, its dependencies, the types it contains, and their members.</span></span> <span data-ttu-id="0e045-110">Metadata se používají pro reflexi a přístup s pozdní vazbou a v některých případech také pomocí kompilátoru a nástrojů sestavení.</span><span class="sxs-lookup"><span data-stu-id="0e045-110">The metadata is used for reflection and late-bound access, and in some cases by compiler and build tools as well.</span></span>

- <span data-ttu-id="0e045-111">Implementační kód.</span><span class="sxs-lookup"><span data-stu-id="0e045-111">Implementation code.</span></span> <span data-ttu-id="0e045-112">Skládá se z operačních kódů jazyka IL (Intermediate Language).</span><span class="sxs-lookup"><span data-stu-id="0e045-112">This consists of intermediate language (IL) opcodes.</span></span> <span data-ttu-id="0e045-113">Za běhu je kompilátor JIT (just-in-time) překládá do nativního kódu pro cílovou platformu.</span><span class="sxs-lookup"><span data-stu-id="0e045-113">At runtime, the just-in-time (JIT) compiler translates it into native code for the target platform.</span></span>

 <span data-ttu-id="0e045-114">Kromě vašeho hlavního sestavení aplikace aplikace vyžaduje, aby byla k dispozici následující:</span><span class="sxs-lookup"><span data-stu-id="0e045-114">In addition to your main application assembly, an app requires that the following be present:</span></span>

- <span data-ttu-id="0e045-115">Všechny další knihovny tříd nebo sestavení třetích stran, které vaše aplikace vyžaduje.</span><span class="sxs-lookup"><span data-stu-id="0e045-115">Any additional class libraries or third-party assemblies that are required by your app.</span></span> <span data-ttu-id="0e045-116">Tato sestavení podobně zahrnují metadata, která popisují sestavení, jeho typy a jejich členy, a také IL, který implementuje všechny členy typu.</span><span class="sxs-lookup"><span data-stu-id="0e045-116">These assemblies similarly include metadata that describes the assembly, its types, and their members, as well as the IL that implements all type members.</span></span>

- <span data-ttu-id="0e045-117">Knihovna tříd .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="0e045-117">The .NET Framework Class Library.</span></span> <span data-ttu-id="0e045-118">Toto je kolekce sestavení, která jsou nainstalována v místním systému s instalací .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="0e045-118">This is a collection of assemblies that is installed on the local system with the .NET Framework installation.</span></span> <span data-ttu-id="0e045-119">Sestavení zahrnutá do knihovny tříd .NET Framework zahrnují úplnou sadu metadat a implementačního kódu.</span><span class="sxs-lookup"><span data-stu-id="0e045-119">The assemblies included in the .NET Framework Class Library include a complete set of metadata and implementation code.</span></span>

- <span data-ttu-id="0e045-120">Modul CLR (Common Language Runtime)</span><span class="sxs-lookup"><span data-stu-id="0e045-120">The common language runtime.</span></span> <span data-ttu-id="0e045-121">Jedná se o kolekci knihoven DLL, které provádějí takové služby jako načítání sestavení, správu paměti a uvolňování paměti, zpracování výjimek, kompilaci za běhu, vzdálené komunikace a interoperabilitu.</span><span class="sxs-lookup"><span data-stu-id="0e045-121">This is a collection of dynamic link libraries that perform such services as assembly loading, memory management and garbage collection, exception handling, just-in-time compilation, remoting, and interop.</span></span> <span data-ttu-id="0e045-122">Podobně jako u knihovny tříd je modul runtime nainstalován v místním systému jako součást instalace .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="0e045-122">Like the class library, the runtime is installed on the local system as part of the .NET Framework installation.</span></span>

<span data-ttu-id="0e045-123">Všimněte si, že celý modul CLR (Common Language Runtime) i metadata a IL pro všechny typy v sestaveních specifických pro aplikace, sestavení třetích stran a systémová sestavení musí být k dispozici, aby bylo možné aplikaci úspěšně spustit.</span><span class="sxs-lookup"><span data-stu-id="0e045-123">Note that the entire common language runtime, as well as metadata and IL for all types in application-specific assemblies, third-party assemblies, and system assemblies must be present for the app to execute successfully.</span></span>

## <a name="net-native-and-just-in-time-compilation"></a><span data-ttu-id="0e045-124">.NET Native a kompilace za běhu</span><span class="sxs-lookup"><span data-stu-id="0e045-124">.NET Native and just-in-time compilation</span></span>

<span data-ttu-id="0e045-125">Vstup pro .NET Native řetězec nástroje je aplikace pro Windows Store vytvořená kompilátorem C# nebo Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="0e045-125">The input for the .NET Native tool chain is the Windows store app built by the C# or Visual Basic compiler.</span></span> <span data-ttu-id="0e045-126">Jinými slovy řetěz nástrojů .NET Native začíná prováděním, když kompilátor jazyka dokončil kompilaci aplikace pro Windows Store.</span><span class="sxs-lookup"><span data-stu-id="0e045-126">In other words, the .NET Native tool chain begins execution when the language compiler has finished compilation of a Windows Store app.</span></span>

> [!TIP]
> <span data-ttu-id="0e045-127">Vzhledem k tomu, že vstup pro .NET Native je IL a metadata zapsaná do spravovaných sestavení, můžete i nadále provádět vlastní generování kódu nebo jiné vlastní operace pomocí událostí před sestavením nebo po sestavení nebo úpravou souboru projektu MSBuild.</span><span class="sxs-lookup"><span data-stu-id="0e045-127">Because the input to .NET Native is the IL and metadata written to managed assemblies, you can still perform custom code generation or other custom operations by using pre-build or post-build events or by modifying the MSBuild project file.</span></span>
>
> <span data-ttu-id="0e045-128">Nicméně kategorie nástrojů, které upravují IL a zabraňují tak řetězci nástrojů .NET z analýzy IL aplikace, nejsou podporovány.</span><span class="sxs-lookup"><span data-stu-id="0e045-128">However, categories of tools that modify IL and thereby prevent the .NET tool chain from analyzing an app's IL are not supported.</span></span> <span data-ttu-id="0e045-129">Pro tento typ jsou nejvýznamnější nástroje.</span><span class="sxs-lookup"><span data-stu-id="0e045-129">Obfuscators are the most notable tools of this type.</span></span>

<span data-ttu-id="0e045-130">V průběhu převodu aplikace z IL na nativní kód řetěz nástroje .NET Native provádí operace podobné následujícímu:</span><span class="sxs-lookup"><span data-stu-id="0e045-130">In the course of converting an app from IL to native code, the .NET Native tool chain performs operations like the following:</span></span>

- <span data-ttu-id="0e045-131">Pro určité cesty kódu nahrazuje kód, který spoléhá na reflexi a metadata se statickým nativním kódem.</span><span class="sxs-lookup"><span data-stu-id="0e045-131">For certain code paths, it replaces code that relies on reflection and metadata with static native code.</span></span> <span data-ttu-id="0e045-132">Například pokud typ hodnoty nepřepisuje metodu <xref:System.ValueType.Equals%2A?displayProperty=nameWithType>, použije výchozí test rovnosti reflexe k načtení <xref:System.Reflection.FieldInfo> objektů, které reprezentují pole hodnotového typu, a pak porovná hodnoty pole dvou instancí.</span><span class="sxs-lookup"><span data-stu-id="0e045-132">For example, if a value type does not override the <xref:System.ValueType.Equals%2A?displayProperty=nameWithType> method, the default test for equality uses reflection to retrieve <xref:System.Reflection.FieldInfo> objects that represent the value type's fields, then compares the field values of two instances.</span></span> <span data-ttu-id="0e045-133">Při kompilování do nativního kódu řetězec nástroje .NET Native nahradí kód reflexe a metadata statickým porovnáním hodnot polí.</span><span class="sxs-lookup"><span data-stu-id="0e045-133">When compiling to native code, the .NET Native tool chain replaces the reflection code and metadata with a static comparison of the field values.</span></span>

- <span data-ttu-id="0e045-134">Pokud je to možné, pokusí se eliminovat všechna metadata.</span><span class="sxs-lookup"><span data-stu-id="0e045-134">Where possible, it attempts to eliminate all metadata.</span></span>

- <span data-ttu-id="0e045-135">Obsahuje v konečných sestaveních aplikace pouze kód implementace, který je skutečně vyvolán aplikací.</span><span class="sxs-lookup"><span data-stu-id="0e045-135">It includes in the final app assemblies only the implementation code that is actually invoked by the app.</span></span> <span data-ttu-id="0e045-136">To se týká zejména kódu v knihovnách třetích stran a v knihovně tříd .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="0e045-136">This particularly affects code in third-party libraries and in the .NET Framework Class Library.</span></span> <span data-ttu-id="0e045-137">V důsledku toho aplikace již nezávisí na knihovně třetích stran nebo na úplné .NET Framework knihovně tříd. místo toho je kód v knihovnách tříd třetích stran a .NET Framework pro aplikaci nyní místní.</span><span class="sxs-lookup"><span data-stu-id="0e045-137">As a result, an application no longer depends on either third-party libraries or the full .NET Framework Class Library; instead, code in third-party and .NET Framework class libraries is now local to the app.</span></span>

- <span data-ttu-id="0e045-138">Nahrazuje úplný CLR pomocí refaktoringu modulu runtime, který primárně obsahuje systém uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="0e045-138">It replaces the full CLR with a refactored runtime that primarily contains the garbage collector.</span></span> <span data-ttu-id="0e045-139">Refaktored runtime se nachází v knihovně s názvem mrt100_app. dll, která je pro aplikaci místní a má velikost jenom několik set kilobajtů.</span><span class="sxs-lookup"><span data-stu-id="0e045-139">The refactored runtime is found in a library named mrt100_app.dll that is local to the app and is only a few hundred kilobytes in size.</span></span> <span data-ttu-id="0e045-140">To je možné, protože statické propojení eliminuje potřebu mnoha služeb, které provádí modul CLR (Common Language Runtime).</span><span class="sxs-lookup"><span data-stu-id="0e045-140">This is possible because static linking eliminates the need for many of the services performed by the common language runtime.</span></span>

  > [!NOTE]
  > <span data-ttu-id="0e045-141">.NET Native používá stejný systém uvolňování paměti jako standardní modul CLR (Common Language Runtime).</span><span class="sxs-lookup"><span data-stu-id="0e045-141">.NET Native uses the same garbage collector as the standard common language runtime.</span></span> <span data-ttu-id="0e045-142">V .NET Native uvolňování paměti na pozadí je ve výchozím nastavení povolené uvolňování paměti na pozadí.</span><span class="sxs-lookup"><span data-stu-id="0e045-142">In the .NET Native garbage collector, background garbage collection is enabled by default.</span></span> <span data-ttu-id="0e045-143">Další informace o uvolňování paměti najdete v tématu [Základy uvolňování paměti](../../standard/garbage-collection/fundamentals.md).</span><span class="sxs-lookup"><span data-stu-id="0e045-143">For more information about garbage collection, see [Fundamentals of Garbage Collection](../../standard/garbage-collection/fundamentals.md).</span></span>

> [!IMPORTANT]
> <span data-ttu-id="0e045-144">.NET Native zkompiluje celou aplikaci do nativní aplikace.</span><span class="sxs-lookup"><span data-stu-id="0e045-144">.NET Native compiles an entire application to a native application.</span></span> <span data-ttu-id="0e045-145">Neumožňuje kompilovat jedno sestavení, které obsahuje knihovnu tříd do nativního kódu, aby jej bylo možné volat nezávisle ze spravovaného kódu.</span><span class="sxs-lookup"><span data-stu-id="0e045-145">It does not allow you to compile a single assembly that contains a class library to native code so that it can be called independently from managed code.</span></span>

<span data-ttu-id="0e045-146">Výsledná aplikace, která je vytvořena řetězcem nástroje .NET Native, je zapsána do adresáře s názvem ilc. out v adresáři pro ladění nebo vydání adresáře projektu.</span><span class="sxs-lookup"><span data-stu-id="0e045-146">The resulting app that is produced by the .NET Native tool chain is written to a directory named ilc.out in the Debug or Release directory of your project directory.</span></span> <span data-ttu-id="0e045-147">Skládá se z následujících souborů:</span><span class="sxs-lookup"><span data-stu-id="0e045-147">It consists of the following files:</span></span>

- <span data-ttu-id="0e045-148">*\<název_aplikace >* . exe, spustitelný soubor se zástupnými procedurami, který jednoduše přenáší řízení na speciální `Main` export do *\<AppName >* . dll.</span><span class="sxs-lookup"><span data-stu-id="0e045-148">*\<appName>*.exe, a stub executable that simply transfers control to a special `Main` export in *\<appName>*.dll.</span></span>

- <span data-ttu-id="0e045-149">*\<název_aplikace >* . dll, knihovnu dynamického propojení Windows, která obsahuje veškerý kód vaší aplikace, a kód z knihovny tříd .NET Framework a všechny knihovny třetích stran, na kterých máte závislost.</span><span class="sxs-lookup"><span data-stu-id="0e045-149">*\<appName>*.dll, a Windows dynamic link library that contains all your application code, as well as code from the .NET Framework Class Library and any third-party libraries that you have a dependency on.</span></span>  <span data-ttu-id="0e045-150">Obsahuje také kód podpory, jako je třeba kód potřebný pro vzájemnou spolupráci s Windows a serializaci objektů ve vaší aplikaci.</span><span class="sxs-lookup"><span data-stu-id="0e045-150">It also contains support code, such as the code necessary to interoperate with Windows and to serialize objects in your app.</span></span>

- <span data-ttu-id="0e045-151">mrt100_app. dll, refaktored runtime, který poskytuje běhové služby, jako je uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="0e045-151">mrt100_app.dll, a refactored runtime that provides runtime services such as garbage collection.</span></span>

 <span data-ttu-id="0e045-152">Všechny závislosti jsou zachyceny manifestem APPX aplikace.</span><span class="sxs-lookup"><span data-stu-id="0e045-152">All dependencies are captured by the app's APPX manifest.</span></span>  <span data-ttu-id="0e045-153">Kromě exe aplikace, knihovny DLL a mrt100_app. dll, které jsou přímo v balíčku appx, to zahrnuje dva další soubory:</span><span class="sxs-lookup"><span data-stu-id="0e045-153">In addition to the application exe, dll, and mrt100_app.dll, which are bundled directly in the appx package, this includes two more files:</span></span>

- <span data-ttu-id="0e045-154">msvcr140_app. dll, Knihovna CRT (C run-time), kterou používá mrt100_app. dll.</span><span class="sxs-lookup"><span data-stu-id="0e045-154">msvcr140_app.dll, the C run-time (CRT) library used by mrt100_app.dll.</span></span> <span data-ttu-id="0e045-155">Obsahuje odkaz na rozhraní v balíčku.</span><span class="sxs-lookup"><span data-stu-id="0e045-155">It is included by a framework reference in the package.</span></span>

- <span data-ttu-id="0e045-156">mrt100. dll.</span><span class="sxs-lookup"><span data-stu-id="0e045-156">mrt100.dll.</span></span> <span data-ttu-id="0e045-157">Tato knihovna obsahuje funkce, které mohou zlepšit výkon souboru mrt100_app. dll, i když jeho absence nebrání v fungování mrt100_app. dll.</span><span class="sxs-lookup"><span data-stu-id="0e045-157">This library includes functions that can improve the performance of mrt100_app.dll, although its absence does not prevent mrt100_app.dll from functioning.</span></span> <span data-ttu-id="0e045-158">Je načten z adresáře System32 na místním počítači, pokud je k dispozici.</span><span class="sxs-lookup"><span data-stu-id="0e045-158">It is loaded from the system32 directory on the local machine, if it is present.</span></span>

<span data-ttu-id="0e045-159">Vzhledem k tomu, že řetězec nástroje .NET Native propojuje implementační kód do vaší aplikace pouze v případě, že ví, že vaše aplikace skutečně vyvolá tento kód, nemusí být do vaší aplikace zahrnuty metadata nebo implementační kód vyžadovaný v následujících scénářích:</span><span class="sxs-lookup"><span data-stu-id="0e045-159">Because the .NET Native tool chain links implementation code into your app only if it knows that your app actually invokes that code, either the metadata or the implementation code required in the following scenarios may not be included with your app:</span></span>

- <span data-ttu-id="0e045-160">Operace.</span><span class="sxs-lookup"><span data-stu-id="0e045-160">Reflection.</span></span>

- <span data-ttu-id="0e045-161">Dynamické nebo pozdní vyvolání vazby.</span><span class="sxs-lookup"><span data-stu-id="0e045-161">Dynamic or late-bound invocation.</span></span>

- <span data-ttu-id="0e045-162">Serializace a deserializace.</span><span class="sxs-lookup"><span data-stu-id="0e045-162">Serialization and deserialization.</span></span>

- <span data-ttu-id="0e045-163">Zprostředkovatel komunikace s objekty COM.</span><span class="sxs-lookup"><span data-stu-id="0e045-163">COM interop.</span></span>

<span data-ttu-id="0e045-164">Pokud v době běhu chybí potřebná metadata nebo implementační kód, modul runtime .NET Native vyvolá výjimku.</span><span class="sxs-lookup"><span data-stu-id="0e045-164">If the necessary metadata or implementation code is absent at runtime, the .NET Native runtime throws an exception.</span></span> <span data-ttu-id="0e045-165">Můžete zabránit těmto výjimkám a zajistit, aby řetěz nástrojů .NET Native zahrnoval požadovaná metadata a implementační kód, pomocí [souboru XML direktiv](runtime-directives-rd-xml-configuration-file-reference.md), soubor XML, který určí prvky programu, jejichž metadata nebo implementace kód musí být k dispozici za běhu a přiřadí jim běhové zásady.</span><span class="sxs-lookup"><span data-stu-id="0e045-165">You can prevent these exceptions, and ensure that the .NET Native tool chain includes the required metadata and implementation code, by using a [runtime directives file](runtime-directives-rd-xml-configuration-file-reference.md), an XML file that designates the program elements whose metadata or implementation code must be available at runtime and assigns a runtime policy to them.</span></span> <span data-ttu-id="0e045-166">Následuje výchozí soubor direktiv modulu runtime, který je přidán do projektu Windows Store, který je zkompilován pomocí .NET Nativeho řetězu nástrojů:</span><span class="sxs-lookup"><span data-stu-id="0e045-166">The following is the default runtime directives file that is added to a Windows Store project that is compiled by the .NET Native tool chain:</span></span>

```xml
<Directives xmlns="http://schemas.microsoft.com/netfx/2013/01/metadata">
  <Application>
    <Assembly Name="*Application*" Dynamic="Required All" />
  </Application>
</Directives>
```

<span data-ttu-id="0e045-167">To umožňuje všem typům a všem jejich členům ve všech sestaveních v balíčku aplikace pro reflexi a dynamické vyvolání.</span><span class="sxs-lookup"><span data-stu-id="0e045-167">This enables all the types, as well as all their members, in all the assemblies in your app package for reflection and dynamic invocation.</span></span> <span data-ttu-id="0e045-168">Nicméně nepovoluje reflexi nebo dynamickou aktivaci typů v sestaveních knihovny tříd .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="0e045-168">However, it does not enable reflection or dynamic activation of types in .NET Framework Class Library assemblies.</span></span> <span data-ttu-id="0e045-169">V mnoha případech to postačuje.</span><span class="sxs-lookup"><span data-stu-id="0e045-169">In many cases, this is adequate.</span></span>

## <a name="net-native-and-ngen"></a><span data-ttu-id="0e045-170">.NET Native a NGEN</span><span class="sxs-lookup"><span data-stu-id="0e045-170">.NET Native and NGEN</span></span>

<span data-ttu-id="0e045-171">[(Generátor nativních bitových kopií](../tools/ngen-exe-native-image-generator.md) (NGen) zkompiluje sestavení do nativního kódu a nainstaluje je do mezipaměti nativních imagí v místním počítači.</span><span class="sxs-lookup"><span data-stu-id="0e045-171">The [(Native Image Generator](../tools/ngen-exe-native-image-generator.md) (NGEN) compiles assemblies to native code and installs them in the native image cache on the local computer.</span></span> <span data-ttu-id="0e045-172">Nicméně i když NGEN, jako je například .NET Native, vytváří nativní kód, liší se od .NET Native nějakými značnými způsoby:</span><span class="sxs-lookup"><span data-stu-id="0e045-172">However, although NGEN, like .NET Native, produces native code, it differs from .NET Native in some significant ways:</span></span>

- <span data-ttu-id="0e045-173">Pokud není pro konkrétní metodu k dispozici žádná nativní image, NGEN se vrátí k JITing kódu.</span><span class="sxs-lookup"><span data-stu-id="0e045-173">If no native image is available for a particular method, NGEN falls back to JITing code.</span></span> <span data-ttu-id="0e045-174">To znamená, že nativní bitové kopie musí nadále zahrnovat metadata a IL v případě, že NGEN musí přejít zpět na kompilaci JIT.</span><span class="sxs-lookup"><span data-stu-id="0e045-174">This means that native images must continue to include metadata and IL in the event that NGEN needs to fall back to JIT compilation.</span></span> <span data-ttu-id="0e045-175">Naproti tomu .NET Native vytvoří pouze nativní bitové kopie a nevrátí se do kompilace JIT.</span><span class="sxs-lookup"><span data-stu-id="0e045-175">In contrast, .NET Native produces only native images and does not fall back to JIT compilation.</span></span> <span data-ttu-id="0e045-176">V důsledku toho musí být zachována pouze metadata požadovaná pro scénáře pro odraz, serializaci a spolupráci.</span><span class="sxs-lookup"><span data-stu-id="0e045-176">As a result, only metadata required for some reflection, serialization, and interop scenarios must be preserved.</span></span>

- <span data-ttu-id="0e045-177">Služba NGEN nadále spoléhá na úplný modul CLR (Common Language Runtime) pro služby, jako je načítání sestavení, Vzdálená komunikace, spolupráce, Správa paměti, uvolňování paměti a v případě potřeby i kompilace JIT.</span><span class="sxs-lookup"><span data-stu-id="0e045-177">NGEN continues to rely on the full common language runtime for services such as assembly loading, remoting, interop, memory management, garbage collection, and, if necessary, JIT compilation.</span></span> <span data-ttu-id="0e045-178">V .NET Native mnohé z těchto služeb jsou buď zbytečné (kompilace JIT), nebo jsou vyřešeny v době sestavení a zahrnuty v sestavení aplikace.</span><span class="sxs-lookup"><span data-stu-id="0e045-178">In .NET Native, many of these services are either unnecessary (JIT compilation) or are resolved at build-time and incorporated in the app assembly.</span></span> <span data-ttu-id="0e045-179">Zbývající služby, jejichž nejdůležitější z nich je uvolňování paměti, jsou součástí mnohem menšího, refaktoringového modulu runtime s názvem mrt100_app. dll.</span><span class="sxs-lookup"><span data-stu-id="0e045-179">The remaining services, the most important of which is garbage collection, are included in a much smaller, refactored runtime named mrt100_app.dll.</span></span>

- <span data-ttu-id="0e045-180">Image NGEN mají v úmyslu být křehké.</span><span class="sxs-lookup"><span data-stu-id="0e045-180">NGEN images tend to be fragile.</span></span> <span data-ttu-id="0e045-181">Například oprava nebo změna závislosti obvykle vyžaduje, aby byla sestavení, která používají, také znovu NGENed.</span><span class="sxs-lookup"><span data-stu-id="0e045-181">For example, a patch or change to a dependency typically requires that the assemblies that use it also be re-NGENed.</span></span> <span data-ttu-id="0e045-182">To platí zejména pro systémová sestavení v knihovně tříd .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="0e045-182">This is particularly true of system assemblies in the .NET Framework Class Library.</span></span> <span data-ttu-id="0e045-183">Naproti tomu .NET Native umožňuje aplikacím, aby byly obsluhovány nezávisle na sobě.</span><span class="sxs-lookup"><span data-stu-id="0e045-183">In contrast, .NET Native allows applications to be served independently of one another.</span></span>

## <a name="see-also"></a><span data-ttu-id="0e045-184">Viz také:</span><span class="sxs-lookup"><span data-stu-id="0e045-184">See also</span></span>

- [<span data-ttu-id="0e045-185">Metadata a komponenty popisující samy sebe</span><span class="sxs-lookup"><span data-stu-id="0e045-185">Metadata and Self-Describing Components</span></span>](../../standard/metadata-and-self-describing-components.md)
- [<span data-ttu-id="0e045-186">Uvnitř .NET Native (video kanálu 9)</span><span class="sxs-lookup"><span data-stu-id="0e045-186">Inside .NET Native (Channel 9 Video)</span></span>](https://channel9.msdn.com/Shows/Going+Deep/Inside-NET-Native)
- [<span data-ttu-id="0e045-187">Reflexe a .NET Native</span><span class="sxs-lookup"><span data-stu-id="0e045-187">Reflection and .NET Native</span></span>](reflection-and-net-native.md)
- [<span data-ttu-id="0e045-188">Obecné řešení potíží s .NET Native</span><span class="sxs-lookup"><span data-stu-id="0e045-188">.NET Native General Troubleshooting</span></span>](net-native-general-troubleshooting.md)
