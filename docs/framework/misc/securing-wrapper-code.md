---
title: Zabezpečení kódu obálky
ms.date: 03/30/2017
helpviewer_keywords:
- security [.NET Framework], wrapper code
- wrapper code, securing
- secure coding, wrapper code
- code security, wrapper code
ms.assetid: 1df6c516-5bba-48bd-b450-1070e04b7389
ms.openlocfilehash: 3d38a4d4fd33798cf5987f5ce67305725ad9daec
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/14/2020
ms.locfileid: "79399908"
---
# <a name="securing-wrapper-code"></a><span data-ttu-id="d53c0-102">Zabezpečení kódu obálky</span><span class="sxs-lookup"><span data-stu-id="d53c0-102">Securing Wrapper Code</span></span>
[!INCLUDE[net_security_note](../../../includes/net-security-note-md.md)]  
  
 <span data-ttu-id="d53c0-103">Kód obálky, zejména pokud obálka má vyšší důvěryhodnost než kód, který ji používá, můžete otevřít jedinečnou sadu slabých míst zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="d53c0-103">Wrapper code, especially where the wrapper has higher trust than code that uses it, can open a unique set of security weaknesses.</span></span> <span data-ttu-id="d53c0-104">Cokoli, co se provádí jménem volajícího, kde omezená oprávnění volajícího nejsou zahrnuta do příslušné kontroly zabezpečení, je potenciální slabinou, která má být zneužita.</span><span class="sxs-lookup"><span data-stu-id="d53c0-104">Anything done on behalf of a caller, where the caller's limited permissions are not included in the appropriate security check, is a potential weakness to be exploited.</span></span>  
  
 <span data-ttu-id="d53c0-105">Nikdy nepovolte něco prostřednictvím obálky, že volající nemohl udělat sám.</span><span class="sxs-lookup"><span data-stu-id="d53c0-105">Never enable something through the wrapper that the caller could not do itself.</span></span> <span data-ttu-id="d53c0-106">To je zvláštní nebezpečí, když dělá něco, co zahrnuje omezenou bezpečnostní kontrolu, na rozdíl od plného zásobníku chůze poptávky.</span><span class="sxs-lookup"><span data-stu-id="d53c0-106">This is a special danger when doing something that involves a limited security check, as opposed to a full stack walk demand.</span></span> <span data-ttu-id="d53c0-107">Pokud jsou zapojeny jednoúrovňové kontroly, zanesení kódu obálky mezi skutečný volající a daný prvek rozhraní API může snadno způsobit, že kontrola zabezpečení bude úspěšná, pokud by neměla, což oslabí zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="d53c0-107">When single-level checks are involved, interposing the wrapper code between the real caller and the API element in question can easily cause the security check to succeed when it should not, thereby weakening security.</span></span>  
  
## <a name="delegates"></a><span data-ttu-id="d53c0-108">Delegáty</span><span class="sxs-lookup"><span data-stu-id="d53c0-108">Delegates</span></span>  
 <span data-ttu-id="d53c0-109">Zabezpečení delegáta se liší mezi verzemi rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="d53c0-109">Delegate security differs between versions of the .NET Framework.</span></span>  <span data-ttu-id="d53c0-110">Tato část popisuje různé chování delegáta a související aspekty zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="d53c0-110">This section describes the different delegate behaviors and associated security considerations.</span></span>  
  
### <a name="in-version-10-and-11-of-the-net-framework"></a><span data-ttu-id="d53c0-111">Ve verzích 1.0 a 1.1 rozhraní .NET Framework</span><span class="sxs-lookup"><span data-stu-id="d53c0-111">In version 1.0 and 1.1 of the .NET Framework</span></span>  
 <span data-ttu-id="d53c0-112">Verze 1.0 a 1.1 rozhraní .NET Framework provádějí následující akce zabezpečení proti autorovi delegáta a volajícímu delegáta.</span><span class="sxs-lookup"><span data-stu-id="d53c0-112">Version 1.0 and 1.1 of the .NET Framework perform the following security actions against a delegate creator and a delegate caller.</span></span>  
  
- <span data-ttu-id="d53c0-113">Při vytvoření delegáta jsou požadavky na propojení zabezpečení na metodu cíle delegáta prováděny proti grantové sadě tvůrce delegáta.</span><span class="sxs-lookup"><span data-stu-id="d53c0-113">When a delegate is created, security link demands on the delegate target method are performed against the grant set of the delegate creator.</span></span>  <span data-ttu-id="d53c0-114">Nesplnění akce zabezpečení má <xref:System.Security.SecurityException>za následek .</span><span class="sxs-lookup"><span data-stu-id="d53c0-114">Failure to satisfy the security action results in a <xref:System.Security.SecurityException>.</span></span>  
  
- <span data-ttu-id="d53c0-115">Při vyvolání delegáta jsou provedeny všechny existující požadavky na zabezpečení volajícího delegáta.</span><span class="sxs-lookup"><span data-stu-id="d53c0-115">When the delegate is invoked, any existing security demands on the delegate caller are performed.</span></span>  
  
 <span data-ttu-id="d53c0-116">Vždy, když <xref:System.Delegate> váš kód trvá z méně důvěryhodného kódu, který jej může volat, ujistěte se, že nepovolujete méně důvěryhodný kód eskalovat jeho oprávnění.</span><span class="sxs-lookup"><span data-stu-id="d53c0-116">Whenever your code takes a <xref:System.Delegate> from less-trusted code that might call it, make sure that you are not enabling the less-trusted code to escalate its permissions.</span></span> <span data-ttu-id="d53c0-117">Pokud vezmete delegáta a použijete jej později, kód, který delegáta vytvořil, není v zásobníku volání a jeho oprávnění nebudou testována, pokud se kód v delegátovi nebo pod ním pokusí o chráněnou operaci.</span><span class="sxs-lookup"><span data-stu-id="d53c0-117">If you take a delegate and use it later, the code that created the delegate is not on the call stack and its permissions will not be tested if code in or under the delegate attempts a protected operation.</span></span> <span data-ttu-id="d53c0-118">Pokud váš kód a kód volajícího mají vyšší oprávnění než tvůrce, tvůrce může organizovat cestu volání, aniž by byl součástí zásobníku volání.</span><span class="sxs-lookup"><span data-stu-id="d53c0-118">If your code and the caller code have higher privileges than the creator, the creator can orchestrate the call path without being part of the call stack.</span></span>  
  
### <a name="in-version-20-and-later-versions-of-the-net-framework"></a><span data-ttu-id="d53c0-119">Ve verzi 2.0 a novějších verzích rozhraní .NET Framework</span><span class="sxs-lookup"><span data-stu-id="d53c0-119">In version 2.0 and later versions of the .NET Framework</span></span>  
 <span data-ttu-id="d53c0-120">Na rozdíl od předchozích verzí verze 2.0 a novější verze rozhraní .NET Framework provádí akci zabezpečení proti delegátovi při vytvoření a volání delegáta.</span><span class="sxs-lookup"><span data-stu-id="d53c0-120">Unlike previous versions, version 2.0 and later versions of the .NET Framework performs security action against the delegate creator when the delegate is created and called.</span></span>  
  
- <span data-ttu-id="d53c0-121">Při vytvoření delegáta jsou požadavky na propojení zabezpečení na metodu cíle delegáta prováděny proti grantové sadě tvůrce delegáta.</span><span class="sxs-lookup"><span data-stu-id="d53c0-121">When a delegate is created, security link demands on the delegate target method are performed against the grant set of the delegate creator.</span></span>  <span data-ttu-id="d53c0-122">Nesplnění akce zabezpečení má <xref:System.Security.SecurityException>za následek .</span><span class="sxs-lookup"><span data-stu-id="d53c0-122">Failure to satisfy the security action results in a <xref:System.Security.SecurityException>.</span></span>  
  
- <span data-ttu-id="d53c0-123">Sada grantů tvůrce delegáta je také zachycena během vytváření delegáta a uložena u delegáta.</span><span class="sxs-lookup"><span data-stu-id="d53c0-123">The delegate creator's grant set is also captured during delegate creation and stored with the delegate.</span></span>  
  
- <span data-ttu-id="d53c0-124">Při vyvolání delegáta je zachycená grantová sada tvůrce delegáta nejprve vyhodnocena proti všem požadavkům v aktuálním kontextu, pokud tvůrce delegáta a volající patří do různých sestavení.</span><span class="sxs-lookup"><span data-stu-id="d53c0-124">When the delegate is invoked, the delegate creator's captured grant set is first evaluated against any demands in the current context if the delegate creator and caller belong to different assemblies.</span></span>  <span data-ttu-id="d53c0-125">Dále jsou prováděny všechny existující požadavky na zabezpečení volajícího delegáta.</span><span class="sxs-lookup"><span data-stu-id="d53c0-125">Next, any existing security demands on the delegate caller are performed.</span></span>  
  
## <a name="link-demands-and-wrappers"></a><span data-ttu-id="d53c0-126">Požadavky na propojení a obálky</span><span class="sxs-lookup"><span data-stu-id="d53c0-126">Link demands and wrappers</span></span>  
 <span data-ttu-id="d53c0-127">Zvláštní případ ochrany s požadavky na propojení byl posílen v bezpečnostní infrastruktuře, ale je stále zdrojem možné slabosti ve vašem kódu.</span><span class="sxs-lookup"><span data-stu-id="d53c0-127">A special protection case with link demands has been strengthened in the security infrastructure, but it is still a source of possible weakness in your code.</span></span>  
  
 <span data-ttu-id="d53c0-128">Pokud plně důvěryhodný kód volá vlastnost, událost nebo metodu chráněnou [linkdemand](link-demands.md), volání proběhne úspěšně, pokud je splněna kontrola oprávnění **LinkDemand** pro volajícího.</span><span class="sxs-lookup"><span data-stu-id="d53c0-128">If fully trusted code calls a property, event, or method protected by a [LinkDemand](link-demands.md), the call succeeds if the **LinkDemand** permission check for the caller is satisfied.</span></span> <span data-ttu-id="d53c0-129">Navíc pokud plně důvěryhodný kód zpřístupňuje třídu, která přebírá název vlastnosti a volá jeho **get** přistupující objekt pomocí reflexe, toto volání **get** přistupujícího objektu úspěšné i v případě, že uživatelský kód nemá právo na přístup k této vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="d53c0-129">Additionally, if the fully trusted code exposes a class that takes the name of a property and calls its **get** accessor using reflection, that call to the **get** accessor succeeds even though the user code does not have the right to access this property.</span></span> <span data-ttu-id="d53c0-130">Důvodem je, **že LinkDemand** kontroluje pouze bezprostřední volajícího, což je plně důvěryhodný kód.</span><span class="sxs-lookup"><span data-stu-id="d53c0-130">This is because the **LinkDemand** checks only the immediate caller, which is the fully trusted code.</span></span> <span data-ttu-id="d53c0-131">V podstatě plně důvěryhodný kód provádí privilegované volání jménem uživatelského kódu, aniž by se ujistil, že uživatelský kód má právo toto volání provést.</span><span class="sxs-lookup"><span data-stu-id="d53c0-131">In essence, the fully trusted code is making a privileged call on behalf of user code without making sure that the user code has the right to make that call.</span></span>  
  
 <span data-ttu-id="d53c0-132">Chcete-li zabránit takové bezpečnostní díry, za běhu společného jazyka rozšiřuje kontrolu do úplné hojné poptávky procházení zásobníku na jakékoli nepřímé volání metody, konstruktoru, vlastnosti nebo události chráněné **LinkDemand**.</span><span class="sxs-lookup"><span data-stu-id="d53c0-132">To help prevent such security holes, the common language runtime extends the check into a full stack-walking demand on any indirect call to a method, constructor, property, or event protected by a **LinkDemand**.</span></span> <span data-ttu-id="d53c0-133">Tato ochrana vznikne některé náklady na výkon a změní sémantiku kontroly zabezpečení; úplné procházení zásobníku může selhat tam, kde by prošla rychlejší jednoúrovňová kontrola.</span><span class="sxs-lookup"><span data-stu-id="d53c0-133">This protection incurs some performance costs and changes the semantics of the security check; the full stack-walk demand might fail where the faster, one-level check would have passed.</span></span>  
  
## <a name="assembly-loading-wrappers"></a><span data-ttu-id="d53c0-134">Obálky pro načítání sestavy</span><span class="sxs-lookup"><span data-stu-id="d53c0-134">Assembly loading wrappers</span></span>  
 <span data-ttu-id="d53c0-135">Několik metod používaných k <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>načtení spravovaného kódu, včetně načtení sestavení s důkazy volajícího.</span><span class="sxs-lookup"><span data-stu-id="d53c0-135">Several methods used to load managed code, including <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>, load assemblies with the evidence of the caller.</span></span> <span data-ttu-id="d53c0-136">Pokud zabalíte některou z těchto metod, systém zabezpečení může použít udělení oprávnění vašeho kódu namísto oprávnění volajícího k obalu k načtení sestavení.</span><span class="sxs-lookup"><span data-stu-id="d53c0-136">If you wrap any of these methods, the security system could use your code's permission grant, instead of the permissions of the caller to your wrapper, to load the assemblies.</span></span> <span data-ttu-id="d53c0-137">Méně důvěryhodnému kódu byste neměli povolit načtení kódu, kterému jsou udělena vyšší oprávnění než oprávnění volajícího do obálky.</span><span class="sxs-lookup"><span data-stu-id="d53c0-137">You should not allow less-trusted code to load code that is granted higher permissions than those of the caller to your wrapper.</span></span>  
  
 <span data-ttu-id="d53c0-138">Jakýkoli kód, který má úplný vztah důvěryhodnosti nebo výrazně vyšší důvěryhodnost než potenciální volající (včetně volajícího na úrovni oprávnění k Internetu), by tímto způsobem mohl oslabit zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="d53c0-138">Any code that has full trust or significantly higher trust than a potential caller (including an Internet-permissions-level caller) could weaken security in this way.</span></span> <span data-ttu-id="d53c0-139">Pokud váš kód má veřejnou metodu, která trvá bajt pole a předá jej **Assembly.Load**, a tím vytvořit sestavení na účet volajícího, může přerušit zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="d53c0-139">If your code has a public method that takes a byte array and passes it to **Assembly.Load**, thereby creating an assembly on the caller's behalf, it might break security.</span></span>  
  
 <span data-ttu-id="d53c0-140">Tento problém se týká následujících prvků rozhraní API:</span><span class="sxs-lookup"><span data-stu-id="d53c0-140">This issue applies to the following API elements:</span></span>  
  
- <xref:System.AppDomain.DefineDynamicAssembly%2A?displayProperty=nameWithType>  
  
- <xref:System.AppDomain.Load%2A?displayProperty=nameWithType>  
  
- <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>  
  
- <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>  
  
## <a name="demand-vs-linkdemand"></a><span data-ttu-id="d53c0-141">Požadavek proti LinkDemand</span><span class="sxs-lookup"><span data-stu-id="d53c0-141">Demand vs. LinkDemand</span></span>  
 <span data-ttu-id="d53c0-142">Deklarativní zabezpečení nabízí dva druhy kontrol zabezpečení, které jsou podobné, ale provádějí velmi odlišné kontroly.</span><span class="sxs-lookup"><span data-stu-id="d53c0-142">Declarative security offers two kinds of security checks that are similar but perform very different checks.</span></span> <span data-ttu-id="d53c0-143">Měli byste pochopit oba formuláře, protože nesprávná volba může mít za následek slabé zabezpečení nebo ztrátu výkonu.</span><span class="sxs-lookup"><span data-stu-id="d53c0-143">You should understand both forms because the wrong choice can result in weak security or performance loss.</span></span>  
  
 <span data-ttu-id="d53c0-144">Deklarativní zabezpečení nabízí následující kontroly zabezpečení:</span><span class="sxs-lookup"><span data-stu-id="d53c0-144">Declarative security offers the following security checks:</span></span>  
  
- <span data-ttu-id="d53c0-145"><xref:System.Security.Permissions.SecurityAction.Demand>určuje procházení zásobníku zabezpečení přístupu kódu.</span><span class="sxs-lookup"><span data-stu-id="d53c0-145"><xref:System.Security.Permissions.SecurityAction.Demand> specifies the code access security stack walk.</span></span> <span data-ttu-id="d53c0-146">Všichni volající v zásobníku musí mít zadané oprávnění nebo identitu předat.</span><span class="sxs-lookup"><span data-stu-id="d53c0-146">All callers on the stack must have the specified permission or identity to pass.</span></span> <span data-ttu-id="d53c0-147">**Poptávka** dochází při každém volání, protože zásobníku může obsahovat různé volající.</span><span class="sxs-lookup"><span data-stu-id="d53c0-147">**Demand** occurs on every call because the stack might contain different callers.</span></span> <span data-ttu-id="d53c0-148">Pokud voláte metodu opakovaně, tato kontrola zabezpečení dochází pokaždé.</span><span class="sxs-lookup"><span data-stu-id="d53c0-148">If you call a method repeatedly, this security check occurs each time.</span></span> <span data-ttu-id="d53c0-149">**Poptávka** je dobrou ochranou proti lákavě útokům; neoprávněný kód se snaží dostat přes to bude detekován.</span><span class="sxs-lookup"><span data-stu-id="d53c0-149">**Demand** is good protection against luring attacks; unauthorized code trying to get through it will be detected.</span></span>  
  
- <span data-ttu-id="d53c0-150">[LinkDemand](link-demands.md) se stane v době kompilace just-in-time (JIT) a zkontroluje pouze okamžité volajícího.</span><span class="sxs-lookup"><span data-stu-id="d53c0-150">[LinkDemand](link-demands.md) happens at just-in-time (JIT) compilation time and checks only the immediate caller.</span></span> <span data-ttu-id="d53c0-151">Tato kontrola zabezpečení nekontroluje volajícího volajícího.</span><span class="sxs-lookup"><span data-stu-id="d53c0-151">This security check does not check the caller's caller.</span></span> <span data-ttu-id="d53c0-152">Jakmile tato kontrola projde, neexistuje žádná další režie zabezpečení bez ohledu na to, kolikrát může volající volat.</span><span class="sxs-lookup"><span data-stu-id="d53c0-152">Once this check passes, there is no additional security overhead no matter how many times the caller might call.</span></span> <span data-ttu-id="d53c0-153">Neexistuje však ani žádná ochrana před lákavou útoky.</span><span class="sxs-lookup"><span data-stu-id="d53c0-153">However, there is also no protection from luring attacks.</span></span> <span data-ttu-id="d53c0-154">S **LinkDemand**, jakýkoli kód, který projde testem a může odkazovat na váš kód může potenciálně přerušit zabezpečení tím, že škodlivý kód pro volání pomocí autorizovaného kódu.</span><span class="sxs-lookup"><span data-stu-id="d53c0-154">With **LinkDemand**, any code that passes the test and can reference your code can potentially break security by allowing malicious code to call using the authorized code.</span></span> <span data-ttu-id="d53c0-155">Proto nepoužívejte **LinkDemand,** pokud všechny možné nedostatky lze důkladně vyhnout.</span><span class="sxs-lookup"><span data-stu-id="d53c0-155">Therefore, do not use **LinkDemand** unless all the possible weaknesses can be thoroughly avoided.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="d53c0-156">V rozhraní .NET Framework 4 byly požadavky <xref:System.Security.SecurityCriticalAttribute> na <xref:System.Security.SecurityRuleSet.Level2> propojení nahrazeny atributem v sestaveních.</span><span class="sxs-lookup"><span data-stu-id="d53c0-156">In the .NET Framework 4, link demands have been replaced by the <xref:System.Security.SecurityCriticalAttribute> attribute in <xref:System.Security.SecurityRuleSet.Level2> assemblies.</span></span> <span data-ttu-id="d53c0-157">Je <xref:System.Security.SecurityCriticalAttribute> ekvivalentní požadavku na propojení pro úplnou důvěryhodnost; však ovlivňuje také pravidla dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="d53c0-157">The <xref:System.Security.SecurityCriticalAttribute> is equivalent to a link demand for full trust; however, it also affects inheritance rules.</span></span> <span data-ttu-id="d53c0-158">Další informace o této změně naleznete v [tématu Transparentní kód zabezpečení, úroveň 2](security-transparent-code-level-2.md).</span><span class="sxs-lookup"><span data-stu-id="d53c0-158">For more information about this change, see [Security-Transparent Code, Level 2](security-transparent-code-level-2.md).</span></span>  
  
 <span data-ttu-id="d53c0-159">Zvláštní opatření požadovaná při používání **LinkDemand** musí být naprogramována individuálně; bezpečnostní systém může pomoci s vymáháním.</span><span class="sxs-lookup"><span data-stu-id="d53c0-159">The extra precautions required when using **LinkDemand** must be programmed individually; the security system can help with enforcement.</span></span> <span data-ttu-id="d53c0-160">Každá chyba otevírá bezpečnostní slabinu.</span><span class="sxs-lookup"><span data-stu-id="d53c0-160">Any mistake opens a security weakness.</span></span> <span data-ttu-id="d53c0-161">Veškerý autorizovaný kód, který používá váš kód, musí být zodpovědný za implementaci dalšího zabezpečení následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="d53c0-161">All authorized code that uses your code must be responsible for implementing additional security by doing the following:</span></span>  
  
- <span data-ttu-id="d53c0-162">Omezení přístupu volajícího kódu ke třídě nebo sestavení.</span><span class="sxs-lookup"><span data-stu-id="d53c0-162">Restricting the calling code's access to the class or assembly.</span></span>  
  
- <span data-ttu-id="d53c0-163">Umístění stejné kontroly zabezpečení na volající kód, který se zobrazí na kód volány a zavazující jeho volající, aby tak učinily.</span><span class="sxs-lookup"><span data-stu-id="d53c0-163">Placing the same security checks on the calling code that appear on the code being called and obligating its callers to do so.</span></span> <span data-ttu-id="d53c0-164">Například pokud napíšete kód, který volá metodu, <xref:System.Security.Permissions.SecurityPermission> která <xref:System.Security.Permissions.SecurityPermissionFlag.UnmanagedCode> je chráněna **LinkDemand** pro s příznakem zadaným, vaše metoda by měla také **vytvořit LinkDemand** (nebo **Poptávka**, která je silnější) pro toto oprávnění.</span><span class="sxs-lookup"><span data-stu-id="d53c0-164">For example, if you write code that calls a method that is protected with a **LinkDemand** for the <xref:System.Security.Permissions.SecurityPermission> with the <xref:System.Security.Permissions.SecurityPermissionFlag.UnmanagedCode> flag specified, your method should also make a **LinkDemand** (or **Demand**, which is stronger) for this permission.</span></span> <span data-ttu-id="d53c0-165">Výjimkou je, pokud váš kód používá **LinkDemand**chráněné metody v omezeném způsobem, který se rozhodnete je bezpečné, vzhledem k jiné mechanismy ochrany zabezpečení (například požadavky) ve vašem kódu.</span><span class="sxs-lookup"><span data-stu-id="d53c0-165">The exception is if your code uses the **LinkDemand**-protected method in a limited way that you decide is safe, given other security protection mechanisms (such as demands) in your code.</span></span> <span data-ttu-id="d53c0-166">V tomto výjimečném případě volající přebírá odpovědnost za oslabení ochrany zabezpečení na základní kód.</span><span class="sxs-lookup"><span data-stu-id="d53c0-166">In this exceptional case, the caller takes responsibility in weakening the security protection on the underlying code.</span></span>  
  
- <span data-ttu-id="d53c0-167">Zajištění, že volající kódu nemůže trik váš kód do volání chráněného kódu jejich jménem.</span><span class="sxs-lookup"><span data-stu-id="d53c0-167">Ensuring that your code's callers cannot trick your code into calling the protected code on their behalf.</span></span> <span data-ttu-id="d53c0-168">Jinými slovy volající nelze vynutit autorizovaný kód předat určité parametry chráněného kódu nebo získat výsledky zpět z něj.</span><span class="sxs-lookup"><span data-stu-id="d53c0-168">In other words, callers cannot force the authorized code to pass specific parameters to the protected code, or to get results back from it.</span></span>  
  
### <a name="interfaces-and-link-demands"></a><span data-ttu-id="d53c0-169">Rozhraní a požadavky na propojení</span><span class="sxs-lookup"><span data-stu-id="d53c0-169">Interfaces and Link Demands</span></span>  
 <span data-ttu-id="d53c0-170">Pokud virtuální metoda, vlastnost nebo událost s **LinkDemand** přepíše metodu základní třídy, metoda základní třídy musí mít také stejnou **linkdemand** pro přepsané metody, aby byla účinná.</span><span class="sxs-lookup"><span data-stu-id="d53c0-170">If a virtual method, property, or event with **LinkDemand** overrides a base class method, the base class method must also have the same **LinkDemand** for the overridden method in order to be effective.</span></span> <span data-ttu-id="d53c0-171">Je možné pro škodlivý kód přetypování zpět na základní typ a volání metody základní třídy.</span><span class="sxs-lookup"><span data-stu-id="d53c0-171">It is possible for malicious code to cast back to the base type and call the base class method.</span></span> <span data-ttu-id="d53c0-172">Všimněte si také, že požadavky na propojení lze <xref:System.Security.AllowPartiallyTrustedCallersAttribute> přidat implicitně do sestavení, které nemají atribut na úrovni sestavení.</span><span class="sxs-lookup"><span data-stu-id="d53c0-172">Also note that link demands can be added implicitly to assemblies that do not have the <xref:System.Security.AllowPartiallyTrustedCallersAttribute> assembly-level attribute.</span></span>  
  
 <span data-ttu-id="d53c0-173">Je vhodné chránit implementace metod s požadavky na propojení, pokud mají metody rozhraní také požadavky na propojení.</span><span class="sxs-lookup"><span data-stu-id="d53c0-173">It is a good practice to protect method implementations with link demands when interface methods also have link demands.</span></span> <span data-ttu-id="d53c0-174">Všimněte si následující informace o použití požadavků na propojení s rozhraními:</span><span class="sxs-lookup"><span data-stu-id="d53c0-174">Note the following about using link demands with interfaces:</span></span>  
  
- <span data-ttu-id="d53c0-175">Pokud umístíte **LinkDemand** na veřejnou metodu třídy, která implementuje metodu rozhraní, **LinkDemand** nebude vynucena, pokud potom přetypování do rozhraní a volání metody.</span><span class="sxs-lookup"><span data-stu-id="d53c0-175">If you place a **LinkDemand** on a public method of a class that implements an interface method, the **LinkDemand** will not be enforced if you then cast to the interface and call the method.</span></span> <span data-ttu-id="d53c0-176">V tomto případě, protože jste propojeni proti rozhraní, je dodržena pouze **LinkDemand** na rozhraní.</span><span class="sxs-lookup"><span data-stu-id="d53c0-176">In this case, because you linked against the interface, only the **LinkDemand** on the interface is honored.</span></span>  
  
 <span data-ttu-id="d53c0-177">Zkontrolujte následující položky, pokud nejste problémy se zabezpečením:</span><span class="sxs-lookup"><span data-stu-id="d53c0-177">Review the following items for security issues:</span></span>  
  
- <span data-ttu-id="d53c0-178">Explicitní požadavky na propojení na metody rozhraní.</span><span class="sxs-lookup"><span data-stu-id="d53c0-178">Explicit link demands on interface methods.</span></span> <span data-ttu-id="d53c0-179">Ujistěte se, že tyto požadavky na propojení nabízejí očekávanou ochranu.</span><span class="sxs-lookup"><span data-stu-id="d53c0-179">Make sure these link demands offer the expected protection.</span></span> <span data-ttu-id="d53c0-180">Zjistěte, zda škodlivý kód můžete použít přetypování obejít požadavky na propojení, jak je popsáno výše.</span><span class="sxs-lookup"><span data-stu-id="d53c0-180">Determine whether malicious code can use a cast to get around the link demands as described previously.</span></span>  
  
- <span data-ttu-id="d53c0-181">Virtuální metody s požadavky na propojení.</span><span class="sxs-lookup"><span data-stu-id="d53c0-181">Virtual methods with link demands applied.</span></span>  
  
- <span data-ttu-id="d53c0-182">Typy a rozhraní, které implementují.</span><span class="sxs-lookup"><span data-stu-id="d53c0-182">Types and the interfaces they implement.</span></span> <span data-ttu-id="d53c0-183">Ty by měly používat požadavky na propojení konzistentně.</span><span class="sxs-lookup"><span data-stu-id="d53c0-183">These should use link demands consistently.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="d53c0-184">Viz také</span><span class="sxs-lookup"><span data-stu-id="d53c0-184">See also</span></span>

- [<span data-ttu-id="d53c0-185">Pokyny pro zabezpečené kódování</span><span class="sxs-lookup"><span data-stu-id="d53c0-185">Secure Coding Guidelines</span></span>](../../standard/security/secure-coding-guidelines.md)
