---
title: Aplikační domény
ms.date: 03/30/2017
helpviewer_keywords:
- process boundaries for isolation
- application isolation
- application domains, about
- common language runtime, application domains
- application domains
- runtime, application domains
- isolation between applications
- code, verification process
- verification testing code
ms.assetid: 113a8bbf-6875-4a72-a49d-ca2d92e19cc8
ms.openlocfilehash: a5c9f4248e060d231941269f39cadbc7147ce27f
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/14/2020
ms.locfileid: "79399873"
---
# <a name="application-domains"></a><span data-ttu-id="c8366-102">Aplikační domény</span><span class="sxs-lookup"><span data-stu-id="c8366-102">Application domains</span></span>

<span data-ttu-id="c8366-103">Operační systémy a runtime prostředí obvykle poskytují určitou formu izolace mezi aplikacemi.</span><span class="sxs-lookup"><span data-stu-id="c8366-103">Operating systems and runtime environments typically provide some form of isolation between applications.</span></span> <span data-ttu-id="c8366-104">Systém Windows například používá procesy k izolování aplikací.</span><span class="sxs-lookup"><span data-stu-id="c8366-104">For example, Windows uses processes to isolate applications.</span></span> <span data-ttu-id="c8366-105">Tato izolace je nezbytná k zajištění, že kód spuštěný v jedné aplikaci nemůže nepříznivě ovlivnit jiné nesouvisející aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-105">This isolation is necessary to ensure that code running in one application cannot adversely affect other, unrelated applications.</span></span>  
  
 <span data-ttu-id="c8366-106">Aplikační domény poskytují hranice izolace pro zabezpečení, spolehlivost a správu verzí a pro uvolnění sestavení.</span><span class="sxs-lookup"><span data-stu-id="c8366-106">Application domains provide an isolation boundary for security, reliability, and versioning, and for unloading assemblies.</span></span> <span data-ttu-id="c8366-107">Domény aplikací jsou obvykle vytvářeny hostiteli runtime, kteří jsou zodpovědní za zasunutí modulu runtime společného jazyka před spuštěním aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-107">Application domains are typically created by runtime hosts, which are responsible for bootstrapping the common language runtime before an application is run.</span></span>  
  
## <a name="the-benefits-of-isolating-applications"></a><span data-ttu-id="c8366-108">Výhody izolace aplikací</span><span class="sxs-lookup"><span data-stu-id="c8366-108">The benefits of isolating applications</span></span>

 <span data-ttu-id="c8366-109">Historicky byly hranice procesu použity k izolátí aplikací spuštěných ve stejném počítači.</span><span class="sxs-lookup"><span data-stu-id="c8366-109">Historically, process boundaries have been used to isolate applications running on the same computer.</span></span> <span data-ttu-id="c8366-110">Každá aplikace je načtena do samostatného procesu, který izoluje aplikaci od jiných aplikací spuštěných ve stejném počítači.</span><span class="sxs-lookup"><span data-stu-id="c8366-110">Each application is loaded into a separate process, which isolates the application from other applications running on the same computer.</span></span>  
  
 <span data-ttu-id="c8366-111">Aplikace jsou izolované, protože adresy paměti jsou relativní k procesu; ukazatel paměti předaný z jednoho procesu do druhého nelze použít žádným smysluplným způsobem v cílovém procesu.</span><span class="sxs-lookup"><span data-stu-id="c8366-111">The applications are isolated because memory addresses are process-relative; a memory pointer passed from one process to another cannot be used in any meaningful way in the target process.</span></span> <span data-ttu-id="c8366-112">Kromě toho nelze provádět přímé volání mezi dvěma procesy.</span><span class="sxs-lookup"><span data-stu-id="c8366-112">In addition, you cannot make direct calls between two processes.</span></span> <span data-ttu-id="c8366-113">Místo toho je nutné použít proxy servery, které poskytují úroveň dereference.</span><span class="sxs-lookup"><span data-stu-id="c8366-113">Instead, you must use proxies, which provide a level of indirection.</span></span>  
  
 <span data-ttu-id="c8366-114">Spravovaný kód musí být před spuštěním předán ověřovacím procesem (pokud správce neudělil oprávnění k přeskočení ověření).</span><span class="sxs-lookup"><span data-stu-id="c8366-114">Managed code must be passed through a verification process before it can be run (unless the administrator has granted permission to skip the verification).</span></span> <span data-ttu-id="c8366-115">Proces ověření určuje, zda se kód může pokusit o přístup k neplatným adresám paměti nebo provést jinou akci, která by mohla způsobit, že proces, ve kterém je spuštěn, nebude správně fungovat.</span><span class="sxs-lookup"><span data-stu-id="c8366-115">The verification process determines whether the code can attempt to access invalid memory addresses or perform some other action that could cause the process in which it is running to fail to operate properly.</span></span> <span data-ttu-id="c8366-116">Kód, který projde ověřovací zkouškou, je považován za typově bezpečný.</span><span class="sxs-lookup"><span data-stu-id="c8366-116">Code that passes the verification test is said to be type-safe.</span></span> <span data-ttu-id="c8366-117">Možnost ověřit kód jako typově bezpečné umožňuje za běhu společného jazyka poskytnout stejně velkou úroveň izolace jako hranice procesu, při mnohem nižších nákladech na výkon.</span><span class="sxs-lookup"><span data-stu-id="c8366-117">The ability to verify code as type-safe enables the common language runtime to provide as great a level of isolation as the process boundary, at a much lower performance cost.</span></span>  
  
 <span data-ttu-id="c8366-118">Aplikační domény poskytují bezpečnější a univerzálnější jednotku zpracování, kterou může běžný jazyk runtime použít k zajištění izolace mezi aplikacemi.</span><span class="sxs-lookup"><span data-stu-id="c8366-118">Application domains provide a more secure and versatile unit of processing that the common language runtime can use to provide isolation between applications.</span></span> <span data-ttu-id="c8366-119">Můžete spustit několik aplikačních domén v jednom procesu se stejnou úrovní izolace, která by existovala v samostatných procesech, ale bez další režie provádění meziprocesových volání nebo přepínání mezi procesy.</span><span class="sxs-lookup"><span data-stu-id="c8366-119">You can run several application domains in a single process with the same level of isolation that would exist in separate processes, but without incurring the additional overhead of making cross-process calls or switching between processes.</span></span> <span data-ttu-id="c8366-120">Možnost spouštět více aplikací v rámci jednoho procesu výrazně zvyšuje škálovatelnost serveru.</span><span class="sxs-lookup"><span data-stu-id="c8366-120">The ability to run multiple applications within a single process dramatically increases server scalability.</span></span>  
  
 <span data-ttu-id="c8366-121">Izolace aplikací je také důležitá pro zabezpečení aplikací.</span><span class="sxs-lookup"><span data-stu-id="c8366-121">Isolating applications is also important for application security.</span></span> <span data-ttu-id="c8366-122">Můžete například spustit ovládací prvky z několika webových aplikací v jednom procesu prohlížeče takovým způsobem, že ovládací prvky nemají přístup k datům a prostředkům ostatních.</span><span class="sxs-lookup"><span data-stu-id="c8366-122">For example, you can run controls from several Web applications in a single browser process in such a way that the controls cannot access each other's data and resources.</span></span>  
  
 <span data-ttu-id="c8366-123">Izolace poskytovaná aplikačními doménami má následující výhody:</span><span class="sxs-lookup"><span data-stu-id="c8366-123">The isolation provided by application domains has the following benefits:</span></span>  
  
- <span data-ttu-id="c8366-124">Chyby v jedné aplikaci nemohou ovlivnit jiné aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-124">Faults in one application cannot affect other applications.</span></span> <span data-ttu-id="c8366-125">Vzhledem k tomu, že typově bezpečný kód nemůže způsobit chyby paměti, použití aplikačních domén zajišťuje, že kód spuštěný v jedné doméně nemůže ovlivnit jiné aplikace v procesu.</span><span class="sxs-lookup"><span data-stu-id="c8366-125">Because type-safe code cannot cause memory faults, using application domains ensures that code running in one domain cannot affect other applications in the process.</span></span>  
  
- <span data-ttu-id="c8366-126">Jednotlivé aplikace lze zastavit bez zastavení celého procesu.</span><span class="sxs-lookup"><span data-stu-id="c8366-126">Individual applications can be stopped without stopping the entire process.</span></span> <span data-ttu-id="c8366-127">Použití aplikačních domén umožňuje uvolnit kód spuštěný v jedné aplikaci.</span><span class="sxs-lookup"><span data-stu-id="c8366-127">Using application domains enables you to unload the code running in a single application.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="c8366-128">Nelze uvolnit jednotlivá sestavení nebo typy.</span><span class="sxs-lookup"><span data-stu-id="c8366-128">You cannot unload individual assemblies or types.</span></span> <span data-ttu-id="c8366-129">Lze uvolnit pouze úplnou doménu.</span><span class="sxs-lookup"><span data-stu-id="c8366-129">Only a complete domain can be unloaded.</span></span>  
  
- <span data-ttu-id="c8366-130">Kód spuštěný v jedné aplikaci nemůže přímo přistupovat ke kódu nebo prostředkům z jiné aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-130">Code running in one application cannot directly access code or resources from another application.</span></span> <span data-ttu-id="c8366-131">Běžný jazyk runtime vynucuje tuto izolaci tím, že brání přímé volání mezi objekty v různých aplikačních doménách.</span><span class="sxs-lookup"><span data-stu-id="c8366-131">The common language runtime enforces this isolation by preventing direct calls between objects in different application domains.</span></span> <span data-ttu-id="c8366-132">Objekty, které procházejí mezi doménami, jsou zkopírovány nebo přístupné prostřednictvím serveru proxy.</span><span class="sxs-lookup"><span data-stu-id="c8366-132">Objects that pass between domains are either copied or accessed by proxy.</span></span> <span data-ttu-id="c8366-133">Pokud je objekt zkopírován, volání objektu je místní.</span><span class="sxs-lookup"><span data-stu-id="c8366-133">If the object is copied, the call to the object is local.</span></span> <span data-ttu-id="c8366-134">To znamená, že volající i objekt, na který se odkazuje, jsou ve stejné doméně aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-134">That is, both the caller and the object being referenced are in the same application domain.</span></span> <span data-ttu-id="c8366-135">Pokud je objekt přístupný prostřednictvím proxy serveru, volání objektu je vzdálené.</span><span class="sxs-lookup"><span data-stu-id="c8366-135">If the object is accessed through a proxy, the call to the object is remote.</span></span> <span data-ttu-id="c8366-136">V tomto případě volající a objekt odkazuje jsou v různých doménách aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-136">In this case, the caller and the object being referenced are in different application domains.</span></span> <span data-ttu-id="c8366-137">Volání mezi doménami používají stejnou infrastrukturu vzdáleného volání jako volání mezi dvěma procesy nebo mezi dvěma počítači.</span><span class="sxs-lookup"><span data-stu-id="c8366-137">Cross-domain calls use the same remote call infrastructure as calls between two processes or between two machines.</span></span> <span data-ttu-id="c8366-138">Jako takové metadata pro odkazovaný objekt musí být k dispozici pro obě domény aplikace povolit volání metody, které mají být správně kompilovány JIT.</span><span class="sxs-lookup"><span data-stu-id="c8366-138">As such, the metadata for the object being referenced must be available to both application domains to allow the method call to be JIT-compiled properly.</span></span> <span data-ttu-id="c8366-139">Pokud volající doména nemá přístup k metadatům volaného objektu, může kompilace selhat s výjimkou typu <xref:System.IO.FileNotFoundException>.</span><span class="sxs-lookup"><span data-stu-id="c8366-139">If the calling domain does not have access to the metadata for the object being called, the compilation might fail with an exception of type <xref:System.IO.FileNotFoundException>.</span></span> <span data-ttu-id="c8366-140">Další informace naleznete [v tématu Vzdálené objekty](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/72x4h507(v=vs.100)).</span><span class="sxs-lookup"><span data-stu-id="c8366-140">For more information, see [Remote Objects](https://docs.microsoft.com/previous-versions/dotnet/netframework-4.0/72x4h507(v=vs.100)).</span></span> <span data-ttu-id="c8366-141">Mechanismus pro určení, jak lze přistupovat k objektům napříč doménami, je určen objektem.</span><span class="sxs-lookup"><span data-stu-id="c8366-141">The mechanism for determining how objects can be accessed across domains is determined by the object.</span></span> <span data-ttu-id="c8366-142">Další informace naleznete v tématu <xref:System.MarshalByRefObject?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="c8366-142">For more information, see <xref:System.MarshalByRefObject?displayProperty=nameWithType>.</span></span>  
  
- <span data-ttu-id="c8366-143">Chování kódu je vymezeno aplikací, ve které je spuštěn.</span><span class="sxs-lookup"><span data-stu-id="c8366-143">The behavior of code is scoped by the application in which it runs.</span></span> <span data-ttu-id="c8366-144">Jinými slovy, doména aplikace poskytuje nastavení konfigurace, jako jsou zásady verze aplikace, umístění všech vzdálených sestavení, ke kterým přistupuje, a informace o tom, kde najít sestavení načtená do domény.</span><span class="sxs-lookup"><span data-stu-id="c8366-144">In other words, the application domain provides configuration settings such as application version policies, the location of any remote assemblies it accesses, and information about where to locate assemblies that are loaded into the domain.</span></span>  
  
- <span data-ttu-id="c8366-145">Oprávnění udělená kódu mohou být řízena doménou aplikace, ve které je kód spuštěn.</span><span class="sxs-lookup"><span data-stu-id="c8366-145">Permissions granted to code can be controlled by the application domain in which the code is running.</span></span>  
  
## <a name="application-domains-and-assemblies"></a><span data-ttu-id="c8366-146">Domény a sestavení aplikací</span><span class="sxs-lookup"><span data-stu-id="c8366-146">Application domains and assemblies</span></span>

 <span data-ttu-id="c8366-147">Tato část popisuje vztah mezi aplikačními doménami a sestaveními.</span><span class="sxs-lookup"><span data-stu-id="c8366-147">This section describes the relationship between application domains and assemblies.</span></span> <span data-ttu-id="c8366-148">Před spuštěním kódu, který obsahuje, je nutné načíst sestavení do domény aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-148">You must load an assembly into an application domain before you can execute the code it contains.</span></span> <span data-ttu-id="c8366-149">Spuštění typické aplikace způsobí, že několik sestavení, které mají být načteny do domény aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-149">Running a typical application causes several assemblies to be loaded into an application domain.</span></span>  
  
 <span data-ttu-id="c8366-150">Způsob, jakým je načten oseka sestavení určuje, zda jeho just-in-time (JIT) zkompilovaný kód může být sdílena více aplikačních domén v procesu a zda sestavení lze uvolnit z procesu.</span><span class="sxs-lookup"><span data-stu-id="c8366-150">The way an assembly is loaded determines whether its just-in-time (JIT) compiled code can be shared by multiple application domains in the process, and whether the assembly can be unloaded from the process.</span></span>  
  
- <span data-ttu-id="c8366-151">Pokud je sestavení načteno neutrální z domény, všechny domény aplikace, které sdílejí stejnou sadu grantů zabezpečení, mohou sdílet stejný kód zkompilovaný JIT, což snižuje paměť požadovanou aplikací.</span><span class="sxs-lookup"><span data-stu-id="c8366-151">If an assembly is loaded domain-neutral, all application domains that share the same security grant set can share the same JIT-compiled code, which reduces the memory required by the application.</span></span> <span data-ttu-id="c8366-152">Sestavení však nelze nikdy uvolnit z procesu.</span><span class="sxs-lookup"><span data-stu-id="c8366-152">However, the assembly can never be unloaded from the process.</span></span>  
  
- <span data-ttu-id="c8366-153">Pokud sestavení není načteno neutrální domény, musí být kompilován JIT v každé doméně aplikace, ve které je načten.</span><span class="sxs-lookup"><span data-stu-id="c8366-153">If an assembly is not loaded domain-neutral, it must be JIT-compiled in every application domain in which it is loaded.</span></span> <span data-ttu-id="c8366-154">Sestavení však může být uvolněna z procesu uvolněním všech domén aplikace, ve kterém je načten.</span><span class="sxs-lookup"><span data-stu-id="c8366-154">However, the assembly can be unloaded from the process by unloading all the application domains in which it is loaded.</span></span>  
  
 <span data-ttu-id="c8366-155">Hostitel runtime určuje, zda má být načíst sestavení jako neutrální domény při načtení runtime do procesu.</span><span class="sxs-lookup"><span data-stu-id="c8366-155">The runtime host determines whether to load assemblies as domain-neutral when it loads the runtime into a process.</span></span> <span data-ttu-id="c8366-156">Pro spravované aplikace <xref:System.LoaderOptimizationAttribute> použijte atribut pro metodu vstupního bodu pro proces <xref:System.LoaderOptimization> a zadejte hodnotu z přidruženého výčtu.</span><span class="sxs-lookup"><span data-stu-id="c8366-156">For managed applications, apply the <xref:System.LoaderOptimizationAttribute> attribute to the entry-point method for the process, and specify a value from the associated <xref:System.LoaderOptimization> enumeration.</span></span> <span data-ttu-id="c8366-157">Pro nespravované aplikace, které jsou hostiteli za běhu společného jazyka, zadejte příslušný příznak při volání metody [Funkce CorBindToRuntimeEx.](../unmanaged-api/hosting/corbindtoruntimeex-function.md)</span><span class="sxs-lookup"><span data-stu-id="c8366-157">For unmanaged applications that host the common language runtime, specify the appropriate flag when you call the [CorBindToRuntimeEx Function](../unmanaged-api/hosting/corbindtoruntimeex-function.md) method.</span></span>  
  
 <span data-ttu-id="c8366-158">Existují tři možnosti pro načítání neutrálních sestavení domény:</span><span class="sxs-lookup"><span data-stu-id="c8366-158">There are three options for loading domain-neutral assemblies:</span></span>  
  
- <span data-ttu-id="c8366-159"><xref:System.LoaderOptimization.SingleDomain?displayProperty=nameWithType>načte žádná sestavení jako neutrální domény, s výjimkou Mscorlib, který je vždy načten domény neutrální.</span><span class="sxs-lookup"><span data-stu-id="c8366-159"><xref:System.LoaderOptimization.SingleDomain?displayProperty=nameWithType> loads no assemblies as domain-neutral, except Mscorlib, which is always loaded domain-neutral.</span></span> <span data-ttu-id="c8366-160">Toto nastavení se nazývá jedna doména, protože se běžně používá, když hostitel používá pouze jednu aplikaci v procesu.</span><span class="sxs-lookup"><span data-stu-id="c8366-160">This setting is called single domain because it is commonly used when the host is running only a single application in the process.</span></span>

- <span data-ttu-id="c8366-161"><xref:System.LoaderOptimization.MultiDomain?displayProperty=nameWithType>načte všechna sestavení jako neutrální z domény.</span><span class="sxs-lookup"><span data-stu-id="c8366-161"><xref:System.LoaderOptimization.MultiDomain?displayProperty=nameWithType> loads all assemblies as domain-neutral.</span></span> <span data-ttu-id="c8366-162">Toto nastavení použijte, pokud je v procesu více aplikačních domén, které spouštějí stejný kód.</span><span class="sxs-lookup"><span data-stu-id="c8366-162">Use this setting when there are multiple application domains in the process, all of which run the same code.</span></span>

- <span data-ttu-id="c8366-163"><xref:System.LoaderOptimization.MultiDomainHost?displayProperty=nameWithType>načte sestavení se silným názvem jako neutrální domény, pokud byly nainstalovány oni a všechny jejich závislosti v globální mezipaměti sestavení.</span><span class="sxs-lookup"><span data-stu-id="c8366-163"><xref:System.LoaderOptimization.MultiDomainHost?displayProperty=nameWithType> loads strong-named assemblies as domain-neutral, if they and all their dependencies have been installed in the global assembly cache.</span></span> <span data-ttu-id="c8366-164">Ostatní sestavení jsou načteny a JIT kompilovány samostatně pro každou doménu aplikace, ve které jsou načteny, a proto mohou být uvolněny z procesu.</span><span class="sxs-lookup"><span data-stu-id="c8366-164">Other assemblies are loaded and JIT-compiled separately for each application domain in which they are loaded, and thus can be unloaded from the process.</span></span> <span data-ttu-id="c8366-165">Toto nastavení použijte při spuštění více než jedné aplikace ve stejném procesu nebo pokud máte směs sestavení, které jsou sdíleny mnoha aplikačních domén a sestavení, které je třeba uvolnit z procesu.</span><span class="sxs-lookup"><span data-stu-id="c8366-165">Use this setting when running more than one application in the same process, or if you have a mixture of assemblies that are shared by many application domains and assemblies that need to be unloaded from the process.</span></span>
  
 <span data-ttu-id="c8366-166">Jit kompilovaný kód nelze sdílet pro sestavení načtená do <xref:System.Reflection.Assembly.LoadFrom%2A> kontextu load-from, pomocí metody <xref:System.Reflection.Assembly> třídy <xref:System.Reflection.Assembly.Load%2A> nebo načtená z bitových kopií pomocí přetížení metody, která určují bajtová pole.</span><span class="sxs-lookup"><span data-stu-id="c8366-166">JIT-compiled code cannot be shared for assemblies loaded into the load-from context, using the <xref:System.Reflection.Assembly.LoadFrom%2A> method of the <xref:System.Reflection.Assembly> class, or loaded from images using overloads of the <xref:System.Reflection.Assembly.Load%2A> method that specify byte arrays.</span></span>  
  
 <span data-ttu-id="c8366-167">Sestavení, která byla zkompilována do nativního kódu pomocí [ngen.exe (Native Image Generator),](../tools/ngen-exe-native-image-generator.md) mohou být sdílena mezi doménami aplikace, pokud jsou načteny neutrální domény při prvním načtení do procesu.</span><span class="sxs-lookup"><span data-stu-id="c8366-167">Assemblies that have been compiled to native code by using the [Ngen.exe (Native Image Generator)](../tools/ngen-exe-native-image-generator.md) can be shared between application domains, if they are loaded domain-neutral the first time they are loaded into a process.</span></span>  
  
 <span data-ttu-id="c8366-168">JIT kompilovaný kód pro sestavení, který obsahuje vstupní bod aplikace je sdílena pouze v případě, že všechny jeho závislosti lze sdílet.</span><span class="sxs-lookup"><span data-stu-id="c8366-168">JIT-compiled code for the assembly that contains the application entry point is shared only if all its dependencies can be shared.</span></span>  
  
 <span data-ttu-id="c8366-169">Neutrální sestavení domény může být jit kompilován více než jednou.</span><span class="sxs-lookup"><span data-stu-id="c8366-169">A domain-neutral assembly can be JIT-compiled more than once.</span></span> <span data-ttu-id="c8366-170">Například pokud se liší sady grantů zabezpečení dvou aplikačních domén, nemohou sdílet stejný kód zkompilovaný JIT.</span><span class="sxs-lookup"><span data-stu-id="c8366-170">For example, when the security grant sets of two application domains are different, they cannot share the same JIT-compiled code.</span></span> <span data-ttu-id="c8366-171">Každá kopie sestavení zkompilovaného JIT však může být sdílena s jinými aplikačními doménami, které mají stejnou sadu grantů.</span><span class="sxs-lookup"><span data-stu-id="c8366-171">However, each copy of the JIT-compiled assembly can be shared with other application domains that have the same grant set.</span></span>  
  
 <span data-ttu-id="c8366-172">Při rozhodování, zda načíst sestavení jako neutrální domény, je nutné provést kompromis mezi sníženívyužití paměti a další faktory výkonu.</span><span class="sxs-lookup"><span data-stu-id="c8366-172">When you decide whether to load assemblies as domain-neutral, you must make a tradeoff between reducing memory use and other performance factors.</span></span>  
  
- <span data-ttu-id="c8366-173">Přístup ke statickým datům a metodám je pomalejší pro sestavení neutrální z domény z důvodu nutnosti izolovat sestavení.</span><span class="sxs-lookup"><span data-stu-id="c8366-173">Access to static data and methods is slower for domain-neutral assemblies because of the need to isolate assemblies.</span></span> <span data-ttu-id="c8366-174">Každá aplikační doména, která přistupuje k sestavení, musí mít samostatnou kopii statických dat, aby odkazy na objekty ve statických polích nepřekročily hranice domény.</span><span class="sxs-lookup"><span data-stu-id="c8366-174">Each application domain that accesses the assembly must have a separate copy of the static data, to prevent references to objects in static fields from crossing domain boundaries.</span></span> <span data-ttu-id="c8366-175">V důsledku toho runtime obsahuje další logiku nasměrovat volajícího na příslušnou kopii statických dat nebo metody.</span><span class="sxs-lookup"><span data-stu-id="c8366-175">As a result, the runtime contains additional logic to direct a caller to the appropriate copy of the static data or method.</span></span> <span data-ttu-id="c8366-176">Tato další logika zpomaluje volání.</span><span class="sxs-lookup"><span data-stu-id="c8366-176">This extra logic slows down the call.</span></span>  
  
- <span data-ttu-id="c8366-177">Všechny závislosti sestavení musí být lokalizovány a načteny při načítání sestavení neutrální domény, protože závislost, která nemůže být načtena neutrální domény zabraňuje načtení neutrální ho sestavení domény.</span><span class="sxs-lookup"><span data-stu-id="c8366-177">All the dependencies of an assembly must be located and loaded when the assembly is loaded domain-neutral, because a dependency that cannot be loaded domain-neutral prevents the assembly from being loaded domain-neutral.</span></span>  
  
## <a name="application-domains-and-threads"></a><span data-ttu-id="c8366-178">Aplikační domény a vlákna</span><span class="sxs-lookup"><span data-stu-id="c8366-178">Application domains and threads</span></span>

 <span data-ttu-id="c8366-179">Doména aplikace tvoří hranici izolace pro zabezpečení, správu verzí, spolehlivost a uvolnění spravovaného kódu.</span><span class="sxs-lookup"><span data-stu-id="c8366-179">An application domain forms an isolation boundary for security, versioning, reliability, and unloading of managed code.</span></span> <span data-ttu-id="c8366-180">Vlákno je konstrukce operačního systému používaná běžným jazykem runtime ke spuštění kódu.</span><span class="sxs-lookup"><span data-stu-id="c8366-180">A thread is the operating system construct used by the common language runtime to execute code.</span></span> <span data-ttu-id="c8366-181">Za běhu je veškerý spravovaný kód načten do domény aplikace a je spuštěn jedním nebo více spravovanými vlákny.</span><span class="sxs-lookup"><span data-stu-id="c8366-181">At run time, all managed code is loaded into an application domain and is run by one or more managed threads.</span></span>  
  
 <span data-ttu-id="c8366-182">Neexistuje korelace 1:1 mezi doménami aplikace a vlákny.</span><span class="sxs-lookup"><span data-stu-id="c8366-182">There is not a one-to-one correlation between application domains and threads.</span></span> <span data-ttu-id="c8366-183">Několik podprocesů lze spustit v jedné doméně aplikace v daném okamžiku a konkrétní vlákno není omezena na jednu doménu aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-183">Several threads can execute in a single application domain at any given time, and a particular thread is not confined to a single application domain.</span></span> <span data-ttu-id="c8366-184">To znamená, že vlákna mohou volně překračovat hranice domény aplikace; nové vlákno není vytvořeno pro každou doménu aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-184">That is, threads are free to cross application domain boundaries; a new thread is not created for each application domain.</span></span>  
  
 <span data-ttu-id="c8366-185">V každém okamžiku se každé vlákno spustí v doméně aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-185">At any given time, every thread executes in an application domain.</span></span> <span data-ttu-id="c8366-186">Nula, jeden nebo více vláken může být provádění v dané doméně aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-186">Zero, one, or multiple threads might be executing in any given application domain.</span></span> <span data-ttu-id="c8366-187">Runtime sleduje, která vlákna jsou spuštěna ve kterých aplikačních doménách.</span><span class="sxs-lookup"><span data-stu-id="c8366-187">The runtime keeps track of which threads are running in which application domains.</span></span> <span data-ttu-id="c8366-188">Můžete vyhledat doménu, ve které je vlákno spuštěno <xref:System.Threading.Thread.GetDomain%2A?displayProperty=nameWithType> kdykoli voláním metody.</span><span class="sxs-lookup"><span data-stu-id="c8366-188">You can locate the domain in which a thread is executing at any time by calling the <xref:System.Threading.Thread.GetDomain%2A?displayProperty=nameWithType> method.</span></span>

### <a name="application-domains-and-cultures"></a><span data-ttu-id="c8366-189">Aplikační domény a jazykové verze</span><span class="sxs-lookup"><span data-stu-id="c8366-189">Application domains and cultures</span></span>

 <span data-ttu-id="c8366-190">Jazyková verze, která <xref:System.Globalization.CultureInfo> je reprezentována objektem, je přidružena k vláknům.</span><span class="sxs-lookup"><span data-stu-id="c8366-190">Culture, which is represented by a <xref:System.Globalization.CultureInfo> object, is associated with threads.</span></span> <span data-ttu-id="c8366-191">Můžete získat jazykovou verzi, která je přidružena k <xref:System.Globalization.CultureInfo.CurrentCulture%2A?displayProperty=nameWithType> aktuálně spuštěné vlákno pomocí vlastnosti a můžete získat nebo nastavit <xref:System.Threading.Thread.CurrentCulture%2A?displayProperty=nameWithType> jazykovou verzi, která je přidružena k aktuálně spuštěné vlákno pomocí vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="c8366-191">You can get the culture that is associated with the currently executing thread by using the <xref:System.Globalization.CultureInfo.CurrentCulture%2A?displayProperty=nameWithType> property, and you can get or set the culture that is associated with the currently executing thread by using the <xref:System.Threading.Thread.CurrentCulture%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="c8366-192">Pokud jazyková verze, která je přidružena k <xref:System.Threading.Thread.CurrentCulture%2A?displayProperty=nameWithType> podprocesu byla explicitně nastavena pomocí vlastnosti, bude i nadále přidružena k tomuto vláknu, když vlákno překročí hranice domény aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-192">If the culture that is associated with a thread has been explicitly set by using the <xref:System.Threading.Thread.CurrentCulture%2A?displayProperty=nameWithType> property, it continues to be associated with that thread when the thread crosses application domain boundaries.</span></span> <span data-ttu-id="c8366-193">V opačném případě je jazyková verze, která je přidružena <xref:System.Globalization.CultureInfo.DefaultThreadCurrentCulture%2A?displayProperty=nameWithType> k podprocesu v daném okamžiku, určena hodnotou vlastnosti v doméně aplikace, ve které je vlákno spuštěno:</span><span class="sxs-lookup"><span data-stu-id="c8366-193">Otherwise, the culture that is associated with the thread at any given time is determined by the value of the <xref:System.Globalization.CultureInfo.DefaultThreadCurrentCulture%2A?displayProperty=nameWithType> property in the application domain in which the thread is executing:</span></span>  
  
- <span data-ttu-id="c8366-194">Pokud hodnota vlastnosti není `null`, jazyková verze, která je vrácena vlastností je <xref:System.Threading.Thread.CurrentCulture%2A?displayProperty=nameWithType> <xref:System.Globalization.CultureInfo.CurrentCulture%2A?displayProperty=nameWithType> přidružena k podprocesu (a proto vrácena vlastnostmi a).</span><span class="sxs-lookup"><span data-stu-id="c8366-194">If the value of the property is not `null`, the culture that is returned by the property is associated with the thread (and therefore returned by the <xref:System.Threading.Thread.CurrentCulture%2A?displayProperty=nameWithType> and <xref:System.Globalization.CultureInfo.CurrentCulture%2A?displayProperty=nameWithType> properties).</span></span>  
  
- <span data-ttu-id="c8366-195">Pokud je `null`hodnota vlastnosti , aktuální jazyková verze systému je přidružena k vláknu.</span><span class="sxs-lookup"><span data-stu-id="c8366-195">If the value of the property is `null`, the current system culture is associated with the thread.</span></span>  
  
## <a name="programming-with-application-domains"></a><span data-ttu-id="c8366-196">Programování s aplikačními doménami</span><span class="sxs-lookup"><span data-stu-id="c8366-196">Programming with application domains</span></span>

 <span data-ttu-id="c8366-197">Aplikační domény jsou obvykle vytvářeny a řízeny programově hostiteli za běhu.</span><span class="sxs-lookup"><span data-stu-id="c8366-197">Application domains are usually created and manipulated programmatically by runtime hosts.</span></span> <span data-ttu-id="c8366-198">Někdy však může aplikační program také chtít pracovat s aplikačními doménami.</span><span class="sxs-lookup"><span data-stu-id="c8366-198">However, sometimes an application program might also want to work with application domains.</span></span> <span data-ttu-id="c8366-199">Aplikační program může například načíst součást aplikace do domény, aby bylo možné uvolnit doménu (a komponentu), aniž by bylo třeba zastavit celou aplikaci.</span><span class="sxs-lookup"><span data-stu-id="c8366-199">For example, an application program could load an application component into a domain to be able to unload the domain (and the component) without having to stop the entire application.</span></span>  
  
 <span data-ttu-id="c8366-200">Jedná <xref:System.AppDomain> se o programové rozhraní k aplikačním doménám.</span><span class="sxs-lookup"><span data-stu-id="c8366-200">The <xref:System.AppDomain> is the programmatic interface to application domains.</span></span> <span data-ttu-id="c8366-201">Tato třída zahrnuje metody pro vytváření a uvolňování domén, k vytvoření instancí typů v doménách a k registraci různých oznámení, jako je například uvolnění domény aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-201">This class includes methods to create and unload domains, to create instances of types in domains, and to register for various notifications such as application domain unloading.</span></span> <span data-ttu-id="c8366-202">V následující tabulce jsou <xref:System.AppDomain> uvedeny běžně používané metody.</span><span class="sxs-lookup"><span data-stu-id="c8366-202">The following table lists commonly used <xref:System.AppDomain> methods.</span></span>  
  
|<span data-ttu-id="c8366-203">Metoda Domény aplikace</span><span class="sxs-lookup"><span data-stu-id="c8366-203">AppDomain Method</span></span>|<span data-ttu-id="c8366-204">Popis</span><span class="sxs-lookup"><span data-stu-id="c8366-204">Description</span></span>|  
|----------------------|-----------------|  
|<xref:System.AppDomain.CreateDomain%2A>|<span data-ttu-id="c8366-205">Vytvoří novou doménu aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-205">Creates a new application domain.</span></span> <span data-ttu-id="c8366-206">Doporučuje se použít přetížení této metody, která <xref:System.AppDomainSetup> určuje objekt.</span><span class="sxs-lookup"><span data-stu-id="c8366-206">It is recommended that you use an overload of this method that specifies an <xref:System.AppDomainSetup> object.</span></span> <span data-ttu-id="c8366-207">Toto je upřednostňovaný způsob nastavení vlastností nové domény, jako je například základ aplikace nebo kořenový adresář aplikace. umístění konfiguračního souboru pro doménu; a vyhledávací cestu, která je za běhu společného jazyka použít k načtení sestavení do domény.</span><span class="sxs-lookup"><span data-stu-id="c8366-207">This is the preferred way to set the properties of a new domain, such as the application base, or root directory for the application; the location of the configuration file for the domain; and the search path that the common language runtime is to use to load assemblies into the domain.</span></span>|  
|<span data-ttu-id="c8366-208"><xref:System.AppDomain.ExecuteAssembly%2A> a <xref:System.AppDomain.ExecuteAssemblyByName%2A></span><span class="sxs-lookup"><span data-stu-id="c8366-208"><xref:System.AppDomain.ExecuteAssembly%2A> and <xref:System.AppDomain.ExecuteAssemblyByName%2A></span></span>|<span data-ttu-id="c8366-209">Spustí sestavení v doméně aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-209">Executes an assembly in the application domain.</span></span> <span data-ttu-id="c8366-210">Jedná se o metodu instance, takže ji lze použít ke spuštění kódu v jiné doméně aplikace, na kterou máte odkaz.</span><span class="sxs-lookup"><span data-stu-id="c8366-210">This is an instance method, so it can be used to execute code in another application domain to which you have a reference.</span></span>|  
|<xref:System.AppDomain.CreateInstanceAndUnwrap%2A>|<span data-ttu-id="c8366-211">Vytvoří instanci zadaného typu v doméně aplikace a vrátí proxy server.</span><span class="sxs-lookup"><span data-stu-id="c8366-211">Creates an instance of a specified type in the application domain, and returns a proxy.</span></span> <span data-ttu-id="c8366-212">Tuto metodu použijte, abyste se vyhnuli načítání sestavení obsahujícího vytvořený typ do volajícího sestavení.</span><span class="sxs-lookup"><span data-stu-id="c8366-212">Use this method to avoid loading the assembly containing the created type into the calling assembly.</span></span>|  
|<xref:System.AppDomain.Unload%2A>|<span data-ttu-id="c8366-213">Provede řádné vypnutí domény.</span><span class="sxs-lookup"><span data-stu-id="c8366-213">Performs a graceful shutdown of the domain.</span></span> <span data-ttu-id="c8366-214">Doména aplikace není uvolněna, dokud se všechna vlákna spuštěná v doméně nezastavila nebo již nejsou v doméně.</span><span class="sxs-lookup"><span data-stu-id="c8366-214">The application domain is not unloaded until all threads running in the domain have either stopped or are no longer in the domain.</span></span>|  
  
> [!NOTE]
> <span data-ttu-id="c8366-215">Běžný jazyk runtime nepodporuje serializaci globálních metod, takže delegáti nelze použít ke spuštění globálních metod v jiných aplikačních doménách.</span><span class="sxs-lookup"><span data-stu-id="c8366-215">The common language runtime does not support serialization of global methods, so delegates cannot be used to execute global methods in other application domains.</span></span>  
  
 <span data-ttu-id="c8366-216">Nespravovaná rozhraní popsaná ve specifikaci common language runtime hosting interfaces také poskytují přístup k aplikačním doménám.</span><span class="sxs-lookup"><span data-stu-id="c8366-216">The unmanaged interfaces described in the common language runtime Hosting Interfaces Specification also provide access to application domains.</span></span> <span data-ttu-id="c8366-217">Hostitelé runtime mohou používat rozhraní z nespravovaného kódu k vytvoření a získání přístupu k aplikačním doménám v rámci procesu.</span><span class="sxs-lookup"><span data-stu-id="c8366-217">Runtime hosts can use interfaces from unmanaged code to create and gain access to the application domains within a process.</span></span>  
  
## <a name="the-complus_loaderoptimization-environment-variable"></a><span data-ttu-id="c8366-218">Proměnná prostředí COMPLUS_LoaderOptimization</span><span class="sxs-lookup"><span data-stu-id="c8366-218">The COMPLUS_LoaderOptimization environment variable</span></span>

 <span data-ttu-id="c8366-219">Proměnná prostředí, která nastavuje výchozí zásadu optimalizace zavaděče spustitelné aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-219">An environment variable that sets the default loader optimization policy of an executable application.</span></span>  
  
### <a name="syntax"></a><span data-ttu-id="c8366-220">Syntaxe</span><span class="sxs-lookup"><span data-stu-id="c8366-220">Syntax</span></span>  
  
```env  
COMPLUS_LoaderOptimization = 1  
```  
  
### <a name="remarks"></a><span data-ttu-id="c8366-221">Poznámky</span><span class="sxs-lookup"><span data-stu-id="c8366-221">Remarks</span></span>

 <span data-ttu-id="c8366-222">Typická aplikace načte několik sestavení do domény aplikace před kód, který obsahují mohou být provedeny.</span><span class="sxs-lookup"><span data-stu-id="c8366-222">A typical application loads several assemblies into an application domain before the code they contain can be executed.</span></span>  
  
 <span data-ttu-id="c8366-223">Způsob, jakým je načten oseka, určuje, zda jeho kompilovaný kód za čase (JIT) může být sdílen více aplikačními doménami v procesu.</span><span class="sxs-lookup"><span data-stu-id="c8366-223">The way the assembly is loaded determines whether its just-in-time (JIT) compiled code can be shared by multiple application domains in the process.</span></span>  
  
- <span data-ttu-id="c8366-224">Pokud je sestavení načteno neutrální z domény, všechny domény aplikace, které sdílejí stejnou sadu grantů zabezpečení, mohou sdílet stejný kód zkompilovaný JIT.</span><span class="sxs-lookup"><span data-stu-id="c8366-224">If an assembly is loaded domain-neutral, all application domains that share the same security grant set can share the same JIT-compiled code.</span></span> <span data-ttu-id="c8366-225">To snižuje paměť požadovanou aplikací.</span><span class="sxs-lookup"><span data-stu-id="c8366-225">This reduces the memory required by the application.</span></span>  
  
- <span data-ttu-id="c8366-226">Pokud sestavení není načteno neutrální domény, musí být kompilován JIT v každé doméně aplikace, ve které je načteno, a zavaděč nesmí sdílet interní prostředky mezi doménami aplikace.</span><span class="sxs-lookup"><span data-stu-id="c8366-226">If an assembly is not loaded domain-neutral, it must be JIT-compiled in every application domain in which it is loaded and the loader must not share internal resources across application domains.</span></span>  
  
 <span data-ttu-id="c8366-227">Pokud je nastavena na 1, příznak prostředí COMPLUS_LoaderOptimization vynutí hostitele runtime načíst všechna sestavení v nedomény neutrální způsobem známý jako SingleDomain.</span><span class="sxs-lookup"><span data-stu-id="c8366-227">When set to 1, the COMPLUS_LoaderOptimization environment flag forces the runtime host to load all assemblies in non-domain-neutral way known as SingleDomain.</span></span> <span data-ttu-id="c8366-228">SingleDomain načte žádná sestavení jako neutrální domény, s výjimkou Mscorlib, který je vždy načten domény neutrální.</span><span class="sxs-lookup"><span data-stu-id="c8366-228">SingleDomain loads no assemblies as domain-neutral, except Mscorlib, which is always loaded domain-neutral.</span></span> <span data-ttu-id="c8366-229">Toto nastavení se nazývá jedna doména, protože se běžně používá, když hostitel používá pouze jednu aplikaci v procesu.</span><span class="sxs-lookup"><span data-stu-id="c8366-229">This setting is called single domain because it is commonly used when the host is running only a single application in the process.</span></span>  
  
> [!CAUTION]
> <span data-ttu-id="c8366-230">Příznak prostředí COMPLUS_LoaderOptimization byl navržen pro použití v diagnostických a testovacích scénářích.</span><span class="sxs-lookup"><span data-stu-id="c8366-230">The COMPLUS_LoaderOptimization environment flag was designed to be used in diagnostic and test scenarios.</span></span> <span data-ttu-id="c8366-231">Zapnutí příznaku může způsobit výrazné zpomalení a zvýšení využití paměti.</span><span class="sxs-lookup"><span data-stu-id="c8366-231">Having the flag turned on can cause severe slow-down and increase in memory usage.</span></span>  
  
### <a name="code-example"></a><span data-ttu-id="c8366-232">Příklad kódu</span><span class="sxs-lookup"><span data-stu-id="c8366-232">Code example</span></span>

 <span data-ttu-id="c8366-233">Vynutit, aby všechna sestavení nebyla načtena jako neutrální pro službu `COMPLUS_LoaderOptimization=1` IISADMIN neutrální, lze dosáhnout připojením k multiřetězcové hodnotě prostředí v klíči HKEY_LOCAL_MACHINE\CURRENTControlSet\services\IISADMIN.</span><span class="sxs-lookup"><span data-stu-id="c8366-233">To force all assemblies not to be loaded as domain-neutral for the IISADMIN service can be achieved by appending `COMPLUS_LoaderOptimization=1` to the Environment’s Multi-String Value in the HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\services\IISADMIN key.</span></span>  
  
```env  
Key = HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\services\IISADMIN  
Name = Environment  
Type = REG_MULTI_SZ  
Value (to append) = COMPLUS_LoaderOptimization=1  
```  
  
## <a name="see-also"></a><span data-ttu-id="c8366-234">Viz také</span><span class="sxs-lookup"><span data-stu-id="c8366-234">See also</span></span>

- <xref:System.AppDomain?displayProperty=nameWithType>
- <xref:System.MarshalByRefObject?displayProperty=nameWithType>
- [<span data-ttu-id="c8366-235">Programování s doménami aplikací a sestaveními</span><span class="sxs-lookup"><span data-stu-id="c8366-235">Programming with Application Domains and Assemblies</span></span>](index.md)
- [<span data-ttu-id="c8366-236">Používání domén aplikací</span><span class="sxs-lookup"><span data-stu-id="c8366-236">Using Application Domains</span></span>](use.md)
