---
title: Podrobná syntaxe XAML
ms.date: 03/30/2017
helpviewer_keywords:
- XML [WPF], namespaces
- XAML [WPF], parsing of attributes
- parsing of attributes [WPF]
- XAML [WPF], markup extensions
- attached properties [WPF]
- tag syntax [XAML]
- markup extensions [WPF]
- XAML [WPF], object element syntax
- XAML [WPF], syntax terminology
- attached events [WPF]
- lookup semantics [WPF]
- XAML [WPF], attached events
- XAML [WPF], content syntax
- XAML [WPF], lookup semantics
- content syntax [WPF]
- object element syntax [WPF]
- syntax terminology [XAML]
- XAML [WPF], attached properties
- attributes [XAML], parsing
- XAML [WPF], tag syntax
- XAML [WPF], attribute syntax
- property element syntax [WPF]
- terminology [XAML]
- namespaces [WPF], XML
- attribute syntax [XAML]
- XAML [WPF], property element syntax
ms.assetid: 67cce290-ca26-4c41-a797-b68aabc45479
ms.openlocfilehash: 5f8bb862ce443fd7397036b10f69cda65a6960bc
ms.sourcegitcommit: 62285ec11fa8e8424bab00511a90760c60e63c95
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/20/2020
ms.locfileid: "81646148"
---
# <a name="xaml-syntax-in-detail"></a><span data-ttu-id="abc8c-102">Podrobná syntaxe XAML</span><span class="sxs-lookup"><span data-stu-id="abc8c-102">XAML Syntax In Detail</span></span>
<span data-ttu-id="abc8c-103">Toto téma definuje termíny, které se používají k popisu prvků syntaxe XAML.</span><span class="sxs-lookup"><span data-stu-id="abc8c-103">This topic defines the terms that are used to describe the elements of XAML syntax.</span></span> <span data-ttu-id="abc8c-104">Tyto termíny se často používají ve zbývající části této dokumentace, a to jak pro wpf dokumentaci konkrétně a pro ostatní rámce, které používají XAML nebo základní xaml koncepty povolené podporou jazyka XAML na úrovni System.Xaml.</span><span class="sxs-lookup"><span data-stu-id="abc8c-104">These terms are used frequently throughout the remainder of this documentation, both for WPF documentation specifically and for the other frameworks that use XAML or the basic XAML concepts enabled by the XAML language support at the System.Xaml level.</span></span> <span data-ttu-id="abc8c-105">Toto téma rozšiřuje základní terminologii zavedenou v tématu [Přehled XAML (WPF)](../../../desktop-wpf/fundamentals/xaml.md).</span><span class="sxs-lookup"><span data-stu-id="abc8c-105">This topic expands on the basic terminology introduced in the topic [XAML Overview (WPF)](../../../desktop-wpf/fundamentals/xaml.md).</span></span>  

<a name="the_xaml_language_specification"></a>
## <a name="the-xaml-language-specification"></a><span data-ttu-id="abc8c-106">Specifikace jazyka XAML</span><span class="sxs-lookup"><span data-stu-id="abc8c-106">The XAML Language Specification</span></span>  
 <span data-ttu-id="abc8c-107">Zde definovaná syntaxe XAML je také definována nebo odkazována ve specifikaci jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="abc8c-107">The XAML syntax terminology defined here is also defined or referenced within the XAML language specification.</span></span> <span data-ttu-id="abc8c-108">XAML je jazyk založený na XML a následuje nebo rozšiřuje strukturální pravidla XML.</span><span class="sxs-lookup"><span data-stu-id="abc8c-108">XAML is a language based on XML and follows or expands upon XML structural rules.</span></span> <span data-ttu-id="abc8c-109">Některé terminologie jsou sdíleny z terminologie běžně používané při popisu jazyka XML nebo objektového modelu dokumentu XML nebo jsou založeny na ní.</span><span class="sxs-lookup"><span data-stu-id="abc8c-109">Some of the terminology is shared from or is based on the terminology commonly used when describing the XML language or the XML document object model.</span></span>  
  
 <span data-ttu-id="abc8c-110">Další informace o specifikaci jazyka XAML získáte stažením [ \[ms-xaml\] ](https://download.microsoft.com/download/0/A/6/0A6F7755-9AF5-448B-907D-13985ACCF53E/[MS-XAML].pdf) ze služby Stažení softwaru.</span><span class="sxs-lookup"><span data-stu-id="abc8c-110">For more information about the XAML language specification, download [\[MS-XAML\]](https://download.microsoft.com/download/0/A/6/0A6F7755-9AF5-448B-907D-13985ACCF53E/[MS-XAML].pdf) from the Microsoft Download Center.</span></span>  
  
<a name="xaml_and_clr"></a>
## <a name="xaml-and-clr"></a><span data-ttu-id="abc8c-111">XAML a CLR</span><span class="sxs-lookup"><span data-stu-id="abc8c-111">XAML and CLR</span></span>  
 <span data-ttu-id="abc8c-112">XAML je značkovací jazyk.</span><span class="sxs-lookup"><span data-stu-id="abc8c-112">XAML is a markup language.</span></span> <span data-ttu-id="abc8c-113">Běžný jazyk runtime (CLR), jak je uvedeno jeho název, umožňuje spuštění za běhu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-113">The common language runtime (CLR), as implied by its name, enables runtime execution.</span></span> <span data-ttu-id="abc8c-114">XAML není sám o sobě jedním z běžných jazyků, který je přímo spotřebován runtime CLR.</span><span class="sxs-lookup"><span data-stu-id="abc8c-114">XAML is not by itself one of the common languages that is directly consumed by the CLR runtime.</span></span> <span data-ttu-id="abc8c-115">Místo toho si můžete myslet XAML jako podporu vlastního systému typu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-115">Instead, you can think of XAML as supporting its own type system.</span></span> <span data-ttu-id="abc8c-116">Konkrétní systém analýzy XAML, který používá WPF, je postaven na clr a systému typu CLR.</span><span class="sxs-lookup"><span data-stu-id="abc8c-116">The particular XAML parsing system that is used by WPF is built on the CLR and the CLR type system.</span></span> <span data-ttu-id="abc8c-117">XAML typy jsou mapovány na typy CLR k vytvoření instance reprezentace běhu při analýzy XAML pro WPF.</span><span class="sxs-lookup"><span data-stu-id="abc8c-117">XAML types are mapped to CLR types to instantiate a run time representation when the XAML for WPF is parsed.</span></span> <span data-ttu-id="abc8c-118">Z tohoto důvodu bude zbývající část diskuse o syntaxi v tomto dokumentu obsahovat odkazy na systém typu CLR, přestože ekvivalentní diskuse o syntaxi ve specifikaci jazyka XAML nejsou.</span><span class="sxs-lookup"><span data-stu-id="abc8c-118">For this reason, the remainder of discussion of syntax in this document will include references to the CLR type system, even though the equivalent syntax discussions in the XAML language specification do not.</span></span> <span data-ttu-id="abc8c-119">(Na úrovni specifikace jazyka XAML mohou být typy XAML mapovány na jiný typový systém, který nemusí být CLR, ale který by vyžadoval vytvoření a použití jiného analyzátoru XAML.)</span><span class="sxs-lookup"><span data-stu-id="abc8c-119">(Per the XAML language specification level, XAML types could be mapped to any other type system, which does not have to be the CLR, but that would require the creation and use of a different XAML parser.)</span></span>  
  
#### <a name="members-of-types-and-class-inheritance"></a><span data-ttu-id="abc8c-120">Členové typů a dědičnosti tříd</span><span class="sxs-lookup"><span data-stu-id="abc8c-120">Members of Types and Class Inheritance</span></span>  
 <span data-ttu-id="abc8c-121">Vlastnosti a události tak, jak se [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] zobrazují jako členy XAML typu, jsou často zděděny ze základních typů.</span><span class="sxs-lookup"><span data-stu-id="abc8c-121">Properties and events as they appear as XAML members of a [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] type are often inherited from base types.</span></span> <span data-ttu-id="abc8c-122">Zvažte například tento `<Button Background="Blue" .../>`příklad: .</span><span class="sxs-lookup"><span data-stu-id="abc8c-122">For example, consider this example: `<Button Background="Blue" .../>`.</span></span> <span data-ttu-id="abc8c-123">Vlastnost <xref:System.Windows.Controls.Control.Background%2A> není okamžitě deklarované <xref:System.Windows.Controls.Button> vlastnosti na třídu, pokud jste se podívat na definici třídy, výsledky reflexe nebo dokumentaci.</span><span class="sxs-lookup"><span data-stu-id="abc8c-123">The <xref:System.Windows.Controls.Control.Background%2A> property is not an immediately declared property on the <xref:System.Windows.Controls.Button> class, if you were to look at the class definition, reflection results, or the documentation.</span></span> <span data-ttu-id="abc8c-124">Místo <xref:System.Windows.Controls.Control.Background%2A> toho je zděděn ze základní <xref:System.Windows.Controls.Control> třídy.</span><span class="sxs-lookup"><span data-stu-id="abc8c-124">Instead, <xref:System.Windows.Controls.Control.Background%2A> is inherited from the base <xref:System.Windows.Controls.Control> class.</span></span>  
  
 <span data-ttu-id="abc8c-125">Chování dědičnosti [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] třídy prvků XAML je významným odklonem od interpretace značek XML vynucených schématem.</span><span class="sxs-lookup"><span data-stu-id="abc8c-125">The class inheritance behavior of [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] XAML elements is a significant departure from a schema-enforced interpretation of XML markup.</span></span> <span data-ttu-id="abc8c-126">Dědičnost třídy může být složitá, zejména pokud jsou zprostředkující základní třídy abstraktní nebo pokud se jedná o rozhraní.</span><span class="sxs-lookup"><span data-stu-id="abc8c-126">Class inheritance can become complex, particularly when intermediate base classes are abstract, or when interfaces are involved.</span></span> <span data-ttu-id="abc8c-127">To je jeden z důvodů, že sada prvků XAML a jejich přípustné atributy je obtížné přesně a úplně reprezentovat pomocí typů schématu, které se obvykle používají pro programování XML, jako je například formát DTD nebo XSD.</span><span class="sxs-lookup"><span data-stu-id="abc8c-127">This is one reason that the set of XAML elements and their permissible attributes is difficult to represent accurately and completely using the schema types that are typically used for XML programming, such as DTD or XSD format.</span></span> <span data-ttu-id="abc8c-128">Dalším důvodem je, že rozšiřitelnost a funkce mapování typů samotného jazyka XAML vylučují úplnost jakékoli pevné reprezentace přípustných typů a členů.</span><span class="sxs-lookup"><span data-stu-id="abc8c-128">Another reason is that extensibility and type-mapping features of the XAML language itself preclude completeness of any fixed representation of the permissible types and members.</span></span>  
  
<a name="object_element_syntax"></a>
## <a name="object-element-syntax"></a><span data-ttu-id="abc8c-129">Syntaxe prvku objektu</span><span class="sxs-lookup"><span data-stu-id="abc8c-129">Object Element Syntax</span></span>  
 <span data-ttu-id="abc8c-130">*Syntaxe prvku objektu* je syntaxe značek XAML, která inkasuje třídu nebo strukturu CLR deklarováním elementu XML.</span><span class="sxs-lookup"><span data-stu-id="abc8c-130">*Object element syntax* is the XAML markup syntax that instantiates a CLR class or structure by declaring an XML element.</span></span> <span data-ttu-id="abc8c-131">Tato syntaxe se podobá syntaxi elementu jiných značkovacích jazyků, jako je například HTML.</span><span class="sxs-lookup"><span data-stu-id="abc8c-131">This syntax resembles the element syntax of other markup languages such as HTML.</span></span> <span data-ttu-id="abc8c-132">Syntaxe prvku objektu začíná levou\<úhlovou závorkou ( ), za níž bezprostředně následuje název typu instance třídy nebo struktury.</span><span class="sxs-lookup"><span data-stu-id="abc8c-132">Object element syntax begins with a left angle bracket (\<), followed immediately by the type name of the class or structure being instantiated.</span></span> <span data-ttu-id="abc8c-133">Nula nebo více mezer může následovat název typu a nula nebo více atributů může být také deklarována na elementu objektu, s jedním nebo více mezerami oddělujícími každý pár atributů name="value".</span><span class="sxs-lookup"><span data-stu-id="abc8c-133">Zero or more spaces can follow the type name, and zero or more attributes may also be declared on the object element, with one or more spaces separating each attribute name="value" pair.</span></span> <span data-ttu-id="abc8c-134">Nakonec musí být splněna jedna z následujících skutečností:</span><span class="sxs-lookup"><span data-stu-id="abc8c-134">Finally, one of the following must be true:</span></span>  
  
- <span data-ttu-id="abc8c-135">Prvek a štítek musí být uzavřeny lomítkem (/) následovaným okamžitě pravou úhlovou závorkou (>).</span><span class="sxs-lookup"><span data-stu-id="abc8c-135">The element and tag must be closed by a forward slash (/) followed immediately by a right angle bracket (>).</span></span>  
  
- <span data-ttu-id="abc8c-136">Otevírací značka musí být doplněna pravým úhlem (>).</span><span class="sxs-lookup"><span data-stu-id="abc8c-136">The opening tag must be completed by a right angle bracket (>).</span></span> <span data-ttu-id="abc8c-137">Další prvky objektu, prvky vlastností nebo vnitřní text mohou následovat počáteční značku.</span><span class="sxs-lookup"><span data-stu-id="abc8c-137">Other object elements, property elements, or inner text, can follow the opening tag.</span></span> <span data-ttu-id="abc8c-138">Přesně jaký obsah zde může být obsažen, je obvykle omezen objektovým modelem prvku.</span><span class="sxs-lookup"><span data-stu-id="abc8c-138">Exactly what content may be contained here is typically constrained by the object model of the element.</span></span> <span data-ttu-id="abc8c-139">Ekvivalentní uzavírací značka pro prvek objektu musí také existovat, ve správném vnoření a vyvážení s ostatními dvojicemi otevírací a uzavírací značky.</span><span class="sxs-lookup"><span data-stu-id="abc8c-139">The equivalent closing tag for the object element must also exist, in proper nesting and balance with other opening and closing tag pairs.</span></span>  
  
 <span data-ttu-id="abc8c-140">XAML implementované rozhraním .NET má sadu pravidel, která mapují prvky objektu do typů, atributů do vlastností nebo událostí a obory názvů XAML do oborů názvů CLR plus sestavení.</span><span class="sxs-lookup"><span data-stu-id="abc8c-140">XAML as implemented by .NET has a set of rules that map object elements into types, attributes into properties or events, and XAML namespaces to CLR namespaces plus assembly.</span></span> <span data-ttu-id="abc8c-141">Pro WPF a .NET, XAML objektové prvky mapovat na typy .NET, jak je definováno v odkazovaných sestaveních a atributy mapovat na členy těchto typů.</span><span class="sxs-lookup"><span data-stu-id="abc8c-141">For WPF and .NET, XAML object elements map to .NET types as defined in referenced assemblies, and the attributes map to members of those types.</span></span> <span data-ttu-id="abc8c-142">Při odkazu na typ CLR v XAML, máte přístup k zděděné členy tohoto typu také.</span><span class="sxs-lookup"><span data-stu-id="abc8c-142">When you reference a CLR type in XAML, you have access to the inherited members of that type as well.</span></span>  
  
 <span data-ttu-id="abc8c-143">Například následující příklad je syntaxe elementu objektu, která <xref:System.Windows.Controls.Button> inkonfikuje <xref:System.Windows.FrameworkElement.Name%2A> novou instanci třídy a také určuje atribut a hodnotu pro tento atribut:</span><span class="sxs-lookup"><span data-stu-id="abc8c-143">For example, the following example is object element syntax that instantiates a new instance of the <xref:System.Windows.Controls.Button> class, and also specifies a <xref:System.Windows.FrameworkElement.Name%2A> attribute and a value for that attribute:</span></span>  
  
 [!code-xaml[XAMLOvwSupport#SyntaxOE](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/Page1.xaml#syntaxoe)]  
  
 <span data-ttu-id="abc8c-144">Následující příklad je syntaxe elementu objektu, která obsahuje také syntaxi vlastnosti obsahu XAML.</span><span class="sxs-lookup"><span data-stu-id="abc8c-144">The following example is object element syntax that also includes XAML content property syntax.</span></span> <span data-ttu-id="abc8c-145">Vnitřní text obsažený v písmenu a) bude použit k nastavení vlastnosti <xref:System.Windows.Controls.TextBox> obsahu XAML <xref:System.Windows.Controls.TextBox.Text%2A>.</span><span class="sxs-lookup"><span data-stu-id="abc8c-145">The inner text contained within will be used to set the <xref:System.Windows.Controls.TextBox> XAML content property, <xref:System.Windows.Controls.TextBox.Text%2A>.</span></span>  
  
 [!code-xaml[XAMLOvwSupport#ThisIsATextBox](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/Page1.xaml#thisisatextbox)]  
  
### <a name="content-models"></a><span data-ttu-id="abc8c-146">Modely obsahu</span><span class="sxs-lookup"><span data-stu-id="abc8c-146">Content Models</span></span>  
 <span data-ttu-id="abc8c-147">Třída může podporovat použití jako prvek objektu XAML z hlediska syntaxe, ale tento prvek bude fungovat správně pouze v aplikaci nebo na stránce, pokud je umístěn v očekávané pozici modelu celkového obsahu nebo stromu prvků.</span><span class="sxs-lookup"><span data-stu-id="abc8c-147">A class might support a usage as a XAML object element in terms of the syntax, but that element will only function properly in an application or page when it is placed in an expected position of an overall content model or element tree.</span></span> <span data-ttu-id="abc8c-148">Například <xref:System.Windows.Controls.MenuItem> by měl být obvykle umístěn pouze <xref:System.Windows.Controls.Primitives.MenuBase> jako podřízený <xref:System.Windows.Controls.Menu>z odvozené třídy, jako je například .</span><span class="sxs-lookup"><span data-stu-id="abc8c-148">For example, a <xref:System.Windows.Controls.MenuItem> should typically only be placed as a child of a <xref:System.Windows.Controls.Primitives.MenuBase> derived class such as <xref:System.Windows.Controls.Menu>.</span></span> <span data-ttu-id="abc8c-149">Modely obsahu pro určité prvky jsou dokumentovány jako součást poznámek na stránkách třídy pro ovládací prvky a další [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] třídy, které lze použít jako prvky XAML.</span><span class="sxs-lookup"><span data-stu-id="abc8c-149">Content models for specific elements are documented as part of the remarks on the class pages for controls and other [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] classes that can be used as XAML elements.</span></span>  
  
<a name="properties_of_object_elements"></a>
## <a name="properties-of-object-elements"></a><span data-ttu-id="abc8c-150">Vlastnosti prvků objektu</span><span class="sxs-lookup"><span data-stu-id="abc8c-150">Properties of Object Elements</span></span>  
 <span data-ttu-id="abc8c-151">Vlastnosti v XAML jsou nastaveny řadou možných syntaxí.</span><span class="sxs-lookup"><span data-stu-id="abc8c-151">Properties in XAML are set by a variety of possible syntaxes.</span></span> <span data-ttu-id="abc8c-152">Syntaxe, kterou lze použít pro určitou vlastnost, se bude lišit v závislosti na základních charakteristikách systému typu vlastnosti, kterou nastavujete.</span><span class="sxs-lookup"><span data-stu-id="abc8c-152">Which syntax can be used for a particular property will vary, based on the underlying type system characteristics of the property that you are setting.</span></span>  
  
 <span data-ttu-id="abc8c-153">Nastavením hodnot vlastností přidáte prvky nebo charakteristiky k objektům tak, jak existují v grafu objektů běhu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-153">By setting values of properties, you add features or characteristics to objects as they exist in the run time object graph.</span></span> <span data-ttu-id="abc8c-154">Počáteční stav vytvořeného objektu z prvku objektu je založen na chování konstruktoru bez parametrů.</span><span class="sxs-lookup"><span data-stu-id="abc8c-154">The initial state of the created object from a object element is based on the parameterless constructor behavior.</span></span> <span data-ttu-id="abc8c-155">Aplikace obvykle použije něco jiného než zcela výchozí instanci daného objektu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-155">Typically, your application will use something other than a completely default instance of any given object.</span></span>  
  
<a name="attribute_syntax_properties"></a>
## <a name="attribute-syntax-properties"></a><span data-ttu-id="abc8c-156">Syntaxe atributu (vlastnosti)</span><span class="sxs-lookup"><span data-stu-id="abc8c-156">Attribute Syntax (Properties)</span></span>  
 <span data-ttu-id="abc8c-157">Syntaxe atributu je syntaxe značek XAML, která nastavuje hodnotu vlastnosti deklarováním atributu existujícího prvku objektu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-157">Attribute syntax is the XAML markup syntax that sets a value for a property by declaring an attribute on an existing object element.</span></span> <span data-ttu-id="abc8c-158">Název atributu se musí shodovat s názvem člena CLR vlastnosti třídy, která podporuje příslušný prvek objektu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-158">The attribute name must match the CLR member name of the property of the class that backs the relevant object element.</span></span> <span data-ttu-id="abc8c-159">Za názvem atributu následuje operátor přiřazení (=).</span><span class="sxs-lookup"><span data-stu-id="abc8c-159">The attribute name is followed by an assignment operator (=).</span></span> <span data-ttu-id="abc8c-160">Hodnota atributu musí být řetězec uzavřený v uvozovkách.</span><span class="sxs-lookup"><span data-stu-id="abc8c-160">The attribute value must be a string enclosed within quotes.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="abc8c-161">Střídání uvozovek můžete použít k umístění literálu uvozovky do atributu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-161">You can use alternating quotes to place a literal quotation mark within an attribute.</span></span> <span data-ttu-id="abc8c-162">Například můžete použít jednoduché uvozovky jako prostředek k deklarování řetězec, který obsahuje znak dvojité uvozovky v něm.</span><span class="sxs-lookup"><span data-stu-id="abc8c-162">For instance you can use single quotes as a means to declare a string that contains a double quote character within it.</span></span> <span data-ttu-id="abc8c-163">Bez ohledu na to, zda používáte jednoduché nebo dvojité uvozovky, měli byste použít odpovídající dvojici pro otevření a zavření řetězce hodnoty atributu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-163">Whether you use single or double quotes, you should use a matching pair for opening and closing the attribute value string.</span></span> <span data-ttu-id="abc8c-164">K dispozici jsou také únikové sekvence nebo jiné techniky pro práci kolem omezení znaků, které jsou uloženy libovolnou konkrétní syntaxí XAML.</span><span class="sxs-lookup"><span data-stu-id="abc8c-164">There are also escape sequences or other techniques available for working around character restrictions imposed by any particular XAML syntax.</span></span> <span data-ttu-id="abc8c-165">Viz [Entity znaků XML a XAML](../../../desktop-wpf/xaml-services/xml-character-entities.md).</span><span class="sxs-lookup"><span data-stu-id="abc8c-165">See [XML Character Entities and XAML](../../../desktop-wpf/xaml-services/xml-character-entities.md).</span></span>  
  
 <span data-ttu-id="abc8c-166">Aby bylo možné nastavit prostřednictvím syntaxe atributu, vlastnost musí být veřejné a musí být zapisovatelné.</span><span class="sxs-lookup"><span data-stu-id="abc8c-166">In order to be set through attribute syntax, a property must be public and must be writeable.</span></span> <span data-ttu-id="abc8c-167">Hodnota vlastnosti v systému typu zálohování musí být typ hodnoty nebo musí být typ odkazu, který může být vytvořena instance nebo odkazována procesorem XAML při přístupu k příslušnému typu zálohování.</span><span class="sxs-lookup"><span data-stu-id="abc8c-167">The value of the property in the backing type system must be a value type, or must be a reference type that can be instantiated or referenced by a XAML processor when accessing the relevant backing type.</span></span>  
  
 <span data-ttu-id="abc8c-168">Pro události WPF XAML událost, která je odkazována jako název atributu, musí být veřejná a mít veřejného delegáta.</span><span class="sxs-lookup"><span data-stu-id="abc8c-168">For WPF XAML events, the event that is referenced as the attribute name must be public and have a public delegate.</span></span>  
  
 <span data-ttu-id="abc8c-169">Vlastnost nebo událost musí být členem třídy nebo struktury, která je vytvořena pomocí elementu obsahujícího objektu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-169">The property or event must be a member of the class or structure that is instantiated by the containing object element.</span></span>  
  
### <a name="processing-of-attribute-values"></a><span data-ttu-id="abc8c-170">Zpracování hodnot atributů</span><span class="sxs-lookup"><span data-stu-id="abc8c-170">Processing of Attribute Values</span></span>  
 <span data-ttu-id="abc8c-171">Hodnota řetězce obsažená v počátečních a závěrečných uvozovkách je zpracována procesorem XAML.</span><span class="sxs-lookup"><span data-stu-id="abc8c-171">The string value contained within the opening and closing quotation marks is processed by a XAML processor.</span></span> <span data-ttu-id="abc8c-172">Pro vlastnosti je výchozí chování zpracování určeno typem základní vlastnosti CLR.</span><span class="sxs-lookup"><span data-stu-id="abc8c-172">For properties, the default processing behavior is determined by the type of the underlying CLR property.</span></span>  
  
 <span data-ttu-id="abc8c-173">Hodnota atributu je vyplněna jedním z následujících, pomocí tohoto pořadí zpracování:</span><span class="sxs-lookup"><span data-stu-id="abc8c-173">The attribute value is filled by one of the following, using this processing order:</span></span>  
  
1. <span data-ttu-id="abc8c-174">Pokud procesor XAML narazí na složené složená závorka nebo prvek objektu, který je odvozen z <xref:System.Windows.Markup.MarkupExtension>, pak odkazované rozšíření značky je vyhodnocena jako první místo zpracování hodnoty jako řetězec a objekt vrácený rozšíření značky se používá jako hodnota.</span><span class="sxs-lookup"><span data-stu-id="abc8c-174">If the XAML processor encounters a curly brace, or an object element that derives from <xref:System.Windows.Markup.MarkupExtension>, then the referenced markup extension is evaluated first rather than processing the value as a string, and the object returned by the markup extension is used as the value.</span></span> <span data-ttu-id="abc8c-175">V mnoha případech objekt vrácený rozšíření mnoství matné značky bude odkaz na existující objekt nebo výraz, který odkládá hodnocení až do běhu a není nově instance objektu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-175">In many cases the object returned by a markup extension will be a reference to an existing object, or an expression that defers evaluation until run time, and is not a newly instantiated object.</span></span>  
  
2. <span data-ttu-id="abc8c-176">Pokud je vlastnost deklarována s přiřazeným <xref:System.ComponentModel.TypeConverter>nebo typ hodnoty <xref:System.ComponentModel.TypeConverter>této vlastnosti je deklarován s atributem , je řetězcová hodnota atributu odeslána převaděči typu jako vstup převodu a převaděč vrátí novou instanci objektu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-176">If the property is declared with an attributed <xref:System.ComponentModel.TypeConverter>, or the value type of that property is declared with an attributed <xref:System.ComponentModel.TypeConverter>, the string value of the attribute is submitted to the type converter as a conversion input, and the converter will return a new object instance.</span></span>  
  
3. <span data-ttu-id="abc8c-177">Pokud není <xref:System.ComponentModel.TypeConverter>k dispozici , je pokus o přímý převod na typ vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="abc8c-177">If there is no <xref:System.ComponentModel.TypeConverter>, a direct conversion to the property type is attempted.</span></span> <span data-ttu-id="abc8c-178">Tato konečná úroveň je přímý převod na parser nativní hodnotu mezi typy jazyka XAML primitivní nebo kontrola názvů pojmenovaných konstant ve výčtu (analyzátor pak přistupuje k odpovídající hodnoty).</span><span class="sxs-lookup"><span data-stu-id="abc8c-178">This final level is a direct conversion at the parser-native value between XAML language primitive types, or a check for the names of named constants in an enumeration (the parser then accesses the matching values).</span></span>  
  
#### <a name="enumeration-attribute-values"></a><span data-ttu-id="abc8c-179">Hodnoty atributů výčtu</span><span class="sxs-lookup"><span data-stu-id="abc8c-179">Enumeration Attribute Values</span></span>  
 <span data-ttu-id="abc8c-180">Výčty v XAML jsou zpracovány vnitřně analyzátory XAML a členy výčtu by měly být určeny zadáním názvu řetězce jedné z pojmenovaných konstant výčtu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-180">Enumerations in XAML are processed intrinsically by XAML parsers, and the members of an enumeration should be specified by specifying the string name of one of the enumeration's named constants.</span></span>  
  
 <span data-ttu-id="abc8c-181">Pro hodnoty výčtu bez příznaku je nativní chování zpracovat řetězec hodnoty atributu a vyřešit jej na jednu z hodnot výčtu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-181">For nonflag enumeration values, the native behavior is to process the string of an attribute value and resolve it to one of the enumeration values.</span></span> <span data-ttu-id="abc8c-182">Výčet ve formátu *Výčet*nezadáte . *Hodnota*, stejně jako v kódu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-182">You do not specify the enumeration in the format *Enumeration*.*Value*, as you do in code.</span></span> <span data-ttu-id="abc8c-183">Místo toho zadáte pouze *Value*a *výčet* je odvozen podle typu vlastnosti, kterou nastavujete.</span><span class="sxs-lookup"><span data-stu-id="abc8c-183">Instead, you specify only *Value*, and *Enumeration* is inferred by the type of the property you are setting.</span></span> <span data-ttu-id="abc8c-184">Pokud zadáte atribut ve *výčtu*. *Hodnotový* formulář, nevyřeší správně.</span><span class="sxs-lookup"><span data-stu-id="abc8c-184">If you specify an attribute in the *Enumeration*.*Value* form, it will not resolve correctly.</span></span>  
  
 <span data-ttu-id="abc8c-185">Pro výčty ve výšce ve vlajkovém směru je chování založeno na metodě. <xref:System.Enum.Parse%2A?displayProperty=nameWithType></span><span class="sxs-lookup"><span data-stu-id="abc8c-185">For flagwise enumerations, the behavior is based on the <xref:System.Enum.Parse%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="abc8c-186">Můžete zadat více hodnot pro výčtu ve vlajkovém směru oddělením každé hodnoty čárkou.</span><span class="sxs-lookup"><span data-stu-id="abc8c-186">You can specify multiple values for a flagwise enumeration by separating each value with a comma.</span></span> <span data-ttu-id="abc8c-187">Nelze však kombinovat hodnoty výčtu, které nejsou ve samáza.</span><span class="sxs-lookup"><span data-stu-id="abc8c-187">However, you cannot combine enumeration values that are not flagwise.</span></span> <span data-ttu-id="abc8c-188">Například nelze použít syntaxi čárky k pokusu <xref:System.Windows.Trigger> o vytvoření, který funguje na více podmínek výčtu bez příznaku:</span><span class="sxs-lookup"><span data-stu-id="abc8c-188">For instance, you cannot use the comma syntax to attempt to create a <xref:System.Windows.Trigger> that acts on multiple conditions of a nonflag enumeration:</span></span>  
  
```xaml  
<!--This will not compile, because Visibility is not a flagwise enumeration.-->  
...  
<Trigger Property="Visibility" Value="Collapsed,Hidden">  
  <Setter ... />  
</Trigger>  
...  
```  
  
 <span data-ttu-id="abc8c-189">Počet výčtů ve wawise, které podporují atributy, které jsou v XAML nastaveny, jsou v WPF vzácné.</span><span class="sxs-lookup"><span data-stu-id="abc8c-189">Flagwise enumerations that support attributes that are settable in XAML are rare in WPF.</span></span> <span data-ttu-id="abc8c-190">Jeden takový výčet je <xref:System.Windows.Media.StyleSimulations>však .</span><span class="sxs-lookup"><span data-stu-id="abc8c-190">However, one such enumeration is <xref:System.Windows.Media.StyleSimulations>.</span></span> <span data-ttu-id="abc8c-191">Můžete například použít syntaxi atributu flagwise oddělenou čárkou k úpravě příkladu <xref:System.Windows.Documents.Glyphs> uvedeného v poznámkách pro třídu; `StyleSimulations = "BoldSimulation"` by `StyleSimulations = "BoldSimulation,ItalicSimulation"`se mohla stát .</span><span class="sxs-lookup"><span data-stu-id="abc8c-191">You could, for instance, use the comma-delimited flagwise attribute syntax to modify the example provided in the Remarks for the <xref:System.Windows.Documents.Glyphs> class; `StyleSimulations = "BoldSimulation"` could become `StyleSimulations = "BoldSimulation,ItalicSimulation"`.</span></span> <span data-ttu-id="abc8c-192"><xref:System.Windows.Input.KeyBinding.Modifiers%2A?displayProperty=nameWithType>je jiná vlastnost, kde lze zadat více než jednu hodnotu výčtu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-192"><xref:System.Windows.Input.KeyBinding.Modifiers%2A?displayProperty=nameWithType> is another property where more than one enumeration value can be specified.</span></span> <span data-ttu-id="abc8c-193">Tato vlastnost je však stane zvláštní <xref:System.Windows.Input.ModifierKeys> případ, protože výčet podporuje jeho vlastní převaděč typu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-193">However, this property happens to be a special case, because the <xref:System.Windows.Input.ModifierKeys> enumeration supports its own type converter.</span></span> <span data-ttu-id="abc8c-194">Převaděč typu pro modifikátory používá znaménko plus (+) jako oddělovač spíše než čárku (,).</span><span class="sxs-lookup"><span data-stu-id="abc8c-194">The type converter for modifiers uses a plus sign (+) as a delimiter rather than a comma (,).</span></span> <span data-ttu-id="abc8c-195">Tento převod podporuje tradiční syntaxi představující kombinace kláves v programování systému Microsoft Windows, například "Ctrl+Alt".</span><span class="sxs-lookup"><span data-stu-id="abc8c-195">This conversion supports the more traditional syntax to represent key combinations in Microsoft Windows programming, such as "Ctrl+Alt".</span></span>  
  
### <a name="properties-and-event-member-name-references"></a><span data-ttu-id="abc8c-196">Odkazy na názvy členů událostí a události</span><span class="sxs-lookup"><span data-stu-id="abc8c-196">Properties and Event Member Name References</span></span>  
 <span data-ttu-id="abc8c-197">Při zadávání atributu můžete odkazovat na libovolnou vlastnost nebo událost, která existuje jako člen typu CLR, který jste instance pro prvek obsahující objekt.</span><span class="sxs-lookup"><span data-stu-id="abc8c-197">When specifying an attribute, you can reference any property or event that exists as a member of the CLR type you instantiated for the containing object element.</span></span>  
  
 <span data-ttu-id="abc8c-198">Nebo můžete odkazovat na připojenou vlastnost nebo připojenou událost, nezávisle na prvku obsahujícího objektu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-198">Or, you can reference an attached property or attached event, independent of the containing object element.</span></span> <span data-ttu-id="abc8c-199">(Připojené vlastnosti jsou popsány v nadcházející části.)</span><span class="sxs-lookup"><span data-stu-id="abc8c-199">(Attached properties are discussed in an upcoming section.)</span></span>  
  
 <span data-ttu-id="abc8c-200">Můžete také pojmenovat libovolnou událost z libovolného objektu, který je přístupný prostřednictvím výchozího oboru názvů pomocí *typeName*. *událost* částečně kvalifikovaný název; Tato syntaxe podporuje připojení obslužných rutin pro směrované události, kde je obslužná rutina určena ke zpracování směrování událostí z podřízených prvků, ale nadřazený prvek nemá tuto událost také v tabulce členů.</span><span class="sxs-lookup"><span data-stu-id="abc8c-200">You can also name any event from any object that is accessible through the default namespace by using a *typeName*.*event* partially qualified name; this syntax supports attaching handlers for routed events where the handler is intended to handle events routing from child elements, but the parent element does not also have that event in its members table.</span></span> <span data-ttu-id="abc8c-201">Tato syntaxe se podobá připojené syntaxi události, ale událost zde není true attached událost.</span><span class="sxs-lookup"><span data-stu-id="abc8c-201">This syntax resembles an attached event syntax, but the event here is not a true attached event.</span></span> <span data-ttu-id="abc8c-202">Místo toho odkazujete na událost s kvalifikovaným názvem.</span><span class="sxs-lookup"><span data-stu-id="abc8c-202">Instead, you are referencing an event with a qualified name.</span></span> <span data-ttu-id="abc8c-203">Další informace naleznete v tématu [Přehled směrovaných událostí](routed-events-overview.md).</span><span class="sxs-lookup"><span data-stu-id="abc8c-203">For more information, see [Routed Events Overview](routed-events-overview.md).</span></span>  
  
 <span data-ttu-id="abc8c-204">Pro některé scénáře jsou někdy názvy vlastností k dispozici jako hodnota atributu, nikoli jako název atributu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-204">For some scenarios, property names are sometimes provided as the value of an attribute, rather than the attribute name.</span></span> <span data-ttu-id="abc8c-205">Tento název vlastnosti může také obsahovat kvalifikátory, například vlastnost zadaná ve formuláři *ownerType*. *dependencyPropertyName*.</span><span class="sxs-lookup"><span data-stu-id="abc8c-205">That property name can also include qualifiers, such as the property specified in the form *ownerType*.*dependencyPropertyName*.</span></span> <span data-ttu-id="abc8c-206">Tento scénář je běžné při psaní stylů nebo šablon v XAML.</span><span class="sxs-lookup"><span data-stu-id="abc8c-206">This scenario is common when writing styles or templates in XAML.</span></span> <span data-ttu-id="abc8c-207">Pravidla zpracování pro názvy vlastností poskytované jako hodnota atributu se liší a řídí se typem nastavené vlastnosti nebo chováním konkrétních podsystémů WPF.</span><span class="sxs-lookup"><span data-stu-id="abc8c-207">The processing rules for property names provided as an attribute value are different, and are governed by the type of the property being set or by the behaviors of particular WPF subsystems.</span></span> <span data-ttu-id="abc8c-208">Podrobnosti viz [Styling a šablonování](../../../desktop-wpf/fundamentals/styles-templates-overview.md).</span><span class="sxs-lookup"><span data-stu-id="abc8c-208">For details, see [Styling and Templating](../../../desktop-wpf/fundamentals/styles-templates-overview.md).</span></span>  
  
 <span data-ttu-id="abc8c-209">Jiné použití pro názvy vlastností je, když hodnota atributu popisuje vztah vlastnost-vlastnost.</span><span class="sxs-lookup"><span data-stu-id="abc8c-209">Another usage for property names is when an attribute value describes a property-property relationship.</span></span> <span data-ttu-id="abc8c-210">Tato funkce se používá pro datové vazby a pro <xref:System.Windows.PropertyPath> cíle scénáře a je povolena třídou a jeho převaděč typu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-210">This feature is used for data binding and for storyboard targets, and is enabled by the <xref:System.Windows.PropertyPath> class and its type converter.</span></span> <span data-ttu-id="abc8c-211">Podrobnější popis vyhledávací sémantiky naleznete v tématu [PropertyPath XAML Syntaxe](propertypath-xaml-syntax.md).</span><span class="sxs-lookup"><span data-stu-id="abc8c-211">For a more complete description of the lookup semantics, see [PropertyPath XAML Syntax](propertypath-xaml-syntax.md).</span></span>  
  
<a name="property_element_syntax"></a>
## <a name="property-element-syntax"></a><span data-ttu-id="abc8c-212">Syntaxe prvku vlastnosti</span><span class="sxs-lookup"><span data-stu-id="abc8c-212">Property Element Syntax</span></span>  
 <span data-ttu-id="abc8c-213">*Syntaxe prvku vlastnosti* je syntaxe, která se poněkud liší od základních pravidel syntaxe XML pro elementy.</span><span class="sxs-lookup"><span data-stu-id="abc8c-213">*Property element syntax* is a syntax that diverges somewhat from the basic XML syntax rules for elements.</span></span> <span data-ttu-id="abc8c-214">V jazyce XML je hodnota atributu de facto řetězcem, přičemž jedinou možnou variantou je, který formát kódování řetězců se používá.</span><span class="sxs-lookup"><span data-stu-id="abc8c-214">In XML, the value of an attribute is a de facto string, with the only possible variation being which string encoding format is being used.</span></span> <span data-ttu-id="abc8c-215">V XAML můžete přiřadit další prvky objektu jako hodnotu vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="abc8c-215">In XAML, you can assign other object elements to be the value of a property.</span></span> <span data-ttu-id="abc8c-216">Tato funkce je povolena syntaxí prvku vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="abc8c-216">This capability is enabled by the property element syntax.</span></span> <span data-ttu-id="abc8c-217">Místo vlastnosti, která je zadána jako atribut v rámci značky elementu, je vlastnost určena pomocí značky počátečního prvku v *elementu Name*. *propertyName* formulář, hodnota vlastnosti je zadán v rámci a pak je prvek vlastnosti uzavřen.</span><span class="sxs-lookup"><span data-stu-id="abc8c-217">Instead of the property being specified as an attribute within the element tag, the property is specified using an opening element tag in *elementTypeName*.*propertyName* form, the value of the property is specified within, and then the property element is closed.</span></span>  
  
 <span data-ttu-id="abc8c-218">Konkrétně syntaxe začíná levou úhlovou\<závorkou ( ), následovanou bezprostředně názvem typu třídy nebo struktury, ve které je obsažena syntaxe prvku vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="abc8c-218">Specifically, the syntax begins with a left angle bracket (\<), followed immediately by the type name of the class or structure that the property element syntax is contained within.</span></span> <span data-ttu-id="abc8c-219">Následuje bezprostředně jedna tečka (.), poté název vlastnosti, pak pravý úhel držáku (>).</span><span class="sxs-lookup"><span data-stu-id="abc8c-219">This is followed immediately by a single dot (.), then by the name of a property, then by a right angle bracket (>).</span></span> <span data-ttu-id="abc8c-220">Stejně jako u syntaxe atributu musí tato vlastnost existovat v rámci deklarovaných veřejných členů zadaného typu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-220">As with attribute syntax, that property must exist within the declared public members of the specified type.</span></span> <span data-ttu-id="abc8c-221">Hodnota, která má být přiřazena vlastnosti, je obsažena v elementu vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="abc8c-221">The value to be assigned to the property is contained within the property element.</span></span> <span data-ttu-id="abc8c-222">Hodnota je obvykle uvedena jako jeden nebo více prvků objektu, protože určení objektů jako hodnot je scénář, který má syntaxe prvku vlastnosti adresovat.</span><span class="sxs-lookup"><span data-stu-id="abc8c-222">Typically, the value is given as one or more object elements, because specifying objects as values is the scenario that property element syntax is intended to address.</span></span> <span data-ttu-id="abc8c-223">Nakonec ekvivalentní uzavírací značka určující stejný *elementTypeName*. *propertyName* kombinace musí být poskytnuta, ve správném vnoření a vyvážení s jinými značkami elementu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-223">Finally, an equivalent closing tag specifying the same *elementTypeName*.*propertyName* combination must be provided, in proper nesting and balance with other element tags.</span></span>  
  
 <span data-ttu-id="abc8c-224">Například následující je syntaxe elementu vlastnosti <xref:System.Windows.FrameworkElement.ContextMenu%2A> pro vlastnost . <xref:System.Windows.Controls.Button></span><span class="sxs-lookup"><span data-stu-id="abc8c-224">For example, the following is property element syntax for the <xref:System.Windows.FrameworkElement.ContextMenu%2A> property of a <xref:System.Windows.Controls.Button>.</span></span>  
  
 [!code-xaml[XAMLOvwSupport#ContextMenu](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/Page1.xaml#contextmenu)]  
  
 <span data-ttu-id="abc8c-225">Hodnota v rámci elementu vlastnosti může být také uvedena jako vnitřní text, v případech, kdy je zadán typ vlastnosti primitivní typ hodnoty, například <xref:System.String>, nebo výčet, kde je zadán název.</span><span class="sxs-lookup"><span data-stu-id="abc8c-225">The value within a property element can also be given as inner text, in cases where the property type being specified is a primitive value type, such as <xref:System.String>, or an enumeration where a name is specified.</span></span> <span data-ttu-id="abc8c-226">Tato dvě použití jsou poněkud neobvyklé, protože každý z těchto případů může také použít jednodušší atribut syntaxe.</span><span class="sxs-lookup"><span data-stu-id="abc8c-226">These two usages are somewhat uncommon, because each of these cases could also use a simpler attribute syntax.</span></span> <span data-ttu-id="abc8c-227">Jeden scénář pro vyplnění elementvlastnosti řetězcem je pro vlastnosti, které nejsou vlastností obsahu XAML, ale stále se používají pro reprezentaci textu uživatelského rozhraní a určité prázdné prvky, jako jsou linefeeds, se musí zobrazit v textu uživatelského rozhraní.</span><span class="sxs-lookup"><span data-stu-id="abc8c-227">One scenario for filling a property element with a string is for properties that are not the XAML content property but still are used for representation of UI text, and particular white-space elements such as linefeeds are required to appear in that UI text.</span></span> <span data-ttu-id="abc8c-228">Syntaxe atributu nemůže zachovat linefeeds, ale hodnota elementu vlastnosti může, pokud je aktivní významné zachování prázdného místa (podrobnosti viz [Zpracování prázdného místa v XAML).](../../../desktop-wpf/xaml-services/white-space-processing.md)</span><span class="sxs-lookup"><span data-stu-id="abc8c-228">Attribute syntax cannot preserve linefeeds, but property element syntax can, so long as significant white-space preservation is active (for details, see [White space processing in XAML](../../../desktop-wpf/xaml-services/white-space-processing.md)).</span></span> <span data-ttu-id="abc8c-229">Jiný scénář je tak, že [x:Uid směrnice](../../../desktop-wpf/xaml-services/xuid-directive.md) lze použít na element vlastnosti a proto označit hodnotu v rámci jako hodnotu, která by měla být lokalizována ve výstupu WPF BAML nebo jinými technikami.</span><span class="sxs-lookup"><span data-stu-id="abc8c-229">Another scenario is so that [x:Uid Directive](../../../desktop-wpf/xaml-services/xuid-directive.md) can be applied to the property element and thus mark the value within as a value that should be localized in the WPF output BAML or by other techniques.</span></span>  
  
 <span data-ttu-id="abc8c-230">Prvek vlastnosti není reprezentován v logickém stromu WPF.</span><span class="sxs-lookup"><span data-stu-id="abc8c-230">A property element is not represented in the WPF logical tree.</span></span> <span data-ttu-id="abc8c-231">Element vlastnosti je pouze určitá syntaxe pro nastavení vlastnosti a není prvek, který má instanci nebo objekt, který ji podporuje.</span><span class="sxs-lookup"><span data-stu-id="abc8c-231">A property element is just a particular syntax for setting a property, and is not an element that has an instance or object backing it.</span></span> <span data-ttu-id="abc8c-232">(Podrobnosti o konceptu logického stromu naleznete [v tématu Stromy v WPF](trees-in-wpf.md).)</span><span class="sxs-lookup"><span data-stu-id="abc8c-232">(For details on the logical tree concept, see [Trees in WPF](trees-in-wpf.md).)</span></span>  
  
 <span data-ttu-id="abc8c-233">Pro vlastnosti, kde jsou podporovány syntaxe atributu a elementu vlastností, mají dvě syntaxe obvykle stejný výsledek, i když jemnosti, jako je například zpracování prázdného místa, se mohou mírně lišit mezi syntaxemi.</span><span class="sxs-lookup"><span data-stu-id="abc8c-233">For properties where both attribute and property element syntax are supported, the two syntaxes generally have the same result, although subtleties such as white-space handling can vary slightly between syntaxes.</span></span>  
  
<a name="collection_syntax"></a>
## <a name="collection-syntax"></a><span data-ttu-id="abc8c-234">Syntaxe kolekce</span><span class="sxs-lookup"><span data-stu-id="abc8c-234">Collection Syntax</span></span>  
 <span data-ttu-id="abc8c-235">Specifikace XAML vyžaduje implementace procesoru XAML k identifikaci vlastností, kde je typ hodnoty kolekce.</span><span class="sxs-lookup"><span data-stu-id="abc8c-235">The XAML specification requires XAML processor implementations to identify properties where the value type is a collection.</span></span> <span data-ttu-id="abc8c-236">Obecná implementace procesoru XAML v rozhraní .NET je založena na spravovaném kódu a CLR a identifikuje typy kolekcí prostřednictvím jedné z následujících možností:</span><span class="sxs-lookup"><span data-stu-id="abc8c-236">The general XAML processor implementation in .NET is based on managed code and the CLR, and it identifies collection types through one of the following:</span></span>  
  
- <span data-ttu-id="abc8c-237">Typ implements <xref:System.Collections.IList>.</span><span class="sxs-lookup"><span data-stu-id="abc8c-237">Type implements <xref:System.Collections.IList>.</span></span>  
  
- <span data-ttu-id="abc8c-238">Typ implements <xref:System.Collections.IDictionary>.</span><span class="sxs-lookup"><span data-stu-id="abc8c-238">Type implements <xref:System.Collections.IDictionary>.</span></span>  
  
- <span data-ttu-id="abc8c-239">Typ je <xref:System.Array> odvozen od (další informace o polích v XAML naleznete v tématu [x:Array Markup Extension](../../../desktop-wpf/xaml-services/xarray-markup-extension.md).)</span><span class="sxs-lookup"><span data-stu-id="abc8c-239">Type derives from <xref:System.Array> (for more information about arrays in XAML, see [x:Array Markup Extension](../../../desktop-wpf/xaml-services/xarray-markup-extension.md).)</span></span>  
  
 <span data-ttu-id="abc8c-240">Pokud je typ vlastnosti kolekce, pak odvozený typ kolekce nemusí být zadán ve značkách jako element objektu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-240">If the type of a property is a collection, then the inferred collection type does not need to be specified in the markup as an object element.</span></span> <span data-ttu-id="abc8c-241">Místo toho prvky, které jsou určeny k tomu, aby se staly položkami v kolekci, jsou určeny jako jeden nebo více podřízených prvků elementu vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="abc8c-241">Instead, the elements that are intended to become the items in the collection are specified as one or more child elements of the property element.</span></span> <span data-ttu-id="abc8c-242">Každá taková položka je vyhodnocena na objekt během `Add` načítání a přidána do kolekce voláním metody implicitní kolekce.</span><span class="sxs-lookup"><span data-stu-id="abc8c-242">Each such item is evaluated to an object during loading and added to the collection by calling the `Add` method of the implied collection.</span></span> <span data-ttu-id="abc8c-243">Například <xref:System.Windows.Style.Triggers%2A> vlastnost <xref:System.Windows.Style> přebírá typ specializované kolekce <xref:System.Windows.TriggerCollection>, který <xref:System.Collections.IList>implementuje .</span><span class="sxs-lookup"><span data-stu-id="abc8c-243">For example, the <xref:System.Windows.Style.Triggers%2A> property of <xref:System.Windows.Style> takes the specialized collection type <xref:System.Windows.TriggerCollection>, which implements <xref:System.Collections.IList>.</span></span> <span data-ttu-id="abc8c-244">Není nutné vytvořit instanci <xref:System.Windows.TriggerCollection> prvku objektu ve značkách.</span><span class="sxs-lookup"><span data-stu-id="abc8c-244">It is not necessary to instantiate a <xref:System.Windows.TriggerCollection> object element in the markup.</span></span> <span data-ttu-id="abc8c-245">Místo toho zadáte jednu nebo <xref:System.Windows.Trigger> více `Style.Triggers` položek jako <xref:System.Windows.Trigger> prvky v rámci elementu vlastnosti, kde (nebo odvozené <xref:System.Windows.TriggerCollection>třídy) je typ očekávaný jako typ položky pro silně typované a implicitní .</span><span class="sxs-lookup"><span data-stu-id="abc8c-245">Instead, you specify one or more <xref:System.Windows.Trigger> items as elements within the `Style.Triggers` property element, where <xref:System.Windows.Trigger> (or a derived class) is the type expected as the item type for the strongly typed and implicit <xref:System.Windows.TriggerCollection>.</span></span>  
  
 [!code-xaml[XAMLOvwSupport#SyntaxPECollection](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/Page1.xaml#syntaxpecollection)]  
  
 <span data-ttu-id="abc8c-246">Vlastnost může být typ kolekce a xaml content vlastnost pro tento typ a odvozené typy, který je popsán v další části tohoto tématu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-246">A property may be both a collection type and the XAML content property for that type and derived types, which is discussed in the next section of this topic.</span></span>  
  
 <span data-ttu-id="abc8c-247">Implicitní prvek kolekce vytvoří člen v reprezentaci logického stromu, i když se nezobrazí ve značkách jako prvek.</span><span class="sxs-lookup"><span data-stu-id="abc8c-247">An implicit collection element creates a member in the logical tree representation, even though it does not appear in the markup as an element.</span></span> <span data-ttu-id="abc8c-248">Obvykle konstruktor nadřazeného typu provádí instanci kolekce, která je jednou z jeho vlastností a zpočátku prázdná kolekce se stane součástí stromu objektů.</span><span class="sxs-lookup"><span data-stu-id="abc8c-248">Usually the constructor of the parent type performs the instantiation for the collection that is one of its properties, and the initially empty collection becomes part of the object tree.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="abc8c-249">Obecné seznam a slovníkrozhraní<xref:System.Collections.Generic.IList%601> ( <xref:System.Collections.Generic.IDictionary%602>a ) nejsou podporovány pro zjišťování kolekce.</span><span class="sxs-lookup"><span data-stu-id="abc8c-249">The generic list and dictionary interfaces (<xref:System.Collections.Generic.IList%601> and <xref:System.Collections.Generic.IDictionary%602>) are not supported for collection detection.</span></span> <span data-ttu-id="abc8c-250">Třídu <xref:System.Collections.Generic.List%601> však můžete použít jako základní třídu, protože implementuje <xref:System.Collections.IList> přímo nebo <xref:System.Collections.Generic.Dictionary%602> <xref:System.Collections.IDictionary> jako základní třídu, protože implementuje přímo.</span><span class="sxs-lookup"><span data-stu-id="abc8c-250">However, you can use the <xref:System.Collections.Generic.List%601> class as a base class, because it implements <xref:System.Collections.IList> directly, or <xref:System.Collections.Generic.Dictionary%602> as a base class, because it implements <xref:System.Collections.IDictionary> directly.</span></span>  
  
 <span data-ttu-id="abc8c-251">Na odkazových stránkách .NET pro typy kolekcí je tato syntaxe s záměrným vynecháním elementu objektu pro kolekci příležitostně zaznamenána v částech syntaxe XAML jako Implicitní syntaxe kolekce.</span><span class="sxs-lookup"><span data-stu-id="abc8c-251">In the .NET Reference pages for collection types, this syntax with the deliberate omission of the object element for a collection is occasionally noted in the XAML syntax sections as Implicit Collection Syntax.</span></span>  
  
 <span data-ttu-id="abc8c-252">S výjimkou kořenového prvku je každý element objektu v souboru XAML, který je vnořen jako podřízený prvek jiného prvku, skutečně elementem, který je jedním nebo oběma následujícími případy: členem implicitní vlastnosti kolekce nadřazeného prvku nebo elementem, který určuje hodnotu vlastnosti obsahu XAML pro nadřazený prvek (vlastnosti obsahu XAML budou popsány v nadcházející části).</span><span class="sxs-lookup"><span data-stu-id="abc8c-252">With the exception of the root element, every object element in a XAML file that is nested as a child element of another element is really an element that is one or both of the following cases: a member of an implicit collection property of its parent element, or an element that specifies the value of the XAML content property for the parent element (XAML content properties will be discussed in an upcoming section).</span></span> <span data-ttu-id="abc8c-253">Jinými slovy vztah nadřazených prvků a podřízených prvků na stránce značek je skutečně jeden objekt v kořenovém adresáři a každý prvek objektu pod kořenem je buď jedna instance, která poskytuje hodnotu vlastnosti nadřazeného, nebo jednu z položek v rámci kolekce, která je také hodnotou vlastnosti typu kolekce nadřazeného objektu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-253">In other words, the relationship of parent elements and child elements in a markup page is really a single object at the root, and every object element beneath the root is either a single instance that provides a property value of the parent, or one of the items within a collection that is also a collection-type property value of the parent.</span></span> <span data-ttu-id="abc8c-254">Tento koncept jednoho kořene je společný s XML a je často posílena v chování <xref:System.Windows.Markup.XamlReader.Load%2A>rozhraní API, která načítají XAML, například .</span><span class="sxs-lookup"><span data-stu-id="abc8c-254">This single-root concept is common with XML, and is frequently reinforced in the behavior of APIs that load XAML such as <xref:System.Windows.Markup.XamlReader.Load%2A>.</span></span>  
  
 <span data-ttu-id="abc8c-255">Následující příklad je syntaxe s elementem<xref:System.Windows.Media.GradientStopCollection>objektu pro kolekci ( ) zadanou explicitně.</span><span class="sxs-lookup"><span data-stu-id="abc8c-255">The following example is a syntax with the object element for a collection (<xref:System.Windows.Media.GradientStopCollection>) specified explicitly.</span></span>  
  
```xaml  
<LinearGradientBrush>  
  <LinearGradientBrush.GradientStops>  
    <GradientStopCollection>  
      <GradientStop Offset="0.0" Color="Red" />  
      <GradientStop Offset="1.0" Color="Blue" />  
    </GradientStopCollection>  
  </LinearGradientBrush.GradientStops>  
</LinearGradientBrush>  
```  
  
 <span data-ttu-id="abc8c-256">Všimněte si, že není vždy možné explicitně deklarovat kolekce.</span><span class="sxs-lookup"><span data-stu-id="abc8c-256">Note that it is not always possible to explicitly declare the collection.</span></span> <span data-ttu-id="abc8c-257">Například pokus o <xref:System.Windows.TriggerCollection> deklarování <xref:System.Windows.Style.Triggers%2A> explicitně v dříve zobrazeném příkladu by se nezdaří.</span><span class="sxs-lookup"><span data-stu-id="abc8c-257">For instance, attempting to declare <xref:System.Windows.TriggerCollection> explicitly in the previously shown <xref:System.Windows.Style.Triggers%2A> example would fail.</span></span> <span data-ttu-id="abc8c-258">Explicitně deklarování kolekce vyžaduje, aby třída kolekce <xref:System.Windows.TriggerCollection> musí podporovat konstruktor bez parametrů a nemá konstruktor bez parametrů.</span><span class="sxs-lookup"><span data-stu-id="abc8c-258">Explicitly declaring the collection requires that the collection class must support a parameterless constructor, and <xref:System.Windows.TriggerCollection> does not have a parameterless constructor.</span></span>  
  
<a name="xaml_content_properties"></a>
## <a name="xaml-content-properties"></a><span data-ttu-id="abc8c-259">Vlastnosti obsahu XAML</span><span class="sxs-lookup"><span data-stu-id="abc8c-259">XAML Content Properties</span></span>  
 <span data-ttu-id="abc8c-260">Syntaxe obsahu XAML je syntaxe, která <xref:System.Windows.Markup.ContentPropertyAttribute> je povolena pouze u tříd, které určují jako součást deklarace třídy.</span><span class="sxs-lookup"><span data-stu-id="abc8c-260">XAML content syntax is a syntax that is only enabled on classes that specify the <xref:System.Windows.Markup.ContentPropertyAttribute> as part of their class declaration.</span></span> <span data-ttu-id="abc8c-261">Odkazuje <xref:System.Windows.Markup.ContentPropertyAttribute> na název vlastnosti, která je vlastností content pro tento typ prvku (včetně odvozených tříd).</span><span class="sxs-lookup"><span data-stu-id="abc8c-261">The <xref:System.Windows.Markup.ContentPropertyAttribute> references the property name that is the content property for that type of element (including derived classes).</span></span> <span data-ttu-id="abc8c-262">Při zpracování procesorem XAML budou všechny podřízené prvky nebo vnitřní text, které se nacházejí mezi otevírací a uzavírací tagy elementu objektu, přiřazeny hodnotě vlastnosti obsahu XAML pro daný objekt.</span><span class="sxs-lookup"><span data-stu-id="abc8c-262">When processed by a XAML processor, any child elements or inner text that are found between the opening and closing tags of the object element will be assigned to be the value of the XAML content property for that object.</span></span> <span data-ttu-id="abc8c-263">Můžete zadat explicitní prvky vlastnosti pro vlastnost content, ale toto použití není obecně zobrazeno v částech syntaxe XAML v odkazu .NET.</span><span class="sxs-lookup"><span data-stu-id="abc8c-263">You are permitted to specify explicit property elements for the content property, but this usage is not generally shown in the XAML syntax sections in the .NET reference.</span></span> <span data-ttu-id="abc8c-264">Explicitní/podrobné technika má příležitostnou hodnotu pro srozumitelnost značky nebo jako věc stylu značky, ale obvykle záměrem vlastnosti content je zefektivnit značky tak, aby prvky, které jsou intuitivně související jako nadřazené dítě může být vnořeno přímo.</span><span class="sxs-lookup"><span data-stu-id="abc8c-264">The explicit/verbose technique has occasional value for markup clarity or as a matter of markup style, but usually the intent of a content property is to streamline the markup so that elements that are intuitively related as parent-child can be nested directly.</span></span> <span data-ttu-id="abc8c-265">Tagy elementu vlastností pro jiné vlastnosti prvku nejsou přiřazeny jako "obsah" podle striktní definice jazyka XAML; jsou zpracovány dříve v pořadí zpracování analyzátoru XAML a nejsou považovány za "obsah".</span><span class="sxs-lookup"><span data-stu-id="abc8c-265">Property element tags for other properties on an element are not assigned as "content" per a strict XAML language definition; they are processed previously in the XAML parser's processing order and are not considered to be "content".</span></span>  
  
### <a name="xaml-content-property-values-must-be-contiguous"></a><span data-ttu-id="abc8c-266">Hodnoty vlastností obsahu XAML musí být souvislé.</span><span class="sxs-lookup"><span data-stu-id="abc8c-266">XAML Content Property Values Must Be Contiguous</span></span>  
 <span data-ttu-id="abc8c-267">Hodnota vlastnosti obsahu XAML musí být uvedena zcela před nebo zcela za jinými prvky vlastnosti v tomto elementu objektu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-267">The value of a XAML content property must be given either entirely before or entirely after any other property elements on that object element.</span></span> <span data-ttu-id="abc8c-268">To platí bez ohledu na to, zda je hodnota vlastnosti obsahu XAML zadána jako řetězec nebo jako jeden nebo více objektů.</span><span class="sxs-lookup"><span data-stu-id="abc8c-268">This is true whether the value of a XAML content property is specified as a string, or as one or more objects.</span></span> <span data-ttu-id="abc8c-269">Například následující značky neanalyzují:</span><span class="sxs-lookup"><span data-stu-id="abc8c-269">For example, the following markup does not parse:</span></span>  
  
```xaml  
<Button>I am a
  <Button.Background>Blue</Button.Background>  
  blue button</Button>  
```  
  
 <span data-ttu-id="abc8c-270">To je v podstatě neplatné, protože pokud by tato syntaxe byla explicitně provedena pomocí syntaxe elementu vlastnosti pro vlastnost content, pak by vlastnost content byla nastavena dvakrát:</span><span class="sxs-lookup"><span data-stu-id="abc8c-270">This is illegal essentially because if this syntax were made explicit by using property element syntax for the content property, then the content property would be set twice:</span></span>  
  
```xaml  
<Button>  
  <Button.Content>I am a </Button.Content>  
  <Button.Background>Blue</Button.Background>  
  <Button.Content> blue button</Button.Content>  
</Button>  
```  
  
 <span data-ttu-id="abc8c-271">Podobně nelegální příklad je, pokud je vlastnost content kolekce a podřízené prvky jsou proloženy prvky vlastností:</span><span class="sxs-lookup"><span data-stu-id="abc8c-271">A similarly illegal example is if the content property is a collection, and child elements are interspersed with property elements:</span></span>  
  
```xaml  
<StackPanel>  
  <Button>This example</Button>  
  <StackPanel.Resources>  
    <SolidColorBrush x:Key="BlueBrush" Color="Blue"/>  
  </StackPanel.Resources>  
  <Button>... is illegal XAML</Button>  
</StackPanel>  
```  
  
<a name="content_properties_and_collection_syntax_combined"></a>
## <a name="content-properties-and-collection-syntax-combined"></a><span data-ttu-id="abc8c-272">Kombinační vlastnosti obsahu a syntaxe kolekce</span><span class="sxs-lookup"><span data-stu-id="abc8c-272">Content Properties and Collection Syntax Combined</span></span>  
 <span data-ttu-id="abc8c-273">Chcete-li přijmout více než jeden prvek objektu jako obsah, musí být typ vlastnosti content konkrétně typ kolekce.</span><span class="sxs-lookup"><span data-stu-id="abc8c-273">In order to accept more than a single object element as content, the type of the content property must specifically be a collection type.</span></span> <span data-ttu-id="abc8c-274">Podobně jako syntaxe prvku vlastnosti pro typy kolekcí musí procesor XAML identifikovat typy, které jsou typy kolekcí.</span><span class="sxs-lookup"><span data-stu-id="abc8c-274">Similar to property element syntax for collection types, a XAML processor must identify types that are collection types.</span></span> <span data-ttu-id="abc8c-275">Pokud má prvek vlastnost obsahu XAML a typ vlastnosti obsahu XAML je kolekce, nemusí být implicitní typ kolekce zadán ve značkách jako element objektu a vlastnost obsahu XAML nemusí být zadána jako prvek vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="abc8c-275">If an element has a XAML content property and the type of the XAML content property is a collection, then the implied collection type does not need to be specified in the markup as an object element and the XAML content property does not need to be specified as a property element.</span></span> <span data-ttu-id="abc8c-276">Proto zdánlivý obsah modelu ve značkách nyní může mít více než jeden podřízený prvek přiřazen jako obsah.</span><span class="sxs-lookup"><span data-stu-id="abc8c-276">Therefore the apparent content model in the markup can now have more than one child element assigned as the content.</span></span> <span data-ttu-id="abc8c-277">Následuje syntaxe obsahu <xref:System.Windows.Controls.Panel> pro odvozenou třídu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-277">The following is content syntax for a <xref:System.Windows.Controls.Panel> derived class.</span></span> <span data-ttu-id="abc8c-278">Všechny <xref:System.Windows.Controls.Panel> odvozené třídy vytvářejí vlastnost obsahu <xref:System.Windows.Controls.Panel.Children%2A>XAML , <xref:System.Windows.Controls.UIElementCollection>která vyžaduje hodnotu typu .</span><span class="sxs-lookup"><span data-stu-id="abc8c-278">All <xref:System.Windows.Controls.Panel> derived classes establish the XAML content property to be <xref:System.Windows.Controls.Panel.Children%2A>, which requires a value of type <xref:System.Windows.Controls.UIElementCollection>.</span></span>  
  
 [!code-xaml[XAMLOvwSupport#SyntaxContent](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page5.xaml#syntaxcontent)]  
  
 <span data-ttu-id="abc8c-279">Všimněte si, že <xref:System.Windows.Controls.Panel.Children%2A> ani element vlastnosti pro ani prvek pro <xref:System.Windows.Controls.UIElementCollection> je požadováno ve značkách.</span><span class="sxs-lookup"><span data-stu-id="abc8c-279">Note that neither the property element for <xref:System.Windows.Controls.Panel.Children%2A> nor the element for the <xref:System.Windows.Controls.UIElementCollection> is required in the markup.</span></span> <span data-ttu-id="abc8c-280">Toto je funkce návrhu XAML tak, aby rekurzivně obsažené prvky, které definují [!INCLUDE[TLA2#tla_ui](../../../../includes/tla2sharptla-ui-md.md)] jsou intuitivně jistoly jako strom vnořených prvků s okamžitými vztahy elementu nadřazený podřízený, bez zasahování značky elementu vlastnosti nebo objekty kolekce.</span><span class="sxs-lookup"><span data-stu-id="abc8c-280">This is a design feature of XAML so that recursively contained elements that define a [!INCLUDE[TLA2#tla_ui](../../../../includes/tla2sharptla-ui-md.md)] are more intuitively represented as a tree of nested elements with immediate parent-child element relationships, without intervening property element tags or collection objects.</span></span> <span data-ttu-id="abc8c-281">Ve skutečnosti <xref:System.Windows.Controls.UIElementCollection> nelze explicitně zadat ve značkách jako prvek objektu, podle návrhu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-281">In fact, <xref:System.Windows.Controls.UIElementCollection> cannot be specified explicitly in markup as an object element, by design.</span></span> <span data-ttu-id="abc8c-282">Vzhledem k tomu, že jeho <xref:System.Windows.Controls.UIElementCollection> pouze zamýšlené použití je jako implicitní kolekce, nezveřejňuje veřejný konstruktor bez parametrů a proto nelze vytvořit instanci jako element objektu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-282">Because its only intended use is as an implicit collection, <xref:System.Windows.Controls.UIElementCollection> does not expose a public parameterless constructor and thus cannot be instantiated as an object element.</span></span>  
  
### <a name="mixing-property-elements-and-object-elements-in-an-object-with-a-content-property"></a><span data-ttu-id="abc8c-283">Míchání prvků vlastností a prvků objektu v objektu s vlastností obsahu</span><span class="sxs-lookup"><span data-stu-id="abc8c-283">Mixing Property Elements and Object Elements in an Object with a Content Property</span></span>  
 <span data-ttu-id="abc8c-284">Specifikace XAML deklaruje, že procesor XAML může vynutit, že prvky objektu, které se používají k vyplnění vlastnosti obsahu XAML v rámci prvku objektu, musí být souvislé a nesmí být smíšené.</span><span class="sxs-lookup"><span data-stu-id="abc8c-284">The XAML specification declares that a XAML processor can enforce that object elements that are used to fill the XAML content property within an object element must be contiguous, and must not be mixed.</span></span> <span data-ttu-id="abc8c-285">Toto omezení proti míchání prvků vlastností [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] a obsahu je vynuceno procesory XAML.</span><span class="sxs-lookup"><span data-stu-id="abc8c-285">This restriction against mixing property elements and content is enforced by the [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] XAML processors.</span></span>  
  
 <span data-ttu-id="abc8c-286">Můžete mít podřízený prvek objektu jako první okamžité značky v rámci prvku objektu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-286">You can have a child object element as the first immediate markup within an object element.</span></span> <span data-ttu-id="abc8c-287">Pak můžete zavést prvky vlastností.</span><span class="sxs-lookup"><span data-stu-id="abc8c-287">Then you can introduce property elements.</span></span> <span data-ttu-id="abc8c-288">Nebo můžete zadat jeden nebo více prvků vlastností, pak obsah a pak další prvky vlastností.</span><span class="sxs-lookup"><span data-stu-id="abc8c-288">Or, you can specify one or more property elements, then content, then more property elements.</span></span> <span data-ttu-id="abc8c-289">Ale jakmile element vlastnosti následuje obsah, nelze zavést žádný další obsah, můžete pouze přidat prvky vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="abc8c-289">But once a property element follows content, you cannot introduce any further content, you can only add property elements.</span></span>  
  
 <span data-ttu-id="abc8c-290">Tento požadavek na pořadí obsahu / vlastností se nevztahuje na vnitřní text používaný jako obsah.</span><span class="sxs-lookup"><span data-stu-id="abc8c-290">This content / property element order requirement does not apply to inner text used as content.</span></span> <span data-ttu-id="abc8c-291">Je však stále dobrý styl značky zachovat vnitřní text souvislý, protože významné prázdné místo bude obtížné zjistit vizuálně ve značkách, pokud jsou prvky vlastností proložené vnitřním textem.</span><span class="sxs-lookup"><span data-stu-id="abc8c-291">However, it is still a good markup style to keep inner text contiguous, because significant white space will be difficult to detect visually in the markup if property elements are interspersed with inner text.</span></span>  
  
<a name="xaml_namespaces"></a>
## <a name="xaml-namespaces"></a><span data-ttu-id="abc8c-292">Obory názvů jazyka XAML</span><span class="sxs-lookup"><span data-stu-id="abc8c-292">XAML Namespaces</span></span>  
 <span data-ttu-id="abc8c-293">Žádný z předchozích příkladů syntaxe nezadal jiný obor názvů XAML než výchozí obor názvů XAML.</span><span class="sxs-lookup"><span data-stu-id="abc8c-293">None of the preceding syntax examples specified a XAML namespace other than the default XAML namespace.</span></span> <span data-ttu-id="abc8c-294">V [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] typických aplikacích je výchozí obor názvů [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] XAML určen jako obor názvů.</span><span class="sxs-lookup"><span data-stu-id="abc8c-294">In typical [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] applications, the default XAML namespace is specified to be the [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] namespace.</span></span> <span data-ttu-id="abc8c-295">Můžete určit jiné obory názvů XAML než výchozí obor názvů XAML a stále používat podobnou syntaxi.</span><span class="sxs-lookup"><span data-stu-id="abc8c-295">You can specify XAML namespaces other than the default XAML namespace and still use similar syntax.</span></span> <span data-ttu-id="abc8c-296">Ale pak kdekoli, kde je pojmenována třída, která není přístupná v rámci výchozího oboru názvů XAML, tento název třídy musí předcházet předponu oboru názvů XAML jako mapována na odpovídající obor názvů CLR.</span><span class="sxs-lookup"><span data-stu-id="abc8c-296">But then, anywhere where a class is named that is not accessible within the default XAML namespace, that class name must be preceded with the prefix of the XAML namespace as mapped to the corresponding CLR namespace.</span></span> <span data-ttu-id="abc8c-297">Například `<custom:Example/>` je syntaxe prvku objektu k vytvoření `Example` instance třídy, kde byl obor názvů CLR obsahující tuto třídu (a případně `custom` informace o externím sestavení, které obsahuje typy zálohování) dříve mapován na předponu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-297">For example, `<custom:Example/>` is object element syntax to instantiate an instance of the `Example` class, where the CLR namespace containing that class (and possibly the external assembly information that contains backing types) was previously mapped to the `custom` prefix.</span></span>  
  
 <span data-ttu-id="abc8c-298">Další informace o oborech názvů XAML naleznete v [tématu XAML Namespaces and Namespace Mapping for WPF XAML](xaml-namespaces-and-namespace-mapping-for-wpf-xaml.md).</span><span class="sxs-lookup"><span data-stu-id="abc8c-298">For more information about XAML namespaces, see [XAML Namespaces and Namespace Mapping for WPF XAML](xaml-namespaces-and-namespace-mapping-for-wpf-xaml.md).</span></span>  
  
<a name="markup_extensions"></a>
## <a name="markup-extensions"></a><span data-ttu-id="abc8c-299">Rozšíření značek</span><span class="sxs-lookup"><span data-stu-id="abc8c-299">Markup Extensions</span></span>  
 <span data-ttu-id="abc8c-300">XAML definuje programovací entitu rozšíření značek, která umožňuje únik z normálního procesoru XAML zpracování hodnot atributů řetězce nebo prvků objektu a odkládá zpracování na záložní třídu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-300">XAML defines a markup extension programming entity that enables an escape from the normal XAML processor handling of string attribute values or object elements, and defers the processing to a backing class.</span></span> <span data-ttu-id="abc8c-301">Znak, který identifikuje rozšíření značek pro procesor XAML při použití syntaxe atributu, je otevírací složená závorka ({), následovaná libovolným znakem jiným než uzavírací složenou závorkou (}).</span><span class="sxs-lookup"><span data-stu-id="abc8c-301">The character that identifies a markup extension to a XAML processor when using attribute syntax is the opening curly brace ({), followed by any character other than a closing curly brace (}).</span></span> <span data-ttu-id="abc8c-302">První řetězec za počáteční složených závorky musí odkazovat na třídu, která poskytuje konkrétní chování rozšíření, kde odkaz může vynechat podřetězec "Rozšíření", pokud je tento podřetězec součástí názvu třídy true.</span><span class="sxs-lookup"><span data-stu-id="abc8c-302">The first string following the opening curly brace must reference the class that provides the particular extension behavior, where the reference may omit the substring "Extension" if that substring is part of the true class name.</span></span> <span data-ttu-id="abc8c-303">Poté se může zobrazit jedna mezera a potom každý následující znak se používá jako vstup implementace rozšíření, až do uzavření složené závorky je zjištěna.</span><span class="sxs-lookup"><span data-stu-id="abc8c-303">Thereafter, a single space may appear, and then each succeeding character is used as input by the extension implementation, up until the closing curly brace is encountered.</span></span>  
  
 <span data-ttu-id="abc8c-304">Implementace .NET XAML <xref:System.Windows.Markup.MarkupExtension> používá abstraktní třídu jako základ pro všechna [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] rozšíření značek podporovaná stejně jako další architektury nebo technologie.</span><span class="sxs-lookup"><span data-stu-id="abc8c-304">The .NET XAML implementation uses the <xref:System.Windows.Markup.MarkupExtension> abstract class as the basis for all of the markup extensions supported by [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] as well as other frameworks or technologies.</span></span> <span data-ttu-id="abc8c-305">Rozšíření značek, které [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] konkrétně implementuje jsou často určeny k poskytnutí prostředků k odkazování na jiné existující objekty nebo k odložené odkazy na objekty, které budou vyhodnoceny za běhu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-305">The markup extensions that [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] specifically implements are often intended to provide a means to reference other existing objects, or to make deferred references to objects that will be evaluated at run time.</span></span> <span data-ttu-id="abc8c-306">Například jednoduchá datová vazba WPF se `{Binding}` provádí zadáním rozšíření značek namísto hodnoty, která by obvykle trvat určitou vlastnost.</span><span class="sxs-lookup"><span data-stu-id="abc8c-306">For example, a simple WPF data binding is accomplished by specifying the `{Binding}` markup extension in place of the value that a particular property would ordinarily take.</span></span> <span data-ttu-id="abc8c-307">Mnoho rozšíření značek WPF povolit syntaxi atributu pro vlastnosti, kde by jinak nebylo možné syntaxe atributu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-307">Many of the WPF markup extensions enable an attribute syntax for properties where an attribute syntax would not otherwise be possible.</span></span> <span data-ttu-id="abc8c-308"><xref:System.Windows.Style> Například objekt je poměrně složitý typ, který obsahuje vnořenou řadu objektů a vlastností.</span><span class="sxs-lookup"><span data-stu-id="abc8c-308">For example, a <xref:System.Windows.Style> object is a relatively complex type that contains a nested series of objects and properties.</span></span> <span data-ttu-id="abc8c-309">Styly v WPF jsou obvykle definovány jako prostředek v <xref:System.Windows.ResourceDictionary>aplikaci a poté odkazovány prostřednictvím jednoho ze dvou rozšíření značek WPF, která požadují prostředek.</span><span class="sxs-lookup"><span data-stu-id="abc8c-309">Styles in WPF are typically defined as a resource in a <xref:System.Windows.ResourceDictionary>, and then referenced through one of the two WPF markup extensions that request a resource.</span></span> <span data-ttu-id="abc8c-310">Rozšíření značek odloží vyhodnocení hodnoty vlastnosti na vyhledávání prostředků a povolí <xref:System.Windows.FrameworkElement.Style%2A> zadání hodnoty <xref:System.Windows.Style>vlastnosti s typem , v syntaxi atributu jako v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="abc8c-310">The markup extension defers the evaluation of the property value to a resource lookup and enables providing the value of the <xref:System.Windows.FrameworkElement.Style%2A> property, taking type <xref:System.Windows.Style>, in attribute syntax as in the following example:</span></span>  
  
 `<Button Style="{StaticResource MyStyle}">My button</Button>`  
  
 <span data-ttu-id="abc8c-311">Zde `StaticResource` identifikuje třídu <xref:System.Windows.StaticResourceExtension> poskytující implementaci rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="abc8c-311">Here, `StaticResource` identifies the <xref:System.Windows.StaticResourceExtension> class providing the markup extension implementation.</span></span> <span data-ttu-id="abc8c-312">Další řetězec `MyStyle` se používá jako vstup pro <xref:System.Windows.StaticResourceExtension> nevýchozí konstruktor, kde parametr převzatý z <xref:System.Windows.ResourceKey>rozšiřujícího řetězce deklaruje požadovaný .</span><span class="sxs-lookup"><span data-stu-id="abc8c-312">The next string `MyStyle` is used as the input for the non-default <xref:System.Windows.StaticResourceExtension> constructor, where the parameter as taken from the extension string declares the requested <xref:System.Windows.ResourceKey>.</span></span> <span data-ttu-id="abc8c-313">`MyStyle`očekává se, že [hodnota x:Key](../../../desktop-wpf/xaml-services/xkey-directive.md) <xref:System.Windows.Style> definovaného jako prostředek.</span><span class="sxs-lookup"><span data-stu-id="abc8c-313">`MyStyle` is expected to be the [x:Key](../../../desktop-wpf/xaml-services/xkey-directive.md) value of a <xref:System.Windows.Style> defined as a resource.</span></span> <span data-ttu-id="abc8c-314">Požadavky na využití [rozšíření značky staticresource](staticresource-markup-extension.md) ustavený prostředek, které mají být použity k poskytnutí hodnoty <xref:System.Windows.Style> vlastnosti prostřednictvím logiky vyhledávání statických prostředků v době načítání.</span><span class="sxs-lookup"><span data-stu-id="abc8c-314">The [StaticResource Markup Extension](staticresource-markup-extension.md) usage requests that the resource be used to provide the <xref:System.Windows.Style> property value through static resource lookup logic at load time.</span></span>  
  
 <span data-ttu-id="abc8c-315">Další informace o rozšířeních značek naleznete v [tématech Rozšíření značek a WPF XAML](markup-extensions-and-wpf-xaml.md).</span><span class="sxs-lookup"><span data-stu-id="abc8c-315">For more information about markup extensions, see [Markup Extensions and WPF XAML](markup-extensions-and-wpf-xaml.md).</span></span> <span data-ttu-id="abc8c-316">Odkaz na rozšíření značek a další programovací funkce XAML povolené v obecné implementaci rozhraní .NET XAML naleznete v [tématu XAML Namespace (x:) Funkce jazyka](../../../desktop-wpf/xaml-services/namespace-language-features.md).</span><span class="sxs-lookup"><span data-stu-id="abc8c-316">For a reference of markup extensions and other XAML programming features enabled in the general .NET XAML implementation, see [XAML Namespace (x:) Language Features](../../../desktop-wpf/xaml-services/namespace-language-features.md).</span></span> <span data-ttu-id="abc8c-317">Rozšíření značek specifická pro WPF naleznete v tématu [WPF XAML Extensions](wpf-xaml-extensions.md).</span><span class="sxs-lookup"><span data-stu-id="abc8c-317">For WPF-specific markup extensions, see [WPF XAML Extensions](wpf-xaml-extensions.md).</span></span>  
  
<a name="attached_properties"></a>
## <a name="attached-properties"></a><span data-ttu-id="abc8c-318">Přidružené vlastnosti</span><span class="sxs-lookup"><span data-stu-id="abc8c-318">Attached Properties</span></span>  
 <span data-ttu-id="abc8c-319">Připojené vlastnosti jsou programovací koncept zavedený v XAML, kdy vlastnosti mohou být vlastněny a definovány určitým typem, ale nastaveny jako atributy nebo prvky vlastností na libovolném prvku.</span><span class="sxs-lookup"><span data-stu-id="abc8c-319">Attached properties are a programming concept introduced in XAML whereby properties can be owned and defined by a particular type, but set as attributes or property elements on any element.</span></span> <span data-ttu-id="abc8c-320">Primární scénář, který připojené vlastnosti jsou určeny pro je povolit podřízené prvky ve struktuře značek sestavy informace nadřazený prvek bez nutnosti rozsáhle sdílený objektový model napříč všemi prvky.</span><span class="sxs-lookup"><span data-stu-id="abc8c-320">The primary scenario that attached properties are intended for is to enable child elements in a markup structure to report information to a parent element without requiring an extensively shared object model across all elements.</span></span> <span data-ttu-id="abc8c-321">Naopak připojené vlastnosti lze použít nadřazené prvky k hlášení informací podřízeným prvkům.</span><span class="sxs-lookup"><span data-stu-id="abc8c-321">Conversely, attached properties can be used by parent elements to report information to child elements.</span></span> <span data-ttu-id="abc8c-322">Další informace o účelu připojených vlastností a o tom, jak vytvořit vlastní připojené vlastnosti, naleznete v tématu [Přehled připojených vlastností](attached-properties-overview.md).</span><span class="sxs-lookup"><span data-stu-id="abc8c-322">For more information on the purpose of attached properties and how to create your own attached properties, see [Attached Properties Overview](attached-properties-overview.md).</span></span>  
  
 <span data-ttu-id="abc8c-323">Připojené vlastnosti používají syntaxi, která se povrchně podobá syntaxi prvku vlastnosti, v tom, že také zadáte *typeName*. *propertyName* kombinace.</span><span class="sxs-lookup"><span data-stu-id="abc8c-323">Attached properties use a syntax that superficially resembles property element syntax, in that you also specify a *typeName*.*propertyName* combination.</span></span> <span data-ttu-id="abc8c-324">Existují dva důležité rozdíly:</span><span class="sxs-lookup"><span data-stu-id="abc8c-324">There are two important differences:</span></span>  
  
- <span data-ttu-id="abc8c-325">Můžete použít *typeName*. *propertyName* kombinace i při nastavení připojené vlastnosti pomocí syntaxe atributu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-325">You can use the *typeName*.*propertyName* combination even when setting an attached property through attribute syntax.</span></span> <span data-ttu-id="abc8c-326">Připojené vlastnosti jsou jediným případem, kdy je kvalifikace názvu vlastnosti požadavkem v syntaxi atributu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-326">Attached properties are the only case where qualifying the property name is a requirement in an attribute syntax.</span></span>  
  
- <span data-ttu-id="abc8c-327">Syntaxe prvku vlastnosti můžete také použít pro připojené vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="abc8c-327">You can also use property element syntax for attached properties.</span></span> <span data-ttu-id="abc8c-328">Pro syntaxi typického prvku vlastnosti je však zadaný *majedlo typeName* elementem objektu, který obsahuje element vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="abc8c-328">However, for typical property element syntax, the *typeName* you specify is the object element that contains the property element.</span></span> <span data-ttu-id="abc8c-329">Pokud odkazujete na připojenou vlastnost, pak *typeName* je třída, která definuje připojenou vlastnost, nikoli prvek obsahující objekt.</span><span class="sxs-lookup"><span data-stu-id="abc8c-329">If you are referring to an attached property, then the *typeName* is the class that defines the attached property, not the containing object element.</span></span>  
  
<a name="attached_events"></a>
## <a name="attached-events"></a><span data-ttu-id="abc8c-330">Přidružené události</span><span class="sxs-lookup"><span data-stu-id="abc8c-330">Attached Events</span></span>  
 <span data-ttu-id="abc8c-331">Připojené události jsou další programovací koncept zavedený v XAML, kde události mohou být definovány určitým typem, ale obslužné rutiny mohou být připojeny k libovolnému prvku objektu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-331">Attached events are another programming concept introduced in XAML where events can be defined by a specific type, but handlers may be attached on any object element.</span></span> <span data-ttu-id="abc8c-332">V implementaci WOF často typ, který definuje připojené události je statický typ, který definuje službu a někdy tyto připojené události jsou vystaveny alias směrované události v typech, které zveřejňují službu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-332">In the WOF implementation, often the type that defines an attached event is a static type that defines a service, and sometimes those attached events are exposed by a routed event alias in types that expose the service.</span></span> <span data-ttu-id="abc8c-333">Obslužné rutiny pro připojené události jsou určeny pomocí syntaxe atributu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-333">Handlers for attached events are specified through attribute syntax.</span></span> <span data-ttu-id="abc8c-334">Stejně jako u připojených událostí je syntaxe atributu rozšířena pro připojené události tak, aby umožňovala *typeName*. *eventName* usage, where *typeName* je `Add` `Remove` třída, která poskytuje a přistupující přístupové členy obslužné rutiny události pro infrastrukturu připojených událostí a *eventName* je název události.</span><span class="sxs-lookup"><span data-stu-id="abc8c-334">As with attached events, the attribute syntax is expanded for attached events to allow a *typeName*.*eventName* usage, where *typeName* is the class that provides `Add` and `Remove` event handler accessors for the attached event infrastructure, and *eventName* is the event name.</span></span>  
  
<a name="anatomy_of_a_xaml_page_root_element"></a>
## <a name="anatomy-of-a-xaml-root-element"></a><span data-ttu-id="abc8c-335">Anatomie kořenového prvku XAML</span><span class="sxs-lookup"><span data-stu-id="abc8c-335">Anatomy of a XAML Root Element</span></span>  
 <span data-ttu-id="abc8c-336">V následující tabulce je uveden typický kořenový prvek XAML, který je rozdělen a který zobrazuje specifické atributy kořenového prvku:</span><span class="sxs-lookup"><span data-stu-id="abc8c-336">The following table shows a typical XAML root element broken down, showing the specific attributes of a root element:</span></span>  
  
|||  
|-|-|  
|`<Page`|<span data-ttu-id="abc8c-337">Otevření elementu objektu kořenového prvku</span><span class="sxs-lookup"><span data-stu-id="abc8c-337">Opening object element of the root element</span></span>|  
|`xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"`|<span data-ttu-id="abc8c-338">Výchozí ([!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)]) obor názvů XAML</span><span class="sxs-lookup"><span data-stu-id="abc8c-338">The default ([!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)]) XAML namespace</span></span>|  
|`xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"`|<span data-ttu-id="abc8c-339">Obor názvů XAML jazyka XAML</span><span class="sxs-lookup"><span data-stu-id="abc8c-339">The XAML language XAML namespace</span></span>|  
|`x:Class="ExampleNamespace.ExampleCode"`|<span data-ttu-id="abc8c-340">Deklarace částečné třídy, která spojuje značky s libovolným kódem na pozadí definovaným pro částečnou třídu</span><span class="sxs-lookup"><span data-stu-id="abc8c-340">The partial class declaration that connects markup to any code-behind defined for the partial class</span></span>|  
|`>`|<span data-ttu-id="abc8c-341">Konec prvku objektu pro kořen.</span><span class="sxs-lookup"><span data-stu-id="abc8c-341">End of object element for the root.</span></span> <span data-ttu-id="abc8c-342">Objekt ještě není uzavřen, protože prvek obsahuje podřízené prvky.</span><span class="sxs-lookup"><span data-stu-id="abc8c-342">Object is not closed yet because the element contains child elements</span></span>|  
  
<a name="optional_and_nonrecommended_xaml_usages"></a>
## <a name="optional-and-nonrecommended-xaml-usages"></a><span data-ttu-id="abc8c-343">Volitelné a nedoporučené použití XAML</span><span class="sxs-lookup"><span data-stu-id="abc8c-343">Optional and Nonrecommended XAML Usages</span></span>  
 <span data-ttu-id="abc8c-344">Následující části popisují použití XAML, které jsou technicky podporovány procesory XAML, ale které vytvářejí podrobnostnebo jiné estetické problémy, které narušují soubory XAML, které zůstávají čitelné pro člověka při vývoji aplikací, které obsahují zdroje XAML.</span><span class="sxs-lookup"><span data-stu-id="abc8c-344">The following sections describe XAML usages that are technically supported by XAML processors, but that produce verbosity or other aesthetic issues that interfere with XAML files remaining human-readable when you develop applications that contain XAML sources.</span></span>  
  
### <a name="optional-property-element-usages"></a><span data-ttu-id="abc8c-345">Použití volitelných prvků vlastností</span><span class="sxs-lookup"><span data-stu-id="abc8c-345">Optional Property Element Usages</span></span>  
 <span data-ttu-id="abc8c-346">Volitelné použití prvků vlastnosti zahrnují explicitní zápis vlastností obsahu prvku, které procesor XAML považuje za implicitní.</span><span class="sxs-lookup"><span data-stu-id="abc8c-346">Optional property element usages include explicitly writing out element content properties that the XAML processor considers implicit.</span></span> <span data-ttu-id="abc8c-347">Například při deklarování <xref:System.Windows.Controls.Menu>obsahu , můžete explicitně <xref:System.Windows.Controls.ItemsControl.Items%2A> deklarovat kolekci <xref:System.Windows.Controls.Menu> jako `<Menu.Items>` vlastnost <xref:System.Windows.Controls.MenuItem> `<Menu.Items>`element tag a umístit každý v rámci , spíše <xref:System.Windows.Controls.Menu> než <xref:System.Windows.Controls.MenuItem> pomocí implicitní <xref:System.Windows.Controls.ItemsControl.Items%2A> chování procesoru XAML, že všechny podřízené prvky musí být a jsou umístěny v kolekci.</span><span class="sxs-lookup"><span data-stu-id="abc8c-347">For example, when you declare the contents of a <xref:System.Windows.Controls.Menu>, you could choose to explicitly declare the <xref:System.Windows.Controls.ItemsControl.Items%2A> collection of the <xref:System.Windows.Controls.Menu> as a `<Menu.Items>` property element tag, and place each <xref:System.Windows.Controls.MenuItem> within `<Menu.Items>`, rather than using the implicit XAML processor behavior that all child elements of a <xref:System.Windows.Controls.Menu> must be a <xref:System.Windows.Controls.MenuItem> and are placed in the <xref:System.Windows.Controls.ItemsControl.Items%2A> collection.</span></span> <span data-ttu-id="abc8c-348">Někdy volitelné použití může pomoci vizuálně objasnit strukturu objektu, jak je znázorněno ve značkách.</span><span class="sxs-lookup"><span data-stu-id="abc8c-348">Sometimes the optional usages can help to visually clarify the object structure as represented in the markup.</span></span> <span data-ttu-id="abc8c-349">Nebo někdy explicitní vlastnost prvek použití můžete vyhnout značky, která je technicky funkční, ale vizuálně matoucí, jako je například vnořené značkovací rozšíření v rámci hodnoty atributu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-349">Or sometimes an explicit property element usage can avoid markup that is technically functional but visually confusing, such as nested markup extensions within an attribute value.</span></span>  
  
### <a name="full-typenamemembername-qualified-attributes"></a><span data-ttu-id="abc8c-350">Úplné atributy typeName.memberName Qualified</span><span class="sxs-lookup"><span data-stu-id="abc8c-350">Full typeName.memberName Qualified Attributes</span></span>  
 <span data-ttu-id="abc8c-351">Název *typu*. *MemberName* formulář pro atribut ve skutečnosti funguje více univerzálně než jen směrované události případu.</span><span class="sxs-lookup"><span data-stu-id="abc8c-351">The *typeName*.*memberName* form for an attribute actually works more universally than just the routed event case.</span></span> <span data-ttu-id="abc8c-352">Ale v jiných situacích, které tvoří, je nadbytečné a měli byste se tomu vyhnout, i když jen z důvodů stylu značek a čitelnosti.</span><span class="sxs-lookup"><span data-stu-id="abc8c-352">But in other situations that form is superfluous and you should avoid it, if only for reasons of markup style and readability.</span></span> <span data-ttu-id="abc8c-353">V následujícím příkladu jsou všechny tři <xref:System.Windows.Controls.Control.Background%2A> odkazy na atribut zcela rovnocenné:</span><span class="sxs-lookup"><span data-stu-id="abc8c-353">In the following example, each of the three references to the <xref:System.Windows.Controls.Control.Background%2A> attribute are completely equivalent:</span></span>  
  
 [!code-xaml[XAMLOvwSupport#TypeNameProp](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page8.xaml#typenameprop)]  
  
 <span data-ttu-id="abc8c-354">`Button.Background`funguje, protože kvalifikované vyhledávání pro <xref:System.Windows.Controls.Button> tuto<xref:System.Windows.Controls.Control.Background%2A> vlastnost na je úspěšný <xref:System.Windows.Controls.Button> (byl zděděn z Control) a je třída elementu objektu nebo základní třídy.</span><span class="sxs-lookup"><span data-stu-id="abc8c-354">`Button.Background` works because the qualified lookup for that property on <xref:System.Windows.Controls.Button> is successful (<xref:System.Windows.Controls.Control.Background%2A> was inherited from Control) and <xref:System.Windows.Controls.Button> is the class of the object element or a base class.</span></span> <span data-ttu-id="abc8c-355">`Control.Background`<xref:System.Windows.Controls.Control> funguje, protože třída <xref:System.Windows.Controls.Control.Background%2A> ve <xref:System.Windows.Controls.Control> skutečnosti <xref:System.Windows.Controls.Button> definuje a je základní třídy.</span><span class="sxs-lookup"><span data-stu-id="abc8c-355">`Control.Background` works because the <xref:System.Windows.Controls.Control> class actually defines <xref:System.Windows.Controls.Control.Background%2A> and <xref:System.Windows.Controls.Control> is a <xref:System.Windows.Controls.Button> base class.</span></span>  
  
 <span data-ttu-id="abc8c-356">Následující *typeName*. *memberName* form example nefunguje, a je tedy zobrazen komentoval:</span><span class="sxs-lookup"><span data-stu-id="abc8c-356">However, the following *typeName*.*memberName* form example does not work and is thus shown commented:</span></span>  
  
 [!code-xaml[XAMLOvwSupport#TypeNameBadProp](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page8.xaml#typenamebadprop)]  
  
 <span data-ttu-id="abc8c-357"><xref:System.Windows.Controls.Label>je další odvozené <xref:System.Windows.Controls.Control>třídy , `Label.Background` a <xref:System.Windows.Controls.Label> pokud jste zadali v rámci elementu objektu, toto použití by fungovalo.</span><span class="sxs-lookup"><span data-stu-id="abc8c-357"><xref:System.Windows.Controls.Label> is another derived class of <xref:System.Windows.Controls.Control>, and if you had specified `Label.Background` within a <xref:System.Windows.Controls.Label> object element, this usage would have worked.</span></span> <span data-ttu-id="abc8c-358">Však <xref:System.Windows.Controls.Label> protože není třída nebo <xref:System.Windows.Controls.Button>základní třída , zadané chování procesoru `Label.Background` XAML je pak zpracovat jako připojené vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="abc8c-358">However, because <xref:System.Windows.Controls.Label> is not the class or base class of <xref:System.Windows.Controls.Button>, the specified XAML processor behavior is to then process `Label.Background` as an attached property.</span></span> <span data-ttu-id="abc8c-359">`Label.Background`není k dispozici připojené vlastnosti a toto použití se nezdaří.</span><span class="sxs-lookup"><span data-stu-id="abc8c-359">`Label.Background` is not an available attached property, and this usage fails.</span></span>  
  
### <a name="basetypenamemembername-property-elements"></a><span data-ttu-id="abc8c-360">elementy vlastností baseTypeName.memberName</span><span class="sxs-lookup"><span data-stu-id="abc8c-360">baseTypeName.memberName Property Elements</span></span>  
 <span data-ttu-id="abc8c-361">Analogickým způsobem, jak *typeName*. *MemberName* formulář funguje pro syntaxi atributu, *baseTypeName*. syntaxe *memberName* funguje pro syntaxi prvku vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="abc8c-361">In an analogous way to how the *typeName*.*memberName* form works for attribute syntax, a *baseTypeName*.*memberName* syntax works for property element syntax.</span></span> <span data-ttu-id="abc8c-362">Například následující syntaxe funguje:</span><span class="sxs-lookup"><span data-stu-id="abc8c-362">For instance, the following syntax works:</span></span>  
  
 [!code-xaml[XAMLOvwSupport#GoofyPE](~/samples/snippets/csharp/VS_Snippets_Wpf/XAMLOvwSupport/CSharp/page8.xaml#goofype)]  
  
 <span data-ttu-id="abc8c-363">Zde byl prvek vlastnosti `Control.Background` uveden jako přesto, že `Button`prvek vlastnosti byl obsažen v .</span><span class="sxs-lookup"><span data-stu-id="abc8c-363">Here, the property element was given as `Control.Background` even though the property element was contained in `Button`.</span></span>  
  
 <span data-ttu-id="abc8c-364">Ale stejně jako *typeName*. *memberName* formulář pro atributy, *baseTypeName*. *memberName* je špatný styl ve značkách a měli byste se mu vyhnout.</span><span class="sxs-lookup"><span data-stu-id="abc8c-364">But just like *typeName*.*memberName* form for attributes, *baseTypeName*.*memberName* is poor style in markup, and you should avoid it.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="abc8c-365">Viz také</span><span class="sxs-lookup"><span data-stu-id="abc8c-365">See also</span></span>

- [<span data-ttu-id="abc8c-366">Přehled XAML (WPF)</span><span class="sxs-lookup"><span data-stu-id="abc8c-366">XAML Overview (WPF)</span></span>](../../../desktop-wpf/fundamentals/xaml.md)
- [<span data-ttu-id="abc8c-367">Jazykové funkce kompatibility značek (mc:)</span><span class="sxs-lookup"><span data-stu-id="abc8c-367">XAML Namespace (x:) Language Features</span></span>](../../../desktop-wpf/xaml-services/namespace-language-features.md)
- [<span data-ttu-id="abc8c-368">Rozšíření WPF XAML</span><span class="sxs-lookup"><span data-stu-id="abc8c-368">WPF XAML Extensions</span></span>](wpf-xaml-extensions.md)
- [<span data-ttu-id="abc8c-369">Přehled vlastností závislosti</span><span class="sxs-lookup"><span data-stu-id="abc8c-369">Dependency Properties Overview</span></span>](dependency-properties-overview.md)
- [<span data-ttu-id="abc8c-370">TypeConverters a XAML</span><span class="sxs-lookup"><span data-stu-id="abc8c-370">TypeConverters and XAML</span></span>](typeconverters-and-xaml.md)
- [<span data-ttu-id="abc8c-371">XAML a vlastní třídy pro WPF</span><span class="sxs-lookup"><span data-stu-id="abc8c-371">XAML and Custom Classes for WPF</span></span>](xaml-and-custom-classes-for-wpf.md)
