---
title: Přehled připojených událostí
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- handling attached events [WPF]
- defining attached events as routed events [WPF]
- attached events [WPF], scenarios for
- attached events vs. routed events [WPF]
- backing attached events with routed events [WPF]
- attached events [WPF], definition
ms.assetid: 2c40eae3-80e4-4a45-ae09-df6c9ab4d91e
ms.openlocfilehash: 40e7bd34388bec06cd8a7c3610599d814aef101f
ms.sourcegitcommit: cf9515122fce716bcfb6618ba366e39b5a2eb81e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/15/2019
ms.locfileid: "69038158"
---
# <a name="attached-events-overview"></a><span data-ttu-id="1518b-102">Přehled připojených událostí</span><span class="sxs-lookup"><span data-stu-id="1518b-102">Attached Events Overview</span></span>

<span data-ttu-id="1518b-103">Jazyk Extensible Application Markup Language (XAML) (XAML) definuje jazykovou komponentu a typ události, která se nazývá *připojená událost*.</span><span class="sxs-lookup"><span data-stu-id="1518b-103">Extensible Application Markup Language (XAML) defines a language component and type of event called an *attached event*.</span></span> <span data-ttu-id="1518b-104">Koncept připojené události umožňuje přidat obslužnou rutinu pro konkrétní událost libovolnému prvku, nikoli prvku, který skutečně definuje nebo dědí událost.</span><span class="sxs-lookup"><span data-stu-id="1518b-104">The concept of an attached event enables you to add a handler for a particular event to an arbitrary element rather than to an element that actually defines or inherits the event.</span></span> <span data-ttu-id="1518b-105">V takovém případě objekt potenciálně nevyvolal událost, ani instance zpracování cíle nedefinuje nebo jinak vlastní událost.</span><span class="sxs-lookup"><span data-stu-id="1518b-105">In this case, neither the object potentially raising the event nor the destination handling instance defines or otherwise "owns" the event.</span></span>  

<a name="prerequisites"></a>   
## <a name="prerequisites"></a><span data-ttu-id="1518b-106">Požadavky</span><span class="sxs-lookup"><span data-stu-id="1518b-106">Prerequisites</span></span>  
 <span data-ttu-id="1518b-107">V tomto tématu se předpokládá, že jste si přečetli [Přehled směrované události](routed-events-overview.md) a [Přehled XAML (WPF)](xaml-overview-wpf.md).</span><span class="sxs-lookup"><span data-stu-id="1518b-107">This topic assumes that you have read [Routed Events Overview](routed-events-overview.md) and [XAML Overview (WPF)](xaml-overview-wpf.md).</span></span>  
  
<a name="Syntax"></a>   
## <a name="attached-event-syntax"></a><span data-ttu-id="1518b-108">Syntaxe připojené události</span><span class="sxs-lookup"><span data-stu-id="1518b-108">Attached Event Syntax</span></span>  
 <span data-ttu-id="1518b-109">Připojené události mají syntaxi jazyka XAML a vzor kódu, který musí být použit v případě, že se používá záložní kód, aby bylo možné podporovat použití připojené události.</span><span class="sxs-lookup"><span data-stu-id="1518b-109">Attached events have a XAML syntax and a coding pattern that must be used by the backing code in order to support the attached event usage.</span></span>  
  
 <span data-ttu-id="1518b-110">V syntaxi jazyka XAML je připojená událost určena nejen názvem události, ale podle jejího vlastnícího typu a názvu události oddělené tečkou (.).</span><span class="sxs-lookup"><span data-stu-id="1518b-110">In XAML syntax, the attached event is specified not just by its event name, but by its owning type plus the event name, separated by a dot (.).</span></span> <span data-ttu-id="1518b-111">Vzhledem k tomu, že název události je kvalifikován s názvem jeho vlastnícího typu, připojená syntaxe události umožňuje připojit jakoukoli připojenou událost k jakémukoli prvku, který lze vytvořit z instance.</span><span class="sxs-lookup"><span data-stu-id="1518b-111">Because the event name is qualified with the name of its owning type, the attached event syntax allows any attached event to be attached to any element that can be instantiated.</span></span>  
  
 <span data-ttu-id="1518b-112">Například následuje syntaxe jazyka XAML pro připojení obslužné rutiny vlastní `NeedsCleaning` připojené události:</span><span class="sxs-lookup"><span data-stu-id="1518b-112">For example, the following is the XAML syntax for attaching a handler for a custom `NeedsCleaning` attached event:</span></span>  
  
 [!code-xaml[WPFAquariumSln#AE](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFAquariumSln/CSharp/WPFAquarium/Window1.xaml#ae)]  
  
 <span data-ttu-id="1518b-113">Všimněte si `aqua:` předpony. v takovém případě je nutná předpona, protože připojená událost je vlastní událost, která přichází z vlastního mapovaného xmlns.</span><span class="sxs-lookup"><span data-stu-id="1518b-113">Note the `aqua:` prefix; the prefix is necessary in this case because the attached event is a custom event that comes from a custom mapped xmlns.</span></span>  
  
<a name="WPFImplements"></a>   
## <a name="how-wpf-implements-attached-events"></a><span data-ttu-id="1518b-114">Jak WPF implementuje připojené události</span><span class="sxs-lookup"><span data-stu-id="1518b-114">How WPF Implements Attached Events</span></span>

<span data-ttu-id="1518b-115">V rámci WPF jsou připojené události zálohovány <xref:System.Windows.RoutedEvent> polem a jsou směrovány prostřednictvím stromu po jejich vyvolání.</span><span class="sxs-lookup"><span data-stu-id="1518b-115">In WPF, attached events are backed by a <xref:System.Windows.RoutedEvent> field and are routed through the tree after they are raised.</span></span> <span data-ttu-id="1518b-116">Zdroj připojené události (objekt, který vyvolává událost) obvykle představuje systém nebo zdroj služby a objekt, který spouští kód, který vyvolává událost, není proto přímo součástí stromu elementu.</span><span class="sxs-lookup"><span data-stu-id="1518b-116">Typically, the source of the attached event (the object that raises the event) is a system or service source, and the object that runs the code that raises the event is therefore not a direct part of the element tree.</span></span>  
  
<a name="Scenarios"></a>   
## <a name="scenarios-for-attached-events"></a><span data-ttu-id="1518b-117">Scénáře pro připojené události</span><span class="sxs-lookup"><span data-stu-id="1518b-117">Scenarios for Attached Events</span></span>  
 <span data-ttu-id="1518b-118">V rámci WPF jsou připojené události přítomny v některých oblastech funkce, kde je abstrakce na úrovni služby, například pro události, které jsou povoleny statickou <xref:System.Windows.Input.Mouse> třídou <xref:System.Windows.Controls.Validation> nebo třídou.</span><span class="sxs-lookup"><span data-stu-id="1518b-118">In WPF, attached events are present in certain feature areas where there is service-level abstraction, such as for the events enabled by the static <xref:System.Windows.Input.Mouse> class or the <xref:System.Windows.Controls.Validation> class.</span></span> <span data-ttu-id="1518b-119">Třídy, které pracují se službou nebo používají službu, mohou buď používat událost v připojené syntaxi události, nebo mohou zvolit, že připojená událost bude připojena jako směrované událost, která je součástí způsobu, jakým třída integruje schopnosti služby.</span><span class="sxs-lookup"><span data-stu-id="1518b-119">Classes that interact with or use the service can either use the event in the attached event syntax, or they can choose to surface the attached event as a routed event that is part of how the class integrates the capabilities of the service.</span></span>  
  
 <span data-ttu-id="1518b-120">I když WPF definuje množství připojených událostí, ve scénářích, kdy budete buď používat nebo zpracovávat připojenou událost přímo, velmi omezené.</span><span class="sxs-lookup"><span data-stu-id="1518b-120">Although WPF defines a number of attached events, the scenarios where you will either use or handle the attached event directly are very limited.</span></span> <span data-ttu-id="1518b-121">Obecně připojená událost slouží k účelu architektury, ale je poté předána na nepřipojenou (se zálohovaná s událostí CLR "obálk"), která je směrována.</span><span class="sxs-lookup"><span data-stu-id="1518b-121">Generally, the attached event serves an architecture purpose, but is then forwarded to a non-attached (backed with a CLR event "wrapper") routed event.</span></span>  
  
 <span data-ttu-id="1518b-122">Například <xref:System.Windows.Input.Mouse.MouseDown?displayProperty=nameWithType> základní připojená událost může být snáze zpracována na <xref:System.Windows.UIElement> základě toho, že používá <xref:System.Windows.UIElement.MouseDown> , <xref:System.Windows.UIElement> a nikoli s připojenou syntaxí události v jazyce XAML nebo kódu.</span><span class="sxs-lookup"><span data-stu-id="1518b-122">For instance, the underlying attached event <xref:System.Windows.Input.Mouse.MouseDown?displayProperty=nameWithType> can more easily be handled on any given <xref:System.Windows.UIElement> by using <xref:System.Windows.UIElement.MouseDown> on that <xref:System.Windows.UIElement> rather than dealing with attached event syntax either in XAML or code.</span></span> <span data-ttu-id="1518b-123">Připojená událost slouží k tomu účelu v architektuře, protože umožňuje budoucí rozšíření vstupních zařízení.</span><span class="sxs-lookup"><span data-stu-id="1518b-123">The attached event serves a purpose in the architecture because it allows for future expansion of input devices.</span></span> <span data-ttu-id="1518b-124">Hypotetické zařízení by se muselo vyvolávat <xref:System.Windows.Input.Mouse.MouseDown?displayProperty=nameWithType> jenom pro simulaci vstupu myši a nemusí se odvozovat od <xref:System.Windows.Input.Mouse> tohoto důvodu.</span><span class="sxs-lookup"><span data-stu-id="1518b-124">The hypothetical device would only need to raise <xref:System.Windows.Input.Mouse.MouseDown?displayProperty=nameWithType> in order to simulate mouse input, and would not need to derive from <xref:System.Windows.Input.Mouse> to do so.</span></span> <span data-ttu-id="1518b-125">Nicméně tento scénář zahrnuje zpracování kódu událostí a zpracování XAML připojené události není relevantní pro tento scénář.</span><span class="sxs-lookup"><span data-stu-id="1518b-125">However, this scenario involves code handling of the events, and XAML handling of the attached event is not relevant to this scenario.</span></span>  
  
<a name="Handling"></a>   
## <a name="handling-an-attached-event-in-wpf"></a><span data-ttu-id="1518b-126">Zpracování připojené události v subsystému WPF</span><span class="sxs-lookup"><span data-stu-id="1518b-126">Handling an Attached Event in WPF</span></span>  
 <span data-ttu-id="1518b-127">Proces pro zpracování připojené události a kód obslužné rutiny, který budete zapisovat, je v podstatě stejný jako u směrované události.</span><span class="sxs-lookup"><span data-stu-id="1518b-127">The process for handling an attached event, and the handler code that you will write, is basically the same as for a routed event.</span></span>  
  
 <span data-ttu-id="1518b-128">Obecně je připojená událost WPF nerozdílná od události směrovaného WPF.</span><span class="sxs-lookup"><span data-stu-id="1518b-128">In general, a WPF attached event is not very different from a WPF routed event.</span></span> <span data-ttu-id="1518b-129">Rozdíly představují způsob, jakým je událost zdrojem, a způsob jejich zpřístupnění třídou jako člen (což má vliv na syntaxi obslužné rutiny XAML).</span><span class="sxs-lookup"><span data-stu-id="1518b-129">The differences are how the event is sourced and how it is exposed by a class as a member (which also affects the XAML handler syntax).</span></span>  
  
 <span data-ttu-id="1518b-130">Jak již bylo uvedeno dříve, existující události připojené k WPF nejsou zejména určeny pro zpracování v WPF.</span><span class="sxs-lookup"><span data-stu-id="1518b-130">However, as noted earlier, the existing WPF attached events are not particularly intended for handling in WPF.</span></span> <span data-ttu-id="1518b-131">Častým účelem události je povolit složený element, aby nahlásil stav nadřazenému prvku v skládání. v takovém případě je událost obvykle vyvolána v kódu a také spoléhá na zpracování tříd v příslušné nadřazené třídě.</span><span class="sxs-lookup"><span data-stu-id="1518b-131">More often, the purpose of the event is to enable a composited element to report a state to a parent element in compositing, in which case the event is usually raised in code and also relies on class handling in the relevant parent class.</span></span> <span data-ttu-id="1518b-132">Například položky <xref:System.Windows.Controls.Primitives.Selector> v rámci mají vyvolat připojenou <xref:System.Windows.Controls.Primitives.Selector.Selected> událost, která je <xref:System.Windows.Controls.Primitives.Selector> pak třídou zpracována třídou a následně může být převedena <xref:System.Windows.Controls.Primitives.Selector> třídou na jinou směrovanou událost, <xref:System.Windows.Controls.Primitives.Selector.SelectionChanged> .</span><span class="sxs-lookup"><span data-stu-id="1518b-132">For instance, items within a <xref:System.Windows.Controls.Primitives.Selector> are expected to raise the attached <xref:System.Windows.Controls.Primitives.Selector.Selected> event, which is then class handled by the <xref:System.Windows.Controls.Primitives.Selector> class and then potentially converted by the <xref:System.Windows.Controls.Primitives.Selector> class into a different routed event, <xref:System.Windows.Controls.Primitives.Selector.SelectionChanged>.</span></span> <span data-ttu-id="1518b-133">Další informace o směrovaných událostech a zpracování tříd naleznete v tématu [Označení směrovaných událostí jako zpracovaných a zpracování tříd](marking-routed-events-as-handled-and-class-handling.md).</span><span class="sxs-lookup"><span data-stu-id="1518b-133">For more information on routed events and class handling, see [Marking Routed Events as Handled, and Class Handling](marking-routed-events-as-handled-and-class-handling.md).</span></span>  
  
<a name="Custom"></a>   
## <a name="defining-your-own-attached-events-as-routed-events"></a><span data-ttu-id="1518b-134">Definování vlastních připojených událostí jako směrovaných událostí</span><span class="sxs-lookup"><span data-stu-id="1518b-134">Defining Your Own Attached Events as Routed Events</span></span>  
 <span data-ttu-id="1518b-135">Pokud odvozete ze běžných základních tříd WPF, můžete implementovat vlastní připojené události, včetně určitých metod vzoru ve třídě a pomocí pomocných metod, které jsou již přítomny v základních třídách.</span><span class="sxs-lookup"><span data-stu-id="1518b-135">If you are deriving from common WPF base classes, you can implement your own attached events by including certain pattern methods in your class and by using utility methods that are already present on the base classes.</span></span>  
  
 <span data-ttu-id="1518b-136">Vzor je následující:</span><span class="sxs-lookup"><span data-stu-id="1518b-136">The pattern is as follows:</span></span>  
  
- <span data-ttu-id="1518b-137">Metoda __přidávají obslužnou rutinu*EventName*__  se dvěma parametry.</span><span class="sxs-lookup"><span data-stu-id="1518b-137">A method __Add*EventName*Handler__ with two parameters.</span></span> <span data-ttu-id="1518b-138">První parametr je instance, ke které je přidána obslužná rutina události.</span><span class="sxs-lookup"><span data-stu-id="1518b-138">The first parameter is the instance to which the event handler is added.</span></span> <span data-ttu-id="1518b-139">Druhý parametr je obslužná rutina události, která se má přidat.</span><span class="sxs-lookup"><span data-stu-id="1518b-139">The second parameter is the event handler to add.</span></span> <span data-ttu-id="1518b-140">Metoda musí být `public` a `static`, bez návratové hodnoty.</span><span class="sxs-lookup"><span data-stu-id="1518b-140">The method must be `public` and `static`, with no return value.</span></span>  
  
- <span data-ttu-id="1518b-141">Metoda __odebrání obslužné rutiny*EventName*__ se dvěma parametry.</span><span class="sxs-lookup"><span data-stu-id="1518b-141">A method __Remove*EventName*Handler__ with two parameters.</span></span> <span data-ttu-id="1518b-142">První parametr je instance, ze které je obslužná rutina události odebrána.</span><span class="sxs-lookup"><span data-stu-id="1518b-142">The first parameter is the instance from which the event handler is removed.</span></span> <span data-ttu-id="1518b-143">Druhý parametr je obslužná rutina události, která se má odebrat.</span><span class="sxs-lookup"><span data-stu-id="1518b-143">The second parameter is the event handler to remove.</span></span> <span data-ttu-id="1518b-144">Metoda musí být `public` a `static`, bez návratové hodnoty.</span><span class="sxs-lookup"><span data-stu-id="1518b-144">The method must be `public` and `static`, with no return value.</span></span>  
  
 <span data-ttu-id="1518b-145">Metoda __Přidat přístupový objekt obslužné rutiny*EventName*__ usnadňuje zpracování XAML, když jsou připojené atributy obslužné rutiny události deklarovány u elementu.</span><span class="sxs-lookup"><span data-stu-id="1518b-145">The __Add*EventName*Handler__ accessor method facilitates XAML processing when attached event handler attributes are declared on an element.</span></span> <span data-ttu-id="1518b-146">Metoda __Přidání obslužné__ rutiny *EventName* a __odebrat obslužné rutiny*EventName*__  také umožňuje přístup kódu k úložišti obslužných rutin událostí pro připojenou událost.</span><span class="sxs-lookup"><span data-stu-id="1518b-146">The __Add*EventName*Handler__ and __Remove*EventName*Handler__ methods also enable code access to the event handler store for the attached event.</span></span>  
  
 <span data-ttu-id="1518b-147">Tento obecný vzor není zatím dostatečně přesný pro praktické implementace v rámci architektury, protože jakákoli konkrétní implementace čtečky XAML může mít různá schémata pro identifikaci základních událostí v podpůrném jazyce a architektuře.</span><span class="sxs-lookup"><span data-stu-id="1518b-147">This general pattern is not yet precise enough for practical implementation in a framework, because any given XAML reader implementation might have different schemes for identifying underlying events in the supporting language and architecture.</span></span> <span data-ttu-id="1518b-148">Toto je jeden z důvodů, proč WPF implementuje připojené události jako směrované události. identifikátor, který má být použit pro událost<xref:System.Windows.RoutedEvent>(), je již definován systémem událostí WPF.</span><span class="sxs-lookup"><span data-stu-id="1518b-148">This is one of the reasons that WPF implements attached events as routed events; the identifier to use for an event (<xref:System.Windows.RoutedEvent>) is already defined by the WPF event system.</span></span> <span data-ttu-id="1518b-149">Směrování události je také rozšířením přirozené implementace v konceptu na úrovni jazyka XAML připojené události.</span><span class="sxs-lookup"><span data-stu-id="1518b-149">Also, routing an event is a natural implementation extension on the XAML language-level concept of an attached event.</span></span>  
  
 <span data-ttu-id="1518b-150">Implementace __obslužné rutiny*EventName*__ pro událost připojenou k WPF se skládá <xref:System.Windows.UIElement.AddHandler%2A> z volání metody s směrovanou událostmi a obslužnou rutinou jako argumenty.</span><span class="sxs-lookup"><span data-stu-id="1518b-150">The __Add*EventName*Handler__ implementation for a WPF attached event consists of calling the <xref:System.Windows.UIElement.AddHandler%2A> with the routed event and handler as arguments.</span></span>  
  
 <span data-ttu-id="1518b-151">Tato strategie implementace a systém směrovaného systému událostí obecně omezují manipulaci s připojenými událostmi <xref:System.Windows.UIElement> buď na odvozené <xref:System.Windows.ContentElement> třídy, nebo na odvozené třídy, protože <xref:System.Windows.UIElement.AddHandler%2A> pouze tyto třídy mají implementace.</span><span class="sxs-lookup"><span data-stu-id="1518b-151">This implementation strategy and the routed event system in general restrict handling for attached events to either <xref:System.Windows.UIElement> derived classes or <xref:System.Windows.ContentElement> derived classes, because only those classes have <xref:System.Windows.UIElement.AddHandler%2A> implementations.</span></span>  
  
 <span data-ttu-id="1518b-152">Například následující kód definuje `NeedsCleaning` připojenou událost třídy `Aquarium`Owner pomocí strategie události připojené k WPF, která deklaruje připojenou událost jako směrovanou událost.</span><span class="sxs-lookup"><span data-stu-id="1518b-152">For example, the following code defines the `NeedsCleaning` attached event on the owner class `Aquarium`, using the WPF attached event strategy of declaring the attached event as a routed event.</span></span>  
  
 [!code-csharp[WPFAquariumSln#AECode](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFAquariumSln/CSharp/WPFAquariumObjects/Class1.cs#aecode)]
 [!code-vb[WPFAquariumSln#AECode](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFAquariumSln/visualbasic/wpfaquariumobjects/class1.vb#aecode)]  
  
 <span data-ttu-id="1518b-153">Všimněte si, že metoda použitá pro vytvoření pole <xref:System.Windows.EventManager.RegisterRoutedEvent%2A>identifikátoru připojené události je vlastně stejnou metodou, která se používá k registraci nepřipojené směrované události.</span><span class="sxs-lookup"><span data-stu-id="1518b-153">Note that the method used to establish the attached event identifier field, <xref:System.Windows.EventManager.RegisterRoutedEvent%2A>, is actually the same method that is used to register a non-attached routed event.</span></span> <span data-ttu-id="1518b-154">Připojené události a směrované události jsou zaregistrované do centralizovaného interního úložiště.</span><span class="sxs-lookup"><span data-stu-id="1518b-154">Attached events and routed events all are registered to a centralized internal store.</span></span> <span data-ttu-id="1518b-155">Tato implementace úložiště událostí umožňuje koncepční porozumění "událostem jako rozhraní", které je popsáno v tématu [Přehled směrovaných událostí](routed-events-overview.md).</span><span class="sxs-lookup"><span data-stu-id="1518b-155">This event store implementation enables the "events as an interface" conceptual consideration that is discussed in [Routed Events Overview](routed-events-overview.md).</span></span>  
  
<a name="Raising"></a>   
## <a name="raising-a-wpf-attached-event"></a><span data-ttu-id="1518b-156">Vyvolává se událost připojená k WPF.</span><span class="sxs-lookup"><span data-stu-id="1518b-156">Raising a WPF Attached Event</span></span>  
 <span data-ttu-id="1518b-157">Obvykle není nutné vyvolat existující připojené události definované WPF z kódu.</span><span class="sxs-lookup"><span data-stu-id="1518b-157">You do not typically need to raise existing WPF-defined attached events from your code.</span></span> <span data-ttu-id="1518b-158">Tyto události následují jako Obecné koncepční model "služba" a třídy <xref:System.Windows.Input.InputManager> služeb, jako jsou zodpovědné za vyvolání událostí.</span><span class="sxs-lookup"><span data-stu-id="1518b-158">These events follow the general "service" conceptual model, and service classes such as <xref:System.Windows.Input.InputManager> are responsible for raising the events.</span></span>  
  
 <span data-ttu-id="1518b-159">Nicméně pokud definujete vlastní připojenou událost na základě modelu WPF, na kterém jsou připojené události na <xref:System.Windows.RoutedEvent>, můžete použít <xref:System.Windows.UIElement.RaiseEvent%2A> k vyvolání připojené události z libovolného <xref:System.Windows.UIElement> nebo <xref:System.Windows.ContentElement>.</span><span class="sxs-lookup"><span data-stu-id="1518b-159">However, if you are defining a custom attached event based on the WPF model of basing attached events on <xref:System.Windows.RoutedEvent>, you can use <xref:System.Windows.UIElement.RaiseEvent%2A> to raise an attached event from any <xref:System.Windows.UIElement> or <xref:System.Windows.ContentElement>.</span></span> <span data-ttu-id="1518b-160">Vyvolání směrované události (připojená nebo ne) vyžaduje deklaraci konkrétního prvku ve stromu elementu jako zdroj události; Tento zdroj je hlášen jako <xref:System.Windows.UIElement.RaiseEvent%2A> volající.</span><span class="sxs-lookup"><span data-stu-id="1518b-160">Raising a routed event (attached or not) requires that you declare a particular element in the element tree as the event source; that source is reported as the <xref:System.Windows.UIElement.RaiseEvent%2A> caller.</span></span> <span data-ttu-id="1518b-161">Určení prvku, který je hlášen jako zdroj ve stromu, je zodpovědností vaší služby</span><span class="sxs-lookup"><span data-stu-id="1518b-161">Determining which element is reported as the source in the tree is your service's responsibility</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="1518b-162">Viz také:</span><span class="sxs-lookup"><span data-stu-id="1518b-162">See also</span></span>

- [<span data-ttu-id="1518b-163">Přehled směrovaných událostí</span><span class="sxs-lookup"><span data-stu-id="1518b-163">Routed Events Overview</span></span>](routed-events-overview.md)
- [<span data-ttu-id="1518b-164">Podrobná syntaxe XAML</span><span class="sxs-lookup"><span data-stu-id="1518b-164">XAML Syntax In Detail</span></span>](xaml-syntax-in-detail.md)
- [<span data-ttu-id="1518b-165">XAML a vlastní třídy pro WPF</span><span class="sxs-lookup"><span data-stu-id="1518b-165">XAML and Custom Classes for WPF</span></span>](xaml-and-custom-classes-for-wpf.md)
