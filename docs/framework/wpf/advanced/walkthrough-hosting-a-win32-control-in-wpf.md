---
title: Hostování ovládacího prvku Win32 v subsystému WPF
titleSuffix: ''
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- hosting Win32 control in WPF [WPF]
- Win32 code [WPF], WPF interoperation
ms.assetid: a676b1eb-fc55-4355-93ab-df840c41cea0
ms.openlocfilehash: eb497a88c119dece85d61d6a32e7b86fb03b44b5
ms.sourcegitcommit: de17a7a0a37042f0d4406f5ae5393531caeb25ba
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 01/24/2020
ms.locfileid: "76744929"
---
# <a name="walkthrough-host-a-win32-control-in-wpf"></a><span data-ttu-id="b2aca-102">Návod: hostování ovládacího prvku Win32 v subsystému WPF</span><span class="sxs-lookup"><span data-stu-id="b2aca-102">Walkthrough: Host a Win32 Control in WPF</span></span>
<span data-ttu-id="b2aca-103">Windows Presentation Foundation (WPF) poskytuje bohatou prostředí pro vytváření aplikací.</span><span class="sxs-lookup"><span data-stu-id="b2aca-103">Windows Presentation Foundation (WPF) provides a rich environment for creating applications.</span></span> <span data-ttu-id="b2aca-104">Nicméně pokud máte významnou investici do kódu Win32, může být efektivnější znovu použít alespoň část kódu v aplikaci WPF, nikoli úplně přepsat.</span><span class="sxs-lookup"><span data-stu-id="b2aca-104">However, when you have a substantial investment in Win32 code, it may be more effective to reuse at least some of that code in your WPF application rather than rewrite it completely.</span></span> <span data-ttu-id="b2aca-105">WPF poskytuje jednoduchý mechanismus pro hostování okna Win32 na stránce WPF.</span><span class="sxs-lookup"><span data-stu-id="b2aca-105">WPF provides a straightforward mechanism for hosting a Win32 window, on a WPF page.</span></span>  
  
 <span data-ttu-id="b2aca-106">Toto téma vás provede aplikací a [hostitelem ovládacího prvku seznamu Win32 v UKÁZCE WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control), který je hostitelem ovládacího prvku seznam Win32.</span><span class="sxs-lookup"><span data-stu-id="b2aca-106">This topic walks you through an application, [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control), that hosts a Win32 list box control.</span></span> <span data-ttu-id="b2aca-107">Tento obecný postup lze rozšířit na hostování jakéhokoli okna systému Win32.</span><span class="sxs-lookup"><span data-stu-id="b2aca-107">This general procedure can be extended to hosting any Win32 window.</span></span>  

<a name="requirements"></a>   
## <a name="requirements"></a><span data-ttu-id="b2aca-108">Požadavky</span><span class="sxs-lookup"><span data-stu-id="b2aca-108">Requirements</span></span>  
 <span data-ttu-id="b2aca-109">V tomto tématu se předpokládá základní znalost pro programování WPF i Windows API.</span><span class="sxs-lookup"><span data-stu-id="b2aca-109">This topic assumes a basic familiarity with both WPF and Windows API programming.</span></span> <span data-ttu-id="b2aca-110">Základní informace o programování WPF naleznete v tématu [Začínáme](../getting-started/index.md).</span><span class="sxs-lookup"><span data-stu-id="b2aca-110">For a basic introduction to WPF programming, see [Getting Started](../getting-started/index.md).</span></span> <span data-ttu-id="b2aca-111">Úvod do programování rozhraní API systému Windows najdete v libovolné řadě knih v předmětu, zejména v *oknech programování* podle Charles Petzold.</span><span class="sxs-lookup"><span data-stu-id="b2aca-111">For an introduction to Windows API programming, see any of the numerous books on the subject, in particular *Programming Windows* by Charles Petzold.</span></span>  
  
 <span data-ttu-id="b2aca-112">Vzhledem k tomu, že je ukázka, která provází toto C#téma, implementována v systému, používá pro přístup k rozhraní Windows API služby vyvolání platformy (PInvoke).</span><span class="sxs-lookup"><span data-stu-id="b2aca-112">Because the sample that accompanies this topic is implemented in C#, it makes use of Platform Invocation Services (PInvoke) to access the Windows API.</span></span> <span data-ttu-id="b2aca-113">Některé zkušenosti s PInvoke jsou užitečné, ale nejsou nezbytné.</span><span class="sxs-lookup"><span data-stu-id="b2aca-113">Some familiarity with PInvoke is helpful but not essential.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="b2aca-114">Toto téma obsahuje několik příkladů kódu z přidruženého vzorku.</span><span class="sxs-lookup"><span data-stu-id="b2aca-114">This topic includes a number of code examples from the associated sample.</span></span> <span data-ttu-id="b2aca-115">Pro čitelnost ale nezahrnuje kompletní vzorový kód.</span><span class="sxs-lookup"><span data-stu-id="b2aca-115">However, for readability, it does not include the complete sample code.</span></span> <span data-ttu-id="b2aca-116">Můžete získat nebo zobrazit úplný kód z [hostování ovládacího prvku seznamu Win32 v UKÁZCE WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control).</span><span class="sxs-lookup"><span data-stu-id="b2aca-116">You can obtain or view complete code from [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control).</span></span>  
  
<a name="basic_procedure"></a>   
## <a name="the-basic-procedure"></a><span data-ttu-id="b2aca-117">Základní postup</span><span class="sxs-lookup"><span data-stu-id="b2aca-117">The Basic Procedure</span></span>  
 <span data-ttu-id="b2aca-118">Tato část popisuje základní postup pro hostování okna Win32 na stránce WPF.</span><span class="sxs-lookup"><span data-stu-id="b2aca-118">This section outlines the basic procedure for hosting a Win32 window on a WPF page.</span></span> <span data-ttu-id="b2aca-119">Zbývající části si Projděte podrobnosti o jednotlivých krocích.</span><span class="sxs-lookup"><span data-stu-id="b2aca-119">The remaining sections go through the details of each step.</span></span>  
  
 <span data-ttu-id="b2aca-120">Základní hostující procedura je:</span><span class="sxs-lookup"><span data-stu-id="b2aca-120">The basic hosting procedure is:</span></span>  
  
1. <span data-ttu-id="b2aca-121">Implementujte stránku WPF pro hostování okna.</span><span class="sxs-lookup"><span data-stu-id="b2aca-121">Implement a WPF page to host the window.</span></span> <span data-ttu-id="b2aca-122">Jednou z postupů je vytvoření prvku <xref:System.Windows.Controls.Border>, který vyhradí část stránky pro hostované okno.</span><span class="sxs-lookup"><span data-stu-id="b2aca-122">One technique is to create a <xref:System.Windows.Controls.Border> element to reserve a section of the page for the hosted window.</span></span>  
  
2. <span data-ttu-id="b2aca-123">Implementujte třídu pro hostování ovládacího prvku, který dědí z <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="b2aca-123">Implement a class to host the control that inherits from <xref:System.Windows.Interop.HwndHost>.</span></span>  
  
3. <span data-ttu-id="b2aca-124">V této třídě přepište <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>člena třídy <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="b2aca-124">In that class, override the <xref:System.Windows.Interop.HwndHost> class member <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>.</span></span>  
  
4. <span data-ttu-id="b2aca-125">Vytvoření hostovaného okna jako podřízeného okna, které obsahuje stránku WPF.</span><span class="sxs-lookup"><span data-stu-id="b2aca-125">Create the hosted window as a child of the window that contains the WPF page.</span></span> <span data-ttu-id="b2aca-126">I když konvenční programování WPF nemusí explicitně používat, hostující stránka je okno s popisovačem (HWND).</span><span class="sxs-lookup"><span data-stu-id="b2aca-126">Although conventional WPF programming does not need to explicitly make use of it, the hosting page is a window with a handle (HWND).</span></span> <span data-ttu-id="b2aca-127">Zobrazí se HWND stránky pomocí parametru `hwndParent` metody <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>.</span><span class="sxs-lookup"><span data-stu-id="b2aca-127">You receive the page HWND through the `hwndParent` parameter of the <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A> method.</span></span> <span data-ttu-id="b2aca-128">Hostované okno by se mělo vytvořit jako podřízený prvek tohoto HWND.</span><span class="sxs-lookup"><span data-stu-id="b2aca-128">The hosted window should be created as a child of this HWND.</span></span>  
  
5. <span data-ttu-id="b2aca-129">Po vytvoření hostitelského okna vraťte HWND hostovaného okna.</span><span class="sxs-lookup"><span data-stu-id="b2aca-129">Once you have created the host window, return the HWND of the hosted window.</span></span> <span data-ttu-id="b2aca-130">Chcete-li hostovat jeden nebo více ovládacích prvků Win32, obvykle vytvoříte hostitelské okno jako podřízený prvek HWND a nastavíte podřízené prvky tohoto hostitelského okna.</span><span class="sxs-lookup"><span data-stu-id="b2aca-130">If you want to host one or more Win32 controls, you typically create a host window as a child of the HWND and make the controls children of that host window.</span></span> <span data-ttu-id="b2aca-131">Zabalení ovládacích prvků v hostitelském okně poskytuje jednoduchý způsob, jak může vaše stránka WPF přijímat oznámení z ovládacích prvků, což se zabývá konkrétními problémy Win32 s oznámeními v rámci hranice HWND.</span><span class="sxs-lookup"><span data-stu-id="b2aca-131">Wrapping the controls in a host window provides a simple way for your WPF page to receive notifications from the controls, which deals with some particular Win32 issues with notifications across the HWND boundary.</span></span>  
  
6. <span data-ttu-id="b2aca-132">Zpracuje vybrané zprávy odeslané do hostitelského okna, například oznámení z podřízených ovládacích prvků.</span><span class="sxs-lookup"><span data-stu-id="b2aca-132">Handle selected messages sent to the host window, such as notifications from child controls.</span></span> <span data-ttu-id="b2aca-133">To můžete provést dvěma způsoby.</span><span class="sxs-lookup"><span data-stu-id="b2aca-133">There are two ways to do this.</span></span>  
  
    - <span data-ttu-id="b2aca-134">Pokud dáváte přednost zpracování zpráv ve vaší hostující třídě, přepište metodu <xref:System.Windows.Interop.HwndHost.WndProc%2A> třídy <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="b2aca-134">If you prefer to handle messages in your hosting class, override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> method of the <xref:System.Windows.Interop.HwndHost> class.</span></span>  
  
    - <span data-ttu-id="b2aca-135">Pokud dáváte přednost tomu, aby WPF zpracoval zprávy, zpracujte v kódu na pozadí událost <xref:System.Windows.Interop.HwndHost> třídy <xref:System.Windows.Interop.HwndHost.MessageHook>.</span><span class="sxs-lookup"><span data-stu-id="b2aca-135">If you prefer to have the WPF handle the messages, handle the <xref:System.Windows.Interop.HwndHost> class <xref:System.Windows.Interop.HwndHost.MessageHook> event in your code-behind.</span></span> <span data-ttu-id="b2aca-136">K této události dochází u každé zprávy, která je přijata hostovaným oknem.</span><span class="sxs-lookup"><span data-stu-id="b2aca-136">This event occurs for every message that is received by the hosted window.</span></span> <span data-ttu-id="b2aca-137">Pokud zvolíte tuto možnost, musíte přesto přepsat <xref:System.Windows.Interop.HwndHost.WndProc%2A>, ale budete potřebovat jenom minimální implementaci.</span><span class="sxs-lookup"><span data-stu-id="b2aca-137">If you choose this option, you must still override <xref:System.Windows.Interop.HwndHost.WndProc%2A>, but you only need a minimal implementation.</span></span>  
  
7. <span data-ttu-id="b2aca-138">Přepsat <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> a <xref:System.Windows.Interop.HwndHost.WndProc%2A> metody <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="b2aca-138">Override the <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> and <xref:System.Windows.Interop.HwndHost.WndProc%2A> methods of <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="b2aca-139">Tyto metody je nutné přepsat, aby splňovaly <xref:System.Windows.Interop.HwndHost> kontraktu, ale je možné, že budete muset pouze zadat minimální implementaci.</span><span class="sxs-lookup"><span data-stu-id="b2aca-139">You must override these methods to satisfy the <xref:System.Windows.Interop.HwndHost> contract, but you may only need to provide a minimal implementation.</span></span>  
  
8. <span data-ttu-id="b2aca-140">V souboru kódu na pozadí vytvořte instanci třídy hostování ovládacího prvku a nastavte ji jako podřízenou prvku <xref:System.Windows.Controls.Border>, který je určen pro hostování okna.</span><span class="sxs-lookup"><span data-stu-id="b2aca-140">In your code-behind file, create an instance of the control hosting class and make it a child of the <xref:System.Windows.Controls.Border> element that is intended to host the window.</span></span>  
  
9. <span data-ttu-id="b2aca-141">Komunikujte s hostovaným oknem odesláním zpráv Microsoft Windows a zpracováním zpráv ze svých podřízených oken, jako jsou oznámení odesílaná ovládacími prvky.</span><span class="sxs-lookup"><span data-stu-id="b2aca-141">Communicate with the hosted window by sending it Microsoft Windows messages and handling messages from its child windows, such as notifications sent by controls.</span></span>  
  
<a name="page_layout"></a>   
## <a name="implement-the-page-layout"></a><span data-ttu-id="b2aca-142">Implementace rozložení stránky</span><span class="sxs-lookup"><span data-stu-id="b2aca-142">Implement the Page Layout</span></span>  
 <span data-ttu-id="b2aca-143">Rozložení stránky WPF, která je hostitelem ovládacího prvku ListBox, se skládá ze dvou oblastí.</span><span class="sxs-lookup"><span data-stu-id="b2aca-143">The layout for the WPF page that hosts the ListBox Control consists of two regions.</span></span> <span data-ttu-id="b2aca-144">Levá strana stránky je hostitelem několika ovládacích prvků WPF, které poskytují [!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)], které umožňují manipulaci s ovládacím prvkem Win32.</span><span class="sxs-lookup"><span data-stu-id="b2aca-144">The left side of the page hosts several WPF controls that provide a [!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)] that allows you to manipulate the Win32 control.</span></span> <span data-ttu-id="b2aca-145">V pravém horním rohu stránky je čtvercová oblast pro hostovaný ovládací prvek seznamu.</span><span class="sxs-lookup"><span data-stu-id="b2aca-145">The upper right corner of the page has a square region for the hosted ListBox Control.</span></span>  
  
 <span data-ttu-id="b2aca-146">Kód pro implementaci tohoto rozložení je poměrně jednoduchý.</span><span class="sxs-lookup"><span data-stu-id="b2aca-146">The code to implement this layout is quite simple.</span></span> <span data-ttu-id="b2aca-147">Kořenový prvek je <xref:System.Windows.Controls.DockPanel>, který má dva podřízené elementy.</span><span class="sxs-lookup"><span data-stu-id="b2aca-147">The root element is a <xref:System.Windows.Controls.DockPanel> that has two child elements.</span></span> <span data-ttu-id="b2aca-148">První je <xref:System.Windows.Controls.Border> prvek, který je hostitelem ovládacího prvku ListBox.</span><span class="sxs-lookup"><span data-stu-id="b2aca-148">The first is a <xref:System.Windows.Controls.Border> element that hosts the ListBox Control.</span></span> <span data-ttu-id="b2aca-149">Zabírá v pravém horním rohu stránky 200x200 čtverec.</span><span class="sxs-lookup"><span data-stu-id="b2aca-149">It occupies a 200x200 square in the upper right corner of the page.</span></span> <span data-ttu-id="b2aca-150">Druhým je <xref:System.Windows.Controls.StackPanel> prvek, který obsahuje sadu ovládacích prvků WPF, které zobrazují informace a umožňují manipulaci s ovládacím prvkem seznamu nastavením vlastností zpřístupněných operací.</span><span class="sxs-lookup"><span data-stu-id="b2aca-150">The second is a <xref:System.Windows.Controls.StackPanel> element that contains a set of WPF controls that display information and allow you to manipulate the ListBox Control by setting exposed interoperation properties.</span></span> <span data-ttu-id="b2aca-151">Pro každý element, který je podřízených prvků <xref:System.Windows.Controls.StackPanel>, přečtěte si referenční materiál pro různé prvky, které se používají k podrobnostem o tom, jaké jsou tyto prvky nebo co dělají, jsou uvedeny v níže uvedeném příkladu kódu, ale nebudete je vysvětlit (základní model vzájemné operace nevyžaduje žádný z nich, je k dispozici pro přidání některé interaktivity k ukázce).</span><span class="sxs-lookup"><span data-stu-id="b2aca-151">For each of the elements that are children of the <xref:System.Windows.Controls.StackPanel>, see the reference material for the various elements used for details on what these elements are or what they do, these are listed in the example code below but will not be explained here (the basic interoperation model does not require any of them, they are provided to add some interactivity to the sample).</span></span>  
  
 [!code-xaml[WPFHostingWin32Control#WPFUI](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml#wpfui)]  
  
<a name="host_class"></a>   
## <a name="implement-a-class-to-host-the-microsoft-win32-control"></a><span data-ttu-id="b2aca-152">Implementace třídy pro hostování ovládacího prvku Microsoft Win32</span><span class="sxs-lookup"><span data-stu-id="b2aca-152">Implement a Class to Host the Microsoft Win32 Control</span></span>  
 <span data-ttu-id="b2aca-153">Jádrem této ukázky je třída, která je ve skutečnosti hostitelem ovládacího prvku, ControlHost.cs.</span><span class="sxs-lookup"><span data-stu-id="b2aca-153">The core of this sample is the class that actually hosts the control, ControlHost.cs.</span></span> <span data-ttu-id="b2aca-154">Dědí z <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="b2aca-154">It inherits from <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="b2aca-155">Konstruktor přijímá dva parametry, výšku a šířku, které odpovídají výšce a šířce <xref:System.Windows.Controls.Border> prvku, který je hostitelem ovládacího prvku ListBox.</span><span class="sxs-lookup"><span data-stu-id="b2aca-155">The constructor takes two parameters, height and width, which correspond to the height and width of the <xref:System.Windows.Controls.Border> element that hosts the ListBox control.</span></span> <span data-ttu-id="b2aca-156">Tyto hodnoty se používají později, aby se zajistilo, že velikost ovládacího prvku odpovídá prvku <xref:System.Windows.Controls.Border>.</span><span class="sxs-lookup"><span data-stu-id="b2aca-156">These values are used later to ensure that the size of the control matches the <xref:System.Windows.Controls.Border> element.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostclass)]
 [!code-vb[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostclass)]  
  
 <span data-ttu-id="b2aca-157">K dispozici je také sada konstant.</span><span class="sxs-lookup"><span data-stu-id="b2aca-157">There is also a set of constants.</span></span> <span data-ttu-id="b2aca-158">Tyto konstanty jsou z velké části z Winuser. h a umožňují při volání funkcí Win32 použít konvenční názvy.</span><span class="sxs-lookup"><span data-stu-id="b2aca-158">These constants are largely taken from Winuser.h, and allow you to use conventional names when calling Win32 functions.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostconstants)]
 [!code-vb[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostconstants)]  
  
<a name="buildwindowcore"></a>   
### <a name="override-buildwindowcore-to-create-the-microsoft-win32-window"></a><span data-ttu-id="b2aca-159">Přepsat BuildWindowCore a vytvořit okno Microsoft Win32</span><span class="sxs-lookup"><span data-stu-id="b2aca-159">Override BuildWindowCore to Create the Microsoft Win32 Window</span></span>  
 <span data-ttu-id="b2aca-160">Tuto metodu přepíšete, chcete-li vytvořit okno Win32, které bude hostováno stránkou, a vytvořit propojení mezi oknem a stránkou.</span><span class="sxs-lookup"><span data-stu-id="b2aca-160">You override this method to create the Win32 window that will be hosted by the page, and make the connection between the window and the page.</span></span> <span data-ttu-id="b2aca-161">Vzhledem k tomu, že tato ukázka zahrnuje hostování ovládacího prvku seznamu, vytvoří se dvě okna.</span><span class="sxs-lookup"><span data-stu-id="b2aca-161">Because this sample involves hosting a ListBox Control, two windows are created.</span></span> <span data-ttu-id="b2aca-162">První je okno, které je skutečně hostováno stránkou WPF.</span><span class="sxs-lookup"><span data-stu-id="b2aca-162">The first is the window that is actually hosted by the WPF page.</span></span> <span data-ttu-id="b2aca-163">Ovládací prvek ListBox je vytvořen jako podřízený objekt tohoto okna.</span><span class="sxs-lookup"><span data-stu-id="b2aca-163">The ListBox Control is created as a child of that window.</span></span>  
  
 <span data-ttu-id="b2aca-164">Důvodem tohoto přístupu je zjednodušení procesu přijímání oznámení z ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="b2aca-164">The reason for this approach is to simplify the process of receiving notifications from the control.</span></span> <span data-ttu-id="b2aca-165">Třída <xref:System.Windows.Interop.HwndHost> umožňuje zpracovat zprávy odesílané do okna, které je hostitelem.</span><span class="sxs-lookup"><span data-stu-id="b2aca-165">The <xref:System.Windows.Interop.HwndHost> class allows you to process messages sent to the window that it is hosting.</span></span> <span data-ttu-id="b2aca-166">Pokud je přímo hostitelem ovládacího prvku Win32, obdržíte zprávy odeslané do interní smyčky zpráv ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="b2aca-166">If you host a Win32 control directly, you receive the messages sent to the internal message loop of the control.</span></span> <span data-ttu-id="b2aca-167">Můžete zobrazit ovládací prvek a odeslat zprávy, ale neobdržíte oznámení, která ovládací prvek odešle do svého nadřazeného okna.</span><span class="sxs-lookup"><span data-stu-id="b2aca-167">You can display the control and send it messages, but you do not receive the notifications that the control sends to its parent window.</span></span> <span data-ttu-id="b2aca-168">To mimo jiné znamená, že nemáte žádný způsob, jak zjistit, kdy uživatel komunikuje s ovládacím prvkem.</span><span class="sxs-lookup"><span data-stu-id="b2aca-168">This means, among other things, that you have no way of detecting when the user interacts with the control.</span></span> <span data-ttu-id="b2aca-169">Místo toho vytvořte okno hostitele a nastavte ho jako podřízenou položku tohoto okna.</span><span class="sxs-lookup"><span data-stu-id="b2aca-169">Instead, create a host window and make the control a child of that window.</span></span> <span data-ttu-id="b2aca-170">To vám umožní zpracovat zprávy pro hostitelské okno, včetně oznámení, která mu ovládací prvek odesílají.</span><span class="sxs-lookup"><span data-stu-id="b2aca-170">This allows you to process the messages for the host window including the notifications sent to it by the control.</span></span> <span data-ttu-id="b2aca-171">Pro usnadnění, protože hostitelské okno je o něco větší než jednoduchá obálka pro ovládací prvek, bude balíček označován jako ovládací prvek seznamu.</span><span class="sxs-lookup"><span data-stu-id="b2aca-171">For convenience, since the host window is little more than a simple wrapper for the control, the package will be referred to as a ListBox control.</span></span>  
  
<a name="create_the_window_and_listbox"></a>   
#### <a name="create-the-host-window-and-listbox-control"></a><span data-ttu-id="b2aca-172">Vytvoření hostitelského okna a ovládacího prvku seznamu</span><span class="sxs-lookup"><span data-stu-id="b2aca-172">Create the Host Window and ListBox Control</span></span>  
 <span data-ttu-id="b2aca-173">Můžete použít PInvoke k vytvoření hostitelského okna pro ovládací prvek vytvořením a registrací třídy okna a tak dále.</span><span class="sxs-lookup"><span data-stu-id="b2aca-173">You can use PInvoke to create a host window for the control by creating and registering a window class, and so on.</span></span> <span data-ttu-id="b2aca-174">Mnohem jednodušší přístup je však vytvořit okno s předdefinovanou "" statickou "třídou" Window ".</span><span class="sxs-lookup"><span data-stu-id="b2aca-174">However, a much simpler approach is to create a window with the predefined "static" window class.</span></span> <span data-ttu-id="b2aca-175">To vám poskytne postup v okně, který potřebujete pro příjem oznámení z ovládacího prvku a vyžaduje minimální kódování.</span><span class="sxs-lookup"><span data-stu-id="b2aca-175">This provides you with the window procedure you need in order to receive notifications from the control, and requires minimal coding.</span></span>  
  
 <span data-ttu-id="b2aca-176">Vlastnost HWND ovládacího prvku je vystavena prostřednictvím vlastnosti jen pro čtení, aby ji hostitelská stránka mohla použít k posílání zpráv do ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="b2aca-176">The HWND of the control is exposed through a read-only property, such that the host page can use it to send messages to the control.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#intptrproperty)]
 [!code-vb[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#intptrproperty)]  
  
 <span data-ttu-id="b2aca-177">Ovládací prvek ListBox je vytvořen jako podřízený uzel hostitelského okna.</span><span class="sxs-lookup"><span data-stu-id="b2aca-177">The ListBox control is created as a child of the host window.</span></span> <span data-ttu-id="b2aca-178">Výška a šířka obou oken je nastavena na hodnoty předané do konstruktoru, popsané výše.</span><span class="sxs-lookup"><span data-stu-id="b2aca-178">The height and width of both windows are set to the values passed to the constructor, discussed above.</span></span> <span data-ttu-id="b2aca-179">Tím se zajistí, že velikost hostitelského okna a ovládacího prvku je stejná jako vyhrazená oblast na stránce.</span><span class="sxs-lookup"><span data-stu-id="b2aca-179">This ensures that the size of the host window and control is identical to the reserved area on the page.</span></span>  <span data-ttu-id="b2aca-180">Po vytvoření okna ukázka vrátí objekt <xref:System.Runtime.InteropServices.HandleRef>, který obsahuje HWND okna hostitele.</span><span class="sxs-lookup"><span data-stu-id="b2aca-180">After the windows are created, the sample returns a <xref:System.Runtime.InteropServices.HandleRef> object that contains the HWND of the host window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcore)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcore)]  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcorehelper)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcorehelper)]  
  
<a name="destroywindow_wndproc"></a>   
### <a name="implement-destroywindow-and-wndproc"></a><span data-ttu-id="b2aca-181">Implementace DestroyWindow a WndProc</span><span class="sxs-lookup"><span data-stu-id="b2aca-181">Implement DestroyWindow and WndProc</span></span>  
 <span data-ttu-id="b2aca-182">Kromě <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>musíte také přepsat metody <xref:System.Windows.Interop.HwndHost.WndProc%2A> a <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="b2aca-182">In addition to <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>, you must also override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> methods of the <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="b2aca-183">V tomto příkladu jsou zprávy pro ovládací prvek zpracovávány obslužnou rutinou <xref:System.Windows.Interop.HwndHost.MessageHook>, takže implementace <xref:System.Windows.Interop.HwndHost.WndProc%2A> a <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> je minimální.</span><span class="sxs-lookup"><span data-stu-id="b2aca-183">In this example, the messages for the control are handled by the <xref:System.Windows.Interop.HwndHost.MessageHook> handler, thus the implementation of <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> is minimal.</span></span> <span data-ttu-id="b2aca-184">V případě <xref:System.Windows.Interop.HwndHost.WndProc%2A>nastavte `handled` na `false` pro indikaci, že zpráva nebyla zpracována a vrátí hodnotu 0.</span><span class="sxs-lookup"><span data-stu-id="b2aca-184">In the case of <xref:System.Windows.Interop.HwndHost.WndProc%2A>, set `handled` to `false` to indicate that the  message was not handled and return 0.</span></span> <span data-ttu-id="b2aca-185">V případě <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>jednoduše zničí okno.</span><span class="sxs-lookup"><span data-stu-id="b2aca-185">For <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>, simply destroy the window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroy)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroy)]  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroyhelper)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroyhelper)]  
  
<a name="host_the_control"></a>   
## <a name="host-the-control-on-the-page"></a><span data-ttu-id="b2aca-186">Hostování ovládacího prvku na stránce</span><span class="sxs-lookup"><span data-stu-id="b2aca-186">Host the Control on the Page</span></span>  
 <span data-ttu-id="b2aca-187">Chcete-li hostovat ovládací prvek na stránce, nejprve vytvořte novou instanci třídy `ControlHost`.</span><span class="sxs-lookup"><span data-stu-id="b2aca-187">To host the control on the page, you first create a new instance of the `ControlHost` class.</span></span> <span data-ttu-id="b2aca-188">Předejte výšku a šířku prvku ohraničení, který obsahuje ovládací prvek (`ControlHostElement`) do konstruktoru `ControlHost`.</span><span class="sxs-lookup"><span data-stu-id="b2aca-188">Pass the height and width of the border element that contains the control (`ControlHostElement`) to the `ControlHost` constructor.</span></span> <span data-ttu-id="b2aca-189">Tím se zajistí, že se seznam bude správně měnit.</span><span class="sxs-lookup"><span data-stu-id="b2aca-189">This ensures that the ListBox is sized correctly.</span></span> <span data-ttu-id="b2aca-190">Potom můžete ovládací prvek hostovat na stránce přiřazením objektu `ControlHost` k vlastnosti <xref:System.Windows.Controls.Decorator.Child%2A> <xref:System.Windows.Controls.Border>hostitele.</span><span class="sxs-lookup"><span data-stu-id="b2aca-190">You then host the control on the page by assigning the `ControlHost` object to the <xref:System.Windows.Controls.Decorator.Child%2A> property of the host <xref:System.Windows.Controls.Border>.</span></span>  
  
 <span data-ttu-id="b2aca-191">Ukázka připojí obslužnou rutinu k události <xref:System.Windows.Interop.HwndHost.MessageHook> `ControlHost` pro příjem zpráv z ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="b2aca-191">The sample attaches a handler to the <xref:System.Windows.Interop.HwndHost.MessageHook> event of the `ControlHost` to receive messages from the control.</span></span> <span data-ttu-id="b2aca-192">Tato událost je vyvolána pro každou zprávu odeslanou do hostovaného okna.</span><span class="sxs-lookup"><span data-stu-id="b2aca-192">This event is raised for every message sent to the hosted window.</span></span> <span data-ttu-id="b2aca-193">V tomto případě se jedná o zprávy odesílané do okna, které zabalí skutečný ovládací prvek seznamu, včetně oznámení z ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="b2aca-193">In this case, these are the messages sent to window that wraps the actual ListBox control, including notifications from the control.</span></span> <span data-ttu-id="b2aca-194">Ukázka volá metodu SendMessage pro získání informací z ovládacího prvku a pro úpravu jeho obsahu.</span><span class="sxs-lookup"><span data-stu-id="b2aca-194">The sample calls SendMessage to obtain information from the control and modify its contents.</span></span> <span data-ttu-id="b2aca-195">Podrobnosti o tom, jak stránka komunikuje s ovládacím prvkem, jsou popsány v následující části.</span><span class="sxs-lookup"><span data-stu-id="b2aca-195">The details of how the page communicates with the control are discussed in the next section.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="b2aca-196">Všimněte si, že existují dvě deklarace PInvoke pro SendMessage.</span><span class="sxs-lookup"><span data-stu-id="b2aca-196">Notice that there are two PInvoke declarations for SendMessage.</span></span> <span data-ttu-id="b2aca-197">To je nezbytné, protože jeden používá parametr `wParam` k předání řetězce a druhý ho používá k předání celého čísla.</span><span class="sxs-lookup"><span data-stu-id="b2aca-197">This is necessary because one uses the `wParam` parameter to pass a string and the other uses it to pass an integer.</span></span> <span data-ttu-id="b2aca-198">Pro každý podpis potřebujete samostatnou deklaraci, abyste zajistili správné zařazení dat.</span><span class="sxs-lookup"><span data-stu-id="b2aca-198">You need a separate declaration for each signature to ensure that the data is marshaled correctly.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#hostwindowclass)]
 [!code-vb[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#hostwindowclass)]  
  
 [!code-csharp[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#controlmsgfiltersendmessage)]
 [!code-vb[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#controlmsgfiltersendmessage)]  
  
<a name="communication"></a>   
## <a name="implement-communication-between-the-control-and-the-page"></a><span data-ttu-id="b2aca-199">Implementace komunikace mezi ovládacím prvkem a stránkou</span><span class="sxs-lookup"><span data-stu-id="b2aca-199">Implement Communication Between the Control and the Page</span></span>  
 <span data-ttu-id="b2aca-200">Řízení se řídí odesláním zpráv systému Windows.</span><span class="sxs-lookup"><span data-stu-id="b2aca-200">You manipulate the control by sending it Windows messages.</span></span> <span data-ttu-id="b2aca-201">Ovládací prvek vás upozorní, když s ním uživatel komunikuje odesláním oznámení do hostitelského okna.</span><span class="sxs-lookup"><span data-stu-id="b2aca-201">The control notifies you when the user interacts with it by sending notifications to its host window.</span></span> <span data-ttu-id="b2aca-202">[Hostování ovládacího prvku seznamu Win32 v UKÁZCE WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control) obsahuje uživatelské rozhraní, které poskytuje několik příkladů toho, jak to funguje:</span><span class="sxs-lookup"><span data-stu-id="b2aca-202">The [Hosting a Win32 ListBox Control in WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control) sample includes a UI that provides several examples of how this works:</span></span>  
  
- <span data-ttu-id="b2aca-203">Připojí položku k seznamu.</span><span class="sxs-lookup"><span data-stu-id="b2aca-203">Append an item to the list.</span></span>  
  
- <span data-ttu-id="b2aca-204">Odstranit vybranou položku ze seznamu</span><span class="sxs-lookup"><span data-stu-id="b2aca-204">Delete the selected item from the list</span></span>  
  
- <span data-ttu-id="b2aca-205">Zobrazí text aktuálně vybrané položky.</span><span class="sxs-lookup"><span data-stu-id="b2aca-205">Display the text of the currently selected item.</span></span>  
  
- <span data-ttu-id="b2aca-206">Zobrazí počet položek v seznamu.</span><span class="sxs-lookup"><span data-stu-id="b2aca-206">Display the number of items in the list.</span></span>  
  
 <span data-ttu-id="b2aca-207">Uživatel může také vybrat položku v seznamu kliknutím na ni, stejně jako v případě konvenční aplikace Win32.</span><span class="sxs-lookup"><span data-stu-id="b2aca-207">The user can also select an item in the list box by clicking on it, just as they would for a conventional Win32 application.</span></span> <span data-ttu-id="b2aca-208">Zobrazená data se aktualizují pokaždé, když uživatel změní stav pole seznam tak, že vybere, přidá nebo připojí položku.</span><span class="sxs-lookup"><span data-stu-id="b2aca-208">The displayed data is updated each time the user changes the state of the list box by selecting, adding, or appending an item.</span></span>  
  
 <span data-ttu-id="b2aca-209">Chcete-li připojit položky, odešlete seznam [`LB_ADDSTRING` zprávu](/windows/desktop/Controls/lb-addstring).</span><span class="sxs-lookup"><span data-stu-id="b2aca-209">To append items, send the list box an [`LB_ADDSTRING` message](/windows/desktop/Controls/lb-addstring).</span></span> <span data-ttu-id="b2aca-210">Chcete-li odstranit položky, odešlete [`LB_GETCURSEL`](/windows/desktop/Controls/lb-getcursel) , abyste získali index aktuálního výběru a pak [`LB_DELETESTRING`](/windows/desktop/Controls/lb-deletestring) položku odstranili.</span><span class="sxs-lookup"><span data-stu-id="b2aca-210">To delete items, send [`LB_GETCURSEL`](/windows/desktop/Controls/lb-getcursel) to get the index of the current selection and then [`LB_DELETESTRING`](/windows/desktop/Controls/lb-deletestring) to delete the item.</span></span> <span data-ttu-id="b2aca-211">Ukázka také odesílá [`LB_GETCOUNT`](/windows/desktop/Controls/lb-getcount)a pomocí vrácené hodnoty aktualizuje zobrazení, které zobrazuje počet položek.</span><span class="sxs-lookup"><span data-stu-id="b2aca-211">The sample also sends [`LB_GETCOUNT`](/windows/desktop/Controls/lb-getcount), and uses the returned value to update the display that shows the number of items.</span></span> <span data-ttu-id="b2aca-212">Obě tyto instance [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) používají jeden z deklarací PInvoke popsaných v předchozí části.</span><span class="sxs-lookup"><span data-stu-id="b2aca-212">Both these instances of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) use one of the PInvoke declarations discussed in the previous section.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#appenddeletetext)]
 [!code-vb[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#appenddeletetext)]  
  
 <span data-ttu-id="b2aca-213">Když uživatel vybere položku nebo změní jejich výběr, ovládací prvek upozorní okno hostitele tím, že pošle [`WM_COMMAND` zprávu](/windows/desktop/menurc/wm-command), která vyvolává událost <xref:System.Windows.Interop.HwndHost.MessageHook> stránky.</span><span class="sxs-lookup"><span data-stu-id="b2aca-213">When the user selects an item or changes their selection, the control notifies the host window by sending it a [`WM_COMMAND` message](/windows/desktop/menurc/wm-command), which raises the <xref:System.Windows.Interop.HwndHost.MessageHook> event for the page.</span></span> <span data-ttu-id="b2aca-214">Obslužná rutina obdrží stejné informace jako hlavní okno v rámci okna hostitele.</span><span class="sxs-lookup"><span data-stu-id="b2aca-214">The handler receives the same information as the main window procedure of the host window.</span></span> <span data-ttu-id="b2aca-215">Také předává odkaz na logickou hodnotu, `handled`.</span><span class="sxs-lookup"><span data-stu-id="b2aca-215">It also passes a reference to a Boolean value, `handled`.</span></span> <span data-ttu-id="b2aca-216">Nastavením `handled` `true` označíte, že jste zprávu zpracovali a ještě není potřeba žádné další zpracování.</span><span class="sxs-lookup"><span data-stu-id="b2aca-216">You set `handled` to `true` to indicate that you have handled the message and no further processing is needed.</span></span>  
  
 <span data-ttu-id="b2aca-217">[`WM_COMMAND`](/windows/desktop/menurc/wm-command) se odesílá z nejrůznějších důvodů, takže je nutné prostudovat ID oznámení, abyste zjistili, jestli se jedná o událost, kterou chcete zpracovat.</span><span class="sxs-lookup"><span data-stu-id="b2aca-217">[`WM_COMMAND`](/windows/desktop/menurc/wm-command) is sent for a variety of reasons, so you must examine the notification ID to determine whether it is an event that you wish to handle.</span></span> <span data-ttu-id="b2aca-218">ID je obsaženo v horním slově parametru `wParam`.</span><span class="sxs-lookup"><span data-stu-id="b2aca-218">The ID is contained in the high word of the `wParam` parameter.</span></span> <span data-ttu-id="b2aca-219">Ukázka používá k extrakci IDENTIFIKÁTORu bitové operátory.</span><span class="sxs-lookup"><span data-stu-id="b2aca-219">The sample uses bitwise operators to extract the ID.</span></span> <span data-ttu-id="b2aca-220">Pokud uživatel provedl nebo změnil výběr, bude ID [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange).</span><span class="sxs-lookup"><span data-stu-id="b2aca-220">If the user has made or changed their selection, the ID will be [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange).</span></span>  
  
 <span data-ttu-id="b2aca-221">Při přijetí [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange) získá ukázka index vybrané položky odesláním ovládacího prvku [`LB_GETCURSEL` zprávu](/windows/desktop/Controls/lb-getcursel).</span><span class="sxs-lookup"><span data-stu-id="b2aca-221">When [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange) is received, the sample gets the index of the selected item by sending the control a [`LB_GETCURSEL` message](/windows/desktop/Controls/lb-getcursel).</span></span> <span data-ttu-id="b2aca-222">Chcete-li získat text, je třeba nejprve vytvořit <xref:System.Text.StringBuilder>.</span><span class="sxs-lookup"><span data-stu-id="b2aca-222">To get the text, you first create a <xref:System.Text.StringBuilder>.</span></span> <span data-ttu-id="b2aca-223">Pak ovládacímu prvku odešlete [`LB_GETTEXT`ovou zprávu](/windows/desktop/Controls/lb-gettext).</span><span class="sxs-lookup"><span data-stu-id="b2aca-223">You then send the control an [`LB_GETTEXT` message](/windows/desktop/Controls/lb-gettext).</span></span> <span data-ttu-id="b2aca-224">Předejte prázdný <xref:System.Text.StringBuilder> objekt jako parametr `wParam`.</span><span class="sxs-lookup"><span data-stu-id="b2aca-224">Pass the empty <xref:System.Text.StringBuilder> object as the `wParam` parameter.</span></span> <span data-ttu-id="b2aca-225">Když [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) vrátí, <xref:System.Text.StringBuilder> bude obsahovat text vybrané položky.</span><span class="sxs-lookup"><span data-stu-id="b2aca-225">When [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) returns, the <xref:System.Text.StringBuilder> will contain the text of the selected item.</span></span> <span data-ttu-id="b2aca-226">Toto použití [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) vyžaduje ještě jinou deklaraci PInvoke.</span><span class="sxs-lookup"><span data-stu-id="b2aca-226">This use of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) requires yet another PInvoke declaration.</span></span>  
  
 <span data-ttu-id="b2aca-227">Nakonec nastavte `handled` na `true`, aby označovala, že zpráva byla zpracována.</span><span class="sxs-lookup"><span data-stu-id="b2aca-227">Finally, set `handled` to `true` to indicate that the message has been handled.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="b2aca-228">Viz také:</span><span class="sxs-lookup"><span data-stu-id="b2aca-228">See also</span></span>

- <xref:System.Windows.Interop.HwndHost>
- [<span data-ttu-id="b2aca-229">Vzájemná spolupráce grafického subsystému WPF a systému Win32</span><span class="sxs-lookup"><span data-stu-id="b2aca-229">WPF and Win32 Interoperation</span></span>](wpf-and-win32-interoperation.md)
- [<span data-ttu-id="b2aca-230">Návod: Moje první desktopová aplikace WPF</span><span class="sxs-lookup"><span data-stu-id="b2aca-230">Walkthrough: My first WPF desktop application</span></span>](../getting-started/walkthrough-my-first-wpf-desktop-application.md)
