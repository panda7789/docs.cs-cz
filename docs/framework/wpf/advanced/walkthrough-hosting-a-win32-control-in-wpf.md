---
title: 'Návod: Hostování ovládacího prvku Win32 ve WPF'
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- hosting Win32 control in WPF [WPF]
- Win32 code [WPF], WPF interoperation
ms.assetid: a676b1eb-fc55-4355-93ab-df840c41cea0
ms.openlocfilehash: 1ba060fcefb2d8be24d597c7b1ccb7a79d6d5ceb
ms.sourcegitcommit: 5b6d778ebb269ee6684fb57ad69a8c28b06235b9
ms.translationtype: HT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/08/2019
ms.locfileid: "59160690"
---
# <a name="walkthrough-hosting-a-win32-control-in-wpf"></a><span data-ttu-id="6bd43-102">Návod: Hostování ovládacího prvku Win32 ve WPF</span><span class="sxs-lookup"><span data-stu-id="6bd43-102">Walkthrough: Hosting a Win32 Control in WPF</span></span>
<span data-ttu-id="6bd43-103">Windows Presentation Foundation (WPF) poskytuje bohaté prostředí pro vytváření aplikací.</span><span class="sxs-lookup"><span data-stu-id="6bd43-103">Windows Presentation Foundation (WPF) provides a rich environment for creating applications.</span></span> <span data-ttu-id="6bd43-104">Ale pokud máte značné investice v kódu Win32, může být efektivnější opakovaně používat alespoň některé tohoto kódu v aplikaci WPF spíše než přepíše zcela.</span><span class="sxs-lookup"><span data-stu-id="6bd43-104">However, when you have a substantial investment in Win32 code, it may be more effective to reuse at least some of that code in your WPF application rather than rewrite it completely.</span></span> <span data-ttu-id="6bd43-105">WPF poskytuje jednoduchý mechanismus pro hostování okně Win32, na stránce WPF.</span><span class="sxs-lookup"><span data-stu-id="6bd43-105">WPF provides a straightforward mechanism for hosting a Win32 window, on a WPF page.</span></span>  
  
 <span data-ttu-id="6bd43-106">Toto téma vás provede aplikace, [hostování ovládacím prvku Win32 v ukázce WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control), že ovládací prvek pole se seznamem Win32 hostitele.</span><span class="sxs-lookup"><span data-stu-id="6bd43-106">This topic walks you through an application, [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control), that hosts a Win32 list box control.</span></span> <span data-ttu-id="6bd43-107">Tento obecný postup je možné rozšířit na hostování jakékoli okno Win32.</span><span class="sxs-lookup"><span data-stu-id="6bd43-107">This general procedure can be extended to hosting any Win32 window.</span></span>  

<a name="requirements"></a>   
## <a name="requirements"></a><span data-ttu-id="6bd43-108">Požadavky</span><span class="sxs-lookup"><span data-stu-id="6bd43-108">Requirements</span></span>  
 <span data-ttu-id="6bd43-109">Toto téma předpokládá základní znalost WPF a Windows API programování.</span><span class="sxs-lookup"><span data-stu-id="6bd43-109">This topic assumes a basic familiarity with both WPF and Windows API programming.</span></span> <span data-ttu-id="6bd43-110">Základní informace o programování WPF, naleznete v tématu [Začínáme](../getting-started/index.md).</span><span class="sxs-lookup"><span data-stu-id="6bd43-110">For a basic introduction to WPF programming, see [Getting Started](../getting-started/index.md).</span></span> <span data-ttu-id="6bd43-111">Úvod k programování v rozhraní Windows API, naleznete v některém z mnoha knihy k tomuto tématu, zejména *programování Windows* podle Charles Petzold.</span><span class="sxs-lookup"><span data-stu-id="6bd43-111">For an introduction to Windows API programming, see any of the numerous books on the subject, in particular *Programming Windows* by Charles Petzold.</span></span>  
  
 <span data-ttu-id="6bd43-112">Protože vzorku, který doprovází toto téma je implementována v C#, ho využívá platformu vyvolání služby (PInvoke) pro přístup k rozhraní API Windows.</span><span class="sxs-lookup"><span data-stu-id="6bd43-112">Because the sample that accompanies this topic is implemented in C#, it makes use of Platform Invocation Services (PInvoke) to access the Windows API.</span></span> <span data-ttu-id="6bd43-113">Některé znalost PInvoke je užitečné, ale není nutná.</span><span class="sxs-lookup"><span data-stu-id="6bd43-113">Some familiarity with PInvoke is helpful but not essential.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="6bd43-114">Toto téma obsahuje několik příkladů kódu z ukázky přidružené.</span><span class="sxs-lookup"><span data-stu-id="6bd43-114">This topic includes a number of code examples from the associated sample.</span></span> <span data-ttu-id="6bd43-115">Ale pro lepší čitelnost, neobsahuje úplnou ukázku kódu.</span><span class="sxs-lookup"><span data-stu-id="6bd43-115">However, for readability, it does not include the complete sample code.</span></span> <span data-ttu-id="6bd43-116">Můžete získat nebo zobrazit kompletní kód z [hostování ovládacím prvku Win32 v ukázce WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control).</span><span class="sxs-lookup"><span data-stu-id="6bd43-116">You can obtain or view complete code from [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control).</span></span>  
  
<a name="basic_procedure"></a>   
## <a name="the-basic-procedure"></a><span data-ttu-id="6bd43-117">Základní postup</span><span class="sxs-lookup"><span data-stu-id="6bd43-117">The Basic Procedure</span></span>  
 <span data-ttu-id="6bd43-118">Tato část popisuje základní postup pro hostování okně Win32 na stránce WPF.</span><span class="sxs-lookup"><span data-stu-id="6bd43-118">This section outlines the basic procedure for hosting a Win32 window on a WPF page.</span></span> <span data-ttu-id="6bd43-119">Zbývající části projít podrobnosti o jednotlivých kroků.</span><span class="sxs-lookup"><span data-stu-id="6bd43-119">The remaining sections go through the details of each step.</span></span>  
  
 <span data-ttu-id="6bd43-120">Základní postup hostingu je:</span><span class="sxs-lookup"><span data-stu-id="6bd43-120">The basic hosting procedure is:</span></span>  
  
1.  <span data-ttu-id="6bd43-121">Implementace stránky WPF pro hostování v okně.</span><span class="sxs-lookup"><span data-stu-id="6bd43-121">Implement a WPF page to host the window.</span></span> <span data-ttu-id="6bd43-122">Je jednou z metod vytvoření <xref:System.Windows.Controls.Border> element rezervovat oddíl na stránce pro okno prostředí.</span><span class="sxs-lookup"><span data-stu-id="6bd43-122">One technique is to create a <xref:System.Windows.Controls.Border> element to reserve a section of the page for the hosted window.</span></span>  
  
2.  <span data-ttu-id="6bd43-123">Implementace třídy pro hostování ovládacího prvku, který dědí z <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="6bd43-123">Implement a class to host the control that inherits from <xref:System.Windows.Interop.HwndHost>.</span></span>  
  
3.  <span data-ttu-id="6bd43-124">V této třídě přepsat <xref:System.Windows.Interop.HwndHost> člena třídy <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>.</span><span class="sxs-lookup"><span data-stu-id="6bd43-124">In that class, override the <xref:System.Windows.Interop.HwndHost> class member <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>.</span></span>  
  
4.  <span data-ttu-id="6bd43-125">Vytvoření okna prostředí jako podřízená položka okna, která obsahuje stránku WPF.</span><span class="sxs-lookup"><span data-stu-id="6bd43-125">Create the hosted window as a child of the window that contains the WPF page.</span></span> <span data-ttu-id="6bd43-126">I když není potřeba explicitně vytvořit konvenční programování WPF využít, stránka hostingu je okno s popisovačem (HWND).</span><span class="sxs-lookup"><span data-stu-id="6bd43-126">Although conventional WPF programming does not need to explicitly make use of it, the hosting page is a window with a handle (HWND).</span></span> <span data-ttu-id="6bd43-127">Se zobrazí stránka HWND prostřednictvím `hwndParent` parametr <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="6bd43-127">You receive the page HWND through the `hwndParent` parameter of the <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A> method.</span></span> <span data-ttu-id="6bd43-128">V okně prostředí musí být vytvořené jako podřízený objekt tento HWND.</span><span class="sxs-lookup"><span data-stu-id="6bd43-128">The hosted window should be created as a child of this HWND.</span></span>  
  
5.  <span data-ttu-id="6bd43-129">Po vytvoření okno hostitele vrací HWND okno prostředí.</span><span class="sxs-lookup"><span data-stu-id="6bd43-129">Once you have created the host window, return the HWND of the hosted window.</span></span> <span data-ttu-id="6bd43-130">Pokud chcete hostovat jeden nebo více ovládacích prvků systému Win32, obvykle vytvoříte okno hostitele jako podřízený objekt HWND a ujistěte se, podřízené ovládací prvky tohoto okna hostitele.</span><span class="sxs-lookup"><span data-stu-id="6bd43-130">If you want to host one or more Win32 controls, you typically create a host window as a child of the HWND and make the controls children of that host window.</span></span> <span data-ttu-id="6bd43-131">Obtékání ovládacích prvků v okně hostitel poskytuje jednoduchý způsob WPF stránky pro příjem oznámení z ovládacích prvků, která se zabývá některé konkrétní Win32 problémy s oznámeními hranice HWND.</span><span class="sxs-lookup"><span data-stu-id="6bd43-131">Wrapping the controls in a host window provides a simple way for your WPF page to receive notifications from the controls, which deals with some particular Win32 issues with notifications across the HWND boundary.</span></span>  
  
6.  <span data-ttu-id="6bd43-132">Zpracování vybrané zprávy odeslané do okna hostitele, jako jsou oznámení z podřízených ovládacích prvků.</span><span class="sxs-lookup"><span data-stu-id="6bd43-132">Handle selected messages sent to the host window, such as notifications from child controls.</span></span> <span data-ttu-id="6bd43-133">Existují dva způsoby, jak to provést.</span><span class="sxs-lookup"><span data-stu-id="6bd43-133">There are two ways to do this.</span></span>  
  
    -   <span data-ttu-id="6bd43-134">Pokud chcete zpracovávat zprávy v hostující třídy, má přednost před <xref:System.Windows.Interop.HwndHost.WndProc%2A> metodu <xref:System.Windows.Interop.HwndHost> třídy.</span><span class="sxs-lookup"><span data-stu-id="6bd43-134">If you prefer to handle messages in your hosting class, override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> method of the <xref:System.Windows.Interop.HwndHost> class.</span></span>  
  
    -   <span data-ttu-id="6bd43-135">Pokud chcete mít WPF slouží ke zpracování zpráv, nastavte popisovač <xref:System.Windows.Interop.HwndHost> třídy <xref:System.Windows.Interop.HwndHost.MessageHook> události do vašeho kódu.</span><span class="sxs-lookup"><span data-stu-id="6bd43-135">If you prefer to have the WPF handle the messages, handle the <xref:System.Windows.Interop.HwndHost> class <xref:System.Windows.Interop.HwndHost.MessageHook> event in your code-behind.</span></span> <span data-ttu-id="6bd43-136">Pro každou zprávu, která se zobrazila v okně prostředí dojde k této události.</span><span class="sxs-lookup"><span data-stu-id="6bd43-136">This event occurs for every message that is received by the hosted window.</span></span> <span data-ttu-id="6bd43-137">Pokud zvolíte tuto možnost, je nutné přepsat <xref:System.Windows.Interop.HwndHost.WndProc%2A>, ale potřebujete jenom minimální implementaci.</span><span class="sxs-lookup"><span data-stu-id="6bd43-137">If you choose this option, you must still override <xref:System.Windows.Interop.HwndHost.WndProc%2A>, but you only need a minimal implementation.</span></span>  
  
7.  <span data-ttu-id="6bd43-138">Přepsat <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> a <xref:System.Windows.Interop.HwndHost.WndProc%2A> metody <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="6bd43-138">Override the <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> and <xref:System.Windows.Interop.HwndHost.WndProc%2A> methods of <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="6bd43-139">Je nutné přepsat tyto metody splňovat <xref:System.Windows.Interop.HwndHost> smlouvy, ale může být pouze nutné zadat minimální implementaci.</span><span class="sxs-lookup"><span data-stu-id="6bd43-139">You must override these methods to satisfy the <xref:System.Windows.Interop.HwndHost> contract, but you may only need to provide a minimal implementation.</span></span>  
  
8.  <span data-ttu-id="6bd43-140">V souboru kódu na pozadí, vytvořte instanci hostující třídy ovládacího prvku a nastavte ji podřízený <xref:System.Windows.Controls.Border> element, který je určena k hostování okna.</span><span class="sxs-lookup"><span data-stu-id="6bd43-140">In your code-behind file, create an instance of the control hosting class and make it a child of the <xref:System.Windows.Controls.Border> element that is intended to host the window.</span></span>  
  
9. <span data-ttu-id="6bd43-141">Komunikovat s hostovanou okna a odeslat ho [!INCLUDE[TLA#tla_win](../../../../includes/tlasharptla-win-md.md)] zprávy a zpracování zprávy z jeho podřízených oken, jako je například oznámení zaslaná z ovládacích prvků.</span><span class="sxs-lookup"><span data-stu-id="6bd43-141">Communicate with the hosted window by sending it [!INCLUDE[TLA#tla_win](../../../../includes/tlasharptla-win-md.md)] messages and handling messages from its child windows, such as notifications sent by controls.</span></span>  
  
<a name="page_layout"></a>   
## <a name="implement-the-page-layout"></a><span data-ttu-id="6bd43-142">Implementace rozložení stránky</span><span class="sxs-lookup"><span data-stu-id="6bd43-142">Implement the Page Layout</span></span>  
 <span data-ttu-id="6bd43-143">Rozložení pro stránku WPF, který je hostitelem ListBox – ovládací prvek se skládá ze dvou oblastech.</span><span class="sxs-lookup"><span data-stu-id="6bd43-143">The layout for the WPF page that hosts the ListBox Control consists of two regions.</span></span> <span data-ttu-id="6bd43-144">Levé straně stránky hostitelem několik ovládacích prvků WPF, které poskytují [!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)] , který umožňuje manipulaci s ovládacího prvku Win32.</span><span class="sxs-lookup"><span data-stu-id="6bd43-144">The left side of the page hosts several WPF controls that provide a [!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)] that allows you to manipulate the Win32 control.</span></span> <span data-ttu-id="6bd43-145">Pravém horním rohu stránky má Čtvereček oblast pro hostované ListBox – ovládací prvek.</span><span class="sxs-lookup"><span data-stu-id="6bd43-145">The upper right corner of the page has a square region for the hosted ListBox Control.</span></span>  
  
 <span data-ttu-id="6bd43-146">Kód pro implementaci toto rozložení je poměrně jednoduché.</span><span class="sxs-lookup"><span data-stu-id="6bd43-146">The code to implement this layout is quite simple.</span></span> <span data-ttu-id="6bd43-147">Kořenový element <xref:System.Windows.Controls.DockPanel> , který má dva podřízené prvky.</span><span class="sxs-lookup"><span data-stu-id="6bd43-147">The root element is a <xref:System.Windows.Controls.DockPanel> that has two child elements.</span></span> <span data-ttu-id="6bd43-148">První je <xref:System.Windows.Controls.Border> element, který je hostitelem ListBox – ovládací prvek.</span><span class="sxs-lookup"><span data-stu-id="6bd43-148">The first is a <xref:System.Windows.Controls.Border> element that hosts the ListBox Control.</span></span> <span data-ttu-id="6bd43-149">Zabírá Čtvereček in 200 x 200 pravém horním rohu stránky.</span><span class="sxs-lookup"><span data-stu-id="6bd43-149">It occupies a 200x200 square in the upper right corner of the page.</span></span> <span data-ttu-id="6bd43-150">Druhým je <xref:System.Windows.Controls.StackPanel> element, který obsahuje sadu ovládacích prvků WPF, které zobrazují informace a umožňují manipulovat s ListBox – ovládací prvek tak, že nastavíte vystavený součinnosti vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="6bd43-150">The second is a <xref:System.Windows.Controls.StackPanel> element that contains a set of WPF controls that display information and allow you to manipulate the ListBox Control by setting exposed interoperation properties.</span></span> <span data-ttu-id="6bd43-151">Pro každý z prvků, které jsou podřízené <xref:System.Windows.Controls.StackPanel>, naleznete v tématu referenční materiál pro různé prvky používá pro podrobnosti o tyto prvky jsou nebo co dělají, ty jsou uvedené ve níže uvedeného ukázkového kódu, ale nebudou je zde vysvětleno (na úrovni basic vzájemné spolupráce modelu nevyžaduje žádnou z nich, jsou poskytovány přidat některé interaktivitu k ukázce).</span><span class="sxs-lookup"><span data-stu-id="6bd43-151">For each of the elements that are children of the <xref:System.Windows.Controls.StackPanel>, see the reference material for the various elements used for details on what these elements are or what they do, these are listed in the example code below but will not be explained here (the basic interoperation model does not require any of them, they are provided to add some interactivity to the sample).</span></span>  
  
 [!code-xaml[WPFHostingWin32Control#WPFUI](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml#wpfui)]  
  
<a name="host_class"></a>   
## <a name="implement-a-class-to-host-the-microsoft-win32-control"></a><span data-ttu-id="6bd43-152">Implementace třídy pro hostování ovládacího prvku Microsoft Win32</span><span class="sxs-lookup"><span data-stu-id="6bd43-152">Implement a Class to Host the Microsoft Win32 Control</span></span>  
 <span data-ttu-id="6bd43-153">Jádrem této ukázce je třída, která ve skutečnosti hostuje ovládací prvek, ControlHost.cs.</span><span class="sxs-lookup"><span data-stu-id="6bd43-153">The core of this sample is the class that actually hosts the control, ControlHost.cs.</span></span> <span data-ttu-id="6bd43-154">Dědí z <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="6bd43-154">It inherits from <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="6bd43-155">Konstruktor přijímá dva parametry, výšku a šířku, které odpovídají výšku a šířku <xref:System.Windows.Controls.Border> element, který je hostitelem ListBox – ovládací prvek.</span><span class="sxs-lookup"><span data-stu-id="6bd43-155">The constructor takes two parameters, height and width, which correspond to the height and width of the <xref:System.Windows.Controls.Border> element that hosts the ListBox control.</span></span> <span data-ttu-id="6bd43-156">Tyto hodnoty se později použijí k Ujistěte se, že velikost odpovídá ovládacího prvku <xref:System.Windows.Controls.Border> elementu.</span><span class="sxs-lookup"><span data-stu-id="6bd43-156">These values are used later to ensure that the size of the control matches the <xref:System.Windows.Controls.Border> element.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostclass)]
 [!code-vb[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostclass)]  
  
 <span data-ttu-id="6bd43-157">Existuje také sada konstanty.</span><span class="sxs-lookup"><span data-stu-id="6bd43-157">There is also a set of constants.</span></span> <span data-ttu-id="6bd43-158">Tyto konstanty do značné míry pocházejí ze winuser a umožňují používat běžné názvy při volání funkce Win32.</span><span class="sxs-lookup"><span data-stu-id="6bd43-158">These constants are largely taken from Winuser.h, and allow you to use conventional names when calling Win32 functions.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostconstants)]
 [!code-vb[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostconstants)]  
  
<a name="buildwindowcore"></a>   
### <a name="override-buildwindowcore-to-create-the-microsoft-win32-window"></a><span data-ttu-id="6bd43-159">Přepsat BuildWindowCore se vytvořit okno Microsoft Win32</span><span class="sxs-lookup"><span data-stu-id="6bd43-159">Override BuildWindowCore to Create the Microsoft Win32 Window</span></span>  
 <span data-ttu-id="6bd43-160">Můžete přepsat tuto metodu za účelem vytvoření okně Win32, které budou hostovány stránky a vytvoření připojení mezi oknem a na stránce.</span><span class="sxs-lookup"><span data-stu-id="6bd43-160">You override this method to create the Win32 window that will be hosted by the page, and make the connection between the window and the page.</span></span> <span data-ttu-id="6bd43-161">Vzhledem k tomu, že tento příklad zahrnuje hostování ovládacího prvku ListBox, se vytvoří dvě okna.</span><span class="sxs-lookup"><span data-stu-id="6bd43-161">Because this sample involves hosting a ListBox Control, two windows are created.</span></span> <span data-ttu-id="6bd43-162">První je okno, které je ve skutečnosti hostované na stránce WPF.</span><span class="sxs-lookup"><span data-stu-id="6bd43-162">The first is the window that is actually hosted by the WPF page.</span></span> <span data-ttu-id="6bd43-163">ListBox – ovládací prvek je vytvořen jako podřízený objekt tohoto okna.</span><span class="sxs-lookup"><span data-stu-id="6bd43-163">The ListBox Control is created as a child of that window.</span></span>  
  
 <span data-ttu-id="6bd43-164">Důvod pro tento přístup je ke zjednodušení procesu pro příjem oznámení z ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="6bd43-164">The reason for this approach is to simplify the process of receiving notifications from the control.</span></span> <span data-ttu-id="6bd43-165"><xref:System.Windows.Interop.HwndHost> Třída umožňuje zpracovávat zprávy odeslané do okna, který je hostitelem.</span><span class="sxs-lookup"><span data-stu-id="6bd43-165">The <xref:System.Windows.Interop.HwndHost> class allows you to process messages sent to the window that it is hosting.</span></span> <span data-ttu-id="6bd43-166">Pokud hostitele Win32 řídit přímo, zobrazí se zprávy odeslané do smyčky zpráv interní ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="6bd43-166">If you host a Win32 control directly, you receive the messages sent to the internal message loop of the control.</span></span> <span data-ttu-id="6bd43-167">Můžete zobrazit ovládací prvek a pošlete ji zprávy, ale neobdržíte oznámení, které ovládací prvek odešle nezašle nadřazenému oknu.</span><span class="sxs-lookup"><span data-stu-id="6bd43-167">You can display the control and send it messages, but you do not receive the notifications that the control sends to its parent window.</span></span> <span data-ttu-id="6bd43-168">To znamená, mimo jiné, že nemůžete nijak zjistit při interakci uživatele s ovládacím prvkem.</span><span class="sxs-lookup"><span data-stu-id="6bd43-168">This means, among other things, that you have no way of detecting when the user interacts with the control.</span></span> <span data-ttu-id="6bd43-169">Místo toho vytvořte okno hostitele a aby byl ovládací prvek podřízeným prvkem tohoto okna.</span><span class="sxs-lookup"><span data-stu-id="6bd43-169">Instead, create a host window and make the control a child of that window.</span></span> <span data-ttu-id="6bd43-170">To umožňuje zpracovávat zprávy pro okno hostitele, včetně oznámení odesílat ovládacím prvkem.</span><span class="sxs-lookup"><span data-stu-id="6bd43-170">This allows you to process the messages for the host window including the notifications sent to it by the control.</span></span> <span data-ttu-id="6bd43-171">Pro usnadnění práce protože okno hostitele je trochu více než jednoduché obálky ovládacího prvku balíček bude odkazovat jako ovládací prvek ListBox.</span><span class="sxs-lookup"><span data-stu-id="6bd43-171">For convenience, since the host window is little more than a simple wrapper for the control, the package will be referred to as a ListBox control.</span></span>  
  
<a name="create_the_window_and_listbox"></a>   
#### <a name="create-the-host-window-and-listbox-control"></a><span data-ttu-id="6bd43-172">Vytvořit okno hostitele a ListBox – ovládací prvek</span><span class="sxs-lookup"><span data-stu-id="6bd43-172">Create the Host Window and ListBox Control</span></span>  
 <span data-ttu-id="6bd43-173">Můžete použít k vytvoření do hostitelského okna pro ovládací prvek tak, že vytvoření a registrace tříd oken PInvoke a tak dále.</span><span class="sxs-lookup"><span data-stu-id="6bd43-173">You can use PInvoke to create a host window for the control by creating and registering a window class, and so on.</span></span> <span data-ttu-id="6bd43-174">Je však mnohem jednodušší přístup vytvořit okno s třídou předdefinované "statických" okna.</span><span class="sxs-lookup"><span data-stu-id="6bd43-174">However, a much simpler approach is to create a window with the predefined "static" window class.</span></span> <span data-ttu-id="6bd43-175">To vám poskytne proceduru okna potřebovat k přijímání oznámení z ovládacího prvku a vyžaduje minimální kódování.</span><span class="sxs-lookup"><span data-stu-id="6bd43-175">This provides you with the window procedure you need in order to receive notifications from the control, and requires minimal coding.</span></span>  
  
 <span data-ttu-id="6bd43-176">HWND ovládací prvek je přístupný prostřednictvím vlastnosti jen pro čtení, tak, aby stránka hostitele můžete použít k odesílání zpráv do ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="6bd43-176">The HWND of the control is exposed through a read-only property, such that the host page can use it to send messages to the control.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#intptrproperty)]
 [!code-vb[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#intptrproperty)]  
  
 <span data-ttu-id="6bd43-177">ListBox – ovládací prvek je vytvořen jako podřízená položka okna hostitele.</span><span class="sxs-lookup"><span data-stu-id="6bd43-177">The ListBox control is created as a child of the host window.</span></span> <span data-ttu-id="6bd43-178">Výšku a šířku windows jsou nastaveny na hodnoty předané do konstruktoru, bylo uvedeno výše.</span><span class="sxs-lookup"><span data-stu-id="6bd43-178">The height and width of both windows are set to the values passed to the constructor, discussed above.</span></span> <span data-ttu-id="6bd43-179">Tím se zajistí, že velikost okna hostitele a ovládací prvek je stejné jako vyhrazené oblasti na stránce.</span><span class="sxs-lookup"><span data-stu-id="6bd43-179">This ensures that the size of the host window and control is identical to the reserved area on the page.</span></span>  <span data-ttu-id="6bd43-180">Po vytvoření okna vrátí vzorek <xref:System.Runtime.InteropServices.HandleRef> objekt, který obsahuje HWND okno hostitele.</span><span class="sxs-lookup"><span data-stu-id="6bd43-180">After the windows are created, the sample returns a <xref:System.Runtime.InteropServices.HandleRef> object that contains the HWND of the host window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcore)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcore)]  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcorehelper)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcorehelper)]  
  
<a name="destroywindow_wndproc"></a>   
### <a name="implement-destroywindow-and-wndproc"></a><span data-ttu-id="6bd43-181">Destroywindow – implementace a WndProc</span><span class="sxs-lookup"><span data-stu-id="6bd43-181">Implement DestroyWindow and WndProc</span></span>  
 <span data-ttu-id="6bd43-182">Kromě <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>, musí také přepsat <xref:System.Windows.Interop.HwndHost.WndProc%2A> a <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> metody <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="6bd43-182">In addition to <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>, you must also override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> methods of the <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="6bd43-183">V tomto příkladu jsou zpracovávány zprávy pro ovládací prvek <xref:System.Windows.Interop.HwndHost.MessageHook> obslužné rutiny, takže provádění <xref:System.Windows.Interop.HwndHost.WndProc%2A> a <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> je minimální.</span><span class="sxs-lookup"><span data-stu-id="6bd43-183">In this example, the messages for the control are handled by the <xref:System.Windows.Interop.HwndHost.MessageHook> handler, thus the implementation of <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> is minimal.</span></span> <span data-ttu-id="6bd43-184">V případě třídy <xref:System.Windows.Interop.HwndHost.WndProc%2A>, nastavte `handled` k `false` do značí, že zpráva nebyla zpracována a vrátí 0.</span><span class="sxs-lookup"><span data-stu-id="6bd43-184">In the case of <xref:System.Windows.Interop.HwndHost.WndProc%2A>, set `handled` to `false` to indicate that the  message was not handled and return 0.</span></span> <span data-ttu-id="6bd43-185">Pro <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>, jednoduše odstranit okno.</span><span class="sxs-lookup"><span data-stu-id="6bd43-185">For <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>, simply destroy the window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroy)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroy)]  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroyhelper)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroyhelper)]  
  
<a name="host_the_control"></a>   
## <a name="host-the-control-on-the-page"></a><span data-ttu-id="6bd43-186">Hostování ovládacího prvku na stránce</span><span class="sxs-lookup"><span data-stu-id="6bd43-186">Host the Control on the Page</span></span>  
 <span data-ttu-id="6bd43-187">K hostování ovládacího prvku na stránce, nejprve vytvoříte novou instanci třídy `ControlHost` třídy.</span><span class="sxs-lookup"><span data-stu-id="6bd43-187">To host the control on the page, you first create a new instance of the `ControlHost` class.</span></span> <span data-ttu-id="6bd43-188">Předejte výšku a šířku ohraničení prvku, který obsahuje ovládací prvek (`ControlHostElement`) k `ControlHost` konstruktoru.</span><span class="sxs-lookup"><span data-stu-id="6bd43-188">Pass the height and width of the border element that contains the control (`ControlHostElement`) to the `ControlHost` constructor.</span></span> <span data-ttu-id="6bd43-189">Tím se zajistí, že objekt ListBox správnou velikost.</span><span class="sxs-lookup"><span data-stu-id="6bd43-189">This ensures that the ListBox is sized correctly.</span></span> <span data-ttu-id="6bd43-190">Potom hostovat ovládací prvek na stránce pomocí přiřazení `ControlHost` objektu <xref:System.Windows.Controls.Decorator.Child%2A> vlastnost hostitele <xref:System.Windows.Controls.Border>.</span><span class="sxs-lookup"><span data-stu-id="6bd43-190">You then host the control on the page by assigning the `ControlHost` object to the <xref:System.Windows.Controls.Decorator.Child%2A> property of the host <xref:System.Windows.Controls.Border>.</span></span>  
  
 <span data-ttu-id="6bd43-191">Ukázka připojí obslužnou rutinu pro <xref:System.Windows.Interop.HwndHost.MessageHook> událost `ControlHost` pro příjem zpráv z ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="6bd43-191">The sample attaches a handler to the <xref:System.Windows.Interop.HwndHost.MessageHook> event of the `ControlHost` to receive messages from the control.</span></span> <span data-ttu-id="6bd43-192">Tato událost je aktivována pro všechny zprávy odeslané do okna prostředí.</span><span class="sxs-lookup"><span data-stu-id="6bd43-192">This event is raised for every message sent to the hosted window.</span></span> <span data-ttu-id="6bd43-193">V tomto případě jsou tyto zprávy odeslané do okna, která obaluje skutečný ListBox – ovládací prvek, včetně oznámení z ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="6bd43-193">In this case, these are the messages sent to window that wraps the actual ListBox control, including notifications from the control.</span></span> <span data-ttu-id="6bd43-194">Ukázka volá SendMessage k získání informací z ovládacího prvku a upravte jeho obsah.</span><span class="sxs-lookup"><span data-stu-id="6bd43-194">The sample calls SendMessage to obtain information from the control and modify its contents.</span></span> <span data-ttu-id="6bd43-195">V další části jsou popsány podrobnosti o tom, jak na stránce komunikuje s ovládacím prvkem.</span><span class="sxs-lookup"><span data-stu-id="6bd43-195">The details of how the page communicates with the control are discussed in the next section.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="6bd43-196">Všimněte si, že existují dvě Deklarace SendMessage PInvoke.</span><span class="sxs-lookup"><span data-stu-id="6bd43-196">Notice that there are two PInvoke declarations for SendMessage.</span></span> <span data-ttu-id="6bd43-197">To je nezbytné, protože jeden používá `wParam` předat řetězec a druhý parametr se používá k předání celé číslo.</span><span class="sxs-lookup"><span data-stu-id="6bd43-197">This is necessary because one uses the `wParam` parameter to pass a string and the other uses it to pass an integer.</span></span> <span data-ttu-id="6bd43-198">Budete potřebovat samostatné prohlášení pro každý podpis k zajištění, že je správně zařazovat data.</span><span class="sxs-lookup"><span data-stu-id="6bd43-198">You need a separate declaration for each signature to ensure that the data is marshaled correctly.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#hostwindowclass)]
 [!code-vb[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#hostwindowclass)]  
  
 [!code-csharp[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#controlmsgfiltersendmessage)]
 [!code-vb[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#controlmsgfiltersendmessage)]  
  
<a name="communication"></a>   
## <a name="implement-communication-between-the-control-and-the-page"></a><span data-ttu-id="6bd43-199">Implementace komunikace mezi ovládacím prvkem a na stránce</span><span class="sxs-lookup"><span data-stu-id="6bd43-199">Implement Communication Between the Control and the Page</span></span>  
 <span data-ttu-id="6bd43-200">Manipulovat s prvkem odesíláním zpráv Windows.</span><span class="sxs-lookup"><span data-stu-id="6bd43-200">You manipulate the control by sending it Windows messages.</span></span> <span data-ttu-id="6bd43-201">Ovládací prvek vás upozorní, když uživatel pracuje s jejich odesláním oznámení na jeho hostitelské okno.</span><span class="sxs-lookup"><span data-stu-id="6bd43-201">The control notifies you when the user interacts with it by sending notifications to its host window.</span></span> <span data-ttu-id="6bd43-202">[Hostování ovládacího prvku ListBox Win32 v subsystému WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control) ukázka zahrnuje uživatelské rozhraní, které obsahuje několik příkladů toho, jak to funguje:</span><span class="sxs-lookup"><span data-stu-id="6bd43-202">The [Hosting a Win32 ListBox Control in WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control) sample includes a UI that provides several examples of how this works:</span></span>  
  
-   <span data-ttu-id="6bd43-203">Připojte položku do seznamu.</span><span class="sxs-lookup"><span data-stu-id="6bd43-203">Append an item to the list.</span></span>  
  
-   <span data-ttu-id="6bd43-204">Odstranit vybranou položku ze seznamu</span><span class="sxs-lookup"><span data-stu-id="6bd43-204">Delete the selected item from the list</span></span>  
  
-   <span data-ttu-id="6bd43-205">Zobrazte text vybrané položky.</span><span class="sxs-lookup"><span data-stu-id="6bd43-205">Display the text of the currently selected item.</span></span>  
  
-   <span data-ttu-id="6bd43-206">Zobrazte počet položek v seznamu.</span><span class="sxs-lookup"><span data-stu-id="6bd43-206">Display the number of items in the list.</span></span>  
  
 <span data-ttu-id="6bd43-207">Uživatele můžete také vybrat položku v seznamu kliknutím na ni, stejně jako běžné aplikace Win32.</span><span class="sxs-lookup"><span data-stu-id="6bd43-207">The user can also select an item in the list box by clicking on it, just as they would for a conventional Win32 application.</span></span> <span data-ttu-id="6bd43-208">Zobrazená data se aktualizují pokaždé, když uživatel změní stav pole se seznamem vyberete, přidáte nebo přidávání položky.</span><span class="sxs-lookup"><span data-stu-id="6bd43-208">The displayed data is updated each time the user changes the state of the list box by selecting, adding, or appending an item.</span></span>  
  
 <span data-ttu-id="6bd43-209">Připojit položky odeslat pole se seznamem [ `LB_ADDSTRING` zpráva](/windows/desktop/Controls/lb-addstring).</span><span class="sxs-lookup"><span data-stu-id="6bd43-209">To append items, send the list box an [`LB_ADDSTRING` message](/windows/desktop/Controls/lb-addstring).</span></span> <span data-ttu-id="6bd43-210">Odstranit položky odeslat [ `LB_GETCURSEL` ](/windows/desktop/Controls/lb-getcursel) získat index aktuálního výběru a potom [ `LB_DELETESTRING` ](/windows/desktop/Controls/lb-deletestring) odstranit položku.</span><span class="sxs-lookup"><span data-stu-id="6bd43-210">To delete items, send [`LB_GETCURSEL`](/windows/desktop/Controls/lb-getcursel) to get the index of the current selection and then [`LB_DELETESTRING`](/windows/desktop/Controls/lb-deletestring) to delete the item.</span></span> <span data-ttu-id="6bd43-211">Ukázka rovněž odesílá [ `LB_GETCOUNT` ](/windows/desktop/Controls/lb-getcount)a aktualizovat zobrazení, který zobrazuje počet položek, které používá vrácené hodnoty.</span><span class="sxs-lookup"><span data-stu-id="6bd43-211">The sample also sends [`LB_GETCOUNT`](/windows/desktop/Controls/lb-getcount), and uses the returned value to update the display that shows the number of items.</span></span> <span data-ttu-id="6bd43-212">Obě tyto instance [ `SendMessage` ](/windows/desktop/api/winuser/nf-winuser-sendmessage) použijte jednu z deklarace PInvoke popsané v předchozí části.</span><span class="sxs-lookup"><span data-stu-id="6bd43-212">Both these instances of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) use one of the PInvoke declarations discussed in the previous section.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#appenddeletetext)]
 [!code-vb[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#appenddeletetext)]  
  
 <span data-ttu-id="6bd43-213">Když uživatel vybere položku nebo mění jejich výběr, ovládací prvek upozorní okno hostitele a odeslat ho [ `WM_COMMAND` zprávy](/windows/desktop/menurc/wm-command), která vyvolává <xref:System.Windows.Interop.HwndHost.MessageHook> událostí stránky.</span><span class="sxs-lookup"><span data-stu-id="6bd43-213">When the user selects an item or changes their selection, the control notifies the host window by sending it a [`WM_COMMAND` message](/windows/desktop/menurc/wm-command), which raises the <xref:System.Windows.Interop.HwndHost.MessageHook> event for the page.</span></span> <span data-ttu-id="6bd43-214">Obslužná rutina obdrží stejné informace jako hlavní okno procedury okna hostitele.</span><span class="sxs-lookup"><span data-stu-id="6bd43-214">The handler receives the same information as the main window procedure of the host window.</span></span> <span data-ttu-id="6bd43-215">Také předá odkazem na hodnotu typu Boolean `handled`.</span><span class="sxs-lookup"><span data-stu-id="6bd43-215">It also passes a reference to a Boolean value, `handled`.</span></span> <span data-ttu-id="6bd43-216">Nastavíte `handled` k `true` k označení, že mají zpracovat zprávu a je potřeba žádné další zpracování.</span><span class="sxs-lookup"><span data-stu-id="6bd43-216">You set `handled` to `true` to indicate that you have handled the message and no further processing is needed.</span></span>  
  
 <span data-ttu-id="6bd43-217">[`WM_COMMAND`](/windows/desktop/menurc/wm-command) pro celou řadu důvodů se odešle, tak, aby musí zkontrolovat ID oznámení k určení, zda je událost, kterou chcete zpracovat.</span><span class="sxs-lookup"><span data-stu-id="6bd43-217">[`WM_COMMAND`](/windows/desktop/menurc/wm-command) is sent for a variety of reasons, so you must examine the notification ID to determine whether it is an event that you wish to handle.</span></span> <span data-ttu-id="6bd43-218">ID je součástí vysokou slova `wParam` parametru.</span><span class="sxs-lookup"><span data-stu-id="6bd43-218">The ID is contained in the high word of the `wParam` parameter.</span></span> <span data-ttu-id="6bd43-219">Ukázka používá bitové operátory k extrakci ID.</span><span class="sxs-lookup"><span data-stu-id="6bd43-219">The sample uses bitwise operators to extract the ID.</span></span> <span data-ttu-id="6bd43-220">Pokud uživatel provedené nebo změnit jejich výběr, ID bude [ `LBN_SELCHANGE` ](/windows/desktop/Controls/lbn-selchange).</span><span class="sxs-lookup"><span data-stu-id="6bd43-220">If the user has made or changed their selection, the ID will be [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange).</span></span>  
  
 <span data-ttu-id="6bd43-221">Když [ `LBN_SELCHANGE` ](/windows/desktop/Controls/lbn-selchange) je přijata, ukázka získá index vybrané položky odesláním ovládacího prvku [ `LB_GETCURSEL` zpráva](/windows/desktop/Controls/lb-getcursel).</span><span class="sxs-lookup"><span data-stu-id="6bd43-221">When [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange) is received, the sample gets the index of the selected item by sending the control a [`LB_GETCURSEL` message](/windows/desktop/Controls/lb-getcursel).</span></span> <span data-ttu-id="6bd43-222">Pokud chcete získat text, je třeba nejprve vytvořit <xref:System.Text.StringBuilder>.</span><span class="sxs-lookup"><span data-stu-id="6bd43-222">To get the text, you first create a <xref:System.Text.StringBuilder>.</span></span> <span data-ttu-id="6bd43-223">Poté pošlete ovládacího prvku [ `LB_GETTEXT` zpráva](/windows/desktop/Controls/lb-gettext).</span><span class="sxs-lookup"><span data-stu-id="6bd43-223">You then send the control an [`LB_GETTEXT` message](/windows/desktop/Controls/lb-gettext).</span></span> <span data-ttu-id="6bd43-224">Předat prázdnou <xref:System.Text.StringBuilder> objektu jako `wParam` parametru.</span><span class="sxs-lookup"><span data-stu-id="6bd43-224">Pass the empty <xref:System.Text.StringBuilder> object as the `wParam` parameter.</span></span> <span data-ttu-id="6bd43-225">Když [ `SendMessage` ](/windows/desktop/api/winuser/nf-winuser-sendmessage) návratu <xref:System.Text.StringBuilder> bude obsahovat text vybrané položky.</span><span class="sxs-lookup"><span data-stu-id="6bd43-225">When [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) returns, the <xref:System.Text.StringBuilder> will contain the text of the selected item.</span></span> <span data-ttu-id="6bd43-226">Toto použití [ `SendMessage` ](/windows/desktop/api/winuser/nf-winuser-sendmessage) vyžaduje další deklarace PInvoke.</span><span class="sxs-lookup"><span data-stu-id="6bd43-226">This use of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) requires yet another PInvoke declaration.</span></span>  
  
 <span data-ttu-id="6bd43-227">Nakonec nastavte `handled` k `true` k označení, že zpráva byla zpracována.</span><span class="sxs-lookup"><span data-stu-id="6bd43-227">Finally, set `handled` to `true` to indicate that the message has been handled.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="6bd43-228">Viz také:</span><span class="sxs-lookup"><span data-stu-id="6bd43-228">See also</span></span>

- <xref:System.Windows.Interop.HwndHost>
- [<span data-ttu-id="6bd43-229">Vzájemná spolupráce grafického subsystému WPF a systému Win32</span><span class="sxs-lookup"><span data-stu-id="6bd43-229">WPF and Win32 Interoperation</span></span>](wpf-and-win32-interoperation.md)
- [<span data-ttu-id="6bd43-230">Návod: Moje první desktopová aplikace WPF</span><span class="sxs-lookup"><span data-stu-id="6bd43-230">Walkthrough: My first WPF desktop application</span></span>](../getting-started/walkthrough-my-first-wpf-desktop-application.md)
