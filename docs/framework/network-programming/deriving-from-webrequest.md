---
title: Odvození ze žádosti WebRequest
ms.date: 03/30/2017
helpviewer_keywords:
- WebRequest class, pluggable protocols
- protocol-specific request handler
- sending data, pluggable protocols
- pluggable protocols, class criteria
- Internet, pluggable protocols
- receiving data, pluggable protocols
- protocols, pluggable
ms.assetid: 9810c177-973e-43d7-823c-14960bd625ea
ms.openlocfilehash: 6bee864f8d24076d16f226c29d61801e856739d9
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/15/2020
ms.locfileid: "71048603"
---
# <a name="deriving-from-webrequest"></a><span data-ttu-id="f9713-102">Odvození ze žádosti WebRequest</span><span class="sxs-lookup"><span data-stu-id="f9713-102">Deriving from WebRequest</span></span>
<span data-ttu-id="f9713-103">Třída <xref:System.Net.WebRequest> je abstraktní základní třída, která poskytuje základní metody a vlastnosti pro vytvoření obslužné rutiny požadavku specifické pro protokol, která odpovídá modelu protokolu připojitelného rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="f9713-103">The <xref:System.Net.WebRequest> class is an abstract base class that provides the basic methods and properties for creating a protocol-specific request handler that fits the .NET Framework pluggable protocol model.</span></span> <span data-ttu-id="f9713-104">Aplikace, které používají třídu **WebRequest,** mohou požadovat data pomocí libovolného podporovaného protokolu bez nutnosti zadat použitý protokol.</span><span class="sxs-lookup"><span data-stu-id="f9713-104">Applications that use the **WebRequest** class can request data using any supported protocol without needing to specify the protocol used.</span></span>  
  
 <span data-ttu-id="f9713-105">Aby byla třída specifická pro protokol použita jako připojitelný protokol, musí být <xref:System.Net.IWebRequestCreate> splněna dvě kritéria: <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> Třída musí implementovat rozhraní a musí se zaregistrovat metodou.</span><span class="sxs-lookup"><span data-stu-id="f9713-105">Two criteria must be met in order for a protocol-specific class to be used as a pluggable protocol: The class must implement the <xref:System.Net.IWebRequestCreate> interface, and it must register with the <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="f9713-106">Třída musí přepsat všechny abstraktní metody a vlastnosti **WebRequest** poskytnout připojitelné rozhraní.</span><span class="sxs-lookup"><span data-stu-id="f9713-106">The class must override all the abstract methods and properties of **WebRequest** to provide the pluggable interface.</span></span>  
  
 <span data-ttu-id="f9713-107">**Instance WebRequest** jsou určeny pro jednorázové použití. Pokud chcete podat další požadavek, vytvořte nový **WebRequest**.</span><span class="sxs-lookup"><span data-stu-id="f9713-107">**WebRequest** instances are intended for one-time use; if you want to make another request, create a new **WebRequest**.</span></span> <span data-ttu-id="f9713-108">**WebRequest** podporuje <xref:System.Runtime.Serialization.ISerializable> rozhraní, které vývojářům umožňuje serializovat šablonu **WebRequest** a poté rekonstruovat šablonu pro další požadavky.</span><span class="sxs-lookup"><span data-stu-id="f9713-108">**WebRequest** supports the <xref:System.Runtime.Serialization.ISerializable> interface to enable developers to serialize a template **WebRequest** and then reconstruct the template for additional requests.</span></span>  
  
## <a name="iwebrequest-create-method"></a><span data-ttu-id="f9713-109">Metoda vytvoření iWebRequest</span><span class="sxs-lookup"><span data-stu-id="f9713-109">IWebRequest Create Method</span></span>  
 <span data-ttu-id="f9713-110">Metoda <xref:System.Net.IWebRequestCreate.Create%2A> je zodpovědná za inicializaci nové instance třídy specifické pro protokol.</span><span class="sxs-lookup"><span data-stu-id="f9713-110">The <xref:System.Net.IWebRequestCreate.Create%2A> method is responsible for initializing a new instance of the protocol-specific class.</span></span> <span data-ttu-id="f9713-111">Při vytvoření nového požadavku <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> **WebRequest** odpovídá metoda požadovanému identifikátoru URI s předčíslími URI registrovanými metodou **RegisterPrefix.**</span><span class="sxs-lookup"><span data-stu-id="f9713-111">When a new **WebRequest** is created, the <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> method matches the requested URI with the URI prefixes registered with the **RegisterPrefix** method.</span></span> <span data-ttu-id="f9713-112">Create **Create** Metoda správného potomka specifického pro protokol musí vrátit inicializovanou instanci následníka schopného provádět standardní transakci požadavku a odpovědi pro protokol bez nutnosti změny polí specifických pro protokol.</span><span class="sxs-lookup"><span data-stu-id="f9713-112">The **Create** method of the proper protocol-specific descendant must return an initialized instance of the descendant capable of performing a standard request/response transaction for the protocol without needing any protocol-specific fields modified.</span></span>  
  
## <a name="connectiongroupname-property"></a><span data-ttu-id="f9713-113">Vlastnost ConnectionGroupName</span><span class="sxs-lookup"><span data-stu-id="f9713-113">ConnectionGroupName Property</span></span>  
 <span data-ttu-id="f9713-114">Vlastnost <xref:System.Net.WebRequest.ConnectionGroupName%2A> se používá k pojmenování skupiny připojení k prostředku tak, aby více požadavků lze provést přes jedno připojení.</span><span class="sxs-lookup"><span data-stu-id="f9713-114">The <xref:System.Net.WebRequest.ConnectionGroupName%2A> property is used to name a group of connections to a resource so that multiple requests can be made over a single connection.</span></span> <span data-ttu-id="f9713-115">Chcete-li implementovat sdílení připojení, musíte použít metodu sdružování a přiřazování připojení specifickou pro protokol.</span><span class="sxs-lookup"><span data-stu-id="f9713-115">To implement connection-sharing, you must use a protocol-specific method of pooling and assigning connections.</span></span> <span data-ttu-id="f9713-116">Například za předpokladu, že <xref:System.Net.ServicePointManager> třída <xref:System.Net.HttpWebRequest> implementuje sdílení připojení pro třídu.</span><span class="sxs-lookup"><span data-stu-id="f9713-116">For example, the provided <xref:System.Net.ServicePointManager> class implements connection sharing for the <xref:System.Net.HttpWebRequest> class.</span></span> <span data-ttu-id="f9713-117">Třída **ServicePointManager** vytvoří <xref:System.Net.ServicePoint> připojení k určitému serveru pro každou skupinu připojení.</span><span class="sxs-lookup"><span data-stu-id="f9713-117">The **ServicePointManager** class creates a <xref:System.Net.ServicePoint> that provides a connection to a specific server for each connection group.</span></span>  
  
## <a name="contentlength-property"></a><span data-ttu-id="f9713-118">Vlastnost ContentLength</span><span class="sxs-lookup"><span data-stu-id="f9713-118">ContentLength Property</span></span>  
 <span data-ttu-id="f9713-119">Vlastnost <xref:System.Net.WebRequest.ContentLength%2A> určuje počet bajtů dat, která budou odeslána na server při nahrávání dat.</span><span class="sxs-lookup"><span data-stu-id="f9713-119">The <xref:System.Net.WebRequest.ContentLength%2A> property specifies the number of bytes of data that will be sent to the server when uploading data.</span></span>  
  
 <span data-ttu-id="f9713-120">Vlastnost <xref:System.Net.WebRequest.Method%2A> musí být obvykle nastavena tak, aby označovala, že nahrávání probíhá, když je vlastnost **ContentLength** nastavena na hodnotu větší než nula.</span><span class="sxs-lookup"><span data-stu-id="f9713-120">Typically the <xref:System.Net.WebRequest.Method%2A> property must be set to indicate that an upload is taking place when the **ContentLength** property is set to a value greater than zero.</span></span>  
  
## <a name="contenttype-property"></a><span data-ttu-id="f9713-121">Vlastnost ContentType</span><span class="sxs-lookup"><span data-stu-id="f9713-121">ContentType Property</span></span>  
 <span data-ttu-id="f9713-122">Tato <xref:System.Net.WebRequest.ContentType%2A> vlastnost poskytuje všechny zvláštní informace, které protokol vyžaduje, abyste odeslali na server k identifikaci typu odesílaného obsahu.</span><span class="sxs-lookup"><span data-stu-id="f9713-122">The <xref:System.Net.WebRequest.ContentType%2A> property provides any special information that your protocol requires you to send to the server to identify the type of content that you are sending.</span></span> <span data-ttu-id="f9713-123">Obvykle se jedná o typ obsahu MIME všech nahraných dat.</span><span class="sxs-lookup"><span data-stu-id="f9713-123">Typically this is the MIME content type of any data uploaded.</span></span>  
  
## <a name="credentials-property"></a><span data-ttu-id="f9713-124">Vlastnost pověření</span><span class="sxs-lookup"><span data-stu-id="f9713-124">Credentials Property</span></span>  
 <span data-ttu-id="f9713-125">Vlastnost <xref:System.Net.WebRequest.Credentials%2A> obsahuje informace potřebné k ověření požadavku na serveru.</span><span class="sxs-lookup"><span data-stu-id="f9713-125">The <xref:System.Net.WebRequest.Credentials%2A> property contains information needed to authenticate the request with the server.</span></span> <span data-ttu-id="f9713-126">Je nutné implementovat podrobnosti procesu ověřování pro váš protokol.</span><span class="sxs-lookup"><span data-stu-id="f9713-126">You must implement the details of the authentication process for your protocol.</span></span> <span data-ttu-id="f9713-127">Třída <xref:System.Net.AuthenticationManager> je zodpovědná za ověřování požadavků a poskytování ověřovacího tokenu.</span><span class="sxs-lookup"><span data-stu-id="f9713-127">The <xref:System.Net.AuthenticationManager> class is responsible for authenticating requests and providing an authentication token.</span></span> <span data-ttu-id="f9713-128">Třída, která poskytuje pověření používaná protokolem, musí implementovat <xref:System.Net.ICredentials> rozhraní.</span><span class="sxs-lookup"><span data-stu-id="f9713-128">The class that provides the credentials used by your protocol must implement the <xref:System.Net.ICredentials> interface.</span></span>  
  
## <a name="headers-property"></a><span data-ttu-id="f9713-129">Vlastnost záhlaví</span><span class="sxs-lookup"><span data-stu-id="f9713-129">Headers Property</span></span>  
 <span data-ttu-id="f9713-130">Vlastnost <xref:System.Net.WebRequest.Headers%2A> obsahuje libovolnou kolekci párů názvů a hodnot metadat přidružených k požadavku.</span><span class="sxs-lookup"><span data-stu-id="f9713-130">The <xref:System.Net.WebRequest.Headers%2A> property contains an arbitrary collection of name/value pairs of metadata associated with the request.</span></span> <span data-ttu-id="f9713-131">Všechna metadata potřebná protokolem, která mohou být vyjádřena jako dvojice název/hodnota, mohou být zahrnuta do **vlastnosti Headers.**</span><span class="sxs-lookup"><span data-stu-id="f9713-131">Any metadata needed by the protocol that can be expressed as a name/value pair can be included in the **Headers** property.</span></span> <span data-ttu-id="f9713-132">Obvykle tyto informace musí být <xref:System.Net.WebRequest.GetRequestStream%2A> nastaveny před voláním nebo <xref:System.Net.WebRequest.GetResponse%2A> metody; po vytvoření žádosti jsou metadata považována za jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="f9713-132">Typically this information must be set before calling the <xref:System.Net.WebRequest.GetRequestStream%2A> or <xref:System.Net.WebRequest.GetResponse%2A> methods; once the request has been made, the metadata is considered read-only.</span></span>  
  
 <span data-ttu-id="f9713-133">Není nutné používat **Headers** vlastnost používat metadata záhlaví.</span><span class="sxs-lookup"><span data-stu-id="f9713-133">You are not required to use the **Headers** property to use header metadata.</span></span> <span data-ttu-id="f9713-134">Metadata specifická pro protokol mohou být vystavena jako vlastnosti; <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> například vlastnost zpřístupňuje hlavičku HTTP **user-agenta.**</span><span class="sxs-lookup"><span data-stu-id="f9713-134">Protocol-specific metadata can be exposed as properties; for example, the <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> property exposes the **User-Agent** HTTP header.</span></span> <span data-ttu-id="f9713-135">Když zveřejňujete metadata záhlaví jako vlastnost, neměli byste povolit stejnou vlastnost, která má být nastavena pomocí **headers** vlastnost.</span><span class="sxs-lookup"><span data-stu-id="f9713-135">When you expose header metadata as a property, you should not allow the same property to be set using the **Headers** property.</span></span>  
  
## <a name="method-property"></a><span data-ttu-id="f9713-136">Vlastnost metody</span><span class="sxs-lookup"><span data-stu-id="f9713-136">Method Property</span></span>  
 <span data-ttu-id="f9713-137">Vlastnost <xref:System.Net.WebRequest.Method%2A> obsahuje příkaz nebo akci, o kterou požadavek žádá server.</span><span class="sxs-lookup"><span data-stu-id="f9713-137">The <xref:System.Net.WebRequest.Method%2A> property contains the verb or action that the request is asking the server to perform.</span></span> <span data-ttu-id="f9713-138">Výchozí hodnota vlastnosti **Method** musí povolit standardní akci požadavku a odpovědi bez nutnosti nastavení vlastností specifických pro protokol.</span><span class="sxs-lookup"><span data-stu-id="f9713-138">The default for the **Method** property must enable a standard request/response action without requiring any protocol-specific properties to be set.</span></span> <span data-ttu-id="f9713-139">Například <xref:System.Net.HttpWebResponse.Method%2A> metoda výchozí GET, který požaduje prostředek z webového serveru a vrátí odpověď.</span><span class="sxs-lookup"><span data-stu-id="f9713-139">For example, the <xref:System.Net.HttpWebResponse.Method%2A> method defaults to GET, which requests a resource from a Web server and returns the response.</span></span>  
  
 <span data-ttu-id="f9713-140">Vlastnost **ContentLength** musí být obvykle nastavena na hodnotu větší než nula, pokud je vlastnost **Method** nastavena na příkaz ové nebo akce, která označuje, že dochází k nahrávání.</span><span class="sxs-lookup"><span data-stu-id="f9713-140">Typically the **ContentLength** property must be set to a value greater than zero when the **Method** property is set to a verb or action that indicates that an upload is taking place.</span></span>  
  
## <a name="preauthenticate-property"></a><span data-ttu-id="f9713-141">Vlastnost Předběžné ověření</span><span class="sxs-lookup"><span data-stu-id="f9713-141">PreAuthenticate Property</span></span>  
 <span data-ttu-id="f9713-142">Aplikace nastavit <xref:System.Net.WebRequest.PreAuthenticate%2A> vlastnost označující, že informace o ověřování by měly být odesílány s počáteční požadavek spíše než čekání na výzvu ověřování.</span><span class="sxs-lookup"><span data-stu-id="f9713-142">Applications set the <xref:System.Net.WebRequest.PreAuthenticate%2A> property to indicate that authentication information should be sent with the initial request rather than waiting for an authentication challenge.</span></span> <span data-ttu-id="f9713-143">Vlastnost **PreAuthenticate** je smysluplná pouze v případě, že protokol podporuje ověřovací pověření odeslaná s počátečním požadavkem.</span><span class="sxs-lookup"><span data-stu-id="f9713-143">The **PreAuthenticate** property is only meaningful if the protocol supports authentication credentials sent with the initial request.</span></span>  
  
## <a name="proxy-property"></a><span data-ttu-id="f9713-144">Vlastnost proxy serveru</span><span class="sxs-lookup"><span data-stu-id="f9713-144">Proxy Property</span></span>  
 <span data-ttu-id="f9713-145">Vlastnost <xref:System.Net.WebRequest.Proxy%2A> obsahuje <xref:System.Net.IWebProxy> rozhraní, které se používá pro přístup k požadovanému prostředku.</span><span class="sxs-lookup"><span data-stu-id="f9713-145">The <xref:System.Net.WebRequest.Proxy%2A> property contains an <xref:System.Net.IWebProxy> interface that is used to access the requested resource.</span></span> <span data-ttu-id="f9713-146">Vlastnost **Proxy** má smysl pouze v případě, že protokol podporuje proxied požadavky.</span><span class="sxs-lookup"><span data-stu-id="f9713-146">The **Proxy** property is meaningful only if your protocol supports proxied requests.</span></span> <span data-ttu-id="f9713-147">Je nutné nastavit výchozí proxy server, pokud je vyžadován protokolem.</span><span class="sxs-lookup"><span data-stu-id="f9713-147">You must set the default proxy if one is required by your protocol.</span></span>  
  
 <span data-ttu-id="f9713-148">V některých prostředích, například za podnikovou bránou firewall, může být protokol vyžadován k použití proxy serveru.</span><span class="sxs-lookup"><span data-stu-id="f9713-148">In some environments, such as behind a corporate firewall, your protocol might be required to use a proxy.</span></span> <span data-ttu-id="f9713-149">V takovém případě je nutné implementovat rozhraní **IWebProxy** k vytvoření proxy třídy, která bude fungovat pro váš protokol.</span><span class="sxs-lookup"><span data-stu-id="f9713-149">In that case, you must implement the **IWebProxy** interface to create a proxy class that will work for your protocol.</span></span>  
  
## <a name="requesturi-property"></a><span data-ttu-id="f9713-150">Vlastnost RequestUri</span><span class="sxs-lookup"><span data-stu-id="f9713-150">RequestUri Property</span></span>  
 <span data-ttu-id="f9713-151">Vlastnost <xref:System.Net.WebRequest.RequestUri%2A> obsahuje identifikátor URI, který byl předán metodě **WebRequest.Create.**</span><span class="sxs-lookup"><span data-stu-id="f9713-151">The <xref:System.Net.WebRequest.RequestUri%2A> property contains the URI that was passed to the **WebRequest.Create** method.</span></span> <span data-ttu-id="f9713-152">Je jen pro čtení a nelze jej změnit, jakmile byl vytvořen **požadavek WebRequest.**</span><span class="sxs-lookup"><span data-stu-id="f9713-152">It is read-only and cannot be changed once the **WebRequest** has been created.</span></span> <span data-ttu-id="f9713-153">Pokud váš protokol podporuje přesměrování, odpověď může pocházet z prostředku identifikovaného jiným identifikátorem URI.</span><span class="sxs-lookup"><span data-stu-id="f9713-153">If your protocol supports redirection, the response can come from a resource identified by a different URI.</span></span> <span data-ttu-id="f9713-154">Pokud potřebujete poskytnout přístup k identifikátoru URI, který odpověděl, je nutné zadat další vlastnost obsahující tento identifikátor URI.</span><span class="sxs-lookup"><span data-stu-id="f9713-154">If you need to provide access to the URI that responded, you must provide an additional property containing that URI.</span></span>  
  
## <a name="timeout-property"></a><span data-ttu-id="f9713-155">Vlastnost časového opovenču</span><span class="sxs-lookup"><span data-stu-id="f9713-155">Timeout Property</span></span>  
 <span data-ttu-id="f9713-156">Vlastnost <xref:System.Net.WebRequest.Timeout%2A> obsahuje dobu v milisekundách, než časový čas požadavku a vyvolá výjimku.</span><span class="sxs-lookup"><span data-stu-id="f9713-156">The <xref:System.Net.WebRequest.Timeout%2A> property contains the length of time, in milliseconds, to wait before the request times out and throws an exception.</span></span> <span data-ttu-id="f9713-157">**Časový čas** se vztahuje pouze na synchronní <xref:System.Net.WebRequest.GetResponse%2A> požadavky provedené s metodou; asynchronní požadavky musí použít <xref:System.Net.WebRequest.Abort%2A> metodu ke zrušení čekající ho požadavku.</span><span class="sxs-lookup"><span data-stu-id="f9713-157">**Timeout** applies only to synchronous requests made with the <xref:System.Net.WebRequest.GetResponse%2A> method; asynchronous requests must use the <xref:System.Net.WebRequest.Abort%2A> method to cancel a pending request.</span></span>  
  
 <span data-ttu-id="f9713-158">Nastavení **Timeout** vlastnost je smysluplné pouze v případě, že třída specifické pro protokol implementuje proces časového opomu.</span><span class="sxs-lookup"><span data-stu-id="f9713-158">Setting the **Timeout** property is meaningful only if the protocol-specific class implements a time-out process.</span></span>  
  
## <a name="abort-method"></a><span data-ttu-id="f9713-159">Abort – metoda</span><span class="sxs-lookup"><span data-stu-id="f9713-159">Abort Method</span></span>  
 <span data-ttu-id="f9713-160">Metoda <xref:System.Net.WebRequest.Abort%2A> zruší čekající asynchronní požadavek na server.</span><span class="sxs-lookup"><span data-stu-id="f9713-160">The <xref:System.Net.WebRequest.Abort%2A> method cancels a pending asynchronous request to a server.</span></span> <span data-ttu-id="f9713-161">Po zrušení požadavku bude volání **GetResponse**, **BeginGetResponse**, **EndGetResponse**, GetStream , **GetGetStream**, **BeginGetRequestStream**nebo **EndGetRequestStream** vyvolá a <xref:System.Net.WebException> s vlastností <xref:System.Net.WebException.Status%2A> nastavenou na <xref:System.Net.WebExceptionStatus>.</span><span class="sxs-lookup"><span data-stu-id="f9713-161">After the request has been canceled, calling **GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**, **BeginGetRequestStream**, or **EndGetRequestStream** will throw a <xref:System.Net.WebException> with the <xref:System.Net.WebException.Status%2A> property set to <xref:System.Net.WebExceptionStatus>.</span></span>  
  
## <a name="begingetrequeststream-and-endgetrequeststream-methods"></a><span data-ttu-id="f9713-162">Metody BeginGetRequestStream a EndGetRequestStream</span><span class="sxs-lookup"><span data-stu-id="f9713-162">BeginGetRequestStream and EndGetRequestStream Methods</span></span>  
 <span data-ttu-id="f9713-163">Metoda <xref:System.Net.WebRequest.BeginGetRequestStream%2A> spustí asynchronní požadavek pro datový proud, který se používá k odeslání dat na server.</span><span class="sxs-lookup"><span data-stu-id="f9713-163">The <xref:System.Net.WebRequest.BeginGetRequestStream%2A> method starts an asynchronous request for the stream that is used to upload data to the server.</span></span> <span data-ttu-id="f9713-164">Metoda <xref:System.Net.WebRequest.EndGetRequestStream%2A> dokončí asynchronní požadavek a vrátí požadovaný datový proud.</span><span class="sxs-lookup"><span data-stu-id="f9713-164">The <xref:System.Net.WebRequest.EndGetRequestStream%2A> method completes the asynchronous request and returns the requested stream.</span></span> <span data-ttu-id="f9713-165">Tyto metody implementují metodu **GetRequestStream** pomocí standardního asynchronního vzoru rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="f9713-165">These methods implement the **GetRequestStream** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="begingetresponse-and-endgetresponse-methods"></a><span data-ttu-id="f9713-166">Metody BeginGetResponse a EndGetResponse</span><span class="sxs-lookup"><span data-stu-id="f9713-166">BeginGetResponse and EndGetResponse Methods</span></span>  
 <span data-ttu-id="f9713-167">Metoda <xref:System.Net.WebRequest.BeginGetResponse%2A> spustí asynchronní požadavek na server.</span><span class="sxs-lookup"><span data-stu-id="f9713-167">The <xref:System.Net.WebRequest.BeginGetResponse%2A> method starts an asynchronous request to a server.</span></span> <span data-ttu-id="f9713-168">Metoda <xref:System.Net.WebRequest.EndGetResponse%2A> dokončí asynchronní požadavek a vrátí požadovanou odpověď.</span><span class="sxs-lookup"><span data-stu-id="f9713-168">The <xref:System.Net.WebRequest.EndGetResponse%2A> method completes the asynchronous request and returns the requested response.</span></span> <span data-ttu-id="f9713-169">Tyto metody implementují metodu **GetResponse** pomocí standardního asynchronního vzoru rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="f9713-169">These methods implement the **GetResponse** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="getrequeststream-method"></a><span data-ttu-id="f9713-170">Metoda GetRequestStream</span><span class="sxs-lookup"><span data-stu-id="f9713-170">GetRequestStream Method</span></span>  
 <span data-ttu-id="f9713-171">Metoda <xref:System.Net.WebRequest.GetRequestStream%2A> vrátí datový proud, který se používá k zápisu dat na požadovaný server.</span><span class="sxs-lookup"><span data-stu-id="f9713-171">The <xref:System.Net.WebRequest.GetRequestStream%2A> method returns a stream that is used to write data to the requested server.</span></span> <span data-ttu-id="f9713-172">Datový proud vrácena by měl být datový proud pouze pro zápis, který nehledá; je určen jako jednosměrný datový proud, který je zapsán na server.</span><span class="sxs-lookup"><span data-stu-id="f9713-172">The stream returned should be a write-only stream that does not seek; it is intended as a one-way stream of data that is written to the server.</span></span> <span data-ttu-id="f9713-173">Datový proud vrátí <xref:System.IO.Stream.CanRead%2A> false <xref:System.IO.Stream.CanSeek%2A> pro vlastnosti <xref:System.IO.Stream.CanWrite%2A> a true pro vlastnost.</span><span class="sxs-lookup"><span data-stu-id="f9713-173">The stream returns false for the <xref:System.IO.Stream.CanRead%2A> and <xref:System.IO.Stream.CanSeek%2A> properties and true for the <xref:System.IO.Stream.CanWrite%2A> property.</span></span>  
  
 <span data-ttu-id="f9713-174">Metoda **GetRequestStream** obvykle otevře připojení k serveru a před vrácením datového proudu odešle informace záhlaví, které označují, že data jsou odesílána na server.</span><span class="sxs-lookup"><span data-stu-id="f9713-174">The **GetRequestStream** method typically opens a connection to the server and, before returning the stream, sends header information that indicates that data is being sent to the server.</span></span> <span data-ttu-id="f9713-175">Vzhledem k tomu, **že GetRequestStream** začíná požadavek, nastavení všech vlastností **záhlaví** nebo **ContentLength** vlastnost je obvykle není povoleno po volání **GetRequestStream**.</span><span class="sxs-lookup"><span data-stu-id="f9713-175">Because **GetRequestStream** begins the request, setting any **Header** properties or the **ContentLength** property is typically not allowed after calling **GetRequestStream**.</span></span>  
  
## <a name="getresponse-method"></a><span data-ttu-id="f9713-176">Metoda getresponse</span><span class="sxs-lookup"><span data-stu-id="f9713-176">GetResponse Method</span></span>  
 <span data-ttu-id="f9713-177">Metoda <xref:System.Net.WebRequest.GetResponse%2A> vrátí potomek <xref:System.Net.WebResponse> třídy specifický pro protokol, který představuje odpověď ze serveru.</span><span class="sxs-lookup"><span data-stu-id="f9713-177">The <xref:System.Net.WebRequest.GetResponse%2A> method returns a protocol-specific descendant of the <xref:System.Net.WebResponse> class that represents the response from the server.</span></span> <span data-ttu-id="f9713-178">Pokud požadavek již byla zahájena **GetRequestStream** metoda, **GetResponse** metoda vytvoří připojení k prostředku identifikované **RequestUri**, odešle informace záhlaví označující typ požadavku, který je podán, a pak obdrží odpověď z prostředku.</span><span class="sxs-lookup"><span data-stu-id="f9713-178">Unless the request has already been initiated by the **GetRequestStream** method, the **GetResponse** method creates a connection to the resource identified by **RequestUri**, sends header information indicating the type of request being made, and then receives the response from the resource.</span></span>  
  
 <span data-ttu-id="f9713-179">Jakmile **getresponse** metoda je volána, všechny vlastnosti by měly být považovány za jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="f9713-179">Once the **GetResponse** method is called, all properties should be considered read-only.</span></span> <span data-ttu-id="f9713-180">**Instance WebRequest** jsou určeny pro jednorázové použití. Pokud chcete podat další požadavek, měli byste vytvořit nový **WebRequest**.</span><span class="sxs-lookup"><span data-stu-id="f9713-180">**WebRequest** instances are intended for one-time use; if you want to make another request, you should create a new **WebRequest**.</span></span>  
  
 <span data-ttu-id="f9713-181">**Metoda GetResponse** je zodpovědná za vytvoření příslušného potomka **WebResponse,** který bude obsahovat příchozí odpověď.</span><span class="sxs-lookup"><span data-stu-id="f9713-181">The **GetResponse** method is responsible for creating an appropriate **WebResponse** descendant to contain the incoming response.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="f9713-182">Viz také</span><span class="sxs-lookup"><span data-stu-id="f9713-182">See also</span></span>

- <xref:System.Net.WebRequest>
- <xref:System.Net.HttpWebRequest>
- <xref:System.Net.FileWebRequest>
- [<span data-ttu-id="f9713-183">Programování připojitelných protokolů</span><span class="sxs-lookup"><span data-stu-id="f9713-183">Programming Pluggable Protocols</span></span>](programming-pluggable-protocols.md)
- [<span data-ttu-id="f9713-184">Odvození z odpovědi WebResponse</span><span class="sxs-lookup"><span data-stu-id="f9713-184">Deriving from WebResponse</span></span>](deriving-from-webresponse.md)
