---
title: Přehled rozšíření značek pro jazyk XAML
ms.date: 03/30/2017
helpviewer_keywords:
- markup extensions [XAML Services], custom
- XAML [XAML Services], markup extensions
ms.assetid: 261b2b11-2dc0-462f-8c66-55b8c9c6e436
ms.openlocfilehash: 41fe3cb368bed12ccb2dbe9bd31f95fd556e3968
ms.sourcegitcommit: 0be8a279af6d8a43e03141e349d3efd5d35f8767
ms.translationtype: HT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/18/2019
ms.locfileid: "59224920"
---
# <a name="markup-extensions-for-xaml-overview"></a><span data-ttu-id="c3698-102">Přehled rozšíření značek pro jazyk XAML</span><span class="sxs-lookup"><span data-stu-id="c3698-102">Markup Extensions for XAML Overview</span></span>
<span data-ttu-id="c3698-103">Rozšíření značek jsou technika XAML pro získání hodnotu, která není na primitivní ani určitého typu XAML.</span><span class="sxs-lookup"><span data-stu-id="c3698-103">Markup extensions are a XAML technique for obtaining a value that is neither a primitive nor a specific XAML type.</span></span> <span data-ttu-id="c3698-104">Pro použití atributu rozšíření značek používat známé znak sekvence otevírající složenou závorku `{` k zadání oboru rozšíření značek a uzavírací složenou závorku `}` ukončíte.</span><span class="sxs-lookup"><span data-stu-id="c3698-104">For attribute usage, markup extensions use the known character sequence of an opening curly brace `{` to enter the markup extension scope, and a closing curly brace `}` to exit.</span></span> <span data-ttu-id="c3698-105">Při použití rozhraní .NET Framework XAML Services, můžete použít některé z předdefinovaných rozšíření značek jazyka XAML v oboru názvů System.Xaml sestavení.</span><span class="sxs-lookup"><span data-stu-id="c3698-105">When using .NET Framework XAML Services, you can use some of the predefined XAML language markup extensions from the System.Xaml assembly.</span></span> <span data-ttu-id="c3698-106">Můžete také jsou podtřídami tříd <xref:System.Windows.Markup.MarkupExtension> třídy definované v oboru názvů System.Xaml a definovat vlastní rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="c3698-106">You can also subclass from the <xref:System.Windows.Markup.MarkupExtension> class, defined in System.Xaml, and define your own markup extensions.</span></span> <span data-ttu-id="c3698-107">Nebo můžete použít rozšíření značek, které jsou definována v určité rozhraní, pokud se už odkazuje na dané rozhraní.</span><span class="sxs-lookup"><span data-stu-id="c3698-107">Or you can use markup extensions defined by a particular framework if you are already referencing that framework .</span></span>  
  
 <span data-ttu-id="c3698-108">Při přístupu k použití rozšíření značky XAML objektu zapisovače mohou poskytovat služby k vlastní <xref:System.Windows.Markup.MarkupExtension> třídu v rámci připojení služby k časovému okamžiku <xref:System.Windows.Markup.MarkupExtension.ProvideValue%2A?displayProperty=nameWithType> přepsat.</span><span class="sxs-lookup"><span data-stu-id="c3698-108">When a markup extension usage is accessed, the XAML object writer can provide services to a custom <xref:System.Windows.Markup.MarkupExtension> class through a service connection point in the <xref:System.Windows.Markup.MarkupExtension.ProvideValue%2A?displayProperty=nameWithType> override.</span></span> <span data-ttu-id="c3698-109">Služby lze získat kontextové informace o využití, specifické možnosti objektu zapisovače, kontext schématu XAML a tak dále.</span><span class="sxs-lookup"><span data-stu-id="c3698-109">The services can be used to obtain context about the usage, specific capabilities of the object writer, XAML schema context, and so on.</span></span>  
  
<a name="XAML_Defined_Markup_Extensions"></a>   
## <a name="xaml-defined-markup-extensions"></a><span data-ttu-id="c3698-110">Rozšíření značek XAML definovaný</span><span class="sxs-lookup"><span data-stu-id="c3698-110">XAML-Defined Markup Extensions</span></span>  
 <span data-ttu-id="c3698-111">Několik rozšíření značek jsou implementované rozhraní .NET Framework XAML Services pro podporu jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="c3698-111">Several markup extensions are implemented by .NET Framework XAML Services for XAML language support.</span></span> <span data-ttu-id="c3698-112">Tyto přípony označení odpovídají částí specifikace XAML jako jazyk.</span><span class="sxs-lookup"><span data-stu-id="c3698-112">These markup extensions correspond to parts of the specification of XAML as a language.</span></span> <span data-ttu-id="c3698-113">Toto jsou obvykle poznáte ho podle `x:` předponu v syntaxi, jak je vidět v běžných použití.</span><span class="sxs-lookup"><span data-stu-id="c3698-113">These are typically identifiable by the `x:` prefix in the syntax as seen in common usage.</span></span> <span data-ttu-id="c3698-114">Implementace rozhraní .NET Framework XAML Services pro tyto prvky jazyka XAML všechny odvozovat <xref:System.Windows.Markup.MarkupExtension> základní třídy.</span><span class="sxs-lookup"><span data-stu-id="c3698-114">The .NET Framework XAML Services implementations for these XAML language elements all derive from the  <xref:System.Windows.Markup.MarkupExtension> base class .</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="c3698-115">`x:` Předpona se používá pro typické oboru názvů XAML mapování oboru názvů jazyka XAML, v kořenový prvek XAML produkční.</span><span class="sxs-lookup"><span data-stu-id="c3698-115">The `x:` prefix is used for the typical XAML namespace mapping of the XAML language namespace, in the root element of a XAML production.</span></span> <span data-ttu-id="c3698-116">Například šablony sady Visual Studio projekt a stránky pro různé architektury, konkrétní inicializovat pomocí tohoto souboru XAML `x:` mapování.</span><span class="sxs-lookup"><span data-stu-id="c3698-116">For example, the Visual Studio project and page templates for various specific frameworks initiate a XAML file using this `x:` mapping.</span></span> <span data-ttu-id="c3698-117">Můžete se rozhodnout token jinou předponu ve vlastním XAML mapování oboru názvů, ale tato dokumentace se předpokládá výchozí `x:` mapování jako způsob určení těchto entit, které jsou definované součástí oboru názvů XAML jazyka XAML, nikoli obor názvů XAML výchozí konkrétní framework nebo jiných libovolného nebo kód XML, CLR oborech názvů.</span><span class="sxs-lookup"><span data-stu-id="c3698-117">You could choose a different prefix token in your own XAML namespace mapping, but this documentation will assume the default `x:` mapping as a means of identifying those entities that are a defined part of the XAML language XAML namespace, as opposed to a specific framework's default XAML namespace or other arbitrary CLR or XML namespaces.</span></span>  
  
### <a name="xtype"></a><span data-ttu-id="c3698-118">x:Type</span><span class="sxs-lookup"><span data-stu-id="c3698-118">x:Type</span></span>  
 <span data-ttu-id="c3698-119">`x:Type` dodává <xref:System.Type> objekt s názvem typu.</span><span class="sxs-lookup"><span data-stu-id="c3698-119">`x:Type` supplies the <xref:System.Type> object for the named type.</span></span> <span data-ttu-id="c3698-120">Tato funkce se nejčastěji používá v odložení mechanismy, které používají základní typ CLR a typ odvození jako zástupný název seskupení nebo identifikátor.</span><span class="sxs-lookup"><span data-stu-id="c3698-120">This functionality is used most frequently in deferral mechanisms that use underlying CLR type and type derivation as a grouping moniker or identifier.</span></span> <span data-ttu-id="c3698-121">WPF styly a šablony a jejich využití `TargetType` vlastnosti, jsou konkrétní příklad.</span><span class="sxs-lookup"><span data-stu-id="c3698-121">WPF styles and templates, and their usage of `TargetType` properties, are a specific example.</span></span> <span data-ttu-id="c3698-122">Další informace najdete v tématu [x: Type – rozšíření značek](x-type-markup-extension.md).</span><span class="sxs-lookup"><span data-stu-id="c3698-122">For more information, see [x:Type Markup Extension](x-type-markup-extension.md).</span></span>  
  
### <a name="xstatic"></a><span data-ttu-id="c3698-123">X:static</span><span class="sxs-lookup"><span data-stu-id="c3698-123">x:Static</span></span>  
 <span data-ttu-id="c3698-124">`x:Static` vytváří statické hodnoty z entity kód typ hodnoty, které nejsou přímo typ hodnoty vlastnosti, ale může být vyhodnocen jako typu.</span><span class="sxs-lookup"><span data-stu-id="c3698-124">`x:Static` produces static values from value-type code entities that are not directly the type of a property's value, but can be evaluated to that type.</span></span> <span data-ttu-id="c3698-125">To je užitečné pro zadání hodnoty, které již existují jako dobře známé konstanty v definici typu.</span><span class="sxs-lookup"><span data-stu-id="c3698-125">This is useful for specifying values that already exist as well-known constants in a type definition.</span></span> <span data-ttu-id="c3698-126">Další informace najdete v tématu [x: Static – rozšíření značek](x-static-markup-extension.md).</span><span class="sxs-lookup"><span data-stu-id="c3698-126">For more information, see [x:Static Markup Extension](x-static-markup-extension.md).</span></span>  
  
### <a name="xnull"></a><span data-ttu-id="c3698-127">x: Null</span><span class="sxs-lookup"><span data-stu-id="c3698-127">x:Null</span></span>  
 <span data-ttu-id="c3698-128">`x:Null` Určuje `null` jako hodnotu pro člena XAML.</span><span class="sxs-lookup"><span data-stu-id="c3698-128">`x:Null` specifies `null` as a value for a XAML member.</span></span> <span data-ttu-id="c3698-129">V závislosti na návrhu konkrétní typy nebo na větší framework koncepty `null` není vždy výchozí hodnotu pro vlastnost nebo implicitní hodnota atributu prázdný řetězec.</span><span class="sxs-lookup"><span data-stu-id="c3698-129">Depending on the design of specific types or on larger framework concepts, `null` is not always a default value for a property, or the implied value of an empty string attribute.</span></span> <span data-ttu-id="c3698-130">Další informace najdete v tématu [x: Null – rozšíření značek](x-null-markup-extension.md).</span><span class="sxs-lookup"><span data-stu-id="c3698-130">For more information, see [x:Null Markup Extension](x-null-markup-extension.md).</span></span>  
  
### <a name="xarray"></a><span data-ttu-id="c3698-131">x: Array</span><span class="sxs-lookup"><span data-stu-id="c3698-131">x:Array</span></span>  
 <span data-ttu-id="c3698-132">`x:Array` podporuje vytváření obecné polí v syntaxe XAML v případech, kdy je záměrně není použita podporu kolekce, které poskytuje základní elementy a modely ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="c3698-132">`x:Array` supports creation of general arrays in XAML syntax in cases where the collection support that is provided by base elements and control models is deliberately not used.</span></span> <span data-ttu-id="c3698-133">Další informace najdete v tématu [x: Array – rozšíření značek](x-array-markup-extension.md).</span><span class="sxs-lookup"><span data-stu-id="c3698-133">For more information, see [x:Array Markup Extension](x-array-markup-extension.md).</span></span> <span data-ttu-id="c3698-134">V XAML 2009 konkrétně pole jsou dostupné jako primitivní místo jako rozšíření.</span><span class="sxs-lookup"><span data-stu-id="c3698-134">In XAML 2009 specifically, arrays are accessed as language primitives instead of as an extension.</span></span> <span data-ttu-id="c3698-135">Další informace najdete v tématu [funkce jazyka XAML 2009](xaml-2009-language-features.md).</span><span class="sxs-lookup"><span data-stu-id="c3698-135">For more information, see [XAML 2009 Language Features](xaml-2009-language-features.md).</span></span>  
  
### <a name="xreference"></a><span data-ttu-id="c3698-136">x: Reference</span><span class="sxs-lookup"><span data-stu-id="c3698-136">x:Reference</span></span>  
 <span data-ttu-id="c3698-137">`x:Reference` je součástí XAML 2009, rozšíření z původní sady language (2006).</span><span class="sxs-lookup"><span data-stu-id="c3698-137">`x:Reference` is part of XAML 2009, an extension of the original (2006) language set.</span></span> <span data-ttu-id="c3698-138">`x:Reference` představuje odkaz na jiný existující objekt v grafu objektů.</span><span class="sxs-lookup"><span data-stu-id="c3698-138">`x:Reference` represents a reference to another existing object in an object graph.</span></span> <span data-ttu-id="c3698-139">Tento objekt je identifikován jeho `x:Name`.</span><span class="sxs-lookup"><span data-stu-id="c3698-139">That object is identified by its `x:Name`.</span></span> <span data-ttu-id="c3698-140">Další informace najdete v tématu [x: Reference – rozšíření značek](x-reference-markup-extension.md).</span><span class="sxs-lookup"><span data-stu-id="c3698-140">For more information, see [x:Reference Markup Extension](x-reference-markup-extension.md).</span></span>  
  
### <a name="other-x-constructs"></a><span data-ttu-id="c3698-141">Další x: Konstrukce</span><span class="sxs-lookup"><span data-stu-id="c3698-141">Other x: Constructs</span></span>  
 <span data-ttu-id="c3698-142">Další `x:` existují konstrukty, které podporují funkce jazyka XAML, ale ty nejsou implementované jako rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="c3698-142">Other `x:` constructs to support XAML language features exist, but these are not implemented as markup extensions.</span></span> <span data-ttu-id="c3698-143">Další informace najdete v tématu [Namespace XAML (x:) Funkce jazyka](xaml-namespace-x-language-features.md).</span><span class="sxs-lookup"><span data-stu-id="c3698-143">For more information, see [XAML Namespace (x:) Language Features](xaml-namespace-x-language-features.md).</span></span>  
  
<a name="the_markupextension_base_class"></a>   
## <a name="the-markupextension-base-class"></a><span data-ttu-id="c3698-144">MarkupExtension základní třída</span><span class="sxs-lookup"><span data-stu-id="c3698-144">The MarkupExtension Base Class</span></span>  
 <span data-ttu-id="c3698-145">Chcete-li definovat rozšíření vlastního kódu, které mohou komunikovat s výchozí implementace XAML čtečky a zapisovače XAML v oboru názvů System.Xaml odvodit třídu z abstraktní <xref:System.Windows.Markup.MarkupExtension> třídy.</span><span class="sxs-lookup"><span data-stu-id="c3698-145">To define a custom markup extension that can interact with the default implementations of XAML readers and XAML writers in System.Xaml, you derive a class from the abstract <xref:System.Windows.Markup.MarkupExtension> class.</span></span> <span data-ttu-id="c3698-146">Že třída má jedna metoda přepsání, která je <xref:System.Windows.Markup.MarkupExtension.ProvideValue%2A>.</span><span class="sxs-lookup"><span data-stu-id="c3698-146">That class has one method to override, which is <xref:System.Windows.Markup.MarkupExtension.ProvideValue%2A>.</span></span> <span data-ttu-id="c3698-147">Budete také muset definovat další konstruktory pro podporu argumenty pro použití rozšíření značky a odpovídající nastavitelné vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="c3698-147">You might also need to define additional constructors to support arguments to the markup extension usage, and matching settable properties.</span></span>  
  
 <span data-ttu-id="c3698-148">Prostřednictvím <xref:System.Windows.Markup.MarkupExtension.ProvideValue%2A>, rozšíření vlastního kódu má přístup do kontextu služby, který bude hlásit prostředí, kde rozšíření značek ve skutečnosti vyvolávat procesoru XAML.</span><span class="sxs-lookup"><span data-stu-id="c3698-148">Through <xref:System.Windows.Markup.MarkupExtension.ProvideValue%2A>, a custom markup extension has access to a service context that reports the environment where the markup extension is actually invoked by a XAML processor.</span></span> <span data-ttu-id="c3698-149">V cestě zatížení obvykle se jedná <xref:System.Xaml.XamlObjectWriter>.</span><span class="sxs-lookup"><span data-stu-id="c3698-149">In the load path this is typically a <xref:System.Xaml.XamlObjectWriter>.</span></span> <span data-ttu-id="c3698-150">V Uložit cestu obvykle se jedná <xref:System.Xaml.XamlXmlWriter>.</span><span class="sxs-lookup"><span data-stu-id="c3698-150">In the save path this is typically a <xref:System.Xaml.XamlXmlWriter>.</span></span> <span data-ttu-id="c3698-151">Každá sestava kontext služby jako interní XAML služby zprostředkovatele kontextu třída, která implementuje vzor poskytovatele služeb.</span><span class="sxs-lookup"><span data-stu-id="c3698-151">Each report the service context as an internal XAML service provider context class that implements a service provider pattern.</span></span> <span data-ttu-id="c3698-152">Další informace o dostupných službách a co představují, najdete v části [převaděče typů a rozšíření značek pro XAML](type-converters-and-markup-extensions-for-xaml.md).</span><span class="sxs-lookup"><span data-stu-id="c3698-152">For more information about the available services and what they represent, see [Type Converters and Markup Extensions for XAML](type-converters-and-markup-extensions-for-xaml.md).</span></span>  
  
 <span data-ttu-id="c3698-153">Vaše třída rozšíření značek musí používat úroveň veřejného přístupu; XAML procesory musí být vždy umožňuje vytvořit instanci pomocná třída rozšíření značek, aby bylo možné používat jeho služeb.</span><span class="sxs-lookup"><span data-stu-id="c3698-153">Your markup extension class must use a public access level; XAML processors must always be able to instantiate the markup extension's support class in order to use its services.</span></span>  
  
<a name="naming_the_support_type"></a>   
## <a name="defining-the-support-type-for-a-custom-markup-extension"></a><span data-ttu-id="c3698-154">Definování typu podporu pro rozšíření vlastního kódu</span><span class="sxs-lookup"><span data-stu-id="c3698-154">Defining the Support Type for a Custom Markup Extension</span></span>  
 <span data-ttu-id="c3698-155">Při použití rozhraní .NET Framework XAML Services nebo rozhraní, které staví na rozhraní .NET Framework XAML Services, máte dvě možnosti, jak zadat název typu podporu rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="c3698-155">When you use .NET Framework XAML Services or frameworks that build on .NET Framework XAML Services, you have two choices for how to name the markup extension support type.</span></span> <span data-ttu-id="c3698-156">Název typu je relevantní pro jak XAML objekt zapisovače pokus o přístup k a má být vyvolán jako typ podpory rozšíření značek při použití rozšíření značky v XAML narazí.</span><span class="sxs-lookup"><span data-stu-id="c3698-156">The type name is relevant to how XAML object writers attempt to access and invoke a markup extension support type when they encounter a markup extension usage in XAML.</span></span> <span data-ttu-id="c3698-157">Použijte jednu z následujících strategií pojmenování:</span><span class="sxs-lookup"><span data-stu-id="c3698-157">Use one of the following naming strategies:</span></span>  
  
-   <span data-ttu-id="c3698-158">Název typu být přesnou shodou token využití značek XAML.</span><span class="sxs-lookup"><span data-stu-id="c3698-158">Name the type name to be an exact match to the XAML markup usage token.</span></span> <span data-ttu-id="c3698-159">Například pro podporu `{Collate ...}` použití rozšíření, název typu podporu `Collate`.</span><span class="sxs-lookup"><span data-stu-id="c3698-159">For example, to support a `{Collate ...}` extension usage, name the support type `Collate`.</span></span>  
  
-   <span data-ttu-id="c3698-160">Název typu jako řetězec s tokenem využití a přípona `Extension`.</span><span class="sxs-lookup"><span data-stu-id="c3698-160">Name the type name to be the usage string token plus the suffix `Extension`.</span></span> <span data-ttu-id="c3698-161">Například pro podporu `{Collate ...}` použití rozšíření, název typu podporu `CollateExtension`.</span><span class="sxs-lookup"><span data-stu-id="c3698-161">For example, to support a `{Collate ...}` extension usage, name the support type `CollateExtension`.</span></span>  
  
 <span data-ttu-id="c3698-162">Pořadí vyhledávání je vás pod rouškou `Extension`-příponami třídy pojmenujte první a potom vyhledejte název třídy bez `Extension` příponu.</span><span class="sxs-lookup"><span data-stu-id="c3698-162">The order of lookup is to look for the `Extension`-suffixed class name first and then look for the class name without the `Extension` suffix.</span></span>  
  
 <span data-ttu-id="c3698-163">Z hlediska využití značek včetně `Extension` přípony jako součást využití je platný.</span><span class="sxs-lookup"><span data-stu-id="c3698-163">From the markup usage perspective, including the `Extension` suffix as part of the usage is valid.</span></span> <span data-ttu-id="c3698-164">Ale to se chová jako kdyby `Extension` je skutečně část názvu třídy a XAML objekt zapisovače selže vyřešíte pomocná třída rozšíření značek tohle využívání pomocná třída nemá `Extension` příponu.</span><span class="sxs-lookup"><span data-stu-id="c3698-164">However, this behaves as if `Extension` is truly part of the class name, and XAML object writers would fail to resolve a markup extension support class for that usage if the support class did not have the `Extension` suffix.</span></span>  
  
### <a name="the-default-constructor"></a><span data-ttu-id="c3698-165">Výchozí konstruktor</span><span class="sxs-lookup"><span data-stu-id="c3698-165">The Default Constructor</span></span>  
 <span data-ttu-id="c3698-166">Všechna rozšíření značek podporují typy, by měly vystavit veřejný výchozí konstruktor.</span><span class="sxs-lookup"><span data-stu-id="c3698-166">For all markup extension support types, you should expose a public default constructor.</span></span> <span data-ttu-id="c3698-167">Výchozí konstruktor je vyžadován pro případ, kdy zapisovače objektu XAML vytváří instanci rozšíření značek z použití elementu objektu.</span><span class="sxs-lookup"><span data-stu-id="c3698-167">A default constructor is required for any case where a XAML object writer instantiates the markup extension from an object element usage.</span></span> <span data-ttu-id="c3698-168">Podpora použití elementu objektu je veletrh očekávání ohledně rozšíření značek, zejména pro serializaci.</span><span class="sxs-lookup"><span data-stu-id="c3698-168">Supporting object element usage is a fair expectation for a markup extension, particularly for serialization.</span></span> <span data-ttu-id="c3698-169">Pokud chcete podporovat použití atributu rozšíření značek, je však implementovat rozšíření značek bez veřejného konstruktoru.</span><span class="sxs-lookup"><span data-stu-id="c3698-169">However, you can implement a markup extension without a public constructor if you only intend to support attribute usages of the markup extension.</span></span>  
  
 <span data-ttu-id="c3698-170">Pokud vaše použití rozšíření značky nemá žádné argumenty, je potřeba podporovat použití výchozího konstruktoru.</span><span class="sxs-lookup"><span data-stu-id="c3698-170">If your markup extension usage has no arguments, the default constructor is required to support usage.</span></span>  
  
<a name="constructor_patterns_and_positional_arguments_for_a_custom_markup_extension"></a>   
## <a name="constructor-patterns-and-positional-arguments-for-a-custom-markup-extension"></a><span data-ttu-id="c3698-171">Zabezpečené vzory konstruktoru a poziční argumenty pro rozšíření vlastního kódu</span><span class="sxs-lookup"><span data-stu-id="c3698-171">Constructor Patterns and Positional Arguments for a Custom Markup Extension</span></span>  
 <span data-ttu-id="c3698-172">Pro rozšíření značek s využitím odpovídající argument veřejné konstruktory musí odpovídat režimy zamýšlené použití.</span><span class="sxs-lookup"><span data-stu-id="c3698-172">For a markup extension with intended argument usage, the public constructors must correspond to the modes of the intended usage.</span></span> <span data-ttu-id="c3698-173">Jinými slovy Pokud vaše – rozšíření značek je navržen tak, aby vyžadovala jedna poziční argument jako platný využití, by měl podporovat veřejný konstruktor s jeden vstupní parametr, který přijímá poziční argument.</span><span class="sxs-lookup"><span data-stu-id="c3698-173">In other words, if your markup extension is designed to require one positional argument as a valid usage, you should support a public constructor with one input parameter that takes the positional argument.</span></span>  
  
 <span data-ttu-id="c3698-174">Předpokládejme například, že `Collate` – rozšíření značek je určené pro podporu pouze režim stanoveno, je-li jeden poziční argument, který představuje jeho režimu, `CollationMode` konstanta výčtu.</span><span class="sxs-lookup"><span data-stu-id="c3698-174">For example, suppose the `Collate` markup extension is intended to support only a mode where there is one positional argument that represents its mode, specified as a `CollationMode` enumeration constant.</span></span> <span data-ttu-id="c3698-175">V takovém případě by měl být konstruktor s následující tvar:</span><span class="sxs-lookup"><span data-stu-id="c3698-175">In this case, there should be a constructor with the following form:</span></span>  
  
```  
public Collate(CollationMode collationMode) {...}  
```  
  
 <span data-ttu-id="c3698-176">Na základní úrovni argumenty předané do rozšíření značek jsou řetězce, protože jsou předávány z hodnot atributů kódu.</span><span class="sxs-lookup"><span data-stu-id="c3698-176">At a basic level, the arguments passed to a markup extension are a string because they are being forwarded from the markup's attribute values.</span></span> <span data-ttu-id="c3698-177">Můžete provést všechny vaše argumenty řetězce a pracovat se vstupem na této úrovni.</span><span class="sxs-lookup"><span data-stu-id="c3698-177">You can make all of your arguments strings and work with input at that level.</span></span> <span data-ttu-id="c3698-178">Ale máte přístup k určité zpracování, který nastal dřív, než jsou předány argumenty přípony označení pomocná třída.</span><span class="sxs-lookup"><span data-stu-id="c3698-178">However, you do have access to certain processing that occurs before the markup extension arguments are passed to the support class.</span></span>  
  
 <span data-ttu-id="c3698-179">Zpracování se koncepčně funguje jakoby rozšíření značek je objekt, který se má vytvořit, a nastavte hodnoty jeho členů.</span><span class="sxs-lookup"><span data-stu-id="c3698-179">The processing works conceptually as if the markup extension is an object to be created, and then its member values are set.</span></span> <span data-ttu-id="c3698-180">Každá zadaná vlastnost pro nastavení je vyhodnocen podobný jak zadaného člena lze nastavit pro objekt vytvořený při XAML je analyzován.</span><span class="sxs-lookup"><span data-stu-id="c3698-180">Each specified property to set is evaluated similar to how a specified member can be set on a created object when XAML is parsed.</span></span> <span data-ttu-id="c3698-181">Existují dva důležité rozdíly:</span><span class="sxs-lookup"><span data-stu-id="c3698-181">There are two important differences:</span></span>  
  
-   <span data-ttu-id="c3698-182">Jak bylo uvedeno dříve, není potřeba mít výchozí konstruktor, aby bylo možné vytvořit instanci v XAML jako typ podpory rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="c3698-182">As noted previously, a markup extension support type does not need to have a default constructor in order to be instantiated in XAML.</span></span> <span data-ttu-id="c3698-183">Konstrukce objektu je odloženo, dokud jeho možné argumenty v textu syntaxe jsou tokenizovaného a vyhodnotí jako poziční nebo pojmenované argumenty a odpovídajícího konstruktoru je volána v daném čase.</span><span class="sxs-lookup"><span data-stu-id="c3698-183">Its object construction is deferred until its possible arguments in the text syntax are tokenized and evaluated as either positional or named arguments, and the appropriate constructor is called at that time.</span></span>  
  
-   <span data-ttu-id="c3698-184">Použití rozšíření značky mohou být vnořené.</span><span class="sxs-lookup"><span data-stu-id="c3698-184">Markup extensions usages can be nested.</span></span> <span data-ttu-id="c3698-185">Rozšíření pro vnitřní kód je vyhodnocen jako první.</span><span class="sxs-lookup"><span data-stu-id="c3698-185">The innermost markup extension is evaluated first.</span></span> <span data-ttu-id="c3698-186">Proto můžete předpokládat takové použití a deklarujte jeden z parametrů konstrukce na typ, který vyžaduje převaděč hodnoty (jako je například rozšíření značek) k vytvoření.</span><span class="sxs-lookup"><span data-stu-id="c3698-186">Therefore, you can assume such a usage and declare one of the construction parameters to be a type that requires a value converter (such as a markup extension) to produce.</span></span>  
  
 <span data-ttu-id="c3698-187">Závislost na toto zpracování se zobrazilo v předchozím příkladu.</span><span class="sxs-lookup"><span data-stu-id="c3698-187">A reliance on such processing was shown in the previous example.</span></span> <span data-ttu-id="c3698-188">Objekt zapisovače rozhraní .NET Framework XAML Services XAML procesu výčtu konstant názvy do výčtové hodnoty na nativní úroveň.</span><span class="sxs-lookup"><span data-stu-id="c3698-188">The .NET Framework XAML Services XAML object writer processes enumeration constant names into enumerated values at a native level.</span></span>  
  
 <span data-ttu-id="c3698-189">Zpracování textová syntaxe značek pozičních parametrů rozšíření můžete také využívají konvertor typu, který je přidružen typ, který je v argumentu konstrukce.</span><span class="sxs-lookup"><span data-stu-id="c3698-189">Processing text syntax of a markup extension positional parameter can also rely on a type converter that is associated with the type that is in the construction argument.</span></span>  
  
 <span data-ttu-id="c3698-190">Argumenty jsou nazývány poziční argumenty, protože odpovídá pořadí, ve kterém dochází tokeny ve využití pořadí poziční parametr konstruktoru, ke kterému jsou přiřazeny.</span><span class="sxs-lookup"><span data-stu-id="c3698-190">The arguments are called positional arguments because the order in which the tokens in the usage is encountered corresponds to the positional order of the constructor parameter to which they are assigned.</span></span> <span data-ttu-id="c3698-191">Představte si třeba následující podpis konstruktoru:</span><span class="sxs-lookup"><span data-stu-id="c3698-191">For example, consider the following constructor signature:</span></span>  
  
```  
public Collate(CollationMode collationMode, object collateThis) {...}  
```  
  
 <span data-ttu-id="c3698-192">Procesor XAML očekává, že dva poziční argumenty tohoto rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="c3698-192">A XAML processor expects two positional arguments for this markup extension.</span></span> <span data-ttu-id="c3698-193">Pokud se využití `{Collate AlphaUp,{x:Reference circularFile}}`, `AlphaUp` token se odesílají do první parametr a vyhodnotí jako `CollationMode` výčtu s názvem – konstanta.</span><span class="sxs-lookup"><span data-stu-id="c3698-193">If there was a usage `{Collate AlphaUp,{x:Reference circularFile}}`, the `AlphaUp` token is sent to the first parameter and evaluated as a `CollationMode` enumeration named constant.</span></span> <span data-ttu-id="c3698-194">Výsledek vnitřního `x:Reference` se odesílají na druhý parametr a vyhodnotit jako objekt.</span><span class="sxs-lookup"><span data-stu-id="c3698-194">The result of the inner `x:Reference` is sent to the second parameter and evaluated as an object.</span></span>  
  
 <span data-ttu-id="c3698-195">XAML je zadaná pravidla pro rozšíření syntaxe značek a zpracování, čárky oddělovač mezi argumenty, jestli tyto argumenty jsou poziční argumenty nebo pojmenované argumenty.</span><span class="sxs-lookup"><span data-stu-id="c3698-195">In the XAML specified rules for markup extension syntax and processing, the comma is the delimiter between arguments, whether those arguments are positional arguments or named arguments.</span></span>  
  
### <a name="duplicate-arity-of-positional-arguments"></a><span data-ttu-id="c3698-196">Duplicitní Arita pro instanci poziční argumenty</span><span class="sxs-lookup"><span data-stu-id="c3698-196">Duplicate Arity of Positional Arguments</span></span>  
 <span data-ttu-id="c3698-197">Pokud se použití rozšíření značky s poziční argumenty, zaznamená zapisovače objektu XAML a existuje více argumentů konstruktoru, které tohoto čísla argumenty (duplicitní Arita), který není nutně chybu.</span><span class="sxs-lookup"><span data-stu-id="c3698-197">If a XAML object writer encounters a markup extension usage with positional arguments, and there are multiple constructor arguments that take that number of arguments (a duplicate arity), that is not necessarily an error.</span></span> <span data-ttu-id="c3698-198">Chování závisí na přizpůsobitelné XAML schématu nastavení kontextu, <xref:System.Xaml.XamlSchemaContextSettings.SupportMarkupExtensionsWithDuplicateArity%2A>.</span><span class="sxs-lookup"><span data-stu-id="c3698-198">The behavior depends on a customizable XAML schema context setting, <xref:System.Xaml.XamlSchemaContextSettings.SupportMarkupExtensionsWithDuplicateArity%2A>.</span></span> <span data-ttu-id="c3698-199">Pokud <xref:System.Xaml.XamlSchemaContextSettings.SupportMarkupExtensionsWithDuplicateArity%2A> je `true`, zapisovače objektu XAML by neměla vyvolávat výjimka pouze z důvodů duplicitní Arita.</span><span class="sxs-lookup"><span data-stu-id="c3698-199">If <xref:System.Xaml.XamlSchemaContextSettings.SupportMarkupExtensionsWithDuplicateArity%2A> is `true`, a XAML object writer should not throw an exception only for reasons of duplicate arity.</span></span> <span data-ttu-id="c3698-200">Chování za daným bodem není striktně definované.</span><span class="sxs-lookup"><span data-stu-id="c3698-200">Behavior beyond that point is not strictly defined.</span></span> <span data-ttu-id="c3698-201">Předpoklad základním návrhu je, že kontextu schématu je k dispozici pro parametry pro konkrétní informace o typu a může pokus o explicitní přetypování, které odpovídají duplicitní kandidáty zobrazíte podpis, který může být nejlepší shodu.</span><span class="sxs-lookup"><span data-stu-id="c3698-201">The basic design assumption is that the schema context has type information available for the specific parameters and can attempt explicit casts that match the duplicate candidates to see which signature might be the best match.</span></span> <span data-ttu-id="c3698-202">Pokud žádné podpisy projdou testy, které jsou uložené tento konkrétní schématu kontext, který běží na zapisovače objektu XAML, může být stále vyvolána výjimka.</span><span class="sxs-lookup"><span data-stu-id="c3698-202">An exception might still be thrown if no signatures can pass the tests that are imposed by that particular schema context that is running on a XAML object writer.</span></span>  
  
 <span data-ttu-id="c3698-203">Ve výchozím nastavení <xref:System.Xaml.XamlSchemaContextSettings.SupportMarkupExtensionsWithDuplicateArity%2A> je `false` v CLR-based <xref:System.Xaml.XamlSchemaContext> pro rozhraní .NET Framework XAML Services.</span><span class="sxs-lookup"><span data-stu-id="c3698-203">By default, <xref:System.Xaml.XamlSchemaContextSettings.SupportMarkupExtensionsWithDuplicateArity%2A> is `false` in the CLR-based <xref:System.Xaml.XamlSchemaContext> for .NET Framework XAML Services.</span></span> <span data-ttu-id="c3698-204">Proto výchozí <xref:System.Xaml.XamlObjectWriter> vyvolá výjimky, pokud se setká s použití rozšíření značky ve kterých je duplicitní Arita v konstruktory základního typu.</span><span class="sxs-lookup"><span data-stu-id="c3698-204">Thus, the default <xref:System.Xaml.XamlObjectWriter> throws exceptions if it encounters a markup extension usage where there is duplicate arity in the backing type's constructors.</span></span>  
  
<a name="named_arguments_for_a_custom_markup_extension"></a>   
## <a name="named-arguments-for-a-custom-markup-extension"></a><span data-ttu-id="c3698-205">Pojmenované argumenty pro rozšíření vlastního kódu</span><span class="sxs-lookup"><span data-stu-id="c3698-205">Named Arguments for a Custom Markup Extension</span></span>  
 <span data-ttu-id="c3698-206">Rozšíření značek XAML určený můžete také formuláře pojmenované argumenty pro využití.</span><span class="sxs-lookup"><span data-stu-id="c3698-206">Markup extensions as specified by XAML can also use a named arguments form for usage.</span></span> <span data-ttu-id="c3698-207">Na první úrovni Tokenizace textová syntaxe se dělí argumenty.</span><span class="sxs-lookup"><span data-stu-id="c3698-207">At the first level of tokenization, the text syntax is divided into arguments.</span></span> <span data-ttu-id="c3698-208">Přítomnost znak rovná se (=) v rámci některý argument určuje argument jako pojmenovaný argument.</span><span class="sxs-lookup"><span data-stu-id="c3698-208">The presence of an equals sign (=) within any argument identifies an argument as a named argument.</span></span> <span data-ttu-id="c3698-209">Takové argument je také tokenizovaného do dvojici název/hodnota.</span><span class="sxs-lookup"><span data-stu-id="c3698-209">Such an argument is also tokenized into a name/value pair.</span></span> <span data-ttu-id="c3698-210">Jména v tomto případě veřejné nastavitelnou vlastnost typu podporu rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="c3698-210">The name in this case names a public settable property of the markup extension's support type.</span></span> <span data-ttu-id="c3698-211">Pokud máte v úmyslu podporují použití pojmenovaných argumentů, je vhodné zadat tyto veřejné nastavitelné vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="c3698-211">If you intend to support named argument usage, you should provide these public settable properties.</span></span> <span data-ttu-id="c3698-212">Vlastnosti může být zděděné vlastnosti, tak dlouho, dokud zůstává zachována možnost jejich veřejné.</span><span class="sxs-lookup"><span data-stu-id="c3698-212">The properties can be inherited properties as long as they remain public.</span></span>  
  
<a name="accessing_service_provider_context_from_a_markup_extension_implementation"></a>   
## <a name="accessing-service-provider-context-from-a-markup-extension-implementation"></a><span data-ttu-id="c3698-213">Přístup k kontext zprostředkovatele služby z implementace rozšíření značek</span><span class="sxs-lookup"><span data-stu-id="c3698-213">Accessing Service Provider Context from a Markup Extension Implementation</span></span>  
 <span data-ttu-id="c3698-214">Dostupné služby jsou stejné pro všechny převaděč hodnoty.</span><span class="sxs-lookup"><span data-stu-id="c3698-214">The services available are the same for any value converter.</span></span> <span data-ttu-id="c3698-215">Rozdíl je v jak každý převaděč hodnoty obdrží kontext služby.</span><span class="sxs-lookup"><span data-stu-id="c3698-215">The difference is in how each value converter receives the service context.</span></span> <span data-ttu-id="c3698-216">Přístup k službám a službám, které jsou k dispozici jsou popsány v tématu [převaděče typů a rozšíření značek pro XAML](type-converters-and-markup-extensions-for-xaml.md).</span><span class="sxs-lookup"><span data-stu-id="c3698-216">Accessing services and the services available are documented in the topic [Type Converters and Markup Extensions for XAML](type-converters-and-markup-extensions-for-xaml.md).</span></span>  
  
<a name="property_element_usage_of_a_markup_extension"></a>   
## <a name="property-element-usage-of-a-markup-extension"></a><span data-ttu-id="c3698-217">Použití elementu vlastnosti rozšíření značek</span><span class="sxs-lookup"><span data-stu-id="c3698-217">Property Element Usage of a Markup Extension</span></span>  
 <span data-ttu-id="c3698-218">Scénáře pro použití rozšíření značky jsou často navržené s ohledem pomocí rozšíření značek v použití atributu.</span><span class="sxs-lookup"><span data-stu-id="c3698-218">The scenarios for markup extension usages are often designed around using the markup extension in attribute usage.</span></span> <span data-ttu-id="c3698-219">Je však také potenciálně možné základní třídy pro podporu použití elementu vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="c3698-219">However, it is also potentially possible to define the backing class to support property element usage.</span></span>  
  
 <span data-ttu-id="c3698-220">Chcete-li podporovat použití elementu vlastnosti vašeho rozšíření značek, definujte veřejný výchozí konstruktor.</span><span class="sxs-lookup"><span data-stu-id="c3698-220">To support property element usage of your markup extension, define a public default constructor.</span></span> <span data-ttu-id="c3698-221">To by měl být konstruktor instance není statický konstruktor.</span><span class="sxs-lookup"><span data-stu-id="c3698-221">This should be an instance constructor not a static constructor.</span></span> <span data-ttu-id="c3698-222">Se totiž procesoru XAML musí obecně vyvolání výchozího konstruktoru u libovolného elementu objektu, který zpracovává ze značek, a to zahrnuje třídy rozšíření značek jako elementů objektu.</span><span class="sxs-lookup"><span data-stu-id="c3698-222">This is required because a XAML processor must generally invoke the default constructor on any object element it processes from markup, and this includes markup extension classes as object elements.</span></span> <span data-ttu-id="c3698-223">Pro pokročilé scénáře můžete definovat cesty k jiné než výchozí konstrukci pro třídy.</span><span class="sxs-lookup"><span data-stu-id="c3698-223">For advanced scenarios, you can define non-default construction paths for classes.</span></span> <span data-ttu-id="c3698-224">(Další informace najdete v tématu [x: FactoryMethod – direktiva](x-factorymethod-directive.md).) Tato schémata však neměli byste používat pro účely rozšíření značek, protože díky tomu zjišťování vzor používání mnohem obtížnější, pro profesionální návrháře využívající i pro uživatele nezpracovaný kód.</span><span class="sxs-lookup"><span data-stu-id="c3698-224">(For more information, see [x:FactoryMethod Directive](x-factorymethod-directive.md).) However, you should not use these patterns for markup extension purposes because this makes discovery of the usage pattern much more difficult, both for designers and for users of raw markup.</span></span>  
  
<a name="attributing_for_a_custom_markup_extension"></a>   
## <a name="attributing-for-a-custom-markup-extension"></a><span data-ttu-id="c3698-225">Přidělování pro rozšíření vlastního kódu</span><span class="sxs-lookup"><span data-stu-id="c3698-225">Attributing for a Custom Markup Extension</span></span>  
 <span data-ttu-id="c3698-226">Pro podporu vybavená vývojová prostředí a určitých scénářů objekt zapisovače XAML, by měl atribut jako typ podpory rozšíření značek s několika atributy CLR.</span><span class="sxs-lookup"><span data-stu-id="c3698-226">To support both design environments and certain XAML object writer scenarios, you should attribute a markup extension support type with several CLR attributes.</span></span> <span data-ttu-id="c3698-227">Tyto atributy zasílání zpráv o využití rozšíření odpovídající značky.</span><span class="sxs-lookup"><span data-stu-id="c3698-227">These attributes report the intended markup extension usage.</span></span>  
  
 <span data-ttu-id="c3698-228"><xref:System.Windows.Markup.MarkupExtensionReturnTypeAttribute> sestavy <xref:System.Type> zadejte informace o objektu, který <xref:System.Windows.Markup.ArrayExtension.ProvideValue%2A> vrátí.</span><span class="sxs-lookup"><span data-stu-id="c3698-228"><xref:System.Windows.Markup.MarkupExtensionReturnTypeAttribute> reports the <xref:System.Type> information for the object type that <xref:System.Windows.Markup.ArrayExtension.ProvideValue%2A> returns.</span></span> <span data-ttu-id="c3698-229">Čistě podpisem <xref:System.Windows.Markup.ArrayExtension.ProvideValue%2A> vrátí <xref:System.Object>.</span><span class="sxs-lookup"><span data-stu-id="c3698-229">By its pure signature, <xref:System.Windows.Markup.ArrayExtension.ProvideValue%2A> returns <xref:System.Object>.</span></span> <span data-ttu-id="c3698-230">Ale různé uživatele může být vhodné přesnější informace návratovým typem.</span><span class="sxs-lookup"><span data-stu-id="c3698-230">But various consumers might want more precise return type information.</span></span> <span data-ttu-id="c3698-231">Sem patří:</span><span class="sxs-lookup"><span data-stu-id="c3698-231">This includes:</span></span>  
  
-   <span data-ttu-id="c3698-232">Návrháři a Integrovaná vývojová prostředí, který může být schopný poskytnout s ohledem na typ podporu pro použití rozšíření značky.</span><span class="sxs-lookup"><span data-stu-id="c3698-232">Designers and IDEs, who might be able to provide type-aware support for markup extension usages.</span></span>  
  
-   <span data-ttu-id="c3698-233">Implementace rozšířené `SetMarkupExtension` obslužné rutiny na cílové třídy, které může závisí na reflexi k určení návratového typu rozšíření značek namísto větvení na konkrétní známé <xref:System.Windows.Markup.MarkupExtension> implementace podle názvu.</span><span class="sxs-lookup"><span data-stu-id="c3698-233">Advanced implementations of `SetMarkupExtension` handlers on target classes, which might rely on reflection to determine a markup extension's return type instead of branching on specific known <xref:System.Windows.Markup.MarkupExtension> implementations by name.</span></span>  
  
<a name="serialization_of_markup_extension_usages"></a>   
## <a name="serialization-of-markup-extension-usages"></a><span data-ttu-id="c3698-234">Serializace použití rozšíření značek</span><span class="sxs-lookup"><span data-stu-id="c3698-234">Serialization of Markup Extension Usages</span></span>  
 <span data-ttu-id="c3698-235">Při zápisu objektu XAML zpracovává použití rozšíření značky a volání <xref:System.Windows.Markup.MarkupExtension.ProvideValue%2A>, kontext pro něj předtím se použití rozšíření značky přetrvává v datovém proudu uzlu XAML, ale ne v grafu objektů.</span><span class="sxs-lookup"><span data-stu-id="c3698-235">When a XAML object writer processes a markup extension usage and calls <xref:System.Windows.Markup.MarkupExtension.ProvideValue%2A>, the context for it previously being a markup extension usage persists in the XAML node stream but not in the object graph.</span></span> <span data-ttu-id="c3698-236">V grafu objektů je zachována pouze hodnotu.</span><span class="sxs-lookup"><span data-stu-id="c3698-236">In the object graph, only the value is preserved.</span></span> <span data-ttu-id="c3698-237">Pokud máte scénáře návrhu nebo jiných důvodů, proč trvalé původní použití rozšíření značky na serializovaná výstupu, je třeba navrhnout vlastní infrastrukturu pro sledování využití rozšíření značek v datovém proudu uzlu XAML cesta zatížení.</span><span class="sxs-lookup"><span data-stu-id="c3698-237">If you have design scenarios or other reasons for persisting the original markup extension usage into the serialized output, you must design your own infrastructure for tracking the markup extension usages from the load path XAML node stream.</span></span> <span data-ttu-id="c3698-238">Můžete implementovat chování, abyste mohli znovu vytvořit prvky datový proud uzlu z cesty zatížení a jejich přehrávání pro autory XAML pro serializaci v Uložit cestu, nahraďte hodnoty v příslušné pozici datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="c3698-238">You can implement behavior to recreate the elements of the node stream from the load path and play them back to XAML writers for serialization in the save path, substituting for the value in the appropriate position of the node stream.</span></span>  
  
<a name="markup_extensions_in_the_xaml_node_stream"></a>   
## <a name="markup-extensions-in-the-xaml-node-stream"></a><span data-ttu-id="c3698-239">Rozšíření značek v Stream uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="c3698-239">Markup Extensions in the XAML Node Stream</span></span>  
 <span data-ttu-id="c3698-240">Pokud pracujete s datový proud uzlu XAML v cestě zatížení, zobrazí se použití rozšíření značky v datovém proudu uzlu jako objekt.</span><span class="sxs-lookup"><span data-stu-id="c3698-240">If you are working with a XAML node stream on the load path, a markup extension usage appears in the node stream as an object.</span></span>  
  
 <span data-ttu-id="c3698-241">Pokud se použití rozšíření značky používá poziční argumenty, je datum vyjádřeno jako počáteční objekt s hodnotou inicializace.</span><span class="sxs-lookup"><span data-stu-id="c3698-241">If the markup extension usage uses positional arguments, it is represented as a start object with an initialization value.</span></span> <span data-ttu-id="c3698-242">Jako hrubý textové vyjádření datovém proudu uzlu vypadá přibližně takto:</span><span class="sxs-lookup"><span data-stu-id="c3698-242">As a rough text representation, the node stream resembles the following:</span></span>  
  
 <span data-ttu-id="c3698-243">`StartObject` (<xref:System.Xaml.XamlType> je typ definice rozšíření značek, její návratový typ)</span><span class="sxs-lookup"><span data-stu-id="c3698-243">`StartObject` (<xref:System.Xaml.XamlType> is the markup extension's definition type, not its return type)</span></span>  
  
 <span data-ttu-id="c3698-244">`StartMember` (název <xref:System.Xaml.XamlMember> je `_InitializationText`)</span><span class="sxs-lookup"><span data-stu-id="c3698-244">`StartMember` (name of the <xref:System.Xaml.XamlMember> is `_InitializationText`)</span></span>  
  
 <span data-ttu-id="c3698-245">`Value` (hodnota je poziční argumenty jako řetězec, včetně použité oddělovače)</span><span class="sxs-lookup"><span data-stu-id="c3698-245">`Value` (value is the positional arguments as a string including the intervening delimiters)</span></span>  
  
 `EndMember`  
  
 `EndObject`  
  
 <span data-ttu-id="c3698-246">Použití rozšíření značky s pojmenovanými argumenty je reprezentována jako objekt s členy od příslušného názvu každého nastavení s textovými řetězci hodnotami.</span><span class="sxs-lookup"><span data-stu-id="c3698-246">A markup extension usage with named arguments is represented as an object with members of the relevant names, each set with text string values.</span></span>  
  
 <span data-ttu-id="c3698-247">Ve skutečnosti volání `ProvideValue` provádění rozšíření značek vyžaduje kontext schématu XAML, protože, která vyžaduje mapování typu a vytvoření instance typu podporu rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="c3698-247">Actually invoking the `ProvideValue` implementation of a markup extension requires the XAML schema context because that requires type-mapping and creating a markup extension support type instance.</span></span> <span data-ttu-id="c3698-248">Toto je jedním z důvodů, proč použití rozšíření značky jsou zachovány tímto způsobem v datové proudy rozhraní .NET Framework XAML Services uzlu výchozí – čtečky část cesty zatížení, který je často nemá potřebné XAML schématu kontextu k dispozici.</span><span class="sxs-lookup"><span data-stu-id="c3698-248">This is one reason why markup extension usages are preserved this way in the default .NET Framework XAML Services node streams -  the reader part of a load path often does not have the necessary XAML schema context available.</span></span>  
  
 <span data-ttu-id="c3698-249">Pokud pracujete s datový proud uzlu XAML Uložit cestu, obvykle není k dispozici v reprezentaci grafu objektů, který informuje, že objekt určený k serializaci byla původně poskytované použití rozšíření značky a `ProvideValue` výsledek.</span><span class="sxs-lookup"><span data-stu-id="c3698-249">If you are working with a XAML node stream on the save path, there generally is nothing present in an object graph representation that can inform you that the object to serialize was originally provided by a markup extension usage and a `ProvideValue` result.</span></span> <span data-ttu-id="c3698-250">Scénáře, které je potřeba zachovat použití rozšíření značky u verzemi, zatímco také zaznamenání další změny v grafu objektů musí navrhnout svoje vlastní techniky pro zachování informací o použití rozšíření značky z původní vstup XAML.</span><span class="sxs-lookup"><span data-stu-id="c3698-250">Scenarios that need to persist markup extension usages for round-tripping while also capturing other changes in the object graph must devise their own techniques for preserving the knowledge of a markup extension usage from the original XAML input.</span></span> <span data-ttu-id="c3698-251">Například chcete-li obnovit použití rozšíření značky, budete muset pracovat s datovém proudu uzlu na Uložit cestu, aby bylo možné obnovit použití rozšíření značky nebo provádí nějaký typ sloučení mezi původní XAML a XAML odbavovaná.</span><span class="sxs-lookup"><span data-stu-id="c3698-251">For example, to restore the markup extension usages, you may need to work with the node stream on the save path in order to restore markup extension usages, or perform some type of merge between the original XAML and the round-tripped XAML.</span></span> <span data-ttu-id="c3698-252">Některé implementace XAML architektury, jako jsou WPF reprezentují případy, kdy použití rozšíření značky zadané hodnoty pomocí vnitřní typy (výrazy).</span><span class="sxs-lookup"><span data-stu-id="c3698-252">Some XAML-implementing frameworks such as WPF use intermediate types (expressions) to help represent cases where markup extension usages provided the values.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="c3698-253">Viz také:</span><span class="sxs-lookup"><span data-stu-id="c3698-253">See also</span></span>

- <xref:System.Windows.Markup.MarkupExtension>
- [<span data-ttu-id="c3698-254">Převaděče typů a rozšíření značek pro jazyk XAML</span><span class="sxs-lookup"><span data-stu-id="c3698-254">Type Converters and Markup Extensions for XAML</span></span>](type-converters-and-markup-extensions-for-xaml.md)
- [<span data-ttu-id="c3698-255">Rozšíření značek a WPF XAML</span><span class="sxs-lookup"><span data-stu-id="c3698-255">Markup Extensions and WPF XAML</span></span>](../wpf/advanced/markup-extensions-and-wpf-xaml.md)
