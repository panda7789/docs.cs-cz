---
title: Obory názvů jazyka XAML pro technologii .NET Framework XAML Services
ms.date: 03/30/2017
ms.assetid: e4f15f13-c420-4c1e-aeab-9b6f50212047
ms.openlocfilehash: 11bf5d112c64fb0b942decf7635f02b90a98bdeb
ms.sourcegitcommit: a4f9b754059f0210e29ae0578363a27b9ba84b64
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 12/05/2019
ms.locfileid: "74837165"
---
# <a name="xaml-namespaces-for-net-framework-xaml-services"></a><span data-ttu-id="3bb93-102">Obory názvů jazyka XAML pro technologii .NET Framework XAML Services</span><span class="sxs-lookup"><span data-stu-id="3bb93-102">XAML Namespaces for .NET Framework XAML Services</span></span>
<span data-ttu-id="3bb93-103">Obor názvů XAML je koncept, který se rozbalí do definice oboru názvů XML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-103">A XAML namespace is a concept that expands on the definition of an XML namespace.</span></span> <span data-ttu-id="3bb93-104">Podobně jako obor názvů XML, můžete definovat obor názvů XAML pomocí atributu `xmlns` v kódu.</span><span class="sxs-lookup"><span data-stu-id="3bb93-104">Similar to an XML namespace, you can define a XAML namespace using an `xmlns` attribute in markup.</span></span> <span data-ttu-id="3bb93-105">Obory názvů XAML jsou také zastoupeny v datovém proudu uzlu XAML a v dalších rozhraních API služeb XAML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-105">XAML namespaces are also represented in the XAML node stream and other XAML Services APIs.</span></span> <span data-ttu-id="3bb93-106">Toto téma definuje koncept oboru názvů XAML a popisuje, jak lze definovat obory názvů XAML a jsou používány kontexty schématu XAML a dalšími aspekty .NET Frameworkch služeb XAML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-106">This topic defines the XAML namespace concept, and describes how XAML namespaces can be defined and are used by XAML schema contexts and other aspects of .NET Framework XAML Services.</span></span>  
  
## <a name="xml-namespace-and-xaml-namespace"></a><span data-ttu-id="3bb93-107">Obor názvů XML a obor názvů XAML</span><span class="sxs-lookup"><span data-stu-id="3bb93-107">XML Namespace and XAML Namespace</span></span>  
 <span data-ttu-id="3bb93-108">Obor názvů XAML je specializovaný obor názvů XML, stejně jako XAML je specializované formuláře XML a používá základní formulář XML pro svůj kód.</span><span class="sxs-lookup"><span data-stu-id="3bb93-108">A XAML namespace is a specialized XML namespace, just as XAML is a specialized form of XML and uses the basic XML form for its markup.</span></span> <span data-ttu-id="3bb93-109">V kódu deklarujete obor názvů XAML a jeho mapování pomocí atributu `xmlns` použitého pro element.</span><span class="sxs-lookup"><span data-stu-id="3bb93-109">In markup, you declare a XAML namespace and its mapping through an `xmlns` attribute applied to an element.</span></span> <span data-ttu-id="3bb93-110">Deklaraci `xmlns` lze provést u stejného prvku, v němž je deklarován obor názvů XAML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-110">The `xmlns` declaration can be made to the same element that the XAML namespace is declared in.</span></span> <span data-ttu-id="3bb93-111">Deklarace oboru názvů XAML vytvořená pro element je platná pro tento element, všechny atributy tohoto prvku a všechny podřízené prvky tohoto prvku.</span><span class="sxs-lookup"><span data-stu-id="3bb93-111">A XAML namespace declaration made to an element is valid for that element, all attributes of that element, and all children of that element.</span></span> <span data-ttu-id="3bb93-112">Atributy mohou používat obory názvů XAML, které nejsou stejné jako u prvku, který obsahuje atribut, pokud název atributu sám odkazuje na předponu jako součást názvu atributu v kódu.</span><span class="sxs-lookup"><span data-stu-id="3bb93-112">Attributes can use a XAML namespaces that is not the same as the element that contains the attribute, so long as the attribute name itself references the prefix as part of its attribute name in markup.</span></span>  
  
 <span data-ttu-id="3bb93-113">Rozlišení oboru názvů XAML oproti oboru názvů XML je, že obor názvů XML lze použít k odkazování na schéma, nebo může být použit k pouhému odlišení entit.</span><span class="sxs-lookup"><span data-stu-id="3bb93-113">The distinction of a XAML namespace versus an XML namespace is that an XML namespace might be used to reference a schema, or might be used to simply differentiate entities.</span></span> <span data-ttu-id="3bb93-114">V jazyce XAML musí být typy a členy, jak je použit v jazyce XAML, nakonec přeloženy na typy zálohování a koncepty schémat XML se pro tuto funkci nevztahují.</span><span class="sxs-lookup"><span data-stu-id="3bb93-114">For XAML, the types and members as used in XAML must ultimately be resolved to backing types, and XML schema concepts do not apply well to this capability.</span></span> <span data-ttu-id="3bb93-115">Obor názvů XAML obsahuje informace, které musí mít kontext schématu XAML k dispozici, aby bylo možné provést toto mapování typu.</span><span class="sxs-lookup"><span data-stu-id="3bb93-115">The XAML namespace contains information that the XAML schema context must have available in order to perform this type mapping.</span></span>  
  
## <a name="xaml-namespace-components"></a><span data-ttu-id="3bb93-116">Komponenty oboru názvů XAML</span><span class="sxs-lookup"><span data-stu-id="3bb93-116">XAML Namespace Components</span></span>  
 <span data-ttu-id="3bb93-117">Definice oboru názvů XAML má dvě komponenty: předponu a identifikátor.</span><span class="sxs-lookup"><span data-stu-id="3bb93-117">The XAML namespace definition has two components: a prefix, and an identifier.</span></span> <span data-ttu-id="3bb93-118">Každá z těchto komponent je přítomna v případě, že je obor názvů XAML deklarován v kódu nebo je definován v systému typů XAML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-118">Each of these components are present when a XAML namespace is declared in markup, or defined in the XAML type system.</span></span>  
  
 <span data-ttu-id="3bb93-119">Předpona může být libovolný řetězec, který je povolený [obory názvů W3C ve specifikaci XML 1,0](https://www.w3.org/TR/REC-xml-names/) .</span><span class="sxs-lookup"><span data-stu-id="3bb93-119">The prefix can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/) .</span></span> <span data-ttu-id="3bb93-120">Podle konvence jsou předpony obvykle velmi krátké řetězce, protože předpona je opakována mnohokrát v typickém souboru označení.</span><span class="sxs-lookup"><span data-stu-id="3bb93-120">By convention, the prefixes are typically very short strings, because the prefix is repeated many times in a typical markup file.</span></span> <span data-ttu-id="3bb93-121">Některé obory názvů jazyka XAML, které jsou určeny pro použití v několika implementacích jazyka XAML, používají konkrétní konvenční předpony.</span><span class="sxs-lookup"><span data-stu-id="3bb93-121">Certain XAML namespaces that are intended to be used in multiple XAML implementations use particular conventional prefixes.</span></span> <span data-ttu-id="3bb93-122">Například obor názvů XAML jazyka XAML je obvykle namapován pomocí `x`předpony.</span><span class="sxs-lookup"><span data-stu-id="3bb93-122">For example, the XAML language XAML namespace is typically mapped using the prefix `x`.</span></span> <span data-ttu-id="3bb93-123">Můžete definovat výchozí obor názvů XAML, kde Předpona není uvedena v definici, ale je reprezentována jako prázdný řetězec, pokud je definováno nebo dotazované rozhraní API služby by.NET Framework XAML Services.</span><span class="sxs-lookup"><span data-stu-id="3bb93-123">You can define a default XAML namespace, where the prefix is not given in the definition but is represented as an empty string if defined or queried by.NET Framework XAML Services API.</span></span> <span data-ttu-id="3bb93-124">Výchozí obor názvů XAML je obvykle záměrně vybrán pro zvýšení úrovně maximalizovaného kódu, který je vynechán, pomocí technologie implementující XAML a jeho scénářů a slovníků.</span><span class="sxs-lookup"><span data-stu-id="3bb93-124">Typically, the default XAML namespace is deliberately chosen in order to promote a maximized amount of prefix-omitting markup by a XAML-implementing technology and its scenarios and vocabularies.</span></span>  
  
 <span data-ttu-id="3bb93-125">Identifikátor může být libovolný řetězec, který je povolený [obory názvů W3C ve specifikaci XML 1,0](https://www.w3.org/TR/REC-xml-names/).</span><span class="sxs-lookup"><span data-stu-id="3bb93-125">The identifier can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/).</span></span> <span data-ttu-id="3bb93-126">Podle konvence jsou identifikátory pro obory názvů XML nebo XAML často předány ve formě identifikátoru URI, obvykle jako absolutní identifikátor URI kvalifikovaný pro protokol.</span><span class="sxs-lookup"><span data-stu-id="3bb93-126">By convention, identifiers for either XML namespaces or XAML namespaces are often given in URI form, typically as a protocol-qualified absolute URI.</span></span> <span data-ttu-id="3bb93-127">Často jsou informace o verzi definující konkrétní slovníky XAML odvozeny jako součást řetězce cesty.</span><span class="sxs-lookup"><span data-stu-id="3bb93-127">Often, version information that defines a particular XAML vocabulary is implied as part of the path string.</span></span> <span data-ttu-id="3bb93-128">Obory názvů XAML přidejte další konvenci identifikátoru za rámec konvence identifikátoru URI XML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-128">XAML namespaces add an additional identifier convention beyond the XML URI convention.</span></span> <span data-ttu-id="3bb93-129">Pro obory názvů XAML identifikátor komunikuje informace, které jsou vyžadovány kontextem schématu XAML, aby bylo možné přeložit typy, které jsou zadány jako elementy v daném oboru názvů XAML, nebo pro řešení atributů na členy.</span><span class="sxs-lookup"><span data-stu-id="3bb93-129">For XAML namespaces, the identifier communicates information that is needed by a XAML schema context in order to resolve the types that are specified as elements under that XAML namespace, or to resolve attributes to members.</span></span>  
  
 <span data-ttu-id="3bb93-130">Pro účely sdělování informací do kontextu schématu XAML může být identifikátor oboru názvů XAML stále ve formátu identifikátoru URI.</span><span class="sxs-lookup"><span data-stu-id="3bb93-130">For purposes of communicating information to a XAML schema context, the identifier for a XAML namespace might still be in URI form.</span></span> <span data-ttu-id="3bb93-131">Nicméně v tomto případě je identifikátor URI deklarován také jako shodný identifikátor v konkrétním sestavení nebo seznamu sestavení.</span><span class="sxs-lookup"><span data-stu-id="3bb93-131">However, in this case the URI is also declared as a matching identifier in a particular assembly or list of assemblies.</span></span> <span data-ttu-id="3bb93-132">To se provádí v sestaveních, které připisují sestavení pomocí <xref:System.Windows.Markup.XmlnsDefinitionAttribute>.</span><span class="sxs-lookup"><span data-stu-id="3bb93-132">This is done in assemblies by attributing the assembly with <xref:System.Windows.Markup.XmlnsDefinitionAttribute>.</span></span> <span data-ttu-id="3bb93-133">Tato metoda identifikace oboru názvů XAML a podpora chování rozlišení typů založeného na CLR v sestavení s atributy je podporována výchozím kontextem schématu XAML v .NET Framework služby XAML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-133">This method of identifying the XAML namespace and supporting a CLR-based type resolution behavior in the attributed assembly is supported by the default XAML schema context in .NET Framework XAML Services.</span></span> <span data-ttu-id="3bb93-134">Obecně platí, že tato konvence je možné použít pro případy, kdy kontext schématu XAML obsahuje CLR nebo je založen na výchozím kontextu schématu XAML, který je nezbytný pro čtení atributů CLR ze sestavení CLR.</span><span class="sxs-lookup"><span data-stu-id="3bb93-134">More generally, this convention can be used for cases where the XAML schema context incorporates the CLR or is based on the default XAML schema context, which is necessary in order to read CLR attributes from CLR assemblies.</span></span>  
  
 <span data-ttu-id="3bb93-135">Obory názvů XAML lze také identifikovat pomocí konvence, která komunikuje s oborem názvů CLR a sestavením definujícím typ.</span><span class="sxs-lookup"><span data-stu-id="3bb93-135">XAML namespaces also can be identified by a convention that communicates a CLR namespace and a type-defining assembly.</span></span> <span data-ttu-id="3bb93-136">Tato konvence se používá v případech, kdy neexistuje žádná <xref:System.Windows.Markup.XmlnsDefinitionAttribute> označení v sestaveních, která obsahují typy.</span><span class="sxs-lookup"><span data-stu-id="3bb93-136">This convention is used in cases where no <xref:System.Windows.Markup.XmlnsDefinitionAttribute> attribution exists in the assemblies that contain types.</span></span> <span data-ttu-id="3bb93-137">Tato konvence je potenciálně složitější než konvence identifikátoru URI a zároveň má potenciál na nejednoznačnost a duplicity, protože existuje více způsobů, jak odkazovat na sestavení.</span><span class="sxs-lookup"><span data-stu-id="3bb93-137">This convention is potentially more complex than the URI convention, and also has the potential for ambiguity and duplication, because there are multiple ways of referring to an assembly.</span></span>  
  
 <span data-ttu-id="3bb93-138">Nejzákladnější forma identifikátoru, který používá obor názvů CLR a konvenci sestavení, je následující:</span><span class="sxs-lookup"><span data-stu-id="3bb93-138">The most basic form of an identifier that uses the CLR namespace and assembly convention is as follows:</span></span>  
  
 <span data-ttu-id="3bb93-139">`clr-namespace:` *clrnsName* `; assembly=` *assemblyShortName*</span><span class="sxs-lookup"><span data-stu-id="3bb93-139">`clr-namespace:` *clrnsName* `; assembly=` *assemblyShortName*</span></span>  
  
 <span data-ttu-id="3bb93-140">`clr-namespace:` a `; assembly=` jsou komponenty literálů syntaxe.</span><span class="sxs-lookup"><span data-stu-id="3bb93-140">`clr-namespace:` and `; assembly=` are literal components of the syntax.</span></span>  
  
 <span data-ttu-id="3bb93-141">*clrnsName* je název řetězce, který identifikuje obor názvů CLR.</span><span class="sxs-lookup"><span data-stu-id="3bb93-141">*clrnsName* is the string name that identifies a CLR namespace.</span></span> <span data-ttu-id="3bb93-142">Tento název řetězce obsahuje všechny znaky interních teček (.), které poskytují nápovědu k oboru názvů CLR a jeho vztah k jiným oborům názvů CLR.</span><span class="sxs-lookup"><span data-stu-id="3bb93-142">This string name includes any internal dot characters (.) that provide hints about the CLR namespace and its relation to other CLR namespaces.</span></span>  
  
 <span data-ttu-id="3bb93-143">*assemblyShortName* je název řetězce sestavení definující typy, které jsou užitečné v jazyce XAML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-143">*assemblyShortName* is the string name of an assembly that defines types that are useful in XAML.</span></span> <span data-ttu-id="3bb93-144">Je očekáváno, že typy, které mají být získány prostřednictvím deklarovaného oboru názvů XAML, definovány sestavením a jsou výslovně deklarovány v rámci oboru názvů CLR určeného parametrem *clrnsName*.</span><span class="sxs-lookup"><span data-stu-id="3bb93-144">The types to be accessed through the declared XAML namespace are expected to be defined by the assembly and to be specifically declared within the CLR namespace specified by *clrnsName*.</span></span> <span data-ttu-id="3bb93-145">Tento název řetězce obvykle paralelismus informace, jak je uvedeno <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="3bb93-145">This string name typically parallels the information as reported by <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="3bb93-146">Úplnější definice oboru názvů CLR a konvence sestavení jsou následující:</span><span class="sxs-lookup"><span data-stu-id="3bb93-146">A more complete definition of the CLR namespace and assembly convention is as follows:</span></span>  
  
 <span data-ttu-id="3bb93-147">`clr-namespace:` *clrnsName* `; assembly=` *AssemblyName*</span><span class="sxs-lookup"><span data-stu-id="3bb93-147">`clr-namespace:` *clrnsName* `; assembly=` *assemblyName*</span></span>  
  
 <span data-ttu-id="3bb93-148">*AssemblyName* představuje libovolný řetězec, který je platný jako vstup <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="3bb93-148">*assemblyName* represents any string that is legal as an <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> input.</span></span> <span data-ttu-id="3bb93-149">Tento řetězec může obsahovat informace o jazykové verzi, veřejném klíči nebo verzi (definice těchto konceptů jsou definovány v referenčním tématu pro <xref:System.Reflection.Assembly>).</span><span class="sxs-lookup"><span data-stu-id="3bb93-149">This string can include culture, public key, or version information (definitions of these concepts are defined in the reference topic for <xref:System.Reflection.Assembly>).</span></span> <span data-ttu-id="3bb93-150">Formát COFF a legitimace (jak je používá jiné přetížení <xref:System.Reflection.Assembly.Load%2A>) nejsou relevantní pro účely načítání sestavení XAML. všechny informace o načítání musí být uvedeny jako řetězec.</span><span class="sxs-lookup"><span data-stu-id="3bb93-150">COFF format and evidence (as used by other overloads of <xref:System.Reflection.Assembly.Load%2A>) are not relevant for XAML assembly loading purposes; all load information must be presented as a string.</span></span>  
  
 <span data-ttu-id="3bb93-151">Zadání veřejného klíče pro sestavení je užitečnou technikou pro zabezpečení XAML nebo pro odstranění možné nejednoznačnosti, která může existovat, pokud jsou sestavení načítána pomocí jednoduchého názvu nebo v mezipaměti nebo doméně aplikace.</span><span class="sxs-lookup"><span data-stu-id="3bb93-151">Specifying a public key for the assembly is a useful technique for XAML security, or for removing possible ambiguity that can exist if assemblies are loaded by simple name, or pre-exist in a cache or application domain.</span></span> <span data-ttu-id="3bb93-152">Další informace najdete v tématu věnovaném [hlediskům zabezpečení XAML](xaml-security-considerations.md).</span><span class="sxs-lookup"><span data-stu-id="3bb93-152">For more information, see [XAML Security Considerations](xaml-security-considerations.md).</span></span>  
  
## <a name="xaml-namespace-declarations-in-the-xaml-services-api"></a><span data-ttu-id="3bb93-153">Deklarace oboru názvů XAML v rozhraní API služby XAML</span><span class="sxs-lookup"><span data-stu-id="3bb93-153">XAML Namespace Declarations in the XAML Services API</span></span>  
 <span data-ttu-id="3bb93-154">V rozhraní API služeb XAML je deklarace oboru názvů XAML reprezentována objektem <xref:System.Xaml.NamespaceDeclaration>.</span><span class="sxs-lookup"><span data-stu-id="3bb93-154">In the XAML Services API, a XAML namespace declaration is represented by a <xref:System.Xaml.NamespaceDeclaration> object.</span></span> <span data-ttu-id="3bb93-155">Pokud deklarujete obor názvů XAML v kódu, zavoláte konstruktor <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29>.</span><span class="sxs-lookup"><span data-stu-id="3bb93-155">If you are declaring a XAML namespace in code, you call the <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29> constructor.</span></span> <span data-ttu-id="3bb93-156">Parametry `ns` a `prefix` jsou zadány jako řetězce a zadání pro tyto parametry odpovídá definici identifikátoru oboru názvů XAML a předpony oboru názvů jazyka XAML, jak je uvedeno dříve v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="3bb93-156">The `ns` and `prefix` parameters are specified as strings, and the input to provide for these parameters corresponds to the definition of XAML namespace identifier and XAML namespace prefix as provided previously in this topic.</span></span>  
  
 <span data-ttu-id="3bb93-157">Pokud zkoumáte informace oboru názvů XAML jako součást datového proudu uzlu XAML nebo prostřednictvím jiného přístupu k systému typů XAML, <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> nahlásí identifikátor oboru názvů XAML a <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> vypíše předponu oboru názvů XAML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-157">If you are examining XAML namespace information as part of a XAML node stream or through other access to the XAML type system, <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> reports the XAML namespace identifier, and <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> reports the XAML namespace prefix.</span></span>  
  
 <span data-ttu-id="3bb93-158">V datovém proudu uzlu XAML se mohou informace o oboru názvů XAML zobrazit jako uzel XAML, který předchází entitě, na kterou se vztahuje.</span><span class="sxs-lookup"><span data-stu-id="3bb93-158">In a XAML node stream, the XAML namespace information can appear as a XAML node that precedes the entity to which it applies.</span></span> <span data-ttu-id="3bb93-159">To zahrnuje případy, kdy informace oboru názvů XAML předchází `StartObject` kořenového prvku jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-159">This includes cases where the XAML namespace information precedes the `StartObject` of the XAML root element.</span></span> <span data-ttu-id="3bb93-160">Další informace najdete v tématu [Principy struktur a konceptů datového proudu uzlu XAML](understanding-xaml-node-stream-structures-and-concepts.md).</span><span class="sxs-lookup"><span data-stu-id="3bb93-160">For more information, see [Understanding XAML Node Stream Structures and Concepts](understanding-xaml-node-stream-structures-and-concepts.md).</span></span>  
  
 <span data-ttu-id="3bb93-161">Pro mnoho scénářů, které používají .NET Framework rozhraní API služby XAML, musí existovat alespoň jedna deklarace oboru názvů jazyka XAML a deklarace musí buď obsahovat, nebo odkazovat na informace, které jsou vyžadovány kontextem schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-161">For many scenarios that use .NET Framework XAML Services API, at least one XAML namespace declaration is expected to exist, and the declaration must either contain or refer to information that is required by a XAML schema context.</span></span> <span data-ttu-id="3bb93-162">Obory názvů XAML musí buď určovat sestavení, která mají být načtena, nebo pomáhat při řešení specifických typů v oborech názvů a sestaveních, které jsou již načteny nebo známy v kontextu schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-162">The XAML namespaces must either specify assemblies to be loaded, or assist in resolving specific types within namespaces and assemblies that are already loaded or known by the XAML schema context.</span></span>  
  
 <span data-ttu-id="3bb93-163">Aby bylo možné generovat datový proud uzlu XAML, musí být k dispozici informace o typu XAML prostřednictvím kontextu schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-163">In order to generate a XAML node stream, XAML type information must be available, through the XAML schema context.</span></span> <span data-ttu-id="3bb93-164">Informace o typu XAML nelze určit bez prvotního určení odpovídajícího oboru názvů XAML pro každý uzel, který chcete vytvořit.</span><span class="sxs-lookup"><span data-stu-id="3bb93-164">The XAML type information cannot be determined without first determining the relevant XAML namespace for each node to create.</span></span> <span data-ttu-id="3bb93-165">V tomto okamžiku nejsou ještě vytvořeny žádné instance typů, ale kontext schématu XAML může potřebovat vyhledat informace z definujícího sestavení a typu zálohování.</span><span class="sxs-lookup"><span data-stu-id="3bb93-165">At this point, no instances of types are created yet, but the XAML schema context may need to look up information from the defining assembly and backing type.</span></span> <span data-ttu-id="3bb93-166">Například pro zpracování `<Party><PartyFavor/></Party>`značek musí být kontext schématu XAML schopný určit název a typ `ContentProperty` `Party`, a tak musí také znát informace oboru názvů XAML pro `Party` a `PartyFavor`.</span><span class="sxs-lookup"><span data-stu-id="3bb93-166">For example, in order to process the markup `<Party><PartyFavor/></Party>`, the XAML schema context must be able to determine the name and type of the `ContentProperty` of `Party`, and thus also must know the XAML namespace information for `Party` and `PartyFavor`.</span></span> <span data-ttu-id="3bb93-167">V případě výchozího kontextu schématu XAML, statické reflexe sestaví většinu systémových informací typu XAML, které jsou potřebné pro generování uzlů typu XAML v proudu uzlů.</span><span class="sxs-lookup"><span data-stu-id="3bb93-167">In the case of the default XAML schema context, static reflection reports much of the XAML type system information that is needed to generate XAML type nodes in the node stream.</span></span>  
  
 <span data-ttu-id="3bb93-168">Aby bylo možné generovat graf objektů z datového proudu uzlu XAML, musí existovat deklarace oboru názvů XAML pro každou předponu XAML použitou v původním kódu a zaznamenanou v datovém proudu uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="3bb93-168">In order to generate an object graph from a XAML node stream, XAML namespace declarations must exist for each XAML prefix used in the original markup and recorded in the XAML node stream.</span></span> <span data-ttu-id="3bb93-169">V tomto okamžiku jsou vytvářeny instance a dojde k chování mapování typu true.</span><span class="sxs-lookup"><span data-stu-id="3bb93-169">At this point, instances are being created, and true type-mapping behavior occurs.</span></span>  
  
 <span data-ttu-id="3bb93-170">Pokud potřebujete předem naplnit informace oboru názvů XAML, v případech, kdy se obor názvů XAML, který chcete použít pro použití kontextu schématu XAML, nedefinuje v kódu, jedna technika, kterou můžete použít, je deklarovat deklarace oboru názvů XML v <xref:System.Xml.XmlParserContext> pro <xref:System.Xml.XmlReader>.</span><span class="sxs-lookup"><span data-stu-id="3bb93-170">If you need to prepopulate XAML namespace information, in cases where the XAML namespace you intend the XAML schema context to use is not defined in the markup, one technique you can use is to declare XML namespace declarations in the <xref:System.Xml.XmlParserContext> for an <xref:System.Xml.XmlReader>.</span></span> <span data-ttu-id="3bb93-171">Pak použijte tento <xref:System.Xml.XmlReader> jako vstup pro konstruktor čtečky XAML nebo <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="3bb93-171">Then use that <xref:System.Xml.XmlReader> as input for a XAML reader constructor, or <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="3bb93-172">Druhé rozhraní API, které je relevantní pro zpracování oboru názvů XAML v .NET Framework služby XAML, jsou atributy <xref:System.Windows.Markup.XmlnsDefinitionAttribute> a <xref:System.Windows.Markup.XmlnsPrefixAttribute>.</span><span class="sxs-lookup"><span data-stu-id="3bb93-172">Two other API that are relevant for XAML namespace handling in .NET Framework XAML Services are the attributes <xref:System.Windows.Markup.XmlnsDefinitionAttribute> and <xref:System.Windows.Markup.XmlnsPrefixAttribute>.</span></span> <span data-ttu-id="3bb93-173">Tyto atributy se vztahují na sestavení.</span><span class="sxs-lookup"><span data-stu-id="3bb93-173">These attributes apply to assemblies.</span></span> <span data-ttu-id="3bb93-174"><xref:System.Windows.Markup.XmlnsDefinitionAttribute> používá kontext schématu XAML k interpretaci jakékoli deklarace oboru názvů XAML, která obsahuje identifikátor URI.</span><span class="sxs-lookup"><span data-stu-id="3bb93-174"><xref:System.Windows.Markup.XmlnsDefinitionAttribute> is used by a XAML schema context to interpret any XAML namespace declaration that includes a URI.</span></span> <span data-ttu-id="3bb93-175"><xref:System.Windows.Markup.XmlnsPrefixAttribute> jsou používány pomocí nástrojů, které generují XAML tak, aby určitý obor názvů XAML mohl být serializován s předvídatelné předponou.</span><span class="sxs-lookup"><span data-stu-id="3bb93-175"><xref:System.Windows.Markup.XmlnsPrefixAttribute> is used by tools that emit XAML so that a particular XAML namespace can be serialized with a predictable prefix.</span></span> <span data-ttu-id="3bb93-176">Další informace naleznete v tématu [atributy CLR související s jazykem XAML pro vlastní typy a knihovny](xaml-related-clr-attributes-for-custom-types-and-libraries.md).</span><span class="sxs-lookup"><span data-stu-id="3bb93-176">For more information, see [XAML-Related CLR Attributes for Custom Types and Libraries](xaml-related-clr-attributes-for-custom-types-and-libraries.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="3bb93-177">Viz také:</span><span class="sxs-lookup"><span data-stu-id="3bb93-177">See also</span></span>

- [<span data-ttu-id="3bb93-178">Principy struktur a koncepcí streamu uzlů XAML</span><span class="sxs-lookup"><span data-stu-id="3bb93-178">Understanding XAML Node Stream Structures and Concepts</span></span>](understanding-xaml-node-stream-structures-and-concepts.md)
