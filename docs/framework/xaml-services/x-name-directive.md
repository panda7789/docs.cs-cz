---
title: x:Name – direktiva
ms.date: 03/30/2017
f1_keywords:
- x:Name
- xName
- Name
helpviewer_keywords:
- x:Name attribute [XAML Services]
- XAML [XAML Services], x:Name attribute
- Name attribute in XAML [XAML Services]
ms.assetid: b7e61222-e8cf-48d2-acd0-6df3b7685d48
ms.openlocfilehash: 08594d9757596eed470ffba8b5b63a01c493c358
ms.sourcegitcommit: 76a304c79a32aa13889ebcf4b9789a4542b48e3e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/14/2018
ms.locfileid: "45583632"
---
# <a name="xname-directive"></a><span data-ttu-id="abb43-102">x:Name – direktiva</span><span class="sxs-lookup"><span data-stu-id="abb43-102">x:Name Directive</span></span>
<span data-ttu-id="abb43-103">Jednoznačně identifikuje elementy XAML definované v XAML namescope.</span><span class="sxs-lookup"><span data-stu-id="abb43-103">Uniquely identifies XAML-defined elements in a XAML namescope.</span></span> <span data-ttu-id="abb43-104">Obory názvů XAML a jejich modelů jedinečnost lze použít instance objektů, rozhraní poskytují rozhraní API nebo implementaci chování, které přistupují k graf objektu XAML vytvoří za běhu.</span><span class="sxs-lookup"><span data-stu-id="abb43-104">XAML namescopes and their uniqueness models can be applied to the instantiated objects, when frameworks provide APIs or implement behaviors that access the XAML-created object graph at run time.</span></span>  
  
## <a name="xaml-attribute-usage"></a><span data-ttu-id="abb43-105">Použití atributu XAML</span><span class="sxs-lookup"><span data-stu-id="abb43-105">XAML Attribute Usage</span></span>  
  
```xaml  
<object x:Name="XAMLNameValue".../>  
```  
  
## <a name="xaml-values"></a><span data-ttu-id="abb43-106">Hodnoty XAML</span><span class="sxs-lookup"><span data-stu-id="abb43-106">XAML Values</span></span>  
  
|||  
|-|-|  
|`XAMLNameValue`|<span data-ttu-id="abb43-107">Řetězec, který odpovídá omezení [xamlname – gramatika](../../../docs/framework/xaml-services/xamlname-grammar.md).</span><span class="sxs-lookup"><span data-stu-id="abb43-107">A string that conforms to the restrictions of the [XamlName Grammar](../../../docs/framework/xaml-services/xamlname-grammar.md).</span></span>|  
  
## <a name="remarks"></a><span data-ttu-id="abb43-108">Poznámky</span><span class="sxs-lookup"><span data-stu-id="abb43-108">Remarks</span></span>  
 <span data-ttu-id="abb43-109">Po `x:Name` platí pro rozhraní uživatele zálohování programovací model, název je ekvivalentní k proměnné, která obsahuje odkaz na objekt nebo instance vrácená rozhraním konstruktor.</span><span class="sxs-lookup"><span data-stu-id="abb43-109">After `x:Name` is applied to a framework's backing programming model, the name is equivalent to the variable that holds an object reference or an instance as returned by a constructor.</span></span>  
  
 <span data-ttu-id="abb43-110">Hodnota `x:Name` direktivy využití musí být jedinečný v rámci XAML namescope.</span><span class="sxs-lookup"><span data-stu-id="abb43-110">The value of an `x:Name` directive usage must be unique within a XAML namescope.</span></span> <span data-ttu-id="abb43-111">Ve výchozím nastavení při použití rozhraní .NET Framework XAML Services API primární obor namescope XAML je definován na kořenový prvek XAML jedné výroby XAML a zahrnuje elementy, které jsou součástí výroby XAML.</span><span class="sxs-lookup"><span data-stu-id="abb43-111">By default when used by .NET Framework XAML Services API, the primary XAML namescope is defined at the XAML root element of a single XAML production, and encompasses the elements that are contained in that XAML production.</span></span> <span data-ttu-id="abb43-112">Další samostatné obory názvů XAML, které může dojít v rámci jednoho produkční XAML lze definovat podle platformy, které se vztahují k určité scénářům.</span><span class="sxs-lookup"><span data-stu-id="abb43-112">Additional discrete XAML namescopes that might occur within a single XAML production can be defined by frameworks to address specific scenarios.</span></span> <span data-ttu-id="abb43-113">Například v WPF nové obory názvů XAML jsou definovány a vytvořil všechny šablony, která je také definováno v XAML výroby.</span><span class="sxs-lookup"><span data-stu-id="abb43-113">For example, in WPF, new XAML namescopes are defined and created by any template that is also defined on that XAML production.</span></span> <span data-ttu-id="abb43-114">Další informace o obory názvů XAML (ale relevantní pro mnoho konceptů namescope XAML napsané pro WPF), najdete v části [obory názvů WPF XAML](../../../docs/framework/wpf/advanced/wpf-xaml-namescopes.md).</span><span class="sxs-lookup"><span data-stu-id="abb43-114">For more information about XAML namescopes (written for WPF but relevant for many XAML namescope concepts), see [WPF XAML Namescopes](../../../docs/framework/wpf/advanced/wpf-xaml-namescopes.md).</span></span>  
  
 <span data-ttu-id="abb43-115">Obecně platí `x:Name` neměla používat v situacích, které používají také `x:Key`.</span><span class="sxs-lookup"><span data-stu-id="abb43-115">In general, `x:Name` should not be applied in situations that also use `x:Key`.</span></span> <span data-ttu-id="abb43-116">Implementace XAML pomocí konkrétní stávajících architektur zavedli nahrazení koncepty mezi `x:Key` a `x:Name`, ale toto není doporučený postup.</span><span class="sxs-lookup"><span data-stu-id="abb43-116">XAML implementations by specific existing frameworks have introduced substitution concepts between `x:Key` and `x:Name`, but that is not a recommended practice.</span></span> <span data-ttu-id="abb43-117">Rozhraní .NET framework XAML Services nepodporuje tyto koncepty nahrazení při zpracování informací o název a klíč jako <xref:System.Windows.Markup.INameScope> nebo <xref:System.Windows.Markup.DictionaryKeyPropertyAttribute>.</span><span class="sxs-lookup"><span data-stu-id="abb43-117">.NET Framework XAML Services does not support such substitution concepts when handling name/key information such as <xref:System.Windows.Markup.INameScope> or <xref:System.Windows.Markup.DictionaryKeyPropertyAttribute>.</span></span>  
  
 <span data-ttu-id="abb43-118">Pravidla pro permittance z `x:Name` a také vynucení jedinečnost názvu jsou potenciálně definovaném konkrétní implementace rozhraní.</span><span class="sxs-lookup"><span data-stu-id="abb43-118">Rules for permittance of `x:Name` as well as the name uniqueness enforcement are potentially defined by specific implementing frameworks.</span></span> <span data-ttu-id="abb43-119">Však možné používat s technologií .NET Framework XAML Services, definice rozhraní framework jedinečnosti obor namescope XAML by měl být konzistentní s definicí <xref:System.Windows.Markup.INameScope> informace v této dokumentaci a o tom, kam by měly používat stejná pravidla informace se použijí.</span><span class="sxs-lookup"><span data-stu-id="abb43-119">However, to be usable with .NET Framework XAML Services, the framework definitions of XAML namescope uniqueness should be consistent with the definition of <xref:System.Windows.Markup.INameScope> information in this documentation, and should use the same rules regarding where the information is applied.</span></span> <span data-ttu-id="abb43-120">Například [!INCLUDE[TLA#tla_winclient](../../../includes/tlasharptla-winclient-md.md)] implementace rozdělí různé prvky kódu na samostatném <xref:System.Windows.NameScope> rozsahy adres, jako jsou slovníky prostředků, logického stromu vytvořené úrovni stránky XAML, šablony a další odložené obsahu a potom vynucuje XAML jedinečnost názvu v rámci každé z těchto obory názvů XAML.</span><span class="sxs-lookup"><span data-stu-id="abb43-120">For example, the [!INCLUDE[TLA#tla_winclient](../../../includes/tlasharptla-winclient-md.md)] implementation divides various markup elements into separate <xref:System.Windows.NameScope> ranges, such as resource dictionaries, the logical tree created by the page-level XAML, templates, and other deferred content, and then enforces XAML name uniqueness within each of those XAML namescopes.</span></span>  
  
 <span data-ttu-id="abb43-121">Pro vlastní typy, které používají rozhraní .NET Framework XAML Services XAML objekt zapisovače, vlastnost, která se mapuje na `x:Name` na typ lze vytvořit nebo změnit.</span><span class="sxs-lookup"><span data-stu-id="abb43-121">For custom types that use .NET Framework XAML Services XAML object writers, a property that maps to `x:Name` on a type can be established or changed.</span></span> <span data-ttu-id="abb43-122">Toto chování můžete definovat pomocí odkazu na název vlastnosti pro mapování s <xref:System.Windows.Markup.RuntimeNamePropertyAttribute> v kódu definice typu.</span><span class="sxs-lookup"><span data-stu-id="abb43-122">You define this behavior by referencing the name of the property to map with the <xref:System.Windows.Markup.RuntimeNamePropertyAttribute> in the type definition code.</span></span>  <span data-ttu-id="abb43-123"><xref:System.Windows.Markup.RuntimeNamePropertyAttribute> je atribut úrovni typu.</span><span class="sxs-lookup"><span data-stu-id="abb43-123"><xref:System.Windows.Markup.RuntimeNamePropertyAttribute> is a type-level attribute.</span></span>  
  
 <span data-ttu-id="abb43-124">Using.NET Framework XAML Services základní logiku pro podporu namescope XAML lze definovat v rámci jazykově neutrální způsob implementací <xref:System.Windows.Markup.INameScope> rozhraní.</span><span class="sxs-lookup"><span data-stu-id="abb43-124">Using.NET Framework XAML Services, the backing logic for XAML namescope support can be defined in a framework-neutral way by implementing the <xref:System.Windows.Markup.INameScope> interface.</span></span>  
  
## <a name="wpf-usage-notes"></a><span data-ttu-id="abb43-125">Poznámky k použití WPF</span><span class="sxs-lookup"><span data-stu-id="abb43-125">WPF Usage Notes</span></span>  
 <span data-ttu-id="abb43-126">V části standard sestavení [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] aplikaci, která používá XAML, částečné třídy a modelu code-behind, zadaný `x:Name` stane názvem pole, které se vytvoří v základním kódu při [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] zpracovává značky Úloha kompilace sestavení a toto pole obsahuje odkaz na objekt.</span><span class="sxs-lookup"><span data-stu-id="abb43-126">Under the standard build configuration for a [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] application that uses XAML, partial classes, and code-behind, the specified `x:Name` becomes the name of a field that is created in the underlying code when [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] is processed by a markup compilation build task, and that field holds a reference to the object.</span></span> <span data-ttu-id="abb43-127">Ve výchozím nastavení je interní vytvořené pole.</span><span class="sxs-lookup"><span data-stu-id="abb43-127">By default, the created field is internal.</span></span> <span data-ttu-id="abb43-128">Přístup k poli můžete změnit zadáním [x: fieldmodifier – atribut](../../../docs/framework/xaml-services/x-fieldmodifier-directive.md).</span><span class="sxs-lookup"><span data-stu-id="abb43-128">You can change field access by specifying the [x:FieldModifier attribute](../../../docs/framework/xaml-services/x-fieldmodifier-directive.md).</span></span> <span data-ttu-id="abb43-129">V WPF a Silverlight sekvence je, že definuje značky kompilace a názvy polí v částečné třídy, ale hodnota je původně prázdné.</span><span class="sxs-lookup"><span data-stu-id="abb43-129">In WPF and Silverlight, the sequence is that the markup compile defines and names the field in a partial class, but the value is initially empty.</span></span> <span data-ttu-id="abb43-130">Potom vytvořena metoda s názvem `InitializeComponent` je volána v rámci konstruktoru třídy.</span><span class="sxs-lookup"><span data-stu-id="abb43-130">Then, a generated method named `InitializeComponent` is called from within the class constructor.</span></span> <span data-ttu-id="abb43-131">`InitializeComponent` se skládá z `FindName` volá pomocí všech `x:Name` vstupní hodnoty, které existují v součástí definice XAML částečné třídy jako řetězce.</span><span class="sxs-lookup"><span data-stu-id="abb43-131">`InitializeComponent` consists of `FindName` calls using each of the `x:Name` values that exist in the XAML-defined part of the partial class as input strings.</span></span> <span data-ttu-id="abb43-132">Vrácené hodnoty jsou pak přiřadí odkaz s názvem jako pole k vyplnění pole hodnot s objekty, které byly vytvořeny z XAML analýza kódu.</span><span class="sxs-lookup"><span data-stu-id="abb43-132">The return values are then assigned to the like-named field reference to fill the field values with objects that were created from XAML parsing.</span></span> <span data-ttu-id="abb43-133">Provedení příkazu `InitializeComponent` umožňují odkaz na objekt grafu běhu pomocí `x:Name` / název pole přímo, namísto nutnosti volat `FindName` explicitně kdykoli potřebujete odkaz na objekt definice XAML.</span><span class="sxs-lookup"><span data-stu-id="abb43-133">The execution of `InitializeComponent` make it possible to reference the run time object graph using the `x:Name` / field name directly, rather than having to call `FindName` explicitly any time you need a reference to a XAML-defined object.</span></span>  
  
 <span data-ttu-id="abb43-134">Pro WPF aplikace, která používá Microsoft Visual Basicu cílí a zahrnuje soubory XAML s `Page` akci sestavení, samostatné referenční vlastnost se vytvoří během kompilaci, která se přidá `WithEvents` – klíčové slovo na všechny elementy, které mají `x:Name`pro zajištění podpory `Handles` syntaxe pro delegáty obsluhy událostí.</span><span class="sxs-lookup"><span data-stu-id="abb43-134">For a WPF application that uses the Microsoft Visual Basic targets and includes XAML files with `Page` build action, a separate reference property is created during compilation that adds the `WithEvents` keyword to all elements that have an `x:Name`, to support `Handles` syntax for event handler delegates.</span></span> <span data-ttu-id="abb43-135">Tato vlastnost je vždy přístup public.</span><span class="sxs-lookup"><span data-stu-id="abb43-135">This property is always public.</span></span> <span data-ttu-id="abb43-136">Další informace najdete v tématu [jazyka Visual Basic a WPF zpracování událostí](../../../docs/framework/wpf/advanced/visual-basic-and-wpf-event-handling.md).</span><span class="sxs-lookup"><span data-stu-id="abb43-136">For more information, see [Visual Basic and WPF Event Handling](../../../docs/framework/wpf/advanced/visual-basic-and-wpf-event-handling.md).</span></span>  
  
 <span data-ttu-id="abb43-137">`x:Name` procesor WPF XAML se použije k registraci názvu do XAML namescope v okamžiku načtení, dokonce i pro případy, kdy stránky není kód zkompilován s akcí sestavení (například volný XAML slovníku prostředků).</span><span class="sxs-lookup"><span data-stu-id="abb43-137">`x:Name` is used by the WPF XAML processor to register a name into a XAML namescope at load time, even for cases where the page is not markup-compiled by build actions (for example, loose XAML of a resource dictionary).</span></span> <span data-ttu-id="abb43-138">Jedním z důvodů pro toto chování je vzhledem k tomu, `x:Name` je můžou být potřebné pro <xref:System.Windows.Data.Binding.ElementName%2A> vazby.</span><span class="sxs-lookup"><span data-stu-id="abb43-138">One reason for this behavior is because the `x:Name` is potentially needed for <xref:System.Windows.Data.Binding.ElementName%2A> binding.</span></span> <span data-ttu-id="abb43-139">Podrobnosti najdete v tématu [přehled datových vazeb](../../../docs/framework/wpf/data/data-binding-overview.md).</span><span class="sxs-lookup"><span data-stu-id="abb43-139">For details, see [Data Binding Overview](../../../docs/framework/wpf/data/data-binding-overview.md).</span></span>  
  
 <span data-ttu-id="abb43-140">Jak už bylo zmíněno dříve, `x:Name` (nebo `Name`) neměla používat v situacích, které používají také `x:Key`.</span><span class="sxs-lookup"><span data-stu-id="abb43-140">As mentioned previously, `x:Name` (or `Name`) should not be applied in situations that also use `x:Key`.</span></span> <span data-ttu-id="abb43-141">[!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] <xref:System.Windows.ResourceDictionary> Má zvláštní chování samotné definice jako obor namescope XAML, ale vrací Neimplementováno nebo hodnoty null pro <xref:System.Windows.Markup.INameScope> rozhraní API jako způsob, jak vynucovat toto chování.</span><span class="sxs-lookup"><span data-stu-id="abb43-141">The [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] <xref:System.Windows.ResourceDictionary> has a special behavior of defining itself as a XAML namescope but returning Not Implemented or null values for <xref:System.Windows.Markup.INameScope> APIs as a way to enforce this behavior.</span></span> <span data-ttu-id="abb43-142">Pokud analyzátor WPF XAML zaznamená `Name` nebo `x:Name` v XAML definované <xref:System.Windows.ResourceDictionary>, název není přidán do jakékoli obor namescope XAML.</span><span class="sxs-lookup"><span data-stu-id="abb43-142">If the WPF XAML parser encounters `Name` or `x:Name` in a XAML-defined <xref:System.Windows.ResourceDictionary>, the name is not added to any XAML namescope.</span></span> <span data-ttu-id="abb43-143">Pokusu o nalezení tento název ze libovolný obor namescope XAML a `FindName` metody nebude vrátí platné výsledky.</span><span class="sxs-lookup"><span data-stu-id="abb43-143">Attempting to find that name from any XAML namescope and the `FindName` methods will not return valid results.</span></span>  
  
### <a name="xname-and-name"></a><span data-ttu-id="abb43-144">x: Name a název</span><span class="sxs-lookup"><span data-stu-id="abb43-144">x:Name and Name</span></span>  
 <span data-ttu-id="abb43-145">Mnoho scénářů aplikaci WPF se můžete vyhnout použitím této `x:Name` atribut, protože `Name` vlastnost závislosti jako zadaný ve výchozím oboru názvů XAML pro několik důležitých základních tříd, jako <xref:System.Windows.FrameworkElement> a <xref:System.Windows.FrameworkContentElement> splňuje stejný účel.</span><span class="sxs-lookup"><span data-stu-id="abb43-145">Many WPF application scenarios can avoid any use of the `x:Name` attribute, because the `Name` dependency property as specified in the default XAML namespace for several of the important base classes such as <xref:System.Windows.FrameworkElement> and <xref:System.Windows.FrameworkContentElement> satisfies this same purpose.</span></span> <span data-ttu-id="abb43-146">Dál jsou uvedeny některé obvyklé scénáře XAML a WPF kde kód přístup k prvku bez `Name` vlastnosti na úrovni rozhraní framework je důležité.</span><span class="sxs-lookup"><span data-stu-id="abb43-146">There are still some common XAML and WPF scenarios where code access to an element with no `Name` property at the framework level is important.</span></span> <span data-ttu-id="abb43-147">Například některé animace a scénáře třídy, které podporují nepodporují `Name` vlastnost, ale často potřebují nejde odkazovat v kódu, aby bylo možné řídit animace.</span><span class="sxs-lookup"><span data-stu-id="abb43-147">For example, certain animation and storyboard support classes do not support a `Name` property, but they often need to be referenced in code in order to control the animation.</span></span> <span data-ttu-id="abb43-148">Je třeba zadat `x:Name` jako atribut časové osy a transformace, které jsou vytvořeny v XAML, pokud chcete později odkazovat z kódu.</span><span class="sxs-lookup"><span data-stu-id="abb43-148">You should specify `x:Name` as an attribute on timelines and transforms that are created in XAML, if you intend to reference them from code later.</span></span>  
  
 <span data-ttu-id="abb43-149">Pokud <xref:System.Windows.FrameworkElement.Name%2A> je k dispozici jako vlastnost ve třídě, <xref:System.Windows.FrameworkElement.Name%2A> a `x:Name` zaměnitelné jako atributy, ale pokud se zadají obě jsou u stejného elementu se dojít k výjimce analýzy.</span><span class="sxs-lookup"><span data-stu-id="abb43-149">If <xref:System.Windows.FrameworkElement.Name%2A> is available as a property on the class, <xref:System.Windows.FrameworkElement.Name%2A> and `x:Name` can be used interchangeably as attributes, but a parse exception will result if both are specified on the same element.</span></span> <span data-ttu-id="abb43-150">Pokud XAML je kompilaci kódu, výjimka dojde při kompilaci kódu, jinak proběhne zatížení.</span><span class="sxs-lookup"><span data-stu-id="abb43-150">If the XAML is markup compiled, the exception will occur on the markup compile, otherwise it occurs on load.</span></span>  
  
 <span data-ttu-id="abb43-151"><xref:System.Windows.FrameworkElement.Name%2A> lze nastavit pomocí syntaxe atributu XAML a v kódu pomocí <xref:System.Windows.DependencyObject.SetValue%2A>; však toto nastavení <xref:System.Windows.FrameworkElement.Name%2A> vlastností v kódu nevytvoří odkaz reprezentativní pole v rámci XAML namescope ve většině případů, ve kterém je již XAML načíst.</span><span class="sxs-lookup"><span data-stu-id="abb43-151"><xref:System.Windows.FrameworkElement.Name%2A> can be set using XAML attribute syntax, and in code using <xref:System.Windows.DependencyObject.SetValue%2A>; note however that setting the <xref:System.Windows.FrameworkElement.Name%2A> property in code does not create the representative field reference within the XAML namescope in most circumstances where the XAML is already loaded.</span></span> <span data-ttu-id="abb43-152">Namísto pokusu o nastavení <xref:System.Windows.FrameworkElement.Name%2A> v kódu, použijte <xref:System.Windows.NameScope> metody z kódu proti odpovídající obor namescope.</span><span class="sxs-lookup"><span data-stu-id="abb43-152">Instead of attempting to set <xref:System.Windows.FrameworkElement.Name%2A> in code, use <xref:System.Windows.NameScope> methods from code, against the appropriate namescope.</span></span>  
  
 <span data-ttu-id="abb43-153"><xref:System.Windows.FrameworkElement.Name%2A> Můžete také nastavit pomocí syntax prvku vlastnosti vnitřního ovládacího prvku, ale to je běžné.</span><span class="sxs-lookup"><span data-stu-id="abb43-153"><xref:System.Windows.FrameworkElement.Name%2A> can also be set using property element syntax with inner text, but that is uncommon.</span></span> <span data-ttu-id="abb43-154">Naproti tomu `x:Name` nelze nastavit v syntax prvku vlastnosti XAML, nebo v kódu pomocí <xref:System.Windows.DependencyObject.SetValue%2A>; ho lze nastavit pouze pomocí syntaxe atributu na objekty, protože se jedná direktivu.</span><span class="sxs-lookup"><span data-stu-id="abb43-154">In contrast, `x:Name` cannot be set in XAML property element syntax, or in code using <xref:System.Windows.DependencyObject.SetValue%2A>; it can only be set using attribute syntax on objects because it is a directive.</span></span>  
  
## <a name="silverlight-usage-notes"></a><span data-ttu-id="abb43-155">Poznámky k použití aplikace Silverlight</span><span class="sxs-lookup"><span data-stu-id="abb43-155">Silverlight Usage Notes</span></span>  
 <span data-ttu-id="abb43-156">`x:Name` pro prostředí Silverlight je zdokumentován samostatně.</span><span class="sxs-lookup"><span data-stu-id="abb43-156">`x:Name` for Silverlight is documented separately.</span></span> <span data-ttu-id="abb43-157">Další informace najdete v tématu [Namespace XAML (x:) Funkce jazyka (Silverlight)](https://go.microsoft.com/fwlink/?LinkId=199081).</span><span class="sxs-lookup"><span data-stu-id="abb43-157">For more information, see [XAML Namespace (x:) Language Features (Silverlight)](https://go.microsoft.com/fwlink/?LinkId=199081).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="abb43-158">Viz také</span><span class="sxs-lookup"><span data-stu-id="abb43-158">See Also</span></span>  
 <xref:System.Windows.FrameworkElement.Name%2A?displayProperty=nameWithType>  
 <xref:System.Windows.FrameworkContentElement.Name%2A?displayProperty=nameWithType>  
 [<span data-ttu-id="abb43-159">Stromy v subsystému WPF</span><span class="sxs-lookup"><span data-stu-id="abb43-159">Trees in WPF</span></span>](../../../docs/framework/wpf/advanced/trees-in-wpf.md)
