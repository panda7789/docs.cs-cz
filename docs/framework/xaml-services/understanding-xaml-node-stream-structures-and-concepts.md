---
title: Principy struktur a koncepcí datových proudů uzlů XAML
ms.date: 03/30/2017
helpviewer_keywords:
- XAML node streams [XAML Services]
- nodes [XAML Services], XAML node stream
- XAML [XAML Services], XAML node streams
ms.assetid: 7c11abec-1075-474c-9d9b-778e5dab21c3
ms.openlocfilehash: c873961982cd1642d8b354e5d77b06105c0b7a1e
ms.sourcegitcommit: 30a83efb57c468da74e9e218de26cf88d3254597
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/20/2019
ms.locfileid: "68364304"
---
# <a name="understanding-xaml-node-stream-structures-and-concepts"></a><span data-ttu-id="38217-102">Principy struktur a koncepcí datových proudů uzlů XAML</span><span class="sxs-lookup"><span data-stu-id="38217-102">Understanding XAML Node Stream Structures and Concepts</span></span>

<span data-ttu-id="38217-103">Čtečky XAML a zapisovače XAML, jak jsou implementovány v .NET Framework služby XAML, jsou založeny na konceptu návrhu datového proudu uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-103">XAML readers and XAML writers as implemented in .NET Framework XAML Services are based on the design concept of a XAML node stream.</span></span> <span data-ttu-id="38217-104">Datový proud uzlu XAML je koncepční sada uzlů XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-104">The XAML node stream is a conceptualization of a set of XAML nodes.</span></span> <span data-ttu-id="38217-105">V tomto principu provede procesor XAML strukturu vztahů uzlů v jazyce XAML v jednom okamžiku.</span><span class="sxs-lookup"><span data-stu-id="38217-105">In this conceptualization, a XAML processor walks through the structure of the node relationships in the XAML one at a time.</span></span> <span data-ttu-id="38217-106">V otevřeném datovém proudu uzlu XAML již existuje pouze jeden aktuální záznam nebo aktuální pozice, přičemž mnoho aspektů rozhraní API hlásí pouze informace, které jsou z této pozice k dispozici.</span><span class="sxs-lookup"><span data-stu-id="38217-106">At any time, only one current record or current position exists in an open XAML node stream, and many aspects of the API report only the information available from that position.</span></span> <span data-ttu-id="38217-107">Aktuální uzel v datovém proudu uzlu XAML lze popsat jako objekt, člen nebo hodnotu.</span><span class="sxs-lookup"><span data-stu-id="38217-107">The current node in a XAML node stream can be described as being an object, a member, or a value.</span></span> <span data-ttu-id="38217-108">Čtečky XAML mohou komunikovat s moduly pro zápis XAML a povolit programu zobrazení, interakci s nebo změnou obsahu datového proudu uzlu XAML během cesty načtení nebo operace uložení cesty, která zahrnuje XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-108">By treating XAML as a XAML node stream, XAML readers can communicate with XAML writers and enable a program to view, interact with, or alter the contents of a XAML node stream during either a load path or a save path operation that involves XAML.</span></span> <span data-ttu-id="38217-109">Návrh rozhraní API pro čtení a zápis v XAML a koncept datového proudu uzlu XAML se podobá předchozím souvisejícím designům a návrhům pro [!INCLUDE[TLA#tla_xmldom](../../../includes/tlasharptla-xmldom-md.md)] zápis a <xref:System.Xml.XmlReader> k <xref:System.Xml.XmlWriter> konceptům, jako jsou třídy a a.</span><span class="sxs-lookup"><span data-stu-id="38217-109">XAML reader and writer API design and the XAML node stream concept are similar to previous related reader and writer designs and concepts, such as the [!INCLUDE[TLA#tla_xmldom](../../../includes/tlasharptla-xmldom-md.md)] and the <xref:System.Xml.XmlReader> and <xref:System.Xml.XmlWriter> classes.</span></span> <span data-ttu-id="38217-110">Toto téma popisuje koncepty pro streamy uzlu XAML a popisuje, jak můžete napsat rutiny, které pracují s reprezentacemi XAML na úrovni uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-110">This topic discusses XAML node stream concepts and describes how you can write routines that interact with XAML representations at the XAML node level.</span></span>

<a name="loading_into_a_xaml_reader"></a>

## <a name="loading-xaml-into-a-xaml-reader"></a><span data-ttu-id="38217-111">Načtení XAML do čtečky XAML</span><span class="sxs-lookup"><span data-stu-id="38217-111">Loading XAML into a XAML Reader</span></span>

<span data-ttu-id="38217-112">Základní <xref:System.Xaml.XamlReader> třída nedeklaruje konkrétní techniku pro načtení počátečního XAML do čtečky XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-112">The base <xref:System.Xaml.XamlReader> class does not declare a particular technique for loading the initial XAML into a XAML reader.</span></span> <span data-ttu-id="38217-113">Namísto toho odvozená třída deklaruje a implementuje techniku načítání, včetně obecných vlastností a omezení jejich vstupního zdroje pro jazyk XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-113">Instead, a derived class declares and implements the loading technique, including the general characteristics and constraints of its input source for XAML.</span></span> <span data-ttu-id="38217-114">Například <xref:System.Xaml.XamlObjectReader> přečte graf objektu, počínaje zdrojem vstupu jednoho objektu, který představuje kořen nebo základní.</span><span class="sxs-lookup"><span data-stu-id="38217-114">For example, a <xref:System.Xaml.XamlObjectReader> reads an object graph, starting from the input source of a single object that represents the root or base.</span></span> <span data-ttu-id="38217-115"><xref:System.Xaml.XamlObjectReader> Pak vytvoří datový proud uzlu XAML z grafu objektu.</span><span class="sxs-lookup"><span data-stu-id="38217-115">The <xref:System.Xaml.XamlObjectReader> then produces a XAML node stream from the object graph.</span></span>

<span data-ttu-id="38217-116">Nejvýraznější .NET Framework služby XAML – definovaná <xref:System.Xaml.XamlReader> podtřída je. <xref:System.Xaml.XamlXmlReader></span><span class="sxs-lookup"><span data-stu-id="38217-116">The most prominent .NET Framework XAML Services–defined <xref:System.Xaml.XamlReader> subclass is <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="38217-117"><xref:System.Xaml.XamlXmlReader>Načte počáteční kód XAML buď načtením textového souboru přímo pomocí datového proudu nebo cesty k souboru, nebo nepřímo prostřednictvím související třídy <xref:System.IO.TextReader>čtenář, jako je například.</span><span class="sxs-lookup"><span data-stu-id="38217-117"><xref:System.Xaml.XamlXmlReader> loads the initial XAML, either by loading a text file directly through a stream or file path, or indirectly through a related reader class such as <xref:System.IO.TextReader>.</span></span> <span data-ttu-id="38217-118">Lze <xref:System.Xaml.XamlReader> si představit jako obsahující celou vstupní zdroj XAML poté, co byl načten.</span><span class="sxs-lookup"><span data-stu-id="38217-118">The <xref:System.Xaml.XamlReader> can be thought of as containing the entirety of the XAML input source after it has loaded.</span></span> <span data-ttu-id="38217-119"><xref:System.Xaml.XamlReader> Základní rozhraní API je však navrženo tak, aby čtenář komunikoval s jedním uzlem XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-119">However, the <xref:System.Xaml.XamlReader> base API is designed so that the reader is interacting with a single node of the XAML.</span></span> <span data-ttu-id="38217-120">Při prvním načtení je prvním jedním uzlem, ke kterému se setkáte, kořenem XAML a jeho počátečním objektem.</span><span class="sxs-lookup"><span data-stu-id="38217-120">When first loaded, the first single node you encounter is the root of the XAML, and its start object.</span></span>

### <a name="the-xaml-node-stream-concept"></a><span data-ttu-id="38217-121">Koncept datového proudu uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="38217-121">The XAML Node Stream Concept</span></span>

<span data-ttu-id="38217-122">Pokud jste všeobecně obeznámeni s modelem DOM, metafora stromu nebo přístupem založenými na dotazech k přístupu k technologiím založeným na jazyce XML, je užitečný způsob, jak conceptualize datový proud uzlu XAML, je následující.</span><span class="sxs-lookup"><span data-stu-id="38217-122">If you are generally more familiar with a DOM, tree metaphor, or query-based approach towards accessing XML-based technologies, a helpful way to conceptualize a XAML node stream is as follows.</span></span> <span data-ttu-id="38217-123">Představte si, že načtený kód XAML je model DOM nebo strom, ve kterém je každý možný uzel rozbalený celým způsobem a následně zobrazený lineárně.</span><span class="sxs-lookup"><span data-stu-id="38217-123">Imagine that the loaded XAML is a DOM or a tree where every possible node is expanded all the way, and then presented linearly.</span></span> <span data-ttu-id="38217-124">Při přechodu mezi uzly můžete přecházet "in" nebo "out" úrovní, které by byly relevantní pro model DOM, ale datový proud uzlu XAML explicitně nesleduje, protože tyto koncepty úrovně nejsou relevantní pro datový proud uzlu.</span><span class="sxs-lookup"><span data-stu-id="38217-124">As you advance through the nodes, you might be traversing "in" or "out" of levels that would be relevant to a DOM, but the XAML node stream does not explicitly keep track because these level concepts are not relevant to a node stream.</span></span> <span data-ttu-id="38217-125">Datový proud uzlu má "aktuální" pozici, ale pokud jste sami neuložili jiné části streamu jako odkazy, každý aspekt streamu uzlu, který je jiný než aktuální pozice uzlu, je mimo zobrazení.</span><span class="sxs-lookup"><span data-stu-id="38217-125">The node stream has a "current" position, but unless you have stored other parts of the stream yourself as references, every aspect of the node stream other than the current node position is out of view.</span></span>

<span data-ttu-id="38217-126">Koncept datového proudu uzlu XAML má významnou výhodu, že pokud procházíte celým datovým proudem uzlu, máte jistotu, že jste zpracovali celé vyjádření reprezentace v jazyce XAML; Nemusíte se obávat, že dotaz, operace modelu DOM nebo nějaký jiný nelineární přístup ke zpracování informací neobsahují část úplné reprezentace XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-126">The XAML node stream concept has the notable advantage that if you go through the entire node stream, you are assured that you have processed the entire XAML representation; you do not need to worry that a query, a DOM operation, or some other nonlinear approach to processing information has missed some part of the complete XAML representation.</span></span> <span data-ttu-id="38217-127">Z tohoto důvodu je reprezentace datového proudu uzlu XAML ideální pro připojení čtenářů XAML a zapisovačů XAML a pro poskytnutí systému, kde můžete vložit vlastní proces, který funguje mezi fázemi čtení a zápisu operace zpracování XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-127">For this reason, the XAML node stream representation is ideal both for connecting XAML readers and XAML writers, and for providing a system where you can insert your own process that acts between the read and write phases of a XAML processing operation.</span></span> <span data-ttu-id="38217-128">V mnoha případech je pořadí uzlů v datovém proudu uzlu XAML záměrně optimalizováno nebo přeobjednáno čtečkami XAML a jak se může pořadí zobrazit ve zdrojovém textu, binárním souboru nebo grafu objektů.</span><span class="sxs-lookup"><span data-stu-id="38217-128">In many cases, the ordering of nodes in the XAML node stream is deliberately optimized or reordered by XAML readers versus how the order might appear in the source text, binary, or object graph.</span></span> <span data-ttu-id="38217-129">Toto chování je určeno k vykonání architektury zpracování XAML, kdy zapisovače XAML nejsou nikdy v pozici, kde se musí v proudu uzlů vrátit zpět.</span><span class="sxs-lookup"><span data-stu-id="38217-129">This behavior is intended to enforce a XAML processing architecture whereby XAML writers are never in a position where they have to go "back" in the node stream.</span></span> <span data-ttu-id="38217-130">V ideálním případě by měly být všechny operace zápisu XAML schopny pracovat na kontextu schématu a na aktuální pozici datového proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="38217-130">Ideally, all XAML write operations should be able to act based on schema context plus the current position of the node stream.</span></span>

<a name="a_basic_reading_node_loop"></a>

## <a name="a-basic-reading-node-loop"></a><span data-ttu-id="38217-131">Základní smyčka uzlu čtení</span><span class="sxs-lookup"><span data-stu-id="38217-131">A Basic Reading Node Loop</span></span>

<span data-ttu-id="38217-132">Základní smyčka uzlu čtení pro zkoumání datového proudu uzlu XAML se skládá z následujících konceptů.</span><span class="sxs-lookup"><span data-stu-id="38217-132">A basic reading node loop for examining a XAML node stream consists of the following concepts.</span></span> <span data-ttu-id="38217-133">Pro účely smyček uzlů, jak je popsáno v tomto tématu, Předpokládejme, že čtete textový soubor XAML čitelný pomocí <xref:System.Xaml.XamlXmlReader>.</span><span class="sxs-lookup"><span data-stu-id="38217-133">For purposes of node loops as discussed in this topic, assume that you are reading a text-based, human-readable XAML file using <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="38217-134">Odkazy v této části odkazují na konkrétní rozhraní API smyčky uzlu XAML implementované <xref:System.Xaml.XamlXmlReader>.</span><span class="sxs-lookup"><span data-stu-id="38217-134">The links in this section refer to the particular XAML node loop API implemented by <xref:System.Xaml.XamlXmlReader>.</span></span>

- <span data-ttu-id="38217-135">Ujistěte se, že nejste na konci datového proudu uzlu XAML (Zkontrolujte <xref:System.Xaml.XamlXmlReader.IsEof%2A>nebo <xref:System.Xaml.XamlXmlReader.Read%2A> použijte návratovou hodnotu).</span><span class="sxs-lookup"><span data-stu-id="38217-135">Make sure that you are not at the end of the XAML node stream (check <xref:System.Xaml.XamlXmlReader.IsEof%2A>, or use the <xref:System.Xaml.XamlXmlReader.Read%2A> return value).</span></span> <span data-ttu-id="38217-136">Pokud jste na konci streamu, neexistuje žádný aktuální uzel a měli byste skončit.</span><span class="sxs-lookup"><span data-stu-id="38217-136">If you are at the end of the stream, there is no current node and you should exit.</span></span>

- <span data-ttu-id="38217-137">Ověřte, jaký typ uzlu datový proud uzlu XAML aktuálně zpřístupňuje voláním <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span><span class="sxs-lookup"><span data-stu-id="38217-137">Check what type of node the XAML node stream currently exposes by calling <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span>

- <span data-ttu-id="38217-138">Máte-li přidružený zapisovač objektů XAML, který je připojen přímo, obecně se na <xref:System.Xaml.XamlWriter.WriteNode%2A> tento bod volá.</span><span class="sxs-lookup"><span data-stu-id="38217-138">If you have an associated XAML object writer that is connected directly, you generally call <xref:System.Xaml.XamlWriter.WriteNode%2A> at this point.</span></span>

- <span data-ttu-id="38217-139">Na základě toho <xref:System.Xaml.XamlNodeType> , který je hlášen jako aktuální uzel nebo aktuální záznam, zavolejte jednu z následujících hodnot pro získání informací o obsahu uzlu:</span><span class="sxs-lookup"><span data-stu-id="38217-139">Based on which <xref:System.Xaml.XamlNodeType> is reported as the current node or current record, call one of the following to obtain information about the node contents:</span></span>

  - <span data-ttu-id="38217-140"><xref:System.Xaml.XamlXmlReader.NodeType%2A> Pro<xref:System.Xaml.XamlNodeType.EndMember>nebo volejte volání<xref:System.Xaml.XamlXmlReader.Member%2A> pro získání<xref:System.Xaml.XamlMember>informacíočlenu. <xref:System.Xaml.XamlNodeType.StartMember></span><span class="sxs-lookup"><span data-stu-id="38217-140">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartMember> or <xref:System.Xaml.XamlNodeType.EndMember>, call <xref:System.Xaml.XamlXmlReader.Member%2A> to obtain <xref:System.Xaml.XamlMember> information about a member.</span></span> <span data-ttu-id="38217-141">Všimněte si, že člen může být <xref:System.Xaml.XamlDirective>, a proto nemusí být nutně běžným uživatelem definovaným členem předchozího objektu.</span><span class="sxs-lookup"><span data-stu-id="38217-141">Note that the member might be a <xref:System.Xaml.XamlDirective>, and thus might not necessarily be a conventional type-defined member of the preceding object.</span></span> <span data-ttu-id="38217-142">Například `x:Name` použití na objekt se zobrazí jako člen jazyka XAML, kde <xref:System.Xaml.XamlMember.IsDirective%2A> je hodnota true a <xref:System.Xaml.XamlMember.Name%2A> člen je `Name`, s jinými vlastnostmi, které označují, že tato direktiva je v oboru názvů XAML jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-142">For example, `x:Name` applied to an object appears as a XAML member where <xref:System.Xaml.XamlMember.IsDirective%2A> is true and the <xref:System.Xaml.XamlMember.Name%2A> of the member is `Name`, with other properties indicating that this directive is under the XAML language XAML namespace.</span></span>

  - <span data-ttu-id="38217-143"><xref:System.Xaml.XamlXmlReader.NodeType%2A> Pro<xref:System.Xaml.XamlNodeType.EndObject>nebo volejte volání<xref:System.Xaml.XamlXmlReader.Type%2A> k získání<xref:System.Xaml.XamlType>informacíoobjektu. <xref:System.Xaml.XamlNodeType.StartObject></span><span class="sxs-lookup"><span data-stu-id="38217-143">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartObject> or <xref:System.Xaml.XamlNodeType.EndObject>, call <xref:System.Xaml.XamlXmlReader.Type%2A> to obtain <xref:System.Xaml.XamlType> information about an object.</span></span>

  - <span data-ttu-id="38217-144">Pro, zavolejte<xref:System.Xaml.XamlXmlReader.Value%2A>. <xref:System.Xaml.XamlXmlReader.NodeType%2A> <xref:System.Xaml.XamlNodeType.Value></span><span class="sxs-lookup"><span data-stu-id="38217-144">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.Value>, call <xref:System.Xaml.XamlXmlReader.Value%2A>.</span></span> <span data-ttu-id="38217-145">Uzel je hodnota pouze v případě, že se jedná o nejjednodušší výraz hodnoty pro člena nebo inicializační text pro objekt (Nicméně byste měli být vědomi chování převodu typů, jak je popsáno v nadcházející části tohoto tématu).</span><span class="sxs-lookup"><span data-stu-id="38217-145">A node is a value only if it is the simplest expression of a value for a member, or the initialization text for an object (however, you should be aware of type conversion behavior as documented in an upcoming section of this topic).</span></span>

  - <span data-ttu-id="38217-146">Pro je volána volání metody <xref:System.Xaml.XamlXmlReader.Namespace%2A> získání informací o oboru názvů pro uzel oboru názvů. <xref:System.Xaml.XamlXmlReader.NodeType%2A> <xref:System.Xaml.XamlNodeType.NamespaceDeclaration></span><span class="sxs-lookup"><span data-stu-id="38217-146">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.NamespaceDeclaration>, call <xref:System.Xaml.XamlXmlReader.Namespace%2A> to obtain namespace information for a namespace node.</span></span>

- <span data-ttu-id="38217-147">Zavolejte <xref:System.Xaml.XamlXmlReader.Read%2A> k posunutí čtečky XAML na další uzel v datovém proudu uzlu XAML a opakujte kroky znovu.</span><span class="sxs-lookup"><span data-stu-id="38217-147">Call <xref:System.Xaml.XamlXmlReader.Read%2A> to advance the XAML reader to the next node in the XAML node stream, and repeat the steps again.</span></span>

<span data-ttu-id="38217-148">Datový proud uzlu XAML, který poskytuje .NET Framework XAML Services XAML Readers vždy poskytuje úplný a hlubokou procházejí všechny možné uzly.</span><span class="sxs-lookup"><span data-stu-id="38217-148">The XAML node stream provided by .NET Framework XAML Services XAML readers always provides a full, deep traversal of all possible nodes.</span></span> <span data-ttu-id="38217-149">Typické techniky řízení toku pro smyčku uzlu XAML zahrnují definování těla v rámci `while (reader.Read())`a přechod na <xref:System.Xaml.XamlXmlReader.NodeType%2A> každý bod uzlu ve smyčce uzlu.</span><span class="sxs-lookup"><span data-stu-id="38217-149">Typical flow-control techniques for a XAML node loop include defining a body within `while (reader.Read())`, and switching on <xref:System.Xaml.XamlXmlReader.NodeType%2A> at each node point in the node loop.</span></span>

<span data-ttu-id="38217-150">Pokud je datový proud uzlu na konci souboru, aktuální uzel je null.</span><span class="sxs-lookup"><span data-stu-id="38217-150">If the node stream is at end of file, the current node is null.</span></span>

<span data-ttu-id="38217-151">Nejjednodušší smyčka, která používá čtecí modul a zapisovač, se podobá následujícímu příkladu.</span><span class="sxs-lookup"><span data-stu-id="38217-151">The simplest loop that uses a reader and writer resembles the following example.</span></span>

```csharp
XamlXmlReader xxr = new XamlXmlReader(new StringReader(xamlStringToLoad));
//where xamlStringToLoad is a string of well formed XAML
XamlObjectWriter xow = new XamlObjectWriter(xxr.SchemaContext);
while (xxr.Read()) {
  xow.WriteNode(xxr);
}
```

<span data-ttu-id="38217-152">Tento základní příklad smyčky uzlů jazyka XAML pro načtení je transparentně připojen ke čtečce XAML a zapisovači XAML, takže nic neliší od toho, pokud <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>jste použili.</span><span class="sxs-lookup"><span data-stu-id="38217-152">This basic example of a load path XAML node loop transparently connects the XAML reader and XAML writer, doing nothing different than if you had used <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="38217-153">Tato základní struktura se pak rozbalí, aby se mohla vztahovat na váš scénář čtení nebo zápisu.</span><span class="sxs-lookup"><span data-stu-id="38217-153">But this basic structure is then expanded to apply to your reading or writing scenario.</span></span> <span data-ttu-id="38217-154">Některé možné scénáře jsou následující:</span><span class="sxs-lookup"><span data-stu-id="38217-154">Some possible scenarios are as follows:</span></span>

- <span data-ttu-id="38217-155"><xref:System.Xaml.XamlXmlReader.NodeType%2A>Zapnout.</span><span class="sxs-lookup"><span data-stu-id="38217-155">Switch on <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span> <span data-ttu-id="38217-156">Provádění různých akcí v závislosti na tom, který typ uzlu je právě čten.</span><span class="sxs-lookup"><span data-stu-id="38217-156">Perform different actions depending on which node type is being read.</span></span>

- <span data-ttu-id="38217-157">Nevolejte <xref:System.Xaml.XamlWriter.WriteNode%2A> ve všech případech.</span><span class="sxs-lookup"><span data-stu-id="38217-157">Do not call <xref:System.Xaml.XamlWriter.WriteNode%2A> in all cases.</span></span> <span data-ttu-id="38217-158">Zavolá <xref:System.Xaml.XamlWriter.WriteNode%2A> se jenom <xref:System.Xaml.XamlXmlReader.NodeType%2A> v některých případech.</span><span class="sxs-lookup"><span data-stu-id="38217-158">Only call <xref:System.Xaml.XamlWriter.WriteNode%2A> in some <xref:System.Xaml.XamlXmlReader.NodeType%2A> cases.</span></span>

- <span data-ttu-id="38217-159">V rámci logiky pro konkrétní typ uzlu Analyzujte specifiky daného uzlu a na nich budou pracovat.</span><span class="sxs-lookup"><span data-stu-id="38217-159">Within the logic for a particular node type, analyze the specifics of that node and act on them.</span></span> <span data-ttu-id="38217-160">Můžete například zapsat pouze objekty, které pocházejí z konkrétního oboru názvů XAML, a pak vyřadit nebo odložit objekty, které nejsou z daného oboru názvů XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-160">For example, you could write only objects that come from a particular XAML namespace, and then drop or defer any objects not from that XAML namespace.</span></span> <span data-ttu-id="38217-161">Případně můžete vyřadit nebo jinak znovu zpracovat všechny direktivy jazyka XAML, které váš systém XAML nepodporuje jako součást zpracování členů.</span><span class="sxs-lookup"><span data-stu-id="38217-161">Or you could drop or otherwise reprocess any XAML directives that your XAML system does not support as part of your member processing.</span></span>

- <span data-ttu-id="38217-162">Definujte vlastní <xref:System.Xaml.XamlObjectWriter> přepsání `Write*` metod, případně proveďte mapování typů, které obchází kontext schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-162">Define a custom <xref:System.Xaml.XamlObjectWriter> that overrides `Write*` methods, possibly performing type mapping that bypasses XAML schema context.</span></span>

- <span data-ttu-id="38217-163"><xref:System.Xaml.XamlXmlReader> Vytvořte pro použití nevýchozího kontextu schématu XAML, aby byly přizpůsobené rozdíly v chování XAML použity čtenářem i zapisovačem.</span><span class="sxs-lookup"><span data-stu-id="38217-163">Construct the <xref:System.Xaml.XamlXmlReader> to use a nondefault XAML schema context, so that customized differences in XAML behavior are used both by the reader and the writer.</span></span>

### <a name="accessing-xaml-beyond-the-node-loop-concept"></a><span data-ttu-id="38217-164">Přístup k XAML za koncept smyčky uzlu</span><span class="sxs-lookup"><span data-stu-id="38217-164">Accessing XAML Beyond the Node Loop Concept</span></span>

<span data-ttu-id="38217-165">Existují potenciálně jiné způsoby, jak pracovat s reprezentacemi XAML jinou než jako smyčka uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-165">There are potentially other ways to work with a XAML representation other than as a XAML node loop.</span></span> <span data-ttu-id="38217-166">Například může existovat čtečka XAML, která může číst indexovaný uzel nebo konkrétně přistupuje k uzlům, a to `x:Name` `x:Uid`přímo pomocí, nebo prostřednictvím jiných identifikátorů.</span><span class="sxs-lookup"><span data-stu-id="38217-166">For example, there could exist a XAML reader that can read an indexed node, or in particular accesses nodes directly by `x:Name`, by `x:Uid`, or through other identifiers.</span></span> <span data-ttu-id="38217-167">.NET Framework služby XAML neposkytuje úplnou implementaci, ale poskytuje navrhovaný vzor prostřednictvím služeb a typů podpory.</span><span class="sxs-lookup"><span data-stu-id="38217-167">.NET Framework XAML Services does not provide a full implementation, but provides a suggested pattern through services and support types.</span></span> <span data-ttu-id="38217-168">Další informace naleznete v tématu <xref:System.Xaml.IXamlIndexingReader> a <xref:System.Xaml.XamlNodeList>.</span><span class="sxs-lookup"><span data-stu-id="38217-168">For more information, see <xref:System.Xaml.IXamlIndexingReader> and <xref:System.Xaml.XamlNodeList>.</span></span>

> [!TIP]
> <span data-ttu-id="38217-169">Microsoft taky vytvoří mimo IP síť, která je známá jako Microsoft XAML Toolkit.</span><span class="sxs-lookup"><span data-stu-id="38217-169">Microsoft also produces an out-of-band release known as the Microsoft XAML Toolkit.</span></span> <span data-ttu-id="38217-170">Tato verze mimo IP síť je stále ve fázích předběžného vydání.</span><span class="sxs-lookup"><span data-stu-id="38217-170">This out-of-band release is still in its pre-release stages.</span></span> <span data-ttu-id="38217-171">Nicméně pokud jste ochotni pracovat s předběžnými komponentami, Microsoft XAML Toolkit poskytuje některé zajímavé prostředky pro nástroje jazyka XAML a statickou analýzu jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-171">However, if you are willing to work with pre-release components, the Microsoft XAML Toolkit provides some interesting resources for XAML tooling and static analysis of XAML.</span></span> <span data-ttu-id="38217-172">Microsoft XAML Toolkit obsahuje rozhraní API XAML DOM, podporu analýzy FxCop a kontext schématu XAML pro Silverlight.</span><span class="sxs-lookup"><span data-stu-id="38217-172">The Microsoft XAML Toolkit includes a XAML DOM API, support for FxCop analysis, and a XAML schema context for Silverlight.</span></span> <span data-ttu-id="38217-173">Další informace najdete v tématu [Microsoft XAML Toolkit](https://code.msdn.microsoft.com/XAML).</span><span class="sxs-lookup"><span data-stu-id="38217-173">For more information, see [Microsoft XAML Toolkit](https://code.msdn.microsoft.com/XAML).</span></span>

<a name="working_with_the_current_node"></a>

## <a name="working-with-the-current-node"></a><span data-ttu-id="38217-174">Práce s aktuálním uzlem</span><span class="sxs-lookup"><span data-stu-id="38217-174">Working with the Current Node</span></span>

<span data-ttu-id="38217-175">Většina scénářů, které používají smyčku uzlu XAML, nečtou pouze uzly.</span><span class="sxs-lookup"><span data-stu-id="38217-175">Most scenarios that use a XAML node loop do not only read the nodes.</span></span> <span data-ttu-id="38217-176">Většina scénářů zpracovává aktuální uzly a projde každý uzel po jednom do implementace <xref:System.Xaml.XamlWriter>.</span><span class="sxs-lookup"><span data-stu-id="38217-176">Most scenarios process current nodes and pass each node one at a time to an implementation of <xref:System.Xaml.XamlWriter>.</span></span>

<span data-ttu-id="38217-177">V obvyklém scénáři <xref:System.Xaml.XamlXmlReader> zatížení vytvoří datový proud uzlu XAML; uzly XAML jsou zpracovány podle vaší logiky a kontextu schématu XAML a uzly jsou předány <xref:System.Xaml.XamlObjectWriter>do.</span><span class="sxs-lookup"><span data-stu-id="38217-177">In the typical load path scenario, a <xref:System.Xaml.XamlXmlReader> produces a XAML node stream; the XAML nodes are processed according to your logic and XAML schema context; and the nodes are passed to a <xref:System.Xaml.XamlObjectWriter>.</span></span> <span data-ttu-id="38217-178">Následně Integrujte výsledný graf objektů do vaší aplikace nebo architektury.</span><span class="sxs-lookup"><span data-stu-id="38217-178">You then integrate the resulting object graph into your application or framework.</span></span>

<span data-ttu-id="38217-179">V typickém scénáři <xref:System.Xaml.XamlObjectReader> uložení cesty přečte graf objektů, jednotlivé uzly XAML <xref:System.Xaml.XamlXmlWriter> a výstup serializovaného výsledku jako textový soubor XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-179">In a typical save path scenario, a <xref:System.Xaml.XamlObjectReader> reads the object graph, individual XAML nodes are processed, and a <xref:System.Xaml.XamlXmlWriter> outputs the serialized result as a XAML text file.</span></span> <span data-ttu-id="38217-180">Klíč spočívá v tom, že jak cesty i scénáře zahrnují práci s právě jedním uzlem jazyka XAML v čase, a uzly XAML jsou k dispozici pro zpracování standardizovaným způsobem, který je definován pomocí systému typů XAML a rozhraní API služby the.NET Framework XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-180">The key is that both paths and scenarios involve working with exactly one XAML node at a time, and the XAML nodes are available for treatment in a standardized way that is defined by the XAML type system and the.NET Framework XAML Services APIs.</span></span>

### <a name="frames-and-scope"></a><span data-ttu-id="38217-181">Rámce a rozsah</span><span class="sxs-lookup"><span data-stu-id="38217-181">Frames and Scope</span></span>

<span data-ttu-id="38217-182">Smyčka uzlu XAML projde datovým proudem uzlu XAML lineárním způsobem.</span><span class="sxs-lookup"><span data-stu-id="38217-182">A XAML node loop walks through a XAML node stream in a linear way.</span></span> <span data-ttu-id="38217-183">Datový proud uzlu se přesměruje do objektů, do členů, které obsahují jiné objekty atd.</span><span class="sxs-lookup"><span data-stu-id="38217-183">The node stream traverses into objects, into members that contain other objects, and so on.</span></span> <span data-ttu-id="38217-184">Je často vhodné sledovat obor v rámci datového proudu uzlu XAML implementací konceptu rámce a zásobníku.</span><span class="sxs-lookup"><span data-stu-id="38217-184">It is often useful to keep track of scope within the XAML node stream by implementing a frame and stack concept.</span></span> <span data-ttu-id="38217-185">To platí zejména v případě, že aktivně nastavujete datový proud uzlu, který je v něm.</span><span class="sxs-lookup"><span data-stu-id="38217-185">This is particularly true if you are actively adjusting the node stream while you are in it.</span></span> <span data-ttu-id="38217-186">Podpora rámce a zásobníku, kterou implementujete v rámci logiky smyčky uzlu, by mohla `StartObject` počítat ( `GetObject`nebo) `EndObject` a rozsahy během navýšení struktury uzlu XAML, pokud je struktura považovat z perspektivy modelu DOM.</span><span class="sxs-lookup"><span data-stu-id="38217-186">The frame and stack support that you implement as part of your node loop logic could count `StartObject` (or `GetObject`) and `EndObject` scopes as you descend into a XAML node structure if the structure is thought of from a DOM perspective.</span></span>

<a name="traversing_and_entering_object_nodes"></a>

## <a name="traversing-and-entering-object-nodes"></a><span data-ttu-id="38217-187">Procházení a zadávání uzlů objektů</span><span class="sxs-lookup"><span data-stu-id="38217-187">Traversing and Entering Object Nodes</span></span>

<span data-ttu-id="38217-188">První uzel v proudu uzlu, je-li otevřen čtečkou XAML, je uzel start-Object kořenového objektu.</span><span class="sxs-lookup"><span data-stu-id="38217-188">The first node in a node stream when it is opened by a XAML reader is the start-object node of the root object.</span></span> <span data-ttu-id="38217-189">Podle definice je tento objekt vždy jediným uzlem objektu a nemá žádné partnerské uzly.</span><span class="sxs-lookup"><span data-stu-id="38217-189">By definition, this object is always a single object node and has no peers.</span></span> <span data-ttu-id="38217-190">V jakémkoli reálném příkladu XAML je kořenový objekt definován tak, aby obsahoval jednu nebo více vlastností, které obsahují více objektů, a tyto vlastnosti mají členské uzly.</span><span class="sxs-lookup"><span data-stu-id="38217-190">In any real-world XAML example, the root object is defined to have one or more properties that hold more objects, and these properties have member nodes.</span></span> <span data-ttu-id="38217-191">Členské uzly pak mají jeden nebo více uzlů objektů nebo mohou také končit místo toho v uzlu hodnoty.</span><span class="sxs-lookup"><span data-stu-id="38217-191">The member nodes then have one or more object nodes, or might also terminate in a value node instead.</span></span> <span data-ttu-id="38217-192">Kořenový objekt obvykle definuje XAML obory názvů WPF, které jsou syntakticky přiřazeny jako atributy v kódu jazyka XAML, ale mapovány na `Namescope` typ uzlu v reprezentaci datového proudu uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-192">The root object typically defines XAML namescopes, which are syntactically assigned as attributes in the XAML text markup but map to a `Namescope` node type in the XAML node stream representation.</span></span>

<span data-ttu-id="38217-193">Vezměte v úvahu následující příklad XAML (Jedná se o libovolný kód XAML, který není zálohovaný existujícími typy v .NET Framework).</span><span class="sxs-lookup"><span data-stu-id="38217-193">Consider the following XAML example (this is arbitrary XAML, not backed by existing types in the .NET Framework).</span></span> <span data-ttu-id="38217-194">Předpokládejme, že v tomto objektovém `FavorCollection` modelu `List<T>` `Favor`je `Balloon` a `NoiseMaker` lze jej přiřadit k `Favor` `Balloon.Color` vlastnosti, která je zazálohována `Color` objektem podobným tomu, jak WPF definuje. barvy jako názvy známých barev a `Color` podporují konvertor typu pro syntaxi atributů.</span><span class="sxs-lookup"><span data-stu-id="38217-194">Assume that in this object model, `FavorCollection` is `List<T>` of `Favor`, `Balloon` and `NoiseMaker` are assignable to `Favor`, the `Balloon.Color` property is backed by a `Color` object similar to how WPF defines colors as known color names, and `Color` supports a type converter for attribute syntax.</span></span>

|<span data-ttu-id="38217-195">Kód XAML</span><span class="sxs-lookup"><span data-stu-id="38217-195">XAML markup</span></span>|<span data-ttu-id="38217-196">Výsledný datový proud uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="38217-196">Resulting XAML node stream</span></span>|
|-----------------|--------------------------------|
|`<Party`|<span data-ttu-id="38217-197">`Namespace`uzel pro`Party`</span><span class="sxs-lookup"><span data-stu-id="38217-197">`Namespace` node for `Party`</span></span>|
|`xmlns="PartyXamlNamespace">`|<span data-ttu-id="38217-198">`StartObject`uzel pro`Party`</span><span class="sxs-lookup"><span data-stu-id="38217-198">`StartObject` node for `Party`</span></span>|
|`<Party.Favors>`|<span data-ttu-id="38217-199">`StartMember`uzel pro`Party.Favors`</span><span class="sxs-lookup"><span data-stu-id="38217-199">`StartMember` node for `Party.Favors`</span></span>|
||<span data-ttu-id="38217-200">`StartObject`uzel pro implicitní`FavorCollection`</span><span class="sxs-lookup"><span data-stu-id="38217-200">`StartObject` node for implicit `FavorCollection`</span></span>|
||<span data-ttu-id="38217-201">`StartMember`uzel pro vlastnost `FavorCollection` implicitních položek</span><span class="sxs-lookup"><span data-stu-id="38217-201">`StartMember` node for implicit `FavorCollection` items property.</span></span>|
|`<Balloon`|<span data-ttu-id="38217-202">`StartObject`uzel pro`Balloon`</span><span class="sxs-lookup"><span data-stu-id="38217-202">`StartObject` node for `Balloon`</span></span>|
|`Color="Red"`|<span data-ttu-id="38217-203">`StartMember`uzel pro`Color`</span><span class="sxs-lookup"><span data-stu-id="38217-203">`StartMember` node for `Color`</span></span><br /><br /> <span data-ttu-id="38217-204">`Value`uzel pro řetězec hodnoty atributu`"Red"`</span><span class="sxs-lookup"><span data-stu-id="38217-204">`Value` node for the attribute value string `"Red"`</span></span><br /><br /> <span data-ttu-id="38217-205">`EndMember` pro `Color`</span><span class="sxs-lookup"><span data-stu-id="38217-205">`EndMember` for `Color`</span></span>|
|`HasHelium="True"`|<span data-ttu-id="38217-206">`StartMember`uzel pro`HasHelium`</span><span class="sxs-lookup"><span data-stu-id="38217-206">`StartMember` node for `HasHelium`</span></span><br /><br /> <span data-ttu-id="38217-207">`Value`uzel pro řetězec hodnoty atributu`"True"`</span><span class="sxs-lookup"><span data-stu-id="38217-207">`Value` node for the attribute value string `"True"`</span></span><br /><br /> <span data-ttu-id="38217-208">`EndMember` pro `HasHelium`</span><span class="sxs-lookup"><span data-stu-id="38217-208">`EndMember` for `HasHelium`</span></span>|
|`>`|<span data-ttu-id="38217-209">`EndObject` pro `Balloon`</span><span class="sxs-lookup"><span data-stu-id="38217-209">`EndObject` for `Balloon`</span></span>|
|`<NoiseMaker>Loudest</NoiseMaker>`|<span data-ttu-id="38217-210">`StartObject`uzel pro`NoiseMaker`</span><span class="sxs-lookup"><span data-stu-id="38217-210">`StartObject` node for `NoiseMaker`</span></span><br /><br /> <span data-ttu-id="38217-211">`StartMember`uzel pro`_Initialization`</span><span class="sxs-lookup"><span data-stu-id="38217-211">`StartMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="38217-212">`Value`uzel pro řetězec inicializační hodnoty`"Loudest"`</span><span class="sxs-lookup"><span data-stu-id="38217-212">`Value` node for the initialization value string `"Loudest"`</span></span><br /><br /> <span data-ttu-id="38217-213">`EndMember`uzel pro`_Initialization`</span><span class="sxs-lookup"><span data-stu-id="38217-213">`EndMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="38217-214">`EndObject` pro `NoiseMaker`</span><span class="sxs-lookup"><span data-stu-id="38217-214">`EndObject` for `NoiseMaker`</span></span>|
||<span data-ttu-id="38217-215">`EndMember`uzel pro vlastnost `FavorCollection` implicitních položek</span><span class="sxs-lookup"><span data-stu-id="38217-215">`EndMember` node for implicit `FavorCollection` items property.</span></span>|
||<span data-ttu-id="38217-216">`EndObject`uzel pro implicitní`FavorCollection`</span><span class="sxs-lookup"><span data-stu-id="38217-216">`EndObject` node for implicit `FavorCollection`</span></span>|
|`</Party.Favors>`|<span data-ttu-id="38217-217">`EndMember` pro `Favors`</span><span class="sxs-lookup"><span data-stu-id="38217-217">`EndMember` for `Favors`</span></span>|
|`</Party>`|<span data-ttu-id="38217-218">`EndObject` pro `Party`</span><span class="sxs-lookup"><span data-stu-id="38217-218">`EndObject` for `Party`</span></span>|

<span data-ttu-id="38217-219">V datovém proudu uzlu XAML můžete spoléhat na následující chování:</span><span class="sxs-lookup"><span data-stu-id="38217-219">In the XAML node stream, you can rely on the following behavior:</span></span>

- <span data-ttu-id="38217-220">Pokud uzel existuje, je přidán do datového proudu bezprostředně `StartObject` před `xmlns`deklarací, které jsou deklarovány v oboru názvů XAML. `Namespace`</span><span class="sxs-lookup"><span data-stu-id="38217-220">If a `Namespace` node exists, it is added to the stream immediately before the `StartObject` that declared the XAML namespace with `xmlns`.</span></span> <span data-ttu-id="38217-221">Prohlédněte si předchozí tabulku pomocí XAML a Ukázkový datový proud uzlu.</span><span class="sxs-lookup"><span data-stu-id="38217-221">Look at the previous table with the XAML and example node stream again.</span></span> <span data-ttu-id="38217-222">Všimněte si, `StartObject` jak `Namespace` se uzly a zdají být přemístění a jejich deklarace do značek textu.</span><span class="sxs-lookup"><span data-stu-id="38217-222">Notice how the `StartObject` and `Namespace` nodes seem to be transposed versus their declaration positions in text markup.</span></span> <span data-ttu-id="38217-223">To je zástupce chování, kde se uzly oboru názvů vždy zobrazují před uzlem, na které se vztahují v proudu uzlů.</span><span class="sxs-lookup"><span data-stu-id="38217-223">This is representative of the behavior where the namespace nodes always appear ahead of the node they apply to in the node stream.</span></span> <span data-ttu-id="38217-224">Účelem tohoto návrhu je, že informace oboru názvů jsou zásadní pro zapisovače objektů a musí být známé před tím, než se zapisovač objektů pokusí provést mapování typů nebo jinak zpracovat objekt.</span><span class="sxs-lookup"><span data-stu-id="38217-224">The purpose of this design is that the namespace information is vital to object writers and must be known before the object writer attempts to perform type mapping or otherwise process the object.</span></span> <span data-ttu-id="38217-225">Umístění informací o oboru názvů XAML před jeho rozsahem aplikace v datovém proudu usnadňuje vždy zpracování datového proudu uzlu v uvedeném pořadí.</span><span class="sxs-lookup"><span data-stu-id="38217-225">Placing the XAML namespace information ahead of its application scope in the stream makes it simpler to always process the node stream in its presented order.</span></span>

- <span data-ttu-id="38217-226">Vzhledem k výše uvedenému aspektu je jeden nebo více `Namespace` uzlů, které si přečtete poprvé ve většině případů v reálném čase při procházení uzlů od začátku, `StartObject` nikoli z kořene.</span><span class="sxs-lookup"><span data-stu-id="38217-226">Because of the above consideration, it is one or more `Namespace` nodes that you read first in most real-world markup cases when traversing nodes from the start, not the `StartObject` of the root.</span></span>

- <span data-ttu-id="38217-227">Za uzlem může `EndObject`následovat `Value`,nebobezprostřední `StartMember` `StartObject` .</span><span class="sxs-lookup"><span data-stu-id="38217-227">A `StartObject` node can be followed by `StartMember`, `Value`, or an immediate `EndObject`.</span></span> <span data-ttu-id="38217-228">Nikdy není následován jiným `StartObject`.</span><span class="sxs-lookup"><span data-stu-id="38217-228">It is never followed immediately by another `StartObject`.</span></span>

- <span data-ttu-id="38217-229">Může následovat a `StartObject`, `Value`nebo bezprostřední `EndMember`. `StartMember`</span><span class="sxs-lookup"><span data-stu-id="38217-229">A `StartMember` can be followed by a `StartObject`, `Value`, or an immediate `EndMember`.</span></span> <span data-ttu-id="38217-230">Může následovat `GetObject`za členy, kde hodnota by měla pocházet z existující hodnoty nadřazeného objektu, nikoli pomocí `StartObject` , který by vytvořil instanci nové hodnoty.</span><span class="sxs-lookup"><span data-stu-id="38217-230">It can be followed by `GetObject`, for members where the value is supposed to come from an existing value of the parent object rather than a `StartObject` that would instantiate a new value.</span></span> <span data-ttu-id="38217-231">Může být také následován `Namespace` uzlem, který se vztahuje na nadcházející. `StartObject`</span><span class="sxs-lookup"><span data-stu-id="38217-231">It can also be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span> <span data-ttu-id="38217-232">Nikdy není následován jiným `StartMember`.</span><span class="sxs-lookup"><span data-stu-id="38217-232">It is never followed immediately by another `StartMember`.</span></span>

- <span data-ttu-id="38217-233">`Value` Uzel představuje hodnotu samotnou; není k dispozici žádná "hodnota EndValue".</span><span class="sxs-lookup"><span data-stu-id="38217-233">A `Value` node represents the value itself; there is no "EndValue".</span></span> <span data-ttu-id="38217-234">Může následovat jenom `EndMember`.</span><span class="sxs-lookup"><span data-stu-id="38217-234">It can be followed only by an `EndMember`.</span></span>

  - <span data-ttu-id="38217-235">Inicializační text XAML objektu, který může být použit konstrukcí, nemá za následek strukturu objektů a hodnot.</span><span class="sxs-lookup"><span data-stu-id="38217-235">XAML initialization text of the object as might be used by construction does not result in an Object-Value structure.</span></span> <span data-ttu-id="38217-236">Místo toho je vytvořen vyhrazený členský uzel pro člena s `_Initialization` názvem.</span><span class="sxs-lookup"><span data-stu-id="38217-236">Instead, a dedicated member node for a member named `_Initialization` is created.</span></span> <span data-ttu-id="38217-237">a tento členský uzel obsahuje řetězec hodnoty inicializace.</span><span class="sxs-lookup"><span data-stu-id="38217-237">and that member node contains the initialization value string.</span></span> <span data-ttu-id="38217-238">Pokud existuje, `_Initialization` je vždy první `StartMember`.</span><span class="sxs-lookup"><span data-stu-id="38217-238">If it exists, `_Initialization` is always the first `StartMember`.</span></span> <span data-ttu-id="38217-239">`_Initialization`může být kvalifikován v některých reprezentacích služby XAML pomocí jazyka XAML namescope XAML pro objasnění, `_Initialization` že není definovaná vlastnost v typu zálohování.</span><span class="sxs-lookup"><span data-stu-id="38217-239">`_Initialization` may be qualified in some XAML services representations with the XAML language XAML namescope, to clarify that `_Initialization` is not a defined property in backing types.</span></span>

  - <span data-ttu-id="38217-240">Kombinace člen-hodnota představuje nastavení atributu hodnoty.</span><span class="sxs-lookup"><span data-stu-id="38217-240">A Member-Value combination represents an attribute setting of the value.</span></span> <span data-ttu-id="38217-241">Může se stát, že se do zpracování této hodnoty přinese konvertor hodnot a hodnota je prostý řetězec.</span><span class="sxs-lookup"><span data-stu-id="38217-241">There might eventually be a value converter involved in processing this value, and the value is a plain string.</span></span> <span data-ttu-id="38217-242">Nicméně to není vyhodnoceno, dokud modul pro zápis objektů XAML nezpracovává tento datový proud uzlu.</span><span class="sxs-lookup"><span data-stu-id="38217-242">However, that is not evaluated until a XAML object writer processes this node stream.</span></span> <span data-ttu-id="38217-243">Modul pro zápis objektů XAML má nezbytný kontext schématu XAML, typ mapování systému a další podporu potřebnou pro převody hodnot.</span><span class="sxs-lookup"><span data-stu-id="38217-243">The XAML object writer possesses the necessary XAML schema context, type system mapping, and other support needed for value conversions.</span></span>

- <span data-ttu-id="38217-244">Za uzlem může následovat `StartMember` uzel pro následného `EndObject` člena nebo uzel pro vlastníka člena. `EndMember`</span><span class="sxs-lookup"><span data-stu-id="38217-244">An `EndMember` node can be followed by a `StartMember` node for a subsequent member, or by an `EndObject` node for the member owner.</span></span>

- <span data-ttu-id="38217-245">Za uzlem může následovat `EndMember` uzel. `EndObject`</span><span class="sxs-lookup"><span data-stu-id="38217-245">An `EndObject` node can be followed by an `EndMember` node.</span></span> <span data-ttu-id="38217-246">Může být také následován `StartObject` uzlem pro případy, kdy objekty jsou partnerské uzly v položkách kolekce.</span><span class="sxs-lookup"><span data-stu-id="38217-246">It can also be followed by a `StartObject` node for cases where the objects are peers in a collection's items.</span></span> <span data-ttu-id="38217-247">Případně může následovat `Namespace` uzel, který se vztahuje na nadcházející `StartObject`.</span><span class="sxs-lookup"><span data-stu-id="38217-247">Or it can be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span>

  - <span data-ttu-id="38217-248">V případě jedinečného případu zavření celého datového proudu `EndObject` uzlu nenásleduje žádná z nich. čtenář je nyní konec souboru a <xref:System.Xaml.XamlReader.Read%2A> vrátí `false`.</span><span class="sxs-lookup"><span data-stu-id="38217-248">For the unique case of closing the entire node stream, the `EndObject` of the root is not followed by anything; the reader is now end-of-file, and <xref:System.Xaml.XamlReader.Read%2A> returns `false`.</span></span>

<a name="value_converters_and_the_xaml_node_stream"></a>

## <a name="value-converters-and-the-xaml-node-stream"></a><span data-ttu-id="38217-249">Převaděče hodnot a datový proud uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="38217-249">Value Converters and the XAML Node Stream</span></span>

<span data-ttu-id="38217-250">Konvertor hodnot je obecný termín pro rozšíření značek, konvertor typu (včetně serializátorů hodnot) nebo jiné vyhrazené třídy, která je hlášena jako konvertor hodnot pomocí systému typů XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-250">A value converter is a general term for a markup extension, a type converter (including value serializers) or another dedicated class that is reported as a value converter through the XAML type system.</span></span> <span data-ttu-id="38217-251">V datovém proudu uzlu XAML mají použití konvertoru typu a použití rozšíření značek velmi odlišné reprezentace.</span><span class="sxs-lookup"><span data-stu-id="38217-251">In the XAML node stream, a type converter usage and a markup extension usage have very different representations.</span></span>

### <a name="type-converters-in-the-xaml-node-stream"></a><span data-ttu-id="38217-252">Převaděče typů v datovém proudu uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="38217-252">Type Converters in the XAML Node Stream</span></span>

<span data-ttu-id="38217-253">Sada atributů, která nakonec má za následek, že použití konvertoru typu je hlášeno v datovém proudu uzlu XAML jako hodnota člena.</span><span class="sxs-lookup"><span data-stu-id="38217-253">An attribute set that eventually results in a type converter usage is reported in the XAML node stream as a value of a member.</span></span> <span data-ttu-id="38217-254">Datový proud uzlu XAML se nepokusí vytvořit objekt instance konvertoru typu a předat mu hodnotu.</span><span class="sxs-lookup"><span data-stu-id="38217-254">The XAML node stream does not attempt to produce a type converter instance object and pass the value to it.</span></span> <span data-ttu-id="38217-255">Použití implementace převodu konvertoru typu vyžaduje vyvolání kontextu schématu XAML a jeho použití pro mapování typů.</span><span class="sxs-lookup"><span data-stu-id="38217-255">Using a type converter's conversion implementation requires invoking the XAML schema context and using it for type-mapping.</span></span> <span data-ttu-id="38217-256">Dokonce i určení, která třída konvertoru typů by měla být použita ke zpracování hodnoty, vyžaduje nepřímý kontext schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-256">Even determining which type converter class should be used to process the value requires the XAML schema context indirectly.</span></span> <span data-ttu-id="38217-257">Použijete-li výchozí kontext schématu XAML, jsou tyto informace k dispozici v systému typů XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-257">When you use the default XAML schema context, that information is available from the XAML type system.</span></span> <span data-ttu-id="38217-258">Pokud potřebujete informace třídy konvertoru typu na úrovni datového proudu uzlu XAML před připojením k zapisovači XAML, můžete ho získat z <xref:System.Xaml.XamlMember> informací o nastavování člena.</span><span class="sxs-lookup"><span data-stu-id="38217-258">If you need the type converter class information at the XAML node stream level before connection to a XAML writer, you can obtain it from the <xref:System.Xaml.XamlMember> information of the member being set.</span></span> <span data-ttu-id="38217-259">V opačném případě by vstup konvertoru typu měl být zachován v datovém proudu uzlu XAML jako jednoduchá hodnota, dokud nebudou provedeny zbývající operace, které vyžadují systém mapování typů a kontext schématu XAML, například vytvoření objektu modulem pro zápis objektů XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-259">But otherwise, type converter input should be preserved in the XAML node stream as a plain value until the remainder of operations that require the type mapping system and XAML schema context are performed, for example the object creation by a XAML object writer.</span></span>

<span data-ttu-id="38217-260">Zvažte například následující osnova definice třídy a použití jazyka XAML:</span><span class="sxs-lookup"><span data-stu-id="38217-260">For example, consider the following class definition outline and XAML usage for it:</span></span>

```csharp
public class BoardSizeConverter : TypeConverter {
  //converts from string to an int[2] by splitting on an "x" char
}
public class GameBoard {
  [TypeConverter(typeof(BoardSizeConverter))]
  public int[] BoardSize; //2x2 array, initialization not shown
}
```

```xaml
<GameBoard BoardSize="8x8"/>
```

<span data-ttu-id="38217-261">Textová reprezentace datového proudu uzlu XAML pro toto použití může být vyjádřena jako následující:</span><span class="sxs-lookup"><span data-stu-id="38217-261">A text representation of the XAML node stream for this usage could be expressed as the following:</span></span>

<span data-ttu-id="38217-262">`StartObject`<xref:System.Xaml.XamlType> představují`GameBoard`</span><span class="sxs-lookup"><span data-stu-id="38217-262">`StartObject` with <xref:System.Xaml.XamlType> representing `GameBoard`</span></span>

<span data-ttu-id="38217-263">`StartMember`<xref:System.Xaml.XamlMember> představují`BoardSize`</span><span class="sxs-lookup"><span data-stu-id="38217-263">`StartMember` with <xref:System.Xaml.XamlMember> representing `BoardSize`</span></span>

<span data-ttu-id="38217-264">`Value`uzel s textovým řetězcem`8x8`""</span><span class="sxs-lookup"><span data-stu-id="38217-264">`Value` node, with text string "`8x8`"</span></span>

<span data-ttu-id="38217-265">`EndMember`vyhovují`BoardSize`</span><span class="sxs-lookup"><span data-stu-id="38217-265">`EndMember` matches `BoardSize`</span></span>

<span data-ttu-id="38217-266">`EndObject`vyhovují`GameBoard`</span><span class="sxs-lookup"><span data-stu-id="38217-266">`EndObject` matches `GameBoard`</span></span>

<span data-ttu-id="38217-267">Všimněte si, že v tomto datovém proudu uzlu není žádná instance převaděče typu.</span><span class="sxs-lookup"><span data-stu-id="38217-267">Notice that there is no type converter instance in this node stream.</span></span> <span data-ttu-id="38217-268">Můžete ale získat informace o převaděči typů voláním <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> metody <xref:System.Xaml.XamlMember> for `BoardSize`.</span><span class="sxs-lookup"><span data-stu-id="38217-268">But you can get type converter information by calling <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> on the <xref:System.Xaml.XamlMember> for `BoardSize`.</span></span> <span data-ttu-id="38217-269">Pokud máte platný kontext schématu XAML, můžete také vyvolat metody převaděče získáním instance z <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>.</span><span class="sxs-lookup"><span data-stu-id="38217-269">If you have a valid XAML schema context, you can also invoke the converter methods by obtaining an instance from <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>.</span></span>

### <a name="markup-extensions-in-the-xaml-node-stream"></a><span data-ttu-id="38217-270">Rozšíření značek v datovém proudu uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="38217-270">Markup Extensions in the XAML Node Stream</span></span>

<span data-ttu-id="38217-271">Použití rozšíření značek je hlášeno v proudu uzlu XAML jako uzel objektu v rámci člena, kde objekt představuje instanci rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="38217-271">A markup extension usage is reported in the XAML node stream as an object node within a member, where the object represents a markup extension instance.</span></span> <span data-ttu-id="38217-272">Proto je použití rozšíření značek v rámci reprezentace streamu uzlu v případě, že je to více, uvedeno v reprezentaci, než je použití konvertoru typu, a další informace.</span><span class="sxs-lookup"><span data-stu-id="38217-272">Thus a markup extension usage is presented more explicitly in the node stream representation than a type converter usage is, and carries more information.</span></span> <span data-ttu-id="38217-273"><xref:System.Xaml.XamlMember>informace nemohly získat informaci o rozšíření značek, protože použití je situace a se liší v každém možném případu s kódem. není vyhrazen a implicitně na typ nebo člen, jako je to případ s konvertory typů.</span><span class="sxs-lookup"><span data-stu-id="38217-273"><xref:System.Xaml.XamlMember> information could not have told you anything about the markup extension, because the usage is situational and varies in each possible markup case; it is not dedicated and implicit per type or member as is the case with type converters.</span></span>

<span data-ttu-id="38217-274">Datový proud uzlu reprezentace rozšíření značek jako uzly objektů je případ i v případě, že využití rozšíření značek bylo provedeno ve formě atributu v kódu textu XAML (což je často případ).</span><span class="sxs-lookup"><span data-stu-id="38217-274">The node stream representation of markup extensions as object nodes is the case even if the markup extension usage was made in attribute form in the XAML text markup (which is often the case).</span></span> <span data-ttu-id="38217-275">Použití rozšíření značek, které používaly explicitní formulář elementu Object, je ošetřeno stejným způsobem.</span><span class="sxs-lookup"><span data-stu-id="38217-275">Markup extension usages that used an explicit object element form are treated the same way.</span></span>

<span data-ttu-id="38217-276">V rámci uzlu objektu rozšíření značek mohou být členy této přípony označení.</span><span class="sxs-lookup"><span data-stu-id="38217-276">Within a markup extension object node, there may be members of that markup extension.</span></span> <span data-ttu-id="38217-277">Reprezentace datového proudu uzlu XAML zachovává použití této přípony označení, ať už se jedná o použití pozičního parametru, nebo použití s explicitními pojmenovanými parametry.</span><span class="sxs-lookup"><span data-stu-id="38217-277">The XAML node stream representation preserves the usage of that markup extension, whether that be a positional parameter usage or a usage with explicit named parameters.</span></span>

<span data-ttu-id="38217-278">Pro použití pozičního parametru obsahuje datový proud uzlu XAML vlastnost `_PositionalParameters` definovanou v jazyce XAML, která zaznamenává využití.</span><span class="sxs-lookup"><span data-stu-id="38217-278">For a positional parameter usage, the XAML node stream contains a XAML language-defined property `_PositionalParameters` that records the usage.</span></span> <span data-ttu-id="38217-279">Tato vlastnost je obecná <xref:System.Collections.Generic.List%601> s <xref:System.Object> omezením.</span><span class="sxs-lookup"><span data-stu-id="38217-279">This property is a generic <xref:System.Collections.Generic.List%601> with <xref:System.Object> constraint.</span></span> <span data-ttu-id="38217-280">Omezení je Object a ne String, protože použití pozičního parametru může obsahovat vnořené použití rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="38217-280">The constraint is object and not string because conceivably a positional parameter usage could contain nested markup extension usages within it.</span></span> <span data-ttu-id="38217-281">Chcete-li získat přístup k pozičním parametrům z použití, můžete iterovat v seznamu a použít indexery pro jednotlivé hodnoty seznamu.</span><span class="sxs-lookup"><span data-stu-id="38217-281">To access the positional parameters from the usage, you could iterate through the list and use the indexers for individual list values.</span></span>

<span data-ttu-id="38217-282">Pro použití pojmenovaných parametrů je každý pojmenovaný parametr reprezentován jako členský uzel daného názvu v datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="38217-282">For a named parameter usage, each named parameter is represented as a member node of that name in the node stream.</span></span> <span data-ttu-id="38217-283">Hodnoty členů nejsou nutně řetězcem, protože by mohlo dojít k použití vloženého rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="38217-283">The member values are not necessarily strings, because there could be a nested markup extension usage.</span></span>

<span data-ttu-id="38217-284">`ProvideValue`z rozšíření značek se ještě nevolá.</span><span class="sxs-lookup"><span data-stu-id="38217-284">`ProvideValue` from the markup extension is not yet invoked.</span></span> <span data-ttu-id="38217-285">Je však vyvolána, pokud připojíte čtečku XAML a zapisovač XAML, `WriteEndObject` takže je vyvolána v uzlu rozšíření značek při jeho kontrole v proudu uzlů.</span><span class="sxs-lookup"><span data-stu-id="38217-285">However, it is invoked if you connect a XAML reader and XAML writer so that `WriteEndObject` is invoked on the markup extension node when you examine it in the node stream.</span></span> <span data-ttu-id="38217-286">Z tohoto důvodu je obecně potřeba, aby byl k dispozici stejný kontext schématu XAML, jako by byl použit k vytvoření grafu objektu v cestě načtení.</span><span class="sxs-lookup"><span data-stu-id="38217-286">For this reason, you generally need the same XAML schema context available as would be used in order to form the object graph on the load path.</span></span> <span data-ttu-id="38217-287">V opačném případě může zevšechrozšířeníznačekvyvolatvýjimky,protoženejsoukdispoziciočekávanéslužby.`ProvideValue`</span><span class="sxs-lookup"><span data-stu-id="38217-287">Otherwise, `ProvideValue` from any markup extension can throw exceptions here, because it does not have expected services available.</span></span>

<a name="xaml_and_xml_languagedefined_members_in_the_xaml_node_stream"></a>

## <a name="xaml-and-xml-language-defined-members-in-the-xaml-node-stream"></a><span data-ttu-id="38217-288">Uživatelsky definované členy jazyka XAML a XML v datovém proudu uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="38217-288">XAML and XML Language-Defined Members in the XAML Node Stream</span></span>

<span data-ttu-id="38217-289">Někteří členové jsou představeni do datového proudu uzlu XAML z důvodu interpretace a konvence čtečky XAML namísto explicitního <xref:System.Xaml.XamlMember> vyhledávání nebo konstrukce.</span><span class="sxs-lookup"><span data-stu-id="38217-289">Certain members are introduced to a XAML node stream because of interpretations and conventions of a XAML reader, instead of through an explicit <xref:System.Xaml.XamlMember> lookup or construction.</span></span> <span data-ttu-id="38217-290">Tyto členy jsou často direktivy XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-290">Often, these members are XAML directives.</span></span> <span data-ttu-id="38217-291">V některých případech je to pro čtení jazyka XAML, který zavádí direktivu do datového proudu uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-291">In some cases, it is the act of reading the XAML that introduces the directive into the XAML node stream.</span></span> <span data-ttu-id="38217-292">Jinými slovy, původní vstupní text XAML nespecifikuje explicitně členskou direktivu, ale čtečka XAML vloží direktivu, aby splňovala strukturální konvence XAML a informace sestavy v proudu uzlu XAML před tím, než dojde ke ztrátě těchto informací.</span><span class="sxs-lookup"><span data-stu-id="38217-292">In other words, the original input XAML text did not explicitly specify the member directive, but the XAML reader inserts the directive in order to satisfy a structural XAML convention and report information in the XAML node stream before that information is lost.</span></span>

<span data-ttu-id="38217-293">Následující seznam obsahuje poznámky ke všem případům, kde se očekává, že čtečka XAML zavádí členský uzel jazyka XAML a jak je tento členský uzel identifikovaný v .NET Framework implementaci služby XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-293">The following list notes all cases where a XAML reader is expected to introduce a directive XAML member node, and how that member node is identified in the .NET Framework XAML Services implementations.</span></span>

- <span data-ttu-id="38217-294">**Inicializační text pro uzel objektu:** Název tohoto členského uzlu je `_Initialization`, představuje direktivu jazyka XAML a je definován v oboru názvů XAML jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-294">**Initialization text for an object node:** The name of this member node is `_Initialization`, it represents a XAML directive, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="38217-295">Můžete získat statickou entitu z <xref:System.Xaml.XamlLanguage.Initialization%2A>.</span><span class="sxs-lookup"><span data-stu-id="38217-295">You can get a static entity for it from <xref:System.Xaml.XamlLanguage.Initialization%2A>.</span></span>

- <span data-ttu-id="38217-296">**Poziční parametry pro rozšíření značek:** Název tohoto členského uzlu je `_PositionalParameters`a je definován v oboru názvů XAML jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-296">**Positional parameters for a markup extension:** The name of this member node is `_PositionalParameters`, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="38217-297">Vždy obsahuje obecný seznam objektů, z nichž každý je poziční parametr předem oddělený rozdělením na `,` znak oddělovače, který je zadán ve vstupním kódu XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-297">It always contains a generic list of objects, each of which is a positional parameter pre-separated by splitting on the `,` delimiter character as supplied in the input XAML.</span></span> <span data-ttu-id="38217-298">Můžete získat statickou entitu pro direktivu pozičních parametrů z <xref:System.Xaml.XamlLanguage.PositionalParameters%2A>.</span><span class="sxs-lookup"><span data-stu-id="38217-298">You can get a static entity for the positional parameters directive from <xref:System.Xaml.XamlLanguage.PositionalParameters%2A>.</span></span>

- <span data-ttu-id="38217-299">**Neznámý obsah:** Název tohoto členského uzlu je `_UnknownContent`.</span><span class="sxs-lookup"><span data-stu-id="38217-299">**Unknown content:** The name of this member node is `_UnknownContent`.</span></span> <span data-ttu-id="38217-300">Výhradně řečeno, <xref:System.Xaml.XamlDirective>je a je definována v oboru názvů XAML jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-300">Strictly speaking, it is a <xref:System.Xaml.XamlDirective>, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="38217-301">Tato direktiva se používá jako Sentinel pro případy, kdy prvek objektu XAML obsahuje obsah ve zdrojovém kódu XAML, ale nelze určit vlastnost Content v aktuálně dostupném kontextu schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-301">This directive is used as a sentinel for cases where a XAML object element contains content in the source XAML but no content property can be determined under the currently available XAML schema context.</span></span> <span data-ttu-id="38217-302">Tento případ můžete zjistit v datovém proudu uzlu XAML kontrolou členů s názvem `_UnknownContent`.</span><span class="sxs-lookup"><span data-stu-id="38217-302">You can detect this case in a XAML node stream by checking for members named `_UnknownContent`.</span></span> <span data-ttu-id="38217-303">Pokud není provedena žádná jiná akce v datovém proudu uzlu XAML zatížení, výchozí hodnota <xref:System.Xaml.XamlObjectWriter> je vyvolána, `WriteEndObject` `_UnknownContent` když dojde k pokusu o členství v jakémkoli objektu.</span><span class="sxs-lookup"><span data-stu-id="38217-303">If no other action is taken in a load path XAML node stream, the default <xref:System.Xaml.XamlObjectWriter> throws on attempted `WriteEndObject` when it encounters the `_UnknownContent` member on any object.</span></span> <span data-ttu-id="38217-304">Výchozí hodnota <xref:System.Xaml.XamlXmlWriter> nevyvolává a považuje člena za implicitní.</span><span class="sxs-lookup"><span data-stu-id="38217-304">The default <xref:System.Xaml.XamlXmlWriter> does not throw, and treats the member as implicit.</span></span> <span data-ttu-id="38217-305">Můžete získat statickou entitu pro `_UnknownContent` z. <xref:System.Xaml.XamlLanguage.UnknownContent%2A></span><span class="sxs-lookup"><span data-stu-id="38217-305">You can get a static entity for `_UnknownContent` from <xref:System.Xaml.XamlLanguage.UnknownContent%2A>.</span></span>

- <span data-ttu-id="38217-306">**Vlastnost kolekce kolekce:** I když je typ CLR pro třídu kolekce, který se používá pro XAML, obvykle vyhrazená pojmenovaná vlastnost, která obsahuje položky kolekce, tato vlastnost není před rozlišením typu back-Type známa v systému typů XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-306">**Collection property of a collection:** Although the backing CLR type of a collection class that is used for XAML usually has a dedicated named property that holds the collection items, that property is not known to a XAML type system prior to backing type resolution.</span></span> <span data-ttu-id="38217-307">Místo toho datový proud uzlu XAML zavádí `Items` zástupný symbol jako člen typu XAML kolekce.</span><span class="sxs-lookup"><span data-stu-id="38217-307">Instead, the XAML node stream introduces an `Items` placeholder as a member of the collection XAML type.</span></span> <span data-ttu-id="38217-308">V .NET Framework implementaci XAML Services je `_Items`název této direktivy/členu v datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="38217-308">In the .NET Framework XAML Services implementation the name of this directive / member in the node stream is `_Items`.</span></span> <span data-ttu-id="38217-309">Konstantu pro tuto direktivu lze získat z <xref:System.Xaml.XamlLanguage.Items%2A>.</span><span class="sxs-lookup"><span data-stu-id="38217-309">A constant for this directive can be obtained from <xref:System.Xaml.XamlLanguage.Items%2A>.</span></span>

    <span data-ttu-id="38217-310">Všimněte si, že datový proud uzlu XAML může obsahovat vlastnost Items s položkami, které nemusejí být analyzovatelné na základě rozlišení typu zálohování a kontextu schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-310">Note that a XAML node stream might contain an Items property with items that turn out to not be parsable based on the backing type resolution and XAML schema context.</span></span> <span data-ttu-id="38217-311">Například</span><span class="sxs-lookup"><span data-stu-id="38217-311">For example,</span></span>

- <span data-ttu-id="38217-312">**Členové definovaní XML:** `xml:base`Definované `lang` `space` `base`XML a členy`xml:space` jsou hlášeny jako direktivy XAML s názvem, a v implementacích .NET Framework XAML Services. `xml:lang`</span><span class="sxs-lookup"><span data-stu-id="38217-312">**XML-defined members:** The XML-defined `xml:base`, `xml:lang` and `xml:space` members are reported as XAML directives named `base`, `lang`, and `space` in the .NET Framework XAML Services implementations.</span></span> <span data-ttu-id="38217-313">Obor názvů pro toto je obor názvů `http://www.w3.org/XML/1998/namespace`XML.</span><span class="sxs-lookup"><span data-stu-id="38217-313">The namespace for these is the XML namespace `http://www.w3.org/XML/1998/namespace`.</span></span> <span data-ttu-id="38217-314">Konstanty pro každý z nich lze získat z <xref:System.Xaml.XamlLanguage>.</span><span class="sxs-lookup"><span data-stu-id="38217-314">Constants for each of these can be obtained from <xref:System.Xaml.XamlLanguage>.</span></span>

## <a name="node-order"></a><span data-ttu-id="38217-315">Pořadí uzlů</span><span class="sxs-lookup"><span data-stu-id="38217-315">Node Order</span></span>

<span data-ttu-id="38217-316">V některých případech <xref:System.Xaml.XamlXmlReader> mění pořadí uzlů XAML v datovém proudu uzlu XAML, oproti pořadí, ve kterém se uzly zobrazí, pokud jsou zobrazeny ve značce nebo při zpracování jako XML.</span><span class="sxs-lookup"><span data-stu-id="38217-316">In some cases, <xref:System.Xaml.XamlXmlReader> changes the order of XAML nodes in the XAML node stream, versus the order the nodes appear if viewed in the markup or if processed as XML.</span></span> <span data-ttu-id="38217-317">To se provádí, aby bylo <xref:System.Xaml.XamlObjectWriter> možné seřadit uzly tak, aby mohl zpracovat datový proud uzlu pouze s dopředné.</span><span class="sxs-lookup"><span data-stu-id="38217-317">This is done in order to order the nodes such that a <xref:System.Xaml.XamlObjectWriter> can process the node stream in a forward-only manner.</span></span>  <span data-ttu-id="38217-318">V .NET Framework služby XAML čtečka XAML změní pořadí uzlů místo toho, aby opouští tuto úlohu zapisovači XAML, jako optimalizaci výkonu pro uživatele modulu streamování objektů XAML.</span><span class="sxs-lookup"><span data-stu-id="38217-318">In .NET Framework XAML Services, the XAML reader reorders nodes rather than leaving this task to the XAML writer, as a performance optimization for XAML object writer consumers of the node stream.</span></span>

<span data-ttu-id="38217-319">Některé direktivy jsou určeny konkrétně k poskytnutí dalších informací pro vytvoření objektu z prvku objektu.</span><span class="sxs-lookup"><span data-stu-id="38217-319">Certain directives are intended specifically to provide more information for the creation of an object from an object element.</span></span> <span data-ttu-id="38217-320">Tyto direktivy jsou `Initialization`: `PositionalParameters`, `TypeArguments`, `FactoryMethod`, `Arguments`,.</span><span class="sxs-lookup"><span data-stu-id="38217-320">These directives are: `Initialization`, `PositionalParameters`, `TypeArguments`, `FactoryMethod`, `Arguments`.</span></span> <span data-ttu-id="38217-321">Čtečky XAML .NET Framework XAML Services se pokoušejí tyto direktivy umístit jako první členy proudu uzlů za objektem `StartObject`, z důvodů, které jsou vysvětleny v další části.</span><span class="sxs-lookup"><span data-stu-id="38217-321">The .NET Framework XAML Services XAML readers attempt to place these directives as the first members in the node stream following an object's `StartObject`, for reasons that are explained in the next section.</span></span>

### <a name="xamlobjectwriter-behavior-and-node-order"></a><span data-ttu-id="38217-322">Chování XamlObjectWriter a pořadí uzlů</span><span class="sxs-lookup"><span data-stu-id="38217-322">XamlObjectWriter Behavior and Node Order</span></span>

<span data-ttu-id="38217-323">`StartObject`<xref:System.Xaml.XamlObjectWriter> do nemusí nutně signalizovat zapisovač objektů XAML pro okamžité vytvoření instance objektu.</span><span class="sxs-lookup"><span data-stu-id="38217-323">`StartObject` to a <xref:System.Xaml.XamlObjectWriter> is not necessarily a signal to the XAML object writer to immediately construct the object instance.</span></span> <span data-ttu-id="38217-324">XAML obsahuje několik funkcí jazyka, které umožňují inicializaci objektu s dalšími vstupy a nespoléhat se výhradně na vyvolání konstruktoru bez parametrů k vytvoření počátečního objektu a nastavení vlastností.</span><span class="sxs-lookup"><span data-stu-id="38217-324">XAML includes several language features that make it possible to initialize an object with additional input, and to not rely entirely on invoking a parameterless constructor to produce the initial object, and only then setting properties.</span></span> <span data-ttu-id="38217-325">Mezi tyto funkce patří <xref:System.Windows.Markup.XamlDeferLoadAttribute>:; inicializační text; [x:TypeArguments –](x-typearguments-directive.md); poziční parametry rozšíření značek; metody pro vytváření a přidružené uzly [x:arguments –](x-arguments-directive.md) (XAML 2009).</span><span class="sxs-lookup"><span data-stu-id="38217-325">These features include: <xref:System.Windows.Markup.XamlDeferLoadAttribute>; initialization text; [x:TypeArguments](x-typearguments-directive.md); positional parameters of a markup extension; factory methods and associated [x:Arguments](x-arguments-directive.md) nodes (XAML 2009).</span></span> <span data-ttu-id="38217-326">Každý z těchto případů zpozdí skutečnou konstrukci objektu a vzhledem k tomu, že je přeobjednán datový proud uzlu, může zapisovač objektů XAML spoléhat na chování ve skutečné konstrukci instance vždy, když se vyskytne počáteční člen, který není specificky konstrukcí. Direktiva pro tento typ objektu.</span><span class="sxs-lookup"><span data-stu-id="38217-326">Each of these cases delay the actual object construction, and because the node stream is reordered, the XAML object writer can rely on a behavior of actually constructing the instance whenever a start member is encountered that is not specifically a construction directive for that object type.</span></span>

### <a name="getobject"></a><span data-ttu-id="38217-327">GetObject</span><span class="sxs-lookup"><span data-stu-id="38217-327">GetObject</span></span>

<span data-ttu-id="38217-328">`GetObject`představuje uzel XAML, kde místo vytváření nového objektu by měl zapisovač objektů XAML místo toho získat hodnotu obsahující vlastnosti objektu.</span><span class="sxs-lookup"><span data-stu-id="38217-328">`GetObject` represents a XAML node where rather than constructing a new object, a XAML object writer should instead get the value of the object's containing property.</span></span> <span data-ttu-id="38217-329">Typický případ, kdy `GetObject` je uzel zjištěn v datovém proudu uzlu XAML, je pro objekt kolekce nebo objekt Dictionary, pokud je nadřazená vlastnost pouze pro čtení v objektovém modelu záložního typu.</span><span class="sxs-lookup"><span data-stu-id="38217-329">A typical  case where a `GetObject` node is encountered in a XAML node stream is for a collection object or a dictionary object, when the containing property is deliberately read-only in the backing type's object model.</span></span> <span data-ttu-id="38217-330">V tomto scénáři je kolekce nebo slovník často vytvořen a inicializován (obvykle prázdný) pomocí inicializační logiky vlastnícího typu.</span><span class="sxs-lookup"><span data-stu-id="38217-330">In this scenario, the collection or dictionary often is created and initialized (usually empty) by the initialization logic of an owning type.</span></span>

## <a name="see-also"></a><span data-ttu-id="38217-331">Viz také:</span><span class="sxs-lookup"><span data-stu-id="38217-331">See also</span></span>

- <xref:System.Xaml.XamlObjectReader>
- [<span data-ttu-id="38217-332">XAML Services</span><span class="sxs-lookup"><span data-stu-id="38217-332">XAML Services</span></span>](index.md)
- [<span data-ttu-id="38217-333">Obory názvů jazyka XAML</span><span class="sxs-lookup"><span data-stu-id="38217-333">XAML Namespaces</span></span>](xaml-namespaces-for-net-framework-xaml-services.md)
