---
title: Principy struktur a koncepcí datových proudů uzlů XAML
ms.date: 03/30/2017
helpviewer_keywords:
- XAML node streams [XAML Services]
- nodes [XAML Services], XAML node stream
- XAML [XAML Services], XAML node streams
ms.assetid: 7c11abec-1075-474c-9d9b-778e5dab21c3
ms.openlocfilehash: babf98b7dd30cd60e72e310ae8ba8c9a42d9125f
ms.sourcegitcommit: 0be8a279af6d8a43e03141e349d3efd5d35f8767
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/18/2019
ms.locfileid: "58824426"
---
# <a name="understanding-xaml-node-stream-structures-and-concepts"></a><span data-ttu-id="53edf-102">Principy struktur a koncepcí datových proudů uzlů XAML</span><span class="sxs-lookup"><span data-stu-id="53edf-102">Understanding XAML Node Stream Structures and Concepts</span></span>

<span data-ttu-id="53edf-103">XAML čtečky a zapisovače XAML, jak je implementován v rozhraní .NET Framework XAML Services jsou založeny na konceptu návrhu datový proud uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-103">XAML readers and XAML writers as implemented in .NET Framework XAML Services are based on the design concept of a XAML node stream.</span></span> <span data-ttu-id="53edf-104">Datový proud uzlu XAML je koncepci sadu uzlů XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-104">The XAML node stream is a conceptualization of a set of XAML nodes.</span></span> <span data-ttu-id="53edf-105">V tomto koncepci procesoru XAML vás provede struktura uzel vztahů v XAML, jeden najednou.</span><span class="sxs-lookup"><span data-stu-id="53edf-105">In this conceptualization, a XAML processor walks through the structure of the node relationships in the XAML one at a time.</span></span> <span data-ttu-id="53edf-106">V každém okamžiku v otevřít datový proud uzlu XAML jenom jednomu záznamu v aktuální nebo current pozici existuje a mnoho aspektů rozhraní API sestavy pouze informace k dispozici z této pozici.</span><span class="sxs-lookup"><span data-stu-id="53edf-106">At any time, only one current record or current position exists in an open XAML node stream, and many aspects of the API report only the information available from that position.</span></span> <span data-ttu-id="53edf-107">Aktuální uzel v datovém proudu uzlu XAML lze popsat jako objekt, člen nebo hodnota.</span><span class="sxs-lookup"><span data-stu-id="53edf-107">The current node in a XAML node stream can be described as being an object, a member, or a value.</span></span> <span data-ttu-id="53edf-108">Zpracováním XAML jako datový proud uzlu XAML XAML čtenáři komunikovat se zapisovači XAML a povolit program k zobrazení, pracovat s nebo změnit obsah datový proud uzlu XAML během cesta načtení nebo uložení cesta operace, která zahrnuje XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-108">By treating XAML as a XAML node stream, XAML readers can communicate with XAML writers and enable a program to view, interact with, or alter the contents of a XAML node stream during either a load path or a save path operation that involves XAML.</span></span> <span data-ttu-id="53edf-109">Návrh XAML čtečky a zapisovače rozhraní API a koncept datový proud uzlu XAML podobají předchozí související čtečky a zapisovače návrhy a koncepty, jako [!INCLUDE[TLA#tla_xmldom](../../../includes/tlasharptla-xmldom-md.md)] a <xref:System.Xml.XmlReader> a <xref:System.Xml.XmlWriter> třídy.</span><span class="sxs-lookup"><span data-stu-id="53edf-109">XAML reader and writer API design and the XAML node stream concept are similar to previous related reader and writer designs and concepts, such as the [!INCLUDE[TLA#tla_xmldom](../../../includes/tlasharptla-xmldom-md.md)] and the <xref:System.Xml.XmlReader> and <xref:System.Xml.XmlWriter> classes.</span></span> <span data-ttu-id="53edf-110">Toto téma popisuje koncepty datový proud uzlu XAML a popisuje, jak může zapisovat rutiny, které pracují s reprezentací XAML na úrovni uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-110">This topic discusses XAML node stream concepts and describes how you can write routines that interact with XAML representations at the XAML node level.</span></span>

<a name="loading_into_a_xaml_reader"></a>

## <a name="loading-xaml-into-a-xaml-reader"></a><span data-ttu-id="53edf-111">Načítání XAML do čtečky XAML</span><span class="sxs-lookup"><span data-stu-id="53edf-111">Loading XAML into a XAML Reader</span></span>

<span data-ttu-id="53edf-112">Základní <xref:System.Xaml.XamlReader> třídy nedeklaruje konkrétní postup načítání počáteční XAML do čtečky XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-112">The base <xref:System.Xaml.XamlReader> class does not declare a particular technique for loading the initial XAML into a XAML reader.</span></span> <span data-ttu-id="53edf-113">Místo toho odvozená třída deklaruje a implementuje načítání techniku, včetně obecné vlastnosti a omezení v jeho vstupního zdroje pro XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-113">Instead, a derived class declares and implements the loading technique, including the general characteristics and constraints of its input source for XAML.</span></span> <span data-ttu-id="53edf-114">Například <xref:System.Xaml.XamlObjectReader> přečte grafu objektů, spouští se ze vstupního zdroje jeden objekt, který představuje kořenový server WSUS nebo base.</span><span class="sxs-lookup"><span data-stu-id="53edf-114">For example, a <xref:System.Xaml.XamlObjectReader> reads an object graph, starting from the input source of a single object that represents the root or base.</span></span> <span data-ttu-id="53edf-115"><xref:System.Xaml.XamlObjectReader> Pak vytvoří datový proud uzlu XAML z grafu objektů.</span><span class="sxs-lookup"><span data-stu-id="53edf-115">The <xref:System.Xaml.XamlObjectReader> then produces a XAML node stream from the object graph.</span></span>

<span data-ttu-id="53edf-116">Většina viditelného definovaných rozhraní .NET Framework XAML Services <xref:System.Xaml.XamlReader> je podtřídou <xref:System.Xaml.XamlXmlReader>.</span><span class="sxs-lookup"><span data-stu-id="53edf-116">The most prominent .NET Framework XAML Services–defined <xref:System.Xaml.XamlReader> subclass is <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="53edf-117"><xref:System.Xaml.XamlXmlReader> načte počáteční XAML, buď načtením do textového souboru přímo prostřednictvím datového proudu nebo souboru cesty nebo nepřímo prostřednictvím třídy související čtečky <xref:System.IO.TextReader>.</span><span class="sxs-lookup"><span data-stu-id="53edf-117"><xref:System.Xaml.XamlXmlReader> loads the initial XAML, either by loading a text file directly through a stream or file path, or indirectly through a related reader class such as <xref:System.IO.TextReader>.</span></span> <span data-ttu-id="53edf-118"><xref:System.Xaml.XamlReader> Si lze představit jako obsahující celého vstupního zdroje XAML po jeho načtení.</span><span class="sxs-lookup"><span data-stu-id="53edf-118">The <xref:System.Xaml.XamlReader> can be thought of as containing the entirety of the XAML input source after it has loaded.</span></span> <span data-ttu-id="53edf-119">Ale <xref:System.Xaml.XamlReader> základní rozhraní API je navržený tak, aby čtenář dixons carphone se jeden uzel XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-119">However, the <xref:System.Xaml.XamlReader> base API is designed so that the reader is interacting with a single node of the XAML.</span></span> <span data-ttu-id="53edf-120">Při prvním načtení, první jeden uzel, se kterými je kořenový adresář XAML a jeho spuštění objekt.</span><span class="sxs-lookup"><span data-stu-id="53edf-120">When first loaded, the first single node you encounter is the root of the XAML, and its start object.</span></span>

### <a name="the-xaml-node-stream-concept"></a><span data-ttu-id="53edf-121">Koncept Stream uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="53edf-121">The XAML Node Stream Concept</span></span>

<span data-ttu-id="53edf-122">Pokud jste obecně více do hloubky modelu DOM, stromu jedná nebo založené na dotazech přístupu k přístupu k technologie založené na XML, je užitečný způsob, jak pohodlným datový proud uzlu XAML následujícím způsobem.</span><span class="sxs-lookup"><span data-stu-id="53edf-122">If you are generally more familiar with a DOM, tree metaphor, or query-based approach towards accessing XML-based technologies, a helpful way to conceptualize a XAML node stream is as follows.</span></span> <span data-ttu-id="53edf-123">Představte si, že je načteno XAML modelu DOM nebo stromové struktury, ve kterém je každý uzel je to možné úplně rozbalen a potom prezentovaná lineárně.</span><span class="sxs-lookup"><span data-stu-id="53edf-123">Imagine that the loaded XAML is a DOM or a tree where every possible node is expanded all the way, and then presented linearly.</span></span> <span data-ttu-id="53edf-124">Jako postupoval uzly vám může být procházení "v" nebo "out" úrovní, které by byly relevantní pro modelu DOM, ale datový proud uzlu XAML neudržuje explicitně sledování protože tyto koncepty úrovně nejsou relevantní pro datový proud uzlu.</span><span class="sxs-lookup"><span data-stu-id="53edf-124">As you advance through the nodes, you might be traversing "in" or "out" of levels that would be relevant to a DOM, but the XAML node stream does not explicitly keep track because these level concepts are not relevant to a node stream.</span></span> <span data-ttu-id="53edf-125">Datový proud uzlu má "aktuální" pozici, ale pokud jste uložili jiné části datového proudu sami sebe jako odkazy, každý aspekt datový proud uzlu, než je aktuální pozice uzlu je mimo zobrazení.</span><span class="sxs-lookup"><span data-stu-id="53edf-125">The node stream has a "current" position, but unless you have stored other parts of the stream yourself as references, every aspect of the node stream other than the current node position is out of view.</span></span>

<span data-ttu-id="53edf-126">Koncept datový proud uzlu XAML má významné výhodu v tom, že pokud při procházení datový proud celého uzlu, budete mít jistotu, že mají zpracovat celou znázornění XAML; nemusíte se obávat, že dotaz, operace DOM nebo jiný nelineárních přístup k informacím o zpracování byl vynechán některá část dokončení zastoupení XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-126">The XAML node stream concept has the notable advantage that if you go through the entire node stream, you are assured that you have processed the entire XAML representation; you do not need to worry that a query, a DOM operation, or some other nonlinear approach to processing information has missed some part of the complete XAML representation.</span></span> <span data-ttu-id="53edf-127">Z tohoto důvodu je reprezentace datový proud uzlu XAML ideální pro připojení XAML čtečky a zapisovače XAML i pro poskytování systému ve kterém můžete vložit vlastní proces, který funguje mezi jednotlivými fázemi pro čtení a zápisu operace zpracování XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-127">For this reason, the XAML node stream representation is ideal both for connecting XAML readers and XAML writers, and for providing a system where you can insert your own process that acts between the read and write phases of a XAML processing operation.</span></span> <span data-ttu-id="53edf-128">V mnoha případech řazení uzly v datovém proudu uzlu XAML je úmyslně optimalizované nebo přeuspořádány čtenářům XAML a jak pořadí se může objevit zdrojový text, binární soubor nebo objekt grafu.</span><span class="sxs-lookup"><span data-stu-id="53edf-128">In many cases, the ordering of nodes in the XAML node stream is deliberately optimized or reordered by XAML readers versus how the order might appear in the source text, binary, or object graph.</span></span> <span data-ttu-id="53edf-129">Toto chování slouží vynutit architektuře zpracování s XAML, kterým jsou XAML zapisovače nikdy v umístění, které se mají vrátit "" v datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="53edf-129">This behavior is intended to enforce a XAML processing architecture whereby XAML writers are never in a position where they have to go "back" in the node stream.</span></span> <span data-ttu-id="53edf-130">V ideálním případě by všechny XAML operace zápisu by být možné tak, aby fungoval založená na schéma kontext a aktuální pozici datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="53edf-130">Ideally, all XAML write operations should be able to act based on schema context plus the current position of the node stream.</span></span>

<a name="a_basic_reading_node_loop"></a>

## <a name="a-basic-reading-node-loop"></a><span data-ttu-id="53edf-131">Smyčku základní čtení uzlu</span><span class="sxs-lookup"><span data-stu-id="53edf-131">A Basic Reading Node Loop</span></span>

<span data-ttu-id="53edf-132">Základní čtení uzlu smyčky pro zkoumání datový proud uzlu XAML se skládá z následujících konceptů.</span><span class="sxs-lookup"><span data-stu-id="53edf-132">A basic reading node loop for examining a XAML node stream consists of the following concepts.</span></span> <span data-ttu-id="53edf-133">Pro účely tohoto uzlu smyčky jak je popsáno v tomto tématu se předpokládá, že čtete založený na textu, čitelné XAML soubor pomocí <xref:System.Xaml.XamlXmlReader>.</span><span class="sxs-lookup"><span data-stu-id="53edf-133">For purposes of node loops as discussed in this topic, assume that you are reading a text-based, human-readable XAML file using <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="53edf-134">Odkazy v této části najdete konkrétní smyčky uzlu XAML rozhraní API implementované <xref:System.Xaml.XamlXmlReader>.</span><span class="sxs-lookup"><span data-stu-id="53edf-134">The links in this section refer to the particular XAML node loop API implemented by <xref:System.Xaml.XamlXmlReader>.</span></span>

- <span data-ttu-id="53edf-135">Ujistěte se, že zatím nejste na konci datový proud uzlu XAML (zkontrolujte <xref:System.Xaml.XamlXmlReader.IsEof%2A>, nebo použijte <xref:System.Xaml.XamlXmlReader.Read%2A> návratová hodnota).</span><span class="sxs-lookup"><span data-stu-id="53edf-135">Make sure that you are not at the end of the XAML node stream (check <xref:System.Xaml.XamlXmlReader.IsEof%2A>, or use the <xref:System.Xaml.XamlXmlReader.Read%2A> return value).</span></span> <span data-ttu-id="53edf-136">Pokud jste na konci datového proudu, není aktuální uzel a má ukončit.</span><span class="sxs-lookup"><span data-stu-id="53edf-136">If you are at the end of the stream, there is no current node and you should exit.</span></span>

- <span data-ttu-id="53edf-137">Zkontrolujte, jaký typ uzlu datový proud uzlu XAML nyní poskytuje voláním <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span><span class="sxs-lookup"><span data-stu-id="53edf-137">Check what type of node the XAML node stream currently exposes by calling <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span>

- <span data-ttu-id="53edf-138">Pokud máte přidružených zapisovače objektu XAML, která je připojena přímo, obecně volání <xref:System.Xaml.XamlWriter.WriteNode%2A> v tomto okamžiku.</span><span class="sxs-lookup"><span data-stu-id="53edf-138">If you have an associated XAML object writer that is connected directly, you generally call <xref:System.Xaml.XamlWriter.WriteNode%2A> at this point.</span></span>

- <span data-ttu-id="53edf-139">Závislosti na němž <xref:System.Xaml.XamlNodeType> se ohlásí jako aktuální uzel nebo aktuální záznam volat jednu z následujících k získání informací o obsah uzlu:</span><span class="sxs-lookup"><span data-stu-id="53edf-139">Based on which <xref:System.Xaml.XamlNodeType> is reported as the current node or current record, call one of the following to obtain information about the node contents:</span></span>

    - <span data-ttu-id="53edf-140">Pro <xref:System.Xaml.XamlXmlReader.NodeType%2A> z <xref:System.Xaml.XamlNodeType.StartMember> nebo <xref:System.Xaml.XamlNodeType.EndMember>, volání <xref:System.Xaml.XamlXmlReader.Member%2A> získat <xref:System.Xaml.XamlMember> informace o členu.</span><span class="sxs-lookup"><span data-stu-id="53edf-140">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartMember> or <xref:System.Xaml.XamlNodeType.EndMember>, call <xref:System.Xaml.XamlXmlReader.Member%2A> to obtain <xref:System.Xaml.XamlMember> information about a member.</span></span> <span data-ttu-id="53edf-141">Všimněte si, že může být člen <xref:System.Xaml.XamlDirective>, a proto nemusí být nutně konvenční člen definován typ předchozí objektu.</span><span class="sxs-lookup"><span data-stu-id="53edf-141">Note that the member might be a <xref:System.Xaml.XamlDirective>, and thus might not necessarily be a conventional type-defined member of the preceding object.</span></span> <span data-ttu-id="53edf-142">Například `x:Name` u objektu se zobrazí jako člena XAML kde <xref:System.Xaml.XamlMember.IsDirective%2A> má hodnotu true a <xref:System.Xaml.XamlMember.Name%2A> člena je `Name`, s jinými vlastnostmi označující, že tato direktiva je v oboru názvů XAML jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-142">For example, `x:Name` applied to an object appears as a XAML member where <xref:System.Xaml.XamlMember.IsDirective%2A> is true and the <xref:System.Xaml.XamlMember.Name%2A> of the member is `Name`, with other properties indicating that this directive is under the XAML language XAML namespace.</span></span>

    - <span data-ttu-id="53edf-143">Pro <xref:System.Xaml.XamlXmlReader.NodeType%2A> z <xref:System.Xaml.XamlNodeType.StartObject> nebo <xref:System.Xaml.XamlNodeType.EndObject>, volání <xref:System.Xaml.XamlXmlReader.Type%2A> získat <xref:System.Xaml.XamlType> informace o objektu.</span><span class="sxs-lookup"><span data-stu-id="53edf-143">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartObject> or <xref:System.Xaml.XamlNodeType.EndObject>, call <xref:System.Xaml.XamlXmlReader.Type%2A> to obtain <xref:System.Xaml.XamlType> information about an object.</span></span>

    - <span data-ttu-id="53edf-144">Pro <xref:System.Xaml.XamlXmlReader.NodeType%2A> z <xref:System.Xaml.XamlNodeType.Value>, volání <xref:System.Xaml.XamlXmlReader.Value%2A>.</span><span class="sxs-lookup"><span data-stu-id="53edf-144">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.Value>, call <xref:System.Xaml.XamlXmlReader.Value%2A>.</span></span> <span data-ttu-id="53edf-145">Uzel je hodnota pouze v případě, že je nejjednodušší výraz hodnotu člena nebo textové inicializace pro objekt (ale byste měli vědět o chování převodu typu, jak je uvedeno v následující části tohoto tématu).</span><span class="sxs-lookup"><span data-stu-id="53edf-145">A node is a value only if it is the simplest expression of a value for a member, or the initialization text for an object (however, you should be aware of type conversion behavior as documented in an upcoming section of this topic).</span></span>

    - <span data-ttu-id="53edf-146">Pro <xref:System.Xaml.XamlXmlReader.NodeType%2A> z <xref:System.Xaml.XamlNodeType.NamespaceDeclaration>, volání <xref:System.Xaml.XamlXmlReader.Namespace%2A> získat informace o oboru názvů pro uzel oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="53edf-146">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.NamespaceDeclaration>, call <xref:System.Xaml.XamlXmlReader.Namespace%2A> to obtain namespace information for a namespace node.</span></span>

- <span data-ttu-id="53edf-147">Volání <xref:System.Xaml.XamlXmlReader.Read%2A> čtečky XAML, přejděte k dalšímu uzlu v datovém proudu uzlu XAML, a opakujte znovu kroky.</span><span class="sxs-lookup"><span data-stu-id="53edf-147">Call <xref:System.Xaml.XamlXmlReader.Read%2A> to advance the XAML reader to the next node in the XAML node stream, and repeat the steps again.</span></span>

<span data-ttu-id="53edf-148">Datový proud uzlu XAML poskytované rozhraní .NET Framework XAML Services XAML čtenáři vždy poskytuje úplné, podrobné procházení všech možných uzlů.</span><span class="sxs-lookup"><span data-stu-id="53edf-148">The XAML node stream provided by .NET Framework XAML Services XAML readers always provides a full, deep traversal of all possible nodes.</span></span> <span data-ttu-id="53edf-149">Mezi dostupné techniky typické řízení toku pro smyčku uzlu XAML patří definování textu v rámci `while (reader.Read())`a přepnete <xref:System.Xaml.XamlXmlReader.NodeType%2A> v každém uzlu bodu ve smyčce uzlu.</span><span class="sxs-lookup"><span data-stu-id="53edf-149">Typical flow-control techniques for a XAML node loop include defining a body within `while (reader.Read())`, and switching on <xref:System.Xaml.XamlXmlReader.NodeType%2A> at each node point in the node loop.</span></span>

<span data-ttu-id="53edf-150">Pokud datový proud uzlu je na konci souboru, aktuální uzel má hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="53edf-150">If the node stream is at end of file, the current node is null.</span></span>

<span data-ttu-id="53edf-151">Nejjednodušší smyčky, která používá čtečky a zapisovače vypadá podobně jako v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="53edf-151">The simplest loop that uses a reader and writer resembles the following example.</span></span>

```csharp
XamlXmlReader xxr = new XamlXmlReader(new StringReader(xamlStringToLoad));
//where xamlStringToLoad is a string of well formed XAML
XamlObjectWriter xow = new XamlObjectWriter(xxr.SchemaContext);
while (xxr.Read()) {
  xow.WriteNode(xxr);
}
```

<span data-ttu-id="53edf-152">Tento základní příklad smyčku zatížení cesta XAML uzel připojí transparentně XAML čtečky a zapisovače XAML, to nic dělat jinak než v případě byste použili <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="53edf-152">This basic example of a load path XAML node loop transparently connects the XAML reader and XAML writer, doing nothing different than if you had used <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="53edf-153">Ale tato základní struktura je pak rozšířen použít pro čtení nebo zápis scénář.</span><span class="sxs-lookup"><span data-stu-id="53edf-153">But this basic structure is then expanded to apply to your reading or writing scenario.</span></span> <span data-ttu-id="53edf-154">Některé možné scénáře jsou následující:</span><span class="sxs-lookup"><span data-stu-id="53edf-154">Some possible scenarios are as follows:</span></span>

- <span data-ttu-id="53edf-155">Přepnout <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span><span class="sxs-lookup"><span data-stu-id="53edf-155">Switch on <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span> <span data-ttu-id="53edf-156">Proveďte různé akce v závislosti na tom, který uzel typu načítán.</span><span class="sxs-lookup"><span data-stu-id="53edf-156">Perform different actions depending on which node type is being read.</span></span>

- <span data-ttu-id="53edf-157">Nevolejte <xref:System.Xaml.XamlWriter.WriteNode%2A> ve všech případech.</span><span class="sxs-lookup"><span data-stu-id="53edf-157">Do not call <xref:System.Xaml.XamlWriter.WriteNode%2A> in all cases.</span></span> <span data-ttu-id="53edf-158">Volat pouze <xref:System.Xaml.XamlWriter.WriteNode%2A> v některých <xref:System.Xaml.XamlXmlReader.NodeType%2A> případy.</span><span class="sxs-lookup"><span data-stu-id="53edf-158">Only call <xref:System.Xaml.XamlWriter.WriteNode%2A> in some <xref:System.Xaml.XamlXmlReader.NodeType%2A> cases.</span></span>

- <span data-ttu-id="53edf-159">V rámci logiku pro konkrétní typ analyzovat specifika tento uzel a na jejich základě reagovat.</span><span class="sxs-lookup"><span data-stu-id="53edf-159">Within the logic for a particular node type, analyze the specifics of that node and act on them.</span></span> <span data-ttu-id="53edf-160">Můžete například napsat pouze objekty, které pocházejí z konkrétní obor názvů XAML a potom vyřaďte nebo odložit všechny objekty nejsou z daného oboru názvů XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-160">For example, you could write only objects that come from a particular XAML namespace, and then drop or defer any objects not from that XAML namespace.</span></span> <span data-ttu-id="53edf-161">Nebo můžete vyřadit nebo jinak jako součást vaší člen zpracování znovu zpracovat všechny direktivy XAML, které váš systém XAML není podporováno.</span><span class="sxs-lookup"><span data-stu-id="53edf-161">Or you could drop or otherwise reprocess any XAML directives that your XAML system does not support as part of your member processing.</span></span>

- <span data-ttu-id="53edf-162">Definování vlastní <xref:System.Xaml.XamlObjectWriter> , která přepíše `Write*` metody, pravděpodobně provádí mapování typu, který obchází kontext schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-162">Define a custom <xref:System.Xaml.XamlObjectWriter> that overrides `Write*` methods, possibly performing type mapping that bypasses XAML schema context.</span></span>

- <span data-ttu-id="53edf-163">Vytvořit <xref:System.Xaml.XamlXmlReader> používat jiný než výchozí kontext schématu XAML tak, aby vlastní rozdíly v chování XAML se používají i tak, že čtečky a zapisovače.</span><span class="sxs-lookup"><span data-stu-id="53edf-163">Construct the <xref:System.Xaml.XamlXmlReader> to use a nondefault XAML schema context, so that customized differences in XAML behavior are used both by the reader and the writer.</span></span>

### <a name="accessing-xaml-beyond-the-node-loop-concept"></a><span data-ttu-id="53edf-164">Přístup k XAML mimo smyčku koncept uzlu</span><span class="sxs-lookup"><span data-stu-id="53edf-164">Accessing XAML Beyond the Node Loop Concept</span></span>

<span data-ttu-id="53edf-165">Existují další způsoby, jak pracovat s XAML reprezentaci jiných než jako smyčky uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-165">There are potentially other ways to work with a XAML representation other than as a XAML node loop.</span></span> <span data-ttu-id="53edf-166">Například může existovat čtečku XAML, který může číst indexované uzlu nebo zejména přímo nástrojem přistupuje k uzly `x:Name`, `x:Uid`, nebo prostřednictvím další identifikátory.</span><span class="sxs-lookup"><span data-stu-id="53edf-166">For example, there could exist a XAML reader that can read an indexed node, or in particular accesses nodes directly by `x:Name`, by `x:Uid`, or through other identifiers.</span></span> <span data-ttu-id="53edf-167">Rozhraní .NET framework XAML Services neposkytuje úplnou implementaci, ale poskytuje navrhovaný model prostřednictvím typů služeb a podpory.</span><span class="sxs-lookup"><span data-stu-id="53edf-167">.NET Framework XAML Services does not provide a full implementation, but provides a suggested pattern through services and support types.</span></span> <span data-ttu-id="53edf-168">Další informace naleznete v tématu <xref:System.Xaml.IXamlIndexingReader> a <xref:System.Xaml.XamlNodeList>.</span><span class="sxs-lookup"><span data-stu-id="53edf-168">For more information, see <xref:System.Xaml.IXamlIndexingReader> and <xref:System.Xaml.XamlNodeList>.</span></span>

> [!TIP]
> <span data-ttu-id="53edf-169">Microsoft také vytváří o verzi out-of-band označuje jako Microsoft XAML Toolkit.</span><span class="sxs-lookup"><span data-stu-id="53edf-169">Microsoft also produces an out-of-band release known as the Microsoft XAML Toolkit.</span></span> <span data-ttu-id="53edf-170">Toto vydání out-of-band stále probíhá jeho fází předběžné verze.</span><span class="sxs-lookup"><span data-stu-id="53edf-170">This out-of-band release is still in its pre-release stages.</span></span> <span data-ttu-id="53edf-171">Ale pokud jste ochotní pro práci s předběžných komponenty, Microsoft XAML Toolkit poskytuje některé zajímavé prostředky pro nástroje XAML a statické analýzy XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-171">However, if you are willing to work with pre-release components, the Microsoft XAML Toolkit provides some interesting resources for XAML tooling and static analysis of XAML.</span></span> <span data-ttu-id="53edf-172">Microsoft XAML Toolkit obsahuje API modelu DOM XAML, podporu pro analýzu FxCop a kontext schématu XAML pro technologii Silverlight.</span><span class="sxs-lookup"><span data-stu-id="53edf-172">The Microsoft XAML Toolkit includes a XAML DOM API, support for FxCop analysis, and a XAML schema context for Silverlight.</span></span> <span data-ttu-id="53edf-173">Další informace najdete v tématu [Microsoft XAML Toolkit](https://code.msdn.microsoft.com/XAML).</span><span class="sxs-lookup"><span data-stu-id="53edf-173">For more information, see [Microsoft XAML Toolkit](https://code.msdn.microsoft.com/XAML).</span></span>

<a name="working_with_the_current_node"></a>

## <a name="working-with-the-current-node"></a><span data-ttu-id="53edf-174">Práce s aktuální uzel</span><span class="sxs-lookup"><span data-stu-id="53edf-174">Working with the Current Node</span></span>

<span data-ttu-id="53edf-175">Většina scénářů, které používají smyčky uzlu XAML jenom nečtou uzlů.</span><span class="sxs-lookup"><span data-stu-id="53edf-175">Most scenarios that use a XAML node loop do not only read the nodes.</span></span> <span data-ttu-id="53edf-176">Většina scénářů zpracování aktuální uzly a předat každý uzel, jeden po druhém implementace <xref:System.Xaml.XamlWriter>.</span><span class="sxs-lookup"><span data-stu-id="53edf-176">Most scenarios process current nodes and pass each node one at a time to an implementation of <xref:System.Xaml.XamlWriter>.</span></span>

<span data-ttu-id="53edf-177">Ve scénáři cesta obvyklém zatížení <xref:System.Xaml.XamlXmlReader> vytvoří datový proud uzlu XAML; uzlů XAML se zpracovávají podle logiky a kontext schématu XAML a uzly jsou předány <xref:System.Xaml.XamlObjectWriter>.</span><span class="sxs-lookup"><span data-stu-id="53edf-177">In the typical load path scenario, a <xref:System.Xaml.XamlXmlReader> produces a XAML node stream; the XAML nodes are processed according to your logic and XAML schema context; and the nodes are passed to a <xref:System.Xaml.XamlObjectWriter>.</span></span> <span data-ttu-id="53edf-178">Výsledný graf objektu se potom začlenit do vaší aplikace nebo rozhraní.</span><span class="sxs-lookup"><span data-stu-id="53edf-178">You then integrate the resulting object graph into your application or framework.</span></span>

<span data-ttu-id="53edf-179">V typické Uložit cestu scénář <xref:System.Xaml.XamlObjectReader> přečte graf objektu, zpracování jednotlivých uzlů XAML a <xref:System.Xaml.XamlXmlWriter> výstupy serializovaná výsledek v podobě textového souboru XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-179">In a typical save path scenario, a <xref:System.Xaml.XamlObjectReader> reads the object graph, individual XAML nodes are processed, and a <xref:System.Xaml.XamlXmlWriter> outputs the serialized result as a XAML text file.</span></span> <span data-ttu-id="53edf-180">Klíč je, že cesty a scénáře zahrnují práci se přesně jedna XAML uzlů najednou a uzlů XAML jsou k dispozici pro zpracování a standardizovaným způsobem, který je definován tak, že systém typů XAML a rozhraní.NET Framework XAML Services API.</span><span class="sxs-lookup"><span data-stu-id="53edf-180">The key is that both paths and scenarios involve working with exactly one XAML node at a time, and the XAML nodes are available for treatment in a standardized way that is defined by the XAML type system and the.NET Framework XAML Services APIs.</span></span>

### <a name="frames-and-scope"></a><span data-ttu-id="53edf-181">Rámce a oboru</span><span class="sxs-lookup"><span data-stu-id="53edf-181">Frames and Scope</span></span>

<span data-ttu-id="53edf-182">Smyčka uzlu XAML vás provede datový proud uzlu XAML lineárním způsobem.</span><span class="sxs-lookup"><span data-stu-id="53edf-182">A XAML node loop walks through a XAML node stream in a linear way.</span></span> <span data-ttu-id="53edf-183">Datový proud uzlu prochází do objektů do členů, které obsahují další objekty a tak dále.</span><span class="sxs-lookup"><span data-stu-id="53edf-183">The node stream traverses into objects, into members that contain other objects, and so on.</span></span> <span data-ttu-id="53edf-184">Často je užitečné udržovat přehled o oboru v rámci datový proud uzlu XAML implementací koncept rámec a v zásobníku.</span><span class="sxs-lookup"><span data-stu-id="53edf-184">It is often useful to keep track of scope within the XAML node stream by implementing a frame and stack concept.</span></span> <span data-ttu-id="53edf-185">To platí zejména pokud jsou v něm jsou aktivně upravit datový proud uzlu.</span><span class="sxs-lookup"><span data-stu-id="53edf-185">This is particularly true if you are actively adjusting the node stream while you are in it.</span></span> <span data-ttu-id="53edf-186">Rámce a zásobníku podporují implementovat jako součást logiky smyčky uzel může počítat `StartObject` (nebo `GetObject`) a `EndObject` obory, jako sestup do struktury uzlu XAML, pokud je struktura mluvit z hlediska modelu DOM.</span><span class="sxs-lookup"><span data-stu-id="53edf-186">The frame and stack support that you implement as part of your node loop logic could count `StartObject` (or `GetObject`) and `EndObject` scopes as you descend into a XAML node structure if the structure is thought of from a DOM perspective.</span></span>

<a name="traversing_and_entering_object_nodes"></a>

## <a name="traversing-and-entering-object-nodes"></a><span data-ttu-id="53edf-187">Procházení a zadat uzly objektu</span><span class="sxs-lookup"><span data-stu-id="53edf-187">Traversing and Entering Object Nodes</span></span>

<span data-ttu-id="53edf-188">Prvním uzlem v datovém proudu uzlu při otevření čtečka XAML je uzel počáteční objekt kořenového objektu.</span><span class="sxs-lookup"><span data-stu-id="53edf-188">The first node in a node stream when it is opened by a XAML reader is the start-object node of the root object.</span></span> <span data-ttu-id="53edf-189">Podle definice tento objekt je vždy jeden objekt uzlu a nemá žádné partnerské uzly.</span><span class="sxs-lookup"><span data-stu-id="53edf-189">By definition, this object is always a single object node and has no peers.</span></span> <span data-ttu-id="53edf-190">V XAML jakékoli reálný příklad kořenový objekt je definován s jednu nebo více vlastností, které obsahují další objekty a členskými uzly mají tyto vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="53edf-190">In any real-world XAML example, the root object is defined to have one or more properties that hold more objects, and these properties have member nodes.</span></span> <span data-ttu-id="53edf-191">Členskými uzly potom mít jeden nebo více uzly objektu, nebo může také ukončit v uzlu s hodnotou, se místo toho.</span><span class="sxs-lookup"><span data-stu-id="53edf-191">The member nodes then have one or more object nodes, or might also terminate in a value node instead.</span></span> <span data-ttu-id="53edf-192">Kořenový objekt obvykle definuje obory názvů XAML, které jsou přiřazeny syntakticky jako atributy ve značce XAML text, ale mapování na `Namescope` typ uzlu v reprezentaci datový proud uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-192">The root object typically defines XAML namescopes, which are syntactically assigned as attributes in the XAML text markup but map to a `Namescope` node type in the XAML node stream representation.</span></span>

<span data-ttu-id="53edf-193">Zvažte následující příklad XAML (to je libovolný XAML, nezálohované existující typy v rozhraní .NET Framework).</span><span class="sxs-lookup"><span data-stu-id="53edf-193">Consider the following XAML example (this is arbitrary XAML, not backed by existing types in the .NET Framework).</span></span> <span data-ttu-id="53edf-194">Předpokládejme, že v tomto modelu objektu `FavorCollection` je `List<T>` z `Favor`, `Balloon` a `NoiseMaker` jsou přiřaditelné k `Favor`, `Balloon.Color` vlastnost tímto modulem stojí `Color` objekt podobný jak definuje WPF barvy jako názvy známé barev a `Color` podporuje konvertor typu pro syntaxi atributů.</span><span class="sxs-lookup"><span data-stu-id="53edf-194">Assume that in this object model, `FavorCollection` is `List<T>` of `Favor`, `Balloon` and `NoiseMaker` are assignable to `Favor`, the `Balloon.Color` property is backed by a `Color` object similar to how WPF defines colors as known color names, and `Color` supports a type converter for attribute syntax.</span></span>

|<span data-ttu-id="53edf-195">Značky XAML</span><span class="sxs-lookup"><span data-stu-id="53edf-195">XAML markup</span></span>|<span data-ttu-id="53edf-196">Výsledný datový proud uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="53edf-196">Resulting XAML node stream</span></span>|
|-----------------|--------------------------------|
|`<Party`|<span data-ttu-id="53edf-197">`Namespace` uzel pro `Party`</span><span class="sxs-lookup"><span data-stu-id="53edf-197">`Namespace` node for `Party`</span></span>|
|`xmlns="PartyXamlNamespace">`|<span data-ttu-id="53edf-198">`StartObject` uzel pro `Party`</span><span class="sxs-lookup"><span data-stu-id="53edf-198">`StartObject` node for `Party`</span></span>|
|`<Party.Favors>`|<span data-ttu-id="53edf-199">`StartMember` uzel pro `Party.Favors`</span><span class="sxs-lookup"><span data-stu-id="53edf-199">`StartMember` node for `Party.Favors`</span></span>|
||<span data-ttu-id="53edf-200">`StartObject` uzel pro implicitní `FavorCollection`</span><span class="sxs-lookup"><span data-stu-id="53edf-200">`StartObject` node for implicit `FavorCollection`</span></span>|
||<span data-ttu-id="53edf-201">`StartMember` uzel pro implicitní `FavorCollection` vlastnosti položky.</span><span class="sxs-lookup"><span data-stu-id="53edf-201">`StartMember` node for implicit `FavorCollection` items property.</span></span>|
|`<Balloon`|<span data-ttu-id="53edf-202">`StartObject` uzel pro `Balloon`</span><span class="sxs-lookup"><span data-stu-id="53edf-202">`StartObject` node for `Balloon`</span></span>|
|`Color="Red"`|<span data-ttu-id="53edf-203">`StartMember` uzel pro `Color`</span><span class="sxs-lookup"><span data-stu-id="53edf-203">`StartMember` node for `Color`</span></span><br /><br /> <span data-ttu-id="53edf-204">`Value` uzel pro řetězec hodnoty atributu `"Red"`</span><span class="sxs-lookup"><span data-stu-id="53edf-204">`Value` node for the attribute value string `"Red"`</span></span><br /><br /> <span data-ttu-id="53edf-205">`EndMember` pro `Color`</span><span class="sxs-lookup"><span data-stu-id="53edf-205">`EndMember` for `Color`</span></span>|
|`HasHelium="True"`|<span data-ttu-id="53edf-206">`StartMember` uzel pro `HasHelium`</span><span class="sxs-lookup"><span data-stu-id="53edf-206">`StartMember` node for `HasHelium`</span></span><br /><br /> <span data-ttu-id="53edf-207">`Value` uzel pro řetězec hodnoty atributu `"True"`</span><span class="sxs-lookup"><span data-stu-id="53edf-207">`Value` node for the attribute value string `"True"`</span></span><br /><br /> <span data-ttu-id="53edf-208">`EndMember` pro `HasHelium`</span><span class="sxs-lookup"><span data-stu-id="53edf-208">`EndMember` for `HasHelium`</span></span>|
|`>`|<span data-ttu-id="53edf-209">`EndObject` pro `Balloon`</span><span class="sxs-lookup"><span data-stu-id="53edf-209">`EndObject` for `Balloon`</span></span>|
|`<NoiseMaker>Loudest</NoiseMaker>`|<span data-ttu-id="53edf-210">`StartObject` uzel pro `NoiseMaker`</span><span class="sxs-lookup"><span data-stu-id="53edf-210">`StartObject` node for `NoiseMaker`</span></span><br /><br /> <span data-ttu-id="53edf-211">`StartMember` uzel pro `_Initialization`</span><span class="sxs-lookup"><span data-stu-id="53edf-211">`StartMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="53edf-212">`Value` uzel pro hodnotu inicializačního řetězce `"Loudest"`</span><span class="sxs-lookup"><span data-stu-id="53edf-212">`Value` node for the initialization value string `"Loudest"`</span></span><br /><br /> <span data-ttu-id="53edf-213">`EndMember` uzel pro `_Initialization`</span><span class="sxs-lookup"><span data-stu-id="53edf-213">`EndMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="53edf-214">`EndObject` pro `NoiseMaker`</span><span class="sxs-lookup"><span data-stu-id="53edf-214">`EndObject` for `NoiseMaker`</span></span>|
||<span data-ttu-id="53edf-215">`EndMember` uzel pro implicitní `FavorCollection` vlastnosti položky.</span><span class="sxs-lookup"><span data-stu-id="53edf-215">`EndMember` node for implicit `FavorCollection` items property.</span></span>|
||<span data-ttu-id="53edf-216">`EndObject` uzel pro implicitní `FavorCollection`</span><span class="sxs-lookup"><span data-stu-id="53edf-216">`EndObject` node for implicit `FavorCollection`</span></span>|
|`</Party.Favors>`|<span data-ttu-id="53edf-217">`EndMember` pro `Favors`</span><span class="sxs-lookup"><span data-stu-id="53edf-217">`EndMember` for `Favors`</span></span>|
|`</Party>`|<span data-ttu-id="53edf-218">`EndObject` pro `Party`</span><span class="sxs-lookup"><span data-stu-id="53edf-218">`EndObject` for `Party`</span></span>|

<span data-ttu-id="53edf-219">V datovém proudu uzlu XAML se spoléháte na toto chování:</span><span class="sxs-lookup"><span data-stu-id="53edf-219">In the XAML node stream, you can rely on the following behavior:</span></span>

- <span data-ttu-id="53edf-220">Pokud `Namespace` uzel existuje, přidá se do datového proudu bezprostředně před `StartObject` , který je deklarován obor názvů XAML s `xmlns`.</span><span class="sxs-lookup"><span data-stu-id="53edf-220">If a `Namespace` node exists, it is added to the stream immediately before the `StartObject` that declared the XAML namespace with `xmlns`.</span></span> <span data-ttu-id="53edf-221">Podívejte se na předchozí tabulky pomocí XAML a příklad datovém proudu uzlu znovu.</span><span class="sxs-lookup"><span data-stu-id="53edf-221">Look at the previous table with the XAML and example node stream again.</span></span> <span data-ttu-id="53edf-222">Všimněte si, že jak `StartObject` a `Namespace` uzly zdá se, že přehození oproti deklarace pozice v textu značky.</span><span class="sxs-lookup"><span data-stu-id="53edf-222">Notice how the `StartObject` and `Namespace` nodes seem to be transposed versus their declaration positions in text markup.</span></span> <span data-ttu-id="53edf-223">Toto je zástupce chování, ve kterém uzly oboru názvů vždy zobrazují před uzlem použijí v datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="53edf-223">This is representative of the behavior where the namespace nodes always appear ahead of the node they apply to in the node stream.</span></span> <span data-ttu-id="53edf-224">Účelem tohoto návrhu je, informace o oboru názvů je důležité objektu zapisovače a musí být známo předtím, než objekt zapisovače objekt se pokusí provést typ mapování nebo jinak zpracovat.</span><span class="sxs-lookup"><span data-stu-id="53edf-224">The purpose of this design is that the namespace information is vital to object writers and must be known before the object writer attempts to perform type mapping or otherwise process the object.</span></span> <span data-ttu-id="53edf-225">Uvedení informace oboru názvů XAML náskok před jeho oboru aplikace v datovém proudu zjednodušuje vždy zpracovat datový proud uzlu v jeho prezentované pořadí.</span><span class="sxs-lookup"><span data-stu-id="53edf-225">Placing the XAML namespace information ahead of its application scope in the stream makes it simpler to always process the node stream in its presented order.</span></span>

- <span data-ttu-id="53edf-226">Z důvodu výše zvážit, je jeden nebo více `Namespace` uzly, které si přečíst nejprve ve většině případů značkách reálného světa při procházení uzly od začátku, ne `StartObject` kořene.</span><span class="sxs-lookup"><span data-stu-id="53edf-226">Because of the above consideration, it is one or more `Namespace` nodes that you read first in most real-world markup cases when traversing nodes from the start, not the `StartObject` of the root.</span></span>

- <span data-ttu-id="53edf-227">A `StartObject` uzlu může být následován `StartMember`, `Value`, nebo ihned `EndObject`.</span><span class="sxs-lookup"><span data-stu-id="53edf-227">A `StartObject` node can be followed by `StartMember`, `Value`, or an immediate `EndObject`.</span></span> <span data-ttu-id="53edf-228">To se nikdy vzápětí jiného `StartObject`.</span><span class="sxs-lookup"><span data-stu-id="53edf-228">It is never followed immediately by another `StartObject`.</span></span>

- <span data-ttu-id="53edf-229">A `StartMember` může být následován `StartObject`, `Value`, nebo ihned `EndMember`.</span><span class="sxs-lookup"><span data-stu-id="53edf-229">A `StartMember` can be followed by a `StartObject`, `Value`, or an immediate `EndMember`.</span></span> <span data-ttu-id="53edf-230">To může být následován `GetObject`, pro členy, kde hodnota by měl pocházet z existující hodnoty nadřazeného objektu spíše než `StartObject` , které byste měli vytvořit instanci novou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="53edf-230">It can be followed by `GetObject`, for members where the value is supposed to come from an existing value of the parent object rather than a `StartObject` that would instantiate a new value.</span></span> <span data-ttu-id="53edf-231">To může být následován znakem `Namespace` uzlu, která se vztahuje na nadcházející `StartObject`.</span><span class="sxs-lookup"><span data-stu-id="53edf-231">It can also be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span> <span data-ttu-id="53edf-232">To se nikdy vzápětí jiného `StartMember`.</span><span class="sxs-lookup"><span data-stu-id="53edf-232">It is never followed immediately by another `StartMember`.</span></span>

- <span data-ttu-id="53edf-233">A `Value` samotná hodnota představuje uzel; není žádný "EndValue".</span><span class="sxs-lookup"><span data-stu-id="53edf-233">A `Value` node represents the value itself; there is no "EndValue".</span></span> <span data-ttu-id="53edf-234">To může následovat pouze `EndMember`.</span><span class="sxs-lookup"><span data-stu-id="53edf-234">It can be followed only by an `EndMember`.</span></span>

    - <span data-ttu-id="53edf-235">Text XAML inicializace objektu by mohly používat konstrukce nemá za následek struktury hodnotu objektu.</span><span class="sxs-lookup"><span data-stu-id="53edf-235">XAML initialization text of the object as might be used by construction does not result in an Object-Value structure.</span></span> <span data-ttu-id="53edf-236">Místo toho uzel vyhrazené člena pro člena s názvem `_Initialization` se vytvoří.</span><span class="sxs-lookup"><span data-stu-id="53edf-236">Instead, a dedicated member node for a member named `_Initialization` is created.</span></span> <span data-ttu-id="53edf-237">a tento člen uzel obsahuje inicializační řetězec hodnoty.</span><span class="sxs-lookup"><span data-stu-id="53edf-237">and that member node contains the initialization value string.</span></span> <span data-ttu-id="53edf-238">Pokud existuje, `_Initialization` je vždy první `StartMember`.</span><span class="sxs-lookup"><span data-stu-id="53edf-238">If it exists, `_Initialization` is always the first `StartMember`.</span></span> <span data-ttu-id="53edf-239">`_Initialization` může být kvalifikovaný v některých služeb reprezentace XAML s obor namescope XAML jazyka XAML pro vysvětlení, že `_Initialization` se nenachází ve vlastnosti definované v typy zálohování.</span><span class="sxs-lookup"><span data-stu-id="53edf-239">`_Initialization` may be qualified in some XAML services representations with the XAML language XAML namescope, to clarify that `_Initialization` is not a defined property in backing types.</span></span>

    - <span data-ttu-id="53edf-240">Hodnota člena kombinaci představuje nastavení atributu hodnoty.</span><span class="sxs-lookup"><span data-stu-id="53edf-240">A Member-Value combination represents an attribute setting of the value.</span></span> <span data-ttu-id="53edf-241">Nakonec pravděpodobně převaděč hodnot při zpracování tato hodnota a hodnota je prostý řetězec.</span><span class="sxs-lookup"><span data-stu-id="53edf-241">There might eventually be a value converter involved in processing this value, and the value is a plain string.</span></span> <span data-ttu-id="53edf-242">Nicméně, který není vyhodnocen, dokud zapisovače objektu XAML zpracuje tento datový proud uzlu.</span><span class="sxs-lookup"><span data-stu-id="53edf-242">However, that is not evaluated until a XAML object writer processes this node stream.</span></span> <span data-ttu-id="53edf-243">Objekt zapisovače XAML má potřebné kontext schématu XAML, mapování typu systému a další podporu potřebné pro převody hodnot.</span><span class="sxs-lookup"><span data-stu-id="53edf-243">The XAML object writer possesses the necessary XAML schema context, type system mapping, and other support needed for value conversions.</span></span>

- <span data-ttu-id="53edf-244">`EndMember` Uzlu může být následován znakem `StartMember` uzel pro následující člen, nebo podle `EndObject` uzlu vlastníka člena.</span><span class="sxs-lookup"><span data-stu-id="53edf-244">An `EndMember` node can be followed by a `StartMember` node for a subsequent member, or by an `EndObject` node for the member owner.</span></span>

- <span data-ttu-id="53edf-245">`EndObject` Uzlu může být následován `EndMember` uzlu.</span><span class="sxs-lookup"><span data-stu-id="53edf-245">An `EndObject` node can be followed by an `EndMember` node.</span></span> <span data-ttu-id="53edf-246">To může být následován znakem `StartObject` uzel pro případy, ve kterém jsou objekty partnerské uzly v položky kolekce.</span><span class="sxs-lookup"><span data-stu-id="53edf-246">It can also be followed by a `StartObject` node for cases where the objects are peers in a collection's items.</span></span> <span data-ttu-id="53edf-247">Nebo může být následován znakem `Namespace` uzlu, která se vztahuje na nadcházející `StartObject`.</span><span class="sxs-lookup"><span data-stu-id="53edf-247">Or it can be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span>

    - <span data-ttu-id="53edf-248">Pro jedinečný případ ukončení datový proud celého uzlu `EndObject` z kořenové nenásleduje nic; čtecí modul je nyní end souboru, a <xref:System.Xaml.XamlReader.Read%2A> vrátí `false`.</span><span class="sxs-lookup"><span data-stu-id="53edf-248">For the unique case of closing the entire node stream, the `EndObject` of the root is not followed by anything; the reader is now end-of-file, and <xref:System.Xaml.XamlReader.Read%2A> returns `false`.</span></span>

<a name="value_converters_and_the_xaml_node_stream"></a>

## <a name="value-converters-and-the-xaml-node-stream"></a><span data-ttu-id="53edf-249">Převaděče hodnot a Stream uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="53edf-249">Value Converters and the XAML Node Stream</span></span>

<span data-ttu-id="53edf-250">Převaděč hodnoty je obecný termín pro rozšíření značek, konvertor typu (včetně hodnoty serializátory) nebo jiný vyhrazený třídu, která se hlásí jako převaděč hodnoty prostřednictvím typu systému XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-250">A value converter is a general term for a markup extension, a type converter (including value serializers) or another dedicated class that is reported as a value converter through the XAML type system.</span></span> <span data-ttu-id="53edf-251">V datovém proudu uzlu XAML mají velmi odlišné reprezentace použití převaděče typů a použití rozšíření značky.</span><span class="sxs-lookup"><span data-stu-id="53edf-251">In the XAML node stream, a type converter usage and a markup extension usage have very different representations.</span></span>

### <a name="type-converters-in-the-xaml-node-stream"></a><span data-ttu-id="53edf-252">Převaděče typů v Stream uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="53edf-252">Type Converters in the XAML Node Stream</span></span>

<span data-ttu-id="53edf-253">Atribut nastavit, že nakonec výsledkem použití převaděče typů v hlásí datový proud uzlu XAML jako hodnotu člena.</span><span class="sxs-lookup"><span data-stu-id="53edf-253">An attribute set that eventually results in a type converter usage is reported in the XAML node stream as a value of a member.</span></span> <span data-ttu-id="53edf-254">Datový proud uzlu XAML se nebude pokoušet vytvořit objekt instance převaděče typů a do něj předáte hodnotu.</span><span class="sxs-lookup"><span data-stu-id="53edf-254">The XAML node stream does not attempt to produce a type converter instance object and pass the value to it.</span></span> <span data-ttu-id="53edf-255">Pomocí implementace převodu konvertor typu vyžaduje volání kontext schématu XAML a jeho použití pro mapování typu.</span><span class="sxs-lookup"><span data-stu-id="53edf-255">Using a type converter's conversion implementation requires invoking the XAML schema context and using it for type-mapping.</span></span> <span data-ttu-id="53edf-256">Kontext schématu XAML dokonce určující, které třídy konvertor typu by měla sloužit ke zpracování hodnota vyžaduje nepřímo.</span><span class="sxs-lookup"><span data-stu-id="53edf-256">Even determining which type converter class should be used to process the value requires the XAML schema context indirectly.</span></span> <span data-ttu-id="53edf-257">Pokud použijete výchozí kontext schématu XAML, tyto informace jsou dostupné z typu systému XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-257">When you use the default XAML schema context, that information is available from the XAML type system.</span></span> <span data-ttu-id="53edf-258">Pokud potřebujete informace o převaděč třídy typu na úrovni datový proud uzlu XAML před připojení pro zápis XAML, získáte ho od <xref:System.Xaml.XamlMember> informace člen nastavena.</span><span class="sxs-lookup"><span data-stu-id="53edf-258">If you need the type converter class information at the XAML node stream level before connection to a XAML writer, you can obtain it from the <xref:System.Xaml.XamlMember> information of the member being set.</span></span> <span data-ttu-id="53edf-259">Ale v opačném případě vstupní typ převaděče by měl být zachovaná v datovém proudu uzlu XAML jako obyčejný hodnotu, dokud se zbývající operace, které vyžadují systém mapování typů a kontext schématu XAML jsou prováděny, například vytvoření objektu pomocí XAML objekt zapisovače.</span><span class="sxs-lookup"><span data-stu-id="53edf-259">But otherwise, type converter input should be preserved in the XAML node stream as a plain value until the remainder of operations that require the type mapping system and XAML schema context are performed, for example the object creation by a XAML object writer.</span></span>

<span data-ttu-id="53edf-260">Představte si třeba následující osnova – třída definice a používání XAML pro něj:</span><span class="sxs-lookup"><span data-stu-id="53edf-260">For example, consider the following class definition outline and XAML usage for it:</span></span>

```csharp
public class BoardSizeConverter : TypeConverter {
  //converts from string to an int[2] by splitting on an "x" char
}
public class GameBoard {
  [TypeConverter(typeof(BoardSizeConverter))]
  public int[] BoardSize; //2x2 array, initialization not shown
}
```

```xaml
<GameBoard BoardSize="8x8"/>
```

<span data-ttu-id="53edf-261">Textové vyjádření datový proud uzlu XAML pro toto použití může být vyjádřený jako následující:</span><span class="sxs-lookup"><span data-stu-id="53edf-261">A text representation of the XAML node stream for this usage could be expressed as the following:</span></span>

<span data-ttu-id="53edf-262">`StartObject` s <xref:System.Xaml.XamlType> představující `GameBoard`</span><span class="sxs-lookup"><span data-stu-id="53edf-262">`StartObject` with <xref:System.Xaml.XamlType> representing `GameBoard`</span></span>

<span data-ttu-id="53edf-263">`StartMember` s <xref:System.Xaml.XamlMember> představující `BoardSize`</span><span class="sxs-lookup"><span data-stu-id="53edf-263">`StartMember` with <xref:System.Xaml.XamlMember> representing `BoardSize`</span></span>

<span data-ttu-id="53edf-264">`Value` uzel textovým řetězcem "`8x8`"</span><span class="sxs-lookup"><span data-stu-id="53edf-264">`Value` node, with text string "`8x8`"</span></span>

<span data-ttu-id="53edf-265">`EndMember` Shody `BoardSize`</span><span class="sxs-lookup"><span data-stu-id="53edf-265">`EndMember` matches `BoardSize`</span></span>

<span data-ttu-id="53edf-266">`EndObject` Shody `GameBoard`</span><span class="sxs-lookup"><span data-stu-id="53edf-266">`EndObject` matches `GameBoard`</span></span>

<span data-ttu-id="53edf-267">Všimněte si, že neexistuje žádná instance konvertor typu v tomto datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="53edf-267">Notice that there is no type converter instance in this node stream.</span></span> <span data-ttu-id="53edf-268">Ale můžete získat informace o typu konvertoru voláním <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> na <xref:System.Xaml.XamlMember> pro `BoardSize`.</span><span class="sxs-lookup"><span data-stu-id="53edf-268">But you can get type converter information by calling <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> on the <xref:System.Xaml.XamlMember> for `BoardSize`.</span></span> <span data-ttu-id="53edf-269">Pokud máte platný kontext schématu XAML, můžete také vyvolat metody převaděč získat instanci ze <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>.</span><span class="sxs-lookup"><span data-stu-id="53edf-269">If you have a valid XAML schema context, you can also invoke the converter methods by obtaining an instance from <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>.</span></span>

### <a name="markup-extensions-in-the-xaml-node-stream"></a><span data-ttu-id="53edf-270">Rozšíření značek v Stream uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="53edf-270">Markup Extensions in the XAML Node Stream</span></span>

<span data-ttu-id="53edf-271">Použití rozšíření značky se hlásí v datovém proudu uzlu XAML jako uzel objektu v členu, kde objekt představuje instanci rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="53edf-271">A markup extension usage is reported in the XAML node stream as an object node within a member, where the object represents a markup extension instance.</span></span> <span data-ttu-id="53edf-272">Proto použití rozšíření značky se zobrazí více explicitně v reprezentaci datový proud uzlu než je použití převaděče typů a provede další informace.</span><span class="sxs-lookup"><span data-stu-id="53edf-272">Thus a markup extension usage is presented more explicitly in the node stream representation than a type converter usage is, and carries more information.</span></span> <span data-ttu-id="53edf-273"><xref:System.Xaml.XamlMember> informace nelze mít uvedli jako vám nic o rozšíření značek, protože využití je povědomí a liší se v každém případě je to možné značek; není vyhrazené a implicitní za typ nebo člen stejně jako v případě pomocí převaděče typů.</span><span class="sxs-lookup"><span data-stu-id="53edf-273"><xref:System.Xaml.XamlMember> information could not have told you anything about the markup extension, because the usage is situational and varies in each possible markup case; it is not dedicated and implicit per type or member as is the case with type converters.</span></span>

<span data-ttu-id="53edf-274">Reprezentace datového proudu uzlu přípony označení jako uzly objektu platí i v případě použití rozšíření značky proběhla do formuláře atributů ve značkách XAML text (což se stává často).</span><span class="sxs-lookup"><span data-stu-id="53edf-274">The node stream representation of markup extensions as object nodes is the case even if the markup extension usage was made in attribute form in the XAML text markup (which is often the case).</span></span> <span data-ttu-id="53edf-275">Použití rozšíření značky, které používají formuláři prvku objektu explicitní zachází stejně.</span><span class="sxs-lookup"><span data-stu-id="53edf-275">Markup extension usages that used an explicit object element form are treated the same way.</span></span>

<span data-ttu-id="53edf-276">V rámci uzlu objektu rozšíření značek mohou být členy tohoto rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="53edf-276">Within a markup extension object node, there may be members of that markup extension.</span></span> <span data-ttu-id="53edf-277">Reprezentace datový proud uzlu XAML zachová využití tohoto rozšíření značek, jestli používání pozičních parametrů více dopředu nebo využití pomocí explicitní pojmenované parametry.</span><span class="sxs-lookup"><span data-stu-id="53edf-277">The XAML node stream representation preserves the usage of that markup extension, whether that be a positional parameter usage or a usage with explicit named parameters.</span></span>

<span data-ttu-id="53edf-278">Pro použití pozičních parametrů více dopředu, datový proud uzlu XAML obsahuje vlastnost definovaný jazyk XAML `_PositionalParameters` , který zaznamenává využití.</span><span class="sxs-lookup"><span data-stu-id="53edf-278">For a positional parameter usage, the XAML node stream contains a XAML language-defined property `_PositionalParameters` that records the usage.</span></span> <span data-ttu-id="53edf-279">Tato vlastnost je obecný <xref:System.Collections.Generic.List%601> s <xref:System.Object> omezení.</span><span class="sxs-lookup"><span data-stu-id="53edf-279">This property is a generic <xref:System.Collections.Generic.List%601> with <xref:System.Object> constraint.</span></span> <span data-ttu-id="53edf-280">Omezení je objekt a není řetězec, protože odcizenou pozičních parametrů více dopředu využití může zahrnovat použití rozšíření značky vnořené v něm.</span><span class="sxs-lookup"><span data-stu-id="53edf-280">The constraint is object and not string because conceivably a positional parameter usage could contain nested markup extension usages within it.</span></span> <span data-ttu-id="53edf-281">Pro přístup k poziční parametry z využití, může iteraci v rámci seznamu a pomocí indexerů seznamu jednotlivých hodnot.</span><span class="sxs-lookup"><span data-stu-id="53edf-281">To access the positional parameters from the usage, you could iterate through the list and use the indexers for individual list values.</span></span>

<span data-ttu-id="53edf-282">Pojmenovaným parametrem využití každý pojmenovaný parametr reprezentované jako uzel člen s tímto názvem v datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="53edf-282">For a named parameter usage, each named parameter is represented as a member node of that name in the node stream.</span></span> <span data-ttu-id="53edf-283">Člen hodnoty nejsou nutně řetězce, protože to může být použití rozšíření značky vnořené.</span><span class="sxs-lookup"><span data-stu-id="53edf-283">The member values are not necessarily strings, because there could be a nested markup extension usage.</span></span>

<span data-ttu-id="53edf-284">`ProvideValue` ještě není vyvolána rozšíření, od značky.</span><span class="sxs-lookup"><span data-stu-id="53edf-284">`ProvideValue` from the markup extension is not yet invoked.</span></span> <span data-ttu-id="53edf-285">Nicméně, je vyvolána, pokud připojíte XAML čtečky a zapisovače XAML tak, aby `WriteEndObject` vyvolání při zkoumání v datovém proudu uzlu na uzel rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="53edf-285">However, it is invoked if you connect a XAML reader and XAML writer so that `WriteEndObject` is invoked on the markup extension node when you examine it in the node stream.</span></span> <span data-ttu-id="53edf-286">Z tohoto důvodu musíte obecně stejné XAML schématu kontextu k dispozici, protože by se použily, aby bylo možné formulář grafu objektů v cestě zatížení.</span><span class="sxs-lookup"><span data-stu-id="53edf-286">For this reason, you generally need the same XAML schema context available as would be used in order to form the object graph on the load path.</span></span> <span data-ttu-id="53edf-287">V opačném případě `ProvideValue` z jakoukoli marži rozšíření může vyvolat výjimky, protože nemá očekávaný služby, které jsou k dispozici.</span><span class="sxs-lookup"><span data-stu-id="53edf-287">Otherwise, `ProvideValue` from any markup extension can throw exceptions here, because it does not have expected services available.</span></span>

<a name="xaml_and_xml_languagedefined_members_in_the_xaml_node_stream"></a>

## <a name="xaml-and-xml-language-defined-members-in-the-xaml-node-stream"></a><span data-ttu-id="53edf-288">XAML a XML definice jazyk členy v Stream uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="53edf-288">XAML and XML Language-Defined Members in the XAML Node Stream</span></span>

<span data-ttu-id="53edf-289">Některé členy zavedení do datový proud uzlu XAML z důvodu interpretace a konvence čtečku XAML namísto prostřednictvím explicitní <xref:System.Xaml.XamlMember> vyhledávání nebo konstrukce.</span><span class="sxs-lookup"><span data-stu-id="53edf-289">Certain members are introduced to a XAML node stream because of interpretations and conventions of a XAML reader, instead of through an explicit <xref:System.Xaml.XamlMember> lookup or construction.</span></span> <span data-ttu-id="53edf-290">Tyto členy jsou často, direktivy XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-290">Often, these members are XAML directives.</span></span> <span data-ttu-id="53edf-291">V některých případech je operace čtení XAML, který představuje direktivu do datový proud uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-291">In some cases, it is the act of reading the XAML that introduces the directive into the XAML node stream.</span></span> <span data-ttu-id="53edf-292">Jinými slovy původní vstup XAML text neurčil explicitně member – direktiva, ale čtečky XAML vloží směrnice splníte a strukturální XAML konvence a sestavy informace v datovém proudu uzlu XAML předtím, než dojde ke ztrátě informací.</span><span class="sxs-lookup"><span data-stu-id="53edf-292">In other words, the original input XAML text did not explicitly specify the member directive, but the XAML reader inserts the directive in order to satisfy a structural XAML convention and report information in the XAML node stream before that information is lost.</span></span>

<span data-ttu-id="53edf-293">Následující poznámky k seznamu, které jsou všechny případy, kde je očekávána čtečku XAML zavést direktiv uzel člena XAML, a jak se tento uzel člen identifikovat v implementacích rozhraní .NET Framework XAML Services.</span><span class="sxs-lookup"><span data-stu-id="53edf-293">The following list notes all cases where a XAML reader is expected to introduce a directive XAML member node, and how that member node is identified in the .NET Framework XAML Services implementations.</span></span>

- <span data-ttu-id="53edf-294">**Inicializace text pro objekt uzlu:** Název tohoto uzlu člen je `_Initialization`představuje direktivu XAML a je definován v oboru názvů jazyka XAML XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-294">**Initialization text for an object node:** The name of this member node is `_Initialization`, it represents a XAML directive, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="53edf-295">Statické entity můžete získat z <xref:System.Xaml.XamlLanguage.Initialization%2A>.</span><span class="sxs-lookup"><span data-stu-id="53edf-295">You can get a static entity for it from <xref:System.Xaml.XamlLanguage.Initialization%2A>.</span></span>

- <span data-ttu-id="53edf-296">**Poziční parametry pro rozšíření značek:** Název tohoto uzlu člen je `_PositionalParameters`, a je definován v oboru názvů jazyka XAML XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-296">**Positional parameters for a markup extension:** The name of this member node is `_PositionalParameters`, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="53edf-297">Vždy obsahuje obecný seznam objektů, z nichž každý je poziční parametr předem oddělené rozdělení na `,` oddělovací znak jako dodaného vstup XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-297">It always contains a generic list of objects, each of which is a positional parameter pre-separated by splitting on the `,` delimiter character as supplied in the input XAML.</span></span> <span data-ttu-id="53edf-298">Statické entity můžete získat pro direktivu poziční parametry z <xref:System.Xaml.XamlLanguage.PositionalParameters%2A>.</span><span class="sxs-lookup"><span data-stu-id="53edf-298">You can get a static entity for the positional parameters directive from <xref:System.Xaml.XamlLanguage.PositionalParameters%2A>.</span></span>

- <span data-ttu-id="53edf-299">**Neznámá obsah:** Název tohoto uzlu člen je `_UnknownContent`.</span><span class="sxs-lookup"><span data-stu-id="53edf-299">**Unknown content:** The name of this member node is `_UnknownContent`.</span></span> <span data-ttu-id="53edf-300">Přesněji řečeno, jde <xref:System.Xaml.XamlDirective>, a je definován v oboru názvů jazyka XAML XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-300">Strictly speaking, it is a <xref:System.Xaml.XamlDirective>, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="53edf-301">Tato direktiva se používá jako sentinel pro případy, kdy elementu objektu XAML obsahuje obsah, zdroje XAML, ale v tuto chvíli k dispozici kontext schématu XAML se dá určit bez vlastnost content.</span><span class="sxs-lookup"><span data-stu-id="53edf-301">This directive is used as a sentinel for cases where a XAML object element contains content in the source XAML but no content property can be determined under the currently available XAML schema context.</span></span> <span data-ttu-id="53edf-302">Tento případ, v datovém proudu uzlu XAML můžete zjistit kontrolou pro členy s názvem `_UnknownContent`.</span><span class="sxs-lookup"><span data-stu-id="53edf-302">You can detect this case in a XAML node stream by checking for members named `_UnknownContent`.</span></span> <span data-ttu-id="53edf-303">Pokud nebyla provedena žádná další akce v XAML uzel zatížení cesty toku, výchozí <xref:System.Xaml.XamlObjectWriter> vyvolá pokus o `WriteEndObject` když narazí `_UnknownContent` člen libovolného objektu.</span><span class="sxs-lookup"><span data-stu-id="53edf-303">If no other action is taken in a load path XAML node stream, the default <xref:System.Xaml.XamlObjectWriter> throws on attempted `WriteEndObject` when it encounters the `_UnknownContent` member on any object.</span></span> <span data-ttu-id="53edf-304">Výchozí hodnota <xref:System.Xaml.XamlXmlWriter> nevyvolá a považuje za člena implicitní.</span><span class="sxs-lookup"><span data-stu-id="53edf-304">The default <xref:System.Xaml.XamlXmlWriter> does not throw, and treats the member as implicit.</span></span> <span data-ttu-id="53edf-305">Můžete získat statické entity pro `_UnknownContent` z <xref:System.Xaml.XamlLanguage.UnknownContent%2A>.</span><span class="sxs-lookup"><span data-stu-id="53edf-305">You can get a static entity for `_UnknownContent` from <xref:System.Xaml.XamlLanguage.UnknownContent%2A>.</span></span>

- <span data-ttu-id="53edf-306">**Vlastnost kolekce kolekce:** I když má vyhrazené s názvem vlastnosti, která obsahuje položky kolekce základní typ CLR tohoto třídu kolekce, která se obvykle používá pro XAML, tato vlastnost není znám o typu systému XAML před typ řešení zálohování.</span><span class="sxs-lookup"><span data-stu-id="53edf-306">**Collection property of a collection:** Although the backing CLR type of a collection class that is used for XAML usually has a dedicated named property that holds the collection items, that property is not known to a XAML type system prior to backing type resolution.</span></span> <span data-ttu-id="53edf-307">Místo toho představuje datový proud uzlu XAML `Items` zástupný text jako člen kolekce typu XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-307">Instead, the XAML node stream introduces an `Items` placeholder as a member of the collection XAML type.</span></span> <span data-ttu-id="53edf-308">V implementaci rozhraní .NET Framework XAML Services název této směrnice / člena v datovém proudu uzlu je `_Items`.</span><span class="sxs-lookup"><span data-stu-id="53edf-308">In the .NET Framework XAML Services implementation the name of this directive / member in the node stream is `_Items`.</span></span> <span data-ttu-id="53edf-309">Konstanty pro tuto direktivu lze získat z <xref:System.Xaml.XamlLanguage.Items%2A>.</span><span class="sxs-lookup"><span data-stu-id="53edf-309">A constant for this directive can be obtained from <xref:System.Xaml.XamlLanguage.Items%2A>.</span></span>

    <span data-ttu-id="53edf-310">Všimněte si, že datový proud uzlu XAML může obsahovat vlastnosti Items u položek, které vyjít najevo, nebude možné analyzovat podle typu řešení zálohování a kontext schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="53edf-310">Note that a XAML node stream might contain an Items property with items that turn out to not be parsable based on the backing type resolution and XAML schema context.</span></span> <span data-ttu-id="53edf-311">Například</span><span class="sxs-lookup"><span data-stu-id="53edf-311">For example,</span></span>

- <span data-ttu-id="53edf-312">**XML definované členy:** Definice XML `xml:base`, `xml:lang` a `xml:space` členy označené jako s názvem direktivy XAML `base`, `lang`, a `space` v implementacích rozhraní .NET Framework XAML Services.</span><span class="sxs-lookup"><span data-stu-id="53edf-312">**XML-defined members:** The XML-defined `xml:base`, `xml:lang` and `xml:space` members are reported as XAML directives named `base`, `lang`, and `space` in the .NET Framework XAML Services implementations.</span></span> <span data-ttu-id="53edf-313">Obor názvů pro toto je obor názvů XML `http://www.w3.org/XML/1998/namespace`.</span><span class="sxs-lookup"><span data-stu-id="53edf-313">The namespace for these is the XML namespace `http://www.w3.org/XML/1998/namespace`.</span></span> <span data-ttu-id="53edf-314">Konstanty pro každou z nich můžete získat z <xref:System.Xaml.XamlLanguage>.</span><span class="sxs-lookup"><span data-stu-id="53edf-314">Constants for each of these can be obtained from <xref:System.Xaml.XamlLanguage>.</span></span>

## <a name="node-order"></a><span data-ttu-id="53edf-315">Uzel pořadí</span><span class="sxs-lookup"><span data-stu-id="53edf-315">Node Order</span></span>

<span data-ttu-id="53edf-316">V některých případech <xref:System.Xaml.XamlXmlReader> změní pořadí uzlů XAML v proudu uzlu XAML, a pořadí uzly zobrazí-li zobrazit v kódu nebo pokud zpracování formátu XML.</span><span class="sxs-lookup"><span data-stu-id="53edf-316">In some cases, <xref:System.Xaml.XamlXmlReader> changes the order of XAML nodes in the XAML node stream, versus the order the nodes appear if viewed in the markup or if processed as XML.</span></span> <span data-ttu-id="53edf-317">To se provádí, aby bylo možné pořadí uzly tak, aby <xref:System.Xaml.XamlObjectWriter> může zpracovat datový proud uzlu dopředné způsobem.</span><span class="sxs-lookup"><span data-stu-id="53edf-317">This is done in order to order the nodes such that a <xref:System.Xaml.XamlObjectWriter> can process the node stream in a forward-only manner.</span></span>  <span data-ttu-id="53edf-318">V rozhraní .NET Framework XAML Services čtečky XAML změní pořadí uzly spíše než byste museli opustit tuto úlohu do zapisovače XAML optimalizace výkonu pro spotřebitele zapisovače objektu XAML datový proud uzlu.</span><span class="sxs-lookup"><span data-stu-id="53edf-318">In .NET Framework XAML Services, the XAML reader reorders nodes rather than leaving this task to the XAML writer, as a performance optimization for XAML object writer consumers of the node stream.</span></span>

<span data-ttu-id="53edf-319">Některé direktivy jsou určené konkrétně pro zajištění Další informace pro vytvoření objektu z objektu elementu.</span><span class="sxs-lookup"><span data-stu-id="53edf-319">Certain directives are intended specifically to provide more information for the creation of an object from an object element.</span></span> <span data-ttu-id="53edf-320">Tyto směrnice jsou: `Initialization`, `PositionalParameters`, `TypeArguments`, `FactoryMethod`, `Arguments`.</span><span class="sxs-lookup"><span data-stu-id="53edf-320">These directives are: `Initialization`, `PositionalParameters`, `TypeArguments`, `FactoryMethod`, `Arguments`.</span></span> <span data-ttu-id="53edf-321">Pokus o tyto direktivy umístit jako první členy v datovém proudu uzlu po objektu rozhraní .NET Framework XAML Services XAML čtenáři `StartObject`, z důvodů, které jsou vysvětlené v následující části.</span><span class="sxs-lookup"><span data-stu-id="53edf-321">The .NET Framework XAML Services XAML readers attempt to place these directives as the first members in the node stream following an object's `StartObject`, for reasons that are explained in the next section.</span></span>

### <a name="xamlobjectwriter-behavior-and-node-order"></a><span data-ttu-id="53edf-322">Chování XamlObjectWriter a pořadí uzlů</span><span class="sxs-lookup"><span data-stu-id="53edf-322">XamlObjectWriter Behavior and Node Order</span></span>

<span data-ttu-id="53edf-323">`StartObject` k <xref:System.Xaml.XamlObjectWriter> není nutně signál, který objekt zapisovače XAML okamžitě vytvořit instanci objektu.</span><span class="sxs-lookup"><span data-stu-id="53edf-323">`StartObject` to a <xref:System.Xaml.XamlObjectWriter> is not necessarily a signal to the XAML object writer to immediately construct the object instance.</span></span> <span data-ttu-id="53edf-324">XAML obsahuje několik funkcí jazyka, které umožňují inicializovat objekt s další vstupy a nespoléhala se výhradně na vyvolání výchozího konstruktoru vytvoří počáteční objekt a teprve pak nastavení vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="53edf-324">XAML includes several language features that make it possible to initialize an object with additional input, and to not rely entirely on invoking a default constructor to produce the initial object, and only then setting properties.</span></span> <span data-ttu-id="53edf-325">Tyto funkce patří: <xref:System.Windows.Markup.XamlDeferLoadAttribute>; inicializace textu. [x: TypeArguments](x-typearguments-directive.md); poziční parametry rozšíření značek; metody pro vytváření objektů a související [x: Arguments](x-arguments-directive.md) uzly (XAML 2009).</span><span class="sxs-lookup"><span data-stu-id="53edf-325">These features include: <xref:System.Windows.Markup.XamlDeferLoadAttribute>; initialization text; [x:TypeArguments](x-typearguments-directive.md); positional parameters of a markup extension; factory methods and associated [x:Arguments](x-arguments-directive.md) nodes (XAML 2009).</span></span> <span data-ttu-id="53edf-326">Každá z těchto případů zpoždění konstrukce skutečný objekt a vzhledem k tomu, že je pořadí změníte datový proud uzlu, můžete zapisovače objektu XAML Spolehněte se na chování ve skutečnosti vytváření instance pokaždé, když se zjistila člen start, které nejsou konkrétně konstrukce směrnice pro tento typ objektu.</span><span class="sxs-lookup"><span data-stu-id="53edf-326">Each of these cases delay the actual object construction, and because the node stream is reordered, the XAML object writer can rely on a behavior of actually constructing the instance whenever a start member is encountered that is not specifically a construction directive for that object type.</span></span>

### <a name="getobject"></a><span data-ttu-id="53edf-327">Funkce GetObject</span><span class="sxs-lookup"><span data-stu-id="53edf-327">GetObject</span></span>

<span data-ttu-id="53edf-328">`GetObject` představuje uzel s XAML, kde místo vytváření nového objektu zapisovače objektu XAML by měl místo toho získat hodnoty vlastnosti nadřazeného objektu.</span><span class="sxs-lookup"><span data-stu-id="53edf-328">`GetObject` represents a XAML node where rather than constructing a new object, a XAML object writer should instead get the value of the object's containing property.</span></span> <span data-ttu-id="53edf-329">Obvyklý případ kde `GetObject` nebude nalezen uzel v uzlu XAML je pro objekt kolekce nebo objekt slovníku při nadřazenou vlastnost je záměrně jen pro čtení v objektovém modelu základního typu.</span><span class="sxs-lookup"><span data-stu-id="53edf-329">A typical  case where a `GetObject` node is encountered in a XAML node stream is for a collection object or a dictionary object, when the containing property is deliberately read-only in the backing type's object model.</span></span> <span data-ttu-id="53edf-330">V tomto scénáři kolekci ani slovník často je vytvořen a inicializován (obvykle prázdná) logikou inicializace vlastnícího typu.</span><span class="sxs-lookup"><span data-stu-id="53edf-330">In this scenario, the collection or dictionary often is created and initialized (usually empty) by the initialization logic of an owning type.</span></span>

## <a name="see-also"></a><span data-ttu-id="53edf-331">Viz také:</span><span class="sxs-lookup"><span data-stu-id="53edf-331">See also</span></span>

- <xref:System.Xaml.XamlObjectReader>
- [<span data-ttu-id="53edf-332">XAML Services</span><span class="sxs-lookup"><span data-stu-id="53edf-332">XAML Services</span></span>](index.md)
- [<span data-ttu-id="53edf-333">Obory názvů jazyka XAML</span><span class="sxs-lookup"><span data-stu-id="53edf-333">XAML Namespaces</span></span>](xaml-namespaces-for-net-framework-xaml-services.md)
