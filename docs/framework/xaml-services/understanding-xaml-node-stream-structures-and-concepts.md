---
title: Principy struktur a koncepcí datových proudů uzlů XAML
ms.date: 03/30/2017
helpviewer_keywords:
- XAML node streams [XAML Services]
- nodes [XAML Services], XAML node stream
- XAML [XAML Services], XAML node streams
ms.assetid: 7c11abec-1075-474c-9d9b-778e5dab21c3
ms.openlocfilehash: 2c8093c3ef497bd836427f71098e62626f228e24
ms.sourcegitcommit: 22be09204266253d45ece46f51cc6f080f2b3fd6
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 11/07/2019
ms.locfileid: "73733392"
---
# <a name="understanding-xaml-node-stream-structures-and-concepts"></a><span data-ttu-id="427a7-102">Principy struktur a koncepcí datových proudů uzlů XAML</span><span class="sxs-lookup"><span data-stu-id="427a7-102">Understanding XAML Node Stream Structures and Concepts</span></span>

<span data-ttu-id="427a7-103">Čtečky XAML a zapisovače XAML, jak jsou implementovány v .NET Framework služby XAML, jsou založeny na konceptu návrhu datového proudu uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-103">XAML readers and XAML writers as implemented in .NET Framework XAML Services are based on the design concept of a XAML node stream.</span></span> <span data-ttu-id="427a7-104">Datový proud uzlu XAML je koncepční sada uzlů XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-104">The XAML node stream is a conceptualization of a set of XAML nodes.</span></span> <span data-ttu-id="427a7-105">V tomto principu provede procesor XAML strukturu vztahů uzlů v jazyce XAML v jednom okamžiku.</span><span class="sxs-lookup"><span data-stu-id="427a7-105">In this conceptualization, a XAML processor walks through the structure of the node relationships in the XAML one at a time.</span></span> <span data-ttu-id="427a7-106">V otevřeném datovém proudu uzlu XAML již existuje pouze jeden aktuální záznam nebo aktuální pozice, přičemž mnoho aspektů rozhraní API hlásí pouze informace, které jsou z této pozice k dispozici.</span><span class="sxs-lookup"><span data-stu-id="427a7-106">At any time, only one current record or current position exists in an open XAML node stream, and many aspects of the API report only the information available from that position.</span></span> <span data-ttu-id="427a7-107">Aktuální uzel v datovém proudu uzlu XAML lze popsat jako objekt, člen nebo hodnotu.</span><span class="sxs-lookup"><span data-stu-id="427a7-107">The current node in a XAML node stream can be described as being an object, a member, or a value.</span></span> <span data-ttu-id="427a7-108">Čtečky XAML mohou komunikovat s moduly pro zápis XAML a povolit programu zobrazení, interakci s nebo změnou obsahu datového proudu uzlu XAML během cesty načtení nebo operace uložení cesty, která zahrnuje XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-108">By treating XAML as a XAML node stream, XAML readers can communicate with XAML writers and enable a program to view, interact with, or alter the contents of a XAML node stream during either a load path or a save path operation that involves XAML.</span></span> <span data-ttu-id="427a7-109">Návrh rozhraní API pro čtení a zápis v XAML a koncept datového proudu uzlu XAML se podobá předchozím souvisejícím designům a návrhům pro zápis a k konceptům, jako je XML model DOM (Document Object Model) (DOM) a <xref:System.Xml.XmlReader> a <xref:System.Xml.XmlWriter> tříd.</span><span class="sxs-lookup"><span data-stu-id="427a7-109">XAML reader and writer API design and the XAML node stream concept are similar to previous related reader and writer designs and concepts, such as the XML Document Object Model (DOM) and the <xref:System.Xml.XmlReader> and <xref:System.Xml.XmlWriter> classes.</span></span> <span data-ttu-id="427a7-110">Toto téma popisuje koncepty pro streamy uzlu XAML a popisuje, jak můžete napsat rutiny, které pracují s reprezentacemi XAML na úrovni uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-110">This topic discusses XAML node stream concepts and describes how you can write routines that interact with XAML representations at the XAML node level.</span></span>

<a name="loading_into_a_xaml_reader"></a>

## <a name="loading-xaml-into-a-xaml-reader"></a><span data-ttu-id="427a7-111">Načtení XAML do čtečky XAML</span><span class="sxs-lookup"><span data-stu-id="427a7-111">Loading XAML into a XAML Reader</span></span>

<span data-ttu-id="427a7-112">Základní <xref:System.Xaml.XamlReader> třída nedeklaruje konkrétní techniku pro načtení počátečního XAML do čtečky XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-112">The base <xref:System.Xaml.XamlReader> class does not declare a particular technique for loading the initial XAML into a XAML reader.</span></span> <span data-ttu-id="427a7-113">Namísto toho odvozená třída deklaruje a implementuje techniku načítání, včetně obecných vlastností a omezení jejich vstupního zdroje pro jazyk XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-113">Instead, a derived class declares and implements the loading technique, including the general characteristics and constraints of its input source for XAML.</span></span> <span data-ttu-id="427a7-114">Například <xref:System.Xaml.XamlObjectReader> čte graf objektu, počínaje zdrojem vstupu jednoho objektu, který představuje kořen nebo základ.</span><span class="sxs-lookup"><span data-stu-id="427a7-114">For example, a <xref:System.Xaml.XamlObjectReader> reads an object graph, starting from the input source of a single object that represents the root or base.</span></span> <span data-ttu-id="427a7-115"><xref:System.Xaml.XamlObjectReader> potom vytvoří datový proud uzlu XAML z grafu objektu.</span><span class="sxs-lookup"><span data-stu-id="427a7-115">The <xref:System.Xaml.XamlObjectReader> then produces a XAML node stream from the object graph.</span></span>

<span data-ttu-id="427a7-116">Nejvýraznější .NET Framework služby XAML – definovaná <xref:System.Xaml.XamlReader> podtřídou je <xref:System.Xaml.XamlXmlReader>.</span><span class="sxs-lookup"><span data-stu-id="427a7-116">The most prominent .NET Framework XAML Services–defined <xref:System.Xaml.XamlReader> subclass is <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="427a7-117"><xref:System.Xaml.XamlXmlReader> načte počáteční kód XAML buď načtením textového souboru přímo pomocí datového proudu nebo cesty k souboru, nebo nepřímo prostřednictvím související třídy čtenářů, jako je například <xref:System.IO.TextReader>.</span><span class="sxs-lookup"><span data-stu-id="427a7-117"><xref:System.Xaml.XamlXmlReader> loads the initial XAML, either by loading a text file directly through a stream or file path, or indirectly through a related reader class such as <xref:System.IO.TextReader>.</span></span> <span data-ttu-id="427a7-118"><xref:System.Xaml.XamlReader> lze představit jako obsahující celou vstupní zdroj XAML poté, co byl načten.</span><span class="sxs-lookup"><span data-stu-id="427a7-118">The <xref:System.Xaml.XamlReader> can be thought of as containing the entirety of the XAML input source after it has loaded.</span></span> <span data-ttu-id="427a7-119">Rozhraní <xref:System.Xaml.XamlReader> Base API je však navrženo tak, aby čtenář komunikoval s jedním uzlem XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-119">However, the <xref:System.Xaml.XamlReader> base API is designed so that the reader is interacting with a single node of the XAML.</span></span> <span data-ttu-id="427a7-120">Při prvním načtení je prvním jedním uzlem, ke kterému se setkáte, kořenem XAML a jeho počátečním objektem.</span><span class="sxs-lookup"><span data-stu-id="427a7-120">When first loaded, the first single node you encounter is the root of the XAML, and its start object.</span></span>

### <a name="the-xaml-node-stream-concept"></a><span data-ttu-id="427a7-121">Koncept datového proudu uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="427a7-121">The XAML Node Stream Concept</span></span>

<span data-ttu-id="427a7-122">Pokud jste všeobecně obeznámeni s modelem DOM, metafora stromu nebo přístupem založenými na dotazech k přístupu k technologiím založeným na jazyce XML, je užitečný způsob, jak conceptualize datový proud uzlu XAML, je následující.</span><span class="sxs-lookup"><span data-stu-id="427a7-122">If you are generally more familiar with a DOM, tree metaphor, or query-based approach towards accessing XML-based technologies, a helpful way to conceptualize a XAML node stream is as follows.</span></span> <span data-ttu-id="427a7-123">Představte si, že načtený kód XAML je model DOM nebo strom, ve kterém je každý možný uzel rozbalený celým způsobem a následně zobrazený lineárně.</span><span class="sxs-lookup"><span data-stu-id="427a7-123">Imagine that the loaded XAML is a DOM or a tree where every possible node is expanded all the way, and then presented linearly.</span></span> <span data-ttu-id="427a7-124">Při přechodu mezi uzly můžete přecházet "in" nebo "out" úrovní, které by byly relevantní pro model DOM, ale datový proud uzlu XAML explicitně nesleduje, protože tyto koncepty úrovně nejsou relevantní pro datový proud uzlu.</span><span class="sxs-lookup"><span data-stu-id="427a7-124">As you advance through the nodes, you might be traversing "in" or "out" of levels that would be relevant to a DOM, but the XAML node stream does not explicitly keep track because these level concepts are not relevant to a node stream.</span></span> <span data-ttu-id="427a7-125">Datový proud uzlu má "aktuální" pozici, ale pokud jste sami neuložili jiné části streamu jako odkazy, každý aspekt streamu uzlu, který je jiný než aktuální pozice uzlu, je mimo zobrazení.</span><span class="sxs-lookup"><span data-stu-id="427a7-125">The node stream has a "current" position, but unless you have stored other parts of the stream yourself as references, every aspect of the node stream other than the current node position is out of view.</span></span>

<span data-ttu-id="427a7-126">Koncept datového proudu uzlu XAML má významnou výhodu, že pokud procházíte celým datovým proudem uzlu, máte jistotu, že jste zpracovali celé vyjádření reprezentace v jazyce XAML; Nemusíte se obávat, že dotaz, operace modelu DOM nebo nějaký jiný nelineární přístup ke zpracování informací neobsahují část úplné reprezentace XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-126">The XAML node stream concept has the notable advantage that if you go through the entire node stream, you are assured that you have processed the entire XAML representation; you do not need to worry that a query, a DOM operation, or some other nonlinear approach to processing information has missed some part of the complete XAML representation.</span></span> <span data-ttu-id="427a7-127">Z tohoto důvodu je reprezentace datového proudu uzlu XAML ideální pro připojení čtenářů XAML a zapisovačů XAML a pro poskytnutí systému, kde můžete vložit vlastní proces, který funguje mezi fázemi čtení a zápisu operace zpracování XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-127">For this reason, the XAML node stream representation is ideal both for connecting XAML readers and XAML writers, and for providing a system where you can insert your own process that acts between the read and write phases of a XAML processing operation.</span></span> <span data-ttu-id="427a7-128">V mnoha případech je pořadí uzlů v datovém proudu uzlu XAML záměrně optimalizováno nebo přeobjednáno čtečkami XAML a jak se může pořadí zobrazit ve zdrojovém textu, binárním souboru nebo grafu objektů.</span><span class="sxs-lookup"><span data-stu-id="427a7-128">In many cases, the ordering of nodes in the XAML node stream is deliberately optimized or reordered by XAML readers versus how the order might appear in the source text, binary, or object graph.</span></span> <span data-ttu-id="427a7-129">Toto chování je určeno k vykonání architektury zpracování XAML, kdy zapisovače XAML nejsou nikdy v pozici, kde se musí v proudu uzlů vrátit zpět.</span><span class="sxs-lookup"><span data-stu-id="427a7-129">This behavior is intended to enforce a XAML processing architecture whereby XAML writers are never in a position where they have to go "back" in the node stream.</span></span> <span data-ttu-id="427a7-130">V ideálním případě by měly být všechny operace zápisu XAML schopny pracovat na kontextu schématu a na aktuální pozici datového proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="427a7-130">Ideally, all XAML write operations should be able to act based on schema context plus the current position of the node stream.</span></span>

<a name="a_basic_reading_node_loop"></a>

## <a name="a-basic-reading-node-loop"></a><span data-ttu-id="427a7-131">Základní smyčka uzlu čtení</span><span class="sxs-lookup"><span data-stu-id="427a7-131">A Basic Reading Node Loop</span></span>

<span data-ttu-id="427a7-132">Základní smyčka uzlu čtení pro zkoumání datového proudu uzlu XAML se skládá z následujících konceptů.</span><span class="sxs-lookup"><span data-stu-id="427a7-132">A basic reading node loop for examining a XAML node stream consists of the following concepts.</span></span> <span data-ttu-id="427a7-133">Pro účely smyček uzlů, jak je popsáno v tomto tématu, Předpokládejme, že čtete textový soubor XAML čitelný pomocí <xref:System.Xaml.XamlXmlReader>.</span><span class="sxs-lookup"><span data-stu-id="427a7-133">For purposes of node loops as discussed in this topic, assume that you are reading a text-based, human-readable XAML file using <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="427a7-134">Odkazy v této části odkazují na konkrétní rozhraní API smyčky uzlu XAML implementované <xref:System.Xaml.XamlXmlReader>.</span><span class="sxs-lookup"><span data-stu-id="427a7-134">The links in this section refer to the particular XAML node loop API implemented by <xref:System.Xaml.XamlXmlReader>.</span></span>

- <span data-ttu-id="427a7-135">Ujistěte se, že nejste na konci datového proudu uzlu XAML (Zkontrolujte <xref:System.Xaml.XamlXmlReader.IsEof%2A>, nebo použijte návratovou hodnotu <xref:System.Xaml.XamlXmlReader.Read%2A>).</span><span class="sxs-lookup"><span data-stu-id="427a7-135">Make sure that you are not at the end of the XAML node stream (check <xref:System.Xaml.XamlXmlReader.IsEof%2A>, or use the <xref:System.Xaml.XamlXmlReader.Read%2A> return value).</span></span> <span data-ttu-id="427a7-136">Pokud jste na konci streamu, neexistuje žádný aktuální uzel a měli byste skončit.</span><span class="sxs-lookup"><span data-stu-id="427a7-136">If you are at the end of the stream, there is no current node and you should exit.</span></span>

- <span data-ttu-id="427a7-137">Ověřte, jaký typ uzlu datový proud uzlu XAML aktuálně zpřístupňuje voláním <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span><span class="sxs-lookup"><span data-stu-id="427a7-137">Check what type of node the XAML node stream currently exposes by calling <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span>

- <span data-ttu-id="427a7-138">Pokud máte přidružený modul pro zápis objektů XAML, který je připojený přímo, obecně v tomto okamžiku zavoláte <xref:System.Xaml.XamlWriter.WriteNode%2A>.</span><span class="sxs-lookup"><span data-stu-id="427a7-138">If you have an associated XAML object writer that is connected directly, you generally call <xref:System.Xaml.XamlWriter.WriteNode%2A> at this point.</span></span>

- <span data-ttu-id="427a7-139">Na základě toho, které <xref:System.Xaml.XamlNodeType> jsou hlášeny jako aktuální uzel nebo aktuální záznam, zavolejte jednu z následujících hodnot pro získání informací o obsahu uzlu:</span><span class="sxs-lookup"><span data-stu-id="427a7-139">Based on which <xref:System.Xaml.XamlNodeType> is reported as the current node or current record, call one of the following to obtain information about the node contents:</span></span>

  - <span data-ttu-id="427a7-140">Pro <xref:System.Xaml.XamlXmlReader.NodeType%2A> <xref:System.Xaml.XamlNodeType.StartMember> nebo <xref:System.Xaml.XamlNodeType.EndMember>volejte <xref:System.Xaml.XamlXmlReader.Member%2A> pro získání <xref:System.Xaml.XamlMember> informací o členu.</span><span class="sxs-lookup"><span data-stu-id="427a7-140">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartMember> or <xref:System.Xaml.XamlNodeType.EndMember>, call <xref:System.Xaml.XamlXmlReader.Member%2A> to obtain <xref:System.Xaml.XamlMember> information about a member.</span></span> <span data-ttu-id="427a7-141">Všimněte si, že člen může být <xref:System.Xaml.XamlDirective>, a proto nemusí být nutně běžným uživatelem definovaným členem předchozího objektu.</span><span class="sxs-lookup"><span data-stu-id="427a7-141">Note that the member might be a <xref:System.Xaml.XamlDirective>, and thus might not necessarily be a conventional type-defined member of the preceding object.</span></span> <span data-ttu-id="427a7-142">Například `x:Name` použít na objekt se zobrazí jako člen jazyka XAML, kde <xref:System.Xaml.XamlMember.IsDirective%2A> je true a <xref:System.Xaml.XamlMember.Name%2A> člena `Name`, s jinými vlastnostmi, které označují, že tato direktiva je v oboru názvů XAML jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-142">For example, `x:Name` applied to an object appears as a XAML member where <xref:System.Xaml.XamlMember.IsDirective%2A> is true and the <xref:System.Xaml.XamlMember.Name%2A> of the member is `Name`, with other properties indicating that this directive is under the XAML language XAML namespace.</span></span>

  - <span data-ttu-id="427a7-143">Pro <xref:System.Xaml.XamlXmlReader.NodeType%2A> <xref:System.Xaml.XamlNodeType.StartObject> nebo <xref:System.Xaml.XamlNodeType.EndObject>volejte <xref:System.Xaml.XamlXmlReader.Type%2A> pro získání <xref:System.Xaml.XamlType> informací o objektu.</span><span class="sxs-lookup"><span data-stu-id="427a7-143">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartObject> or <xref:System.Xaml.XamlNodeType.EndObject>, call <xref:System.Xaml.XamlXmlReader.Type%2A> to obtain <xref:System.Xaml.XamlType> information about an object.</span></span>

  - <span data-ttu-id="427a7-144">Pro <xref:System.Xaml.XamlXmlReader.NodeType%2A> <xref:System.Xaml.XamlNodeType.Value>volejte <xref:System.Xaml.XamlXmlReader.Value%2A>.</span><span class="sxs-lookup"><span data-stu-id="427a7-144">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.Value>, call <xref:System.Xaml.XamlXmlReader.Value%2A>.</span></span> <span data-ttu-id="427a7-145">Uzel je hodnota pouze v případě, že se jedná o nejjednodušší výraz hodnoty pro člena nebo inicializační text pro objekt (Nicméně byste měli být vědomi chování převodu typů, jak je popsáno v nadcházející části tohoto tématu).</span><span class="sxs-lookup"><span data-stu-id="427a7-145">A node is a value only if it is the simplest expression of a value for a member, or the initialization text for an object (however, you should be aware of type conversion behavior as documented in an upcoming section of this topic).</span></span>

  - <span data-ttu-id="427a7-146">Pro <xref:System.Xaml.XamlXmlReader.NodeType%2A> <xref:System.Xaml.XamlNodeType.NamespaceDeclaration>volejte <xref:System.Xaml.XamlXmlReader.Namespace%2A> pro získání informací o oboru názvů uzlu oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="427a7-146">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.NamespaceDeclaration>, call <xref:System.Xaml.XamlXmlReader.Namespace%2A> to obtain namespace information for a namespace node.</span></span>

- <span data-ttu-id="427a7-147">Zavolejte <xref:System.Xaml.XamlXmlReader.Read%2A> pro posunutí čtečky XAML na další uzel v datovém proudu uzlu XAML a opakujte kroky znovu.</span><span class="sxs-lookup"><span data-stu-id="427a7-147">Call <xref:System.Xaml.XamlXmlReader.Read%2A> to advance the XAML reader to the next node in the XAML node stream, and repeat the steps again.</span></span>

<span data-ttu-id="427a7-148">Datový proud uzlu XAML, který poskytuje .NET Framework XAML Services XAML Readers vždy poskytuje úplný a hlubokou procházejí všechny možné uzly.</span><span class="sxs-lookup"><span data-stu-id="427a7-148">The XAML node stream provided by .NET Framework XAML Services XAML readers always provides a full, deep traversal of all possible nodes.</span></span> <span data-ttu-id="427a7-149">Typické techniky řízení toku pro smyčku uzlu XAML zahrnují definování těla v rámci `while (reader.Read())`a přepínání <xref:System.Xaml.XamlXmlReader.NodeType%2A> v jednotlivých uzlech smyčkou uzlu.</span><span class="sxs-lookup"><span data-stu-id="427a7-149">Typical flow-control techniques for a XAML node loop include defining a body within `while (reader.Read())`, and switching on <xref:System.Xaml.XamlXmlReader.NodeType%2A> at each node point in the node loop.</span></span>

<span data-ttu-id="427a7-150">Pokud je datový proud uzlu na konci souboru, aktuální uzel je null.</span><span class="sxs-lookup"><span data-stu-id="427a7-150">If the node stream is at end of file, the current node is null.</span></span>

<span data-ttu-id="427a7-151">Nejjednodušší smyčka, která používá čtecí modul a zapisovač, se podobá následujícímu příkladu.</span><span class="sxs-lookup"><span data-stu-id="427a7-151">The simplest loop that uses a reader and writer resembles the following example.</span></span>

```csharp
XamlXmlReader xxr = new XamlXmlReader(new StringReader(xamlStringToLoad));
//where xamlStringToLoad is a string of well formed XAML
XamlObjectWriter xow = new XamlObjectWriter(xxr.SchemaContext);
while (xxr.Read()) {
  xow.WriteNode(xxr);
}
```

<span data-ttu-id="427a7-152">Tento základní příklad smyčky uzlu XAML zatížení je transparentně připojen ke čtečce XAML a zapisovači XAML, takže nic neliší od toho, pokud jste použili <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="427a7-152">This basic example of a load path XAML node loop transparently connects the XAML reader and XAML writer, doing nothing different than if you had used <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="427a7-153">Tato základní struktura se pak rozbalí, aby se mohla vztahovat na váš scénář čtení nebo zápisu.</span><span class="sxs-lookup"><span data-stu-id="427a7-153">But this basic structure is then expanded to apply to your reading or writing scenario.</span></span> <span data-ttu-id="427a7-154">Některé možné scénáře jsou následující:</span><span class="sxs-lookup"><span data-stu-id="427a7-154">Some possible scenarios are as follows:</span></span>

- <span data-ttu-id="427a7-155">Přepněte na <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span><span class="sxs-lookup"><span data-stu-id="427a7-155">Switch on <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span> <span data-ttu-id="427a7-156">Provádění různých akcí v závislosti na tom, který typ uzlu je právě čten.</span><span class="sxs-lookup"><span data-stu-id="427a7-156">Perform different actions depending on which node type is being read.</span></span>

- <span data-ttu-id="427a7-157">Nevolejte <xref:System.Xaml.XamlWriter.WriteNode%2A> ve všech případech.</span><span class="sxs-lookup"><span data-stu-id="427a7-157">Do not call <xref:System.Xaml.XamlWriter.WriteNode%2A> in all cases.</span></span> <span data-ttu-id="427a7-158">V některých případech <xref:System.Xaml.XamlXmlReader.NodeType%2A> volat pouze <xref:System.Xaml.XamlWriter.WriteNode%2A>.</span><span class="sxs-lookup"><span data-stu-id="427a7-158">Only call <xref:System.Xaml.XamlWriter.WriteNode%2A> in some <xref:System.Xaml.XamlXmlReader.NodeType%2A> cases.</span></span>

- <span data-ttu-id="427a7-159">V rámci logiky pro konkrétní typ uzlu Analyzujte specifiky daného uzlu a na nich budou pracovat.</span><span class="sxs-lookup"><span data-stu-id="427a7-159">Within the logic for a particular node type, analyze the specifics of that node and act on them.</span></span> <span data-ttu-id="427a7-160">Můžete například zapsat pouze objekty, které pocházejí z konkrétního oboru názvů XAML, a pak vyřadit nebo odložit objekty, které nejsou z daného oboru názvů XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-160">For example, you could write only objects that come from a particular XAML namespace, and then drop or defer any objects not from that XAML namespace.</span></span> <span data-ttu-id="427a7-161">Případně můžete vyřadit nebo jinak znovu zpracovat všechny direktivy jazyka XAML, které váš systém XAML nepodporuje jako součást zpracování členů.</span><span class="sxs-lookup"><span data-stu-id="427a7-161">Or you could drop or otherwise reprocess any XAML directives that your XAML system does not support as part of your member processing.</span></span>

- <span data-ttu-id="427a7-162">Definujte vlastní <xref:System.Xaml.XamlObjectWriter>, který přepíše `Write*` metody, případně provede mapování typů, které obchází kontext schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-162">Define a custom <xref:System.Xaml.XamlObjectWriter> that overrides `Write*` methods, possibly performing type mapping that bypasses XAML schema context.</span></span>

- <span data-ttu-id="427a7-163">Sestavte <xref:System.Xaml.XamlXmlReader> pro použití nevýchozího kontextu schématu XAML, aby byly vlastní rozdíly v chování XAML použity čtenářem i zapisovačem.</span><span class="sxs-lookup"><span data-stu-id="427a7-163">Construct the <xref:System.Xaml.XamlXmlReader> to use a nondefault XAML schema context, so that customized differences in XAML behavior are used both by the reader and the writer.</span></span>

### <a name="accessing-xaml-beyond-the-node-loop-concept"></a><span data-ttu-id="427a7-164">Přístup k XAML za koncept smyčky uzlu</span><span class="sxs-lookup"><span data-stu-id="427a7-164">Accessing XAML Beyond the Node Loop Concept</span></span>

<span data-ttu-id="427a7-165">Existují potenciálně jiné způsoby, jak pracovat s reprezentacemi XAML jinou než jako smyčka uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-165">There are potentially other ways to work with a XAML representation other than as a XAML node loop.</span></span> <span data-ttu-id="427a7-166">Například je možné, že existuje čtečka XAML, která může číst indexovaný uzel nebo konkrétně přistupuje k uzlům přímo pomocí `x:Name`, pomocí `x:Uid`nebo prostřednictvím jiných identifikátorů.</span><span class="sxs-lookup"><span data-stu-id="427a7-166">For example, there could exist a XAML reader that can read an indexed node, or in particular accesses nodes directly by `x:Name`, by `x:Uid`, or through other identifiers.</span></span> <span data-ttu-id="427a7-167">.NET Framework služby XAML neposkytuje úplnou implementaci, ale poskytuje navrhovaný vzor prostřednictvím služeb a typů podpory.</span><span class="sxs-lookup"><span data-stu-id="427a7-167">.NET Framework XAML Services does not provide a full implementation, but provides a suggested pattern through services and support types.</span></span> <span data-ttu-id="427a7-168">Další informace naleznete v tématu <xref:System.Xaml.IXamlIndexingReader> a <xref:System.Xaml.XamlNodeList>.</span><span class="sxs-lookup"><span data-stu-id="427a7-168">For more information, see <xref:System.Xaml.IXamlIndexingReader> and <xref:System.Xaml.XamlNodeList>.</span></span>

<a name="working_with_the_current_node"></a>

## <a name="working-with-the-current-node"></a><span data-ttu-id="427a7-169">Práce s aktuálním uzlem</span><span class="sxs-lookup"><span data-stu-id="427a7-169">Working with the Current Node</span></span>

<span data-ttu-id="427a7-170">Většina scénářů, které používají smyčku uzlu XAML, nečtou pouze uzly.</span><span class="sxs-lookup"><span data-stu-id="427a7-170">Most scenarios that use a XAML node loop do not only read the nodes.</span></span> <span data-ttu-id="427a7-171">Většina scénářů zpracovává aktuální uzly a každý uzel projde po jednom do implementace <xref:System.Xaml.XamlWriter>.</span><span class="sxs-lookup"><span data-stu-id="427a7-171">Most scenarios process current nodes and pass each node one at a time to an implementation of <xref:System.Xaml.XamlWriter>.</span></span>

<span data-ttu-id="427a7-172">V typickém scénáři cesty načtení <xref:System.Xaml.XamlXmlReader> vytvoří datový proud uzlu XAML; uzly XAML jsou zpracovávány podle vaší logiky a kontextu schématu XAML; a uzly jsou předány do <xref:System.Xaml.XamlObjectWriter>.</span><span class="sxs-lookup"><span data-stu-id="427a7-172">In the typical load path scenario, a <xref:System.Xaml.XamlXmlReader> produces a XAML node stream; the XAML nodes are processed according to your logic and XAML schema context; and the nodes are passed to a <xref:System.Xaml.XamlObjectWriter>.</span></span> <span data-ttu-id="427a7-173">Následně Integrujte výsledný graf objektů do vaší aplikace nebo architektury.</span><span class="sxs-lookup"><span data-stu-id="427a7-173">You then integrate the resulting object graph into your application or framework.</span></span>

<span data-ttu-id="427a7-174">V typickém scénáři uložení <xref:System.Xaml.XamlObjectReader> čte graf objektů, zpracovává jednotlivé uzly XAML a <xref:System.Xaml.XamlXmlWriter> výstup serializovaného výsledku jako textový soubor XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-174">In a typical save path scenario, a <xref:System.Xaml.XamlObjectReader> reads the object graph, individual XAML nodes are processed, and a <xref:System.Xaml.XamlXmlWriter> outputs the serialized result as a XAML text file.</span></span> <span data-ttu-id="427a7-175">Klíč spočívá v tom, že jak cesty i scénáře zahrnují práci s právě jedním uzlem jazyka XAML v čase, a uzly XAML jsou k dispozici pro zpracování standardizovaným způsobem, který je definován pomocí systému typů XAML a rozhraní API služby the.NET Framework XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-175">The key is that both paths and scenarios involve working with exactly one XAML node at a time, and the XAML nodes are available for treatment in a standardized way that is defined by the XAML type system and the.NET Framework XAML Services APIs.</span></span>

### <a name="frames-and-scope"></a><span data-ttu-id="427a7-176">Rámce a rozsah</span><span class="sxs-lookup"><span data-stu-id="427a7-176">Frames and Scope</span></span>

<span data-ttu-id="427a7-177">Smyčka uzlu XAML projde datovým proudem uzlu XAML lineárním způsobem.</span><span class="sxs-lookup"><span data-stu-id="427a7-177">A XAML node loop walks through a XAML node stream in a linear way.</span></span> <span data-ttu-id="427a7-178">Datový proud uzlu se přesměruje do objektů, do členů, které obsahují jiné objekty atd.</span><span class="sxs-lookup"><span data-stu-id="427a7-178">The node stream traverses into objects, into members that contain other objects, and so on.</span></span> <span data-ttu-id="427a7-179">Je často vhodné sledovat obor v rámci datového proudu uzlu XAML implementací konceptu rámce a zásobníku.</span><span class="sxs-lookup"><span data-stu-id="427a7-179">It is often useful to keep track of scope within the XAML node stream by implementing a frame and stack concept.</span></span> <span data-ttu-id="427a7-180">To platí zejména v případě, že aktivně nastavujete datový proud uzlu, který je v něm.</span><span class="sxs-lookup"><span data-stu-id="427a7-180">This is particularly true if you are actively adjusting the node stream while you are in it.</span></span> <span data-ttu-id="427a7-181">Podpora rámce a zásobníku, kterou implementujete v rámci logiky smyčky uzlu, by mohla počítat `StartObject` (nebo `GetObject`) a obory `EndObject`, protože jste propadli do struktury uzlu XAML, pokud se na strukturu domníváme z perspektivy modelu DOM.</span><span class="sxs-lookup"><span data-stu-id="427a7-181">The frame and stack support that you implement as part of your node loop logic could count `StartObject` (or `GetObject`) and `EndObject` scopes as you descend into a XAML node structure if the structure is thought of from a DOM perspective.</span></span>

<a name="traversing_and_entering_object_nodes"></a>

## <a name="traversing-and-entering-object-nodes"></a><span data-ttu-id="427a7-182">Procházení a zadávání uzlů objektů</span><span class="sxs-lookup"><span data-stu-id="427a7-182">Traversing and Entering Object Nodes</span></span>

<span data-ttu-id="427a7-183">První uzel v proudu uzlu, je-li otevřen čtečkou XAML, je uzel start-Object kořenového objektu.</span><span class="sxs-lookup"><span data-stu-id="427a7-183">The first node in a node stream when it is opened by a XAML reader is the start-object node of the root object.</span></span> <span data-ttu-id="427a7-184">Podle definice je tento objekt vždy jediným uzlem objektu a nemá žádné partnerské uzly.</span><span class="sxs-lookup"><span data-stu-id="427a7-184">By definition, this object is always a single object node and has no peers.</span></span> <span data-ttu-id="427a7-185">V jakémkoli reálném příkladu XAML je kořenový objekt definován tak, aby obsahoval jednu nebo více vlastností, které obsahují více objektů, a tyto vlastnosti mají členské uzly.</span><span class="sxs-lookup"><span data-stu-id="427a7-185">In any real-world XAML example, the root object is defined to have one or more properties that hold more objects, and these properties have member nodes.</span></span> <span data-ttu-id="427a7-186">Členské uzly pak mají jeden nebo více uzlů objektů nebo mohou také končit místo toho v uzlu hodnoty.</span><span class="sxs-lookup"><span data-stu-id="427a7-186">The member nodes then have one or more object nodes, or might also terminate in a value node instead.</span></span> <span data-ttu-id="427a7-187">Kořenový objekt obvykle definuje XAML obory názvů WPF, které jsou syntakticky přiřazeny jako atributy v kódu jazyka XAML, ale mapovány na `Namescope` typ uzlu v reprezentaci datového proudu uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-187">The root object typically defines XAML namescopes, which are syntactically assigned as attributes in the XAML text markup but map to a `Namescope` node type in the XAML node stream representation.</span></span>

<span data-ttu-id="427a7-188">Vezměte v úvahu následující příklad XAML (Jedná se o libovolný kód XAML, který není zálohovaný existujícími typy v .NET Framework).</span><span class="sxs-lookup"><span data-stu-id="427a7-188">Consider the following XAML example (this is arbitrary XAML, not backed by existing types in the .NET Framework).</span></span> <span data-ttu-id="427a7-189">Předpokládejme, že v tomto objektovém modelu `FavorCollection` je `List<T>` `Favor`, `Balloon` a `NoiseMaker` lze přiřadit k `Favor`, `Balloon.Color` vlastnost je zajištěna `Color` objekt podobným tomu, jak WPF definuje jako názvy známých barev. a `Color` podporuje konvertor typu pro syntaxi atributů.</span><span class="sxs-lookup"><span data-stu-id="427a7-189">Assume that in this object model, `FavorCollection` is `List<T>` of `Favor`, `Balloon` and `NoiseMaker` are assignable to `Favor`, the `Balloon.Color` property is backed by a `Color` object similar to how WPF defines colors as known color names, and `Color` supports a type converter for attribute syntax.</span></span>

|<span data-ttu-id="427a7-190">Kód XAML</span><span class="sxs-lookup"><span data-stu-id="427a7-190">XAML markup</span></span>|<span data-ttu-id="427a7-191">Výsledný datový proud uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="427a7-191">Resulting XAML node stream</span></span>|
|-----------------|--------------------------------|
|`<Party`|<span data-ttu-id="427a7-192">uzel `Namespace` pro `Party`</span><span class="sxs-lookup"><span data-stu-id="427a7-192">`Namespace` node for `Party`</span></span>|
|`xmlns="PartyXamlNamespace">`|<span data-ttu-id="427a7-193">uzel `StartObject` pro `Party`</span><span class="sxs-lookup"><span data-stu-id="427a7-193">`StartObject` node for `Party`</span></span>|
|`<Party.Favors>`|<span data-ttu-id="427a7-194">uzel `StartMember` pro `Party.Favors`</span><span class="sxs-lookup"><span data-stu-id="427a7-194">`StartMember` node for `Party.Favors`</span></span>|
||<span data-ttu-id="427a7-195">uzel `StartObject` pro implicitní `FavorCollection`</span><span class="sxs-lookup"><span data-stu-id="427a7-195">`StartObject` node for implicit `FavorCollection`</span></span>|
||<span data-ttu-id="427a7-196">uzel `StartMember` pro vlastnost implicitních `FavorCollection` položek.</span><span class="sxs-lookup"><span data-stu-id="427a7-196">`StartMember` node for implicit `FavorCollection` items property.</span></span>|
|`<Balloon`|<span data-ttu-id="427a7-197">uzel `StartObject` pro `Balloon`</span><span class="sxs-lookup"><span data-stu-id="427a7-197">`StartObject` node for `Balloon`</span></span>|
|`Color="Red"`|<span data-ttu-id="427a7-198">uzel `StartMember` pro `Color`</span><span class="sxs-lookup"><span data-stu-id="427a7-198">`StartMember` node for `Color`</span></span><br /><br /> <span data-ttu-id="427a7-199">uzel `Value` pro řetězec hodnoty atributu `"Red"`</span><span class="sxs-lookup"><span data-stu-id="427a7-199">`Value` node for the attribute value string `"Red"`</span></span><br /><br /> <span data-ttu-id="427a7-200">`EndMember` pro `Color`</span><span class="sxs-lookup"><span data-stu-id="427a7-200">`EndMember` for `Color`</span></span>|
|`HasHelium="True"`|<span data-ttu-id="427a7-201">uzel `StartMember` pro `HasHelium`</span><span class="sxs-lookup"><span data-stu-id="427a7-201">`StartMember` node for `HasHelium`</span></span><br /><br /> <span data-ttu-id="427a7-202">uzel `Value` pro řetězec hodnoty atributu `"True"`</span><span class="sxs-lookup"><span data-stu-id="427a7-202">`Value` node for the attribute value string `"True"`</span></span><br /><br /> <span data-ttu-id="427a7-203">`EndMember` pro `HasHelium`</span><span class="sxs-lookup"><span data-stu-id="427a7-203">`EndMember` for `HasHelium`</span></span>|
|`>`|<span data-ttu-id="427a7-204">`EndObject` pro `Balloon`</span><span class="sxs-lookup"><span data-stu-id="427a7-204">`EndObject` for `Balloon`</span></span>|
|`<NoiseMaker>Loudest</NoiseMaker>`|<span data-ttu-id="427a7-205">uzel `StartObject` pro `NoiseMaker`</span><span class="sxs-lookup"><span data-stu-id="427a7-205">`StartObject` node for `NoiseMaker`</span></span><br /><br /> <span data-ttu-id="427a7-206">uzel `StartMember` pro `_Initialization`</span><span class="sxs-lookup"><span data-stu-id="427a7-206">`StartMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="427a7-207">uzel `Value` pro řetězec hodnoty inicializace `"Loudest"`</span><span class="sxs-lookup"><span data-stu-id="427a7-207">`Value` node for the initialization value string `"Loudest"`</span></span><br /><br /> <span data-ttu-id="427a7-208">uzel `EndMember` pro `_Initialization`</span><span class="sxs-lookup"><span data-stu-id="427a7-208">`EndMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="427a7-209">`EndObject` pro `NoiseMaker`</span><span class="sxs-lookup"><span data-stu-id="427a7-209">`EndObject` for `NoiseMaker`</span></span>|
||<span data-ttu-id="427a7-210">uzel `EndMember` pro vlastnost implicitních `FavorCollection` položek.</span><span class="sxs-lookup"><span data-stu-id="427a7-210">`EndMember` node for implicit `FavorCollection` items property.</span></span>|
||<span data-ttu-id="427a7-211">uzel `EndObject` pro implicitní `FavorCollection`</span><span class="sxs-lookup"><span data-stu-id="427a7-211">`EndObject` node for implicit `FavorCollection`</span></span>|
|`</Party.Favors>`|<span data-ttu-id="427a7-212">`EndMember` pro `Favors`</span><span class="sxs-lookup"><span data-stu-id="427a7-212">`EndMember` for `Favors`</span></span>|
|`</Party>`|<span data-ttu-id="427a7-213">`EndObject` pro `Party`</span><span class="sxs-lookup"><span data-stu-id="427a7-213">`EndObject` for `Party`</span></span>|

<span data-ttu-id="427a7-214">V datovém proudu uzlu XAML můžete spoléhat na následující chování:</span><span class="sxs-lookup"><span data-stu-id="427a7-214">In the XAML node stream, you can rely on the following behavior:</span></span>

- <span data-ttu-id="427a7-215">Pokud uzel `Namespace` existuje, je přidán do datového proudu bezprostředně před `StartObject` deklarovaným oborem názvů XAML s `xmlns`.</span><span class="sxs-lookup"><span data-stu-id="427a7-215">If a `Namespace` node exists, it is added to the stream immediately before the `StartObject` that declared the XAML namespace with `xmlns`.</span></span> <span data-ttu-id="427a7-216">Prohlédněte si předchozí tabulku pomocí XAML a Ukázkový datový proud uzlu.</span><span class="sxs-lookup"><span data-stu-id="427a7-216">Look at the previous table with the XAML and example node stream again.</span></span> <span data-ttu-id="427a7-217">Všimněte si, jak se `StartObject` a `Namespace` uzly zdají být přemístěné a jejich deklarace do značek textu.</span><span class="sxs-lookup"><span data-stu-id="427a7-217">Notice how the `StartObject` and `Namespace` nodes seem to be transposed versus their declaration positions in text markup.</span></span> <span data-ttu-id="427a7-218">To je zástupce chování, kde se uzly oboru názvů vždy zobrazují před uzlem, na které se vztahují v proudu uzlů.</span><span class="sxs-lookup"><span data-stu-id="427a7-218">This is representative of the behavior where the namespace nodes always appear ahead of the node they apply to in the node stream.</span></span> <span data-ttu-id="427a7-219">Účelem tohoto návrhu je, že informace oboru názvů jsou zásadní pro zapisovače objektů a musí být známé před tím, než se zapisovač objektů pokusí provést mapování typů nebo jinak zpracovat objekt.</span><span class="sxs-lookup"><span data-stu-id="427a7-219">The purpose of this design is that the namespace information is vital to object writers and must be known before the object writer attempts to perform type mapping or otherwise process the object.</span></span> <span data-ttu-id="427a7-220">Umístění informací o oboru názvů XAML před jeho rozsahem aplikace v datovém proudu usnadňuje vždy zpracování datového proudu uzlu v uvedeném pořadí.</span><span class="sxs-lookup"><span data-stu-id="427a7-220">Placing the XAML namespace information ahead of its application scope in the stream makes it simpler to always process the node stream in its presented order.</span></span>

- <span data-ttu-id="427a7-221">Vzhledem k výše uvedené úvahě je jedním nebo více `Namespace` uzly, které si přečtete poprvé ve většině případů reálného světa při procházení uzlů od spuštění, nikoli `StartObject` kořenového adresáře.</span><span class="sxs-lookup"><span data-stu-id="427a7-221">Because of the above consideration, it is one or more `Namespace` nodes that you read first in most real-world markup cases when traversing nodes from the start, not the `StartObject` of the root.</span></span>

- <span data-ttu-id="427a7-222">`StartObject` uzel může následovat `StartMember`, `Value`nebo okamžitý `EndObject`.</span><span class="sxs-lookup"><span data-stu-id="427a7-222">A `StartObject` node can be followed by `StartMember`, `Value`, or an immediate `EndObject`.</span></span> <span data-ttu-id="427a7-223">Nikdy nenásleduje další `StartObject`.</span><span class="sxs-lookup"><span data-stu-id="427a7-223">It is never followed immediately by another `StartObject`.</span></span>

- <span data-ttu-id="427a7-224">`StartMember` může následovat `StartObject`, `Value`nebo okamžité `EndMember`.</span><span class="sxs-lookup"><span data-stu-id="427a7-224">A `StartMember` can be followed by a `StartObject`, `Value`, or an immediate `EndMember`.</span></span> <span data-ttu-id="427a7-225">U členů, u kterých má být hodnota z existující hodnoty nadřazeného objektu místo `StartObject`, která by vytvořila novou hodnotu, může následovat `GetObject`.</span><span class="sxs-lookup"><span data-stu-id="427a7-225">It can be followed by `GetObject`, for members where the value is supposed to come from an existing value of the parent object rather than a `StartObject` that would instantiate a new value.</span></span> <span data-ttu-id="427a7-226">Může být také následován `Namespace`m uzlem, který se vztahuje na nadcházející `StartObject`.</span><span class="sxs-lookup"><span data-stu-id="427a7-226">It can also be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span> <span data-ttu-id="427a7-227">Nikdy nenásleduje další `StartMember`.</span><span class="sxs-lookup"><span data-stu-id="427a7-227">It is never followed immediately by another `StartMember`.</span></span>

- <span data-ttu-id="427a7-228">Uzel `Value` představuje samotnou hodnotu; není k dispozici žádný "hodnota EndValue".</span><span class="sxs-lookup"><span data-stu-id="427a7-228">A `Value` node represents the value itself; there is no "EndValue".</span></span> <span data-ttu-id="427a7-229">Může následovat jenom `EndMember`.</span><span class="sxs-lookup"><span data-stu-id="427a7-229">It can be followed only by an `EndMember`.</span></span>

  - <span data-ttu-id="427a7-230">Inicializační text XAML objektu, který může být použit konstrukcí, nemá za následek strukturu objektů a hodnot.</span><span class="sxs-lookup"><span data-stu-id="427a7-230">XAML initialization text of the object as might be used by construction does not result in an Object-Value structure.</span></span> <span data-ttu-id="427a7-231">Místo toho je vytvořen vyhrazený členský uzel pro člena s názvem `_Initialization`.</span><span class="sxs-lookup"><span data-stu-id="427a7-231">Instead, a dedicated member node for a member named `_Initialization` is created.</span></span> <span data-ttu-id="427a7-232">a tento členský uzel obsahuje řetězec hodnoty inicializace.</span><span class="sxs-lookup"><span data-stu-id="427a7-232">and that member node contains the initialization value string.</span></span> <span data-ttu-id="427a7-233">Pokud existuje, `_Initialization` je vždy první `StartMember`.</span><span class="sxs-lookup"><span data-stu-id="427a7-233">If it exists, `_Initialization` is always the first `StartMember`.</span></span> <span data-ttu-id="427a7-234">`_Initialization` může být kvalifikována v některých reprezentacích služby XAML pomocí jazyka XAML XAML namescope, aby se vyjasnilo, že `_Initialization` není definovaná vlastnost v části typy zálohování.</span><span class="sxs-lookup"><span data-stu-id="427a7-234">`_Initialization` may be qualified in some XAML services representations with the XAML language XAML namescope, to clarify that `_Initialization` is not a defined property in backing types.</span></span>

  - <span data-ttu-id="427a7-235">Kombinace člen-hodnota představuje nastavení atributu hodnoty.</span><span class="sxs-lookup"><span data-stu-id="427a7-235">A Member-Value combination represents an attribute setting of the value.</span></span> <span data-ttu-id="427a7-236">Může se stát, že se do zpracování této hodnoty přinese konvertor hodnot a hodnota je prostý řetězec.</span><span class="sxs-lookup"><span data-stu-id="427a7-236">There might eventually be a value converter involved in processing this value, and the value is a plain string.</span></span> <span data-ttu-id="427a7-237">Nicméně to není vyhodnoceno, dokud modul pro zápis objektů XAML nezpracovává tento datový proud uzlu.</span><span class="sxs-lookup"><span data-stu-id="427a7-237">However, that is not evaluated until a XAML object writer processes this node stream.</span></span> <span data-ttu-id="427a7-238">Modul pro zápis objektů XAML má nezbytný kontext schématu XAML, typ mapování systému a další podporu potřebnou pro převody hodnot.</span><span class="sxs-lookup"><span data-stu-id="427a7-238">The XAML object writer possesses the necessary XAML schema context, type system mapping, and other support needed for value conversions.</span></span>

- <span data-ttu-id="427a7-239">Za `EndMember` uzel může následovat `StartMember` uzel pro následného člena nebo uzel `EndObject` pro vlastníka člena.</span><span class="sxs-lookup"><span data-stu-id="427a7-239">An `EndMember` node can be followed by a `StartMember` node for a subsequent member, or by an `EndObject` node for the member owner.</span></span>

- <span data-ttu-id="427a7-240">Za uzlem `EndObject` může následovat `EndMember` uzel.</span><span class="sxs-lookup"><span data-stu-id="427a7-240">An `EndObject` node can be followed by an `EndMember` node.</span></span> <span data-ttu-id="427a7-241">Může být také následován `StartObject`m uzlem pro případy, kdy objekty jsou partnerské uzly v položkách kolekce.</span><span class="sxs-lookup"><span data-stu-id="427a7-241">It can also be followed by a `StartObject` node for cases where the objects are peers in a collection's items.</span></span> <span data-ttu-id="427a7-242">Případně může následovat `Namespace` uzel, který se vztahuje na nadcházející `StartObject`.</span><span class="sxs-lookup"><span data-stu-id="427a7-242">Or it can be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span>

  - <span data-ttu-id="427a7-243">V případě jedinečného případu zavření celého datového proudu uzlu nenásleduje u `EndObject` kořenu cokoli; čtenář je nyní konec souboru a <xref:System.Xaml.XamlReader.Read%2A> vrátí `false`.</span><span class="sxs-lookup"><span data-stu-id="427a7-243">For the unique case of closing the entire node stream, the `EndObject` of the root is not followed by anything; the reader is now end-of-file, and <xref:System.Xaml.XamlReader.Read%2A> returns `false`.</span></span>

<a name="value_converters_and_the_xaml_node_stream"></a>

## <a name="value-converters-and-the-xaml-node-stream"></a><span data-ttu-id="427a7-244">Převaděče hodnot a datový proud uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="427a7-244">Value Converters and the XAML Node Stream</span></span>

<span data-ttu-id="427a7-245">Konvertor hodnot je obecný termín pro rozšíření značek, konvertor typu (včetně serializátorů hodnot) nebo jiné vyhrazené třídy, která je hlášena jako konvertor hodnot pomocí systému typů XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-245">A value converter is a general term for a markup extension, a type converter (including value serializers) or another dedicated class that is reported as a value converter through the XAML type system.</span></span> <span data-ttu-id="427a7-246">V datovém proudu uzlu XAML mají použití konvertoru typu a použití rozšíření značek velmi odlišné reprezentace.</span><span class="sxs-lookup"><span data-stu-id="427a7-246">In the XAML node stream, a type converter usage and a markup extension usage have very different representations.</span></span>

### <a name="type-converters-in-the-xaml-node-stream"></a><span data-ttu-id="427a7-247">Převaděče typů v datovém proudu uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="427a7-247">Type Converters in the XAML Node Stream</span></span>

<span data-ttu-id="427a7-248">Sada atributů, která nakonec má za následek, že použití konvertoru typu je hlášeno v datovém proudu uzlu XAML jako hodnota člena.</span><span class="sxs-lookup"><span data-stu-id="427a7-248">An attribute set that eventually results in a type converter usage is reported in the XAML node stream as a value of a member.</span></span> <span data-ttu-id="427a7-249">Datový proud uzlu XAML se nepokusí vytvořit objekt instance konvertoru typu a předat mu hodnotu.</span><span class="sxs-lookup"><span data-stu-id="427a7-249">The XAML node stream does not attempt to produce a type converter instance object and pass the value to it.</span></span> <span data-ttu-id="427a7-250">Použití implementace převodu konvertoru typu vyžaduje vyvolání kontextu schématu XAML a jeho použití pro mapování typů.</span><span class="sxs-lookup"><span data-stu-id="427a7-250">Using a type converter's conversion implementation requires invoking the XAML schema context and using it for type-mapping.</span></span> <span data-ttu-id="427a7-251">Dokonce i určení, která třída konvertoru typů by měla být použita ke zpracování hodnoty, vyžaduje nepřímý kontext schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-251">Even determining which type converter class should be used to process the value requires the XAML schema context indirectly.</span></span> <span data-ttu-id="427a7-252">Použijete-li výchozí kontext schématu XAML, jsou tyto informace k dispozici v systému typů XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-252">When you use the default XAML schema context, that information is available from the XAML type system.</span></span> <span data-ttu-id="427a7-253">Pokud potřebujete informace třídy konvertoru typu na úrovni datového proudu uzlu XAML před připojením k zapisovači XAML, můžete jej získat z <xref:System.Xaml.XamlMember> informací o nastavování člena.</span><span class="sxs-lookup"><span data-stu-id="427a7-253">If you need the type converter class information at the XAML node stream level before connection to a XAML writer, you can obtain it from the <xref:System.Xaml.XamlMember> information of the member being set.</span></span> <span data-ttu-id="427a7-254">V opačném případě by vstup konvertoru typu měl být zachován v datovém proudu uzlu XAML jako jednoduchá hodnota, dokud nebudou provedeny zbývající operace, které vyžadují systém mapování typů a kontext schématu XAML, například vytvoření objektu modulem pro zápis objektů XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-254">But otherwise, type converter input should be preserved in the XAML node stream as a plain value until the remainder of operations that require the type mapping system and XAML schema context are performed, for example the object creation by a XAML object writer.</span></span>

<span data-ttu-id="427a7-255">Zvažte například následující osnova definice třídy a použití jazyka XAML:</span><span class="sxs-lookup"><span data-stu-id="427a7-255">For example, consider the following class definition outline and XAML usage for it:</span></span>

```csharp
public class BoardSizeConverter : TypeConverter {
  //converts from string to an int[2] by splitting on an "x" char
}
public class GameBoard {
  [TypeConverter(typeof(BoardSizeConverter))]
  public int[] BoardSize; //2x2 array, initialization not shown
}
```

```xaml
<GameBoard BoardSize="8x8"/>
```

<span data-ttu-id="427a7-256">Textová reprezentace datového proudu uzlu XAML pro toto použití může být vyjádřena jako následující:</span><span class="sxs-lookup"><span data-stu-id="427a7-256">A text representation of the XAML node stream for this usage could be expressed as the following:</span></span>

<span data-ttu-id="427a7-257">`StartObject` s <xref:System.Xaml.XamlType> reprezentujícím `GameBoard`</span><span class="sxs-lookup"><span data-stu-id="427a7-257">`StartObject` with <xref:System.Xaml.XamlType> representing `GameBoard`</span></span>

<span data-ttu-id="427a7-258">`StartMember` s <xref:System.Xaml.XamlMember> reprezentujícím `BoardSize`</span><span class="sxs-lookup"><span data-stu-id="427a7-258">`StartMember` with <xref:System.Xaml.XamlMember> representing `BoardSize`</span></span>

<span data-ttu-id="427a7-259">`Value` uzel s textovým řetězcem "`8x8`"</span><span class="sxs-lookup"><span data-stu-id="427a7-259">`Value` node, with text string "`8x8`"</span></span>

<span data-ttu-id="427a7-260">`EndMember` odpovídá `BoardSize`</span><span class="sxs-lookup"><span data-stu-id="427a7-260">`EndMember` matches `BoardSize`</span></span>

<span data-ttu-id="427a7-261">`EndObject` odpovídá `GameBoard`</span><span class="sxs-lookup"><span data-stu-id="427a7-261">`EndObject` matches `GameBoard`</span></span>

<span data-ttu-id="427a7-262">Všimněte si, že v tomto datovém proudu uzlu není žádná instance převaděče typu.</span><span class="sxs-lookup"><span data-stu-id="427a7-262">Notice that there is no type converter instance in this node stream.</span></span> <span data-ttu-id="427a7-263">Můžete ale získat informace o převaděči typů voláním <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> na <xref:System.Xaml.XamlMember> pro `BoardSize`.</span><span class="sxs-lookup"><span data-stu-id="427a7-263">But you can get type converter information by calling <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> on the <xref:System.Xaml.XamlMember> for `BoardSize`.</span></span> <span data-ttu-id="427a7-264">Pokud máte platný kontext schématu XAML, můžete také vyvolat metody převaděče získáním instance z <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>.</span><span class="sxs-lookup"><span data-stu-id="427a7-264">If you have a valid XAML schema context, you can also invoke the converter methods by obtaining an instance from <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>.</span></span>

### <a name="markup-extensions-in-the-xaml-node-stream"></a><span data-ttu-id="427a7-265">Rozšíření značek v datovém proudu uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="427a7-265">Markup Extensions in the XAML Node Stream</span></span>

<span data-ttu-id="427a7-266">Použití rozšíření značek je hlášeno v proudu uzlu XAML jako uzel objektu v rámci člena, kde objekt představuje instanci rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="427a7-266">A markup extension usage is reported in the XAML node stream as an object node within a member, where the object represents a markup extension instance.</span></span> <span data-ttu-id="427a7-267">Proto je použití rozšíření značek v rámci reprezentace streamu uzlu v případě, že je to více, uvedeno v reprezentaci, než je použití konvertoru typu, a další informace.</span><span class="sxs-lookup"><span data-stu-id="427a7-267">Thus a markup extension usage is presented more explicitly in the node stream representation than a type converter usage is, and carries more information.</span></span> <span data-ttu-id="427a7-268">informace <xref:System.Xaml.XamlMember> nemohly získat informaci o rozšíření značek, protože použití je situace a se liší v každém možném případu s kódem. není vyhrazen a implicitně na typ nebo člen, jako je to případ s konvertory typů.</span><span class="sxs-lookup"><span data-stu-id="427a7-268"><xref:System.Xaml.XamlMember> information could not have told you anything about the markup extension, because the usage is situational and varies in each possible markup case; it is not dedicated and implicit per type or member as is the case with type converters.</span></span>

<span data-ttu-id="427a7-269">Datový proud uzlu reprezentace rozšíření značek jako uzly objektů je případ i v případě, že využití rozšíření značek bylo provedeno ve formě atributu v kódu textu XAML (což je často případ).</span><span class="sxs-lookup"><span data-stu-id="427a7-269">The node stream representation of markup extensions as object nodes is the case even if the markup extension usage was made in attribute form in the XAML text markup (which is often the case).</span></span> <span data-ttu-id="427a7-270">Použití rozšíření značek, které používaly explicitní formulář elementu Object, je ošetřeno stejným způsobem.</span><span class="sxs-lookup"><span data-stu-id="427a7-270">Markup extension usages that used an explicit object element form are treated the same way.</span></span>

<span data-ttu-id="427a7-271">V rámci uzlu objektu rozšíření značek mohou být členy této přípony označení.</span><span class="sxs-lookup"><span data-stu-id="427a7-271">Within a markup extension object node, there may be members of that markup extension.</span></span> <span data-ttu-id="427a7-272">Reprezentace datového proudu uzlu XAML zachovává použití této přípony označení, ať už se jedná o použití pozičního parametru, nebo použití s explicitními pojmenovanými parametry.</span><span class="sxs-lookup"><span data-stu-id="427a7-272">The XAML node stream representation preserves the usage of that markup extension, whether that be a positional parameter usage or a usage with explicit named parameters.</span></span>

<span data-ttu-id="427a7-273">Pro použití pozičního parametru obsahuje datový proud uzlu XAML `_PositionalParameters`ovou vlastnost definovanou v jazyce XAML, která zaznamenává využití.</span><span class="sxs-lookup"><span data-stu-id="427a7-273">For a positional parameter usage, the XAML node stream contains a XAML language-defined property `_PositionalParameters` that records the usage.</span></span> <span data-ttu-id="427a7-274">Tato vlastnost je obecná <xref:System.Collections.Generic.List%601> s omezením <xref:System.Object>.</span><span class="sxs-lookup"><span data-stu-id="427a7-274">This property is a generic <xref:System.Collections.Generic.List%601> with <xref:System.Object> constraint.</span></span> <span data-ttu-id="427a7-275">Omezení je Object a ne String, protože použití pozičního parametru může obsahovat vnořené použití rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="427a7-275">The constraint is object and not string because conceivably a positional parameter usage could contain nested markup extension usages within it.</span></span> <span data-ttu-id="427a7-276">Chcete-li získat přístup k pozičním parametrům z použití, můžete iterovat v seznamu a použít indexery pro jednotlivé hodnoty seznamu.</span><span class="sxs-lookup"><span data-stu-id="427a7-276">To access the positional parameters from the usage, you could iterate through the list and use the indexers for individual list values.</span></span>

<span data-ttu-id="427a7-277">Pro použití pojmenovaných parametrů je každý pojmenovaný parametr reprezentován jako členský uzel daného názvu v datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="427a7-277">For a named parameter usage, each named parameter is represented as a member node of that name in the node stream.</span></span> <span data-ttu-id="427a7-278">Hodnoty členů nejsou nutně řetězcem, protože by mohlo dojít k použití vloženého rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="427a7-278">The member values are not necessarily strings, because there could be a nested markup extension usage.</span></span>

<span data-ttu-id="427a7-279">`ProvideValue` z rozšíření značek ještě není vyvoláno.</span><span class="sxs-lookup"><span data-stu-id="427a7-279">`ProvideValue` from the markup extension is not yet invoked.</span></span> <span data-ttu-id="427a7-280">Je však vyvolána, pokud připojíte čtečku XAML a zapisovač XAML, aby se `WriteEndObject` vyvolala v uzlu rozšíření značek při jejich kontrole v proudu uzlů.</span><span class="sxs-lookup"><span data-stu-id="427a7-280">However, it is invoked if you connect a XAML reader and XAML writer so that `WriteEndObject` is invoked on the markup extension node when you examine it in the node stream.</span></span> <span data-ttu-id="427a7-281">Z tohoto důvodu je obecně potřeba, aby byl k dispozici stejný kontext schématu XAML, jako by byl použit k vytvoření grafu objektu v cestě načtení.</span><span class="sxs-lookup"><span data-stu-id="427a7-281">For this reason, you generally need the same XAML schema context available as would be used in order to form the object graph on the load path.</span></span> <span data-ttu-id="427a7-282">Jinak `ProvideValue` ze všech rozšíření značek může vyvolat výjimky, protože nemá očekávané služby, které jsou k dispozici.</span><span class="sxs-lookup"><span data-stu-id="427a7-282">Otherwise, `ProvideValue` from any markup extension can throw exceptions here, because it does not have expected services available.</span></span>

<a name="xaml_and_xml_languagedefined_members_in_the_xaml_node_stream"></a>

## <a name="xaml-and-xml-language-defined-members-in-the-xaml-node-stream"></a><span data-ttu-id="427a7-283">Uživatelsky definované členy jazyka XAML a XML v datovém proudu uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="427a7-283">XAML and XML Language-Defined Members in the XAML Node Stream</span></span>

<span data-ttu-id="427a7-284">Někteří členové jsou představeni do datového proudu uzlu XAML z důvodu interpretace a konvence čtečky XAML namísto explicitního vyhledávání <xref:System.Xaml.XamlMember> nebo konstrukce.</span><span class="sxs-lookup"><span data-stu-id="427a7-284">Certain members are introduced to a XAML node stream because of interpretations and conventions of a XAML reader, instead of through an explicit <xref:System.Xaml.XamlMember> lookup or construction.</span></span> <span data-ttu-id="427a7-285">Tyto členy jsou často direktivy XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-285">Often, these members are XAML directives.</span></span> <span data-ttu-id="427a7-286">V některých případech je to pro čtení jazyka XAML, který zavádí direktivu do datového proudu uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-286">In some cases, it is the act of reading the XAML that introduces the directive into the XAML node stream.</span></span> <span data-ttu-id="427a7-287">Jinými slovy, původní vstupní text XAML nespecifikuje explicitně členskou direktivu, ale čtečka XAML vloží direktivu, aby splňovala strukturální konvence XAML a informace sestavy v proudu uzlu XAML před tím, než dojde ke ztrátě těchto informací.</span><span class="sxs-lookup"><span data-stu-id="427a7-287">In other words, the original input XAML text did not explicitly specify the member directive, but the XAML reader inserts the directive in order to satisfy a structural XAML convention and report information in the XAML node stream before that information is lost.</span></span>

<span data-ttu-id="427a7-288">Následující seznam obsahuje poznámky ke všem případům, kde se očekává, že čtečka XAML zavádí členský uzel jazyka XAML a jak je tento členský uzel identifikovaný v .NET Framework implementaci služby XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-288">The following list notes all cases where a XAML reader is expected to introduce a directive XAML member node, and how that member node is identified in the .NET Framework XAML Services implementations.</span></span>

- <span data-ttu-id="427a7-289">**Inicializační text pro uzel objektu:** Název tohoto členského uzlu je `_Initialization`, představuje direktivu XAML a je definován v oboru názvů XAML jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-289">**Initialization text for an object node:** The name of this member node is `_Initialization`, it represents a XAML directive, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="427a7-290">Z <xref:System.Xaml.XamlLanguage.Initialization%2A>můžete získat statickou entitu.</span><span class="sxs-lookup"><span data-stu-id="427a7-290">You can get a static entity for it from <xref:System.Xaml.XamlLanguage.Initialization%2A>.</span></span>

- <span data-ttu-id="427a7-291">**Poziční parametry pro rozšíření značek:** Název tohoto členského uzlu je `_PositionalParameters`a je definován v oboru názvů XAML jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-291">**Positional parameters for a markup extension:** The name of this member node is `_PositionalParameters`, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="427a7-292">Vždy obsahuje obecný seznam objektů, z nichž každý je poziční parametr předem oddělený rozdělením znaku oddělovače `,`, jak je uvedeno ve vstupním kódu XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-292">It always contains a generic list of objects, each of which is a positional parameter pre-separated by splitting on the `,` delimiter character as supplied in the input XAML.</span></span> <span data-ttu-id="427a7-293">Můžete získat statickou entitu pro direktivu pozičních parametrů z <xref:System.Xaml.XamlLanguage.PositionalParameters%2A>.</span><span class="sxs-lookup"><span data-stu-id="427a7-293">You can get a static entity for the positional parameters directive from <xref:System.Xaml.XamlLanguage.PositionalParameters%2A>.</span></span>

- <span data-ttu-id="427a7-294">**Neznámý obsah:** Název tohoto členského uzlu je `_UnknownContent`.</span><span class="sxs-lookup"><span data-stu-id="427a7-294">**Unknown content:** The name of this member node is `_UnknownContent`.</span></span> <span data-ttu-id="427a7-295">Výhradně řečeno, je <xref:System.Xaml.XamlDirective>a je definována v oboru názvů XAML jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-295">Strictly speaking, it is a <xref:System.Xaml.XamlDirective>, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="427a7-296">Tato direktiva se používá jako Sentinel pro případy, kdy prvek objektu XAML obsahuje obsah ve zdrojovém kódu XAML, ale nelze určit vlastnost Content v aktuálně dostupném kontextu schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-296">This directive is used as a sentinel for cases where a XAML object element contains content in the source XAML but no content property can be determined under the currently available XAML schema context.</span></span> <span data-ttu-id="427a7-297">Tento případ můžete zjistit v datovém proudu uzlu XAML kontrolou členů s názvem `_UnknownContent`.</span><span class="sxs-lookup"><span data-stu-id="427a7-297">You can detect this case in a XAML node stream by checking for members named `_UnknownContent`.</span></span> <span data-ttu-id="427a7-298">Pokud není provedena žádná jiná akce v datovém proudu uzlu XAML, výchozí <xref:System.Xaml.XamlObjectWriter> vyvolá pokus o `WriteEndObject` při výskytu `_UnknownContent` člena na jakémkoli objektu.</span><span class="sxs-lookup"><span data-stu-id="427a7-298">If no other action is taken in a load path XAML node stream, the default <xref:System.Xaml.XamlObjectWriter> throws on attempted `WriteEndObject` when it encounters the `_UnknownContent` member on any object.</span></span> <span data-ttu-id="427a7-299">Výchozí <xref:System.Xaml.XamlXmlWriter> nevyvolává a považuje člena za implicitní.</span><span class="sxs-lookup"><span data-stu-id="427a7-299">The default <xref:System.Xaml.XamlXmlWriter> does not throw, and treats the member as implicit.</span></span> <span data-ttu-id="427a7-300">Můžete získat statickou entitu pro `_UnknownContent` z <xref:System.Xaml.XamlLanguage.UnknownContent%2A>.</span><span class="sxs-lookup"><span data-stu-id="427a7-300">You can get a static entity for `_UnknownContent` from <xref:System.Xaml.XamlLanguage.UnknownContent%2A>.</span></span>

- <span data-ttu-id="427a7-301">**Vlastnost kolekce kolekce:** I když je typ CLR pro třídu kolekce, který se používá pro XAML, obvykle vyhrazená pojmenovaná vlastnost, která obsahuje položky kolekce, tato vlastnost není před rozlišením typu back-Type známa v systému typů XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-301">**Collection property of a collection:** Although the backing CLR type of a collection class that is used for XAML usually has a dedicated named property that holds the collection items, that property is not known to a XAML type system prior to backing type resolution.</span></span> <span data-ttu-id="427a7-302">Místo toho datový proud uzlu XAML zavádí zástupný symbol `Items` jako člen typu XAML kolekce.</span><span class="sxs-lookup"><span data-stu-id="427a7-302">Instead, the XAML node stream introduces an `Items` placeholder as a member of the collection XAML type.</span></span> <span data-ttu-id="427a7-303">V .NET Framework implementaci XAML Services je název této direktivy/členu v datovém proudu uzlů `_Items`.</span><span class="sxs-lookup"><span data-stu-id="427a7-303">In the .NET Framework XAML Services implementation the name of this directive / member in the node stream is `_Items`.</span></span> <span data-ttu-id="427a7-304">Konstanta této direktivy se dá získat z <xref:System.Xaml.XamlLanguage.Items%2A>.</span><span class="sxs-lookup"><span data-stu-id="427a7-304">A constant for this directive can be obtained from <xref:System.Xaml.XamlLanguage.Items%2A>.</span></span>

    <span data-ttu-id="427a7-305">Všimněte si, že datový proud uzlu XAML může obsahovat vlastnost Items s položkami, které nemusejí být analyzovatelné na základě rozlišení typu zálohování a kontextu schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-305">Note that a XAML node stream might contain an Items property with items that turn out to not be parsable based on the backing type resolution and XAML schema context.</span></span> <span data-ttu-id="427a7-306">Například</span><span class="sxs-lookup"><span data-stu-id="427a7-306">For example,</span></span>

- <span data-ttu-id="427a7-307">**Členové definovaní XML:** `xml:base`definované XML `xml:lang` a `xml:space` jsou hlášeny jako direktivy jazyka XAML s názvem `base`, `lang`a `space` v implementacích .NET Framework XAML Services.</span><span class="sxs-lookup"><span data-stu-id="427a7-307">**XML-defined members:** The XML-defined `xml:base`, `xml:lang` and `xml:space` members are reported as XAML directives named `base`, `lang`, and `space` in the .NET Framework XAML Services implementations.</span></span> <span data-ttu-id="427a7-308">Obor názvů pro tyto obory názvů XML `http://www.w3.org/XML/1998/namespace`.</span><span class="sxs-lookup"><span data-stu-id="427a7-308">The namespace for these is the XML namespace `http://www.w3.org/XML/1998/namespace`.</span></span> <span data-ttu-id="427a7-309">Konstanty pro každý z nich lze získat z <xref:System.Xaml.XamlLanguage>.</span><span class="sxs-lookup"><span data-stu-id="427a7-309">Constants for each of these can be obtained from <xref:System.Xaml.XamlLanguage>.</span></span>

## <a name="node-order"></a><span data-ttu-id="427a7-310">Pořadí uzlů</span><span class="sxs-lookup"><span data-stu-id="427a7-310">Node Order</span></span>

<span data-ttu-id="427a7-311">V některých případech <xref:System.Xaml.XamlXmlReader> mění pořadí uzlů XAML v datovém proudu uzlu XAML, oproti pořadí, ve kterém se uzly zobrazí, pokud jsou zobrazeny ve značce nebo při zpracování jako XML.</span><span class="sxs-lookup"><span data-stu-id="427a7-311">In some cases, <xref:System.Xaml.XamlXmlReader> changes the order of XAML nodes in the XAML node stream, versus the order the nodes appear if viewed in the markup or if processed as XML.</span></span> <span data-ttu-id="427a7-312">K tomu je potřeba seřadit uzly tak, aby <xref:System.Xaml.XamlObjectWriter> mohl zpracovat datový proud uzlu pouze s dopředné.</span><span class="sxs-lookup"><span data-stu-id="427a7-312">This is done in order to order the nodes such that a <xref:System.Xaml.XamlObjectWriter> can process the node stream in a forward-only manner.</span></span>  <span data-ttu-id="427a7-313">V .NET Framework služby XAML čtečka XAML změní pořadí uzlů místo toho, aby opouští tuto úlohu zapisovači XAML, jako optimalizaci výkonu pro uživatele modulu streamování objektů XAML.</span><span class="sxs-lookup"><span data-stu-id="427a7-313">In .NET Framework XAML Services, the XAML reader reorders nodes rather than leaving this task to the XAML writer, as a performance optimization for XAML object writer consumers of the node stream.</span></span>

<span data-ttu-id="427a7-314">Některé direktivy jsou určeny konkrétně k poskytnutí dalších informací pro vytvoření objektu z prvku objektu.</span><span class="sxs-lookup"><span data-stu-id="427a7-314">Certain directives are intended specifically to provide more information for the creation of an object from an object element.</span></span> <span data-ttu-id="427a7-315">Tyto direktivy jsou: `Initialization`, `PositionalParameters`, `TypeArguments`, `FactoryMethod`, `Arguments`.</span><span class="sxs-lookup"><span data-stu-id="427a7-315">These directives are: `Initialization`, `PositionalParameters`, `TypeArguments`, `FactoryMethod`, `Arguments`.</span></span> <span data-ttu-id="427a7-316">Čtečky XAML .NET Framework XAML Services se pokoušejí tyto direktivy umístit jako první členové datového proudu uzlu za `StartObject`objektu z důvodů, které jsou vysvětleny v další části.</span><span class="sxs-lookup"><span data-stu-id="427a7-316">The .NET Framework XAML Services XAML readers attempt to place these directives as the first members in the node stream following an object's `StartObject`, for reasons that are explained in the next section.</span></span>

### <a name="xamlobjectwriter-behavior-and-node-order"></a><span data-ttu-id="427a7-317">Chování XamlObjectWriter a pořadí uzlů</span><span class="sxs-lookup"><span data-stu-id="427a7-317">XamlObjectWriter Behavior and Node Order</span></span>

<span data-ttu-id="427a7-318">`StartObject` do <xref:System.Xaml.XamlObjectWriter> není nutně signálem zapisovače objektu XAML, aby bylo možné hned vytvořit instanci objektu.</span><span class="sxs-lookup"><span data-stu-id="427a7-318">`StartObject` to a <xref:System.Xaml.XamlObjectWriter> is not necessarily a signal to the XAML object writer to immediately construct the object instance.</span></span> <span data-ttu-id="427a7-319">XAML obsahuje několik funkcí jazyka, které umožňují inicializaci objektu s dalšími vstupy a nespoléhat se výhradně na vyvolání konstruktoru bez parametrů k vytvoření počátečního objektu a nastavení vlastností.</span><span class="sxs-lookup"><span data-stu-id="427a7-319">XAML includes several language features that make it possible to initialize an object with additional input, and to not rely entirely on invoking a parameterless constructor to produce the initial object, and only then setting properties.</span></span> <span data-ttu-id="427a7-320">Mezi tyto funkce patří: <xref:System.Windows.Markup.XamlDeferLoadAttribute>; inicializační text; [x:TypeArguments –](x-typearguments-directive.md); poziční parametry rozšíření značek; metody pro vytváření a přidružené uzly [x:arguments –](x-arguments-directive.md) (XAML 2009).</span><span class="sxs-lookup"><span data-stu-id="427a7-320">These features include: <xref:System.Windows.Markup.XamlDeferLoadAttribute>; initialization text; [x:TypeArguments](x-typearguments-directive.md); positional parameters of a markup extension; factory methods and associated [x:Arguments](x-arguments-directive.md) nodes (XAML 2009).</span></span> <span data-ttu-id="427a7-321">Každý z těchto případů zpozdí skutečnou konstrukci objektu a vzhledem k tomu, že je přeobjednán datový proud uzlu, může zapisovač objektů XAML spoléhat na chování ve skutečné konstrukci instance vždy, když se vyskytne počáteční člen, který není specificky konstrukcí. Direktiva pro tento typ objektu.</span><span class="sxs-lookup"><span data-stu-id="427a7-321">Each of these cases delay the actual object construction, and because the node stream is reordered, the XAML object writer can rely on a behavior of actually constructing the instance whenever a start member is encountered that is not specifically a construction directive for that object type.</span></span>

### <a name="getobject"></a><span data-ttu-id="427a7-322">GetObject</span><span class="sxs-lookup"><span data-stu-id="427a7-322">GetObject</span></span>

<span data-ttu-id="427a7-323">`GetObject` představuje uzel XAML, kde spíše než vytváření nového objektu, by měl zapisovač objektů XAML místo toho získat hodnotu obsahující vlastnosti objektu.</span><span class="sxs-lookup"><span data-stu-id="427a7-323">`GetObject` represents a XAML node where rather than constructing a new object, a XAML object writer should instead get the value of the object's containing property.</span></span> <span data-ttu-id="427a7-324">Typický případ, kde je nalezen uzel `GetObject` v datovém proudu uzlu XAML, je pro objekt kolekce nebo objekt Dictionary, pokud je nadřazená vlastnost v objektovém modelu typu zálohování záměrně jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="427a7-324">A typical  case where a `GetObject` node is encountered in a XAML node stream is for a collection object or a dictionary object, when the containing property is deliberately read-only in the backing type's object model.</span></span> <span data-ttu-id="427a7-325">V tomto scénáři je kolekce nebo slovník často vytvořen a inicializován (obvykle prázdný) pomocí inicializační logiky vlastnícího typu.</span><span class="sxs-lookup"><span data-stu-id="427a7-325">In this scenario, the collection or dictionary often is created and initialized (usually empty) by the initialization logic of an owning type.</span></span>

## <a name="see-also"></a><span data-ttu-id="427a7-326">Viz také:</span><span class="sxs-lookup"><span data-stu-id="427a7-326">See also</span></span>

- <xref:System.Xaml.XamlObjectReader>
- [<span data-ttu-id="427a7-327">XAML Services</span><span class="sxs-lookup"><span data-stu-id="427a7-327">XAML Services</span></span>](index.md)
- [<span data-ttu-id="427a7-328">Obory názvů jazyka XAML</span><span class="sxs-lookup"><span data-stu-id="427a7-328">XAML Namespaces</span></span>](xaml-namespaces-for-net-framework-xaml-services.md)
