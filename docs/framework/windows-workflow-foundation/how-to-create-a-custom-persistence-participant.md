---
title: "Postupy: vytvoření vlastního účastník"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.tgt_pltfrm: 
ms.topic: article
ms.assetid: 1d9cc47a-8966-4286-94d5-4221403d9c06
caps.latest.revision: "6"
author: Erikre
ms.author: erikre
manager: erikre
ms.openlocfilehash: 4c690a0233a425a706ead7441447f589d8330bc0
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/18/2017
---
# <a name="how-to-create-a-custom-persistence-participant"></a><span data-ttu-id="27de6-102">Postupy: vytvoření vlastního účastník</span><span class="sxs-lookup"><span data-stu-id="27de6-102">How to: Create a Custom Persistence Participant</span></span>
<span data-ttu-id="27de6-103">Následující postup obsahuje kroky k vytvoření účastník trvalost.</span><span class="sxs-lookup"><span data-stu-id="27de6-103">The following procedure has steps to create a persistence participant.</span></span> <span data-ttu-id="27de6-104">Najdete v článku [účastní trvalost](http://go.microsoft.com/fwlink/?LinkID=177735) ukázka a [rozšiřitelnost úložiště](../../../docs/framework/windows-workflow-foundation/store-extensibility.md) téma pro ukázkové implementace trvalost účastníky.</span><span class="sxs-lookup"><span data-stu-id="27de6-104">See the [Participating in Persistence](http://go.microsoft.com/fwlink/?LinkID=177735) sample and [Store Extensibility](../../../docs/framework/windows-workflow-foundation/store-extensibility.md) topic for sample implementations of persistence participants.</span></span>  
  
1.  <span data-ttu-id="27de6-105">Vytvořte třídu odvozování z <xref:System.Activities.Persistence.PersistenceParticipant> nebo <xref:System.Activities.Persistence.PersistenceIOParticipant> třídy.</span><span class="sxs-lookup"><span data-stu-id="27de6-105">Create a class deriving from the <xref:System.Activities.Persistence.PersistenceParticipant> or the <xref:System.Activities.Persistence.PersistenceIOParticipant> class.</span></span> <span data-ttu-id="27de6-106">Třída PersistenceIOParticipant nabízí stejné body rozšiřitelnosti jako třída PersistenceParticipant kromě toho mohou podílet vstupně-výstupní operace.</span><span class="sxs-lookup"><span data-stu-id="27de6-106">The PersistenceIOParticipant class offers the same extensibility points as the PersistenceParticipant class in addition to being able to participate in IO operations.</span></span> <span data-ttu-id="27de6-107">Postupujte podle jednoho nebo více z následujících kroků.</span><span class="sxs-lookup"><span data-stu-id="27de6-107">Follow one or more of the following steps.</span></span>  
  
2.  <span data-ttu-id="27de6-108">Implementace <xref:System.Activities.Persistence.PersistenceParticipant.CollectValues%2A> metoda.</span><span class="sxs-lookup"><span data-stu-id="27de6-108">Implement the <xref:System.Activities.Persistence.PersistenceParticipant.CollectValues%2A> method.</span></span> <span data-ttu-id="27de6-109">**CollectValues** metoda má dva slovník parametrů, jeden pro ukládání hodnot pro čtení a zápis a dalších ten pro ukládání jen pro zápis hodnoty (používá se později v dotazech).</span><span class="sxs-lookup"><span data-stu-id="27de6-109">The **CollectValues** method has two dictionary parameters, one for storing read/write values and the other one for storing write-only values (used later in queries).</span></span> <span data-ttu-id="27de6-110">Tato metoda by měla naplnění těchto slovníků s daty, která je specifická pro účastníka trvalost.</span><span class="sxs-lookup"><span data-stu-id="27de6-110">In this method, you should populate these dictionaries with data that is specific to a persistence participant.</span></span> <span data-ttu-id="27de6-111">Každý slovník obsahuje název hodnoty jako klíč a hodnotu sám sebe jako <xref:System.Runtime.DurableInstancing.InstanceValue> objektu.</span><span class="sxs-lookup"><span data-stu-id="27de6-111">Each dictionary contains the name of the value as the key and the value itself as an <xref:System.Runtime.DurableInstancing.InstanceValue> object.</span></span>  
  
     <span data-ttu-id="27de6-112">Hodnoty ve slovníku readWriteValues spojených jako **InstanceValue** objekty.</span><span class="sxs-lookup"><span data-stu-id="27de6-112">The values in the readWriteValues dictionary are packaged as **InstanceValue** objects.</span></span> <span data-ttu-id="27de6-113">Hodnoty ve slovníku jen pro zápis spojených jako **InstanceValue** objekty sadou InstanceValueOptions.Optional a InstanceValueOption.WriteOnly.</span><span class="sxs-lookup"><span data-stu-id="27de6-113">The values in the write-only dictionary are packaged as **InstanceValue** objects with InstanceValueOptions.Optional and InstanceValueOption.WriteOnly set.</span></span> <span data-ttu-id="27de6-114">Každý **InstanceValue** poskytované **CollectValues** implementace napříč všichni účastníci trvalost musí mít jedinečný název.</span><span class="sxs-lookup"><span data-stu-id="27de6-114">Each **InstanceValue** provided by the **CollectValues** implementations across all persistence participants must have a unique name.</span></span>  
  
    ```  
    protected virtual void CollectValues (out IDictionary<XName,Object> readWriteValues, out IDictionary<XName,Object> writeOnlyValues)  
    ```  
  
3.  <span data-ttu-id="27de6-115">Implementace <xref:System.Activities.Persistence.PersistenceParticipant.MapValues%2A> metoda.</span><span class="sxs-lookup"><span data-stu-id="27de6-115">Implement the <xref:System.Activities.Persistence.PersistenceParticipant.MapValues%2A> method.</span></span> <span data-ttu-id="27de6-116">**MapValues** metoda přebírá dva parametry, které jsou podobné parametry, **CollectValues** metoda obdrží.</span><span class="sxs-lookup"><span data-stu-id="27de6-116">The **MapValues** method takes two parameters that are similar to the parameters that the **CollectValues** method receives.</span></span> <span data-ttu-id="27de6-117">Všechny hodnoty shromažďované **CollectValues** fáze se předaly tyto parametry slovníku.</span><span class="sxs-lookup"><span data-stu-id="27de6-117">All the values collected in the **CollectValues** stage are passed through these dictionary parameters.</span></span> <span data-ttu-id="27de6-118">S novými hodnotami přidal **MapValues** fáze jsou přidány do jen pro zápis hodnoty.</span><span class="sxs-lookup"><span data-stu-id="27de6-118">The new values added by the **MapValues** stage are added to the write-only values.</span></span>  <span data-ttu-id="27de6-119">Slovník jen pro zápis slouží k poskytování dat do externího zdroje není přímo přidružené hodnoty instance.</span><span class="sxs-lookup"><span data-stu-id="27de6-119">The write-only dictionary is used to provide data to an external source not directly associated with the instance values.</span></span> <span data-ttu-id="27de6-120">Každá hodnota poskytované implementace **MapValues** metoda napříč všichni účastníci trvalost musí mít jedinečný název.</span><span class="sxs-lookup"><span data-stu-id="27de6-120">Each value provided by implementations of the **MapValues** method across all persistence participants must have a unique name.</span></span>  
  
    ```  
    protected virtual IDictionary<XName,Object> MapValues (IDictionary<XName,Object> readWriteValues,IDictionary<XName,Object> writeOnlyValues)  
    ```  
  
     <span data-ttu-id="27de6-121"><xref:System.Activities.Persistence.PersistenceParticipant.MapValues%2A> Metoda poskytuje funkce, <xref:System.Activities.Persistence.PersistenceParticipant.CollectValues%2A> nemá, v, aby bylo povoleno pro závislost na jinou hodnotu poskytovanou infrastrukturou jiné trvalost člena, který nebyl zpracován podle <xref:System.Activities.Persistence.PersistenceParticipant.CollectValues%2A> ještě.</span><span class="sxs-lookup"><span data-stu-id="27de6-121">The <xref:System.Activities.Persistence.PersistenceParticipant.MapValues%2A> method provides functionality that <xref:System.Activities.Persistence.PersistenceParticipant.CollectValues%2A> does not, in that it allows for a dependency on another value provided by another persistence participant that hasn’t been processed by <xref:System.Activities.Persistence.PersistenceParticipant.CollectValues%2A> yet.</span></span>  
  
4.  <span data-ttu-id="27de6-122">Implementace **PublishValues** metoda.</span><span class="sxs-lookup"><span data-stu-id="27de6-122">Implement the **PublishValues** method.</span></span> <span data-ttu-id="27de6-123">**PublishValues** metoda přijímá slovník obsahující všechny hodnoty načíst z úložiště trvalosti.</span><span class="sxs-lookup"><span data-stu-id="27de6-123">The **PublishValues** method receives a dictionary containing all the values loaded from the persistence store.</span></span>  
  
    ```  
    protected virtual void PublishValues (IDictionary<XName,Object> readWriteValues)  
    ```  
  
5.  <span data-ttu-id="27de6-124">Implementace **BeginOnSave** metoda Pokud účastník je účastníkem trvalost vstupně-výstupní operace.</span><span class="sxs-lookup"><span data-stu-id="27de6-124">Implement the **BeginOnSave** method if the participant is a persistence IO participant.</span></span> <span data-ttu-id="27de6-125">Tato metoda je volána v průběhu operace uložit.</span><span class="sxs-lookup"><span data-stu-id="27de6-125">This method is called during a Save operation.</span></span> <span data-ttu-id="27de6-126">V této metodě je třeba provést adjunct vstupně-výstupní operace pro uchování (ukládání) instancí pracovních postupů.</span><span class="sxs-lookup"><span data-stu-id="27de6-126">In this method, you should perform IO adjunct to the persisting (saving) workflow instances.</span></span>  <span data-ttu-id="27de6-127">Pokud hostitel používá transakce pro příkaz odpovídající trvalost, stejné transakci je součástí Transaction.Current.</span><span class="sxs-lookup"><span data-stu-id="27de6-127">If the host is using a transaction for the corresponding persistence command, the same transaction is provided in Transaction.Current.</span></span>  <span data-ttu-id="27de6-128">Kromě toho může PersistenceIOParticipants inzerovat transakční konzistence požadavek, ve kterém případ hostitele vytvoří transakci pro trvalost díl, pokud by jinak je možné použít.</span><span class="sxs-lookup"><span data-stu-id="27de6-128">Additionally, PersistenceIOParticipants may advertise a transactional consistency requirement, in which case the host creates a transaction for the persistence episode if one would not otherwise be used.</span></span>  
  
    ```  
    protected virtual IAsyncResult BeginOnSave (IDictionary<XName,Object> readWriteValues, IDictionary<XName,Object> writeOnlyValues, TimeSpan timeout, AsyncCallback callback, Object state)  
    ```  
  
6.  <span data-ttu-id="27de6-129">Implementace **BeginOnLoad** metoda Pokud účastník je účastníkem trvalost vstupně-výstupní operace.</span><span class="sxs-lookup"><span data-stu-id="27de6-129">Implement the **BeginOnLoad** method if the participant is a persistence IO participant.</span></span> <span data-ttu-id="27de6-130">Tato metoda je volána v průběhu operace načtení.</span><span class="sxs-lookup"><span data-stu-id="27de6-130">This method is called during a Load operation.</span></span> <span data-ttu-id="27de6-131">V této metodě je třeba provést adjunct vstupně-výstupní operace pro načítání instance pracovního postupu.</span><span class="sxs-lookup"><span data-stu-id="27de6-131">In this method, you should perform IO adjunct to the loading of workflow instances.</span></span> <span data-ttu-id="27de6-132">Pokud hostitel používá transakce pro příkaz odpovídající trvalost, stejné transakci je součástí Transaction.Current.</span><span class="sxs-lookup"><span data-stu-id="27de6-132">If the host is using a transaction for the corresponding persistence command, the same transaction is provided in Transaction.Current.</span></span> <span data-ttu-id="27de6-133">Kromě toho může trvalost vstupně-výstupní operace účastníky inzerovat transakční konzistence požadavek, ve kterém případ hostitele vytvoří transakci pro trvalost díl, pokud by jinak je možné použít.</span><span class="sxs-lookup"><span data-stu-id="27de6-133">Additionally, Persistence IO participants may advertise a transactional consistency requirement, in which case the host creates a transaction for the persistence episode if one would not otherwise be used.</span></span>  
  
    ```  
    protected virtual IAsyncResult BeginOnLoad (IDictionary<XName,Object> readWriteValues, TimeSpan timeout, AsyncCallback callback, Object state)  
    ```
