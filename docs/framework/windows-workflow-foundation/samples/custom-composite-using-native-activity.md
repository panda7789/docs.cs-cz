---
title: "Vlastní složené pomocí nativní aktivity"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.tgt_pltfrm: 
ms.topic: article
ms.assetid: ef9e739c-8a8a-4d11-9e25-cb42c62e3c76
caps.latest.revision: "14"
author: Erikre
ms.author: erikre
manager: erikre
ms.openlocfilehash: 7e67febe1197a026b7bdcc6a4be27467c24da1c1
ms.sourcegitcommit: 5177d6ae2e9baf026f07ee0631556700a5a193f7
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 11/28/2017
---
# <a name="custom-composite-using-native-activity"></a><span data-ttu-id="36f6f-102">Vlastní složené pomocí nativní aktivity</span><span class="sxs-lookup"><span data-stu-id="36f6f-102">Custom Composite using Native Activity</span></span>
<span data-ttu-id="36f6f-103">Tento příklad ukazuje, jak k zápisu <xref:System.Activities.NativeActivity> , jiné plány <xref:System.Activities.Activity> objekty, které se řídí tok spuštění pracovního postupu.</span><span class="sxs-lookup"><span data-stu-id="36f6f-103">This sample demonstrates how to write a <xref:System.Activities.NativeActivity> that schedules other <xref:System.Activities.Activity> objects to control the flow of a workflow’s execution.</span></span> <span data-ttu-id="36f6f-104">Tato ukázka používá dvě běžné řízení toky, pořadí a při, aby ukazují, jak to udělat.</span><span class="sxs-lookup"><span data-stu-id="36f6f-104">This sample uses two common control flows, Sequence and While, to demonstrate how to do this.</span></span>  
  
## <a name="sample-details"></a><span data-ttu-id="36f6f-105">Ukázka podrobnosti</span><span class="sxs-lookup"><span data-stu-id="36f6f-105">Sample Details</span></span>  
 <span data-ttu-id="36f6f-106">Počínaje `MySequence`, je nejprve si všimněte si, že je odvozena z <xref:System.Activities.NativeActivity>.</span><span class="sxs-lookup"><span data-stu-id="36f6f-106">Starting with `MySequence`, the first thing to notice is that it derives from <xref:System.Activities.NativeActivity>.</span></span> <span data-ttu-id="36f6f-107"><xref:System.Activities.NativeActivity>je <xref:System.Activities.Activity> objekt, který zveřejňuje úplného spektra modulu runtime pracovního postupu pomocí <xref:System.Activities.NativeActivityContext> předaný `Execute` metoda.</span><span class="sxs-lookup"><span data-stu-id="36f6f-107"><xref:System.Activities.NativeActivity> is the <xref:System.Activities.Activity> object that exposes the full breadth of the workflow runtime through the <xref:System.Activities.NativeActivityContext> passed to the `Execute` method.</span></span>  
  
 <span data-ttu-id="36f6f-108">`MySequence`poskytuje veřejné kolekci <xref:System.Activities.Activity> objekty, které získá naplněny autorem pracovního postupu.</span><span class="sxs-lookup"><span data-stu-id="36f6f-108">`MySequence` exposes a public collection of <xref:System.Activities.Activity> objects that gets populated by the workflow author.</span></span> <span data-ttu-id="36f6f-109">Před provedením pracovní postup volá modulu runtime pracovního postupu <xref:System.Activities.Activity.CacheMetadata%2A> metoda na každou aktivitu v pracovním postupu.</span><span class="sxs-lookup"><span data-stu-id="36f6f-109">Before the workflow is executed, the workflow runtime calls the <xref:System.Activities.Activity.CacheMetadata%2A> method on each activity in a workflow.</span></span> <span data-ttu-id="36f6f-110">Během tohoto procesu vytvoří modul runtime vztahů nadřazenosti a podřízenosti pro správu dat na rozsahu a doby platnosti.</span><span class="sxs-lookup"><span data-stu-id="36f6f-110">During this process, the runtime establishes parent-child relationships for data scoping and lifetime management.</span></span> <span data-ttu-id="36f6f-111">Výchozí implementaci <xref:System.Activities.Activity.CacheMetadata%2A> metoda používá <xref:System.ComponentModel.TypeDescriptor> instance třídy pro `MySequence` aktivitu přidat všechny veřejné vlastnosti typu <xref:System.Activities.Activity> nebo <xref:System.Collections.IEnumerable> \< <xref:System.Activities.Activity>> jako podřízené objekty `MySequence` aktivity.</span><span class="sxs-lookup"><span data-stu-id="36f6f-111">The default implementation of the <xref:System.Activities.Activity.CacheMetadata%2A> method uses the <xref:System.ComponentModel.TypeDescriptor> instance class for the `MySequence` activity to add any public property of type <xref:System.Activities.Activity> or <xref:System.Collections.IEnumerable>\<<xref:System.Activities.Activity>> as children of the `MySequence` activity.</span></span>  
  
 <span data-ttu-id="36f6f-112">Vždy, když aktivita poskytuje veřejné kolekci podřízené aktivity, je pravděpodobné, že tyto aktivity podřízené sdílet stavu.</span><span class="sxs-lookup"><span data-stu-id="36f6f-112">Whenever an activity exposes a public collection of child activities, it is likely those child activities share state.</span></span> <span data-ttu-id="36f6f-113">V takovém případě je nejvhodnějším postupem pro nadřazené aktivity `MySequence`, také vystavit kolekci proměnných, pomocí kterých podřízené aktivity se dá dosáhnout.</span><span class="sxs-lookup"><span data-stu-id="36f6f-113">It is a best practice for the parent activity, in this case `MySequence`, to also expose a collection of variables through which the child activities can accomplish this.</span></span> <span data-ttu-id="36f6f-114">Podřízené aktivity, jako <xref:System.Activities.Activity.CacheMetadata%2A> metoda přidá veřejné vlastnosti typu <xref:System.Activities.Variable> nebo <xref:System.Collections.IEnumerable> \< <xref:System.Activities.Variable>> jako proměnné přidružené `MySequence` aktivity.</span><span class="sxs-lookup"><span data-stu-id="36f6f-114">Like child activities, the <xref:System.Activities.Activity.CacheMetadata%2A> method adds public properties of type <xref:System.Activities.Variable> or <xref:System.Collections.IEnumerable>\<<xref:System.Activities.Variable>> as variables associated with the `MySequence` activity.</span></span>  
  
 <span data-ttu-id="36f6f-115">Kromě toho veřejné proměnné, které jsou manipulovat podřízené objekty daného `MySequence`, `MySequence` musí také udržovat zaznamenávat, kde je při provádění jeho podřízených položek.</span><span class="sxs-lookup"><span data-stu-id="36f6f-115">Besides the public variables, which are manipulated by the children of `MySequence`, `MySequence` must also keep track of where it is in the execution of its children.</span></span> <span data-ttu-id="36f6f-116">Používá soukromé proměnné `currentIndex`, toho chcete dosáhnout.</span><span class="sxs-lookup"><span data-stu-id="36f6f-116">It uses a private variable, `currentIndex`, to accomplish this.</span></span> <span data-ttu-id="36f6f-117">Tato proměnná je zaregistrován v rámci `MySequence` prostředí tak, že přidáte volání <xref:System.Activities.NativeActivityMetadata.AddImplementationVariable%2A> metoda v rámci `MySequence` aktivity <xref:System.Activities.Activity.CacheMetadata%2A> metoda.</span><span class="sxs-lookup"><span data-stu-id="36f6f-117">This variable is registered as part of the `MySequence` environment by adding a call to the <xref:System.Activities.NativeActivityMetadata.AddImplementationVariable%2A> method within the `MySequence` activity’s <xref:System.Activities.Activity.CacheMetadata%2A> method.</span></span> <span data-ttu-id="36f6f-118"><xref:System.Activities.Activity> Objektů přidaných do `MySequence` `Activities` kolekce nemůže získat přístup k proměnné přidány tímto způsobem.</span><span class="sxs-lookup"><span data-stu-id="36f6f-118">The <xref:System.Activities.Activity> objects added to the `MySequence` `Activities` collection cannot access variables added this way.</span></span>  
  
 <span data-ttu-id="36f6f-119">Když `MySequence` je provedený modulu runtime volání modulu runtime jeho <xref:System.Activities.NativeActivity.Execute%2A> předávání v případě metody <xref:System.Activities.NativeActivityContext>.</span><span class="sxs-lookup"><span data-stu-id="36f6f-119">When `MySequence` is executed by the runtime, the runtime calls its <xref:System.Activities.NativeActivity.Execute%2A> method, passing in an <xref:System.Activities.NativeActivityContext>.</span></span> <span data-ttu-id="36f6f-120"><xref:System.Activities.NativeActivityContext> Je aktivity proxy zpět do modulu runtime pro vyhodnocení argumentů a proměnných a také další plánování <xref:System.Activities.Activity> objekty, nebo `ActivityDelegates`.</span><span class="sxs-lookup"><span data-stu-id="36f6f-120">The <xref:System.Activities.NativeActivityContext> is the activity’s proxy back into the runtime for dereferencing arguments and variables as well as scheduling other <xref:System.Activities.Activity> objects, or `ActivityDelegates`.</span></span> <span data-ttu-id="36f6f-121">`MySequence`používá `InternalExecute` metoda má být zapouzdřena logiku plánování první podřízený objekt a všechny následné podřízené objekty v jedné metody.</span><span class="sxs-lookup"><span data-stu-id="36f6f-121">`MySequence` uses an `InternalExecute` method to encapsulate the logic of scheduling the first child and all subsequent children in a single method.</span></span> <span data-ttu-id="36f6f-122">Začne tím vyhodnocení `currentIndex`.</span><span class="sxs-lookup"><span data-stu-id="36f6f-122">It starts by dereferencing the `currentIndex`.</span></span> <span data-ttu-id="36f6f-123">Pokud se rovná počtu v `Activities` dokončení kolekce a potom toto pořadí aktivita vrátí bez plánování veškerou práci, a modul runtime, přesune ho do <xref:System.Activities.ActivityInstanceState.Closed> stavu.</span><span class="sxs-lookup"><span data-stu-id="36f6f-123">If it is equal to the count in the `Activities` collection, then the sequence is finished, the activity returns without scheduling any work and the runtime moves it into the <xref:System.Activities.ActivityInstanceState.Closed> state.</span></span> <span data-ttu-id="36f6f-124">Pokud `currentIndex` je menší než počet aktivit, další podřízené se získávají z `Activities` kolekce a `MySequence` volání <xref:System.Activities.NativeActivityContext.ScheduleActivity%2A>, předejte v podřízeném k naplánování a <xref:System.Activities.CompletionCallback> který odkazuje na `InternalExecute` Metoda.</span><span class="sxs-lookup"><span data-stu-id="36f6f-124">If the `currentIndex` is less than the count of activities, the next child is obtained from the `Activities` collection and `MySequence` calls <xref:System.Activities.NativeActivityContext.ScheduleActivity%2A>, passing in the child to be scheduled and a <xref:System.Activities.CompletionCallback> that points at the `InternalExecute` method.</span></span> <span data-ttu-id="36f6f-125">Nakonec `currentIndex` se zvýší a ovládací prvek, je vhodné zpět do modulu runtime.</span><span class="sxs-lookup"><span data-stu-id="36f6f-125">Finally, the `currentIndex` is incremented and control is yielded back to the runtime.</span></span> <span data-ttu-id="36f6f-126">Stejně dlouho jako instanci `MySequence` má podřízenou <xref:System.Activities.Activity> objekt naplánované, modul runtime považuje ve stavu zpracování.</span><span class="sxs-lookup"><span data-stu-id="36f6f-126">As long as an instance of `MySequence` has a child <xref:System.Activities.Activity> object scheduled, the runtime considers it to be in the Executing state.</span></span>  
  
 <span data-ttu-id="36f6f-127">Po dokončení podřízené aktivity <xref:System.Activities.CompletionCallback> se spustí.</span><span class="sxs-lookup"><span data-stu-id="36f6f-127">When the child activity completes, the <xref:System.Activities.CompletionCallback> is executed.</span></span> <span data-ttu-id="36f6f-128">Smyčky pokračuje shora.</span><span class="sxs-lookup"><span data-stu-id="36f6f-128">The loop continues from the top.</span></span> <span data-ttu-id="36f6f-129">Jako `Execute`, <xref:System.Activities.CompletionCallback> trvá <xref:System.Activities.NativeActivityContext>, udělíte přístup implementátor modulu runtime.</span><span class="sxs-lookup"><span data-stu-id="36f6f-129">Like `Execute`, a <xref:System.Activities.CompletionCallback> takes an <xref:System.Activities.NativeActivityContext>, giving the implementer access to the runtime.</span></span>  
  
 <span data-ttu-id="36f6f-130">`MyWhile`se liší od `MySequence` v tom, že naplánuje jedné <xref:System.Activities.Activity> objektu opakovaně a v tom, že používá <xref:System.Activities.Activity%601>< bool\> s názvem `Condition` k určení, zda tento plánování provedeno.</span><span class="sxs-lookup"><span data-stu-id="36f6f-130">`MyWhile` differs from `MySequence` in that it schedules a single <xref:System.Activities.Activity> object repeatedly, and in that it uses a <xref:System.Activities.Activity%601><bool\> named `Condition` to determine whether this scheduling should occur.</span></span> <span data-ttu-id="36f6f-131">Jako `MySequence`, `MyWhile` používá `InternalExecute` metoda a centralizovat plánování logiky pravidla.</span><span class="sxs-lookup"><span data-stu-id="36f6f-131">Like `MySequence`, `MyWhile` uses an `InternalExecute` method to centralize its scheduling logic.</span></span> <span data-ttu-id="36f6f-132">Naplánuje `Condition` <xref:System.Activities.Activity>< bool\> s <xref:System.Activities.CompletionCallback%601> \<bool > s názvem `OnEvaluationCompleted`.</span><span class="sxs-lookup"><span data-stu-id="36f6f-132">It schedules the `Condition`<xref:System.Activities.Activity><bool\> with a <xref:System.Activities.CompletionCallback%601>\<bool> named `OnEvaluationCompleted`.</span></span> <span data-ttu-id="36f6f-133">Při provádění `Condition` je dokončena, její výsledek bude k dispozici prostřednictvím to <xref:System.Activities.CompletionCallback> parametr silného typu s názvem `result`.</span><span class="sxs-lookup"><span data-stu-id="36f6f-133">When the execution of `Condition` is completed, its result becomes available through this <xref:System.Activities.CompletionCallback> in a strongly-typed parameter named `result`.</span></span> <span data-ttu-id="36f6f-134">Pokud `true`, `MyWhile` volání <xref:System.Activities.NativeActivityContext.ScheduleActivity%2A>a předejte `Body` <xref:System.Activities.Activity> objektu a `InternalExecute` jako <xref:System.Activities.CompletionCallback>.</span><span class="sxs-lookup"><span data-stu-id="36f6f-134">If `true`, `MyWhile` calls  <xref:System.Activities.NativeActivityContext.ScheduleActivity%2A>, passing in the `Body`<xref:System.Activities.Activity> object and `InternalExecute` as the <xref:System.Activities.CompletionCallback>.</span></span> <span data-ttu-id="36f6f-135">Při provádění `Body` dokončení `Condition` získá znovu v naplánované `InternalExecute`, spouštění smyčky přes znovu.</span><span class="sxs-lookup"><span data-stu-id="36f6f-135">When the execution of `Body` completes, `Condition` gets scheduled again in `InternalExecute`, starting the loop over again.</span></span> <span data-ttu-id="36f6f-136">Při `Condition` vrátí `false`, instanci `MyWhile` nabízí kontrolu zpět do runtime bez plánování `Body` a modul runtime přesouvá ji do <xref:System.Activities.ActivityInstanceState.Closed> stavu.</span><span class="sxs-lookup"><span data-stu-id="36f6f-136">When the `Condition` returns `false`, an instance of `MyWhile` gives control back to the runtime without scheduling the `Body` and the runtime moves it to the <xref:System.Activities.ActivityInstanceState.Closed> state.</span></span>  
  
#### <a name="to-set-up-build-and-run-the-sample"></a><span data-ttu-id="36f6f-137">Pokud chcete nastavit, sestavit a spustit ukázku</span><span class="sxs-lookup"><span data-stu-id="36f6f-137">To set up, build, and run the sample</span></span>  
  
1.  <span data-ttu-id="36f6f-138">Otevřete Composite.sln ukázkové řešení v [!INCLUDE[vs2010](../../../../includes/vs2010-md.md)].</span><span class="sxs-lookup"><span data-stu-id="36f6f-138">Open the Composite.sln sample solution in [!INCLUDE[vs2010](../../../../includes/vs2010-md.md)].</span></span>  
  
2.  <span data-ttu-id="36f6f-139">Sestavení a spuštění řešení.</span><span class="sxs-lookup"><span data-stu-id="36f6f-139">Build and run the solution.</span></span>  
  
> [!IMPORTANT]
>  <span data-ttu-id="36f6f-140">Ukázky může být již nainstalována na váš počítač.</span><span class="sxs-lookup"><span data-stu-id="36f6f-140">The samples may already be installed on your machine.</span></span> <span data-ttu-id="36f6f-141">Před pokračováním zkontrolovat na následující adresář (výchozí).</span><span class="sxs-lookup"><span data-stu-id="36f6f-141">Check for the following (default) directory before continuing.</span></span>  
>   
>  `<InstallDrive>:\WF_WCF_Samples`  
>   
>  <span data-ttu-id="36f6f-142">Pokud tento adresář neexistuje, přejděte na [Windows Communication Foundation (WCF) a ukázky Windows Workflow Foundation (WF) pro rozhraní .NET Framework 4](http://go.microsoft.com/fwlink/?LinkId=150780) ke stažení všechny [!INCLUDE[indigo1](../../../../includes/indigo1-md.md)] a [!INCLUDE[wf1](../../../../includes/wf1-md.md)] ukázky.</span><span class="sxs-lookup"><span data-stu-id="36f6f-142">If this directory does not exist, go to [Windows Communication Foundation (WCF) and Windows Workflow Foundation (WF) Samples for .NET Framework 4](http://go.microsoft.com/fwlink/?LinkId=150780) to download all [!INCLUDE[indigo1](../../../../includes/indigo1-md.md)] and [!INCLUDE[wf1](../../../../includes/wf1-md.md)] samples.</span></span> <span data-ttu-id="36f6f-143">Tato ukázka se nachází v následujícím adresáři.</span><span class="sxs-lookup"><span data-stu-id="36f6f-143">This sample is located in the following directory.</span></span>  
>   
>  `<InstallDrive>:\WF_WCF_Samples\WF\Basic\CustomActivities\Code-Bodied\CustomCompositeNativeActivity`