---
title: Opožděná inicializace
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- lazy initialization in .NET, introduction
ms.assetid: 56b4ae5c-4745-44ff-ad78-ffe4fcde6b9b
ms.openlocfilehash: 4f2b585dded6e20bb604f623217c6d1f1505c097
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/12/2020
ms.locfileid: "79180568"
---
# <a name="lazy-initialization"></a><span data-ttu-id="7840b-102">Opožděná inicializace</span><span class="sxs-lookup"><span data-stu-id="7840b-102">Lazy Initialization</span></span>
<span data-ttu-id="7840b-103">*Opožděná inicializace* objektu znamená, že jeho vytvoření je odloženo, dokud není poprvé použit.</span><span class="sxs-lookup"><span data-stu-id="7840b-103">*Lazy initialization* of an object means that its creation is deferred until it is first used.</span></span> <span data-ttu-id="7840b-104">(Pro toto téma jsou synonyma termíny *opožděná inicializace* a *opožděná instance.)* Opožděná inicializace se používá především ke zlepšení výkonu, vyhnout se nehospodárným výpočtům a snížit požadavky na paměť programu.</span><span class="sxs-lookup"><span data-stu-id="7840b-104">(For this topic, the terms *lazy initialization* and *lazy instantiation* are synonymous.) Lazy initialization is primarily used to improve performance, avoid wasteful computation, and reduce program memory requirements.</span></span> <span data-ttu-id="7840b-105">Toto jsou nejběžnější scénáře:</span><span class="sxs-lookup"><span data-stu-id="7840b-105">These are the most common scenarios:</span></span>  
  
- <span data-ttu-id="7840b-106">Pokud máte objekt, který je nákladné vytvořit a program nemusí použít.</span><span class="sxs-lookup"><span data-stu-id="7840b-106">When you have an object that is expensive to create, and the program might not use it.</span></span> <span data-ttu-id="7840b-107">Předpokládejme například, že máte `Customer` v paměti `Orders` objekt, který má `Order` vlastnost, která obsahuje velké pole objektů, které, které mají být inicializovány, vyžaduje připojení k databázi.</span><span class="sxs-lookup"><span data-stu-id="7840b-107">For example, assume that you have in memory a `Customer` object that has an `Orders` property that contains a large array of `Order` objects that, to be initialized, requires a database connection.</span></span> <span data-ttu-id="7840b-108">Pokud uživatel nikdy nepožádá o zobrazení objednávky nebo použití dat ve výpočtu, pak není důvod k jeho vytvoření použít systémovou paměť nebo výpočetní cykly.</span><span class="sxs-lookup"><span data-stu-id="7840b-108">If the user never asks to display the Orders or use the data in a computation, then there is no reason to use system memory or computing cycles to create it.</span></span> <span data-ttu-id="7840b-109">Pomocí `Lazy<Orders>` deklarovat `Orders` objekt pro opožděné inicializace, můžete se vyhnout plýtvání systémové prostředky, pokud objekt není použit.</span><span class="sxs-lookup"><span data-stu-id="7840b-109">By using `Lazy<Orders>` to declare the `Orders` object for lazy initialization, you can avoid wasting system resources when the object is not used.</span></span>  
  
- <span data-ttu-id="7840b-110">Pokud máte objekt, který je nákladné vytvořit a chcete odložit jeho vytvoření až po dokončení jiných nákladných operací.</span><span class="sxs-lookup"><span data-stu-id="7840b-110">When you have an object that is expensive to create, and you want to defer its creation until after other expensive operations have been completed.</span></span> <span data-ttu-id="7840b-111">Předpokládejme například, že program načte několik instancí objektu při spuštění, ale pouze některé z nich jsou vyžadovány okamžitě.</span><span class="sxs-lookup"><span data-stu-id="7840b-111">For example, assume that your program loads several object instances when it starts, but only some of them are required immediately.</span></span> <span data-ttu-id="7840b-112">Výkon spuštění programu můžete zlepšit odložením inicializace objektů, které nejsou požadovány, dokud nebudou vytvořeny požadované objekty.</span><span class="sxs-lookup"><span data-stu-id="7840b-112">You can improve the startup performance of the program by deferring initialization of the objects that are not required until the required objects have been created.</span></span>  
  
 <span data-ttu-id="7840b-113">I když můžete napsat vlastní kód k provedení opožděné <xref:System.Lazy%601> inicializace, doporučujeme použít místo.</span><span class="sxs-lookup"><span data-stu-id="7840b-113">Although you can write your own code to perform lazy initialization, we recommend that you use <xref:System.Lazy%601> instead.</span></span> <span data-ttu-id="7840b-114"><xref:System.Lazy%601>a jeho související typy také podporují bezpečnost vláken a poskytují konzistentní zásady šíření výjimek.</span><span class="sxs-lookup"><span data-stu-id="7840b-114"><xref:System.Lazy%601> and its related types also support thread-safety and provide a consistent exception propagation policy.</span></span>  
  
 <span data-ttu-id="7840b-115">V následující tabulce jsou uvedeny typy, které poskytuje rozhraní .NET Framework verze 4 pro povolení opožděné inicializace v různých scénářích.</span><span class="sxs-lookup"><span data-stu-id="7840b-115">The following table lists the types that the .NET Framework version 4 provides to enable lazy initialization in different scenarios.</span></span>  
  
|<span data-ttu-id="7840b-116">Typ</span><span class="sxs-lookup"><span data-stu-id="7840b-116">Type</span></span>|<span data-ttu-id="7840b-117">Popis</span><span class="sxs-lookup"><span data-stu-id="7840b-117">Description</span></span>|  
|----------|-----------------|  
|<xref:System.Lazy%601>|<span data-ttu-id="7840b-118">Obálka třída, která poskytuje opožděné inicializace sémantiku pro všechny třídy knihovny nebo uživatelem definovaný typ.</span><span class="sxs-lookup"><span data-stu-id="7840b-118">A wrapper class that provides lazy initialization semantics for any class library or user-defined type.</span></span>|  
|<xref:System.Threading.ThreadLocal%601>|<span data-ttu-id="7840b-119">Podobá <xref:System.Lazy%601> se s tím rozdílem, že poskytuje opožděné inicializace sémantiku na místní bázi vlákna.</span><span class="sxs-lookup"><span data-stu-id="7840b-119">Resembles <xref:System.Lazy%601> except that it provides lazy initialization semantics on a thread-local basis.</span></span> <span data-ttu-id="7840b-120">Každé vlákno má přístup ke své vlastní jedinečné hodnotě.</span><span class="sxs-lookup"><span data-stu-id="7840b-120">Every thread has access to its own unique value.</span></span>|  
|<xref:System.Threading.LazyInitializer>|<span data-ttu-id="7840b-121">Poskytuje rozšířené `static` `Shared` (v jazyce Visual Basic) metody pro opožděné inicializace objektů bez režie třídy.</span><span class="sxs-lookup"><span data-stu-id="7840b-121">Provides advanced `static` (`Shared` in Visual Basic) methods for lazy initialization of objects without the overhead of a class.</span></span>|  
  
## <a name="basic-lazy-initialization"></a><span data-ttu-id="7840b-122">Základní opožděná inicializace</span><span class="sxs-lookup"><span data-stu-id="7840b-122">Basic Lazy Initialization</span></span>  
 <span data-ttu-id="7840b-123">Chcete-li definovat typ opožděné inicializované, `MyType`například , use `Lazy<MyType>` (`Lazy(Of MyType)` v jazyce Visual Basic), jak je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="7840b-123">To define a lazy-initialized type, for example, `MyType`, use `Lazy<MyType>` (`Lazy(Of MyType)` in Visual Basic), as shown in the following example.</span></span> <span data-ttu-id="7840b-124">Pokud žádný delegát je <xref:System.Lazy%601> předán v konstruktoru, zalomený typ je vytvořen pomocí <xref:System.Activator.CreateInstance%2A?displayProperty=nameWithType> při prvním přístupu k vlastnosti value.</span><span class="sxs-lookup"><span data-stu-id="7840b-124">If no delegate is passed in the <xref:System.Lazy%601> constructor, the wrapped type is created by using <xref:System.Activator.CreateInstance%2A?displayProperty=nameWithType> when the value property is first accessed.</span></span> <span data-ttu-id="7840b-125">Pokud typ nemá konstruktor bez parametrů, je vyvolána výjimka za běhu.</span><span class="sxs-lookup"><span data-stu-id="7840b-125">If the type does not have a parameterless constructor, a run-time exception is thrown.</span></span>  
  
 <span data-ttu-id="7840b-126">V následujícím příkladu `Orders` předpokládejme, že se `Order` jedná o třídu, která obsahuje pole objektů načtených z databáze.</span><span class="sxs-lookup"><span data-stu-id="7840b-126">In the following example, assume that `Orders` is a class that contains an array of `Order` objects retrieved from a database.</span></span> <span data-ttu-id="7840b-127">Objekt `Customer` obsahuje instanci `Orders`aplikace , ale v závislosti `Orders` na akcích uživatele nemusí být data z objektu vyžadována.</span><span class="sxs-lookup"><span data-stu-id="7840b-127">A `Customer` object contains an instance of `Orders`, but depending on user actions, the data from the `Orders` object might not be required.</span></span>  
  
 [!code-csharp[Lazy#1](../../../samples/snippets/csharp/VS_Snippets_Misc/lazy/cs/cs_lazycodefile.cs#1)]
 [!code-vb[Lazy#1](../../../samples/snippets/visualbasic/VS_Snippets_Misc/lazy/vb/lazy_vb.vb#1)]  
  
 <span data-ttu-id="7840b-128">Můžete také předat delegáta <xref:System.Lazy%601> v konstruktoru, který vyvolá přetížení konkrétníkonstruktor u zabaleného typu v době vytvoření a provést všechny další kroky inicializace, které jsou požadovány, jak je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="7840b-128">You can also pass a delegate in the <xref:System.Lazy%601> constructor that invokes a specific constructor overload on the wrapped type at creation time, and perform any other initialization steps that are required, as shown in the following example.</span></span>  
  
 [!code-csharp[Lazy#2](../../../samples/snippets/csharp/VS_Snippets_Misc/lazy/cs/cs_lazycodefile.cs#2)]
 [!code-vb[Lazy#2](../../../samples/snippets/visualbasic/VS_Snippets_Misc/lazy/vb/lazy_vb.vb#2)]  
  
 <span data-ttu-id="7840b-129">Po vytvoření Lazy objekt je `Orders` vytvořen žádná <xref:System.Lazy%601.Value%2A> instance, dokud je poprvé přístupná vlastnost Lazy proměnné.</span><span class="sxs-lookup"><span data-stu-id="7840b-129">After the Lazy object is created, no instance of `Orders` is created until the <xref:System.Lazy%601.Value%2A> property of the Lazy variable is accessed for the first time.</span></span> <span data-ttu-id="7840b-130">Při prvním přístupu je zabalený typ vytvořen a vrácen a uložen pro jakýkoli budoucí přístup.</span><span class="sxs-lookup"><span data-stu-id="7840b-130">On first access, the wrapped type is created and returned, and stored for any future access.</span></span>  
  
 [!code-csharp[Lazy#3](../../../samples/snippets/csharp/VS_Snippets_Misc/lazy/cs/cs_lazycodefile.cs#3)]
 [!code-vb[Lazy#3](../../../samples/snippets/visualbasic/VS_Snippets_Misc/lazy/vb/lazy_vb.vb#3)]  
  
 <span data-ttu-id="7840b-131">Objekt <xref:System.Lazy%601> vždy vrátí stejný objekt nebo hodnotu, se kterou byl inicializován.</span><span class="sxs-lookup"><span data-stu-id="7840b-131">A <xref:System.Lazy%601> object always returns the same object or value that it was initialized with.</span></span> <span data-ttu-id="7840b-132">Proto je <xref:System.Lazy%601.Value%2A> vlastnost jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="7840b-132">Therefore, the <xref:System.Lazy%601.Value%2A> property is read-only.</span></span> <span data-ttu-id="7840b-133">Pokud <xref:System.Lazy%601.Value%2A> ukládá typ odkazu, nelze mu přiřadit nový objekt.</span><span class="sxs-lookup"><span data-stu-id="7840b-133">If <xref:System.Lazy%601.Value%2A> stores a reference type, you cannot assign a new object to it.</span></span> <span data-ttu-id="7840b-134">(Můžete však změnit hodnotu jeho měnitelných veřejných polí a vlastností.) Pokud <xref:System.Lazy%601.Value%2A> ukládá typ hodnoty, nelze změnit jeho hodnotu.</span><span class="sxs-lookup"><span data-stu-id="7840b-134">(However, you can change the value of its settable public fields and properties.) If <xref:System.Lazy%601.Value%2A> stores a value type, you cannot modify its value.</span></span> <span data-ttu-id="7840b-135">Nicméně můžete vytvořit novou proměnnou znovu vyvoláním konstruktoru proměnné pomocí nových argumentů.</span><span class="sxs-lookup"><span data-stu-id="7840b-135">Nevertheless, you can create a new variable by invoking the variable constructor again by using new arguments.</span></span>  
  
 [!code-csharp[Lazy#4](../../../samples/snippets/csharp/VS_Snippets_Misc/lazy/cs/cs_lazycodefile.cs#4)]
 [!code-vb[Lazy#4](../../../samples/snippets/visualbasic/VS_Snippets_Misc/lazy/vb/lazy_vb.vb#4)]  
  
 <span data-ttu-id="7840b-136">Nová líná instance, stejně jako předchozí, `Orders` není <xref:System.Lazy%601.Value%2A> vytvořena instance, dokud není nejprve přístupná k její vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="7840b-136">The new lazy instance, like the earlier one, does not instantiate `Orders` until its <xref:System.Lazy%601.Value%2A> property is first accessed.</span></span>  
  
### <a name="thread-safe-initialization"></a><span data-ttu-id="7840b-137">Inicializace bezpečná pro přístup z více vláken</span><span class="sxs-lookup"><span data-stu-id="7840b-137">Thread-Safe Initialization</span></span>  
 <span data-ttu-id="7840b-138">Ve výchozím <xref:System.Lazy%601> nastavení jsou objekty bezpečné pro přístup z více vláken.</span><span class="sxs-lookup"><span data-stu-id="7840b-138">By default, <xref:System.Lazy%601> objects are thread-safe.</span></span> <span data-ttu-id="7840b-139">To znamená, že pokud konstruktor neurčuje druh <xref:System.Lazy%601> bezpečnosti podprocesu, objekty, které vytvoří, jsou bezpečné pro přístup z více vláken.</span><span class="sxs-lookup"><span data-stu-id="7840b-139">That is, if the constructor does not specify the kind of thread safety, the <xref:System.Lazy%601> objects it creates are thread-safe.</span></span> <span data-ttu-id="7840b-140">Ve scénářích s více vlákny první <xref:System.Lazy%601.Value%2A> vlákno pro přístup <xref:System.Lazy%601> k vlastnosti objektu bezpečného pro přístup k vláknům inicializuje pro všechny následné přístupy ve všech vláknech a všechna vlákna sdílejí stejná data.</span><span class="sxs-lookup"><span data-stu-id="7840b-140">In multi-threaded scenarios, the first thread to access the <xref:System.Lazy%601.Value%2A> property of a thread-safe <xref:System.Lazy%601> object initializes it for all subsequent accesses on all threads, and all threads share the same data.</span></span> <span data-ttu-id="7840b-141">Proto nezáleží na tom, které vlákno inicializuje objekt a podmínky časovky jsou neškodné.</span><span class="sxs-lookup"><span data-stu-id="7840b-141">Therefore, it does not matter which thread initializes the object, and race conditions are benign.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="7840b-142">Tuto konzistenci můžete rozšířit na chybové stavy pomocí ukládání výjimek do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="7840b-142">You can extend this consistency to error conditions by using exception caching.</span></span> <span data-ttu-id="7840b-143">Další informace naleznete v další části [Výjimky v opožděné objekty](lazy-initialization.md#ExceptionsInLazyObjects).</span><span class="sxs-lookup"><span data-stu-id="7840b-143">For more information, see the next section, [Exceptions in Lazy Objects](lazy-initialization.md#ExceptionsInLazyObjects).</span></span>  
  
 <span data-ttu-id="7840b-144">Následující příklad ukazuje, `Lazy<int>` že stejná instance má stejnou hodnotu pro tři samostatné podprocesy.</span><span class="sxs-lookup"><span data-stu-id="7840b-144">The following example shows that the same `Lazy<int>` instance has the same value for three separate threads.</span></span>  
  
 [!code-csharp[Lazy#8](../../../samples/snippets/csharp/VS_Snippets_Misc/lazy/cs/cs_lazycodefile.cs#8)]
 [!code-vb[Lazy#8](../../../samples/snippets/visualbasic/VS_Snippets_Misc/lazy/vb/lazy_vb.vb#8)]  
  
 <span data-ttu-id="7840b-145">Pokud požadujete samostatná data v <xref:System.Threading.ThreadLocal%601> každém vlákně, použijte typ, jak je popsáno dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="7840b-145">If you require separate data on each thread, use the <xref:System.Threading.ThreadLocal%601> type, as described later in this topic.</span></span>  
  
 <span data-ttu-id="7840b-146">Někteří <xref:System.Lazy%601> konstruktory mají logický `isThreadSafe` parametr s názvem, <xref:System.Lazy%601.Value%2A> který se používá k určení, zda bude vlastnost přístupná z více vláken.</span><span class="sxs-lookup"><span data-stu-id="7840b-146">Some <xref:System.Lazy%601> constructors have a Boolean parameter named `isThreadSafe` that is used to specify whether the <xref:System.Lazy%601.Value%2A> property will be accessed from multiple threads.</span></span> <span data-ttu-id="7840b-147">Pokud máte v úmyslu získat přístup k `false` vlastnosti pouze z jednoho vlákna, předat získat výhodu skromný výkon.</span><span class="sxs-lookup"><span data-stu-id="7840b-147">If you intend to access the property from just one thread, pass in `false` to obtain a modest performance benefit.</span></span> <span data-ttu-id="7840b-148">Pokud máte v úmyslu získat přístup k `true` vlastnosti <xref:System.Lazy%601> z více vláken, předat pokyn instance správně zpracovat spory, ve kterém jedno vlákno vyvolá výjimku v době inicializace.</span><span class="sxs-lookup"><span data-stu-id="7840b-148">If you intend to access the property from multiple threads, pass in `true` to instruct the <xref:System.Lazy%601> instance to correctly handle race conditions in which one thread throws an exception at initialization time.</span></span>  
  
 <span data-ttu-id="7840b-149">Některé <xref:System.Lazy%601> konstruktory <xref:System.Threading.LazyThreadSafetyMode> mají `mode`parametr s názvem .</span><span class="sxs-lookup"><span data-stu-id="7840b-149">Some <xref:System.Lazy%601> constructors have a <xref:System.Threading.LazyThreadSafetyMode> parameter named `mode`.</span></span> <span data-ttu-id="7840b-150">Tyto konstruktory poskytují další režim bezpečnosti závitu.</span><span class="sxs-lookup"><span data-stu-id="7840b-150">These constructors provide an additional thread safety mode.</span></span> <span data-ttu-id="7840b-151">Následující tabulka ukazuje, jak je <xref:System.Lazy%601> bezpečnost podprocesu objektu ovlivněna parametry konstruktoru, které určují bezpečnost podprocesu.</span><span class="sxs-lookup"><span data-stu-id="7840b-151">The following table shows how the thread safety of a <xref:System.Lazy%601> object is affected by constructor parameters that specify thread safety.</span></span> <span data-ttu-id="7840b-152">Každý konstruktor má maximálně jeden takový parametr.</span><span class="sxs-lookup"><span data-stu-id="7840b-152">Each constructor has at most one such parameter.</span></span>  
  
|<span data-ttu-id="7840b-153">Bezpečnost závitu objektu</span><span class="sxs-lookup"><span data-stu-id="7840b-153">Thread safety of the object</span></span>|<span data-ttu-id="7840b-154">`LazyThreadSafetyMode``mode` parametr</span><span class="sxs-lookup"><span data-stu-id="7840b-154">`LazyThreadSafetyMode` `mode` parameter</span></span>|<span data-ttu-id="7840b-155">Logický `isThreadSafe` parametr</span><span class="sxs-lookup"><span data-stu-id="7840b-155">Boolean `isThreadSafe` parameter</span></span>|<span data-ttu-id="7840b-156">Žádné bezpečnostní parametry závitu</span><span class="sxs-lookup"><span data-stu-id="7840b-156">No thread safety parameters</span></span>|  
|---------------------------------|---------------------------------------------|--------------------------------------|---------------------------------|  
|<span data-ttu-id="7840b-157">Plně bezpečné pro přístup z více vláken; pouze jedno vlákno najednou se pokusí inicializovat hodnotu.</span><span class="sxs-lookup"><span data-stu-id="7840b-157">Fully thread-safe; only one thread at a time tries to initialize the value.</span></span>|<xref:System.Threading.LazyThreadSafetyMode.ExecutionAndPublication>|`true`|<span data-ttu-id="7840b-158">Ano.</span><span class="sxs-lookup"><span data-stu-id="7840b-158">Yes.</span></span>|  
|<span data-ttu-id="7840b-159">Není bezpečné pro přístup z více vláken.</span><span class="sxs-lookup"><span data-stu-id="7840b-159">Not thread-safe.</span></span>|<xref:System.Threading.LazyThreadSafetyMode.None>|`false`|<span data-ttu-id="7840b-160">Neužívá se.</span><span class="sxs-lookup"><span data-stu-id="7840b-160">Not applicable.</span></span>|  
|<span data-ttu-id="7840b-161">Plně bezpečné pro přístup z více vláken; vlákna závod inicializovat hodnotu.</span><span class="sxs-lookup"><span data-stu-id="7840b-161">Fully thread-safe; threads race to initialize the value.</span></span>|<xref:System.Threading.LazyThreadSafetyMode.PublicationOnly>|<span data-ttu-id="7840b-162">Neužívá se.</span><span class="sxs-lookup"><span data-stu-id="7840b-162">Not applicable.</span></span>|<span data-ttu-id="7840b-163">Neužívá se.</span><span class="sxs-lookup"><span data-stu-id="7840b-163">Not applicable.</span></span>|  
  
 <span data-ttu-id="7840b-164">Jak ukazuje tabulka, <xref:System.Threading.LazyThreadSafetyMode.ExecutionAndPublication?displayProperty=nameWithType> určení `mode` parametru je stejné `true` jako `isThreadSafe` určení parametru <xref:System.Threading.LazyThreadSafetyMode.None?displayProperty=nameWithType> a určení je `false`stejné jako určení .</span><span class="sxs-lookup"><span data-stu-id="7840b-164">As the table shows, specifying <xref:System.Threading.LazyThreadSafetyMode.ExecutionAndPublication?displayProperty=nameWithType> for the `mode` parameter is the same as specifying `true` for the `isThreadSafe` parameter, and specifying <xref:System.Threading.LazyThreadSafetyMode.None?displayProperty=nameWithType> is the same as specifying `false`.</span></span>  
  
 <span data-ttu-id="7840b-165">Zadání <xref:System.Threading.LazyThreadSafetyMode.PublicationOnly?displayProperty=nameWithType> umožňuje více vláken pokusit se inicializovat <xref:System.Lazy%601> instanci.</span><span class="sxs-lookup"><span data-stu-id="7840b-165">Specifying <xref:System.Threading.LazyThreadSafetyMode.PublicationOnly?displayProperty=nameWithType> allows multiple threads to attempt to initialize the <xref:System.Lazy%601> instance.</span></span> <span data-ttu-id="7840b-166">Pouze jedno vlákno může vyhrát tento závod a všechny ostatní podprocesy obdrží hodnotu, která byla inicializována úspěšným vláknem.</span><span class="sxs-lookup"><span data-stu-id="7840b-166">Only one thread can win this race, and all the other threads receive the value that was initialized by the successful thread.</span></span> <span data-ttu-id="7840b-167">Pokud je vyvolána výjimka na vlákno během inicializace, toto vlákno neobdrží hodnotu nastavenou úspěšným vláknem.</span><span class="sxs-lookup"><span data-stu-id="7840b-167">If an exception is thrown on a thread during initialization, that thread does not receive the value set by the successful thread.</span></span> <span data-ttu-id="7840b-168">Výjimky nejsou ukládány do mezipaměti, <xref:System.Lazy%601.Value%2A> takže následný pokus o přístup k vlastnosti může mít za následek úspěšnou inicializaci.</span><span class="sxs-lookup"><span data-stu-id="7840b-168">Exceptions are not cached, so a subsequent attempt to access the <xref:System.Lazy%601.Value%2A> property can result in successful initialization.</span></span> <span data-ttu-id="7840b-169">To se liší od způsobu, jakým jsou výjimky zpracovány v jiných režimech, což je popsáno v následující části.</span><span class="sxs-lookup"><span data-stu-id="7840b-169">This differs from the way exceptions are treated in other modes, which is described in the following section.</span></span> <span data-ttu-id="7840b-170">Další informace naleznete <xref:System.Threading.LazyThreadSafetyMode> ve výčtu.</span><span class="sxs-lookup"><span data-stu-id="7840b-170">For more information, see the <xref:System.Threading.LazyThreadSafetyMode> enumeration.</span></span>  
  
<a name="ExceptionsInLazyObjects"></a>
## <a name="exceptions-in-lazy-objects"></a><span data-ttu-id="7840b-171">Výjimky v opožděných objektech</span><span class="sxs-lookup"><span data-stu-id="7840b-171">Exceptions in Lazy Objects</span></span>  
 <span data-ttu-id="7840b-172">Jak již bylo <xref:System.Lazy%601> uvedeno dříve, objekt vždy vrátí stejný objekt nebo <xref:System.Lazy%601.Value%2A> hodnotu, která byla inicializována s, a proto je vlastnost jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="7840b-172">As stated earlier, a <xref:System.Lazy%601> object always returns the same object or value that it was initialized with, and therefore the <xref:System.Lazy%601.Value%2A> property is read-only.</span></span> <span data-ttu-id="7840b-173">Pokud povolíte ukládání výjimek do mezipaměti, tato neměnnost se vztahuje také na chování výjimek.</span><span class="sxs-lookup"><span data-stu-id="7840b-173">If you enable exception caching, this immutability also extends to exception behavior.</span></span> <span data-ttu-id="7840b-174">Pokud opožděně inicializovaný objekt má povolen ukládání výjimek do mezipaměti <xref:System.Lazy%601.Value%2A> a vyvolá výjimku z jeho metody inicializace <xref:System.Lazy%601.Value%2A> při prvním přístupu k vlastnosti, je vyvolána stejná výjimka při každém následném pokusu o přístup k vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="7840b-174">If a lazy-initialized object has exception caching enabled and throws an exception from its initialization method when the <xref:System.Lazy%601.Value%2A> property is first accessed, that same exception is thrown on every subsequent attempt to access the <xref:System.Lazy%601.Value%2A> property.</span></span> <span data-ttu-id="7840b-175">Jinými slovy konstruktor zabalené typu je nikdy znovu vyvolána, a to i ve scénářích s více vlákny.</span><span class="sxs-lookup"><span data-stu-id="7840b-175">In other words, the constructor of the wrapped type is never re-invoked, even in multithreaded scenarios.</span></span> <span data-ttu-id="7840b-176">Proto <xref:System.Lazy%601> objekt nemůže vyvolat výjimku na jeden přístup a vrátit hodnotu na následný přístup.</span><span class="sxs-lookup"><span data-stu-id="7840b-176">Therefore, the <xref:System.Lazy%601> object cannot throw an exception on one access and return a value on a subsequent access.</span></span>  
  
 <span data-ttu-id="7840b-177">Ukládání výjimek do mezipaměti <xref:System.Lazy%601?displayProperty=nameWithType> je povoleno při použití libovolného konstruktoru, který přebírá metodu inicializace (parametr);`valueFactory` například je povolena při `Lazy(T)(Func(T))`použití konstruktoru.</span><span class="sxs-lookup"><span data-stu-id="7840b-177">Exception caching is enabled when you use any <xref:System.Lazy%601?displayProperty=nameWithType> constructor that takes an initialization method (`valueFactory` parameter); for example, it is enabled when you use the `Lazy(T)(Func(T))`constructor.</span></span> <span data-ttu-id="7840b-178">Pokud konstruktor také <xref:System.Threading.LazyThreadSafetyMode> přebírá`mode` hodnotu <xref:System.Threading.LazyThreadSafetyMode.ExecutionAndPublication?displayProperty=nameWithType> ( <xref:System.Threading.LazyThreadSafetyMode.None?displayProperty=nameWithType>parametr), zadejte nebo .</span><span class="sxs-lookup"><span data-stu-id="7840b-178">If the constructor also takes a <xref:System.Threading.LazyThreadSafetyMode> value (`mode` parameter), specify <xref:System.Threading.LazyThreadSafetyMode.ExecutionAndPublication?displayProperty=nameWithType> or <xref:System.Threading.LazyThreadSafetyMode.None?displayProperty=nameWithType>.</span></span> <span data-ttu-id="7840b-179">Zadání metody inicializace umožňuje ukládání výjimek do mezipaměti pro tyto dva režimy.</span><span class="sxs-lookup"><span data-stu-id="7840b-179">Specifying an initialization method enables exception caching for these two modes.</span></span> <span data-ttu-id="7840b-180">Metoda inicializace může být velmi jednoduchá.</span><span class="sxs-lookup"><span data-stu-id="7840b-180">The initialization method can be very simple.</span></span> <span data-ttu-id="7840b-181">Například může volat konstruktor bez `T`parametrů `new Lazy<Contents>(() => new Contents(), mode)` pro : `New Lazy(Of Contents)(Function() New Contents())` v jazyce C# nebo v jazyce Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="7840b-181">For example, it might call the parameterless constructor for `T`: `new Lazy<Contents>(() => new Contents(), mode)` in C#, or `New Lazy(Of Contents)(Function() New Contents())` in Visual Basic.</span></span> <span data-ttu-id="7840b-182">Pokud použijete <xref:System.Lazy%601?displayProperty=nameWithType> konstruktor, který neurčuje metodu inicializace, výjimky, které `T` jsou vyvolány konstruktorem bez parametrů, nejsou uloženy do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="7840b-182">If you use a <xref:System.Lazy%601?displayProperty=nameWithType> constructor that does not specify an initialization method, exceptions that are thrown by the parameterless constructor for `T` are not cached.</span></span> <span data-ttu-id="7840b-183">Další informace naleznete <xref:System.Threading.LazyThreadSafetyMode> ve výčtu.</span><span class="sxs-lookup"><span data-stu-id="7840b-183">For more information, see the <xref:System.Threading.LazyThreadSafetyMode> enumeration.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="7840b-184">Pokud vytvoříte <xref:System.Lazy%601> objekt `isThreadSafe` s parametrem `false` konstruktoru nastaveným na nebo parametrem konstruktoru `mode` nastaveným na <xref:System.Threading.LazyThreadSafetyMode.None?displayProperty=nameWithType>, musíte k objektu <xref:System.Lazy%601> přistupovat z jednoho vlákna nebo zadat vlastní synchronizaci.</span><span class="sxs-lookup"><span data-stu-id="7840b-184">If you create a <xref:System.Lazy%601> object with the `isThreadSafe` constructor parameter set to `false` or the `mode` constructor parameter set to <xref:System.Threading.LazyThreadSafetyMode.None?displayProperty=nameWithType>, you must access the <xref:System.Lazy%601> object from a single thread or provide your own synchronization.</span></span> <span data-ttu-id="7840b-185">To platí pro všechny aspekty objektu, včetně ukládání výjimek do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="7840b-185">This applies to all aspects of the object, including exception caching.</span></span>  
  
 <span data-ttu-id="7840b-186">Jak je uvedeno v <xref:System.Lazy%601> předchozí části, <xref:System.Threading.LazyThreadSafetyMode.PublicationOnly?displayProperty=nameWithType> objekty vytvořené zadáním výjimky jinak.</span><span class="sxs-lookup"><span data-stu-id="7840b-186">As noted in the previous section, <xref:System.Lazy%601> objects created by specifying <xref:System.Threading.LazyThreadSafetyMode.PublicationOnly?displayProperty=nameWithType> treat exceptions differently.</span></span> <span data-ttu-id="7840b-187">S <xref:System.Threading.LazyThreadSafetyMode.PublicationOnly>, více vláken může soutěžit <xref:System.Lazy%601> o inicializaci instance.</span><span class="sxs-lookup"><span data-stu-id="7840b-187">With <xref:System.Threading.LazyThreadSafetyMode.PublicationOnly>, multiple threads can compete to initialize the <xref:System.Lazy%601> instance.</span></span> <span data-ttu-id="7840b-188">V tomto případě výjimky nejsou uloženy do mezipaměti a pokusy o přístup k <xref:System.Lazy%601.Value%2A> vlastnosti může pokračovat, dokud inicializace je úspěšná.</span><span class="sxs-lookup"><span data-stu-id="7840b-188">In this case, exceptions are not cached, and attempts to access the <xref:System.Lazy%601.Value%2A> property can continue until initialization is successful.</span></span>  
  
 <span data-ttu-id="7840b-189">Následující tabulka shrnuje <xref:System.Lazy%601> způsob, jakým konstruktory řídí ukládání výjimek do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="7840b-189">The following table summarizes the way the <xref:System.Lazy%601> constructors control exception caching.</span></span>  
  
|<span data-ttu-id="7840b-190">Konstruktor</span><span class="sxs-lookup"><span data-stu-id="7840b-190">Constructor</span></span>|<span data-ttu-id="7840b-191">Režim bezpečnosti závitu</span><span class="sxs-lookup"><span data-stu-id="7840b-191">Thread safety mode</span></span>|<span data-ttu-id="7840b-192">Používá metodu inicializace.</span><span class="sxs-lookup"><span data-stu-id="7840b-192">Uses initialization method</span></span>|<span data-ttu-id="7840b-193">Výjimky jsou ukládány do mezipaměti</span><span class="sxs-lookup"><span data-stu-id="7840b-193">Exceptions are cached</span></span>|  
|-----------------|------------------------|--------------------------------|---------------------------|  
|<span data-ttu-id="7840b-194">Líný(T)()</span><span class="sxs-lookup"><span data-stu-id="7840b-194">Lazy(T)()</span></span>|<span data-ttu-id="7840b-195">(<xref:System.Threading.LazyThreadSafetyMode.ExecutionAndPublication>)</span><span class="sxs-lookup"><span data-stu-id="7840b-195">(<xref:System.Threading.LazyThreadSafetyMode.ExecutionAndPublication>)</span></span>|<span data-ttu-id="7840b-196">Ne</span><span class="sxs-lookup"><span data-stu-id="7840b-196">No</span></span>|<span data-ttu-id="7840b-197">Ne</span><span class="sxs-lookup"><span data-stu-id="7840b-197">No</span></span>|  
|<span data-ttu-id="7840b-198">Líný(T)(Func(T))</span><span class="sxs-lookup"><span data-stu-id="7840b-198">Lazy(T)(Func(T))</span></span>|<span data-ttu-id="7840b-199">(<xref:System.Threading.LazyThreadSafetyMode.ExecutionAndPublication>)</span><span class="sxs-lookup"><span data-stu-id="7840b-199">(<xref:System.Threading.LazyThreadSafetyMode.ExecutionAndPublication>)</span></span>|<span data-ttu-id="7840b-200">Ano</span><span class="sxs-lookup"><span data-stu-id="7840b-200">Yes</span></span>|<span data-ttu-id="7840b-201">Ano</span><span class="sxs-lookup"><span data-stu-id="7840b-201">Yes</span></span>|  
|<span data-ttu-id="7840b-202">Lazy(T)(logická hodnota)</span><span class="sxs-lookup"><span data-stu-id="7840b-202">Lazy(T)(Boolean)</span></span>|<span data-ttu-id="7840b-203">`True`(<xref:System.Threading.LazyThreadSafetyMode.ExecutionAndPublication>) `false` <xref:System.Threading.LazyThreadSafetyMode.None>nebo (</span><span class="sxs-lookup"><span data-stu-id="7840b-203">`True` (<xref:System.Threading.LazyThreadSafetyMode.ExecutionAndPublication>) or `false` (<xref:System.Threading.LazyThreadSafetyMode.None>)</span></span>|<span data-ttu-id="7840b-204">Ne</span><span class="sxs-lookup"><span data-stu-id="7840b-204">No</span></span>|<span data-ttu-id="7840b-205">Ne</span><span class="sxs-lookup"><span data-stu-id="7840b-205">No</span></span>|  
|<span data-ttu-id="7840b-206">Lazy(T)(Func(T), logická)</span><span class="sxs-lookup"><span data-stu-id="7840b-206">Lazy(T)(Func(T), Boolean)</span></span>|<span data-ttu-id="7840b-207">`True`(<xref:System.Threading.LazyThreadSafetyMode.ExecutionAndPublication>) `false` <xref:System.Threading.LazyThreadSafetyMode.None>nebo (</span><span class="sxs-lookup"><span data-stu-id="7840b-207">`True` (<xref:System.Threading.LazyThreadSafetyMode.ExecutionAndPublication>) or `false` (<xref:System.Threading.LazyThreadSafetyMode.None>)</span></span>|<span data-ttu-id="7840b-208">Ano</span><span class="sxs-lookup"><span data-stu-id="7840b-208">Yes</span></span>|<span data-ttu-id="7840b-209">Ano</span><span class="sxs-lookup"><span data-stu-id="7840b-209">Yes</span></span>|  
|<span data-ttu-id="7840b-210">Lazy(T)(LazyThreadSafetyMode)</span><span class="sxs-lookup"><span data-stu-id="7840b-210">Lazy(T)(LazyThreadSafetyMode)</span></span>|<span data-ttu-id="7840b-211">Zadané uživatelem</span><span class="sxs-lookup"><span data-stu-id="7840b-211">User-specified</span></span>|<span data-ttu-id="7840b-212">Ne</span><span class="sxs-lookup"><span data-stu-id="7840b-212">No</span></span>|<span data-ttu-id="7840b-213">Ne</span><span class="sxs-lookup"><span data-stu-id="7840b-213">No</span></span>|  
|<span data-ttu-id="7840b-214">Lazy(T)(Func(T), LazyThreadSafetyMode)</span><span class="sxs-lookup"><span data-stu-id="7840b-214">Lazy(T)(Func(T), LazyThreadSafetyMode)</span></span>|<span data-ttu-id="7840b-215">Zadané uživatelem</span><span class="sxs-lookup"><span data-stu-id="7840b-215">User-specified</span></span>|<span data-ttu-id="7840b-216">Ano</span><span class="sxs-lookup"><span data-stu-id="7840b-216">Yes</span></span>|<span data-ttu-id="7840b-217">Ne, pokud <xref:System.Threading.LazyThreadSafetyMode.PublicationOnly>uživatel určuje ; jinak ano.</span><span class="sxs-lookup"><span data-stu-id="7840b-217">No if user specifies <xref:System.Threading.LazyThreadSafetyMode.PublicationOnly>; otherwise, yes.</span></span>|  
  
## <a name="implementing-a-lazy-initialized-property"></a><span data-ttu-id="7840b-218">Implementace vlastnosti inicializované inicializovanou</span><span class="sxs-lookup"><span data-stu-id="7840b-218">Implementing a Lazy-Initialized Property</span></span>  
 <span data-ttu-id="7840b-219">Chcete-li implementovat veřejnou vlastnost pomocí opožděné inicializace, definujte záložní pole vlastnosti jako <xref:System.Lazy%601>a <xref:System.Lazy%601.Value%2A> vrátíte vlastnost z `get` přistupujícího objektu vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="7840b-219">To implement a public property by using lazy initialization, define the backing field of the property as a <xref:System.Lazy%601>, and return the <xref:System.Lazy%601.Value%2A> property from the `get` accessor of the property.</span></span>  
  
 [!code-csharp[Lazy#5](../../../samples/snippets/csharp/VS_Snippets_Misc/lazy/cs/cs_lazycodefile.cs#5)]
 [!code-vb[Lazy#5](../../../samples/snippets/visualbasic/VS_Snippets_Misc/lazy/vb/lazy_vb.vb#5)]  
  
 <span data-ttu-id="7840b-220">Vlastnost <xref:System.Lazy%601.Value%2A> je jen pro čtení; proto vlastnost, která zveřejňuje nemá `set` žádný přistupující objekt.</span><span class="sxs-lookup"><span data-stu-id="7840b-220">The <xref:System.Lazy%601.Value%2A> property is read-only; therefore, the property that exposes it has no `set` accessor.</span></span> <span data-ttu-id="7840b-221">Pokud požadujete vlastnost pro čtení a <xref:System.Lazy%601> zápis `set` podporovanou objektem, <xref:System.Lazy%601> musí přistupující objekt vytvořit nový objekt a přiřadit jej k záložnímu úložišti.</span><span class="sxs-lookup"><span data-stu-id="7840b-221">If you require a read/write property backed by a <xref:System.Lazy%601> object, the `set` accessor must create a new <xref:System.Lazy%601> object and assign it to the backing store.</span></span> <span data-ttu-id="7840b-222">Přistupující `set` objekt musí vytvořit výraz lambda, který vrátí `set` novou hodnotu vlastnosti, která byla předána <xref:System.Lazy%601> přistupujícímu objektu, a předat tento výraz lambda konstruktoru pro nový objekt.</span><span class="sxs-lookup"><span data-stu-id="7840b-222">The `set` accessor must create a lambda expression that returns the new property value that was passed to the `set` accessor, and pass that lambda expression to the constructor for the new <xref:System.Lazy%601> object.</span></span> <span data-ttu-id="7840b-223">Další přístup vlastnosti <xref:System.Lazy%601.Value%2A> způsobí inicializaci <xref:System.Lazy%601>nového <xref:System.Lazy%601.Value%2A> a jeho vlastnost poté vrátí novou hodnotu, která byla přiřazena vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="7840b-223">The next access of the <xref:System.Lazy%601.Value%2A> property will cause initialization of the new <xref:System.Lazy%601>, and its <xref:System.Lazy%601.Value%2A> property will thereafter return the new value that was assigned to the property.</span></span> <span data-ttu-id="7840b-224">Důvodem pro toto spletité uspořádání je zachovat vícevláknové ochrany zabudované do <xref:System.Lazy%601>.</span><span class="sxs-lookup"><span data-stu-id="7840b-224">The reason for this convoluted arrangement is to preserve the multithreading protections built into <xref:System.Lazy%601>.</span></span> <span data-ttu-id="7840b-225">V opačném případě by přístupové objekty vlastnosti <xref:System.Lazy%601.Value%2A> musely ukládat do mezipaměti první hodnotu vrácenou vlastností a upravovat pouze hodnotu uloženou v mezipaměti a k tomu by bylo nutné napsat vlastní kód bezpečný pro přístup pod proces.</span><span class="sxs-lookup"><span data-stu-id="7840b-225">Otherwise, the property accessors would have to cache the first value returned by the <xref:System.Lazy%601.Value%2A> property and only modify the cached value, and you would have to write your own thread-safe code to do that.</span></span> <span data-ttu-id="7840b-226">Z důvodu další inicializace vyžadované <xref:System.Lazy%601> vlastnosti pro čtení a zápis ukrytý objektem nemusí být výkon přijatelný.</span><span class="sxs-lookup"><span data-stu-id="7840b-226">Because of the additional initializations required by a read/write property backed by a <xref:System.Lazy%601> object, the performance might not be acceptable.</span></span> <span data-ttu-id="7840b-227">Kromě toho, v závislosti na konkrétním scénáři, může být nutná další koordinace, aby se zabránilo sporným podmínkám mezi settery a gettery.</span><span class="sxs-lookup"><span data-stu-id="7840b-227">Furthermore, depending on the specific scenario, additional coordination might be required to avoid race conditions between setters and getters.</span></span>  
  
## <a name="thread-local-lazy-initialization"></a><span data-ttu-id="7840b-228">Opožděná inicializace místního vlákna</span><span class="sxs-lookup"><span data-stu-id="7840b-228">Thread-Local Lazy Initialization</span></span>  
 <span data-ttu-id="7840b-229">V některých scénářích s více vlákny můžete chtít dát každému vláknu vlastní soukromá data.</span><span class="sxs-lookup"><span data-stu-id="7840b-229">In some multithreaded scenarios, you might want to give each thread its own private data.</span></span> <span data-ttu-id="7840b-230">Tato data se nazývají *místní data podprocesu*.</span><span class="sxs-lookup"><span data-stu-id="7840b-230">Such data is called *thread-local data*.</span></span> <span data-ttu-id="7840b-231">V rozhraní .NET Framework verze 3.5 a `ThreadStatic` starší, můžete použít atribut statickou proměnnou, aby bylo vlákno místní.</span><span class="sxs-lookup"><span data-stu-id="7840b-231">In the .NET Framework version 3.5 and earlier, you could apply the `ThreadStatic` attribute to a static variable to make it thread-local.</span></span> <span data-ttu-id="7840b-232">Použití atributu `ThreadStatic` však může vést k jemným chybám.</span><span class="sxs-lookup"><span data-stu-id="7840b-232">However, using the `ThreadStatic` attribute can lead to subtle errors.</span></span> <span data-ttu-id="7840b-233">Například i základní inicializační příkazy způsobí, že proměnná bude inicializována pouze v prvním vlákně, které k ní přistupuje, jak je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="7840b-233">For example, even basic initialization statements will cause the variable to be initialized only on the first thread that accesses it, as shown in the following example.</span></span>  
  
 [!code-csharp[Lazy#6](../../../samples/snippets/csharp/VS_Snippets_Misc/lazy/cs/cs_lazycodefile.cs#6)]
 [!code-vb[Lazy#6](../../../samples/snippets/visualbasic/VS_Snippets_Misc/lazy/vb/lazy_vb.vb#6)]  
  
 <span data-ttu-id="7840b-234">Ve všech ostatních vláknech bude proměnná inicializována pomocí výchozí hodnoty (nula).</span><span class="sxs-lookup"><span data-stu-id="7840b-234">On all other threads, the variable will be initialized by using its default value (zero).</span></span> <span data-ttu-id="7840b-235">Jako alternativu v rozhraní .NET Framework verze <xref:System.Threading.ThreadLocal%601?displayProperty=nameWithType> 4 můžete použít typ k vytvoření proměnné založené na instancích, místní vlákno, která je inicializována na všech podvcích <xref:System.Action%601> delegátem, který zadáte.</span><span class="sxs-lookup"><span data-stu-id="7840b-235">As an alternative in the .NET Framework version 4, you can use the <xref:System.Threading.ThreadLocal%601?displayProperty=nameWithType> type to create an instance-based, thread-local variable that is initialized on all threads by the <xref:System.Action%601> delegate that you provide.</span></span> <span data-ttu-id="7840b-236">V následujícím příkladu se všem `counter` vláknům, ke kterým přistupuje, zobrazí počáteční hodnota jako 1.</span><span class="sxs-lookup"><span data-stu-id="7840b-236">In the following example, all threads that access `counter` will see its starting value as 1.</span></span>  
  
 [!code-csharp[Lazy#7](../../../samples/snippets/csharp/VS_Snippets_Misc/lazy/cs/cs_lazycodefile.cs#7)]
 [!code-vb[Lazy#7](../../../samples/snippets/visualbasic/VS_Snippets_Misc/lazy/vb/lazy_vb.vb#7)]  
  
 <span data-ttu-id="7840b-237"><xref:System.Threading.ThreadLocal%601>zabalí svůj objekt v podstatě stejným způsobem jako <xref:System.Lazy%601>, s těmito základními rozdíly:</span><span class="sxs-lookup"><span data-stu-id="7840b-237"><xref:System.Threading.ThreadLocal%601> wraps its object in much the same way as <xref:System.Lazy%601>, with these essential differences:</span></span>  
  
- <span data-ttu-id="7840b-238">Každé vlákno inicializuje místní proměnnou podprocesu pomocí vlastních soukromých dat, která nejsou přístupná z jiných vláken.</span><span class="sxs-lookup"><span data-stu-id="7840b-238">Each thread initializes the thread-local variable by using its own private data that is not accessible from other threads.</span></span>  
  
- <span data-ttu-id="7840b-239">Vlastnost <xref:System.Threading.ThreadLocal%601.Value%2A?displayProperty=nameWithType> je čtení a zápis a lze upravit libovolný počet opakování.</span><span class="sxs-lookup"><span data-stu-id="7840b-239">The <xref:System.Threading.ThreadLocal%601.Value%2A?displayProperty=nameWithType> property is read-write, and can be modified any number of times.</span></span> <span data-ttu-id="7840b-240">To může ovlivnit šíření výjimek, `get` například jedna operace může vyvolat výjimku, ale další může úspěšně inicializovat hodnotu.</span><span class="sxs-lookup"><span data-stu-id="7840b-240">This can affect exception propagation, for example, one `get` operation can raise an exception but the next one can successfully initialize the value.</span></span>  
  
- <span data-ttu-id="7840b-241">Pokud není k dispozici žádný <xref:System.Threading.ThreadLocal%601> delegát inicializace, inicializuje jeho zalomený typ pomocí výchozí hodnoty typu.</span><span class="sxs-lookup"><span data-stu-id="7840b-241">If no initialization delegate is provided, <xref:System.Threading.ThreadLocal%601> will initialize its wrapped type by using the default value of the type.</span></span> <span data-ttu-id="7840b-242">V tomto <xref:System.Threading.ThreadLocal%601> ohledu je <xref:System.ThreadStaticAttribute> v souladu s atributem.</span><span class="sxs-lookup"><span data-stu-id="7840b-242">In this regard, <xref:System.Threading.ThreadLocal%601> is consistent with the <xref:System.ThreadStaticAttribute> attribute.</span></span>  
  
 <span data-ttu-id="7840b-243">Následující příklad ukazuje, že každé vlákno, které přistupuje k `ThreadLocal<int>` instanci získá vlastní jedinečnou kopii dat.</span><span class="sxs-lookup"><span data-stu-id="7840b-243">The following example demonstrates that every thread that accesses the `ThreadLocal<int>` instance gets its own unique copy of the data.</span></span>  
  
 [!code-csharp[Lazy#9](../../../samples/snippets/csharp/VS_Snippets_Misc/lazy/cs/cs_lazycodefile.cs#9)]
 [!code-vb[Lazy#9](../../../samples/snippets/visualbasic/VS_Snippets_Misc/lazy/vb/lazy_vb.vb#9)]  
  
## <a name="thread-local-variables-in-parallelfor-and-foreach"></a><span data-ttu-id="7840b-244">Místní proměnné podprocesu v Parallel.For a ForEach</span><span class="sxs-lookup"><span data-stu-id="7840b-244">Thread-Local Variables in Parallel.For and ForEach</span></span>  
 <span data-ttu-id="7840b-245">Při použití <xref:System.Threading.Tasks.Parallel.For%2A?displayProperty=nameWithType> metody <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> nebo metody iterát přes zdroje dat paralelně, můžete použít přetížení, které mají vestavěnou podporu pro místní data podprocesu.</span><span class="sxs-lookup"><span data-stu-id="7840b-245">When you use the <xref:System.Threading.Tasks.Parallel.For%2A?displayProperty=nameWithType> method or <xref:System.Threading.Tasks.Parallel.ForEach%2A?displayProperty=nameWithType> method to iterate over data sources in parallel, you can use the overloads that have built-in support for thread-local data.</span></span> <span data-ttu-id="7840b-246">V těchto metodách je lokalita vlákna dosažena pomocí místních delegátů k vytvoření, přístupu a vyčištění dat.</span><span class="sxs-lookup"><span data-stu-id="7840b-246">In these methods, the thread-locality is achieved by using local delegates to create, access, and clean up the data.</span></span> <span data-ttu-id="7840b-247">Další informace naleznete v [tématu How to: Write a Parallel.For Loop with Thread-Local Variables](../../standard/parallel-programming/how-to-write-a-parallel-for-loop-with-thread-local-variables.md) and [How to: Write a Parallel.ForEach Loop with Partition-Local Variables](../../standard/parallel-programming/how-to-write-a-parallel-foreach-loop-with-partition-local-variables.md).</span><span class="sxs-lookup"><span data-stu-id="7840b-247">For more information, see [How to: Write a Parallel.For Loop with Thread-Local Variables](../../standard/parallel-programming/how-to-write-a-parallel-for-loop-with-thread-local-variables.md) and [How to: Write a Parallel.ForEach Loop with Partition-Local Variables](../../standard/parallel-programming/how-to-write-a-parallel-foreach-loop-with-partition-local-variables.md).</span></span>  
  
## <a name="using-lazy-initialization-for-low-overhead-scenarios"></a><span data-ttu-id="7840b-248">Použití opožděné inicializace pro scénáře s nízkou režií</span><span class="sxs-lookup"><span data-stu-id="7840b-248">Using Lazy Initialization for Low-Overhead Scenarios</span></span>  
 <span data-ttu-id="7840b-249">Ve scénářích, kde je třeba opožděně inicializovat velký počet objektů, <xref:System.Lazy%601> můžete se rozhodnout, že obtékání každého objektu v vyžaduje příliš mnoho paměti nebo příliš mnoho výpočetních prostředků.</span><span class="sxs-lookup"><span data-stu-id="7840b-249">In scenarios where you have to lazy-initialize a large number of objects, you might decide that wrapping each object in a <xref:System.Lazy%601> requires too much memory or too many computing resources.</span></span> <span data-ttu-id="7840b-250">Nebo můžete mít přísné požadavky na jak opožděné inicializace je vystavena.</span><span class="sxs-lookup"><span data-stu-id="7840b-250">Or, you might have stringent requirements about how lazy initialization is exposed.</span></span> <span data-ttu-id="7840b-251">V takových případech můžete `static` použít`Shared` ( v jazyce <xref:System.Threading.LazyInitializer?displayProperty=nameWithType> Visual Basic) metody třídy opožděně inicializovat každý objekt bez obtékání v instanci <xref:System.Lazy%601>.</span><span class="sxs-lookup"><span data-stu-id="7840b-251">In such cases, you can use the `static` (`Shared` in Visual Basic) methods of the <xref:System.Threading.LazyInitializer?displayProperty=nameWithType> class to lazy-initialize each object without wrapping it in an instance of <xref:System.Lazy%601>.</span></span>  
  
 <span data-ttu-id="7840b-252">V následujícím příkladu předpokládejme, že `Orders` místo zabalení celého objektu v jednom <xref:System.Lazy%601> objektu máte opožděné inicializované jednotlivé `Order` objekty pouze v případě, že jsou požadovány.</span><span class="sxs-lookup"><span data-stu-id="7840b-252">In the following example, assume that, instead of wrapping an entire `Orders` object in one <xref:System.Lazy%601> object, you have lazy-initialized individual `Order` objects only if they are required.</span></span>  
  
 [!code-csharp[Lazy#10](../../../samples/snippets/csharp/VS_Snippets_Misc/lazy/cs/cs_lazycodefile.cs#10)]
 [!code-vb[Lazy#10](../../../samples/snippets/visualbasic/VS_Snippets_Misc/lazy/vb/lazy_vb.vb#10)]  
  
 <span data-ttu-id="7840b-253">V tomto příkladu všimněte si, že postup inicializace je vyvolána na každé iteraci smyčky.</span><span class="sxs-lookup"><span data-stu-id="7840b-253">In this example, notice that the initialization procedure is invoked on every iteration of the loop.</span></span> <span data-ttu-id="7840b-254">Ve scénářích s více vlákny je první vlákno, které vyvolá inicializační proceduru, ten, jehož hodnotu vidí všechna vlákna.</span><span class="sxs-lookup"><span data-stu-id="7840b-254">In multi-threaded scenarios, the first thread to invoke the initialization procedure is the one whose value is seen by all threads.</span></span> <span data-ttu-id="7840b-255">Pozdější vlákna také vyvolat postup inicializace, ale jejich výsledky nejsou použity.</span><span class="sxs-lookup"><span data-stu-id="7840b-255">Later threads also invoke the initialization procedure, but their results are not used.</span></span> <span data-ttu-id="7840b-256">Pokud tento druh potenciální spor není přijatelné, použijte <xref:System.Threading.LazyInitializer.EnsureInitialized%2A?displayProperty=nameWithType> přetížení, který trvá logický argument a objekt synchronizace.</span><span class="sxs-lookup"><span data-stu-id="7840b-256">If this kind of potential race condition is not acceptable, use the overload of <xref:System.Threading.LazyInitializer.EnsureInitialized%2A?displayProperty=nameWithType> that takes a Boolean argument and a synchronization object.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="7840b-257">Viz také</span><span class="sxs-lookup"><span data-stu-id="7840b-257">See also</span></span>

- [<span data-ttu-id="7840b-258">Základy dělení na spravovaná vlákna</span><span class="sxs-lookup"><span data-stu-id="7840b-258">Managed Threading Basics</span></span>](../../standard/threading/managed-threading-basics.md)
- [<span data-ttu-id="7840b-259">Vlákna a dělení na vlákna</span><span class="sxs-lookup"><span data-stu-id="7840b-259">Threads and Threading</span></span>](../../standard/threading/threads-and-threading.md)
- [<span data-ttu-id="7840b-260">Task Parallel Library (TPL)</span><span class="sxs-lookup"><span data-stu-id="7840b-260">Task Parallel Library (TPL)</span></span>](../../standard/parallel-programming/task-parallel-library-tpl.md)
- [<span data-ttu-id="7840b-261">Postupy: Provádění opožděné inicializace objektů</span><span class="sxs-lookup"><span data-stu-id="7840b-261">How to: Perform Lazy Initialization of Objects</span></span>](how-to-perform-lazy-initialization-of-objects.md)
