---
title: Poskytovatelé CLR ETW
ms.date: 03/30/2017
helpviewer_keywords:
- ETW, CLR providers
- CLR ETW providers
ms.assetid: 0beafad4-b2c8-47f4-b342-83411d57a51f
ms.openlocfilehash: 33ef7491c2bffeda4ef737ed8f826cdfbfbb119d
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/14/2020
ms.locfileid: "79400076"
---
# <a name="clr-etw-providers"></a><span data-ttu-id="9507b-102">Poskytovatelé CLR ETW</span><span class="sxs-lookup"><span data-stu-id="9507b-102">CLR ETW Providers</span></span>
<span data-ttu-id="9507b-103">Běžný jazyk runtime (CLR) má dva zprostředkovatele: zprostředkovatele runtime a zprostředkovatele rundown.</span><span class="sxs-lookup"><span data-stu-id="9507b-103">The common language runtime (CLR) has two providers: the runtime provider and the rundown provider.</span></span>  
  
 <span data-ttu-id="9507b-104">Zprostředkovatel runtime vyvolává události v závislosti na tom, která klíčová slova (kategorie událostí) jsou povolena.</span><span class="sxs-lookup"><span data-stu-id="9507b-104">The runtime provider raises events, depending on which keywords (categories of events) are enabled.</span></span> <span data-ttu-id="9507b-105">Můžete například shromažďovat události zavaděče povolením klíčového `LoaderKeyword` slova.</span><span class="sxs-lookup"><span data-stu-id="9507b-105">For example, you can collect loader events by enabling the `LoaderKeyword` keyword.</span></span>  
  
 <span data-ttu-id="9507b-106">Události trasování událostí pro Windows (ETW) jsou zaznamenány do souboru, který má příponu .etl, která může být později podle potřeby post-processována v souborech s hodnotou oddělenou čárkou (.csv).</span><span class="sxs-lookup"><span data-stu-id="9507b-106">Event Tracing for Windows (ETW) events are logged into a file that has an .etl extension, which can later be post-processed in comma-separated value (.csv) files as needed.</span></span> <span data-ttu-id="9507b-107">Informace o převodu souboru .etl na soubor CSV naleznete [v tématu Controlling .NET Framework Logging](controlling-logging.md).</span><span class="sxs-lookup"><span data-stu-id="9507b-107">For information about how to convert the .etl file to a .csv file, see [Controlling .NET Framework Logging](controlling-logging.md).</span></span>  
  
## <a name="the-runtime-provider"></a><span data-ttu-id="9507b-108">Zprostředkovatel runtime</span><span class="sxs-lookup"><span data-stu-id="9507b-108">The Runtime Provider</span></span>  
 <span data-ttu-id="9507b-109">Zprostředkovatel runtime je hlavním poskytovatelem CLR ETW.</span><span class="sxs-lookup"><span data-stu-id="9507b-109">The runtime provider is the main CLR ETW provider.</span></span>  
  
 <span data-ttu-id="9507b-110">Identifikátor GUID zprostředkovatele runtime CLR je e13c0d23-ccbc-4e12-931b-d9cc2eee27e4.</span><span class="sxs-lookup"><span data-stu-id="9507b-110">The CLR runtime provider GUID is e13c0d23-ccbc-4e12-931b-d9cc2eee27e4.</span></span>  
  
 <span data-ttu-id="9507b-111">Příklady protokolování a zobrazení událostí CLR ETW pomocí běžně dostupných nástrojů naleznete [v tématu Controlling .NET Framework Logging](controlling-logging.md).</span><span class="sxs-lookup"><span data-stu-id="9507b-111">For examples of how to log and view CLR ETW events by using commonly available tools, see [Controlling .NET Framework Logging](controlling-logging.md).</span></span>  
  
 <span data-ttu-id="9507b-112">Kromě použití klíčových slov, jako `LoaderKeyword`je například , bude pravděpodobně muset povolit klíčová slova pro protokolování událostí, které mohou být vyvolány příliš často.</span><span class="sxs-lookup"><span data-stu-id="9507b-112">In addition to using keywords such as `LoaderKeyword`, you may have to enable keywords for logging events that may be raised too frequently.</span></span> <span data-ttu-id="9507b-113">Klíčová `StartEnumerationKeyword` `EndEnumerationKeyword` slova a umožňují tyto události a jsou shrnuty v [CLR ETW klíčová slova a úrovně](clr-etw-keywords-and-levels.md).</span><span class="sxs-lookup"><span data-stu-id="9507b-113">The `StartEnumerationKeyword` and the `EndEnumerationKeyword` keywords enable these events and are summarized in [CLR ETW Keywords and Levels](clr-etw-keywords-and-levels.md).</span></span>  
  
## <a name="the-rundown-provider"></a><span data-ttu-id="9507b-114">Zprostředkovatel rundownu</span><span class="sxs-lookup"><span data-stu-id="9507b-114">The Rundown Provider</span></span>  
 <span data-ttu-id="9507b-115">Zprostředkovatel rundown musí být zapnutý pro určité speciální použití.</span><span class="sxs-lookup"><span data-stu-id="9507b-115">The rundown provider must be turned on for certain special-purpose uses.</span></span> <span data-ttu-id="9507b-116">Pro většinu uživatelů by však měl stačit zprostředkovatel runtime.</span><span class="sxs-lookup"><span data-stu-id="9507b-116">However, for a majority of users, the runtime provider should suffice.</span></span>  
  
 <span data-ttu-id="9507b-117">Identifikátor GUID zprostředkovatele rundown u CLR je A669021C-C450-4609-A035-5AF59AF4DF18.</span><span class="sxs-lookup"><span data-stu-id="9507b-117">The CLR rundown provider GUID is A669021C-C450-4609-A035-5AF59AF4DF18.</span></span>  
  
 <span data-ttu-id="9507b-118">Za normálních okolností protokolování ETW je povolena před spuštěním procesu a protokolování je vypnuto po ukončení procesu.</span><span class="sxs-lookup"><span data-stu-id="9507b-118">Normally, ETW logging is enabled before a process launches, and the logging is turned off after the process exits.</span></span> <span data-ttu-id="9507b-119">Pokud je však protokolování ETW zapnuto během provádění procesu, jsou potřebné další informace o procesu.</span><span class="sxs-lookup"><span data-stu-id="9507b-119">However, if ETW logging is turned on while the process is executing, additional information is needed about the process.</span></span> <span data-ttu-id="9507b-120">Například pro rozlišení symbolů je třeba protokolovat události metody pro metody, které již byly načteny před zapnutím protokolování.</span><span class="sxs-lookup"><span data-stu-id="9507b-120">For example, for symbol resolution you have to log method events for methods that were already loaded before logging was turned on.</span></span>  
  
 <span data-ttu-id="9507b-121">A `DCStart` `DCEnd` události zachycují stav procesu při spuštění a zastavení shromažďování dat.</span><span class="sxs-lookup"><span data-stu-id="9507b-121">The `DCStart` and `DCEnd` events capture the state of the process when data collection was started and stopped.</span></span> <span data-ttu-id="9507b-122">(State odkazuje na informace na vysoké úrovni, včetně metod, které byly již just-in-time (JIT) zkompilovány a sestavení, které byly načteny.) Tyto dvě události mohou poskytnout informace o tom, co se již v tomto procesu stalo; například, které metody byly zkompilovány JIT a tak dále.</span><span class="sxs-lookup"><span data-stu-id="9507b-122">(State refers to information at a high level, including the methods that were already just-in-time (JIT) compiled and assemblies that were loaded.) These two events can provide information about what has already happened in the process; for example, which methods were JIT- compiled, and so on.</span></span>  
  
 <span data-ttu-id="9507b-123">Pouze události `DC`s `DCStart` `DCEnd`, `DCInit` , , nebo v jejich názvech jsou vyvolány v rámci zprostředkovatele rundown.</span><span class="sxs-lookup"><span data-stu-id="9507b-123">Only the events with `DC`, `DCStart`, `DCEnd`, or `DCInit` in their names are raised under the rundown provider.</span></span> <span data-ttu-id="9507b-124">Kromě toho tyto události jsou vyvolány pouze v rámci zprostředkovatele rundown.</span><span class="sxs-lookup"><span data-stu-id="9507b-124">Additionally, these events are raised only under the rundown provider.</span></span>  
  
 <span data-ttu-id="9507b-125">Kromě filtrů klíčových slov události podporuje zprostředkovatel `StartRundownKeyword` `EndRundownKeyword` rundown také klíčová slova a poskytuje cílené filtrování.</span><span class="sxs-lookup"><span data-stu-id="9507b-125">In addition to the event keyword filters, the rundown provider also supports the `StartRundownKeyword` and `EndRundownKeyword` keywords to provide targeted filtering.</span></span>  
  
### <a name="start-rundown"></a><span data-ttu-id="9507b-126">Spustit rundown</span><span class="sxs-lookup"><span data-stu-id="9507b-126">Start Rundown</span></span>  
 <span data-ttu-id="9507b-127">Spuštění rundown se aktivuje při protokolování pod zprostředkovatele rundown je povoleno s klíčovým slovem. `StartRundownKeyword`</span><span class="sxs-lookup"><span data-stu-id="9507b-127">A start rundown is triggered when logging under the rundown provider is enabled with the `StartRundownKeyword` keyword.</span></span> <span data-ttu-id="9507b-128">To způsobí, že `DCStart` událost, která má být vyvolána a zachycuje stav systému.</span><span class="sxs-lookup"><span data-stu-id="9507b-128">This causes the `DCStart` event to be raised, and captures the state of the system.</span></span> <span data-ttu-id="9507b-129">Před zahájením výčtu je `DCStartInit` vyvolána událost.</span><span class="sxs-lookup"><span data-stu-id="9507b-129">Before the start of the enumeration, the `DCStartInit` event is raised.</span></span> <span data-ttu-id="9507b-130">Na konci výčtu `DCStartComplete` je vyvolána událost oznámit správce, že shromažďování dat ukončeno normálně.</span><span class="sxs-lookup"><span data-stu-id="9507b-130">At the end of the enumeration, the `DCStartComplete` event is raised to notify the controller that data collection terminated normally.</span></span>  
  
### <a name="end-rundown"></a><span data-ttu-id="9507b-131">Konec rundownu</span><span class="sxs-lookup"><span data-stu-id="9507b-131">End Rundown</span></span>  
 <span data-ttu-id="9507b-132">Konec rundown se aktivuje při protokolování pod zprostředkovatele rundown je povoleno s klíčovým slovem. `EndRundownKeyword`</span><span class="sxs-lookup"><span data-stu-id="9507b-132">An end rundown is triggered when logging under the rundown provider is enabled with the `EndRundownKeyword` keyword.</span></span> <span data-ttu-id="9507b-133">Konec rundown zastaví profilování na proces, který pokračuje v provádění.</span><span class="sxs-lookup"><span data-stu-id="9507b-133">End rundown stops profiling on a process that continues to execute.</span></span> <span data-ttu-id="9507b-134">Události `DCEnd` zachycují stav systému při zastavení profilování.</span><span class="sxs-lookup"><span data-stu-id="9507b-134">The `DCEnd` events capture the state of the system when profiling is stopped.</span></span>  
  
 <span data-ttu-id="9507b-135">Před zahájením výčtu je `DCEndInit` vyvolána událost.</span><span class="sxs-lookup"><span data-stu-id="9507b-135">Before the start of the enumeration, the `DCEndInit` event is raised.</span></span> <span data-ttu-id="9507b-136">Na konci výčtu `DCEndComplete` je vyvolána událost oznámit spotřebiteli, že shromažďování dat ukončeno normálně.</span><span class="sxs-lookup"><span data-stu-id="9507b-136">At the end of the enumeration, the `DCEndComplete` event is raised to notify the consumer that data collection terminated normally.</span></span> <span data-ttu-id="9507b-137">Spustit rundown a end rundown se používají především pro spravované rozlišení symbolů.</span><span class="sxs-lookup"><span data-stu-id="9507b-137">Start rundown and end rundown are primarily used for managed symbol resolution.</span></span> <span data-ttu-id="9507b-138">Spuštění rundown můžete poskytnout informace o rozsahu adres pro metody, které byly již JIT kompilovány před zahájením relace profilování.</span><span class="sxs-lookup"><span data-stu-id="9507b-138">Start rundown can provide address range information for methods that were already JIT-compiled before the profiling session was started.</span></span> <span data-ttu-id="9507b-139">End rundown může poskytnout informace o rozsahu adres pro všechny metody, které byly zkompilovány JIT při profilování se chystá vypnout.</span><span class="sxs-lookup"><span data-stu-id="9507b-139">End rundown can provide address range information for all methods that have been JIT-compiled when profiling is about to be turned off.</span></span>  
  
 <span data-ttu-id="9507b-140">Konec rundown se nestane automaticky při profilování relace je zastavena.</span><span class="sxs-lookup"><span data-stu-id="9507b-140">End rundown does not happen automatically when a profiling session is stopped.</span></span> <span data-ttu-id="9507b-141">Místo toho nástroj, který se snaží provést spravované rozlišení symbolu má explicitně vyvolat relaci zprostředkovatele clr s povoleným `EndRundownKeyword` klíčovým slovem, těsně před profilování je zastavena.</span><span class="sxs-lookup"><span data-stu-id="9507b-141">Instead, a tool that is seeking to perform managed symbol resolution has to explicitly invoke a CLR rundown provider session with the `EndRundownKeyword` keyword enabled, just before profiling is stopped.</span></span>  
  
 <span data-ttu-id="9507b-142">I když buď start rundown nebo end rundown může poskytnout informace o `EndRundownKeyword` rozsahu adresy metody `DCEnd` pro spravované rozlišení symbolů, doporučujeme použít klíčové slovo (které dodává události) namísto `StartRundownKeyword` klíčového slova (které poskytuje `DCStart` události).</span><span class="sxs-lookup"><span data-stu-id="9507b-142">Although either start rundown or end rundown can provide method address range information for managed symbol resolution, we recommend that you use the `EndRundownKeyword` keyword (which supplies `DCEnd` events) instead of the `StartRundownKeyword` keyword (which supplies `DCStart` events).</span></span> <span data-ttu-id="9507b-143">Použití `StartRundownKeyword` způsobí, že rundown dojít během relace profilování, které mohou narušit profilovaný scénář.</span><span class="sxs-lookup"><span data-stu-id="9507b-143">Using `StartRundownKeyword` causes the rundown to happen during the profiling session, which may disturb the profiled scenario.</span></span>  
  
## <a name="etw-data-collection-using-runtime-and-rundown-providers"></a><span data-ttu-id="9507b-144">Shromažďování dat ETW pomocí runtime a zprostředkovatelů rundownu</span><span class="sxs-lookup"><span data-stu-id="9507b-144">ETW Data Collection Using Runtime and Rundown Providers</span></span>  
 <span data-ttu-id="9507b-145">Následující příklad ukazuje, jak používat zprostředkovatele rundown CLR způsobem, který umožňuje rozlišení symbolů spravovaných procesů s minimálním dopadem, bez ohledu na to, zda procesy začínají nebo končí uvnitř nebo vně profilovaného okna.</span><span class="sxs-lookup"><span data-stu-id="9507b-145">The following example demonstrates how to use the CLR rundown provider in a way that allows symbol resolution of managed processes with minimal impact, regardless of whether the processes start or end inside or outside the profiled window.</span></span>  
  
1. <span data-ttu-id="9507b-146">Zapněte protokolování ETW pomocí zprostředkovatele runtime CLR:</span><span class="sxs-lookup"><span data-stu-id="9507b-146">Turn on ETW logging by using the CLR runtime provider:</span></span>  
  
    ```console
    xperf -start clr -on e13c0d23-ccbc-4e12-931b-d9cc2eee27e4:0x1CCBD:0x5 -f clr1.etl
    ```  
  
     <span data-ttu-id="9507b-147">Protokol bude uložen do souboru clr1.etl.</span><span class="sxs-lookup"><span data-stu-id="9507b-147">The log will be saved to the clr1.etl file.</span></span>  
  
2. <span data-ttu-id="9507b-148">Chcete-li zastavit profilování, zatímco proces pokračuje v provádění, spusťte zprostředkovatele rundown zachytit `DCEnd` události:</span><span class="sxs-lookup"><span data-stu-id="9507b-148">To stop profiling while the process continues to execute, start the rundown provider to capture the `DCEnd` events:</span></span>  
  
    ```console
    xperf -start clrRundown -on A669021C-C450-4609-A035-5AF59AF4DF18:0xB8:0x5 -f clr2.etl
    ```  
  
     <span data-ttu-id="9507b-149">To umožňuje shromažďování `DCEnd` událostí spustit relaci rundown.</span><span class="sxs-lookup"><span data-stu-id="9507b-149">This enables the collection of `DCEnd` events to start a rundown session.</span></span> <span data-ttu-id="9507b-150">Možná budete muset počkat 30 až 60 sekund pro všechny události, které mají být shromažďovány.</span><span class="sxs-lookup"><span data-stu-id="9507b-150">You may need to wait 30 to 60 seconds for all events to be collected.</span></span> <span data-ttu-id="9507b-151">Protokol bude uložen do souboru clr1.et2.</span><span class="sxs-lookup"><span data-stu-id="9507b-151">The log will be saved to the clr1.et2 file.</span></span>  
  
3. <span data-ttu-id="9507b-152">Vypněte veškeré profilování ETW:</span><span class="sxs-lookup"><span data-stu-id="9507b-152">Turn off all ETW profiling:</span></span>  
  
    ```console
    xperf -stop clrRundown
    xperf -stop clr  
    ```  
  
4. <span data-ttu-id="9507b-153">Sloučit profily a vytvořit jeden soubor protokolu:</span><span class="sxs-lookup"><span data-stu-id="9507b-153">Merge the profiles to create one log file:</span></span>  
  
    ```console
    xperf -merge clr1.etl clr2.etl merged.etl  
    ```  
  
     <span data-ttu-id="9507b-154">Soubor merged.etl bude obsahovat události z runtime a rundown zprostředkovatele relací.</span><span class="sxs-lookup"><span data-stu-id="9507b-154">The merged.etl file will contain the events from the runtime and the rundown provider sessions.</span></span>  
  
 <span data-ttu-id="9507b-155">Nástroj může provést kroky 2 a 3 (spuštění rundown ové relace a následné ukončení profilování) namísto okamžitého vypnutí profilování, když uživatel požádá o zastavení profilování.</span><span class="sxs-lookup"><span data-stu-id="9507b-155">A tool can execute steps 2 and 3 (starting a rundown session and then terminating profiling) instead of immediately turning off profiling when a user requests profiling to be stopped.</span></span> <span data-ttu-id="9507b-156">Nástroj může také provést krok 4.</span><span class="sxs-lookup"><span data-stu-id="9507b-156">A tool can also execute step 4.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="9507b-157">Viz také</span><span class="sxs-lookup"><span data-stu-id="9507b-157">See also</span></span>

- [<span data-ttu-id="9507b-158">Události Trasování událostí pro Windows v CLR (Common Language Runtime)</span><span class="sxs-lookup"><span data-stu-id="9507b-158">ETW Events in the Common Language Runtime</span></span>](etw-events-in-the-common-language-runtime.md)
