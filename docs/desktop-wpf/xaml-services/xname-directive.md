---
title: x:Name – direktiva
ms.date: 03/30/2017
f1_keywords:
- x:Name
- xName
- Name
helpviewer_keywords:
- x:Name attribute [XAML Services]
- XAML [XAML Services], x:Name attribute
- Name attribute in XAML [XAML Services]
ms.assetid: b7e61222-e8cf-48d2-acd0-6df3b7685d48
ms.openlocfilehash: 9f812a49a3217a563be1bd7f1d999b641c28463d
ms.sourcegitcommit: c2d9718996402993cf31541f11e95531bc68bad0
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 02/27/2020
ms.locfileid: "82071449"
---
# <a name="xname-directive"></a><span data-ttu-id="341d6-102">x:Name – direktiva</span><span class="sxs-lookup"><span data-stu-id="341d6-102">x:Name Directive</span></span>

<span data-ttu-id="341d6-103">Jednoznačně identifikuje prvky definované XAML v oboru názvů XAML.</span><span class="sxs-lookup"><span data-stu-id="341d6-103">Uniquely identifies XAML-defined elements in a XAML namescope.</span></span> <span data-ttu-id="341d6-104">XAML namescopes a jejich modely jedinečnosti lze použít na instanci objekty, když rozhraní poskytují rozhraní API nebo implementovat chování, které přístup k grafu objektů vytvořených XAML za běhu.</span><span class="sxs-lookup"><span data-stu-id="341d6-104">XAML namescopes and their uniqueness models can be applied to the instantiated objects, when frameworks provide APIs or implement behaviors that access the XAML-created object graph at run time.</span></span>

## <a name="xaml-attribute-usage"></a><span data-ttu-id="341d6-105">Použití atributu XAML</span><span class="sxs-lookup"><span data-stu-id="341d6-105">XAML Attribute Usage</span></span>

```xaml
<object x:Name="XAMLNameValue".../>
```

## <a name="xaml-values"></a><span data-ttu-id="341d6-106">Hodnoty XAML</span><span class="sxs-lookup"><span data-stu-id="341d6-106">XAML Values</span></span>

|||
|-|-|
|`XAMLNameValue`|<span data-ttu-id="341d6-107">Řetězec, který odpovídá [omezením gramatiky XamlName](xamlname-grammar.md).</span><span class="sxs-lookup"><span data-stu-id="341d6-107">A string that conforms to the restrictions of the [XamlName Grammar](xamlname-grammar.md).</span></span>|

## <a name="remarks"></a><span data-ttu-id="341d6-108">Poznámky</span><span class="sxs-lookup"><span data-stu-id="341d6-108">Remarks</span></span>

<span data-ttu-id="341d6-109">Po `x:Name` použití na rámec je záložní programovací model, název je ekvivalentní proměnné, která obsahuje odkaz na objekt nebo instance, jak je vrácena konstruktoru.</span><span class="sxs-lookup"><span data-stu-id="341d6-109">After `x:Name` is applied to a framework's backing programming model, the name is equivalent to the variable that holds an object reference or an instance as returned by a constructor.</span></span>

<span data-ttu-id="341d6-110">Hodnota `x:Name` použití směrnice musí být jedinečný v rámci oboru názvů XAML.</span><span class="sxs-lookup"><span data-stu-id="341d6-110">The value of an `x:Name` directive usage must be unique within a XAML namescope.</span></span> <span data-ttu-id="341d6-111">Ve výchozím nastavení při použití rozhraní .NET XAML Services API je primární název pole XAML definován v kořenovém prvku XAML jedné produkční hodu XAML a zahrnuje prvky, které jsou obsaženy v této výrobě XAML.</span><span class="sxs-lookup"><span data-stu-id="341d6-111">By default when used by .NET XAML Services API, the primary XAML namescope is defined at the XAML root element of a single XAML production, and encompasses the elements that are contained in that XAML production.</span></span> <span data-ttu-id="341d6-112">Další diskrétní názvy XAML, ke kterým může dojít v rámci jedné výroby XAML, mohou být definovány rámci pro řešení konkrétních scénářů.</span><span class="sxs-lookup"><span data-stu-id="341d6-112">Additional discrete XAML namescopes that might occur within a single XAML production can be defined by frameworks to address specific scenarios.</span></span> <span data-ttu-id="341d6-113">Například v WPF nové názvové obory XAML jsou definovány a vytvořeny libovolnou šablonou, která je také definována v této výrobě XAML.</span><span class="sxs-lookup"><span data-stu-id="341d6-113">For example, in WPF, new XAML namescopes are defined and created by any template that is also defined on that XAML production.</span></span> <span data-ttu-id="341d6-114">Další informace o názvových oborech XAML (napsaných pro WPF, ale relevantních pro mnoho konceptů oboru názvů XAML) naleznete v tématu [WPF XAML Namescopes](../../framework/wpf/advanced/wpf-xaml-namescopes.md).</span><span class="sxs-lookup"><span data-stu-id="341d6-114">For more information about XAML namescopes (written for WPF but relevant for many XAML namescope concepts), see [WPF XAML Namescopes](../../framework/wpf/advanced/wpf-xaml-namescopes.md).</span></span>

<span data-ttu-id="341d6-115">Obecně by `x:Name` se neměly používat v `x:Key`situacích, které se používají také .</span><span class="sxs-lookup"><span data-stu-id="341d6-115">In general, `x:Name` should not be applied in situations that also use `x:Key`.</span></span> <span data-ttu-id="341d6-116">Implementace XAML podle konkrétních existujících rámců `x:Key` zavedly koncepty substituce mezi písmeny a) a `x:Name`b), ale to není doporučený postup.</span><span class="sxs-lookup"><span data-stu-id="341d6-116">XAML implementations by specific existing frameworks have introduced substitution concepts between `x:Key` and `x:Name`, but that is not a recommended practice.</span></span> <span data-ttu-id="341d6-117">Služba .NET XAML Services nepodporuje tyto koncepty nahrazení <xref:System.Windows.Markup.INameScope> při <xref:System.Windows.Markup.DictionaryKeyPropertyAttribute>zpracování informací o názvu/klíči, jako je například nebo .</span><span class="sxs-lookup"><span data-stu-id="341d6-117">.NET XAML Services does not support such substitution concepts when handling name/key information such as <xref:System.Windows.Markup.INameScope> or <xref:System.Windows.Markup.DictionaryKeyPropertyAttribute>.</span></span>

<span data-ttu-id="341d6-118">Pravidla pro `x:Name` povolení, jakož i prosazování jedinečnosti názvu jsou potenciálně definována konkrétními prováděcími rámci.</span><span class="sxs-lookup"><span data-stu-id="341d6-118">Rules for permittance of `x:Name` as well as the name uniqueness enforcement are potentially defined by specific implementing frameworks.</span></span> <span data-ttu-id="341d6-119">Aby však byly použitelné se službami .NET XAML Services, definice architektury jedinečnosti oboru názvů <xref:System.Windows.Markup.INameScope> XAML by měly být v souladu s definicí informací v této dokumentaci a měly by používat stejná pravidla týkající se použití informací.</span><span class="sxs-lookup"><span data-stu-id="341d6-119">However, to be usable with .NET XAML Services, the framework definitions of XAML namescope uniqueness should be consistent with the definition of <xref:System.Windows.Markup.INameScope> information in this documentation, and should use the same rules regarding where the information is applied.</span></span> <span data-ttu-id="341d6-120">[!INCLUDE[TLA#tla_winclient](../../../includes/tlasharptla-winclient-md.md)] Implementace například rozděluje různé prvky značek <xref:System.Windows.NameScope> do samostatných oblastí, jako jsou slovníky prostředků, logický strom vytvořený xaml na úrovni stránky, šablony a další odložený obsah, a pak vynucuje jedinečnost názvu XAML v rámci každého z těchto oborů názvů XAML.</span><span class="sxs-lookup"><span data-stu-id="341d6-120">For example, the [!INCLUDE[TLA#tla_winclient](../../../includes/tlasharptla-winclient-md.md)] implementation divides various markup elements into separate <xref:System.Windows.NameScope> ranges, such as resource dictionaries, the logical tree created by the page-level XAML, templates, and other deferred content, and then enforces XAML name uniqueness within each of those XAML namescopes.</span></span>

<span data-ttu-id="341d6-121">Pro vlastní typy, které používají zapisovače objektů XAML `x:Name` služby .NET XAML, lze vytvořit nebo změnit vlastnost, která se mapuje na typ.</span><span class="sxs-lookup"><span data-stu-id="341d6-121">For custom types that use .NET XAML Services XAML object writers, a property that maps to `x:Name` on a type can be established or changed.</span></span> <span data-ttu-id="341d6-122">Toto chování definujete odkazem na název vlastnosti mapovat s <xref:System.Windows.Markup.RuntimeNamePropertyAttribute> v kódu definice typu.</span><span class="sxs-lookup"><span data-stu-id="341d6-122">You define this behavior by referencing the name of the property to map with the <xref:System.Windows.Markup.RuntimeNamePropertyAttribute> in the type definition code.</span></span>  <span data-ttu-id="341d6-123"><xref:System.Windows.Markup.RuntimeNamePropertyAttribute>je atribut na úrovni typu.</span><span class="sxs-lookup"><span data-stu-id="341d6-123"><xref:System.Windows.Markup.RuntimeNamePropertyAttribute> is a type-level attribute.</span></span>

<span data-ttu-id="341d6-124">Using.NET služby XAML lze logiku zálohování pro podporu název oboru XAML definovat <xref:System.Windows.Markup.INameScope> způsobem neutrálním v rámci implementací rozhraní.</span><span class="sxs-lookup"><span data-stu-id="341d6-124">Using.NET XAML Services, the backing logic for XAML namescope support can be defined in a framework-neutral way by implementing the <xref:System.Windows.Markup.INameScope> interface.</span></span>

## <a name="wpf-usage-notes"></a><span data-ttu-id="341d6-125">Poznámky k použití WPF</span><span class="sxs-lookup"><span data-stu-id="341d6-125">WPF Usage Notes</span></span>

<span data-ttu-id="341d6-126">Pod standardní konfigurace sestavení [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] pro aplikaci, která používá XAML, částečné `x:Name` třídy a kód na pozadí, zadaný stane název pole, které je vytvořeno v podkladovém kódu, když [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] je zpracována úkol sestavení kompilace značek a toto pole obsahuje odkaz na objekt.</span><span class="sxs-lookup"><span data-stu-id="341d6-126">Under the standard build configuration for a [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] application that uses XAML, partial classes, and code-behind, the specified `x:Name` becomes the name of a field that is created in the underlying code when [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] is processed by a markup compilation build task, and that field holds a reference to the object.</span></span> <span data-ttu-id="341d6-127">Ve výchozím nastavení je vytvořené pole interní.</span><span class="sxs-lookup"><span data-stu-id="341d6-127">By default, the created field is internal.</span></span> <span data-ttu-id="341d6-128">Přístup k polím můžete změnit zadáním [atributu x:FieldModifier](xfieldmodifier-directive.md).</span><span class="sxs-lookup"><span data-stu-id="341d6-128">You can change field access by specifying the [x:FieldModifier attribute](xfieldmodifier-directive.md).</span></span> <span data-ttu-id="341d6-129">V WPF a Silverlight pořadí je, že kompilace značek definuje a názvy pole v částečné třídě, ale hodnota je zpočátku prázdný.</span><span class="sxs-lookup"><span data-stu-id="341d6-129">In WPF and Silverlight, the sequence is that the markup compile defines and names the field in a partial class, but the value is initially empty.</span></span> <span data-ttu-id="341d6-130">Potom generované metody s `InitializeComponent` názvem je volána z v rámci konstruktoru třídy.</span><span class="sxs-lookup"><span data-stu-id="341d6-130">Then, a generated method named `InitializeComponent` is called from within the class constructor.</span></span> <span data-ttu-id="341d6-131">`InitializeComponent`se skládá `FindName` z volání `x:Name` pomocí každé z hodnot, které existují v XAML definované části částečné třídy jako vstupní řetězce.</span><span class="sxs-lookup"><span data-stu-id="341d6-131">`InitializeComponent` consists of `FindName` calls using each of the `x:Name` values that exist in the XAML-defined part of the partial class as input strings.</span></span> <span data-ttu-id="341d6-132">Vrácené hodnoty jsou pak přiřazeny k odkazu na pole s označením like, aby vyplnily hodnoty polí objekty, které byly vytvořeny z analýzy XAML.</span><span class="sxs-lookup"><span data-stu-id="341d6-132">The return values are then assigned to the like-named field reference to fill the field values with objects that were created from XAML parsing.</span></span> <span data-ttu-id="341d6-133">Spuštění umožňují `InitializeComponent` odkazovat na graf objektu za `x:Name` běhu pomocí názvu pole / `FindName` přímo, spíše než muset explicitně volat kdykoli budete potřebovat odkaz na objekt definovaný xaml.</span><span class="sxs-lookup"><span data-stu-id="341d6-133">The execution of `InitializeComponent` make it possible to reference the run time object graph using the `x:Name` / field name directly, rather than having to call `FindName` explicitly any time you need a reference to a XAML-defined object.</span></span>

<span data-ttu-id="341d6-134">Pro aplikaci WPF, která používá cíle jazyka Microsoft `Page` Visual Basic a obsahuje soubory XAML `WithEvents` s akcí sestavení, `x:Name`je během `Handles` kompilace vytvořena samostatná vlastnost odkazu, která přidá klíčové slovo ke všem prvkům, které mají , pro podporu syntaxe pro delegáty obslužné rutiny události.</span><span class="sxs-lookup"><span data-stu-id="341d6-134">For a WPF application that uses the Microsoft Visual Basic targets and includes XAML files with `Page` build action, a separate reference property is created during compilation that adds the `WithEvents` keyword to all elements that have an `x:Name`, to support `Handles` syntax for event handler delegates.</span></span> <span data-ttu-id="341d6-135">Tato vlastnost je vždy veřejná.</span><span class="sxs-lookup"><span data-stu-id="341d6-135">This property is always public.</span></span> <span data-ttu-id="341d6-136">Další informace naleznete [v tématu Visual Basic a WPF Event Handling](../../framework/wpf/advanced/visual-basic-and-wpf-event-handling.md).</span><span class="sxs-lookup"><span data-stu-id="341d6-136">For more information, see [Visual Basic and WPF Event Handling](../../framework/wpf/advanced/visual-basic-and-wpf-event-handling.md).</span></span>

<span data-ttu-id="341d6-137">`x:Name`používá wpf XAML procesor zaregistrovat název do oboru názvů XAML v době načítání, a to i v případech, kdy stránka není zkompilována značky pomocí akce sestavení (například volné XAML slovníku prostředků).</span><span class="sxs-lookup"><span data-stu-id="341d6-137">`x:Name` is used by the WPF XAML processor to register a name into a XAML namescope at load time, even for cases where the page is not markup-compiled by build actions (for example, loose XAML of a resource dictionary).</span></span> <span data-ttu-id="341d6-138">Jedním z důvodů pro `x:Name` toto chování <xref:System.Windows.Data.Binding.ElementName%2A> je, protože je potenciálně potřebné pro vazbu.</span><span class="sxs-lookup"><span data-stu-id="341d6-138">One reason for this behavior is because the `x:Name` is potentially needed for <xref:System.Windows.Data.Binding.ElementName%2A> binding.</span></span> <span data-ttu-id="341d6-139">Podrobnosti naleznete v [tématu Přehled datových vazeb](../data/data-binding-overview.md).</span><span class="sxs-lookup"><span data-stu-id="341d6-139">For details, see [Data Binding Overview](../data/data-binding-overview.md).</span></span>

<span data-ttu-id="341d6-140">Jak již bylo `x:Name` zmíněno dříve, (nebo `Name`) `x:Key`by neměly být používány v situacích, které také používají .</span><span class="sxs-lookup"><span data-stu-id="341d6-140">As mentioned previously, `x:Name` (or `Name`) should not be applied in situations that also use `x:Key`.</span></span> <span data-ttu-id="341d6-141">Má [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] <xref:System.Windows.ResourceDictionary> zvláštní chování definování sebe jako název oboru XAML, ale vrací <xref:System.Windows.Markup.INameScope> není implementována nebo null hodnoty pro api jako způsob, jak vynutit toto chování.</span><span class="sxs-lookup"><span data-stu-id="341d6-141">The [!INCLUDE[TLA2#tla_winclient](../../../includes/tla2sharptla-winclient-md.md)] <xref:System.Windows.ResourceDictionary> has a special behavior of defining itself as a XAML namescope but returning Not Implemented or null values for <xref:System.Windows.Markup.INameScope> APIs as a way to enforce this behavior.</span></span> <span data-ttu-id="341d6-142">Pokud se analyzátor WPF XAML setká `Name` nebo `x:Name` je <xref:System.Windows.ResourceDictionary>definován xaml , název není přidán do žádného oboru názvů XAML.</span><span class="sxs-lookup"><span data-stu-id="341d6-142">If the WPF XAML parser encounters `Name` or `x:Name` in a XAML-defined <xref:System.Windows.ResourceDictionary>, the name is not added to any XAML namescope.</span></span> <span data-ttu-id="341d6-143">Pokus o nalezení tohoto názvu z libovolného `FindName` oboru názvů XAML a metody nevrátí platné výsledky.</span><span class="sxs-lookup"><span data-stu-id="341d6-143">Attempting to find that name from any XAML namescope and the `FindName` methods will not return valid results.</span></span>

### <a name="xname-and-name"></a><span data-ttu-id="341d6-144">x:Jméno a název</span><span class="sxs-lookup"><span data-stu-id="341d6-144">x:Name and Name</span></span>

<span data-ttu-id="341d6-145">Mnoho scénářů aplikace WPF můžete `x:Name` vyhnout jakékoli `Name` použití atributu, protože vlastnost závislosti, jak je uvedeno ve <xref:System.Windows.FrameworkElement> výchozím <xref:System.Windows.FrameworkContentElement> oboru názvů XAML pro několik důležitých základních tříd, jako je například a splňuje stejný účel.</span><span class="sxs-lookup"><span data-stu-id="341d6-145">Many WPF application scenarios can avoid any use of the `x:Name` attribute, because the `Name` dependency property as specified in the default XAML namespace for several of the important base classes such as <xref:System.Windows.FrameworkElement> and <xref:System.Windows.FrameworkContentElement> satisfies this same purpose.</span></span> <span data-ttu-id="341d6-146">Stále existují některé běžné scénáře XAML a WPF, `Name` kde je důležitý přístup kódu k elementu bez vlastnosti na úrovni rozhraní.</span><span class="sxs-lookup"><span data-stu-id="341d6-146">There are still some common XAML and WPF scenarios where code access to an element with no `Name` property at the framework level is important.</span></span> <span data-ttu-id="341d6-147">Například některé třídy podpory animace a `Name` scénáře nepodporují vlastnost, ale často je třeba odkazovat v kódu, aby bylo možné řídit animaci.</span><span class="sxs-lookup"><span data-stu-id="341d6-147">For example, certain animation and storyboard support classes do not support a `Name` property, but they often need to be referenced in code in order to control the animation.</span></span> <span data-ttu-id="341d6-148">Měli byste `x:Name` zadat jako atribut na časové osy a transformace, které jsou vytvořeny v XAML, pokud máte v úmyslu odkazovat je z kódu později.</span><span class="sxs-lookup"><span data-stu-id="341d6-148">You should specify `x:Name` as an attribute on timelines and transforms that are created in XAML, if you intend to reference them from code later.</span></span>

<span data-ttu-id="341d6-149">Pokud <xref:System.Windows.FrameworkElement.Name%2A> je k dispozici jako <xref:System.Windows.FrameworkElement.Name%2A> vlastnost `x:Name` na třídu a lze použít zaměnitelně jako atributy, ale výjimka analýzy bude mít za následek, pokud jsou zadány na stejný prvek.</span><span class="sxs-lookup"><span data-stu-id="341d6-149">If <xref:System.Windows.FrameworkElement.Name%2A> is available as a property on the class, <xref:System.Windows.FrameworkElement.Name%2A> and `x:Name` can be used interchangeably as attributes, but a parse exception will result if both are specified on the same element.</span></span> <span data-ttu-id="341d6-150">Pokud xaml je zkompilován značky, dojde k výjimce na značky kompilace, jinak k němu dojde při zatížení.</span><span class="sxs-lookup"><span data-stu-id="341d6-150">If the XAML is markup compiled, the exception will occur on the markup compile, otherwise it occurs on load.</span></span>

<span data-ttu-id="341d6-151"><xref:System.Windows.FrameworkElement.Name%2A>lze nastavit pomocí syntaxe atributu XAML a v kódu pomocí <xref:System.Windows.DependencyObject.SetValue%2A>; Všimněte si <xref:System.Windows.FrameworkElement.Name%2A> však, že nastavení vlastnosti v kódu nevytvoří reprezentativní odkaz na pole v rámci oboru názvů XAML ve většině okolností, kdy je xaml již načten.</span><span class="sxs-lookup"><span data-stu-id="341d6-151"><xref:System.Windows.FrameworkElement.Name%2A> can be set using XAML attribute syntax, and in code using <xref:System.Windows.DependencyObject.SetValue%2A>; note however that setting the <xref:System.Windows.FrameworkElement.Name%2A> property in code does not create the representative field reference within the XAML namescope in most circumstances where the XAML is already loaded.</span></span> <span data-ttu-id="341d6-152">Namísto pokusu <xref:System.Windows.FrameworkElement.Name%2A> o nastavení <xref:System.Windows.NameScope> v kódu, použijte metody z kódu, proti příslušné namescope.</span><span class="sxs-lookup"><span data-stu-id="341d6-152">Instead of attempting to set <xref:System.Windows.FrameworkElement.Name%2A> in code, use <xref:System.Windows.NameScope> methods from code, against the appropriate namescope.</span></span>

<span data-ttu-id="341d6-153"><xref:System.Windows.FrameworkElement.Name%2A>lze také nastavit pomocí syntaxe prvku vlastnosti s vnitřním textem, ale to je neobvyklé.</span><span class="sxs-lookup"><span data-stu-id="341d6-153"><xref:System.Windows.FrameworkElement.Name%2A> can also be set using property element syntax with inner text, but that is uncommon.</span></span> <span data-ttu-id="341d6-154">Naproti tomu `x:Name` nelze nastavit v syntaxi elementu vlastnosti <xref:System.Windows.DependencyObject.SetValue%2A>XAML nebo v kódu pomocí ; lze nastavit pouze pomocí syntaxe atributu na objektech, protože se jedná o direktivu.</span><span class="sxs-lookup"><span data-stu-id="341d6-154">In contrast, `x:Name` cannot be set in XAML property element syntax, or in code using <xref:System.Windows.DependencyObject.SetValue%2A>; it can only be set using attribute syntax on objects because it is a directive.</span></span>

## <a name="silverlight-usage-notes"></a><span data-ttu-id="341d6-155">Poznámky k použití aplikace Silverlight</span><span class="sxs-lookup"><span data-stu-id="341d6-155">Silverlight Usage Notes</span></span>

<span data-ttu-id="341d6-156">`x:Name`pro Program Silverlight je dokumentován samostatně.</span><span class="sxs-lookup"><span data-stu-id="341d6-156">`x:Name` for Silverlight is documented separately.</span></span> <span data-ttu-id="341d6-157">Další informace naleznete v [tématu XAML Namespace (x:) Jazykové funkce (Silverlight)](https://docs.microsoft.com/previous-versions/windows/silverlight/dotnet-windows-silverlight/cc188995(v=vs.95)).</span><span class="sxs-lookup"><span data-stu-id="341d6-157">For more information, see [XAML Namespace (x:) Language Features (Silverlight)](https://docs.microsoft.com/previous-versions/windows/silverlight/dotnet-windows-silverlight/cc188995(v=vs.95)).</span></span>

## <a name="see-also"></a><span data-ttu-id="341d6-158">Viz také</span><span class="sxs-lookup"><span data-stu-id="341d6-158">See also</span></span>

- <xref:System.Windows.FrameworkElement.Name%2A?displayProperty=nameWithType>
- <xref:System.Windows.FrameworkContentElement.Name%2A?displayProperty=nameWithType>
- [<span data-ttu-id="341d6-159">Stromy v subsystému WPF</span><span class="sxs-lookup"><span data-stu-id="341d6-159">Trees in WPF</span></span>](../../framework/wpf/advanced/trees-in-wpf.md)
