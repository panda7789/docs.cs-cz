---
title: Obory názvů XAML pro služby .NET XAML
ms.date: 03/30/2017
ms.assetid: e4f15f13-c420-4c1e-aeab-9b6f50212047
ms.openlocfilehash: 2ae57d08fade85c59fea2a54b653a2f775b57415
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/12/2020
ms.locfileid: "82071841"
---
# <a name="xaml-namespaces-for-net-xaml-services"></a><span data-ttu-id="c3367-102">Obory názvů XAML pro služby .NET XAML</span><span class="sxs-lookup"><span data-stu-id="c3367-102">XAML Namespaces for .NET XAML Services</span></span>
<span data-ttu-id="c3367-103">Obor názvů XAML je koncept, který rozšiřuje definici oboru názvů XML.</span><span class="sxs-lookup"><span data-stu-id="c3367-103">A XAML namespace is a concept that expands on the definition of an XML namespace.</span></span> <span data-ttu-id="c3367-104">Podobně jako v oboru názvů XML můžete definovat obor `xmlns` názvů XAML pomocí atributu ve značkách.</span><span class="sxs-lookup"><span data-stu-id="c3367-104">Similar to an XML namespace, you can define a XAML namespace using an `xmlns` attribute in markup.</span></span> <span data-ttu-id="c3367-105">Obory názvů XAML jsou také reprezentovány v datovém proudu uzlů XAML a dalších api služeb XAML.</span><span class="sxs-lookup"><span data-stu-id="c3367-105">XAML namespaces are also represented in the XAML node stream and other XAML Services APIs.</span></span> <span data-ttu-id="c3367-106">Toto téma definuje koncept oboru názvů XAML a popisuje, jak lze definovat obory názvů XAML a jak je používají kontexty schématu XAML a další aspekty služby .NET XAML Services.</span><span class="sxs-lookup"><span data-stu-id="c3367-106">This topic defines the XAML namespace concept, and describes how XAML namespaces can be defined and are used by XAML schema contexts and other aspects of .NET XAML Services.</span></span>  
  
## <a name="xml-namespace-and-xaml-namespace"></a><span data-ttu-id="c3367-107">Obor názvů XML a Obor názvů XAML</span><span class="sxs-lookup"><span data-stu-id="c3367-107">XML Namespace and XAML Namespace</span></span>  
 <span data-ttu-id="c3367-108">Obor názvů XAML je specializovaný obor názvů XML, stejně jako XAML je specializovaná forma xml a používá základní formulář XML pro své značky.</span><span class="sxs-lookup"><span data-stu-id="c3367-108">A XAML namespace is a specialized XML namespace, just as XAML is a specialized form of XML and uses the basic XML form for its markup.</span></span> <span data-ttu-id="c3367-109">Ve značkování deklarujete obor názvů XAML a jeho mapování pomocí atributu `xmlns` použitého na prvek.</span><span class="sxs-lookup"><span data-stu-id="c3367-109">In markup, you declare a XAML namespace and its mapping through an `xmlns` attribute applied to an element.</span></span> <span data-ttu-id="c3367-110">Deklarace `xmlns` může být provedena na stejný prvek, který xaml obor názvů je deklarován v.</span><span class="sxs-lookup"><span data-stu-id="c3367-110">The `xmlns` declaration can be made to the same element that the XAML namespace is declared in.</span></span> <span data-ttu-id="c3367-111">Deklarace oboru názvů XAML provedená prvku je platná pro tento prvek, všechny atributy tohoto prvku a všechny podřízené objekty tohoto prvku.</span><span class="sxs-lookup"><span data-stu-id="c3367-111">A XAML namespace declaration made to an element is valid for that element, all attributes of that element, and all children of that element.</span></span> <span data-ttu-id="c3367-112">Atributy mohou používat obor názvů XAML, který není stejný jako prvek, který obsahuje atribut, pokud samotný název atributu odkazuje na předponu jako součást názvu atributu ve značkách.</span><span class="sxs-lookup"><span data-stu-id="c3367-112">Attributes can use a XAML namespace that is not the same as the element that contains the attribute, so long as the attribute name itself references the prefix as part of its attribute name in markup.</span></span>  
  
 <span data-ttu-id="c3367-113">Obor názvů XAML a obor názvů XML je rozdíl, že obor názvů XML může být použit k odkazování na schéma nebo jednoduše k rozlišení entit.</span><span class="sxs-lookup"><span data-stu-id="c3367-113">The distinction of a XAML namespace versus an XML namespace is that an XML namespace might be used to reference a schema or simply to differentiate entities.</span></span> <span data-ttu-id="c3367-114">Pro XAML typy a členy používané v XAML musí být nakonec vyřešeny na záložní typy a koncepty schématu XML se na tuto schopnost nevztahují dobře.</span><span class="sxs-lookup"><span data-stu-id="c3367-114">For XAML, the types and members as used in XAML must ultimately be resolved to backing types, and XML schema concepts do not apply well to this capability.</span></span> <span data-ttu-id="c3367-115">Obor názvů XAML obsahuje informace, které musí mít k dispozici kontext schématu XAML, aby bylo možné provést toto mapování typů.</span><span class="sxs-lookup"><span data-stu-id="c3367-115">The XAML namespace contains information that the XAML schema context must have available in order to perform this type mapping.</span></span>  
  
## <a name="xaml-namespace-components"></a><span data-ttu-id="c3367-116">Součásti oboru názvů XAML</span><span class="sxs-lookup"><span data-stu-id="c3367-116">XAML Namespace Components</span></span>  
 <span data-ttu-id="c3367-117">Definice oboru názvů XAML má dvě součásti: předponu a identifikátor.</span><span class="sxs-lookup"><span data-stu-id="c3367-117">The XAML namespace definition has two components: a prefix, and an identifier.</span></span> <span data-ttu-id="c3367-118">Každá z těchto součástí je k dispozici, když je ve značkách deklarován obor názvů XAML nebo definován v systému typu XAML.</span><span class="sxs-lookup"><span data-stu-id="c3367-118">Each of these components is present when a XAML namespace is declared in markup, or defined in the XAML type system.</span></span>  
  
 <span data-ttu-id="c3367-119">Předpona může být libovolný řetězec, jak je povoleno [W3C Namespaces ve specifikaci XML 1.0](https://www.w3.org/TR/REC-xml-names/).</span><span class="sxs-lookup"><span data-stu-id="c3367-119">The prefix can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/).</span></span> <span data-ttu-id="c3367-120">Podle konvence jsou předpony obvykle krátké řetězce, protože předpona se opakuje mnohokrát v typickém souboru značek.</span><span class="sxs-lookup"><span data-stu-id="c3367-120">By convention, the prefixes are typically short strings, because the prefix is repeated many times in a typical markup file.</span></span> <span data-ttu-id="c3367-121">Některé obory názvů XAML, které jsou určeny k použití ve více implementacích XAML, používají konkrétní konvenční předpony.</span><span class="sxs-lookup"><span data-stu-id="c3367-121">Certain XAML namespaces that are intended to be used in multiple XAML implementations use particular conventional prefixes.</span></span> <span data-ttu-id="c3367-122">Například obor názvů XAML jazyka XAML je obvykle mapován pomocí předpony `x`.</span><span class="sxs-lookup"><span data-stu-id="c3367-122">For example, the XAML language XAML namespace is typically mapped using the prefix `x`.</span></span> <span data-ttu-id="c3367-123">Můžete definovat výchozí obor názvů XAML, kde předpona není uvedena v definici, ale je reprezentována jako prázdný řetězec, pokud je definován nebo dotazován by.NET rozhraní API služby XAML.</span><span class="sxs-lookup"><span data-stu-id="c3367-123">You can define a default XAML namespace, where the prefix is not given in the definition but is represented as an empty string if defined or queried by.NET XAML Services API.</span></span> <span data-ttu-id="c3367-124">Výchozí obor názvů XAML je obvykle záměrně vybrán, aby se podpořilo maximální množství značek, které vynechují předponu, technologií implementující XAML a jejími scénáři a slovníky.</span><span class="sxs-lookup"><span data-stu-id="c3367-124">Typically, the default XAML namespace is deliberately chosen in order to promote a maximized amount of prefix-omitting markup by a XAML-implementing technology and its scenarios and vocabularies.</span></span>  
  
 <span data-ttu-id="c3367-125">Identifikátor může být libovolný řetězec, jak je povoleno [W3C Namespaces ve specifikaci XML 1.0](https://www.w3.org/TR/REC-xml-names/).</span><span class="sxs-lookup"><span data-stu-id="c3367-125">The identifier can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/).</span></span> <span data-ttu-id="c3367-126">Podle konvence jsou identifikátory pro obory názvů XML nebo obory názvů XAML často uvedeny ve formuláři URI, obvykle jako absolutní identifikátor URI s kvalifikací protokolu.</span><span class="sxs-lookup"><span data-stu-id="c3367-126">By convention, identifiers for either XML namespaces or XAML namespaces are often given in URI form, typically as a protocol-qualified absolute URI.</span></span> <span data-ttu-id="c3367-127">Informace o verzi, která definuje konkrétní slovník XAML je implicitní jako součást řetězce cesty.</span><span class="sxs-lookup"><span data-stu-id="c3367-127">Often, version information that defines a particular XAML vocabulary is implied as part of the path string.</span></span> <span data-ttu-id="c3367-128">Obory názvů XAML přidávají další konvenci identifikátorů nad rámec konvence IDENTIFIKÁTOR XML.</span><span class="sxs-lookup"><span data-stu-id="c3367-128">XAML namespaces add an additional identifier convention beyond the XML URI convention.</span></span> <span data-ttu-id="c3367-129">Pro obory názvů XAML identifikátor sděluje informace, které je potřeba kontext schématu XAML k vyřešení typy, které jsou určeny jako prvky v rámci tohoto oboru názvů XAML nebo přeložit atributy členům.</span><span class="sxs-lookup"><span data-stu-id="c3367-129">For XAML namespaces, the identifier communicates information that is needed by a XAML schema context in order to resolve the types that are specified as elements under that XAML namespace, or to resolve attributes to members.</span></span>  
  
 <span data-ttu-id="c3367-130">Pro účely sdělování informací kontextu schématu XAML může být identifikátor oboru názvů XAML stále ve formě identifikátoru URI.</span><span class="sxs-lookup"><span data-stu-id="c3367-130">For purposes of communicating information to a XAML schema context, the identifier for a XAML namespace might still be in URI form.</span></span> <span data-ttu-id="c3367-131">V tomto případě je však identifikátor URI také deklarován jako odpovídající identifikátor v určitém sestavení nebo seznamu sestavení.</span><span class="sxs-lookup"><span data-stu-id="c3367-131">However, in this case the URI is also declared as a matching identifier in a particular assembly or list of assemblies.</span></span> <span data-ttu-id="c3367-132">To se provádí v sestaveních přiřazením sestavení s <xref:System.Windows.Markup.XmlnsDefinitionAttribute>.</span><span class="sxs-lookup"><span data-stu-id="c3367-132">This is done in assemblies by attributing the assembly with <xref:System.Windows.Markup.XmlnsDefinitionAttribute>.</span></span> <span data-ttu-id="c3367-133">Tato metoda identifikace oboru názvů XAML a podpora chování překladu typu založeného na CLR v přiděleném sestavení je podporována výchozím kontextem schématu XAML ve službě .NET XAML Services.</span><span class="sxs-lookup"><span data-stu-id="c3367-133">This method of identifying the XAML namespace and supporting a CLR-based type resolution behavior in the attributed assembly is supported by the default XAML schema context in .NET XAML Services.</span></span> <span data-ttu-id="c3367-134">Obecněji lze tuto konvenci použít pro případy, kdy kontext schématu XAML zahrnuje clr nebo je založen na výchozí kontext schématu XAML, který je nezbytný pro čtení atributů CLR ze sestavení CLR.</span><span class="sxs-lookup"><span data-stu-id="c3367-134">More generally, this convention can be used for cases where the XAML schema context incorporates the CLR or is based on the default XAML schema context, which is necessary in order to read CLR attributes from CLR assemblies.</span></span>  
  
 <span data-ttu-id="c3367-135">Obory názvů XAML lze také identifikovat podle konvence, která komunikuje obor názvů CLR a sestavení definující typ.</span><span class="sxs-lookup"><span data-stu-id="c3367-135">XAML namespaces also can be identified by a convention that communicates a CLR namespace and a type-defining assembly.</span></span> <span data-ttu-id="c3367-136">Tato konvence se používá <xref:System.Windows.Markup.XmlnsDefinitionAttribute> v případech, kdy neexistuje žádné přiřazení v sestaveních, které obsahují typy.</span><span class="sxs-lookup"><span data-stu-id="c3367-136">This convention is used in cases where no <xref:System.Windows.Markup.XmlnsDefinitionAttribute> attribution exists in the assemblies that contain types.</span></span> <span data-ttu-id="c3367-137">Tato konvence je potenciálně složitější než konvence URI a má také potenciál pro nejednoznačnost a duplikaci, protože existuje několik způsobů, jak odkazovat na sestavení.</span><span class="sxs-lookup"><span data-stu-id="c3367-137">This convention is potentially more complex than the URI convention, and also has the potential for ambiguity and duplication, because there are multiple ways of referring to an assembly.</span></span>  
  
 <span data-ttu-id="c3367-138">Nejzákladnější forma identifikátoru, který používá obor názvů CLR a konvence sestavení, je následující:</span><span class="sxs-lookup"><span data-stu-id="c3367-138">The most basic form of an identifier that uses the CLR namespace and assembly convention is as follows:</span></span>  
  
 `clr-namespace:clrnsName; assembly=assemblyShortName`
  
 <span data-ttu-id="c3367-139">`clr-namespace:`a `; assembly=` jsou doslovné součásti syntaxe.</span><span class="sxs-lookup"><span data-stu-id="c3367-139">`clr-namespace:` and `; assembly=` are literal components of the syntax.</span></span>  
  
 <span data-ttu-id="c3367-140">*clrnsName* je název řetězce, který identifikuje obor názvů CLR.</span><span class="sxs-lookup"><span data-stu-id="c3367-140">*clrnsName* is the string name that identifies a CLR namespace.</span></span> <span data-ttu-id="c3367-141">Tento název řetězce obsahuje všechny vnitřní tečky znaky (.), které poskytují rady o oboru názvů CLR a jeho vztah k jiným oborům názvů CLR.</span><span class="sxs-lookup"><span data-stu-id="c3367-141">This string name includes any internal dot characters (.) that provide hints about the CLR namespace and its relation to other CLR namespaces.</span></span>
  
 <span data-ttu-id="c3367-142">*assemblyShortName* je název řetězce sestavení, který definuje typy, které jsou užitečné v XAML.</span><span class="sxs-lookup"><span data-stu-id="c3367-142">*assemblyShortName* is the string name of an assembly that defines types that are useful in XAML.</span></span> <span data-ttu-id="c3367-143">Očekává se, že typy, ke kterým bude přístup n. c) prostřednictvím deklarovaného oboru názvů XAML, budou definovány sestavením a budou deklarovány v rámci oboru názvů CLR určeného *clrnsName*.</span><span class="sxs-lookup"><span data-stu-id="c3367-143">The types to be accessed through the declared XAML namespace are expected to be defined by the assembly and to be declared within the CLR namespace specified by *clrnsName*.</span></span> <span data-ttu-id="c3367-144">Tento název řetězce obvykle paralely informace, jak je uvedeno . <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType></span><span class="sxs-lookup"><span data-stu-id="c3367-144">This string name typically parallels the information as reported by <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="c3367-145">Úplnější definice oboru názvů CLR a konvence sestavení je následující:</span><span class="sxs-lookup"><span data-stu-id="c3367-145">A more complete definition of the CLR namespace and assembly convention is as follows:</span></span>  
  
 `clr-namespace:clrnsName; assembly=assemblyName`
  
 <span data-ttu-id="c3367-146">*assemblyName* představuje libovolný řetězec, <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> který je legální jako vstup.</span><span class="sxs-lookup"><span data-stu-id="c3367-146">*assemblyName* represents any string that is legal as an <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> input.</span></span> <span data-ttu-id="c3367-147">Tento řetězec může obsahovat informace o jazykové verzi, veřejném klíči nebo verzi <xref:System.Reflection.Assembly>(definice těchto konceptů jsou definovány v referenčním tématu pro).</span><span class="sxs-lookup"><span data-stu-id="c3367-147">This string can include culture, public key, or version information (definitions of these concepts are defined in the reference topic for <xref:System.Reflection.Assembly>).</span></span> <span data-ttu-id="c3367-148">Formát COFF a důkazy (používané jinými přetíženími) <xref:System.Reflection.Assembly.Load%2A>nejsou relevantní pro účely zatížení sestavy XAML; všechny informace o zatížení musí být prezentovány jako řetězec.</span><span class="sxs-lookup"><span data-stu-id="c3367-148">COFF format and evidence (as used by other overloads of <xref:System.Reflection.Assembly.Load%2A>) are not relevant for XAML assembly loading purposes; all load information must be presented as a string.</span></span>  
  
 <span data-ttu-id="c3367-149">Určení veřejného klíče pro sestavení je užitečná technika pro zabezpečení XAML nebo pro odstranění možné nejednoznačnosti, která může existovat, pokud jsou sestavení načtena jednoduchým názvem nebo existují předem v mezipaměti nebo doméně aplikace.</span><span class="sxs-lookup"><span data-stu-id="c3367-149">Specifying a public key for the assembly is a useful technique for XAML security, or for removing possible ambiguity that can exist if assemblies are loaded by simple name, or pre-exist in a cache or application domain.</span></span> <span data-ttu-id="c3367-150">Další informace naleznete v [tématu Aspekty zabezpečení XAML](security-considerations.md).</span><span class="sxs-lookup"><span data-stu-id="c3367-150">For more information, see [XAML Security Considerations](security-considerations.md).</span></span>  
  
## <a name="xaml-namespace-declarations-in-the-xaml-services-api"></a><span data-ttu-id="c3367-151">Deklarace oboru názvů XAML v rozhraní API služby XAML</span><span class="sxs-lookup"><span data-stu-id="c3367-151">XAML Namespace Declarations in the XAML Services API</span></span>  
 <span data-ttu-id="c3367-152">V rozhraní API služby XAML je deklarace oboru <xref:System.Xaml.NamespaceDeclaration> názvů XAML reprezentována objektem.</span><span class="sxs-lookup"><span data-stu-id="c3367-152">In the XAML Services API, a XAML namespace declaration is represented by a <xref:System.Xaml.NamespaceDeclaration> object.</span></span> <span data-ttu-id="c3367-153">Pokud deklarujete obor názvů XAML <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29> v kódu, zavoláte konstruktor.</span><span class="sxs-lookup"><span data-stu-id="c3367-153">If you are declaring a XAML namespace in code, you call the <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29> constructor.</span></span> <span data-ttu-id="c3367-154">`ns` Parametry `prefix` a jsou určeny jako řetězce a vstup pro poskytnutí těchto parametrů odpovídá definici identifikátoru oboru názvů XAML a předpony oboru názvů XAML, jak je uvedeno dříve v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="c3367-154">The `ns` and `prefix` parameters are specified as strings, and the input to provide for these parameters corresponds to the definition of XAML namespace identifier and XAML namespace prefix as provided previously in this topic.</span></span>  
  
 <span data-ttu-id="c3367-155">Pokud zkoumáte informace o oboru názvů XAML jako součást datového proudu uzlu XAML nebo <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> prostřednictvím jiného přístupu <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> k systému typu XAML, hlásí identifikátor oboru názvů XAML a hlásí předponu oboru názvů XAML.</span><span class="sxs-lookup"><span data-stu-id="c3367-155">If you are examining XAML namespace information as part of a XAML node stream or through other access to the XAML type system, <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> reports the XAML namespace identifier, and <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> reports the XAML namespace prefix.</span></span>  
  
 <span data-ttu-id="c3367-156">V datovém proudu uzlu XAML se informace o oboru názvů XAML mohou zobrazit jako uzel XAML, který předchází entitě, na kterou se vztahuje.</span><span class="sxs-lookup"><span data-stu-id="c3367-156">In a XAML node stream, the XAML namespace information can appear as a XAML node that precedes the entity to which it applies.</span></span> <span data-ttu-id="c3367-157">To zahrnuje případy, kdy informace o oboru `StartObject` názvů XAML předchází kořenový prvek XAML.</span><span class="sxs-lookup"><span data-stu-id="c3367-157">This includes cases where the XAML namespace information precedes the `StartObject` of the XAML root element.</span></span> <span data-ttu-id="c3367-158">Další informace naleznete [v tématu Principy struktury a koncepty datových proudů uzlů XAML](understanding-xaml-node-stream-structures-and-concepts.md).</span><span class="sxs-lookup"><span data-stu-id="c3367-158">For more information, see [Understanding XAML Node Stream Structures and Concepts](understanding-xaml-node-stream-structures-and-concepts.md).</span></span>  
  
 <span data-ttu-id="c3367-159">Pro mnoho scénářů, které používají rozhraní API služby .NET XAML, se očekává, že existuje alespoň jedna deklarace oboru názvů XAML a deklarace musí obsahovat nebo odkazovat na informace, které jsou vyžadovány kontextem schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="c3367-159">For many scenarios that use .NET XAML Services API, at least one XAML namespace declaration is expected to exist, and the declaration must either contain or refer to information that is required by a XAML schema context.</span></span> <span data-ttu-id="c3367-160">Obory názvů XAML musí buď zadat sestavení, která mají být načtena, nebo pomoci při řešení určitých typů v oborech názvů a sestaveních, které jsou již načteny nebo známy kontextem schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="c3367-160">The XAML namespaces must either specify assemblies to be loaded, or assist in resolving specific types within namespaces and assemblies that are already loaded or known by the XAML schema context.</span></span>  
  
 <span data-ttu-id="c3367-161">Aby bylo možné generovat datový proud uzlu XAML, musí být k dispozici informace o typu XAML prostřednictvím kontextu schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="c3367-161">In order to generate a XAML node stream, XAML type information must be available, through the XAML schema context.</span></span> <span data-ttu-id="c3367-162">Informace o typu XAML nelze určit bez prvního určení příslušného oboru názvů XAML pro každý uzel, který má být vytvořit.</span><span class="sxs-lookup"><span data-stu-id="c3367-162">The XAML type information cannot be determined without first determining the relevant XAML namespace for each node to create.</span></span> <span data-ttu-id="c3367-163">V tomto okamžiku jsou zatím vytvořeny žádné instance typů, ale kontext schématu XAML může být nutné vyhledat informace z definující sestavení a typ zálohování.</span><span class="sxs-lookup"><span data-stu-id="c3367-163">At this point, no instances of types are created yet, but the XAML schema context may need to look up information from the defining assembly and backing type.</span></span> <span data-ttu-id="c3367-164">Chcete-li například zpracovat `<Party><PartyFavor/></Party>`značky , musí být kontext schématu XAML schopen určit `ContentProperty` `Party`název a typ of , a proto `Party` musí `PartyFavor`také znát informace o oboru názvů XAML pro a .</span><span class="sxs-lookup"><span data-stu-id="c3367-164">For example, in order to process the markup `<Party><PartyFavor/></Party>`, the XAML schema context must be able to determine the name and type of the `ContentProperty` of `Party`, and thus also must know the XAML namespace information for `Party` and `PartyFavor`.</span></span> <span data-ttu-id="c3367-165">V případě výchozího kontextu schématu XAML hlásí statický odraz většinu informací o systému typu XAML, které jsou potřebné ke generování uzlů typu XAML v datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="c3367-165">In the case of the default XAML schema context, static reflection reports much of the XAML type system information that is needed to generate XAML type nodes in the node stream.</span></span>  
  
 <span data-ttu-id="c3367-166">Aby bylo možné generovat objektový graf z datového proudu uzlu XAML, musí existovat deklarace oboru názvů XAML pro každou předponu XAML použitou v původní značce a zaznamenanou v datovém proudu uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="c3367-166">In order to generate an object graph from a XAML node stream, XAML namespace declarations must exist for each XAML prefix used in the original markup and recorded in the XAML node stream.</span></span> <span data-ttu-id="c3367-167">V tomto okamžiku jsou vytvářeny instance a true chování mapování typu dochází.</span><span class="sxs-lookup"><span data-stu-id="c3367-167">At this point, instances are being created, and true type-mapping behavior occurs.</span></span>  
  
 <span data-ttu-id="c3367-168">Pokud potřebujete předem vyplnit informace o oboru názvů XAML, není v kódu definován obor názvů XAML, který chcete použít v kontextu schématu XAML, je <xref:System.Xml.XmlParserContext> jedna <xref:System.Xml.XmlReader>technika, kterou můžete použít, deklarovat deklarace oboru názvů XML v aplikaci .</span><span class="sxs-lookup"><span data-stu-id="c3367-168">If you need to prepopulate XAML namespace information, in cases where the XAML namespace you intend the XAML schema context to use is not defined in the markup, one technique you can use is to declare XML namespace declarations in the <xref:System.Xml.XmlParserContext> for an <xref:System.Xml.XmlReader>.</span></span> <span data-ttu-id="c3367-169">Pak použijte <xref:System.Xml.XmlReader> jako vstup pro konstruktor čtečky <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>XAML, nebo .</span><span class="sxs-lookup"><span data-stu-id="c3367-169">Then use that <xref:System.Xml.XmlReader> as input for a XAML reader constructor, or <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="c3367-170">Dvě další rozhraní API, která jsou relevantní pro zpracování oboru názvů XAML ve službě .NET XAML Services, jsou atributy <xref:System.Windows.Markup.XmlnsDefinitionAttribute> a <xref:System.Windows.Markup.XmlnsPrefixAttribute>.</span><span class="sxs-lookup"><span data-stu-id="c3367-170">Two other APIs that are relevant for XAML namespace handling in .NET XAML Services are the attributes <xref:System.Windows.Markup.XmlnsDefinitionAttribute> and <xref:System.Windows.Markup.XmlnsPrefixAttribute>.</span></span> <span data-ttu-id="c3367-171">Tyto atributy platí pro sestavení.</span><span class="sxs-lookup"><span data-stu-id="c3367-171">These attributes apply to assemblies.</span></span> <span data-ttu-id="c3367-172"><xref:System.Windows.Markup.XmlnsDefinitionAttribute>používá kontext schématu XAML k interpretaci jakékoli deklarace oboru názvů XAML, která obsahuje identifikátor URI.</span><span class="sxs-lookup"><span data-stu-id="c3367-172"><xref:System.Windows.Markup.XmlnsDefinitionAttribute> is used by a XAML schema context to interpret any XAML namespace declaration that includes a URI.</span></span> <span data-ttu-id="c3367-173"><xref:System.Windows.Markup.XmlnsPrefixAttribute>používá nástroje, které vyzařují XAML tak, aby konkrétní obor názvů XAML lze serializovat s předvídatelnou předponou.</span><span class="sxs-lookup"><span data-stu-id="c3367-173"><xref:System.Windows.Markup.XmlnsPrefixAttribute> is used by tools that emit XAML so that a particular XAML namespace can be serialized with a predictable prefix.</span></span> <span data-ttu-id="c3367-174">Další informace naleznete [v tématu Atributy CLR související s XAML pro vlastní typy a knihovny](clr-attributes-with-custom-types-and-libraries.md).</span><span class="sxs-lookup"><span data-stu-id="c3367-174">For more information, see [XAML-Related CLR Attributes for Custom Types and Libraries](clr-attributes-with-custom-types-and-libraries.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="c3367-175">Viz také</span><span class="sxs-lookup"><span data-stu-id="c3367-175">See also</span></span>

- [<span data-ttu-id="c3367-176">Principy struktur a koncepcí datových proudů uzlů XAML</span><span class="sxs-lookup"><span data-stu-id="c3367-176">Understanding XAML Node Stream Structures and Concepts</span></span>](understanding-xaml-node-stream-structures-and-concepts.md)
