---
title: x:Shared – atribut
ms.date: 03/30/2017
helpviewer_keywords:
- XAML [XAML Services], x:Shared attribute
- x:Shared attribute [XAML Services]
- Shared attribute in XAML [XAML Services]
ms.assetid: c8cff434-2785-405f-9f95-16deb34c9e64
ms.openlocfilehash: e1cd1d9db5c19decd840b433f986e0ba53557a8b
ms.sourcegitcommit: c2d9718996402993cf31541f11e95531bc68bad0
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 02/27/2020
ms.locfileid: "82071372"
---
# <a name="xshared-attribute"></a><span data-ttu-id="ccd89-102">x:Shared – atribut</span><span class="sxs-lookup"><span data-stu-id="ccd89-102">x:Shared Attribute</span></span>

<span data-ttu-id="ccd89-103">Pokud je `false`nastavena na , upraví wpf chování načítání prostředků tak, aby požadavky na přiřazený prostředek vytvořit novou instanci pro každý požadavek namísto sdílení stejné instance pro všechny požadavky.</span><span class="sxs-lookup"><span data-stu-id="ccd89-103">When set to `false`, modifies WPF resource-retrieval behavior so that requests for the attributed resource create a new instance for each request instead of sharing the same instance for all requests.</span></span>

## <a name="xaml-attribute-usage"></a><span data-ttu-id="ccd89-104">Použití atributu XAML</span><span class="sxs-lookup"><span data-stu-id="ccd89-104">XAML Attribute Usage</span></span>

```xaml
<ResourceDictionary>
  <object x:Shared="false".../>
</ResourceDictionary>
```

## <a name="remarks"></a><span data-ttu-id="ccd89-105">Poznámky</span><span class="sxs-lookup"><span data-stu-id="ccd89-105">Remarks</span></span>

<span data-ttu-id="ccd89-106">`x:Shared`je mapován na obor názvů XAML jazyka XAML a je rozpoznán jako platný jazykový prvek XAML službou .NET XAML Services a jejími čtečkami XAML.</span><span class="sxs-lookup"><span data-stu-id="ccd89-106">`x:Shared` is mapped to the XAML language XAML namespace and is recognized as a valid XAML language element by .NET XAML Services and its XAML readers.</span></span> <span data-ttu-id="ccd89-107">Uvedené možnosti `x:Shared` jsou však relevantní pouze pro aplikace WPF a pro analyzátor WPF XAML.</span><span class="sxs-lookup"><span data-stu-id="ccd89-107">However, the stated capabilities of `x:Shared` are only relevant for WPF applications and for the WPF XAML parser.</span></span> <span data-ttu-id="ccd89-108">V WPF, `x:Shared` je užitečné pouze jako atribut, pokud je použita <xref:System.Windows.ResourceDictionary>na objekt, který existuje v rámci WPF .</span><span class="sxs-lookup"><span data-stu-id="ccd89-108">In WPF, `x:Shared` is only useful as an attribute when it is applied to an object that exists within a WPF <xref:System.Windows.ResourceDictionary>.</span></span> <span data-ttu-id="ccd89-109">Jiné použití nevyvolání výjimky analýzy nebo jiné chyby, ale nemají žádný vliv.</span><span class="sxs-lookup"><span data-stu-id="ccd89-109">Other usages do not throw parse exceptions or other errors, but they have no effect.</span></span>

<span data-ttu-id="ccd89-110">Význam pro `x:Shared` není specifikován ve specifikaci jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="ccd89-110">The meaning of `x:Shared` is not specified in the XAML language specification.</span></span> <span data-ttu-id="ccd89-111">Jiné implementace XAML, například ty, které jsou navazují na služby .NET XAML, nemusí nutně poskytovat podporu pro sdílení prostředků.</span><span class="sxs-lookup"><span data-stu-id="ccd89-111">Other XAML implementations, such as those that build on .NET XAML Services, do not necessarily provide resource-sharing support.</span></span> <span data-ttu-id="ccd89-112">Takové implementace XAML může poskytnout podobné chování v `x:Shared` podpůrné masy, která také používá hodnoty.</span><span class="sxs-lookup"><span data-stu-id="ccd89-112">Such XAML implementations could provide similar behavior in the supporting framework that also used `x:Shared` values.</span></span>

<span data-ttu-id="ccd89-113">V WPF je `x:Shared` `true`výchozí podmínkou pro prostředky .</span><span class="sxs-lookup"><span data-stu-id="ccd89-113">In WPF, the default `x:Shared` condition for resources is `true`.</span></span> <span data-ttu-id="ccd89-114">Tato podmínka znamená, že každý daný požadavek na prostředek vždy vrátí stejnou instanci.</span><span class="sxs-lookup"><span data-stu-id="ccd89-114">This condition means that any given resource request always returns the same instance.</span></span>

<span data-ttu-id="ccd89-115">Úprava objektu, který je vrácen prostřednictvím rozhraní API prostředků, <xref:System.Windows.FrameworkElement.FindResource%2A> <xref:System.Windows.ResourceDictionary>například , nebo úpravy objektu přímo v rámci , změní původní prostředek.</span><span class="sxs-lookup"><span data-stu-id="ccd89-115">Modifying an object that is returned through a resource API, such as <xref:System.Windows.FrameworkElement.FindResource%2A>, or modifying an object directly within a <xref:System.Windows.ResourceDictionary>, changes the original resource.</span></span> <span data-ttu-id="ccd89-116">Pokud odkazy na tento prostředek byly dynamické odkazy na prostředky, spotřebitelé tohoto prostředku získat změněný prostředek.</span><span class="sxs-lookup"><span data-stu-id="ccd89-116">If references to that resource were dynamic resource references, the consumers of that resource get the changed resource.</span></span>

<span data-ttu-id="ccd89-117">Pokud odkazy na prostředek byly statické odkazy na prostředky, změny prostředku po [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] zpracování času jsou irelevantní.</span><span class="sxs-lookup"><span data-stu-id="ccd89-117">If references to the resource were static resource references, changes to the resource after [!INCLUDE[TLA2#tla_xaml](../../../includes/tla2sharptla-xaml-md.md)] processing time are irrelevant.</span></span> <span data-ttu-id="ccd89-118">Další informace o statických a dynamických odkazech na prostředky naleznete v tématu [XAML Resources](../fundamentals/xaml-resources-define.md).</span><span class="sxs-lookup"><span data-stu-id="ccd89-118">For more information about static versus dynamic resource references, see [XAML Resources](../fundamentals/xaml-resources-define.md).</span></span>

<span data-ttu-id="ccd89-119">Explicitně `x:Shared="true"` zadání se provádí zřídka, protože to je již výchozí.</span><span class="sxs-lookup"><span data-stu-id="ccd89-119">Explicitly specifying `x:Shared="true"` is rarely done, because that is already the default.</span></span> <span data-ttu-id="ccd89-120">Neexistuje žádný přímý ekvivalent `x:Shared` kódu v objektovém modelu WPF; lze jej zadat pouze v použití XAML a musí být zpracovány buď výchozí wpf chování nebo v zprostředkující xaml uzel proudu na cestě zatížení, pokud jsou zpracovány pomocí .NET XAML Services a jeho XAML čtečky.</span><span class="sxs-lookup"><span data-stu-id="ccd89-120">There is no direct code equivalent for `x:Shared` in the WPF object model; it can only be specified in a XAML usage and must be processed either by the default WPF behavior or in an intermediate XAML node stream on the load path if processed using .NET XAML Services and its XAML readers.</span></span>

<span data-ttu-id="ccd89-121">Scénář pro `x:Shared="false"` je, pokud <xref:System.Windows.FrameworkElement> <xref:System.Windows.FrameworkContentElement> definujete nebo odvozené třídy jako prostředek a potom zavést prostředek prvku do modelu obsahu.</span><span class="sxs-lookup"><span data-stu-id="ccd89-121">A scenario for `x:Shared="false"` is if you define a <xref:System.Windows.FrameworkElement> or <xref:System.Windows.FrameworkContentElement> derived class as a resource and then you introduce the element resource into a content model.</span></span> <span data-ttu-id="ccd89-122">`x:Shared="false"`umožňuje prostředek prvku, který má být zaveden vícekrát <xref:System.Windows.Controls.UIElementCollection>ve stejné kolekci (například ).</span><span class="sxs-lookup"><span data-stu-id="ccd89-122">`x:Shared="false"` enables an element resource to be introduced multiple times in the same collection (such as a <xref:System.Windows.Controls.UIElementCollection>).</span></span> <span data-ttu-id="ccd89-123">Bez `x:Shared="false"` tohoto je neplatný, protože kolekce vynucuje jedinečnost jeho obsahu.</span><span class="sxs-lookup"><span data-stu-id="ccd89-123">Without `x:Shared="false"` this is invalid because the collection enforces uniqueness of its contents.</span></span> <span data-ttu-id="ccd89-124">Chování však `x:Shared="false"` vytvoří jinou identickou instanci prostředku namísto vrácení stejné instance.</span><span class="sxs-lookup"><span data-stu-id="ccd89-124">However, the `x:Shared="false"` behavior creates another identical instance of the resource instead of returning the same instance.</span></span>

<span data-ttu-id="ccd89-125">Jiný scénář `x:Shared="false"` pro je, <xref:System.Windows.Freezable> pokud používáte prostředek pro hodnoty animace, ale chcete upravit prostředek na základě animace.</span><span class="sxs-lookup"><span data-stu-id="ccd89-125">Another scenario for `x:Shared="false"` is if you use a <xref:System.Windows.Freezable> resource for animation values but want to modify the resource on a per animation basis.</span></span>

<span data-ttu-id="ccd89-126">Zpracování `false` řetězce není rozlišována malá a velká písmena.</span><span class="sxs-lookup"><span data-stu-id="ccd89-126">The string handling of `false` is not case sensitive.</span></span>

<span data-ttu-id="ccd89-127">V WPF, `x:Shared` je platný pouze za následujících podmínek:</span><span class="sxs-lookup"><span data-stu-id="ccd89-127">In WPF, `x:Shared` is only valid under the following conditions:</span></span>

- <span data-ttu-id="ccd89-128">Který <xref:System.Windows.ResourceDictionary> obsahuje položky `x:Shared` s musí být zkompilován.</span><span class="sxs-lookup"><span data-stu-id="ccd89-128">The <xref:System.Windows.ResourceDictionary> that contains the items with `x:Shared` must be compiled.</span></span> <span data-ttu-id="ccd89-129">Nemůže <xref:System.Windows.ResourceDictionary> být v rámci volné XAML nebo použít pro motivy.</span><span class="sxs-lookup"><span data-stu-id="ccd89-129">The <xref:System.Windows.ResourceDictionary> cannot be within loose XAML or used for themes.</span></span>

- <span data-ttu-id="ccd89-130">Položka, <xref:System.Windows.ResourceDictionary> která obsahuje položky, nesmí <xref:System.Windows.ResourceDictionary>být vnořena do jiného .</span><span class="sxs-lookup"><span data-stu-id="ccd89-130">The <xref:System.Windows.ResourceDictionary> that contains the items must not be nested within another <xref:System.Windows.ResourceDictionary>.</span></span> <span data-ttu-id="ccd89-131">Například nelze použít `x:Shared` pro položky <xref:System.Windows.ResourceDictionary> v, <xref:System.Windows.Style> která je <xref:System.Windows.ResourceDictionary> v rámci, která je již položka.</span><span class="sxs-lookup"><span data-stu-id="ccd89-131">For example, you cannot use `x:Shared` for items in a <xref:System.Windows.ResourceDictionary> that is within a <xref:System.Windows.Style> that is already a <xref:System.Windows.ResourceDictionary> item.</span></span>

## <a name="see-also"></a><span data-ttu-id="ccd89-132">Viz také</span><span class="sxs-lookup"><span data-stu-id="ccd89-132">See also</span></span>

- <xref:System.Windows.ResourceDictionary>
- [<span data-ttu-id="ccd89-133">Zdroje XAML</span><span class="sxs-lookup"><span data-stu-id="ccd89-133">XAML Resources</span></span>](../fundamentals/xaml-resources-define.md)
- [<span data-ttu-id="ccd89-134">Základní elementy</span><span class="sxs-lookup"><span data-stu-id="ccd89-134">Base Elements</span></span>](../../framework/wpf/advanced/base-elements.md)
