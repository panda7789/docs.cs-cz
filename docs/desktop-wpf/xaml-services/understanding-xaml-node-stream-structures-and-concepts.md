---
title: Principy struktur a koncepcí datových proudů uzlů XAML
ms.date: 03/30/2017
helpviewer_keywords:
- XAML node streams [XAML Services]
- nodes [XAML Services], XAML node stream
- XAML [XAML Services], XAML node streams
ms.assetid: 7c11abec-1075-474c-9d9b-778e5dab21c3
ms.openlocfilehash: b3de3dca029c5e676fc7cdebc7735cfdade0228a
ms.sourcegitcommit: c2d9718996402993cf31541f11e95531bc68bad0
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 02/27/2020
ms.locfileid: "82071617"
---
# <a name="xaml-node-stream-structures-and-concepts"></a><span data-ttu-id="c1063-102">Struktury a koncepty datových proudů uzlů XAML</span><span class="sxs-lookup"><span data-stu-id="c1063-102">XAML node stream structures and concepts</span></span>

<span data-ttu-id="c1063-103">Čtecí programy XAML a zapisovače XAML implementované ve službách .NET XAML jsou založeny na konceptu návrhu datového proudu uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-103">XAML readers and XAML writers as implemented in .NET XAML Services are based on the design concept of a XAML node stream.</span></span> <span data-ttu-id="c1063-104">Datový proud uzlu XAML je konceptualizace sady uzlů XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-104">The XAML node stream is a conceptualization of a set of XAML nodes.</span></span> <span data-ttu-id="c1063-105">V této konceptualizaci xaml procesor prochází strukturu vztahů uzlů v XAML jeden po druhém.</span><span class="sxs-lookup"><span data-stu-id="c1063-105">In this conceptualization, a XAML processor walks through the structure of the node relationships in the XAML one at a time.</span></span> <span data-ttu-id="c1063-106">V otevřeném datovém proudu uzlu XAML existuje kdykoli pouze jeden aktuální záznam nebo aktuální pozice a mnoho aspektů sestavy rozhraní API pouze informace dostupné z této pozice.</span><span class="sxs-lookup"><span data-stu-id="c1063-106">At any time, only one current record or current position exists in an open XAML node stream, and many aspects of the API report only the information available from that position.</span></span> <span data-ttu-id="c1063-107">Aktuální uzel v datovém proudu uzlu XAML lze popsat jako objekt, člen nebo hodnotu.</span><span class="sxs-lookup"><span data-stu-id="c1063-107">The current node in a XAML node stream can be described as being an object, a member, or a value.</span></span> <span data-ttu-id="c1063-108">Zpracováním XAML jako datového proudu uzlu XAML mohou čtenáři XAML komunikovat se zapisovateli XAML a povolit programu zobrazení, interakci nebo změnu obsahu datového proudu uzlu XAML během cesty načítání nebo operace uložení cesty, která zahrnuje XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-108">By treating XAML as a XAML node stream, XAML readers can communicate with XAML writers and enable a program to view, interact with, or alter the contents of a XAML node stream during either a load path or a save path operation that involves XAML.</span></span> <span data-ttu-id="c1063-109">Návrh rozhraní API pro čtení a zapisovače XAML a koncept datového proudu uzlů XAML jsou podobné předchozím <xref:System.Xml.XmlReader> souvisejícím návrhům a konceptům pro čtení a zapisovače, jako je například xml document object model (DOM) a třídy a. <xref:System.Xml.XmlWriter></span><span class="sxs-lookup"><span data-stu-id="c1063-109">XAML reader and writer API design and the XAML node stream concept are similar to previous related reader and writer designs and concepts, such as the XML Document Object Model (DOM) and the <xref:System.Xml.XmlReader> and <xref:System.Xml.XmlWriter> classes.</span></span> <span data-ttu-id="c1063-110">Toto téma popisuje koncepty datového proudu uzlů XAML a popisuje, jak můžete psát rutiny, které interagují s reprezentacemi XAML na úrovni uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-110">This topic discusses XAML node stream concepts and describes how you can write routines that interact with XAML representations at the XAML node level.</span></span>

## <a name="loading-xaml-into-a-xaml-reader"></a><span data-ttu-id="c1063-111">Načítání XAML do čtečky XAML</span><span class="sxs-lookup"><span data-stu-id="c1063-111">Loading XAML into a XAML Reader</span></span>

<span data-ttu-id="c1063-112">Základní <xref:System.Xaml.XamlReader> třída nedeklaruje konkrétní techniku pro načítání počáteční XAML do čtečky XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-112">The base <xref:System.Xaml.XamlReader> class does not declare a particular technique for loading the initial XAML into a XAML reader.</span></span> <span data-ttu-id="c1063-113">Místo toho odvozené třídy deklaruje a implementuje načítat techniku, včetně obecné charakteristiky a omezení jeho vstupní zdroj pro XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-113">Instead, a derived class declares and implements the loading technique, including the general characteristics and constraints of its input source for XAML.</span></span> <span data-ttu-id="c1063-114">Například přečte <xref:System.Xaml.XamlObjectReader> objektgrafu, počínaje vstupní zdroj jednoho objektu, který představuje kořen nebo základnu.</span><span class="sxs-lookup"><span data-stu-id="c1063-114">For example, a <xref:System.Xaml.XamlObjectReader> reads an object graph, starting from the input source of a single object that represents the root or base.</span></span> <span data-ttu-id="c1063-115">Potom <xref:System.Xaml.XamlObjectReader> vytvoří datový proud uzlu XAML z grafu objektu.</span><span class="sxs-lookup"><span data-stu-id="c1063-115">The <xref:System.Xaml.XamlObjectReader> then produces a XAML node stream from the object graph.</span></span>

<span data-ttu-id="c1063-116">Nejvýznamnější podtřída služby .NET <xref:System.Xaml.XamlReader> XAML <xref:System.Xaml.XamlXmlReader>services je .</span><span class="sxs-lookup"><span data-stu-id="c1063-116">The most prominent .NET XAML Services–defined <xref:System.Xaml.XamlReader> subclass is <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="c1063-117"><xref:System.Xaml.XamlXmlReader>načte počáteční XAML, buď načtením textového souboru přímo přes datový proud nebo <xref:System.IO.TextReader>cestu k souboru, nebo nepřímo prostřednictvím související třídy čtečky, jako je například .</span><span class="sxs-lookup"><span data-stu-id="c1063-117"><xref:System.Xaml.XamlXmlReader> loads the initial XAML, either by loading a text file directly through a stream or file path, or indirectly through a related reader class such as <xref:System.IO.TextReader>.</span></span> <span data-ttu-id="c1063-118">Lze <xref:System.Xaml.XamlReader> si myslet jako obsahující celý vstupní zdroj XAML po jeho načtení.</span><span class="sxs-lookup"><span data-stu-id="c1063-118">The <xref:System.Xaml.XamlReader> can be thought of as containing the entirety of the XAML input source after it has loaded.</span></span> <span data-ttu-id="c1063-119"><xref:System.Xaml.XamlReader> Základní rozhraní API je však navržentak, aby čtenář pracuje s jedním uzlem XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-119">However, the <xref:System.Xaml.XamlReader> base API is designed so that the reader is interacting with a single node of the XAML.</span></span> <span data-ttu-id="c1063-120">Při prvním načtení je prvním samostatným uzlovým uzlem kořen xaml a jeho počáteční objekt.</span><span class="sxs-lookup"><span data-stu-id="c1063-120">When first loaded, the first single node you encounter is the root of the XAML, and its start object.</span></span>

### <a name="the-xaml-node-stream-concept"></a><span data-ttu-id="c1063-121">Koncept datového proudu uzlů XAML</span><span class="sxs-lookup"><span data-stu-id="c1063-121">The XAML Node Stream Concept</span></span>

<span data-ttu-id="c1063-122">Pokud jste více obeznámeni s DOM, strom metafora nebo přístup založený na dotazu k přístupu k technologiím založeným na XML, užitečný způsob, jak konceptualizovat datový proud uzlu XAML je následující.</span><span class="sxs-lookup"><span data-stu-id="c1063-122">If you are more familiar with a DOM, tree metaphor, or query-based approach towards accessing XML-based technologies, a helpful way to conceptualize a XAML node stream is as follows.</span></span> <span data-ttu-id="c1063-123">Představte si, že načtený XAML je DOM nebo strom, kde jsou všechny možné uzlení rozbaleny a pak prezentovány lineárně.</span><span class="sxs-lookup"><span data-stu-id="c1063-123">Imagine that the loaded XAML is a DOM or a tree where every possible node is expanded all the way, and then presented linearly.</span></span> <span data-ttu-id="c1063-124">Při postupu přes uzly, může být procházení "in" nebo "out" úrovní, které by byly relevantní pro DOM, ale datový proud uzlu XAML není explicitně sledovat, protože tyto koncepty úrovně nejsou relevantní pro datový proud uzlu.</span><span class="sxs-lookup"><span data-stu-id="c1063-124">As you advance through the nodes, you might be traversing "in" or "out" of levels that would be relevant to a DOM, but the XAML node stream does not explicitly keep track because these level concepts are not relevant to a node stream.</span></span> <span data-ttu-id="c1063-125">Datový proud uzlu má "aktuální" pozici, ale pokud jste uložili jiné části datového proudu sami jako odkazy, každý aspekt datového proudu uzlu než aktuální pozice uzlu je mimo zobrazení.</span><span class="sxs-lookup"><span data-stu-id="c1063-125">The node stream has a "current" position, but unless you have stored other parts of the stream yourself as references, every aspect of the node stream other than the current node position is out of view.</span></span>

<span data-ttu-id="c1063-126">Koncept datového proudu uzlu XAML má významnou výhodu, že pokud procházíte celý datový proud uzlu, máte jistotu, že jste zpracovali celou reprezentaci XAML; nemusíte se obávat, že dotaz, operace DOM nebo jiný nelineární přístup ke zpracování informací vynechal některé části úplné reprezentace XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-126">The XAML node stream concept has the notable advantage that if you go through the entire node stream, you are assured that you have processed the entire XAML representation; you do not need to worry that a query, a DOM operation, or some other nonlinear approach to processing information has missed some part of the complete XAML representation.</span></span> <span data-ttu-id="c1063-127">Z tohoto důvodu reprezentace datového proudu uzlu XAML je ideální pro připojení čteček XAML a zapisovačů XAML a pro poskytování systému, kde můžete vložit vlastní proces, který funguje mezi fázečtení a zápisu operace zpracování XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-127">For this reason, the XAML node stream representation is ideal both for connecting XAML readers and XAML writers, and for providing a system where you can insert your own process that acts between the read and write phases of a XAML processing operation.</span></span> <span data-ttu-id="c1063-128">V mnoha případech je řazení uzlů v datovém proudu uzlu XAML záměrně optimalizováno nebo uspořádáno čtenáři XAML versus jak se pořadí může zobrazit ve zdrojovém textu, binárním nebo objektovém grafu.</span><span class="sxs-lookup"><span data-stu-id="c1063-128">In many cases, the ordering of nodes in the XAML node stream is deliberately optimized or reordered by XAML readers versus how the order might appear in the source text, binary, or object graph.</span></span> <span data-ttu-id="c1063-129">Toto chování je určena k vynucení architektury zpracování XAML, kdy autoři XAML nejsou nikdy v pozici, kde musí přejít "zpět" v datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="c1063-129">This behavior is intended to enforce a XAML processing architecture whereby XAML writers are never in a position where they have to go "back" in the node stream.</span></span> <span data-ttu-id="c1063-130">V ideálním případě by všechny operace zápisu XAML měly být schopny fungovat na základě kontextu schématu a aktuální pozice datového proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="c1063-130">Ideally, all XAML write operations should be able to act based on schema context plus the current position of the node stream.</span></span>

## <a name="a-basic-reading-node-loop"></a><span data-ttu-id="c1063-131">Smyčka základního čtecího uzlu</span><span class="sxs-lookup"><span data-stu-id="c1063-131">A Basic Reading Node Loop</span></span>

<span data-ttu-id="c1063-132">Základní smyčka čtecího uzlu pro zkoumání datového proudu uzlu XAML se skládá z následujících konceptů.</span><span class="sxs-lookup"><span data-stu-id="c1063-132">A basic reading node loop for examining a XAML node stream consists of the following concepts.</span></span> <span data-ttu-id="c1063-133">Pro účely smyčků uzlů, jak je popsáno v tomto tématu, předpokládejme, že <xref:System.Xaml.XamlXmlReader>čtete textově čitelný soubor XAML založený na člověku pomocí .</span><span class="sxs-lookup"><span data-stu-id="c1063-133">For purposes of node loops as discussed in this topic, assume that you are reading a text-based, human-readable XAML file using <xref:System.Xaml.XamlXmlReader>.</span></span> <span data-ttu-id="c1063-134">Odkazy v této části odkazují na konkrétní rozhraní API <xref:System.Xaml.XamlXmlReader>smyčky uzlů XAML implementované rozhraním .</span><span class="sxs-lookup"><span data-stu-id="c1063-134">The links in this section refer to the particular XAML node loop API implemented by <xref:System.Xaml.XamlXmlReader>.</span></span>

- <span data-ttu-id="c1063-135">Ujistěte se, že nejste na konci datového proudu <xref:System.Xaml.XamlXmlReader.IsEof%2A>uzlu XAML (kontrola , nebo použijte vrácenou <xref:System.Xaml.XamlXmlReader.Read%2A> hodnotu).</span><span class="sxs-lookup"><span data-stu-id="c1063-135">Make sure that you are not at the end of the XAML node stream (check <xref:System.Xaml.XamlXmlReader.IsEof%2A>, or use the <xref:System.Xaml.XamlXmlReader.Read%2A> return value).</span></span> <span data-ttu-id="c1063-136">Pokud jste na konci datového proudu, neexistuje žádný aktuální uzel a měli byste ukončit.</span><span class="sxs-lookup"><span data-stu-id="c1063-136">If you are at the end of the stream, there is no current node and you should exit.</span></span>

- <span data-ttu-id="c1063-137">Zkontrolujte, jaký typ uzlu datový proud uzlu XAML <xref:System.Xaml.XamlXmlReader.NodeType%2A>aktuálně zveřejňuje voláním .</span><span class="sxs-lookup"><span data-stu-id="c1063-137">Check what type of node the XAML node stream currently exposes by calling <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span>

- <span data-ttu-id="c1063-138">Pokud máte přidružený zapisovač objektů XAML, <xref:System.Xaml.XamlWriter.WriteNode%2A> který je připojen přímo, obvykle voláte v tomto okamžiku.</span><span class="sxs-lookup"><span data-stu-id="c1063-138">If you have an associated XAML object writer that is connected directly, you generally call <xref:System.Xaml.XamlWriter.WriteNode%2A> at this point.</span></span>

- <span data-ttu-id="c1063-139">Na základě <xref:System.Xaml.XamlNodeType> toho, který je hlášen jako aktuální uzel nebo aktuální záznam, volejte jednu z následujících získat informace o obsahu uzlu:</span><span class="sxs-lookup"><span data-stu-id="c1063-139">Based on which <xref:System.Xaml.XamlNodeType> is reported as the current node or current record, call one of the following to obtain information about the node contents:</span></span>

  - <span data-ttu-id="c1063-140">Pro <xref:System.Xaml.XamlXmlReader.NodeType%2A> nebo <xref:System.Xaml.XamlNodeType.StartMember> <xref:System.Xaml.XamlNodeType.EndMember>, <xref:System.Xaml.XamlXmlReader.Member%2A> volání <xref:System.Xaml.XamlMember> získat informace o člen.</span><span class="sxs-lookup"><span data-stu-id="c1063-140">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartMember> or <xref:System.Xaml.XamlNodeType.EndMember>, call <xref:System.Xaml.XamlXmlReader.Member%2A> to obtain <xref:System.Xaml.XamlMember> information about a member.</span></span> <span data-ttu-id="c1063-141">Člen může být <xref:System.Xaml.XamlDirective>, a proto nemusí být nutně konvenční typově definovaný člen předchozího objektu.</span><span class="sxs-lookup"><span data-stu-id="c1063-141">The member might be a <xref:System.Xaml.XamlDirective>, and thus might not necessarily be a conventional type-defined member of the preceding object.</span></span> <span data-ttu-id="c1063-142">Například `x:Name` použít na objekt se zobrazí jako <xref:System.Xaml.XamlMember.IsDirective%2A> člen XAML, kde je true a <xref:System.Xaml.XamlMember.Name%2A> člen je `Name`, s dalšími vlastnostmi označující, že tato směrnice je pod xaml jazyk xaml oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="c1063-142">For example, `x:Name` applied to an object appears as a XAML member where <xref:System.Xaml.XamlMember.IsDirective%2A> is true and the <xref:System.Xaml.XamlMember.Name%2A> of the member is `Name`, with other properties indicating that this directive is under the XAML language XAML namespace.</span></span>

  - <span data-ttu-id="c1063-143">Pro <xref:System.Xaml.XamlXmlReader.NodeType%2A> a <xref:System.Xaml.XamlNodeType.StartObject> <xref:System.Xaml.XamlNodeType.EndObject>nebo <xref:System.Xaml.XamlXmlReader.Type%2A> , <xref:System.Xaml.XamlType> volání získat informace o objektu.</span><span class="sxs-lookup"><span data-stu-id="c1063-143">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.StartObject> or <xref:System.Xaml.XamlNodeType.EndObject>, call <xref:System.Xaml.XamlXmlReader.Type%2A> to obtain <xref:System.Xaml.XamlType> information about an object.</span></span>

  - <span data-ttu-id="c1063-144">Pro <xref:System.Xaml.XamlXmlReader.NodeType%2A> a, <xref:System.Xaml.XamlNodeType.Value> <xref:System.Xaml.XamlXmlReader.Value%2A>volejte .</span><span class="sxs-lookup"><span data-stu-id="c1063-144">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.Value>, call <xref:System.Xaml.XamlXmlReader.Value%2A>.</span></span> <span data-ttu-id="c1063-145">Uzel je hodnota pouze v případě, že se jedná o nejjednodušší výraz hodnoty pro člena nebo inicializační text pro objekt (měli byste však vědět o chování převodu typu, jak je popsáno v nadcházející části tohoto tématu).</span><span class="sxs-lookup"><span data-stu-id="c1063-145">A node is a value only if it is the simplest expression of a value for a member, or the initialization text for an object (however, you should be aware of type conversion behavior as documented in an upcoming section of this topic).</span></span>

  - <span data-ttu-id="c1063-146">V <xref:System.Xaml.XamlXmlReader.NodeType%2A> případě <xref:System.Xaml.XamlNodeType.NamespaceDeclaration>a <xref:System.Xaml.XamlXmlReader.Namespace%2A> od , volání získat informace o oboru názvů pro uzel oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="c1063-146">For a <xref:System.Xaml.XamlXmlReader.NodeType%2A> of <xref:System.Xaml.XamlNodeType.NamespaceDeclaration>, call <xref:System.Xaml.XamlXmlReader.Namespace%2A> to obtain namespace information for a namespace node.</span></span>

- <span data-ttu-id="c1063-147">Volání <xref:System.Xaml.XamlXmlReader.Read%2A> předem čtečky XAML na další uzel v datovém proudu uzlu XAML a opakujte kroky znovu.</span><span class="sxs-lookup"><span data-stu-id="c1063-147">Call <xref:System.Xaml.XamlXmlReader.Read%2A> to advance the XAML reader to the next node in the XAML node stream, and repeat the steps again.</span></span>

<span data-ttu-id="c1063-148">Datový proud uzlu XAML poskytovaný čtenáři XAML služby .NET služby xaml vždy poskytuje úplný, hluboký průchod všech možných uzlů.</span><span class="sxs-lookup"><span data-stu-id="c1063-148">The XAML node stream provided by .NET XAML Services XAML readers always provides a full, deep traversal of all possible nodes.</span></span> <span data-ttu-id="c1063-149">Typické techniky řízení toku pro smyčku uzlu XAML zahrnují definování těla v rámci `while (reader.Read())`a zapnutí <xref:System.Xaml.XamlXmlReader.NodeType%2A> v každém bodě uzlu ve smyčce uzlu.</span><span class="sxs-lookup"><span data-stu-id="c1063-149">Typical flow-control techniques for a XAML node loop include defining a body within `while (reader.Read())`, and switching on <xref:System.Xaml.XamlXmlReader.NodeType%2A> at each node point in the node loop.</span></span>

<span data-ttu-id="c1063-150">Pokud je datový proud uzlu na konci souboru, aktuální uzel má hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="c1063-150">If the node stream is at end of file, the current node is null.</span></span>

<span data-ttu-id="c1063-151">Nejjednodušší smyčka, která používá čtečku a zapisovač, se podobá následujícímu příkladu.</span><span class="sxs-lookup"><span data-stu-id="c1063-151">The simplest loop that uses a reader and writer resembles the following example.</span></span>

```csharp
XamlXmlReader xxr = new XamlXmlReader(new StringReader(xamlStringToLoad));
//where xamlStringToLoad is a string of well formed XAML
XamlObjectWriter xow = new XamlObjectWriter(xxr.SchemaContext);
while (xxr.Read()) {
  xow.WriteNode(xxr);
}
```

<span data-ttu-id="c1063-152">Tento základní příklad smyčky uzlu XAML cesty zatížení transparentně spojuje čtečku XAML a zapisovač <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>XAML a nedělá nic jiného, než kdybyste použili .</span><span class="sxs-lookup"><span data-stu-id="c1063-152">This basic example of a load path XAML node loop transparently connects the XAML reader and XAML writer, doing nothing different than if you had used <xref:System.Xaml.XamlServices.Parse%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="c1063-153">Ale tato základní struktura je pak rozšířena tak, aby se vztahovala na váš scénář čtení nebo psaní.</span><span class="sxs-lookup"><span data-stu-id="c1063-153">But this basic structure is then expanded to apply to your reading or writing scenario.</span></span> <span data-ttu-id="c1063-154">Některé možné scénáře jsou následující:</span><span class="sxs-lookup"><span data-stu-id="c1063-154">Some possible scenarios are as follows:</span></span>

- <span data-ttu-id="c1063-155">Zapněte <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span><span class="sxs-lookup"><span data-stu-id="c1063-155">Switch on <xref:System.Xaml.XamlXmlReader.NodeType%2A>.</span></span> <span data-ttu-id="c1063-156">Proveďte různé akce v závislosti na typu uzlu, který se čte.</span><span class="sxs-lookup"><span data-stu-id="c1063-156">Perform different actions depending on which node type is being read.</span></span>

- <span data-ttu-id="c1063-157">Nevolejte <xref:System.Xaml.XamlWriter.WriteNode%2A> ve všech případech.</span><span class="sxs-lookup"><span data-stu-id="c1063-157">Do not call <xref:System.Xaml.XamlWriter.WriteNode%2A> in all cases.</span></span> <span data-ttu-id="c1063-158">Zavolej <xref:System.Xaml.XamlWriter.WriteNode%2A> jen <xref:System.Xaml.XamlXmlReader.NodeType%2A> v některých případech.</span><span class="sxs-lookup"><span data-stu-id="c1063-158">Only call <xref:System.Xaml.XamlWriter.WriteNode%2A> in some <xref:System.Xaml.XamlXmlReader.NodeType%2A> cases.</span></span>

- <span data-ttu-id="c1063-159">V rámci logiky pro konkrétní typ uzlu analyzujte specifika tohoto uzlu a jednejte na ně.</span><span class="sxs-lookup"><span data-stu-id="c1063-159">Within the logic for a particular node type, analyze the specifics of that node and act on them.</span></span> <span data-ttu-id="c1063-160">Můžete například zapisovat pouze objekty, které pocházejí z určitého oboru názvů XAML, a potom přetáhnout nebo odložit všechny objekty, které nejsou z tohoto oboru názvů XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-160">For example, you could only write objects that come from a particular XAML namespace, and then drop or defer any objects not from that XAML namespace.</span></span> <span data-ttu-id="c1063-161">Nebo můžete vynechat nebo jinak znovu zpracovat všechny direktivy XAML, které váš systém XAML nepodporuje jako součást zpracování členů.</span><span class="sxs-lookup"><span data-stu-id="c1063-161">Or you could drop or otherwise reprocess any XAML directives that your XAML system does not support as part of your member processing.</span></span>

- <span data-ttu-id="c1063-162">Definujte <xref:System.Xaml.XamlObjectWriter> vlastní, `Write*` která přepíše metody, případně provádění mapování typu, které obchází kontext schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-162">Define a custom <xref:System.Xaml.XamlObjectWriter> that overrides `Write*` methods, possibly performing type mapping that bypasses XAML schema context.</span></span>

- <span data-ttu-id="c1063-163">Vytvořte <xref:System.Xaml.XamlXmlReader> použít nevýchozí kontext schématu XAML tak, aby vlastní rozdíly v chování XAML jsou používány jak čtenář a zapisovač.</span><span class="sxs-lookup"><span data-stu-id="c1063-163">Construct the <xref:System.Xaml.XamlXmlReader> to use a nondefault XAML schema context, so that customized differences in XAML behavior are used both by the reader and the writer.</span></span>

### <a name="accessing-xaml-beyond-the-node-loop-concept"></a><span data-ttu-id="c1063-164">Přístup k XAML za konceptem smyčky uzlů</span><span class="sxs-lookup"><span data-stu-id="c1063-164">Accessing XAML Beyond the Node Loop Concept</span></span>

<span data-ttu-id="c1063-165">Existují potenciálně jiné způsoby práce s reprezentací XAML než jako smyčka uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-165">There are potentially other ways to work with a XAML representation other than as a XAML node loop.</span></span> <span data-ttu-id="c1063-166">Může například existovat čtečka XAML, která dokáže číst indexovaný uzel, nebo `x:Name`zejména `x:Uid`přistupuje k uzlům přímo pomocí , aplikacem nebo prostřednictvím jiných identifikátorů.</span><span class="sxs-lookup"><span data-stu-id="c1063-166">For example, there could exist a XAML reader that can read an indexed node, or in particular accesses nodes directly by `x:Name`, by `x:Uid`, or through other identifiers.</span></span> <span data-ttu-id="c1063-167">Služby .NET XAML neposkytuje úplnou implementaci, ale poskytuje navrhovaný vzor prostřednictvím služeb a typů podpory.</span><span class="sxs-lookup"><span data-stu-id="c1063-167">.NET XAML Services does not provide a full implementation, but provides a suggested pattern through services and support types.</span></span> <span data-ttu-id="c1063-168">Další informace naleznete v tématech <xref:System.Xaml.IXamlIndexingReader> a <xref:System.Xaml.XamlNodeList>.</span><span class="sxs-lookup"><span data-stu-id="c1063-168">For more information, see <xref:System.Xaml.IXamlIndexingReader> and <xref:System.Xaml.XamlNodeList>.</span></span>

## <a name="working-with-the-current-node"></a><span data-ttu-id="c1063-169">Práce s aktuálním uzlem</span><span class="sxs-lookup"><span data-stu-id="c1063-169">Working with the Current Node</span></span>

<span data-ttu-id="c1063-170">Většina scénářů, které používají smyčku uzlu XAML, nečtou pouze uzly.</span><span class="sxs-lookup"><span data-stu-id="c1063-170">Most scenarios that use a XAML node loop do not only read the nodes.</span></span> <span data-ttu-id="c1063-171">Většina scénářů zpracovává aktuální uzly a předává <xref:System.Xaml.XamlWriter>každý uzel jeden po druhém implementaci aplikace .</span><span class="sxs-lookup"><span data-stu-id="c1063-171">Most scenarios process current nodes and pass each node one at a time to an implementation of <xref:System.Xaml.XamlWriter>.</span></span>

<span data-ttu-id="c1063-172">V případě typické cesty <xref:System.Xaml.XamlXmlReader> zatížení vytvoří datový proud uzlu XAML; uzly XAML jsou zpracovány podle vaší logiky a kontextu schématu XAML; a uzly jsou <xref:System.Xaml.XamlObjectWriter>předány .</span><span class="sxs-lookup"><span data-stu-id="c1063-172">In the typical load path scenario, a <xref:System.Xaml.XamlXmlReader> produces a XAML node stream; the XAML nodes are processed according to your logic and XAML schema context; and the nodes are passed to a <xref:System.Xaml.XamlObjectWriter>.</span></span> <span data-ttu-id="c1063-173">Potom integrovat výsledný objekt grafu do aplikace nebo rozhraní.</span><span class="sxs-lookup"><span data-stu-id="c1063-173">You then integrate the resulting object graph into your application or framework.</span></span>

<span data-ttu-id="c1063-174">V typickém scénáři <xref:System.Xaml.XamlObjectReader> cesty pro uložení přečte graf objektu, jednotlivé <xref:System.Xaml.XamlXmlWriter> uzly XAML jsou zpracovány a výstupy serializované výsledek jako textový soubor XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-174">In a typical save path scenario, a <xref:System.Xaml.XamlObjectReader> reads the object graph, individual XAML nodes are processed, and a <xref:System.Xaml.XamlXmlWriter> outputs the serialized result as a XAML text file.</span></span> <span data-ttu-id="c1063-175">Klíčem je, že cesty i scénáře zahrnují práci s přesně jedním uzlem XAML najednou a uzly XAML jsou k dispozici pro zpracování standardizovaným způsobem, který je definován systémem typu XAML a the.NET rozhraní API služby XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-175">The key is that both paths and scenarios involve working with exactly one XAML node at a time, and the XAML nodes are available for treatment in a standardized way that is defined by the XAML type system and the.NET XAML Services APIs.</span></span>

### <a name="frames-and-scope"></a><span data-ttu-id="c1063-176">Rámce a rozsah</span><span class="sxs-lookup"><span data-stu-id="c1063-176">Frames and Scope</span></span>

<span data-ttu-id="c1063-177">Smyčka uzlu XAML prochází datovým proudem uzlu XAML lineárním způsobem.</span><span class="sxs-lookup"><span data-stu-id="c1063-177">A XAML node loop walks through a XAML node stream in a linear way.</span></span> <span data-ttu-id="c1063-178">Datový proud uzlu prochází do objektů, do členů, které obsahují jiné objekty a tak dále.</span><span class="sxs-lookup"><span data-stu-id="c1063-178">The node stream traverses into objects, into members that contain other objects, and so on.</span></span> <span data-ttu-id="c1063-179">Je často užitečné sledovat rozsah v rámci datového proudu uzlu XAML implementací konceptu rámce a zásobníku.</span><span class="sxs-lookup"><span data-stu-id="c1063-179">It is often useful to keep track of scope within the XAML node stream by implementing a frame and stack concept.</span></span> <span data-ttu-id="c1063-180">To platí zejména v případě, že aktivně upravujete datový proud uzlu, když jste v něm.</span><span class="sxs-lookup"><span data-stu-id="c1063-180">This is particularly true if you are actively adjusting the node stream while you are in it.</span></span> <span data-ttu-id="c1063-181">Podpora rámce a zásobníku, které implementujete jako `StartObject` součást `GetObject`logiky smyčky uzlu, může počítat (nebo ) a `EndObject` obory při sestupu do struktury uzlu XAML, pokud je struktura myšlenka z hlediska modelu DOM.</span><span class="sxs-lookup"><span data-stu-id="c1063-181">The frame and stack support that you implement as part of your node loop logic could count `StartObject` (or `GetObject`) and `EndObject` scopes as you descend into a XAML node structure if the structure is thought of from a DOM perspective.</span></span>

## <a name="traversing-and-entering-object-nodes"></a><span data-ttu-id="c1063-182">Procházení a zadávání uzlů objektů</span><span class="sxs-lookup"><span data-stu-id="c1063-182">Traversing and Entering Object Nodes</span></span>

<span data-ttu-id="c1063-183">První uzel v datovém proudu uzlu při otevření čtečkou XAML je uzel počátečního objektu kořenového objektu.</span><span class="sxs-lookup"><span data-stu-id="c1063-183">The first node in a node stream when it is opened by a XAML reader is the start-object node of the root object.</span></span> <span data-ttu-id="c1063-184">Podle definice je tento objekt vždy uzel jednoho objektu a nemá žádné partnery.</span><span class="sxs-lookup"><span data-stu-id="c1063-184">By definition, this object is always a single object node and has no peers.</span></span> <span data-ttu-id="c1063-185">V každém příkladu XAML reálného světa je kořenový objekt definován tak, aby měl jednu nebo více vlastností, které mají více objektů, a tyto vlastnosti mají členské uzly.</span><span class="sxs-lookup"><span data-stu-id="c1063-185">In any real-world XAML example, the root object is defined to have one or more properties that hold more objects, and these properties have member nodes.</span></span> <span data-ttu-id="c1063-186">Členské uzly pak mají jeden nebo více uzlů objektu nebo může také ukončit v uzlu hodnoty místo.</span><span class="sxs-lookup"><span data-stu-id="c1063-186">The member nodes then have one or more object nodes, or might also terminate in a value node instead.</span></span> <span data-ttu-id="c1063-187">Kořenový objekt obvykle definuje názvové obory XAML, které jsou syntakticky přiřazeny jako atributy v textové značce XAML, ale mapují se na typ `Namescope` uzlu v reprezentaci datového proudu uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-187">The root object typically defines XAML namescopes, which are syntactically assigned as attributes in the XAML text markup but map to a `Namescope` node type in the XAML node stream representation.</span></span>

<span data-ttu-id="c1063-188">Vezměme si následující příklad XAML (to je libovolný XAML, není zálohována existující typy v .NET).</span><span class="sxs-lookup"><span data-stu-id="c1063-188">Consider the following XAML example (this is arbitrary XAML, not backed by existing types in .NET).</span></span> <span data-ttu-id="c1063-189">`FavorCollection` Předpokládejme, že v `List<T>` tomto `Favor` `Balloon` objektovém modelu je , a `NoiseMaker` jsou přiřaditelné `Favor`k , `Balloon.Color` vlastnost je zálohována objekt podobný `Color` tomu, jak WPF definuje barvy jako známé názvy barev a `Color` podporuje převaděč typů pro syntaxi atributu.</span><span class="sxs-lookup"><span data-stu-id="c1063-189">Assume that in this object model, `FavorCollection` is `List<T>` of `Favor`, `Balloon` and `NoiseMaker` are assignable to `Favor`, the `Balloon.Color` property is backed by a `Color` object similar to how WPF defines colors as known color names, and `Color` supports a type converter for attribute syntax.</span></span>

|<span data-ttu-id="c1063-190">Značky XAML</span><span class="sxs-lookup"><span data-stu-id="c1063-190">XAML markup</span></span>|<span data-ttu-id="c1063-191">Výsledný datový proud uzlu XAML</span><span class="sxs-lookup"><span data-stu-id="c1063-191">Resulting XAML node stream</span></span>|
|-----------------|--------------------------------|
|`<Party`|<span data-ttu-id="c1063-192">`Namespace`uzel pro`Party`</span><span class="sxs-lookup"><span data-stu-id="c1063-192">`Namespace` node for `Party`</span></span>|
|`xmlns="PartyXamlNamespace">`|<span data-ttu-id="c1063-193">`StartObject`uzel pro`Party`</span><span class="sxs-lookup"><span data-stu-id="c1063-193">`StartObject` node for `Party`</span></span>|
|`<Party.Favors>`|<span data-ttu-id="c1063-194">`StartMember`uzel pro`Party.Favors`</span><span class="sxs-lookup"><span data-stu-id="c1063-194">`StartMember` node for `Party.Favors`</span></span>|
||<span data-ttu-id="c1063-195">`StartObject`uzel pro implicitní`FavorCollection`</span><span class="sxs-lookup"><span data-stu-id="c1063-195">`StartObject` node for implicit `FavorCollection`</span></span>|
||<span data-ttu-id="c1063-196">`StartMember`uzel pro `FavorCollection` vlastnost implicitní položky.</span><span class="sxs-lookup"><span data-stu-id="c1063-196">`StartMember` node for implicit `FavorCollection` items property.</span></span>|
|`<Balloon`|<span data-ttu-id="c1063-197">`StartObject`uzel pro`Balloon`</span><span class="sxs-lookup"><span data-stu-id="c1063-197">`StartObject` node for `Balloon`</span></span>|
|`Color="Red"`|<span data-ttu-id="c1063-198">`StartMember`uzel pro`Color`</span><span class="sxs-lookup"><span data-stu-id="c1063-198">`StartMember` node for `Color`</span></span><br /><br /> <span data-ttu-id="c1063-199">`Value`uzel pro řetězec hodnoty atributu`"Red"`</span><span class="sxs-lookup"><span data-stu-id="c1063-199">`Value` node for the attribute value string `"Red"`</span></span><br /><br /> <span data-ttu-id="c1063-200">`EndMember`Pro`Color`</span><span class="sxs-lookup"><span data-stu-id="c1063-200">`EndMember` for `Color`</span></span>|
|`HasHelium="True"`|<span data-ttu-id="c1063-201">`StartMember`uzel pro`HasHelium`</span><span class="sxs-lookup"><span data-stu-id="c1063-201">`StartMember` node for `HasHelium`</span></span><br /><br /> <span data-ttu-id="c1063-202">`Value`uzel pro řetězec hodnoty atributu`"True"`</span><span class="sxs-lookup"><span data-stu-id="c1063-202">`Value` node for the attribute value string `"True"`</span></span><br /><br /> <span data-ttu-id="c1063-203">`EndMember`Pro`HasHelium`</span><span class="sxs-lookup"><span data-stu-id="c1063-203">`EndMember` for `HasHelium`</span></span>|
|`>`|<span data-ttu-id="c1063-204">`EndObject`Pro`Balloon`</span><span class="sxs-lookup"><span data-stu-id="c1063-204">`EndObject` for `Balloon`</span></span>|
|`<NoiseMaker>Loudest</NoiseMaker>`|<span data-ttu-id="c1063-205">`StartObject`uzel pro`NoiseMaker`</span><span class="sxs-lookup"><span data-stu-id="c1063-205">`StartObject` node for `NoiseMaker`</span></span><br /><br /> <span data-ttu-id="c1063-206">`StartMember`uzel pro`_Initialization`</span><span class="sxs-lookup"><span data-stu-id="c1063-206">`StartMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="c1063-207">`Value`uzel pro řetězec hodnoty inicializace`"Loudest"`</span><span class="sxs-lookup"><span data-stu-id="c1063-207">`Value` node for the initialization value string `"Loudest"`</span></span><br /><br /> <span data-ttu-id="c1063-208">`EndMember`uzel pro`_Initialization`</span><span class="sxs-lookup"><span data-stu-id="c1063-208">`EndMember` node for `_Initialization`</span></span><br /><br /> <span data-ttu-id="c1063-209">`EndObject`Pro`NoiseMaker`</span><span class="sxs-lookup"><span data-stu-id="c1063-209">`EndObject` for `NoiseMaker`</span></span>|
||<span data-ttu-id="c1063-210">`EndMember`uzel pro `FavorCollection` vlastnost implicitní položky.</span><span class="sxs-lookup"><span data-stu-id="c1063-210">`EndMember` node for implicit `FavorCollection` items property.</span></span>|
||<span data-ttu-id="c1063-211">`EndObject`uzel pro implicitní`FavorCollection`</span><span class="sxs-lookup"><span data-stu-id="c1063-211">`EndObject` node for implicit `FavorCollection`</span></span>|
|`</Party.Favors>`|<span data-ttu-id="c1063-212">`EndMember`Pro`Favors`</span><span class="sxs-lookup"><span data-stu-id="c1063-212">`EndMember` for `Favors`</span></span>|
|`</Party>`|<span data-ttu-id="c1063-213">`EndObject`Pro`Party`</span><span class="sxs-lookup"><span data-stu-id="c1063-213">`EndObject` for `Party`</span></span>|

<span data-ttu-id="c1063-214">V datovém proudu uzlu XAML se můžete spolehnout na následující chování:</span><span class="sxs-lookup"><span data-stu-id="c1063-214">In the XAML node stream, you can rely on the following behavior:</span></span>

- <span data-ttu-id="c1063-215">Pokud `Namespace` uzel existuje, je přidán do datového `StartObject` proudu bezprostředně před, který `xmlns`deklaroval obor názvů XAML s .</span><span class="sxs-lookup"><span data-stu-id="c1063-215">If a `Namespace` node exists, it is added to the stream immediately before the `StartObject` that declared the XAML namespace with `xmlns`.</span></span> <span data-ttu-id="c1063-216">Podívejte se na předchozí tabulku s XAML a ukázkový datový proud uzlu znovu.</span><span class="sxs-lookup"><span data-stu-id="c1063-216">Look at the previous table with the XAML and example node stream again.</span></span> <span data-ttu-id="c1063-217">Všimněte `StartObject` si, jak a `Namespace` uzly se zdají být provedeny oproti jejich prohlášení pozice v textové značky.</span><span class="sxs-lookup"><span data-stu-id="c1063-217">Notice how the `StartObject` and `Namespace` nodes seem to be transposed versus their declaration positions in text markup.</span></span> <span data-ttu-id="c1063-218">Toto je zástupce chování, kde uzly oboru názvů vždy zobrazí před uzlu, které platí pro v datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="c1063-218">This is representative of the behavior where the namespace nodes always appear ahead of the node they apply to in the node stream.</span></span> <span data-ttu-id="c1063-219">Účelem tohoto návrhu je, že informace o oboru názvů je důležité pro zapisovače objektů a musí být známy před objektem zapisovač pokusí provést mapování typu nebo jinak zpracovat objekt.</span><span class="sxs-lookup"><span data-stu-id="c1063-219">The purpose of this design is that the namespace information is vital to object writers and must be known before the object writer attempts to perform type mapping or otherwise process the object.</span></span> <span data-ttu-id="c1063-220">Umístění informací o oboru názvů XAML před rozsah aplikace v datovém proudu usnadňuje vždy zpracování datového proudu uzlu v prezentovaném pořadí.</span><span class="sxs-lookup"><span data-stu-id="c1063-220">Placing the XAML namespace information ahead of its application scope in the stream makes it simpler to always process the node stream in its presented order.</span></span>

- <span data-ttu-id="c1063-221">Z důvodu výše uvedeného zvažování `Namespace` je jeden nebo více uzlů, které si přečtete jako první ve `StartObject` většině případů značek v reálném světě při procházení uzlů od začátku, nikoli kořenového adresáře.</span><span class="sxs-lookup"><span data-stu-id="c1063-221">Because of the above consideration, it is one or more `Namespace` nodes that you read first in most real-world markup cases when traversing nodes from the start, not the `StartObject` of the root.</span></span>

- <span data-ttu-id="c1063-222">Uzel `StartObject` může být `StartMember`následován `Value`, nebo `EndObject`okamžité .</span><span class="sxs-lookup"><span data-stu-id="c1063-222">A `StartObject` node can be followed by `StartMember`, `Value`, or an immediate `EndObject`.</span></span> <span data-ttu-id="c1063-223">Nikdy není bezprostředně následován jiným `StartObject`.</span><span class="sxs-lookup"><span data-stu-id="c1063-223">It is never followed immediately by another `StartObject`.</span></span>

- <span data-ttu-id="c1063-224">A `StartMember` může následovat `StartObject`a `Value`, nebo `EndMember`okamžité .</span><span class="sxs-lookup"><span data-stu-id="c1063-224">A `StartMember` can be followed by a `StartObject`, `Value`, or an immediate `EndMember`.</span></span> <span data-ttu-id="c1063-225">Může následovat `GetObject`, pro členy, kde hodnota má pocházet z existující hodnoty `StartObject` nadřazeného objektu, nikoli který by instanci nové hodnoty.</span><span class="sxs-lookup"><span data-stu-id="c1063-225">It can be followed by `GetObject`, for members where the value is supposed to come from an existing value of the parent object rather than a `StartObject` that would instantiate a new value.</span></span> <span data-ttu-id="c1063-226">Může také následovat `Namespace` uzel, který se vztahuje na `StartObject`nadcházející .</span><span class="sxs-lookup"><span data-stu-id="c1063-226">It can also be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span> <span data-ttu-id="c1063-227">Nikdy není bezprostředně následován jiným `StartMember`.</span><span class="sxs-lookup"><span data-stu-id="c1063-227">It is never followed immediately by another `StartMember`.</span></span>

- <span data-ttu-id="c1063-228">Uzel `Value` představuje samotnou hodnotu; neexistuje žádná "EndValue".</span><span class="sxs-lookup"><span data-stu-id="c1063-228">A `Value` node represents the value itself; there is no "EndValue".</span></span> <span data-ttu-id="c1063-229">Může následovat pouze `EndMember`.</span><span class="sxs-lookup"><span data-stu-id="c1063-229">It can be followed only by an `EndMember`.</span></span>

  - <span data-ttu-id="c1063-230">XAML inicializace text objektu, které mohou být použity konstrukce nemá za následek objekt value struktury.</span><span class="sxs-lookup"><span data-stu-id="c1063-230">XAML initialization text of the object as might be used by construction does not result in an Object-Value structure.</span></span> <span data-ttu-id="c1063-231">Místo toho je vytvořen uzel vyhrazeného člena pro člena s názvem. `_Initialization`</span><span class="sxs-lookup"><span data-stu-id="c1063-231">Instead, a dedicated member node for a member named `_Initialization` is created.</span></span> <span data-ttu-id="c1063-232">a tento uzel člena obsahuje řetězec inicializační hodnoty.</span><span class="sxs-lookup"><span data-stu-id="c1063-232">and that member node contains the initialization value string.</span></span> <span data-ttu-id="c1063-233">Pokud existuje, `_Initialization` je vždy `StartMember`první .</span><span class="sxs-lookup"><span data-stu-id="c1063-233">If it exists, `_Initialization` is always the first `StartMember`.</span></span> <span data-ttu-id="c1063-234">`_Initialization`může být kvalifikován v některých reprezentacích služeb XAML s jazykem `_Initialization` XAML XAML namescope, aby bylo jasné, že není definovanou vlastností v typech zálohování.</span><span class="sxs-lookup"><span data-stu-id="c1063-234">`_Initialization` may be qualified in some XAML services representations with the XAML language XAML namescope, to clarify that `_Initialization` is not a defined property in backing types.</span></span>

  - <span data-ttu-id="c1063-235">Kombinace Člen-Hodnota představuje nastavení atributu hodnoty.</span><span class="sxs-lookup"><span data-stu-id="c1063-235">A Member-Value combination represents an attribute setting of the value.</span></span> <span data-ttu-id="c1063-236">Nakonec může být převaděč hodnoty zapojených do zpracování této hodnoty a hodnota je prostý řetězec.</span><span class="sxs-lookup"><span data-stu-id="c1063-236">There might eventually be a value converter involved in processing this value, and the value is a plain string.</span></span> <span data-ttu-id="c1063-237">To však není vyhodnocena, dokud zapisovač objektu XAML zpracovává tento datový proud uzlu.</span><span class="sxs-lookup"><span data-stu-id="c1063-237">However, that is not evaluated until a XAML object writer processes this node stream.</span></span> <span data-ttu-id="c1063-238">Zapisovač objektů XAML má potřebný kontext schématu XAML, mapování systému typů a další podporu potřebnou pro převody hodnot.</span><span class="sxs-lookup"><span data-stu-id="c1063-238">The XAML object writer possesses the necessary XAML schema context, type system mapping, and other support needed for value conversions.</span></span>

- <span data-ttu-id="c1063-239">Uzel `EndMember` může být následován `StartMember` uztelem pro následující `EndObject` člen nebo uztelem pro vlastníka člena.</span><span class="sxs-lookup"><span data-stu-id="c1063-239">An `EndMember` node can be followed by a `StartMember` node for a subsequent member, or by an `EndObject` node for the member owner.</span></span>

- <span data-ttu-id="c1063-240">Po `EndObject` uzlu může následovat `EndMember` uzel.</span><span class="sxs-lookup"><span data-stu-id="c1063-240">An `EndObject` node can be followed by an `EndMember` node.</span></span> <span data-ttu-id="c1063-241">Může také následovat `StartObject` uzel pro případy, kdy jsou objekty partnery v položkách kolekce.</span><span class="sxs-lookup"><span data-stu-id="c1063-241">It can also be followed by a `StartObject` node for cases where the objects are peers in a collection's items.</span></span> <span data-ttu-id="c1063-242">Nebo může následovat `Namespace` uzel, který se vztahuje na `StartObject`nadcházející .</span><span class="sxs-lookup"><span data-stu-id="c1063-242">Or it can be followed by a `Namespace` node, which applies to an upcoming `StartObject`.</span></span>

  - <span data-ttu-id="c1063-243">Pro jedinečný případ uzavření celého datového `EndObject` proudu uzlu kořene není následován nic; čtečka je nyní konec souboru <xref:System.Xaml.XamlReader.Read%2A> `false`a vrátí .</span><span class="sxs-lookup"><span data-stu-id="c1063-243">For the unique case of closing the entire node stream, the `EndObject` of the root is not followed by anything; the reader is now end-of-file, and <xref:System.Xaml.XamlReader.Read%2A> returns `false`.</span></span>

## <a name="value-converters-and-the-xaml-node-stream"></a><span data-ttu-id="c1063-244">Převaděče hodnot a datový proud uzlů XAML</span><span class="sxs-lookup"><span data-stu-id="c1063-244">Value Converters and the XAML Node Stream</span></span>

<span data-ttu-id="c1063-245">Převaděč hodnot je obecný termín pro rozšíření značek, převaděč typu (včetně serializárů hodnot) nebo jinou vyhrazenou třídu, která je vykazována jako převaděč hodnot prostřednictvím systému typu XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-245">A value converter is a general term for a markup extension, a type converter (including value serializers) or another dedicated class that is reported as a value converter through the XAML type system.</span></span> <span data-ttu-id="c1063-246">V datovém proudu uzlu XAML mají použití převaděče typů a použití rozšíření značek velmi odlišné reprezentace.</span><span class="sxs-lookup"><span data-stu-id="c1063-246">In the XAML node stream, a type converter usage and a markup extension usage have very different representations.</span></span>

### <a name="type-converters-in-the-xaml-node-stream"></a><span data-ttu-id="c1063-247">Převaděče typů v datovém proudu uzlů XAML</span><span class="sxs-lookup"><span data-stu-id="c1063-247">Type Converters in the XAML Node Stream</span></span>

<span data-ttu-id="c1063-248">Sada atributů, která nakonec vede k použití převaděče typu, je uvedena v datovém proudu uzlu XAML jako hodnota člena.</span><span class="sxs-lookup"><span data-stu-id="c1063-248">An attribute set that eventually results in a type converter usage is reported in the XAML node stream as a value of a member.</span></span> <span data-ttu-id="c1063-249">Datový proud uzlu XAML se nepokouší vytvořit objekt instance převaděče typu a předat mu hodnotu.</span><span class="sxs-lookup"><span data-stu-id="c1063-249">The XAML node stream does not attempt to produce a type converter instance object and pass the value to it.</span></span> <span data-ttu-id="c1063-250">Použití implementace převodu typu převodu vyžaduje vyvolání kontextu schématu XAML a jeho použití pro mapování typů.</span><span class="sxs-lookup"><span data-stu-id="c1063-250">Using a type converter's conversion implementation requires invoking the XAML schema context and using it for type-mapping.</span></span> <span data-ttu-id="c1063-251">I určení, která třída převaděče typu by měla být použita ke zpracování hodnoty, vyžaduje kontext schématu XAML nepřímo.</span><span class="sxs-lookup"><span data-stu-id="c1063-251">Even determining which type converter class should be used to process the value requires the XAML schema context indirectly.</span></span> <span data-ttu-id="c1063-252">Při použití výchozího kontextu schématu XAML jsou tyto informace k dispozici v systému typu XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-252">When you use the default XAML schema context, that information is available from the XAML type system.</span></span> <span data-ttu-id="c1063-253">Pokud potřebujete informace o třídě převaděče typu na úrovni datového proudu uzlu XAML před <xref:System.Xaml.XamlMember> připojením k zapisovateli XAML, můžete je získat z informací o nastaveném členu.</span><span class="sxs-lookup"><span data-stu-id="c1063-253">If you need the type converter class information at the XAML node stream level before connection to a XAML writer, you can obtain it from the <xref:System.Xaml.XamlMember> information of the member being set.</span></span> <span data-ttu-id="c1063-254">Ale jinak by měl být vstup převaděče typu zachován v datovém proudu uzlu XAML jako prostá hodnota, dokud nebude provedena zbývající část operací, které vyžadují systém mapování typu a kontext schématu XAML, například vytvoření objektu zapisovačem objektu XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-254">But otherwise, type converter input should be preserved in the XAML node stream as a plain value until the remainder of operations that require the type-mapping system and XAML schema context are performed, for example the object creation by a XAML object writer.</span></span>

<span data-ttu-id="c1063-255">Zvažte například následující osnovu definice třídy a použití XAML pro něj:</span><span class="sxs-lookup"><span data-stu-id="c1063-255">For example, consider the following class definition outline and XAML usage for it:</span></span>

```csharp
public class BoardSizeConverter : TypeConverter {
  //converts from string to an int[2] by splitting on an "x" char
}
public class GameBoard {
  [TypeConverter(typeof(BoardSizeConverter))]
  public int[] BoardSize; //2x2 array, initialization not shown
}
```

```xaml
<GameBoard BoardSize="8x8"/>
```

<span data-ttu-id="c1063-256">Textová reprezentace datového proudu uzlu XAML pro toto použití může být vyjádřena takto:</span><span class="sxs-lookup"><span data-stu-id="c1063-256">A text representation of the XAML node stream for this usage could be expressed as the following:</span></span>

<span data-ttu-id="c1063-257">`StartObject`s <xref:System.Xaml.XamlType> reprezentací`GameBoard`</span><span class="sxs-lookup"><span data-stu-id="c1063-257">`StartObject` with <xref:System.Xaml.XamlType> representing `GameBoard`</span></span>

<span data-ttu-id="c1063-258">`StartMember`s <xref:System.Xaml.XamlMember> reprezentací`BoardSize`</span><span class="sxs-lookup"><span data-stu-id="c1063-258">`StartMember` with <xref:System.Xaml.XamlMember> representing `BoardSize`</span></span>

<span data-ttu-id="c1063-259">`Value`uzel s textovým`8x8`řetězcem " "</span><span class="sxs-lookup"><span data-stu-id="c1063-259">`Value` node, with text string "`8x8`"</span></span>

<span data-ttu-id="c1063-260">`EndMember`Odpovídá`BoardSize`</span><span class="sxs-lookup"><span data-stu-id="c1063-260">`EndMember` matches `BoardSize`</span></span>

<span data-ttu-id="c1063-261">`EndObject`Odpovídá`GameBoard`</span><span class="sxs-lookup"><span data-stu-id="c1063-261">`EndObject` matches `GameBoard`</span></span>

<span data-ttu-id="c1063-262">Všimněte si, že v tomto datovém proudu uzlu neexistuje žádná instance převaděče typu.</span><span class="sxs-lookup"><span data-stu-id="c1063-262">Notice that there is no type converter instance in this node stream.</span></span> <span data-ttu-id="c1063-263">Ale můžete získat informace převaděče <xref:System.Xaml.XamlMember> `BoardSize`typu voláním <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> na pro .</span><span class="sxs-lookup"><span data-stu-id="c1063-263">But you can get type converter information by calling <xref:System.Xaml.XamlMember.TypeConverter%2A?displayProperty=nameWithType> on the <xref:System.Xaml.XamlMember> for `BoardSize`.</span></span> <span data-ttu-id="c1063-264">Pokud máte platný kontext schématu XAML, můžete také vyvolat metody převaděče získáním instance z <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>.</span><span class="sxs-lookup"><span data-stu-id="c1063-264">If you have a valid XAML schema context, you can also invoke the converter methods by obtaining an instance from <xref:System.Xaml.Schema.XamlValueConverter%601.ConverterInstance%2A>.</span></span>

### <a name="markup-extensions-in-the-xaml-node-stream"></a><span data-ttu-id="c1063-265">Rozšíření značek v datovém proudu uzlů XAML</span><span class="sxs-lookup"><span data-stu-id="c1063-265">Markup Extensions in the XAML Node Stream</span></span>

<span data-ttu-id="c1063-266">Použití rozšíření značek je hlášeno v datovém proudu uzlu XAML jako uzel objektu v rámci člena, kde objekt představuje instanci rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="c1063-266">A markup extension usage is reported in the XAML node stream as an object node within a member, where the object represents a markup extension instance.</span></span> <span data-ttu-id="c1063-267">Proto je použití rozšíření značek je uveden a explicitněji v reprezentaci datového proudu uzlu než použití převaděče typu a nese další informace.</span><span class="sxs-lookup"><span data-stu-id="c1063-267">Thus a markup extension usage is presented more explicitly in the node stream representation than a type converter usage is, and carries more information.</span></span> <span data-ttu-id="c1063-268"><xref:System.Xaml.XamlMember>informace vám nemohly říct nic o rozšíření značek, protože použití je situační a liší se v každém možném případě značky; není vyhrazena a implicitní podle typu nebo člena, jako je tomu v případě převaděčů typu.</span><span class="sxs-lookup"><span data-stu-id="c1063-268"><xref:System.Xaml.XamlMember> information could not have told you anything about the markup extension, because the usage is situational and varies in each possible markup case; it is not dedicated and implicit per type or member as is the case with type converters.</span></span>

<span data-ttu-id="c1063-269">Reprezentace datového proudu uzlu rozšíření značek jako uzly objektu je případ i v případě, že použití rozšíření značky byla provedena ve formě atributu v text značky XAML (což je často případ).</span><span class="sxs-lookup"><span data-stu-id="c1063-269">The node stream representation of markup extensions as object nodes is the case even if the markup extension usage was made in attribute form in the XAML text markup (which is often the case).</span></span> <span data-ttu-id="c1063-270">Použití rozšíření značek, která používají formulář elementu explicitní objekt, jsou zpracována stejným způsobem.</span><span class="sxs-lookup"><span data-stu-id="c1063-270">Markup extension usages that used an explicit object element form are treated the same way.</span></span>

<span data-ttu-id="c1063-271">V rámci uzlu objektu rozšíření značek mohou být členy tohoto rozšíření značek.</span><span class="sxs-lookup"><span data-stu-id="c1063-271">Within a markup extension object node, there may be members of that markup extension.</span></span> <span data-ttu-id="c1063-272">Reprezentace datového proudu uzlu XAML zachovává použití tohoto rozšíření značek, ať už se jedná o použití pozičního parametru nebo použití s explicitními pojmenovanými parametry.</span><span class="sxs-lookup"><span data-stu-id="c1063-272">The XAML node stream representation preserves the usage of that markup extension, whether that be a positional parameter usage or a usage with explicit named parameters.</span></span>

<span data-ttu-id="c1063-273">Pro použití pozičního parametru obsahuje datový proud uzlu XAML vlastnost `_PositionalParameters` definovanou jazykem XAML, která zaznamenává použití.</span><span class="sxs-lookup"><span data-stu-id="c1063-273">For a positional parameter usage, the XAML node stream contains a XAML language-defined property `_PositionalParameters` that records the usage.</span></span> <span data-ttu-id="c1063-274">Tato vlastnost je <xref:System.Collections.Generic.List%601> <xref:System.Object> obecný s omezením.</span><span class="sxs-lookup"><span data-stu-id="c1063-274">This property is a generic <xref:System.Collections.Generic.List%601> with <xref:System.Object> constraint.</span></span> <span data-ttu-id="c1063-275">Omezení je objekt a ne řetězec, protože je možné, že použití pozičního parametru může obsahovat vnořené použití rozšíření značek v něm.</span><span class="sxs-lookup"><span data-stu-id="c1063-275">The constraint is object and not string because conceivably a positional parameter usage could contain nested markup extension usages within it.</span></span> <span data-ttu-id="c1063-276">Chcete-li získat přístup k pozičníparametry z použití, můžete iterovat prostřednictvím seznamu a použít indexery pro jednotlivé hodnoty seznamu.</span><span class="sxs-lookup"><span data-stu-id="c1063-276">To access the positional parameters from the usage, you could iterate through the list and use the indexers for individual list values.</span></span>

<span data-ttu-id="c1063-277">Pro použití pojmenovanéparametr, každý pojmenovaný parametr je reprezentován jako uzel člena tohoto názvu v datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="c1063-277">For a named parameter usage, each named parameter is represented as a member node of that name in the node stream.</span></span> <span data-ttu-id="c1063-278">Hodnoty členů nejsou nutně řetězce, protože může být vnořené použití rozšíření značky.</span><span class="sxs-lookup"><span data-stu-id="c1063-278">The member values are not necessarily strings, because there could be a nested markup extension usage.</span></span>

<span data-ttu-id="c1063-279">`ProvideValue`z rozšíření značek ještě není vyvolána.</span><span class="sxs-lookup"><span data-stu-id="c1063-279">`ProvideValue` from the markup extension is not yet invoked.</span></span> <span data-ttu-id="c1063-280">Je však vyvolána, pokud připojíte čtečku XAML `WriteEndObject` a zapisovač XAML tak, aby byl vyvolán v uzlu rozšíření značek při jeho zkoumání v datovém proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="c1063-280">However, it is invoked if you connect a XAML reader and XAML writer so that `WriteEndObject` is invoked on the markup extension node when you examine it in the node stream.</span></span> <span data-ttu-id="c1063-281">Z tohoto důvodu obvykle potřebujete stejný kontext schématu XAML k dispozici jako by se použít k vytvoření grafu objektu na cestě zatížení.</span><span class="sxs-lookup"><span data-stu-id="c1063-281">For this reason, you generally need the same XAML schema context available as would be used in order to form the object graph on the load path.</span></span> <span data-ttu-id="c1063-282">V `ProvideValue` opačném případě z jakékoli rozšíření značky může vyvolat výjimky zde, protože nemá očekávané služby k dispozici.</span><span class="sxs-lookup"><span data-stu-id="c1063-282">Otherwise, `ProvideValue` from any markup extension can throw exceptions here, because it does not have expected services available.</span></span>

## <a name="xaml-and-xml-language-defined-members-in-the-xaml-node-stream"></a><span data-ttu-id="c1063-283">Členové xaml a xml v datovém proudu uzlů XAML</span><span class="sxs-lookup"><span data-stu-id="c1063-283">XAML and XML Language-Defined Members in the XAML Node Stream</span></span>

<span data-ttu-id="c1063-284">Některé členy jsou zavedeny do datového proudu uzlu XAML z důvodu interpretace a konvence <xref:System.Xaml.XamlMember> čtečky XAML, namísto prostřednictvím explicitní vyhledávání nebo konstrukce.</span><span class="sxs-lookup"><span data-stu-id="c1063-284">Certain members are introduced to a XAML node stream because of interpretations and conventions of a XAML reader, instead of through an explicit <xref:System.Xaml.XamlMember> lookup or construction.</span></span> <span data-ttu-id="c1063-285">Často tyto členy jsou direktivy XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-285">Often, these members are XAML directives.</span></span> <span data-ttu-id="c1063-286">V některých případech je akt čtení XAML, který zavádí směrnice do datového proudu uzlu XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-286">In some cases, it is the act of reading the XAML that introduces the directive into the XAML node stream.</span></span> <span data-ttu-id="c1063-287">Jinými slovy původní vstupní text XAML explicitně nespecifikoval členské směrnice, ale čtečka XAML vloží direktivu, aby splnila strukturální konvence XAML a informace o sestavě v datovém proudu uzlu XAML před ztrátou této informace.</span><span class="sxs-lookup"><span data-stu-id="c1063-287">In other words, the original input XAML text did not explicitly specify the member directive, but the XAML reader inserts the directive in order to satisfy a structural XAML convention and report information in the XAML node stream before that information is lost.</span></span>

<span data-ttu-id="c1063-288">Následující seznam bere na vědomí všechny případy, kdy se očekává, že čtečka XAML zavede uzel člena xaml směrnice a jak je tento uzel člena identifikován v implementacích služby .NET XAML Services.</span><span class="sxs-lookup"><span data-stu-id="c1063-288">The following list notes all cases where a XAML reader is expected to introduce a directive XAML member node, and how that member node is identified in .NET XAML Services implementations.</span></span>

- <span data-ttu-id="c1063-289">**Inicializační text pro uzel objektu:** Název tohoto členského uzlu `_Initialization`je , představuje direktivu XAML a je definován v oboru názvů XAML jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-289">**Initialization text for an object node:** The name of this member node is `_Initialization`, it represents a XAML directive, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="c1063-290">Můžete získat statickou entitu pro něj z <xref:System.Xaml.XamlLanguage.Initialization%2A>.</span><span class="sxs-lookup"><span data-stu-id="c1063-290">You can get a static entity for it from <xref:System.Xaml.XamlLanguage.Initialization%2A>.</span></span>

- <span data-ttu-id="c1063-291">**Poziční parametry pro rozšíření značek:** Název tohoto členského uzlu `_PositionalParameters`je a je definován v oboru názvů XAML jazyka XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-291">**Positional parameters for a markup extension:** The name of this member node is `_PositionalParameters`, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="c1063-292">Vždy obsahuje obecný seznam objektů, z nichž každý je poziční `,` parametr předem oddělen rozdělením na znak oddělovače, jak je dodáván ve vstupním XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-292">It always contains a generic list of objects, each of which is a positional parameter pre-separated by splitting on the `,` delimiter character as supplied in the input XAML.</span></span> <span data-ttu-id="c1063-293">Můžete získat statickou entitu pro poziční parametry směrnice z <xref:System.Xaml.XamlLanguage.PositionalParameters%2A>.</span><span class="sxs-lookup"><span data-stu-id="c1063-293">You can get a static entity for the positional parameters directive from <xref:System.Xaml.XamlLanguage.PositionalParameters%2A>.</span></span>

- <span data-ttu-id="c1063-294">**Neznámý obsah:** Název tohoto uzlu člena `_UnknownContent`je .</span><span class="sxs-lookup"><span data-stu-id="c1063-294">**Unknown content:** The name of this member node is `_UnknownContent`.</span></span> <span data-ttu-id="c1063-295">Přísně vzato, <xref:System.Xaml.XamlDirective>je to a , a je definovánv xaml jazyk xaml oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="c1063-295">Strictly speaking, it is a <xref:System.Xaml.XamlDirective>, and it is defined in the XAML language XAML namespace.</span></span> <span data-ttu-id="c1063-296">Tato směrnice se používá jako sentinel pro případy, kdy element objektu XAML obsahuje obsah ve zdrojovém XAML, ale v kontextu aktuálně dostupného schématu XAML nelze určit žádnou vlastnost obsahu.</span><span class="sxs-lookup"><span data-stu-id="c1063-296">This directive is used as a sentinel for cases where a XAML object element contains content in the source XAML but no content property can be determined under the currently available XAML schema context.</span></span> <span data-ttu-id="c1063-297">Tento případ můžete zjistit v datovém proudu uzlu XAML kontrolou členů s názvem `_UnknownContent`.</span><span class="sxs-lookup"><span data-stu-id="c1063-297">You can detect this case in a XAML node stream by checking for members named `_UnknownContent`.</span></span> <span data-ttu-id="c1063-298">Pokud žádná jiná akce je přijata v datovém proudu <xref:System.Xaml.XamlObjectWriter> uzlu načíst `WriteEndObject` xaml, `_UnknownContent` výchozí vyvolá pokus o pokus, když narazí na člena na libovolný objekt.</span><span class="sxs-lookup"><span data-stu-id="c1063-298">If no other action is taken in a load path XAML node stream, the default <xref:System.Xaml.XamlObjectWriter> throws on attempted `WriteEndObject` when it encounters the `_UnknownContent` member on any object.</span></span> <span data-ttu-id="c1063-299">Výchozí <xref:System.Xaml.XamlXmlWriter> nevyvolá a považuje člen za implicitní.</span><span class="sxs-lookup"><span data-stu-id="c1063-299">The default <xref:System.Xaml.XamlXmlWriter> does not throw, and treats the member as implicit.</span></span> <span data-ttu-id="c1063-300">Můžete získat statickou `_UnknownContent` entitu z . <xref:System.Xaml.XamlLanguage.UnknownContent%2A></span><span class="sxs-lookup"><span data-stu-id="c1063-300">You can get a static entity for `_UnknownContent` from <xref:System.Xaml.XamlLanguage.UnknownContent%2A>.</span></span>

- <span data-ttu-id="c1063-301">**Vlastnost kolekce kolekce:** Přestože záložní typ CLR třídy kolekce, která se používá pro XAML, má obvykle vyhrazenou pojmenovanou vlastnost, která obsahuje položky kolekce, tato vlastnost není známa systému typu XAML před rezolucí typu zálohování.</span><span class="sxs-lookup"><span data-stu-id="c1063-301">**Collection property of a collection:** Although the backing CLR type of a collection class that is used for XAML usually has a dedicated named property that holds the collection items, that property is not known to a XAML type system prior to backing type resolution.</span></span> <span data-ttu-id="c1063-302">Místo toho datový proud uzlu XAML zavádí zástupný `Items` symbol jako člen typu XAML kolekce.</span><span class="sxs-lookup"><span data-stu-id="c1063-302">Instead, the XAML node stream introduces an `Items` placeholder as a member of the collection XAML type.</span></span> <span data-ttu-id="c1063-303">V implementaci služby .NET XAML Services je `_Items`název této směrnice nebo člena v datovém proudu uzlu .</span><span class="sxs-lookup"><span data-stu-id="c1063-303">In .NET XAML Services implementation, the name of this directive or member in the node stream is `_Items`.</span></span> <span data-ttu-id="c1063-304">Konstanta pro tuto směrnici <xref:System.Xaml.XamlLanguage.Items%2A>lze získat z .</span><span class="sxs-lookup"><span data-stu-id="c1063-304">A constant for this directive can be obtained from <xref:System.Xaml.XamlLanguage.Items%2A>.</span></span>

    <span data-ttu-id="c1063-305">Všimněte si, že datový proud uzlu XAML může obsahovat vlastnost Items s položkami, které se nedají analyzovat na základě rozlišení typu zálohování a kontextu schématu XAML.</span><span class="sxs-lookup"><span data-stu-id="c1063-305">Note that a XAML node stream might contain an Items property with items that turn out to not be parsable based on the backing type resolution and XAML schema context.</span></span> <span data-ttu-id="c1063-306">Například:</span><span class="sxs-lookup"><span data-stu-id="c1063-306">For example,</span></span>

- <span data-ttu-id="c1063-307">**Členy definované jazykem XML:** Členové definovaní `xml:base` `xml:lang` jazykem `xml:space` XML a členové jsou `base`vykazovány jako direktivy XAML s názvem , `lang`a `space` v implementacích služby .NET XAML Services.</span><span class="sxs-lookup"><span data-stu-id="c1063-307">**XML-defined members:** The XML-defined `xml:base`, `xml:lang` and `xml:space` members are reported as XAML directives named `base`, `lang`, and `space` in .NET XAML Services implementations.</span></span> <span data-ttu-id="c1063-308">Obor názvů pro tyto jsou `http://www.w3.org/XML/1998/namespace`soubory názvů XML .</span><span class="sxs-lookup"><span data-stu-id="c1063-308">The namespace for these is the XML namespace `http://www.w3.org/XML/1998/namespace`.</span></span> <span data-ttu-id="c1063-309">Konstanty pro každou z nich <xref:System.Xaml.XamlLanguage>lze získat z .</span><span class="sxs-lookup"><span data-stu-id="c1063-309">Constants for each of these can be obtained from <xref:System.Xaml.XamlLanguage>.</span></span>

## <a name="node-order"></a><span data-ttu-id="c1063-310">Pořadí uzlů</span><span class="sxs-lookup"><span data-stu-id="c1063-310">Node Order</span></span>

<span data-ttu-id="c1063-311">V některých <xref:System.Xaml.XamlXmlReader> případech změní pořadí uzlů XAML v datovém proudu uzlu XAML oproti pořadí, ve které se uzly zobrazí, pokud jsou zobrazeny ve značkách nebo pokud jsou zpracovány jako XML.</span><span class="sxs-lookup"><span data-stu-id="c1063-311">In some cases, <xref:System.Xaml.XamlXmlReader> changes the order of XAML nodes in the XAML node stream, versus the order the nodes appear if viewed in the markup or if processed as XML.</span></span> <span data-ttu-id="c1063-312">To se provádí za účelem pořadí <xref:System.Xaml.XamlObjectWriter> uzlů tak, aby datový proud uzlu můžete zpracovat pouze pro předávání.</span><span class="sxs-lookup"><span data-stu-id="c1063-312">This is done in order to order the nodes such that a <xref:System.Xaml.XamlObjectWriter> can process the node stream in a forward-only manner.</span></span>  <span data-ttu-id="c1063-313">Ve službě .NET XAML služby čtecí zařízení XAML změní pořadí uzlů, nikoli ponechá tuto úlohu zapisovateli XAML jako optimalizaci výkonu pro spotřebitele zapisovače objektů XAML datového proudu uzlu.</span><span class="sxs-lookup"><span data-stu-id="c1063-313">In .NET XAML Services, the XAML reader reorders nodes rather than leaving this task to the XAML writer, as a performance optimization for XAML object writer consumers of the node stream.</span></span>

<span data-ttu-id="c1063-314">Některé direktivy jsou určeny konkrétně poskytnout další informace pro vytvoření objektu z prvku objektu.</span><span class="sxs-lookup"><span data-stu-id="c1063-314">Certain directives are intended specifically to provide more information for the creation of an object from an object element.</span></span> <span data-ttu-id="c1063-315">Těmito směrnicemi `Initialization` `PositionalParameters`jsou: `FactoryMethod` `Arguments`, , `TypeArguments`, . .</span><span class="sxs-lookup"><span data-stu-id="c1063-315">These directives are: `Initialization`, `PositionalParameters`, `TypeArguments`, `FactoryMethod`, `Arguments`.</span></span> <span data-ttu-id="c1063-316">.NET XAML Services XAML čtenáři pokusí umístit tyto direktivy jako `StartObject`první členy v datovém proudu uzlu po objektu , z důvodů, které jsou vysvětleny v další části.</span><span class="sxs-lookup"><span data-stu-id="c1063-316">.NET XAML Services XAML readers attempt to place these directives as the first members in the node stream following an object's `StartObject`, for reasons that are explained in the next section.</span></span>

### <a name="xamlobjectwriter-behavior-and-node-order"></a><span data-ttu-id="c1063-317">Chování a pořadí uzlů XamlObjectWriter</span><span class="sxs-lookup"><span data-stu-id="c1063-317">XamlObjectWriter Behavior and Node Order</span></span>

<span data-ttu-id="c1063-318">`StartObject`to <xref:System.Xaml.XamlObjectWriter> a není nutně signál pro zapisovač objektu XAML okamžitě vytvořit instanci objektu.</span><span class="sxs-lookup"><span data-stu-id="c1063-318">`StartObject` to a <xref:System.Xaml.XamlObjectWriter> is not necessarily a signal to the XAML object writer to immediately construct the object instance.</span></span> <span data-ttu-id="c1063-319">XAML obsahuje několik funkcí jazyka, které umožňují inicializovat objekt s dalším vstupem a nespoléhat se zcela na vyvolání konstruktoru bez parametrů k vytvoření počátečního objektu a teprve potom nastavení vlastností.</span><span class="sxs-lookup"><span data-stu-id="c1063-319">XAML includes several language features that make it possible to initialize an object with additional input, and to not rely entirely on invoking a parameterless constructor to produce the initial object, and only then setting properties.</span></span> <span data-ttu-id="c1063-320">Mezi tyto <xref:System.Windows.Markup.XamlDeferLoadAttribute>funkce patří: ; inicializační text; [x:Argumenty typu](xtypearguments-directive.md); poziční parametry rozšíření značky; metody výroby a přidružené uzly [x:Arguments](xarguments-directive.md) (XAML 2009).</span><span class="sxs-lookup"><span data-stu-id="c1063-320">These features include: <xref:System.Windows.Markup.XamlDeferLoadAttribute>; initialization text; [x:TypeArguments](xtypearguments-directive.md); positional parameters of a markup extension; factory methods and associated [x:Arguments](xarguments-directive.md) nodes (XAML 2009).</span></span> <span data-ttu-id="c1063-321">Každý z těchto případů zpoždění skutečné konstrukce objektu a protože datový proud uzlu je přeuspořádaný, zapisovač objektu XAML může spoléhat na chování skutečně konstrukce instance vždy, když je zjištěn a start člen, který není konkrétně stavební směrnice pro tento typ objektu.</span><span class="sxs-lookup"><span data-stu-id="c1063-321">Each of these cases delays the actual object construction, and because the node stream is reordered, the XAML object writer can rely on a behavior of actually constructing the instance whenever a start member is encountered that is not specifically a construction directive for that object type.</span></span>

### <a name="getobject"></a><span data-ttu-id="c1063-322">GetObject</span><span class="sxs-lookup"><span data-stu-id="c1063-322">GetObject</span></span>

<span data-ttu-id="c1063-323">`GetObject`představuje uzel XAML, kde místo vytváření nového objektu by měl zapisovač objektu XAML místo toho získat hodnotu vlastnosti obsahující objekt.</span><span class="sxs-lookup"><span data-stu-id="c1063-323">`GetObject` represents a XAML node where rather than constructing a new object, a XAML object writer should instead get the value of the object's containing property.</span></span> <span data-ttu-id="c1063-324">Typický případ, `GetObject` kdy je uzel zjištěn v datovém proudu uzlu XAML je pro objekt kolekce nebo objekt slovníku, když je obsahující vlastnost záměrně jen pro čtení v objektovém modelu typu zálohování.</span><span class="sxs-lookup"><span data-stu-id="c1063-324">A typical  case where a `GetObject` node is encountered in a XAML node stream is for a collection object or a dictionary object, when the containing property is deliberately read-only in the backing type's object model.</span></span> <span data-ttu-id="c1063-325">V tomto scénáři kolekce nebo slovníku často je vytvořen a inicializován (obvykle prázdný) logikou inicializace vlastnícího typu.</span><span class="sxs-lookup"><span data-stu-id="c1063-325">In this scenario, the collection or dictionary often is created and initialized (usually empty) by the initialization logic of an owning type.</span></span>

## <a name="see-also"></a><span data-ttu-id="c1063-326">Viz také</span><span class="sxs-lookup"><span data-stu-id="c1063-326">See also</span></span>

- <xref:System.Xaml.XamlObjectReader>
- [<span data-ttu-id="c1063-327">XAML Services</span><span class="sxs-lookup"><span data-stu-id="c1063-327">XAML Services</span></span>](index.md)
- [<span data-ttu-id="c1063-328">Obory názvů jazyka XAML</span><span class="sxs-lookup"><span data-stu-id="c1063-328">XAML Namespaces</span></span>](namespaces.md)
