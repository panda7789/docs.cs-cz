---
title: Migrace aplikací WPF na .NET Core 3,0
description: Naučte se migrovat aplikaci Windows Presentation Foundation (WPF) na .NET Core 3,0.
author: mjrousos
ms.date: 09/12/2019
ms.author: mikerou
ms.openlocfilehash: f52005e7c8a6312b8c4e09a950f1f635af1894e4
ms.sourcegitcommit: cdf5084648bf5e77970cbfeaa23f1cab3e6e234e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 02/01/2020
ms.locfileid: "82071309"
---
# <a name="migrating-wpf-apps-to-net-core"></a><span data-ttu-id="57d17-103">Migrace aplikací WPF do .NET Core</span><span class="sxs-lookup"><span data-stu-id="57d17-103">Migrating WPF apps to .NET Core</span></span>

<span data-ttu-id="57d17-104">Tento článek popisuje kroky potřebné k migraci aplikace Windows Presentation Foundation (WPF) z .NET Framework na .NET Core 3,0.</span><span class="sxs-lookup"><span data-stu-id="57d17-104">This article covers the steps necessary to migrate a Windows Presentation Foundation (WPF) app from .NET Framework to .NET Core 3.0.</span></span> <span data-ttu-id="57d17-105">Pokud nemáte k dispozici aplikaci WPF na možnost port, ale chcete si ji vyzkoušet, můžete použít ukázkovou aplikaci pro **obchodní** použití, která je dostupná na [GitHubu](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader).</span><span class="sxs-lookup"><span data-stu-id="57d17-105">If you don't have a WPF app on hand to port, but would like to try out the process, you can use the **Bean Trader** sample app available on [GitHub](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader).</span></span> <span data-ttu-id="57d17-106">Původní aplikace (cílící .NET Framework 4.7.2) je dostupná ve složce NetFx\BeanTraderClient.</span><span class="sxs-lookup"><span data-stu-id="57d17-106">The original app (targeting .NET Framework 4.7.2) is available in the NetFx\BeanTraderClient folder.</span></span> <span data-ttu-id="57d17-107">Nejdříve vyvysvětlíme kroky nezbytné pro obecně přihlašování aplikací a potom provedeme konkrétní změny, které se vztahují na ukázku pro **obchodníky v bobů** .</span><span class="sxs-lookup"><span data-stu-id="57d17-107">First we'll explain the steps necessary to port apps in general, and then we'll walk through the specific changes that apply to the **Bean Trader** sample.</span></span>

[!INCLUDE [desktop guide under construction](../../../includes/desktop-guide-preview-note.md)]

<span data-ttu-id="57d17-108">K migraci na .NET Core musíte nejdřív:</span><span class="sxs-lookup"><span data-stu-id="57d17-108">To migrate to .NET Core, you must first:</span></span>

01. <span data-ttu-id="57d17-109">Pochopení a aktualizace závislostí NuGet:</span><span class="sxs-lookup"><span data-stu-id="57d17-109">Understand and update NuGet dependencies:</span></span>

    01. <span data-ttu-id="57d17-110">Upgradujte závislosti NuGet pro použití `<PackageReference>` formátu.</span><span class="sxs-lookup"><span data-stu-id="57d17-110">Upgrade NuGet dependencies to use the `<PackageReference>` format.</span></span>
    01. <span data-ttu-id="57d17-111">Zkontrolujte závislosti na nejvyšší úrovni NuGet pro .NET Core nebo .NET Standard Compatibility.</span><span class="sxs-lookup"><span data-stu-id="57d17-111">Review top-level NuGet dependencies for .NET Core or .NET Standard compatibility.</span></span>
    01. <span data-ttu-id="57d17-112">Upgradujte balíčky NuGet na novější verze.</span><span class="sxs-lookup"><span data-stu-id="57d17-112">Upgrade NuGet packages to newer versions.</span></span>
    01. <span data-ttu-id="57d17-113">Pomocí [analyzátoru přenositelnosti .NET](../../standard/analyzers/portability-analyzer.md) pochopíte závislosti rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="57d17-113">Use the [.NET Portability Analyzer](../../standard/analyzers/portability-analyzer.md) to understand .NET dependencies.</span></span>

01. <span data-ttu-id="57d17-114">Migrujte soubor projektu do nového formátu sady SDK:</span><span class="sxs-lookup"><span data-stu-id="57d17-114">Migrate the project file to the new SDK-style format:</span></span>

    01. <span data-ttu-id="57d17-115">Vyberte, jestli se mají cílit .NET Core i .NET Framework, nebo jenom .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-115">Choose whether to target both .NET Core and .NET Framework, or only .NET Core.</span></span>
    01. <span data-ttu-id="57d17-116">Zkopírujte příslušné vlastnosti souboru projektu a položky do nového souboru projektu.</span><span class="sxs-lookup"><span data-stu-id="57d17-116">Copy relevant project file properties and items to the new project file.</span></span>

01. <span data-ttu-id="57d17-117">Opravit problémy sestavení:</span><span class="sxs-lookup"><span data-stu-id="57d17-117">Fix build issues:</span></span>

    01. <span data-ttu-id="57d17-118">Přidejte odkaz na balíček [Microsoft. Windows. Compatibility](https://www.nuget.org/packages/Microsoft.Windows.Compatibility/) .</span><span class="sxs-lookup"><span data-stu-id="57d17-118">Add a reference to the [Microsoft.Windows.Compatibility](https://www.nuget.org/packages/Microsoft.Windows.Compatibility/) package.</span></span>
    01. <span data-ttu-id="57d17-119">Najděte a opravte rozdíly na úrovni rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="57d17-119">Find and fix API-level differences.</span></span>
    01. <span data-ttu-id="57d17-120">Odeberte části *App. config* jiné než `appSettings` nebo `connectionStrings`.</span><span class="sxs-lookup"><span data-stu-id="57d17-120">Remove *app.config* sections other than `appSettings` or `connectionStrings`.</span></span>
    01. <span data-ttu-id="57d17-121">V případě potřeby znovu vygenerujte generovaný kód.</span><span class="sxs-lookup"><span data-stu-id="57d17-121">Regenerate generated code, if necessary.</span></span>

01. <span data-ttu-id="57d17-122">Testování za běhu:</span><span class="sxs-lookup"><span data-stu-id="57d17-122">Runtime testing:</span></span>

    01. <span data-ttu-id="57d17-123">Potvrďte, že portovaná aplikace funguje podle očekávání.</span><span class="sxs-lookup"><span data-stu-id="57d17-123">Confirm the ported app works as expected.</span></span>
    01. <span data-ttu-id="57d17-124">Pozor na <xref:System.NotSupportedException> výjimky.</span><span class="sxs-lookup"><span data-stu-id="57d17-124">Beware of <xref:System.NotSupportedException> exceptions.</span></span>

## <a name="about-the-sample"></a><span data-ttu-id="57d17-125">O ukázce</span><span class="sxs-lookup"><span data-stu-id="57d17-125">About the sample</span></span>

<span data-ttu-id="57d17-126">Tento článek se odkazuje na [ukázkovou aplikaci v programu Bob obchodník](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader) , protože používá nejrůznější závislosti, které jsou podobné těm, které mohou mít reálné aplikace WPF.</span><span class="sxs-lookup"><span data-stu-id="57d17-126">This article references the [Bean Trader sample app](https://github.com/dotnet/windows-desktop/tree/master/Samples/BeanTrader) because it uses a variety of dependencies similar to those that real-world WPF apps might have.</span></span> <span data-ttu-id="57d17-127">Aplikace není velká, ale měla by být v souvislosti s složitostí krok z Hello World.</span><span class="sxs-lookup"><span data-stu-id="57d17-127">The app isn't large, but is meant to be a step up from 'Hello World' in terms of complexity.</span></span> <span data-ttu-id="57d17-128">Tato aplikace předvádí některé problémy, se kterými se uživatelé mohou setkat při přenosu reálných aplikací.</span><span class="sxs-lookup"><span data-stu-id="57d17-128">The app demonstrates some issues users may encounter while porting real apps.</span></span> <span data-ttu-id="57d17-129">Aplikace komunikuje se službou WCF, takže aby fungovala správně, budete také muset spustit projekt BeanTraderServer (dostupný ve stejném úložišti GitHubu) a ujistit se, že konfigurace BeanTraderClient odkazuje na správný koncový bod.</span><span class="sxs-lookup"><span data-stu-id="57d17-129">The app communicates with a WCF service, so for it to run properly, you'll also need to run the BeanTraderServer project (available in the same GitHub repository) and make sure the BeanTraderClient configuration points to the correct endpoint.</span></span> <span data-ttu-id="57d17-130">(Ve výchozím nastavení ukázka předpokládá, že server běží na stejném počítači v *http://localhost:8090*, což bude platit, pokud BeanTraderServer spustíte místně.)</span><span class="sxs-lookup"><span data-stu-id="57d17-130">(By default, the sample assumes the server is running on the same machine at *http://localhost:8090*, which will be true if you launch BeanTraderServer locally.)</span></span>

<span data-ttu-id="57d17-131">Mějte na paměti, že tato ukázková aplikace je určená k tomu, aby se ukázaly problémy s přenosem v .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-131">Keep in mind that this sample app is meant to demonstrate .NET Core porting challenges and solutions.</span></span> <span data-ttu-id="57d17-132">Není určeno k předvedení osvědčených postupů pro WPF.</span><span class="sxs-lookup"><span data-stu-id="57d17-132">It's not meant to demonstrate WPF best practices.</span></span> <span data-ttu-id="57d17-133">Ve skutečnosti záměrně obsahují některé antipatterny, abyste se ujistili, že budete mít k dispozici alespoň několik zajímavých výzev při přenosu.</span><span class="sxs-lookup"><span data-stu-id="57d17-133">In fact, it deliberately includes some anti-patterns to make sure you come across at least a couple of interesting challenges while porting.</span></span>

## <a name="getting-ready"></a><span data-ttu-id="57d17-134">Příprava</span><span class="sxs-lookup"><span data-stu-id="57d17-134">Getting ready</span></span>

<span data-ttu-id="57d17-135">Primárním problémem migrace aplikace .NET Framework do .NET Core je, že její závislosti můžou fungovat různě nebo vůbec.</span><span class="sxs-lookup"><span data-stu-id="57d17-135">The primary challenge of migrating a .NET Framework app to .NET Core is that its dependencies may work differently or not at all.</span></span> <span data-ttu-id="57d17-136">Migrace je mnohem jednodušší než při použití. mnoho balíčků NuGet teď cílí na .NET Standard.</span><span class="sxs-lookup"><span data-stu-id="57d17-136">Migration is much easier than it used to be; many NuGet packages now target .NET Standard.</span></span> <span data-ttu-id="57d17-137">Počínaje platformou .NET Core 2,0 se .NET Framework a oblasti .NET Core Surface budou podobné.</span><span class="sxs-lookup"><span data-stu-id="57d17-137">Starting with .NET Core 2.0, the .NET Framework and .NET Core surface areas have become similar.</span></span> <span data-ttu-id="57d17-138">I tak některé rozdíly (v podpoře z balíčků NuGet a v dostupných rozhraních API .NET) zůstanou zachovány.</span><span class="sxs-lookup"><span data-stu-id="57d17-138">Even so, some differences (both in support from NuGet packages and in available .NET APIs) remain.</span></span> <span data-ttu-id="57d17-139">Prvním krokem migrace je Kontrola závislostí aplikace a zajistěte, aby byly odkazy ve formátu, který je snadno migrován do .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-139">The first step in migrating is to review the app's dependencies and make sure references are in a format that's easily migrated to .NET Core.</span></span>

### <a name="upgrade-to-packagereference-nuget-references"></a><span data-ttu-id="57d17-140">Upgradovat `<PackageReference>` na reference NuGet</span><span class="sxs-lookup"><span data-stu-id="57d17-140">Upgrade to `<PackageReference>` NuGet references</span></span>

<span data-ttu-id="57d17-141">Starší projekty .NET Framework obvykle uvádějí své závislosti NuGet v souboru *Packages. config* .</span><span class="sxs-lookup"><span data-stu-id="57d17-141">Older .NET Framework projects typically list their NuGet dependencies in a *packages.config* file.</span></span> <span data-ttu-id="57d17-142">Nový formát souboru projektu ve stylu sady SDK odkazuje na balíčky NuGet [`<PackageReference>`](/nuget/consume-packages/package-references-in-project-files) jako prvky v samotném souboru csproj, nikoli v samostatném konfiguračním souboru.</span><span class="sxs-lookup"><span data-stu-id="57d17-142">The new SDK-style project file format references NuGet packages as [`<PackageReference>`](/nuget/consume-packages/package-references-in-project-files) elements in the csproj file itself rather than in a separate config file.</span></span>

<span data-ttu-id="57d17-143">Při migraci existují dvě výhody použití odkazů ve `<PackageReference>`stylu:</span><span class="sxs-lookup"><span data-stu-id="57d17-143">When migrating, there are two advantages to using `<PackageReference>`-style references:</span></span>

- <span data-ttu-id="57d17-144">Toto je styl odkazu NuGet, který je vyžadován pro nový soubor projektu .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-144">This is the style of NuGet reference that is required for the new .NET Core project file.</span></span> <span data-ttu-id="57d17-145">Pokud jste již používali `<PackageReference>`, tyto prvky souboru projektu lze zkopírovat a vložit přímo do nového projektu.</span><span class="sxs-lookup"><span data-stu-id="57d17-145">If you're already using `<PackageReference>`, those project file elements can be copied and pasted directly into the new project.</span></span>
- <span data-ttu-id="57d17-146">Na rozdíl od souboru Packages. config `<PackageReference>` elementy odkazují pouze na závislosti nejvyšší úrovně, které váš projekt závisí přímo na úrovni.</span><span class="sxs-lookup"><span data-stu-id="57d17-146">Unlike a packages.config file, `<PackageReference>` elements only refer to the top-level dependencies that your project depends on directly.</span></span> <span data-ttu-id="57d17-147">Všechny ostatní přenositelné balíčky NuGet se určí v době obnovení a zaznamenávají se do automaticky generovaného souboru obj\project.assets.JSON.</span><span class="sxs-lookup"><span data-stu-id="57d17-147">All other transitive NuGet packages will be determined at restore time and recorded in the autogenerated obj\project.assets.json file.</span></span> <span data-ttu-id="57d17-148">Díky tomu je mnohem snazší určit závislosti, které má váš projekt, což je užitečné při určování, zda budou nezbytné závislosti fungovat na rozhraní .NET Core nebo ne.</span><span class="sxs-lookup"><span data-stu-id="57d17-148">This makes it much easier to determine what dependencies your project has, which is useful when determining whether the necessary dependencies will work on .NET Core or not.</span></span>

<span data-ttu-id="57d17-149">Prvním krokem pro migraci aplikace .NET Framework do .NET Core je aktualizovat ji tak, aby používala `<PackageReference>` odkazy na NuGet.</span><span class="sxs-lookup"><span data-stu-id="57d17-149">The first step to migrating a .NET Framework app to .NET Core is to update it to use `<PackageReference>` NuGet references.</span></span> <span data-ttu-id="57d17-150">Visual Studio toto dělá jednoduché.</span><span class="sxs-lookup"><span data-stu-id="57d17-150">Visual Studio makes this simple.</span></span> <span data-ttu-id="57d17-151">Stačí kliknout pravým tlačítkem myši na soubor *Packages. config Packages* v aplikaci Visual Studio **Průzkumník řešení**a pak vyberte **migrovat Packages. config na PackageReference**.</span><span class="sxs-lookup"><span data-stu-id="57d17-151">Just right-click the project's *packages.config* file in Visual Studio's **Solution Explorer**, and then select **Migrate packages.config to PackageReference**.</span></span>

![Upgrade na PackageReference](./media/convert-project-from-net-framework/package-reference-migration.png)

<span data-ttu-id="57d17-153">Zobrazí se dialogové okno s vypočítanými závislostmi NuGet nejvyšší úrovně a s žádostí o další balíčky NuGet by se měly zvýšit na nejvyšší úroveň.</span><span class="sxs-lookup"><span data-stu-id="57d17-153">A dialog appears showing calculated top-level NuGet dependencies and asking which other NuGet packages should be promoted to top-level.</span></span> <span data-ttu-id="57d17-154">Žádný z těchto dalších balíčků nemusí být na nejvyšší úrovni pro ukázku pro obchodní dodavatele, takže můžete zrušit kontrolu všech těchto polí.</span><span class="sxs-lookup"><span data-stu-id="57d17-154">None of these other packages need to be top-level for the Bean Trader sample, so you can uncheck all of those boxes.</span></span> <span data-ttu-id="57d17-155">Pak klikněte na tlačítko **OK** a soubor *Packages. config* se odebere `<PackageReference>` a prvky se přidají do souboru projektu.</span><span class="sxs-lookup"><span data-stu-id="57d17-155">Then, click **Ok** and the *packages.config* file is removed and `<PackageReference>` elements are added to the project file.</span></span>

<span data-ttu-id="57d17-156">`<PackageReference>`– odkazy na styl neukládají balíčky NuGet místně do složky Packages.</span><span class="sxs-lookup"><span data-stu-id="57d17-156">`<PackageReference>`-style references don't store NuGet packages locally in a packages folder.</span></span> <span data-ttu-id="57d17-157">Místo toho jsou uloženy globálně jako optimalizace.</span><span class="sxs-lookup"><span data-stu-id="57d17-157">Instead, they're stored globally as an optimization.</span></span> <span data-ttu-id="57d17-158">Po dokončení migrace upravte soubor CSPROJ a odeberte všechny `<Analyzer>` prvky odkazující na analyzátory, které dříve byly z *.. Adresář \Packages*</span><span class="sxs-lookup"><span data-stu-id="57d17-158">After the migration completes, edit the csproj file and remove any `<Analyzer>` elements referring to the analyzers that previously came from the *..\packages* directory.</span></span> <span data-ttu-id="57d17-159">Nedělejte si starosti; vzhledem k tomu, že stále máte odkazy na balíček NuGet, analyzátory budou zahrnuty v projektu.</span><span class="sxs-lookup"><span data-stu-id="57d17-159">Don't worry; since you still have the NuGet package references, the analyzers will be included in the project.</span></span> <span data-ttu-id="57d17-160">Stačí vyčistit staré prvky stylu `<Analyzer>` Packages. config.</span><span class="sxs-lookup"><span data-stu-id="57d17-160">You just need to clean up the old packages.config-style `<Analyzer>` elements.</span></span>

### <a name="review-nuget-packages"></a><span data-ttu-id="57d17-161">Zkontrolovat balíčky NuGet</span><span class="sxs-lookup"><span data-stu-id="57d17-161">Review NuGet packages</span></span>

<span data-ttu-id="57d17-162">Teď, když vidíte balíčky NuGet nejvyšší úrovně, na kterých závisí projekt, můžete zkontrolovat, jestli jsou tyto balíčky dostupné v .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-162">Now that you can see the top-level NuGet packages that the project depends on, you can review whether those packages are available on .NET Core.</span></span> <span data-ttu-id="57d17-163">Můžete určit, jestli balíček podporuje .NET Core, a to tak, že se podíváte na závislosti na [NuGet.org](https://www.nuget.org/). Lokalita vytvořená komunitou [fuget.org](https://www.fuget.org/) zobrazuje tyto informace výrazně v horní části stránky s informacemi o balíčku.</span><span class="sxs-lookup"><span data-stu-id="57d17-163">You can determine whether a package supports .NET Core by looking at its dependencies on [nuget.org](https://www.nuget.org/). The community-created [fuget.org](https://www.fuget.org/) site shows this information prominently at the top of the package information page.</span></span>

<span data-ttu-id="57d17-164">Při cílení na rozhraní .NET Core 3,0 by měly všechny balíčky cílené na rozhraní .NET Core nebo .NET Standard fungovat (vzhledem k tomu, že .NET Core implementuje .NET Standard plochu).</span><span class="sxs-lookup"><span data-stu-id="57d17-164">When targeting .NET Core 3.0, any packages targeting .NET Core or .NET Standard should work (since .NET Core implements the .NET Standard surface area).</span></span> <span data-ttu-id="57d17-165">V některých případech se konkrétní verze balíčku, který se používá, necílí na rozhraní .NET Core ani .NET Standard, ale novější verze.</span><span class="sxs-lookup"><span data-stu-id="57d17-165">In some cases, the specific version of a package that's used won't target .NET Core or .NET Standard, but newer versions will.</span></span> <span data-ttu-id="57d17-166">V takovém případě byste měli zvážit upgrade na nejnovější verzi balíčku.</span><span class="sxs-lookup"><span data-stu-id="57d17-166">In this case, you should consider upgrading to the latest version of the package.</span></span>

<span data-ttu-id="57d17-167">Můžete použít také balíčky cílené na .NET Framework, ale to přináší nějaké riziko.</span><span class="sxs-lookup"><span data-stu-id="57d17-167">You can use packages targeting .NET Framework, as well, but that introduces some risk.</span></span> <span data-ttu-id="57d17-168">Rozhraní .NET Core pro .NET Framework závislosti jsou povoleny, protože oblasti .NET Core a .NET Framework Surface jsou natolik podobné, že tyto závislosti *často* fungují.</span><span class="sxs-lookup"><span data-stu-id="57d17-168">.NET Core to .NET Framework dependencies are allowed because .NET Core and .NET Framework surface areas are similar enough that such dependencies *often* work.</span></span> <span data-ttu-id="57d17-169">Pokud se však balíček pokusí použít rozhraní .NET API, které není k dispozici v rozhraní .NET Core, dojde k výjimce za běhu.</span><span class="sxs-lookup"><span data-stu-id="57d17-169">However, if the package tries to use a .NET API that isn't present in .NET Core, you'll encounter a runtime exception.</span></span> <span data-ttu-id="57d17-170">Z tohoto důvodu byste měli odkazovat pouze na .NET Framework balíčky, pokud nejsou k dispozici žádné jiné možnosti a nevíte, že by to mělo za starosti zátěž testu.</span><span class="sxs-lookup"><span data-stu-id="57d17-170">Because of that, you should only reference .NET Framework packages when no other options are available and understand that doing so imposes a test burden.</span></span>

<span data-ttu-id="57d17-171">Pokud se odkazuje na balíčky, které necílí na rozhraní .NET Core nebo .NET Standard, budete si muset představit další alternativy:</span><span class="sxs-lookup"><span data-stu-id="57d17-171">If there are packages referenced that don't target .NET Core or .NET Standard, you'll have to think about other alternatives:</span></span>

- <span data-ttu-id="57d17-172">Existují jiné podobné balíčky, které lze použít místo toho?</span><span class="sxs-lookup"><span data-stu-id="57d17-172">Are there other similar packages that can be used instead?</span></span> <span data-ttu-id="57d17-173">Někdy autoři NuGet publikují samostatně. Základní verze svých knihoven konkrétně cílí na .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-173">Sometimes NuGet authors publish separate '.Core' versions of their libraries specifically targeting .NET Core.</span></span> <span data-ttu-id="57d17-174">Balíčky podnikových knihoven představují příklad publikování komunitou. NetCore "alternativy.</span><span class="sxs-lookup"><span data-stu-id="57d17-174">Enterprise Library packages are an example of the community publishing ".NetCore" alternatives.</span></span> <span data-ttu-id="57d17-175">V ostatních případech jsou pro .NET Standard k dispozici novější sady SDK pro konkrétní službu (někdy s různými názvy balíčků).</span><span class="sxs-lookup"><span data-stu-id="57d17-175">In other cases, newer SDKs for a particular service (sometimes with different package names) are available for .NET Standard.</span></span> <span data-ttu-id="57d17-176">Pokud nejsou k dispozici žádné alternativy, můžete pokračovat v používání .NET Framework cílené balíčky s vědomím, že je budete muset důkladně otestovat po spuštění v .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-176">If no alternatives are available, you can proceed using the .NET Framework-targeted packages, bearing in mind that you'll need to test them thoroughly once running on .NET Core.</span></span>

<span data-ttu-id="57d17-177">Ukázka Bob obchodník má následující závislosti NuGet nejvyšší úrovně:</span><span class="sxs-lookup"><span data-stu-id="57d17-177">The Bean Trader sample has the following top-level NuGet dependencies:</span></span>

- [<span data-ttu-id="57d17-178">**Castle. Windsor, verze 4.1.1**</span><span class="sxs-lookup"><span data-stu-id="57d17-178">**Castle.Windsor, version 4.1.1**</span></span>](https://www.castleproject.org/projects/windsor/)  

  <span data-ttu-id="57d17-179">Tento balíček cílí na .NET Standard 1,6, takže funguje na .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-179">This package targets .NET Standard 1.6, so it works on .NET Core.</span></span>

- [<span data-ttu-id="57d17-180">**Microsoft. CodeAnalysis. FxCopAnalyzers, verze 2.6.3**</span><span class="sxs-lookup"><span data-stu-id="57d17-180">**Microsoft.CodeAnalysis.FxCopAnalyzers, version 2.6.3**</span></span>](https://www.nuget.org/packages/Microsoft.CodeAnalysis.FxCopAnalyzers/2.6.3)  
  <span data-ttu-id="57d17-181">Toto je meta balíček, takže není okamžitě zřejmé, které platformy podporuje, ale [dokumentace](https://github.com/dotnet/roslyn-analyzers#microsoftcodeanalysisfxcopanalyzers) indikuje, že jeho nejnovější verze (2.9.2) bude fungovat jak pro .NET Framework, tak pro .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-181">This is a meta-package, so it's not immediately obvious which platforms it supports, but [documentation](https://github.com/dotnet/roslyn-analyzers#microsoftcodeanalysisfxcopanalyzers) indicates that its newest version (2.9.2) will work for both .NET Framework and .NET Core.</span></span>

- [<span data-ttu-id="57d17-182">**Nito. AsyncEx, verze 4.0.1**</span><span class="sxs-lookup"><span data-stu-id="57d17-182">**Nito.AsyncEx, version 4.0.1**</span></span>](https://www.nuget.org/packages/Nito.AsyncEx/4.0.1)  

  <span data-ttu-id="57d17-183">Tento balíček necílí na .NET Core, ale novější verze 5,0.</span><span class="sxs-lookup"><span data-stu-id="57d17-183">This package doesn't target .NET Core, but the newer 5.0 version does.</span></span> <span data-ttu-id="57d17-184">To je běžné při migraci, protože mnoho balíčků NuGet se v poslední době přidalo .NET Standard podpoře, ale starší verze projektu budou pouze cílit .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="57d17-184">This is common when migrating because many NuGet packages have added .NET Standard support recently, but older project versions will only target .NET Framework.</span></span> <span data-ttu-id="57d17-185">Je-li rozdíl verze pouze dílčí rozdíl verze, je často snadné upgradovat na novější verzi.</span><span class="sxs-lookup"><span data-stu-id="57d17-185">If the version difference is only a minor version difference, it's often easy to upgrade to the newer version.</span></span> <span data-ttu-id="57d17-186">Vzhledem k tomu, že se jedná o zásadní změnu verze, je nutné upgradovat upgrade, protože by mohlo dojít k zásadním změnám balíčku.</span><span class="sxs-lookup"><span data-stu-id="57d17-186">Because this is a major version change, you need to be cautious upgrading since there could be breaking changes in the package.</span></span> <span data-ttu-id="57d17-187">K dispozici je cesta s předstihem, i když.</span><span class="sxs-lookup"><span data-stu-id="57d17-187">There is a path forward, though, which is good.</span></span>

- [<span data-ttu-id="57d17-188">**MahApps. metro, verze 1.6.5**</span><span class="sxs-lookup"><span data-stu-id="57d17-188">**MahApps.Metro, version 1.6.5**</span></span>](https://www.nuget.org/packages/MahApps.Metro/1.6.5)  

  <span data-ttu-id="57d17-189">Tento balíček také necílí na rozhraní .NET Core, ale má novější verzi předběžného (2,0-alfa).</span><span class="sxs-lookup"><span data-stu-id="57d17-189">This package also doesn't target .NET Core, but has a newer pre-release (2.0-alpha) that does.</span></span> <span data-ttu-id="57d17-190">Znovu se budete muset podívat na nevyřešené změny, ale novější balíček ho podporuje.</span><span class="sxs-lookup"><span data-stu-id="57d17-190">Again, you have to look out for breaking changes, but the newer package is encouraging.</span></span>

<span data-ttu-id="57d17-191">Všechny cílové .NET Standard/. NET Core nebo mají novější verze, a proto se tady nemusejí zablokovat žádné problémy.</span><span class="sxs-lookup"><span data-stu-id="57d17-191">The Bean Trader sample's NuGet dependencies all either target .NET Standard/.NET Core or have newer versions that do, so there are unlikely to be any blocking issues here.</span></span>

### <a name="upgrade-nuget-packages"></a><span data-ttu-id="57d17-192">Upgradovat balíčky NuGet</span><span class="sxs-lookup"><span data-stu-id="57d17-192">Upgrade NuGet packages</span></span>

<span data-ttu-id="57d17-193">Pokud je to možné, by bylo vhodné upgradovat verze všech balíčků, které v tomto okamžiku pouze cílí jenom na rozhraní .NET Core nebo .NET Standard s novějšími verzemi (s projektem pořád cílíte .NET Framework), abyste zjistili a vyřešili všechny zásadní změny včas.</span><span class="sxs-lookup"><span data-stu-id="57d17-193">If possible, it would be good to upgrade the versions of any packages that only target .NET Core or .NET Standard with more recent versions at this point (with the project still targeting .NET Framework) to discover and address any breaking changes early.</span></span>

<span data-ttu-id="57d17-194">Pokud byste nemuseli dělat žádné podstatné změny v existující .NET Framework verzi aplikace, může to počkat, až budete mít nový soubor projektu cílící na .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-194">If you would rather not make any material changes to the existing .NET Framework version of the app, this can wait until you have a new project file targeting .NET Core.</span></span> <span data-ttu-id="57d17-195">Upgrade balíčků NuGet na verze, které jsou kompatibilní s .NET Core, ale předem usnadňuje proces migrace, a to i po vytvoření nového souboru projektu a omezení počtu rozdílů mezi .NET Framework a verzemi .NET Core aplikace.</span><span class="sxs-lookup"><span data-stu-id="57d17-195">However, upgrading the NuGet packages to .NET Core-compatible versions ahead of time makes the migration process even easier once you create the new project file and reduces the number of differences between the .NET Framework and .NET Core versions of the app.</span></span>

<span data-ttu-id="57d17-196">V případě ukázky na základě obchodníka je možné snadno provést všechny nezbytné upgrady (pomocí Správce balíčků NuGet sady Visual Studio) s jednou výjimkou: upgrade z **MahApps. metro 1.6.5** na **2,0** odhalí zásadní změny související s rozhraními API pro správu motivů a zvýraznění.</span><span class="sxs-lookup"><span data-stu-id="57d17-196">With the Bean Trader sample, all of the necessary upgrades can be made easily (using Visual Studio's NuGet package manager) with one exception: upgrading from **MahApps.Metro 1.6.5** to **2.0** reveals breaking changes related to theme and accent management APIs.</span></span>

<span data-ttu-id="57d17-197">V ideálním případě by se aplikace aktualizovala tak, aby používala novější verzi balíčku (vzhledem k tomu, že je pravděpodobnější funkčnost .NET Core).</span><span class="sxs-lookup"><span data-stu-id="57d17-197">Ideally, the app would be updated to use the newer version of the package (since that is more likely to work on .NET Core).</span></span> <span data-ttu-id="57d17-198">V některých případech ale nemusí být proveditelné.</span><span class="sxs-lookup"><span data-stu-id="57d17-198">In some cases, however, that may not be feasible.</span></span> <span data-ttu-id="57d17-199">V těchto případech NEUPGRADUJTE **MahApps. metro** , protože potřebné změny jsou netriviální a tento kurz se zaměřuje na migraci na .NET Core 3, ne na **MahApps. Metro 2.**</span><span class="sxs-lookup"><span data-stu-id="57d17-199">In these cases, don't upgrade **MahApps.Metro** because the necessary changes are non-trivial and this tutorial focuses on migrating to .NET Core 3, not to **MahApps.Metro 2.**</span></span> <span data-ttu-id="57d17-200">Také se jedná o ne.NET Frameworkou závislost, protože obchodník z aplikace Bob uplatňuje jenom malou část **MahApps. metro**.</span><span class="sxs-lookup"><span data-stu-id="57d17-200">Also, this is a low-risk .NET Framework dependency because the Bean Trader app only exercises a small part of **MahApps.Metro**.</span></span> <span data-ttu-id="57d17-201">Budeme samozřejmě potřebovat testování, aby se zajistilo, že vše funguje, až se migrace dokončí.</span><span class="sxs-lookup"><span data-stu-id="57d17-201">It will, of course, require testing to make sure everything's working once the migration is complete.</span></span> <span data-ttu-id="57d17-202">Pokud se jednalo o reálný scénář, mělo by být dobré zaslat problém pro sledování práce, která se má přesunout na **MahApps. metro** verze 2,0, protože migrace teď nevede k nějakému technickému dluhu.</span><span class="sxs-lookup"><span data-stu-id="57d17-202">If this were a real-world scenario, it would be good to file an issue to track the work to move to **MahApps.Metro** version 2.0 since not doing the migration now leaves behind some technical debt.</span></span>

<span data-ttu-id="57d17-203">Jakmile se balíčky NuGet aktualizují na nejnovější verze, skupina `<PackageReference>` položek v souboru projektu ukázkového účastníka společnosti Bob by měla vypadat takto.</span><span class="sxs-lookup"><span data-stu-id="57d17-203">Once the NuGet packages are updated to recent versions, the `<PackageReference>` item group in the Bean Trader sample's project file should look like this.</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Castle.Windsor">
    <Version>4.1.1</Version>
  </PackageReference>
  <PackageReference Include="MahApps.Metro">
    <Version>1.6.5</Version>
  </PackageReference>
  <PackageReference Include="Microsoft.CodeAnalysis.FxCopAnalyzers">
    <Version>2.9.2</Version>
  </PackageReference>
  <PackageReference Include="Nito.AsyncEx">
    <Version>5.0.0</Version>
  </PackageReference>
</ItemGroup>
```

### <a name="net-framework-portability-analysis"></a><span data-ttu-id="57d17-204">Analýza přenositelnosti .NET Framework</span><span class="sxs-lookup"><span data-stu-id="57d17-204">.NET Framework portability analysis</span></span>

<span data-ttu-id="57d17-205">Jakmile porozumíte stavu závislostí NuGet vašeho projektu, další věc, kterou je potřeba zvážit, je .NET Framework závislosti rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="57d17-205">Once you understand the state of your project's NuGet dependencies, the next thing to consider is .NET Framework API dependencies.</span></span> <span data-ttu-id="57d17-206">Nástroj [analyzátor přenositelnosti .NET](../../standard/analyzers/portability-analyzer.md) je užitečný pro porozumění tomu, které rozhraní API .NET vaše projekty používá, jsou k dispozici na jiných platformách .NET.</span><span class="sxs-lookup"><span data-stu-id="57d17-206">The [.NET Portability Analyzer](../../standard/analyzers/portability-analyzer.md) tool is useful for understanding which of the .NET APIs your project uses are available on other .NET platforms.</span></span>

<span data-ttu-id="57d17-207">Nástroj se dodává jako [modul plug-in sady Visual Studio](https://marketplace.visualstudio.com/items?itemName=ConnieYau.NETPortabilityAnalyzer), [Nástroj příkazového řádku](https://github.com/Microsoft/dotnet-apiport/releases)nebo zabalený do [jednoduchého grafického uživatelského rozhraní](https://github.com/Microsoft/dotnet-apiport-ui), které zjednodušuje jeho možnosti.</span><span class="sxs-lookup"><span data-stu-id="57d17-207">The tool comes as a [Visual Studio plugin](https://marketplace.visualstudio.com/items?itemName=ConnieYau.NETPortabilityAnalyzer), a [command-line tool](https://github.com/Microsoft/dotnet-apiport/releases), or wrapped in a [simple GUI](https://github.com/Microsoft/dotnet-apiport-ui), which simplifies its options.</span></span> <span data-ttu-id="57d17-208">Další informace o použití analyzátoru přenositelnosti .NET (port rozhraní API) najdete v tomto příspěvku na blogu v tématu [portování desktopových aplikací do .NET Core](https://devblogs.microsoft.com/dotnet/porting-desktop-apps-to-net-core/) .</span><span class="sxs-lookup"><span data-stu-id="57d17-208">You can read more about using the .NET Portability Analyzer (API Port) using the GUI in the [Porting desktop apps to .NET Core](https://devblogs.microsoft.com/dotnet/porting-desktop-apps-to-net-core/) blog post.</span></span> <span data-ttu-id="57d17-209">Pokud dáváte přednost použití příkazového řádku, jsou nezbytné kroky:</span><span class="sxs-lookup"><span data-stu-id="57d17-209">If you prefer to use the command line, the necessary steps are:</span></span>

1. <span data-ttu-id="57d17-210">Stáhněte si [analyzátor přenositelnosti .NET](https://github.com/Microsoft/dotnet-apiport/releases) , pokud ho ještě nemáte.</span><span class="sxs-lookup"><span data-stu-id="57d17-210">Download the [.NET Portability Analyzer](https://github.com/Microsoft/dotnet-apiport/releases) if you don't already have it.</span></span>
1. <span data-ttu-id="57d17-211">Zajistěte, aby byla aplikace .NET Framework správně předaná sestavení (to je dobrý nápad před migrací bez ohledu na migraci).</span><span class="sxs-lookup"><span data-stu-id="57d17-211">Make sure the .NET Framework app to be ported builds successfully (this is a good idea prior to migration regardless).</span></span>
1. <span data-ttu-id="57d17-212">Spusťte port rozhraní API s příkazovým řádkem.</span><span class="sxs-lookup"><span data-stu-id="57d17-212">Run API Port with a command line like this.</span></span>

    ```console
    ApiPort.exe analyze -f <PathToBeanTraderBinaries> -r html -r excel -t ".NET Core"
    ```

    <span data-ttu-id="57d17-213">`-f` Argument určuje cestu obsahující binární soubory, které se mají analyzovat.</span><span class="sxs-lookup"><span data-stu-id="57d17-213">The `-f` argument specifies the path containing the binaries to analyze.</span></span> <span data-ttu-id="57d17-214">`-r` Argument určuje formát výstupního souboru, který chcete.</span><span class="sxs-lookup"><span data-stu-id="57d17-214">The `-r` argument specifies which output file format you want.</span></span> <span data-ttu-id="57d17-215">`-t` Argument určuje, která platforma .NET bude analyzovat využití rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="57d17-215">The `-t` argument specifies which .NET platform to analyze API usage against.</span></span> <span data-ttu-id="57d17-216">V takovém případě chcete .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-216">In this case, you want .NET Core.</span></span>

<span data-ttu-id="57d17-217">Když otevřete sestavu HTML, v první části se zobrazí všechny analyzované binární soubory a procento používaných rozhraní API .NET, které jsou k dispozici na cílové platformě.</span><span class="sxs-lookup"><span data-stu-id="57d17-217">When you open the HTML report, the first section will list all of the analyzed binaries and what percentage of the .NET APIs they use are available on the targeted platform.</span></span> <span data-ttu-id="57d17-218">Procento není smysluplné.</span><span class="sxs-lookup"><span data-stu-id="57d17-218">The percentage is not meaningful by itself.</span></span> <span data-ttu-id="57d17-219">Užitečnější je zobrazit konkrétní chybějící rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="57d17-219">What's more useful is to see the specific APIs that are missing.</span></span> <span data-ttu-id="57d17-220">Chcete-li to provést, buď vyberte název sestavení, nebo se posuňte dolů k sestavám pro jednotlivá sestavení.</span><span class="sxs-lookup"><span data-stu-id="57d17-220">To do that, either select an assembly name or scroll down to the reports for individual assemblies.</span></span>

<span data-ttu-id="57d17-221">Zaměřte se na sestavení, pro která vlastníte zdrojový kód.</span><span class="sxs-lookup"><span data-stu-id="57d17-221">Focus on the assemblies that you own the source code for.</span></span> <span data-ttu-id="57d17-222">V sestavě ApiPorta v programu Bob obchodník je například uveden mnoho binárních souborů, ale většina z nich patří do balíčků NuGet.</span><span class="sxs-lookup"><span data-stu-id="57d17-222">In the Bean Trader ApiPort report, for example, there are many binaries listed, but most of them belong to NuGet packages.</span></span> <span data-ttu-id="57d17-223">`Castle.Windsor`ukazuje, že závisí na některých rozhraních API System. Web, které chybí v .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-223">`Castle.Windsor` shows that it depends on some System.Web APIs that are missing in .NET Core.</span></span> <span data-ttu-id="57d17-224">Nejedná se o problém, protože jste předtím `Castle.Windsor` ověřili, že produkt podporuje .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-224">This isn't a concern because you previously verified that `Castle.Windsor` supports .NET Core.</span></span> <span data-ttu-id="57d17-225">Pro balíčky NuGet je běžné, že mají různé binární soubory pro použití s různými platformami .NET, takže jestli .NET Framework verze nástroje `Castle.Windsor` používá rozhraní API System. Web nebo not není relevantní, dokud balíček cílí také .NET Standard nebo .NET Core (to dělá).</span><span class="sxs-lookup"><span data-stu-id="57d17-225">It is common for NuGet packages to have different binaries for use with different .NET platforms, so whether the .NET Framework version of `Castle.Windsor` uses System.Web APIs or not is irrelevant as long as the package also targets .NET Standard or .NET Core (which it does).</span></span>

<span data-ttu-id="57d17-226">V případě ukázky pro obchodníka v programu Bob je jediným binárním souborem, který je třeba vzít v úvahu, **BeanTraderClient** a tato sestava uvádí, `System.ServiceModel.ClientBase<T>.Close` že `System.ServiceModel.ClientBase<T>.Open`chybí pouze dvě rozhraní API .NET: a.</span><span class="sxs-lookup"><span data-stu-id="57d17-226">With the Bean Trader sample, the only binary that you need to consider is **BeanTraderClient** and the report shows that only two .NET APIs are missing: `System.ServiceModel.ClientBase<T>.Close` and `System.ServiceModel.ClientBase<T>.Open`.</span></span>

![Sestava přenositelnosti BeanTraderClient](./media/convert-project-from-net-framework/portability-report.png)

<span data-ttu-id="57d17-228">Je pravděpodobné, že nebudou blokovat problémy, protože rozhraní API klienta WCF jsou v .NET Core podporována (většinou), takže pro tato centrální rozhraní API musí být k dispozici alternativní řešení.</span><span class="sxs-lookup"><span data-stu-id="57d17-228">These are unlikely to be blocking issues because WCF Client APIs are (mostly) supported on .NET Core, so there must be alternatives available for these central APIs.</span></span> <span data-ttu-id="57d17-229">Ve skutečnosti se podívejte na `System.ServiceModel`oblast .NET Core Surface (pomocí <https://apisof.net>), že místo toho jsou v .NET Core k dispozici asynchronní alternativy.</span><span class="sxs-lookup"><span data-stu-id="57d17-229">In fact, looking at `System.ServiceModel`'s .NET Core surface area (using <https://apisof.net>), you see that there are async alternatives in .NET Core instead.</span></span>

<span data-ttu-id="57d17-230">Na základě této sestavy a předchozí analýzy závislostí NuGet vypadá to, že by se neměly provádět žádné zásadní problémy migrace ukázkového účastníka z části Bob obchodník do .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-230">Based on this report and the previous NuGet dependency analysis, it looks like there should be no major issues migrating the Bean Trader sample to .NET Core.</span></span> <span data-ttu-id="57d17-231">Jste připraveni na další krok, ve kterém bude migrace skutečně zahájena.</span><span class="sxs-lookup"><span data-stu-id="57d17-231">You're ready for the next step in which you'll actually start the migration.</span></span>

## <a name="migrating-the-project-file"></a><span data-ttu-id="57d17-232">Migruje se soubor projektu.</span><span class="sxs-lookup"><span data-stu-id="57d17-232">Migrating the project file</span></span>

<span data-ttu-id="57d17-233">Pokud vaše aplikace nepoužívá nový [Formát souboru projektu ve stylu sady SDK](../../core/tools/csproj.md), budete potřebovat nový soubor projektu pro cílové rozhraní .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-233">If your app isn't using the new [SDK-style project file format](../../core/tools/csproj.md), you'll need a new project file to target .NET Core.</span></span> <span data-ttu-id="57d17-234">Existující soubor CSPROJ můžete nahradit, nebo pokud upřednostňujete zachování existujícího projektu v jeho aktuálním stavu, můžete přidat nový soubor CSPROJ cílící na rozhraní .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-234">You can replace the existing csproj file or, if you prefer to keep the existing project untouched in its current state, you can add a new csproj file targeting .NET Core.</span></span> <span data-ttu-id="57d17-235">Můžete sestavit verze aplikace pro .NET Framework a .NET Core s jedním souborem projektu ve stylu sady SDK s [cílením](../../standard/library-guidance/cross-platform-targeting.md) na více verzí (určením více `<TargetFrameworks>` cílů).</span><span class="sxs-lookup"><span data-stu-id="57d17-235">You can build versions of the app for .NET Framework and .NET Core with a single SDK-style project file with [multi-targeting](../../standard/library-guidance/cross-platform-targeting.md) (specifying multiple `<TargetFrameworks>` targets).</span></span>

<span data-ttu-id="57d17-236">Chcete-li vytvořit nový soubor projektu, můžete vytvořit nový projekt WPF v aplikaci Visual Studio nebo použít `dotnet new wpf` příkaz v dočasném adresáři pro vygenerování souboru projektu a potom jej zkopírovat nebo přejmenovat do správného umístění.</span><span class="sxs-lookup"><span data-stu-id="57d17-236">To create the new project file, you can create a new WPF project in Visual Studio or use the `dotnet new wpf` command in a temporary directory to generate the project file and then copy/rename it to the correct location.</span></span> <span data-ttu-id="57d17-237">K dispozici je také nástroj [CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017)vytvořený komunitou, který může automatizovat některé migrace souborů projektu.</span><span class="sxs-lookup"><span data-stu-id="57d17-237">There is also a community-created tool, [CsprojToVs2017](https://github.com/hvanbakel/CsprojToVs2017), that can automate some of the project file migration.</span></span> <span data-ttu-id="57d17-238">Tento nástroj je užitečný, ale ještě potřebuje člověk ke kontrole výsledků, aby se zajistilo, že všechny podrobnosti migrace jsou správné.</span><span class="sxs-lookup"><span data-stu-id="57d17-238">The tool is helpful but still needs a human to review the results to make sure all the details of the migration are correct.</span></span> <span data-ttu-id="57d17-239">Jedna konkrétní oblast, kterou nástroj nezpracovává optimálně, migruje balíčky NuGet ze souborů *Packages. config* .</span><span class="sxs-lookup"><span data-stu-id="57d17-239">One particular area that the tool doesn't handle optimally is migrating NuGet packages from *packages.config* files.</span></span> <span data-ttu-id="57d17-240">Pokud nástroj běží na souboru projektu, který stále používá soubor *Packages. config* k odkazování na balíčky NuGet, migruje se `<PackageReference>` na prvky automaticky, ale přidá `<PackageReference>` prvky pro *všechny* balíčky místo pouze těch, které mají nejvyšší úroveň.</span><span class="sxs-lookup"><span data-stu-id="57d17-240">If the tool runs on a project file that still uses a *packages.config* file to reference NuGet packages, it will migrate to `<PackageReference>` elements automatically, but will add `<PackageReference>` elements for *all* of the packages instead of just top-level ones.</span></span> <span data-ttu-id="57d17-241">Pokud jste již migrovali na`<PackageReference>` prvky pomocí sady Visual Studio (jak jste to udělali v této ukázce), nástroj může pomáhat se zbytkem převodu.</span><span class="sxs-lookup"><span data-stu-id="57d17-241">If you have already migrated to`<PackageReference>` elements with Visual Studio (as you've done in this sample), then the tool can help with the rest of the conversion.</span></span> <span data-ttu-id="57d17-242">Podobně jako Scott Hanselman doporučuje ve [svém blogovém příspěvku o migraci souborů csproj](https://www.hanselman.com/blog/UpgradingAnExistingNETProjectFilesToTheLeanNewCSPROJFormatFromNETCore.aspx). přenos po ruce je vzdělávací a výsledkem je lepší výsledky, pokud máte pouze několik projektů na port.</span><span class="sxs-lookup"><span data-stu-id="57d17-242">Like Scott Hanselman recommends in [his blog post on migrating csproj files](https://www.hanselman.com/blog/UpgradingAnExistingNETProjectFilesToTheLeanNewCSPROJFormatFromNETCore.aspx), porting by hand is educational and will give better results if you only have a few projects to port.</span></span> <span data-ttu-id="57d17-243">Pokud ale předáváte desítky nebo stovky souborů projektu, pak může být nástroj jako [CsprojToVs2017] help.</span><span class="sxs-lookup"><span data-stu-id="57d17-243">But if you are porting dozens or hundreds of project files, then a tool like [CsprojToVs2017] can be a help.</span></span>

<span data-ttu-id="57d17-244">Chcete-li vytvořit nový soubor projektu pro ukázku pro obchodní oddělení, `dotnet new wpf` spusťte příkaz v dočasném adresáři a přesuňte vygenerovaný soubor *. csproj* do složky *BeanTraderClient* a přejmenujte jej na **BeanTraderClient. Core. csproj**.</span><span class="sxs-lookup"><span data-stu-id="57d17-244">To create a new project file for the Bean Trader sample, run `dotnet new wpf` in a temporary directory and move the generated *.csproj* file into the *BeanTraderClient* folder and rename it **BeanTraderClient.Core.csproj**.</span></span>

<span data-ttu-id="57d17-245">Vzhledem k tomu, že nový formát souboru projektu automaticky obsahuje soubory jazyka C#, soubory *RESX* a soubory XAML, které nalezne v nebo v jejím adresáři, je soubor projektu již skoro dokončen.</span><span class="sxs-lookup"><span data-stu-id="57d17-245">Because the new project file format automatically includes C# files, *resx* files, and XAML files that it finds in or under its directory, the project file is already almost complete!</span></span> <span data-ttu-id="57d17-246">Pro dokončení migrace otevřete staré a nové soubory projektu vedle sebe a Prohlédněte si starou a podívejte se, jestli je potřeba migrovat nějaké informace, které obsahuje.</span><span class="sxs-lookup"><span data-stu-id="57d17-246">To finish the migration, open the old and new project files side-by-side and look through the old one to see if any information it contains needs to be migrated.</span></span> <span data-ttu-id="57d17-247">V ukázkovém případu pro hospodářský subjekt by měly být do nového projektu zkopírovány následující položky:</span><span class="sxs-lookup"><span data-stu-id="57d17-247">In the Bean Trader sample case, the following items should be copied to the new project:</span></span>

- <span data-ttu-id="57d17-248">Všechny `<RootNamespace>`vlastnosti `<AssemblyName>`, a `<ApplicationIcon>` by měly být kopírovány.</span><span class="sxs-lookup"><span data-stu-id="57d17-248">The `<RootNamespace>`, `<AssemblyName>`, and `<ApplicationIcon>` properties should all be copied.</span></span>

- <span data-ttu-id="57d17-249">Také je nutné přidat do nového `<GenerateAssemblyInfo>false</GenerateAssemblyInfo>` souboru projektu vlastnost, protože ukázka pro obchodníka v nástroji Bob zahrnuje atributy na úrovni sestavení ( `[AssemblyTitle]`například) v souboru AssemblyInfo.cs.</span><span class="sxs-lookup"><span data-stu-id="57d17-249">You also need to add a `<GenerateAssemblyInfo>false</GenerateAssemblyInfo>` property to the new project file since the Bean Trader sample includes assembly-level attributes (like `[AssemblyTitle]`) in an AssemblyInfo.cs file.</span></span> <span data-ttu-id="57d17-250">Ve výchozím nastavení budou nové projekty ve stylu sady SDK tyto atributy generovat na základě vlastností v souboru csproj.</span><span class="sxs-lookup"><span data-stu-id="57d17-250">By default, new SDK-style projects will autogenerate these attributes based on properties in the csproj file.</span></span> <span data-ttu-id="57d17-251">Vzhledem k tomu, že v tomto případě nechcete, aby k tomu docházelo (automaticky vygenerovaný atribut by byl v konfliktu s hodnotami z AssemblyInfo.cs), zakážete `<GenerateAssemblyInfo>`automaticky generované atributy pomocí.</span><span class="sxs-lookup"><span data-stu-id="57d17-251">Because you don't want that to happen in this case (the autogenerated attributes would conflict with those from AssemblyInfo.cs), you disable the autogenerated attributes with `<GenerateAssemblyInfo>`.</span></span>

- <span data-ttu-id="57d17-252">I když soubory *RESX* jsou automaticky zahrnuty jako vložené prostředky, `<Resource>` jiné položky jako obrázky nejsou.</span><span class="sxs-lookup"><span data-stu-id="57d17-252">Although *resx* files are automatically included as embedded resources, other `<Resource>` items like images are not.</span></span> <span data-ttu-id="57d17-253">Proto zkopírujte `<Resource>` prvky pro vkládání obrázků a souborů ikon.</span><span class="sxs-lookup"><span data-stu-id="57d17-253">So, copy the `<Resource>` elements for embedding image and icon files.</span></span> <span data-ttu-id="57d17-254">Můžete zjednodušit odkazy PNG na jeden řádek pomocí nového formátu souboru projektu, který je podporován pro vzory expanze názvů: `<Resource Include="**\*.png" />`.</span><span class="sxs-lookup"><span data-stu-id="57d17-254">You can simplify the png references to a single line by using the new project file format's support for globbing patterns: `<Resource Include="**\*.png" />`.</span></span>

- <span data-ttu-id="57d17-255">Podobně jsou `<None>` položky automaticky zahrnuty, ale ve výchozím nastavení nejsou zkopírovány do výstupního adresáře.</span><span class="sxs-lookup"><span data-stu-id="57d17-255">Similarly, `<None>` items are included automatically, but they aren't copied to the output directory, by default.</span></span> <span data-ttu-id="57d17-256">Vzhledem k tomu, že projekt `<None>` fazolového účastníka obsahuje položku, která *je* zkopírována `PreserveNewest` do výstupního adresáře (pomocí chování), je `<None>` třeba aktualizovat automaticky vyplněnou položku pro tento soubor, například.</span><span class="sxs-lookup"><span data-stu-id="57d17-256">Because the Bean Trader project includes a `<None>` item that *is* copied to the output directory (using `PreserveNewest` behaviors), you need to update the automatically populated `<None>` item for that file, like this.</span></span>

  ```xml
  <None Update="BeanTrader.pfx">
    <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </None>
  ```

- <span data-ttu-id="57d17-257">Ukázka pro obchodníka Bob zahrnuje soubor XAML (default. akcent. XAML) jako `Content` (nikoli jako `Page`), protože motivy a zvýraznění definované v tomto souboru jsou načteny z XAML souboru za běhu, místo aby se vložily do samotné aplikace.</span><span class="sxs-lookup"><span data-stu-id="57d17-257">The Bean Trader sample includes a XAML file (Default.Accent.xaml) as `Content` (rather than as a `Page`) because themes and accents defined in this file are loaded from the file's XAML at runtime, rather than being embedded in the app itself.</span></span> <span data-ttu-id="57d17-258">Nový projektový systém automaticky zahrne tento soubor jako `<Page>`, protože se jedná o soubor XAML.</span><span class="sxs-lookup"><span data-stu-id="57d17-258">The new project system automatically includes this file as a `<Page>`, however, since it's a XAML file.</span></span> <span data-ttu-id="57d17-259">Proto je nutné soubor XAML odebrat jako stránku (`<Page Remove="**\Default.Accent.xaml" />`) a přidat jej jako obsah.</span><span class="sxs-lookup"><span data-stu-id="57d17-259">So, you need to both remove the XAML file as a page (`<Page Remove="**\Default.Accent.xaml" />`) and add it as content.</span></span>

  ```xml
  <Content Include="Resources\Themes\Default.Accent.xaml">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </Content>
  ```

- <span data-ttu-id="57d17-260">Nakonec přidejte odkazy na NuGet tím, že `<ItemGroup>` zkopírujete všechny `<PackageReference>` prvky.</span><span class="sxs-lookup"><span data-stu-id="57d17-260">Finally, add NuGet references by copying the `<ItemGroup>` with all the `<PackageReference>` elements.</span></span> <span data-ttu-id="57d17-261">Pokud jste jste dříve upgradovali balíčky NuGet na verze kompatibilní se standardem .NET Core, můžete to udělat, pokud jsou odkazy na balíček v projektu specifickém pro .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-261">If you hadn't previously upgraded the NuGet packages to .NET Core-compatible versions, you could do that now that the package references are in a .NET Core-specific project.</span></span>

<span data-ttu-id="57d17-262">V tomto okamžiku by mělo být možné přidat nový projekt do řešení BeanTrader a otevřít ho v aplikaci Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="57d17-262">At this point, it should be possible to add the new project to the BeanTrader solution and open it in Visual Studio.</span></span> <span data-ttu-id="57d17-263">Projekt by měl vypadat správně v **Průzkumník řešení**a `dotnet restore BeanTraderClient.Core.csproj` měl by úspěšně obnovit balíčky (se dvěma očekávanými upozorněními souvisejícími s verzí MahApps. metro, kterou používáte cílení .NET Framework).</span><span class="sxs-lookup"><span data-stu-id="57d17-263">The project should look correct in **Solution Explorer**, and `dotnet restore BeanTraderClient.Core.csproj` should successfully restore packages (with two expected warnings related to the MahApps.Metro version you're using targeting .NET Framework).</span></span>

<span data-ttu-id="57d17-264">I když je možné uchovávat oba soubory projektu souběžně (a může to být žádoucí, pokud chcete zachovat původní projekt přesně tak, jak byl), ztěžuje proces migrace (tyto dva projekty se pokusí použít stejné složky bin a obj) a obvykle není nutné.</span><span class="sxs-lookup"><span data-stu-id="57d17-264">Although it's possible to keep both project files side-by-side (and may even be desirable if you want to keep building the old project exactly as it was), it complicates the migration process (the two projects will try to use the same bin and obj folders) and usually isn't necessary.</span></span> <span data-ttu-id="57d17-265">Pokud chcete sestavovat pro cíle .NET Core i .NET Framework, můžete `<TargetFramework>netcoreapp3.0</TargetFramework>` `<TargetFrameworks>netcoreapp3.0;net472</TargetFrameworks>` místo toho nahradit vlastnost v novém souboru projektu.</span><span class="sxs-lookup"><span data-stu-id="57d17-265">If you want to build for both .NET Core and .NET Framework targets, you can replace the `<TargetFramework>netcoreapp3.0</TargetFramework>` property in the new project file with `<TargetFrameworks>netcoreapp3.0;net472</TargetFrameworks>` instead.</span></span> <span data-ttu-id="57d17-266">V případě ukázky pro obchodní oddělení odstraňte starý soubor projektu (BeanTraderClient. csproj), protože už není potřeba.</span><span class="sxs-lookup"><span data-stu-id="57d17-266">For the Bean Trader sample, delete the old project file (BeanTraderClient.csproj) since it's no longer needed.</span></span> <span data-ttu-id="57d17-267">Pokud chcete zachovat oba soubory projektu, nezapomeňte je nechat sestavit do různých výstupních a zprostředkujících výstupních cest.</span><span class="sxs-lookup"><span data-stu-id="57d17-267">If you prefer to keep both project files, be sure to have them build to different output and intermediate output paths.</span></span>

## <a name="fix-build-issues"></a><span data-ttu-id="57d17-268">Opravit problémy sestavení</span><span class="sxs-lookup"><span data-stu-id="57d17-268">Fix build issues</span></span>

<span data-ttu-id="57d17-269">Třetí krok procesu přenosu získává projekt, který se má sestavit.</span><span class="sxs-lookup"><span data-stu-id="57d17-269">The third step of the porting process is getting the project to build.</span></span> <span data-ttu-id="57d17-270">Některé aplikace budou po převedení souboru projektu na projekt ve stylu sady SDK již sestaveny úspěšně.</span><span class="sxs-lookup"><span data-stu-id="57d17-270">Some apps will already build successfully once the project file is converted to an SDK-style project.</span></span> <span data-ttu-id="57d17-271">Pokud je to váš případ pro vaši aplikaci, Blahopřejeme!</span><span class="sxs-lookup"><span data-stu-id="57d17-271">If that's the case for your app, congratulations!</span></span> <span data-ttu-id="57d17-272">Můžete přejít ke kroku 4.</span><span class="sxs-lookup"><span data-stu-id="57d17-272">You can go on to Step 4.</span></span> <span data-ttu-id="57d17-273">Ostatní aplikace budou potřebovat nějaké aktualizace, aby je mohli sestavit pro .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-273">Other apps will need some updates to get them building for .NET Core.</span></span> <span data-ttu-id="57d17-274">Pokud se pokusíte spustit `dotnet build` ukázkový projekt v programu Bob obchodník, například (nebo ho sestavit v aplikaci Visual Studio), dojde k velkému počtu chyb, ale budete je moct rychle opravit.</span><span class="sxs-lookup"><span data-stu-id="57d17-274">If you try to run `dotnet build` on the Bean Trader sample project now, for example, (or build it in Visual Studio), there will be many errors, but you'll get them fixed quickly.</span></span>

### <a name="systemservicemodel-references-and-microsoftwindowscompatibility"></a><span data-ttu-id="57d17-275">Odkazy na System. ServiceModel a Microsoft. Windows. Compatibility</span><span class="sxs-lookup"><span data-stu-id="57d17-275">System.ServiceModel references and Microsoft.Windows.Compatibility</span></span>

<span data-ttu-id="57d17-276">Ve společném zdroji chyb chybí odkazy pro rozhraní API, která jsou k dispozici pro .NET Core, ale nejsou automaticky zahrnutá do aplikace .NET Core Metapackage.</span><span class="sxs-lookup"><span data-stu-id="57d17-276">A common source of errors is missing references for APIs that are available for .NET Core but not automatically included in the .NET Core app metapackage.</span></span> <span data-ttu-id="57d17-277">Pokud to chcete vyřešit, měli byste se `Microsoft.Windows.Compatibility` na balíček odkazovat.</span><span class="sxs-lookup"><span data-stu-id="57d17-277">To address this, you should reference the `Microsoft.Windows.Compatibility` package.</span></span> <span data-ttu-id="57d17-278">Balíček kompatibility zahrnuje širokou škálu rozhraní API, která jsou společná pro desktopové aplikace pro Windows, jako je například klient WCF, adresářové služby, registr, konfigurace, rozhraní API seznamů ACL a další.</span><span class="sxs-lookup"><span data-stu-id="57d17-278">The compatibility package includes a broad set of APIs that are common in Windows desktop apps, such as WCF client, directory services, registry, configuration, ACLs APIs, and more.</span></span>

<span data-ttu-id="57d17-279">V případě ukázky v programu Bob obchodník je většina chyb sestavení způsobena chybějícími <xref:System.ServiceModel> typy.</span><span class="sxs-lookup"><span data-stu-id="57d17-279">With the Bean Trader sample, the majority of the build errors are due to missing <xref:System.ServiceModel> types.</span></span> <span data-ttu-id="57d17-280">Ty je možné řešit pomocí odkazů na potřebné balíčky NuGet WCF.</span><span class="sxs-lookup"><span data-stu-id="57d17-280">These could be addressed by referencing the necessary WCF NuGet packages.</span></span> <span data-ttu-id="57d17-281">Klientská rozhraní API WCF jsou mezi systémy přítomnými v `Microsoft.Windows.Compatibility` balíčku, a to i tak, aby odkazovaly na balíček kompatibility ještě lepším řešením (protože taky řeší všechny problémy související s rozhraními API a také řešení problémů, ke kterým balíček kompatibility zpřístupňuje přístup).</span><span class="sxs-lookup"><span data-stu-id="57d17-281">WCF client APIs are among those present in the `Microsoft.Windows.Compatibility` package, though, so referencing the compatibility package is an even better solution (since it also addresses any issues related to APIs as well as solutions to the WCF issues that the compatibility package makes available).</span></span> <span data-ttu-id="57d17-282">`Microsoft.Windows.Compatibility` Balíček pomáhá ve většině scénářů pro přenos rozhraní .net Core 3,0 WPF a WinForms.</span><span class="sxs-lookup"><span data-stu-id="57d17-282">The `Microsoft.Windows.Compatibility` package helps in most .NET Core 3.0 WPF and WinForms porting scenarios.</span></span> <span data-ttu-id="57d17-283">Po přidání odkazu na NuGet do `Microsoft.Windows.Compatibility`se zachová jenom jedna chyba buildu!</span><span class="sxs-lookup"><span data-stu-id="57d17-283">After adding the NuGet reference to `Microsoft.Windows.Compatibility`, only one build error remains!</span></span>

### <a name="cleaning-up-unused-files"></a><span data-ttu-id="57d17-284">Čištění nepoužívaných souborů</span><span class="sxs-lookup"><span data-stu-id="57d17-284">Cleaning up unused files</span></span>

<span data-ttu-id="57d17-285">Jeden typ problému migrace, který se často týká souborů C# a XAML, které nebyly dříve zahrnuty do sestavení, vybírají nové projekty ve stylu sady SDK, které zahrnují *všechny* zdroje automaticky.</span><span class="sxs-lookup"><span data-stu-id="57d17-285">One type of migration issue that comes up often relates to C# and XAML files that weren't previously included in the build getting picked up by the new SDK-style projects that include *all* source automatically.</span></span>

<span data-ttu-id="57d17-286">Další chyba sestavení, kterou vidíte v ukázce pro obchodní vztah, odkazuje na chybnou implementaci rozhraní v *OldUnusedViewModel.cs*.</span><span class="sxs-lookup"><span data-stu-id="57d17-286">The next build error you see in the Bean Trader sample refers to a bad interface implementation in *OldUnusedViewModel.cs*.</span></span> <span data-ttu-id="57d17-287">Název souboru je pomocný parametr, ale při kontrole zjistíte, že tento zdrojový soubor není správný.</span><span class="sxs-lookup"><span data-stu-id="57d17-287">The file name is a hint, but on inspection, you'll find that this source file is incorrect.</span></span> <span data-ttu-id="57d17-288">Nezpůsobila problémy dříve, protože neobsahovala původní .NET Framework projekt.</span><span class="sxs-lookup"><span data-stu-id="57d17-288">It didn't cause issues previously because it wasn't included in the original .NET Framework project.</span></span> <span data-ttu-id="57d17-289">Zdrojové soubory, které byly přítomny na disku, ale nejsou zahrnuté do starého souboru *csproj* , jsou teď zahrnuté automaticky.</span><span class="sxs-lookup"><span data-stu-id="57d17-289">Source files that were present on disk but not included in the old *csproj* are included automatically now.</span></span>

<span data-ttu-id="57d17-290">V případě nedostatku problému se dá snadno porovnat s předchozími *csproj* a potvrdit, že soubor není potřeba, a pak ho buď `<Compile Remove="" />` nebo, pokud už zdrojový soubor ještě není potřeba, odstranit.</span><span class="sxs-lookup"><span data-stu-id="57d17-290">For one-off issues like this, it's easy to compare to the previous *csproj* to confirm that the file isn't needed, and then either `<Compile Remove="" />` it or, if the source file isn't needed anywhere anymore, delete it.</span></span> <span data-ttu-id="57d17-291">V tomto případě je bezpečné jenom odstranit *OldUnusedViewModel.cs*.</span><span class="sxs-lookup"><span data-stu-id="57d17-291">In this case, it's safe to just delete *OldUnusedViewModel.cs*.</span></span>

<span data-ttu-id="57d17-292">Pokud máte mnoho zdrojových souborů, které by musely být vyloučeny tímto způsobem, můžete zakázat automatické zahrnutí souborů jazyka C# nastavením `<EnableDefaultCompileItems>` vlastnosti na hodnotu false v souboru projektu.</span><span class="sxs-lookup"><span data-stu-id="57d17-292">If you have many source files that would need to be excluded this way, you can disable auto-inclusion of C# files by setting the `<EnableDefaultCompileItems>` property to false in the project file.</span></span> <span data-ttu-id="57d17-293">Pak můžete zkopírovat `<Compile Include>` položky ze starého souboru projektu do nového, aby bylo možné sestavit pouze zdroje, které mají být zahrnuty.</span><span class="sxs-lookup"><span data-stu-id="57d17-293">Then, you can copy `<Compile Include>` items from the old project file to the new one in order to only build sources you intended to include.</span></span> <span data-ttu-id="57d17-294">Podobně `<EnableDefaultPageItems>` lze použít k vypnutí automatického zahrnutí stránek XAML a `<EnableDefaultItems>` může řídit jak jedinou vlastností.</span><span class="sxs-lookup"><span data-stu-id="57d17-294">Similarly, `<EnableDefaultPageItems>` can be used to turn off auto-inclusion of XAML pages and `<EnableDefaultItems>` can control both with a single property.</span></span>

### <a name="a-brief-aside-on-multi-pass-compilers"></a><span data-ttu-id="57d17-295">Stručný výběr v kompilátorech s více průchody</span><span class="sxs-lookup"><span data-stu-id="57d17-295">A brief aside on multi-pass compilers</span></span>

<span data-ttu-id="57d17-296">Po odebrání problematického souboru z ukázky pro obchodníka z fazolového účastníka můžete znovu sestavit a zobrazí se čtyři chyby.</span><span class="sxs-lookup"><span data-stu-id="57d17-296">After removing the offending file from the Bean Trader sample, you can re-build and will get four errors.</span></span> <span data-ttu-id="57d17-297">Ještě nemáte nějaké předplatné?</span><span class="sxs-lookup"><span data-stu-id="57d17-297">Didn't you have one before?</span></span> <span data-ttu-id="57d17-298">Proč počet chyb zachází?</span><span class="sxs-lookup"><span data-stu-id="57d17-298">Why did the number of errors go up?</span></span> <span data-ttu-id="57d17-299">Kompilátor jazyka C# je [Vícenásobný předávací kompilátor](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes).</span><span class="sxs-lookup"><span data-stu-id="57d17-299">The C# compiler is a [multi-pass compiler](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes).</span></span> <span data-ttu-id="57d17-300">To znamená, že každý zdrojový soubor projde dvakrát.</span><span class="sxs-lookup"><span data-stu-id="57d17-300">This means that it goes through each source file twice.</span></span> <span data-ttu-id="57d17-301">Za prvé kompilátor pouze prohlíží metadata a deklarace v každém zdrojovém souboru a identifikuje všechny problémy na úrovni deklarace.</span><span class="sxs-lookup"><span data-stu-id="57d17-301">First, the compiler just looks at metadata and declarations in each source file and identifies any declaration-level problems.</span></span> <span data-ttu-id="57d17-302">Jedná se o chyby, které jste opravili.</span><span class="sxs-lookup"><span data-stu-id="57d17-302">Those are the errors you've fixed.</span></span> <span data-ttu-id="57d17-303">Poté provede kód znovu a sestaví zdroj C# do IL; Ty jsou druhou sadou chyb, které vidíte nyní.</span><span class="sxs-lookup"><span data-stu-id="57d17-303">Then it goes through the code again to build the C# source into IL; those are the second set of errors that you're seeing now.</span></span>

> [!NOTE]
> <span data-ttu-id="57d17-304">Kompilátor jazyka C# provede [více než pouze dva průchody](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes), ale konečný výsledek je, že chyby kompilátoru pro velké změny kódu, jako by to vedlo k tomu, že jsou dva vlny.</span><span class="sxs-lookup"><span data-stu-id="57d17-304">The C# compiler does [more than just two passes](https://docs.microsoft.com/archive/blogs/ericlippert/how-many-passes), but the end result is that compiler errors for large code changes like this tend to come in two waves.</span></span>

### <a name="third-party-dependency-fixes-castlewindsor"></a><span data-ttu-id="57d17-305">Opravy závislostí třetích stran (Castle. Windsor)</span><span class="sxs-lookup"><span data-stu-id="57d17-305">Third-party dependency fixes (Castle.Windsor)</span></span>

<span data-ttu-id="57d17-306">Další třídou problému, která se nachází v některých scénářích migrace, jsou rozdíly v rozhraní API mezi .NET Framework a verzemi závislostí .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-306">Another class of issue that comes up in some migration scenarios is API differences between .NET Framework and .NET Core versions of dependencies.</span></span> <span data-ttu-id="57d17-307">I v případě, že balíček NuGet cílí na .NET Framework i .NET Standard nebo .NET Core, můžou existovat různé knihovny pro použití s různými cíli .NET.</span><span class="sxs-lookup"><span data-stu-id="57d17-307">Even if a NuGet package targets both .NET Framework and .NET Standard or .NET Core, there may be different libraries for use with different .NET targets.</span></span> <span data-ttu-id="57d17-308">Díky tomu mohou balíčky podporovat mnoho různých platforem .NET, které mohou vyžadovat různé implementace.</span><span class="sxs-lookup"><span data-stu-id="57d17-308">This allows the packages to support many different .NET platforms, which may require different implementations.</span></span> <span data-ttu-id="57d17-309">Také to znamená, že při cílení na různé platformy .NET můžou knihovny obsahovat malé rozdíly v rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="57d17-309">It also means that there may be small API differences in the libraries when targeting different .NET platforms.</span></span>

<span data-ttu-id="57d17-310">Další sada chyb, které vidíte v ukázce obchodníka v programu Bob, se vztahuje `Castle.Windsor` na rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="57d17-310">The next set of errors you'll see in the Bean Trader sample are related to `Castle.Windsor` APIs.</span></span> <span data-ttu-id="57d17-311">Projekt .NET Core Bob používá stejnou verzi `Castle.Windsor` jako projekt cílený na .NET Framework (4.1.1), ale implementace těchto dvou platforem se mírně liší.</span><span class="sxs-lookup"><span data-stu-id="57d17-311">The .NET Core Bean Trader project uses the same version of `Castle.Windsor` as the .NET Framework-targeted project (4.1.1), but the implementations for those two platforms are slightly different.</span></span>

<span data-ttu-id="57d17-312">V takovém případě se zobrazí následující problémy, které je potřeba opravit:</span><span class="sxs-lookup"><span data-stu-id="57d17-312">In this case, you see the following issues that need to be fixed:</span></span>

1. <span data-ttu-id="57d17-313">`Castle.MicroKernel.Registration.Classes.FromThisAssembly`není k dispozici v .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-313">`Castle.MicroKernel.Registration.Classes.FromThisAssembly` isn't available on .NET Core.</span></span> <span data-ttu-id="57d17-314">K dispozici je však podobné rozhraní API `Classes.FromAssemblyContaining` , takže můžeme nahradit obě použití `Classes.FromThisAssembly()` voláním metody `Classes.FromAssemblyContaining(t)`, kde `t` je typ, který provádí volání.</span><span class="sxs-lookup"><span data-stu-id="57d17-314">There is, however, the similar API `Classes.FromAssemblyContaining` available, so we can replace both uses of `Classes.FromThisAssembly()` with calls to `Classes.FromAssemblyContaining(t)`, where `t` is the type making the call.</span></span>
1. <span data-ttu-id="57d17-315">Podobně v *Bootstrapper.cs* `Castle.Windsor.Installer.FromAssembly`. Tato technologie není v .NET Core dostupná.</span><span class="sxs-lookup"><span data-stu-id="57d17-315">Similarly, in *Bootstrapper.cs*, `Castle.Windsor.Installer.FromAssembly`.This is unavailable on .NET Core.</span></span> <span data-ttu-id="57d17-316">Místo toho lze toto volání nahradit pomocí `FromAssembly.Containing(typeof(Bootstrapper))`.</span><span class="sxs-lookup"><span data-stu-id="57d17-316">Instead, that call can be replaced with `FromAssembly.Containing(typeof(Bootstrapper))`.</span></span>

### <a name="updating-wcf-client-usage"></a><span data-ttu-id="57d17-317">Aktualizace využití klienta WCF</span><span class="sxs-lookup"><span data-stu-id="57d17-317">Updating WCF client usage</span></span>

<span data-ttu-id="57d17-318">V případě, `Castle.Windsor` že jsou rozdíly vyřešeny, poslední zbývající chyba sestavení v projektu .NET Core Bob obchodník `BeanTraderServiceClient` je, že (která `DuplexClientBase`je odvozena z `Open` ) nemá metodu.</span><span class="sxs-lookup"><span data-stu-id="57d17-318">Having fixed the `Castle.Windsor` differences, the last remaining build error in the .NET Core Bean Trader project is that `BeanTraderServiceClient` (which derives from `DuplexClientBase`) doesn't have an `Open` method.</span></span> <span data-ttu-id="57d17-319">To není překvapivé, protože se jedná o rozhraní API, které na začátku tohoto procesu migrace zvýrazní analyzátor přenositelnosti .NET.</span><span class="sxs-lookup"><span data-stu-id="57d17-319">This isn't surprising since this is an API that was highlighted by the .NET Portability Analyzer at the beginning of this migration process.</span></span> <span data-ttu-id="57d17-320">Podíváme se na to, co `BeanTraderServiceClient` se věnuje pozornost většímu problému, ale.</span><span class="sxs-lookup"><span data-stu-id="57d17-320">Looking at `BeanTraderServiceClient` draws our attention to a larger issue, though.</span></span> <span data-ttu-id="57d17-321">Tento klient WCF byl automaticky vygenerován nástrojem [Svcutil. exe](../../framework/wcf/servicemodel-metadata-utility-tool-svcutil-exe.md) .</span><span class="sxs-lookup"><span data-stu-id="57d17-321">This WCF client was autogenerated by the [Svcutil.exe](../../framework/wcf/servicemodel-metadata-utility-tool-svcutil-exe.md) tool.</span></span>

<span data-ttu-id="57d17-322">**Klienti WCF vygenerované službou Svcutil jsou určeny pro použití v .NET Framework.**</span><span class="sxs-lookup"><span data-stu-id="57d17-322">**WCF clients generated by Svcutil are meant for use on .NET Framework.**</span></span>

<span data-ttu-id="57d17-323">Řešení, která používají Svcutil klienty WCF, budou muset znovu vygenerovat klienty kompatibilní s .NET Standard pro použití s .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-323">Solutions that use svcutil-generated WCF clients will need to regenerate .NET Standard-compatible clients for use with .NET Core.</span></span> <span data-ttu-id="57d17-324">Jedním z hlavních důvodů, proč původní klienti nebudou fungovat, jsou závislé na konfiguraci aplikací pro definování vazeb a koncových bodů WCF.</span><span class="sxs-lookup"><span data-stu-id="57d17-324">One of the main reasons the old clients won't work is that they depend on app configuration for defining WCF bindings and endpoints.</span></span> <span data-ttu-id="57d17-325">Protože .NET Standard rozhraní API WCF můžou pracovat na různých platformách (kde nejsou k dispozici rozhraní API pro System. Configuration), klienti WCF pro scénáře .NET Core a .NET Standard musí definovat vazby a koncové body programově místo v konfiguraci.</span><span class="sxs-lookup"><span data-stu-id="57d17-325">Because .NET Standard WCF APIs can work cross-platform (where System.Configuration APIs aren't available), WCF clients for .NET Core and .NET Standard scenarios must define bindings and endpoints programmatically instead of in configuration.</span></span>

<span data-ttu-id="57d17-326">Ve skutečnosti bude nutné změnit použití všech klientů WCF, který `<system.serviceModel>` závisí na oddílu App. config (ať už vytvořené pomocí Svcutil nebo ručně), aby fungovalo na .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-326">In fact, any WCF client usage that depends on the `<system.serviceModel>` app.config section (whether created with Svcutil or manually) will need to be changed to work on .NET Core.</span></span>

<span data-ttu-id="57d17-327">Existují dva způsoby, jak automaticky generovat .NET Standard kompatibilních klientů WCF:</span><span class="sxs-lookup"><span data-stu-id="57d17-327">There are two ways to automatically generate .NET Standard-compatible WCF clients:</span></span>

- <span data-ttu-id="57d17-328">Tento `dotnet-svcutil` nástroj je nástroj .NET, který GENERUJE klienty WCF způsobem, který je podobný tomu, jak Svcutil pracovali dříve.</span><span class="sxs-lookup"><span data-stu-id="57d17-328">The `dotnet-svcutil` tool is a .NET tool that generates WCF clients in a way that is similar to how Svcutil worked previously.</span></span>
- <span data-ttu-id="57d17-329">Visual Studio může generovat klienty WCF pomocí možnosti [reference webové služby WCF](../../core/additional-tools/wcf-web-service-reference-guide.md) funkce připojené služby.</span><span class="sxs-lookup"><span data-stu-id="57d17-329">Visual Studio can generate WCF clients using the [WCF Web Service Reference](../../core/additional-tools/wcf-web-service-reference-guide.md) option of its Connected Services feature.</span></span>

<span data-ttu-id="57d17-330">Oba postupy fungují dobře.</span><span class="sxs-lookup"><span data-stu-id="57d17-330">Either approach works well.</span></span> <span data-ttu-id="57d17-331">Můžete samozřejmě napsat kód klienta WCF sami.</span><span class="sxs-lookup"><span data-stu-id="57d17-331">Alternatively, of course, you could write the WCF client code yourself.</span></span> <span data-ttu-id="57d17-332">V této ukázce se používá funkce připojená služba sady Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="57d17-332">For this sample, I chose to use the Visual Studio Connected Service feature.</span></span> <span data-ttu-id="57d17-333">Provedete to tak, že kliknete pravým tlačítkem na projekt *BeanTraderClient. Core* v Průzkumníku řešení sady Visual Studio a vyberete **Přidat** > **připojenou službu**.</span><span class="sxs-lookup"><span data-stu-id="57d17-333">To do that, right-click on the *BeanTraderClient.Core* project in Visual Studio's solution explorer and select **Add** > **Connected Service**.</span></span> <span data-ttu-id="57d17-334">Dále vyberte poskytovatele referencí webové služby WCF.</span><span class="sxs-lookup"><span data-stu-id="57d17-334">Next, choose the WCF Web Service Reference Provider.</span></span> <span data-ttu-id="57d17-335">Tím se zobrazí dialogové okno, kde můžete zadat adresu webové služby back-end účastníka (`localhost:8080` Pokud používáte server místně) a obor názvů, který mají generované typy použít (například**BeanTrader. Service**).</span><span class="sxs-lookup"><span data-stu-id="57d17-335">This will bring up a dialog where you can specify the address of the backend Bean Trader web service (`localhost:8080` if you are running the server locally) and the namespace that generated types should use (**BeanTrader.Service**, for example).</span></span>

![Dialogová okna propojené služby odkazu webové služby WCF](./media/convert-project-from-net-framework/connected-service-dialog.png)

<span data-ttu-id="57d17-337">Po výběru tlačítka **Dokončit** je do projektu přidán nový uzel připojené služby a do tohoto uzlu bude přidán soubor reference.cs, který obsahuje .NET Standard nového klienta WCF pro přístup ke službě pro přístup k nástroji fazole.</span><span class="sxs-lookup"><span data-stu-id="57d17-337">After you select the **Finish** button, a new Connected Services node is added to the project and a Reference.cs file is added under that node containing the new .NET Standard WCF client for accessing the Bean Trader service.</span></span> <span data-ttu-id="57d17-338">Pokud se podíváte `GetEndpointAddress` na `GetBindingForEndpoint` metody nebo v tomto souboru, uvidíte, že vazby a koncové body se nyní generují programově (místo pomocí konfigurace aplikace).</span><span class="sxs-lookup"><span data-stu-id="57d17-338">If you look at the `GetEndpointAddress` or `GetBindingForEndpoint` methods in that file, you'll see that bindings and endpoints are now generated programmatically (instead of via app config).</span></span> <span data-ttu-id="57d17-339">Funkce přidat připojené služby může také přidat odkazy na některé balíčky System. ServiceModel v souboru projektu, které nejsou potřeba, protože všechny potřebné balíčky WCF jsou zahrnuté přes Microsoft. Windows. Compatibility.</span><span class="sxs-lookup"><span data-stu-id="57d17-339">The 'Add Connected Services' feature may also add references to some System.ServiceModel packages in the project file, which aren't needed since all necessary WCF packages are included via Microsoft.Windows.Compatibility.</span></span> <span data-ttu-id="57d17-340">Zkontrolujte csproj a podívejte se, jestli se přidaly další `<PackageReference>` položky System. ServiceModel, a pokud ano, odeberte je.</span><span class="sxs-lookup"><span data-stu-id="57d17-340">Check the csproj to see if any extra System.ServiceModel `<PackageReference>` items have been added, and if so, remove them.</span></span>

<span data-ttu-id="57d17-341">Náš projekt teď má nové třídy klienta WCF (v *reference.cs*), ale zároveň má i staré (v BeanTrader.cs).</span><span class="sxs-lookup"><span data-stu-id="57d17-341">Our project has new WCF client classes now (in *Reference.cs*), but it also still has the old ones (in BeanTrader.cs).</span></span> <span data-ttu-id="57d17-342">V tuto chvíli jsou k dispozici dvě možnosti:</span><span class="sxs-lookup"><span data-stu-id="57d17-342">There are two options at this point:</span></span>

- <span data-ttu-id="57d17-343">Pokud chcete mít možnost sestavit původní .NET Framework projekt (společně s novým cílem rozhraní .NET Core), můžete použít `<Compile Remove="BeanTrader.cs" />` položku v souboru csproj projektu .NET Core tak, aby .NET Framework a verze .NET Core aplikace používaly různé klienty WCF.</span><span class="sxs-lookup"><span data-stu-id="57d17-343">If you want to be able to build the original .NET Framework project (alongside the new .NET Core-targeted one), you can use a `<Compile Remove="BeanTrader.cs" />` item in the .NET Core project's csproj file so that the .NET Framework and .NET Core versions of the app use different WCF clients.</span></span> <span data-ttu-id="57d17-344">Tato výhoda má opustit stávající projekt .NET Framework beze změny, ale má nevýhodu, že kód pomocí generovaných klientů WCF může být v případě .NET Core v případě, že je v projektu .NET Framework, trochu odlišný, takže pravděpodobně budete muset použít `#if` direktivy k podmíněně zkompilování některých využití klientů WCF (vytváření klientů, například), aby fungovaly jedním ze způsobů, jak je sestaven pro .NET Core, a jiný způsob, jak je sestaven pro .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="57d17-344">This has the advantage of leaving the existing .NET Framework project unchanged, but has the disadvantage that code using the generated WCF clients may need to be slightly different in the .NET Core case than it was in the .NET Framework project, so you'll likely need to use `#if` directives to conditionally compile some WCF client usage (creating clients, for example) to work one way when built for .NET Core and another way when built for .NET Framework.</span></span>

- <span data-ttu-id="57d17-345">Pokud je naopak některé změny kódu v existujícím projektu .NET Framework přijatelné, můžete *BeanTrader.cs* vše odebrat dohromady.</span><span class="sxs-lookup"><span data-stu-id="57d17-345">If, on the other hand, some code churn in the existing .NET Framework project is acceptable, you can remove *BeanTrader.cs* all together.</span></span> <span data-ttu-id="57d17-346">Vzhledem k tomu, že nový klient služby WCF je sestaven pro .NET Standard, bude fungovat ve scénářích .NET Core i .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="57d17-346">Because the new WCF client is built for .NET Standard, it will work in both .NET Core and .NET Framework scenarios.</span></span> <span data-ttu-id="57d17-347">Pokud sestavíte pro .NET Framework kromě .NET Core (buď pomocí cílení na více platforem, nebo se dvěma soubory csproj), můžete tento nový soubor *reference.cs* použít pro oba cíle.</span><span class="sxs-lookup"><span data-stu-id="57d17-347">If you are building for .NET Framework in addition to .NET Core (either by multi-targeting or by having two csproj files), you can use this new *Reference.cs* file for both targets.</span></span> <span data-ttu-id="57d17-348">Tento přístup má výhodu, že kód nemusí bifurcate k podpoře dvou různých klientů WCF. stejný kód bude použit všude.</span><span class="sxs-lookup"><span data-stu-id="57d17-348">This approach has the advantage that the code won't need to bifurcate to support two different WCF clients; the same code will be used everywhere.</span></span> <span data-ttu-id="57d17-349">Nevýhodou je, že zahrnuje změnu (PŘEDPOKLÁDANĚ stabilní) .NET Framework projektu.</span><span class="sxs-lookup"><span data-stu-id="57d17-349">The drawback is that it involves changing the (presumably stable) .NET Framework project.</span></span>

<span data-ttu-id="57d17-350">V případě ukázky programu Bob obchodník můžete v původním projektu provést drobné změny, pokud to usnadňuje migraci, takže pomocí těchto kroků sjednotete použití klientů WCF:</span><span class="sxs-lookup"><span data-stu-id="57d17-350">In the case of the Bean Trader sample, you can make small changes to the original project if it makes migration easier, so follow these steps to reconcile WCF client usage:</span></span>

01. <span data-ttu-id="57d17-351">Přidejte nový soubor Reference.cs do projektu .NET Framework *BeanTraderClient. csproj* pomocí kontextové nabídky Přidat existující položku z Průzkumníka řešení.</span><span class="sxs-lookup"><span data-stu-id="57d17-351">Add the new Reference.cs file to the .NET Framework *BeanTraderClient.csproj* project using the 'Add existing item' context menu from the solution explorer.</span></span> <span data-ttu-id="57d17-352">Nezapomeňte přidat "as Link", aby oba projekty používaly stejný soubor (na rozdíl od kopírování souboru v jazyce C#).</span><span class="sxs-lookup"><span data-stu-id="57d17-352">Be sure to add 'as link' so that the same file is used by both projects (as opposed to copying the C# file).</span></span> <span data-ttu-id="57d17-353">Pokud vytváříte rozhraní .NET Core i .NET Framework s jedním csproj (pomocí cílení na více platforem), tento krok není nezbytný.</span><span class="sxs-lookup"><span data-stu-id="57d17-353">If you are building for both .NET Core and .NET Framework with a single csproj (using multi-targeting) then this step isn't necessary.</span></span>

01. <span data-ttu-id="57d17-354">Odstraňte *BeanTrader.cs*.</span><span class="sxs-lookup"><span data-stu-id="57d17-354">Delete *BeanTrader.cs*.</span></span>

01. <span data-ttu-id="57d17-355">Nový klient služby WCF je podobný původnímu, ale několik oborů názvů v generovaném kódu se liší.</span><span class="sxs-lookup"><span data-stu-id="57d17-355">The new WCF client is similar to the old one, but a number of namespaces in the generated code are different.</span></span> <span data-ttu-id="57d17-356">Z tohoto důvodu je nutné aktualizovat projekt tak, aby se typy klientů WCF používaly ze služby BeanTrader. Service (nebo libovolného názvu oboru názvů, který jste zvolili) místo BeanTrader. model nebo bez oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="57d17-356">Because of this, it is necessary to update the project so that WCF client types are used from BeanTrader.Service (or whatever namespace name you chose) instead of BeanTrader.Model or without a namespace.</span></span> <span data-ttu-id="57d17-357">Sestavování *BeanTraderClient. Core. csproj* vám pomůže zjistit, kde se tyto změny musí provést.</span><span class="sxs-lookup"><span data-stu-id="57d17-357">Building *BeanTraderClient.Core.csproj* will help to identify where these changes need to be made.</span></span> <span data-ttu-id="57d17-358">Opravy budou potřeba v C# i ve zdrojových souborech XAML.</span><span class="sxs-lookup"><span data-stu-id="57d17-358">Fixes will be needed both in C# and in XAML source files.</span></span>

01. <span data-ttu-id="57d17-359">Nakonec zjistíte, že v *BeanTraderServiceClientFactory.cs* dojde k chybě, protože se změnily dostupné konstruktory pro `BeanTraderServiceClient` daný typ.</span><span class="sxs-lookup"><span data-stu-id="57d17-359">Finally, you'll discover that there is an error in *BeanTraderServiceClientFactory.cs* because the available constructors for the `BeanTraderServiceClient` type have changed.</span></span> <span data-ttu-id="57d17-360">Slouží k zadání `InstanceContext` argumentu (který byl vytvořen pomocí `CallbackHandler` z kontejneru `Castle.Windsor` IOC).</span><span class="sxs-lookup"><span data-stu-id="57d17-360">It used to be possible to supply an `InstanceContext` argument (which was created using a `CallbackHandler` from the `Castle.Windsor` IoC container).</span></span> <span data-ttu-id="57d17-361">Nové konstruktory vytvoří nové `CallbackHandler`s.</span><span class="sxs-lookup"><span data-stu-id="57d17-361">The new constructors create new `CallbackHandler`s.</span></span> <span data-ttu-id="57d17-362">Existují však konstruktory v `BeanTraderServiceClient`základním typu, které odpovídají požadovaným požadavkům.</span><span class="sxs-lookup"><span data-stu-id="57d17-362">There are, however, constructors in `BeanTraderServiceClient`'s base type that match what you want.</span></span> <span data-ttu-id="57d17-363">Vzhledem k tomu, že automaticky generovaný kód klienta WCF existuje v dílčích třídách, je možné jej snadno zvětšit.</span><span class="sxs-lookup"><span data-stu-id="57d17-363">Since the autogenerated WCF client code all exists in partial classes, you can easily extend it.</span></span> <span data-ttu-id="57d17-364">Chcete-li to provést, vytvořte nový soubor s názvem *BeanTraderServiceClient.cs* a pak vytvořte částečnou třídu se stejným názvem (pomocí oboru názvů BeanTrader. Service).</span><span class="sxs-lookup"><span data-stu-id="57d17-364">To do this, create a new file called *BeanTraderServiceClient.cs* and then create a partial class with that same name (using the BeanTrader.Service namespace).</span></span> <span data-ttu-id="57d17-365">Pak přidejte jeden konstruktor na částečný typ, jak je znázorněno zde.</span><span class="sxs-lookup"><span data-stu-id="57d17-365">Then, add one constructor to the partial type as shown here.</span></span>

    ```csharp
    public BeanTraderServiceClient(System.ServiceModel.InstanceContext callbackInstance) :
        base(callbackInstance, EndpointConfiguration.NetTcpBinding_BeanTraderService)
            { }
    ```

<span data-ttu-id="57d17-366">V případě těchto změn nyní bude ukázka pro obchodníky v programu Bob používat nového klienta WCF kompatibilního s .NET Standard a můžete provést konečnou opravu změny `Open` volání v *TradingService.cs* . `await OpenAsync`</span><span class="sxs-lookup"><span data-stu-id="57d17-366">With those changes made, the Bean Trader sample will now be using a new .NET Standard-compatible WCF client and you can make the final fix of changing the `Open` call in *TradingService.cs* to use `await OpenAsync` instead.</span></span>

<span data-ttu-id="57d17-367">S vyřešenými problémy WCF se nyní sestavuje verze .NET Core ukázkového obchodníka v programu Bob.</span><span class="sxs-lookup"><span data-stu-id="57d17-367">With the WCF issues addressed, the .NET Core version of the Bean Trader sample now builds cleanly!</span></span>

## <a name="runtime-testing"></a><span data-ttu-id="57d17-368">Testování za běhu</span><span class="sxs-lookup"><span data-stu-id="57d17-368">Runtime testing</span></span>

<span data-ttu-id="57d17-369">Je snadné se zapomenout, že migrace se neprovádí ihned po vyčištění sestavení projektu proti .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-369">It's easy to forget that migration work isn't done as soon as the project builds cleanly against .NET Core.</span></span> <span data-ttu-id="57d17-370">Je důležité, abyste ponechali čas při testování portu aplikace.</span><span class="sxs-lookup"><span data-stu-id="57d17-370">It's important to leave time for testing the ported app, too.</span></span> <span data-ttu-id="57d17-371">Jakmile se všechno úspěšně sestaví, ujistěte se, že aplikace běží a funguje podle očekávání, zvlášť pokud používáte jakékoliv balíčky cílené .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="57d17-371">Once things build successfully, make sure the app runs and works as expected, especially if you are using any packages targeting .NET Framework.</span></span>

<span data-ttu-id="57d17-372">Pojďme se pokusit spustit aplikaci s portem pro obchodní informování a zjistit, co se stane.</span><span class="sxs-lookup"><span data-stu-id="57d17-372">Let's try launching the ported Bean Trader app and see what happens.</span></span> <span data-ttu-id="57d17-373">Aplikace se před selháním nezdaří s následující výjimkou.</span><span class="sxs-lookup"><span data-stu-id="57d17-373">The app doesn't get far before failing with the following exception.</span></span>

```output
System.Configuration.ConfigurationErrorsException: 'Configuration system failed to initialize'

Inner Exception
ConfigurationErrorsException: Unrecognized configuration section system.serviceModel.
```

<span data-ttu-id="57d17-374">To samozřejmě dává smysl.</span><span class="sxs-lookup"><span data-stu-id="57d17-374">This makes sense, of course.</span></span> <span data-ttu-id="57d17-375">Pamatujte, že WCF už nepoužívá konfiguraci aplikací, takže je potřeba odebrat starý oddíl System. serviceModel souboru App. config.</span><span class="sxs-lookup"><span data-stu-id="57d17-375">Remember that WCF no longer uses app configuration, so the old system.serviceModel section of the app.config file needs to be removed.</span></span> <span data-ttu-id="57d17-376">Aktualizovaný klient služby WCF zahrnuje všechny stejné informace ve svém kódu, takže konfigurační oddíl už není potřeba.</span><span class="sxs-lookup"><span data-stu-id="57d17-376">The updated WCF client includes all of the same information in its code, so the config section isn't needed anymore.</span></span> <span data-ttu-id="57d17-377">Pokud jste chtěli nakonfigurovat koncový bod WCF v App. config, můžete ho přidat jako nastavení aplikace a aktualizovat kód klienta WCF pro načtení koncového bodu služby WCF z konfigurace.</span><span class="sxs-lookup"><span data-stu-id="57d17-377">If you wanted the WCF endpoint to be configurable in app.config, you could add it as an app setting and update the WCF client code to retrieve the WCF service endpoint from configuration.</span></span>

<span data-ttu-id="57d17-378">Po odebrání oddílu System. serviceModel v *App. config*se aplikace spustí, ale když se uživatel přihlásí, dojde k chybě s jinou výjimkou.</span><span class="sxs-lookup"><span data-stu-id="57d17-378">After removing the system.serviceModel section of *app.config*, the app launches but fails with another exception when a user signs in.</span></span>

```output
System.PlatformNotSupportedException: 'Operation is not supported on this platform.'
```

<span data-ttu-id="57d17-379">Nepodporované rozhraní API `Func<T>.BeginInvoke`je.</span><span class="sxs-lookup"><span data-stu-id="57d17-379">The unsupported API is `Func<T>.BeginInvoke`.</span></span> <span data-ttu-id="57d17-380">Jak je vysvětleno v [dotnet/corefx # 5940](https://github.com/dotnet/corefx/issues/5940), .NET Core nepodporuje `BeginInvoke` metody `EndInvoke` a na typech delegátů z důvodu základních závislostí vzdálené komunikace.</span><span class="sxs-lookup"><span data-stu-id="57d17-380">As explained in [dotnet/corefx#5940](https://github.com/dotnet/corefx/issues/5940), .NET Core doesn't support the `BeginInvoke` and `EndInvoke` methods on delegate types due to underlying remoting dependencies.</span></span> <span data-ttu-id="57d17-381">Tento problém a jeho opravu jsou podrobněji popsány v článku [migrace delegáta. BeginInvoke volání pro](https://devblogs.microsoft.com/dotnet/migrating-delegate-begininvoke-calls-for-net-core/) Blogový příspěvek .NET Core, ale v registru `BeginInvoke` je `EndInvoke` to, že pokud je `Task.Run` to možné, by volání měla být nahrazena (nebo asynchronními alternativami).</span><span class="sxs-lookup"><span data-stu-id="57d17-381">This issue and its fix are explained in more detail in the [Migrating Delegate.BeginInvoke Calls for .NET Core](https://devblogs.microsoft.com/dotnet/migrating-delegate-begininvoke-calls-for-net-core/) blog post, but the gist is that `BeginInvoke` and `EndInvoke` calls should be replaced with `Task.Run` (or async alternatives, if possible).</span></span> <span data-ttu-id="57d17-382">Když použijete obecné řešení, `BeginInvoke` volání se dá nahradit `Invoke` voláním spuštěným v. `Task.Run`</span><span class="sxs-lookup"><span data-stu-id="57d17-382">Applying the general solution here, the `BeginInvoke` call can be replaced with an `Invoke` call launched by `Task.Run`.</span></span>

```csharp
Task.Run(() =>
{
    return userInfoRetriever.Invoke();
}).ContinueWith(result =>
{
    // BeginInvoke's callback is replaced with ContinueWith
    var task = result.ConfigureAwait(false);
    CurrentTrader = task.GetAwaiter().GetResult();
}, TaskScheduler.Default);
```

<span data-ttu-id="57d17-383">Po odebrání `BeginInvoke` použití se aplikace Bob obchodník úspěšně spustí pro .NET Core.</span><span class="sxs-lookup"><span data-stu-id="57d17-383">After removing the `BeginInvoke` usage, the Bean Trader app runs successfully on .NET Core!</span></span>

![Obchodník pro fazole běžící v .NET Core](./media/convert-project-from-net-framework/running-on-core.png)

<span data-ttu-id="57d17-385">Všechny aplikace se liší, takže konkrétní kroky potřebné k migraci vlastních aplikací do .NET Core se budou lišit.</span><span class="sxs-lookup"><span data-stu-id="57d17-385">All apps are different, so the specific steps needed to migrate your own apps to .NET Core will vary.</span></span> <span data-ttu-id="57d17-386">Ale snad v ukázce Bob obchodník ukazuje obecný pracovní postup a typy problémů, které je možné očekávat.</span><span class="sxs-lookup"><span data-stu-id="57d17-386">But hopefully the Bean Trader sample demonstrates the general workflow and the types of issues that can be expected.</span></span> <span data-ttu-id="57d17-387">A i přes délku tohoto článku se skutečné změny potřebné v ukázce pro obchodníka v rámci nástroje Bob, aby fungovaly v .NET Core, byly poměrně omezené.</span><span class="sxs-lookup"><span data-stu-id="57d17-387">And, despite this article's length, the actual changes needed in the Bean Trader sample to make it work on .NET Core were fairly limited.</span></span> <span data-ttu-id="57d17-388">Mnoho aplikací migruje do .NET Core stejným způsobem; s omezením nebo dokonce bez nutnosti změn kódu.</span><span class="sxs-lookup"><span data-stu-id="57d17-388">Many apps migrate to .NET Core in this same way; with limited or even no code changes needed.</span></span>
