---
title: Třídy (F#)
description: 'Zjistěte, jak třídy F # jsou typy, které představují objekty, které mohou mít vlastnosti, metody a události.'
ms.date: 05/16/2016
ms.openlocfilehash: 71cd713d192d28565e879b79b2fc9e0530e5f841
ms.sourcegitcommit: ad99773e5e45068ce03b99518008397e1299e0d1
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/22/2018
ms.locfileid: "46577370"
---
# <a name="classes"></a><span data-ttu-id="d5e49-103">Třídy</span><span class="sxs-lookup"><span data-stu-id="d5e49-103">Classes</span></span>

<span data-ttu-id="d5e49-104">*Třídy* jsou typy, které představují objekty, které mohou mít vlastnosti, metody a události.</span><span class="sxs-lookup"><span data-stu-id="d5e49-104">*Classes* are types that represent objects that can have properties, methods, and events.</span></span>

## <a name="syntax"></a><span data-ttu-id="d5e49-105">Syntaxe</span><span class="sxs-lookup"><span data-stu-id="d5e49-105">Syntax</span></span>

```fsharp
// Class definition:
type [access-modifier] type-name [type-params] [access-modifier] ( parameter-list ) [ as identifier ] =
[ class ]
[ inherit base-type-name(base-constructor-args) ]
[ let-bindings ]
[ do-bindings ]
member-list
...
[ end ]
// Mutually recursive class definitions:
type [access-modifier] type-name1 ...
and [access-modifier] type-name2 ...
...
```

## <a name="remarks"></a><span data-ttu-id="d5e49-106">Poznámky</span><span class="sxs-lookup"><span data-stu-id="d5e49-106">Remarks</span></span>

<span data-ttu-id="d5e49-107">Třídy představují základní popis typů objektů .NET; Třída je koncept primární typ, který podporuje objektově orientované programování v jazyce F #.</span><span class="sxs-lookup"><span data-stu-id="d5e49-107">Classes represent the fundamental description of .NET object types; the class is the primary type concept that supports object-oriented programming in F#.</span></span>

<span data-ttu-id="d5e49-108">V předchozí syntaxi `type-name` je libovolný platný identifikátor.</span><span class="sxs-lookup"><span data-stu-id="d5e49-108">In the preceding syntax, the `type-name` is any valid identifier.</span></span> <span data-ttu-id="d5e49-109">`type-params` Popisuje volitelné obecné parametry typu.</span><span class="sxs-lookup"><span data-stu-id="d5e49-109">The `type-params` describes optional generic type parameters.</span></span> <span data-ttu-id="d5e49-110">Zahrnuje názvy parametrů typů a omezením uzavřen do lomených závorek (`<` a `>`).</span><span class="sxs-lookup"><span data-stu-id="d5e49-110">It consists of type parameter names and constraints enclosed in angle brackets (`<` and `>`).</span></span> <span data-ttu-id="d5e49-111">Další informace najdete v tématu [obecných typů](generics/index.md) a [omezení](generics/constraints.md).</span><span class="sxs-lookup"><span data-stu-id="d5e49-111">For more information, see [Generics](generics/index.md) and [Constraints](generics/constraints.md).</span></span> <span data-ttu-id="d5e49-112">`parameter-list` Popisuje parametry konstruktoru.</span><span class="sxs-lookup"><span data-stu-id="d5e49-112">The `parameter-list` describes constructor parameters.</span></span> <span data-ttu-id="d5e49-113">První modifikátor přístupu vztahují na daný typ druhý se vztahují k primárnímu konstruktoru.</span><span class="sxs-lookup"><span data-stu-id="d5e49-113">The first access modifier pertains to the type; the second pertains to the primary constructor.</span></span> <span data-ttu-id="d5e49-114">V obou případech se výchozí hodnota je `public`.</span><span class="sxs-lookup"><span data-stu-id="d5e49-114">In both cases, the default is `public`.</span></span>

<span data-ttu-id="d5e49-115">Zadejte základní třídu pro třídy pomocí `inherit` – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="d5e49-115">You specify the base class for a class by using the `inherit` keyword.</span></span> <span data-ttu-id="d5e49-116">Je nutné zadat argumenty, v závorkách, pro konstruktor základní třídy.</span><span class="sxs-lookup"><span data-stu-id="d5e49-116">You must supply arguments, in parentheses, for the base class constructor.</span></span>

<span data-ttu-id="d5e49-117">Deklarace pole nebo hodnoty, které jsou místní pro třídu pomocí funkce `let` vazby a musí následovat po obecná pravidla pro `let` vazby.</span><span class="sxs-lookup"><span data-stu-id="d5e49-117">You declare fields or function values that are local to the class by using `let` bindings, and you must follow the general rules for `let` bindings.</span></span> <span data-ttu-id="d5e49-118">`do-bindings` Části obsahuje kód, který se spustí při vytváření objektu.</span><span class="sxs-lookup"><span data-stu-id="d5e49-118">The `do-bindings` section includes code to be executed upon object construction.</span></span>

<span data-ttu-id="d5e49-119">`member-list` Se skládá z dalších konstruktory, instanci a deklarace statické metody, deklarace rozhraní, abstraktní vazby a deklaracích vlastností a událostí.</span><span class="sxs-lookup"><span data-stu-id="d5e49-119">The `member-list` consists of additional constructors, instance and static method declarations, interface declarations, abstract bindings, and property and event declarations.</span></span> <span data-ttu-id="d5e49-120">Tyto možnosti jsou popsány v [členy](members/index.md).</span><span class="sxs-lookup"><span data-stu-id="d5e49-120">These are described in [Members](members/index.md).</span></span>

<span data-ttu-id="d5e49-121">`identifier` , Který se používá s nepovinným `as` – klíčové slovo udává název proměnnou instance nebo vlastní identifikátor, který je možné odkazovat na instanci typu v definici typu.</span><span class="sxs-lookup"><span data-stu-id="d5e49-121">The `identifier` that is used with the optional `as` keyword gives a name to the instance variable, or self identifier, which can be used in the type definition to refer to the instance of the type.</span></span> <span data-ttu-id="d5e49-122">Další informace najdete v části identifikátory Self dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="d5e49-122">For more information, see the section Self Identifiers later in this topic.</span></span>

<span data-ttu-id="d5e49-123">Klíčová slova `class` a `end` toto označení začátku a konce definice jsou volitelné.</span><span class="sxs-lookup"><span data-stu-id="d5e49-123">The keywords `class` and `end` that mark the start and end of the definition are optional.</span></span>

<span data-ttu-id="d5e49-124">Vzájemně rekurzivních typů, které jsou typy, které odkazují na sobě navzájem, jsou spojeny spolu s `and` – klíčové slovo pouze jsou vzájemně rekurzivní funkce.</span><span class="sxs-lookup"><span data-stu-id="d5e49-124">Mutually recursive types, which are types that reference each other, are joined together with the `and` keyword just as mutually recursive functions are.</span></span> <span data-ttu-id="d5e49-125">Příklad najdete v části vzájemně rekurzivních typů.</span><span class="sxs-lookup"><span data-stu-id="d5e49-125">For an example, see the section Mutually Recursive Types.</span></span>

## <a name="constructors"></a><span data-ttu-id="d5e49-126">Konstruktory</span><span class="sxs-lookup"><span data-stu-id="d5e49-126">Constructors</span></span>

<span data-ttu-id="d5e49-127">Konstruktor je kód, který vytvoří instanci typu třídy.</span><span class="sxs-lookup"><span data-stu-id="d5e49-127">The constructor is code that creates an instance of the class type.</span></span> <span data-ttu-id="d5e49-128">Konstruktory pro třídy fungují v jazyce F # trochu jinak než v jiných jazycích rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="d5e49-128">Constructors for classes work somewhat differently in F# than they do in other .NET languages.</span></span> <span data-ttu-id="d5e49-129">V F # třídy, je vždy primární konstruktor, jehož argumenty jsou popsány v `parameter-list` , který následuje název typu a jehož subjekt se skládá z `let` (a `let rec`) vazby na začátku deklarace třídy a `do`vazby, které následují.</span><span class="sxs-lookup"><span data-stu-id="d5e49-129">In an F# class, there is always a primary constructor whose arguments are described in the `parameter-list` that follows the type name, and whose body consists of the `let` (and `let rec`) bindings at the start of the class declaration and the `do` bindings that follow.</span></span> <span data-ttu-id="d5e49-130">Argumenty konstruktoru primární jsou v oboru v rámci deklarace třídy.</span><span class="sxs-lookup"><span data-stu-id="d5e49-130">The arguments of the primary constructor are in scope throughout the class declaration.</span></span>

<span data-ttu-id="d5e49-131">Můžete přidat další konstruktory pomocí `new` – klíčové slovo přidání člena, následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="d5e49-131">You can add additional constructors by using the `new` keyword to add a member, as follows:</span></span>

<span data-ttu-id="d5e49-132">`new`(`argument-list`) = `constructor-body`</span><span class="sxs-lookup"><span data-stu-id="d5e49-132">`new`(`argument-list`) = `constructor-body`</span></span>

<span data-ttu-id="d5e49-133">Tělo konstruktoru new musí vyvolat primární konstruktor, který je zadán v horní části deklarace třídy.</span><span class="sxs-lookup"><span data-stu-id="d5e49-133">The body of the new constructor must invoke the primary constructor that is specified at the top of the class declaration.</span></span>

<span data-ttu-id="d5e49-134">Tento koncept znázorňuje následující příklad.</span><span class="sxs-lookup"><span data-stu-id="d5e49-134">The following example illustrates this concept.</span></span> <span data-ttu-id="d5e49-135">V následujícím kódu `MyClass` má dva konstruktory, primárního konstruktoru, který přebírá dva argumenty a jiného konstruktoru, který nepřijímá žádné argumenty.</span><span class="sxs-lookup"><span data-stu-id="d5e49-135">In the following code, `MyClass` has two constructors, a primary constructor that takes two arguments and another constructor that takes no arguments.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-1/snippet2401.fs)]

## <a name="let-and-do-bindings"></a><span data-ttu-id="d5e49-136">let a do – vazby</span><span class="sxs-lookup"><span data-stu-id="d5e49-136">let and do Bindings</span></span>

<span data-ttu-id="d5e49-137">`let` a `do` vazby v definici třídy formuláře těla konstruktoru třídy primární, a spouští se proto vždy, když je vytvořena instance třídy.</span><span class="sxs-lookup"><span data-stu-id="d5e49-137">The `let` and `do` bindings in a class definition form the body of the primary class constructor, and therefore they run whenever a class instance is created.</span></span> <span data-ttu-id="d5e49-138">Pokud `let` vazby je funkce, pak se zkompiluje do člena.</span><span class="sxs-lookup"><span data-stu-id="d5e49-138">If a `let` binding is a function, then it is compiled into a member.</span></span> <span data-ttu-id="d5e49-139">Pokud `let` vazby má hodnotu, která se nepoužívá žádné funkce nebo člen, bude se zkompiluje do proměnné, která je místní pro konstruktor.</span><span class="sxs-lookup"><span data-stu-id="d5e49-139">If the `let` binding is a value that is not used in any function or member, then it is compiled into a variable that is local to the constructor.</span></span> <span data-ttu-id="d5e49-140">V opačném případě je zkompilován do pole třídy.</span><span class="sxs-lookup"><span data-stu-id="d5e49-140">Otherwise, it is compiled into a field of the class.</span></span> <span data-ttu-id="d5e49-141">`do` Výrazy, které následují jsou kompilovány do primárního konstruktoru a spustit kód inicializace pro každou instanci.</span><span class="sxs-lookup"><span data-stu-id="d5e49-141">The `do` expressions that follow are compiled into the primary constructor and execute initialization code for every instance.</span></span> <span data-ttu-id="d5e49-142">Protože žádné další konstruktory vždy volat primárnímu konstruktoru `let` vazby a `do` vazby vždy bude provedena bez ohledu na to, které se nazývá konstruktor.</span><span class="sxs-lookup"><span data-stu-id="d5e49-142">Because any additional constructors always call the primary constructor, the `let` bindings and `do` bindings always execute regardless of which constructor is called.</span></span>

<span data-ttu-id="d5e49-143">Pole, která vytvořila `let` vazby je přístupná v rámci metody a vlastnosti třídy, ale jsou přístupné z statické metody, i v případě, že statické metody přijímají proměnnou instance jako parametr.</span><span class="sxs-lookup"><span data-stu-id="d5e49-143">Fields that are created by `let` bindings can be accessed throughout the methods and properties of the class; however, they cannot be accessed from static methods, even if the static methods take an instance variable as a parameter.</span></span> <span data-ttu-id="d5e49-144">K nim nelze přistupovat pomocí identifikátoru samotného, pokud existuje.</span><span class="sxs-lookup"><span data-stu-id="d5e49-144">They cannot be accessed by using the self identifier, if one exists.</span></span>

## <a name="self-identifiers"></a><span data-ttu-id="d5e49-145">Self – identifikátory</span><span class="sxs-lookup"><span data-stu-id="d5e49-145">Self Identifiers</span></span>

<span data-ttu-id="d5e49-146">A *identifikátoru samotného* je název, který představuje aktuální instanci.</span><span class="sxs-lookup"><span data-stu-id="d5e49-146">A *self identifier* is a name that represents the current instance.</span></span> <span data-ttu-id="d5e49-147">Self – identifikátory vypadat podobně jako `this` – klíčové slovo v jazyce C# nebo C++ nebo `Me` v jazyce Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="d5e49-147">Self identifiers resemble the `this` keyword in C# or C++ or `Me` in Visual Basic.</span></span> <span data-ttu-id="d5e49-148">Můžete definovat vlastní identifikátor dvěma různými způsoby v závislosti na tom, zda chcete identifikátoru samotného v oboru pro definici celé jedné třídy nebo pouze pro jednotlivé metody.</span><span class="sxs-lookup"><span data-stu-id="d5e49-148">You can define a self identifier in two different ways, depending on whether you want the self identifier to be in scope for the whole class definition or just for an individual method.</span></span>

<span data-ttu-id="d5e49-149">Chcete-li definovat vlastní identifikátor pro celou třídu, použijte `as` – klíčové slovo po pravých závorek parametru konstruktoru seznamu a zadejte název identifikátoru.</span><span class="sxs-lookup"><span data-stu-id="d5e49-149">To define a self identifier for the whole class, use the `as` keyword after the closing parentheses of the constructor parameter list, and specify the identifier name.</span></span>

<span data-ttu-id="d5e49-150">Pokud chcete definovat vlastní identifikátor pro právě jedna metoda, poskytují identifikátoru samotného v deklaraci člena, těsně před název metody a tečku (.) jako oddělovač.</span><span class="sxs-lookup"><span data-stu-id="d5e49-150">To define a self identifier for just one method, provide the self identifier in the member declaration, just before the method name and a period (.) as a separator.</span></span>

<span data-ttu-id="d5e49-151">Následující příklad kódu ukazuje dva způsoby vytvoření identifikátoru samotného.</span><span class="sxs-lookup"><span data-stu-id="d5e49-151">The following code example illustrates the two ways to create a self identifier.</span></span> <span data-ttu-id="d5e49-152">V prvním řádku `as` – klíčové slovo se používá k definování vlastní identifikátor.</span><span class="sxs-lookup"><span data-stu-id="d5e49-152">In the first line, the `as` keyword is used to define the self identifier.</span></span> <span data-ttu-id="d5e49-153">V pátém řádku identifikátor `this` se používá k definování vlastní identifikátor, jejichž rozsah je omezen na metodu `PrintMessage`.</span><span class="sxs-lookup"><span data-stu-id="d5e49-153">In the fifth line, the identifier `this` is used to define a self identifier whose scope is restricted to the method `PrintMessage`.</span></span>

```fsharp
type MyClass2(dataIn) as self =
    let data = dataIn
    do
        self.PrintMessage()
    member this.PrintMessage() =
        printf "Creating MyClass2 with Data %d" data
```

<span data-ttu-id="d5e49-154">Na rozdíl od v jiných jazycích technologie .NET můžete pojmenovat identifikátoru samotného představ; můžete omezit na názvy, jako `self`, `Me`, nebo `this`.</span><span class="sxs-lookup"><span data-stu-id="d5e49-154">Unlike in other .NET languages, you can name the self identifier however you want; you are not restricted to names such as `self`, `Me`, or `this`.</span></span>

<span data-ttu-id="d5e49-155">Vlastní identifikátor, který je deklarován s `as` – klíčové slovo není inicializována až po `let` vazby jsou spouštěny.</span><span class="sxs-lookup"><span data-stu-id="d5e49-155">The self identifier that is declared with the `as` keyword is not initialized until after the `let` bindings are executed.</span></span> <span data-ttu-id="d5e49-156">Proto jej nelze použít v `let` vazby.</span><span class="sxs-lookup"><span data-stu-id="d5e49-156">Therefore, it cannot be used in the `let` bindings.</span></span> <span data-ttu-id="d5e49-157">Můžete použít vlastní identifikátor v `do` část vazby.</span><span class="sxs-lookup"><span data-stu-id="d5e49-157">You can use the self identifier in the `do` bindings section.</span></span>

## <a name="generic-type-parameters"></a><span data-ttu-id="d5e49-158">Parametry obecného typu</span><span class="sxs-lookup"><span data-stu-id="d5e49-158">Generic Type Parameters</span></span>

<span data-ttu-id="d5e49-159">Parametry obecného typu jsou určené v lomených závorkách (`<` a `>`), ve formě jednoduché uvozovky, za nímž následuje identifikátor.</span><span class="sxs-lookup"><span data-stu-id="d5e49-159">Generic type parameters are specified in angle brackets (`<` and `>`), in the form of a single quotation mark followed by an identifier.</span></span> <span data-ttu-id="d5e49-160">Více parametrů obecného typu jsou odděleny čárkami.</span><span class="sxs-lookup"><span data-stu-id="d5e49-160">Multiple generic type parameters are separated by commas.</span></span> <span data-ttu-id="d5e49-161">Parametr obecného typu je v oboru v celém deklarace.</span><span class="sxs-lookup"><span data-stu-id="d5e49-161">The generic type parameter is in scope throughout the declaration.</span></span> <span data-ttu-id="d5e49-162">Následující příklad kódu ukazuje, jak zadat parametry obecného typu.</span><span class="sxs-lookup"><span data-stu-id="d5e49-162">The following code example shows how to specify generic type parameters.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-1/snippet2403.fs)]

<span data-ttu-id="d5e49-163">Argumenty typu jsou odvozeny při použití typu.</span><span class="sxs-lookup"><span data-stu-id="d5e49-163">Type arguments are inferred when the type is used.</span></span> <span data-ttu-id="d5e49-164">V následujícím kódu je odvozený typ posloupnost řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="d5e49-164">In the following code, the inferred type is a sequence of tuples.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-1/snippet24031.fs)]

## <a name="specifying-inheritance"></a><span data-ttu-id="d5e49-165">Určení dědičnosti</span><span class="sxs-lookup"><span data-stu-id="d5e49-165">Specifying Inheritance</span></span>

<span data-ttu-id="d5e49-166">`inherit` Klauzule určuje přímou základní třídu, pokud existuje.</span><span class="sxs-lookup"><span data-stu-id="d5e49-166">The `inherit` clause identifies the direct base class, if there is one.</span></span> <span data-ttu-id="d5e49-167">V jazyce F # je povolen pouze jednu přímou základní třídu.</span><span class="sxs-lookup"><span data-stu-id="d5e49-167">In F#, only one direct base class is allowed.</span></span> <span data-ttu-id="d5e49-168">Rozhraní, která třída implementuje nejsou považovány za základní třídy.</span><span class="sxs-lookup"><span data-stu-id="d5e49-168">Interfaces that a class implements are not considered base classes.</span></span> <span data-ttu-id="d5e49-169">Rozhraní jsou popsány v [rozhraní](Interfaces.md) tématu.</span><span class="sxs-lookup"><span data-stu-id="d5e49-169">Interfaces are discussed in the [Interfaces](Interfaces.md) topic.</span></span>

<span data-ttu-id="d5e49-170">Metody a vlastnosti ze základní třídy lze přistupovat z odvozené třídy pomocí klíčového slova jazyka `base` jako identifikátor, následované tečkou (.) a název členu.</span><span class="sxs-lookup"><span data-stu-id="d5e49-170">You can access the methods and properties of the base class from the derived class by using the language keyword `base` as an identifier, followed by a period (.) and the name of the member.</span></span>

<span data-ttu-id="d5e49-171">Další informace najdete v tématu [dědičnosti](inheritance.md).</span><span class="sxs-lookup"><span data-stu-id="d5e49-171">For more information, see [Inheritance](inheritance.md).</span></span>

## <a name="members-section"></a><span data-ttu-id="d5e49-172">Oddíl členové</span><span class="sxs-lookup"><span data-stu-id="d5e49-172">Members Section</span></span>

<span data-ttu-id="d5e49-173">V této části můžete definovat statické nebo instanci metody, vlastnosti, implementace rozhraní, abstraktní členy, deklarace události a další konstruktory.</span><span class="sxs-lookup"><span data-stu-id="d5e49-173">You can define static or instance methods, properties, interface implementations, abstract members, event declarations, and additional constructors in this section.</span></span> <span data-ttu-id="d5e49-174">Umožní a proveďte vazby nemůže být použit v této části.</span><span class="sxs-lookup"><span data-stu-id="d5e49-174">Let and do bindings cannot appear in this section.</span></span> <span data-ttu-id="d5e49-175">Vzhledem k tomu, že členové mohou být přidány do různých typů F # kromě třídy, jsou popsány v samostatném tématu [členy](members/index.md).</span><span class="sxs-lookup"><span data-stu-id="d5e49-175">Because members can be added to a variety of F# types in addition to classes, they are discussed in a separate topic, [Members](members/index.md).</span></span>

## <a name="mutually-recursive-types"></a><span data-ttu-id="d5e49-176">Vzájemně rekurzivní typy</span><span class="sxs-lookup"><span data-stu-id="d5e49-176">Mutually Recursive Types</span></span>

<span data-ttu-id="d5e49-177">Při definování typů, které odkazovat na sebe navzájem cyklické způsobem je řetězec společně definice typu s použitím `and` – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="d5e49-177">When you define types that reference each other in a circular way, you string together the type definitions by using the `and` keyword.</span></span> <span data-ttu-id="d5e49-178">`and` Nahradí – klíčové slovo `type` – klíčové slovo pro všechny s výjimkou první definici následujícím způsobem.</span><span class="sxs-lookup"><span data-stu-id="d5e49-178">The `and` keyword replaces the `type` keyword on all except the first definition, as follows.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-1/snippet2404.fs)]

<span data-ttu-id="d5e49-179">Výstupem je seznam všech souborů v aktuálním adresáři.</span><span class="sxs-lookup"><span data-stu-id="d5e49-179">The output is a list of all the files in the current directory.</span></span>

## <a name="when-to-use-classes-unions-records-and-structures"></a><span data-ttu-id="d5e49-180">Kdy použít třídy, sjednocení, záznamů a struktury</span><span class="sxs-lookup"><span data-stu-id="d5e49-180">When to Use Classes, Unions, Records, and Structures</span></span>

<span data-ttu-id="d5e49-181">S ohledem na celou řadu typů lze vybírat, musíte mít dostatečné povědomí o co každý typ je určen pro vyberte požadovaný typ pro konkrétní situaci.</span><span class="sxs-lookup"><span data-stu-id="d5e49-181">Given the variety of types to choose from, you need to have a good understanding of what each type is designed for to select the appropriate type for a particular situation.</span></span> <span data-ttu-id="d5e49-182">Třídy jsou určeny k použití v objektově orientované programování kontextech.</span><span class="sxs-lookup"><span data-stu-id="d5e49-182">Classes are designed for use in object-oriented programming contexts.</span></span> <span data-ttu-id="d5e49-183">Objektově orientované programování je dominantní paradigma použít v aplikacích, které jsou určeny pro rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="d5e49-183">Object-oriented programming is the dominant paradigm used in applications that are written for the .NET Framework.</span></span> <span data-ttu-id="d5e49-184">Pokud je váš kód F # těsná spolupráce s rozhraní .NET Framework nebo jiné objektově orientované knihovny a zejména v případě, že budete muset rozšířit z objektově orientované typu systému, jako je například knihovna uživatelského rozhraní, třídy jsou pravděpodobně vhodné.</span><span class="sxs-lookup"><span data-stu-id="d5e49-184">If your F# code has to work closely with the .NET Framework or another object-oriented library, and especially if you have to extend from an object-oriented type system such as a UI library, classes are probably appropriate.</span></span>

<span data-ttu-id="d5e49-185">Pokud nejsou úzce spolupráce s objektově orientované kódu nebo pokud píšete kód, který je samostatná a proto chráněný před častá interakce s kódem objektově orientované, zvažte použití záznamů a rozlišovaná sjednocení.</span><span class="sxs-lookup"><span data-stu-id="d5e49-185">If you are not interoperating closely with object-oriented code, or if you are writing code that is self-contained and therefore protected from frequent interaction with object-oriented code, you should consider using records and discriminated unions.</span></span> <span data-ttu-id="d5e49-186">Jediné, dobře přiměje – out jako jednodušší alternativu k hierarchii objektů lze často použít diskriminované sjednocení, společně s odpovídající vzor odpovídající kód.</span><span class="sxs-lookup"><span data-stu-id="d5e49-186">A single, well thought–out discriminated union, together with appropriate pattern matching code, can often be used as a simpler alternative to an object hierarchy.</span></span> <span data-ttu-id="d5e49-187">Další informace o rozlišovaná sjednocení, naleznete v tématu [Rozlišované sjednocení](discriminated-unions.md).</span><span class="sxs-lookup"><span data-stu-id="d5e49-187">For more information about discriminated unions, see [Discriminated Unions](discriminated-unions.md).</span></span>

<span data-ttu-id="d5e49-188">Výhodou je jednodušší než třídy jsou záznamy, ale záznamy nejsou vhodné, když požadavky typu překročí, co lze provést pomocí jejich jednoduchost.</span><span class="sxs-lookup"><span data-stu-id="d5e49-188">Records have the advantage of being simpler than classes, but records are not appropriate when the demands of a type exceed what can be accomplished with their simplicity.</span></span> <span data-ttu-id="d5e49-189">Záznamy jsou v podstatě jednoduchá agregace hodnot, bez samostatné konstruktory, které můžete provést vlastní akce, bez skrytých polí a bez implementace dědičnosti nebo rozhraní.</span><span class="sxs-lookup"><span data-stu-id="d5e49-189">Records are basically simple aggregates of values, without separate constructors that can perform custom actions, without hidden fields, and without inheritance or interface implementations.</span></span> <span data-ttu-id="d5e49-190">Pro záznamy, aby bylo mnohem složitější jejich chování je možné přidat členy jako jsou vlastnosti a metody, pole, uložená v záznamu jsou stále jednoduché agregace hodnot.</span><span class="sxs-lookup"><span data-stu-id="d5e49-190">Although members such as properties and methods can be added to records to make their behavior more complex, the fields stored in a record are still a simple aggregate of values.</span></span> <span data-ttu-id="d5e49-191">Další informace o záznamech najdete v tématu [záznamy](records.md).</span><span class="sxs-lookup"><span data-stu-id="d5e49-191">For more information about records, see [Records](records.md).</span></span>

<span data-ttu-id="d5e49-192">Struktury jsou také užitečné pro malá agregace dat, ale liší od třídy a záznamy, že jsou typy hodnot .NET.</span><span class="sxs-lookup"><span data-stu-id="d5e49-192">Structures are also useful for small aggregates of data, but they differ from classes and records in that they are .NET value types.</span></span> <span data-ttu-id="d5e49-193">Záznamy a třídy jsou odkazové typy .NET.</span><span class="sxs-lookup"><span data-stu-id="d5e49-193">Classes and records are .NET reference types.</span></span> <span data-ttu-id="d5e49-194">Sémantika typy hodnot a odkazové typy se liší v tom, že typy hodnot jsou předávány hodnotou.</span><span class="sxs-lookup"><span data-stu-id="d5e49-194">The semantics of value types and reference types are different in that value types are passed by value.</span></span> <span data-ttu-id="d5e49-195">To znamená, že se zkopírují bit bitu, když se předá jako parametr nebo vrácená z funkce.</span><span class="sxs-lookup"><span data-stu-id="d5e49-195">This means that they are copied bit for bit when they are passed as a parameter or returned from a function.</span></span> <span data-ttu-id="d5e49-196">Jsou také uloženy do zásobníku nebo, pokud jsou použity jako pole, vložená do nadřazeného objektu namísto uložené v samostatném umístění na haldě.</span><span class="sxs-lookup"><span data-stu-id="d5e49-196">They are also stored on the stack or, if they are used as a field, embedded inside the parent object instead of stored in their own separate location on the heap.</span></span> <span data-ttu-id="d5e49-197">Struktury jsou proto vhodné pro často používaná data při režijní náklady na přístup k haldě je nějaký problém.</span><span class="sxs-lookup"><span data-stu-id="d5e49-197">Therefore, structures are appropriate for frequently accessed data when the overhead of accessing the heap is a problem.</span></span> <span data-ttu-id="d5e49-198">Další informace o strukturách naleznete v tématu [struktury](structures.md).</span><span class="sxs-lookup"><span data-stu-id="d5e49-198">For more information about structures, see [Structures](structures.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="d5e49-199">Viz také:</span><span class="sxs-lookup"><span data-stu-id="d5e49-199">See also</span></span>

- [<span data-ttu-id="d5e49-200">Referenční dokumentace jazyka F#</span><span class="sxs-lookup"><span data-stu-id="d5e49-200">F# Language Reference</span></span>](index.md)
- [<span data-ttu-id="d5e49-201">Členové</span><span class="sxs-lookup"><span data-stu-id="d5e49-201">Members</span></span>](members/index.md)
- [<span data-ttu-id="d5e49-202">Dědičnost</span><span class="sxs-lookup"><span data-stu-id="d5e49-202">Inheritance</span></span>](inheritance.md)
- [<span data-ttu-id="d5e49-203">Rozhraní</span><span class="sxs-lookup"><span data-stu-id="d5e49-203">Interfaces</span></span>](interfaces.md)
