---
title: Chyba kompilátoru FS0008
ms.date: 01/27/2020
f1_keywords:
- FS0008
helpviewer_keywords:
- FS0008
ms.openlocfilehash: d6f9febf24c96c121e07b02d0d8b661aa457d544
ms.sourcegitcommit: cdf5084648bf5e77970cbfeaa23f1cab3e6e234e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 02/01/2020
ms.locfileid: "76929564"
---
# <a name="fs0008-indeterminate-runtime-coercion"></a><span data-ttu-id="27f91-102">FS0008: neurčitá konverze za běhu</span><span class="sxs-lookup"><span data-stu-id="27f91-102">FS0008: Indeterminate runtime coercion</span></span>

<span data-ttu-id="27f91-103">Tato zpráva se zobrazí, když se vzor typu testu používá ke spárování hodnoty, ale přetypování nelze bezpečně prokazatelně zjistit v době kompilace, protože hodnota typu nebyla kompilátorem určena.</span><span class="sxs-lookup"><span data-stu-id="27f91-103">This message is given when a type-test pattern is used to match a value, but the cast cannot be safely proven at compile time because the type value has not been determined by the compiler.</span></span> <span data-ttu-id="27f91-104">K této situaci často dochází, když se shodují s parametrem funkce, jehož typ byl odvozen.</span><span class="sxs-lookup"><span data-stu-id="27f91-104">This often happens when matching on a function parameter whose type has been inferred.</span></span>  <span data-ttu-id="27f91-105">Následující kód ilustruje příklad této:</span><span class="sxs-lookup"><span data-stu-id="27f91-105">The following code illustrates an example of this:</span></span>

[!code-fsharp[FS0008-error](~/samples/snippets/fsharp/compiler-messages/fs0008.fs#L2-L5)]

<span data-ttu-id="27f91-106">Výsledkem tohoto kódu je následující výstup:</span><span class="sxs-lookup"><span data-stu-id="27f91-106">This code results in the following output:</span></span>

```text
This runtime coercion or type test from type
    'a
to
    string
involves an indeterminate type based on information prior to this program point. Runtime type tests are not allowed on some types. Further type annotations are needed.
```

<span data-ttu-id="27f91-107">V tomto kódu parametr funkce `x` neměl pro něj odvozený typ.</span><span class="sxs-lookup"><span data-stu-id="27f91-107">In this code, the function parameter `x` has not had a type inferred for it.</span></span> <span data-ttu-id="27f91-108">To může být z libovolného počtu důvodů, od parametru, který není používán způsobem, který poskytuje algoritmus odvození typu jakékoli informace, do funkce, která není volána nikde jinde v kódu.</span><span class="sxs-lookup"><span data-stu-id="27f91-108">This could be for any number of reasons, ranging from the parameter not being used in ways that give the type-inference algorithm any information, to the function not being called anywhere else in the code.</span></span>  <span data-ttu-id="27f91-109">Vzhledem k tomu, že typ `x` nebyl odvozen, kompilátor nemůže zaručit, že typ `x` může být převeden jako řetězec.</span><span class="sxs-lookup"><span data-stu-id="27f91-109">Because the type of `x` has not been inferred, the compiler cannot guarantee that the type of `x` is able to be cast as a string.</span></span>  <span data-ttu-id="27f91-110">Vzhledem k tomu, že kompilátor nemůže zaručit, že je možné provést toto přetypování za běhu, poskytne tuto zprávu.</span><span class="sxs-lookup"><span data-stu-id="27f91-110">Because the compiler cannot guarantee that this cast is able to be performed at runtime, it gives this message.</span></span>

<span data-ttu-id="27f91-111">Chcete-li opravit tuto zprávu, je nutné poskytnout kompilátor více informací o typu hodnoty, pro kterou používáte vzor typu-test.</span><span class="sxs-lookup"><span data-stu-id="27f91-111">To fix this message you must provide the compiler more information about the type of the value for which you are using the type-test pattern.</span></span>

<span data-ttu-id="27f91-112">Jedním ze způsobů opravy výše uvedeného kódu by byl použití typu na parametr `x` přímo:</span><span class="sxs-lookup"><span data-stu-id="27f91-112">One way of fixing the above code would be to apply a type to the `x` parameter directly:</span></span>

[!code-fsharp[FS0008-x-given-type](~/samples/snippets/fsharp/compiler-messages/fs0008.fs#L8-L11)]

<span data-ttu-id="27f91-113">V tomto kódu má parametr `x` typ `obj`a vzhledem k tomu, že `obj` je možné testovaného typu, aby se zjistilo, jestli je základní reprezentace `string`, funkce se teď zkompiluje.</span><span class="sxs-lookup"><span data-stu-id="27f91-113">In this code, the `x` parameter is given a type of `obj`, and since `obj` is able to be type-tested to see if the underlying representation is `string`, the function now compiles.</span></span>

<span data-ttu-id="27f91-114">Dalším způsobem, jak chybný kód opravit, je použít jiný způsob převodu `x` parametru na `string`:</span><span class="sxs-lookup"><span data-stu-id="27f91-114">Another way of fixing the erroneous code is to use a different way of converting the `x` parameter to a `string`:</span></span>

[!code-fsharp[FS0008-x-given-type](~/samples/snippets/fsharp/compiler-messages/fs0008.fs#L14-L15)]

<span data-ttu-id="27f91-115">V tomto kódu se použití funkce `string` v parametru `x` předali kompilátoru dostatek informací pro odvození typu `x` jako `string`, takže chyba je vyřešena refaktoringem funkce.</span><span class="sxs-lookup"><span data-stu-id="27f91-115">In this code, the use of the `string` function on the `x` parameter has given the compiler enough information to infer the type of `x` as `string`, so the error is resolved by refactoring the function.</span></span>
