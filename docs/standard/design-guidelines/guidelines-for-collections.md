---
title: Pokyny pro kolekce
ms.date: 03/30/2017
ms.technology: dotnet-standard
ms.assetid: 297b8f1d-b11f-4dc6-960a-8e990817304e
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 3571ebb2fdd2bcdfd8be1f0087d096e01f18790a
ms.sourcegitcommit: 6eac9a01ff5d70c6d18460324c016a3612c5e268
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/16/2018
ms.locfileid: "45674088"
---
# <a name="guidelines-for-collections"></a><span data-ttu-id="0848d-102">Pokyny pro kolekce</span><span class="sxs-lookup"><span data-stu-id="0848d-102">Guidelines for Collections</span></span>
<span data-ttu-id="0848d-103">Jakýkoli typ určený konkrétně pro manipulaci s skupinu objektů, které mají některé běžné charakteristiky lze považovat za kolekce.</span><span class="sxs-lookup"><span data-stu-id="0848d-103">Any type designed specifically to manipulate a group of objects having some common characteristic can be considered a collection.</span></span> <span data-ttu-id="0848d-104">Je téměř vždy vhodné pro tyto typy implementace <xref:System.Collections.IEnumerable> nebo <xref:System.Collections.Generic.IEnumerable%601>, takže se v této části považujeme pouze typy implementace jeden nebo oba z těchto rozhraní být kolekce.</span><span class="sxs-lookup"><span data-stu-id="0848d-104">It is almost always appropriate for such types to implement <xref:System.Collections.IEnumerable> or <xref:System.Collections.Generic.IEnumerable%601>, so in this section we only consider types implementing one or both of those interfaces to be collections.</span></span>  
  
 <span data-ttu-id="0848d-105">**X DO NOT** slabě typovaná kolekce použít veřejné rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="0848d-105">**X DO NOT** use weakly typed collections in public APIs.</span></span>  
  
 <span data-ttu-id="0848d-106">Typ položky přesně, nikoli jakákoliv z jeho základních typů (platí jenom pro veřejné členy kolekce) by měl být typu všechny návratové hodnoty a parametry představující kolekci položek.</span><span class="sxs-lookup"><span data-stu-id="0848d-106">The type of all return values and parameters representing collection items should be the exact item type, not any of its base types (this applies only to public members of the collection).</span></span>  
  
 <span data-ttu-id="0848d-107">**X DO NOT** použít <xref:System.Collections.ArrayList> nebo <xref:System.Collections.Generic.List%601> veřejné rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="0848d-107">**X DO NOT** use <xref:System.Collections.ArrayList> or <xref:System.Collections.Generic.List%601> in public APIs.</span></span>  
  
 <span data-ttu-id="0848d-108">Tyto typy jsou datové struktury, která se používá v interní implementace, nejsou ve veřejných rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="0848d-108">These types are data structures designed to be used in internal implementation, not in public APIs.</span></span> <span data-ttu-id="0848d-109">`List<T>` je optimalizované pro výkon a výkon za cenu čistotu rozhraní API a flexibilitu.</span><span class="sxs-lookup"><span data-stu-id="0848d-109">`List<T>` is optimized for performance and power at the cost of cleanness of the APIs and flexibility.</span></span> <span data-ttu-id="0848d-110">Například, pokud vrátíte `List<T>`, nikdy nebudete moct dostávat oznámení, když kód klienta upraví kolekci.</span><span class="sxs-lookup"><span data-stu-id="0848d-110">For example, if you return `List<T>`, you will not ever be able to receive notifications when client code modifies the collection.</span></span> <span data-ttu-id="0848d-111">Navíc `List<T>` poskytuje mnoho členů, jako například <xref:System.Collections.Generic.List%601.BinarySearch%2A>, které nejsou užitečné nebo použít v mnoha scénářích.</span><span class="sxs-lookup"><span data-stu-id="0848d-111">Also, `List<T>` exposes many members, such as <xref:System.Collections.Generic.List%601.BinarySearch%2A>, that are not useful or applicable in many scenarios.</span></span> <span data-ttu-id="0848d-112">Následující dvě části popisují typy (abstrakce) určený speciálně pro použití ve veřejných rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="0848d-112">The following two sections describe types (abstractions) designed specifically for use in public APIs.</span></span>  
  
 <span data-ttu-id="0848d-113">**X DO NOT** použít `Hashtable` nebo `Dictionary<TKey,TValue>` veřejné rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="0848d-113">**X DO NOT** use `Hashtable` or `Dictionary<TKey,TValue>` in public APIs.</span></span>  
  
 <span data-ttu-id="0848d-114">Tyto typy jsou datové struktury, která se používá v interní implementaci.</span><span class="sxs-lookup"><span data-stu-id="0848d-114">These types are data structures designed to be used in internal implementation.</span></span> <span data-ttu-id="0848d-115">Veřejné rozhraní API používejte <xref:System.Collections.IDictionary>, `IDictionary <TKey, TValue>`, nebo vlastní typ implementující jedno nebo obě rozhraní.</span><span class="sxs-lookup"><span data-stu-id="0848d-115">Public APIs should use <xref:System.Collections.IDictionary>, `IDictionary <TKey, TValue>`, or a custom type implementing one or both of the interfaces.</span></span>  
  
 <span data-ttu-id="0848d-116">**X DO NOT** použít <xref:System.Collections.Generic.IEnumerator%601>, <xref:System.Collections.IEnumerator>, nebo jakýkoli jiný typ, který implementuje kterékoli z těchto rozhraní, s výjimkou jako návratový typ `GetEnumerator` metoda.</span><span class="sxs-lookup"><span data-stu-id="0848d-116">**X DO NOT** use <xref:System.Collections.Generic.IEnumerator%601>, <xref:System.Collections.IEnumerator>, or any other type that implements either of these interfaces, except as the return type of a `GetEnumerator` method.</span></span>  
  
 <span data-ttu-id="0848d-117">Typy jiné než vrácení enumerátory z metody `GetEnumerator` nelze použít s `foreach` příkazu.</span><span class="sxs-lookup"><span data-stu-id="0848d-117">Types returning enumerators from methods other than `GetEnumerator` cannot be used with the `foreach` statement.</span></span>  
  
 <span data-ttu-id="0848d-118">**X DO NOT** implementovat obě `IEnumerator<T>` a `IEnumerable<T>` na stejného typu.</span><span class="sxs-lookup"><span data-stu-id="0848d-118">**X DO NOT** implement both `IEnumerator<T>` and `IEnumerable<T>` on the same type.</span></span> <span data-ttu-id="0848d-119">Totéž platí i pro neobecné rozhraní `IEnumerator` a `IEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="0848d-119">The same applies to the nongeneric interfaces `IEnumerator` and `IEnumerable`.</span></span>  
  
## <a name="collection-parameters"></a><span data-ttu-id="0848d-120">Kolekce parametrů</span><span class="sxs-lookup"><span data-stu-id="0848d-120">Collection Parameters</span></span>  
 <span data-ttu-id="0848d-121">**✓ DO** typ specializuje nejmenší možné použít jako typ parametru.</span><span class="sxs-lookup"><span data-stu-id="0848d-121">**✓ DO** use the least-specialized type possible as a parameter type.</span></span> <span data-ttu-id="0848d-122">Většina členy trvá kolekcí, jak používat parametry `IEnumerable<T>` rozhraní.</span><span class="sxs-lookup"><span data-stu-id="0848d-122">Most members taking collections as parameters use the `IEnumerable<T>` interface.</span></span>  
  
 <span data-ttu-id="0848d-123">**X AVOID** pomocí <xref:System.Collections.Generic.ICollection%601> nebo <xref:System.Collections.ICollection> jako parametr pouze pro přístup `Count` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="0848d-123">**X AVOID** using <xref:System.Collections.Generic.ICollection%601> or <xref:System.Collections.ICollection> as a parameter just to access the `Count` property.</span></span>  
  
 <span data-ttu-id="0848d-124">Místo toho zvažte použití `IEnumerable<T>` nebo `IEnumerable` a dynamicky kontroluje, zda daný objekt implementuje `ICollection<T>` nebo `ICollection`.</span><span class="sxs-lookup"><span data-stu-id="0848d-124">Instead, consider using `IEnumerable<T>` or `IEnumerable` and dynamically checking whether the object implements `ICollection<T>` or `ICollection`.</span></span>  
  
## <a name="collection-properties-and-return-values"></a><span data-ttu-id="0848d-125">Kolekce vlastností a návratové hodnoty</span><span class="sxs-lookup"><span data-stu-id="0848d-125">Collection Properties and Return Values</span></span>  
 <span data-ttu-id="0848d-126">**X DO NOT** zadejte vlastnosti nastavit kolekce.</span><span class="sxs-lookup"><span data-stu-id="0848d-126">**X DO NOT** provide settable collection properties.</span></span>  
  
 <span data-ttu-id="0848d-127">Uživatelům můžete nahradit obsah kolekce nejprve vymazáním kolekce a následným přidáním nového obsahu.</span><span class="sxs-lookup"><span data-stu-id="0848d-127">Users can replace the contents of the collection by clearing the collection first and then adding the new contents.</span></span> <span data-ttu-id="0848d-128">Pokud nahrazování celé kolekce je běžný scénář, zvažte poskytnutí `AddRange` metody v kolekci.</span><span class="sxs-lookup"><span data-stu-id="0848d-128">If replacing the whole collection is a common scenario, consider providing the `AddRange` method on the collection.</span></span>  
  
 <span data-ttu-id="0848d-129">**✓ DO** použít `Collection<T>` nebo podtřídou třídy `Collection<T>` pro vlastnosti nebo return hodnoty představující kolekce pro čtení a zápis.</span><span class="sxs-lookup"><span data-stu-id="0848d-129">**✓ DO** use `Collection<T>` or a subclass of `Collection<T>` for properties or return values representing read/write collections.</span></span>  
  
 <span data-ttu-id="0848d-130">Pokud `Collection<T>` nesplňuje některé požadavky (například kolekce nesmí implementovat <xref:System.Collections.IList>), použijte vlastní kolekce implementací `IEnumerable<T>`, `ICollection<T>`, nebo <xref:System.Collections.Generic.IList%601>.</span><span class="sxs-lookup"><span data-stu-id="0848d-130">If `Collection<T>` does not meet some requirement (e.g., the collection must not implement <xref:System.Collections.IList>), use a custom collection by implementing `IEnumerable<T>`, `ICollection<T>`, or <xref:System.Collections.Generic.IList%601>.</span></span>  
  
 <span data-ttu-id="0848d-131">**✓ DO** použít <xref:System.Collections.ObjectModel.ReadOnlyCollection%601>, podtřídou třídy `ReadOnlyCollection<T>`, nebo ve výjimečných případech `IEnumerable<T>` pro vlastnosti nebo return hodnoty představující kolekce jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="0848d-131">**✓ DO** use <xref:System.Collections.ObjectModel.ReadOnlyCollection%601>, a subclass of `ReadOnlyCollection<T>`, or in rare cases `IEnumerable<T>` for properties or return values representing read-only collections.</span></span>  
  
 <span data-ttu-id="0848d-132">Obecně platí, dáváte přednost `ReadOnlyCollection<T>`.</span><span class="sxs-lookup"><span data-stu-id="0848d-132">In general, prefer `ReadOnlyCollection<T>`.</span></span> <span data-ttu-id="0848d-133">Pokud nesplňuje některé požadavky (například kolekce nesmí implementovat `IList`), použijte vlastní kolekce implementací `IEnumerable<T>`, `ICollection<T>`, nebo `IList<T>`.</span><span class="sxs-lookup"><span data-stu-id="0848d-133">If it does not meet some requirement (e.g., the collection must not implement `IList`), use a custom collection by implementing `IEnumerable<T>`, `ICollection<T>`, or `IList<T>`.</span></span> <span data-ttu-id="0848d-134">Pokud se rozhodnete implementovat vlastní kolekce jen pro čtení, implementovat `ICollection<T>.IsReadOnly` vrátit `true`.</span><span class="sxs-lookup"><span data-stu-id="0848d-134">If you do implement a custom read-only collection, implement `ICollection<T>.IsReadOnly` to return `true`.</span></span>  
  
 <span data-ttu-id="0848d-135">V případech, kdy jste si jisti, že je jediným případem se někdy budete chtít podporu dopředné iterace, můžete jednoduše použít `IEnumerable<T>`.</span><span class="sxs-lookup"><span data-stu-id="0848d-135">In cases where you are sure that the only scenario you will ever want to support is forward-only iteration, you can simply use `IEnumerable<T>`.</span></span>  
  
 <span data-ttu-id="0848d-136">**✓ CONSIDER** pomocí podtřídy obecné základní kolekce místo použití kolekce přímo.</span><span class="sxs-lookup"><span data-stu-id="0848d-136">**✓ CONSIDER** using subclasses of generic base collections instead of using the collections directly.</span></span>  
  
 <span data-ttu-id="0848d-137">To umožňuje lepší názvu a pro přidávání členů pomocné rutiny, které nejsou k dispozici u typů kolekce základní.</span><span class="sxs-lookup"><span data-stu-id="0848d-137">This allows for a better name and for adding helper members that are not present on the base collection types.</span></span> <span data-ttu-id="0848d-138">To platí zejména pro rozhraní API vysoké úrovně.</span><span class="sxs-lookup"><span data-stu-id="0848d-138">This is especially applicable to high-level APIs.</span></span>  
  
 <span data-ttu-id="0848d-139">**✓ CONSIDER** vrácení podtřídou třídy `Collection<T>` nebo `ReadOnlyCollection<T>` velmi běžně používaných metod a vlastností.</span><span class="sxs-lookup"><span data-stu-id="0848d-139">**✓ CONSIDER** returning a subclass of `Collection<T>` or `ReadOnlyCollection<T>` from very commonly used methods and properties.</span></span>  
  
 <span data-ttu-id="0848d-140">To znamená, že umožní přidají metody helper nebo změňte implementaci kolekce v budoucnu.</span><span class="sxs-lookup"><span data-stu-id="0848d-140">This will make it possible for you to add helper methods or change the collection implementation in the future.</span></span>  
  
 <span data-ttu-id="0848d-141">**✓ CONSIDER** pomocí kolekci s klíčem, je-li mít jedinečné klíče položky uložené v kolekci (názvy, ID, atd.).</span><span class="sxs-lookup"><span data-stu-id="0848d-141">**✓ CONSIDER** using a keyed collection if the items stored in the collection have unique keys (names, IDs, etc.).</span></span> <span data-ttu-id="0848d-142">Kolekce s klíči jsou kolekce, které můžete indexovat pomocí celé číslo a klíč a jsou obvykle implementovány pomocí dědění z `KeyedCollection<TKey,TItem>`.</span><span class="sxs-lookup"><span data-stu-id="0848d-142">Keyed collections are collections that can be indexed by both an integer and a key and are usually implemented by inheriting from `KeyedCollection<TKey,TItem>`.</span></span>  
  
 <span data-ttu-id="0848d-143">Kolekce s klíči obvykle mají větší pracovníkům paměti by se neměl používat, pokud zatížení paměti převažuje získané výhody s klíči.</span><span class="sxs-lookup"><span data-stu-id="0848d-143">Keyed collections usually have larger memory footprints and should not be used if the memory overhead outweighs the benefits of having the keys.</span></span>  
  
 <span data-ttu-id="0848d-144">**X DO NOT** vrátit hodnotu null. hodnoty z vlastnosti kolekce nebo z metody, které vrací kolekce.</span><span class="sxs-lookup"><span data-stu-id="0848d-144">**X DO NOT** return null values from collection properties or from methods returning collections.</span></span> <span data-ttu-id="0848d-145">Místo toho vrátí prázdnou kolekci nebo prázdné pole.</span><span class="sxs-lookup"><span data-stu-id="0848d-145">Return an empty collection or an empty array instead.</span></span>  
  
 <span data-ttu-id="0848d-146">Obecně platí, že by měla být hodnota null a prázdné kolekce (0 položek) nebo pole chovají stejně.</span><span class="sxs-lookup"><span data-stu-id="0848d-146">The general rule is that null and empty (0 item) collections or arrays should be treated the same.</span></span>  
  
### <a name="snapshots-versus-live-collections"></a><span data-ttu-id="0848d-147">Snímky a živé kolekce</span><span class="sxs-lookup"><span data-stu-id="0848d-147">Snapshots Versus Live Collections</span></span>  
 <span data-ttu-id="0848d-148">Kolekce představující stavu v určitém okamžiku v čase se kterým se říká kolekce snímku.</span><span class="sxs-lookup"><span data-stu-id="0848d-148">Collections representing a state at some point in time are called snapshot collections.</span></span> <span data-ttu-id="0848d-149">Kolekce obsahující řádků vrácená z dotazu databáze by například snímku.</span><span class="sxs-lookup"><span data-stu-id="0848d-149">For example, a collection containing rows returned from a database query would be a snapshot.</span></span> <span data-ttu-id="0848d-150">Kolekce, vždycky představujících aktuální stav, se nazývají živé kolekce.</span><span class="sxs-lookup"><span data-stu-id="0848d-150">Collections that always represent the current state are called live collections.</span></span> <span data-ttu-id="0848d-151">Například kolekce `ComboBox` položky je kolekce za provozu.</span><span class="sxs-lookup"><span data-stu-id="0848d-151">For example, a collection of `ComboBox` items is a live collection.</span></span>  
  
 <span data-ttu-id="0848d-152">**X DO NOT** vracejí snímek kolekce z vlastností.</span><span class="sxs-lookup"><span data-stu-id="0848d-152">**X DO NOT** return snapshot collections from properties.</span></span> <span data-ttu-id="0848d-153">Vlastnosti by měla vrátit živé kolekce.</span><span class="sxs-lookup"><span data-stu-id="0848d-153">Properties should return live collections.</span></span>  
  
 <span data-ttu-id="0848d-154">Gettery vlastností by měl být velmi jednoduché operace.</span><span class="sxs-lookup"><span data-stu-id="0848d-154">Property getters should be very lightweight operations.</span></span> <span data-ttu-id="0848d-155">Vrácení snímku vyžaduje vytvoření kopie vnitřní kolekce v operaci O(n).</span><span class="sxs-lookup"><span data-stu-id="0848d-155">Returning a snapshot requires creating a copy of an internal collection in an O(n) operation.</span></span>  
  
 <span data-ttu-id="0848d-156">**✓ DO** použít kolekci snímku nebo živé `IEnumerable<T>` (nebo jeho dílčí) představují kolekce, které jsou volatile (tj, který můžete změnit bez explicitně úpravy kolekce).</span><span class="sxs-lookup"><span data-stu-id="0848d-156">**✓ DO** use either a snapshot collection or a live `IEnumerable<T>` (or its subtype) to represent collections that are volatile (i.e., that can change without explicitly modifying the collection).</span></span>  
  
 <span data-ttu-id="0848d-157">Všechny kolekce představující sdíleného prostředku (např. soubory v adresáři) jsou obecně volatile.</span><span class="sxs-lookup"><span data-stu-id="0848d-157">In general, all collections representing a shared resource (e.g., files in a directory) are volatile.</span></span> <span data-ttu-id="0848d-158">Tato kolekce jsou velmi obtížné či nemožné implementovat jako živé kolekcí, není-li implementace jednoduše dopředné enumerátor.</span><span class="sxs-lookup"><span data-stu-id="0848d-158">Such collections are very difficult or impossible to implement as live collections unless the implementation is simply a forward-only enumerator.</span></span>  
  
## <a name="choosing-between-arrays-and-collections"></a><span data-ttu-id="0848d-159">Volba mezi pole a kolekce</span><span class="sxs-lookup"><span data-stu-id="0848d-159">Choosing Between Arrays and Collections</span></span>  
 <span data-ttu-id="0848d-160">**✓ DO** upřednostnit kolekce přes pole.</span><span class="sxs-lookup"><span data-stu-id="0848d-160">**✓ DO** prefer collections over arrays.</span></span>  
  
 <span data-ttu-id="0848d-161">Kolekce poskytují větší kontrolu nad obsah, můžete v průběhu času vyvíjejí a jsou lépe použitelná.</span><span class="sxs-lookup"><span data-stu-id="0848d-161">Collections provide more control over contents, can evolve over time, and are more usable.</span></span> <span data-ttu-id="0848d-162">Kromě toho pomocí pole pro scénáře, jen pro čtení se nedoporučují, protože je odrazovat vysokými náklady na klonování pole.</span><span class="sxs-lookup"><span data-stu-id="0848d-162">In addition, using arrays for read-only scenarios is discouraged because the cost of cloning the array is prohibitive.</span></span> <span data-ttu-id="0848d-163">Použitelnost studií vyplývá, že někteří vývojáři klidem více pomocí rozhraní API založená na kolekci.</span><span class="sxs-lookup"><span data-stu-id="0848d-163">Usability studies have shown that some developers feel more comfortable using collection-based APIs.</span></span>  
  
 <span data-ttu-id="0848d-164">Ale pokud vyvíjíte rozhraní API nízké úrovně, může být vhodnější použít pole pro čtení i zápis scénáře.</span><span class="sxs-lookup"><span data-stu-id="0848d-164">However, if you are developing low-level APIs, it might be better to use arrays for read-write scenarios.</span></span> <span data-ttu-id="0848d-165">Pole mají nižší paměťové nároky, což pomáhá snižovat pracovní sady, a přístup k prvkům v poli je rychlejší, protože je optimalizovaný modul runtime.</span><span class="sxs-lookup"><span data-stu-id="0848d-165">Arrays have a smaller memory footprint, which helps reduce the working set, and access to elements in an array is faster because it is optimized by the runtime.</span></span>  
  
 <span data-ttu-id="0848d-166">**✓ CONSIDER** použití polí v nižší úrovně rozhraní API minimalizovat využití paměti a maximalizovat výkon.</span><span class="sxs-lookup"><span data-stu-id="0848d-166">**✓ CONSIDER** using arrays in low-level APIs to minimize memory consumption and maximize performance.</span></span>  
  
 <span data-ttu-id="0848d-167">**✓ DO** pomocí bajtových polí místo kolekcí bajtů.</span><span class="sxs-lookup"><span data-stu-id="0848d-167">**✓ DO** use byte arrays instead of collections of bytes.</span></span>  
  
 <span data-ttu-id="0848d-168">**X DO NOT** použít pole pro vlastnosti, pokud by pak bylo vlastnost vrací nové pole (například kopii interní pole) pokaždé, když je volána metoda getter vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="0848d-168">**X DO NOT** use arrays for properties if the property would have to return a new array (e.g., a copy of an internal array) every time the property getter is called.</span></span>  
  
## <a name="implementing-custom-collections"></a><span data-ttu-id="0848d-169">Implementace vlastních kolekcí</span><span class="sxs-lookup"><span data-stu-id="0848d-169">Implementing Custom Collections</span></span>  
 <span data-ttu-id="0848d-170">**✓ CONSIDER** dědění z `Collection<T>`, `ReadOnlyCollection<T>`, nebo `KeyedCollection<TKey,TItem>` při návrhu nové kolekce.</span><span class="sxs-lookup"><span data-stu-id="0848d-170">**✓ CONSIDER** inheriting from `Collection<T>`, `ReadOnlyCollection<T>`, or `KeyedCollection<TKey,TItem>` when designing new collections.</span></span>  
  
 <span data-ttu-id="0848d-171">**✓ DO** implementovat `IEnumerable<T>` při návrhu nové kolekce.</span><span class="sxs-lookup"><span data-stu-id="0848d-171">**✓ DO** implement `IEnumerable<T>` when designing new collections.</span></span> <span data-ttu-id="0848d-172">Zvažte implementaci `ICollection<T>` nebo dokonce `IList<T>` to bude dávat smysl.</span><span class="sxs-lookup"><span data-stu-id="0848d-172">Consider implementing `ICollection<T>` or even `IList<T>` where it makes sense.</span></span>  
  
 <span data-ttu-id="0848d-173">Při implementaci těchto vlastní kolekce, mají tvar API stanovené `Collection<T>` a `ReadOnlyCollection<T>` co nejpřesněji.</span><span class="sxs-lookup"><span data-stu-id="0848d-173">When implementing such custom collection, follow the API pattern established by `Collection<T>` and `ReadOnlyCollection<T>` as closely as possible.</span></span> <span data-ttu-id="0848d-174">To znamená explicitně implementovat stejné členy, pojmenujte parametry, jako jsou tyto dvě kolekce název je a tak dále.</span><span class="sxs-lookup"><span data-stu-id="0848d-174">That is, implement the same members explicitly, name the parameters like these two collections name them, and so on.</span></span>  
  
 <span data-ttu-id="0848d-175">**✓ CONSIDER** implementace rozhraní neobecné kolekce (`IList` a `ICollection`) Pokud kolekce se často předá rozhraní API trvá tato rozhraní jako vstup.</span><span class="sxs-lookup"><span data-stu-id="0848d-175">**✓ CONSIDER** implementing nongeneric collection interfaces (`IList` and `ICollection`) if the collection will often be passed to APIs taking these interfaces as input.</span></span>  
  
 <span data-ttu-id="0848d-176">**X AVOID** implementace rozhraní pro kolekce na typy s komplexní rozhraní API koncept kolekce, které nejsou.</span><span class="sxs-lookup"><span data-stu-id="0848d-176">**X AVOID** implementing collection interfaces on types with complex APIs unrelated to the concept of a collection.</span></span>  
  
 <span data-ttu-id="0848d-177">**X DO NOT** dědit z neobecného základní kolekcí, jako `CollectionBase`.</span><span class="sxs-lookup"><span data-stu-id="0848d-177">**X DO NOT** inherit from nongeneric base collections such as `CollectionBase`.</span></span> <span data-ttu-id="0848d-178">Použití `Collection<T>`, `ReadOnlyCollection<T>`, a `KeyedCollection<TKey,TItem>` místo.</span><span class="sxs-lookup"><span data-stu-id="0848d-178">Use `Collection<T>`, `ReadOnlyCollection<T>`, and `KeyedCollection<TKey,TItem>` instead.</span></span>  
  
### <a name="naming-custom-collections"></a><span data-ttu-id="0848d-179">Názvy vlastních kolekcí</span><span class="sxs-lookup"><span data-stu-id="0848d-179">Naming Custom Collections</span></span>  
 <span data-ttu-id="0848d-180">Kolekce (typy, které implementují `IEnumerable`) jsou vytvořeny hlavně pro dva důvody: (1) Chcete-li vytvořit novou strukturu dat s operacemi specifický pro strukturu a často odlišných výkonových existujícím datovým strukturám charakteristik (například <xref:System.Collections.Generic.List%601>, <xref:System.Collections.Generic.LinkedList%601>, <xref:System.Collections.Generic.Stack%601>) a (2) Chcete-li vytvořit kolekci specializované pro uchování konkrétní sady položek (například <xref:System.Collections.Specialized.StringCollection>).</span><span class="sxs-lookup"><span data-stu-id="0848d-180">Collections (types that implement `IEnumerable`) are created mainly for two reasons: (1) to create a new data structure with structure-specific operations and often different performance characteristics than existing data structures (e.g.,  <xref:System.Collections.Generic.List%601>, <xref:System.Collections.Generic.LinkedList%601>, <xref:System.Collections.Generic.Stack%601>), and (2) to create a specialized collection for holding a specific set of items (e.g.,  <xref:System.Collections.Specialized.StringCollection>).</span></span> <span data-ttu-id="0848d-181">Datové struktury jsou nejčastěji používají v interní implementaci aplikací a knihoven.</span><span class="sxs-lookup"><span data-stu-id="0848d-181">Data structures are most often used in the internal implementation of applications and libraries.</span></span> <span data-ttu-id="0848d-182">Specializované kolekce se hlavně zveřejněné v rozhraní API (jako typy vlastností a parametrů).</span><span class="sxs-lookup"><span data-stu-id="0848d-182">Specialized collections are mainly to be exposed in APIs (as property and parameter types).</span></span>  
  
 <span data-ttu-id="0848d-183">**✓ DO** používat příponu "Slovník" v názvech abstrakce implementace `IDictionary` nebo `IDictionary<TKey,TValue>`.</span><span class="sxs-lookup"><span data-stu-id="0848d-183">**✓ DO** use the "Dictionary" suffix in names of abstractions implementing `IDictionary` or `IDictionary<TKey,TValue>`.</span></span>  
  
 <span data-ttu-id="0848d-184">**✓ DO** používat příponu "Kolekce" v názvech typů implementace `IEnumerable` (nebo některého z jejich potomků) a představuje seznam položek.</span><span class="sxs-lookup"><span data-stu-id="0848d-184">**✓ DO** use the "Collection" suffix in names of types implementing `IEnumerable` (or any of its descendants) and representing a list of items.</span></span>  
  
 <span data-ttu-id="0848d-185">**✓ DO** použijte název struktura příslušná data pro vlastní datové struktury.</span><span class="sxs-lookup"><span data-stu-id="0848d-185">**✓ DO** use the appropriate data structure name for custom data structures.</span></span>  
  
 <span data-ttu-id="0848d-186">**X AVOID** pomocí libovolné přípony zdání konkrétní implementace, jako je například "LinkedList" nebo "Zatřiďovací tabulky," v názvech abstrakce kolekce.</span><span class="sxs-lookup"><span data-stu-id="0848d-186">**X AVOID** using any suffixes implying particular implementation, such as "LinkedList" or "Hashtable," in names of collection abstractions.</span></span>  
  
 <span data-ttu-id="0848d-187">**✓ CONSIDER** prefixu názvy kolekci s názvem typu položky.</span><span class="sxs-lookup"><span data-stu-id="0848d-187">**✓ CONSIDER** prefixing collection names with the name of the item type.</span></span> <span data-ttu-id="0848d-188">Například kolekce ukládání položek typu `Address` (implementace `IEnumerable<Address>`) by měly být pojmenovány `AddressCollection`.</span><span class="sxs-lookup"><span data-stu-id="0848d-188">For example, a collection storing items of type `Address` (implementing `IEnumerable<Address>`) should be named `AddressCollection`.</span></span> <span data-ttu-id="0848d-189">Pokud typ položky je rozhraní, předponu "I" položky lze vynechat typu.</span><span class="sxs-lookup"><span data-stu-id="0848d-189">If the item type is an interface, the "I" prefix of the item type can be omitted.</span></span> <span data-ttu-id="0848d-190">Díky tomu se kolekce <xref:System.IDisposable> položky lze volat `DisposableCollection`.</span><span class="sxs-lookup"><span data-stu-id="0848d-190">Thus, a collection of <xref:System.IDisposable> items can be called `DisposableCollection`.</span></span>  
  
 <span data-ttu-id="0848d-191">**✓ CONSIDER** pomocí předpony "Jen pro čtení" v názvech kolekcí jen pro čtení, pokud odpovídající kolekci lze zapisovat, mohou být přidány nebo již existuje v rámci.</span><span class="sxs-lookup"><span data-stu-id="0848d-191">**✓ CONSIDER** using the "ReadOnly" prefix in names of read-only collections if a corresponding writeable collection might be added or already exists in the framework.</span></span>  
  
 <span data-ttu-id="0848d-192">Například by měla být volána jen pro čtení kolekce řetězců `ReadOnlyStringCollection`.</span><span class="sxs-lookup"><span data-stu-id="0848d-192">For example, a read-only collection of strings should be called `ReadOnlyStringCollection`.</span></span>  
  
 <span data-ttu-id="0848d-193">*Části © 2005, 2009 Microsoft Corporation. Všechna práva vyhrazena.*</span><span class="sxs-lookup"><span data-stu-id="0848d-193">*Portions © 2005, 2009 Microsoft Corporation. All rights reserved.*</span></span>  
  
 <span data-ttu-id="0848d-194">*Přetištěno podle oprávnění Pearson vzdělávání, Inc. z [pokyny k návrhu architektury: konvence, Idiomy a vzory pro opakovaně použitelného knihovny .NET, 2nd Edition](https://www.informit.com/store/framework-design-guidelines-conventions-idioms-and-9780321545619) Krzysztof Cwalina a Brad Abrams publikované 22 Oct 2008, Designing Effective jako části této série Microsoft Windows Development.*</span><span class="sxs-lookup"><span data-stu-id="0848d-194">*Reprinted by permission of Pearson Education, Inc. from [Framework Design Guidelines: Conventions, Idioms, and Patterns for Reusable .NET Libraries, 2nd Edition](https://www.informit.com/store/framework-design-guidelines-conventions-idioms-and-9780321545619) by Krzysztof Cwalina and Brad Abrams, published Oct 22, 2008 by Addison-Wesley Professional as part of the Microsoft Windows Development Series.*</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="0848d-195">Viz také:</span><span class="sxs-lookup"><span data-stu-id="0848d-195">See also</span></span>

- [<span data-ttu-id="0848d-196">Pokyny k návrhu architektury</span><span class="sxs-lookup"><span data-stu-id="0848d-196">Framework Design Guidelines</span></span>](../../../docs/standard/design-guidelines/index.md)  
- [<span data-ttu-id="0848d-197">Pokyny k používání</span><span class="sxs-lookup"><span data-stu-id="0848d-197">Usage Guidelines</span></span>](../../../docs/standard/design-guidelines/usage-guidelines.md)
