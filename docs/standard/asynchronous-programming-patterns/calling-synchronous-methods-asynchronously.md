---
title: Asynchronní volání synchronních metod
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
- cpp
helpviewer_keywords:
- asynchronous programming, delegates
- asynchronous delegates
- AsyncWaitHandle property
- callback methods
- calling synchronous methods in asynchronous manner
- WaitHandle class, code examples
- asynchronous programming, status polling
- polling asynchronous operation status
- delegates [.NET Framework], asynchronous
- synchronous calling in asynchronous manner
- waiting for asynchronous calls
- status information [.NET Framework], asynchronous operations
ms.assetid: 41972034-92ed-450a-9664-ab93fcc6f1fb
ms.openlocfilehash: 06df584f0120fbd4978e18647854a3ee844a2095
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/15/2020
ms.locfileid: "73105130"
---
# <a name="calling-synchronous-methods-asynchronously"></a><span data-ttu-id="45fa7-102">Asynchronní volání synchronních metod</span><span class="sxs-lookup"><span data-stu-id="45fa7-102">Calling Synchronous Methods Asynchronously</span></span>

<span data-ttu-id="45fa7-103">Rozhraní .NET Framework umožňuje volat libovolnou metodu asynchronně.</span><span class="sxs-lookup"><span data-stu-id="45fa7-103">The .NET Framework enables you to call any method asynchronously.</span></span> <span data-ttu-id="45fa7-104">Chcete-li to provést, definujte delegáta se stejným podpisem jako metoda, kterou chcete volat; společný jazyk runtime automaticky `BeginInvoke` `EndInvoke` definuje a metody pro tohoto delegáta, s příslušnými podpisy.</span><span class="sxs-lookup"><span data-stu-id="45fa7-104">To do this you define a delegate with the same signature as the method you want to call; the common language runtime automatically defines `BeginInvoke` and `EndInvoke` methods for this delegate, with the appropriate signatures.</span></span>

> [!NOTE]
> <span data-ttu-id="45fa7-105">Asynchronní delegátská volání, `BeginInvoke` `EndInvoke` konkrétně a metody, nejsou v rozhraní .NET Compact Framework podporována.</span><span class="sxs-lookup"><span data-stu-id="45fa7-105">Asynchronous delegate calls, specifically the `BeginInvoke` and `EndInvoke` methods, are not supported in the .NET Compact Framework.</span></span>

<span data-ttu-id="45fa7-106">Metoda `BeginInvoke` iniciuje asynchronní volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-106">The `BeginInvoke` method initiates the asynchronous call.</span></span> <span data-ttu-id="45fa7-107">Má stejné parametry jako metoda, kterou chcete spustit asynchronně, plus dva další volitelné parametry.</span><span class="sxs-lookup"><span data-stu-id="45fa7-107">It has the same parameters as the method that you want to execute asynchronously, plus two additional optional parameters.</span></span> <span data-ttu-id="45fa7-108">První parametr je <xref:System.AsyncCallback> delegát, který odkazuje na metodu, která má být volána po dokončení asynchronního volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-108">The first parameter is an <xref:System.AsyncCallback> delegate that references a method to be called when the asynchronous call completes.</span></span> <span data-ttu-id="45fa7-109">Druhý parametr je uživatelem definovaný objekt, který předává informace do metody zpětného volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-109">The second parameter is a user-defined object that passes information into the callback method.</span></span> <span data-ttu-id="45fa7-110">`BeginInvoke`vrátí okamžitě a nečeká na dokončení asynchronního volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-110">`BeginInvoke` returns immediately and does not wait for the asynchronous call to complete.</span></span> <span data-ttu-id="45fa7-111">`BeginInvoke`vrátí <xref:System.IAsyncResult>, který lze použít ke sledování průběhu asynchronního volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-111">`BeginInvoke` returns an <xref:System.IAsyncResult>, which can be used to monitor the progress of the asynchronous call.</span></span>

<span data-ttu-id="45fa7-112">Metoda `EndInvoke` načte výsledky asynchronní volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-112">The `EndInvoke` method retrieves the results of the asynchronous call.</span></span> <span data-ttu-id="45fa7-113">To může být volána kdykoliv po `BeginInvoke`.</span><span class="sxs-lookup"><span data-stu-id="45fa7-113">It can be called any time after `BeginInvoke`.</span></span> <span data-ttu-id="45fa7-114">Pokud asynchronní volání nebylo dokončeno, `EndInvoke` blokuje volající vlákno, dokud nebude dokončeno.</span><span class="sxs-lookup"><span data-stu-id="45fa7-114">If the asynchronous call has not completed, `EndInvoke` blocks the calling thread until it completes.</span></span> <span data-ttu-id="45fa7-115">`EndInvoke` Parametry zahrnují `out` parametry `ref` a`<Out>` `ByRef` ( `ByRef` a v jazyce Visual Basic) metody, kterou chcete <xref:System.IAsyncResult> provést `BeginInvoke`asynchronně, plus vrácené .</span><span class="sxs-lookup"><span data-stu-id="45fa7-115">The parameters of `EndInvoke` include the `out` and `ref` parameters (`<Out>` `ByRef` and `ByRef` in Visual Basic) of the method that you want to execute asynchronously, plus the <xref:System.IAsyncResult> returned by `BeginInvoke`.</span></span>

> [!NOTE]
> <span data-ttu-id="45fa7-116">Funkce IntelliSense v sadě Visual Studio `BeginInvoke` `EndInvoke`zobrazuje parametry aplikace a .</span><span class="sxs-lookup"><span data-stu-id="45fa7-116">The IntelliSense feature in Visual Studio displays the parameters of `BeginInvoke` and `EndInvoke`.</span></span> <span data-ttu-id="45fa7-117">Pokud nepoužíváte Visual Studio nebo podobný nástroj, nebo pokud používáte C# s Visual Studio, naleznete v tématu [asynchronní programovací model (APM)](../../../docs/standard/asynchronous-programming-patterns/asynchronous-programming-model-apm.md) popis parametrů definovaných pro tyto metody.</span><span class="sxs-lookup"><span data-stu-id="45fa7-117">If you're not using Visual Studio or a similar tool, or if you're using C# with Visual Studio, see [Asynchronous Programming Model (APM)](../../../docs/standard/asynchronous-programming-patterns/asynchronous-programming-model-apm.md) for a description of the parameters defined for these methods.</span></span>

<span data-ttu-id="45fa7-118">Příklady kódu v tomto tématu ukazují `BeginInvoke` `EndInvoke` čtyři běžné způsoby použití a provádět asynchronní volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-118">The code examples in this topic demonstrate four common ways to use `BeginInvoke` and `EndInvoke` to make asynchronous calls.</span></span> <span data-ttu-id="45fa7-119">Po `BeginInvoke` zavolání můžete provést následující kroky:</span><span class="sxs-lookup"><span data-stu-id="45fa7-119">After calling `BeginInvoke` you can do the following:</span></span>

- <span data-ttu-id="45fa7-120">Proveďte nějakou práci `EndInvoke` a pak volejte blokovat, dokud hovor neskončí.</span><span class="sxs-lookup"><span data-stu-id="45fa7-120">Do some work and then call `EndInvoke` to block until the call completes.</span></span>

- <span data-ttu-id="45fa7-121">Získat <xref:System.Threading.WaitHandle> pomocí <xref:System.IAsyncResult.AsyncWaitHandle%2A?displayProperty=nameWithType> vlastnosti, <xref:System.Threading.WaitHandle.WaitOne%2A> použijte jeho metodu <xref:System.Threading.WaitHandle> blokovat provádění, dokud `EndInvoke`je signalizována a pak volání .</span><span class="sxs-lookup"><span data-stu-id="45fa7-121">Obtain a <xref:System.Threading.WaitHandle> using the <xref:System.IAsyncResult.AsyncWaitHandle%2A?displayProperty=nameWithType> property, use its <xref:System.Threading.WaitHandle.WaitOne%2A> method to block execution until the <xref:System.Threading.WaitHandle> is signaled, and then call `EndInvoke`.</span></span>

- <span data-ttu-id="45fa7-122">Dotazování <xref:System.IAsyncResult> vrácené podle `BeginInvoke` určit, kdy bylo dokončeno asynchronní volání a potom volání `EndInvoke`.</span><span class="sxs-lookup"><span data-stu-id="45fa7-122">Poll the <xref:System.IAsyncResult> returned by `BeginInvoke` to determine when the asynchronous call has completed, and then call `EndInvoke`.</span></span>

- <span data-ttu-id="45fa7-123">Předejte delegáta pro metodu zpětného volání společnosti `BeginInvoke`.</span><span class="sxs-lookup"><span data-stu-id="45fa7-123">Pass a delegate for a callback method to `BeginInvoke`.</span></span> <span data-ttu-id="45fa7-124">Metoda je spuštěna <xref:System.Threading.ThreadPool> ve vlákně po dokončení asynchronního volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-124">The method is executed on a <xref:System.Threading.ThreadPool> thread when the asynchronous call completes.</span></span> <span data-ttu-id="45fa7-125">Volání metody zpětného volání `EndInvoke`.</span><span class="sxs-lookup"><span data-stu-id="45fa7-125">The callback method calls `EndInvoke`.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="45fa7-126">Bez ohledu na to, `EndInvoke` jakou techniku používáte, vždy volání k dokončení asynchronní volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-126">No matter which technique you use, always call `EndInvoke` to complete your asynchronous call.</span></span>

## <a name="defining-the-test-method-and-asynchronous-delegate"></a><span data-ttu-id="45fa7-127">Definování testovací metody a asynchronního delegáta</span><span class="sxs-lookup"><span data-stu-id="45fa7-127">Defining the Test Method and Asynchronous Delegate</span></span>
 <span data-ttu-id="45fa7-128">Příklady kódu, které následují ukazují různé způsoby volání `TestMethod`stejné metody dlouhotrvající, asynchronně.</span><span class="sxs-lookup"><span data-stu-id="45fa7-128">The code examples that follow demonstrate various ways of calling the same long-running method, `TestMethod`, asynchronously.</span></span> <span data-ttu-id="45fa7-129">Metoda `TestMethod` zobrazí zprávu konzoly, která ukazuje, že byla zahájena zpracování, přepne několik sekund do režimu spánku a poté skončí.</span><span class="sxs-lookup"><span data-stu-id="45fa7-129">The `TestMethod` method displays a console message to show that it has begun processing, sleeps for a few seconds, and then ends.</span></span> <span data-ttu-id="45fa7-130">`TestMethod`má `out` parametr, který ukazuje způsob, jakým jsou tyto `BeginInvoke` `EndInvoke`parametry přidány k podpisům a .</span><span class="sxs-lookup"><span data-stu-id="45fa7-130">`TestMethod` has an `out` parameter to demonstrate the way such parameters are added to the signatures of `BeginInvoke` and `EndInvoke`.</span></span> <span data-ttu-id="45fa7-131">Parametry `ref` můžete zpracovat podobně.</span><span class="sxs-lookup"><span data-stu-id="45fa7-131">You can handle `ref` parameters similarly.</span></span>

 <span data-ttu-id="45fa7-132">Následující příklad kódu ukazuje `TestMethod` definici a `AsyncMethodCaller` delegáta s `TestMethod` názvem, který lze použít k volání asynchronně.</span><span class="sxs-lookup"><span data-stu-id="45fa7-132">The following code example shows the definition of `TestMethod` and the delegate named `AsyncMethodCaller` that can be used to call `TestMethod` asynchronously.</span></span> <span data-ttu-id="45fa7-133">Chcete-li zkompilovat příklady kódu, `TestMethod` musíte `AsyncMethodCaller` zahrnout definice pro a delegáta.</span><span class="sxs-lookup"><span data-stu-id="45fa7-133">To compile the code examples, you must include the definitions for `TestMethod` and the `AsyncMethodCaller` delegate.</span></span>

 [!code-cpp[AsyncDelegateExamples#1](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/TestMethod.cpp#1)]
 [!code-csharp[AsyncDelegateExamples#1](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/TestMethod.cs#1)]
 [!code-vb[AsyncDelegateExamples#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/TestMethod.vb#1)]

## <a name="waiting-for-an-asynchronous-call-with-endinvoke"></a><span data-ttu-id="45fa7-134">Čekání na asynchronní volání s EndInvoke</span><span class="sxs-lookup"><span data-stu-id="45fa7-134">Waiting for an Asynchronous Call with EndInvoke</span></span>
 <span data-ttu-id="45fa7-135">Nejjednodušší způsob, jak spustit metodu asynchronně je spustit provádění metody voláním `BeginInvoke` metody delegáta, provést nějakou práci v hlavním `EndInvoke` vlákně a potom volat metodu delegáta.</span><span class="sxs-lookup"><span data-stu-id="45fa7-135">The simplest way to execute a method asynchronously is to start executing the method by calling the delegate's `BeginInvoke` method, do some work on the main thread, and then call the delegate's `EndInvoke` method.</span></span> <span data-ttu-id="45fa7-136">`EndInvoke`může blokovat volající vlákno, protože se nevrátí, dokud nedokončí asynchronní volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-136">`EndInvoke` might block the calling thread because it does not return until the asynchronous call completes.</span></span> <span data-ttu-id="45fa7-137">To je dobrá technika pro použití se souborem nebo síťovými operacemi.</span><span class="sxs-lookup"><span data-stu-id="45fa7-137">This is a good technique to use with file or network operations.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="45fa7-138">Protože `EndInvoke` může blokovat, nikdy byste měli volat z podprocesů, které služby uživatelské rozhraní.</span><span class="sxs-lookup"><span data-stu-id="45fa7-138">Because `EndInvoke` might block, you should never call it from threads that service the user interface.</span></span>

 [!code-cpp[AsyncDelegateExamples#2](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/EndInvoke.cpp#2)]
 [!code-csharp[AsyncDelegateExamples#2](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/EndInvoke.cs#2)]
 [!code-vb[AsyncDelegateExamples#2](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/EndInvoke.vb#2)]

## <a name="waiting-for-an-asynchronous-call-with-waithandle"></a><span data-ttu-id="45fa7-139">Čekání na asynchronní volání s WaitHandle</span><span class="sxs-lookup"><span data-stu-id="45fa7-139">Waiting for an Asynchronous Call with WaitHandle</span></span>
 <span data-ttu-id="45fa7-140">Můžete získat <xref:System.Threading.WaitHandle> pomocí <xref:System.IAsyncResult.AsyncWaitHandle%2A> vlastnostvrácena <xref:System.IAsyncResult> . `BeginInvoke`</span><span class="sxs-lookup"><span data-stu-id="45fa7-140">You can obtain a <xref:System.Threading.WaitHandle> by using the <xref:System.IAsyncResult.AsyncWaitHandle%2A> property of the <xref:System.IAsyncResult> returned by `BeginInvoke`.</span></span> <span data-ttu-id="45fa7-141">Je <xref:System.Threading.WaitHandle> signalizována po dokončení asynchronního volání a můžete na <xref:System.Threading.WaitHandle.WaitOne%2A> něj počkat voláním metody.</span><span class="sxs-lookup"><span data-stu-id="45fa7-141">The <xref:System.Threading.WaitHandle> is signaled when the asynchronous call completes, and you can wait for it by calling the <xref:System.Threading.WaitHandle.WaitOne%2A> method.</span></span>

 <span data-ttu-id="45fa7-142">Pokud používáte <xref:System.Threading.WaitHandle>, můžete provést další zpracování před nebo po dokončení asynchronního volání, ale před voláním `EndInvoke` k načtení výsledků.</span><span class="sxs-lookup"><span data-stu-id="45fa7-142">If you use a <xref:System.Threading.WaitHandle>, you can perform additional processing before or after the asynchronous call completes, but before calling `EndInvoke` to retrieve the results.</span></span>

> [!NOTE]
> <span data-ttu-id="45fa7-143">Popisovač čekání není automaticky uzavřen `EndInvoke`při volání .</span><span class="sxs-lookup"><span data-stu-id="45fa7-143">The wait handle is not closed automatically when you call `EndInvoke`.</span></span> <span data-ttu-id="45fa7-144">Pokud uvolníte všechny odkazy na popisovač čekání, systémové prostředky jsou uvolněny při uvolnění uvolňování paměti uvolní popisovač čekání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-144">If you release all references to the wait handle, system resources are freed when garbage collection reclaims the wait handle.</span></span> <span data-ttu-id="45fa7-145">Chcete-li uvolnit systémové prostředky, jakmile dokončíte pomocí popisovač čekání, <xref:System.Threading.WaitHandle.Close%2A?displayProperty=nameWithType> vyřazujte jej voláním metody.</span><span class="sxs-lookup"><span data-stu-id="45fa7-145">To free the system resources as soon as you are finished using the wait handle, dispose of it by calling the <xref:System.Threading.WaitHandle.Close%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="45fa7-146">Uvolňování paměti funguje efektivněji při jednorázové objekty jsou explicitně uvolněny.</span><span class="sxs-lookup"><span data-stu-id="45fa7-146">Garbage collection works more efficiently when disposable objects are explicitly disposed.</span></span>

 [!code-cpp[AsyncDelegateExamples#3](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/waithandle.cpp#3)]
 [!code-csharp[AsyncDelegateExamples#3](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/waithandle.cs#3)]
 [!code-vb[AsyncDelegateExamples#3](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/WaitHandle.vb#3)]

## <a name="polling-for-asynchronous-call-completion"></a><span data-ttu-id="45fa7-147">Dotazování pro dokončení asynchronního volání</span><span class="sxs-lookup"><span data-stu-id="45fa7-147">Polling for Asynchronous Call Completion</span></span>
 <span data-ttu-id="45fa7-148">Můžete použít <xref:System.IAsyncResult.IsCompleted%2A> vlastnost <xref:System.IAsyncResult> vrácené `BeginInvoke` podle zjistit, kdy asynchronní volání dokončí.</span><span class="sxs-lookup"><span data-stu-id="45fa7-148">You can use the <xref:System.IAsyncResult.IsCompleted%2A> property of the <xref:System.IAsyncResult> returned by `BeginInvoke` to discover when the asynchronous call completes.</span></span> <span data-ttu-id="45fa7-149">Můžete to provést při provádění asynchronní volání z vlákna, které služby uživatelské rozhraní.</span><span class="sxs-lookup"><span data-stu-id="45fa7-149">You might do this when making the asynchronous call from a thread that services the user interface.</span></span> <span data-ttu-id="45fa7-150">Dotazování pro dokončení umožňuje volající vlákno pokračovat v provádění, zatímco <xref:System.Threading.ThreadPool> asynchronní volání provede ve vlákně.</span><span class="sxs-lookup"><span data-stu-id="45fa7-150">Polling for completion allows the calling thread to continue executing while the asynchronous call executes on a <xref:System.Threading.ThreadPool> thread.</span></span>

 [!code-cpp[AsyncDelegateExamples#4](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/polling.cpp#4)]
 [!code-csharp[AsyncDelegateExamples#4](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/polling.cs#4)]
 [!code-vb[AsyncDelegateExamples#4](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/polling.vb#4)]

## <a name="executing-a-callback-method-when-an-asynchronous-call-completes"></a><span data-ttu-id="45fa7-151">Spuštění metody zpětného volání při dokončení asynchronního volání</span><span class="sxs-lookup"><span data-stu-id="45fa7-151">Executing a Callback Method When an Asynchronous Call Completes</span></span>
 <span data-ttu-id="45fa7-152">Pokud vlákno, které iniciuje asynchronní volání nemusí být podproces, který zpracovává výsledky, můžete spustit metodu zpětného volání po dokončení volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-152">If the thread that initiates the asynchronous call does not need to be the thread that processes the results, you can execute a callback method when the call completes.</span></span> <span data-ttu-id="45fa7-153">Metoda zpětného volání je <xref:System.Threading.ThreadPool> spuštěna ve vlákně.</span><span class="sxs-lookup"><span data-stu-id="45fa7-153">The callback method is executed on a <xref:System.Threading.ThreadPool> thread.</span></span>

 <span data-ttu-id="45fa7-154">Chcete-li použít metodu zpětného volání, musíte předat `BeginInvoke` delegáta, <xref:System.AsyncCallback> který představuje metodu zpětného volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-154">To use a callback method, you must pass `BeginInvoke` an <xref:System.AsyncCallback> delegate that represents the callback method.</span></span> <span data-ttu-id="45fa7-155">Můžete také předat objekt, který obsahuje informace, které mají být použity metodou zpětného volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-155">You can also pass an object that contains information to be used by the callback method.</span></span> <span data-ttu-id="45fa7-156">V metodě zpětného volání <xref:System.IAsyncResult>můžete přetypovat , což je jediný <xref:System.Runtime.Remoting.Messaging.AsyncResult> parametr metody zpětného volání, na objekt.</span><span class="sxs-lookup"><span data-stu-id="45fa7-156">In the callback method, you can cast the <xref:System.IAsyncResult>, which is the only parameter of the callback method, to an <xref:System.Runtime.Remoting.Messaging.AsyncResult> object.</span></span> <span data-ttu-id="45fa7-157">Vlastnost potom můžete <xref:System.Runtime.Remoting.Messaging.AsyncResult.AsyncDelegate%2A?displayProperty=nameWithType> použít k získání delegáta, který byl použit `EndInvoke`k zahájení volání, takže můžete volat .</span><span class="sxs-lookup"><span data-stu-id="45fa7-157">You can then use the <xref:System.Runtime.Remoting.Messaging.AsyncResult.AsyncDelegate%2A?displayProperty=nameWithType> property to get the delegate that was used to initiate the call so that you can call `EndInvoke`.</span></span>

 <span data-ttu-id="45fa7-158">Poznámky k příkladu:</span><span class="sxs-lookup"><span data-stu-id="45fa7-158">Notes on the example:</span></span>

- <span data-ttu-id="45fa7-159">Parametr `threadId` `TestMethod` je `out` parametr ([`<Out>` `ByRef` v jazyce Visual Basic), takže `TestMethod`jeho vstupní hodnota není nikdy použita .</span><span class="sxs-lookup"><span data-stu-id="45fa7-159">The `threadId` parameter of `TestMethod` is an `out` parameter ([`<Out>` `ByRef` in Visual Basic), so its input value is never used by `TestMethod`.</span></span> <span data-ttu-id="45fa7-160">Fiktivní proměnná je předána `BeginInvoke` volání.</span><span class="sxs-lookup"><span data-stu-id="45fa7-160">A dummy variable is passed to the `BeginInvoke` call.</span></span> <span data-ttu-id="45fa7-161">Pokud `threadId` by parametr `ref` emitoval parametr (`ByRef` v jazyce Visual Basic), musela by proměnná `BeginInvoke` `EndInvoke`být pole na úrovni třídy, aby mohla být předána oběma a .</span><span class="sxs-lookup"><span data-stu-id="45fa7-161">If the `threadId` parameter were a `ref` parameter (`ByRef` in Visual Basic), the variable would have to be a class-level field so that it could be passed to both `BeginInvoke` and `EndInvoke`.</span></span>

- <span data-ttu-id="45fa7-162">Informace o stavu, `BeginInvoke` který je předán, je formátovací řetězec, který metoda zpětného volání používá k formátování výstupní zprávy.</span><span class="sxs-lookup"><span data-stu-id="45fa7-162">The state information that is passed to `BeginInvoke` is a format string, which the callback method uses to format an output message.</span></span> <span data-ttu-id="45fa7-163">Vzhledem k tomu, že je předán jako typ <xref:System.Object>, informace o stavu musí být přetypován na jeho správný typ před jeho použití.</span><span class="sxs-lookup"><span data-stu-id="45fa7-163">Because it is passed as type <xref:System.Object>, the state information must be cast to its proper type before it can be used.</span></span>

- <span data-ttu-id="45fa7-164">Zpětné volání se provádí <xref:System.Threading.ThreadPool> ve vlákně.</span><span class="sxs-lookup"><span data-stu-id="45fa7-164">The callback is made on a <xref:System.Threading.ThreadPool> thread.</span></span> <span data-ttu-id="45fa7-165"><xref:System.Threading.ThreadPool>vlákna jsou vlákna na pozadí, které neudržují aplikaci spuštěnou, pokud hlavní vlákno končí, takže hlavní vlákno příkladu musí spát dostatečně dlouho, aby bylo zpětné volání dokončeno.</span><span class="sxs-lookup"><span data-stu-id="45fa7-165"><xref:System.Threading.ThreadPool> threads are background threads, which do not keep the application running if the main thread ends, so the main thread of the example has to sleep long enough for the callback to finish.</span></span>

 [!code-cpp[AsyncDelegateExamples#5](../../../samples/snippets/cpp/VS_Snippets_CLR/AsyncDelegateExamples/cpp/callback.cpp#5)]
 [!code-csharp[AsyncDelegateExamples#5](../../../samples/snippets/csharp/VS_Snippets_CLR/AsyncDelegateExamples/CS/callback.cs#5)]
 [!code-vb[AsyncDelegateExamples#5](../../../samples/snippets/visualbasic/VS_Snippets_CLR/AsyncDelegateExamples/VB/callback.vb#5)]

## <a name="see-also"></a><span data-ttu-id="45fa7-166">Viz také</span><span class="sxs-lookup"><span data-stu-id="45fa7-166">See also</span></span>

- <xref:System.Delegate>
- [<span data-ttu-id="45fa7-167">Asynchronní vzor založený na událostech (EAP)</span><span class="sxs-lookup"><span data-stu-id="45fa7-167">Event-based Asynchronous Pattern (EAP)</span></span>](../../../docs/standard/asynchronous-programming-patterns/event-based-asynchronous-pattern-eap.md)
