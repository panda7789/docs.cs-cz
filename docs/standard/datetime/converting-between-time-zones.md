---
title: Převádění časů mezi časovými pásmy
ms.date: 04/10/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- times [.NET Framework], converting
- time zones [.NET Framework], conversions
- UTC times, converting
- converting times
- local time conversions
ms.assetid: a51e1a3b-c983-4320-b31a-1f9fa3cf824a
ms.openlocfilehash: e17b32131e6abc1dba8126799281206f02d46d35
ms.sourcegitcommit: 33deec3e814238fb18a49b2a7e89278e27888291
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/02/2020
ms.locfileid: "84278178"
---
# <a name="converting-times-between-time-zones"></a><span data-ttu-id="c6880-102">Převádění časů mezi časovými pásmy</span><span class="sxs-lookup"><span data-stu-id="c6880-102">Converting times between time zones</span></span>

<span data-ttu-id="c6880-103">U každé aplikace, která pracuje s daty a časy, je stále důležitější pro zpracování rozdílů mezi časovými pásmy.</span><span class="sxs-lookup"><span data-stu-id="c6880-103">It is becoming increasingly important for any application that works with dates and times to handle differences between time zones.</span></span> <span data-ttu-id="c6880-104">Aplikace již nemůže předpokládat, že všechny časy mohou být vyjádřeny v místním čase, což je čas dostupný ze <xref:System.DateTime> struktury.</span><span class="sxs-lookup"><span data-stu-id="c6880-104">An application can no longer assume that all times can be expressed in the local time, which is the time available from the <xref:System.DateTime> structure.</span></span> <span data-ttu-id="c6880-105">Například webová stránka, která zobrazuje aktuální čas ve východní části USA, nebude mít u zákazníka ve východní Asie hodnotu věrohodnosti.</span><span class="sxs-lookup"><span data-stu-id="c6880-105">For example, a Web page that displays the current time in the eastern part of the United States will lack credibility to a customer in eastern Asia.</span></span> <span data-ttu-id="c6880-106">Toto téma vysvětluje, jak převést časy z jednoho časového pásma na jiný a jak převést <xref:System.DateTimeOffset> hodnoty, které mají omezené povědomí o časovém pásmu.</span><span class="sxs-lookup"><span data-stu-id="c6880-106">This topic explains how to convert times from one time zone to another, as well as how to convert <xref:System.DateTimeOffset> values that have limited time zone awareness.</span></span>

## <a name="converting-to-coordinated-universal-time"></a><span data-ttu-id="c6880-107">Převod na koordinovaný světový čas</span><span class="sxs-lookup"><span data-stu-id="c6880-107">Converting to Coordinated Universal Time</span></span>

<span data-ttu-id="c6880-108">Koordinovaný světový čas (UTC) je Standard s vysokou přesností.</span><span class="sxs-lookup"><span data-stu-id="c6880-108">Coordinated Universal Time (UTC) is a high-precision, atomic time standard.</span></span> <span data-ttu-id="c6880-109">Světová časová pásma jsou vyjádřena jako kladná nebo záporná posun od času UTC.</span><span class="sxs-lookup"><span data-stu-id="c6880-109">The world’s time zones are expressed as positive or negative offsets from UTC.</span></span> <span data-ttu-id="c6880-110">UTC tedy poskytuje typ časového pásma volná a neutrální čas v časovém pásmu.</span><span class="sxs-lookup"><span data-stu-id="c6880-110">Thus, UTC provides a kind of time-zone free or time-zone neutral time.</span></span> <span data-ttu-id="c6880-111">Použití času UTC se doporučuje v případě, kdy je důležité přenositelnost data a času napříč počítači.</span><span class="sxs-lookup"><span data-stu-id="c6880-111">The use of UTC time is recommended when a date and time's portability across computers is important.</span></span> <span data-ttu-id="c6880-112">(Podrobnosti a další osvědčené postupy, které používají data a časy, najdete v tématu [osvědčené postupy pro kódování pomocí DateTime v .NET Framework](https://docs.microsoft.com/previous-versions/dotnet/articles/ms973825(v=msdn.10)).) Převod jednotlivých časových pásem na čas UTC usnadňuje porovnávání času.</span><span class="sxs-lookup"><span data-stu-id="c6880-112">(For details and other best practices using dates and times, see [Coding best practices using DateTime in the .NET Framework](https://docs.microsoft.com/previous-versions/dotnet/articles/ms973825(v=msdn.10)).) Converting individual time zones to UTC makes time comparisons easy.</span></span>

> [!NOTE]
> <span data-ttu-id="c6880-113">Můžete také serializovat <xref:System.DateTimeOffset> strukturu tak, aby jednoznačně představovala jediný bod v čase.</span><span class="sxs-lookup"><span data-stu-id="c6880-113">You can also serialize a <xref:System.DateTimeOffset> structure to unambiguously represent a single point in time.</span></span> <span data-ttu-id="c6880-114">Vzhledem <xref:System.DateTimeOffset> k tomu, že objekty ukládají hodnotu data a času spolu s jejich posunem od času UTC, vždy reprezentují konkrétní bod v čase ve vztahu k času UTC.</span><span class="sxs-lookup"><span data-stu-id="c6880-114">Because <xref:System.DateTimeOffset> objects store a date and time value along with its offset from UTC, they always represent a particular point in time in relationship to UTC.</span></span>

<span data-ttu-id="c6880-115">Nejjednodušší způsob, jak převést čas na čas UTC, je zavolat `static` metodu ( `Shared` v Visual Basic) <xref:System.TimeZoneInfo.ConvertTimeToUtc%28System.DateTime%29?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="c6880-115">The easiest way to convert a time to UTC is to call the `static` (`Shared` in Visual Basic) <xref:System.TimeZoneInfo.ConvertTimeToUtc%28System.DateTime%29?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="c6880-116">Přesný převod prováděný metodou závisí na hodnotě `dateTime` <xref:System.DateTime.Kind%2A> Vlastnosti parametru, jak je uvedeno v následující tabulce.</span><span class="sxs-lookup"><span data-stu-id="c6880-116">The exact conversion performed by the method depends on the value of the `dateTime` parameter's <xref:System.DateTime.Kind%2A> property, as the following table shows.</span></span>

| `DateTime.Kind`            | <span data-ttu-id="c6880-117">Převod</span><span class="sxs-lookup"><span data-stu-id="c6880-117">Conversion</span></span>                                                                     |
| -------------------------- | ------------------------------------------------------------------------------ |
| `DateTimeKind.Local`       | <span data-ttu-id="c6880-118">Převede místní čas na čas UTC.</span><span class="sxs-lookup"><span data-stu-id="c6880-118">Converts local time to UTC.</span></span>                                                    |
| `DateTimeKind.Unspecified` | <span data-ttu-id="c6880-119">Předpokládá, že `dateTime` parametr je místní čas a převede místní čas na UTC.</span><span class="sxs-lookup"><span data-stu-id="c6880-119">Assumes the `dateTime` parameter is local time and converts local time to UTC.</span></span> |
| `DateTimeKind.Utc`         | <span data-ttu-id="c6880-120">Vrátí `dateTime` parametr beze změny.</span><span class="sxs-lookup"><span data-stu-id="c6880-120">Returns the `dateTime` parameter unchanged.</span></span>                                    |

<span data-ttu-id="c6880-121">Následující kód převede aktuální místní čas na čas UTC a zobrazí výsledek pro konzolu.</span><span class="sxs-lookup"><span data-stu-id="c6880-121">The following code converts the current local time to UTC and displays the result to the console.</span></span>

[!code-csharp[System.TimeZone2.Concepts#6](../../../samples/snippets/csharp/VS_Snippets_CLR_System/system.TimeZone2.Concepts/CS/TimeZone2Concepts.cs#6)]
[!code-vb[System.TimeZone2.Concepts#6](../../../samples/snippets/visualbasic/VS_Snippets_CLR_System/system.TimeZone2.Concepts/VB/TimeZone2Concepts.vb#6)]

<span data-ttu-id="c6880-122">Pokud hodnota data a času nepředstavuje buď místní čas, nebo UTC, <xref:System.DateTime.ToUniversalTime%2A> Metoda bude nejspíš vracet chybný výsledek.</span><span class="sxs-lookup"><span data-stu-id="c6880-122">If the date and time value does not represent either the local time or UTC, the <xref:System.DateTime.ToUniversalTime%2A> method will likely return an erroneous result.</span></span> <span data-ttu-id="c6880-123">Můžete však použít <xref:System.TimeZoneInfo.ConvertTimeToUtc%2A?displayProperty=nameWithType> metodu k převedení data a času ze zadaného časového pásma.</span><span class="sxs-lookup"><span data-stu-id="c6880-123">However, you can use the <xref:System.TimeZoneInfo.ConvertTimeToUtc%2A?displayProperty=nameWithType> method to convert the date and time from a specified time zone.</span></span> <span data-ttu-id="c6880-124">(Podrobnosti o načtení <xref:System.TimeZoneInfo> objektu, který představuje cílové časové pásmo, najdete v tématu [vyhledání časových pásem definovaných v místním systému](finding-the-time-zones-on-local-system.md).) Následující kód používá <xref:System.TimeZoneInfo.ConvertTimeToUtc%2A?displayProperty=nameWithType> metodu k převodu východního standardního času na čas UTC.</span><span class="sxs-lookup"><span data-stu-id="c6880-124">(For details on retrieving a <xref:System.TimeZoneInfo> object that represents the destination time zone, see [Finding the time zones defined on a local system](finding-the-time-zones-on-local-system.md).) The following code uses the <xref:System.TimeZoneInfo.ConvertTimeToUtc%2A?displayProperty=nameWithType> method to convert Eastern Standard Time to UTC.</span></span>

[!code-csharp[System.TimeZone2.Concepts#7](../../../samples/snippets/csharp/VS_Snippets_CLR_System/system.TimeZone2.Concepts/CS/TimeZone2Concepts.cs#7)]
[!code-vb[System.TimeZone2.Concepts#7](../../../samples/snippets/visualbasic/VS_Snippets_CLR_System/system.TimeZone2.Concepts/VB/TimeZone2Concepts.vb#7)]

<span data-ttu-id="c6880-125">Všimněte si, že tato metoda vyvolá výjimku, <xref:System.ArgumentException> Pokud se <xref:System.DateTime> <xref:System.DateTime.Kind%2A> neshoduje vlastnost objektu a časové pásmo.</span><span class="sxs-lookup"><span data-stu-id="c6880-125">Note that this method throws an <xref:System.ArgumentException> if the <xref:System.DateTime> object's <xref:System.DateTime.Kind%2A> property and the time zone are mismatched.</span></span> <span data-ttu-id="c6880-126">K neshodě dojde, pokud <xref:System.DateTime.Kind%2A> je vlastnost, <xref:System.DateTimeKind.Local?displayProperty=nameWithType> ale <xref:System.TimeZoneInfo> objekt nepředstavuje místní časové pásmo, nebo pokud <xref:System.DateTime.Kind%2A> je vlastnost, <xref:System.DateTimeKind.Utc?displayProperty=nameWithType> ale objekt se <xref:System.TimeZoneInfo> nerovná <xref:System.TimeZoneInfo.Utc?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="c6880-126">A mismatch occurs if the <xref:System.DateTime.Kind%2A> property is <xref:System.DateTimeKind.Local?displayProperty=nameWithType> but the <xref:System.TimeZoneInfo> object does not represent the local time zone, or if the <xref:System.DateTime.Kind%2A> property is <xref:System.DateTimeKind.Utc?displayProperty=nameWithType> but the <xref:System.TimeZoneInfo> object does not equal <xref:System.TimeZoneInfo.Utc?displayProperty=nameWithType>.</span></span>

<span data-ttu-id="c6880-127">Všechny tyto metody přijímají <xref:System.DateTime> hodnoty jako parametry a vracejí <xref:System.DateTime> hodnotu.</span><span class="sxs-lookup"><span data-stu-id="c6880-127">All of these methods take <xref:System.DateTime> values as parameters and return a <xref:System.DateTime> value.</span></span> <span data-ttu-id="c6880-128">Pro <xref:System.DateTimeOffset> hodnoty <xref:System.DateTimeOffset> má struktura <xref:System.DateTimeOffset.ToUniversalTime%2A> metodu instance, která převede datum a čas aktuální instance na čas UTC.</span><span class="sxs-lookup"><span data-stu-id="c6880-128">For <xref:System.DateTimeOffset> values, the <xref:System.DateTimeOffset> structure has a <xref:System.DateTimeOffset.ToUniversalTime%2A> instance method that converts the date and time of the current instance to UTC.</span></span> <span data-ttu-id="c6880-129">Následující příklad volá <xref:System.DateTimeOffset.ToUniversalTime%2A> metodu pro převod místního času a několik dalších časů na koordinovaný světový čas (UTC).</span><span class="sxs-lookup"><span data-stu-id="c6880-129">The following example calls the <xref:System.DateTimeOffset.ToUniversalTime%2A> method to convert a local time and several other times to Coordinated Universal Time (UTC).</span></span>

[!code-csharp[System.DateTimeOffset.Methods#16](../../../samples/snippets/csharp/VS_Snippets_CLR_System/system.DateTimeOffset.Methods/cs/Methods.cs#16)]
[!code-vb[System.DateTimeOffset.Methods#16](../../../samples/snippets/visualbasic/VS_Snippets_CLR_System/system.DateTimeOffset.Methods/vb/Methods.vb#16)]

## <a name="converting-utc-to-a-designated-time-zone"></a><span data-ttu-id="c6880-130">Převod UTC na určené časové pásmo</span><span class="sxs-lookup"><span data-stu-id="c6880-130">Converting UTC to a designated time zone</span></span>

<span data-ttu-id="c6880-131">Chcete-li převést čas UTC na místní čas, přečtěte si následující oddíl "převod UTC na místní čas".</span><span class="sxs-lookup"><span data-stu-id="c6880-131">To convert UTC to local time, see the "Converting UTC to Local Time" section that follows.</span></span> <span data-ttu-id="c6880-132">Chcete-li převést čas UTC na čas v jakémkoli časovém pásmu, které určíte, zavolejte <xref:System.TimeZoneInfo.ConvertTimeFromUtc%2A> metodu.</span><span class="sxs-lookup"><span data-stu-id="c6880-132">To convert UTC to the time in any time zone that you designate, call the <xref:System.TimeZoneInfo.ConvertTimeFromUtc%2A> method.</span></span> <span data-ttu-id="c6880-133">Metoda používá dva parametry:</span><span class="sxs-lookup"><span data-stu-id="c6880-133">The method takes two parameters:</span></span>

- <span data-ttu-id="c6880-134">ČAS UTC, který se má převést.</span><span class="sxs-lookup"><span data-stu-id="c6880-134">The UTC to convert.</span></span> <span data-ttu-id="c6880-135">Musí to být <xref:System.DateTime> hodnota, jejíž <xref:System.DateTime.Kind%2A> vlastnost je nastavena na `Unspecified` nebo `Utc` .</span><span class="sxs-lookup"><span data-stu-id="c6880-135">This must be a <xref:System.DateTime> value whose <xref:System.DateTime.Kind%2A> property is set to `Unspecified` or `Utc`.</span></span>

- <span data-ttu-id="c6880-136">Časové pásmo k převedení UTC na.</span><span class="sxs-lookup"><span data-stu-id="c6880-136">The time zone to convert the UTC to.</span></span>

<span data-ttu-id="c6880-137">Následující kód převede čas UTC na střední (běžný čas).</span><span class="sxs-lookup"><span data-stu-id="c6880-137">The following code converts UTC to Central Standard Time.</span></span>

[!code-csharp[System.TimeZone2.Concepts#8](../../../samples/snippets/csharp/VS_Snippets_CLR_System/system.TimeZone2.Concepts/CS/TimeZone2Concepts.cs#8)]
[!code-vb[System.TimeZone2.Concepts#8](../../../samples/snippets/visualbasic/VS_Snippets_CLR_System/system.TimeZone2.Concepts/VB/TimeZone2Concepts.vb#8)]

## <a name="converting-utc-to-local-time"></a><span data-ttu-id="c6880-138">Převod UTC na místní čas</span><span class="sxs-lookup"><span data-stu-id="c6880-138">Converting UTC to local time</span></span>

<span data-ttu-id="c6880-139">Chcete-li převést čas UTC na místní čas, zavolejte <xref:System.DateTime.ToLocalTime%2A> metodu <xref:System.DateTime> objektu, jehož čas chcete převést.</span><span class="sxs-lookup"><span data-stu-id="c6880-139">To convert UTC to local time, call the <xref:System.DateTime.ToLocalTime%2A> method of the <xref:System.DateTime> object whose time you want to convert.</span></span> <span data-ttu-id="c6880-140">Přesné chování metody závisí na hodnotě <xref:System.DateTime.Kind%2A> vlastnosti objektu, jak je uvedeno v následující tabulce.</span><span class="sxs-lookup"><span data-stu-id="c6880-140">The exact behavior of the method depends on the value of the object’s <xref:System.DateTime.Kind%2A> property, as the following table shows.</span></span>

| `DateTime.Kind`            | <span data-ttu-id="c6880-141">Převod</span><span class="sxs-lookup"><span data-stu-id="c6880-141">Conversion</span></span>                                                                               |
| -------------------------- | ---------------------------------------------------------------------------------------- |
| `DateTimeKind.Local`       | <span data-ttu-id="c6880-142">Vrátí <xref:System.DateTime> hodnotu beze změny.</span><span class="sxs-lookup"><span data-stu-id="c6880-142">Returns the <xref:System.DateTime> value unchanged.</span></span>                                      |
| `DateTimeKind.Unspecified` | <span data-ttu-id="c6880-143">Předpokládá, že <xref:System.DateTime> hodnota je UTC a převede čas UTC na místní čas.</span><span class="sxs-lookup"><span data-stu-id="c6880-143">Assumes that the <xref:System.DateTime> value is UTC and converts the UTC to local time.</span></span> |
| `DateTimeKind.Utc`         | <span data-ttu-id="c6880-144">Převede <xref:System.DateTime> hodnotu na místní čas.</span><span class="sxs-lookup"><span data-stu-id="c6880-144">Converts the <xref:System.DateTime> value to local time.</span></span>                                 |

> [!NOTE]
> <span data-ttu-id="c6880-145"><xref:System.TimeZone.ToLocalTime%2A?displayProperty=nameWithType>Metoda se chová stejně jako `DateTime.ToLocalTime` metoda.</span><span class="sxs-lookup"><span data-stu-id="c6880-145">The <xref:System.TimeZone.ToLocalTime%2A?displayProperty=nameWithType> method behaves identically to the `DateTime.ToLocalTime` method.</span></span> <span data-ttu-id="c6880-146">Přijímá jeden parametr, což je hodnota data a času, která se má převést.</span><span class="sxs-lookup"><span data-stu-id="c6880-146">It takes a single parameter, which is the date and time value to convert.</span></span>

<span data-ttu-id="c6880-147">Můžete také převést čas v jakémkoli určeném časovém pásmu na místní čas pomocí `static` metody ( `Shared` v Visual Basic) <xref:System.TimeZoneInfo.ConvertTime%2A?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="c6880-147">You can also convert the time in any designated time zone to local time by using the `static` (`Shared` in Visual Basic) <xref:System.TimeZoneInfo.ConvertTime%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="c6880-148">Tato technika je popsána v následující části.</span><span class="sxs-lookup"><span data-stu-id="c6880-148">This technique is discussed in the next section.</span></span>

## <a name="converting-between-any-two-time-zones"></a><span data-ttu-id="c6880-149">Převod mezi dvěma časovými pásmy</span><span class="sxs-lookup"><span data-stu-id="c6880-149">Converting between any two time zones</span></span>

<span data-ttu-id="c6880-150">Můžete převádět mezi dvěma časovými pásmy pomocí kterékoli z následujících dvou `static` metod ( `Shared` v Visual Basic) <xref:System.TimeZoneInfo> třídy:</span><span class="sxs-lookup"><span data-stu-id="c6880-150">You can convert between any two time zones by using either of the following two `static` (`Shared` in Visual Basic) methods of the <xref:System.TimeZoneInfo> class:</span></span>

- <xref:System.TimeZoneInfo.ConvertTime%2A>

  <span data-ttu-id="c6880-151">Parametry této metody jsou hodnota data a času, která se má převést, `TimeZoneInfo` objekt, který představuje časové pásmo hodnoty data a času, a `TimeZoneInfo` objekt, který představuje časové pásmo pro převod hodnoty data a času na.</span><span class="sxs-lookup"><span data-stu-id="c6880-151">This method's parameters are the date and time value to convert, a `TimeZoneInfo` object that represents the time zone of the date and time value, and a `TimeZoneInfo` object that represents the time zone to convert the date and time value to.</span></span>

- <xref:System.TimeZoneInfo.ConvertTimeBySystemTimeZoneId%2A>

  <span data-ttu-id="c6880-152">Parametry této metody jsou hodnota data a času pro převod, identifikátor časového pásma hodnoty data a času a identifikátor časového pásma, na které má být převedena hodnota data a času.</span><span class="sxs-lookup"><span data-stu-id="c6880-152">This method's parameters are the date and time value to convert, the identifier of the date and time value's time zone, and the identifier of the time zone to convert the date and time value to.</span></span>

<span data-ttu-id="c6880-153">Obě metody vyžadují, aby <xref:System.DateTime.Kind%2A> vlastnost hodnoty data a času pro převod a <xref:System.TimeZoneInfo> objekt nebo identifikátor časového pásma, který představuje své časové pásmo, odpovídala sobě.</span><span class="sxs-lookup"><span data-stu-id="c6880-153">Both methods require that the <xref:System.DateTime.Kind%2A> property of the date and time value to convert and the <xref:System.TimeZoneInfo> object or time zone identifier that represents its time zone correspond to one another.</span></span> <span data-ttu-id="c6880-154">V opačném případě <xref:System.ArgumentException> je vyvolána výjimka.</span><span class="sxs-lookup"><span data-stu-id="c6880-154">Otherwise, an <xref:System.ArgumentException> is thrown.</span></span> <span data-ttu-id="c6880-155">Například pokud `Kind` vlastnost hodnoty data a času je `DateTimeKind.Local` , je vyvolána výjimka, pokud je `TimeZoneInfo` objekt předán jako parametr metody, nerovná se `TimeZoneInfo.Local` .</span><span class="sxs-lookup"><span data-stu-id="c6880-155">For example, if the `Kind` property of the date and time value is `DateTimeKind.Local`, an exception is thrown if the `TimeZoneInfo` object passed as a parameter to the method is not equal to `TimeZoneInfo.Local`.</span></span> <span data-ttu-id="c6880-156">Výjimka je také vyvolána, pokud se identifikátor předaný jako parametr metodě nerovná `TimeZoneInfo.Local.Id` .</span><span class="sxs-lookup"><span data-stu-id="c6880-156">An exception is also thrown if the identifier passed as a parameter to the method is not equal to `TimeZoneInfo.Local.Id`.</span></span>

<span data-ttu-id="c6880-157">Následující příklad používá <xref:System.TimeZoneInfo.ConvertTime%2A> metodu pro převod z havajština standardního času na místní čas.</span><span class="sxs-lookup"><span data-stu-id="c6880-157">The following example uses the <xref:System.TimeZoneInfo.ConvertTime%2A> method to convert from Hawaiian Standard Time to local time.</span></span>

[!code-csharp[System.TimeZone2.Concepts#9](../../../samples/snippets/csharp/VS_Snippets_CLR_System/system.TimeZone2.Concepts/CS/TimeZone2Concepts.cs#9)]
[!code-vb[System.TimeZone2.Concepts#9](../../../samples/snippets/visualbasic/VS_Snippets_CLR_System/system.TimeZone2.Concepts/VB/TimeZone2Concepts.vb#9)]

## <a name="converting-datetimeoffset-values"></a><span data-ttu-id="c6880-158">Převádění hodnot DateTimeOffset</span><span class="sxs-lookup"><span data-stu-id="c6880-158">Converting DateTimeOffset values</span></span>

<span data-ttu-id="c6880-159">Hodnoty data a času reprezentované <xref:System.DateTimeOffset> objekty nejsou plně v časovém pásmu, protože objekt je v době, kdy je vytvořen, odstraněný v časovém pásmu.</span><span class="sxs-lookup"><span data-stu-id="c6880-159">Date and time values represented by <xref:System.DateTimeOffset> objects are not fully time-zone aware because the object is disassociated from its time zone at the time it is instantiated.</span></span> <span data-ttu-id="c6880-160">V mnoha případech však aplikace jednoduše potřebuje převést datum a čas na základě dvou různých posunů od času UTC, nikoli podle času v konkrétních časových pásmech.</span><span class="sxs-lookup"><span data-stu-id="c6880-160">However, in many cases an application simply needs to convert a date and time based on two different offsets from UTC rather than on the time in particular time zones.</span></span> <span data-ttu-id="c6880-161">Chcete-li provést tento převod, můžete zavolat metodu aktuální instance <xref:System.DateTimeOffset.ToOffset%2A> .</span><span class="sxs-lookup"><span data-stu-id="c6880-161">To perform this conversion, you can call the current instance's <xref:System.DateTimeOffset.ToOffset%2A> method.</span></span> <span data-ttu-id="c6880-162">Jediným parametrem metody je posun nové hodnoty data a času, kterou metoda vrátí.</span><span class="sxs-lookup"><span data-stu-id="c6880-162">The method's single parameter is the offset of the new date and time value that the method is to return.</span></span>

<span data-ttu-id="c6880-163">Například pokud je datum a čas požadavku uživatele na webovou stránku známo a serializován jako řetězec ve formátu MM/dd/rrrr hh: mm: SS zzzz, následující `ReturnTimeOnServer` Metoda převede tuto hodnotu data a času na webový server na datum a čas.</span><span class="sxs-lookup"><span data-stu-id="c6880-163">For example, if the date and time of a user request for a Web page is known and is serialized as a string in the format MM/dd/yyyy hh:mm:ss zzzz, the following `ReturnTimeOnServer` method converts this date and time value to the date and time on the Web server.</span></span>

[!code-csharp[System.DateTimeOffset.Conceptual.OffsetConversions#1](../../../samples/snippets/csharp/VS_Snippets_CLR_System/system.DateTimeOffset.Conceptual.OffsetConversions/cs/TimeConversions.cs#1)]
[!code-vb[System.DateTimeOffset.Conceptual.OffsetConversions#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR_System/system.DateTimeOffset.Conceptual.OffsetConversions/vb/TimeConversions.vb#1)]

<span data-ttu-id="c6880-164">Pokud je metodě předán řetězec "9/1/2007 5:32:07 -05:00", který představuje datum a čas v časovém pásmu 5 hodin před časem UTC, vrátí 9/1/2007 3:32:07 dop. 07:00 pro server umístěný v oblasti USA (běžný čas).</span><span class="sxs-lookup"><span data-stu-id="c6880-164">If the method is passed the string "9/1/2007 5:32:07 -05:00", which represents the date and time in a time zone five hours earlier than UTC, it returns 9/1/2007 3:32:07 AM -07:00 for a server located in the U.S. Pacific Standard Time zone.</span></span>

<span data-ttu-id="c6880-165"><xref:System.TimeZoneInfo>Třída také obsahuje přetížení <xref:System.TimeZoneInfo.ConvertTime%28System.DateTimeOffset%2CSystem.TimeZoneInfo%29?displayProperty=nameWithType> metody, která provádí převody časového pásma s <xref:System.DateTimeOffset.ToOffset(System.TimeSpan)> hodnotami.</span><span class="sxs-lookup"><span data-stu-id="c6880-165">The <xref:System.TimeZoneInfo> class also includes an overload of the <xref:System.TimeZoneInfo.ConvertTime%28System.DateTimeOffset%2CSystem.TimeZoneInfo%29?displayProperty=nameWithType> method that performs time zone conversions with <xref:System.DateTimeOffset.ToOffset(System.TimeSpan)> values.</span></span> <span data-ttu-id="c6880-166">Parametry metody jsou <xref:System.DateTimeOffset> hodnota a odkaz na časové pásmo, do kterého má být čas převeden.</span><span class="sxs-lookup"><span data-stu-id="c6880-166">The method's parameters are a <xref:System.DateTimeOffset> value and a reference to the time zone to which the time is to be converted.</span></span> <span data-ttu-id="c6880-167">Volání metody vrací <xref:System.DateTimeOffset> hodnotu.</span><span class="sxs-lookup"><span data-stu-id="c6880-167">The method call returns a <xref:System.DateTimeOffset> value.</span></span> <span data-ttu-id="c6880-168">Například `ReturnTimeOnServer` metoda v předchozím příkladu může být přepsána následujícím způsobem pro volání <xref:System.TimeZoneInfo.ConvertTime%28System.DateTimeOffset%2CSystem.TimeZoneInfo%29> metody.</span><span class="sxs-lookup"><span data-stu-id="c6880-168">For example, the `ReturnTimeOnServer` method in the previous example could be rewritten as follows to call the <xref:System.TimeZoneInfo.ConvertTime%28System.DateTimeOffset%2CSystem.TimeZoneInfo%29> method.</span></span>

[!code-csharp[System.DateTimeOffset.Conceptual.OffsetConversions#2](../../../samples/snippets/csharp/VS_Snippets_CLR_System/system.DateTimeOffset.Conceptual.OffsetConversions/cs/timeconversions2.cs#2)]
[!code-vb[System.DateTimeOffset.Conceptual.OffsetConversions#2](../../../samples/snippets/visualbasic/VS_Snippets_CLR_System/system.DateTimeOffset.Conceptual.OffsetConversions/vb/TimeConversions2.vb#2)]

## <a name="see-also"></a><span data-ttu-id="c6880-169">Viz také</span><span class="sxs-lookup"><span data-stu-id="c6880-169">See also</span></span>

- <xref:System.TimeZoneInfo>
- [<span data-ttu-id="c6880-170">Data, časy a časová pásma</span><span class="sxs-lookup"><span data-stu-id="c6880-170">Dates, times, and time zones</span></span>](index.md)
- [<span data-ttu-id="c6880-171">Hledání časových pásem definovaných v lokálním systému</span><span class="sxs-lookup"><span data-stu-id="c6880-171">Finding the time zones defined on a local system</span></span>](finding-the-time-zones-on-local-system.md)
