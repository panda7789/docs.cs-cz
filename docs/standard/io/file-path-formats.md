---
title: Formáty cesty k souborům v systémech Windows
ms.date: 06/06/2019
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- I/O, long paths
- long paths
- path formats, Windows
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 75261bc44b938432c9c22b90dc4db30ca00d630b
ms.sourcegitcommit: a8d3504f0eae1a40bda2b06bd441ba01f1631ef0
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/18/2019
ms.locfileid: "67170731"
---
# <a name="file-path-formats-on-windows-systems"></a><span data-ttu-id="f92aa-102">Formáty cesty k souborům v systémech Windows</span><span class="sxs-lookup"><span data-stu-id="f92aa-102">File path formats on Windows systems</span></span>

<span data-ttu-id="f92aa-103">Členy typů v mnoha <xref:System.IO> zahrnovat obor názvů `path` parametr, který vám umožní zadat absolutní nebo relativní cesta k souboru systémového prostředku.</span><span class="sxs-lookup"><span data-stu-id="f92aa-103">Members of many of the types in the <xref:System.IO> namespace include a `path` parameter that lets you specify an absolute or relative path to a file system resource.</span></span> <span data-ttu-id="f92aa-104">Tato cesta je pak předán [systém rozhraní API souborů Windows](/windows/desktop/fileio/file-systems).</span><span class="sxs-lookup"><span data-stu-id="f92aa-104">This path is then passed to [Windows file system APIs](/windows/desktop/fileio/file-systems).</span></span> <span data-ttu-id="f92aa-105">Toto téma popisuje formáty pro cesty k souborům, které vám v systémech Windows.</span><span class="sxs-lookup"><span data-stu-id="f92aa-105">This topic discusses the formats for file paths that you can use on Windows systems.</span></span>

## <a name="traditional-dos-paths"></a><span data-ttu-id="f92aa-106">Tradiční DOS cesty</span><span class="sxs-lookup"><span data-stu-id="f92aa-106">Traditional DOS paths</span></span>

<span data-ttu-id="f92aa-107">Standardní cesta systému DOS se může skládat ze tří komponent:</span><span class="sxs-lookup"><span data-stu-id="f92aa-107">A standard DOS path can consist of three components:</span></span>

- <span data-ttu-id="f92aa-108">Na svazku nebo písmeno jednotky, za nímž následuje oddělovač svazků (`:`).</span><span class="sxs-lookup"><span data-stu-id="f92aa-108">A volume or drive letter followed by the volume separator (`:`).</span></span>
- <span data-ttu-id="f92aa-109">Název adresáře.</span><span class="sxs-lookup"><span data-stu-id="f92aa-109">A directory name.</span></span> <span data-ttu-id="f92aa-110">[Znakem oddělovače adresářů](<xref:System.IO.Path.DirectorySeparatorChar>) odděluje podadresáře v rámci hierarchie vnořené adresářů.</span><span class="sxs-lookup"><span data-stu-id="f92aa-110">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates subdirectories within the nested directory hierarchy.</span></span>
- <span data-ttu-id="f92aa-111">Volitelný název souboru.</span><span class="sxs-lookup"><span data-stu-id="f92aa-111">An optional filename.</span></span> <span data-ttu-id="f92aa-112">[Znakem oddělovače adresářů](<xref:System.IO.Path.DirectorySeparatorChar>) odděluje cestu k souboru a název souboru.</span><span class="sxs-lookup"><span data-stu-id="f92aa-112">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates the file path and the filename.</span></span>

<span data-ttu-id="f92aa-113">Pokud jsou k dispozici všechny tři komponenty, je absolutní cesta.</span><span class="sxs-lookup"><span data-stu-id="f92aa-113">If all three components are present, the path is absolute.</span></span> <span data-ttu-id="f92aa-114">Pokud je zadán žádný svazku nebo písmeno jednotky a název adresáře začíná [znakem oddělovače adresářů](<xref:System.IO.Path.DirectorySeparatorChar>), cesta je relativní od kořenové aktuální jednotku.</span><span class="sxs-lookup"><span data-stu-id="f92aa-114">If no volume or drive letter is specified and the directory name begins with the [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>), the path is relative from the root of the current drive.</span></span> <span data-ttu-id="f92aa-115">V opačném případě cesta je relativní vzhledem k aktuálnímu adresáři.</span><span class="sxs-lookup"><span data-stu-id="f92aa-115">Otherwise, the path is relative to the current directory.</span></span> <span data-ttu-id="f92aa-116">V následující tabulce jsou uvedeny některé možné adresáře a cesty k souborům.</span><span class="sxs-lookup"><span data-stu-id="f92aa-116">The following table shows some possible directory and file paths.</span></span>

|<span data-ttu-id="f92aa-117">Cesta</span><span class="sxs-lookup"><span data-stu-id="f92aa-117">Path</span></span>  |<span data-ttu-id="f92aa-118">Popis</span><span class="sxs-lookup"><span data-stu-id="f92aa-118">Description</span></span>  |
| -- | -- |
| `C:\Documents\Newsletters\Summer2018.pdf` | <span data-ttu-id="f92aa-119">Absolutní cestu k souboru z kořenové složky jednotky C:</span><span class="sxs-lookup"><span data-stu-id="f92aa-119">An absolute file path from the root of drive C:</span></span> |
| `\Program Files\Custom Utilities\StringFinder.exe` | <span data-ttu-id="f92aa-120">Absolutní cesta z kořenového adresáře aktuální jednotku.</span><span class="sxs-lookup"><span data-stu-id="f92aa-120">An absolute path from the root of the current drive.</span></span> |
| `2018\January.xlsx` | <span data-ttu-id="f92aa-121">Relativní cesta k souboru v podadresáři aktuálního adresáře.</span><span class="sxs-lookup"><span data-stu-id="f92aa-121">A relative path to a file in a subdirectory of the current directory.</span></span> |
| `..\Publications\TravelBrochure.pdf` | <span data-ttu-id="f92aa-122">Relativní cesta k souboru v adresáři, který je partnerské zařízení do aktuálního adresáře.</span><span class="sxs-lookup"><span data-stu-id="f92aa-122">A relative path to file in a directory that is a peer of the current directory.</span></span> |
| `C:\Projects\apilibrary\apilibrary.sln` | <span data-ttu-id="f92aa-123">Absolutní cesta k souboru z kořenové složky jednotky C:</span><span class="sxs-lookup"><span data-stu-id="f92aa-123">An absolute path to a file from the root of drive C:</span></span> |
| `C:Projects\apilibrary\apilibrary.sln` | <span data-ttu-id="f92aa-124">Relativní cesta z aktuálního adresáře jednotku C:.</span><span class="sxs-lookup"><span data-stu-id="f92aa-124">A relative path from the current directory of the C: drive.</span></span> |

> [!IMPORTANT]
> <span data-ttu-id="f92aa-125">Všimněte si rozdílu mezi poslední dvě cesty.</span><span class="sxs-lookup"><span data-stu-id="f92aa-125">Note the difference between the last two paths.</span></span> <span data-ttu-id="f92aa-126">Jak zadat volitelný svazku specifikátor (C: v obou případech), ale první začíná kořenové zadaný svazek, zatímco druhá ne.</span><span class="sxs-lookup"><span data-stu-id="f92aa-126">Both specify the optional volume specifier (C: in both cases), but the first begins with the root of the specified volume, whereas the second does not.</span></span> <span data-ttu-id="f92aa-127">První je jako výsledek, absolutní cesta z kořenového adresáře jednotku C:, zatímco druhá je relativní cesta z aktuálního adresáře jednotce C:.</span><span class="sxs-lookup"><span data-stu-id="f92aa-127">As result, the first is an absolute path from the root directory of drive C:, whereas the second is a relative path from the current directory of drive C:.</span></span> <span data-ttu-id="f92aa-128">Použijte druhý formulář, když je první určené je běžné příčiny chyby, které se týkají cesty k souborům Windows.</span><span class="sxs-lookup"><span data-stu-id="f92aa-128">Use of the second form when the first is intended is a common source of bugs that involve Windows file paths.</span></span>

<span data-ttu-id="f92aa-129">Můžete určit, zda je plně kvalifikovanou cestu k souboru (to znamená, ho cesta je nezávislý na aktuálním adresáři a nemění, když se změní aktuální adresář) voláním <xref:System.IO.Path.IsPathFullyQualified%2A?displayProperty=nameWthType> metody.</span><span class="sxs-lookup"><span data-stu-id="f92aa-129">You can determine whether a file path is fully qualified (that is, it the path is independent of the current directory and does not change when the current directory changes) by calling the <xref:System.IO.Path.IsPathFullyQualified%2A?displayProperty=nameWthType> method.</span></span> <span data-ttu-id="f92aa-130">Všimněte si, že tyto cesty může obsahovat relativní directory segmentů (`.` a `..`) a i nadále plně kvalifikovaný Pokud Vyhodnocená cesta vždy odkazuje na stejném umístění.</span><span class="sxs-lookup"><span data-stu-id="f92aa-130">Note that such a path can include relative directory segments (`.` and `..`) and still be fully qualified if the resolved path always points to the same location.</span></span>

<span data-ttu-id="f92aa-131">Následující příklad ukazuje rozdíl mezi absolutní a relativní cesty.</span><span class="sxs-lookup"><span data-stu-id="f92aa-131">The following example illustrates the difference between absolute and relative paths.</span></span> <span data-ttu-id="f92aa-132">Předpokládá, že existuje adresář D:\FY2018\ a, že jste nenastavili žádné aktuální adresář pro D:\ z příkazového řádku před spuštěním v příkladu.</span><span class="sxs-lookup"><span data-stu-id="f92aa-132">It assumes that the directory D:\FY2018\ exists, and that you haven't set any curent directory for D:\ from the command prompt before running the example.</span></span>

[!code-csharp[absolute-and-relative-paths](~/samples/snippets/standard/io/file-names/cs/paths.cs)]
[!code-vb[absolute-and-relative-paths](~/samples/snippets/standard/io/file-names/vb/paths.vb)]

## <a name="unc-paths"></a><span data-ttu-id="f92aa-133">Cesty UNC</span><span class="sxs-lookup"><span data-stu-id="f92aa-133">UNC paths</span></span>

<span data-ttu-id="f92aa-134">Universal naming convention (UNC) cesty, které se používají pro přístup k síťovým prostředkům, mají tento formát:</span><span class="sxs-lookup"><span data-stu-id="f92aa-134">Universal naming convention (UNC) paths, which are used to access network resources, have the following format:</span></span>

- <span data-ttu-id="f92aa-135">Název serveru nebo hostitele, která je uvedena ve \\ \\.</span><span class="sxs-lookup"><span data-stu-id="f92aa-135">A server or host name, which is prefaced by \\\\.</span></span> <span data-ttu-id="f92aa-136">Název serveru může být název počítače pro rozhraní NetBIOS nebo IP nebo plně kvalifikovaný název domény adresy (IPv4 a IPv6 jsou podporovány).</span><span class="sxs-lookup"><span data-stu-id="f92aa-136">The server name can be a NetBIOS machine name or an IP/FQDN address (IPv4 as well as v6 are supported).</span></span>
- <span data-ttu-id="f92aa-137">Název sdílené složky, která je oddělená od názvu hostitele ve \\.</span><span class="sxs-lookup"><span data-stu-id="f92aa-137">A share name, which is separated from the host name by \\.</span></span> <span data-ttu-id="f92aa-138">Název sdílené složky serveru a tvoří společně svazku.</span><span class="sxs-lookup"><span data-stu-id="f92aa-138">Together, the server and share name make up the volume.</span></span>
- <span data-ttu-id="f92aa-139">Název adresáře.</span><span class="sxs-lookup"><span data-stu-id="f92aa-139">A directory name.</span></span> <span data-ttu-id="f92aa-140">[Znakem oddělovače adresářů](<xref:System.IO.Path.DirectorySeparatorChar>) odděluje podadresáře v rámci hierarchie vnořené adresářů.</span><span class="sxs-lookup"><span data-stu-id="f92aa-140">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates subdirectories within the nested directory hierarchy.</span></span>
- <span data-ttu-id="f92aa-141">Volitelný název souboru.</span><span class="sxs-lookup"><span data-stu-id="f92aa-141">An optional filename.</span></span> <span data-ttu-id="f92aa-142">[Znakem oddělovače adresářů](<xref:System.IO.Path.DirectorySeparatorChar>) odděluje cestu k souboru a název souboru.</span><span class="sxs-lookup"><span data-stu-id="f92aa-142">The [directory separator character](<xref:System.IO.Path.DirectorySeparatorChar>) separates the file path and the filename.</span></span>

<span data-ttu-id="f92aa-143">Následují příklady cesty UNC:</span><span class="sxs-lookup"><span data-stu-id="f92aa-143">The following are some examples of UNC paths:</span></span>

|<span data-ttu-id="f92aa-144">Cesta</span><span class="sxs-lookup"><span data-stu-id="f92aa-144">Path</span></span>  |<span data-ttu-id="f92aa-145">Popis</span><span class="sxs-lookup"><span data-stu-id="f92aa-145">Description</span></span>  |
| -- | -- |
| `\\system07\C$\` | <span data-ttu-id="f92aa-146">Do kořenového adresáře C: jednotky na `system07`.</span><span class="sxs-lookup"><span data-stu-id="f92aa-146">The root directory of the C: drive on `system07`.</span></span> |
| `\\Server2\Share\Test\Foo.txt` | <span data-ttu-id="f92aa-147">Foo.txt soubor v adresáři Test \\ \\Server2\\sdílet svazku.</span><span class="sxs-lookup"><span data-stu-id="f92aa-147">The Foo.txt file in the Test directory of the \\\\Server2\\Share volume.</span></span>|

<span data-ttu-id="f92aa-148">Musí být vždy plně kvalifikované cesty UNC.</span><span class="sxs-lookup"><span data-stu-id="f92aa-148">UNC paths must always be fully qualified.</span></span> <span data-ttu-id="f92aa-149">Mohou obsahovat relativní directory segmentů (`.` a `..`), ale musí být součástí plně kvalifikovanou cestu.</span><span class="sxs-lookup"><span data-stu-id="f92aa-149">They can include relative directory segments (`.` and `..`), but these must be part of a fully qualified path.</span></span> <span data-ttu-id="f92aa-150">Pouze pomocí mapování cestu UNC k písmenu jednotky můžete použít relativní cesty.</span><span class="sxs-lookup"><span data-stu-id="f92aa-150">You can use relative paths only by mapping a UNC path to a drive letter.</span></span>

## <a name="dos-device-paths"></a><span data-ttu-id="f92aa-151">Umístění zařízení DOS</span><span class="sxs-lookup"><span data-stu-id="f92aa-151">DOS device paths</span></span>

<span data-ttu-id="f92aa-152">Operační systém Windows obsahuje jednotné objektový model, který odkazuje na všechny prostředky, včetně souborů.</span><span class="sxs-lookup"><span data-stu-id="f92aa-152">The Windows operating system has a unified object model that points to all resources, including files.</span></span> <span data-ttu-id="f92aa-153">Tyto cesty k objektům jsou přístupné z okna konzoly a jsou přístupné prostřednictvím speciální složky symbolické odkazy, které jsou starší verze programů DOS a UNC cesty mapované na vrstvě Win32.</span><span class="sxs-lookup"><span data-stu-id="f92aa-153">These object paths are accessible from the console window and are exposed to the Win32 layer through a special folder of symbolic links that legacy DOS and UNC paths are mapped to.</span></span> <span data-ttu-id="f92aa-154">Tato zvláštní složka se přistupuje přes syntaxe cesty zařízení DOS, což je jedna z:</span><span class="sxs-lookup"><span data-stu-id="f92aa-154">This special folder is accessed via the DOS device path syntax, which is one of:</span></span>

`\\.\C:\Test\Foo.txt`  
`\\?\C:\Test\Foo.txt`

<span data-ttu-id="f92aa-155">Kromě identifikace disku podle jeho písmeno jednotky, můžete identifikovat pomocí jeho identifikátoru GUID svazku.</span><span class="sxs-lookup"><span data-stu-id="f92aa-155">In addition to identifying a drive by its drive letter, you can identify a volume by using its volume GUID.</span></span> <span data-ttu-id="f92aa-156">To má podobu:</span><span class="sxs-lookup"><span data-stu-id="f92aa-156">This takes the form:</span></span>

`\\.\Volume{b75e2c83-0000-0000-0000-602f00000000}\Test\Foo.txt`
`\\?\Volume{b75e2c83-0000-0000-0000-602f00000000}\Test\Foo.txt`

> [!NOTE]
> <span data-ttu-id="f92aa-157">Syntaxe cesty zařízení DOS je podporovaná v implementace .NET běžící na Windows od verze 1.1 rozhraní .NET Core a .NET Framework 4.6.2.</span><span class="sxs-lookup"><span data-stu-id="f92aa-157">DOS device path syntax is supported on .NET implementations running on Windows starting with .NET Core 1.1 and .NET Framework 4.6.2.</span></span>

<span data-ttu-id="f92aa-158">Cesta systému DOS zařízení se skládá z následujících součástí:</span><span class="sxs-lookup"><span data-stu-id="f92aa-158">The DOS device path consists of the following components:</span></span>

- <span data-ttu-id="f92aa-159">Specifikátor cesta zařízení (`\\.\` nebo `\\?\`), který určuje cestu jako cesta systému DOS zařízení.</span><span class="sxs-lookup"><span data-stu-id="f92aa-159">The device path specifier (`\\.\` or `\\?\`), which identifies the path as a DOS device path.</span></span>

   > [!NOTE]
   > <span data-ttu-id="f92aa-160">`\\?\` Je podporována ve všech verzích .NET Core a .NET Framework, od verze 4.6.2.</span><span class="sxs-lookup"><span data-stu-id="f92aa-160">The `\\?\` is supported in all versions of .NET Core and in the .NET Framework starting with version 4.6.2.</span></span>

- <span data-ttu-id="f92aa-161">Symbolický odkaz na objekt "real" zařízení (v případě název jednotky C:) nebo svazek {b75e2c83-0000-0000-0000-602f00000000} v případě identifikátoru GUID svazku.</span><span class="sxs-lookup"><span data-stu-id="f92aa-161">A symbolic link to the "real" device object (C: in the case of a drive name, or Volume{b75e2c83-0000-0000-0000-602f00000000} in the case of a volume GUID).</span></span>

   <span data-ttu-id="f92aa-162">První segment cesty zařízení DOS po specifikátoru cesta zařízení identifikuje svazku nebo jednotce.</span><span class="sxs-lookup"><span data-stu-id="f92aa-162">The first segment of the DOS device path after the device path specifier identifies the volume or drive.</span></span> <span data-ttu-id="f92aa-163">(Například `\\?\C:\` a `\\.\BootPartition\`.)</span><span class="sxs-lookup"><span data-stu-id="f92aa-163">(For example, `\\?\C:\` and `\\.\BootPartition\`.)</span></span>

   <span data-ttu-id="f92aa-164">Konkrétní odkaz pro UNC, která je volána, nejsou překvapivě `UNC`.</span><span class="sxs-lookup"><span data-stu-id="f92aa-164">There is a specific link for UNCs that is called, not surprisingly, `UNC`.</span></span> <span data-ttu-id="f92aa-165">Příklad:</span><span class="sxs-lookup"><span data-stu-id="f92aa-165">For example:</span></span>

  `\\.\UNC\Server\Share\Test\Foo.txt`  
  `\\?\UNC\Server\Share\Test\Foo.txt`

    <span data-ttu-id="f92aa-166">Pro zařízení UNC tvoří část sdílené složky serveru/svazek.</span><span class="sxs-lookup"><span data-stu-id="f92aa-166">For device UNCs, the server/share portion forms the volume.</span></span> <span data-ttu-id="f92aa-167">Například v `\\?\server1\e:\utilities\\filecomparer\`, část server a sdílet je server1\utilities.</span><span class="sxs-lookup"><span data-stu-id="f92aa-167">For example, in `\\?\server1\e:\utilities\\filecomparer\`, the server/share portion is server1\utilities.</span></span> <span data-ttu-id="f92aa-168">To je důležité při volání metody, jako <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> relativní directory segmenty; se nikdy možné přejít po svazku.</span><span class="sxs-lookup"><span data-stu-id="f92aa-168">This is significant when calling a method such as <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> with relative directory segments; it is never possible to navigate past the volume.</span></span> 

<span data-ttu-id="f92aa-169">Podle definice jsou plně kvalifikované cesty zařízení DOS.</span><span class="sxs-lookup"><span data-stu-id="f92aa-169">DOS device paths are fully qualified by definition.</span></span> <span data-ttu-id="f92aa-170">Relativní directory segmentů (`.` a `..`) nejsou povoleny.</span><span class="sxs-lookup"><span data-stu-id="f92aa-170">Relative directory segments (`.` and `..`) are not allowed.</span></span> <span data-ttu-id="f92aa-171">Aktuální adresáře nikdy odsouhlasit jejich využití.</span><span class="sxs-lookup"><span data-stu-id="f92aa-171">Current directories never enter into their usage.</span></span>

## <a name="example-ways-to-refer-to-the-same-file"></a><span data-ttu-id="f92aa-172">Příklad: Způsoby, jak odkazovat na stejný soubor</span><span class="sxs-lookup"><span data-stu-id="f92aa-172">Example: Ways to refer to the same file</span></span>

<span data-ttu-id="f92aa-173">Následující příklad ukazuje několik způsobů jak, ve kterém najdete soubor při použití rozhraní API v <xref:System.IO> oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="f92aa-173">The following example illustrates some of the ways in which you can refer to a file when using the APIs in the <xref:System.IO> namespace.</span></span> <span data-ttu-id="f92aa-174">Příkladu je vytvořena instance <xref:System.IO.FileInfo> a použije jeho <xref:System.IO.FileInfo.Name> a <xref:System.IO.FileInfo.Length> vlastnosti, které chcete zobrazit název souboru a délku souboru.</span><span class="sxs-lookup"><span data-stu-id="f92aa-174">The example instantiates a <xref:System.IO.FileInfo> object and uses its <xref:System.IO.FileInfo.Name> and <xref:System.IO.FileInfo.Length> properties to display the filename and the length of the file.</span></span>

[!code-csharp[referring-to-the-same-file](~/samples/snippets/standard/io/file-names/cs/file-refs.cs)]
[!code-vb[referring-to-the-same-file](~/samples/snippets/standard/io/file-names/vb/file-refs.vb)]

## <a name="path-normalization"></a><span data-ttu-id="f92aa-175">Normalizace cesta</span><span class="sxs-lookup"><span data-stu-id="f92aa-175">Path normalization</span></span>

<span data-ttu-id="f92aa-176">Jsou normalizovány téměř všechny cesty předán rozhraní Windows API.</span><span class="sxs-lookup"><span data-stu-id="f92aa-176">Almost all paths passed to Windows APIs are normalized.</span></span> <span data-ttu-id="f92aa-177">Během normalizace Windows provede následující kroky:</span><span class="sxs-lookup"><span data-stu-id="f92aa-177">During normalization, Windows performs the following steps:</span></span>

- <span data-ttu-id="f92aa-178">Určuje cestu.</span><span class="sxs-lookup"><span data-stu-id="f92aa-178">Identifies the path.</span></span>
- <span data-ttu-id="f92aa-179">Částečně kvalifikované (relativní) cesty se týká aktuální adresář.</span><span class="sxs-lookup"><span data-stu-id="f92aa-179">Applies the current directory to partially qualified (relative) paths.</span></span>
- <span data-ttu-id="f92aa-180">Canonicalizes oddělovače komponentu a adresáře.</span><span class="sxs-lookup"><span data-stu-id="f92aa-180">Canonicalizes component and directory separators.</span></span>
- <span data-ttu-id="f92aa-181">Vyhodnotí jako relativní directory součásti (`.` pro aktuální adresář a `..` pro nadřazený adresář).</span><span class="sxs-lookup"><span data-stu-id="f92aa-181">Evaluates relative directory components (`.` for the current directory and `..` for the parent directory).</span></span>
- <span data-ttu-id="f92aa-182">Ořízne určitých znaků.</span><span class="sxs-lookup"><span data-stu-id="f92aa-182">Trims certain characters.</span></span>

<span data-ttu-id="f92aa-183">Implicitně se stane toto normalizace, ale můžete to provést explicitně voláním <xref:System.IO.Path.GetFullPath%2A?displayProperty=nameWithType> metoda, která zabalí volání [GetFullPathName() funkce](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).</span><span class="sxs-lookup"><span data-stu-id="f92aa-183">This normalization happens implicitly, but you can do it explicitly by calling the <xref:System.IO.Path.GetFullPath%2A?displayProperty=nameWithType> method, which wraps a call to the  [GetFullPathName() function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).</span></span> <span data-ttu-id="f92aa-184">Můžete také volat Windows [GetFullPathName() funkce](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) přímo pomocí deklarace P/Invoke.</span><span class="sxs-lookup"><span data-stu-id="f92aa-184">You can also call the Windows [GetFullPathName() function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) directly using P/Invoke.</span></span>

### <a name="identifying-the-path"></a><span data-ttu-id="f92aa-185">Určení cesty</span><span class="sxs-lookup"><span data-stu-id="f92aa-185">Identifying the path</span></span>

<span data-ttu-id="f92aa-186">Prvním krokem při normalizaci cesta je identifikace typu cesty.</span><span class="sxs-lookup"><span data-stu-id="f92aa-186">The first step in path normalization is identifying the type of path.</span></span> <span data-ttu-id="f92aa-187">Cesty spadají do jedné z několika kategorií:</span><span class="sxs-lookup"><span data-stu-id="f92aa-187">Paths fall into one of a few categories:</span></span>

- <span data-ttu-id="f92aa-188">Jsou cesty zařízení; To znamená, že začínat dva oddělovače a otazník nebo období (`\\?` nebo `\\.`).</span><span class="sxs-lookup"><span data-stu-id="f92aa-188">They are device paths; that is, they begin with two separators and a question mark or period (`\\?` or `\\.`).</span></span>
- <span data-ttu-id="f92aa-189">Jsou cesty UNC; To znamená že začínat dva oddělovače bez otazník nebo období.</span><span class="sxs-lookup"><span data-stu-id="f92aa-189">They are UNC paths; that is, they begin with two separators without a question mark or period.</span></span> 
- <span data-ttu-id="f92aa-190">Jsou plně kvalifikovanou cestou DOS; To znamená, začněte s písmenem jednotky, oddělovač svazků a oddělovač komponent (`C:\`).</span><span class="sxs-lookup"><span data-stu-id="f92aa-190">They are fully qualified DOS paths; that is, they begin with a drive letter, a volume separator, and a component separator (`C:\`).</span></span>
- <span data-ttu-id="f92aa-191">Určí starší zařízení (`CON`, `LPT1`).</span><span class="sxs-lookup"><span data-stu-id="f92aa-191">They designate a legacy device (`CON`, `LPT1`).</span></span>
- <span data-ttu-id="f92aa-192">Jsou kořeni aktuální jednotce. To znamená, začínat oddělovačem jednotlivé součásti (`\`).</span><span class="sxs-lookup"><span data-stu-id="f92aa-192">They are relative to the root of the current drive; that is, they begin with a single component separator (`\`).</span></span>
- <span data-ttu-id="f92aa-193">Jsou relativní vzhledem k aktuální adresář zadané jednotky. To znamená, začněte s písmenem jednotky, oddělovač svazků a žádný oddělovač komponent (`C:`).</span><span class="sxs-lookup"><span data-stu-id="f92aa-193">They are relative to the current directory of a specified drive; that is, they begin with a drive letter, a volume separator, and no component separator (`C:`).</span></span>
- <span data-ttu-id="f92aa-194">Jsou relativní vzhledem k aktuálnímu adresáři; To znamená, že začínat cokoli jiného (`temp\testfile.txt`).</span><span class="sxs-lookup"><span data-stu-id="f92aa-194">They are relative to the current directory; that is, they begin with anything else (`temp\testfile.txt`).</span></span>

<span data-ttu-id="f92aa-195">Typ cesty určuje, zda je nějakým způsobem použít aktuální adresář.</span><span class="sxs-lookup"><span data-stu-id="f92aa-195">The type of the path determines whether or not a current directory is applied in some way.</span></span> <span data-ttu-id="f92aa-196">Také určuje, co je "root" cesty.</span><span class="sxs-lookup"><span data-stu-id="f92aa-196">It also determines what the "root" of the path is.</span></span>

### <a name="handling-legacy-devices"></a><span data-ttu-id="f92aa-197">Manipulace se zařízením starší verze</span><span class="sxs-lookup"><span data-stu-id="f92aa-197">Handling legacy devices</span></span>

<span data-ttu-id="f92aa-198">Pokud cestu, jako je starší verze zařízení DOS `CON`, `COM1`, nebo `LPT1`, je převeden na cestu zařízení předponou v podobě `\\.\` a vrátilo.</span><span class="sxs-lookup"><span data-stu-id="f92aa-198">If the path is a legacy DOS device such as `CON`, `COM1`, or `LPT1`, it is converted into a device path by prepending `\\.\` and returned.</span></span> 

<span data-ttu-id="f92aa-199">Cestu, která začíná názvem starší zařízení je vždy interpretováno jako starší zařízení podle <xref:System.IO.Path.GetFullPath(System.String)?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="f92aa-199">A path that begins with a legacy device name is always interpreted as a legacy device by the <xref:System.IO.Path.GetFullPath(System.String)?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="f92aa-200">Například zařízení cesta systému DOS pro `CON.TXT` je `\\.\CON`a cesta systému DOS zařízení pro `COM1.TXT\file1.txt` je `\\.\COM1`.</span><span class="sxs-lookup"><span data-stu-id="f92aa-200">For example, the DOS device path for `CON.TXT` is `\\.\CON`, and the DOS device path for `COM1.TXT\file1.txt` is `\\.\COM1`.</span></span>

### <a name="applying-the-current-directory"></a><span data-ttu-id="f92aa-201">Použití aktuální adresář</span><span class="sxs-lookup"><span data-stu-id="f92aa-201">Applying the current directory</span></span>

<span data-ttu-id="f92aa-202">Pokud není plně kvalifikovanou cestu, Windows použije aktuální adresář na něj.</span><span class="sxs-lookup"><span data-stu-id="f92aa-202">If a path isn't fully qualified, Windows applies the current directory to it.</span></span> <span data-ttu-id="f92aa-203">UNC a cesty k zařízení není nutné použít aktuální adresář.</span><span class="sxs-lookup"><span data-stu-id="f92aa-203">UNCs and device paths do not have the current directory applied.</span></span> <span data-ttu-id="f92aa-204">Ani nemá jednotku úplné oddělovačem C:\\.</span><span class="sxs-lookup"><span data-stu-id="f92aa-204">Neither does a full drive with separator C:\\.</span></span>

<span data-ttu-id="f92aa-205">Pokud cesta musí začínat znakem oddělovače jednu součást, použije se jednotky z aktuálního adresáře.</span><span class="sxs-lookup"><span data-stu-id="f92aa-205">If the path starts with a single component separator, the drive from the current directory is applied.</span></span> <span data-ttu-id="f92aa-206">Například, pokud je cesta k souboru `\utilities` a aktuální adresář je `C:\temp\`, vytvoří normalizace `C:\utilities`.</span><span class="sxs-lookup"><span data-stu-id="f92aa-206">For example, if the file path is `\utilities` and the current directory is `C:\temp\`, normalization produces `C:\utilities`.</span></span>

<span data-ttu-id="f92aa-207">Pokud cesta musí začínat písmenem jednotky, oddělovač svazků a žádný oddělovač komponent, použije se poslední aktuální adresář sady z příkazového okna na zadané jednotce.</span><span class="sxs-lookup"><span data-stu-id="f92aa-207">If the path starts with a drive letter, volume separator, and no component separator, the last current directory set from the command shell for the specified drive is applied.</span></span> <span data-ttu-id="f92aa-208">Pokud poslední aktuální adresář nebyla nastavena, použije se na jednotku, samotného.</span><span class="sxs-lookup"><span data-stu-id="f92aa-208">If the last current directory was not set, the drive alone is applied.</span></span> <span data-ttu-id="f92aa-209">Například, pokud je cesta k souboru `D:sources`, aktuální adresář je `C:\Documents\`, a poslední aktuální adresář na jednotce D: `D:\sources\`, výsledkem je `D:\sources\sources`.</span><span class="sxs-lookup"><span data-stu-id="f92aa-209">For example, if the file path is `D:sources`, the current directory is `C:\Documents\`, and the last current directory on drive D: was `D:\sources\`, the result is `D:\sources\sources`.</span></span> <span data-ttu-id="f92aa-210">Tyto cesty "jednotka relativní" jsou běžné příčiny chyby logiku programu a skript.</span><span class="sxs-lookup"><span data-stu-id="f92aa-210">These "drive relative" paths are a common source of program and script logic errors.</span></span> <span data-ttu-id="f92aa-211">Za předpokladu, že se relativní cesta začínající písmenem a dvojtečky je samozřejmě není správná.</span><span class="sxs-lookup"><span data-stu-id="f92aa-211">Assuming that a path beginning with a letter and a colon isn't relative is obviously not correct.</span></span>

<span data-ttu-id="f92aa-212">Pokud cesta musí začínat znakem něco jiného než oddělovač, se použijí aktuální jednotky a aktuální adresář.</span><span class="sxs-lookup"><span data-stu-id="f92aa-212">If the path starts with something other than a separator, the current drive and current directory are applied.</span></span> <span data-ttu-id="f92aa-213">Například, pokud je cesta `filecompare` a aktuální adresář je `C:\utilities\`, výsledkem je `C:\utilities\filecompare\`.</span><span class="sxs-lookup"><span data-stu-id="f92aa-213">For example, if the path is `filecompare` and the current directory is `C:\utilities\`, the result is `C:\utilities\filecompare\`.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="f92aa-214">Relativní cesty jsou nebezpečné ve vícevláknových aplikacích (to znamená, že většina aplikací), protože aktuální adresář je na úrovni jednotlivého procesu nastavení.</span><span class="sxs-lookup"><span data-stu-id="f92aa-214">Relative paths are dangerous in multithreaded applications (that is, most applications) because the current directory is a per-process setting.</span></span> <span data-ttu-id="f92aa-215">Jakékoli vlákno může kdykoli změnit aktuální adresář.</span><span class="sxs-lookup"><span data-stu-id="f92aa-215">Any thread can change the current directory at any time.</span></span> <span data-ttu-id="f92aa-216">Počínaje .NET Core 2.1, lze volat <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> metodu k získání absolutní cesta relativní cestu a základní cesta (aktuální adresář), který chcete vyřešit proti.</span><span class="sxs-lookup"><span data-stu-id="f92aa-216">Starting with .NET Core 2.1, you can call the <xref:System.IO.Path.GetFullPath(System.String,System.String)?displayProperty=nameWithType> method to get an absolute path from a relative path and the base path (the current directory) that you want to resolve it against.</span></span> 

### <a name="canonicalizing-separators"></a><span data-ttu-id="f92aa-217">Kanonizace oddělovače</span><span class="sxs-lookup"><span data-stu-id="f92aa-217">Canonicalizing separators</span></span>

<span data-ttu-id="f92aa-218">Všechna lomítka (`/`) se převedou na standardní oddělovač Windows zpětné lomítko (`\`).</span><span class="sxs-lookup"><span data-stu-id="f92aa-218">All forward slashes (`/`) are converted into the standard Windows separator, the back slash (`\`).</span></span> <span data-ttu-id="f92aa-219">Pokud jsou k dispozici, řadu lomítka, které následují první dvě lomítka, jsou sbaleny do jedné lomítko.</span><span class="sxs-lookup"><span data-stu-id="f92aa-219">If they are present, a series of slashes that follow the first two slashes are collapsed into a single slash.</span></span>

### <a name="evaluating-relative-components"></a><span data-ttu-id="f92aa-220">Hodnocení relativní komponenty</span><span class="sxs-lookup"><span data-stu-id="f92aa-220">Evaluating relative components</span></span>

<span data-ttu-id="f92aa-221">Po zpracování cestu žádné komponenty nebo segmenty, které se skládají z jednoho ani dvojitou tečku (`.` nebo `..`) jsou vyhodnoceny:</span><span class="sxs-lookup"><span data-stu-id="f92aa-221">As the path is processed, any components or segments that are composed of a single or a double period (`.` or `..`) are evaluated:</span></span> 

- <span data-ttu-id="f92aa-222">Pro jedno období aktuální segment Odebereme, protože odkazuje na aktuální adresář.</span><span class="sxs-lookup"><span data-stu-id="f92aa-222">For a single period, the current segment is removed, since it refers to the current directory.</span></span>

- <span data-ttu-id="f92aa-223">Pro dvojitou tečku aktuální segment a tento segment nadřazené jsou odebrané, protože dvojitou tečku odkazuje na nadřazený adresář.</span><span class="sxs-lookup"><span data-stu-id="f92aa-223">For a double period, the current segment and the parent segment are removed, since the double period refers to the parent directory.</span></span>

   <span data-ttu-id="f92aa-224">Nadřazené adresáře se odeberou, pouze pokud nejsou po kořen cesty.</span><span class="sxs-lookup"><span data-stu-id="f92aa-224">Parent directories are only removed if they aren't past the root of the path.</span></span> <span data-ttu-id="f92aa-225">Kořenovou cestu, závisí na typu cesty.</span><span class="sxs-lookup"><span data-stu-id="f92aa-225">The root of the path depends on the type of path.</span></span> <span data-ttu-id="f92aa-226">Je na jednotce (`C:\`) pro cesty DOS, nebo sdílené složky na serveru pro UNC (`\\Server\Share`) a předpona cesty zařízení pro zařízení cesty (`\\?\` nebo `\\.\`).</span><span class="sxs-lookup"><span data-stu-id="f92aa-226">It is the drive (`C:\`) for DOS paths, the server/share for UNCs (`\\Server\Share`), and the device path prefix for device paths (`\\?\` or `\\.\`).</span></span>

### <a name="trimming-characters"></a><span data-ttu-id="f92aa-227">Oříznutí znaků</span><span class="sxs-lookup"><span data-stu-id="f92aa-227">Trimming characters</span></span>

<span data-ttu-id="f92aa-228">Spolu s výskytů oddělovače a relativní segmenty odebrat dříve odeberou se nějakými dalšími znaky během normalizace:</span><span class="sxs-lookup"><span data-stu-id="f92aa-228">Along with the runs of separators and relative segments removed earlier, some additional characters are removed during normalization:</span></span>

- <span data-ttu-id="f92aa-229">Segment, který končí na jednu tečku, odeberou se tohoto období.</span><span class="sxs-lookup"><span data-stu-id="f92aa-229">If a segment ends in a single period, that period is removed.</span></span> <span data-ttu-id="f92aa-230">(Segment jednoduchých nebo dvojitých období je normalizovány v předchozím kroku.</span><span class="sxs-lookup"><span data-stu-id="f92aa-230">(A segment of a single or double period is normalized in the previous step.</span></span> <span data-ttu-id="f92aa-231">Segment tři nebo více období není normalizovaná a je ve skutečnosti název platný soubor nebo adresář.)</span><span class="sxs-lookup"><span data-stu-id="f92aa-231">A segment of three or more periods is not normalized and is actually a valid file/directory name.)</span></span>

- <span data-ttu-id="f92aa-232">Pokud cesta není končit oddělovačem, všechny koncové tečky ani mezery (U + 0020) se odeberou.</span><span class="sxs-lookup"><span data-stu-id="f92aa-232">If the path doesn't end in a separator, all trailing periods and spaces (U+0020) are removed.</span></span> <span data-ttu-id="f92aa-233">Pokud poslední segment je jednoduše jednoduchých nebo dvojitých období, spadají do výše uvedené pravidlo relativní komponenty.</span><span class="sxs-lookup"><span data-stu-id="f92aa-233">If the last segment is simply a single or double period, it falls under the relative components rule above.</span></span> 

   <span data-ttu-id="f92aa-234">Toto pravidlo znamená, že můžete vytvořit název adresáře s koncovou mezerou přidáním koncové oddělovače za prostor.</span><span class="sxs-lookup"><span data-stu-id="f92aa-234">This rule means that you can create a directory name with a trailing space by adding a trailing separator after the space.</span></span>  

   > [!IMPORTANT]
   > <span data-ttu-id="f92aa-235">Měli byste **nikdy** vytvořit adresář nebo název souboru s koncovou mezerou.</span><span class="sxs-lookup"><span data-stu-id="f92aa-235">You should **never** create a directory or filename with a trailing space.</span></span> <span data-ttu-id="f92aa-236">Koncové mezery může být obtížné nebo nemožné pro přístup k adresáři a aplikace běžně selhání při pokusu o zpracování adresářů a souborů, jejichž názvy obsahují koncové mezery.</span><span class="sxs-lookup"><span data-stu-id="f92aa-236">Trailing spaces can make it difficult or impossible to access a directory, and applications commonly fail when attempting to handle directories or files whose names include trailing spaces.</span></span>

## <a name="skipping-normalization"></a><span data-ttu-id="f92aa-237">Přeskakuje se normalizace</span><span class="sxs-lookup"><span data-stu-id="f92aa-237">Skipping normalization</span></span>

<span data-ttu-id="f92aa-238">Za normálních okolností libovolnou cestu předán rozhraní API Windows (efektivně) předán [GetFullPathName funkce](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) a normalizovaná.</span><span class="sxs-lookup"><span data-stu-id="f92aa-238">Normally, any path passed to a Windows API is (effectively) passed to the [GetFullPathName function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) and normalized.</span></span> <span data-ttu-id="f92aa-239">Existuje jedna důležitá výjimka: cesta zařízení, která začíná otazníkem místo tečku.</span><span class="sxs-lookup"><span data-stu-id="f92aa-239">There is one important exception: a device path that begins with a question mark instead of a period.</span></span> <span data-ttu-id="f92aa-240">Pokud cesta musí začínat znakem přesně `\\?\` (Všimněte si použití canonical zpětné lomítko), je normalizovat.</span><span class="sxs-lookup"><span data-stu-id="f92aa-240">Unless the path starts exactly with `\\?\` (note the use of the canonical backslash), it is normalized.</span></span>

<span data-ttu-id="f92aa-241">Proč byste měli přeskočit normalizace?</span><span class="sxs-lookup"><span data-stu-id="f92aa-241">Why would you want to skip normalization?</span></span> <span data-ttu-id="f92aa-242">Existují tři hlavní důvody:</span><span class="sxs-lookup"><span data-stu-id="f92aa-242">There are three major reasons:</span></span>

1. <span data-ttu-id="f92aa-243">Chcete-li získat přístup k cesty, které jsou běžně k dispozici, ale jsou platné.</span><span class="sxs-lookup"><span data-stu-id="f92aa-243">To get access to paths that are normally unavailable but are legal.</span></span> <span data-ttu-id="f92aa-244">Soubor nebo adresář volá `hidden.`, například je možné pro přístup k žádným jiným způsobem.</span><span class="sxs-lookup"><span data-stu-id="f92aa-244">A file or directory called `hidden.`, for example, is impossible to access in any other way.</span></span> 

1. <span data-ttu-id="f92aa-245">Ke zlepšení výkonu přeskočením normalizace, pokud jste již normalizovat.</span><span class="sxs-lookup"><span data-stu-id="f92aa-245">To improve performance by skipping normalization if you've already normalized.</span></span>

1. <span data-ttu-id="f92aa-246">Na rozhraní .NET Framework pouze, přejděte `MAX_PATH` vyhledat délka cesty umožňující cesty, které jsou větší než 259 znaků.</span><span class="sxs-lookup"><span data-stu-id="f92aa-246">On the .NET Framework only, to skip the `MAX_PATH` check for path length to allow for paths that are greater than 259 characters.</span></span> <span data-ttu-id="f92aa-247">Většina rozhraní API povolí tato, s několika výjimkami.</span><span class="sxs-lookup"><span data-stu-id="f92aa-247">Most APIs allow this, with some exceptions.</span></span>

> [!NOTE]
> <span data-ttu-id="f92aa-248">.NET core implicitně zpracovává dlouhé cesty a nebude provádět `MAX_PATH` zkontrolovat.</span><span class="sxs-lookup"><span data-stu-id="f92aa-248">.NET Core handles long paths implicitly and does not perform a `MAX_PATH` check.</span></span> <span data-ttu-id="f92aa-249">`MAX_PATH` Kontrola platí, pouze na rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="f92aa-249">The `MAX_PATH` check applies only to the .NET Framework.</span></span>

<span data-ttu-id="f92aa-250">Přeskočení kontroly cesta normalizace a maximální počet je jediným rozdílem mezi syntaxe cesty dvě zařízení; jinak jsou identické.</span><span class="sxs-lookup"><span data-stu-id="f92aa-250">Skipping normalization and max path checks is the only difference between the two device path syntaxes; they are otherwise identical.</span></span> <span data-ttu-id="f92aa-251">Buďte opatrní při přeskočení normalizace, protože můžete snadno vytvořit cesty, které jsou pro "normální" aplikace řešit složité používání.</span><span class="sxs-lookup"><span data-stu-id="f92aa-251">Be careful with skipping normalization, since you can easily create paths that are difficult for "normal" applications to deal with.</span></span>

<span data-ttu-id="f92aa-252">Cesty, které začínají `\\?\` jsou stále normalized, pokud jim explicitně předávat [GetFullPathName funkce](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).</span><span class="sxs-lookup"><span data-stu-id="f92aa-252">Paths that start with `\\?\` are still normalized if you explicitly pass them to the [GetFullPathName function](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea).</span></span>

<span data-ttu-id="f92aa-253">Všimněte si, že je možné cesty více než `MAX_PATH` znaků [GetFullPathName](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) bez `\\?\`.</span><span class="sxs-lookup"><span data-stu-id="f92aa-253">Note that you can paths of more than `MAX_PATH` characters to [GetFullPathName](/windows/desktop/api/fileapi/nf-fileapi-getfullpathnamea) without `\\?\`.</span></span> <span data-ttu-id="f92aa-254">Podporuje libovolné délky cesty až maximální velikost řetězce, který dokáže zpracovat Windows.</span><span class="sxs-lookup"><span data-stu-id="f92aa-254">It supports arbitrary length paths up to the maximum string size that Windows can handle.</span></span>

## <a name="case-and-the-windows-file-system"></a><span data-ttu-id="f92aa-255">Případ a systému souborů Windows</span><span class="sxs-lookup"><span data-stu-id="f92aa-255">Case and the Windows file system</span></span>

<span data-ttu-id="f92aa-256">Zvláštností systému souborů Windows, který není Windows uživatele a vývojáře najít matoucí této cesty a názvy adresářů jsou malá a velká písmena.</span><span class="sxs-lookup"><span data-stu-id="f92aa-256">A peculiarity of the Windows file system that non-Windows users and developers find confusing is that path and directory names are case-insensitive.</span></span> <span data-ttu-id="f92aa-257">To znamená adresáře a souboru názvy odrážet malých a velkých písmen řetězců používané při jejich vytváření.</span><span class="sxs-lookup"><span data-stu-id="f92aa-257">That is, directory and file names reflect the casing of the strings used when they are created.</span></span> <span data-ttu-id="f92aa-258">Například volání metody</span><span class="sxs-lookup"><span data-stu-id="f92aa-258">For example, the method call</span></span>

```csharp
Directory.Create("TeStDiReCtOrY");
```

```vb
Directory.Create("TeStDiReCtOrY")
```

<span data-ttu-id="f92aa-259">Vytvoří adresář s názvem TeStDiReCtOrY.</span><span class="sxs-lookup"><span data-stu-id="f92aa-259">creates a directory named TeStDiReCtOrY.</span></span> <span data-ttu-id="f92aa-260">Pokud přejmenujete adresář nebo soubor změnit jeho velikost písmen, názvu adresáře nebo souboru odráží případ řetězec použitý při jeho přejmenování.</span><span class="sxs-lookup"><span data-stu-id="f92aa-260">If you rename a directory or file to change its case, the directory or file name reflects the case of the string used when you rename it.</span></span> <span data-ttu-id="f92aa-261">Následující kód například přejmenuje soubor s názvem test.txt do Test.txt:</span><span class="sxs-lookup"><span data-stu-id="f92aa-261">For example, the following code renames a file named test.txt to Test.txt:</span></span>

[!code-csharp[case-and-renaming](~/samples/snippets/standard/io/file-names/cs/rename.cs)]
[!code-vb[case-and-renaming](~/samples/snippets/standard/io/file-names/vb/rename.vb)]

<span data-ttu-id="f92aa-262">Porovnání název adresáře a souboru jsou však malá a velká písmena.</span><span class="sxs-lookup"><span data-stu-id="f92aa-262">However, directory and file name comparisons are case-insensitive.</span></span> <span data-ttu-id="f92aa-263">Je-li vyhledat soubor s názvem "test.txt" rozhraní API pro .NET systému souborů ignorovat velikost písmen při porovnání.</span><span class="sxs-lookup"><span data-stu-id="f92aa-263">If you search for a file named "test.txt", .NET file system APIs ignore case in the comparison.</span></span> <span data-ttu-id="f92aa-264">Test.txt, testu. TXT, testu. "Test.txt" bude odpovídat TXT a jinou kombinaci velkých a malých písmen.</span><span class="sxs-lookup"><span data-stu-id="f92aa-264">Test.txt, TEST.TXT, test.TXT, and any other combination of upper- and lowercase letters will match "test.txt".</span></span>
