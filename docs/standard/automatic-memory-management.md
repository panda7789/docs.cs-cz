---
title: Automatická správa paměti
ms.date: 03/30/2017
ms.technology: dotnet-standard
helpviewer_keywords:
- garbage collection, automatic memory management
- memory, allocating
- memory, automatic memory management
- memory, releasing
- common language runtime, automatic memory management
- automatic memory management
- managed heap
- runtime, automatic memory management
ms.assetid: d4850de5-fa63-4936-a250-5678d118acba
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: e343d48b5e50fdaef3a3667f066894dea03eeb80
ms.sourcegitcommit: 76a304c79a32aa13889ebcf4b9789a4542b48e3e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/13/2018
ms.locfileid: "45519640"
---
# <a name="automatic-memory-management"></a><span data-ttu-id="ff247-102">Automatická správa paměti</span><span class="sxs-lookup"><span data-stu-id="ff247-102">Automatic Memory Management</span></span>
<span data-ttu-id="ff247-103">Automatická správa paměti je jedna ze služeb, které poskytuje modul Common Language Runtime během [Managed Execution](../../docs/standard/managed-execution-process.md).</span><span class="sxs-lookup"><span data-stu-id="ff247-103">Automatic memory management is one of the services that the Common Language Runtime provides during [Managed Execution](../../docs/standard/managed-execution-process.md).</span></span> <span data-ttu-id="ff247-104">Modul Common Language Runtime systému uvolňování paměti spravuje přidělování a uvolňování paměti pro aplikaci.</span><span class="sxs-lookup"><span data-stu-id="ff247-104">The Common Language Runtime's garbage collector manages the allocation and release of memory for an application.</span></span> <span data-ttu-id="ff247-105">Pro vývojáře to znamená, že není nutné napsat kód k provedení paměti, že se úlohy správy počítačů při vývoji spravované aplikace.</span><span class="sxs-lookup"><span data-stu-id="ff247-105">For developers, this means that you do not have to write code to perform memory management tasks when you develop managed applications.</span></span> <span data-ttu-id="ff247-106">Automatická správa paměti může eliminovat běžné problémy, jako je například zapomínání uvolnění objektu a způsobuje nevrácení paměti nebo pokusu o přístup k paměti pro objekt, který již byl uvolněn.</span><span class="sxs-lookup"><span data-stu-id="ff247-106">Automatic memory management can eliminate common problems, such as forgetting to free an object and causing a memory leak, or attempting to access memory for an object that has already been freed.</span></span> <span data-ttu-id="ff247-107">Tato část popisuje, jak systému uvolňování paměti přiděluje a uvolňuje paměť.</span><span class="sxs-lookup"><span data-stu-id="ff247-107">This section describes how the garbage collector allocates and releases memory.</span></span>  
  
## <a name="allocating-memory"></a><span data-ttu-id="ff247-108">Přidělování paměti</span><span class="sxs-lookup"><span data-stu-id="ff247-108">Allocating Memory</span></span>  
 <span data-ttu-id="ff247-109">Při inicializaci nový proces, modul runtime rezervuje souvislé oblastí adresního prostoru procesu.</span><span class="sxs-lookup"><span data-stu-id="ff247-109">When you initialize a new process, the runtime reserves a contiguous region of address space for the process.</span></span> <span data-ttu-id="ff247-110">Tato vyhrazeného adresního prostoru se nazývá spravovaná halda.</span><span class="sxs-lookup"><span data-stu-id="ff247-110">This reserved address space is called the managed heap.</span></span> <span data-ttu-id="ff247-111">Spravovaná halda uchovává ukazatel na adresu, kde bude přidělena další objekt v haldě.</span><span class="sxs-lookup"><span data-stu-id="ff247-111">The managed heap maintains a pointer to the address where the next object in the heap will be allocated.</span></span> <span data-ttu-id="ff247-112">This – ukazatel na začátku je nastavena na spravované haldě základní adresa.</span><span class="sxs-lookup"><span data-stu-id="ff247-112">Initially, this pointer is set to the managed heap's base address.</span></span> <span data-ttu-id="ff247-113">Všechny [referenční typy](../../docs/standard/base-types/common-type-system.md) jsou přidělovány na spravované haldě.</span><span class="sxs-lookup"><span data-stu-id="ff247-113">All [reference types](../../docs/standard/base-types/common-type-system.md) are allocated on the managed heap.</span></span> <span data-ttu-id="ff247-114">Pokud aplikace vytvoří první typ odkazu, je paměť přidělena typu spravované haldě na základní adrese.</span><span class="sxs-lookup"><span data-stu-id="ff247-114">When an application creates the first reference type, memory is allocated for the type at the base address of the managed heap.</span></span> <span data-ttu-id="ff247-115">Aplikace vytvoří další objekt, systému uvolňování paměti přiděluje paměť pro něj v adresním prostoru, hned za první objekt.</span><span class="sxs-lookup"><span data-stu-id="ff247-115">When the application creates the next object, the garbage collector allocates memory for it in the address space immediately following the first object.</span></span> <span data-ttu-id="ff247-116">Adresní prostor je k dispozici, pokračuje v systému uvolňování paměti k přidělení místa pro nové objekty tímto způsobem.</span><span class="sxs-lookup"><span data-stu-id="ff247-116">As long as address space is available, the garbage collector continues to allocate space for new objects in this manner.</span></span>  
  
 <span data-ttu-id="ff247-117">Přidělování paměti ze spravované haldy je rychlejší než nespravovanou paměť přidělení.</span><span class="sxs-lookup"><span data-stu-id="ff247-117">Allocating memory from the managed heap is faster than unmanaged memory allocation.</span></span> <span data-ttu-id="ff247-118">Vzhledem k tomu, že modul runtime přiděluje paměť pro objekt přidáním hodnoty na ukazatel, je skoro tak rychle jako přidělování paměti ze zásobníku.</span><span class="sxs-lookup"><span data-stu-id="ff247-118">Because the runtime allocates memory for an object by adding a value to a pointer, it is almost as fast as allocating memory from the stack.</span></span> <span data-ttu-id="ff247-119">Navíc vzhledem k tomu, že nové objekty, které jsou přiděleny po sobě jdoucích jsou uložena souvisle ve spravované haldě, aplikace může přístup k objektům velmi rychle.</span><span class="sxs-lookup"><span data-stu-id="ff247-119">In addition, because new objects that are allocated consecutively are stored contiguously in the managed heap, an application can access the objects very quickly.</span></span>  
  
<a name="cpconautomaticmemorymanagementreleasingmemoryanchor1"></a>   
## <a name="releasing-memory"></a><span data-ttu-id="ff247-120">Uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="ff247-120">Releasing Memory</span></span>  
 <span data-ttu-id="ff247-121">Optimalizující modul systému uvolňování paměti určuje nejvhodnější čas k provedení uvolnění podle přidělení prováděné.</span><span class="sxs-lookup"><span data-stu-id="ff247-121">The garbage collector's optimizing engine determines the best time to perform a collection based on the allocations being made.</span></span> <span data-ttu-id="ff247-122">Když uvolňování paměti provede kolekce, uvolní paměť pro objekty, které jsou již nejsou déle používány aplikací.</span><span class="sxs-lookup"><span data-stu-id="ff247-122">When the garbage collector performs a collection, it releases the memory for objects that are no longer being used by the application.</span></span> <span data-ttu-id="ff247-123">Určuje, které objekty jsou již nejsou déle používány prozkoumáním kořenů aplikace.</span><span class="sxs-lookup"><span data-stu-id="ff247-123">It determines which objects are no longer being used by examining the application's roots.</span></span> <span data-ttu-id="ff247-124">Každá aplikace má sadu kořenových adresářů.</span><span class="sxs-lookup"><span data-stu-id="ff247-124">Every application has a set of roots.</span></span> <span data-ttu-id="ff247-125">Kořenovém adresáři každého odkazuje na objekt na spravované haldě nebo je nastaven na hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="ff247-125">Each root either refers to an object on the managed heap or is set to null.</span></span> <span data-ttu-id="ff247-126">Kořeny aplikace zahrnují statická pole, místní proměnné a parametry v zásobníku vlákna a registruje procesor.</span><span class="sxs-lookup"><span data-stu-id="ff247-126">An application's roots include static fields, local variables and parameters on a thread's stack, and CPU registers.</span></span> <span data-ttu-id="ff247-127">Uvolňování paměti má přístup k seznamu aktivní kořenových adresářů, který [kompilátor just-in-time (JIT)](../../docs/standard/managed-execution-process.md) a udržovat modulu runtime.</span><span class="sxs-lookup"><span data-stu-id="ff247-127">The garbage collector has access to the list of active roots that the [just-in-time (JIT) compiler](../../docs/standard/managed-execution-process.md) and the runtime maintain.</span></span> <span data-ttu-id="ff247-128">Pomocí tohoto seznamu, prověří kořeny aplikace a vytvoří graf, který obsahuje všechny objekty, které jsou dostupné z kořenů v procesu.</span><span class="sxs-lookup"><span data-stu-id="ff247-128">Using this list, it examines an application's roots, and in the process creates a graph that contains all the objects that are reachable from the roots.</span></span>  
  
 <span data-ttu-id="ff247-129">Objekty, které nejsou v grafu jsou dostupná z kořenové adresáře aplikace.</span><span class="sxs-lookup"><span data-stu-id="ff247-129">Objects that are not in the graph are unreachable from the application's roots.</span></span> <span data-ttu-id="ff247-130">Uvolňování paměti bere v úvahu nedostupné objekty uvolnění paměti a vydá je paměť přidělená pro ně.</span><span class="sxs-lookup"><span data-stu-id="ff247-130">The garbage collector considers unreachable objects garbage and will release the memory allocated for them.</span></span> <span data-ttu-id="ff247-131">Během shromažďování uvolňování prozkoumá spravované haldě, hledají adresní prostor obsazena nedostupné objekty.</span><span class="sxs-lookup"><span data-stu-id="ff247-131">During a collection, the garbage collector examines the managed heap, looking for the blocks of address space occupied by unreachable objects.</span></span> <span data-ttu-id="ff247-132">Jako nedostupný objekt zjistí, používá funkci kopírování paměti ke komprimaci dostupný objektů v paměti se bloky adresní prostory, které jsou přiděleny do nedostupný objektů.</span><span class="sxs-lookup"><span data-stu-id="ff247-132">As it discovers each unreachable object, it uses a memory-copying function to compact the reachable objects in memory, freeing up the blocks of address spaces allocated to unreachable objects.</span></span> <span data-ttu-id="ff247-133">Jakmile setřepána paměť pro dostupné objekty, uvolňování paměti provede opravy nutné ukazatel tak, aby aplikace kořeny odkazují na objekty v jejich novém umístění.</span><span class="sxs-lookup"><span data-stu-id="ff247-133">Once the memory for the reachable objects has been compacted, the garbage collector makes the necessary pointer corrections so that the application's roots point to the objects in their new locations.</span></span> <span data-ttu-id="ff247-134">Také umístí spravované haldě ukazatel po poslední dostupný objekt.</span><span class="sxs-lookup"><span data-stu-id="ff247-134">It also positions the managed heap's pointer after the last reachable object.</span></span> <span data-ttu-id="ff247-135">Všimněte si že paměť je komprimovat pouze v případě, že zjistí velký počet nedostupné objekty kolekce.</span><span class="sxs-lookup"><span data-stu-id="ff247-135">Note that memory is compacted only if a collection discovers a significant number of unreachable objects.</span></span> <span data-ttu-id="ff247-136">Pokud všechny objekty ve spravované haldě překonání kolekci, není nutné pro komprimaci paměti.</span><span class="sxs-lookup"><span data-stu-id="ff247-136">If all the objects in the managed heap survive a collection, then there is no need for memory compaction.</span></span>  
  
 <span data-ttu-id="ff247-137">Pokud chcete zlepšit výkon, modul runtime přiděluje paměť pro velké objekty v samostatných haldy.</span><span class="sxs-lookup"><span data-stu-id="ff247-137">To improve performance, the runtime allocates memory for large objects in a separate heap.</span></span> <span data-ttu-id="ff247-138">Uvolňování paměti automaticky uvolní paměť pro velké objekty.</span><span class="sxs-lookup"><span data-stu-id="ff247-138">The garbage collector automatically releases the memory for large objects.</span></span> <span data-ttu-id="ff247-139">Abyste se vyhnuli přenášení velkých objektů v paměti, ale tato paměť není setřepána.</span><span class="sxs-lookup"><span data-stu-id="ff247-139">However, to avoid moving large objects in memory, this memory is not compacted.</span></span>  
  
## <a name="generations-and-performance"></a><span data-ttu-id="ff247-140">Generace a výkon</span><span class="sxs-lookup"><span data-stu-id="ff247-140">Generations and Performance</span></span>  
 <span data-ttu-id="ff247-141">Za účelem optimalizace výkonu systému uvolňování paměti spravované haldy je rozdělen na tři generace: 0, 1 a 2.</span><span class="sxs-lookup"><span data-stu-id="ff247-141">To optimize the performance of the garbage collector, the managed heap is divided into three generations: 0, 1, and 2.</span></span> <span data-ttu-id="ff247-142">Modul runtime uvolňování paměti kolekce algoritmus je založená na několik generalizace, které softwarovém průmyslu počítač objevil na hodnotu true Experimentováním s uvolňování paměti kolekce schémat.</span><span class="sxs-lookup"><span data-stu-id="ff247-142">The runtime's garbage collection algorithm is based on several generalizations that the computer software industry has discovered to be true by experimenting with garbage collection schemes.</span></span> <span data-ttu-id="ff247-143">Nejprve je rychlejší ke komprimaci paměti pro část spravované haldy než pro celou spravovanou haldu.</span><span class="sxs-lookup"><span data-stu-id="ff247-143">First, it is faster to compact the memory for a portion of the managed heap than for the entire managed heap.</span></span> <span data-ttu-id="ff247-144">Za druhé novější objekty se mají životnost kratší a starší objekty se mají delší životnost.</span><span class="sxs-lookup"><span data-stu-id="ff247-144">Secondly, newer objects will have shorter lifetimes and older objects will have longer lifetimes.</span></span> <span data-ttu-id="ff247-145">A konečně, novější objekty mají tendenci vzájemně souvisí a přistupuje aplikace přibližně ve stejnou dobu.</span><span class="sxs-lookup"><span data-stu-id="ff247-145">Lastly, newer objects tend to be related to each other and accessed by the application around the same time.</span></span>  
  
 <span data-ttu-id="ff247-146">Modul runtime systému uvolňování paměti uloží nové objekty 0. generace.</span><span class="sxs-lookup"><span data-stu-id="ff247-146">The runtime's garbage collector stores new objects in generation 0.</span></span> <span data-ttu-id="ff247-147">Objekty vytvořené v rané fázi životního cyklu aplikace, které byly zachovány při kolekce jsou povýšeny a uloženy v generace 1 a 2.</span><span class="sxs-lookup"><span data-stu-id="ff247-147">Objects created early in the application's lifetime that survive collections are promoted and stored in generations 1 and 2.</span></span> <span data-ttu-id="ff247-148">Proces povýšení objektu je popsána dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="ff247-148">The process of object promotion is described later in this topic.</span></span> <span data-ttu-id="ff247-149">Protože je rychlejší compact část spravované haldy než celý haldy, toto schéma umožňuje uvolňování paměti uvolní paměť při konkrétní generování spíše než uvolnění paměti pro celou spravovanou haldu pokaždé, když provádí kolekce.</span><span class="sxs-lookup"><span data-stu-id="ff247-149">Because it is faster to compact a portion of the managed heap than the entire heap, this scheme allows the garbage collector to release the memory in a specific generation rather than release the memory for the entire managed heap each time it performs a collection.</span></span>  
  
 <span data-ttu-id="ff247-150">Ve skutečnosti provádí systému uvolňování paměti kolekce při zaplnění 0. generace.</span><span class="sxs-lookup"><span data-stu-id="ff247-150">In reality, the garbage collector performs a collection when generation 0 is full.</span></span> <span data-ttu-id="ff247-151">Pokud se aplikace pokusí vytvořit nový objekt při zaplnění 0. generace, uvolňování paměti zjistí, že neexistuje žádný adresní prostor zbývajících v generaci 0 pro přidělení objektu.</span><span class="sxs-lookup"><span data-stu-id="ff247-151">If an application attempts to create a new object when generation 0 is full, the garbage collector discovers that there is no address space remaining in generation 0 to allocate for the object.</span></span> <span data-ttu-id="ff247-152">Uvolňování paměti kolekce provádí při pokusu o uvolnění adresní prostor v generaci 0 pro objekt.</span><span class="sxs-lookup"><span data-stu-id="ff247-152">The garbage collector performs a collection in an attempt to free address space in generation 0 for the object.</span></span> <span data-ttu-id="ff247-153">Uvolňování paměti spustí porovnáním objektů v generaci 0, ne všechny objekty ve spravované haldě.</span><span class="sxs-lookup"><span data-stu-id="ff247-153">The garbage collector starts by examining the objects in generation 0 rather than all objects in the managed heap.</span></span> <span data-ttu-id="ff247-154">Toto je nejefektivnější postup vzhledem k tomu, že nové objekty jsou obvykle krátkou životnost a očekává se, že mnoho objektů v generaci 0 již nebude používán aplikací při kolekce se provádí.</span><span class="sxs-lookup"><span data-stu-id="ff247-154">This is the most efficient approach, because new objects tend to have short lifetimes, and it is expected that many of the objects in generation 0 will no longer be in use by the application when a collection is performed.</span></span> <span data-ttu-id="ff247-155">Sběr generace 0 samostatně navíc uvolňuje často dostatek paměti, aby aplikace mohla pokračovat ve vytváření nových objektů.</span><span class="sxs-lookup"><span data-stu-id="ff247-155">In addition, a collection of generation 0 alone often reclaims enough memory to allow the application to continue creating new objects.</span></span>  
  
 <span data-ttu-id="ff247-156">Po uvolňování paměti provádí sběr generace 0, komprimuje paměť pro dostupné objekty jak je vysvětleno v [uvolňování paměti](#cpconautomaticmemorymanagementreleasingmemoryanchor1) výše v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="ff247-156">After the garbage collector performs a collection of generation 0, it compacts the memory for the reachable objects as explained in [Releasing Memory](#cpconautomaticmemorymanagementreleasingmemoryanchor1) earlier in this topic.</span></span> <span data-ttu-id="ff247-157">Uvolňování paměti pak podporuje tyto objekty a bere v úvahu tuto část 1. generace spravované haldě.</span><span class="sxs-lookup"><span data-stu-id="ff247-157">The garbage collector then promotes these objects and considers this portion of the managed heap generation 1.</span></span> <span data-ttu-id="ff247-158">Protože objekty, které byly zachovány při kolekce jsou často mají delší životnost, má smysl pro povýšení na vyšší generace.</span><span class="sxs-lookup"><span data-stu-id="ff247-158">Because objects that survive collections tend to have longer lifetimes, it makes sense to promote them to a higher generation.</span></span> <span data-ttu-id="ff247-159">Uvolňování paměti v důsledku toho není nutné prozkoumat objekty v generacích 1 a 2 pokaždé, když provádí sběr generace 0.</span><span class="sxs-lookup"><span data-stu-id="ff247-159">As a result, the garbage collector does not have to reexamine the objects in generations 1 and 2 each time it performs a collection of generation 0.</span></span>  
  
 <span data-ttu-id="ff247-160">Poté, co systému uvolňování paměti provede jeho první kolekce generace 0 a podporuje dostupné objekty do 1. generace, bude považovat za zbývající spravované haldě generace 0.</span><span class="sxs-lookup"><span data-stu-id="ff247-160">After the garbage collector performs its first collection of generation 0 and promotes the reachable objects to generation 1, it considers the remainder of the managed heap generation 0.</span></span> <span data-ttu-id="ff247-161">Pokračuje se přidělit paměť pro nové objekty v generaci 0 až 0. generace je plný a je potřeba provést jiné kolekce.</span><span class="sxs-lookup"><span data-stu-id="ff247-161">It continues to allocate memory for new objects in generation 0 until generation 0 is full and it is necessary to perform another collection.</span></span> <span data-ttu-id="ff247-162">Optimalizující modul systému uvolňování paměti v tomto okamžiku Určuje, zda je nutné prozkoumat objekty v generacích starší.</span><span class="sxs-lookup"><span data-stu-id="ff247-162">At this point, the garbage collector's optimizing engine determines whether it is necessary to examine the objects in older generations.</span></span> <span data-ttu-id="ff247-163">Například pokud kolekce generace 0 nezíská dostatek paměti pro aplikaci pro úspěšné dokončení jeho pokusu o vytvoření nového objektu, můžete provádět systému uvolňování paměti kolekce generace 1 a pak 2. generace.</span><span class="sxs-lookup"><span data-stu-id="ff247-163">For example, if a collection of generation 0 does not reclaim enough memory for the application to successfully complete its attempt to create a new object, the garbage collector can perform a collection of generation 1, then generation 2.</span></span> <span data-ttu-id="ff247-164">Pokud to nezíská dostatek paměti, můžete provádět systému uvolňování paměti kolekce generace 2 a 1, 0.</span><span class="sxs-lookup"><span data-stu-id="ff247-164">If this does not reclaim enough memory, the garbage collector can perform a collection of generations 2, 1, and 0.</span></span> <span data-ttu-id="ff247-165">Za každou kolekci systému uvolňování paměti komprimuje dostupné objekty v generaci 0 a podporuje je 1. generace.</span><span class="sxs-lookup"><span data-stu-id="ff247-165">After each collection, the garbage collector compacts the reachable objects in generation 0 and promotes them to generation 1.</span></span> <span data-ttu-id="ff247-166">Objekty v 1. generace, které byly zachovány při kolekce jsou povýšeny do generace 2.</span><span class="sxs-lookup"><span data-stu-id="ff247-166">Objects in generation 1 that survive collections are promoted to generation 2.</span></span> <span data-ttu-id="ff247-167">Protože systému uvolňování paměti se podporuje jenom tři generace, objekty v 2. generace, které byly zachovány při kolekci zůstanou v generaci 2, dokud se určují nedostupná v budoucnu kolekci.</span><span class="sxs-lookup"><span data-stu-id="ff247-167">Because the garbage collector supports only three generations, objects in generation 2 that survive a collection remain in generation 2 until they are determined to be unreachable in a future collection.</span></span>  
  
## <a name="releasing-memory-for-unmanaged-resources"></a><span data-ttu-id="ff247-168">Uvolňování paměti pro nespravované prostředky</span><span class="sxs-lookup"><span data-stu-id="ff247-168">Releasing Memory for Unmanaged Resources</span></span>  
 <span data-ttu-id="ff247-169">U většiny objektů, které vytváří vaše aplikace můžete se spolehnout na uvolňování paměti automaticky provádět úlohy správy dost paměti.</span><span class="sxs-lookup"><span data-stu-id="ff247-169">For the majority of the objects that your application creates, you can rely on the garbage collector to automatically perform the necessary memory management tasks.</span></span> <span data-ttu-id="ff247-170">Nespravované prostředky však vyžaduje explicitní vyčištění.</span><span class="sxs-lookup"><span data-stu-id="ff247-170">However, unmanaged resources require explicit cleanup.</span></span> <span data-ttu-id="ff247-171">Nejběžnějším typem nespravovaného prostředku je objekt, který obaluje prostředek operačního systému, například popisovač souboru, popisovač okna nebo připojení k síti.</span><span class="sxs-lookup"><span data-stu-id="ff247-171">The most common type of unmanaged resource is an object that wraps an operating system resource, such as a file handle, window handle, or network connection.</span></span> <span data-ttu-id="ff247-172">I když uvolňování paměti je schopen sledovat dobu života spravovaný objekt, který zapouzdřuje nespravovaný prostředek, nemá specifické znalosti o tom, jak prostředek vyčistit.</span><span class="sxs-lookup"><span data-stu-id="ff247-172">Although the garbage collector is able to track the lifetime of a managed object that encapsulates an unmanaged resource, it does not have specific knowledge about how to clean up the resource.</span></span> <span data-ttu-id="ff247-173">Při vytváření objektu, který zapouzdřuje nespravovaný prostředek, doporučuje se, že zadáte potřebný kód pro vyčištění nespravovaných prostředků ve veřejném **Dispose** metody.</span><span class="sxs-lookup"><span data-stu-id="ff247-173">When you create an object that encapsulates an unmanaged resource, it is recommended that you provide the necessary code to clean up the unmanaged resource in a public **Dispose** method.</span></span> <span data-ttu-id="ff247-174">Tím, že poskytuje **Dispose** metoda, povolíte uživatelům objektu explicitně uvolnit jeho paměti po ukončení práce s objektem.</span><span class="sxs-lookup"><span data-stu-id="ff247-174">By providing a **Dispose** method, you enable users of your object to explicitly free its memory when they are finished with the object.</span></span> <span data-ttu-id="ff247-175">Použijete-li objekt, který zapouzdřuje nespravovaný prostředek, byste měli vědět **Dispose** a volejte jej podle potřeby.</span><span class="sxs-lookup"><span data-stu-id="ff247-175">When you use an object that encapsulates an unmanaged resource, you should be aware of **Dispose** and call it as necessary.</span></span> <span data-ttu-id="ff247-176">Další informace o vyčištění nespravovaných prostředků a tu je ukázka návrhový vzor pro implementování **Dispose**, naleznete v tématu [uvolňování](../../docs/standard/garbage-collection/index.md).</span><span class="sxs-lookup"><span data-stu-id="ff247-176">For more information about cleaning up unmanaged resources and an example of a design pattern for implementing **Dispose**, see [Garbage Collection](../../docs/standard/garbage-collection/index.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="ff247-177">Viz také:</span><span class="sxs-lookup"><span data-stu-id="ff247-177">See also</span></span>

- <xref:System.GC>  
- [<span data-ttu-id="ff247-178">Uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="ff247-178">Garbage Collection</span></span>](../../docs/standard/garbage-collection/index.md)  
- [<span data-ttu-id="ff247-179">Proces spravovaného spuštění</span><span class="sxs-lookup"><span data-stu-id="ff247-179">Managed Execution Process</span></span>](../../docs/standard/managed-execution-process.md)
