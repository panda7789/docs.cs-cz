---
title: Automatická správa paměti
ms.date: 03/30/2017
ms.technology: dotnet-standard
helpviewer_keywords:
- garbage collection, automatic memory management
- memory, allocating
- memory, automatic memory management
- memory, releasing
- common language runtime, automatic memory management
- automatic memory management
- managed heap
- runtime, automatic memory management
ms.assetid: d4850de5-fa63-4936-a250-5678d118acba
ms.openlocfilehash: 1038f16dca507e58005189c9558a9ec8dae4b34f
ms.sourcegitcommit: 00aa62e2f469c2272a457b04e66b4cc3c97a800b
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 02/28/2020
ms.locfileid: "78159699"
---
# <a name="automatic-memory-management"></a><span data-ttu-id="9b649-102">Automatická správa paměti</span><span class="sxs-lookup"><span data-stu-id="9b649-102">Automatic Memory Management</span></span>
<span data-ttu-id="9b649-103">Automatická správa paměti je jednou ze služeb, které modul CLR (Common Language Runtime) poskytuje během [spravovaného spuštění](../../docs/standard/managed-execution-process.md).</span><span class="sxs-lookup"><span data-stu-id="9b649-103">Automatic memory management is one of the services that the Common Language Runtime provides during [Managed Execution](../../docs/standard/managed-execution-process.md).</span></span> <span data-ttu-id="9b649-104">Systém uvolňování paměti modulu CLR (Common Language Runtime) spravuje přidělování a uvolňování paměti pro aplikaci.</span><span class="sxs-lookup"><span data-stu-id="9b649-104">The Common Language Runtime's garbage collector manages the allocation and release of memory for an application.</span></span> <span data-ttu-id="9b649-105">Pro vývojáře to znamená, že nemusíte psát kód pro provádění úloh správy paměti při vývoji spravovaných aplikací.</span><span class="sxs-lookup"><span data-stu-id="9b649-105">For developers, this means that you do not have to write code to perform memory management tasks when you develop managed applications.</span></span> <span data-ttu-id="9b649-106">Automatická správa paměti může eliminovat běžné problémy, například forgetting k uvolnění objektu a příčinou nevrácení paměti nebo pokusu o přístup k paměti pro objekt, který již byl uvolněn.</span><span class="sxs-lookup"><span data-stu-id="9b649-106">Automatic memory management can eliminate common problems, such as forgetting to free an object and causing a memory leak, or attempting to access memory for an object that has already been freed.</span></span> <span data-ttu-id="9b649-107">Tato část popisuje, jak systém uvolňování paměti přiděluje a uvolňuje paměť.</span><span class="sxs-lookup"><span data-stu-id="9b649-107">This section describes how the garbage collector allocates and releases memory.</span></span>  
  
## <a name="allocating-memory"></a><span data-ttu-id="9b649-108">Přidělování paměti</span><span class="sxs-lookup"><span data-stu-id="9b649-108">Allocating Memory</span></span>  
 <span data-ttu-id="9b649-109">Při inicializaci nového procesu vyhradí modul runtime souvislou oblast adresního prostoru pro daný proces.</span><span class="sxs-lookup"><span data-stu-id="9b649-109">When you initialize a new process, the runtime reserves a contiguous region of address space for the process.</span></span> <span data-ttu-id="9b649-110">Tento vyhrazený adresní prostor se nazývá spravovaná halda.</span><span class="sxs-lookup"><span data-stu-id="9b649-110">This reserved address space is called the managed heap.</span></span> <span data-ttu-id="9b649-111">Spravovaná halda udržuje ukazatel na adresu, kde bude přidělen další objekt v haldě.</span><span class="sxs-lookup"><span data-stu-id="9b649-111">The managed heap maintains a pointer to the address where the next object in the heap will be allocated.</span></span> <span data-ttu-id="9b649-112">Zpočátku je tento ukazatel nastavený na základní adresu spravované haldy.</span><span class="sxs-lookup"><span data-stu-id="9b649-112">Initially, this pointer is set to the managed heap's base address.</span></span> <span data-ttu-id="9b649-113">Všechny [typy odkazů](../../docs/standard/base-types/common-type-system.md) jsou přiděleny na spravovanou haldu.</span><span class="sxs-lookup"><span data-stu-id="9b649-113">All [reference types](../../docs/standard/base-types/common-type-system.md) are allocated on the managed heap.</span></span> <span data-ttu-id="9b649-114">Když aplikace vytvoří první typ odkazu, přidělí se paměť pro typ na základní adrese spravované haldy.</span><span class="sxs-lookup"><span data-stu-id="9b649-114">When an application creates the first reference type, memory is allocated for the type at the base address of the managed heap.</span></span> <span data-ttu-id="9b649-115">Když aplikace vytvoří další objekt, systém uvolňování paměti přidělí paměť v adresním prostoru hned za prvním objektem.</span><span class="sxs-lookup"><span data-stu-id="9b649-115">When the application creates the next object, the garbage collector allocates memory for it in the address space immediately following the first object.</span></span> <span data-ttu-id="9b649-116">Pokud je k dispozici adresní prostor, uvolňování paměti pro nové objekty tímto způsobem stále přiděluje místo.</span><span class="sxs-lookup"><span data-stu-id="9b649-116">As long as address space is available, the garbage collector continues to allocate space for new objects in this manner.</span></span>  
  
 <span data-ttu-id="9b649-117">Přidělování paměti ze spravované haldy je rychlejší než nespravované přidělení paměti.</span><span class="sxs-lookup"><span data-stu-id="9b649-117">Allocating memory from the managed heap is faster than unmanaged memory allocation.</span></span> <span data-ttu-id="9b649-118">Vzhledem k tomu, že modul Runtime přiděluje paměť pro objekt přidáním hodnoty k ukazateli, je téměř stejně rychlá jako přidělování paměti ze zásobníku.</span><span class="sxs-lookup"><span data-stu-id="9b649-118">Because the runtime allocates memory for an object by adding a value to a pointer, it is almost as fast as allocating memory from the stack.</span></span> <span data-ttu-id="9b649-119">Kromě toho vzhledem k tomu, že nové objekty, které jsou přiděleny po sobě, jsou uloženy souvisle ve spravované haldě, aplikace může přistupovat k objektům velmi rychle.</span><span class="sxs-lookup"><span data-stu-id="9b649-119">In addition, because new objects that are allocated consecutively are stored contiguously in the managed heap, an application can access the objects very quickly.</span></span>  
  
<a name="cpconautomaticmemorymanagementreleasingmemoryanchor1"></a>
## <a name="releasing-memory"></a><span data-ttu-id="9b649-120">Uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="9b649-120">Releasing Memory</span></span>  
 <span data-ttu-id="9b649-121">Optimalizující modul systému uvolňování paměti určuje nejvhodnější čas k provedení kolekce na základě přidělení.</span><span class="sxs-lookup"><span data-stu-id="9b649-121">The garbage collector's optimizing engine determines the best time to perform a collection based on the allocations being made.</span></span> <span data-ttu-id="9b649-122">Když systém uvolňování paměti provede kolekci, uvolní paměť pro objekty, které již aplikace nepoužívá.</span><span class="sxs-lookup"><span data-stu-id="9b649-122">When the garbage collector performs a collection, it releases the memory for objects that are no longer being used by the application.</span></span> <span data-ttu-id="9b649-123">Určuje, které objekty již nejsou používány, prozkoumáním kořenů aplikace.</span><span class="sxs-lookup"><span data-stu-id="9b649-123">It determines which objects are no longer being used by examining the application's roots.</span></span> <span data-ttu-id="9b649-124">Každá aplikace má sadu kořenů.</span><span class="sxs-lookup"><span data-stu-id="9b649-124">Every application has a set of roots.</span></span> <span data-ttu-id="9b649-125">Každý kořenový adresář buď odkazuje na objekt na spravované haldě, nebo je nastaven na hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="9b649-125">Each root either refers to an object on the managed heap or is set to null.</span></span> <span data-ttu-id="9b649-126">Kořeny aplikace zahrnují statická pole, místní proměnné a parametry v zásobníku vlákna a Registry procesoru.</span><span class="sxs-lookup"><span data-stu-id="9b649-126">An application's roots include static fields, local variables and parameters on a thread's stack, and CPU registers.</span></span> <span data-ttu-id="9b649-127">Systém uvolňování paměti má přístup k seznamu aktivních kořenových adresářů, které [kompilátor JIT (just-in-time)](../../docs/standard/managed-execution-process.md) a modul runtime udržují.</span><span class="sxs-lookup"><span data-stu-id="9b649-127">The garbage collector has access to the list of active roots that the [just-in-time (JIT) compiler](../../docs/standard/managed-execution-process.md) and the runtime maintain.</span></span> <span data-ttu-id="9b649-128">Pomocí tohoto seznamu prohlíží kořeny aplikace a v procesu vytvoří graf, který obsahuje všechny objekty, které jsou dostupné z kořenů.</span><span class="sxs-lookup"><span data-stu-id="9b649-128">Using this list, it examines an application's roots, and in the process creates a graph that contains all the objects that are reachable from the roots.</span></span>  
  
 <span data-ttu-id="9b649-129">Objekty, které nejsou v grafu, jsou nedosažitelné z kořenů aplikace.</span><span class="sxs-lookup"><span data-stu-id="9b649-129">Objects that are not in the graph are unreachable from the application's roots.</span></span> <span data-ttu-id="9b649-130">Systém uvolňování paměti považuje nedosažitelné objekty za paměti a uvolní přidělenou paměť.</span><span class="sxs-lookup"><span data-stu-id="9b649-130">The garbage collector considers unreachable objects garbage and will release the memory allocated for them.</span></span> <span data-ttu-id="9b649-131">V průběhu kolekce systém uvolňování paměti ověřuje spravovanou haldu a hledá bloky adresního prostoru obsazené nedosažitelnými objekty.</span><span class="sxs-lookup"><span data-stu-id="9b649-131">During a collection, the garbage collector examines the managed heap, looking for the blocks of address space occupied by unreachable objects.</span></span> <span data-ttu-id="9b649-132">Vzhledem k tomu, že zjistí každý nedosažitelný objekt, používá funkci kopírování paměti pro komprimaci dosažitelných objektů v paměti a uvolní bloky adresních prostorů přidělených nedostupným objektům.</span><span class="sxs-lookup"><span data-stu-id="9b649-132">As it discovers each unreachable object, it uses a memory-copying function to compact the reachable objects in memory, freeing up the blocks of address spaces allocated to unreachable objects.</span></span> <span data-ttu-id="9b649-133">Po komprimaci paměti pro dostupné objekty provede systém uvolňování paměti nezbytné opravy ukazatelů, aby kořeny aplikace odkazovaly na objekty v jejich nových umístěních.</span><span class="sxs-lookup"><span data-stu-id="9b649-133">Once the memory for the reachable objects has been compacted, the garbage collector makes the necessary pointer corrections so that the application's roots point to the objects in their new locations.</span></span> <span data-ttu-id="9b649-134">Také umístí ukazatel spravované haldy za poslední dosažitelný objekt.</span><span class="sxs-lookup"><span data-stu-id="9b649-134">It also positions the managed heap's pointer after the last reachable object.</span></span> <span data-ttu-id="9b649-135">Všimněte si, že paměť je komprimována pouze v případě, že kolekce zjistí významný počet nedosažitelných objektů.</span><span class="sxs-lookup"><span data-stu-id="9b649-135">Note that memory is compacted only if a collection discovers a significant number of unreachable objects.</span></span> <span data-ttu-id="9b649-136">Pokud všechny objekty ve spravované haldě přestanou kolekci, nepotřebujete komprimaci paměti.</span><span class="sxs-lookup"><span data-stu-id="9b649-136">If all the objects in the managed heap survive a collection, then there is no need for memory compaction.</span></span>  
  
 <span data-ttu-id="9b649-137">Za účelem zvýšení výkonu přiděluje modul runtime paměť pro velké objekty v samostatné haldě.</span><span class="sxs-lookup"><span data-stu-id="9b649-137">To improve performance, the runtime allocates memory for large objects in a separate heap.</span></span> <span data-ttu-id="9b649-138">Systém uvolňování paměti automaticky uvolňuje paměť pro velké objekty.</span><span class="sxs-lookup"><span data-stu-id="9b649-138">The garbage collector automatically releases the memory for large objects.</span></span> <span data-ttu-id="9b649-139">Chcete-li však zabránit přesunutí velkých objektů do paměti, Tato paměť není komprimována.</span><span class="sxs-lookup"><span data-stu-id="9b649-139">However, to avoid moving large objects in memory, this memory is not compacted.</span></span>  
  
## <a name="generations-and-performance"></a><span data-ttu-id="9b649-140">Generace a výkon</span><span class="sxs-lookup"><span data-stu-id="9b649-140">Generations and Performance</span></span>  
 <span data-ttu-id="9b649-141">Pro optimalizaci výkonu uvolňování paměti je spravovaná halda rozdělená na tři generace: 0, 1 a 2.</span><span class="sxs-lookup"><span data-stu-id="9b649-141">To optimize the performance of the garbage collector, the managed heap is divided into three generations: 0, 1, and 2.</span></span> <span data-ttu-id="9b649-142">Algoritmus uvolňování paměti modulu runtime je založen na několika generalizech, které zjistila, že počítač softwarového softwaru je pravdivý, a to experimentováním se schématy uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="9b649-142">The runtime's garbage collection algorithm is based on several generalizations that the computer software industry has discovered to be true by experimenting with garbage collection schemes.</span></span> <span data-ttu-id="9b649-143">Nejdřív je rychlejší komprimace paměti pro část spravované haldy, než pro celou spravovanou haldu.</span><span class="sxs-lookup"><span data-stu-id="9b649-143">First, it is faster to compact the memory for a portion of the managed heap than for the entire managed heap.</span></span> <span data-ttu-id="9b649-144">Za druhé budou novější objekty kratší životnost a starší objekty budou mít delší životnost.</span><span class="sxs-lookup"><span data-stu-id="9b649-144">Secondly, newer objects will have shorter lifetimes and older objects will have longer lifetimes.</span></span> <span data-ttu-id="9b649-145">A konečně, novější objekty by měly být vzájemně vzájemně propojené a k nim budou mít k dispozici aplikace přibližně stejnou dobu.</span><span class="sxs-lookup"><span data-stu-id="9b649-145">Lastly, newer objects tend to be related to each other and accessed by the application around the same time.</span></span>  
  
 <span data-ttu-id="9b649-146">Systém uvolňování paměti modulu runtime ukládá nové objekty v generaci 0.</span><span class="sxs-lookup"><span data-stu-id="9b649-146">The runtime's garbage collector stores new objects in generation 0.</span></span> <span data-ttu-id="9b649-147">Objekty vytvořené na začátku v životním cyklu aplikace, které jsou převedené na kolekce, jsou povýšeny a uloženy v generacích 1 a 2.</span><span class="sxs-lookup"><span data-stu-id="9b649-147">Objects created early in the application's lifetime that survive collections are promoted and stored in generations 1 and 2.</span></span> <span data-ttu-id="9b649-148">Proces povýšení objektu je popsán dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="9b649-148">The process of object promotion is described later in this topic.</span></span> <span data-ttu-id="9b649-149">Vzhledem k tomu, že je rychlejší komprimovat část spravované haldy, než je celá halda, toto schéma umožňuje uvolňování paměti uvolnit paměť v konkrétní generaci místo uvolnění paměti pro celou spravovanou haldu pokaždé, když aplikace provede kolekci.</span><span class="sxs-lookup"><span data-stu-id="9b649-149">Because it is faster to compact a portion of the managed heap than the entire heap, this scheme allows the garbage collector to release the memory in a specific generation rather than release the memory for the entire managed heap each time it performs a collection.</span></span>  
  
 <span data-ttu-id="9b649-150">Ve skutečnosti systém uvolňování paměti provede kolekci, pokud je generace 0 plná.</span><span class="sxs-lookup"><span data-stu-id="9b649-150">In reality, the garbage collector performs a collection when generation 0 is full.</span></span> <span data-ttu-id="9b649-151">Pokud se aplikace pokusí vytvořit nový objekt, když je generace 0 plná, zjistí systém uvolňování paměti, že v generaci 0 není zbývající adresní prostor pro přidělení objektu.</span><span class="sxs-lookup"><span data-stu-id="9b649-151">If an application attempts to create a new object when generation 0 is full, the garbage collector discovers that there is no address space remaining in generation 0 to allocate for the object.</span></span> <span data-ttu-id="9b649-152">Systém uvolňování paměti provede kolekci v pokusu o uvolnění adresního prostoru v generaci 0 pro daný objekt.</span><span class="sxs-lookup"><span data-stu-id="9b649-152">The garbage collector performs a collection in an attempt to free address space in generation 0 for the object.</span></span> <span data-ttu-id="9b649-153">Systém uvolňování paměti začíná prozkoumáním objektů v generaci 0 místo všech objektů ve spravované haldě.</span><span class="sxs-lookup"><span data-stu-id="9b649-153">The garbage collector starts by examining the objects in generation 0 rather than all objects in the managed heap.</span></span> <span data-ttu-id="9b649-154">Jedná se o nejúčinnější přístup, protože nové objekty mají krátkou životnost a očekává se, že celá řada objektů v generaci 0 již nebude aplikací používána při provádění kolekce.</span><span class="sxs-lookup"><span data-stu-id="9b649-154">This is the most efficient approach, because new objects tend to have short lifetimes, and it is expected that many of the objects in generation 0 will no longer be in use by the application when a collection is performed.</span></span> <span data-ttu-id="9b649-155">Kromě toho kolekce samotné generace 0 často uvolňuje dostatek paměti, aby aplikace mohla pokračovat v vytváření nových objektů.</span><span class="sxs-lookup"><span data-stu-id="9b649-155">In addition, a collection of generation 0 alone often reclaims enough memory to allow the application to continue creating new objects.</span></span>  
  
 <span data-ttu-id="9b649-156">Jakmile systém uvolňování paměti provede kolekci generace 0, zkomprimuje paměť pro dosažitelné objekty, jak je vysvětleno v části [uvolnění paměti](#cpconautomaticmemorymanagementreleasingmemoryanchor1) dříve v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="9b649-156">After the garbage collector performs a collection of generation 0, it compacts the memory for the reachable objects as explained in [Releasing Memory](#cpconautomaticmemorymanagementreleasingmemoryanchor1) earlier in this topic.</span></span> <span data-ttu-id="9b649-157">Systém uvolňování paměti pak propaguje tyto objekty a vezme v úvahu tuto část spravované haldy 1. generace.</span><span class="sxs-lookup"><span data-stu-id="9b649-157">The garbage collector then promotes these objects and considers this portion of the managed heap generation 1.</span></span> <span data-ttu-id="9b649-158">Vzhledem k tomu, že objekty, které zachují kolekce, mají obvykle delší životnost, je vhodné je zvýšit na vyšší generaci.</span><span class="sxs-lookup"><span data-stu-id="9b649-158">Because objects that survive collections tend to have longer lifetimes, it makes sense to promote them to a higher generation.</span></span> <span data-ttu-id="9b649-159">V důsledku toho systém uvolňování paměti nemusí znovu prošetřit objekty v generacích 1 a 2 pokaždé, když provede kolekci generace 0.</span><span class="sxs-lookup"><span data-stu-id="9b649-159">As a result, the garbage collector does not have to reexamine the objects in generations 1 and 2 each time it performs a collection of generation 0.</span></span>  
  
 <span data-ttu-id="9b649-160">Poté, co systém uvolňování paměti provede první kolekci generace 0 a propaguje dosažitelné objekty na generaci 1, považuje to za zbytek spravované haldy 0.</span><span class="sxs-lookup"><span data-stu-id="9b649-160">After the garbage collector performs its first collection of generation 0 and promotes the reachable objects to generation 1, it considers the remainder of the managed heap generation 0.</span></span> <span data-ttu-id="9b649-161">Nadále přiděluje paměť pro nové objekty v generaci 0, dokud není generace 0 plná a je nutné provést jinou kolekci.</span><span class="sxs-lookup"><span data-stu-id="9b649-161">It continues to allocate memory for new objects in generation 0 until generation 0 is full and it is necessary to perform another collection.</span></span> <span data-ttu-id="9b649-162">V této fázi optimalizující modul systému uvolňování paměti určuje, zda je nutné prošetřit objekty ve starších generacích.</span><span class="sxs-lookup"><span data-stu-id="9b649-162">At this point, the garbage collector's optimizing engine determines whether it is necessary to examine the objects in older generations.</span></span> <span data-ttu-id="9b649-163">Například pokud kolekce generace 0 neuvolní dostatek paměti pro aplikaci k úspěšnému dokončení svého pokusu o vytvoření nového objektu, systém uvolňování paměti může provést kolekci 1. generace, pak 2. generace.</span><span class="sxs-lookup"><span data-stu-id="9b649-163">For example, if a collection of generation 0 does not reclaim enough memory for the application to successfully complete its attempt to create a new object, the garbage collector can perform a collection of generation 1, then generation 2.</span></span> <span data-ttu-id="9b649-164">Pokud to neuvolní dostatek paměti, může systém uvolňování paměti provést kolekci generací 2, 1 a 0.</span><span class="sxs-lookup"><span data-stu-id="9b649-164">If this does not reclaim enough memory, the garbage collector can perform a collection of generations 2, 1, and 0.</span></span> <span data-ttu-id="9b649-165">Po každé kolekci systém uvolňování paměti zkomprimuje dostupné objekty v generaci 0 a propaguje je na generaci 1.</span><span class="sxs-lookup"><span data-stu-id="9b649-165">After each collection, the garbage collector compacts the reachable objects in generation 0 and promotes them to generation 1.</span></span> <span data-ttu-id="9b649-166">Objekty v generaci 1, které dodržely kolekce, jsou povýšeny na generaci 2.</span><span class="sxs-lookup"><span data-stu-id="9b649-166">Objects in generation 1 that survive collections are promoted to generation 2.</span></span> <span data-ttu-id="9b649-167">Vzhledem k tomu, že systém uvolňování paměti podporuje pouze tři generace, objekty v generaci 2, které přestanou kolekci, zůstávají v generaci 2, dokud nebudou v budoucí kolekci nedostupné.</span><span class="sxs-lookup"><span data-stu-id="9b649-167">Because the garbage collector supports only three generations, objects in generation 2 that survive a collection remain in generation 2 until they are determined to be unreachable in a future collection.</span></span>  
  
## <a name="releasing-memory-for-unmanaged-resources"></a><span data-ttu-id="9b649-168">Uvolnění paměti pro nespravované prostředky</span><span class="sxs-lookup"><span data-stu-id="9b649-168">Releasing Memory for Unmanaged Resources</span></span>  
 <span data-ttu-id="9b649-169">Pro většinu objektů, které vaše aplikace vytvoří, můžete spoléhat na systém uvolňování paměti, aby automaticky prováděla nezbytné úlohy správy paměti.</span><span class="sxs-lookup"><span data-stu-id="9b649-169">For the majority of the objects that your application creates, you can rely on the garbage collector to automatically perform the necessary memory management tasks.</span></span> <span data-ttu-id="9b649-170">Nespravované prostředky však vyžadují explicitní vyčištění.</span><span class="sxs-lookup"><span data-stu-id="9b649-170">However, unmanaged resources require explicit cleanup.</span></span> <span data-ttu-id="9b649-171">Nejběžnějším typem nespravovaného prostředku je objekt, který balí prostředek operačního systému, jako je například popisovač souboru, popisovač okna nebo síťové připojení.</span><span class="sxs-lookup"><span data-stu-id="9b649-171">The most common type of unmanaged resource is an object that wraps an operating system resource, such as a file handle, window handle, or network connection.</span></span> <span data-ttu-id="9b649-172">I když je systém uvolňování paměti schopný sledovat životnost spravovaného objektu, který zapouzdřuje nespravovaný prostředek, nemá specifické znalosti o tom, jak prostředek vyčistit.</span><span class="sxs-lookup"><span data-stu-id="9b649-172">Although the garbage collector is able to track the lifetime of a managed object that encapsulates an unmanaged resource, it does not have specific knowledge about how to clean up the resource.</span></span> <span data-ttu-id="9b649-173">Při vytváření objektu, který zapouzdřuje nespravovaný prostředek, se doporučuje zadat potřebný kód pro vyčištění nespravovaného prostředku ve veřejné metodě **Dispose** .</span><span class="sxs-lookup"><span data-stu-id="9b649-173">When you create an object that encapsulates an unmanaged resource, it is recommended that you provide the necessary code to clean up the unmanaged resource in a public **Dispose** method.</span></span> <span data-ttu-id="9b649-174">Poskytnutím metody **Dispose** umožníte uživatelům vašeho objektu explicitně uvolnit svou paměť, až se dokončí s objektem.</span><span class="sxs-lookup"><span data-stu-id="9b649-174">By providing a **Dispose** method, you enable users of your object to explicitly free its memory when they are finished with the object.</span></span> <span data-ttu-id="9b649-175">Při použití objektu, který zapouzdřuje nespravovaný prostředek, byste měli být vědomi **Dispose** a zavolat ho podle potřeby.</span><span class="sxs-lookup"><span data-stu-id="9b649-175">When you use an object that encapsulates an unmanaged resource, you should be aware of **Dispose** and call it as necessary.</span></span> <span data-ttu-id="9b649-176">Další informace o čištění nespravovaných prostředků a příklad vzoru návrhu pro implementaci **Dispose**naleznete v tématu [uvolňování paměti](../../docs/standard/garbage-collection/index.md).</span><span class="sxs-lookup"><span data-stu-id="9b649-176">For more information about cleaning up unmanaged resources and an example of a design pattern for implementing **Dispose**, see [Garbage Collection](../../docs/standard/garbage-collection/index.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="9b649-177">Viz také</span><span class="sxs-lookup"><span data-stu-id="9b649-177">See also</span></span>

- <xref:System.GC>
- [<span data-ttu-id="9b649-178">Uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="9b649-178">Garbage Collection</span></span>](../../docs/standard/garbage-collection/index.md)
- [<span data-ttu-id="9b649-179">Proces spravovaného spuštění</span><span class="sxs-lookup"><span data-stu-id="9b649-179">Managed Execution Process</span></span>](../../docs/standard/managed-execution-process.md)
