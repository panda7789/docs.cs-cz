---
title: Základní informace o uvolňování paměti
description: Přečtěte si, jak systém uvolňování paměti funguje a jak je možné ho nakonfigurovat pro optimální výkon.
ms.date: 11/15/2019
ms.technology: dotnet-standard
helpviewer_keywords:
- garbage collection, generations
- garbage collection, background
- garbage collection, concurrent
- garbage collection, server
- garbage collection, workstation
- garbage collection, managed heap
ms.assetid: 67c5a20d-1be1-4ea7-8a9a-92b0b08658d2
ms.openlocfilehash: 438188b6d694bdeab772c43ef92e5621c68facff
ms.sourcegitcommit: 45c8eed045779b70a47b23169897459d0323dc89
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/18/2020
ms.locfileid: "84990216"
---
# <a name="fundamentals-of-garbage-collection"></a><span data-ttu-id="6c4e9-103">Základní informace o uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="6c4e9-103">Fundamentals of garbage collection</span></span>

<span data-ttu-id="6c4e9-104">V modulu CLR (Common Language Runtime) slouží systém uvolňování paměti (GC) jako automatický správce paměti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-104">In the common language runtime (CLR), the garbage collector (GC) serves as an automatic memory manager.</span></span> <span data-ttu-id="6c4e9-105">Systém uvolňování paměti spravuje přidělování a uvolňování paměti aplikace.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-105">The garbage collector manages the allocation and release of memory for an application.</span></span> <span data-ttu-id="6c4e9-106">Pro vývojáře, kteří pracují se spravovaným kódem, to znamená, že nemusíte psát kód pro provádění úloh správy paměti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-106">For developers working with managed code, this means that you don't have to write code to perform memory management tasks.</span></span> <span data-ttu-id="6c4e9-107">Automatická správa paměti může eliminovat běžné problémy, například forgetting k uvolnění objektu a příčinou nevrácení paměti nebo pokusu o přístup k paměti pro objekt, který je již uvolněn.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-107">Automatic memory management can eliminate common problems, such as forgetting to free an object and causing a memory leak or attempting to access memory for an object that's already been freed.</span></span>

<span data-ttu-id="6c4e9-108">Tento článek popisuje základní koncepty uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-108">This article describes the core concepts of garbage collection.</span></span>

## <a name="benefits"></a><span data-ttu-id="6c4e9-109">Výhody</span><span class="sxs-lookup"><span data-stu-id="6c4e9-109">Benefits</span></span>

<span data-ttu-id="6c4e9-110">Systém uvolňování paměti přináší následující výhody:</span><span class="sxs-lookup"><span data-stu-id="6c4e9-110">The garbage collector provides the following benefits:</span></span>

- <span data-ttu-id="6c4e9-111">Uvolňuje vývojářům, aby nemuseli ručně uvolnit paměť.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-111">Frees developers from having to manually release memory.</span></span>

- <span data-ttu-id="6c4e9-112">Přiděluje objekty na spravované haldě efektivně.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-112">Allocates objects on the managed heap efficiently.</span></span>

- <span data-ttu-id="6c4e9-113">Redeklarace objektů, které se již nepoužívají, vymaže jejich paměť a udržuje dostupnou paměť pro budoucí přidělení.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-113">Reclaims objects that are no longer being used, clears their memory, and keeps the memory available for future allocations.</span></span> <span data-ttu-id="6c4e9-114">Spravované objekty automaticky dostanou čistým obsahem, takže jejich konstruktory nemusí inicializovat každé datové pole.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-114">Managed objects automatically get clean content to start with, so their constructors don't have to initialize every data field.</span></span>

- <span data-ttu-id="6c4e9-115">Zajišťuje bezpečnost paměti tím, že zajistí, že objekt nemůže použít obsah jiného objektu.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-115">Provides memory safety by making sure that an object cannot use the content of another object.</span></span>

## <a name="fundamentals-of-memory"></a><span data-ttu-id="6c4e9-116">Základy paměti</span><span class="sxs-lookup"><span data-stu-id="6c4e9-116">Fundamentals of memory</span></span>

<span data-ttu-id="6c4e9-117">Následující seznam shrnuje důležité koncepty paměti CLR.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-117">The following list summarizes important CLR memory concepts.</span></span>

- <span data-ttu-id="6c4e9-118">Každý proces má vlastní, oddělený virtuální adresní prostor.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-118">Each process has its own, separate virtual address space.</span></span> <span data-ttu-id="6c4e9-119">Všechny procesy ve stejném počítači sdílejí stejnou fyzickou paměť a stránkovací soubor, pokud je nějaký.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-119">All processes on the same computer share the same physical memory and the page file, if there is one.</span></span>

- <span data-ttu-id="6c4e9-120">Ve výchozím nastavení na 32 počítačích má každý proces virtuální adresní prostor 2 GB v uživatelském režimu.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-120">By default, on 32-bit computers, each process has a 2-GB user-mode virtual address space.</span></span>

- <span data-ttu-id="6c4e9-121">Jako vývojář aplikací pracujete jenom s virtuálním adresním prostorem a nikdy nepracujete s fyzickou pamětí přímo.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-121">As an application developer, you work only with virtual address space and never manipulate physical memory directly.</span></span> <span data-ttu-id="6c4e9-122">Systém uvolňování paměti přiděluje a uvolňuje virtuální paměť na spravované haldě.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-122">The garbage collector allocates and frees virtual memory for you on the managed heap.</span></span>

  <span data-ttu-id="6c4e9-123">Pokud píšete nativní kód, použijte funkce systému Windows pro práci s virtuálním adresním prostorem.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-123">If you're writing native code, you use Windows functions to work with the virtual address space.</span></span> <span data-ttu-id="6c4e9-124">Tyto funkce přidělují a uvolňují virtuální paměti pro vás na nativních haldách.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-124">These functions allocate and free virtual memory for you on native heaps.</span></span>

- <span data-ttu-id="6c4e9-125">Virtuální paměť může být ve třech stavech:</span><span class="sxs-lookup"><span data-stu-id="6c4e9-125">Virtual memory can be in three states:</span></span>

  | <span data-ttu-id="6c4e9-126">State</span><span class="sxs-lookup"><span data-stu-id="6c4e9-126">State</span></span> | <span data-ttu-id="6c4e9-127">Popis</span><span class="sxs-lookup"><span data-stu-id="6c4e9-127">Description</span></span> |
  |---------|---------|
  | <span data-ttu-id="6c4e9-128">Free</span><span class="sxs-lookup"><span data-stu-id="6c4e9-128">Free</span></span> | <span data-ttu-id="6c4e9-129">Blok paměti neobsahuje žádné odkazy a je k dispozici pro přidělení.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-129">The block of memory has no references to it and is available for allocation.</span></span> |
  | <span data-ttu-id="6c4e9-130">Vyhrazeno</span><span class="sxs-lookup"><span data-stu-id="6c4e9-130">Reserved</span></span> | <span data-ttu-id="6c4e9-131">Blok paměti je k dispozici pro vaše použití a nelze jej použít pro žádnou jinou žádost o přidělení.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-131">The block of memory is available for your use and cannot be used for any other allocation request.</span></span> <span data-ttu-id="6c4e9-132">Do tohoto bloku paměti však nelze ukládat data, dokud není potvrzeno.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-132">However, you cannot store data to this memory block until it is committed.</span></span> |
  | <span data-ttu-id="6c4e9-133">Committed</span><span class="sxs-lookup"><span data-stu-id="6c4e9-133">Committed</span></span> | <span data-ttu-id="6c4e9-134">Blok paměti je přiřazený k fyzickému úložišti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-134">The block of memory is assigned to physical storage.</span></span> |

- <span data-ttu-id="6c4e9-135">Virtuální adresní prostor se může fragmentovat.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-135">Virtual address space can get fragmented.</span></span> <span data-ttu-id="6c4e9-136">To znamená, že v adresním prostoru jsou k dispozici bezplatné bloky, označované také jako díry.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-136">This means that there are free blocks, also known as holes, in the address space.</span></span> <span data-ttu-id="6c4e9-137">Po vyžádání přidělení virtuální paměti musí správce virtuální paměti najít jeden bezplatný blok, který je dostatečně velký pro splnění této žádosti o přidělení.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-137">When a virtual memory allocation is requested, the virtual memory manager has to find a single free block that is large enough to satisfy that allocation request.</span></span> <span data-ttu-id="6c4e9-138">I v případě, že máte 2 GB volného místa, přidělení, které vyžaduje 2 GB, bude neúspěšné, pokud všechny volné místo nejsou v jednom bloku adres.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-138">Even if you have 2 GB of free space, an allocation that requires 2 GB will be unsuccessful unless all of that free space is in a single address block.</span></span>

- <span data-ttu-id="6c4e9-139">Pokud není k dispozici dostatek adresního prostoru pro rezervaci nebo fyzické místo pro potvrzení, můžete nedostatek paměti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-139">You can run out of memory if there isn't enough virtual address space to reserve or physical space to commit.</span></span>

  <span data-ttu-id="6c4e9-140">Stránkovací soubor se používá i v případě, že je nízká tlak fyzické paměti (tj. poptávka pro fyzickou paměť).</span><span class="sxs-lookup"><span data-stu-id="6c4e9-140">The page file is used even if physical memory pressure (that is, demand for physical memory) is low.</span></span> <span data-ttu-id="6c4e9-141">Při prvním vysokém zatížení fyzické paměti musí operační systém uvolnit místo ve fyzické paměti pro ukládání dat a pak zálohuje některá data, která jsou ve fyzické paměti pro stránkovací soubor.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-141">The first time that physical memory pressure is high, the operating system must make room in physical memory to store data, and it backs up some of the data that is in physical memory to the page file.</span></span> <span data-ttu-id="6c4e9-142">Tato data nejsou stránkovaná, dokud je nepotřebujete, takže je možné vyskytnout stránkování v situacích, kdy je nízký tlak fyzické paměti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-142">That data is not paged until it's needed, so it's possible to encounter paging in situations where the physical memory pressure is low.</span></span>
  
### <a name="memory-allocation"></a><span data-ttu-id="6c4e9-143">Přidělení paměti</span><span class="sxs-lookup"><span data-stu-id="6c4e9-143">Memory allocation</span></span>

<span data-ttu-id="6c4e9-144">Při inicializaci nového procesu vyhradí modul runtime souvislou oblast adresního prostoru pro daný proces.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-144">When you initialize a new process, the runtime reserves a contiguous region of address space for the process.</span></span> <span data-ttu-id="6c4e9-145">Tento vyhrazený adresní prostor se nazývá spravovaná halda.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-145">This reserved address space is called the managed heap.</span></span> <span data-ttu-id="6c4e9-146">Spravovaná halda udržuje ukazatel na adresu, kde bude přidělen další objekt v haldě.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-146">The managed heap maintains a pointer to the address where the next object in the heap will be allocated.</span></span> <span data-ttu-id="6c4e9-147">Zpočátku je tento ukazatel nastavený na základní adresu spravované haldy.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-147">Initially, this pointer is set to the managed heap's base address.</span></span> <span data-ttu-id="6c4e9-148">Všechny typy odkazů jsou přiděleny na spravovanou haldu.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-148">All reference types are allocated on the managed heap.</span></span> <span data-ttu-id="6c4e9-149">Když aplikace vytvoří první typ odkazu, přidělí se paměť pro typ na základní adrese spravované haldy.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-149">When an application creates the first reference type, memory is allocated for the type at the base address of the managed heap.</span></span> <span data-ttu-id="6c4e9-150">Když aplikace vytvoří další objekt, systém uvolňování paměti přidělí paměť v adresním prostoru hned za prvním objektem.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-150">When the application creates the next object, the garbage collector allocates memory for it in the address space immediately following the first object.</span></span> <span data-ttu-id="6c4e9-151">Pokud je k dispozici adresní prostor, uvolňování paměti pro nové objekty tímto způsobem stále přiděluje místo.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-151">As long as address space is available, the garbage collector continues to allocate space for new objects in this manner.</span></span>

<span data-ttu-id="6c4e9-152">Přidělování paměti ze spravované haldy je rychlejší než nespravované přidělení paměti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-152">Allocating memory from the managed heap is faster than unmanaged memory allocation.</span></span> <span data-ttu-id="6c4e9-153">Vzhledem k tomu, že modul Runtime přiděluje paměť pro objekt přidáním hodnoty na ukazatel, je téměř stejně rychlá jako přidělování paměti ze zásobníku.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-153">Because the runtime allocates memory for an object by adding a value to a pointer, it's almost as fast as allocating memory from the stack.</span></span> <span data-ttu-id="6c4e9-154">Kromě toho vzhledem k tomu, že nové objekty, které jsou přiděleny po sobě, jsou uloženy souvisle ve spravované haldě, aplikace může přistupovat k objektům rychle.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-154">In addition, because new objects that are allocated consecutively are stored contiguously in the managed heap, an application can access the objects quickly.</span></span>

### <a name="memory-release"></a><span data-ttu-id="6c4e9-155">Verze paměti</span><span class="sxs-lookup"><span data-stu-id="6c4e9-155">Memory release</span></span>

<span data-ttu-id="6c4e9-156">Optimalizující modul systému uvolňování paměti určuje nejvhodnější čas k provedení kolekce na základě přidělení.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-156">The garbage collector's optimizing engine determines the best time to perform a collection based on the allocations being made.</span></span> <span data-ttu-id="6c4e9-157">Když systém uvolňování paměti provede kolekci, uvolní paměť pro objekty, které již aplikace nepoužívá.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-157">When the garbage collector performs a collection, it releases the memory for objects that are no longer being used by the application.</span></span> <span data-ttu-id="6c4e9-158">Určuje, které objekty již nejsou používány, prozkoumáním *kořenů*aplikace.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-158">It determines which objects are no longer being used by examining the application's *roots*.</span></span> <span data-ttu-id="6c4e9-159">Kořeny aplikace zahrnují statická pole, místní proměnné a parametry v zásobníku vlákna a Registry procesoru.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-159">An application's roots include static fields, local variables and parameters on a thread's stack, and CPU registers.</span></span> <span data-ttu-id="6c4e9-160">Každý kořenový adresář buď odkazuje na objekt na spravované haldě, nebo je nastaven na hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-160">Each root either refers to an object on the managed heap or is set to null.</span></span> <span data-ttu-id="6c4e9-161">Systém uvolňování paměti má přístup k seznamu aktivních kořenových adresářů, které kompilátor JIT (just-in-time) a modul runtime udržují.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-161">The garbage collector has access to the list of active roots that the just-in-time (JIT) compiler and the runtime maintain.</span></span> <span data-ttu-id="6c4e9-162">Pomocí tohoto seznamu vytvoří systém uvolňování paměti graf, který obsahuje všechny objekty, které jsou dosažitelné z kořenů.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-162">Using this list, the garbage collector creates a graph that contains all the objects that are reachable from the roots.</span></span>

<span data-ttu-id="6c4e9-163">Objekty, které nejsou v grafu, jsou nedosažitelné z kořenů aplikace.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-163">Objects that are not in the graph are unreachable from the application's roots.</span></span> <span data-ttu-id="6c4e9-164">Systém uvolňování paměti považuje nedosažitelné objekty za paměti a uvolní přidělenou paměť.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-164">The garbage collector considers unreachable objects garbage and releases the memory allocated for them.</span></span> <span data-ttu-id="6c4e9-165">V průběhu kolekce systém uvolňování paměti ověřuje spravovanou haldu a hledá bloky adresního prostoru obsazené nedosažitelnými objekty.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-165">During a collection, the garbage collector examines the managed heap, looking for the blocks of address space occupied by unreachable objects.</span></span> <span data-ttu-id="6c4e9-166">Vzhledem k tomu, že zjistí každý nedosažitelný objekt, používá funkci kopírování paměti pro komprimaci dosažitelných objektů v paměti a uvolní bloky adresních prostorů přidělených nedostupným objektům.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-166">As it discovers each unreachable object, it uses a memory-copying function to compact the reachable objects in memory, freeing up the blocks of address spaces allocated to unreachable objects.</span></span> <span data-ttu-id="6c4e9-167">Po komprimaci paměti pro dostupné objekty provede systém uvolňování paměti nezbytné opravy ukazatelů, aby kořeny aplikace odkazovaly na objekty v jejich nových umístěních.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-167">Once the memory for the reachable objects has been compacted, the garbage collector makes the necessary pointer corrections so that the application's roots point to the objects in their new locations.</span></span> <span data-ttu-id="6c4e9-168">Také umístí ukazatel spravované haldy za poslední dosažitelný objekt.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-168">It also positions the managed heap's pointer after the last reachable object.</span></span>

<span data-ttu-id="6c4e9-169">Paměť je komprimována pouze v případě, že kolekce zjistí významný počet nedosažitelných objektů.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-169">Memory is compacted only if a collection discovers a significant number of unreachable objects.</span></span> <span data-ttu-id="6c4e9-170">Pokud všechny objekty ve spravované haldě přestanou kolekci, nepotřebujete komprimaci paměti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-170">If all the objects in the managed heap survive a collection, then there is no need for memory compaction.</span></span>

<span data-ttu-id="6c4e9-171">Za účelem zvýšení výkonu přiděluje modul runtime paměť pro velké objekty v samostatné haldě.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-171">To improve performance, the runtime allocates memory for large objects in a separate heap.</span></span> <span data-ttu-id="6c4e9-172">Systém uvolňování paměti automaticky uvolňuje paměť pro velké objekty.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-172">The garbage collector automatically releases the memory for large objects.</span></span> <span data-ttu-id="6c4e9-173">Chcete-li se ale vyhnout přesunutí velkých objektů v paměti, obvykle se tato paměť nekomprimuje.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-173">However, to avoid moving large objects in memory, this memory is usually not compacted.</span></span>

## <a name="conditions-for-a-garbage-collection"></a><span data-ttu-id="6c4e9-174">Podmínky pro uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="6c4e9-174">Conditions for a garbage collection</span></span>

<span data-ttu-id="6c4e9-175">Uvolňování paměti nastane, pokud je splněna jedna z následujících podmínek:</span><span class="sxs-lookup"><span data-stu-id="6c4e9-175">Garbage collection occurs when one of the following conditions is true:</span></span>

- <span data-ttu-id="6c4e9-176">Systém má nedostatek fyzické paměti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-176">The system has low physical memory.</span></span> <span data-ttu-id="6c4e9-177">Tato možnost je zjištěna buď pomocí oznámení o nedostatku paměti z operačního systému, nebo z nedostatku paměti, jak je uvedeno v hostiteli.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-177">This is detected by either the low memory notification from the OS or low memory as indicated by the host.</span></span>

- <span data-ttu-id="6c4e9-178">Paměť, kterou používá přidělené objekty na spravované haldě, překročí přijatelnou prahovou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-178">The memory that's used by allocated objects on the managed heap surpasses an acceptable threshold.</span></span> <span data-ttu-id="6c4e9-179">Tato prahová hodnota je průběžně upravována při spuštění procesu.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-179">This threshold is continuously adjusted as the process runs.</span></span>

- <span data-ttu-id="6c4e9-180"><xref:System.GC.Collect%2A?displayProperty=nameWithType>Metoda je volána.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-180">The <xref:System.GC.Collect%2A?displayProperty=nameWithType> method is called.</span></span> <span data-ttu-id="6c4e9-181">Ve většině případů nemusíte volat tuto metodu, protože systém uvolňování paměti běží nepřetržitě.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-181">In almost all cases, you don't have to call this method, because the garbage collector runs continuously.</span></span> <span data-ttu-id="6c4e9-182">Tato metoda se primárně používá pro jedinečné situace a testování.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-182">This method is primarily used for unique situations and testing.</span></span>

## <a name="the-managed-heap"></a><span data-ttu-id="6c4e9-183">Spravovaná halda</span><span class="sxs-lookup"><span data-stu-id="6c4e9-183">The managed heap</span></span>

<span data-ttu-id="6c4e9-184">Po inicializaci uvolňování paměti modulem CLR přidělí segment paměti pro ukládání a správu objektů.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-184">After the garbage collector is initialized by the CLR, it allocates a segment of memory to store and manage objects.</span></span> <span data-ttu-id="6c4e9-185">Tato paměť se nazývá spravovaná halda, nikoli nativní halda v operačním systému.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-185">This memory is called the managed heap, as opposed to a native heap in the operating system.</span></span>

<span data-ttu-id="6c4e9-186">Pro každý spravovaný proces existuje spravovaná halda.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-186">There is a managed heap for each managed process.</span></span> <span data-ttu-id="6c4e9-187">Všechna vlákna v procesu přidělují paměť pro objekty ve stejné haldě.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-187">All threads in the process allocate memory for objects on the same heap.</span></span>

<span data-ttu-id="6c4e9-188">Pro vyhradení paměti systém uvolňování paměti zavolá funkci Windows [VirtualAlloc](/windows/desktop/api/memoryapi/nf-memoryapi-virtualalloc) a rezervuje pro spravované aplikace jeden segment paměti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-188">To reserve memory, the garbage collector calls the Windows [VirtualAlloc](/windows/desktop/api/memoryapi/nf-memoryapi-virtualalloc) function and reserves one segment of memory at a time for managed applications.</span></span> <span data-ttu-id="6c4e9-189">Systém uvolňování paměti také vyhrazuje segmenty, jak je potřeba, a uvolní segmenty zpátky do operačního systému (po vymazání všech objektů) voláním funkce [VirtualFree](/windows/desktop/api/memoryapi/nf-memoryapi-virtualfree) systému Windows.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-189">The garbage collector also reserves segments, as needed, and releases segments back to the operating system (after clearing them of any objects) by calling the Windows [VirtualFree](/windows/desktop/api/memoryapi/nf-memoryapi-virtualfree) function.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="6c4e9-190">Velikost segmentů přidělených systémem uvolňování paměti je specifická pro konkrétní implementaci a může se kdykoli změnit, včetně pravidelných aktualizací.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-190">The size of segments allocated by the garbage collector is implementation-specific and is subject to change at any time, including in periodic updates.</span></span> <span data-ttu-id="6c4e9-191">Vaše aplikace by nikdy neměla zabývat ani záviset na konkrétní velikosti segmentu, ani by se neměla pokoušet nakonfigurovat množství paměti dostupné pro přidělení segmentů.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-191">Your app should never make assumptions about or depend on a particular segment size, nor should it attempt to configure the amount of memory available for segment allocations.</span></span>

<span data-ttu-id="6c4e9-192">Čím méně objektů bylo přiděleno haldě, tím méně práce musí systém uvolňování paměti dělat.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-192">The fewer objects allocated on the heap, the less work the garbage collector has to do.</span></span> <span data-ttu-id="6c4e9-193">Při přidělování objektů nepoužívejte zaoblené hodnoty, které překračují vaše potřeby, jako je například přidělení pole 32 bajtů, pokud potřebujete pouze 15 bajtů.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-193">When you allocate objects, don't use rounded-up values that exceed your needs, such as allocating an array of 32 bytes when you need only 15 bytes.</span></span>

<span data-ttu-id="6c4e9-194">Když je aktivováno uvolňování paměti, uvolňování paměti uvolní paměť, která je obsazena mrtvými objekty.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-194">When a garbage collection is triggered, the garbage collector reclaims the memory that's occupied by dead objects.</span></span> <span data-ttu-id="6c4e9-195">Proces opětovného získání komprimuje živé objekty tak, aby se přesunuly dohromady, a nedoručené místo se odebere, takže halda bude menší.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-195">The reclaiming process compacts live objects so that they are moved together, and the dead space is removed, thereby making the heap smaller.</span></span> <span data-ttu-id="6c4e9-196">Tím se zajistí, aby objekty, které jsou přiděleny dohromady, zůstaly na spravované haldě, aby se zachovala jejich místní aktuálnost.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-196">This ensures that objects that are allocated together stay together on the managed heap to preserve their locality.</span></span>

<span data-ttu-id="6c4e9-197">Rušivost (četnost a doba trvání) uvolňování paměti je výsledkem objemu přidělení a množství zachované paměti na spravované haldě.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-197">The intrusiveness (frequency and duration) of garbage collections is the result of the volume of allocations and the amount of survived memory on the managed heap.</span></span>

<span data-ttu-id="6c4e9-198">Halda může být považována za akumulaci dvou hald: [halda velkých objektů](large-object-heap.md) a haldy malých objektů.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-198">The heap can be considered as the accumulation of two heaps: the [large object heap](large-object-heap.md) and the small object heap.</span></span> <span data-ttu-id="6c4e9-199">Halda velkých objektů obsahuje objekty, které jsou 85 000 bajtů a větší, což jsou obvykle pole.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-199">The large object heap contains objects that are 85,000 bytes and larger, which are usually arrays.</span></span> <span data-ttu-id="6c4e9-200">Pro objekt instance je zřídka velká.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-200">It's rare for an instance object to be extremely large.</span></span>

> [!TIP]
> <span data-ttu-id="6c4e9-201">Můžete [nakonfigurovat prahovou velikost](../../core/run-time-config/garbage-collector.md#large-object-heap-threshold) pro objekty, které mají být v haldě velkých objektů.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-201">You can [configure the threshold size](../../core/run-time-config/garbage-collector.md#large-object-heap-threshold) for objects to go on the large object heap.</span></span>

## <a name="generations"></a><span data-ttu-id="6c4e9-202">Všechna</span><span class="sxs-lookup"><span data-stu-id="6c4e9-202">Generations</span></span>

<span data-ttu-id="6c4e9-203">Algoritmus GC je založený na několika ohledech:</span><span class="sxs-lookup"><span data-stu-id="6c4e9-203">The GC algorithm is based on several considerations:</span></span>

- <span data-ttu-id="6c4e9-204">Je rychlejší komprimovat paměť pro část spravované haldy, než pro celou spravovanou haldu.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-204">It's faster to compact the memory for a portion of the managed heap than for the entire managed heap.</span></span>
- <span data-ttu-id="6c4e9-205">Novější objekty mají kratší životnost a starší objekty mají delší životnost.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-205">Newer objects have shorter lifetimes and older objects have longer lifetimes.</span></span>
- <span data-ttu-id="6c4e9-206">Novější objekty by měly být vzájemně vzájemně propojené a k nim přistupují aplikace po stejnou dobu.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-206">Newer objects tend to be related to each other and accessed by the application around the same time.</span></span>

<span data-ttu-id="6c4e9-207">K uvolňování paměti dochází hlavně v případě opětovného získávání krátkodobých objektů.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-207">Garbage collection primarily occurs with the reclamation of short-lived objects.</span></span> <span data-ttu-id="6c4e9-208">Pro optimalizaci výkonu uvolňování paměti je spravovaná halda rozdělena na tři generace, 0, 1 a 2, takže může zpracovávat dlouhodobé a krátkodobé objekty samostatně.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-208">To optimize the performance of the garbage collector, the managed heap is divided into three generations, 0, 1, and 2, so it can handle long-lived and short-lived objects separately.</span></span> <span data-ttu-id="6c4e9-209">Systém uvolňování paměti ukládá nové objekty v generaci 0.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-209">The garbage collector stores new objects in generation 0.</span></span> <span data-ttu-id="6c4e9-210">Objekty vytvořené na začátku v životním cyklu aplikace, které jsou převedené na kolekce, jsou povýšeny a uloženy v generacích 1 a 2.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-210">Objects created early in the application's lifetime that survive collections are promoted and stored in generations 1 and 2.</span></span> <span data-ttu-id="6c4e9-211">Vzhledem k tomu, že je rychlejší komprimovat část spravované haldy, než je celá halda, toto schéma umožňuje uvolňování paměti uvolnit paměť v konkrétní generaci místo uvolnění paměti pro celou spravovanou haldu pokaždé, když aplikace provede kolekci.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-211">Because it's faster to compact a portion of the managed heap than the entire heap, this scheme allows the garbage collector to release the memory in a specific generation rather than release the memory for the entire managed heap each time it performs a collection.</span></span>

- <span data-ttu-id="6c4e9-212">**Generace 0**.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-212">**Generation 0**.</span></span> <span data-ttu-id="6c4e9-213">Toto je nejmladšího sourozence generace a obsahuje krátkodobé objekty.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-213">This is the youngest generation and contains short-lived objects.</span></span> <span data-ttu-id="6c4e9-214">Příkladem krátkodobého objektu je dočasná proměnná.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-214">An example of a short-lived object is a temporary variable.</span></span> <span data-ttu-id="6c4e9-215">K uvolňování paměti dochází nejčastěji v této generaci.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-215">Garbage collection occurs most frequently in this generation.</span></span>

  <span data-ttu-id="6c4e9-216">Nově přidělené objekty tvoří novou generaci objektů a jsou implicitními kolekcemi 0. generace.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-216">Newly allocated objects form a new generation of objects and are implicitly generation 0 collections.</span></span> <span data-ttu-id="6c4e9-217">Pokud se však jedná o velké objekty, přejdou na haldu velkých objektů (LOH), což je někdy označováno jako *generace 3*.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-217">However, if they are large objects, they go on the large object heap (LOH), which is sometimes referred to as *generation 3*.</span></span> <span data-ttu-id="6c4e9-218">Generace 3 je fyzická generace, která je logicky shromažďována jako součást generace 2.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-218">Generation 3 is a physical generation that's logically collected as part of generation 2.</span></span>

  <span data-ttu-id="6c4e9-219">Většina objektů se uvolní pro uvolňování paměti v generaci 0 a nepřekoná se k další generaci.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-219">Most objects are reclaimed for garbage collection in generation 0 and don't survive to the next generation.</span></span>
  
  <span data-ttu-id="6c4e9-220">Pokud se aplikace pokusí vytvořit nový objekt, když je generace 0 plná, systém uvolňování paměti provede kolekci v pokusu o uvolnění adresního prostoru pro daný objekt.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-220">If an application attempts to create a new object when generation 0 is full, the garbage collector performs a collection in an attempt to free address space for the object.</span></span> <span data-ttu-id="6c4e9-221">Systém uvolňování paměti začíná prozkoumáním objektů v generaci 0 místo všech objektů ve spravované haldě.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-221">The garbage collector starts by examining the objects in generation 0 rather than all objects in the managed heap.</span></span> <span data-ttu-id="6c4e9-222">Kolekce samotné generace 0 často uvolňuje dostatek paměti, aby mohla aplikace pokračovat v vytváření nových objektů.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-222">A collection of generation 0 alone often reclaims enough memory to enable the application to continue creating new objects.</span></span>

- <span data-ttu-id="6c4e9-223">**Generace 1**.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-223">**Generation 1**.</span></span> <span data-ttu-id="6c4e9-224">Tato generace obsahuje krátkodobé objekty a slouží jako vyrovnávací paměť mezi krátkodobé a dlouhodobé objekty.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-224">This generation contains short-lived objects and serves as a buffer between short-lived objects and long-lived objects.</span></span>

  <span data-ttu-id="6c4e9-225">Jakmile systém uvolňování paměti provede kolekci generace 0, zkomprimuje paměť pro dostupné objekty a propaguje je na generaci 1.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-225">After the garbage collector performs a collection of generation 0, it compacts the memory for the reachable objects and promotes them to generation 1.</span></span> <span data-ttu-id="6c4e9-226">Vzhledem k tomu, že objekty, které zachují kolekce, mají obvykle delší životnost, je vhodné je zvýšit na vyšší generaci.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-226">Because objects that survive collections tend to have longer lifetimes, it makes sense to promote them to a higher generation.</span></span> <span data-ttu-id="6c4e9-227">Systém uvolňování paměti nemusí přezkoumávat objekty v generacích 1 a 2 pokaždé, když provede kolekci generace 0.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-227">The garbage collector doesn't have to reexamine the objects in generations 1 and 2 each time it performs a collection of generation 0.</span></span>
  
  <span data-ttu-id="6c4e9-228">Pokud kolekce generace 0 neuvolní dostatek paměti pro aplikaci, aby vytvořila nový objekt, může systém uvolňování paměti provést kolekci 1. generace a pak 2. generace.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-228">If a collection of generation 0 does not reclaim enough memory for the application to create a new object, the garbage collector can perform a collection of generation 1, then generation 2.</span></span> <span data-ttu-id="6c4e9-229">Objekty v generaci 1, které dodržely kolekce, jsou povýšeny na generaci 2.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-229">Objects in generation 1 that survive collections are promoted to generation 2.</span></span>

- <span data-ttu-id="6c4e9-230">**Generace 2**.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-230">**Generation 2**.</span></span> <span data-ttu-id="6c4e9-231">Tato generace obsahuje dlouhodobě nedlouhodobé objekty.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-231">This generation contains long-lived objects.</span></span> <span data-ttu-id="6c4e9-232">Příkladem dlouhotrvajícího objektu je objekt v serverové aplikaci, který obsahuje statická data, která jsou po dobu trvání procesu aktivní.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-232">An example of a long-lived object is an object in a server application that contains static data that's live for the duration of the process.</span></span>

  <span data-ttu-id="6c4e9-233">Objekty v generaci 2, které dodržely kolekci, zůstávají v generaci 2, dokud nebudou v budoucí kolekci nedostupné.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-233">Objects in generation 2 that survive a collection remain in generation 2 until they are determined to be unreachable in a future collection.</span></span>
  
  <span data-ttu-id="6c4e9-234">Objekty v haldě velkých objektů (což je někdy označováno jako *generace 3*) jsou shromažďovány také v generaci 2.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-234">Objects on the large object heap (which is sometimes referred to as *generation 3*) are also collected in generation 2.</span></span>

<span data-ttu-id="6c4e9-235">K uvolňování paměti dochází na určitých generacích, protože podmínky zaručují.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-235">Garbage collections occur on specific generations as conditions warrant.</span></span> <span data-ttu-id="6c4e9-236">Shromáždění generace znamená shromažďování objektů v této generaci a všech jejích mladších generací.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-236">Collecting a generation means collecting objects in that generation and all its younger generations.</span></span> <span data-ttu-id="6c4e9-237">Uvolňování paměti 2. generace se označuje také jako úplné uvolňování paměti, protože objekty ve všech generacích (tj. všechny objekty ve spravované haldě) uvolňují.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-237">A generation 2 garbage collection is also known as a full garbage collection, because it reclaims objects in all generations (that is, all objects in the managed heap).</span></span>

### <a name="survival-and-promotions"></a><span data-ttu-id="6c4e9-238">Přežití a propagační akce</span><span class="sxs-lookup"><span data-stu-id="6c4e9-238">Survival and promotions</span></span>

<span data-ttu-id="6c4e9-239">Objekty, které nejsou znovu získány v uvolňování paměti, jsou označovány jako zachované a jsou povýšeny na novou generaci:</span><span class="sxs-lookup"><span data-stu-id="6c4e9-239">Objects that are not reclaimed in a garbage collection are known as survivors and are promoted to the next generation:</span></span>

- <span data-ttu-id="6c4e9-240">Objekty, které jsou zachovány uvolňováním paměti generace 0, jsou povýšeny na generaci 1.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-240">Objects that survive a generation 0 garbage collection are promoted to generation 1.</span></span>
- <span data-ttu-id="6c4e9-241">Objekty, které jsou zachovány uvolňováním paměti generace 1, jsou povýšeny na generaci 2.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-241">Objects that survive a generation 1 garbage collection are promoted to generation 2.</span></span>
- <span data-ttu-id="6c4e9-242">Objekty, které jsou v procesu uvolňování paměti generace 2, zůstávají v generaci 2.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-242">Objects that survive a generation 2 garbage collection remain in generation 2.</span></span>

<span data-ttu-id="6c4e9-243">Když systém uvolňování paměti zjistí, že je míra přežití v generaci vysoká, zvyšuje se prahová hodnota přidělení pro tuto generaci.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-243">When the garbage collector detects that the survival rate is high in a generation, it increases the threshold of allocations for that generation.</span></span> <span data-ttu-id="6c4e9-244">Další kolekce získá značnou velikost uvolněné paměti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-244">The next collection gets a substantial size of reclaimed memory.</span></span> <span data-ttu-id="6c4e9-245">CLR průběžně vyrovnává dvě priority: Pokud nechcete, aby pracovní sada aplikace byla příliš velká, pomocí opožděného uvolňování paměti a neumožnilo spouštění uvolňování paměti příliš často.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-245">The CLR continually balances two priorities: not letting an application's working set get too large by delaying garbage collection and not letting the garbage collection run too frequently.</span></span>

### <a name="ephemeral-generations-and-segments"></a><span data-ttu-id="6c4e9-246">Dočasné generace a segmenty</span><span class="sxs-lookup"><span data-stu-id="6c4e9-246">Ephemeral generations and segments</span></span>

<span data-ttu-id="6c4e9-247">Vzhledem k tomu, že objekty v generacích 0 a 1 jsou krátkodobé, tyto generace jsou známy jako *dočasné generace*.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-247">Because objects in generations 0 and 1 are short-lived, these generations are known as the *ephemeral generations*.</span></span>

<span data-ttu-id="6c4e9-248">Dočasné generace jsou přiděleny v segmentu paměti, který je známý jako dočasný segment.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-248">Ephemeral generations are allocated in the memory segment that's known as the ephemeral segment.</span></span> <span data-ttu-id="6c4e9-249">Každý nový segment získaný systémem uvolňování paměti se stal novým dočasným segmentem a obsahuje objekty, které předržely uvolňování paměti generace 0.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-249">Each new segment acquired by the garbage collector becomes the new ephemeral segment and contains the objects that survived a generation 0 garbage collection.</span></span> <span data-ttu-id="6c4e9-250">Starý dočasný segment se vytvoří jako nový segment 2. generace.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-250">The old ephemeral segment becomes the new generation 2 segment.</span></span>

<span data-ttu-id="6c4e9-251">Velikost dočasného segmentu se liší v závislosti na tom, zda je systém 32 nebo 64 a na typu uvolňování paměti, které je spuštěn ([pracovní stanice nebo Server uvolňování](workstation-server-gc.md)paměti).</span><span class="sxs-lookup"><span data-stu-id="6c4e9-251">The size of the ephemeral segment varies depending on whether a system is 32-bit or 64-bit and on the type of garbage collector it is running ([workstation or server GC](workstation-server-gc.md)).</span></span> <span data-ttu-id="6c4e9-252">V následující tabulce jsou uvedeny výchozí velikosti dočasného segmentu.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-252">The following table shows the default sizes of the ephemeral segment.</span></span>

|<span data-ttu-id="6c4e9-253">Uvolňování paměti pracovní stanice/serveru</span><span class="sxs-lookup"><span data-stu-id="6c4e9-253">Workstation/server GC</span></span>|<span data-ttu-id="6c4e9-254">32bitová</span><span class="sxs-lookup"><span data-stu-id="6c4e9-254">32-bit</span></span>|<span data-ttu-id="6c4e9-255">64bitová</span><span class="sxs-lookup"><span data-stu-id="6c4e9-255">64-bit</span></span>|
|-|-------------|-------------|
|<span data-ttu-id="6c4e9-256">GC pracovní stanice</span><span class="sxs-lookup"><span data-stu-id="6c4e9-256">Workstation GC</span></span>|<span data-ttu-id="6c4e9-257">16 MB</span><span class="sxs-lookup"><span data-stu-id="6c4e9-257">16 MB</span></span>|<span data-ttu-id="6c4e9-258">256 MB</span><span class="sxs-lookup"><span data-stu-id="6c4e9-258">256 MB</span></span>|
|<span data-ttu-id="6c4e9-259">Uvolňování paměti serveru</span><span class="sxs-lookup"><span data-stu-id="6c4e9-259">Server GC</span></span>|<span data-ttu-id="6c4e9-260">64 MB</span><span class="sxs-lookup"><span data-stu-id="6c4e9-260">64 MB</span></span>|<span data-ttu-id="6c4e9-261">4 GB</span><span class="sxs-lookup"><span data-stu-id="6c4e9-261">4 GB</span></span>|
|<span data-ttu-id="6c4e9-262">GC serveru s logickými procesory > 4</span><span class="sxs-lookup"><span data-stu-id="6c4e9-262">Server GC with > 4 logical CPUs</span></span>|<span data-ttu-id="6c4e9-263">32 MB</span><span class="sxs-lookup"><span data-stu-id="6c4e9-263">32 MB</span></span>|<span data-ttu-id="6c4e9-264">2 GB</span><span class="sxs-lookup"><span data-stu-id="6c4e9-264">2 GB</span></span>|
|<span data-ttu-id="6c4e9-265">Uvolňování paměti serveru s využitím > 8 logických procesorů</span><span class="sxs-lookup"><span data-stu-id="6c4e9-265">Server GC with > 8 logical CPUs</span></span>|<span data-ttu-id="6c4e9-266">16 MB</span><span class="sxs-lookup"><span data-stu-id="6c4e9-266">16 MB</span></span>|<span data-ttu-id="6c4e9-267">1 GB</span><span class="sxs-lookup"><span data-stu-id="6c4e9-267">1 GB</span></span>|

<span data-ttu-id="6c4e9-268">Dočasný segment může zahrnovat objekty generace 2.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-268">The ephemeral segment can include generation 2 objects.</span></span> <span data-ttu-id="6c4e9-269">Objekty generace 2 můžou používat víc segmentů (tolik, kolik je váš proces vyžaduje, a paměť umožňuje).</span><span class="sxs-lookup"><span data-stu-id="6c4e9-269">Generation 2 objects can use multiple segments (as many as your process requires and memory allows for).</span></span>

<span data-ttu-id="6c4e9-270">Velikost volné paměti z dočasného uvolňování paměti je omezená na velikost dočasného segmentu.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-270">The amount of freed memory from an ephemeral garbage collection is limited to the size of the ephemeral segment.</span></span> <span data-ttu-id="6c4e9-271">Velikost paměti, která je uvolněna, je úměrná místu, které byly obsazeny neaktivními objekty.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-271">The amount of memory that is freed is proportional to the space that was occupied by the dead objects.</span></span>

## <a name="what-happens-during-a-garbage-collection"></a><span data-ttu-id="6c4e9-272">Co se stane během uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="6c4e9-272">What happens during a garbage collection</span></span>

<span data-ttu-id="6c4e9-273">Uvolňování paměti má následující fáze:</span><span class="sxs-lookup"><span data-stu-id="6c4e9-273">A garbage collection has the following phases:</span></span>

- <span data-ttu-id="6c4e9-274">Fáze označení, která najde a vytvoří seznam všech živých objektů.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-274">A marking phase that finds and creates a list of all live objects.</span></span>

- <span data-ttu-id="6c4e9-275">Fáze přemístění, která aktualizuje odkazy na objekty, které budou zkomprimovány.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-275">A relocating phase that updates the references to the objects that will be compacted.</span></span>

- <span data-ttu-id="6c4e9-276">Kompaktní fáze, která uvolňuje místo obsazené neaktivními objekty a komprimuje zbývající objekty.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-276">A compacting phase that reclaims the space occupied by the dead objects and compacts the surviving objects.</span></span> <span data-ttu-id="6c4e9-277">Fáze komprimace přesune objekty, které předržely uvolňování paměti směrem na starší konec segmentu.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-277">The compacting phase moves objects that have survived a garbage collection toward the older end of the segment.</span></span>

  <span data-ttu-id="6c4e9-278">Vzhledem k tomu, že kolekce 2. generace mohou zabírat více segmentů, objekty, které jsou povýšeny do generace 2, lze přesunout do staršího segmentu.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-278">Because generation 2 collections can occupy multiple segments, objects that are promoted into generation 2 can be moved into an older segment.</span></span> <span data-ttu-id="6c4e9-279">Generace 1 i 2, zachované, je možné přesunout do jiného segmentu, protože jsou povýšeny na generaci 2.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-279">Both generation 1 and generation 2 survivors can be moved to a different segment, because they are promoted to generation 2.</span></span>

  <span data-ttu-id="6c4e9-280">Obvykle se nekomprimuje halda pro velké objekty (LOH), protože kopírování velkých objektů ukládá snížení výkonu.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-280">Ordinarily, the large object heap (LOH) is not compacted, because copying large objects imposes a performance penalty.</span></span> <span data-ttu-id="6c4e9-281">V rozhraní .NET Core a v .NET Framework 4.5.1 a novějších verzích ale můžete použít <xref:System.Runtime.GCSettings.LargeObjectHeapCompactionMode%2A?displayProperty=nameWithType> vlastnost k komprimaci haldy velkých objektů na vyžádání.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-281">However, in .NET Core and in .NET Framework 4.5.1 and later, you can use the <xref:System.Runtime.GCSettings.LargeObjectHeapCompactionMode%2A?displayProperty=nameWithType> property to compact the large object heap on demand.</span></span> <span data-ttu-id="6c4e9-282">Kromě toho se LOH automaticky zkomprimuje, když se nastaví pevný limit zadáním těchto znaků:</span><span class="sxs-lookup"><span data-stu-id="6c4e9-282">In addition, the LOH is automatically compacted when a hard limit is set by specifying either:</span></span>

  - <span data-ttu-id="6c4e9-283">Omezení paměti pro kontejner.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-283">A memory limit on a container.</span></span>
  - <span data-ttu-id="6c4e9-284">[GCHeapHardLimit](../../core/run-time-config/garbage-collector.md#systemgcheaphardlimitcomplus_gcheaphardlimit) nebo [GCHeapHardLimitPercent](../../core/run-time-config/garbage-collector.md#systemgcheaphardlimitpercentcomplus_gcheaphardlimitpercent) možnosti konfigurace modulu runtime.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-284">The [GCHeapHardLimit](../../core/run-time-config/garbage-collector.md#systemgcheaphardlimitcomplus_gcheaphardlimit) or [GCHeapHardLimitPercent](../../core/run-time-config/garbage-collector.md#systemgcheaphardlimitpercentcomplus_gcheaphardlimitpercent) run-time configuration options.</span></span>

<span data-ttu-id="6c4e9-285">Systém uvolňování paměti používá následující informace k určení, zda jsou objekty živé:</span><span class="sxs-lookup"><span data-stu-id="6c4e9-285">The garbage collector uses the following information to determine whether objects are live:</span></span>

- <span data-ttu-id="6c4e9-286">**Kořeny zásobníku**.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-286">**Stack roots**.</span></span> <span data-ttu-id="6c4e9-287">Proměnné zásobníku poskytované kompilátorem JIT (just-in-time) a zásobníkem Stack prohlížeč.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-287">Stack variables provided by the just-in-time (JIT) compiler and stack walker.</span></span> <span data-ttu-id="6c4e9-288">Optimalizace JIT mohou prodloužit nebo zkrátit oblasti kódu, ve kterých jsou proměnné zásobníku hlášeny do uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-288">JIT optimizations can lengthen or shorten regions of code within which stack variables are reported to the garbage collector.</span></span>

- <span data-ttu-id="6c4e9-289">**Popisovače uvolňování paměti**.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-289">**Garbage collection handles**.</span></span> <span data-ttu-id="6c4e9-290">Obslužné rutiny, které odkazují na spravované objekty a které mohou být přiděleny uživatelským kódem nebo modulem CLR (Common Language Runtime).</span><span class="sxs-lookup"><span data-stu-id="6c4e9-290">Handles that point to managed objects and that can be allocated by user code or by the common language runtime.</span></span>

- <span data-ttu-id="6c4e9-291">**Statická data**.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-291">**Static data**.</span></span> <span data-ttu-id="6c4e9-292">Statické objekty v aplikačních doménách, které by mohly odkazovat na jiné objekty.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-292">Static objects in application domains that could be referencing other objects.</span></span> <span data-ttu-id="6c4e9-293">Každá doména aplikace udržuje přehled o svých statických objektech.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-293">Each application domain keeps track of its static objects.</span></span>

<span data-ttu-id="6c4e9-294">Před spuštěním uvolňování paměti jsou všechna spravovaná vlákna pozastavena s výjimkou vlákna, které vyvolalo uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-294">Before a garbage collection starts, all managed threads are suspended except for the thread that triggered the garbage collection.</span></span>

<span data-ttu-id="6c4e9-295">Následující ilustrace znázorňuje vlákno, které spouští uvolňování paměti a způsobuje pozastavení ostatních vláken.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-295">The following illustration shows a thread that triggers a garbage collection and causes the other threads to be suspended.</span></span>

![Když vlákno spustí uvolňování paměti](media/gc-triggered.png)

## <a name="unmanaged-resources"></a><span data-ttu-id="6c4e9-297">Nespravované prostředky</span><span class="sxs-lookup"><span data-stu-id="6c4e9-297">Unmanaged resources</span></span>

<span data-ttu-id="6c4e9-298">Pro většinu objektů, které vaše aplikace vytvoří, můžete spoléhat na uvolňování paměti, aby se automaticky prováděly nezbytné úlohy správy paměti.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-298">For most of the objects that your application creates, you can rely on garbage collection to automatically perform the necessary memory management tasks.</span></span> <span data-ttu-id="6c4e9-299">Nespravované prostředky však vyžadují explicitní vyčištění.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-299">However, unmanaged resources require explicit cleanup.</span></span> <span data-ttu-id="6c4e9-300">Nejběžnějším typem nespravovaného prostředku je objekt, který balí prostředek operačního systému, jako je například popisovač souboru, popisovač okna nebo síťové připojení.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-300">The most common type of unmanaged resource is an object that wraps an operating system resource, such as a file handle, window handle, or network connection.</span></span> <span data-ttu-id="6c4e9-301">I když je systém uvolňování paměti schopný sledovat životnost spravovaného objektu, který zapouzdřuje nespravovaný prostředek, nemá specifické znalosti o tom, jak prostředek vyčistit.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-301">Although the garbage collector is able to track the lifetime of a managed object that encapsulates an unmanaged resource, it doesn't have specific knowledge about how to clean up the resource.</span></span>

<span data-ttu-id="6c4e9-302">Při vytváření objektu, který zapouzdřuje nespravovaný prostředek, je doporučeno zadat potřebný kód pro vyčištění nespravovaného prostředku ve veřejné `Dispose` metodě.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-302">When you create an object that encapsulates an unmanaged resource, it's recommended that you provide the necessary code to clean up the unmanaged resource in a public `Dispose` method.</span></span> <span data-ttu-id="6c4e9-303">Poskytnutím `Dispose` metody umožníte uživatelům vašeho objektu explicitně uvolnit svou paměť, až se dokončí s objektem.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-303">By providing a `Dispose` method, you enable users of your object to explicitly free its memory when they are finished with the object.</span></span> <span data-ttu-id="6c4e9-304">Při použití objektu, který zapouzdřuje nespravovaný prostředek, se ujistěte, že zavoláte `Dispose` podle potřeby.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-304">When you use an object that encapsulates an unmanaged resource, make sure to call `Dispose` as necessary.</span></span>

<span data-ttu-id="6c4e9-305">Musíte také zadat způsob, jakým se mají nespravované prostředky uvolnit pro případ, že příjemce vašeho typu zapomene volání `Dispose` .</span><span class="sxs-lookup"><span data-stu-id="6c4e9-305">You must also provide a way for your unmanaged resources to be released in case a consumer of your type forgets to call `Dispose`.</span></span> <span data-ttu-id="6c4e9-306">Můžete buď použít bezpečný popisovač k zabalení nespravovaného prostředku, nebo přepsat <xref:System.Object.Finalize?displayProperty=nameWithType> metodu.</span><span class="sxs-lookup"><span data-stu-id="6c4e9-306">You can either use a safe handle to wrap the unmanaged resource, or override the <xref:System.Object.Finalize?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="6c4e9-307">Další informace o čištění nespravovaných prostředků najdete v tématu [vyčištění nespravovaných prostředků](unmanaged.md).</span><span class="sxs-lookup"><span data-stu-id="6c4e9-307">For more information about cleaning up unmanaged resources, see [Clean up unmanaged resources](unmanaged.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="6c4e9-308">Viz také</span><span class="sxs-lookup"><span data-stu-id="6c4e9-308">See also</span></span>

- [<span data-ttu-id="6c4e9-309">Uvolnění paměti pracovní stanice a serveru</span><span class="sxs-lookup"><span data-stu-id="6c4e9-309">Workstation and server garbage collection</span></span>](workstation-server-gc.md)
- [<span data-ttu-id="6c4e9-310">Uvolňování paměti na pozadí</span><span class="sxs-lookup"><span data-stu-id="6c4e9-310">Background garbage collection</span></span>](background-gc.md)
- [<span data-ttu-id="6c4e9-311">Možnosti konfigurace pro GC</span><span class="sxs-lookup"><span data-stu-id="6c4e9-311">Configuration options for GC</span></span>](../../core/run-time-config/garbage-collector.md)
- [<span data-ttu-id="6c4e9-312">Uvolnění paměti</span><span class="sxs-lookup"><span data-stu-id="6c4e9-312">Garbage collection</span></span>](index.md)
