---
title: Základy uvolňování paměti
description: Zjistěte, jak funguje systém uvolňování paměti a jak jej lze nakonfigurovat pro optimální výkon.
ms.date: 11/15/2019
ms.technology: dotnet-standard
helpviewer_keywords:
- garbage collection, generations
- garbage collection, background
- garbage collection, concurrent
- garbage collection, server
- garbage collection, workstation
- garbage collection, managed heap
ms.assetid: 67c5a20d-1be1-4ea7-8a9a-92b0b08658d2
ms.openlocfilehash: 1fdf7fcd61fb4bf9e8e0cbfe28842208f6eadd00
ms.sourcegitcommit: 73aa9653547a1cd70ee6586221f79cc29b588ebd
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/23/2020
ms.locfileid: "82102434"
---
# <a name="fundamentals-of-garbage-collection"></a><span data-ttu-id="41833-103">Základy uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="41833-103">Fundamentals of garbage collection</span></span>

<span data-ttu-id="41833-104">V clr (COMMON Language runtime) slouží systém uvolňování paměti jako automatický správce paměti.</span><span class="sxs-lookup"><span data-stu-id="41833-104">In the common language runtime (CLR), the garbage collector (GC) serves as an automatic memory manager.</span></span> <span data-ttu-id="41833-105">Systém uvolňování paměti spravuje přidělení a uvolnění paměti pro aplikaci.</span><span class="sxs-lookup"><span data-stu-id="41833-105">The garbage collector manages the allocation and release of memory for an application.</span></span> <span data-ttu-id="41833-106">Pro vývojáře pracující se spravovaným kódem to znamená, že nemusíte psát kód k provádění úloh správy paměti.</span><span class="sxs-lookup"><span data-stu-id="41833-106">For developers working with managed code, this means that you don't have to write code to perform memory management tasks.</span></span> <span data-ttu-id="41833-107">Automatická správa paměti může eliminovat běžné problémy, jako je například zapomínání na uvolnění objektu a příčinou nevracení paměti nebo pokusu o přístup k paměti pro objekt, který již byl uvolněn.</span><span class="sxs-lookup"><span data-stu-id="41833-107">Automatic memory management can eliminate common problems, such as forgetting to free an object and causing a memory leak or attempting to access memory for an object that's already been freed.</span></span>

<span data-ttu-id="41833-108">Tento článek popisuje základní koncepty uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="41833-108">This article describes the core concepts of garbage collection.</span></span>

## <a name="benefits"></a><span data-ttu-id="41833-109">Výhody</span><span class="sxs-lookup"><span data-stu-id="41833-109">Benefits</span></span>

<span data-ttu-id="41833-110">Systém uvolňování paměti poskytuje následující výhody:</span><span class="sxs-lookup"><span data-stu-id="41833-110">The garbage collector provides the following benefits:</span></span>

- <span data-ttu-id="41833-111">Osvobozuje vývojáře od nutnosti ručně uvolnit paměť.</span><span class="sxs-lookup"><span data-stu-id="41833-111">Frees developers from having to manually release memory.</span></span>

- <span data-ttu-id="41833-112">Přiděluje objekty na spravované haldy efektivně.</span><span class="sxs-lookup"><span data-stu-id="41833-112">Allocates objects on the managed heap efficiently.</span></span>

- <span data-ttu-id="41833-113">Uvolní objekty, které se již nepoužívají, vymaže jejich paměť a udržuje paměť k dispozici pro budoucí přidělení.</span><span class="sxs-lookup"><span data-stu-id="41833-113">Reclaims objects that are no longer being used, clears their memory, and keeps the memory available for future allocations.</span></span> <span data-ttu-id="41833-114">Spravované objekty automaticky získat čistý obsah začít, takže jejich konstruktory nemusí inicializovat každé datové pole.</span><span class="sxs-lookup"><span data-stu-id="41833-114">Managed objects automatically get clean content to start with, so their constructors don't have to initialize every data field.</span></span>

- <span data-ttu-id="41833-115">Poskytuje bezpečnost paměti tím, že se ujistí, že objekt nemůže použít obsah jiného objektu.</span><span class="sxs-lookup"><span data-stu-id="41833-115">Provides memory safety by making sure that an object cannot use the content of another object.</span></span>

## <a name="fundamentals-of-memory"></a><span data-ttu-id="41833-116">Základy paměti</span><span class="sxs-lookup"><span data-stu-id="41833-116">Fundamentals of memory</span></span>

<span data-ttu-id="41833-117">Následující seznam shrnuje důležité koncepty paměti CLR.</span><span class="sxs-lookup"><span data-stu-id="41833-117">The following list summarizes important CLR memory concepts.</span></span>

- <span data-ttu-id="41833-118">Každý proces má svůj vlastní, samostatný virtuální adresní prostor.</span><span class="sxs-lookup"><span data-stu-id="41833-118">Each process has its own, separate virtual address space.</span></span> <span data-ttu-id="41833-119">Všechny procesy ve stejném počítači sdílejí stejnou fyzickou paměť a stránkovací soubor, pokud existuje.</span><span class="sxs-lookup"><span data-stu-id="41833-119">All processes on the same computer share the same physical memory and the page file, if there is one.</span></span>

- <span data-ttu-id="41833-120">Ve výchozím nastavení má každý proces v 32bitových počítačích virtuální adresní prostor v uživatelském režimu 2 GB.</span><span class="sxs-lookup"><span data-stu-id="41833-120">By default, on 32-bit computers, each process has a 2-GB user-mode virtual address space.</span></span>

- <span data-ttu-id="41833-121">Jako vývojář aplikace pracujete pouze s virtuálním adresovým prostorem a nikdy nemanipulujete s fyzickou pamětí přímo.</span><span class="sxs-lookup"><span data-stu-id="41833-121">As an application developer, you work only with virtual address space and never manipulate physical memory directly.</span></span> <span data-ttu-id="41833-122">Systém uvolňování paměti přiděluje a uvolňuje virtuální paměť pro vás na spravované haldy.</span><span class="sxs-lookup"><span data-stu-id="41833-122">The garbage collector allocates and frees virtual memory for you on the managed heap.</span></span>

  <span data-ttu-id="41833-123">Pokud píšete nativní kód, můžete používat funkce systému Windows pro práci s virtuálním adresovým prostorem.</span><span class="sxs-lookup"><span data-stu-id="41833-123">If you're writing native code, you use Windows functions to work with the virtual address space.</span></span> <span data-ttu-id="41833-124">Tyto funkce přidělit a uvolnit virtuální paměť pro vás na nativní hromady.</span><span class="sxs-lookup"><span data-stu-id="41833-124">These functions allocate and free virtual memory for you on native heaps.</span></span>

- <span data-ttu-id="41833-125">Virtuální paměť může být ve třech stavech:</span><span class="sxs-lookup"><span data-stu-id="41833-125">Virtual memory can be in three states:</span></span>

  | <span data-ttu-id="41833-126">Stav</span><span class="sxs-lookup"><span data-stu-id="41833-126">State</span></span> | <span data-ttu-id="41833-127">Popis</span><span class="sxs-lookup"><span data-stu-id="41833-127">Description</span></span> |
  |---------|---------|
  | <span data-ttu-id="41833-128">Free</span><span class="sxs-lookup"><span data-stu-id="41833-128">Free</span></span> | <span data-ttu-id="41833-129">Blok paměti nemá žádné odkazy na něj a je k dispozici pro přidělení.</span><span class="sxs-lookup"><span data-stu-id="41833-129">The block of memory has no references to it and is available for allocation.</span></span> |
  | <span data-ttu-id="41833-130">Vyhrazeno</span><span class="sxs-lookup"><span data-stu-id="41833-130">Reserved</span></span> | <span data-ttu-id="41833-131">Blok paměti je k dispozici pro vaše použití a nelze jej použít pro žádný jiný požadavek na přidělení.</span><span class="sxs-lookup"><span data-stu-id="41833-131">The block of memory is available for your use and cannot be used for any other allocation request.</span></span> <span data-ttu-id="41833-132">Však nelze ukládat data do tohoto bloku paměti, dokud je potvrzena.</span><span class="sxs-lookup"><span data-stu-id="41833-132">However, you cannot store data to this memory block until it is committed.</span></span> |
  | <span data-ttu-id="41833-133">Committed</span><span class="sxs-lookup"><span data-stu-id="41833-133">Committed</span></span> | <span data-ttu-id="41833-134">Blok paměti je přiřazen fyzickému úložišti.</span><span class="sxs-lookup"><span data-stu-id="41833-134">The block of memory is assigned to physical storage.</span></span> |

- <span data-ttu-id="41833-135">Virtuální adresní prostor může být fragmentován.</span><span class="sxs-lookup"><span data-stu-id="41833-135">Virtual address space can get fragmented.</span></span> <span data-ttu-id="41833-136">To znamená, že v adresním prostoru jsou volné bloky, známé také jako díry.</span><span class="sxs-lookup"><span data-stu-id="41833-136">This means that there are free blocks, also known as holes, in the address space.</span></span> <span data-ttu-id="41833-137">Je-li požadováno přidělení virtuální paměti, správce virtuální paměti musí najít jeden volný blok, který je dostatečně velký, aby splňoval tento požadavek na přidělení.</span><span class="sxs-lookup"><span data-stu-id="41833-137">When a virtual memory allocation is requested, the virtual memory manager has to find a single free block that is large enough to satisfy that allocation request.</span></span> <span data-ttu-id="41833-138">I v případě, že máte 2 GB volného místa, přidělení, které vyžaduje 2 GB bude neúspěšné, pokud všechny, které volné místo je v jednom bloku adresy.</span><span class="sxs-lookup"><span data-stu-id="41833-138">Even if you have 2 GB of free space, an allocation that requires 2 GB will be unsuccessful unless all of that free space is in a single address block.</span></span>

- <span data-ttu-id="41833-139">Pokud není dostatek virtuálního adresového prostoru k rezervaci nebo fyzického místa k potvrzení, můžete dojít paměť.</span><span class="sxs-lookup"><span data-stu-id="41833-139">You can run out of memory if there isn't enough virtual address space to reserve or physical space to commit.</span></span>

  <span data-ttu-id="41833-140">Stránkovací soubor se používá i v případě, že fyzický tlak paměti (tj. poptávka po fyzické paměti) je nízká.</span><span class="sxs-lookup"><span data-stu-id="41833-140">The page file is used even if physical memory pressure (that is, demand for physical memory) is low.</span></span> <span data-ttu-id="41833-141">Při prvním fyzickém tlaku paměti je vysoká, operační systém musí vytvořit místo ve fyzické paměti pro ukládání dat a zálohuje některá data, která je ve fyzické paměti do stránkovacího souboru.</span><span class="sxs-lookup"><span data-stu-id="41833-141">The first time physical memory pressure is high, the operating system must make room in physical memory to store data, and it backs up some of the data that is in physical memory to the page file.</span></span> <span data-ttu-id="41833-142">Tato data nejsou stránkována, dokud není potřeba, takže je možné se setkat stránkování v situacích, kdy je nízký tlak fyzické paměti.</span><span class="sxs-lookup"><span data-stu-id="41833-142">That data is not paged until it's needed, so it's possible to encounter paging in situations where the physical memory pressure is low.</span></span>
  
### <a name="memory-allocation"></a><span data-ttu-id="41833-143">Přidělení paměti</span><span class="sxs-lookup"><span data-stu-id="41833-143">Memory allocation</span></span>

<span data-ttu-id="41833-144">Při inicializaci nového procesu si zaběhu rezervuje souvislou oblast adresního prostoru pro proces.</span><span class="sxs-lookup"><span data-stu-id="41833-144">When you initialize a new process, the runtime reserves a contiguous region of address space for the process.</span></span> <span data-ttu-id="41833-145">Tento vyhrazený adresní prostor se nazývá spravovaná halda.</span><span class="sxs-lookup"><span data-stu-id="41833-145">This reserved address space is called the managed heap.</span></span> <span data-ttu-id="41833-146">Spravovaná halda udržuje ukazatel na adresu, kde bude přidělen další objekt v haldě.</span><span class="sxs-lookup"><span data-stu-id="41833-146">The managed heap maintains a pointer to the address where the next object in the heap will be allocated.</span></span> <span data-ttu-id="41833-147">Zpočátku tento ukazatel je nastavena na základní adresu spravované haldy.</span><span class="sxs-lookup"><span data-stu-id="41833-147">Initially, this pointer is set to the managed heap's base address.</span></span> <span data-ttu-id="41833-148">Všechny typy odkazů jsou přiděleny na spravované haldy.</span><span class="sxs-lookup"><span data-stu-id="41833-148">All reference types are allocated on the managed heap.</span></span> <span data-ttu-id="41833-149">Když aplikace vytvoří první typ odkazu, je přidělena paměť pro typ na základní adrese spravované haldy.</span><span class="sxs-lookup"><span data-stu-id="41833-149">When an application creates the first reference type, memory is allocated for the type at the base address of the managed heap.</span></span> <span data-ttu-id="41833-150">Když aplikace vytvoří další objekt, systém uvolňování paměti pro něj přiděluje paměť v adresním prostoru bezprostředně za prvním objektem.</span><span class="sxs-lookup"><span data-stu-id="41833-150">When the application creates the next object, the garbage collector allocates memory for it in the address space immediately following the first object.</span></span> <span data-ttu-id="41833-151">Tak dlouho, dokud adresní prostor je k dispozici, systém uvolňování paměti nadále přidělovat prostor pro nové objekty tímto způsobem.</span><span class="sxs-lookup"><span data-stu-id="41833-151">As long as address space is available, the garbage collector continues to allocate space for new objects in this manner.</span></span>

<span data-ttu-id="41833-152">Přidělení paměti ze spravované haldy je rychlejší než přidělení nespravované paměti.</span><span class="sxs-lookup"><span data-stu-id="41833-152">Allocating memory from the managed heap is faster than unmanaged memory allocation.</span></span> <span data-ttu-id="41833-153">Vzhledem k tomu, že runtime přiděluje paměť pro objekt přidáním hodnoty k ukazateli, je téměř stejně rychlý jako přidělení paměti ze zásobníku.</span><span class="sxs-lookup"><span data-stu-id="41833-153">Because the runtime allocates memory for an object by adding a value to a pointer, it's almost as fast as allocating memory from the stack.</span></span> <span data-ttu-id="41833-154">Navíc vzhledem k tomu, že nové objekty, které jsou přiděleny postupně, jsou uloženy souvislí ve spravované haldě, aplikace může rychle přistupovat k objektům.</span><span class="sxs-lookup"><span data-stu-id="41833-154">In addition, because new objects that are allocated consecutively are stored contiguously in the managed heap, an application can access the objects quickly.</span></span>

### <a name="memory-release"></a><span data-ttu-id="41833-155">Uvolnění paměti</span><span class="sxs-lookup"><span data-stu-id="41833-155">Memory release</span></span>

<span data-ttu-id="41833-156">Optimalizační modul systému uvolňování paměti určuje nejlepší čas k provedení kolekce na základě provedených přidělení.</span><span class="sxs-lookup"><span data-stu-id="41833-156">The garbage collector's optimizing engine determines the best time to perform a collection based on the allocations being made.</span></span> <span data-ttu-id="41833-157">Když systém uvolňování paměti provádí kolekci, uvolní paměť pro objekty, které již nejsou používány aplikací.</span><span class="sxs-lookup"><span data-stu-id="41833-157">When the garbage collector performs a collection, it releases the memory for objects that are no longer being used by the application.</span></span> <span data-ttu-id="41833-158">Určuje, které objekty se již nepoužívají, a to zkoumáním *kořenů*aplikace .</span><span class="sxs-lookup"><span data-stu-id="41833-158">It determines which objects are no longer being used by examining the application's *roots*.</span></span> <span data-ttu-id="41833-159">Kořeny aplikace zahrnují statická pole, místní proměnné a parametry v zásobníku vlákna a registry procesoru.</span><span class="sxs-lookup"><span data-stu-id="41833-159">An application's roots include static fields, local variables and parameters on a thread's stack, and CPU registers.</span></span> <span data-ttu-id="41833-160">Každý kořen odkazuje na objekt na spravované haldě nebo je nastavenna na hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="41833-160">Each root either refers to an object on the managed heap or is set to null.</span></span> <span data-ttu-id="41833-161">Systém uvolňování paměti má přístup k seznamu aktivních kořenů, které kompilátor just-in-time (JIT) a za běhu udržují.</span><span class="sxs-lookup"><span data-stu-id="41833-161">The garbage collector has access to the list of active roots that the just-in-time (JIT) compiler and the runtime maintain.</span></span> <span data-ttu-id="41833-162">Pomocí tohoto seznamu systém uvolňování paměti vytvoří graf, který obsahuje všechny objekty, které jsou dosažitelné z kořenů.</span><span class="sxs-lookup"><span data-stu-id="41833-162">Using this list, the garbage collector creates a graph that contains all the objects that are reachable from the roots.</span></span>

<span data-ttu-id="41833-163">Objekty, které nejsou v grafu jsou nedostupné z kořenů aplikace.</span><span class="sxs-lookup"><span data-stu-id="41833-163">Objects that are not in the graph are unreachable from the application's roots.</span></span> <span data-ttu-id="41833-164">Systém uvolňování paměti považuje nedosažitelné objekty za nesmyslné a uvolní paměť, která je pro ně přidělena.</span><span class="sxs-lookup"><span data-stu-id="41833-164">The garbage collector considers unreachable objects garbage and releases the memory allocated for them.</span></span> <span data-ttu-id="41833-165">Během kolekce systém uvolňování paměti zkoumá spravované haldy, hledá bloky adresního prostoru obsazené nedosažitelné objekty.</span><span class="sxs-lookup"><span data-stu-id="41833-165">During a collection, the garbage collector examines the managed heap, looking for the blocks of address space occupied by unreachable objects.</span></span> <span data-ttu-id="41833-166">Při zjišťování každého nedosažitelného objektu používá funkci kopírování paměti k komprimaci dosažitelných objektů v paměti a uvolňuje bloky adresních prostorů přidělených nedostupným objektům.</span><span class="sxs-lookup"><span data-stu-id="41833-166">As it discovers each unreachable object, it uses a memory-copying function to compact the reachable objects in memory, freeing up the blocks of address spaces allocated to unreachable objects.</span></span> <span data-ttu-id="41833-167">Jakmile paměť pro dosažitelné objekty byla zkomprimována, systém uvolňování paměti provede potřebné opravy ukazatele tak, aby kořeny aplikace odkazují na objekty v jejich nových umístěních.</span><span class="sxs-lookup"><span data-stu-id="41833-167">Once the memory for the reachable objects has been compacted, the garbage collector makes the necessary pointer corrections so that the application's roots point to the objects in their new locations.</span></span> <span data-ttu-id="41833-168">Také umístí ukazatele spravované haldy za poslední dosažitelný objekt.</span><span class="sxs-lookup"><span data-stu-id="41833-168">It also positions the managed heap's pointer after the last reachable object.</span></span>

<span data-ttu-id="41833-169">Paměť je komprimována pouze v případě, že kolekce zjistí významný počet nedostupných objektů.</span><span class="sxs-lookup"><span data-stu-id="41833-169">Memory is compacted only if a collection discovers a significant number of unreachable objects.</span></span> <span data-ttu-id="41833-170">Pokud všechny objekty ve spravované haldy přežít kolekce, pak není nutné pro zhutňování paměti.</span><span class="sxs-lookup"><span data-stu-id="41833-170">If all the objects in the managed heap survive a collection, then there is no need for memory compaction.</span></span>

<span data-ttu-id="41833-171">Chcete-li zlepšit výkon, runtime přiděluje paměť pro velké objekty v samostatné haldy.</span><span class="sxs-lookup"><span data-stu-id="41833-171">To improve performance, the runtime allocates memory for large objects in a separate heap.</span></span> <span data-ttu-id="41833-172">Systém uvolňování paměti automaticky uvolní paměť pro velké objekty.</span><span class="sxs-lookup"><span data-stu-id="41833-172">The garbage collector automatically releases the memory for large objects.</span></span> <span data-ttu-id="41833-173">Chcete-li se však vyhnout přesouvání velkých objektů v paměti, tato paměť obvykle není komprimována.</span><span class="sxs-lookup"><span data-stu-id="41833-173">However, to avoid moving large objects in memory, this memory is usually not compacted.</span></span>

## <a name="conditions-for-a-garbage-collection"></a><span data-ttu-id="41833-174">Podmínky pro uvolnění paměti</span><span class="sxs-lookup"><span data-stu-id="41833-174">Conditions for a garbage collection</span></span>

<span data-ttu-id="41833-175">Uvolnění paměti dochází, pokud je splněna jedna z následujících podmínek:</span><span class="sxs-lookup"><span data-stu-id="41833-175">Garbage collection occurs when one of the following conditions is true:</span></span>

- <span data-ttu-id="41833-176">Systém má nedostatek fyzické paměti.</span><span class="sxs-lookup"><span data-stu-id="41833-176">The system has low physical memory.</span></span> <span data-ttu-id="41833-177">To je detekováno oznámením nedostatku paměti z operačního systému nebo nedostatku paměti, jak je uvedeno hostitelem.</span><span class="sxs-lookup"><span data-stu-id="41833-177">This is detected by either the low memory notification from the OS or low memory as indicated by the host.</span></span>

- <span data-ttu-id="41833-178">Paměť, která je používána přidělené objekty na spravované haldy překračuje přijatelnou prahovou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="41833-178">The memory that's used by allocated objects on the managed heap surpasses an acceptable threshold.</span></span> <span data-ttu-id="41833-179">Tato prahová hodnota je průběžně upravována při spuštění procesu.</span><span class="sxs-lookup"><span data-stu-id="41833-179">This threshold is continuously adjusted as the process runs.</span></span>

- <span data-ttu-id="41833-180">Metoda <xref:System.GC.Collect%2A?displayProperty=nameWithType> je volána.</span><span class="sxs-lookup"><span data-stu-id="41833-180">The <xref:System.GC.Collect%2A?displayProperty=nameWithType> method is called.</span></span> <span data-ttu-id="41833-181">Téměř ve všech případech není třeba volat tuto metodu, protože uvolňování systém běží nepřetržitě.</span><span class="sxs-lookup"><span data-stu-id="41833-181">In almost all cases, you don't have to call this method, because the garbage collector runs continuously.</span></span> <span data-ttu-id="41833-182">Tato metoda se používá především pro jedinečné situace a testování.</span><span class="sxs-lookup"><span data-stu-id="41833-182">This method is primarily used for unique situations and testing.</span></span>

## <a name="the-managed-heap"></a><span data-ttu-id="41833-183">Spravovanou haldu</span><span class="sxs-lookup"><span data-stu-id="41833-183">The managed heap</span></span>

<span data-ttu-id="41833-184">Po systému uvolňování paměti je inicializován clr, přiděluje segment paměti pro ukládání a správu objektů.</span><span class="sxs-lookup"><span data-stu-id="41833-184">After the garbage collector is initialized by the CLR, it allocates a segment of memory to store and manage objects.</span></span> <span data-ttu-id="41833-185">Tato paměť se nazývá spravované haldy, na rozdíl od nativní haldy v operačním systému.</span><span class="sxs-lookup"><span data-stu-id="41833-185">This memory is called the managed heap, as opposed to a native heap in the operating system.</span></span>

<span data-ttu-id="41833-186">Pro každý spravovaný proces existuje spravovaná halda.</span><span class="sxs-lookup"><span data-stu-id="41833-186">There is a managed heap for each managed process.</span></span> <span data-ttu-id="41833-187">Všechna vlákna v procesu přidělit paměť pro objekty na stejné haldě.</span><span class="sxs-lookup"><span data-stu-id="41833-187">All threads in the process allocate memory for objects on the same heap.</span></span>

<span data-ttu-id="41833-188">Chcete-li rezervovat paměť, systém uvolňování paměti volá funkci Windows [VirtualAlloc](/windows/desktop/api/memoryapi/nf-memoryapi-virtualalloc) a rezervuje jeden segment paměti najednou pro spravované aplikace.</span><span class="sxs-lookup"><span data-stu-id="41833-188">To reserve memory, the garbage collector calls the Windows [VirtualAlloc](/windows/desktop/api/memoryapi/nf-memoryapi-virtualalloc) function and reserves one segment of memory at a time for managed applications.</span></span> <span data-ttu-id="41833-189">Systém uvolňování paměti také podle potřeby rezervuje segmenty a uvolní segmenty zpět do operačního systému (po jejich vymazání všech objektů) voláním funkce Windows [VirtualFree.](/windows/desktop/api/memoryapi/nf-memoryapi-virtualfree)</span><span class="sxs-lookup"><span data-stu-id="41833-189">The garbage collector also reserves segments, as needed, and releases segments back to the operating system (after clearing them of any objects) by calling the Windows [VirtualFree](/windows/desktop/api/memoryapi/nf-memoryapi-virtualfree) function.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="41833-190">Velikost segmentů přidělených systémem uvolňování paměti je specifická pro implementaci a může se kdykoli změnit, včetně pravidelných aktualizací.</span><span class="sxs-lookup"><span data-stu-id="41833-190">The size of segments allocated by the garbage collector is implementation-specific and is subject to change at any time, including in periodic updates.</span></span> <span data-ttu-id="41833-191">Vaše aplikace by nikdy neměla dělat předpoklady o konkrétní velikosti segmentu nebo na ní záviset, ani by se neměla pokoušet nakonfigurovat množství paměti, která je k dispozici pro přidělení segmentu.</span><span class="sxs-lookup"><span data-stu-id="41833-191">Your app should never make assumptions about or depend on a particular segment size, nor should it attempt to configure the amount of memory available for segment allocations.</span></span>

<span data-ttu-id="41833-192">Méně objektů přidělených na haldě, méně práce systému uvolňování paměti musí provést.</span><span class="sxs-lookup"><span data-stu-id="41833-192">The fewer objects allocated on the heap, the less work the garbage collector has to do.</span></span> <span data-ttu-id="41833-193">Při přidělování objektů nepoužívejte hodnoty zaobleného nahoru, které přesahují vaše potřeby, například přidělení pole 32 bajtů, když potřebujete pouze 15 bajtů.</span><span class="sxs-lookup"><span data-stu-id="41833-193">When you allocate objects, don't use rounded-up values that exceed your needs, such as allocating an array of 32 bytes when you need only 15 bytes.</span></span>

<span data-ttu-id="41833-194">Při uvolnění paměti je spuštěna, uvolňování paměti uvolní paměť, která je obsazena mrtvé objekty.</span><span class="sxs-lookup"><span data-stu-id="41833-194">When a garbage collection is triggered, the garbage collector reclaims the memory that's occupied by dead objects.</span></span> <span data-ttu-id="41833-195">Proces rekultivace komprimuje živé objekty tak, aby byly přesunuty společně a mrtvý prostor je odebrán, čímž se halda menší.</span><span class="sxs-lookup"><span data-stu-id="41833-195">The reclaiming process compacts live objects so that they are moved together, and the dead space is removed, thereby making the heap smaller.</span></span> <span data-ttu-id="41833-196">Tím je zajištěno, že objekty, které jsou přiděleny společně zůstat společně na spravované haldy zachovat jejich lokalitu.</span><span class="sxs-lookup"><span data-stu-id="41833-196">This ensures that objects that are allocated together stay together on the managed heap to preserve their locality.</span></span>

<span data-ttu-id="41833-197">Rušivost (frekvence a trvání) uvolňování paměti je výsledkem objemu přidělení a množství paměti, která přežila na spravované haldě.</span><span class="sxs-lookup"><span data-stu-id="41833-197">The intrusiveness (frequency and duration) of garbage collections is the result of the volume of allocations and the amount of survived memory on the managed heap.</span></span>

<span data-ttu-id="41833-198">Haldy lze považovat za akumulaci dvou hald: [haldy velkého objektu](large-object-heap.md) a haldy malého objektu.</span><span class="sxs-lookup"><span data-stu-id="41833-198">The heap can be considered as the accumulation of two heaps: the [large object heap](large-object-heap.md) and the small object heap.</span></span> <span data-ttu-id="41833-199">Halda velkých objektů obsahuje objekty, které jsou 85 000 bajtů a větší, což jsou obvykle pole.</span><span class="sxs-lookup"><span data-stu-id="41833-199">The large object heap contains objects that are 85,000 bytes and larger, which are usually arrays.</span></span> <span data-ttu-id="41833-200">Je vzácné, aby byl objekt instance extrémně velký.</span><span class="sxs-lookup"><span data-stu-id="41833-200">It's rare for an instance object to be extremely large.</span></span>

> [!TIP]
> <span data-ttu-id="41833-201">Můžete [nakonfigurovat velikost prahové hodnoty](../../core/run-time-config/garbage-collector.md#large-object-heap-threshold) pro objekty jít na haldy velkého objektu.</span><span class="sxs-lookup"><span data-stu-id="41833-201">You can [configure the threshold size](../../core/run-time-config/garbage-collector.md#large-object-heap-threshold) for objects to go on the large object heap.</span></span>

## <a name="generations"></a><span data-ttu-id="41833-202">Generace</span><span class="sxs-lookup"><span data-stu-id="41833-202">Generations</span></span>

<span data-ttu-id="41833-203">Algoritmus GC je založen na několika aspektech:</span><span class="sxs-lookup"><span data-stu-id="41833-203">The GC algorithm is based on several considerations:</span></span>

- <span data-ttu-id="41833-204">Je rychlejší komprimovat paměť pro část spravované haldy než pro celou spravovanou haldu.</span><span class="sxs-lookup"><span data-stu-id="41833-204">It's faster to compact the memory for a portion of the managed heap than for the entire managed heap.</span></span>
- <span data-ttu-id="41833-205">Novější objekty mají kratší životnost a starší objekty mají delší životnost.</span><span class="sxs-lookup"><span data-stu-id="41833-205">Newer objects have shorter lifetimes and older objects have longer lifetimes.</span></span>
- <span data-ttu-id="41833-206">Novější objekty mají tendenci být vzájemně propojeny a přistupovat k nim přibližně ve stejnou dobu.</span><span class="sxs-lookup"><span data-stu-id="41833-206">Newer objects tend to be related to each other and accessed by the application around the same time.</span></span>

<span data-ttu-id="41833-207">Uvolňování paměti dochází především s rekultivací krátkodobé objekty.</span><span class="sxs-lookup"><span data-stu-id="41833-207">Garbage collection primarily occurs with the reclamation of short-lived objects.</span></span> <span data-ttu-id="41833-208">Chcete-li optimalizovat výkon systému uvolňování paměti, spravované haldy je rozdělena do tří generací, 0, 1 a 2, takže může zpracovávat dlouhodobé a krátkodobé objekty samostatně.</span><span class="sxs-lookup"><span data-stu-id="41833-208">To optimize the performance of the garbage collector, the managed heap is divided into three generations, 0, 1, and 2, so it can handle long-lived and short-lived objects separately.</span></span> <span data-ttu-id="41833-209">Systém uvolňování paměti ukládá nové objekty v generaci 0.</span><span class="sxs-lookup"><span data-stu-id="41833-209">The garbage collector stores new objects in generation 0.</span></span> <span data-ttu-id="41833-210">Objekty vytvořené v rané fázi životnosti aplikace, které přežijí kolekce jsou povýšeny a uloženy v generacích 1 a 2.</span><span class="sxs-lookup"><span data-stu-id="41833-210">Objects created early in the application's lifetime that survive collections are promoted and stored in generations 1 and 2.</span></span> <span data-ttu-id="41833-211">Vzhledem k tomu, že je rychlejší komprimovat část spravované haldy než celou haldu, toto schéma umožňuje uvolňování paměti v určité generaci, nikoli uvolnění paměti pro celou spravovanou haldu pokaždé, když provádí kolekci.</span><span class="sxs-lookup"><span data-stu-id="41833-211">Because it's faster to compact a portion of the managed heap than the entire heap, this scheme allows the garbage collector to release the memory in a specific generation rather than release the memory for the entire managed heap each time it performs a collection.</span></span>

- <span data-ttu-id="41833-212">**Generace 0**.</span><span class="sxs-lookup"><span data-stu-id="41833-212">**Generation 0**.</span></span> <span data-ttu-id="41833-213">Jedná se o nejmladší generaci a obsahuje krátkodobé objekty.</span><span class="sxs-lookup"><span data-stu-id="41833-213">This is the youngest generation and contains short-lived objects.</span></span> <span data-ttu-id="41833-214">Příkladem krátkodobého objektu je dočasná proměnná.</span><span class="sxs-lookup"><span data-stu-id="41833-214">An example of a short-lived object is a temporary variable.</span></span> <span data-ttu-id="41833-215">Uvolňování paměti dochází nejčastěji v této generaci.</span><span class="sxs-lookup"><span data-stu-id="41833-215">Garbage collection occurs most frequently in this generation.</span></span>

  <span data-ttu-id="41833-216">Nově přidělené objekty tvoří novou generaci objektů a jsou implicitně generace 0 kolekce.</span><span class="sxs-lookup"><span data-stu-id="41833-216">Newly allocated objects form a new generation of objects and are implicitly generation 0 collections.</span></span> <span data-ttu-id="41833-217">Však pokud jsou velké objekty, přejdou na haldy velkého objektu v kolekci generace 2.</span><span class="sxs-lookup"><span data-stu-id="41833-217">However, if they are large objects, they go on the large object heap in a generation 2 collection.</span></span>

  <span data-ttu-id="41833-218">Většina objektů jsou uvolněny pro uvolnění paměti v generaci 0 a nepřežijí na další generaci.</span><span class="sxs-lookup"><span data-stu-id="41833-218">Most objects are reclaimed for garbage collection in generation 0 and don't survive to the next generation.</span></span>
  
  <span data-ttu-id="41833-219">Pokud se aplikace pokusí vytvořit nový objekt při generování 0 je plná, systém uvolňování paměti provede kolekci ve snaze uvolnit adresní prostor pro objekt.</span><span class="sxs-lookup"><span data-stu-id="41833-219">If an application attempts to create a new object when generation 0 is full, the garbage collector performs a collection in an attempt to free address space for the object.</span></span> <span data-ttu-id="41833-220">Uvolňování začíná zkoumáním objektů v generaci 0, nikoli všechny objekty ve spravované haldě.</span><span class="sxs-lookup"><span data-stu-id="41833-220">The garbage collector starts by examining the objects in generation 0 rather than all objects in the managed heap.</span></span> <span data-ttu-id="41833-221">Kolekce generace 0 sám často uvolní dostatek paměti, aby aplikace pokračovat ve vytváření nových objektů.</span><span class="sxs-lookup"><span data-stu-id="41833-221">A collection of generation 0 alone often reclaims enough memory to enable the application to continue creating new objects.</span></span>

- <span data-ttu-id="41833-222">**Generace 1**.</span><span class="sxs-lookup"><span data-stu-id="41833-222">**Generation 1**.</span></span> <span data-ttu-id="41833-223">Tato generace obsahuje krátkodobé objekty a slouží jako vyrovnávací paměť mezi krátkodobé objekty a dlouhodobé objekty.</span><span class="sxs-lookup"><span data-stu-id="41833-223">This generation contains short-lived objects and serves as a buffer between short-lived objects and long-lived objects.</span></span>

  <span data-ttu-id="41833-224">Po uvolňování provede kolekci generace 0, zkomprimuje paměť pro dosažitelné objekty a povýší je na generaci 1.</span><span class="sxs-lookup"><span data-stu-id="41833-224">After the garbage collector performs a collection of generation 0, it compacts the memory for the reachable objects and promotes them to generation 1.</span></span> <span data-ttu-id="41833-225">Protože objekty, které přežívají kolekce mají tendenci mít delší životnost, má smysl je povýšit na vyšší generaci.</span><span class="sxs-lookup"><span data-stu-id="41833-225">Because objects that survive collections tend to have longer lifetimes, it makes sense to promote them to a higher generation.</span></span> <span data-ttu-id="41833-226">Systém uvolňování paměti nemusí znovu prozkoumat objekty v generacích 1 a 2 pokaždé, když provede kolekci generace 0.</span><span class="sxs-lookup"><span data-stu-id="41833-226">The garbage collector doesn't have to reexamine the objects in generations 1 and 2 each time it performs a collection of generation 0.</span></span>
  
  <span data-ttu-id="41833-227">Pokud kolekce generace 0 neuvolněna dostatek paměti pro aplikaci k vytvoření nového objektu, systém uvolňování paměti může provést kolekci generace 1, pak generace 2.</span><span class="sxs-lookup"><span data-stu-id="41833-227">If a collection of generation 0 does not reclaim enough memory for the application to create a new object, the garbage collector can perform a collection of generation 1, then generation 2.</span></span> <span data-ttu-id="41833-228">Objekty v generaci 1, které přežijí kolekce jsou povýšeny na generaci 2.</span><span class="sxs-lookup"><span data-stu-id="41833-228">Objects in generation 1 that survive collections are promoted to generation 2.</span></span>

- <span data-ttu-id="41833-229">**Generace 2**.</span><span class="sxs-lookup"><span data-stu-id="41833-229">**Generation 2**.</span></span> <span data-ttu-id="41833-230">Tato generace obsahuje objekty s dlouhou životností.</span><span class="sxs-lookup"><span data-stu-id="41833-230">This generation contains long-lived objects.</span></span> <span data-ttu-id="41833-231">Příkladem objektu s dlouhou životností je objekt v serverové aplikaci, který obsahuje statická data, která jsou aktivní po dobu trvání procesu.</span><span class="sxs-lookup"><span data-stu-id="41833-231">An example of a long-lived object is an object in a server application that contains static data that's live for the duration of the process.</span></span>

  <span data-ttu-id="41833-232">Objekty v generaci 2, které přežijí kolekce zůstávají v generaci 2, dokud nejsou určeny jako nedosažitelné v budoucí kolekci.</span><span class="sxs-lookup"><span data-stu-id="41833-232">Objects in generation 2 that survive a collection remain in generation 2 until they are determined to be unreachable in a future collection.</span></span>

<span data-ttu-id="41833-233">Uvolňování paměti dojít na konkrétní generace jako podmínky rozkaz.</span><span class="sxs-lookup"><span data-stu-id="41833-233">Garbage collections occur on specific generations as conditions warrant.</span></span> <span data-ttu-id="41833-234">Shromažďování generace znamená shromažďování předmětů v této generaci a všech jejích mladších generacích.</span><span class="sxs-lookup"><span data-stu-id="41833-234">Collecting a generation means collecting objects in that generation and all its younger generations.</span></span> <span data-ttu-id="41833-235">Uvolnění paměti generace 2 je také známé jako úplné uvolnění paměti, protože uvolňuje objekty ve všech generacích (to znamená, že všechny objekty ve spravované haldě).</span><span class="sxs-lookup"><span data-stu-id="41833-235">A generation 2 garbage collection is also known as a full garbage collection, because it reclaims objects in all generations (that is, all objects in the managed heap).</span></span>

### <a name="survival-and-promotions"></a><span data-ttu-id="41833-236">Přežití a propagace</span><span class="sxs-lookup"><span data-stu-id="41833-236">Survival and promotions</span></span>

<span data-ttu-id="41833-237">Objekty, které nejsou uvolněny v uvolňování paměti jsou označovány jako survivors a jsou povýšeny na další generaci:</span><span class="sxs-lookup"><span data-stu-id="41833-237">Objects that are not reclaimed in a garbage collection are known as survivors and are promoted to the next generation:</span></span>

- <span data-ttu-id="41833-238">Objekty, které přežijí uvolnění paměti generace 0, jsou povýšeny na generaci 1.</span><span class="sxs-lookup"><span data-stu-id="41833-238">Objects that survive a generation 0 garbage collection are promoted to generation 1.</span></span>
- <span data-ttu-id="41833-239">Objekty, které přežijí uvolnění paměti generace 1 jsou povýšeny na generaci 2.</span><span class="sxs-lookup"><span data-stu-id="41833-239">Objects that survive a generation 1 garbage collection are promoted to generation 2.</span></span>
- <span data-ttu-id="41833-240">Objekty, které přežijí uvolnění paměti generace 2, zůstávají v generaci 2.</span><span class="sxs-lookup"><span data-stu-id="41833-240">Objects that survive a generation 2 garbage collection remain in generation 2.</span></span>

<span data-ttu-id="41833-241">Když systém uvolňování paměti zjistí, že míra přežití je vysoká v generaci, zvýší prahovou hodnotu přidělení pro toto generování.</span><span class="sxs-lookup"><span data-stu-id="41833-241">When the garbage collector detects that the survival rate is high in a generation, it increases the threshold of allocations for that generation.</span></span> <span data-ttu-id="41833-242">Další kolekce získá značnou velikost regenerované paměti.</span><span class="sxs-lookup"><span data-stu-id="41833-242">The next collection gets a substantial size of reclaimed memory.</span></span> <span data-ttu-id="41833-243">CLR neustále vyvažuje dvě priority: nenechat pracovní sadu aplikace příliš velké zpožděním uvolňování paměti a neumožňuje uvolnění paměti spustit příliš často.</span><span class="sxs-lookup"><span data-stu-id="41833-243">The CLR continually balances two priorities: not letting an application's working set get too large by delaying garbage collection and not letting the garbage collection run too frequently.</span></span>

### <a name="ephemeral-generations-and-segments"></a><span data-ttu-id="41833-244">Pomíjivé generace a segmenty</span><span class="sxs-lookup"><span data-stu-id="41833-244">Ephemeral generations and segments</span></span>

<span data-ttu-id="41833-245">Protože objekty v generacích 0 a 1 jsou krátkodobé, tyto generace jsou známé jako *dočasné generace*.</span><span class="sxs-lookup"><span data-stu-id="41833-245">Because objects in generations 0 and 1 are short-lived, these generations are known as the *ephemeral generations*.</span></span>

<span data-ttu-id="41833-246">Dočasné generace jsou přiděleny v segmentu paměti, který se označuje jako dočasný segment.</span><span class="sxs-lookup"><span data-stu-id="41833-246">Ephemeral generations are allocated in the memory segment that's known as the ephemeral segment.</span></span> <span data-ttu-id="41833-247">Každý nový segment získaný systémem uvolňování paměti se stane novým dočasným segmentem a obsahuje objekty, které přežily uvolnění paměti generace 0.</span><span class="sxs-lookup"><span data-stu-id="41833-247">Each new segment acquired by the garbage collector becomes the new ephemeral segment and contains the objects that survived a generation 0 garbage collection.</span></span> <span data-ttu-id="41833-248">Starý dočasný segment se stává segmentem nové generace 2.</span><span class="sxs-lookup"><span data-stu-id="41833-248">The old ephemeral segment becomes the new generation 2 segment.</span></span>

<span data-ttu-id="41833-249">Velikost dočasného segmentu se liší v závislosti na tom, zda je systém 32bitový nebo 64bitový a na typu systému uvolňování paměti, který je spuštěn[(pracovní stanice nebo server GC](workstation-server-gc.md)).</span><span class="sxs-lookup"><span data-stu-id="41833-249">The size of the ephemeral segment varies depending on whether a system is 32-bit or 64-bit and on the type of garbage collector it is running ([workstation or server GC](workstation-server-gc.md)).</span></span> <span data-ttu-id="41833-250">V následující tabulce jsou uvedeny výchozí velikosti dočasného segmentu.</span><span class="sxs-lookup"><span data-stu-id="41833-250">The following table shows the default sizes of the ephemeral segment.</span></span>

|<span data-ttu-id="41833-251">Pracovní stanice/server GC</span><span class="sxs-lookup"><span data-stu-id="41833-251">Workstation/server GC</span></span>|<span data-ttu-id="41833-252">32bitová</span><span class="sxs-lookup"><span data-stu-id="41833-252">32-bit</span></span>|<span data-ttu-id="41833-253">64bitová</span><span class="sxs-lookup"><span data-stu-id="41833-253">64-bit</span></span>|
|-|-------------|-------------|
|<span data-ttu-id="41833-254">Pracovní stanice GC</span><span class="sxs-lookup"><span data-stu-id="41833-254">Workstation GC</span></span>|<span data-ttu-id="41833-255">16 MB</span><span class="sxs-lookup"><span data-stu-id="41833-255">16 MB</span></span>|<span data-ttu-id="41833-256">256 MB</span><span class="sxs-lookup"><span data-stu-id="41833-256">256 MB</span></span>|
|<span data-ttu-id="41833-257">Server GC</span><span class="sxs-lookup"><span data-stu-id="41833-257">Server GC</span></span>|<span data-ttu-id="41833-258">64 MB</span><span class="sxs-lookup"><span data-stu-id="41833-258">64 MB</span></span>|<span data-ttu-id="41833-259">4 GB</span><span class="sxs-lookup"><span data-stu-id="41833-259">4 GB</span></span>|
|<span data-ttu-id="41833-260">Server GC s logickými procesory > 4</span><span class="sxs-lookup"><span data-stu-id="41833-260">Server GC with > 4 logical CPUs</span></span>|<span data-ttu-id="41833-261">32 MB</span><span class="sxs-lookup"><span data-stu-id="41833-261">32 MB</span></span>|<span data-ttu-id="41833-262">2 GB</span><span class="sxs-lookup"><span data-stu-id="41833-262">2 GB</span></span>|
|<span data-ttu-id="41833-263">Server GC s logickými procesory > 8</span><span class="sxs-lookup"><span data-stu-id="41833-263">Server GC with > 8 logical CPUs</span></span>|<span data-ttu-id="41833-264">16 MB</span><span class="sxs-lookup"><span data-stu-id="41833-264">16 MB</span></span>|<span data-ttu-id="41833-265">1 GB</span><span class="sxs-lookup"><span data-stu-id="41833-265">1 GB</span></span>|

<span data-ttu-id="41833-266">Dočasný segment může obsahovat objekty generace 2.</span><span class="sxs-lookup"><span data-stu-id="41833-266">The ephemeral segment can include generation 2 objects.</span></span> <span data-ttu-id="41833-267">Generace 2 objekty mohou používat více segmentů (tolik, kolik vyžaduje proces a paměť umožňuje).</span><span class="sxs-lookup"><span data-stu-id="41833-267">Generation 2 objects can use multiple segments (as many as your process requires and memory allows for).</span></span>

<span data-ttu-id="41833-268">Množství uvolněné paměti z dočasné uvolňování paměti je omezena na velikost dočasného segmentu.</span><span class="sxs-lookup"><span data-stu-id="41833-268">The amount of freed memory from an ephemeral garbage collection is limited to the size of the ephemeral segment.</span></span> <span data-ttu-id="41833-269">Množství paměti, která je uvolněna, je úměrná prostoru, který byl obsazen mrtvými objekty.</span><span class="sxs-lookup"><span data-stu-id="41833-269">The amount of memory that is freed is proportional to the space that was occupied by the dead objects.</span></span>

## <a name="what-happens-during-a-garbage-collection"></a><span data-ttu-id="41833-270">Co se stane během uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="41833-270">What happens during a garbage collection</span></span>

<span data-ttu-id="41833-271">Uvolnění paměti má následující fáze:</span><span class="sxs-lookup"><span data-stu-id="41833-271">A garbage collection has the following phases:</span></span>

- <span data-ttu-id="41833-272">Fáze značení, která vyhledá a vytvoří seznam všech živých objektů.</span><span class="sxs-lookup"><span data-stu-id="41833-272">A marking phase that finds and creates a list of all live objects.</span></span>

- <span data-ttu-id="41833-273">Fáze přemístění, která aktualizuje odkazy na objekty, které budou komprimovány.</span><span class="sxs-lookup"><span data-stu-id="41833-273">A relocating phase that updates the references to the objects that will be compacted.</span></span>

- <span data-ttu-id="41833-274">Fáze komprimace, která uvolňuje prostor obsazený mrtvými objekty a komprimuje přežívající objekty.</span><span class="sxs-lookup"><span data-stu-id="41833-274">A compacting phase that reclaims the space occupied by the dead objects and compacts the surviving objects.</span></span> <span data-ttu-id="41833-275">Fáze komprimace přesune objekty, které přežily uvolňování paměti směrem ke staršímu konci segmentu.</span><span class="sxs-lookup"><span data-stu-id="41833-275">The compacting phase moves objects that have survived a garbage collection toward the older end of the segment.</span></span>

  <span data-ttu-id="41833-276">Vzhledem k tomu, že kolekce generace 2 mohou zabírat více segmentů, objekty, které jsou povýšeny na generaci 2, lze přesunout do staršího segmentu.</span><span class="sxs-lookup"><span data-stu-id="41833-276">Because generation 2 collections can occupy multiple segments, objects that are promoted into generation 2 can be moved into an older segment.</span></span> <span data-ttu-id="41833-277">Generace 1 i generace 2, které přežily, mohou být přesunuty do jiného segmentu, protože jsou povýšeni na generaci 2.</span><span class="sxs-lookup"><span data-stu-id="41833-277">Both generation 1 and generation 2 survivors can be moved to a different segment, because they are promoted to generation 2.</span></span>

  <span data-ttu-id="41833-278">Obvykle haldy velkých objektů (LOH) není komprimován, protože kopírování velkých objektů ukládá snížení výkonu.</span><span class="sxs-lookup"><span data-stu-id="41833-278">Ordinarily, the large object heap (LOH) is not compacted, because copying large objects imposes a performance penalty.</span></span> <span data-ttu-id="41833-279">V .NET Core a v rozhraní .NET Framework 4.5.1 <xref:System.Runtime.GCSettings.LargeObjectHeapCompactionMode%2A?displayProperty=nameWithType> a novějších však můžete vlastnost použít k komprimaci haldy velkých objektů na vyžádání.</span><span class="sxs-lookup"><span data-stu-id="41833-279">However, in .NET Core and in .NET Framework 4.5.1 and later, you can use the <xref:System.Runtime.GCSettings.LargeObjectHeapCompactionMode%2A?displayProperty=nameWithType> property to compact the large object heap on demand.</span></span> <span data-ttu-id="41833-280">Kromě toho je LOH automaticky zhutněn, když je nastaven pevný limit, a to zadáním:</span><span class="sxs-lookup"><span data-stu-id="41833-280">In addition, the LOH is automatically compacted when a hard limit is set by specifying either:</span></span>

  - <span data-ttu-id="41833-281">Omezení paměti na kontejneru.</span><span class="sxs-lookup"><span data-stu-id="41833-281">A memory limit on a container.</span></span>
  - <span data-ttu-id="41833-282">Možnosti konfigurace [GCHeapHardLimit](../../core/run-time-config/garbage-collector.md#systemgcheaphardlimitcomplus_gcheaphardlimit) nebo [GCHeapHardLimitPercent.](../../core/run-time-config/garbage-collector.md#systemgcheaphardlimitpercentcomplus_gcheaphardlimitpercent)</span><span class="sxs-lookup"><span data-stu-id="41833-282">The [GCHeapHardLimit](../../core/run-time-config/garbage-collector.md#systemgcheaphardlimitcomplus_gcheaphardlimit) or [GCHeapHardLimitPercent](../../core/run-time-config/garbage-collector.md#systemgcheaphardlimitpercentcomplus_gcheaphardlimitpercent) run-time configuration options.</span></span>

<span data-ttu-id="41833-283">Systém uvolňování paměti používá následující informace k určení, zda jsou objekty aktivní:</span><span class="sxs-lookup"><span data-stu-id="41833-283">The garbage collector uses the following information to determine whether objects are live:</span></span>

- <span data-ttu-id="41833-284">**Kořeny zásobníku**.</span><span class="sxs-lookup"><span data-stu-id="41833-284">**Stack roots**.</span></span> <span data-ttu-id="41833-285">Proměnné zásobníku poskytované kompilátorem just-in-time (JIT) a tchytačivým zásobníkem.</span><span class="sxs-lookup"><span data-stu-id="41833-285">Stack variables provided by the just-in-time (JIT) compiler and stack walker.</span></span> <span data-ttu-id="41833-286">Jit optimalizace můžete prodloužit nebo zkrátit oblasti kódu, ve kterém jsou hlášeny proměnné zásobníku do systému uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="41833-286">JIT optimizations can lengthen or shorten regions of code within which stack variables are reported to the garbage collector.</span></span>

- <span data-ttu-id="41833-287">**Popisovače uvolňování paměti**.</span><span class="sxs-lookup"><span data-stu-id="41833-287">**Garbage collection handles**.</span></span> <span data-ttu-id="41833-288">Zpracovává, které odkazují na spravované objekty a které mohou být přiděleny podle uživatelského kódu nebo běžného jazyka runtime.</span><span class="sxs-lookup"><span data-stu-id="41833-288">Handles that point to managed objects and that can be allocated by user code or by the common language runtime.</span></span>

- <span data-ttu-id="41833-289">**Statická data**.</span><span class="sxs-lookup"><span data-stu-id="41833-289">**Static data**.</span></span> <span data-ttu-id="41833-290">Statické objekty v aplikačních doménách, které mohou odkazovat na jiné objekty.</span><span class="sxs-lookup"><span data-stu-id="41833-290">Static objects in application domains that could be referencing other objects.</span></span> <span data-ttu-id="41833-291">Každá doména aplikace sleduje své statické objekty.</span><span class="sxs-lookup"><span data-stu-id="41833-291">Each application domain keeps track of its static objects.</span></span>

<span data-ttu-id="41833-292">Před spuštěním uvolňování paměti jsou všechna spravovaná vlákna pozastavena s výjimkou vlákna, které spustilo uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="41833-292">Before a garbage collection starts, all managed threads are suspended except for the thread that triggered the garbage collection.</span></span>

<span data-ttu-id="41833-293">Následující obrázek znázorňuje vlákno, které aktivuje uvolňování paměti a způsobí, že ostatní vlákna mají být pozastavena.</span><span class="sxs-lookup"><span data-stu-id="41833-293">The following illustration shows a thread that triggers a garbage collection and causes the other threads to be suspended.</span></span>

![Když vlákno aktivuje uvolňování paměti](./media/gc-triggered.png)

## <a name="unmanaged-resources"></a><span data-ttu-id="41833-295">Nespravované prostředky</span><span class="sxs-lookup"><span data-stu-id="41833-295">Unmanaged resources</span></span>

<span data-ttu-id="41833-296">Pro většinu objektů, které aplikace vytvoří, můžete se spolehnout na uvolnění paměti automaticky provádět nezbytné úlohy správy paměti.</span><span class="sxs-lookup"><span data-stu-id="41833-296">For most of the objects that your application creates, you can rely on garbage collection to automatically perform the necessary memory management tasks.</span></span> <span data-ttu-id="41833-297">Nespravované prostředky však vyžadují explicitní vyčištění.</span><span class="sxs-lookup"><span data-stu-id="41833-297">However, unmanaged resources require explicit cleanup.</span></span> <span data-ttu-id="41833-298">Nejběžnějším typem nespravovaného prostředku je objekt, který obtéká prostředek operačního systému, například popisovač souboru, popisovač okna nebo síťové připojení.</span><span class="sxs-lookup"><span data-stu-id="41833-298">The most common type of unmanaged resource is an object that wraps an operating system resource, such as a file handle, window handle, or network connection.</span></span> <span data-ttu-id="41833-299">Přestože systém uvolňování paměti je schopen sledovat životnost spravovaného objektu, který zapouzdřuje nespravovaný prostředek, nemá konkrétní znalosti o tom, jak vyčistit prostředek.</span><span class="sxs-lookup"><span data-stu-id="41833-299">Although the garbage collector is able to track the lifetime of a managed object that encapsulates an unmanaged resource, it doesn't have specific knowledge about how to clean up the resource.</span></span>

<span data-ttu-id="41833-300">Při vytváření objektu, který zapouzdřuje nespravovaný prostředek, doporučujeme zadat potřebný kód `Dispose` k vyčištění nespravovaného prostředku ve veřejné metodě.</span><span class="sxs-lookup"><span data-stu-id="41833-300">When you create an object that encapsulates an unmanaged resource, it's recommended that you provide the necessary code to clean up the unmanaged resource in a public `Dispose` method.</span></span> <span data-ttu-id="41833-301">Poskytnutím `Dispose` metody povolíte uživatelům objektu explicitně uvolnit jeho paměť po dokončení s objektem.</span><span class="sxs-lookup"><span data-stu-id="41833-301">By providing a `Dispose` method, you enable users of your object to explicitly free its memory when they are finished with the object.</span></span> <span data-ttu-id="41833-302">Při použití objektu, který zapouzdřuje nespravovaný prostředek, ujistěte se, že volání `Dispose` podle potřeby.</span><span class="sxs-lookup"><span data-stu-id="41833-302">When you use an object that encapsulates an unmanaged resource, make sure to call `Dispose` as necessary.</span></span>

<span data-ttu-id="41833-303">Je také nutné poskytnout způsob, jak nespravované prostředky, které mají být `Dispose`uvolněny v případě, že spotřebitel vašeho typu zapomene volat .</span><span class="sxs-lookup"><span data-stu-id="41833-303">You must also provide a way for your unmanaged resources to be released in case a consumer of your type forgets to call `Dispose`.</span></span> <span data-ttu-id="41833-304">Můžete buď použít bezpečný popisovač k zalomení <xref:System.Object.Finalize?displayProperty=nameWithType> nespravovaného prostředku, nebo přepsat metodu.</span><span class="sxs-lookup"><span data-stu-id="41833-304">You can either use a safe handle to wrap the unmanaged resource, or override the <xref:System.Object.Finalize?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="41833-305">Další informace o vyčištění nespravovaných prostředků naleznete v tématu [Vyčištění nespravovaných prostředků](unmanaged.md).</span><span class="sxs-lookup"><span data-stu-id="41833-305">For more information about cleaning up unmanaged resources, see [Clean up unmanaged resources](unmanaged.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="41833-306">Viz také</span><span class="sxs-lookup"><span data-stu-id="41833-306">See also</span></span>

- [<span data-ttu-id="41833-307">Uvolňování paměti pracovní stanice a serveru</span><span class="sxs-lookup"><span data-stu-id="41833-307">Workstation and server garbage collection</span></span>](workstation-server-gc.md)
- [<span data-ttu-id="41833-308">Uvolňování paměti na pozadí</span><span class="sxs-lookup"><span data-stu-id="41833-308">Background garbage collection</span></span>](background-gc.md)
- [<span data-ttu-id="41833-309">Možnosti konfigurace pro gc</span><span class="sxs-lookup"><span data-stu-id="41833-309">Configuration options for GC</span></span>](../../core/run-time-config/garbage-collector.md)
- [<span data-ttu-id="41833-310">Uvolnění paměti</span><span class="sxs-lookup"><span data-stu-id="41833-310">Garbage collection</span></span>](index.md)
