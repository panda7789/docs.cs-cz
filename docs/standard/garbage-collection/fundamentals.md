---
title: Základy uvolňování paměti
description: Zjistěte, jak funguje systém uvolňování paměti a jak jej lze nakonfigurovat pro optimální výkon.
ms.date: 11/15/2019
ms.technology: dotnet-standard
helpviewer_keywords:
- garbage collection, generations
- garbage collection, background
- garbage collection, concurrent
- garbage collection, server
- garbage collection, workstation
- garbage collection, managed heap
ms.assetid: 67c5a20d-1be1-4ea7-8a9a-92b0b08658d2
ms.openlocfilehash: ea8aef03d2f5837f35ecb31209e57853c0c8257b
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/15/2020
ms.locfileid: "79400538"
---
# <a name="fundamentals-of-garbage-collection"></a><span data-ttu-id="605ba-103">Základy uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="605ba-103">Fundamentals of garbage collection</span></span>

<span data-ttu-id="605ba-104">V clr (COMMON Language runtime) slouží systém uvolňování paměti jako automatický správce paměti.</span><span class="sxs-lookup"><span data-stu-id="605ba-104">In the common language runtime (CLR), the garbage collector (GC) serves as an automatic memory manager.</span></span> <span data-ttu-id="605ba-105">Poskytuje následující výhody:</span><span class="sxs-lookup"><span data-stu-id="605ba-105">It provides the following benefits:</span></span>

- <span data-ttu-id="605ba-106">Umožňuje vyvíjet aplikaci bez nutnosti ručně uvolnit paměť.</span><span class="sxs-lookup"><span data-stu-id="605ba-106">Enables you to develop your application without having to manually free memory.</span></span>

- <span data-ttu-id="605ba-107">Přiděluje objekty na spravované haldy efektivně.</span><span class="sxs-lookup"><span data-stu-id="605ba-107">Allocates objects on the managed heap efficiently.</span></span>

- <span data-ttu-id="605ba-108">Uvolní objekty, které se již nepoužívají, vymaže jejich paměť a udržuje paměť k dispozici pro budoucí přidělení.</span><span class="sxs-lookup"><span data-stu-id="605ba-108">Reclaims objects that are no longer being used, clears their memory, and keeps the memory available for future allocations.</span></span> <span data-ttu-id="605ba-109">Spravované objekty automaticky získat čistý obsah začít, takže jejich konstruktory nemusí inicializovat každé datové pole.</span><span class="sxs-lookup"><span data-stu-id="605ba-109">Managed objects automatically get clean content to start with, so their constructors do not have to initialize every data field.</span></span>

- <span data-ttu-id="605ba-110">Poskytuje bezpečnost paměti tím, že se ujistí, že objekt nemůže použít obsah jiného objektu.</span><span class="sxs-lookup"><span data-stu-id="605ba-110">Provides memory safety by making sure that an object cannot use the content of another object.</span></span>

<span data-ttu-id="605ba-111">Tento článek popisuje základní koncepty uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="605ba-111">This article describes the core concepts of garbage collection.</span></span>

## <a name="fundamentals-of-memory"></a><span data-ttu-id="605ba-112">Základy paměti</span><span class="sxs-lookup"><span data-stu-id="605ba-112">Fundamentals of memory</span></span>

<span data-ttu-id="605ba-113">Následující seznam shrnuje důležité koncepty paměti CLR.</span><span class="sxs-lookup"><span data-stu-id="605ba-113">The following list summarizes important CLR memory concepts.</span></span>

- <span data-ttu-id="605ba-114">Každý proces má svůj vlastní, samostatný virtuální adresní prostor.</span><span class="sxs-lookup"><span data-stu-id="605ba-114">Each process has its own, separate virtual address space.</span></span> <span data-ttu-id="605ba-115">Všechny procesy ve stejném počítači sdílejí stejnou fyzickou paměť a stránkovací soubor, pokud existuje.</span><span class="sxs-lookup"><span data-stu-id="605ba-115">All processes on the same computer share the same physical memory and the page file, if there is one.</span></span>

- <span data-ttu-id="605ba-116">Ve výchozím nastavení má každý proces v 32bitových počítačích virtuální adresní prostor v uživatelském režimu 2 GB.</span><span class="sxs-lookup"><span data-stu-id="605ba-116">By default, on 32-bit computers, each process has a 2-GB user-mode virtual address space.</span></span>

- <span data-ttu-id="605ba-117">Jako vývojář aplikace pracujete pouze s virtuálním adresovým prostorem a nikdy nemanipulujete s fyzickou pamětí přímo.</span><span class="sxs-lookup"><span data-stu-id="605ba-117">As an application developer, you work only with virtual address space and never manipulate physical memory directly.</span></span> <span data-ttu-id="605ba-118">Systém uvolňování paměti přiděluje a uvolňuje virtuální paměť pro vás na spravované haldy.</span><span class="sxs-lookup"><span data-stu-id="605ba-118">The garbage collector allocates and frees virtual memory for you on the managed heap.</span></span>

  <span data-ttu-id="605ba-119">Pokud píšete nativní kód, můžete používat funkce systému Windows pro práci s virtuálním adresovým prostorem.</span><span class="sxs-lookup"><span data-stu-id="605ba-119">If you are writing native code, you use Windows functions to work with the virtual address space.</span></span> <span data-ttu-id="605ba-120">Tyto funkce přidělit a uvolnit virtuální paměť pro vás na nativní hromady.</span><span class="sxs-lookup"><span data-stu-id="605ba-120">These functions allocate and free virtual memory for you on native heaps.</span></span>

- <span data-ttu-id="605ba-121">Virtuální paměť může být ve třech stavech:</span><span class="sxs-lookup"><span data-stu-id="605ba-121">Virtual memory can be in three states:</span></span>

  - <span data-ttu-id="605ba-122">Zdarma.</span><span class="sxs-lookup"><span data-stu-id="605ba-122">Free.</span></span> <span data-ttu-id="605ba-123">Blok paměti nemá žádné odkazy na něj a je k dispozici pro přidělení.</span><span class="sxs-lookup"><span data-stu-id="605ba-123">The block of memory has no references to it and is available for allocation.</span></span>

  - <span data-ttu-id="605ba-124">Vyhrazeno.</span><span class="sxs-lookup"><span data-stu-id="605ba-124">Reserved.</span></span> <span data-ttu-id="605ba-125">Blok paměti je k dispozici pro vaše použití a nelze jej použít pro žádný jiný požadavek na přidělení.</span><span class="sxs-lookup"><span data-stu-id="605ba-125">The block of memory is available for your use and cannot be used for any other allocation request.</span></span> <span data-ttu-id="605ba-126">Však nelze ukládat data do tohoto bloku paměti, dokud je potvrzena.</span><span class="sxs-lookup"><span data-stu-id="605ba-126">However, you cannot store data to this memory block until it is committed.</span></span>

  - <span data-ttu-id="605ba-127">Zavázala.</span><span class="sxs-lookup"><span data-stu-id="605ba-127">Committed.</span></span> <span data-ttu-id="605ba-128">Blok paměti je přiřazen fyzickému úložišti.</span><span class="sxs-lookup"><span data-stu-id="605ba-128">The block of memory is assigned to physical storage.</span></span>

- <span data-ttu-id="605ba-129">Virtuální adresní prostor může být fragmentován.</span><span class="sxs-lookup"><span data-stu-id="605ba-129">Virtual address space can get fragmented.</span></span> <span data-ttu-id="605ba-130">To znamená, že v adresním prostoru jsou volné bloky, známé také jako díry.</span><span class="sxs-lookup"><span data-stu-id="605ba-130">This means that there are free blocks, also known as holes, in the address space.</span></span> <span data-ttu-id="605ba-131">Je-li požadováno přidělení virtuální paměti, správce virtuální paměti musí najít jeden volný blok, který je dostatečně velký, aby splňoval tento požadavek na přidělení.</span><span class="sxs-lookup"><span data-stu-id="605ba-131">When a virtual memory allocation is requested, the virtual memory manager has to find a single free block that is large enough to satisfy that allocation request.</span></span> <span data-ttu-id="605ba-132">I v případě, že máte 2 GB volného místa, přidělení, které vyžaduje 2 GB bude neúspěšné, pokud všechny, které volné místo je v jednom bloku adresy.</span><span class="sxs-lookup"><span data-stu-id="605ba-132">Even if you have 2 GB of free space, the allocation that requires 2 GB will be unsuccessful unless all of that free space is in a single address block.</span></span>

- <span data-ttu-id="605ba-133">Pokud není dostatek virtuálního adresového prostoru k rezervaci nebo fyzického místa k potvrzení, můžete dojít paměť.</span><span class="sxs-lookup"><span data-stu-id="605ba-133">You can run out of memory if there isn't enough virtual address space to reserve or physical space to commit.</span></span>

  <span data-ttu-id="605ba-134">Stránkovací soubor se používá i v případě, že fyzický tlak paměti (tj. poptávka po fyzické paměti) je nízká.</span><span class="sxs-lookup"><span data-stu-id="605ba-134">The page file is used even if physical memory pressure (that is, demand for physical memory) is low.</span></span> <span data-ttu-id="605ba-135">Při prvním fyzickém tlaku paměti je vysoká, operační systém musí vytvořit místo ve fyzické paměti pro ukládání dat a zálohuje některá data, která je ve fyzické paměti do stránkovacího souboru.</span><span class="sxs-lookup"><span data-stu-id="605ba-135">The first time physical memory pressure is high, the operating system must make room in physical memory to store data, and it backs up some of the data that is in physical memory to the page file.</span></span> <span data-ttu-id="605ba-136">Tato data nejsou stránkována, dokud není potřeba, takže je možné se setkat stránkování v situacích, kdy je nízký tlak fyzické paměti.</span><span class="sxs-lookup"><span data-stu-id="605ba-136">That data is not paged until it's needed, so it's possible to encounter paging in situations where the physical memory pressure is low.</span></span>

## <a name="conditions-for-a-garbage-collection"></a><span data-ttu-id="605ba-137">Podmínky pro uvolnění paměti</span><span class="sxs-lookup"><span data-stu-id="605ba-137">Conditions for a garbage collection</span></span>

<span data-ttu-id="605ba-138">Uvolnění paměti dochází, pokud je splněna jedna z následujících podmínek:</span><span class="sxs-lookup"><span data-stu-id="605ba-138">Garbage collection occurs when one of the following conditions is true:</span></span>

- <span data-ttu-id="605ba-139">Systém má nedostatek fyzické paměti.</span><span class="sxs-lookup"><span data-stu-id="605ba-139">The system has low physical memory.</span></span> <span data-ttu-id="605ba-140">To je detekováno oznámením nedostatku paměti z operačního systému nebo nedostatku paměti, jak je uvedeno hostitelem.</span><span class="sxs-lookup"><span data-stu-id="605ba-140">This is detected by either the low memory notification from the OS or low memory as indicated by the host.</span></span>

- <span data-ttu-id="605ba-141">Paměť, která je používána přidělené objekty na spravované haldy překračuje přijatelnou prahovou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="605ba-141">The memory that is used by allocated objects on the managed heap surpasses an acceptable threshold.</span></span> <span data-ttu-id="605ba-142">Tato prahová hodnota je průběžně upravována při spuštění procesu.</span><span class="sxs-lookup"><span data-stu-id="605ba-142">This threshold is continuously adjusted as the process runs.</span></span>

- <span data-ttu-id="605ba-143">Metoda <xref:System.GC.Collect%2A?displayProperty=nameWithType> je volána.</span><span class="sxs-lookup"><span data-stu-id="605ba-143">The <xref:System.GC.Collect%2A?displayProperty=nameWithType> method is called.</span></span> <span data-ttu-id="605ba-144">Téměř ve všech případech není třeba volat tuto metodu, protože uvolňování systém běží nepřetržitě.</span><span class="sxs-lookup"><span data-stu-id="605ba-144">In almost all cases, you do not have to call this method, because the garbage collector runs continuously.</span></span> <span data-ttu-id="605ba-145">Tato metoda se používá především pro jedinečné situace a testování.</span><span class="sxs-lookup"><span data-stu-id="605ba-145">This method is primarily used for unique situations and testing.</span></span>

## <a name="the-managed-heap"></a><span data-ttu-id="605ba-146">Spravovanou haldu</span><span class="sxs-lookup"><span data-stu-id="605ba-146">The managed heap</span></span>

<span data-ttu-id="605ba-147">Po systému uvolňování paměti je inicializován clr, přiděluje segment paměti pro ukládání a správu objektů.</span><span class="sxs-lookup"><span data-stu-id="605ba-147">After the garbage collector is initialized by the CLR, it allocates a segment of memory to store and manage objects.</span></span> <span data-ttu-id="605ba-148">Tato paměť se nazývá spravované haldy, na rozdíl od nativní haldy v operačním systému.</span><span class="sxs-lookup"><span data-stu-id="605ba-148">This memory is called the managed heap, as opposed to a native heap in the operating system.</span></span>

<span data-ttu-id="605ba-149">Pro každý spravovaný proces existuje spravovaná halda.</span><span class="sxs-lookup"><span data-stu-id="605ba-149">There is a managed heap for each managed process.</span></span> <span data-ttu-id="605ba-150">Všechna vlákna v procesu přidělit paměť pro objekty na stejné haldě.</span><span class="sxs-lookup"><span data-stu-id="605ba-150">All threads in the process allocate memory for objects on the same heap.</span></span>

<span data-ttu-id="605ba-151">Chcete-li rezervovat paměť, systém uvolňování paměti volá funkci Windows [VirtualAlloc](/windows/desktop/api/memoryapi/nf-memoryapi-virtualalloc) a rezervuje jeden segment paměti najednou pro spravované aplikace.</span><span class="sxs-lookup"><span data-stu-id="605ba-151">To reserve memory, the garbage collector calls the Windows [VirtualAlloc](/windows/desktop/api/memoryapi/nf-memoryapi-virtualalloc) function and reserves one segment of memory at a time for managed applications.</span></span> <span data-ttu-id="605ba-152">Systém uvolňování paměti také podle potřeby rezervuje segmenty a uvolní segmenty zpět do operačního systému (po jejich vymazání všech objektů) voláním funkce Windows [VirtualFree.](/windows/desktop/api/memoryapi/nf-memoryapi-virtualfree)</span><span class="sxs-lookup"><span data-stu-id="605ba-152">The garbage collector also reserves segments, as needed, and releases segments back to the operating system (after clearing them of any objects) by calling the Windows [VirtualFree](/windows/desktop/api/memoryapi/nf-memoryapi-virtualfree) function.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="605ba-153">Velikost segmentů přidělených systémem uvolňování paměti je specifická pro implementaci a může se kdykoli změnit, včetně pravidelných aktualizací.</span><span class="sxs-lookup"><span data-stu-id="605ba-153">The size of segments allocated by the garbage collector is implementation-specific and is subject to change at any time, including in periodic updates.</span></span> <span data-ttu-id="605ba-154">Vaše aplikace by nikdy neměla dělat předpoklady o konkrétní velikosti segmentu nebo na ní záviset, ani by se neměla pokoušet nakonfigurovat množství paměti, která je k dispozici pro přidělení segmentu.</span><span class="sxs-lookup"><span data-stu-id="605ba-154">Your app should never make assumptions about or depend on a particular segment size, nor should it attempt to configure the amount of memory available for segment allocations.</span></span>

<span data-ttu-id="605ba-155">Méně objektů přidělených na haldě, méně práce systému uvolňování paměti musí provést.</span><span class="sxs-lookup"><span data-stu-id="605ba-155">The fewer objects allocated on the heap, the less work the garbage collector has to do.</span></span> <span data-ttu-id="605ba-156">Při přidělování objektů nepoužívejte hodnoty zaobleného nahoru, které přesahují vaše potřeby, například přidělení pole 32 bajtů, když potřebujete pouze 15 bajtů.</span><span class="sxs-lookup"><span data-stu-id="605ba-156">When you allocate objects, do not use rounded-up values that exceed your needs, such as allocating an array of 32 bytes when you need only 15 bytes.</span></span>

<span data-ttu-id="605ba-157">Při uvolnění paměti je spuštěna, uvolňování paměti uvolní paměť, která je obsazena mrtvé objekty.</span><span class="sxs-lookup"><span data-stu-id="605ba-157">When a garbage collection is triggered, the garbage collector reclaims the memory that is occupied by dead objects.</span></span> <span data-ttu-id="605ba-158">Proces rekultivace komprimuje živé objekty tak, aby byly přesunuty společně a mrtvý prostor je odebrán, čímž se halda menší.</span><span class="sxs-lookup"><span data-stu-id="605ba-158">The reclaiming process compacts live objects so that they are moved together, and the dead space is removed, thereby making the heap smaller.</span></span> <span data-ttu-id="605ba-159">Tím je zajištěno, že objekty, které jsou přiděleny společně zůstat společně na spravované haldy, zachovat jejich lokalitu.</span><span class="sxs-lookup"><span data-stu-id="605ba-159">This ensures that objects that are allocated together stay together on the managed heap, to preserve their locality.</span></span>

<span data-ttu-id="605ba-160">Rušivost (frekvence a trvání) uvolňování paměti je výsledkem objemu přidělení a množství paměti, která přežila na spravované haldě.</span><span class="sxs-lookup"><span data-stu-id="605ba-160">The intrusiveness (frequency and duration) of garbage collections is the result of the volume of allocations and the amount of survived memory on the managed heap.</span></span>

<span data-ttu-id="605ba-161">Haldy lze považovat za akumulaci dvou hald: [haldy velkého objektu](large-object-heap.md) a haldy malého objektu.</span><span class="sxs-lookup"><span data-stu-id="605ba-161">The heap can be considered as the accumulation of two heaps: the [large object heap](large-object-heap.md) and the small object heap.</span></span>

<span data-ttu-id="605ba-162">[Haldy velkých objektů](large-object-heap.md) obsahuje velmi velké objekty, které jsou 85 000 bajtů a větší.</span><span class="sxs-lookup"><span data-stu-id="605ba-162">The [large object heap](large-object-heap.md) contains very large objects that are 85,000 bytes and larger.</span></span> <span data-ttu-id="605ba-163">Objekty na haldě velkého objektu jsou obvykle matice.</span><span class="sxs-lookup"><span data-stu-id="605ba-163">The objects on the large object heap are usually arrays.</span></span> <span data-ttu-id="605ba-164">Je vzácné pro objekt instance být velmi velké.</span><span class="sxs-lookup"><span data-stu-id="605ba-164">It is rare for an instance object to be extremely large.</span></span>

> [!TIP]
> <span data-ttu-id="605ba-165">Můžete [nakonfigurovat velikost prahové hodnoty](../../core/run-time-config/garbage-collector.md#large-object-heap-threshold) pro objekty jít na haldy velkého objektu.</span><span class="sxs-lookup"><span data-stu-id="605ba-165">You can [configure the threshold size](../../core/run-time-config/garbage-collector.md#large-object-heap-threshold) for objects to go on the large object heap.</span></span>

## <a name="generations"></a><span data-ttu-id="605ba-166">Generace</span><span class="sxs-lookup"><span data-stu-id="605ba-166">Generations</span></span>

<span data-ttu-id="605ba-167">Halda je uspořádána do generací tak, aby mohla zpracovávat dlouhodobé a krátkodobé objekty.</span><span class="sxs-lookup"><span data-stu-id="605ba-167">The heap is organized into generations so it can handle long-lived and short-lived objects.</span></span> <span data-ttu-id="605ba-168">Uvolňování paměti dochází především s rekultivací krátkodobé objekty, které obvykle zabírají pouze malou část haldy.</span><span class="sxs-lookup"><span data-stu-id="605ba-168">Garbage collection primarily occurs with the reclamation of short-lived objects that typically occupy only a small part of the heap.</span></span> <span data-ttu-id="605ba-169">Existují tři generace objektů na haldě:</span><span class="sxs-lookup"><span data-stu-id="605ba-169">There are three generations of objects on the heap:</span></span>

- <span data-ttu-id="605ba-170">**Generace 0**.</span><span class="sxs-lookup"><span data-stu-id="605ba-170">**Generation 0**.</span></span> <span data-ttu-id="605ba-171">Jedná se o nejmladší generaci a obsahuje krátkodobé objekty.</span><span class="sxs-lookup"><span data-stu-id="605ba-171">This is the youngest generation and contains short-lived objects.</span></span> <span data-ttu-id="605ba-172">Příkladem krátkodobého objektu je dočasná proměnná.</span><span class="sxs-lookup"><span data-stu-id="605ba-172">An example of a short-lived object is a temporary variable.</span></span> <span data-ttu-id="605ba-173">Uvolňování paměti dochází nejčastěji v této generaci.</span><span class="sxs-lookup"><span data-stu-id="605ba-173">Garbage collection occurs most frequently in this generation.</span></span>

  <span data-ttu-id="605ba-174">Nově přidělené objekty tvoří novou generaci objektů a jsou implicitně generace 0 kolekce.</span><span class="sxs-lookup"><span data-stu-id="605ba-174">Newly allocated objects form a new generation of objects and are implicitly generation 0 collections.</span></span> <span data-ttu-id="605ba-175">Však pokud jsou velké objekty, přejdou na haldy velkého objektu v kolekci generace 2.</span><span class="sxs-lookup"><span data-stu-id="605ba-175">However, if they are large objects, they go on the large object heap in a generation 2 collection.</span></span>

  <span data-ttu-id="605ba-176">Většina objektů jsou uvolněny pro uvolnění paměti v generaci 0 a nepřežijí další generace.</span><span class="sxs-lookup"><span data-stu-id="605ba-176">Most objects are reclaimed for garbage collection in generation 0 and do not survive to the next generation.</span></span>

- <span data-ttu-id="605ba-177">**Generace 1**.</span><span class="sxs-lookup"><span data-stu-id="605ba-177">**Generation 1**.</span></span> <span data-ttu-id="605ba-178">Tato generace obsahuje krátkodobé objekty a slouží jako vyrovnávací paměť mezi krátkodobé objekty a dlouhodobé objekty.</span><span class="sxs-lookup"><span data-stu-id="605ba-178">This generation contains short-lived objects and serves as a buffer between short-lived objects and long-lived objects.</span></span>

- <span data-ttu-id="605ba-179">**Generace 2**.</span><span class="sxs-lookup"><span data-stu-id="605ba-179">**Generation 2**.</span></span> <span data-ttu-id="605ba-180">Tato generace obsahuje objekty s dlouhou životností.</span><span class="sxs-lookup"><span data-stu-id="605ba-180">This generation contains long-lived objects.</span></span> <span data-ttu-id="605ba-181">Příkladem objektu s dlouhou životností je objekt v serverové aplikaci, který obsahuje statická data, která jsou aktivní po dobu trvání procesu.</span><span class="sxs-lookup"><span data-stu-id="605ba-181">An example of a long-lived object is an object in a server application that contains static data that's live for the duration of the process.</span></span>

<span data-ttu-id="605ba-182">Uvolňování paměti dojít na konkrétní generace jako podmínky rozkaz.</span><span class="sxs-lookup"><span data-stu-id="605ba-182">Garbage collections occur on specific generations as conditions warrant.</span></span> <span data-ttu-id="605ba-183">Shromažďování generace znamená shromažďování předmětů v této generaci a všech jejích mladších generacích.</span><span class="sxs-lookup"><span data-stu-id="605ba-183">Collecting a generation means collecting objects in that generation and all its younger generations.</span></span> <span data-ttu-id="605ba-184">Uvolnění paměti generace 2 je také známé jako úplné uvolnění paměti, protože uvolňuje všechny objekty ve všech generacích (to znamená, že všechny objekty ve spravované haldě).</span><span class="sxs-lookup"><span data-stu-id="605ba-184">A generation 2 garbage collection is also known as a full garbage collection, because it reclaims all objects in all generations (that is, all objects in the managed heap).</span></span>

### <a name="survival-and-promotions"></a><span data-ttu-id="605ba-185">Přežití a propagace</span><span class="sxs-lookup"><span data-stu-id="605ba-185">Survival and promotions</span></span>

<span data-ttu-id="605ba-186">Objekty, které nejsou uvolněny v uvolňování paměti jsou označovány jako survivors a jsou povýšeny na další generaci.</span><span class="sxs-lookup"><span data-stu-id="605ba-186">Objects that are not reclaimed in a garbage collection are known as survivors and are promoted to the next generation.</span></span> <span data-ttu-id="605ba-187">Objekty, které přežijí uvolnění paměti generace 0, jsou povýšeny na generaci 1; objekty, které přežijí uvolnění paměti generace 1, jsou povýšeny na generaci 2; a objekty, které přežijí uvolnění paměti generace 2 zůstávají v generaci 2.</span><span class="sxs-lookup"><span data-stu-id="605ba-187">Objects that survive a generation 0 garbage collection are promoted to generation 1; objects that survive a generation 1 garbage collection are promoted to generation 2; and objects that survive a generation 2 garbage collection remain in generation 2.</span></span>

<span data-ttu-id="605ba-188">Když systém uvolňování paměti zjistí, že míra přežití je vysoká v generaci, zvýší prahovou hodnotu přidělení pro toto generování.</span><span class="sxs-lookup"><span data-stu-id="605ba-188">When the garbage collector detects that the survival rate is high in a generation, it increases the threshold of allocations for that generation.</span></span> <span data-ttu-id="605ba-189">Další kolekce získá značnou velikost regenerované paměti.</span><span class="sxs-lookup"><span data-stu-id="605ba-189">The next collection gets a substantial size of reclaimed memory.</span></span> <span data-ttu-id="605ba-190">CLR neustále vyvažuje dvě priority: nenechat pracovní sadu aplikace příliš velké zpožděním uvolňování paměti a neumožňuje uvolnění paměti spustit příliš často.</span><span class="sxs-lookup"><span data-stu-id="605ba-190">The CLR continually balances two priorities: not letting an application's working set get too large by delaying garbage collection and not letting the garbage collection run too frequently.</span></span>

### <a name="ephemeral-generations-and-segments"></a><span data-ttu-id="605ba-191">Pomíjivé generace a segmenty</span><span class="sxs-lookup"><span data-stu-id="605ba-191">Ephemeral generations and segments</span></span>

<span data-ttu-id="605ba-192">Protože objekty v generacích 0 a 1 jsou krátkodobé, tyto generace jsou známé jako dočasné generace.</span><span class="sxs-lookup"><span data-stu-id="605ba-192">Because objects in generations 0 and 1 are short-lived, these generations are known as the ephemeral generations.</span></span>

<span data-ttu-id="605ba-193">Dočasné generace musí být přiděleny v segmentu paměti, který je označován jako dočasný segment.</span><span class="sxs-lookup"><span data-stu-id="605ba-193">Ephemeral generations must be allocated in the memory segment that is known as the ephemeral segment.</span></span> <span data-ttu-id="605ba-194">Každý nový segment získaný systémem uvolňování paměti se stane novým dočasným segmentem a obsahuje objekty, které přežily uvolnění paměti generace 0.</span><span class="sxs-lookup"><span data-stu-id="605ba-194">Each new segment acquired by the garbage collector becomes the new ephemeral segment and contains the objects that survived a generation 0 garbage collection.</span></span> <span data-ttu-id="605ba-195">Starý dočasný segment se stává segmentem nové generace 2.</span><span class="sxs-lookup"><span data-stu-id="605ba-195">The old ephemeral segment becomes the new generation 2 segment.</span></span>

<span data-ttu-id="605ba-196">Velikost dočasného segmentu se liší v závislosti na tom, zda je systém 32bitový nebo 64bitový a na typu systému uvolňování paměti, který je spuštěn.</span><span class="sxs-lookup"><span data-stu-id="605ba-196">The size of the ephemeral segment varies depending on whether a system is 32-bit or 64-bit, and on the type of garbage collector it is running.</span></span> <span data-ttu-id="605ba-197">Výchozí hodnoty jsou uvedeny v následující tabulce.</span><span class="sxs-lookup"><span data-stu-id="605ba-197">Default values are shown in the following table.</span></span>

||<span data-ttu-id="605ba-198">32bitová</span><span class="sxs-lookup"><span data-stu-id="605ba-198">32-bit</span></span>|<span data-ttu-id="605ba-199">64bitová</span><span class="sxs-lookup"><span data-stu-id="605ba-199">64-bit</span></span>|
|-|-------------|-------------|
|<span data-ttu-id="605ba-200">Pracovní stanice GC</span><span class="sxs-lookup"><span data-stu-id="605ba-200">Workstation GC</span></span>|<span data-ttu-id="605ba-201">16 MB</span><span class="sxs-lookup"><span data-stu-id="605ba-201">16 MB</span></span>|<span data-ttu-id="605ba-202">256 MB</span><span class="sxs-lookup"><span data-stu-id="605ba-202">256 MB</span></span>|
|<span data-ttu-id="605ba-203">Server GC</span><span class="sxs-lookup"><span data-stu-id="605ba-203">Server GC</span></span>|<span data-ttu-id="605ba-204">64 MB</span><span class="sxs-lookup"><span data-stu-id="605ba-204">64 MB</span></span>|<span data-ttu-id="605ba-205">4 GB</span><span class="sxs-lookup"><span data-stu-id="605ba-205">4 GB</span></span>|
|<span data-ttu-id="605ba-206">Server GC s logickými procesory > 4</span><span class="sxs-lookup"><span data-stu-id="605ba-206">Server GC with > 4 logical CPUs</span></span>|<span data-ttu-id="605ba-207">32 MB</span><span class="sxs-lookup"><span data-stu-id="605ba-207">32 MB</span></span>|<span data-ttu-id="605ba-208">2 GB</span><span class="sxs-lookup"><span data-stu-id="605ba-208">2 GB</span></span>|
|<span data-ttu-id="605ba-209">Server GC s logickými procesory > 8</span><span class="sxs-lookup"><span data-stu-id="605ba-209">Server GC with > 8 logical CPUs</span></span>|<span data-ttu-id="605ba-210">16 MB</span><span class="sxs-lookup"><span data-stu-id="605ba-210">16 MB</span></span>|<span data-ttu-id="605ba-211">1 GB</span><span class="sxs-lookup"><span data-stu-id="605ba-211">1 GB</span></span>|

<span data-ttu-id="605ba-212">Dočasný segment může obsahovat objekty generace 2.</span><span class="sxs-lookup"><span data-stu-id="605ba-212">The ephemeral segment can include generation 2 objects.</span></span> <span data-ttu-id="605ba-213">Generace 2 objekty mohou používat více segmentů (tolik, kolik vyžaduje proces a paměť umožňuje).</span><span class="sxs-lookup"><span data-stu-id="605ba-213">Generation 2 objects can use multiple segments (as many as your process requires and memory allows for).</span></span>

<span data-ttu-id="605ba-214">Množství uvolněné paměti z dočasné uvolňování paměti je omezena na velikost dočasného segmentu.</span><span class="sxs-lookup"><span data-stu-id="605ba-214">The amount of freed memory from an ephemeral garbage collection is limited to the size of the ephemeral segment.</span></span> <span data-ttu-id="605ba-215">Množství paměti, která je uvolněna, je úměrná prostoru, který byl obsazen mrtvými objekty.</span><span class="sxs-lookup"><span data-stu-id="605ba-215">The amount of memory that is freed is proportional to the space that was occupied by the dead objects.</span></span>

## <a name="what-happens-during-a-garbage-collection"></a><span data-ttu-id="605ba-216">Co se stane během uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="605ba-216">What happens during a garbage collection</span></span>

<span data-ttu-id="605ba-217">Uvolnění paměti má následující fáze:</span><span class="sxs-lookup"><span data-stu-id="605ba-217">A garbage collection has the following phases:</span></span>

- <span data-ttu-id="605ba-218">Fáze značení, která vyhledá a vytvoří seznam všech živých objektů.</span><span class="sxs-lookup"><span data-stu-id="605ba-218">A marking phase that finds and creates a list of all live objects.</span></span>

- <span data-ttu-id="605ba-219">Fáze přemístění, která aktualizuje odkazy na objekty, které budou komprimovány.</span><span class="sxs-lookup"><span data-stu-id="605ba-219">A relocating phase that updates the references to the objects that will be compacted.</span></span>

- <span data-ttu-id="605ba-220">Fáze komprimace, která uvolňuje prostor obsazený mrtvými objekty a komprimuje přežívající objekty.</span><span class="sxs-lookup"><span data-stu-id="605ba-220">A compacting phase that reclaims the space occupied by the dead objects and compacts the surviving objects.</span></span> <span data-ttu-id="605ba-221">Fáze komprimace přesune objekty, které přežily uvolňování paměti směrem ke staršímu konci segmentu.</span><span class="sxs-lookup"><span data-stu-id="605ba-221">The compacting phase moves objects that have survived a garbage collection toward the older end of the segment.</span></span>

  <span data-ttu-id="605ba-222">Vzhledem k tomu, že kolekce generace 2 mohou zabírat více segmentů, objekty, které jsou povýšeny na generaci 2, lze přesunout do staršího segmentu.</span><span class="sxs-lookup"><span data-stu-id="605ba-222">Because generation 2 collections can occupy multiple segments, objects that are promoted into generation 2 can be moved into an older segment.</span></span> <span data-ttu-id="605ba-223">Generace 1 i generace 2, které přežily, mohou být přesunuty do jiného segmentu, protože jsou povýšeni na generaci 2.</span><span class="sxs-lookup"><span data-stu-id="605ba-223">Both generation 1 and generation 2 survivors can be moved to a different segment, because they are promoted to generation 2.</span></span>

  <span data-ttu-id="605ba-224">Obvykle haldy velkých objektů (LOH) není komprimován, protože kopírování velkých objektů ukládá snížení výkonu.</span><span class="sxs-lookup"><span data-stu-id="605ba-224">Ordinarily, the large object heap (LOH) is not compacted, because copying large objects imposes a performance penalty.</span></span> <span data-ttu-id="605ba-225">V .NET Core a v rozhraní .NET Framework 4.5.1 <xref:System.Runtime.GCSettings.LargeObjectHeapCompactionMode%2A?displayProperty=nameWithType> a novějších však můžete vlastnost použít k komprimaci haldy velkých objektů na vyžádání.</span><span class="sxs-lookup"><span data-stu-id="605ba-225">However, in .NET Core and in .NET Framework 4.5.1 and later, you can use the <xref:System.Runtime.GCSettings.LargeObjectHeapCompactionMode%2A?displayProperty=nameWithType> property to compact the large object heap on demand.</span></span> <span data-ttu-id="605ba-226">Kromě toho je LOH automaticky zhutněn, když je nastaven pevný limit, a to zadáním:</span><span class="sxs-lookup"><span data-stu-id="605ba-226">In addition, the LOH is automatically compacted when a hard limit is set by specifying either:</span></span>

  - <span data-ttu-id="605ba-227">limit paměti na kontejneru, nebo</span><span class="sxs-lookup"><span data-stu-id="605ba-227">a memory limit on a container, or</span></span>
  - <span data-ttu-id="605ba-228">Možnosti konfigurace [GCHeapHardLimit](../../core/run-time-config/garbage-collector.md#systemgcheaphardlimitcomplus_gcheaphardlimit) nebo [GCHeapHardLimitPercent](../../core/run-time-config/garbage-collector.md#systemgcheaphardlimitpercentcomplus_gcheaphardlimitpercent)</span><span class="sxs-lookup"><span data-stu-id="605ba-228">the [GCHeapHardLimit](../../core/run-time-config/garbage-collector.md#systemgcheaphardlimitcomplus_gcheaphardlimit) or [GCHeapHardLimitPercent](../../core/run-time-config/garbage-collector.md#systemgcheaphardlimitpercentcomplus_gcheaphardlimitpercent) run-time configuration options</span></span>

<span data-ttu-id="605ba-229">Systém uvolňování paměti používá následující informace k určení, zda jsou objekty aktivní:</span><span class="sxs-lookup"><span data-stu-id="605ba-229">The garbage collector uses the following information to determine whether objects are live:</span></span>

- <span data-ttu-id="605ba-230">**Kořeny zásobníku**.</span><span class="sxs-lookup"><span data-stu-id="605ba-230">**Stack roots**.</span></span> <span data-ttu-id="605ba-231">Proměnné zásobníku poskytované kompilátorem just-in-time (JIT) a tchytačivým zásobníkem.</span><span class="sxs-lookup"><span data-stu-id="605ba-231">Stack variables provided by the just-in-time (JIT) compiler and stack walker.</span></span> <span data-ttu-id="605ba-232">Jit optimalizace můžete prodloužit nebo zkrátit oblasti kódu, ve kterém jsou hlášeny proměnné zásobníku do systému uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="605ba-232">JIT optimizations can lengthen or shorten regions of code within which stack variables are reported to the garbage collector.</span></span>

- <span data-ttu-id="605ba-233">**Popisovače uvolňování paměti**.</span><span class="sxs-lookup"><span data-stu-id="605ba-233">**Garbage collection handles**.</span></span> <span data-ttu-id="605ba-234">Zpracovává, které odkazují na spravované objekty a které mohou být přiděleny podle uživatelského kódu nebo běžného jazyka runtime.</span><span class="sxs-lookup"><span data-stu-id="605ba-234">Handles that point to managed objects and that can be allocated by user code or by the common language runtime.</span></span>

- <span data-ttu-id="605ba-235">**Statická data**.</span><span class="sxs-lookup"><span data-stu-id="605ba-235">**Static data**.</span></span> <span data-ttu-id="605ba-236">Statické objekty v aplikačních doménách, které mohou odkazovat na jiné objekty.</span><span class="sxs-lookup"><span data-stu-id="605ba-236">Static objects in application domains that could be referencing other objects.</span></span> <span data-ttu-id="605ba-237">Každá doména aplikace sleduje své statické objekty.</span><span class="sxs-lookup"><span data-stu-id="605ba-237">Each application domain keeps track of its static objects.</span></span>

<span data-ttu-id="605ba-238">Před spuštěním uvolňování paměti jsou všechna spravovaná vlákna pozastavena s výjimkou vlákna, které spustilo uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="605ba-238">Before a garbage collection starts, all managed threads are suspended except for the thread that triggered the garbage collection.</span></span>

<span data-ttu-id="605ba-239">Následující obrázek znázorňuje vlákno, které aktivuje uvolňování paměti a způsobí, že ostatní vlákna mají být pozastavena.</span><span class="sxs-lookup"><span data-stu-id="605ba-239">The following illustration shows a thread that triggers a garbage collection and causes the other threads to be suspended.</span></span>

![Když vlákno aktivuje uvolňování paměti](./media/gc-triggered.png)

## <a name="manipulate-unmanaged-resources"></a><span data-ttu-id="605ba-241">Manipulace s nespravovanými prostředky</span><span class="sxs-lookup"><span data-stu-id="605ba-241">Manipulate unmanaged resources</span></span>

<span data-ttu-id="605ba-242">Pokud spravované objekty odkazují na nespravované objekty pomocí jejich nativních popisovačů souborů, musíte explicitně uvolnit nespravované objekty, protože systém uvolňování paměti sleduje pouze paměť na spravované haldě.</span><span class="sxs-lookup"><span data-stu-id="605ba-242">If managed objects reference unmanaged objects by using their native file handles, you have to explicitly free the unmanaged objects, because the garbage collector only tracks memory on the managed heap.</span></span>

<span data-ttu-id="605ba-243">Uživatelé spravovaného objektu nesmí nakládat nativní prostředky používané objektem.</span><span class="sxs-lookup"><span data-stu-id="605ba-243">Users of the managed object may not dispose the native resources used by the object.</span></span> <span data-ttu-id="605ba-244">Chcete-li provést vyčištění, můžete provést spravovaný objekt finalizovat.</span><span class="sxs-lookup"><span data-stu-id="605ba-244">To perform the cleanup, you can make the managed object finalizable.</span></span> <span data-ttu-id="605ba-245">Dokončení se skládá z akcí vyčištění, které se provádějí, když se objekt již nepoužívá.</span><span class="sxs-lookup"><span data-stu-id="605ba-245">Finalization consists of cleanup actions that execute when the object is no longer in use.</span></span> <span data-ttu-id="605ba-246">Když spravovaný objekt zemře, provede akce vyčištění, které jsou zadány v jeho finalizační metody.</span><span class="sxs-lookup"><span data-stu-id="605ba-246">When the managed object dies, it performs cleanup actions that are specified in its finalizer method.</span></span>

<span data-ttu-id="605ba-247">Když je zjistit, že finalizovatelný objekt je mrtvý, jeho finalizační metoda je umístěna do fronty tak, aby byly provedeny jeho akce vyčištění, ale samotný objekt je povýšen na další generaci.</span><span class="sxs-lookup"><span data-stu-id="605ba-247">When a finalizable object is discovered to be dead, its finalizer is put in a queue so that its cleanup actions are executed, but the object itself is promoted to the next generation.</span></span> <span data-ttu-id="605ba-248">Proto budete muset počkat, až další uvolnění paměti, ke kterému dochází na této generace (což není nutně další uvolnění paměti) k určení, zda byl objekt uvolněn.</span><span class="sxs-lookup"><span data-stu-id="605ba-248">Therefore, you have to wait until the next garbage collection that occurs on that generation (which is not necessarily the next garbage collection) to determine whether the object has been reclaimed.</span></span>

<span data-ttu-id="605ba-249">Další informace o dokončení <xref:System.Object.Finalize?displayProperty=nameWithType>naleznete v tématu .</span><span class="sxs-lookup"><span data-stu-id="605ba-249">For more information about finalization, see <xref:System.Object.Finalize?displayProperty=nameWithType>.</span></span>

## <a name="workstation-and-server-garbage-collection"></a><span data-ttu-id="605ba-250">Uvolňování paměti pracovní stanice a serveru</span><span class="sxs-lookup"><span data-stu-id="605ba-250">Workstation and server garbage collection</span></span>

<span data-ttu-id="605ba-251">Systém uvolňování paměti je samoladný a může pracovat v široké škále scénářů.</span><span class="sxs-lookup"><span data-stu-id="605ba-251">The garbage collector is self-tuning and can work in a wide variety of scenarios.</span></span> <span data-ttu-id="605ba-252">[Nastavení konfiguračního souboru](../../core/run-time-config/garbage-collector.md#flavors-of-garbage-collection) můžete použít k nastavení typu uvolňování paměti na základě charakteristik pracovního vytížení.</span><span class="sxs-lookup"><span data-stu-id="605ba-252">You can use a [configuration file setting](../../core/run-time-config/garbage-collector.md#flavors-of-garbage-collection) to set the type of garbage collection based on the characteristics of the workload.</span></span> <span data-ttu-id="605ba-253">CLR poskytuje následující typy uvolňování paměti:</span><span class="sxs-lookup"><span data-stu-id="605ba-253">The CLR provides the following types of garbage collection:</span></span>

- <span data-ttu-id="605ba-254">Uvolňování paměti pracovní stanice (GC) je určen pro klientské aplikace.</span><span class="sxs-lookup"><span data-stu-id="605ba-254">Workstation garbage collection (GC) is designed for client apps.</span></span> <span data-ttu-id="605ba-255">Jedná se o výchozí variantu GC pro samostatné aplikace.</span><span class="sxs-lookup"><span data-stu-id="605ba-255">It is the default GC flavor for standalone apps.</span></span> <span data-ttu-id="605ba-256">Pro hostované aplikace, například ty, které hostuje ASP.NET, hostitel určuje výchozí gc příchuť.</span><span class="sxs-lookup"><span data-stu-id="605ba-256">For hosted apps, for example, those hosted by ASP.NET, the host determines the default GC flavor.</span></span>

  <span data-ttu-id="605ba-257">Uvolňování paměti pracovní stanice může být souběžné nebo nesouběžné.</span><span class="sxs-lookup"><span data-stu-id="605ba-257">Workstation garbage collection can be concurrent or non-concurrent.</span></span> <span data-ttu-id="605ba-258">Souběžné uvolňování paměti umožňuje spravovaným vláknům pokračovat v operacích během uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="605ba-258">Concurrent garbage collection enables managed threads to continue operations during a garbage collection.</span></span> <span data-ttu-id="605ba-259">[Uvolňování paměti na pozadí](#background-workstation-garbage-collection) nahrazuje [souběžné uvolňování paměti](#concurrent-garbage-collection) v rozhraní .NET Framework 4 a novějších verzích.</span><span class="sxs-lookup"><span data-stu-id="605ba-259">[Background garbage collection](#background-workstation-garbage-collection) replaces [concurrent garbage collection](#concurrent-garbage-collection) in .NET Framework 4 and later versions.</span></span>

- <span data-ttu-id="605ba-260">Uvolňování paměti serveru, který je určen pro serverové aplikace, které vyžadují vysokou propustnost a škálovatelnost.</span><span class="sxs-lookup"><span data-stu-id="605ba-260">Server garbage collection, which is intended for server applications that need high throughput and scalability.</span></span>

  - <span data-ttu-id="605ba-261">V .NET Core může být uvolňování paměti serveru nesouběžné nebo na pozadí.</span><span class="sxs-lookup"><span data-stu-id="605ba-261">In .NET Core, server garbage collection can be non-concurrent or background.</span></span>

  - <span data-ttu-id="605ba-262">V rozhraní .NET Framework 4.5 a novějších verzích může být uvolňování paměti serveru nesouběžné nebo na pozadí (uvolňování paměti na pozadí nahrazuje souběžné uvolňování paměti).</span><span class="sxs-lookup"><span data-stu-id="605ba-262">In .NET Framework 4.5 and later versions, server garbage collection can be non-concurrent or background (background garbage collection replaces concurrent garbage collection).</span></span> <span data-ttu-id="605ba-263">V rozhraní .NET Framework 4 a předchozích verzích je uvolňování paměti serveru nesouběžné.</span><span class="sxs-lookup"><span data-stu-id="605ba-263">In .NET Framework 4 and previous versions, server garbage collection is non-concurrent.</span></span>

<span data-ttu-id="605ba-264">Následující obrázek znázorňuje vyhrazená vlákna, která provádějí uvolňování paměti na serveru:</span><span class="sxs-lookup"><span data-stu-id="605ba-264">The following illustration shows the dedicated threads that perform the garbage collection on a server:</span></span>

![Vlákna uvolňování paměti serveru](./media/gc-server.png)

### <a name="compare-workstation-and-server-garbage-collection"></a><span data-ttu-id="605ba-266">Porovnat uvolňování paměti pracovní stanice a serveru</span><span class="sxs-lookup"><span data-stu-id="605ba-266">Compare workstation and server garbage collection</span></span>

<span data-ttu-id="605ba-267">Níže jsou zřetězení a výkon aspekty pro uvolňování paměti pracovní stanice:</span><span class="sxs-lookup"><span data-stu-id="605ba-267">The following are threading and performance considerations for workstation garbage collection:</span></span>

- <span data-ttu-id="605ba-268">Kolekce dochází ve vlákně uživatele, který spustil uvolňování paměti a zůstává na stejnou prioritu.</span><span class="sxs-lookup"><span data-stu-id="605ba-268">The collection occurs on the user thread that triggered the garbage collection and remains at the same priority.</span></span> <span data-ttu-id="605ba-269">Vzhledem k tomu, že vlákna uživatele obvykle běží s normální prioritou, systém uvolňování paměti (který běží v normálním vlákně s prioritou) musí soutěžit s jinými vlákny pro čas procesoru.</span><span class="sxs-lookup"><span data-stu-id="605ba-269">Because user threads typically run at normal priority, the garbage collector (which runs on a normal priority thread) must compete with other threads for CPU time.</span></span> <span data-ttu-id="605ba-270">(Vlákna, která spouštějí nativní kód, nejsou pozastavena na serveru ani na uvolnění paměti pracovní stanice.)</span><span class="sxs-lookup"><span data-stu-id="605ba-270">(Threads that run native code are not suspended on either server or workstation garbage collection.)</span></span>

- <span data-ttu-id="605ba-271">Uvolňování paměti pracovní stanice se vždy používá v počítači, který má pouze jeden procesor, bez ohledu na [nastavení konfigurace](../../core/run-time-config/garbage-collector.md#systemgcservercomplus_gcserver).</span><span class="sxs-lookup"><span data-stu-id="605ba-271">Workstation garbage collection is always used on a computer that has only one processor, regardless of the [configuration setting](../../core/run-time-config/garbage-collector.md#systemgcservercomplus_gcserver).</span></span>

<span data-ttu-id="605ba-272">Následují aspekty zřetězení a výkonu pro uvolnění paměti serveru:</span><span class="sxs-lookup"><span data-stu-id="605ba-272">The following are threading and performance considerations for server garbage collection:</span></span>

- <span data-ttu-id="605ba-273">Kolekce se vyskytuje na více vyhrazených `THREAD_PRIORITY_HIGHEST` podprocesů, které jsou spuštěny na úrovni priority.</span><span class="sxs-lookup"><span data-stu-id="605ba-273">The collection occurs on multiple dedicated threads that are running at `THREAD_PRIORITY_HIGHEST` priority level.</span></span>

- <span data-ttu-id="605ba-274">Haldy a vyhrazené vlákno k provedení uvolňování paměti jsou k dispozici pro každý procesor a haldy jsou shromažďovány ve stejnou dobu.</span><span class="sxs-lookup"><span data-stu-id="605ba-274">A heap and a dedicated thread to perform garbage collection are provided for each CPU, and the heaps are collected at the same time.</span></span> <span data-ttu-id="605ba-275">Každá halda obsahuje haldu malého objektu a haldu velkého objektu a všechny haldy jsou přístupné podle uživatelského kódu.</span><span class="sxs-lookup"><span data-stu-id="605ba-275">Each heap contains a small object heap and a large object heap, and all heaps can be accessed by user code.</span></span> <span data-ttu-id="605ba-276">Objekty na různých hromadách mohou odkazovat na sebe navzájem.</span><span class="sxs-lookup"><span data-stu-id="605ba-276">Objects on different heaps can refer to each other.</span></span>

- <span data-ttu-id="605ba-277">Vzhledem k tomu, že více podprocesů uvolňování paměti spolupracovat, uvolňování paměti serveru je rychlejší než uvolňování paměti pracovní stanice na stejné haldy velikosti.</span><span class="sxs-lookup"><span data-stu-id="605ba-277">Because multiple garbage collection threads work together, server garbage collection is faster than workstation garbage collection on the same size heap.</span></span>

- <span data-ttu-id="605ba-278">Uvolňování paměti serveru má často větší segmenty velikosti.</span><span class="sxs-lookup"><span data-stu-id="605ba-278">Server garbage collection often has larger size segments.</span></span> <span data-ttu-id="605ba-279">Toje však pouze generalizace: velikost segmentu je specifické pro implementaci a může se změnit.</span><span class="sxs-lookup"><span data-stu-id="605ba-279">However, this is only a generalization: segment size is implementation-specific and is subject to change.</span></span> <span data-ttu-id="605ba-280">Neprovádělte předpoklady o velikosti segmentů přidělených systémem uvolňování paměti při ladění aplikace.</span><span class="sxs-lookup"><span data-stu-id="605ba-280">Don't make assumptions about the size of segments allocated by the garbage collector when tuning your app.</span></span>

- <span data-ttu-id="605ba-281">Uvolňování paměti serveru může být náročné na prostředky.</span><span class="sxs-lookup"><span data-stu-id="605ba-281">Server garbage collection can be resource-intensive.</span></span> <span data-ttu-id="605ba-282">Představte si například, že existuje 12 procesů, které používají server GC spuštěné v počítači, který má 4 procesory.</span><span class="sxs-lookup"><span data-stu-id="605ba-282">For example, imagine that there are 12 processes that use server GC running on a computer that has 4 processors.</span></span> <span data-ttu-id="605ba-283">Pokud všechny procesy náhodou sbírat odpadky ve stejnou dobu, by zasahovali do sebe, protože by bylo 12 podprocesů naplánovaných na stejném procesoru.</span><span class="sxs-lookup"><span data-stu-id="605ba-283">If all the processes happen to collect garbage at the same time, they would interfere with each other, as there would be 12 threads scheduled on the same processor.</span></span> <span data-ttu-id="605ba-284">Pokud jsou procesy aktivní, není vhodné, aby všechny používaly server GC.</span><span class="sxs-lookup"><span data-stu-id="605ba-284">If the processes are active, it's not a good idea to have them all use server GC.</span></span>

<span data-ttu-id="605ba-285">Pokud používáte stovky instancí aplikace, zvažte použití uvolňování paměti pracovní stanice s uvolňování souběžných uvolňování paměti zakázáno.</span><span class="sxs-lookup"><span data-stu-id="605ba-285">If you're running hundreds of instances of an application, consider using workstation garbage collection with concurrent garbage collection disabled.</span></span> <span data-ttu-id="605ba-286">To bude mít za následek méně přepínání kontextu, což může zlepšit výkon.</span><span class="sxs-lookup"><span data-stu-id="605ba-286">This will result in less context switching, which can improve performance.</span></span>

## <a name="background-workstation-garbage-collection"></a><span data-ttu-id="605ba-287">Uvolňování paměti pracovní stanice na pozadí</span><span class="sxs-lookup"><span data-stu-id="605ba-287">Background workstation garbage collection</span></span>

<span data-ttu-id="605ba-288">V uvolňování paměti pracovní stanice na pozadí dočasné generace (0 a 1) jsou shromažďovány podle potřeby, zatímco kolekce generace 2 probíhá.</span><span class="sxs-lookup"><span data-stu-id="605ba-288">In background workstation garbage collection, ephemeral generations (0 and 1) are collected as needed while the collection of generation 2 is in progress.</span></span> <span data-ttu-id="605ba-289">Uvolňování paměti pracovní stanice na pozadí se provádí ve vyhrazeném vlákně a platí pouze pro kolekce generace 2.</span><span class="sxs-lookup"><span data-stu-id="605ba-289">Background workstation garbage collection is performed on a dedicated thread and applies only to generation 2 collections.</span></span>

<span data-ttu-id="605ba-290">Uvolňování paměti na pozadí je ve výchozím nastavení povoleno a může být povoleno nebo zakázáno nastavením [konfigurace gcConcurrent](../../../docs/framework/configure-apps/file-schema/runtime/gcconcurrent-element.md) v aplikacích rozhraní .NET Framework nebo v aplikacích [System.GC.Concurrent](../../core/run-time-config/garbage-collector.md#systemgcconcurrentcomplus_gcconcurrent) v aplikacích .NET Core.</span><span class="sxs-lookup"><span data-stu-id="605ba-290">Background garbage collection is enabled by default and can be enabled or disabled with the [gcConcurrent](../../../docs/framework/configure-apps/file-schema/runtime/gcconcurrent-element.md) configuration setting in .NET Framework apps or the [System.GC.Concurrent](../../core/run-time-config/garbage-collector.md#systemgcconcurrentcomplus_gcconcurrent) setting in .NET Core apps.</span></span>

> [!NOTE]
> <span data-ttu-id="605ba-291">Uvolňování paměti na pozadí nahrazuje [souběžné uvolňování paměti](#concurrent-garbage-collection) a je k dispozici v rozhraní .NET Framework 4 a novějších verzích.</span><span class="sxs-lookup"><span data-stu-id="605ba-291">Background garbage collection replaces [concurrent garbage collection](#concurrent-garbage-collection) and is available in .NET Framework 4 and later versions.</span></span> <span data-ttu-id="605ba-292">V rozhraní .NET Framework 4 je podporována pouze pro uvolňování paměti pracovní stanice.</span><span class="sxs-lookup"><span data-stu-id="605ba-292">In .NET Framework 4, it's supported only for workstation garbage collection.</span></span> <span data-ttu-id="605ba-293">Počínaje rozhraním .NET Framework 4.5 je k dispozici uvolňování paměti na pozadí pro uvolňování paměti pracovní stanice i serveru.</span><span class="sxs-lookup"><span data-stu-id="605ba-293">Starting with .NET Framework 4.5, background garbage collection is available for both workstation and server garbage collection.</span></span>

<span data-ttu-id="605ba-294">Kolekce na dočasné generace během uvolňování paměti na pozadí se označuje jako uvolňování paměti popředí.</span><span class="sxs-lookup"><span data-stu-id="605ba-294">A collection on ephemeral generations during background garbage collection is known as foreground garbage collection.</span></span> <span data-ttu-id="605ba-295">Dojde-li k uvolnění paměti v popředí, jsou pozastavena všechna spravovaná vlákna.</span><span class="sxs-lookup"><span data-stu-id="605ba-295">When foreground garbage collections occur, all managed threads are suspended.</span></span>

<span data-ttu-id="605ba-296">Pokud probíhá uvolňování paměti na pozadí a v generaci 0 jste přidělili dostatek objektů, clr provádí generování 0 nebo generace 1 uvolňování paměti na popředí.</span><span class="sxs-lookup"><span data-stu-id="605ba-296">When background garbage collection is in progress and you've allocated enough objects in generation 0, the CLR performs a generation 0 or generation 1 foreground garbage collection.</span></span> <span data-ttu-id="605ba-297">Vyhrazené vlákno uvolňování paměti na pozadí kontroluje v častých bezpečných bodech k určení, zda existuje požadavek na uvolnění paměti v popředí.</span><span class="sxs-lookup"><span data-stu-id="605ba-297">The dedicated background garbage collection thread checks at frequent safe points to determine whether there is a request for foreground garbage collection.</span></span> <span data-ttu-id="605ba-298">Pokud existuje, kolekce pozadí pozastaví sám tak, aby může dojít k uvolňování paměti popředí.</span><span class="sxs-lookup"><span data-stu-id="605ba-298">If there is, the background collection suspends itself so that foreground garbage collection can occur.</span></span> <span data-ttu-id="605ba-299">Po dokončení uvolňování paměti na popředí, vyhrazené vlákno uvolňování paměti na pozadí a vlákna uživatele pokračovat.</span><span class="sxs-lookup"><span data-stu-id="605ba-299">After the foreground garbage collection is completed, the dedicated background garbage collection thread and user threads resume.</span></span>

<span data-ttu-id="605ba-300">Uvolňování paměti na pozadí odebere omezení přidělení uložená souběžným uvolňováním paměti, protože dočasné uvolnění paměti může nastat během uvolňování paměti na pozadí.</span><span class="sxs-lookup"><span data-stu-id="605ba-300">Background garbage collection removes allocation restrictions imposed by concurrent garbage collection, because ephemeral garbage collections can occur during background garbage collection.</span></span> <span data-ttu-id="605ba-301">Uvolňování paměti na pozadí může odebrat nevrácené objekty v dočasných generacích.</span><span class="sxs-lookup"><span data-stu-id="605ba-301">Background garbage collection can remove dead objects in ephemeral generations.</span></span> <span data-ttu-id="605ba-302">Může také rozbalit haldy v případě potřeby během uvolnění paměti generace 1.</span><span class="sxs-lookup"><span data-stu-id="605ba-302">It can also expand the heap if needed during a generation 1 garbage collection.</span></span>

<span data-ttu-id="605ba-303">Následující obrázek znázorňuje uvolňování paměti na pozadí prováděné v samostatném vyhrazeném vlákně na pracovní stanici:</span><span class="sxs-lookup"><span data-stu-id="605ba-303">The following illustration shows background garbage collection performed on a separate dedicated thread on a workstation:</span></span>

![Uvolňování paměti pracovní stanice na pozadí](./media/fundamentals/background-workstation-garbage-collection.png)

### <a name="background-server-garbage-collection"></a><span data-ttu-id="605ba-305">Uvolnění paměti serveru na pozadí</span><span class="sxs-lookup"><span data-stu-id="605ba-305">Background server garbage collection</span></span>

<span data-ttu-id="605ba-306">Počínaje rozhraním .NET Framework 4.5 je uvolnění paměti serveru na pozadí výchozím režimem pro uvolnění paměti serveru.</span><span class="sxs-lookup"><span data-stu-id="605ba-306">Starting with .NET Framework 4.5, background server garbage collection is the default mode for server garbage collection.</span></span>

<span data-ttu-id="605ba-307">Funkce uvolňování paměti serveru na pozadí podobně jako uvolňování paměti pracovní stanice na pozadí, popsané v předchozí části, ale existuje několik rozdílů:</span><span class="sxs-lookup"><span data-stu-id="605ba-307">Background server garbage collection functions similarly to background workstation garbage collection, described in the previous section, but there are a few differences:</span></span>

- <span data-ttu-id="605ba-308">Uvolňování paměti pracovní stanice na pozadí používá jedno vyhrazené vlákno uvolňování paměti na pozadí, zatímco uvolňování paměti serveru na pozadí používá více vláken.</span><span class="sxs-lookup"><span data-stu-id="605ba-308">Background workstation garbage collection uses one dedicated background garbage collection thread, whereas background server garbage collection uses multiple threads.</span></span> <span data-ttu-id="605ba-309">Obvykle je vyhrazené vlákno pro každý logický procesor.</span><span class="sxs-lookup"><span data-stu-id="605ba-309">Typically, there's a dedicated thread for each logical processor.</span></span>

- <span data-ttu-id="605ba-310">Na rozdíl od vlákna uvolňování paměti na pozadí pracovní stanice tyto podprocesy nečasový mat.</span><span class="sxs-lookup"><span data-stu-id="605ba-310">Unlike the workstation background garbage collection thread, these threads do not time out.</span></span>

<span data-ttu-id="605ba-311">Následující obrázek znázorňuje uvolňování paměti na pozadí prováděné v samostatném vyhrazeném vlákně na serveru:</span><span class="sxs-lookup"><span data-stu-id="605ba-311">The following illustration shows background garbage collection performed on a separate dedicated thread on a server:</span></span>

![Uvolnění paměti serveru na pozadí](./media/fundamentals/background-server-garbage-collection.png)

## <a name="concurrent-garbage-collection"></a><span data-ttu-id="605ba-313">Souběžné uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="605ba-313">Concurrent garbage collection</span></span>

> [!TIP]
> <span data-ttu-id="605ba-314">Tento oddíl se vztahuje na:</span><span class="sxs-lookup"><span data-stu-id="605ba-314">This section applies to:</span></span>
>
> - <span data-ttu-id="605ba-315">Rozhraní .NET Framework 3.5 a starší pro uvolňování paměti pracovní stanice</span><span class="sxs-lookup"><span data-stu-id="605ba-315">.NET Framework 3.5 and earlier for workstation garbage collection</span></span>
> - <span data-ttu-id="605ba-316">Rozhraní .NET Framework 4 a starší pro uvolnění paměti serveru</span><span class="sxs-lookup"><span data-stu-id="605ba-316">.NET Framework 4 and earlier for server garbage collection</span></span>
>
> <span data-ttu-id="605ba-317">Souběžné nesmysly jsou nahrazeny [uvolňováním paměti na pozadí](#background-workstation-garbage-collection) v novějších verzích.</span><span class="sxs-lookup"><span data-stu-id="605ba-317">Concurrent garbage is replaced by [background garbage collection](#background-workstation-garbage-collection) in later versions.</span></span>

<span data-ttu-id="605ba-318">V uvolňování paměti pracovní stanice nebo serveru můžete [povolit souběžné uvolňování paměti](../../../docs/framework/configure-apps/file-schema/runtime/gcconcurrent-element.md), které umožňuje souběžné spuštění vláken s vyhrazeným vláknem, které provádí uvolňování paměti po většinu doby trvání kolekce.</span><span class="sxs-lookup"><span data-stu-id="605ba-318">In workstation or server garbage collection, you can [enable concurrent garbage collection](../../../docs/framework/configure-apps/file-schema/runtime/gcconcurrent-element.md), which enables threads to run concurrently with a dedicated thread that performs the garbage collection for most of the duration of the collection.</span></span> <span data-ttu-id="605ba-319">Tato možnost ovlivňuje pouze uvolňování paměti v generaci 2; generace 0 a 1 jsou vždy nesouběžné, protože končí velmi rychle.</span><span class="sxs-lookup"><span data-stu-id="605ba-319">This option affects only garbage collections in generation 2; generations 0 and 1 are always non-concurrent because they finish very fast.</span></span>

<span data-ttu-id="605ba-320">Souběžné uvolňování paměti umožňuje interaktivní masy lépe reagovat minimalizací pauzy pro kolekci.</span><span class="sxs-lookup"><span data-stu-id="605ba-320">Concurrent garbage collection enables interactive applications to be more responsive by minimizing pauses for a collection.</span></span> <span data-ttu-id="605ba-321">Spravovaná vlákna mohou pokračovat ve většině času při spuštění souběžného vlákna uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="605ba-321">Managed threads can continue to run most of the time while the concurrent garbage collection thread is running.</span></span> <span data-ttu-id="605ba-322">To má za následek kratší pauzy při uvolňování paměti dochází.</span><span class="sxs-lookup"><span data-stu-id="605ba-322">This results in shorter pauses while a garbage collection is occurring.</span></span>

<span data-ttu-id="605ba-323">Souběžné uvolňování paměti se provádí ve vyhrazeném vlákně.</span><span class="sxs-lookup"><span data-stu-id="605ba-323">Concurrent garbage collection is performed on a dedicated thread.</span></span> <span data-ttu-id="605ba-324">Ve výchozím nastavení clr spustí uvolňování paměti pracovní stanice s povolenou souběžnou uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="605ba-324">By default, the CLR runs workstation garbage collection with concurrent garbage collection enabled.</span></span> <span data-ttu-id="605ba-325">To platí pro počítače s jedním procesorem a více procesory.</span><span class="sxs-lookup"><span data-stu-id="605ba-325">This is true for single-processor and multi-processor computers.</span></span>

<span data-ttu-id="605ba-326">Následující obrázek znázorňuje souběžné uvolňování paměti prováděné v samostatném vyhrazeném vlákně.</span><span class="sxs-lookup"><span data-stu-id="605ba-326">The following illustration shows concurrent garbage collection performed on a separate dedicated thread.</span></span>

![Souběžná vlákna uvolňování paměti](./media/gc-concurrent.png)

## <a name="see-also"></a><span data-ttu-id="605ba-328">Viz také</span><span class="sxs-lookup"><span data-stu-id="605ba-328">See also</span></span>

- [<span data-ttu-id="605ba-329">Možnosti konfigurace pro gc</span><span class="sxs-lookup"><span data-stu-id="605ba-329">Configuration options for GC</span></span>](../../core/run-time-config/garbage-collector.md)
- [<span data-ttu-id="605ba-330">Uvolněné</span><span class="sxs-lookup"><span data-stu-id="605ba-330">Garbage collection</span></span>](index.md)
