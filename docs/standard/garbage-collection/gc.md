---
title: "Kolekce paměti automatické správy a uvolňování paměti"
description: "Zjistěte, jak automatické paměti Správa je jedním ze služby, které poskytuje modul Common Language Runtime během spravovaného spouštění."
keywords: "Rozhraní .NET, .NET core"
author: dotnet-bot
ms.author: dotnetcontent
ms.date: 07/22/2016
ms.topic: article
ms.prod: .net
ms.technology: dotnet-standard
ms.devlang: dotnet
ms.assetid: d095b0b6-2454-4e23-80b4-c9e8a447116c
ms.openlocfilehash: 30bcf08ebb4e05706d5b25e2f805474dd1ee428e
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/18/2017
---
# <a name="automatic-memory-management-and-garbage-collection"></a><span data-ttu-id="dac02-104">Kolekce paměti automatické správy a uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="dac02-104">Automatic memory management and garbage collection</span></span>

<span data-ttu-id="dac02-105">Automatická správa paměti je jedním z služby, které poskytuje modul Common Language Runtime během spravovaného spouštění.</span><span class="sxs-lookup"><span data-stu-id="dac02-105">Automatic memory management is one of the services that the Common Language Runtime provides during managed execution.</span></span> <span data-ttu-id="dac02-106">Uvolňování paměti Common Language Runtime spravuje přidělování a uvolňování paměti pro aplikaci.</span><span class="sxs-lookup"><span data-stu-id="dac02-106">The Common Language Runtime's garbage collector manages the allocation and release of memory for an application.</span></span> <span data-ttu-id="dac02-107">Pro vývojáře to znamená, že nemáte napsat kód pro provedení paměti, že úlohy správy při vývoji spravované aplikace.</span><span class="sxs-lookup"><span data-stu-id="dac02-107">For developers, this means that you do not have to write code to perform memory management tasks when you develop managed applications.</span></span> <span data-ttu-id="dac02-108">Automatická správa paměti může eliminovat běžné problémy, jako je například zapomenutí volného objektu a způsobuje nevracení paměti nebo pokusu o přístup k paměti pro objekt, který již byl uvolněn.</span><span class="sxs-lookup"><span data-stu-id="dac02-108">Automatic memory management can eliminate common problems, such as forgetting to free an object and causing a memory leak, or attempting to access memory for an object that has already been freed.</span></span> <span data-ttu-id="dac02-109">Tato část popisuje, jak má systém uvolňování přiděluje a uvolní paměť.</span><span class="sxs-lookup"><span data-stu-id="dac02-109">This section describes how the garbage collector allocates and releases memory.</span></span>

## <a name="allocating-memory"></a><span data-ttu-id="dac02-110">Přidělování paměti</span><span class="sxs-lookup"><span data-stu-id="dac02-110">Allocating Memory</span></span>

<span data-ttu-id="dac02-111">Při inicializaci nový proces modulu runtime vyhradí souvislou oblast adresního prostoru pro proces.</span><span class="sxs-lookup"><span data-stu-id="dac02-111">When you initialize a new process, the runtime reserves a contiguous region of address space for the process.</span></span> <span data-ttu-id="dac02-112">Tento vyhrazené adresní prostor se nazývá spravovaná halda.</span><span class="sxs-lookup"><span data-stu-id="dac02-112">This reserved address space is called the managed heap.</span></span> <span data-ttu-id="dac02-113">Spravovaná halda uchovává ukazatel na adresu, kde se další objekt v haldě přidělí.</span><span class="sxs-lookup"><span data-stu-id="dac02-113">The managed heap maintains a pointer to the address where the next object in the heap will be allocated.</span></span> <span data-ttu-id="dac02-114">Na začátku tento ukazatel je nastavena na spravovaná halda základní adresu.</span><span class="sxs-lookup"><span data-stu-id="dac02-114">Initially, this pointer is set to the managed heap's base address.</span></span> <span data-ttu-id="dac02-115">Spravovaná halda přidělovány všechny odkazové typy.</span><span class="sxs-lookup"><span data-stu-id="dac02-115">All reference types are allocated on the managed heap.</span></span> <span data-ttu-id="dac02-116">Když aplikace vytvoří první typ odkazu, se přidělí paměť pro typ na základní adrese spravovaná halda.</span><span class="sxs-lookup"><span data-stu-id="dac02-116">When an application creates the first reference type, memory is allocated for the type at the base address of the managed heap.</span></span> <span data-ttu-id="dac02-117">Aplikace vytvoří další objekt, bude systém uvolňování přidělí paměť pro něj v adresním prostoru, hned za první objekt.</span><span class="sxs-lookup"><span data-stu-id="dac02-117">When the application creates the next object, the garbage collector allocates memory for it in the address space immediately following the first object.</span></span> <span data-ttu-id="dac02-118">Tak dlouho, dokud adresní prostor je k dispozici, pokračuje uvolňování paměti k přidělení místa pro všechny nové objekty tímto způsobem.</span><span class="sxs-lookup"><span data-stu-id="dac02-118">As long as address space is available, the garbage collector continues to allocate space for new objects in this manner.</span></span>

<span data-ttu-id="dac02-119">Přidělování paměti ze spravované haldy je rychlejší než nespravované přidělování paměti.</span><span class="sxs-lookup"><span data-stu-id="dac02-119">Allocating memory from the managed heap is faster than unmanaged memory allocation.</span></span> <span data-ttu-id="dac02-120">Modul runtime přidělí paměť pro objekt přidáním hodnoty do ukazatel, proto je téměř tak rychlý jako přidělování paměti ze zásobníku.</span><span class="sxs-lookup"><span data-stu-id="dac02-120">Because the runtime allocates memory for an object by adding a value to a pointer, it is almost as fast as allocating memory from the stack.</span></span> <span data-ttu-id="dac02-121">Kromě toho nové objekty, které jsou alokovány postupně jsou uloženy souvisle ve spravované haldě, aplikace může přistupovat k objektům velmi rychle.</span><span class="sxs-lookup"><span data-stu-id="dac02-121">In addition, because new objects that are allocated consecutively are stored contiguously in the managed heap, an application can access the objects very quickly.</span></span>

## <a name="releasing-memory"></a><span data-ttu-id="dac02-122">Uvolňování paměti</span><span class="sxs-lookup"><span data-stu-id="dac02-122">Releasing Memory</span></span>

<span data-ttu-id="dac02-123">Optimalizace engine systému uvolňování paměti určuje nejvhodnější čas k provedení v kolekci podle byla provedena alokace.</span><span class="sxs-lookup"><span data-stu-id="dac02-123">The garbage collector's optimizing engine determines the best time to perform a collection based on the allocations being made.</span></span> <span data-ttu-id="dac02-124">Při uvolňování paměti provádí kolekce, uvolní paměť pro objekty, které jsou již používá aplikace.</span><span class="sxs-lookup"><span data-stu-id="dac02-124">When the garbage collector performs a collection, it releases the memory for objects that are no longer being used by the application.</span></span> <span data-ttu-id="dac02-125">Určuje, které objekty jsou již používány zkoumáním kořenů aplikace.</span><span class="sxs-lookup"><span data-stu-id="dac02-125">It determines which objects are no longer being used by examining the application's roots.</span></span> <span data-ttu-id="dac02-126">Každá aplikace má sadu kořenových certifikačních autorit.</span><span class="sxs-lookup"><span data-stu-id="dac02-126">Every application has a set of roots.</span></span> <span data-ttu-id="dac02-127">Kořenovém adresáři každého odkazuje na objekt v haldě spravované nebo je nastaven na hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="dac02-127">Each root either refers to an object on the managed heap or is set to null.</span></span> <span data-ttu-id="dac02-128">Kořeny aplikace zahrnout statických polí, místní proměnné a parametry v zásobníku podprocesu a registry procesoru.</span><span class="sxs-lookup"><span data-stu-id="dac02-128">An application's roots include static fields, local variables and parameters on a thread's stack, and CPU registers.</span></span> <span data-ttu-id="dac02-129">Uvolňování paměti má přístup k seznamu active kořeny, které udržují kompilátoru v běhu (JIT) a modulu runtime.</span><span class="sxs-lookup"><span data-stu-id="dac02-129">The garbage collector has access to the list of active roots that the just-in-time (JIT) compiler and the runtime maintain.</span></span> <span data-ttu-id="dac02-130">Pomocí tohoto seznamu, prověří kořeny aplikace a v procesu vytvoří graf, který obsahuje všechny objekty, které jsou dostupné z kořenů.</span><span class="sxs-lookup"><span data-stu-id="dac02-130">Using this list, it examines an application's roots, and in the process creates a graph that contains all the objects that are reachable from the roots.</span></span>

<span data-ttu-id="dac02-131">Objekty, které nejsou v grafu nejsou dostupné z kořenů aplikace.</span><span class="sxs-lookup"><span data-stu-id="dac02-131">Objects that are not in the graph are unreachable from the application's roots.</span></span> <span data-ttu-id="dac02-132">Uvolňování paměti považuje nedostupný objekty paměti a vydá je paměť přidělená pro ně.</span><span class="sxs-lookup"><span data-stu-id="dac02-132">The garbage collector considers unreachable objects garbage and will release the memory allocated for them.</span></span> <span data-ttu-id="dac02-133">V průběhu kolekce bude systém uvolňování prověří spravovaná halda hledá bloky adresního prostoru obsazené nedostupný objekty.</span><span class="sxs-lookup"><span data-stu-id="dac02-133">During a collection, the garbage collector examines the managed heap, looking for the blocks of address space occupied by unreachable objects.</span></span> <span data-ttu-id="dac02-134">Při zjišťování nedostupný objekt, používá funkci kopírování paměti kompaktních dosažitelné objekty v paměti, uvolnění bloky adresní prostory přidělené nedostupný objekty.</span><span class="sxs-lookup"><span data-stu-id="dac02-134">As it discovers each unreachable object, it uses a memory-copying function to compact the reachable objects in memory, freeing up the blocks of address spaces allocated to unreachable objects.</span></span> <span data-ttu-id="dac02-135">Jakmile je paměť pro dosažitelné objekty byly komprimován, uvolňování paměti provede nezbytné korekce ukazatele tak, aby kořenů aplikace přejděte k objektům v jejich nová umístění.</span><span class="sxs-lookup"><span data-stu-id="dac02-135">Once the memory for the reachable objects has been compacted, the garbage collector makes the necessary pointer corrections so that the application's roots point to the objects in their new locations.</span></span> <span data-ttu-id="dac02-136">Spravovaná halda ukazatel také umístí po poslední dostupný objekt.</span><span class="sxs-lookup"><span data-stu-id="dac02-136">It also positions the managed heap's pointer after the last reachable object.</span></span> <span data-ttu-id="dac02-137">Poznámka: Tento paměti se zkomprimuje pouze v případě, že kolekce zjistí velký počet objektů nedostupný.</span><span class="sxs-lookup"><span data-stu-id="dac02-137">Note that memory is compacted only if a collection discovers a significant number of unreachable objects.</span></span> <span data-ttu-id="dac02-138">Pokud všechny objekty v haldě spravovaný přežijí sběr, není třeba pro komprimaci paměti.</span><span class="sxs-lookup"><span data-stu-id="dac02-138">If all the objects in the managed heap survive a collection, then there is no need for memory compaction.</span></span>

<span data-ttu-id="dac02-139">Chcete-li zvýšit výkon, modul runtime přidělí paměť pro velké objekty v samostatné haldě.</span><span class="sxs-lookup"><span data-stu-id="dac02-139">To improve performance, the runtime allocates memory for large objects in a separate heap.</span></span> <span data-ttu-id="dac02-140">Uvolňování paměti automaticky uvolní paměť pro velké objekty.</span><span class="sxs-lookup"><span data-stu-id="dac02-140">The garbage collector automatically releases the memory for large objects.</span></span> <span data-ttu-id="dac02-141">Nechcete-li přesouvání velkých objektů v paměti, ale není zkomprimovanou tuto paměť.</span><span class="sxs-lookup"><span data-stu-id="dac02-141">However, to avoid moving large objects in memory, this memory is not compacted.</span></span>

## <a name="generations-and-performance"></a><span data-ttu-id="dac02-142">Generace a výkon</span><span class="sxs-lookup"><span data-stu-id="dac02-142">Generations and Performance</span></span>

<span data-ttu-id="dac02-143">Za účelem optimalizace výkonu systému uvolňování spravovaná halda je rozdělené do tří generací: 0, 1 a 2.</span><span class="sxs-lookup"><span data-stu-id="dac02-143">To optimize the performance of the garbage collector, the managed heap is divided into three generations: 0, 1, and 2.</span></span> <span data-ttu-id="dac02-144">Modul runtime paměti kolekce algoritmus je založena na několik generalizace, které zjistí počítač softwarového oboru na hodnotu true, a experimentování se uvolňování paměti kolekci schémat.</span><span class="sxs-lookup"><span data-stu-id="dac02-144">The runtime's garbage collection algorithm is based on several generalizations that the computer software industry has discovered to be true by experimenting with garbage collection schemes.</span></span> <span data-ttu-id="dac02-145">Nejprve je rychlejší compact paměti pro část spravovaná halda než pro celý spravovaná halda.</span><span class="sxs-lookup"><span data-stu-id="dac02-145">First, it is faster to compact the memory for a portion of the managed heap than for the entire managed heap.</span></span> <span data-ttu-id="dac02-146">Za druhé novější objekty budou mít kratší životnost a starší objekty budou mít delší životnost.</span><span class="sxs-lookup"><span data-stu-id="dac02-146">Secondly, newer objects will have shorter lifetimes and older objects will have longer lifetimes.</span></span> <span data-ttu-id="dac02-147">Nakonec novější objekty často vzájemně souvisí a získat přístup k aplikací přibližně ve stejnou dobu.</span><span class="sxs-lookup"><span data-stu-id="dac02-147">Lastly, newer objects tend to be related to each other and accessed by the application around the same time.</span></span>

<span data-ttu-id="dac02-148">Modul runtime systém uvolňování paměti ukládá nové objekty v 0. generace.</span><span class="sxs-lookup"><span data-stu-id="dac02-148">The runtime's garbage collector stores new objects in generation 0.</span></span> <span data-ttu-id="dac02-149">Objekty vytvořené dříve v životního cyklu aplikace, které zůstanou platné i po kolekce jsou povýší a uložené v generace 1 a 2.</span><span class="sxs-lookup"><span data-stu-id="dac02-149">Objects created early in the application's lifetime that survive collections are promoted and stored in generations 1 and 2.</span></span> <span data-ttu-id="dac02-150">Proces povýšení objekt je popsán dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="dac02-150">The process of object promotion is described later in this topic.</span></span> <span data-ttu-id="dac02-151">Protože je rychlejší kompaktních část spravovaná halda než celou haldu, toto schéma umožňuje uvolňování paměti uvolnit paměť v konkrétní generování než uvolnit paměť pro celý spravovaná halda pokaždé, když provádí kolekce.</span><span class="sxs-lookup"><span data-stu-id="dac02-151">Because it is faster to compact a portion of the managed heap than the entire heap, this scheme allows the garbage collector to release the memory in a specific generation rather than release the memory for the entire managed heap each time it performs a collection.</span></span>

<span data-ttu-id="dac02-152">Ve skutečnosti provádí garbage collector v kolekci při zaplnění generace 0.</span><span class="sxs-lookup"><span data-stu-id="dac02-152">In reality, the garbage collector performs a collection when generation 0 is full.</span></span> <span data-ttu-id="dac02-153">Pokud se aplikace pokusí vytvořit nový objekt při zaplnění generace 0, uvolňování paměti zjistí se žádná adresa zbývající prostor v generace 0 přidělení pro objekt.</span><span class="sxs-lookup"><span data-stu-id="dac02-153">If an application attempts to create a new object when generation 0 is full, the garbage collector discovers that there is no address space remaining in generation 0 to allocate for the object.</span></span> <span data-ttu-id="dac02-154">Uvolňování paměti provádí kolekce ve snaze volné místo na adresu v generace 0 pro objekt.</span><span class="sxs-lookup"><span data-stu-id="dac02-154">The garbage collector performs a collection in an attempt to free address space in generation 0 for the object.</span></span> <span data-ttu-id="dac02-155">Uvolňování paměti začne prověřením objektů v generace 0 a nikoli všech objektů v spravovaná halda.</span><span class="sxs-lookup"><span data-stu-id="dac02-155">The garbage collector starts by examining the objects in generation 0 rather than all objects in the managed heap.</span></span> <span data-ttu-id="dac02-156">Toto je nejvhodnější přístup, protože nové objekty mívají k krátkou životnost a očekává se, že mnoho objektů v generace 0 již nebude používán aplikací při provádění kolekce.</span><span class="sxs-lookup"><span data-stu-id="dac02-156">This is the most efficient approach, because new objects tend to have short lifetimes, and it is expected that many of the objects in generation 0 will no longer be in use by the application when a collection is performed.</span></span> <span data-ttu-id="dac02-157">Kromě toho kolekce samotné generace 0 často získá dostatek paměti pro umožňuje aplikaci pokračovat ve vytváření nových objektů.</span><span class="sxs-lookup"><span data-stu-id="dac02-157">In addition, a collection of generation 0 alone often reclaims enough memory to allow the application to continue creating new objects.</span></span>

<span data-ttu-id="dac02-158">Po uvolňování paměti provádí sběr generace 0, zkomprimuje paměť pro dosažitelné objekty jak je popsáno v [uvolňování paměti](#releasing-memory) výše v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="dac02-158">After the garbage collector performs a collection of generation 0, it compacts the memory for the reachable objects as explained in [Releasing Memory](#releasing-memory) earlier in this topic.</span></span> <span data-ttu-id="dac02-159">Uvolňování paměti potom povýší tyto objekty a považuje tuto část spravovaná halda generace 1.</span><span class="sxs-lookup"><span data-stu-id="dac02-159">The garbage collector then promotes these objects and considers this portion of the managed heap generation 1.</span></span> <span data-ttu-id="dac02-160">Protože objekty, které zůstanou platné i po kolekce mívají k delší životnost, má smysl povýšit je vyšší generace.</span><span class="sxs-lookup"><span data-stu-id="dac02-160">Because objects that survive collections tend to have longer lifetimes, it makes sense to promote them to a higher generation.</span></span> <span data-ttu-id="dac02-161">Uvolňování paměti v důsledku toho není nutné přezkoumat objekty v generace 1 a 2 pokaždé, když provádí sběr generace 0.</span><span class="sxs-lookup"><span data-stu-id="dac02-161">As a result, the garbage collector does not have to reexamine the objects in generations 1 and 2 each time it performs a collection of generation 0.</span></span>

<span data-ttu-id="dac02-162">Po uvolňování provede jeho první kolekce generace 0 a zvýší úroveň dostupné objekty, které se 1. generace, považuje zbytek spravovaná halda generace 0.</span><span class="sxs-lookup"><span data-stu-id="dac02-162">After the garbage collector performs its first collection of generation 0 and promotes the reachable objects to generation 1, it considers the remainder of the managed heap generation 0.</span></span> <span data-ttu-id="dac02-163">Nadále přidělit paměť pro nové objekty v 0. generace, dokud zaplnění generace 0 a je nutné provést jiné kolekce.</span><span class="sxs-lookup"><span data-stu-id="dac02-163">It continues to allocate memory for new objects in generation 0 until generation 0 is full and it is necessary to perform another collection.</span></span> <span data-ttu-id="dac02-164">Optimalizace engine systému uvolňování paměti v tomto okamžiku Určuje, zda je potřebné k prozkoumání objektů v starší generace.</span><span class="sxs-lookup"><span data-stu-id="dac02-164">At this point, the garbage collector's optimizing engine determines whether it is necessary to examine the objects in older generations.</span></span> <span data-ttu-id="dac02-165">Například pokud sběr generace 0 nezíská dostatek paměti pro aplikace úspěšně dokončit pokus vytvořit nový objekt, bude systém uvolňování provést sběr generace 1, pak generace 2.</span><span class="sxs-lookup"><span data-stu-id="dac02-165">For example, if a collection of generation 0 does not reclaim enough memory for the application to successfully complete its attempt to create a new object, the garbage collector can perform a collection of generation 1, then generation 2.</span></span> <span data-ttu-id="dac02-166">Pokud to nezíská dostatek paměti, bude systém uvolňování provést kolekce generace 2, 1 nebo 0.</span><span class="sxs-lookup"><span data-stu-id="dac02-166">If this does not reclaim enough memory, the garbage collector can perform a collection of generations 2, 1, and 0.</span></span> <span data-ttu-id="dac02-167">Po každé kolekce uvolňování zkomprimuje dosažitelné objekty v generace 0 a povýší je 1. generace.</span><span class="sxs-lookup"><span data-stu-id="dac02-167">After each collection, the garbage collector compacts the reachable objects in generation 0 and promotes them to generation 1.</span></span> <span data-ttu-id="dac02-168">Objekty v 1. generace, které zůstanou platné i po kolekce povýšené na 2. generace.</span><span class="sxs-lookup"><span data-stu-id="dac02-168">Objects in generation 1 that survive collections are promoted to generation 2.</span></span> <span data-ttu-id="dac02-169">Protože uvolňování podporuje jenom tři generace, zůstanou objekty v 2. generace, které zůstanou platné i po kolekce v 2. generace, dokud jsou určeny k není k dispozici v budoucí kolekci.</span><span class="sxs-lookup"><span data-stu-id="dac02-169">Because the garbage collector supports only three generations, objects in generation 2 that survive a collection remain in generation 2 until they are determined to be unreachable in a future collection.</span></span>

## <a name="releasing-memory-for-unmanaged-resources"></a><span data-ttu-id="dac02-170">Uvolňování paměti pro nespravované prostředky</span><span class="sxs-lookup"><span data-stu-id="dac02-170">Releasing Memory for Unmanaged Resources</span></span>

<span data-ttu-id="dac02-171">Pro většinu objektů, které vytváří vaše aplikace můžete spolehnout na uvolňování automaticky provádět úlohy správy paměti.</span><span class="sxs-lookup"><span data-stu-id="dac02-171">For the majority of the objects that your application creates, you can rely on the garbage collector to automatically perform the necessary memory management tasks.</span></span> <span data-ttu-id="dac02-172">Nicméně nespravované prostředky vyžadují explicitní vyčištění.</span><span class="sxs-lookup"><span data-stu-id="dac02-172">However, unmanaged resources require explicit cleanup.</span></span> <span data-ttu-id="dac02-173">Nejběžnějším typem nespravovaných prostředků je objekt, který zabalí prostředek operačního systému, například popisovač souboru, popisovač okna nebo síťové připojení.</span><span class="sxs-lookup"><span data-stu-id="dac02-173">The most common type of unmanaged resource is an object that wraps an operating system resource, such as a file handle, window handle, or network connection.</span></span> <span data-ttu-id="dac02-174">I když bude systém uvolňování je schopný sledovat životnost spravovaného objektu, který zapouzdřuje nespravovaný prostředek, nemá specifické znalosti o tom, jak vyčistit prostředku.</span><span class="sxs-lookup"><span data-stu-id="dac02-174">Although the garbage collector is able to track the lifetime of a managed object that encapsulates an unmanaged resource, it does not have specific knowledge about how to clean up the resource.</span></span> <span data-ttu-id="dac02-175">Když vytvoříte objekt, který zapouzdřuje nespravovaný prostředek, doporučujeme zadat kód nezbytné k vyčištění nespravovaný prostředek veřejné `Dispose` metoda.</span><span class="sxs-lookup"><span data-stu-id="dac02-175">When you create an object that encapsulates an unmanaged resource, it is recommended that you provide the necessary code to clean up the unmanaged resource in a public `Dispose` method.</span></span> <span data-ttu-id="dac02-176">Tím, že poskytuje `Dispose` metoda, povolíte uživatelům objektu explicitně uvolnit jeho paměť po ukončení práce s daným objektem.</span><span class="sxs-lookup"><span data-stu-id="dac02-176">By providing a `Dispose` method, you enable users of your object to explicitly free its memory when they are finished with the object.</span></span> <span data-ttu-id="dac02-177">Použijete-li objekt, který zapouzdřuje nespravovaný prostředek, byste měli vědět o `Dispose` a pojmenujte ji podle potřeby.</span><span class="sxs-lookup"><span data-stu-id="dac02-177">When you use an object that encapsulates an unmanaged resource, you should be aware of `Dispose` and call it as necessary.</span></span> <span data-ttu-id="dac02-178">Další informace o čištění nespravovaných prostředků a příklad tohoto vzoru návrhu pro implementaci `Dispose`, najdete v části [uvolňování paměti v rozhraní .NET](index.md).</span><span class="sxs-lookup"><span data-stu-id="dac02-178">For more information about cleaning up unmanaged resources and an example of a design pattern for implementing `Dispose`, see [Garbage Collection in .NET](index.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="dac02-179">Viz také</span><span class="sxs-lookup"><span data-stu-id="dac02-179">See Also</span></span>

[<span data-ttu-id="dac02-180">System.GC</span><span class="sxs-lookup"><span data-stu-id="dac02-180">System.GC</span></span>](xref:System.GC)

[<span data-ttu-id="dac02-181">Uvolňování paměti v rozhraní .NET</span><span class="sxs-lookup"><span data-stu-id="dac02-181">Garbage collection in .NET</span></span>](index.md)

