---
title: Návrhový vzor Pozorovatel
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- IObserver(Of T) interface
- IObservable<T> interface
- IObserver<T> interface
- IObservable(Of T) interface
- observer design pattern [.NET Framework]
ms.assetid: 3680171f-f522-453c-aa4a-54f755a78f88
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 06dbc4b7124aa873fd8471794fa25b0f47f8ce3d
ms.sourcegitcommit: d6e27023aeaffc4b5a3cb4b88685018d6284ada4
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/09/2019
ms.locfileid: "67663581"
---
# <a name="observer-design-pattern"></a><span data-ttu-id="730d5-102">Návrhový vzor Pozorovatel</span><span class="sxs-lookup"><span data-stu-id="730d5-102">Observer Design Pattern</span></span>

<span data-ttu-id="730d5-103">Návrhový vzor pozorovatel umožňuje předplatiteli zaregistrovat se a dostávat upozornění od poskytovatele.</span><span class="sxs-lookup"><span data-stu-id="730d5-103">The observer design pattern enables a subscriber to register with and receive notifications from a provider.</span></span> <span data-ttu-id="730d5-104">Je vhodný pro jakýkoli scénář, který vyžaduje nabízené oznámení.</span><span class="sxs-lookup"><span data-stu-id="730d5-104">It is suitable for any scenario that requires push-based notification.</span></span> <span data-ttu-id="730d5-105">Definuje vzor *poskytovatele* (označované také jako *subjektu* nebo *pozorovat*) a nula, jeden nebo více *pozorovatelů*.</span><span class="sxs-lookup"><span data-stu-id="730d5-105">The pattern defines a *provider* (also known as a *subject* or an *observable*) and zero, one, or more *observers*.</span></span> <span data-ttu-id="730d5-106">Pozorovatelé zaregistrovat u poskytovatele a pokaždé, když předdefinovanou podmínku, události nebo změnu stavu dochází, zprostředkovatel automaticky upozorní všechny pozorovatelů ve volání jedné z jejich metod.</span><span class="sxs-lookup"><span data-stu-id="730d5-106">Observers register with the provider, and whenever a predefined condition, event, or state change occurs, the provider automatically notifies all observers by calling one of their methods.</span></span> <span data-ttu-id="730d5-107">Při volání této metody zprostředkovatele lze také poskytovat aktuální informace o stavu pozorovatelů.</span><span class="sxs-lookup"><span data-stu-id="730d5-107">In this method call, the provider can also provide current state information to observers.</span></span> <span data-ttu-id="730d5-108">V rozhraní .NET Framework, se použije návrhový vzor pozorovatel implementací Obecné <xref:System.IObservable%601?displayProperty=nameWithType> a <xref:System.IObserver%601?displayProperty=nameWithType> rozhraní.</span><span class="sxs-lookup"><span data-stu-id="730d5-108">In the .NET Framework, the observer design pattern is applied by implementing the generic <xref:System.IObservable%601?displayProperty=nameWithType> and <xref:System.IObserver%601?displayProperty=nameWithType> interfaces.</span></span> <span data-ttu-id="730d5-109">Parametr obecného typu představuje typ, který poskytuje informace o oznámení.</span><span class="sxs-lookup"><span data-stu-id="730d5-109">The generic type parameter represents the type that provides notification information.</span></span>

## <a name="applying-the-pattern"></a><span data-ttu-id="730d5-110">Použití tohoto vzoru</span><span class="sxs-lookup"><span data-stu-id="730d5-110">Applying the Pattern</span></span>

<span data-ttu-id="730d5-111">Návrhový vzor pozorovatel je vhodná pro distribuované nabízené oznámení, protože umožňuje čisté oddělení mezi dva různé součásti nebo vrstvy aplikace, jako je například vrstva (obchodní logiky) zdroj dat a vrstvě uživatelského rozhraní (Zobrazit).</span><span class="sxs-lookup"><span data-stu-id="730d5-111">The observer design pattern is suitable for distributed push-based notifications, because it supports a clean separation between two different components or application layers, such as a data source (business logic) layer and a user interface (display) layer.</span></span> <span data-ttu-id="730d5-112">Vzorek je možné implementovat pokaždé, když se poskytovatel používá zpětná volání k poskytování své klienty s aktuální informace.</span><span class="sxs-lookup"><span data-stu-id="730d5-112">The pattern can be implemented whenever a provider uses callbacks to supply its clients with current information.</span></span>

<span data-ttu-id="730d5-113">Implementace vzoru vyžaduje, musíte poskytnout následující:</span><span class="sxs-lookup"><span data-stu-id="730d5-113">Implementing the pattern requires that you provide the following:</span></span>

- <span data-ttu-id="730d5-114">Zprostředkovatel nebo předmětu, což je objekt, který odesílá oznámení pozorovatele.</span><span class="sxs-lookup"><span data-stu-id="730d5-114">A provider or subject, which is the object that sends notifications to observers.</span></span> <span data-ttu-id="730d5-115">Zprostředkovatel je třída nebo struktura, která implementuje <xref:System.IObservable%601> rozhraní.</span><span class="sxs-lookup"><span data-stu-id="730d5-115">A provider is a class or structure that implements the <xref:System.IObservable%601> interface.</span></span> <span data-ttu-id="730d5-116">Zprostředkovatel musí implementovat jedinou metodu <xref:System.IObservable%601.Subscribe%2A?displayProperty=nameWithType>, která je volána metodou pozorovatelé, které chcete dostávat upozornění od poskytovatele.</span><span class="sxs-lookup"><span data-stu-id="730d5-116">The provider must implement a single method, <xref:System.IObservable%601.Subscribe%2A?displayProperty=nameWithType>, which is called by observers that wish to receive notifications from the provider.</span></span>

- <span data-ttu-id="730d5-117">Pozorovatele, který je objekt, který dostává oznámení ze zprostředkovatele.</span><span class="sxs-lookup"><span data-stu-id="730d5-117">An observer, which is an object that receives notifications from a provider.</span></span> <span data-ttu-id="730d5-118">Pozorovatelem je třída nebo struktura, která implementuje <xref:System.IObserver%601> rozhraní.</span><span class="sxs-lookup"><span data-stu-id="730d5-118">An observer is a class or structure that implements the <xref:System.IObserver%601> interface.</span></span> <span data-ttu-id="730d5-119">Pozorovatel musí implementovat tři metody, které se nazývají zprostředkovatelem:</span><span class="sxs-lookup"><span data-stu-id="730d5-119">The observer must implement three methods, all of which are called by the provider:</span></span>

  - <span data-ttu-id="730d5-120"><xref:System.IObserver%601.OnNext%2A?displayProperty=nameWithType>, který poskytuje pozorovatel s informacemi o nových nebo aktuální.</span><span class="sxs-lookup"><span data-stu-id="730d5-120"><xref:System.IObserver%601.OnNext%2A?displayProperty=nameWithType>, which supplies the observer with new or current information.</span></span>

  - <span data-ttu-id="730d5-121"><xref:System.IObserver%601.OnError%2A?displayProperty=nameWithType>, který informuje pozorovatele, který došlo k chybě.</span><span class="sxs-lookup"><span data-stu-id="730d5-121"><xref:System.IObserver%601.OnError%2A?displayProperty=nameWithType>, which informs the observer that an error has occurred.</span></span>

  - <span data-ttu-id="730d5-122"><xref:System.IObserver%601.OnCompleted%2A?displayProperty=nameWithType>, což znamená, že zprostředkovatel dokončení odesílání oznámení.</span><span class="sxs-lookup"><span data-stu-id="730d5-122"><xref:System.IObserver%601.OnCompleted%2A?displayProperty=nameWithType>, which indicates that the provider has finished sending notifications.</span></span>

- <span data-ttu-id="730d5-123">Mechanismus, který umožňuje poskytovateli udržovat přehled o pozorovatelů.</span><span class="sxs-lookup"><span data-stu-id="730d5-123">A mechanism that allows the provider to keep track of observers.</span></span> <span data-ttu-id="730d5-124">Obvykle, poskytovatel použije objekt kontejneru, jako <xref:System.Collections.Generic.List%601?displayProperty=nameWithType> objekt obsahovat odkazy na <xref:System.IObserver%601> implementace, které mají přihlásili k odběru oznámení.</span><span class="sxs-lookup"><span data-stu-id="730d5-124">Typically, the provider uses a container object, such as a <xref:System.Collections.Generic.List%601?displayProperty=nameWithType> object, to hold references to the <xref:System.IObserver%601> implementations that have subscribed to notifications.</span></span> <span data-ttu-id="730d5-125">Použití kontejner úložiště pro tento účel umožňuje poskytovateli zpracování nula k neomezenému počtu pozorovatelů.</span><span class="sxs-lookup"><span data-stu-id="730d5-125">Using a storage container for this purpose enables the provider to handle zero to an unlimited number of observers.</span></span> <span data-ttu-id="730d5-126">Není definováno pořadí, ve kterém pozorovatelů dostávat oznámení; Zprostředkovatel je můžete použít libovolnou metodu pro určení pořadí.</span><span class="sxs-lookup"><span data-stu-id="730d5-126">The order in which observers receive notifications is not defined; the provider is free to use any method to determine the order.</span></span>

- <span data-ttu-id="730d5-127"><xref:System.IDisposable> Implementace, která umožňuje poskytovateli odeberte pozorovatelů po oznámení dokončení.</span><span class="sxs-lookup"><span data-stu-id="730d5-127">An <xref:System.IDisposable> implementation that enables the provider to remove observers when notification is complete.</span></span> <span data-ttu-id="730d5-128">Pozorovatelé zobrazí odkaz na <xref:System.IDisposable> implementaci <xref:System.IObservable%601.Subscribe%2A> metoda, takže můžete také volat <xref:System.IDisposable.Dispose%2A?displayProperty=nameWithType> metoda k odhlášení odběru před dokončením příkazu se zprostředkovatel, odesílání oznámení.</span><span class="sxs-lookup"><span data-stu-id="730d5-128">Observers receive a reference to the <xref:System.IDisposable> implementation from the <xref:System.IObservable%601.Subscribe%2A> method, so they can also call the <xref:System.IDisposable.Dispose%2A?displayProperty=nameWithType> method to unsubscribe before the provider has finished sending notifications.</span></span>

- <span data-ttu-id="730d5-129">Objekt, který obsahuje data, která odešle zprostředkovatel jeho pozorovatelů.</span><span class="sxs-lookup"><span data-stu-id="730d5-129">An object that contains the data that the provider sends to its observers.</span></span> <span data-ttu-id="730d5-130">Typ tohoto objektu, který odpovídá parametru obecného typu <xref:System.IObservable%601> a <xref:System.IObserver%601> rozhraní.</span><span class="sxs-lookup"><span data-stu-id="730d5-130">The type of this object corresponds to the generic type parameter of the <xref:System.IObservable%601> and <xref:System.IObserver%601> interfaces.</span></span> <span data-ttu-id="730d5-131">I když tento objekt může být stejný jako <xref:System.IObservable%601> implementace, nejčastěji je samostatného typu.</span><span class="sxs-lookup"><span data-stu-id="730d5-131">Although this object can be the same as the <xref:System.IObservable%601> implementation, most commonly it is a separate type.</span></span>

> [!NOTE]
> <span data-ttu-id="730d5-132">Kromě provádění návrhový vzor pozorovatel vás může zajímat zkoumání knihovny, které se vytvářejí pomocí <xref:System.IObservable%601> a <xref:System.IObserver%601> rozhraní.</span><span class="sxs-lookup"><span data-stu-id="730d5-132">In addition to implementing the observer design pattern, you may be interested in exploring libraries that are built using the <xref:System.IObservable%601> and <xref:System.IObserver%601> interfaces.</span></span> <span data-ttu-id="730d5-133">Například [Reactive Extensions pro platformu .NET (obdx)](https://docs.microsoft.com/previous-versions/dotnet/reactive-extensions/hh242985(v=vs.103)) sestávat z několika metod rozšíření a pořadí standardní operátory LINQ, aby podporoval asynchronní programování.</span><span class="sxs-lookup"><span data-stu-id="730d5-133">For example, [Reactive Extensions for .NET (Rx)](https://docs.microsoft.com/previous-versions/dotnet/reactive-extensions/hh242985(v=vs.103)) consist of a set of extension methods and LINQ standard sequence operators to support asynchronous programming.</span></span>

## <a name="implementing-the-pattern"></a><span data-ttu-id="730d5-134">Implementace vzoru</span><span class="sxs-lookup"><span data-stu-id="730d5-134">Implementing the Pattern</span></span>

<span data-ttu-id="730d5-135">Návrhový vzor pozorovatel k implementaci informačním systému letiště sobě deklarace identity v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="730d5-135">The following example uses the observer design pattern to implement an airport baggage claim information system.</span></span> <span data-ttu-id="730d5-136">A `BaggageInfo` třída poskytuje informace o opravovány letů a karusel, kde je k dispozici pro vyzvednutí sobě z jednotlivých let.</span><span class="sxs-lookup"><span data-stu-id="730d5-136">A `BaggageInfo` class provides information about arriving flights and the carousels where baggage from each flight is available for pickup.</span></span> <span data-ttu-id="730d5-137">Je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="730d5-137">It is shown in the following example.</span></span>

[!code-csharp[Conceptual.ObserverDesignPattern#1](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.observerdesignpattern/cs/provider.cs#1)]
[!code-vb[Conceptual.ObserverDesignPattern#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.observerdesignpattern/vb/provider.vb#1)]

<span data-ttu-id="730d5-138">A `BaggageHandler` třídy zodpovídá za příjem informací o lety opravovány a sobě Karusel deklarací identity.</span><span class="sxs-lookup"><span data-stu-id="730d5-138">A `BaggageHandler` class is responsible for receiving information about arriving flights and baggage claim carousels.</span></span> <span data-ttu-id="730d5-139">Interně spravuje dvě kolekce:</span><span class="sxs-lookup"><span data-stu-id="730d5-139">Internally, it maintains two collections:</span></span>

- <span data-ttu-id="730d5-140">`observers` -Kolekce klienty, kteří budou dostávat aktualizované informace.</span><span class="sxs-lookup"><span data-stu-id="730d5-140">`observers` - A collection of clients that will receive updated information.</span></span>

- <span data-ttu-id="730d5-141">`flights` -Kolekce letů a jejich přiřazené Karusel.</span><span class="sxs-lookup"><span data-stu-id="730d5-141">`flights` - A collection of flights and their assigned carousels.</span></span>

<span data-ttu-id="730d5-142">Obě kolekce jsou reprezentované pomocí obecného <xref:System.Collections.Generic.List%601> objekty, které jsou vytvořeny v `BaggageHandler` konstruktoru třídy.</span><span class="sxs-lookup"><span data-stu-id="730d5-142">Both collections are represented by generic <xref:System.Collections.Generic.List%601> objects that are instantiated in the `BaggageHandler` class constructor.</span></span> <span data-ttu-id="730d5-143">Zdrojový kód `BaggageHandler` třídy je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="730d5-143">The source code for the `BaggageHandler` class is shown in the following example.</span></span>

[!code-csharp[Conceptual.ObserverDesignPattern#2](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.observerdesignpattern/cs/provider.cs#2)]
[!code-vb[Conceptual.ObserverDesignPattern#2](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.observerdesignpattern/vb/provider.vb#2)]

<span data-ttu-id="730d5-144">Klienti, kteří si nepřejete dostávat aktualizované informace volání `BaggageHandler.Subscribe` metody.</span><span class="sxs-lookup"><span data-stu-id="730d5-144">Clients that wish to receive updated information call the `BaggageHandler.Subscribe` method.</span></span> <span data-ttu-id="730d5-145">Pokud klient nebyl dříve přihlásili k odběru oznámení, odkaz na straně klienta <xref:System.IObserver%601> implementace je přidán do `observers` kolekce.</span><span class="sxs-lookup"><span data-stu-id="730d5-145">If the client has not previously subscribed to notifications, a reference to the client's <xref:System.IObserver%601> implementation is added to the `observers` collection.</span></span>

<span data-ttu-id="730d5-146">Přetížené `BaggageHandler.BaggageStatus` metodu lze volat pro označení, že sobě z let uvolňován, nebo je již uvolněna.</span><span class="sxs-lookup"><span data-stu-id="730d5-146">The overloaded `BaggageHandler.BaggageStatus` method can be called to indicate that baggage from a flight either is being unloaded or is no longer being unloaded.</span></span> <span data-ttu-id="730d5-147">V prvním případě metodě je předána číslo letu, letiště, ze kterého pochází letu a karuselu kde sobě uvolňován.</span><span class="sxs-lookup"><span data-stu-id="730d5-147">In the first case, the method is passed a flight number, the airport from which the flight originated, and the carousel where baggage is being unloaded.</span></span> <span data-ttu-id="730d5-148">V druhém případě metodě je předána jenom číslo letu.</span><span class="sxs-lookup"><span data-stu-id="730d5-148">In the second case, the method is passed only a flight number.</span></span> <span data-ttu-id="730d5-149">K sobě, která se zrušilo jeho načtení, metoda zkontroluje, jestli `BaggageInfo` informace předaný metodě existuje v `flights` kolekce.</span><span class="sxs-lookup"><span data-stu-id="730d5-149">For baggage that is being unloaded, the method checks whether the `BaggageInfo` information passed to the method exists in the `flights` collection.</span></span> <span data-ttu-id="730d5-150">Pokud tomu tak není, metoda přidá informace a zavolá každý pozorovatel `OnNext` metody.</span><span class="sxs-lookup"><span data-stu-id="730d5-150">If it does not, the method adds the information and calls each observer's `OnNext` method.</span></span> <span data-ttu-id="730d5-151">Pro lety, jehož sobě již probíhá uvolnění, metoda zkontroluje, zda informace na této cestě jsou uloženy v `flights` kolekce.</span><span class="sxs-lookup"><span data-stu-id="730d5-151">For flights whose baggage is no longer being unloaded, the method checks whether information on that flight is stored in the `flights` collection.</span></span> <span data-ttu-id="730d5-152">Pokud se jedná, volá metodu každý pozorovatel `OnNext` metoda a odebere `BaggageInfo` objektu z `flights` kolekce.</span><span class="sxs-lookup"><span data-stu-id="730d5-152">If it is, the method calls each observer's `OnNext` method and removes the `BaggageInfo` object from the `flights` collection.</span></span>

<span data-ttu-id="730d5-153">Při poslední letu dne má dostali a zpracovala jeho sobě `BaggageHandler.LastBaggageClaimed` metoda je volána.</span><span class="sxs-lookup"><span data-stu-id="730d5-153">When the last flight of the day has landed and its baggage has been processed, the `BaggageHandler.LastBaggageClaimed` method is called.</span></span> <span data-ttu-id="730d5-154">Tato metoda volá každý pozorovatel `OnCompleted` indikace, že všechna oznámení dokončení a poté vymaže `observers` kolekce.</span><span class="sxs-lookup"><span data-stu-id="730d5-154">This method calls each observer's `OnCompleted` method to indicate that all notifications have completed, and then clears the `observers` collection.</span></span>

<span data-ttu-id="730d5-155">Poskytovatele <xref:System.IObservable%601.Subscribe%2A> vrátí metoda <xref:System.IDisposable> implementace, která umožňuje pozorovatelů k ukončení přijímání oznámení před <xref:System.IObserver%601.OnCompleted%2A> metoda je volána.</span><span class="sxs-lookup"><span data-stu-id="730d5-155">The provider's <xref:System.IObservable%601.Subscribe%2A> method returns an <xref:System.IDisposable> implementation that enables observers to stop receiving notifications before the <xref:System.IObserver%601.OnCompleted%2A> method is called.</span></span> <span data-ttu-id="730d5-156">Zdrojový kód `Unsubscriber(Of BaggageInfo)` třídy je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="730d5-156">The source code for this `Unsubscriber(Of BaggageInfo)` class is shown in the following example.</span></span> <span data-ttu-id="730d5-157">Když je vytvořena instance třídy v `BaggageHandler.Subscribe` metodě je předána odkazem na `observers` kolekce a odkaz na pozorovatele, který je přidán do kolekce.</span><span class="sxs-lookup"><span data-stu-id="730d5-157">When the class is instantiated in the `BaggageHandler.Subscribe` method, it is passed a reference to the `observers` collection and a reference to the observer that is added to the collection.</span></span> <span data-ttu-id="730d5-158">Tyto odkazy jsou přiřazeny k místní proměnné.</span><span class="sxs-lookup"><span data-stu-id="730d5-158">These references are assigned to local variables.</span></span> <span data-ttu-id="730d5-159">Při objektu `Dispose` metoda je volána, zkontroluje, zda pozorovatel stále existuje v `observers` kolekce a pokud ano, odebere pozorovatele.</span><span class="sxs-lookup"><span data-stu-id="730d5-159">When the object's `Dispose` method is called, it checks whether the observer still exists in the `observers` collection, and, if it does, removes the observer.</span></span>

[!code-csharp[Conceptual.ObserverDesignPattern#3](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.observerdesignpattern/cs/provider.cs#3)]
[!code-vb[Conceptual.ObserverDesignPattern#3](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.observerdesignpattern/vb/provider.vb#3)]

<span data-ttu-id="730d5-160">Následující příklad uvádí <xref:System.IObserver%601> implementace s názvem `ArrivalsMonitor`, což je základní třída, která zobrazí informace o deklaraci identity sobě.</span><span class="sxs-lookup"><span data-stu-id="730d5-160">The following example provides an <xref:System.IObserver%601> implementation named `ArrivalsMonitor`, which is a base class that displays baggage claim information.</span></span> <span data-ttu-id="730d5-161">Informace se zobrazí abecedně, podle názvu původní město.</span><span class="sxs-lookup"><span data-stu-id="730d5-161">The information is displayed alphabetically, by the name of the originating city.</span></span> <span data-ttu-id="730d5-162">Metody `ArrivalsMonitor` jsou označeny jako `overridable` (v jazyce Visual Basic) nebo `virtual` (v C#), takže je lze všechny přepsat v odvozené třídě.</span><span class="sxs-lookup"><span data-stu-id="730d5-162">The methods of `ArrivalsMonitor` are marked as `overridable` (in Visual Basic) or `virtual` (in C#), so they can all be overridden by a derived class.</span></span>

[!code-csharp[Conceptual.ObserverDesignPattern#4](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.observerdesignpattern/cs/observer.cs#4)]
[!code-vb[Conceptual.ObserverDesignPattern#4](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.observerdesignpattern/vb/observer.vb#4)]

<span data-ttu-id="730d5-163">`ArrivalsMonitor` Třída zahrnuje `Subscribe` a `Unsubscribe` metody.</span><span class="sxs-lookup"><span data-stu-id="730d5-163">The `ArrivalsMonitor` class includes the `Subscribe` and `Unsubscribe` methods.</span></span> <span data-ttu-id="730d5-164">`Subscribe` Metoda umožňuje třídě Uložit <xref:System.IDisposable> implementace vrácený voláním na <xref:System.IObservable%601.Subscribe%2A> privátní proměnné.</span><span class="sxs-lookup"><span data-stu-id="730d5-164">The `Subscribe` method enables the class to save the <xref:System.IDisposable> implementation returned by the call to <xref:System.IObservable%601.Subscribe%2A> to a private variable.</span></span> <span data-ttu-id="730d5-165">`Unsubscribe` Metoda umožňuje třídě odběr oznámení voláním poskytovatele se <xref:System.IDisposable.Dispose%2A> implementace.</span><span class="sxs-lookup"><span data-stu-id="730d5-165">The `Unsubscribe` method enables the class to unsubscribe from notifications by calling the provider's <xref:System.IDisposable.Dispose%2A> implementation.</span></span> <span data-ttu-id="730d5-166">`ArrivalsMonitor` také poskytuje implementace <xref:System.IObserver%601.OnNext%2A>, <xref:System.IObserver%601.OnError%2A>, a <xref:System.IObserver%601.OnCompleted%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="730d5-166">`ArrivalsMonitor` also provides implementations of the <xref:System.IObserver%601.OnNext%2A>, <xref:System.IObserver%601.OnError%2A>, and <xref:System.IObserver%601.OnCompleted%2A> methods.</span></span> <span data-ttu-id="730d5-167">Pouze <xref:System.IObserver%601.OnNext%2A> značné množství kódu obsahuje implementaci.</span><span class="sxs-lookup"><span data-stu-id="730d5-167">Only the <xref:System.IObserver%601.OnNext%2A> implementation contains a significant amount of code.</span></span> <span data-ttu-id="730d5-168">Metoda pracuje s privátní, seřazený, obecný <xref:System.Collections.Generic.List%601> objekt, který uchovává informace o letištích počátek opravovány letů a karusel, na kterých je jejich sobě k dispozici.</span><span class="sxs-lookup"><span data-stu-id="730d5-168">The method works with a private, sorted, generic <xref:System.Collections.Generic.List%601> object that maintains information about the airports of origin for arriving flights and the carousels on which their baggage is available.</span></span> <span data-ttu-id="730d5-169">Pokud `BaggageHandler` třídy hlásí nový doručení letu, <xref:System.IObserver%601.OnNext%2A> implementace metody přidá informace o této cestě do seznamu.</span><span class="sxs-lookup"><span data-stu-id="730d5-169">If the `BaggageHandler` class reports a new flight arrival, the <xref:System.IObserver%601.OnNext%2A> method implementation adds information about that flight to the list.</span></span> <span data-ttu-id="730d5-170">Pokud `BaggageHandler` třídy hlásí, že letu sobě byl uvolněn, <xref:System.IObserver%601.OnNext%2A> metoda odstraní tento let ze seznamu.</span><span class="sxs-lookup"><span data-stu-id="730d5-170">If the `BaggageHandler` class reports that the flight's baggage has been unloaded, the <xref:System.IObserver%601.OnNext%2A> method removes that flight from the list.</span></span> <span data-ttu-id="730d5-171">Pokaždé, když dojde ke změně, je v seznamu seřazené a zobrazí v konzole.</span><span class="sxs-lookup"><span data-stu-id="730d5-171">Whenever a change is made, the list is sorted and displayed to the console.</span></span>

<span data-ttu-id="730d5-172">Následující příklad obsahuje vstupní bod aplikace, která vytváří instanci `BaggageHandler` třídy a také dvě instance `ArrivalsMonitor` třídy a používá `BaggageHandler.BaggageStatus` metodu pro přidání a odebrání informací o opravovány lety.</span><span class="sxs-lookup"><span data-stu-id="730d5-172">The following example contains the application entry point that instantiates the `BaggageHandler` class as well as two instances of the `ArrivalsMonitor` class, and uses the `BaggageHandler.BaggageStatus` method to add and remove information about arriving flights.</span></span> <span data-ttu-id="730d5-173">V obou případech pozorovatelů získávat aktualizace a správně zobrazit informace o deklaraci identity sobě.</span><span class="sxs-lookup"><span data-stu-id="730d5-173">In each case, the observers receive updates and correctly display baggage claim information.</span></span>

[!code-csharp[Conceptual.ObserverDesignPattern#5](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.observerdesignpattern/cs/program.cs#5)]
[!code-vb[Conceptual.ObserverDesignPattern#5](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.observerdesignpattern/vb/module1.vb#5)]

## <a name="related-topics"></a><span data-ttu-id="730d5-174">Související témata</span><span class="sxs-lookup"><span data-stu-id="730d5-174">Related Topics</span></span>

|<span data-ttu-id="730d5-175">Název</span><span class="sxs-lookup"><span data-stu-id="730d5-175">Title</span></span>|<span data-ttu-id="730d5-176">Popis</span><span class="sxs-lookup"><span data-stu-id="730d5-176">Description</span></span>|
|-----------|-----------------|
|[<span data-ttu-id="730d5-177">Doporučené postupy pro návrhový vzor Pozorovatel</span><span class="sxs-lookup"><span data-stu-id="730d5-177">Observer Design Pattern Best Practices</span></span>](../../../docs/standard/events/observer-design-pattern-best-practices.md)|<span data-ttu-id="730d5-178">Popisuje osvědčené postupy při vývoji aplikací, které implementují návrhový vzor pozorovatel přijmout.</span><span class="sxs-lookup"><span data-stu-id="730d5-178">Describes best practices to adopt when developing applications that implement the observer design pattern.</span></span>|
|[<span data-ttu-id="730d5-179">Postupy: Implementace poskytovatele</span><span class="sxs-lookup"><span data-stu-id="730d5-179">How to: Implement a Provider</span></span>](../../../docs/standard/events/how-to-implement-a-provider.md)|<span data-ttu-id="730d5-180">Poskytuje podrobné implementaci zprostředkovatele pro teploty monitorování aplikace.</span><span class="sxs-lookup"><span data-stu-id="730d5-180">Provides a step-by-step implementation of a provider for a temperature monitoring application.</span></span>|
|[<span data-ttu-id="730d5-181">Postupy: Implementace pozorovatele</span><span class="sxs-lookup"><span data-stu-id="730d5-181">How to: Implement an Observer</span></span>](../../../docs/standard/events/how-to-implement-an-observer.md)|<span data-ttu-id="730d5-182">Poskytuje podrobné provádění pozorovatele pro teploty monitorování aplikace.</span><span class="sxs-lookup"><span data-stu-id="730d5-182">Provides a step-by-step implementation of an observer for a temperature monitoring application.</span></span>|
