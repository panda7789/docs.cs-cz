---
title: Překlad externích prostředků
ms.date: 03/30/2017
ms.technology: dotnet-standard
ms.assetid: ad3fa320-4b8f-4e5c-b549-01157591007a
author: mairaw
ms.author: mairaw
ms.openlocfilehash: ef31d101769dca00f5cff545c72b3afbd59bc638
ms.sourcegitcommit: 8c2ece71e54f46aef9a2153540d0bda7e74b19a9
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/12/2018
ms.locfileid: "44509983"
---
# <a name="resolving-external-resources"></a><span data-ttu-id="a625d-102">Překlad externích prostředků</span><span class="sxs-lookup"><span data-stu-id="a625d-102">Resolving External Resources</span></span>
<span data-ttu-id="a625d-103">**Objekt XmlResolver** vlastnost **XmlDocument** používá **třídou XMLDocument nastavenou na** třídy najít prostředky, které nejsou vložené v datech XML, jako je například typ externího dokumentu definice (DTD), entit a schémata.</span><span class="sxs-lookup"><span data-stu-id="a625d-103">The **XmlResolver** property of the **XmlDocument** is used by the **XmlDocument** class to locate resources that are not inline in the XML data, such as external document type definitions (DTDs), entities, and schemas.</span></span> <span data-ttu-id="a625d-104">Tyto položky mohou být umístěny v síti nebo na místním disku a identifikovat podle identifikátor URI (Uniform Resource).</span><span class="sxs-lookup"><span data-stu-id="a625d-104">These items can be located on a network or on a local drive, and are identifiable by a Uniform Resource Identifier (URI).</span></span> <span data-ttu-id="a625d-105">Díky tomu **XmlDocument** vyřešit **EntityReference** uzlů, které jsou k dispozici v dokumentu a ověřit dokument podle externí DTD nebo schématu.</span><span class="sxs-lookup"><span data-stu-id="a625d-105">This allows the **XmlDocument** to resolve **EntityReference** nodes that are present in the document and validate the document according to the external DTD or schema.</span></span>  
  
## <a name="fully-trusted-xmldocument"></a><span data-ttu-id="a625d-106">Plně důvěryhodné XmlDocument</span><span class="sxs-lookup"><span data-stu-id="a625d-106">Fully-Trusted XmlDocument</span></span>  
 <span data-ttu-id="a625d-107">**Objekt XmlResolver** vlastnost má vliv na funkci **XmlDocument.Load** metody.</span><span class="sxs-lookup"><span data-stu-id="a625d-107">The **XmlResolver** property affects the functionality of the **XmlDocument.Load** method.</span></span> <span data-ttu-id="a625d-108">V tabulce níže ukazuje jak **XmlDocument.XmlResolver** vlastnost funguje, když **XmlDocument** objektu je plně důvěryhodné.</span><span class="sxs-lookup"><span data-stu-id="a625d-108">The table below shows how the **XmlDocument.XmlResolver** property works when the **XmlDocument** object is fully trusted.</span></span> <span data-ttu-id="a625d-109">Následující tabulka ukazuje **XmlDocument.Load** metody, když je vstup pro zatížení **TextReader**, **řetězec**, **Stream**, nebo  **Identifikátor URI**.</span><span class="sxs-lookup"><span data-stu-id="a625d-109">The following table shows the **XmlDocument.Load** methods when the input to the Load is a **TextReader**, **String**, **Stream**, or **URI**.</span></span> <span data-ttu-id="a625d-110">Tato tabulka se nedá použít u **zatížení** metoda Pokud **třídou XMLDocument nastavenou na** je načteno z **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a625d-110">This table does not apply to the **Load** method if the **XmlDocument** is loaded from an **XmlReader**.</span></span>  
  
|<span data-ttu-id="a625d-111">Objekt XmlResolver vlastnost</span><span class="sxs-lookup"><span data-stu-id="a625d-111">XmlResolver property</span></span>|<span data-ttu-id="a625d-112">Funkce</span><span class="sxs-lookup"><span data-stu-id="a625d-112">Function</span></span>|<span data-ttu-id="a625d-113">Poznámky</span><span class="sxs-lookup"><span data-stu-id="a625d-113">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="a625d-114">Je nastavena na **objekt XmlResolver** třídu, která byla dříve vytvořena a má vlastnosti již nastaven na něm uživatel.</span><span class="sxs-lookup"><span data-stu-id="a625d-114">The property is set to an **XmlResolver** class that has been previously created and has properties already set on it by the user.</span></span>|<span data-ttu-id="a625d-115">**XmlDocument** používá **objekt XmlResolver** , která je přidělena přeložit názvy souborů, chcete-li vyřešit odkazy na externí prostředky, jako je specifikace DTD, entit a schémata.</span><span class="sxs-lookup"><span data-stu-id="a625d-115">The **XmlDocument** uses the **XmlResolver** that is given to resolve file names, to resolve references to external resources such as DTDs, entities, and schemas.</span></span><br /><br /> <span data-ttu-id="a625d-116">**Objekt XmlResolver** se také používá při překlad externích prostředků, které jsou potřeba při přidávání nebo úpravách uzly v **XmlDocument**.</span><span class="sxs-lookup"><span data-stu-id="a625d-116">The **XmlResolver** is also used when resolving external resources that are needed when adding or editing nodes in the **XmlDocument**.</span></span>|<span data-ttu-id="a625d-117">**Objekt XmlResolver** věnovat **XmlDocument** je překladače, který se používá při každém externím prostředkům potřeba nachází a vyřešit.</span><span class="sxs-lookup"><span data-stu-id="a625d-117">The **XmlResolver** given to the **XmlDocument** is the resolver that is used whenever external resources need to be located and resolved.</span></span>|  
|<span data-ttu-id="a625d-118">Je nastavena na **null** (**nic** v aplikaci Microsoft Visual Basic .NET).</span><span class="sxs-lookup"><span data-stu-id="a625d-118">The property is set to **null** (**Nothing** in Microsoft Visual Basic .NET).</span></span>|<span data-ttu-id="a625d-119">Funkce, které vyžadují externího prostředku nejsou podporovány, jako je například hledání externí schéma nebo DTD.</span><span class="sxs-lookup"><span data-stu-id="a625d-119">Features that require an external resource are not supported, such as locating an external schema, or DTD.</span></span> <span data-ttu-id="a625d-120">Ani externí entity bude vyřešen a provádí úpravy funkce, jako je například vkládání uzlů entity, které vyžadují řešení, se nepodporují.</span><span class="sxs-lookup"><span data-stu-id="a625d-120">Nor will external entities be resolved, and performing editing functions, such as inserting entity nodes that require resolution, are not supported.</span></span>|<span data-ttu-id="a625d-121">**XmlDocument** načte soubory pod označením anonymní a nebude pokoušet o vyřešit všechny další prostředky.</span><span class="sxs-lookup"><span data-stu-id="a625d-121">The **XmlDocument** loads files as anonymous and does not attempt to resolve any other resources.</span></span>|  
|<span data-ttu-id="a625d-122">Vlastnost není nastavena, ale zbývá ve svém výchozím stavu.</span><span class="sxs-lookup"><span data-stu-id="a625d-122">The property is not set, but left in its default state.</span></span>|<span data-ttu-id="a625d-123">**XmlUrlResolver** s hodnotou NULL pověření vytvořena instance, který se používá **XmlDocument** při překládání názvů souboru, vyhledávání externí specifikace DTD, entit a schémata, a **null** přihlašovací údaje se používají při úpravách uzly.</span><span class="sxs-lookup"><span data-stu-id="a625d-123">An **XmlUrlResolver** with NULL credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>||  
  
 <span data-ttu-id="a625d-124">Následující tabulka ukazuje **XmlDocument.Load** – metoda při vstupu do **zatížení** je **XmlReader** a **třídou XMLDocument nastavenou na** je plně důvěryhodné.</span><span class="sxs-lookup"><span data-stu-id="a625d-124">The following table shows the **XmlDocument.Load** method when the input to the **Load** is an **XmlReader** and the **XmlDocument** is fully trusted.</span></span>  
  
|<span data-ttu-id="a625d-125">Objekt XmlResolver vlastnost</span><span class="sxs-lookup"><span data-stu-id="a625d-125">XmlResolver property</span></span>|<span data-ttu-id="a625d-126">Funkce</span><span class="sxs-lookup"><span data-stu-id="a625d-126">Function</span></span>|<span data-ttu-id="a625d-127">Poznámky</span><span class="sxs-lookup"><span data-stu-id="a625d-127">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="a625d-128">**Objekt XmlResolver** třídy používané **XmlDocument** je stejnou třídou, která je používána ve **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a625d-128">The **XmlResolver** class used by the **XmlDocument** is the same class being used by the **XmlReader**.</span></span>|<span data-ttu-id="a625d-129">**XmlDocument** používá **objekt XmlResolver** , který byl přiřazen **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a625d-129">The **XmlDocument** uses the **XmlResolver** that was assigned to the **XmlReader**.</span></span><br /><br /> <span data-ttu-id="a625d-130">**XmlDocument.Resolver** nemůže být vlastnost nastavena, bez ohledu na to **XmlDocument** úroveň, důvěryhodnosti, protože je stále **objekt XmlResolver** z  **Objekt XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a625d-130">The **XmlDocument.Resolver** property cannot be set, regardless of the **XmlDocument** trust level, because it is getting an **XmlResolver** from the **XmlReader**.</span></span> <span data-ttu-id="a625d-131">Pokusíte přepsat nastavení **XmlReaders**" **objekt XmlResolver** nastavením **objekt XmlResolver** vlastnost **XmlDocument**.</span><span class="sxs-lookup"><span data-stu-id="a625d-131">You cannot attempt to override the settings of the **XmlReaders**' **XmlResolver** by setting the **XmlResolver** property of the **XmlDocument**.</span></span>|<span data-ttu-id="a625d-132">**XmlReader** může být **XmlTextReader**, **objekt XmlValidatingReader**, nebo čtečku zapisovat vlastní.</span><span class="sxs-lookup"><span data-stu-id="a625d-132">The **XmlReader** can be the **XmlTextReader**, **XmlValidatingReader**, or a custom-written reader.</span></span> <span data-ttu-id="a625d-133">Pokud čtení byl použit podporuje entity řešení, jsou vyřešeny externí entity.</span><span class="sxs-lookup"><span data-stu-id="a625d-133">If the reader used supports entity resolution, external entities are resolved.</span></span> <span data-ttu-id="a625d-134">Pokud čtečka nepodporuje odkazy na entity, pak nejsou přeložit odkazy na entity.</span><span class="sxs-lookup"><span data-stu-id="a625d-134">If the reader passed does not support entity references, then entity references are not resolved.</span></span>|  
  
## <a name="semi-trusted-xmldocument"></a><span data-ttu-id="a625d-135">Částečně důvěryhodné XmlDocument</span><span class="sxs-lookup"><span data-stu-id="a625d-135">Semi-Trusted XmlDocument</span></span>  
 <span data-ttu-id="a625d-136">Následující tabulka ukazuje jak **XmlDocument.XmlResolver** vlastnost funguje, když je objekt částečně důvěryhodné.</span><span class="sxs-lookup"><span data-stu-id="a625d-136">The following table shows how the **XmlDocument.XmlResolver** property works when the object is semi-trusted.</span></span> <span data-ttu-id="a625d-137">Tato tabulka platí pro **XmlDocument.Load** metody, když je vstup pro zatížení **TextReader**, **řetězec**, **Stream**, nebo  **Identifikátor URI**.</span><span class="sxs-lookup"><span data-stu-id="a625d-137">This table applies to the **XmlDocument.Load** methods when the input to the Load is a **TextReader**, **String**, **Stream**, or **URI**.</span></span> <span data-ttu-id="a625d-138">Tato tabulka se nedá použít u **zatížení** metoda Pokud **třídou XMLDocument nastavenou na** je načteno z **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a625d-138">This table does not apply to the **Load** method if the **XmlDocument** is loaded from an **XmlReader**.</span></span>  
  
|<span data-ttu-id="a625d-139">Objekt XmlResolver vlastnost</span><span class="sxs-lookup"><span data-stu-id="a625d-139">XmlResolver property</span></span>|<span data-ttu-id="a625d-140">Funkce</span><span class="sxs-lookup"><span data-stu-id="a625d-140">Function</span></span>|<span data-ttu-id="a625d-141">Poznámky</span><span class="sxs-lookup"><span data-stu-id="a625d-141">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="a625d-142">V částečně důvěryhodné scénáři **objekt XmlResolver** vlastnost nemůže být nastavena na jinou hodnotu než null.</span><span class="sxs-lookup"><span data-stu-id="a625d-142">In the semi-trusted scenario, the **XmlResolver** property cannot be set to anything other than null.</span></span>|<span data-ttu-id="a625d-143">**XmlUrlResolver** s **null** pověření vytvořena instance, který se používá **XmlDocument** při překládání názvů souboru, vyhledávání externí specifikace DTD, entity, a schémata, a **null** přihlašovací údaje se používají při úpravách uzly.</span><span class="sxs-lookup"><span data-stu-id="a625d-143">An **XmlUrlResolver** with **null** credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>|<span data-ttu-id="a625d-144">Toto chování je stejné jako chování při **objekt XmlResolver** vlastnost není nastavena, ale zbývá ve svém výchozím stavu.</span><span class="sxs-lookup"><span data-stu-id="a625d-144">This behavior is identical to the behavior when the **XmlResolver** property is not set, but left in its default state.</span></span><br /><br /> <span data-ttu-id="a625d-145">**XmlDocument** používá anonymní oprávnění pro všechny akce.</span><span class="sxs-lookup"><span data-stu-id="a625d-145">The **XmlDocument** uses anonymous permissions for all actions.</span></span>|  
|<span data-ttu-id="a625d-146">Je nastavena na **null** (**nic** v aplikaci Microsoft Visual Basic .NET).</span><span class="sxs-lookup"><span data-stu-id="a625d-146">The property is set to **null** (**Nothing** in Microsoft Visual Basic .NET).</span></span>|<span data-ttu-id="a625d-147">Žádné funkce, které vyžadují externího prostředku jsou podporovány, jako je například vyhledávání externí schématu nebo souboru DTD.</span><span class="sxs-lookup"><span data-stu-id="a625d-147">No features that require an external resource are supported, such as locating an external schema or DTD.</span></span> <span data-ttu-id="a625d-148">Ani externí entity bude vyřešen a provádí úpravy funkce jako je například vkládání uzlů entity, které vyžadují řešení, nejsou podporovány.</span><span class="sxs-lookup"><span data-stu-id="a625d-148">Nor will external entities be resolved, and performing editing functions such as inserting entity nodes that require resolution, are not supported.</span></span>|<span data-ttu-id="a625d-149">Pokud je vlastnost **null**, chování je stejný bez ohledu na to, když **XmlDocument** plně důvěryhodný nebo je částečně důvěryhodné.</span><span class="sxs-lookup"><span data-stu-id="a625d-149">When the property is **null**, the behavior is the same regardless if the **XmlDocument** is fully trusted or semi-trusted.</span></span>|  
|<span data-ttu-id="a625d-150">Vlastnost není nastavena, ale zbývá ve svém výchozím stavu.</span><span class="sxs-lookup"><span data-stu-id="a625d-150">The property is not set, but left in its default state.</span></span>|<span data-ttu-id="a625d-151">**XmlUrlResolver** s **null** pověření vytvořena instance, který se používá **XmlDocument** při překládání názvů souboru, vyhledávání externí specifikace DTD, entity, a schémata, a **null** přihlašovací údaje se používají při úpravách uzly.</span><span class="sxs-lookup"><span data-stu-id="a625d-151">An **XmlUrlResolver** with **null** credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>|<span data-ttu-id="a625d-152">**XmlDocument** používá anonymní oprávnění pro všechny akce.</span><span class="sxs-lookup"><span data-stu-id="a625d-152">The **XmlDocument** uses anonymous permissions for all actions.</span></span>|  
  
 <span data-ttu-id="a625d-153">Tato tabulka platí pro **XmlDocument.Load** – metoda při vstupu do **zatížení** je **XmlReader**a **třídou XMLDocument nastavenou na** je částečně důvěryhodné.</span><span class="sxs-lookup"><span data-stu-id="a625d-153">This table applies to the **XmlDocument.Load** method when the input to the **Load** is an **XmlReader**, and the **XmlDocument** is semi-trusted.</span></span>  
  
|<span data-ttu-id="a625d-154">Objekt XmlResolver vlastnost</span><span class="sxs-lookup"><span data-stu-id="a625d-154">XmlResolver property</span></span>|<span data-ttu-id="a625d-155">Funkce</span><span class="sxs-lookup"><span data-stu-id="a625d-155">Function</span></span>|<span data-ttu-id="a625d-156">Poznámky</span><span class="sxs-lookup"><span data-stu-id="a625d-156">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="a625d-157">**Objekt XmlResolver** třídy používané **XmlDocument** je stejná jako ta, která je používána ve **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a625d-157">The **XmlResolver** class used by the **XmlDocument** is the same one being used by the **XmlReader**.</span></span>|<span data-ttu-id="a625d-158">**XmlDocument** používá **objekt XmlResolver** , který byl přiřazen **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a625d-158">The **XmlDocument** uses the **XmlResolver** that was assigned to the **XmlReader**.</span></span><br /><br /> <span data-ttu-id="a625d-159">**XmlDocument.Resolver** nemůže být vlastnost nastavena, bez ohledu na to **XmlDocument** úroveň, důvěryhodnosti, protože je stále **objekt XmlResolver** z  **Objekt XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="a625d-159">The **XmlDocument.Resolver** property cannot be set, regardless of the **XmlDocument** trust level, because it is getting an **XmlResolver** from the **XmlReader**.</span></span> <span data-ttu-id="a625d-160">Pokusíte přepsat nastavení **XmlReaders** **objekt XmlResolver** nastavením **objekt XmlResolver** vlastnost **XmlDocument**.</span><span class="sxs-lookup"><span data-stu-id="a625d-160">You cannot attempt to override the settings of the **XmlReaders** **XmlResolver** by setting the **XmlResolver** property of the **XmlDocument**.</span></span>|<span data-ttu-id="a625d-161">**XmlReader** může být **XmlTextReader**, ověřování <xref:System.Xml.XmlReader>, nebo čtečku zapisovat vlastní.</span><span class="sxs-lookup"><span data-stu-id="a625d-161">The **XmlReader** can be the **XmlTextReader**, validating <xref:System.Xml.XmlReader>, or a custom-written reader.</span></span> <span data-ttu-id="a625d-162">Pokud čtení byl použit podporuje entity řešení, jsou vyřešeny externí entity.</span><span class="sxs-lookup"><span data-stu-id="a625d-162">If the reader used supports entity resolution, external entities are resolved.</span></span> <span data-ttu-id="a625d-163">Pokud čtečka předaný nepodporuje odkazy na entity, nejsou přeložit odkazy na entity.</span><span class="sxs-lookup"><span data-stu-id="a625d-163">If the reader passed in does not support entity references, then entity references are not resolved.</span></span>|  
  
 <span data-ttu-id="a625d-164">Nastavení objekt XmlResolver tak, aby obsahovala správné přihlašovací údaje umožní přístup k externím prostředkům.</span><span class="sxs-lookup"><span data-stu-id="a625d-164">Setting the XmlResolver to contain the correct credentials allows access to external resources.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="a625d-165">Neexistuje žádný způsob, jak načíst **objekt XmlResolver** vlastnost.</span><span class="sxs-lookup"><span data-stu-id="a625d-165">There is no way to retrieve the **XmlResolver** property.</span></span> <span data-ttu-id="a625d-166">To pomáhá zabránit uživateli v opakovaném použití **objekt XmlResolver** na jaké přihlašovací údaje jsou nastavené.</span><span class="sxs-lookup"><span data-stu-id="a625d-166">This helps to prevent a user from reusing an **XmlResolver** on which credentials have been set.</span></span> <span data-ttu-id="a625d-167">Také pokud **XmlTextReader** nebo ověřování <xref:System.Xml.XmlReader> slouží k načtení **XmlDocument** a **třídou XMLDocument nastavenou na** má překladače byla nastavena, překladače z neukládá do mezipaměti tyto čtenáři **XmlDocument** po **zatížení** fáze, protože to také představuje bezpečnostní riziko.</span><span class="sxs-lookup"><span data-stu-id="a625d-167">Also, if an **XmlTextReader** or validating <xref:System.Xml.XmlReader> is used to load the **XmlDocument** and the **XmlDocument** has a resolver that has been set, the resolvers from these readers are not cached by the **XmlDocument** after the **Load** phase, since this also presents a security risk.</span></span>  
  
 <span data-ttu-id="a625d-168">Další informace najdete v části poznámky <xref:System.Xml.XmlResolver> referenční stránce.</span><span class="sxs-lookup"><span data-stu-id="a625d-168">For more information, see the Remarks section of the <xref:System.Xml.XmlResolver> reference page.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="a625d-169">Viz také:</span><span class="sxs-lookup"><span data-stu-id="a625d-169">See also</span></span>

- [<span data-ttu-id="a625d-170">Model DOM (Document Object Model) dokumentu XML</span><span class="sxs-lookup"><span data-stu-id="a625d-170">XML Document Object Model (DOM)</span></span>](../../../../docs/standard/data/xml/xml-document-object-model-dom.md)
