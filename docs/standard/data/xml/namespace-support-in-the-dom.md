---
title: Podpora oboru názvů v modelu DOM
ms.date: 03/30/2017
ms.technology: dotnet-standard
ms.assetid: f0548ead-0fed-41ee-b33e-117ba900d3bc
ms.openlocfilehash: 6fefce961c2ff91530a9110f5563fd921a7838a3
ms.sourcegitcommit: 33deec3e814238fb18a49b2a7e89278e27888291
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/02/2020
ms.locfileid: "84288794"
---
# <a name="namespace-support-in-the-dom"></a><span data-ttu-id="14247-102">Podpora oboru názvů v modelu DOM</span><span class="sxs-lookup"><span data-stu-id="14247-102">Namespace Support in the DOM</span></span>
<span data-ttu-id="14247-103">XML model DOM (Document Object Model) (DOM) je plně podporující obor názvů.</span><span class="sxs-lookup"><span data-stu-id="14247-103">The XML Document Object Model (DOM) is completely namespace-aware.</span></span> <span data-ttu-id="14247-104">Jsou podporovány pouze dokumenty XML zohledňující obor názvů.</span><span class="sxs-lookup"><span data-stu-id="14247-104">Only namespace-aware XML documents are supported.</span></span> <span data-ttu-id="14247-105">Konsorcium World Wide Web (W3C) určuje, že aplikace modelu DOM, které implementují úroveň 1, můžou být nezohledňované a funkce DOM úrovně 2 se s oborem názvů nepodporují.</span><span class="sxs-lookup"><span data-stu-id="14247-105">The World Wide Web Consortium (W3C) specifies that DOM applications that implement Level 1 can be non-namespace-aware, and DOM Level 2 features are namespace-aware.</span></span> <span data-ttu-id="14247-106">Všechny funkce v modelu XML DOM však podporují obor názvů bez ohledu na to, zda je metoda z doporučení úrovně 1 nebo 2 DOM.</span><span class="sxs-lookup"><span data-stu-id="14247-106">However, all features in the XML DOM are namespace-aware, regardless if the method is from the Level 1 or Level 2 DOM Recommendation.</span></span>  
  
 <span data-ttu-id="14247-107">Například v případě nastavení, které nepoužívá obor názvů, volání `setAttribute("A:b", "123")` , jak je uvedeno v doporučení DOM úrovně 1, nemá za následek atribut s předponou `A` a místním názvem `b` .</span><span class="sxs-lookup"><span data-stu-id="14247-107">For example, in a non-namespace-aware setting, calling `setAttribute("A:b", "123")`, as specified in the DOM Level 1 Recommendation, does not result in an attribute with a prefix of `A` and a local name of `b`.</span></span> <span data-ttu-id="14247-108">Výsledkem by byl atribut s hodnotou `A:b` .</span><span class="sxs-lookup"><span data-stu-id="14247-108">It would result in an attribute with the value `A:b`.</span></span>  
  
 <span data-ttu-id="14247-109">V prostředí, které podporuje obor názvů, volání metody DOM úrovně 2 má `setAttribute("A:b", "123")` za následek atribut s předponou `A` a místním názvem `b` .</span><span class="sxs-lookup"><span data-stu-id="14247-109">In a namespace-aware environment, the call to the DOM Level 2 `setAttribute("A:b", "123")` results in an attribute with a prefix of `A` and a local name of `b`.</span></span> <span data-ttu-id="14247-110">Tímto způsobem funguje Microsoft .NET Framework DOM.</span><span class="sxs-lookup"><span data-stu-id="14247-110">This is how the Microsoft .NET Framework DOM works.</span></span>  
  
 <span data-ttu-id="14247-111">Proto pro všechny metody, které přijímají parametr Name, tyto metody také převezmou předponu, která bude kvalifikována název.</span><span class="sxs-lookup"><span data-stu-id="14247-111">Therefore, for all methods that take a name parameter, these methods also take a prefix to qualify the name.</span></span> <span data-ttu-id="14247-112">Parametr Name, jako je například `A:b` v metodě **SetAttributes** DOM úrovně 1, je analyzován následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="14247-112">The name parameter, such as the `A:b` in the **setAttribute** DOM Level 1 method, is parsed as follows:</span></span>  
  
- <span data-ttu-id="14247-113">Pokud neexistují žádné dvojtečky (:) znaky, místní název je nastaven na `name` parametr a předpona a NamespaceURI jsou prázdné řetězce.</span><span class="sxs-lookup"><span data-stu-id="14247-113">If there are no colon (:) characters, then the local name is set to the `name` parameter, and the prefix and NamespaceURI are empty strings.</span></span>  
  
- <span data-ttu-id="14247-114">Je-li nalezen dvojtečka, název je rozdělen do dvou částí na základě pozice prvního znaku dvojtečky.</span><span class="sxs-lookup"><span data-stu-id="14247-114">If a colon is found, then the name is split into two parts based on the position of the first colon character.</span></span> <span data-ttu-id="14247-115">Předpona je nastavena na řetězec, který byl nalezen před dvojtečkou a místní název je nastaven na řetězec, který byl nalezen za dvojtečkou.</span><span class="sxs-lookup"><span data-stu-id="14247-115">The prefix is set to the string found before the colon, and local name is set to the string found after the colon.</span></span> <span data-ttu-id="14247-116">Pro metody, které nevezmou hodnotu NamespaceURI, NamespaceURI není vyřešen a zůstane nastaven na prázdný řetězec.</span><span class="sxs-lookup"><span data-stu-id="14247-116">For methods that do not take a NamespaceURI value, the NamespaceURI is not resolved and remains set to empty string.</span></span> <span data-ttu-id="14247-117">V opačném případě je NamespaceURI nastaven na řetězec předaný metodě.</span><span class="sxs-lookup"><span data-stu-id="14247-117">Otherwise, the NamespaceURI is set to the string passed to the method.</span></span> <span data-ttu-id="14247-118">Pokud není předpona definována, pak vlastnosti **Save** a **InnerXml** a **OuterXml** selžou.</span><span class="sxs-lookup"><span data-stu-id="14247-118">If the prefix is undefined, then the **Save** method and **InnerXml** and **OuterXml** properties fail.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="14247-119">Viz také</span><span class="sxs-lookup"><span data-stu-id="14247-119">See also</span></span>

- [<span data-ttu-id="14247-120">model DOM (Document Object Model) dokumentu XML</span><span class="sxs-lookup"><span data-stu-id="14247-120">XML Document Object Model (DOM)</span></span>](xml-document-object-model-dom.md)
