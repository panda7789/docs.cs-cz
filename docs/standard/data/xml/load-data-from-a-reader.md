---
title: Načtení dat z čtečky
ms.date: 03/30/2017
ms.technology: dotnet-standard
ms.assetid: 7e74918c-bc72-4977-a49b-e1520a6d8f60
author: mairaw
ms.author: mairaw
ms.openlocfilehash: 55756092f086de47c4b2acb8f147ca3ab231abe1
ms.sourcegitcommit: c7f3e2e9d6ead6cc3acd0d66b10a251d0c66e59d
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/08/2018
ms.locfileid: "44207223"
---
# <a name="load-data-from-a-reader"></a><span data-ttu-id="d48f9-102">Načtení dat z čtečky</span><span class="sxs-lookup"><span data-stu-id="d48f9-102">Load Data from a Reader</span></span>
<span data-ttu-id="d48f9-103">Pokud dokument XML je načtené pomocí možnosti <xref:System.Xml.XmlDocument.Load%2A> metoda a parametr <xref:System.Xml.XmlReader>, existují rozdíly v chování, která nastane v porovnání s chování načítání dat z jiných formátů.</span><span class="sxs-lookup"><span data-stu-id="d48f9-103">If an XML document is loaded using the <xref:System.Xml.XmlDocument.Load%2A> method and a parameter of an <xref:System.Xml.XmlReader>, there are differences in the behavior that occurs when compared to the behavior of loading data from the other formats.</span></span> <span data-ttu-id="d48f9-104">Pokud čtečka nachází ve stavu počáteční <xref:System.Xml.XmlDocument.Load%2A> využívá celý obsah ze čtečky a sestavení XML Document Object Model (DOM) z všechna data ve čtečce.</span><span class="sxs-lookup"><span data-stu-id="d48f9-104">If the reader is in its initial state, <xref:System.Xml.XmlDocument.Load%2A> consumes the entire contents from the reader and builds the XML Document Object Model (DOM) from all the data in the reader.</span></span>  
  
 <span data-ttu-id="d48f9-105">Pokud ukazatel čtení je již umístěný na uzlu někde v dokumentu a čtečky je pak předán <xref:System.Xml.XmlDocument.Load%2A> metody <xref:System.Xml.XmlDocument.Load%2A> se pokusí přečíst aktuální uzel a všechny na stejné úrovni, až koncovou značku, která ukončí aktuální hloubka do paměti.</span><span class="sxs-lookup"><span data-stu-id="d48f9-105">If the reader is already positioned on a node somewhere in the document, and the reader is then passed to the <xref:System.Xml.XmlDocument.Load%2A> method, <xref:System.Xml.XmlDocument.Load%2A> attempts to read the current node and all of its siblings, up to the end tag that closes the current depth into memory.</span></span> <span data-ttu-id="d48f9-106">Úspěch pokusu o <xref:System.Xml.XmlDocument.Load%2A> závisí na uzlu, který je čtečka na při pokusu o zatížení, jako <xref:System.Xml.XmlDocument.Load%2A> ověří, jestli je ve správném formátu XML ze čtečky.</span><span class="sxs-lookup"><span data-stu-id="d48f9-106">The success of the attempted <xref:System.Xml.XmlDocument.Load%2A> depends on the node that the reader is on when the load is attempted, as <xref:System.Xml.XmlDocument.Load%2A> verifies that the XML from the reader is well-formed.</span></span> <span data-ttu-id="d48f9-107">Pokud kód XML není ve správném formátu, <xref:System.Xml.XmlDocument.Load%2A> vyvolá výjimku.</span><span class="sxs-lookup"><span data-stu-id="d48f9-107">If the XML is not well-formed, the <xref:System.Xml.XmlDocument.Load%2A> throws an exception.</span></span> <span data-ttu-id="d48f9-108">Například následující sadu uzlů obsahovat dva prvky na kořenové úrovni serveru, kód XML není ve správném formátu, a <xref:System.Xml.XmlDocument.Load%2A> vyvolá výjimku.</span><span class="sxs-lookup"><span data-stu-id="d48f9-108">For example, the following set of nodes contain two root-level elements, the XML is not well-formed, and <xref:System.Xml.XmlDocument.Load%2A> throws an exception.</span></span>  
  
-   <span data-ttu-id="d48f9-109">Uzel komentáře, za nímž následuje uzlu elementu, za nímž následuje uzlu elementu, za nímž následuje uzlu EndElement.</span><span class="sxs-lookup"><span data-stu-id="d48f9-109">Comment node, followed by an Element node, followed by an Element node, followed by an EndElement node.</span></span>  
  
 <span data-ttu-id="d48f9-110">Následující sada uzlů vytvoří neúplné modelu DOM, protože neexistuje žádný element na kořenové úrovni.</span><span class="sxs-lookup"><span data-stu-id="d48f9-110">The following set of nodes creates an incomplete DOM, because there is no root-level element.</span></span>  
  
-   <span data-ttu-id="d48f9-111">Uzel komentáře, za nímž následuje za nímž následuje uzel komentáře, za nímž následuje uzlu EndElement ProcessingInstruction uzlu.</span><span class="sxs-lookup"><span data-stu-id="d48f9-111">Comment node followed by a ProcessingInstruction node followed by a Comment node followed by an EndElement node.</span></span>  
  
 <span data-ttu-id="d48f9-112">Nevyvolá výjimku a načíst data.</span><span class="sxs-lookup"><span data-stu-id="d48f9-112">This does not throw an exception, and the data is loaded.</span></span> <span data-ttu-id="d48f9-113">Můžete přidat kořenový element k hornímu okraji tyto uzly a vytvořit ve správném formátu XML, který můžete uložit bez chyb.</span><span class="sxs-lookup"><span data-stu-id="d48f9-113">You can add a root element to the top of these nodes and create well-formed XML that can be saved without error.</span></span>  
  
 <span data-ttu-id="d48f9-114">Pokud ukazatel čtení je umístěný na uzel typu list, který není platný pro kořenové úrovni dokumentu (například bílého místa nebo atribut uzlu), čtečky pokračovat ve čtení, dokud je umístěn na uzlu, který lze použít pro kořen.</span><span class="sxs-lookup"><span data-stu-id="d48f9-114">If the reader is positioned on a leaf node that is invalid for the root level of a document (for example, a white space or attribute node), the reader continues to read until it is positioned on a node that can be used for the root.</span></span> <span data-ttu-id="d48f9-115">Dokument začíná načítání v tomto okamžiku.</span><span class="sxs-lookup"><span data-stu-id="d48f9-115">The document begins loading at this point.</span></span>  
  
 <span data-ttu-id="d48f9-116">Ve výchozím nastavení <xref:System.Xml.XmlDocument.Load%2A> neověřuje, zda je platný, pomocí definice typu dokumentu (DTD) nebo ověřování schématu XML.</span><span class="sxs-lookup"><span data-stu-id="d48f9-116">By default, <xref:System.Xml.XmlDocument.Load%2A> does not verify whether the XML is valid using document type definition (DTD) or schema validation.</span></span> <span data-ttu-id="d48f9-117">Pouze ověří, zda je soubor XML ve správném formátu.</span><span class="sxs-lookup"><span data-stu-id="d48f9-117">It only verifies whether the XML is well-formed.</span></span> <span data-ttu-id="d48f9-118">Aby dojde k ověření, je potřeba vytvořit <xref:System.Xml.XmlReader> pomocí <xref:System.Xml.XmlReaderSettings> třídy.</span><span class="sxs-lookup"><span data-stu-id="d48f9-118">In order for validation to occur, you need to create an <xref:System.Xml.XmlReader> using the <xref:System.Xml.XmlReaderSettings> class.</span></span> <span data-ttu-id="d48f9-119"><xref:System.Xml.XmlReader> Třída může vynutit ověřování pomocí DTD nebo schématu definice jazyk (XSD) schématu.</span><span class="sxs-lookup"><span data-stu-id="d48f9-119">The <xref:System.Xml.XmlReader> class can enforce validation using a DTD or Schema definition language (XSD) schema.</span></span> <span data-ttu-id="d48f9-120"><xref:System.Xml.ValidationType> Vlastnost na <xref:System.Xml.XmlReaderSettings> třída určuje, zda <xref:System.Xml.XmlReader> instance vynucuje ověřování.</span><span class="sxs-lookup"><span data-stu-id="d48f9-120">The <xref:System.Xml.ValidationType> property on the <xref:System.Xml.XmlReaderSettings> class determines whether the <xref:System.Xml.XmlReader> instance enforces validation.</span></span> <span data-ttu-id="d48f9-121">Další informace o ověřování XML dat, najdete v části poznámky <xref:System.Xml.XmlReader> referenční stránce.</span><span class="sxs-lookup"><span data-stu-id="d48f9-121">For more information about validating XML data, see the Remarks section of the <xref:System.Xml.XmlReader> reference page.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="d48f9-122">Viz také:</span><span class="sxs-lookup"><span data-stu-id="d48f9-122">See also</span></span>

- [<span data-ttu-id="d48f9-123">Model DOM (Document Object Model) dokumentu XML</span><span class="sxs-lookup"><span data-stu-id="d48f9-123">XML Document Object Model (DOM)</span></span>](../../../../docs/standard/data/xml/xml-document-object-model-dom.md)
