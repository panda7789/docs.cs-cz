---
title: Ověřování dokumentu XML v modelu DOM
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
- cpp
ms.assetid: 2c61c920-d0f8-4c72-bfcc-6524570f3060
ms.openlocfilehash: 93686ddf1afff76926e77acdbf5aa58e93d6cb77
ms.sourcegitcommit: 5f236cd78cf09593c8945a7d753e0850e96a0b80
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 01/07/2020
ms.locfileid: "75710034"
---
# <a name="validating-an-xml-document-in-the-dom"></a><span data-ttu-id="46ee1-102">Ověřování dokumentu XML v modelu DOM</span><span class="sxs-lookup"><span data-stu-id="46ee1-102">Validating an XML Document in the DOM</span></span>

<span data-ttu-id="46ee1-103">Třída<xref:System.Xml.XmlDocument> neověřuje XML v model DOM (Document Object Model) (DOM) proti schématu XSD (XML Schema Definition Language) nebo definici typu dokumentu (DTD) ve výchozím nastavení. XML je ověřeno pouze jako správně vytvořený.</span><span class="sxs-lookup"><span data-stu-id="46ee1-103">The<xref:System.Xml.XmlDocument> class does not validate the XML in the Document Object Model (DOM) against an XML Schema definition language (XSD) schema or document type definition (DTD) by default; the XML is only verified to be well-formed.</span></span>

<span data-ttu-id="46ee1-104">Chcete-li ověřit XML v modelu DOM, můžete ověřit XML, jak je načteno do modelu DOM předáním schématu <xref:System.Xml.XmlReader> ověřováním do metody <xref:System.Xml.XmlDocument.Load%2A> třídy <xref:System.Xml.XmlDocument>, nebo ověřením dříve neověřeného dokumentu XML v modelu DOM pomocí metody <xref:System.Xml.XmlDocument.Validate%2A> třídy <xref:System.Xml.XmlDocument>.</span><span class="sxs-lookup"><span data-stu-id="46ee1-104">To validate the XML in the DOM, you can validate the XML as it is loaded into the DOM by passing a schema-validating <xref:System.Xml.XmlReader> to the <xref:System.Xml.XmlDocument.Load%2A> method of the <xref:System.Xml.XmlDocument> class, or validate a previously unvalidated XML document in the DOM using the <xref:System.Xml.XmlDocument.Validate%2A> method of the <xref:System.Xml.XmlDocument> class.</span></span>

## <a name="validating-an-xml-document-as-it-is-loaded-into-the-dom"></a><span data-ttu-id="46ee1-105">Ověření dokumentu XML, jak je načteno do modelu DOM</span><span class="sxs-lookup"><span data-stu-id="46ee1-105">Validating an XML Document As It Is Loaded into the DOM</span></span>

<span data-ttu-id="46ee1-106">Třída <xref:System.Xml.XmlDocument> ověřuje data XML, když jsou načtena do modelu DOM, když je ověřování <xref:System.Xml.XmlReader> předáno metodě <xref:System.Xml.XmlDocument.Load%2A> třídy <xref:System.Xml.XmlDocument>.</span><span class="sxs-lookup"><span data-stu-id="46ee1-106">The <xref:System.Xml.XmlDocument> class validates the XML data as it is loaded into the DOM when a validating <xref:System.Xml.XmlReader> is passed to the <xref:System.Xml.XmlDocument.Load%2A> method of the <xref:System.Xml.XmlDocument> class.</span></span>

<span data-ttu-id="46ee1-107">Po úspěšném ověření se použije výchozí nastavení schématu, textové hodnoty se v případě potřeby převedou na atomické hodnoty a informace o typu se přidruží k položkám ověřovaných informací.</span><span class="sxs-lookup"><span data-stu-id="46ee1-107">After successful validation, schema defaults are applied, text values are converted to atomic values as necessary, and type information is associated with validated information items.</span></span> <span data-ttu-id="46ee1-108">V důsledku toho typované XML data nahradí dříve netypové XML data.</span><span class="sxs-lookup"><span data-stu-id="46ee1-108">As a result, typed XML data replaces previously untyped XML data.</span></span>

### <a name="creating-an-xml-schema-validating-xmlreader"></a><span data-ttu-id="46ee1-109">Vytvoření schématu XML – ověření objektu XmlReader</span><span class="sxs-lookup"><span data-stu-id="46ee1-109">Creating an XML Schema-Validating XmlReader</span></span>

<span data-ttu-id="46ee1-110">Chcete-li vytvořit schéma XML – ověřování <xref:System.Xml.XmlReader>, postupujte podle těchto kroků.</span><span class="sxs-lookup"><span data-stu-id="46ee1-110">To create an XML schema-validating <xref:System.Xml.XmlReader>, follow these steps.</span></span>

1. <span data-ttu-id="46ee1-111">Vytvořte novou instanci <xref:System.Xml.XmlReaderSettings>.</span><span class="sxs-lookup"><span data-stu-id="46ee1-111">Construct a new <xref:System.Xml.XmlReaderSettings> instance.</span></span>

2. <span data-ttu-id="46ee1-112">Přidejte schéma XML do vlastnosti <xref:System.Xml.XmlReaderSettings.Schemas%2A> instance <xref:System.Xml.XmlReaderSettings>.</span><span class="sxs-lookup"><span data-stu-id="46ee1-112">Add an XML schema to the <xref:System.Xml.XmlReaderSettings.Schemas%2A> property of the <xref:System.Xml.XmlReaderSettings> instance.</span></span>

3. <span data-ttu-id="46ee1-113">Jako <xref:System.Xml.XmlReaderSettings.ValidationType%2A>zadejte `Schema`.</span><span class="sxs-lookup"><span data-stu-id="46ee1-113">Specify `Schema` as the <xref:System.Xml.XmlReaderSettings.ValidationType%2A>.</span></span>

4. <span data-ttu-id="46ee1-114">Volitelně můžete zadat <xref:System.Xml.XmlReaderSettings.ValidationFlags%2A> a <xref:System.Xml.XmlReaderSettings.ValidationEventHandler> pro zpracování chyb ověřování schématu a upozornění zjištěná během ověřování.</span><span class="sxs-lookup"><span data-stu-id="46ee1-114">Optionally specify <xref:System.Xml.XmlReaderSettings.ValidationFlags%2A> and a <xref:System.Xml.XmlReaderSettings.ValidationEventHandler> to handle schema validation errors and warnings encountered during validation.</span></span>

5. <span data-ttu-id="46ee1-115">Nakonec předejte objekt <xref:System.Xml.XmlReaderSettings> do metody <xref:System.Xml.XmlReader.Create%2A> třídy <xref:System.Xml.XmlReader> spolu s dokumentem XML a vytvořte <xref:System.Xml.XmlReader>pro ověřování schématu.</span><span class="sxs-lookup"><span data-stu-id="46ee1-115">Finally, pass the <xref:System.Xml.XmlReaderSettings> object to the <xref:System.Xml.XmlReader.Create%2A> method of the <xref:System.Xml.XmlReader> class along with the XML document, creating a schema-validating <xref:System.Xml.XmlReader>.</span></span>

### <a name="example"></a><span data-ttu-id="46ee1-116">Příklad</span><span class="sxs-lookup"><span data-stu-id="46ee1-116">Example</span></span>

<span data-ttu-id="46ee1-117">V následujícím příkladu kódu je ověřování schématu <xref:System.Xml.XmlReader> ověřuje XML data načtená do modelu DOM.</span><span class="sxs-lookup"><span data-stu-id="46ee1-117">In the code example that follows, a schema-validating <xref:System.Xml.XmlReader> validates the XML data loaded into the DOM.</span></span> <span data-ttu-id="46ee1-118">V dokumentu XML jsou provedeny neplatné změny a potom se dokument znovu ověří a způsobuje chyby při ověřování schématu.</span><span class="sxs-lookup"><span data-stu-id="46ee1-118">Invalid modifications are made to the XML document and the document is then revalidated, causing schema validation errors.</span></span> <span data-ttu-id="46ee1-119">Nakonec se jedna z chyb opravuje a pak je částečně ověřená část dokumentu XML.</span><span class="sxs-lookup"><span data-stu-id="46ee1-119">Finally, one of the errors is corrected, and then part of the XML document is partially validated.</span></span>

[!code-cpp[XmlDocumentValidation.Load#1](../../../../samples/snippets/cpp/VS_Snippets_Data/XmlDocumentValidation.Load/CPP/XmlDocumentValidationExample.cpp#1)]
[!code-csharp[XmlDocumentValidation.Load#1](../../../../samples/snippets/csharp/VS_Snippets_Data/XmlDocumentValidation.Load/CS/XmlDocumentValidationExample.cs#1)]
[!code-vb[XmlDocumentValidation.Load#1](../../../../samples/snippets/visualbasic/VS_Snippets_Data/XmlDocumentValidation.Load/VB/XmlDocumentValidationExample.vb#1)]

<span data-ttu-id="46ee1-120">V příkladu se jako vstup používá soubor `contosoBooks.xml`.</span><span class="sxs-lookup"><span data-stu-id="46ee1-120">The example takes the `contosoBooks.xml` file as input.</span></span>

[!code-xml[XPathXMLExamples#2](../../../../samples/snippets/xml/VS_Snippets_Data/XPathXMLExamples/XML/contosoBooks.xml#2)]

<span data-ttu-id="46ee1-121">Příklad také přebírá soubor `contosoBooks.xsd` jako vstup.</span><span class="sxs-lookup"><span data-stu-id="46ee1-121">The example also takes the `contosoBooks.xsd` file as input.</span></span>

[!code-xml[XPathXMLExamples#3](../../../../samples/snippets/xml/VS_Snippets_Data/XPathXMLExamples/XML/contosoBooks.xsd#3)]

<span data-ttu-id="46ee1-122">Při ověřování dat XML při jejich načítání do modelu DOM Vezměte v úvahu následující skutečnosti.</span><span class="sxs-lookup"><span data-stu-id="46ee1-122">Consider the following when validating XML data as it is loaded into the DOM.</span></span>

- <span data-ttu-id="46ee1-123">V předchozím příkladu je <xref:System.Xml.XmlReaderSettings.ValidationEventHandler> volána při každém zjištění neplatného typu.</span><span class="sxs-lookup"><span data-stu-id="46ee1-123">In the above example, the <xref:System.Xml.XmlReaderSettings.ValidationEventHandler> is called whenever an invalid type is encountered.</span></span> <span data-ttu-id="46ee1-124">Pokud <xref:System.Xml.XmlReaderSettings.ValidationEventHandler> není nastavena při ověřování <xref:System.Xml.XmlReader>, je vyvolána <xref:System.Xml.Schema.XmlSchemaValidationException> při volání <xref:System.Xml.XmlDocument.Load%2A>, pokud jakýkoli typ atributu nebo elementu neodpovídá odpovídajícímu typu zadanému v ověřovacím schématu.</span><span class="sxs-lookup"><span data-stu-id="46ee1-124">If a <xref:System.Xml.XmlReaderSettings.ValidationEventHandler> is not set on the validating <xref:System.Xml.XmlReader>,an <xref:System.Xml.Schema.XmlSchemaValidationException> is thrown when <xref:System.Xml.XmlDocument.Load%2A> is called if any attribute or element type does not match the corresponding type specified in the validating schema.</span></span>

- <span data-ttu-id="46ee1-125">Při načtení dokumentu XML do objektu <xref:System.Xml.XmlDocument> s přidruženým schématem, které definuje výchozí hodnoty, <xref:System.Xml.XmlDocument> zachází s těmito výchozími hodnotami, jako by se zobrazily v dokumentu XML.</span><span class="sxs-lookup"><span data-stu-id="46ee1-125">When an XML document is loaded into an <xref:System.Xml.XmlDocument> object with an associated schema that defines default values, the <xref:System.Xml.XmlDocument> treats these defaults as if they appeared in the XML document.</span></span> <span data-ttu-id="46ee1-126">To znamená, že vlastnost <xref:System.Xml.XmlReader.IsEmptyElement%2A> vždy vrátí `false` pro prvek, který byl nastaven jako výchozí ze schématu.</span><span class="sxs-lookup"><span data-stu-id="46ee1-126">This means that the <xref:System.Xml.XmlReader.IsEmptyElement%2A> property always returns `false` for an element that was defaulted from the schema.</span></span> <span data-ttu-id="46ee1-127">I když v dokumentu XML, byl napsán jako prázdný prvek.</span><span class="sxs-lookup"><span data-stu-id="46ee1-127">Even if in the XML document, it was written as an empty element.</span></span>

## <a name="validating-an-xml-document-in-the-dom"></a><span data-ttu-id="46ee1-128">Ověřování dokumentu XML v modelu DOM</span><span class="sxs-lookup"><span data-stu-id="46ee1-128">Validating an XML Document in the DOM</span></span>

<span data-ttu-id="46ee1-129">Metoda <xref:System.Xml.XmlDocument.Validate%2A> třídy <xref:System.Xml.XmlDocument> ověřuje data XML načtená v modelu DOM proti schématům ve vlastnosti <xref:System.Xml.XmlDocument.Schemas%2A> objektu <xref:System.Xml.XmlDocument>.</span><span class="sxs-lookup"><span data-stu-id="46ee1-129">The <xref:System.Xml.XmlDocument.Validate%2A> method of the <xref:System.Xml.XmlDocument> class validates the XML data loaded in the DOM against the schemas in the <xref:System.Xml.XmlDocument> object's <xref:System.Xml.XmlDocument.Schemas%2A> property.</span></span> <span data-ttu-id="46ee1-130">Po úspěšném ověření se použije výchozí nastavení schématu, textové hodnoty se v případě potřeby převedou na atomické hodnoty a informace o typu se přidruží k položkám ověřovaných informací.</span><span class="sxs-lookup"><span data-stu-id="46ee1-130">After successful validation, schema defaults are applied, text values are converted to atomic values as necessary, and type information is associated with validated information items.</span></span> <span data-ttu-id="46ee1-131">V důsledku toho typované XML data nahradí dříve netypové XML data.</span><span class="sxs-lookup"><span data-stu-id="46ee1-131">As a result, typed XML data replaces previously untyped XML data.</span></span>

<span data-ttu-id="46ee1-132">Následující příklad je podobný příkladu v tématu "ověřování dokumentu XML při jeho načtení do modelu DOM" výše.</span><span class="sxs-lookup"><span data-stu-id="46ee1-132">The example below is similar to the example in "Validating an XML Document As It Is Loaded into the DOM" above.</span></span> <span data-ttu-id="46ee1-133">V tomto příkladu není dokument XML ověřen, protože je načten do modelu DOM, ale je ověřen poté, co byl načten do modelu DOM pomocí metody <xref:System.Xml.XmlDocument.Validate%2A> třídy <xref:System.Xml.XmlDocument>.</span><span class="sxs-lookup"><span data-stu-id="46ee1-133">In this example, the XML document is not validated as it is loaded into the DOM, but rather is validated after it has been loaded into the DOM using the <xref:System.Xml.XmlDocument.Validate%2A> method of the <xref:System.Xml.XmlDocument> class.</span></span> <span data-ttu-id="46ee1-134">Metoda <xref:System.Xml.XmlDocument.Validate%2A> ověří dokument XML proti schématům XML obsaženým ve vlastnosti <xref:System.Xml.XmlDocument.Schemas%2A> <xref:System.Xml.XmlDocument>.</span><span class="sxs-lookup"><span data-stu-id="46ee1-134">The <xref:System.Xml.XmlDocument.Validate%2A> method validates the XML document against the XML schemas contained in the <xref:System.Xml.XmlDocument.Schemas%2A> property of the <xref:System.Xml.XmlDocument>.</span></span> <span data-ttu-id="46ee1-135">V dokumentu XML jsou následně provedeny neplatné změny a pak se dokument znovu ověří a způsobuje chyby při ověřování schématu.</span><span class="sxs-lookup"><span data-stu-id="46ee1-135">Invalid modifications are then made to the XML document, and the document is then revalidated, causing schema validation errors.</span></span> <span data-ttu-id="46ee1-136">Nakonec se jedna z chyb opravuje a pak je částečně ověřená část dokumentu XML.</span><span class="sxs-lookup"><span data-stu-id="46ee1-136">Finally, one of the errors is corrected, and then part of the XML document is partially validated.</span></span>

[!code-csharp[XmlDocumentValidation.Validate#1](../../../../samples/snippets/csharp/VS_Snippets_Data/XmlDocumentValidation.Validate/CS/XmlDocumentValidationExample.cs#1)]
[!code-vb[XmlDocumentValidation.Validate#1](../../../../samples/snippets/visualbasic/VS_Snippets_Data/XmlDocumentValidation.Validate/VB/XmlDocumentValidationExample.vb#1)]

<span data-ttu-id="46ee1-137">Příklad přijímá jako vstup soubory `contosoBooks.xml` a `contosoBooks.xsd`, na které se říká "ověřování dokumentu XML, jak je načten do modelu DOM" výše.</span><span class="sxs-lookup"><span data-stu-id="46ee1-137">The example takes as input the `contosoBooks.xml` and `contosoBooks.xsd` files referred to in "Validating an XML Document as it is Loaded into the DOM" above.</span></span>

## <a name="handling-validation-errors-and-warnings"></a><span data-ttu-id="46ee1-138">Zpracování chyb ověřování a upozornění</span><span class="sxs-lookup"><span data-stu-id="46ee1-138">Handling Validation Errors and Warnings</span></span>

<span data-ttu-id="46ee1-139">Při ověřování dat XML načtených v modelu DOM jsou hlášeny chyby ověřování schématu XML.</span><span class="sxs-lookup"><span data-stu-id="46ee1-139">XML schema validation errors are reported when validating XML data loaded in the DOM.</span></span> <span data-ttu-id="46ee1-140">Zobrazí se oznámení o všech chybách ověřování schématu při ověřování dat XML při jejich načítání nebo při ověřování dříve neověřeného dokumentu XML.</span><span class="sxs-lookup"><span data-stu-id="46ee1-140">You are notified about all schema validation errors found while validating the XML data as it is being loaded, or when validating a previously unvalidated XML document.</span></span>

<span data-ttu-id="46ee1-141">Chyby ověřování jsou zpracovávány <xref:System.Xml.Schema.ValidationEventHandler>.</span><span class="sxs-lookup"><span data-stu-id="46ee1-141">Validation errors are handled by the <xref:System.Xml.Schema.ValidationEventHandler>.</span></span> <span data-ttu-id="46ee1-142">Pokud byla <xref:System.Xml.Schema.ValidationEventHandler> přiřazena k instanci <xref:System.Xml.XmlReaderSettings> nebo předána metodě <xref:System.Xml.XmlDocument.Validate%2A> třídy <xref:System.Xml.XmlDocument>, <xref:System.Xml.Schema.ValidationEventHandler> bude zpracovávat chyby ověřování schématu; v opačném případě je při zjištění chyby ověřování schématu vyvolána <xref:System.Xml.Schema.XmlSchemaValidationException>.</span><span class="sxs-lookup"><span data-stu-id="46ee1-142">If a <xref:System.Xml.Schema.ValidationEventHandler> was assigned to the <xref:System.Xml.XmlReaderSettings> instance, or passed to the <xref:System.Xml.XmlDocument.Validate%2A> method of the <xref:System.Xml.XmlDocument> class, the <xref:System.Xml.Schema.ValidationEventHandler> will handle schema validation errors; otherwise an <xref:System.Xml.Schema.XmlSchemaValidationException> is raised when a schema validation error is encountered.</span></span>

> [!NOTE]
> <span data-ttu-id="46ee1-143">Data XML jsou načtena do modelu DOM navzdory chybám ověřování schématu, pokud <xref:System.Xml.Schema.ValidationEventHandler> nevyvolává výjimku k zastavení procesu.</span><span class="sxs-lookup"><span data-stu-id="46ee1-143">The XML data is loaded into the DOM despite schema validation errors unless your <xref:System.Xml.Schema.ValidationEventHandler> raises an exception to stop the process.</span></span>
>
> <span data-ttu-id="46ee1-144">Upozornění ověřování schématu nejsou hlášeny, pokud není pro objekt <xref:System.Xml.XmlReaderSettings> zadán příznak <xref:System.Xml.Schema.XmlSchemaValidationFlags.ReportValidationWarnings>.</span><span class="sxs-lookup"><span data-stu-id="46ee1-144">Schema validation warnings are not reported unless the <xref:System.Xml.Schema.XmlSchemaValidationFlags.ReportValidationWarnings> flag is specified to the <xref:System.Xml.XmlReaderSettings> object.</span></span>

 <span data-ttu-id="46ee1-145">Příklady ilustrující <xref:System.Xml.Schema.ValidationEventHandler>naleznete v části "ověřování dokumentu XML, jak je načten do modelu DOM" a "ověřování dokumentu XML v modelu DOM" výše.</span><span class="sxs-lookup"><span data-stu-id="46ee1-145">For examples illustrating the <xref:System.Xml.Schema.ValidationEventHandler>, see "Validating an XML Document As It Is Loaded into the DOM" and "Validating an XML Document in the DOM" above.</span></span>

## <a name="see-also"></a><span data-ttu-id="46ee1-146">Viz také:</span><span class="sxs-lookup"><span data-stu-id="46ee1-146">See also</span></span>

- <xref:System.Xml.XmlDocument>
- <xref:System.Xml.XmlReader>
- <xref:System.Xml.Schema.ValidationEventHandler>
- <xref:System.Xml.XmlReaderSettings>
- [<span data-ttu-id="46ee1-147">Zpracování dat XML pomocí modelu DOM</span><span class="sxs-lookup"><span data-stu-id="46ee1-147">Process XML Data Using the DOM Model</span></span>](../../../../docs/standard/data/xml/process-xml-data-using-the-dom-model.md)
- [<span data-ttu-id="46ee1-148">Práce se schématy XML</span><span class="sxs-lookup"><span data-stu-id="46ee1-148">Working with XML Schemas</span></span>](../../../../docs/standard/data/xml/working-with-xml-schemas.md)
