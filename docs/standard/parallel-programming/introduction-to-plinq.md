---
title: Úvod do PLINQ
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- PLINQ queries, introduction to
ms.assetid: eaa720d8-8999-4eb7-8df5-3c19ca61cad0
ms.openlocfilehash: e50b2bf15d9a627f70ff01616bf2c72c70d7ff33
ms.sourcegitcommit: 33deec3e814238fb18a49b2a7e89278e27888291
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/02/2020
ms.locfileid: "84290678"
---
# <a name="introduction-to-plinq"></a><span data-ttu-id="45b65-102">Úvod do PLINQ</span><span class="sxs-lookup"><span data-stu-id="45b65-102">Introduction to PLINQ</span></span>

<span data-ttu-id="45b65-103">Paralelní LINQ (PLINQ) je paralelní implementace vzoru [LINQ (Language-Integrated Query)](../../csharp/programming-guide/concepts/linq/index.md) .</span><span class="sxs-lookup"><span data-stu-id="45b65-103">Parallel LINQ (PLINQ) is a parallel implementation of the [Language-Integrated Query (LINQ)](../../csharp/programming-guide/concepts/linq/index.md) pattern.</span></span> <span data-ttu-id="45b65-104">PLINQ implementuje úplnou sadu standardních operátorů dotazů LINQ jako metody rozšíření pro <xref:System.Linq> obor názvů a má další operátory pro paralelní operace.</span><span class="sxs-lookup"><span data-stu-id="45b65-104">PLINQ implements the full set of LINQ standard query operators as extension methods for the <xref:System.Linq> namespace and has additional operators for parallel operations.</span></span> <span data-ttu-id="45b65-105">PLINQ kombinuje jednoduchost a čitelnost syntaxe LINQ s výkonem paralelního programování.</span><span class="sxs-lookup"><span data-stu-id="45b65-105">PLINQ combines the simplicity and readability of LINQ syntax with the power of parallel programming.</span></span>

> [!TIP]
> <span data-ttu-id="45b65-106">Pokud nejste obeznámeni s LINQ, nabízí jednotný model pro dotazování jakéhokoli vyčíslitelného zdroje dat bezpečným způsobem.</span><span class="sxs-lookup"><span data-stu-id="45b65-106">If you're not familiar with LINQ, it features a unified model for querying any enumerable data source in a type-safe manner.</span></span> <span data-ttu-id="45b65-107">LINQ to Objects je název pro dotazy LINQ, které jsou spouštěny proti kolekcím v paměti, jako jsou například <xref:System.Collections.Generic.List%601> a pole.</span><span class="sxs-lookup"><span data-stu-id="45b65-107">LINQ to Objects is the name for LINQ queries that are run against in-memory collections such as <xref:System.Collections.Generic.List%601> and arrays.</span></span> <span data-ttu-id="45b65-108">V tomto článku se předpokládá, že máte základní znalosti jazyka LINQ.</span><span class="sxs-lookup"><span data-stu-id="45b65-108">This article assumes that you have a basic understanding of LINQ.</span></span> <span data-ttu-id="45b65-109">Další informace najdete v tématu [LINQ (Language-Integrated Query)](../../csharp/programming-guide/concepts/linq/index.md).</span><span class="sxs-lookup"><span data-stu-id="45b65-109">For more information, see [Language-Integrated Query (LINQ)](../../csharp/programming-guide/concepts/linq/index.md).</span></span>

## <a name="what-is-a-parallel-query"></a><span data-ttu-id="45b65-110">Co je paralelní dotaz?</span><span class="sxs-lookup"><span data-stu-id="45b65-110">What is a Parallel query?</span></span>

<span data-ttu-id="45b65-111">Dotaz PLINQ v mnoha způsobech připomíná neparalelní LINQ to Objects dotaz.</span><span class="sxs-lookup"><span data-stu-id="45b65-111">A PLINQ query in many ways resembles a non-parallel LINQ to Objects query.</span></span> <span data-ttu-id="45b65-112">PLINQ dotazy, stejně jako sekvenční dotazy LINQ, pracují na jakémkoli z paměti <xref:System.Collections.IEnumerable> nebo <xref:System.Collections.Generic.IEnumerable%601> zdroji dat a mají odložené provádění, což znamená, že se nespustí, dokud se dotaz nevytvoří.</span><span class="sxs-lookup"><span data-stu-id="45b65-112">PLINQ queries, just like sequential LINQ queries, operate on any in-memory <xref:System.Collections.IEnumerable> or <xref:System.Collections.Generic.IEnumerable%601> data source, and have deferred execution, which means they do not begin executing until the query is enumerated.</span></span> <span data-ttu-id="45b65-113">Hlavním rozdílem je, že PLINQ se pokouší plně využívat všechny procesory systému.</span><span class="sxs-lookup"><span data-stu-id="45b65-113">The primary difference is that PLINQ attempts to make full use of all the processors on the system.</span></span> <span data-ttu-id="45b65-114">Provádí to rozdělením zdroje dat na segmenty a následným spuštěním dotazu na každém segmentu v samostatných pracovních vláknech paralelně na více procesorech.</span><span class="sxs-lookup"><span data-stu-id="45b65-114">It does this by partitioning the data source into segments, and then executing the query on each segment on separate worker threads in parallel on multiple processors.</span></span> <span data-ttu-id="45b65-115">V mnoha případech paralelní spouštění dotazu funguje výrazně rychleji.</span><span class="sxs-lookup"><span data-stu-id="45b65-115">In many cases, parallel execution means that the query runs significantly faster.</span></span>

<span data-ttu-id="45b65-116">Pomocí paralelního provádění PLINQ může dosáhnout výrazného zlepšení výkonu oproti staršímu kódu pro určité typy dotazů, často pouze přidáním <xref:System.Linq.ParallelEnumerable.AsParallel%2A> operace dotazu do zdroje dat.</span><span class="sxs-lookup"><span data-stu-id="45b65-116">Through parallel execution, PLINQ can achieve significant performance improvements over legacy code for certain kinds of queries, often just by adding the <xref:System.Linq.ParallelEnumerable.AsParallel%2A> query operation to the data source.</span></span> <span data-ttu-id="45b65-117">Paralelismus však může zavádět své vlastní složitosti a ne všechny operace dotazů se v PLINQ rychleji spouštět.</span><span class="sxs-lookup"><span data-stu-id="45b65-117">However, parallelism can introduce its own complexities, and not all query operations run faster in PLINQ.</span></span> <span data-ttu-id="45b65-118">Ve skutečnosti paralelně zpomaluje některé dotazy.</span><span class="sxs-lookup"><span data-stu-id="45b65-118">In fact, parallelization actually slows down certain queries.</span></span> <span data-ttu-id="45b65-119">Proto byste měli pochopit, jak problémy, jako je řazení, ovlivňují paralelní dotazy.</span><span class="sxs-lookup"><span data-stu-id="45b65-119">Therefore, you should understand how issues such as ordering affect parallel queries.</span></span> <span data-ttu-id="45b65-120">Další informace naleznete v tématu [Principy zrychlení v PLINQ](understanding-speedup-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="45b65-120">For more information, see [Understanding Speedup in PLINQ](understanding-speedup-in-plinq.md).</span></span>

> [!NOTE]
> <span data-ttu-id="45b65-121">Tato dokumentace používá lambda výrazy k definování delegátů v PLINQ.</span><span class="sxs-lookup"><span data-stu-id="45b65-121">This documentation uses lambda expressions to define delegates in PLINQ.</span></span> <span data-ttu-id="45b65-122">Pokud nejste obeznámeni s lambda výrazy v jazyce C# nebo Visual Basic, přečtěte si téma [lambda výrazy v PLINQ a TPL](lambda-expressions-in-plinq-and-tpl.md).</span><span class="sxs-lookup"><span data-stu-id="45b65-122">If you are not familiar with lambda expressions in C# or Visual Basic, see [Lambda Expressions in PLINQ and TPL](lambda-expressions-in-plinq-and-tpl.md).</span></span>

<span data-ttu-id="45b65-123">Zbývající část tohoto článku poskytuje přehled hlavních tříd PLINQ a popisuje, jak vytvářet dotazy PLINQ.</span><span class="sxs-lookup"><span data-stu-id="45b65-123">The remainder of this article gives an overview of the main PLINQ classes and discusses how to create PLINQ queries.</span></span> <span data-ttu-id="45b65-124">Každý oddíl obsahuje odkazy na podrobnější informace a příklady kódu.</span><span class="sxs-lookup"><span data-stu-id="45b65-124">Each section contains links to more detailed information and code examples.</span></span>

## <a name="the-parallelenumerable-class"></a><span data-ttu-id="45b65-125">Třída ParallelEnumerable</span><span class="sxs-lookup"><span data-stu-id="45b65-125">The ParallelEnumerable Class</span></span>

<span data-ttu-id="45b65-126"><xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType>Třída zpřístupňuje téměř všechny funkce PLINQ.</span><span class="sxs-lookup"><span data-stu-id="45b65-126">The <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> class exposes almost all of PLINQ's functionality.</span></span> <span data-ttu-id="45b65-127">A zbytek <xref:System.Linq?displayProperty=nameWithType> typů oboru názvů je zkompilován do sestavení System. Core. dll.</span><span class="sxs-lookup"><span data-stu-id="45b65-127">It and the rest of the <xref:System.Linq?displayProperty=nameWithType> namespace types are compiled into the System.Core.dll assembly.</span></span> <span data-ttu-id="45b65-128">Výchozí projekty C# a Visual Basic v aplikaci Visual Studio odkazují na sestavení a importují obor názvů.</span><span class="sxs-lookup"><span data-stu-id="45b65-128">The default C# and Visual Basic projects in Visual Studio both reference the assembly and import the namespace.</span></span>

<span data-ttu-id="45b65-129"><xref:System.Linq.ParallelEnumerable>zahrnuje implementace všech standardních operátorů dotazů, které LINQ to Objects podporuje, i když se o ně nepokusí paralelizovat.</span><span class="sxs-lookup"><span data-stu-id="45b65-129"><xref:System.Linq.ParallelEnumerable> includes implementations of all the standard query operators that LINQ to Objects supports, although it does not attempt to parallelize each one.</span></span> <span data-ttu-id="45b65-130">Pokud nejste obeznámeni s LINQ, přečtěte si téma [Úvod do LINQ (C#)](../../csharp/programming-guide/concepts/linq/index.md) a [Úvod do LINQ (Visual Basic)](../../visual-basic/programming-guide/concepts/linq/introduction-to-linq.md).</span><span class="sxs-lookup"><span data-stu-id="45b65-130">If you are not familiar with LINQ, see [Introduction to LINQ (C#)](../../csharp/programming-guide/concepts/linq/index.md) and [Introduction to LINQ (Visual Basic)](../../visual-basic/programming-guide/concepts/linq/introduction-to-linq.md).</span></span>

<span data-ttu-id="45b65-131">Kromě standardních operátorů dotazu <xref:System.Linq.ParallelEnumerable> Třída obsahuje sadu metod, které umožňují chování specifické pro paralelní spuštění.</span><span class="sxs-lookup"><span data-stu-id="45b65-131">In addition to the standard query operators, the <xref:System.Linq.ParallelEnumerable> class contains a set of methods that enable behaviors specific to parallel execution.</span></span> <span data-ttu-id="45b65-132">Tyto metody specifické pro PLINQ jsou uvedeny v následující tabulce.</span><span class="sxs-lookup"><span data-stu-id="45b65-132">These PLINQ-specific methods are listed in the following table.</span></span>

|<span data-ttu-id="45b65-133">ParallelEnumerable – operátor</span><span class="sxs-lookup"><span data-stu-id="45b65-133">ParallelEnumerable Operator</span></span>|<span data-ttu-id="45b65-134">Popis</span><span class="sxs-lookup"><span data-stu-id="45b65-134">Description</span></span>|
|---------------------------------|-----------------|
|<xref:System.Linq.ParallelEnumerable.AsParallel%2A>|<span data-ttu-id="45b65-135">Vstupní bod pro PLINQ.</span><span class="sxs-lookup"><span data-stu-id="45b65-135">The entry point for PLINQ.</span></span> <span data-ttu-id="45b65-136">Určuje, že zbytek dotazu by měl být paralelní, pokud je to možné.</span><span class="sxs-lookup"><span data-stu-id="45b65-136">Specifies that the rest of the query should be parallelized, if it is possible.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsSequential%2A>|<span data-ttu-id="45b65-137">Určuje, že zbytek dotazu by měl být spuštěn sekvenčně, jako neparalelní dotaz LINQ.</span><span class="sxs-lookup"><span data-stu-id="45b65-137">Specifies that the rest of the query should be run sequentially, as a non-parallel LINQ query.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsOrdered%2A>|<span data-ttu-id="45b65-138">Určuje, že PLINQ by měl zachovat pořadí zdrojové sekvence pro zbytek dotazu nebo dokud se nezmění řazení, například použitím klauzule orderby (ORDER by in Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="45b65-138">Specifies that PLINQ should preserve the ordering of the source sequence for the rest of the query, or until the ordering is changed, for example by the use of an orderby (Order By in Visual Basic) clause.</span></span>|
|<xref:System.Linq.ParallelEnumerable.AsUnordered%2A>|<span data-ttu-id="45b65-139">Určuje, že PLINQ pro zbytek dotazu není vyžadován k zachování pořadí zdrojové sekvence.</span><span class="sxs-lookup"><span data-stu-id="45b65-139">Specifies that PLINQ for the rest of the query is not required to preserve the ordering of the source sequence.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithCancellation%2A>|<span data-ttu-id="45b65-140">Určuje, že PLINQ by měl pravidelně monitorovat stav poskytnutého tokenu zrušení a zrušit provádění, pokud je požadován.</span><span class="sxs-lookup"><span data-stu-id="45b65-140">Specifies that PLINQ should periodically monitor the state of the provided cancellation token and cancel execution if it is requested.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A>|<span data-ttu-id="45b65-141">Určuje maximální počet procesorů, které by měl PLINQ použít k paralelizovat dotazu.</span><span class="sxs-lookup"><span data-stu-id="45b65-141">Specifies the maximum number of processors that PLINQ should use to parallelize the query.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A>|<span data-ttu-id="45b65-142">Poskytuje nápovědu ke způsobu, jakým by měl PLINQ, pokud je to možné, sloučit paralelní výsledky zpět do jediné sekvence v náročném vlákně.</span><span class="sxs-lookup"><span data-stu-id="45b65-142">Provides a hint about how PLINQ should, if it is possible, merge parallel results back into just one sequence on the consuming thread.</span></span>|
|<xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A>|<span data-ttu-id="45b65-143">Určuje, zda by měl PLINQ paralelizovat dotaz i v případě, že výchozí chování by bylo spuštěno sekvenčně.</span><span class="sxs-lookup"><span data-stu-id="45b65-143">Specifies whether PLINQ should parallelize the query even when the default behavior would be to run it sequentially.</span></span>|
|<xref:System.Linq.ParallelEnumerable.ForAll%2A>|<span data-ttu-id="45b65-144">Vícevláknová metoda výčtu, která na rozdíl od iterace na základě výsledků dotazu umožňuje souběžné zpracování výsledků bez předchozího sloučení zpět do vlákna příjemce.</span><span class="sxs-lookup"><span data-stu-id="45b65-144">A multithreaded enumeration method that, unlike iterating over the results of the query, enables results to be processed in parallel without first merging back to the consumer thread.</span></span>|
|<span data-ttu-id="45b65-145"><xref:System.Linq.ParallelEnumerable.Aggregate%2A>metody</span><span class="sxs-lookup"><span data-stu-id="45b65-145"><xref:System.Linq.ParallelEnumerable.Aggregate%2A> overload</span></span>|<span data-ttu-id="45b65-146">Přetížení, které je jedinečné pro PLINQ a umožňuje mezilehlou agregaci přes místní segmenty vlákna a také konečnou agregační funkci pro kombinování výsledků všech oddílů.</span><span class="sxs-lookup"><span data-stu-id="45b65-146">An overload that is unique to PLINQ and enables intermediate aggregation over thread-local partitions, plus a final aggregation function to combine the results of all partitions.</span></span>|

## <a name="the-opt-in-model"></a><span data-ttu-id="45b65-147">Model výslovných přihlášení</span><span class="sxs-lookup"><span data-stu-id="45b65-147">The Opt-in Model</span></span>

<span data-ttu-id="45b65-148">Při psaní dotazu Přihlaste se k PLINQ vyvoláním <xref:System.Linq.ParallelEnumerable.AsParallel%2A?displayProperty=nameWithType> metody rozšíření ve zdroji dat, jak je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="45b65-148">When you write a query, opt in to PLINQ by invoking the <xref:System.Linq.ParallelEnumerable.AsParallel%2A?displayProperty=nameWithType> extension method on the data source, as shown in the following example.</span></span>

[!code-csharp[PLINQ#1](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#1)]
[!code-vb[PLINQ#1](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#1)]

<span data-ttu-id="45b65-149"><xref:System.Linq.ParallelEnumerable.AsParallel%2A>Metoda rozšíření váže následující operátory pro dotazování, v tomto případě `where` a `select` , do <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> implementace.</span><span class="sxs-lookup"><span data-stu-id="45b65-149">The <xref:System.Linq.ParallelEnumerable.AsParallel%2A> extension method binds the subsequent query operators, in this case, `where` and `select`, to the <xref:System.Linq.ParallelEnumerable?displayProperty=nameWithType> implementations.</span></span>

## <a name="execution-modes"></a><span data-ttu-id="45b65-150">Režimy spuštění</span><span class="sxs-lookup"><span data-stu-id="45b65-150">Execution Modes</span></span>

<span data-ttu-id="45b65-151">Ve výchozím nastavení je PLINQ konzervativní.</span><span class="sxs-lookup"><span data-stu-id="45b65-151">By default, PLINQ is conservative.</span></span> <span data-ttu-id="45b65-152">V době spuštění PLINQ infrastruktura analyzuje celkovou strukturu dotazu.</span><span class="sxs-lookup"><span data-stu-id="45b65-152">At run time, the PLINQ infrastructure analyzes the overall structure of the query.</span></span> <span data-ttu-id="45b65-153">Pokud je dotaz pravděpodobně výsledkem urychlení paralelismus, PLINQ rozdělí zdrojovou sekvenci na úlohy, které lze spustit souběžně.</span><span class="sxs-lookup"><span data-stu-id="45b65-153">If the query is likely to yield speedups by parallelization, PLINQ partitions the source sequence into tasks that can be run concurrently.</span></span> <span data-ttu-id="45b65-154">Pokud není bezpečné paralelizovat dotaz, PLINQ pouze spustí dotaz sekvenčně.</span><span class="sxs-lookup"><span data-stu-id="45b65-154">If it is not safe to parallelize a query, PLINQ just runs the query sequentially.</span></span> <span data-ttu-id="45b65-155">Pokud má PLINQ možnost volby mezi potenciálně nákladným paralelním algoritmem nebo levným sekvenčním algoritmem, zvolí sekvenční algoritmus ve výchozím nastavení.</span><span class="sxs-lookup"><span data-stu-id="45b65-155">If PLINQ has a choice between a potentially expensive parallel algorithm or an inexpensive sequential algorithm, it chooses the sequential algorithm by default.</span></span> <span data-ttu-id="45b65-156">Můžete použít <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> metodu a <xref:System.Linq.ParallelExecutionMode?displayProperty=nameWithType> výčet k instruování PLINQ k výběru paralelního algoritmu.</span><span class="sxs-lookup"><span data-stu-id="45b65-156">You can use the <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> method and the <xref:System.Linq.ParallelExecutionMode?displayProperty=nameWithType> enumeration to instruct PLINQ to select the parallel algorithm.</span></span> <span data-ttu-id="45b65-157">To je užitečné, když znáte testování a měření, že se konkrétní dotaz spouští paralelně.</span><span class="sxs-lookup"><span data-stu-id="45b65-157">This is useful when you know by testing and measurement that a particular query executes faster in parallel.</span></span> <span data-ttu-id="45b65-158">Další informace naleznete v tématu [How to: určení režimu spouštění v PLINQ](how-to-specify-the-execution-mode-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="45b65-158">For more information, see [How to: Specify the Execution Mode in PLINQ](how-to-specify-the-execution-mode-in-plinq.md).</span></span>

## <a name="degree-of-parallelism"></a><span data-ttu-id="45b65-159">Stupeň paralelismu</span><span class="sxs-lookup"><span data-stu-id="45b65-159">Degree of Parallelism</span></span>

<span data-ttu-id="45b65-160">Ve výchozím nastavení PLINQ používá všechny procesory v hostitelském počítači.</span><span class="sxs-lookup"><span data-stu-id="45b65-160">By default, PLINQ uses all of the processors on the host computer.</span></span> <span data-ttu-id="45b65-161">Můžete určit, aby PLINQ používal maximálně zadaný počet procesorů pomocí <xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="45b65-161">You can instruct PLINQ to use no more than a specified number of processors by using the <xref:System.Linq.ParallelEnumerable.WithDegreeOfParallelism%2A> method.</span></span> <span data-ttu-id="45b65-162">To je užitečné, když chcete zajistit, aby ostatní procesy spuštěné v počítači dostaly určité množství času procesoru.</span><span class="sxs-lookup"><span data-stu-id="45b65-162">This is useful when you want to make sure that other processes running on the computer receive a certain amount of CPU time.</span></span> <span data-ttu-id="45b65-163">Následující fragment kódu omezuje dotaz na využití maximálně dvou procesorů.</span><span class="sxs-lookup"><span data-stu-id="45b65-163">The following snippet limits the query to utilizing a maximum of two processors.</span></span>

[!code-csharp[PLINQ#5](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinqsamples.cs#5)]
[!code-vb[PLINQ#5](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#5)]

<span data-ttu-id="45b65-164">V případech, kdy dotaz provádí značné množství práce vázané na nevýpočetní prostředí, jako je vstupně-výstupní operace se soubory, může být výhodné zadat stupeň paralelismu větší než počet jader v počítači.</span><span class="sxs-lookup"><span data-stu-id="45b65-164">In cases where a query is performing a significant amount of non-compute-bound work such as File I/O, it might be beneficial to specify a degree of parallelism greater than the number of cores on the machine.</span></span>

## <a name="ordered-versus-unordered-parallel-queries"></a><span data-ttu-id="45b65-165">Seřazené versus neuspořádané paralelní dotazy</span><span class="sxs-lookup"><span data-stu-id="45b65-165">Ordered Versus Unordered Parallel Queries</span></span>

<span data-ttu-id="45b65-166">V některých dotazech musí operátor dotazu vytvořit výsledky, které zachovávají pořadí zdrojové sekvence.</span><span class="sxs-lookup"><span data-stu-id="45b65-166">In some queries, a query operator must produce results that preserve the ordering of the source sequence.</span></span> <span data-ttu-id="45b65-167">PLINQ poskytuje <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> operátor pro tento účel.</span><span class="sxs-lookup"><span data-stu-id="45b65-167">PLINQ provides the <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> operator for this purpose.</span></span> <span data-ttu-id="45b65-168"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A>se liší od <xref:System.Linq.ParallelEnumerable.AsSequential%2A> .</span><span class="sxs-lookup"><span data-stu-id="45b65-168"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A> is distinct from <xref:System.Linq.ParallelEnumerable.AsSequential%2A>.</span></span> <span data-ttu-id="45b65-169"><xref:System.Linq.ParallelEnumerable.AsOrdered%2A>Sekvence je stále zpracovávána paralelně, ale její výsledky jsou uloženy do vyrovnávací paměti a seřazeny.</span><span class="sxs-lookup"><span data-stu-id="45b65-169">An <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sequence is still processed in parallel, but its results are buffered and sorted.</span></span> <span data-ttu-id="45b65-170">Vzhledem k tomu, že zachování pořadí obvykle zahrnuje další práci, <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sekvence může být zpracována pomaleji než výchozí <xref:System.Linq.ParallelEnumerable.AsUnordered%2A> sekvence.</span><span class="sxs-lookup"><span data-stu-id="45b65-170">Because order preservation typically involves extra work, an <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> sequence might be processed more slowly than the default <xref:System.Linq.ParallelEnumerable.AsUnordered%2A> sequence.</span></span> <span data-ttu-id="45b65-171">Zda je konkrétní objednaná paralelní operace rychlejší než sekvenční verze operace závisí na mnoha faktorech.</span><span class="sxs-lookup"><span data-stu-id="45b65-171">Whether a particular ordered parallel operation is faster than a sequential version of the operation depends on many factors.</span></span>

<span data-ttu-id="45b65-172">Následující příklad kódu ukazuje, jak se vyjádřit, abyste mohli seřadit zachování.</span><span class="sxs-lookup"><span data-stu-id="45b65-172">The following code example shows how to opt in to order preservation.</span></span>

[!code-csharp[PLINQ#3](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#3)]
[!code-vb[PLINQ#3](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#3)]

<span data-ttu-id="45b65-173">Další informace naleznete v tématu [pořadí zachování v PLINQ](order-preservation-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="45b65-173">For more information, see [Order Preservation in PLINQ](order-preservation-in-plinq.md).</span></span>

## <a name="parallel-vs-sequential-queries"></a><span data-ttu-id="45b65-174">Paralelní vs. sekvenční dotazy</span><span class="sxs-lookup"><span data-stu-id="45b65-174">Parallel vs. Sequential Queries</span></span>

<span data-ttu-id="45b65-175">Některé operace vyžadují, aby se zdrojová data doručila sekvenčním způsobem.</span><span class="sxs-lookup"><span data-stu-id="45b65-175">Some operations require that the source data be delivered in a sequential manner.</span></span> <span data-ttu-id="45b65-176"><xref:System.Linq.ParallelEnumerable>Operátory dotazu se v případě potřeby vrátí do sekvenčního režimu automaticky.</span><span class="sxs-lookup"><span data-stu-id="45b65-176">The <xref:System.Linq.ParallelEnumerable> query operators revert to sequential mode automatically when it is required.</span></span> <span data-ttu-id="45b65-177">Pro uživatelsky definované operátory dotazů a delegáty uživatelů, kteří vyžadují sekvenční provádění, PLINQ poskytuje <xref:System.Linq.ParallelEnumerable.AsSequential%2A> metodu.</span><span class="sxs-lookup"><span data-stu-id="45b65-177">For user-defined query operators and user delegates that require sequential execution, PLINQ provides the <xref:System.Linq.ParallelEnumerable.AsSequential%2A> method.</span></span> <span data-ttu-id="45b65-178">Když použijete <xref:System.Linq.ParallelEnumerable.AsSequential%2A> , všechny následné operátory v dotazu se spustí postupně, dokud <xref:System.Linq.ParallelEnumerable.AsParallel%2A> se znovu nevolá.</span><span class="sxs-lookup"><span data-stu-id="45b65-178">When you use <xref:System.Linq.ParallelEnumerable.AsSequential%2A>, all subsequent operators in the query are executed sequentially until <xref:System.Linq.ParallelEnumerable.AsParallel%2A> is called again.</span></span> <span data-ttu-id="45b65-179">Další informace naleznete v tématu [How to: Kombinování paralelních a sekvenčních dotazů LINQ](how-to-combine-parallel-and-sequential-linq-queries.md).</span><span class="sxs-lookup"><span data-stu-id="45b65-179">For more information, see [How to: Combine Parallel and Sequential LINQ Queries](how-to-combine-parallel-and-sequential-linq-queries.md).</span></span>

## <a name="options-for-merging-query-results"></a><span data-ttu-id="45b65-180">Možnosti pro sloučení výsledků dotazu</span><span class="sxs-lookup"><span data-stu-id="45b65-180">Options for Merging Query Results</span></span>

<span data-ttu-id="45b65-181">Když je PLINQ dotaz spuštěn paralelně, jeho výsledky z každého pracovního vlákna musí být sloučeny zpět do hlavního vlákna pro použití `foreach` smyčkou ( `For Each` v Visual Basic) nebo vložení do seznamu nebo pole.</span><span class="sxs-lookup"><span data-stu-id="45b65-181">When a PLINQ query executes in parallel, its results from each worker thread must be merged back onto the main thread for consumption by a `foreach` loop (`For Each` in Visual Basic), or insertion into a list or array.</span></span> <span data-ttu-id="45b65-182">V některých případech může být výhodné zadat konkrétní druh operace sloučení, například pro rychlejší zahájení výroby výsledků.</span><span class="sxs-lookup"><span data-stu-id="45b65-182">In some cases, it might be beneficial to specify a particular kind of merge operation, for example, to begin producing results more quickly.</span></span> <span data-ttu-id="45b65-183">Pro účely tohoto účelu PLINQ podporuje <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> metodu a <xref:System.Linq.ParallelMergeOptions> výčet.</span><span class="sxs-lookup"><span data-stu-id="45b65-183">For this purpose, PLINQ supports the <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> method, and the <xref:System.Linq.ParallelMergeOptions> enumeration.</span></span> <span data-ttu-id="45b65-184">Další informace naleznete v tématu [možnosti sloučení v PLINQ](merge-options-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="45b65-184">For more information, see [Merge Options in PLINQ](merge-options-in-plinq.md).</span></span>

## <a name="the-forall-operator"></a><span data-ttu-id="45b65-185">Operátor ForAll</span><span class="sxs-lookup"><span data-stu-id="45b65-185">The ForAll Operator</span></span>

<span data-ttu-id="45b65-186">V sekvenčních dotazech LINQ je provádění odloženo, dokud se dotaz nevytvoří ve `foreach` `For Each` smyčce (ve Visual Basic) nebo vyvoláním metody <xref:System.Linq.ParallelEnumerable.ToList%2A> , jako je, <xref:System.Linq.ParallelEnumerable.ToArray%2A> nebo <xref:System.Linq.ParallelEnumerable.ToDictionary%2A> .</span><span class="sxs-lookup"><span data-stu-id="45b65-186">In sequential LINQ queries, execution is deferred until the query is enumerated either in a `foreach` (`For Each` in Visual Basic) loop or by invoking a method such as <xref:System.Linq.ParallelEnumerable.ToList%2A> , <xref:System.Linq.ParallelEnumerable.ToArray%2A> , or <xref:System.Linq.ParallelEnumerable.ToDictionary%2A>.</span></span> <span data-ttu-id="45b65-187">V PLINQ můžete použít také `foreach` k provedení dotazu a iterování výsledků.</span><span class="sxs-lookup"><span data-stu-id="45b65-187">In PLINQ, you can also use `foreach` to execute the query and iterate through the results.</span></span> <span data-ttu-id="45b65-188">Sám o `foreach` sobě ale neběží paralelně a proto vyžaduje, aby se výstup ze všech paralelních úloh sloučil zpátky do vlákna, na kterém je smyčka spuštěná.</span><span class="sxs-lookup"><span data-stu-id="45b65-188">However, `foreach` itself does not run in parallel, and therefore, it requires that the output from all parallel tasks be merged back into the thread on which the loop is running.</span></span> <span data-ttu-id="45b65-189">V PLINQ můžete použít, `foreach` Pokud chcete zachovat konečné řazení výsledků dotazu, a také kdykoli zpracováváte výsledky pomocí sériového způsobu, například při volání `Console.WriteLine` pro každý prvek.</span><span class="sxs-lookup"><span data-stu-id="45b65-189">In PLINQ, you can use `foreach` when you must preserve the final ordering of the query results, and also whenever you are processing the results in a serial manner, for example when you are calling `Console.WriteLine` for each element.</span></span> <span data-ttu-id="45b65-190">Pro rychlejší provádění dotazů, pokud není potřeba pořadí řazení, a když zpracování výsledků může být paralelní, použijte <xref:System.Linq.ParallelEnumerable.ForAll%2A> metodu pro spuštění dotazu PLINQ.</span><span class="sxs-lookup"><span data-stu-id="45b65-190">For faster query execution when order preservation is not required and when the processing of the results can itself be parallelized, use the <xref:System.Linq.ParallelEnumerable.ForAll%2A> method to execute a PLINQ query.</span></span> <span data-ttu-id="45b65-191"><xref:System.Linq.ParallelEnumerable.ForAll%2A>neprovádí tento finální krok sloučení.</span><span class="sxs-lookup"><span data-stu-id="45b65-191"><xref:System.Linq.ParallelEnumerable.ForAll%2A> does not perform this final merge step.</span></span> <span data-ttu-id="45b65-192">Následující příklad kódu ukazuje, jak použít <xref:System.Linq.ParallelEnumerable.ForAll%2A> metodu.</span><span class="sxs-lookup"><span data-stu-id="45b65-192">The following code example shows how to use the <xref:System.Linq.ParallelEnumerable.ForAll%2A> method.</span></span> <span data-ttu-id="45b65-193"><xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType>je zde použita, protože je optimalizována pro více vláken, která se přidávají souběžně bez pokusu o odebrání jakékoli položky.</span><span class="sxs-lookup"><span data-stu-id="45b65-193"><xref:System.Collections.Concurrent.ConcurrentBag%601?displayProperty=nameWithType> is used here because it is optimized for multiple threads adding concurrently without attempting to remove any items.</span></span>

[!code-csharp[PLINQ#4](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#4)]
[!code-vb[PLINQ#4](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#4)]

<span data-ttu-id="45b65-194">Následující ilustrace znázorňuje rozdíl mezi `foreach` a <xref:System.Linq.ParallelEnumerable.ForAll%2A> s ohledem na provádění dotazu.</span><span class="sxs-lookup"><span data-stu-id="45b65-194">The following illustration shows the difference between `foreach` and <xref:System.Linq.ParallelEnumerable.ForAll%2A> with regard to query execution.</span></span>

<span data-ttu-id="45b65-195">![ForAll vs. ForEach](media/vs-isvnt-allvseach.png "VS_ISVNT_ALLvsEACH")</span><span class="sxs-lookup"><span data-stu-id="45b65-195">![ForAll vs. ForEach](media/vs-isvnt-allvseach.png "VS_ISVNT_ALLvsEACH")</span></span>

## <a name="cancellation"></a><span data-ttu-id="45b65-196">Zrušení</span><span class="sxs-lookup"><span data-stu-id="45b65-196">Cancellation</span></span>

<span data-ttu-id="45b65-197">PLINQ je integrován s typy zrušení v .NET Framework 4.</span><span class="sxs-lookup"><span data-stu-id="45b65-197">PLINQ is integrated with the cancellation types in .NET Framework 4.</span></span> <span data-ttu-id="45b65-198">(Další informace naleznete v tématu [zrušení ve spravovaných vláknech](../threading/cancellation-in-managed-threads.md).) Proto lze na rozdíl od sekvenčních dotazů LINQ to Objects zrušit dotazy PLINQ.</span><span class="sxs-lookup"><span data-stu-id="45b65-198">(For more information, see [Cancellation in Managed Threads](../threading/cancellation-in-managed-threads.md).) Therefore, unlike sequential LINQ to Objects queries, PLINQ queries can be canceled.</span></span> <span data-ttu-id="45b65-199">Chcete-li vytvořit PLINQ dotaz, který lze zrušit, použijte <xref:System.Linq.ParallelEnumerable.WithCancellation%2A> operátor na dotaz a zadejte <xref:System.Threading.CancellationToken> instanci jako argument.</span><span class="sxs-lookup"><span data-stu-id="45b65-199">To create a cancelable PLINQ query, use the <xref:System.Linq.ParallelEnumerable.WithCancellation%2A> operator on the query and provide a <xref:System.Threading.CancellationToken> instance as the argument.</span></span> <span data-ttu-id="45b65-200">Je-li <xref:System.Threading.CancellationToken.IsCancellationRequested%2A> vlastnost na tokenu nastavena na hodnotu true, PLINQ si ho upozorní, zastaví zpracování na všech vláknech a vyvolá <xref:System.OperationCanceledException> .</span><span class="sxs-lookup"><span data-stu-id="45b65-200">When the <xref:System.Threading.CancellationToken.IsCancellationRequested%2A> property on the token is set to true, PLINQ will notice it, stop processing on all threads, and throw an <xref:System.OperationCanceledException>.</span></span>

<span data-ttu-id="45b65-201">Je možné, že PLINQ dotaz může pokračovat ve zpracování některých prvků po nastavení tokenu zrušení.</span><span class="sxs-lookup"><span data-stu-id="45b65-201">It is possible that a PLINQ query might continue to process some elements after the cancellation token is set.</span></span>

<span data-ttu-id="45b65-202">Pro větší rychlost odezvy můžete také reagovat na žádosti o zrušení v dlouhotrvajících uživatelských delegátech.</span><span class="sxs-lookup"><span data-stu-id="45b65-202">For greater responsiveness, you can also respond to cancellation requests in long-running user delegates.</span></span> <span data-ttu-id="45b65-203">Další informace naleznete v tématu [How to: Cancel a PLINQ Query](how-to-cancel-a-plinq-query.md).</span><span class="sxs-lookup"><span data-stu-id="45b65-203">For more information, see [How to: Cancel a PLINQ Query](how-to-cancel-a-plinq-query.md).</span></span>

## <a name="exceptions"></a><span data-ttu-id="45b65-204">Výjimky</span><span class="sxs-lookup"><span data-stu-id="45b65-204">Exceptions</span></span>

<span data-ttu-id="45b65-205">Při spuštění dotazu PLINQ může být více výjimek vyvoláno z různých vláken současně.</span><span class="sxs-lookup"><span data-stu-id="45b65-205">When a PLINQ query executes, multiple exceptions might be thrown from different threads simultaneously.</span></span> <span data-ttu-id="45b65-206">Také kód pro zpracování výjimky může být v jiném vlákně než kód, který výjimku vyvolal.</span><span class="sxs-lookup"><span data-stu-id="45b65-206">Also, the code to handle the exception might be on a different thread than the code that threw the exception.</span></span> <span data-ttu-id="45b65-207">PLINQ používá <xref:System.AggregateException> typ k zapouzdření všech výjimek vyvolaných dotazem a zařazování těchto výjimek zpět do volajícího vlákna.</span><span class="sxs-lookup"><span data-stu-id="45b65-207">PLINQ uses the <xref:System.AggregateException> type to encapsulate all the exceptions that were thrown by a query, and marshal those exceptions back to the calling thread.</span></span> <span data-ttu-id="45b65-208">Ve volajícím vlákně je vyžadován pouze jeden blok try-catch.</span><span class="sxs-lookup"><span data-stu-id="45b65-208">On the calling thread, only one try-catch block is required.</span></span> <span data-ttu-id="45b65-209">Můžete však iterovat všemi výjimkami, které jsou zapouzdřeny v <xref:System.AggregateException> a zachytit všechny, ze kterých lze bezpečně obnovit.</span><span class="sxs-lookup"><span data-stu-id="45b65-209">However, you can iterate through all of the exceptions that are encapsulated in the <xref:System.AggregateException> and catch any that you can safely recover from.</span></span> <span data-ttu-id="45b65-210">Ve výjimečných případech mohou být vyvolány některé výjimky, které nejsou zabaleny do <xref:System.AggregateException> , a <xref:System.Threading.ThreadAbortException> s nejsou zabaleny také.</span><span class="sxs-lookup"><span data-stu-id="45b65-210">In rare cases, some exceptions may be thrown that are not wrapped in an <xref:System.AggregateException>, and <xref:System.Threading.ThreadAbortException>s  are also not wrapped.</span></span>

<span data-ttu-id="45b65-211">Pokud jsou výjimky povoleny k bublinám zpět do spojovacího vlákna, pak je možné, že dotaz může pokračovat ve zpracování některých položek poté, co je vyvolána výjimka.</span><span class="sxs-lookup"><span data-stu-id="45b65-211">When exceptions are allowed to bubble up back to the joining thread, then it is possible that a query may continue to process some items after the exception is raised.</span></span>

<span data-ttu-id="45b65-212">Další informace naleznete v tématu [How to: Handle Exceptions in a PLINQ Query](how-to-handle-exceptions-in-a-plinq-query.md).</span><span class="sxs-lookup"><span data-stu-id="45b65-212">For more information, see [How to: Handle Exceptions in a PLINQ Query](how-to-handle-exceptions-in-a-plinq-query.md).</span></span>

## <a name="custom-partitioners"></a><span data-ttu-id="45b65-213">Vlastní dělicí metody</span><span class="sxs-lookup"><span data-stu-id="45b65-213">Custom Partitioners</span></span>

<span data-ttu-id="45b65-214">V některých případech můžete zlepšit výkon dotazů vytvořením vlastního rozdělovače, který využívá některé charakteristické vlastnosti zdrojových dat.</span><span class="sxs-lookup"><span data-stu-id="45b65-214">In some cases, you can improve query performance by writing a custom partitioner that takes advantage of some characteristic of the source data.</span></span> <span data-ttu-id="45b65-215">V dotazu vlastní rozdělovač sám o sobě vyčíslitelnému objektu, na který se dotazuje.</span><span class="sxs-lookup"><span data-stu-id="45b65-215">In the query, the custom partitioner itself is the enumerable object that is queried.</span></span>

[!code-csharp[PLINQ#2](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinq2_cs.cs#2)]
[!code-vb[PLINQ#2](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq3.vb#2)]

<span data-ttu-id="45b65-216">PLINQ podporuje pevný počet oddílů (Přestože data mohou být dynamicky přiřazena k těmto oddílům za běhu pro vyrovnávání zatížení).</span><span class="sxs-lookup"><span data-stu-id="45b65-216">PLINQ supports a fixed number of partitions (although data may be dynamically reassigned to those partitions during run time for load balancing.).</span></span> <span data-ttu-id="45b65-217"><xref:System.Threading.Tasks.Parallel.For%2A>a <xref:System.Threading.Tasks.Parallel.ForEach%2A> podporují pouze dynamické dělení, což znamená, že počet oddílů se v době běhu mění.</span><span class="sxs-lookup"><span data-stu-id="45b65-217"><xref:System.Threading.Tasks.Parallel.For%2A> and <xref:System.Threading.Tasks.Parallel.ForEach%2A> support only dynamic partitioning, which means that the number of partitions changes at run time.</span></span> <span data-ttu-id="45b65-218">Další informace najdete v tématu [vlastní dělicí metody pro PLINQ a TPL](custom-partitioners-for-plinq-and-tpl.md).</span><span class="sxs-lookup"><span data-stu-id="45b65-218">For more information, see [Custom Partitioners for PLINQ and TPL](custom-partitioners-for-plinq-and-tpl.md).</span></span>

## <a name="measuring-plinq-performance"></a><span data-ttu-id="45b65-219">Měření výkonu PLINQ</span><span class="sxs-lookup"><span data-stu-id="45b65-219">Measuring PLINQ Performance</span></span>

<span data-ttu-id="45b65-220">V mnoha případech může být dotaz paralelní, ale režie nastavení paralelního dotazu převažuje nad nabytým výkonem.</span><span class="sxs-lookup"><span data-stu-id="45b65-220">In many cases, a query can be parallelized, but the overhead of setting up the parallel query outweighs the performance benefit gained.</span></span> <span data-ttu-id="45b65-221">Pokud dotaz neprovádí mnoho výpočtů nebo pokud je zdroj dat malý, může PLINQ dotaz být pomalejší než sekvenční LINQ to Objects dotaz.</span><span class="sxs-lookup"><span data-stu-id="45b65-221">If a query does not perform much computation or if the data source is small, a PLINQ query may be slower than a sequential LINQ to Objects query.</span></span> <span data-ttu-id="45b65-222">Můžete použít paralelní Analyzátor výkonu v aplikaci Visual Studio Team Server k porovnání výkonu různých dotazů, k vyhledání problémových míst zpracování a určení, zda je dotaz spuštěn paralelně nebo sekvenčně.</span><span class="sxs-lookup"><span data-stu-id="45b65-222">You can use the Parallel Performance Analyzer in Visual Studio Team Server to compare the performance of various queries, to locate processing bottlenecks, and to determine whether your query is running in parallel or sequentially.</span></span> <span data-ttu-id="45b65-223">Další informace naleznete v tématech [Vizualizér souběžnosti](/visualstudio/profiling/concurrency-visualizer) a [Postupy: měření výkonu dotazu PLINQ](how-to-measure-plinq-query-performance.md).</span><span class="sxs-lookup"><span data-stu-id="45b65-223">For more information, see [Concurrency Visualizer](/visualstudio/profiling/concurrency-visualizer) and [How to: Measure PLINQ Query Performance](how-to-measure-plinq-query-performance.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="45b65-224">Viz také</span><span class="sxs-lookup"><span data-stu-id="45b65-224">See also</span></span>

- [<span data-ttu-id="45b65-225">Paralelní LINQ (PLINQ)</span><span class="sxs-lookup"><span data-stu-id="45b65-225">Parallel LINQ (PLINQ)</span></span>](introduction-to-plinq.md)
- [<span data-ttu-id="45b65-226">Porozumění zrychlení v PLINQ</span><span class="sxs-lookup"><span data-stu-id="45b65-226">Understanding Speedup in PLINQ</span></span>](understanding-speedup-in-plinq.md)
