---
title: TPL a tradiční asynchronní programování v .NET Framework
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- tasks, with other asynchronous models
ms.assetid: e7b31170-a156-433f-9f26-b1fc7cd1776f
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 8024fe6673b39a611c55eb55742bcfd981300e7e
ms.sourcegitcommit: ba5c189bf44d44204a3e8838e59ec378a62d82f3
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/12/2018
ms.locfileid: "44705882"
---
# <a name="tpl-and-traditional-net-framework-asynchronous-programming"></a><span data-ttu-id="218fe-102">TPL a tradiční asynchronní programování v .NET Framework</span><span class="sxs-lookup"><span data-stu-id="218fe-102">TPL and Traditional .NET Framework Asynchronous Programming</span></span>
<span data-ttu-id="218fe-103">Rozhraní .NET Framework poskytuje následující dva standardní vzory pro provádění vstupně-výstupní a výpočetní asynchronních operací:</span><span class="sxs-lookup"><span data-stu-id="218fe-103">The .NET Framework provides the following two standard patterns for performing I/O-bound and compute-bound asynchronous operations:</span></span>  
  
-   <span data-ttu-id="218fe-104">Asynchronní Programovací Model (APM), ve kterém asynchronní operace reprezentovány dvojice metody Begin/End, jako <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> a <xref:System.IO.Stream.EndRead%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="218fe-104">Asynchronous Programming Model (APM), in which asynchronous operations are represented by a pair of Begin/End methods such as <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> and <xref:System.IO.Stream.EndRead%2A?displayProperty=nameWithType>.</span></span>  
  
-   <span data-ttu-id="218fe-105">Událost asynchronní vzor založený (EAP), ve kterém jsou asynchronní operace reprezentovány dvojicí metoda/událost, která se nazývá *OperationName*asynchronní a *OperationName*dokončeno, například <xref:System.Net.WebClient.DownloadStringAsync%2A?displayProperty=nameWithType> a <xref:System.Net.WebClient.DownloadStringCompleted?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="218fe-105">Event-based asynchronous pattern (EAP), in which asynchronous operations are represented by a method/event pair that are named *OperationName*Async and *OperationName*Completed, for example, <xref:System.Net.WebClient.DownloadStringAsync%2A?displayProperty=nameWithType> and <xref:System.Net.WebClient.DownloadStringCompleted?displayProperty=nameWithType>.</span></span> <span data-ttu-id="218fe-106">(EAP byla zavedena v rozhraní .NET Framework verze 2.0).</span><span class="sxs-lookup"><span data-stu-id="218fe-106">(EAP was introduced in the .NET Framework version 2.0.)</span></span>  
  
 <span data-ttu-id="218fe-107">Task Parallel Library (TPL) lze použít různými způsoby ve spojení s jedním z asynchronní zpracování.</span><span class="sxs-lookup"><span data-stu-id="218fe-107">The Task Parallel Library (TPL) can be used in various ways in conjunction with either of the asynchronous patterns.</span></span> <span data-ttu-id="218fe-108">Operace APM a EAP mohou vystavit jako úlohy knihovny spotřebitelům nebo vystavit tyto vzory se dají APM, ale implementace je interně pomocí objekty úloh.</span><span class="sxs-lookup"><span data-stu-id="218fe-108">You can expose both APM and EAP operations as Tasks to library consumers, or you can expose the APM patterns but use Task objects to implement them internally.</span></span> <span data-ttu-id="218fe-109">V obou případech pomocí objektů úloh můžete zjednodušit kód a využijte užitečné následující funkce:</span><span class="sxs-lookup"><span data-stu-id="218fe-109">In both scenarios, by using Task objects, you can simplify the code and take advantage of the following useful functionality:</span></span>  
  
-   <span data-ttu-id="218fe-110">Registrace zpětných volání, ve formuláři pokračování úlohy, kdykoli po spuštění úlohy.</span><span class="sxs-lookup"><span data-stu-id="218fe-110">Register callbacks, in the form of task continuations, at any time after the task has started.</span></span>  
  
-   <span data-ttu-id="218fe-111">Koordinace více operací, které jsou spuštěny v reakci na `Begin_` metodu, pomocí <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAll%2A> a <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAny%2A> metody, nebo <xref:System.Threading.Tasks.Task.WaitAll%2A> – metoda nebo <xref:System.Threading.Tasks.Task.WaitAny%2A> metoda.</span><span class="sxs-lookup"><span data-stu-id="218fe-111">Coordinate multiple operations that execute in response to a `Begin_` method, by using the <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAll%2A> and <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAny%2A> methods, or the <xref:System.Threading.Tasks.Task.WaitAll%2A> method or the <xref:System.Threading.Tasks.Task.WaitAny%2A> method.</span></span>  
  
-   <span data-ttu-id="218fe-112">Zapouzdření asynchronní vstupně-výstupní a výpočetní operace ve stejném objektu úlohy.</span><span class="sxs-lookup"><span data-stu-id="218fe-112">Encapsulate asynchronous I/O-bound and compute-bound operations in the same Task object.</span></span>  
  
-   <span data-ttu-id="218fe-113">Monitorujte stav objektu Task.</span><span class="sxs-lookup"><span data-stu-id="218fe-113">Monitor the status of the Task object.</span></span>  
  
-   <span data-ttu-id="218fe-114">Zařazování stav operace pro objekt úlohy s použitím <xref:System.Threading.Tasks.TaskCompletionSource%601>.</span><span class="sxs-lookup"><span data-stu-id="218fe-114">Marshal the status of an operation to a Task object by using <xref:System.Threading.Tasks.TaskCompletionSource%601>.</span></span>  
  
## <a name="wrapping-apm-operations-in-a-task"></a><span data-ttu-id="218fe-115">Zabalení APM v rámci úlohy</span><span class="sxs-lookup"><span data-stu-id="218fe-115">Wrapping APM Operations in a Task</span></span>  
 <span data-ttu-id="218fe-116">Jak <xref:System.Threading.Tasks.TaskFactory?displayProperty=nameWithType> a <xref:System.Threading.Tasks.TaskFactory%601?displayProperty=nameWithType> třídy poskytují několik přetížení <xref:System.Threading.Tasks.TaskFactory.FromAsync%2A?displayProperty=nameWithType> a <xref:System.Threading.Tasks.TaskFactory%601.FromAsync%2A?displayProperty=nameWithType> metody, které umožňují zapouzdřit páru APM začátek/konec metody v jednom <xref:System.Threading.Tasks.Task> nebo <xref:System.Threading.Tasks.Task%601> instance.</span><span class="sxs-lookup"><span data-stu-id="218fe-116">Both the <xref:System.Threading.Tasks.TaskFactory?displayProperty=nameWithType> and <xref:System.Threading.Tasks.TaskFactory%601?displayProperty=nameWithType> classes provide several overloads of the <xref:System.Threading.Tasks.TaskFactory.FromAsync%2A?displayProperty=nameWithType> and <xref:System.Threading.Tasks.TaskFactory%601.FromAsync%2A?displayProperty=nameWithType> methods that let you encapsulate an APM Begin/End method pair in one <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> instance.</span></span> <span data-ttu-id="218fe-117">Různé přetížení pojmout všechny dvojice metody Begin/End, který jste od nuly do tří vstupní parametry.</span><span class="sxs-lookup"><span data-stu-id="218fe-117">The various overloads accommodate any Begin/End method pair that have from zero to three input parameters.</span></span>  
  
 <span data-ttu-id="218fe-118">Dvojice, které mají pro `End` metody, které vracejí hodnotu (`Function` v jazyce Visual Basic), použijte metody v <xref:System.Threading.Tasks.TaskFactory%601> , vytvořit <xref:System.Threading.Tasks.Task%601>.</span><span class="sxs-lookup"><span data-stu-id="218fe-118">For pairs that have `End` methods that return a value (`Function` in Visual Basic), use the methods in <xref:System.Threading.Tasks.TaskFactory%601> that create a <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="218fe-119">Pro `End` metody, které vracejí void (`Sub` v jazyce Visual Basic), použijte metody v <xref:System.Threading.Tasks.TaskFactory> , vytvořit <xref:System.Threading.Tasks.Task>.</span><span class="sxs-lookup"><span data-stu-id="218fe-119">For `End` methods that return void (`Sub` in Visual Basic), use the methods in <xref:System.Threading.Tasks.TaskFactory> that create a <xref:System.Threading.Tasks.Task>.</span></span>  
  
 <span data-ttu-id="218fe-120">Pro několik případů, ve kterém `Begin` metoda má více než tři parametry, nebo obsahuje `ref` nebo `out` parametry, další `FromAsync` přetížení, které provádí zapouzdření pouze `End` metody jsou k dispozici.</span><span class="sxs-lookup"><span data-stu-id="218fe-120">For those few cases in which the `Begin` method has more than three parameters or contains `ref` or `out` parameters, additional `FromAsync` overloads that encapsulate only the `End` method are provided.</span></span>  
  
 <span data-ttu-id="218fe-121">Následující příklad ukazuje podpis pro `FromAsync` přetížení, které odpovídá <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> a <xref:System.IO.FileStream.EndRead%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="218fe-121">The following example shows the signature for the `FromAsync` overload that matches the <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> and <xref:System.IO.FileStream.EndRead%2A?displayProperty=nameWithType> methods.</span></span> <span data-ttu-id="218fe-122">Toto přetížení má tři vstupní parametry, následujícím způsobem.</span><span class="sxs-lookup"><span data-stu-id="218fe-122">This overload takes three input parameters, as follows.</span></span>  
  
 [!code-csharp[FromAsync#01](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#01)]
 [!code-vb[FromAsync#01](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#01)]  
  
 <span data-ttu-id="218fe-123">První parametr <xref:System.Func%606> delegáta, který se shoduje se signaturou <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> metoda.</span><span class="sxs-lookup"><span data-stu-id="218fe-123">The first parameter is a <xref:System.Func%606> delegate that matches the signature of the <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="218fe-124">Druhý parametr je <xref:System.Func%602> delegáta, který přebírá <xref:System.IAsyncResult> a vrátí `TResult`.</span><span class="sxs-lookup"><span data-stu-id="218fe-124">The second parameter is a <xref:System.Func%602> delegate that takes an <xref:System.IAsyncResult> and returns a `TResult`.</span></span> <span data-ttu-id="218fe-125">Protože <xref:System.IO.FileStream.EndRead%2A> vrátí celé číslo, kompilátor odvodí typ `TResult` jako <xref:System.Int32> a typ úlohy jako <xref:System.Threading.Tasks.Task>.</span><span class="sxs-lookup"><span data-stu-id="218fe-125">Because <xref:System.IO.FileStream.EndRead%2A> returns an integer, the compiler infers the type of `TResult` as <xref:System.Int32> and the type of the task as <xref:System.Threading.Tasks.Task>.</span></span> <span data-ttu-id="218fe-126">Poslední čtyři parametry jsou stejné jako ty v <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> metody:</span><span class="sxs-lookup"><span data-stu-id="218fe-126">The last four parameters are identical to those in the <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> method:</span></span>  
  
-   <span data-ttu-id="218fe-127">Vyrovnávací paměť, ve kterém k ukládání dat souborů.</span><span class="sxs-lookup"><span data-stu-id="218fe-127">The buffer in which to store the file data.</span></span>  
  
-   <span data-ttu-id="218fe-128">Posunutí ve vyrovnávací paměti, ve kterém se má začít zapisovat data.</span><span class="sxs-lookup"><span data-stu-id="218fe-128">The offset in the buffer at which to begin writing data.</span></span>  
  
-   <span data-ttu-id="218fe-129">Maximální množství dat ke čtení ze souboru.</span><span class="sxs-lookup"><span data-stu-id="218fe-129">The maximum amount of data to read from the file.</span></span>  
  
-   <span data-ttu-id="218fe-130">Volitelný objekt, který ukládá data o stavu na definovaný uživatelem k předání do zpětného volání.</span><span class="sxs-lookup"><span data-stu-id="218fe-130">An optional object that stores user-defined state data to pass to the callback.</span></span>  
  
### <a name="using-continuewith-for-the-callback-functionality"></a><span data-ttu-id="218fe-131">Použití ContinueWith pro funkci zpětného volání</span><span class="sxs-lookup"><span data-stu-id="218fe-131">Using ContinueWith for the Callback Functionality</span></span>  
 <span data-ttu-id="218fe-132">Pokud budete potřebovat přístup k datům v souboru, na rozdíl od jenom počet bajtů, <xref:System.Threading.Tasks.TaskFactory.FromAsync%2A> metoda není dostatečná.</span><span class="sxs-lookup"><span data-stu-id="218fe-132">If you require access to the data in the file, as opposed to just the number of bytes, the <xref:System.Threading.Tasks.TaskFactory.FromAsync%2A> method is not sufficient.</span></span> <span data-ttu-id="218fe-133">Místo toho použijte <xref:System.Threading.Tasks.Task>, jehož `Result` vlastnost obsahuje datový soubor.</span><span class="sxs-lookup"><span data-stu-id="218fe-133">Instead, use <xref:System.Threading.Tasks.Task>, whose `Result` property contains the file data.</span></span> <span data-ttu-id="218fe-134">Můžete to provést tak, že přidáte na původním úkolu pokračování.</span><span class="sxs-lookup"><span data-stu-id="218fe-134">You can do this by adding a continuation to the original task.</span></span> <span data-ttu-id="218fe-135">Pokračování práce, které by se obvykle provádí pomocí provádí <xref:System.AsyncCallback> delegovat.</span><span class="sxs-lookup"><span data-stu-id="218fe-135">The continuation performs the work that would typically be performed by the <xref:System.AsyncCallback> delegate.</span></span> <span data-ttu-id="218fe-136">Vyvolá se, když je předchůdce dokončen a vyrovnávací paměť dat bylo vyplněno.</span><span class="sxs-lookup"><span data-stu-id="218fe-136">It is invoked when the antecedent completes, and the data buffer has been filled.</span></span> <span data-ttu-id="218fe-137">( <xref:System.IO.FileStream> Objekt by měl být zavřený před vrácením.)</span><span class="sxs-lookup"><span data-stu-id="218fe-137">(The <xref:System.IO.FileStream> object should be closed before returning.)</span></span>  
  
 <span data-ttu-id="218fe-138">Následující příklad ukazuje, jak vrátit <xref:System.Threading.Tasks.Task> , který zapouzdřuje BeginRead/EndRead dvojici <xref:System.IO.FileStream> třídy.</span><span class="sxs-lookup"><span data-stu-id="218fe-138">The following example shows how to return a <xref:System.Threading.Tasks.Task> that encapsulates the BeginRead/EndRead pair of the <xref:System.IO.FileStream> class.</span></span>  
  
 [!code-csharp[FromAsync#03](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#03)]
 [!code-vb[FromAsync#03](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#03)]  
  
 <span data-ttu-id="218fe-139">Metoda může pak být volána, následujícím způsobem.</span><span class="sxs-lookup"><span data-stu-id="218fe-139">The method can then be called, as follows.</span></span>  
  
 [!code-csharp[FromAsync#04](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#04)]
 [!code-vb[FromAsync#04](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#04)]  
  
### <a name="providing-custom-state-data"></a><span data-ttu-id="218fe-140">Poskytuje Data o stavu vlastního</span><span class="sxs-lookup"><span data-stu-id="218fe-140">Providing Custom State Data</span></span>  
 <span data-ttu-id="218fe-141">V typické <xref:System.IAsyncResult> operace, pokud vaše <xref:System.AsyncCallback> delegáta vyžaduje některé vlastní stavová data, budete muset projít ji v posledním parametrem v `Begin` metody tak, aby data můžou být zabaleny do <xref:System.IAsyncResult> objekt, který se nakonec předaný metodě zpětného volání.</span><span class="sxs-lookup"><span data-stu-id="218fe-141">In typical <xref:System.IAsyncResult> operations, if your <xref:System.AsyncCallback> delegate requires some custom state data, you have to pass it in through the last parameter in the `Begin` method, so that the data can be packaged into the <xref:System.IAsyncResult> object that is eventually passed to the callback method.</span></span> <span data-ttu-id="218fe-142">To není obvykle potřeba při `FromAsync` metody se používají.</span><span class="sxs-lookup"><span data-stu-id="218fe-142">This is typically not required when the `FromAsync` methods are used.</span></span> <span data-ttu-id="218fe-143">Pokud vlastní data se ví, že pokračování, pak lze ji zachytit přímo v delegátovi pokračování.</span><span class="sxs-lookup"><span data-stu-id="218fe-143">If the custom data is known to the continuation, then it can be captured directly in the continuation delegate.</span></span> <span data-ttu-id="218fe-144">Následující příklad je podobný předchozímu příkladu, ale místo zkoumání `Result` vlastnosti předchůdce, pokračování zkontroluje stav vlastní data, která jsou přímo přístupné specialistům uživatelskému delegátovi pokračování.</span><span class="sxs-lookup"><span data-stu-id="218fe-144">The following example resembles the previous example, but instead of examining the `Result` property of the antecedent, the continuation examines the custom state data that is directly accessible to the user delegate of the continuation.</span></span>  
  
 [!code-csharp[FromAsync#05](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#05)]
 [!code-vb[FromAsync#05](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#05)]  
  
### <a name="synchronizing-multiple-fromasync-tasks"></a><span data-ttu-id="218fe-145">Synchronizaci více úkolů FromAsync</span><span class="sxs-lookup"><span data-stu-id="218fe-145">Synchronizing Multiple FromAsync Tasks</span></span>  
 <span data-ttu-id="218fe-146">Statické <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAll%2A> a <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAny%2A> metody poskytují vyšší flexibilitu při použití ve spojení s `FromAsync` metody.</span><span class="sxs-lookup"><span data-stu-id="218fe-146">The static <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAll%2A> and <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAny%2A> methods provide added flexibility when used in conjunction with the `FromAsync` methods.</span></span> <span data-ttu-id="218fe-147">Následující příklad ukazuje, jak spustit několik asynchronních vstupně-výstupních operací a potom počkat na jejich dokončení ještě před spuštěním pokračování.</span><span class="sxs-lookup"><span data-stu-id="218fe-147">The following example shows how to initiate multiple asynchronous I/O operations, and then wait for all of them to complete before you execute the continuation.</span></span>  
  
 [!code-csharp[FromAsync#06](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#06)]
 [!code-vb[FromAsync#06](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#06)]  
  
### <a name="fromasync-tasks-for-only-the-end-method"></a><span data-ttu-id="218fe-148">Úlohy FromAsync pouze v případě metody End</span><span class="sxs-lookup"><span data-stu-id="218fe-148">FromAsync Tasks For Only the End Method</span></span>  
 <span data-ttu-id="218fe-149">Pro několik případů, ve kterém `Begin` metoda vyžaduje víc než tři vstupní parametry nebo má `ref` nebo `out` parametry, můžete použít `FromAsync` přetížení, například <xref:System.Threading.Tasks.TaskFactory%601.FromAsync%28System.IAsyncResult%2CSystem.Func%7BSystem.IAsyncResult%2C%600%7D%29?displayProperty=nameWithType>, které představují pouze `End`metody.</span><span class="sxs-lookup"><span data-stu-id="218fe-149">For those few cases in which the `Begin` method requires more than three input parameters, or has `ref` or `out` parameters, you can use the `FromAsync` overloads, for example, <xref:System.Threading.Tasks.TaskFactory%601.FromAsync%28System.IAsyncResult%2CSystem.Func%7BSystem.IAsyncResult%2C%600%7D%29?displayProperty=nameWithType>, that represent only the `End` method.</span></span> <span data-ttu-id="218fe-150">Tyto metody lze také ve všech scénářích, ve kterém jsou předány <xref:System.IAsyncResult> a chcete zapouzdřit v rámci úlohy.</span><span class="sxs-lookup"><span data-stu-id="218fe-150">These methods can also be used in any scenario in which you are passed an <xref:System.IAsyncResult> and want to encapsulate it in a Task.</span></span>  
  
 [!code-csharp[FromAsync#07](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#07)]
 [!code-vb[FromAsync#07](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#07)]  
  
### <a name="starting-and-canceling-fromasync-tasks"></a><span data-ttu-id="218fe-151">Spuštění a zrušení úloh FromAsync</span><span class="sxs-lookup"><span data-stu-id="218fe-151">Starting and Canceling FromAsync Tasks</span></span>  
 <span data-ttu-id="218fe-152">Úloha vrácená `FromAsync` metoda je ve stavu WaitingForActivation a bude spuštěna v systému v určitém okamžiku po vytvoření úlohy.</span><span class="sxs-lookup"><span data-stu-id="218fe-152">The task returned by a `FromAsync` method has a status of WaitingForActivation and will be started by the system at some point after the task is created.</span></span> <span data-ttu-id="218fe-153">Při pokusu o kontaktování Start na takového úkolu, bude vyvolána výjimka.</span><span class="sxs-lookup"><span data-stu-id="218fe-153">If you attempt to call Start on such a task, an exception will be raised.</span></span>  
  
 <span data-ttu-id="218fe-154">Nelze zrušit `FromAsync` úloh, protože základní rozhraní .NET Framework API aktuálně podporují zrušení v průběhu souboru ani / v sítě.</span><span class="sxs-lookup"><span data-stu-id="218fe-154">You cannot cancel a `FromAsync` task, because the underlying .NET Framework APIs currently do not support in-progress cancellation of file or network I/O.</span></span> <span data-ttu-id="218fe-155">Můžete přidat metody, která zapouzdřuje funkce zrušení `FromAsync` volání, ale můžete reagovat jen na zrušení před `FromAsync` volání a po jeho dokončení (například v úloze pokračování).</span><span class="sxs-lookup"><span data-stu-id="218fe-155">You can add cancellation functionality to a method that encapsulates a `FromAsync` call, but you can only respond to the cancellation before `FromAsync` is called or after it completed (for example, in a continuation task).</span></span>  
  
 <span data-ttu-id="218fe-156">Některé třídy, které podporují protokol EAP, například <xref:System.Net.WebClient>podporují zrušení a, které tuto funkci nativní zrušení můžete integrovat s použitím tokenů zrušení.</span><span class="sxs-lookup"><span data-stu-id="218fe-156">Some classes that support EAP, for example, <xref:System.Net.WebClient>, do support cancellation, and you can integrate that native cancellation functionality by using cancellation tokens.</span></span>  
  
## <a name="exposing-complex-eap-operations-as-tasks"></a><span data-ttu-id="218fe-157">Vystavení komplexní EAP operace jako úlohy</span><span class="sxs-lookup"><span data-stu-id="218fe-157">Exposing Complex EAP Operations As Tasks</span></span>  
 <span data-ttu-id="218fe-158">TPL neposkytuje žádné metody, které jsou vytvořené speciálně pro zapouzdření založený na událostech asynchronní operace ve stejné způsobu, jakým `FromAsync` řady metod kolem <xref:System.IAsyncResult> vzor.</span><span class="sxs-lookup"><span data-stu-id="218fe-158">The TPL does not provide any methods that are specifically designed to encapsulate an event-based asynchronous operation in the same way that the `FromAsync` family of methods wrap the <xref:System.IAsyncResult> pattern.</span></span> <span data-ttu-id="218fe-159">Poskytuje však TPL <xref:System.Threading.Tasks.TaskCompletionSource%601?displayProperty=nameWithType> třídu, která slouží k reprezentaci jakékoli sady operací, jako <xref:System.Threading.Tasks.Task%601>.</span><span class="sxs-lookup"><span data-stu-id="218fe-159">However, the TPL does provide the <xref:System.Threading.Tasks.TaskCompletionSource%601?displayProperty=nameWithType> class, which can be used to represent any arbitrary set of operations as a <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="218fe-160">Operace může být synchronní nebo asynchronní a může být vázaný vstupně-výstupních operací nebo výpočetní.</span><span class="sxs-lookup"><span data-stu-id="218fe-160">The operations may be synchronous or asynchronous, and may be I/O bound or compute-bound, or both.</span></span>  
  
 <span data-ttu-id="218fe-161">Následující příklad ukazuje způsob použití <xref:System.Threading.Tasks.TaskCompletionSource%601> k vystavení sady asynchronním <xref:System.Net.WebClient> operací pro klientský kód jako základní <xref:System.Threading.Tasks.Task%601>.</span><span class="sxs-lookup"><span data-stu-id="218fe-161">The following example shows how to use a <xref:System.Threading.Tasks.TaskCompletionSource%601> to expose a set of asynchronous <xref:System.Net.WebClient> operations to client code as a basic <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="218fe-162">Metoda vám umožňuje zadat pole adresy URL webových a termín nebo název pro hledání a potom vrátí počet pokusů, které se vyvolá hledaný termín v každé lokalitě.</span><span class="sxs-lookup"><span data-stu-id="218fe-162">The method lets you enter an array of Web URLs, and a term or name to search for, and then returns the number of times the search term occurs on each site.</span></span>  
  
 [!code-csharp[FromAsync#10](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/snippet10.cs#10)]
 [!code-vb[FromAsync#10](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/snippet10.vb#10)]  
  
 <span data-ttu-id="218fe-163">Kompletní příklad, který obsahuje další zpracování výjimek a ukazuje, jak volat metodu z klientského kódu, naleznete v tématu [postupy: zalomení vzorů EAP v úloze](../../../docs/standard/parallel-programming/how-to-wrap-eap-patterns-in-a-task.md).</span><span class="sxs-lookup"><span data-stu-id="218fe-163">For a more complete example, which includes additional exception handling and shows how to call the method from client code, see [How to: Wrap EAP Patterns in a Task](../../../docs/standard/parallel-programming/how-to-wrap-eap-patterns-in-a-task.md).</span></span>  
  
 <span data-ttu-id="218fe-164">Mějte na paměti, že každá úloha, která vytvoří <xref:System.Threading.Tasks.TaskCompletionSource%601> TaskCompletionSource se spustí, a proto by neměl volat metodu Start pro tento úkol uživatelského kódu.</span><span class="sxs-lookup"><span data-stu-id="218fe-164">Remember that any task that is created by a <xref:System.Threading.Tasks.TaskCompletionSource%601> will be started by that TaskCompletionSource and, therefore, user code should not call the Start method on that task.</span></span>  
  
## <a name="implementing-the-apm-pattern-by-using-tasks"></a><span data-ttu-id="218fe-165">Implementace vzoru APM pomocí úloh</span><span class="sxs-lookup"><span data-stu-id="218fe-165">Implementing the APM Pattern By Using Tasks</span></span>  
 <span data-ttu-id="218fe-166">V některých případech může být žádoucí zveřejnit přímo <xref:System.IAsyncResult> vzor pomocí dvojice metody Begin/End v rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="218fe-166">In some scenarios, it may be desirable to directly expose the <xref:System.IAsyncResult> pattern by using Begin/End method pairs in an API.</span></span> <span data-ttu-id="218fe-167">Například můžete chtít zachovat konzistenci pomocí stávajících rozhraní API, nebo jste automatizovali nástroje, které vyžadují tento model.</span><span class="sxs-lookup"><span data-stu-id="218fe-167">For example, you may want to maintain consistency with existing APIs, or you may have automated tools that require this pattern.</span></span> <span data-ttu-id="218fe-168">V takovém případě můžete použít úkoly pro zjednodušení, jak je vzorek APM implementována interně.</span><span class="sxs-lookup"><span data-stu-id="218fe-168">In such cases, you can use Tasks to simplify how the APM pattern is implemented internally.</span></span>  
  
 <span data-ttu-id="218fe-169">Následující příklad ukazuje, jak použít úlohy k implementaci páru APM začátek/konec metody pro dlouho běžící výpočetní metodu.</span><span class="sxs-lookup"><span data-stu-id="218fe-169">The following example shows how to use tasks to implement an APM Begin/End method pair for a long-running compute-bound method.</span></span>  
  
 [!code-csharp[FromAsync#09](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#09)]
 [!code-vb[FromAsync#09](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#09)]  
  
## <a name="using-the-streamextensions-sample-code"></a><span data-ttu-id="218fe-170">Pomocí StreamExtensions ukázkového kódu</span><span class="sxs-lookup"><span data-stu-id="218fe-170">Using the StreamExtensions Sample Code</span></span>  
 <span data-ttu-id="218fe-171">Soubor v Streamextensions.cs, [ukázky pro paralelní programování pomocí rozhraní .NET Framework 4](https://code.msdn.microsoft.com/ParExtSamples), obsahuje několik referenční implementace, které používají objekty úloh pro asynchronní soubory a vstupně-výstupní operace sítě.</span><span class="sxs-lookup"><span data-stu-id="218fe-171">The Streamextensions.cs file, in [Samples for Parallel Programming with the .NET Framework 4](https://code.msdn.microsoft.com/ParExtSamples), contains several reference implementations that use Task objects for asynchronous file and network I/O.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="218fe-172">Viz také:</span><span class="sxs-lookup"><span data-stu-id="218fe-172">See also</span></span>

- [<span data-ttu-id="218fe-173">Task Parallel Library (TPL)</span><span class="sxs-lookup"><span data-stu-id="218fe-173">Task Parallel Library (TPL)</span></span>](../../../docs/standard/parallel-programming/task-parallel-library-tpl.md)
