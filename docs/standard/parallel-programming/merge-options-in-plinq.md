---
title: Možnosti sloučení v PLINQ
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- PLINQ queries, merge options
ms.assetid: e8f7be3b-88de-4f33-ab14-dc008e76c1ba
ms.openlocfilehash: a2c238cb66c5018cd1dd4085c6541ef3c9371beb
ms.sourcegitcommit: 33deec3e814238fb18a49b2a7e89278e27888291
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/02/2020
ms.locfileid: "84290639"
---
# <a name="merge-options-in-plinq"></a><span data-ttu-id="801cb-102">Možnosti sloučení v PLINQ</span><span class="sxs-lookup"><span data-stu-id="801cb-102">Merge Options in PLINQ</span></span>
<span data-ttu-id="801cb-103">Když je dotaz spuštěn paralelně, PLINQ rozdělí zdrojové sekvence tak, aby více vláken mohlo pracovat souběžně na různých částech, obvykle v samostatných vláknech.</span><span class="sxs-lookup"><span data-stu-id="801cb-103">When a query is executing as parallel, PLINQ partitions the source sequence so that multiple threads can work on different parts concurrently, typically on separate threads.</span></span> <span data-ttu-id="801cb-104">Pokud mají být výsledky spotřebovány v jednom vlákně, například ve `foreach` `For Each` smyčce (ve Visual Basic), pak se výsledky z každého vlákna musí sloučit zpátky do jedné sekvence.</span><span class="sxs-lookup"><span data-stu-id="801cb-104">If the results are to be consumed on one thread, for example, in a `foreach` (`For Each` in Visual Basic) loop, then the results from every thread must be merged back into one sequence.</span></span> <span data-ttu-id="801cb-105">Typ sloučení, který PLINQ provede, závisí na operátorech, které jsou k dispozici v dotazu.</span><span class="sxs-lookup"><span data-stu-id="801cb-105">The kind of merge that PLINQ performs depends on the operators that are present in the query.</span></span> <span data-ttu-id="801cb-106">Například operátory, které mají novou objednávku pro výsledky, musí ukládat do vyrovnávací paměti všechny prvky ze všech vláken.</span><span class="sxs-lookup"><span data-stu-id="801cb-106">For example, operators that impose a new order on the results must buffer all elements from all threads.</span></span> <span data-ttu-id="801cb-107">Z perspektivy nenáročného vlákna (což je také uživatel aplikace) může být spuštěný dotaz s úplnými vyrovnávacími pamětmi, který je možné určit včas před tím, než vytvoří svůj první výsledek.</span><span class="sxs-lookup"><span data-stu-id="801cb-107">From the perspective of the consuming thread (which is also that of the application user) a fully buffered query might run for a noticeable period of time before it produces its first result.</span></span> <span data-ttu-id="801cb-108">Jiné operátory jsou ve výchozím nastavení částečně uloženy do vyrovnávací paměti. poskytují své výsledky v dávkách.</span><span class="sxs-lookup"><span data-stu-id="801cb-108">Other operators, by default, are partially buffered; they yield their results in batches.</span></span> <span data-ttu-id="801cb-109">Jeden operátor není <xref:System.Linq.ParallelEnumerable.ForAll%2A> ve výchozím nastavení ukládán do vyrovnávací paměti.</span><span class="sxs-lookup"><span data-stu-id="801cb-109">One operator, <xref:System.Linq.ParallelEnumerable.ForAll%2A> is not buffered by default.</span></span> <span data-ttu-id="801cb-110">Poskytuje všechny prvky ze všech vláken okamžitě.</span><span class="sxs-lookup"><span data-stu-id="801cb-110">It yields all elements from all threads immediately.</span></span>  
  
 <span data-ttu-id="801cb-111">Pomocí <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> metody, jak je znázorněno v následujícím příkladu, můžete poskytnout nápovědu pro PLINQ, který označuje, jaký typ sloučení provést.</span><span class="sxs-lookup"><span data-stu-id="801cb-111">By using the <xref:System.Linq.ParallelEnumerable.WithMergeOptions%2A> method, as shown in the following example, you can provide a hint to PLINQ that indicates what kind of merging to perform.</span></span>  
  
 [!code-csharp[PLINQ#26](../../../samples/snippets/csharp/VS_Snippets_Misc/plinq/cs/plinqsamples.cs#26)]
 [!code-vb[PLINQ#26](../../../samples/snippets/visualbasic/VS_Snippets_Misc/plinq/vb/plinq2_vb.vb#26)]  
  
 <span data-ttu-id="801cb-112">Úplný příklad naleznete v tématu [How to: Zadejte možnosti sloučení v PLINQ](how-to-specify-merge-options-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="801cb-112">For the complete example, see [How to: Specify Merge Options in PLINQ](how-to-specify-merge-options-in-plinq.md).</span></span>  
  
 <span data-ttu-id="801cb-113">Pokud konkrétní dotaz nepodporuje požadovanou možnost, bude možnost pouze ignorována.</span><span class="sxs-lookup"><span data-stu-id="801cb-113">If the particular query cannot support the requested option, then the option will just be ignored.</span></span> <span data-ttu-id="801cb-114">Ve většině případů není nutné zadávat možnost sloučení pro dotaz PLINQ.</span><span class="sxs-lookup"><span data-stu-id="801cb-114">In most cases, you do not have to specify a merge option for a PLINQ query.</span></span> <span data-ttu-id="801cb-115">V některých případech však můžete zjistit testováním a měřením, že se dotaz vykoná nejlépe v režimu, který není výchozí.</span><span class="sxs-lookup"><span data-stu-id="801cb-115">However, in some cases you may find by testing and measurement that a query executes best in a non-default mode.</span></span> <span data-ttu-id="801cb-116">Běžnou možností použití této možnosti je vynutit operátor sloučení bloků dat pro streamování výsledků, aby bylo možné zajistit lépe reagující uživatelské rozhraní.</span><span class="sxs-lookup"><span data-stu-id="801cb-116">A common use of this option is to force a chunk-merging operator to stream its results in order to provide a more responsive user interface.</span></span>  
  
## <a name="parallelmergeoptions"></a><span data-ttu-id="801cb-117">ParallelMergeOptions</span><span class="sxs-lookup"><span data-stu-id="801cb-117">ParallelMergeOptions</span></span>  
 <span data-ttu-id="801cb-118"><xref:System.Linq.ParallelMergeOptions>Výčet obsahuje následující možnosti, které určují, jak se mají u podporovaných tvarů dotazů vydávat konečný výstup dotazu, když se výsledky spotřebují v jednom vlákně:</span><span class="sxs-lookup"><span data-stu-id="801cb-118">The <xref:System.Linq.ParallelMergeOptions> enumeration includes the following options that specify, for supported query shapes, how the final output of the query is yielded when the results are consumed on one thread:</span></span>  
  
- `Not Buffered`  
  
     <span data-ttu-id="801cb-119"><xref:System.Linq.ParallelMergeOptions.NotBuffered>Možnost způsobí, že každý zpracovávaný prvek bude vrácen z každého vlákna, jakmile je vytvořen.</span><span class="sxs-lookup"><span data-stu-id="801cb-119">The <xref:System.Linq.ParallelMergeOptions.NotBuffered> option causes each processed element to be returned from each thread as soon as it is produced.</span></span> <span data-ttu-id="801cb-120">Toto chování je podobné jako výstup "streamování".</span><span class="sxs-lookup"><span data-stu-id="801cb-120">This behavior is analogous to "streaming" the output.</span></span> <span data-ttu-id="801cb-121">Pokud se <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> operátor nachází v dotazu, `NotBuffered` zachovává pořadí zdrojových elementů.</span><span class="sxs-lookup"><span data-stu-id="801cb-121">If the <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> operator is present in the query, `NotBuffered` preserves the order of the source elements.</span></span> <span data-ttu-id="801cb-122">I když `NotBuffered` začne vracet výsledky, jakmile jsou k dispozici, celková doba pro vyprodukování všech výsledků může být stále delší než použití jedné z dalších možností sloučení.</span><span class="sxs-lookup"><span data-stu-id="801cb-122">Although `NotBuffered` starts yielding results as soon as they're available, the total time to produce all the results might still be longer than using one of the other merge options.</span></span>  
  
- `Auto Buffered`  
  
     <span data-ttu-id="801cb-123">Tato <xref:System.Linq.ParallelMergeOptions.AutoBuffered> možnost způsobí, že dotaz shromáždí prvky do vyrovnávací paměti a poté pravidelně vyhodnotí obsah vyrovnávací paměti na náročné vlákno.</span><span class="sxs-lookup"><span data-stu-id="801cb-123">The <xref:System.Linq.ParallelMergeOptions.AutoBuffered> option causes the query to collect elements into a buffer and then periodically yield the buffer contents all at once to the consuming thread.</span></span> <span data-ttu-id="801cb-124">Jedná se o podobnou funkci pro zpracování zdrojových dat v blocích "místo použití" streamování " `NotBuffered` .</span><span class="sxs-lookup"><span data-stu-id="801cb-124">This is analogous to yielding the source data in "chunks" instead of using the "streaming" behavior of `NotBuffered`.</span></span> <span data-ttu-id="801cb-125">`AutoBuffered`může trvat déle než, `NotBuffered` aby byl první prvek k dispozici na náročném vlákně.</span><span class="sxs-lookup"><span data-stu-id="801cb-125">`AutoBuffered` may take longer than `NotBuffered` to make the first element available on the consuming thread.</span></span> <span data-ttu-id="801cb-126">Velikost vyrovnávací paměti a přesné chování při vracení není konfigurovatelné a může se lišit v závislosti na různých faktorech, které se vztahují k dotazu.</span><span class="sxs-lookup"><span data-stu-id="801cb-126">The size of the buffer and the exact yielding behavior are not configurable and may vary, depending on various factors that relate to the query.</span></span>  
  
- `FullyBuffered`  
  
     <span data-ttu-id="801cb-127"><xref:System.Linq.ParallelMergeOptions.FullyBuffered>Možnost způsobí, že výstup celého dotazu bude uložen do vyrovnávací paměti před tím, než budou získány jakékoli prvky.</span><span class="sxs-lookup"><span data-stu-id="801cb-127">The <xref:System.Linq.ParallelMergeOptions.FullyBuffered> option causes the output of the whole query to be buffered before any of the elements are yielded.</span></span> <span data-ttu-id="801cb-128">Použijete-li tuto možnost, může trvat déle, než bude první prvek k dispozici ve vybírající vlákně, ale kompletní výsledky mohou být stále vytvořeny rychleji než pomocí dalších možností.</span><span class="sxs-lookup"><span data-stu-id="801cb-128">When you use this option, it can take longer before the first element is available on the consuming thread, but the complete results might still be produced faster than by using the other options.</span></span>  
  
## <a name="query-operators-that-support-merge-options"></a><span data-ttu-id="801cb-129">Operátory dotazů, které podporují možnosti sloučení</span><span class="sxs-lookup"><span data-stu-id="801cb-129">Query Operators that Support Merge Options</span></span>  
 <span data-ttu-id="801cb-130">V následující tabulce jsou uvedeny operátory, které podporují všechny režimy možností sloučení, v souladu se zadanými omezeními.</span><span class="sxs-lookup"><span data-stu-id="801cb-130">The following table lists the operators that support all merge option modes, subject to the specified restrictions.</span></span>  
  
|<span data-ttu-id="801cb-131">Operátor</span><span class="sxs-lookup"><span data-stu-id="801cb-131">Operator</span></span>|<span data-ttu-id="801cb-132">Omezení</span><span class="sxs-lookup"><span data-stu-id="801cb-132">Restrictions</span></span>|  
|--------------|------------------|  
|<xref:System.Linq.ParallelEnumerable.AsEnumerable%2A>|<span data-ttu-id="801cb-133">Žádné</span><span class="sxs-lookup"><span data-stu-id="801cb-133">None</span></span>|  
|<xref:System.Linq.ParallelEnumerable.Cast%2A>|<span data-ttu-id="801cb-134">Žádné</span><span class="sxs-lookup"><span data-stu-id="801cb-134">None</span></span>|  
|<xref:System.Linq.ParallelEnumerable.Concat%2A>|<span data-ttu-id="801cb-135">Neseřazené dotazy, které mají pouze zdroj pole nebo seznamu.</span><span class="sxs-lookup"><span data-stu-id="801cb-135">Non-ordered queries that have an Array or List source only.</span></span>|  
|<xref:System.Linq.ParallelEnumerable.DefaultIfEmpty%2A>|<span data-ttu-id="801cb-136">Žádné</span><span class="sxs-lookup"><span data-stu-id="801cb-136">None</span></span>|  
|<xref:System.Linq.ParallelEnumerable.OfType%2A>|<span data-ttu-id="801cb-137">Žádné</span><span class="sxs-lookup"><span data-stu-id="801cb-137">None</span></span>|  
|<xref:System.Linq.ParallelEnumerable.Reverse%2A>|<span data-ttu-id="801cb-138">Neseřazené dotazy, které mají pouze zdroj pole nebo seznamu.</span><span class="sxs-lookup"><span data-stu-id="801cb-138">Non-ordered queries that have an Array or List source only.</span></span>|  
|<xref:System.Linq.ParallelEnumerable.Select%2A>|<span data-ttu-id="801cb-139">Žádné</span><span class="sxs-lookup"><span data-stu-id="801cb-139">None</span></span>|  
|<xref:System.Linq.ParallelEnumerable.SelectMany%2A>|<span data-ttu-id="801cb-140">Žádné</span><span class="sxs-lookup"><span data-stu-id="801cb-140">None</span></span>|  
|<xref:System.Linq.ParallelEnumerable.Skip%2A>|<span data-ttu-id="801cb-141">Žádné</span><span class="sxs-lookup"><span data-stu-id="801cb-141">None</span></span>|  
|<xref:System.Linq.ParallelEnumerable.Take%2A>|<span data-ttu-id="801cb-142">Žádné</span><span class="sxs-lookup"><span data-stu-id="801cb-142">None</span></span>|  
|<xref:System.Linq.ParallelEnumerable.Where%2A>|<span data-ttu-id="801cb-143">Žádné</span><span class="sxs-lookup"><span data-stu-id="801cb-143">None</span></span>|  
  
 <span data-ttu-id="801cb-144">Všechny ostatní operátory dotazů PLINQ mohou ignorovat možnosti sloučení poskytované uživatelem.</span><span class="sxs-lookup"><span data-stu-id="801cb-144">All other PLINQ query operators might ignore user-provided merge options.</span></span> <span data-ttu-id="801cb-145">Některé operátory dotazů, například <xref:System.Linq.ParallelEnumerable.Reverse%2A> a <xref:System.Linq.ParallelEnumerable.OrderBy%2A> , nemohou vracet žádné prvky, dokud nebudou všechny vyprodukovány a přeobjednány.</span><span class="sxs-lookup"><span data-stu-id="801cb-145">Some query operators, for example, <xref:System.Linq.ParallelEnumerable.Reverse%2A> and <xref:System.Linq.ParallelEnumerable.OrderBy%2A>, cannot yield any elements until all have been produced and reordered.</span></span> <span data-ttu-id="801cb-146">Proto při <xref:System.Linq.ParallelMergeOptions> použití v dotazu, který obsahuje také operátor, jako je <xref:System.Linq.ParallelEnumerable.Reverse%2A> , chování sloučení nebude v dotazu použito, dokud tento operátor nevyrobí své výsledky.</span><span class="sxs-lookup"><span data-stu-id="801cb-146">Therefore, when <xref:System.Linq.ParallelMergeOptions> is used in a query that also contains an operator such as <xref:System.Linq.ParallelEnumerable.Reverse%2A>, the merge behavior will not be applied in the query until after that operator has produced its results.</span></span>  
  
 <span data-ttu-id="801cb-147">Schopnost některých operátorů zpracovat možnosti sloučení závisí na typu zdrojové sekvence a na tom, zda <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> byl operátor použit dříve v dotazu.</span><span class="sxs-lookup"><span data-stu-id="801cb-147">The ability of some operators to handle merge options depends on the type of the source sequence, and whether the <xref:System.Linq.ParallelEnumerable.AsOrdered%2A> operator was used earlier in the query.</span></span> <span data-ttu-id="801cb-148"><xref:System.Linq.ParallelEnumerable.ForAll%2A>je vždy <xref:System.Linq.ParallelMergeOptions.NotBuffered> , vrátí své prvky hned.</span><span class="sxs-lookup"><span data-stu-id="801cb-148"><xref:System.Linq.ParallelEnumerable.ForAll%2A> is always <xref:System.Linq.ParallelMergeOptions.NotBuffered> ; it yields its elements immediately.</span></span> <span data-ttu-id="801cb-149"><xref:System.Linq.ParallelEnumerable.OrderBy%2A>je vždycky <xref:System.Linq.ParallelMergeOptions.FullyBuffered> ; před tím, než to bude mít, musí seřadit celý seznam.</span><span class="sxs-lookup"><span data-stu-id="801cb-149"><xref:System.Linq.ParallelEnumerable.OrderBy%2A> is always <xref:System.Linq.ParallelMergeOptions.FullyBuffered>; it must sort the whole list before it yields.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="801cb-150">Viz také</span><span class="sxs-lookup"><span data-stu-id="801cb-150">See also</span></span>

- [<span data-ttu-id="801cb-151">Paralelní LINQ (PLINQ)</span><span class="sxs-lookup"><span data-stu-id="801cb-151">Parallel LINQ (PLINQ)</span></span>](introduction-to-plinq.md)
- [<span data-ttu-id="801cb-152">Postupy: Určení možností sloučení v PLINQ</span><span class="sxs-lookup"><span data-stu-id="801cb-152">How to: Specify Merge Options in PLINQ</span></span>](how-to-specify-merge-options-in-plinq.md)
