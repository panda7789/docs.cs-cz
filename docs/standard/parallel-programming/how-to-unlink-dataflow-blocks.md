---
title: 'Postupy: Zrušení propojení bloků toku dat'
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- dataflow blocks, unlinking in TPL
- Task Parallel Library, dataflows
- TPL dataflow library, unlinking dataflow blocks
ms.assetid: 40f0208d-4618-47f7-85cf-4913d07d2d7d
ms.openlocfilehash: 8af978ca2ca237988dae8328656d70574dbc1f14
ms.sourcegitcommit: 33deec3e814238fb18a49b2a7e89278e27888291
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/02/2020
ms.locfileid: "84288053"
---
# <a name="how-to-unlink-dataflow-blocks"></a><span data-ttu-id="f55b9-102">Postupy: Zrušení propojení bloků toku dat</span><span class="sxs-lookup"><span data-stu-id="f55b9-102">How to: Unlink Dataflow Blocks</span></span>
<span data-ttu-id="f55b9-103">Tento dokument popisuje, jak odpojit cílový blok toku dat od jeho zdroje.</span><span class="sxs-lookup"><span data-stu-id="f55b9-103">This document describes how to unlink a target dataflow block from its source.</span></span>

[!INCLUDE [tpl-install-instructions](../../../includes/tpl-install-instructions.md)]

## <a name="example"></a><span data-ttu-id="f55b9-104">Příklad</span><span class="sxs-lookup"><span data-stu-id="f55b9-104">Example</span></span>  
 <span data-ttu-id="f55b9-105">Následující příklad vytvoří tři <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> objekty, z nichž každý volá `TrySolution` metodu pro výpočet hodnoty.</span><span class="sxs-lookup"><span data-stu-id="f55b9-105">The following example creates three <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> objects, each of which calls the `TrySolution` method to compute a value.</span></span> <span data-ttu-id="f55b9-106">Tento příklad vyžaduje pouze výsledek od prvního volání do `TrySolution` dokončení.</span><span class="sxs-lookup"><span data-stu-id="f55b9-106">This example requires only the result from the first call to `TrySolution` to finish.</span></span>  
  
 [!code-csharp[TPLDataflow_ReceiveAny#1](../../../samples/snippets/csharp/VS_Snippets_Misc/tpldataflow_receiveany/cs/dataflowreceiveany.cs#1)]
 [!code-vb[TPLDataflow_ReceiveAny#1](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpldataflow_receiveany/vb/dataflowreceiveany.vb#1)]  
  
 <span data-ttu-id="f55b9-107">Chcete-li získat hodnotu z prvního <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> objektu, který je dokončen, tento příklad definuje `ReceiveFromAny(T)` metodu.</span><span class="sxs-lookup"><span data-stu-id="f55b9-107">To receive the value from the first <xref:System.Threading.Tasks.Dataflow.TransformBlock%602> object that finishes, this example defines the `ReceiveFromAny(T)` method.</span></span> <span data-ttu-id="f55b9-108">`ReceiveFromAny(T)`Metoda přijímá pole <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601> objektů a propojí každý z těchto objektů s <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> objektem.</span><span class="sxs-lookup"><span data-stu-id="f55b9-108">The `ReceiveFromAny(T)` method accepts an array of <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601> objects and links each of these objects to a <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object.</span></span> <span data-ttu-id="f55b9-109">Použijete-li <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> metodu k propojení zdrojového bloku toku dat s cílovým blokem, zdroj šíří zprávy do cíle, protože data budou k dispozici.</span><span class="sxs-lookup"><span data-stu-id="f55b9-109">When you use the <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> method to link a source dataflow block to a target block, the source propagates messages to the target as data becomes available.</span></span> <span data-ttu-id="f55b9-110">Vzhledem k tomu, že <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> Třída přijímá pouze první zprávu, která je nabízena, `ReceiveFromAny(T)` Metoda vytvoří svůj výsledek voláním <xref:System.Threading.Tasks.Dataflow.DataflowBlock.Receive%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="f55b9-110">Because the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> class accepts only the first message that it is offered, the `ReceiveFromAny(T)` method produces its result by calling the <xref:System.Threading.Tasks.Dataflow.DataflowBlock.Receive%2A> method.</span></span> <span data-ttu-id="f55b9-111">Tím se vytvoří první zpráva, která je nabídnuta <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> objektu.</span><span class="sxs-lookup"><span data-stu-id="f55b9-111">This produces the first message that is offered to the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object.</span></span> <span data-ttu-id="f55b9-112"><xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A>Metoda má přetíženou verzi, která přebírá <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions> objekt s <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions.MaxMessages> vlastností, která, pokud je nastavena na `1` , instruuje zdrojový blok k odpojení od cíle poté, co cíl přijme jednu zprávu ze zdroje.</span><span class="sxs-lookup"><span data-stu-id="f55b9-112">The <xref:System.Threading.Tasks.Dataflow.ISourceBlock%601.LinkTo%2A> method has an overloaded version that takes an <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions> object with a <xref:System.Threading.Tasks.Dataflow.DataflowLinkOptions.MaxMessages> property that, when it is set to `1`, instructs the source block to unlink from the target after the target receives one message from the source.</span></span> <span data-ttu-id="f55b9-113">Je důležité, <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> aby objekt mohl odkázat z jeho zdrojů, protože vztah mezi polem zdrojů a <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> objektem již není požadován poté, co <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> objekt obdrží zprávu.</span><span class="sxs-lookup"><span data-stu-id="f55b9-113">It is important for the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object to unlink from its sources because the relationship between the array of sources and the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object is no longer required after the <xref:System.Threading.Tasks.Dataflow.WriteOnceBlock%601> object receives a message.</span></span>  
  
 <span data-ttu-id="f55b9-114">Chcete-li povolit zbývající volání na `TrySolution` konec, poté, co jeden z nich vypočítá hodnotu, `TrySolution` Metoda převezme <xref:System.Threading.CancellationToken> objekt, který je zrušen po volání metody `ReceiveFromAny(T)` return.</span><span class="sxs-lookup"><span data-stu-id="f55b9-114">To enable the remaining calls to `TrySolution` to end after one of them computes a value, the `TrySolution` method takes a <xref:System.Threading.CancellationToken> object that is canceled after the call to `ReceiveFromAny(T)` returns.</span></span> <span data-ttu-id="f55b9-115"><xref:System.Threading.SpinWait.SpinUntil%2A>Metoda vrátí, když <xref:System.Threading.CancellationToken> je tento objekt zrušen.</span><span class="sxs-lookup"><span data-stu-id="f55b9-115">The <xref:System.Threading.SpinWait.SpinUntil%2A> method returns when this <xref:System.Threading.CancellationToken> object is canceled.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="f55b9-116">Viz také</span><span class="sxs-lookup"><span data-stu-id="f55b9-116">See also</span></span>

- [<span data-ttu-id="f55b9-117">Tok dat</span><span class="sxs-lookup"><span data-stu-id="f55b9-117">Dataflow</span></span>](dataflow-task-parallel-library.md)
