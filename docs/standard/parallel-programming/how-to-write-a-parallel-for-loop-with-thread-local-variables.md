---
title: 'Postupy: Zápis smyčky Parallel.For pomocí proměnných v místním vláknu'
description: Podívejte se na příklad, jak napsat Parallel. for smyčka v rozhraní .NET, která používá proměnné místního vlákna, které ukládají a načítají stav každého samostatného úkolu ve smyčce.
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- parallel for loops, how to use local state
ms.assetid: 68384064-7ee7-41e2-90e3-71f00bde01bb
ms.openlocfilehash: 9cff507757aab2e5676df2fabb02a237a2172c17
ms.sourcegitcommit: cdb295dd1db589ce5169ac9ff096f01fd0c2da9d
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/09/2020
ms.locfileid: "84599786"
---
# <a name="how-to-write-a-parallelfor-loop-with-thread-local-variables"></a><span data-ttu-id="c1efc-103">Postupy: Zápis smyčky Parallel.For pomocí proměnných v místním vláknu</span><span class="sxs-lookup"><span data-stu-id="c1efc-103">How to: Write a Parallel.For Loop with Thread-Local Variables</span></span>
<span data-ttu-id="c1efc-104">Tento příklad ukazuje, jak použít proměnné místního vlákna k ukládání a načítání stavu v každé samostatné úloze, která je vytvořena <xref:System.Threading.Tasks.Parallel.For%2A> smyčkou.</span><span class="sxs-lookup"><span data-stu-id="c1efc-104">This example shows how to use thread-local variables to store and retrieve state in each separate task that is created by a <xref:System.Threading.Tasks.Parallel.For%2A> loop.</span></span> <span data-ttu-id="c1efc-105">Pomocí místních dat vlákna se můžete vyhnout režie synchronizace velkého počtu přístupů ke sdílenému stavu.</span><span class="sxs-lookup"><span data-stu-id="c1efc-105">By using thread-local data, you can avoid the overhead of synchronizing a large number of accesses to shared state.</span></span> <span data-ttu-id="c1efc-106">Místo psaní do sdíleného prostředku v každé iteraci vypočítáte a uložíte hodnotu, dokud nebudou všechny iterace úlohy dokončeny.</span><span class="sxs-lookup"><span data-stu-id="c1efc-106">Instead of writing to a shared resource on each iteration, you compute and store the value until all iterations for the task are complete.</span></span> <span data-ttu-id="c1efc-107">Nakonec můžete zapsat konečný výsledek do sdíleného prostředku nebo ho předat jiné metodě.</span><span class="sxs-lookup"><span data-stu-id="c1efc-107">You can then write the final result once to the shared resource, or pass it to another method.</span></span>  
  
## <a name="example"></a><span data-ttu-id="c1efc-108">Příklad</span><span class="sxs-lookup"><span data-stu-id="c1efc-108">Example</span></span>  
 <span data-ttu-id="c1efc-109">Následující příklad volá <xref:System.Threading.Tasks.Parallel.For%60%601%28System.Int32%2CSystem.Int32%2CSystem.Func%7B%60%600%7D%2CSystem.Func%7BSystem.Int32%2CSystem.Threading.Tasks.ParallelLoopState%2C%60%600%2C%60%600%7D%2CSystem.Action%7B%60%600%7D%29> metodu pro výpočet součtu hodnot v poli, které obsahuje prvky 1 000 000.</span><span class="sxs-lookup"><span data-stu-id="c1efc-109">The following example calls the <xref:System.Threading.Tasks.Parallel.For%60%601%28System.Int32%2CSystem.Int32%2CSystem.Func%7B%60%600%7D%2CSystem.Func%7BSystem.Int32%2CSystem.Threading.Tasks.ParallelLoopState%2C%60%600%2C%60%600%7D%2CSystem.Action%7B%60%600%7D%29> method to calculate the sum of the values in an array that contains one million elements.</span></span> <span data-ttu-id="c1efc-110">Hodnota každého prvku se rovná jeho indexu.</span><span class="sxs-lookup"><span data-stu-id="c1efc-110">The value of each element is equal to its index.</span></span>  
  
 [!code-csharp[TPL_Parallel#05](../../../samples/snippets/csharp/VS_Snippets_Misc/tpl_parallel/cs/forandforeach_simple.cs#05)]
 [!code-vb[TPL_Parallel#05](../../../samples/snippets/visualbasic/VS_Snippets_Misc/tpl_parallel/vb/forwiththreadlocal.vb#05)]  
  
 <span data-ttu-id="c1efc-111">První dva parametry každé <xref:System.Threading.Tasks.Parallel.For%2A> metody určují počáteční a koncovou hodnotu iterace.</span><span class="sxs-lookup"><span data-stu-id="c1efc-111">The first two parameters of every <xref:System.Threading.Tasks.Parallel.For%2A> method specify the beginning and ending iteration values.</span></span> <span data-ttu-id="c1efc-112">V tomto přetížení metody třetí parametr je místo, kde inicializujete svůj místní stav.</span><span class="sxs-lookup"><span data-stu-id="c1efc-112">In this overload of the method, the third parameter is where you initialize your local state.</span></span> <span data-ttu-id="c1efc-113">V tomto kontextu znamená místní stav proměnnou, jejíž životnost přesahuje těsně před první iterací smyčky v aktuálním vlákně, a to bezprostředně za poslední iterací.</span><span class="sxs-lookup"><span data-stu-id="c1efc-113">In this context, local state means a variable whose lifetime extends from just before the first iteration of the loop on the current thread, to just after the last iteration.</span></span>  
  
 <span data-ttu-id="c1efc-114">Typ třetího parametru je, <xref:System.Func%601> kde `TResult` je typ proměnné, která bude ukládat místní stav vlákna.</span><span class="sxs-lookup"><span data-stu-id="c1efc-114">The type of the third parameter is a <xref:System.Func%601> where `TResult` is the type of the variable that will store the thread-local state.</span></span> <span data-ttu-id="c1efc-115">Jeho typ je definován argumentem obecného typu zadaným při volání obecné <xref:System.Threading.Tasks.Parallel.For%60%601%28System.Int32%2CSystem.Int32%2CSystem.Func%7B%60%600%7D%2CSystem.Func%7BSystem.Int32%2CSystem.Threading.Tasks.ParallelLoopState%2C%60%600%2C%60%600%7D%2CSystem.Action%7B%60%600%7D%29> metody, která v tomto případě je <xref:System.Int64> .</span><span class="sxs-lookup"><span data-stu-id="c1efc-115">Its type is defined by the generic type argument supplied when calling the generic <xref:System.Threading.Tasks.Parallel.For%60%601%28System.Int32%2CSystem.Int32%2CSystem.Func%7B%60%600%7D%2CSystem.Func%7BSystem.Int32%2CSystem.Threading.Tasks.ParallelLoopState%2C%60%600%2C%60%600%7D%2CSystem.Action%7B%60%600%7D%29> method, which in this case is <xref:System.Int64>.</span></span> <span data-ttu-id="c1efc-116">Argument typu instruuje kompilátor typ dočasné proměnné, která bude použita k uložení místního stavu vlákna.</span><span class="sxs-lookup"><span data-stu-id="c1efc-116">The type argument tells the compiler the type of the temporary variable that will be used to store the thread-local state.</span></span> <span data-ttu-id="c1efc-117">V tomto příkladu výraz `() => 0` (nebo `Function() 0` v Visual Basic) inicializuje proměnnou místního vlákna na hodnotu nula.</span><span class="sxs-lookup"><span data-stu-id="c1efc-117">In this example, the expression `() => 0` (or `Function() 0` in Visual Basic) initializes the thread-local variable to zero.</span></span> <span data-ttu-id="c1efc-118">Pokud je argumentem obecného typu typ odkazu nebo uživatelsky definovaný typ hodnoty, výraz by vypadal takto:</span><span class="sxs-lookup"><span data-stu-id="c1efc-118">If the generic type argument is a reference type or user-defined value type, the expression would look like this:</span></span>  
  
```csharp  
() => new MyClass()  
```  
  
```vb  
Function() new MyClass()  
```  
  
 <span data-ttu-id="c1efc-119">Čtvrtý parametr definuje logiku smyčky.</span><span class="sxs-lookup"><span data-stu-id="c1efc-119">The fourth parameter defines the loop logic.</span></span> <span data-ttu-id="c1efc-120">Musí se jednat o delegáta nebo výraz lambda, jehož signatura je `Func<int, ParallelLoopState, long, long>` v jazyce C# nebo `Func(Of Integer, ParallelLoopState, Long, Long)` v Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="c1efc-120">It must be a delegate or lambda expression whose signature is `Func<int, ParallelLoopState, long, long>` in C# or `Func(Of Integer, ParallelLoopState, Long, Long)` in Visual Basic.</span></span> <span data-ttu-id="c1efc-121">První parametr je hodnota čítače smyčky pro tuto iteraci smyčky.</span><span class="sxs-lookup"><span data-stu-id="c1efc-121">The first parameter is the value of the loop counter for that iteration of the loop.</span></span> <span data-ttu-id="c1efc-122">Druhým je <xref:System.Threading.Tasks.ParallelLoopState> objekt, který lze použít k přerušení smyčky; tento objekt je poskytován <xref:System.Threading.Tasks.Parallel> třídou pro každý výskyt smyčky.</span><span class="sxs-lookup"><span data-stu-id="c1efc-122">The second is a <xref:System.Threading.Tasks.ParallelLoopState> object that can be used to break out of the loop; this object is provided by the <xref:System.Threading.Tasks.Parallel> class to each occurrence of the loop.</span></span> <span data-ttu-id="c1efc-123">Třetí parametr je proměnná lokální pro vlákno.</span><span class="sxs-lookup"><span data-stu-id="c1efc-123">The third parameter is the thread-local variable.</span></span> <span data-ttu-id="c1efc-124">Poslední parametr je návratový typ.</span><span class="sxs-lookup"><span data-stu-id="c1efc-124">The last parameter is the return type.</span></span> <span data-ttu-id="c1efc-125">V tomto případě je typ <xref:System.Int64> , protože se jedná o typ, který jsme určili v <xref:System.Threading.Tasks.Parallel.For%2A> argumentu typu.</span><span class="sxs-lookup"><span data-stu-id="c1efc-125">In this case, the type is <xref:System.Int64> because that is the type we specified in the <xref:System.Threading.Tasks.Parallel.For%2A> type argument.</span></span> <span data-ttu-id="c1efc-126">Tato proměnná je pojmenována `subtotal` a je vrácena výrazem lambda.</span><span class="sxs-lookup"><span data-stu-id="c1efc-126">That variable is named `subtotal` and is returned by the lambda expression.</span></span> <span data-ttu-id="c1efc-127">Návratová hodnota se používá k inicializaci `subtotal` při každé další iteraci smyčky.</span><span class="sxs-lookup"><span data-stu-id="c1efc-127">The return value is used to initialize `subtotal` on each subsequent iteration of the loop.</span></span> <span data-ttu-id="c1efc-128">Tento poslední parametr si také můžete představit jako hodnotu, která je předána každé iteraci a poté předána `localFinally` delegátovi po dokončení poslední iterace.</span><span class="sxs-lookup"><span data-stu-id="c1efc-128">You can also think of this last parameter as a value that is passed to each iteration, and then passed to the `localFinally` delegate when the last iteration is complete.</span></span>  
  
 <span data-ttu-id="c1efc-129">Pátý parametr definuje metodu, která je volána jednou, po dokončení všech iterací v konkrétním vlákně.</span><span class="sxs-lookup"><span data-stu-id="c1efc-129">The fifth parameter defines the method that is called once, after all the iterations on a particular thread have completed.</span></span> <span data-ttu-id="c1efc-130">Typ vstupního argumentu znovu odpovídá argumentu typu <xref:System.Threading.Tasks.Parallel.For%60%601%28System.Int32%2CSystem.Int32%2CSystem.Func%7B%60%600%7D%2CSystem.Func%7BSystem.Int32%2CSystem.Threading.Tasks.ParallelLoopState%2C%60%600%2C%60%600%7D%2CSystem.Action%7B%60%600%7D%29> metody a typu vrácené výrazem lambda těla.</span><span class="sxs-lookup"><span data-stu-id="c1efc-130">The type of the input argument again corresponds to the type argument of the <xref:System.Threading.Tasks.Parallel.For%60%601%28System.Int32%2CSystem.Int32%2CSystem.Func%7B%60%600%7D%2CSystem.Func%7BSystem.Int32%2CSystem.Threading.Tasks.ParallelLoopState%2C%60%600%2C%60%600%7D%2CSystem.Action%7B%60%600%7D%29> method and the type returned by the body lambda expression.</span></span> <span data-ttu-id="c1efc-131">V tomto příkladu je hodnota přidána do proměnné v rozsahu třídy v bezpečném způsobu vlákna voláním <xref:System.Threading.Interlocked.Add%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="c1efc-131">In this example, the value is added to a variable at class scope in a thread safe way by calling the <xref:System.Threading.Interlocked.Add%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="c1efc-132">Pomocí proměnné místního vlákna se při každé iteraci smyčky vyhneme psaní do této proměnné třídy.</span><span class="sxs-lookup"><span data-stu-id="c1efc-132">By using a thread-local variable, we have avoided writing to this class variable on every iteration of the loop.</span></span>  
  
 <span data-ttu-id="c1efc-133">Další informace o použití výrazů lambda naleznete v tématu [lambda výrazy v PLINQ a TPL](lambda-expressions-in-plinq-and-tpl.md).</span><span class="sxs-lookup"><span data-stu-id="c1efc-133">For more information about how to use lambda expressions, see [Lambda Expressions in PLINQ and TPL](lambda-expressions-in-plinq-and-tpl.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="c1efc-134">Viz také</span><span class="sxs-lookup"><span data-stu-id="c1efc-134">See also</span></span>

- [<span data-ttu-id="c1efc-135">Datový paralelismus</span><span class="sxs-lookup"><span data-stu-id="c1efc-135">Data Parallelism</span></span>](data-parallelism-task-parallel-library.md)
- [<span data-ttu-id="c1efc-136">Paralelní programování</span><span class="sxs-lookup"><span data-stu-id="c1efc-136">Parallel Programming</span></span>](index.md)
- [<span data-ttu-id="c1efc-137">Task Parallel Library (TPL)</span><span class="sxs-lookup"><span data-stu-id="c1efc-137">Task Parallel Library (TPL)</span></span>](task-parallel-library-tpl.md)
- [<span data-ttu-id="c1efc-138">Výrazy lambda v PLINQ a TPL</span><span class="sxs-lookup"><span data-stu-id="c1efc-138">Lambda Expressions in PLINQ and TPL</span></span>](lambda-expressions-in-plinq-and-tpl.md)
