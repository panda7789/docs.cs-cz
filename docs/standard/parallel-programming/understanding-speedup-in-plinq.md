---
title: Porozumění zrychlení v PLINQ
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- PLINQ queries, performance tuning
ms.assetid: 53706c7e-397d-467a-98cd-c0d1fd63ba5e
ms.openlocfilehash: 627f1327a9fe87fc226dfbb40df50ec4855edfb9
ms.sourcegitcommit: 33deec3e814238fb18a49b2a7e89278e27888291
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/02/2020
ms.locfileid: "84284894"
---
# <a name="understanding-speedup-in-plinq"></a><span data-ttu-id="789fe-102">Porozumění zrychlení v PLINQ</span><span class="sxs-lookup"><span data-stu-id="789fe-102">Understanding Speedup in PLINQ</span></span>
<span data-ttu-id="789fe-103">Hlavním účelem PLINQ je urychlení provádění LINQ to Objectsch dotazů tím, že se paralelně spouští Delegáti dotazů na vícejádrových počítačích.</span><span class="sxs-lookup"><span data-stu-id="789fe-103">The primary purpose of PLINQ is to speed up the execution of LINQ to Objects queries by executing the query delegates in parallel on multi-core computers.</span></span> <span data-ttu-id="789fe-104">PLINQ provádí nejlepší, pokud je zpracování každého prvku ve zdrojové kolekci nezávislé, bez jakéhokoli sdíleného stavu mezi jednotlivými delegáty.</span><span class="sxs-lookup"><span data-stu-id="789fe-104">PLINQ performs best when the processing of each element in a source collection is independent, with no shared state involved among the individual delegates.</span></span> <span data-ttu-id="789fe-105">Tyto operace jsou běžné v LINQ to Objects a PLINQ a často se nazývají "*delightfully Parallel*", protože se dají snadno naplánovat na více vláken.</span><span class="sxs-lookup"><span data-stu-id="789fe-105">Such operations are common in LINQ to Objects and PLINQ, and are often called "*delightfully parallel*" because they lend themselves easily to scheduling on multiple threads.</span></span> <span data-ttu-id="789fe-106">Ne všechny dotazy však sestávají výhradně z delightfully paralelních operací; ve většině případů dotaz zahrnuje některé operátory, které nelze buď paralelně, nebo které zpomalují paralelní spouštění.</span><span class="sxs-lookup"><span data-stu-id="789fe-106">However, not all queries consist entirely of delightfully parallel operations; in most cases, a query involves some operators that either cannot be parallelized, or that slow down parallel execution.</span></span> <span data-ttu-id="789fe-107">A dokonce i s dotazy, které jsou zcela delightfully paralelně, PLINQ musí stále rozdělit zdroj dat a naplánovat práci na vláknech a obvykle sloučit výsledky po dokončení dotazu.</span><span class="sxs-lookup"><span data-stu-id="789fe-107">And even with queries that are entirely delightfully parallel, PLINQ must still partition the data source and schedule the work on the threads, and usually merge the results when the query completes.</span></span> <span data-ttu-id="789fe-108">Všechny tyto operace se přidávají k výpočetním nákladům na paralelní zpracování; Tyto náklady na přidání paralelismu se nazývají *režijní*náklady.</span><span class="sxs-lookup"><span data-stu-id="789fe-108">All these operations add to the computational cost of parallelization; these costs of adding parallelization are called *overhead*.</span></span> <span data-ttu-id="789fe-109">Aby bylo možné dosáhnout optimálního výkonu v PLINQ dotazu, cílem je maximalizovat části, které jsou delightfully paralelně, a minimalizovat součásti, které vyžadují režii.</span><span class="sxs-lookup"><span data-stu-id="789fe-109">To achieve optimum performance in a PLINQ query, the goal is to maximize the parts that are delightfully parallel and minimize the parts that require overhead.</span></span> <span data-ttu-id="789fe-110">Tento článek poskytuje informace, které vám pomohou psát PLINQ dotazy, které jsou co nejefektivnější, a přitom stále poskytovat správné výsledky.</span><span class="sxs-lookup"><span data-stu-id="789fe-110">This article provides information that will help you write PLINQ queries that are as efficient as possible while still yielding correct results.</span></span>  
  
## <a name="factors-that-impact-plinq-query-performance"></a><span data-ttu-id="789fe-111">Faktory, které ovlivňují výkon dotazů PLINQ</span><span class="sxs-lookup"><span data-stu-id="789fe-111">Factors that Impact PLINQ Query Performance</span></span>  
 <span data-ttu-id="789fe-112">V následujících částech jsou uvedeny některé z nejdůležitějších faktorů, které mají vliv na výkon paralelních dotazů.</span><span class="sxs-lookup"><span data-stu-id="789fe-112">The following sections lists some of the most important factors that impact parallel query performance.</span></span> <span data-ttu-id="789fe-113">Jedná se o obecné příkazy, které samy o sebe nejsou dostačující pro předpověď výkonu dotazů ve všech případech.</span><span class="sxs-lookup"><span data-stu-id="789fe-113">These are general statements that by themselves are not sufficient to predict query performance in all cases.</span></span> <span data-ttu-id="789fe-114">Jako vždy je důležité změřit skutečný výkon konkrétních dotazů na počítačích s řadou reprezentativních konfigurací a zatížení.</span><span class="sxs-lookup"><span data-stu-id="789fe-114">As always, it is important to measure actual performance of specific queries on computers with a range of representative configurations and loads.</span></span>  
  
1. <span data-ttu-id="789fe-115">Výpočetní náklady na celkovou práci.</span><span class="sxs-lookup"><span data-stu-id="789fe-115">Computational cost of the overall work.</span></span>  
  
     <span data-ttu-id="789fe-116">Aby bylo možné dosáhnout zrychlení, musí mít PLINQ dotaz dostatek delightfully paralelní práce, aby mohl kompenzovat režii.</span><span class="sxs-lookup"><span data-stu-id="789fe-116">To achieve speedup, a PLINQ query must have enough delightfully parallel work to offset the overhead.</span></span> <span data-ttu-id="789fe-117">Práci lze vyjádřit jako výpočetní náklady každého delegáta vynásobený počtem prvků ve zdrojové kolekci.</span><span class="sxs-lookup"><span data-stu-id="789fe-117">The work can be expressed as the computational cost of each delegate multiplied by the number of elements in the source collection.</span></span> <span data-ttu-id="789fe-118">Za předpokladu, že operace může být paralelně náročná, znamená to více výpočetních příležitostí, což je větší příležitost pro zrychlení.</span><span class="sxs-lookup"><span data-stu-id="789fe-118">Assuming that an operation can be parallelized, the more computationally expensive it is, the greater the opportunity for speedup.</span></span> <span data-ttu-id="789fe-119">Například pokud funkce trvá jednu milisekundu, sekvenční dotaz více než 1000 prvků provede určitou operaci, zatímco paralelní dotaz na počítači se čtyřmi jádry může trvat pouze 250 milisekund.</span><span class="sxs-lookup"><span data-stu-id="789fe-119">For example, if a function takes one millisecond to execute, a sequential query over 1000 elements will take one second to perform that operation, whereas a parallel query on a computer with four cores might take only 250 milliseconds.</span></span> <span data-ttu-id="789fe-120">Výsledkem je 750 zrychlení milisekund.</span><span class="sxs-lookup"><span data-stu-id="789fe-120">This yields a speedup of 750 milliseconds.</span></span> <span data-ttu-id="789fe-121">Pokud je pro každý prvek požadována funkce jedna sekunda, zrychlení by byl 750 sekund.</span><span class="sxs-lookup"><span data-stu-id="789fe-121">If the function required one second to execute for each element, then the speedup would be 750 seconds.</span></span> <span data-ttu-id="789fe-122">Pokud je delegát velmi nákladný, může PLINQ nabízet významné zrychleníy s několika položkami ve zdrojové kolekci.</span><span class="sxs-lookup"><span data-stu-id="789fe-122">If the delegate is very expensive, then PLINQ might offer significant speedup with only a few items in the source collection.</span></span> <span data-ttu-id="789fe-123">Naopak malé zdrojové kolekce s triviálními delegáty obecně nejsou vhodnými kandidáty pro PLINQ.</span><span class="sxs-lookup"><span data-stu-id="789fe-123">Conversely, small source collections with trivial delegates are generally not good candidates for PLINQ.</span></span>  
  
     <span data-ttu-id="789fe-124">V následujícím příkladu je dotazce pravděpodobně dobrým kandidátem pro PLINQ za předpokladu, že jeho funkce Select zahrnuje hodně práce.</span><span class="sxs-lookup"><span data-stu-id="789fe-124">In the following example, queryA is probably a good candidate for PLINQ, assuming that its Select function involves a lot of work.</span></span> <span data-ttu-id="789fe-125">queryB je pravděpodobně nevhodný kandidát, protože v příkazu SELECT není dostatek práce a režie pro paralelní navýšení bude posunuta na většinu nebo všechny zrychlení.</span><span class="sxs-lookup"><span data-stu-id="789fe-125">queryB is probably not a good candidate because there is not enough work in the Select statement, and the overhead of parallelization will offset most or all of the speedup.</span></span>  
  
    ```vb  
    Dim queryA = From num In numberList.AsParallel()  
                 Select ExpensiveFunction(num); 'good for PLINQ  
  
    Dim queryB = From num In numberList.AsParallel()  
                 Where num Mod 2 > 0  
                 Select num; 'not as good for PLINQ  
    ```  
  
    ```csharp  
    var queryA = from num in numberList.AsParallel()  
                 select ExpensiveFunction(num); //good for PLINQ  
  
    var queryB = from num in numberList.AsParallel()  
                 where num % 2 > 0  
                 select num; //not as good for PLINQ  
    ```  
  
2. <span data-ttu-id="789fe-126">Počet logických jader v systému (stupeň paralelismu).</span><span class="sxs-lookup"><span data-stu-id="789fe-126">The number of logical cores on the system (degree of parallelism).</span></span>  
  
     <span data-ttu-id="789fe-127">Tento bod je zjevným corollaryem předchozí části, dotazy, které jsou delightfully paralelně, fungují na počítačích s více jádry, protože práci lze rozdělit mezi více souběžných vláken.</span><span class="sxs-lookup"><span data-stu-id="789fe-127">This point is an obvious corollary to the previous section, queries that are delightfully parallel run faster on machines with more cores because the work can be divided among more concurrent threads.</span></span> <span data-ttu-id="789fe-128">Celkové množství zrychlení závisí na tom, jaké procento celkové práce dotazu je paralelizovat.</span><span class="sxs-lookup"><span data-stu-id="789fe-128">The overall amount of speedup depends on what percentage of the overall work of the query is parallelizable.</span></span> <span data-ttu-id="789fe-129">Nepředpokládá se však, že všechny dotazy budou dvakrát spuštěny na osmi základních počítačích v případě čtyř základních počítačů.</span><span class="sxs-lookup"><span data-stu-id="789fe-129">However, do not assume that all queries will run twice as fast on an eight core computer as a four core computer.</span></span> <span data-ttu-id="789fe-130">Při ladění dotazů na optimální výkon je důležité změřit skutečné výsledky na počítačích s různými počty jader.</span><span class="sxs-lookup"><span data-stu-id="789fe-130">When tuning queries for optimal performance, it is important to measure actual results on computers with various numbers of cores.</span></span> <span data-ttu-id="789fe-131">Tento bod se vztahuje k bodu #1: aby bylo možné využívat větší výpočetní prostředky, vyžadují se větší datové sady.</span><span class="sxs-lookup"><span data-stu-id="789fe-131">This point is related to point #1: larger datasets are required to take advantage of greater computing resources.</span></span>  
  
3. <span data-ttu-id="789fe-132">Počet a druh operací.</span><span class="sxs-lookup"><span data-stu-id="789fe-132">The number and kind of operations.</span></span>  
  
     <span data-ttu-id="789fe-133">PLINQ poskytuje operátor metodu AsOrdered pro situace, kdy je nutné zachovat pořadí prvků ve zdrojové sekvenci.</span><span class="sxs-lookup"><span data-stu-id="789fe-133">PLINQ provides the AsOrdered operator for situations in which it is necessary to maintain the order of elements in the source sequence.</span></span> <span data-ttu-id="789fe-134">K řazení se účtují náklady, ale tyto náklady jsou obvykle mírné.</span><span class="sxs-lookup"><span data-stu-id="789fe-134">There is a cost associated with ordering, but this cost is usually modest.</span></span> <span data-ttu-id="789fe-135">Operace GroupBy a JOIN se také účtují jako režijní náklady.</span><span class="sxs-lookup"><span data-stu-id="789fe-135">GroupBy and Join operations likewise incur overhead.</span></span> <span data-ttu-id="789fe-136">PLINQ má nejlepší výkon, pokud je povoleno zpracovávat prvky ve zdrojové kolekci v libovolném pořadí a předat operátorovi Next, jakmile jsou připraveni.</span><span class="sxs-lookup"><span data-stu-id="789fe-136">PLINQ performs best when it is allowed to process elements in the source collection in any order, and pass them to the next operator as soon as they are ready.</span></span> <span data-ttu-id="789fe-137">Další informace naleznete v tématu [pořadí zachování v PLINQ](order-preservation-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="789fe-137">For more information, see [Order Preservation in PLINQ](order-preservation-in-plinq.md).</span></span>  
  
4. <span data-ttu-id="789fe-138">Forma provedení dotazu.</span><span class="sxs-lookup"><span data-stu-id="789fe-138">The form of query execution.</span></span>  
  
     <span data-ttu-id="789fe-139">Pokud ukládáte výsledky dotazu voláním ToArray – nebo ToList –, pak musí být výsledky ze všech paralelních vláken sloučeny do jediné datové struktury.</span><span class="sxs-lookup"><span data-stu-id="789fe-139">If you are storing the results of a query by calling ToArray or ToList, then the results from all parallel threads must be merged into the single data structure.</span></span> <span data-ttu-id="789fe-140">To zahrnuje nenevyhnutelné výpočetní náklady.</span><span class="sxs-lookup"><span data-stu-id="789fe-140">This involves an unavoidable computational cost.</span></span> <span data-ttu-id="789fe-141">Podobně platí, že pokud provádíte iteraci výsledků pomocí příkazu foreach (for each Visual Basic), výsledky z pracovních vláken musí být serializovány do vlákna enumerátoru.</span><span class="sxs-lookup"><span data-stu-id="789fe-141">Likewise, if you iterate the results by using a foreach (For Each in Visual Basic) loop, the results from the worker threads need to be serialized onto the enumerator thread.</span></span> <span data-ttu-id="789fe-142">Pokud ale chcete pouze provést určitou akci na základě výsledku z každého vlákna, můžete použít metodu ForAll k provedení této práce na více vláknech.</span><span class="sxs-lookup"><span data-stu-id="789fe-142">But if you just want to perform some action based on the result from each thread, you can use the ForAll method to perform this work on multiple threads.</span></span>  
  
5. <span data-ttu-id="789fe-143">Typ možností sloučení.</span><span class="sxs-lookup"><span data-stu-id="789fe-143">The type of merge options.</span></span>  
  
     <span data-ttu-id="789fe-144">PLINQ se dá nakonfigurovat buď na bufferový výstup, a vytvořit ho v blocích nebo všechny najednou po vyprodukování celé sady výsledků, nebo jinak pro streamování jednotlivých výsledků při jejich tvorbě.</span><span class="sxs-lookup"><span data-stu-id="789fe-144">PLINQ can be configured to either buffer its output, and produce it in chunks or all at once after the entire result set is produced, or else to stream individual results as they are produced.</span></span> <span data-ttu-id="789fe-145">V předchozích výsledcích se snížila celková doba spuštění a výsledkem je snížení latence mezi vydanými prvky.</span><span class="sxs-lookup"><span data-stu-id="789fe-145">The former results in decreased overall execution time and the latter results in decreased latency between yielded elements.</span></span>  <span data-ttu-id="789fe-146">I když možnosti sloučení nemají vždy zásadní dopad na celkový výkon dotazů, mohou ovlivnit vnímaný výkon, protože určují, jak dlouho musí uživatel čekat na zobrazení výsledků.</span><span class="sxs-lookup"><span data-stu-id="789fe-146">While the merge options do not always have a major impact on overall query performance, they can impact perceived performance because they control how long a user must wait to see results.</span></span> <span data-ttu-id="789fe-147">Další informace naleznete v tématu [možnosti sloučení v PLINQ](merge-options-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="789fe-147">For more information, see [Merge Options in PLINQ](merge-options-in-plinq.md).</span></span>  
  
6. <span data-ttu-id="789fe-148">Druh dělení.</span><span class="sxs-lookup"><span data-stu-id="789fe-148">The kind of partitioning.</span></span>  
  
     <span data-ttu-id="789fe-149">V některých případech může PLINQ dotaz nad indexovanou zdrojovou kolekcí způsobit nevyvážené pracovní zatížení.</span><span class="sxs-lookup"><span data-stu-id="789fe-149">In some cases, a PLINQ query over an indexable source collection may result in an unbalanced work load.</span></span> <span data-ttu-id="789fe-150">Pokud k tomu dojde, může být možné zvýšit výkon dotazu vytvořením vlastního rozdělovače.</span><span class="sxs-lookup"><span data-stu-id="789fe-150">When this occurs, you might be able to increase the query performance by creating a custom partitioner.</span></span> <span data-ttu-id="789fe-151">Další informace najdete v tématu [vlastní dělicí metody pro PLINQ a TPL](custom-partitioners-for-plinq-and-tpl.md).</span><span class="sxs-lookup"><span data-stu-id="789fe-151">For more information, see [Custom Partitioners for PLINQ and TPL](custom-partitioners-for-plinq-and-tpl.md).</span></span>  
  
## <a name="when-plinq-chooses-sequential-mode"></a><span data-ttu-id="789fe-152">Když PLINQ zvolí sekvenční režim</span><span class="sxs-lookup"><span data-stu-id="789fe-152">When PLINQ Chooses Sequential Mode</span></span>  
 <span data-ttu-id="789fe-153">PLINQ se vždycky pokusí spustit dotaz aspoň tak rychle, protože dotaz by se spouštěl sekvenčně.</span><span class="sxs-lookup"><span data-stu-id="789fe-153">PLINQ will always attempt to execute a query at least as fast as the query would run sequentially.</span></span> <span data-ttu-id="789fe-154">I když PLINQ nevypadá na tom, jak výpočetní náklady jsou delegáti uživatelů nebo jak velké je vstupní zdroj, hledá konkrétní dotaz "tvary".</span><span class="sxs-lookup"><span data-stu-id="789fe-154">Although PLINQ does not look at how computationally expensive the user delegates are, or how big the input source is, it does look for certain query "shapes."</span></span> <span data-ttu-id="789fe-155">Konkrétně vyhledává operátory nebo kombinace operátorů dotazů, které obvykle způsobují zpomalení dotazu v paralelním režimu.</span><span class="sxs-lookup"><span data-stu-id="789fe-155">Specifically, it looks for query operators or combinations of operators that typically cause a query to execute more slowly in parallel mode.</span></span> <span data-ttu-id="789fe-156">Když tyto tvary najde, PLINQ ve výchozím nastavení přejde zpět do sekvenčního režimu.</span><span class="sxs-lookup"><span data-stu-id="789fe-156">When it finds such shapes, PLINQ by default falls back to sequential mode.</span></span>  
  
 <span data-ttu-id="789fe-157">Po měření výkonu konkrétního dotazu ale můžete určit, že ve skutečnosti běží rychleji v paralelním režimu.</span><span class="sxs-lookup"><span data-stu-id="789fe-157">However, after measuring a specific query's performance, you may determine that it actually runs faster in parallel mode.</span></span> <span data-ttu-id="789fe-158">V takových případech můžete použít <xref:System.Linq.ParallelExecutionMode.ForceParallelism?displayProperty=nameWithType> příznak prostřednictvím <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> metody pro pokyn pro PLINQ k paralelizovat dotazu.</span><span class="sxs-lookup"><span data-stu-id="789fe-158">In such cases you can use the <xref:System.Linq.ParallelExecutionMode.ForceParallelism?displayProperty=nameWithType> flag via the <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> method to instruct PLINQ to parallelize the query.</span></span> <span data-ttu-id="789fe-159">Další informace naleznete v tématu [How to: určení režimu spouštění v PLINQ](how-to-specify-the-execution-mode-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="789fe-159">For more information, see [How to: Specify the Execution Mode in PLINQ](how-to-specify-the-execution-mode-in-plinq.md).</span></span>  
  
 <span data-ttu-id="789fe-160">Následující seznam popisuje obrazce dotazů, které PLINQ ve výchozím nastavení spustí v sekvenčním režimu:</span><span class="sxs-lookup"><span data-stu-id="789fe-160">The following list describes the query shapes that PLINQ by default will execute in sequential mode:</span></span>  
  
- <span data-ttu-id="789fe-161">Dotazy, které obsahují klauzuli SELECT, Indexed WHERE, indexované operátor SelectMany nebo ElementAt za operátorem řazení nebo filtrování, který odstranil nebo přeřadí původní indexy.</span><span class="sxs-lookup"><span data-stu-id="789fe-161">Queries that contain a Select, indexed Where, indexed SelectMany, or ElementAt clause after an ordering or filtering operator that has removed or rearranged original indices.</span></span>  
  
- <span data-ttu-id="789fe-162">Dotazy, které obsahují operátor vzít, TakeWhile –, Skip, SkipWhile – a kde indexy ve zdrojové sekvenci nejsou v původním pořadí.</span><span class="sxs-lookup"><span data-stu-id="789fe-162">Queries that contain a Take, TakeWhile, Skip, SkipWhile operator and where indices in the source sequence are not in the original order.</span></span>  
  
- <span data-ttu-id="789fe-163">Dotazy, které obsahují zip nebo SequenceEquals, pokud jeden z zdrojů dat nemá původně seřazený index a druhý zdroj dat je indexované (tj. pole nebo IList (T)).</span><span class="sxs-lookup"><span data-stu-id="789fe-163">Queries that contain Zip or SequenceEquals, unless one of the data sources has an originally ordered index and the other data source is indexable (i.e. an array or IList(T)).</span></span>  
  
- <span data-ttu-id="789fe-164">Dotazy, které obsahují Concat, pokud nejsou aplikovány na indexované zdroje dat.</span><span class="sxs-lookup"><span data-stu-id="789fe-164">Queries that contain Concat, unless it is applied to indexable data sources.</span></span>  
  
- <span data-ttu-id="789fe-165">Dotazy, které obsahují reverzní, pokud nejsou aplikovány na indexovací zdroj dat.</span><span class="sxs-lookup"><span data-stu-id="789fe-165">Queries that contain Reverse, unless applied to an indexable data source.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="789fe-166">Viz také</span><span class="sxs-lookup"><span data-stu-id="789fe-166">See also</span></span>

- [<span data-ttu-id="789fe-167">Paralelní LINQ (PLINQ)</span><span class="sxs-lookup"><span data-stu-id="789fe-167">Parallel LINQ (PLINQ)</span></span>](introduction-to-plinq.md)
