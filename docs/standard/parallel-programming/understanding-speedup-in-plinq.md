---
title: Porozumění zrychlení v PLINQ
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- PLINQ queries, performance tuning
ms.assetid: 53706c7e-397d-467a-98cd-c0d1fd63ba5e
ms.openlocfilehash: 07b5027d560a4caccc6c0a516c3f70c11df6be83
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/15/2020
ms.locfileid: "73139902"
---
# <a name="understanding-speedup-in-plinq"></a><span data-ttu-id="5fe51-102">Porozumění zrychlení v PLINQ</span><span class="sxs-lookup"><span data-stu-id="5fe51-102">Understanding Speedup in PLINQ</span></span>
<span data-ttu-id="5fe51-103">Primárním účelem PLINQ je urychlit provádění linq na objekty dotazů spuštěním delegáty dotazu paralelně na vícejádrových počítačích.</span><span class="sxs-lookup"><span data-stu-id="5fe51-103">The primary purpose of PLINQ is to speed up the execution of LINQ to Objects queries by executing the query delegates in parallel on multi-core computers.</span></span> <span data-ttu-id="5fe51-104">PLINQ funguje nejlépe při zpracování každého prvku ve zdrojové kolekci je nezávislý, bez sdíleného stavu zapojených mezi jednotlivé delegáty.</span><span class="sxs-lookup"><span data-stu-id="5fe51-104">PLINQ performs best when the processing of each element in a source collection is independent, with no shared state involved among the individual delegates.</span></span> <span data-ttu-id="5fe51-105">Tyto operace jsou běžné v LINQ na objekty a PLINQ a jsou často nazývány "*nádherně paralelní*", protože se snadno hodí k plánování na více vláknech.</span><span class="sxs-lookup"><span data-stu-id="5fe51-105">Such operations are common in LINQ to Objects and PLINQ, and are often called "*delightfully parallel*" because they lend themselves easily to scheduling on multiple threads.</span></span> <span data-ttu-id="5fe51-106">Ne všechny dotazy se však skládají výhradně z nádherně paralelních operací; ve většině případů dotaz zahrnuje některé operátory, které buď nelze paralelizovat, nebo které zpomalují paralelní spuštění.</span><span class="sxs-lookup"><span data-stu-id="5fe51-106">However, not all queries consist entirely of delightfully parallel operations; in most cases, a query involves some operators that either cannot be parallelized, or that slow down parallel execution.</span></span> <span data-ttu-id="5fe51-107">A i s dotazy, které jsou zcela nádherně paralelní, PLINQ musí stále rozdělit zdroj dat a naplánovat práci na vláknech a obvykle sloučit výsledky po dokončení dotazu.</span><span class="sxs-lookup"><span data-stu-id="5fe51-107">And even with queries that are entirely delightfully parallel, PLINQ must still partition the data source and schedule the work on the threads, and usually merge the results when the query completes.</span></span> <span data-ttu-id="5fe51-108">Všechny tyto operace přidat výpočetní náklady na paralelizaci; tyto náklady na přidání paralelizace se nazývají *režie*.</span><span class="sxs-lookup"><span data-stu-id="5fe51-108">All these operations add to the computational cost of parallelization; these costs of adding parallelization are called *overhead*.</span></span> <span data-ttu-id="5fe51-109">Chcete-li dosáhnout optimálního výkonu v dotazu PLINQ, cílem je maximalizovat součásti, které jsou nádherně paralelní a minimalizovat součásti, které vyžadují režii.</span><span class="sxs-lookup"><span data-stu-id="5fe51-109">To achieve optimum performance in a PLINQ query, the goal is to maximize the parts that are delightfully parallel and minimize the parts that require overhead.</span></span> <span data-ttu-id="5fe51-110">Tento článek obsahuje informace, které vám pomohou psát plinq dotazy, které jsou co nejúčinnější, zatímco stále přináší správné výsledky.</span><span class="sxs-lookup"><span data-stu-id="5fe51-110">This article provides information that will help you write PLINQ queries that are as efficient as possible while still yielding correct results.</span></span>  
  
## <a name="factors-that-impact-plinq-query-performance"></a><span data-ttu-id="5fe51-111">Faktory, které ovlivňují výkon dotazu PLINQ</span><span class="sxs-lookup"><span data-stu-id="5fe51-111">Factors that Impact PLINQ Query Performance</span></span>  
 <span data-ttu-id="5fe51-112">V následujících částech jsou uvedeny některé nejdůležitější faktory, které ovlivňují výkon paralelních dotazů.</span><span class="sxs-lookup"><span data-stu-id="5fe51-112">The following sections lists some of the most important factors that impact parallel query performance.</span></span> <span data-ttu-id="5fe51-113">Jedná se o obecné příkazy, které samy o sobě nestačí předpovědět výkon dotazu ve všech případech.</span><span class="sxs-lookup"><span data-stu-id="5fe51-113">These are general statements that by themselves are not sufficient to predict query performance in all cases.</span></span> <span data-ttu-id="5fe51-114">Jako vždy je důležité měřit skutečný výkon konkrétních dotazů v počítačích s řadou reprezentativních konfigurací a zatížení.</span><span class="sxs-lookup"><span data-stu-id="5fe51-114">As always, it is important to measure actual performance of specific queries on computers with a range of representative configurations and loads.</span></span>  
  
1. <span data-ttu-id="5fe51-115">Výpočetní náklady na celkovou práci.</span><span class="sxs-lookup"><span data-stu-id="5fe51-115">Computational cost of the overall work.</span></span>  
  
     <span data-ttu-id="5fe51-116">Chcete-li dosáhnout zrychlení, musí mít dotaz PLINQ dostatek nádherně paralelní práce k vyrovnání režie.</span><span class="sxs-lookup"><span data-stu-id="5fe51-116">To achieve speedup, a PLINQ query must have enough delightfully parallel work to offset the overhead.</span></span> <span data-ttu-id="5fe51-117">Práce může být vyjádřena jako výpočetní náklady každého delegáta vynásobené počtem prvků ve zdrojové kolekci.</span><span class="sxs-lookup"><span data-stu-id="5fe51-117">The work can be expressed as the computational cost of each delegate multiplied by the number of elements in the source collection.</span></span> <span data-ttu-id="5fe51-118">Za předpokladu, že operace může být paralelizována, čím více je výpočtově nákladná, tím větší je příležitost pro zrychlení.</span><span class="sxs-lookup"><span data-stu-id="5fe51-118">Assuming that an operation can be parallelized, the more computationally expensive it is, the greater the opportunity for speedup.</span></span> <span data-ttu-id="5fe51-119">Například pokud funkce trvá jednu milisekundu ke spuštění, sekvenční dotaz více než 1000 prvků bude trvat jednu sekundu k provedení této operace, zatímco paralelní dotaz v počítači se čtyřmi jádry může trvat pouze 250 milisekund.</span><span class="sxs-lookup"><span data-stu-id="5fe51-119">For example, if a function takes one millisecond to execute, a sequential query over 1000 elements will take one second to perform that operation, whereas a parallel query on a computer with four cores might take only 250 milliseconds.</span></span> <span data-ttu-id="5fe51-120">To dává zrychlení 750 milisekund.</span><span class="sxs-lookup"><span data-stu-id="5fe51-120">This yields a speedup of 750 milliseconds.</span></span> <span data-ttu-id="5fe51-121">Pokud funkce vyžaduje jednu sekundu ke spuštění pro každý prvek, pak zrychlení by 750 sekund.</span><span class="sxs-lookup"><span data-stu-id="5fe51-121">If the function required one second to execute for each element, then the speedup would be 750 seconds.</span></span> <span data-ttu-id="5fe51-122">Pokud delegát je velmi nákladné, pak PLINQ může nabídnout významné zrychlení pouze několik položek ve zdrojové kolekci.</span><span class="sxs-lookup"><span data-stu-id="5fe51-122">If the delegate is very expensive, then PLINQ might offer significant speedup with only a few items in the source collection.</span></span> <span data-ttu-id="5fe51-123">Naopak malé zdrojové kolekce s triviálnídelegáti obecně nejsou dobré kandidáty pro PLINQ.</span><span class="sxs-lookup"><span data-stu-id="5fe51-123">Conversely, small source collections with trivial delegates are generally not good candidates for PLINQ.</span></span>  
  
     <span data-ttu-id="5fe51-124">V následujícím příkladu queryA je pravděpodobně dobrým kandidátem pro PLINQ, za předpokladu, že jeho Select funkce zahrnuje mnoho práce.</span><span class="sxs-lookup"><span data-stu-id="5fe51-124">In the following example, queryA is probably a good candidate for PLINQ, assuming that its Select function involves a lot of work.</span></span> <span data-ttu-id="5fe51-125">queryB pravděpodobně není dobrým kandidátem, protože v příkazu Select není dostatek práce a režie paralelizace odsaze většinu nebo všechny zrychlení.</span><span class="sxs-lookup"><span data-stu-id="5fe51-125">queryB is probably not a good candidate because there is not enough work in the Select statement, and the overhead of parallelization will offset most or all of the speedup.</span></span>  
  
    ```vb  
    Dim queryA = From num In numberList.AsParallel()  
                 Select ExpensiveFunction(num); 'good for PLINQ  
  
    Dim queryB = From num In numberList.AsParallel()  
                 Where num Mod 2 > 0  
                 Select num; 'not as good for PLINQ  
    ```  
  
    ```csharp  
    var queryA = from num in numberList.AsParallel()  
                 select ExpensiveFunction(num); //good for PLINQ  
  
    var queryB = from num in numberList.AsParallel()  
                 where num % 2 > 0  
                 select num; //not as good for PLINQ  
    ```  
  
2. <span data-ttu-id="5fe51-126">Počet logických jader v systému (stupeň paralelismu).</span><span class="sxs-lookup"><span data-stu-id="5fe51-126">The number of logical cores on the system (degree of parallelism).</span></span>  
  
     <span data-ttu-id="5fe51-127">Tento bod je zřejmý důsledek předchozí části, dotazy, které jsou nádherně paralelní spustit rychleji na počítačích s více jádry, protože práce může být rozdělena mezi více souběžných vláken.</span><span class="sxs-lookup"><span data-stu-id="5fe51-127">This point is an obvious corollary to the previous section, queries that are delightfully parallel run faster on machines with more cores because the work can be divided among more concurrent threads.</span></span> <span data-ttu-id="5fe51-128">Celkové množství zrychlení závisí na tom, jaké procento celkové práce dotazu je paralelizovatelné.</span><span class="sxs-lookup"><span data-stu-id="5fe51-128">The overall amount of speedup depends on what percentage of the overall work of the query is parallelizable.</span></span> <span data-ttu-id="5fe51-129">Nepředpokládejte však, že všechny dotazy budou spuštěny dvakrát rychleji v počítači s osmi jádry jako čtyřjádrový počítač.</span><span class="sxs-lookup"><span data-stu-id="5fe51-129">However, do not assume that all queries will run twice as fast on an eight core computer as a four core computer.</span></span> <span data-ttu-id="5fe51-130">Při ladění dotazů pro optimální výkon je důležité měřit skutečné výsledky v počítačích s různým počtem jader.</span><span class="sxs-lookup"><span data-stu-id="5fe51-130">When tuning queries for optimal performance, it is important to measure actual results on computers with various numbers of cores.</span></span> <span data-ttu-id="5fe51-131">Tento bod souvisí s bodem #1: větší datové sady jsou nutné využít větší výpočetní prostředky.</span><span class="sxs-lookup"><span data-stu-id="5fe51-131">This point is related to point #1: larger datasets are required to take advantage of greater computing resources.</span></span>  
  
3. <span data-ttu-id="5fe51-132">Počet a druh operací.</span><span class="sxs-lookup"><span data-stu-id="5fe51-132">The number and kind of operations.</span></span>  
  
     <span data-ttu-id="5fe51-133">PLINQ poskytuje AsOrdered operátor pro situace, ve kterých je nutné udržovat pořadí prvků ve zdrojové sekvenci.</span><span class="sxs-lookup"><span data-stu-id="5fe51-133">PLINQ provides the AsOrdered operator for situations in which it is necessary to maintain the order of elements in the source sequence.</span></span> <span data-ttu-id="5fe51-134">S objednáním jsou spojeny náklady, ale tyto náklady jsou obvykle skromné.</span><span class="sxs-lookup"><span data-stu-id="5fe51-134">There is a cost associated with ordering, but this cost is usually modest.</span></span> <span data-ttu-id="5fe51-135">GroupBy a join operace také vznikají režii.</span><span class="sxs-lookup"><span data-stu-id="5fe51-135">GroupBy and Join operations likewise incur overhead.</span></span> <span data-ttu-id="5fe51-136">PLINQ funguje nejlépe, když je povoleno zpracovávat prvky ve zdrojové kolekci v libovolném pořadí a předat je dalšímu operátoru, jakmile jsou připraveny.</span><span class="sxs-lookup"><span data-stu-id="5fe51-136">PLINQ performs best when it is allowed to process elements in the source collection in any order, and pass them to the next operator as soon as they are ready.</span></span> <span data-ttu-id="5fe51-137">Další informace naleznete [v tématu Order Preservation in PLINQ](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="5fe51-137">For more information, see [Order Preservation in PLINQ](../../../docs/standard/parallel-programming/order-preservation-in-plinq.md).</span></span>  
  
4. <span data-ttu-id="5fe51-138">Forma spuštění dotazu.</span><span class="sxs-lookup"><span data-stu-id="5fe51-138">The form of query execution.</span></span>  
  
     <span data-ttu-id="5fe51-139">Pokud ukládáte výsledky dotazu voláním ToArray nebo ToList, musí být výsledky ze všech paralelních vláken sloučeny do jediné datové struktury.</span><span class="sxs-lookup"><span data-stu-id="5fe51-139">If you are storing the results of a query by calling ToArray or ToList, then the results from all parallel threads must be merged into the single data structure.</span></span> <span data-ttu-id="5fe51-140">To zahrnuje nevyhnutelné výpočetní náklady.</span><span class="sxs-lookup"><span data-stu-id="5fe51-140">This involves an unavoidable computational cost.</span></span> <span data-ttu-id="5fe51-141">Podobně pokud iterát výsledky pomocí foreach (pro každý v jazyce Visual Basic) smyčky, výsledky z pracovních podprocesů je třeba serializovat do vlákna čítače.</span><span class="sxs-lookup"><span data-stu-id="5fe51-141">Likewise, if you iterate the results by using a foreach (For Each in Visual Basic) loop, the results from the worker threads need to be serialized onto the enumerator thread.</span></span> <span data-ttu-id="5fe51-142">Ale pokud chcete provést některé akce na základě výsledku z každého vlákna, můžete použít ForAll metoda k provedení této práce na více vláken.</span><span class="sxs-lookup"><span data-stu-id="5fe51-142">But if you just want to perform some action based on the result from each thread, you can use the ForAll method to perform this work on multiple threads.</span></span>  
  
5. <span data-ttu-id="5fe51-143">Typ možností sloučení.</span><span class="sxs-lookup"><span data-stu-id="5fe51-143">The type of merge options.</span></span>  
  
     <span data-ttu-id="5fe51-144">PLINQ lze nakonfigurovat tak, aby buď vyrovnávací paměti jeho výstup a vyrábět v blocích nebo najednou po vytvoření celé sady výsledků, nebo jinak streamovat jednotlivé výsledky, jak jsou vyráběny.</span><span class="sxs-lookup"><span data-stu-id="5fe51-144">PLINQ can be configured to either buffer its output, and produce it in chunks or all at once after the entire result set is produced, or else to stream individual results as they are produced.</span></span> <span data-ttu-id="5fe51-145">První výsledky v snížení celkové doby provádění a druhý má za následek snížení latence mezi prvky výnosné.</span><span class="sxs-lookup"><span data-stu-id="5fe51-145">The former results in decreased overall execution time and the latter results in decreased latency between yielded elements.</span></span>  <span data-ttu-id="5fe51-146">Zatímco možnosti sloučení nemají vždy významný vliv na celkový výkon dotazu, mohou ovlivnit vnímaný výkon, protože řídí, jak dlouho musí uživatel čekat na výsledky.</span><span class="sxs-lookup"><span data-stu-id="5fe51-146">While the merge options do not always have a major impact on overall query performance, they can impact perceived performance because they control how long a user must wait to see results.</span></span> <span data-ttu-id="5fe51-147">Další informace naleznete [v tématu Merge Options in PLINQ](../../../docs/standard/parallel-programming/merge-options-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="5fe51-147">For more information, see [Merge Options in PLINQ](../../../docs/standard/parallel-programming/merge-options-in-plinq.md).</span></span>  
  
6. <span data-ttu-id="5fe51-148">Druh dělení.</span><span class="sxs-lookup"><span data-stu-id="5fe51-148">The kind of partitioning.</span></span>  
  
     <span data-ttu-id="5fe51-149">V některých případech dotaz PLINQ přes indexovatelné zdrojové kolekce může mít za následek nevyvážené pracovní zatížení.</span><span class="sxs-lookup"><span data-stu-id="5fe51-149">In some cases, a PLINQ query over an indexable source collection may result in an unbalanced work load.</span></span> <span data-ttu-id="5fe51-150">V takovém případě může být možné zvýšit výkon dotazu vytvořením vlastního rozdělovače.</span><span class="sxs-lookup"><span data-stu-id="5fe51-150">When this occurs, you might be able to increase the query performance by creating a custom partitioner.</span></span> <span data-ttu-id="5fe51-151">Další informace naleznete [v tématu Custom Partitioners for PLINQ and TPL](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md).</span><span class="sxs-lookup"><span data-stu-id="5fe51-151">For more information, see [Custom Partitioners for PLINQ and TPL](../../../docs/standard/parallel-programming/custom-partitioners-for-plinq-and-tpl.md).</span></span>  
  
## <a name="when-plinq-chooses-sequential-mode"></a><span data-ttu-id="5fe51-152">Když PLINQ zvolí sekvenční režim</span><span class="sxs-lookup"><span data-stu-id="5fe51-152">When PLINQ Chooses Sequential Mode</span></span>  
 <span data-ttu-id="5fe51-153">PLINQ se vždy pokusí spustit dotaz alespoň tak rychle, jak by dotaz spustit postupně.</span><span class="sxs-lookup"><span data-stu-id="5fe51-153">PLINQ will always attempt to execute a query at least as fast as the query would run sequentially.</span></span> <span data-ttu-id="5fe51-154">Přestože PLINQ nedívá na jak výpočtově drahé delegáty uživatele jsou nebo jak velký vstupní zdroj je, přehlíží určité "tvary dotazu".</span><span class="sxs-lookup"><span data-stu-id="5fe51-154">Although PLINQ does not look at how computationally expensive the user delegates are, or how big the input source is, it does look for certain query "shapes."</span></span> <span data-ttu-id="5fe51-155">Konkrétně hledá operátory dotazu nebo kombinace operátorů, které obvykle způsobí, že dotaz spustit pomaleji v paralelním režimu.</span><span class="sxs-lookup"><span data-stu-id="5fe51-155">Specifically, it looks for query operators or combinations of operators that typically cause a query to execute more slowly in parallel mode.</span></span> <span data-ttu-id="5fe51-156">Když najde takové tvary, PLINQ ve výchozím nastavení přejde zpět do sekvenčního režimu.</span><span class="sxs-lookup"><span data-stu-id="5fe51-156">When it finds such shapes, PLINQ by default falls back to sequential mode.</span></span>  
  
 <span data-ttu-id="5fe51-157">Však po měření výkonu konkrétní dotaz, můžete určit, že ve skutečnosti běží rychleji v paralelním režimu.</span><span class="sxs-lookup"><span data-stu-id="5fe51-157">However, after measuring a specific query's performance, you may determine that it actually runs faster in parallel mode.</span></span> <span data-ttu-id="5fe51-158">V takových případech můžete <xref:System.Linq.ParallelExecutionMode.ForceParallelism?displayProperty=nameWithType> použít <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> příznak prostřednictvím metody pokyn PLINQ paralelizovat dotaz.</span><span class="sxs-lookup"><span data-stu-id="5fe51-158">In such cases you can use the <xref:System.Linq.ParallelExecutionMode.ForceParallelism?displayProperty=nameWithType> flag via the <xref:System.Linq.ParallelEnumerable.WithExecutionMode%2A> method to instruct PLINQ to parallelize the query.</span></span> <span data-ttu-id="5fe51-159">Další informace naleznete v [tématu How to: Specify the Execution Mode in PLINQ](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md).</span><span class="sxs-lookup"><span data-stu-id="5fe51-159">For more information, see [How to: Specify the Execution Mode in PLINQ](../../../docs/standard/parallel-programming/how-to-specify-the-execution-mode-in-plinq.md).</span></span>  
  
 <span data-ttu-id="5fe51-160">Následující seznam popisuje obrazce dotazu, které bude ve výchozím nastavení spuštěna funkce PLINQ v sekvenčním režimu:</span><span class="sxs-lookup"><span data-stu-id="5fe51-160">The following list describes the query shapes that PLINQ by default will execute in sequential mode:</span></span>  
  
- <span data-ttu-id="5fe51-161">Dotazy, které obsahují Select, indexovány Kde, indexované SelectMany nebo ElementAt klauzule po řazení nebo filtrování operátor, který odebral nebo přeuspořádané původní indexy.</span><span class="sxs-lookup"><span data-stu-id="5fe51-161">Queries that contain a Select, indexed Where, indexed SelectMany, or ElementAt clause after an ordering or filtering operator that has removed or rearranged original indices.</span></span>  
  
- <span data-ttu-id="5fe51-162">Dotazy, které obsahují Take, TakeWhile, Skip, SkipWhile operátor a kde indexy ve zdrojové sekvenci nejsou v původním pořadí.</span><span class="sxs-lookup"><span data-stu-id="5fe51-162">Queries that contain a Take, TakeWhile, Skip, SkipWhile operator and where indices in the source sequence are not in the original order.</span></span>  
  
- <span data-ttu-id="5fe51-163">Dotazy, které obsahují ZIP nebo SequenceEquals, pokud jeden ze zdrojů dat má původně uspořádaný index a jiný zdroj dat je indexovatelný (tj. pole nebo IList(T)).</span><span class="sxs-lookup"><span data-stu-id="5fe51-163">Queries that contain Zip or SequenceEquals, unless one of the data sources has an originally ordered index and the other data source is indexable (i.e. an array or IList(T)).</span></span>  
  
- <span data-ttu-id="5fe51-164">Dotazy, které obsahují Concat, pokud se použije na indexovatelné zdroje dat.</span><span class="sxs-lookup"><span data-stu-id="5fe51-164">Queries that contain Concat, unless it is applied to indexable data sources.</span></span>  
  
- <span data-ttu-id="5fe51-165">Dotazy, které obsahují Reverse, pokud nejsou použity na indexovatelný zdroj dat.</span><span class="sxs-lookup"><span data-stu-id="5fe51-165">Queries that contain Reverse, unless applied to an indexable data source.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="5fe51-166">Viz také</span><span class="sxs-lookup"><span data-stu-id="5fe51-166">See also</span></span>

- [<span data-ttu-id="5fe51-167">Paralelní LINQ (PLINQ)</span><span class="sxs-lookup"><span data-stu-id="5fe51-167">Parallel LINQ (PLINQ)</span></span>](../../../docs/standard/parallel-programming/parallel-linq-plinq.md)
