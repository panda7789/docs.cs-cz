---
title: "Obecný systém typů"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-standard
ms.tgt_pltfrm: 
ms.topic: article
dev_langs:
- csharp
- vb
helpviewer_keywords:
- type system
- common type system
- assemblies [.NET Framework], types
- reference types
- value types
- cross-language interoperability
- namespaces [.NET Framework], types
- types, about types
ms.assetid: 53c57c96-83e1-4ee3-9543-9ac832671a89
caps.latest.revision: "25"
author: rpetrusha
ms.author: ronpet
manager: wpickett
ms.openlocfilehash: 9f9952cfd6ed2d5bac66d1cd5e3c8eed7506cd5c
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 11/21/2017
---
# <a name="common-type-system"></a><span data-ttu-id="d20ef-102">Obecný systém typů</span><span class="sxs-lookup"><span data-stu-id="d20ef-102">Common Type System</span></span>
<span data-ttu-id="d20ef-103">Obecný systém typů definuje, jak jsou typy deklarovat, použít a spravovat v modulu common language runtime který je taky důležitou součástí modulu runtime podporu integrace mezi jazyky.</span><span class="sxs-lookup"><span data-stu-id="d20ef-103">The common type system defines how types are declared, used, and managed in the common language runtime, and is also an important part of the runtime's support for cross-language integration.</span></span> <span data-ttu-id="d20ef-104">Obecný systém typů provádí následující funkce:</span><span class="sxs-lookup"><span data-stu-id="d20ef-104">The common type system performs the following functions:</span></span>  
  
-   <span data-ttu-id="d20ef-105">Vytvoří rozhraní, které pomáhá povolení integrace mezi jazyky, bezpečnost typů a provádění kódu vysoce výkonné.</span><span class="sxs-lookup"><span data-stu-id="d20ef-105">Establishes a framework that helps enable cross-language integration, type safety, and high-performance code execution.</span></span>  
  
-   <span data-ttu-id="d20ef-106">Poskytuje objektově orientovaný model, který podporuje úplnou implementaci řadě programovacích jazyků.</span><span class="sxs-lookup"><span data-stu-id="d20ef-106">Provides an object-oriented model that supports the complete implementation of many programming languages.</span></span>  
  
-   <span data-ttu-id="d20ef-107">Definuje pravidla, které musí postupovat podle jazyky, což pomůže zajistit, že objekty, které jsou napsané v různých jazycích může dojít ke vzájemné interakci.</span><span class="sxs-lookup"><span data-stu-id="d20ef-107">Defines rules that languages must follow, which helps ensure that objects written in different languages can interact with each other.</span></span>  
  
-   <span data-ttu-id="d20ef-108">Poskytuje knihovnu, která obsahuje primitivní datové typy (například <xref:System.Boolean>, <xref:System.Byte>, <xref:System.Char>, <xref:System.Int32>, a <xref:System.UInt64>) používané při vývoji aplikace.</span><span class="sxs-lookup"><span data-stu-id="d20ef-108">Provides a library that contains the primitive data types (such as <xref:System.Boolean>, <xref:System.Byte>, <xref:System.Char>, <xref:System.Int32>, and <xref:System.UInt64>) used in application development.</span></span>  
  
 <span data-ttu-id="d20ef-109">Toto téma obsahuje následující oddíly:</span><span class="sxs-lookup"><span data-stu-id="d20ef-109">This topic contains the following sections:</span></span>  
  
-   [<span data-ttu-id="d20ef-110">Typy v rozhraní .NET</span><span class="sxs-lookup"><span data-stu-id="d20ef-110">Types in .NET</span></span>](#types_in_the_net_framework)  
  
-   [<span data-ttu-id="d20ef-111">Definice typů</span><span class="sxs-lookup"><span data-stu-id="d20ef-111">Type Definitions</span></span>](#type_definitions)  
  
-   [<span data-ttu-id="d20ef-112">Členy typu</span><span class="sxs-lookup"><span data-stu-id="d20ef-112">Type Members</span></span>](#type_members)  
  
-   [<span data-ttu-id="d20ef-113">Charakteristika členy typu</span><span class="sxs-lookup"><span data-stu-id="d20ef-113">Characteristics of Type Members</span></span>](#characteristics_of_type_members)  
  
<a name="types_in_the_net_framework"></a>   
## <a name="types-in-net"></a><span data-ttu-id="d20ef-114">Typy v rozhraní .NET</span><span class="sxs-lookup"><span data-stu-id="d20ef-114">Types in .NET</span></span>  
 <span data-ttu-id="d20ef-115">Všechny typy v rozhraní .NET jsou buď hodnotové nebo odkazové typy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-115">All types in .NET are either value types or reference types.</span></span>  
  
 <span data-ttu-id="d20ef-116">Typy hodnot jsou datové typy, jejichž objekty jsou reprezentované pomocí objektu skutečnou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-116">Value types are data types whose objects are represented by the object's actual value.</span></span> <span data-ttu-id="d20ef-117">Pokud instance typu hodnota je přiřazený k proměnné, tuto proměnnou dostane novou kopii hodnoty.</span><span class="sxs-lookup"><span data-stu-id="d20ef-117">If an instance of a value type is assigned to a variable, that variable is given a fresh copy of the value.</span></span>  
  
 <span data-ttu-id="d20ef-118">Odkazové typy jsou datové typy, jejichž objekty jsou reprezentované pomocí odkaz (podobně jako ukazatel) na skutečnou hodnotu objektu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-118">Reference types are data types whose objects are represented by a reference (similar to a pointer) to the object's actual value.</span></span> <span data-ttu-id="d20ef-119">Pokud typ odkazu je přiřazený k proměnné, tuto proměnnou odkazuje (směřuje) původní hodnotu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-119">If a reference type is assigned to a variable, that variable references (points to) the original value.</span></span> <span data-ttu-id="d20ef-120">Je vytvořena žádná kopie.</span><span class="sxs-lookup"><span data-stu-id="d20ef-120">No copy is made.</span></span>  
  
 <span data-ttu-id="d20ef-121">Obecný systém typů v rozhraní .NET podporuje následujících pět kategorií typů:</span><span class="sxs-lookup"><span data-stu-id="d20ef-121">The common type system in .NET supports the following five categories of types:</span></span>  
  
-   [<span data-ttu-id="d20ef-122">Třídy</span><span class="sxs-lookup"><span data-stu-id="d20ef-122">Classes</span></span>](#Classes)  
  
-   [<span data-ttu-id="d20ef-123">Struktury</span><span class="sxs-lookup"><span data-stu-id="d20ef-123">Structures</span></span>](#Structures)  
  
-   [<span data-ttu-id="d20ef-124">Výčty</span><span class="sxs-lookup"><span data-stu-id="d20ef-124">Enumerations</span></span>](#Enumerations)  
  
-   [<span data-ttu-id="d20ef-125">Rozhraní</span><span class="sxs-lookup"><span data-stu-id="d20ef-125">Interfaces</span></span>](#Interfaces)  
  
-   [<span data-ttu-id="d20ef-126">Delegáti</span><span class="sxs-lookup"><span data-stu-id="d20ef-126">Delegates</span></span>](#Delegates)  
  
<a name="Classes"></a>   
### <a name="classes"></a><span data-ttu-id="d20ef-127">Třídy</span><span class="sxs-lookup"><span data-stu-id="d20ef-127">Classes</span></span>  
 <span data-ttu-id="d20ef-128">Třída je typu odkazu, který lze odvodit přímo z jiné třídy, a je implicitně odvozena z <xref:System.Object?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="d20ef-128">A class is a reference type that can be derived directly from another class and that is derived implicitly from <xref:System.Object?displayProperty=nameWithType>.</span></span> <span data-ttu-id="d20ef-129">Třída definuje operace, že objekt (což je instance třídy) může provádět (metody, události nebo vlastnosti) a data, že objekt obsahuje (pole).</span><span class="sxs-lookup"><span data-stu-id="d20ef-129">The class defines the operations that an object (which is an instance of the class) can perform (methods, events, or properties) and the data that the object contains (fields).</span></span> <span data-ttu-id="d20ef-130">I když třídu obvykle obsahuje definice a implementaci (na rozdíl od rozhraní, například, které obsahují pouze definici bez implementace), může mít jeden nebo více členů, které nemají implementaci.</span><span class="sxs-lookup"><span data-stu-id="d20ef-130">Although a class generally includes both definition and implementation (unlike interfaces, for example, which contain only definition without implementation), it can have one or more members that have no implementation.</span></span>  
  
 <span data-ttu-id="d20ef-131">Následující tabulka popisuje některé charakteristiky, které mohou mít třídu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-131">The following table describes some of the characteristics that a class may have.</span></span> <span data-ttu-id="d20ef-132">Každý jazyk, který podporuje modul runtime poskytuje způsob, jak označuje, že třída nebo člen třídy obsahuje jeden nebo více z těchto vlastností.</span><span class="sxs-lookup"><span data-stu-id="d20ef-132">Each language that supports the runtime provides a way to indicate that a class or class member has one or more of these characteristics.</span></span> <span data-ttu-id="d20ef-133">Ale jednotlivé programovací jazyky cílených .NET nemusí mít tyto charakteristiky k dispozici.</span><span class="sxs-lookup"><span data-stu-id="d20ef-133">However, individual programming languages that target .NET may not make all these characteristics available.</span></span>  
  
|<span data-ttu-id="d20ef-134">Vlastnosti</span><span class="sxs-lookup"><span data-stu-id="d20ef-134">Characteristic</span></span>|<span data-ttu-id="d20ef-135">Popis</span><span class="sxs-lookup"><span data-stu-id="d20ef-135">Description</span></span>|  
|--------------------|-----------------|  
|<span data-ttu-id="d20ef-136">sealed</span><span class="sxs-lookup"><span data-stu-id="d20ef-136">sealed</span></span>|<span data-ttu-id="d20ef-137">Určuje, že z tohoto typu nelze odvodit jiné třídy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-137">Specifies that another class cannot be derived from this type.</span></span>|  
|<span data-ttu-id="d20ef-138">implements</span><span class="sxs-lookup"><span data-stu-id="d20ef-138">implements</span></span>|<span data-ttu-id="d20ef-139">Označuje, že třída používá jedno nebo více rozhraní tím, že poskytuje implementace členů rozhraní.</span><span class="sxs-lookup"><span data-stu-id="d20ef-139">Indicates that the class uses one or more interfaces by providing implementations of interface members.</span></span>|  
|<span data-ttu-id="d20ef-140">abstract</span><span class="sxs-lookup"><span data-stu-id="d20ef-140">abstract</span></span>|<span data-ttu-id="d20ef-141">Označuje, že nelze vytvořit instanci třídy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-141">Indicates that the class cannot be instantiated.</span></span> <span data-ttu-id="d20ef-142">Pokud chcete použít, musí být jiné třídy z něj.</span><span class="sxs-lookup"><span data-stu-id="d20ef-142">To use it, you must derive another class from it.</span></span>|  
|<span data-ttu-id="d20ef-143">dědí</span><span class="sxs-lookup"><span data-stu-id="d20ef-143">inherits</span></span>|<span data-ttu-id="d20ef-144">Označuje, že instance této třídy lze použít všude, kde je zadán základní třídy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-144">Indicates that instances of the class can be used anywhere the base class is specified.</span></span> <span data-ttu-id="d20ef-145">Odvozené třídy, která dědí z databáze třídu, můžete použít implementace všechny veřejné členy poskytuje základní třídu nebo odvozené třídy můžete přepsat implementace veřejné členy vlastní implementací.</span><span class="sxs-lookup"><span data-stu-id="d20ef-145">A derived class that inherits from a base class can use the implementation of any public members provided by the base class, or the derived class can override the implementation of the public members with its own implementation.</span></span>|  
|<span data-ttu-id="d20ef-146">Export nebo není exportovat</span><span class="sxs-lookup"><span data-stu-id="d20ef-146">exported or not exported</span></span>|<span data-ttu-id="d20ef-147">Určuje, zda je třída viditelné mimo sestavení, ve kterém je definovaný.</span><span class="sxs-lookup"><span data-stu-id="d20ef-147">Indicates whether a class is visible outside the assembly in which it is defined.</span></span> <span data-ttu-id="d20ef-148">Tato vlastnost se týká pouze na nejvyšší úrovni třídy a vnořené třídy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-148">This characteristic applies only to top-level classes and not to nested classes.</span></span>|  
  
> [!NOTE]
>  <span data-ttu-id="d20ef-149">Třídy mohou být také vnořené v nadřazené třídě nebo struktuře.</span><span class="sxs-lookup"><span data-stu-id="d20ef-149">A class can also be nested in a parent class or structure.</span></span> <span data-ttu-id="d20ef-150">Vnořené třídy také mít vlastnosti člena.</span><span class="sxs-lookup"><span data-stu-id="d20ef-150">Nested classes also have member characteristics.</span></span> <span data-ttu-id="d20ef-151">Další informace najdete v tématu [vnořené typy](#NestedTypes).</span><span class="sxs-lookup"><span data-stu-id="d20ef-151">For more information, see [Nested Types](#NestedTypes).</span></span>  
  
 <span data-ttu-id="d20ef-152">Členy třídy, které nemají implementaci jsou abstraktní členy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-152">Class members that have no implementation are abstract members.</span></span> <span data-ttu-id="d20ef-153">Třída, která má jeden nebo více abstraktní členy je sám abstraktní; Nelze vytvořit novou instanci.</span><span class="sxs-lookup"><span data-stu-id="d20ef-153">A class that has one or more abstract members is itself abstract; new instances of it cannot be created.</span></span> <span data-ttu-id="d20ef-154">Některé jazyky, které používají modul runtime umožňuje označit jako abstraktní třídu, i v případě, že žádný z jejích členů není abstraktní.</span><span class="sxs-lookup"><span data-stu-id="d20ef-154">Some languages that target the runtime let you mark a class as abstract even if none of its members are abstract.</span></span> <span data-ttu-id="d20ef-155">Abstraktní třídu můžete použít, pokud chcete zapouzdřit základní sadu funkcí, které odvozených třídách můžete dědit nebo případě potřeby přepsat.</span><span class="sxs-lookup"><span data-stu-id="d20ef-155">You can use an abstract class when you want to encapsulate a basic set of functionality that derived classes can inherit or override when appropriate.</span></span> <span data-ttu-id="d20ef-156">Třídy, které nejsou abstraktní jsou označovány jako konkrétní třídy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-156">Classes that are not abstract are referred to as concrete classes.</span></span>  
  
 <span data-ttu-id="d20ef-157">Třída může implementovat libovolný počet rozhraní, ale může dědit vlastnosti pouze z jediné základní třídy kromě <xref:System.Object?displayProperty=nameWithType>, z všechny třídy dědí implicitně.</span><span class="sxs-lookup"><span data-stu-id="d20ef-157">A class can implement any number of interfaces, but it can inherit from only one base class in addition to <xref:System.Object?displayProperty=nameWithType>, from which all classes inherit implicitly.</span></span> <span data-ttu-id="d20ef-158">Všechny třídy musí mít alespoň jeden konstruktor, který inicializuje nové instance třídy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-158">All classes must have at least one constructor, which initializes new instances of the class.</span></span> <span data-ttu-id="d20ef-159">Pokud konstruktor nejsou explicitně definovány, většina kompilátory automaticky poskytne výchozí konstruktor (bez parametrů).</span><span class="sxs-lookup"><span data-stu-id="d20ef-159">If you do not explicitly define a constructor, most compilers will automatically provide a default (parameterless) constructor.</span></span>  
  
<a name="Structures"></a>   
### <a name="structures"></a><span data-ttu-id="d20ef-160">Struktury</span><span class="sxs-lookup"><span data-stu-id="d20ef-160">Structures</span></span>  
 <span data-ttu-id="d20ef-161">Struktura je hodnota typ, který je implicitně odvozena z <xref:System.ValueType?displayProperty=nameWithType>, který je zase odvozen z <xref:System.Object?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="d20ef-161">A structure is a value type that derives implicitly from <xref:System.ValueType?displayProperty=nameWithType>, which in turn is derived from <xref:System.Object?displayProperty=nameWithType>.</span></span> <span data-ttu-id="d20ef-162">Struktura je velmi užitečná pro představující hodnoty jsou malé požadavky na paměť a předání hodnoty jako parametry-hodnota metody, které mají parametry silného typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-162">A structure is very useful for representing values whose memory requirements are small, and for passing values as by-value parameters to methods that have strongly typed parameters.</span></span> <span data-ttu-id="d20ef-163">V rozhraní .NET, všechny primitivní datové typy (<xref:System.Boolean>, <xref:System.Byte>, <xref:System.Char>, <xref:System.DateTime>, <xref:System.Decimal>, <xref:System.Double>, <xref:System.Int16>, <xref:System.Int32>, <xref:System.Int64>, <xref:System.SByte>, <xref:System.Single>, <xref:System.UInt16>, <xref:System.UInt32>, a <xref:System.UInt64>) jsou definovány jako struktury.</span><span class="sxs-lookup"><span data-stu-id="d20ef-163">In .NET, all primitive data types (<xref:System.Boolean>, <xref:System.Byte>, <xref:System.Char>, <xref:System.DateTime>, <xref:System.Decimal>, <xref:System.Double>, <xref:System.Int16>, <xref:System.Int32>, <xref:System.Int64>, <xref:System.SByte>, <xref:System.Single>, <xref:System.UInt16>, <xref:System.UInt32>, and <xref:System.UInt64>) are defined as structures.</span></span>  
  
 <span data-ttu-id="d20ef-164">Stejně jako třídy struktury definovat dat (pole struktury) a operace, které lze provést na data (metody struktury).</span><span class="sxs-lookup"><span data-stu-id="d20ef-164">Like classes, structures define both data (the fields of the structure) and the operations that can be performed on that data (the methods of the structure).</span></span> <span data-ttu-id="d20ef-165">To znamená, že můžete volat metody pro struktury, včetně virtuální metody definované na <xref:System.Object?displayProperty=nameWithType> a <xref:System.ValueType?displayProperty=nameWithType> třídy a jakékoli metody definované na typ hodnoty sám sebe.</span><span class="sxs-lookup"><span data-stu-id="d20ef-165">This means that you can call methods on structures, including the virtual methods defined on the <xref:System.Object?displayProperty=nameWithType> and <xref:System.ValueType?displayProperty=nameWithType> classes, and any methods defined on the value type itself.</span></span> <span data-ttu-id="d20ef-166">Jinými slovy struktury může mít pole, vlastnosti a události, jakož i statické a nestatické metody.</span><span class="sxs-lookup"><span data-stu-id="d20ef-166">In other words, structures can have fields, properties, and events, as well as static and nonstatic methods.</span></span> <span data-ttu-id="d20ef-167">Můžete vytvořit instance struktury, je předat jako parametry, uložit je jako místní proměnné, nebo uložit je do pole jiný typ hodnoty nebo typ odkazu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-167">You can create instances of structures, pass them as parameters, store them as local variables, or store them in a field of another value type or reference type.</span></span> <span data-ttu-id="d20ef-168">Struktury můžete taky implementovat rozhraní.</span><span class="sxs-lookup"><span data-stu-id="d20ef-168">Structures can also implement interfaces.</span></span>  
  
 <span data-ttu-id="d20ef-169">Typy hodnot také z tříd v několika ohledech liší.</span><span class="sxs-lookup"><span data-stu-id="d20ef-169">Value types also differ from classes in several respects.</span></span> <span data-ttu-id="d20ef-170">První, i když implicitně dědí z <xref:System.ValueType?displayProperty=nameWithType>, nemohou přímo dědit z libovolného typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-170">First, although they implicitly inherit from <xref:System.ValueType?displayProperty=nameWithType>, they cannot directly inherit from any type.</span></span> <span data-ttu-id="d20ef-171">Podobně, jako jsou zapečetěné všechny typy hodnot, což znamená, že žádný jiný typ lze odvodit z nich.</span><span class="sxs-lookup"><span data-stu-id="d20ef-171">Similarly, all value types are sealed, which means that no other type can be derived from them.</span></span> <span data-ttu-id="d20ef-172">Také nevyžadují konstruktory.</span><span class="sxs-lookup"><span data-stu-id="d20ef-172">They also do not require constructors.</span></span>  
  
 <span data-ttu-id="d20ef-173">Pro každý typ hodnoty poskytuje modul common language runtime odpovídající zabalený typ, což je třída, která má stejné chování jako typ hodnoty a stavu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-173">For each value type, the common language runtime supplies a corresponding boxed type, which is a class that has the same state and behavior as the value type.</span></span> <span data-ttu-id="d20ef-174">Instance typu hodnota je zabalená, když je předán do metodu, která přijímá parametr typu <xref:System.Object?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="d20ef-174">An instance of a value type is boxed when it is passed to a method that accepts a parameter of type <xref:System.Object?displayProperty=nameWithType>.</span></span> <span data-ttu-id="d20ef-175">Nezabalený (to znamená, převést z instance třídy zpět na instanci typu hodnoty) při vrácení řízení z volání metody, která přijímá typ hodnoty jako parametr odkazem.</span><span class="sxs-lookup"><span data-stu-id="d20ef-175">It is unboxed (that is, converted from an instance of a class back to an instance of a value type) when control returns from a method call that accepts a value type as a by-reference parameter.</span></span> <span data-ttu-id="d20ef-176">Některé jazyky vyžadují použití speciální syntaxe, pokud je potřeba; zabalený typ. pevně určené typ ostatní automaticky používat, pokud je to potřeba.</span><span class="sxs-lookup"><span data-stu-id="d20ef-176">Some languages require that you use special syntax when the boxed type is required; others automatically use the boxed type when it is needed.</span></span> <span data-ttu-id="d20ef-177">Když definujete typ hodnoty, definujete zabalený i nezabalený typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-177">When you define a value type, you are defining both the boxed and the unboxed type.</span></span>  
  
<a name="Enumerations"></a>   
### <a name="enumerations"></a><span data-ttu-id="d20ef-178">Výčty</span><span class="sxs-lookup"><span data-stu-id="d20ef-178">Enumerations</span></span>  
 <span data-ttu-id="d20ef-179">Výčet (výčtu) je typ hodnoty, který dědí přímo z <xref:System.Enum?displayProperty=nameWithType> a že zdroje alternativní názvy pro hodnoty základní primitivního typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-179">An enumeration (enum) is a value type that inherits directly from <xref:System.Enum?displayProperty=nameWithType> and that supplies alternate names for the values of an underlying primitive type.</span></span> <span data-ttu-id="d20ef-180">Typ výčtu, má název, základní typ, který musí být jeden z typů předdefinované podepsaný držitelem nebo bez znaménka celé číslo (například <xref:System.Byte>, <xref:System.Int32>, nebo <xref:System.UInt64>) a sady polí.</span><span class="sxs-lookup"><span data-stu-id="d20ef-180">An enumeration type has a name, an underlying type that must be one of the built-in signed or unsigned integer types (such as <xref:System.Byte>, <xref:System.Int32>, or <xref:System.UInt64>), and a set of fields.</span></span> <span data-ttu-id="d20ef-181">Pole jsou statických polí literálu, z nichž každý představuje konstantu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-181">The fields are static literal fields, each of which represents a constant.</span></span> <span data-ttu-id="d20ef-182">Stejnou hodnotu lze přiřadit k více polí.</span><span class="sxs-lookup"><span data-stu-id="d20ef-182">The same value can be assigned to multiple fields.</span></span> <span data-ttu-id="d20ef-183">V takovém případě je třeba jedna z hodnot označit jako primární hodnotu výčtu pro reflexe a převod řetězce.</span><span class="sxs-lookup"><span data-stu-id="d20ef-183">When this occurs, you must mark one of the values as the primary enumeration value for reflection and string conversion.</span></span>  
  
 <span data-ttu-id="d20ef-184">Můžete přiřadit hodnotu základní typ výčtu a naopak (žádné přetypování vyžadují modul runtime).</span><span class="sxs-lookup"><span data-stu-id="d20ef-184">You can assign a value of the underlying type to an enumeration and vice versa (no cast is required by the runtime).</span></span> <span data-ttu-id="d20ef-185">Můžete vytvořit instanci výčtu a volat metody <xref:System.Enum?displayProperty=nameWithType>, stejně jako jakékoli metody definované u základního typu výčtu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-185">You can create an instance of an enumeration and call the methods of <xref:System.Enum?displayProperty=nameWithType>, as well as any methods defined on the enumeration's underlying type.</span></span> <span data-ttu-id="d20ef-186">Ale některé jazyky pravděpodobně neumožní předat výčet jako parametr, pokud je nutné použít instanci základní typ (nebo naopak).</span><span class="sxs-lookup"><span data-stu-id="d20ef-186">However, some languages might not let you pass an enumeration as a parameter when an instance of the underlying type is required (or vice versa).</span></span>  
  
 <span data-ttu-id="d20ef-187">Pro výčty platí následující omezení:</span><span class="sxs-lookup"><span data-stu-id="d20ef-187">The following additional restrictions apply to enumerations:</span></span>  
  
-   <span data-ttu-id="d20ef-188">Nemohou definovat vlastní metody.</span><span class="sxs-lookup"><span data-stu-id="d20ef-188">They cannot define their own methods.</span></span>  
  
-   <span data-ttu-id="d20ef-189">Nemohou implementovat rozhraní.</span><span class="sxs-lookup"><span data-stu-id="d20ef-189">They cannot implement interfaces.</span></span>  
  
-   <span data-ttu-id="d20ef-190">Nemohou definovat vlastnosti nebo události.</span><span class="sxs-lookup"><span data-stu-id="d20ef-190">They cannot define properties or events.</span></span>  
  
-   <span data-ttu-id="d20ef-191">Nemohou být obecné, pokud nejsou obecné pouze vzhledem k tomu, že jsou vnořené do obecného typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-191">They cannot be generic, unless they are generic only because they are nested within a generic type.</span></span> <span data-ttu-id="d20ef-192">To znamená výčet nemůže mít parametry typu své vlastní.</span><span class="sxs-lookup"><span data-stu-id="d20ef-192">That is, an enumeration cannot have type parameters of its own.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="d20ef-193">Vnořené typy (včetně výčty) jsou vytvořené pomocí jazyka Visual Basic, C# a C++ zahrnují parametry typu všech nadřazených obecných typů a jsou tedy Obecné, i když nemají vlastní parametry typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-193">Nested types (including enumerations) created with Visual Basic, C#, and C++ include the type parameters of all enclosing generic types, and are therefore generic even if they do not have type parameters of their own.</span></span> <span data-ttu-id="d20ef-194">Další informace najdete v části "Vnořené typy" v <xref:System.Type.MakeGenericType%2A?displayProperty=nameWithType> referenční téma.</span><span class="sxs-lookup"><span data-stu-id="d20ef-194">For more information, see "Nested Types" in the <xref:System.Type.MakeGenericType%2A?displayProperty=nameWithType> reference topic.</span></span>  
  
 <span data-ttu-id="d20ef-195"><xref:System.FlagsAttribute> Atribut označuje zvláštní druh výčtu nazývaný bitové pole.</span><span class="sxs-lookup"><span data-stu-id="d20ef-195">The <xref:System.FlagsAttribute> attribute denotes a special kind of enumeration called a bit field.</span></span> <span data-ttu-id="d20ef-196">Samotný modul runtime nerozlišuje mezi tradiční výčty a bitová pole, ale váš jazyk může udělat.</span><span class="sxs-lookup"><span data-stu-id="d20ef-196">The runtime itself does not distinguish between traditional enumerations and bit fields, but your language might do so.</span></span> <span data-ttu-id="d20ef-197">Když se rozlišují, bitové operátory lze na bitová pole, ale ne na výčty, generování nepojmenované hodnot.</span><span class="sxs-lookup"><span data-stu-id="d20ef-197">When this distinction is made, bitwise operators can be used on bit fields, but not on enumerations, to generate unnamed values.</span></span> <span data-ttu-id="d20ef-198">Výčty jsou obvykle používány k seznam jedinečných prvky, jako jsou například dny v týdnu, země nebo oblasti názvy a tak dále.</span><span class="sxs-lookup"><span data-stu-id="d20ef-198">Enumerations are generally used for lists of unique elements, such as days of the week, country or region names, and so on.</span></span> <span data-ttu-id="d20ef-199">Bitová pole jsou obecně používány k seznamy vlastností nebo množství, které můžou nastat v kombinaci, jako například `Red And Big And Fast`.</span><span class="sxs-lookup"><span data-stu-id="d20ef-199">Bit fields are generally used for lists of qualities or quantities that might occur in combination, such as `Red And Big And Fast`.</span></span>  
  
 <span data-ttu-id="d20ef-200">Následující příklad ukazuje způsob použití bitových polí a tradiční výčty.</span><span class="sxs-lookup"><span data-stu-id="d20ef-200">The following example shows how to use both bit fields and traditional enumerations.</span></span>  
  
 [!code-csharp[Conceptual.Types.Enum#1](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.types.enum/cs/example.cs#1)]
 [!code-vb[Conceptual.Types.Enum#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.types.enum/vb/example.vb#1)]  
  
<a name="Interfaces"></a>   
### <a name="interfaces"></a><span data-ttu-id="d20ef-201">Rozhraní</span><span class="sxs-lookup"><span data-stu-id="d20ef-201">Interfaces</span></span>  
 <span data-ttu-id="d20ef-202">Rozhraní definuje kontrakt, který určuje vztah "provést" nebo "má" relace.</span><span class="sxs-lookup"><span data-stu-id="d20ef-202">An interface defines a contract that specifies a "can do" relationship or a "has a" relationship.</span></span> <span data-ttu-id="d20ef-203">Rozhraní se často používají k implementaci funkcí, jako je například porovnávání a řazení ( <xref:System.IComparable> a <xref:System.IComparable%601> rozhraní), testování rovnosti ( <xref:System.IEquatable%601> rozhraní), nebo vytváření výčtu položky v kolekci ( <xref:System.Collections.IEnumerable> a <xref:System.Collections.Generic.IEnumerable%601> rozhraní).</span><span class="sxs-lookup"><span data-stu-id="d20ef-203">Interfaces are often used to implement functionality, such as comparing and sorting (the <xref:System.IComparable> and <xref:System.IComparable%601> interfaces), testing for equality (the <xref:System.IEquatable%601> interface), or enumerating items in a collection (the <xref:System.Collections.IEnumerable> and <xref:System.Collections.Generic.IEnumerable%601> interfaces).</span></span> <span data-ttu-id="d20ef-204">Rozhraní může mít vlastnosti, metod a události, které jsou abstraktní členy; To znamená i když rozhraní definuje členy a jejich podpisy, zůstanou ho typu, který implementuje rozhraní definovat funkci u každého člena rozhraní.</span><span class="sxs-lookup"><span data-stu-id="d20ef-204">Interfaces can have properties, methods, and events, all of which are abstract members; that is, although the interface defines the members and their signatures, it leaves it to the type that implements the interface to define the functionality of each interface member.</span></span> <span data-ttu-id="d20ef-205">To znamená, že všechny třídu nebo strukturu, která implementuje rozhraní musí poskytnout definici pro abstraktní členy deklarované v rozhraní.</span><span class="sxs-lookup"><span data-stu-id="d20ef-205">This means that any class or structure that implements an interface must supply definitions for the abstract members declared in the interface.</span></span> <span data-ttu-id="d20ef-206">Rozhraní může vyžadovat žádné implementující třídu nebo strukturu také implementovat jednu nebo více rozhraní.</span><span class="sxs-lookup"><span data-stu-id="d20ef-206">An interface can require any implementing class or structure to also implement one or more other interfaces.</span></span>  
  
 <span data-ttu-id="d20ef-207">Rozhraní se vztahují následující omezení:</span><span class="sxs-lookup"><span data-stu-id="d20ef-207">The following restrictions apply to interfaces:</span></span>  
  
-   <span data-ttu-id="d20ef-208">Rozhraní lze deklarovat s žádné usnadnění, ale všechny členy rozhraní musí mít veřejnou dostupnost.</span><span class="sxs-lookup"><span data-stu-id="d20ef-208">An interface can be declared with any accessibility, but interface members must all have public accessibility.</span></span>  
  
-   <span data-ttu-id="d20ef-209">Rozhraní nelze definovat konstruktory.</span><span class="sxs-lookup"><span data-stu-id="d20ef-209">Interfaces cannot define constructors.</span></span>  
  
-   <span data-ttu-id="d20ef-210">Rozhraní nelze definovat pole.</span><span class="sxs-lookup"><span data-stu-id="d20ef-210">Interfaces cannot define fields.</span></span>  
  
-   <span data-ttu-id="d20ef-211">Rozhraní můžete definovat jenom členy instancí.</span><span class="sxs-lookup"><span data-stu-id="d20ef-211">Interfaces can define only instance members.</span></span> <span data-ttu-id="d20ef-212">Nemohou definovat statické členy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-212">They cannot define static members.</span></span>  
  
 <span data-ttu-id="d20ef-213">Jednotlivé jazyky nutné zadat pravidla pro mapování implementace rozhraní, která vyžaduje člena, protože víc než jedno rozhraní můžou deklarovat člen se stejným podpisem a tito členové můžou mít samostatné implementace.</span><span class="sxs-lookup"><span data-stu-id="d20ef-213">Each language must provide rules for mapping an implementation to the interface that requires the member, because more than one interface can declare a member with the same signature, and these members can have separate implementations.</span></span>  
  
<a name="Delegates"></a>   
### <a name="delegates"></a><span data-ttu-id="d20ef-214">Delegáty</span><span class="sxs-lookup"><span data-stu-id="d20ef-214">Delegates</span></span>  
 <span data-ttu-id="d20ef-215">Delegáti jsou odkazové typy, které mají účely podobná ukazatelů na funkce v jazyce C++.</span><span class="sxs-lookup"><span data-stu-id="d20ef-215">Delegates are reference types that serve a purpose similar to that of function pointers in C++.</span></span> <span data-ttu-id="d20ef-216">Používají se pro obslužné rutiny událostí a funkce zpětného volání v rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="d20ef-216">They are used for event handlers and callback functions in .NET.</span></span> <span data-ttu-id="d20ef-217">Na rozdíl od ukazatelů na funkce Delegáti jsou zabezpečené, ověřit a zadejte bezpečné.</span><span class="sxs-lookup"><span data-stu-id="d20ef-217">Unlike function pointers, delegates are secure, verifiable, and type safe.</span></span> <span data-ttu-id="d20ef-218">Typem delegáta může představovat libovolnou metodu instance nebo statickou metodu, která má kompatibilní podpis.</span><span class="sxs-lookup"><span data-stu-id="d20ef-218">A delegate type can represent any instance method or static method that has a compatible signature.</span></span>  
  
 <span data-ttu-id="d20ef-219">Parametr delegáta je kompatibilní s odpovídajícího parametru metody, pokud je typ parametru delegáta více omezující než typ parametru metody, protože to zaručuje, že argument předaný delegát můžete předat bezpečně Metoda.</span><span class="sxs-lookup"><span data-stu-id="d20ef-219">A parameter of a delegate is compatible with the corresponding parameter of a method if the type of the delegate parameter is more restrictive than the type of the method parameter, because this guarantees that an argument passed to the delegate can be passed safely to the method.</span></span>  
  
 <span data-ttu-id="d20ef-220">Návratový typ delegáta je podobně kompatibilní s návratovým typem metody, pokud je návratový typ metody více omezující než návratový typ delegáta, protože to zaručuje, že návratová hodnota metody můžete bezpečně přetypovat na návratový typ e delegáta.</span><span class="sxs-lookup"><span data-stu-id="d20ef-220">Similarly, the return type of a delegate is compatible with the return type of a method if the return type of the method is more restrictive than the return type of the delegate, because this guarantees that the return value of the method can be cast safely to the return type of the delegate.</span></span>  
  
 <span data-ttu-id="d20ef-221">Například delegáta, který má parametr typu <xref:System.Collections.IEnumerable> a návratový typ <xref:System.Object> může představovat metodu, která má parametr typu <xref:System.Object> a návratovou hodnotu typu <xref:System.Collections.IEnumerable>.</span><span class="sxs-lookup"><span data-stu-id="d20ef-221">For example, a delegate that has a parameter of type <xref:System.Collections.IEnumerable> and a return type of <xref:System.Object> can represent a method that has a parameter of type <xref:System.Object> and a return value of type <xref:System.Collections.IEnumerable>.</span></span> <span data-ttu-id="d20ef-222">Další informace a ukázkový kód, najdete v části <xref:System.Delegate.CreateDelegate%28System.Type%2CSystem.Object%2CSystem.Reflection.MethodInfo%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="d20ef-222">For more information and example code, see <xref:System.Delegate.CreateDelegate%28System.Type%2CSystem.Object%2CSystem.Reflection.MethodInfo%29?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="d20ef-223">Delegát je označován bylo vázané na metodu, kterou představuje.</span><span class="sxs-lookup"><span data-stu-id="d20ef-223">A delegate is said to be bound to the method it represents.</span></span> <span data-ttu-id="d20ef-224">Kromě vázaná metodu delegáta může být vázána na objekt.</span><span class="sxs-lookup"><span data-stu-id="d20ef-224">In addition to being bound to the method, a delegate can be bound to an object.</span></span> <span data-ttu-id="d20ef-225">Objekt představuje první parametr metody a je předaný metodě pokaždé, když je vyvolán delegát.</span><span class="sxs-lookup"><span data-stu-id="d20ef-225">The object represents the first parameter of the method, and is passed to the method every time the delegate is invoked.</span></span> <span data-ttu-id="d20ef-226">Pokud je metoda metody instance, vázaný objekt je předán jako implicitní `this` parametr (`Me` v jazyce Visual Basic); Pokud metoda je statická, objekt je předán jako první formální parametr metody a delegáta musí odpovídat zbývající parametry.</span><span class="sxs-lookup"><span data-stu-id="d20ef-226">If the method is an instance method, the bound object is passed as the implicit `this` parameter (`Me` in Visual Basic); if the method is static, the object is passed as the first formal parameter of the method, and the delegate signature must match the remaining parameters.</span></span> <span data-ttu-id="d20ef-227">Další informace a ukázkový kód, najdete v části <xref:System.Delegate?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="d20ef-227">For more information and example code, see <xref:System.Delegate?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="d20ef-228">Všechny delegáti dědí z <xref:System.MulticastDelegate?displayProperty=nameWithType>, který dědí z <xref:System.Delegate?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="d20ef-228">All delegates inherit from <xref:System.MulticastDelegate?displayProperty=nameWithType>, which inherits from <xref:System.Delegate?displayProperty=nameWithType>.</span></span> <span data-ttu-id="d20ef-229">Jazyk C#, Visual Basic a C++ neumožňují dědičnost z těchto typů.</span><span class="sxs-lookup"><span data-stu-id="d20ef-229">The C#, Visual Basic, and C++ languages do not allow inheritance from these types.</span></span> <span data-ttu-id="d20ef-230">Místo toho poskytují klíčová slova pro deklarování delegáti.</span><span class="sxs-lookup"><span data-stu-id="d20ef-230">Instead, they provide keywords for declaring delegates.</span></span>  
  
 <span data-ttu-id="d20ef-231">Protože delegáti dědí z <xref:System.MulticastDelegate>, delegát má seznam vyvolání, což je seznam metod, které představuje delegáta a které jsou spouštěny při vyvolání delegáta.</span><span class="sxs-lookup"><span data-stu-id="d20ef-231">Because delegates inherit from <xref:System.MulticastDelegate>, a delegate has an invocation list, which is a list of methods that the delegate represents and that are executed when the delegate is invoked.</span></span> <span data-ttu-id="d20ef-232">Všechny metody v seznamu zobrazí zadané argumenty při vyvolání delegáta.</span><span class="sxs-lookup"><span data-stu-id="d20ef-232">All methods in the list receive the arguments supplied when the delegate is invoked.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="d20ef-233">Návratová hodnota není definován pro delegáta, který má více než jednu metodu ve svém seznamu volání i v případě, že má návratový typ delegáta.</span><span class="sxs-lookup"><span data-stu-id="d20ef-233">The return value is not defined for a delegate that has more than one method in its invocation list, even if the delegate has a return type.</span></span>  
  
 <span data-ttu-id="d20ef-234">V mnoha případech, jako je zpětné volání metod, delegát představuje pouze jednu metodu a pouze akce, které je třeba provést jsou vytváření delegáta a vyvolání ho.</span><span class="sxs-lookup"><span data-stu-id="d20ef-234">In many cases, such as with callback methods, a delegate represents only one method, and the only actions you have to take are creating the delegate and invoking it.</span></span>  
  
 <span data-ttu-id="d20ef-235">Pro delegáti, které představují několik metod, .NET poskytuje metody <xref:System.Delegate> a <xref:System.MulticastDelegate> delegovat třídy pro podporu operací, jako je například přidání metody do seznamu vyvolání tohoto delegáta ( <xref:System.Delegate.Combine%2A?displayProperty=nameWithType> metoda), odebrání metody ( <xref:System.Delegate.Remove%2A?displayProperty=nameWithType> metoda) a získání seznamu vyvolání ( <xref:System.Delegate.GetInvocationList%2A?displayProperty=nameWithType> metoda).</span><span class="sxs-lookup"><span data-stu-id="d20ef-235">For delegates that represent multiple methods, .NET provides methods of the <xref:System.Delegate> and <xref:System.MulticastDelegate> delegate classes to support operations such as adding a method to a delegate's invocation list (the <xref:System.Delegate.Combine%2A?displayProperty=nameWithType> method), removing a method (the <xref:System.Delegate.Remove%2A?displayProperty=nameWithType> method), and getting the invocation list (the <xref:System.Delegate.GetInvocationList%2A?displayProperty=nameWithType> method).</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="d20ef-236">Není potřeba použít tyto metody pro delegátů obslužných rutin událostí v jazyce C#, C++ a Visual Basic, protože tyto jazyky poskytují syntaxi pro přidávání a odebírání obslužných rutin událostí.</span><span class="sxs-lookup"><span data-stu-id="d20ef-236">It is not necessary to use these methods for event-handler delegates in C#, C++, and Visual Basic, because these languages provide syntax for adding and removing event handlers.</span></span>  
  
 
  
<a name="type_definitions"></a>   
## <a name="type-definitions"></a><span data-ttu-id="d20ef-237">Definice typů</span><span class="sxs-lookup"><span data-stu-id="d20ef-237">Type Definitions</span></span>  
 <span data-ttu-id="d20ef-238">Definice typu zahrnuje následující položky:</span><span class="sxs-lookup"><span data-stu-id="d20ef-238">A type definition includes the following:</span></span>  
  
-   <span data-ttu-id="d20ef-239">Všechny atributy definovanou u typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-239">Any attributes defined on the type.</span></span>  
  
-   <span data-ttu-id="d20ef-240">Přístupnost typu (přehled).</span><span class="sxs-lookup"><span data-stu-id="d20ef-240">The type's accessibility (visibility).</span></span>  
  
-   <span data-ttu-id="d20ef-241">Název typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-241">The type's name.</span></span>  
  
-   <span data-ttu-id="d20ef-242">Základní typ tohoto typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-242">The type's base type.</span></span>  
  
-   <span data-ttu-id="d20ef-243">Žádné rozhraní implementované typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-243">Any interfaces implemented by the type.</span></span>  
  
-   <span data-ttu-id="d20ef-244">Definice pro každý z členů tohoto typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-244">Definitions for each of the type's members.</span></span>  
  
### <a name="attributes"></a><span data-ttu-id="d20ef-245">Atributy</span><span class="sxs-lookup"><span data-stu-id="d20ef-245">Attributes</span></span>  
 <span data-ttu-id="d20ef-246">Atributy poskytují další metadata definovaná uživatelem.</span><span class="sxs-lookup"><span data-stu-id="d20ef-246">Attributes provide additional user-defined metadata.</span></span> <span data-ttu-id="d20ef-247">Nejčastěji se používají k ukládání Další informace o typu v jeho sestavení nebo chcete změnit chování člena typu buď návrhu nebo běhové prostředí.</span><span class="sxs-lookup"><span data-stu-id="d20ef-247">Most commonly, they are used to store additional information about a type in its assembly, or to modify the behavior of a type member in either the design-time or run-time environment.</span></span>  
  
 <span data-ttu-id="d20ef-248">Atributy jsou samotné třídy, které dědí od <xref:System.Attribute?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="d20ef-248">Attributes are themselves classes that inherit from <xref:System.Attribute?displayProperty=nameWithType>.</span></span> <span data-ttu-id="d20ef-249">Jazyky, které podporují použití atributů mají své vlastní syntaxe pro použití atributů do elementu jazyk.</span><span class="sxs-lookup"><span data-stu-id="d20ef-249">Languages that support the use of attributes each have their own syntax for applying attributes to a language element.</span></span> <span data-ttu-id="d20ef-250">Atributy lze použít jako prakticky libovolný element jazyka. konkrétní elementy, na které můžete použít atribut jsou definované <xref:System.AttributeUsageAttribute> který se použije pro danou třídu atributů.</span><span class="sxs-lookup"><span data-stu-id="d20ef-250">Attributes can be applied to almost any language element; the specific elements to which an attribute can be applied are defined by the <xref:System.AttributeUsageAttribute> that is applied to that attribute class.</span></span>  
  
### <a name="type-accessibility"></a><span data-ttu-id="d20ef-251">Typ usnadnění</span><span class="sxs-lookup"><span data-stu-id="d20ef-251">Type Accessibility</span></span>  
 <span data-ttu-id="d20ef-252">Všechny typy mají modifikátor, který řídí jejich usnadnění od ostatních typů.</span><span class="sxs-lookup"><span data-stu-id="d20ef-252">All types have a modifier that governs their accessibility from other types.</span></span> <span data-ttu-id="d20ef-253">Následující tabulka popisuje přístupnosti typu podporovaná modulem runtime.</span><span class="sxs-lookup"><span data-stu-id="d20ef-253">The following table describes the type accessibilities supported by the runtime.</span></span>  
  
|<span data-ttu-id="d20ef-254">Usnadnění</span><span class="sxs-lookup"><span data-stu-id="d20ef-254">Accessibility</span></span>|<span data-ttu-id="d20ef-255">Popis</span><span class="sxs-lookup"><span data-stu-id="d20ef-255">Description</span></span>|  
|-------------------|-----------------|  
|<span data-ttu-id="d20ef-256">public</span><span class="sxs-lookup"><span data-stu-id="d20ef-256">public</span></span>|<span data-ttu-id="d20ef-257">Typ je přístupný pro všechny sestavení.</span><span class="sxs-lookup"><span data-stu-id="d20ef-257">The type is accessible by all assemblies.</span></span>|  
|<span data-ttu-id="d20ef-258">sestavení</span><span class="sxs-lookup"><span data-stu-id="d20ef-258">assembly</span></span>|<span data-ttu-id="d20ef-259">Typ je přístupné pouze v rámci jeho sestavení.</span><span class="sxs-lookup"><span data-stu-id="d20ef-259">The type is accessible only from within its assembly.</span></span>|  
  
 <span data-ttu-id="d20ef-260">Usnadnění vnořeného typu závisí na jeho doména přístupnosti, což je dáno tím deklarované usnadnění člena a doména přístupnosti okamžitě nadřazeného typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-260">The accessibility of a nested type depends on its accessibility domain, which is determined by both the declared accessibility of the member and the accessibility domain of the immediately containing type.</span></span> <span data-ttu-id="d20ef-261">Doména přístupnosti vnořeného typu však nesmí překročit u nadřazeného typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-261">However, the accessibility domain of a nested type cannot exceed that of the containing type.</span></span>  
  
 <span data-ttu-id="d20ef-262">Doména přístupnosti vnořeného člena `M` deklarovaného v typu `T` v rámci programu `P` je definován následujícím způsobem (který poznamenat `M` může být sám typ):</span><span class="sxs-lookup"><span data-stu-id="d20ef-262">The accessibility domain of a nested member `M` declared in a type `T` within a program `P` is defined as follows (noting that `M` might itself be a type):</span></span>  
  
-   <span data-ttu-id="d20ef-263">Pokud je deklarovaná přístupnost člena `M` je `public`, doména přístupnosti `M` je doména přístupnosti `T`.</span><span class="sxs-lookup"><span data-stu-id="d20ef-263">If the declared accessibility of `M` is `public`, the accessibility domain of `M` is the accessibility domain of `T`.</span></span>  
  
-   <span data-ttu-id="d20ef-264">Pokud je deklarovaná přístupnost člena `M` je `protected internal`, doména přístupnosti `M` je průnik doména přístupnosti `T` s textem programu `P` a textem programu jakýkoli typ odvozený z `T` deklarovaný mimo `P`.</span><span class="sxs-lookup"><span data-stu-id="d20ef-264">If the declared accessibility of `M` is `protected internal`, the accessibility domain of `M` is the intersection of the accessibility domain of `T` with the program text of `P` and the program text of any type derived from `T` declared outside `P`.</span></span>  
  
-   <span data-ttu-id="d20ef-265">Pokud je deklarovaná přístupnost člena `M` je `protected`, doména přístupnosti `M` je průnik doména přístupnosti `T` s textem programu `T` a jakéhokoli typu odvozeného z `T`.</span><span class="sxs-lookup"><span data-stu-id="d20ef-265">If the declared accessibility of `M` is `protected`, the accessibility domain of `M` is the intersection of the accessibility domain of `T` with the program text of `T` and any type derived from `T`.</span></span>  
  
-   <span data-ttu-id="d20ef-266">Pokud je deklarovaná přístupnost člena `M` je `internal`, doména přístupnosti `M` je průnik doména přístupnosti `T` s textem programu `P`.</span><span class="sxs-lookup"><span data-stu-id="d20ef-266">If the declared accessibility of `M` is `internal`, the accessibility domain of `M` is the intersection of the accessibility domain of `T` with the program text of `P`.</span></span>  
  
-   <span data-ttu-id="d20ef-267">Pokud je deklarovaná přístupnost člena `M` je `private`, doména přístupnosti `M` je text programu `T`.</span><span class="sxs-lookup"><span data-stu-id="d20ef-267">If the declared accessibility of `M` is `private`, the accessibility domain of `M` is the program text of `T`.</span></span>  
  
### <a name="type-names"></a><span data-ttu-id="d20ef-268">Názvy typů</span><span class="sxs-lookup"><span data-stu-id="d20ef-268">Type Names</span></span>  
 <span data-ttu-id="d20ef-269">Obecný systém typů vynucuje pouze dvě omezení na názvy:</span><span class="sxs-lookup"><span data-stu-id="d20ef-269">The common type system imposes only two restrictions on names:</span></span>  
  
-   <span data-ttu-id="d20ef-270">Všechny názvy jsou zakódovány jako řetězce znaků Unicode (16 bitů).</span><span class="sxs-lookup"><span data-stu-id="d20ef-270">All names are encoded as strings of Unicode (16-bit) characters.</span></span>  
  
-   <span data-ttu-id="d20ef-271">Názvy nejsou povolené tak, aby měl hodnotu embedded (16 bitů) 0x0000.</span><span class="sxs-lookup"><span data-stu-id="d20ef-271">Names are not permitted to have an embedded (16-bit) value of 0x0000.</span></span>  
  
 <span data-ttu-id="d20ef-272">Většina jazyků však zavádí další omezení pro názvy typů.</span><span class="sxs-lookup"><span data-stu-id="d20ef-272">However, most languages impose additional restrictions on type names.</span></span> <span data-ttu-id="d20ef-273">Všechny porovnání se provádí na základě bajtové byte a proto jsou velká a malá písmena a nezávislé na národním prostředí.</span><span class="sxs-lookup"><span data-stu-id="d20ef-273">All comparisons are done on a byte-by-byte basis, and are therefore case-sensitive and locale-independent.</span></span>  
  
 <span data-ttu-id="d20ef-274">I když typu může odkazové typy z jiných moduly a sestavení, musí být typu plně definována v rámci jednoho modulu .NET.</span><span class="sxs-lookup"><span data-stu-id="d20ef-274">Although a type might reference types from other modules and assemblies, a type must be fully defined within one .NET module.</span></span> <span data-ttu-id="d20ef-275">(V závislosti na podpoře kompilátoru však ho lze rozdělit do několika soubory zdrojového kódu.) Názvy typů musí být jedinečný pouze v rámci oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="d20ef-275">(Depending on compiler support, however, it can be divided into multiple source code files.) Type names need be unique only within a namespace.</span></span> <span data-ttu-id="d20ef-276">Pokud chcete plně identifikovat typu, musí být kvalifikovaný název typu obor názvů, který obsahuje implementaci typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-276">To fully identify a type, the type name must be qualified by the namespace that contains the implementation of the type.</span></span>  
  
### <a name="base-types-and-interfaces"></a><span data-ttu-id="d20ef-277">Základní typy a rozhraní</span><span class="sxs-lookup"><span data-stu-id="d20ef-277">Base Types and Interfaces</span></span>  
 <span data-ttu-id="d20ef-278">Typ může dědit vlastnosti hodnoty a chování z jiného typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-278">A type can inherit values and behaviors from another type.</span></span> <span data-ttu-id="d20ef-279">Obecný systém typů nepovoluje typy dědění z více než jeden základního typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-279">The common type system does not allow types to inherit from more than one base type.</span></span>  
  
 <span data-ttu-id="d20ef-280">Typ můžete implementovat libovolný počet rozhraní.</span><span class="sxs-lookup"><span data-stu-id="d20ef-280">A type can implement any number of interfaces.</span></span> <span data-ttu-id="d20ef-281">Pokud chcete implementovat rozhraní, musí typ implementovat virtuální členy rozhraní.</span><span class="sxs-lookup"><span data-stu-id="d20ef-281">To implement an interface, a type must implement all the virtual members of that interface.</span></span> <span data-ttu-id="d20ef-282">Virtuální metoda může být implementováno odvozeným typem a vyvolána staticky nebo dynamicky.</span><span class="sxs-lookup"><span data-stu-id="d20ef-282">A virtual method can be implemented by a derived type and can be invoked either statically or dynamically.</span></span>  
  
  
  
<a name="type_members"></a>   
## <a name="type-members"></a><span data-ttu-id="d20ef-283">Členy typu</span><span class="sxs-lookup"><span data-stu-id="d20ef-283">Type Members</span></span>  
 <span data-ttu-id="d20ef-284">Modul runtime umožňuje definovat členy vaší typu, který určuje chování a stav tohoto typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-284">The runtime enables you to define members of your type, which specifies the behavior and state of a type.</span></span> <span data-ttu-id="d20ef-285">Členy typu zahrnují následující:</span><span class="sxs-lookup"><span data-stu-id="d20ef-285">Type members include the following:</span></span>  
  
-   [<span data-ttu-id="d20ef-286">Pole</span><span class="sxs-lookup"><span data-stu-id="d20ef-286">Fields</span></span>](#Fields)  
  
-   [<span data-ttu-id="d20ef-287">Vlastnosti</span><span class="sxs-lookup"><span data-stu-id="d20ef-287">Properties</span></span>](#Properties)  
  
-   [<span data-ttu-id="d20ef-288">Metody</span><span class="sxs-lookup"><span data-stu-id="d20ef-288">Methods</span></span>](#Methods)  
  
-   [<span data-ttu-id="d20ef-289">Konstruktory</span><span class="sxs-lookup"><span data-stu-id="d20ef-289">Constructors</span></span>](#Constructors)  
  
-   [<span data-ttu-id="d20ef-290">Události</span><span class="sxs-lookup"><span data-stu-id="d20ef-290">Events</span></span>](#Events)  
  
-   [<span data-ttu-id="d20ef-291">Vnořené typy</span><span class="sxs-lookup"><span data-stu-id="d20ef-291">Nested types</span></span>](#NestedTypes)  
  
<a name="Fields"></a>   
### <a name="fields"></a><span data-ttu-id="d20ef-292">Pole</span><span class="sxs-lookup"><span data-stu-id="d20ef-292">Fields</span></span>  
 <span data-ttu-id="d20ef-293">Pole popisuje a obsahuje část stavu typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-293">A field describes and contains part of the type's state.</span></span> <span data-ttu-id="d20ef-294">Pole můžou být jakéhokoli typu podporovaná modulem runtime.</span><span class="sxs-lookup"><span data-stu-id="d20ef-294">Fields can be of any type supported by the runtime.</span></span> <span data-ttu-id="d20ef-295">Nejčastěji, jsou pole buď `private` nebo `protected`, takže budou přístupné pouze v rámci třídy nebo z odvozené třídy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-295">Most commonly, fields are either `private` or `protected`, so that they are accessible only from within the class or from a derived class.</span></span> <span data-ttu-id="d20ef-296">Pokud hodnotu pole může být změněna mimo její typ, použije se obvykle vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="d20ef-296">If the value of a field can be modified from outside its type, a property set accessor is typically used.</span></span> <span data-ttu-id="d20ef-297">Veřejně vystavené položky jsou obvykle jen pro čtení a může mít dva typy:</span><span class="sxs-lookup"><span data-stu-id="d20ef-297">Publicly exposed fields are usually read-only and can be of two types:</span></span>  
  
-   <span data-ttu-id="d20ef-298">Konstanty, jehož hodnota je přiřazena v době návrhu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-298">Constants, whose value is assigned at design time.</span></span> <span data-ttu-id="d20ef-299">Tyto jsou statické členy třídy, i když nejsou definovány pomocí `static` (`Shared` v jazyce Visual Basic) – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="d20ef-299">These are static members of a class, although they are not defined using the `static` (`Shared` in Visual Basic) keyword.</span></span>  
  
-   <span data-ttu-id="d20ef-300">Jen pro čtení proměnné, jejichž hodnoty lze přiřadit v konstruktoru třídy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-300">Read-only variables, whose values can be assigned in the class constructor.</span></span>  
  
 <span data-ttu-id="d20ef-301">Následující příklad znázorňuje tyto dvě použití pole jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="d20ef-301">The following example illustrates these two usages of read-only fields.</span></span>  
  
 [!code-csharp[Conceptual.Types.Members.Fields#1](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.types.members.fields/cs/example.cs#1)]
 [!code-vb[Conceptual.Types.Members.Fields#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.types.members.fields/vb/example.vb#1)]  
  
<a name="Properties"></a>   
### <a name="properties"></a><span data-ttu-id="d20ef-302">Vlastnosti</span><span class="sxs-lookup"><span data-stu-id="d20ef-302">Properties</span></span>  
 <span data-ttu-id="d20ef-303">Vlastnost názvy hodnota nebo stavu typu a definuje metody pro získání nebo nastavení hodnoty vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="d20ef-303">A property names a value or state of the type and defines methods for getting or setting the property's value.</span></span> <span data-ttu-id="d20ef-304">Vlastnosti mohou být primitivní typy, kolekce primitivní typy, uživatelem definované typy nebo kolekce uživatelem definované typy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-304">Properties can be primitive types, collections of primitive types, user-defined types, or collections of user-defined types.</span></span> <span data-ttu-id="d20ef-305">Vlastnosti se často používají k udržování nezávislé veřejné rozhraní typu z tohoto typu skutečný.</span><span class="sxs-lookup"><span data-stu-id="d20ef-305">Properties are often used to keep the public interface of a type independent from the type's actual representation.</span></span> <span data-ttu-id="d20ef-306">To umožňuje vlastnosti tak, aby odrážela hodnoty, které nejsou přímo uložené ve třídě (například když vlastnost vrací vypočtenou hodnotou) nebo provést ověření, než k privátním polím přiřazené hodnoty.</span><span class="sxs-lookup"><span data-stu-id="d20ef-306">This enables properties to reflect values that are not directly stored in the class (for example, when a property returns a computed value) or to perform validation before values are assigned to private fields.</span></span> <span data-ttu-id="d20ef-307">Následující příklad znázorňuje druhý případ.</span><span class="sxs-lookup"><span data-stu-id="d20ef-307">The following example illustrates the latter pattern.</span></span>  
  
 [!code-csharp[Conceptual.Types.Members.Properties#1](../../../samples/snippets/csharp/VS_Snippets_CLR/conceptual.types.members.properties/cs/example.cs#1)]
 [!code-vb[Conceptual.Types.Members.Properties#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/conceptual.types.members.properties/vb/example.vb#1)]  
  
 <span data-ttu-id="d20ef-308">Kromě samotné vlastnosti, včetně Microsoft (MSIL intermediate language) pro typ, který obsahuje čitelnou vlastnost zahrnuje `get_` *propertyname* metoda a MSIL pro typ, který obsahuje možností zápisu obsahuje vlastnost `set_` *propertyname* metoda.</span><span class="sxs-lookup"><span data-stu-id="d20ef-308">In addition to including the property itself, the Microsoft intermediate language (MSIL) for a type that contains a readable property includes a `get_`*propertyname* method, and the MSIL for a type that contains a writable property includes a `set_`*propertyname* method.</span></span>  
  
<a name="Methods"></a>   
### <a name="methods"></a><span data-ttu-id="d20ef-309">Metody</span><span class="sxs-lookup"><span data-stu-id="d20ef-309">Methods</span></span>  
 <span data-ttu-id="d20ef-310">Metoda popisuje operace, které jsou k dispozici u typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-310">A method describes operations that are available on the type.</span></span> <span data-ttu-id="d20ef-311">Metoda podpis určuje povolené typy všechny jeho parametrů a hodnoty.</span><span class="sxs-lookup"><span data-stu-id="d20ef-311">A method's signature specifies the allowable types of all its parameters and of its return value.</span></span>  
  
 <span data-ttu-id="d20ef-312">I když většina metody definovat přesné počet parametrů požadovaných pro volání metod, některé metody podporují proměnný počet parametrů.</span><span class="sxs-lookup"><span data-stu-id="d20ef-312">Although most methods define the precise number of parameters required for method calls, some methods support a variable number of parameters.</span></span> <span data-ttu-id="d20ef-313">Výsledný deklarovaný parametr těchto metod je označené jako <xref:System.ParamArrayAttribute> atribut.</span><span class="sxs-lookup"><span data-stu-id="d20ef-313">The final declared parameter of these methods is marked with the <xref:System.ParamArrayAttribute> attribute.</span></span> <span data-ttu-id="d20ef-314">Kompilátory jazyka obvykle poskytují klíčového slova, například `params` v jazyce C# a `ParamArray` v jazyce Visual Basic, který zpřístupňuje explicitní použití <xref:System.ParamArrayAttribute> zbytečné.</span><span class="sxs-lookup"><span data-stu-id="d20ef-314">Language compilers typically provide a keyword, such as `params` in C# and `ParamArray` in Visual Basic, that makes explicit use of <xref:System.ParamArrayAttribute> unnecessary.</span></span>  
  
<a name="Constructors"></a>   
### <a name="constructors"></a><span data-ttu-id="d20ef-315">Konstruktory</span><span class="sxs-lookup"><span data-stu-id="d20ef-315">Constructors</span></span>  
 <span data-ttu-id="d20ef-316">Konstruktor je zvláštní druh metodu, která vytvoří nové instance třídy třídu nebo strukturu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-316">A constructor is a special kind of method that creates new instances of a class or structure.</span></span> <span data-ttu-id="d20ef-317">Podobně jako jakékoli jiné metody konstruktor může obsahovat parametry; ale konstruktory nemají žádnou návratovou hodnotu (to znamená, že budou vracet `void`).</span><span class="sxs-lookup"><span data-stu-id="d20ef-317">Like any other method, a constructor can include parameters; however, constructors have no return value (that is, they return `void`).</span></span>  
  
 <span data-ttu-id="d20ef-318">Pokud zdrojový kód pro třídu explicitně nedefinuje konstruktor, kompilátor obsahuje výchozí konstruktor (bez parametrů).</span><span class="sxs-lookup"><span data-stu-id="d20ef-318">If the source code for a class does not explicitly define a constructor, the compiler includes a default (parameterless) constructor.</span></span> <span data-ttu-id="d20ef-319">Ale pokud zdrojový kód pro třídu definuje pouze parametrizované konstruktory, kompilátory jazyka Visual Basic a C# negenerují konstruktor bez parametrů.</span><span class="sxs-lookup"><span data-stu-id="d20ef-319">However, if the source code for a class defines only parameterized constructors, the Visual Basic and C# compilers do not generate a parameterless constructor.</span></span>  
  
 <span data-ttu-id="d20ef-320">Pokud zdrojový kód pro strukturu definuje konstruktory, musí být parametry; struktura nemůže definovat výchozí konstruktor (bez parametrů) a negenerují bezparametrové konstruktory struktury nebo jiné typy hodnot.</span><span class="sxs-lookup"><span data-stu-id="d20ef-320">If the source code for a structure defines constructors, they must be parameterized; a structure cannot define a default (parameterless) constructor, and compilers do not generate parameterless constructors for structures or other value types.</span></span> <span data-ttu-id="d20ef-321">Všechny typy hodnot mají implicitní výchozí konstruktor.</span><span class="sxs-lookup"><span data-stu-id="d20ef-321">All value types do have an implicit default constructor.</span></span> <span data-ttu-id="d20ef-322">Tento konstruktor je implementováno modulem modul common language runtime a inicializuje všechna pole na výchozí hodnoty strukturu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-322">This constructor is implemented by the common language runtime and initializes all fields of the structure to their default values.</span></span>  
  
<a name="Events"></a>   
### <a name="events"></a><span data-ttu-id="d20ef-323">Události</span><span class="sxs-lookup"><span data-stu-id="d20ef-323">Events</span></span>  
 <span data-ttu-id="d20ef-324">Událost definuje incident, který může být odpověděl a definuje metody pro přihlášení k odběru, Odhlašujete a vyvolá událost.</span><span class="sxs-lookup"><span data-stu-id="d20ef-324">An event defines an incident that can be responded to, and defines methods for subscribing to, unsubscribing from, and raising the event.</span></span> <span data-ttu-id="d20ef-325">Události se často používají k informování jiné typy změny stavu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-325">Events are often used to inform other types of state changes.</span></span> <span data-ttu-id="d20ef-326">Další informace najdete v tématu [události](../../../docs/standard/events/index.md).</span><span class="sxs-lookup"><span data-stu-id="d20ef-326">For more information, see [Events](../../../docs/standard/events/index.md).</span></span>  
  
<a name="NestedTypes"></a>   
### <a name="nested-types"></a><span data-ttu-id="d20ef-327">Vnořené typy</span><span class="sxs-lookup"><span data-stu-id="d20ef-327">Nested Types</span></span>  
 <span data-ttu-id="d20ef-328">Vnořené typ je typ, který je členem některé jiné typy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-328">A nested type is a type that is a member of some other type.</span></span> <span data-ttu-id="d20ef-329">Vnořené typy by měla být úzce spojeny s jejich obsahující typ a nesmí být užitečné jako typ pro obecné účely.</span><span class="sxs-lookup"><span data-stu-id="d20ef-329">Nested types should be tightly coupled to their containing type and must not be useful as a general-purpose type.</span></span> <span data-ttu-id="d20ef-330">Vnořené typy jsou užitečné, když deklarující typ používá a vytváří instance vnořené typy a použití vnořených typu není viditelné ve veřejné členy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-330">Nested types are useful when the declaring type uses and creates instances of the nested type, and use of the nested type is not exposed in public members.</span></span>  
  
 <span data-ttu-id="d20ef-331">Vnořené typy jsou pro někteří vývojáři matoucí a nesmí být veřejně viditelný, pokud nejsou přesvědčivý důvod pro viditelnosti.</span><span class="sxs-lookup"><span data-stu-id="d20ef-331">Nested types are confusing to some developers and should not be publicly visible unless there is a compelling reason for visibility.</span></span> <span data-ttu-id="d20ef-332">V knihovně dobře navrženou by vývojáři obvykle nemusí použít vnořené typy pro vytvoření instancí objektů nebo deklarujte proměnné.</span><span class="sxs-lookup"><span data-stu-id="d20ef-332">In a well-designed library, developers should rarely have to use nested types to instantiate objects or declare variables.</span></span>  
  
  
  
<a name="characteristics_of_type_members"></a>   
## <a name="characteristics-of-type-members"></a><span data-ttu-id="d20ef-333">Charakteristika členy typu</span><span class="sxs-lookup"><span data-stu-id="d20ef-333">Characteristics of Type Members</span></span>  
 <span data-ttu-id="d20ef-334">Obecný systém typů umožňuje členům typu mít různé vlastnosti; jazyky však nemusí podporovat všechny tyto vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="d20ef-334">The common type system allows type members to have a variety of characteristics; however, languages are not required to support all these characteristics.</span></span> <span data-ttu-id="d20ef-335">Následující tabulka popisuje vlastnosti členů.</span><span class="sxs-lookup"><span data-stu-id="d20ef-335">The following table describes member characteristics.</span></span>  
  
|<span data-ttu-id="d20ef-336">Vlastnosti</span><span class="sxs-lookup"><span data-stu-id="d20ef-336">Characteristic</span></span>|<span data-ttu-id="d20ef-337">Můžete použít pro</span><span class="sxs-lookup"><span data-stu-id="d20ef-337">Can apply to</span></span>|<span data-ttu-id="d20ef-338">Popis</span><span class="sxs-lookup"><span data-stu-id="d20ef-338">Description</span></span>|  
|--------------------|------------------|-----------------|  
|<span data-ttu-id="d20ef-339">abstract</span><span class="sxs-lookup"><span data-stu-id="d20ef-339">abstract</span></span>|<span data-ttu-id="d20ef-340">Metody, vlastnosti a události</span><span class="sxs-lookup"><span data-stu-id="d20ef-340">Methods, properties, and events</span></span>|<span data-ttu-id="d20ef-341">Typ neposkytuje implementaci metody.</span><span class="sxs-lookup"><span data-stu-id="d20ef-341">The type does not supply the method's implementation.</span></span> <span data-ttu-id="d20ef-342">Typy, které dědí nebo implementují abstraktní metody musí poskytnout implementaci pro metodu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-342">Types that inherit or implement abstract methods must supply an implementation for the method.</span></span> <span data-ttu-id="d20ef-343">Jedinou výjimkou je, když odvozený typ je sám typu abstraktní.</span><span class="sxs-lookup"><span data-stu-id="d20ef-343">The only exception is when the derived type is itself an abstract type.</span></span> <span data-ttu-id="d20ef-344">Všechny abstraktní metody jsou virtuální.</span><span class="sxs-lookup"><span data-stu-id="d20ef-344">All abstract methods are virtual.</span></span>|  
|<span data-ttu-id="d20ef-345">privátní, rodiny, sestavení, rodiny a sestavení, rodina nebo sestavení nebo veřejné</span><span class="sxs-lookup"><span data-stu-id="d20ef-345">private, family, assembly, family and assembly, family or assembly, or public</span></span>|<span data-ttu-id="d20ef-346">Všechny</span><span class="sxs-lookup"><span data-stu-id="d20ef-346">All</span></span>|<span data-ttu-id="d20ef-347">Definuje usnadnění člena:</span><span class="sxs-lookup"><span data-stu-id="d20ef-347">Defines the accessibility of the member:</span></span><br /><br /> <span data-ttu-id="d20ef-348">private</span><span class="sxs-lookup"><span data-stu-id="d20ef-348">private</span></span><br /> <span data-ttu-id="d20ef-349">Přístupné pouze v rámci stejného typu jako člen, nebo v rámci vnořené typy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-349">Accessible only from within the same type as the member, or within a nested type.</span></span><br /><br /> <span data-ttu-id="d20ef-350">Rodina</span><span class="sxs-lookup"><span data-stu-id="d20ef-350">family</span></span><br /> <span data-ttu-id="d20ef-351">Je dostupný z v rámci stejného typu jako člen a odvozených typů, které dědí z něj.</span><span class="sxs-lookup"><span data-stu-id="d20ef-351">Accessible from within the same type as the member, and from derived types that inherit from it.</span></span><br /><br /> <span data-ttu-id="d20ef-352">sestavení</span><span class="sxs-lookup"><span data-stu-id="d20ef-352">assembly</span></span><br /> <span data-ttu-id="d20ef-353">Přístupné pouze v sestavení, ve kterém je definovaný typ.</span><span class="sxs-lookup"><span data-stu-id="d20ef-353">Accessible only in the assembly in which the type is defined.</span></span><br /><br /> <span data-ttu-id="d20ef-354">Rodina a sestavení</span><span class="sxs-lookup"><span data-stu-id="d20ef-354">family and assembly</span></span><br /> <span data-ttu-id="d20ef-355">Přístupné pouze typy, které jsou způsobilé pro přístup k řadě a sestavení.</span><span class="sxs-lookup"><span data-stu-id="d20ef-355">Accessible only from types that qualify for both family and assembly access.</span></span><br /><br /> <span data-ttu-id="d20ef-356">třídu nebo sestavení</span><span class="sxs-lookup"><span data-stu-id="d20ef-356">family or assembly</span></span><br /> <span data-ttu-id="d20ef-357">Přístupné pouze typy, které jsou způsobilé pro přístup k třídu nebo sestavení.</span><span class="sxs-lookup"><span data-stu-id="d20ef-357">Accessible only from types that qualify for either family or assembly access.</span></span><br /><br /> <span data-ttu-id="d20ef-358">public</span><span class="sxs-lookup"><span data-stu-id="d20ef-358">public</span></span><br /> <span data-ttu-id="d20ef-359">Přístupná z libovolného typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-359">Accessible from any type.</span></span>|  
|<span data-ttu-id="d20ef-360">finální</span><span class="sxs-lookup"><span data-stu-id="d20ef-360">final</span></span>|<span data-ttu-id="d20ef-361">Metody, vlastnosti a události</span><span class="sxs-lookup"><span data-stu-id="d20ef-361">Methods, properties, and events</span></span>|<span data-ttu-id="d20ef-362">Virtuální metody nelze přepsat v odvozeném typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-362">The virtual method cannot be overridden in a derived type.</span></span>|  
|<span data-ttu-id="d20ef-363">pouze inicializovat</span><span class="sxs-lookup"><span data-stu-id="d20ef-363">initialize-only</span></span>|<span data-ttu-id="d20ef-364">Pole</span><span class="sxs-lookup"><span data-stu-id="d20ef-364">Fields</span></span>|<span data-ttu-id="d20ef-365">Hodnota může být pouze inicializována a nelze zapsat po inicializaci.</span><span class="sxs-lookup"><span data-stu-id="d20ef-365">The value can only be initialized, and cannot be written after initialization.</span></span>|  
|<span data-ttu-id="d20ef-366">Instance</span><span class="sxs-lookup"><span data-stu-id="d20ef-366">instance</span></span>|<span data-ttu-id="d20ef-367">Pole, metody, vlastnosti a události</span><span class="sxs-lookup"><span data-stu-id="d20ef-367">Fields, methods, properties, and events</span></span>|<span data-ttu-id="d20ef-368">Pokud člen není označen jako `static` (C# a C++), `Shared` (Visual Basic), `virtual` (C# a C++), nebo `Overridable` (Visual Basic), je členem instance (není žádné klíčové slovo instance).</span><span class="sxs-lookup"><span data-stu-id="d20ef-368">If a member is not marked as `static` (C# and C++), `Shared` (Visual Basic), `virtual` (C# and C++), or `Overridable` (Visual Basic), it is an instance member (there is no instance keyword).</span></span> <span data-ttu-id="d20ef-369">Budou existovat libovolný počet kopií takové členy v paměti jsou objekty, které ho používají.</span><span class="sxs-lookup"><span data-stu-id="d20ef-369">There will be as many copies of such members in memory as there are objects that use it.</span></span>|  
|<span data-ttu-id="d20ef-370">literál</span><span class="sxs-lookup"><span data-stu-id="d20ef-370">literal</span></span>|<span data-ttu-id="d20ef-371">Pole</span><span class="sxs-lookup"><span data-stu-id="d20ef-371">Fields</span></span>|<span data-ttu-id="d20ef-372">Hodnota přiřazená k poli je pevná hodnota, známé v době kompilace typu předdefinované hodnoty.</span><span class="sxs-lookup"><span data-stu-id="d20ef-372">The value assigned to the field is a fixed value, known at compile time, of a built-in value type.</span></span> <span data-ttu-id="d20ef-373">Literál pole jsou někdy označovány jako konstanty.</span><span class="sxs-lookup"><span data-stu-id="d20ef-373">Literal fields are sometimes referred to as constants.</span></span>|  
|<span data-ttu-id="d20ef-374">newslot nebo přepsání</span><span class="sxs-lookup"><span data-stu-id="d20ef-374">newslot or override</span></span>|<span data-ttu-id="d20ef-375">Všechny</span><span class="sxs-lookup"><span data-stu-id="d20ef-375">All</span></span>|<span data-ttu-id="d20ef-376">Definuje, jak člen komunikuje s zděděné členy, které mají stejný podpis:</span><span class="sxs-lookup"><span data-stu-id="d20ef-376">Defines how the member interacts with inherited members that have the same signature:</span></span><br /><br /> <span data-ttu-id="d20ef-377">newslot</span><span class="sxs-lookup"><span data-stu-id="d20ef-377">newslot</span></span><br /> <span data-ttu-id="d20ef-378">Skryje zděděné členy, kteří mají stejným podpisem.</span><span class="sxs-lookup"><span data-stu-id="d20ef-378">Hides inherited members that have the same signature.</span></span><br /><br /> <span data-ttu-id="d20ef-379">override</span><span class="sxs-lookup"><span data-stu-id="d20ef-379">override</span></span><br /> <span data-ttu-id="d20ef-380">Nahradí definici zděděné virtuální metody.</span><span class="sxs-lookup"><span data-stu-id="d20ef-380">Replaces the definition of an inherited virtual method.</span></span><br /><br /> <span data-ttu-id="d20ef-381">Výchozí hodnota je newslot.</span><span class="sxs-lookup"><span data-stu-id="d20ef-381">The default is newslot.</span></span>|  
|<span data-ttu-id="d20ef-382">static</span><span class="sxs-lookup"><span data-stu-id="d20ef-382">static</span></span>|<span data-ttu-id="d20ef-383">Pole, metody, vlastnosti a události</span><span class="sxs-lookup"><span data-stu-id="d20ef-383">Fields, methods, properties, and events</span></span>|<span data-ttu-id="d20ef-384">Člen patří typu, který je definován, není pro konkrétní instanci typu; člena existuje, i když není vytvořená instance typu a je sdílena mezi všechny instance typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-384">The member belongs to the type it is defined on, not to a particular instance of the type; the member exists even if an instance of the type is not created, and it is shared among all instances of the type.</span></span>|  
|<span data-ttu-id="d20ef-385">virtual</span><span class="sxs-lookup"><span data-stu-id="d20ef-385">virtual</span></span>|<span data-ttu-id="d20ef-386">Metody, vlastnosti a události</span><span class="sxs-lookup"><span data-stu-id="d20ef-386">Methods, properties, and events</span></span>|<span data-ttu-id="d20ef-387">Metoda může být implementováno odvozeným typem a vyvolána staticky nebo dynamicky.</span><span class="sxs-lookup"><span data-stu-id="d20ef-387">The method can be implemented by a derived type and can be invoked either statically or dynamically.</span></span> <span data-ttu-id="d20ef-388">Pokud je použito dynamické vyvolání, typ instance, která provádí volání v době běhu (spíše než v době kompilace známý typ) určuje, které implementace metoda je volána.</span><span class="sxs-lookup"><span data-stu-id="d20ef-388">If dynamic invocation is used, the type of the instance that makes the call at run time (rather than the type known at compile time) determines which implementation of the method is called.</span></span> <span data-ttu-id="d20ef-389">Pro vyvolání virtuální metody staticky, možná muset proměnnou přetypovat na typ, který používá požadovanou verzi metody.</span><span class="sxs-lookup"><span data-stu-id="d20ef-389">To invoke a virtual method statically, the variable might have to be cast to a type that uses the desired version of the method.</span></span>|  
  
### <a name="overloading"></a><span data-ttu-id="d20ef-390">Přetížení</span><span class="sxs-lookup"><span data-stu-id="d20ef-390">Overloading</span></span>  
 <span data-ttu-id="d20ef-391">Každý typ člen má jedinečný podpis.</span><span class="sxs-lookup"><span data-stu-id="d20ef-391">Each type member has a unique signature.</span></span> <span data-ttu-id="d20ef-392">Metoda podpisy obsahovat název metody a seznam parametrů (pořadí a typy argumentů metody).</span><span class="sxs-lookup"><span data-stu-id="d20ef-392">Method signatures consist of the method name and a parameter list (the order and types of the method's arguments).</span></span> <span data-ttu-id="d20ef-393">Několik metod se stejným názvem, může být definován v rámci typu tak dlouho, dokud se liší jejich podpisy.</span><span class="sxs-lookup"><span data-stu-id="d20ef-393">Multiple methods with the same name can be defined within a type as long as their signatures differ.</span></span> <span data-ttu-id="d20ef-394">Když jsou definovány dvě nebo více metod se stejným názvem, metoda říká, že je přetížený.</span><span class="sxs-lookup"><span data-stu-id="d20ef-394">When two or more methods with the same name are defined, the method is said to be overloaded.</span></span> <span data-ttu-id="d20ef-395">Například v <xref:System.Char?displayProperty=nameWithType>, <xref:System.Char.IsDigit%2A> metoda je přetížena.</span><span class="sxs-lookup"><span data-stu-id="d20ef-395">For example, in <xref:System.Char?displayProperty=nameWithType>, the <xref:System.Char.IsDigit%2A> method is overloaded.</span></span> <span data-ttu-id="d20ef-396">Jedna metoda má <xref:System.Char>.</span><span class="sxs-lookup"><span data-stu-id="d20ef-396">One method takes a <xref:System.Char>.</span></span> <span data-ttu-id="d20ef-397">Jiná metoda má <xref:System.String> a <xref:System.Int32>.</span><span class="sxs-lookup"><span data-stu-id="d20ef-397">The other method takes a <xref:System.String> and an <xref:System.Int32>.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="d20ef-398">Návratový typ není považovány za součást signatury metody.</span><span class="sxs-lookup"><span data-stu-id="d20ef-398">The return type is not considered part of a method's signature.</span></span> <span data-ttu-id="d20ef-399">To znamená metody nemohou být přetíženy, pokud se liší pouze návratového typu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-399">That is, methods cannot be overloaded if they differ only by return type.</span></span>  
  
### <a name="inheriting-overriding-and-hiding-members"></a><span data-ttu-id="d20ef-400">Dědění, přepsání a skrytí členy</span><span class="sxs-lookup"><span data-stu-id="d20ef-400">Inheriting, Overriding, and Hiding Members</span></span>  
 <span data-ttu-id="d20ef-401">Odvozený typ dědí všechny členy jeho základní typ; To znamená tito členové jsou definovány tak k dispozici pro odvozený typ.</span><span class="sxs-lookup"><span data-stu-id="d20ef-401">A derived type inherits all members of its base type; that is, these members are defined on, and available to, the derived type.</span></span> <span data-ttu-id="d20ef-402">Chování a vlastnosti zděděné členy se dají změnit dvěma způsoby:</span><span class="sxs-lookup"><span data-stu-id="d20ef-402">The behavior or qualities of inherited members can be modified in two ways:</span></span>  
  
-   <span data-ttu-id="d20ef-403">Odvozený typ skrýt zděděného členu definice nového člena se stejným podpisem.</span><span class="sxs-lookup"><span data-stu-id="d20ef-403">A derived type can hide an inherited member by defining a new member with the same signature.</span></span> <span data-ttu-id="d20ef-404">To může být provedeno, aby privátní dříve veřejného člena nebo definovat nové chování pro zděděnou metodu, která je označena jako `final`.</span><span class="sxs-lookup"><span data-stu-id="d20ef-404">This might be done to make a previously public member private or to define new behavior for an inherited method that is marked as `final`.</span></span>  
  
-   <span data-ttu-id="d20ef-405">Odvozený typ můžete přepsat zděděnou virtuální metodu.</span><span class="sxs-lookup"><span data-stu-id="d20ef-405">A derived type can override an inherited virtual method.</span></span> <span data-ttu-id="d20ef-406">Přepsání metody poskytuje novou definici metody, která bude volána na základě typu hodnoty na dobu spuštění, nikoli typ proměnné označuje v době kompilace.</span><span class="sxs-lookup"><span data-stu-id="d20ef-406">The overriding method provides a new definition of the method that will be invoked based on the type of the value at run time rather than the type of the variable known at compile time.</span></span> <span data-ttu-id="d20ef-407">Metodu můžete přepsat virtuální metodu pouze v případě, že virtuální metoda není označená jako `final` a nová metoda je dostupný jako virtuální metody.</span><span class="sxs-lookup"><span data-stu-id="d20ef-407">A method can override a virtual method only if the virtual method is not marked as `final` and the new method is at least as accessible as the virtual method.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="d20ef-408">Viz také</span><span class="sxs-lookup"><span data-stu-id="d20ef-408">See Also</span></span>  
 [<span data-ttu-id="d20ef-409">Knihovna tříd rozhraní .NET</span><span class="sxs-lookup"><span data-stu-id="d20ef-409">.NET Class Library</span></span>](http://go.microsoft.com/fwlink/?LinkID=217856)  
 [<span data-ttu-id="d20ef-410">Modul Common Language Runtime</span><span class="sxs-lookup"><span data-stu-id="d20ef-410">Common Language Runtime</span></span>](../../../docs/standard/clr.md)  
 [<span data-ttu-id="d20ef-411">Převod typů v rozhraní .NET</span><span class="sxs-lookup"><span data-stu-id="d20ef-411">Type Conversion in .NET</span></span>](../../../docs/standard/base-types/type-conversion.md)
