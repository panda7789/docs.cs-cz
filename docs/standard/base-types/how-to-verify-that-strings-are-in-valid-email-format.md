---
title: Ověření platnosti e-mailového formátu řetězců
ms.date: 12/10/2018
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- regular expressions, examples
- user input, examples
- Regex.IsMatch method
- regular expressions [.NET Framework], examples
- examples [Visual Basic], strings
- IsValidEmail
- validation, email strings
- input, checking
- strings [.NET Framework], examples [Visual Basic]
- email [.NET Framework], validating
- IsMatch method
ms.assetid: 7536af08-4e86-4953-98a1-a8298623df92
ms.openlocfilehash: 360ed985575358dd9603a55fc2d5d6c297621ec8
ms.sourcegitcommit: 33deec3e814238fb18a49b2a7e89278e27888291
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/02/2020
ms.locfileid: "84290419"
---
# <a name="how-to-verify-that-strings-are-in-valid-email-format"></a><span data-ttu-id="d55fa-102">Ověření platnosti e-mailového formátu řetězců</span><span class="sxs-lookup"><span data-stu-id="d55fa-102">How to verify that strings are in valid email format</span></span>

<span data-ttu-id="d55fa-103">Následující příklad používá regulární výraz pro ověření, zda je řetězec v platném formátu e-mailu.</span><span class="sxs-lookup"><span data-stu-id="d55fa-103">The following example uses a regular expression to verify that a string is in valid email format.</span></span>

## <a name="example"></a><span data-ttu-id="d55fa-104">Příklad</span><span class="sxs-lookup"><span data-stu-id="d55fa-104">Example</span></span>

<span data-ttu-id="d55fa-105">Příklad definuje `IsValidEmail` metodu, která vrátí, `true` Pokud řetězec obsahuje platnou e-mailovou adresu `false` , a pokud ne, ale neprovede žádnou jinou akci.</span><span class="sxs-lookup"><span data-stu-id="d55fa-105">The example defines an `IsValidEmail` method, which returns `true` if the string contains a valid email address and `false` if it does not, but takes no other action.</span></span>

<span data-ttu-id="d55fa-106">Chcete-li ověřit, zda je e-mailová adresa platná, `IsValidEmail` metoda volá <xref:System.Text.RegularExpressions.Regex.Replace%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.MatchEvaluator%29?displayProperty=nameWithType> metodu se `(@)(.+)$` vzorem regulárního výrazu, který odděluje název domény od e-mailové adresy.</span><span class="sxs-lookup"><span data-stu-id="d55fa-106">To verify that the email address is valid, the `IsValidEmail` method calls the <xref:System.Text.RegularExpressions.Regex.Replace%28System.String%2CSystem.String%2CSystem.Text.RegularExpressions.MatchEvaluator%29?displayProperty=nameWithType> method with the `(@)(.+)$` regular expression pattern to separate the domain name from the email address.</span></span> <span data-ttu-id="d55fa-107">Třetí parametr je <xref:System.Text.RegularExpressions.MatchEvaluator> delegát, který představuje metodu, která zpracovává a nahrazuje odpovídající text.</span><span class="sxs-lookup"><span data-stu-id="d55fa-107">The third parameter is a <xref:System.Text.RegularExpressions.MatchEvaluator> delegate that represents the method that processes and replaces the matched text.</span></span> <span data-ttu-id="d55fa-108">Vzor regulárního výrazu je interpretován následujícím způsobem.</span><span class="sxs-lookup"><span data-stu-id="d55fa-108">The regular expression pattern is interpreted as follows.</span></span>

|<span data-ttu-id="d55fa-109">Vzor</span><span class="sxs-lookup"><span data-stu-id="d55fa-109">Pattern</span></span>|<span data-ttu-id="d55fa-110">Popis</span><span class="sxs-lookup"><span data-stu-id="d55fa-110">Description</span></span>|
|-------------|-----------------|
|`(@)`|<span data-ttu-id="d55fa-111">Porovnává znak @.</span><span class="sxs-lookup"><span data-stu-id="d55fa-111">Match the @ character.</span></span> <span data-ttu-id="d55fa-112">Toto je první zachytávající skupina.</span><span class="sxs-lookup"><span data-stu-id="d55fa-112">This is the first capturing group.</span></span>|
|`(.+)`|<span data-ttu-id="d55fa-113">Porovnává jeden nebo více výskytů libovolného znaku.</span><span class="sxs-lookup"><span data-stu-id="d55fa-113">Match one or more occurrences of any character.</span></span> <span data-ttu-id="d55fa-114">Toto je druhá zachytávající skupina.</span><span class="sxs-lookup"><span data-stu-id="d55fa-114">This is the second capturing group.</span></span>|
|`$`|<span data-ttu-id="d55fa-115">Ukončí porovnávání na konci řetězce.</span><span class="sxs-lookup"><span data-stu-id="d55fa-115">End the match at the end of the string.</span></span>|

<span data-ttu-id="d55fa-116">Název domény společně s znakem @ je předán `DomainMapper` metodě, která používá <xref:System.Globalization.IdnMapping> třídu k překladu znaků Unicode, které jsou mimo rozsah znaků US-ASCII na Punycode.</span><span class="sxs-lookup"><span data-stu-id="d55fa-116">The domain name along with the @ character is passed to the `DomainMapper` method, which uses the <xref:System.Globalization.IdnMapping> class to translate Unicode characters that are outside the US-ASCII character range to Punycode.</span></span> <span data-ttu-id="d55fa-117">Metoda také nastaví `invalid` příznak na, `True` Pokud <xref:System.Globalization.IdnMapping.GetAscii%2A?displayProperty=nameWithType> Metoda zjistí v názvu domény neplatné znaky.</span><span class="sxs-lookup"><span data-stu-id="d55fa-117">The method also sets the `invalid` flag to `True` if the <xref:System.Globalization.IdnMapping.GetAscii%2A?displayProperty=nameWithType> method detects any invalid characters in the domain name.</span></span> <span data-ttu-id="d55fa-118">Metoda vrátí název domény Punycode předchází symbolu @ do `IsValidEmail` metody.</span><span class="sxs-lookup"><span data-stu-id="d55fa-118">The method returns the Punycode domain name preceded by the @ symbol to the `IsValidEmail` method.</span></span>

<span data-ttu-id="d55fa-119">`IsValidEmail`Metoda pak zavolá <xref:System.Text.RegularExpressions.Regex.IsMatch%28System.String%2CSystem.String%29?displayProperty=nameWithType> metodu pro ověření, že adresa odpovídá vzoru regulárního výrazu.</span><span class="sxs-lookup"><span data-stu-id="d55fa-119">The `IsValidEmail` method then calls the <xref:System.Text.RegularExpressions.Regex.IsMatch%28System.String%2CSystem.String%29?displayProperty=nameWithType> method to verify that the address conforms to a regular expression pattern.</span></span>

<span data-ttu-id="d55fa-120">Všimněte si, že `IsValidEmail` Metoda neprovádí ověřování pro ověření e-mailové adresy.</span><span class="sxs-lookup"><span data-stu-id="d55fa-120">Note that the `IsValidEmail` method does not perform authentication to validate the email address.</span></span> <span data-ttu-id="d55fa-121">Pouze určuje, zda je formát platný pro e-mailovou adresu.</span><span class="sxs-lookup"><span data-stu-id="d55fa-121">It merely determines whether its format is valid for an email address.</span></span> <span data-ttu-id="d55fa-122">Kromě toho `IsValidEmail` metoda neověřuje, jestli je název domény nejvyšší úrovně platný název domény uvedený v [databázi kořenové zóny IANA](https://www.iana.org/domains/root/db), což by vyžadovalo vyhledávací operaci.</span><span class="sxs-lookup"><span data-stu-id="d55fa-122">In addition, the `IsValidEmail` method does not verify that the top-level domain name is a valid domain name listed at the [IANA Root Zone Database](https://www.iana.org/domains/root/db), which would require a look-up operation.</span></span> <span data-ttu-id="d55fa-123">Místo toho regulární výraz pouze ověří, zda se název domény nejvyšší úrovně skládá ze dvou a dvaceti čtyř znaků ASCII, s alfanumerickým prvním a posledním znakem a zbývajícími znaky buď alfanumerických, nebo spojovníkem (-).</span><span class="sxs-lookup"><span data-stu-id="d55fa-123">Instead, the regular expression merely verifies that the top-level domain name consists of between two and twenty-four ASCII characters, with alphanumeric first and last characters and the remaining characters being either alphanumeric or a hyphen (-).</span></span>

[!code-csharp[RegularExpressions.Examples.Email#7](../../../samples/snippets/csharp/VS_Snippets_CLR/RegularExpressions.Examples.Email/cs/example4.cs#7)]
[!code-vb[RegularExpressions.Examples.Email#7](../../../samples/snippets/visualbasic/VS_Snippets_CLR/RegularExpressions.Examples.Email/vb/example4.vb#7)]

<span data-ttu-id="d55fa-124">V tomto příkladu je vzor regulárního výrazu ``^(?(")(".+?(?<!\\)"@)|(([0-9a-z]((\.(?!\.))|[-!#\$%&'\*\+/=\?\^`\{\}\|~\w])*)(?<=[0-9a-z])@))(?(\[)(\[(\d{1,3}\.){3}\d{1,3}\])|(([0-9a-z][-0-9a-z]*[0-9a-z]*\.)+[a-z0-9][\-a-z0-9]{0,22}[a-z0-9]))$`` interpretován tak, jak je znázorněno v následující legendě.</span><span class="sxs-lookup"><span data-stu-id="d55fa-124">In this example, the regular expression pattern ``^(?(")(".+?(?<!\\)"@)|(([0-9a-z]((\.(?!\.))|[-!#\$%&'\*\+/=\?\^`\{\}\|~\w])*)(?<=[0-9a-z])@))(?(\[)(\[(\d{1,3}\.){3}\d{1,3}\])|(([0-9a-z][-0-9a-z]*[0-9a-z]*\.)+[a-z0-9][\-a-z0-9]{0,22}[a-z0-9]))$`` is interpreted as shown in the following legend.</span></span> <span data-ttu-id="d55fa-125">Regulární výraz je kompilován pomocí <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase?displayProperty=nameWithType> příznaku.</span><span class="sxs-lookup"><span data-stu-id="d55fa-125">The regular expression is compiled using the <xref:System.Text.RegularExpressions.RegexOptions.IgnoreCase?displayProperty=nameWithType> flag.</span></span>

<span data-ttu-id="d55fa-126">Vzor `^` : začátek porovnávání na začátku řetězce.</span><span class="sxs-lookup"><span data-stu-id="d55fa-126">Pattern `^`: Begin the match at the start of the string.</span></span>

<span data-ttu-id="d55fa-127">Vzor `(?(")` : Určete, zda je první znak uvozovka.</span><span class="sxs-lookup"><span data-stu-id="d55fa-127">Pattern `(?(")`: Determine whether the first character is a quotation mark.</span></span> <span data-ttu-id="d55fa-128">`(?(")`je začátek konstrukce alternace.</span><span class="sxs-lookup"><span data-stu-id="d55fa-128">`(?(")` is the beginning of an alternation construct.</span></span>

<span data-ttu-id="d55fa-129">Vzor `(?(")(".+?(?<!\\)"@)` : Pokud je první znak uvozovky, porovnává počáteční uvozovku následovanou alespoň jedním výskytem libovolného znaku následovaný koncovou uvozovkou.</span><span class="sxs-lookup"><span data-stu-id="d55fa-129">Pattern `(?(")(".+?(?<!\\)"@)`: If the first character is a quotation mark, match a beginning quotation mark followed by at least one occurrence of any character, followed by an ending quotation mark.</span></span> <span data-ttu-id="d55fa-130">Koncovému uvozovku nesmí předcházet znak zpětného lomítka ( \\ ).</span><span class="sxs-lookup"><span data-stu-id="d55fa-130">The ending quotation mark must not be preceded by a backslash character (\\).</span></span> <span data-ttu-id="d55fa-131">`(?<!`je začátek kontrolního výrazu negativního zpětného vyhledávání s nulovou šířkou.</span><span class="sxs-lookup"><span data-stu-id="d55fa-131">`(?<!` is the beginning of a zero-width negative lookbehind assertion.</span></span> <span data-ttu-id="d55fa-132">Řetězec by měl být uzavřen znakem @.</span><span class="sxs-lookup"><span data-stu-id="d55fa-132">The string should conclude with an at sign (@).</span></span>

<span data-ttu-id="d55fa-133">Vzor `|(([0-9a-z]` : Pokud první znak není uvozovka, odpovídá jakémukoli abecednímu znaku z a až z nebo a až z (porovnání rozlišuje malá a velká písmena) nebo libovolný číselný znak od 0 do 9.</span><span class="sxs-lookup"><span data-stu-id="d55fa-133">Pattern `|(([0-9a-z]`: If the first character is not a quotation mark, match any alphabetic character from a to z or A to Z (the comparison is case insensitive), or any numeric character from 0 to 9.</span></span>

<span data-ttu-id="d55fa-134">Vzor `(\.(?!\.))` : Pokud je další znak tečky, porovnejte ho.</span><span class="sxs-lookup"><span data-stu-id="d55fa-134">Pattern `(\.(?!\.))`: If the next character is a period, match it.</span></span> <span data-ttu-id="d55fa-135">Pokud se nejedná o tečku, hledejte dopředu na další znak a pokračujte v porovnávání.</span><span class="sxs-lookup"><span data-stu-id="d55fa-135">If it is not a period, look ahead to the next character and continue the match.</span></span> <span data-ttu-id="d55fa-136">`(?!\.)`je negativní kontrolní výraz dopředného vyhledávání s nulovou šířkou, který brání v zobrazení dvou po sobě jdoucích teček v místní části e-mailové adresy.</span><span class="sxs-lookup"><span data-stu-id="d55fa-136">`(?!\.)` is a zero-width negative lookahead assertion that prevents two consecutive periods from appearing in the local part of an email address.</span></span>

<span data-ttu-id="d55fa-137">Vzor ``|[-!#\$%&'\*\+/=\?\^`\{\}\|~\w]`` : Pokud následující znak není tečka, odpovídá libovolnému znaku slova nebo jednomu z následujících znaků:-! # $% & ' \* +/=? ^ \` {} | ~</span><span class="sxs-lookup"><span data-stu-id="d55fa-137">Pattern ``|[-!#\$%&'\*\+/=\?\^`\{\}\|~\w]``: If the next character is not a period, match any word character or one of the following characters: -!#$%&'\*+/=?^\`{}|~</span></span>

<span data-ttu-id="d55fa-138">Vzor ``((\.(?!\.))|[-!#\$%&'\*\+/=\?\^`\{\}\|~\w])*`` : vyrovnává vzor alternace (perioda následovaná netečkou nebo jedním z několika znaků) nula nebo vícekrát.</span><span class="sxs-lookup"><span data-stu-id="d55fa-138">Pattern ``((\.(?!\.))|[-!#\$%&'\*\+/=\?\^`\{\}\|~\w])*``: Match the alternation pattern (a period followed by a non-period, or one of a number of characters) zero or more times.</span></span>

<span data-ttu-id="d55fa-139">Vzor `@` : porovnává znak @.</span><span class="sxs-lookup"><span data-stu-id="d55fa-139">Pattern `@`: Match the @ character.</span></span>

<span data-ttu-id="d55fa-140">Vzor `(?<=[0-9a-z])` : pokračuje v porovnávání, pokud znak, který předchází znaku @, je a až z, a až z nebo 0 až 9.</span><span class="sxs-lookup"><span data-stu-id="d55fa-140">Pattern `(?<=[0-9a-z])`: Continue the match if the character that precedes the @ character is A through Z, a through z, or 0 through 9.</span></span> <span data-ttu-id="d55fa-141">Tento model definuje kontrolní výraz pozitivního zpětného vyhledávání s nulovou šířkou.</span><span class="sxs-lookup"><span data-stu-id="d55fa-141">This pattern defines a zero-width positive lookbehind assertion.</span></span>

<span data-ttu-id="d55fa-142">Vzor `(?(\[)` : Ověřte, zda je znak, který následuje po znaku @, levou hranatou závorkou.</span><span class="sxs-lookup"><span data-stu-id="d55fa-142">Pattern `(?(\[)`: Check whether the character that follows @ is an opening bracket.</span></span>

<span data-ttu-id="d55fa-143">Vzor `(\[(\d{1,3}\.){3}\d{1,3}\])` : Pokud se jedná o levou hranatou závorku, porovnává levou hranatou závorku následovanou IP adresou (čtyři sady jedna až tři číslice, každá sada oddělená tečkou) a pravou hranatou závorku.</span><span class="sxs-lookup"><span data-stu-id="d55fa-143">Pattern `(\[(\d{1,3}\.){3}\d{1,3}\])`: If it is an opening bracket, match the opening bracket followed by an IP address (four sets of one to three digits, with each set separated by a period) and a closing bracket.</span></span>

<span data-ttu-id="d55fa-144">Vzor `|(([0-9a-z][-0-9a-z]*[0-9a-z]*\.)+` : Pokud znak, který následuje za @, není levou hranatou závorkou, porovná jeden alfanumerický znak s hodnotou a-z, a-z nebo 0-9, následovaný žádným nebo více výskyty pomlčky, následovaný žádným nebo jedním alfanumerickým znakem a hodnotou a-z, a-z nebo 0-9 následovaný tečkou.</span><span class="sxs-lookup"><span data-stu-id="d55fa-144">Pattern `|(([0-9a-z][-0-9a-z]*[0-9a-z]*\.)+`: If the character that follows @ is not an opening bracket, match one alphanumeric character with a value of A-Z, a-z, or 0-9, followed by zero or more occurrences of a hyphen, followed by zero or one alphanumeric character with a value of A-Z, a-z, or 0-9, followed by a period.</span></span> <span data-ttu-id="d55fa-145">Tento model se může opakovat jednou nebo víckrát a musí následovat název domény nejvyšší úrovně.</span><span class="sxs-lookup"><span data-stu-id="d55fa-145">This pattern can be repeated one or more times, and must be followed by the top-level domain name.</span></span>

<span data-ttu-id="d55fa-146">Vzor `[a-z0-9][\-a-z0-9]{0,22}[a-z0-9]))` : název domény nejvyšší úrovně musí začínat a končit alfanumerickým znakem (a-z, a-z a 0-9).</span><span class="sxs-lookup"><span data-stu-id="d55fa-146">Pattern `[a-z0-9][\-a-z0-9]{0,22}[a-z0-9]))`: The top-level domain name must begin and end with an alphanumeric character (a-z, A-Z, and 0-9).</span></span> <span data-ttu-id="d55fa-147">Může také obsahovat nula až 22 znaků ASCII, které jsou buď alfanumerické, nebo spojovníky.</span><span class="sxs-lookup"><span data-stu-id="d55fa-147">It can also include from zero to 22 ASCII characters that are either alphanumeric or hyphens.</span></span>

<span data-ttu-id="d55fa-148">Vzor `$` : ukončí porovnávání na konci řetězce.</span><span class="sxs-lookup"><span data-stu-id="d55fa-148">Pattern `$`: End the match at the end of the string.</span></span>

## <a name="compile-the-code"></a><span data-ttu-id="d55fa-149">Kompilovat kód</span><span class="sxs-lookup"><span data-stu-id="d55fa-149">Compile the code</span></span>

<span data-ttu-id="d55fa-150">`IsValidEmail`Metody a `DomainMapper` mohou být zahrnuty v knihovně nástrojů regulárních výrazů, nebo mohou být zahrnuty jako privátní statické nebo metody instance třídy aplikace.</span><span class="sxs-lookup"><span data-stu-id="d55fa-150">The `IsValidEmail` and `DomainMapper` methods can be included in a library of regular expression utility methods, or they can be included as private static or instance methods in the application class.</span></span>

<span data-ttu-id="d55fa-151">Můžete také použít <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A?displayProperty=nameWithType> metodu k zahrnutí tohoto regulárního výrazu do knihovny regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="d55fa-151">You can also use the <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A?displayProperty=nameWithType> method to include this regular expression in a regular expression library.</span></span>

<span data-ttu-id="d55fa-152">Pokud jsou použity v knihovně regulárních výrazů, můžete je volat pomocí kódu, jako je následující:</span><span class="sxs-lookup"><span data-stu-id="d55fa-152">If they are used in a regular expression library, you can call them by using code such as the following:</span></span>

[!code-csharp[RegularExpressions.Examples.Email#8](../../../samples/snippets/csharp/VS_Snippets_CLR/RegularExpressions.Examples.Email/cs/example4.cs#8)]
[!code-vb[RegularExpressions.Examples.Email#8](../../../samples/snippets/visualbasic/VS_Snippets_CLR/RegularExpressions.Examples.Email/vb/example4.vb#8)]

## <a name="see-also"></a><span data-ttu-id="d55fa-153">Viz také</span><span class="sxs-lookup"><span data-stu-id="d55fa-153">See also</span></span>

- [<span data-ttu-id="d55fa-154">.NET Framework – regulární výrazy</span><span class="sxs-lookup"><span data-stu-id="d55fa-154">.NET Framework Regular Expressions</span></span>](regular-expressions.md)
