---
title: Kompilace a opětovné používání v regulárních výrazech
ms.date: 03/30/2017
ms.technology: dotnet-standard
helpviewer_keywords:
- parsing text with regular expressions, compilation
- searching with regular expressions, compilation
- .NET Framework regular expressions, engines
- .NET Framework regular expressions, compilation
- regular expressions, compilation
- compilation, regular expressions
- pattern-matching with regular expressions, compilation
- regular expressions, engines
ms.assetid: 182ec76d-5a01-4d73-996c-0b0d14fcea18
ms.openlocfilehash: 3e1dfe8373145286b03e503f038e267ff0d4c4f3
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/15/2020
ms.locfileid: "73091734"
---
# <a name="compilation-and-reuse-in-regular-expressions"></a><span data-ttu-id="45e4f-102">Kompilace a opětovné používání v regulárních výrazech</span><span class="sxs-lookup"><span data-stu-id="45e4f-102">Compilation and Reuse in Regular Expressions</span></span>
<span data-ttu-id="45e4f-103">Můžete optimalizovat výkon aplikací, které poskytují rozsáhlé využití regulárních výrazů pochopením toho, jak modul regulárních výrazů kompiluje výrazy, a pochopením způsobu ukládání regulárních výrazů do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="45e4f-103">You can optimize the performance of applications that make extensive use of regular expressions by understanding how the regular expression engine compiles expressions and by understanding how regular expressions are cached.</span></span> <span data-ttu-id="45e4f-104">Toto téma popisuje kompilaci i ukládání do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="45e4f-104">This topic discusses both compilation and caching.</span></span>  
  
## <a name="compiled-regular-expressions"></a><span data-ttu-id="45e4f-105">Zkompilované regulární výrazy</span><span class="sxs-lookup"><span data-stu-id="45e4f-105">Compiled Regular Expressions</span></span>  
 <span data-ttu-id="45e4f-106">Ve výchozím nastavení modul regulárních výrazů zkompiluje regulární výraz do posloupnosti interních instrukcí (jedná se o kódy vysoké úrovně, které se liší od zprostředkujícího jazyka společnosti Microsoft nebo MSIL).</span><span class="sxs-lookup"><span data-stu-id="45e4f-106">By default, the regular expression engine compiles a regular expression to a sequence of internal instructions (these are high-level codes that are different from Microsoft intermediate language, or MSIL).</span></span> <span data-ttu-id="45e4f-107">Když modul spustí regulární výraz, interpretuje vnitřní kódy.</span><span class="sxs-lookup"><span data-stu-id="45e4f-107">When the engine executes a regular expression, it interprets the internal codes.</span></span>  
  
 <span data-ttu-id="45e4f-108">Pokud <xref:System.Text.RegularExpressions.Regex> je objekt vytvořen <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> s možností, zkompiluje regulární výraz do explicitního kódu MSIL namísto interních pokynů regulárního výrazu vysoké úrovně.</span><span class="sxs-lookup"><span data-stu-id="45e4f-108">If a <xref:System.Text.RegularExpressions.Regex> object is constructed with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option, it compiles the regular expression to explicit MSIL code instead of high-level regular expression internal instructions.</span></span> <span data-ttu-id="45e4f-109">To umožňuje . NET je just-in-time (JIT) kompilátor převést výraz na nativní strojový kód pro vyšší výkon.</span><span class="sxs-lookup"><span data-stu-id="45e4f-109">This allows .NET's just-in-time (JIT) compiler to convert the expression to native machine code for higher performance.</span></span>  
  
<span data-ttu-id="45e4f-110">Vygenerovaný manit však nelze uvolnit.</span><span class="sxs-lookup"><span data-stu-id="45e4f-110">However, generated MSIL cannot be unloaded.</span></span> <span data-ttu-id="45e4f-111">Jediným způsobem, jak uvolnit kód, je uvolnit celou doménu aplikace (to znamená uvolnit veškerý kód aplikace.).</span><span class="sxs-lookup"><span data-stu-id="45e4f-111">The only way to unload code is to unload an entire application domain (that is, to unload all of your application's code.).</span></span> <span data-ttu-id="45e4f-112">Efektivně, jakmile regulární výraz je <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> zkompilován s možností, nikdy uvolní prostředky používané zkompilovaný <xref:System.Text.RegularExpressions.Regex> výraz, i v případě, že regulární výraz byl vytvořen objektem, který je sám propuštěn do uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="45e4f-112">Effectively, once a regular expression is compiled with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option,   never releases the resources used by the compiled expression, even if the regular expression was created by a <xref:System.Text.RegularExpressions.Regex> object that is itself released to garbage collection.</span></span>  
  
 <span data-ttu-id="45e4f-113">Musíte být opatrní omezit počet různých regulárních <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> výrazů, které kompilujete s možností, aby se zabránilo spotřebování příliš mnoho zdrojů.</span><span class="sxs-lookup"><span data-stu-id="45e4f-113">You must be careful to limit the number of different regular expressions you compile with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option to avoid consuming too many resources.</span></span> <span data-ttu-id="45e4f-114">Pokud aplikace musí používat velký nebo neomezený počet regulárních výrazů, každý výraz by měl být interpretován, nikoli kompilován.</span><span class="sxs-lookup"><span data-stu-id="45e4f-114">If an application must use a large or unbounded number of regular expressions, each expression should be interpreted, not compiled.</span></span> <span data-ttu-id="45e4f-115">Pokud se však opakovaně používá malý počet regulárních výrazů, měly by být kompilovány pro <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> lepší výkon.</span><span class="sxs-lookup"><span data-stu-id="45e4f-115">However, if a small number of regular expressions are used repeatedly, they should be compiled with <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> for better performance.</span></span> <span data-ttu-id="45e4f-116">Alternativou je použití předkompilovaných regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="45e4f-116">An alternative is to use precompiled regular expressions.</span></span> <span data-ttu-id="45e4f-117">Pomocí metody můžete zkompilovat všechny výrazy do <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A> opakovaně použitelné knihovny DLL.</span><span class="sxs-lookup"><span data-stu-id="45e4f-117">You can compile all of your expressions into a reusable DLL by using the <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A> method.</span></span> <span data-ttu-id="45e4f-118">Tím se zabrání nutnosti kompilace za běhu a zároveň těžit z rychlosti kompilovaných regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="45e4f-118">This avoids the need to compile at runtime while still benefiting from the speed of compiled regular expressions.</span></span>  
  
## <a name="the-regular-expressions-cache"></a><span data-ttu-id="45e4f-119">Mezipaměť regulárních výrazů</span><span class="sxs-lookup"><span data-stu-id="45e4f-119">The Regular Expressions Cache</span></span>  
 <span data-ttu-id="45e4f-120">Pro zvýšení výkonu udržuje modul regulárních výrazů celou mezipaměť zkompilovaných regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="45e4f-120">To improve performance, the regular expression engine maintains an application-wide cache of compiled regular expressions.</span></span> <span data-ttu-id="45e4f-121">V mezipaměti jsou uloženy vzory regulárních výrazů, které se používají pouze při volání statické metody.</span><span class="sxs-lookup"><span data-stu-id="45e4f-121">The cache stores regular expression patterns that are used only in static method calls.</span></span> <span data-ttu-id="45e4f-122">(Vzory regulárních výrazů dodávané metodám instancí nejsou ukládány do mezipaměti.) Tím se zabrání nutnosti měnit analýzu výrazu do kódu bajtů vysoké úrovně při každém použití.</span><span class="sxs-lookup"><span data-stu-id="45e4f-122">(Regular expression patterns supplied to instance methods are not cached.) This avoids the need to reparse an expression into high-level byte code each time it is used.</span></span>  
  
 <span data-ttu-id="45e4f-123">Maximální počet regulárních výrazů uložených v `static` `Shared` mezipaměti je <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> určen hodnotou vlastnosti ( in Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="45e4f-123">The maximum number of cached regular expressions is determined by the value of the `static` (`Shared` in Visual Basic) <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="45e4f-124">Ve výchozím nastavení ukládá modul regulárních výrazů až 15 zkompilovaných regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="45e4f-124">By default, the regular expression engine caches up to 15 compiled regular expressions.</span></span> <span data-ttu-id="45e4f-125">Pokud počet zkompilovaných regulárních výrazů překročí velikost mezipaměti, je zahozen nejméně naposledy použitý regulární výraz a nový regulární výraz je uložen do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="45e4f-125">If the number of compiled regular expressions exceeds the cache size, the least recently used regular expression is discarded and the new regular expression is cached.</span></span>  
  
 <span data-ttu-id="45e4f-126">Aplikace může využít předkompilované regulární výrazy jedním z následujících dvou způsobů:</span><span class="sxs-lookup"><span data-stu-id="45e4f-126">Your application can take advantage of precompiled regular expressions in one of the following two ways:</span></span>  
  
- <span data-ttu-id="45e4f-127">Pomocí statické metody objektu <xref:System.Text.RegularExpressions.Regex> k definování regulárního výrazu.</span><span class="sxs-lookup"><span data-stu-id="45e4f-127">By using a static method of the <xref:System.Text.RegularExpressions.Regex> object to define the regular expression.</span></span> <span data-ttu-id="45e4f-128">Pokud používáte vzor regulárního výrazu, který již byl definován v jiném volání statické metody, modul regulárních výrazů jej načte z mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="45e4f-128">If you are using a regular expression pattern that has already been defined in another static method call, the regular expression engine will retrieve it from the cache.</span></span> <span data-ttu-id="45e4f-129">Pokud ne, modul zkompiluje regulární výraz a přidá jej do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="45e4f-129">If not, the engine will compile the regular expression and add it to the cache.</span></span>  
  
- <span data-ttu-id="45e4f-130">Opětovným použitím <xref:System.Text.RegularExpressions.Regex> existujícího objektu tak dlouho, dokud je potřeba jeho vzor regulárního výrazu.</span><span class="sxs-lookup"><span data-stu-id="45e4f-130">By reusing an existing <xref:System.Text.RegularExpressions.Regex> object as long as its regular expression pattern is needed.</span></span>  
  
 <span data-ttu-id="45e4f-131">Z důvodu režie instanování objektů a kompilace regulárních výrazů je vytváření a rychlé ničení mnoha <xref:System.Text.RegularExpressions.Regex> objektů velmi nákladným procesem.</span><span class="sxs-lookup"><span data-stu-id="45e4f-131">Because of the overhead of object instantiation and regular expression compilation, creating and rapidly destroying numerous <xref:System.Text.RegularExpressions.Regex> objects is a very expensive process.</span></span> <span data-ttu-id="45e4f-132">Pro aplikace, které používají velký počet různých regulárních výrazů, můžete optimalizovat výkon pomocí volání statických `Regex` metod a případně zvětšením velikosti mezipaměti regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="45e4f-132">For applications that use a large number of different regular expressions, you can optimize performance by using calls to static `Regex` methods and possibly by increasing the size of the regular expression cache.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="45e4f-133">Viz také</span><span class="sxs-lookup"><span data-stu-id="45e4f-133">See also</span></span>

- [<span data-ttu-id="45e4f-134">Regulární výrazy rozhraní .NET</span><span class="sxs-lookup"><span data-stu-id="45e4f-134">.NET Regular Expressions</span></span>](../../../docs/standard/base-types/regular-expressions.md)
