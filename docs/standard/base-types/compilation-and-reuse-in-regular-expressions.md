---
title: Kompilace a opětovné používání v regulárních výrazech
ms.date: 03/30/2017
ms.technology: dotnet-standard
helpviewer_keywords:
- parsing text with regular expressions, compilation
- searching with regular expressions, compilation
- .NET Framework regular expressions, engines
- .NET Framework regular expressions, compilation
- regular expressions, compilation
- compilation, regular expressions
- pattern-matching with regular expressions, compilation
- regular expressions, engines
ms.assetid: 182ec76d-5a01-4d73-996c-0b0d14fcea18
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 2166412269a84329d42f58c7e3423229be4327b8
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: HT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/23/2019
ms.locfileid: "61650307"
---
# <a name="compilation-and-reuse-in-regular-expressions"></a><span data-ttu-id="02ca9-102">Kompilace a opětovné používání v regulárních výrazech</span><span class="sxs-lookup"><span data-stu-id="02ca9-102">Compilation and Reuse in Regular Expressions</span></span>
<span data-ttu-id="02ca9-103">Můžete optimalizovat výkon aplikací, které usnadňují rozsáhlé používání regulárních výrazů, pochopení, jak modul regulárních výrazů zkompiluje výrazy a principy regulárních výrazů v mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="02ca9-103">You can optimize the performance of applications that make extensive use of regular expressions by understanding how the regular expression engine compiles expressions and by understanding how regular expressions are cached.</span></span> <span data-ttu-id="02ca9-104">Toto téma popisuje kompilace a ukládání do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="02ca9-104">This topic discusses both compilation and caching.</span></span>  
  
## <a name="compiled-regular-expressions"></a><span data-ttu-id="02ca9-105">Kompilované regulární výrazy</span><span class="sxs-lookup"><span data-stu-id="02ca9-105">Compiled Regular Expressions</span></span>  
 <span data-ttu-id="02ca9-106">Ve výchozím nastavení zkompiluje modul regulárních výrazů regulární výraz k sekvenci interních instrukcí (jedná se vysoké úrovně kódy, které se liší od jazyk Microsoft intermediate language, nebo jazyk MSIL).</span><span class="sxs-lookup"><span data-stu-id="02ca9-106">By default, the regular expression engine compiles a regular expression to a sequence of internal instructions (these are high-level codes that are different from Microsoft intermediate language, or MSIL).</span></span> <span data-ttu-id="02ca9-107">Když se modul spouští regulárního výrazu, interpretuje interní kódy.</span><span class="sxs-lookup"><span data-stu-id="02ca9-107">When the engine executes a regular expression, it interprets the internal codes.</span></span>  
  
 <span data-ttu-id="02ca9-108">Pokud <xref:System.Text.RegularExpressions.Regex> objekt je vytvořen s <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> možnost kompiluje regulární výraz k explicitní kód jazyka MSIL místo interní pokyny vysoké úrovně regulární výraz.</span><span class="sxs-lookup"><span data-stu-id="02ca9-108">If a <xref:System.Text.RegularExpressions.Regex> object is constructed with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option, it compiles the regular expression to explicit MSIL code instead of high-level regular expression internal instructions.</span></span> <span data-ttu-id="02ca9-109">To umožňuje. NET pro kompilátor just-in-time (JIT) převede výraz do nativního strojového kódu pro vyšší výkon.</span><span class="sxs-lookup"><span data-stu-id="02ca9-109">This allows .NET's just-in-time (JIT) compiler to convert the expression to native machine code for higher performance.</span></span>  
  
<span data-ttu-id="02ca9-110">Generovaný jazyk MSIL však nelze uvolnit.</span><span class="sxs-lookup"><span data-stu-id="02ca9-110">However, generated MSIL cannot be unloaded.</span></span> <span data-ttu-id="02ca9-111">Jediný způsob, jak uvolnit kód je k uvolnění domény celé aplikace (to znamená, uvolnit všechny aplikace v kódu.).</span><span class="sxs-lookup"><span data-stu-id="02ca9-111">The only way to unload code is to unload an entire application domain (that is, to unload all of your application's code.).</span></span> <span data-ttu-id="02ca9-112">Efektivně Jakmile regulární výraz je zkompilován s <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> možnost, nikdy uvolní prostředky využívané třídou kompilované výrazu i v případě, že byl vytvořen regulárního výrazu <xref:System.Text.RegularExpressions.Regex> objekt, který je sám uvolněn uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="02ca9-112">Effectively, once a regular expression is compiled with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option,   never releases the resources used by the compiled expression, even if the regular expression was created by a <xref:System.Text.RegularExpressions.Regex> object that is itself released to garbage collection.</span></span>  
  
 <span data-ttu-id="02ca9-113">Musíte být opatrní a omezit počet různých regulárních výrazů při kompilaci s <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> možnost vyhnout se spotřebovávají příliš mnoho zdrojů.</span><span class="sxs-lookup"><span data-stu-id="02ca9-113">You must be careful to limit the number of different regular expressions you compile with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option to avoid consuming too many resources.</span></span> <span data-ttu-id="02ca9-114">Pokud aplikace musí používat velká, nebo bez vazby počet regulárních výrazů, každý výraz by měl být interpretován, není zkompilován.</span><span class="sxs-lookup"><span data-stu-id="02ca9-114">If an application must use a large or unbounded number of regular expressions, each expression should be interpreted, not compiled.</span></span> <span data-ttu-id="02ca9-115">Ale pokud malý počet regulárních výrazů jsou opakovaně používá, že by měl být zkompilován s <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> pro zajištění lepšího výkonu.</span><span class="sxs-lookup"><span data-stu-id="02ca9-115">However, if a small number of regular expressions are used repeatedly, they should be compiled with <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> for better performance.</span></span> <span data-ttu-id="02ca9-116">Alternativou je použití předkompilované regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="02ca9-116">An alternative is to use precompiled regular expressions.</span></span> <span data-ttu-id="02ca9-117">Všechny vaše výrazy do opakovaně použitelné knihovny DLL můžete zkompilovat pomocí <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="02ca9-117">You can compile all of your expressions into a reusable DLL by using the <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A> method.</span></span> <span data-ttu-id="02ca9-118">Tím se vyhnete nutnosti kompilace za běhu přitom výhod rychlost kompilované regulární výrazy.</span><span class="sxs-lookup"><span data-stu-id="02ca9-118">This avoids the need to compile at runtime while still benefiting from the speed of compiled regular expressions.</span></span>  
  
## <a name="the-regular-expressions-cache"></a><span data-ttu-id="02ca9-119">Mezipaměť regulárních výrazů</span><span class="sxs-lookup"><span data-stu-id="02ca9-119">The Regular Expressions Cache</span></span>  
 <span data-ttu-id="02ca9-120">Kvůli zvýšení výkonu se modul regulárních výrazů udržuje mezipaměť celou aplikaci zkompilovaných regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="02ca9-120">To improve performance, the regular expression engine maintains an application-wide cache of compiled regular expressions.</span></span> <span data-ttu-id="02ca9-121">Mezipaměti ukládá vzory regulárních výrazů, které se používají pouze ve volání statické metody.</span><span class="sxs-lookup"><span data-stu-id="02ca9-121">The cache stores regular expression patterns that are used only in static method calls.</span></span> <span data-ttu-id="02ca9-122">(Vzory regulárních výrazů zadané metod, které nejsou uložené v mezipaměti.) Tím se vyhnete nutnosti rozboru výraz do vysoké úrovně bajtový kód pokaždé, když se používá.</span><span class="sxs-lookup"><span data-stu-id="02ca9-122">(Regular expression patterns supplied to instance methods are not cached.) This avoids the need to reparse an expression into high-level byte code each time it is used.</span></span>  
  
 <span data-ttu-id="02ca9-123">Maximální počet v mezipaměti regulárních výrazů je určen hodnotou `static` (`Shared` v jazyce Visual Basic) <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> vlastnost.</span><span class="sxs-lookup"><span data-stu-id="02ca9-123">The maximum number of cached regular expressions is determined by the value of the `static` (`Shared` in Visual Basic) <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="02ca9-124">Ve výchozím nastavení modul regulárních výrazů ukládá do mezipaměti až 15 kompilované regulární výrazy.</span><span class="sxs-lookup"><span data-stu-id="02ca9-124">By default, the regular expression engine caches up to 15 compiled regular expressions.</span></span> <span data-ttu-id="02ca9-125">Pokud počet zkompilovaných regulárních výrazů překračuje velikost mezipaměti, nejdéle regulárního výrazu zahozena a nové regulárního výrazu se uloží do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="02ca9-125">If the number of compiled regular expressions exceeds the cache size, the least recently used regular expression is discarded and the new regular expression is cached.</span></span>  
  
 <span data-ttu-id="02ca9-126">Aplikace můžete využít předkompilované regulárních výrazů v jednom z následujících dvou způsobů:</span><span class="sxs-lookup"><span data-stu-id="02ca9-126">Your application can take advantage of precompiled regular expressions in one of the following two ways:</span></span>  
  
- <span data-ttu-id="02ca9-127">Pomocí statické metody <xref:System.Text.RegularExpressions.Regex> objektu k definování regulárního výrazu.</span><span class="sxs-lookup"><span data-stu-id="02ca9-127">By using a static method of the <xref:System.Text.RegularExpressions.Regex> object to define the regular expression.</span></span> <span data-ttu-id="02ca9-128">Pokud používáte vzor regulárního výrazu, který již byl definován v jiné volání statické metody, modul regulárních výrazů budou načítat z mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="02ca9-128">If you are using a regular expression pattern that has already been defined in another static method call, the regular expression engine will retrieve it from the cache.</span></span> <span data-ttu-id="02ca9-129">Pokud ne, modul se kompiluje regulární výraz a přidejte ji do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="02ca9-129">If not, the engine will compile the regular expression and add it to the cache.</span></span>  
  
- <span data-ttu-id="02ca9-130">Opětovným použitím existující <xref:System.Text.RegularExpressions.Regex> objekt tak dlouho, dokud jeho vzor regulárního výrazu je potřeba.</span><span class="sxs-lookup"><span data-stu-id="02ca9-130">By reusing an existing <xref:System.Text.RegularExpressions.Regex> object as long as its regular expression pattern is needed.</span></span>  
  
 <span data-ttu-id="02ca9-131">Z důvodu režie vytváření instancí objektu a kompilace regulárních výrazů, vytváření a ničení rychle mnoho <xref:System.Text.RegularExpressions.Regex> objekty je velmi nákladný proces.</span><span class="sxs-lookup"><span data-stu-id="02ca9-131">Because of the overhead of object instantiation and regular expression compilation, creating and rapidly destroying numerous <xref:System.Text.RegularExpressions.Regex> objects is a very expensive process.</span></span> <span data-ttu-id="02ca9-132">Pro aplikace, které používají velký počet různých regulárních výrazů, můžete optimalizovat výkon pomocí volání statických `Regex` metody a případně zvětšením velikosti mezipaměti regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="02ca9-132">For applications that use a large number of different regular expressions, you can optimize performance by using calls to static `Regex` methods and possibly by increasing the size of the regular expression cache.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="02ca9-133">Viz také:</span><span class="sxs-lookup"><span data-stu-id="02ca9-133">See also</span></span>

- [<span data-ttu-id="02ca9-134">Regulárních výrazů .NET</span><span class="sxs-lookup"><span data-stu-id="02ca9-134">.NET Regular Expressions</span></span>](../../../docs/standard/base-types/regular-expressions.md)
