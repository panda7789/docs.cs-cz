---
title: Kompilace a opětovné používání v regulárních výrazech
ms.date: 03/30/2017
ms.technology: dotnet-standard
helpviewer_keywords:
- parsing text with regular expressions, compilation
- searching with regular expressions, compilation
- .NET Framework regular expressions, engines
- .NET Framework regular expressions, compilation
- regular expressions, compilation
- compilation, regular expressions
- pattern-matching with regular expressions, compilation
- regular expressions, engines
ms.assetid: 182ec76d-5a01-4d73-996c-0b0d14fcea18
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 8a9adb5d39eb420496030d85dacd95a1cccd6fd2
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 05/04/2018
ms.locfileid: "33568755"
---
# <a name="compilation-and-reuse-in-regular-expressions"></a><span data-ttu-id="28082-102">Kompilace a opětovné používání v regulárních výrazech</span><span class="sxs-lookup"><span data-stu-id="28082-102">Compilation and Reuse in Regular Expressions</span></span>
<span data-ttu-id="28082-103">Můžete optimalizovat výkon aplikací, které rozsáhlé používání regulárních výrazů pochopení, jak modul regulárních výrazů zkompiluje výrazy a vysvětlení, jak běžných výrazů v mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="28082-103">You can optimize the performance of applications that make extensive use of regular expressions by understanding how the regular expression engine compiles expressions and by understanding how regular expressions are cached.</span></span> <span data-ttu-id="28082-104">Toto téma popisuje kompilace a ukládání do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="28082-104">This topic discusses both compilation and caching.</span></span>  
  
## <a name="compiled-regular-expressions"></a><span data-ttu-id="28082-105">Kompilované regulární výrazy</span><span class="sxs-lookup"><span data-stu-id="28082-105">Compiled Regular Expressions</span></span>  
 <span data-ttu-id="28082-106">Ve výchozím nastavení modul regulárních výrazů zkompiluje regulární výraz k posloupnost vnitřních pokynů (jsou tyto kódy vysoké úrovně, které se liší od společnosti Microsoft převodní jazyk nebo MSIL).</span><span class="sxs-lookup"><span data-stu-id="28082-106">By default, the regular expression engine compiles a regular expression to a sequence of internal instructions (these are high-level codes that are different from Microsoft intermediate language, or MSIL).</span></span> <span data-ttu-id="28082-107">Když modul provede regulární výraz, interpretuje interní kódy.</span><span class="sxs-lookup"><span data-stu-id="28082-107">When the engine executes a regular expression, it interprets the internal codes.</span></span>  
  
 <span data-ttu-id="28082-108">Pokud <xref:System.Text.RegularExpressions.Regex> objektu je vytvořený pomocí <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> možnost, se zkompiluje regulární výraz explicitní MSIL kód místo interní pokynů vysoké úrovně regulárního výrazu.</span><span class="sxs-lookup"><span data-stu-id="28082-108">If a <xref:System.Text.RegularExpressions.Regex> object is constructed with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option, it compiles the regular expression to explicit MSIL code instead of high-level regular expression internal instructions.</span></span> <span data-ttu-id="28082-109">To umožňuje. Kompilátor v běhu (JIT) pro NET převést výraz do nativního kódu počítače pro vyšší výkon.</span><span class="sxs-lookup"><span data-stu-id="28082-109">This allows .NET's just-in-time (JIT) compiler to convert the expression to native machine code for higher performance.</span></span>  
  
<span data-ttu-id="28082-110">Vygenerovaný MSIL však nemůže být odpojen.</span><span class="sxs-lookup"><span data-stu-id="28082-110">However, generated MSIL cannot be unloaded.</span></span> <span data-ttu-id="28082-111">Jediným způsobem, jak uvolnit kód je k uvolnění domény aplikace celý (to znamená, uvolnit všechny vaše aplikace je kód.).</span><span class="sxs-lookup"><span data-stu-id="28082-111">The only way to unload code is to unload an entire application domain (that is, to unload all of your application's code.).</span></span> <span data-ttu-id="28082-112">Efektivní Jakmile regulární výraz zkompilován s <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> možnost, nikdy uvolní prostředky využívané kompilované výrazem i v případě, že byl vytvořen s regulárním výrazem <xref:System.Text.RegularExpressions.Regex> objekt, který je sám uvolněn uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="28082-112">Effectively, once a regular expression is compiled with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option,   never releases the resources used by the compiled expression, even if the regular expression was created by a <xref:System.Text.RegularExpressions.Regex> object that is itself released to garbage collection.</span></span>  
  
 <span data-ttu-id="28082-113">Je třeba dbát omezit počet různých regulárních výrazů, které zkompilujete pomocí <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> možnost, aby se zabránilo spotřebovávají příliš mnoho zdrojů.</span><span class="sxs-lookup"><span data-stu-id="28082-113">You must be careful to limit the number of different regular expressions you compile with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option to avoid consuming too many resources.</span></span> <span data-ttu-id="28082-114">Pokud aplikace musí používat velká nebo neomezený počet regulární výrazy, každý výraz by měl interpretovány, není zkompilovat.</span><span class="sxs-lookup"><span data-stu-id="28082-114">If an application must use a large or unbounded number of regular expressions, each expression should be interpreted, not compiled.</span></span> <span data-ttu-id="28082-115">Ale pokud malý počet regulární výrazy se používají opakovaně, že má kompilovat s <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> pro dosažení vyššího výkonu.</span><span class="sxs-lookup"><span data-stu-id="28082-115">However, if a small number of regular expressions are used repeatedly, they should be compiled with <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> for better performance.</span></span> <span data-ttu-id="28082-116">Další možností je použití předkompilovaných regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="28082-116">An alternative is to use precompiled regular expressions.</span></span> <span data-ttu-id="28082-117">Všechny vaše výrazy do opakovaně použitelné knihovny DLL můžete zkompilovat pomocí <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A> metoda.</span><span class="sxs-lookup"><span data-stu-id="28082-117">You can compile all of your expressions into a reusable DLL by using the <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A> method.</span></span> <span data-ttu-id="28082-118">Tím je zabráněno potřeba kompilace za běhu při stále využívání rychlosti zkompilovaných regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="28082-118">This avoids the need to compile at runtime while still benefiting from the speed of compiled regular expressions.</span></span>  
  
## <a name="the-regular-expressions-cache"></a><span data-ttu-id="28082-119">Mezipaměť regulárních výrazů</span><span class="sxs-lookup"><span data-stu-id="28082-119">The Regular Expressions Cache</span></span>  
 <span data-ttu-id="28082-120">Chcete-li zvýšit výkon, modul regulárních výrazů udržuje mezipaměť celou aplikaci kompilované regulární výrazy.</span><span class="sxs-lookup"><span data-stu-id="28082-120">To improve performance, the regular expression engine maintains an application-wide cache of compiled regular expressions.</span></span> <span data-ttu-id="28082-121">Mezipaměti ukládá vzory regulárních výrazů, které se používají pouze ve voláních statickou metodu.</span><span class="sxs-lookup"><span data-stu-id="28082-121">The cache stores regular expression patterns that are used only in static method calls.</span></span> <span data-ttu-id="28082-122">(Regulární výraz vzory zadaný metod, které nejsou v mezipaměti.) Tím je zabráněno potřeba převedení výrazu do byte kódu vysoké úrovně pokaždé, když se používá.</span><span class="sxs-lookup"><span data-stu-id="28082-122">(Regular expression patterns supplied to instance methods are not cached.) This avoids the need to reparse an expression into high-level byte code each time it is used.</span></span>  
  
 <span data-ttu-id="28082-123">Maximální počet regulárních výrazů v mezipaměti je dáno hodnotu `static` (`Shared` v jazyce Visual Basic) <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> vlastnost.</span><span class="sxs-lookup"><span data-stu-id="28082-123">The maximum number of cached regular expressions is determined by the value of the `static` (`Shared` in Visual Basic) <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="28082-124">Ve výchozím nastavení modul regulárních výrazů ukládá do mezipaměti až 15 kompilované regulární výrazy.</span><span class="sxs-lookup"><span data-stu-id="28082-124">By default, the regular expression engine caches up to 15 compiled regular expressions.</span></span> <span data-ttu-id="28082-125">Pokud počet zkompilovaných regulárních výrazů překračuje velikost mezipaměti, budou zahozeny nejdéle nepoužitých regulárnímu výrazu a nové regulární výraz je uložený v mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="28082-125">If the number of compiled regular expressions exceeds the cache size, the least recently used regular expression is discarded and the new regular expression is cached.</span></span>  
  
 <span data-ttu-id="28082-126">Aplikace mohou využít výhod předkompilovaných regulárních výrazů v jednom z následujících dvou způsobů:</span><span class="sxs-lookup"><span data-stu-id="28082-126">Your application can take advantage of precompiled regular expressions in one of the following two ways:</span></span>  
  
-   <span data-ttu-id="28082-127">Pomocí statické metody <xref:System.Text.RegularExpressions.Regex> objekt, který chcete definovat regulární výraz.</span><span class="sxs-lookup"><span data-stu-id="28082-127">By using a static method of the <xref:System.Text.RegularExpressions.Regex> object to define the regular expression.</span></span> <span data-ttu-id="28082-128">Pokud používáte vzor regulárního výrazu, který již byl definován v jiném volání statickou metodu, modul regulárních výrazů budou načítat z mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="28082-128">If you are using a regular expression pattern that has already been defined in another static method call, the regular expression engine will retrieve it from the cache.</span></span> <span data-ttu-id="28082-129">Pokud ne, modul bude zkompilovat regulárnímu výrazu a přidejte ji do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="28082-129">If not, the engine will compile the regular expression and add it to the cache.</span></span>  
  
-   <span data-ttu-id="28082-130">Opětovným použitím stávající <xref:System.Text.RegularExpressions.Regex> objektu, dokud je zapotřebí jeho vzor regulárního výrazu.</span><span class="sxs-lookup"><span data-stu-id="28082-130">By reusing an existing <xref:System.Text.RegularExpressions.Regex> object as long as its regular expression pattern is needed.</span></span>  
  
 <span data-ttu-id="28082-131">Z důvodu režijní náklady na vytvoření instance objektu a kompilace regulárních výrazů, vytváření a rychle rušení velkého množství <xref:System.Text.RegularExpressions.Regex> objektů je velmi nákladný proces.</span><span class="sxs-lookup"><span data-stu-id="28082-131">Because of the overhead of object instantiation and regular expression compilation, creating and rapidly destroying numerous <xref:System.Text.RegularExpressions.Regex> objects is a very expensive process.</span></span> <span data-ttu-id="28082-132">Pro aplikace, které používají velké množství různých regulárních výrazů, můžete optimalizovat výkon pomocí volání statických `Regex` metody a případně zvětšením velikosti mezipaměti regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="28082-132">For applications that use a large number of different regular expressions, you can optimize performance by using calls to static `Regex` methods and possibly by increasing the size of the regular expression cache.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="28082-133">Viz také</span><span class="sxs-lookup"><span data-stu-id="28082-133">See Also</span></span>  
 [<span data-ttu-id="28082-134">Regulární výrazy rozhraní .NET</span><span class="sxs-lookup"><span data-stu-id="28082-134">.NET Regular Expressions</span></span>](../../../docs/standard/base-types/regular-expressions.md)
