---
title: Kompilace a opětovné používání v regulárních výrazech
ms.date: 03/30/2017
ms.technology: dotnet-standard
helpviewer_keywords:
- parsing text with regular expressions, compilation
- searching with regular expressions, compilation
- .NET Framework regular expressions, engines
- .NET Framework regular expressions, compilation
- regular expressions, compilation
- compilation, regular expressions
- pattern-matching with regular expressions, compilation
- regular expressions, engines
ms.assetid: 182ec76d-5a01-4d73-996c-0b0d14fcea18
ms.openlocfilehash: 3e1dfe8373145286b03e503f038e267ff0d4c4f3
ms.sourcegitcommit: 559fcfbe4871636494870a8b716bf7325df34ac5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/30/2019
ms.locfileid: "73091734"
---
# <a name="compilation-and-reuse-in-regular-expressions"></a><span data-ttu-id="3777c-102">Kompilace a opětovné používání v regulárních výrazech</span><span class="sxs-lookup"><span data-stu-id="3777c-102">Compilation and Reuse in Regular Expressions</span></span>
<span data-ttu-id="3777c-103">Můžete optimalizovat výkon aplikací, které využívají rozsáhlé používání regulárních výrazů, a porozumět, jak modul regulárních výrazů kompiluje výrazy a porozumění způsobu, jakým jsou regulární výrazy ukládány do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="3777c-103">You can optimize the performance of applications that make extensive use of regular expressions by understanding how the regular expression engine compiles expressions and by understanding how regular expressions are cached.</span></span> <span data-ttu-id="3777c-104">Toto téma popisuje kompilaci a ukládání do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="3777c-104">This topic discusses both compilation and caching.</span></span>  
  
## <a name="compiled-regular-expressions"></a><span data-ttu-id="3777c-105">Kompilované regulární výrazy</span><span class="sxs-lookup"><span data-stu-id="3777c-105">Compiled Regular Expressions</span></span>  
 <span data-ttu-id="3777c-106">Ve výchozím nastavení modul regulárních výrazů zkompiluje regulární výraz do sekvence vnitřních instrukcí (Jedná se o kódy vysoké úrovně, které se liší od jazyka MSIL nebo jazyka MSIL).</span><span class="sxs-lookup"><span data-stu-id="3777c-106">By default, the regular expression engine compiles a regular expression to a sequence of internal instructions (these are high-level codes that are different from Microsoft intermediate language, or MSIL).</span></span> <span data-ttu-id="3777c-107">Když modul spustí regulární výraz, interpretuje vnitřní kódy.</span><span class="sxs-lookup"><span data-stu-id="3777c-107">When the engine executes a regular expression, it interprets the internal codes.</span></span>  
  
 <span data-ttu-id="3777c-108">Pokud je objekt <xref:System.Text.RegularExpressions.Regex> vytvořen pomocí možnosti <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType>, Kompiluje regulární výraz na explicitní kód jazyka MSIL namísto interních instrukcí regulárního výrazu vysoké úrovně.</span><span class="sxs-lookup"><span data-stu-id="3777c-108">If a <xref:System.Text.RegularExpressions.Regex> object is constructed with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option, it compiles the regular expression to explicit MSIL code instead of high-level regular expression internal instructions.</span></span> <span data-ttu-id="3777c-109">To umožňuje. Kompilátor JIT (just-in-time) pro převod výrazu na nativní strojový kód pro vyšší výkon.</span><span class="sxs-lookup"><span data-stu-id="3777c-109">This allows .NET's just-in-time (JIT) compiler to convert the expression to native machine code for higher performance.</span></span>  
  
<span data-ttu-id="3777c-110">Generovaný jazyk MSIL však nelze uvolnit.</span><span class="sxs-lookup"><span data-stu-id="3777c-110">However, generated MSIL cannot be unloaded.</span></span> <span data-ttu-id="3777c-111">Jediným způsobem, jak uvolnit kód, je uvolnit celou doménu aplikace (tj. pro uvolnění všech kódů vaší aplikace).</span><span class="sxs-lookup"><span data-stu-id="3777c-111">The only way to unload code is to unload an entire application domain (that is, to unload all of your application's code.).</span></span> <span data-ttu-id="3777c-112">Po kompilaci regulárního výrazu s možností <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> nikdy neuvolní prostředky používané kompilovaným výrazem, a to ani v případě, že regulární výraz byl vytvořen pomocí objektu <xref:System.Text.RegularExpressions.Regex>, který je samotný uvolněn do uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="3777c-112">Effectively, once a regular expression is compiled with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option,   never releases the resources used by the compiled expression, even if the regular expression was created by a <xref:System.Text.RegularExpressions.Regex> object that is itself released to garbage collection.</span></span>  
  
 <span data-ttu-id="3777c-113">Musíte být opatrní, abyste omezili počet různých regulárních výrazů, které kompilujete s možností <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType>, abyste se vyhnuli využívání příliš velkého počtu prostředků.</span><span class="sxs-lookup"><span data-stu-id="3777c-113">You must be careful to limit the number of different regular expressions you compile with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option to avoid consuming too many resources.</span></span> <span data-ttu-id="3777c-114">Pokud aplikace musí používat velký nebo neohraničený počet regulárních výrazů, každý výraz by měl být interpretován, nikoli kompilován.</span><span class="sxs-lookup"><span data-stu-id="3777c-114">If an application must use a large or unbounded number of regular expressions, each expression should be interpreted, not compiled.</span></span> <span data-ttu-id="3777c-115">Pokud se však používá malý počet regulárních výrazů opakovaně, měly by být kompilovány s <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> pro lepší výkon.</span><span class="sxs-lookup"><span data-stu-id="3777c-115">However, if a small number of regular expressions are used repeatedly, they should be compiled with <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> for better performance.</span></span> <span data-ttu-id="3777c-116">Alternativou je použití předkompilovaných regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="3777c-116">An alternative is to use precompiled regular expressions.</span></span> <span data-ttu-id="3777c-117">Všechny vaše výrazy můžete zkompilovat do opakovaně použitelné knihovny DLL pomocí metody <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A>.</span><span class="sxs-lookup"><span data-stu-id="3777c-117">You can compile all of your expressions into a reusable DLL by using the <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A> method.</span></span> <span data-ttu-id="3777c-118">Tím se vyhnete nutnosti kompilovat za běhu a přitom stále využívat rychlost kompilovaných regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="3777c-118">This avoids the need to compile at runtime while still benefiting from the speed of compiled regular expressions.</span></span>  
  
## <a name="the-regular-expressions-cache"></a><span data-ttu-id="3777c-119">Mezipaměť regulárních výrazů</span><span class="sxs-lookup"><span data-stu-id="3777c-119">The Regular Expressions Cache</span></span>  
 <span data-ttu-id="3777c-120">Pro zlepšení výkonu modul regulárních výrazů udržuje mezipaměť zkompilovaných regulárních výrazů v celé aplikaci.</span><span class="sxs-lookup"><span data-stu-id="3777c-120">To improve performance, the regular expression engine maintains an application-wide cache of compiled regular expressions.</span></span> <span data-ttu-id="3777c-121">Mezipaměť ukládá vzory regulárních výrazů, které jsou používány pouze ve voláních statických metod.</span><span class="sxs-lookup"><span data-stu-id="3777c-121">The cache stores regular expression patterns that are used only in static method calls.</span></span> <span data-ttu-id="3777c-122">(Vzory regulárních výrazů dodávané do metod instancí nejsou ukládány do mezipaměti.) To brání nutnosti znovu analyzovat výraz do bajtového kódu vysoké úrovně pokaždé, když se použije.</span><span class="sxs-lookup"><span data-stu-id="3777c-122">(Regular expression patterns supplied to instance methods are not cached.) This avoids the need to reparse an expression into high-level byte code each time it is used.</span></span>  
  
 <span data-ttu-id="3777c-123">Maximální počet regulárních výrazů uložených v mezipaměti je určen hodnotou `static` (`Shared` ve Visual Basic) <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> Property.</span><span class="sxs-lookup"><span data-stu-id="3777c-123">The maximum number of cached regular expressions is determined by the value of the `static` (`Shared` in Visual Basic) <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="3777c-124">Ve výchozím nastavení modul regulárních výrazů ukládá do mezipaměti až 15 kompilovaných regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="3777c-124">By default, the regular expression engine caches up to 15 compiled regular expressions.</span></span> <span data-ttu-id="3777c-125">Pokud počet zkompilovaných regulárních výrazů překročí velikost mezipaměti, je poslední nepoužitý regulární výraz zahozen a nový regulární výraz je uložen do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="3777c-125">If the number of compiled regular expressions exceeds the cache size, the least recently used regular expression is discarded and the new regular expression is cached.</span></span>  
  
 <span data-ttu-id="3777c-126">Vaše aplikace může využít předkompilovaných regulárních výrazů jedním z následujících dvou způsobů:</span><span class="sxs-lookup"><span data-stu-id="3777c-126">Your application can take advantage of precompiled regular expressions in one of the following two ways:</span></span>  
  
- <span data-ttu-id="3777c-127">Pomocí statické metody objektu <xref:System.Text.RegularExpressions.Regex> k definování regulárního výrazu.</span><span class="sxs-lookup"><span data-stu-id="3777c-127">By using a static method of the <xref:System.Text.RegularExpressions.Regex> object to define the regular expression.</span></span> <span data-ttu-id="3777c-128">Pokud používáte vzor regulárního výrazu, který již byl definován v jiném volání statické metody, modul regulárních výrazů ho načte z mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="3777c-128">If you are using a regular expression pattern that has already been defined in another static method call, the regular expression engine will retrieve it from the cache.</span></span> <span data-ttu-id="3777c-129">V takovém případě bude modul kompilovat regulární výraz a přidat jej do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="3777c-129">If not, the engine will compile the regular expression and add it to the cache.</span></span>  
  
- <span data-ttu-id="3777c-130">Tím, že znovu použijete existující <xref:System.Text.RegularExpressions.Regex> objekt, pokud je třeba jeho vzor regulárního výrazu.</span><span class="sxs-lookup"><span data-stu-id="3777c-130">By reusing an existing <xref:System.Text.RegularExpressions.Regex> object as long as its regular expression pattern is needed.</span></span>  
  
 <span data-ttu-id="3777c-131">Kvůli režii při vytváření instancí objektů a kompilaci regulárních výrazů vytváření a rychlé zničení řady <xref:System.Text.RegularExpressions.Regex> objektů je velmi nákladný proces.</span><span class="sxs-lookup"><span data-stu-id="3777c-131">Because of the overhead of object instantiation and regular expression compilation, creating and rapidly destroying numerous <xref:System.Text.RegularExpressions.Regex> objects is a very expensive process.</span></span> <span data-ttu-id="3777c-132">Pro aplikace, které používají velký počet různých regulárních výrazů, můžete optimalizovat výkon pomocí volání statických `Regex` metod a případně zvýšením velikosti mezipaměti regulárních výrazů.</span><span class="sxs-lookup"><span data-stu-id="3777c-132">For applications that use a large number of different regular expressions, you can optimize performance by using calls to static `Regex` methods and possibly by increasing the size of the regular expression cache.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="3777c-133">Viz také:</span><span class="sxs-lookup"><span data-stu-id="3777c-133">See also</span></span>

- [<span data-ttu-id="3777c-134">Regulární výrazy .NET</span><span class="sxs-lookup"><span data-stu-id="3777c-134">.NET Regular Expressions</span></span>](../../../docs/standard/base-types/regular-expressions.md)
