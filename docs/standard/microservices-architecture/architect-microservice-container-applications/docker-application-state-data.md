---
title: Stav a data v aplikacích Dockeru
description: Stav a správu dat v aplikacích Dockeru. Instance Mikroslužeb jsou postradatelnými, ale DATA nejsou, jak o to postarají s využitím mikroslužeb.
ms.date: 09/20/2018
ms.openlocfilehash: 9d7b0ff0e73267c6b80be2f1c956c3b4eae140e2
ms.sourcegitcommit: 8699383914c24a0df033393f55db3369db728a7b
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 05/15/2019
ms.locfileid: "65641368"
---
# <a name="state-and-data-in-docker-applications"></a><span data-ttu-id="d1c8d-104">Stav a data v aplikacích Dockeru</span><span class="sxs-lookup"><span data-stu-id="d1c8d-104">State and data in Docker applications</span></span>

<span data-ttu-id="d1c8d-105">Ve většině případů si můžete představit kontejneru jako instance procesu.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-105">In most cases, you can think of a container as an instance of a process.</span></span> <span data-ttu-id="d1c8d-106">Proces nemá udržovat trvalý stav.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-106">A process doesn't maintain persistent state.</span></span> <span data-ttu-id="d1c8d-107">Zatímco kontejner může zapisovat do své místní úložiště, za předpokladu, že instance bude po neomezeně dlouho by například za předpokladu, že bude trvalý jednoho umístění v paměti.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-107">While a container can write to its local storage, assuming that an instance will be around indefinitely would be like assuming that a single location in memory will be durable.</span></span> <span data-ttu-id="d1c8d-108">Byste měli předpokládat, že Image kontejneru, jako jsou procesy, máte více instancí nebo nakonec budou ukončeny.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-108">You should assume that container images, like processes, have multiple instances or will eventually be killed.</span></span> <span data-ttu-id="d1c8d-109">Pokud jsou spravovaná pomocí orchestrátoru kontejneru byste měli předpokládat, že se může získat přesouvat z jednoho uzlu nebo virtuální počítač do druhého.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-109">If they're managed with a container orchestrator, you should assume that they might get moved from one node or VM to another.</span></span>

<span data-ttu-id="d1c8d-110">Následující řešení se používají ke správě trvalá data v aplikacích Dockeru:</span><span class="sxs-lookup"><span data-stu-id="d1c8d-110">The following solutions are used to manage persistent data in Docker applications:</span></span>

<span data-ttu-id="d1c8d-111">Z hostitele Docker jako [Docker svazky](https://docs.docker.com/engine/admin/volumes/):</span><span class="sxs-lookup"><span data-stu-id="d1c8d-111">From the Docker host, as [Docker Volumes](https://docs.docker.com/engine/admin/volumes/):</span></span>

- <span data-ttu-id="d1c8d-112">**Svazky** jsou uloženy v oblasti systému souborů hostitele, který je spravovaný nástrojem Docker.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-112">**Volumes** are stored in an area of the host filesystem that's managed by Docker.</span></span>

- <span data-ttu-id="d1c8d-113">**Navázat připojení** můžete namapovat na libovolnou složku v systému souborů hostitele, takže nemůže být řízený z procesu Docker přístup a může představovat bezpečnostní riziko, protože kontejner může přístup ke složkám na citlivé operačního systému.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-113">**Bind mounts** can map to any folder in the host filesystem, so access can't be controlled from Docker process and can pose a security risk as a container could access sensitive OS folders.</span></span>

- <span data-ttu-id="d1c8d-114">**připojí tmpfs** jsou například virtuální složky, které jenom v paměti hostiteli existují a nikdy se zapisují do systému souborů.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-114">**tmpfs mounts** are like virtual folders that only exist in the host's memory and are never written to the filesystem.</span></span>

<span data-ttu-id="d1c8d-115">Ze vzdáleného úložiště:</span><span class="sxs-lookup"><span data-stu-id="d1c8d-115">From remote storage:</span></span>

- <span data-ttu-id="d1c8d-116">[Azure Storage](https://azure.microsoft.com/documentation/services/storage/), který poskytuje geo Distribuovatelný storage, která poskytuje dobré dlouhodobým řešením trvalosti pro kontejnery.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-116">[Azure Storage](https://azure.microsoft.com/documentation/services/storage/), which provides geo-distributable storage, providing a good long-term persistence solution for containers.</span></span>

- <span data-ttu-id="d1c8d-117">Vzdálené relační databáze, jako jsou [Azure SQL Database](https://azure.microsoft.com/services/sql-database/) nebo databáze NoSQL, jako jsou [služby Azure Cosmos DB](https://docs.microsoft.com/azure/cosmos-db/introduction), nebo službám, jako je do mezipaměti [Redis](https://redis.io/).</span><span class="sxs-lookup"><span data-stu-id="d1c8d-117">Remote relational databases like [Azure SQL Database](https://azure.microsoft.com/services/sql-database/) or NoSQL databases like [Azure Cosmos DB](https://docs.microsoft.com/azure/cosmos-db/introduction), or cache services like [Redis](https://redis.io/).</span></span>

<span data-ttu-id="d1c8d-118">Z kontejneru Dockeru:</span><span class="sxs-lookup"><span data-stu-id="d1c8d-118">From the Docker container:</span></span>

> <span data-ttu-id="d1c8d-119">Docker nabízí funkci s názvem *překryv systému souborů*.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-119">Docker provides a feature named the *overlay file system*.</span></span> <span data-ttu-id="d1c8d-120">To implementuje úlohu kopírování na zápis, že úložiště aktualizované informace o kořenové systému souborů kontejneru.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-120">This implements a copy-on-write task that stores updated information to the root file system of the container.</span></span> <span data-ttu-id="d1c8d-121">Tyto informace je navíc k původní bitové kopie, na kterých je založena kontejner.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-121">That information is in addition to the original image on which the container is based.</span></span> <span data-ttu-id="d1c8d-122">Pokud kontejner je ze systému odstraněn, tyto změny budou ztraceny.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-122">If the container is deleted from the system, those changes are lost.</span></span> <span data-ttu-id="d1c8d-123">Proto i když je možné uložit stav kontejneru v rámci své místní úložiště, návrhu systému tomuto by vznikl konflikt se místní kontejner návrh, který ve výchozím nastavení jsou bezstavové.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-123">Therefore, while it's possible to save the state of a container within its local storage, designing a system around this would conflict with the premise of container design, which by default is stateless.</span></span>
>
> <span data-ttu-id="d1c8d-124">Dříve uvedených svazky Dockeru je však nyní preferovaný způsob, jak zpracování místních dat Dockeru.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-124">However, the previously introduced Docker Volumes is now the preferred way to handling local data Docker.</span></span> <span data-ttu-id="d1c8d-125">Pokud potřebujete další informace o službě storage v kontejnerech, zkontrolujte na [ovladačů úložiště Dockeru](https://docs.docker.com/storage/storagedriver/select-storage-driver/) a [o ovladačích úložiště](https://docs.docker.com/storage/storagedriver/).</span><span class="sxs-lookup"><span data-stu-id="d1c8d-125">If you need more information about storage in containers check on [Docker storage drivers](https://docs.docker.com/storage/storagedriver/select-storage-driver/) and [About storage drivers](https://docs.docker.com/storage/storagedriver/).</span></span>

<span data-ttu-id="d1c8d-126">Následují další podrobnosti o těchto možnostech:</span><span class="sxs-lookup"><span data-stu-id="d1c8d-126">The following provides more detail about these options:</span></span>

<span data-ttu-id="d1c8d-127">**Svazky** jsou adresáře, které jsou namapované na adresáře v kontejnerech z hostitelského operačního systému.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-127">**Volumes** are directories mapped from the host OS to directories in containers.</span></span> <span data-ttu-id="d1c8d-128">Pokud má kód v kontejneru přístup k adresáři, který ve skutečnosti je přístup k adresáři na hostitelském operačním systému.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-128">When code in the container has access to the directory, that access is actually to a directory on the host OS.</span></span> <span data-ttu-id="d1c8d-129">Tento adresář se neváže k době života kontejner sám o sobě, a adresář je spravovaný nástrojem Docker a izolovaně od základní funkce hostitelského počítače.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-129">This directory is not tied to the lifetime of the container itself, and the directory is managed by Docker and isolated from the core functionality of the host machine.</span></span> <span data-ttu-id="d1c8d-130">Proto datové svazky jsou navržené k uchování dat bez ohledu na jejich životnosti kontejneru.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-130">Thus, data volumes are designed to persist data independently of the life of the container.</span></span> <span data-ttu-id="d1c8d-131">Pokud odstraníte kontejner nebo bitové kopie z hostitele Docker dat uchovávaných v objem dat se neodstraní.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-131">If you delete a container or an image from the Docker host, the data persisted in the data volume isn't deleted.</span></span>

<span data-ttu-id="d1c8d-132">Svazky mohou být pojmenované a anonymní (výchozí).</span><span class="sxs-lookup"><span data-stu-id="d1c8d-132">Volumes can be named or anonymous (the default).</span></span> <span data-ttu-id="d1c8d-133">Vývoj jsou pojmenované svazky **kontejnery svazků Data** a usnadňují sdílení dat mezi kontejnery.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-133">Named volumes are the evolution of **Data Volume Containers** and make it easy to share data between containers.</span></span> <span data-ttu-id="d1c8d-134">Svazky také podporu ovladačů svazků, které vám umožňují ukládat data na vzdáleného hostitele, mezi další možnosti.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-134">Volumes also support volume drivers, that allow you to store data on remote hosts, among other options.</span></span>

<span data-ttu-id="d1c8d-135">**Navázat připojení** jsou k dispozici od dlouhou dobou a umožňují mapování všechny složky přípojný bod v kontejneru.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-135">**Bind mounts** are available since a long time ago and allow the mapping of any folder to a mount point in a container.</span></span> <span data-ttu-id="d1c8d-136">Vazby připojení mít další omezení než svazky a některé problémy se zabezpečením důležité, proto doporučujeme jsou svazky.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-136">Bind mounts have more limitations than volumes and some important security issues, so volumes are the recommended option.</span></span>

<span data-ttu-id="d1c8d-137">**připojí tmpfs** jsou v podstatě virtuální složky, které live pouze v paměti hostitele a nikdy se zapisují do systému souborů.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-137">**tmpfs mounts** are basically virtual folders that live only in the host's memory and are never written to the filesystem.</span></span> <span data-ttu-id="d1c8d-138">Jsou rychlé a zabezpečené, ale používají paměti a jsou určeny pouze pro dočasné data.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-138">They are fast and secure but use memory and are only meant for non-persistent data.</span></span>

<span data-ttu-id="d1c8d-139">Jak ukazuje obrázek 4 – 5, pravidelné Docker svazky mohou být uloženy mimo kontejnery sami, ale v rámci fyzické hranice hostitelský server nebo virtuální počítač.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-139">As shown in Figure 4-5, regular Docker volumes can be stored outside of the containers themselves but within the physical boundaries of the host server or VM.</span></span> <span data-ttu-id="d1c8d-140">Ale kontejnerů Dockeru od nemají přístup k svazku jeden hostitelský server nebo virtuální počítač do jiného.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-140">However, Docker containers can't access a volume from one host server or VM to another.</span></span> <span data-ttu-id="d1c8d-141">Jinými slovy tyto svazky, není možné spravovat data sdílena mezi kontejnerů, které běží na různých hostitelích, Docker, i když ho lze dosáhnout pomocí ovladače svazku, který podporuje vzdáleného hostitele.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-141">In other words, with these volumes, it isn't possible to manage data shared between containers that run on different Docker hosts, although it could be achieved with a volume driver that supports remote hosts.</span></span>

![<span data-ttu-id="d1c8d-142">Svazky lze sdílet mezi kontejnery, ale jenom do stejného hostitele, pokud nechcete použít vzdálené ovladač, který podporuje vzdálené hostitele.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-142">Volumes can be shared between containers, but only in the same host, unless you use a remote driver that supports remote hosts.</span></span> ](./media/image5.png)

<span data-ttu-id="d1c8d-143">**Obrázek 4 až 5**.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-143">**Figure 4-5**.</span></span> <span data-ttu-id="d1c8d-144">Svazky a externích zdrojů dat pro aplikace založené na kontejnerech</span><span class="sxs-lookup"><span data-stu-id="d1c8d-144">Volumes and external data sources for container-based applications</span></span>

<span data-ttu-id="d1c8d-145">Navíc když kontejnery Dockeru jsou spravovaná pomocí orchestrátoru, kontejnery může "Přesun" mezi hostiteli, v závislosti na optimalizace prováděné v clusteru.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-145">In addition, when Docker containers are managed by an orchestrator, containers might "move" between hosts, depending on the optimizations performed by the cluster.</span></span> <span data-ttu-id="d1c8d-146">Proto se nedoporučuje používat datové svazky pro obchodní data.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-146">Therefore, it isn't recommended that you use data volumes for business data.</span></span> <span data-ttu-id="d1c8d-147">Ale jsou dobrým mechanismus pro práci se soubory trasování, dočasné soubory, nebo podobné neovlivní konzistence obchodní data.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-147">But they're a good mechanism to work with trace files, temporal files, or similar that will not impact business data consistency.</span></span>

<span data-ttu-id="d1c8d-148">**Vzdálené zdroje dat. a mezipaměti** nástrojů, jako je Azure SQL Database, Azure Cosmos DB nebo vzdálené mezipaměti, jako jsou Redis je možné v stejným způsobem se používají při vývoji bez kontejnery kontejnerizovaných aplikací.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-148">**Remote data sources and cache** tools like Azure SQL Database, Azure Cosmos DB, or a remote cache like Redis can be used in containerized applications the same way they are used when developing without containers.</span></span> <span data-ttu-id="d1c8d-149">Toto je což je osvědčený způsob k ukládání dat obchodní aplikace.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-149">This is a proven way to store business application data.</span></span>

<span data-ttu-id="d1c8d-150">**Azure Storage.**</span><span class="sxs-lookup"><span data-stu-id="d1c8d-150">**Azure Storage.**</span></span> <span data-ttu-id="d1c8d-151">Obchodní data obvykle je nutné umístit do externího zdroje nebo databáze, jako je Azure Storage.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-151">Business data usually will need to be placed in external resources or databases, like Azure Storage.</span></span> <span data-ttu-id="d1c8d-152">Služba Azure Storage v konkrétní, poskytuje tyto služby v cloudu:</span><span class="sxs-lookup"><span data-stu-id="d1c8d-152">Azure Storage, in concrete, provides the following services in the cloud:</span></span>

- <span data-ttu-id="d1c8d-153">BLOB storage ukládá nestrukturované datové objekty.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-153">Blob storage stores unstructured object data.</span></span> <span data-ttu-id="d1c8d-154">Objekt blob může být jakýkoli typ textových nebo binárních dat, jako je například dokumentu nebo média (soubory obrázků, zvuku a videa).</span><span class="sxs-lookup"><span data-stu-id="d1c8d-154">A blob can be any type of text or binary data, such as document or media files (images, audio, and video files).</span></span> <span data-ttu-id="d1c8d-155">Úložiště objektů blob se taky označuje jako úložiště objektů.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-155">Blob storage is also referred to as Object storage.</span></span>

- <span data-ttu-id="d1c8d-156">File storage nabízí sdílené úložiště pro starší verze aplikace používající standardní protokol SMB.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-156">File storage offers shared storage for legacy applications using standard SMB protocol.</span></span> <span data-ttu-id="d1c8d-157">Virtuální počítače Azure a cloudové služby můžou sdílet souborová data mezi komponentami aplikace přes sdílené složky.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-157">Azure virtual machines and cloud services can share file data across application components via mounted shares.</span></span> <span data-ttu-id="d1c8d-158">Místní aplikace můžou k souborovým datům ve sdílené složce, prostřednictvím rozhraní REST API služby File.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-158">On-premises applications can access file data in a share via the File service REST API.</span></span>

- <span data-ttu-id="d1c8d-159">Table storage ukládá strukturované datové sady.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-159">Table storage stores structured datasets.</span></span> <span data-ttu-id="d1c8d-160">Úložiště Table je úložiště dat klíč atribut NoSQL, která umožňuje rychlý vývoj a přístup k velkým objemům dat.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-160">Table storage is a NoSQL key-attribute data store, which allows rapid development and fast access to large quantities of data.</span></span>

<span data-ttu-id="d1c8d-161">**Relační databáze a databáze NoSQL.**</span><span class="sxs-lookup"><span data-stu-id="d1c8d-161">**Relational databases and NoSQL databases.**</span></span> <span data-ttu-id="d1c8d-162">Existuje mnoho možností pro externí databáze, od relačních databází, jako jsou SQL Server, PostgreSQL, Oracle nebo NoSQL databáze jako služby Azure Cosmos DB, MongoDB atd. Tyto databáze nebudou vysvětlené v rámci tohoto průvodce, protože jsou v úplně jiných předmětu.</span><span class="sxs-lookup"><span data-stu-id="d1c8d-162">There are many choices for external databases, from relational databases like SQL Server, PostgreSQL, Oracle, or NoSQL databases like Azure Cosmos DB, MongoDB, etc. These databases are not going to be explained as part of this guide since they are in a completely different subject.</span></span>

>[!div class="step-by-step"]
><span data-ttu-id="d1c8d-163">[Předchozí](containerize-monolithic-applications.md)
>[další](service-oriented-architecture.md)</span><span class="sxs-lookup"><span data-stu-id="d1c8d-163">[Previous](containerize-monolithic-applications.md)
[Next](service-oriented-architecture.md)</span></span>
