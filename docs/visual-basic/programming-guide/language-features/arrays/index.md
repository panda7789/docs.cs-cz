---
title: Pole
ms.date: 12/06/2017
f1_keywords:
- vb.Array
helpviewer_keywords:
- arrays [Visual Basic]
- Visual Basic, arrays
ms.assetid: dbf29737-b589-4443-bee6-a27588d9c67e
ms.openlocfilehash: 5093f28f05c5b72294dce9a4e69723acafb31a9f
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/04/2020
ms.locfileid: "84413089"
---
# <a name="arrays-in-visual-basic"></a><span data-ttu-id="191c7-102">Pole v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="191c7-102">Arrays in Visual Basic</span></span>

<span data-ttu-id="191c7-103">Pole je sada hodnot, které jsou *výrazy, které*jsou logicky vzájemně propojené.</span><span class="sxs-lookup"><span data-stu-id="191c7-103">An array is a set of values, which are termed *elements*, that are logically related to each other.</span></span> <span data-ttu-id="191c7-104">Pole může například sestávat z počtu studentů v každé třídě v gramatické škole; Každý prvek pole je počet studentů v jedné třídě.</span><span class="sxs-lookup"><span data-stu-id="191c7-104">For example, an array may consist of the number of students in each grade in a grammar school; each element of the array is the number of students in a single grade.</span></span> <span data-ttu-id="191c7-105">Podobně může pole sestávat ze tříd studenta pro třídu; Každý prvek pole je jednou ze stupňů.</span><span class="sxs-lookup"><span data-stu-id="191c7-105">Similarly, an array may consist of a student's grades for a class; each element of the array is a single grade.</span></span>

<span data-ttu-id="191c7-106">Je možné, že jednotlivé proměnné budou ukládat jednotlivé datové položky.</span><span class="sxs-lookup"><span data-stu-id="191c7-106">It is possible individual variables to store each of our data items.</span></span> <span data-ttu-id="191c7-107">Například pokud naše aplikace analyzuje třídy studenta, můžeme použít samostatnou proměnnou pro každou třídu studenta, jako `englishGrade1` je například, `englishGrade2` atd. Tento přístup má tři hlavní omezení:</span><span class="sxs-lookup"><span data-stu-id="191c7-107">For example, if our application analyzes student grades, we can use a separate variable for each student's grade, such as `englishGrade1`, `englishGrade2`, etc. This approach has three major limitations:</span></span>

- <span data-ttu-id="191c7-108">V době návrhu musíme přesně zjistit, kolik stupňů potřebujeme zpracovat.</span><span class="sxs-lookup"><span data-stu-id="191c7-108">We have to know at design time exactly how many grades we have to handle.</span></span>
- <span data-ttu-id="191c7-109">Rychlé zpracování velkého počtu druhů se nepraktický.</span><span class="sxs-lookup"><span data-stu-id="191c7-109">Handling large numbers of grades quickly becomes unwieldy.</span></span> <span data-ttu-id="191c7-110">Tím dojde k tomu, že aplikace bude mnohem pravděpodobnější, že bude mít vážné chyby.</span><span class="sxs-lookup"><span data-stu-id="191c7-110">This in turn makes an application much more likely to have serious bugs.</span></span>
- <span data-ttu-id="191c7-111">Údržbu je obtížné.</span><span class="sxs-lookup"><span data-stu-id="191c7-111">It is difficult to maintain.</span></span> <span data-ttu-id="191c7-112">Každou novou třídu, kterou přidáváme, vyžaduje, aby se aplikace upravila, znovu zkompiluje a znovu nasadila.</span><span class="sxs-lookup"><span data-stu-id="191c7-112">Each new grade that we add requires that the application be modified, recompiled, and redeployed.</span></span>

<span data-ttu-id="191c7-113">Pomocí pole můžete odkazovat na tyto související hodnoty se stejným názvem a použít číslo, které se nazývá *index* nebo *dolní index* k identifikaci jednotlivého prvku na základě jeho pozice v poli.</span><span class="sxs-lookup"><span data-stu-id="191c7-113">By using an array, you can refer to these related values by the same name, and use a number that’s called an *index* or *subscript* to identify an individual element based on its position in the array.</span></span> <span data-ttu-id="191c7-114">Indexy rozsahu pole od 0 do jednoho menšího než celkový počet prvků v poli.</span><span class="sxs-lookup"><span data-stu-id="191c7-114">The indexes of an array range from 0 to one less than the total number of elements in the array.</span></span> <span data-ttu-id="191c7-115">Použijete-li syntaxi Visual Basic k definování velikosti pole, určíte jeho nejvyšší index, nikoli celkový počet prvků v poli.</span><span class="sxs-lookup"><span data-stu-id="191c7-115">When you use Visual Basic syntax to define the size of an array, you specify its highest index, not the total number of elements in the array.</span></span> <span data-ttu-id="191c7-116">S polem můžete pracovat jako s jednotkou a možnost iterovat své prvky je nepotřebné k tomu, abyste přesně věděli, kolik prvků obsahuje v době návrhu.</span><span class="sxs-lookup"><span data-stu-id="191c7-116">You can work with the array as a unit, and the ability to iterate its elements frees you from needing to know exactly how many elements it contains at design time.</span></span>

<span data-ttu-id="191c7-117">Některé rychlé příklady před vysvětlením:</span><span class="sxs-lookup"><span data-stu-id="191c7-117">Some quick examples before explanation:</span></span>

```vb
' Declare a single-dimension array of 5 numbers.
Dim numbers(4) As Integer

' Declare a single-dimension array and set its 4 values.
Dim numbers = New Integer() {1, 2, 4, 8}

' Change the size of an existing array to 16 elements and retain the current values.
ReDim Preserve numbers(15)

' Redefine the size of an existing array and reset the values.
ReDim numbers(15)

' Declare a 6 x 6 multidimensional array.
Dim matrix(5, 5) As Double

' Declare a 4 x 3 multidimensional array and set array element values.
Dim matrix = New Integer(3, 2) {{1, 2, 3}, {2, 3, 4}, {3, 4, 5}, {4, 5, 6}}

' Declare a jagged array
Dim sales()() As Double = New Double(11)() {}
```

## <a name="array-elements-in-a-simple-array"></a><span data-ttu-id="191c7-118">Prvky pole v jednoduchém poli</span><span class="sxs-lookup"><span data-stu-id="191c7-118">Array elements in a simple array</span></span>

<span data-ttu-id="191c7-119">Pojďme vytvořit pole s názvem `students` pro uložení počtu studentů v každé třídě v gramatické škole.</span><span class="sxs-lookup"><span data-stu-id="191c7-119">Let's create an array named `students` to store the number of students in each grade in a grammar school.</span></span> <span data-ttu-id="191c7-120">Indexy prvků jsou v rozsahu od 0 do 6.</span><span class="sxs-lookup"><span data-stu-id="191c7-120">The indexes of the elements range from 0 through 6.</span></span> <span data-ttu-id="191c7-121">Použití tohoto pole je jednodušší než deklarace sedmi proměnných.</span><span class="sxs-lookup"><span data-stu-id="191c7-121">Using this array is simpler than declaring seven variables.</span></span>

<span data-ttu-id="191c7-122">Následující ilustrace znázorňuje `students` pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-122">The following illustration shows the `students` array.</span></span> <span data-ttu-id="191c7-123">Pro každý prvek pole:</span><span class="sxs-lookup"><span data-stu-id="191c7-123">For each element of the array:</span></span>

- <span data-ttu-id="191c7-124">Index elementu reprezentuje třídu (index 0 představuje kindergarten).</span><span class="sxs-lookup"><span data-stu-id="191c7-124">The index of the element represents the grade (index 0 represents kindergarten).</span></span>

- <span data-ttu-id="191c7-125">Hodnota, která je obsažena v elementu, představuje počet studentů v této třídě.</span><span class="sxs-lookup"><span data-stu-id="191c7-125">The value that’s contained in the element represents the number of students in that grade.</span></span>

![Diagram znázorňující pole čísel studentů](./media/index/students-array-elements.gif)

<span data-ttu-id="191c7-127">Následující příklad obsahuje kód Visual Basic, který vytváří a používá pole:</span><span class="sxs-lookup"><span data-stu-id="191c7-127">The following example contains the Visual Basic code that creates and uses the array:</span></span>

[!code-vb[simple-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/simple-array.vb)]

<span data-ttu-id="191c7-128">Příklad provádí tři věci:</span><span class="sxs-lookup"><span data-stu-id="191c7-128">The example does three things:</span></span>

- <span data-ttu-id="191c7-129">Deklaruje `students` pole s sedmi prvky.</span><span class="sxs-lookup"><span data-stu-id="191c7-129">It declares a `students` array with seven elements.</span></span> <span data-ttu-id="191c7-130">Číslo `6` v deklaraci pole označuje poslední index v poli, je menší než počet prvků v poli.</span><span class="sxs-lookup"><span data-stu-id="191c7-130">The number `6` in the array declaration indicates the last index in the array; it is one less than the number of elements in the array.</span></span>
- <span data-ttu-id="191c7-131">Přiřadí hodnoty každému prvku v poli.</span><span class="sxs-lookup"><span data-stu-id="191c7-131">It assigns values to each element in the array.</span></span> <span data-ttu-id="191c7-132">K prvkům pole se dostanete pomocí názvu pole a zahrnutím indexu jednotlivého prvku do závorek.</span><span class="sxs-lookup"><span data-stu-id="191c7-132">Array elements are accessed by using the array name and including the index of the individual element in parentheses.</span></span>
- <span data-ttu-id="191c7-133">Obsahuje seznam všech hodnot pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-133">It lists each value of the array.</span></span> <span data-ttu-id="191c7-134">V příkladu se používá [`For`](../../../language-reference/statements/for-next-statement.md) příkaz pro přístup k jednotlivým prvkům pole podle jeho čísla indexu.</span><span class="sxs-lookup"><span data-stu-id="191c7-134">The example uses a [`For`](../../../language-reference/statements/for-next-statement.md) statement to access each element of the array by its index number.</span></span>

<span data-ttu-id="191c7-135">`students`Pole v předchozím příkladu je jednorozměrné pole, protože používá jeden index.</span><span class="sxs-lookup"><span data-stu-id="191c7-135">The `students` array in the preceding example is a one-dimensional array because it uses one index.</span></span> <span data-ttu-id="191c7-136">Pole, které používá více než jeden index nebo dolní index, je označováno jako *multidimenzionální*.</span><span class="sxs-lookup"><span data-stu-id="191c7-136">An array that uses more than one index or subscript is called *multidimensional*.</span></span> <span data-ttu-id="191c7-137">Další informace najdete v tématu zbývající část tohoto článku a [rozměry pole v Visual Basic](array-dimensions.md).</span><span class="sxs-lookup"><span data-stu-id="191c7-137">For more information, see the rest of this article and [Array Dimensions in Visual Basic](array-dimensions.md).</span></span>

## <a name="creating-an-array"></a><span data-ttu-id="191c7-138">Vytvoření pole</span><span class="sxs-lookup"><span data-stu-id="191c7-138">Creating an array</span></span>

<span data-ttu-id="191c7-139">Velikost pole můžete definovat několika způsoby:</span><span class="sxs-lookup"><span data-stu-id="191c7-139">You can define the size of an array in several ways:</span></span>

- <span data-ttu-id="191c7-140">Můžete určit velikost při deklaraci pole:</span><span class="sxs-lookup"><span data-stu-id="191c7-140">You can specify the size when the array is declared:</span></span>

  [!code-vb[creating1](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#1)]

- <span data-ttu-id="191c7-141">Můžete použít `New` klauzuli k poskytnutí velikosti pole při jeho vytvoření:</span><span class="sxs-lookup"><span data-stu-id="191c7-141">You can use a `New` clause to supply the size of an array when it’s created:</span></span>

  [!code-vb[creating2](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#2)]

<span data-ttu-id="191c7-142">Máte-li existující pole, lze jeho velikost změnit pomocí [`ReDim`](../../../language-reference/statements/redim-statement.md) příkazu.</span><span class="sxs-lookup"><span data-stu-id="191c7-142">If you have an existing array, you can redefine its size by using the [`ReDim`](../../../language-reference/statements/redim-statement.md) statement.</span></span> <span data-ttu-id="191c7-143">Můžete určit, že `ReDim` příkaz zachová hodnoty, které jsou v poli, nebo můžete určit, že vytvoří prázdné pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-143">You can specify that the `ReDim` statement keep the values that are in the array, or you can specify that it create an empty array.</span></span> <span data-ttu-id="191c7-144">Následující příklad ukazuje různá použití `ReDim` příkazu pro úpravu velikosti existujícího pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-144">The following example shows different uses of the `ReDim` statement to modify the size of an existing array.</span></span>

[!code-vb[redimensioning](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#3)]

<span data-ttu-id="191c7-145">Další informace naleznete v [příkazu ReDim](../../../language-reference/statements/redim-statement.md).</span><span class="sxs-lookup"><span data-stu-id="191c7-145">For more information, see the [ReDim Statement](../../../language-reference/statements/redim-statement.md).</span></span>

## <a name="storing-values-in-an-array"></a><span data-ttu-id="191c7-146">Ukládání hodnot do pole</span><span class="sxs-lookup"><span data-stu-id="191c7-146">Storing values in an array</span></span>

<span data-ttu-id="191c7-147">Ke každému umístění v poli můžete přistupovat pomocí indexu typu `Integer` .</span><span class="sxs-lookup"><span data-stu-id="191c7-147">You can access each location in an array by using an index of type `Integer`.</span></span> <span data-ttu-id="191c7-148">Můžete uložit a načíst hodnoty v poli odkazem na každé umístění pole pomocí jeho indexu uzavřeného v závorkách.</span><span class="sxs-lookup"><span data-stu-id="191c7-148">You can store and retrieve values in an array by referencing each array location by using its index enclosed in parentheses.</span></span> <span data-ttu-id="191c7-149">Indexy multidimenzionálních polí jsou odděleny čárkami (,).</span><span class="sxs-lookup"><span data-stu-id="191c7-149">Indexes for multidimensional arrays are separated by commas (,).</span></span> <span data-ttu-id="191c7-150">Pro každou dimenzi pole potřebujete jeden index.</span><span class="sxs-lookup"><span data-stu-id="191c7-150">You need one index for each array dimension.</span></span>

<span data-ttu-id="191c7-151">Následující příklad ukazuje některé příkazy, které ukládají a načítají hodnoty v polích.</span><span class="sxs-lookup"><span data-stu-id="191c7-151">The following example shows some statements that store and retrieve values in arrays.</span></span>

[!code-vb[store-and-retrieve](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/store-and-retrieve.vb)]

## <a name="populating-an-array-with-array-literals"></a><span data-ttu-id="191c7-152">Naplnění pole pomocí literálů pole</span><span class="sxs-lookup"><span data-stu-id="191c7-152">Populating an array with array literals</span></span>

<span data-ttu-id="191c7-153">Pomocí literálu pole lze naplnit pole počáteční sadou hodnot ve stejnou dobu, kterou vytvoříte.</span><span class="sxs-lookup"><span data-stu-id="191c7-153">By using an array literal, you can populate an array with an initial set of values at the same time that you create it.</span></span> <span data-ttu-id="191c7-154">Literál pole se skládá ze seznamu hodnot oddělených čárkami, které jsou uzavřeny ve složených závorkách ( `{}` ).</span><span class="sxs-lookup"><span data-stu-id="191c7-154">An array literal consists of a list of comma-separated values that are enclosed in braces (`{}`).</span></span>

<span data-ttu-id="191c7-155">Při vytváření pole pomocí literálu pole můžete buď zadat typ pole nebo použít odvození typu k určení typu pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-155">When you create an array by using an array literal, you can either supply the array type or use type inference to determine the array type.</span></span> <span data-ttu-id="191c7-156">Následující příklad ukazuje obě možnosti.</span><span class="sxs-lookup"><span data-stu-id="191c7-156">The following example shows both options.</span></span>

[!code-vb[create-with-literals](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#4)]

<span data-ttu-id="191c7-157">Při použití odvození typu je typ pole určen *dominantním typem* v seznamu hodnot literálů.</span><span class="sxs-lookup"><span data-stu-id="191c7-157">When you use type inference, the type of the array is determined by the *dominant type* in the list of literal values.</span></span> <span data-ttu-id="191c7-158">Dominantní typ je typ, na který lze rozšířit všechny ostatní typy v poli.</span><span class="sxs-lookup"><span data-stu-id="191c7-158">The dominant type is the type to which all other types in the array can widen.</span></span> <span data-ttu-id="191c7-159">Pokud tento jedinečný typ nelze určit, dominantní typ je jedinečný typ, na který mohou být zúženy všechny ostatní typy v poli.</span><span class="sxs-lookup"><span data-stu-id="191c7-159">If this unique type can’t be determined, the dominant type is the unique type to which all other types in the array can narrow.</span></span> <span data-ttu-id="191c7-160">Pokud ani jeden z těchto jedinečných typů nelze určit, dominantní typ je `Object` .</span><span class="sxs-lookup"><span data-stu-id="191c7-160">If neither of these unique types can be determined, the dominant type is `Object`.</span></span> <span data-ttu-id="191c7-161">Například pokud seznam hodnot, které jsou zadány do literálu pole, obsahuje hodnoty typu `Integer` , `Long` , a `Double` výsledné pole je typu `Double` .</span><span class="sxs-lookup"><span data-stu-id="191c7-161">For example, if the list of values that’s supplied to the array literal contains values of type `Integer`, `Long`, and `Double`, the resulting array is of type `Double`.</span></span> <span data-ttu-id="191c7-162">Protože `Integer` a `Long` rozšíření pouze na `Double` , `Double` je dominantní typ.</span><span class="sxs-lookup"><span data-stu-id="191c7-162">Because `Integer` and `Long` widen only to `Double`, `Double` is the dominant type.</span></span> <span data-ttu-id="191c7-163">Další informace najdete v tématu [rozšiřování a zúžení převodů](../data-types/widening-and-narrowing-conversions.md).</span><span class="sxs-lookup"><span data-stu-id="191c7-163">For more information, see [Widening and Narrowing Conversions](../data-types/widening-and-narrowing-conversions.md).</span></span>

> [!NOTE]
> <span data-ttu-id="191c7-164">Odvození typu lze použít pouze pro pole, která jsou definována jako lokální proměnné v rámci člena typu.</span><span class="sxs-lookup"><span data-stu-id="191c7-164">You can use type inference only for arrays that are defined as local variables in a type member.</span></span> <span data-ttu-id="191c7-165">Pokud není k dispozici definice explicitního typu, pole definovaná s literály pole na úrovni třídy jsou typu `Object[]` .</span><span class="sxs-lookup"><span data-stu-id="191c7-165">If an explicit type definition is absent, arrays defined with array literals at the class level are of type `Object[]`.</span></span> <span data-ttu-id="191c7-166">Další informace naleznete v tématu [odvození místního typu](../variables/local-type-inference.md).</span><span class="sxs-lookup"><span data-stu-id="191c7-166">For more information, see [Local type inference](../variables/local-type-inference.md).</span></span>

<span data-ttu-id="191c7-167">Všimněte si, že předchozí příklad definuje `values` jako pole typu, `Double` i když všechny literály pole jsou typu `Integer` .</span><span class="sxs-lookup"><span data-stu-id="191c7-167">Note that the previous example defines `values` as an array of type `Double` even though all the array literals are of type `Integer`.</span></span> <span data-ttu-id="191c7-168">Toto pole lze vytvořit, protože hodnoty v literálu pole lze rozšířit na `Double` hodnoty.</span><span class="sxs-lookup"><span data-stu-id="191c7-168">You can create this array because the values in the array literal can widen to `Double` values.</span></span>

<span data-ttu-id="191c7-169">Můžete také vytvořit a naplnit multidimenzionální pole pomocí *literálů vnořeného pole*.</span><span class="sxs-lookup"><span data-stu-id="191c7-169">You can also create and populate a multidimensional array by using *nested array literals*.</span></span> <span data-ttu-id="191c7-170">Literály vnořeného pole musí obsahovat řadu dimenzí, které jsou konzistentní s výsledným polem.</span><span class="sxs-lookup"><span data-stu-id="191c7-170">Nested array literals must have a number of dimensions that’s consistent with the resulting array.</span></span> <span data-ttu-id="191c7-171">Následující příklad vytvoří dvojrozměrné pole celých čísel pomocí literálů vnořeného pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-171">The following example creates a two-dimensional array of integers by using nested array literals.</span></span>

[!code-vb[nested-array-literals](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#5)]

<span data-ttu-id="191c7-172">Při použití literálů vnořeného pole k vytvoření a naplnění pole dojde k chybě, pokud se počet prvků v literálech vnořeného pole neshoduje.</span><span class="sxs-lookup"><span data-stu-id="191c7-172">When using nested array literals to create and populate an array, an error occurs if the number of elements in the nested array literals don't match.</span></span> <span data-ttu-id="191c7-173">K chybě dojde také v případě, že explicitně deklarujete proměnnou pole tak, aby měla jiný počet rozměrů než literály pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-173">An error also occurs if you explicitly declare the array variable to have a different number of dimensions than the array literals.</span></span>

<span data-ttu-id="191c7-174">Stejně jako u jednorozměrného pole lze při vytváření multidimenzionálního pole s literály vnořeného pole spoléhat na odvození typu.</span><span class="sxs-lookup"><span data-stu-id="191c7-174">Just as you can for one-dimensional arrays, you can rely on type inference when creating a multidimensional array with nested array literals.</span></span> <span data-ttu-id="191c7-175">Odvozený typ je dominantní typ pro všechny hodnoty ve všech literálech pole pro všechny úrovně vnoření.</span><span class="sxs-lookup"><span data-stu-id="191c7-175">The inferred type is the dominant type for all the values in all the array literals for all nesting level.</span></span> <span data-ttu-id="191c7-176">Následující příklad vytvoří dvourozměrné pole typu `Double[,]` z hodnot, které jsou typu `Integer` a `Double` .</span><span class="sxs-lookup"><span data-stu-id="191c7-176">The following example creates a two-dimensional array of type `Double[,]` from values that are of type `Integer` and `Double`.</span></span>

[!code-vb[nested-type-inference](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#6)]

<span data-ttu-id="191c7-177">Další příklady naleznete v tématu [How to: Initialize a Array Variable in Visual Basic](how-to-initialize-an-array-variable.md).</span><span class="sxs-lookup"><span data-stu-id="191c7-177">For additional examples, see [How to: Initialize an Array Variable in Visual Basic](how-to-initialize-an-array-variable.md).</span></span>

## <a name="iterating-through-an-array"></a><span data-ttu-id="191c7-178">Iterace prostřednictvím pole</span><span class="sxs-lookup"><span data-stu-id="191c7-178">Iterating through an array</span></span>

<span data-ttu-id="191c7-179">Při iteraci přes pole přistupujete ke každému prvku v poli z nejnižší index na nejvyšší nebo od nejvyšších po nejnižší.</span><span class="sxs-lookup"><span data-stu-id="191c7-179">When you iterate through an array, you access each element in the array from the lowest index to the highest or from the highest to the lowest.</span></span> <span data-ttu-id="191c7-180">Obvykle použijte buď [pro... Další příkaz](../../../language-reference/statements/for-next-statement.md) nebo [pro každý... Další příkaz](../../../language-reference/statements/for-each-next-statement.md) pro iteraci prvky pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-180">Typically, use either the [For...Next Statement](../../../language-reference/statements/for-next-statement.md) or the [For Each...Next Statement](../../../language-reference/statements/for-each-next-statement.md) to iterate through the elements of an array.</span></span> <span data-ttu-id="191c7-181">Pokud neznáte horní meze pole, můžete zavolat <xref:System.Array.GetUpperBound%2A?displayProperty=nameWithType> metodu pro získání nejvyšší hodnoty indexu.</span><span class="sxs-lookup"><span data-stu-id="191c7-181">When you don't know the upper bounds of the array, you can call the <xref:System.Array.GetUpperBound%2A?displayProperty=nameWithType> method to get the highest value of the index.</span></span> <span data-ttu-id="191c7-182">I když nejnižší hodnota indexu je skoro vždycky 0, můžete zavolat <xref:System.Array.GetLowerBound%2A?displayProperty=nameWithType> metodu, abyste získali nejnižší hodnotu indexu.</span><span class="sxs-lookup"><span data-stu-id="191c7-182">Although lowest index value is almost always 0, you can call the <xref:System.Array.GetLowerBound%2A?displayProperty=nameWithType> method to get the lowest value of the index.</span></span>

<span data-ttu-id="191c7-183">Následující příklad projde jednorozměrné pole pomocí [`For...Next`](../../../language-reference/statements/for-next-statement.md) příkazu.</span><span class="sxs-lookup"><span data-stu-id="191c7-183">The following example iterates through a one-dimensional array by using the [`For...Next`](../../../language-reference/statements/for-next-statement.md) statement.</span></span>

[!code-vb[iterate-one-dimensional-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate1d.vb)]

<span data-ttu-id="191c7-184">Následující příklad provede iteraci multidimenzionálního pole pomocí [`For...Next`](../../../language-reference/statements/for-next-statement.md) příkazu.</span><span class="sxs-lookup"><span data-stu-id="191c7-184">The following example iterates through a multidimensional array by using a [`For...Next`](../../../language-reference/statements/for-next-statement.md) statement.</span></span> <span data-ttu-id="191c7-185"><xref:System.Array.GetUpperBound%2A>Metoda má parametr, který určuje dimenzi.</span><span class="sxs-lookup"><span data-stu-id="191c7-185">The <xref:System.Array.GetUpperBound%2A> method has a parameter that specifies the dimension.</span></span> <span data-ttu-id="191c7-186">`GetUpperBound(0)`Vrátí nejvyšší index prvního rozměru a `GetUpperBound(1)` vrátí nejvyšší index druhé dimenze.</span><span class="sxs-lookup"><span data-stu-id="191c7-186">`GetUpperBound(0)` returns the highest index of the first dimension, and `GetUpperBound(1)` returns the highest index of the second dimension.</span></span>

[!code-vb[iterate-two-dimensional-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate2d.vb)]

<span data-ttu-id="191c7-187">Následující příklad používá [pro každý... Další příkaz](../../../language-reference/statements/for-each-next-statement.md)pro iterování pomocí jednorozměrného pole a dvojrozměrného pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-187">The following example uses a [For Each...Next Statement](../../../language-reference/statements/for-each-next-statement.md)to iterate through a one-dimensional array and a two-dimensional array.</span></span>

[!code-vb[iterate-for-each-next](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate-for-each-next.vb)]

## <a name="array-size"></a><span data-ttu-id="191c7-188">Velikost pole</span><span class="sxs-lookup"><span data-stu-id="191c7-188">Array size</span></span>

<span data-ttu-id="191c7-189">Velikost pole je součinem délek všech jeho rozměrů.</span><span class="sxs-lookup"><span data-stu-id="191c7-189">The size of an array is the product of the lengths of all its dimensions.</span></span> <span data-ttu-id="191c7-190">Představuje celkový počet prvků, které jsou aktuálně obsaženy v poli.</span><span class="sxs-lookup"><span data-stu-id="191c7-190">It represents the total number of elements currently contained in the array.</span></span>  <span data-ttu-id="191c7-191">Například následující příklad deklaruje dvojrozměrné pole se čtyřmi prvky v každé dimenzi.</span><span class="sxs-lookup"><span data-stu-id="191c7-191">For example, the following example declares a 2-dimensional array with four elements in each dimension.</span></span> <span data-ttu-id="191c7-192">Jak ukazuje výstup z příkladu, je velikost pole 16 (nebo (3 + 1) \* (3 + 1).</span><span class="sxs-lookup"><span data-stu-id="191c7-192">As the output from the example shows, the array's size is 16 (or (3 + 1) \* (3 + 1).</span></span>

[!code-vb[array-size](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-size.vb)]

> [!NOTE]
> <span data-ttu-id="191c7-193">Tato diskuze o velikosti pole se nevztahuje na vícenásobná pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-193">This discussion of array size does not apply to jagged arrays.</span></span> <span data-ttu-id="191c7-194">Informace o vícenásobných polích a určení velikosti vícenásobného pole naleznete v části [vícenásobná pole](#jagged-arrays) .</span><span class="sxs-lookup"><span data-stu-id="191c7-194">For information on jagged arrays and determining the size of a jagged array, see the [Jagged arrays](#jagged-arrays) section.</span></span>

<span data-ttu-id="191c7-195">Velikost pole můžete najít pomocí <xref:System.Array.Length%2A?displayProperty=nameWithType> Vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="191c7-195">You can find the size of an array by using the <xref:System.Array.Length%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="191c7-196">Délku každé dimenze multidimenzionálního pole lze najít pomocí <xref:System.Array.GetLength%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="191c7-196">You can find the length of each dimension of a multidimensional array by using the <xref:System.Array.GetLength%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="191c7-197">Můžete změnit velikost proměnné pole přiřazením nového objektu Array k poli nebo pomocí příkazu [ `ReDim` příkazu](../../../language-reference/statements/redim-statement.md) .</span><span class="sxs-lookup"><span data-stu-id="191c7-197">You can resize an array variable by assigning a new array object to it or by using the [`ReDim` Statement](../../../language-reference/statements/redim-statement.md) statement.</span></span> <span data-ttu-id="191c7-198">Následující příklad používá `ReDim` příkaz ke změně pole 100 elementu na pole 51-element.</span><span class="sxs-lookup"><span data-stu-id="191c7-198">The following example uses the `ReDim` statement to change a 100-element array to a 51-element array.</span></span>

[!code-vb[resize-an-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-size2.vb)]

<span data-ttu-id="191c7-199">Při práci s velikostí pole je potřeba mít na paměti několik věcí.</span><span class="sxs-lookup"><span data-stu-id="191c7-199">There are several things to keep in mind when dealing with the size of an array.</span></span>

|||
|---|---|
|<span data-ttu-id="191c7-200">Délka dimenze</span><span class="sxs-lookup"><span data-stu-id="191c7-200">Dimension Length</span></span>|<span data-ttu-id="191c7-201">Index každé dimenze je založen na 0, což znamená, že je rozsah od 0 do horní meze.</span><span class="sxs-lookup"><span data-stu-id="191c7-201">The index of each dimension is 0-based, which means it ranges from 0 to its upper bound.</span></span> <span data-ttu-id="191c7-202">Proto je délka dané dimenze jedna větší než deklarovaná horní mez této dimenze.</span><span class="sxs-lookup"><span data-stu-id="191c7-202">Therefore, the length of a given dimension is one greater than the declared upper bound of that dimension.</span></span>|
|<span data-ttu-id="191c7-203">Omezení délky</span><span class="sxs-lookup"><span data-stu-id="191c7-203">Length Limits</span></span>|<span data-ttu-id="191c7-204">Délka každé dimenze pole je omezená na maximální hodnotu `Integer` datového typu, což je <xref:System.Int32.MaxValue?displayProperty=nameWithType> nebo (2 ^ 31)-1.</span><span class="sxs-lookup"><span data-stu-id="191c7-204">The length of every dimension of an array is limited to the maximum value of the `Integer` data type, which is <xref:System.Int32.MaxValue?displayProperty=nameWithType> or (2 ^ 31) - 1.</span></span> <span data-ttu-id="191c7-205">Celková velikost pole je ale také omezená pamětí, která je k dispozici ve vašem systému.</span><span class="sxs-lookup"><span data-stu-id="191c7-205">However, the total size of an array is also limited by the memory available on your system.</span></span> <span data-ttu-id="191c7-206">Pokud se pokusíte inicializovat pole, které překračuje množství dostupné paměti, modul runtime vyvolá <xref:System.OutOfMemoryException> .</span><span class="sxs-lookup"><span data-stu-id="191c7-206">If you attempt to initialize an array that exceeds the amount of available memory, the runtime throws an <xref:System.OutOfMemoryException>.</span></span>|
|<span data-ttu-id="191c7-207">Velikost a velikost elementu</span><span class="sxs-lookup"><span data-stu-id="191c7-207">Size and Element Size</span></span>|<span data-ttu-id="191c7-208">Velikost pole je nezávislá na datovém typu jeho prvků.</span><span class="sxs-lookup"><span data-stu-id="191c7-208">An array's size is independent of the data type of its elements.</span></span> <span data-ttu-id="191c7-209">Velikost vždy představuje celkový počet prvků, nikoli počet bajtů, které spotřebovávají v paměti.</span><span class="sxs-lookup"><span data-stu-id="191c7-209">The size always represents the total number of elements, not the number of bytes that they consume in memory.</span></span>|
|<span data-ttu-id="191c7-210">Memory Consumption</span><span class="sxs-lookup"><span data-stu-id="191c7-210">Memory Consumption</span></span>|<span data-ttu-id="191c7-211">Není bezpečné dělat žádné předpoklady týkající se toho, jak je pole Uloženo v paměti.</span><span class="sxs-lookup"><span data-stu-id="191c7-211">It is not safe to make any assumptions regarding how an array is stored in memory.</span></span> <span data-ttu-id="191c7-212">Úložiště se liší na platformách různých šířek dat, takže stejné pole může spotřebovat víc paměti v 64 systému, než na 32 systému.</span><span class="sxs-lookup"><span data-stu-id="191c7-212">Storage varies on platforms of different data widths, so the same array can consume more memory on a 64-bit system than on a 32-bit system.</span></span> <span data-ttu-id="191c7-213">V závislosti na konfiguraci systému při inicializaci pole může modul CLR (Common Language Runtime) přiřadit úložiště buď k zabalení prvků co nejblíže, nebo pro jejich zarovnání na hranice přirozeného hardwaru.</span><span class="sxs-lookup"><span data-stu-id="191c7-213">Depending on system configuration when you initialize an array, the common language runtime (CLR) can assign storage either to pack elements as close together as possible, or to align them all on natural hardware boundaries.</span></span> <span data-ttu-id="191c7-214">Pole také vyžaduje režijní náklady na úložiště pro informace o ovládacím prvku a tato režie se zvyšuje s každou přidanou dimenzí.</span><span class="sxs-lookup"><span data-stu-id="191c7-214">Also, an array requires a storage overhead for its control information, and this overhead increases with each added dimension.</span></span>|

## <a name="the-array-type"></a><span data-ttu-id="191c7-215">Typ pole</span><span class="sxs-lookup"><span data-stu-id="191c7-215">The array type</span></span>

<span data-ttu-id="191c7-216">Každé pole má datový typ, který se liší od datového typu jeho prvků.</span><span class="sxs-lookup"><span data-stu-id="191c7-216">Every array has a data type, which differs from the data type of its elements.</span></span> <span data-ttu-id="191c7-217">Pro všechna pole není k dispozici žádný jediný datový typ.</span><span class="sxs-lookup"><span data-stu-id="191c7-217">There is no single data type for all arrays.</span></span> <span data-ttu-id="191c7-218">Místo toho se datový typ pole určuje podle počtu rozměrů nebo *pořadí*, pole a datového typu prvků v poli.</span><span class="sxs-lookup"><span data-stu-id="191c7-218">Instead, the data type of an array is determined by the number of dimensions, or *rank*, of the array, and the data type of the elements in the array.</span></span> <span data-ttu-id="191c7-219">Dvě proměnné pole jsou stejného datového typu pouze v případě, že mají stejný rozsah a jejich prvky mají stejný datový typ.</span><span class="sxs-lookup"><span data-stu-id="191c7-219">Two array variables are of the same data type only when they have the same rank and their elements have the same data type.</span></span> <span data-ttu-id="191c7-220">Délky rozměrů pole neovlivňují datový typ pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-220">The lengths of the dimensions of an array do not influence the array data type.</span></span>

<span data-ttu-id="191c7-221">Každé pole dědí z <xref:System.Array?displayProperty=nameWithType> třídy a můžete deklarovat proměnnou, která má být typu `Array` , ale nelze vytvořit pole typu `Array` .</span><span class="sxs-lookup"><span data-stu-id="191c7-221">Every array inherits from the <xref:System.Array?displayProperty=nameWithType> class, and you can declare a variable to be of type `Array`, but you cannot create an array of type `Array`.</span></span> <span data-ttu-id="191c7-222">Například Přestože následující kód deklaruje `arr` proměnnou jako typ `Array` a volá <xref:System.Array.CreateInstance%2A?displayProperty=nameWithType> metodu pro vytvoření instance pole, typ pole se projeví jako objekt [].</span><span class="sxs-lookup"><span data-stu-id="191c7-222">For example, although the following code declares the `arr` variable to be of type `Array` and calls the <xref:System.Array.CreateInstance%2A?displayProperty=nameWithType> method to instantiate the array, the array's type proves to be Object[].</span></span>

[!code-vb[array-class](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-class.vb)]

<span data-ttu-id="191c7-223">[Příkaz ReDim](../../../language-reference/statements/redim-statement.md) nelze také použít pro proměnnou deklarovanou jako typ `Array` .</span><span class="sxs-lookup"><span data-stu-id="191c7-223">Also, the [ReDim Statement](../../../language-reference/statements/redim-statement.md) cannot operate on a variable declared as type `Array`.</span></span> <span data-ttu-id="191c7-224">Z těchto důvodů a pro bezpečnost typů je vhodné deklarovat každé pole jako konkrétní typ.</span><span class="sxs-lookup"><span data-stu-id="191c7-224">For these reasons, and for type safety, it is advisable to declare every array as a specific type.</span></span>

<span data-ttu-id="191c7-225">Datový typ pole nebo jeho prvků můžete zjistit několika způsoby.</span><span class="sxs-lookup"><span data-stu-id="191c7-225">You can find out the data type of either an array or its elements in several ways.</span></span>

- <span data-ttu-id="191c7-226">Můžete zavolat <xref:System.Object.GetType%2A> metodu na proměnnou a získat tak <xref:System.Type> objekt, který představuje běhový typ proměnné.</span><span class="sxs-lookup"><span data-stu-id="191c7-226">You can call the <xref:System.Object.GetType%2A> method on the variable to get a <xref:System.Type> object that represents the run-time type of the variable.</span></span> <span data-ttu-id="191c7-227"><xref:System.Type>Objekt obsahuje rozsáhlé informace o vlastnostech a metodách.</span><span class="sxs-lookup"><span data-stu-id="191c7-227">The <xref:System.Type> object holds extensive information in its properties and methods.</span></span>
- <span data-ttu-id="191c7-228">Proměnnou můžete předat do <xref:Microsoft.VisualBasic.Information.TypeName%2A> funkce a získat tak `String` název typu za běhu.</span><span class="sxs-lookup"><span data-stu-id="191c7-228">You can pass the variable to the <xref:Microsoft.VisualBasic.Information.TypeName%2A> function to get a `String` with the name of run-time type.</span></span>

<span data-ttu-id="191c7-229">Následující příklad volá jak `GetType` metodu, tak `TypeName` funkci pro určení typu pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-229">The following example calls the both the `GetType` method and the `TypeName` function to determine the type of an array.</span></span> <span data-ttu-id="191c7-230">Typ pole je `Byte(,)` .</span><span class="sxs-lookup"><span data-stu-id="191c7-230">The array type is `Byte(,)`.</span></span> <span data-ttu-id="191c7-231">Všimněte si, že <xref:System.Type.BaseType%2A?displayProperty=nameWithType> vlastnost také označuje, že základní typ bajtového pole je <xref:System.Array> Třída.</span><span class="sxs-lookup"><span data-stu-id="191c7-231">Note that the <xref:System.Type.BaseType%2A?displayProperty=nameWithType> property also indicates that the base type of the byte array is the <xref:System.Array> class.</span></span>

[!code-vb[array-type](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-type.vb)]

## <a name="arrays-as-return-values-and-parameters"></a><span data-ttu-id="191c7-232">Pole jako návratové hodnoty a parametry</span><span class="sxs-lookup"><span data-stu-id="191c7-232">Arrays as return values and parameters</span></span>

<span data-ttu-id="191c7-233">Chcete-li vrátit pole z `Function` procedury, zadejte datový typ Array a počet dimenzí jako návratový typ [příkazu funkce](../../../language-reference/statements/function-statement.md).</span><span class="sxs-lookup"><span data-stu-id="191c7-233">To return an array from a `Function` procedure, specify the array data type and the number of dimensions as the return type of the [Function Statement](../../../language-reference/statements/function-statement.md).</span></span> <span data-ttu-id="191c7-234">V rámci funkce deklarujte místní proměnnou pole se stejným datovým typem a počtem rozměrů.</span><span class="sxs-lookup"><span data-stu-id="191c7-234">Within the function, declare a local array variable with same data type and number of dimensions.</span></span> <span data-ttu-id="191c7-235">V [příkazu return](../../../language-reference/statements/return-statement.md)zahrňte proměnnou místního pole bez závorek.</span><span class="sxs-lookup"><span data-stu-id="191c7-235">In the [Return Statement](../../../language-reference/statements/return-statement.md), include the local array variable without parentheses.</span></span>

<span data-ttu-id="191c7-236">Chcete-li určit pole jako parametr pro `Sub` `Function` proceduru nebo, definujte parametr jako pole se zadaným datovým typem a počtem dimenzí.</span><span class="sxs-lookup"><span data-stu-id="191c7-236">To specify an array as a parameter to a `Sub` or `Function` procedure, define the parameter as an array with a specified data type and number of dimensions.</span></span> <span data-ttu-id="191c7-237">V volání procedury předejte proměnnou pole se stejným datovým typem a počtem dimenzí.</span><span class="sxs-lookup"><span data-stu-id="191c7-237">In the call to the procedure, pass an array variable with the same data type and number of dimensions.</span></span>

<span data-ttu-id="191c7-238">V následujícím příkladu `GetNumbers` funkce vrací jednorozměrné `Integer()` pole typu `Integer` .</span><span class="sxs-lookup"><span data-stu-id="191c7-238">In the following example, the `GetNumbers` function returns an `Integer()`, a one-dimensional array of type `Integer`.</span></span> <span data-ttu-id="191c7-239">`ShowNumbers`Procedura přijímá `Integer()` argument.</span><span class="sxs-lookup"><span data-stu-id="191c7-239">The `ShowNumbers` procedure accepts an `Integer()` argument.</span></span>

[!code-vb[return-value-and-params](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/return-values-and-params.vb)]

<span data-ttu-id="191c7-240">V následujícím příkladu `GetNumbersMultiDim` funkce vrací dvojrozměrné `Integer(,)` pole typu `Integer` .</span><span class="sxs-lookup"><span data-stu-id="191c7-240">In the following example, the `GetNumbersMultiDim` function returns an `Integer(,)`, a two-dimensional array of type `Integer`.</span></span>  <span data-ttu-id="191c7-241">`ShowNumbersMultiDim`Procedura přijímá `Integer(,)` argument.</span><span class="sxs-lookup"><span data-stu-id="191c7-241">The `ShowNumbersMultiDim` procedure accepts an `Integer(,)` argument.</span></span>

[!code-vb[multidimensional-return-value](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/return-values-and-params-2d.vb)]

## <a name="jagged-arrays"></a><span data-ttu-id="191c7-242">Vícenásobná pole</span><span class="sxs-lookup"><span data-stu-id="191c7-242">Jagged arrays</span></span>

<span data-ttu-id="191c7-243">V některých případech je struktura dat ve vaší aplikaci dvourozměrná, ale ne pravoúhlá.</span><span class="sxs-lookup"><span data-stu-id="191c7-243">Sometimes the data structure in your application is two-dimensional but not rectangular.</span></span> <span data-ttu-id="191c7-244">Například můžete použít pole k ukládání dat o vysoké teplotě každého dne v měsíci.</span><span class="sxs-lookup"><span data-stu-id="191c7-244">For example, you might use an array to store data about the high temperature of each day of the month.</span></span> <span data-ttu-id="191c7-245">První rozměr pole představuje měsíc, ale druhá dimenze představuje počet dní a počet dní v měsíci není stejnorodý.</span><span class="sxs-lookup"><span data-stu-id="191c7-245">The first dimension of the array represents the month, but the second dimension represents the number of days, and the number of days in a month is not uniform.</span></span> <span data-ttu-id="191c7-246">*Vícenásobné pole*, které se označuje také jako *pole polí*, je navrženo pro tyto scénáře.</span><span class="sxs-lookup"><span data-stu-id="191c7-246">A *jagged array*, which is also called an *array of arrays*, is designed for such scenarios.</span></span> <span data-ttu-id="191c7-247">Vícenásobné pole je pole, jehož prvky jsou také pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-247">A jagged array is an array whose elements are also arrays.</span></span> <span data-ttu-id="191c7-248">Vícenásobné pole a každý prvek ve vícenásobném poli může mít jednu nebo více dimenzí.</span><span class="sxs-lookup"><span data-stu-id="191c7-248">A jagged array and each element in a jagged array can have one or more dimensions.</span></span>

<span data-ttu-id="191c7-249">Následující příklad používá pole měsíců, přičemž každý prvek je pole dnů.</span><span class="sxs-lookup"><span data-stu-id="191c7-249">The following example uses an array of months, each element of which is an array of days.</span></span> <span data-ttu-id="191c7-250">V příkladu se používá vícenásobné pole, protože různé měsíce mají různý počet dnů.</span><span class="sxs-lookup"><span data-stu-id="191c7-250">The example uses a jagged array because different months have different numbers of days.</span></span>  <span data-ttu-id="191c7-251">Tento příklad ukazuje, jak vytvořit vícenásobné pole, přiřadit k němu hodnoty a načíst a zobrazit jeho hodnoty.</span><span class="sxs-lookup"><span data-stu-id="191c7-251">The example shows how to create a jagged array, assign values to it, and retrieve and display its values.</span></span>

[!code-vb[jagged-arrays](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged.vb)]

<span data-ttu-id="191c7-252">Předchozí příklad přiřadí hodnoty vícenásobnému poli na základě prvku pomocí `For...Next` smyčky.</span><span class="sxs-lookup"><span data-stu-id="191c7-252">The previous example assigns values to the jagged array on an element-by-element basis by using a `For...Next` loop.</span></span> <span data-ttu-id="191c7-253">Můžete také přiřadit hodnoty prvkům vícenásobného pole pomocí literálů vnořeného pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-253">You can also assign values to the elements of a jagged array by using nested array literals.</span></span> <span data-ttu-id="191c7-254">Pokus o použití literálů vnořeného pole (například `Dim valuesjagged = {{1, 2}, {2, 3, 4}}` ) vygeneruje chybu kompilátoru [BC30568](../../../misc/bc30568.md).</span><span class="sxs-lookup"><span data-stu-id="191c7-254">However, the attempt to use nested array literals (for example, `Dim valuesjagged = {{1, 2}, {2, 3, 4}}`) generates compiler error [BC30568](../../../misc/bc30568.md).</span></span> <span data-ttu-id="191c7-255">Chcete-li chybu opravit, vložte literály vnitřních polí do závorek.</span><span class="sxs-lookup"><span data-stu-id="191c7-255">To correct the error, enclose the inner array literals in parentheses.</span></span> <span data-ttu-id="191c7-256">Závorky přinutí vyhodnotit výraz literálu pole a výsledné hodnoty jsou použity s vnějším literálem pole, jak ukazuje následující příklad.</span><span class="sxs-lookup"><span data-stu-id="191c7-256">The parentheses force the array literal expression to be evaluated, and the resulting values are used with the outer array literal, as the following example shows.</span></span>

[!code-vb[jagged-array-initialization](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged-assign.vb)]

<span data-ttu-id="191c7-257">Vícenásobné pole je jednorozměrné pole, jehož prvky obsahují pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-257">A jagged array is a one-dimensional array whose elements contain arrays.</span></span> <span data-ttu-id="191c7-258">Proto <xref:System.Array.Length%2A?displayProperty=nameWithType> vlastnost a `Array.GetLength(0)` Metoda vrátí počet prvků v jednorozměrném poli a `Array.GetLength(1)` vyvolá výjimku, <xref:System.IndexOutOfRangeException> protože vícenásobné pole není vícerozměrné.</span><span class="sxs-lookup"><span data-stu-id="191c7-258">Therefore, the <xref:System.Array.Length%2A?displayProperty=nameWithType> property and the `Array.GetLength(0)` method return the number of elements in the one-dimensional array, and `Array.GetLength(1)` throws an <xref:System.IndexOutOfRangeException> because a jagged array is not multidimensional.</span></span> <span data-ttu-id="191c7-259">Určíte počet prvků v každém podpoli načtením hodnoty vlastnosti každého podpole <xref:System.Array.Length%2A?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="191c7-259">You determine the number of elements in each subarray by retrieving the value of each subarray's <xref:System.Array.Length%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="191c7-260">Následující příklad ukazuje, jak určit počet prvků ve vícenásobném poli.</span><span class="sxs-lookup"><span data-stu-id="191c7-260">The following example illustrates how to determine the number of elements in a jagged array.</span></span>

[!code-vb[jagged-array-size](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged-length.vb)]

## <a name="zero-length-arrays"></a><span data-ttu-id="191c7-261">Pole s nulovou délkou</span><span class="sxs-lookup"><span data-stu-id="191c7-261">Zero-length arrays</span></span>

<span data-ttu-id="191c7-262">Visual Basic rozlišuje mezi neinicializovaným polem (polem, jehož hodnota je `Nothing` ) a *polem s nulovou délkou* nebo prázdným polem (pole, které neobsahuje žádné prvky.) Neinicializovaný objekt Array je ten, který nemá vytvořenou dimenzi nebo k němu byly přiřazeny nějaké hodnoty.</span><span class="sxs-lookup"><span data-stu-id="191c7-262">Visual Basic differentiates between a uninitialized array (an array whose value is `Nothing`) and a *zero-length array* or empty array (an array that has no elements.) An uninitialized array is one that has not been dimensioned or had any values assigned to it.</span></span> <span data-ttu-id="191c7-263">Příklad:</span><span class="sxs-lookup"><span data-stu-id="191c7-263">For example:</span></span>

```vb
Dim arr() As String
```

<span data-ttu-id="191c7-264">Pole s nulovou délkou je deklarováno s rozměrem-1.</span><span class="sxs-lookup"><span data-stu-id="191c7-264">A zero-length array is declared with a dimension of -1.</span></span> <span data-ttu-id="191c7-265">Příklad:</span><span class="sxs-lookup"><span data-stu-id="191c7-265">For example:</span></span>

```vb
Dim arrZ(-1) As String
```

<span data-ttu-id="191c7-266">Pole s nulovou délkou možná budete muset vytvořit za následujících okolností:</span><span class="sxs-lookup"><span data-stu-id="191c7-266">You might need to create a zero-length array under the following circumstances:</span></span>

- <span data-ttu-id="191c7-267">Aniž by došlo k <xref:System.NullReferenceException> výjimce, váš kód musí mít přístup ke členům třídy, jako je například <xref:System.Array> <xref:System.Array.Length%2A> nebo <xref:System.Array.Rank%2A> , nebo volat funkci Visual Basic, jako je například <xref:Microsoft.VisualBasic.Information.UBound%2A> .</span><span class="sxs-lookup"><span data-stu-id="191c7-267">Without risking a <xref:System.NullReferenceException> exception, your code must access members of the <xref:System.Array> class, such as <xref:System.Array.Length%2A> or <xref:System.Array.Rank%2A>, or call a Visual Basic function such as <xref:Microsoft.VisualBasic.Information.UBound%2A>.</span></span>

- <span data-ttu-id="191c7-268">Chcete, aby byl kód jednoduchý, nemusíte ho kontrolovat `Nothing` jako zvláštní případ.</span><span class="sxs-lookup"><span data-stu-id="191c7-268">You want to keep your code simple by not having to check for `Nothing` as a special case.</span></span>

- <span data-ttu-id="191c7-269">Váš kód komunikuje s rozhraním API (Application Programming Interface), které vyžaduje, abyste předávali pole nulové délky k jednomu nebo více procedurám nebo v jednom nebo více postupech vrátili pole s nulovou délkou.</span><span class="sxs-lookup"><span data-stu-id="191c7-269">Your code interacts with an application programming interface (API) that either requires you to pass a zero-length array to one or more procedures or returns a zero-length array from one or more procedures.</span></span>

## <a name="splitting-an-array"></a><span data-ttu-id="191c7-270">Rozdělení pole</span><span class="sxs-lookup"><span data-stu-id="191c7-270">Splitting an array</span></span>

<span data-ttu-id="191c7-271">V některých případech může být nutné rozdělit jedno pole do více polí.</span><span class="sxs-lookup"><span data-stu-id="191c7-271">In some cases, you may need to split a single array into multiple arrays.</span></span> <span data-ttu-id="191c7-272">To zahrnuje identifikaci bodu nebo bodů, ve kterých má být pole rozděleno, a pak Spitting pole do dvou nebo více samostatných polí.</span><span class="sxs-lookup"><span data-stu-id="191c7-272">This involves identifying the point or points at which the array is to be split, and then spitting the array into two or more separate arrays.</span></span>

> [!NOTE]
> <span data-ttu-id="191c7-273">Tato část nepopisuje rozdělení jednoho řetězce do pole řetězců na základě některého oddělovače.</span><span class="sxs-lookup"><span data-stu-id="191c7-273">This section does not discuss splitting a single string into a string array based on some delimiter.</span></span> <span data-ttu-id="191c7-274">Informace o rozdělení řetězce naleznete v <xref:System.String.Split%2A?displayProperty=nameWithType> metodě.</span><span class="sxs-lookup"><span data-stu-id="191c7-274">For information on splitting a string, see the <xref:System.String.Split%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="191c7-275">Nejběžnější kritéria pro rozdělení pole jsou:</span><span class="sxs-lookup"><span data-stu-id="191c7-275">The most common criteria for splitting an array are:</span></span>

- <span data-ttu-id="191c7-276">Počet prvků v poli.</span><span class="sxs-lookup"><span data-stu-id="191c7-276">The number of elements in the array.</span></span> <span data-ttu-id="191c7-277">Například můžete chtít rozdělit pole více než zadaný počet prvků na číslo přibližně stejné části.</span><span class="sxs-lookup"><span data-stu-id="191c7-277">For example, you might want to split an array of more than a specified number of elements into a number of approximately equal parts.</span></span> <span data-ttu-id="191c7-278">Pro účely tohoto účelu můžete použít hodnotu vrácenou <xref:System.Array.Length%2A?displayProperty=nameWithType> <xref:System.Array.GetLength%2A?displayProperty=nameWithType> metodou nebo.</span><span class="sxs-lookup"><span data-stu-id="191c7-278">For this purpose, you can use the value returned by either the <xref:System.Array.Length%2A?displayProperty=nameWithType> or <xref:System.Array.GetLength%2A?displayProperty=nameWithType> method.</span></span>

- <span data-ttu-id="191c7-279">Hodnota prvku, který slouží jako oddělovač, který označuje, kde má být pole rozděleno.</span><span class="sxs-lookup"><span data-stu-id="191c7-279">The value of an element, which serves as a delimiter that indicates where the array should be split.</span></span> <span data-ttu-id="191c7-280">Konkrétní hodnotu můžete vyhledat voláním <xref:System.Array.FindIndex%2A?displayProperty=nameWithType> <xref:System.Array.FindLastIndex%2A?displayProperty=nameWithType> metod a.</span><span class="sxs-lookup"><span data-stu-id="191c7-280">You can search for a specific value by calling the <xref:System.Array.FindIndex%2A?displayProperty=nameWithType> and <xref:System.Array.FindLastIndex%2A?displayProperty=nameWithType> methods.</span></span>

<span data-ttu-id="191c7-281">Jakmile určíte index nebo indexy, u kterých má být pole rozděleno, můžete vytvořit jednotlivá pole voláním <xref:System.Array.Copy%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="191c7-281">Once you've determined the index or indexes at which the array should be split, you can then create the individual arrays by calling the <xref:System.Array.Copy%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="191c7-282">Následující příklad rozdělí pole do dvou polí přibližně stejné velikosti.</span><span class="sxs-lookup"><span data-stu-id="191c7-282">The following example splits an array into two arrays of approximately equal size.</span></span> <span data-ttu-id="191c7-283">(Pokud celkový počet elementů pole je lichý, první pole má jeden další prvek než druhý.)</span><span class="sxs-lookup"><span data-stu-id="191c7-283">(If the total number of array elements is odd, the first array has one more element than the second.)</span></span>

[!code-vb[splitting-an-array-by-length](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/split1.vb)]

<span data-ttu-id="191c7-284">Následující příklad rozdělí pole řetězců do dvou polí na základě přítomnosti elementu, jehož hodnota je "ZZZ", která slouží jako oddělovač pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-284">The following example splits a string array into two arrays based on the presence of an element whose value is "zzz", which serves as the array delimiter.</span></span> <span data-ttu-id="191c7-285">Nová pole neobsahují element, který obsahuje oddělovač.</span><span class="sxs-lookup"><span data-stu-id="191c7-285">The new arrays do not include the element that contains the delimiter.</span></span>

[!code-vb[splitting-an-array-by-delimiter](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/split2.vb)]

## <a name="joining-arrays"></a><span data-ttu-id="191c7-286">Spojování polí</span><span class="sxs-lookup"><span data-stu-id="191c7-286">Joining arrays</span></span>

<span data-ttu-id="191c7-287">V jednom větším poli můžete také zkombinovat několik polí.</span><span class="sxs-lookup"><span data-stu-id="191c7-287">You can also combine a number of arrays into a single larger array.</span></span> <span data-ttu-id="191c7-288">K tomu můžete použít také <xref:System.Array.Copy%2A?displayProperty=nameWithType> metodu.</span><span class="sxs-lookup"><span data-stu-id="191c7-288">To do this, you also use the <xref:System.Array.Copy%2A?displayProperty=nameWithType> method.</span></span>

> [!NOTE]
> <span data-ttu-id="191c7-289">Tato část se nezabývá připojením pole řetězců k jednomu řetězci.</span><span class="sxs-lookup"><span data-stu-id="191c7-289">This section does not discuss joining a string array into a single string.</span></span> <span data-ttu-id="191c7-290">Informace o spojování pole řetězců naleznete v <xref:System.String.Join%2A?displayProperty=nameWithType> metodě.</span><span class="sxs-lookup"><span data-stu-id="191c7-290">For information on joining a string array, see the <xref:System.String.Join%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="191c7-291">Před zkopírováním prvků každého pole do nového pole je nutné nejprve zajistit, aby bylo pole inicializováno tak, aby bylo dostatečně velké, aby bylo možné přizpůsobit novému poli.</span><span class="sxs-lookup"><span data-stu-id="191c7-291">Before copying the elements of each array into the new array, you must first ensure that you have initialized the array so that it is large enough to accommodate the new array.</span></span> <span data-ttu-id="191c7-292">Toto lze provést jedním ze dvou způsobů:</span><span class="sxs-lookup"><span data-stu-id="191c7-292">You can do this in one of two ways:</span></span>

- <span data-ttu-id="191c7-293">Použijte [`ReDim Preserve`](../../../language-reference/statements/redim-statement.md) příkaz k dynamickému rozbalení pole před přidáním nových prvků do něj.</span><span class="sxs-lookup"><span data-stu-id="191c7-293">Use the [`ReDim Preserve`](../../../language-reference/statements/redim-statement.md) statement to dynamically expand the array before adding new elements to it.</span></span> <span data-ttu-id="191c7-294">Toto je nejjednodušší postup, ale může způsobit snížení výkonu a nadměrné využití paměti při kopírování velkých polí.</span><span class="sxs-lookup"><span data-stu-id="191c7-294">This is the easiest technique, but it can result in performance degradation and excessive memory consumption when you are copying large arrays.</span></span>
- <span data-ttu-id="191c7-295">Vypočítá celkový počet prvků potřebných pro nové velké pole a potom do něj přidejte prvky každého zdrojového pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-295">Calculate the total number of elements needed for the new large array, then add the elements of each source array to it.</span></span>

<span data-ttu-id="191c7-296">Následující příklad používá druhý přístup pro přidání čtyř polí s deseti prvky každý do jednoho pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-296">The following example uses the second approach to add four arrays with ten elements each to a single array.</span></span>

[!code-vb[joining-an-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/join.vb)]

<span data-ttu-id="191c7-297">Vzhledem k tomu, že v tomto případě jsou zdrojová pole malá, můžeme také dynamicky rozšířit pole, protože do něj přidáte prvky každého nového pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-297">Since in this case the source arrays are all small, we can also dynamically expand the array as we add the elements of each new array to it.</span></span> <span data-ttu-id="191c7-298">V následujícím příkladu je to.</span><span class="sxs-lookup"><span data-stu-id="191c7-298">The following example does that.</span></span>

[!code-vb[joining-an-array-dynamically](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/join2.vb)]

## <a name="collections-as-an-alternative-to-arrays"></a><span data-ttu-id="191c7-299">Kolekce jako alternativa k polím</span><span class="sxs-lookup"><span data-stu-id="191c7-299">Collections as an alternative to arrays</span></span>

<span data-ttu-id="191c7-300">Pole jsou užitečná pro vytváření a práci s pevným počtem silně typových objektů.</span><span class="sxs-lookup"><span data-stu-id="191c7-300">Arrays are most useful for creating and working with a fixed number of strongly typed objects.</span></span> <span data-ttu-id="191c7-301">Kolekce poskytují pružnější způsob práce se skupinami objektů.</span><span class="sxs-lookup"><span data-stu-id="191c7-301">Collections provide a more flexible way to work with groups of objects.</span></span> <span data-ttu-id="191c7-302">Na rozdíl od polí, která vyžadují, abyste explicitně změnili velikost pole [ `ReDim` příkazem](../../../language-reference/statements/redim-statement.md), kolekce se dynamicky zvětšují a zmenšují podle potřeb změny aplikace.</span><span class="sxs-lookup"><span data-stu-id="191c7-302">Unlike arrays, which require that you explicitly change the size of an array with the [`ReDim` Statement](../../../language-reference/statements/redim-statement.md), collections grow and shrink dynamically as the needs of an application change.</span></span>

<span data-ttu-id="191c7-303">Když použijete `ReDim` k redimenzi pole, Visual Basic vytvoří nové pole a uvolní předchozí.</span><span class="sxs-lookup"><span data-stu-id="191c7-303">When you use `ReDim` to redimension an array, Visual Basic creates a new array and releases the previous one.</span></span> <span data-ttu-id="191c7-304">Tím se provede doba provádění.</span><span class="sxs-lookup"><span data-stu-id="191c7-304">This takes execution time.</span></span> <span data-ttu-id="191c7-305">Proto pokud počet položek, se kterými pracujete, často mění nebo nemůžete odhadnout maximální počet položek, které potřebujete, obvykle získáte lepší výkon pomocí kolekce.</span><span class="sxs-lookup"><span data-stu-id="191c7-305">Therefore, if the number of items you are working with changes frequently, or you cannot predict the maximum number of items you need, you'll usually obtain better performance by using a collection.</span></span>

<span data-ttu-id="191c7-306">U některých kolekcí můžete přiřadit klíč k libovolnému objektu, který vložíte do kolekce, abyste mohli rychle načíst objekt pomocí klíče.</span><span class="sxs-lookup"><span data-stu-id="191c7-306">For some collections, you can assign a key to any object that you put into the collection so that you can quickly retrieve the object by using the key.</span></span>

<span data-ttu-id="191c7-307">Pokud kolekce obsahuje prvky pouze jednoho typu dat, můžete použít jednu ze tříd v <xref:System.Collections.Generic?displayProperty=nameWithType> oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="191c7-307">If your collection contains elements of only one data type, you can use one of the classes in the <xref:System.Collections.Generic?displayProperty=nameWithType> namespace.</span></span> <span data-ttu-id="191c7-308">Obecná kolekce vynutila bezpečnost typů, takže do ní nelze přidat žádný jiný datový typ.</span><span class="sxs-lookup"><span data-stu-id="191c7-308">A generic collection enforces type safety so that no other data type can be added to it.</span></span>

<span data-ttu-id="191c7-309">Další informace o kolekcích najdete v tématu [kolekce](../../concepts/collections.md).</span><span class="sxs-lookup"><span data-stu-id="191c7-309">For more information about collections, see [Collections](../../concepts/collections.md).</span></span>

## <a name="related-topics"></a><span data-ttu-id="191c7-310">Související témata</span><span class="sxs-lookup"><span data-stu-id="191c7-310">Related topics</span></span>

|<span data-ttu-id="191c7-311">Pojem</span><span class="sxs-lookup"><span data-stu-id="191c7-311">Term</span></span>|<span data-ttu-id="191c7-312">Definice</span><span class="sxs-lookup"><span data-stu-id="191c7-312">Definition</span></span>|
|----------|----------------|
|[<span data-ttu-id="191c7-313">Rozměry pole v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="191c7-313">Array Dimensions in Visual Basic</span></span>](array-dimensions.md)|<span data-ttu-id="191c7-314">Vysvětluje pořadí a dimenze v polích.</span><span class="sxs-lookup"><span data-stu-id="191c7-314">Explains rank and dimensions in arrays.</span></span>|
|[<span data-ttu-id="191c7-315">Postupy: Inicializace proměnné pole v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="191c7-315">How to: Initialize an Array Variable in Visual Basic</span></span>](how-to-initialize-an-array-variable.md)|<span data-ttu-id="191c7-316">Popisuje, jak naplnit pole počátečními hodnotami.</span><span class="sxs-lookup"><span data-stu-id="191c7-316">Describes how to populate arrays with initial values.</span></span>|
|[<span data-ttu-id="191c7-317">Postupy: Řazení pole v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="191c7-317">How to: Sort An Array in Visual Basic</span></span>](how-to-sort-an-array.md)|<span data-ttu-id="191c7-318">Ukazuje, jak seřadit prvky pole abecedně.</span><span class="sxs-lookup"><span data-stu-id="191c7-318">Shows how to sort the elements of an array alphabetically.</span></span>|
|[<span data-ttu-id="191c7-319">Postupy: Přiřazení jednoho pole ke druhému</span><span class="sxs-lookup"><span data-stu-id="191c7-319">How to: Assign One Array to Another Array</span></span>](how-to-assign-one-array-to-another-array.md)|<span data-ttu-id="191c7-320">Popisuje pravidla a kroky pro přiřazení pole k jiné proměnné pole.</span><span class="sxs-lookup"><span data-stu-id="191c7-320">Describes the rules and steps for assigning an array to another array variable.</span></span>|
|[<span data-ttu-id="191c7-321">Řešení potíží s poli</span><span class="sxs-lookup"><span data-stu-id="191c7-321">Troubleshooting Arrays</span></span>](troubleshooting-arrays.md)|<span data-ttu-id="191c7-322">Popisuje některé běžné problémy, které vznikají při práci s poli.</span><span class="sxs-lookup"><span data-stu-id="191c7-322">Discusses some common problems that arise when working with arrays.</span></span>|

## <a name="see-also"></a><span data-ttu-id="191c7-323">Viz také</span><span class="sxs-lookup"><span data-stu-id="191c7-323">See also</span></span>

- <xref:System.Array?displayProperty=nameWithType>
- [<span data-ttu-id="191c7-324">Dim – příkaz</span><span class="sxs-lookup"><span data-stu-id="191c7-324">Dim Statement</span></span>](../../../language-reference/statements/dim-statement.md)
- [<span data-ttu-id="191c7-325">ReDim – příkaz</span><span class="sxs-lookup"><span data-stu-id="191c7-325">ReDim Statement</span></span>](../../../language-reference/statements/redim-statement.md)
