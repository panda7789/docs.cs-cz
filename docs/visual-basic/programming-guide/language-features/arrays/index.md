---
title: Pole v jazyce Visual Basic
ms.custom: 
ms.date: 07/20/2015
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: devlang-visual-basic
ms.topic: article
f1_keywords: vb.Array
helpviewer_keywords:
- arrays [Visual Basic]
- Visual Basic, arrays
ms.assetid: dbf29737-b589-4443-bee6-a27588d9c67e
caps.latest.revision: "47"
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: 04deeccd19fd4edb3f2c88310d660eedf5c707d5
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 11/21/2017
---
# <a name="arrays-in-visual-basic"></a><span data-ttu-id="ee773-102">Pole v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="ee773-102">Arrays in Visual Basic</span></span>
<span data-ttu-id="ee773-103">Pole je sada hodnot, které jsou logicky vzájemně souvisí, jako je počet studentů v každé úrovni ve škole gramatika.</span><span class="sxs-lookup"><span data-stu-id="ee773-103">An array is a set of values that are logically related to each other, such as the number of students in each grade in a grammar school.</span></span>  <span data-ttu-id="ee773-104">Pokud hledáte pomoc na pole v jazyce Visual Basic for Applications (VBA), přečtěte si téma [referenční příručka jazyka](https://msdn.microsoft.com/library/office/gg264383\(v=office.14\).aspx).</span><span class="sxs-lookup"><span data-stu-id="ee773-104">If you are looking for help on arrays in Visual Basic for Applications (VBA), see the [language reference](https://msdn.microsoft.com/library/office/gg264383\(v=office.14\).aspx).</span></span>  
  
 <span data-ttu-id="ee773-105">Pomocí pole můžete odkazovat na tyto hodnoty související se stejným názvem a použijte číslo, které se má volat index nebo dolní index, aby věděli, od sebe.</span><span class="sxs-lookup"><span data-stu-id="ee773-105">By using an array, you can refer to these related values by the same name, and use a number that’s called an index or subscript to tell them apart.</span></span> <span data-ttu-id="ee773-106">Jednotlivé hodnoty se nazývají elementy pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-106">The individual values are called the elements of the array.</span></span> <span data-ttu-id="ee773-107">Jsou souvislý z indexu 0 prostřednictvím nejvyšší hodnotu indexu.</span><span class="sxs-lookup"><span data-stu-id="ee773-107">They’re contiguous from index 0 through the highest index value.</span></span>  
  
 <span data-ttu-id="ee773-108">Na rozdíl od pole, je názvem proměnné, která obsahují jednu hodnotu *skalární* proměnné.</span><span class="sxs-lookup"><span data-stu-id="ee773-108">In contrast to an array, a variable that contain a single value is called a *scalar* variable.</span></span>  
  
 <span data-ttu-id="ee773-109">Rychlé příklady před vysvětlení:</span><span class="sxs-lookup"><span data-stu-id="ee773-109">Some quick examples before explanation:</span></span>  
  
```vb  
'Declare a single-dimension array of 5 values  
Dim numbers(4) As Integer   
  
'Declare a single-dimension array and set array element values  
Dim numbers = New Integer() {1, 2, 4, 8}  
  
'Redefine the size of an existing array retaining the current values  
ReDim Preserve numbers(15)  
  
'Redefine the size of an existing array, resetting the values  
ReDim numbers(15)  
  
'Declare a multi-dimensional array  
Dim matrix(5, 5) As Double  
  
'Declare a multi-dimensional array and set array element values  
Dim matrix = New Integer(4, 4) {{1, 2}, {3, 4}, {5, 6}, {7, 8}}  
  
'Declare a jagged array  
Dim sales()() As Double = New Double(11)() {}  
```  
  
 <span data-ttu-id="ee773-110">**V tomto tématu**</span><span class="sxs-lookup"><span data-stu-id="ee773-110">**In this topic**</span></span>  
  
-   [<span data-ttu-id="ee773-111">Elementy pole v jednoduchých polí</span><span class="sxs-lookup"><span data-stu-id="ee773-111">Array Elements in a Simple Array</span></span>](#BKMK_ArrayElements)  
  
-   [<span data-ttu-id="ee773-112">Vytváření pole</span><span class="sxs-lookup"><span data-stu-id="ee773-112">Creating an Array</span></span>](#BKMK_CreatingAnArray)  
  
-   [<span data-ttu-id="ee773-113">Ukládání hodnot v matici</span><span class="sxs-lookup"><span data-stu-id="ee773-113">Storing Values in an Array</span></span>](#BKMK_StoringValues)  
  
-   [<span data-ttu-id="ee773-114">Vyplní pole s počáteční hodnoty</span><span class="sxs-lookup"><span data-stu-id="ee773-114">Populating an Array with Initial Values</span></span>](#BKMK_Populating)  
  
    -   [<span data-ttu-id="ee773-115">Vnořená pole literály</span><span class="sxs-lookup"><span data-stu-id="ee773-115">Nested Array Literals</span></span>](#BKMK_NestedArrayLiterals)  
  
-   [<span data-ttu-id="ee773-116">Iterace v rámci pole</span><span class="sxs-lookup"><span data-stu-id="ee773-116">Iterating Through an Array</span></span>](#BKMK_Iterating)  
  
-   [<span data-ttu-id="ee773-117">Pole jako parametry a návratové hodnoty</span><span class="sxs-lookup"><span data-stu-id="ee773-117">Arrays as Return Values and Parameters</span></span>](#BKMK_ReturnValues)  
  
-   [<span data-ttu-id="ee773-118">Vícenásobná pole</span><span class="sxs-lookup"><span data-stu-id="ee773-118">Jagged Arrays</span></span>](#BKMK_JaggedArrays)  
  
-   [<span data-ttu-id="ee773-119">Pole s nulovou délkou</span><span class="sxs-lookup"><span data-stu-id="ee773-119">Zero-Length Arrays</span></span>](#BKMK_ZeroLength)  
  
-   [<span data-ttu-id="ee773-120">Velikost pole</span><span class="sxs-lookup"><span data-stu-id="ee773-120">Array Size</span></span>](#BKMK_ArraySize)  
  
-   [<span data-ttu-id="ee773-121">Typy polí a dalších typů</span><span class="sxs-lookup"><span data-stu-id="ee773-121">Array Types and Other Types</span></span>](#BKMK_ArrayTypes)  
  
-   [<span data-ttu-id="ee773-122">Kolekce jako alternativu k polím</span><span class="sxs-lookup"><span data-stu-id="ee773-122">Collections as an Alternative to Arrays</span></span>](#BKMK_Collections)  
  
##  <span data-ttu-id="ee773-123"><a name="BKMK_ArrayElements"></a>Elementy pole v jednoduchých polí</span><span class="sxs-lookup"><span data-stu-id="ee773-123"><a name="BKMK_ArrayElements"></a> Array Elements in a Simple Array</span></span>  
 <span data-ttu-id="ee773-124">Následující příklad deklaruje proměnné pole pro uložení počet studentů v každé úrovni ve škole gramatika.</span><span class="sxs-lookup"><span data-stu-id="ee773-124">The following example declares an array variable to hold the number of students in each grade in a grammar school.</span></span>  
  
 [!code-vb[VbVbalrArrays#2](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#2)]  
  
 <span data-ttu-id="ee773-125">Pole `students` v předchozím příkladu obsahuje sedm elementy.</span><span class="sxs-lookup"><span data-stu-id="ee773-125">The array `students` in the preceding example contains seven elements.</span></span> <span data-ttu-id="ee773-126">Indexy elementů rozsahu od 0 do 6.</span><span class="sxs-lookup"><span data-stu-id="ee773-126">The indexes of the elements range from 0 through 6.</span></span> <span data-ttu-id="ee773-127">Toto pole je jednodušší než deklarování sedm proměnných.</span><span class="sxs-lookup"><span data-stu-id="ee773-127">Having this array is simpler than declaring seven variables.</span></span>  
  
 <span data-ttu-id="ee773-128">Následující obrázek znázorňuje pole `students`.</span><span class="sxs-lookup"><span data-stu-id="ee773-128">The following illustration shows the array `students`.</span></span> <span data-ttu-id="ee773-129">Pro každý element pole:</span><span class="sxs-lookup"><span data-stu-id="ee773-129">For each element of the array:</span></span>  
  
-   <span data-ttu-id="ee773-130">Index elementu představuje úrovni (index 0 představuje mateřské školy).</span><span class="sxs-lookup"><span data-stu-id="ee773-130">The index of the element represents the grade (index 0 represents kindergarten).</span></span>  
  
-   <span data-ttu-id="ee773-131">Hodnota, která je obsažena v elementu představuje počet studentů v této úrovni.</span><span class="sxs-lookup"><span data-stu-id="ee773-131">The value that’s contained in the element represents the number of students in that grade.</span></span>  
  
 <span data-ttu-id="ee773-132">![Obrázek znázorňující počet studentů pole](../../../../visual-basic/programming-guide/language-features/arrays/media/arrayexampleschool.gif "ArrayExampleSchool")</span><span class="sxs-lookup"><span data-stu-id="ee773-132">![Picture of array showing numbers of students](../../../../visual-basic/programming-guide/language-features/arrays/media/arrayexampleschool.gif "ArrayExampleSchool")</span></span>  
<span data-ttu-id="ee773-133">Prvky pole "studenty"</span><span class="sxs-lookup"><span data-stu-id="ee773-133">Elements of the "students" array</span></span>  
  
 <span data-ttu-id="ee773-134">Následující příklad ukazuje, jak odkazovat na první, druhý a posledním elementem pole `students`.</span><span class="sxs-lookup"><span data-stu-id="ee773-134">The following example shows how to refer to the first, second, and last element of the array `students`.</span></span>  
  
 [!code-vb[VbVbalrArrays#3](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#3)]  
  
 <span data-ttu-id="ee773-135">Pole najdete jako celek s použitím právě názvu proměnné pole bez indexy.</span><span class="sxs-lookup"><span data-stu-id="ee773-135">You can refer to the array as a whole by using just the array variable name without indexes.</span></span>  
  
 <span data-ttu-id="ee773-136">Pole `students` v předchozím příkladu používá jeden index a je označen jako jednorozměrné.</span><span class="sxs-lookup"><span data-stu-id="ee773-136">The array `students` in the preceding example uses one index and is said to be one-dimensional.</span></span> <span data-ttu-id="ee773-137">Pole, které používá více než jeden index nebo dolní index se nazývá multidimenzionální.</span><span class="sxs-lookup"><span data-stu-id="ee773-137">An array that uses more than one index or subscript is called multidimensional.</span></span> <span data-ttu-id="ee773-138">Další informace najdete v tématu zbývající část tohoto tématu a [rozměry pole v jazyce Visual Basic](../../../../visual-basic/programming-guide/language-features/arrays/array-dimensions.md).</span><span class="sxs-lookup"><span data-stu-id="ee773-138">For more information, see the rest of this topic and [Array Dimensions in Visual Basic](../../../../visual-basic/programming-guide/language-features/arrays/array-dimensions.md).</span></span>  
  
##  <span data-ttu-id="ee773-139"><a name="BKMK_CreatingAnArray"></a>Vytváření pole</span><span class="sxs-lookup"><span data-stu-id="ee773-139"><a name="BKMK_CreatingAnArray"></a> Creating an Array</span></span>  
 <span data-ttu-id="ee773-140">Můžete definovat velikosti pole několika způsoby.</span><span class="sxs-lookup"><span data-stu-id="ee773-140">You can define the size of an array several ways.</span></span> <span data-ttu-id="ee773-141">Když toto pole je deklarován jako následující příklad ukazuje, můžete zadat velikost.</span><span class="sxs-lookup"><span data-stu-id="ee773-141">You can supply the size when the array is declared, as the following example shows.</span></span>  
  
 [!code-vb[VbVbalrArrays#12](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#12)]  
  
 <span data-ttu-id="ee773-142">Můžete použít také `New` klauzule zadat velikost pole, když je vytvořen, jak ukazuje následující příklad.</span><span class="sxs-lookup"><span data-stu-id="ee773-142">You can also use a `New` clause to supply the size of an array when it’s created, as the following example shows.</span></span>  
  
 [!code-vb[VbVbalrArrays#11](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#11)]  
  
 <span data-ttu-id="ee773-143">Pokud máte existující pole, můžete změnit její velikost pomocí `Redim` příkaz.</span><span class="sxs-lookup"><span data-stu-id="ee773-143">If you have an existing array, you can redefine its size by using the `Redim` statement.</span></span> <span data-ttu-id="ee773-144">Můžete určit, že `Redim` příkaz měli mít hodnoty, které jsou v poli, nebo můžete zadat, že ho vytvořit prázdné pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-144">You can specify that the `Redim` statement should keep the values that are in the array, or you can specify that it create an empty array.</span></span> <span data-ttu-id="ee773-145">Následující příklad ukazuje použití různých `Redim` příkaz mohli provádět úpravu velikosti pole existujících.</span><span class="sxs-lookup"><span data-stu-id="ee773-145">The following example shows different uses of the `Redim` statement to modify the size of an existing array.</span></span>  
  
 [!code-vb[VbVbalrArrays#13](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#13)]  
  
 <span data-ttu-id="ee773-146">Další informace najdete v tématu [ReDim – příkaz](../../../../visual-basic/language-reference/statements/redim-statement.md).</span><span class="sxs-lookup"><span data-stu-id="ee773-146">For more information, see [ReDim Statement](../../../../visual-basic/language-reference/statements/redim-statement.md).</span></span>  
  
##  <span data-ttu-id="ee773-147"><a name="BKMK_StoringValues"></a>Ukládání hodnot v matici</span><span class="sxs-lookup"><span data-stu-id="ee773-147"><a name="BKMK_StoringValues"></a> Storing Values in an Array</span></span>  
 <span data-ttu-id="ee773-148">Mají přístup ke každé umístění v matici pomocí indexu typu `Integer`.</span><span class="sxs-lookup"><span data-stu-id="ee773-148">You can access each location in an array by using an index of type `Integer`.</span></span> <span data-ttu-id="ee773-149">Můžete ukládat a načíst hodnoty v pole pod položkou každé pole umístění pomocí jeho index uzavřený v závorkách.</span><span class="sxs-lookup"><span data-stu-id="ee773-149">You can store and retrieve values in an array by referencing each array location by using its index enclosed in parentheses.</span></span> <span data-ttu-id="ee773-150">Indexů pro multidimenzionální pole se oddělují čárkami (,).</span><span class="sxs-lookup"><span data-stu-id="ee773-150">Indexes for multi-dimensional arrays are separated by commas (,).</span></span> <span data-ttu-id="ee773-151">Pro každé pole dimenze potřebovat jeden index.</span><span class="sxs-lookup"><span data-stu-id="ee773-151">You need one index for each array dimension.</span></span> <span data-ttu-id="ee773-152">Následující příklad ukazuje některé příkazy, které ukládají hodnoty v polích.</span><span class="sxs-lookup"><span data-stu-id="ee773-152">The following example shows some statements that store values in arrays.</span></span>  
  
 [!code-vb[VbVbalrArrays#5](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#5)]  
  
 <span data-ttu-id="ee773-153">Následující příklad ukazuje některé příkazy, které získávají hodnoty z pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-153">The following example shows some statements that get values from arrays.</span></span>  
  
 [!code-vb[VbVbalrArrays#6](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#6)]  
  
##  <span data-ttu-id="ee773-154"><a name="BKMK_Populating"></a>Vyplní pole s počáteční hodnoty</span><span class="sxs-lookup"><span data-stu-id="ee773-154"><a name="BKMK_Populating"></a> Populating an Array with Initial Values</span></span>  
 <span data-ttu-id="ee773-155">Pomocí pole literálu, můžete vytvořit pole, které obsahuje počáteční sadu hodnot.</span><span class="sxs-lookup"><span data-stu-id="ee773-155">By using an array literal, you can create an array that contains an initial set of values.</span></span> <span data-ttu-id="ee773-156">Literál pole sestává ze seznamu hodnot oddělených čárkami, které jsou uzavřené do složených závorek (`{}`).</span><span class="sxs-lookup"><span data-stu-id="ee773-156">An array literal consists of a list of comma-separated values that are enclosed in braces (`{}`).</span></span>  
  
 <span data-ttu-id="ee773-157">Když vytvoříte pole pomocí pole literálu, můžete zadat typ pole nebo použít odvození typu určit typ pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-157">When you create an array by using an array literal, you can either supply the array type or use type inference to determine the array type.</span></span> <span data-ttu-id="ee773-158">Následující kód ukazuje obě možnosti.</span><span class="sxs-lookup"><span data-stu-id="ee773-158">The following code shows both options.</span></span>  
  
 [!code-vb[VbVbalrCollectionInitializers#3](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrCollectionInitializers/VB/Module1.vb#3)]  
  
 <span data-ttu-id="ee773-159">Při použití odvození typu typ pole je určen podle dominantní typu v seznamu hodnot pro pole literál poskytnutý.</span><span class="sxs-lookup"><span data-stu-id="ee773-159">When you use type inference, the type of the array is determined by the dominant type in the list of values that’s supplied for the array literal.</span></span> <span data-ttu-id="ee773-160">Dominantní typ je jedinečný typ, do které všechny ostatní typy v poli můžete rozšířit literál.</span><span class="sxs-lookup"><span data-stu-id="ee773-160">The dominant type is a unique type to which all other types in the array literal can widen.</span></span> <span data-ttu-id="ee773-161">Pokud tento typ jedinečného nelze určit, dominantní typ je typ jedinečného, do kterého můžete zúžit všechny ostatní typy v poli.</span><span class="sxs-lookup"><span data-stu-id="ee773-161">If this unique type can’t be determined, the dominant type is the unique type to which all other types in the array can narrow.</span></span> <span data-ttu-id="ee773-162">Pokud žádná z těchto jedinečný se dá určit, že dominantní typ je `Object`.</span><span class="sxs-lookup"><span data-stu-id="ee773-162">If neither of these unique types can be determined, the dominant type is `Object`.</span></span> <span data-ttu-id="ee773-163">Například, pokud je seznam hodnot, do pole literál poskytnutý obsahuje hodnoty typu `Integer`, `Long`, a `Double`, výsledné pole je typu `Double`.</span><span class="sxs-lookup"><span data-stu-id="ee773-163">For example, if the list of values that’s supplied to the array literal contains values of type `Integer`, `Long`, and `Double`, the resulting array is of type `Double`.</span></span> <span data-ttu-id="ee773-164">Obě `Integer` a `Long` rozšířit pouze `Double`.</span><span class="sxs-lookup"><span data-stu-id="ee773-164">Both `Integer` and `Long` widen only to `Double`.</span></span> <span data-ttu-id="ee773-165">Proto `Double` dominantní typu.</span><span class="sxs-lookup"><span data-stu-id="ee773-165">Therefore, `Double` is the dominant type.</span></span> <span data-ttu-id="ee773-166">Další informace najdete v tématu [Widening a zužující převody](../../../../visual-basic/programming-guide/language-features/data-types/widening-and-narrowing-conversions.md).</span><span class="sxs-lookup"><span data-stu-id="ee773-166">For more information, see [Widening and Narrowing Conversions](../../../../visual-basic/programming-guide/language-features/data-types/widening-and-narrowing-conversions.md).</span></span> <span data-ttu-id="ee773-167">Tato odvozená pravidla platí pro typy, které jsou odvozené pro pole, které jsou místní proměnné, které jsou definovány v člena třídy.</span><span class="sxs-lookup"><span data-stu-id="ee773-167">These inference rules apply to types that are inferred for arrays that are local variables that are defined in a class member.</span></span> <span data-ttu-id="ee773-168">I když literály pole můžete použít při vytváření proměnné na úrovni, nelze použít odvození typu proměnné na úrovni třídy.</span><span class="sxs-lookup"><span data-stu-id="ee773-168">Although you can use array literals when you create class-level variables, you can’t use type inference at the class level.</span></span> <span data-ttu-id="ee773-169">V důsledku toho literály pole, které jsou zadány na úrovni třídy odvození hodnoty, které jsou zadaná pro pole literálu jako typ `Object`.</span><span class="sxs-lookup"><span data-stu-id="ee773-169">As a result, array literals that are specified at the class level infer the values that are supplied for the array literal as type `Object`.</span></span>  
  
 <span data-ttu-id="ee773-170">Typ elementů můžete explicitně zadáte v pole, které je vytvořená pomocí pole literálu.</span><span class="sxs-lookup"><span data-stu-id="ee773-170">You can explicitly specify the type of the elements in an array that’s created by using an array literal.</span></span> <span data-ttu-id="ee773-171">V takovém případě musí rozšířit hodnoty v poli literálu typu elementy pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-171">In this case, the values in the array literal must widen to the type of the elements of the array.</span></span> <span data-ttu-id="ee773-172">Následující příklad kódu vytvoří pole typu `Double` ze seznamu celých čísel.</span><span class="sxs-lookup"><span data-stu-id="ee773-172">The following code example creates an array of type `Double` from a list of integers.</span></span>  
  
 [!code-vb[VbVbalrCollectionInitializers#4](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrCollectionInitializers/VB/Module1.vb#4)]  
  
###  <span data-ttu-id="ee773-173"><a name="BKMK_NestedArrayLiterals"></a>Vnořená pole literály</span><span class="sxs-lookup"><span data-stu-id="ee773-173"><a name="BKMK_NestedArrayLiterals"></a> Nested Array Literals</span></span>  
 <span data-ttu-id="ee773-174">Multidimenzionální pole můžete vytvořit pomocí literály vnořená pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-174">You can create a multidimensional array by using nested array literals.</span></span> <span data-ttu-id="ee773-175">Literály vnořená pole musí mít dimenze, počet nebo rank, který je v souladu s výsledné pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-175">Nested array literals must have a dimension and number of dimensions, or rank, that’s consistent with the resulting array.</span></span> <span data-ttu-id="ee773-176">Následující příklad kódu vytvoří dvourozměrná pole celých čísel pomocí pole literálu.</span><span class="sxs-lookup"><span data-stu-id="ee773-176">The following code example creates a two-dimensional array of integers by using an array literal.</span></span>  
  
 [!code-vb[VbVbalrCollectionInitializers#7](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrCollectionInitializers/VB/Module1.vb#7)]  
  
 <span data-ttu-id="ee773-177">V předchozím příkladu by dojít k chybě, pokud počet elementů v literálech vnořená pole se neshodovaly.</span><span class="sxs-lookup"><span data-stu-id="ee773-177">In the previous example, an error would occur if the number of elements in the nested array literals didn’t match.</span></span> <span data-ttu-id="ee773-178">Pokud explicitně deklarovat proměnnou pole být jiné než dvourozměrná by také dojít k chybě.</span><span class="sxs-lookup"><span data-stu-id="ee773-178">An error would also occur if you explicitly declared the array variable to be other than two-dimensional.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="ee773-179">Když zadáte vnořená pole literály různých dimenzí uzavřením literály vnitřní pole do závorek se můžete vyhnout k chybě.</span><span class="sxs-lookup"><span data-stu-id="ee773-179">You can avoid an error when you supply nested array literals of different dimensions by enclosing the inner array literals in parentheses.</span></span> <span data-ttu-id="ee773-180">Vynutit závorek literálu výraz pole k vyhodnocení a výsledné hodnoty se používají s poli vnější literálu, jak ukazuje následující kód.</span><span class="sxs-lookup"><span data-stu-id="ee773-180">The parentheses force the array literal expression to be evaluated, and the resulting values are used with the outer array literal, as the following code shows.</span></span>  
  
 [!code-vb[VbVbalrCollectionInitializers#11](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrCollectionInitializers/VB/Module1.vb#11)]  
  
 <span data-ttu-id="ee773-181">Když vytvoříte multidimenzionálního pole pomocí literály vnořená pole, můžete použít odvození typu.</span><span class="sxs-lookup"><span data-stu-id="ee773-181">When you create a multidimensional array by using nested array literals, you can use type inference.</span></span> <span data-ttu-id="ee773-182">Při použití odvození typu odvozené typ je typ dominantní pro všechny hodnoty ve všech literály pole pro úroveň vnoření.</span><span class="sxs-lookup"><span data-stu-id="ee773-182">When you use type inference, the inferred type is the dominant type for all the values in all the array literals for a nesting level.</span></span> <span data-ttu-id="ee773-183">Následující příklad kódu vytvoří dvourozměrná pole typu `Double` z hodnoty, které jsou typu `Integer` a `Double`.</span><span class="sxs-lookup"><span data-stu-id="ee773-183">The following code example creates a two-dimensional array of type `Double` from values that are of type `Integer` and `Double`.</span></span>  
  
 [!code-vb[VbVbalrCollectionInitializers#8](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrCollectionInitializers/VB/Module1.vb#8)]  
  
 <span data-ttu-id="ee773-184">Další příklady najdete v tématu [postupy: Inicializace proměnné pole v jazyce Visual Basic](../../../../visual-basic/programming-guide/language-features/arrays/how-to-initialize-an-array-variable.md).</span><span class="sxs-lookup"><span data-stu-id="ee773-184">For additional examples, see [How to: Initialize an Array Variable in Visual Basic](../../../../visual-basic/programming-guide/language-features/arrays/how-to-initialize-an-array-variable.md).</span></span>  
  
##  <span data-ttu-id="ee773-185"><a name="BKMK_Iterating"></a>Iterace v rámci pole</span><span class="sxs-lookup"><span data-stu-id="ee773-185"><a name="BKMK_Iterating"></a> Iterating Through an Array</span></span>  
 <span data-ttu-id="ee773-186">Při iteraci pole přístup k nejvyšší index každý prvek v poli od nejnižší indexu.</span><span class="sxs-lookup"><span data-stu-id="ee773-186">When you iterate through an array, you access each element in the array from the lowest index to the highest index.</span></span>  
  
 <span data-ttu-id="ee773-187">V následujícím příkladu prochází jednorozměrné pole s použitím [pro... Další příkaz](../../../../visual-basic/language-reference/statements/for-next-statement.md).</span><span class="sxs-lookup"><span data-stu-id="ee773-187">The following example iterates through a one-dimensional array by using the [For...Next Statement](../../../../visual-basic/language-reference/statements/for-next-statement.md).</span></span> <span data-ttu-id="ee773-188"><xref:System.Array.GetUpperBound%2A> Metoda vrátí nejvyšší hodnotu, která může mít index.</span><span class="sxs-lookup"><span data-stu-id="ee773-188">The <xref:System.Array.GetUpperBound%2A> method returns the highest value that the index can have.</span></span> <span data-ttu-id="ee773-189">Nejnižší hodnotu indexu je vždy 0.</span><span class="sxs-lookup"><span data-stu-id="ee773-189">The lowest index value is always 0.</span></span>  
  
 [!code-vb[VbVbalrArrays#41](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#41)]  
  
 <span data-ttu-id="ee773-190">V následujícím příkladu prochází multidimenzionálního pole pomocí `For...Next` příkaz.</span><span class="sxs-lookup"><span data-stu-id="ee773-190">The following example iterates through a multidimensional array by using a `For...Next` statement.</span></span> <span data-ttu-id="ee773-191"><xref:System.Array.GetUpperBound%2A> Metoda má parametr, který určuje dimenzi.</span><span class="sxs-lookup"><span data-stu-id="ee773-191">The <xref:System.Array.GetUpperBound%2A> method has a parameter that specifies the dimension.</span></span> <span data-ttu-id="ee773-192">`GetUpperBound(0)`Vrátí hodnotu vysoké index pro první dimenze a `GetUpperBound(1)` vrací hodnotu vysoké index pro druhý dimenze.</span><span class="sxs-lookup"><span data-stu-id="ee773-192">`GetUpperBound(0)` returns the high index value for the first dimension, and `GetUpperBound(1)` returns the high index value for the second dimension.</span></span>  
  
 [!code-vb[VbVbalrArrays#42](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#42)]  
  
 <span data-ttu-id="ee773-193">V následujícím příkladu prochází jednorozměrné pole pomocí [For Each... Další příkaz](../../../../visual-basic/language-reference/statements/for-each-next-statement.md).</span><span class="sxs-lookup"><span data-stu-id="ee773-193">The following example iterates through a one-dimensional array by using a [For Each...Next Statement](../../../../visual-basic/language-reference/statements/for-each-next-statement.md).</span></span>  
  
 [!code-vb[VbVbalrArrays#43](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#43)]  
  
 <span data-ttu-id="ee773-194">V následujícím příkladu prochází multidimenzionálního pole pomocí `For Each...Next` příkaz.</span><span class="sxs-lookup"><span data-stu-id="ee773-194">The following example iterates through a multidimensional array by using a `For Each...Next` statement.</span></span> <span data-ttu-id="ee773-195">Ale máte větší kontrolu nad prvky multidimenzionální pole Pokud používáte vnořený `For…Next` příkaz, jak ukazuje předchozí příklad, místo `For Each…Next` příkaz.</span><span class="sxs-lookup"><span data-stu-id="ee773-195">However, you have more control over the elements of a multidimensional array if you use a nested `For…Next` statement, as in a previous example, instead of a `For Each…Next` statement.</span></span>  
  
 [!code-vb[VbVbalrArrays#44](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#44)]  
  
##  <span data-ttu-id="ee773-196"><a name="BKMK_ReturnValues"></a>Pole jako parametry a návratové hodnoty</span><span class="sxs-lookup"><span data-stu-id="ee773-196"><a name="BKMK_ReturnValues"></a> Arrays as Return Values and Parameters</span></span>  
 <span data-ttu-id="ee773-197">Pro vrácení pole z `Function` postupu zadejte pole datový typ a počet dimenzí jako návratový typ [funkce příkaz](../../../../visual-basic/language-reference/statements/function-statement.md).</span><span class="sxs-lookup"><span data-stu-id="ee773-197">To return an array from a `Function` procedure, specify the array data type and the number of dimensions as the return type of the [Function Statement](../../../../visual-basic/language-reference/statements/function-statement.md).</span></span> <span data-ttu-id="ee773-198">V rámci funkce deklarujte proměnnou místní pole stejným datový typ a počet dimenzí.</span><span class="sxs-lookup"><span data-stu-id="ee773-198">Within the function, declare a local array variable with same data type and number of dimensions.</span></span> <span data-ttu-id="ee773-199">V [příkaz Return](../../../../visual-basic/language-reference/statements/return-statement.md), zahrnout proměnnou místní pole bez závorek.</span><span class="sxs-lookup"><span data-stu-id="ee773-199">In the [Return Statement](../../../../visual-basic/language-reference/statements/return-statement.md), include the local array variable without parentheses.</span></span>  
  
 <span data-ttu-id="ee773-200">Chcete určit pole jako parametr, který se `Sub` nebo `Function` postupu, definujte parametr jako pole s zadaný datový typ a počet dimenzí.</span><span class="sxs-lookup"><span data-stu-id="ee773-200">To specify an array as a parameter to a `Sub` or `Function` procedure, define the parameter as an array with a specified data type and number of dimensions.</span></span> <span data-ttu-id="ee773-201">Ve volání postup odeslání proměnné pole s stejný datový typ a počet dimenzí.</span><span class="sxs-lookup"><span data-stu-id="ee773-201">In the call to the procedure, send an array variable with the same data type and number of dimensions.</span></span>  
  
 <span data-ttu-id="ee773-202">V následujícím příkladu `GetNumbers` funkce vrátí `Integer()`.</span><span class="sxs-lookup"><span data-stu-id="ee773-202">In the following example, the `GetNumbers` function returns an `Integer()`.</span></span> <span data-ttu-id="ee773-203">Tento typ pole je typu jednorozměrné pole `Integer`.</span><span class="sxs-lookup"><span data-stu-id="ee773-203">This array type is a one dimensional array of type `Integer`.</span></span> <span data-ttu-id="ee773-204">`ShowNumbers` Přijímá postup `Integer()` argument.</span><span class="sxs-lookup"><span data-stu-id="ee773-204">The `ShowNumbers` procedure accepts an `Integer()` argument.</span></span>  
  
 [!code-vb[VbVbalrArrays#51](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#51)]  
  
 <span data-ttu-id="ee773-205">V následujícím příkladu `GetNumbersMultiDim` funkce vrátí `Integer(,)`.</span><span class="sxs-lookup"><span data-stu-id="ee773-205">In the following example, the `GetNumbersMultiDim` function returns an `Integer(,)`.</span></span> <span data-ttu-id="ee773-206">Tento typ pole je dva dimenzí pole typu `Integer`.</span><span class="sxs-lookup"><span data-stu-id="ee773-206">This array type is a two dimensional array of type `Integer`.</span></span>  <span data-ttu-id="ee773-207">`ShowNumbersMultiDim` Přijímá postup `Integer(,)` argument.</span><span class="sxs-lookup"><span data-stu-id="ee773-207">The `ShowNumbersMultiDim` procedure accepts an `Integer(,)` argument.</span></span>  
  
 [!code-vb[VbVbalrArrays#52](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#52)]  
  
##  <span data-ttu-id="ee773-208"><a name="BKMK_JaggedArrays"></a>Vícenásobná pole</span><span class="sxs-lookup"><span data-stu-id="ee773-208"><a name="BKMK_JaggedArrays"></a> Jagged Arrays</span></span>  
 <span data-ttu-id="ee773-209">Pole, které obsahuje další pole jako elementy se označuje jako pole polí nebo Vícenásobná pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-209">An array that holds other arrays as elements is known as an array of arrays or a jagged array.</span></span> <span data-ttu-id="ee773-210">Vícenásobná pole a každý prvek v Vícenásobná pole může mít jeden nebo více dimenzí.</span><span class="sxs-lookup"><span data-stu-id="ee773-210">A jagged array and each element in a jagged array can have one or more dimensions.</span></span> <span data-ttu-id="ee773-211">Struktura dat v aplikaci je někdy dvourozměrná, ale není obdélníkový.</span><span class="sxs-lookup"><span data-stu-id="ee773-211">Sometimes the data structure in your application is two-dimensional but not rectangular.</span></span>  
  
 <span data-ttu-id="ee773-212">V následujícím příkladu má pole měsíců, je každý element pole dní.</span><span class="sxs-lookup"><span data-stu-id="ee773-212">The following example has an array of months, each element of which is an array of days.</span></span> <span data-ttu-id="ee773-213">Protože různé měsíce mají různý počet dní, nemáte elementy formuláři obdélníková dvourozměrná pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-213">Because different months have different numbers of days, the elements don’t form a rectangular two-dimensional array.</span></span> <span data-ttu-id="ee773-214">Vícenásobná pole se tedy používá místo multidimenzionálního pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-214">Therefore, a jagged array is used instead of a multidimensional array.</span></span>  
  
 [!code-vb[VbVbalrArrays#21](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#21)]  
  
##  <span data-ttu-id="ee773-215"><a name="BKMK_ZeroLength"></a>Pole s nulovou délkou</span><span class="sxs-lookup"><span data-stu-id="ee773-215"><a name="BKMK_ZeroLength"></a> Zero-Length Arrays</span></span>  
 <span data-ttu-id="ee773-216">Pole, které neobsahuje žádné elementy, je také označován pole nulové délky.</span><span class="sxs-lookup"><span data-stu-id="ee773-216">An array that contains no elements is also called a zero-length array.</span></span> <span data-ttu-id="ee773-217">Proměnné, která obsahuje pole nulovou délkou nemá hodnotu `Nothing`.</span><span class="sxs-lookup"><span data-stu-id="ee773-217">A variable that holds a zero-length array doesn’t have the value `Nothing`.</span></span> <span data-ttu-id="ee773-218">Pokud chcete vytvořit pole, které nemá žádné elementy, deklarujte jeden z tohoto pole dimenze mít hodnotu -1, jak ukazuje následující příklad.</span><span class="sxs-lookup"><span data-stu-id="ee773-218">To create an array that has no elements, declare one of the array's dimensions to be -1, as the following example shows.</span></span>  
  
 [!code-vb[VbVbalrArrays#14](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#14)]  
  
 <span data-ttu-id="ee773-219">Můžete potřebovat pro vytvoření nulová délka pole v následujících případech:</span><span class="sxs-lookup"><span data-stu-id="ee773-219">You might need to create a zero-length array under the following circumstances:</span></span>  
  
-   <span data-ttu-id="ee773-220">Bez rizika <xref:System.NullReferenceException> výjimky, kódu musí přístup ke členům v <xref:System.Array> třídy, jako například <xref:System.Array.Length%2A> nebo <xref:System.Array.Rank%2A>, nebo volejte [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] funkce, jako <xref:Microsoft.VisualBasic.Information.UBound%2A>.</span><span class="sxs-lookup"><span data-stu-id="ee773-220">Without risking a <xref:System.NullReferenceException> exception, your code must access members of the <xref:System.Array> class, such as <xref:System.Array.Length%2A> or <xref:System.Array.Rank%2A>, or call a [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] function such as <xref:Microsoft.VisualBasic.Information.UBound%2A>.</span></span>  
  
-   <span data-ttu-id="ee773-221">Chcete zachovat jednodušší náročné kód tak, že není zkontrolujte `Nothing` zvláštním způsobem.</span><span class="sxs-lookup"><span data-stu-id="ee773-221">You want to keep the consuming code simpler by not having to check for `Nothing` as a special case.</span></span>  
  
-   <span data-ttu-id="ee773-222">Váš kód komunikuje s programovací rozhraní aplikace (API), který vyžaduje, abyste předat nulová délka pole jednoho nebo více procedur nebo vrátí pole nulovou délkou z jednoho nebo více procedur.</span><span class="sxs-lookup"><span data-stu-id="ee773-222">Your code interacts with an application programming interface (API) that either requires you to pass a zero-length array to one or more procedures or returns a zero-length array from one or more procedures.</span></span>  
  
##  <span data-ttu-id="ee773-223"><a name="BKMK_ArraySize"></a>Velikost pole</span><span class="sxs-lookup"><span data-stu-id="ee773-223"><a name="BKMK_ArraySize"></a> Array Size</span></span>  
 <span data-ttu-id="ee773-224">Velikost pole je produkt délek všechny jeho dimenze.</span><span class="sxs-lookup"><span data-stu-id="ee773-224">The size of an array is the product of the lengths of all its dimensions.</span></span> <span data-ttu-id="ee773-225">Představuje celkový počet elementů aktuálně obsažených v poli.</span><span class="sxs-lookup"><span data-stu-id="ee773-225">It represents the total number of elements currently contained in the array.</span></span>  
  
 <span data-ttu-id="ee773-226">Následující příklad deklaruje trojrozměrné.</span><span class="sxs-lookup"><span data-stu-id="ee773-226">The following example declares a three-dimensional array.</span></span>  
  
```vb
Dim prices(3, 4, 5) As Long  
```  
  
 <span data-ttu-id="ee773-227">Celkovou velikost pole v proměnné `prices` (3 + 1) x (4 + 1) x (5 + 1) = 120.</span><span class="sxs-lookup"><span data-stu-id="ee773-227">The overall size of the array in variable `prices` is (3 + 1) x (4 + 1) x (5 + 1) = 120.</span></span>  
  
 <span data-ttu-id="ee773-228">Velikost pole můžete najít pomocí <xref:System.Array.Length%2A> vlastnost.</span><span class="sxs-lookup"><span data-stu-id="ee773-228">You can find the size of an array by using the <xref:System.Array.Length%2A> property.</span></span> <span data-ttu-id="ee773-229">Délka Každá dimenze vícerozměrné můžete najít pomocí <xref:System.Array.GetLength%2A> metoda.</span><span class="sxs-lookup"><span data-stu-id="ee773-229">You can find the length of each dimension of a multi-dimensional array by using the <xref:System.Array.GetLength%2A> method.</span></span>  
  
 <span data-ttu-id="ee773-230">Velikost proměnné pole můžete změnit přiřazením nový objekt pole k němu nebo pomocí `ReDim` příkaz.</span><span class="sxs-lookup"><span data-stu-id="ee773-230">You can resize an array variable by assigning a new array object to it or by using the `ReDim` statement.</span></span>  
  
 <span data-ttu-id="ee773-231">Je třeba vzít v úvahu při plánování práce s velikost pole několik věcí.</span><span class="sxs-lookup"><span data-stu-id="ee773-231">There are several things to keep in mind when dealing with the size of an array.</span></span>  
  
|||  
|---|---|  
|<span data-ttu-id="ee773-232">Délka dimenze</span><span class="sxs-lookup"><span data-stu-id="ee773-232">Dimension Length</span></span>|<span data-ttu-id="ee773-233">Index Každá dimenze je založené na 0, což znamená, že ho rozsahu od 0 do jeho horní mez.</span><span class="sxs-lookup"><span data-stu-id="ee773-233">The index of each dimension is 0-based, which means it ranges from 0 through its upper bound.</span></span> <span data-ttu-id="ee773-234">Délka dané dimenze je proto o 1 větší deklarované horní mez pro daná dimenze.</span><span class="sxs-lookup"><span data-stu-id="ee773-234">Therefore, the length of a given dimension is greater by 1 than the declared upper bound for that dimension.</span></span>|  
|<span data-ttu-id="ee773-235">Omezení délky</span><span class="sxs-lookup"><span data-stu-id="ee773-235">Length Limits</span></span>|<span data-ttu-id="ee773-236">Délka všech dimenzí pole je omezená na maximální hodnota, která `Integer` datového typu, který je (2 ^ 31) - 1.</span><span class="sxs-lookup"><span data-stu-id="ee773-236">The length of every dimension of an array is limited to the maximum value of the `Integer` data type, which is (2 ^ 31) - 1.</span></span> <span data-ttu-id="ee773-237">Ale celková velikost pole je také omezena paměti k dispozici v systému.</span><span class="sxs-lookup"><span data-stu-id="ee773-237">However, the total size of an array is also limited by the memory available on your system.</span></span> <span data-ttu-id="ee773-238">Pokud pokus o inicializaci pole, přesahuje množství dostupné paměti RAM, vyvolá modul common language runtime <xref:System.OutOfMemoryException> výjimka.</span><span class="sxs-lookup"><span data-stu-id="ee773-238">If you attempt to initialize an array that exceeds the amount of available RAM, the common language runtime throws an <xref:System.OutOfMemoryException> exception.</span></span>|  
|<span data-ttu-id="ee773-239">Velikost a velikost – Element</span><span class="sxs-lookup"><span data-stu-id="ee773-239">Size and Element Size</span></span>|<span data-ttu-id="ee773-240">Velikost tohoto pole je nezávislé na datový typ jejích elementů.</span><span class="sxs-lookup"><span data-stu-id="ee773-240">An array's size is independent of the data type of its elements.</span></span> <span data-ttu-id="ee773-241">Velikost vždy představuje celkový počet elementů, nikoli počet bajtů, které budou využívat v úložišti.</span><span class="sxs-lookup"><span data-stu-id="ee773-241">The size always represents the total number of elements, not the number of bytes that they consume in storage.</span></span>|  
|<span data-ttu-id="ee773-242">Paměťové nároky</span><span class="sxs-lookup"><span data-stu-id="ee773-242">Memory Consumption</span></span>|<span data-ttu-id="ee773-243">Není bezpečné, aby všechny předpoklady týkající se uložení pole v paměti.</span><span class="sxs-lookup"><span data-stu-id="ee773-243">It is not safe to make any assumptions regarding how an array is stored in memory.</span></span> <span data-ttu-id="ee773-244">Úložiště se liší na platformy různých datových šířek, takže stejného pole můžete spotřebuje další paměť na 64bitovém systému než v 32bitové verzi systému.</span><span class="sxs-lookup"><span data-stu-id="ee773-244">Storage varies on platforms of different data widths, so the same array can consume more memory on a 64-bit system than on a 32-bit system.</span></span> <span data-ttu-id="ee773-245">V závislosti na konfiguraci systému při inicializaci pole, modul CLR (CLR) můžete přiřadit úložiště jako zavřete společně, nejdříve pack elementy nebo přizpůsobit je všechny na hranicích přirozené hardwaru.</span><span class="sxs-lookup"><span data-stu-id="ee773-245">Depending on system configuration when you initialize an array, the common language runtime (CLR) can assign storage either to pack elements as close together as possible, or to align them all on natural hardware boundaries.</span></span> <span data-ttu-id="ee773-246">Také pole úložiště režie vyžaduje pro své informace o řízení a režijní náklady na tato hodnota se zvyšuje s každá přidané dimenze.</span><span class="sxs-lookup"><span data-stu-id="ee773-246">Also, an array requires a storage overhead for its control information, and this overhead increases with each added dimension.</span></span>|  
  
##  <span data-ttu-id="ee773-247"><a name="BKMK_ArrayTypes"></a>Typy polí a dalších typů</span><span class="sxs-lookup"><span data-stu-id="ee773-247"><a name="BKMK_ArrayTypes"></a> Array Types and Other Types</span></span>  
 <span data-ttu-id="ee773-248">Každé pole má datový typ, ale liší se od datového typu jejích elementů.</span><span class="sxs-lookup"><span data-stu-id="ee773-248">Every array has a data type, but it differs from the data type of its elements.</span></span> <span data-ttu-id="ee773-249">Neexistuje žádný single – datový typ pro všechna pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-249">There is no single data type for all arrays.</span></span> <span data-ttu-id="ee773-250">Místo toho je datový typ pole určuje počet dimenzí, nebo *pořadí*, pole a datový typ elementů v poli.</span><span class="sxs-lookup"><span data-stu-id="ee773-250">Instead, the data type of an array is determined by the number of dimensions, or *rank*, of the array, and the data type of the elements in the array.</span></span> <span data-ttu-id="ee773-251">Dvě proměnné pole, které jsou považovány za mít stejný datový typ, pouze pokud mají stejné pořadí a jejich elementů mít stejný datový typ.</span><span class="sxs-lookup"><span data-stu-id="ee773-251">Two array variables are considered to be of the same data type only when they have the same rank and their elements have the same data type.</span></span> <span data-ttu-id="ee773-252">Dimenze v pole délek neovlivňují datový typ pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-252">The lengths of the dimensions in an array do not influence the array data type.</span></span>  
  
 <span data-ttu-id="ee773-253">Každé pole dědí z <xref:System.Array?displayProperty=nameWithType> třídy a můžou deklarovat proměnnou být typu `Array`, ale nelze vytvořit pole typu `Array`.</span><span class="sxs-lookup"><span data-stu-id="ee773-253">Every array inherits from the <xref:System.Array?displayProperty=nameWithType> class, and you can declare a variable to be of type `Array`, but you cannot create an array of type `Array`.</span></span> <span data-ttu-id="ee773-254">Navíc [ReDim – příkaz](../../../../visual-basic/language-reference/statements/redim-statement.md) nemůže pracovat na proměnnou deklarován jako typ `Array`.</span><span class="sxs-lookup"><span data-stu-id="ee773-254">Also, the [ReDim Statement](../../../../visual-basic/language-reference/statements/redim-statement.md) cannot operate on a variable declared as type `Array`.</span></span> <span data-ttu-id="ee773-255">Z těchto důvodů a pro bezpečnost typů, je třeba deklarovat každé pole jako specifického typu, například `Integer` v předchozím příkladu.</span><span class="sxs-lookup"><span data-stu-id="ee773-255">For these reasons, and for type safety, it is advisable to declare every array as a specific type, such as `Integer` in the preceding example.</span></span>  
  
 <span data-ttu-id="ee773-256">Můžete zjistit datový typ pole nebo jeho prvky několika způsoby.</span><span class="sxs-lookup"><span data-stu-id="ee773-256">You can find out the data type of either an array or its elements in several ways.</span></span>  
  
-   <span data-ttu-id="ee773-257">Můžete volat <xref:System.Object.GetType%2A?displayProperty=nameWithType> metoda na proměnnou pro příjem <xref:System.Type> objektu pro typ spuštění proměnné.</span><span class="sxs-lookup"><span data-stu-id="ee773-257">You can call the <xref:System.Object.GetType%2A?displayProperty=nameWithType> method on the variable to receive a <xref:System.Type> object for the run-time type of the variable.</span></span> <span data-ttu-id="ee773-258"><xref:System.Type> Objektu obsahuje velké množství informací v její vlastnosti a metody.</span><span class="sxs-lookup"><span data-stu-id="ee773-258">The <xref:System.Type> object holds extensive information in its properties and methods.</span></span>  
  
-   <span data-ttu-id="ee773-259">Můžete předat proměnnou <xref:Microsoft.VisualBasic.Information.TypeName%2A> funkce pro příjem `String` obsahující název typu běhu.</span><span class="sxs-lookup"><span data-stu-id="ee773-259">You can pass the variable to the <xref:Microsoft.VisualBasic.Information.TypeName%2A> function to receive a `String` containing the name of run-time type.</span></span>  
  
-   <span data-ttu-id="ee773-260">Můžete předat proměnnou <xref:Microsoft.VisualBasic.Information.VarType%2A> funkce pro příjem `VariantType` hodnotu představující klasifikace typ proměnné.</span><span class="sxs-lookup"><span data-stu-id="ee773-260">You can pass the variable to the <xref:Microsoft.VisualBasic.Information.VarType%2A> function to receive a `VariantType` value representing the type classification of the variable.</span></span>  
  
 <span data-ttu-id="ee773-261">Následující příklad volání `TypeName` funkce k určení typu pole a typ elementů v poli.</span><span class="sxs-lookup"><span data-stu-id="ee773-261">The following example calls the `TypeName` function to determine the type of the array and the type of the elements in the array.</span></span> <span data-ttu-id="ee773-262">Typ pole je `Integer(,)` a prvků v poli typu `Integer`.</span><span class="sxs-lookup"><span data-stu-id="ee773-262">The array type is `Integer(,)` and the elements in the array are of type `Integer`.</span></span>  
  
 [!code-vb[VbVbalrArrays#15](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#15)]  
  
##  <span data-ttu-id="ee773-263"><a name="BKMK_Collections"></a>Kolekce jako alternativu k polím</span><span class="sxs-lookup"><span data-stu-id="ee773-263"><a name="BKMK_Collections"></a> Collections as an Alternative to Arrays</span></span>  
 <span data-ttu-id="ee773-264">Pole jsou velmi užitečné pro vytváření a práci s pevný počet objektů se silným typem.</span><span class="sxs-lookup"><span data-stu-id="ee773-264">Arrays are most useful for creating and working with a fixed number of strongly typed objects.</span></span> <span data-ttu-id="ee773-265">Kolekce poskytují flexibilnější způsob, jak pracovat s skupiny objektů.</span><span class="sxs-lookup"><span data-stu-id="ee773-265">Collections provide a more flexible way to work with groups of objects.</span></span> <span data-ttu-id="ee773-266">Na rozdíl od pole skupinu objektů, které pracují se můžete zvýšit nebo snížit dynamicky potřebám změna aplikace.</span><span class="sxs-lookup"><span data-stu-id="ee773-266">Unlike arrays, the group of objects that you work with can grow and shrink dynamically as the needs of the application change.</span></span>  
  
 <span data-ttu-id="ee773-267">Pokud potřebujete změnit velikost pole, je nutné použít [ReDim – příkaz](../../../../visual-basic/language-reference/statements/redim-statement.md).</span><span class="sxs-lookup"><span data-stu-id="ee773-267">If you need to change the size of an array, you must use the [ReDim Statement](../../../../visual-basic/language-reference/statements/redim-statement.md).</span></span> <span data-ttu-id="ee773-268">Při tomto, [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] vytvoří nové pole a uvolní předchozí pole pro uvolnění.</span><span class="sxs-lookup"><span data-stu-id="ee773-268">When you do this, [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] creates a new array and releases the previous array for disposal.</span></span> <span data-ttu-id="ee773-269">Tato akce trvá dobu provádění.</span><span class="sxs-lookup"><span data-stu-id="ee773-269">This takes execution time.</span></span> <span data-ttu-id="ee773-270">Proto pokud počet položek, které pracují se často mění, nebo nelze předpovědět maximální počet položek, které potřebujete, může získat lepší výkon pomocí kolekce.</span><span class="sxs-lookup"><span data-stu-id="ee773-270">Therefore, if the number of items you are working with changes frequently, or you cannot predict the maximum number of items you need, you might obtain better performance using a collection.</span></span>  
  
 <span data-ttu-id="ee773-271">Pro některé kolekce můžete přiřadit klíč pro libovolný objekt, který vložíte do kolekce, takže můžete rychle načíst objekt pomocí klíče.</span><span class="sxs-lookup"><span data-stu-id="ee773-271">For some collections, you can assign a key to any object that you put into the collection so that you can quickly retrieve the object by using the key.</span></span>  
  
 <span data-ttu-id="ee773-272">Pokud kolekce obsahuje prvky pouze jeden datový typ, můžete použít jednu z tříd ve <xref:System.Collections.Generic?displayProperty=nameWithType> oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="ee773-272">If your collection contains elements of only one data type, you can use one of the classes in the <xref:System.Collections.Generic?displayProperty=nameWithType> namespace.</span></span> <span data-ttu-id="ee773-273">Obecné kolekce vynucuje bezpečnost typů tak, aby žádný datový typ lze přidat do ní.</span><span class="sxs-lookup"><span data-stu-id="ee773-273">A generic collection enforces type safety so that no other data type can be added to it.</span></span> <span data-ttu-id="ee773-274">Při načítání element z obecnou kolekci, není nutné určit její datový typ nebo je převeďte.</span><span class="sxs-lookup"><span data-stu-id="ee773-274">When you retrieve an element from a generic collection, you do not have to determine its data type or convert it.</span></span>  
  
 <span data-ttu-id="ee773-275">Další informace o kolekcích najdete v tématu [kolekce](http://msdn.microsoft.com/library/e76533a9-5033-4a0b-b003-9c2be60d185b).</span><span class="sxs-lookup"><span data-stu-id="ee773-275">For more information about collections, see [Collections](http://msdn.microsoft.com/library/e76533a9-5033-4a0b-b003-9c2be60d185b).</span></span>  
  
### <a name="example"></a><span data-ttu-id="ee773-276">Příklad</span><span class="sxs-lookup"><span data-stu-id="ee773-276">Example</span></span>  
 <span data-ttu-id="ee773-277">Následující příklad používá [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] – obecná třída <xref:System.Collections.Generic.List%601?displayProperty=nameWithType> k vytvoření kolekce seznamu `Customer` objekty.</span><span class="sxs-lookup"><span data-stu-id="ee773-277">The following example uses the [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] generic class <xref:System.Collections.Generic.List%601?displayProperty=nameWithType> to create a list collection of `Customer` objects.</span></span>  
  
 [!code-vb[VbVbalrArrays#1](../../../../../samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrArrays/VB/Class1.vb#1)]  
  
 <span data-ttu-id="ee773-278">Prohlášení o `CustomerFile` kolekce Určuje, že může obsahovat pouze elementy typu `Customer`.</span><span class="sxs-lookup"><span data-stu-id="ee773-278">The declaration of the `CustomerFile` collection specifies that it can contain elements only of type `Customer`.</span></span> <span data-ttu-id="ee773-279">Umožňuje také počáteční kapacita 200 elementů.</span><span class="sxs-lookup"><span data-stu-id="ee773-279">It also provides for an initial capacity of 200 elements.</span></span> <span data-ttu-id="ee773-280">Postup `AddNewCustomer` kontroluje nového elementu pro platnosti a poté jej přidá do kolekce.</span><span class="sxs-lookup"><span data-stu-id="ee773-280">The procedure `AddNewCustomer` checks the new element for validity and then adds it to the collection.</span></span> <span data-ttu-id="ee773-281">Postup `PrintCustomers` používá `For Each` smyčky k procházení kolekce a zobrazení jejích elementů.</span><span class="sxs-lookup"><span data-stu-id="ee773-281">The procedure `PrintCustomers` uses a `For Each` loop to traverse the collection and display its elements.</span></span>  
  
## <a name="related-topics"></a><span data-ttu-id="ee773-282">Související témata</span><span class="sxs-lookup"><span data-stu-id="ee773-282">Related Topics</span></span>  
  
|<span data-ttu-id="ee773-283">Termín</span><span class="sxs-lookup"><span data-stu-id="ee773-283">Term</span></span>|<span data-ttu-id="ee773-284">Definice</span><span class="sxs-lookup"><span data-stu-id="ee773-284">Definition</span></span>|  
|----------|----------------|  
|[<span data-ttu-id="ee773-285">Rozměry pole v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="ee773-285">Array Dimensions in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/array-dimensions.md)|<span data-ttu-id="ee773-286">Popisuje pořadí a dimenzí v polích.</span><span class="sxs-lookup"><span data-stu-id="ee773-286">Explains rank and dimensions in arrays.</span></span>|  
|[<span data-ttu-id="ee773-287">Postupy: Inicializace proměnné pole v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="ee773-287">How to: Initialize an Array Variable in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/how-to-initialize-an-array-variable.md)|<span data-ttu-id="ee773-288">Popisuje, jak k vyplnění polí se počáteční hodnoty.</span><span class="sxs-lookup"><span data-stu-id="ee773-288">Describes how to populate arrays with initial values.</span></span>|  
|[<span data-ttu-id="ee773-289">Postupy: řazení pole v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="ee773-289">How to: Sort An Array in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/how-to-sort-an-array.md)|<span data-ttu-id="ee773-290">Ukazuje, jak abecedně seřaďte prvků pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-290">Shows how to sort the elements of an array alphabetically.</span></span>|  
|[<span data-ttu-id="ee773-291">Postupy: přiřazení jednoho pole ke druhému</span><span class="sxs-lookup"><span data-stu-id="ee773-291">How to: Assign One Array to Another Array</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/how-to-assign-one-array-to-another-array.md)|<span data-ttu-id="ee773-292">Popisuje pravidla a kroky pro přiřazení k jiné proměnné pole pole.</span><span class="sxs-lookup"><span data-stu-id="ee773-292">Describes the rules and steps for assigning an array to another array variable.</span></span>|  
|[<span data-ttu-id="ee773-293">Řešení potíží s poli</span><span class="sxs-lookup"><span data-stu-id="ee773-293">Troubleshooting Arrays</span></span>](../../../../visual-basic/programming-guide/language-features/arrays/troubleshooting-arrays.md)|<span data-ttu-id="ee773-294">Popisuje některé běžné problémy, které vznikají při práci s poli.</span><span class="sxs-lookup"><span data-stu-id="ee773-294">Discusses some common problems that arise when working with arrays.</span></span>|  
  
## <a name="see-also"></a><span data-ttu-id="ee773-295">Viz také</span><span class="sxs-lookup"><span data-stu-id="ee773-295">See Also</span></span>  
 <xref:System.Array>  
 [<span data-ttu-id="ee773-296">Dim – příkaz</span><span class="sxs-lookup"><span data-stu-id="ee773-296">Dim Statement</span></span>](../../../../visual-basic/language-reference/statements/dim-statement.md)  
 [<span data-ttu-id="ee773-297">ReDim – příkaz</span><span class="sxs-lookup"><span data-stu-id="ee773-297">ReDim Statement</span></span>](../../../../visual-basic/language-reference/statements/redim-statement.md)
