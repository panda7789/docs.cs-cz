---
title: "Doba života objektu: Vytváření a zničení objektů (Visual Basic)"
ms.custom: 
ms.date: 07/20/2015
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology:
- devlang-visual-basic
ms.topic: article
f1_keywords:
- vb.Constructor
helpviewer_keywords:
- destructors, object lifetime
- Sub Finalize destructor
- objects [Visual Basic], destroying
- lifetime [Visual Basic], objects
- Sub New constructor, object lifetime
- Finalize method [Visual Basic], object lifetime
- objects [Visual Basic], creating
- Class_Terminate
- Dispose method [Visual Basic], object lifetime
- Class_Initialize
- object creation [Visual Basic], object lifetime
- parameterized constructors
- objects [Visual Basic], lifetime
- objects [Visual Basic], garbage collection
- constructors [Visual Basic], object lifetime
- Sub Dispose destructor
- garbage collection [Visual Basic], Visual Basic
ms.assetid: f1ee8458-b156-44e0-9a8a-5dd171648cd8
caps.latest.revision: 
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: f985d6bf7b26ec22d6e533eae1f1d7ea0682e56c
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 11/21/2017
---
# <a name="object-lifetime-how-objects-are-created-and-destroyed-visual-basic"></a><span data-ttu-id="f46ff-102">Doba života objektu: Vytváření a zničení objektů (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="f46ff-102">Object Lifetime: How Objects Are Created and Destroyed (Visual Basic)</span></span>
<span data-ttu-id="f46ff-103">Instance třídy objekt, je vytvořená pomocí `New` – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="f46ff-103">An instance of a class, an object, is created by using the `New` keyword.</span></span> <span data-ttu-id="f46ff-104">Úlohy inicializace často musí být provedeny na nové objekty před jejich použití.</span><span class="sxs-lookup"><span data-stu-id="f46ff-104">Initialization tasks often must be performed on new objects before they are used.</span></span> <span data-ttu-id="f46ff-105">Běžné úlohy inicializace zahrnují otevírání souborů, připojení k databázím a čtení hodnoty klíčů registru.</span><span class="sxs-lookup"><span data-stu-id="f46ff-105">Common initialization tasks include opening files, connecting to databases, and reading values of registry keys.</span></span> <span data-ttu-id="f46ff-106">Inicializace nových objektů pomocí procedury volané ovládací prvky jazyka Visual Basic *konstruktory* (speciální metody, které umožňují řídit inicializace).</span><span class="sxs-lookup"><span data-stu-id="f46ff-106">Visual Basic controls the initialization of new objects using procedures called *constructors* (special methods that allow control over initialization).</span></span>  
  
 <span data-ttu-id="f46ff-107">Objekt odešel oboru, vydal common language runtime (CLR).</span><span class="sxs-lookup"><span data-stu-id="f46ff-107">After an object leaves scope, it is released by the common language runtime (CLR).</span></span> <span data-ttu-id="f46ff-108">Ovládací prvky jazyka Visual Basic verzi systémové prostředky pomocí procedury volané *destruktory*.</span><span class="sxs-lookup"><span data-stu-id="f46ff-108">Visual Basic controls the release of system resources using procedures called *destructors*.</span></span> <span data-ttu-id="f46ff-109">Konstruktory a destruktory společně podporují vytvoření knihovny tříd robustní a předvídatelné.</span><span class="sxs-lookup"><span data-stu-id="f46ff-109">Together, constructors and destructors support the creation of robust and predictable class libraries.</span></span>  
  
## <a name="using-constructors-and-destructors"></a><span data-ttu-id="f46ff-110">Použití konstruktorů a destruktorů</span><span class="sxs-lookup"><span data-stu-id="f46ff-110">Using Constructors and Destructors</span></span>  
 <span data-ttu-id="f46ff-111">Konstruktory a destruktory řídí vytváření a odstraňování objektů.</span><span class="sxs-lookup"><span data-stu-id="f46ff-111">Constructors and destructors control the creation and destruction of objects.</span></span> <span data-ttu-id="f46ff-112">`Sub New` a `Sub Finalize` procedury v jazyce Visual Basic inicializace a zrušení objekty; nahrazují `Class_Initialize` a `Class_Terminate` metody používané v [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] 6.0 a starší verze.</span><span class="sxs-lookup"><span data-stu-id="f46ff-112">The `Sub New` and `Sub Finalize` procedures in Visual Basic initialize and destroy objects; they replace the `Class_Initialize` and `Class_Terminate` methods used in [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] 6.0 and earlier versions.</span></span>  
  
### <a name="sub-new"></a><span data-ttu-id="f46ff-113">Sub – nový</span><span class="sxs-lookup"><span data-stu-id="f46ff-113">Sub New</span></span>  
 <span data-ttu-id="f46ff-114">`Sub New` Konstruktor může spustit pouze po vytvoření třídy.</span><span class="sxs-lookup"><span data-stu-id="f46ff-114">The `Sub New` constructor can run only once when a class is created.</span></span> <span data-ttu-id="f46ff-115">Ji nelze volat explicitně kdekoli jinak než v prvním řádku kódu jiný konstruktor ze stejné třídy nebo z odvozené třídy.</span><span class="sxs-lookup"><span data-stu-id="f46ff-115">It cannot be called explicitly anywhere other than in the first line of code of another constructor from either the same class or from a derived class.</span></span> <span data-ttu-id="f46ff-116">Kromě toho kód `Sub New` metoda vždy spuštěna před jakýkoli jiný kód v třídě.</span><span class="sxs-lookup"><span data-stu-id="f46ff-116">Furthermore, the code in the `Sub New` method always runs before any other code in a class.</span></span> [!INCLUDE[vbprvblong](~/includes/vbprvblong-md.md)]<span data-ttu-id="f46ff-117">a novějších verzích implicitně vytvořit `Sub New` konstruktor za běhu, pokud nejsou explicitně definovány `Sub New` postup pro třídu.</span><span class="sxs-lookup"><span data-stu-id="f46ff-117"> and later versions implicitly create a `Sub New` constructor at run time if you do not explicitly define a `Sub New` procedure for a class.</span></span>  
  
 <span data-ttu-id="f46ff-118">Pokud chcete vytvořit konstruktor pro třídu, vytvoření procedury s názvem `Sub New` kdekoli v definici třídy.</span><span class="sxs-lookup"><span data-stu-id="f46ff-118">To create a constructor for a class, create a procedure named `Sub New` anywhere in the class definition.</span></span> <span data-ttu-id="f46ff-119">Pokud chcete vytvořit parametrizované konstruktor, zadejte názvy a datové typy argumentů `Sub New` stejně, jako by zadejte argumenty pro další postup, jako v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="f46ff-119">To create a parameterized constructor, specify the names and data types of arguments to `Sub New` just as you would specify arguments for any other procedure, as in the following code:</span></span>  
  
 [!code-vb[VbVbalrOOP#42](../../../../visual-basic/misc/codesnippet/VisualBasic/object-lifetime-how-objects-are-created-and-destroyed_1.vb)]  
  
 <span data-ttu-id="f46ff-120">Konstruktory jsou často přetížené, jako v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="f46ff-120">Constructors are frequently overloaded, as in the following code:</span></span>  
  
 [!code-vb[VbVbalrOOP#116](../../../../visual-basic/misc/codesnippet/VisualBasic/object-lifetime-how-objects-are-created-and-destroyed_2.vb)]  
  
 <span data-ttu-id="f46ff-121">Když definujete třídy odvozené z jiné třídy, první řádek konstruktor musí být volání konstruktoru základní třídy, pokud základní třída má dostupný konstruktor, které nepřijímá žádné parametry.</span><span class="sxs-lookup"><span data-stu-id="f46ff-121">When you define a class derived from another class, the first line of a constructor must be a call to the constructor of the base class, unless the base class has an accessible constructor that takes no parameters.</span></span> <span data-ttu-id="f46ff-122">Volání základní třídy, která obsahuje výše konstruktoru, například by `MyBase.New(s)`.</span><span class="sxs-lookup"><span data-stu-id="f46ff-122">A call to the base class that contains the above constructor, for example, would be `MyBase.New(s)`.</span></span> <span data-ttu-id="f46ff-123">V opačném `MyBase.New` je volitelná a [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] runtime nazve je implicitně.</span><span class="sxs-lookup"><span data-stu-id="f46ff-123">Otherwise, `MyBase.New` is optional, and the [!INCLUDE[vbprvb](~/includes/vbprvb-md.md)] runtime calls it implicitly.</span></span>  
  
 <span data-ttu-id="f46ff-124">Když napíšete kód volat konstruktor nadřazený objekt, můžete přidat žádné další inicializace kód pro `Sub New` postupu.</span><span class="sxs-lookup"><span data-stu-id="f46ff-124">After you write the code to call the parent object's constructor, you can add any additional initialization code to the `Sub New` procedure.</span></span> <span data-ttu-id="f46ff-125">`Sub New`Můžete přijmout argumenty při volání jako parametrizované konstruktor.</span><span class="sxs-lookup"><span data-stu-id="f46ff-125">`Sub New` can accept arguments when called as a parameterized constructor.</span></span> <span data-ttu-id="f46ff-126">Tyto parametry se předávají v postupu volání konstruktoru, například `Dim AnObject As New ThisClass(X)`.</span><span class="sxs-lookup"><span data-stu-id="f46ff-126">These parameters are passed from the procedure calling the constructor, for example, `Dim AnObject As New ThisClass(X)`.</span></span>  
  
### <a name="sub-finalize"></a><span data-ttu-id="f46ff-127">Sub Finalize</span><span class="sxs-lookup"><span data-stu-id="f46ff-127">Sub Finalize</span></span>  
 <span data-ttu-id="f46ff-128">Před uvolněním objekty, budou automaticky volání modulu CLR `Finalize` metodu pro objekty, které definují `Sub Finalize` postupu.</span><span class="sxs-lookup"><span data-stu-id="f46ff-128">Before releasing objects, the CLR automatically calls the `Finalize` method for objects that define a `Sub Finalize` procedure.</span></span> <span data-ttu-id="f46ff-129">`Finalize` Metoda může obsahovat kód, který je potřeba provést těsně před zničení objektu, například kód pro zavírání souborů a ukládání informací o stavu.</span><span class="sxs-lookup"><span data-stu-id="f46ff-129">The `Finalize` method can contain code that needs to execute just before an object is destroyed, such as code for closing files and saving state information.</span></span> <span data-ttu-id="f46ff-130">Je snížení výkonu mírné pro provádění `Sub Finalize`, takže byste měli definovat `Sub Finalize` metoda jenom v případě, že je potřeba explicitně uvolnění objektů.</span><span class="sxs-lookup"><span data-stu-id="f46ff-130">There is a slight performance penalty for executing `Sub Finalize`, so you should define a `Sub Finalize` method only when you need to release objects explicitly.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="f46ff-131">Uvolňování paměti v modulu CLR není (a nemůže) odstranění *nespravované objekty*, objekty, které operační systém provede přímo, mimo prostředí CLR.</span><span class="sxs-lookup"><span data-stu-id="f46ff-131">The garbage collector in the CLR does not (and cannot) dispose of *unmanaged objects*, objects that the operating system executes directly, outside the CLR environment.</span></span> <span data-ttu-id="f46ff-132">Je to proto, že různé nespravované objekty je nutné odstranit z různými způsoby.</span><span class="sxs-lookup"><span data-stu-id="f46ff-132">This is because different unmanaged objects must be disposed of in different ways.</span></span> <span data-ttu-id="f46ff-133">Tyto informace není přímo spojeny s nespravované objektem; je nutné nalézt v dokumentaci pro objekt.</span><span class="sxs-lookup"><span data-stu-id="f46ff-133">That information is not directly associated with the unmanaged object; it must be found in the documentation for the object.</span></span> <span data-ttu-id="f46ff-134">Třídu, která využívá nespravované objekty musí dispose z nich v jeho `Finalize` metoda.</span><span class="sxs-lookup"><span data-stu-id="f46ff-134">A class that uses unmanaged objects must dispose of them in its `Finalize` method.</span></span>  
  
 <span data-ttu-id="f46ff-135">`Finalize` Destruktor je chráněná metoda, kterou lze volat pouze z třídy patří do nebo z odvozené třídy.</span><span class="sxs-lookup"><span data-stu-id="f46ff-135">The `Finalize` destructor is a protected method that can be called only from the class it belongs to, or from derived classes.</span></span> <span data-ttu-id="f46ff-136">Systémová volání `Finalize` automaticky při objekt zničení, takže by neměla explicitně volání `Finalize` z mimo odvozené třídy `Finalize` implementace.</span><span class="sxs-lookup"><span data-stu-id="f46ff-136">The system calls `Finalize` automatically when an object is destroyed, so you should not explicitly call `Finalize` from outside of a derived class's `Finalize` implementation.</span></span>  
  
 <span data-ttu-id="f46ff-137">Na rozdíl od `Class_Terminate`, který provede co nejrychleji objekt je nastaven na hodnotu nothing, obvykle dochází ke zpoždění mezi při objekt ztratí oboru a při volání jazyka Visual Basic `Finalize` destruktor.</span><span class="sxs-lookup"><span data-stu-id="f46ff-137">Unlike `Class_Terminate`, which executes as soon as an object is set to nothing, there is usually a delay between when an object loses scope and when Visual Basic calls the `Finalize` destructor.</span></span> [!INCLUDE[vbprvblong](~/includes/vbprvblong-md.md)]<span data-ttu-id="f46ff-138">a novějších verzích povolit pro druhý druh destruktor, <xref:System.IDisposable.Dispose%2A>, což může být explicitně volána kdykoli a uvolnit prostředky.</span><span class="sxs-lookup"><span data-stu-id="f46ff-138"> and later versions allow for a second kind of destructor, <xref:System.IDisposable.Dispose%2A>, which can be explicitly called at any time to immediately release resources.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="f46ff-139">A `Finalize` – destruktor nesmí vyvolat výjimky, protože nelze zpracovat, aplikací a může způsobit, že je aplikace ukončena.</span><span class="sxs-lookup"><span data-stu-id="f46ff-139">A `Finalize` destructor should not throw exceptions, because they cannot be handled by the application and can cause the application to terminate.</span></span>  
  
### <a name="how-new-and-finalize-methods-work-in-a-class-hierarchy"></a><span data-ttu-id="f46ff-140">Jak nové a dokončit pracovní metody v hierarchii – třída</span><span class="sxs-lookup"><span data-stu-id="f46ff-140">How New and Finalize Methods Work in a Class Hierarchy</span></span>  
 <span data-ttu-id="f46ff-141">Vždy, když je vytvořena instance třídy, modul CLR (CLR) pokusí spustit proceduru s názvem `New`, pokud existuje v tomto objektu.</span><span class="sxs-lookup"><span data-stu-id="f46ff-141">Whenever an instance of a class is created, the common language runtime (CLR) attempts to execute a procedure named `New`, if it exists in that object.</span></span> <span data-ttu-id="f46ff-142">`New`je typ postupu názvem `constructor` sloužící k inicializaci nové objekty, před provedením dalších kódu v objektu.</span><span class="sxs-lookup"><span data-stu-id="f46ff-142">`New` is a type of procedure called a `constructor` that is used to initialize new objects before any other code in an object executes.</span></span> <span data-ttu-id="f46ff-143">A `New` konstruktor lze použít k otevření souborů, připojení k databázím, inicializace proměnné a postará o další úlohy, které je třeba provést před použitím objektu.</span><span class="sxs-lookup"><span data-stu-id="f46ff-143">A `New` constructor can be used to open files, connect to databases, initialize variables, and take care of any other tasks that need to be done before an object can be used.</span></span>  
  
 <span data-ttu-id="f46ff-144">Při vytváření instance odvozené třídy `Sub New` konstruktor základní třídy provede první, za nímž následuje konstruktorů v odvozených třídách.</span><span class="sxs-lookup"><span data-stu-id="f46ff-144">When an instance of a derived class is created, the `Sub New` constructor of the base class executes first, followed by constructors in derived classes.</span></span> <span data-ttu-id="f46ff-145">K tomu dojde, protože první řádek kódu `Sub New` konstruktor se používá syntaxe `MyBase.New()`volání konstruktoru třídy okamžitě výše sám v hierarchii tříd.</span><span class="sxs-lookup"><span data-stu-id="f46ff-145">This happens because the first line of code in a `Sub New` constructor uses the syntax `MyBase.New()`to call the constructor of the class immediately above itself in the class hierarchy.</span></span> <span data-ttu-id="f46ff-146">`Sub New` Pak volání konstruktoru pro každou třídu v hierarchii tříd do konstruktoru pro dosažení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="f46ff-146">The `Sub New` constructor is then called for each class in the class hierarchy until the constructor for the base class is reached.</span></span> <span data-ttu-id="f46ff-147">V tomto okamžiku kód v konstruktoru pro základní třídu provede, za nímž následuje kód v jednotlivých konstruktor pro všechny odvozené třídy a poslední spuštění kódu v maximální odvozené funkcí.</span><span class="sxs-lookup"><span data-stu-id="f46ff-147">At that point, the code in the constructor for the base class executes, followed by the code in each constructor in all derived classes and the code in the most derived classes is executed last.</span></span>  
  
 <span data-ttu-id="f46ff-148">![Konstruktory a dědičnost](../../../../visual-basic/programming-guide/language-features/objects-and-classes/media/vaconstructorsinheritance.gif "vaConstructorsInheritance")</span><span class="sxs-lookup"><span data-stu-id="f46ff-148">![Constructors and Inheritance](../../../../visual-basic/programming-guide/language-features/objects-and-classes/media/vaconstructorsinheritance.gif "vaConstructorsInheritance")</span></span>  
  
 <span data-ttu-id="f46ff-149">Pokud objekt je již nepotřebujete, volání modulu CLR <xref:System.Object.Finalize%2A> metodu pro tento objekt před uvolnění paměti.</span><span class="sxs-lookup"><span data-stu-id="f46ff-149">When an object is no longer needed, the CLR calls the <xref:System.Object.Finalize%2A> method for that object before freeing its memory.</span></span> <span data-ttu-id="f46ff-150"><xref:System.Object.Finalize%2A> Metoda je volána `destructor` protože provádí úlohy čištění, jako je například ukládá informace o stavu, zavírání souborů a připojení k databázím a dalších úlohách, které je třeba provést před uvolněním objektu.</span><span class="sxs-lookup"><span data-stu-id="f46ff-150">The <xref:System.Object.Finalize%2A> method is called a `destructor` because it performs cleanup tasks, such as saving state information, closing files and connections to databases, and other tasks that must be done before releasing the object.</span></span>  
  
 <span data-ttu-id="f46ff-151">![Konstruktory Inheritance2](../../../../visual-basic/programming-guide/language-features/objects-and-classes/media/vaconstructorsinheritance_2.gif "vaConstructorsInheritance_2")</span><span class="sxs-lookup"><span data-stu-id="f46ff-151">![Constructors Inheritance2](../../../../visual-basic/programming-guide/language-features/objects-and-classes/media/vaconstructorsinheritance_2.gif "vaConstructorsInheritance_2")</span></span>  
  
## <a name="idisposable-interface"></a><span data-ttu-id="f46ff-152">Rozhraní IDisposable</span><span class="sxs-lookup"><span data-stu-id="f46ff-152">IDisposable Interface</span></span>  
 <span data-ttu-id="f46ff-153">Instance třídy často řídit prostředky, které nejsou spravované přes modul CLR, jako je například obslužné rutiny Windows a připojení k databázi.</span><span class="sxs-lookup"><span data-stu-id="f46ff-153">Class instances often control resources not managed by the CLR, such as Windows handles and database connections.</span></span> <span data-ttu-id="f46ff-154">Tyto prostředky je nutné odstranit z v `Finalize` metoda třídy, tak, aby se budou vydané při objekt zničen modulem garbage collector.</span><span class="sxs-lookup"><span data-stu-id="f46ff-154">These resources must be disposed of in the `Finalize` method of the class, so that they will be released when the object is destroyed by the garbage collector.</span></span> <span data-ttu-id="f46ff-155">Uvolňování paměti však odstraní objekty pouze v případě modulu CLR vyžaduje další volné paměti.</span><span class="sxs-lookup"><span data-stu-id="f46ff-155">However, the garbage collector destroys objects only when the CLR requires more free memory.</span></span> <span data-ttu-id="f46ff-156">To znamená, že prostředky nemusí být vydán dokud dlouho po opuštění oboru.</span><span class="sxs-lookup"><span data-stu-id="f46ff-156">This means that the resources may not be released until long after the object goes out of scope.</span></span>  
  
 <span data-ttu-id="f46ff-157">Doplníte uvolňování paměti, může vaše třídy poskytují mechanismus aktivně Správa systémových prostředků, pokud se implementovat <xref:System.IDisposable> rozhraní.</span><span class="sxs-lookup"><span data-stu-id="f46ff-157">To supplement garbage collection, your classes can provide a mechanism to actively manage system resources if they implement the <xref:System.IDisposable> interface.</span></span> <span data-ttu-id="f46ff-158"><xref:System.IDisposable>má jednu metodu <xref:System.IDisposable.Dispose%2A>, klienty, kteří by měly volat po ukončení používání objektu.</span><span class="sxs-lookup"><span data-stu-id="f46ff-158"><xref:System.IDisposable> has one method, <xref:System.IDisposable.Dispose%2A>, which clients should call when they finish using an object.</span></span> <span data-ttu-id="f46ff-159">Můžete použít <xref:System.IDisposable.Dispose%2A> metoda okamžitě uvolnění prostředků a provádět úlohy, jako je zavírání souborů a připojení databáze.</span><span class="sxs-lookup"><span data-stu-id="f46ff-159">You can use the <xref:System.IDisposable.Dispose%2A> method to immediately release resources and perform tasks such as closing files and database connections.</span></span> <span data-ttu-id="f46ff-160">Na rozdíl od `Finalize` destruktor, <xref:System.IDisposable.Dispose%2A> metoda není volána automaticky.</span><span class="sxs-lookup"><span data-stu-id="f46ff-160">Unlike the `Finalize` destructor, the <xref:System.IDisposable.Dispose%2A> method is not called automatically.</span></span> <span data-ttu-id="f46ff-161">Klienti třídy musí explicitně volat <xref:System.IDisposable.Dispose%2A> když chcete okamžitě uvolnění prostředků.</span><span class="sxs-lookup"><span data-stu-id="f46ff-161">Clients of a class must explicitly call <xref:System.IDisposable.Dispose%2A> when you want to immediately release resources.</span></span>  
  
### <a name="implementing-idisposable"></a><span data-ttu-id="f46ff-162">Implementace rozhraní IDisposable</span><span class="sxs-lookup"><span data-stu-id="f46ff-162">Implementing IDisposable</span></span>  
 <span data-ttu-id="f46ff-163">Třídu, která implementuje <xref:System.IDisposable> rozhraní by měla obsahovat tyto části kódu:</span><span class="sxs-lookup"><span data-stu-id="f46ff-163">A class that implements the <xref:System.IDisposable> interface should include these sections of code:</span></span>  
  
-   <span data-ttu-id="f46ff-164">Pole pro udržování přehledu o, zda objekt byl zlikvidován:</span><span class="sxs-lookup"><span data-stu-id="f46ff-164">A field for keeping track of whether the object has been disposed:</span></span>  
  
    ```  
    Protected disposed As Boolean = False  
    ```  
  
-   <span data-ttu-id="f46ff-165">Přetížení <xref:System.IDisposable.Dispose%2A> který uvolní prostředky třídy.</span><span class="sxs-lookup"><span data-stu-id="f46ff-165">An overload of the <xref:System.IDisposable.Dispose%2A> that frees the class's resources.</span></span> <span data-ttu-id="f46ff-166">Tato metoda by měla být volána <xref:System.IDisposable.Dispose%2A> a `Finalize` metody třídy base:</span><span class="sxs-lookup"><span data-stu-id="f46ff-166">This method should be called by the <xref:System.IDisposable.Dispose%2A> and `Finalize` methods of the base class:</span></span>  
  
    ```  
    Protected Overridable Sub Dispose(ByVal disposing As Boolean)  
        If Not Me.disposed Then  
            If disposing Then  
                ' Insert code to free managed resources.  
            End If  
            ' Insert code to free unmanaged resources.  
        End If  
        Me.disposed = True  
    End Sub  
    ```  
  
-   <span data-ttu-id="f46ff-167">Implementace <xref:System.IDisposable.Dispose%2A> obsahující pouze následující kód:</span><span class="sxs-lookup"><span data-stu-id="f46ff-167">An implementation of <xref:System.IDisposable.Dispose%2A> that contains only the following code:</span></span>  
  
    ```  
    Public Sub Dispose() Implements IDisposable.Dispose  
        Dispose(True)  
        GC.SuppressFinalize(Me)  
    End Sub  
    ```  
  
-   <span data-ttu-id="f46ff-168">Přepsání `Finalize` metoda, která obsahuje jenom následující kód:</span><span class="sxs-lookup"><span data-stu-id="f46ff-168">An override of the `Finalize` method that contains only the following code:</span></span>  
  
    ```  
    Protected Overrides Sub Finalize()  
        Dispose(False)  
        MyBase.Finalize()  
    End Sub  
    ```  
  
### <a name="deriving-from-a-class-that-implements-idisposable"></a><span data-ttu-id="f46ff-169">Odvozování od třídy, který implementuje rozhraní IDisposable</span><span class="sxs-lookup"><span data-stu-id="f46ff-169">Deriving from a Class that Implements IDisposable</span></span>  
 <span data-ttu-id="f46ff-170">Třída odvozená z databáze třídu, která implementuje <xref:System.IDisposable> rozhraní není potřeba základní metod přepsat, pokud používá další prostředky, které musí být zrušen.</span><span class="sxs-lookup"><span data-stu-id="f46ff-170">A class that derives from a base class that implements the <xref:System.IDisposable> interface does not need to override any of the base methods unless it uses additional resources that need to be disposed.</span></span> <span data-ttu-id="f46ff-171">V takovém případě by měly přepsat odvozené třídy základní třídy `Dispose(disposing)` metoda k uvolnění prostředků odvozené třídy.</span><span class="sxs-lookup"><span data-stu-id="f46ff-171">In that situation, the derived class should override the base class's `Dispose(disposing)` method to dispose of the derived class's resources.</span></span> <span data-ttu-id="f46ff-172">Toto přepsání musí volat základní třídy `Dispose(disposing)` metoda.</span><span class="sxs-lookup"><span data-stu-id="f46ff-172">This override must call the base class's `Dispose(disposing)` method.</span></span>  
  
```  
Protected Overrides Sub Dispose(ByVal disposing As Boolean)  
    If Not Me.disposed Then  
        If disposing Then  
            ' Insert code to free managed resources.  
        End If  
        ' Insert code to free unmanaged resources.  
    End If  
    MyBase.Dispose(disposing)  
End Sub  
```  
  
 <span data-ttu-id="f46ff-173">Odvozené třídě by neměl přepsat základní třídy <xref:System.IDisposable.Dispose%2A> a `Finalize` metody.</span><span class="sxs-lookup"><span data-stu-id="f46ff-173">A derived class should not override the base class's <xref:System.IDisposable.Dispose%2A> and `Finalize` methods.</span></span> <span data-ttu-id="f46ff-174">Pokud tyto metody jsou volány z instance odvozené třídy, základní třída implementace těchto metod volání přepsání odvozené třídě `Dispose(disposing)` metoda.</span><span class="sxs-lookup"><span data-stu-id="f46ff-174">When those methods are called from an instance of the derived class, the base class's implementation of those methods call the derived class's override of the `Dispose(disposing)` method.</span></span>  
  
## <a name="garbage-collection-and-the-finalize-destructor"></a><span data-ttu-id="f46ff-175">Uvolňování paměti a Finalize – destruktor</span><span class="sxs-lookup"><span data-stu-id="f46ff-175">Garbage Collection and the Finalize Destructor</span></span>  
 <span data-ttu-id="f46ff-176">[!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] Používá *uvolňování paměti odkaz trasování* systému, aby pravidelně uvolnění prostředků nepoužívá.</span><span class="sxs-lookup"><span data-stu-id="f46ff-176">The [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] uses the *reference-tracing garbage collection* system to periodically release unused resources.</span></span> <span data-ttu-id="f46ff-177">Visual Basic 6.0 a starší verze použít jiný systém názvem *počítání odkazů* ke správě prostředků.</span><span class="sxs-lookup"><span data-stu-id="f46ff-177">Visual Basic 6.0 and earlier versions used a different system called *reference counting* to manage resources.</span></span> <span data-ttu-id="f46ff-178">Přestože oba systémy provádí stejnou funkci automaticky, existuje několik důležitých rozdílů.</span><span class="sxs-lookup"><span data-stu-id="f46ff-178">Although both systems perform the same function automatically, there are a few important differences.</span></span>  
  
 <span data-ttu-id="f46ff-179">Modul CLR pravidelně odstraní objekty, pokud systém zjistí, že tyto objekty už nejsou potřeba.</span><span class="sxs-lookup"><span data-stu-id="f46ff-179">The CLR periodically destroys objects when the system determines that such objects are no longer needed.</span></span> <span data-ttu-id="f46ff-180">Objekty jsou vydávány rychleji, když jsou systémové prostředky v krátké napájení a méně často jinak.</span><span class="sxs-lookup"><span data-stu-id="f46ff-180">Objects are released more quickly when system resources are in short supply, and less frequently otherwise.</span></span> <span data-ttu-id="f46ff-181">Zpoždění mezi při objekt ztratí oboru a když modulu CLR uvolněním znamená, že na rozdíl od s objekty v jazyce Visual Basic 6.0 a starší verze, nelze určit přesně při objekt budou zničena.</span><span class="sxs-lookup"><span data-stu-id="f46ff-181">The delay between when an object loses scope and when the CLR releases it means that, unlike with objects in Visual Basic 6.0 and earlier versions, you cannot determine exactly when the object will be destroyed.</span></span> <span data-ttu-id="f46ff-182">V takovém případě jsou uvedená objekty tak, aby měl *Nedeterministický životnost*.</span><span class="sxs-lookup"><span data-stu-id="f46ff-182">In such a situation, objects are said to have *non-deterministic lifetime*.</span></span> <span data-ttu-id="f46ff-183">Ve většině případů není deterministický životnost nezmění jak psát aplikace, tak dlouho, dokud nezapomeňte, že `Finalize` destruktor nemusí spustit okamžitě, když objekt ztratí oboru.</span><span class="sxs-lookup"><span data-stu-id="f46ff-183">In most cases, non-deterministic lifetime does not change how you write applications, as long as you remember that the `Finalize` destructor may not immediately execute when an object loses scope.</span></span>  
  
 <span data-ttu-id="f46ff-184">Další rozdíl mezi systémy kolekce paměti zahrnuje použití `Nothing`.</span><span class="sxs-lookup"><span data-stu-id="f46ff-184">Another difference between the garbage-collection systems involves the use of `Nothing`.</span></span> <span data-ttu-id="f46ff-185">Umožní využít v jazyce Visual Basic 6.0 a starší verze při počítání referencí programátory někdy přiřazené `Nothing` k objektu proměnné k uvolnění odkazy na tyto proměnné uchovávat.</span><span class="sxs-lookup"><span data-stu-id="f46ff-185">To take advantage of reference counting in Visual Basic 6.0 and earlier versions, programmers sometimes assigned `Nothing` to object variables to release the references those variables held.</span></span> <span data-ttu-id="f46ff-186">Pokud proměnná uchovávat poslední odkaz na objekt, byly vydané okamžitě objektu prostředky.</span><span class="sxs-lookup"><span data-stu-id="f46ff-186">If the variable held the last reference to the object, the object's resources were released immediately.</span></span> <span data-ttu-id="f46ff-187">V novějších verzích jazyka Visual Basic při můžou nastat případy, ve kterých je stále cenné, tento postup provádění ho nikdy způsobí, že odkazovaného objektu k uvolnění jeho prostředků okamžitě.</span><span class="sxs-lookup"><span data-stu-id="f46ff-187">In later versions of Visual Basic, while there may be cases in which this procedure is still valuable, performing it never causes the referenced object to release its resources immediately.</span></span> <span data-ttu-id="f46ff-188">K uvolnění prostředků okamžitě, použijte objektu <xref:System.IDisposable.Dispose%2A> metodu, pokud je k dispozici.</span><span class="sxs-lookup"><span data-stu-id="f46ff-188">To release resources immediately, use the object's <xref:System.IDisposable.Dispose%2A> method, if available.</span></span> <span data-ttu-id="f46ff-189">Pouze nastavte proměnnou na `Nothing` je po dlouho relativní na dobu, bude systém uvolňování trvá ke zjištění osamocených objektů celé jeho životnosti.</span><span class="sxs-lookup"><span data-stu-id="f46ff-189">The only time you should set a variable to `Nothing` is when its lifetime is long relative to the time the garbage collector takes to detect orphaned objects.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="f46ff-190">Viz také</span><span class="sxs-lookup"><span data-stu-id="f46ff-190">See Also</span></span>  
 <xref:System.IDisposable.Dispose%2A>  
 [<span data-ttu-id="f46ff-191">Inicializace a ukončování součástí</span><span class="sxs-lookup"><span data-stu-id="f46ff-191">Initialization and Termination of Components</span></span>](http://msdn.microsoft.com/library/58444076-a9d2-4c91-b3f6-0e180dc0695d)  
 [<span data-ttu-id="f46ff-192">New – operátor</span><span class="sxs-lookup"><span data-stu-id="f46ff-192">New Operator</span></span>](../../../../visual-basic/language-reference/operators/new-operator.md)  
 [<span data-ttu-id="f46ff-193">Vymazání nespravovaných prostředků</span><span class="sxs-lookup"><span data-stu-id="f46ff-193">Cleaning Up Unmanaged Resources</span></span>](../../../../standard/garbage-collection/unmanaged.md)  
 [<span data-ttu-id="f46ff-194">Nic</span><span class="sxs-lookup"><span data-stu-id="f46ff-194">Nothing</span></span>](../../../../visual-basic/language-reference/nothing.md)
