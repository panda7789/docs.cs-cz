---
title: Vliv jazykové verze na řetězce v jazyce Visual Basic
ms.date: 07/20/2015
helpviewer_keywords:
- locale [Visual Basic], effect on strings
- strings [Visual Basic], locale dependence
ms.assetid: c4664444-ee0d-47bf-bef1-eaa3c54bdd7f
ms.openlocfilehash: d3c7ae9da9c18e53da393928e34dcfbf04fc891c
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: HT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/23/2019
ms.locfileid: "62024687"
---
# <a name="how-culture-affects-strings-in-visual-basic"></a><span data-ttu-id="12293-102">Vliv jazykové verze na řetězce v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="12293-102">How Culture Affects Strings in Visual Basic</span></span>
<span data-ttu-id="12293-103">Tato stránka nápovědy popisuje, jak jazyka Visual Basic používá informace o jazykové verzi provádět převody řetězce a porovnávání řetězců.</span><span class="sxs-lookup"><span data-stu-id="12293-103">This Help page discusses how Visual Basic uses culture information to perform string conversions and comparisons.</span></span>  
  
## <a name="when-to-use-culture-specific-strings"></a><span data-ttu-id="12293-104">Kdy použít řetězců specifické pro jazykovou verzi</span><span class="sxs-lookup"><span data-stu-id="12293-104">When to Use Culture-Specific Strings</span></span>  
 <span data-ttu-id="12293-105">By obvykle řetězce specifické pro jazykovou verzi použít pro všechna data budou zobrazovat a číst od uživatelů a použít invariantní jazyková verze řetězce pro interní data vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="12293-105">Typically, you should use culture-specific strings for all data presented to and read from users, and use culture-invariant strings for your application's internal data.</span></span>  
  
 <span data-ttu-id="12293-106">Pokud vaše aplikace zobrazí uživatelé zadat datum jako řetězec, očekávat uživatelé k formátování řetězce podle jeho jazykovou verzi a aplikace by měla odpovídajícím způsobem převést řetězec.</span><span class="sxs-lookup"><span data-stu-id="12293-106">For example, if your application asks users to enter a date as a string, it should expect users to format the strings according to their culture, and the application should convert the string appropriately.</span></span> <span data-ttu-id="12293-107">Pokud vaše aplikace se pak zobrazí data ve svém uživatelském rozhraní, se by se měl prokázat v jazykové verzi uživatele.</span><span class="sxs-lookup"><span data-stu-id="12293-107">If your application then presents that date in its user interface, it should present it in the user's culture.</span></span>  
  
 <span data-ttu-id="12293-108">Nicméně pokud aplikace odešle data do centrálního serveru, by měl formátu řetězce podle jeden konkrétní jazykovou verzi, aby se zabránilo záměny potenciálně různé formáty.</span><span class="sxs-lookup"><span data-stu-id="12293-108">However, if the application uploads the date to a central server, it should format the string according to one specific culture, to prevent confusion between potentially different date formats.</span></span>  
  
## <a name="culture-sensitive-functions"></a><span data-ttu-id="12293-109">Jazykové funkce</span><span class="sxs-lookup"><span data-stu-id="12293-109">Culture-Sensitive Functions</span></span>  
 <span data-ttu-id="12293-110">Všechny funkce pro převod řetězců jazyka Visual Basic (s výjimkou `Str` a `Val` funkce) ujistěte se, že převod a porovnání jsou vhodné pro jazykovou verzi vaší aplikace pomocí informací o jazykové verzi aplikace uživatel.</span><span class="sxs-lookup"><span data-stu-id="12293-110">All of the Visual Basic string-conversion functions (except for the `Str` and `Val` functions) use the application's culture information to make sure that the conversions and comparisons are appropriate for the culture of the application's user.</span></span>  
  
 <span data-ttu-id="12293-111">Klíčem k úspěšně pomocí funkce pro převod řetězců v aplikacích, které běží na počítačích s nastavením jinou jazykovou verzi je pochopit, které funkce pomocí konkrétní jazykové verze nastavení a nastavení aktuální jazykové verze, která použít.</span><span class="sxs-lookup"><span data-stu-id="12293-111">The key to successfully using string-conversion functions in applications that run on computers with different culture settings is to understand which functions use a specific culture setting, and which use the current culture setting.</span></span> <span data-ttu-id="12293-112">Všimněte si, že nastavení jazykové verze aplikace se ve výchozím nastavení, dědí z nastavení jazykové verze operačního systému.</span><span class="sxs-lookup"><span data-stu-id="12293-112">Notice that the application's culture settings are, by default, inherited from the culture settings of the operating system.</span></span> <span data-ttu-id="12293-113">Další informace najdete v tématu <xref:Microsoft.VisualBasic.Strings.Asc%2A>, <xref:Microsoft.VisualBasic.Strings.AscW%2A>, <xref:Microsoft.VisualBasic.Strings.Chr%2A>, <xref:Microsoft.VisualBasic.Strings.ChrW%2A>, <xref:Microsoft.VisualBasic.Strings.Format%2A>, <xref:Microsoft.VisualBasic.Conversion.Hex%2A>, <xref:Microsoft.VisualBasic.Conversion.Oct%2A>, a [funkce pro převod typů](../../../../visual-basic/language-reference/functions/type-conversion-functions.md).</span><span class="sxs-lookup"><span data-stu-id="12293-113">For more information, see <xref:Microsoft.VisualBasic.Strings.Asc%2A>, <xref:Microsoft.VisualBasic.Strings.AscW%2A>, <xref:Microsoft.VisualBasic.Strings.Chr%2A>, <xref:Microsoft.VisualBasic.Strings.ChrW%2A>, <xref:Microsoft.VisualBasic.Strings.Format%2A>, <xref:Microsoft.VisualBasic.Conversion.Hex%2A>, <xref:Microsoft.VisualBasic.Conversion.Oct%2A>, and [Type Conversion Functions](../../../../visual-basic/language-reference/functions/type-conversion-functions.md).</span></span>  
  
 <span data-ttu-id="12293-114">`Str` (Převádí čísla na řetězce) a `Val` (převede řetězců na čísla) funkce nepoužívejte informace o jazykové verzi aplikace, když převod mezi řetězci a čísla.</span><span class="sxs-lookup"><span data-stu-id="12293-114">The `Str` (converts numbers to strings) and `Val` (converts strings to numbers) functions do not use the application's culture information when converting between strings and numbers.</span></span> <span data-ttu-id="12293-115">Místo toho jsou pouze tečkou (.) rozpoznán jako platný oddělovač desetinných míst.</span><span class="sxs-lookup"><span data-stu-id="12293-115">Instead, they recognize only the period (.) as a valid decimal separator.</span></span> <span data-ttu-id="12293-116">V kulturách s ohledem na analogy z těchto funkcí jsou:</span><span class="sxs-lookup"><span data-stu-id="12293-116">The culturally-aware analogues of these functions are:</span></span>  
  
- <span data-ttu-id="12293-117">**Převody, které používají aktuální jazykovou verzi.**</span><span class="sxs-lookup"><span data-stu-id="12293-117">**Conversions that use the current culture.**</span></span> <span data-ttu-id="12293-118">`CStr` a `Format` funkcí převede číslo na řetězec a `CDbl` a `CInt` funkce převádějí řetězec na číslo.</span><span class="sxs-lookup"><span data-stu-id="12293-118">The `CStr` and `Format` functions convert a number to a string, and the `CDbl` and `CInt` functions convert a string to a number.</span></span>  
  
- <span data-ttu-id="12293-119">**Převody, které používají konkrétní jazykovou verzi.**</span><span class="sxs-lookup"><span data-stu-id="12293-119">**Conversions that use a specific culture.**</span></span> <span data-ttu-id="12293-120">Má každé číslo objekt `ToString(IFormatProvider)` metodu, která převede číslo na řetězec a `Parse(String, IFormatProvider)` metodu, která převede řetězec na číslo.</span><span class="sxs-lookup"><span data-stu-id="12293-120">Each number object has a `ToString(IFormatProvider)` method that converts a number to a string, and a `Parse(String, IFormatProvider)` method that converts a string to a number.</span></span> <span data-ttu-id="12293-121">Například `Double` typ poskytuje <xref:System.Double.ToString%28System.IFormatProvider%29> a <xref:System.Double.Parse%28System.String%2CSystem.IFormatProvider%29> metody.</span><span class="sxs-lookup"><span data-stu-id="12293-121">For example, the `Double` type provides the <xref:System.Double.ToString%28System.IFormatProvider%29> and <xref:System.Double.Parse%28System.String%2CSystem.IFormatProvider%29> methods.</span></span>  
  
 <span data-ttu-id="12293-122">Další informace naleznete v tématu <xref:Microsoft.VisualBasic.Conversion.Str%2A> a <xref:Microsoft.VisualBasic.Conversion.Val%2A>.</span><span class="sxs-lookup"><span data-stu-id="12293-122">For more information, see <xref:Microsoft.VisualBasic.Conversion.Str%2A> and <xref:Microsoft.VisualBasic.Conversion.Val%2A>.</span></span>  
  
## <a name="using-a-specific-culture"></a><span data-ttu-id="12293-123">Pomocí konkrétní jazykové verze</span><span class="sxs-lookup"><span data-stu-id="12293-123">Using a Specific Culture</span></span>  
 <span data-ttu-id="12293-124">Představte si, že vyvíjíte aplikaci, která odesílá data (naformátovaná jako řetězec) k webové službě.</span><span class="sxs-lookup"><span data-stu-id="12293-124">Imagine that you are developing an application that sends a date (formatted as a string) to a Web service.</span></span> <span data-ttu-id="12293-125">V takovém případě musí aplikace použít konkrétní jazykovou verzi pro převod řetězce.</span><span class="sxs-lookup"><span data-stu-id="12293-125">In this case, your application must use a specific culture for the string conversion.</span></span> <span data-ttu-id="12293-126">Pro ilustraci, důvod, proč, vezměte v úvahu výsledek použití datum <xref:System.DateTime.ToString> metody: Pokud vaše aplikace používá k formátování data 4. července 2005, tato metoda vrátí "7/4/2005 12:00:00 AM" při spuštění pomocí jazykové verze Angličtina-Spojené státy (en US), ale vrátí "04.07.2005 00:00:00" při spuštění s němčina (de-DE) jazykovou verzi.</span><span class="sxs-lookup"><span data-stu-id="12293-126">To illustrate why, consider the result of using the date's <xref:System.DateTime.ToString> method: If your application uses that method to format the date July 4, 2005, it returns "7/4/2005 12:00:00 AM" when run with the United States English (en-US) culture, but it returns "04.07.2005 00:00:00" when run with the German (de-DE) culture.</span></span>  
  
 <span data-ttu-id="12293-127">Když potřebujete provést konverzi na řetězec ve formátu konkrétní jazykové verze, byste měli použít `CultureInfo` třídu, která je integrovaná [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)].</span><span class="sxs-lookup"><span data-stu-id="12293-127">When you need to perform a string conversion in a specific culture format, you should use the `CultureInfo` class that is built into the [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)].</span></span> <span data-ttu-id="12293-128">Můžete vytvořit nový `CultureInfo` pro konkrétní jazykovou verzi předáním název pro jazykovou verzi <xref:System.Globalization.CultureInfo.%23ctor%2A> konstruktoru.</span><span class="sxs-lookup"><span data-stu-id="12293-128">You can create a new `CultureInfo` object for a specific culture by passing the culture's name to the <xref:System.Globalization.CultureInfo.%23ctor%2A> constructor.</span></span> <span data-ttu-id="12293-129">Názvy podporované jazykové verze jsou uvedeny v <xref:System.Globalization.CultureInfo> třídy stránku nápovědy.</span><span class="sxs-lookup"><span data-stu-id="12293-129">The supported culture names are listed in the <xref:System.Globalization.CultureInfo> class Help page.</span></span>  
  
 <span data-ttu-id="12293-130">Alternativně můžete získat instanci *invariantní jazyková verze* z <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType> vlastnost.</span><span class="sxs-lookup"><span data-stu-id="12293-130">Alternatively, you can get an instance of the *invariant culture* from the <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="12293-131">Neutrální jazykové verze je založená na anglické jazykové verzi, ale existují určité rozdíly.</span><span class="sxs-lookup"><span data-stu-id="12293-131">The invariant culture is based on the English culture, but there are some differences.</span></span> <span data-ttu-id="12293-132">Například určuje neutrální jazykové verze 24hodinový formát místo 12 hodin.</span><span class="sxs-lookup"><span data-stu-id="12293-132">For example, the invariant culture specifies a 24-hour clock instead of a 12-hour clock.</span></span>  
  
 <span data-ttu-id="12293-133">Datum převést na řetězec pro jazykovou verzi, předejte <xref:System.Globalization.CultureInfo> do objektu date <xref:System.DateTime.ToString%28System.IFormatProvider%29> metoda.</span><span class="sxs-lookup"><span data-stu-id="12293-133">To convert a date to the culture's string, pass the <xref:System.Globalization.CultureInfo> object to the date object's <xref:System.DateTime.ToString%28System.IFormatProvider%29> method.</span></span> <span data-ttu-id="12293-134">Například následující kód zobrazí "07/04/2005 00:00:00", bez ohledu na nastavení jazykové verze vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="12293-134">For example, the following code displays "07/04/2005 00:00:00", regardless of the application's culture settings.</span></span>  
  
 [!code-vb[VbVbalrConcepts#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#1)]  
  
> [!NOTE]
>  <span data-ttu-id="12293-135">Date – literály jsou vždy interpretováno podle jazykové verze Angličtina.</span><span class="sxs-lookup"><span data-stu-id="12293-135">Date literals are always interpreted according to the English culture.</span></span>  
  
## <a name="comparing-strings"></a><span data-ttu-id="12293-136">Porovnávání řetězců</span><span class="sxs-lookup"><span data-stu-id="12293-136">Comparing Strings</span></span>  
 <span data-ttu-id="12293-137">Existují dvě důležité situace, kde jsou potřeba porovnávání řetězců:</span><span class="sxs-lookup"><span data-stu-id="12293-137">There are two important situations where string comparisons are needed:</span></span>  
  
- <span data-ttu-id="12293-138">**Řazení dat v zobrazení pro uživatele.**</span><span class="sxs-lookup"><span data-stu-id="12293-138">**Sorting data for display to the user.**</span></span> <span data-ttu-id="12293-139">Použití operací na základě aktuální jazykovou verzi tak řazení řetězců odpovídajícím způsobem.</span><span class="sxs-lookup"><span data-stu-id="12293-139">Use operations based on the current culture so the strings sort appropriately.</span></span>  
  
- <span data-ttu-id="12293-140">**Určení, pokud dva řetězce interní aplikace přesně odpovídat (obvykle pro účely zabezpečení).**</span><span class="sxs-lookup"><span data-stu-id="12293-140">**Determining if two application-internal strings exactly match (typically for security purposes).**</span></span> <span data-ttu-id="12293-141">Pomocí operace, které ignorovat aktuální jazykové verze.</span><span class="sxs-lookup"><span data-stu-id="12293-141">Use operations that disregard the current culture.</span></span>  
  
 <span data-ttu-id="12293-142">Oba typy porovnání s jazykem Visual Basic můžete provádět <xref:Microsoft.VisualBasic.Strings.StrComp%2A> funkce.</span><span class="sxs-lookup"><span data-stu-id="12293-142">You can perform both types of comparisons with the Visual Basic <xref:Microsoft.VisualBasic.Strings.StrComp%2A> function.</span></span> <span data-ttu-id="12293-143">Zadejte nepovinný `Compare` argument řídit typ porovnání: `Text` pro většinu vstupů a výstupů `Binary` pro určení přesné shody.</span><span class="sxs-lookup"><span data-stu-id="12293-143">Specify the optional `Compare` argument to control the type of comparison: `Text` for most input and output `Binary` for determining exact matches.</span></span>  
  
 <span data-ttu-id="12293-144">`StrComp` Funkce vrátí celé číslo, které označuje vztah mezi dvěma porovnání řetězce podle pořadí řazení.</span><span class="sxs-lookup"><span data-stu-id="12293-144">The `StrComp` function returns an integer that indicates the relationship between the two compared strings based on the sorting order.</span></span> <span data-ttu-id="12293-145">Kladnou hodnotu pro výsledek označuje, zda je první řetězec větší než druhý řetězec.</span><span class="sxs-lookup"><span data-stu-id="12293-145">A positive value for the result indicates that the first string is greater than the second string.</span></span> <span data-ttu-id="12293-146">Záporný výsledek označuje první řetězec, který je menší a nula znamená rovnost řetězců.</span><span class="sxs-lookup"><span data-stu-id="12293-146">A negative result indicates the first string is smaller, and zero indicates equality between the strings.</span></span>  
  
 [!code-vb[VbVbalrStrings#22](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStrings/VB/Class1.vb#22)]  
  
 <span data-ttu-id="12293-147">Můžete také použít [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] partner `StrComp` funkce, <xref:System.String.Compare%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="12293-147">You can also use the [!INCLUDE[dnprdnshort](~/includes/dnprdnshort-md.md)] partner of the `StrComp` function, the <xref:System.String.Compare%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="12293-148">Toto je statické, přetížené metody třídy base řetězec.</span><span class="sxs-lookup"><span data-stu-id="12293-148">This is a static, overloaded method of the base string class.</span></span> <span data-ttu-id="12293-149">Následující příklad ukazuje, jak tato metoda se používá:</span><span class="sxs-lookup"><span data-stu-id="12293-149">The following example illustrates how this method is used:</span></span>  
  
 [!code-vb[VbVbalrStrings#48](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStrings/VB/Class2.vb#48)]  
  
 <span data-ttu-id="12293-150">Pro lepší kontrolu nad jak porovnání se provádí, můžete použít další přetížení <xref:System.String.Compare%2A> metody.</span><span class="sxs-lookup"><span data-stu-id="12293-150">For finer control over how the comparisons are performed, you can use additional overloads of the <xref:System.String.Compare%2A> method.</span></span> <span data-ttu-id="12293-151">S <xref:System.String.Compare%2A?displayProperty=nameWithType> metodu, můžete použít `comparisonType` argument určit typ porovnání, které se použít.</span><span class="sxs-lookup"><span data-stu-id="12293-151">With the <xref:System.String.Compare%2A?displayProperty=nameWithType> method, you can use the `comparisonType` argument to specify which type of comparison to use.</span></span>  
  
|<span data-ttu-id="12293-152">Hodnota pro `comparisonType` argument</span><span class="sxs-lookup"><span data-stu-id="12293-152">Value for `comparisonType` argument</span></span>|<span data-ttu-id="12293-153">Typ porovnání</span><span class="sxs-lookup"><span data-stu-id="12293-153">Type of comparison</span></span>|<span data-ttu-id="12293-154">Kdy je použít</span><span class="sxs-lookup"><span data-stu-id="12293-154">When to use</span></span>|  
|---|---|---|  
|`Ordinal`|<span data-ttu-id="12293-155">Porovnání podle bajtů součást řetězců.</span><span class="sxs-lookup"><span data-stu-id="12293-155">Comparison based on strings' component bytes.</span></span>|<span data-ttu-id="12293-156">Tuto hodnotu použijte, pokud porovnávání: malá a velká písmena identifikátorů, nastavení související se zabezpečením nebo jiné nejazykové identifikátory, kde počet bajtů se musí přesně shodovat.</span><span class="sxs-lookup"><span data-stu-id="12293-156">Use this value when comparing: case-sensitive identifiers, security-related settings, or other non-linguistic identifiers where the bytes must match exactly.</span></span>|  
|`OrdinalIgnoreCase`|<span data-ttu-id="12293-157">Porovnání podle bajtů součást řetězců.</span><span class="sxs-lookup"><span data-stu-id="12293-157">Comparison based on strings' component bytes.</span></span><br /><br /> <span data-ttu-id="12293-158">`OrdinalIgnoreCase` používá invariantní jazyková verze informace k určení, kdy dva znaky se liší pouze velikostí malá a velká písmena.</span><span class="sxs-lookup"><span data-stu-id="12293-158">`OrdinalIgnoreCase` uses the invariant culture information to determine when two characters differ only in capitalization.</span></span>|<span data-ttu-id="12293-159">Tuto hodnotu použijte, pokud porovnávání: malá a velká písmena identifikátorů, bezpečnostních nastavení a data uložená ve Windows.</span><span class="sxs-lookup"><span data-stu-id="12293-159">Use this value when comparing: case-insensitive identifiers, security-related settings, and data stored in Windows.</span></span>|  
|<span data-ttu-id="12293-160">`CurrentCulture` Nebo `CurrentCultureIgnoreCase`</span><span class="sxs-lookup"><span data-stu-id="12293-160">`CurrentCulture` or `CurrentCultureIgnoreCase`</span></span>|<span data-ttu-id="12293-161">Porovnání podle interpretace řetězců v aktuální jazykové verze.</span><span class="sxs-lookup"><span data-stu-id="12293-161">Comparison based on the strings' interpretation in the current culture.</span></span>|<span data-ttu-id="12293-162">Použijte tyto hodnoty při porovnání: data, která se zobrazí uživateli, většina vstup uživatele a další data, která vyžaduje lingvistické výkladu.</span><span class="sxs-lookup"><span data-stu-id="12293-162">Use these values when comparing: data that is displayed to the user, most user input, and other data that requires linguistic interpretation.</span></span>|  
|<span data-ttu-id="12293-163">`InvariantCulture` Nebo `InvariantCultureIgnoreCase`</span><span class="sxs-lookup"><span data-stu-id="12293-163">`InvariantCulture` or `InvariantCultureIgnoreCase`</span></span>|<span data-ttu-id="12293-164">Porovnání podle interpretace řetězců v invariantní jazykové verzi.</span><span class="sxs-lookup"><span data-stu-id="12293-164">Comparison based on the strings' interpretation in the invariant culture.</span></span><br /><br /> <span data-ttu-id="12293-165">To se liší od `Ordinal` a `OrdinalIgnoreCase`, protože invariantní jazyková verze považuje za znaky mimo rozsah přijatelných ekvivalentní invariantní znaků.</span><span class="sxs-lookup"><span data-stu-id="12293-165">This is different than the `Ordinal` and `OrdinalIgnoreCase`, because the invariant culture treats characters outside its accepted range as equivalent invariant characters.</span></span>|<span data-ttu-id="12293-166">Tyto hodnoty použijte jen při porovnávání trvalá data nebo zobrazení lingvistických relevantní data, která vyžaduje pevnou řazení.</span><span class="sxs-lookup"><span data-stu-id="12293-166">Use these values only when comparing persisting data or displaying linguistically-relevant data that requires a fixed sort order.</span></span>|  
  
### <a name="security-considerations"></a><span data-ttu-id="12293-167">Důležité informace o zabezpečení</span><span class="sxs-lookup"><span data-stu-id="12293-167">Security Considerations</span></span>  
 <span data-ttu-id="12293-168">Pokud vaše aplikace provádí rozhodnutí o zabezpečení založeno na výsledku porovnání nebo operaci změny velikosti písmen, pak by měl použít operaci <xref:System.String.Compare%2A?displayProperty=nameWithType> a předáte `Ordinal` nebo `OrdinalIgnoreCase` pro `comparisonType` argument.</span><span class="sxs-lookup"><span data-stu-id="12293-168">If your application makes security decisions based on the result of a comparison or case-change operation, then the operation should use the <xref:System.String.Compare%2A?displayProperty=nameWithType> method, and pass `Ordinal` or `OrdinalIgnoreCase` for the `comparisonType` argument.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="12293-169">Viz také:</span><span class="sxs-lookup"><span data-stu-id="12293-169">See also</span></span>

- <xref:System.Globalization.CultureInfo>
- [<span data-ttu-id="12293-170">Úvod do řetězců v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="12293-170">Introduction to Strings in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/strings/introduction-to-strings.md)
- [<span data-ttu-id="12293-171">Funkce pro převod typů</span><span class="sxs-lookup"><span data-stu-id="12293-171">Type Conversion Functions</span></span>](../../../../visual-basic/language-reference/functions/type-conversion-functions.md)
