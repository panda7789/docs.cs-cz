---
title: Řešení potíží s procedurami (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- troubleshooting Visual Basic, procedures
- procedures [Visual Basic], troubleshooting
- Visual Basic code, procedures
- troubleshooting procedures
- procedures [Visual Basic], about procedures
ms.assetid: 525721e8-2e02-4f75-b5d8-6b893462cf2b
ms.openlocfilehash: 5ef0a485a0b114f465aac694970ec3350b26f35a
ms.sourcegitcommit: 6b308cf6d627d78ee36dbbae8972a310ac7fd6c8
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 01/23/2019
ms.locfileid: "54648544"
---
# <a name="troubleshooting-procedures-visual-basic"></a><span data-ttu-id="38699-102">Řešení potíží s procedurami (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="38699-102">Troubleshooting Procedures (Visual Basic)</span></span>
<span data-ttu-id="38699-103">Tato stránka obsahuje některé běžné problémy, které se mohou vyskytnout při práci s postupy.</span><span class="sxs-lookup"><span data-stu-id="38699-103">This page lists some common problems that can occur when working with procedures.</span></span>  
  
## <a name="returning-an-array-type-from-a-function-procedure"></a><span data-ttu-id="38699-104">Vrací typ pole z procedury – funkce</span><span class="sxs-lookup"><span data-stu-id="38699-104">Returning an Array Type from a Function Procedure</span></span>  
 <span data-ttu-id="38699-105">Pokud `Function` procedura vrací datový typ pole, nelze použít `Function` název pro ukládání hodnot prvků pole.</span><span class="sxs-lookup"><span data-stu-id="38699-105">If a `Function` procedure returns an array data type, you cannot use the `Function` name to store values in the elements of the array.</span></span> <span data-ttu-id="38699-106">Pokud se pokusíte k tomu, kompilátor ji interpretuje jako volání `Function`.</span><span class="sxs-lookup"><span data-stu-id="38699-106">If you attempt to do this, the compiler interprets it as a call to the `Function`.</span></span> <span data-ttu-id="38699-107">Následující příklad generuje chyby kompilátoru.</span><span class="sxs-lookup"><span data-stu-id="38699-107">The following example generates compiler errors.</span></span>  
  
 `Function allOnes(ByVal n As Integer) As Integer()`  
  
 `For i As Integer = 1 To n - 1`  
  
 <span data-ttu-id="38699-108">`' The following statement generates a`   `COMPILER ERROR`  `.`</span><span class="sxs-lookup"><span data-stu-id="38699-108">`' The following statement generates a`   `COMPILER ERROR`  `.`</span></span>  
  
 `allOnes(i) = 1`  
  
 `Next i`  
  
 <span data-ttu-id="38699-109">`' The following statement generates a`   `COMPILER ERROR`  `.`</span><span class="sxs-lookup"><span data-stu-id="38699-109">`' The following statement generates a`   `COMPILER ERROR`  `.`</span></span>  
  
 `Return allOnes()`  
  
 `End Function`  
  
 <span data-ttu-id="38699-110">Příkaz `allOnes(i) = 1` vygeneruje chybu kompilátoru, protože to vypadá, volání `allOnes` s argumentem typu chybná data (jednotlivý prvek `Integer` místo `Integer` pole).</span><span class="sxs-lookup"><span data-stu-id="38699-110">The statement `allOnes(i) = 1` generates a compiler error because it appears to call `allOnes` with an argument of the wrong data type (a singleton `Integer` instead of an `Integer` array).</span></span> <span data-ttu-id="38699-111">Příkaz `Return allOnes()` vygeneruje chybu kompilátoru, protože to vypadá, volání `allOnes` s žádný argument.</span><span class="sxs-lookup"><span data-stu-id="38699-111">The statement `Return allOnes()` generates a compiler error because it appears to call `allOnes` with no argument.</span></span>  
  
 <span data-ttu-id="38699-112">**Správný způsob:** Aby bylo možné upravit prvky pole, které má být vrácena, definujte interní pole jako místní proměnná.</span><span class="sxs-lookup"><span data-stu-id="38699-112">**Correct Approach:** To be able to modify the elements of an array that is to be returned, define an internal array as a local variable.</span></span> <span data-ttu-id="38699-113">Následující příklad se zkompiluje bez chyb.</span><span class="sxs-lookup"><span data-stu-id="38699-113">The following example compiles without error.</span></span>  
  
 [!code-vb[VbVbcnProcedures#66](./codesnippet/VisualBasic/troubleshooting-procedures_1.vb)]  
  
## <a name="argument-not-being-modified-by-procedure-call"></a><span data-ttu-id="38699-114">Argument není právě upravuje voláním procedury</span><span class="sxs-lookup"><span data-stu-id="38699-114">Argument Not Being Modified by Procedure Call</span></span>  
 <span data-ttu-id="38699-115">Pokud chcete povolit postup, chcete-li změnit programovací element základní argumentu ve volajícím kódu, musíte jí předat podle odkazu.</span><span class="sxs-lookup"><span data-stu-id="38699-115">If you intend to allow a procedure to change a programming element underlying an argument in the calling code, you must pass it by reference.</span></span> <span data-ttu-id="38699-116">Ale postup může přistupovat k prvkům argument typu odkazu, i v případě, předat podle hodnoty.</span><span class="sxs-lookup"><span data-stu-id="38699-116">But a procedure can access the elements of a reference type argument even if you pass it by value.</span></span>  
  
-   <span data-ttu-id="38699-117">**Základní proměnná**.</span><span class="sxs-lookup"><span data-stu-id="38699-117">**Underlying Variable**.</span></span> <span data-ttu-id="38699-118">Povolit postup k nahrazení hodnoty základní proměnné elementu samotného, procedura musí deklarovat parametr [ByRef](../../../../visual-basic/language-reference/modifiers/byref.md).</span><span class="sxs-lookup"><span data-stu-id="38699-118">To allow the procedure to replace the value of the underlying variable element itself, the procedure must declare the parameter [ByRef](../../../../visual-basic/language-reference/modifiers/byref.md).</span></span> <span data-ttu-id="38699-119">Navíc volající kód nesmí uvést argument v závorkách, protože, který by se mělo přepsat `ByRef` předávání mechanismus.</span><span class="sxs-lookup"><span data-stu-id="38699-119">Also, the calling code must not enclose the argument in parentheses, because that would override the `ByRef` passing mechanism.</span></span>  
  
-   <span data-ttu-id="38699-120">**Odkazovat na prvky typu**.</span><span class="sxs-lookup"><span data-stu-id="38699-120">**Reference Type Elements**.</span></span> <span data-ttu-id="38699-121">Pokud deklarujete parametr [ByVal](../../../../visual-basic/language-reference/modifiers/byval.md), postup nelze změnit základní proměnné elementu samotného.</span><span class="sxs-lookup"><span data-stu-id="38699-121">If you declare a parameter [ByVal](../../../../visual-basic/language-reference/modifiers/byval.md), the procedure cannot modify the underlying variable element itself.</span></span> <span data-ttu-id="38699-122">Ale pokud je argumentem Typ odkazu, postupu můžete upravit členy objektu, na kterou odkazuje, i když ho nelze nahradit hodnotu proměnné.</span><span class="sxs-lookup"><span data-stu-id="38699-122">However, if the argument is a reference type, the procedure can modify the members of the object to which it points, even though it cannot replace the variable's value.</span></span> <span data-ttu-id="38699-123">Například pokud má argument hodnotu proměnné pole, postup nelze přiřadit nové pole do ní, ale můžete změnit jednu nebo více z jeho prvků.</span><span class="sxs-lookup"><span data-stu-id="38699-123">For example, if the argument is an array variable, the procedure cannot assign a new array to it, but it can change one or more of its elements.</span></span> <span data-ttu-id="38699-124">Změněné prvky se projeví v základní proměnné pole ve volajícím kódu.</span><span class="sxs-lookup"><span data-stu-id="38699-124">The changed elements are reflected in the underlying array variable in the calling code.</span></span>  
  
 <span data-ttu-id="38699-125">Následující příklad definuje dva postupy, které v této proměnné pole hodnota a provozují na jeho prvků.</span><span class="sxs-lookup"><span data-stu-id="38699-125">The following example defines two procedures that take an array variable by value and operate on its elements.</span></span> <span data-ttu-id="38699-126">Postup `increase` jednoduše přidá jednu na každý prvek.</span><span class="sxs-lookup"><span data-stu-id="38699-126">Procedure `increase` simply adds one to each element.</span></span> <span data-ttu-id="38699-127">Postup `replace` přiřadí nové pole parametru `a()` a pak přidá jednu na každý prvek.</span><span class="sxs-lookup"><span data-stu-id="38699-127">Procedure `replace` assigns a new array to the parameter `a()` and then adds one to each element.</span></span> <span data-ttu-id="38699-128">Ale přeřazení neovlivní základní proměnné pole ve volajícím kódu, protože `a()` je deklarován `ByVal`.</span><span class="sxs-lookup"><span data-stu-id="38699-128">However, the reassignment does not affect the underlying array variable in the calling code because `a()` is declared `ByVal`.</span></span>  
  
 [!code-vb[VbVbcnProcedures#35](./codesnippet/VisualBasic/troubleshooting-procedures_2.vb)]  
  
 [!code-vb[VbVbcnProcedures#38](./codesnippet/VisualBasic/troubleshooting-procedures_3.vb)]  
  
 <span data-ttu-id="38699-129">Následující příklad provede volání `increase` a `replace`.</span><span class="sxs-lookup"><span data-stu-id="38699-129">The following example makes calls to `increase` and `replace`.</span></span>  
  
 [!code-vb[VbVbcnProcedures#37](./codesnippet/VisualBasic/troubleshooting-procedures_4.vb)]  
  
 <span data-ttu-id="38699-130">První `MsgBox` volání zobrazí "po increase(n): 11, 21, 31, 41".</span><span class="sxs-lookup"><span data-stu-id="38699-130">The first `MsgBox` call displays "After increase(n): 11, 21, 31, 41".</span></span> <span data-ttu-id="38699-131">Protože `n` je typem odkazu `increase` lze měnit její členy, i když je jí předán `ByVal`.</span><span class="sxs-lookup"><span data-stu-id="38699-131">Because `n` is a reference type, `increase` can change its members, even though it is passed `ByVal`.</span></span>  
  
 <span data-ttu-id="38699-132">Druhá `MsgBox` volání zobrazí "po replace(n): 11, 21, 31, 41".</span><span class="sxs-lookup"><span data-stu-id="38699-132">The second `MsgBox` call displays "After replace(n): 11, 21, 31, 41".</span></span> <span data-ttu-id="38699-133">Protože `n` je předán `ByVal`, `replace` nelze upravit proměnnou `n` přiřazením nového pole.</span><span class="sxs-lookup"><span data-stu-id="38699-133">Because `n` is passed `ByVal`, `replace` cannot modify the variable `n` by assigning a new array to it.</span></span> <span data-ttu-id="38699-134">Když `replace` vytvoří novou instanci pole `k` a přiřadí ji na místní proměnnou `a`, ztratí odkaz na `n` předaných v volající kód.</span><span class="sxs-lookup"><span data-stu-id="38699-134">When `replace` creates the new array instance `k` and assigns it to the local variable `a`, it loses the reference to `n` passed in by the calling code.</span></span> <span data-ttu-id="38699-135">Když zvýší členy `a`, pouze místní pole `k` má vliv.</span><span class="sxs-lookup"><span data-stu-id="38699-135">When it increments the members of `a`, only the local array `k` is affected.</span></span>  
  
 <span data-ttu-id="38699-136">**Správný způsob:** Abyste mohli upravovat základní prvek proměnné samotné, předávání odkazem.</span><span class="sxs-lookup"><span data-stu-id="38699-136">**Correct Approach:** To be able to modify an underlying variable element itself, pass it by reference.</span></span> <span data-ttu-id="38699-137">Následující příklad ukazuje změnu v deklaraci `replace` , který umožňuje jedno pole nahradit jiným ve volajícím kódu.</span><span class="sxs-lookup"><span data-stu-id="38699-137">The following example shows the change in the declaration of `replace` that allows it to replace one array with another in the calling code.</span></span>  
  
 [!code-vb[VbVbcnProcedures#64](./codesnippet/VisualBasic/troubleshooting-procedures_5.vb)]  
  
## <a name="unable-to-define-an-overload"></a><span data-ttu-id="38699-138">Nelze definovat přetíženou</span><span class="sxs-lookup"><span data-stu-id="38699-138">Unable to Define an Overload</span></span>  
 <span data-ttu-id="38699-139">Pokud chcete definovat přetížené verze procedury, musíte použít stejný název, ale jiným podpisem.</span><span class="sxs-lookup"><span data-stu-id="38699-139">If you want to define an overloaded version of a procedure, you must use the same name but a different signature.</span></span> <span data-ttu-id="38699-140">Pokud kompilátor nemůže rozlišit vaše deklarace z přetížení se stejným podpisem, dojde k chybě.</span><span class="sxs-lookup"><span data-stu-id="38699-140">If the compiler cannot differentiate your declaration from an overload with the same signature, it generates an error.</span></span>  
  
 <span data-ttu-id="38699-141">*Podpis* procedury je určen název procedury a seznamu parametrů.</span><span class="sxs-lookup"><span data-stu-id="38699-141">The *signature* of a procedure is determined by the procedure name and the parameter list.</span></span> <span data-ttu-id="38699-142">Jednotlivá přetížení, musí mít stejný název jako všechna přetížení ale se musí lišit od všechny z nich alespoň v jedné další komponenty podpisu.</span><span class="sxs-lookup"><span data-stu-id="38699-142">Each overload must have the same name as all the other overloads but must differ from all of them in at least one of the other components of the signature.</span></span> <span data-ttu-id="38699-143">Další informace najdete v tématu [přetížení procedury](./procedure-overloading.md).</span><span class="sxs-lookup"><span data-stu-id="38699-143">For more information, see [Procedure Overloading](./procedure-overloading.md).</span></span>  
  
 <span data-ttu-id="38699-144">Následující položky, i v případě, že jde o jejich vztah k seznamu parametrů nejsou součástí podpis postupem:</span><span class="sxs-lookup"><span data-stu-id="38699-144">The following items, even though they pertain to the parameter list, are not components of a procedure's signature:</span></span>  
  
-   <span data-ttu-id="38699-145">Postup modifikátor klíčová slova, jako například `Public`, `Shared`, a `Static`</span><span class="sxs-lookup"><span data-stu-id="38699-145">Procedure modifier keywords, such as `Public`, `Shared`, and `Static`</span></span>  
  
-   <span data-ttu-id="38699-146">Názvy parametrů</span><span class="sxs-lookup"><span data-stu-id="38699-146">Parameter names</span></span>  
  
-   <span data-ttu-id="38699-147">Klíčová slova modifikátor parametru, jako například `ByRef` a `Optional`</span><span class="sxs-lookup"><span data-stu-id="38699-147">Parameter modifier keywords, such as `ByRef` and `Optional`</span></span>  
  
-   <span data-ttu-id="38699-148">Datový typ vrácené hodnoty (s výjimkou operátoru převodu)</span><span class="sxs-lookup"><span data-stu-id="38699-148">The data type of the return value (except for a conversion operator)</span></span>  
  
 <span data-ttu-id="38699-149">Pomocí různých pouze jeden nebo více předchozích položek nelze přetížení procedury.</span><span class="sxs-lookup"><span data-stu-id="38699-149">You cannot overload a procedure by varying only one or more of the preceding items.</span></span>  
  
 <span data-ttu-id="38699-150">**Správný způsob:** Aby bylo možné definovat přetížení procedury, musí se liší podpis.</span><span class="sxs-lookup"><span data-stu-id="38699-150">**Correct Approach:** To be able to define a procedure overload, you must vary the signature.</span></span> <span data-ttu-id="38699-151">Protože je nutné použít stejný název, musí se liší počet, pořadí nebo datové typy parametrů.</span><span class="sxs-lookup"><span data-stu-id="38699-151">Because you must use the same name, you must vary the number, order, or data types of the parameters.</span></span> <span data-ttu-id="38699-152">V obecný postup je popsán můžete měnit počet parametrů typu.</span><span class="sxs-lookup"><span data-stu-id="38699-152">In a generic procedure, you can vary the number of type parameters.</span></span> <span data-ttu-id="38699-153">V operátoru převodu ([funkce CType](../../../../visual-basic/language-reference/functions/ctype-function.md)), návratový typ se může lišit.</span><span class="sxs-lookup"><span data-stu-id="38699-153">In a conversion operator ([CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md)), you can vary the return type.</span></span>  
  
### <a name="overload-resolution-with-optional-and-paramarray-arguments"></a><span data-ttu-id="38699-154">Přetížení překlad IP adres s volitelným a ParamArray – argumenty</span><span class="sxs-lookup"><span data-stu-id="38699-154">Overload Resolution with Optional and ParamArray Arguments</span></span>  
 <span data-ttu-id="38699-155">Pokud jsou přetížení procedury s jednou nebo více [volitelné](../../../../visual-basic/language-reference/modifiers/optional.md) parametry nebo [ParamArray](../../../../visual-basic/language-reference/modifiers/paramarray.md) parametr, je třeba se vyvarovat duplikování všech *implicitní přetížení*.</span><span class="sxs-lookup"><span data-stu-id="38699-155">If you are overloading a procedure with one or more [Optional](../../../../visual-basic/language-reference/modifiers/optional.md) parameters or a [ParamArray](../../../../visual-basic/language-reference/modifiers/paramarray.md) parameter, you must avoid duplicating any of the *implicit overloads*.</span></span> <span data-ttu-id="38699-156">Informace najdete v tématu [aspekty přetížení procedur](./considerations-in-overloading-procedures.md).</span><span class="sxs-lookup"><span data-stu-id="38699-156">For information, see [Considerations in Overloading Procedures](./considerations-in-overloading-procedures.md).</span></span>  
  
## <a name="calling-a-wrong-version-of-an-overloaded-procedure"></a><span data-ttu-id="38699-157">Volání nesprávné verze třídy přetížené procedury</span><span class="sxs-lookup"><span data-stu-id="38699-157">Calling a Wrong Version of an Overloaded Procedure</span></span>  
 <span data-ttu-id="38699-158">Pokud procedura má několik přetížené verze, by měl být obeznámeni s jejich seznamy parametrů a pochopit, jak Visual Basic přeloží volání mezi přetížení.</span><span class="sxs-lookup"><span data-stu-id="38699-158">If a procedure has several overloaded versions, you should be familiar with all their parameter lists and understand how Visual Basic resolves calls among the overloads.</span></span> <span data-ttu-id="38699-159">Jinak lze volat přetížení než zamýšlené.</span><span class="sxs-lookup"><span data-stu-id="38699-159">Otherwise you could call an overload other than the intended one.</span></span>  
  
 <span data-ttu-id="38699-160">Pokud jste určili přetížení, které chcete volat, dejte pozor, dodržovat následující pravidla:</span><span class="sxs-lookup"><span data-stu-id="38699-160">When you have determined which overload you want to call, be careful to observe the following rules:</span></span>  
  
-   <span data-ttu-id="38699-161">Zadejte správný počet argumentů a ve správném pořadí.</span><span class="sxs-lookup"><span data-stu-id="38699-161">Supply the correct number of arguments, and in the correct order.</span></span>  
  
-   <span data-ttu-id="38699-162">V ideálním případě by vaše argumenty by měly obsahovat přesně stejné datové typy jako odpovídající parametry.</span><span class="sxs-lookup"><span data-stu-id="38699-162">Ideally, your arguments should have the exact same data types as the corresponding parameters.</span></span> <span data-ttu-id="38699-163">Datový typ každého argumentu musí v každém případě rozšířit na u jeho odpovídajícího parametru.</span><span class="sxs-lookup"><span data-stu-id="38699-163">In any case, the data type of each argument must widen to that of its corresponding parameter.</span></span> <span data-ttu-id="38699-164">To platí i v případě [Option Strict – příkaz](../../../../visual-basic/language-reference/statements/option-strict-statement.md) nastavena na `Off`.</span><span class="sxs-lookup"><span data-stu-id="38699-164">This is true even with the [Option Strict Statement](../../../../visual-basic/language-reference/statements/option-strict-statement.md) set to `Off`.</span></span> <span data-ttu-id="38699-165">Pokud jakýkoli zužující převod seznam argumentů, které přetížení vyžaduje přetížení nemá oprávnění volat.</span><span class="sxs-lookup"><span data-stu-id="38699-165">If an overload requires any narrowing conversion from your argument list, that overload is not eligible to be called.</span></span>  
  
-   <span data-ttu-id="38699-166">Pokud zadáte argumenty, které vyžadují rozšíření, ujistěte se, co nejblíže k odpovídající datové typy parametrů jejich datové typy.</span><span class="sxs-lookup"><span data-stu-id="38699-166">If you supply arguments that require widening, make their data types as close as possible to the corresponding parameter data types.</span></span> <span data-ttu-id="38699-167">Pokud dva nebo více přetížení přijmout vaší datové typy argumentů, kompilátor překládá volání přetížení, které vyžaduje minimální množství rozšíření.</span><span class="sxs-lookup"><span data-stu-id="38699-167">If two or more overloads accept your argument data types, the compiler resolves your call to the overload that calls for the least amount of widening.</span></span>  
  
 <span data-ttu-id="38699-168">Můžete snížit pravděpodobnost svého neshody typů dat pomocí [funkce CType](../../../../visual-basic/language-reference/functions/ctype-function.md) – klíčové slovo převodu při přípravě vaše argumenty.</span><span class="sxs-lookup"><span data-stu-id="38699-168">You can reduce the chance of data type mismatches by using the [CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md) conversion keyword when preparing your arguments.</span></span>  
  
### <a name="overload-resolution-failure"></a><span data-ttu-id="38699-169">Chybě rozlišení přetížení</span><span class="sxs-lookup"><span data-stu-id="38699-169">Overload Resolution Failure</span></span>  
 <span data-ttu-id="38699-170">Při volání přetížené procedury, kompilátor se pokusí odstranit všechny kromě jednoho z přetížení.</span><span class="sxs-lookup"><span data-stu-id="38699-170">When you call an overloaded procedure, the compiler attempts to eliminate all but one of the overloads.</span></span> <span data-ttu-id="38699-171">Pokud se aktivace podaří, přeloží volání tohoto přetížení.</span><span class="sxs-lookup"><span data-stu-id="38699-171">If it succeeds, it resolves the call to that overload.</span></span> <span data-ttu-id="38699-172">Pokud eliminuje všechna přetížení, nebo pokud ji nemůžete zmenšit oprávněné přetížení k jedné Release candidate, dojde k chybě.</span><span class="sxs-lookup"><span data-stu-id="38699-172">If it eliminates all the overloads, or if it cannot reduce the eligible overloads to a single candidate, it generates an error.</span></span>  
  
 <span data-ttu-id="38699-173">Následující příklad znázorňuje proces řešení přetížení.</span><span class="sxs-lookup"><span data-stu-id="38699-173">The following example illustrates the overload resolution process.</span></span>  
  
 [!code-vb[VbVbcnProcedures#62](./codesnippet/VisualBasic/troubleshooting-procedures_6.vb)]  
  
 [!code-vb[VbVbcnProcedures#63](./codesnippet/VisualBasic/troubleshooting-procedures_7.vb)]  
  
 <span data-ttu-id="38699-174">Při prvním volání, kompilátor eliminuje první přetížení, protože typ prvního argumentu (`Short`) zužuje na typ odpovídající parametru (`Byte`).</span><span class="sxs-lookup"><span data-stu-id="38699-174">In the first call, the compiler eliminates the first overload because the type of the first argument (`Short`) narrows to the type of the corresponding parameter (`Byte`).</span></span> <span data-ttu-id="38699-175">Eliminuje pak třetí přetížení, protože typ každého argumentu ve druhé přetížení (`Short` a `Single`) rozšiřuje na odpovídající typ v třetí přetížení (`Integer` a `Single`).</span><span class="sxs-lookup"><span data-stu-id="38699-175">It then eliminates the third overload because each argument type in the second overload (`Short` and `Single`) widens to the corresponding type in the third overload (`Integer` and `Single`).</span></span> <span data-ttu-id="38699-176">Druhé přetížení vyžaduje méně rozšíření, takže kompilátor používá volání.</span><span class="sxs-lookup"><span data-stu-id="38699-176">The second overload requires less widening, so the compiler uses it for the call.</span></span>  
  
 <span data-ttu-id="38699-177">V druhém volání nelze odstranit kompilátor některý z přetížení na základě zužující.</span><span class="sxs-lookup"><span data-stu-id="38699-177">In the second call, the compiler cannot eliminate any of the overloads on the basis of narrowing.</span></span> <span data-ttu-id="38699-178">Eliminuje třetí přetížení ze stejného důvodu jako první volání, vzhledem k tomu, že může volat druhé přetížení s méně rozšiřující typy argumentů.</span><span class="sxs-lookup"><span data-stu-id="38699-178">It eliminates the third overload for the same reason as in the first call, because it can call the second overload with less widening of the argument types.</span></span> <span data-ttu-id="38699-179">Kompilátor však nelze rozlišit mezi první a druhé přetížení.</span><span class="sxs-lookup"><span data-stu-id="38699-179">However, the compiler cannot resolve between the first and second overloads.</span></span> <span data-ttu-id="38699-180">Každá má jeden typ definovaný parametr, který rozšiřuje na odpovídající typ v jiném (`Byte` k `Short`, ale `Single` k `Double`).</span><span class="sxs-lookup"><span data-stu-id="38699-180">Each has one defined parameter type that widens to the corresponding type in the other (`Byte` to `Short`, but `Single` to `Double`).</span></span> <span data-ttu-id="38699-181">Kompilátor tedy dojde k chybě rozlišení přetížení.</span><span class="sxs-lookup"><span data-stu-id="38699-181">The compiler therefore generates an overload resolution error.</span></span>  
  
 <span data-ttu-id="38699-182">**Správný způsob:** Aby bylo možné volání přetížené procedury bez nejednoznačnost, použijte [funkce CType](../../../../visual-basic/language-reference/functions/ctype-function.md) tak, aby odpovídaly datové typy argumentů na typy parametrů.</span><span class="sxs-lookup"><span data-stu-id="38699-182">**Correct Approach:** To be able to call an overloaded procedure without ambiguity, use [CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md) to match the argument data types to the parameter types.</span></span> <span data-ttu-id="38699-183">Následující příklad ukazuje volání `z` druhé přetížení, která vynutí řešení.</span><span class="sxs-lookup"><span data-stu-id="38699-183">The following example shows a call to `z` that forces resolution to the second overload.</span></span>  
  
 [!code-vb[VbVbcnProcedures#65](./codesnippet/VisualBasic/troubleshooting-procedures_8.vb)]  
  
### <a name="overload-resolution-with-optional-and-paramarray-arguments"></a><span data-ttu-id="38699-184">Přetížení překlad IP adres s volitelným a ParamArray – argumenty</span><span class="sxs-lookup"><span data-stu-id="38699-184">Overload Resolution with Optional and ParamArray Arguments</span></span>  
 <span data-ttu-id="38699-185">Pokud dvě přetížení procedury, mají stejné podpisy, s tím rozdílem, že je deklarován poslední parametr [volitelné](../../../../visual-basic/language-reference/modifiers/optional.md) v jednom a [ParamArray](../../../../visual-basic/language-reference/modifiers/paramarray.md) , ve druhém přeloží kompilátor volání této procedury podle nejvíce odpovídá.</span><span class="sxs-lookup"><span data-stu-id="38699-185">If two overloads of a procedure have identical signatures except that the last parameter is declared [Optional](../../../../visual-basic/language-reference/modifiers/optional.md) in one and [ParamArray](../../../../visual-basic/language-reference/modifiers/paramarray.md) in the other, the compiler resolves a call to that procedure according to the closest match.</span></span> <span data-ttu-id="38699-186">Další informace najdete v tématu [rozlišení přetížení](./overload-resolution.md).</span><span class="sxs-lookup"><span data-stu-id="38699-186">For more information, see [Overload Resolution](./overload-resolution.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="38699-187">Viz také:</span><span class="sxs-lookup"><span data-stu-id="38699-187">See also</span></span>
- [<span data-ttu-id="38699-188">Procedury</span><span class="sxs-lookup"><span data-stu-id="38699-188">Procedures</span></span>](./index.md)
- [<span data-ttu-id="38699-189">Procedury Sub</span><span class="sxs-lookup"><span data-stu-id="38699-189">Sub Procedures</span></span>](./sub-procedures.md)
- [<span data-ttu-id="38699-190">Procedury funkce</span><span class="sxs-lookup"><span data-stu-id="38699-190">Function Procedures</span></span>](./function-procedures.md)
- [<span data-ttu-id="38699-191">Procedury vlastnosti</span><span class="sxs-lookup"><span data-stu-id="38699-191">Property Procedures</span></span>](./property-procedures.md)
- [<span data-ttu-id="38699-192">Procedury operátoru</span><span class="sxs-lookup"><span data-stu-id="38699-192">Operator Procedures</span></span>](./operator-procedures.md)
- [<span data-ttu-id="38699-193">Parametry a argumenty procedury</span><span class="sxs-lookup"><span data-stu-id="38699-193">Procedure Parameters and Arguments</span></span>](./procedure-parameters-and-arguments.md)
- [<span data-ttu-id="38699-194">Přetížení procedury</span><span class="sxs-lookup"><span data-stu-id="38699-194">Procedure Overloading</span></span>](./procedure-overloading.md)
- [<span data-ttu-id="38699-195">Aspekty přetížení procedur</span><span class="sxs-lookup"><span data-stu-id="38699-195">Considerations in Overloading Procedures</span></span>](./considerations-in-overloading-procedures.md)
- [<span data-ttu-id="38699-196">Řešení přetížení</span><span class="sxs-lookup"><span data-stu-id="38699-196">Overload Resolution</span></span>](./overload-resolution.md)
