---
title: N-tice
ms.date: 04/23/2017
helpviewer_keywords:
- tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
ms.openlocfilehash: e0310f31d7becb1f79bb023a277bd565421b44fb
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 11/22/2019
ms.locfileid: "74350503"
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="402ad-102">Řazené kolekce členů (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="402ad-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="402ad-103">Počínaje Visual Basic 2017 Visual Basic jazyk nabízí integrovanou podporu pro řazené kolekce členů, které usnadňují vytváření řazených kolekcí členů a přístup k prvkům řazených kolekcí členů.</span><span class="sxs-lookup"><span data-stu-id="402ad-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="402ad-104">Řazená kolekce členů je odlehčená datová struktura, která má určitý počet a sekvenci hodnot.</span><span class="sxs-lookup"><span data-stu-id="402ad-104">A tuple is a lightweight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="402ad-105">Při vytváření instance řazené kolekce členů definujte číslo a datový typ každé hodnoty (nebo element).</span><span class="sxs-lookup"><span data-stu-id="402ad-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="402ad-106">Například 2-Tuple (nebo dvojice) má dva prvky.</span><span class="sxs-lookup"><span data-stu-id="402ad-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="402ad-107">První může být `Boolean` hodnota, zatímco druhá je `String`.</span><span class="sxs-lookup"><span data-stu-id="402ad-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="402ad-108">Vzhledem k tomu, že řazené kolekce členů usnadňují ukládání více hodnot do jediného objektu, jsou často používány jako jednoduchý způsob, jak vrátit více hodnot z metody.</span><span class="sxs-lookup"><span data-stu-id="402ad-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="402ad-109">Podpora řazené kolekce členů vyžaduje typ <xref:System.ValueTuple>.</span><span class="sxs-lookup"><span data-stu-id="402ad-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="402ad-110">Pokud není nainstalován .NET Framework 4,7, je nutné přidat balíček NuGet `System.ValueTuple`, který je k dispozici v galerii NuGet.</span><span class="sxs-lookup"><span data-stu-id="402ad-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="402ad-111">Bez tohoto balíčku se může zobrazit chyba kompilace podobná "předdefinovaný typ ValueTuple (of,,,) není definován ani importován."</span><span class="sxs-lookup"><span data-stu-id="402ad-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="402ad-112">Vytváření instancí a používání řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="402ad-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="402ad-113">Vytvořte instanci řazené kolekce členů uzavřením hodnot oddělených čárkami a závorkou.</span><span class="sxs-lookup"><span data-stu-id="402ad-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="402ad-114">Každá z těchto hodnot se pak stala polem řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="402ad-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="402ad-115">Například následující kód definuje Troji (nebo 3-Tuple) s `Date` jako první hodnotu, `String` jako druhou a `Boolean` jako třetí.</span><span class="sxs-lookup"><span data-stu-id="402ad-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="402ad-116">Ve výchozím nastavení se název každého pole v rámci řazené kolekce členů skládá z řetězce `Item` společně s jednou pozicí pole v řazené kolekci členů.</span><span class="sxs-lookup"><span data-stu-id="402ad-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="402ad-117">U této 3-řazené kolekce členů se pole `Date` `Item1`, `String` pole je `Item2`a `Boolean` pole je `Item3`.</span><span class="sxs-lookup"><span data-stu-id="402ad-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="402ad-118">Následující příklad zobrazí hodnoty polí instance řazené kolekce členů v předchozím řádku kódu.</span><span class="sxs-lookup"><span data-stu-id="402ad-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="402ad-119">Pole Visual Basic řazené kolekce členů jsou pro čtení i zápis; Po vytvoření instance řazené kolekce členů můžete změnit její hodnoty.</span><span class="sxs-lookup"><span data-stu-id="402ad-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="402ad-120">Následující příklad upraví dvě pole řazené kolekce členů vytvořená v předchozím příkladu a zobrazí výsledek.</span><span class="sxs-lookup"><span data-stu-id="402ad-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="402ad-121">Vytváření instancí a používání pojmenované řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="402ad-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="402ad-122">Místo používání výchozích názvů pro pole řazené kolekce členů můžete vytvořit instanci *pojmenované řazené kolekce členů* přiřazením vlastních názvů prvkům řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="402ad-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="402ad-123">K polím řazené kolekce členů lze následně přistupovat pomocí jejich přiřazených názvů *nebo* jejich výchozích názvů.</span><span class="sxs-lookup"><span data-stu-id="402ad-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="402ad-124">Následující příklad vytvoří instanci stejné 3-řazené kolekce členů jako dříve, s tím rozdílem, že explicitně pojmenuje první pole `EventDate`, druhý `Name`a třetí `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="402ad-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="402ad-125">Pak zobrazí hodnoty polí, upraví je a zobrazí hodnoty polí znovu.</span><span class="sxs-lookup"><span data-stu-id="402ad-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="inferred-tuple-element-names"></a><span data-ttu-id="402ad-126">Odvozené názvy elementů řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="402ad-126">Inferred tuple element names</span></span>

<span data-ttu-id="402ad-127">Počínaje Visual Basic 15,3 Visual Basic mohou odvodit názvy elementů řazené kolekce členů; nemusíte je explicitně přiřazovat.</span><span class="sxs-lookup"><span data-stu-id="402ad-127">Starting with Visual Basic 15.3, Visual Basic can infer the names of tuple elements; you do not have to assign them explicitly.</span></span> <span data-ttu-id="402ad-128">Odvozené názvy řazených kolekcí členů jsou užitečné při inicializaci řazené kolekce členů ze sady proměnných a chcete, aby název elementu řazené kolekce členů byl stejný jako název proměnné.</span><span class="sxs-lookup"><span data-stu-id="402ad-128">Inferred tuple names are useful when you initialize a tuple from a set of variables, and you want the tuple element name to be the same as the variable name.</span></span>

<span data-ttu-id="402ad-129">Následující příklad vytvoří řazenou kolekci členů `stateInfo`, která obsahuje tři explicitně pojmenované elementy, `state`, `stateName`a `capital`.</span><span class="sxs-lookup"><span data-stu-id="402ad-129">The following example creates a `stateInfo` tuple that contains three explicitly named elements, `state`, `stateName`, and `capital`.</span></span> <span data-ttu-id="402ad-130">Všimněte si, že při pojmenovávání prvků příkaz inicializace řazené kolekce členů jednoduše přiřadí pojmenované prvky hodnoty identicky pojmenovaných proměnných.</span><span class="sxs-lookup"><span data-stu-id="402ad-130">Note that, in naming the elements, the tuple initialization statement simply assigns the named elements the values of the identically named variables.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#1)]

<span data-ttu-id="402ad-131">Vzhledem k tomu, že prvky a proměnné mají stejný název, kompilátor Visual Basic může odvodit názvy polí, jak ukazuje následující příklad.</span><span class="sxs-lookup"><span data-stu-id="402ad-131">Because elements and variables have the same name, the Visual Basic compiler can infer the names of the fields, as the following example shows.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#2)]

<span data-ttu-id="402ad-132">Chcete-li povolit odvozené názvy elementů řazené kolekce členů, je nutné definovat verzi Visual Basic kompilátoru pro použití v souboru projektu Visual Basic (\*. vbproj):</span><span class="sxs-lookup"><span data-stu-id="402ad-132">To enable inferred tuple element names, you must define the version of the Visual Basic compiler to use in your Visual Basic project (\*.vbproj) file:</span></span>

```xml
<PropertyGroup>
  <LangVersion>15.3</LangVersion>
</PropertyGroup>
```

<span data-ttu-id="402ad-133">Číslo verze může být libovolná verze Visual Basic kompilátoru od 15,3.</span><span class="sxs-lookup"><span data-stu-id="402ad-133">The version number can be any version of the Visual Basic compiler starting with 15.3.</span></span> <span data-ttu-id="402ad-134">Namísto hardwarového kódování konkrétní verze kompilátoru můžete také zadat "nejnovější" jako hodnotu `LangVersion` ke kompilaci s nejnovější verzí Visual Basic kompilátoru nainstalovanou v systému.</span><span class="sxs-lookup"><span data-stu-id="402ad-134">Rather than hard-coding a specific compiler version, you can also specify "Latest" as the value of `LangVersion` to compile with the most recent version of the Visual Basic compiler installed on your system.</span></span>

<span data-ttu-id="402ad-135">Další informace najdete v tématu [nastavení jazykové verze Visual Basic](../../../language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="402ad-135">For more information, see [setting the Visual Basic language version](../../../language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="402ad-136">V některých případech kompilátor Visual Basic nemůže odvodit název prvku řazené kolekce členů z názvu kandidáta a pole řazené kolekce členů může být odkazováno pouze pomocí jeho výchozího názvu, například `Item1`, `Item2`atd. Mezi ně patří:</span><span class="sxs-lookup"><span data-stu-id="402ad-136">In some cases, the Visual Basic compiler cannot infer the tuple element name from the candidate name, and the tuple field can only be referenced using its default name, such as `Item1`, `Item2`, etc. These include:</span></span>

- <span data-ttu-id="402ad-137">Kandidátský název je stejný jako název člena řazené kolekce členů, například `Item3`, `Rest`nebo `ToString`.</span><span class="sxs-lookup"><span data-stu-id="402ad-137">The candidate name is the same as the name of a tuple member, such as `Item3`, `Rest`, or `ToString`.</span></span>

- <span data-ttu-id="402ad-138">Název kandidáta je duplikován v řazené kolekci členů.</span><span class="sxs-lookup"><span data-stu-id="402ad-138">The candidate name is duplicated in the tuple.</span></span>

<span data-ttu-id="402ad-139">Pokud se odvození názvu pole nepovede, Visual Basic negeneruje chybu kompilátoru, ani při běhu není vyvolána výjimka.</span><span class="sxs-lookup"><span data-stu-id="402ad-139">When field name inference fails, Visual Basic does not generate a compiler error, nor is an exception thrown at runtime.</span></span> <span data-ttu-id="402ad-140">Místo toho musí být pole řazené kolekce členů odkazována podle jejich předdefinovaných názvů, například `Item1` a `Item2`.</span><span class="sxs-lookup"><span data-stu-id="402ad-140">Instead, tuple fields must be referenced by their predefined names, such as `Item1` and `Item2`.</span></span>

## <a name="tuples-versus-structures"></a><span data-ttu-id="402ad-141">Řazené kolekce členů versus struktury</span><span class="sxs-lookup"><span data-stu-id="402ad-141">Tuples versus structures</span></span>

<span data-ttu-id="402ad-142">Visual Basic řazená kolekce členů je hodnotový typ, který je instancí jednoho z obecných typů **System. ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="402ad-142">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="402ad-143">Například `holiday` řazené kolekce členů definované v předchozím příkladu je instance <xref:System.ValueTuple%603> struktury.</span><span class="sxs-lookup"><span data-stu-id="402ad-143">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="402ad-144">Je navržený jako odlehčený kontejner pro data.</span><span class="sxs-lookup"><span data-stu-id="402ad-144">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="402ad-145">Vzhledem k tomu, že řazená kolekce členů je snazší vytvořit objekt s více datovými položkami, chybí některé funkce, které může mít vlastní struktura.</span><span class="sxs-lookup"><span data-stu-id="402ad-145">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="402ad-146">Mezi ně patří:</span><span class="sxs-lookup"><span data-stu-id="402ad-146">These include:</span></span>

- <span data-ttu-id="402ad-147">Vlastní členové.</span><span class="sxs-lookup"><span data-stu-id="402ad-147">Custom members.</span></span> <span data-ttu-id="402ad-148">Nemůžete definovat vlastní vlastnosti, metody nebo události pro řazenou kolekci členů.</span><span class="sxs-lookup"><span data-stu-id="402ad-148">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="402ad-149">Export.</span><span class="sxs-lookup"><span data-stu-id="402ad-149">Validation.</span></span> <span data-ttu-id="402ad-150">Nemůžete ověřit pole data přiřazená.</span><span class="sxs-lookup"><span data-stu-id="402ad-150">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="402ad-151">Neměnnosti.</span><span class="sxs-lookup"><span data-stu-id="402ad-151">Immutability.</span></span> <span data-ttu-id="402ad-152">Řazené kolekce členů Visual Basic jsou proměnlivé.</span><span class="sxs-lookup"><span data-stu-id="402ad-152">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="402ad-153">Na rozdíl od vlastní struktury vám umožní řídit, jestli je instance proměnlivá nebo neměnná.</span><span class="sxs-lookup"><span data-stu-id="402ad-153">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="402ad-154">Pokud jsou vlastní členové, ověřování vlastností a polí nebo neměnnosti důležité, měli byste použít příkaz [struktura](../../../language-reference/statements/structure-statement.md) Visual Basic k definování vlastního typu hodnoty.</span><span class="sxs-lookup"><span data-stu-id="402ad-154">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="402ad-155">Visual Basic řazené kolekce členů dědí členy svého typu **ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="402ad-155">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="402ad-156">Kromě polí patří mezi ně tyto metody:</span><span class="sxs-lookup"><span data-stu-id="402ad-156">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="402ad-157">Člen</span><span class="sxs-lookup"><span data-stu-id="402ad-157">Member</span></span> | <span data-ttu-id="402ad-158">Popis</span><span class="sxs-lookup"><span data-stu-id="402ad-158">Description</span></span> |
| ---|---|
| <span data-ttu-id="402ad-159">CompareTo</span><span class="sxs-lookup"><span data-stu-id="402ad-159">CompareTo</span></span> | <span data-ttu-id="402ad-160">Porovná aktuální řazenou kolekci členů s jinou řazenou kolekci členů se stejným počtem prvků.</span><span class="sxs-lookup"><span data-stu-id="402ad-160">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="402ad-161">Je rovno</span><span class="sxs-lookup"><span data-stu-id="402ad-161">Equals</span></span> | <span data-ttu-id="402ad-162">Určuje, zda je aktuální řazená kolekce členů rovna jiné řazené kolekci členů nebo objektu.</span><span class="sxs-lookup"><span data-stu-id="402ad-162">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="402ad-163">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="402ad-163">GetHashCode</span></span> | <span data-ttu-id="402ad-164">Vypočítá kód hash aktuální instance.</span><span class="sxs-lookup"><span data-stu-id="402ad-164">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="402ad-165">Metodu</span><span class="sxs-lookup"><span data-stu-id="402ad-165">ToString</span></span> | <span data-ttu-id="402ad-166">Vrátí řetězcovou reprezentaci této řazené kolekce členů, která převezme `(Item1, Item2...)`formuláře, kde `Item1` a `Item2` reprezentují hodnoty polí řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="402ad-166">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="402ad-167">Kromě toho typy **ValueTuple** implementují rozhraní <xref:System.Collections.IStructuralComparable> a <xref:System.Collections.IStructuralEquatable>, která umožňují definovat porovnávače zákazníků.</span><span class="sxs-lookup"><span data-stu-id="402ad-167">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="402ad-168">Přiřazení a řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="402ad-168">Assignment and tuples</span></span>

<span data-ttu-id="402ad-169">Visual Basic podporuje přiřazení mezi typy řazené kolekce členů, které mají stejný počet polí.</span><span class="sxs-lookup"><span data-stu-id="402ad-169">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="402ad-170">Typy polí lze převést, pokud je splněna jedna z následujících hodnot:</span><span class="sxs-lookup"><span data-stu-id="402ad-170">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="402ad-171">Zdrojové a cílové pole je stejného typu.</span><span class="sxs-lookup"><span data-stu-id="402ad-171">The source and target field are of the same type.</span></span>

- <span data-ttu-id="402ad-172">Je definovaný rozšiřující (nebo implicitní) převod zdrojového typu na cílový typ.</span><span class="sxs-lookup"><span data-stu-id="402ad-172">A widening (or implicit) conversion of the source type to the target type is defined.</span></span>

- <span data-ttu-id="402ad-173">`Option Strict` je `On`a je definována úzká (nebo explicitní) konverze zdrojového typu na cílový typ.</span><span class="sxs-lookup"><span data-stu-id="402ad-173">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="402ad-174">Tento převod může vyvolat výjimku, pokud je zdrojová hodnota mimo rozsah cílového typu.</span><span class="sxs-lookup"><span data-stu-id="402ad-174">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="402ad-175">Jiné převody se nepovažují za přiřazení.</span><span class="sxs-lookup"><span data-stu-id="402ad-175">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="402ad-176">Pojďme se podívat na typy přiřazení, která jsou povolená mezi typy řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="402ad-176">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="402ad-177">Vezměte v úvahu tyto proměnné, které se používají v následujících příkladech:</span><span class="sxs-lookup"><span data-stu-id="402ad-177">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="402ad-178">První dvě proměnné, `unnamed` a `anonymous`, nemají pro pole k dispozici sémantické názvy.</span><span class="sxs-lookup"><span data-stu-id="402ad-178">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="402ad-179">Jejich názvy polí jsou výchozí `Item1` a `Item2`.</span><span class="sxs-lookup"><span data-stu-id="402ad-179">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="402ad-180">Poslední dvě proměnné `named` a `differentName` mají sémantické názvy polí.</span><span class="sxs-lookup"><span data-stu-id="402ad-180">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="402ad-181">Všimněte si, že tyto dvě řazené kolekce členů mají pro pole různé názvy.</span><span class="sxs-lookup"><span data-stu-id="402ad-181">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="402ad-182">Všechny čtyři tyto řazené kolekce členů mají stejný počet polí (označované jako "aritou") a typy těchto polí jsou identické.</span><span class="sxs-lookup"><span data-stu-id="402ad-182">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="402ad-183">Proto všechna tato přiřazení fungují:</span><span class="sxs-lookup"><span data-stu-id="402ad-183">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="402ad-184">Všimněte si, že názvy řazených kolekcí členů nejsou přiřazeny.</span><span class="sxs-lookup"><span data-stu-id="402ad-184">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="402ad-185">Hodnoty polí jsou přiřazeny podle pořadí polí v řazené kolekci členů.</span><span class="sxs-lookup"><span data-stu-id="402ad-185">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="402ad-186">Nakonec si všimněte, že je možné přiřadit `named` řazené kolekce členů k `conversion` řazené kolekci členů, i když je prvním polem `named` `Integer`a prvním polem `conversion` je `Long`.</span><span class="sxs-lookup"><span data-stu-id="402ad-186">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="402ad-187">Toto přiřazení je úspěšné, protože převod `Integer` na `Long` je rozšiřující převod.</span><span class="sxs-lookup"><span data-stu-id="402ad-187">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="402ad-188">Řazené kolekce členů s různými počty polí nelze přiřadit:</span><span class="sxs-lookup"><span data-stu-id="402ad-188">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="402ad-189">Řazené kolekce členů jako návratové hodnoty metody</span><span class="sxs-lookup"><span data-stu-id="402ad-189">Tuples as method return values</span></span>

<span data-ttu-id="402ad-190">Metoda může vracet pouze jednu hodnotu.</span><span class="sxs-lookup"><span data-stu-id="402ad-190">A method can return only a single value.</span></span> <span data-ttu-id="402ad-191">Často se ale jako volání metody vrací více hodnot.</span><span class="sxs-lookup"><span data-stu-id="402ad-191">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="402ad-192">Toto omezení můžete vyřešit několika způsoby:</span><span class="sxs-lookup"><span data-stu-id="402ad-192">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="402ad-193">Můžete vytvořit vlastní třídu nebo strukturu, jejíž vlastnosti nebo pole reprezentují hodnoty vrácené metodou.</span><span class="sxs-lookup"><span data-stu-id="402ad-193">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="402ad-194">Proto je těžké řešení; vyžaduje, abyste definovali vlastní typ, jehož jediným účelem je načíst hodnoty z volání metody.</span><span class="sxs-lookup"><span data-stu-id="402ad-194">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="402ad-195">Můžete vrátit jednu hodnotu z metody a vrátit zbývající hodnoty jejich předáním odkazem na metodu.</span><span class="sxs-lookup"><span data-stu-id="402ad-195">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="402ad-196">To zahrnuje režijní náklady na vytvoření instance proměnné a rizika nechtěně přepíší hodnotu proměnné, kterou předáte odkazem.</span><span class="sxs-lookup"><span data-stu-id="402ad-196">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="402ad-197">Můžete použít řazenou kolekci členů, která poskytuje zjednodušené řešení pro načítání více návratových hodnot.</span><span class="sxs-lookup"><span data-stu-id="402ad-197">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="402ad-198">Například metody **TryParse** v rozhraní .net vracejí `Boolean` hodnotu, která označuje, zda byla operace analýzy úspěšná.</span><span class="sxs-lookup"><span data-stu-id="402ad-198">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="402ad-199">Výsledek operace analýzy je vrácen v proměnné předané pomocí odkazu na metodu.</span><span class="sxs-lookup"><span data-stu-id="402ad-199">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="402ad-200">V normálním případě volání metody analýzy, jako je například <xref:System.Int32.TryParse%2A?displayProperty=nameWithType>, vypadá takto:</span><span class="sxs-lookup"><span data-stu-id="402ad-200">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="402ad-201">Pokud zabalíme volání metody <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> v naší vlastní metodě, můžeme vrátit řazenou kolekci členů z operace analýzy.</span><span class="sxs-lookup"><span data-stu-id="402ad-201">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="402ad-202">V následujícím příkladu `NumericLibrary.ParseInteger` volá metodu <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> a vrací pojmenované řazené kolekce členů se dvěma prvky.</span><span class="sxs-lookup"><span data-stu-id="402ad-202">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="402ad-203">Pak můžete zavolat metodu s kódem podobným následujícímu:</span><span class="sxs-lookup"><span data-stu-id="402ad-203">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="402ad-204">Visual Basic řazené kolekce členů a řazené kolekce členů v .NET Framework</span><span class="sxs-lookup"><span data-stu-id="402ad-204">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="402ad-205">Visual Basic řazená kolekce členů je instancí jednoho z obecných typů **System. ValueTuple** , které byly představeny v .NET Framework 4,7.</span><span class="sxs-lookup"><span data-stu-id="402ad-205">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="402ad-206">.NET Framework také obsahuje sadu obecných tříd **System. Tuple** .</span><span class="sxs-lookup"><span data-stu-id="402ad-206">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="402ad-207">Tyto třídy se však liší od Visual Basicch řazených kolekcí členů a obecných typů **System. ValueTuple** mnoha různými způsoby:</span><span class="sxs-lookup"><span data-stu-id="402ad-207">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="402ad-208">Prvky tříd **řazené kolekce členů** jsou vlastnosti s názvem `Item1`, `Item2`a tak dále.</span><span class="sxs-lookup"><span data-stu-id="402ad-208">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="402ad-209">V Visual Basic řazené kolekce členů a typy **ValueTuple** jsou prvky řazené kolekce členů pole.</span><span class="sxs-lookup"><span data-stu-id="402ad-209">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="402ad-210">K prvkům instance **řazené kolekce členů** nebo instanci **ValueTuple** nelze přiřadit smysluplné názvy.</span><span class="sxs-lookup"><span data-stu-id="402ad-210">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="402ad-211">Visual Basic umožňuje přiřadit názvy, které komunikují význam polí.</span><span class="sxs-lookup"><span data-stu-id="402ad-211">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="402ad-212">Vlastnosti instance **řazené kolekce členů** jsou jen pro čtení; řazené kolekce členů jsou neměnné.</span><span class="sxs-lookup"><span data-stu-id="402ad-212">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="402ad-213">V Visual Basic řazené kolekce členů a typy **ValueTuple** jsou pole řazené kolekce členů i pro čtení i zápis; řazené kolekce členů jsou proměnlivé.</span><span class="sxs-lookup"><span data-stu-id="402ad-213">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="402ad-214">Obecné typy **řazené kolekce členů** jsou odkazové typy.</span><span class="sxs-lookup"><span data-stu-id="402ad-214">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="402ad-215">Použití těchto typů **řazené kolekce členů** znamená přidělení objektů.</span><span class="sxs-lookup"><span data-stu-id="402ad-215">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="402ad-216">U aktivních cest to může mít měřitelný dopad na výkon vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="402ad-216">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="402ad-217">Visual Basic řazené kolekce členů a typy **ValueTuple** jsou typy hodnot.</span><span class="sxs-lookup"><span data-stu-id="402ad-217">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="402ad-218">Metody rozšíření ve třídě <xref:System.TupleExtensions> usnadňují konverzi mezi Visual Basic řazenými kolekcemi členů a objekty **řazené kolekce členů** .NET.</span><span class="sxs-lookup"><span data-stu-id="402ad-218">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="402ad-219">Metoda **ToTuple** převádí Visual Basic řazené kolekce členů na objekt .NET **řazené** kolekce členů a metoda **ToValueTuple** převede objekt **řazené kolekce členů** .NET na Visual Basic řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="402ad-219">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="402ad-220">Následující příklad vytvoří řazenou kolekci členů, převede ji na objekt **řazené kolekce členů** .NET a převede ji zpět na Visual Basic řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="402ad-220">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="402ad-221">Příklad následně Porovná tuto řazenou kolekci členů s původní verzí, aby se zajistilo, že jsou stejné.</span><span class="sxs-lookup"><span data-stu-id="402ad-221">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="402ad-222">Viz také:</span><span class="sxs-lookup"><span data-stu-id="402ad-222">See also</span></span>

- [<span data-ttu-id="402ad-223">Referenční příručka jazyka Visual Basic</span><span class="sxs-lookup"><span data-stu-id="402ad-223">Visual Basic Language Reference</span></span>](index.md)
