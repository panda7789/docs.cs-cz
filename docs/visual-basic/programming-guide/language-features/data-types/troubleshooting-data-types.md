---
title: Řešení potíží s datovými typy (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- Char data type [Visual Basic], converting
- Decimal data type [Visual Basic], conversions
- data types [Visual Basic], troubleshooting
- literals [Visual Basic], default types
- type characters [Visual Basic], literal
- Mod operator [Visual Basic], in floating-point operations
- troubleshooting Visual Basic, data types
- troubleshooting data types [Visual Basic]
- floating-point numbers [Visual Basic], precision
- Boolean data type [Visual Basic], converting
- literal types [Visual Basic]
- literal type characters [Visual Basic]
- floating-point numbers [Visual Basic], imprecision
- String data type [Visual Basic], converting
- floating-point numbers [Visual Basic], comparison
- floating-point numbers
ms.assetid: 90040d67-b630-4125-a6ae-37195b079042
ms.openlocfilehash: 5b2cb0d5270b7e14c3462aeaf54942f939511fd7
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/22/2019
ms.locfileid: "69933236"
---
# <a name="troubleshooting-data-types-visual-basic"></a><span data-ttu-id="62bbd-102">Řešení potíží s datovými typy (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="62bbd-102">Troubleshooting Data Types (Visual Basic)</span></span>
<span data-ttu-id="62bbd-103">Tato stránka obsahuje některé běžné problémy, které mohou nastat při provádění operací na vnitřních datových typech.</span><span class="sxs-lookup"><span data-stu-id="62bbd-103">This page lists some common problems that can occur when you perform operations on intrinsic data types.</span></span>  
  
## <a name="floating-point-expressions-do-not-compare-as-equal"></a><span data-ttu-id="62bbd-104">Výrazy s plovoucí desetinnou čárkou se nerovnávají stejně jako stejné</span><span class="sxs-lookup"><span data-stu-id="62bbd-104">Floating-Point Expressions Do Not Compare as Equal</span></span>  
 <span data-ttu-id="62bbd-105">Když pracujete s čísly s plovoucí desetinnou čárkou ([datový typ s jedním datovým typem](../../../../visual-basic/language-reference/data-types/single-data-type.md) a [dvojitým datovým typem](../../../../visual-basic/language-reference/data-types/double-data-type.md)), pamatujte, že jsou uloženy jako binární zlomky.</span><span class="sxs-lookup"><span data-stu-id="62bbd-105">When you work with floating-point numbers ([Single Data Type](../../../../visual-basic/language-reference/data-types/single-data-type.md) and [Double Data Type](../../../../visual-basic/language-reference/data-types/double-data-type.md)), remember that they are stored as binary fractions.</span></span> <span data-ttu-id="62bbd-106">To znamená, že nemohou uchovávat přesnou reprezentaci žádného množství, které není binární zlomek (z formuláře k/(2 ^ n), kde k a n jsou celá čísla).</span><span class="sxs-lookup"><span data-stu-id="62bbd-106">This means they cannot hold an exact representation of any quantity that is not a binary fraction (of the form k / (2 ^ n) where k and n are integers).</span></span> <span data-ttu-id="62bbd-107">Například 0,5 (= 1/2) a 0,3125 (= 5/16) lze uchovávat jako přesné hodnoty, zatímco 0,2 (= 1/5) a 0,3 (= 3/10) mohou být pouze aproximace.</span><span class="sxs-lookup"><span data-stu-id="62bbd-107">For example, 0.5 (= 1/2) and 0.3125 (= 5/16) can be held as precise values, whereas 0.2 (= 1/5) and 0.3 (= 3/10) can be only approximations.</span></span>  
  
 <span data-ttu-id="62bbd-108">Z důvodu této nepřesnosti nelze při provozování hodnot s plovoucí desetinnou čárkou spoléhat na přesné výsledky.</span><span class="sxs-lookup"><span data-stu-id="62bbd-108">Because of this imprecision, you cannot rely on exact results when you operate on floating-point values.</span></span> <span data-ttu-id="62bbd-109">Zejména dvě hodnoty, které jsou teoreticky stejné, mohou mít mírně odlišné reprezentace.</span><span class="sxs-lookup"><span data-stu-id="62bbd-109">In particular, two values that are theoretically equal might have slightly different representations.</span></span>  
  
| <span data-ttu-id="62bbd-110">Porovnání množství s plovoucí desetinnou čárkou</span><span class="sxs-lookup"><span data-stu-id="62bbd-110">To compare floating-point quantities</span></span> | 
|---| 
|<span data-ttu-id="62bbd-111">1.  Vypočítá absolutní hodnotu jejich rozdílu pomocí <xref:System.Math.Abs%2A> metody <xref:System.Math> třídy v <xref:System> oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="62bbd-111">1.  Calculate the absolute value of their difference by using the <xref:System.Math.Abs%2A> method of the <xref:System.Math> class in the <xref:System> namespace.</span></span><br /><span data-ttu-id="62bbd-112">2.  Určete přijatelný maximální rozdíl, aby bylo možné považovat dvě množství za praktické účely, pokud jejich rozdíl není větší.</span><span class="sxs-lookup"><span data-stu-id="62bbd-112">2.  Determine an acceptable maximum difference, such that you can consider the two quantities to be equal for practical purposes if their difference is no larger.</span></span><br /><span data-ttu-id="62bbd-113">3.  Porovná absolutní hodnotu rozdílu s přijatelným rozdílem.</span><span class="sxs-lookup"><span data-stu-id="62bbd-113">3.  Compare the absolute value of the difference to the acceptable difference.</span></span>|  
  
 <span data-ttu-id="62bbd-114">Následující příklad ukazuje nesprávné a správné porovnání dvou `Double` hodnot.</span><span class="sxs-lookup"><span data-stu-id="62bbd-114">The following example demonstrates both incorrect and correct comparison of two `Double` values.</span></span>  
  
 [!code-vb[VbVbalrDataTypes#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrDataTypes/VB/Class1.vb#10)]  
  
 <span data-ttu-id="62bbd-115">Předchozí příklad používá <xref:System.Double.ToString%2A> metodu <xref:System.Double> struktury tak, aby mohla `CStr` určovat lepší přesnost, než klíčové slovo používá.</span><span class="sxs-lookup"><span data-stu-id="62bbd-115">The previous example uses the <xref:System.Double.ToString%2A> method of the <xref:System.Double> structure so that it can specify better  precision than the `CStr` keyword uses.</span></span> <span data-ttu-id="62bbd-116">Výchozí hodnota je 15 číslic, ale formát "G17" ho rozšiřuje na 17 číslic.</span><span class="sxs-lookup"><span data-stu-id="62bbd-116">The default is 15 digits, but the "G17" format extends it to 17 digits.</span></span>  
  
## <a name="mod-operator-does-not-return-accurate-result"></a><span data-ttu-id="62bbd-117">Operátor mod nevrací přesný výsledek.</span><span class="sxs-lookup"><span data-stu-id="62bbd-117">Mod Operator Does Not Return Accurate Result</span></span>  
 <span data-ttu-id="62bbd-118">Z důvodu nepřesnosti úložiště s plovoucí desetinnou čárkou může [operátor mod](../../../../visual-basic/language-reference/operators/mod-operator.md) vracet neočekávaný výsledek, pokud je alespoň jeden z operandů plovoucí desetinné čárky.</span><span class="sxs-lookup"><span data-stu-id="62bbd-118">Because of the imprecision of floating-point storage, the [Mod Operator](../../../../visual-basic/language-reference/operators/mod-operator.md) can return an unexpected result when at least one of the operands is floating-point.</span></span>  
  
 <span data-ttu-id="62bbd-119">[Datový typ Decimal](../../../../visual-basic/language-reference/data-types/decimal-data-type.md) nepoužívá reprezentaci s plovoucí desetinnou čárkou.</span><span class="sxs-lookup"><span data-stu-id="62bbd-119">The [Decimal Data Type](../../../../visual-basic/language-reference/data-types/decimal-data-type.md) does not use floating-point representation.</span></span> <span data-ttu-id="62bbd-120">Mnoho čísel, která jsou nepřesné `Double` v `Single` a jsou `Decimal` přesně v nástroji (například 0,2 a 0,3).</span><span class="sxs-lookup"><span data-stu-id="62bbd-120">Many numbers that are inexact in `Single` and `Double` are exact in `Decimal` (for example 0.2 and 0.3).</span></span> <span data-ttu-id="62bbd-121">I když aritmetická operace `Decimal` je pomalejší než v pohyblivé řádové čárce, může to znamenat snížení výkonu za účelem dosažení lepší přesnosti.</span><span class="sxs-lookup"><span data-stu-id="62bbd-121">Although arithmetic is slower in `Decimal` than in floating-point, it might be worth the performance decrease to achieve better precision.</span></span>  
  
|<span data-ttu-id="62bbd-122">Vyhledání celého čísla zbytku množství s plovoucí desetinnou čárkou</span><span class="sxs-lookup"><span data-stu-id="62bbd-122">To find the integer remainder of floating-point quantities</span></span>|  
|---|  
|<span data-ttu-id="62bbd-123">1.  Deklarujte proměnné `Decimal`jako.</span><span class="sxs-lookup"><span data-stu-id="62bbd-123">1.  Declare variables as `Decimal`.</span></span><br /><span data-ttu-id="62bbd-124">2.  Použijte znak `D` literálového typu k vynucení literálů `Decimal`pro, pro případ, že jejich hodnoty `Long` jsou pro datový typ příliš velké.</span><span class="sxs-lookup"><span data-stu-id="62bbd-124">2.  Use the literal type character `D` to force literals to `Decimal`, in case their values are too large for the `Long` data type.</span></span>|  
  
 <span data-ttu-id="62bbd-125">Následující příklad ukazuje potenciální nepřesnost operandů s plovoucí desetinnou čárkou.</span><span class="sxs-lookup"><span data-stu-id="62bbd-125">The following example demonstrates the potential imprecision of floating-point operands.</span></span>  
  
 [!code-vb[VbVbalrDataTypes#11](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrDataTypes/VB/Class1.vb#11)]  
  
 <span data-ttu-id="62bbd-126">Předchozí příklad používá <xref:System.Double.ToString%2A> metodu <xref:System.Double> struktury tak, aby mohla `CStr` určovat lepší přesnost, než klíčové slovo používá.</span><span class="sxs-lookup"><span data-stu-id="62bbd-126">The previous example uses the <xref:System.Double.ToString%2A> method of the <xref:System.Double> structure so that it can specify better precision than the `CStr` keyword uses.</span></span> <span data-ttu-id="62bbd-127">Výchozí hodnota je 15 číslic, ale formát "G17" ho rozšiřuje na 17 číslic.</span><span class="sxs-lookup"><span data-stu-id="62bbd-127">The default is 15 digits, but the "G17" format extends it to 17 digits.</span></span>  
  
 <span data-ttu-id="62bbd-128">Vzhledem k tomu `zeroPointTwo`,že jeho hodnota pro 0,2 je nekonečně opakující se binární zlomek s uloženou hodnotou 0.20000000000000001. `Double`</span><span class="sxs-lookup"><span data-stu-id="62bbd-128">Because `zeroPointTwo` is `Double`, its value for 0.2 is an infinitely repeating binary fraction with a stored value of 0.20000000000000001.</span></span> <span data-ttu-id="62bbd-129">Rozdělení 2,0 tímto množstvím vrátí 9.9999999999999995 se zbytkem 0.19999999999999991.</span><span class="sxs-lookup"><span data-stu-id="62bbd-129">Dividing 2.0 by this quantity yields 9.9999999999999995 with a remainder of 0.19999999999999991.</span></span>  
  
 <span data-ttu-id="62bbd-130">Ve výrazu pro `decimalRemainder`, literální znak `D` typu `Decimal`vynutí oba operandy, a 0,2 má přesnou reprezentaci.</span><span class="sxs-lookup"><span data-stu-id="62bbd-130">In the expression for `decimalRemainder`, the literal type character `D` forces both operands to `Decimal`, and 0.2 has a precise representation.</span></span> <span data-ttu-id="62bbd-131">`Mod` Proto operátor vypočítá očekávanou zbývající část 0,0.</span><span class="sxs-lookup"><span data-stu-id="62bbd-131">Therefore the `Mod` operator yields the expected remainder of 0.0.</span></span>  
  
 <span data-ttu-id="62bbd-132">Všimněte si, že není dostačující deklarovat `decimalRemainder` jako. `Decimal`</span><span class="sxs-lookup"><span data-stu-id="62bbd-132">Note that it is not sufficient to declare `decimalRemainder` as `Decimal`.</span></span> <span data-ttu-id="62bbd-133">`Decimal`Musíte taky vynutit literály nebo použít `Double` ve výchozím nastavení a `decimalRemainder` stejnou nepřesnou hodnotu jako `doubleRemainder`.</span><span class="sxs-lookup"><span data-stu-id="62bbd-133">You must also force the literals to `Decimal`, or they use `Double` by default and `decimalRemainder` receives the same inaccurate value as `doubleRemainder`.</span></span>  
  
## <a name="boolean-type-does-not-convert-to-numeric-type-accurately"></a><span data-ttu-id="62bbd-134">Typ Boolean se přesně nepřevádí na číselný typ.</span><span class="sxs-lookup"><span data-stu-id="62bbd-134">Boolean Type Does Not Convert to Numeric Type Accurately</span></span>  
 <span data-ttu-id="62bbd-135">[Logické hodnoty datového typu](../../../../visual-basic/language-reference/data-types/boolean-data-type.md) nejsou uloženy jako čísla a uložené hodnoty nejsou určeny pro ekvivalent čísel.</span><span class="sxs-lookup"><span data-stu-id="62bbd-135">[Boolean Data Type](../../../../visual-basic/language-reference/data-types/boolean-data-type.md) values are not stored as numbers, and the stored values are not intended to be equivalent to numbers.</span></span> <span data-ttu-id="62bbd-136">Pro zajištění kompatibility se staršími verzemi Visual Basic poskytuje klíčová slova převodu ( `CBool`[funkce CType](../../../../visual-basic/language-reference/functions/ctype-function.md), `CInt`, a tak dále) pro `Boolean` převod mezi a číselnými typy.</span><span class="sxs-lookup"><span data-stu-id="62bbd-136">For compatibility with earlier versions, Visual Basic provides conversion keywords ([CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md), `CBool`, `CInt`, and so on) to convert between `Boolean` and numeric types.</span></span> <span data-ttu-id="62bbd-137">Nicméně jiné jazyky někdy provádějí tyto převody odlišně, stejně jako .NET Framework metody.</span><span class="sxs-lookup"><span data-stu-id="62bbd-137">However, other languages sometimes perform these conversions differently, as do the .NET Framework methods.</span></span>  
  
 <span data-ttu-id="62bbd-138">Nikdy byste neměli psát kód, který spoléhá na ekvivalentní číselné hodnoty `True` pro `False`a.</span><span class="sxs-lookup"><span data-stu-id="62bbd-138">You should never write code that relies on equivalent numeric values for `True` and `False`.</span></span> <span data-ttu-id="62bbd-139">Kdykoli je to možné, měli byste omezit `Boolean` použití proměnných na logické hodnoty, pro které jsou navrženy.</span><span class="sxs-lookup"><span data-stu-id="62bbd-139">Whenever possible, you should restrict usage of `Boolean` variables to the logical values for which they are designed.</span></span> <span data-ttu-id="62bbd-140">Pokud musíte kombinovat `Boolean` a číselné hodnoty, ujistěte se, že rozumíte metodě převodu, kterou jste vybrali.</span><span class="sxs-lookup"><span data-stu-id="62bbd-140">If you must mix `Boolean` and numeric values, make sure that you understand the conversion method that you select.</span></span>  
  
### <a name="conversion-in-visual-basic"></a><span data-ttu-id="62bbd-141">Převod v Visual Basic</span><span class="sxs-lookup"><span data-stu-id="62bbd-141">Conversion in Visual Basic</span></span>  
 <span data-ttu-id="62bbd-142">`CType` Když použijete klíčová `CBool` slova pro převod nebo k převodu číselných `Boolean`datových typů na `False` hodnotu `True`, 0 se stane a zobrazí se všechny ostatní hodnoty.</span><span class="sxs-lookup"><span data-stu-id="62bbd-142">When you use the `CType` or `CBool` conversion keywords to convert numeric data types to `Boolean`, 0 becomes `False` and all other values become `True`.</span></span> <span data-ttu-id="62bbd-143">Při převodu `Boolean` hodnot na číselné typy pomocí `False` klíčových slov převodu se hodnota 0 a `True` nastanou na 1.</span><span class="sxs-lookup"><span data-stu-id="62bbd-143">When you convert `Boolean` values to numeric types by using the conversion keywords, `False` becomes 0 and `True` becomes -1.</span></span>  
  
### <a name="conversion-in-the-framework"></a><span data-ttu-id="62bbd-144">Převod v rozhraní .NET Framework</span><span class="sxs-lookup"><span data-stu-id="62bbd-144">Conversion in the Framework</span></span>  
 <span data-ttu-id="62bbd-145">Metoda třídy v `True` oboru názvů se převede na + 1. <xref:System> <xref:System.Convert> <xref:System.Convert.ToInt32%2A></span><span class="sxs-lookup"><span data-stu-id="62bbd-145">The <xref:System.Convert.ToInt32%2A> method of the <xref:System.Convert> class in the <xref:System> namespace converts `True` to +1.</span></span>  
  
 <span data-ttu-id="62bbd-146">Pokud je nutné převést `Boolean` hodnotu na číselný datový typ, buďte opatrní na to, kterou metodu převodu používáte.</span><span class="sxs-lookup"><span data-stu-id="62bbd-146">If you must convert a `Boolean` value to a numeric data type, be careful about which conversion method you use.</span></span>  
  
## <a name="character-literal-generates-compiler-error"></a><span data-ttu-id="62bbd-147">Znakový literál generuje chybu kompilátoru.</span><span class="sxs-lookup"><span data-stu-id="62bbd-147">Character Literal Generates Compiler Error</span></span>  
 <span data-ttu-id="62bbd-148">Při absenci žádných typů znaků Visual Basic předpokládá výchozí datové typy pro literály.</span><span class="sxs-lookup"><span data-stu-id="62bbd-148">In the absence of any type characters, Visual Basic assumes default data types for literals.</span></span> <span data-ttu-id="62bbd-149">Výchozí typ pro znakový literál – uzavřený v uvozovkách (`" "`) – je. `String`</span><span class="sxs-lookup"><span data-stu-id="62bbd-149">The default type for a character literal — enclosed in quotation marks (`" "`) — is `String`.</span></span>  
  
 <span data-ttu-id="62bbd-150">Datový typ nerozšiřuje [datový typ char.](../../../../visual-basic/language-reference/data-types/char-data-type.md) `String`</span><span class="sxs-lookup"><span data-stu-id="62bbd-150">The `String` data type does not widen to the [Char Data Type](../../../../visual-basic/language-reference/data-types/char-data-type.md).</span></span> <span data-ttu-id="62bbd-151">To znamená, že pokud chcete `Char` proměnné přiřadit literál, musíte buď provést zužující převod, nebo vynutit literál `Char` pro typ.</span><span class="sxs-lookup"><span data-stu-id="62bbd-151">This means that if you want to assign a literal to a `Char` variable, you must either make a narrowing conversion or force the literal to the `Char` type.</span></span>  

|<span data-ttu-id="62bbd-152">Vytvoření literálu znaku pro přiřazení proměnné nebo konstantě</span><span class="sxs-lookup"><span data-stu-id="62bbd-152">To create a Char literal to assign to a variable or constant</span></span>|
|---|  
|<span data-ttu-id="62bbd-153">1.  Deklarujte proměnnou nebo konstantu `Char`jako.</span><span class="sxs-lookup"><span data-stu-id="62bbd-153">1.  Declare the variable or constant as `Char`.</span></span><br /><span data-ttu-id="62bbd-154">2.  Uzavřete hodnotu znaku do uvozovek (`" "`).</span><span class="sxs-lookup"><span data-stu-id="62bbd-154">2.  Enclose the character value in quotation marks (`" "`).</span></span><br /><span data-ttu-id="62bbd-155">3.  Použijte uzavírací dvojitou uvozovku pomocí znaku `C` literálového typu k vynucení `Char`literálu.</span><span class="sxs-lookup"><span data-stu-id="62bbd-155">3.  Follow the closing double quotation mark with the literal type character `C` to force the literal to `Char`.</span></span> <span data-ttu-id="62bbd-156">To je nezbytné v případě, že přepínač pro kontrolu typu ([Option Strict](../../../../visual-basic/language-reference/statements/option-strict-statement.md)) je `On`a je v každém případě žádoucí.</span><span class="sxs-lookup"><span data-stu-id="62bbd-156">This is necessary if the type checking switch ([Option Strict Statement](../../../../visual-basic/language-reference/statements/option-strict-statement.md)) is `On`, and it is desirable in any case.</span></span>|  
  
 <span data-ttu-id="62bbd-157">Následující příklad ukazuje neúspěšná a úspěšná přiřazení literálu `Char` proměnné.</span><span class="sxs-lookup"><span data-stu-id="62bbd-157">The following example demonstrates both unsuccessful and successful assignments of a literal to a `Char` variable.</span></span>  
  
 [!code-vb[VbVbalrDataTypes#12](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrDataTypes/VB/Class1.vb#12)]  
  
 <span data-ttu-id="62bbd-158">Použití zužujících převodů vždy představuje riziko, protože v době běhu může selhat.</span><span class="sxs-lookup"><span data-stu-id="62bbd-158">There is always a risk in using narrowing conversions, because they can fail at run time.</span></span> <span data-ttu-id="62bbd-159">Například převod z `String` na na `Char` může selhat, pokud `String` hodnota obsahuje více než jeden znak.</span><span class="sxs-lookup"><span data-stu-id="62bbd-159">For example, a conversion from `String` to `Char` can fail if the `String` value contains more than one character.</span></span> <span data-ttu-id="62bbd-160">Proto je lepší programování pro použití `C` znaku typu.</span><span class="sxs-lookup"><span data-stu-id="62bbd-160">Therefore, it is better programming to use the `C` type character.</span></span>  
  
## <a name="string-conversion-fails-at-run-time"></a><span data-ttu-id="62bbd-161">Převod řetězce v době běhu se nezdařil.</span><span class="sxs-lookup"><span data-stu-id="62bbd-161">String Conversion Fails at Run Time</span></span>  
 <span data-ttu-id="62bbd-162">[Datový typ String](../../../../visual-basic/language-reference/data-types/string-data-type.md) se účastní několika rozšiřujících převodů.</span><span class="sxs-lookup"><span data-stu-id="62bbd-162">The [String Data Type](../../../../visual-basic/language-reference/data-types/string-data-type.md) participates in very few widening conversions.</span></span> <span data-ttu-id="62bbd-163">`String`rozšiřuje se pouze na sebe sama `Object`a `Char` `Char()` a a ( `Char` pole) rozšiřuje na `String`.</span><span class="sxs-lookup"><span data-stu-id="62bbd-163">`String` widens only to itself and `Object`, and only `Char` and `Char()` (a `Char` array) widen to `String`.</span></span> <span data-ttu-id="62bbd-164">Důvodem je `String` , že proměnné a konstanty mohou obsahovat hodnoty, které nemohou obsahovat jiné datové typy.</span><span class="sxs-lookup"><span data-stu-id="62bbd-164">This is because `String` variables and constants can contain values that other data types cannot contain.</span></span>  
  
 <span data-ttu-id="62bbd-165">Když je `On`přepínač pro kontrolu typu ([Option Strict](../../../../visual-basic/language-reference/statements/option-strict-statement.md)), kompilátor zakáže všechny implicitní zužující převody.</span><span class="sxs-lookup"><span data-stu-id="62bbd-165">When the type checking switch ([Option Strict Statement](../../../../visual-basic/language-reference/statements/option-strict-statement.md)) is `On`, the compiler disallows all implicit narrowing conversions.</span></span> <span data-ttu-id="62bbd-166">To zahrnuje i ty `String`, které se týkají.</span><span class="sxs-lookup"><span data-stu-id="62bbd-166">This includes those involving `String`.</span></span> <span data-ttu-id="62bbd-167">Váš kód může stále používat klíčová slova pro `CStr` převod, jako je [funkce a CType](../../../../visual-basic/language-reference/functions/ctype-function.md), které nasměrují .NET Framework k pokusu o převod.</span><span class="sxs-lookup"><span data-stu-id="62bbd-167">Your code can still use conversion keywords such as `CStr` and [CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md), which direct the .NET Framework to attempt the conversion.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="62bbd-168">Chyba zužujícího převodu je potlačena pro převody z prvků v `For Each…Next` kolekci na řídicí proměnnou smyčky.</span><span class="sxs-lookup"><span data-stu-id="62bbd-168">The narrowing-conversion error is suppressed for conversions from the elements in a `For Each…Next` collection to the loop control variable.</span></span> <span data-ttu-id="62bbd-169">Další informace a příklady najdete v části "zužující převody" v tématu.. [. Další příkaz](../../../../visual-basic/language-reference/statements/for-each-next-statement.md).</span><span class="sxs-lookup"><span data-stu-id="62bbd-169">For more information and examples, see the "Narrowing Conversions" section in [For Each...Next Statement](../../../../visual-basic/language-reference/statements/for-each-next-statement.md).</span></span>  
  
### <a name="narrowing-conversion-protection"></a><span data-ttu-id="62bbd-170">Zúžení ochrany před převodem</span><span class="sxs-lookup"><span data-stu-id="62bbd-170">Narrowing Conversion Protection</span></span>  
 <span data-ttu-id="62bbd-171">Nevýhodou zužujících převodů je, že v době běhu mohou selhat.</span><span class="sxs-lookup"><span data-stu-id="62bbd-171">The disadvantage of narrowing conversions is that they can fail at run time.</span></span> <span data-ttu-id="62bbd-172">Například pokud `String` proměnná obsahuje cokoli jiného než "true" nebo "false", nelze ji převést na `Boolean`.</span><span class="sxs-lookup"><span data-stu-id="62bbd-172">For example, if a `String` variable contains anything other than "True" or "False," it cannot be converted to `Boolean`.</span></span> <span data-ttu-id="62bbd-173">Pokud obsahuje interpunkční znaménka, převod na libovolný číselný typ se nezdařil.</span><span class="sxs-lookup"><span data-stu-id="62bbd-173">If it contains punctuation characters, conversion to any numeric type fails.</span></span> <span data-ttu-id="62bbd-174">Pokud si nejste jisti `String` , že proměnná vždy obsahuje hodnoty, které může cílový typ přijmout, neměli byste zkoušet převod.</span><span class="sxs-lookup"><span data-stu-id="62bbd-174">Unless you know that your `String` variable always holds values that the destination type can accept, you should not try a conversion.</span></span>  
  
 <span data-ttu-id="62bbd-175">Pokud je nutné převést z `String` na jiný datový typ, nejbezpečnější postup je uzavřít pokus o převod do [Try... Zachytit... Finally – příkaz](../../../../visual-basic/language-reference/statements/try-catch-finally-statement.md).</span><span class="sxs-lookup"><span data-stu-id="62bbd-175">If you must convert from `String` to another data type, the safest procedure is to enclose the attempted conversion in the [Try...Catch...Finally Statement](../../../../visual-basic/language-reference/statements/try-catch-finally-statement.md).</span></span> <span data-ttu-id="62bbd-176">To vám umožní pracovat s chybou za běhu.</span><span class="sxs-lookup"><span data-stu-id="62bbd-176">This lets you deal with a run-time failure.</span></span>  
  
### <a name="character-arrays"></a><span data-ttu-id="62bbd-177">Pole znaků</span><span class="sxs-lookup"><span data-stu-id="62bbd-177">Character Arrays</span></span>  
 <span data-ttu-id="62bbd-178">Jedno `Char` a `String`pole prvků se rozšíří na. `Char`</span><span class="sxs-lookup"><span data-stu-id="62bbd-178">A single `Char` and an array of `Char` elements both widen to `String`.</span></span> <span data-ttu-id="62bbd-179">Nerozšiřuje se však na `Char()`. `String`</span><span class="sxs-lookup"><span data-stu-id="62bbd-179">However, `String` does not widen to `Char()`.</span></span> <span data-ttu-id="62bbd-180">Chcete-li `String` převést hodnotu `Char` na pole <xref:System.String.ToCharArray%2A> , můžete <xref:System.String?displayProperty=nameWithType> použít metodu třídy.</span><span class="sxs-lookup"><span data-stu-id="62bbd-180">To convert a `String` value to a `Char` array, you can use the <xref:System.String.ToCharArray%2A> method of the <xref:System.String?displayProperty=nameWithType> class.</span></span>  
  
### <a name="meaningless-values"></a><span data-ttu-id="62bbd-181">Nevýznamové hodnoty</span><span class="sxs-lookup"><span data-stu-id="62bbd-181">Meaningless Values</span></span>  
 <span data-ttu-id="62bbd-182">Obecně platí, `String` že hodnoty nejsou smysluplné v jiných datových typech a převod je vysoce umělý a nebezpečný.</span><span class="sxs-lookup"><span data-stu-id="62bbd-182">In general, `String` values are not meaningful in other data types, and conversion is highly artificial and dangerous.</span></span> <span data-ttu-id="62bbd-183">Kdykoli je to možné, měli byste omezit `String` použití proměnných na sekvence znaků, pro které jsou navrženy.</span><span class="sxs-lookup"><span data-stu-id="62bbd-183">Whenever possible, you should restrict usage of `String` variables to the character sequences for which they are designed.</span></span> <span data-ttu-id="62bbd-184">Nikdy byste neměli psát kód, který závisí na ekvivalentních hodnotách v jiných typech.</span><span class="sxs-lookup"><span data-stu-id="62bbd-184">You should never write code that relies on equivalent values in other types.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="62bbd-185">Viz také:</span><span class="sxs-lookup"><span data-stu-id="62bbd-185">See also</span></span>

- [<span data-ttu-id="62bbd-186">Datové typy</span><span class="sxs-lookup"><span data-stu-id="62bbd-186">Data Types</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/index.md)
- [<span data-ttu-id="62bbd-187">Znaky typu</span><span class="sxs-lookup"><span data-stu-id="62bbd-187">Type Characters</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/type-characters.md)
- [<span data-ttu-id="62bbd-188">Typy hodnot a odkazové typy</span><span class="sxs-lookup"><span data-stu-id="62bbd-188">Value Types and Reference Types</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/value-types-and-reference-types.md)
- [<span data-ttu-id="62bbd-189">Převody typu v Visual Basic</span><span class="sxs-lookup"><span data-stu-id="62bbd-189">Type Conversions in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/type-conversions.md)
- [<span data-ttu-id="62bbd-190">Datové typy</span><span class="sxs-lookup"><span data-stu-id="62bbd-190">Data Types</span></span>](../../../../visual-basic/language-reference/data-types/index.md)
- [<span data-ttu-id="62bbd-191">Funkce pro převod typů</span><span class="sxs-lookup"><span data-stu-id="62bbd-191">Type Conversion Functions</span></span>](../../../../visual-basic/language-reference/functions/type-conversion-functions.md)
- [<span data-ttu-id="62bbd-192">Účinné používání datových typů</span><span class="sxs-lookup"><span data-stu-id="62bbd-192">Efficient Use of Data Types</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/efficient-use-of-data-types.md)
