---
title: Zpracování událostí
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: 29d878afbe3669fc88e62b1fec98b306918c303d
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/04/2020
ms.locfileid: "84405077"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="ec22e-102">Návod: Zpracování událostí (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="ec22e-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="ec22e-103">Jedná se o druhý ze dvou témat, která ukazují, jak pracovat s událostmi.</span><span class="sxs-lookup"><span data-stu-id="ec22e-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="ec22e-104">První téma, [Návod: deklarace a vyvolávání událostí](walkthrough-declaring-and-raising-events.md), ukazuje, jak deklarovat a vyvolat události.</span><span class="sxs-lookup"><span data-stu-id="ec22e-104">The first topic, [Walkthrough: Declaring and Raising Events](walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="ec22e-105">V této části se používá formulář a třída z tohoto návodu k zobrazení, jak zpracovávat události, když dojde k jejich provedení.</span><span class="sxs-lookup"><span data-stu-id="ec22e-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="ec22e-106">`Widget`Příklad třídy používá tradiční příkazy pro zpracování událostí.</span><span class="sxs-lookup"><span data-stu-id="ec22e-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="ec22e-107">Visual Basic poskytuje další techniky pro práci s událostmi.</span><span class="sxs-lookup"><span data-stu-id="ec22e-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="ec22e-108">Jako cvičení můžete tento příklad upravit tak, aby používal `AddHandler` `Handles` příkazy a.</span><span class="sxs-lookup"><span data-stu-id="ec22e-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="ec22e-109">Zpracování události PercentDone třídy widgetu</span><span class="sxs-lookup"><span data-stu-id="ec22e-109">To handle the PercentDone event of the Widget class</span></span>  
  
1. <span data-ttu-id="ec22e-110">Vložte následující kód do `Form1` :</span><span class="sxs-lookup"><span data-stu-id="ec22e-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#4)]  
  
     <span data-ttu-id="ec22e-111">`WithEvents`Klíčové slovo určuje, že proměnná `mWidget` slouží ke zpracování událostí objektu.</span><span class="sxs-lookup"><span data-stu-id="ec22e-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="ec22e-112">Typ objektu určíte zadáním názvu třídy, ze které bude objekt vytvořen.</span><span class="sxs-lookup"><span data-stu-id="ec22e-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="ec22e-113">Proměnná `mWidget` je deklarována v, `Form1` protože `WithEvents` proměnné musí být na úrovni třídy.</span><span class="sxs-lookup"><span data-stu-id="ec22e-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="ec22e-114">To platí bez ohledu na typ třídy, ve které je umístíte.</span><span class="sxs-lookup"><span data-stu-id="ec22e-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="ec22e-115">Proměnná `mblnCancel` slouží ke zrušení `LongTask` metody.</span><span class="sxs-lookup"><span data-stu-id="ec22e-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="ec22e-116">Psaní kódu pro zpracování události</span><span class="sxs-lookup"><span data-stu-id="ec22e-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="ec22e-117">Jakmile deklarujete proměnnou pomocí `WithEvents` , bude název proměnné zobrazen v levém rozevíracím seznamu **editoru kódu**třídy.</span><span class="sxs-lookup"><span data-stu-id="ec22e-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="ec22e-118">Když vyberete `mWidget` , zobrazí se `Widget` události třídy v pravém rozevíracím seznamu.</span><span class="sxs-lookup"><span data-stu-id="ec22e-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="ec22e-119">Výběrem události se zobrazí odpovídající procedura události s předponou `mWidget` a podtržítkem.</span><span class="sxs-lookup"><span data-stu-id="ec22e-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="ec22e-120">Všem procedurám události přidruženým k `WithEvents` proměnné je dán název proměnné jako předpona.</span><span class="sxs-lookup"><span data-stu-id="ec22e-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="ec22e-121">Postup zpracování události</span><span class="sxs-lookup"><span data-stu-id="ec22e-121">To handle an event</span></span>  
  
1. <span data-ttu-id="ec22e-122">`mWidget`V **editoru kódu**vyberte v rozevíracím seznamu vlevo.</span><span class="sxs-lookup"><span data-stu-id="ec22e-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="ec22e-123">`PercentDone`V rozevíracím seznamu vyberte událost.</span><span class="sxs-lookup"><span data-stu-id="ec22e-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="ec22e-124">**Editor kódu** otevře `mWidget_PercentDone` proceduru události.</span><span class="sxs-lookup"><span data-stu-id="ec22e-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="ec22e-125">**Editor kódu** je užitečný, ale není povinný pro vkládání nových obslužných rutin událostí.</span><span class="sxs-lookup"><span data-stu-id="ec22e-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="ec22e-126">V tomto návodu je jednodušší pouze kopírovat obslužné rutiny událostí přímo do kódu.</span><span class="sxs-lookup"><span data-stu-id="ec22e-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3. <span data-ttu-id="ec22e-127">Do `mWidget_PercentDone` obslužné rutiny události přidejte následující kód:</span><span class="sxs-lookup"><span data-stu-id="ec22e-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#5)]  
  
     <span data-ttu-id="ec22e-128">Při každém `PercentDone` vyvolání události zobrazuje procedura události v ovládacím prvku procentuální hodnotu dokončení `Label` .</span><span class="sxs-lookup"><span data-stu-id="ec22e-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="ec22e-129">`DoEvents`Metoda umožňuje popisku překreslit a také uživateli nabízí možnost kliknout na tlačítko **Storno** .</span><span class="sxs-lookup"><span data-stu-id="ec22e-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4. <span data-ttu-id="ec22e-130">Přidejte následující kód pro `Button2_Click` obslužnou rutinu události:</span><span class="sxs-lookup"><span data-stu-id="ec22e-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#6)]  
  
 <span data-ttu-id="ec22e-131">Pokud uživatel klikne na tlačítko **Storno** `LongTask` , když je spuštěn, `Button2_Click` událost se spustí, jakmile `DoEvents` příkaz umožní, aby bylo provedeno zpracování události.</span><span class="sxs-lookup"><span data-stu-id="ec22e-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="ec22e-132">Proměnná na úrovni třídy `mblnCancel` je nastavena na hodnotu `True` a `mWidget_PercentDone` událost pak otestuje a nastaví `ByRef Cancel` argument na `True` .</span><span class="sxs-lookup"><span data-stu-id="ec22e-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="ec22e-133">Připojení proměnné WithEvents k objektu</span><span class="sxs-lookup"><span data-stu-id="ec22e-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="ec22e-134">`Form1`je nyní nastaveno na zpracování `Widget` událostí objektu.</span><span class="sxs-lookup"><span data-stu-id="ec22e-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="ec22e-135">Vše, co zbývá, je najít `Widget` někde.</span><span class="sxs-lookup"><span data-stu-id="ec22e-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="ec22e-136">Pokud deklarujete proměnnou `WithEvents` v době návrhu, k ní není přidružen žádný objekt.</span><span class="sxs-lookup"><span data-stu-id="ec22e-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="ec22e-137">`WithEvents`Proměnná je stejně jako jakákoli jiná proměnná objektu.</span><span class="sxs-lookup"><span data-stu-id="ec22e-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="ec22e-138">Musíte vytvořit objekt a přiřadit k němu odkaz s `WithEvents` proměnnou.</span><span class="sxs-lookup"><span data-stu-id="ec22e-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="ec22e-139">Vytvoření objektu a přiřazení odkazu na něj</span><span class="sxs-lookup"><span data-stu-id="ec22e-139">To create an object and assign a reference to it</span></span>  
  
1. <span data-ttu-id="ec22e-140">Z levého rozevíracího seznamu v **editoru kódu**vyberte **(události Form1)** .</span><span class="sxs-lookup"><span data-stu-id="ec22e-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="ec22e-141">`Load`V rozevíracím seznamu vyberte událost.</span><span class="sxs-lookup"><span data-stu-id="ec22e-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="ec22e-142">**Editor kódu** otevře `Form1_Load` proceduru události.</span><span class="sxs-lookup"><span data-stu-id="ec22e-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3. <span data-ttu-id="ec22e-143">Přidejte následující kód pro `Form1_Load` proceduru události pro vytvoření `Widget` :</span><span class="sxs-lookup"><span data-stu-id="ec22e-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#7)]  
  
 <span data-ttu-id="ec22e-144">Když se spustí tento kód, Visual Basic vytvoří `Widget` objekt a připojí své události k procedurám události přidruženým k `mWidget` .</span><span class="sxs-lookup"><span data-stu-id="ec22e-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="ec22e-145">Od tohoto okamžiku, kdykoli `Widget` vyvolá `PercentDone` událost, `mWidget_PercentDone` je provedena procedura události.</span><span class="sxs-lookup"><span data-stu-id="ec22e-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="ec22e-146">Volání metody LongTask</span><span class="sxs-lookup"><span data-stu-id="ec22e-146">To call the LongTask method</span></span>  
  
- <span data-ttu-id="ec22e-147">Do `Button1_Click` obslužné rutiny události přidejte následující kód:</span><span class="sxs-lookup"><span data-stu-id="ec22e-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#8)]  
  
 <span data-ttu-id="ec22e-148">Před `LongTask` voláním metody musí být inicializován popisek, který zobrazuje procento dokončení, a příznak na úrovni třídy `Boolean` pro zrušení metody musí být nastaven na `False` .</span><span class="sxs-lookup"><span data-stu-id="ec22e-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="ec22e-149">`LongTask`je volána s dobou trvání úlohy 12,2 sekund.</span><span class="sxs-lookup"><span data-stu-id="ec22e-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="ec22e-150">`PercentDone`Událost se vyvolá jednou za každou třetinu sekundy.</span><span class="sxs-lookup"><span data-stu-id="ec22e-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="ec22e-151">Při každém vyvolání události se `mWidget_PercentDone` spustí procedura události.</span><span class="sxs-lookup"><span data-stu-id="ec22e-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="ec22e-152">V případě, že je `LongTask` provedena, `mblnCancel` je Testováno pro zjištění, zda `LongTask` skončila normálně nebo pokud bylo zastaveno, protože `mblnCancel` bylo nastaveno na `True` .</span><span class="sxs-lookup"><span data-stu-id="ec22e-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="ec22e-153">Procentuální hodnota dokončení je aktualizována pouze v bývalém případě.</span><span class="sxs-lookup"><span data-stu-id="ec22e-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="ec22e-154">Spuštění programu</span><span class="sxs-lookup"><span data-stu-id="ec22e-154">To run the program</span></span>  
  
1. <span data-ttu-id="ec22e-155">Stisknutím klávesy F5 vložte projekt do režimu spuštění.</span><span class="sxs-lookup"><span data-stu-id="ec22e-155">Press F5 to put the project in run mode.</span></span>  
  
2. <span data-ttu-id="ec22e-156">Klikněte na tlačítko **Spustit úlohu** .</span><span class="sxs-lookup"><span data-stu-id="ec22e-156">Click the **Start Task** button.</span></span> <span data-ttu-id="ec22e-157">Pokaždé `PercentDone` , když se událost vyvolá, popisek se aktualizuje o procento dokončené úlohy.</span><span class="sxs-lookup"><span data-stu-id="ec22e-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3. <span data-ttu-id="ec22e-158">Kliknutím na tlačítko **Storno** úlohu zastavte.</span><span class="sxs-lookup"><span data-stu-id="ec22e-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="ec22e-159">Všimněte si, že se vzhled tlačítka **Zrušit** nemění hned po kliknutí na něj.</span><span class="sxs-lookup"><span data-stu-id="ec22e-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="ec22e-160">`Click`Událost nemůže nastat, dokud `My.Application.DoEvents` příkaz nepovolí zpracování událostí.</span><span class="sxs-lookup"><span data-stu-id="ec22e-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="ec22e-161">`My.Application.DoEvents`Metoda nezpracovává události přesně stejným způsobem jako formulář.</span><span class="sxs-lookup"><span data-stu-id="ec22e-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="ec22e-162">Například v tomto návodu musíte dvakrát kliknout na tlačítko **Zrušit** .</span><span class="sxs-lookup"><span data-stu-id="ec22e-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="ec22e-163">Chcete-li, aby formulář mohl zpracovávat události přímo, můžete použít multithreading.</span><span class="sxs-lookup"><span data-stu-id="ec22e-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="ec22e-164">Další informace najdete v tématu [spravovaná vlákna](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="ec22e-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="ec22e-165">Může se stát, že vám dá pokyn spustit program pomocí klávesy F11 a krokovat kód po řádku.</span><span class="sxs-lookup"><span data-stu-id="ec22e-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="ec22e-166">Můžete zřetelně vidět, jak se spuštění spouští `LongTask` , a pak krátce znovu zadat `Form1` pokaždé, když `PercentDone` se událost vyvolá.</span><span class="sxs-lookup"><span data-stu-id="ec22e-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="ec22e-167">Co se stane, když při provádění bylo vráceno v kódu `Form1` , `LongTask` metoda byla volána znovu?</span><span class="sxs-lookup"><span data-stu-id="ec22e-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="ec22e-168">V nejhorším případě může dojít k přetečení zásobníku, pokud `LongTask` bylo voláno při každém vyvolání události.</span><span class="sxs-lookup"><span data-stu-id="ec22e-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="ec22e-169">Můžete způsobit, že proměnná `mWidget` zpracovává události pro jiný `Widget` objekt přiřazením odkazu k novému `Widget` `mWidget` .</span><span class="sxs-lookup"><span data-stu-id="ec22e-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="ec22e-170">V takovém případě můžete vytvořit kód `Button1_Click` pokaždé, když kliknete na tlačítko.</span><span class="sxs-lookup"><span data-stu-id="ec22e-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="ec22e-171">Zpracování událostí pro různé pomůcky</span><span class="sxs-lookup"><span data-stu-id="ec22e-171">To handle events for a different widget</span></span>  
  
- <span data-ttu-id="ec22e-172">Do procedury přidejte následující řádek kódu `Button1_Click` , bezprostředně před řádek, který čte `mWidget.LongTask(12.2, 0.33)` :</span><span class="sxs-lookup"><span data-stu-id="ec22e-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#9)]  
  
 <span data-ttu-id="ec22e-173">Výše uvedený kód vytvoří nové `Widget` pokaždé, když se klikne na tlačítko.</span><span class="sxs-lookup"><span data-stu-id="ec22e-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="ec22e-174">Jakmile se `LongTask` Metoda dokončí, odkaz na se `Widget` uvolní a dojde k jejímu `Widget` zničení.</span><span class="sxs-lookup"><span data-stu-id="ec22e-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="ec22e-175">`WithEvents`Proměnná může současně obsahovat pouze jeden odkaz na objekt, takže pokud přiřadíte jiný `Widget` objekt k `mWidget` , `Widget` události předchozího objektu již nebudou zpracovány.</span><span class="sxs-lookup"><span data-stu-id="ec22e-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="ec22e-176">Pokud `mWidget` je jedinou proměnnou objektu obsahující odkaz na starou `Widget` , je objekt zničen.</span><span class="sxs-lookup"><span data-stu-id="ec22e-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="ec22e-177">Pokud chcete zpracovávat události z několika `Widget` objektů, použijte `AddHandler` příkaz pro zpracování událostí z každého objektu samostatně.</span><span class="sxs-lookup"><span data-stu-id="ec22e-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="ec22e-178">Můžete deklarovat tolik `WithEvents` proměnných, kolik potřebujete, ale pole `WithEvents` proměnných nejsou podporována.</span><span class="sxs-lookup"><span data-stu-id="ec22e-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="ec22e-179">Viz také</span><span class="sxs-lookup"><span data-stu-id="ec22e-179">See also</span></span>

- [<span data-ttu-id="ec22e-180">Návod: Deklarace a vyvolávání událostí</span><span class="sxs-lookup"><span data-stu-id="ec22e-180">Walkthrough: Declaring and Raising Events</span></span>](walkthrough-declaring-and-raising-events.md)
- [<span data-ttu-id="ec22e-181">Události</span><span class="sxs-lookup"><span data-stu-id="ec22e-181">Events</span></span>](index.md)
