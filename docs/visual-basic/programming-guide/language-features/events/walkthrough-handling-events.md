---
title: Zpracování událostí (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: 2ae32f0ac31c504e86d5cf39ed6a36cc5523a4a0
ms.sourcegitcommit: 0be8a279af6d8a43e03141e349d3efd5d35f8767
ms.translationtype: HT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/18/2019
ms.locfileid: "59308559"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="77f1f-102">Návod: Zpracování událostí (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="77f1f-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="77f1f-103">Toto je druhá dvou tématech, které ukazují, jak pracovat s událostmi.</span><span class="sxs-lookup"><span data-stu-id="77f1f-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="77f1f-104">První téma [názorný postup: Deklarující a vyvolání události](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), ukazuje, jak deklarace a vyvolávání událostí.</span><span class="sxs-lookup"><span data-stu-id="77f1f-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="77f1f-105">Tato část používá formulář opravdu zavřít a třídy v tomto návodu k ukazují, jak zpracovávat události, když se provedou.</span><span class="sxs-lookup"><span data-stu-id="77f1f-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="77f1f-106">`Widget` Třídy příkladu tradiční příkazy pro zpracování událostí.</span><span class="sxs-lookup"><span data-stu-id="77f1f-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="77f1f-107">Visual Basic obsahuje jiné techniky pro práci s událostmi.</span><span class="sxs-lookup"><span data-stu-id="77f1f-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="77f1f-108">Jako cvičení, můžete upravit tento příkladu pro použití `AddHandler` a `Handles` příkazy.</span><span class="sxs-lookup"><span data-stu-id="77f1f-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="77f1f-109">Zpracování události PercentDone třídy widgetu</span><span class="sxs-lookup"><span data-stu-id="77f1f-109">To handle the PercentDone event of the Widget class</span></span>  
  
1. <span data-ttu-id="77f1f-110">Umístěte následující kód v `Form1`:</span><span class="sxs-lookup"><span data-stu-id="77f1f-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#4)]  
  
     <span data-ttu-id="77f1f-111">`WithEvents` – Klíčové slovo určuje, že proměnné `mWidget` se používá ke zpracování událostí objektu.</span><span class="sxs-lookup"><span data-stu-id="77f1f-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="77f1f-112">Zadejte typ objektu zadáním názvu třídy, ze kterého se vytvoří objekt.</span><span class="sxs-lookup"><span data-stu-id="77f1f-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="77f1f-113">Proměnná `mWidget` je deklarován v `Form1` protože `WithEvents` proměnné musí být na úrovni třídy.</span><span class="sxs-lookup"><span data-stu-id="77f1f-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="77f1f-114">To platí bez ohledu na typ třídy, které je v umístit.</span><span class="sxs-lookup"><span data-stu-id="77f1f-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="77f1f-115">Proměnná `mblnCancel` se používá pro zrušení `LongTask` metody.</span><span class="sxs-lookup"><span data-stu-id="77f1f-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="77f1f-116">Psaní kódu pro zpracování událostí</span><span class="sxs-lookup"><span data-stu-id="77f1f-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="77f1f-117">Poté, co můžete deklarovat proměnné pomocí `WithEvents`, název proměnné se zobrazí v levém rozevírací seznam třídy **Editor kódu**.</span><span class="sxs-lookup"><span data-stu-id="77f1f-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="77f1f-118">Když vyberete `mWidget`, `Widget` třídy události se zobrazí v pravém rozevíracího seznamu.</span><span class="sxs-lookup"><span data-stu-id="77f1f-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="77f1f-119">Výběr události zobrazí odpovídající postup událostí s předponou `mWidget` a podtržítka.</span><span class="sxs-lookup"><span data-stu-id="77f1f-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="77f1f-120">Všechny přidružené procedury `WithEvents` proměnné jsou předána název proměnné jako předponu.</span><span class="sxs-lookup"><span data-stu-id="77f1f-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="77f1f-121">Zpracování události</span><span class="sxs-lookup"><span data-stu-id="77f1f-121">To handle an event</span></span>  
  
1. <span data-ttu-id="77f1f-122">Vyberte `mWidget` z levé rozevíracího seznamu v **Editor kódu**.</span><span class="sxs-lookup"><span data-stu-id="77f1f-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="77f1f-123">Vyberte `PercentDone` událost z přímo rozevíracího seznamu.</span><span class="sxs-lookup"><span data-stu-id="77f1f-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="77f1f-124">**Editor kódu** otevře `mWidget_PercentDone` proceduru události.</span><span class="sxs-lookup"><span data-stu-id="77f1f-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="77f1f-125">**Editor kódu** je užitečné, ale nejsou vyžadovány, pro vkládání nové obslužné rutiny událostí.</span><span class="sxs-lookup"><span data-stu-id="77f1f-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="77f1f-126">V tomto návodu je přímější stačí jen zkopírovat přímo do kódu obslužné rutiny událostí.</span><span class="sxs-lookup"><span data-stu-id="77f1f-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3. <span data-ttu-id="77f1f-127">Přidejte následující kód, který `mWidget_PercentDone` obslužné rutiny události:</span><span class="sxs-lookup"><span data-stu-id="77f1f-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#5)]  
  
     <span data-ttu-id="77f1f-128">Pokaždé, když `PercentDone` událost se vyvolá, zobrazí se postup události dokončení v procentech `Label` ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="77f1f-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="77f1f-129">`DoEvents` Metoda umožňuje tento popisek repaint a klikněte na možnost také umožňuje uživateli **zrušit** tlačítko.</span><span class="sxs-lookup"><span data-stu-id="77f1f-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4. <span data-ttu-id="77f1f-130">Přidejte následující kód `Button2_Click` obslužné rutiny události:</span><span class="sxs-lookup"><span data-stu-id="77f1f-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#6)]  
  
 <span data-ttu-id="77f1f-131">Pokud uživatel klikne **zrušit** tlačítka při `LongTask` běží, `Button2_Click` provedla událost poté, co `DoEvents` příkaz umožňuje zpracování událostí na výskyt.</span><span class="sxs-lookup"><span data-stu-id="77f1f-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="77f1f-132">Proměnnou na úrovni `mblnCancel` je nastavena na `True`a `mWidget_PercentDone` události poté jej ověří a nastaví `ByRef Cancel` argument `True`.</span><span class="sxs-lookup"><span data-stu-id="77f1f-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="77f1f-133">Připojování k objektu proměnnou WithEvents</span><span class="sxs-lookup"><span data-stu-id="77f1f-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="77f1f-134">`Form1` je nyní nastaveno pro zpracování `Widget` objektu události.</span><span class="sxs-lookup"><span data-stu-id="77f1f-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="77f1f-135">Už jen zbývá k vyhledání `Widget` jinde.</span><span class="sxs-lookup"><span data-stu-id="77f1f-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="77f1f-136">Pokud deklarujete proměnnou `WithEvents` v době návrhu, je k ní přidružen žádný objekt.</span><span class="sxs-lookup"><span data-stu-id="77f1f-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="77f1f-137">A `WithEvents` proměnnou je stejně jako jakoukoli jinou proměnnou objektu.</span><span class="sxs-lookup"><span data-stu-id="77f1f-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="77f1f-138">Je nutné vytvořit objekt a přiřaďte přes odkaz `WithEvents` proměnné.</span><span class="sxs-lookup"><span data-stu-id="77f1f-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="77f1f-139">K vytvoření objektu a přiřazení na ni odkaz</span><span class="sxs-lookup"><span data-stu-id="77f1f-139">To create an object and assign a reference to it</span></span>  
  
1. <span data-ttu-id="77f1f-140">Vyberte **(Form1 události)** z levé rozevíracího seznamu v **Editor kódu**.</span><span class="sxs-lookup"><span data-stu-id="77f1f-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="77f1f-141">Vyberte `Load` událost z přímo rozevíracího seznamu.</span><span class="sxs-lookup"><span data-stu-id="77f1f-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="77f1f-142">**Editor kódu** otevře `Form1_Load` proceduru události.</span><span class="sxs-lookup"><span data-stu-id="77f1f-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3. <span data-ttu-id="77f1f-143">Přidejte následující kód `Form1_Load` události postupu vytvořte `Widget`:</span><span class="sxs-lookup"><span data-stu-id="77f1f-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#7)]  
  
 <span data-ttu-id="77f1f-144">Když tento kód spustí, Visual Basic vytvoří `Widget` objektu a jeho události se připojí k přidružené procedury `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="77f1f-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="77f1f-145">Od této chvíle, vždy, když `Widget` vyvolá jeho `PercentDone` událostí, `mWidget_PercentDone` procedury události.</span><span class="sxs-lookup"><span data-stu-id="77f1f-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="77f1f-146">Volání metody LongTask</span><span class="sxs-lookup"><span data-stu-id="77f1f-146">To call the LongTask method</span></span>  
  
-   <span data-ttu-id="77f1f-147">Přidejte následující kód, který `Button1_Click` obslužné rutiny události:</span><span class="sxs-lookup"><span data-stu-id="77f1f-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#8)]  
  
 <span data-ttu-id="77f1f-148">Před `LongTask` metoda je volána, označení, že zobrazuje procento dokončení musí být inicializován a úrovni třídy `Boolean` příznak pro zrušení metoda musí být nastaveno na `False`.</span><span class="sxs-lookup"><span data-stu-id="77f1f-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="77f1f-149">`LongTask` je volána s určitou dobou trvání úkolu 12.2 sekund.</span><span class="sxs-lookup"><span data-stu-id="77f1f-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="77f1f-150">`PercentDone` Událost je aktivována jednou každých 1 / 3 sekundy.</span><span class="sxs-lookup"><span data-stu-id="77f1f-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="77f1f-151">Pokaždé, když se vyvolá událost, `mWidget_PercentDone` procedury události.</span><span class="sxs-lookup"><span data-stu-id="77f1f-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="77f1f-152">Když `LongTask` se provádí, `mblnCancel` je testován a zjistěte, jestli `LongTask` skončil normálně, nebo pokud je zastavena, protože `mblnCancel` byl nastaven na `True`.</span><span class="sxs-lookup"><span data-stu-id="77f1f-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="77f1f-153">Procento dokončení se aktualizuje jenom v prvním případě.</span><span class="sxs-lookup"><span data-stu-id="77f1f-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="77f1f-154">Chcete-li spustit program</span><span class="sxs-lookup"><span data-stu-id="77f1f-154">To run the program</span></span>  
  
1. <span data-ttu-id="77f1f-155">Stisknutím klávesy F5 projekt uvést do režimu běhu.</span><span class="sxs-lookup"><span data-stu-id="77f1f-155">Press F5 to put the project in run mode.</span></span>  
  
2. <span data-ttu-id="77f1f-156">Klikněte na tlačítko **spustit úlohu** tlačítko.</span><span class="sxs-lookup"><span data-stu-id="77f1f-156">Click the **Start Task** button.</span></span> <span data-ttu-id="77f1f-157">Pokaždé, když `PercentDone` událost se vyvolá, popisek se aktualizuje s procento dokončení úkolu.</span><span class="sxs-lookup"><span data-stu-id="77f1f-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3. <span data-ttu-id="77f1f-158">Klikněte na tlačítko **zrušit** tlačítko Zastavit úlohy.</span><span class="sxs-lookup"><span data-stu-id="77f1f-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="77f1f-159">Všimněte si, že vzhled **zrušit** ihned po kliknutí nezmění tlačítko.</span><span class="sxs-lookup"><span data-stu-id="77f1f-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="77f1f-160">`Click` Událostí nemůže dojít až `My.Application.DoEvents` příkaz umožňuje zpracování událostí.</span><span class="sxs-lookup"><span data-stu-id="77f1f-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="77f1f-161">`My.Application.DoEvents` Metoda nezpracovává události stejným způsobem, stejně jako formulář.</span><span class="sxs-lookup"><span data-stu-id="77f1f-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="77f1f-162">Například v tomto podrobném návodu, musíte kliknout na **zrušit** dvakrát na tlačítko.</span><span class="sxs-lookup"><span data-stu-id="77f1f-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="77f1f-163">Povolit formulář pro zpracování událostí přímo, můžete použít multithreadingu.</span><span class="sxs-lookup"><span data-stu-id="77f1f-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="77f1f-164">Další informace najdete v tématu [dělení na spravovaná vlákna](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="77f1f-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="77f1f-165">Může být pro vás poučné spusťte program s F11 a krokovat kód řádku najednou.</span><span class="sxs-lookup"><span data-stu-id="77f1f-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="77f1f-166">Jasně vidíte, jak provádění zadá `LongTask`a potom stručně znovu zadá `Form1` pokaždé, když `PercentDone` událost se vyvolá.</span><span class="sxs-lookup"><span data-stu-id="77f1f-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="77f1f-167">Co by se stalo if, při spuštění zpět do kódu `Form1`, `LongTask` byla znovu volána metoda?</span><span class="sxs-lookup"><span data-stu-id="77f1f-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="77f1f-168">V nejhorším případě může dojít k přetečení zásobníku, pokud `LongTask` byly volány pokaždé, když byla vyvolána událost.</span><span class="sxs-lookup"><span data-stu-id="77f1f-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="77f1f-169">Proměnná může způsobit `mWidget` zpracování událostí pro jiný `Widget` objektu tak, že přiřadíte odkaz na nové `Widget` k `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="77f1f-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="77f1f-170">Ve skutečnosti můžete vytvořit kód v `Button1_Click` to pokaždé, když kliknete na tlačítko.</span><span class="sxs-lookup"><span data-stu-id="77f1f-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="77f1f-171">Zpracování událostí pro různé pomůcky</span><span class="sxs-lookup"><span data-stu-id="77f1f-171">To handle events for a different widget</span></span>  
  
-   <span data-ttu-id="77f1f-172">Přidejte následující řádek kódu, který `Button1_Click` postupu bezprostředně předchází řádku, který čte `mWidget.LongTask(12.2, 0.33)`:</span><span class="sxs-lookup"><span data-stu-id="77f1f-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#9)]  
  
 <span data-ttu-id="77f1f-173">Výše uvedený kód vytvoří novou `Widget` pokaždé, když dojde ke kliknutí na tlačítko.</span><span class="sxs-lookup"><span data-stu-id="77f1f-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="77f1f-174">Poté, co `LongTask` metoda dokončí, odkaz na `Widget` vydání a `Widget` zničen.</span><span class="sxs-lookup"><span data-stu-id="77f1f-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="77f1f-175">A `WithEvents` proměnná může obsahovat pouze jeden objekt odkazu v době, takže pokud přiřaďte jiný `Widget` objektu `mWidget`, předchozí `Widget` události objektu už se zpracuje.</span><span class="sxs-lookup"><span data-stu-id="77f1f-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="77f1f-176">Pokud `mWidget` je pouze objekt proměnnou obsahující odkaz na původní `Widget`, objekt je zničen.</span><span class="sxs-lookup"><span data-stu-id="77f1f-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="77f1f-177">Pokud chcete zpracovávat události z několika `Widget` objekty, použijte `AddHandler` příkaz pro zpracování událostí z každý objekt odděleně.</span><span class="sxs-lookup"><span data-stu-id="77f1f-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="77f1f-178">Lze deklarovat libovolný počet `WithEvents` proměnné, jako je třeba, ale pole `WithEvents` proměnné nejsou podporovány.</span><span class="sxs-lookup"><span data-stu-id="77f1f-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="77f1f-179">Viz také:</span><span class="sxs-lookup"><span data-stu-id="77f1f-179">See also</span></span>

- [<span data-ttu-id="77f1f-180">Návod: Deklarace a vyvolávání událostí</span><span class="sxs-lookup"><span data-stu-id="77f1f-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)
- [<span data-ttu-id="77f1f-181">Události</span><span class="sxs-lookup"><span data-stu-id="77f1f-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
