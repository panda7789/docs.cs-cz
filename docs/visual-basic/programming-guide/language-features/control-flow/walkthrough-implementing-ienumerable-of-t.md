---
title: Implementace iEnumerable
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: 4151a680050f234d450d8de5e67a767c54e8df68
ms.sourcegitcommit: 43d10ef65f0f1fd6c3b515e363bde11a3fcd8d6d
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/04/2020
ms.locfileid: "78266908"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="aa34f-102">Návod: Implementace IEnumerable(Of T) v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="aa34f-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="aa34f-103">Rozhraní <xref:System.Collections.Generic.IEnumerable%601> je implementováno třídami, které mohou vracet posloupnost hodnot po jedné položce.</span><span class="sxs-lookup"><span data-stu-id="aa34f-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="aa34f-104">Výhodou vrácení dat po jedné položce je, že není nutné načítat úplnou sadu dat do paměti, abyste s ní mohli pracovat.</span><span class="sxs-lookup"><span data-stu-id="aa34f-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="aa34f-105">Stačí použít dostatek paměti k načtení jedné položky z dat.</span><span class="sxs-lookup"><span data-stu-id="aa34f-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="aa34f-106">Třídy, `IEnumerable(T)` které implementují `For Each` rozhraní lze použít s smyčky nebo LINQ dotazy.</span><span class="sxs-lookup"><span data-stu-id="aa34f-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="aa34f-107">Zvažte například aplikaci, která musí číst velký textový soubor a vrátit každý řádek ze souboru, který odpovídá konkrétním kritériím hledání.</span><span class="sxs-lookup"><span data-stu-id="aa34f-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="aa34f-108">Aplikace používá dotaz LINQ k vrácení řádků ze souboru, které odpovídají zadaným kritériím.</span><span class="sxs-lookup"><span data-stu-id="aa34f-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="aa34f-109">Chcete-li dotaz na obsah souboru pomocí dotazu LINQ, aplikace může načíst obsah souboru do pole nebo kolekce.</span><span class="sxs-lookup"><span data-stu-id="aa34f-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="aa34f-110">Načítání celého souboru do pole nebo kolekce by však spotřebovávat mnohem více paměti, než je požadováno.</span><span class="sxs-lookup"><span data-stu-id="aa34f-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="aa34f-111">Dotaz LINQ by mohl místo toho dotaz ovat obsah souboru pomocí enumerovatelné třídy a vrátit pouze hodnoty, které odpovídají kritériím hledání.</span><span class="sxs-lookup"><span data-stu-id="aa34f-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="aa34f-112">Dotazy, které vrátí pouze několik odpovídajících hodnot, by spotřebovávaly mnohem méně paměti.</span><span class="sxs-lookup"><span data-stu-id="aa34f-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="aa34f-113">Můžete vytvořit třídu, která <xref:System.Collections.Generic.IEnumerable%601> implementuje rozhraní vystavit zdrojová data jako výčet dat.</span><span class="sxs-lookup"><span data-stu-id="aa34f-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="aa34f-114">Vaše třída, která `IEnumerable(T)` implementuje rozhraní bude <xref:System.Collections.Generic.IEnumerator%601> vyžadovat jinou třídu, která implementuje rozhraní iterát prostřednictvím zdrojových dat.</span><span class="sxs-lookup"><span data-stu-id="aa34f-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="aa34f-115">Tyto dvě třídy umožňují vrátit položky dat postupně jako určitý typ.</span><span class="sxs-lookup"><span data-stu-id="aa34f-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="aa34f-116">V tomto návodu vytvoříte třídu, `IEnumerable(Of String)` která implementuje rozhraní `IEnumerator(Of String)` a třídu, která implementuje rozhraní pro čtení textového souboru po jednom řádku.</span><span class="sxs-lookup"><span data-stu-id="aa34f-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="aa34f-117">Vytvoření třídy výčtu</span><span class="sxs-lookup"><span data-stu-id="aa34f-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="aa34f-118">**Vytvoření projektu výčtu třídy**</span><span class="sxs-lookup"><span data-stu-id="aa34f-118">**Create the enumerable class project**</span></span>

1. <span data-ttu-id="aa34f-119">V jazyce Visual Basic v nabídce **Soubor** přejděte na **Nový** a potom klepněte na **položku Project**.</span><span class="sxs-lookup"><span data-stu-id="aa34f-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1. <span data-ttu-id="aa34f-120">V dialogovém okně **Nový projekt** zkontrolujte v podokně **Typy projektů,** zda je vybrán **systém Windows.**</span><span class="sxs-lookup"><span data-stu-id="aa34f-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="aa34f-121">V podokně **Šablony** vyberte **Knihovna tříd.**</span><span class="sxs-lookup"><span data-stu-id="aa34f-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="aa34f-122">Do pole **Název** `StreamReaderEnumerable`zadejte a klepněte na tlačítko **OK**.</span><span class="sxs-lookup"><span data-stu-id="aa34f-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="aa34f-123">Zobrazí se nový projekt.</span><span class="sxs-lookup"><span data-stu-id="aa34f-123">The new project is displayed.</span></span>

1. <span data-ttu-id="aa34f-124">V **Průzkumníku řešení**klepněte pravým tlačítkem myši na soubor Class1.vb a klepněte na příkaz **Přejmenovat**.</span><span class="sxs-lookup"><span data-stu-id="aa34f-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="aa34f-125">Přejmenujte soubor `StreamReaderEnumerable.vb` na klávesu ENTER a stiskněte klávesu ENTER.</span><span class="sxs-lookup"><span data-stu-id="aa34f-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="aa34f-126">Přejmenování souboru bude také přejmenovat třídu na `StreamReaderEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="aa34f-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="aa34f-127">Tato třída bude `IEnumerable(Of String)` implementovat rozhraní.</span><span class="sxs-lookup"><span data-stu-id="aa34f-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1. <span data-ttu-id="aa34f-128">Klepněte pravým tlačítkem myši na projekt StreamReaderEnumerable, přejděte na **Přidat**a potom klepněte na příkaz **Nová položka**.</span><span class="sxs-lookup"><span data-stu-id="aa34f-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="aa34f-129">Vyberte šablonu **třídy.**</span><span class="sxs-lookup"><span data-stu-id="aa34f-129">Select the **Class** template.</span></span> <span data-ttu-id="aa34f-130">Do pole **Název** `StreamReaderEnumerator.vb` zadejte a klepněte na **tlačítko OK**.</span><span class="sxs-lookup"><span data-stu-id="aa34f-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="aa34f-131">První třída v tomto projektu je výčet třídy a bude implementovat `IEnumerable(Of String)` rozhraní.</span><span class="sxs-lookup"><span data-stu-id="aa34f-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="aa34f-132">Toto obecné rozhraní <xref:System.Collections.IEnumerable> implementuje rozhraní a zaručuje, že spotřebitelé `String`této třídy mohou přistupovat k hodnotám zadaným jako .</span><span class="sxs-lookup"><span data-stu-id="aa34f-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="aa34f-133">**Přidejte kód k implementaci iEnumerable**</span><span class="sxs-lookup"><span data-stu-id="aa34f-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="aa34f-134">Otevřete soubor StreamReaderEnumerable.vb.</span><span class="sxs-lookup"><span data-stu-id="aa34f-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="aa34f-135">Na řádku `Public Class StreamReaderEnumerable`za , zadejte následující a stiskněte klávesu ENTER.</span><span class="sxs-lookup"><span data-stu-id="aa34f-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#1)]

   <span data-ttu-id="aa34f-136">Visual Basic automaticky naplní třídu členy, `IEnumerable(Of String)` které jsou vyžadovány rozhraním.</span><span class="sxs-lookup"><span data-stu-id="aa34f-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="aa34f-137">Tato třída s početitelnými bude číst řádky z textového souboru po jednom řádku.</span><span class="sxs-lookup"><span data-stu-id="aa34f-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="aa34f-138">Přidejte následující kód do třídy vystavit veřejný konstruktor, který bere cestu k souboru jako vstupní parametr.</span><span class="sxs-lookup"><span data-stu-id="aa34f-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#2)]

4. <span data-ttu-id="aa34f-139">Implementace <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> metody `IEnumerable(Of String)` rozhraní vrátí novou instanci třídy. `StreamReaderEnumerator`</span><span class="sxs-lookup"><span data-stu-id="aa34f-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="aa34f-140">Implementace `GetEnumerator` metody `IEnumerable` rozhraní může být provedena `Private`, protože je třeba vystavit `IEnumerable(Of String)` pouze členy rozhraní.</span><span class="sxs-lookup"><span data-stu-id="aa34f-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="aa34f-141">Nahraďte kód, který jazyk `GetEnumerator` Visual Basic vygeneroval pro metody následujícím kódem.</span><span class="sxs-lookup"><span data-stu-id="aa34f-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#3)]  
  
<span data-ttu-id="aa34f-142">**Přidejte kód k implementaci nástroje IEnumerator**</span><span class="sxs-lookup"><span data-stu-id="aa34f-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="aa34f-143">Otevřete soubor StreamReaderEnumerator.vb.</span><span class="sxs-lookup"><span data-stu-id="aa34f-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="aa34f-144">Na řádku `Public Class StreamReaderEnumerator`za , zadejte následující a stiskněte klávesu ENTER.</span><span class="sxs-lookup"><span data-stu-id="aa34f-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#4)]

   <span data-ttu-id="aa34f-145">Visual Basic automaticky naplní třídu členy, `IEnumerator(Of String)` které jsou vyžadovány rozhraním.</span><span class="sxs-lookup"><span data-stu-id="aa34f-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="aa34f-146">Třída čítače otevře textový soubor a provede vstupně-va souboru pro čtení řádků ze souboru.</span><span class="sxs-lookup"><span data-stu-id="aa34f-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="aa34f-147">Přidejte následující kód do třídy vystavit veřejný konstruktor, který trvá cestu k souboru jako vstupní parametr a otevřete textový soubor pro čtení.</span><span class="sxs-lookup"><span data-stu-id="aa34f-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#5)]

4. <span data-ttu-id="aa34f-148">Vlastnosti `Current` rozhraní `IEnumerator(Of String)` a `IEnumerator` rozhraní vrátí aktuální položku z `String`textového souboru jako .</span><span class="sxs-lookup"><span data-stu-id="aa34f-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="aa34f-149">Implementace `Current` vlastnostrozhraní `IEnumerator` lze provést `Private`, protože je třeba vystavit pouze `IEnumerator(Of String)` členy rozhraní.</span><span class="sxs-lookup"><span data-stu-id="aa34f-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="aa34f-150">Nahraďte kód, který jazyk `Current` Visual Basic vygeneroval pro vlastnosti následujícím kódem.</span><span class="sxs-lookup"><span data-stu-id="aa34f-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#6)]

5. <span data-ttu-id="aa34f-151">Metoda `MoveNext` `IEnumerator` rozhraní přejde na další položku v textovém souboru a `Current` aktualizuje hodnotu, která je vrácena vlastností.</span><span class="sxs-lookup"><span data-stu-id="aa34f-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="aa34f-152">Pokud nejsou žádné další položky `MoveNext` ke `False`čtení, metoda vrátí ; jinak `MoveNext` metoda `True`vrátí .</span><span class="sxs-lookup"><span data-stu-id="aa34f-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="aa34f-153">Do metody `MoveNext` přidejte následující kód.</span><span class="sxs-lookup"><span data-stu-id="aa34f-153">Add the following code to the `MoveNext` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#7)]

6. <span data-ttu-id="aa34f-154">Metoda `Reset` `IEnumerator` rozhraní přesměruje iterátor tak, aby ukazoval na začátek textového souboru a vymaže aktuální hodnotu položky.</span><span class="sxs-lookup"><span data-stu-id="aa34f-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="aa34f-155">Do metody `Reset` přidejte následující kód.</span><span class="sxs-lookup"><span data-stu-id="aa34f-155">Add the following code to the `Reset` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#8)]

7. <span data-ttu-id="aa34f-156">Metoda `Dispose` `IEnumerator` rozhraní zaručuje, že všechny nespravované prostředky jsou uvolněny před zničením iterátoru.</span><span class="sxs-lookup"><span data-stu-id="aa34f-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="aa34f-157">Popisovač souboru, který `StreamReader` je používán objektem je nespravovaný prostředek a musí být uzavřen před zničením instance iterátoru.</span><span class="sxs-lookup"><span data-stu-id="aa34f-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="aa34f-158">Nahraďte kód, který pro `Dispose` metodu vygeneroval jazyk Visual Basic, následujícím kódem.</span><span class="sxs-lookup"><span data-stu-id="aa34f-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#9)]
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="aa34f-159">Použití ukázkového iterátoru</span><span class="sxs-lookup"><span data-stu-id="aa34f-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="aa34f-160">Můžete použít znakovou třídu v kódu spolu s řídicí struktury, `IEnumerable`které vyžadují `For Next` objekt, který implementuje , jako je například smyčka nebo linq dotaz.</span><span class="sxs-lookup"><span data-stu-id="aa34f-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="aa34f-161">Následující příklad ukazuje `StreamReaderEnumerable` v linq dotazu.</span><span class="sxs-lookup"><span data-stu-id="aa34f-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/Module1.vb#10)]  
  
## <a name="see-also"></a><span data-ttu-id="aa34f-162">Viz také</span><span class="sxs-lookup"><span data-stu-id="aa34f-162">See also</span></span>

- [<span data-ttu-id="aa34f-163">Představení technologie LINQ v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="aa34f-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)
- [<span data-ttu-id="aa34f-164">Tok řízení</span><span class="sxs-lookup"><span data-stu-id="aa34f-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)
- [<span data-ttu-id="aa34f-165">Struktury smyčky</span><span class="sxs-lookup"><span data-stu-id="aa34f-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)
- [<span data-ttu-id="aa34f-166">Příkaz For Each...Next</span><span class="sxs-lookup"><span data-stu-id="aa34f-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
