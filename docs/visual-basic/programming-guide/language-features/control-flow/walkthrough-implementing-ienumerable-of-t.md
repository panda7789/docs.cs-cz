---
title: Implementace rozhraní IEnumerable
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: 582957c91eac63cf7f72dd2f6c0cf40e627be686
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/04/2020
ms.locfileid: "84402028"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="28eb6-102">Návod: Implementace IEnumerable(Of T) v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="28eb6-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="28eb6-103"><xref:System.Collections.Generic.IEnumerable%601>Rozhraní je implementováno třídami, které mohou vracet sekvenci hodnot po jednotlivých položkách najednou.</span><span class="sxs-lookup"><span data-stu-id="28eb6-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="28eb6-104">Výhodou vrácení dat jedné položky v čase je, že není nutné načíst úplnou sadu dat do paměti, abyste s ní mohli pracovat.</span><span class="sxs-lookup"><span data-stu-id="28eb6-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="28eb6-105">K načtení jedné položky z dat stačí použít dostatek paměti.</span><span class="sxs-lookup"><span data-stu-id="28eb6-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="28eb6-106">Třídy, které implementují `IEnumerable(T)` rozhraní, lze použít s `For Each` cykly nebo dotazy LINQ.</span><span class="sxs-lookup"><span data-stu-id="28eb6-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="28eb6-107">Představte si například aplikaci, která musí číst velký textový soubor a vrátí každý řádek ze souboru, který odpovídá konkrétním kritériím hledání.</span><span class="sxs-lookup"><span data-stu-id="28eb6-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="28eb6-108">Aplikace používá dotaz LINQ k vrácení řádků ze souboru, které odpovídají zadaným kritériím.</span><span class="sxs-lookup"><span data-stu-id="28eb6-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="28eb6-109">K dotazování obsahu souboru pomocí dotazu LINQ může aplikace načíst obsah souboru do pole nebo kolekce.</span><span class="sxs-lookup"><span data-stu-id="28eb6-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="28eb6-110">Načtení celého souboru do pole nebo kolekce by však využívalo mnohem více paměti, než je vyžadováno.</span><span class="sxs-lookup"><span data-stu-id="28eb6-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="28eb6-111">Dotaz LINQ může místo toho zadat dotaz na obsah souboru pomocí vyčíslitelné třídy a vracet pouze hodnoty, které odpovídají kritériím vyhledávání.</span><span class="sxs-lookup"><span data-stu-id="28eb6-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="28eb6-112">Dotazy, které vracejí pouze pár hodnot, by využívaly mnohem méně paměti.</span><span class="sxs-lookup"><span data-stu-id="28eb6-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="28eb6-113">Můžete vytvořit třídu, která implementuje <xref:System.Collections.Generic.IEnumerable%601> rozhraní k vystavení zdrojových dat jako vyčíslitelného data.</span><span class="sxs-lookup"><span data-stu-id="28eb6-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="28eb6-114">Vaše třída, která implementuje `IEnumerable(T)` rozhraní, bude vyžadovat další třídu, která implementuje <xref:System.Collections.Generic.IEnumerator%601> rozhraní pro iteraci skrze zdrojová data.</span><span class="sxs-lookup"><span data-stu-id="28eb6-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="28eb6-115">Tyto dvě třídy umožňují vracet položky dat sekvenčně jako konkrétní typ.</span><span class="sxs-lookup"><span data-stu-id="28eb6-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="28eb6-116">V tomto návodu vytvoříte třídu, která implementuje `IEnumerable(Of String)` rozhraní a třídu, která implementuje `IEnumerator(Of String)` rozhraní pro čtení textového souboru v jednom řádku.</span><span class="sxs-lookup"><span data-stu-id="28eb6-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="28eb6-117">Vytváření vyčíslitelné třídy</span><span class="sxs-lookup"><span data-stu-id="28eb6-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="28eb6-118">**Vytvoření vyčíslitelného projektu třídy**</span><span class="sxs-lookup"><span data-stu-id="28eb6-118">**Create the enumerable class project**</span></span>

1. <span data-ttu-id="28eb6-119">V Visual Basic v nabídce **soubor** přejděte na příkaz **Nový** a potom klikněte na **projekt**.</span><span class="sxs-lookup"><span data-stu-id="28eb6-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1. <span data-ttu-id="28eb6-120">V dialogovém okně **Nový projekt** v podokně **typy projektů** se ujistěte, že je vybrána možnost **Windows** .</span><span class="sxs-lookup"><span data-stu-id="28eb6-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="28eb6-121">V podokně **šablony** vyberte **Knihovna tříd** .</span><span class="sxs-lookup"><span data-stu-id="28eb6-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="28eb6-122">Do pole **název** zadejte `StreamReaderEnumerable` a pak klikněte na **OK**.</span><span class="sxs-lookup"><span data-stu-id="28eb6-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="28eb6-123">Zobrazí se nový projekt.</span><span class="sxs-lookup"><span data-stu-id="28eb6-123">The new project is displayed.</span></span>

1. <span data-ttu-id="28eb6-124">V **Průzkumník řešení**klikněte pravým tlačítkem na soubor Class1. vb a klikněte na **Přejmenovat**.</span><span class="sxs-lookup"><span data-stu-id="28eb6-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="28eb6-125">Přejmenujte soubor na `StreamReaderEnumerable.vb` a stiskněte klávesu ENTER.</span><span class="sxs-lookup"><span data-stu-id="28eb6-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="28eb6-126">Přejmenováním souboru dojde také k přejmenování třídy na `StreamReaderEnumerable` .</span><span class="sxs-lookup"><span data-stu-id="28eb6-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="28eb6-127">Tato třída implementuje `IEnumerable(Of String)` rozhraní.</span><span class="sxs-lookup"><span data-stu-id="28eb6-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1. <span data-ttu-id="28eb6-128">Klikněte pravým tlačítkem na projekt StreamReaderEnumerable, přejděte na **Přidat**a klikněte na **Nová položka**.</span><span class="sxs-lookup"><span data-stu-id="28eb6-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="28eb6-129">Vyberte šablonu **třídy** .</span><span class="sxs-lookup"><span data-stu-id="28eb6-129">Select the **Class** template.</span></span> <span data-ttu-id="28eb6-130">Do pole **název** zadejte `StreamReaderEnumerator.vb` a klikněte na **OK**.</span><span class="sxs-lookup"><span data-stu-id="28eb6-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="28eb6-131">První třída v tomto projektu je vyčíslitelné třída a implementuje `IEnumerable(Of String)` rozhraní.</span><span class="sxs-lookup"><span data-stu-id="28eb6-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="28eb6-132">Toto obecné rozhraní implementuje <xref:System.Collections.IEnumerable> rozhraní a zaručuje, že příjemci této třídy mají přístup k hodnotám, které jsou zadány jako `String` .</span><span class="sxs-lookup"><span data-stu-id="28eb6-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="28eb6-133">**Přidejte kód pro implementaci rozhraní IEnumerable**</span><span class="sxs-lookup"><span data-stu-id="28eb6-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="28eb6-134">Otevřete soubor StreamReaderEnumerable. vb.</span><span class="sxs-lookup"><span data-stu-id="28eb6-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="28eb6-135">Na řádku `Public Class StreamReaderEnumerable` Zadejte následující příkaz a stiskněte klávesu ENTER.</span><span class="sxs-lookup"><span data-stu-id="28eb6-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#1)]

   <span data-ttu-id="28eb6-136">Visual Basic automaticky naplní třídu členy, které jsou vyžadovány `IEnumerable(Of String)` rozhraním.</span><span class="sxs-lookup"><span data-stu-id="28eb6-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="28eb6-137">Tato Výčtová třída načte řádky z textového souboru po jednom řádku.</span><span class="sxs-lookup"><span data-stu-id="28eb6-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="28eb6-138">Přidejte následující kód do třídy k vystavení veřejného konstruktoru, který jako vstupní parametr převezme cestu k souboru.</span><span class="sxs-lookup"><span data-stu-id="28eb6-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#2)]

4. <span data-ttu-id="28eb6-139">Vaše implementace <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> metody `IEnumerable(Of String)` rozhraní vrátí novou instanci `StreamReaderEnumerator` třídy.</span><span class="sxs-lookup"><span data-stu-id="28eb6-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="28eb6-140">Implementaci `GetEnumerator` metody `IEnumerable` rozhraní lze vytvořit `Private` , protože je nutné vystavit pouze členy `IEnumerable(Of String)` rozhraní.</span><span class="sxs-lookup"><span data-stu-id="28eb6-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="28eb6-141">Nahraďte kód, který Visual Basic vygeneroval pro `GetEnumerator` metody, pomocí následujícího kódu.</span><span class="sxs-lookup"><span data-stu-id="28eb6-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#3)]  
  
<span data-ttu-id="28eb6-142">**Přidání kódu pro implementaci rozhraní IEnumerator**</span><span class="sxs-lookup"><span data-stu-id="28eb6-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="28eb6-143">Otevřete soubor StreamReaderEnumerator. vb.</span><span class="sxs-lookup"><span data-stu-id="28eb6-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="28eb6-144">Na řádku `Public Class StreamReaderEnumerator` Zadejte následující příkaz a stiskněte klávesu ENTER.</span><span class="sxs-lookup"><span data-stu-id="28eb6-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#4)]

   <span data-ttu-id="28eb6-145">Visual Basic automaticky naplní třídu členy, které jsou vyžadovány `IEnumerator(Of String)` rozhraním.</span><span class="sxs-lookup"><span data-stu-id="28eb6-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="28eb6-146">Třída enumerator otevře textový soubor a provede vstupně-výstupní operace souboru pro čtení řádků ze souboru.</span><span class="sxs-lookup"><span data-stu-id="28eb6-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="28eb6-147">Přidejte následující kód do třídy k vystavení veřejného konstruktoru, který jako vstupní parametr převezme cestu k souboru, a otevřete textový soubor pro čtení.</span><span class="sxs-lookup"><span data-stu-id="28eb6-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#5)]

4. <span data-ttu-id="28eb6-148">`Current`Vlastnosti `IEnumerator(Of String)` `IEnumerator` rozhraní a vracejí aktuální položku z textového souboru jako `String` .</span><span class="sxs-lookup"><span data-stu-id="28eb6-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="28eb6-149">Implementaci `Current` vlastnosti `IEnumerator` rozhraní lze vytvořit `Private` , protože je nutné vystavit pouze členy `IEnumerator(Of String)` rozhraní.</span><span class="sxs-lookup"><span data-stu-id="28eb6-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="28eb6-150">Nahraďte kód, který Visual Basic vygeneroval pro `Current` vlastnosti, pomocí následujícího kódu.</span><span class="sxs-lookup"><span data-stu-id="28eb6-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#6)]

5. <span data-ttu-id="28eb6-151">`MoveNext`Metoda `IEnumerator` rozhraní přejde na další položku v textovém souboru a aktualizuje hodnotu vrácenou `Current` vlastností.</span><span class="sxs-lookup"><span data-stu-id="28eb6-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="28eb6-152">Pokud nejsou k dispozici žádné další položky ke čtení, `MoveNext` Metoda vrátí hodnotu `False` ; v opačném případě se `MoveNext` Metoda vrátí `True` .</span><span class="sxs-lookup"><span data-stu-id="28eb6-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="28eb6-153">Do metody `MoveNext` přidejte následující kód.</span><span class="sxs-lookup"><span data-stu-id="28eb6-153">Add the following code to the `MoveNext` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#7)]

6. <span data-ttu-id="28eb6-154">`Reset`Metoda `IEnumerator` rozhraní přesměruje iterátor, aby odkazoval na začátek textového souboru a vymaže hodnotu aktuální položky.</span><span class="sxs-lookup"><span data-stu-id="28eb6-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="28eb6-155">Do metody `Reset` přidejte následující kód.</span><span class="sxs-lookup"><span data-stu-id="28eb6-155">Add the following code to the `Reset` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#8)]

7. <span data-ttu-id="28eb6-156">`Dispose`Metoda `IEnumerator` rozhraní zaručuje, že všechny nespravované prostředky jsou uvolněny před zničením iterátoru.</span><span class="sxs-lookup"><span data-stu-id="28eb6-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="28eb6-157">Popisovač souboru, který je používán `StreamReader` objektem, je nespravovaný prostředek a je třeba jej zavřít před zničením instance iterátoru.</span><span class="sxs-lookup"><span data-stu-id="28eb6-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="28eb6-158">Nahraďte kód, který Visual Basic vygeneroval pro `Dispose` metodu, pomocí následujícího kódu.</span><span class="sxs-lookup"><span data-stu-id="28eb6-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#9)]
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="28eb6-159">Použití ukázkového iterátoru</span><span class="sxs-lookup"><span data-stu-id="28eb6-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="28eb6-160">V kódu můžete použít vyčíslitelné třídy společně s řídicími strukturami, které vyžadují objekt, který implementuje `IEnumerable` , jako je například `For Next` smyčka nebo dotaz LINQ.</span><span class="sxs-lookup"><span data-stu-id="28eb6-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="28eb6-161">Následující příklad ukazuje `StreamReaderEnumerable` v dotazu LINQ.</span><span class="sxs-lookup"><span data-stu-id="28eb6-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/Module1.vb#10)]  
  
## <a name="see-also"></a><span data-ttu-id="28eb6-162">Viz také</span><span class="sxs-lookup"><span data-stu-id="28eb6-162">See also</span></span>

- [<span data-ttu-id="28eb6-163">Představení technologie LINQ v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="28eb6-163">Introduction to LINQ in Visual Basic</span></span>](../linq/introduction-to-linq.md)
- [<span data-ttu-id="28eb6-164">Tok řízení</span><span class="sxs-lookup"><span data-stu-id="28eb6-164">Control Flow</span></span>](index.md)
- [<span data-ttu-id="28eb6-165">Struktury smyčky</span><span class="sxs-lookup"><span data-stu-id="28eb6-165">Loop Structures</span></span>](loop-structures.md)
- [<span data-ttu-id="28eb6-166">For Each...Next – příkaz</span><span class="sxs-lookup"><span data-stu-id="28eb6-166">For Each...Next Statement</span></span>](../../../language-reference/statements/for-each-next-statement.md)
