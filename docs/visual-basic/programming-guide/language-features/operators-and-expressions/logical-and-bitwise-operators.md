---
title: Logické a bitové operátory v jazyce Visual Basic
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: 23f3758527b787551ad83cbd4e19076b788c9dd8
ms.sourcegitcommit: 2701302a99cafbe0d86d53d540eb0fa7e9b46b36
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/28/2019
ms.locfileid: "64649695"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="79bf7-102">Logické a bitové operátory v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="79bf7-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="79bf7-103">Logické operátory porovnávají `Boolean` výrazy a vraťte se `Boolean` výsledek.</span><span class="sxs-lookup"><span data-stu-id="79bf7-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="79bf7-104">`And`, `Or`, `AndAlso`, `OrElse`, A `Xor` operátory jsou *binární* protože přijímají dva operandy, zatímco `Not` operátor je *unární* protože trvá jeden operand.</span><span class="sxs-lookup"><span data-stu-id="79bf7-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="79bf7-105">Některé z těchto operátorů lze provést také bitové logické operace na integrální hodnoty.</span><span class="sxs-lookup"><span data-stu-id="79bf7-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="79bf7-106">Unární logický operátor</span><span class="sxs-lookup"><span data-stu-id="79bf7-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="79bf7-107">[Operátor Not](../../../../visual-basic/language-reference/operators/not-operator.md) provádí logické *negace* na `Boolean` výrazu.</span><span class="sxs-lookup"><span data-stu-id="79bf7-107">The [Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="79bf7-108">Bude vrácen na logický protějšek svého operandu.</span><span class="sxs-lookup"><span data-stu-id="79bf7-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="79bf7-109">Pokud je výraz vyhodnocen `True`, pak `Not` vrátí `False`; Pokud je výraz vyhodnocen `False`, pak `Not` vrátí `True`.</span><span class="sxs-lookup"><span data-stu-id="79bf7-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="79bf7-110">Toto dokládá následující příklad.</span><span class="sxs-lookup"><span data-stu-id="79bf7-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="79bf7-111">Binární logické operátory</span><span class="sxs-lookup"><span data-stu-id="79bf7-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="79bf7-112">[Operátoru](../../../../visual-basic/language-reference/operators/and-operator.md) provádí logické *spojení* na dvou `Boolean` výrazy.</span><span class="sxs-lookup"><span data-stu-id="79bf7-112">The [And Operator](../../../../visual-basic/language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="79bf7-113">Pokud mají oba výrazy `True`, pak `And` vrátí `True`.</span><span class="sxs-lookup"><span data-stu-id="79bf7-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="79bf7-114">Pokud se alespoň jeden z výrazů vyhodnotí jako `False`, pak `And` vrátí `False`.</span><span class="sxs-lookup"><span data-stu-id="79bf7-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="79bf7-115">[Nebo operátor](../../../../visual-basic/language-reference/operators/or-operator.md) provádí logické *disjunkce* nebo *zahrnutí* na dvou `Boolean` výrazy.</span><span class="sxs-lookup"><span data-stu-id="79bf7-115">The [Or Operator](../../../../visual-basic/language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="79bf7-116">Pokud je výraz vyhodnocen jako `True`, nebo obojí vyhodnotit `True`, pak `Or` vrátí `True`.</span><span class="sxs-lookup"><span data-stu-id="79bf7-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="79bf7-117">Pokud ani výraz nevyhodnotí jako `True`, `Or` vrátí `False`.</span><span class="sxs-lookup"><span data-stu-id="79bf7-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="79bf7-118">[Xor Operátor](../../../../visual-basic/language-reference/operators/xor-operator.md) provádí logické *vyloučení* na dvou `Boolean` výrazy.</span><span class="sxs-lookup"><span data-stu-id="79bf7-118">The [Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="79bf7-119">Pokud se přesně jeden výraz vyhodnotí jako `True`, ale ne obojí `Xor` vrátí `True`.</span><span class="sxs-lookup"><span data-stu-id="79bf7-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="79bf7-120">Pokud mají oba výrazy `True` nebo obojí vyhodnotit `False`, `Xor` vrátí `False`.</span><span class="sxs-lookup"><span data-stu-id="79bf7-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="79bf7-121">Následující příklad ukazuje, `And`, `Or`, a `Xor` operátory.</span><span class="sxs-lookup"><span data-stu-id="79bf7-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="79bf7-122">Zkrácení logické operace</span><span class="sxs-lookup"><span data-stu-id="79bf7-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="79bf7-123">[AndAlso – operátor](../../../../visual-basic/language-reference/operators/andalso-operator.md) je velmi podobný `And` operátor v tom, že také provede logickou konjunkci na dvou `Boolean` výrazy.</span><span class="sxs-lookup"><span data-stu-id="79bf7-123">The [AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="79bf7-124">Klíčovým rozdílem mezi těmito dvěma je, že `AndAlso` vykazuje *zkrácenou* chování.</span><span class="sxs-lookup"><span data-stu-id="79bf7-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="79bf7-125">Pokud k prvnímu výrazu v `AndAlso` výraz vyhodnocen jako `False`, a druhý výraz není vyhodnocen, protože ji nelze změnit na konečný výsledek a `AndAlso` vrátí `False`.</span><span class="sxs-lookup"><span data-stu-id="79bf7-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="79bf7-126">Podobně platí [OrElse – operátor](../../../../visual-basic/language-reference/operators/orelse-operator.md) provede zkrácenou logickou disjunkci dvou `Boolean` výrazy.</span><span class="sxs-lookup"><span data-stu-id="79bf7-126">Similarly, the [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="79bf7-127">Pokud k prvnímu výrazu v `OrElse` výraz vyhodnocen jako `True`, a druhý výraz není vyhodnocen, protože ji nelze změnit na konečný výsledek a `OrElse` vrátí `True`.</span><span class="sxs-lookup"><span data-stu-id="79bf7-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="79bf7-128">Krátký cyklus kompromisy</span><span class="sxs-lookup"><span data-stu-id="79bf7-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="79bf7-129">Krátký cyklus lze vylepšit výkon není vyhodnocení výrazu, který nejde změnit výsledek logické operace.</span><span class="sxs-lookup"><span data-stu-id="79bf7-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="79bf7-130">Pokud tento výraz provede další akce, ale zkrácenou přeskočí těchto akcí.</span><span class="sxs-lookup"><span data-stu-id="79bf7-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="79bf7-131">Například, pokud výraz obsahuje volání `Function` postupu, že postup není volána, pokud má výraz hodnotu zkratována a žádný další kód obsažený v `Function` se nespustí.</span><span class="sxs-lookup"><span data-stu-id="79bf7-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="79bf7-132">Proto funkce může spustit jenom občas a nemusí být správně testována.</span><span class="sxs-lookup"><span data-stu-id="79bf7-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="79bf7-133">Nebo aplikaci logiky může záviset na kód v `Function`.</span><span class="sxs-lookup"><span data-stu-id="79bf7-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="79bf7-134">Následující příklad ukazuje rozdíl mezi `And`, `Or`a jejich protějšky short-circuiting.</span><span class="sxs-lookup"><span data-stu-id="79bf7-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="79bf7-135">V předchozím příkladu, Všimněte si, že některé důležité kód uvnitř `checkIfValid()` při volání je zkratována nespustí.</span><span class="sxs-lookup"><span data-stu-id="79bf7-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="79bf7-136">První `If` příkaz volá `checkIfValid()` Přestože `12 > 45` vrátí `False`, protože `And` není zkrácenou.</span><span class="sxs-lookup"><span data-stu-id="79bf7-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="79bf7-137">Druhá `If` příkaz nevolá `checkIfValid()`, protože při `12 > 45` vrátí `False`, `AndAlso` zkratům druhý výraz.</span><span class="sxs-lookup"><span data-stu-id="79bf7-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="79bf7-138">Třetí `If` příkaz volá `checkIfValid()` Přestože `12 < 45` vrátí `True`, protože `Or` není zkrácenou.</span><span class="sxs-lookup"><span data-stu-id="79bf7-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="79bf7-139">Čtvrtý `If` příkaz nevolá `checkIfValid()`, protože při `12 < 45` vrátí `True`, `OrElse` zkratům druhý výraz.</span><span class="sxs-lookup"><span data-stu-id="79bf7-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="79bf7-140">Bitové operace</span><span class="sxs-lookup"><span data-stu-id="79bf7-140">Bitwise Operations</span></span>  
 <span data-ttu-id="79bf7-141">Bitové operace vyhodnotit dvě celočíselné hodnoty ve formuláři binární soubor (základní 2).</span><span class="sxs-lookup"><span data-stu-id="79bf7-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="79bf7-142">Jejich porovnání bity na odpovídající pozice a pak přiřaďte hodnoty založené na porovnání.</span><span class="sxs-lookup"><span data-stu-id="79bf7-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="79bf7-143">Následující příklad ukazuje, `And` operátor.</span><span class="sxs-lookup"><span data-stu-id="79bf7-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="79bf7-144">Předchozí příklad nastaví hodnotu `x` na hodnotu 1.</span><span class="sxs-lookup"><span data-stu-id="79bf7-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="79bf7-145">V takovém z následujících důvodů:</span><span class="sxs-lookup"><span data-stu-id="79bf7-145">This happens for the following reasons:</span></span>  
  
- <span data-ttu-id="79bf7-146">Hodnoty jsou považovány za binární soubor:</span><span class="sxs-lookup"><span data-stu-id="79bf7-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="79bf7-147">3 v binárním formátu = 011</span><span class="sxs-lookup"><span data-stu-id="79bf7-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="79bf7-148">5 v binárním formátu = 101</span><span class="sxs-lookup"><span data-stu-id="79bf7-148">5 in binary form = 101</span></span>  
  
- <span data-ttu-id="79bf7-149">`And` Operátor porovnání vícebinární reprezentace jednu binární pozici (verze) v čase.</span><span class="sxs-lookup"><span data-stu-id="79bf7-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="79bf7-150">Pokud i službu bits na dané pozici 1, 1 je umístěn na této pozici ve výsledku.</span><span class="sxs-lookup"><span data-stu-id="79bf7-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="79bf7-151">Pokud buď bit na hodnotu 0, 0 je umístěn na této pozici ve výsledku.</span><span class="sxs-lookup"><span data-stu-id="79bf7-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="79bf7-152">V předchozím příkladu toto funguje takto:</span><span class="sxs-lookup"><span data-stu-id="79bf7-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="79bf7-153">011 (3 v binárním formátu)</span><span class="sxs-lookup"><span data-stu-id="79bf7-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="79bf7-154">101 (5 v binárním formátu)</span><span class="sxs-lookup"><span data-stu-id="79bf7-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="79bf7-155">001 (výsledek, v binárním formátu)</span><span class="sxs-lookup"><span data-stu-id="79bf7-155">001 (The result, in binary form)</span></span>  
  
- <span data-ttu-id="79bf7-156">Výsledkem je považováno za desítkové.</span><span class="sxs-lookup"><span data-stu-id="79bf7-156">The result is treated as decimal.</span></span> <span data-ttu-id="79bf7-157">Hodnota 001 je binární reprezentace 1, takže `x` = 1.</span><span class="sxs-lookup"><span data-stu-id="79bf7-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="79bf7-158">Bitový `Or` operace je podobný s tím rozdílem, že 1 je přiřazena k bitu výsledku, pokud jeden nebo oba z porovnání bitů je 1.</span><span class="sxs-lookup"><span data-stu-id="79bf7-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="79bf7-159">`Xor` přiřadí k bitu výsledku 1, v případě, že právě jeden z porovnání bity (ne oba) je 1.</span><span class="sxs-lookup"><span data-stu-id="79bf7-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="79bf7-160">`Not` přijímá jeden operand obrátí všechny bity, včetně bitu a přiřadí tuto hodnotu na výsledek.</span><span class="sxs-lookup"><span data-stu-id="79bf7-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="79bf7-161">To znamená, že pro podepsané kladná čísla `Not` vždy vrátí zápornou hodnotu a záporných čísel `Not` vždy vrátí hodnotu pozitivní nebo nulovou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="79bf7-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="79bf7-162">`AndAlso` a `OrElse` operátory nepodporuje bitové operace.</span><span class="sxs-lookup"><span data-stu-id="79bf7-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="79bf7-163">Bitová operace lze provádět na pouze pro integrální typy.</span><span class="sxs-lookup"><span data-stu-id="79bf7-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="79bf7-164">Hodnoty s plovoucí desetinnou čárkou musí převést na integrální typy než bitová operace můžete pokračovat.</span><span class="sxs-lookup"><span data-stu-id="79bf7-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="79bf7-165">Viz také:</span><span class="sxs-lookup"><span data-stu-id="79bf7-165">See also</span></span>

- [<span data-ttu-id="79bf7-166">Logické/bitové operátory (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="79bf7-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../../visual-basic/language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="79bf7-167">Logické výrazy</span><span class="sxs-lookup"><span data-stu-id="79bf7-167">Boolean Expressions</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md)
- [<span data-ttu-id="79bf7-168">Aritmetické operátory v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="79bf7-168">Arithmetic Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/arithmetic-operators.md)
- [<span data-ttu-id="79bf7-169">Operátory porovnání v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="79bf7-169">Comparison Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/comparison-operators.md)
- [<span data-ttu-id="79bf7-170">Operátory řetězení v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="79bf7-170">Concatenation Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/concatenation-operators.md)
- [<span data-ttu-id="79bf7-171">Účinná kombinace operátorů</span><span class="sxs-lookup"><span data-stu-id="79bf7-171">Efficient Combination of Operators</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/efficient-combination-of-operators.md)
