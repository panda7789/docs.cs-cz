---
title: Logické a bitové operátory
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: 55a246c0d56501a409ebbc7d0d0aa39ae9fa1770
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 11/22/2019
ms.locfileid: "74343597"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="a5ab2-102">Logické a bitové operátory v jazyce Visual Basic</span><span class="sxs-lookup"><span data-stu-id="a5ab2-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="a5ab2-103">Logické operátory porovnávají `Boolean` výrazy a vrátí výsledek `Boolean`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="a5ab2-104">Operátory `And`, `Or`, `AndAlso`, `OrElse`a `Xor` jsou *binární* , protože berou dva operandy, zatímco operátor `Not` je *unární* , protože má jeden operand.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="a5ab2-105">Některé z těchto operátorů mohou také provádět bitové logické operace s celočíselnými hodnotami.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="a5ab2-106">Unární logický operátor</span><span class="sxs-lookup"><span data-stu-id="a5ab2-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="a5ab2-107">[Operátor NOT](../../../../visual-basic/language-reference/operators/not-operator.md) provádí logickou *negaci* pro výraz `Boolean`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-107">The [Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="a5ab2-108">Vrací logický opak svého operandu.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="a5ab2-109">Pokud se výraz vyhodnotí jako `True`, vrátí `Not` `False`; Pokud je výraz vyhodnocen jako `False`, `Not` vrátí `True`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="a5ab2-110">Toto dokládá následující příklad.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="a5ab2-111">Binární logické operátory</span><span class="sxs-lookup"><span data-stu-id="a5ab2-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="a5ab2-112">[Operátor and](../../../../visual-basic/language-reference/operators/and-operator.md) provádí logickou *kombinaci* na dvou `Boolean`ch výrazech.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-112">The [And Operator](../../../../visual-basic/language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="a5ab2-113">Pokud jsou oba výrazy vyhodnoceny jako `True`, `And` vrátí `True`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="a5ab2-114">Pokud se alespoň jeden z výrazů vyhodnotí jako `False`, `And` vrátí `False`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="a5ab2-115">[Operátor OR](../../../../visual-basic/language-reference/operators/or-operator.md) provede logickou *disjunkci* nebo *zahrnutí* na dva `Boolean` výrazy.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-115">The [Or Operator](../../../../visual-basic/language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="a5ab2-116">Pokud se jeden výraz vyhodnotí jako `True`nebo se vyhodnotí jako `True`, `Or` vrátí `True`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="a5ab2-117">Pokud není žádný výraz vyhodnocen jako `True`, `Or` vrátí `False`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="a5ab2-118">[Operátor XOR](../../../../visual-basic/language-reference/operators/xor-operator.md) provádí logické *vyloučení* dvou `Boolean` výrazů.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-118">The [Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="a5ab2-119">Pokud se přesně jeden výraz vyhodnotí jako `True`, ale ne obojí, `Xor` vrátí `True`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="a5ab2-120">Pokud se oba výrazy vyhodnotí jako `True` nebo se vyhodnotí jako `False`, `Xor` vrátí `False`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="a5ab2-121">Následující příklad ukazuje operátory `And`, `Or`a `Xor`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="a5ab2-122">Logické operace krátkodobého okruhu</span><span class="sxs-lookup"><span data-stu-id="a5ab2-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="a5ab2-123">[Operátor AndAlso –](../../../../visual-basic/language-reference/operators/andalso-operator.md) je velmi podobný operátoru `And`, v tom, že také provádí logické spojení na dvou `Boolean`ch výrazech.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-123">The [AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="a5ab2-124">Hlavním rozdílem mezi nimi je to, že `AndAlso` vykazuje chování při *krátkém okruhu* .</span><span class="sxs-lookup"><span data-stu-id="a5ab2-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="a5ab2-125">Pokud se první výraz ve výrazu `AndAlso` vyhodnocuje jako `False`, pak druhý výraz není vyhodnocen, protože nemůže změnit konečný výsledek a `AndAlso` vrátí `False`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="a5ab2-126">Podobně [operátor OrElse](../../../../visual-basic/language-reference/operators/orelse-operator.md) provádí zkrácenou logickou disjunkci dvou `Boolean` výrazů.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-126">Similarly, the [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="a5ab2-127">Pokud se první výraz ve výrazu `OrElse` vyhodnocuje jako `True`, pak druhý výraz není vyhodnocen, protože nemůže změnit konečný výsledek a `OrElse` vrátí `True`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="a5ab2-128">Krátkodobé kompromisy</span><span class="sxs-lookup"><span data-stu-id="a5ab2-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="a5ab2-129">Krátkodobé okruhy mohou zvýšit výkon tím, že nevyhodnotí výraz, který nemůže změnit výsledek logické operace.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="a5ab2-130">Nicméně pokud tento výraz provede další akce, při krátkém okruhu se tyto akce přeskočí.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="a5ab2-131">Například pokud výraz obsahuje volání procedury `Function`, není tato procedura volána, pokud je výraz krátký, a jakýkoliv další kód obsažený v `Function` neběží.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="a5ab2-132">Proto může být funkce spuštěna pouze občas a nemusí být testována správně.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="a5ab2-133">Nebo logika programu může záviset na kódu v `Function`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="a5ab2-134">Následující příklad znázorňuje rozdíl mezi `And`, `Or`a jejich protějšky při jejich krátkodobém okruhu.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="a5ab2-135">V předchozím příkladu si všimněte, že některé důležité kódy v rámci `checkIfValid()` neběží, když je volání zkrácené.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="a5ab2-136">První příkaz `If` volá `checkIfValid()`, i když `12 > 45` vrátí `False`, protože `And` nemá krátký okruh.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="a5ab2-137">Druhý příkaz `If` nevolá `checkIfValid()`, protože když `12 > 45` vrátí `False`, `AndAlso` kratší okruh druhý výraz.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="a5ab2-138">Třetí příkaz `If` volá `checkIfValid()`, i když `12 < 45` vrátí `True`, protože `Or` nemá krátký okruh.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="a5ab2-139">Čtvrtý příkaz `If` nevolá `checkIfValid()`, protože když `12 < 45` vrátí `True`, `OrElse` krátkodobého okruhu druhý výraz.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="a5ab2-140">Bitové operace</span><span class="sxs-lookup"><span data-stu-id="a5ab2-140">Bitwise Operations</span></span>  
 <span data-ttu-id="a5ab2-141">Bitové operace vyhodnocují dvě integrální hodnoty v binárním formátu (2. základ).</span><span class="sxs-lookup"><span data-stu-id="a5ab2-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="a5ab2-142">Porovná bity s odpovídajícími pozicemi a pak přiřadí hodnoty na základě porovnání.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="a5ab2-143">Následující příklad ilustruje operátor `And`.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="a5ab2-144">Předchozí příklad nastaví hodnotu `x` na 1.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="a5ab2-145">K tomu dochází z následujících důvodů:</span><span class="sxs-lookup"><span data-stu-id="a5ab2-145">This happens for the following reasons:</span></span>  
  
- <span data-ttu-id="a5ab2-146">Hodnoty se považují za binární:</span><span class="sxs-lookup"><span data-stu-id="a5ab2-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="a5ab2-147">3 v binárním tvaru = 011</span><span class="sxs-lookup"><span data-stu-id="a5ab2-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="a5ab2-148">5 v binárním tvaru = 101</span><span class="sxs-lookup"><span data-stu-id="a5ab2-148">5 in binary form = 101</span></span>  
  
- <span data-ttu-id="a5ab2-149">Operátor `And` porovnává binární reprezentace, jednu binární pozici (bitovou) v jednom okamžiku.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="a5ab2-150">Pokud jsou obě bity na dané pozici 1, pak je ve výsledku umístěna jedna pozice.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="a5ab2-151">Pokud má bit hodnotu 0, pak je na této pozici ve výsledku umístěný 0.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="a5ab2-152">V předchozím příkladu to funguje takto:</span><span class="sxs-lookup"><span data-stu-id="a5ab2-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="a5ab2-153">011 (3 v binárním formátu)</span><span class="sxs-lookup"><span data-stu-id="a5ab2-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="a5ab2-154">101 (5 v binárním formátu)</span><span class="sxs-lookup"><span data-stu-id="a5ab2-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="a5ab2-155">001 (výsledek, v binárním formátu)</span><span class="sxs-lookup"><span data-stu-id="a5ab2-155">001 (The result, in binary form)</span></span>  
  
- <span data-ttu-id="a5ab2-156">Výsledek je považován za desetinný.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-156">The result is treated as decimal.</span></span> <span data-ttu-id="a5ab2-157">Hodnota 001 je binární reprezentace 1, takže `x` = 1.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="a5ab2-158">Bitová operace `Or` je podobná, s tím rozdílem, že 1 je přiřazena ke výslednému bitu, pokud je jedna nebo obě z porovnávaných bitů 1.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="a5ab2-159">`Xor` přiřadí ke výslednému bitu 1, pokud je přesně jedna z porovnávaných bitů (ne obojí) 1.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="a5ab2-160">`Not` bere jeden operand a Invertuje všechny bity, včetně znaku znaménka, a přiřadí tuto hodnotu k výsledku.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="a5ab2-161">To znamená, že u podepsaných kladných čísel `Not` vždycky vrací zápornou hodnotu a pro záporná čísla `Not` vždycky vrací kladnou nebo nulovou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="a5ab2-162">Operátory `AndAlso` a `OrElse` nepodporují bitové operace.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="a5ab2-163">Bitové operace lze provádět pouze na integrálních typech.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="a5ab2-164">Hodnoty s plovoucí desetinnou čárkou musí být převedeny na integrální typy předtím, než může pokračovat bitová operace.</span><span class="sxs-lookup"><span data-stu-id="a5ab2-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="a5ab2-165">Viz také:</span><span class="sxs-lookup"><span data-stu-id="a5ab2-165">See also</span></span>

- [<span data-ttu-id="a5ab2-166">Logické/bitové operátory (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="a5ab2-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../../visual-basic/language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="a5ab2-167">Logické výrazy</span><span class="sxs-lookup"><span data-stu-id="a5ab2-167">Boolean Expressions</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md)
- [<span data-ttu-id="a5ab2-168">Aritmetické operátory v Visual Basic</span><span class="sxs-lookup"><span data-stu-id="a5ab2-168">Arithmetic Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/arithmetic-operators.md)
- [<span data-ttu-id="a5ab2-169">Operátory porovnávání v Visual Basic</span><span class="sxs-lookup"><span data-stu-id="a5ab2-169">Comparison Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/comparison-operators.md)
- [<span data-ttu-id="a5ab2-170">Operátory zřetězení v Visual Basic</span><span class="sxs-lookup"><span data-stu-id="a5ab2-170">Concatenation Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/concatenation-operators.md)
- [<span data-ttu-id="a5ab2-171">Účinná kombinace operátorů</span><span class="sxs-lookup"><span data-stu-id="a5ab2-171">Efficient Combination of Operators</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/efficient-combination-of-operators.md)
