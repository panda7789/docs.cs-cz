---
title: Asynchronní programování pomocí modifikátoru Async a operátoru Await
ms.date: 07/20/2015
ms.assetid: bd7e462b-583b-4395-9c36-45aa9e61072c
ms.openlocfilehash: cbcdd48571855e168f563585088f1210eb6410eb
ms.sourcegitcommit: 43d10ef65f0f1fd6c3b515e363bde11a3fcd8d6d
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/04/2020
ms.locfileid: "78266492"
---
# <a name="asynchronous-programming-with-async-and-await-visual-basic"></a><span data-ttu-id="eec71-102">Asynchronní programování s Async a Await (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eec71-102">Asynchronous programming with Async and Await (Visual Basic)</span></span>

<span data-ttu-id="eec71-103">Pomocí asynchronního programování se můžete vyhnout kritickým bodům a zlepšit celkovou rychlost reakce aplikace.</span><span class="sxs-lookup"><span data-stu-id="eec71-103">You can avoid performance bottlenecks and enhance the overall responsiveness of your application by using asynchronous programming.</span></span> <span data-ttu-id="eec71-104">Tradiční techniky pro psaní asynchronních aplikací však mohou být složité, takže je obtížné je napsat, ladit a udržovat.</span><span class="sxs-lookup"><span data-stu-id="eec71-104">However, traditional techniques for writing asynchronous applications can be complicated, making them difficult to write, debug, and maintain.</span></span>

<span data-ttu-id="eec71-105">Visual Studio 2012 zavedlo zjednodušený přístup, asynchronní programování, který využívá asynchronní podporu v rozhraní .NET Framework 4.5 a vyšší, stejně jako v prostředí Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="eec71-105">Visual Studio 2012 introduced a simplified approach, async programming, that leverages asynchronous support in the .NET Framework 4.5 and higher as well as  in the Windows Runtime.</span></span> <span data-ttu-id="eec71-106">Kompilátor na sebe přejímá náročnou práci, kterou vykonával vývojář, a vaše aplikace si zachovává logickou strukturu, která se podobá synchronnímu kódu.</span><span class="sxs-lookup"><span data-stu-id="eec71-106">The compiler does the difficult work that the developer used to do, and your application retains a logical structure that resembles synchronous code.</span></span> <span data-ttu-id="eec71-107">Výsledkem je, že získáte všechny výhody asynchronního programování při pouhém zlomku úsilí.</span><span class="sxs-lookup"><span data-stu-id="eec71-107">As a result, you get all the advantages of asynchronous programming with a fraction of the effort.</span></span>

<span data-ttu-id="eec71-108">Toto téma obsahuje přehled kdy a jak použít asynchronní programování a obsahuje odkazy na témata podpory, která obsahují podrobné informace a příklady.</span><span class="sxs-lookup"><span data-stu-id="eec71-108">This topic provides an overview of when and how to use async programming and includes links to support topics that contain details and examples.</span></span>

## <a name="async-improves-responsiveness"></a><a name="BKMK_WhentoUseAsynchrony"></a><span data-ttu-id="eec71-109">Async zlepšuje odezvu</span><span class="sxs-lookup"><span data-stu-id="eec71-109">Async improves responsiveness</span></span>

<span data-ttu-id="eec71-110">Asynchronie je nezbytná pro aktivity, které mohou potenciálně blokovat, například pokud vaše aplikace přistupuje na web.</span><span class="sxs-lookup"><span data-stu-id="eec71-110">Asynchrony is essential for activities that are potentially blocking, such as when your application accesses the web.</span></span> <span data-ttu-id="eec71-111">Přístup k webovému prostředku je někdy pomalý nebo zpožděný.</span><span class="sxs-lookup"><span data-stu-id="eec71-111">Access to a web resource sometimes is slow or delayed.</span></span> <span data-ttu-id="eec71-112">Pokud je taková činnost blokována v rámci synchronního procesu, musí čekat celá aplikace.</span><span class="sxs-lookup"><span data-stu-id="eec71-112">If such an activity is blocked within a synchronous process, the entire application must wait.</span></span> <span data-ttu-id="eec71-113">U asynchronního procesu může aplikace pokračovat v další práci, která nezávisí na webovém prostředku, dokud neskončí potenciálně blokující úloha.</span><span class="sxs-lookup"><span data-stu-id="eec71-113">In an asynchronous process, the application can continue with other work that doesn't depend on the web resource until the potentially blocking task finishes.</span></span>

<span data-ttu-id="eec71-114">Následující tabulka ukazuje typické oblasti, kde asynchronní programování zlepšuje rychlost reakce.</span><span class="sxs-lookup"><span data-stu-id="eec71-114">The following table shows typical areas where asynchronous programming improves responsiveness.</span></span> <span data-ttu-id="eec71-115">Uvedená rozhraní API z rozhraní .NET Framework 4.5 a prostředí Windows Runtime obsahují metody, které podporují asynchronní programování.</span><span class="sxs-lookup"><span data-stu-id="eec71-115">The listed APIs from the  .NET Framework 4.5 and the Windows Runtime contain methods that support async programming.</span></span>

|<span data-ttu-id="eec71-116">Oblast aplikace</span><span class="sxs-lookup"><span data-stu-id="eec71-116">Application area</span></span>|<span data-ttu-id="eec71-117">Podpora rozhraní API, která obsahují asynchronní metody</span><span class="sxs-lookup"><span data-stu-id="eec71-117">Supporting APIs that contain async methods</span></span>|
|----------------------|------------------------------------------------|
|<span data-ttu-id="eec71-118">Webový přístup</span><span class="sxs-lookup"><span data-stu-id="eec71-118">Web access</span></span>|<span data-ttu-id="eec71-119"><xref:System.Net.Http.HttpClient>, <xref:Windows.Web.Syndication.SyndicationClient></span><span class="sxs-lookup"><span data-stu-id="eec71-119"><xref:System.Net.Http.HttpClient>, <xref:Windows.Web.Syndication.SyndicationClient></span></span>|
|<span data-ttu-id="eec71-120">Práce se soubory</span><span class="sxs-lookup"><span data-stu-id="eec71-120">Working with files</span></span>|<span data-ttu-id="eec71-121"><xref:Windows.Storage.StorageFile>, <xref:System.IO.StreamWriter>, <xref:System.IO.StreamReader>, <xref:System.Xml.XmlReader></span><span class="sxs-lookup"><span data-stu-id="eec71-121"><xref:Windows.Storage.StorageFile>, <xref:System.IO.StreamWriter>, <xref:System.IO.StreamReader>, <xref:System.Xml.XmlReader></span></span>|
|<span data-ttu-id="eec71-122">Práce s obrázky</span><span class="sxs-lookup"><span data-stu-id="eec71-122">Working with images</span></span>|<span data-ttu-id="eec71-123"><xref:Windows.Media.Capture.MediaCapture>, <xref:Windows.Graphics.Imaging.BitmapEncoder>, <xref:Windows.Graphics.Imaging.BitmapDecoder></span><span class="sxs-lookup"><span data-stu-id="eec71-123"><xref:Windows.Media.Capture.MediaCapture>, <xref:Windows.Graphics.Imaging.BitmapEncoder>, <xref:Windows.Graphics.Imaging.BitmapDecoder></span></span>|
|<span data-ttu-id="eec71-124">Programování WCF</span><span class="sxs-lookup"><span data-stu-id="eec71-124">WCF programming</span></span>|[<span data-ttu-id="eec71-125">Synchronní a asynchronní operace</span><span class="sxs-lookup"><span data-stu-id="eec71-125">Synchronous and Asynchronous Operations</span></span>](../../../../framework/wcf/synchronous-and-asynchronous-operations.md)|
|||

<span data-ttu-id="eec71-126">Asynchronie je obzvláště užitečná pro aplikace, které přistupují k vláknu UI, protože všechny aktivity související s uživatelským rozhraním obvykle sdílí jedno vlákno.</span><span class="sxs-lookup"><span data-stu-id="eec71-126">Asynchrony proves especially valuable for applications that access the UI thread because all UI-related activity usually shares one thread.</span></span> <span data-ttu-id="eec71-127">Pokud je jakýkoli proces blokován v synchronní aplikaci, jsou blokovány všechny.</span><span class="sxs-lookup"><span data-stu-id="eec71-127">If any process is blocked in a synchronous application, all are blocked.</span></span> <span data-ttu-id="eec71-128">Vaše aplikace přestane reagovat a můžete dojít k závěru, že selhala, i když místo toho čeká.</span><span class="sxs-lookup"><span data-stu-id="eec71-128">Your application stops responding, and you might conclude that it has failed when instead it's just waiting.</span></span>

<span data-ttu-id="eec71-129">Při použití asynchronních metod bude aplikace i nadále odpovídat na uživatelské rozhraní.</span><span class="sxs-lookup"><span data-stu-id="eec71-129">When you use asynchronous methods, the application continues to respond to the UI.</span></span> <span data-ttu-id="eec71-130">Pokud nechcete čekat na dokončení, můžete změnit velikost nebo minimalizovat okno, například můžete zavřít aplikaci.</span><span class="sxs-lookup"><span data-stu-id="eec71-130">You can resize or minimize a window, for example, or you can close the application if you don't want to wait for it to finish.</span></span>

<span data-ttu-id="eec71-131">Asynchronní přístup přidává ekvivalent automatického přenosu do seznamu možností, z nichž můžete vybírat při vytváření asynchronní operace.</span><span class="sxs-lookup"><span data-stu-id="eec71-131">The async-based approach adds the equivalent of an automatic transmission to the list of options that you can choose from when designing asynchronous operations.</span></span> <span data-ttu-id="eec71-132">To znamená, že získáte všechny výhody tradičního asynchronního programování, ale s mnohem menším úsilím ze strany vývojáře.</span><span class="sxs-lookup"><span data-stu-id="eec71-132">That is, you get all the benefits of traditional asynchronous programming but with much less effort from the developer.</span></span>

## <a name="async-methods-are-easier-to-write"></a><a name="BKMK_HowtoWriteanAsyncMethod"></a><span data-ttu-id="eec71-133">Asynchronní metody se snadněji zapisují</span><span class="sxs-lookup"><span data-stu-id="eec71-133">Async methods are easier to write</span></span>

<span data-ttu-id="eec71-134">[Klíčová slova Async](../../../../visual-basic/language-reference/modifiers/async.md) a [Await](../../../../visual-basic/language-reference/operators/await-operator.md) v jazyce Visual Basic jsou srdcem asynchronního programování.</span><span class="sxs-lookup"><span data-stu-id="eec71-134">The [Async](../../../../visual-basic/language-reference/modifiers/async.md) and [Await](../../../../visual-basic/language-reference/operators/await-operator.md) keywords in Visual Basic are the heart of async programming.</span></span> <span data-ttu-id="eec71-135">Pomocí těchto dvou klíčových slov můžete použít prostředky v rozhraní .NET Framework nebo Windows Runtime k vytvoření asynchronní metody téměř stejně snadno jako synchronní metodu.</span><span class="sxs-lookup"><span data-stu-id="eec71-135">By using those two keywords, you can use resources in the .NET Framework or the Windows Runtime to create an asynchronous method almost as easily as you create a synchronous method.</span></span> <span data-ttu-id="eec71-136">Asynchronní metody, které definujete pomocí `Async` a `Await` jsou označovány jako asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="eec71-136">Asynchronous methods that you define by using `Async` and `Await` are referred to as async methods.</span></span>

<span data-ttu-id="eec71-137">Následující příklad ukazuje asynchronní metodu.</span><span class="sxs-lookup"><span data-stu-id="eec71-137">The following example shows an async method.</span></span> <span data-ttu-id="eec71-138">Téměř vše v rámci kódu by vám mělo být zcela známé.</span><span class="sxs-lookup"><span data-stu-id="eec71-138">Almost everything in the code should look completely familiar to you.</span></span> <span data-ttu-id="eec71-139">Komentáře volají funkce, které jste přidali při tvorbě asynchronie.</span><span class="sxs-lookup"><span data-stu-id="eec71-139">The comments call out the features that you add to create the asynchrony.</span></span>

<span data-ttu-id="eec71-140">Úplný ukázkový soubor Windows Presentation Foundation (WPF) najdete na konci tohoto tématu a ukázku si můžete stáhnout z [ukázky asynchronní: Příklad z "Asynchronní programování s async a await"](https://docs.microsoft.com/samples/dotnet/samples/async-and-await-vb/).</span><span class="sxs-lookup"><span data-stu-id="eec71-140">You can find a complete Windows Presentation Foundation (WPF) example file at the end of this topic, and you can download the sample from [Async Sample: Example from "Asynchronous Programming with Async and Await"](https://docs.microsoft.com/samples/dotnet/samples/async-and-await-vb/).</span></span>

```vb
' Three things to note about writing an Async Function:
'  - The function has an Async modifier.
'  - Its return type is Task or Task(Of T). (See "Return Types" section.)
'  - As a matter of convention, its name ends in "Async".
Async Function AccessTheWebAsync() As Task(Of Integer)
    Using client As New HttpClient()
        ' Call and await separately.
        '  - AccessTheWebAsync can do other things while GetStringAsync is also running.
        '  - getStringTask stores the task we get from the call to GetStringAsync.
        '  - Task(Of String) means it is a task which returns a String when it is done.
        Dim getStringTask As Task(Of String) =
            client.GetStringAsync("https://docs.microsoft.com/dotnet")
        ' You can do other work here that doesn't rely on the string from GetStringAsync.
        DoIndependentWork()
        ' The Await operator suspends AccessTheWebAsync.
        '  - AccessTheWebAsync does not continue until getStringTask is complete.
        '  - Meanwhile, control returns to the caller of AccessTheWebAsync.
        '  - Control resumes here when getStringTask is complete.
        '  - The Await operator then retrieves the String result from getStringTask.
        Dim urlContents As String = Await getStringTask
        ' The Return statement specifies an Integer result.
        ' A method which awaits AccessTheWebAsync receives the Length value.
        Return urlContents.Length

    End Using

End Function
```

<span data-ttu-id="eec71-141">Pokud `AccessTheWebAsync` nemá žádnou práci, kterou může `GetStringAsync` udělat mezi voláním a čekáním na jeho dokončení, můžete zjednodušit kód voláním a čekáním v následujícím jediném příkazu.</span><span class="sxs-lookup"><span data-stu-id="eec71-141">If `AccessTheWebAsync` doesn't have any work that it can do between calling `GetStringAsync` and awaiting its completion, you can simplify your code by calling and awaiting in the following single statement.</span></span>

```vb
Dim urlContents As String = Await client.GetStringAsync()
```

<span data-ttu-id="eec71-142">Následující charakteristiky shrnují, co dělá předchozí příklad asynchronní metodou:</span><span class="sxs-lookup"><span data-stu-id="eec71-142">The following characteristics summarize what makes the previous example an async method:</span></span>

- <span data-ttu-id="eec71-143">Podpis metody obsahuje `Async` modifikátor.</span><span class="sxs-lookup"><span data-stu-id="eec71-143">The method signature includes an `Async` modifier.</span></span>
- <span data-ttu-id="eec71-144">Název asynchronní metody končí podle konvence příponou „Async“.</span><span class="sxs-lookup"><span data-stu-id="eec71-144">The name of an async method, by convention, ends with an "Async" suffix.</span></span>
- <span data-ttu-id="eec71-145">Návratový typ je jeden z následujících typů:</span><span class="sxs-lookup"><span data-stu-id="eec71-145">The return type is one of the following types:</span></span>

  - <span data-ttu-id="eec71-146">[Task(Of TResult),](xref:System.Threading.Tasks.Task%601) pokud vaše metoda má return prohlášení, ve kterém operand má typ TResult.</span><span class="sxs-lookup"><span data-stu-id="eec71-146">[Task(Of TResult)](xref:System.Threading.Tasks.Task%601) if your method has a return statement in which the operand has type TResult.</span></span>
  - <span data-ttu-id="eec71-147"><xref:System.Threading.Tasks.Task>Pokud vaše metoda nemá return příkaz nebo má návratový příkaz bez operandu.</span><span class="sxs-lookup"><span data-stu-id="eec71-147"><xref:System.Threading.Tasks.Task> if your method has no return statement or has a return statement with no operand.</span></span>
  - <span data-ttu-id="eec71-148">[Sub,](../../../../visual-basic/programming-guide/language-features/procedures/sub-procedures.md) pokud píšete asynchronní obslužnou rutinu události.</span><span class="sxs-lookup"><span data-stu-id="eec71-148">[Sub](../../../../visual-basic/programming-guide/language-features/procedures/sub-procedures.md) if you're writing an async event handler.</span></span>

  <span data-ttu-id="eec71-149">Další informace naleznete v části „Návratové typy a parametry“ dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="eec71-149">For more information, see "Return Types and Parameters" later in this topic.</span></span>

- <span data-ttu-id="eec71-150">Metoda obvykle zahrnuje nejméně jeden očekávaný výraz, který označuje bod, kde metoda nemůže pokračovat, dokud očekávaná asynchronní operace nebude dokončena.</span><span class="sxs-lookup"><span data-stu-id="eec71-150">The method usually includes at least one await expression, which marks a point where the method can't continue until the awaited asynchronous operation is complete.</span></span> <span data-ttu-id="eec71-151">Během této doby je metoda pozastavena a ovládací prvek se vrátí volajícímu metody.</span><span class="sxs-lookup"><span data-stu-id="eec71-151">In the meantime, the method is suspended, and control returns to the method's caller.</span></span> <span data-ttu-id="eec71-152">Další část tohoto tématu ukazuje, co se stane v okamžiku pozastavení.</span><span class="sxs-lookup"><span data-stu-id="eec71-152">The next section of this topic illustrates what happens at the suspension point.</span></span>

<span data-ttu-id="eec71-153">V asynchronních metodách používáte zadaná klíčová slova a typy pro označení, jakou akci chcete provést, a kompilátor udělá zbytek, včetně udržování přehledu o tom, co musí nastat, když se řízení vrátí do bodu „await“ pozastavené metody.</span><span class="sxs-lookup"><span data-stu-id="eec71-153">In async methods, you use the provided keywords and types to indicate what you want to do, and the compiler does the rest, including keeping track of what must happen when control returns to an await point in a suspended method.</span></span> <span data-ttu-id="eec71-154">Některé běžné procesy, jako je zpracování smyček a výjimek, může být v tradičním asynchronním kódu obtížné zpracovat.</span><span class="sxs-lookup"><span data-stu-id="eec71-154">Some routine processes, such as loops and exception handling, can be difficult to handle in traditional asynchronous code.</span></span> <span data-ttu-id="eec71-155">V asynchronní metodě zapisujete tyto prvky podobně, jako byste to udělali v synchronním řešení, a problém je vyřešen.</span><span class="sxs-lookup"><span data-stu-id="eec71-155">In an async method, you write these elements much as you would in a synchronous solution, and the problem is solved.</span></span>

<span data-ttu-id="eec71-156">Další informace o asynchronii v předchozích verzích rozhraní .NET Framework naleznete v [tématech TPL a Traditional .NET Framework AsynchronouProgramming](../../../../standard/parallel-programming/tpl-and-traditional-async-programming.md).</span><span class="sxs-lookup"><span data-stu-id="eec71-156">For more information about asynchrony in previous versions of the .NET Framework, see [TPL and Traditional .NET Framework Asynchronous Programming](../../../../standard/parallel-programming/tpl-and-traditional-async-programming.md).</span></span>

## <a name="what-happens-in-an-async-method"></a><a name="BKMK_WhatHappensUnderstandinganAsyncMethod"></a><span data-ttu-id="eec71-157">Co se děje v asynchronní metodě</span><span class="sxs-lookup"><span data-stu-id="eec71-157">What happens in an Async method</span></span>

<span data-ttu-id="eec71-158">Nejdůležitějším principem, který je třeba pochopit v asynchronním programování, je, jak ovládat přesuny toků od metody k metodě.</span><span class="sxs-lookup"><span data-stu-id="eec71-158">The most important thing to understand in asynchronous programming is how the control flow moves from method to method.</span></span> <span data-ttu-id="eec71-159">Následující diagram vás provede procesem:</span><span class="sxs-lookup"><span data-stu-id="eec71-159">The following diagram leads you through the process:</span></span>

![Diagram, který ukazuje sledování asynchronního programu.](./media/index/navigation-trace-async-program.png)

<span data-ttu-id="eec71-161">Čísla v diagramu odpovídají následujícím krokům:</span><span class="sxs-lookup"><span data-stu-id="eec71-161">The numbers in the diagram correspond to the following steps:</span></span>

1. <span data-ttu-id="eec71-162">Obslužná rutina `AccessTheWebAsync` události volá a čeká na asynchronní metodu.</span><span class="sxs-lookup"><span data-stu-id="eec71-162">An event handler calls and awaits the `AccessTheWebAsync` async method.</span></span>

2. <span data-ttu-id="eec71-163">`AccessTheWebAsync`vytvoří <xref:System.Net.Http.HttpClient> instanci <xref:System.Net.Http.HttpClient.GetStringAsync%2A> a zavolá asynchronní metodu ke stažení obsahu webu jako řetězec.</span><span class="sxs-lookup"><span data-stu-id="eec71-163">`AccessTheWebAsync` creates an <xref:System.Net.Http.HttpClient> instance and calls the <xref:System.Net.Http.HttpClient.GetStringAsync%2A> asynchronous method to download the contents of a website as a string.</span></span>

3. <span data-ttu-id="eec71-164">Něco se `GetStringAsync` stane v tom, že pozastavuje jeho pokrok.</span><span class="sxs-lookup"><span data-stu-id="eec71-164">Something happens in `GetStringAsync` that suspends its progress.</span></span> <span data-ttu-id="eec71-165">Možná je třeba vyčkat na dokončení stahování nebo jiné blokující aktivity na webu.</span><span class="sxs-lookup"><span data-stu-id="eec71-165">Perhaps it must wait for a website to download or some other blocking activity.</span></span> <span data-ttu-id="eec71-166">Chcete-li se `GetStringAsync` vyhnout blokování prostředků, `AccessTheWebAsync`dává ovládací prvek jeho volajícímu , .</span><span class="sxs-lookup"><span data-stu-id="eec71-166">To avoid blocking resources, `GetStringAsync` yields control to its caller, `AccessTheWebAsync`.</span></span>

     <span data-ttu-id="eec71-167">`GetStringAsync`vrátí [task(Of TResult),](xref:System.Threading.Tasks.Task%601) kde TResult je `AccessTheWebAsync` řetězec a přiřadí úkol `getStringTask` proměnné.</span><span class="sxs-lookup"><span data-stu-id="eec71-167">`GetStringAsync` returns a [Task(Of TResult)](xref:System.Threading.Tasks.Task%601) where TResult is a string, and `AccessTheWebAsync` assigns the task to the `getStringTask` variable.</span></span> <span data-ttu-id="eec71-168">Úkol představuje probíhající proces volání `GetStringAsync`do , se závazkem vytvořit skutečnou hodnotu řetězce po dokončení práce.</span><span class="sxs-lookup"><span data-stu-id="eec71-168">The task represents the ongoing process for the call to `GetStringAsync`, with a commitment to produce an actual string value when the work is complete.</span></span>

4. <span data-ttu-id="eec71-169">Vzhledem k tomu, `getStringTask` nebyl `AccessTheWebAsync` dosud očekáván, může pokračovat v jiné `GetStringAsync`práci, která nezávisí na konečný výsledek z .</span><span class="sxs-lookup"><span data-stu-id="eec71-169">Because `getStringTask` hasn't been awaited yet, `AccessTheWebAsync` can continue with other work that doesn't depend on the final result from `GetStringAsync`.</span></span> <span data-ttu-id="eec71-170">Tato práce je reprezentována voláním `DoIndependentWork`synchronní metody .</span><span class="sxs-lookup"><span data-stu-id="eec71-170">That work is represented by a call to the synchronous method `DoIndependentWork`.</span></span>

5. <span data-ttu-id="eec71-171">`DoIndependentWork`je synchronní metoda, která provádí svou práci a vrátí se volajícímu.</span><span class="sxs-lookup"><span data-stu-id="eec71-171">`DoIndependentWork` is a synchronous method that does its work and returns to its caller.</span></span>

6. <span data-ttu-id="eec71-172">`AccessTheWebAsync`vyčerpala práci, kterou může provést bez `getStringTask`výsledku .</span><span class="sxs-lookup"><span data-stu-id="eec71-172">`AccessTheWebAsync` has run out of work that it can do without a result from `getStringTask`.</span></span> <span data-ttu-id="eec71-173">`AccessTheWebAsync`Další chce vypočítat a vrátit délku staženého řetězce, ale metoda nemůže vypočítat tuto hodnotu, dokud metoda má řetězec.</span><span class="sxs-lookup"><span data-stu-id="eec71-173">`AccessTheWebAsync` next wants to calculate and return the length of the downloaded string, but the method can't calculate that value until the method has the string.</span></span>

     <span data-ttu-id="eec71-174">Proto `AccessTheWebAsync` používá operátor await pozastavit jeho průběh a výnos `AccessTheWebAsync`řízení metody, která volala .</span><span class="sxs-lookup"><span data-stu-id="eec71-174">Therefore, `AccessTheWebAsync` uses an await operator to suspend its progress and to yield control to the method that called `AccessTheWebAsync`.</span></span> <span data-ttu-id="eec71-175">`AccessTheWebAsync`vrátí `Task(Of Integer)` volajícímu.</span><span class="sxs-lookup"><span data-stu-id="eec71-175">`AccessTheWebAsync` returns a `Task(Of Integer)` to the caller.</span></span> <span data-ttu-id="eec71-176">Úloha představuje slib vyrábět celé číslo výsledku, který má délku staženého řetězce.</span><span class="sxs-lookup"><span data-stu-id="eec71-176">The task represents a promise to produce an integer result that's the length of the downloaded string.</span></span>

    > [!NOTE]
    > <span data-ttu-id="eec71-177">Pokud `GetStringAsync` (a `getStringTask`proto ) `AccessTheWebAsync` je dokončena před `AccessTheWebAsync`čeká, kontrola zůstává v .</span><span class="sxs-lookup"><span data-stu-id="eec71-177">If `GetStringAsync` (and therefore `getStringTask`) is complete before `AccessTheWebAsync` awaits it, control remains in `AccessTheWebAsync`.</span></span> <span data-ttu-id="eec71-178">Náklady na pozastavení a potom `AccessTheWebAsync` návratu k by být zbytečné, pokud`getStringTask`se nazývá asynchronní proces ( ) již byla dokončena a AccessTheWebSync nemusí čekat na konečný výsledek.</span><span class="sxs-lookup"><span data-stu-id="eec71-178">The expense of suspending and then returning to `AccessTheWebAsync` would be wasted if the called asynchronous process (`getStringTask`) has already completed and AccessTheWebSync doesn't have to wait for the final result.</span></span>

     <span data-ttu-id="eec71-179">Uvnitř volajícího (v tomto případě obslužná rutina události) bude vzor zpracování pokračovat.</span><span class="sxs-lookup"><span data-stu-id="eec71-179">Inside the caller (the event handler in this example), the processing pattern continues.</span></span> <span data-ttu-id="eec71-180">Volající může dělat jinou práci, která nezávisí na výsledku před `AccessTheWebAsync` čekáním na tento výsledek, nebo volající může čekat okamžitě.</span><span class="sxs-lookup"><span data-stu-id="eec71-180">The caller might do other work that doesn't depend on the result from `AccessTheWebAsync` before awaiting that result, or the caller might await immediately.</span></span>   <span data-ttu-id="eec71-181">Obslužná `AccessTheWebAsync`rutina `AccessTheWebAsync` události `GetStringAsync`čeká na a čeká na .</span><span class="sxs-lookup"><span data-stu-id="eec71-181">The event handler is waiting for `AccessTheWebAsync`, and `AccessTheWebAsync` is waiting for `GetStringAsync`.</span></span>

7. <span data-ttu-id="eec71-182">`GetStringAsync`dokončí a vytvoří výsledek řetězce.</span><span class="sxs-lookup"><span data-stu-id="eec71-182">`GetStringAsync` completes and produces a string result.</span></span> <span data-ttu-id="eec71-183">Výsledek řetězce není vrácena volání `GetStringAsync` mj.</span><span class="sxs-lookup"><span data-stu-id="eec71-183">The string result isn't returned by the call to `GetStringAsync` in the way that you might expect.</span></span> <span data-ttu-id="eec71-184">(Nezapomeňte, že metoda již vrátila úkol v kroku 3.) Místo toho výsledek řetězce je uložen v úloze, `getStringTask`která představuje dokončení metody , .</span><span class="sxs-lookup"><span data-stu-id="eec71-184">(Remember that the method already returned a task in step 3.) Instead, the string result is stored in the task that represents the completion of the method, `getStringTask`.</span></span> <span data-ttu-id="eec71-185">Operátor await načte výsledek `getStringTask`z .</span><span class="sxs-lookup"><span data-stu-id="eec71-185">The await operator retrieves the result from `getStringTask`.</span></span> <span data-ttu-id="eec71-186">Příkaz přiřazení přiřadí načtený `urlContents`výsledek společnosti .</span><span class="sxs-lookup"><span data-stu-id="eec71-186">The assignment statement assigns the retrieved result to `urlContents`.</span></span>

8. <span data-ttu-id="eec71-187">Když `AccessTheWebAsync` má výsledek řetězce, metoda může vypočítat délku řetězce.</span><span class="sxs-lookup"><span data-stu-id="eec71-187">When `AccessTheWebAsync` has the string result, the method can calculate the length of the string.</span></span> <span data-ttu-id="eec71-188">Pak je `AccessTheWebAsync` také dokončena práce a obslužná rutina události čekání může pokračovat.</span><span class="sxs-lookup"><span data-stu-id="eec71-188">Then the work of `AccessTheWebAsync` is also complete, and the waiting event handler can resume.</span></span> <span data-ttu-id="eec71-189">V úplném příkladu na konci tématu si můžete potvrdit, že obslužná rutina události načte a vytiskne hodnotu výsledné délky.</span><span class="sxs-lookup"><span data-stu-id="eec71-189">In the full example at the end of the topic, you can confirm that the event handler retrieves and prints the value of the length result.</span></span>

<span data-ttu-id="eec71-190">Pokud jste v oblasti asynchronního programování nováčky, zvažte rozdíl mezi synchronním a asynchronním chováním.</span><span class="sxs-lookup"><span data-stu-id="eec71-190">If you are new to asynchronous programming, take a minute to consider the difference between synchronous and asynchronous behavior.</span></span> <span data-ttu-id="eec71-191">Synchronní metoda je vrácena, jakmile je její práce dokončena (krok 5), ale asynchronní metoda vrátí hodnotu úlohy, když je její práce pozastavena (kroky 3 a 6).</span><span class="sxs-lookup"><span data-stu-id="eec71-191">A synchronous method returns when its work is complete (step 5), but an async method returns a task value when its work is suspended (steps 3 and 6).</span></span> <span data-ttu-id="eec71-192">Když asynchronní metoda nakonec dokončí svou práci, je úloha označena jako dokončená a výsledek, pokud existuje, je uložen v úloze.</span><span class="sxs-lookup"><span data-stu-id="eec71-192">When the async method eventually completes its work, the task is marked as completed and the result, if any, is stored in the task.</span></span>

<span data-ttu-id="eec71-193">Další informace o toku řízení naleznete [v tématu Řízení toku v asynchronních programech (Visual Basic).](control-flow-in-async-programs.md)</span><span class="sxs-lookup"><span data-stu-id="eec71-193">For more information about control flow, see [Control Flow in Async Programs (Visual Basic)](control-flow-in-async-programs.md).</span></span>

## <a name="api-async-methods"></a><a name="BKMK_APIAsyncMethods"></a><span data-ttu-id="eec71-194">Asynchronní metody rozhraní API</span><span class="sxs-lookup"><span data-stu-id="eec71-194">API Async Methods</span></span>

<span data-ttu-id="eec71-195">Možná se divíte, kde `GetStringAsync` najít metody, jako je podpora asynchronníprogramování.</span><span class="sxs-lookup"><span data-stu-id="eec71-195">You might be wondering where to find methods such as `GetStringAsync` that support async programming.</span></span> <span data-ttu-id="eec71-196">Rozhraní .NET Framework 4.5 nebo vyšší `Async` obsahuje `Await`mnoho členů, kteří pracují s a .</span><span class="sxs-lookup"><span data-stu-id="eec71-196">The .NET Framework 4.5 or higher contains many members that work with `Async` and `Await`.</span></span> <span data-ttu-id="eec71-197">Tyto členy můžete rozpoznat podle přípony "Async", která je připojena k <xref:System.Threading.Tasks.Task> názvu člena a návratový typ nebo [Task(Of TResult)](xref:System.Threading.Tasks.Task%601).</span><span class="sxs-lookup"><span data-stu-id="eec71-197">You can recognize these members by the "Async" suffix that's attached to the member name and a return type of <xref:System.Threading.Tasks.Task> or [Task(Of TResult)](xref:System.Threading.Tasks.Task%601).</span></span> <span data-ttu-id="eec71-198">`System.IO.Stream` Třída například obsahuje metody <xref:System.IO.Stream.CopyToAsync%2A>jako <xref:System.IO.Stream.ReadAsync%2A>, <xref:System.IO.Stream.WriteAsync%2A> , a vedle <xref:System.IO.Stream.CopyTo%2A>synchronních metod , <xref:System.IO.Stream.Read%2A>a <xref:System.IO.Stream.Write%2A>.</span><span class="sxs-lookup"><span data-stu-id="eec71-198">For example, the `System.IO.Stream` class contains methods such as <xref:System.IO.Stream.CopyToAsync%2A>, <xref:System.IO.Stream.ReadAsync%2A>, and <xref:System.IO.Stream.WriteAsync%2A> alongside the synchronous methods <xref:System.IO.Stream.CopyTo%2A>, <xref:System.IO.Stream.Read%2A>, and <xref:System.IO.Stream.Write%2A>.</span></span>

<span data-ttu-id="eec71-199">Prostředí Windows Runtime také obsahuje mnoho `Async` metod, které můžete použít s aplikacemi pro Windows a `Await` v nich.</span><span class="sxs-lookup"><span data-stu-id="eec71-199">The Windows Runtime also contains many methods that you can use with `Async` and `Await` in Windows apps.</span></span> <span data-ttu-id="eec71-200">Další informace a ukázkové metody naleznete [v tématech Volání asynchronních rozhraní API v jazyce C# nebo Visual Basic](/windows/uwp/threading-async/call-asynchronous-apis-in-csharp-or-visual-basic), [Asynchronní programování (aplikace prostředí Windows Runtime)](https://docs.microsoft.com/previous-versions/windows/apps/hh464924(v=win.10))a [WhenAny: Přemostění mezi rozhraním .NET Framework a prostředím Windows Runtime](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/jj635140(v=vs.120)).</span><span class="sxs-lookup"><span data-stu-id="eec71-200">For more information and example methods, see [Call asynchronous APIs in C# or Visual Basic](/windows/uwp/threading-async/call-asynchronous-apis-in-csharp-or-visual-basic), [Asynchronous programming (Windows Runtime apps)](https://docs.microsoft.com/previous-versions/windows/apps/hh464924(v=win.10)), and [WhenAny: Bridging between the .NET Framework and the Windows Runtime](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/jj635140(v=vs.120)).</span></span>

## <a name="threads"></a><a name="BKMK_Threads"></a><span data-ttu-id="eec71-201">Vlákna</span><span class="sxs-lookup"><span data-stu-id="eec71-201">Threads</span></span>

<span data-ttu-id="eec71-202">Asynchronní metody mají být neblokující operace.</span><span class="sxs-lookup"><span data-stu-id="eec71-202">Async methods are intended to be non-blocking operations.</span></span> <span data-ttu-id="eec71-203">Výraz `Await` v asynchronní metodě neblokuje aktuální vlákno, když je spuštěna očekávaná úloha.</span><span class="sxs-lookup"><span data-stu-id="eec71-203">An `Await` expression in an async method doesn't block the current thread while the awaited task is running.</span></span> <span data-ttu-id="eec71-204">Namísto toho se výraz zaregistruje pro zbývající metody jako pokračování a vrátí řízení volajícímu asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="eec71-204">Instead, the expression signs up the rest of the method as a continuation and returns control to the caller of the async method.</span></span>

<span data-ttu-id="eec71-205">Klíčová `Async` `Await` slova a nezpůsobí další vlákna, která mají být vytvořena.</span><span class="sxs-lookup"><span data-stu-id="eec71-205">The `Async` and `Await` keywords don't cause additional threads to be created.</span></span> <span data-ttu-id="eec71-206">Asynchronní metody nevyžadují vícevláknové, protože asynchronní metoda neběží ve vlastním vlákně.</span><span class="sxs-lookup"><span data-stu-id="eec71-206">Async methods don't require multi-threading because an async method doesn't run on its own thread.</span></span> <span data-ttu-id="eec71-207">Metoda pracuje na aktuálním kontextu synchronizace a používá čas ve vlákně pouze v případě, že je metoda aktivní.</span><span class="sxs-lookup"><span data-stu-id="eec71-207">The method runs on the current synchronization context and uses time on the thread only when the method is active.</span></span> <span data-ttu-id="eec71-208">Můžete použít <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> k přesunutí práce vázané na procesor do vlákna na pozadí, ale vlákno na pozadí nepomůže s procesem, který čeká na výsledky, které budou k dispozici.</span><span class="sxs-lookup"><span data-stu-id="eec71-208">You can use <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> to move CPU-bound work to a background thread, but a background thread doesn't help with a process that's just waiting for results to become available.</span></span>

<span data-ttu-id="eec71-209">Asynchronní přístup při asynchronním programování se doporučuje v téměř každém případě existujících přístupů.</span><span class="sxs-lookup"><span data-stu-id="eec71-209">The async-based approach to asynchronous programming is preferable to existing approaches in almost every case.</span></span> <span data-ttu-id="eec71-210">Zejména tento přístup je <xref:System.ComponentModel.BackgroundWorker> lepší než pro operace vstupně-výstupních operací, protože kód je jednodušší a není třeba chránit před sporáky.</span><span class="sxs-lookup"><span data-stu-id="eec71-210">In particular, this approach is better than <xref:System.ComponentModel.BackgroundWorker> for I/O-bound operations because the code is simpler and you don't have to guard against race conditions.</span></span> <span data-ttu-id="eec71-211">V kombinaci s <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType>asynchronní <xref:System.ComponentModel.BackgroundWorker> programování je lepší než pro operace vázané na procesor, protože `Task.Run` asynchronní programování odděluje podrobnosti koordinace spuštění kódu z práce, která se přenáší do fondu vláken.</span><span class="sxs-lookup"><span data-stu-id="eec71-211">In combination with <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType>, async programming is better than <xref:System.ComponentModel.BackgroundWorker> for CPU-bound operations because async programming separates the coordination details of running your code from the work that `Task.Run` transfers to the threadpool.</span></span>

## <a name="async-and-await"></a><a name="BKMK_AsyncandAwait"></a><span data-ttu-id="eec71-212">Asynchronní a Čekají</span><span class="sxs-lookup"><span data-stu-id="eec71-212">Async and Await</span></span>

<span data-ttu-id="eec71-213">Pokud zadáte, že metoda je asynchronní metoda pomocí modifikátoru [Async,](../../../../visual-basic/language-reference/modifiers/async.md) povolte následující dvě možnosti.</span><span class="sxs-lookup"><span data-stu-id="eec71-213">If you specify that a method is an async method by using an [Async](../../../../visual-basic/language-reference/modifiers/async.md) modifier, you enable the following two capabilities.</span></span>

- <span data-ttu-id="eec71-214">Označená asynchronní metoda může použít [Await](../../../../visual-basic/language-reference/operators/await-operator.md) k určení bodů pozastavení.</span><span class="sxs-lookup"><span data-stu-id="eec71-214">The marked async method can use [Await](../../../../visual-basic/language-reference/operators/await-operator.md) to designate suspension points.</span></span> <span data-ttu-id="eec71-215">Operátor await sděluje kompilátoru, že s asynchronními metodami nelze za daným bodem pokračovat, dokud nebude dokončen očekávaný asynchronní proces.</span><span class="sxs-lookup"><span data-stu-id="eec71-215">The await operator tells the compiler that the async method can't continue past that point until the awaited asynchronous process is complete.</span></span> <span data-ttu-id="eec71-216">Během této doby se ovládací prvek vrátí volajícímu asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="eec71-216">In the meantime, control returns to the caller of the async method.</span></span>

  <span data-ttu-id="eec71-217">Pozastavení asynchronní metody ve `Await` výrazu nepředstavuje výstup z metody `Finally` a bloky nespustí.</span><span class="sxs-lookup"><span data-stu-id="eec71-217">The suspension of an async method at an `Await` expression doesn't constitute an exit from the method, and `Finally` blocks don't run.</span></span>

- <span data-ttu-id="eec71-218">Samotná označená asynchronní metoda může být očekávána metodami, které ji volaly.</span><span class="sxs-lookup"><span data-stu-id="eec71-218">The marked async method can itself be awaited by methods that call it.</span></span>

<span data-ttu-id="eec71-219">Asynchronní metoda obvykle obsahuje jeden nebo `Await` více výskytů operátoru, ale absence výrazů `Await` nezpůsobí chybu kompilátoru.</span><span class="sxs-lookup"><span data-stu-id="eec71-219">An async method typically contains one or more occurrences of an `Await` operator, but the absence of `Await` expressions doesn't cause a compiler error.</span></span> <span data-ttu-id="eec71-220">Pokud asynchronní metoda nepoužívá `Await` operátor k označení bodu pozastavení, metoda se provede jako synchronní `Async` metoda, navzdory modifikátoru.</span><span class="sxs-lookup"><span data-stu-id="eec71-220">If an async method doesn't use an `Await` operator to mark a suspension point, the method executes as a synchronous method does, despite the `Async` modifier.</span></span> <span data-ttu-id="eec71-221">Kompilátor u takových metod zahlásí upozornění.</span><span class="sxs-lookup"><span data-stu-id="eec71-221">The compiler issues a warning for such methods.</span></span>

<span data-ttu-id="eec71-222">`Async`a `Await` jsou kontextová klíčová slova.</span><span class="sxs-lookup"><span data-stu-id="eec71-222">`Async` and `Await` are contextual keywords.</span></span> <span data-ttu-id="eec71-223">Další informace a příklady naleznete v následujících tématech:</span><span class="sxs-lookup"><span data-stu-id="eec71-223">For more information and examples, see the following topics:</span></span>

- [<span data-ttu-id="eec71-224">Asynchronní</span><span class="sxs-lookup"><span data-stu-id="eec71-224">Async</span></span>](../../../../visual-basic/language-reference/modifiers/async.md)
- [<span data-ttu-id="eec71-225">Čekat operátora</span><span class="sxs-lookup"><span data-stu-id="eec71-225">Await Operator</span></span>](../../../../visual-basic/language-reference/operators/await-operator.md)

## <a name="return-types-and-parameters"></a><a name="BKMK_ReturnTypesandParameters"></a><span data-ttu-id="eec71-226">Návratové typy a parametry</span><span class="sxs-lookup"><span data-stu-id="eec71-226">Return types and parameters</span></span>

<span data-ttu-id="eec71-227">V programování rozhraní .NET Framework asynchronní <xref:System.Threading.Tasks.Task> metoda obvykle vrací nebo [Task(Of TResult)](xref:System.Threading.Tasks.Task%601).</span><span class="sxs-lookup"><span data-stu-id="eec71-227">In .NET Framework programming, an async method typically returns a <xref:System.Threading.Tasks.Task> or a [Task(Of TResult)](xref:System.Threading.Tasks.Task%601).</span></span> <span data-ttu-id="eec71-228">Uvnitř asynchronní metody `Await` operátor je použita na úlohu, která je vrácena z volání do jiné asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="eec71-228">Inside an async method, an `Await` operator is applied to a task that's returned from a call to another async method.</span></span>

<span data-ttu-id="eec71-229">[Úkol(TResult)](xref:System.Threading.Tasks.Task%601) zadáte jako návratový typ, pokud metoda obsahuje [Return](../../../../visual-basic/language-reference/statements/return-statement.md) příkaz, `TResult`který určuje operand typu .</span><span class="sxs-lookup"><span data-stu-id="eec71-229">You specify [Task(Of TResult)](xref:System.Threading.Tasks.Task%601) as the return type if the method contains a [Return](../../../../visual-basic/language-reference/statements/return-statement.md) statement that specifies an operand of type `TResult`.</span></span>

<span data-ttu-id="eec71-230">Použijete `Task` jako návratový typ, pokud metoda nemá žádný příkaz return nebo má příkaz return, který nevrací operand.</span><span class="sxs-lookup"><span data-stu-id="eec71-230">You use `Task` as the return type if the method has no return statement or has a return statement that doesn't return an operand.</span></span>

<span data-ttu-id="eec71-231">Následující příklad ukazuje, jak deklarovat a volat metodu, <xref:System.Threading.Tasks.Task>která vrací [Task(Of TResult)](xref:System.Threading.Tasks.Task%601) nebo :</span><span class="sxs-lookup"><span data-stu-id="eec71-231">The following example shows how you declare and call a method that returns a [Task(Of TResult)](xref:System.Threading.Tasks.Task%601) or a <xref:System.Threading.Tasks.Task>:</span></span>

```vb
' Signature specifies Task(Of Integer)
Async Function TaskOfTResult_MethodAsync() As Task(Of Integer)

    Dim hours As Integer
    ' . . .
    ' Return statement specifies an integer result.
    Return hours
End Function

' Calls to TaskOfTResult_MethodAsync
Dim returnedTaskTResult As Task(Of Integer) = TaskOfTResult_MethodAsync()
Dim intResult As Integer = Await returnedTaskTResult
' or, in a single statement
Dim intResult As Integer = Await TaskOfTResult_MethodAsync()

' Signature specifies Task
Async Function Task_MethodAsync() As Task

    ' . . .
    ' The method has no return statement.
End Function

' Calls to Task_MethodAsync
Task returnedTask = Task_MethodAsync()
Await returnedTask
' or, in a single statement
Await Task_MethodAsync()
```

<span data-ttu-id="eec71-232">Každá vrácená úloha představuje probíhající práci.</span><span class="sxs-lookup"><span data-stu-id="eec71-232">Each returned task represents ongoing work.</span></span> <span data-ttu-id="eec71-233">Úloha zapouzdřuje informace o stavu asynchronního procesu a posléze buď konečný výsledek z procesu, nebo výjimku, kterou proces vyvolá v případě neúspěchu.</span><span class="sxs-lookup"><span data-stu-id="eec71-233">A task encapsulates information about the state of the asynchronous process and, eventually, either the final result from the process or the exception that the process raises if it doesn't succeed.</span></span>

<span data-ttu-id="eec71-234">Asynchronní metoda může `Sub` být také metoda.</span><span class="sxs-lookup"><span data-stu-id="eec71-234">An async method can also be a `Sub` method.</span></span> <span data-ttu-id="eec71-235">Tento návratový typ se používá především k definování obslužné rutiny událostí, kde je vyžadován návratový typ.</span><span class="sxs-lookup"><span data-stu-id="eec71-235">This return type is used primarily to define event handlers, where a return type is required.</span></span> <span data-ttu-id="eec71-236">Asynchronní obslužné rutiny událostí často slouží jako výchozí bod pro asynchronní programy.</span><span class="sxs-lookup"><span data-stu-id="eec71-236">Async event handlers often serve as the starting point for async programs.</span></span>

<span data-ttu-id="eec71-237">Asynchronní metoda, která `Sub` je procedura nelze čekat a volající nemůže zachytit žádné výjimky, které metoda vyvolá.</span><span class="sxs-lookup"><span data-stu-id="eec71-237">An async method that's a `Sub` procedure can't be awaited, and the caller can't catch any exceptions that the method throws.</span></span>

<span data-ttu-id="eec71-238">Asynchronní metoda nemůže deklarovat [Parametry ByRef,](../../../../visual-basic/language-reference/modifiers/byref.md) ale metoda může volat metody, které mají takové parametry.</span><span class="sxs-lookup"><span data-stu-id="eec71-238">An async method can't declare [ByRef](../../../../visual-basic/language-reference/modifiers/byref.md) parameters, but the method can call methods that have such parameters.</span></span>

<span data-ttu-id="eec71-239">Další informace a příklady naleznete [v tématu Asynchronní návratové typy (Visual Basic)](async-return-types.md).</span><span class="sxs-lookup"><span data-stu-id="eec71-239">For more information and examples, see [Async Return Types (Visual Basic)](async-return-types.md).</span></span> <span data-ttu-id="eec71-240">Další informace o tom, jak zachytit výjimky v asynchronních metodách, naleznete v [tématu Try... Chytit... Finally prohlášení](../../../../visual-basic/language-reference/statements/try-catch-finally-statement.md).</span><span class="sxs-lookup"><span data-stu-id="eec71-240">For more information about how to catch exceptions in async methods, see [Try...Catch...Finally Statement](../../../../visual-basic/language-reference/statements/try-catch-finally-statement.md).</span></span>

<span data-ttu-id="eec71-241">Asynchronní rozhraní API v programování prostředí Windows Runtime mají jeden z následujících návratových typů, které jsou podobné úlohám:</span><span class="sxs-lookup"><span data-stu-id="eec71-241">Asynchronous APIs in Windows Runtime programming have one of the following return types, which are similar to tasks:</span></span>

- <span data-ttu-id="eec71-242">[IAsyncOperation(Of TResult)](xref:Windows.Foundation.IAsyncOperation%601), který odpovídá [Task(Of TResult)](xref:System.Threading.Tasks.Task%601)</span><span class="sxs-lookup"><span data-stu-id="eec71-242">[IAsyncOperation(Of TResult)](xref:Windows.Foundation.IAsyncOperation%601), which corresponds to [Task(Of TResult)](xref:System.Threading.Tasks.Task%601)</span></span>
- <span data-ttu-id="eec71-243"><xref:Windows.Foundation.IAsyncAction>, což odpovídá<xref:System.Threading.Tasks.Task></span><span class="sxs-lookup"><span data-stu-id="eec71-243"><xref:Windows.Foundation.IAsyncAction>, which corresponds to <xref:System.Threading.Tasks.Task></span></span>
- [<span data-ttu-id="eec71-244">IAsyncActionWithProgress(Z TProgress)</span><span class="sxs-lookup"><span data-stu-id="eec71-244">IAsyncActionWithProgress(Of TProgress)</span></span>](xref:Windows.Foundation.IAsyncActionWithProgress%601)
- [<span data-ttu-id="eec71-245">IAsyncOperationWithProgress(Of TResult, TProgress)</span><span class="sxs-lookup"><span data-stu-id="eec71-245">IAsyncOperationWithProgress(Of TResult, TProgress)</span></span>](xref:Windows.Foundation.IAsyncOperationWithProgress%602)

<span data-ttu-id="eec71-246">Další informace a příklad naleznete v [tématu Volání asynchronních api v jazyce C# nebo visual basicu](/windows/uwp/threading-async/call-asynchronous-apis-in-csharp-or-visual-basic).</span><span class="sxs-lookup"><span data-stu-id="eec71-246">For more information and an example, see [Call asynchronous APIs in C# or Visual Basic](/windows/uwp/threading-async/call-asynchronous-apis-in-csharp-or-visual-basic).</span></span>

## <a name="naming-convention"></a><a name="BKMK_NamingConvention"></a><span data-ttu-id="eec71-247">Konvence</span><span class="sxs-lookup"><span data-stu-id="eec71-247">Naming convention</span></span>

<span data-ttu-id="eec71-248">Podle konvence připojíte "Async" k názvům metod, které mají `Async` modifikátor.</span><span class="sxs-lookup"><span data-stu-id="eec71-248">By convention, you append "Async" to the names of methods that have an `Async` modifier.</span></span>

<span data-ttu-id="eec71-249">Můžete ignorovat konvenci, kde událost, základní třída a rozhraní smlouvy navrhují odlišný název.</span><span class="sxs-lookup"><span data-stu-id="eec71-249">You can ignore the convention where an event, base class, or interface contract suggests a different name.</span></span> <span data-ttu-id="eec71-250">Například byste neměli přejmenovat běžné obslužné rutiny událostí, například `Button1_Click`.</span><span class="sxs-lookup"><span data-stu-id="eec71-250">For example, you shouldn't rename common event handlers, such as `Button1_Click`.</span></span>

## <a name="related-topics-and-samples-visual-studio"></a><a name="BKMK_RelatedTopics"></a><span data-ttu-id="eec71-251">Příbuzná témata a ukázky (Visual Studio)</span><span class="sxs-lookup"><span data-stu-id="eec71-251">Related topics and samples (Visual Studio)</span></span>

|<span data-ttu-id="eec71-252">Nadpis</span><span class="sxs-lookup"><span data-stu-id="eec71-252">Title</span></span>|<span data-ttu-id="eec71-253">Popis</span><span class="sxs-lookup"><span data-stu-id="eec71-253">Description</span></span>|<span data-ttu-id="eec71-254">Ukázka</span><span class="sxs-lookup"><span data-stu-id="eec71-254">Sample</span></span>|
|-----------|-----------------|------------|
|[<span data-ttu-id="eec71-255">Návod: Přístup k webu pomocí asynchronní a čeká (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eec71-255">Walkthrough: Accessing the Web by Using Async and Await (Visual Basic)</span></span>](walkthrough-accessing-the-web-by-using-async-and-await.md)|<span data-ttu-id="eec71-256">Ukazuje, jak převést synchronní řešení WPF na asynchronní řešení WPF.</span><span class="sxs-lookup"><span data-stu-id="eec71-256">Shows how to convert a synchronous WPF solution to an asynchronous WPF solution.</span></span> <span data-ttu-id="eec71-257">Aplikace stáhne řadu webových stránek.</span><span class="sxs-lookup"><span data-stu-id="eec71-257">The application downloads a series of websites.</span></span>|[<span data-ttu-id="eec71-258">Ukázka asynchroniky: Přístup k webovému návodu</span><span class="sxs-lookup"><span data-stu-id="eec71-258">Async Sample: Accessing the Web Walkthrough</span></span>](https://code.msdn.microsoft.com/Async-Sample-Accessing-the-9c10497f)|
|[<span data-ttu-id="eec71-259">Postup: Rozšíření asynchronního návodu pomocí task.whenall (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eec71-259">How to: Extend the Async Walkthrough by Using Task.WhenAll (Visual Basic)</span></span>](how-to-extend-the-async-walkthrough-by-using-task-whenall.md)|<span data-ttu-id="eec71-260">Přidá <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> k předchozímu návodu.</span><span class="sxs-lookup"><span data-stu-id="eec71-260">Adds <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> to the previous walkthrough.</span></span> <span data-ttu-id="eec71-261">Použití spustí `WhenAll` všechny soubory ke stažení ve stejnou dobu.</span><span class="sxs-lookup"><span data-stu-id="eec71-261">The use of `WhenAll` starts all the downloads at the same time.</span></span>||
|[<span data-ttu-id="eec71-262">Postup: Vytvoření více webových požadavků paralelně pomocí asynchronní a čeká (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eec71-262">How to: Make Multiple Web Requests in Parallel by Using Async and Await (Visual Basic)</span></span>](how-to-make-multiple-web-requests-in-parallel-by-using-async-and-await.md)|<span data-ttu-id="eec71-263">Ukazuje, jak spustit několik úloh současně.</span><span class="sxs-lookup"><span data-stu-id="eec71-263">Demonstrates how to start several tasks at the same time.</span></span>|[<span data-ttu-id="eec71-264">Ukázka asynchroniky: Souběžně provádět více webových požadavků</span><span class="sxs-lookup"><span data-stu-id="eec71-264">Async Sample: Make Multiple Web Requests in Parallel</span></span>](https://code.msdn.microsoft.com/Async-Make-Multiple-Web-49adb82e)|
|[<span data-ttu-id="eec71-265">Asynchronní návratové typy (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eec71-265">Async Return Types (Visual Basic)</span></span>](async-return-types.md)|<span data-ttu-id="eec71-266">Znázorňuje typy, které může vrátit asynchronní metoda, a vysvětluje, kdy se každý typ hodí.</span><span class="sxs-lookup"><span data-stu-id="eec71-266">Illustrates the types that async methods can return and explains when each type is appropriate.</span></span>||
|[<span data-ttu-id="eec71-267">Tok řízení v asynchronních programech (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eec71-267">Control Flow in Async Programs (Visual Basic)</span></span>](control-flow-in-async-programs.md)|<span data-ttu-id="eec71-268">Podrobně sleduje tok řízení pomocí sledu očekávání výrazů v asynchronním programu.</span><span class="sxs-lookup"><span data-stu-id="eec71-268">Traces in detail the flow of control through a succession of await expressions in an asynchronous program.</span></span>|[<span data-ttu-id="eec71-269">Ukázka asynchroniky: Tok řízení v asynchronních programech</span><span class="sxs-lookup"><span data-stu-id="eec71-269">Async Sample: Control Flow in Async Programs</span></span>](https://code.msdn.microsoft.com/Async-Sample-Control-Flow-5c804fc0)|
|[<span data-ttu-id="eec71-270">Jemné doladění asynchronní aplikace (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eec71-270">Fine-Tuning Your Async Application (Visual Basic)</span></span>](fine-tuning-your-async-application.md)|<span data-ttu-id="eec71-271">Ukazuje, jak přidat k asynchronnímu řešení následující funkce:</span><span class="sxs-lookup"><span data-stu-id="eec71-271">Shows how to add the following functionality to your async solution:</span></span><br /><br /> <span data-ttu-id="eec71-272">- [Zrušení asynchronní úlohy nebo seznamu úkolů (Visual Basic)](../../../../visual-basic/programming-guide/concepts/async/cancel-an-async-task-or-a-list-of-tasks.md)</span><span class="sxs-lookup"><span data-stu-id="eec71-272">- [Cancel an Async Task or a List of Tasks (Visual Basic)](../../../../visual-basic/programming-guide/concepts/async/cancel-an-async-task-or-a-list-of-tasks.md)</span></span><br /><span data-ttu-id="eec71-273">- [Zrušit asynchronní úkoly po určité době (Visual Basic)](cancel-async-tasks-after-a-period-of-time.md)</span><span class="sxs-lookup"><span data-stu-id="eec71-273">- [Cancel Async Tasks after a Period of Time (Visual Basic)](cancel-async-tasks-after-a-period-of-time.md)</span></span><br /><span data-ttu-id="eec71-274">- [Zrušit zbývající asynchronní úlohy po dokončení jedné (Visual Basic)](cancel-remaining-async-tasks-after-one-is-complete.md)</span><span class="sxs-lookup"><span data-stu-id="eec71-274">- [Cancel Remaining Async Tasks after One Is Complete (Visual Basic)](cancel-remaining-async-tasks-after-one-is-complete.md)</span></span><br /><span data-ttu-id="eec71-275">- [Spuštění více asynchronních úloh a jejich zpracování po dokončení (Visual Basic)](start-multiple-async-tasks-and-process-them-as-they-complete.md)</span><span class="sxs-lookup"><span data-stu-id="eec71-275">- [Start Multiple Async Tasks and Process Them As They Complete (Visual Basic)](start-multiple-async-tasks-and-process-them-as-they-complete.md)</span></span>|[<span data-ttu-id="eec71-276">Asynchronní ukázka: Jemné doladění aplikace</span><span class="sxs-lookup"><span data-stu-id="eec71-276">Async Sample: Fine Tuning Your Application</span></span>](https://code.msdn.microsoft.com/Async-Fine-Tuning-Your-a676abea)|
|[<span data-ttu-id="eec71-277">Zpracování reentrancy v asynchronních aplikacích (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eec71-277">Handling Reentrancy in Async Apps (Visual Basic)</span></span>](handling-reentrancy-in-async-apps.md)|<span data-ttu-id="eec71-278">Ukazuje, jak zpracovat případy, ve kterých je spuštěna aktivní asynchronní operace.</span><span class="sxs-lookup"><span data-stu-id="eec71-278">Shows how to handle cases in which an active asynchronous operation is restarted while it's running.</span></span>||
|<span data-ttu-id="eec71-279">[WhenAny: Přemostění mezi rozhraním .NET Framework a prostředím Windows Runtime](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/jj635140(v=vs.120))</span><span class="sxs-lookup"><span data-stu-id="eec71-279">[WhenAny: Bridging between the .NET Framework and the Windows Runtime](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/jj635140(v=vs.120))</span></span>|<span data-ttu-id="eec71-280">Ukazuje, jak přemostit mezi typy úloh v rozhraní .NET Framework a IAsyncOperations v prostředí Windows Runtime, takže můžete použít <xref:System.Threading.Tasks.Task.WhenAny%2A> s metodou Prostředí Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="eec71-280">Shows how to bridge between Task types in the .NET Framework and IAsyncOperations in the Windows Runtime so that you can use <xref:System.Threading.Tasks.Task.WhenAny%2A> with a Windows Runtime method.</span></span>|<span data-ttu-id="eec71-281">[Ukázka asynchronní: Přemostění mezi rozhraními .NET a prostředím Windows Runtime (AsTask a WhenAny)](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/jj635140(v=vs.120))</span><span class="sxs-lookup"><span data-stu-id="eec71-281">[Async Sample: Bridging between .NET and Windows Runtime (AsTask and WhenAny)](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/jj635140(v=vs.120))</span></span>|
|<span data-ttu-id="eec71-282">Asynchronní zrušení: přemostění rozhraní .NET Framework a prostředí Windows Runtime</span><span class="sxs-lookup"><span data-stu-id="eec71-282">Async Cancellation: Bridging between the .NET Framework and the Windows Runtime</span></span>|<span data-ttu-id="eec71-283">Ukazuje, jak přemostit mezi typy úloh v rozhraní .NET Framework a IAsyncOperations v prostředí Windows Runtime, takže můžete použít <xref:System.Threading.CancellationTokenSource> s metodou Prostředí Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="eec71-283">Shows how to bridge between Task types in the .NET Framework and IAsyncOperations in the Windows Runtime so that you can use <xref:System.Threading.CancellationTokenSource> with a Windows Runtime method.</span></span>|[<span data-ttu-id="eec71-284">Ukázka asynchronní: Přemostění mezi rozhraními .NET a prostředím Windows Runtime (AsTask & Zrušení)</span><span class="sxs-lookup"><span data-stu-id="eec71-284">Async Sample: Bridging between .NET and Windows Runtime (AsTask & Cancellation)</span></span>](https://code.msdn.microsoft.com/Async-Sample-Bridging-9479eca3)|
|[<span data-ttu-id="eec71-285">Použití aplikace Async pro přístup k souborům (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="eec71-285">Using Async for File Access (Visual Basic)</span></span>](using-async-for-file-access.md)|<span data-ttu-id="eec71-286">Seznam a ukázka výhod použití operátorů async a await při přístupu k souborům.</span><span class="sxs-lookup"><span data-stu-id="eec71-286">Lists and demonstrates the benefits of using async and await to access files.</span></span>||
|[<span data-ttu-id="eec71-287">Asynchronní vzor založený na úlohách (TAP)</span><span class="sxs-lookup"><span data-stu-id="eec71-287">Task-based Asynchronous Pattern (TAP)</span></span>](../../../../standard/asynchronous-programming-patterns/task-based-asynchronous-pattern-tap.md)|<span data-ttu-id="eec71-288">Popisuje nový vzor pro asynchronii v rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="eec71-288">Describes a new pattern for asynchrony in the .NET Framework.</span></span> <span data-ttu-id="eec71-289">Vzor je založen <xref:System.Threading.Tasks.Task> na a [Task(Of TResult)](xref:System.Threading.Tasks.Task%601) typy.</span><span class="sxs-lookup"><span data-stu-id="eec71-289">The pattern is based on the <xref:System.Threading.Tasks.Task> and [Task(Of TResult)](xref:System.Threading.Tasks.Task%601) types.</span></span>||
|[<span data-ttu-id="eec71-290">Asynchronní videa na kanálu 9</span><span class="sxs-lookup"><span data-stu-id="eec71-290">Async Videos on Channel 9</span></span>](https://channel9.msdn.com/search?term=async+&type=All)|<span data-ttu-id="eec71-291">Poskytuje odkazy na různá videa o asynchronním programování.</span><span class="sxs-lookup"><span data-stu-id="eec71-291">Provides links to a variety of videos about async programming.</span></span>||

## <a name="complete-example"></a><a name="BKMK_CompleteExample"></a><span data-ttu-id="eec71-292">Kompletní příklad</span><span class="sxs-lookup"><span data-stu-id="eec71-292">Complete Example</span></span>

<span data-ttu-id="eec71-293">Následující kód je mainwindow.xaml.vb soubor z windows presentation foundation (WPF) aplikace, která popisuje toto téma.</span><span class="sxs-lookup"><span data-stu-id="eec71-293">The following code is the MainWindow.xaml.vb file from the Windows Presentation Foundation (WPF) application that this topic discusses.</span></span> <span data-ttu-id="eec71-294">Ukázku si můžete stáhnout z [ukázky Async: Příklad z "Asynchronní programování s async a Await"](https://docs.microsoft.com/samples/dotnet/samples/async-and-await-vb/).</span><span class="sxs-lookup"><span data-stu-id="eec71-294">You can download the sample from [Async Sample: Example from "Asynchronous Programming with Async and Await"](https://docs.microsoft.com/samples/dotnet/samples/async-and-await-vb/).</span></span>

[!code-vb[async](~/samples/snippets/standard/async/async-and-await/vb/MainWindow.xaml.vb)]

## <a name="see-also"></a><span data-ttu-id="eec71-295">Viz také</span><span class="sxs-lookup"><span data-stu-id="eec71-295">See also</span></span>

- [<span data-ttu-id="eec71-296">Čekat operátora</span><span class="sxs-lookup"><span data-stu-id="eec71-296">Await Operator</span></span>](../../../../visual-basic/language-reference/operators/await-operator.md)
- [<span data-ttu-id="eec71-297">Asynchronní</span><span class="sxs-lookup"><span data-stu-id="eec71-297">Async</span></span>](../../../../visual-basic/language-reference/modifiers/async.md)
