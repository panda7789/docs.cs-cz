---
title: Řešení potíží s interoperabilitou (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- interop, deploying assemblies
- assemblies [Visual Basic]
- interop, installing assemblies that share components
- COM objects, troubleshooting
- interop, sharing components
- troubleshooting interoperability [Visual Basic]
- interoperability, troubleshooting
- COM interop [Visual Basic], troubleshooting
- assemblies [Visual Basic], deploying
- troubleshooting Visual Basic, interoperability
- interop assemblies
- interoperability, sharing components
- shared components, using with assemblies
ms.assetid: b324cc1e-b03c-4f39-aea6-6a6d5bfd0e37
ms.openlocfilehash: 60a61dfa7302611800c0b808a31a386e46304756
ms.sourcegitcommit: c7a7e1468bf0fa7f7065de951d60dfc8d5ba89f5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 05/14/2019
ms.locfileid: "65592168"
---
# <a name="troubleshooting-interoperability-visual-basic"></a><span data-ttu-id="882e6-102">Řešení potíží s interoperabilitou (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="882e6-102">Troubleshooting Interoperability (Visual Basic)</span></span>
<span data-ttu-id="882e6-103">Při můžete zajistit vzájemnou funkční spolupráci mezi modelem COM a spravovaného kódu rozhraní .NET Framework, může dojít k některé z těchto běžných problémů.</span><span class="sxs-lookup"><span data-stu-id="882e6-103">When you interoperate between COM and the managed code of the .NET Framework, you may encounter one or more of the following common issues.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor1"></a> <span data-ttu-id="882e6-104">Zařazování spolupráce</span><span class="sxs-lookup"><span data-stu-id="882e6-104">Interop Marshaling</span></span>  
 <span data-ttu-id="882e6-105">V některých případech bude pravděpodobně nutné použít datové typy, které nejsou součástí rozhraní .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="882e6-105">At times, you may have to use data types that are not part of the .NET Framework.</span></span> <span data-ttu-id="882e6-106">Sestavení vzájemné spolupráce zpracovat většinu práce s objekty COM, ale možná budete muset určit datové typy, které se používají při spravované objekty jsou vystaveny objektům modelu COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-106">Interop assemblies handle most of the work for COM objects, but you may have to control the data types that are used when managed objects are exposed to COM.</span></span> <span data-ttu-id="882e6-107">Například musíte zadat struktury v knihovnách tříd `BStr` nespravovaný typ na řetězce zaslána com. objekty vytvořené pomocí jazyka Visual Basic 6.0 a starší verze.</span><span class="sxs-lookup"><span data-stu-id="882e6-107">For example, structures in class libraries must specify the `BStr` unmanaged type on strings sent to COM objects created by Visual Basic 6.0 and earlier versions.</span></span> <span data-ttu-id="882e6-108">V takovém případě můžete použít <xref:System.Runtime.InteropServices.MarshalAsAttribute> atribut způsobí spravované typy zpřístupní jako nespravované typy.</span><span class="sxs-lookup"><span data-stu-id="882e6-108">In such cases, you can use the <xref:System.Runtime.InteropServices.MarshalAsAttribute> attribute to cause managed types to be exposed as unmanaged types.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor2"></a> <span data-ttu-id="882e6-109">Export řetězce pevné délky na nespravovaný kód</span><span class="sxs-lookup"><span data-stu-id="882e6-109">Exporting Fixed-Length Strings to Unmanaged Code</span></span>  
 <span data-ttu-id="882e6-110">V jazyce Visual Basic verze 6.0 a starší jsou exportovány řetězců na objekty modelu COM jako sekvence bajtů bez ukončení hodnotou null znaků.</span><span class="sxs-lookup"><span data-stu-id="882e6-110">In Visual Basic 6.0 and earlier versions, strings are exported to COM objects as sequences of bytes without a null termination character.</span></span> <span data-ttu-id="882e6-111">Z důvodu kompatibility s jinými jazyky Visual Basic .NET obsahuje znak ukončení při exportu řetězce.</span><span class="sxs-lookup"><span data-stu-id="882e6-111">For compatibility with other languages, Visual Basic .NET includes a termination character when exporting strings.</span></span> <span data-ttu-id="882e6-112">Nejlepší způsob, jak vyřešit k této nekompatibilitě je exportovat řetězce, které chybí ukončovací znak jako pole `Byte` nebo `Char`.</span><span class="sxs-lookup"><span data-stu-id="882e6-112">The best way to address this incompatibility is to export strings that lack the termination character as arrays of `Byte` or `Char`.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor3"></a> <span data-ttu-id="882e6-113">Export hierarchie dědičnosti</span><span class="sxs-lookup"><span data-stu-id="882e6-113">Exporting Inheritance Hierarchies</span></span>  
 <span data-ttu-id="882e6-114">Spravované třídy, které hierarchie sloučit si při vystavení jako objekty modelu COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-114">Managed class hierarchies flatten out when exposed as COM objects.</span></span> <span data-ttu-id="882e6-115">Například pokud definujete základní třídu se členem a potom dědit základní třídy v odvozené třídě, která je vystavena jako objekt modelu COM, klienty, kteří používají odvozené třídy v objektu modelu COM nebudou moct používat zděděné členy.</span><span class="sxs-lookup"><span data-stu-id="882e6-115">For example, if you define a base class with a member, and then inherit the base class in a derived class that is exposed as a COM object, clients that use the derived class in the COM object will not be able to use the inherited members.</span></span> <span data-ttu-id="882e6-116">Členy základní třídy je přístupný z objektů COM pouze jako instance základní třídy a pak pouze v případě, že základní třídy se také vytvoří jako objekt modelu COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-116">Base class members can be accessed from COM objects only as instances of a base class, and then only if the base class is also created as a COM object.</span></span>  
  
## <a name="overloaded-methods"></a><span data-ttu-id="882e6-117">Přetížené metody</span><span class="sxs-lookup"><span data-stu-id="882e6-117">Overloaded Methods</span></span>  
 <span data-ttu-id="882e6-118">I když přetížené metody můžete vytvořit pomocí jazyka Visual Basic, nejsou podporovány modelu COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-118">Although you can create overloaded methods with Visual Basic, they are not supported by COM.</span></span> <span data-ttu-id="882e6-119">Když je třídu, která obsahuje přetížené metody vystaveny jako objekt modelu COM, nové názvy metod jsou generovány pro přetížené metody.</span><span class="sxs-lookup"><span data-stu-id="882e6-119">When a class that contains overloaded methods is exposed as a COM object, new method names are generated for the overloaded methods.</span></span>  
  
 <span data-ttu-id="882e6-120">Představte si třeba třídu, která má dvě přetížení `Synch` metody.</span><span class="sxs-lookup"><span data-stu-id="882e6-120">For example, consider a class that has two overloads of the `Synch` method.</span></span> <span data-ttu-id="882e6-121">Pokud třída je vystavená jako objekt modelu COM, může být nové názvy vytvořena metoda `Synch` a `Synch_2`.</span><span class="sxs-lookup"><span data-stu-id="882e6-121">When the class is exposed as a COM object, the new generated method names could be `Synch` and `Synch_2`.</span></span>  
  
 <span data-ttu-id="882e6-122">Přejmenování může způsobit dva problémy pro spotřebitele objektu COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-122">The renaming can cause two problems for consumers of the COM object.</span></span>  
  
1. <span data-ttu-id="882e6-123">Klienti nemusí očekávat, že názvy vytvořena metoda.</span><span class="sxs-lookup"><span data-stu-id="882e6-123">Clients might not expect the generated method names.</span></span>  
  
2. <span data-ttu-id="882e6-124">Vytvořena metoda názvy ve třídě vystavena jako objekt modelu COM lze změnit nová přetížení se přidají do třídy nebo její základní třídě.</span><span class="sxs-lookup"><span data-stu-id="882e6-124">The generated method names in the class exposed as a COM object can change when new overloads are added to the class or its base class.</span></span> <span data-ttu-id="882e6-125">To může způsobit problémy se správou verzí.</span><span class="sxs-lookup"><span data-stu-id="882e6-125">This can cause versioning problems.</span></span>  
  
 <span data-ttu-id="882e6-126">K řešení problémů, obě, zadejte jednotlivé metody jedinečný název, namísto použití přetížení, při vývoji objekty, které se zveřejní jako objekty modelu COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-126">To solve both problems, give each method a unique name, instead of using overloading, when you develop objects that will be exposed as COM objects.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor4"></a> <span data-ttu-id="882e6-127">Použití objektů modelu COM sestaveních vzájemné spolupráce</span><span class="sxs-lookup"><span data-stu-id="882e6-127">Use of COM Objects Through Interop Assemblies</span></span>  
 <span data-ttu-id="882e6-128">Téměř jako by šlo spravovaného kódu při nahrazování objekty modelu COM, které představují, používají spolupracující sestavení.</span><span class="sxs-lookup"><span data-stu-id="882e6-128">You use interop assemblies almost as if they are managed code replacements for the COM objects they represent.</span></span> <span data-ttu-id="882e6-129">Protože jde o obálkách a ne skutečné objekty modelu COM, existují však určité rozdíly mezi použitím nástrojů sestavení vzájemné spolupráce a standardní sestavení.</span><span class="sxs-lookup"><span data-stu-id="882e6-129">However, because they are wrappers and not actual COM objects, there are some differences between using interop assemblies and standard assemblies.</span></span> <span data-ttu-id="882e6-130">Patří sem rozdílu vystavení tříd a datové typy parametrů a návratové hodnoty.</span><span class="sxs-lookup"><span data-stu-id="882e6-130">These areas of difference include the exposure of classes, and data types for parameters and return values.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor5"></a> <span data-ttu-id="882e6-131">Vystavené jako obě rozhraní třídy a třídy</span><span class="sxs-lookup"><span data-stu-id="882e6-131">Classes Exposed as Both Interfaces and Classes</span></span>  
 <span data-ttu-id="882e6-132">Na rozdíl od tříd v standardní sestavení jsou přístupné třídy modelu COM ve spolupracujícím sestavení jako rozhraní a třídy, která představuje třídu COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-132">Unlike classes in standard assemblies, COM classes are exposed in interop assemblies as both an interface and a class that represents the COM class.</span></span> <span data-ttu-id="882e6-133">Název rozhraní je stejný jako u třídy modelu COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-133">The interface's name is identical to that of the COM class.</span></span> <span data-ttu-id="882e6-134">Název třídy spolupráce je stejný jako původní třídy modelu COM, ale slovo "Třída" připojí.</span><span class="sxs-lookup"><span data-stu-id="882e6-134">The name of the interop class is the same as that of the original COM class, but with the word "Class" appended.</span></span> <span data-ttu-id="882e6-135">Předpokládejme například, že máte projekt s odkazem na sestavení vzájemné spolupráce pro objekt modelu COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-135">For example, suppose you have a project with a reference to an interop assembly for a COM object.</span></span> <span data-ttu-id="882e6-136">Pokud je název třídy modelu COM `MyComClass`, technologie IntelliSense a prohlížeče objektů zobrazit rozhraní s názvem `MyComClass` a třídu s názvem `MyComClassClass`.</span><span class="sxs-lookup"><span data-stu-id="882e6-136">If the COM class is named `MyComClass`, IntelliSense and the Object Browser show an interface named `MyComClass` and a class named `MyComClassClass`.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor6"></a> <span data-ttu-id="882e6-137">Vytvoření instance třídy rozhraní .NET Framework</span><span class="sxs-lookup"><span data-stu-id="882e6-137">Creating Instances of a .NET Framework Class</span></span>  
 <span data-ttu-id="882e6-138">Obecně platí, vytvoření instance třídy pomocí rozhraní .NET Framework `New` příkaz s názvem třídy.</span><span class="sxs-lookup"><span data-stu-id="882e6-138">Generally, you create an instance of a .NET Framework class using the `New` statement with a class name.</span></span> <span data-ttu-id="882e6-139">Třída modelu COM reprezentované definiční sestavení je jeden případ, ve kterém můžete použít `New` příkaz s rozhraním.</span><span class="sxs-lookup"><span data-stu-id="882e6-139">Having a COM class represented by an interop assembly is the one case in which you can use the `New` statement with an interface.</span></span> <span data-ttu-id="882e6-140">Pokud použijete třídu modelu COM s `Inherits` příkazu, můžete použít rozhraní, stejně jako třídy.</span><span class="sxs-lookup"><span data-stu-id="882e6-140">Unless you are using the COM class with an `Inherits` statement, you can use the interface just as you would a class.</span></span> <span data-ttu-id="882e6-141">Následující kód ukazuje, jak vytvořit `Command` objektu v projektu, který obsahuje odkaz na objekt Microsoft ActiveX Data objekty 2.8 knihovny COM:</span><span class="sxs-lookup"><span data-stu-id="882e6-141">The following code demonstrates how to create a `Command` object in a project that has a reference to the Microsoft ActiveX Data Objects 2.8 Library COM object:</span></span>  
  
 [!code-vb[VbVbalrInterop#20](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#20)]  
  
 <span data-ttu-id="882e6-142">Pokud použijete třídu modelu COM jako základ pro odvozenou třídu, však musíte použít třídu spolupráce, který představuje třídu COM, stejně jako v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="882e6-142">However, if you are using the COM class as the base for a derived class, you must use the interop class that represents the COM class, as in the following code:</span></span>  
  
 [!code-vb[VbVbalrInterop#21](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#21)]  
  
> [!NOTE]
>  <span data-ttu-id="882e6-143">Sestavení vzájemné spolupráce implicitně implementovat rozhraní, které představují třídy modelu COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-143">Interop assemblies implicitly implement interfaces that represent COM classes.</span></span> <span data-ttu-id="882e6-144">By se neměl pokoušet použít `Implements` způsobí příkaz k implementaci těchto rozhraní nebo došlo k chybě.</span><span class="sxs-lookup"><span data-stu-id="882e6-144">You should not try to use the `Implements` statement to implement these interfaces or an error will result.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor7"></a> <span data-ttu-id="882e6-145">Datové typy parametrů a návratové hodnoty</span><span class="sxs-lookup"><span data-stu-id="882e6-145">Data Types for Parameters and Return Values</span></span>  
 <span data-ttu-id="882e6-146">Na rozdíl od členů standardní sestavení může sestavení vzájemné spolupráce členů mají datové typy, které se liší od těch, které používají v původní deklaraci objektu.</span><span class="sxs-lookup"><span data-stu-id="882e6-146">Unlike members of standard assemblies, interop assembly members may have data types that differ from those used in the  original object declaration.</span></span> <span data-ttu-id="882e6-147">I když sestavení vzájemné spolupráce implicitně převést na kompatibilní běžných typů modulu runtime jazyka typy modelu COM, měli věnovat pozornost datové typy, které jsou používány obě strany aby se zabránilo chybám za běhu.</span><span class="sxs-lookup"><span data-stu-id="882e6-147">Although interop assemblies implicitly convert COM types to compatible common language runtime types, you should pay attention to the data types that are used by both sides to prevent runtime errors.</span></span> <span data-ttu-id="882e6-148">Například v modelu COM objekty vytvořené v jazyce Visual Basic verze 6.0 a starší, hodnoty typu `Integer` ekvivalentní typ rozhraní .NET Framework, se předpokládá `Short`.</span><span class="sxs-lookup"><span data-stu-id="882e6-148">For example, in COM objects created in Visual Basic 6.0 and earlier versions, values of type `Integer` assume the .NET Framework equivalent type, `Short`.</span></span> <span data-ttu-id="882e6-149">Doporučuje se použít Prohlížeč objektů prozkoumat charakteristiky členů importované před jejich použitím.</span><span class="sxs-lookup"><span data-stu-id="882e6-149">It is recommended that you use the Object Browser to examine the characteristics of imported members before you use them.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor8"></a> <span data-ttu-id="882e6-150">Modul metody na úrovni modelu COM</span><span class="sxs-lookup"><span data-stu-id="882e6-150">Module level COM methods</span></span>  
 <span data-ttu-id="882e6-151">Většina objektů COM se používají tak, že vytvoříte instanci třídy pomocí modelu COM `New` – klíčové slovo a následným voláním metod objektu.</span><span class="sxs-lookup"><span data-stu-id="882e6-151">Most COM objects are used by creating an instance of a COM class using the `New` keyword and then calling methods of the object.</span></span> <span data-ttu-id="882e6-152">Jedinou výjimkou tohoto pravidla zahrnuje objekty modelu COM, které obsahují `AppObj` nebo `GlobalMultiUse` tříd modelu COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-152">One exception to this rule involves COM objects that contain `AppObj` or `GlobalMultiUse` COM classes.</span></span> <span data-ttu-id="882e6-153">Takové třídy vypadat podobně jako na úrovni metod modulu ve třídách jazyka Visual Basic .NET.</span><span class="sxs-lookup"><span data-stu-id="882e6-153">Such classes resemble module level methods in Visual Basic .NET classes.</span></span> <span data-ttu-id="882e6-154">Visual Basic 6.0 a starší verze implicitně vytvoření instancí těchto objektů můžete při prvním volání jedné z jejich metod.</span><span class="sxs-lookup"><span data-stu-id="882e6-154">Visual Basic 6.0 and earlier versions implicitly create instances of such objects for you the first time that you call one of their methods.</span></span> <span data-ttu-id="882e6-155">Například v jazyce Visual Basic 6.0 můžete přidat odkaz na knihovnu objektů 3.6 Microsoft DAO a volání `DBEngine` metody bez vytvoření instance:</span><span class="sxs-lookup"><span data-stu-id="882e6-155">For example, in Visual Basic 6.0 you can add a reference to the Microsoft DAO 3.6 Object Library and call the `DBEngine` method without first creating an instance:</span></span>  
  
```vb  
Dim db As DAO.Database  
' Open the database.  
Set db = DBEngine.OpenDatabase("C:\nwind.mdb")  
' Use the database object.  
```  
  
 <span data-ttu-id="882e6-156">Visual Basic .NET vyžaduje, abyste mohli používat své metody vždy vytvořit instance objektů COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-156">Visual Basic .NET requires that you always create instances of COM objects before you can use their methods.</span></span> <span data-ttu-id="882e6-157">Používat tyto metody v jazyce Visual Basic, deklarovat proměnnou požadovanou třídu a použijte klíčové slovo new k přiřazení objektu k proměnné objektu.</span><span class="sxs-lookup"><span data-stu-id="882e6-157">To use these methods in Visual Basic, declare a variable of the desired class and use the new keyword to assign the object to the object variable.</span></span> <span data-ttu-id="882e6-158">`Shared` – Klíčové slovo lze použít, když chcete Ujistěte se, že se vytvoří tento pouze jedna instance třídy.</span><span class="sxs-lookup"><span data-stu-id="882e6-158">The `Shared` keyword can be used when you want to make sure that only one instance of the class is created.</span></span>  
  
 [!code-vb[VbVbalrInterop#23](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#23)]  
  
## <a name="vbconinteroperabilitymarshalinganchor9"></a> <span data-ttu-id="882e6-159">Neošetřené chyby v obslužných rutinách událostí</span><span class="sxs-lookup"><span data-stu-id="882e6-159">Unhandled Errors in Event Handlers</span></span>  
 <span data-ttu-id="882e6-160">Jeden běžný problém spolupráce zahrnuje chyby v obslužných rutinách událostí, které zpracovávají události vyvolané objektů COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-160">One common interop problem involves errors in event handlers that handle events raised by COM objects.</span></span> <span data-ttu-id="882e6-161">Tyto chyby se ignorovaly, pokud výslovně ověřujete chyby pomocí `On Error` nebo `Try...Catch...Finally` příkazy.</span><span class="sxs-lookup"><span data-stu-id="882e6-161">Such errors are ignored unless you specifically check for errors using `On Error` or `Try...Catch...Finally` statements.</span></span> <span data-ttu-id="882e6-162">Například následující příklad je z projektu jazyka Visual Basic .NET, který obsahuje odkaz na objekt Microsoft ActiveX Data objekty 2.8 knihovny COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-162">For example, the following example is from a Visual Basic .NET project that has a reference to the Microsoft ActiveX Data Objects 2.8 Library COM object.</span></span>  
  
 [!code-vb[VbVbalrInterop#24](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#24)]  
  
 <span data-ttu-id="882e6-163">V tomto příkladu vyvolává chybu podle očekávání.</span><span class="sxs-lookup"><span data-stu-id="882e6-163">This example raises an error as expected.</span></span> <span data-ttu-id="882e6-164">Ale pokud se pokusíte stejný příklad bez `Try...Catch...Finally` bloku, chyba se ignoruje, protože, pokud jste použili `OnError Resume Next` příkazu.</span><span class="sxs-lookup"><span data-stu-id="882e6-164">However, if you try the same example without the `Try...Catch...Finally` block, the error is ignored as if you used the `OnError Resume Next` statement.</span></span> <span data-ttu-id="882e6-165">Bez zpracování chyb bez upozornění selže dělení nulou.</span><span class="sxs-lookup"><span data-stu-id="882e6-165">Without error handling, the division by zero silently fails.</span></span> <span data-ttu-id="882e6-166">Protože tyto chyby nikdy vyvolat chyby neošetřená výjimka, je důležité, abyste používali určitou formu zpracování výjimek v obslužné rutiny událostí, které zpracovávají události od objektů COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-166">Because such errors never raise unhandled exception errors, it is important that you use some form of exception handling in event handlers that handle events from COM objects.</span></span>  
  
### <a name="understanding-com-interop-errors"></a><span data-ttu-id="882e6-167">Vysvětlení chyb vzájemné spolupráce COM</span><span class="sxs-lookup"><span data-stu-id="882e6-167">Understanding COM interop errors</span></span>  
 <span data-ttu-id="882e6-168">Bez zpracování chyb, generování spolupráce volání často chyby, které poskytují pár informací.</span><span class="sxs-lookup"><span data-stu-id="882e6-168">Without error handling, interop calls often generate errors that provide little information.</span></span> <span data-ttu-id="882e6-169">Kdykoli je to možné, použijte strukturované zpracování vám poskytneme Další informace o problémech, které se objeví chyby.</span><span class="sxs-lookup"><span data-stu-id="882e6-169">Whenever possible, use structured error handling to provide more information about problems when they occur.</span></span> <span data-ttu-id="882e6-170">To může být zvláště užitečné při ladění aplikace.</span><span class="sxs-lookup"><span data-stu-id="882e6-170">This can be especially helpful when you debug applications.</span></span> <span data-ttu-id="882e6-171">Příklad:</span><span class="sxs-lookup"><span data-stu-id="882e6-171">For example:</span></span>  
  
 [!code-vb[VbVbalrInterop#25](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#25)]  
  
 <span data-ttu-id="882e6-172">Informace, např. popis chyby, HRESULT a příčiny chyby modelu COM. získáte zkoumání obsahu výjimek objektu.</span><span class="sxs-lookup"><span data-stu-id="882e6-172">You can find information such as the error description, HRESULT, and the source of COM errors by examining the contents of the exception object.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor10"></a> <span data-ttu-id="882e6-173">Problémy s ovládacího prvku ActiveX</span><span class="sxs-lookup"><span data-stu-id="882e6-173">ActiveX Control Issues</span></span>  
 <span data-ttu-id="882e6-174">Většina ovládacích prvků ActiveX, které využívají službu jazyka Visual Basic 6.0 fungovat bez potíže v jazyce Visual Basic .NET.</span><span class="sxs-lookup"><span data-stu-id="882e6-174">Most ActiveX controls that work with Visual Basic 6.0 work with Visual Basic .NET without trouble.</span></span> <span data-ttu-id="882e6-175">Hlavní výjimky jsou ovládací prvky kontejneru nebo ovládací prvky, které vizuálně obsahují další ovládací prvky.</span><span class="sxs-lookup"><span data-stu-id="882e6-175">The main exceptions are container controls, or controls that visually contain other controls.</span></span> <span data-ttu-id="882e6-176">Některé příklady starší ovládacích prvků, které nebudou správně fungovat se sadou Visual Studio jsou následující:</span><span class="sxs-lookup"><span data-stu-id="882e6-176">Some examples of older controls that do not work correctly with Visual Studio are as follows:</span></span>  
  
- <span data-ttu-id="882e6-177">Microsoft Forms 2.0 Frame – ovládací prvek</span><span class="sxs-lookup"><span data-stu-id="882e6-177">Microsoft Forms 2.0 Frame control</span></span>  
  
- <span data-ttu-id="882e6-178">Ovládací prvek směrem nahoru a dolů, označované také jako otočný ovládací prvek</span><span class="sxs-lookup"><span data-stu-id="882e6-178">Up-Down control, also known as the spin control</span></span>  
  
- <span data-ttu-id="882e6-179">Ovládací prvek karty Sheridan</span><span class="sxs-lookup"><span data-stu-id="882e6-179">Sheridan Tab Control</span></span>  
  
 <span data-ttu-id="882e6-180">Existuje pouze několik řešení pro nepodporované problémy ovládacího prvku ActiveX.</span><span class="sxs-lookup"><span data-stu-id="882e6-180">There are only a few workarounds for unsupported ActiveX control problems.</span></span> <span data-ttu-id="882e6-181">Pokud původní zdrojový kód vlastníte, můžete migrovat existující ovládací prvky do sady Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="882e6-181">You can migrate existing controls to Visual Studio if you own the original source code.</span></span> <span data-ttu-id="882e6-182">V opačném případě můžete kontaktovat dodavatele softwaru aktualizovaná. NET kompatibilní s verzí ovládacích prvků k nahrazení nepodporované ovládací prvky ActiveX.</span><span class="sxs-lookup"><span data-stu-id="882e6-182">Otherwise, you can check with software vendors for updated .NET-compatible versions of controls to replace unsupported ActiveX controls.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor11"></a> <span data-ttu-id="882e6-183">Vlastnosti jen pro čtení předání ByRef ovládacích prvků</span><span class="sxs-lookup"><span data-stu-id="882e6-183">Passing ReadOnly Properties of Controls ByRef</span></span>  
 <span data-ttu-id="882e6-184">Visual Basic .NET někdy vyvolává chyby modelu COM jako je například "Chyba 0x800A017F CTL_E_SETNOTSUPPORTED", při předání `ReadOnly` vlastnosti některé starší ovládací prvky ActiveX jako `ByRef` parametry pro další postupy.</span><span class="sxs-lookup"><span data-stu-id="882e6-184">Visual Basic .NET sometimes raises COM errors such as, "Error 0x800A017F CTL_E_SETNOTSUPPORTED", when you pass `ReadOnly` properties of some older ActiveX controls as `ByRef` parameters to other procedures.</span></span> <span data-ttu-id="882e6-185">Podobně jako volání procedur Visual Basic 6.0 nevyvolávejte chybu a parametry jsou zpracovávány jako by to předán podle hodnoty.</span><span class="sxs-lookup"><span data-stu-id="882e6-185">Similar procedure calls from Visual Basic 6.0 do not raise an error, and the parameters are treated as if you passed them by value.</span></span> <span data-ttu-id="882e6-186">Visual Basic .NET chybová zpráva znamená, že se pokoušíte změnit vlastnost, která nemá vlastnost `Set` postup.</span><span class="sxs-lookup"><span data-stu-id="882e6-186">The Visual Basic .NET error message indicates that you are trying to change a property that does not have a property `Set` procedure.</span></span>  
  
 <span data-ttu-id="882e6-187">Pokud máte přístup do volané procedury tuto chybu můžete zabránit pomocí `ByVal` klíčového slova pro deklarování parametrů, které přijímají `ReadOnly` vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="882e6-187">If you have access to the procedure being called, you can prevent this error by using the `ByVal` keyword to declare parameters that accept `ReadOnly` properties.</span></span> <span data-ttu-id="882e6-188">Příklad:</span><span class="sxs-lookup"><span data-stu-id="882e6-188">For example:</span></span>  
  
 [!code-vb[VbVbalrInterop#26](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#26)]  
  
 <span data-ttu-id="882e6-189">Pokud nemáte přístup ke zdrojovému kódu pro procedura volána, můžete vynutit vlastnost, která má být předán podle hodnoty tak, že přidáte další sadu závorek volající procedury.</span><span class="sxs-lookup"><span data-stu-id="882e6-189">If you do not have access to the source code for the procedure being called, you can force the property to be passed by value by adding an extra set of brackets around the calling procedure.</span></span> <span data-ttu-id="882e6-190">Například v projektu, který obsahuje odkaz na objekt Microsoft ActiveX Data objekty 2.8 knihovny COM, můžete použít:</span><span class="sxs-lookup"><span data-stu-id="882e6-190">For example, in a project that has a reference to the Microsoft ActiveX Data Objects 2.8 Library COM object, you can use:</span></span>  
  
 [!code-vb[VbVbalrInterop#27](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#27)]  
  
## <a name="vbconinteroperabilitymarshalinganchor12"></a> <span data-ttu-id="882e6-191">Nasazení sestavení, která zpřístupňují zprostředkovatele komunikace s objekty</span><span class="sxs-lookup"><span data-stu-id="882e6-191">Deploying Assemblies That Expose Interop</span></span>  
 <span data-ttu-id="882e6-192">Nasazení sestavení, které vystavit rozhraní COM prezentuje některé jedinečné výzvy.</span><span class="sxs-lookup"><span data-stu-id="882e6-192">Deploying assemblies that expose COM interfaces presents some unique challenges.</span></span> <span data-ttu-id="882e6-193">Například potenciální problém nastane, pokud samostatné aplikace odkazovat na stejné sestavení modelu COM.</span><span class="sxs-lookup"><span data-stu-id="882e6-193">For example, a potential problem occurs when separate applications reference the same COM assembly.</span></span> <span data-ttu-id="882e6-194">Tato situace je běžné při instalaci nové verze sestavení a stará verze sestavení je stále používá jiná aplikace.</span><span class="sxs-lookup"><span data-stu-id="882e6-194">This situation is common when a new version of an assembly is installed and another application is still using the old version of the assembly.</span></span> <span data-ttu-id="882e6-195">Pokud odinstalujete sestavení, které sdílí knihovnu DLL, můžete neúmyslně si je k dispozici pro jiná sestavení.</span><span class="sxs-lookup"><span data-stu-id="882e6-195">If you uninstall an assembly that shares a DLL, you can unintentionally make it unavailable to the other assemblies.</span></span>  
  
 <span data-ttu-id="882e6-196">K tomuto problému vyhnout, by měl instalaci sdílené sestavení do globální mezipaměti sestavení (GAC) a použít MergeModule pro složku.</span><span class="sxs-lookup"><span data-stu-id="882e6-196">To avoid this problem, you should install shared assemblies to the Global Assembly Cache (GAC) and use a MergeModule for the component.</span></span> <span data-ttu-id="882e6-197">Pokud aplikaci nejde nainstalovat v mezipaměti GAC, ho chcete nainstalovat CommonFilesFolder v podadresáři specifické pro verzi.</span><span class="sxs-lookup"><span data-stu-id="882e6-197">If you cannot install the application in the GAC, it should be installed to CommonFilesFolder in a version-specific subdirectory.</span></span>  
  
 <span data-ttu-id="882e6-198">Sestavení, která se nesdílejí musí nacházet v adresáři s volající aplikace vedle sebe.</span><span class="sxs-lookup"><span data-stu-id="882e6-198">Assemblies that are not shared should be located side by side in the directory with the calling application.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="882e6-199">Viz také:</span><span class="sxs-lookup"><span data-stu-id="882e6-199">See also</span></span>

- <xref:System.Runtime.InteropServices.MarshalAsAttribute>
- [<span data-ttu-id="882e6-200">Zprostředkovatel komunikace s objekty COM</span><span class="sxs-lookup"><span data-stu-id="882e6-200">COM Interop</span></span>](../../../visual-basic/programming-guide/com-interop/index.md)
- [<span data-ttu-id="882e6-201">Tlbimp.exe (importér knihovny typů)</span><span class="sxs-lookup"><span data-stu-id="882e6-201">Tlbimp.exe (Type Library Importer)</span></span>](../../../framework/tools/tlbimp-exe-type-library-importer.md)
- [<span data-ttu-id="882e6-202">Tlbexp.exe (exportér knihovny typů)</span><span class="sxs-lookup"><span data-stu-id="882e6-202">Tlbexp.exe (Type Library Exporter)</span></span>](../../../framework/tools/tlbexp-exe-type-library-exporter.md)
- [<span data-ttu-id="882e6-203">Návod: Implementace dědičnosti pomocí objektů COM</span><span class="sxs-lookup"><span data-stu-id="882e6-203">Walkthrough: Implementing Inheritance with COM Objects</span></span>](../../../visual-basic/programming-guide/com-interop/walkthrough-implementing-inheritance-with-com-objects.md)
- [<span data-ttu-id="882e6-204">Příkaz Inherits</span><span class="sxs-lookup"><span data-stu-id="882e6-204">Inherits Statement</span></span>](../../../visual-basic/language-reference/statements/inherits-statement.md)
- [<span data-ttu-id="882e6-205">Globální mezipaměť sestavení</span><span class="sxs-lookup"><span data-stu-id="882e6-205">Global Assembly Cache</span></span>](../../../framework/app-domains/gac.md)
