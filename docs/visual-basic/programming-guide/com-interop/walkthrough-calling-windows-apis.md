---
title: 'Návod: Volání rozhraní API systému Windows'
ms.date: 07/20/2015
helpviewer_keywords:
- DLLs, calling
- Windows API, walkthroughs
- platform invoke, walkthroughs
- API calls [Visual Basic], walkthroughs [Visual Basic]
- Windows API, calling
- walkthroughs [Visual Basic], API calls
- DllImport attribute, calling Windows API
- Declare statement [Visual Basic], declaring DLL functions
ms.assetid: 9280ca96-7a93-47a3-8d01-6d01be0657cb
ms.openlocfilehash: ec6b8ddc8769fadde52aaebd6ad3701183fac77a
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 11/22/2019
ms.locfileid: "74338669"
---
# <a name="walkthrough-calling-windows-apis-visual-basic"></a><span data-ttu-id="a23b6-102">Návod: Volání rozhraní API systému Windows (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="a23b6-102">Walkthrough: Calling Windows APIs (Visual Basic)</span></span>
<span data-ttu-id="a23b6-103">Rozhraní API systému Windows jsou dynamické knihovny (DLL), které jsou součástí operačního systému Windows.</span><span class="sxs-lookup"><span data-stu-id="a23b6-103">Windows APIs are dynamic-link libraries (DLLs) that are part of the Windows operating system.</span></span> <span data-ttu-id="a23b6-104">Můžete je používat k provádění úkolů, když je obtížné psát ekvivalentní procedury vlastní.</span><span class="sxs-lookup"><span data-stu-id="a23b6-104">You use them to perform tasks when it is difficult to write equivalent procedures of your own.</span></span> <span data-ttu-id="a23b6-105">Například systém Windows poskytuje funkci s názvem `FlashWindowEx`, která umožňuje nastavit záhlaví pro alternativní použití mezi světlými a tmavými odstíny.</span><span class="sxs-lookup"><span data-stu-id="a23b6-105">For example, Windows provides a function named `FlashWindowEx` that lets you make the title bar for an application alternate between light and dark shades.</span></span>  
  
 <span data-ttu-id="a23b6-106">Výhodou použití rozhraní API systému Windows ve vašem kódu je, že mohou ušetřit čas vývoje, protože obsahují desítky užitečných funkcí, které již byly zapsány a čekají na jejich použití.</span><span class="sxs-lookup"><span data-stu-id="a23b6-106">The advantage of using Windows APIs in your code is that they can save development time because they contain dozens of useful functions that are already written and waiting to be used.</span></span> <span data-ttu-id="a23b6-107">Nevýhodou je, že rozhraní API systému Windows může být obtížné pracovat s a unforgiving, když se něco nepodaří.</span><span class="sxs-lookup"><span data-stu-id="a23b6-107">The disadvantage is that Windows APIs can be difficult to work with and unforgiving when things go wrong.</span></span>  
  
 <span data-ttu-id="a23b6-108">Rozhraní API systému Windows představuje zvláštní kategorii interoperability.</span><span class="sxs-lookup"><span data-stu-id="a23b6-108">Windows APIs represent a special category of interoperability.</span></span> <span data-ttu-id="a23b6-109">Rozhraní API systému Windows nepoužívají spravovaný kód, nemají předdefinované knihovny typů a používají datové typy, které se liší od těch, které se používají se sadou Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="a23b6-109">Windows APIs do not use managed code, do not have built-in type libraries, and use data types that are different than those used with Visual Studio.</span></span> <span data-ttu-id="a23b6-110">Z důvodu těchto rozdílů a protože rozhraní API systému Windows nejsou objekty COM, interoperabilita s rozhraními API systému Windows a .NET Framework se provádí pomocí vyvolání platformy nebo PInvoke.</span><span class="sxs-lookup"><span data-stu-id="a23b6-110">Because of these differences, and because Windows APIs are not COM objects, interoperability with Windows APIs and the .NET Framework is performed using platform invoke, or PInvoke.</span></span> <span data-ttu-id="a23b6-111">Vyvolání platformy je služba, která umožňuje spravovanému kódu volat nespravované funkce implementované v knihovnách DLL.</span><span class="sxs-lookup"><span data-stu-id="a23b6-111">Platform invoke is a service that enables managed code to call unmanaged functions implemented in DLLs.</span></span> <span data-ttu-id="a23b6-112">Další informace naleznete v tématu [spotřebovávání nespravovaných funkcí DLL](../../../framework/interop/consuming-unmanaged-dll-functions.md).</span><span class="sxs-lookup"><span data-stu-id="a23b6-112">For more information, see [Consuming Unmanaged DLL Functions](../../../framework/interop/consuming-unmanaged-dll-functions.md).</span></span> <span data-ttu-id="a23b6-113">PInvoke v Visual Basic lze použít buď pomocí příkazu `Declare`, nebo použitím atributu `DllImport` na prázdnou proceduru.</span><span class="sxs-lookup"><span data-stu-id="a23b6-113">You can use PInvoke in Visual Basic by using either the `Declare` statement or applying the `DllImport` attribute to an empty procedure.</span></span>  
  
 <span data-ttu-id="a23b6-114">Volání rozhraní API systému Windows byla důležitou součástí Visual Basicho programování v minulosti, ale není to zřídka nutné pro Visual Basic .NET.</span><span class="sxs-lookup"><span data-stu-id="a23b6-114">Windows API calls were an important part of Visual Basic programming in the past, but are seldom necessary with Visual Basic .NET.</span></span> <span data-ttu-id="a23b6-115">Kdykoli je to možné, měli byste použít spravovaný kód z .NET Framework k provádění úloh místo volání rozhraní API systému Windows.</span><span class="sxs-lookup"><span data-stu-id="a23b6-115">Whenever possible, you should use managed code from the .NET Framework to perform tasks, instead of Windows API calls.</span></span> <span data-ttu-id="a23b6-116">Tento návod poskytuje informace pro případy, kdy je nutné použít rozhraní API systému Windows.</span><span class="sxs-lookup"><span data-stu-id="a23b6-116">This walkthrough provides information for those situations in which using Windows APIs is necessary.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="api-calls-using-declare"></a><span data-ttu-id="a23b6-117">Volání rozhraní API pomocí příkazu Declare</span><span class="sxs-lookup"><span data-stu-id="a23b6-117">API Calls Using Declare</span></span>  
 <span data-ttu-id="a23b6-118">Nejběžnější způsob, jak volat rozhraní API systému Windows, je použití příkazu `Declare`.</span><span class="sxs-lookup"><span data-stu-id="a23b6-118">The most common way to call Windows APIs is by using the `Declare` statement.</span></span>  
  
### <a name="to-declare-a-dll-procedure"></a><span data-ttu-id="a23b6-119">Deklarace procedury DLL</span><span class="sxs-lookup"><span data-stu-id="a23b6-119">To declare a DLL procedure</span></span>  
  
1. <span data-ttu-id="a23b6-120">Určete název funkce, kterou chcete volat, včetně jejích argumentů, typů argumentů a návratové hodnoty, a také název a umístění knihovny DLL, která ho obsahuje.</span><span class="sxs-lookup"><span data-stu-id="a23b6-120">Determine the name of the function you want to call, plus its arguments, argument types, and return value, as well as the name and location of the DLL that contains it.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="a23b6-121">Úplné informace o rozhraních API systému Windows naleznete v dokumentaci k Win32 SDK v rozhraní API platformy Windows API.</span><span class="sxs-lookup"><span data-stu-id="a23b6-121">For complete information about the Windows APIs, see the Win32 SDK documentation in the Platform SDK Windows API.</span></span> <span data-ttu-id="a23b6-122">Další informace o konstantách, které používají rozhraní Windows API, najdete v hlavičkových souborech, jako je Windows. h, které jsou součástí sady SDK platformy.</span><span class="sxs-lookup"><span data-stu-id="a23b6-122">For more information about the constants that Windows APIs use, examine the header files such as Windows.h included with the Platform SDK.</span></span>  
  
2. <span data-ttu-id="a23b6-123">Otevřete nový projekt aplikace pro Windows kliknutím na **Nový** v nabídce **soubor** a potom klikněte na **projekt**.</span><span class="sxs-lookup"><span data-stu-id="a23b6-123">Open a new Windows Application project by clicking **New** on the **File** menu, and then clicking **Project**.</span></span> <span data-ttu-id="a23b6-124">Zobrazí se dialogové okno **Nový projekt** .</span><span class="sxs-lookup"><span data-stu-id="a23b6-124">The **New Project** dialog box appears.</span></span>  
  
3. <span data-ttu-id="a23b6-125">V seznamu Visual Basic šablon projektů vyberte možnost **aplikace systému Windows** .</span><span class="sxs-lookup"><span data-stu-id="a23b6-125">Select **Windows Application** from the list of Visual Basic project templates.</span></span> <span data-ttu-id="a23b6-126">Zobrazí se nový projekt.</span><span class="sxs-lookup"><span data-stu-id="a23b6-126">The new project is displayed.</span></span>  
  
4. <span data-ttu-id="a23b6-127">Přidejte následující funkci `Declare` buď do třídy, nebo modulu, ve kterém chcete použít knihovnu DLL:</span><span class="sxs-lookup"><span data-stu-id="a23b6-127">Add the following `Declare` function either to the class or module in which you want to use the DLL:</span></span>  
  
     [!code-vb[VbVbalrInterop#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#9)]  
  
### <a name="parts-of-the-declare-statement"></a><span data-ttu-id="a23b6-128">Části příkazu Declare</span><span class="sxs-lookup"><span data-stu-id="a23b6-128">Parts of the Declare Statement</span></span>  
 <span data-ttu-id="a23b6-129">Příkaz `Declare` obsahuje následující prvky.</span><span class="sxs-lookup"><span data-stu-id="a23b6-129">The `Declare` statement includes the following elements.</span></span>  
  
#### <a name="auto-modifier"></a><span data-ttu-id="a23b6-130">Automatický modifikátor</span><span class="sxs-lookup"><span data-stu-id="a23b6-130">Auto modifier</span></span>  
 <span data-ttu-id="a23b6-131">Modifikátor `Auto` instruuje modul runtime, aby převedl řetězec na základě názvu metody podle pravidel společného jazykového modulu runtime (nebo název aliasu, je-li zadán).</span><span class="sxs-lookup"><span data-stu-id="a23b6-131">The `Auto` modifier instructs the runtime to convert the string based on the method name according to common language runtime rules (or alias name if specified).</span></span>  
  
#### <a name="lib-and-alias-keywords"></a><span data-ttu-id="a23b6-132">Lib a klíčová slova aliasů</span><span class="sxs-lookup"><span data-stu-id="a23b6-132">Lib and Alias keywords</span></span>  
 <span data-ttu-id="a23b6-133">Název následující po klíčovém slovu `Function` je název, který program používá pro přístup k importované funkci.</span><span class="sxs-lookup"><span data-stu-id="a23b6-133">The name following the `Function` keyword is the name your program uses to access the imported function.</span></span> <span data-ttu-id="a23b6-134">Může to být stejný jako skutečný název volané funkce, nebo můžete použít libovolný platný název procedury a potom pomocí klíčového slova `Alias` zadat skutečný název volané funkce.</span><span class="sxs-lookup"><span data-stu-id="a23b6-134">It can be the same as the real name of the function you are calling, or you can use any valid procedure name and then employ the `Alias` keyword to specify the real name of the function you are calling.</span></span>  
  
 <span data-ttu-id="a23b6-135">Zadejte klíčové slovo `Lib` následovaný názvem a umístěním knihovny DLL, která obsahuje funkci, kterou voláte.</span><span class="sxs-lookup"><span data-stu-id="a23b6-135">Specify the `Lib` keyword, followed by the name and location of the DLL that contains the function you are calling.</span></span> <span data-ttu-id="a23b6-136">Nemusíte zadávat cestu k souborům umístěným v adresářích systému Windows.</span><span class="sxs-lookup"><span data-stu-id="a23b6-136">You do not need to specify the path for files located in the Windows system directories.</span></span>  
  
 <span data-ttu-id="a23b6-137">Klíčové slovo `Alias` použijte v případě, že název volané funkce není platný název Visual Basic procedury nebo je v konfliktu s názvem dalších položek v aplikaci.</span><span class="sxs-lookup"><span data-stu-id="a23b6-137">Use the `Alias` keyword if the name of the function you are calling is not a valid Visual Basic procedure name, or conflicts with the name of other items in your application.</span></span> <span data-ttu-id="a23b6-138">`Alias` označuje skutečný název volané funkce.</span><span class="sxs-lookup"><span data-stu-id="a23b6-138">`Alias` indicates the true name of the function being called.</span></span>  
  
#### <a name="argument-and-data-type-declarations"></a><span data-ttu-id="a23b6-139">Deklarace argumentů a datových typů</span><span class="sxs-lookup"><span data-stu-id="a23b6-139">Argument and Data Type Declarations</span></span>  
 <span data-ttu-id="a23b6-140">Deklarujte argumenty a jejich datové typy.</span><span class="sxs-lookup"><span data-stu-id="a23b6-140">Declare the arguments and their data types.</span></span> <span data-ttu-id="a23b6-141">Tato část může být náročná, protože datové typy, které systém Windows používá, neodpovídají datovým typům sady Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="a23b6-141">This part can be challenging because the data types that Windows uses do not correspond to Visual Studio data types.</span></span> <span data-ttu-id="a23b6-142">Visual Basic provede mnoho práce za vás převodem argumentů na kompatibilní datové typy, což je proces nazvaný *zařazování*.</span><span class="sxs-lookup"><span data-stu-id="a23b6-142">Visual Basic does a lot of the work for you by converting arguments to compatible data types, a process called *marshaling*.</span></span> <span data-ttu-id="a23b6-143">Můžete explicitně řídit, jak jsou argumenty zařazeny pomocí atributu <xref:System.Runtime.InteropServices.MarshalAsAttribute> definovaného v oboru názvů <xref:System.Runtime.InteropServices>.</span><span class="sxs-lookup"><span data-stu-id="a23b6-143">You can explicitly control how arguments are marshaled by using the <xref:System.Runtime.InteropServices.MarshalAsAttribute> attribute defined in the <xref:System.Runtime.InteropServices> namespace.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="a23b6-144">Předchozí verze Visual Basic umožňují deklarovat parametry `As Any`, což znamená, že je možné použít data libovolného datového typu.</span><span class="sxs-lookup"><span data-stu-id="a23b6-144">Previous versions of Visual Basic allowed you to declare parameters `As Any`, meaning that data of any data type could be used.</span></span> <span data-ttu-id="a23b6-145">Visual Basic vyžaduje, abyste pro všechny příkazy `Declare` používali určitý datový typ.</span><span class="sxs-lookup"><span data-stu-id="a23b6-145">Visual Basic requires that you use a specific data type for all `Declare` statements.</span></span>  
  
#### <a name="windows-api-constants"></a><span data-ttu-id="a23b6-146">Konstanty rozhraní API systému Windows</span><span class="sxs-lookup"><span data-stu-id="a23b6-146">Windows API Constants</span></span>  
 <span data-ttu-id="a23b6-147">Některé argumenty představují kombinace konstant.</span><span class="sxs-lookup"><span data-stu-id="a23b6-147">Some arguments are combinations of constants.</span></span> <span data-ttu-id="a23b6-148">Například rozhraní API `MessageBox` zobrazené v tomto návodu přijímá celočíselný argument nazvaný `Typ`, který určuje, jak se okno se zprávou zobrazuje.</span><span class="sxs-lookup"><span data-stu-id="a23b6-148">For example, the `MessageBox` API shown in this walkthrough accepts an integer argument called `Typ` that controls how the message box is displayed.</span></span> <span data-ttu-id="a23b6-149">Číselnou hodnotu těchto konstant můžete určit prozkoumáním příkazů `#define` v souboru WinUser. h.</span><span class="sxs-lookup"><span data-stu-id="a23b6-149">You can determine the numeric value of these constants by examining the `#define` statements in the file WinUser.h.</span></span> <span data-ttu-id="a23b6-150">Číselné hodnoty jsou obecně zobrazeny v hexadecimálním formátu, takže můžete chtít použít kalkulačku pro jejich přidání a převod na desetinné číslo.</span><span class="sxs-lookup"><span data-stu-id="a23b6-150">The numeric values are generally shown in hexadecimal, so you may want to use a calculator to add them and convert to decimal.</span></span> <span data-ttu-id="a23b6-151">Například pokud chcete kombinovat konstanty pro typ vykřičník `MB_ICONEXCLAMATION` 0x00000030 a styl ano/ne `MB_YESNO` 0x00000004, můžete přidat čísla a získat výsledek 0x00000034 nebo desítkového čísla 52.</span><span class="sxs-lookup"><span data-stu-id="a23b6-151">For example, if you want to combine the constants for the exclamation style `MB_ICONEXCLAMATION` 0x00000030 and the Yes/No style `MB_YESNO` 0x00000004, you can add the numbers and get a result of 0x00000034, or 52 decimal.</span></span> <span data-ttu-id="a23b6-152">I když můžete použít výsledek Decimal přímo, je lepší deklarovat tyto hodnoty jako konstanty ve vaší aplikaci a kombinovat je pomocí operátoru `Or`.</span><span class="sxs-lookup"><span data-stu-id="a23b6-152">Although you can use the decimal result directly, it is better to declare these values as constants in your application and combine them using the `Or` operator.</span></span>  
  
##### <a name="to-declare-constants-for-windows-api-calls"></a><span data-ttu-id="a23b6-153">Deklarace konstant pro volání rozhraní API systému Windows</span><span class="sxs-lookup"><span data-stu-id="a23b6-153">To declare constants for Windows API calls</span></span>  
  
1. <span data-ttu-id="a23b6-154">Projděte si dokumentaci pro funkci Windows, kterou voláte.</span><span class="sxs-lookup"><span data-stu-id="a23b6-154">Consult the documentation for the Windows function you are calling.</span></span> <span data-ttu-id="a23b6-155">Určete název konstant, které používá, a název souboru. h, který obsahuje číselné hodnoty pro tyto konstanty.</span><span class="sxs-lookup"><span data-stu-id="a23b6-155">Determine the name of the constants it uses and the name of the .h file that contains the numeric values for these constants.</span></span>  
  
2. <span data-ttu-id="a23b6-156">Použijte textový editor, například Poznámkový blok, k zobrazení obsahu souboru hlaviček (. h) a vyhledejte hodnoty přidružené k konstantám, které používáte.</span><span class="sxs-lookup"><span data-stu-id="a23b6-156">Use a text editor, such as Notepad, to view the contents of the header (.h) file, and find the values associated with the constants you are using.</span></span> <span data-ttu-id="a23b6-157">Rozhraní `MessageBox` API například používá konstantu `MB_ICONQUESTION` k zobrazení otazníku v okně se zprávou.</span><span class="sxs-lookup"><span data-stu-id="a23b6-157">For example, the `MessageBox` API uses the constant `MB_ICONQUESTION` to show a question mark in the message box.</span></span> <span data-ttu-id="a23b6-158">Definice pro `MB_ICONQUESTION` je v WinUser. h a vypadá takto:</span><span class="sxs-lookup"><span data-stu-id="a23b6-158">The definition for `MB_ICONQUESTION` is in WinUser.h and appears as follows:</span></span>  
  
     `#define MB_ICONQUESTION             0x00000020L`  
  
3. <span data-ttu-id="a23b6-159">Přidejte ekvivalentní příkazy `Const` do třídy nebo modulu, aby byly tyto konstanty k dispozici pro vaši aplikaci.</span><span class="sxs-lookup"><span data-stu-id="a23b6-159">Add equivalent `Const` statements to your class or module to make these constants available to your application.</span></span> <span data-ttu-id="a23b6-160">Příklad:</span><span class="sxs-lookup"><span data-stu-id="a23b6-160">For example:</span></span>  
  
     [!code-vb[VbVbalrInterop#11](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#11)]  
  
###### <a name="to-call-the-dll-procedure"></a><span data-ttu-id="a23b6-161">Volání procedury DLL</span><span class="sxs-lookup"><span data-stu-id="a23b6-161">To call the DLL procedure</span></span>  
  
1. <span data-ttu-id="a23b6-162">Přidejte tlačítko s názvem `Button1` do formuláře po spuštění pro váš projekt a pak dvakrát klikněte na něj pro zobrazení kódu.</span><span class="sxs-lookup"><span data-stu-id="a23b6-162">Add a button named `Button1` to the startup form for your project, and then double-click it to view its code.</span></span> <span data-ttu-id="a23b6-163">Zobrazí se obslužná rutina události pro tlačítko.</span><span class="sxs-lookup"><span data-stu-id="a23b6-163">The event handler for the button is displayed.</span></span>  
  
2. <span data-ttu-id="a23b6-164">Přidejte kód do obslužné rutiny události `Click` pro tlačítko, které jste přidali, pro volání procedury a poskytněte příslušné argumenty:</span><span class="sxs-lookup"><span data-stu-id="a23b6-164">Add code to the `Click` event handler for the button you added, to call the procedure and provide the appropriate arguments:</span></span>  
  
     [!code-vb[VbVbalrInterop#12](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#12)]  
  
3. <span data-ttu-id="a23b6-165">Spusťte projekt stisknutím klávesy F5.</span><span class="sxs-lookup"><span data-stu-id="a23b6-165">Run the project by pressing F5.</span></span> <span data-ttu-id="a23b6-166">Okno se zprávou se zobrazí s tlačítky **Ano** i **bez** odpovědi.</span><span class="sxs-lookup"><span data-stu-id="a23b6-166">The message box is displayed with both **Yes** and **No** response buttons.</span></span> <span data-ttu-id="a23b6-167">Klikněte na jednu z těchto.</span><span class="sxs-lookup"><span data-stu-id="a23b6-167">Click either one.</span></span>  
  
#### <a name="data-marshaling"></a><span data-ttu-id="a23b6-168">Zařazování dat</span><span class="sxs-lookup"><span data-stu-id="a23b6-168">Data Marshaling</span></span>  
 <span data-ttu-id="a23b6-169">Visual Basic automaticky převádí datové typy parametrů a návratové hodnoty pro volání rozhraní API systému Windows, ale můžete použít atribut `MarshalAs` k explicitnímu zadání nespravovaných datových typů, které rozhraní API očekává.</span><span class="sxs-lookup"><span data-stu-id="a23b6-169">Visual Basic automatically converts the data types of parameters and return values for Windows API calls, but you can use the `MarshalAs` attribute to explicitly specify unmanaged data types that an API expects.</span></span> <span data-ttu-id="a23b6-170">Další informace o interop marshaling najdete v tématu [zařazování Interop](../../../framework/interop/interop-marshaling.md).</span><span class="sxs-lookup"><span data-stu-id="a23b6-170">For more information about interop marshaling, see [Interop Marshaling](../../../framework/interop/interop-marshaling.md).</span></span>  
  
##### <a name="to-use-declare-and-marshalas-in-an-api-call"></a><span data-ttu-id="a23b6-171">Použití příkazu Declare a MarshalAs v volání rozhraní API</span><span class="sxs-lookup"><span data-stu-id="a23b6-171">To use Declare and MarshalAs in an API call</span></span>  
  
1. <span data-ttu-id="a23b6-172">Určete název funkce, kterou chcete volat, včetně jejích argumentů, datových typů a návratové hodnoty.</span><span class="sxs-lookup"><span data-stu-id="a23b6-172">Determine the name of the function you want to call, plus its arguments, data types, and return value.</span></span>  
  
2. <span data-ttu-id="a23b6-173">Chcete-li zjednodušit přístup k atributu `MarshalAs`, přidejte příkaz `Imports` na začátek kódu pro třídu nebo modul, jako v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="a23b6-173">To simplify access to the `MarshalAs` attribute, add an `Imports` statement to the top of the code for the class or module, as in the following example:</span></span>  
  
     [!code-vb[VbVbalrInterop#13](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#13)]  
  
3. <span data-ttu-id="a23b6-174">Přidejte prototyp funkce pro importovanou funkci do třídy nebo modulu, který používáte, a použijte atribut `MarshalAs` pro parametry nebo návratovou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a23b6-174">Add a function prototype for the imported function to the class or module you are using, and apply the `MarshalAs` attribute to the parameters or return value.</span></span> <span data-ttu-id="a23b6-175">V následujícím příkladu je volání rozhraní API, které očekává typ `void*`, zařazeno jako `AsAny`:</span><span class="sxs-lookup"><span data-stu-id="a23b6-175">In the following example, an API call that expects the type `void*` is marshaled as `AsAny`:</span></span>  
  
     [!code-vb[VbVbalrInterop#14](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#14)]  
  
## <a name="api-calls-using-dllimport"></a><span data-ttu-id="a23b6-176">Volání rozhraní API pomocí DllImport</span><span class="sxs-lookup"><span data-stu-id="a23b6-176">API Calls Using DllImport</span></span>  
 <span data-ttu-id="a23b6-177">Atribut `DllImport` poskytuje druhý způsob volání funkcí v knihovnách DLL bez knihoven typů.</span><span class="sxs-lookup"><span data-stu-id="a23b6-177">The `DllImport` attribute provides a second way to call functions in DLLs without type libraries.</span></span> <span data-ttu-id="a23b6-178">`DllImport` je zhruba ekvivalentní použití příkazu `Declare`, ale poskytuje lepší kontrolu nad tím, jak jsou funkce volány.</span><span class="sxs-lookup"><span data-stu-id="a23b6-178">`DllImport` is roughly equivalent to using a `Declare` statement but provides more control over how functions are called.</span></span>  
  
 <span data-ttu-id="a23b6-179">Můžete použít `DllImport` u většiny volání rozhraní API systému Windows, pokud volání odkazuje na sdílenou (někdy označovanou jako *statickou*) metodu.</span><span class="sxs-lookup"><span data-stu-id="a23b6-179">You can use `DllImport` with most Windows API calls as long as the call refers to a shared (sometimes called *static*) method.</span></span> <span data-ttu-id="a23b6-180">Nemůžete použít metody, které vyžadují instanci třídy.</span><span class="sxs-lookup"><span data-stu-id="a23b6-180">You cannot use methods that require an instance of a class.</span></span> <span data-ttu-id="a23b6-181">Na rozdíl od příkazů `Declare` nemůže `DllImport` volání používat atribut `MarshalAs`.</span><span class="sxs-lookup"><span data-stu-id="a23b6-181">Unlike `Declare` statements, `DllImport` calls cannot use the `MarshalAs` attribute.</span></span>  
  
### <a name="to-call-a-windows-api-using-the-dllimport-attribute"></a><span data-ttu-id="a23b6-182">Volání rozhraní API systému Windows pomocí atributu DllImport</span><span class="sxs-lookup"><span data-stu-id="a23b6-182">To call a Windows API using the DllImport attribute</span></span>  
  
1. <span data-ttu-id="a23b6-183">Otevřete nový projekt aplikace pro Windows kliknutím na **Nový** v nabídce **soubor** a potom klikněte na **projekt**.</span><span class="sxs-lookup"><span data-stu-id="a23b6-183">Open a new Windows Application project by clicking **New** on the **File** menu, and then clicking **Project**.</span></span> <span data-ttu-id="a23b6-184">Zobrazí se dialogové okno **Nový projekt** .</span><span class="sxs-lookup"><span data-stu-id="a23b6-184">The **New Project** dialog box appears.</span></span>  
  
2. <span data-ttu-id="a23b6-185">V seznamu Visual Basic šablon projektů vyberte možnost **aplikace systému Windows** .</span><span class="sxs-lookup"><span data-stu-id="a23b6-185">Select **Windows Application** from the list of Visual Basic project templates.</span></span> <span data-ttu-id="a23b6-186">Zobrazí se nový projekt.</span><span class="sxs-lookup"><span data-stu-id="a23b6-186">The new project is displayed.</span></span>  
  
3. <span data-ttu-id="a23b6-187">Přidejte tlačítko s názvem `Button2` do formuláře po spuštění.</span><span class="sxs-lookup"><span data-stu-id="a23b6-187">Add a button named `Button2` to the startup form.</span></span>  
  
4. <span data-ttu-id="a23b6-188">Dvojím kliknutím na `Button2` otevřete zobrazení kódu pro formulář.</span><span class="sxs-lookup"><span data-stu-id="a23b6-188">Double-click `Button2` to open the code view for the form.</span></span>  
  
5. <span data-ttu-id="a23b6-189">Chcete-li zjednodušit přístup k `DllImport`, přidejte příkaz `Imports` do horní části kódu pro třídu formuláře po spuštění:</span><span class="sxs-lookup"><span data-stu-id="a23b6-189">To simplify access to `DllImport`, add an `Imports` statement to the top of the code for the startup form class:</span></span>  
  
     [!code-vb[VbVbalrInterop#13](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#13)]  
  
6. <span data-ttu-id="a23b6-190">Deklarujte prázdnou funkci před příkazem `End Class` pro formulář a pojmenujte funkci `MoveFile`.</span><span class="sxs-lookup"><span data-stu-id="a23b6-190">Declare an empty function preceding the `End Class` statement for the form, and name the function `MoveFile`.</span></span>  
  
7. <span data-ttu-id="a23b6-191">Použijte modifikátory `Public` a `Shared` na deklaraci funkce a nastavte parametry pro `MoveFile` na základě argumentů, které funkce rozhraní API systému Windows používá:</span><span class="sxs-lookup"><span data-stu-id="a23b6-191">Apply the `Public` and `Shared` modifiers to the function declaration and set parameters for `MoveFile` based on the arguments the Windows API function uses:</span></span>  
  
     [!code-vb[VbVbalrInterop#16](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#16)]  
  
     <span data-ttu-id="a23b6-192">Vaše funkce může mít libovolný platný název procedury; atribut `DllImport` Určuje název v knihovně DLL.</span><span class="sxs-lookup"><span data-stu-id="a23b6-192">Your function can have any valid procedure name; the `DllImport` attribute specifies the name in the DLL.</span></span> <span data-ttu-id="a23b6-193">Také zpracovává zařazování interoperability pro parametry a návratové hodnoty, takže můžete zvolit datové typy sady Visual Studio, které jsou podobné datovým typům, které používá rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="a23b6-193">It also handles interoperability marshaling for the parameters and return values, so you can choose Visual Studio data types that are similar to the data types the API uses.</span></span>  
  
8. <span data-ttu-id="a23b6-194">Použijte atribut `DllImport` pro prázdnou funkci.</span><span class="sxs-lookup"><span data-stu-id="a23b6-194">Apply the `DllImport` attribute to the empty function.</span></span> <span data-ttu-id="a23b6-195">První parametr je název a umístění knihovny DLL obsahující funkci, kterou voláte.</span><span class="sxs-lookup"><span data-stu-id="a23b6-195">The first parameter is the name and location of the DLL containing the function you are calling.</span></span> <span data-ttu-id="a23b6-196">Nemusíte zadávat cestu k souborům umístěným v adresářích systému Windows.</span><span class="sxs-lookup"><span data-stu-id="a23b6-196">You do not need to specify the path for files located in the Windows system directories.</span></span> <span data-ttu-id="a23b6-197">Druhý parametr je pojmenovaný argument, který určuje název funkce v rozhraní API systému Windows.</span><span class="sxs-lookup"><span data-stu-id="a23b6-197">The second parameter is a named argument that specifies the name of the function in the Windows API.</span></span> <span data-ttu-id="a23b6-198">V tomto příkladu atribut `DllImport` vynutí volání `MoveFile` přesměrování do `MoveFileW` v KERNEL32. DLL.</span><span class="sxs-lookup"><span data-stu-id="a23b6-198">In this example, the `DllImport` attribute forces calls to `MoveFile` to be forwarded to `MoveFileW` in KERNEL32.DLL.</span></span> <span data-ttu-id="a23b6-199">Metoda `MoveFileW` zkopíruje soubor z cesty `src` do cesty `dst`.</span><span class="sxs-lookup"><span data-stu-id="a23b6-199">The `MoveFileW` method copies a file from the path `src` to the path `dst`.</span></span>  
  
     [!code-vb[VbVbalrInterop#17](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#17)]  
  
9. <span data-ttu-id="a23b6-200">Přidejte kód do obslužné rutiny události `Button2_Click` pro volání funkce:</span><span class="sxs-lookup"><span data-stu-id="a23b6-200">Add code to the `Button2_Click` event handler to call the function:</span></span>  
  
     [!code-vb[VbVbalrInterop#18](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#18)]  
  
10. <span data-ttu-id="a23b6-201">Vytvořte soubor s názvem test. txt a umístěte ho do adresáře C:\Tmp na pevném disku.</span><span class="sxs-lookup"><span data-stu-id="a23b6-201">Create a file named Test.txt and place it in the C:\Tmp directory on your hard drive.</span></span> <span data-ttu-id="a23b6-202">V případě potřeby vytvořte adresář tmp.</span><span class="sxs-lookup"><span data-stu-id="a23b6-202">Create the Tmp directory if necessary.</span></span>  
  
11. <span data-ttu-id="a23b6-203">Stisknutím klávesy F5 spusťte aplikaci.</span><span class="sxs-lookup"><span data-stu-id="a23b6-203">Press F5 to start the application.</span></span> <span data-ttu-id="a23b6-204">Zobrazí se hlavní formulář.</span><span class="sxs-lookup"><span data-stu-id="a23b6-204">The main form appears.</span></span>  
  
12. <span data-ttu-id="a23b6-205">Klikněte na možnost **Button2**.</span><span class="sxs-lookup"><span data-stu-id="a23b6-205">Click **Button2**.</span></span> <span data-ttu-id="a23b6-206">Zpráva "soubor byl úspěšně přesunut" se zobrazí, pokud jej lze přesunout.</span><span class="sxs-lookup"><span data-stu-id="a23b6-206">The message "The file was moved successfully" is displayed if the file can be moved.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="a23b6-207">Viz také:</span><span class="sxs-lookup"><span data-stu-id="a23b6-207">See also</span></span>

- <xref:System.Runtime.InteropServices.DllImportAttribute>
- <xref:System.Runtime.InteropServices.MarshalAsAttribute>
- [<span data-ttu-id="a23b6-208">Příkaz Declare</span><span class="sxs-lookup"><span data-stu-id="a23b6-208">Declare Statement</span></span>](../../../visual-basic/language-reference/statements/declare-statement.md)
- [<span data-ttu-id="a23b6-209">Auto</span><span class="sxs-lookup"><span data-stu-id="a23b6-209">Auto</span></span>](../../../visual-basic/language-reference/modifiers/auto.md)
- [<span data-ttu-id="a23b6-210">Zástupný</span><span class="sxs-lookup"><span data-stu-id="a23b6-210">Alias</span></span>](../../../visual-basic/language-reference/statements/alias-clause.md)
- [<span data-ttu-id="a23b6-211">Zprostředkovatel komunikace s objekty COM</span><span class="sxs-lookup"><span data-stu-id="a23b6-211">COM Interop</span></span>](../../../visual-basic/programming-guide/com-interop/index.md)
- [<span data-ttu-id="a23b6-212">Vytváření prototypů ve spravovaném kódu</span><span class="sxs-lookup"><span data-stu-id="a23b6-212">Creating Prototypes in Managed Code</span></span>](../../../framework/interop/creating-prototypes-in-managed-code.md)
- [<span data-ttu-id="a23b6-213">Zařazování delegáta jako metody zpětného volání</span><span class="sxs-lookup"><span data-stu-id="a23b6-213">Marshaling a Delegate as a Callback Method</span></span>](../../../framework/interop/marshaling-a-delegate-as-a-callback-method.md)
