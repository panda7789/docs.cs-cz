---
title: Rozšíření oboru názvů My
ms.date: 07/20/2015
f1_keywords:
- vb.AddingMyExtensions
helpviewer_keywords:
- My namespace [Visual Basic], customizing
- My namespace
- My namespace [Visual Basic], extending
ms.assetid: 808e8617-b01c-4135-8b21-babe87389e8e
ms.openlocfilehash: 2a7b0b84061fccd9a333a68e4a19477bd19ca4ff
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 11/22/2019
ms.locfileid: "74330323"
---
# <a name="extending-the-my-namespace-in-visual-basic"></a><span data-ttu-id="7bf63-102">Rozšíření `My` oboru názvů v Visual Basic</span><span class="sxs-lookup"><span data-stu-id="7bf63-102">Extending the `My` Namespace in Visual Basic</span></span>

<span data-ttu-id="7bf63-103">`My` Obor názvů v Visual Basic zpřístupňuje vlastnosti a metody, které vám umožní snadno využít sílu .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="7bf63-103">The `My` namespace in Visual Basic exposes properties and methods that enable you to easily take advantage of the power of the .NET Framework.</span></span> <span data-ttu-id="7bf63-104">`My` Obor názvů zjednodušuje běžné programovací problémy, často snižuje obtížné úlohu na jeden řádek kódu.</span><span class="sxs-lookup"><span data-stu-id="7bf63-104">The `My` namespace simplifies common programming problems, often reducing a difficult task to a single line of code.</span></span> <span data-ttu-id="7bf63-105">`My` Obor názvů je navíc plně rozšiřitelný, takže můžete přizpůsobit chování `My` a přidat nové služby do své hierarchie, abyste je přizpůsobili konkrétním potřebám aplikací.</span><span class="sxs-lookup"><span data-stu-id="7bf63-105">Additionally, the `My` namespace is fully extensible so that you can customize the behavior of `My` and add new services to its hierarchy to adapt to specific application needs.</span></span> <span data-ttu-id="7bf63-106">Toto téma popisuje, jak přizpůsobit stávající členy `My` oboru názvů a jak přidat vlastní třídy do `My` oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="7bf63-106">This topic discusses both how to customize existing members of the `My` namespace and how to add your own custom classes to the `My` namespace.</span></span>

## <a name="customizing-existing-my-namespace-members"></a><span data-ttu-id="7bf63-107">Přizpůsobení stávajících `My` členů oboru názvů</span><span class="sxs-lookup"><span data-stu-id="7bf63-107">Customizing existing `My` namespace members</span></span>

<span data-ttu-id="7bf63-108">`My` Obor názvů v Visual Basic zpřístupňuje často používané informace o vaší aplikaci, počítači a dalších.</span><span class="sxs-lookup"><span data-stu-id="7bf63-108">The `My` namespace in Visual Basic exposes frequently used information about your application, your computer, and more.</span></span> <span data-ttu-id="7bf63-109">Úplný seznam objektů v `My` oboru názvů najdete v tématu [můj odkaz](../../language-reference/keywords/my-reference.md).</span><span class="sxs-lookup"><span data-stu-id="7bf63-109">For a complete list of the objects in the `My` namespace, see [My Reference](../../language-reference/keywords/my-reference.md).</span></span> <span data-ttu-id="7bf63-110">Je možné, že budete muset přizpůsobit stávající členy `My` oboru názvů tak, aby lépe odpovídaly potřebám vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="7bf63-110">You may have to customize existing members of the `My` namespace so that they better match the needs of your application.</span></span> <span data-ttu-id="7bf63-111">Vlastnost objektu v `My` oboru názvů, která není jen pro čtení, může být nastavena na vlastní hodnotu.</span><span class="sxs-lookup"><span data-stu-id="7bf63-111">Any property of an object in the `My` namespace that is not read-only can be set to a custom value.</span></span>

<span data-ttu-id="7bf63-112">Předpokládejme například, že často používáte `My.User` objekt pro přístup k aktuálnímu kontextu zabezpečení pro uživatele, který spouští vaši aplikaci.</span><span class="sxs-lookup"><span data-stu-id="7bf63-112">For example, assume that you frequently use the `My.User` object to access the current security context for the user running your application.</span></span> <span data-ttu-id="7bf63-113">Vaše společnost ale používá vlastní objekt uživatele k vystavování dalších informací a možností pro uživatele v rámci společnosti.</span><span class="sxs-lookup"><span data-stu-id="7bf63-113">However, your company uses a custom user object to expose additional information and capabilities for users within the company.</span></span> <span data-ttu-id="7bf63-114">V tomto scénáři můžete nahradit výchozí hodnotu `My.User.CurrentPrincipal` vlastnosti instancí vlastního objektu zabezpečení, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="7bf63-114">In this scenario, you can replace the default value of the `My.User.CurrentPrincipal` property with an instance of your own custom principal object, as shown in the following example:</span></span>

[!code-vb[VbVbcnExtendingMy#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnExtendingMy/VB/Class1.vb#1)]

<span data-ttu-id="7bf63-115">Nastavením `CurrentPrincipal` vlastnosti `My.User` objektu se změní identita, pod kterou je aplikace spuštěna.</span><span class="sxs-lookup"><span data-stu-id="7bf63-115">Setting the `CurrentPrincipal` property on the `My.User` object changes the identity under which the application runs.</span></span> <span data-ttu-id="7bf63-116">`My.User` Objekt zase vrátí informace o nově zadaném uživateli.</span><span class="sxs-lookup"><span data-stu-id="7bf63-116">The `My.User` object, in turn, returns information about the newly specified user.</span></span>
  
## <a name="adding-members-to-my-objects"></a><span data-ttu-id="7bf63-117">Přidávání členů do `My` objektů</span><span class="sxs-lookup"><span data-stu-id="7bf63-117">Adding members to `My` objects</span></span>

<span data-ttu-id="7bf63-118">Typy vrácené z `My.Application` a `My.Computer` jsou definovány jako `Partial` třídy.</span><span class="sxs-lookup"><span data-stu-id="7bf63-118">The types returned from `My.Application` and `My.Computer` are defined as `Partial` classes.</span></span> <span data-ttu-id="7bf63-119">Proto můžete `My.Application` objekty a `My.Computer` vytvořit tak, že vytvoříte `Partial` třídu s názvem `MyApplication` nebo. `MyComputer`</span><span class="sxs-lookup"><span data-stu-id="7bf63-119">Therefore, you can extend the `My.Application` and `My.Computer` objects by creating a `Partial` class named `MyApplication` or `MyComputer`.</span></span> <span data-ttu-id="7bf63-120">Třída nemůže být `Private` třídou.</span><span class="sxs-lookup"><span data-stu-id="7bf63-120">The class cannot be a `Private` class.</span></span> <span data-ttu-id="7bf63-121">Pokud zadáte třídu jako součást `My` oboru názvů, můžete přidat vlastnosti a metody, které budou zahrnuty do `My.Application` objektů nebo. `My.Computer`</span><span class="sxs-lookup"><span data-stu-id="7bf63-121">If you specify the class as part of the `My` namespace, you can add properties and methods that will be included with the `My.Application` or `My.Computer` objects.</span></span>

<span data-ttu-id="7bf63-122">Následující příklad přidá vlastnost s názvem `DnsServerIPAddresses` do `My.Computer` objektu:</span><span class="sxs-lookup"><span data-stu-id="7bf63-122">The following example adds a property named `DnsServerIPAddresses` to the `My.Computer` object:</span></span>

[!code-vb[VbVbcnExtendingMy#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnExtendingMy/VB/Class2.vb#2)]

## <a name="adding-custom-objects-to-the-my-namespace"></a><span data-ttu-id="7bf63-123">Přidání vlastních objektů do `My` oboru názvů</span><span class="sxs-lookup"><span data-stu-id="7bf63-123">Adding custom objects to the `My` namespace</span></span>

<span data-ttu-id="7bf63-124">I když `My` obor názvů poskytuje řešení pro mnoho běžných programovacích úloh, může dojít k úlohám, které `My` obor názvů neřeší.</span><span class="sxs-lookup"><span data-stu-id="7bf63-124">Although the `My` namespace provides solutions for many common programming tasks, you may encounter tasks that the `My` namespace does not address.</span></span> <span data-ttu-id="7bf63-125">Vaše aplikace může například získat přístup k vlastním adresářovým službám pro uživatelská data nebo vaše aplikace může používat sestavení, která nejsou ve výchozím nastavení nainstalovaná pomocí Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="7bf63-125">For example, your application might access custom directory services for user data, or your application might use assemblies that are not installed by default with Visual Basic.</span></span> <span data-ttu-id="7bf63-126">`My` Obor názvů můžete zvětšit tak, aby zahrnoval vlastní řešení běžných úloh, které jsou specifické pro vaše prostředí.</span><span class="sxs-lookup"><span data-stu-id="7bf63-126">You can extend the `My` namespace to include custom solutions to common tasks that are specific to your environment.</span></span> <span data-ttu-id="7bf63-127">`My` Obor názvů lze snadno rozšířit a přidat nové členy pro splnění rostoucích potřeb aplikací.</span><span class="sxs-lookup"><span data-stu-id="7bf63-127">The `My` namespace can easily be extended to add new members to meet growing application needs.</span></span> <span data-ttu-id="7bf63-128">Kromě toho můžete nasadit rozšíření `My` oboru názvů jiným vývojářům jako šablonu Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="7bf63-128">Additionally, you can deploy your `My` namespace extensions to other developers as a Visual Basic template.</span></span>
  
### <a name="adding-members-to-the-my-namespace"></a><span data-ttu-id="7bf63-129">Přidávání členů do `My` oboru názvů</span><span class="sxs-lookup"><span data-stu-id="7bf63-129">Adding members to the `My` namespace</span></span>

<span data-ttu-id="7bf63-130">Vzhledem `My` k tomu, že se jedná o obor názvů jako jakýkoliv jiný obor názvů, můžete do něj přidat vlastnosti nejvyšší úrovně pouhým přidáním `Namespace` modulu `My`a zadáním typu.</span><span class="sxs-lookup"><span data-stu-id="7bf63-130">Because `My` is a namespace like any other namespace, you can add top-level properties to it by just adding a module and specifying a `Namespace` of `My`.</span></span> <span data-ttu-id="7bf63-131">Přihlaste modulu s `HideModuleName` atributem, jak je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="7bf63-131">Annotate the module with the `HideModuleName` attribute as shown in the following example.</span></span> <span data-ttu-id="7bf63-132">`HideModuleName` Atribut zajišťuje, aby technologie IntelliSense při zobrazení členů `My` oboru názvů nezobrazila název modulu.</span><span class="sxs-lookup"><span data-stu-id="7bf63-132">The `HideModuleName` attribute ensures that IntelliSense will not display the module name when it displays the members of the `My` namespace.</span></span>

[!code-vb[VbVbcnExtendingMy#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnExtendingMy/VB/Class1.vb#3)]

<span data-ttu-id="7bf63-133">Chcete-li přidat členy `My` do oboru názvů, přidejte do modulu vlastnosti podle potřeby.</span><span class="sxs-lookup"><span data-stu-id="7bf63-133">To add members to the `My` namespace, add properties as needed to the module.</span></span> <span data-ttu-id="7bf63-134">Pro každou vlastnost přidanou do `My` oboru názvů přidejte soukromé pole typu `ThreadSafeObjectProvider(Of T)`, kde typ je typ vrácený vaší vlastní vlastností.</span><span class="sxs-lookup"><span data-stu-id="7bf63-134">For each property added to the `My` namespace, add a private field of type `ThreadSafeObjectProvider(Of T)`, where the type is the type returned by your custom property.</span></span> <span data-ttu-id="7bf63-135">Toto pole slouží k vytvoření instancí objektů bezpečných pro přístup z více vláken, které mají být vráceny vlastností `GetInstance` voláním metody.</span><span class="sxs-lookup"><span data-stu-id="7bf63-135">This field is used to create thread-safe object instances to be returned by the property by calling the `GetInstance` method.</span></span> <span data-ttu-id="7bf63-136">V důsledku toho každé vlákno, které přistupuje k rozšířené vlastnosti, obdrží svou vlastní instanci vráceného typu.</span><span class="sxs-lookup"><span data-stu-id="7bf63-136">As a result, each thread that is accessing the extended property receives its own instance of the returned type.</span></span> <span data-ttu-id="7bf63-137">Následující příklad přidá vlastnost s názvem `SampleExtension` , která je typu `SampleExtension` do `My` oboru názvů:</span><span class="sxs-lookup"><span data-stu-id="7bf63-137">The following example adds a property named `SampleExtension` that is of type `SampleExtension` to the `My` namespace:</span></span>

[!code-vb[VbVbcnExtendingMy#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnExtendingMy/VB/Class1.vb#4)]

## <a name="adding-events-to-custom-my-objects"></a><span data-ttu-id="7bf63-138">Přidávání událostí do vlastních `My` objektů</span><span class="sxs-lookup"><span data-stu-id="7bf63-138">Adding events to custom `My` objects</span></span>

<span data-ttu-id="7bf63-139">Můžete použít `My.Application` objekt k vystavení událostí pro vlastní `My` objekty rozšířením `MyApplication` částečné třídy v `My` oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="7bf63-139">You can use the `My.Application` object to expose events for your custom `My` objects by extending the `MyApplication` partial class in the `My` namespace.</span></span> <span data-ttu-id="7bf63-140">Pro projekty založené na systému Windows můžete dvakrát kliknout na uzel **můj projekt** v pro projekt v **Průzkumník řešení**.</span><span class="sxs-lookup"><span data-stu-id="7bf63-140">For Windows-based projects, you can double-click the **My Project** node in for your project in **Solution Explorer**.</span></span> <span data-ttu-id="7bf63-141">V **Návrháři projektu**Visual Basic klikněte na kartu **aplikace** a pak klikněte na tlačítko **Zobrazit události aplikace** .</span><span class="sxs-lookup"><span data-stu-id="7bf63-141">In the Visual Basic **Project Designer**, click the **Application** tab and then click the **View Application Events** button.</span></span> <span data-ttu-id="7bf63-142">Vytvoří se nový soubor s názvem *ApplicationEvents. vb* .</span><span class="sxs-lookup"><span data-stu-id="7bf63-142">A new file that is named *ApplicationEvents.vb* will be created.</span></span> <span data-ttu-id="7bf63-143">Obsahuje následující kód pro rozšíření `MyApplication` třídy:</span><span class="sxs-lookup"><span data-stu-id="7bf63-143">It contains the following code for extending the `MyApplication` class:</span></span>

[!code-vb[VbVbcnExtendingMy#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnExtendingMy/VB/Class1.vb#5)]

<span data-ttu-id="7bf63-144">Můžete přidat obslužné rutiny událostí pro vlastní `My` objekty přidáním vlastních obslužných rutin událostí do `MyApplication` třídy.</span><span class="sxs-lookup"><span data-stu-id="7bf63-144">You can add event handlers for your custom `My` objects by adding custom event handlers to the `MyApplication` class.</span></span> <span data-ttu-id="7bf63-145">Vlastní události umožňují přidat kód, který se spustí při přidání, odebrání nebo vyvolání obslužné rutiny události.</span><span class="sxs-lookup"><span data-stu-id="7bf63-145">Custom events enable you to add code that will execute when an event handler is added, removed, or the event is raised.</span></span> <span data-ttu-id="7bf63-146">Všimněte si, `AddHandler` že kód vlastní události je spuštěn pouze v případě, že uživatel přidá kód pro zpracování události.</span><span class="sxs-lookup"><span data-stu-id="7bf63-146">Note that the `AddHandler` code for a custom event runs only if code is added by a user to handle the event.</span></span> <span data-ttu-id="7bf63-147">Například zvažte, že `SampleExtension` objekt z předchozí části obsahuje `Load` událost, pro kterou chcete přidat vlastní obslužnou rutinu události pro.</span><span class="sxs-lookup"><span data-stu-id="7bf63-147">For example, consider that the `SampleExtension` object from the previous section has a `Load` event that you want to add a custom event handler for.</span></span> <span data-ttu-id="7bf63-148">Následující příklad kódu ukazuje vlastní obslužnou rutinu události s `SampleExtensionLoad` názvem, která bude vyvolána při `My.SampleExtension.Load` výskytu události.</span><span class="sxs-lookup"><span data-stu-id="7bf63-148">The following code example shows a custom event handler named `SampleExtensionLoad` that will be invoked when the `My.SampleExtension.Load` event occurs.</span></span> <span data-ttu-id="7bf63-149">Když je přidán kód pro zpracování nové `My.SampleExtensionLoad` události, je provedena `AddHandler` část tohoto vlastního kódu události.</span><span class="sxs-lookup"><span data-stu-id="7bf63-149">When code is added to handle the new `My.SampleExtensionLoad` event, the `AddHandler` part of this custom event code is executed.</span></span> <span data-ttu-id="7bf63-150">`MyApplication_SampleExtensionLoad` Metoda je obsažena v příkladu kódu k zobrazení příkladu obslužné rutiny události, která zpracovává `My.SampleExtensionLoad` událost.</span><span class="sxs-lookup"><span data-stu-id="7bf63-150">The `MyApplication_SampleExtensionLoad` method is included in the code example to show an example of an event handler that handles the `My.SampleExtensionLoad` event.</span></span> <span data-ttu-id="7bf63-151">Všimněte si, `SampleExtensionLoad` že událost bude k dispozici po výběru možnosti **Moje události aplikace** v levém rozevíracím seznamu nad Editor kódu při úpravách souboru *ApplicationEvents. vb* .</span><span class="sxs-lookup"><span data-stu-id="7bf63-151">Note that the `SampleExtensionLoad` event will be available when you select the **My Application Events** option in the left drop-down list above the Code Editor when you are editing the *ApplicationEvents.vb* file.</span></span>

[!code-vb[VbVbcnExtendingMy#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnExtendingMy/VB/Class1.vb#6)]

## <a name="design-guidelines"></a><span data-ttu-id="7bf63-152">Pokyny pro návrh</span><span class="sxs-lookup"><span data-stu-id="7bf63-152">Design guidelines</span></span>

<span data-ttu-id="7bf63-153">Při vývoji rozšíření pro `My` obor názvů použijte následující pokyny, které vám pomohou minimalizovat náklady na údržbu vašich komponent rozšíření:</span><span class="sxs-lookup"><span data-stu-id="7bf63-153">When you develop extensions to the `My` namespace, use the following guidelines to help minimize the maintenance costs of your extension components:</span></span>

- <span data-ttu-id="7bf63-154">**Zahrňte pouze logiku rozšíření.**</span><span class="sxs-lookup"><span data-stu-id="7bf63-154">**Include only the extension logic.**</span></span> <span data-ttu-id="7bf63-155">Logika obsažená v rozšíření `My` oboru názvů by měla obsahovat pouze kód, který je potřeba k vystavení požadované funkce `My` v oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="7bf63-155">The logic included in the `My` namespace extension should include only the code that is needed to expose the required functionality in the `My` namespace.</span></span> <span data-ttu-id="7bf63-156">Vzhledem k tomu, že se rozšíření bude nacházet v uživatelských projektech jako zdrojový kód, aktualizace komponenty rozšíření má za následek vysoké náklady na údržbu a je třeba se jim vyhnout, pokud je to možné.</span><span class="sxs-lookup"><span data-stu-id="7bf63-156">Because your extension will reside in user projects as source code, updating the extension component incurs a high maintenance cost and should be avoided if possible.</span></span>
- <span data-ttu-id="7bf63-157">**Minimalizujte předpoklady projektu.**</span><span class="sxs-lookup"><span data-stu-id="7bf63-157">**Minimize project assumptions.**</span></span> <span data-ttu-id="7bf63-158">Při vytváření rozšíření `My` oboru názvů nepředpokládají sadu odkazů, import na úrovni projektu ani specifická nastavení kompilátoru (například `Option Strict` off).</span><span class="sxs-lookup"><span data-stu-id="7bf63-158">When you create your extensions of the `My` namespace, do not assume a set of references, project-level imports, or specific compiler settings (for example, `Option Strict` off).</span></span> <span data-ttu-id="7bf63-159">Místo toho minimalizujte závislosti a plně zařaďte všechny odkazy na typy `Global` pomocí klíčového slova.</span><span class="sxs-lookup"><span data-stu-id="7bf63-159">Instead, minimize dependencies and fully qualify all type references by using the `Global` keyword.</span></span> <span data-ttu-id="7bf63-160">Také se ujistěte, že se rozšíření zkompiluje se `Option Strict` zapnutou pro minimalizaci chyb v rozšíření.</span><span class="sxs-lookup"><span data-stu-id="7bf63-160">Also, ensure that the extension compiles with `Option Strict` on to minimize errors in the extension.</span></span>
- <span data-ttu-id="7bf63-161">**Izolujte kód rozšíření.**</span><span class="sxs-lookup"><span data-stu-id="7bf63-161">**Isolate the extension code.**</span></span> <span data-ttu-id="7bf63-162">Vložení kódu do jediného souboru způsobí, že se rozšíření dá snadno nasadit jako šablona položky Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="7bf63-162">Placing the code in a single file makes your extension easily deployable as a Visual Studio item template.</span></span> <span data-ttu-id="7bf63-163">Další informace najdete v části "balení a nasazování rozšíření" dále v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="7bf63-163">For more information, see "Packaging and Deploying Extensions" later in this topic.</span></span> <span data-ttu-id="7bf63-164">Umístění veškerého `My` kódu rozšíření oboru názvů do jednoho souboru nebo samostatné složky v projektu pomůže uživatelům najít rozšíření `My` oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="7bf63-164">Placing all the `My` namespace extension code in a single file or a separate folder in a project will also help users locate the `My` namespace extension.</span></span>

## <a name="designing-class-libraries-for-my"></a><span data-ttu-id="7bf63-165">Navrhování knihoven tříd pro`My`</span><span class="sxs-lookup"><span data-stu-id="7bf63-165">Designing class libraries for `My`</span></span>

<span data-ttu-id="7bf63-166">Stejně jako u většiny objektových modelů některé vzory návrhu dobře fungují v `My` oboru názvů a jiné ne.</span><span class="sxs-lookup"><span data-stu-id="7bf63-166">As is the case with most object models, some design patterns work well in the `My` namespace and others do not.</span></span> <span data-ttu-id="7bf63-167">Při navrhování rozšíření `My` oboru názvů Vezměte v úvahu následující principy:</span><span class="sxs-lookup"><span data-stu-id="7bf63-167">When designing an extension to the `My` namespace, consider the following principles:</span></span>

- <span data-ttu-id="7bf63-168">**Bezstavové metody.**</span><span class="sxs-lookup"><span data-stu-id="7bf63-168">**Stateless methods.**</span></span> <span data-ttu-id="7bf63-169">Metody v `My` oboru názvů by měly pro určitý úkol poskytnout kompletní řešení.</span><span class="sxs-lookup"><span data-stu-id="7bf63-169">Methods in the `My` namespace should provide a complete solution to a specific task.</span></span> <span data-ttu-id="7bf63-170">Zajistěte, aby hodnoty parametrů předané metodě poskytovaly veškerý vstup potřebný k dokončení konkrétního úkolu.</span><span class="sxs-lookup"><span data-stu-id="7bf63-170">Ensure that the parameter values that are passed to the method provide all the input required to complete the particular task.</span></span> <span data-ttu-id="7bf63-171">Vyhněte se vytváření metod, které spoléhají na předchozí stav, jako je například otevření připojení k prostředkům.</span><span class="sxs-lookup"><span data-stu-id="7bf63-171">Avoid creating methods that rely on prior state, such as open connections to resources.</span></span>
- <span data-ttu-id="7bf63-172">**Globální instance.**</span><span class="sxs-lookup"><span data-stu-id="7bf63-172">**Global instances.**</span></span> <span data-ttu-id="7bf63-173">Jediný stav, který je udržován v `My` oboru názvů, je globální pro projekt.</span><span class="sxs-lookup"><span data-stu-id="7bf63-173">The only state that is maintained in the `My` namespace is global to the project.</span></span> <span data-ttu-id="7bf63-174">Například `My.Application.Info` zapouzdřuje stav, který je sdílen v celé aplikaci.</span><span class="sxs-lookup"><span data-stu-id="7bf63-174">For example, `My.Application.Info` encapsulates state that is shared throughout the application.</span></span>
- <span data-ttu-id="7bf63-175">**Jednoduché typy parametrů.**</span><span class="sxs-lookup"><span data-stu-id="7bf63-175">**Simple parameter types.**</span></span> <span data-ttu-id="7bf63-176">Zachovejte věci jednoduché, protože se vyhnete složitým typům parametrů.</span><span class="sxs-lookup"><span data-stu-id="7bf63-176">Keep things simple by avoiding complex parameter types.</span></span> <span data-ttu-id="7bf63-177">Místo toho vytvořte metody, které buď nevyužívají vstup parametrů, nebo které přebírají jednoduché vstupní typy, jako jsou například řetězce, primitivní typy a tak dále.</span><span class="sxs-lookup"><span data-stu-id="7bf63-177">Instead, create methods that either take no parameter input or that take simple input types such as strings, primitive types, and so on.</span></span>
- <span data-ttu-id="7bf63-178">**Metody pro vytváření.**</span><span class="sxs-lookup"><span data-stu-id="7bf63-178">**Factory methods.**</span></span> <span data-ttu-id="7bf63-179">Vytváření instancí je nutně obtížné u některých typů.</span><span class="sxs-lookup"><span data-stu-id="7bf63-179">Some types are necessarily difficult to instantiate.</span></span> <span data-ttu-id="7bf63-180">Poskytování produkčních metod jako rozšíření `My` oboru názvů vám umožní snadněji zjistit a spotřebovat typy, které spadají do této kategorie.</span><span class="sxs-lookup"><span data-stu-id="7bf63-180">Providing factory methods as extensions to the `My` namespace enables you to more easily discover and consume types that fall into this category.</span></span> <span data-ttu-id="7bf63-181">Příklad metody továrny, která funguje dobře, je `My.Computer.FileSystem.OpenTextFileReader`.</span><span class="sxs-lookup"><span data-stu-id="7bf63-181">An example of a factory method that works well is `My.Computer.FileSystem.OpenTextFileReader`.</span></span> <span data-ttu-id="7bf63-182">V .NET Framework je k dispozici několik typů datových proudů.</span><span class="sxs-lookup"><span data-stu-id="7bf63-182">There are several stream types available in the .NET Framework.</span></span> <span data-ttu-id="7bf63-183">Zadáním textových souborů konkrétně `OpenTextFileReader` pomůžete uživateli pochopit, který datový proud chcete použít.</span><span class="sxs-lookup"><span data-stu-id="7bf63-183">By specifying text files specifically, the `OpenTextFileReader` helps the user understand which stream to use.</span></span>

<span data-ttu-id="7bf63-184">Tyto pokyny nevylučují obecné principy návrhu pro knihovny tříd.</span><span class="sxs-lookup"><span data-stu-id="7bf63-184">These guidelines do not preclude general design principles for class libraries.</span></span> <span data-ttu-id="7bf63-185">Místo toho jsou doporučení optimalizovaná pro vývojáře, kteří používají Visual Basic a `My` obor názvů.</span><span class="sxs-lookup"><span data-stu-id="7bf63-185">Rather, they are recommendations that are optimized for developers who are using Visual Basic and the `My` namespace.</span></span> <span data-ttu-id="7bf63-186">Obecné principy navrhování pro vytváření knihoven tříd naleznete v tématu [pokyny pro návrh rozhraní](../../../standard/design-guidelines/index.md).</span><span class="sxs-lookup"><span data-stu-id="7bf63-186">For general design principles for creating class libraries, see [Framework Design Guidelines](../../../standard/design-guidelines/index.md).</span></span>

## <a name="packaging-and-deploying-extensions"></a><span data-ttu-id="7bf63-187">Balení a nasazení rozšíření</span><span class="sxs-lookup"><span data-stu-id="7bf63-187">Packaging and deploying extensions</span></span>

<span data-ttu-id="7bf63-188">Rozšíření oboru názvů `My` můžete zahrnout do šablony projektu sady Visual Studio nebo můžete zabalit rozšíření a nasadit je jako šablonu položky sady Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="7bf63-188">You can include `My` namespace extensions in a Visual Studio project template, or you can package your extensions and deploy them as a Visual Studio item template.</span></span> <span data-ttu-id="7bf63-189">Když zabalíte `My` rozšíření oboru názvů jako šablonu položky sady Visual Studio, můžete využít výhod dalších funkcí poskytovaných Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="7bf63-189">When you package your `My` namespace extensions as a Visual Studio item template, you can take advantage of additional capabilities provided by Visual Basic.</span></span> <span data-ttu-id="7bf63-190">Tyto funkce umožňují zahrnout rozšíření, když projekt odkazuje na konkrétní sestavení, nebo povolit uživatelům explicitní přidání rozšíření `My` oboru názvů pomocí stránky **Moje rozšíření** Návrháře projektu Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="7bf63-190">These capabilities enable you to include an extension when a project references a particular assembly, or enable users to explicitly add your `My` namespace extension by using the **My Extensions** page of the Visual Basic Project Designer.</span></span>

<span data-ttu-id="7bf63-191">Podrobnosti o tom, jak nasadit `My` rozšíření oboru názvů, najdete v tématu [balení a nasazení vlastních rozšíření](packaging-and-deploying-custom-my-extensions.md).</span><span class="sxs-lookup"><span data-stu-id="7bf63-191">For details about how to deploy `My` namespace extensions, see [Packaging and Deploying Custom My Extensions](packaging-and-deploying-custom-my-extensions.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="7bf63-192">Viz také</span><span class="sxs-lookup"><span data-stu-id="7bf63-192">See also</span></span>

- [<span data-ttu-id="7bf63-193">Balení a nasazení vlastních rozšíření oboru názvů My</span><span class="sxs-lookup"><span data-stu-id="7bf63-193">Packaging and Deploying Custom My Extensions</span></span>](packaging-and-deploying-custom-my-extensions.md)
- [<span data-ttu-id="7bf63-194">Rozšíření aplikačního modelu jazyka Visual Basic</span><span class="sxs-lookup"><span data-stu-id="7bf63-194">Extending the Visual Basic Application Model</span></span>](extending-the-visual-basic-application-model.md)
- [<span data-ttu-id="7bf63-195">Přizpůsobení výběru objektů dostupných v oboru názvů My</span><span class="sxs-lookup"><span data-stu-id="7bf63-195">Customizing Which Objects are Available in My</span></span>](customizing-which-objects-are-available-in-my.md)
- [<span data-ttu-id="7bf63-196">Stránka Moje rozšíření, návrhář projektu</span><span class="sxs-lookup"><span data-stu-id="7bf63-196">My Extensions Page, Project Designer</span></span>](/visualstudio/ide/reference/my-extensions-page-project-designer-visual-basic)
- [<span data-ttu-id="7bf63-197">Stránka Aplikace, návrhář projektu (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="7bf63-197">Application Page, Project Designer (Visual Basic)</span></span>](/visualstudio/ide/reference/application-page-project-designer-visual-basic)
- [<span data-ttu-id="7bf63-198">Částečné</span><span class="sxs-lookup"><span data-stu-id="7bf63-198">Partial</span></span>](../../language-reference/modifiers/partial.md)
