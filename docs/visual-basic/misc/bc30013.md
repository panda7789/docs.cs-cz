---
title: 'Klíčovému slova #ElseIf, #Else nebo #End If musí předcházet párové klíčové slovo #If.'
ms.date: 07/20/2015
f1_keywords:
- vbc30013
- bc30013
helpviewer_keywords:
- BC30013
ms.assetid: 8fe2d23c-8b8f-46d8-90f2-7f8857ea43bb
ms.openlocfilehash: d9356860aae0355d9141f1c36464b84c982046da
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/04/2020
ms.locfileid: "84366837"
---
# <a name="elseif-else-or-end-if-must-be-preceded-by-a-matching-if"></a><span data-ttu-id="e1e3e-102">Klíčovému slova #ElseIf, #Else nebo #End If musí předcházet párové klíčové slovo #If.</span><span class="sxs-lookup"><span data-stu-id="e1e3e-102">'#ElseIf', '#Else', or '#End If' must be preceded by a matching '#If'</span></span>
<span data-ttu-id="e1e3e-103">`#ElseIf`, `#Else` a `#End If` jsou direktivy podmíněné kompilace.</span><span class="sxs-lookup"><span data-stu-id="e1e3e-103">`#ElseIf`, `#Else`, and `#End If` are conditional compilation directives.</span></span> <span data-ttu-id="e1e3e-104">`#ElseIf`, `#Else` Nebo `#End If` před ní není uvedena odpovídající `#If` direktiva.</span><span class="sxs-lookup"><span data-stu-id="e1e3e-104">The `#ElseIf`, `#Else`, or `#End If` is not preceded by a corresponding `#If` directive.</span></span>  
  
 <span data-ttu-id="e1e3e-105">**ID chyby:** BC30013</span><span class="sxs-lookup"><span data-stu-id="e1e3e-105">**Error ID:** BC30013</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="e1e3e-106">Oprava této chyby</span><span class="sxs-lookup"><span data-stu-id="e1e3e-106">To correct this error</span></span>  
  
1. <span data-ttu-id="e1e3e-107">Ověřte, že zamýšlená `#If` instance není oddělená od klauzule, která je v daném dotazu, v rámci kterého se nachází v bloku podmíněné kompilace nebo nesprávně umístěná `#End If` .</span><span class="sxs-lookup"><span data-stu-id="e1e3e-107">Check that the intended `#If` is not separated from the clause in question by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="e1e3e-108">`#Else`V každém bloku je povolená jenom jedna `#If` , takže `#Else` Tato chyba může způsobovat dvě direktivy po sobě.</span><span class="sxs-lookup"><span data-stu-id="e1e3e-108">Only one `#Else` is permitted in each `#If` block, so two successive `#Else` directives cause this error.</span></span>  
  
2. <span data-ttu-id="e1e3e-109">Ověřte, že v `#` předchozí direktivě chybí úvodní znak `#If` .</span><span class="sxs-lookup"><span data-stu-id="e1e3e-109">Check that the leading `#` is not missing from an earlier `#If` directive.</span></span>  
  
3. <span data-ttu-id="e1e3e-110">Pokud je vše jiného v daném pořadí, přidejte `#If` do začátku bloku podmíněné kompilace direktivu.</span><span class="sxs-lookup"><span data-stu-id="e1e3e-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="e1e3e-111">Viz také</span><span class="sxs-lookup"><span data-stu-id="e1e3e-111">See also</span></span>

- [<span data-ttu-id="e1e3e-112">#If... Then... #Else – direktivy</span><span class="sxs-lookup"><span data-stu-id="e1e3e-112">#If...Then...#Else Directives</span></span>](../language-reference/directives/if-then-else-directives.md)
