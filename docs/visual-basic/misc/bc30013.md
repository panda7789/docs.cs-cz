---
title: "'#ElseIf', '#Else' nebo '#End If' musí předcházet odpovídající '#If'"
ms.date: 07/20/2015
f1_keywords:
- vbc30013
- bc30013
helpviewer_keywords:
- BC30013
ms.assetid: 8fe2d23c-8b8f-46d8-90f2-7f8857ea43bb
ms.openlocfilehash: 92a481febe4d2fb4045e7fbb50eee81c9656b189
ms.sourcegitcommit: 5c1abeec15fbddcc7dbaa729fabc1f1f29f12045
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/15/2019
ms.locfileid: "58031676"
---
# <a name="elseif-else-or-end-if-must-be-preceded-by-a-matching-if"></a><span data-ttu-id="9a8dd-102">'#ElseIf', '#Else' nebo '#End If' musí předcházet odpovídající '#If'</span><span class="sxs-lookup"><span data-stu-id="9a8dd-102">'#ElseIf', '#Else', or '#End If' must be preceded by a matching '#If'</span></span>
<span data-ttu-id="9a8dd-103">`#ElseIf`, `#Else`, a `#End If` jsou direktivy podmíněné kompilace.</span><span class="sxs-lookup"><span data-stu-id="9a8dd-103">`#ElseIf`, `#Else`, and `#End If` are conditional compilation directives.</span></span> <span data-ttu-id="9a8dd-104">`#ElseIf`, `#Else`, Nebo `#End If` není za znakem odpovídající `#If` směrnice.</span><span class="sxs-lookup"><span data-stu-id="9a8dd-104">The `#ElseIf`, `#Else`, or `#End If` is not preceded by a corresponding `#If` directive.</span></span>  
  
 <span data-ttu-id="9a8dd-105">**ID chyby:** BC30013</span><span class="sxs-lookup"><span data-stu-id="9a8dd-105">**Error ID:** BC30013</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="9a8dd-106">Oprava této chyby</span><span class="sxs-lookup"><span data-stu-id="9a8dd-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="9a8dd-107">Zkontrolujte, že určené `#If` není v klauzuli from oddělené použité bloku podmíněné kompilace nebo nesprávně umístěné `#End If`.</span><span class="sxs-lookup"><span data-stu-id="9a8dd-107">Check that the intended `#If` is not separated from the clause in question by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="9a8dd-108">Pouze jeden `#Else` je povolený v každém `#If` blokovat, tedy dvě po sobě jdoucích `#Else` direktivy příčinou této chyby.</span><span class="sxs-lookup"><span data-stu-id="9a8dd-108">Only one `#Else` is permitted in each `#If` block, so two successive `#Else` directives cause this error.</span></span>  
  
2.  <span data-ttu-id="9a8dd-109">Zkontrolujte, že úvodního `#` nechybí z předchozí `#If` směrnice.</span><span class="sxs-lookup"><span data-stu-id="9a8dd-109">Check that the leading `#` is not missing from an earlier `#If` directive.</span></span>  
  
3.  <span data-ttu-id="9a8dd-110">Pokud všechno ostatní je v pořadí, přidejte `#If` na začátku bloku podmíněné kompilace.</span><span class="sxs-lookup"><span data-stu-id="9a8dd-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="9a8dd-111">Viz také:</span><span class="sxs-lookup"><span data-stu-id="9a8dd-111">See also</span></span>

- [<span data-ttu-id="9a8dd-112">Direktivy #If...Then...#Else</span><span class="sxs-lookup"><span data-stu-id="9a8dd-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
