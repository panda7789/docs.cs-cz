---
title: "'#ElseIf' nemůže následovat po '#Else' jako součást blok '#If'"
ms.date: 07/20/2015
f1_keywords:
- bc32030
- vbc32030
helpviewer_keywords:
- BC32030
ms.assetid: 248d6464-3019-4753-8a33-7070bbe5d2a6
ms.openlocfilehash: efeee964d9dcc617773f433553d4409ea459e960
ms.sourcegitcommit: 0888d7b24f475c346a3f444de8d83ec1ca7cd234
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 12/22/2018
ms.locfileid: "53762054"
---
# <a name="elseif-cannot-follow-else-as-part-of-an-if-block"></a><span data-ttu-id="c1b81-102">'#ElseIf' nemůže následovat po '#Else' jako součást blok '#If'</span><span class="sxs-lookup"><span data-stu-id="c1b81-102">'#ElseIf' cannot follow '#Else' as part of an '#If' block</span></span>
<span data-ttu-id="c1b81-103">`#ElseIf` Následující direktivy podmíněné kompilace `#Else` směrnice.</span><span class="sxs-lookup"><span data-stu-id="c1b81-103">An `#ElseIf` conditional compilation directive follows an `#Else` directive.</span></span> <span data-ttu-id="c1b81-104">`#Else` musí být poslední direktivou v podmíněného blok před `#End If` směrnice.</span><span class="sxs-lookup"><span data-stu-id="c1b81-104">`#Else` must be the last directive in the conditional block before the `#End If` directive.</span></span>  
  
 <span data-ttu-id="c1b81-105">**ID chyby:** BC32030</span><span class="sxs-lookup"><span data-stu-id="c1b81-105">**Error ID:** BC32030</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="c1b81-106">Oprava této chyby</span><span class="sxs-lookup"><span data-stu-id="c1b81-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="c1b81-107">Zaškrtněte, pokud předchozí `#Else` by měla být `#ElseIf`.</span><span class="sxs-lookup"><span data-stu-id="c1b81-107">Check if the preceding `#Else` should be an `#ElseIf`.</span></span>  
  
2.  <span data-ttu-id="c1b81-108">Zkontrolujte, že předchozí `#If` bloku je řádně ukončen a nový `#If` bloku je zahájeno.</span><span class="sxs-lookup"><span data-stu-id="c1b81-108">Check that a preceding `#If` block is properly terminated and that a new `#If` block is initiated.</span></span>  
  
3.  <span data-ttu-id="c1b81-109">Pokud všechno ostatní je správná, přesunout `#ElseIf` směrnice a jeho odpovídajícího příkazu blokovat předcházet `#Else` bloku.</span><span class="sxs-lookup"><span data-stu-id="c1b81-109">If everything else is correct, move this `#ElseIf` directive and its corresponding statement block to precede the `#Else` block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="c1b81-110">Viz také</span><span class="sxs-lookup"><span data-stu-id="c1b81-110">See Also</span></span>  
 [<span data-ttu-id="c1b81-111">Direktivy #If...Then...#Else</span><span class="sxs-lookup"><span data-stu-id="c1b81-111">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
