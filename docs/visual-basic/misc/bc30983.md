---
title: Typ '<variablename>' je nejednoznačný, protože omezení cyklu a klauzuli kroku nejde rozšířit na stejný typ.
ms.date: 07/20/2015
f1_keywords:
- vbc30983
- bc30983
helpviewer_keywords:
- BC30983
ms.assetid: 6b97153c-dee3-4429-b92a-2e5a212c864b
ms.openlocfilehash: 2aa98be339c2aea9aa436c40c2d012748958ef6b
ms.sourcegitcommit: 2701302a99cafbe0d86d53d540eb0fa7e9b46b36
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/28/2019
ms.locfileid: "64621801"
---
# <a name="type-of-variablename-is-ambiguous-because-the-loop-bounds-and-the-step-variable-do-not-widen-to-the-same-type"></a><span data-ttu-id="c07fc-102">Typ '\<NázevProměnné >' je nejednoznačný, protože omezení cyklu a klauzuli kroku nejde rozšířit na stejný typ.</span><span class="sxs-lookup"><span data-stu-id="c07fc-102">Type of '\<variablename>' is ambiguous because the loop bounds and the step variable do not widen to the same type</span></span>
<span data-ttu-id="c07fc-103">Váš kód obsahuje `For...Next` smyčku, ve kterém kompilátor nemůže odvodit typ dat pro řídicí proměnná smyčky for vzhledem k tomu, že platí následující podmínky:</span><span class="sxs-lookup"><span data-stu-id="c07fc-103">Your code contains a `For...Next` loop in which the compiler cannot infer a data type for the loop control variable because the following conditions is true:</span></span>  
  
- <span data-ttu-id="c07fc-104">Není zadaný datový typ řídicí proměnná smyčky s `As` klauzuli.</span><span class="sxs-lookup"><span data-stu-id="c07fc-104">The data type of the loop control variable is not specified with an `As` clause.</span></span>  
  
- <span data-ttu-id="c07fc-105">Omezení cyklu a klauzuli kroku obsahovat aspoň dva datové typy.</span><span class="sxs-lookup"><span data-stu-id="c07fc-105">The loop bounds and step variable contain at least two data types.</span></span>  
  
- <span data-ttu-id="c07fc-106">Existuje více než jeden možný převod mezi datovými typy.</span><span class="sxs-lookup"><span data-stu-id="c07fc-106">More than one possible conversion exists between the data types.</span></span>  
  
- <span data-ttu-id="c07fc-107">Neexistuje žádný nejlepší typ mezi kandidáty, tak, aby typ řídicí proměnná smyčky for je nejednoznačný.</span><span class="sxs-lookup"><span data-stu-id="c07fc-107">There is no best type among the candidates, so that the choice of type for the loop control variable is ambiguous.</span></span>  
  
 <span data-ttu-id="c07fc-108">Například následující smyčka má jednu mez smyčky typu `Integer` a v jednom průchodu vázaný typ `UInteger`:</span><span class="sxs-lookup"><span data-stu-id="c07fc-108">For example, the following loop has one loop bound of type `Integer` and one loop bound of type `UInteger`:</span></span>  
  
```vb  
Dim m As Integer = 1  
Dim n As UInteger = 10  
' Not valid.  
' For i = m To n  
    ' Loop processing.  
' Next  
```  
  
 <span data-ttu-id="c07fc-109">Není možný převod mezi `Integer` a `UInteger`a je to možné převod mezi `UInteger` a `Integer`, ale oba jsou zužujících převodů tak ani je nejlepší volbou.</span><span class="sxs-lookup"><span data-stu-id="c07fc-109">There is a possible conversion between `Integer` and `UInteger`, and a possible conversion between `UInteger` and `Integer`, but both are narrowing conversions so neither is the best choice.</span></span>  
  
 <span data-ttu-id="c07fc-110">V následujícím příkladu, třetí proměnné typu `Double` je přidána.</span><span class="sxs-lookup"><span data-stu-id="c07fc-110">In the next example, a third variable of type `Double` is added.</span></span> <span data-ttu-id="c07fc-111">Obě `Integer` a `UInteger` mají standardní rozšiřující převody na `Double`, takže se převod na `Double` nejlepší volbou.</span><span class="sxs-lookup"><span data-stu-id="c07fc-111">Both `Integer` and `UInteger` have standard widening conversions to `Double`, which makes conversion to `Double` the best choice.</span></span> <span data-ttu-id="c07fc-112">Odvození typu přiřadí řídicí proměnná smyčky for `i` datový typ `Double`.</span><span class="sxs-lookup"><span data-stu-id="c07fc-112">Type inference assigns to loop control variable `i` the data type `Double`.</span></span>  
  
```vb  
Dim stepVar As Double = 1  
' Valid.  
For i = m To n Step stepVar  
    ' Loop processing.  
Next  
```  
  
 <span data-ttu-id="c07fc-113">**ID chyby:** BC30983</span><span class="sxs-lookup"><span data-stu-id="c07fc-113">**Error ID:** BC30983</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="c07fc-114">Oprava této chyby</span><span class="sxs-lookup"><span data-stu-id="c07fc-114">To correct this error</span></span>  
  
- <span data-ttu-id="c07fc-115">Explicitně jedna z proměnných převeďte na typ, že ostatní umožňovat rozšiřitelný převod, například:</span><span class="sxs-lookup"><span data-stu-id="c07fc-115">Explicitly convert one of the variables to a type that the others have a widening conversion to, for example:</span></span>  
  
    ```  
    For i = m To CLng(n)  
    ```  
  
- <span data-ttu-id="c07fc-116">Použití `As` klauzule k určení typu proměnné ovládacího prvku:</span><span class="sxs-lookup"><span data-stu-id="c07fc-116">Use an `As` clause to specify the type of the control variable:</span></span>  
  
    ```  
    For i As Double = m To n   
    ```  
  
## <a name="see-also"></a><span data-ttu-id="c07fc-117">Viz také:</span><span class="sxs-lookup"><span data-stu-id="c07fc-117">See also</span></span>

- [<span data-ttu-id="c07fc-118">Příkaz For...Next</span><span class="sxs-lookup"><span data-stu-id="c07fc-118">For...Next Statement</span></span>](../../visual-basic/language-reference/statements/for-next-statement.md)
- [<span data-ttu-id="c07fc-119">Implicitní a explicitní převody</span><span class="sxs-lookup"><span data-stu-id="c07fc-119">Implicit and Explicit Conversions</span></span>](../../visual-basic/programming-guide/language-features/data-types/implicit-and-explicit-conversions.md)
- [<span data-ttu-id="c07fc-120">Odvození místního typu</span><span class="sxs-lookup"><span data-stu-id="c07fc-120">Local Type Inference</span></span>](../../visual-basic/programming-guide/language-features/variables/local-type-inference.md)
- [<span data-ttu-id="c07fc-121">Příkaz Option Infer</span><span class="sxs-lookup"><span data-stu-id="c07fc-121">Option Infer Statement</span></span>](../../visual-basic/language-reference/statements/option-infer-statement.md)
- [<span data-ttu-id="c07fc-122">Rozšíření a zúžení převodů</span><span class="sxs-lookup"><span data-stu-id="c07fc-122">Widening and Narrowing Conversions</span></span>](../../visual-basic/programming-guide/language-features/data-types/widening-and-narrowing-conversions.md)
