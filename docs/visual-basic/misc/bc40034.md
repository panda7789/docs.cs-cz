---
title: Non-kompatibilní se Specifikací CLS 'MustOverride' člen není povolený v kompatibilní se Specifikací CLS <classname>
ms.date: 07/20/2015
f1_keywords:
- bc40034
- vbc40034
helpviewer_keywords:
- BC40034
ms.assetid: 4eb36b3a-1bbe-4e99-9ecb-a12b8729b128
ms.openlocfilehash: a9c26d5c959066499f940483634867d41ca970bd
ms.sourcegitcommit: 2701302a99cafbe0d86d53d540eb0fa7e9b46b36
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/28/2019
ms.locfileid: "64624365"
---
# <a name="non-cls-compliant-mustoverride-member-is-not-allowed-in-a-cls-compliant-classname"></a><span data-ttu-id="aae43-102">Non-kompatibilní se Specifikací CLS 'MustOverride' člen není povolený v kompatibilní se Specifikací CLS \<classname ></span><span class="sxs-lookup"><span data-stu-id="aae43-102">Non-CLS-compliant 'MustOverride' member is not allowed in a CLS-compliant \<classname></span></span>
<span data-ttu-id="aae43-103">Třída je označena jako `<CLSCompliant(True)>`, ale obsahuje `MustOverride` vlastnost nebo procedura, která je označena jako `<CLSCompliant(False)>` nebo není označen.</span><span class="sxs-lookup"><span data-stu-id="aae43-103">A class is marked as `<CLSCompliant(True)>`, but it contains a `MustOverride` property or procedure that is marked as `<CLSCompliant(False)>` or is not marked.</span></span>  
  
 <span data-ttu-id="aae43-104">Pokud je kompatibilní s třídou [jazyková nezávislost a jazykově nezávislé komponenty](../../standard/language-independence-and-language-independent-components.md) (CLS), pouze členy, které jsou také označené jako přistupuje k aplikaci, která používá tuto třídu `<CLSCompliant(True)>` a ignoruje členy, které nejsou.</span><span class="sxs-lookup"><span data-stu-id="aae43-104">When a class is compliant with the [Language Independence and Language-Independent Components](../../standard/language-independence-and-language-independent-components.md) (CLS), an application that uses that class accesses only the members that are also marked as `<CLSCompliant(True)>` and ignores the members that are not.</span></span> <span data-ttu-id="aae43-105">Však nelze ignorovat aplikace `MustOverride` vlastnost nebo procedura, vzhledem k tomu, že potřebuje přístup k této vlastnost nebo procedura přepsat.</span><span class="sxs-lookup"><span data-stu-id="aae43-105">However, the application cannot ignore a `MustOverride` property or procedure, because it must access that property or procedure to override it.</span></span>  
  
 <span data-ttu-id="aae43-106">Pokud použijete <xref:System.CLSCompliantAttribute> na programovací prvek, nastavíte atributu `isCompliant` buď parametr `True` nebo `False` k označení dodržování předpisů nebo při nedodržení předpisů.</span><span class="sxs-lookup"><span data-stu-id="aae43-106">When you apply the <xref:System.CLSCompliantAttribute> to a programming element, you set the attribute's `isCompliant` parameter to either `True` or `False` to indicate compliance or noncompliance.</span></span> <span data-ttu-id="aae43-107">Neexistuje žádný výchozí hodnotou tohoto parametru, a je nutné zadat hodnotu.</span><span class="sxs-lookup"><span data-stu-id="aae43-107">There is no default for this parameter, and you must supply a value.</span></span>  
  
 <span data-ttu-id="aae43-108">Pokud se nevztahují <xref:System.CLSCompliantAttribute> na element, se považuje za jako nevyhovující.</span><span class="sxs-lookup"><span data-stu-id="aae43-108">If you do not apply the <xref:System.CLSCompliantAttribute> to an element, it is considered to be noncompliant.</span></span>  
  
 <span data-ttu-id="aae43-109">Ve výchozím nastavení tato zpráva je upozornění.</span><span class="sxs-lookup"><span data-stu-id="aae43-109">By default, this message is a warning.</span></span> <span data-ttu-id="aae43-110">Informace o zobrazení nebo skrytí upozornění zpracování upozornění jako chyby, najdete v části [Konfigurace upozornění v jazyce Visual Basic](/visualstudio/ide/configuring-warnings-in-visual-basic).</span><span class="sxs-lookup"><span data-stu-id="aae43-110">For information on hiding warnings or treating warnings as errors, see [Configuring Warnings in Visual Basic](/visualstudio/ide/configuring-warnings-in-visual-basic).</span></span>  
  
 <span data-ttu-id="aae43-111">**ID chyby:** BC40034</span><span class="sxs-lookup"><span data-stu-id="aae43-111">**Error ID:** BC40034</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="aae43-112">Oprava této chyby</span><span class="sxs-lookup"><span data-stu-id="aae43-112">To correct this error</span></span>  
  
- <span data-ttu-id="aae43-113">Pokud se vyžadovat dodržování specifikace CLS a máte kontrolu nad zdrojový kód třídy, označte člena jako `<CLSCompliant(True)>`.</span><span class="sxs-lookup"><span data-stu-id="aae43-113">If you require CLS compliance and have control over the class source code, mark the member as `<CLSCompliant(True)>`.</span></span>  
  
- <span data-ttu-id="aae43-114">Pokud vyžadovat dodržování specifikace CLS a nemáte kontrolu nad zdrojový kód třídy, nebo pokud nesplňuje, aby vyhovovala, definujte tohoto člena v rámci jiné třídy.</span><span class="sxs-lookup"><span data-stu-id="aae43-114">If you require CLS compliance and do not have control over the class source code, or if it does not qualify to be compliant, define this member within a different class.</span></span>  
  
- <span data-ttu-id="aae43-115">Pokud budete vyžadovat, že tento člen zůstanou nekompatibilní, odeberte `MustOverride` odebrat – klíčové slovo z jeho definice <xref:System.CLSCompliantAttribute> z definice třídy nebo označit třídu jako `<CLSCompliant(False)>`.</span><span class="sxs-lookup"><span data-stu-id="aae43-115">If you require that this member remain noncompliant, remove the `MustOverride` keyword from its definition, remove the <xref:System.CLSCompliantAttribute> from the class definition, or mark the class as `<CLSCompliant(False)>`.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="aae43-116">Viz také:</span><span class="sxs-lookup"><span data-stu-id="aae43-116">See also</span></span>

- [<span data-ttu-id="aae43-117">MustOverride</span><span class="sxs-lookup"><span data-stu-id="aae43-117">MustOverride</span></span>](../../visual-basic/language-reference/modifiers/mustoverride.md)
