---
title: Direktiva Option Strict On vyžaduje každý parametr výrazu lambda deklarován s klauzulí 'Jako' Pokud jeho typ nelze odvodit.
ms.date: 07/20/2015
f1_keywords:
- bc36642
- vbc36642
helpviewer_keywords:
- BC36642
ms.assetid: 2aaa62b8-49c9-4ae8-b0f5-08e3f0b5ad10
ms.openlocfilehash: 2f4bc0763b145d9c8ac25fe0077f603d35644fd3
ms.sourcegitcommit: 6b308cf6d627d78ee36dbbae8972a310ac7fd6c8
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 01/23/2019
ms.locfileid: "54549428"
---
# <a name="option-strict-on-requires-each-lambda-expression-parameter-to-be-declared-with-an-as-clause-if-its-type-cannot-be-inferred"></a><span data-ttu-id="3ff9b-102">Direktiva Option Strict On vyžaduje každý parametr výrazu lambda deklarován s klauzulí 'Jako' Pokud jeho typ nelze odvodit.</span><span class="sxs-lookup"><span data-stu-id="3ff9b-102">Option Strict On requires each lambda expression parameter to be declared with an 'As' clause if its type cannot be inferred</span></span>
<span data-ttu-id="3ff9b-103">Je parametr ve výrazu lambda deklarován bez použití `As` klauzule s `Option Strict` na.</span><span class="sxs-lookup"><span data-stu-id="3ff9b-103">You have declared a parameter in a lambda expression without using an `As` clause, with `Option Strict` on.</span></span>  
  
```  
' Not valid when Option Strict is on.  
' Dim increment1 = Function (n) n + 1  
```  
  
 <span data-ttu-id="3ff9b-104">Předchozí deklarace je platný Pokud typ `n` jde odvodit.</span><span class="sxs-lookup"><span data-stu-id="3ff9b-104">The previous declaration is valid if the type of `n` can be inferred.</span></span> <span data-ttu-id="3ff9b-105">Pokud předchozí výraz lambda jsou přiřazení k delegáta funkce, například `Del`:</span><span class="sxs-lookup"><span data-stu-id="3ff9b-105">For example, if you are assigning the previous lambda expression to a function delegate, `Del`:</span></span>  
  
```  
Delegate Function Del(ByVal p As Integer) As Integer  
```  
  
 <span data-ttu-id="3ff9b-106">Nyní typu `n` lze odvodit z parametrů `p`:</span><span class="sxs-lookup"><span data-stu-id="3ff9b-106">Now the type of `n` can be inferred from parameter `p`:</span></span>  
  
```  
Dim increment2 as Del = Function(n) n + 1  
```  
  
 <span data-ttu-id="3ff9b-107">**ID chyby:** BC36642</span><span class="sxs-lookup"><span data-stu-id="3ff9b-107">**Error ID:** BC36642</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="3ff9b-108">Oprava této chyby</span><span class="sxs-lookup"><span data-stu-id="3ff9b-108">To correct this error</span></span>  
  
-   <span data-ttu-id="3ff9b-109">Přidat `As` klauzuli deklarace parametru:</span><span class="sxs-lookup"><span data-stu-id="3ff9b-109">Add an `As` clause to the parameter declaration:</span></span>  
  
    ```  
    Dim increment3 = Function (n As Integer) n + 1  
    ```  
  
## <a name="see-also"></a><span data-ttu-id="3ff9b-110">Viz také:</span><span class="sxs-lookup"><span data-stu-id="3ff9b-110">See also</span></span>
- [<span data-ttu-id="3ff9b-111">Výrazy lambda</span><span class="sxs-lookup"><span data-stu-id="3ff9b-111">Lambda Expressions</span></span>](../../visual-basic/programming-guide/language-features/procedures/lambda-expressions.md)
