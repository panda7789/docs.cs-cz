---
title: For Each...Next – příkaz
ms.date: 07/20/2015
f1_keywords:
- vb.ForEach
- vb.ForEachNext
- vb.Each
- ForEachNext
helpviewer_keywords:
- infinite loops
- Next statement [Visual Basic], For Each...Next
- endless loops
- loop structures [Visual Basic], For Each...Next
- loops, endless
- Each keyword [Visual Basic]
- instructions, repeating
- For Each statement [Visual Basic]
- collections, instruction repetition
- loops, infinite
- For Each...Next statements
- For keyword [Visual Basic], For Each...Next statements
- Exit statement [Visual Basic], For Each...Next statements
- iteration
ms.assetid: ebce3120-95c3-42b1-b70b-fa7da40c75e2
ms.openlocfilehash: 0feb938121a97b06509b472652e6a753841ab2b8
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/04/2020
ms.locfileid: "84404651"
---
# <a name="for-eachnext-statement-visual-basic"></a><span data-ttu-id="041b6-102">For Each...Next – příkaz (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="041b6-102">For Each...Next Statement (Visual Basic)</span></span>

<span data-ttu-id="041b6-103">Opakuje skupinu příkazů pro každý prvek v kolekci.</span><span class="sxs-lookup"><span data-stu-id="041b6-103">Repeats a group of statements for each element in a collection.</span></span>

## <a name="syntax"></a><span data-ttu-id="041b6-104">Syntaxe</span><span class="sxs-lookup"><span data-stu-id="041b6-104">Syntax</span></span>

```vb
For Each element [ As datatype ] In group
    [ statements ]
    [ Continue For ]
    [ statements ]
    [ Exit For ]
    [ statements ]
Next [ element ]
```

## <a name="parts"></a><span data-ttu-id="041b6-105">Součásti</span><span class="sxs-lookup"><span data-stu-id="041b6-105">Parts</span></span>

|<span data-ttu-id="041b6-106">Pojem</span><span class="sxs-lookup"><span data-stu-id="041b6-106">Term</span></span>|<span data-ttu-id="041b6-107">Definice</span><span class="sxs-lookup"><span data-stu-id="041b6-107">Definition</span></span>|
|---|---|
|`element`|<span data-ttu-id="041b6-108">Vyžadováno v `For Each` příkazu.</span><span class="sxs-lookup"><span data-stu-id="041b6-108">Required in the `For Each` statement.</span></span> <span data-ttu-id="041b6-109">Volitelné v `Next` příkazu.</span><span class="sxs-lookup"><span data-stu-id="041b6-109">Optional in the `Next` statement.</span></span> <span data-ttu-id="041b6-110">Variabilní.</span><span class="sxs-lookup"><span data-stu-id="041b6-110">Variable.</span></span> <span data-ttu-id="041b6-111">Slouží k iterování prvků kolekce.</span><span class="sxs-lookup"><span data-stu-id="041b6-111">Used to iterate through the elements of the collection.</span></span>|
|`datatype`|<span data-ttu-id="041b6-112">Volitelné [`Option Infer`](option-infer-statement.md) , pokud je zapnuto (výchozí) nebo `element` je již deklarováno; požadováno, pokud `Option Infer` je vypnuto a `element` ještě není deklarováno.</span><span class="sxs-lookup"><span data-stu-id="041b6-112">Optional if [`Option Infer`](option-infer-statement.md) is on (the default) or `element` is already declared; required if `Option Infer` is off and `element` isn't already declared.</span></span> <span data-ttu-id="041b6-113">Datový typ `element` .</span><span class="sxs-lookup"><span data-stu-id="041b6-113">The data type of `element`.</span></span>|
|`group`|<span data-ttu-id="041b6-114">Povinná hodnota.</span><span class="sxs-lookup"><span data-stu-id="041b6-114">Required.</span></span> <span data-ttu-id="041b6-115">Proměnná s typem, který je typem kolekce nebo objektem.</span><span class="sxs-lookup"><span data-stu-id="041b6-115">A variable with a type that's a collection type or Object.</span></span> <span data-ttu-id="041b6-116">Odkazuje na kolekci, na které se `statements` mají opakovat.</span><span class="sxs-lookup"><span data-stu-id="041b6-116">Refers to the collection over which the `statements` are to be repeated.</span></span>|
|`statements`|<span data-ttu-id="041b6-117">Nepovinný parametr.</span><span class="sxs-lookup"><span data-stu-id="041b6-117">Optional.</span></span> <span data-ttu-id="041b6-118">Jeden nebo více příkazů mezi `For Each` a `Next` , které jsou spuštěny na jednotlivých položkách v `group` .</span><span class="sxs-lookup"><span data-stu-id="041b6-118">One or more statements between `For Each` and `Next` that run on each item in `group`.</span></span>|
|`Continue For`|<span data-ttu-id="041b6-119">Nepovinný parametr.</span><span class="sxs-lookup"><span data-stu-id="041b6-119">Optional.</span></span> <span data-ttu-id="041b6-120">Přenese řízení na začátek `For Each` smyčky.</span><span class="sxs-lookup"><span data-stu-id="041b6-120">Transfers control to the start of the `For Each` loop.</span></span>|
|`Exit For`|<span data-ttu-id="041b6-121">Nepovinný parametr.</span><span class="sxs-lookup"><span data-stu-id="041b6-121">Optional.</span></span> <span data-ttu-id="041b6-122">Přenese řízení ze `For Each` smyčky.</span><span class="sxs-lookup"><span data-stu-id="041b6-122">Transfers control out of the `For Each` loop.</span></span>|
|`Next`|<span data-ttu-id="041b6-123">Povinná hodnota.</span><span class="sxs-lookup"><span data-stu-id="041b6-123">Required.</span></span> <span data-ttu-id="041b6-124">Ukončí definici `For Each` smyčky.</span><span class="sxs-lookup"><span data-stu-id="041b6-124">Terminates the definition of the `For Each` loop.</span></span>|

## <a name="simple-example"></a><span data-ttu-id="041b6-125">Jednoduchý příklad</span><span class="sxs-lookup"><span data-stu-id="041b6-125">Simple Example</span></span>

<span data-ttu-id="041b6-126">Použijte `For Each` smyčku..., `Next` Pokud chcete opakovat sadu příkazů pro každý prvek kolekce nebo pole.</span><span class="sxs-lookup"><span data-stu-id="041b6-126">Use a `For Each`...`Next` loop when you want to repeat a set of statements for each element of a collection or array.</span></span>

> [!TIP]
> <span data-ttu-id="041b6-127">A [pro... Další příkaz](for-next-statement.md) funguje dobře, když můžete přidružit každou iteraci smyčky s proměnnou ovládacího prvku a určit počáteční a konečné hodnoty této proměnné.</span><span class="sxs-lookup"><span data-stu-id="041b6-127">A [For...Next Statement](for-next-statement.md) works well when you can associate each iteration of a loop with a control variable and determine that variable's initial and final values.</span></span> <span data-ttu-id="041b6-128">Pokud však pracujete s kolekcí, koncept počátečních a konečných hodnot není smysluplný a Vy nevíte, kolik prvků kolekce má.</span><span class="sxs-lookup"><span data-stu-id="041b6-128">However, when you are dealing with a collection, the concept of initial and final values isn't meaningful, and you don't necessarily know how many elements the collection has.</span></span> <span data-ttu-id="041b6-129">V tomto typu případu `For Each` je smyčka... `Next` často lepší volbou.</span><span class="sxs-lookup"><span data-stu-id="041b6-129">In this kind of case, a `For Each`...`Next` loop is often a better choice.</span></span>

<span data-ttu-id="041b6-130">V následujícím příkladu `For Each` ...`Next`</span><span class="sxs-lookup"><span data-stu-id="041b6-130">In the following example, the `For Each`…`Next`</span></span> <span data-ttu-id="041b6-131">příkaz prochází všechny prvky kolekce seznamů.</span><span class="sxs-lookup"><span data-stu-id="041b6-131">statement iterates through all the elements of a List collection.</span></span>

[!code-vb[VbVbalrStatements#121](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/class9.vb#121)]

<span data-ttu-id="041b6-132">Další příklady najdete v tématu [kolekce](../../../standard/collections/index.md) a [pole](../../programming-guide/language-features/arrays/index.md).</span><span class="sxs-lookup"><span data-stu-id="041b6-132">For more examples, see [Collections](../../../standard/collections/index.md) and [Arrays](../../programming-guide/language-features/arrays/index.md).</span></span>

## <a name="nested-loops"></a><span data-ttu-id="041b6-133">Vnořené smyčky</span><span class="sxs-lookup"><span data-stu-id="041b6-133">Nested Loops</span></span>

<span data-ttu-id="041b6-134">Smyčky můžete vnořovat vložením `For Each` jedné smyčky do jiné.</span><span class="sxs-lookup"><span data-stu-id="041b6-134">You can nest `For Each` loops by putting one loop within another.</span></span>

<span data-ttu-id="041b6-135">Následující příklad ukazuje vnořený `For Each` ...`Next`</span><span class="sxs-lookup"><span data-stu-id="041b6-135">The following example demonstrates nested `For Each`…`Next`</span></span> <span data-ttu-id="041b6-136">struktury.</span><span class="sxs-lookup"><span data-stu-id="041b6-136">structures.</span></span>

[!code-vb[VbVbalrStatements#122](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/class9.vb#122)]

<span data-ttu-id="041b6-137">Při vnořování smyček musí mít každá smyčka jedinečnou `element` proměnnou.</span><span class="sxs-lookup"><span data-stu-id="041b6-137">When you nest loops, each loop must have a unique `element` variable.</span></span>

<span data-ttu-id="041b6-138">Můžete také vnořit různé druhy řídicích struktur mezi sebou.</span><span class="sxs-lookup"><span data-stu-id="041b6-138">You can also nest different kinds of control structures within each other.</span></span> <span data-ttu-id="041b6-139">Další informace najdete v tématu [vnořené řídicí struktury](../../programming-guide/language-features/control-flow/nested-control-structures.md).</span><span class="sxs-lookup"><span data-stu-id="041b6-139">For more information, see [Nested Control Structures](../../programming-guide/language-features/control-flow/nested-control-structures.md).</span></span>

## <a name="exit-for-and-continue-for"></a><span data-ttu-id="041b6-140">Ukončit po a pokračovat pro</span><span class="sxs-lookup"><span data-stu-id="041b6-140">Exit For and Continue For</span></span>

<span data-ttu-id="041b6-141">Příkaz [Exit for](exit-statement.md) způsobí, že provádění skončí `For` ...`Next`</span><span class="sxs-lookup"><span data-stu-id="041b6-141">The [Exit For](exit-statement.md) statement causes execution to exit the `For`…`Next`</span></span> <span data-ttu-id="041b6-142">Loop a přenáší řízení příkazu, který následuje po `Next` příkazu.</span><span class="sxs-lookup"><span data-stu-id="041b6-142">loop and transfers control to the statement that follows the `Next` statement.</span></span>

<span data-ttu-id="041b6-143">`Continue For`Příkaz přenáší řízení okamžitě na další iteraci smyčky.</span><span class="sxs-lookup"><span data-stu-id="041b6-143">The `Continue For` statement transfers control immediately to the next iteration of the loop.</span></span> <span data-ttu-id="041b6-144">Další informace najdete v tématu [příkaz Continue](continue-statement.md).</span><span class="sxs-lookup"><span data-stu-id="041b6-144">For more information, see [Continue Statement](continue-statement.md).</span></span>

<span data-ttu-id="041b6-145">Následující příklad ukazuje, jak použít `Continue For` `Exit For` příkazy a.</span><span class="sxs-lookup"><span data-stu-id="041b6-145">The following example shows how to use the `Continue For` and `Exit For` statements.</span></span>

[!code-vb[VbVbalrStatements#123](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/class9.vb#123)]

<span data-ttu-id="041b6-146">Do smyčky můžete vložit libovolný počet `Exit For` příkazů `For Each` .</span><span class="sxs-lookup"><span data-stu-id="041b6-146">You can put any number of `Exit For` statements in a `For Each` loop.</span></span> <span data-ttu-id="041b6-147">Při použití v rámci vnořených `For Each` smyček `Exit For` způsobí, že provádění ukončí vnitřní smyčku a přenáší řízení na další vyšší úroveň vnoření.</span><span class="sxs-lookup"><span data-stu-id="041b6-147">When used within nested `For Each` loops, `Exit For` causes execution to exit the innermost loop and transfers control to the next higher level of nesting.</span></span>

<span data-ttu-id="041b6-148">`Exit For`se často používá po vyhodnocení nějaké podmínky, například v `If` ... `Then` ...`Else` strukturované.</span><span class="sxs-lookup"><span data-stu-id="041b6-148">`Exit For` is often used after an evaluation of some condition, for example, in an `If`...`Then`...`Else` structure.</span></span> <span data-ttu-id="041b6-149">Je možné, že budete chtít použít `Exit For` následující podmínky:</span><span class="sxs-lookup"><span data-stu-id="041b6-149">You might want to use `Exit For` for the following conditions:</span></span>

- <span data-ttu-id="041b6-150">Pokračování iterace je zbytečné nebo nemožné.</span><span class="sxs-lookup"><span data-stu-id="041b6-150">Continuing to iterate is unnecessary or impossible.</span></span> <span data-ttu-id="041b6-151">To může být způsobeno chybnou hodnotou nebo žádostí o ukončení.</span><span class="sxs-lookup"><span data-stu-id="041b6-151">This might be caused by an erroneous value or a termination request.</span></span>

- <span data-ttu-id="041b6-152">Výjimka je zachycena v `Try` ... `Catch` ...`Finally`. Můžete použít `Exit For` na konci `Finally` bloku.</span><span class="sxs-lookup"><span data-stu-id="041b6-152">An exception is caught in a `Try`...`Catch`...`Finally`. You might use `Exit For` at the end of the `Finally` block.</span></span>

- <span data-ttu-id="041b6-153">Existuje nekonečné smyčka, což je smyčka, která by mohla běžet velkým nebo dokonce nekonečným počtem výskytů.</span><span class="sxs-lookup"><span data-stu-id="041b6-153">There an endless loop, which is a loop that could run a large or even infinite number of times.</span></span> <span data-ttu-id="041b6-154">Pokud tuto podmínku zjistíte, můžete ji použít `Exit For` k řídicímu panelu smyčky.</span><span class="sxs-lookup"><span data-stu-id="041b6-154">If you detect such a condition, you can use `Exit For` to escape the loop.</span></span> <span data-ttu-id="041b6-155">Další informace najdete v části [do... Příkaz LOOP](do-loop-statement.md)</span><span class="sxs-lookup"><span data-stu-id="041b6-155">For more information, see [Do...Loop Statement](do-loop-statement.md).</span></span>

## <a name="iterators"></a><span data-ttu-id="041b6-156">Iterátory</span><span class="sxs-lookup"><span data-stu-id="041b6-156">Iterators</span></span>

<span data-ttu-id="041b6-157">Pomocí *iterátoru* můžete provést vlastní iteraci v kolekci.</span><span class="sxs-lookup"><span data-stu-id="041b6-157">You use an *iterator* to perform a custom iteration over a collection.</span></span> <span data-ttu-id="041b6-158">Iterátor může být funkce nebo `Get` přistupující objekt.</span><span class="sxs-lookup"><span data-stu-id="041b6-158">An iterator can be a function or a `Get` accessor.</span></span> <span data-ttu-id="041b6-159">Pomocí `Yield` příkazu vrátí každý prvek kolekce po jednom.</span><span class="sxs-lookup"><span data-stu-id="041b6-159">It uses a `Yield` statement to return each element of the collection one at a time.</span></span>

<span data-ttu-id="041b6-160">Zavoláte iterátor pomocí `For Each...Next` příkazu.</span><span class="sxs-lookup"><span data-stu-id="041b6-160">You call an iterator by using a `For Each...Next` statement.</span></span> <span data-ttu-id="041b6-161">Každá iterace `For Each` smyčky volá iterátor.</span><span class="sxs-lookup"><span data-stu-id="041b6-161">Each iteration of the `For Each` loop calls the iterator.</span></span> <span data-ttu-id="041b6-162">Při `Yield` dosažení příkazu v iterátoru `Yield` je vrácen výraz v příkazu a aktuální umístění v kódu je uchováno.</span><span class="sxs-lookup"><span data-stu-id="041b6-162">When a `Yield` statement is reached in the iterator, the expression in the `Yield` statement is returned, and the current location in code is retained.</span></span> <span data-ttu-id="041b6-163">Spuštění je restartováno z tohoto umístění při příštím volání iterátoru.</span><span class="sxs-lookup"><span data-stu-id="041b6-163">Execution is restarted from that location the next time that the iterator is called.</span></span>

<span data-ttu-id="041b6-164">V následujícím příkladu je použita funkce iterátoru.</span><span class="sxs-lookup"><span data-stu-id="041b6-164">The following example uses an iterator function.</span></span> <span data-ttu-id="041b6-165">Funkce iterátoru obsahuje `Yield` příkaz, který je uvnitř [... Další](for-next-statement.md) smyčka</span><span class="sxs-lookup"><span data-stu-id="041b6-165">The iterator function has a `Yield` statement that's inside a [For…Next](for-next-statement.md) loop.</span></span> <span data-ttu-id="041b6-166">V `ListEvenNumbers` metodě Každá iterace `For Each` těla příkazu vytvoří volání funkce iterátoru, která pokračuje k dalšímu `Yield` příkazu.</span><span class="sxs-lookup"><span data-stu-id="041b6-166">In the `ListEvenNumbers` method, each iteration of the `For Each` statement body creates a call to the iterator function, which proceeds to the next `Yield` statement.</span></span>

[!code-vb[VbVbalrStatements#127](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/class9.vb#127)]

<span data-ttu-id="041b6-167">Další informace naleznete v tématu [iterátory](../../programming-guide/concepts/iterators.md), [příkazy yield](yield-statement.md)a [iterátor](../modifiers/iterator.md).</span><span class="sxs-lookup"><span data-stu-id="041b6-167">For more information, see [Iterators](../../programming-guide/concepts/iterators.md), [Yield Statement](yield-statement.md), and [Iterator](../modifiers/iterator.md).</span></span>

## <a name="technical-implementation"></a><span data-ttu-id="041b6-168">Technická implementace</span><span class="sxs-lookup"><span data-stu-id="041b6-168">Technical Implementation</span></span>

<span data-ttu-id="041b6-169">Když `For Each` ...`Next`</span><span class="sxs-lookup"><span data-stu-id="041b6-169">When a `For Each`…`Next`</span></span> <span data-ttu-id="041b6-170">příkaz se spustí, Visual Basic vyhodnocuje kolekci pouze jednou, a to před spuštěním smyčky.</span><span class="sxs-lookup"><span data-stu-id="041b6-170">statement runs, Visual Basic evaluates the collection only one time, before the loop starts.</span></span> <span data-ttu-id="041b6-171">Pokud váš příkaz zablokuje změny `element` nebo `group` tyto změny neovlivňují iteraci smyčky.</span><span class="sxs-lookup"><span data-stu-id="041b6-171">If your statement block changes `element` or `group`, these changes don't affect the iteration of the loop.</span></span>

<span data-ttu-id="041b6-172">Po úspěšném přiřazení všech prvků v kolekci se `element` `For Each` smyčka zastaví a řízení projde příkazem, který následuje za `Next` příkazem.</span><span class="sxs-lookup"><span data-stu-id="041b6-172">When all the elements in the collection have been successively assigned to `element`, the `For Each` loop stops and control passes to the statement following the `Next` statement.</span></span>

<span data-ttu-id="041b6-173">Pokud je nastavená [možnost odvodit](option-infer-statement.md) (výchozí nastavení), kompilátor Visual Basic může odvodit datový typ `element` .</span><span class="sxs-lookup"><span data-stu-id="041b6-173">If [Option Infer](option-infer-statement.md) is on (its default setting), the Visual Basic compiler can infer the data type of `element`.</span></span> <span data-ttu-id="041b6-174">Pokud je vypnutá a `element` nebyla deklarována vně smyčky, je nutné ji deklarovat v `For Each` příkazu.</span><span class="sxs-lookup"><span data-stu-id="041b6-174">If it is off and `element` hasn't been declared outside the loop, you must declare it in the `For Each` statement.</span></span> <span data-ttu-id="041b6-175">Chcete-li explicitně deklarovat datový typ `element` , použijte `As` klauzuli.</span><span class="sxs-lookup"><span data-stu-id="041b6-175">To declare the data type of `element` explicitly, use an `As` clause.</span></span> <span data-ttu-id="041b6-176">Není-li datový typ elementu definován mimo `For Each` konstruktor... `Next` konstrukce, je jeho rozsah hlavním prvkem smyčky.</span><span class="sxs-lookup"><span data-stu-id="041b6-176">Unless the data type of element is defined outside the `For Each`...`Next` construct, its scope is the body of the loop.</span></span> <span data-ttu-id="041b6-177">Všimněte si, že nemůžete deklarovat `element` vnější i uvnitř smyčky.</span><span class="sxs-lookup"><span data-stu-id="041b6-177">Note that you cannot declare `element` both outside and inside the loop.</span></span>

<span data-ttu-id="041b6-178">Volitelně můžete zadat `element` v `Next` příkazu.</span><span class="sxs-lookup"><span data-stu-id="041b6-178">You can optionally specify `element` in the `Next` statement.</span></span> <span data-ttu-id="041b6-179">To zlepšuje čitelnost vašeho programu, zejména v případě, že máte vnořené `For Each` smyčky.</span><span class="sxs-lookup"><span data-stu-id="041b6-179">This improves the readability of your program, especially if you have nested `For Each` loops.</span></span> <span data-ttu-id="041b6-180">Je nutné zadat stejnou proměnnou jako ta, která se zobrazí v příslušném `For Each` příkazu.</span><span class="sxs-lookup"><span data-stu-id="041b6-180">You must specify the same variable as the one that appears in the corresponding `For Each` statement.</span></span>

<span data-ttu-id="041b6-181">Je možné, že se nebudete muset vyhnout změnám hodnoty `element` uvnitř smyčky.</span><span class="sxs-lookup"><span data-stu-id="041b6-181">You might want to avoid changing the value of `element` inside a loop.</span></span> <span data-ttu-id="041b6-182">To může ztížit čtení a ladění kódu.</span><span class="sxs-lookup"><span data-stu-id="041b6-182">Doing this can make it more difficult to read and debug your code.</span></span> <span data-ttu-id="041b6-183">Změna hodnoty `group` nemá vliv na kolekci nebo její prvky, které byly určeny při prvním zadání smyčky.</span><span class="sxs-lookup"><span data-stu-id="041b6-183">Changing the value of `group` doesn't affect the collection or its elements, which were determined when the loop was first entered.</span></span>

<span data-ttu-id="041b6-184">Při vnořování smyček se při výskytu `Next` příkazu na úrovni vnějšího vnoření vyskytne v případě `Next` , že došlo k chybě, kompilátor signalizuje chybu.</span><span class="sxs-lookup"><span data-stu-id="041b6-184">When you're nesting loops, if a `Next` statement of an outer nesting level is encountered before the `Next` of an inner level, the compiler signals an error.</span></span> <span data-ttu-id="041b6-185">Kompilátor však může tuto chybu překrývají pouze v případě, že zadáte `element` příkaz v každém `Next` příkazu.</span><span class="sxs-lookup"><span data-stu-id="041b6-185">However, the compiler can detect this overlapping error only if you specify `element` in every `Next` statement.</span></span>

<span data-ttu-id="041b6-186">Pokud váš kód závisí na průchodu v kolekci v určitém pořadí, `For Each` smyčka... `Next` není nejlepší volbou, pokud neznáte charakteristiky objektu Enumerator, které kolekce zpřístupňuje.</span><span class="sxs-lookup"><span data-stu-id="041b6-186">If your code depends on traversing a collection in a particular order, a `For Each`...`Next` loop isn't the best choice, unless you know the characteristics of the enumerator object the collection exposes.</span></span> <span data-ttu-id="041b6-187">Pořadí průchodu není určeno Visual Basic, ale <xref:System.Collections.IEnumerator.MoveNext%2A> metodou objektu Enumerator.</span><span class="sxs-lookup"><span data-stu-id="041b6-187">The order of traversal isn't determined by Visual Basic, but by the <xref:System.Collections.IEnumerator.MoveNext%2A> method of the enumerator object.</span></span> <span data-ttu-id="041b6-188">Proto možná nebudete moci předpovědět, který prvek kolekce je první, který má být vrácen v `element` nebo který je další, který bude vrácen po daném prvku.</span><span class="sxs-lookup"><span data-stu-id="041b6-188">Therefore, you might not be able to predict which element of the collection is the first to be returned in `element`, or which is the next to be returned after a given element.</span></span> <span data-ttu-id="041b6-189">Můžete dosáhnout spolehlivější výsledků pomocí jiné struktury smyčky, například `For` ... `Next` nebo `Do` ... `Loop` .</span><span class="sxs-lookup"><span data-stu-id="041b6-189">You might achieve more reliable results using a different loop structure, such as `For`...`Next` or `Do`...`Loop`.</span></span>

<span data-ttu-id="041b6-190">Modul runtime musí být schopný převést elementy v `group` na `element` .</span><span class="sxs-lookup"><span data-stu-id="041b6-190">The runtime must be able to convert the elements in `group` to `element`.</span></span> <span data-ttu-id="041b6-191">Příkaz [ `Option Strict` ] řídí, zda jsou povoleny rozšiřující i zúžené převody ( `Option Strict` je vypnutý, její výchozí hodnota) nebo zda jsou povoleny pouze rozšiřující převody `Option Strict` .</span><span class="sxs-lookup"><span data-stu-id="041b6-191">The [`Option Strict`] statement controls whether both widening and narrowing conversions are allowed (`Option Strict` is off, its default value), or whether only widening conversions are allowed (`Option Strict` is on).</span></span> <span data-ttu-id="041b6-192">Další informace najdete v tématu [zúžení převodů](#narrowing-conversions).</span><span class="sxs-lookup"><span data-stu-id="041b6-192">For more information, see [Narrowing conversions](#narrowing-conversions).</span></span>

<span data-ttu-id="041b6-193">Datový typ `group` musí být odkazový typ, který odkazuje na kolekci nebo pole, které je vyčíslitelné.</span><span class="sxs-lookup"><span data-stu-id="041b6-193">The data type of `group` must be a reference type that refers to a collection or an array that's enumerable.</span></span> <span data-ttu-id="041b6-194">Nejčastěji to znamená, že `group` odkazuje na objekt, který implementuje <xref:System.Collections.IEnumerable> rozhraní `System.Collections` oboru názvů nebo <xref:System.Collections.Generic.IEnumerable%601> rozhraní `System.Collections.Generic` oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="041b6-194">Most commonly this means that `group` refers to an object that implements the <xref:System.Collections.IEnumerable> interface of the `System.Collections` namespace or the <xref:System.Collections.Generic.IEnumerable%601> interface of the `System.Collections.Generic` namespace.</span></span> <span data-ttu-id="041b6-195">`System.Collections.IEnumerable`definuje <xref:System.Collections.IEnumerable.GetEnumerator%2A> metodu, která vrací objekt enumerátoru pro kolekci.</span><span class="sxs-lookup"><span data-stu-id="041b6-195">`System.Collections.IEnumerable` defines the <xref:System.Collections.IEnumerable.GetEnumerator%2A> method, which returns an enumerator object for the collection.</span></span> <span data-ttu-id="041b6-196">Objekt enumerátoru implementuje `System.Collections.IEnumerator` rozhraní `System.Collections` oboru názvů a zpřístupňuje <xref:System.Collections.IEnumerator.Current%2A> vlastnost a <xref:System.Collections.IEnumerator.Reset%2A> <xref:System.Collections.IEnumerator.MoveNext%2A> metody a.</span><span class="sxs-lookup"><span data-stu-id="041b6-196">The enumerator object implements the `System.Collections.IEnumerator` interface of the `System.Collections` namespace and exposes the <xref:System.Collections.IEnumerator.Current%2A> property and the <xref:System.Collections.IEnumerator.Reset%2A> and <xref:System.Collections.IEnumerator.MoveNext%2A> methods.</span></span> <span data-ttu-id="041b6-197">Visual Basic je používá k procházení kolekce.</span><span class="sxs-lookup"><span data-stu-id="041b6-197">Visual Basic uses these to traverse the collection.</span></span>

### <a name="narrowing-conversions"></a><span data-ttu-id="041b6-198">Zužující převody</span><span class="sxs-lookup"><span data-stu-id="041b6-198">Narrowing Conversions</span></span>

<span data-ttu-id="041b6-199">Pokud `Option Strict` je nastaven na `On` , zužující převody obvykle způsobují chyby kompilátoru.</span><span class="sxs-lookup"><span data-stu-id="041b6-199">When `Option Strict` is set to `On`, narrowing conversions ordinarily cause compiler errors.</span></span> <span data-ttu-id="041b6-200">V `For Each` příkazu však převod z prvků v `group` na `element` je vyhodnocován a proveden v době běhu a chyby kompilátoru způsobené zužujícími převody jsou potlačeny.</span><span class="sxs-lookup"><span data-stu-id="041b6-200">In a `For Each` statement, however, conversions from the elements in `group` to `element` are evaluated and performed at run time, and compiler errors caused by narrowing conversions are suppressed.</span></span>

<span data-ttu-id="041b6-201">V následujícím příkladu přiřazení `m` jako počáteční hodnotu pro není `n` zkompilováno, pokud `Option Strict` je zapnuto, protože převod typu `Long` na `Integer` je zužující převod.</span><span class="sxs-lookup"><span data-stu-id="041b6-201">In the following example, the assignment of `m` as the initial value for `n` doesn't compile when `Option Strict` is on because the conversion of a `Long` to an `Integer` is a narrowing conversion.</span></span> <span data-ttu-id="041b6-202">V `For Each` příkazu však není hlášena žádná chyba kompilátoru, i když přiřazení `number` vyžaduje stejný převod z `Long` na `Integer` .</span><span class="sxs-lookup"><span data-stu-id="041b6-202">In the `For Each` statement, however, no compiler error is reported, even though the assignment to `number` requires the same conversion from `Long` to `Integer`.</span></span> <span data-ttu-id="041b6-203">V `For Each` příkazu, který obsahuje velké číslo, dojde k chybě za běhu při <xref:Microsoft.VisualBasic.CompilerServices.Conversions.ToInteger%2A> použití na velké číslo.</span><span class="sxs-lookup"><span data-stu-id="041b6-203">In the `For Each` statement that contains a large number, a run-time error occurs when <xref:Microsoft.VisualBasic.CompilerServices.Conversions.ToInteger%2A> is applied to the large number.</span></span>

[!code-vb[VbVbalrStatements#89](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class3.vb#89)]

### <a name="ienumerator-calls"></a><span data-ttu-id="041b6-204">Volání IEnumerator</span><span class="sxs-lookup"><span data-stu-id="041b6-204">IEnumerator Calls</span></span>

<span data-ttu-id="041b6-205">Při spuštění `For Each` smyčky... `Next` Visual Basic ověří, zda `group` odkazuje na platný objekt kolekce.</span><span class="sxs-lookup"><span data-stu-id="041b6-205">When execution of a `For Each`...`Next` loop starts, Visual Basic verifies that `group` refers to a valid collection object.</span></span> <span data-ttu-id="041b6-206">V takovém případě vyvolá výjimku.</span><span class="sxs-lookup"><span data-stu-id="041b6-206">If not, it throws an exception.</span></span> <span data-ttu-id="041b6-207">V opačném případě volá <xref:System.Collections.IEnumerator.MoveNext%2A> metodu a <xref:System.Collections.IEnumerator.Current%2A> vlastnost objektu Enumerator pro návrat prvního prvku.</span><span class="sxs-lookup"><span data-stu-id="041b6-207">Otherwise, it calls the <xref:System.Collections.IEnumerator.MoveNext%2A> method and the <xref:System.Collections.IEnumerator.Current%2A> property of the enumerator object to return the first element.</span></span> <span data-ttu-id="041b6-208">Pokud `MoveNext` znamená, že neexistuje žádný další prvek, tedy pokud je kolekce prázdná, `For Each` smyčka se zastaví a ovládací prvek projde příkazem, který následuje za `Next` příkazem.</span><span class="sxs-lookup"><span data-stu-id="041b6-208">If `MoveNext` indicates that there is no next element, that is, if the collection is empty, the `For Each` loop stops and control passes to the statement following the `Next` statement.</span></span> <span data-ttu-id="041b6-209">V opačném případě Visual Basic nastaví `element` na první prvek a spustí blok příkazu.</span><span class="sxs-lookup"><span data-stu-id="041b6-209">Otherwise, Visual Basic sets `element` to the first element and runs the statement block.</span></span>

<span data-ttu-id="041b6-210">Pokaždé, když Visual Basic nalezne `Next` příkaz, vrátí se do `For Each` příkazu.</span><span class="sxs-lookup"><span data-stu-id="041b6-210">Each time Visual Basic encounters the `Next` statement, it returns to the `For Each` statement.</span></span> <span data-ttu-id="041b6-211">Znovu volá `MoveNext` a `Current` vrátí další prvek a znovu buď spustí blok, nebo zastaví smyčku v závislosti na výsledku.</span><span class="sxs-lookup"><span data-stu-id="041b6-211">Again it calls `MoveNext` and `Current` to return the next element, and again it either runs the block or stops the loop depending on the result.</span></span> <span data-ttu-id="041b6-212">Tento proces pokračuje `MoveNext` , dokud neindikuje, že není k dispozici žádný další prvek nebo `Exit For` příkaz.</span><span class="sxs-lookup"><span data-stu-id="041b6-212">This process continues until `MoveNext` indicates that there is no next element or an `Exit For` statement is encountered.</span></span>

<span data-ttu-id="041b6-213">**Úprava kolekce.**</span><span class="sxs-lookup"><span data-stu-id="041b6-213">**Modifying the Collection.**</span></span> <span data-ttu-id="041b6-214">Objekt enumerátoru vrácený <xref:System.Collections.IEnumerable.GetEnumerator%2A> normálně neumožňuje změnit kolekci přidáním, odstraněním, nahrazením nebo změnou pořadí všech prvků.</span><span class="sxs-lookup"><span data-stu-id="041b6-214">The enumerator object returned by <xref:System.Collections.IEnumerable.GetEnumerator%2A> normally doesn't let you change the collection by adding, deleting, replacing, or reordering any elements.</span></span> <span data-ttu-id="041b6-215">Změníte-li kolekci poté, co jste spustili `For Each` smyčku... `Next` , objekt enumerátoru se změní na neplatný a další pokus o přístup k elementu způsobí <xref:System.InvalidOperationException> výjimku.</span><span class="sxs-lookup"><span data-stu-id="041b6-215">If you change the collection after you have initiated a `For Each`...`Next` loop, the enumerator object becomes invalid, and the next attempt to access an element causes an <xref:System.InvalidOperationException> exception.</span></span>

<span data-ttu-id="041b6-216">Toto blokování úprav však není určeno Visual Basic, ale spíše implementací <xref:System.Collections.IEnumerable> rozhraní.</span><span class="sxs-lookup"><span data-stu-id="041b6-216">However, this blocking of modification isn't determined by Visual Basic, but rather by the implementation of the <xref:System.Collections.IEnumerable> interface.</span></span> <span data-ttu-id="041b6-217">Je možné implementovat `IEnumerable` způsobem, který umožňuje úpravu během iterace.</span><span class="sxs-lookup"><span data-stu-id="041b6-217">It is possible to implement `IEnumerable` in a way that allows for modification during iteration.</span></span> <span data-ttu-id="041b6-218">Pokud zvažujete, že provádíte tuto dynamickou úpravu, ujistěte se, že rozumíte charakteristikám `IEnumerable` implementace v kolekci, kterou používáte.</span><span class="sxs-lookup"><span data-stu-id="041b6-218">If you are considering doing such dynamic modification, make sure that you understand the characteristics of the `IEnumerable` implementation on the collection you are using.</span></span>

<span data-ttu-id="041b6-219">**Úpravy prvků kolekce.**</span><span class="sxs-lookup"><span data-stu-id="041b6-219">**Modifying Collection Elements.**</span></span> <span data-ttu-id="041b6-220"><xref:System.Collections.IEnumerator.Current%2A>Vlastnost objektu Enumerator je určena [jen pro čtení](../modifiers/readonly.md)a vrátí místní kopii každého prvku kolekce.</span><span class="sxs-lookup"><span data-stu-id="041b6-220">The <xref:System.Collections.IEnumerator.Current%2A> property of the enumerator object is [ReadOnly](../modifiers/readonly.md), and it returns a local copy of each collection element.</span></span> <span data-ttu-id="041b6-221">To znamená, že nemůžete změnit samotné prvky ve `For Each` smyčce.... `Next`</span><span class="sxs-lookup"><span data-stu-id="041b6-221">This means that you cannot modify the elements themselves in a `For Each`...`Next` loop.</span></span> <span data-ttu-id="041b6-222">Všechny změny, které provedete, ovlivní jenom místní kopii z `Current` a nereflektují se zpátky do příslušné kolekce.</span><span class="sxs-lookup"><span data-stu-id="041b6-222">Any modification you make affects only the local copy from `Current` and isn't reflected back into the underlying collection.</span></span> <span data-ttu-id="041b6-223">Nicméně pokud je prvek odkazový typ, můžete upravit členy instance, na kterou odkazuje.</span><span class="sxs-lookup"><span data-stu-id="041b6-223">However, if an element is a reference type, you can modify the members of the instance to which it points.</span></span> <span data-ttu-id="041b6-224">Následující příklad upravuje `BackColor` člena každého `thisControl` prvku.</span><span class="sxs-lookup"><span data-stu-id="041b6-224">The following example modifies the `BackColor` member of each `thisControl` element.</span></span> <span data-ttu-id="041b6-225">Nemůžete však změnit `thisControl` sám sebe.</span><span class="sxs-lookup"><span data-stu-id="041b6-225">You cannot, however, modify `thisControl` itself.</span></span>

```vb
Sub LightBlueBackground(thisForm As System.Windows.Forms.Form)
    For Each thisControl In thisForm.Controls
        thisControl.BackColor = System.Drawing.Color.LightBlue
    Next thisControl
End Sub
```

<span data-ttu-id="041b6-226">Předchozí příklad může změnit `BackColor` člena každého `thisControl` prvku, i když nemůže `thisControl` sám změnit.</span><span class="sxs-lookup"><span data-stu-id="041b6-226">The previous example can modify the `BackColor` member of each `thisControl` element, although it cannot modify `thisControl` itself.</span></span>

<span data-ttu-id="041b6-227">**Procházení polí.**</span><span class="sxs-lookup"><span data-stu-id="041b6-227">**Traversing Arrays.**</span></span> <span data-ttu-id="041b6-228">Vzhledem k tomu <xref:System.Array> , že třída implementuje <xref:System.Collections.IEnumerable> rozhraní, všechna pole zpřístupňují <xref:System.Array.GetEnumerator%2A> metodu.</span><span class="sxs-lookup"><span data-stu-id="041b6-228">Because the <xref:System.Array> class implements the <xref:System.Collections.IEnumerable> interface, all arrays expose the <xref:System.Array.GetEnumerator%2A> method.</span></span> <span data-ttu-id="041b6-229">To znamená, že můžete iterovat přes pole pomocí `For Each` smyčky.... `Next`</span><span class="sxs-lookup"><span data-stu-id="041b6-229">This means that you can iterate through an array with a `For Each`...`Next` loop.</span></span> <span data-ttu-id="041b6-230">Lze však číst pouze prvky pole.</span><span class="sxs-lookup"><span data-stu-id="041b6-230">However, you can only read the array elements.</span></span> <span data-ttu-id="041b6-231">Nemůžete je změnit.</span><span class="sxs-lookup"><span data-stu-id="041b6-231">You cannot change them.</span></span>

## <a name="example"></a><span data-ttu-id="041b6-232">Příklad</span><span class="sxs-lookup"><span data-stu-id="041b6-232">Example</span></span>

<span data-ttu-id="041b6-233">Následující příklad zobrazí seznam všech složek v C:\. adresáře pomocí <xref:System.IO.DirectoryInfo> třídy.</span><span class="sxs-lookup"><span data-stu-id="041b6-233">The following example lists all the folders in the C:\ directory by using the <xref:System.IO.DirectoryInfo> class.</span></span>

[!code-vb[VbVbalrStatements#124](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/class9.vb#124)]

## <a name="example"></a><span data-ttu-id="041b6-234">Příklad</span><span class="sxs-lookup"><span data-stu-id="041b6-234">Example</span></span>

<span data-ttu-id="041b6-235">Následující příklad znázorňuje postup pro řazení kolekce.</span><span class="sxs-lookup"><span data-stu-id="041b6-235">The following example illustrates a procedure for sorting a collection.</span></span> <span data-ttu-id="041b6-236">Příklad řadí instance `Car` třídy, které jsou uloženy v <xref:System.Collections.Generic.List%601> .</span><span class="sxs-lookup"><span data-stu-id="041b6-236">The example sorts instances of a `Car` class that are stored in a <xref:System.Collections.Generic.List%601>.</span></span> <span data-ttu-id="041b6-237">`Car`Třída implementuje <xref:System.IComparable%601> rozhraní, které vyžaduje <xref:System.IComparable%601.CompareTo%2A> implementaci metody.</span><span class="sxs-lookup"><span data-stu-id="041b6-237">The `Car` class implements the <xref:System.IComparable%601> interface, which requires that the <xref:System.IComparable%601.CompareTo%2A> method be implemented.</span></span>

<span data-ttu-id="041b6-238">Každé volání <xref:System.IComparable%601.CompareTo%2A> metody provede jedno porovnání, které se používá k řazení.</span><span class="sxs-lookup"><span data-stu-id="041b6-238">Each call to the <xref:System.IComparable%601.CompareTo%2A> method makes a single comparison that's used for sorting.</span></span> <span data-ttu-id="041b6-239">Uživatelsky psaný kód v `CompareTo` metodě vrátí hodnotu pro každé porovnání aktuálního objektu s jiným objektem.</span><span class="sxs-lookup"><span data-stu-id="041b6-239">User-written code in the `CompareTo` method returns a value for each comparison of the current object with another object.</span></span> <span data-ttu-id="041b6-240">Vrácená hodnota je menší než nula, pokud je aktuální objekt menší než druhý objekt, větší než nula, pokud je aktuální objekt větší než druhý objekt a nula, pokud jsou stejné.</span><span class="sxs-lookup"><span data-stu-id="041b6-240">The value returned is less than zero if the current object is less than the other object, greater than zero if the current object is greater than the other object, and zero if they are equal.</span></span> <span data-ttu-id="041b6-241">To umožňuje definovat v kódu kritéria pro větší než, menší než a rovno.</span><span class="sxs-lookup"><span data-stu-id="041b6-241">This enables you to define in code the criteria for greater than, less than, and equal.</span></span>

<span data-ttu-id="041b6-242">V `ListCars` metodě `cars.Sort()` příkaz seřadí seznam.</span><span class="sxs-lookup"><span data-stu-id="041b6-242">In the `ListCars` method, the `cars.Sort()` statement sorts the list.</span></span> <span data-ttu-id="041b6-243">Toto volání <xref:System.Collections.Generic.List%601.Sort%2A> metody metody <xref:System.Collections.Generic.List%601> způsobí, že `CompareTo` Metoda bude automaticky volána pro `Car` objekty v `List` .</span><span class="sxs-lookup"><span data-stu-id="041b6-243">This call to the <xref:System.Collections.Generic.List%601.Sort%2A> method of the <xref:System.Collections.Generic.List%601> causes the `CompareTo` method to be called automatically for the `Car` objects in the `List`.</span></span>

[!code-vb[VbVbalrStatements#125](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/class9.vb#125)]

## <a name="see-also"></a><span data-ttu-id="041b6-244">Viz také</span><span class="sxs-lookup"><span data-stu-id="041b6-244">See also</span></span>

- [<span data-ttu-id="041b6-245">Kolekce</span><span class="sxs-lookup"><span data-stu-id="041b6-245">Collections</span></span>](../../../standard/collections/index.md)
- [<span data-ttu-id="041b6-246">For...Next – příkaz</span><span class="sxs-lookup"><span data-stu-id="041b6-246">For...Next Statement</span></span>](for-next-statement.md)
- [<span data-ttu-id="041b6-247">Struktury smyčky</span><span class="sxs-lookup"><span data-stu-id="041b6-247">Loop Structures</span></span>](../../programming-guide/language-features/control-flow/loop-structures.md)
- [<span data-ttu-id="041b6-248">While...End While – příkaz</span><span class="sxs-lookup"><span data-stu-id="041b6-248">While...End While Statement</span></span>](while-end-while-statement.md)
- [<span data-ttu-id="041b6-249">Do...Loop – příkaz</span><span class="sxs-lookup"><span data-stu-id="041b6-249">Do...Loop Statement</span></span>](do-loop-statement.md)
- [<span data-ttu-id="041b6-250">Rozšíření a zúžení převodů</span><span class="sxs-lookup"><span data-stu-id="041b6-250">Widening and Narrowing Conversions</span></span>](../../programming-guide/language-features/data-types/widening-and-narrowing-conversions.md)
- [<span data-ttu-id="041b6-251">Inicializátory objektů: pojmenované a anonymní typy</span><span class="sxs-lookup"><span data-stu-id="041b6-251">Object Initializers: Named and Anonymous Types</span></span>](../../programming-guide/language-features/objects-and-classes/object-initializers-named-and-anonymous-types.md)
- [<span data-ttu-id="041b6-252">Inicializátory kolekcí</span><span class="sxs-lookup"><span data-stu-id="041b6-252">Collection Initializers</span></span>](../../programming-guide/language-features/collection-initializers/index.md)
- [<span data-ttu-id="041b6-253">Pole</span><span class="sxs-lookup"><span data-stu-id="041b6-253">Arrays</span></span>](../../programming-guide/language-features/arrays/index.md)
