---
title: klauzule FROM – C# odkaz
ms.date: 07/20/2015
f1_keywords:
- from_CSharpKeyword
- from
helpviewer_keywords:
- from clause [C#]
- from keyword [C#]
ms.assetid: 1aefd18c-1314-47f8-99ec-9bcefb09e699
ms.openlocfilehash: 388b9c0245b112d619fc173f6019b3f7dbf59940
ms.sourcegitcommit: 5f236cd78cf09593c8945a7d753e0850e96a0b80
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 01/07/2020
ms.locfileid: "75715293"
---
# <a name="from-clause-c-reference"></a><span data-ttu-id="22ebf-102">from clause (Referenční dokumentace jazyka C#)</span><span class="sxs-lookup"><span data-stu-id="22ebf-102">from clause (C# Reference)</span></span>

<span data-ttu-id="22ebf-103">Výraz dotazu musí začínat klauzulí `from`.</span><span class="sxs-lookup"><span data-stu-id="22ebf-103">A query expression must begin with a `from` clause.</span></span> <span data-ttu-id="22ebf-104">Kromě toho výraz dotazu může obsahovat dílčí dotazy, které také začínají klauzulí `from`.</span><span class="sxs-lookup"><span data-stu-id="22ebf-104">Additionally, a query expression can contain sub-queries, which also begin with a `from` clause.</span></span> <span data-ttu-id="22ebf-105">Klauzule `from` určuje následující:</span><span class="sxs-lookup"><span data-stu-id="22ebf-105">The `from` clause specifies the following:</span></span>

- <span data-ttu-id="22ebf-106">Zdroj dat, na kterém se spustí dotaz nebo dílčí dotaz</span><span class="sxs-lookup"><span data-stu-id="22ebf-106">The data source on which the query or sub-query will be run.</span></span>

- <span data-ttu-id="22ebf-107">Místní *Proměnná rozsahu* , která představuje každý prvek ve zdrojové sekvenci.</span><span class="sxs-lookup"><span data-stu-id="22ebf-107">A local *range variable* that represents each element in the source sequence.</span></span>

<span data-ttu-id="22ebf-108">Jak proměnná rozsahu, tak zdroj dat jsou silného typu.</span><span class="sxs-lookup"><span data-stu-id="22ebf-108">Both the range variable and the data source are strongly typed.</span></span> <span data-ttu-id="22ebf-109">Zdroj dat, na který odkazuje klauzule `from`, musí mít typ <xref:System.Collections.IEnumerable>, <xref:System.Collections.Generic.IEnumerable%601>nebo odvozený typ, jako je například <xref:System.Linq.IQueryable%601>.</span><span class="sxs-lookup"><span data-stu-id="22ebf-109">The data source referenced in the `from` clause must have a type of <xref:System.Collections.IEnumerable>, <xref:System.Collections.Generic.IEnumerable%601>, or a derived type such as <xref:System.Linq.IQueryable%601>.</span></span>

<span data-ttu-id="22ebf-110">V následujícím příkladu je `numbers` zdrojem dat a `num` je proměnná rozsahu.</span><span class="sxs-lookup"><span data-stu-id="22ebf-110">In the following example, `numbers` is the data source and `num` is the range variable.</span></span> <span data-ttu-id="22ebf-111">Všimněte si, že obě proměnné jsou silného typu, i když je použito klíčové slovo [var](var.md) .</span><span class="sxs-lookup"><span data-stu-id="22ebf-111">Note that both variables are strongly typed even though the [var](var.md) keyword is used.</span></span>

[!code-csharp[cscsrefQueryKeywords#1](~/samples/snippets/csharp/VS_Snippets_VBCSharp/CsCsrefQueryKeywords/CS/From.cs#1)]

## <a name="the-range-variable"></a><span data-ttu-id="22ebf-112">Proměnná rozsahu</span><span class="sxs-lookup"><span data-stu-id="22ebf-112">The range variable</span></span>

<span data-ttu-id="22ebf-113">Kompilátor odvodí typ proměnné rozsahu, když zdroj dat implementuje <xref:System.Collections.Generic.IEnumerable%601>.</span><span class="sxs-lookup"><span data-stu-id="22ebf-113">The compiler infers the type of the range variable when the data source implements <xref:System.Collections.Generic.IEnumerable%601>.</span></span> <span data-ttu-id="22ebf-114">Pokud má zdroj například typ `IEnumerable<Customer>`, je proměnná rozsahu odvozena pro `Customer`.</span><span class="sxs-lookup"><span data-stu-id="22ebf-114">For example, if the source has a type of `IEnumerable<Customer>`, then the range variable is inferred to be `Customer`.</span></span> <span data-ttu-id="22ebf-115">Jediným okamžikem, kdy je nutné explicitně zadat typ, je, že zdroj je neobecný `IEnumerable` typ, jako je například <xref:System.Collections.ArrayList>.</span><span class="sxs-lookup"><span data-stu-id="22ebf-115">The only time that you must specify the type explicitly is when the source is a non-generic `IEnumerable` type such as <xref:System.Collections.ArrayList>.</span></span> <span data-ttu-id="22ebf-116">Další informace najdete v tématu [Postup dotazování objektu ArrayList pomocí LINQ](../../programming-guide/concepts/linq/how-to-query-an-arraylist-with-linq.md).</span><span class="sxs-lookup"><span data-stu-id="22ebf-116">For more information, see [How to query an ArrayList with LINQ](../../programming-guide/concepts/linq/how-to-query-an-arraylist-with-linq.md).</span></span>

<span data-ttu-id="22ebf-117">V předchozím příkladu je `num` odvozený jako `int`typu.</span><span class="sxs-lookup"><span data-stu-id="22ebf-117">In the previous example `num` is inferred to be of type `int`.</span></span> <span data-ttu-id="22ebf-118">Vzhledem k tomu, že je proměnná rozsahu silného typu, můžete na ni zavolat metody nebo je použít v jiných operacích.</span><span class="sxs-lookup"><span data-stu-id="22ebf-118">Because the range variable is strongly typed, you can call methods on it or use it in other operations.</span></span> <span data-ttu-id="22ebf-119">Například namísto psaní `select num`můžete napsat `select num.ToString()`, která způsobí, že výraz dotazu vrátí sekvenci řetězců místo celých čísel.</span><span class="sxs-lookup"><span data-stu-id="22ebf-119">For example, instead of writing `select num`, you could write `select num.ToString()` to cause the query expression to return a sequence of strings instead of integers.</span></span> <span data-ttu-id="22ebf-120">Můžete také napsat `select num + 10` a způsobit tak, že výraz vrátí sekvenci 14, 11, 13, 12, 10.</span><span class="sxs-lookup"><span data-stu-id="22ebf-120">Or you could write `select num + 10` to cause the expression to return the sequence 14, 11, 13, 12, 10.</span></span> <span data-ttu-id="22ebf-121">Další informace najdete v tématu [klauzule SELECT](select-clause.md).</span><span class="sxs-lookup"><span data-stu-id="22ebf-121">For more information, see [select clause](select-clause.md).</span></span>

<span data-ttu-id="22ebf-122">Proměnná rozsahu je jako iterační proměnná v příkazu [foreach](foreach-in.md) s výjimkou jednoho velmi důležitého rozdílu: proměnná rozsahu nikdy neukládá data ze zdroje.</span><span class="sxs-lookup"><span data-stu-id="22ebf-122">The range variable is like an iteration variable in a [foreach](foreach-in.md) statement except for one very important difference: a range variable never actually stores data from the source.</span></span> <span data-ttu-id="22ebf-123">Jedná se pouze o syntaktické pohodlí, které umožňuje dotazu popsat, co se vyskytne při spuštění dotazu.</span><span class="sxs-lookup"><span data-stu-id="22ebf-123">It's just a syntactic convenience that enables the query to describe what will occur when the query is executed.</span></span> <span data-ttu-id="22ebf-124">Další informace najdete v tématu [Úvod do dotazů LINQ (C#)](../../programming-guide/concepts/linq/introduction-to-linq-queries.md).</span><span class="sxs-lookup"><span data-stu-id="22ebf-124">For more information, see [Introduction to LINQ Queries (C#)](../../programming-guide/concepts/linq/introduction-to-linq-queries.md).</span></span>

## <a name="compound-from-clauses"></a><span data-ttu-id="22ebf-125">Složené klauzule FROM</span><span class="sxs-lookup"><span data-stu-id="22ebf-125">Compound from clauses</span></span>

<span data-ttu-id="22ebf-126">V některých případech může být každý prvek ve zdrojové sekvenci sám sebou buď sekvence, nebo obsahovat sekvenci.</span><span class="sxs-lookup"><span data-stu-id="22ebf-126">In some cases, each element in the source sequence may itself be either a sequence or contain a sequence.</span></span> <span data-ttu-id="22ebf-127">Zdroj dat může být například `IEnumerable<Student>`, kde každý objekt studenta v sekvenci obsahuje seznam hodnocení testů.</span><span class="sxs-lookup"><span data-stu-id="22ebf-127">For example, your data source may be an `IEnumerable<Student>` where each student object in the sequence contains a list of test scores.</span></span> <span data-ttu-id="22ebf-128">Chcete-li získat přístup k vnitřnímu seznamu v rámci každého prvku `Student`, můžete použít složené klauzule `from`.</span><span class="sxs-lookup"><span data-stu-id="22ebf-128">To access the inner list within each `Student` element, you can use compound `from` clauses.</span></span> <span data-ttu-id="22ebf-129">Technika je jako použití vnořených příkazů [foreach](foreach-in.md) .</span><span class="sxs-lookup"><span data-stu-id="22ebf-129">The technique is like using nested [foreach](foreach-in.md) statements.</span></span> <span data-ttu-id="22ebf-130">Klauzule [WHERE](partial-method.md) nebo [OrderBy](orderby-clause.md) můžete přidat do klauzule `from` pro filtrování výsledků.</span><span class="sxs-lookup"><span data-stu-id="22ebf-130">You can add [where](partial-method.md) or [orderby](orderby-clause.md) clauses to either `from` clause to filter the results.</span></span> <span data-ttu-id="22ebf-131">Následující příklad ukazuje sekvenci `Student` objektů, z nichž každý obsahuje vnitřní `List` celých čísel, která představují hodnocení testů.</span><span class="sxs-lookup"><span data-stu-id="22ebf-131">The following example shows a sequence of `Student` objects, each of which contains an inner `List` of integers representing test scores.</span></span> <span data-ttu-id="22ebf-132">Chcete-li získat přístup k vnitřnímu seznamu, použijte složenou klauzuli `from`.</span><span class="sxs-lookup"><span data-stu-id="22ebf-132">To access the inner list, use a compound `from` clause.</span></span> <span data-ttu-id="22ebf-133">V případě potřeby můžete do obou klauzulí `from` vkládat klauzule.</span><span class="sxs-lookup"><span data-stu-id="22ebf-133">You can insert clauses between the two `from` clauses if necessary.</span></span>

[!code-csharp[cscsrefQueryKeywords#2](~/samples/snippets/csharp/VS_Snippets_VBCSharp/CsCsrefQueryKeywords/CS/From.cs#2)]

## <a name="using-multiple-from-clauses-to-perform-joins"></a><span data-ttu-id="22ebf-134">Použití více klauzulí FROM pro provedení spojení</span><span class="sxs-lookup"><span data-stu-id="22ebf-134">Using Multiple from Clauses to Perform Joins</span></span>

<span data-ttu-id="22ebf-135">Klauzule složeného `from` se používá pro přístup k vnitřním kolekcím v jednom zdroji dat.</span><span class="sxs-lookup"><span data-stu-id="22ebf-135">A compound `from` clause is used to access inner collections in a single data source.</span></span> <span data-ttu-id="22ebf-136">Dotaz však může obsahovat také více klauzulí `from`, které generují doplňkové dotazy z nezávislých zdrojů dat.</span><span class="sxs-lookup"><span data-stu-id="22ebf-136">However, a query can also contain multiple `from` clauses that generate supplemental queries from independent data sources.</span></span> <span data-ttu-id="22ebf-137">Tato technika vám umožní provádět určité typy operací spojení, které nejsou možné pomocí [klauzule JOIN](join-clause.md).</span><span class="sxs-lookup"><span data-stu-id="22ebf-137">This technique enables you to perform certain types of join operations that are not possible by using the [join clause](join-clause.md).</span></span>

<span data-ttu-id="22ebf-138">Následující příklad ukazuje, jak lze pomocí dvou klauzulí `from` vytvořit úplné vzájemné spojení dvou zdrojů dat.</span><span class="sxs-lookup"><span data-stu-id="22ebf-138">The following example shows how two `from` clauses can be used to form a complete cross join of two data sources.</span></span>

[!code-csharp[cscsrefQueryKeywords#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/CsCsrefQueryKeywords/CS/From.cs#3)]

<span data-ttu-id="22ebf-139">Další informace o operacích JOIN, které používají více klauzulí `from`, najdete v tématu [provedení levých vnějších spojení](../../linq/perform-left-outer-joins.md).</span><span class="sxs-lookup"><span data-stu-id="22ebf-139">For more information about join operations that use multiple `from` clauses, see [Perform left outer joins](../../linq/perform-left-outer-joins.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="22ebf-140">Viz také:</span><span class="sxs-lookup"><span data-stu-id="22ebf-140">See also</span></span>

- [<span data-ttu-id="22ebf-141">Klíčová slova dotazu (LINQ)</span><span class="sxs-lookup"><span data-stu-id="22ebf-141">Query Keywords (LINQ)</span></span>](query-keywords.md)
- [<span data-ttu-id="22ebf-142"> (LINQ)</span><span class="sxs-lookup"><span data-stu-id="22ebf-142">Language Integrated Query (LINQ)</span></span>](../../linq/index.md)
