---
title: await – C# odkaz
ms.custom: seodec18
ms.date: 05/22/2017
f1_keywords:
- await_CSharpKeyword
helpviewer_keywords:
- await keyword [C#]
- await [C#]
ms.assetid: 50725c24-ac76-4ca7-bca1-dd57642ffedb
ms.openlocfilehash: 1afd763d41ac3ffd42409ff8d1b8823979ab0c08
ms.sourcegitcommit: 160a88c8087b0e63606e6e35f9bd57fa5f69c168
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/09/2019
ms.locfileid: "57713043"
---
# <a name="await-c-reference"></a><span data-ttu-id="8af8f-102">await – – operátor (Referenční dokumentace jazyka C#)</span><span class="sxs-lookup"><span data-stu-id="8af8f-102">await (C# Reference)</span></span>
<span data-ttu-id="8af8f-103">`await` Operátor je použít pro úkol v asynchronní metodě vložte bodu pozastavení provádění metody až do dokončení očekávané úlohy.</span><span class="sxs-lookup"><span data-stu-id="8af8f-103">The `await` operator is applied to a task in an asynchronous method to insert a suspension point in the execution of the method until the awaited task completes.</span></span> <span data-ttu-id="8af8f-104">Úloha představuje probíhající práci.</span><span class="sxs-lookup"><span data-stu-id="8af8f-104">The task represents ongoing work.</span></span>  
  
<span data-ttu-id="8af8f-105">`await` jde použít jenom v asynchronní metodě změnil [asynchronní](../../../csharp/language-reference/keywords/async.md) – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="8af8f-105">`await` can only be used in an asynchronous method modified by the [async](../../../csharp/language-reference/keywords/async.md) keyword.</span></span> <span data-ttu-id="8af8f-106">Tato metoda, definována pomocí `async` modifikátor a obvykle obsahuje jeden nebo více `await` výrazy, se označuje jako *asynchronní metoda*.</span><span class="sxs-lookup"><span data-stu-id="8af8f-106">Such a method, defined by using the `async` modifier and usually containing one or more `await` expressions, is referred to as an *async method*.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="8af8f-107">`async` a `await` klíčová slova byly zavedeny v jazyce C# 5.</span><span class="sxs-lookup"><span data-stu-id="8af8f-107">The `async` and `await` keywords were introduced in C# 5.</span></span> <span data-ttu-id="8af8f-108">Úvod do asynchronního programování naleznete v tématu [asynchronní programování pomocí modifikátoru async a operátoru await](../../../csharp/programming-guide/concepts/async/index.md).</span><span class="sxs-lookup"><span data-stu-id="8af8f-108">For an introduction to async programming, see [Asynchronous Programming with async and await](../../../csharp/programming-guide/concepts/async/index.md).</span></span>  
  
<span data-ttu-id="8af8f-109">Úkol, na který `await` je použit operátor obvykle je vrácen voláním metody, která implementuje [Task-Based Asynchronous Pattern](../../../standard/asynchronous-programming-patterns/task-based-asynchronous-pattern-tap.md).</span><span class="sxs-lookup"><span data-stu-id="8af8f-109">The task to which the `await` operator is applied typically is returned by a call to a method that implements the [Task-Based Asynchronous Pattern](../../../standard/asynchronous-programming-patterns/task-based-asynchronous-pattern-tap.md).</span></span> <span data-ttu-id="8af8f-110">Patří sem metody, které vracejí <xref:System.Threading.Tasks.Task>, <xref:System.Threading.Tasks.Task%601>, <xref:System.Threading.Tasks.ValueTask>, a <xref:System.Threading.Tasks.ValueTask%601> objekty.</span><span class="sxs-lookup"><span data-stu-id="8af8f-110">They include methods that return <xref:System.Threading.Tasks.Task>, <xref:System.Threading.Tasks.Task%601>, <xref:System.Threading.Tasks.ValueTask>, and <xref:System.Threading.Tasks.ValueTask%601> objects.</span></span>  

<span data-ttu-id="8af8f-111">V následujícím příkladu <xref:System.Net.Http.HttpClient.GetByteArrayAsync%2A?displayProperty=nameWithType> metoda vrátí hodnotu `Task<byte[]>`.</span><span class="sxs-lookup"><span data-stu-id="8af8f-111">In the following example, the <xref:System.Net.Http.HttpClient.GetByteArrayAsync%2A?displayProperty=nameWithType> method returns a `Task<byte[]>`.</span></span> <span data-ttu-id="8af8f-112">Úkol je příslib z k vytvoření pole bajtů po dokončení úkolu.</span><span class="sxs-lookup"><span data-stu-id="8af8f-112">The task is a promise to produce the actual byte array when the task is complete.</span></span> <span data-ttu-id="8af8f-113">`await` Operátor pozastaví provádění, dokud práce <xref:System.Net.Http.HttpClient.GetByteArrayAsync%2A> dokončení metody.</span><span class="sxs-lookup"><span data-stu-id="8af8f-113">The `await` operator suspends execution until the work of the <xref:System.Net.Http.HttpClient.GetByteArrayAsync%2A> method is complete.</span></span> <span data-ttu-id="8af8f-114">Do té doby se ovládací prvek vrátí volajícímu metody `GetPageSizeAsync`.</span><span class="sxs-lookup"><span data-stu-id="8af8f-114">In the meantime, control is returned to the caller of `GetPageSizeAsync`.</span></span> <span data-ttu-id="8af8f-115">Po dokončení provádění úlohy `await` výraz vyhodnocen jako bajtové pole.</span><span class="sxs-lookup"><span data-stu-id="8af8f-115">When the task finishes execution, the `await` expression evaluates to a byte array.</span></span>  

[!code-csharp[await-example](../../../../samples/snippets/csharp/language-reference/keywords/await/await1.cs)]  

> [!IMPORTANT]
> <span data-ttu-id="8af8f-116">Kompletní příklad naleznete v tématu [názorný postup: Přístup k webu pomocí modifikátoru Async a operátoru Await](../../../csharp/programming-guide/concepts/async/walkthrough-accessing-the-web-by-using-async-and-await.md).</span><span class="sxs-lookup"><span data-stu-id="8af8f-116">For the complete example, see [Walkthrough: Accessing the Web by Using Async and Await](../../../csharp/programming-guide/concepts/async/walkthrough-accessing-the-web-by-using-async-and-await.md).</span></span> <span data-ttu-id="8af8f-117">Můžete stáhnout ukázku z [ukázky kódu vývojáře](https://code.msdn.microsoft.com/Async-Sample-Accessing-the-9c10497f) na webu společnosti Microsoft.</span><span class="sxs-lookup"><span data-stu-id="8af8f-117">You can download the sample from [Developer Code Samples](https://code.msdn.microsoft.com/Async-Sample-Accessing-the-9c10497f) on the Microsoft website.</span></span> <span data-ttu-id="8af8f-118">V příkladu je v projektu AsyncWalkthrough_HttpClient.</span><span class="sxs-lookup"><span data-stu-id="8af8f-118">The example is in the AsyncWalkthrough_HttpClient project.</span></span>  
  
<span data-ttu-id="8af8f-119">Jak je znázorněno v předchozím příkladu, pokud `await` se použije na výsledek volání metody, která vrací `Task<TResult>`, potom je typ `await` výraz je `TResult`.</span><span class="sxs-lookup"><span data-stu-id="8af8f-119">As shown in the previous example, if `await` is applied to the result of a method call that returns a `Task<TResult>`, then the type of the `await` expression is `TResult`.</span></span> <span data-ttu-id="8af8f-120">Pokud `await` se použije na výsledek volání metody, která vrací `Task`, potom je typ `await` výraz je `void`.</span><span class="sxs-lookup"><span data-stu-id="8af8f-120">If `await` is applied to the result of a method call that returns a `Task`, then the type of the `await` expression is `void`.</span></span> <span data-ttu-id="8af8f-121">Následující příklad ukazuje rozdíl.</span><span class="sxs-lookup"><span data-stu-id="8af8f-121">The following example illustrates the difference.</span></span>  
  
```csharp  
// await keyword used with a method that returns a Task<TResult>.  
TResult result = await AsyncMethodThatReturnsTaskTResult();  
  
// await keyword used with a method that returns a Task.  
await AsyncMethodThatReturnsTask();  

// await keyword used with a method that returns a ValueTask<TResult>.
TResult result = await AsyncMethodThatReturnsValueTaskTResult();
```  
  
<span data-ttu-id="8af8f-122">`await` Výraz neblokuje vlákno, na kterém je spuštěn.</span><span class="sxs-lookup"><span data-stu-id="8af8f-122">An `await` expression does not block the thread on which it is executing.</span></span> <span data-ttu-id="8af8f-123">Místo toho způsobí, že kompilátor zapíše zbytek asynchronní metody jako pokračování očekávané úlohy.</span><span class="sxs-lookup"><span data-stu-id="8af8f-123">Instead, it causes the compiler to sign up the rest of the async method as a continuation on the awaited task.</span></span> <span data-ttu-id="8af8f-124">Ovládací prvek vrátí volajícímu asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="8af8f-124">Control then returns to the caller of the async method.</span></span> <span data-ttu-id="8af8f-125">Po dokončení úkolu vyvolá jeho pokračování a provádění asynchronní metody pokračuje tam, kde skončila.</span><span class="sxs-lookup"><span data-stu-id="8af8f-125">When the task completes, it invokes its continuation, and execution of the async method resumes where it left off.</span></span>  
  
<span data-ttu-id="8af8f-126">`await` Výraz může dojít pouze v těle jeho nadřazené metody, výrazu lambda nebo anonymní metodu, která musí být označeny pomocí `async` modifikátor.</span><span class="sxs-lookup"><span data-stu-id="8af8f-126">An `await` expression can occur only in the body of its enclosing method, lambda expression, or anonymous method, which must be marked with an `async` modifier.</span></span> <span data-ttu-id="8af8f-127">Termín *await* slouží jako klíčové slovo pouze v tomto kontextu.</span><span class="sxs-lookup"><span data-stu-id="8af8f-127">The term *await* serves as a keyword only in that context.</span></span> <span data-ttu-id="8af8f-128">Jinde je interpretován jako identifikátor.</span><span class="sxs-lookup"><span data-stu-id="8af8f-128">Elsewhere, it is interpreted as an identifier.</span></span> <span data-ttu-id="8af8f-129">V rámci metody, výrazu lambda nebo anonymní metodu `await` výrazu nelze provést v těle synchronní funkce, ve výrazu dotazu v bloku [lock – příkaz](../../../csharp/language-reference/keywords/lock-statement.md), nebo v [unsafe](../../../csharp/language-reference/keywords/unsafe.md) kontextu.</span><span class="sxs-lookup"><span data-stu-id="8af8f-129">Within the method, lambda expression, or anonymous method, an `await` expression cannot occur in the body of a synchronous function, in a query expression, in the block of a [lock statement](../../../csharp/language-reference/keywords/lock-statement.md), or in an [unsafe](../../../csharp/language-reference/keywords/unsafe.md) context.</span></span>  
  
## <a name="exceptions"></a><span data-ttu-id="8af8f-130">Výjimky</span><span class="sxs-lookup"><span data-stu-id="8af8f-130">Exceptions</span></span>  
<span data-ttu-id="8af8f-131">Většina asynchronních metod vrátí <xref:System.Threading.Tasks.Task> nebo <xref:System.Threading.Tasks.Task%601>.</span><span class="sxs-lookup"><span data-stu-id="8af8f-131">Most async methods return a <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="8af8f-132">Vlastnosti vrácené úlohy nesou informaci o jejím stavu a historii, např. Určuje, zda je úkol dokončený, zda asynchronní metoda způsobila výjimku nebo byla zrušena a co je konečný výsledek je.</span><span class="sxs-lookup"><span data-stu-id="8af8f-132">The properties of the returned task carry information about its status and history, such as whether the task is complete, whether the async method caused an exception or was canceled, and what the final result is.</span></span> <span data-ttu-id="8af8f-133">`await` Operátor má přístup k těmto vlastnostem voláním metod na objekt vrácený rutinou `GetAwaiter` metody.</span><span class="sxs-lookup"><span data-stu-id="8af8f-133">The `await` operator accesses those properties by calling methods on the object returned by the `GetAwaiter` method.</span></span>  
  
<span data-ttu-id="8af8f-134">Pokud očekáváte vracející úlohy asynchronní metodu, která způsobí výjimku, `await` operátor znovu vyvolá výjimku.</span><span class="sxs-lookup"><span data-stu-id="8af8f-134">If you await a task-returning async method that causes an exception, the `await` operator rethrows the exception.</span></span>  
  
<span data-ttu-id="8af8f-135">Pokud očekáváte vracející úlohy asynchronní metody, která je zrušena, `await` znovu vyvolá operátor <xref:System.OperationCanceledException>.</span><span class="sxs-lookup"><span data-stu-id="8af8f-135">If you await a task-returning async method that's canceled, the `await` operator rethrows an <xref:System.OperationCanceledException>.</span></span>  
  
<span data-ttu-id="8af8f-136">Jeden úkol, který je v chybovém stavu, může odrážet více výjimek.</span><span class="sxs-lookup"><span data-stu-id="8af8f-136">A single task that is in a faulted state can reflect multiple exceptions.</span></span> <span data-ttu-id="8af8f-137">Úloha může být například výsledek volání <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="8af8f-137">For example, the task might be the result of a call to <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="8af8f-138">Když budete očekávat takový úkol, operace await vrátí pouze jednu z výjimek.</span><span class="sxs-lookup"><span data-stu-id="8af8f-138">When you await such a task, the await operation rethrows only one of the exceptions.</span></span> <span data-ttu-id="8af8f-139">Nelze však předpovědět, které výjimky je znovu vyvolána.</span><span class="sxs-lookup"><span data-stu-id="8af8f-139">However, you can't predict which of the exceptions is rethrown.</span></span>  
  
<span data-ttu-id="8af8f-140">Příklady zpracování chyb v asynchronních metodách, naleznete v tématu [bloku try-catch](../../../csharp/language-reference/keywords/try-catch.md).</span><span class="sxs-lookup"><span data-stu-id="8af8f-140">For examples of error handling in async methods, see [try-catch](../../../csharp/language-reference/keywords/try-catch.md).</span></span>  
  
## <a name="example"></a><span data-ttu-id="8af8f-141">Příklad</span><span class="sxs-lookup"><span data-stu-id="8af8f-141">Example</span></span>  
<span data-ttu-id="8af8f-142">Následující příklad vrátí celkový počet znaků na stránkách, jejichž adresy URL jsou předány jako argumenty příkazového řádku.</span><span class="sxs-lookup"><span data-stu-id="8af8f-142">The following example returns the total number of characters in the pages whose URLs are passed to it as command line arguments.</span></span> <span data-ttu-id="8af8f-143">Příklad volá `GetPageLengthsAsync` metodu, která je označena `async` – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="8af8f-143">The example calls the `GetPageLengthsAsync` method, which is marked with the `async` keyword.</span></span> <span data-ttu-id="8af8f-144">`GetPageLengthsAsync` Dále používá metodu `await` – klíčové slovo await pro čekání volání <xref:System.Net.Http.HttpClient.GetStringAsync%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="8af8f-144">The `GetPageLengthsAsync` method in turn uses the `await` keyword to await calls to the <xref:System.Net.Http.HttpClient.GetStringAsync%2A?displayProperty=nameWithType> method.</span></span>  

[!code-csharp[await-example](../../../../samples/snippets/csharp/language-reference/keywords/await/await2.cs)]  

<span data-ttu-id="8af8f-145">V předchozím příkladu C# 7.1, která podporuje [ `async` `Main` metoda](../../programming-guide/main-and-command-args/index.md).</span><span class="sxs-lookup"><span data-stu-id="8af8f-145">The preceding example uses C# 7.1, which supports the [`async` `Main` method](../../programming-guide/main-and-command-args/index.md).</span></span> <span data-ttu-id="8af8f-146">Protože starší C# verze nepodporují vstupní body aplikace, které vracejí <xref:System.Threading.Tasks.Task> nebo <xref:System.Threading.Tasks.Task%601>, nelze použít `async` modifikátor `Main` metoda a operátoru await `GetPageLengthsAsync` volání metody.</span><span class="sxs-lookup"><span data-stu-id="8af8f-146">Because earlier C# versions don't support application entry points that return <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601>, you cannot apply the `async` modifier to the `Main` method and await the `GetPageLengthsAsync` method call.</span></span> <span data-ttu-id="8af8f-147">V takovém případě můžete zajistit, aby `Main` metoda čeká na dokončení načtením hodnoty asynchronní operace <xref:System.Threading.Tasks.Task%601.Result?displayProperty=nameWithType> vlastnost.</span><span class="sxs-lookup"><span data-stu-id="8af8f-147">In that case, you can ensure that the `Main` method waits for the async operation to complete by retrieving the value of the <xref:System.Threading.Tasks.Task%601.Result?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="8af8f-148">Pro úlohy, které nesmí vracet hodnotu, můžete volat <xref:System.Threading.Tasks.Task.Wait%2A?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="8af8f-148">For tasks that do not return a value, you can call the <xref:System.Threading.Tasks.Task.Wait%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="8af8f-149">Informace o tom, jak vyberte verzi jazyka najdete v tématu [vyberte C# jazykovou verzi](../configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="8af8f-149">For information about how to select the language version, see [Select the C# language version](../configure-language-version.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="8af8f-150">Viz také:</span><span class="sxs-lookup"><span data-stu-id="8af8f-150">See also</span></span>
- [<span data-ttu-id="8af8f-151">Asynchronní programování pomocí modifikátoru Async a operátoru Await</span><span class="sxs-lookup"><span data-stu-id="8af8f-151">Asynchronous Programming with async and await</span></span>](../../../csharp/programming-guide/concepts/async/index.md)
- [<span data-ttu-id="8af8f-152">Návod: Přístup k webu pomocí modifikátoru Async a operátoru Await</span><span class="sxs-lookup"><span data-stu-id="8af8f-152">Walkthrough: Accessing the Web by Using Async and Await</span></span>](../../../csharp/programming-guide/concepts/async/walkthrough-accessing-the-web-by-using-async-and-await.md)
- [<span data-ttu-id="8af8f-153">async</span><span class="sxs-lookup"><span data-stu-id="8af8f-153">async</span></span>](../../../csharp/language-reference/keywords/async.md)
