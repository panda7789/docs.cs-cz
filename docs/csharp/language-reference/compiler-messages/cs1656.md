---
title: Chyba kompilátoru CS1656
ms.date: 07/20/2015
f1_keywords:
- CS1656
helpviewer_keywords:
- CS1656
ms.assetid: b5463a12-d685-4dae-9f88-08383e271b7a
ms.openlocfilehash: 24e764fe39139e13af0c987e4a782826ce4d2c37
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/23/2019
ms.locfileid: "61668858"
---
# <a name="compiler-error-cs1656"></a><span data-ttu-id="13038-102">Chyba kompilátoru CS1656</span><span class="sxs-lookup"><span data-stu-id="13038-102">Compiler Error CS1656</span></span>
<span data-ttu-id="13038-103">Nelze přiřadit 'variable', protože je "jen pro čtení typu proměnné.</span><span class="sxs-lookup"><span data-stu-id="13038-103">Cannot assign to 'variable' because it is a 'read-only variable type'</span></span>  
  
 <span data-ttu-id="13038-104">K této chybě dochází, když dojde k přiřazení k proměnné v rámci jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="13038-104">This error occurs when an assignment to variable occurs in a read-only context.</span></span> <span data-ttu-id="13038-105">Kontexty jen pro čtení zahrnují [foreach](../../../csharp/language-reference/keywords/foreach-in.md) iterační proměnné [pomocí](../../../csharp/language-reference/keywords/using-statement.md) proměnné, a [oprava](../../../csharp/language-reference/keywords/fixed-statement.md) proměnné.</span><span class="sxs-lookup"><span data-stu-id="13038-105">Read-only contexts include [foreach](../../../csharp/language-reference/keywords/foreach-in.md) iteration variables, [using](../../../csharp/language-reference/keywords/using-statement.md) variables, and [fixed](../../../csharp/language-reference/keywords/fixed-statement.md) variables.</span></span> <span data-ttu-id="13038-106">Chcete-li vyřešit tuto chybu, vyhněte se přiřazení k příkazu proměnné v `using` bloky, `foreach` příkazy, a `fixed` příkazy.</span><span class="sxs-lookup"><span data-stu-id="13038-106">To resolve this error, avoid assignments to a statement variable in `using` blocks, `foreach` statements, and `fixed` statements.</span></span>  
  
## <a name="example"></a><span data-ttu-id="13038-107">Příklad</span><span class="sxs-lookup"><span data-stu-id="13038-107">Example</span></span>  
 <span data-ttu-id="13038-108">Následující příklad vygeneruje chybu CS1656, protože se pokusí nahradit kompletní prvků kolekce uvnitř `foreach` smyčky.</span><span class="sxs-lookup"><span data-stu-id="13038-108">The following example generates error CS1656 because it tries to replace complete elements of a collection inside a `foreach` loop.</span></span> <span data-ttu-id="13038-109">Jedním ze způsobů, chcete-li vyřešit chybu je změnit `foreach` smyčku do [pro](../../../csharp/language-reference/keywords/for.md) smyčky.</span><span class="sxs-lookup"><span data-stu-id="13038-109">One way to work around the error is to change the `foreach` loop to a [for](../../../csharp/language-reference/keywords/for.md) loop.</span></span> <span data-ttu-id="13038-110">Dalším způsobem, není vidět, tady je upravit členy existující prvek; To je možné s třídami, ale ne s struktury.</span><span class="sxs-lookup"><span data-stu-id="13038-110">Another way, not shown here, is to modify the members of the existing element; this is possible with classes, but not with structs.</span></span>  
  
```csharp  
using System;  
using System.Collections;  
using System.Collections.Generic;  
using System.Text;  
  
namespace CS1656_2  
{  
  
    class Book  
    {  
        public string Title;  
        public string Author;  
        public double Price;  
        public Book(string t, string a, double p)  
        {  
            Title=t;  
            Author=a;  
            Price=p;  
  
        }  
    }  
  
    class Program  
    {  
        private List<Book> list;  
        static void Main(string[] args)  
        {  
            Program prog = new Program();  
            prog.list = new List<Book>();  
            prog.list.Add(new Book ("The C# Programming Language",  
                                    "Hejlsberg, Wiltamuth, Golde",  
                                     29.95));  
            prog.list.Add(new Book ("The C++ Programming Language",  
                                    "Stroustrup",  
                                     29.95));  
            prog.list.Add(new Book ("The C Programming Language",  
                                    "Kernighan, Ritchie",  
                                    29.95));  
            foreach(Book b in prog.list)  
            {  
                // Cannot modify an entire element in a foreach loop   
                // even with reference types.  
                // Use a for or while loop instead  
                if (b.Title == "The C Programming Language")  
                    // Cannot assign to 'b' because it is a 'foreach   
                    // iteration variable'  
                    b = new Book("Programming Windows, 5th Ed.", "Petzold", 29.95); //CS1656  
            }  
  
            //With a for loop you can modify elements  
            //for(int x = 0; x < prog.list.Count; x++)  
            //{  
            //    if(prog.list[x].Title== "The C Programming Language")  
            //        prog.list[x] = new Book("Programming Windows, 5th Ed.", "Petzold", 29.95);  
            //}  
            //foreach(Book b in prog.list)  
            //    Console.WriteLine(b.Title);  
  
        }  
    }  
}  
```  
  
## <a name="example"></a><span data-ttu-id="13038-111">Příklad</span><span class="sxs-lookup"><span data-stu-id="13038-111">Example</span></span>  
 <span data-ttu-id="13038-112">Následující příklad ukazuje, jak může být generována CS1656 v jiných kontextech kromě `foreach` smyčka:</span><span class="sxs-lookup"><span data-stu-id="13038-112">The following sample demonstrates how CS1656 can be generated in other contexts besides a `foreach` loop:</span></span>  
  
```csharp  
// CS1656.cs  
// compile with: /unsafe  
using System;  
  
class C : IDisposable  
{  
    public void Dispose() { }  
}  
  
class CMain  
{  
    unsafe public static void Main()  
    {  
        using (C c = new C())  
        {  
            // Cannot assign to 'c' because it is a 'using variable'  
            c = new C(); // CS1656  
        }  
  
        int[] ary = new int[] { 1, 2, 3, 4 };  
        fixed (int* p = ary)  
        {  
            // Cannot assign to 'p' because it is a 'fixed variable'  
            p = null; // CS1656  
        }  
    }  
}  
```
