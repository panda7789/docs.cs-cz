---
title: Chyba kompilátoru CS0304
ms.date: 07/20/2015
f1_keywords:
- CS0304
helpviewer_keywords:
- CS0304
ms.assetid: 22dc7211-57a8-4c92-96f6-fc1cf0263b68
ms.openlocfilehash: cf751cc099598f39b00e616154fe438c1edb142e
ms.sourcegitcommit: 986f836f72ef10876878bd6217174e41464c145a
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/19/2019
ms.locfileid: "69603821"
---
# <a name="compiler-error-cs0304"></a><span data-ttu-id="f75cb-102">Chyba kompilátoru CS0304</span><span class="sxs-lookup"><span data-stu-id="f75cb-102">Compiler Error CS0304</span></span>

<span data-ttu-id="f75cb-103">Nejde vytvořit instanci proměnné typu Type, protože nemá omezení New ().</span><span class="sxs-lookup"><span data-stu-id="f75cb-103">Cannot create an instance of the variable type 'type' because it does not have the new() constraint</span></span>  
  
 <span data-ttu-id="f75cb-104">Při implementaci obecné třídy `new` a chcete použít klíčové slovo k vytvoření nové instance libovolného typu, který je dodán pro parametr `T`typu, je nutné použít [omezení New ()](../keywords/new-constraint.md) na `T` v deklaraci třídy, jako znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="f75cb-104">When you implement a generic class, and you want to use the `new` keyword to create a new instance of any type that is supplied for a type parameter `T`, you must apply the [new() constraint](../keywords/new-constraint.md) to `T` in the class declaration, as shown in the following example.</span></span>  
  
```csharp  
class C<T> where T : new()  
```  
  
 <span data-ttu-id="f75cb-105">Omezení vynutilo bezpečnost typů tím, že zaručuje, že jakýkoli konkrétní typ, který `T` je poskytnut pro, má výchozí konstruktor bez parametrů. `new()`</span><span class="sxs-lookup"><span data-stu-id="f75cb-105">The `new()` constraint enforces type safety by guaranteeing that any concrete type that is supplied for `T` has a default, parameterless constructor.</span></span> <span data-ttu-id="f75cb-106">K CS0304 dojde, pokud se pokusíte `new` použít operátor v těle třídy pro vytvoření instance typu parametru `T` , `new()` když `T` neurčuje omezení.</span><span class="sxs-lookup"><span data-stu-id="f75cb-106">CS0304 occurs if you attempt to use the `new` operator in the body of the class to create an instance of type parameter `T` when `T` does not specify the `new()` constraint.</span></span> <span data-ttu-id="f75cb-107">Pokud se při pokusu o vytvoření instance obecné třídy s typem, který nemá žádný konstruktor bez parametrů, vytvoří na straně klienta [Chyba kompilátoru CS0310](./cs0310.md).</span><span class="sxs-lookup"><span data-stu-id="f75cb-107">On the client side, if code attempts to instantiate the generic class with a type that has no parameterless constructor, that code will generate [Compiler Error CS0310](./cs0310.md).</span></span>  
  
 <span data-ttu-id="f75cb-108">Následující příklad generuje CS0304.</span><span class="sxs-lookup"><span data-stu-id="f75cb-108">The following example generates CS0304.</span></span>  
  
```csharp  
// CS0304.cs  
// Compile with: /target:library.  
class C<T>  
{  
    // The following line generates CS0304.  
    T t = new T();  
}  
```  
  
 <span data-ttu-id="f75cb-109">`new` Operátor také není povolen v metodách třídy.</span><span class="sxs-lookup"><span data-stu-id="f75cb-109">The `new` operator also is not allowed in methods of the class.</span></span>  
  
```csharp  
// Compile with: /target:library.  
class C<T>  
{  
    public void ExampleMethod()  
    {  
        // The following line generates CS0304.  
        T t = new T();  
    }  
}  
```  
  
 <span data-ttu-id="f75cb-110">Chcete-li se této chybě vyhnout, Deklarujte třídu `new()` pomocí omezení, jak je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="f75cb-110">To avoid the error, declare the class by using the `new()` constraint, as shown in the following example.</span></span>  
  
```csharp  
// Compile with: /target:library.  
class C<T> where T : new()  
{  
    T t = new T();  
  
    public void ExampleMethod()  
    {  
        T t = new T();  
    }  
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="f75cb-111">Viz také:</span><span class="sxs-lookup"><span data-stu-id="f75cb-111">See also</span></span>

- [<span data-ttu-id="f75cb-112">Chyby kompilátoru jazyka C#</span><span class="sxs-lookup"><span data-stu-id="f75cb-112">C# Compiler Errors</span></span>](./index.md)
