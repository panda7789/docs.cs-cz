---
title: Upozornění kompilátoru (úroveň 1) CS1956
ms.date: 07/20/2015
f1_keywords:
- CS1956
helpviewer_keywords:
- CS1956
ms.assetid: 841f8b2b-692b-49ae-a587-a40797cf9797
ms.openlocfilehash: f848cff8aaedc86cbb2b6abb4a9b856bde85741c
ms.sourcegitcommit: 558d78d2a68acd4c95ef23231c8b4e4c7bac3902
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/09/2019
ms.locfileid: "59342242"
---
# <a name="compiler-warning-level-1-cs1956"></a><span data-ttu-id="2e701-102">Upozornění kompilátoru (úroveň 1) CS1956</span><span class="sxs-lookup"><span data-stu-id="2e701-102">Compiler Warning (level 1) CS1956</span></span>
<span data-ttu-id="2e701-103">Člen 'name' implementuje člena rozhraní 'name' v typu 'type'.</span><span class="sxs-lookup"><span data-stu-id="2e701-103">Member 'name' implements interface member 'name' in type 'type'.</span></span> <span data-ttu-id="2e701-104">Existuje více shod pro tohoto člena rozhraní v době běhu.</span><span class="sxs-lookup"><span data-stu-id="2e701-104">There are multiple matches for the interface member at run-time.</span></span> <span data-ttu-id="2e701-105">Je volaná metoda závisí na konkrétní implementaci.</span><span class="sxs-lookup"><span data-stu-id="2e701-105">It is implementation dependent which method will be called.</span></span>  
  
 <span data-ttu-id="2e701-106">Toto upozornění můžete generovat, když dvě metody rozhraní se liší pouze, zda konkrétní parametru je označen klíčovým `ref` nebo s `out`.</span><span class="sxs-lookup"><span data-stu-id="2e701-106">This warning can be generated when two interface methods are differentiated only by whether a particular parameter is marked with `ref` or with `out`.</span></span> <span data-ttu-id="2e701-107">Je vhodné změnit váš kód k tomuto upozornění předejít, protože není jasné nebo zaručené, která metoda se volá za běhu.</span><span class="sxs-lookup"><span data-stu-id="2e701-107">It is best to change your code to avoid this warning because it is not obvious or guaranteed which method is called at runtime.</span></span>  
  
 <span data-ttu-id="2e701-108">I když C# rozlišuje mezi `out` a `ref`, modul CLR je vnímá jako stejný.</span><span class="sxs-lookup"><span data-stu-id="2e701-108">Although C# distinguishes between `out` and `ref`, the CLR sees them as the same.</span></span> <span data-ttu-id="2e701-109">Při rozhodování, jakou metodu implementuje rozhraní, modul CLR jednu právě vybere.</span><span class="sxs-lookup"><span data-stu-id="2e701-109">When deciding which method implements the interface, the CLR just picks one.</span></span>  
  
### <a name="to-avoid-this-warning"></a><span data-ttu-id="2e701-110">Chcete tomuto upozornění předejít</span><span class="sxs-lookup"><span data-stu-id="2e701-110">To avoid this warning</span></span>  
  
1. <span data-ttu-id="2e701-111">Poskytněte kompilátoru nějaký způsob k rozlišení metody.</span><span class="sxs-lookup"><span data-stu-id="2e701-111">Give the compiler some way to differentiate the methods.</span></span> <span data-ttu-id="2e701-112">Můžete například uživatelům umožnit jiné názvy nebo zadat další parametr na jeden z nich.</span><span class="sxs-lookup"><span data-stu-id="2e701-112">For example, you can give them different names or provide an additional parameter on one of them.</span></span>  
  
## <a name="example"></a><span data-ttu-id="2e701-113">Příklad</span><span class="sxs-lookup"><span data-stu-id="2e701-113">Example</span></span>  
 <span data-ttu-id="2e701-114">Následující kód vygeneruje CS1956, protože dva `Test` metody v `Base` se liší pouze `ref` / `out` modifikátor první parametr.</span><span class="sxs-lookup"><span data-stu-id="2e701-114">The following code generates CS1956 because the two `Test` methods in `Base` differ only by the `ref`/`out` modifier on the first parameter.</span></span>  
  
```csharp  
// cs1956.cs  
class Base<T, S>  
{  
    // This is the method that should be called.  
    public virtual int Test(out T x) // CS1956  
    {  
        x = default(T);  
        return 0;  
    }  
  
    // This is the "last" method and is the one that ends up being called  
    public virtual int Test(ref S x)  
    {  
        return 1;  
    }  
}  
  
interface IFace  
{  
    int Test(out int x);  
}  
  
class Derived : Base<int, int>, IFace  
{  
    static int Main()  
    {  
        IFace x = new Derived();  
        int y;  
        return x.Test(out y);  
    }  
}  
```
