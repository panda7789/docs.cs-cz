---
title: Upozornění kompilátoru (úroveň 1) CS1956
ms.date: 07/20/2015
f1_keywords:
- CS1956
helpviewer_keywords:
- CS1956
ms.assetid: 841f8b2b-692b-49ae-a587-a40797cf9797
ms.openlocfilehash: f848cff8aaedc86cbb2b6abb4a9b856bde85741c
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/14/2020
ms.locfileid: "61682570"
---
# <a name="compiler-warning-level-1-cs1956"></a><span data-ttu-id="c8f32-102">Upozornění kompilátoru (úroveň 1) CS1956</span><span class="sxs-lookup"><span data-stu-id="c8f32-102">Compiler Warning (level 1) CS1956</span></span>
<span data-ttu-id="c8f32-103">Člen 'name' implementuje člen rozhraní 'name' v typu 'type'.</span><span class="sxs-lookup"><span data-stu-id="c8f32-103">Member 'name' implements interface member 'name' in type 'type'.</span></span> <span data-ttu-id="c8f32-104">Existuje více shod pro člena rozhraní za běhu.</span><span class="sxs-lookup"><span data-stu-id="c8f32-104">There are multiple matches for the interface member at run-time.</span></span> <span data-ttu-id="c8f32-105">Je závislá na implementaci, která metoda bude volána.</span><span class="sxs-lookup"><span data-stu-id="c8f32-105">It is implementation dependent which method will be called.</span></span>  
  
 <span data-ttu-id="c8f32-106">Toto upozornění lze vygenerovat, pokud jsou dvě metody rozhraní `ref` rozlišeny pouze podle toho, zda je určitý parametr označen nebo s `out`.</span><span class="sxs-lookup"><span data-stu-id="c8f32-106">This warning can be generated when two interface methods are differentiated only by whether a particular parameter is marked with `ref` or with `out`.</span></span> <span data-ttu-id="c8f32-107">Je vhodné změnit kód, abyste se vyhnuli tomuto upozornění, protože není zřejmé nebo zaručeno, která metoda je volána za běhu.</span><span class="sxs-lookup"><span data-stu-id="c8f32-107">It is best to change your code to avoid this warning because it is not obvious or guaranteed which method is called at runtime.</span></span>  
  
 <span data-ttu-id="c8f32-108">Přestože C# `out` rozlišuje mezi a `ref`, CLR vidí je jako stejné.</span><span class="sxs-lookup"><span data-stu-id="c8f32-108">Although C# distinguishes between `out` and `ref`, the CLR sees them as the same.</span></span> <span data-ttu-id="c8f32-109">Při rozhodování, která metoda implementuje rozhraní, CLR pouze vybere jeden.</span><span class="sxs-lookup"><span data-stu-id="c8f32-109">When deciding which method implements the interface, the CLR just picks one.</span></span>  
  
### <a name="to-avoid-this-warning"></a><span data-ttu-id="c8f32-110">Chcete-li se tomuto varování vyhnout</span><span class="sxs-lookup"><span data-stu-id="c8f32-110">To avoid this warning</span></span>  
  
1. <span data-ttu-id="c8f32-111">Dejte kompilátoru nějaký způsob, jak odlišit metody.</span><span class="sxs-lookup"><span data-stu-id="c8f32-111">Give the compiler some way to differentiate the methods.</span></span> <span data-ttu-id="c8f32-112">Můžete jim například zadat různé názvy nebo zadat další parametr na jednom z nich.</span><span class="sxs-lookup"><span data-stu-id="c8f32-112">For example, you can give them different names or provide an additional parameter on one of them.</span></span>  
  
## <a name="example"></a><span data-ttu-id="c8f32-113">Příklad</span><span class="sxs-lookup"><span data-stu-id="c8f32-113">Example</span></span>  
 <span data-ttu-id="c8f32-114">Následující kód generuje CS1956, `Test` protože `Base` dvě metody `ref` / `out` se liší pouze modifikátorem prvního parametru.</span><span class="sxs-lookup"><span data-stu-id="c8f32-114">The following code generates CS1956 because the two `Test` methods in `Base` differ only by the `ref`/`out` modifier on the first parameter.</span></span>  
  
```csharp  
// cs1956.cs  
class Base<T, S>  
{  
    // This is the method that should be called.  
    public virtual int Test(out T x) // CS1956  
    {  
        x = default(T);  
        return 0;  
    }  
  
    // This is the "last" method and is the one that ends up being called  
    public virtual int Test(ref S x)  
    {  
        return 1;  
    }  
}  
  
interface IFace  
{  
    int Test(out int x);  
}  
  
class Derived : Base<int, int>, IFace  
{  
    static int Main()  
    {  
        IFace x = new Derived();  
        int y;  
        return x.Test(out y);  
    }  
}  
```
