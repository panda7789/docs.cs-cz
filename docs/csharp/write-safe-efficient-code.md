---
title: Zápis bezpečný a účinný C# kódu
description: Poslední vylepšení C# jazyk umožňují také napsat bezpečného kódu s možností ověření, že výkon bylo dřív přidružené nezabezpečený kód.
ms.date: 10/23/2018
ms.custom: mvc
ms.openlocfilehash: 8e58a7f870c742f1c0a90a7b5507ac1e5d8074ea
ms.sourcegitcommit: c93fd5139f9efcf6db514e3474301738a6d1d649
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/28/2018
ms.locfileid: "50201589"
---
# <a name="write-safe-and-efficient-c-code"></a><span data-ttu-id="78e94-103">Zápis bezpečný a účinný C# kódu</span><span class="sxs-lookup"><span data-stu-id="78e94-103">Write safe and efficient C# code</span></span> #

<span data-ttu-id="78e94-104">Nové funkce v C# umožňují také napsat ověřitelný kód bezpečně s lepším výkonem.</span><span class="sxs-lookup"><span data-stu-id="78e94-104">New features in C# enable you to write verifiable safe code with better performance.</span></span> <span data-ttu-id="78e94-105">Pokud použijete pečlivě těchto technik, méně scénáře vyžadují nezabezpečený kód.</span><span class="sxs-lookup"><span data-stu-id="78e94-105">If you carefully apply these techniques, fewer scenarios require unsafe code.</span></span> <span data-ttu-id="78e94-106">Tyto funkce usnadnění použití odkazů na typy hodnot jako argumenty metody a metoda vrátí hodnotu.</span><span class="sxs-lookup"><span data-stu-id="78e94-106">These features make it easier to use references to value types as method arguments and method returns.</span></span> <span data-ttu-id="78e94-107">Po dokončení bezpečně, minimalizovat těchto technik kopírování hodnotové typy.</span><span class="sxs-lookup"><span data-stu-id="78e94-107">When done safely, these techniques minimize copying value types.</span></span> <span data-ttu-id="78e94-108">Pomocí typů hodnot lze minimalizovat počet přidělování a uvolňování paměti kolekce předá.</span><span class="sxs-lookup"><span data-stu-id="78e94-108">By using value types, you can minimize the number of allocations and garbage collection passes.</span></span>

<span data-ttu-id="78e94-109">Většina ukázek kódu v tomto článku používá funkce přidané ve C# 7.2.</span><span class="sxs-lookup"><span data-stu-id="78e94-109">Much of the sample code in this article uses features added in C# 7.2.</span></span> <span data-ttu-id="78e94-110">Pokud chcete používat tyto funkce, je nutné nakonfigurovat projektu pro použití C# 7.2 nebo novější.</span><span class="sxs-lookup"><span data-stu-id="78e94-110">To use those features, you must configure your project to use C# 7.2 or later.</span></span> <span data-ttu-id="78e94-111">Další informace o nastavení jazykové verze, najdete v části [konfigurace jazyková verze](language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="78e94-111">For more information on setting the language version, see [configure the language version](language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="78e94-112">Tento článek se zaměřuje na techniky pro efektivní resource management.</span><span class="sxs-lookup"><span data-stu-id="78e94-112">This article focuses on techniques for efficient resource management.</span></span> <span data-ttu-id="78e94-113">Jednou z výhod použití jiných typů hodnot je, že často vyhnout přidělení haldy.</span><span class="sxs-lookup"><span data-stu-id="78e94-113">One advantage to using value types is that they often avoid heap allocations.</span></span> <span data-ttu-id="78e94-114">Nevýhodou je, že nezkopírují podle hodnoty.</span><span class="sxs-lookup"><span data-stu-id="78e94-114">The disadvantage is that they're copied by value.</span></span> <span data-ttu-id="78e94-115">Tato kompromis učiní obtížnější pro optimalizaci algoritmů, které pracují na velkých objemů dat.</span><span class="sxs-lookup"><span data-stu-id="78e94-115">This tradeoff makes it harder to optimize algorithms that operate on large amounts of data.</span></span> <span data-ttu-id="78e94-116">Nový jazyk funkce v C# 7.2 poskytují mechanismy, které umožňují bezpečné efektivní kódu pomocí odkazů na typy hodnot.</span><span class="sxs-lookup"><span data-stu-id="78e94-116">New language features in C# 7.2 provide mechanisms that enable safe efficient code using references to value types.</span></span> <span data-ttu-id="78e94-117">Pomocí těchto funkcí dobře minimalizovat obou přidělení a operací kopírování.</span><span class="sxs-lookup"><span data-stu-id="78e94-117">Use these features wisely to minimize both allocations and copy operations.</span></span> <span data-ttu-id="78e94-118">Tento článek se věnuje tyto nové funkce.</span><span class="sxs-lookup"><span data-stu-id="78e94-118">This article explores those new features.</span></span>

<span data-ttu-id="78e94-119">Tento článek se týká následujících postupů správy prostředků:</span><span class="sxs-lookup"><span data-stu-id="78e94-119">This article focuses on the following resource management techniques:</span></span>

- <span data-ttu-id="78e94-120">Deklarace [ `readonly struct` ](language-reference/keywords/readonly.md#readonly-struct-example) vyjádřit, že je typem **neměnné** a umožňuje kompilátoru se uložit kopie při použití [ `in` ](language-reference/keywords/in-parameter-modifier.md) parametry.</span><span class="sxs-lookup"><span data-stu-id="78e94-120">Declare a [`readonly struct`](language-reference/keywords/readonly.md#readonly-struct-example) to express that a type is **immutable** and enables the compiler to save copies when using [`in`](language-reference/keywords/in-parameter-modifier.md) parameters.</span></span>
- <span data-ttu-id="78e94-121">Použití [ `ref readonly` ](language-reference/keywords/ref.md#reference-return-values) vrácené v případě, že návratová hodnota je `struct` větší než <xref:System.IntPtr.Size?displayProperty=nameWithType> a životního cyklu úložiště je větší než metoda vrátí hodnotu.</span><span class="sxs-lookup"><span data-stu-id="78e94-121">Use a [`ref readonly`](language-reference/keywords/ref.md#reference-return-values) return when the return value is a `struct` larger than <xref:System.IntPtr.Size?displayProperty=nameWithType> and the storage lifetime is greater than the method returning the value.</span></span>
- <span data-ttu-id="78e94-122">Pokud velikost `readonly struct` je větší než <xref:System.IntPtr.Size?displayProperty=nameWithType>, je třeba předat ji jako `in` parametr z důvodů výkonu.</span><span class="sxs-lookup"><span data-stu-id="78e94-122">When the size of a `readonly struct` is bigger than <xref:System.IntPtr.Size?displayProperty=nameWithType>, you should pass it as an `in` parameter for performance reasons.</span></span>
- <span data-ttu-id="78e94-123">Nikdy předat `struct` jako `in` parametr Pokud je deklarovaná s `readonly` modifikátor protože mohou negativně ovlivnit výkon a může vést k skrytého chování.</span><span class="sxs-lookup"><span data-stu-id="78e94-123">Never pass a `struct` as an `in` parameter unless it's declared with the `readonly` modifier because it may negatively affect performance and could lead to an obscure behavior.</span></span>
- <span data-ttu-id="78e94-124">Použití [ `ref struct` ](language-reference/keywords/ref.md#ref-struct-types), nebo `readonly ref struct` například <xref:System.Span%601> nebo <xref:System.ReadOnlySpan%601> pro práci s pamětí jako sekvence bajtů.</span><span class="sxs-lookup"><span data-stu-id="78e94-124">Use a [`ref struct`](language-reference/keywords/ref.md#ref-struct-types), or a `readonly ref struct` such as <xref:System.Span%601> or <xref:System.ReadOnlySpan%601> to work with memory as a sequence of bytes.</span></span>

<span data-ttu-id="78e94-125">Tyto postupy požadovat, abyste pro dvě konkurenční cíle s ohledem na **odkazy** a **hodnoty**.</span><span class="sxs-lookup"><span data-stu-id="78e94-125">These techniques force you to balance two competing goals with regard to **references** and **values**.</span></span> <span data-ttu-id="78e94-126">Proměnné, které jsou [referenční typy](programming-guide/types/index.md#reference-types) uložení odkazu na umístění v paměti.</span><span class="sxs-lookup"><span data-stu-id="78e94-126">Variables that are [reference types](programming-guide/types/index.md#reference-types) hold a reference to the location in memory.</span></span> <span data-ttu-id="78e94-127">Proměnné, které jsou [typů hodnot](programming-guide/types/index.md#value-types) přímo obsahují své hodnoty.</span><span class="sxs-lookup"><span data-stu-id="78e94-127">Variables that are [value types](programming-guide/types/index.md#value-types) directly contain their value.</span></span> <span data-ttu-id="78e94-128">Tyto rozdíly zvýrazněte klíčové rozdíly, které jsou důležité pro správu paměťových prostředků.</span><span class="sxs-lookup"><span data-stu-id="78e94-128">These differences highlight the key differences that are important for managing memory resources.</span></span> <span data-ttu-id="78e94-129">**Typy hodnot** jsou obvykle kopírovat, pokud je předána metodě nebo vrátil z metody.</span><span class="sxs-lookup"><span data-stu-id="78e94-129">**Value types** are typically copied when passed to a method or returned from a method.</span></span> <span data-ttu-id="78e94-130">Toto chování zahrnuje kopírování hodnoty `this` při volání metody členy typu hodnoty.</span><span class="sxs-lookup"><span data-stu-id="78e94-130">This behavior includes copying the value of `this` when calling members of a value type.</span></span> <span data-ttu-id="78e94-131">Náklady na kopírování se týká velikosti typu.</span><span class="sxs-lookup"><span data-stu-id="78e94-131">The cost of the copy is related to the size of the type.</span></span> <span data-ttu-id="78e94-132">**Odkazování na typy** jsou přidělovány na spravované haldě.</span><span class="sxs-lookup"><span data-stu-id="78e94-132">**Reference types** are allocated on the managed heap.</span></span> <span data-ttu-id="78e94-133">Každý nový objekt vyžaduje nové přidělení a následně musí být získány zpět.</span><span class="sxs-lookup"><span data-stu-id="78e94-133">Each new object requires a new allocation, and subsequently must be reclaimed.</span></span> <span data-ttu-id="78e94-134">Tato operace trvat dobu.</span><span class="sxs-lookup"><span data-stu-id="78e94-134">Both these operations take time.</span></span> <span data-ttu-id="78e94-135">Odkaz je zkopírován, pokud typ odkazu je předán jako argument k metodě nebo vrátil z metody.</span><span class="sxs-lookup"><span data-stu-id="78e94-135">The reference is copied when a reference type is passed as an argument to a method or returned from a method.</span></span>

<span data-ttu-id="78e94-136">Tento článek používá následující příklad konceptu struktura 3D bodu vysvětlit tato doporučení:</span><span class="sxs-lookup"><span data-stu-id="78e94-136">This article uses the following example concept of the 3D-point structure to explain these recommendations:</span></span>

```csharp
public struct Point3D
{
    public double X;
    public double Y;
    public double Z;
}
```

<span data-ttu-id="78e94-137">Různé příklady používají různé implementace tohoto konceptu.</span><span class="sxs-lookup"><span data-stu-id="78e94-137">Different examples use different implementations of this concept.</span></span>

## <a name="declare-readonly-structs-for-immutable-value-types"></a><span data-ttu-id="78e94-138">Deklarace struktur jen pro čtení pro neměnné hodnotových typech</span><span class="sxs-lookup"><span data-stu-id="78e94-138">Declare readonly structs for immutable value types</span></span>

<span data-ttu-id="78e94-139">Deklarace `struct` pomocí `readonly` modifikátor informuje kompilátor, že je máte v úmyslu vytvořit neumlčitelným typem.</span><span class="sxs-lookup"><span data-stu-id="78e94-139">Declaring a `struct` using the `readonly` modifier informs the compiler that your intent is to create an immutable type.</span></span> <span data-ttu-id="78e94-140">Kompilátor vynucuje tuto rozhodnutí o návrhu na základě následujících pravidel:</span><span class="sxs-lookup"><span data-stu-id="78e94-140">The compiler enforces that design decision with the following rules:</span></span>

- <span data-ttu-id="78e94-141">Musí být všechna pole členů `readonly`</span><span class="sxs-lookup"><span data-stu-id="78e94-141">All field members must be `readonly`</span></span>
- <span data-ttu-id="78e94-142">Všechny vlastnosti musí být jen pro čtení, včetně automaticky implementované vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="78e94-142">All properties must be read-only, including auto-implemented properties.</span></span>

<span data-ttu-id="78e94-143">Tyto dvě pravidla jsou dostatečné pro Ujistěte se, že žádný člen `readonly struct` změní stav této struktury.</span><span class="sxs-lookup"><span data-stu-id="78e94-143">These two rules are sufficient to ensure that no member of a `readonly struct` modifies the state of that struct.</span></span> <span data-ttu-id="78e94-144">`struct` Je neměnný.</span><span class="sxs-lookup"><span data-stu-id="78e94-144">The `struct` is immutable.</span></span> <span data-ttu-id="78e94-145">`Point3D` Strukturu můžete nadefinovat jako neměnné struktury, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="78e94-145">The `Point3D` structure could be defined as an immutable struct as shown in the following example:</span></span>

```csharp
readonly public struct ReadonlyPoint3D
{
    public ReadonlyPoint3D(double x, double y, double z)
    {
        this.X = x;
        this.Y = y;
        this.Z = z;
    }

    public double X { get; }
    public double Y { get; }
    public double Z { get; }
}
```

<span data-ttu-id="78e94-146">Pokaždé, když máte v úmyslu návrhu, je vytvoření neměnný hodnota typu, postupujte podle tohoto doporučení.</span><span class="sxs-lookup"><span data-stu-id="78e94-146">Follow this recommendation whenever your design intent is to create an immutable value type.</span></span> <span data-ttu-id="78e94-147">Žádná zlepšení výkonu jsou dodatečná výhoda.</span><span class="sxs-lookup"><span data-stu-id="78e94-147">Any performance improvements are an added benefit.</span></span> <span data-ttu-id="78e94-148">`readonly struct` Jasně vyjadřuje máte v úmyslu návrhu.</span><span class="sxs-lookup"><span data-stu-id="78e94-148">The `readonly struct` clearly expresses your design intent.</span></span>

## <a name="use-ref-readonly-return-statements-for-large-structures-when-possible"></a><span data-ttu-id="78e94-149">Použití `ref readonly return` příkazy pro velké struktury, pokud je to možné</span><span class="sxs-lookup"><span data-stu-id="78e94-149">Use `ref readonly return` statements for large structures when possible</span></span>

<span data-ttu-id="78e94-150">Pokud je vrácená hodnota není místní pro metodu vracející může vracet hodnoty podle odkazů.</span><span class="sxs-lookup"><span data-stu-id="78e94-150">You can return values by reference when the value being returned isn't local to the returning method.</span></span> <span data-ttu-id="78e94-151">Vrácení podle odkazu znamená, že je zkopírován pouze odkaz, není struktury.</span><span class="sxs-lookup"><span data-stu-id="78e94-151">Returning by reference means that only the reference is copied, not the structure.</span></span> <span data-ttu-id="78e94-152">V následujícím příkladu `Origin` vlastnost nelze použít `ref` vrátit, protože se vrací hodnotu místní proměnné:</span><span class="sxs-lookup"><span data-stu-id="78e94-152">In the following example, the `Origin` property can't use a `ref` return because the value being returned is a local variable:</span></span>

```csharp
public Point3D Origin => new Point3D(0,0,0);
```

<span data-ttu-id="78e94-153">Následující definici vlastnosti mohou však vrátit odkazem, protože vrácená hodnota je statický člen:</span><span class="sxs-lookup"><span data-stu-id="78e94-153">However, the following property definition can be returned by reference because the returned value is a static member:</span></span>

```csharp
public struct Point3D
{
    private static Point3D origin = new Point3D(0,0,0);

    // Dangerous! returning a mutable reference to internal storage
    public ref Point3D Origin => ref origin;

    // other members removed for space
}
```

<span data-ttu-id="78e94-154">Nechcete, aby volající úpravy počátek, takže by měl vrátit hodnotu `readonly ref`:</span><span class="sxs-lookup"><span data-stu-id="78e94-154">You don't want callers modifying the origin, so you should return the value by `readonly ref`:</span></span>

```csharp
public struct Point3D
{
    private static Point3D origin = new Point3D(0,0,0);

    public ref readonly Point3D Origin => ref origin;

    // other members removed for space
}
```

<span data-ttu-id="78e94-155">Vrací `ref readonly` umožňuje uložit kopírování větší struktury a zachovat neměnnosti interní datové členy.</span><span class="sxs-lookup"><span data-stu-id="78e94-155">Returning `ref readonly` enables you to save copying larger structures and preserve the immutability of your internal data members.</span></span>

<span data-ttu-id="78e94-156">V lokalitě volání volající výběru možnosti použít `Origin` vlastnosti jako datový typ `readonly ref` ani jako typ hodnoty:</span><span class="sxs-lookup"><span data-stu-id="78e94-156">At the call site, callers make the choice to use the `Origin` property as a `readonly ref` or as a value:</span></span>

[!code-csharp[AssignRefReadonly](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#AssignRefReadonly "Assigning a ref readonly")]

<span data-ttu-id="78e94-157">První přiřazení v předchozím kódu vytvoří kopii `Origin` konstantě a přiřadí, které kopírují.</span><span class="sxs-lookup"><span data-stu-id="78e94-157">The first assignment in the preceding code makes a copy of the `Origin` constant and assigns that copy.</span></span> <span data-ttu-id="78e94-158">Druhá přiřadí odkaz.</span><span class="sxs-lookup"><span data-stu-id="78e94-158">The second assigns a reference.</span></span> <span data-ttu-id="78e94-159">Všimněte si, že `readonly` modifikátor musí být součástí deklarace proměnné.</span><span class="sxs-lookup"><span data-stu-id="78e94-159">Notice that the `readonly` modifier must be part of the declaration of the variable.</span></span> <span data-ttu-id="78e94-160">Odkaz, na který odkazuje, nemůže být upraven.</span><span class="sxs-lookup"><span data-stu-id="78e94-160">The reference to which it refers can't be modified.</span></span> <span data-ttu-id="78e94-161">O to mít za následek chybu v době kompilace.</span><span class="sxs-lookup"><span data-stu-id="78e94-161">Attempts to do so result in a compile-time error.</span></span>

<span data-ttu-id="78e94-162">`readonly` Modifikátor se vyžaduje u deklarace `originReference`.</span><span class="sxs-lookup"><span data-stu-id="78e94-162">The `readonly` modifier is required on the declaration of `originReference`.</span></span>

<span data-ttu-id="78e94-163">Kompilátor vynucuje, volající nemůžete měnit odkaz.</span><span class="sxs-lookup"><span data-stu-id="78e94-163">The compiler enforces that the caller can't modify the reference.</span></span> <span data-ttu-id="78e94-164">Se pokusí přiřadit hodnotu přímo generovat chybu v době kompilace.</span><span class="sxs-lookup"><span data-stu-id="78e94-164">Attempts to assign the value directly generate a compile-time error.</span></span> <span data-ttu-id="78e94-165">Kompilátor však nemůže vědět, pokud libovolné metody člen změní stav struktury.</span><span class="sxs-lookup"><span data-stu-id="78e94-165">However, the compiler can't know if any member method modifies the state of the struct.</span></span>
<span data-ttu-id="78e94-166">Aby bylo zajištěno, že objekt není upraven, kompilátor vytvoří kopii a volá člen odkazů pomocí nástroje tuto kopii.</span><span class="sxs-lookup"><span data-stu-id="78e94-166">To ensure that the object isn't modified, the compiler creates a copy and calls member references using that copy.</span></span> <span data-ttu-id="78e94-167">Všechny změny se do tohoto obranná kopie.</span><span class="sxs-lookup"><span data-stu-id="78e94-167">Any modifications are to that defensive copy.</span></span>

## <a name="apply-the-in-modifier-to-readonly-struct-parameters-larger-than-systemintptrsize"></a><span data-ttu-id="78e94-168">Použít `in` modifikátor `readonly struct` parametry, které jsou větší než `System.IntPtr.Size`</span><span class="sxs-lookup"><span data-stu-id="78e94-168">Apply the `in` modifier to `readonly struct` parameters larger than `System.IntPtr.Size`</span></span>

<span data-ttu-id="78e94-169">`in` – Klíčové slovo doplňuje stávající `ref` a `out` klíčových slov pro předání argumentů odkazem.</span><span class="sxs-lookup"><span data-stu-id="78e94-169">The `in` keyword complements the existing `ref` and `out` keywords to pass arguments by reference.</span></span> <span data-ttu-id="78e94-170">`in` – Klíčové slovo určuje předání argumentu podle odkazu, ale volaná metoda nezmění hodnotu.</span><span class="sxs-lookup"><span data-stu-id="78e94-170">The `in` keyword specifies passing the argument by reference, but the called method doesn't modify the value.</span></span>

<span data-ttu-id="78e94-171">Toto přidání poskytuje úplné slovníku pro vyjádření záměr vašeho návrhu.</span><span class="sxs-lookup"><span data-stu-id="78e94-171">This addition provides a full vocabulary to express your design intent.</span></span>
<span data-ttu-id="78e94-172">Typy hodnot se zkopírují po uplynutí volané metody, pokud nezadáte žádné následující modifikátory v podpisu metody.</span><span class="sxs-lookup"><span data-stu-id="78e94-172">Value types are copied when passed to a called method when you don't specify any of the following modifiers in the method signature.</span></span> <span data-ttu-id="78e94-173">Každá z těchto modifikátorů Určuje, že proměnná je předána odkazem, jak se vyhnout kopírování.</span><span class="sxs-lookup"><span data-stu-id="78e94-173">Each of these modifiers specifies that a variable is passed by reference, avoiding the copy.</span></span> <span data-ttu-id="78e94-174">Každý modifikátor vyjadřuje různých záměr:</span><span class="sxs-lookup"><span data-stu-id="78e94-174">Each modifier expresses a different intent:</span></span>

- <span data-ttu-id="78e94-175">`out`: Tato metoda nastaví hodnotu argument použitý jako tento parametr.</span><span class="sxs-lookup"><span data-stu-id="78e94-175">`out`: This method sets the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="78e94-176">`ref`: Tato metoda může nastavit hodnotu argument použitý jako tento parametr.</span><span class="sxs-lookup"><span data-stu-id="78e94-176">`ref`: This method may set the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="78e94-177">`in`: Tato metoda nezmění hodnotu argument použitý jako tento parametr.</span><span class="sxs-lookup"><span data-stu-id="78e94-177">`in`: This method doesn't modify the value of the argument used as this parameter.</span></span>

<span data-ttu-id="78e94-178">Přidat `in` modifikátor předat argument odkazem a deklarovat vaším záměrem návrhu předání argumentů odkazem, aby předešel zbytečnému kopírování.</span><span class="sxs-lookup"><span data-stu-id="78e94-178">Add the `in` modifier to pass an argument by reference and declare your design intent to pass arguments by reference to avoid unnecessary copying.</span></span> <span data-ttu-id="78e94-179">Nemáte v úmyslu změnit objekt použitý jako tento argument.</span><span class="sxs-lookup"><span data-stu-id="78e94-179">You don't intend to modify the object used as that argument.</span></span>

<span data-ttu-id="78e94-180">Tento postup často zvyšuje výkon u typů hodnot jen pro čtení, které jsou větší než <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="78e94-180">This practice often improves performance for readonly value types that are larger than <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span></span> <span data-ttu-id="78e94-181">Pro jednoduché typy (`sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`, `char`, `float`, `double`, `decimal` a `bool`, a `enum` typy), jsou minimial všechny potenciální zvýšení výkonu.</span><span class="sxs-lookup"><span data-stu-id="78e94-181">For simple types (`sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`, `char`, `float`, `double`, `decimal` and `bool`, and `enum` types), any potential performance gains are minimial.</span></span> <span data-ttu-id="78e94-182">Ve skutečnosti, může se snížit výkon pomocí pass-by-reference pro typy menší než <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="78e94-182">In fact, performance may degrade by using pass-by-reference for types smaller than <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span></span>

<span data-ttu-id="78e94-183">Následující kód ukazuje příklad metodu, která vypočítá vzdálenost mezi dvěma body v 3D prostoru.</span><span class="sxs-lookup"><span data-stu-id="78e94-183">The following code shows an example of a method that calculates the distance between two points in 3D space.</span></span>

[!code-csharp[InArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgument "Specifying an in argument")]

<span data-ttu-id="78e94-184">Argumenty jsou dvě struktury, že každý obsahuje tří hodnot datového typu Double.</span><span class="sxs-lookup"><span data-stu-id="78e94-184">The arguments are two structures that each contain three doubles.</span></span> <span data-ttu-id="78e94-185">Double je 8 bajtů, takže každý argument je 24 bajtů.</span><span class="sxs-lookup"><span data-stu-id="78e94-185">A double is 8 bytes, so each argument is 24 bytes.</span></span> <span data-ttu-id="78e94-186">Zadáním `in` modifikátor, předáte 4 bajtů nebo 8 bajtů odkaz na tyto argumenty, v závislosti na architektuře počítače.</span><span class="sxs-lookup"><span data-stu-id="78e94-186">By specifying the `in` modifier, you pass a 4 byte or 8-byte reference to those arguments, depending on the architecture of the machine.</span></span> <span data-ttu-id="78e94-187">Rozdíl velikosti je malý, ale přidá, když vaše aplikace volá tuto metodu v těsné smyčce pomocí mnoha různých hodnot.</span><span class="sxs-lookup"><span data-stu-id="78e94-187">The difference in size is small, but it adds up when your application calls this method in a tight loop using many different values.</span></span>

<span data-ttu-id="78e94-188">`in` Modifikační komplementy `out` a `ref` i jinými způsoby.</span><span class="sxs-lookup"><span data-stu-id="78e94-188">The `in` modifier complements `out` and `ref` in other ways as well.</span></span> <span data-ttu-id="78e94-189">Nelze vytvořit přetížení metody, která se liší pouze v nichž se nachází z `in`, `out`, nebo `ref`.</span><span class="sxs-lookup"><span data-stu-id="78e94-189">You can't create overloads of a method that differ only in the presence of `in`, `out`, or `ref`.</span></span> <span data-ttu-id="78e94-190">Tato nová pravidla rozšíření stejné chování, která měla vždy definována pro `out` a `ref` parametry.</span><span class="sxs-lookup"><span data-stu-id="78e94-190">These new rules extend the same behavior that had always been defined for `out` and `ref` parameters.</span></span> <span data-ttu-id="78e94-191">Podobně jako `out` a `ref` modifikátory, typy hodnot nejsou zabalená, protože `in` modifikátor se použije.</span><span class="sxs-lookup"><span data-stu-id="78e94-191">Like the `out` and `ref` modifiers, value types aren't boxed because the `in` modifier is applied.</span></span>

<span data-ttu-id="78e94-192">`in` Modifikátor se můžou vztahovat k libovolnému členovi, která přebírá parametry: metody, delegáti, lambda výrazy, místní funkce, indexery, operátory.</span><span class="sxs-lookup"><span data-stu-id="78e94-192">The `in` modifier may be applied to any member that takes parameters: methods, delegates, lambdas, local functions, indexers, operators.</span></span>

<span data-ttu-id="78e94-193">Další funkce `in` parametrů je, že může používat literálními hodnotami nebo konstanty pro argument `in` parametru.</span><span class="sxs-lookup"><span data-stu-id="78e94-193">Another feature of `in` parameters is that you may use literal values or constants for the argument to an `in` parameter.</span></span> <span data-ttu-id="78e94-194">Navíc na rozdíl od `ref` nebo `out` parametr, není nutné použít `in` modifikátor na lokalitu volání.</span><span class="sxs-lookup"><span data-stu-id="78e94-194">Also, unlike a `ref` or `out` parameter, you don't need to apply the `in` modifier at the call site.</span></span> <span data-ttu-id="78e94-195">Následující kód ukazuje, dva příklady volání `CalculateDistance` metody.</span><span class="sxs-lookup"><span data-stu-id="78e94-195">The following code shows you two examples of calling the `CalculateDistance` method.</span></span> <span data-ttu-id="78e94-196">První způsob využívá dvě místní proměnné, které jsou předány podle odkazu.</span><span class="sxs-lookup"><span data-stu-id="78e94-196">The first uses two local variables passed by reference.</span></span> <span data-ttu-id="78e94-197">Druhá zahrnuje dočasnou proměnnou vytvořených jako součást volání metody.</span><span class="sxs-lookup"><span data-stu-id="78e94-197">The second includes a temporary variable created as part of the method call.</span></span>

[!code-csharp[UseInArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#UseInArgument "Specifying an In argument")]

<span data-ttu-id="78e94-198">Existuje několik způsobů, ve kterých kompilátor vynucuje povahy jen pro čtení `in` argument.</span><span class="sxs-lookup"><span data-stu-id="78e94-198">There are several ways in which the compiler enforces the read-only nature of an `in` argument.</span></span>  <span data-ttu-id="78e94-199">Za prvé, volané metody nejde přiřadit přímo `in` parametru.</span><span class="sxs-lookup"><span data-stu-id="78e94-199">First of all, the called method can't directly assign to an `in` parameter.</span></span> <span data-ttu-id="78e94-200">Nelze přiřadit přímo z některého z polí `in` parametr, když je tato hodnota `struct` typu.</span><span class="sxs-lookup"><span data-stu-id="78e94-200">It can't directly assign to any field of an `in` parameter when that value is a `struct` type.</span></span> <span data-ttu-id="78e94-201">Kromě toho nelze předat `in` parametr pomocí libovolné metody `ref` nebo `out` modifikátor.</span><span class="sxs-lookup"><span data-stu-id="78e94-201">In addition, you can't pass an `in` parameter to any method using the `ref` or `out` modifier.</span></span>
<span data-ttu-id="78e94-202">Tato pravidla platí pro všechny oblasti `in` pole je zadán parametr, `struct` typ a parametrem je také `struct` typu.</span><span class="sxs-lookup"><span data-stu-id="78e94-202">These rules apply to any field of an `in` parameter, provided the field is a `struct` type and the parameter is also a `struct` type.</span></span> <span data-ttu-id="78e94-203">Ve skutečnosti, tato pravidla platí pro několik úrovní přístupu ke členu poskytuje, jsou tyto typy na všech úrovních přístupu ke členu `structs`.</span><span class="sxs-lookup"><span data-stu-id="78e94-203">In fact, these rules apply for multiple layers of member access provided the types at all levels of member access are `structs`.</span></span>
<span data-ttu-id="78e94-204">Vynucuje kompilátor `struct` typů předané jako `in` argumenty a jejich `struct` členy jsou jen pro čtení proměnné, když se použije jako argumentů jiným metodám.</span><span class="sxs-lookup"><span data-stu-id="78e94-204">The compiler enforces that `struct` types passed as  `in` arguments and their `struct` members are read-only variables when used as arguments to other methods.</span></span>

<span data-ttu-id="78e94-205">Použití `in` parametry se můžete vyhnout potenciální výkon náklady na vytváření kopií.</span><span class="sxs-lookup"><span data-stu-id="78e94-205">The use of `in` parameters can avoid the potential performance costs of making copies.</span></span> <span data-ttu-id="78e94-206">Sémantika jakékoli volání metody nezmění.</span><span class="sxs-lookup"><span data-stu-id="78e94-206">It doesn't change the semantics of any method call.</span></span> <span data-ttu-id="78e94-207">Proto není nutné určit `in` modifikátor na lokalitu volání.</span><span class="sxs-lookup"><span data-stu-id="78e94-207">Therefore, you don't need to specify the `in` modifier at the call site.</span></span> <span data-ttu-id="78e94-208">Vynechání `in` modifikátor na lokalitu volání informuje kompilátor, že se může vytvořit kopii argumentu z následujících důvodů:</span><span class="sxs-lookup"><span data-stu-id="78e94-208">Omitting the `in` modifier at the call site informs the compiler that it's allowed to make a copy of the argument for the following reasons:</span></span>

- <span data-ttu-id="78e94-209">Existuje implicitní převod, ale není identity konverzi z typu argumentu na typ parametru.</span><span class="sxs-lookup"><span data-stu-id="78e94-209">There exists an implicit conversion but not an identity conversion from the argument type to the parameter type.</span></span>
- <span data-ttu-id="78e94-210">Argument je výraz, ale není zaškrtnuta možnost známé úložiště proměnné.</span><span class="sxs-lookup"><span data-stu-id="78e94-210">The argument is an expression but doesn't have a known storage variable.</span></span>
- <span data-ttu-id="78e94-211">Existuje přetížení, která se liší podle přítomnosti nebo nepřítomnosti `in`.</span><span class="sxs-lookup"><span data-stu-id="78e94-211">An overload exists that differs by the presence or absence of `in`.</span></span> <span data-ttu-id="78e94-212">V takovém případě hodnota přetížení představuje lepší shodu.</span><span class="sxs-lookup"><span data-stu-id="78e94-212">In that case, the by value overload is a better match.</span></span>

<span data-ttu-id="78e94-213">Tato pravidla jsou užitečné, když aktualizujete existující kód, jak používat argumenty odkazu pouze pro čtení.</span><span class="sxs-lookup"><span data-stu-id="78e94-213">These rules are useful as you update existing code to use read-only reference arguments.</span></span> <span data-ttu-id="78e94-214">Uvnitř volané metody lze volat jakékoli metody instance, která používá parametry s hodnotou.</span><span class="sxs-lookup"><span data-stu-id="78e94-214">Inside the called method, you can call any instance method that uses by value parameters.</span></span> <span data-ttu-id="78e94-215">V těchto případech kopii `in` parametr se vytvoří.</span><span class="sxs-lookup"><span data-stu-id="78e94-215">In those instances, a copy of the `in` parameter is created.</span></span> <span data-ttu-id="78e94-216">Protože kompilátor může vytvořit dočasnou proměnnou pro všechny `in` parametr, můžete také určit výchozí hodnoty pro všechny `in` parametru.</span><span class="sxs-lookup"><span data-stu-id="78e94-216">Because the compiler may create a temporary variable for any `in` parameter, you can also specify default values for any `in` parameter.</span></span> <span data-ttu-id="78e94-217">Následující kód určuje jako výchozí hodnotu pro druhý bod počátku (bod 0; 0):</span><span class="sxs-lookup"><span data-stu-id="78e94-217">The following code specifies the origin (point 0,0) as the default value for the second point:</span></span>

[!code-csharp[InArgumentDefault](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgumentDefault "Specifying defaults for an in parameter")]

<span data-ttu-id="78e94-218">Chcete-li vynutit, aby kompilátor mohl předat argumenty jen pro čtení pomocí odkazu, zadejte `in` modifikátor argumenty při volání webu, jak je znázorněno v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="78e94-218">To force the compiler to pass read-only arguments by reference, specify the `in` modifier on the arguments at the call site, as shown in the following code:</span></span>

[!code-csharp[UseInArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#ExplicitInArgument "Specifying an In argument")]

<span data-ttu-id="78e94-219">Toto chování je snazší přijmout `in` parametry v čase ve velkých základů kódu, kde je možné zvýšení výkonu.</span><span class="sxs-lookup"><span data-stu-id="78e94-219">This behavior makes it easier to adopt `in` parameters over time in large codebases where performance gains are possible.</span></span> <span data-ttu-id="78e94-220">Přidáte `in` modifikátor podpisy metod první.</span><span class="sxs-lookup"><span data-stu-id="78e94-220">You add the `in` modifier to method signatures first.</span></span> <span data-ttu-id="78e94-221">Potom můžete přidat `in` modifikátor na volání lokalit a vytvoření `readonly struct` typy umožňující vyhnout se vytváření obranná kopie kompilátor `in` parametry na více místech.</span><span class="sxs-lookup"><span data-stu-id="78e94-221">Then, you can add the `in` modifier at call sites and create `readonly struct` types to enable the compiler to avoid creating defensive copies of `in` parameters in more locations.</span></span>

<span data-ttu-id="78e94-222">`in` Parametr označení lze také s typy odkazů nebo číselné hodnoty.</span><span class="sxs-lookup"><span data-stu-id="78e94-222">The `in` parameter designation can also be used with reference types or numeric values.</span></span> <span data-ttu-id="78e94-223">Ale v obou případech je minimální, pokud existuje.</span><span class="sxs-lookup"><span data-stu-id="78e94-223">However, the benefits in both cases are minimal, if any.</span></span>

## <a name="never-use-mutable-structs-as-in-in-argument"></a><span data-ttu-id="78e94-224">Nikdy nepoužívejte proměnlivé struktury jako v `in` argument</span><span class="sxs-lookup"><span data-stu-id="78e94-224">Never use mutable structs as in `in` argument</span></span>

<span data-ttu-id="78e94-225">Technik popsaných výše vysvětlují, jak se vyhnout kopie vrací odkazy a předáním hodnoty podle odkazu.</span><span class="sxs-lookup"><span data-stu-id="78e94-225">The techniques described above explain how to avoid copies by returning references and passing values by reference.</span></span> <span data-ttu-id="78e94-226">Tyto techniky fungují lépe, když typy argumentů jsou deklarovány jako `readonly struct` typy.</span><span class="sxs-lookup"><span data-stu-id="78e94-226">These techniques work best when the argument types are declared as `readonly struct` types.</span></span> <span data-ttu-id="78e94-227">V opačném případě musí kompilátor vytvořit **obranná kopie** v mnoha situacích k vynucení jen pro čtení ness žádné argumenty.</span><span class="sxs-lookup"><span data-stu-id="78e94-227">Otherwise, the compiler must create **defensive copies** in many situations to enforce the readonly-ness of any arguments.</span></span> <span data-ttu-id="78e94-228">Zvažte následující příklad, který vypočítá vzdálenost 3D bodu ze zdroje:</span><span class="sxs-lookup"><span data-stu-id="78e94-228">Consider the following example that calculates the distance of a 3D point from the origin:</span></span>

[!code-csharp[InArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgument "Specifying an in argument")]

<span data-ttu-id="78e94-229">`Point3D` Struktura je *není* struktury jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="78e94-229">The `Point3D` structure is *not* a readonly struct.</span></span> <span data-ttu-id="78e94-230">Existuje šest hovorů přístup jiné vlastnosti v textu této metody.</span><span class="sxs-lookup"><span data-stu-id="78e94-230">There are six different property access calls in the body of this method.</span></span> <span data-ttu-id="78e94-231">Na první zkoumání budete možná jste si mysleli, že tyto přístupy byly bezpečné.</span><span class="sxs-lookup"><span data-stu-id="78e94-231">On first examination, you may have thought these accesses were safe.</span></span> <span data-ttu-id="78e94-232">Po všech `get` přistupující objekt neměli měnit stav objektu.</span><span class="sxs-lookup"><span data-stu-id="78e94-232">After all, a `get` accessor shouldn't modify the state of the object.</span></span> <span data-ttu-id="78e94-233">Ale neexistuje žádné pravidlo jazyka, které, který vynucuje.</span><span class="sxs-lookup"><span data-stu-id="78e94-233">But there's no language rule that enforces that.</span></span> <span data-ttu-id="78e94-234">Je pouze běžné konvence.</span><span class="sxs-lookup"><span data-stu-id="78e94-234">It's only a common convention.</span></span> <span data-ttu-id="78e94-235">Může implementovat libovolný typ `get` přístupový objekt, který změnit vnitřní stav.</span><span class="sxs-lookup"><span data-stu-id="78e94-235">Any type could implement a `get` accessor that modified the internal state.</span></span> <span data-ttu-id="78e94-236">Bez některé záruku jazyka musí kompilátor vytvoření dočasné kopie argumentu před voláním jakékoli členské.</span><span class="sxs-lookup"><span data-stu-id="78e94-236">Without some language guarantee, the compiler must create a temporary copy of the argument before calling any member.</span></span> <span data-ttu-id="78e94-237">Dočasné úložiště se vytvoří v zásobníku, jsou zkopírovány hodnoty argumentu do dočasného úložiště a zkopírování hodnoty do zásobníku pro každý přístup ke členu jako `this` argument.</span><span class="sxs-lookup"><span data-stu-id="78e94-237">The temporary storage is created on the stack, the values of the argument are copied to the temporary storage, and the value is copied to the stack for each member access as the `this` argument.</span></span> <span data-ttu-id="78e94-238">V mnoha situacích, tyto kopie poškodit výkonu dostatečně této pass podle hodnot je rychlejší než pass podle jen pro čtení odkazu není typ argumentu `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="78e94-238">In many situations, these copies harm performance enough that pass-by-value is faster than pass-by-readonly-reference when the argument type isn't a `readonly struct`.</span></span>

<span data-ttu-id="78e94-239">Místo toho, pokud výpočet vzdálenost používá neměnné struktury `ReadonlyPoint3D`, dočasné objekty nejsou potřeba:</span><span class="sxs-lookup"><span data-stu-id="78e94-239">Instead, if the distance calculation uses the immutable struct, `ReadonlyPoint3D`, temporary objects are not needed:</span></span>

[!code-csharp[readonlyInArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#ReadOnlyInArgument "Specifying a readonly in argument")]

<span data-ttu-id="78e94-240">Kompilátor generuje kód efektivnější při volání členy `readonly struct`: `this` odkaz, namísto kopírování příjemce, je vždy `in` parametr předaný odkazem na metodu member.</span><span class="sxs-lookup"><span data-stu-id="78e94-240">The compiler generates more efficient code when you call members of a `readonly struct`: The `this` reference, instead of a copy of the receiver, is always an `in` parameter passed by reference to the member method.</span></span> <span data-ttu-id="78e94-241">Tato optimalizace uloží kopírování při použití `readonly struct` jako `in` argument.</span><span class="sxs-lookup"><span data-stu-id="78e94-241">This optimization saves copying when you use a `readonly struct` as an `in` argument.</span></span>

<span data-ttu-id="78e94-242">Zobrazí se ukázkový program, který ukazuje, rozdíly ve výkonu pomocí [Benchmark.net](https://www.nuget.org/packages/BenchmarkDotNet/) v našich [úložiště ukázek](https://github.com/dotnet/samples/tree/master/csharp/safe-efficient-code/benchmark) na Githubu.</span><span class="sxs-lookup"><span data-stu-id="78e94-242">You can see an example program that demonstrates the performance differences using [Benchmark.net](https://www.nuget.org/packages/BenchmarkDotNet/) in our [samples repository](https://github.com/dotnet/samples/tree/master/csharp/safe-efficient-code/benchmark) on GitHub.</span></span> <span data-ttu-id="78e94-243">Porovná předáním proměnlivé struktury podle hodnoty a podle reference s předáním neměnné struktury podle hodnoty a podle reference.</span><span class="sxs-lookup"><span data-stu-id="78e94-243">It compares passing a mutable struct by value and by reference with passing an immutable struct by value and by reference.</span></span> <span data-ttu-id="78e94-244">Je nejrychlejším způsobem použití neměnné struktury a předání odkazem.</span><span class="sxs-lookup"><span data-stu-id="78e94-244">The use of the immutable struct and pass by reference is fastest.</span></span>

## <a name="use-ref-struct-types-to-work-with-blocks-or-memory-on-a-single-stack-frame"></a><span data-ttu-id="78e94-245">Použití `ref struct` typy pro práci s bloky nebo paměti na jeden zásobníku</span><span class="sxs-lookup"><span data-stu-id="78e94-245">Use `ref struct` types to work with blocks or memory on a single stack frame</span></span>

<span data-ttu-id="78e94-246">Funkce související jazyk je možnost deklarovat hodnotový typ, který musí být omezen na blok zásobníku jeden.</span><span class="sxs-lookup"><span data-stu-id="78e94-246">A related language feature is the ability to declare a value type that must be constrained to a single stack frame.</span></span> <span data-ttu-id="78e94-247">Toto omezení umožňuje kompilátoru provést několik optimalizací.</span><span class="sxs-lookup"><span data-stu-id="78e94-247">This restriction enables the compiler to make several optimizations.</span></span> <span data-ttu-id="78e94-248">Byl primární motivace pro tuto funkci <xref:System.Span%601> a související struktury.</span><span class="sxs-lookup"><span data-stu-id="78e94-248">The primary motivation for this feature was <xref:System.Span%601> and related structures.</span></span> <span data-ttu-id="78e94-249">Vylepšení výkonu z těchto vylepšení dosáhnete pomocí nové a aktualizované rozhraní API pro .NET, které usnadňují používání <xref:System.Span%601> typu.</span><span class="sxs-lookup"><span data-stu-id="78e94-249">You'll achieve performance improvements from these enhancements by using new and updated .NET APIs that make use of the <xref:System.Span%601> type.</span></span>

<span data-ttu-id="78e94-250">Máte podobné požadavky na práci s pamětí, které jsou vytvořené pomocí [ `stackalloc` ](language-reference/keywords/stackalloc.md) nebo při použití paměti z vzájemné spolupráce rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="78e94-250">You may have similar requirements working with memory created using [`stackalloc`](language-reference/keywords/stackalloc.md) or when using memory from interop APIs.</span></span> <span data-ttu-id="78e94-251">Můžete definovat vlastní `ref struct` typy pro tyto potřeby.</span><span class="sxs-lookup"><span data-stu-id="78e94-251">You can define your own `ref struct` types for those needs.</span></span>

## <a name="readonly-ref-struct-type"></a><span data-ttu-id="78e94-252">`readonly ref struct` Typ</span><span class="sxs-lookup"><span data-stu-id="78e94-252">`readonly ref struct` type</span></span>

<span data-ttu-id="78e94-253">Deklarace struktury jako `readonly ref` kombinuje výhody a omezení `ref struct` a `readonly struct` deklarace.</span><span class="sxs-lookup"><span data-stu-id="78e94-253">Declaring a struct as `readonly ref` combines the benefits and restrictions of `ref struct` and `readonly struct` declarations.</span></span> <span data-ttu-id="78e94-254">Paměť používanou rozpětí jen pro čtení je omezen na blok zásobníku jednoho a paměť používanou rozpětí jen pro čtení nelze upravit.</span><span class="sxs-lookup"><span data-stu-id="78e94-254">The memory used by the readonly span is restricted to a single stack frame, and the memory used by the readonly span can't be modified.</span></span>

## <a name="conclusions"></a><span data-ttu-id="78e94-255">Závěry</span><span class="sxs-lookup"><span data-stu-id="78e94-255">Conclusions</span></span>

<span data-ttu-id="78e94-256">Pomocí typů hodnot minimalizuje počet přidělení operací:</span><span class="sxs-lookup"><span data-stu-id="78e94-256">Using value types minimizes the number of allocation operations:</span></span>

- <span data-ttu-id="78e94-257">Úložiště pro typy hodnot je přiděleny pro místní proměnné a argumenty metody.</span><span class="sxs-lookup"><span data-stu-id="78e94-257">Storage for value types is stack allocated for local variables and method arguments.</span></span>
- <span data-ttu-id="78e94-258">Jako součást tohoto objektu, nikoli jako samostatné přidělení je přiděleno úložiště pro typy hodnot, které jsou členy jiné objekty.</span><span class="sxs-lookup"><span data-stu-id="78e94-258">Storage for value types that are members of other objects is allocated as part of that object, not as a separate allocation.</span></span>
- <span data-ttu-id="78e94-259">Úložiště pro typ hodnoty vrácené hodnoty je přiděleny.</span><span class="sxs-lookup"><span data-stu-id="78e94-259">Storage for value type return values is stack allocated.</span></span>

<span data-ttu-id="78e94-260">Oproti, typy odkazů v těchto stejných situacích:</span><span class="sxs-lookup"><span data-stu-id="78e94-260">Contrast that with reference types in those same situations:</span></span>

- <span data-ttu-id="78e94-261">Úložiště pro typy odkazů jsou haldy přidělené pro místní proměnné a argumenty metody.</span><span class="sxs-lookup"><span data-stu-id="78e94-261">Storage for reference types are heap allocated for local variables and method arguments.</span></span> <span data-ttu-id="78e94-262">Odkaz je uložen v zásobníku.</span><span class="sxs-lookup"><span data-stu-id="78e94-262">The reference is stored on the stack.</span></span>
- <span data-ttu-id="78e94-263">Úložiště pro typy odkazů, které jsou členy jiné objekty samostatně přidělují na haldě.</span><span class="sxs-lookup"><span data-stu-id="78e94-263">Storage for reference types that are members of other objects are separately allocated on the heap.</span></span> <span data-ttu-id="78e94-264">Objekt obsahující uchovává odkaz.</span><span class="sxs-lookup"><span data-stu-id="78e94-264">The containing object stores the reference.</span></span>
- <span data-ttu-id="78e94-265">Úložiště pro typ hodnoty vrácené hodnoty je haldy přidělené.</span><span class="sxs-lookup"><span data-stu-id="78e94-265">Storage for value type return values is heap allocated.</span></span> <span data-ttu-id="78e94-266">Odkaz na toto úložiště je uložen v zásobníku.</span><span class="sxs-lookup"><span data-stu-id="78e94-266">The reference to that storage is stored on the stack.</span></span>

<span data-ttu-id="78e94-267">Minimalizace přidělení se dodává s kompromisy.</span><span class="sxs-lookup"><span data-stu-id="78e94-267">Minimizing allocations comes with tradeoffs.</span></span> <span data-ttu-id="78e94-268">Kopírování větší množství paměti při velikost `struct` je větší než velikost odkaz.</span><span class="sxs-lookup"><span data-stu-id="78e94-268">You copy more memory when the size of the `struct` is larger than the size of a reference.</span></span> <span data-ttu-id="78e94-269">Odkaz je obvykle 64 bitů nebo 32 bitů a závisí na procesoru cílového počítače.</span><span class="sxs-lookup"><span data-stu-id="78e94-269">A reference is typically 64 bits or 32 bits, and depends on the target machine CPU.</span></span>

<span data-ttu-id="78e94-270">Tyto poměry obecně mít minimální vliv zatížení vliv.</span><span class="sxs-lookup"><span data-stu-id="78e94-270">These tradeoffs generally have minimal performance impact.</span></span> <span data-ttu-id="78e94-271">Pro velké struktury nebo větší kolekce, zvyšuje dopad na výkon.</span><span class="sxs-lookup"><span data-stu-id="78e94-271">However, for large structs or larger collections, the performance impact increases.</span></span> <span data-ttu-id="78e94-272">Dopad mohou být velké v těsné smyčky a horké cesty pro programy.</span><span class="sxs-lookup"><span data-stu-id="78e94-272">The impact can be large in tight loops and hot paths for programs.</span></span>

<span data-ttu-id="78e94-273">Těchto vylepšení C# jazyka jsou navrženy pro algoritmů výkon kritických-li minimalizovat přidělení paměti je hlavním faktorem při dosažení nezbytné výkonu.</span><span class="sxs-lookup"><span data-stu-id="78e94-273">These enhancements to the C# language are designed for performance critical algorithms where minimizing memory allocations is a major factor in achieving the necessary performance.</span></span> <span data-ttu-id="78e94-274">Může se stát, že není často používají tyto funkce v kódu, který píšete.</span><span class="sxs-lookup"><span data-stu-id="78e94-274">You may find that you don't often use these features in the code you write.</span></span> <span data-ttu-id="78e94-275">Nicméně tato vylepšení byla přijata v celém rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="78e94-275">However, these enhancements have been adopted throughout .NET.</span></span> <span data-ttu-id="78e94-276">Provádění více rozhraní API z těchto funkcí používat, zobrazí se výkon vašich aplikací, zlepšit.</span><span class="sxs-lookup"><span data-stu-id="78e94-276">As more and more APIs make use of these features, you'll see the performance of your applications improve.</span></span>
