---
title: CS1940 chyby kompilátoru
ms.date: 07/20/2015
f1_keywords:
- CS1940
helpviewer_keywords:
- CS1940
ms.assetid: 546e9bba-725d-4ea9-826f-37ec9d832add
ms.openlocfilehash: 1a6d7bc54162c63fbe81c7a5f87d52050ebef3b0
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 05/04/2018
ms.locfileid: "33314239"
---
# <a name="compiler-error-cs1940"></a><span data-ttu-id="c3de6-102">CS1940 chyby kompilátoru</span><span class="sxs-lookup"><span data-stu-id="c3de6-102">Compiler Error CS1940</span></span>
<span data-ttu-id="c3de6-103">Více implementace vzoru dotazu nebyly nalezeny pro zdrojový typ "typ".</span><span class="sxs-lookup"><span data-stu-id="c3de6-103">Multiple implementations of the query pattern were found for source type 'type'.</span></span> <span data-ttu-id="c3de6-104">Nejednoznačné volání "metody".</span><span class="sxs-lookup"><span data-stu-id="c3de6-104">Ambiguous call to 'method'.</span></span>  
  
 <span data-ttu-id="c3de6-105">Tato chyba je generována, když jsou definovány více implementace metody dotazu a kompilátor nelze odstranit nejednoznačnost, které z nich je nejvhodnější použít pro dotaz.</span><span class="sxs-lookup"><span data-stu-id="c3de6-105">This error is generated when multiple implementations of a query method are defined and the compiler cannot disambiguate which one is best to use for the query.</span></span> <span data-ttu-id="c3de6-106">V následujícím příkladu, obě verze `Select` mít stejným podpisem, protože obě přijmout jeden `int` jako vstupní parametr a mít `int` jako návratová hodnota.</span><span class="sxs-lookup"><span data-stu-id="c3de6-106">In the following example, both versions of `Select` have the same signature, because they both accept one `int` as an input parameter and have `int` as a return value.</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="c3de6-107">Oprava této chyby</span><span class="sxs-lookup"><span data-stu-id="c3de6-107">To correct this error</span></span>  
  
1.  <span data-ttu-id="c3de6-108">Zadejte pouze jeden implementace pro každou metodu.</span><span class="sxs-lookup"><span data-stu-id="c3de6-108">Provide only one implementation for each method.</span></span>  
  
## <a name="example"></a><span data-ttu-id="c3de6-109">Příklad</span><span class="sxs-lookup"><span data-stu-id="c3de6-109">Example</span></span>  
 <span data-ttu-id="c3de6-110">Následující kód generuje CS1940:</span><span class="sxs-lookup"><span data-stu-id="c3de6-110">The following code generates CS1940:</span></span>  
  
```csharp  
// cs1940.cs  
using System; //must include explicitly for types defined in 3.5  
class Test  
{  
    public delegate int Dele(int x);  
    int num = 0;  
    public int Select(Func<int, int> d)  
    {  
        return d(this.num);  
    }  
    public int Select(Dele d) // CS1940  
    {  
        return d(this.num) + 1;  
    }  
    public static void Main()  
    {  
        var q = from x in new Test()  
        select x;  
    }  
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="c3de6-111">Viz také</span><span class="sxs-lookup"><span data-stu-id="c3de6-111">See Also</span></span>  
 [<span data-ttu-id="c3de6-112">Přehled standardních operátorů dotazu</span><span class="sxs-lookup"><span data-stu-id="c3de6-112">Standard Query Operators Overview</span></span>](http://msdn.microsoft.com/library/24cda21e-8af8-4632-b519-c404a839b9b2)
