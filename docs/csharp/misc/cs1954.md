---
title: Chyba kompilátoru CS1954
ms.date: 07/20/2015
f1_keywords:
- CS1954
helpviewer_keywords:
- CS1954
ms.assetid: bdec399e-c43d-46d3-a01b-ef3572786fe5
ms.openlocfilehash: 3fd367560e4e7e0834f12d8ce96ef114b106be6f
ms.sourcegitcommit: 986f836f72ef10876878bd6217174e41464c145a
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/19/2019
ms.locfileid: "69598307"
---
# <a name="compiler-error-cs1954"></a><span data-ttu-id="e5dc8-102">Chyba kompilátoru CS1954</span><span class="sxs-lookup"><span data-stu-id="e5dc8-102">Compiler Error CS1954</span></span>
<span data-ttu-id="e5dc8-103">Nejlepší přetížená metoda se shoduje s metodou "Method" pro element inicializátoru kolekce nelze použít.</span><span class="sxs-lookup"><span data-stu-id="e5dc8-103">The best overloaded method match 'method' for the collection initializer element cannot be used.</span></span> <span data-ttu-id="e5dc8-104">Metody Add inicializátoru kolekce nemůžou mít parametry ref nebo out.</span><span class="sxs-lookup"><span data-stu-id="e5dc8-104">Collection initializer 'Add' methods cannot have ref or out parameters.</span></span>  
  
 <span data-ttu-id="e5dc8-105">Pro třídu kolekce, která má být inicializována pomocí inicializátoru kolekce, musí mít třída veřejnou `Add` metodu, která není `ref` ani `out` parametrem.</span><span class="sxs-lookup"><span data-stu-id="e5dc8-105">For a collection class to be initialized by using a collection initializer, the class must have a public `Add` method that is not a `ref` or `out` parameter.</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="e5dc8-106">Oprava této chyby</span><span class="sxs-lookup"><span data-stu-id="e5dc8-106">To correct this error</span></span>  
  
- <span data-ttu-id="e5dc8-107">Pokud lze upravit zdrojový kód třídy kolekce, zadejte `Add` metodu, která `ref` nepřijímá parametr nebo `out` .</span><span class="sxs-lookup"><span data-stu-id="e5dc8-107">If you can modify the source code of the collection class, provide an `Add` method that does not take a `ref` or `out` parameter.</span></span>  
  
- <span data-ttu-id="e5dc8-108">Pokud třídu kolekce nelze upravovat, inicializujte ji pomocí konstruktorů, které poskytuje.</span><span class="sxs-lookup"><span data-stu-id="e5dc8-108">If you cannot modify the collection class, initialize it with the constructors it provides.</span></span> <span data-ttu-id="e5dc8-109">S ním nelze použít inicializátor kolekce.</span><span class="sxs-lookup"><span data-stu-id="e5dc8-109">You cannot use a collection initializer with it.</span></span>  
  
## <a name="example"></a><span data-ttu-id="e5dc8-110">Příklad</span><span class="sxs-lookup"><span data-stu-id="e5dc8-110">Example</span></span>  
 <span data-ttu-id="e5dc8-111">Následující příklad vytvoří CS1954, protože jediné dostupné přetížení `Add` seznamu v `MyList` má `ref` parametr.</span><span class="sxs-lookup"><span data-stu-id="e5dc8-111">The following example produces CS1954 because the only available overload of the `Add` list in `MyList` has a `ref` parameter.</span></span>  
  
```csharp  
// cs1954.cs  
using System.Collections.Generic;  
class MyList<T> : IEnumerable<T>  
{  
    List<T> _list;  
    public void Add(ref T item)  
    {  
        _list.Add(item);  
    }  
  
    public System.Collections.Generic.IEnumerator<T> GetEnumerator()  
    {  
        int index = 0;  
        T current = _list[index];  
        while (current != null)  
        {  
            yield return _list[index++];  
        }  
    }  
  
    System.Collections.IEnumerator System.Collections.IEnumerable.GetEnumerator()  
    {  
        return GetEnumerator();  
    }  
}  
  
public class MyClass  
{  
    public string tree { get; set; }  
}  
class Program  
{  
    static void Main(string[] args)  
    {  
        MyList<MyClass> myList = new MyList<MyClass> { new MyClass { tree = "maple" } }; // CS1954  
    }  
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="e5dc8-112">Viz také:</span><span class="sxs-lookup"><span data-stu-id="e5dc8-112">See also</span></span>

- [<span data-ttu-id="e5dc8-113">Inicializátory objektu a kolekce</span><span class="sxs-lookup"><span data-stu-id="e5dc8-113">Object and Collection Initializers</span></span>](../programming-guide/classes-and-structs/object-and-collection-initializers.md)
