---
title: Chyba kompilátoru CS1657
ms.date: 07/20/2015
f1_keywords:
- CS1657
helpviewer_keywords:
- CS1657
ms.assetid: 6f0aeebe-5c90-4d5b-981c-1795d2e8fbb9
ms.openlocfilehash: bc1b324603089865a057d60a9fac751b4acb44e1
ms.sourcegitcommit: 986f836f72ef10876878bd6217174e41464c145a
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/19/2019
ms.locfileid: "69610831"
---
# <a name="compiler-error-cs1657"></a><span data-ttu-id="c8512-102">Chyba kompilátoru CS1657</span><span class="sxs-lookup"><span data-stu-id="c8512-102">Compiler Error CS1657</span></span>
<span data-ttu-id="c8512-103">Parametr ' Parameter ' nelze předat jako argument ref nebo out, protože ' důvod ' '</span><span class="sxs-lookup"><span data-stu-id="c8512-103">Cannot pass 'parameter' as a ref or out argument because 'reason''</span></span>  
  
 <span data-ttu-id="c8512-104">K této chybě dojde, pokud je proměnná předána jako argument [ref](../language-reference/keywords/ref.md) nebo [out](../language-reference/keywords/out-parameter-modifier.md) v kontextu, ve kterém je tato proměnná jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="c8512-104">This error occurs when a variable is passed as a [ref](../language-reference/keywords/ref.md) or [out](../language-reference/keywords/out-parameter-modifier.md) argument in a context in which that variable is readonly.</span></span> <span data-ttu-id="c8512-105">Kontexty ReadOnly obsahují proměnné iterace [foreach](../language-reference/keywords/foreach-in.md) , [pomocí](../language-reference/keywords/using-statement.md) proměnných a `fixed` proměnných.</span><span class="sxs-lookup"><span data-stu-id="c8512-105">Readonly contexts include [foreach](../language-reference/keywords/foreach-in.md) iteration variables, [using](../language-reference/keywords/using-statement.md) variables, and `fixed` variables.</span></span> <span data-ttu-id="c8512-106">Chcete-li tuto chybu vyřešit, Nevolejte funkce, které `foreach`přebírají `fixed` , `using` nebo jako `ref` parametr `out` nebo v `using` blocích, `foreach` příkazech a `fixed` učiněn.</span><span class="sxs-lookup"><span data-stu-id="c8512-106">To resolve this error, do not call functions that take the `foreach`, `using` or `fixed` variable as a `ref` or `out` parameter in `using` blocks, `foreach` statements, and `fixed` statements.</span></span>  
  
## <a name="example"></a><span data-ttu-id="c8512-107">Příklad</span><span class="sxs-lookup"><span data-stu-id="c8512-107">Example</span></span>  
 <span data-ttu-id="c8512-108">Následující ukázka generuje CS1657:</span><span class="sxs-lookup"><span data-stu-id="c8512-108">The following sample generates CS1657:</span></span>  
  
```csharp  
// CS1657.cs  
using System;  
class C : IDisposable  
{  
    public int i;  
    public void Dispose() {}  
}  
  
class CMain  
{  
    static void f(ref C c)  
    {  
    }  
    static void Main()  
    {  
        using (C c = new C())  
        {  
            f(ref c);  // CS1657  
        }  
    }  
}  
```  
  
## <a name="example"></a><span data-ttu-id="c8512-109">Příklad</span><span class="sxs-lookup"><span data-stu-id="c8512-109">Example</span></span>  
 <span data-ttu-id="c8512-110">Následující kód ilustruje stejný problém v `fixed` příkazu:</span><span class="sxs-lookup"><span data-stu-id="c8512-110">The following code illustrates the same problem in a `fixed` statement:</span></span>  
  
```csharp  
// CS1657b.cs  
// compile with: /unsafe  
unsafe class C  
{  
    static void F(ref int* p)  
    {  
    }  
  
    static void Main()  
    {  
        int[] a = new int[5];  
        fixed(int* p = a) F(ref p); // CS1657  
    }  
}  
```
