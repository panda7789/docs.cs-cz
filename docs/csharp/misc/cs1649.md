---
title: Compiler Error CS1649
ms.date: 07/20/2015
f1_keywords:
- CS1649
helpviewer_keywords:
- CS1649
ms.assetid: 6355c7f2-157c-441d-8925-500062988636
ms.openlocfilehash: da00bfb56571bc331b8e31bf65dfe9090d258bc4
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/23/2019
ms.locfileid: "61685742"
---
# <a name="compiler-error-cs1649"></a><span data-ttu-id="cafb6-102">Compiler Error CS1649</span><span class="sxs-lookup"><span data-stu-id="cafb6-102">Compiler Error CS1649</span></span>
<span data-ttu-id="cafb6-103">Členy pole jen pro čtení 'identifier' nelze předat jako parametr ref nebo out (kromě konstruktor)</span><span class="sxs-lookup"><span data-stu-id="cafb6-103">Members of readonly field 'identifier' cannot be passed ref or out (except in a constructor)</span></span>  
  
 <span data-ttu-id="cafb6-104">K této chybě dochází, pokud předáte funkci, která je členem skupiny proměnné `readonly` pole `ref` nebo `out` argument.</span><span class="sxs-lookup"><span data-stu-id="cafb6-104">This error occurs if you pass a variable to a function that is a member of a `readonly` field as a `ref` or `out` argument.</span></span> <span data-ttu-id="cafb6-105">Protože `ref` a `out` může změnit parametry funkce, což není povoleno.</span><span class="sxs-lookup"><span data-stu-id="cafb6-105">Since `ref` and `out` parameters may be modified by the function, this is not allowed.</span></span> <span data-ttu-id="cafb6-106">Chcete-li tuto chybu vyřešit, odeberte `readonly` – klíčové slovo v poli, nebo předejte jako objekty její členové `readonly` pole do funkce.</span><span class="sxs-lookup"><span data-stu-id="cafb6-106">To resolve this error, remove the `readonly` keyword on the field, or do not pass the members of the `readonly` field to the function.</span></span> <span data-ttu-id="cafb6-107">Například můžete vyzkoušet vytvoření dočasné proměnné, které lze upravit a předáním jako dočasný `ref` argument, jak je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="cafb6-107">For example, you might try creating a temporary variable which can be modified and passing the temporary as a `ref` argument, as shown in the following example.</span></span>  
  
## <a name="example"></a><span data-ttu-id="cafb6-108">Příklad</span><span class="sxs-lookup"><span data-stu-id="cafb6-108">Example</span></span>  
 <span data-ttu-id="cafb6-109">Následující ukázka generuje CS1649:</span><span class="sxs-lookup"><span data-stu-id="cafb6-109">The following sample generates CS1649:</span></span>  
  
```csharp  
// CS1649.cs  
public struct Inner  
    {  
        public int i;  
    }  
  
class Outer  
{  
    public readonly Inner inner = new Inner();  
}  
  
class D  
{  
    static void f(ref int iref)  
    {  
    }  
  
    static void Main()  
    {  
        Outer outer = new Outer();   
        f(ref outer.inner.i);  // CS1649  
        // Try this code instead:  
        // int tmp = outer.inner.i;  
        // f(ref tmp);  
    }  
}  
```
