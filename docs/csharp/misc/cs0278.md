---
title: Kompilátor CS0278 upozornění (úroveň 2)
ms.date: 07/20/2015
f1_keywords:
- CS0278
helpviewer_keywords:
- CS0278
ms.assetid: 5418cbbe-bcec-4379-a6f6-410987beb96a
ms.openlocfilehash: 48f8228520d02d7f2000d3d0b508b1b48c38c37e
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/23/2019
ms.locfileid: "61658081"
---
# <a name="compiler-warning-level-2-cs0278"></a><span data-ttu-id="9fdd2-102">Kompilátor CS0278 upozornění (úroveň 2)</span><span class="sxs-lookup"><span data-stu-id="9fdd2-102">Compiler Warning (level 2) CS0278</span></span>
<span data-ttu-id="9fdd2-103">'type' neimplementuje vzorek 'název vzoru'.</span><span class="sxs-lookup"><span data-stu-id="9fdd2-103">'type' does not implement the 'pattern name' pattern.</span></span> <span data-ttu-id="9fdd2-104">'název metody' je nejednoznačné s "název metody".</span><span class="sxs-lookup"><span data-stu-id="9fdd2-104">'method name' is ambiguous with 'method name'.</span></span>  
  
<span data-ttu-id="9fdd2-105">Existuje několik příkazů v C# , který závisí na definovaných vzorů, jako například `foreach` a `using`.</span><span class="sxs-lookup"><span data-stu-id="9fdd2-105">There are several statements in C# that rely on defined patterns, such as `foreach` and `using`.</span></span> <span data-ttu-id="9fdd2-106">Například [ `foreach` příkaz](../language-reference/keywords/foreach-in.md) spoléhá na třídu kolekce implementaci vzoru "vyčíslitelné".</span><span class="sxs-lookup"><span data-stu-id="9fdd2-106">For example, the [`foreach` statement](../language-reference/keywords/foreach-in.md) relies on the collection class implementing the "enumerable" pattern.</span></span>
  
<span data-ttu-id="9fdd2-107">CS0278 situace může nastat, pokud kompilátor nemůže provést porovnání z důvodu nejednoznačnosti.</span><span class="sxs-lookup"><span data-stu-id="9fdd2-107">CS0278 can occur if the compiler is unable to make the match due to ambiguities.</span></span> <span data-ttu-id="9fdd2-108">Například "vyčíslitelné" vzor vyžaduje, aby zde být metoda volána `MoveNext`, a může kód obsahovat dvě metody volá `MoveNext`.</span><span class="sxs-lookup"><span data-stu-id="9fdd2-108">For example, the "enumerable" pattern requires that there be a method called `MoveNext`, and your code might contain two methods called `MoveNext`.</span></span> <span data-ttu-id="9fdd2-109">Kompilátor se pokusí najít rozhraní používat, ale doporučuje že určete a vyřešte příčinu nejednoznačnost.</span><span class="sxs-lookup"><span data-stu-id="9fdd2-109">The compiler will attempt to find an interface to use, but it is recommended that you determine and resolve the cause of the ambiguity.</span></span>  
  
## <a name="example"></a><span data-ttu-id="9fdd2-110">Příklad</span><span class="sxs-lookup"><span data-stu-id="9fdd2-110">Example</span></span>  
 <span data-ttu-id="9fdd2-111">Následující ukázka generuje CS0278.</span><span class="sxs-lookup"><span data-stu-id="9fdd2-111">The following sample generates CS0278.</span></span>  
  
```csharp  
// CS0278.cs  
using System.Collections.Generic;  
public class myTest   
{  
   public static void TestForeach<W>(W w)   
      where W: IEnumerable<int>, IEnumerable<string>  
   {  
      foreach (int i in w) {}   // CS0278  
   }  
}  
```
