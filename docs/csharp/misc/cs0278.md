---
description: Upozornění kompilátoru (úroveň 2) CS0278
title: Upozornění kompilátoru (úroveň 2) CS0278
ms.date: 07/20/2015
f1_keywords:
- CS0278
helpviewer_keywords:
- CS0278
ms.assetid: 5418cbbe-bcec-4379-a6f6-410987beb96a
ms.openlocfilehash: 9ecb413746d940b4f3ba44a289d0709aa56f30a5
ms.sourcegitcommit: d579fb5e4b46745fd0f1f8874c94c6469ce58604
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/30/2020
ms.locfileid: "89143269"
---
# <a name="compiler-warning-level-2-cs0278"></a><span data-ttu-id="b76f4-103">Upozornění kompilátoru (úroveň 2) CS0278</span><span class="sxs-lookup"><span data-stu-id="b76f4-103">Compiler Warning (level 2) CS0278</span></span>
<span data-ttu-id="b76f4-104">typ neimplementuje vzor "název vzoru".</span><span class="sxs-lookup"><span data-stu-id="b76f4-104">'type' does not implement the 'pattern name' pattern.</span></span> <span data-ttu-id="b76f4-105">' název metody ' je dvojznačný pomocí ' název metody '.</span><span class="sxs-lookup"><span data-stu-id="b76f4-105">'method name' is ambiguous with 'method name'.</span></span>  
  
<span data-ttu-id="b76f4-106">V jazyce C# je několik příkazů, které spoléhají na definované vzory, například `foreach` a `using` .</span><span class="sxs-lookup"><span data-stu-id="b76f4-106">There are several statements in C# that rely on defined patterns, such as `foreach` and `using`.</span></span> <span data-ttu-id="b76f4-107">Například [ `foreach` příkaz](../language-reference/keywords/foreach-in.md) spoléhá na třídu kolekce implementující vzor "vyčíslitelného".</span><span class="sxs-lookup"><span data-stu-id="b76f4-107">For example, the [`foreach` statement](../language-reference/keywords/foreach-in.md) relies on the collection class implementing the "enumerable" pattern.</span></span>
  
<span data-ttu-id="b76f4-108">K CS0278 může dojít v případě, že kompilátor není schopen provést shodu z důvodu nejednoznačnosti.</span><span class="sxs-lookup"><span data-stu-id="b76f4-108">CS0278 can occur if the compiler is unable to make the match due to ambiguities.</span></span> <span data-ttu-id="b76f4-109">Například vzor "vyčíslitelné" vyžaduje, aby byla volána metoda `MoveNext` a váš kód může obsahovat dvě metody nazvané `MoveNext` .</span><span class="sxs-lookup"><span data-stu-id="b76f4-109">For example, the "enumerable" pattern requires that there be a method called `MoveNext`, and your code might contain two methods called `MoveNext`.</span></span> <span data-ttu-id="b76f4-110">Kompilátor se pokusí najít rozhraní, které se má použít, ale doporučuje se určit a vyřešit příčinu nejednoznačnosti.</span><span class="sxs-lookup"><span data-stu-id="b76f4-110">The compiler will attempt to find an interface to use, but it is recommended that you determine and resolve the cause of the ambiguity.</span></span>  
  
## <a name="example"></a><span data-ttu-id="b76f4-111">Příklad</span><span class="sxs-lookup"><span data-stu-id="b76f4-111">Example</span></span>  
 <span data-ttu-id="b76f4-112">Následující ukázka generuje CS0278.</span><span class="sxs-lookup"><span data-stu-id="b76f4-112">The following sample generates CS0278.</span></span>  
  
```csharp  
// CS0278.cs  
using System.Collections.Generic;  
public class myTest
{  
   public static void TestForeach<W>(W w)
      where W: IEnumerable<int>, IEnumerable<string>  
   {  
      foreach (int i in w) {}   // CS0278  
   }  
}  
```
