---
title: Zahození - průvodce v C#
description: Popisuje podporu jazyka C# pro zahození, což nepřiřazené discardable proměnné a způsoby, ve které je možné zahození.
author: rpetrusha
ms.author: ronpet
ms.date: 07/21/2017
ms.openlocfilehash: 761fb69d3bc774975caf63b8aa665f8c19c0430a
ms.sourcegitcommit: 9bd8f213b50f0e1a73e03bd1e840c917fbd6d20a
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/25/2018
ms.locfileid: "50045649"
---
# <a name="discards---c-guide"></a><span data-ttu-id="d37c8-103">Zahození - průvodce v C#</span><span class="sxs-lookup"><span data-stu-id="d37c8-103">Discards - C# Guide</span></span>

<span data-ttu-id="d37c8-104">Od verze C# 7.0, C# podporuje zahodí, které jsou dočasné a fiktivní proměnné, které nejsou používány záměrně v kódu aplikace.</span><span class="sxs-lookup"><span data-stu-id="d37c8-104">Starting with C# 7.0, C# supports discards, which are temporary, dummy variables that are intentionally unused in application code.</span></span> <span data-ttu-id="d37c8-105">Zahození jsou ekvivalentní nepřiřazené proměnné; nemají hodnotu.</span><span class="sxs-lookup"><span data-stu-id="d37c8-105">Discards are equivalent to unassigned variables; they do not have a value.</span></span> <span data-ttu-id="d37c8-106">Protože existuje pouze jeden discard proměnné a tuto proměnnou i nelze přidělit úložiště, můžete snížit zahození přidělení paměti.</span><span class="sxs-lookup"><span data-stu-id="d37c8-106">Because there is only a single discard variable, and that variable may not even be allocated storage, discards can reduce memory allocations.</span></span> <span data-ttu-id="d37c8-107">Protože jsou záměr kódu jasný, zvýšit jeho čitelnost a udržovatelnosti.</span><span class="sxs-lookup"><span data-stu-id="d37c8-107">Because they make the intent of your code clear, they enhance its readability and maintainability.</span></span>

<span data-ttu-id="d37c8-108">Určujete, že proměnná je zahození přiřazením podtržítko (`_`) jako jeho název.</span><span class="sxs-lookup"><span data-stu-id="d37c8-108">You indicate that a variable is a discard by assigning it the underscore (`_`) as its name.</span></span> <span data-ttu-id="d37c8-109">Například následující volání metody vrací 3 řazené kolekce členů ve kterém jsou hodnoty prvního a druhého zahození a *oblasti* je dřív deklarovanou proměnnou nastavit na odpovídající komponentě třetího vrácený  *GetCityInformation*:</span><span class="sxs-lookup"><span data-stu-id="d37c8-109">For example, the following method call returns a 3-tuple in which the first and second values are discards and *area* is a previously declared variable to be set to the corresponding third component returned by *GetCityInformation*:</span></span>

```csharp
(_, _, area) = city.GetCityInformation(cityName);
```

<span data-ttu-id="d37c8-110">V jazyce C# 7.0 jsou podporovány zahození v přiřazení v následujících kontextů:</span><span class="sxs-lookup"><span data-stu-id="d37c8-110">In C# 7.0, discards are supported in assignments in the following contexts:</span></span>

- <span data-ttu-id="d37c8-111">Řazené kolekce členů a objekt [dekonstrukce](deconstruct.md).</span><span class="sxs-lookup"><span data-stu-id="d37c8-111">Tuple and object [deconstruction](deconstruct.md).</span></span>
- <span data-ttu-id="d37c8-112">Porovnávání vzorů s [je](language-reference/keywords/is.md) a [přepnout](language-reference/keywords/switch.md).</span><span class="sxs-lookup"><span data-stu-id="d37c8-112">Pattern matching with [is](language-reference/keywords/is.md) and [switch](language-reference/keywords/switch.md).</span></span>
- <span data-ttu-id="d37c8-113">Volání metody s `out` parametry.</span><span class="sxs-lookup"><span data-stu-id="d37c8-113">Calls to methods with `out` parameters.</span></span>
- <span data-ttu-id="d37c8-114">Samostatný `_` když ne `_` je v oboru.</span><span class="sxs-lookup"><span data-stu-id="d37c8-114">A standalone `_` when no `_` is in scope.</span></span>

<span data-ttu-id="d37c8-115">Když `_` je platný zahození, pokusu o načtení jeho hodnoty nebo použít operace přiřazení vygeneruje Chyba kompilátoru CS0301, "název"\_' neexistuje v aktuálním kontextu ".</span><span class="sxs-lookup"><span data-stu-id="d37c8-115">When `_` is a valid discard, attempting to retrieve its value or use it in an assignment operation generates compiler error CS0301, "The name '\_' does not exist in the current context".</span></span> <span data-ttu-id="d37c8-116">Důvodem je, že `_` není přiřazena hodnota a nemusí dají přiřadit dokonce i umístění úložiště.</span><span class="sxs-lookup"><span data-stu-id="d37c8-116">This is because `_` is not assigned a value, and may not even be assigned a storage location.</span></span> <span data-ttu-id="d37c8-117">Kdyby skutečná proměnná, nemohl zahodit více než jednu hodnotu, jako předchozí příklad udělal.</span><span class="sxs-lookup"><span data-stu-id="d37c8-117">If it were an actual variable, you could not discard more than one value, as the previous example did.</span></span>

## <a name="tuple-and-object-deconstruction"></a><span data-ttu-id="d37c8-118">Dekonstrukce řazené kolekce členů a objektu</span><span class="sxs-lookup"><span data-stu-id="d37c8-118">Tuple and object deconstruction</span></span>

<span data-ttu-id="d37c8-119">Zahození jsou obzvláště užitečná při práci s řazenými kolekcemi členů, když kód aplikace používá několik elementů řazené kolekce členů, ale ignoruje ostatní.</span><span class="sxs-lookup"><span data-stu-id="d37c8-119">Discards are particularly useful in working with tuples when your application code uses some tuple elements but ignores others.</span></span> <span data-ttu-id="d37c8-120">Například následující `QueryCityDataForYears` metoda vrátí 6-řazené kolekce členů s názvem Město, jeho oblast, rok, název města plnění pro daný rok, druhý rok a název města plnění pro daný druhý rok.</span><span class="sxs-lookup"><span data-stu-id="d37c8-120">For example, the following `QueryCityDataForYears` method returns a 6-tuple with the name of a city, its area, a year, the city's population for that year, a second year, and the city's population for that second year.</span></span> <span data-ttu-id="d37c8-121">Příklad ukazuje změnu počtu obyvatel mezi tyto dva roky.</span><span class="sxs-lookup"><span data-stu-id="d37c8-121">The example shows the change in population between those two years.</span></span> <span data-ttu-id="d37c8-122">Data k dispozici z řazené kolekce členů, jsme unconcerned k oblasti města a víme název města a dvěma kalendářními daty v době návrhu.</span><span class="sxs-lookup"><span data-stu-id="d37c8-122">Of the data available from the tuple, we're unconcerned with the city area, and we know the city name and the two dates at design-time.</span></span> <span data-ttu-id="d37c8-123">V důsledku toho jsme zajímá pouze dvě naplnění hodnoty uložené v řazené kolekci členů a dokáže zpracovat zbývající hodnoty jako zahození.</span><span class="sxs-lookup"><span data-stu-id="d37c8-123">As a result, we're only interested in the two population values stored in the tuple, and can handle its remaining values as discards.</span></span>  

[!code-csharp[Tuple-discard](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="d37c8-124">Další informace o dekonstrukce řazených kolekcí členů se zahodí, naleznete v tématu [Dekonstrukce řazených kolekcí členů a dalších typů](deconstruct.md#deconstructing-tuple-elements-with-discards).</span><span class="sxs-lookup"><span data-stu-id="d37c8-124">For more information on deconstructing tuples with discards, see [Deconstructing tuples and other types](deconstruct.md#deconstructing-tuple-elements-with-discards).</span></span>

<span data-ttu-id="d37c8-125">`Deconstruct` Metoda třídy, struktury nebo rozhraní umožňuje také budete moct načítat a dekonstruovat konkrétní sadu dat z objektu.</span><span class="sxs-lookup"><span data-stu-id="d37c8-125">The `Deconstruct` method of a class, structure, or interface also allows you to retrieve and deconstruct a specific set of data from an object.</span></span> <span data-ttu-id="d37c8-126">Pokud vás zajímají práce s pouze podmnožinu dekonstruovanou hodnoty můžete zahození.</span><span class="sxs-lookup"><span data-stu-id="d37c8-126">You can use discards when you are interested in working with only a subset of the deconstructed values.</span></span> <span data-ttu-id="d37c8-127">Následující příklad deconstructs `Person` objektu do čtyř řetězců (jména a příjmení, města a státu), ale zahodí poslední názvu a stavu.</span><span class="sxs-lookup"><span data-stu-id="d37c8-127">The following example deconstructs a `Person` object into four strings (the first and last names, the city, and the state), but discards the last name and the state.</span></span>

[!code-csharp[Class-discard](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/class-discard1.cs)]

<span data-ttu-id="d37c8-128">Další informace o uživatelem definovaných typů s zahození dekonstrukce, naleznete v tématu [Dekonstrukce řazených kolekcí členů a dalších typů](deconstruct.md#deconstructing-a-user-defined-type-with-discards).</span><span class="sxs-lookup"><span data-stu-id="d37c8-128">For more information on deconstructing user-defined types with discards, see [Deconstructing tuples and other types](deconstruct.md#deconstructing-a-user-defined-type-with-discards).</span></span>

## <a name="pattern-matching-with-switch-and-is"></a><span data-ttu-id="d37c8-129">Porovnávání vzorů s `switch` a `is`</span><span class="sxs-lookup"><span data-stu-id="d37c8-129">Pattern matching with `switch` and `is`</span></span>

<span data-ttu-id="d37c8-130">*Zahodit vzor* lze použít v porovnávání vzorů s [je](language-reference/keywords/is.md) a [přepnout](language-reference/keywords/switch.md) klíčová slova.</span><span class="sxs-lookup"><span data-stu-id="d37c8-130">The *discard pattern* can be used in pattern matching with the [is](language-reference/keywords/is.md) and [switch](language-reference/keywords/switch.md) keywords.</span></span> <span data-ttu-id="d37c8-131">Každý výraz vždy shoduje se vzorem zahození.</span><span class="sxs-lookup"><span data-stu-id="d37c8-131">Every expression always matches the discard pattern.</span></span>

<span data-ttu-id="d37c8-132">Následující příklad definuje `ProvidesFormatInfo` metodu, která používá [je](language-reference/keywords/is.md) příkazy k určení, zda objekt poskytuje <xref:System.IFormatProvider> implementaci a testování, zda je objekt `null`.</span><span class="sxs-lookup"><span data-stu-id="d37c8-132">The following example defines a `ProvidesFormatInfo` method that uses [is](language-reference/keywords/is.md) statements to determine whether an object provides an <xref:System.IFormatProvider> implementation and tests whether the object is `null`.</span></span> <span data-ttu-id="d37c8-133">Také používá vzor pro zrušení zpracování objektů jinou hodnotu než null jakéhokoli jiného typu.</span><span class="sxs-lookup"><span data-stu-id="d37c8-133">It also uses the discard pattern to handle non-null objects of any other type.</span></span>

[!code-csharp[discard-pattern](../../samples/snippets/csharp/programming-guide/discards/discard-pattern2.cs)]

## <a name="calls-to-methods-with-out-parameters"></a><span data-ttu-id="d37c8-134">Volání metody s výstupním parametrům</span><span class="sxs-lookup"><span data-stu-id="d37c8-134">Calls to methods with out parameters</span></span>

<span data-ttu-id="d37c8-135">Při volání `Deconstruct` metoda dekonstruovat uživatelem definovaný typ (instance třídy, struktury nebo rozhraní), můžete zrušit hodnoty jednotlivých `out` argumenty.</span><span class="sxs-lookup"><span data-stu-id="d37c8-135">When calling the `Deconstruct` method to deconstruct a user-defined type (an instance of a class, structure, or interface), you can discard the values of individual `out` arguments.</span></span> <span data-ttu-id="d37c8-136">Ale můžete také zrušit hodnotu `out` argumenty při volání metody jakoukoli metodou s výstupní parametr.</span><span class="sxs-lookup"><span data-stu-id="d37c8-136">But you can also discard the value of `out` arguments when calling any method with an out parameter.</span></span>

<span data-ttu-id="d37c8-137">Následující příklad volá [DateTime.TryParse (String, si data a času)](<xref:System.DateTime.TryParse(System.String,System.DateTime@)>) metodou ke zjištění, zda je v aktuální jazykovou verzi platný řetězec představující datum.</span><span class="sxs-lookup"><span data-stu-id="d37c8-137">The following example calls the [DateTime.TryParse(String, out DateTime)](<xref:System.DateTime.TryParse(System.String,System.DateTime@)>) method to determine whether the string representation of a date is valid in the current culture.</span></span> <span data-ttu-id="d37c8-138">Protože příklad se týká pouze s ověřuje řetězec data a není ho extrahovat data, analýza `out` argument metody je zahození.</span><span class="sxs-lookup"><span data-stu-id="d37c8-138">Because the example is concerned only with validating the date string and not with parsing it to extract the date, the `out` argument to the method is a discard.</span></span>

[!code-csharp[discard-with-out](../../samples/snippets/csharp/programming-guide/discards/discard-out1.cs)]

## <a name="a-standalone-discard"></a><span data-ttu-id="d37c8-139">Samostatná zrušit</span><span class="sxs-lookup"><span data-stu-id="d37c8-139">A standalone discard</span></span>

<span data-ttu-id="d37c8-140">Samostatná zrušit můžete použít k označení jakákoli proměnná, která se můžete rozhodnout ignorovat.</span><span class="sxs-lookup"><span data-stu-id="d37c8-140">You can use a standalone discard to indicate any variable that you choose to ignore.</span></span> <span data-ttu-id="d37c8-141">Následující příklad používá samostatná zrušit Ignorovat <xref:System.Threading.Tasks.Task> vrácený asynchronní operace.</span><span class="sxs-lookup"><span data-stu-id="d37c8-141">The following example uses a standalone discard to ignore the <xref:System.Threading.Tasks.Task> object returned by an asynchronous operation.</span></span> <span data-ttu-id="d37c8-142">To má za následek výjimku, která vyvolá operaci, jako je dokončení potlačení.</span><span class="sxs-lookup"><span data-stu-id="d37c8-142">This has the effect of suppressing the exception that the operation throws as it is about to complete.</span></span>

[!code-csharp[standalone-discard](../../samples/snippets/csharp/programming-guide/discards/standalone-discard1.cs)]

<span data-ttu-id="d37c8-143">Všimněte si, že `_` je také platný identifikátor.</span><span class="sxs-lookup"><span data-stu-id="d37c8-143">Note that `_` is also a valid identifier.</span></span> <span data-ttu-id="d37c8-144">Při použití mimo podporované kontext `_` je zpracováván, nikoli jako zahození ale jako platná proměnná.</span><span class="sxs-lookup"><span data-stu-id="d37c8-144">When used outside of a supported context, `_` is treated not as a discard but as a valid variable.</span></span> <span data-ttu-id="d37c8-145">Pokud identifikátor s názvem `_` je již v oboru, použití `_` jako samostatná zrušit může vést k:</span><span class="sxs-lookup"><span data-stu-id="d37c8-145">If an identifier named `_` is already in scope, the use of `_` as a standalone discard can result in:</span></span>

- <span data-ttu-id="d37c8-146">Nechtěné změny hodnoty příslušné `_` proměnné přiřazením hodnoty určené zahození.</span><span class="sxs-lookup"><span data-stu-id="d37c8-146">Accidental modification of the value of the in-scope `_` variable by assigning it the value of the intended discard.</span></span> <span data-ttu-id="d37c8-147">Příklad:</span><span class="sxs-lookup"><span data-stu-id="d37c8-147">For example:</span></span>

   [!code-csharp[standalone-discard](../../samples/snippets/csharp/programming-guide/discards/standalone-discard2.cs#1)]

- <span data-ttu-id="d37c8-148">Chyba kompilátoru za porušení bezpečnosti typů.</span><span class="sxs-lookup"><span data-stu-id="d37c8-148">A compiler error for violating type safety.</span></span> <span data-ttu-id="d37c8-149">Příklad:</span><span class="sxs-lookup"><span data-stu-id="d37c8-149">For example:</span></span>

   [!code-csharp[standalone-discard](../../samples/snippets/csharp/programming-guide/discards/standalone-discard2.cs#2)]

- <span data-ttu-id="d37c8-150">Chyba kompilátoru CS0136, "místní proměnná nebo parametr s názvem"\_"nelze deklarovat v tomto oboru, protože tento název se používá v uzavírajícím místním oboru pro definování místní proměnné nebo parametru."</span><span class="sxs-lookup"><span data-stu-id="d37c8-150">Compiler error CS0136, "A local or parameter named '\_' cannot be declared in this scope because that name is used in an enclosing local scope to define a local or parameter."</span></span> <span data-ttu-id="d37c8-151">Příklad:</span><span class="sxs-lookup"><span data-stu-id="d37c8-151">For example:</span></span>

   [!code-csharp[standalone-discard](../../samples/snippets/csharp/programming-guide/discards/standalone-discard2.cs#3)]

## <a name="see-also"></a><span data-ttu-id="d37c8-152">Viz také:</span><span class="sxs-lookup"><span data-stu-id="d37c8-152">See also</span></span>

- [<span data-ttu-id="d37c8-153">Dekonstrukce řazených kolekcí členů a dalších typů</span><span class="sxs-lookup"><span data-stu-id="d37c8-153">Deconstructing tuples and other types</span></span>](deconstruct.md)
- [<span data-ttu-id="d37c8-154">`is` Klíčové slovo</span><span class="sxs-lookup"><span data-stu-id="d37c8-154">`is` keyword</span></span>](language-reference/keywords/is.md)
- [<span data-ttu-id="d37c8-155">`switch` Klíčové slovo</span><span class="sxs-lookup"><span data-stu-id="d37c8-155">`switch` keyword</span></span>](language-reference/keywords/switch.md)
