---
title: Základy výrazů dotazu (LINQ v jazyce C#)
description: Zavádí koncepty související s výrazy dotazu.
ms.date: 11/30/2016
ms.assetid: 027db1f8-346f-44d2-a16e-043fcea3a4e0
ms.openlocfilehash: 83beaa82d4b4b42ff9da5230edddd391b33a0717
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/14/2020
ms.locfileid: "79173351"
---
# <a name="query-expression-basics"></a><span data-ttu-id="3b897-103">Základy výrazů dotazů</span><span class="sxs-lookup"><span data-stu-id="3b897-103">Query expression basics</span></span>

<span data-ttu-id="3b897-104">Tento článek představuje základní pojmy související s výrazy dotazu v jazyce C#.</span><span class="sxs-lookup"><span data-stu-id="3b897-104">This article introduces the basic concepts related to query expressions in C#.</span></span>

## <a name="what-is-a-query-and-what-does-it-do"></a><span data-ttu-id="3b897-105">Co je dotaz a co dělá?</span><span class="sxs-lookup"><span data-stu-id="3b897-105">What is a query and what does it do?</span></span>

<span data-ttu-id="3b897-106">*Dotaz* je sada pokynů, která popisuje, jaká data se mají načíst z daného zdroje dat (nebo zdrojů) a jaký obrazec a organizace by měla mít vrácená data.</span><span class="sxs-lookup"><span data-stu-id="3b897-106">A *query* is a set of instructions that describes what data to retrieve from a given data source (or sources) and what shape and organization the returned data should have.</span></span> <span data-ttu-id="3b897-107">Dotaz se liší od výsledků, které vytváří.</span><span class="sxs-lookup"><span data-stu-id="3b897-107">A query is distinct from the results that it produces.</span></span>

<span data-ttu-id="3b897-108">Obecně je zdrojová data uspořádána logicky jako posloupnost prvků stejného druhu.</span><span class="sxs-lookup"><span data-stu-id="3b897-108">Generally, the source data is organized logically as a sequence of elements of the same kind.</span></span> <span data-ttu-id="3b897-109">Například databázová tabulka SQL obsahuje posloupnost řádků.</span><span class="sxs-lookup"><span data-stu-id="3b897-109">For example, a SQL database table contains a sequence of rows.</span></span> <span data-ttu-id="3b897-110">V souboru XML je "sekvence" elementů XML (i když jsou uspořádány hierarchicky ve stromové struktuře).</span><span class="sxs-lookup"><span data-stu-id="3b897-110">In an XML file, there is a "sequence" of XML elements (although these are organized hierarchically in a tree structure).</span></span> <span data-ttu-id="3b897-111">Kolekce v paměti obsahuje posloupnost objektů.</span><span class="sxs-lookup"><span data-stu-id="3b897-111">An in-memory collection contains a sequence of objects.</span></span>

<span data-ttu-id="3b897-112">Z hlediska aplikace není důležitý konkrétní typ a struktura původních zdrojových dat.</span><span class="sxs-lookup"><span data-stu-id="3b897-112">From an application's viewpoint, the specific type and structure of the original source data is not important.</span></span> <span data-ttu-id="3b897-113">Aplikace vždy vidí zdrojová data <xref:System.Collections.Generic.IEnumerable%601> <xref:System.Linq.IQueryable%601> jako kolekce nebo kolekce.</span><span class="sxs-lookup"><span data-stu-id="3b897-113">The application always sees the source data as an <xref:System.Collections.Generic.IEnumerable%601> or <xref:System.Linq.IQueryable%601> collection.</span></span> <span data-ttu-id="3b897-114">Například v LINQ na XML jsou zdrojová `IEnumerable` \< <xref:System.Xml.Linq.XElement> data viditelná jako>.</span><span class="sxs-lookup"><span data-stu-id="3b897-114">For example, in LINQ to XML, the source data is made visible as an `IEnumerable`\<<xref:System.Xml.Linq.XElement>>.</span></span>

<span data-ttu-id="3b897-115">Vzhledem k této zdrojové sekvenci může dotaz provést jednu ze tří věcí:</span><span class="sxs-lookup"><span data-stu-id="3b897-115">Given this source sequence, a query may do one of three things:</span></span>

- <span data-ttu-id="3b897-116">Načíst podmnožinu prvků k vytvoření nové sekvence bez úpravy jednotlivých prvků.</span><span class="sxs-lookup"><span data-stu-id="3b897-116">Retrieve a subset of the elements to produce a new sequence without modifying the individual elements.</span></span> <span data-ttu-id="3b897-117">Dotaz pak může seřadit nebo seskupit vrácenou sekvenci různými způsoby, jak je znázorněno v následujícím příkladu `scores` `int[]`(předpokládejme, že je ):</span><span class="sxs-lookup"><span data-stu-id="3b897-117">The query may then sort or group the returned sequence in various ways, as shown in the following example (assume `scores` is an `int[]`):</span></span>

    [!code-csharp[csrefQueryExpBasics#45](~/samples/snippets/csharp/concepts/linq/query-expression-basics_1.cs)]

- <span data-ttu-id="3b897-118">Načíst posloupnost prvků jako v předchozím příkladu, ale transformovat je na nový typ objektu.</span><span class="sxs-lookup"><span data-stu-id="3b897-118">Retrieve a sequence of elements as in the previous example but transform them to a new type of object.</span></span> <span data-ttu-id="3b897-119">Dotaz může například načíst pouze příjmení z určitých záznamů zákazníků ve zdroji dat.</span><span class="sxs-lookup"><span data-stu-id="3b897-119">For example, a query may retrieve only the last names from certain customer records in a data source.</span></span> <span data-ttu-id="3b897-120">Nebo může načíst celý záznam a potom jej použít k vytvoření jiného typu objektu v paměti nebo dokonce dat XML před generováním konečné sekvence výsledků.</span><span class="sxs-lookup"><span data-stu-id="3b897-120">Or it may retrieve the complete record and then use it to construct another in-memory object type or even XML data before generating the final result sequence.</span></span> <span data-ttu-id="3b897-121">Následující příklad ukazuje projekce `int` z `string`do .</span><span class="sxs-lookup"><span data-stu-id="3b897-121">The following example shows a projection from an `int` to a `string`.</span></span> <span data-ttu-id="3b897-122">Poznamenejte `highScoresQuery`si nový typ .</span><span class="sxs-lookup"><span data-stu-id="3b897-122">Note the new type of `highScoresQuery`.</span></span>

    [!code-csharp[csrefQueryExpBasics#46](~/samples/snippets/csharp/concepts/linq/query-expression-basics_2.cs)]

- <span data-ttu-id="3b897-123">Načíst hodnotu singleton o zdrojových datech, například:</span><span class="sxs-lookup"><span data-stu-id="3b897-123">Retrieve a singleton value about the source data, such as:</span></span>

  - <span data-ttu-id="3b897-124">Počet prvků, které odpovídají určité podmínce.</span><span class="sxs-lookup"><span data-stu-id="3b897-124">The number of elements that match a certain condition.</span></span>

  - <span data-ttu-id="3b897-125">Prvek, který má největší nebo nejmenší hodnotu.</span><span class="sxs-lookup"><span data-stu-id="3b897-125">The element that has the greatest or least value.</span></span>

  - <span data-ttu-id="3b897-126">První prvek, který odpovídá podmínce nebo součet konkrétních hodnot v zadané sadě prvků.</span><span class="sxs-lookup"><span data-stu-id="3b897-126">The first element that matches a condition, or the sum of particular values in a specified set of elements.</span></span> <span data-ttu-id="3b897-127">Například následující dotaz vrátí počet skóre větší než `scores` 80 z celého pole:</span><span class="sxs-lookup"><span data-stu-id="3b897-127">For example, the following query returns the number of scores greater than 80 from the `scores` integer array:</span></span>

    [!code-csharp[csrefQueryExpBasics#47](~/samples/snippets/csharp/concepts/linq/query-expression-basics_3.cs)]

    <span data-ttu-id="3b897-128">V předchozím příkladu si všimněte použití závorek kolem výrazu dotazu před voláním `Count` metody.</span><span class="sxs-lookup"><span data-stu-id="3b897-128">In the previous example, note the use of parentheses around the query expression before the call to the `Count` method.</span></span> <span data-ttu-id="3b897-129">Můžete také vyjádřit pomocí nové proměnné pro uložení konkrétní výsledek.</span><span class="sxs-lookup"><span data-stu-id="3b897-129">You can also express this by using a new variable to store the concrete result.</span></span> <span data-ttu-id="3b897-130">Tato technika je čitelnější, protože udržuje proměnnou, která ukládá dotaz odděleně od dotazu, který ukládá výsledek.</span><span class="sxs-lookup"><span data-stu-id="3b897-130">This technique is more readable because it keeps the variable that stores the query separate from the query that stores a result.</span></span>

    [!code-csharp[csrefQueryExpBasics#48](~/samples/snippets/csharp/concepts/linq/query-expression-basics_4.cs)]

<span data-ttu-id="3b897-131">V předchozím příkladu je dotaz proveden ve `Count`volání `Count` , protože musí itrovat výsledky, aby bylo `highScoresQuery`možné určit počet prvků vrácených .</span><span class="sxs-lookup"><span data-stu-id="3b897-131">In the previous example, the query is executed in the call to `Count`, because `Count` must iterate over the results in order to determine the number of elements returned by `highScoresQuery`.</span></span>

## <a name="what-is-a-query-expression"></a><span data-ttu-id="3b897-132">Co je výraz dotazu?</span><span class="sxs-lookup"><span data-stu-id="3b897-132">What is a query expression?</span></span>

<span data-ttu-id="3b897-133">*Výraz dotazu* je dotaz vyjádřený v syntaxi dotazu.</span><span class="sxs-lookup"><span data-stu-id="3b897-133">A *query expression* is a query expressed in query syntax.</span></span> <span data-ttu-id="3b897-134">Výraz dotazu je konstrukce jazyka první třídy.</span><span class="sxs-lookup"><span data-stu-id="3b897-134">A query expression is a first-class language construct.</span></span> <span data-ttu-id="3b897-135">Je stejně jako jakýkoli jiný výraz a lze použít v libovolném kontextu, ve kterém je platný výraz Jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="3b897-135">It is just like any other expression and can be used in any context in which a C# expression is valid.</span></span> <span data-ttu-id="3b897-136">Výraz dotazu se skládá ze sady klauzulí napsaných v deklarativní syntaxi podobné SQL nebo XQuery.</span><span class="sxs-lookup"><span data-stu-id="3b897-136">A query expression consists of a set of clauses written in a declarative syntax similar to SQL or XQuery.</span></span> <span data-ttu-id="3b897-137">Každá klauzule zase obsahuje jeden nebo více výrazů Jazyka C# a tyto výrazy mohou být samy o sobě výrazem dotazu nebo výrazem dotazu.</span><span class="sxs-lookup"><span data-stu-id="3b897-137">Each clause in turn contains one or more C# expressions, and these expressions may themselves be either a query expression or contain a query expression.</span></span>

<span data-ttu-id="3b897-138">Výraz dotazu musí začínat klauzulí [from](../language-reference/keywords/from-clause.md) a musí končit klauzulí [select](../language-reference/keywords/select-clause.md) nebo [group.](../language-reference/keywords/group-clause.md)</span><span class="sxs-lookup"><span data-stu-id="3b897-138">A query expression must begin with a [from](../language-reference/keywords/from-clause.md) clause and must end with a [select](../language-reference/keywords/select-clause.md) or [group](../language-reference/keywords/group-clause.md) clause.</span></span> <span data-ttu-id="3b897-139">Mezi první `from` klauzule `select` a `group` poslední nebo klauzule, může obsahovat jednu nebo více z těchto volitelných doložek: [kde](../language-reference/keywords/where-clause.md), [orderby](../language-reference/keywords/orderby-clause.md), [join](../language-reference/keywords/join-clause.md), [let](../language-reference/keywords/let-clause.md) a dokonce i další [z](../language-reference/keywords/from-clause.md) doložek.</span><span class="sxs-lookup"><span data-stu-id="3b897-139">Between the first `from` clause and the last `select` or `group` clause, it can contain one or more of these optional clauses: [where](../language-reference/keywords/where-clause.md), [orderby](../language-reference/keywords/orderby-clause.md), [join](../language-reference/keywords/join-clause.md), [let](../language-reference/keywords/let-clause.md) and even additional [from](../language-reference/keywords/from-clause.md) clauses.</span></span> <span data-ttu-id="3b897-140">Můžete také použít [do](../language-reference/keywords/into.md) klíčové slovo povolit výsledek `join` nebo `group` klauzule sloužit jako zdroj pro další klauzule dotazu ve stejném výrazu dotazu.</span><span class="sxs-lookup"><span data-stu-id="3b897-140">You can also use the [into](../language-reference/keywords/into.md) keyword to enable the result of a `join` or `group` clause to serve as the source for additional query clauses in the same query expression.</span></span>

### <a name="query-variable"></a><span data-ttu-id="3b897-141">Proměnná dotazu</span><span class="sxs-lookup"><span data-stu-id="3b897-141">Query variable</span></span>

<span data-ttu-id="3b897-142">V LINQ je proměnná dotazu libovolná proměnná, která ukládá *dotaz* namísto *výsledků* dotazu.</span><span class="sxs-lookup"><span data-stu-id="3b897-142">In LINQ, a query variable is any variable that stores a *query* instead of the *results* of a query.</span></span> <span data-ttu-id="3b897-143">Přesněji řečeno, proměnná dotazu je vždy výčíslný typ, který vytvoří posloupnost prvků, když je iterována přes v příkazu `foreach` nebo přímé volání jeho `IEnumerator.MoveNext` metody.</span><span class="sxs-lookup"><span data-stu-id="3b897-143">More specifically, a query variable is always an enumerable type that will produce a sequence of elements when it is iterated over in a `foreach` statement or a direct call to its `IEnumerator.MoveNext` method.</span></span>

<span data-ttu-id="3b897-144">Následující příklad kódu ukazuje jednoduchý výraz dotazu s jedním zdrojem dat, jednou klauzulí filtrování, jednou klauzulí řazení a žádnou transformací zdrojových prvků.</span><span class="sxs-lookup"><span data-stu-id="3b897-144">The following code example shows a simple query expression with one data source, one filtering clause, one ordering clause, and no transformation of the source elements.</span></span> <span data-ttu-id="3b897-145">Klauzule `select` ukončí dotaz.</span><span class="sxs-lookup"><span data-stu-id="3b897-145">The `select` clause ends the query.</span></span>

[!code-csharp[csrefQueryExpBasics#49](~/samples/snippets/csharp/concepts/linq/query-expression-basics_5.cs)]

<span data-ttu-id="3b897-146">V předchozím příkladu `scoreQuery` je *proměnná dotazu,* která se někdy označuje jako pouze *dotaz*.</span><span class="sxs-lookup"><span data-stu-id="3b897-146">In the previous example, `scoreQuery` is a *query variable,* which is sometimes referred to as just a *query*.</span></span> <span data-ttu-id="3b897-147">Proměnná dotazu ukládá žádná data skutečného `foreach` výsledku, která jsou vytvořena ve smyčce.</span><span class="sxs-lookup"><span data-stu-id="3b897-147">The query variable stores no actual result data, which is produced in the `foreach` loop.</span></span> <span data-ttu-id="3b897-148">A když `foreach` se příkaz spustí, výsledky dotazu nejsou `scoreQuery`vráceny prostřednictvím proměnné dotazu .</span><span class="sxs-lookup"><span data-stu-id="3b897-148">And when the `foreach` statement executes, the query results are not returned through the query variable `scoreQuery`.</span></span> <span data-ttu-id="3b897-149">Spíše jsou vráceny prostřednictvím `testScore`iterace proměnné .</span><span class="sxs-lookup"><span data-stu-id="3b897-149">Rather, they are returned through the iteration variable `testScore`.</span></span> <span data-ttu-id="3b897-150">Proměnná `scoreQuery` může být iterována ve druhé `foreach` smyčce.</span><span class="sxs-lookup"><span data-stu-id="3b897-150">The `scoreQuery` variable can be iterated in a second `foreach` loop.</span></span> <span data-ttu-id="3b897-151">Bude mít stejné výsledky, dokud nebude změněn ani zdroj dat.</span><span class="sxs-lookup"><span data-stu-id="3b897-151">It will produce the same results as long as neither it nor the data source has been modified.</span></span>

<span data-ttu-id="3b897-152">Proměnná dotazu může uložit dotaz, který je vyjádřen v syntaxi dotazu nebo syntaxi metody nebo jejich kombinaci.</span><span class="sxs-lookup"><span data-stu-id="3b897-152">A query variable may store a query that is expressed in query syntax or method syntax, or a combination of the two.</span></span> <span data-ttu-id="3b897-153">V následujících příkladech `queryMajorCities` `queryMajorCities2` jsou proměnné dotazu:</span><span class="sxs-lookup"><span data-stu-id="3b897-153">In the following examples, both `queryMajorCities` and `queryMajorCities2` are query variables:</span></span>

[!code-csharp[csrefQueryExpBasics#50](~/samples/snippets/csharp/concepts/linq/query-expression-basics_6.cs)]

<span data-ttu-id="3b897-154">Na druhé straně následující dva příklady zobrazit proměnné, které nejsou proměnné dotazu, i když každý je inicializován s dotazem.</span><span class="sxs-lookup"><span data-stu-id="3b897-154">On the other hand, the following two examples show variables that are not query variables even though each is initialized with a query.</span></span> <span data-ttu-id="3b897-155">Nejedná se o proměnné dotazu, protože ukládají výsledky:</span><span class="sxs-lookup"><span data-stu-id="3b897-155">They are not query variables because they store results:</span></span>

[!code-csharp[csrefQueryExpBasics#51](~/samples/snippets/csharp/concepts/linq/query-expression-basics_7.cs)]

<span data-ttu-id="3b897-156">Další informace o různých způsobech vyjádření dotazů naleznete [v tématu Syntaxe a syntaxe metody dotazu v linq](../programming-guide/concepts/linq/query-syntax-and-method-syntax-in-linq.md).</span><span class="sxs-lookup"><span data-stu-id="3b897-156">For more information about the different ways to express queries, see [Query syntax and method syntax in LINQ](../programming-guide/concepts/linq/query-syntax-and-method-syntax-in-linq.md).</span></span>

#### <a name="explicit-and-implicit-typing-of-query-variables"></a><span data-ttu-id="3b897-157">Explicitní a implicitní psaní proměnných dotazu</span><span class="sxs-lookup"><span data-stu-id="3b897-157">Explicit and implicit typing of query variables</span></span>

<span data-ttu-id="3b897-158">Tato dokumentace obvykle poskytuje explicitní typ proměnné dotazu, aby se zobrazil vztah typu mezi proměnnou dotazu a [klauzulí select](../language-reference/keywords/select-clause.md).</span><span class="sxs-lookup"><span data-stu-id="3b897-158">This documentation usually provides the explicit type of the query variable in order to show the type relationship between the query variable and the [select clause](../language-reference/keywords/select-clause.md).</span></span> <span data-ttu-id="3b897-159">Můžete však také použít klíčové slovo [var](../language-reference/keywords/var.md) k pokynu kompilátoru odvodit typ proměnné dotazu (nebo jiné místní proměnné) v době kompilace.</span><span class="sxs-lookup"><span data-stu-id="3b897-159">However, you can also use the [var](../language-reference/keywords/var.md) keyword to instruct the compiler to infer the type of a query variable (or any other local variable) at compile time.</span></span> <span data-ttu-id="3b897-160">Příklad dotazu, který byl zobrazen dříve v tomto tématu lze také vyjádřit pomocí implicitní psaní:</span><span class="sxs-lookup"><span data-stu-id="3b897-160">For example, the query example that was shown previously in this topic can also be expressed by using implicit typing:</span></span>

[!code-csharp[csrefQueryExpBasics#52](~/samples/snippets/csharp/concepts/linq/query-expression-basics_8.cs)]

<span data-ttu-id="3b897-161">Další informace naleznete [v tématu Implicitně zadané místní proměnné](../programming-guide/classes-and-structs/implicitly-typed-local-variables.md) a vztahy typu v [operacích dotazu LINQ](../programming-guide/concepts/linq/type-relationships-in-linq-query-operations.md).</span><span class="sxs-lookup"><span data-stu-id="3b897-161">For more information, see [Implicitly typed local variables](../programming-guide/classes-and-structs/implicitly-typed-local-variables.md) and [Type relationships in LINQ query operations](../programming-guide/concepts/linq/type-relationships-in-linq-query-operations.md).</span></span>

### <a name="starting-a-query-expression"></a><span data-ttu-id="3b897-162">Spuštění výrazu dotazu</span><span class="sxs-lookup"><span data-stu-id="3b897-162">Starting a query expression</span></span>

<span data-ttu-id="3b897-163">Výraz dotazu `from` musí začínat klauzulí.</span><span class="sxs-lookup"><span data-stu-id="3b897-163">A query expression must begin with a `from` clause.</span></span> <span data-ttu-id="3b897-164">Určuje zdroj dat spolu s proměnnou rozsahu.</span><span class="sxs-lookup"><span data-stu-id="3b897-164">It specifies a data source together with a range variable.</span></span> <span data-ttu-id="3b897-165">Proměnná rozsahu představuje každý po sobě jdoucí prvek ve zdrojové sekvenci při procházení zdrojové sekvence.</span><span class="sxs-lookup"><span data-stu-id="3b897-165">The range variable represents each successive element in the source sequence as the source sequence is being traversed.</span></span> <span data-ttu-id="3b897-166">Proměnná rozsahu je silně zařazována na základě typu prvků ve zdroji dat.</span><span class="sxs-lookup"><span data-stu-id="3b897-166">The range variable is strongly typed based on the type of elements in the data source.</span></span> <span data-ttu-id="3b897-167">V následujícím příkladu, protože `countries` `Country` je pole objektů, je proměnná rozsahu také zadána jako `Country`.</span><span class="sxs-lookup"><span data-stu-id="3b897-167">In the following example, because `countries` is an array of `Country` objects, the range variable is also typed as `Country`.</span></span> <span data-ttu-id="3b897-168">Vzhledem k tomu, že proměnná rozsahu je silně zadaný, můžete použít operátor tečka pro přístup k všem dostupným členům typu.</span><span class="sxs-lookup"><span data-stu-id="3b897-168">Because the range variable is strongly typed, you can use the dot operator to access any available members of the type.</span></span>

[!code-csharp[csrefQueryExpBasics#53](~/samples/snippets/csharp/concepts/linq/query-expression-basics_9.cs)]

<span data-ttu-id="3b897-169">Proměnná rozsahu je v oboru, dokud není dotaz ukončen s středníkem nebo *s klauzulí pokračování.*</span><span class="sxs-lookup"><span data-stu-id="3b897-169">The range variable is in scope until the query is exited either with a semicolon or with a *continuation* clause.</span></span>

<span data-ttu-id="3b897-170">Výraz dotazu může `from` obsahovat více klauzulí.</span><span class="sxs-lookup"><span data-stu-id="3b897-170">A query expression may contain multiple `from` clauses.</span></span> <span data-ttu-id="3b897-171">Další `from` klauzule použijte, pokud každý prvek ve zdrojové sekvenci je sám kolekce nebo obsahuje kolekci.</span><span class="sxs-lookup"><span data-stu-id="3b897-171">Use additional `from` clauses when each element in the source sequence is itself a collection or contains a collection.</span></span> <span data-ttu-id="3b897-172">Předpokládejme například, že máte `Country` kolekci objektů, z `City` nichž `Cities`každá obsahuje kolekci objektů s názvem .</span><span class="sxs-lookup"><span data-stu-id="3b897-172">For example, assume that you have a collection of `Country` objects, each of which contains a collection of `City` objects named `Cities`.</span></span> <span data-ttu-id="3b897-173">Chcete-li `City` zadat `Country`dotaz na `from` objekty v každém z nich , použijte dvě klauzule, jak je znázorněno zde:</span><span class="sxs-lookup"><span data-stu-id="3b897-173">To query the `City` objects in each `Country`, use two `from` clauses as shown here:</span></span>

[!code-csharp[csrefQueryExpBasics#54](~/samples/snippets/csharp/concepts/linq/query-expression-basics_10.cs)]

<span data-ttu-id="3b897-174">Další informace naleznete v [tématu z klauzule](../language-reference/keywords/from-clause.md).</span><span class="sxs-lookup"><span data-stu-id="3b897-174">For more information, see [from clause](../language-reference/keywords/from-clause.md).</span></span>

### <a name="ending-a-query-expression"></a><span data-ttu-id="3b897-175">Ukončení výrazu dotazu</span><span class="sxs-lookup"><span data-stu-id="3b897-175">Ending a query expression</span></span>

<span data-ttu-id="3b897-176">Výraz dotazu musí končit `group` klauzulí `select` nebo klauzulí.</span><span class="sxs-lookup"><span data-stu-id="3b897-176">A query expression must end with either a `group` clause or a `select` clause.</span></span>

#### <a name="group-clause"></a><span data-ttu-id="3b897-177">group – klauzule</span><span class="sxs-lookup"><span data-stu-id="3b897-177">group clause</span></span>

<span data-ttu-id="3b897-178">Klauzuli `group` použijte k vytvoření posloupnosti skupin uspořádaných podle zadaného klíče.</span><span class="sxs-lookup"><span data-stu-id="3b897-178">Use the `group` clause to produce a sequence of groups organized by a key that you specify.</span></span> <span data-ttu-id="3b897-179">Klíč může být libovolný datový typ.</span><span class="sxs-lookup"><span data-stu-id="3b897-179">The key can be any data type.</span></span> <span data-ttu-id="3b897-180">Například následující dotaz vytvoří posloupnost skupin, `Country` které obsahují jeden `char` nebo více objektů a jejichž klíč je hodnota.</span><span class="sxs-lookup"><span data-stu-id="3b897-180">For example, the following query creates a sequence of groups that contains one or more `Country` objects and whose key is a `char` value.</span></span>

[!code-csharp[csrefQueryExpBasics#55](~/samples/snippets/csharp/concepts/linq/query-expression-basics_11.cs)]

<span data-ttu-id="3b897-181">Další informace o seskupování naleznete v tématu [group clause](../language-reference/keywords/group-clause.md).</span><span class="sxs-lookup"><span data-stu-id="3b897-181">For more information about grouping, see [group clause](../language-reference/keywords/group-clause.md).</span></span>

#### <a name="select-clause"></a><span data-ttu-id="3b897-182">select – klauzule (C#)</span><span class="sxs-lookup"><span data-stu-id="3b897-182">select clause</span></span>

<span data-ttu-id="3b897-183">Pomocí `select` klauzule vytvořit všechny ostatní typy sekvencí.</span><span class="sxs-lookup"><span data-stu-id="3b897-183">Use the `select` clause to produce all other types of sequences.</span></span> <span data-ttu-id="3b897-184">Jednoduchá `select` klauzule pouze vytváří posloupnost stejného typu objektů jako objekty, které jsou obsaženy ve zdroji dat.</span><span class="sxs-lookup"><span data-stu-id="3b897-184">A simple `select` clause just produces a sequence of the same type of objects as the objects that are contained in the data source.</span></span> <span data-ttu-id="3b897-185">V tomto příkladu obsahuje `Country` zdroj dat objekty.</span><span class="sxs-lookup"><span data-stu-id="3b897-185">In this example, the data source contains `Country` objects.</span></span> <span data-ttu-id="3b897-186">Klauzule `orderby` pouze seřadí prvky do `select` nového pořadí a klauzule vytvoří posloupnost přeřazené `Country` objekty.</span><span class="sxs-lookup"><span data-stu-id="3b897-186">The `orderby` clause just sorts the elements into a new order and the `select` clause produces a sequence of the reordered `Country` objects.</span></span>

[!code-csharp[csrefQueryExpBasics#56](~/samples/snippets/csharp/concepts/linq/query-expression-basics_12.cs)]

<span data-ttu-id="3b897-187">Klauzule `select` lze transformovat zdrojová data do posloupností nových typů.</span><span class="sxs-lookup"><span data-stu-id="3b897-187">The `select` clause can be used to transform source data into sequences of new types.</span></span> <span data-ttu-id="3b897-188">Tato transformace se také nazývá *projekce*.</span><span class="sxs-lookup"><span data-stu-id="3b897-188">This transformation is also named a *projection*.</span></span> <span data-ttu-id="3b897-189">V následujícím příkladu `select` klauzule *projekty* posloupnost anonymní typy, která obsahuje pouze podmnožinu polí v původní prvek.</span><span class="sxs-lookup"><span data-stu-id="3b897-189">In the following example, the `select` clause *projects* a sequence of anonymous types which contains only a subset of the fields in the original element.</span></span> <span data-ttu-id="3b897-190">Všimněte si, že nové objekty jsou inicializovány pomocí inicializátoru objektu.</span><span class="sxs-lookup"><span data-stu-id="3b897-190">Note that the new objects are initialized by using an object initializer.</span></span>

[!code-csharp[csrefQueryExpBasics#57](~/samples/snippets/csharp/concepts/linq/query-expression-basics_13.cs)]

<span data-ttu-id="3b897-191">Další informace o všech způsobech, kterými lze `select` klauzuli použít k transformaci zdrojových dat, naleznete v [tématu select clause](../language-reference/keywords/select-clause.md).</span><span class="sxs-lookup"><span data-stu-id="3b897-191">For more information about all the ways that a `select` clause can be used to transform source data, see [select clause](../language-reference/keywords/select-clause.md).</span></span>

#### <a name="continuations-with-into"></a><span data-ttu-id="3b897-192">Pokračování s "into"</span><span class="sxs-lookup"><span data-stu-id="3b897-192">Continuations with "into"</span></span>

<span data-ttu-id="3b897-193">Klíčové `into` slovo v klauzuli `select` nebo `group` můžete použít k vytvoření dočasného identifikátoru, který ukládá dotaz.</span><span class="sxs-lookup"><span data-stu-id="3b897-193">You can use the `into` keyword in a `select` or `group` clause to create a temporary identifier that stores a query.</span></span> <span data-ttu-id="3b897-194">To provedete, když je nutné provést další operace dotazu na dotaz po seskupení nebo vybrat operaci.</span><span class="sxs-lookup"><span data-stu-id="3b897-194">Do this when you must perform additional query operations on a query after a grouping or select operation.</span></span> <span data-ttu-id="3b897-195">V následujícím `countries` příkladu jsou seskupeny podle populace v rozsahu 10 milionů.</span><span class="sxs-lookup"><span data-stu-id="3b897-195">In the following example `countries` are grouped according to population in ranges of 10 million.</span></span> <span data-ttu-id="3b897-196">Po vytvoření těchto skupin další klauzule odfiltrovat některé skupiny a potom seřadit skupiny ve vzestupném pořadí.</span><span class="sxs-lookup"><span data-stu-id="3b897-196">After these groups are created, additional clauses filter out some groups, and then to sort the groups in ascending order.</span></span> <span data-ttu-id="3b897-197">K provedení těchto dalších operací `countryGroup` je vyžadováno pokračování reprezentované.</span><span class="sxs-lookup"><span data-stu-id="3b897-197">To perform those additional operations, the continuation represented by `countryGroup` is required.</span></span>

[!code-csharp[csrefQueryExpBasics#58](~/samples/snippets/csharp/concepts/linq/query-expression-basics_14.cs)]

<span data-ttu-id="3b897-198">Další informace naleznete [v tématu](../language-reference/keywords/into.md).</span><span class="sxs-lookup"><span data-stu-id="3b897-198">For more information, see [into](../language-reference/keywords/into.md).</span></span>

### <a name="filtering-ordering-and-joining"></a><span data-ttu-id="3b897-199">Filtrování, řazení a spojování</span><span class="sxs-lookup"><span data-stu-id="3b897-199">Filtering, ordering, and joining</span></span>

<span data-ttu-id="3b897-200">Mezi počáteční `from` klauzulí a `select` `group` koncovou nebo klauzulí`where` `join`jsou `orderby` `from`všechny `let`ostatní klauzule ( , , , , ) volitelné.</span><span class="sxs-lookup"><span data-stu-id="3b897-200">Between the starting `from` clause, and the ending `select` or `group` clause, all other clauses (`where`, `join`, `orderby`, `from`, `let`) are optional.</span></span> <span data-ttu-id="3b897-201">Všechny volitelné klauzule mohou být použity nula krát nebo vícekrát v textu dotazu.</span><span class="sxs-lookup"><span data-stu-id="3b897-201">Any of the optional clauses may be used zero times or multiple times in a query body.</span></span>

#### <a name="where-clause"></a><span data-ttu-id="3b897-202">where – klauzule</span><span class="sxs-lookup"><span data-stu-id="3b897-202">where clause</span></span>

<span data-ttu-id="3b897-203">Pomocí `where` klauzule můžete odfiltrovat prvky ze zdrojových dat na základě jednoho nebo více predikátových výrazů.</span><span class="sxs-lookup"><span data-stu-id="3b897-203">Use the `where` clause to filter out elements from the source data based on one or more predicate expressions.</span></span> <span data-ttu-id="3b897-204">Klauzule `where` v následujícím příkladu má jeden predikát se dvěma podmínkami.</span><span class="sxs-lookup"><span data-stu-id="3b897-204">The `where` clause in the following example has one predicate with two conditions.</span></span>

[!code-csharp[csrefQueryExpBasics#59](~/samples/snippets/csharp/concepts/linq/query-expression-basics_15.cs)]

<span data-ttu-id="3b897-205">Další informace naleznete v tématu [where clause](../language-reference/keywords/where-clause.md).</span><span class="sxs-lookup"><span data-stu-id="3b897-205">For more information, see [where clause](../language-reference/keywords/where-clause.md).</span></span>

#### <a name="orderby-clause"></a><span data-ttu-id="3b897-206">orderby – klauzule</span><span class="sxs-lookup"><span data-stu-id="3b897-206">orderby clause</span></span>

<span data-ttu-id="3b897-207">Pomocí `orderby` klauzule můžete seřadit výsledky vzestupně nebo sestupně.</span><span class="sxs-lookup"><span data-stu-id="3b897-207">Use the `orderby` clause to sort the results in either ascending or descending order.</span></span> <span data-ttu-id="3b897-208">Můžete také zadat sekundární pořadí řazení.</span><span class="sxs-lookup"><span data-stu-id="3b897-208">You can also specify secondary sort orders.</span></span> <span data-ttu-id="3b897-209">Následující příklad provádí primární řazení `country` na objekty pomocí vlastnosti. `Area`</span><span class="sxs-lookup"><span data-stu-id="3b897-209">The following example performs a primary sort on the `country` objects by using the `Area` property.</span></span> <span data-ttu-id="3b897-210">Potom provede sekundární řazení pomocí `Population` vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="3b897-210">It then performs a secondary sort by using the `Population` property.</span></span>

[!code-csharp[csrefQueryExpBasics#60](~/samples/snippets/csharp/concepts/linq/query-expression-basics_16.cs)]

<span data-ttu-id="3b897-211">Klíčové `ascending` slovo je volitelné; jedná se o výchozí pořadí řazení, pokud není zadáno žádné pořadí.</span><span class="sxs-lookup"><span data-stu-id="3b897-211">The `ascending` keyword is optional; it is the default sort order if no order is specified.</span></span> <span data-ttu-id="3b897-212">Další informace naleznete v [tématu orderby klauzule](../language-reference/keywords/orderby-clause.md).</span><span class="sxs-lookup"><span data-stu-id="3b897-212">For more information, see [orderby clause](../language-reference/keywords/orderby-clause.md).</span></span>

#### <a name="join-clause"></a><span data-ttu-id="3b897-213">join – klauzule</span><span class="sxs-lookup"><span data-stu-id="3b897-213">join clause</span></span>

<span data-ttu-id="3b897-214">Pomocí `join` klauzule můžete přidružit nebo kombinovat prvky z jednoho zdroje dat s prvky z jiného zdroje dat na základě porovnání rovnosti mezi zadanými klíči v každém prvku.</span><span class="sxs-lookup"><span data-stu-id="3b897-214">Use the `join` clause to associate and/or combine elements from one data source with elements from another data source based on an equality comparison between specified keys in each element.</span></span> <span data-ttu-id="3b897-215">V LINQ operace spojení jsou prováděny na sekvence objektů, jejichž prvky jsou různé typy.</span><span class="sxs-lookup"><span data-stu-id="3b897-215">In LINQ, join operations are performed on sequences of objects whose elements are different types.</span></span> <span data-ttu-id="3b897-216">Po vytvoření dvou sekvencí je `select` `group` nutné použít příkaz nebo k určení prvku, který má být do výstupní sekvence uložit.</span><span class="sxs-lookup"><span data-stu-id="3b897-216">After you have joined two sequences, you must use a `select` or `group` statement to specify which element to store in the output sequence.</span></span> <span data-ttu-id="3b897-217">Anonymní typ můžete také použít ke kombinaci vlastností z každé sady přidružených prvků do nového typu pro výstupní sekvenci.</span><span class="sxs-lookup"><span data-stu-id="3b897-217">You can also use an anonymous type to combine properties from each set of associated elements into a new type for the output sequence.</span></span> <span data-ttu-id="3b897-218">Následující příklad přidruží objekty, `prod` jejichž `Category` `categories` vlastnost odpovídá jedné z kategorií v poli řetězců.</span><span class="sxs-lookup"><span data-stu-id="3b897-218">The following example associates `prod` objects whose `Category` property matches one of the categories in the `categories` string array.</span></span> <span data-ttu-id="3b897-219">Produkty, `Category` jejichž neodpovídá `categories` žádnému řetězci, jsou odfiltrovány. Příkaz `select` projekty nový typ, jehož `cat` `prod`vlastnosti jsou převzaty z obou a .</span><span class="sxs-lookup"><span data-stu-id="3b897-219">Products whose `Category` does not match any string in `categories` are filtered out. The `select` statement projects a new type whose properties are taken from both `cat` and `prod`.</span></span>

[!code-csharp[csrefQueryExpBasics#61](~/samples/snippets/csharp/concepts/linq/query-expression-basics_17.cs)]

<span data-ttu-id="3b897-220">Můžete také provést spojení skupiny uložením výsledky `join` operace do dočasné proměnné pomocí [do](../language-reference/keywords/into.md) klíčového slova.</span><span class="sxs-lookup"><span data-stu-id="3b897-220">You can also perform a group join by storing the results of the `join` operation into a temporary variable by using the [into](../language-reference/keywords/into.md) keyword.</span></span> <span data-ttu-id="3b897-221">Další informace naleznete v tématu [join clause](../language-reference/keywords/join-clause.md).</span><span class="sxs-lookup"><span data-stu-id="3b897-221">For more information, see [join clause](../language-reference/keywords/join-clause.md).</span></span>

#### <a name="let-clause"></a><span data-ttu-id="3b897-222">let – klauzule</span><span class="sxs-lookup"><span data-stu-id="3b897-222">let clause</span></span>

<span data-ttu-id="3b897-223">Klauzuli `let` použijte k uložení výsledku výrazu, například volání metody, do nové proměnné rozsahu.</span><span class="sxs-lookup"><span data-stu-id="3b897-223">Use the `let` clause to store the result of an expression, such as a method call, in a new range variable.</span></span> <span data-ttu-id="3b897-224">V následujícím příkladu proměnná `firstName` rozsahu ukládá první prvek pole řetězců, `Split`které je vráceno .</span><span class="sxs-lookup"><span data-stu-id="3b897-224">In the following example, the range variable `firstName` stores the first element of the array of strings that is returned by `Split`.</span></span>

[!code-csharp[csrefQueryExpBasics#62](~/samples/snippets/csharp/concepts/linq/query-expression-basics_18.cs)]

<span data-ttu-id="3b897-225">Další informace naleznete v tématu [let klauzule](../language-reference/keywords/let-clause.md).</span><span class="sxs-lookup"><span data-stu-id="3b897-225">For more information, see [let clause](../language-reference/keywords/let-clause.md).</span></span>

### <a name="subqueries-in-a-query-expression"></a><span data-ttu-id="3b897-226">Poddotazy ve výrazu dotazu</span><span class="sxs-lookup"><span data-stu-id="3b897-226">Subqueries in a query expression</span></span>

<span data-ttu-id="3b897-227">Klauzule dotazu může sama o sobě obsahovat výraz dotazu, který se někdy označuje jako *poddotaz*.</span><span class="sxs-lookup"><span data-stu-id="3b897-227">A query clause may itself contain a query expression, which is sometimes referred to as a *subquery*.</span></span> <span data-ttu-id="3b897-228">Každý poddotaz začíná `from` vlastní klauzulí, která nemusí nutně ukazovat `from` na stejný zdroj dat v první klauzuli.</span><span class="sxs-lookup"><span data-stu-id="3b897-228">Each subquery starts with its own `from` clause that does not necessarily point to the same data source in the first `from` clause.</span></span> <span data-ttu-id="3b897-229">Například následující dotaz zobrazuje výraz dotazu, který se používá v příkazu select k načtení výsledků operace seskupení.</span><span class="sxs-lookup"><span data-stu-id="3b897-229">For example, the following query shows a query expression that is used in the select statement to retrieve the results of a grouping operation.</span></span>

[!code-csharp[csrefQueryExpBasics#63](~/samples/snippets/csharp/concepts/linq/query-expression-basics_19.cs)]

<span data-ttu-id="3b897-230">Další informace naleznete [v tématu Provedení poddotazu na operaci seskupení](perform-a-subquery-on-a-grouping-operation.md).</span><span class="sxs-lookup"><span data-stu-id="3b897-230">For more information, see [Perform a subquery on a grouping operation](perform-a-subquery-on-a-grouping-operation.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="3b897-231">Viz také</span><span class="sxs-lookup"><span data-stu-id="3b897-231">See also</span></span>

- [<span data-ttu-id="3b897-232">Průvodce programováním v C#</span><span class="sxs-lookup"><span data-stu-id="3b897-232">C# programming guide</span></span>](../programming-guide/index.md)
- [<span data-ttu-id="3b897-233">LINQ (Language Integrated Query)</span><span class="sxs-lookup"><span data-stu-id="3b897-233">Language Integrated Query (LINQ)</span></span>](index.md)
- [<span data-ttu-id="3b897-234">Klíčová slova dotazu (LINQ)</span><span class="sxs-lookup"><span data-stu-id="3b897-234">Query keywords (LINQ)</span></span>](../language-reference/keywords/query-keywords.md)
- [<span data-ttu-id="3b897-235">Standardní operátory dotazů – přehled</span><span class="sxs-lookup"><span data-stu-id="3b897-235">Standard query operators overview</span></span>](../programming-guide/concepts/linq/standard-query-operators-overview.md)
