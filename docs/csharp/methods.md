---
title: Metody – Průvodce C#
description: Přehled metod, metoda parametry a návratové hodnoty – metoda
author: rpetrusha
ms.author: ronpet
ms.date: 05/21/2018
ms.assetid: 577a8527-1081-4b36-9b9e-0685b6553c6e
ms.openlocfilehash: a9d78ecfc3911cf959333c098a66614239609c4e
ms.sourcegitcommit: 43924acbdbb3981d103e11049bbe460457d42073
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 05/23/2018
ms.locfileid: "34457953"
---
# <a name="methods"></a><span data-ttu-id="d55e7-103">Metody</span><span class="sxs-lookup"><span data-stu-id="d55e7-103">Methods</span></span> #

<span data-ttu-id="d55e7-104">Metoda je blok kódu, který obsahuje řadu příkazů.</span><span class="sxs-lookup"><span data-stu-id="d55e7-104">A method is a code block that contains a series of statements.</span></span> <span data-ttu-id="d55e7-105">Program způsobí, že příkazy provádět volání metody a zadání argumentů požadovaná metoda.</span><span class="sxs-lookup"><span data-stu-id="d55e7-105">A program causes the statements to be executed by calling the method and specifying any required method arguments.</span></span> <span data-ttu-id="d55e7-106">V jazyce C# každé spuštění instrukce se provádí v kontextu metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-106">In C#, every executed instruction is performed in the context of a method.</span></span> <span data-ttu-id="d55e7-107">`Main` Metoda je vstupní bod pro každou aplikaci C# a je volána metodou common language runtime (CLR) při spuštění programu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-107">The `Main` method is the entry point for every C# application and it is called by the common language runtime (CLR) when the program is started.</span></span>

> [!NOTE]
> <span data-ttu-id="d55e7-108">Toto téma popisuje pojmenované metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-108">This topic discusses named methods.</span></span> <span data-ttu-id="d55e7-109">Informace o anonymní funkce najdete v tématu [anonymní funkce](programming-guide/statements-expressions-operators/anonymous-functions.md).</span><span class="sxs-lookup"><span data-stu-id="d55e7-109">For information about anonymous functions, see [Anonymous Functions](programming-guide/statements-expressions-operators/anonymous-functions.md).</span></span>

<span data-ttu-id="d55e7-110">Toto téma obsahuje následující oddíly:</span><span class="sxs-lookup"><span data-stu-id="d55e7-110">This topic contains the following sections:</span></span>

- [<span data-ttu-id="d55e7-111">Podpisy – metoda</span><span class="sxs-lookup"><span data-stu-id="d55e7-111">Method signatures</span></span>](#signatures)
- [<span data-ttu-id="d55e7-112">Volání metody</span><span class="sxs-lookup"><span data-stu-id="d55e7-112">Method invocation</span></span>](#invocation)
- [<span data-ttu-id="d55e7-113">Zděděné a přepsané metody</span><span class="sxs-lookup"><span data-stu-id="d55e7-113">Inherited and overridden methods</span></span>](#inherited)
- [<span data-ttu-id="d55e7-114">Předávání parametrů</span><span class="sxs-lookup"><span data-stu-id="d55e7-114">Passing parameters</span></span>](#passing)
  - [<span data-ttu-id="d55e7-115">Předávání parametrů podle hodnoty</span><span class="sxs-lookup"><span data-stu-id="d55e7-115">Passing parameters by value</span></span>](#byval)
  - [<span data-ttu-id="d55e7-116">Předávání parametrů odkazem</span><span class="sxs-lookup"><span data-stu-id="d55e7-116">Passing parameters by reference</span></span>](#byref)
  - [<span data-ttu-id="d55e7-117">Pole parametrů</span><span class="sxs-lookup"><span data-stu-id="d55e7-117">Parameter arrays</span></span>](#paramarray)
- [<span data-ttu-id="d55e7-118">Volitelné parametry a argumenty</span><span class="sxs-lookup"><span data-stu-id="d55e7-118">Optional parameters and arguments</span></span>](#optional)
- [<span data-ttu-id="d55e7-119">Návratové hodnoty</span><span class="sxs-lookup"><span data-stu-id="d55e7-119">Return values</span></span>](#return)
- [<span data-ttu-id="d55e7-120">Metody rozšíření</span><span class="sxs-lookup"><span data-stu-id="d55e7-120">Extension methods</span></span>](#extension)
- [<span data-ttu-id="d55e7-121">Asynchronní metody</span><span class="sxs-lookup"><span data-stu-id="d55e7-121">Async Methods</span></span>](#async)
- [<span data-ttu-id="d55e7-122">Členové tvoření výrazy</span><span class="sxs-lookup"><span data-stu-id="d55e7-122">Expression-bodied members</span></span>](#expr)
- [<span data-ttu-id="d55e7-123">Iterátory</span><span class="sxs-lookup"><span data-stu-id="d55e7-123">Iterators</span></span>](#iterators)

<a name="signatures"></a>
## <a name="method-signatures"></a><span data-ttu-id="d55e7-124">Podpisy – metoda</span><span class="sxs-lookup"><span data-stu-id="d55e7-124">Method signatures</span></span> ##

<span data-ttu-id="d55e7-125">Metody, které jsou deklarované v `class` nebo `struct` zadáním:</span><span class="sxs-lookup"><span data-stu-id="d55e7-125">Methods are declared in a `class` or `struct` by specifying:</span></span>

- <span data-ttu-id="d55e7-126">Volitelný přístup úrovně, jako například `public` nebo `private`.</span><span class="sxs-lookup"><span data-stu-id="d55e7-126">An optional access level, such as `public` or `private`.</span></span> <span data-ttu-id="d55e7-127">Výchozí hodnota je `private`.</span><span class="sxs-lookup"><span data-stu-id="d55e7-127">The default is `private`.</span></span>
- <span data-ttu-id="d55e7-128">Volitelné modifikátory jako `abstract` nebo `sealed`.</span><span class="sxs-lookup"><span data-stu-id="d55e7-128">Optional modifiers such as `abstract` or `sealed`.</span></span>
- <span data-ttu-id="d55e7-129">Návratovou hodnotu, nebo `void` Pokud metoda má none.</span><span class="sxs-lookup"><span data-stu-id="d55e7-129">The return value, or `void` if the method has none.</span></span>
- <span data-ttu-id="d55e7-130">Název metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-130">The method name.</span></span>
- <span data-ttu-id="d55e7-131">Všechny parametry metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-131">Any method parameters.</span></span> <span data-ttu-id="d55e7-132">Parametry metody jsou uzavřené v závorkách a jsou odděleny čárkami.</span><span class="sxs-lookup"><span data-stu-id="d55e7-132">Method parameters are enclosed in parentheses and are separated by commas.</span></span> <span data-ttu-id="d55e7-133">Závorky, jinak znamenat, že metoda nevyžaduje žádné parametry.</span><span class="sxs-lookup"><span data-stu-id="d55e7-133">Empty parentheses indicate that the method requires no parameters.</span></span>

<span data-ttu-id="d55e7-134">Tyto části společně tvoří podpis metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-134">These parts together form the method signature.</span></span>

> [!NOTE]
> <span data-ttu-id="d55e7-135">Návratový typ metody není součástí podpis metody pro účely přetěžování metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-135">A return type of a method is not part of the signature of the method for the purposes of method overloading.</span></span> <span data-ttu-id="d55e7-136">Je však součástí podpis metody při určování kompatibilitu mezi delegáta a metodu, která odkazuje na.</span><span class="sxs-lookup"><span data-stu-id="d55e7-136">However, it is part of the signature of the method when determining the compatibility between a delegate and the method that it points to.</span></span>

<span data-ttu-id="d55e7-137">Následující příklad definuje třídu s názvem `Motorcycle` obsahující pět metody:</span><span class="sxs-lookup"><span data-stu-id="d55e7-137">The following example defines a class named `Motorcycle` that contains five methods:</span></span>

[!code-csharp[csSnippets.Methods#40](../../samples/snippets/csharp/concepts/methods/methods40.cs#40)]

<span data-ttu-id="d55e7-138">Všimněte si, že `Motorcycle` třída zahrnuje přetížené metody, `Drive`.</span><span class="sxs-lookup"><span data-stu-id="d55e7-138">Note that the `Motorcycle` class includes an overloaded method, `Drive`.</span></span> <span data-ttu-id="d55e7-139">Dvě metody se stejným názvem, ale musí být rozlišené pomocí jejich typy parametrů.</span><span class="sxs-lookup"><span data-stu-id="d55e7-139">Two methods have the same name, but must be differentiated by their parameter types.</span></span>

<a name="invocation"></a>
## <a name="method-invocation"></a><span data-ttu-id="d55e7-140">Volání metody</span><span class="sxs-lookup"><span data-stu-id="d55e7-140">Method invocation</span></span> ##

<span data-ttu-id="d55e7-141">Metody může být buď *instance* nebo *statické*.</span><span class="sxs-lookup"><span data-stu-id="d55e7-141">Methods can be either *instance* or *static*.</span></span> <span data-ttu-id="d55e7-142">Vyvolání metody instance vyžaduje, abyste vytvoří instanci objektu a volat metodu u tohoto objektu. Metoda instance funguje na tuto instanci a jeho data.</span><span class="sxs-lookup"><span data-stu-id="d55e7-142">Invoking an instance method requires that you instantiate an object and call the method on that object; an instance method operates on that instance and its data.</span></span> <span data-ttu-id="d55e7-143">Vyvolání statickou metodu pod položkou Název typu, do které patří metodu; statické metody pracovat se nevztahují na instance data.</span><span class="sxs-lookup"><span data-stu-id="d55e7-143">You invoke a static method by referencing the name of the type to which the method belongs; static methods operate do not operate on instance data.</span></span> <span data-ttu-id="d55e7-144">Probíhá pokus o volání statickou metodu pomocí instance objektu generuje chybu kompilátoru.</span><span class="sxs-lookup"><span data-stu-id="d55e7-144">Attempting to call a static method through an object instance generates a compiler error.</span></span>

<span data-ttu-id="d55e7-145">Volání metody je jako přístup k poli.</span><span class="sxs-lookup"><span data-stu-id="d55e7-145">Calling a method is like accessing a field.</span></span> <span data-ttu-id="d55e7-146">Po názvu objektu (pokud jsou volání metody instance) nebo název typu (při volání `static` metoda), přidejte období, název metody, a kulaté závorky.</span><span class="sxs-lookup"><span data-stu-id="d55e7-146">After the object name (if you are calling an instance method) or the type name (if you are calling a `static` method), add a period, the name of the method, and parentheses.</span></span> <span data-ttu-id="d55e7-147">Argumenty jsou uvedeny v závorkách a jsou odděleny čárkami.</span><span class="sxs-lookup"><span data-stu-id="d55e7-147">Arguments are listed within the parentheses, and are separated by commas.</span></span>

<span data-ttu-id="d55e7-148">Definice metoda určuje názvy a typy parametrů, které jsou požadovány.</span><span class="sxs-lookup"><span data-stu-id="d55e7-148">The method definition specifies the names and types of any parameters that are required.</span></span> <span data-ttu-id="d55e7-149">Když volající vyvolá metodu, poskytuje konkrétní hodnoty, nazývaných argumenty pro jednotlivé parametry.</span><span class="sxs-lookup"><span data-stu-id="d55e7-149">When a caller invokes the method, it provides concrete values, called arguments, for each parameter.</span></span> <span data-ttu-id="d55e7-150">Argumenty, které musí být kompatibilní s typem parametru, ale název argument, pokud používá v volání kódu, nemusí být stejný jako parametr s názvem definované v metodě.</span><span class="sxs-lookup"><span data-stu-id="d55e7-150">The arguments must be compatible with the parameter type, but the argument name, if one is used in the calling code, does not have to be the same as the parameter named defined in the method.</span></span> <span data-ttu-id="d55e7-151">V následujícím příkladu `Square` metoda obsahuje jeden parametr typu `int` s názvem *i*.</span><span class="sxs-lookup"><span data-stu-id="d55e7-151">In the following example, the `Square` method includes a single parameter of type `int` named *i*.</span></span> <span data-ttu-id="d55e7-152">První metodu volat průchodů `Square` metoda a proměnná typu `int` s názvem *num*; druhý, číselnou konstantu; a třetí, výrazu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-152">The first method call passes the `Square` method a variable of type `int` named *num*; the second, a numeric constant; and the third, an expression.</span></span>

[!code-csharp[csSnippets.Methods#74](../../samples/snippets/csharp/concepts/methods/params74.cs#74)]

<span data-ttu-id="d55e7-153">Nejběžnější volání metody státní poziční argumenty; poskytne argumenty ve stejném pořadí jako parametry metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-153">The most common form of method invocation used positional arguments; it supplies arguments in the same order as method parameters.</span></span> <span data-ttu-id="d55e7-154">Metody `Motorcycle` třída proto nelze volat jako v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-154">The methods of the `Motorcycle` class can therefore be called as in the following example.</span></span> <span data-ttu-id="d55e7-155">Volání `Drive` metoda, například obsahuje dva argumenty, které odpovídají dva parametry v syntaxi metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-155">The call to the `Drive` method, for example, includes two arguments that correspond to the two parameters in the method's syntax.</span></span> <span data-ttu-id="d55e7-156">První se změní na hodnotu `miles` parametr, druhá hodnota `speed` parametr.</span><span class="sxs-lookup"><span data-stu-id="d55e7-156">The first becomes the value of the `miles` parameter, the second the value of the `speed` parameter.</span></span>

[!code-csharp[csSnippets.Methods#41](../../samples/snippets/csharp/concepts/methods/methods40.cs#41)]

<span data-ttu-id="d55e7-157">Můžete také použít *s názvem argumenty* místo poziční argumenty při vyvolání metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-157">You can also used *named arguments* instead of positional arguments when invoking a method.</span></span> <span data-ttu-id="d55e7-158">Při použití s názvem argumenty, je třeba zadat název parametru následovaným dvojtečkou (":") a argumentem.</span><span class="sxs-lookup"><span data-stu-id="d55e7-158">When using named arguments, you specify the parameter name followed by a colon (":") and the argument.</span></span> <span data-ttu-id="d55e7-159">Argumenty pro metodu může vyskytovat v libovolném pořadí, tak dlouho, dokud nejsou všechny požadované argumenty.</span><span class="sxs-lookup"><span data-stu-id="d55e7-159">Arguments to the method can appear in any order, as long as all required arguments are present.</span></span> <span data-ttu-id="d55e7-160">Následující příklad používá pojmenované argumenty pro vyvolání `TestMotorcycle.Drive` metoda.</span><span class="sxs-lookup"><span data-stu-id="d55e7-160">The following example uses named arguments to invoke the `TestMotorcycle.Drive` method.</span></span> <span data-ttu-id="d55e7-161">V tomto příkladu jsou pojmenované argumenty z seznam parametrů metody předán v opačném pořadí.</span><span class="sxs-lookup"><span data-stu-id="d55e7-161">In this example, the named arguments are passed in the opposite order from the method's parameter list.</span></span>

[!code-csharp[csSnippets.Methods#45](../../samples/snippets/csharp/concepts/methods/named1.cs#45)]

<span data-ttu-id="d55e7-162">Můžete vyvolat metodu pomocí obou argumentů umístění a pojmenované argumenty.</span><span class="sxs-lookup"><span data-stu-id="d55e7-162">You can invoke a method using both positional arguments and named arguments.</span></span> <span data-ttu-id="d55e7-163">Poziční argument však nelze sledovat pojmenovaný argument.</span><span class="sxs-lookup"><span data-stu-id="d55e7-163">However, a positional argument cannot follow a named argument.</span></span> <span data-ttu-id="d55e7-164">Následující příklad popisuje vyvolání `TestMotorcycle.Drive` metoda z předchozího příkladu pomocí poziční jeden argument a jednu s názvem argument.</span><span class="sxs-lookup"><span data-stu-id="d55e7-164">The following example invokes the `TestMotorcycle.Drive` method from the previous example using one positional argument and one named argument.</span></span>

[!code-csharp[csSnippets.Methods#46](../../samples/snippets/csharp/concepts/methods/named2.cs#46)]

 <a name="inherited"></a>
 ## <a name="inherited-and-overridden-methods"></a><span data-ttu-id="d55e7-165">Zděděné a přepsané metody</span><span class="sxs-lookup"><span data-stu-id="d55e7-165">Inherited and overridden methods</span></span> ##

<span data-ttu-id="d55e7-166">Kromě členů, které jsou explicitně definované v typu zdědí typu členy definované v jeho základních tříd.</span><span class="sxs-lookup"><span data-stu-id="d55e7-166">In addition to the members that are explicitly defined in a type, a type inherits members defined in its base classes.</span></span> <span data-ttu-id="d55e7-167">Vzhledem k tomu, že všechny typy v systému spravovaný typ dědí přímo nebo nepřímo <xref:System.Object> třída, všechny typy dědit jejích členů, například <xref:System.Object.Equals(System.Object)>, <xref:System.Object.GetType>, a <xref:System.Object.ToString>.</span><span class="sxs-lookup"><span data-stu-id="d55e7-167">Since all types in the managed type system inherit directly or indirectly from the <xref:System.Object> class, all types inherit its members, such as <xref:System.Object.Equals(System.Object)>, <xref:System.Object.GetType>, and <xref:System.Object.ToString>.</span></span> <span data-ttu-id="d55e7-168">V následujícím příkladu definuje `Person` třídy, vytvoří dvě instance `Person` objekty a volá `Person.Equals` metoda k určení, zda jsou oba objekty stejné.</span><span class="sxs-lookup"><span data-stu-id="d55e7-168">The following example defines a `Person` class, instantiates two `Person` objects, and calls the `Person.Equals` method to determine whether the two objects are equal.</span></span> <span data-ttu-id="d55e7-169">`Equals` Metoda, však není definován v `Person` třídy; je zděděn z <xref:System.Object>.</span><span class="sxs-lookup"><span data-stu-id="d55e7-169">The `Equals` method, however, is not defined in the `Person` class; it is inherited from <xref:System.Object>.</span></span>

[!code-csharp[csSnippets.Methods#104](../../samples/snippets/csharp/concepts/methods/inherited1.cs#104)]

<span data-ttu-id="d55e7-170">Typy můžete přepsat zděděné členy pomocí `override` – klíčové slovo a poskytuje implementaci pro metodu přepsané.</span><span class="sxs-lookup"><span data-stu-id="d55e7-170">Types can override inherited members by using the `override` keyword and providing an implementation for the overridden method.</span></span> <span data-ttu-id="d55e7-171">Podpis metody musí být stejný jako přepsané metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-171">The method signature must be the same as that of the overridden method.</span></span> <span data-ttu-id="d55e7-172">Následující příklad je stejný, jako je předchozí s tím rozdílem, že přepíše <xref:System.Object.Equals(System.Object)> metoda.</span><span class="sxs-lookup"><span data-stu-id="d55e7-172">The following example is like the previous one, except that it overrides the <xref:System.Object.Equals(System.Object)> method.</span></span> <span data-ttu-id="d55e7-173">(Potlačí také <xref:System.Object.GetHashCode> metoda, protože tyto dvě metody jsou určeny k poskytování konzistentních výsledků.)</span><span class="sxs-lookup"><span data-stu-id="d55e7-173">(It also overrides the <xref:System.Object.GetHashCode> method, since the two methods are intended to provide consistent results.)</span></span>

[!code-csharp[csSnippets.Methods#105](../../samples/snippets/csharp/concepts/methods/overridden1.cs#105)]

<a name="passing"></a>
## <a name="passing-parameters"></a><span data-ttu-id="d55e7-174">Předávání parametrů</span><span class="sxs-lookup"><span data-stu-id="d55e7-174">Passing parameters</span></span> ##

<span data-ttu-id="d55e7-175">Typy v jazyku C# jsou buď *typů hodnot* nebo *odkazové typy*.</span><span class="sxs-lookup"><span data-stu-id="d55e7-175">Types in C# are either *value types* or *reference types*.</span></span> <span data-ttu-id="d55e7-176">Seznam typů předdefinovaných hodnot najdete v tématu [typy a proměnné](./tour-of-csharp/types-and-variables.md).</span><span class="sxs-lookup"><span data-stu-id="d55e7-176">For a list of built-in value types, see [Types and variables](./tour-of-csharp/types-and-variables.md).</span></span> <span data-ttu-id="d55e7-177">Ve výchozím nastavení typy hodnot a odkazové typy jsou předaný metodě hodnotou.</span><span class="sxs-lookup"><span data-stu-id="d55e7-177">By default, both value types and reference types are passed to a method by value.</span></span>

<a name="byval"></a>
### <a name="passing-parameters-by-value"></a><span data-ttu-id="d55e7-178">Předávání parametrů podle hodnoty</span><span class="sxs-lookup"><span data-stu-id="d55e7-178">Passing parameters by value</span></span> ###

<span data-ttu-id="d55e7-179">Když metoda předána typ hodnoty podle hodnoty, je kopii objektu místo samotného objektu předaný metodě.</span><span class="sxs-lookup"><span data-stu-id="d55e7-179">When a value type is passed to a method by value, a copy of the object instead of the object itself is passed to the method.</span></span> <span data-ttu-id="d55e7-180">Změny objektu volané metody tedy mít žádný vliv na původní objekt při řízení vrátí volajícímu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-180">Therefore, changes to the object in the called method have no effect on the original object when control returns to the caller.</span></span>

<span data-ttu-id="d55e7-181">Následující příklad předá typ hodnoty metodu podle hodnoty a zavolat metodu pokusí změnit hodnotu typ hodnoty.</span><span class="sxs-lookup"><span data-stu-id="d55e7-181">The following example passes a value type to a method by value, and the called method attempts to change the value type's value.</span></span> <span data-ttu-id="d55e7-182">Definuje proměnné typu `int`, což je typ hodnoty, inicializuje jeho hodnotu na 20 a předává je pro metodu s názvem `ModifyValue` , se změní hodnota proměnné na 30.</span><span class="sxs-lookup"><span data-stu-id="d55e7-182">It defines a variable of type `int`, which is a value type, initializes its value to 20, and passes it to a method named `ModifyValue` that changes the variable's value to 30.</span></span> <span data-ttu-id="d55e7-183">Když metoda vrátí, ale hodnota proměnné zůstane beze změny.</span><span class="sxs-lookup"><span data-stu-id="d55e7-183">When the method returns, however, the variable's value remains unchanged.</span></span>

[!code-csharp[csSnippets.Methods#10](../../samples/snippets/csharp/concepts/methods/byvalue10.cs#10)]

<span data-ttu-id="d55e7-184">Když metoda předána objekt typu odkaz podle hodnoty, je předaná hodnota odkaz na objekt.</span><span class="sxs-lookup"><span data-stu-id="d55e7-184">When an object of a reference type is passed to a method by value, a reference to the object is passed by value.</span></span> <span data-ttu-id="d55e7-185">To znamená metoda obdrží není samotného objektu, ale argument, který určuje umístění objektu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-185">That is, the method receives not the object itself, but an argument that indicates the location of the object.</span></span> <span data-ttu-id="d55e7-186">Pokud změníte členem objektu pomocí tohoto odkazu, změny se v objektu, když se vrátí ovládací prvek volání metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-186">If you change a member of the object by using this reference, the change is reflected in the object when control returns to the calling method.</span></span> <span data-ttu-id="d55e7-187">Však nahradit objekt předaný metodě nemá žádný vliv na původní objekt při řízení vrátí volajícímu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-187">However, replacing the object passed to the method has no effect on the original object when control returns to the caller.</span></span>

<span data-ttu-id="d55e7-188">Následující příklad definuje třídu (která je typu odkazu) s názvem `SampleRefType`.</span><span class="sxs-lookup"><span data-stu-id="d55e7-188">The following example defines a class (which is a reference type) named `SampleRefType`.</span></span> <span data-ttu-id="d55e7-189">Vytvoření instance `SampleRefType` objektu, přiřadí 44 k jeho `value` pole a předá objekt, který má `ModifyObject` metoda.</span><span class="sxs-lookup"><span data-stu-id="d55e7-189">It instantiates a `SampleRefType` object, assigns 44 to its `value` field, and passes the object to the `ModifyObject` method.</span></span> <span data-ttu-id="d55e7-190">Tento příklad nemá v podstatě má stejnou funkci jako předchozí příklad – předá argumentu podle hodnoty metodu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-190">This example does essentially the same thing as the previous example -- it passes an argument by value to a method.</span></span> <span data-ttu-id="d55e7-191">Ale protože odkaz na typ se používá, výsledek se liší.</span><span class="sxs-lookup"><span data-stu-id="d55e7-191">But because a reference type is used, the result is different.</span></span> <span data-ttu-id="d55e7-192">Změna, která se provádí v `ModifyObject` k `obj.value` poli také změní `value` pole argumentu, `rt`v `Main` metodu 33, jako výstup ukazuje příklad.</span><span class="sxs-lookup"><span data-stu-id="d55e7-192">The modification that is made in `ModifyObject` to the `obj.value` field also changes the `value` field of the argument, `rt`, in the `Main` method to 33, as the output from the example shows.</span></span>

[!code-csharp[csSnippets.Methods#42](../../samples/snippets/csharp/concepts/methods/byvalue42.cs#42)]

<a name="byref"></a>
### <a name="passing-parameters-by-reference"></a><span data-ttu-id="d55e7-193">Předávání parametrů odkazem</span><span class="sxs-lookup"><span data-stu-id="d55e7-193">Passing parameters by reference</span></span> ###

<span data-ttu-id="d55e7-194">Předání parametru odkazu, pokud chcete změnit hodnotu argumentu v metodě a chcete refect tato změna při volání metody vrátí ovládací prvek.</span><span class="sxs-lookup"><span data-stu-id="d55e7-194">You pass a parameter by reference when you want to change the value of an argument in a method and want to refect that change when control returns to the calling method.</span></span> <span data-ttu-id="d55e7-195">Chcete-li předat parametr odkazu, použijte [ `ref` ](language-reference/keywords/ref.md) nebo [ `out` ](language-reference/keywords/out-parameter-modifier.md) – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="d55e7-195">To pass a parameter by reference, you use the [`ref`](language-reference/keywords/ref.md) or [`out`](language-reference/keywords/out-parameter-modifier.md) keyword.</span></span> <span data-ttu-id="d55e7-196">Můžete také předat hodnotu odkazem vyhnout kopírování, ale stále zabránit úpravy pomocí [ `in` ](language-reference/keywords/in-parameter-modifier.md) – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="d55e7-196">You can also pass a value by reference to avoid copying but still prevent modifications using the [`in`](language-reference/keywords/in-parameter-modifier.md) keyword.</span></span>

<span data-ttu-id="d55e7-197">Následující příklad je stejný jako předchozí, s výjimkou toho, je předaná hodnota odkaz na `ModifyValue` metoda.</span><span class="sxs-lookup"><span data-stu-id="d55e7-197">The following example is identical to the previous one, except the value is passed by reference to the `ModifyValue` method.</span></span> <span data-ttu-id="d55e7-198">Pokud je hodnota parametru pozměněn v `ModifyValue` metoda změnu hodnoty se projeví po návratu řízení volajícímu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-198">When the value of the parameter is modified in the `ModifyValue` method, the change in value is reflected when control returns to the caller.</span></span>

[!code-csharp[csSnippets.Methods#106](../../samples/snippets/csharp/concepts/methods/byref106.cs#106)]

<span data-ttu-id="d55e7-199">Běžný vzor, který používá parametry ref zahrnuje odkládací hodnoty proměnných.</span><span class="sxs-lookup"><span data-stu-id="d55e7-199">A common pattern that uses by ref parameters involves swapping the values of variables.</span></span> <span data-ttu-id="d55e7-200">Dvě proměnné, které předáte metodě odkazem a metoda prohození jejich obsah.</span><span class="sxs-lookup"><span data-stu-id="d55e7-200">You pass two variables to a method by reference, and the method swaps their contents.</span></span> <span data-ttu-id="d55e7-201">Následující příklad prohození celočíselné hodnoty.</span><span class="sxs-lookup"><span data-stu-id="d55e7-201">The following example swaps integer values.</span></span>

[!code-csharp[csSnippets.Methods#106](../../samples/snippets/csharp/concepts/methods/swap107.cs#107)]

<span data-ttu-id="d55e7-202">Předávání parametrů typu odkazu můžete změnit hodnotu odkaz sám sebe, nikoli hodnotu jeho jednotlivé elementy nebo pole.</span><span class="sxs-lookup"><span data-stu-id="d55e7-202">Passing a reference-type parameter allows you to change the value of the reference itself, rather than the value of its individual elements or fields.</span></span>

<a name="paramarray"></a>
### <a name="parameter-arrays"></a><span data-ttu-id="d55e7-203">Pole parametrů</span><span class="sxs-lookup"><span data-stu-id="d55e7-203">Parameter arrays</span></span> ###

<span data-ttu-id="d55e7-204">V některých případech je požadavek je zadat přesně počet argumentů pro metodu omezující.</span><span class="sxs-lookup"><span data-stu-id="d55e7-204">Sometimes, the requirement that you specify the exact number of arguments to your method is restrictive.</span></span> <span data-ttu-id="d55e7-205">Pomocí `params` – klíčové slovo označíte, že je parametr pole parametrů, povolte metodu k volání s proměnlivým počtem argumentů.</span><span class="sxs-lookup"><span data-stu-id="d55e7-205">By using the `params` keyword to indicate that a parameter is a parameter array, you allow your method to be called with a variable number of arguments.</span></span> <span data-ttu-id="d55e7-206">Parametr označené `params` – klíčové slovo musí být typu pole a musí být poslední parametr v seznamu parametrů metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-206">The parameter tagged with the `params` keyword must must be an array type, and it must be the last parameter in the method's parameter list.</span></span>

<span data-ttu-id="d55e7-207">Volající pak můžete vyvolat metodu jedním ze tří způsobů:</span><span class="sxs-lookup"><span data-stu-id="d55e7-207">A caller can then invoke the method in either of three ways:</span></span>

- <span data-ttu-id="d55e7-208">Předáním pole příslušného typu, který obsahuje požadovaný počet elementů.</span><span class="sxs-lookup"><span data-stu-id="d55e7-208">By passing an array of the appropriate type that contains the desired number of elements.</span></span>
- <span data-ttu-id="d55e7-209">Pomocí metody předání čárkami oddělený seznam jednotlivých argumentů příslušného typu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-209">By passing a comma-separated list of individual arguments of the appropriate type to the method.</span></span>
- <span data-ttu-id="d55e7-210">Poskytnutím není argumentu pole parametrů.</span><span class="sxs-lookup"><span data-stu-id="d55e7-210">By not providing an argument to the parameter array.</span></span>

<span data-ttu-id="d55e7-211">V následujícím příkladu definuje metodu s názvem `DoStringOperation` který provede operaci řetězec určeného její první parametr `StringOperation` – člen výčtu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-211">The following example defines a method named `DoStringOperation` that performs the string operation specified by its first parameter, a `StringOperation` enumeration member.</span></span> <span data-ttu-id="d55e7-212">Pole parametrů jsou definovány řetězce, na kterých je k provedení operace.</span><span class="sxs-lookup"><span data-stu-id="d55e7-212">The strings upon which it is to perform the operation are defined by a parameter array.</span></span> <span data-ttu-id="d55e7-213">`Main` Metoda znázorňuje všechny tři způsoby vyvolání metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-213">The `Main` method illustrates all three ways of invoking the method.</span></span> <span data-ttu-id="d55e7-214">Všimněte si, že metoda označené `params` – klíčové slovo musí být schopen zpracovávat tento případ, ve kterém je žádný argument zadaný pro parametr pole, tak, aby jeho hodnota může být `null`.</span><span class="sxs-lookup"><span data-stu-id="d55e7-214">Note that the method tagged with the `params` keyword must be prepared to handle the case in which no argument is supplied for the parameter array, so that its value is `null`.</span></span>

[!code-csharp[csSnippets.Methods#106](../../samples/snippets/csharp/concepts/methods/byref108.cs#108)]

<a name="optional"></a>
## <a name="optional-parameters-and-arguments"></a><span data-ttu-id="d55e7-215">Volitelné parametry a argumenty</span><span class="sxs-lookup"><span data-stu-id="d55e7-215">Optional parameters and arguments</span></span> ##

<span data-ttu-id="d55e7-216">Definice metody můžete určit, že jeho parametry jsou povinné a že jsou volitelné.</span><span class="sxs-lookup"><span data-stu-id="d55e7-216">A method definition can specify that its parameters are required or that they are optional.</span></span> <span data-ttu-id="d55e7-217">Ve výchozím nastavení jsou vyžadovány parametry.</span><span class="sxs-lookup"><span data-stu-id="d55e7-217">By default, parameters are required.</span></span> <span data-ttu-id="d55e7-218">Volitelné parametry jsou určené v definici metoda včetně výchozí hodnoty parametru.</span><span class="sxs-lookup"><span data-stu-id="d55e7-218">Optional parameters are specified by including the parameter's default value in the method definition.</span></span> <span data-ttu-id="d55e7-219">Při volání metody pro volitelný parametr nebude zadán žádný argument, bude místo něj použita výchozí hodnota.</span><span class="sxs-lookup"><span data-stu-id="d55e7-219">When the method is called, if no argument is supplied for an optional parameter, the default value is used instead.</span></span>

<span data-ttu-id="d55e7-220">Výchozí hodnoty parametru musí být přiřadit pomocí jedné z následujících druhy výrazů:</span><span class="sxs-lookup"><span data-stu-id="d55e7-220">The parameter's default value must be assigned by one of the following kinds of expressions:</span></span>

- <span data-ttu-id="d55e7-221">Toto je konstanta, například řetězcového literálu nebo číslo.</span><span class="sxs-lookup"><span data-stu-id="d55e7-221">A constant, such as a literal string or number.</span></span>
- <span data-ttu-id="d55e7-222">Výraz, který formulář `new ValType`, kde `ValType` je typ hodnoty.</span><span class="sxs-lookup"><span data-stu-id="d55e7-222">An expression of the form `new ValType`, where `ValType` is a value type.</span></span> <span data-ttu-id="d55e7-223">Všimněte si, že tím se spustí tento typ hodnoty implicitní výchozí konstruktor, který není skutečné člen daného typu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-223">Note that this invokes the value type's implicit default constructor, which is not an actual member of the type.</span></span>
- <span data-ttu-id="d55e7-224">Výraz, který formulář `default(ValType)`, kde `ValType` je typ hodnoty.</span><span class="sxs-lookup"><span data-stu-id="d55e7-224">An expression of the form `default(ValType)`, where `ValType` is a value type.</span></span>

<span data-ttu-id="d55e7-225">Obsahuje-li metodu povinných a volitelných parametrů, volitelné parametry jsou definovány na konci seznamu parametrů po všechny požadované parametry.</span><span class="sxs-lookup"><span data-stu-id="d55e7-225">If a method includes both required and optional parameters, optional parameters are defined at the end of the parameter list, after all required parameters.</span></span>

<span data-ttu-id="d55e7-226">V následujícím příkladu definuje metodu, `ExampleMethod`, který má jeden požadované a volitelné dva parametry.</span><span class="sxs-lookup"><span data-stu-id="d55e7-226">The following example defines a method, `ExampleMethod`, that has one required and two optional parameters.</span></span>

[!code-csharp[csSnippets.Methods#21](../../samples/snippets/csharp/concepts/methods/optional1.cs#21)]

<span data-ttu-id="d55e7-227">Pokud je metoda s více volitelné argumenty je vyvolána pomocí poziční argumenty, volající musíte zadat argument pro všechny volitelné parametry z první z nich pro naposledy, pro který je zadán argument.</span><span class="sxs-lookup"><span data-stu-id="d55e7-227">If a method with multiple optional arguments is invoked using positional arguments, the caller must supply an argument for all optional parameters from the first one to the last one for which an argument is supplied.</span></span> <span data-ttu-id="d55e7-228">U `ExampleMethod` metoda, například, pokud má volající poskytuje argument pro `description` parametru, je třeba zadat také jeden pro `optionalInt` parametr.</span><span class="sxs-lookup"><span data-stu-id="d55e7-228">In the case of the  `ExampleMethod` method, for example, if the caller supplies an argument for the `description` parameter, it must also supply one for the `optionalInt` parameter.</span></span> <span data-ttu-id="d55e7-229">`opt.ExampleMethod(2, 2, "Addition of 2 and 2");` je neplatná metoda volání; `opt.ExampleMethod(2, , "Addition of 2 and 0);` generuje "Argument chybějící" Chyba kompilátoru.</span><span class="sxs-lookup"><span data-stu-id="d55e7-229">`opt.ExampleMethod(2, 2, "Addition of 2 and 2");` is a valid method call; `opt.ExampleMethod(2, , "Addition of 2 and 0);` generates an "Argument missing" compiler error.</span></span>

<span data-ttu-id="d55e7-230">Pokud je volána metoda pomocí pojmenované argumenty nebo jejich kombinaci poziční a pojmenované argumenty, volající vynechat všechny argumenty, které následují poslední poziční argument při volání metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-230">If a method is called using named arguments or a combination of positional and named arguments, the caller can omit any arguments that follow the last positional argument in the method call.</span></span>

<span data-ttu-id="d55e7-231">Následující příklad volání `ExampleMethod` metoda třikrát.</span><span class="sxs-lookup"><span data-stu-id="d55e7-231">The following example calls the `ExampleMethod` method three times.</span></span>  <span data-ttu-id="d55e7-232">Volání první dvě metody používají poziční argumenty.</span><span class="sxs-lookup"><span data-stu-id="d55e7-232">The first two method calls use positional arguments.</span></span> <span data-ttu-id="d55e7-233">První vynechá obou volitelné argumenty, zatímco druhý vynechá poslední argument.</span><span class="sxs-lookup"><span data-stu-id="d55e7-233">The first omits both optional arguments, while the second omits the last argument.</span></span> <span data-ttu-id="d55e7-234">Třetí volání metody, které poskytuje poziční argument pro požadovaný parametr, ale používá pojmenovaný argument zadat hodnotu, která `description` parametr při vynechání `optionalInt` argument.</span><span class="sxs-lookup"><span data-stu-id="d55e7-234">The third method call supplies a positional argument for the required parameter, but uses a named argument to supply a value to the `description` parameter while omitting the `optionalInt` argument.</span></span>

[!code-csharp[csSnippets.Methods#22](../../samples/snippets/csharp/concepts/methods/optional1.cs#22)]

<span data-ttu-id="d55e7-235">Použití volitelné parametry ovlivňuje *řešení přetížení*, nebo způsobem, ve kterém kompilátor jazyka C# určuje, které konkrétní přetížení by měla být volána metoda volání, následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="d55e7-235">The use of optional parameters affects *overload resolution*, or the way in which the C# compiler determines which particular overload should be invoked by a method call, as follows:</span></span>

- <span data-ttu-id="d55e7-236">Metoda, indexer nebo konstruktor je kandidátem na spuštění, pokud všechny její parametry je volitelný nebo odpovídá podle názvu nebo podle pozici, na jeden argument příkazu volání a který argument lze převést na typ parametru.</span><span class="sxs-lookup"><span data-stu-id="d55e7-236">A method, indexer, or constructor is a candidate for execution if each of its parameters either is optional or corresponds, by name or by position, to a single argument in the calling statement, and that argument can be converted to the type of the parameter.</span></span>
- <span data-ttu-id="d55e7-237">Pokud je nalezen více než jeden candidate, přetížení řešení pro upřednostňované převody pravidla argumenty, které jsou explicitně určena.</span><span class="sxs-lookup"><span data-stu-id="d55e7-237">If more than one candidate is found, overload resolution rules for preferred conversions are applied to the arguments that are explicitly specified.</span></span> <span data-ttu-id="d55e7-238">Vynechání argumenty pro volitelné parametry jsou ignorovány.</span><span class="sxs-lookup"><span data-stu-id="d55e7-238">Omitted arguments for optional parameters are ignored.</span></span>
- <span data-ttu-id="d55e7-239">Pokud dva kandidáty jsou považovány za být stejně dobrý, předvoleb přejde na candidate, který nemá volitelné parametry, pro které byly vynechány argumenty ve volání.</span><span class="sxs-lookup"><span data-stu-id="d55e7-239">If two candidates are judged to be equally good, preference goes to a candidate that does not have optional parameters for which arguments were omitted in the call.</span></span> <span data-ttu-id="d55e7-240">Toto je důsledkem Obecné předvolby rozlišení přetížení pro kandidáty, které mají méně parametry.</span><span class="sxs-lookup"><span data-stu-id="d55e7-240">This is a consequence of a general preference in overload resolution for candidates that have fewer parameters.</span></span>

 <a name="return"></a>
 ## <a name="return-values"></a><span data-ttu-id="d55e7-241">Vrácené hodnoty</span><span class="sxs-lookup"><span data-stu-id="d55e7-241">Return values</span></span> ##

<span data-ttu-id="d55e7-242">Metody můžete vrátit hodnotu volajícímu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-242">Methods can return a value to the caller.</span></span> <span data-ttu-id="d55e7-243">Návratový typ (typ uvedené před název metody) není-li `void`, metoda může vrátit hodnotu pomocí `return` – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="d55e7-243">If the return type (the type listed before the method name) is not `void`, the method can return the value by using the `return` keyword.</span></span> <span data-ttu-id="d55e7-244">Příkaz s `return` – klíčové slovo, za nímž následuje proměnná, konstanta nebo výraz, který odpovídá návratový typ, vrátí tuto hodnotu Metoda volajícího.</span><span class="sxs-lookup"><span data-stu-id="d55e7-244">A statement with the `return` keyword followed by a variable, constant, or expression that matches the return type will return that value to the method caller.</span></span> <span data-ttu-id="d55e7-245">Metody s není void vrací typ jsou nutné k použití `return` – klíčové slovo bude vrácena hodnota.</span><span class="sxs-lookup"><span data-stu-id="d55e7-245">Methods with a non-void return type are required to use the `return` keyword to return a value.</span></span> <span data-ttu-id="d55e7-246">`return` – Klíčové slovo také zastaví provádění metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-246">The `return` keyword also stops the execution of the method.</span></span>

<span data-ttu-id="d55e7-247">Pokud je návratový typ `void`, `return` je stále užitečné zastavit provádění metody příkaz bez hodnoty.</span><span class="sxs-lookup"><span data-stu-id="d55e7-247">If the return type is `void`, a `return` statement without a value is still useful to stop the execution of the method.</span></span> <span data-ttu-id="d55e7-248">Bez `return` – klíčové slovo, metoda skončí při ukončení bloku kódu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-248">Without the `return` keyword, the method will stop executing when it reaches the end of the code block.</span></span>

<span data-ttu-id="d55e7-249">Například použít tyto dvě metody `return` – klíčové slovo vrátit celá čísla:</span><span class="sxs-lookup"><span data-stu-id="d55e7-249">For example, these two methods use the `return` keyword to return integers:</span></span>

[!code-csharp[csSnippets.Methods#44](../../samples/snippets/csharp/concepts/methods/return44.cs#44)]

<span data-ttu-id="d55e7-250">Pokud chcete použít hodnotu, vrátí metoda, můžete použít metodu volání volání metody samotné kdekoli hodnota stejného typu by být dostatečná.</span><span class="sxs-lookup"><span data-stu-id="d55e7-250">To use a value returned from a method, the calling method can use the method call itself anywhere a value of the same type would be sufficient.</span></span> <span data-ttu-id="d55e7-251">Můžete také přiřadit návratovou hodnotu proměnné.</span><span class="sxs-lookup"><span data-stu-id="d55e7-251">You can also assign the return value to a variable.</span></span> <span data-ttu-id="d55e7-252">Například následující příklady kódu dvě dosažení stejné cíle:</span><span class="sxs-lookup"><span data-stu-id="d55e7-252">For example, the following two code examples accomplish the same goal:</span></span>

[!code-csharp[csSnippets.Methods#45](../../samples/snippets/csharp/concepts/methods/return44.cs#45)]

[!code-csharp[csSnippets.Methods#46](../../samples/snippets/csharp/concepts/methods/return44.cs#46)]

<span data-ttu-id="d55e7-253">Použití místní proměnné, v takovém případě `result`, ukládat hodnota je volitelná.</span><span class="sxs-lookup"><span data-stu-id="d55e7-253">Using a local variable, in this case, `result`, to store a value is optional.</span></span> <span data-ttu-id="d55e7-254">Dobře čitelnost kódu, nebo může být nutné, pokud je třeba uložit původní hodnota argumentu pro celý rozsah metodu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-254">It may help the readability of the code, or it may be necessary if you need to store the original value of the argument for the entire scope of the method.</span></span>

<span data-ttu-id="d55e7-255">V některých případech budete chtít metodu vrátit více než jednu hodnotu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-255">Sometimes, you want your method to return more than a single value.</span></span> <span data-ttu-id="d55e7-256">Od verze 7.0 C#, můžete k tomu snadno pomocí *řazené kolekce členů typy* a *řazené kolekce členů literály*.</span><span class="sxs-lookup"><span data-stu-id="d55e7-256">Starting with C# 7.0, you can do this easily by using *tuple types* and *tuple literals*.</span></span> <span data-ttu-id="d55e7-257">Typ řazené kolekce členů definuje datové typy elementů řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="d55e7-257">The tuple type defines the data types of the tuple's elements.</span></span> <span data-ttu-id="d55e7-258">Literály řazené kolekce členů zadejte skutečné hodnoty vrácené řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="d55e7-258">Tuple literals provide the actual values of the returned tuple.</span></span> <span data-ttu-id="d55e7-259">V následujícím příkladu `(string, string, string, int)` definuje typ řazené kolekce členů, která je vrácena `GetPersonalInfo` metoda.</span><span class="sxs-lookup"><span data-stu-id="d55e7-259">In the following example, `(string, string, string, int)` defines the tuple type that is returned by the `GetPersonalInfo` method.</span></span> <span data-ttu-id="d55e7-260">Výraz `(per.FirstName, per.MiddleName, per.LastName, per.Age)` je literál; název první a poslední společně s stáří, vrátí metoda z řazenou kolekci členů `PersonInfo` objektu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-260">The expression `(per.FirstName, per.MiddleName, per.LastName, per.Age)` is the tuple literal; the method returns the first, middle, and last name, along with the age, of a `PersonInfo` object.</span></span>

```csharp
public (string, string, string, int) GetPersonalInfo(string id)
{
    PersonInfo per = PersonInfo.RetrieveInfoById(id);
    if (per != null)
       return (per.FirstName, per.MiddleName, per.LastName, per.Age);
    else
       return null;
}
```

<span data-ttu-id="d55e7-261">Volající pak spotřebovat vrácený řazené kolekce členů s kódem takto:</span><span class="sxs-lookup"><span data-stu-id="d55e7-261">The caller can then consume the returned tuple with code like the following:</span></span>

```csharp
var person = GetPersonalInfo("111111111")
if (person != null)
   Console.WriteLine("{person.Item1} {person.Item3}: age = {person.Item4}");
```

<span data-ttu-id="d55e7-262">Názvy můžete také přiřazený k elementům řazené kolekce členů v definici typu řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="d55e7-262">Names can also be assigned to the tuple elements in the tuple type definition.</span></span> <span data-ttu-id="d55e7-263">Následující příklad ukazuje alternativní verze `GetPersonalInfo` metoda, která používá s názvem prvky:</span><span class="sxs-lookup"><span data-stu-id="d55e7-263">The following example shows an alternate version of the `GetPersonalInfo` method that uses named elements:</span></span>

```csharp
public (string FName, string MName, string LName, int Age) GetPersonalInfo(string id)
{
    PersonInfo per = PersonInfo.RetrieveInfoById(id);
    if (per != null)
       return (per.FirstName, per.MiddleName, per.LastName, per.Age);
    else
       return null;
}
```

<span data-ttu-id="d55e7-264">Předchozí volání `GetPersonInfo` metoda pak lze upravit takto:</span><span class="sxs-lookup"><span data-stu-id="d55e7-264">The previous call to the `GetPersonInfo` method can then be modified as follows:</span></span>

```csharp
var person = GetPersonalInfo("111111111");
if (person != null)
   Console.WriteLine("{person.FName} {person.LName}: age = {person.Age}");
```

<span data-ttu-id="d55e7-265">Pokud metoda pole je předat jako argument a upraví hodnoty jednotlivých prvků, není nutné pro metodu vrátit pole, i když můžete tak učinit pro dobrý styl nebo funkční toku hodnot.</span><span class="sxs-lookup"><span data-stu-id="d55e7-265">If a method is passed an array as an argument and modifies the value of individual elements, it is not necessary for the method to return the array, although you may choose to do so for good style or functional flow of values.</span></span>  <span data-ttu-id="d55e7-266">Je to proto C# předá všechny odkazové typy podle hodnoty a odkaz na pole hodnotu má ukazatel na pole.</span><span class="sxs-lookup"><span data-stu-id="d55e7-266">This is because C# passes all reference types by value, and the value of an array reference is the pointer to the array.</span></span> <span data-ttu-id="d55e7-267">V následujícím příkladu se změní na obsah `values` pole, které jsou vytvářeny v `DoubleValues` metoda jsou lze zobrazit pomocí kód, který obsahuje odkaz na pole.</span><span class="sxs-lookup"><span data-stu-id="d55e7-267">In the following example, changes to the contents of the `values` array that are made in the `DoubleValues` method are observable by any code that has a reference to the array.</span></span>

[!code-csharp[csSnippets.Methods#101](../../samples/snippets/csharp/concepts/methods/returnarray1.cs#101)]

 <a name="exten"></a>
 ## <a name="extension-methods"></a><span data-ttu-id="d55e7-268">Metody rozšíření</span><span class="sxs-lookup"><span data-stu-id="d55e7-268">Extension methods</span></span> ##

<span data-ttu-id="d55e7-269">Normálně existují dva způsoby, jak přidat metodu do stávající typ:</span><span class="sxs-lookup"><span data-stu-id="d55e7-269">Ordinarily, there are two ways to add a method to an existing type:</span></span>

- <span data-ttu-id="d55e7-270">Zdrojový kód pro tento typ změňte.</span><span class="sxs-lookup"><span data-stu-id="d55e7-270">Modify the source code for that type.</span></span> <span data-ttu-id="d55e7-271">Nelze to uděláte, samozřejmě, pokud nejste vlastníkem tohoto typu zdrojového kódu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-271">You cannot do this, of course, if you do not own the type's source code.</span></span> <span data-ttu-id="d55e7-272">A to se stane narušující změně, pokud přidáte také všechna pole privátní dat podporuje metodu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-272">And this becomes a breaking change if you also add any private data fields to support the method.</span></span>
- <span data-ttu-id="d55e7-273">Definujte nové metody v odvozené třídě.</span><span class="sxs-lookup"><span data-stu-id="d55e7-273">Define the new method in a derived class.</span></span> <span data-ttu-id="d55e7-274">Metodu nelze přidat tímto způsobem použití dědičnosti pro jiné typy, jako je například struktury a výčty.</span><span class="sxs-lookup"><span data-stu-id="d55e7-274">A method cannot be added in this way using inheritance for other types, such as structures and enumerations.</span></span> <span data-ttu-id="d55e7-275">Ani jej lze "Přidat" metodu zapečetěné třídy.</span><span class="sxs-lookup"><span data-stu-id="d55e7-275">Nor can it be used to "add" a method to a sealed class.</span></span>

<span data-ttu-id="d55e7-276">Rozšiřující metody umožňují "Přidat" metodu existující typ bez úpravy vlastní typ nebo implementace nová metoda v zděděné typu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-276">Extension methods let you "add" a method to an existing type without modifying the type itself or implementing the new method in an inherited type.</span></span> <span data-ttu-id="d55e7-277">Metody rozšíření také nemá být umístěné ve stejném sestavení jako typ, který rozšiřuje ji.</span><span class="sxs-lookup"><span data-stu-id="d55e7-277">The extension method also does not have to reside in the same assembly as the type it extends.</span></span> <span data-ttu-id="d55e7-278">Metody rozšíření volání, jako by šlo definované člena typu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-278">You call an extension method as if it were a defined member of a type.</span></span>

<span data-ttu-id="d55e7-279">Další informace najdete v tématu [rozšiřující metody](programming-guide/classes-and-structs/extension-methods.md).</span><span class="sxs-lookup"><span data-stu-id="d55e7-279">For more information, see [Extension Methods](programming-guide/classes-and-structs/extension-methods.md).</span></span>

<a name="async"></a>
## <a name="async-methods"></a><span data-ttu-id="d55e7-280">Asynchronní metody</span><span class="sxs-lookup"><span data-stu-id="d55e7-280">Async Methods</span></span> ##

<span data-ttu-id="d55e7-281">Pomocí funkce asynchronní můžete vyvolat asynchronních metod bez použití explicitní zpětná volání nebo Ruční rozdělení kódu mezi více metod nebo výrazy lambda.</span><span class="sxs-lookup"><span data-stu-id="d55e7-281">By using the async feature, you can invoke asynchronous methods without using explicit callbacks or manually splitting your code across multiple methods or lambda expressions.</span></span>

<span data-ttu-id="d55e7-282">Pokud označíte metodu s [asynchronní](language-reference/keywords/async.md) modifikátor, můžete použít [await](language-reference/keywords/await.md) operátor v metodě.</span><span class="sxs-lookup"><span data-stu-id="d55e7-282">If you mark a method with the [async](language-reference/keywords/async.md) modifier, you can use the [await](language-reference/keywords/await.md) operator in the method.</span></span> <span data-ttu-id="d55e7-283">Při řízení dosáhnou `await` výrazu v asynchronní metody řízení vrátí volající Pokud awaited úloha není dokončena a pokroku v metodě s `await` – klíčové slovo je pozastaveno, dokud se nedokončí awaited úloh.</span><span class="sxs-lookup"><span data-stu-id="d55e7-283">When control reaches an `await` expression in the async method, control returns to the caller if the awaited task is not completed, and progress in the method with the `await` keyword is suspended until the awaited task completes.</span></span> <span data-ttu-id="d55e7-284">Po dokončení úlohy provádění může pokračovat v metodě.</span><span class="sxs-lookup"><span data-stu-id="d55e7-284">When the task is complete, execution can resume in the method.</span></span>

> [!NOTE]
> <span data-ttu-id="d55e7-285">Asynchronní metody vrátí volající, pokud zjistí první awaited objekt, který ještě není dokončena nebo získá na konec asynchronní metody, cokoliv nastane dříve.</span><span class="sxs-lookup"><span data-stu-id="d55e7-285">An async method returns to the caller when either it encounters the first awaited object that’s not yet complete or it gets to the end of the async method, whichever occurs first.</span></span>

<span data-ttu-id="d55e7-286">Asynchronní metody může mít návratový typ <xref:System.Threading.Tasks.Task%601>, <xref:System.Threading.Tasks.Task>, nebo `void`.</span><span class="sxs-lookup"><span data-stu-id="d55e7-286">An async method can have a return type of <xref:System.Threading.Tasks.Task%601>, <xref:System.Threading.Tasks.Task>, or `void`.</span></span> <span data-ttu-id="d55e7-287">`void` Vrátí typ se používá hlavně k definování obslužné rutiny událostí, kde `void` návratový typ je požadovaná.</span><span class="sxs-lookup"><span data-stu-id="d55e7-287">The `void` return type is used primarily to define event handlers, where a `void` return type is required.</span></span> <span data-ttu-id="d55e7-288">Asynchronní metody, která vrací `void` nemůže být očekáváno, a volající metody vrácení void nelze catch výjimky, které vyvolá metoda.</span><span class="sxs-lookup"><span data-stu-id="d55e7-288">An async method that returns `void` can't be awaited, and the caller of a void-returning method can't catch exceptions that the method throws.</span></span> <span data-ttu-id="d55e7-289">Od verze 7.0 C#, může mít použití asynchronní metody [všech úkolů jako návratový typ](./whats-new/csharp-7.md#generalized-async-return-types).</span><span class="sxs-lookup"><span data-stu-id="d55e7-289">Starting with C# 7.0, an async method can have [any task-like return type](./whats-new/csharp-7.md#generalized-async-return-types).</span></span>

<span data-ttu-id="d55e7-290">V následujícím příkladu `DelayAsync` je asynchronní metody, který má návratový příkaz, který vrátí celé číslo.</span><span class="sxs-lookup"><span data-stu-id="d55e7-290">In the following example, `DelayAsync` is an async method that has a return statement that returns an integer.</span></span> <span data-ttu-id="d55e7-291">Protože je asynchronní metody, jeho metoda deklarace musí mít návratový typ `Task<int>`.</span><span class="sxs-lookup"><span data-stu-id="d55e7-291">Because it is an async method, its method declaration must have a return type of `Task<int>`.</span></span> <span data-ttu-id="d55e7-292">Vzhledem k tomu, že je návratový typ `Task<int>`, vyhodnocení `await` výrazu v `DoSomethingAsync` vytváří celé jako následující `int result = await delayTask` ukazuje příkaz.</span><span class="sxs-lookup"><span data-stu-id="d55e7-292">Because the return type is `Task<int>`, the evaluation of the `await` expression in `DoSomethingAsync` produces an integer, as the following `int result = await delayTask` statement demonstrates.</span></span>

[!code-csharp[csSnippets.Methods#102](../../samples/snippets/csharp/concepts/methods/async1.cs#102)]

<span data-ttu-id="d55e7-293">Asynchronní metody nelze deklarovat všechny [v](language-reference/keywords/in-parameter-modifier.md), [ref](language-reference/keywords/ref.md), nebo [out](language-reference/keywords/out-parameter-modifier.md) parametry, ale můžete volat metody, které mají tyto parametry.</span><span class="sxs-lookup"><span data-stu-id="d55e7-293">An async method can't declare any [in](language-reference/keywords/in-parameter-modifier.md), [ref](language-reference/keywords/ref.md), or [out](language-reference/keywords/out-parameter-modifier.md) parameters, but it can call methods that have such parameters.</span></span>

 <span data-ttu-id="d55e7-294">Další informace o asynchronní metody najdete v tématu [asynchronní programování s Async a Await](async.md), [řízení toku v asynchronních programech](programming-guide/concepts/async/control-flow-in-async-programs.md), a [asynchronní vrátit typy](programming-guide/concepts/async/async-return-types.md).</span><span class="sxs-lookup"><span data-stu-id="d55e7-294">For more information about async methods, see [Asynchronous Programming with Async and Await](async.md), [Control Flow in Async Programs](programming-guide/concepts/async/control-flow-in-async-programs.md), and [Async Return Types](programming-guide/concepts/async/async-return-types.md).</span></span>

<a name="expr"></a>
## <a name="expression-bodied-members"></a><span data-ttu-id="d55e7-295">Výraz vozidlo členy</span><span class="sxs-lookup"><span data-stu-id="d55e7-295">Expression-bodied members</span></span> ##

<span data-ttu-id="d55e7-296">Je běžné metoda definicemi, jednoduše okamžitě vrátí s výsledkem výrazu nebo které mají jediný příkaz jako text metody.</span><span class="sxs-lookup"><span data-stu-id="d55e7-296">It is common to have method definitions that simply return immediately with the result of an expression, or that have a single statement as the body of the method.</span></span>  <span data-ttu-id="d55e7-297">Je syntaxe zástupce pro definování těchto metod, pomocí `=>`:</span><span class="sxs-lookup"><span data-stu-id="d55e7-297">There is a syntax shortcut for defining such methods using `=>`:</span></span>

```csharp
public Point Move(int dx, int dy) => new Point(x + dx, y + dy);
public void Print() => Console.WriteLine(First + " " + Last);
// Works with operators, properties, and indexers too.
public static Complex operator +(Complex a, Complex b) => a.Add(b);
public string Name => First + " " + Last;
public Customer this[long id] => store.LookupCustomer(id);
```

<span data-ttu-id="d55e7-298">Pokud metoda vrátí `void` nebo asynchronní metody, obsah metody musí být výraz – příkaz (stejné jako u lambdas).</span><span class="sxs-lookup"><span data-stu-id="d55e7-298">If the method returns `void` or is an async method, the body of the method must be a statement expression (same as with lambdas).</span></span>  <span data-ttu-id="d55e7-299">Pro vlastnostmi a indexery, musí být jen pro čtení a nepoužívejte `get` – klíčové slovo přistupujícího objektu.</span><span class="sxs-lookup"><span data-stu-id="d55e7-299">For properties and indexers, they must be read-only, and you do not use the `get` accessor keyword.</span></span>

<a name="iterators"></a>
## <a name="iterators"></a><span data-ttu-id="d55e7-300">Iterátory</span><span class="sxs-lookup"><span data-stu-id="d55e7-300">Iterators</span></span> ##

<span data-ttu-id="d55e7-301">Iterátor provede vlastní iterace nad kolekcí, jako je například seznam nebo pole.</span><span class="sxs-lookup"><span data-stu-id="d55e7-301">An iterator performs a custom iteration over a collection, such as a list or an array.</span></span> <span data-ttu-id="d55e7-302">Iterátor používá [yield vrátit](language-reference/keywords/yield.md) příkaz vrátit každý element, jeden v čase.</span><span class="sxs-lookup"><span data-stu-id="d55e7-302">An iterator uses the [yield return](language-reference/keywords/yield.md) statement to return each element one at a time.</span></span> <span data-ttu-id="d55e7-303">Když `yield return` příkaz je dosaženo, aktuální umístění je uloží, takže volající může požádat o další prvek v pořadí.</span><span class="sxs-lookup"><span data-stu-id="d55e7-303">When a `yield return` statement is reached, the current location is remembered so that the caller can request the next element in the sequence.</span></span>

<span data-ttu-id="d55e7-304">Návratový typ iterovat může být <xref:System.Collections.IEnumerable>, <xref:System.Collections.Generic.IEnumerable%601>, <xref:System.Collections.IEnumerator>, nebo <xref:System.Collections.Generic.IEnumerator%601>.</span><span class="sxs-lookup"><span data-stu-id="d55e7-304">The return type of an iterator can be <xref:System.Collections.IEnumerable>, <xref:System.Collections.Generic.IEnumerable%601>, <xref:System.Collections.IEnumerator>, or <xref:System.Collections.Generic.IEnumerator%601>.</span></span>

<span data-ttu-id="d55e7-305">Další informace najdete v tématu [iterátory](programming-guide/concepts/iterators.md).</span><span class="sxs-lookup"><span data-stu-id="d55e7-305">For more information, see [Iterators](programming-guide/concepts/iterators.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="d55e7-306">Viz také</span><span class="sxs-lookup"><span data-stu-id="d55e7-306">See also</span></span> ##

<span data-ttu-id="d55e7-307">[Modifikátory přístupu](language-reference/keywords/access-modifiers.md) </span><span class="sxs-lookup"><span data-stu-id="d55e7-307">[Access Modifiers](language-reference/keywords/access-modifiers.md) </span></span>  
<span data-ttu-id="d55e7-308">[Statické třídy a jejich členové](programming-guide/classes-and-structs/static-classes-and-static-class-members.md) </span><span class="sxs-lookup"><span data-stu-id="d55e7-308">[Static Classes and Static Class Members](programming-guide/classes-and-structs/static-classes-and-static-class-members.md) </span></span>  
<span data-ttu-id="d55e7-309">[dědičnost](programming-guide/classes-and-structs/inheritance.md) </span><span class="sxs-lookup"><span data-stu-id="d55e7-309">[Inheritance](programming-guide/classes-and-structs/inheritance.md) </span></span>  
<span data-ttu-id="d55e7-310">[Abstraktní a uzavřené třídy a jejich členové](programming-guide/classes-and-structs/abstract-and-sealed-classes-and-class-members.md) </span><span class="sxs-lookup"><span data-stu-id="d55e7-310">[Abstract and Sealed Classes and Class Members](programming-guide/classes-and-structs/abstract-and-sealed-classes-and-class-members.md) </span></span>  
<span data-ttu-id="d55e7-311">[Parametry](language-reference/keywords/params.md) </span><span class="sxs-lookup"><span data-stu-id="d55e7-311">[params](language-reference/keywords/params.md) </span></span>  
<span data-ttu-id="d55e7-312">[na více systémů](language-reference/keywords/out-parameter-modifier.md) </span><span class="sxs-lookup"><span data-stu-id="d55e7-312">[out](language-reference/keywords/out-parameter-modifier.md) </span></span>  
<span data-ttu-id="d55e7-313">[REF](language-reference/keywords/ref.md) </span><span class="sxs-lookup"><span data-stu-id="d55e7-313">[ref](language-reference/keywords/ref.md) </span></span>  
<span data-ttu-id="d55e7-314">[V](language-reference/keywords/in-parameter-modifier.md) </span><span class="sxs-lookup"><span data-stu-id="d55e7-314">[in](language-reference/keywords/in-parameter-modifier.md) </span></span>  
[<span data-ttu-id="d55e7-315">Předávání parametrů</span><span class="sxs-lookup"><span data-stu-id="d55e7-315">Passing Parameters</span></span>](programming-guide/classes-and-structs/passing-parameters.md)
