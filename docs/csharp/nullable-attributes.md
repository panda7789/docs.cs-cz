---
title: Upgradujte rozhraní API pro typy odkazů s možnou hodnotou null s atributy, které definují očekávání pro hodnoty null.
description: Naučte se používat popisné atributy AllowNull má, DisallowNull, MaybeNull, NotNull a další k úplnému popisu stavu null vašich rozhraní API.
ms.technology: csharp-null-safety
ms.date: 07/31/2019
ms.openlocfilehash: 64dcc70565de0c3094ef1c10866aafce9e18a5c9
ms.sourcegitcommit: 22be09204266253d45ece46f51cc6f080f2b3fd6
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 11/07/2019
ms.locfileid: "73737880"
---
# <a name="update-libraries-to-use-nullable-reference-types-and-communicate-nullable-rules-to-callers"></a><span data-ttu-id="1fa3e-103">Aktualizace knihoven pro použití typů odkazů s možnou hodnotou null a sdělování pravidel s možnou hodnotou null volajícím</span><span class="sxs-lookup"><span data-stu-id="1fa3e-103">Update libraries to use nullable reference types and communicate nullable rules to callers</span></span>

<span data-ttu-id="1fa3e-104">Přidání [odkazových typů s možnou hodnotou null](nullable-references.md) znamená, že můžete deklarovat, zda je hodnota `null` pro každou proměnnou povolená nebo očekávaná.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-104">The addition of [nullable reference types](nullable-references.md) means you can declare whether or not a `null` value is allowed or expected for every variable.</span></span> <span data-ttu-id="1fa3e-105">Kromě toho můžete použít několik atributů: `AllowNull`, `DisallowNull`, `MaybeNull`, `NotNull`, `NotNullWhen`, `MaybeNullWhen`a `NotNullWhenNotNull` k úplnému popisu stavů null argumentů a vrácených hodnot.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-105">In addition, you can apply a number of attributes: `AllowNull`, `DisallowNull`, `MaybeNull`, `NotNull`, `NotNullWhen`, `MaybeNullWhen`, and `NotNullWhenNotNull` to completely describe the null states of argument and return values.</span></span> <span data-ttu-id="1fa3e-106">Poskytuje skvělé prostředí při psaní kódu.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-106">That provides a great experience as you write code.</span></span> <span data-ttu-id="1fa3e-107">Zobrazí se upozornění, pokud může být proměnná bez hodnoty null nastavena na `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-107">You get warnings if a non-nullable variable might be set to `null`.</span></span> <span data-ttu-id="1fa3e-108">Zobrazí se upozornění, pokud u proměnné s možnou hodnotou null není zkontrolována hodnota null před tím, než ji zrušíte.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-108">You get warnings if a nullable variable isn't null-checked before you dereference it.</span></span> <span data-ttu-id="1fa3e-109">Aktualizace knihoven může chvíli trvat, ale výnosy je.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-109">Updating your libraries can take time, but the payoffs are worth it.</span></span> <span data-ttu-id="1fa3e-110">Další informace, které do kompilátoru zadáte, *Pokud* je hodnota `null` povolená nebo zakázaná, budou se lépe zobrazovat uživatelé vašeho rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-110">The more information you provide to the compiler about *when* a `null` value is allowed or prohibited, the better warnings users of your API will get.</span></span> <span data-ttu-id="1fa3e-111">Pojďme začít se známým příkladem.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-111">Let's start with a familiar example.</span></span> <span data-ttu-id="1fa3e-112">Představte si, že knihovna má následující rozhraní API k načtení řetězce prostředků:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-112">Imagine your library has the following API to retrieve a resource string:</span></span>

```csharp
bool TryGetMessage(string key, out string message)
```

<span data-ttu-id="1fa3e-113">Předchozí příklad dodržuje známý vzor `Try*` v rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-113">The preceding example follows the familiar `Try*` pattern in .NET.</span></span> <span data-ttu-id="1fa3e-114">Pro toto rozhraní API existují dva argumenty odkazů: `key` a parametr `message`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-114">There are two reference arguments for this API: the `key` and the `message` parameter.</span></span> <span data-ttu-id="1fa3e-115">Toto rozhraní API má následující pravidla týkající se hodnoty null těchto argumentů:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-115">This API has the following rules relating to the nullness of these arguments:</span></span>

- <span data-ttu-id="1fa3e-116">Volající by neměl předat `null` jako argument pro `key`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-116">Callers shouldn't pass `null` as the argument for `key`.</span></span>
- <span data-ttu-id="1fa3e-117">Volající mohou předat proměnnou, jejíž hodnota je `null` jako argument pro `message`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-117">Callers can pass a variable whose value is `null` as the argument for `message`.</span></span>
- <span data-ttu-id="1fa3e-118">Pokud metoda `TryGetMessage` vrátí `true`, hodnota `message` není null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-118">If the `TryGetMessage` method returns `true`, the value of `message` isn't null.</span></span> <span data-ttu-id="1fa3e-119">Pokud je vrácená hodnota `false,`, hodnota `message` (a její stav null) má hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-119">If the return value is `false,` the value of `message` (and its null state) is null.</span></span>

<span data-ttu-id="1fa3e-120">Pravidlo pro `key` může být zcela vyjádřeno typem proměnné: `key` by měl být odkazový typ, který není null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-120">The rule for `key` can be completely expressed by the variable type: `key` should be a non-nullable reference type.</span></span> <span data-ttu-id="1fa3e-121">Parametr `message` je složitější.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-121">The `message` parameter is more complex.</span></span> <span data-ttu-id="1fa3e-122">Povoluje jako argument hodnotu `null`, ale garantuje, že při úspěchu není argument `out` null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-122">It allows `null` as the argument, but guarantees that, on success, that `out` argument isn't null.</span></span> <span data-ttu-id="1fa3e-123">V těchto scénářích potřebujete podrobnější slovník, který popisuje očekávání.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-123">For these scenarios, you need a richer vocabulary to describe the expectations.</span></span>

<span data-ttu-id="1fa3e-124">Aktualizace knihovny pro odkazy s možnou hodnotou null vyžaduje více než automatické postřikovače `?` u některých proměnných a názvů typů.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-124">Updating your library for nullable references requires more than sprinkling `?` on some of the variables and type names.</span></span> <span data-ttu-id="1fa3e-125">Předchozí příklad ukazuje, že je nutné projít vaše rozhraní API a vzít v úvahu vaše očekávání pro každý vstupní argument.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-125">The preceding example shows that you need to examine your APIs and consider your expectations for each input argument.</span></span> <span data-ttu-id="1fa3e-126">Vezměte v úvahu záruky pro vrácenou hodnotu a všechny argumenty `out` nebo `ref` na vrácení metody.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-126">Consider the guarantees for the return value, and any `out` or `ref` arguments upon the method's return.</span></span> <span data-ttu-id="1fa3e-127">Potom tyto pravidla sdělí kompilátoru a kompilátor poskytne upozornění, pokud se volajícím tyto pravidla neřídí.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-127">Then communicate those rules to the compiler, and the compiler will provide warnings when callers don't abide by those rules.</span></span>

<span data-ttu-id="1fa3e-128">Tato práce trvá určitou dobu.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-128">This work takes time.</span></span> <span data-ttu-id="1fa3e-129">Pojďme začít s strategiemi, které vaší knihovně nebo aplikaci zohledňují hodnoty null, zatímco se vyrovnávají další požadavky a dodávky.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-129">Let's start with strategies to make your library or application nullable-aware, while balancing other requirements and deliverables.</span></span> <span data-ttu-id="1fa3e-130">Uvidíte, jak vyrovnávat průběžný vývoj a povolit typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-130">You'll see how to balance ongoing development enabling nullable reference types.</span></span> <span data-ttu-id="1fa3e-131">Seznámíte se s problémy s definicemi obecného typu.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-131">You'll learn challenges for generic type definitions.</span></span> <span data-ttu-id="1fa3e-132">Dozvíte se, jak použít atributy k popisu před a po jednotlivých podmínkách rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-132">You'll learn to apply attributes to describe pre- and post-conditions on individual APIs.</span></span>

## <a name="choose-a-nullable-strategy"></a><span data-ttu-id="1fa3e-133">Výběr strategie s možnou hodnotou null</span><span class="sxs-lookup"><span data-stu-id="1fa3e-133">Choose a nullable strategy</span></span>

<span data-ttu-id="1fa3e-134">První volbou je, že ve výchozím nastavení by se měly zapnout nebo vypnout typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-134">The first choice is whether nullable reference types should be on or off by default.</span></span> <span data-ttu-id="1fa3e-135">Máte dvě strategie:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-135">You have two strategies:</span></span>

- <span data-ttu-id="1fa3e-136">Povolte typy odkazů s možnou hodnotou null pro celý projekt a zakažte ho v kódu, který není připravený.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-136">Enable nullable reference types for the entire project, and disable it in code that's not ready.</span></span>
- <span data-ttu-id="1fa3e-137">Povolit pouze typy odkazů s možnou hodnotou null pro kód, který je opatřen poznámkou pro typy s možnou hodnotou null</span><span class="sxs-lookup"><span data-stu-id="1fa3e-137">Only enable nullable reference types for code that's been annotated for nullable reference types.</span></span>

<span data-ttu-id="1fa3e-138">První strategie funguje nejlépe při přidávání dalších funkcí do knihovny při jejich aktualizaci na typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-138">The first strategy works best when you're adding other features to the library as you update it for nullable reference types.</span></span> <span data-ttu-id="1fa3e-139">Veškerý nový vývoj má na paměti s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-139">All new development is nullable aware.</span></span> <span data-ttu-id="1fa3e-140">Při aktualizaci existujícího kódu povolíte v těchto třídách typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-140">As you update existing code, you enable nullable reference types in those classes.</span></span>

<span data-ttu-id="1fa3e-141">Po této první strategii provedete následující:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-141">Following this first strategy, you do the following:</span></span>

1. <span data-ttu-id="1fa3e-142">Povolit typy s možnou hodnotou null pro celý projekt přidáním prvku `<Nullable>enable</Nullable>` do souborů *csproj* .</span><span class="sxs-lookup"><span data-stu-id="1fa3e-142">Enable nullable types for the entire project by adding the `<Nullable>enable</Nullable>` element to your *csproj* files.</span></span> 
1. <span data-ttu-id="1fa3e-143">Přidejte direktivu pragma `#nullable disable` do každého zdrojového souboru v projektu.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-143">Add the `#nullable disable` pragma to every source file in your project.</span></span> 
1. <span data-ttu-id="1fa3e-144">Při práci na jednotlivých souborech odeberte direktivu pragma a vyřešte všechna upozornění.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-144">As you work on each file, remove the pragma and address any warnings.</span></span>

<span data-ttu-id="1fa3e-145">Tato první strategie obsahuje více než více práce pro přidání direktivy pragma do každého souboru.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-145">This first strategy has more up-front work to add the pragma to every file.</span></span> <span data-ttu-id="1fa3e-146">Výhodou je, že každý nový soubor kódu přidaný do projektu bude mít povolenou hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-146">The advantage is that every new code file added to the project will be nullable enabled.</span></span> <span data-ttu-id="1fa3e-147">Jakékoli nové práce budou mít na paměti nabývat hodnoty null; je nutné aktualizovat pouze existující kód.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-147">Any new work will be nullable aware; only existing code must be updated.</span></span>

<span data-ttu-id="1fa3e-148">Druhá strategie funguje lépe, pokud je knihovna obecně stabilní a hlavní fokus vývoje je přijmout typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-148">The second strategy works better if the library is generally stable, and the main focus of the development is to adopt nullable reference types.</span></span> <span data-ttu-id="1fa3e-149">Při přidávání poznámek k rozhraním API můžete zapnout typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-149">You turn on nullable reference types as you annotate APIs.</span></span> <span data-ttu-id="1fa3e-150">Až budete hotovi, povolte v celém projektu typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-150">When you've finished, you enable nullable reference types for the entire project.</span></span>

<span data-ttu-id="1fa3e-151">Po této druhé strategii provedete následující:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-151">Following this second strategy you do the following:</span></span>

1. <span data-ttu-id="1fa3e-152">Přidejte direktivu pragma `#nullable enable` do souboru, pro který chcete nastavit nepovolenou hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-152">Add the `#nullable enable` pragma to the file you want to make nullable aware.</span></span>
1. <span data-ttu-id="1fa3e-153">Vyřešte všechna upozornění.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-153">Address any warnings.</span></span>
1. <span data-ttu-id="1fa3e-154">Pokračujte v těchto prvních dvou krocích, dokud nebudete mít k celou knihovnu s podporou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-154">Continue these first two steps until you've made the entire library nullable aware.</span></span>
1. <span data-ttu-id="1fa3e-155">Povolit typy s možnou hodnotou null pro celý projekt přidáním prvku `<Nullable>enable</Nullable>` do souborů *csproj* .</span><span class="sxs-lookup"><span data-stu-id="1fa3e-155">Enable nullable types for the entire project by adding the `<Nullable>enable</Nullable>` element to your *csproj* files.</span></span> 
1. <span data-ttu-id="1fa3e-156">Odeberte direktivy pragma `#nullable enable`, protože už nejsou potřeba.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-156">Remove the `#nullable enable` pragmas, as they're no longer needed.</span></span>

<span data-ttu-id="1fa3e-157">Tato druhá strategie má méně práce předem.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-157">This second strategy has less work up-front.</span></span> <span data-ttu-id="1fa3e-158">Kompromisy je, že první úkol při vytváření nového souboru je přidání direktivy pragma a zpřístupnění hodnoty null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-158">The tradeoff is that the first task when you create a new file is to add the pragma and make it nullable aware.</span></span> <span data-ttu-id="1fa3e-159">Pokud se některý z vývojářů v týmu zapomene, je nový kód v nedokončené práci, aby se zajistilo, že bude mít veškerý kód na hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-159">If any developers on your team forget, that new code is now in the backlog of work to make all code nullable aware.</span></span>

<span data-ttu-id="1fa3e-160">Které z těchto strategií vybíráte, závisí na tom, kolik aktivního vývoje se v projektu provádí.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-160">Which of these strategies you pick depends on how much active development is taking place in your project.</span></span> <span data-ttu-id="1fa3e-161">Tím se zlepší a stabilnější váš projekt, tím lepší je druhá strategie.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-161">The more mature and stable your project, the better the second strategy.</span></span> <span data-ttu-id="1fa3e-162">Vyvíjejí se další funkce, což je lepší první strategie.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-162">The more features being developed, the better the first strategy.</span></span>

## <a name="should-nullable-warnings-introduce-breaking-changes"></a><span data-ttu-id="1fa3e-163">Má upozornění na hodnotu null zavést průlomové změny?</span><span class="sxs-lookup"><span data-stu-id="1fa3e-163">Should nullable warnings introduce breaking changes?</span></span>

<span data-ttu-id="1fa3e-164">Než povolíte typy odkazů s možnou hodnotou null, jsou proměnné považovány za *Nullable oblivious*.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-164">Before you enable nullable reference types, variables are considered *nullable oblivious*.</span></span> <span data-ttu-id="1fa3e-165">Jakmile povolíte typy odkazů s možnou hodnotou null, všechny tyto proměnné nebudou *mít hodnotu null*.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-165">Once you enable nullable reference types, all those variables are *non-nullable*.</span></span> <span data-ttu-id="1fa3e-166">Kompilátor bude vydávat upozornění, pokud tyto proměnné nejsou inicializovány na hodnoty, které nejsou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-166">The compiler will issue warnings if those variables aren't initialized to non-null values.</span></span>

<span data-ttu-id="1fa3e-167">Dalším pravděpodobným zdrojem upozornění je vrácení hodnot, pokud hodnota nebyla inicializována.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-167">Another likely source of warnings is return values when the value hasn't been initialized.</span></span>

<span data-ttu-id="1fa3e-168">Prvním krokem při adresování upozornění kompilátoru je použití `?` anotace u parametrů a návratových typů, které označují, kdy argumenty nebo návratové hodnoty mohou být null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-168">The first step in addressing the compiler warnings is to use `?` annotations on parameter and return types to indicate when arguments or return values may be null.</span></span> <span data-ttu-id="1fa3e-169">Pokud referenční proměnné nesmí mít hodnotu null, je původní deklarace správná.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-169">When reference variables must not be null, the original declaration is correct.</span></span> <span data-ttu-id="1fa3e-170">V takovém případě váš cíl nestačí jenom opravit upozornění.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-170">As you do this, your goal isn't just to fix warnings.</span></span> <span data-ttu-id="1fa3e-171">Důležitější je, že kompilátor porozuměl vašemu záměru pro potenciální hodnoty null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-171">The more important goal is to make the compiler understand your intent for potential null values.</span></span> <span data-ttu-id="1fa3e-172">Při kontrole upozornění se dostanete k vašemu dalšímu hlavnímu rozhodnutí o vaší knihovně.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-172">As you examine the warnings, you reach your next major decision for your library.</span></span> <span data-ttu-id="1fa3e-173">Chcete zvážit úpravu signatur rozhraní API, abyste mohli snadněji sdělit záměr návrhu?</span><span class="sxs-lookup"><span data-stu-id="1fa3e-173">Do you want to consider modifying API signatures to more clearly communicate your design intent?</span></span> <span data-ttu-id="1fa3e-174">Lepší signatura rozhraní API pro metodu `TryGetMessage` prošetřená výše může být:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-174">A better API signature for the `TryGetMessage` method examined earlier could be:</span></span>

```csharp
string? TryGetMessage(string key);
```

<span data-ttu-id="1fa3e-175">Vrácená hodnota označuje úspěch nebo neúspěch a přenese hodnotu, pokud byla hodnota nalezena.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-175">The return value indicates success or failure, and carries the value if the value was found.</span></span> <span data-ttu-id="1fa3e-176">V mnoha případech může změna signatur rozhraní API zlepšit způsob, jakým komunikuje hodnoty null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-176">In many cases, changing API signatures can improve how they communicate null values.</span></span>

<span data-ttu-id="1fa3e-177">Pro veřejné knihovny nebo knihovny s velkými základy uživatelů ale můžete raději nezavádět změny signatur rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-177">However, for public libraries, or libraries with large user bases, you may prefer not introducing any API signature changes.</span></span> <span data-ttu-id="1fa3e-178">Pro tyto případy a další běžné vzory můžete použít atributy pro snadnější definování, když argument nebo návratová hodnota může být `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-178">For those cases, and other common patterns, you can apply attributes to more clearly define when an argument or return value may be `null`.</span></span> <span data-ttu-id="1fa3e-179">Bez ohledu na to, zda zvažte změnu povrchu rozhraní API, pravděpodobně zjistíte, že tyto anotace typu nejsou dostačující pro popis hodnot `null` pro argumenty nebo návratové hodnoty.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-179">Whether or not you consider changing the surface of your API, you'll likely find that type annotations alone aren't sufficient for describing `null` values for arguments or return values.</span></span> <span data-ttu-id="1fa3e-180">V těchto případech můžete použít atributy pro přehlednější Popis rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-180">In those instances, you can apply attributes to more clearly describe an API.</span></span> 

## <a name="attributes-extend-type-annotations"></a><span data-ttu-id="1fa3e-181">Atributy rozšiřuje anotace typu</span><span class="sxs-lookup"><span data-stu-id="1fa3e-181">Attributes extend type annotations</span></span>

<span data-ttu-id="1fa3e-182">Bylo přidáno několik atributů pro vyjádření dalších informací o stavu hodnoty null proměnných.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-182">Several attributes have been added to express additional information about the null state of variables.</span></span> <span data-ttu-id="1fa3e-183">Veškerý kód, který jste C# napsali před 8 představenými typy odkazů s možnou hodnotou null, byl *null oblivious*</span><span class="sxs-lookup"><span data-stu-id="1fa3e-183">All code you wrote before C# 8 introduced nullable reference types was *null oblivious*.</span></span> <span data-ttu-id="1fa3e-184">To znamená, že jakákoli proměnná typu odkazu může mít hodnotu null, ale kontroly hodnoty null nejsou požadovány.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-184">That means any reference type variable may be null, but null checks aren't required.</span></span> <span data-ttu-id="1fa3e-185">Jakmile kód bude *mít na paměti hodnotu s možnou hodnotou null*, změní se tato pravidla.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-185">Once your code is *nullable aware*, those rules change.</span></span> <span data-ttu-id="1fa3e-186">Odkazové typy by nikdy neměly být hodnotou `null` a typy odkazů s možnou hodnotou null musí být před zpětným odkazem zkontrolovány na `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-186">Reference types should never be the `null` value, and nullable reference types must be checked against `null` before being dereferenced.</span></span>

<span data-ttu-id="1fa3e-187">Pravidla pro vaše rozhraní API jsou pravděpodobně složitější, jak jste viděli ve scénáři `TryGetValue` rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-187">The rules for your APIs are likely more complicated, as you saw with the `TryGetValue` API scenario.</span></span> <span data-ttu-id="1fa3e-188">Mnohé z vašich rozhraní API mají složitější pravidla, kdy proměnné můžou nebo nemůžou být `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-188">Many of your APIs have more complex rules for when variables can or can't be `null`.</span></span> <span data-ttu-id="1fa3e-189">V těchto případech použijete jeden z následujících atributů pro vyjádření těchto pravidel:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-189">In these cases, you'll use one of the following attributes to express those rules:</span></span>

- <span data-ttu-id="1fa3e-190">[AllowNull má](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): vstupní argument, který nemůže mít hodnotu null, může mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-190">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): A non-nullable input argument may be null.</span></span>
- <span data-ttu-id="1fa3e-191">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): vstupní argument s možnou hodnotou null by nikdy neměl mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-191">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): A nullable input argument should never be null.</span></span>
- <span data-ttu-id="1fa3e-192">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): návratová hodnota, která není null, může mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-192">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): A non-nullable return value may be null.</span></span>
- <span data-ttu-id="1fa3e-193">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): návratová hodnota s možnou hodnotou null nikdy nebude null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-193">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): A nullable return value will never be null.</span></span>
- <span data-ttu-id="1fa3e-194">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): vstupní argument, který nemůže mít hodnotu null, může mít hodnotu null, pokud metoda vrátí zadanou hodnotu `bool`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-194">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): A non-nullable input argument may be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="1fa3e-195">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): vstupní argument s možnou hodnotou null nebude mít hodnotu null, pokud metoda vrátí zadanou hodnotu `bool`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-195">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): A nullable input argument will not be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="1fa3e-196">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): návratová hodnota není null, pokud argument pro zadaný parametr není null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-196">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): A return value isn't null if the argument for the specified parameter isn't null.</span></span>

<span data-ttu-id="1fa3e-197">Předchozí popisy představují rychlý odkaz na to, co každý atribut dělá.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-197">The preceding descriptions are a quick reference to what each attribute does.</span></span> <span data-ttu-id="1fa3e-198">Každá z následujících částí popisuje chování a lepší význam.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-198">Each following section describes the behavior and meaning more thoroughly.</span></span>

<span data-ttu-id="1fa3e-199">Přidáním těchto atributů poskytne kompilátor více informací o pravidlech pro vaše rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-199">Adding these attributes gives the compiler more information about the rules for your API.</span></span> <span data-ttu-id="1fa3e-200">Při volání kódu v kontextu s povolenou hodnotou null, kompilátor upozorní volající, když porušují tato pravidla.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-200">When calling code is compiled in a nullable enabled context, the compiler will warn callers when they violate those rules.</span></span> <span data-ttu-id="1fa3e-201">Tyto atributy neumožňují u implementace další kontroly.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-201">These attributes don't enable additional checks on your implementation.</span></span>

## <a name="specify-preconditions-allownull-and-disallownull"></a><span data-ttu-id="1fa3e-202">Zadejte předpoklady: `AllowNull` a `DisallowNull`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-202">Specify preconditions: `AllowNull` and `DisallowNull`</span></span>

<span data-ttu-id="1fa3e-203">Zvažte vlastnost pro čtení a zápis, která nikdy nevrací `null`, protože má rozumnou výchozí hodnotu.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-203">Consider a read/write property that never returns `null` because it has a reasonable default value.</span></span> <span data-ttu-id="1fa3e-204">Volající přecházejí `null` do přístupového objektu set při jeho nastavování na tuto výchozí hodnotu.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-204">Callers pass `null` to the set accessor when setting it to that default value.</span></span> <span data-ttu-id="1fa3e-205">Představte si třeba systém zasílání zpráv, který se zeptá na název obrazovky v chatovací místnosti.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-205">For example, consider a messaging system that asks for a screen name in a chat room.</span></span> <span data-ttu-id="1fa3e-206">Pokud není k dispozici žádný, systém vygeneruje náhodný název:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-206">If none is provided, the system generates a random name:</span></span>

```csharp
public string ScreenName
{
   get => screenName;
   set => screenName = value ?? GenerateRandomScreenName();
}
private string screenName;
```

<span data-ttu-id="1fa3e-207">Pokud kompilujete předchozí kód v oblivious kontextu s možnou hodnotou null, vše je dobré.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-207">When you compile the preceding code in a nullable oblivious context, everything is fine.</span></span> <span data-ttu-id="1fa3e-208">Po povolení typů odkazů s možnou hodnotou null se vlastnost `ScreenName` stala odkazem, který neumožňuje hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-208">Once you enable nullable reference types, the `ScreenName` property becomes a non-nullable reference.</span></span> <span data-ttu-id="1fa3e-209">To je správné pro přístup `get`: nikdy nevrátí `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-209">That's correct for the `get` accessor: it never returns `null`.</span></span> <span data-ttu-id="1fa3e-210">Volající nepotřebují kontrolovat vrácenou vlastnost pro `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-210">Callers don't need to check the returned property for `null`.</span></span> <span data-ttu-id="1fa3e-211">Ale nyní nastavuje vlastnost na `null` vygeneruje upozornění.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-211">But now setting the property to `null` generates a warning.</span></span> <span data-ttu-id="1fa3e-212">Aby bylo možné pokračovat v podpoře tohoto typu kódu, přidejte do vlastnosti atribut <xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute?displayProperty=nameWithType>, jak je znázorněno v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-212">In order to continue to support this type of code, you add the <xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute?displayProperty=nameWithType> attribute to the property, as shown in the following code:</span></span> 

```csharp
[AllowNull]
public string ScreenName
{
   get => screenName;
   set => screenName = value ?? GenerateRandomScreenName();
}
private string screenName = GenerateRandomScreenName();
```

<span data-ttu-id="1fa3e-213">Pro použití tohoto a dalších atributů, které jsou popsány v tomto článku, bude pravděpodobně nutné přidat direktivu `using` pro <xref:System.Diagnostics.CodeAnalysis>.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-213">You may need to add a `using` directive for <xref:System.Diagnostics.CodeAnalysis> to use this and other attributes discussed in this article.</span></span> <span data-ttu-id="1fa3e-214">Atribut se použije na vlastnost, nikoli přístupový objekt `set`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-214">The attribute is applied to the property, not the `set` accessor.</span></span> <span data-ttu-id="1fa3e-215">Atribut `AllowNull` určuje *předběžné podmínky*a vztahuje se pouze na vstupy.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-215">The `AllowNull` attribute specifies *pre-conditions*, and only applies to inputs.</span></span> <span data-ttu-id="1fa3e-216">Přístupový objekt `get` má návratovou hodnotu, ale žádné vstupní argumenty.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-216">The `get` accessor has a return value, but no input arguments.</span></span> <span data-ttu-id="1fa3e-217">Proto atribut `AllowNull` platí pouze pro přístup k přístupovému objektu `set`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-217">Therefore, the `AllowNull` attribute only applies to the `set` accessor.</span></span>

<span data-ttu-id="1fa3e-218">Předchozí příklad ukazuje, co je třeba najít při přidávání atributu `AllowNull` v argumentu:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-218">The preceding example demonstrates what to look for when adding the `AllowNull` attribute on an argument:</span></span>

1. <span data-ttu-id="1fa3e-219">Obecnou smlouvou pro tuto proměnnou je, že by neměla být `null`, takže chcete typ odkazu, který nepovoluje hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-219">The general contract for that variable is that it shouldn't be `null`, so you want a non-nullable reference type.</span></span>
1. <span data-ttu-id="1fa3e-220">Existují situace, kdy vstupní proměnná bude `null`, i když nejsou nejběžnějším využitím.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-220">There are scenarios for the input variable to be `null`, though they aren't the most common usage.</span></span>

<span data-ttu-id="1fa3e-221">Nejčastěji budete potřebovat tento atribut pro vlastnosti nebo argumenty `in`, `out` a `ref`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-221">Most often you'll need this attribute for properties, or `in`, `out`, and `ref` arguments.</span></span> <span data-ttu-id="1fa3e-222">Je-li proměnná obvykle nenulová, je nejlepší volbou atribut `AllowNull`, ale je třeba v předběžných podmínkách umožnit `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-222">The `AllowNull` attribute is the best choice when a variable is typically non-null, but you need to allow `null` as a precondition.</span></span>

<span data-ttu-id="1fa3e-223">Na rozdíl od scénářů použití `DisallowNull`: pomocí tohoto atributu určíte, že vstupní proměnná typu s možnou hodnotou null by neměla být `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-223">Contrast that with scenarios for using `DisallowNull`: You use this attribute to specify that an input variable of a nullable type shouldn't be `null`.</span></span> <span data-ttu-id="1fa3e-224">Vezměte v úvahu vlastnost, kde `null` je výchozí hodnota, ale klienti ji můžou nastavit jenom na jinou hodnotu než null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-224">Consider a property where `null` is the default value, but clients can only set it to a non-null value.</span></span> <span data-ttu-id="1fa3e-225">Vezměte v úvahu následující kód:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-225">Consider the following code:</span></span>

```csharp
public string ReviewComment
{
    get => _comment;
    set => _comment = value ?? throw new ArgumentNullException(nameof(value), "Cannot set to null");
}
string _comment;
```

<span data-ttu-id="1fa3e-226">Předchozí kód je nejlepším způsobem, jak vyjádřit návrh, že `ReviewComment` by mohlo být `null`, ale nelze je nastavit na `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-226">The preceding code is the best way to express your design that the `ReviewComment` could be `null`, but can't be set to `null`.</span></span> <span data-ttu-id="1fa3e-227">Jakmile tento kód bude mít na paměti hodnotu s možnou hodnotou null, můžete tento koncept zřetelně vyjádřit volajícím pomocí <xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute?displayProperty=nameWithType>:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-227">Once this code is nullable aware, you can express this concept more clearly to callers using the <xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute?displayProperty=nameWithType>:</span></span>

```csharp
[DisallowNull] 
public string? ReviewComment
{
    get => _comment;
    set => _comment = value ?? throw new ArgumentNullException(nameof(value), "Cannot set to null");
}
string? _comment;
```

<span data-ttu-id="1fa3e-228">V kontextu s možnou hodnotou null může přístupový objekt `ReviewComment` `get` vrátit výchozí hodnotu `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-228">In a nullable context, the `ReviewComment` `get` accessor could return the default value of `null`.</span></span> <span data-ttu-id="1fa3e-229">Kompilátor upozorní, že před přístupem musí být zkontrolován.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-229">The compiler warns that it must be checked before access.</span></span> <span data-ttu-id="1fa3e-230">Kromě toho upozorňuje volající, že i když by to bylo `null`, volající by se neměli explicitně nastavit na `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-230">Furthermore, it warns callers that, even though it could be `null`, callers shouldn't explicitly set it to `null`.</span></span> <span data-ttu-id="1fa3e-231">Atribut `DisallowNull` také určuje *předběžnou podmínku*, nemá vliv na přistupující objekt `get`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-231">The `DisallowNull` attribute also specifies a *pre-condition*, it does not affect the `get` accessor.</span></span> <span data-ttu-id="1fa3e-232">Pokud si vyberete tyto vlastnosti, měli byste použít atribut `DisallowNull`:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-232">You should choose to use the `DisallowNull` attribute when you observe these characteristics about:</span></span>

1. <span data-ttu-id="1fa3e-233">Proměnná by mohla být `null` v základních scénářích, často při první instanci.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-233">The variable could be `null` in core scenarios, often when first instantiated.</span></span>
1. <span data-ttu-id="1fa3e-234">Proměnná by neměla být explicitně nastavená na `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-234">The variable shouldn't be explicitly set to `null`.</span></span>

<span data-ttu-id="1fa3e-235">Tyto situace jsou běžné v kódu, který byl původně *null oblivious*.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-235">These situations are common in code that was originally *null oblivious*.</span></span> <span data-ttu-id="1fa3e-236">Je možné, že vlastnosti objektu jsou nastaveny ve dvou různých inicializačních operacích.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-236">It may be that object properties are set in two distinct initialization operations.</span></span> <span data-ttu-id="1fa3e-237">Je možné, že některé vlastnosti jsou nastaveny až po dokončení některé asynchronní práce.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-237">It may be that some properties are set only after some asynchronous work has completed.</span></span>

<span data-ttu-id="1fa3e-238">Atributy `AllowNull` a `DisallowNull` umožňují určit, že představy pro proměnné nemusí odpovídat na tyto proměnné anotace s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-238">The `AllowNull` and `DisallowNull` attributes enable you to specify that preconditions on variables may not match the nullable annotations on those variables.</span></span> <span data-ttu-id="1fa3e-239">Poskytují podrobnější informace o vlastnostech vašeho rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-239">These provide more detail about the characteristics of your API.</span></span> <span data-ttu-id="1fa3e-240">Tyto další informace pomáhají volajícímu používat rozhraní API správně.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-240">This additional information helps callers use your API correctly.</span></span> <span data-ttu-id="1fa3e-241">Nezapomeňte zadat předpoklady pomocí následujících atributů:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-241">Remember you specify preconditions using the following attributes:</span></span>

- <span data-ttu-id="1fa3e-242">[AllowNull má](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): vstupní argument, který nemůže mít hodnotu null, může mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-242">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): A non-nullable input argument may be null.</span></span>
- <span data-ttu-id="1fa3e-243">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): vstupní argument s možnou hodnotou null by nikdy neměl mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-243">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): A nullable input argument should never be null.</span></span>

## <a name="specify-post-conditions-maybenull-and-notnull"></a><span data-ttu-id="1fa3e-244">Zadejte podmínky post-Conditions: `MaybeNull` a `NotNull`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-244">Specify post-conditions: `MaybeNull` and `NotNull`</span></span>

<span data-ttu-id="1fa3e-245">Předpokládejme, že máte metodu s následujícím podpisem:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-245">Suppose you have a method with the following signature:</span></span>

```csharp
public Customer FindCustomer(string lastName, string firstName)
```

<span data-ttu-id="1fa3e-246">Pravděpodobně jste napsali metodu, jako to, aby vracela `null`, když se hledané jméno nenašlo.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-246">You've likely written a method like this to return `null` when the name sought wasn't found.</span></span> <span data-ttu-id="1fa3e-247">`null` jasně indikuje, že se záznam nenašel.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-247">The `null` clearly indicates that the record wasn't found.</span></span> <span data-ttu-id="1fa3e-248">V tomto příkladu jste pravděpodobně změnili návratový typ z `Customer` na `Customer?`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-248">In this example, you'd likely change the return type from `Customer` to `Customer?`.</span></span> <span data-ttu-id="1fa3e-249">Deklarace návratové hodnoty jako typ odkazu s možnou hodnotou null určuje záměr tohoto rozhraní API jasně.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-249">Declaring the return value as a nullable reference type specifies the intent of this API clearly.</span></span> 

<span data-ttu-id="1fa3e-250">Z důvodů uvedených v rámci [obecných definic a možností použití hodnoty null](#generic-definitions-and-nullability) , které technika nepracuje s obecnými metodami.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-250">For reasons covered under [Generic definitions and nullability](#generic-definitions-and-nullability) that technique does not work with generic methods.</span></span> <span data-ttu-id="1fa3e-251">Je možné, že budete mít obecnou metodu, která následuje podobný vzor:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-251">You may have a generic method that follows a similar pattern:</span></span>

```csharp
public T Find<T>(IEnumerable<T> sequence, Func<T, bool> match)
```

<span data-ttu-id="1fa3e-252">Nelze určit, že návratová hodnota je `T?`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-252">You can't specify that the return value is `T?`.</span></span> <span data-ttu-id="1fa3e-253">Metoda vrátí `null`, pokud se požadovaná položka nenajde.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-253">The method returns `null` when the sought item isn't found.</span></span> <span data-ttu-id="1fa3e-254">Vzhledem k tomu, že nemůžete deklarovat návratový typ `T?`, přidáte do metody návratovou poznámku `MaybeNull`:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-254">Since you can't declare a `T?` return type, you add the `MaybeNull` annotation to the method return:</span></span>

```csharp
[return: MaybeNull]
public T Find<T>(IEnumerable<T> sequence, Func<T, bool> match)
```

<span data-ttu-id="1fa3e-255">Předchozí kód informuje volající, že kontrakt implikuje typ, který nemůže mít hodnotu null, ale vrácená hodnota *může* být null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-255">The preceding code informs callers that the contract implies a non-nullable type, but the return value *may* actually be null.</span></span>  <span data-ttu-id="1fa3e-256">Použijte atribut `MaybeNull`, pokud by vaše rozhraní API mělo být typ, který není null, obvykle parametr obecného typu, ale může se jednat o instance, kde by se vrátila `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-256">Use the `MaybeNull` attribute when your API should be a non-nullable type, typically a generic type parameter, but there may be instances where `null` would be returned.</span></span>

<span data-ttu-id="1fa3e-257">Můžete také určit, že návratová hodnota nebo argument `out` nebo `ref` není null, přestože typ je typ s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-257">You can also specify that a return value or an `out` or `ref` argument isn't null even though the type is a nullable type.</span></span> <span data-ttu-id="1fa3e-258">Vezměte v úvahu metodu, která zajistí, že je pole dostatečně velké pro udržení počtu prvků.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-258">Consider a method that ensures an array is large enough to hold a number of elements.</span></span> <span data-ttu-id="1fa3e-259">Pokud vstupní argument nemá kapacitu, rutina přidělí nové pole a zkopíruje do něj všechny existující prvky.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-259">If the input argument doesn't have capacity, the routine would allocate a new array and copy all the existing elements into it.</span></span> <span data-ttu-id="1fa3e-260">Pokud je vstupním argumentem `null`, rutina by přidělila nové úložiště.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-260">If the input argument is `null`, the routine would allocate new storage.</span></span> <span data-ttu-id="1fa3e-261">Pokud existuje dostatečná kapacita, rutina neprovede nic:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-261">If there's sufficient capacity, the routine does nothing:</span></span>

```csharp
public void EnsureCapacity<T>(ref T[] storage, int size)
```

<span data-ttu-id="1fa3e-262">Tuto rutinu můžete zavolat následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-262">You could call this routine as follows:</span></span>

```csharp
// messages has the default value (null) when EnsureCapacity is called:
EnsureCapacity<string>(ref messages, 10);
// messages is not null.
EnsureCapacity<string>(messages, 50);
```

<span data-ttu-id="1fa3e-263">Po povolení typů odkazů s hodnotou null Chcete zajistit, aby předchozí kód byl zkompilován bez upozornění.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-263">After enabling null reference types, you want to ensure that the preceding code compiles without warnings.</span></span> <span data-ttu-id="1fa3e-264">Když metoda vrátí hodnotu, argument `storage` zaručuje, že nesmí mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-264">When the method returns, the `storage` argument is guaranteed to be not null.</span></span> <span data-ttu-id="1fa3e-265">Je však přijatelné volat `EnsureCapacity` s odkazem s hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-265">However, it's acceptable to call `EnsureCapacity` with a null reference.</span></span> <span data-ttu-id="1fa3e-266">Můžete nastavit `storage` typ odkazu s možnou hodnotou null a přidat podmínku `NotNull` po deklaraci parametru:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-266">You can make `storage` a nullable reference type, and add the `NotNull` post-condition to the parameter declaration:</span></span>

```csharp
public void EnsureCapacity<T>([NotNull]ref T[]? storage, int size)
```

<span data-ttu-id="1fa3e-267">Předchozí kód vyjadřuje jednoznačně stávající kontrakt: volající mohou předat proměnnou s hodnotou `null`, ale návratová hodnota je zaručena, že nikdy nebude mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-267">The preceding code expresses the existing contract very clearly: Callers can pass a variable with the `null` value, but the return value is guaranteed to never be null.</span></span> <span data-ttu-id="1fa3e-268">Atribut `NotNull` je nejužitečnější pro argumenty `ref` a `out`, kde `null` může být předán jako argument, ale u tohoto argumentu je zaručeno, že nebude mít hodnotu null, pokud se metoda vrátí.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-268">The `NotNull` attribute is most useful for `ref` and `out` arguments where `null` may be passed as an argument, but that argument is guaranteed to be not null when the method returns.</span></span>

<span data-ttu-id="1fa3e-269">Nepodmíněné následné podmínky se zadává pomocí následujících atributů:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-269">You specify unconditional postconditions using the following attributes:</span></span>

- <span data-ttu-id="1fa3e-270">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): návratová hodnota, která není null, může mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-270">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): A non-nullable return value may be null.</span></span>
- <span data-ttu-id="1fa3e-271">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): návratová hodnota s možnou hodnotou null nikdy nebude null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-271">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): A nullable return value will never be null.</span></span>

## <a name="specify-conditional-post-conditions-notnullwhen-maybenullwhen-and-notnullifnotnull"></a><span data-ttu-id="1fa3e-272">Zadejte podmíněné podmínky odeslání: `NotNullWhen`, `MaybeNullWhen` a `NotNullIfNotNull`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-272">Specify conditional post-conditions: `NotNullWhen`, `MaybeNullWhen`, and `NotNullIfNotNull`</span></span>

<span data-ttu-id="1fa3e-273">Pravděpodobně jste obeznámeni s metodou `string` <xref:System.String.IsNullOrEmpty(System.String)?DisplayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-273">You're likely familiar with the `string` method <xref:System.String.IsNullOrEmpty(System.String)?DisplayProperty=nameWithType>.</span></span> <span data-ttu-id="1fa3e-274">Tato metoda vrací `true`, pokud má argument hodnotu null nebo prázdný řetězec.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-274">This method returns `true` when the argument is null or an empty string.</span></span> <span data-ttu-id="1fa3e-275">Jedná se o podobu hodnoty null-check: volající nemusí mít hodnotu null. Pokud metoda vrátí hodnotu `false`, zaškrtnete argument.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-275">It's a form of null-check: Callers don't need to null-check the argument if the method returns `false`.</span></span> <span data-ttu-id="1fa3e-276">Chcete-li vytvořit metodu, jako je tato vlastnost s možnou hodnotou null, nastavte argument na typ s možnou hodnotou null a přidejte atribut `NotNullWhen`:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-276">To make a method like this nullable aware, you'd set the argument to a nullable type, and add the `NotNullWhen` attribute:</span></span>

```csharp
bool IsNullOrEmpty([NotNullWhen(false)]string? value);
```

<span data-ttu-id="1fa3e-277">Informuje kompilátor o tom, že jakýkoli kód, kde je vrácená hodnota `false` nemusí být zaškrtnutou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-277">That informs the compiler that any code where the return value is `false` need not be null-checked.</span></span> <span data-ttu-id="1fa3e-278">Přidání atributu informuje o statické analýze kompilátoru, které `IsNullOrEmpty` provádí potřebnou kontrolu null: když vrátí `false`, vstupní argument není `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-278">The addition of the attribute informs the compiler's static analysis that `IsNullOrEmpty` performs the necessary null check: when it returns `false`, the input argument is not `null`.</span></span>

```csharp
string? userInput = GetUserInput();
if (!(string.IsNullOrEmpty(userInput))
{
   int messageLength = userInput.Length; // no null check needed.
}
// null check needed on userInput here.
```

<span data-ttu-id="1fa3e-279">Metoda <xref:System.String.IsNullOrEmpty(System.String)?DisplayProperty=nameWithType> bude označena jako uvedená výše pro .NET Core 3,0.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-279">The <xref:System.String.IsNullOrEmpty(System.String)?DisplayProperty=nameWithType> method will be annotated as shown above for .NET Core 3.0.</span></span> <span data-ttu-id="1fa3e-280">Můžete mít podobné metody v základu kódu, které kontrolují stav objektů pro hodnoty null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-280">You may have similar methods in your codebase that check the state of objects for null values.</span></span> <span data-ttu-id="1fa3e-281">Kompilátor nerozpozná vlastní metody kontroly null a bude nutné poznámky přidat sami.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-281">The compiler won't recognize custom null check methods, and you'll need to add the annotations yourself.</span></span> <span data-ttu-id="1fa3e-282">Když přidáte atribut, statická analýza kompilátoru zná, když byla testovaná proměnná vrácena na hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-282">When you add the attribute, the compiler's static analysis knows when the tested variable has been null checked.</span></span>

<span data-ttu-id="1fa3e-283">Jiné použití pro tyto atributy je `Try*` vzor.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-283">Another use for these attributes is the `Try*` pattern.</span></span> <span data-ttu-id="1fa3e-284">Následné podmínky pro proměnné `ref` a `out` jsou přenášeny prostřednictvím návratové hodnoty.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-284">The postconditions for `ref` and `out` variables are communicated through the return value.</span></span> <span data-ttu-id="1fa3e-285">Zvažte tuto metodu popsanou výše:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-285">Consider this method shown earlier:</span></span>

```csharp
bool TryGetMessage(string key, out string message)
```

<span data-ttu-id="1fa3e-286">Předchozí metoda dodržuje typické rozhraní .NET idiom: vrácená hodnota označuje, zda byl `message` nastaven na zjištěnou hodnotu, nebo pokud není nalezena žádná zpráva, na výchozí hodnotu.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-286">The preceding method follows a typical .NET idiom: the return value indicates if `message` was set to the found value or, if no message is found, to the default value.</span></span> <span data-ttu-id="1fa3e-287">Pokud metoda vrátí hodnotu `true`, hodnota `message` není null; v opačném případě metoda nastaví `message` na hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-287">If the method returns `true`, the value of `message` isn't null; otherwise, the method sets `message` to null.</span></span>

<span data-ttu-id="1fa3e-288">Pomocí atributu `NotNullWhen` můžete komunikovat s tímto idiom.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-288">You can communicate that idiom using the `NotNullWhen` attribute.</span></span> <span data-ttu-id="1fa3e-289">Když aktualizujete podpis pro typy odkazů s možnou hodnotou null, udělejte `message` a `string?` a přidejte atribut:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-289">When you update the signature for nullable reference types, make `message` a `string?` and add an attribute:</span></span>

```csharp
bool TryGetMessage(string key, [NotNullWhen(true)] out string? message)
```

<span data-ttu-id="1fa3e-290">V předchozím příkladu je hodnota `message` v případě, že `TryGetMessage` vrátí `true`, považována za null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-290">In the preceding example, the value of `message` is known to be not null when `TryGetMessage` returns `true`.</span></span> <span data-ttu-id="1fa3e-291">Podobné metody v základu kódu je vhodné opatřit stejným způsobem: argumenty mohou být `null` a známé jako nenulové, pokud metoda vrátí `true`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-291">You should annotate similar methods in your codebase in the same way: the arguments could be `null`, and are known to be not null when the method returns `true`.</span></span>

<span data-ttu-id="1fa3e-292">Je možné, že budete potřebovat i jeden konečný atribut.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-292">There's one final attribute you may also need.</span></span> <span data-ttu-id="1fa3e-293">V některých případech je nulový stav návratové hodnoty závislý na nulovém stavu jednoho nebo více vstupních argumentů.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-293">Sometimes the null state of a return value depends on the null state of one or more input arguments.</span></span> <span data-ttu-id="1fa3e-294">Tyto metody vrátí hodnotu, která není null, pokud některé vstupní argumenty nejsou `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-294">These methods will return a non-null value whenever certain input arguments aren't `null`.</span></span> <span data-ttu-id="1fa3e-295">Chcete-li tyto metody správně opatřit poznámkami, použijte atribut `NotNullIfNotNull`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-295">To correctly annotate these methods, you use the `NotNullIfNotNull` attribute.</span></span> <span data-ttu-id="1fa3e-296">Vezměte v úvahu následující metodu:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-296">Consider the following method:</span></span>

```csharp
string GetTopLevelDomainFromFullUrl(string url);
```

<span data-ttu-id="1fa3e-297">Pokud argument `url` není null, výstup není `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-297">If the `url` argument isn't null, the output isn't `null`.</span></span> <span data-ttu-id="1fa3e-298">Jakmile jsou povoleny odkazy s hodnotou null, tento podpis funguje správně, pokud rozhraní API nikdy nepřijímá vstup s hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-298">Once nullable references are enabled, that signature works correctly, provided your API never accepts a null input.</span></span> <span data-ttu-id="1fa3e-299">Pokud ale může mít vstup hodnotu null, může být návratová hodnota také null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-299">However, if the input could be null, then return value could also be null.</span></span> <span data-ttu-id="1fa3e-300">Proto můžete změnit signaturu na následující kód:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-300">Therefore, you could change the signature to the following code:</span></span>

```csharp
string? GetTopLevelDomainFromFullUrl(string? url);
```

<span data-ttu-id="1fa3e-301">To funguje i v případě, že bude často vynutit, aby volající implementovali dodatečné kontroly `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-301">That also works, but will often force callers to implement extra `null` checks.</span></span> <span data-ttu-id="1fa3e-302">Kontraktem je, že návratová hodnota `null` pouze v případě, že je vstupní argument `url` `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-302">The contract is that the return value would be `null` only when the input argument `url` is `null`.</span></span> <span data-ttu-id="1fa3e-303">Pro vyjádření této smlouvy byste si tuto metodu měli opatřit poznámkami, jak je znázorněno v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-303">To express that contract, you would annotate this method as shown in the following code:</span></span>

```csharp
[return: NotNullIfNotNull("url")]
string? GetTopLevelDomainFromFullUrl(string? url);
```

<span data-ttu-id="1fa3e-304">Pro návratovou hodnotu a argument byly oba poznámy s `?`, což značí, že by mohlo být `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-304">The return value and the argument have both been annotated with the `?` indicating that either could be `null`.</span></span> <span data-ttu-id="1fa3e-305">Atribut dále upřesňuje, že návratová hodnota nebude null, pokud argument `url` není `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-305">The attribute further clarifies that the return value won't be null when the `url` argument isn't `null`.</span></span>

<span data-ttu-id="1fa3e-306">Podmíněný následné podmínky můžete zadat pomocí těchto atributů:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-306">You specify conditional postconditions using these attributes:</span></span>

- <span data-ttu-id="1fa3e-307">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): vstupní argument, který nemůže mít hodnotu null, může mít hodnotu null, pokud metoda vrátí zadanou hodnotu `bool`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-307">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): A non-nullable input argument may be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="1fa3e-308">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): vstupní argument s možnou hodnotou null nebude mít hodnotu null, pokud metoda vrátí zadanou hodnotu `bool`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-308">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): A nullable input argument will not be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="1fa3e-309">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): návratová hodnota není null, pokud vstupní argument pro zadaný parametr není null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-309">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): A return value isn't null if the input argument for the specified parameter isn't null.</span></span>

## <a name="generic-definitions-and-nullability"></a><span data-ttu-id="1fa3e-310">Obecné definice a možnost použití hodnoty null</span><span class="sxs-lookup"><span data-stu-id="1fa3e-310">Generic definitions and nullability</span></span>

<span data-ttu-id="1fa3e-311">Správně komunikující stav null obecných typů a obecné metody vyžadují zvláštní péči.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-311">Correctly communicating the null state of generic types and generic methods requires special care.</span></span> <span data-ttu-id="1fa3e-312">To je fakt, že typ hodnoty s možnou hodnotou null a typ odkazu s možnou hodnotou null jsou zásadním rozdílem.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-312">This stems from the fact that a nullable value type and a nullable reference type are fundamentally different.</span></span> <span data-ttu-id="1fa3e-313">`int?` je synonymum pro `Nullable<int>`, zatímco `string?` je `string` s atributem přidaným kompilátorem.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-313">An `int?` is a synonym for `Nullable<int>`, whereas `string?` is `string` with an attribute added by the compiler.</span></span> <span data-ttu-id="1fa3e-314">Výsledkem je, že kompilátor nemůže vygenerovat správný kód pro `T?` bez vědomí, zda `T` je `class` nebo `struct`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-314">The result is that the compiler can't generate correct code for `T?` without knowing if `T` is a `class` or a `struct`.</span></span> 

<span data-ttu-id="1fa3e-315">To neznamená, že nemůžete použít typ s povolenou hodnotou null (buď typ hodnoty, nebo odkazový typ) jako argument typu pro uzavřený obecný typ.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-315">This doesn't mean you can't use a nullable type (either value type or reference type) as the type argument for a closed generic type.</span></span> <span data-ttu-id="1fa3e-316">`List<string?>` i `List<int?>` jsou platné instance `List<T>`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-316">Both `List<string?>` and `List<int?>` are valid instantiations of `List<T>`.</span></span> 

<span data-ttu-id="1fa3e-317">To znamená, že nemůžete použít `T?` v deklaraci obecné třídy nebo metody bez omezení.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-317">What it does mean is that you can't use `T?` in a generic class or method declaration without constraints.</span></span> <span data-ttu-id="1fa3e-318"><xref:System.Linq.Enumerable.FirstOrDefault%60%601(System.Collections.Generic.IEnumerable%7B%60%600%7D)?displayProperty=nameWithType> například nebude změněno, aby vracela `T?`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-318">For example, <xref:System.Linq.Enumerable.FirstOrDefault%60%601(System.Collections.Generic.IEnumerable%7B%60%600%7D)?displayProperty=nameWithType> won't be changed to return `T?`.</span></span> <span data-ttu-id="1fa3e-319">Toto omezení můžete překonat přidáním omezení `struct` nebo `class`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-319">You can overcome this limitation by adding either the `struct` or `class` constraint.</span></span> <span data-ttu-id="1fa3e-320">U některého z těchto omezení kompilátor ví, jak generovat kód pro `T` i `T?`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-320">With either of those constraints, the compiler knows how to generate code for both `T` and `T?`.</span></span>

<span data-ttu-id="1fa3e-321">Můžete chtít omezit typy používané pro argument obecného typu, aby byly typy bez hodnoty null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-321">You may want to restrict the types used for a generic type argument to be non-nullable types.</span></span> <span data-ttu-id="1fa3e-322">To lze provést přidáním omezení `notnull` u tohoto argumentu typu.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-322">You can do that by adding the `notnull` constraint on that type argument.</span></span> <span data-ttu-id="1fa3e-323">Při použití tohoto omezení nesmí argument type být typ s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-323">When that constraint is applied, the type argument must not be a nullable type.</span></span>

## <a name="conclusions"></a><span data-ttu-id="1fa3e-324">Závěry</span><span class="sxs-lookup"><span data-stu-id="1fa3e-324">Conclusions</span></span>

<span data-ttu-id="1fa3e-325">Přidání typů odkazů s možnou hodnotou null poskytuje počáteční slovník pro popis očekávání vašich rozhraní API pro proměnné, které by mohly být `null`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-325">Adding nullable reference types provides an initial vocabulary to describe your APIs expectations for variables that could be `null`.</span></span> <span data-ttu-id="1fa3e-326">Další atributy poskytují bohatší slovník popisující stav null proměnných jako předpoklady a následné podmínky.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-326">The additional attributes provide a richer vocabulary to describe the null state of variables as preconditions and postconditions.</span></span> <span data-ttu-id="1fa3e-327">Tyto atributy podrobněji popisují vaše očekávání a poskytují lepší prostředí pro vývojáře pomocí vašich rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-327">These attributes more clearly describe your expectations and provide a better experience for the developers using your APIs.</span></span>

<span data-ttu-id="1fa3e-328">Když aktualizujete knihovny pro kontext s možnou hodnotou null, přidejte tyto atributy, aby uživatelé vašich rozhraní API měli k správné využití.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-328">As you update libraries for a nullable context, add these attributes to guide users of your APIs to the correct usage.</span></span> <span data-ttu-id="1fa3e-329">Tyto atributy vám pomůžou plně popsat stav hodnoty null vstupních argumentů a vrácených hodnot:</span><span class="sxs-lookup"><span data-stu-id="1fa3e-329">These attributes help you fully describe the null-state of input arguments and return values:</span></span>

- <span data-ttu-id="1fa3e-330">[AllowNull má](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): vstupní argument, který nemůže mít hodnotu null, může mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-330">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): A non-nullable input argument may be null.</span></span>
- <span data-ttu-id="1fa3e-331">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): vstupní argument s možnou hodnotou null by nikdy neměl mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-331">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): A nullable input argument should never be null.</span></span>
- <span data-ttu-id="1fa3e-332">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): návratová hodnota, která není null, může mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-332">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): A non-nullable return value may be null.</span></span>
- <span data-ttu-id="1fa3e-333">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): návratová hodnota s možnou hodnotou null nikdy nebude null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-333">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): A nullable return value will never be null.</span></span>
- <span data-ttu-id="1fa3e-334">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): vstupní argument, který nemůže mít hodnotu null, může mít hodnotu null, pokud metoda vrátí zadanou hodnotu `bool`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-334">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): A non-nullable input argument may be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="1fa3e-335">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): vstupní argument s možnou hodnotou null nebude mít hodnotu null, pokud metoda vrátí zadanou hodnotu `bool`.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-335">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): A nullable input argument will not be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="1fa3e-336">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): návratová hodnota není null, pokud vstupní argument pro zadaný parametr není null.</span><span class="sxs-lookup"><span data-stu-id="1fa3e-336">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): A return value isn't null if the input argument for the specified parameter isn't null.</span></span>
