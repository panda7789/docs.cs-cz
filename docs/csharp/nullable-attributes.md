---
title: Upgrade polí API pro typy odkazů s možnou hodnotou null s atributy, které definují očekávání pro hodnoty null
description: Naučte se používat popisné atributy AllowNull, DisallowNull, MaybeNull, NotNull a další plně popsat stav null vašich api.
ms.technology: csharp-null-safety
ms.date: 07/31/2019
ms.openlocfilehash: ca04db800271b9b01b5b9f1482dd5a0db2cc1c35
ms.sourcegitcommit: 99b153b93bf94d0fecf7c7bcecb58ac424dfa47c
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/25/2020
ms.locfileid: "80249242"
---
# <a name="update-libraries-to-use-nullable-reference-types-and-communicate-nullable-rules-to-callers"></a><span data-ttu-id="a67d7-103">Aktualizace knihoven za účelem použití typů odkazů s možnou hodnotou null a komunikace pravidel s možnou hodnotou null volajícím</span><span class="sxs-lookup"><span data-stu-id="a67d7-103">Update libraries to use nullable reference types and communicate nullable rules to callers</span></span>

<span data-ttu-id="a67d7-104">Přidání [typů odkazů s možnou hodnotou](nullable-references.md) null `null` znamená, že můžete deklarovat, zda je hodnota povolena nebo očekávána pro každou proměnnou.</span><span class="sxs-lookup"><span data-stu-id="a67d7-104">The addition of [nullable reference types](nullable-references.md) means you can declare whether or not a `null` value is allowed or expected for every variable.</span></span> <span data-ttu-id="a67d7-105">Kromě toho `AllowNull`můžete použít řadu atributů: `DisallowNull` `MaybeNull`, `NotNull` `NotNullWhen`, `MaybeNullWhen`, `NotNullWhenNotNull` , a zcela popsat nulové stavy argumentů a vrácených hodnot.</span><span class="sxs-lookup"><span data-stu-id="a67d7-105">In addition, you can apply a number of attributes: `AllowNull`, `DisallowNull`, `MaybeNull`, `NotNull`, `NotNullWhen`, `MaybeNullWhen`, and `NotNullWhenNotNull` to completely describe the null states of argument and return values.</span></span> <span data-ttu-id="a67d7-106">To poskytuje skvělé zkušenosti při psaní kódu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-106">That provides a great experience as you write code.</span></span> <span data-ttu-id="a67d7-107">Upozornění se vám objeví, pokud může být `null`proměnná s možnou hodnotou null nastavena na hodnotu .</span><span class="sxs-lookup"><span data-stu-id="a67d7-107">You get warnings if a non-nullable variable might be set to `null`.</span></span> <span data-ttu-id="a67d7-108">Upozornění, pokud proměnná s možnou hodnotou null není před dereferenced.</span><span class="sxs-lookup"><span data-stu-id="a67d7-108">You get warnings if a nullable variable isn't null-checked before you dereference it.</span></span> <span data-ttu-id="a67d7-109">Aktualizace knihoven může nějakou dobu trvat, ale odměny stojí za to.</span><span class="sxs-lookup"><span data-stu-id="a67d7-109">Updating your libraries can take time, but the payoffs are worth it.</span></span> <span data-ttu-id="a67d7-110">Čím více informací, které poskytnete `null` kompilátoru o *tom, kdy* je hodnota povolena nebo zakázána, tím lepší upozornění uživatelé vašeho rozhraní API získají.</span><span class="sxs-lookup"><span data-stu-id="a67d7-110">The more information you provide to the compiler about *when* a `null` value is allowed or prohibited, the better warnings users of your API will get.</span></span> <span data-ttu-id="a67d7-111">Začněme se známým příkladem.</span><span class="sxs-lookup"><span data-stu-id="a67d7-111">Let's start with a familiar example.</span></span> <span data-ttu-id="a67d7-112">Představte si, že vaše knihovna má následující rozhraní API pro načtení řetězce prostředků:</span><span class="sxs-lookup"><span data-stu-id="a67d7-112">Imagine your library has the following API to retrieve a resource string:</span></span>

```csharp
bool TryGetMessage(string key, out string message)
```

<span data-ttu-id="a67d7-113">Předchozí příklad následuje známý `Try*` vzor v rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="a67d7-113">The preceding example follows the familiar `Try*` pattern in .NET.</span></span> <span data-ttu-id="a67d7-114">Existují dva referenční argumenty pro `key` toto `message` rozhraní API: a parametr.</span><span class="sxs-lookup"><span data-stu-id="a67d7-114">There are two reference arguments for this API: the `key` and the `message` parameter.</span></span> <span data-ttu-id="a67d7-115">Toto rozhraní API má následující pravidla týkající se nullness těchto argumentů:</span><span class="sxs-lookup"><span data-stu-id="a67d7-115">This API has the following rules relating to the nullness of these arguments:</span></span>

- <span data-ttu-id="a67d7-116">Volající by neměli `null` projít jako `key`argument pro .</span><span class="sxs-lookup"><span data-stu-id="a67d7-116">Callers shouldn't pass `null` as the argument for `key`.</span></span>
- <span data-ttu-id="a67d7-117">Volající mohou předat proměnnou, `null` jejíž `message`hodnota je jako argument pro .</span><span class="sxs-lookup"><span data-stu-id="a67d7-117">Callers can pass a variable whose value is `null` as the argument for `message`.</span></span>
- <span data-ttu-id="a67d7-118">Pokud `TryGetMessage` metoda `true`vrátí , `message` hodnota není null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-118">If the `TryGetMessage` method returns `true`, the value of `message` isn't null.</span></span> <span data-ttu-id="a67d7-119">Pokud je `false,` vrácená hodnota `message` hodnota (a její stav null) je null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-119">If the return value is `false,` the value of `message` (and its null state) is null.</span></span>

<span data-ttu-id="a67d7-120">Pravidlo pro `key` může být zcela vyjádřeno `key` typem proměnné: by měl být typ odkazu s hodnotou nes hotelnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-120">The rule for `key` can be completely expressed by the variable type: `key` should be a non-nullable reference type.</span></span> <span data-ttu-id="a67d7-121">Parametr `message` je složitější.</span><span class="sxs-lookup"><span data-stu-id="a67d7-121">The `message` parameter is more complex.</span></span> <span data-ttu-id="a67d7-122">Umožňuje `null` jako argument, ale zaručuje, že na `out` úspěch, že argument není null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-122">It allows `null` as the argument, but guarantees that, on success, that `out` argument isn't null.</span></span> <span data-ttu-id="a67d7-123">Pro tyto scénáře potřebujete bohatší slovní zásobu k popisu očekávání.</span><span class="sxs-lookup"><span data-stu-id="a67d7-123">For these scenarios, you need a richer vocabulary to describe the expectations.</span></span>

<span data-ttu-id="a67d7-124">Aktualizace knihovny pro odkazy s možnou `?` hodnotou null vyžaduje více než pokrokání na některé proměnné a názvy typů.</span><span class="sxs-lookup"><span data-stu-id="a67d7-124">Updating your library for nullable references requires more than sprinkling `?` on some of the variables and type names.</span></span> <span data-ttu-id="a67d7-125">Předchozí příklad ukazuje, že je třeba prozkoumat vaše api a zvážit vaše očekávání pro každý vstupní argument.</span><span class="sxs-lookup"><span data-stu-id="a67d7-125">The preceding example shows that you need to examine your APIs and consider your expectations for each input argument.</span></span> <span data-ttu-id="a67d7-126">Zvažte záruky pro vrácenou hodnotu a všechny `out` nebo `ref` argumenty na vrácení metody.</span><span class="sxs-lookup"><span data-stu-id="a67d7-126">Consider the guarantees for the return value, and any `out` or `ref` arguments upon the method's return.</span></span> <span data-ttu-id="a67d7-127">Pak sdělte tato pravidla kompilátoru a kompilátor poskytne upozornění, když volající nedodržují tato pravidla.</span><span class="sxs-lookup"><span data-stu-id="a67d7-127">Then communicate those rules to the compiler, and the compiler will provide warnings when callers don't abide by those rules.</span></span>

<span data-ttu-id="a67d7-128">Tahle práce chce čas.</span><span class="sxs-lookup"><span data-stu-id="a67d7-128">This work takes time.</span></span> <span data-ttu-id="a67d7-129">Začněme se strategiemi, aby vaše knihovna nebo aplikace byla upozorněna na nulu a zároveň vyvažovala další požadavky a dodávky.</span><span class="sxs-lookup"><span data-stu-id="a67d7-129">Let's start with strategies to make your library or application nullable-aware, while balancing other requirements and deliverables.</span></span> <span data-ttu-id="a67d7-130">Uvidíte, jak vyvážit probíhající vývoj, který umožňuje typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-130">You'll see how to balance ongoing development enabling nullable reference types.</span></span> <span data-ttu-id="a67d7-131">Dozvíte se výzvy pro definice obecných typů.</span><span class="sxs-lookup"><span data-stu-id="a67d7-131">You'll learn challenges for generic type definitions.</span></span> <span data-ttu-id="a67d7-132">Naučíte se použít atributy k popisu před a po podmínkách na jednotlivých api.</span><span class="sxs-lookup"><span data-stu-id="a67d7-132">You'll learn to apply attributes to describe pre- and post-conditions on individual APIs.</span></span>

## <a name="choose-a-strategy-for-nullable-reference-types"></a><span data-ttu-id="a67d7-133">Zvolte strategii pro typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-133">Choose a strategy for nullable reference types</span></span>

<span data-ttu-id="a67d7-134">První volbou je, zda null typy odkazů by měly být zapnuty nebo vypnuty ve výchozím nastavení.</span><span class="sxs-lookup"><span data-stu-id="a67d7-134">The first choice is whether nullable reference types should be on or off by default.</span></span> <span data-ttu-id="a67d7-135">Máte dvě strategie:</span><span class="sxs-lookup"><span data-stu-id="a67d7-135">You have two strategies:</span></span>

- <span data-ttu-id="a67d7-136">Povolte typy odkazů s možnou hodnotou null pro celý projekt a zakažte je v kódu, který není připraven.</span><span class="sxs-lookup"><span data-stu-id="a67d7-136">Enable nullable reference types for the entire project, and disable it in code that's not ready.</span></span>
- <span data-ttu-id="a67d7-137">Povolit pouze typy odkazů s možnou hodnotou null pro kód, který byl anotován pro typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-137">Only enable nullable reference types for code that's been annotated for nullable reference types.</span></span>

<span data-ttu-id="a67d7-138">První strategie funguje nejlépe, když přidáváte další funkce do knihovny při aktualizaci pro typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-138">The first strategy works best when you're adding other features to the library as you update it for nullable reference types.</span></span> <span data-ttu-id="a67d7-139">Všechny nové vývoj je null,,,,,,,,,,,,,,,,,,,,,</span><span class="sxs-lookup"><span data-stu-id="a67d7-139">All new development is nullable aware.</span></span> <span data-ttu-id="a67d7-140">Při aktualizaci existujícího kódu povolíte v těchto třídách typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-140">As you update existing code, you enable nullable reference types in those classes.</span></span>

<span data-ttu-id="a67d7-141">Po této první strategii postupujte takto:</span><span class="sxs-lookup"><span data-stu-id="a67d7-141">Following this first strategy, you do the following:</span></span>

1. <span data-ttu-id="a67d7-142">Povolte typy odkazů s možnou `<Nullable>enable</Nullable>` hodnotou null pro celý projekt přidáním prvku do souborů *csproj.*</span><span class="sxs-lookup"><span data-stu-id="a67d7-142">Enable nullable reference types for the entire project by adding the `<Nullable>enable</Nullable>` element to your *csproj* files.</span></span>
1. <span data-ttu-id="a67d7-143">Přidejte `#nullable disable` pragma do každého zdrojového souboru v projektu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-143">Add the `#nullable disable` pragma to every source file in your project.</span></span>
1. <span data-ttu-id="a67d7-144">Při práci na každém souboru odeberte pragmu a zřete všechna varování.</span><span class="sxs-lookup"><span data-stu-id="a67d7-144">As you work on each file, remove the pragma and address any warnings.</span></span>

<span data-ttu-id="a67d7-145">Tato první strategie má více práce předem přidat pragma do každého souboru.</span><span class="sxs-lookup"><span data-stu-id="a67d7-145">This first strategy has more up-front work to add the pragma to every file.</span></span> <span data-ttu-id="a67d7-146">Výhodou je, že každý nový soubor kódu přidaný do projektu bude mít hodnotu null povolenou hodnotou.</span><span class="sxs-lookup"><span data-stu-id="a67d7-146">The advantage is that every new code file added to the project will be nullable enabled.</span></span> <span data-ttu-id="a67d7-147">Všechny nové práce budou nullable vědomi; je třeba aktualizovat pouze existující kód.</span><span class="sxs-lookup"><span data-stu-id="a67d7-147">Any new work will be nullable aware; only existing code must be updated.</span></span>

<span data-ttu-id="a67d7-148">Druhá strategie funguje lépe, pokud je knihovna obecně stabilní a hlavním zaměřením vývoje je přijmout typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-148">The second strategy works better if the library is generally stable, and the main focus of the development is to adopt nullable reference types.</span></span> <span data-ttu-id="a67d7-149">Při osazování polí API zapnete typy odkazů s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-149">You turn on nullable reference types as you annotate APIs.</span></span> <span data-ttu-id="a67d7-150">Po dokončení povolíte typy odkazů s možnou hodnotou null pro celý projekt.</span><span class="sxs-lookup"><span data-stu-id="a67d7-150">When you've finished, you enable nullable reference types for the entire project.</span></span>

<span data-ttu-id="a67d7-151">V návaznosti na tuto druhou strategii postupujte takto:</span><span class="sxs-lookup"><span data-stu-id="a67d7-151">Following this second strategy you do the following:</span></span>

1. <span data-ttu-id="a67d7-152">Přidejte `#nullable enable` pragma do souboru, který chcete, aby null být vědomi.</span><span class="sxs-lookup"><span data-stu-id="a67d7-152">Add the `#nullable enable` pragma to the file you want to make nullable aware.</span></span>
1. <span data-ttu-id="a67d7-153">Adresují všechna varování.</span><span class="sxs-lookup"><span data-stu-id="a67d7-153">Address any warnings.</span></span>
1. <span data-ttu-id="a67d7-154">Pokračujte v těchto prvních dvou krocích, dokud neuvědomíte celou knihovnu, která by byla známa.</span><span class="sxs-lookup"><span data-stu-id="a67d7-154">Continue these first two steps until you've made the entire library nullable aware.</span></span>
1. <span data-ttu-id="a67d7-155">Povolte typy s možnou hodnotou null pro celý projekt přidáním `<Nullable>enable</Nullable>` prvku do souborů *csproj.*</span><span class="sxs-lookup"><span data-stu-id="a67d7-155">Enable nullable types for the entire project by adding the `<Nullable>enable</Nullable>` element to your *csproj* files.</span></span>
1. <span data-ttu-id="a67d7-156">Odstraňte `#nullable enable` pragmy, protože už nejsou potřeba.</span><span class="sxs-lookup"><span data-stu-id="a67d7-156">Remove the `#nullable enable` pragmas, as they're no longer needed.</span></span>

<span data-ttu-id="a67d7-157">Tato druhá strategie má méně práce předem.</span><span class="sxs-lookup"><span data-stu-id="a67d7-157">This second strategy has less work up-front.</span></span> <span data-ttu-id="a67d7-158">Kompromis je, že první úkol při vytváření nového souboru je přidat pragma a učinit ji nullable aware.</span><span class="sxs-lookup"><span data-stu-id="a67d7-158">The tradeoff is that the first task when you create a new file is to add the pragma and make it nullable aware.</span></span> <span data-ttu-id="a67d7-159">Pokud všichni vývojáři ve vašem týmu zapomenout, že nový kód je nyní v nevyřízených položkách práce, aby všechny kód nullable aware.</span><span class="sxs-lookup"><span data-stu-id="a67d7-159">If any developers on your team forget, that new code is now in the backlog of work to make all code nullable aware.</span></span>

<span data-ttu-id="a67d7-160">Která z těchto strategií si vyberete, závisí na tom, kolik aktivního vývoje probíhá ve vašem projektu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-160">Which of these strategies you pick depends on how much active development is taking place in your project.</span></span> <span data-ttu-id="a67d7-161">Čím zralejší a stabilnější váš projekt, tím lepší je druhá strategie.</span><span class="sxs-lookup"><span data-stu-id="a67d7-161">The more mature and stable your project, the better the second strategy.</span></span> <span data-ttu-id="a67d7-162">Čím více funkcí se vyvíjí, tím lepší je první strategie.</span><span class="sxs-lookup"><span data-stu-id="a67d7-162">The more features being developed, the better the first strategy.</span></span>

## <a name="should-nullable-warnings-introduce-breaking-changes"></a><span data-ttu-id="a67d7-163">Měla by zavést neplatné výstrahy narušující změny?</span><span class="sxs-lookup"><span data-stu-id="a67d7-163">Should nullable warnings introduce breaking changes?</span></span>

<span data-ttu-id="a67d7-164">Před povolením typů odkazů s možnou hodnotou null jsou proměnné považovány za *netečné*.</span><span class="sxs-lookup"><span data-stu-id="a67d7-164">Before you enable nullable reference types, variables are considered *nullable oblivious*.</span></span> <span data-ttu-id="a67d7-165">Jakmile povolíte typy odkazů s možnou hodnotou null, všechny tyto proměnné jsou *nenulové*.</span><span class="sxs-lookup"><span data-stu-id="a67d7-165">Once you enable nullable reference types, all those variables are *non-nullable*.</span></span> <span data-ttu-id="a67d7-166">Kompilátor vydá upozornění, pokud tyto proměnné nejsou inicializovány na hodnoty bez nuly.</span><span class="sxs-lookup"><span data-stu-id="a67d7-166">The compiler will issue warnings if those variables aren't initialized to non-null values.</span></span>

<span data-ttu-id="a67d7-167">Dalším pravděpodobným zdrojem upozornění je vrácené hodnoty, pokud hodnota nebyla inicializována.</span><span class="sxs-lookup"><span data-stu-id="a67d7-167">Another likely source of warnings is return values when the value hasn't been initialized.</span></span>

<span data-ttu-id="a67d7-168">Prvním krokem při adresování upozornění kompilátoru je použití `?` anotací na parametr a návratové typy k označení, kdy argumenty nebo vrácené hodnoty mohou být nulové.</span><span class="sxs-lookup"><span data-stu-id="a67d7-168">The first step in addressing the compiler warnings is to use `?` annotations on parameter and return types to indicate when arguments or return values may be null.</span></span> <span data-ttu-id="a67d7-169">Pokud referenční proměnné nesmí být null, původní deklarace je správná.</span><span class="sxs-lookup"><span data-stu-id="a67d7-169">When reference variables must not be null, the original declaration is correct.</span></span> <span data-ttu-id="a67d7-170">Jak to uděláte, vaším cílem není jen opravit varování.</span><span class="sxs-lookup"><span data-stu-id="a67d7-170">As you do this, your goal isn't just to fix warnings.</span></span> <span data-ttu-id="a67d7-171">Důležitější je, aby kompilátor pochopil váš záměr pro potenciální hodnoty null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-171">The more important goal is to make the compiler understand your intent for potential null values.</span></span> <span data-ttu-id="a67d7-172">Při zkoumání varování dosáhnete dalšího zásadního rozhodnutí pro vaši knihovnu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-172">As you examine the warnings, you reach your next major decision for your library.</span></span> <span data-ttu-id="a67d7-173">Chcete zvážit úpravu podpisů rozhraní API tak, aby jasněji sdělovaly záměr návrhu?</span><span class="sxs-lookup"><span data-stu-id="a67d7-173">Do you want to consider modifying API signatures to more clearly communicate your design intent?</span></span> <span data-ttu-id="a67d7-174">Lepší podpis rozhraní `TryGetMessage` API pro dříve zkoumanou metodu může být:</span><span class="sxs-lookup"><span data-stu-id="a67d7-174">A better API signature for the `TryGetMessage` method examined earlier could be:</span></span>

```csharp
string? TryGetMessage(string key);
```

<span data-ttu-id="a67d7-175">Vrácená hodnota označuje úspěch nebo neúspěch a nese hodnotu, pokud byla nalezena hodnota.</span><span class="sxs-lookup"><span data-stu-id="a67d7-175">The return value indicates success or failure, and carries the value if the value was found.</span></span> <span data-ttu-id="a67d7-176">V mnoha případech může změna podpisů rozhraní API zlepšit způsob, jakým komunikují hodnoty null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-176">In many cases, changing API signatures can improve how they communicate null values.</span></span>

<span data-ttu-id="a67d7-177">Pro veřejné knihovny nebo knihovny s velkými uživatelskými základnami však můžete upřednostňovat nezavádět žádné změny podpisu rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="a67d7-177">However, for public libraries, or libraries with large user bases, you may prefer not introducing any API signature changes.</span></span> <span data-ttu-id="a67d7-178">Pro tyto případy a další běžné vzory můžete atributy použít jasněji definovat, `null`kdy argument nebo vrácená hodnota může být .</span><span class="sxs-lookup"><span data-stu-id="a67d7-178">For those cases, and other common patterns, you can apply attributes to more clearly define when an argument or return value may be `null`.</span></span> <span data-ttu-id="a67d7-179">Bez ohledu na to, zda uvažujete o změně povrchu rozhraní API, pravděpodobně zjistíte, `null` že samotné poznámky typu nejsou dostatečné pro popis hodnot argumentů nebo vrácených hodnot.</span><span class="sxs-lookup"><span data-stu-id="a67d7-179">Whether or not you consider changing the surface of your API, you'll likely find that type annotations alone aren't sufficient for describing `null` values for arguments or return values.</span></span> <span data-ttu-id="a67d7-180">V těchto případech můžete atributy použít jasněji popsat rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="a67d7-180">In those instances, you can apply attributes to more clearly describe an API.</span></span>

## <a name="attributes-extend-type-annotations"></a><span data-ttu-id="a67d7-181">Atributy rozšiřují poznámky typu</span><span class="sxs-lookup"><span data-stu-id="a67d7-181">Attributes extend type annotations</span></span>

<span data-ttu-id="a67d7-182">Několik atributů byly přidány vyjádřit další informace o stavu null proměnných.</span><span class="sxs-lookup"><span data-stu-id="a67d7-182">Several attributes have been added to express additional information about the null state of variables.</span></span> <span data-ttu-id="a67d7-183">Veškerý kód, který jste napsali před c# 8 představil null reference typy byl *null nevšímavý*.</span><span class="sxs-lookup"><span data-stu-id="a67d7-183">All code you wrote before C# 8 introduced nullable reference types was *null oblivious*.</span></span> <span data-ttu-id="a67d7-184">To znamená, že všechny proměnné typu odkazu může být null, ale null kontroly nejsou vyžadovány.</span><span class="sxs-lookup"><span data-stu-id="a67d7-184">That means any reference type variable may be null, but null checks aren't required.</span></span> <span data-ttu-id="a67d7-185">Jakmile váš kód je *null,,,* tato pravidla změnit.</span><span class="sxs-lookup"><span data-stu-id="a67d7-185">Once your code is *nullable aware*, those rules change.</span></span> <span data-ttu-id="a67d7-186">Referenční typy by `null` nikdy měla být hodnota a nullable `null` typy odkazů musí být zkontrolovány proti před tím, než je odkazováno.</span><span class="sxs-lookup"><span data-stu-id="a67d7-186">Reference types should never be the `null` value, and nullable reference types must be checked against `null` before being dereferenced.</span></span>

<span data-ttu-id="a67d7-187">Pravidla pro vaše rozhraní API jsou pravděpodobně složitější, `TryGetValue` jak jste viděli u scénáře rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="a67d7-187">The rules for your APIs are likely more complicated, as you saw with the `TryGetValue` API scenario.</span></span> <span data-ttu-id="a67d7-188">Mnoho vašich api mají složitější pravidla pro proměnné může `null`nebo nemůže být .</span><span class="sxs-lookup"><span data-stu-id="a67d7-188">Many of your APIs have more complex rules for when variables can or can't be `null`.</span></span> <span data-ttu-id="a67d7-189">V těchto případech použijete k vyjádření těchto pravidel jeden z následujících atributů:</span><span class="sxs-lookup"><span data-stu-id="a67d7-189">In these cases, you'll use one of the following attributes to express those rules:</span></span>

- <span data-ttu-id="a67d7-190">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): Vstupní argument, který nelze upustit, může mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-190">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): A non-nullable input argument may be null.</span></span>
- <span data-ttu-id="a67d7-191">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): Vstupní argument s možnou hodnotou null by nikdy neměl mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-191">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): A nullable input argument should never be null.</span></span>
- <span data-ttu-id="a67d7-192">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): Vrácená hodnota, jejíž nenulovatelná hodnota může být null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-192">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): A non-nullable return value may be null.</span></span>
- <span data-ttu-id="a67d7-193">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): Vrácená hodnota s možnou hodnotou null nikdy nebude null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-193">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): A nullable return value will never be null.</span></span>
- <span data-ttu-id="a67d7-194">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): Vstupní argument, který nelze utnou, `bool` může mít hodnotu null, pokud metoda vrátí zadanou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-194">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): A non-nullable input argument may be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="a67d7-195">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): Vstupní argument s možnou hodnotou null `bool` nebude null, pokud metoda vrátí zadanou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-195">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): A nullable input argument will not be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="a67d7-196">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): Vrácená hodnota není null, pokud argument pro zadaný parametr není null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-196">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): A return value isn't null if the argument for the specified parameter isn't null.</span></span>

<span data-ttu-id="a67d7-197">Předchozí popisy jsou stručný odkaz na to, co každý atribut dělá.</span><span class="sxs-lookup"><span data-stu-id="a67d7-197">The preceding descriptions are a quick reference to what each attribute does.</span></span> <span data-ttu-id="a67d7-198">Každá následující část popisuje chování a význam důkladněji.</span><span class="sxs-lookup"><span data-stu-id="a67d7-198">Each following section describes the behavior and meaning more thoroughly.</span></span>

<span data-ttu-id="a67d7-199">Přidání těchto atributů poskytuje kompilátoru další informace o pravidlech pro rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="a67d7-199">Adding these attributes gives the compiler more information about the rules for your API.</span></span> <span data-ttu-id="a67d7-200">Při volání kódu je kompilován v kontextu s povoleným hodnotou null, kompilátor bude varovat volající, když porušují tato pravidla.</span><span class="sxs-lookup"><span data-stu-id="a67d7-200">When calling code is compiled in a nullable enabled context, the compiler will warn callers when they violate those rules.</span></span> <span data-ttu-id="a67d7-201">Tyto atributy neumožňují další kontroly implementace.</span><span class="sxs-lookup"><span data-stu-id="a67d7-201">These attributes don't enable additional checks on your implementation.</span></span>

## <a name="specify-preconditions-allownull-and-disallownull"></a><span data-ttu-id="a67d7-202">Určete předběžné `AllowNull` podmínky: a`DisallowNull`</span><span class="sxs-lookup"><span data-stu-id="a67d7-202">Specify preconditions: `AllowNull` and `DisallowNull`</span></span>

<span data-ttu-id="a67d7-203">Zvažte vlastnost pro čtení `null` a zápis, která se nikdy nevrátí, protože má přiměřenou výchozí hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-203">Consider a read/write property that never returns `null` because it has a reasonable default value.</span></span> <span data-ttu-id="a67d7-204">Volající předat `null` nastavit přistupujícího pole při jeho nastavení na tuto výchozí hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-204">Callers pass `null` to the set accessor when setting it to that default value.</span></span> <span data-ttu-id="a67d7-205">Zvažte například systém zasílání zpráv, který požádá o název obrazovky v chatovací místnosti.</span><span class="sxs-lookup"><span data-stu-id="a67d7-205">For example, consider a messaging system that asks for a screen name in a chat room.</span></span> <span data-ttu-id="a67d7-206">Pokud není k dispozici žádný, systém generuje náhodný název:</span><span class="sxs-lookup"><span data-stu-id="a67d7-206">If none is provided, the system generates a random name:</span></span>

```csharp
public string ScreenName
{
   get => screenName;
   set => screenName = value ?? GenerateRandomScreenName();
}
private string screenName;
```

<span data-ttu-id="a67d7-207">Při kompilaci předchozíkód v nečitelný kontext, vše je v pořádku.</span><span class="sxs-lookup"><span data-stu-id="a67d7-207">When you compile the preceding code in a nullable oblivious context, everything is fine.</span></span> <span data-ttu-id="a67d7-208">Jakmile povolíte typy odkazů `ScreenName` s možnou hodnotou null, vlastnost se stane nenulelným odkazem.</span><span class="sxs-lookup"><span data-stu-id="a67d7-208">Once you enable nullable reference types, the `ScreenName` property becomes a non-nullable reference.</span></span> <span data-ttu-id="a67d7-209">To je správné `get` pro přistupující `null`ho: nikdy se vrátí .</span><span class="sxs-lookup"><span data-stu-id="a67d7-209">That's correct for the `get` accessor: it never returns `null`.</span></span> <span data-ttu-id="a67d7-210">Volající nemusí kontrolovat vrácenou vlastnost pro `null`.</span><span class="sxs-lookup"><span data-stu-id="a67d7-210">Callers don't need to check the returned property for `null`.</span></span> <span data-ttu-id="a67d7-211">Ale nyní nastavení `null` vlastnosti generuje upozornění.</span><span class="sxs-lookup"><span data-stu-id="a67d7-211">But now setting the property to `null` generates a warning.</span></span> <span data-ttu-id="a67d7-212">Chcete-li i nadále podporovat tento typ <xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute?displayProperty=nameWithType> kódu, přidejte atribut do vlastnosti, jak je znázorněno v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="a67d7-212">In order to continue to support this type of code, you add the <xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute?displayProperty=nameWithType> attribute to the property, as shown in the following code:</span></span>

```csharp
[AllowNull]
public string ScreenName
{
   get => screenName;
   set => screenName = value ?? GenerateRandomScreenName();
}
private string screenName = GenerateRandomScreenName();
```

<span data-ttu-id="a67d7-213">Možná budete muset `using` přidat <xref:System.Diagnostics.CodeAnalysis> direktivu pro použití tohoto a dalších atributů popsaných v tomto článku.</span><span class="sxs-lookup"><span data-stu-id="a67d7-213">You may need to add a `using` directive for <xref:System.Diagnostics.CodeAnalysis> to use this and other attributes discussed in this article.</span></span> <span data-ttu-id="a67d7-214">Atribut je použit a vlastnost, `set` nikoli přistupující objekt.</span><span class="sxs-lookup"><span data-stu-id="a67d7-214">The attribute is applied to the property, not the `set` accessor.</span></span> <span data-ttu-id="a67d7-215">Atribut `AllowNull` určuje *předběžné podmínky*a vztahuje se pouze na vstupy.</span><span class="sxs-lookup"><span data-stu-id="a67d7-215">The `AllowNull` attribute specifies *pre-conditions*, and only applies to inputs.</span></span> <span data-ttu-id="a67d7-216">Přistupující `get` odkaz má vrácenou hodnotu, ale žádné vstupní argumenty.</span><span class="sxs-lookup"><span data-stu-id="a67d7-216">The `get` accessor has a return value, but no input arguments.</span></span> <span data-ttu-id="a67d7-217">Proto `AllowNull` atribut platí pouze pro `set` přistupujícího pole.</span><span class="sxs-lookup"><span data-stu-id="a67d7-217">Therefore, the `AllowNull` attribute only applies to the `set` accessor.</span></span>

<span data-ttu-id="a67d7-218">Předchozí příklad ukazuje, co hledat při `AllowNull` přidávání atributu na argument:</span><span class="sxs-lookup"><span data-stu-id="a67d7-218">The preceding example demonstrates what to look for when adding the `AllowNull` attribute on an argument:</span></span>

1. <span data-ttu-id="a67d7-219">Obecná smlouva pro tuto proměnnou je, `null`že by neměla být , takže chcete typ odkazu s nemožnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-219">The general contract for that variable is that it shouldn't be `null`, so you want a non-nullable reference type.</span></span>
1. <span data-ttu-id="a67d7-220">Existují scénáře pro vstupní proměnné `null`, i když nejsou nejběžnější použití.</span><span class="sxs-lookup"><span data-stu-id="a67d7-220">There are scenarios for the input variable to be `null`, though they aren't the most common usage.</span></span>

<span data-ttu-id="a67d7-221">Nejčastěji budete potřebovat tento atribut pro `in` `out`vlastnosti `ref` , nebo , a argumenty.</span><span class="sxs-lookup"><span data-stu-id="a67d7-221">Most often you'll need this attribute for properties, or `in`, `out`, and `ref` arguments.</span></span> <span data-ttu-id="a67d7-222">Atribut `AllowNull` je nejlepší volbou, pokud je proměnná obvykle non-null, ale je třeba povolit `null` jako předběžnou podmínku.</span><span class="sxs-lookup"><span data-stu-id="a67d7-222">The `AllowNull` attribute is the best choice when a variable is typically non-null, but you need to allow `null` as a precondition.</span></span>

<span data-ttu-id="a67d7-223">Kontrast, který se `DisallowNull`scénáře pro použití : Tento atribut slouží k určení, že `null`vstupní proměnná typu odkazu s možnou hodnotou null by neměla být .</span><span class="sxs-lookup"><span data-stu-id="a67d7-223">Contrast that with scenarios for using `DisallowNull`: You use this attribute to specify that an input variable of a nullable reference type shouldn't be `null`.</span></span> <span data-ttu-id="a67d7-224">Zvažte vlastnost, kde `null` je výchozí hodnota, ale klienti ji mohou nastavit pouze na hodnotu, která není null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-224">Consider a property where `null` is the default value, but clients can only set it to a non-null value.</span></span> <span data-ttu-id="a67d7-225">Uvažujte následující kód:</span><span class="sxs-lookup"><span data-stu-id="a67d7-225">Consider the following code:</span></span>

```csharp
public string ReviewComment
{
    get => _comment;
    set => _comment = value ?? throw new ArgumentNullException(nameof(value), "Cannot set to null");
}
string _comment;
```

<span data-ttu-id="a67d7-226">Předchozí kód je nejlepší způsob, jak vyjádřit `ReviewComment` svůj `null`návrh, který by `null`mohl být , ale nelze nastavit na .</span><span class="sxs-lookup"><span data-stu-id="a67d7-226">The preceding code is the best way to express your design that the `ReviewComment` could be `null`, but can't be set to `null`.</span></span> <span data-ttu-id="a67d7-227">Jakmile je tento kód nullable vědomi, můžete vyjádřit tento <xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute?displayProperty=nameWithType>koncept jasněji volajícím pomocí :</span><span class="sxs-lookup"><span data-stu-id="a67d7-227">Once this code is nullable aware, you can express this concept more clearly to callers using the <xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute?displayProperty=nameWithType>:</span></span>

```csharp
[DisallowNull]
public string? ReviewComment
{
    get => _comment;
    set => _comment = value ?? throw new ArgumentNullException(nameof(value), "Cannot set to null");
}
string? _comment;
```

<span data-ttu-id="a67d7-228">V kontextu s možnou hodnotou `ReviewComment` `get` null může `null`přistupující odkaz vrátit výchozí hodnotu aplikace .</span><span class="sxs-lookup"><span data-stu-id="a67d7-228">In a nullable context, the `ReviewComment` `get` accessor could return the default value of `null`.</span></span> <span data-ttu-id="a67d7-229">Kompilátor varuje, že musí být kontrolovány před přístupem.</span><span class="sxs-lookup"><span data-stu-id="a67d7-229">The compiler warns that it must be checked before access.</span></span> <span data-ttu-id="a67d7-230">Kromě toho varuje volající, že i když `null`by to mohlo být , `null`volající by nemělexplicitně nastavit na .</span><span class="sxs-lookup"><span data-stu-id="a67d7-230">Furthermore, it warns callers that, even though it could be `null`, callers shouldn't explicitly set it to `null`.</span></span> <span data-ttu-id="a67d7-231">Atribut `DisallowNull` také určuje *předběžnou podmínku*, nemá `get` vliv na přistupující ho.</span><span class="sxs-lookup"><span data-stu-id="a67d7-231">The `DisallowNull` attribute also specifies a *pre-condition*, it does not affect the `get` accessor.</span></span> <span data-ttu-id="a67d7-232">Atribut byste měli `DisallowNull` použít, pokud sledujete tyto charakteristiky:</span><span class="sxs-lookup"><span data-stu-id="a67d7-232">You should choose to use the `DisallowNull` attribute when you observe these characteristics about:</span></span>

1. <span data-ttu-id="a67d7-233">Proměnná může `null` být v základních scénářích, často při první instanci.</span><span class="sxs-lookup"><span data-stu-id="a67d7-233">The variable could be `null` in core scenarios, often when first instantiated.</span></span>
1. <span data-ttu-id="a67d7-234">Proměnná by neměla být `null`explicitně nastavena na .</span><span class="sxs-lookup"><span data-stu-id="a67d7-234">The variable shouldn't be explicitly set to `null`.</span></span>

<span data-ttu-id="a67d7-235">Tyto situace jsou běžné v kódu, který byl původně *null nevšímavý*.</span><span class="sxs-lookup"><span data-stu-id="a67d7-235">These situations are common in code that was originally *null oblivious*.</span></span> <span data-ttu-id="a67d7-236">Je možné, že vlastnosti objektu jsou nastaveny ve dvou odlišných inicializačních operacích.</span><span class="sxs-lookup"><span data-stu-id="a67d7-236">It may be that object properties are set in two distinct initialization operations.</span></span> <span data-ttu-id="a67d7-237">Je možné, že některé vlastnosti jsou nastaveny pouze po dokončení některé asynchronní práce.</span><span class="sxs-lookup"><span data-stu-id="a67d7-237">It may be that some properties are set only after some asynchronous work has completed.</span></span>

<span data-ttu-id="a67d7-238">`AllowNull` Atributy `DisallowNull` a umožňují určit, že předběžné podmínky na proměnné nemusí odpovídat poznámky s hodnotou null na tyto proměnné.</span><span class="sxs-lookup"><span data-stu-id="a67d7-238">The `AllowNull` and `DisallowNull` attributes enable you to specify that preconditions on variables may not match the nullable annotations on those variables.</span></span> <span data-ttu-id="a67d7-239">Tyto poskytují další podrobnosti o charakteristikách rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="a67d7-239">These provide more detail about the characteristics of your API.</span></span> <span data-ttu-id="a67d7-240">Tyto další informace pomáhají volajícím správně používat vaše rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="a67d7-240">This additional information helps callers use your API correctly.</span></span> <span data-ttu-id="a67d7-241">Nezapomeňte zadat předběžné podmínky pomocí následujících atributů:</span><span class="sxs-lookup"><span data-stu-id="a67d7-241">Remember you specify preconditions using the following attributes:</span></span>

- <span data-ttu-id="a67d7-242">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): Vstupní argument, který nelze upustit, může mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-242">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): A non-nullable input argument may be null.</span></span>
- <span data-ttu-id="a67d7-243">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): Vstupní argument s možnou hodnotou null by nikdy neměl mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-243">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): A nullable input argument should never be null.</span></span>

## <a name="specify-post-conditions-maybenull-and-notnull"></a><span data-ttu-id="a67d7-244">Uveďte post-podmínky: `MaybeNull` a`NotNull`</span><span class="sxs-lookup"><span data-stu-id="a67d7-244">Specify post-conditions: `MaybeNull` and `NotNull`</span></span>

<span data-ttu-id="a67d7-245">Předpokládejme, že máte metodu s následujícím podpisem:</span><span class="sxs-lookup"><span data-stu-id="a67d7-245">Suppose you have a method with the following signature:</span></span>

```csharp
public Customer FindCustomer(string lastName, string firstName)
```

<span data-ttu-id="a67d7-246">Pravděpodobně jste napsali metodu, `null` jako je tato, abyste se vrátili, když se nenašel hledaný název.</span><span class="sxs-lookup"><span data-stu-id="a67d7-246">You've likely written a method like this to return `null` when the name sought wasn't found.</span></span> <span data-ttu-id="a67d7-247">Jasně `null` ukazuje, že záznam nebyl nalezen.</span><span class="sxs-lookup"><span data-stu-id="a67d7-247">The `null` clearly indicates that the record wasn't found.</span></span> <span data-ttu-id="a67d7-248">V tomto příkladu byste pravděpodobně změnit `Customer` návratový typ z na `Customer?`.</span><span class="sxs-lookup"><span data-stu-id="a67d7-248">In this example, you'd likely change the return type from `Customer` to `Customer?`.</span></span> <span data-ttu-id="a67d7-249">Deklarování vrácené hodnoty jako typu odkazu s možnou hodnotou null jasně určuje záměr tohoto rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="a67d7-249">Declaring the return value as a nullable reference type specifies the intent of this API clearly.</span></span>

<span data-ttu-id="a67d7-250">Z důvodů uvedených v [obecných definicích a nullability](#generic-definitions-and-nullability) tato technika nefunguje s obecnými metodami.</span><span class="sxs-lookup"><span data-stu-id="a67d7-250">For reasons covered under [Generic definitions and nullability](#generic-definitions-and-nullability) that technique does not work with generic methods.</span></span> <span data-ttu-id="a67d7-251">Můžete mít obecnou metodu, která následuje podobný vzor:</span><span class="sxs-lookup"><span data-stu-id="a67d7-251">You may have a generic method that follows a similar pattern:</span></span>

```csharp
public T Find<T>(IEnumerable<T> sequence, Func<T, bool> match)
```

<span data-ttu-id="a67d7-252">Nelze určit, že vrácená `T?`hodnota je .</span><span class="sxs-lookup"><span data-stu-id="a67d7-252">You can't specify that the return value is `T?`.</span></span> <span data-ttu-id="a67d7-253">Metoda vrátí, `null` když není nalezena požadovaná položka.</span><span class="sxs-lookup"><span data-stu-id="a67d7-253">The method returns `null` when the sought item isn't found.</span></span> <span data-ttu-id="a67d7-254">Vzhledem k tomu, `T?` že nelze deklarovat návratový typ, přidáte poznámku `MaybeNull` k metodě return:</span><span class="sxs-lookup"><span data-stu-id="a67d7-254">Since you can't declare a `T?` return type, you add the `MaybeNull` annotation to the method return:</span></span>

```csharp
[return: MaybeNull]
public T Find<T>(IEnumerable<T> sequence, Func<T, bool> match)
```

<span data-ttu-id="a67d7-255">Předchozí kód informuje volající, že smlouva znamená typ s nulou, ale vrácená hodnota *může* být ve skutečnosti null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-255">The preceding code informs callers that the contract implies a non-nullable type, but the return value *may* actually be null.</span></span>  <span data-ttu-id="a67d7-256">Použijte `MaybeNull` atribut, pokud by vaše rozhraní API mělo být typu s nulovým hodnotou, `null` obvykle parametr obecného typu, ale mohou existovat instance, kde by měl být vrácen.</span><span class="sxs-lookup"><span data-stu-id="a67d7-256">Use the `MaybeNull` attribute when your API should be a non-nullable type, typically a generic type parameter, but there may be instances where `null` would be returned.</span></span>

<span data-ttu-id="a67d7-257">Můžete také určit, že vrácená hodnota nebo `out` nebo `ref` argument není null, i když typ je typ odkazu s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-257">You can also specify that a return value or an `out` or `ref` argument isn't null even though the type is a nullable reference type.</span></span> <span data-ttu-id="a67d7-258">Zvažte metodu, která zajišťuje, že pole je dostatečně velké pro uložení několika prvků.</span><span class="sxs-lookup"><span data-stu-id="a67d7-258">Consider a method that ensures an array is large enough to hold a number of elements.</span></span> <span data-ttu-id="a67d7-259">Pokud vstupní argument nemá kapacitu, rutina by přidělit nové pole a zkopírovat všechny existující prvky do něj.</span><span class="sxs-lookup"><span data-stu-id="a67d7-259">If the input argument doesn't have capacity, the routine would allocate a new array and copy all the existing elements into it.</span></span> <span data-ttu-id="a67d7-260">Pokud je `null`vstupní argument , rutina by přidělit nové úložiště.</span><span class="sxs-lookup"><span data-stu-id="a67d7-260">If the input argument is `null`, the routine would allocate new storage.</span></span> <span data-ttu-id="a67d7-261">Pokud je dostatečná kapacita, rutina nedělá nic:</span><span class="sxs-lookup"><span data-stu-id="a67d7-261">If there's sufficient capacity, the routine does nothing:</span></span>

```csharp
public void EnsureCapacity<T>(ref T[] storage, int size)
```

<span data-ttu-id="a67d7-262">Dalo by se volat tuto rutinu takto:</span><span class="sxs-lookup"><span data-stu-id="a67d7-262">You could call this routine as follows:</span></span>

```csharp
// messages has the default value (null) when EnsureCapacity is called:
EnsureCapacity<string>(ref messages, 10);
// messages is not null.
EnsureCapacity<string>(messages, 50);
```

<span data-ttu-id="a67d7-263">Po povolení typů odkazů null, chcete zajistit, že předchozí kód zkompiluje bez upozornění.</span><span class="sxs-lookup"><span data-stu-id="a67d7-263">After enabling null reference types, you want to ensure that the preceding code compiles without warnings.</span></span> <span data-ttu-id="a67d7-264">Když metoda vrátí, `storage` argument je zaručeno, že není null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-264">When the method returns, the `storage` argument is guaranteed to be not null.</span></span> <span data-ttu-id="a67d7-265">Je však přijatelné volat `EnsureCapacity` s nulovým odkazem.</span><span class="sxs-lookup"><span data-stu-id="a67d7-265">However, it's acceptable to call `EnsureCapacity` with a null reference.</span></span> <span data-ttu-id="a67d7-266">Můžete vytvořit `storage` typ odkazu s možnou `NotNull` hodnotou null a přidat do deklarace parametru podmínku post-condition:</span><span class="sxs-lookup"><span data-stu-id="a67d7-266">You can make `storage` a nullable reference type, and add the `NotNull` post-condition to the parameter declaration:</span></span>

```csharp
public void EnsureCapacity<T>([NotNull]ref T[]? storage, int size)
```

<span data-ttu-id="a67d7-267">Předchozí kód vyjadřuje existující smlouvy velmi jasně: Volající můžete předat `null` proměnnou s hodnotou, ale vrácená hodnota je zaručeno, že nikdy být null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-267">The preceding code expresses the existing contract very clearly: Callers can pass a variable with the `null` value, but the return value is guaranteed to never be null.</span></span> <span data-ttu-id="a67d7-268">Atribut `NotNull` je nejužitečnější pro `ref` a `out` argumenty, kde `null` mohou být předány jako argument, ale tento argument je zaručeno, že není null, když metoda vrátí.</span><span class="sxs-lookup"><span data-stu-id="a67d7-268">The `NotNull` attribute is most useful for `ref` and `out` arguments where `null` may be passed as an argument, but that argument is guaranteed to be not null when the method returns.</span></span>

<span data-ttu-id="a67d7-269">Můžete zadat bezpodmínečné postconditions pomocí následujících atributů:</span><span class="sxs-lookup"><span data-stu-id="a67d7-269">You specify unconditional postconditions using the following attributes:</span></span>

- <span data-ttu-id="a67d7-270">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): Vrácená hodnota, jejíž nenulovatelná hodnota může být null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-270">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): A non-nullable return value may be null.</span></span>
- <span data-ttu-id="a67d7-271">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): Vrácená hodnota s možnou hodnotou null nikdy nebude null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-271">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): A nullable return value will never be null.</span></span>

## <a name="specify-conditional-post-conditions-notnullwhen-maybenullwhen-and-notnullifnotnull"></a><span data-ttu-id="a67d7-272">Určete podmíněné `NotNullWhen`post-podmínky: , `MaybeNullWhen`a`NotNullIfNotNull`</span><span class="sxs-lookup"><span data-stu-id="a67d7-272">Specify conditional post-conditions: `NotNullWhen`, `MaybeNullWhen`, and `NotNullIfNotNull`</span></span>

<span data-ttu-id="a67d7-273">Pravděpodobně jste obeznámeni `string` s <xref:System.String.IsNullOrEmpty(System.String)?DisplayProperty=nameWithType>metodou .</span><span class="sxs-lookup"><span data-stu-id="a67d7-273">You're likely familiar with the `string` method <xref:System.String.IsNullOrEmpty(System.String)?DisplayProperty=nameWithType>.</span></span> <span data-ttu-id="a67d7-274">Tato metoda `true` vrátí, pokud je argument null nebo prázdný řetězec.</span><span class="sxs-lookup"><span data-stu-id="a67d7-274">This method returns `true` when the argument is null or an empty string.</span></span> <span data-ttu-id="a67d7-275">Je to forma null-check: Volající nemusí null-zkontrolovat argument, pokud metoda `false`vrátí .</span><span class="sxs-lookup"><span data-stu-id="a67d7-275">It's a form of null-check: Callers don't need to null-check the argument if the method returns `false`.</span></span> <span data-ttu-id="a67d7-276">Chcete-li metodu, jako je tato nullable aware, byste nastavit argument `NotNullWhen` na typ odkazu s hodnotou null a přidat atribut:</span><span class="sxs-lookup"><span data-stu-id="a67d7-276">To make a method like this nullable aware, you'd set the argument to a nullable reference type, and add the `NotNullWhen` attribute:</span></span>

```csharp
bool IsNullOrEmpty([NotNullWhen(false)]string? value);
```

<span data-ttu-id="a67d7-277">To informuje kompilátor, že žádný kód, kde je `false` vrácená hodnota nemusí být null-kontrolovány.</span><span class="sxs-lookup"><span data-stu-id="a67d7-277">That informs the compiler that any code where the return value is `false` need not be null-checked.</span></span> <span data-ttu-id="a67d7-278">Přidání atributu informuje statické analýzy kompilátoru, který `IsNullOrEmpty` provádí potřebnou `false`kontrolu null: `null`když vrátí , vstupní argument není .</span><span class="sxs-lookup"><span data-stu-id="a67d7-278">The addition of the attribute informs the compiler's static analysis that `IsNullOrEmpty` performs the necessary null check: when it returns `false`, the input argument is not `null`.</span></span>

```csharp
string? userInput = GetUserInput();
if (!string.IsNullOrEmpty(userInput))
{
   int messageLength = userInput.Length; // no null check needed.
}
// null check needed on userInput here.
```

<span data-ttu-id="a67d7-279">Metoda <xref:System.String.IsNullOrEmpty(System.String)?DisplayProperty=nameWithType> bude uvedena v notaci, jak je uvedeno výše pro .NET Core 3.0.</span><span class="sxs-lookup"><span data-stu-id="a67d7-279">The <xref:System.String.IsNullOrEmpty(System.String)?DisplayProperty=nameWithType> method will be annotated as shown above for .NET Core 3.0.</span></span> <span data-ttu-id="a67d7-280">Můžete mít podobné metody v základu kódu, které kontrolují stav objektů pro nulové hodnoty.</span><span class="sxs-lookup"><span data-stu-id="a67d7-280">You may have similar methods in your codebase that check the state of objects for null values.</span></span> <span data-ttu-id="a67d7-281">Kompilátor nerozpozná vlastní metody kontroly null a poznámky budete muset přidat sami.</span><span class="sxs-lookup"><span data-stu-id="a67d7-281">The compiler won't recognize custom null check methods, and you'll need to add the annotations yourself.</span></span> <span data-ttu-id="a67d7-282">Když přidáte atribut, statická analýza kompilátoru ví, kdy byla ověřená proměnná zkontrolována.</span><span class="sxs-lookup"><span data-stu-id="a67d7-282">When you add the attribute, the compiler's static analysis knows when the tested variable has been null checked.</span></span>

<span data-ttu-id="a67d7-283">Dalším použitím pro tyto `Try*` atributy je vzor.</span><span class="sxs-lookup"><span data-stu-id="a67d7-283">Another use for these attributes is the `Try*` pattern.</span></span> <span data-ttu-id="a67d7-284">Postconditions pro `ref` `out` a proměnné jsou sdělovány prostřednictvím vrácená hodnota.</span><span class="sxs-lookup"><span data-stu-id="a67d7-284">The postconditions for `ref` and `out` variables are communicated through the return value.</span></span> <span data-ttu-id="a67d7-285">Zvažte tuto metodu zobrazenou dříve:</span><span class="sxs-lookup"><span data-stu-id="a67d7-285">Consider this method shown earlier:</span></span>

```csharp
bool TryGetMessage(string key, out string message)
```

<span data-ttu-id="a67d7-286">Předchozí metoda následuje za typickým idiomem .NET: `message` vrácená hodnota označuje, zda byla nastavena na nalezenou hodnotu nebo, pokud není nalezena žádná zpráva, na výchozí hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-286">The preceding method follows a typical .NET idiom: the return value indicates if `message` was set to the found value or, if no message is found, to the default value.</span></span> <span data-ttu-id="a67d7-287">Pokud metoda `true`vrátí , `message` hodnota není null; jinak metoda nastaví `message` na null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-287">If the method returns `true`, the value of `message` isn't null; otherwise, the method sets `message` to null.</span></span>

<span data-ttu-id="a67d7-288">Můžete komunikovat, že idiom pomocí atributu. `NotNullWhen`</span><span class="sxs-lookup"><span data-stu-id="a67d7-288">You can communicate that idiom using the `NotNullWhen` attribute.</span></span> <span data-ttu-id="a67d7-289">Při aktualizaci podpisu pro typy `message` odkazů `string?` s možnou hodnotou null vytvořte atribut a přidejte:</span><span class="sxs-lookup"><span data-stu-id="a67d7-289">When you update the signature for nullable reference types, make `message` a `string?` and add an attribute:</span></span>

```csharp
bool TryGetMessage(string key, [NotNullWhen(true)] out string? message)
```

<span data-ttu-id="a67d7-290">V předchozím příkladu `message` je známo, že hodnota `TryGetMessage` není `true`null při vrátí .</span><span class="sxs-lookup"><span data-stu-id="a67d7-290">In the preceding example, the value of `message` is known to be not null when `TryGetMessage` returns `true`.</span></span> <span data-ttu-id="a67d7-291">Měli byste anotovat podobné metody v základu kódu stejným způsobem: argumenty mohou být `null`a je `true`známo, že nejsou null, když metoda vrátí .</span><span class="sxs-lookup"><span data-stu-id="a67d7-291">You should annotate similar methods in your codebase in the same way: the arguments could be `null`, and are known to be not null when the method returns `true`.</span></span>

<span data-ttu-id="a67d7-292">Je tu ještě jeden poslední atribut, který můžete také potřebovat.</span><span class="sxs-lookup"><span data-stu-id="a67d7-292">There's one final attribute you may also need.</span></span> <span data-ttu-id="a67d7-293">Někdy stav null vrácené hodnoty závisí na stavu null jednoho nebo více vstupních argumentů.</span><span class="sxs-lookup"><span data-stu-id="a67d7-293">Sometimes the null state of a return value depends on the null state of one or more input arguments.</span></span> <span data-ttu-id="a67d7-294">Tyto metody vrátí hodnotu bez hodnoty null vždy, `null`když určité vstupní argumenty nejsou .</span><span class="sxs-lookup"><span data-stu-id="a67d7-294">These methods will return a non-null value whenever certain input arguments aren't `null`.</span></span> <span data-ttu-id="a67d7-295">Chcete-li správně opatří tyto metody, použijte `NotNullIfNotNull` atribut.</span><span class="sxs-lookup"><span data-stu-id="a67d7-295">To correctly annotate these methods, you use the `NotNullIfNotNull` attribute.</span></span> <span data-ttu-id="a67d7-296">Zvažte následující metodu:</span><span class="sxs-lookup"><span data-stu-id="a67d7-296">Consider the following method:</span></span>

```csharp
string GetTopLevelDomainFromFullUrl(string url);
```

<span data-ttu-id="a67d7-297">Pokud `url` argument není null, výstup není `null`.</span><span class="sxs-lookup"><span data-stu-id="a67d7-297">If the `url` argument isn't null, the output isn't `null`.</span></span> <span data-ttu-id="a67d7-298">Po povolení odkazů s možnou hodnotou null bude tento podpis fungovat správně za předpokladu, že rozhraní API nikdy nepřijme nulový vstup.</span><span class="sxs-lookup"><span data-stu-id="a67d7-298">Once nullable references are enabled, that signature works correctly, provided your API never accepts a null input.</span></span> <span data-ttu-id="a67d7-299">Však pokud vstup může být null, pak vrácená hodnota může být také null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-299">However, if the input could be null, then return value could also be null.</span></span> <span data-ttu-id="a67d7-300">Proto můžete změnit podpis na následující kód:</span><span class="sxs-lookup"><span data-stu-id="a67d7-300">Therefore, you could change the signature to the following code:</span></span>

```csharp
string? GetTopLevelDomainFromFullUrl(string? url);
```

<span data-ttu-id="a67d7-301">To také funguje, ale často nutí `null` volající implementovat další kontroly.</span><span class="sxs-lookup"><span data-stu-id="a67d7-301">That also works, but will often force callers to implement extra `null` checks.</span></span> <span data-ttu-id="a67d7-302">Smlouva je, že vrácená `null` hodnota by `url` být `null`pouze v případě, že vstupní argument je .</span><span class="sxs-lookup"><span data-stu-id="a67d7-302">The contract is that the return value would be `null` only when the input argument `url` is `null`.</span></span> <span data-ttu-id="a67d7-303">Chcete-li vyjádřit tuto smlouvu, měli byste anotovat tuto metodu, jak je znázorněno v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="a67d7-303">To express that contract, you would annotate this method as shown in the following code:</span></span>

```csharp
[return: NotNullIfNotNull("url")]
string? GetTopLevelDomainFromFullUrl(string? url);
```

<span data-ttu-id="a67d7-304">Vrácená hodnota a argument byly anotovány `?` s označením, `null`že buď může být .</span><span class="sxs-lookup"><span data-stu-id="a67d7-304">The return value and the argument have both been annotated with the `?` indicating that either could be `null`.</span></span> <span data-ttu-id="a67d7-305">Atribut dále objasňuje, že vrácená hodnota `url` nebude null, `null`pokud argument není .</span><span class="sxs-lookup"><span data-stu-id="a67d7-305">The attribute further clarifies that the return value won't be null when the `url` argument isn't `null`.</span></span>

<span data-ttu-id="a67d7-306">Podmíněné postconditions zadáte pomocí těchto atributů:</span><span class="sxs-lookup"><span data-stu-id="a67d7-306">You specify conditional postconditions using these attributes:</span></span>

- <span data-ttu-id="a67d7-307">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): Vstupní argument, který nelze utnou, `bool` může mít hodnotu null, pokud metoda vrátí zadanou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-307">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): A non-nullable input argument may be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="a67d7-308">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): Vstupní argument s možnou hodnotou null `bool` nebude null, pokud metoda vrátí zadanou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-308">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): A nullable input argument will not be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="a67d7-309">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): Vrácená hodnota není null, pokud vstupní argument pro zadaný parametr není null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-309">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): A return value isn't null if the input argument for the specified parameter isn't null.</span></span>

## <a name="generic-definitions-and-nullability"></a><span data-ttu-id="a67d7-310">Obecné definice a nullability</span><span class="sxs-lookup"><span data-stu-id="a67d7-310">Generic definitions and nullability</span></span>

<span data-ttu-id="a67d7-311">Správná komunikace nulového stavu obecných typů a obecných metod vyžaduje zvláštní péči.</span><span class="sxs-lookup"><span data-stu-id="a67d7-311">Correctly communicating the null state of generic types and generic methods requires special care.</span></span> <span data-ttu-id="a67d7-312">To vyplývá ze skutečnosti, že typ hodnoty s možnou hodnotou s hodnotou null a typ odkazu s možnou hodnotou s možnou hodnotou null se zásadně liší.</span><span class="sxs-lookup"><span data-stu-id="a67d7-312">This stems from the fact that a nullable value type and a nullable reference type are fundamentally different.</span></span> <span data-ttu-id="a67d7-313">Synonymum `int?` pro `Nullable<int>`, `string?` vzhledem k tomu, že je `string` s atributem přidaným kompilátorem.</span><span class="sxs-lookup"><span data-stu-id="a67d7-313">An `int?` is a synonym for `Nullable<int>`, whereas `string?` is `string` with an attribute added by the compiler.</span></span> <span data-ttu-id="a67d7-314">Výsledkem je, že kompilátor nemůže `T?` generovat správný `T` kód, aniž by věděl, zda je `class` nebo `struct`.</span><span class="sxs-lookup"><span data-stu-id="a67d7-314">The result is that the compiler can't generate correct code for `T?` without knowing if `T` is a `class` or a `struct`.</span></span>

<span data-ttu-id="a67d7-315">To neznamená, že nelze použít typ s hodnotou null (typ hodnoty nebo typ odkazu) jako argument typu pro uzavřený obecný typ.</span><span class="sxs-lookup"><span data-stu-id="a67d7-315">This doesn't mean you can't use a nullable type (either value type or reference type) as the type argument for a closed generic type.</span></span> <span data-ttu-id="a67d7-316">Oba `List<string?>` `List<int?>` a jsou platné instance `List<T>`.</span><span class="sxs-lookup"><span data-stu-id="a67d7-316">Both `List<string?>` and `List<int?>` are valid instantiations of `List<T>`.</span></span>

<span data-ttu-id="a67d7-317">Co to znamená, že nelze `T?` použít v deklaraci obecné třídy nebo metody bez omezení.</span><span class="sxs-lookup"><span data-stu-id="a67d7-317">What it does mean is that you can't use `T?` in a generic class or method declaration without constraints.</span></span> <span data-ttu-id="a67d7-318">Například <xref:System.Linq.Enumerable.FirstOrDefault%60%601(System.Collections.Generic.IEnumerable%7B%60%600%7D)?displayProperty=nameWithType> nebude změněn na návrat `T?`.</span><span class="sxs-lookup"><span data-stu-id="a67d7-318">For example, <xref:System.Linq.Enumerable.FirstOrDefault%60%601(System.Collections.Generic.IEnumerable%7B%60%600%7D)?displayProperty=nameWithType> won't be changed to return `T?`.</span></span> <span data-ttu-id="a67d7-319">Toto omezení můžete překonat `struct` přidáním omezení nebo. `class`</span><span class="sxs-lookup"><span data-stu-id="a67d7-319">You can overcome this limitation by adding either the `struct` or `class` constraint.</span></span> <span data-ttu-id="a67d7-320">S některou z těchto omezení kompilátor ví, `T` `T?`jak generovat kód pro oba a .</span><span class="sxs-lookup"><span data-stu-id="a67d7-320">With either of those constraints, the compiler knows how to generate code for both `T` and `T?`.</span></span>

<span data-ttu-id="a67d7-321">Můžete chtít omezit typy používané pro obecný typ argumentu, které mají být nenulové typy.</span><span class="sxs-lookup"><span data-stu-id="a67d7-321">You may want to restrict the types used for a generic type argument to be non-nullable types.</span></span> <span data-ttu-id="a67d7-322">Můžete to udělat přidáním `notnull` omezení pro tento argument typu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-322">You can do that by adding the `notnull` constraint on that type argument.</span></span> <span data-ttu-id="a67d7-323">When that constraint is applied, the type argument must not be a nullable type.</span><span class="sxs-lookup"><span data-stu-id="a67d7-323">When that constraint is applied, the type argument must not be a nullable type.</span></span>

## <a name="conclusions"></a><span data-ttu-id="a67d7-324">Závěry</span><span class="sxs-lookup"><span data-stu-id="a67d7-324">Conclusions</span></span>

<span data-ttu-id="a67d7-325">Přidání typů odkazů s možnou hodnotou null poskytuje počáteční slovní `null`zásobu k popisu očekávání api pro proměnné, které by mohly být .</span><span class="sxs-lookup"><span data-stu-id="a67d7-325">Adding nullable reference types provides an initial vocabulary to describe your APIs expectations for variables that could be `null`.</span></span> <span data-ttu-id="a67d7-326">Další atributy poskytují bohatší slovní zásobu k popisu stavu null proměnných jako předpoklady a postconditions.</span><span class="sxs-lookup"><span data-stu-id="a67d7-326">The additional attributes provide a richer vocabulary to describe the null state of variables as preconditions and postconditions.</span></span> <span data-ttu-id="a67d7-327">Tyto atributy jasněji popisují vaše očekávání a poskytují lepší prostředí pro vývojáře, kteří používají vaše rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="a67d7-327">These attributes more clearly describe your expectations and provide a better experience for the developers using your APIs.</span></span>

<span data-ttu-id="a67d7-328">Při aktualizaci knihoven pro kontext s možnou hodnotou null přidejte tyto atributy, které uživatele vašich api navedou ke správnému použití.</span><span class="sxs-lookup"><span data-stu-id="a67d7-328">As you update libraries for a nullable context, add these attributes to guide users of your APIs to the correct usage.</span></span> <span data-ttu-id="a67d7-329">Tyto atributy vám pomohou plně popsat stav null vstupních argumentů a vrácených hodnot:</span><span class="sxs-lookup"><span data-stu-id="a67d7-329">These attributes help you fully describe the null-state of input arguments and return values:</span></span>

- <span data-ttu-id="a67d7-330">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): Vstupní argument, který nelze upustit, může mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-330">[AllowNull](xref:System.Diagnostics.CodeAnalysis.AllowNullAttribute): A non-nullable input argument may be null.</span></span>
- <span data-ttu-id="a67d7-331">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): Vstupní argument s možnou hodnotou null by nikdy neměl mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-331">[DisallowNull](xref:System.Diagnostics.CodeAnalysis.DisallowNullAttribute): A nullable input argument should never be null.</span></span>
- <span data-ttu-id="a67d7-332">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): Vrácená hodnota, jejíž nenulovatelná hodnota může být null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-332">[MaybeNull](xref:System.Diagnostics.CodeAnalysis.MaybeNullAttribute): A non-nullable return value may be null.</span></span>
- <span data-ttu-id="a67d7-333">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): Vrácená hodnota s možnou hodnotou null nikdy nebude null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-333">[NotNull](xref:System.Diagnostics.CodeAnalysis.NotNullAttribute): A nullable return value will never be null.</span></span>
- <span data-ttu-id="a67d7-334">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): Vstupní argument, který nelze utnou, `bool` může mít hodnotu null, pokud metoda vrátí zadanou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-334">[MaybeNullWhen](xref:System.Diagnostics.CodeAnalysis.MaybeNullWhenAttribute): A non-nullable input argument may be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="a67d7-335">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): Vstupní argument s možnou hodnotou null `bool` nebude null, pokud metoda vrátí zadanou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a67d7-335">[NotNullWhen](xref:System.Diagnostics.CodeAnalysis.NotNullWhenAttribute): A nullable input argument will not be null when the method returns the specified `bool` value.</span></span>
- <span data-ttu-id="a67d7-336">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): Vrácená hodnota není null, pokud vstupní argument pro zadaný parametr není null.</span><span class="sxs-lookup"><span data-stu-id="a67d7-336">[NotNullIfNotNull](xref:System.Diagnostics.CodeAnalysis.NotNullIfNotNullAttribute): A return value isn't null if the input argument for the specified parameter isn't null.</span></span>
