---
title: Vlastnosti
description: Další informace o C# vlastnosti, jako je například funkce pro ověření, vypočítané hodnoty, opožděné vyhodnocení, a vlastnost změnit oznámení.
ms.date: 04/25/2018
ms.openlocfilehash: d4fa7b6117bec63c41318dd4bcc3850ce55a5907
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/23/2019
ms.locfileid: "61675777"
---
# <a name="properties"></a><span data-ttu-id="40c5e-103">Vlastnosti</span><span class="sxs-lookup"><span data-stu-id="40c5e-103">Properties</span></span>

<span data-ttu-id="40c5e-104">Vlastnosti jsou první třídy občanům v C#.</span><span class="sxs-lookup"><span data-stu-id="40c5e-104">Properties are first class citizens in C#.</span></span> <span data-ttu-id="40c5e-105">Jazyk definuje syntaxe, která vývojářům umožňuje psát kód, který se přesně vyjadřoval jejich návrhu záměr.</span><span class="sxs-lookup"><span data-stu-id="40c5e-105">The language defines syntax that enables developers to write code that accurately expresses their design intent.</span></span>

<span data-ttu-id="40c5e-106">Vlastnosti se chovat jako pole, když k nim přistupovat.</span><span class="sxs-lookup"><span data-stu-id="40c5e-106">Properties behave like fields when they are accessed.</span></span>
<span data-ttu-id="40c5e-107">Na rozdíl od pole, ale vlastnosti jsou implementovány s přístupové objekty, které definují příkazy, které jsou spouštěny, když je vlastnost přistupovat ani přiřazené.</span><span class="sxs-lookup"><span data-stu-id="40c5e-107">However, unlike fields, properties are implemented with accessors that define the statements executed when a property is accessed or assigned.</span></span>

## <a name="property-syntax"></a><span data-ttu-id="40c5e-108">Syntaxe vlastnosti</span><span class="sxs-lookup"><span data-stu-id="40c5e-108">Property syntax</span></span>

<span data-ttu-id="40c5e-109">Syntaxe vlastností je přirozeným pole.</span><span class="sxs-lookup"><span data-stu-id="40c5e-109">The syntax for properties is a natural extension to fields.</span></span> <span data-ttu-id="40c5e-110">Pole určuje umístění úložiště:</span><span class="sxs-lookup"><span data-stu-id="40c5e-110">A field defines a storage location:</span></span>

[!code-csharp[Person class with public fields](../../samples/snippets/csharp/properties/Person.cs#1)]

<span data-ttu-id="40c5e-111">Definice vlastnosti obsahuje deklarace `get` a `set` přístupový objekt, který načte a přiřadí hodnotu této vlastnosti:</span><span class="sxs-lookup"><span data-stu-id="40c5e-111">A property definition contains declarations for a `get` and `set` accessor that retrieves and assigns the value of that property:</span></span>

[!code-csharp[Person class with public properties](../../samples/snippets/csharp/properties/Person.cs#2)]

<span data-ttu-id="40c5e-112">Syntaxe uvedené výše je *auto vlastnost* syntaxe.</span><span class="sxs-lookup"><span data-stu-id="40c5e-112">The syntax shown above is the *auto property* syntax.</span></span> <span data-ttu-id="40c5e-113">Kompilátor generuje umístění úložiště pro pole, který zálohuje vlastnost.</span><span class="sxs-lookup"><span data-stu-id="40c5e-113">The compiler generates the storage location for the field that backs up the property.</span></span> <span data-ttu-id="40c5e-114">Kompilátor také implementuje text `get` a `set` přistupující objekty.</span><span class="sxs-lookup"><span data-stu-id="40c5e-114">The compiler also implements the body of the `get` and `set` accessors.</span></span>

<span data-ttu-id="40c5e-115">V některých případech je nutné inicializovat vlastnost, která má jinou hodnotu než výchozí nastavení pro jeho typu.</span><span class="sxs-lookup"><span data-stu-id="40c5e-115">Sometimes, you need to initialize a property to a value other than the default for its type.</span></span>  <span data-ttu-id="40c5e-116">C#který umožňuje tak, že nastavíte hodnotu po pravé složené závorce pro vlastnost.</span><span class="sxs-lookup"><span data-stu-id="40c5e-116">C# enables that by setting a value after the closing brace for the property.</span></span> <span data-ttu-id="40c5e-117">Možná dáte přednost počáteční hodnotu `FirstName` vlastnost jako prázdný řetězec spíše než `null`.</span><span class="sxs-lookup"><span data-stu-id="40c5e-117">You may prefer the initial value for the `FirstName` property to be the empty string rather than `null`.</span></span> <span data-ttu-id="40c5e-118">By určíte, jak je znázorněno níže:</span><span class="sxs-lookup"><span data-stu-id="40c5e-118">You would specify that as shown below:</span></span>

[!code-csharp[Person class with properties and initializer](../../samples/snippets/csharp/properties/Person.cs#3)]

<span data-ttu-id="40c5e-119">Konkrétní inicializační je zvláště užitečná pro vlastnosti jen pro čtení, jak uvidíte později v tomto článku.</span><span class="sxs-lookup"><span data-stu-id="40c5e-119">Specific initialization is most useful for read-only properties, as you'll see later in this article.</span></span>

<span data-ttu-id="40c5e-120">Můžete také definovat úložiště sami, jak je znázorněno níže:</span><span class="sxs-lookup"><span data-stu-id="40c5e-120">You can also define the storage yourself, as shown below:</span></span>

[!code-csharp[Person class with properties and backing field](../../samples/snippets/csharp/properties/Person.cs#4)]

<span data-ttu-id="40c5e-121">Pokud provádění vlastnost je jediný výraz, můžete použít *členové tvoření* pro getter nebo setter:</span><span class="sxs-lookup"><span data-stu-id="40c5e-121">When a property implementation is a single expression, you can use *expression-bodied members* for the getter or setter:</span></span>

[!code-csharp[Person class with properties and expression bodied getters and setters](../../samples/snippets/csharp/properties/Person.cs#5)]

<span data-ttu-id="40c5e-122">Tato zjednodušenou syntaxi se použije, případně v celém tomto článku.</span><span class="sxs-lookup"><span data-stu-id="40c5e-122">This simplified syntax will be used where applicable throughout this article.</span></span>

<span data-ttu-id="40c5e-123">Definice vlastnosti uvedené výše je vlastnost pro čtení i zápis.</span><span class="sxs-lookup"><span data-stu-id="40c5e-123">The property definition shown above is a read-write property.</span></span> <span data-ttu-id="40c5e-124">Všimněte si, že klíčové slovo `value` přístupového objektu set.</span><span class="sxs-lookup"><span data-stu-id="40c5e-124">Notice the keyword `value` in the set accessor.</span></span> <span data-ttu-id="40c5e-125">`set` Přístupového objektu má vždy jeden parametr s názvem `value`.</span><span class="sxs-lookup"><span data-stu-id="40c5e-125">The `set` accessor always has a single parameter named `value`.</span></span> <span data-ttu-id="40c5e-126">`get` Přístupového objektu musí vracet hodnotu, která je převoditelná na typ proměnné (`string` v tomto příkladu).</span><span class="sxs-lookup"><span data-stu-id="40c5e-126">The `get` accessor must return a value that is convertible to the type of the property (`string` in this example).</span></span>

<span data-ttu-id="40c5e-127">To je základní informace o syntaxi.</span><span class="sxs-lookup"><span data-stu-id="40c5e-127">That's the basics of the syntax.</span></span> <span data-ttu-id="40c5e-128">Existuje mnoho různých změn, které podporují širokou škálu návrhů idiomy.</span><span class="sxs-lookup"><span data-stu-id="40c5e-128">There are many different variations that support a variety of different design idioms.</span></span> <span data-ttu-id="40c5e-129">Pojďme prozkoumat a další možnosti syntaxe pro každý.</span><span class="sxs-lookup"><span data-stu-id="40c5e-129">Let's explore, and learn the syntax options for each.</span></span>

## <a name="scenarios"></a><span data-ttu-id="40c5e-130">Scénáře</span><span class="sxs-lookup"><span data-stu-id="40c5e-130">Scenarios</span></span>

<span data-ttu-id="40c5e-131">Výše uvedené příklady nám ukázaly, jedním z nejjednodušší případů definici vlastnosti: vlastnost pro čtení a zápis bez ověřování.</span><span class="sxs-lookup"><span data-stu-id="40c5e-131">The examples above showed one of the simplest cases of property definition: a read-write property with no validation.</span></span> <span data-ttu-id="40c5e-132">Napsáním kódu, které chcete v `get` a `set` přístupové objekty, můžete vytvořit mnoho různých scénářů.</span><span class="sxs-lookup"><span data-stu-id="40c5e-132">By writing the code you want in the `get` and `set` accessors, you can create many different scenarios.</span></span>

### <a name="validation"></a><span data-ttu-id="40c5e-133">Ověřování</span><span class="sxs-lookup"><span data-stu-id="40c5e-133">Validation</span></span>

<span data-ttu-id="40c5e-134">Můžete napsat kód `set` přistupující k zajištění, že je vlastnost reprezentována jsou hodnoty vždycky platné.</span><span class="sxs-lookup"><span data-stu-id="40c5e-134">You can write code in the `set` accessor to ensure that the values represented by a property are always valid.</span></span> <span data-ttu-id="40c5e-135">Předpokládejme například, že jedno pravidlo pro `Person` třída je, že název nemůže být prázdné nebo prázdný znak.</span><span class="sxs-lookup"><span data-stu-id="40c5e-135">For example, suppose one rule for the `Person` class is that the name cannot be blank or white space.</span></span> <span data-ttu-id="40c5e-136">Měli byste napsat následující:</span><span class="sxs-lookup"><span data-stu-id="40c5e-136">You would write that as follows:</span></span>

[!code-csharp[Validating property setters](../../samples/snippets/csharp/properties/Person.cs#6)]

<span data-ttu-id="40c5e-137">V předchozím příkladu se dá zjednodušit pomocí`throw` výraz jako součást ověření setter vlastnosti:</span><span class="sxs-lookup"><span data-stu-id="40c5e-137">The preceding example can be simplified by using a`throw` expression as part of the property setter validation:</span></span>

[!code-csharp[Validating property setters](../../samples/snippets/csharp/properties/Person.cs#7)]

<span data-ttu-id="40c5e-138">Výše uvedený příklad vynucuje pravidla, křestní jméno nesmí být prázdné nebo prázdný znak.</span><span class="sxs-lookup"><span data-stu-id="40c5e-138">The example above enforces the rule that the first name must not be blank or white space.</span></span> <span data-ttu-id="40c5e-139">Pokud vývojář zapíše</span><span class="sxs-lookup"><span data-stu-id="40c5e-139">If a developer writes</span></span>

```csharp
hero.FirstName = "";
```

<span data-ttu-id="40c5e-140">Vyvolá přiřazení `ArgumentException`.</span><span class="sxs-lookup"><span data-stu-id="40c5e-140">That assignment throws an `ArgumentException`.</span></span> <span data-ttu-id="40c5e-141">Protože přístupový objekt set vlastnosti musí mít typ vrácené hodnoty void, vám umožňuje nahlásit chyby u přístupového objektu set vyvoláním výjimky.</span><span class="sxs-lookup"><span data-stu-id="40c5e-141">Because a property set accessor must have a void return type, you report errors in the set accessor by throwing an exception.</span></span>

<span data-ttu-id="40c5e-142">Tahle stejné syntaxe cokoli, co je potřeba ve vašem scénáři můžete rozšířit.</span><span class="sxs-lookup"><span data-stu-id="40c5e-142">You can extend this same syntax to anything needed in your scenario.</span></span> <span data-ttu-id="40c5e-143">Můžete zkontrolovat vztahy mezi různými vlastnostmi nebo ověřovat proti všechny externí podmínky.</span><span class="sxs-lookup"><span data-stu-id="40c5e-143">You can check the relationships between different properties, or validate against any external conditions.</span></span> <span data-ttu-id="40c5e-144">Libovolný platný C# příkazy jsou platné v přistupujícího objektu vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="40c5e-144">Any valid C# statements are valid in a property accessor.</span></span>

### <a name="read-only"></a><span data-ttu-id="40c5e-145">jen pro čtení</span><span class="sxs-lookup"><span data-stu-id="40c5e-145">Read-only</span></span>

<span data-ttu-id="40c5e-146">Do této chvíle jsou všechny definice vlastností, které jste viděli vlastností čtení/zápisu s veřejnou přistupující objekty.</span><span class="sxs-lookup"><span data-stu-id="40c5e-146">Up to this point, all the property definitions you have seen are read/write properties with public accessors.</span></span> <span data-ttu-id="40c5e-147">To není jenom platné usnadnění přístupu pro vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="40c5e-147">That's not the only valid accessibility for properties.</span></span>
<span data-ttu-id="40c5e-148">Můžete vytvořit vlastnosti jen pro čtení, nebo poskytují různou přístupností. do sady a získat přístupové objekty.</span><span class="sxs-lookup"><span data-stu-id="40c5e-148">You can create read-only properties, or give different accessibility to the set and get accessors.</span></span> <span data-ttu-id="40c5e-149">Předpokládejme, že vaše `Person` třída by měla pouze povolit, změna hodnoty `FirstName` vlastnost z jiné metody v dané třídě.</span><span class="sxs-lookup"><span data-stu-id="40c5e-149">Suppose that your `Person` class should only enable changing the value of the `FirstName` property from other methods in that class.</span></span> <span data-ttu-id="40c5e-150">Můžete zadat přístupový objekt set `private` usnadnění místo `public`:</span><span class="sxs-lookup"><span data-stu-id="40c5e-150">You could give the set accessor `private` accessibility instead of `public`:</span></span>

[!code-csharp[Using a private setter for a publicly readonly property](../../samples/snippets/csharp/properties/Person.cs#8)]

<span data-ttu-id="40c5e-151">Nyní `FirstName` vlastnost je přístupná z jakéhokoli kódu, ale lze přiřadit pouze od jiného kódu v `Person` třídy.</span><span class="sxs-lookup"><span data-stu-id="40c5e-151">Now, the `FirstName` property can be accessed from any code, but it can only be assigned from other code in the `Person` class.</span></span>

<span data-ttu-id="40c5e-152">Můžete přidat libovolné modifikátor omezující přístupová buď sadu nebo získat přístupové objekty.</span><span class="sxs-lookup"><span data-stu-id="40c5e-152">You can add any restrictive access modifier to either the set or get accessors.</span></span> <span data-ttu-id="40c5e-153">Žádné modifikátor přístupu, který umístíte na jednotlivé přístupový objekt musí být omezeny více než modifikátor přístupu v definici vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="40c5e-153">Any access modifier you place on the individual accessor must be more limited than the access modifier on the property definition.</span></span> <span data-ttu-id="40c5e-154">Výše uvedené je právní protože `FirstName` vlastnost `public`, ale přistupující objekt Jet není `private`.</span><span class="sxs-lookup"><span data-stu-id="40c5e-154">The above is legal because the `FirstName` property is `public`, but the set accessor is `private`.</span></span> <span data-ttu-id="40c5e-155">Nelze deklarovat `private` vlastnost s `public` přistupujícího objektu.</span><span class="sxs-lookup"><span data-stu-id="40c5e-155">You could not declare a `private` property with a `public` accessor.</span></span> <span data-ttu-id="40c5e-156">Deklarace vlastností lze také deklarovat `protected`, `internal`, `protected internal`, nebo dokonce `private`.</span><span class="sxs-lookup"><span data-stu-id="40c5e-156">Property declarations can also be declared `protected`, `internal`, `protected internal`, or, even `private`.</span></span>

<span data-ttu-id="40c5e-157">Je také možné umístit na víc omezující modifikátor `get` přistupujícího objektu.</span><span class="sxs-lookup"><span data-stu-id="40c5e-157">It is also legal to place the more restrictive modifier on the `get` accessor.</span></span> <span data-ttu-id="40c5e-158">Například můžete mít `public` vlastnost, ale omezit `get` přístupového objektu `private`.</span><span class="sxs-lookup"><span data-stu-id="40c5e-158">For example, you could have a `public` property, but restrict the `get` accessor to `private`.</span></span> <span data-ttu-id="40c5e-159">Tento scénář se jen zřídka provádí v praxi.</span><span class="sxs-lookup"><span data-stu-id="40c5e-159">That scenario is rarely done in practice.</span></span>

<span data-ttu-id="40c5e-160">Můžete také omezit změny vlastnosti tak, aby lze nastavit pouze v konstruktoru nebo inicializátoru vlastnost.</span><span class="sxs-lookup"><span data-stu-id="40c5e-160">You can also restrict modifications to a property so that it can only be set in a constructor or a property initializer.</span></span> <span data-ttu-id="40c5e-161">Můžete upravit `Person` třídy to následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="40c5e-161">You can modify the `Person` class so as follows:</span></span>

[!code-csharp[A readonly auto implemented property](../../samples/snippets/csharp/properties/Person.cs#9)]

<span data-ttu-id="40c5e-162">Tato funkce se nejčastěji používá pro inicializaci kolekce, které jsou vystaveny jako vlastnosti jen pro čtení:</span><span class="sxs-lookup"><span data-stu-id="40c5e-162">This feature is most commonly used for initializing collections that are exposed as read-only properties:</span></span>

```csharp
public class Measurements
{
    public ICollection<DataPoint> points { get; } = new List<DataPoint>();
}
```

### <a name="computed-properties"></a><span data-ttu-id="40c5e-163">Vypočítané vlastnosti</span><span class="sxs-lookup"><span data-stu-id="40c5e-163">Computed properties</span></span>

<span data-ttu-id="40c5e-164">Vlastnost není potřeba jednoduše vrátit hodnotu člena pole.</span><span class="sxs-lookup"><span data-stu-id="40c5e-164">A property does not need to simply return the value of a member field.</span></span> <span data-ttu-id="40c5e-165">Můžete vytvořit vlastnosti, které vrací vypočítanou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="40c5e-165">You can create properties that return a computed value.</span></span> <span data-ttu-id="40c5e-166">Můžeme rozšířit `Person` objekt vrátí úplný název, počítaný zřetězením jména a příjmení:</span><span class="sxs-lookup"><span data-stu-id="40c5e-166">Let's expand the `Person` object to return the full name, computed by concatenating the first and last names:</span></span>

[!code-csharp[A computed property](../../samples/snippets/csharp/properties/Person.cs#10)]

<span data-ttu-id="40c5e-167">V příkladu výše používá [interpolace](../csharp/language-reference/tokens/interpolated.md) funkci pro vytvoření formátovaný řetězec pro úplný název.</span><span class="sxs-lookup"><span data-stu-id="40c5e-167">The example above uses the [string interpolation](../csharp/language-reference/tokens/interpolated.md) feature to create the formatted string for the full name.</span></span>

<span data-ttu-id="40c5e-168">Můžete také použít *s výrazem v těle členské*, která poskytuje stručnější způsob, jak vytvořit počítaných `FullName` vlastnost:</span><span class="sxs-lookup"><span data-stu-id="40c5e-168">You can also use an *expression-bodied member*, which provides a more succinct way to create the computed `FullName` property:</span></span>

[!code-csharp[A computed property using an expression bodied member](../../samples/snippets/csharp/properties/Person.cs#11)]

<span data-ttu-id="40c5e-169">*Členové tvoření* použít *výraz lambda* syntaxe pro definování metod, které obsahují jeden výraz.</span><span class="sxs-lookup"><span data-stu-id="40c5e-169">*Expression-bodied members* use the *lambda expression* syntax to define methods that contain a single expression.</span></span> <span data-ttu-id="40c5e-170">Tady tento výraz vrátí úplný název objektu osoba.</span><span class="sxs-lookup"><span data-stu-id="40c5e-170">Here, that expression returns the full name for the person object.</span></span>

### <a name="cached-evaluated-properties"></a><span data-ttu-id="40c5e-171">Vyhodnocený vlastnostem uloženým v mezipaměti</span><span class="sxs-lookup"><span data-stu-id="40c5e-171">Cached evaluated properties</span></span>

<span data-ttu-id="40c5e-172">Můžete kombinovat koncept vypočítané vlastnosti s úložištěm a vytvářet *uložené v mezipaměti Vyhodnocená vlastnost*.</span><span class="sxs-lookup"><span data-stu-id="40c5e-172">You can mix the concept of a computed property with storage and create a *cached evaluated property*.</span></span>  <span data-ttu-id="40c5e-173">Například můžete aktualizovat `FullName` vlastnost tak, aby formátování řetězce pouze se stalo se použila poprvé:</span><span class="sxs-lookup"><span data-stu-id="40c5e-173">For example, you could update the `FullName` property so that the string formatting only happened the first time it was accessed:</span></span>

[!code-csharp[Caching the value of a computed property](../../samples/snippets/csharp/properties/Person.cs#12)]

<span data-ttu-id="40c5e-174">Výše uvedený kód však obsahuje chybu.</span><span class="sxs-lookup"><span data-stu-id="40c5e-174">The above code contains a bug though.</span></span> <span data-ttu-id="40c5e-175">Pokud kód aktualizuje hodnotu buď `FirstName` nebo `LastName` vlastnost, dříve Vyhodnocená `fullName` pole je neplatné.</span><span class="sxs-lookup"><span data-stu-id="40c5e-175">If code updates the value of either the `FirstName` or `LastName` property, the previously evaluated `fullName` field is invalid.</span></span> <span data-ttu-id="40c5e-176">Můžete změnit `set` přistupující objekty `FirstName` a `LastName` vlastnost tak, aby `fullName` pole se vypočítá znovu:</span><span class="sxs-lookup"><span data-stu-id="40c5e-176">You modify the `set` accessors of the `FirstName` and `LastName` property so that the `fullName` field is calculated again:</span></span>

[!code-csharp[Invalidating the cache correctly](../../samples/snippets/csharp/properties/Person.cs#13)]

<span data-ttu-id="40c5e-177">Tento poslední verze vyhodnocuje `FullName` vlastnost jenom v případě potřeby.</span><span class="sxs-lookup"><span data-stu-id="40c5e-177">This final version evaluates the `FullName` property only when needed.</span></span>
<span data-ttu-id="40c5e-178">Pokud dříve vypočtené verze je platný, se používá.</span><span class="sxs-lookup"><span data-stu-id="40c5e-178">If the previously calculated version is valid, it's used.</span></span> <span data-ttu-id="40c5e-179">Pokud jiná změna stavu zruší platnost dříve vypočtené verze, bude přepočítat.</span><span class="sxs-lookup"><span data-stu-id="40c5e-179">If another state change invalidates the previously calculated version, it will be recalculated.</span></span> <span data-ttu-id="40c5e-180">Vývojáři, kteří tuto třídu použít, není potřeba znát podrobnosti implementace.</span><span class="sxs-lookup"><span data-stu-id="40c5e-180">Developers that use this class do not need to know the details of the implementation.</span></span> <span data-ttu-id="40c5e-181">Žádná z těchto změn mají vliv na použití objektu osoba.</span><span class="sxs-lookup"><span data-stu-id="40c5e-181">None of these internal changes affect the use of the Person object.</span></span> <span data-ttu-id="40c5e-182">To je důvod klíče pro použití vlastností ke zveřejnění datové členy objektu.</span><span class="sxs-lookup"><span data-stu-id="40c5e-182">That's the key reason for using Properties to expose data members of an object.</span></span>

### <a name="attaching-attributes-to-auto-implemented-properties"></a><span data-ttu-id="40c5e-183">Atributy se připojuje k automaticky implementované vlastnosti</span><span class="sxs-lookup"><span data-stu-id="40c5e-183">Attaching attributes to auto-implemented properties</span></span>

<span data-ttu-id="40c5e-184">Počínaje C# 7.3, atributy polí lze připojit k pomocným polem generované kompilátorem v automaticky implementované vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="40c5e-184">Beginning with C# 7.3, field attributes can be attached to the compiler generated backing field in auto-implemented properties.</span></span> <span data-ttu-id="40c5e-185">Představte si třeba revizi `Person` třídu, která přidá jedinečné celé `Id` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="40c5e-185">For example, consider a revision to the `Person` class that adds a unique integer `Id` property.</span></span>
<span data-ttu-id="40c5e-186">Při psaní`Id` vlastnost pomocí automaticky implementované vlastnosti, ale váš návrh nevolá pro trvalé `Id` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="40c5e-186">You write the`Id` property using an auto-implemented property, but your design does not call for persisting the `Id` property.</span></span> <span data-ttu-id="40c5e-187"><xref:System.NonSerializedAttribute> Lze připojit pouze na pole, nikoli vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="40c5e-187">The <xref:System.NonSerializedAttribute> can only be attached to fields, not properties.</span></span> <span data-ttu-id="40c5e-188">Můžete připojit <xref:System.NonSerializedAttribute> na pomocné pole `Id` vlastnost s použitím `field:` specifikátor atributu, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="40c5e-188">You can attach the <xref:System.NonSerializedAttribute> to the backing field for the `Id` property by using the `field:` specifier on the attribute, as shown in the following example:</span></span>

[!code-csharp[Attaching attributes to a backing field](../../samples/snippets/csharp/properties/Person.cs#14)]

<span data-ttu-id="40c5e-189">Tento postup funguje pro všechny atributy, které můžete připojit na automaticky implementované vlastnosti pole zálohování.</span><span class="sxs-lookup"><span data-stu-id="40c5e-189">This technique works for any attribute you attach to the backing field on the auto-implemented property.</span></span>

### <a name="implementing-inotifypropertychanged"></a><span data-ttu-id="40c5e-190">Implementace rozhraní INotifyPropertyChanged.</span><span class="sxs-lookup"><span data-stu-id="40c5e-190">Implementing INotifyPropertyChanged</span></span>

<span data-ttu-id="40c5e-191">Poslední scénář, kde budete muset psát kód v přistupující objekt vlastnosti je podpora <xref:System.ComponentModel.INotifyPropertyChanged> rozhraní používá k upozornění klientů vazby dat, které byla hodnota změněna.</span><span class="sxs-lookup"><span data-stu-id="40c5e-191">A final scenario where you need to write code in a property accessor is to support the <xref:System.ComponentModel.INotifyPropertyChanged> interface used to notify data binding clients that a value has changed.</span></span> <span data-ttu-id="40c5e-192">Při změně hodnoty vlastnosti, vyvolá objekt <xref:System.ComponentModel.INotifyPropertyChanged.PropertyChanged?displayProperty=nameWithType> událost označující změnu.</span><span class="sxs-lookup"><span data-stu-id="40c5e-192">When the value of a property changes, the object raises the <xref:System.ComponentModel.INotifyPropertyChanged.PropertyChanged?displayProperty=nameWithType> event to indicate the change.</span></span> <span data-ttu-id="40c5e-193">Datové vazby knihovny, zase aktualizaci založené na tuto změnu zobrazení elementů.</span><span class="sxs-lookup"><span data-stu-id="40c5e-193">The data binding libraries, in turn, update display elements based on that change.</span></span> <span data-ttu-id="40c5e-194">Následující kód ukazuje, jak by implementovat `INotifyPropertyChanged` pro `FirstName` vlastnosti třídy této osoby.</span><span class="sxs-lookup"><span data-stu-id="40c5e-194">The code below shows how you would implement `INotifyPropertyChanged` for the `FirstName` property of this person class.</span></span>

[!code-csharp[invalidating the cache correctly](../../samples/snippets/csharp/properties/Person.cs#15)]

<span data-ttu-id="40c5e-195">`?.` Operátor je volána *null podmiňovací operátor*.</span><span class="sxs-lookup"><span data-stu-id="40c5e-195">The `?.` operator is called the *null conditional operator*.</span></span> <span data-ttu-id="40c5e-196">Před vyhodnocením pravé straně operátoru zkontroluje odkaz s hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="40c5e-196">It checks for a null reference before evaluating the right side of the operator.</span></span> <span data-ttu-id="40c5e-197">Konečným výsledkem je, že pokud neexistují žádné Odběratelé `PropertyChanged` událostí, nebude spouštět kód pro vyvolání události.</span><span class="sxs-lookup"><span data-stu-id="40c5e-197">The end result is that if there are no subscribers to the `PropertyChanged` event, the code to raise the event doesn't execute.</span></span> <span data-ttu-id="40c5e-198">To vede `NullReferenceException` bez to zkontrolovat v takovém případě.</span><span class="sxs-lookup"><span data-stu-id="40c5e-198">It would throw a `NullReferenceException` without this check in that case.</span></span> <span data-ttu-id="40c5e-199">Další informace najdete na webu [`events`](delegates-events.md).</span><span class="sxs-lookup"><span data-stu-id="40c5e-199">For more information, see [`events`](delegates-events.md).</span></span> <span data-ttu-id="40c5e-200">Tento příklad také používá nový `nameof` operátor převodu ze symbolů vlastnost název na jeho textové vyjádření.</span><span class="sxs-lookup"><span data-stu-id="40c5e-200">This example also uses the new `nameof` operator to convert from the property name symbol to its text representation.</span></span>
<span data-ttu-id="40c5e-201">Pomocí `nameof` může snížit chyby, kde jste zadali název vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="40c5e-201">Using `nameof` can reduce errors where you have mistyped the name of the property.</span></span>

<span data-ttu-id="40c5e-202">Znovu, implementace <xref:System.ComponentModel.INotifyPropertyChanged> je příkladem případem, kde psát kód v vaše přístupové objekty pro zajištění podpory scénářů, které potřebujete.</span><span class="sxs-lookup"><span data-stu-id="40c5e-202">Again, implementing <xref:System.ComponentModel.INotifyPropertyChanged> is an example of a case where you can write code in your accessors to support the scenarios you need.</span></span>

## <a name="summing-up"></a><span data-ttu-id="40c5e-203">Shrnutí</span><span class="sxs-lookup"><span data-stu-id="40c5e-203">Summing up</span></span>

<span data-ttu-id="40c5e-204">Vlastnosti jsou formou inteligentních polí ve třídě nebo objekt.</span><span class="sxs-lookup"><span data-stu-id="40c5e-204">Properties are a form of smart fields in a class or object.</span></span> <span data-ttu-id="40c5e-205">Z mimo objektu, zobrazí se jako pole v objektu.</span><span class="sxs-lookup"><span data-stu-id="40c5e-205">From outside the object, they appear like fields in the object.</span></span> <span data-ttu-id="40c5e-206">Nicméně vlastnosti je možné implementovat pomocí úplné paletu C# funkce.</span><span class="sxs-lookup"><span data-stu-id="40c5e-206">However, properties can be implemented using the full palette of C# functionality.</span></span>
<span data-ttu-id="40c5e-207">Můžete zadat ověření, různou přístupností, opožděné vyhodnocení nebo všechny požadavky potřebné pro vaše scénáře.</span><span class="sxs-lookup"><span data-stu-id="40c5e-207">You can provide validation, different accessibility, lazy evaluation, or any requirements your scenarios need.</span></span>
