---
title: Omezení parametrů typu – C# Průvodce programováním
ms.custom: seodec18
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: bb545d9da73154c237f55809a3a72ff0f121ce1a
ms.sourcegitcommit: 4e2d355baba82814fa53efd6b8bbb45bfe054d11
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/04/2019
ms.locfileid: "70253011"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="044fd-102">Omezení parametrů typu (C# Průvodce programováním)</span><span class="sxs-lookup"><span data-stu-id="044fd-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="044fd-103">Omezení informují kompilátor o možnostech, které musí mít argument typu.</span><span class="sxs-lookup"><span data-stu-id="044fd-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="044fd-104">Bez omezení může být argumentem typu libovolný typ.</span><span class="sxs-lookup"><span data-stu-id="044fd-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="044fd-105">Kompilátor může předpokládat pouze členy <xref:System.Object?displayProperty=nameWithType>, což je maximální základní třída pro libovolný typ rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="044fd-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="044fd-106">Další informace najdete v tématu [Proč použít omezení](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="044fd-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="044fd-107">Pokud se klientský kód pokusí vytvořit instanci vaší třídy pomocí typu, který není povolen omezením, výsledkem je chyba při kompilaci.</span><span class="sxs-lookup"><span data-stu-id="044fd-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="044fd-108">Omezení jsou určena pomocí `where` klíčového slova kontextové.</span><span class="sxs-lookup"><span data-stu-id="044fd-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="044fd-109">V následující tabulce jsou uvedeny sedm typů omezení:</span><span class="sxs-lookup"><span data-stu-id="044fd-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="044fd-110">Jedinečn</span><span class="sxs-lookup"><span data-stu-id="044fd-110">Constraint</span></span>|<span data-ttu-id="044fd-111">Popis</span><span class="sxs-lookup"><span data-stu-id="044fd-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="044fd-112">Argument typu musí být typ hodnoty.</span><span class="sxs-lookup"><span data-stu-id="044fd-112">The type argument must be a value type.</span></span> <span data-ttu-id="044fd-113">Jakýkoli typ hodnoty s <xref:System.Nullable%601> výjimkou lze zadat.</span><span class="sxs-lookup"><span data-stu-id="044fd-113">Any value type except <xref:System.Nullable%601> can be specified.</span></span> <span data-ttu-id="044fd-114">Další informace o typech Nullable naleznete v tématu [typy s možnou hodnotou null](../nullable-types/index.md).</span><span class="sxs-lookup"><span data-stu-id="044fd-114">For more information about nullable types, see [Nullable types](../nullable-types/index.md).</span></span>|
|`where T : class`|<span data-ttu-id="044fd-115">Argument typu musí být typ odkazu.</span><span class="sxs-lookup"><span data-stu-id="044fd-115">The type argument must be a reference type.</span></span> <span data-ttu-id="044fd-116">Toto omezení platí také pro libovolnou třídu, rozhraní, delegáta nebo typ pole.</span><span class="sxs-lookup"><span data-stu-id="044fd-116">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="044fd-117">Argument typu musí být typ, který nemůže mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="044fd-117">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="044fd-118">Argumentem může být typ odkazu, který neumožňuje hodnotu C# null, v 8,0 nebo novějším, nebo typ hodnoty, která není null.</span><span class="sxs-lookup"><span data-stu-id="044fd-118">The argument can be a non-nullable reference type in C# 8.0 or later, or a not nullable value type.</span></span> <span data-ttu-id="044fd-119">Toto omezení platí také pro libovolnou třídu, rozhraní, delegáta nebo typ pole.</span><span class="sxs-lookup"><span data-stu-id="044fd-119">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="044fd-120">Argument typu musí být [nespravovaný typ](../../language-reference/builtin-types/unmanaged-types.md).</span><span class="sxs-lookup"><span data-stu-id="044fd-120">The type argument must be an [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span>|
|`where T : new()`|<span data-ttu-id="044fd-121">Argument typu musí mít veřejný konstruktor bez parametrů.</span><span class="sxs-lookup"><span data-stu-id="044fd-121">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="044fd-122">Při použití společně s jinými omezeními je `new()` třeba omezení zadat jako poslední.</span><span class="sxs-lookup"><span data-stu-id="044fd-122">When used together with other constraints, the `new()` constraint must be specified last.</span></span>|
|<span data-ttu-id="044fd-123">`where T :`název základní třídy >  *\<*</span><span class="sxs-lookup"><span data-stu-id="044fd-123">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="044fd-124">Argument typu musí být nebo odvozen ze zadané základní třídy.</span><span class="sxs-lookup"><span data-stu-id="044fd-124">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="044fd-125">`where T :` *názevrozhraní\<>*</span><span class="sxs-lookup"><span data-stu-id="044fd-125">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="044fd-126">Argument typu musí být nebo implementovat zadané rozhraní.</span><span class="sxs-lookup"><span data-stu-id="044fd-126">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="044fd-127">Je možné zadat více omezení rozhraní.</span><span class="sxs-lookup"><span data-stu-id="044fd-127">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="044fd-128">Omezení rozhraní může být také obecné.</span><span class="sxs-lookup"><span data-stu-id="044fd-128">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="044fd-129">Argument typu zadaný pro T musí být nebo odvozen od argumentu zadaného pro U.</span><span class="sxs-lookup"><span data-stu-id="044fd-129">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

<span data-ttu-id="044fd-130">Některá omezení se vzájemně vylučují.</span><span class="sxs-lookup"><span data-stu-id="044fd-130">Some of the constraints are mutually exclusive.</span></span> <span data-ttu-id="044fd-131">Všechny typy hodnot musí mít přístupný konstruktor bez parametrů.</span><span class="sxs-lookup"><span data-stu-id="044fd-131">All value types must have an accessible parameterless constructor.</span></span> <span data-ttu-id="044fd-132">Omezení implikuje `new()`omezenía omezení nelze kombinovat s `struct` omezením. `new()` `struct`</span><span class="sxs-lookup"><span data-stu-id="044fd-132">The `struct` constraint implies the `new()` constraint and the `new()` constraint can't be combined with the `struct` constraint.</span></span> <span data-ttu-id="044fd-133">`unmanaged` Omezení implikuje omezení. `struct`</span><span class="sxs-lookup"><span data-stu-id="044fd-133">The `unmanaged` constraint implies the `struct` constraint.</span></span> <span data-ttu-id="044fd-134">Omezení nelze kombinovat s `struct` omezeními nebo `new()`. `unmanaged`</span><span class="sxs-lookup"><span data-stu-id="044fd-134">The `unmanaged` constraint can't be combined with either the `struct` or `new()` constraints.</span></span>

## <a name="why-use-constraints"></a><span data-ttu-id="044fd-135">Proč použít omezení</span><span class="sxs-lookup"><span data-stu-id="044fd-135">Why use constraints</span></span>

<span data-ttu-id="044fd-136">Omezením parametru typu zvýšíte počet povolených operací a volání metod do těch, které jsou podporovány typem omezení a všemi typy v její hierarchii dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="044fd-136">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="044fd-137">Pokud navrhujete obecné třídy nebo metody, pokud budete provádět jakoukoli operaci u obecných členů nad rámec jednoduchého přiřazení nebo volání jakékoli metody <xref:System.Object?displayProperty=nameWithType>, kterou nepodporuje, budete muset použít omezení na parametr typu.</span><span class="sxs-lookup"><span data-stu-id="044fd-137">When you design generic classes or methods, if you'll be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="044fd-138">Například omezení základní třídy říká kompilátoru, že jako argumenty typu budou použity pouze objekty tohoto typu nebo odvozené z tohoto typu.</span><span class="sxs-lookup"><span data-stu-id="044fd-138">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="044fd-139">Jakmile má kompilátor tuto záruku, může dovolit volání metod tohoto typu v obecné třídě.</span><span class="sxs-lookup"><span data-stu-id="044fd-139">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="044fd-140">Následující příklad kódu ukazuje funkce, které lze přidat do `GenericList<T>` třídy (v části [Úvod do obecných](introduction-to-generics.md)) pomocí omezení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="044fd-140">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](introduction-to-generics.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="044fd-141">Omezení umožňuje, aby obecná třída používala `Employee.Name` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="044fd-141">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="044fd-142">Omezení určuje, že všechny položky typu `T` jsou zaručeny `Employee` buď objekt, nebo objekt, který dědí z `Employee`.</span><span class="sxs-lookup"><span data-stu-id="044fd-142">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="044fd-143">U stejného parametru typu lze použít více omezení a samotné omezení mohou být obecné typy, a to takto:</span><span class="sxs-lookup"><span data-stu-id="044fd-143">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="044fd-144">Při použití `where T : class` omezení se `==` Vyhněte operátorům `!=` a v parametru typu, protože tyto operátory budou testovat pouze referenční identitu, nikoli pro rovnost hodnot.</span><span class="sxs-lookup"><span data-stu-id="044fd-144">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="044fd-145">K tomuto chování dochází, i když jsou tyto operátory přetíženy v typu, který se používá jako argument.</span><span class="sxs-lookup"><span data-stu-id="044fd-145">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="044fd-146">Následující kód ilustruje tento bod; výstup je false, i když <xref:System.String> třída přetěžuje `==` operátor.</span><span class="sxs-lookup"><span data-stu-id="044fd-146">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="044fd-147">Kompilátor zná `T` pouze odkazový typ v době kompilace a musí používat výchozí operátory, které jsou platné pro všechny typy odkazů.</span><span class="sxs-lookup"><span data-stu-id="044fd-147">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="044fd-148">Pokud je nutné testovat rovnost hodnot, doporučuje se také použít `where T : IEquatable<T>` omezení nebo `where T : IComparable<T>` a implementovat rozhraní v jakékoli třídě, která bude použita k vytvoření obecné třídy.</span><span class="sxs-lookup"><span data-stu-id="044fd-148">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="044fd-149">Omezení více parametrů</span><span class="sxs-lookup"><span data-stu-id="044fd-149">Constraining multiple parameters</span></span>

<span data-ttu-id="044fd-150">Můžete použít omezení na více parametrů a více omezení na jeden parametr, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="044fd-150">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="044fd-151">Neohraničené parametry typu</span><span class="sxs-lookup"><span data-stu-id="044fd-151">Unbounded type parameters</span></span>

 <span data-ttu-id="044fd-152">Parametry typu, které nemají žádná omezení, jako je například T ve `SampleClass<T>{}`veřejné třídě, se nazývají neohraničené parametry typu.</span><span class="sxs-lookup"><span data-stu-id="044fd-152">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="044fd-153">Neohraničené parametry typu mají následující pravidla:</span><span class="sxs-lookup"><span data-stu-id="044fd-153">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="044fd-154">Operátory `!=` a`==` nelze použít, protože není nijak zaručeno, že konkrétní argument typu bude podporovat tyto operátory.</span><span class="sxs-lookup"><span data-stu-id="044fd-154">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="044fd-155">Lze je převést na nebo z `System.Object` nebo explicitně převést na libovolný typ rozhraní.</span><span class="sxs-lookup"><span data-stu-id="044fd-155">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="044fd-156">Můžete je porovnat s [hodnotou null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="044fd-156">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="044fd-157">Pokud je neohraničený parametr porovnán `null`, porovnávání vždy vrátí hodnotu false, pokud je argumentem typu hodnotový typ.</span><span class="sxs-lookup"><span data-stu-id="044fd-157">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="044fd-158">Parametry typu jako omezení</span><span class="sxs-lookup"><span data-stu-id="044fd-158">Type parameters as constraints</span></span>

<span data-ttu-id="044fd-159">Použití parametru obecného typu jako omezení je užitečné, pokud členská funkce s vlastním parametrem typu musí omezit tento parametr na parametr typu nadřazeného typu, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="044fd-159">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="044fd-160">V předchozím příkladu `T` je omezení typu v kontextu `Add` metody a parametr nevázaného typu `List` v kontextu třídy.</span><span class="sxs-lookup"><span data-stu-id="044fd-160">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="044fd-161">Parametry typu lze také použít jako omezení v definicích obecných tříd.</span><span class="sxs-lookup"><span data-stu-id="044fd-161">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="044fd-162">Parametr typu musí být deklarovaný v lomených závorkách spolu s jinými parametry typu:</span><span class="sxs-lookup"><span data-stu-id="044fd-162">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="044fd-163">Užitečnost parametrů typu jako omezení s obecnými třídami je omezená, protože kompilátor může předpokládat nic o parametru typu s výjimkou toho, že je `System.Object`odvozen z.</span><span class="sxs-lookup"><span data-stu-id="044fd-163">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="044fd-164">Použijte parametry typu jako omezení pro obecné třídy ve scénářích, ve kterých chcete vynutit vztah dědičnosti mezi dvěma parametry typu.</span><span class="sxs-lookup"><span data-stu-id="044fd-164">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="044fd-165">Omezení NotNull</span><span class="sxs-lookup"><span data-stu-id="044fd-165">NotNull constraint</span></span>

<span data-ttu-id="044fd-166">Počínaje C# 8,0 můžete `notnull` omezení použít k určení toho, že argument typu musí být typ hodnoty, která není null, nebo odkaz na typ, který nepovoluje hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="044fd-166">Beginning with C# 8.0, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="044fd-167">Omezení lze použít pouze `nullable enable` v kontextu. `notnull`</span><span class="sxs-lookup"><span data-stu-id="044fd-167">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="044fd-168">Kompilátor vygeneruje upozornění, pokud přidáte `notnull` omezení do oblivious kontextu s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="044fd-168">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span> 

<span data-ttu-id="044fd-169">Na rozdíl od jiných omezení, pokud argument typu narušuje `notnull` omezení, kompilátor vygeneruje upozornění, když je tento kód zkompilován `nullable enable` v kontextu.</span><span class="sxs-lookup"><span data-stu-id="044fd-169">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="044fd-170">Pokud je kód zkompilován v oblivious kontextu s možnou hodnotou null, kompilátor negeneruje žádná upozornění ani chyby.</span><span class="sxs-lookup"><span data-stu-id="044fd-170">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="044fd-171">Nespravované omezení</span><span class="sxs-lookup"><span data-stu-id="044fd-171">Unmanaged constraint</span></span>

<span data-ttu-id="044fd-172">Počínaje C# 7,3 můžete `unmanaged` omezení použít k určení, že parametr typu musí být [nespravovaného typu](../../language-reference/builtin-types/unmanaged-types.md).</span><span class="sxs-lookup"><span data-stu-id="044fd-172">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be an [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="044fd-173">`unmanaged` Omezení umožňuje napsat opakovaně použitelné rutiny pro práci s typy, které mohou být manipulovány jako bloky paměti, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="044fd-173">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="044fd-174">Předchozí metoda musí být zkompilována v `unsafe` kontextu, protože `sizeof` používá operátor u typu, který není známý jako vestavěný typ.</span><span class="sxs-lookup"><span data-stu-id="044fd-174">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="044fd-175">`unmanaged` Bez omezení`sizeof` není operátor k dispozici.</span><span class="sxs-lookup"><span data-stu-id="044fd-175">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="044fd-176">Omezení delegování</span><span class="sxs-lookup"><span data-stu-id="044fd-176">Delegate constraints</span></span>

<span data-ttu-id="044fd-177">Počínaje C# 7,3 můžete také použít <xref:System.Delegate?displayProperty=nameWithType> nebo <xref:System.MulticastDelegate?displayProperty=nameWithType> jako omezení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="044fd-177">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="044fd-178">CLR vždy povoluje toto omezení, ale C# jazyk ho nepovolil.</span><span class="sxs-lookup"><span data-stu-id="044fd-178">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="044fd-179">`System.Delegate` Omezení umožňuje napsat kód, který pracuje s delegáty způsobem bezpečným pro typ.</span><span class="sxs-lookup"><span data-stu-id="044fd-179">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="044fd-180">Následující kód definuje metodu rozšíření, která kombinuje dva delegáty, pokud jsou stejného typu:</span><span class="sxs-lookup"><span data-stu-id="044fd-180">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="044fd-181">K kombinování delegátů, kteří mají stejný typ, můžete použít výše uvedenou metodu:</span><span class="sxs-lookup"><span data-stu-id="044fd-181">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="044fd-182">Pokud Odkomentujete poslední řádek, nebude zkompilován.</span><span class="sxs-lookup"><span data-stu-id="044fd-182">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="044fd-183">`first` A`test` jsou typy delegátů, ale jsou různými typy delegátů.</span><span class="sxs-lookup"><span data-stu-id="044fd-183">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="044fd-184">Omezení výčtu</span><span class="sxs-lookup"><span data-stu-id="044fd-184">Enum constraints</span></span>

<span data-ttu-id="044fd-185">Počínaje C# 7,3 můžete také zadat <xref:System.Enum?displayProperty=nameWithType> typ jako omezení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="044fd-185">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="044fd-186">CLR vždy povoluje toto omezení, ale C# jazyk ho nepovolil.</span><span class="sxs-lookup"><span data-stu-id="044fd-186">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="044fd-187">Obecné typy pomocí `System.Enum` poskytují programování zajišťující bezpečnost typů pro ukládání výsledků z použití statických metod v `System.Enum`.</span><span class="sxs-lookup"><span data-stu-id="044fd-187">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="044fd-188">Následující ukázka vyhledá všechny platné hodnoty pro typ výčtu a potom vytvoří slovník, který tyto hodnoty mapuje na jeho řetězcovou reprezentaci.</span><span class="sxs-lookup"><span data-stu-id="044fd-188">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="044fd-189">Používané metody využívají reflexi, což má dopad na výkon.</span><span class="sxs-lookup"><span data-stu-id="044fd-189">The methods used make use of reflection, which has performance implications.</span></span> <span data-ttu-id="044fd-190">Tuto metodu lze zavolat pro sestavení kolekce, která je uložena v mezipaměti a znovu použita, nikoli volání, která vyžadují reflexi.</span><span class="sxs-lookup"><span data-stu-id="044fd-190">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="044fd-191">Můžete ji použít, jak je znázorněno v následujícím příkladu, pro vytvoření výčtu a sestavení slovníku jeho hodnot a názvů:</span><span class="sxs-lookup"><span data-stu-id="044fd-191">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="044fd-192">Viz také:</span><span class="sxs-lookup"><span data-stu-id="044fd-192">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="044fd-193">Průvodce programováním v jazyce C#</span><span class="sxs-lookup"><span data-stu-id="044fd-193">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="044fd-194">Úvod do obecných typů</span><span class="sxs-lookup"><span data-stu-id="044fd-194">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="044fd-195">Obecné třídy</span><span class="sxs-lookup"><span data-stu-id="044fd-195">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="044fd-196">new – omezení</span><span class="sxs-lookup"><span data-stu-id="044fd-196">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
