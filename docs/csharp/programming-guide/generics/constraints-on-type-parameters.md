---
title: Omezení parametrů typů (Průvodce programováním v C#)
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: df5a509296f3fb9e8e77a273a0636c74a6f86da3
ms.sourcegitcommit: 3ab9254890a52a50762995fa6d7d77a00348db7e
ms.translationtype: HT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/20/2018
ms.locfileid: "46473019"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="5f39c-102">Omezení parametrů typů (C# Programming Guide)</span><span class="sxs-lookup"><span data-stu-id="5f39c-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="5f39c-103">Omezení kompilátoru informuje o možnosti, které argument typu musí mít.</span><span class="sxs-lookup"><span data-stu-id="5f39c-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="5f39c-104">Bez jakýchkoliv omezení typu argument může být libovolného typu.</span><span class="sxs-lookup"><span data-stu-id="5f39c-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="5f39c-105">Kompilátor může převzít jenom jako objekty její členové <xref:System.Object?displayPropety=nameWithType>, což je základní třídy ultimate pro jakýkoli typ .NET.</span><span class="sxs-lookup"><span data-stu-id="5f39c-105">The compiler can only assume the members of <xref:System.Object?displayPropety=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="5f39c-106">Další informace najdete v tématu [Proč používat omezení](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="5f39c-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="5f39c-107">Pokud klientský kód se pokusí vytvořit instanci své třídy pomocí typu, který není povolen omezením, výsledkem je chyba kompilace.</span><span class="sxs-lookup"><span data-stu-id="5f39c-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="5f39c-108">Omezení je určené vlastností `where` kontextové klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="5f39c-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="5f39c-109">V následující tabulce jsou uvedeny sedm typy omezujících podmínek:</span><span class="sxs-lookup"><span data-stu-id="5f39c-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="5f39c-110">Omezení</span><span class="sxs-lookup"><span data-stu-id="5f39c-110">Constraint</span></span>|<span data-ttu-id="5f39c-111">Popis</span><span class="sxs-lookup"><span data-stu-id="5f39c-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="5f39c-112">Argument typu musí být typem hodnoty.</span><span class="sxs-lookup"><span data-stu-id="5f39c-112">The type argument must be a value type.</span></span> <span data-ttu-id="5f39c-113">Některá hodnota typu s výjimkou <xref:System.Nullable%601> lze zadat.</span><span class="sxs-lookup"><span data-stu-id="5f39c-113">Any value type except <xref:System.Nullable%601> can be specified.</span></span> <span data-ttu-id="5f39c-114">Další informace o typech s povolenou hodnotou Null, naleznete v tématu [typy připouštějící hodnotu Null](../nullable-types/index.md).</span><span class="sxs-lookup"><span data-stu-id="5f39c-114">For more information about nullable types, see [Nullable types](../nullable-types/index.md).</span></span>|
|`where T : class`|<span data-ttu-id="5f39c-115">Argument typu musí být typ odkazu.</span><span class="sxs-lookup"><span data-stu-id="5f39c-115">The type argument must be a reference type.</span></span> <span data-ttu-id="5f39c-116">Toto omezení platí také pro třídy, rozhraní, delegáta nebo typ pole.</span><span class="sxs-lookup"><span data-stu-id="5f39c-116">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="5f39c-117">Argument typu nemůže být odkazovým typem a nesmí obsahovat žádné členy typu odkazu na libovolné úrovni vnoření.</span><span class="sxs-lookup"><span data-stu-id="5f39c-117">The type argument must not be a reference type and must not contain any reference type members at any level of nesting.</span></span>|
|`where T : new()`|<span data-ttu-id="5f39c-118">Argument typu musí mít veřejný konstruktor bez parametrů.</span><span class="sxs-lookup"><span data-stu-id="5f39c-118">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="5f39c-119">Při použití s dalšími omezeními `new()` omezení musí být uvedený jako poslední.</span><span class="sxs-lookup"><span data-stu-id="5f39c-119">When used together with other constraints, the `new()` constraint must be specified last.</span></span>|
|<span data-ttu-id="5f39c-120">`where T :` *\<Název základní třídy >*</span><span class="sxs-lookup"><span data-stu-id="5f39c-120">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="5f39c-121">Argument typu musí být nebo odvozen od zadané základní třídy.</span><span class="sxs-lookup"><span data-stu-id="5f39c-121">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="5f39c-122">`where T :` *\<Název rozhraní >*</span><span class="sxs-lookup"><span data-stu-id="5f39c-122">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="5f39c-123">Argument typu musí být nebo implementovat rozhraní zadané.</span><span class="sxs-lookup"><span data-stu-id="5f39c-123">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="5f39c-124">Je možné zadat více omezením rozhraní.</span><span class="sxs-lookup"><span data-stu-id="5f39c-124">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="5f39c-125">Omezující rozhraní může být obecný.</span><span class="sxs-lookup"><span data-stu-id="5f39c-125">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="5f39c-126">Argument typu zadaný pro T musí být nebo odvozené z argument zadaný pro U.</span><span class="sxs-lookup"><span data-stu-id="5f39c-126">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

<span data-ttu-id="5f39c-127">Některá omezení se vzájemně vylučují.</span><span class="sxs-lookup"><span data-stu-id="5f39c-127">Some of the constraints are mutually exclusive.</span></span> <span data-ttu-id="5f39c-128">Všechny hodnotové typy musí mít dostupný konstruktor bez parametrů.</span><span class="sxs-lookup"><span data-stu-id="5f39c-128">All value types must have an accessible parameterless constructor.</span></span> <span data-ttu-id="5f39c-129">`struct` Znamená omezení `new()` omezení a `new()` omezení nelze kombinovat s `struct` omezení.</span><span class="sxs-lookup"><span data-stu-id="5f39c-129">The `struct` constraint implies the `new()` constraint and the `new()` constraint cannot be combined with the `struct` constraint.</span></span> <span data-ttu-id="5f39c-130">`unmanaged` Omezení znamená, `struct` omezení.</span><span class="sxs-lookup"><span data-stu-id="5f39c-130">The `unmanaged` constraint implies the `struct` constraint.</span></span> <span data-ttu-id="5f39c-131">`unmanaged` Omezení nelze kombinovat s buď `struct` nebo `new()` omezení.</span><span class="sxs-lookup"><span data-stu-id="5f39c-131">The `unmanaged` constraint cannot be combined with either the `struct` or `new()` constraints.</span></span>

## <a name="why-use-constraints"></a><span data-ttu-id="5f39c-132">Proč používat omezení</span><span class="sxs-lookup"><span data-stu-id="5f39c-132">Why use constraints</span></span>

<span data-ttu-id="5f39c-133">Pomocí omezení parametru typu, zvyšte počet povolených operací a volání metody, které jsou podporovány omezující typ a všechny typy v hierarchii dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="5f39c-133">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="5f39c-134">Při návrhu obecných tříd nebo metod, pokud jste se všechny operace na obecné členy nad rámec jednoduché přiřazení nebo voláním jakékoli metody, které nepodporují <xref:System.Object?displayProperty=nameWithType>, budete muset použít omezení parametru typu.</span><span class="sxs-lookup"><span data-stu-id="5f39c-134">When you design generic classes or methods, if you will be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you will have to apply constraints to the type parameter.</span></span> <span data-ttu-id="5f39c-135">Například omezení základní třídy instruuje kompilátor, že pouze objekty tohoto typu nebo odvozený z tohoto typu se použije jako argumenty typu.</span><span class="sxs-lookup"><span data-stu-id="5f39c-135">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="5f39c-136">Jakmile kompilátor této záruky, můžete povolit metody tohoto typu k volání v obecné třídě.</span><span class="sxs-lookup"><span data-stu-id="5f39c-136">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="5f39c-137">Následující příklad kódu ukazuje funkce, můžete přidat `GenericList<T>` třídy (v [Úvod do obecných typů](introduction-to-generics.md)) s použitím omezení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="5f39c-137">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](introduction-to-generics.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="5f39c-138">Omezení umožňuje použití obecné třídy `Employee.Name` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="5f39c-138">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="5f39c-139">Omezení, která určuje všechny položky typu `T` je zaručeno, že se buď `Employee` objektu nebo objekt, který dědí z `Employee`.</span><span class="sxs-lookup"><span data-stu-id="5f39c-139">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="5f39c-140">Více omezení můžete použít pro stejný parametr typu a omezení sami může být obecné typy, následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="5f39c-140">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="5f39c-141">Při použití `where T : class` omezení, vyhněte `==` a `!=` operátory u parametru typu vzhledem k tomu, že tyto operátory testovat odkaz identity jenom, ne pro rovnost hodnot.</span><span class="sxs-lookup"><span data-stu-id="5f39c-141">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="5f39c-142">K tomuto chování dochází i v případě, že jsou tyto operátory přetíženy v typu, který se používá jako argument.</span><span class="sxs-lookup"><span data-stu-id="5f39c-142">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="5f39c-143">Následující kód znázorňuje tento bod; i když má hodnotu false výstup <xref:System.String> třídy přetížení `==` operátor.</span><span class="sxs-lookup"><span data-stu-id="5f39c-143">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="5f39c-144">Kompilátor ví pouze, že T je typ odkazu v době kompilace a musí používat výchozí operátory, které jsou platné pro všechny typy odkazů.</span><span class="sxs-lookup"><span data-stu-id="5f39c-144">The compiler only knows that T is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="5f39c-145">Pokud musíte otestovat pro hodnotu rovnosti, doporučený postup je také použít `where T : IEquatable<T>` nebo `where T : IComparable<T>` omezení a implementovat rozhraní do třídy, která se použije k vytvoření obecné třídy.</span><span class="sxs-lookup"><span data-stu-id="5f39c-145">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="5f39c-146">Omezení více parametrů</span><span class="sxs-lookup"><span data-stu-id="5f39c-146">Constraining multiple parameters</span></span>

<span data-ttu-id="5f39c-147">Více parametrů a více omezení na jeden parametr, můžete použít omezení, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="5f39c-147">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="5f39c-148">Parametry typu bez vazby</span><span class="sxs-lookup"><span data-stu-id="5f39c-148">Unbounded type parameters</span></span>

 <span data-ttu-id="5f39c-149">Zadejte parametry, které obsahují bez omezení, jako je například T v veřejnou třídu `SampleClass<T>{}`, jsou označované jako parametry typu bez vazby.</span><span class="sxs-lookup"><span data-stu-id="5f39c-149">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="5f39c-150">Parametry typu bez vazby mají následující pravidla:</span><span class="sxs-lookup"><span data-stu-id="5f39c-150">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="5f39c-151">`!=` a `==` operátory nelze použít, protože neexistuje žádná záruka, že argument typu implementujícího typ bude podporovat tyto operátory.</span><span class="sxs-lookup"><span data-stu-id="5f39c-151">The `!=` and `==` operators cannot be used because there is no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="5f39c-152">Může být převeden do a z `System.Object` nebo explicitně převést na libovolný typ rozhraní.</span><span class="sxs-lookup"><span data-stu-id="5f39c-152">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="5f39c-153">Můžete porovnat jim [null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="5f39c-153">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="5f39c-154">Pokud bez vazby parametru je ve srovnání s `null`, porovnání vždy vrátí hodnotu false, pokud typ argumentu je typ hodnoty.</span><span class="sxs-lookup"><span data-stu-id="5f39c-154">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="5f39c-155">Parametry typu jako omezení</span><span class="sxs-lookup"><span data-stu-id="5f39c-155">Type parameters as constraints</span></span>

<span data-ttu-id="5f39c-156">Použití parametru obecného typu je užitečné, když členské funkce s vlastním parametrem typu omezení má omezení parametru pro parametr typu nadřazeného typu, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="5f39c-156">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="5f39c-157">V předchozím příkladu `T` se omezení typu v kontextu `Add` metoda a parametr typu bez vazby v kontextu `List` třídy.</span><span class="sxs-lookup"><span data-stu-id="5f39c-157">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="5f39c-158">Parametry typu lze také použít jako omezení pro definice obecných tříd.</span><span class="sxs-lookup"><span data-stu-id="5f39c-158">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="5f39c-159">Parametr typu musí být deklarována v lomených závorkách společně s další parametry typu:</span><span class="sxs-lookup"><span data-stu-id="5f39c-159">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="5f39c-160">Užitečnost parametry typu jako omezení pomocí obecných tříd je omezený, protože kompilátor může převzít nic neříká o parametr typu s tím rozdílem, že se odvozuje od `System.Object`.</span><span class="sxs-lookup"><span data-stu-id="5f39c-160">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="5f39c-161">Parametry typu použijte jako omezení u obecných tříd ve scénářích, ve kterých chcete vynutit vztah dědičnosti mezi dvěma parametry typu.</span><span class="sxs-lookup"><span data-stu-id="5f39c-161">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="5f39c-162">Nespravované omezení</span><span class="sxs-lookup"><span data-stu-id="5f39c-162">Unmanaged constraint</span></span>

<span data-ttu-id="5f39c-163">Od verze C# 7.3, můžete použít `unmanaged` omezení, které chcete určit, že musí být parametr typu **nespravovaný typ**.</span><span class="sxs-lookup"><span data-stu-id="5f39c-163">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be an **unmanaged type**.</span></span> <span data-ttu-id="5f39c-164">**Nespravovaný typ** je typ, který není typem odkazu a neobsahuje pole typu odkazu na libovolné úrovni vnoření.</span><span class="sxs-lookup"><span data-stu-id="5f39c-164">An **unmanaged type** is a type that is not a reference type and doesn't contain reference type fields at any level of nesting.</span></span> <span data-ttu-id="5f39c-165">`unmanaged` Omezení umožňuje zapisovat opakovaně použitelných rutin pro práci s typy, které lze manipulovat jako bloky paměti, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="5f39c-165">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="5f39c-166">Předchozí metoda musí být zkompilovány do `unsafe` kontextu protože používá `sizeof` operátor pro typ není známé jako předdefinovaný typ.</span><span class="sxs-lookup"><span data-stu-id="5f39c-166">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="5f39c-167">Bez `unmanaged` omezení, `sizeof` operátor není k dispozici.</span><span class="sxs-lookup"><span data-stu-id="5f39c-167">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="5f39c-168">Omezení delegáta</span><span class="sxs-lookup"><span data-stu-id="5f39c-168">Delegate constraints</span></span>

<span data-ttu-id="5f39c-169">Také od verze C# 7.3, můžete použít <xref:System.Delegate?displayProperty=nameWithType> nebo <xref:System.MulticastDelegate?displayProperty=nameWithType> jako základní třída omezení.</span><span class="sxs-lookup"><span data-stu-id="5f39c-169">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="5f39c-170">Modul CLR je vždycky povolená omezení, ale jazyk C# je zakázané.</span><span class="sxs-lookup"><span data-stu-id="5f39c-170">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="5f39c-171">`System.Delegate` Omezení umožňuje napsat kód, který funguje s delegáty způsobem typově bezpečné.</span><span class="sxs-lookup"><span data-stu-id="5f39c-171">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="5f39c-172">Následující kód definuje metodu rozšíření, která kombinuje dva delegáty předpokladu, že jsou stejného typu:</span><span class="sxs-lookup"><span data-stu-id="5f39c-172">The following code defines an extension method that combines two delegates provided they are the same type:</span></span>

[!code-csharp[using the delegate constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="5f39c-173">Výše uvedené metody můžete kombinovat delegáty, kteří jsou stejného typu:</span><span class="sxs-lookup"><span data-stu-id="5f39c-173">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="5f39c-174">Pokud můžete zrušit komentář na posledním řádku, nebude kompilovat.</span><span class="sxs-lookup"><span data-stu-id="5f39c-174">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="5f39c-175">Obě `first` a `test` jsou typy delegátů, ale jsou typy různých delegáta.</span><span class="sxs-lookup"><span data-stu-id="5f39c-175">Both `first` and `test` are delegate types, but they are different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="5f39c-176">Omezení enum</span><span class="sxs-lookup"><span data-stu-id="5f39c-176">Enum constraints</span></span>

<span data-ttu-id="5f39c-177">Od C# 7.3, můžete také určit <xref:System.Enum?displayProperty=nameWithType> typ jako základní třída omezení.</span><span class="sxs-lookup"><span data-stu-id="5f39c-177">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="5f39c-178">Modul CLR je vždycky povolená omezení, ale jazyk C# je zakázané.</span><span class="sxs-lookup"><span data-stu-id="5f39c-178">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="5f39c-179">Použití obecných typů `System.Enum` poskytují zajišťující bezpečnost typů programování výsledky do mezipaměti pomocí statické metody v `System.Enum`.</span><span class="sxs-lookup"><span data-stu-id="5f39c-179">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="5f39c-180">Následující příklad vyhledá všechny platné hodnoty pro typ výčtu a poté sestaví slovník, který mapuje tyto hodnoty na řetězcové vyjádření.</span><span class="sxs-lookup"><span data-stu-id="5f39c-180">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="5f39c-181">Ujistěte se, metod používaných využívání reflection, což má vliv na výkon.</span><span class="sxs-lookup"><span data-stu-id="5f39c-181">The methods used make use of reflection, which has performance implications.</span></span> <span data-ttu-id="5f39c-182">Můžete volat tuto metodu za účelem vytvoření kolekce, která je do mezipaměti a znovu použít namísto opakování volání, které vyžadují reflexe.</span><span class="sxs-lookup"><span data-stu-id="5f39c-182">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="5f39c-183">Můžete ji třeba použít jak je znázorněno v následujícím příkladu vytvořit výčet a vytvářet slovník jeho hodnoty a názvy:</span><span class="sxs-lookup"><span data-stu-id="5f39c-183">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="5f39c-184">Viz také</span><span class="sxs-lookup"><span data-stu-id="5f39c-184">See Also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="5f39c-185">Průvodce programováním v jazyce C#</span><span class="sxs-lookup"><span data-stu-id="5f39c-185">C# Programming Guide</span></span>](../../../csharp/programming-guide/index.md)  
- [<span data-ttu-id="5f39c-186">Úvod do obecných typů</span><span class="sxs-lookup"><span data-stu-id="5f39c-186">Introduction to Generics</span></span>](../../../csharp/programming-guide/generics/introduction-to-generics.md)  
- [<span data-ttu-id="5f39c-187">Obecné třídy</span><span class="sxs-lookup"><span data-stu-id="5f39c-187">Generic Classes</span></span>](../../../csharp/programming-guide/generics/generic-classes.md)  
- [<span data-ttu-id="5f39c-188">new – omezení</span><span class="sxs-lookup"><span data-stu-id="5f39c-188">new Constraint</span></span>](../../../csharp/language-reference/keywords/new-constraint.md)  
