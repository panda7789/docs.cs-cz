---
title: Omezení parametrů typů (Průvodce programováním v C#)
ms.date: 04/12/2018
ms.prod: .net
ms.technology:
- devlang-csharp
ms.topic: article
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
author: BillWagner
ms.author: wiwagn
ms.openlocfilehash: 25c4a1137df335080659bf878fce5e3a3270dfda
ms.sourcegitcommit: 2042de78fcdceebb6b8ac4b7a292b93e8782cbf5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/27/2018
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="5cb3f-102">Omezení parametrů typů (C# Průvodce programováním)</span><span class="sxs-lookup"><span data-stu-id="5cb3f-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="5cb3f-103">Omezení kompilátoru informovat o funkcích, které musí mít argument typu.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="5cb3f-104">Bez žádná omezení může být argument typu libovolného typu.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="5cb3f-105">Kompilátor můžete předpokládat pouze členové <xref:System.Object?displayPropety=nameWithType>, což je ultimate základní třídu pro jakýkoli typ rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-105">The compiler can only assume the members of <xref:System.Object?displayPropety=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="5cb3f-106">Další informace najdete v tématu [Proč používat omezení](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="5cb3f-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="5cb3f-107">Pokud kód klienta se pokusí vytvořit třídu pomocí typu, která není povolena omezení, výsledkem je chyba kompilace.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="5cb3f-108">Omezení zadávají pomocí `where` kontextové klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="5cb3f-109">Následující tabulka uvádí typy sedm omezení:</span><span class="sxs-lookup"><span data-stu-id="5cb3f-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="5cb3f-110">Omezení</span><span class="sxs-lookup"><span data-stu-id="5cb3f-110">Constraint</span></span>|<span data-ttu-id="5cb3f-111">Popis</span><span class="sxs-lookup"><span data-stu-id="5cb3f-111">Description</span></span>|
|----------------|-----------------|
|`where T: struct`|<span data-ttu-id="5cb3f-112">Argument typu musí být typ hodnoty.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-112">The type argument must be a value type.</span></span> <span data-ttu-id="5cb3f-113">Některé hodnoty typ s výjimkou <xref:System.Nullable> lze zadat.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-113">Any value type except <xref:System.Nullable> can be specified.</span></span> <span data-ttu-id="5cb3f-114">Další informace najdete v tématu [pomocí typy s možnou hodnotou Null](../nullable-types/using-nullable-types.md).</span><span class="sxs-lookup"><span data-stu-id="5cb3f-114">For more information, see [Using Nullable Types](../nullable-types/using-nullable-types.md).</span></span>|
|`where T : class`|<span data-ttu-id="5cb3f-115">Argument typu musí být odkazového typu.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-115">The type argument must be a reference type.</span></span> <span data-ttu-id="5cb3f-116">Toto omezení platí také pro třídu, rozhraní, delegát nebo typ pole.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-116">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="5cb3f-117">Argument typu nesmí být odkazového typu a nesmí obsahovat žádné členy typu odkaz na libovolnou úroveň vnoření.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-117">The type argument must not be a reference type and must not contain any reference type members at any level of nesting.</span></span>|
|`where T : new()`|<span data-ttu-id="5cb3f-118">Argument typu musí mít konstruktor public bez parametrů.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-118">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="5cb3f-119">Při použití spolu s jiná omezení `new()` omezení musí být uvedený jako poslední.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-119">When used together with other constraints, the `new()` constraint must be specified last.</span></span>|
|<span data-ttu-id="5cb3f-120">`where T :` *\<Název základní třídy >*</span><span class="sxs-lookup"><span data-stu-id="5cb3f-120">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="5cb3f-121">Argument typu musí být nebo odvozena od zadané základní třídy.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-121">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="5cb3f-122">`where T :` *\<Název rozhraní >*</span><span class="sxs-lookup"><span data-stu-id="5cb3f-122">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="5cb3f-123">Argument typu musí být nebo implementovat rozhraní zadaný.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-123">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="5cb3f-124">Je možné zadat více omezení rozhraní.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-124">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="5cb3f-125">Omezení rozhraní může být také obecné.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-125">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="5cb3f-126">Argument typu zadaná pro T musí být nebo odvozena od argument zadaný pro U.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-126">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

<span data-ttu-id="5cb3f-127">Některá omezení se vzájemně vylučují.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-127">Some of the constraints are mutually exclusive.</span></span> <span data-ttu-id="5cb3f-128">Všechny typy hodnot musí mít přístupné bezparametrový konstruktor.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-128">All value types must have an accessible parameterless constructor.</span></span> <span data-ttu-id="5cb3f-129">`struct` Znamená omezení `new()` omezení a `new()` omezení nelze kombinovat s `struct` omezení.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-129">The `struct` constraint implies the `new()` constraint and the `new()` constraint cannot be combined with the `struct` constraint.</span></span> <span data-ttu-id="5cb3f-130">`unmanaged` Znamená omezení `struct` omezení.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-130">The `unmanaged` constraint implies the `struct` constraint.</span></span> <span data-ttu-id="5cb3f-131">`unmanaged` Omezení nelze kombinovat s buď `struct` nebo `new()` omezení.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-131">The `unmanaged` constraint cannot be combined with either the `struct` or `new()` constraints.</span></span>

## <a name="why-use-constraints"></a><span data-ttu-id="5cb3f-132">Proč používat omezení</span><span class="sxs-lookup"><span data-stu-id="5cb3f-132">Why use constraints</span></span>

<span data-ttu-id="5cb3f-133">Omezíte parametr typu, můžete zvýšit počet povolených operací a volání metody jsou podporovány všechny typy v hierarchii dědičnosti a omezení typu.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-133">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="5cb3f-134">Při návrhu obecné třídy nebo metody, pokud se budete provádět všechny operace v obecné členy nad rámec jednoduché přiřazení nebo volání žádné metody, které nepodporují <xref:System.Object?displayProperty=nameWithType>, budete muset použít omezení pro parametr typu.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-134">When you design generic classes or methods, if you will be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you will have to apply constraints to the type parameter.</span></span> <span data-ttu-id="5cb3f-135">Například omezení základní třídy říká kompilátoru, že pouze objekty tohoto typu nebo odvozené z tohoto typu se použije jako argumenty typu.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-135">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="5cb3f-136">Jakmile kompilátor tento záruka, můžete povolit metod k volání ve třídě obecného typu.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-136">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="5cb3f-137">Následující příklad kódu ukazuje funkci můžete přidat do `GenericList<T>` – třída (v [Úvod do obecných typů](introduction-to-generics.md)) použitím omezení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-137">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](introduction-to-generics.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="5cb3f-138">Omezení umožňuje obecná třída používat `Employee.Name` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-138">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="5cb3f-139">Omezení určuje, že všechny položky typu `T` jsou musí být buď `Employee` objekt nebo objekt, který dědí z `Employee`.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-139">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="5cb3f-140">Více omezení lze použít pro stejný parametr typu a omezení sami lze obecné typy, následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="5cb3f-140">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="5cb3f-141">Při použití `where T : class` omezení, vyhněte se `==` a `!=` operátory na parametr typu vzhledem k tomu, že tyto operátory testovat pro referenční identity jenom, ne pro rovnosti hodnoty.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-141">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="5cb3f-142">K tomuto chování dochází, i když jsou tyto operátory přetížené v typu, který se používá jako argument.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-142">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="5cb3f-143">Následující kód ukazuje tento bod; i když je hodnota false výstup <xref:System.String> třídy přetížení `==` operátor.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-143">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="5cb3f-144">Kompilátor pouze ví, že T typem je odkaz v době kompilace a musí používat výchozí operátory, které jsou platné pro všechny typy odkazů.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-144">The compiler only knows that T is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="5cb3f-145">Pokud musíte otestovat rovnosti hodnotu, doporučený způsob je se rovněž vztahují `where T : IEquatable<T>` nebo `where T : IComparable<T>` omezení a implementace rozhraní do třídy, která se použije ke konstrukci obecná třída.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-145">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="5cb3f-146">Chovaly několik parametrů</span><span class="sxs-lookup"><span data-stu-id="5cb3f-146">Constraining multiple parameters</span></span>

<span data-ttu-id="5cb3f-147">Omezení můžete použít pro několik parametrů a více omezení pro jeden parametr, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="5cb3f-147">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="5cb3f-148">Parametry typu bez vazby</span><span class="sxs-lookup"><span data-stu-id="5cb3f-148">Unbounded type parameters</span></span>

 <span data-ttu-id="5cb3f-149">Zadejte parametry, které mají žádná omezení, jako je například T v veřejnou třídu `SampleClass<T>{}`, se nazývají typu bez vazby parametrů.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-149">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="5cb3f-150">Parametry typu bez vazby mají následující pravidla:</span><span class="sxs-lookup"><span data-stu-id="5cb3f-150">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="5cb3f-151">`!=` a `==` operátory nelze použít, protože není zaručeno, že konkrétní typ argumentu bude podporovat tyto operátory.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-151">The `!=` and `==` operators cannot be used because there is no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="5cb3f-152">Mohou být převedeny do a z `System.Object` nebo explicitně převést na typ všech rozhraní.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-152">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="5cb3f-153">Můžete porovnat jejich [null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="5cb3f-153">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="5cb3f-154">Pokud se porovná bez vazby parametru `null`, porovnání vždy vrátí hodnotu false, pokud argument typ je typ hodnoty.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-154">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="5cb3f-155">Parametry typu jako omezení</span><span class="sxs-lookup"><span data-stu-id="5cb3f-155">Type parameters as constraints</span></span>

<span data-ttu-id="5cb3f-156">Použití parametr obecného typu jako omezení je užitečné, pokud členské funkce s parametrem vlastní typ má omezit tento parametr pro parametr typu obsahující typu, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="5cb3f-156">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="5cb3f-157">V předchozím příkladu `T` je omezení typu v kontextu `Add` metoda a parametru typu bez vazby v kontextu `List` třídy.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-157">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="5cb3f-158">Parametry typu mohou sloužit také jako omezení v definicích – obecná třída.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-158">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="5cb3f-159">Parametr typu musí být deklarován v závorkách úhel spolu s dalšími parametry typu:</span><span class="sxs-lookup"><span data-stu-id="5cb3f-159">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="5cb3f-160">Užitečnost parametry typu jako omezení s obecné třídy je omezená, protože kompilátor můžete předpokládat nic o parametr typu s tím rozdílem, že je odvozena z `System.Object`.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-160">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="5cb3f-161">Pomocí parametrů typu jako omezení obecných tříd ve scénářích, ve kterých chcete vynutit vztah dědičnosti mezi dvěma parametry typu.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-161">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="5cb3f-162">Nespravované omezení</span><span class="sxs-lookup"><span data-stu-id="5cb3f-162">Unmanaged constraint</span></span>

<span data-ttu-id="5cb3f-163">Počínaje 7.3 C#, můžete použít `unmanaged` omezení k určení, že musí být parametr typu **nespravované typu**.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-163">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be an **unmanaged type**.</span></span> <span data-ttu-id="5cb3f-164">**Nespravované typu** je typ, který není typu odkazu a neobsahuje pole typu odkaz na libovolnou úroveň vnoření.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-164">An **unmanaged type** is a type that is not a reference type and doesn't contain reference type fields at any level of nesting.</span></span> <span data-ttu-id="5cb3f-165">`unmanaged` Omezení umožňuje zapisovat opakovaně použitelné rutiny pro práci s typy, které smí uživatel manipulovat jako bloky paměti, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="5cb3f-165">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="5cb3f-166">Předchozí postup musí být zkompilovány v `unsafe` kontextu protože používá `sizeof` operátor pro typ není známý jako předdefinovaný typ.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-166">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="5cb3f-167">Bez `unmanaged` omezení, `sizeof` operátor je k dispozici.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-167">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="5cb3f-168">Delegát omezení</span><span class="sxs-lookup"><span data-stu-id="5cb3f-168">Delegate constraints</span></span>

<span data-ttu-id="5cb3f-169">Také počínaje 7.3 C#, můžete použít <xref:System.Delegate?displayProperty=nameWithType> nebo <xref:System.MulticastDelegate?displayProperty=nameWithType> jako základní třída omezení.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-169">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="5cb3f-170">Modul CLR vždycky povolená toto omezení, ale jazyka C# nepovolené ho.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-170">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="5cb3f-171">`System.Delegate` Omezení umožňuje psát kód, který funguje s delegáti způsobem bezpečnost typů.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-171">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="5cb3f-172">Následující kód definuje metody rozšíření, které se kombinuje dvě delegáti předpokladu, že jsou stejného typu:</span><span class="sxs-lookup"><span data-stu-id="5cb3f-172">The following code defines an extension method that combines two delegates provided they are the same type:</span></span>

[!code-csharp[using the delegate constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="5cb3f-173">Metodu výše můžete kombinování delegátů, které jsou stejného typu:</span><span class="sxs-lookup"><span data-stu-id="5cb3f-173">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="5cb3f-174">Pokud zrušte komentář u poslední řádek nebude zkompilují.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-174">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="5cb3f-175">Obě `first` a `test` jsou typy delegáta, ale jsou typy různých delegáta.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-175">Both `first` and `test` are delegate types, but they are different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="5cb3f-176">Omezení výčtu</span><span class="sxs-lookup"><span data-stu-id="5cb3f-176">Enum constraints</span></span>

<span data-ttu-id="5cb3f-177">Počínaje 7.3 C#, můžete také určit <xref:System.Enum?displayProperty=nameWithType> typ jako základní třída omezení.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-177">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="5cb3f-178">Modul CLR vždycky povolená toto omezení, ale jazyka C# nepovolené ho.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-178">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="5cb3f-179">Obecné typy pomocí `System.Enum` poskytují bezpečnost typů programování výsledky do mezipaměti pomocí statických metod v `System.Enum`.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-179">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="5cb3f-180">Následující příklad vyhledá všechny platné hodnoty pro typ výčtu a potom vytvoří slovník, který mapuje tyto hodnoty na řetězcovou reprezentaci.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-180">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="5cb3f-181">Ujistěte se, metody použité pomocí reflexe, který má ovlivnit výkon.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-181">The methods used make use of reflection, which has performance implications.</span></span> <span data-ttu-id="5cb3f-182">Můžete volat tuto metodu za účelem vytvoření kolekce, která je do mezipaměti a znovu použít místo opakování volání, které vyžadují reflexe.</span><span class="sxs-lookup"><span data-stu-id="5cb3f-182">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="5cb3f-183">Je možné jej použít jak znázorňuje následující ukázka vytvořit výčet a sestavení slovník jeho hodnoty a názvy:</span><span class="sxs-lookup"><span data-stu-id="5cb3f-183">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the unmanaged constraint](../../../../samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="5cb3f-184">Viz také</span><span class="sxs-lookup"><span data-stu-id="5cb3f-184">See also</span></span>

 <span data-ttu-id="5cb3f-185"><xref:System.Collections.Generic> [Průvodce programováním v C#](../../../csharp/programming-guide/index.md)</span><span class="sxs-lookup"><span data-stu-id="5cb3f-185"><xref:System.Collections.Generic> [C# Programming Guide](../../../csharp/programming-guide/index.md)</span></span>  
 [<span data-ttu-id="5cb3f-186">Úvod do obecných typů</span><span class="sxs-lookup"><span data-stu-id="5cb3f-186">Introduction to Generics</span></span>](../../../csharp/programming-guide/generics/introduction-to-generics.md)  
 [<span data-ttu-id="5cb3f-187">Obecné třídy</span><span class="sxs-lookup"><span data-stu-id="5cb3f-187">Generic Classes</span></span>](../../../csharp/programming-guide/generics/generic-classes.md)  
 [<span data-ttu-id="5cb3f-188">new – omezení</span><span class="sxs-lookup"><span data-stu-id="5cb3f-188">new Constraint</span></span>](../../../csharp/language-reference/keywords/new-constraint.md)  
