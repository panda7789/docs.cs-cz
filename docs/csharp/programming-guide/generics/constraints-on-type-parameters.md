---
title: Omezení parametrů typu – C# Průvodce programováním
ms.custom: seodec18
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 19bf511ee7252bc305dbb4b6f32636955eba0ab8
ms.sourcegitcommit: 30a558d23e3ac5a52071121a52c305c85fe15726
ms.translationtype: HT
ms.contentlocale: cs-CZ
ms.lasthandoff: 12/25/2019
ms.locfileid: "75337261"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="db991-102">Omezení parametrů typu (C# Průvodce programováním)</span><span class="sxs-lookup"><span data-stu-id="db991-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="db991-103">Omezení informují kompilátor o možnostech, které musí mít argument typu.</span><span class="sxs-lookup"><span data-stu-id="db991-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="db991-104">Bez omezení může být argumentem typu libovolný typ.</span><span class="sxs-lookup"><span data-stu-id="db991-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="db991-105">Kompilátor může předpokládat pouze členy <xref:System.Object?displayProperty=nameWithType>, což je maximální základní třída pro libovolný typ rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="db991-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="db991-106">Další informace najdete v tématu [Proč použít omezení](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="db991-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="db991-107">Pokud se klientský kód pokusí vytvořit instanci vaší třídy pomocí typu, který není povolen omezením, výsledkem je chyba při kompilaci.</span><span class="sxs-lookup"><span data-stu-id="db991-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="db991-108">Omezení jsou určena pomocí klíčového slova `where` kontextové.</span><span class="sxs-lookup"><span data-stu-id="db991-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="db991-109">V následující tabulce jsou uvedeny sedm typů omezení:</span><span class="sxs-lookup"><span data-stu-id="db991-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="db991-110">Jedinečn</span><span class="sxs-lookup"><span data-stu-id="db991-110">Constraint</span></span>|<span data-ttu-id="db991-111">Popis</span><span class="sxs-lookup"><span data-stu-id="db991-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="db991-112">Argument typu musí být typ hodnoty, která není null.</span><span class="sxs-lookup"><span data-stu-id="db991-112">The type argument must be a non-nullable value type.</span></span> <span data-ttu-id="db991-113">Informace o typech hodnot s možnou hodnotou null naleznete v tématu [hodnoty s možnou hodnotou null](../../language-reference/builtin-types/nullable-value-types.md).</span><span class="sxs-lookup"><span data-stu-id="db991-113">For information about nullable value types, see [Nullable value types](../../language-reference/builtin-types/nullable-value-types.md).</span></span> <span data-ttu-id="db991-114">Vzhledem k tomu, že všechny typy hodnot mají přístupný konstruktor bez parametrů, omezení `struct` implikuje omezení `new()` a nelze je kombinovat s omezením `new()`.</span><span class="sxs-lookup"><span data-stu-id="db991-114">Because all value types have an accessible parameterless constructor, the `struct` constraint implies the `new()` constraint and can't be combined with the `new()` constraint.</span></span> <span data-ttu-id="db991-115">Omezení `struct` také nelze kombinovat s omezením `unmanaged`.</span><span class="sxs-lookup"><span data-stu-id="db991-115">You also cannot combine the `struct` constraint with the `unmanaged` constraint.</span></span>|
|`where T : class`|<span data-ttu-id="db991-116">Argument typu musí být typ odkazu.</span><span class="sxs-lookup"><span data-stu-id="db991-116">The type argument must be a reference type.</span></span> <span data-ttu-id="db991-117">Toto omezení platí také pro libovolnou třídu, rozhraní, delegáta nebo typ pole.</span><span class="sxs-lookup"><span data-stu-id="db991-117">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="db991-118">Argument typu musí být typ, který nemůže mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="db991-118">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="db991-119">Argumentem může být typ odkazu, který neumožňuje hodnotu C# null, v 8,0 nebo novějším, nebo typ hodnoty, která není null.</span><span class="sxs-lookup"><span data-stu-id="db991-119">The argument can be a non-nullable reference type in C# 8.0 or later, or a not nullable value type.</span></span> <span data-ttu-id="db991-120">Toto omezení platí také pro libovolnou třídu, rozhraní, delegáta nebo typ pole.</span><span class="sxs-lookup"><span data-stu-id="db991-120">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="db991-121">Argument typu musí být [nespravovaný typ](../../language-reference/builtin-types/unmanaged-types.md), který nemůže mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="db991-121">The type argument must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="db991-122">Omezení `unmanaged` implikuje omezení `struct` a nelze je kombinovat s omezeními `struct` ani `new()`.</span><span class="sxs-lookup"><span data-stu-id="db991-122">The `unmanaged` constraint implies the `struct` constraint and can't be combined with either the `struct` or `new()` constraints.</span></span>|
|`where T : new()`|<span data-ttu-id="db991-123">Argument typu musí mít veřejný konstruktor bez parametrů.</span><span class="sxs-lookup"><span data-stu-id="db991-123">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="db991-124">Při použití společně s jinými omezeními je nutné zadat omezení `new()` jako poslední.</span><span class="sxs-lookup"><span data-stu-id="db991-124">When used together with other constraints, the `new()` constraint must be specified last.</span></span> <span data-ttu-id="db991-125">Omezení `new()` nelze kombinovat s omezeními `struct` a `unmanaged`.</span><span class="sxs-lookup"><span data-stu-id="db991-125">The `new()` constraint can't be combined with the `struct` and `unmanaged` constraints.</span></span>|
|<span data-ttu-id="db991-126">*název základní třídy `where T :`\<*</span><span class="sxs-lookup"><span data-stu-id="db991-126">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="db991-127">Argument typu musí být nebo odvozen ze zadané základní třídy.</span><span class="sxs-lookup"><span data-stu-id="db991-127">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="db991-128">*název rozhraní `where T :`\<*</span><span class="sxs-lookup"><span data-stu-id="db991-128">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="db991-129">Argument typu musí být nebo implementovat zadané rozhraní.</span><span class="sxs-lookup"><span data-stu-id="db991-129">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="db991-130">Je možné zadat více omezení rozhraní.</span><span class="sxs-lookup"><span data-stu-id="db991-130">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="db991-131">Omezení rozhraní může být také obecné.</span><span class="sxs-lookup"><span data-stu-id="db991-131">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="db991-132">Argument typu zadaný pro T musí být nebo odvozen od argumentu zadaného pro U.</span><span class="sxs-lookup"><span data-stu-id="db991-132">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

## <a name="why-use-constraints"></a><span data-ttu-id="db991-133">Proč použít omezení</span><span class="sxs-lookup"><span data-stu-id="db991-133">Why use constraints</span></span>

<span data-ttu-id="db991-134">Omezením parametru typu zvýšíte počet povolených operací a volání metod do těch, které jsou podporovány typem omezení a všemi typy v její hierarchii dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="db991-134">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="db991-135">Pokud navrhujete obecné třídy nebo metody, pokud budete provádět jakoukoli operaci u obecných členů nad rámec jednoduchého přiřazení nebo volání jakékoli metody, které <xref:System.Object?displayProperty=nameWithType>nepodporuje, budete muset použít omezení pro parametr typu.</span><span class="sxs-lookup"><span data-stu-id="db991-135">When you design generic classes or methods, if you'll be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="db991-136">Například omezení základní třídy říká kompilátoru, že jako argumenty typu budou použity pouze objekty tohoto typu nebo odvozené z tohoto typu.</span><span class="sxs-lookup"><span data-stu-id="db991-136">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="db991-137">Jakmile má kompilátor tuto záruku, může dovolit volání metod tohoto typu v obecné třídě.</span><span class="sxs-lookup"><span data-stu-id="db991-137">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="db991-138">Následující příklad kódu ukazuje funkce, které lze přidat do `GenericList<T>` třídy (v [úvodu k obecným](../../../standard/generics/index.md)) pomocí omezení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="db991-138">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](../../../standard/generics/index.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="db991-139">Omezení umožňuje, aby obecná třída používala vlastnost `Employee.Name`.</span><span class="sxs-lookup"><span data-stu-id="db991-139">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="db991-140">Omezení určuje, že všechny položky typu `T` jsou zaručeny buď objekt `Employee` nebo objekt, který dědí z `Employee`.</span><span class="sxs-lookup"><span data-stu-id="db991-140">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="db991-141">U stejného parametru typu lze použít více omezení a samotné omezení mohou být obecné typy, a to takto:</span><span class="sxs-lookup"><span data-stu-id="db991-141">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="db991-142">Při použití omezení `where T : class` Vyhněte se operátorům `==` a `!=` na parametru typu, protože tyto operátory budou testovány pouze pro referenční identitu, nikoli pro rovnost hodnot.</span><span class="sxs-lookup"><span data-stu-id="db991-142">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="db991-143">K tomuto chování dochází, i když jsou tyto operátory přetíženy v typu, který se používá jako argument.</span><span class="sxs-lookup"><span data-stu-id="db991-143">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="db991-144">Následující kód ilustruje tento bod; výstup je false, i když <xref:System.String> třída přetěžuje operátor `==`.</span><span class="sxs-lookup"><span data-stu-id="db991-144">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="db991-145">Kompilátor ví pouze, že `T` je odkazový typ v době kompilace a musí používat výchozí operátory, které jsou platné pro všechny typy odkazů.</span><span class="sxs-lookup"><span data-stu-id="db991-145">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="db991-146">Pokud je nutné testovat rovnost hodnot, doporučuje se použít také omezení `where T : IEquatable<T>` nebo `where T : IComparable<T>` a implementovat rozhraní v jakékoli třídě, která bude použita k vytvoření obecné třídy.</span><span class="sxs-lookup"><span data-stu-id="db991-146">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="db991-147">Omezení více parametrů</span><span class="sxs-lookup"><span data-stu-id="db991-147">Constraining multiple parameters</span></span>

<span data-ttu-id="db991-148">Můžete použít omezení na více parametrů a více omezení na jeden parametr, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="db991-148">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="db991-149">Neohraničené parametry typu</span><span class="sxs-lookup"><span data-stu-id="db991-149">Unbounded type parameters</span></span>

 <span data-ttu-id="db991-150">Parametry typu bez omezení, jako například T ve veřejné třídě `SampleClass<T>{}`, se nazývají neohraničené parametry typu.</span><span class="sxs-lookup"><span data-stu-id="db991-150">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="db991-151">Neohraničené parametry typu mají následující pravidla:</span><span class="sxs-lookup"><span data-stu-id="db991-151">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="db991-152">Operátory `!=` a `==` nelze použít, protože není nijak zaručeno, že konkrétní argument typu bude podporovat tyto operátory.</span><span class="sxs-lookup"><span data-stu-id="db991-152">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="db991-153">Lze je převést na nebo z `System.Object` nebo explicitně převést na libovolný typ rozhraní.</span><span class="sxs-lookup"><span data-stu-id="db991-153">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="db991-154">Můžete je porovnat s [hodnotou null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="db991-154">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="db991-155">Pokud je nevázaný parametr porovnán s `null`, porovnávání vždy vrátí hodnotu false, pokud je argumentem typu hodnotový typ.</span><span class="sxs-lookup"><span data-stu-id="db991-155">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="db991-156">Parametry typu jako omezení</span><span class="sxs-lookup"><span data-stu-id="db991-156">Type parameters as constraints</span></span>

<span data-ttu-id="db991-157">Použití parametru obecného typu jako omezení je užitečné, pokud členská funkce s vlastním parametrem typu musí omezit tento parametr na parametr typu nadřazeného typu, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="db991-157">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="db991-158">V předchozím příkladu `T` je omezení typu v kontextu metody `Add` a neohraničený parametr typu v kontextu třídy `List`.</span><span class="sxs-lookup"><span data-stu-id="db991-158">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="db991-159">Parametry typu lze také použít jako omezení v definicích obecných tříd.</span><span class="sxs-lookup"><span data-stu-id="db991-159">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="db991-160">Parametr typu musí být deklarovaný v lomených závorkách spolu s jinými parametry typu:</span><span class="sxs-lookup"><span data-stu-id="db991-160">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="db991-161">Užitečnost parametrů typu jako omezení s obecnými třídami je omezená, protože kompilátor nemůže předpokládat nic o parametru typu s tím rozdílem, že je odvozen od `System.Object`.</span><span class="sxs-lookup"><span data-stu-id="db991-161">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="db991-162">Použijte parametry typu jako omezení pro obecné třídy ve scénářích, ve kterých chcete vynutit vztah dědičnosti mezi dvěma parametry typu.</span><span class="sxs-lookup"><span data-stu-id="db991-162">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="db991-163">Omezení NotNull</span><span class="sxs-lookup"><span data-stu-id="db991-163">NotNull constraint</span></span>

<span data-ttu-id="db991-164">Počínaje C# 8,0 můžete použít omezení `notnull` k určení, že argument typu musí být typ hodnoty, která není null, nebo odkaz na typ, který nepovoluje hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="db991-164">Beginning with C# 8.0, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="db991-165">Omezení `notnull` lze použít pouze v kontextu `nullable enable`.</span><span class="sxs-lookup"><span data-stu-id="db991-165">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="db991-166">Kompilátor vygeneruje upozornění, pokud přidáte omezení `notnull` v oblivious kontextu s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="db991-166">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span> 

<span data-ttu-id="db991-167">Na rozdíl od jiných omezení, pokud argument typu narušuje omezení `notnull`, kompilátor vygeneruje upozornění, když je tento kód zkompilován v kontextu `nullable enable`.</span><span class="sxs-lookup"><span data-stu-id="db991-167">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="db991-168">Pokud je kód zkompilován v oblivious kontextu s možnou hodnotou null, kompilátor negeneruje žádná upozornění ani chyby.</span><span class="sxs-lookup"><span data-stu-id="db991-168">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="db991-169">Nespravované omezení</span><span class="sxs-lookup"><span data-stu-id="db991-169">Unmanaged constraint</span></span>

<span data-ttu-id="db991-170">Počínaje C# 7,3 můžete pomocí omezení `unmanaged` určit, že parametr typu musí být [nespravovaný typ](../../language-reference/builtin-types/unmanaged-types.md), který není null.</span><span class="sxs-lookup"><span data-stu-id="db991-170">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="db991-171">Omezení `unmanaged` umožňuje psát opakovaně použitelné rutiny pro práci s typy, které mohou být manipulovány jako bloky paměti, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="db991-171">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="db991-172">Předchozí metoda musí být zkompilována v kontextu `unsafe`, protože používá operátor `sizeof` u typu, který není známý jako vestavěný typ.</span><span class="sxs-lookup"><span data-stu-id="db991-172">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="db991-173">Bez omezení `unmanaged` není operátor `sizeof` k dispozici.</span><span class="sxs-lookup"><span data-stu-id="db991-173">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

<span data-ttu-id="db991-174">Omezení `unmanaged` implikuje omezení `struct` a nedá se s ním kombinovat.</span><span class="sxs-lookup"><span data-stu-id="db991-174">The `unmanaged` constraint implies the `struct` constraint and can't be combined with it.</span></span> <span data-ttu-id="db991-175">Vzhledem k tomu, že omezení `struct` implikuje omezení `new()`, nelze omezení `unmanaged` kombinovat i s omezením `new()`.</span><span class="sxs-lookup"><span data-stu-id="db991-175">Because the `struct` constraint implies the `new()` constraint, the `unmanaged` constraint can't be combined with the `new()` constraint as well.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="db991-176">Omezení delegování</span><span class="sxs-lookup"><span data-stu-id="db991-176">Delegate constraints</span></span>

<span data-ttu-id="db991-177">Počínaje C# 7,3 můžete také použít <xref:System.Delegate?displayProperty=nameWithType> nebo <xref:System.MulticastDelegate?displayProperty=nameWithType> jako omezení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="db991-177">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="db991-178">CLR vždy povoluje toto omezení, ale C# jazyk ho nepovolil.</span><span class="sxs-lookup"><span data-stu-id="db991-178">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="db991-179">Omezení `System.Delegate` umožňuje napsat kód, který spolupracuje s delegáty způsobem, který je typově bezpečný.</span><span class="sxs-lookup"><span data-stu-id="db991-179">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="db991-180">Následující kód definuje metodu rozšíření, která kombinuje dva delegáty, pokud jsou stejného typu:</span><span class="sxs-lookup"><span data-stu-id="db991-180">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="db991-181">K kombinování delegátů, kteří mají stejný typ, můžete použít výše uvedenou metodu:</span><span class="sxs-lookup"><span data-stu-id="db991-181">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="db991-182">Pokud Odkomentujete poslední řádek, nebude zkompilován.</span><span class="sxs-lookup"><span data-stu-id="db991-182">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="db991-183">`first` i `test` jsou typy delegátů, ale jedná se o různé typy delegátů.</span><span class="sxs-lookup"><span data-stu-id="db991-183">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="db991-184">Omezení výčtu</span><span class="sxs-lookup"><span data-stu-id="db991-184">Enum constraints</span></span>

<span data-ttu-id="db991-185">Počínaje C# 7,3 můžete také zadat typ <xref:System.Enum?displayProperty=nameWithType> jako omezení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="db991-185">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="db991-186">CLR vždy povoluje toto omezení, ale C# jazyk ho nepovolil.</span><span class="sxs-lookup"><span data-stu-id="db991-186">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="db991-187">Obecné typy pomocí `System.Enum` poskytují programové programování pro ukládání výsledků do mezipaměti pomocí statických metod v `System.Enum`.</span><span class="sxs-lookup"><span data-stu-id="db991-187">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="db991-188">Následující ukázka vyhledá všechny platné hodnoty pro typ výčtu a potom vytvoří slovník, který tyto hodnoty mapuje na jeho řetězcovou reprezentaci.</span><span class="sxs-lookup"><span data-stu-id="db991-188">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the enum constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="db991-189">Metody použité k použití reflexe, který má dopad na výkon.</span><span class="sxs-lookup"><span data-stu-id="db991-189">The methods used to make use of reflection, which has performance implications.</span></span> <span data-ttu-id="db991-190">Tuto metodu lze zavolat pro sestavení kolekce, která je uložena v mezipaměti a znovu použita, nikoli volání, která vyžadují reflexi.</span><span class="sxs-lookup"><span data-stu-id="db991-190">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="db991-191">Můžete ji použít, jak je znázorněno v následujícím příkladu, pro vytvoření výčtu a sestavení slovníku jeho hodnot a názvů:</span><span class="sxs-lookup"><span data-stu-id="db991-191">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[enum definition](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the enum constrained method](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="db991-192">Viz také:</span><span class="sxs-lookup"><span data-stu-id="db991-192">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="db991-193">Průvodce programováním v jazyce C#</span><span class="sxs-lookup"><span data-stu-id="db991-193">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="db991-194">Úvod do obecných typů</span><span class="sxs-lookup"><span data-stu-id="db991-194">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="db991-195">Obecné třídy</span><span class="sxs-lookup"><span data-stu-id="db991-195">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="db991-196">new – omezení</span><span class="sxs-lookup"><span data-stu-id="db991-196">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
