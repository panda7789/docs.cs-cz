---
title: Omezení parametrů typu - Průvodce programováním jazyka C#
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 76cd00b9c84f128d2a181115293df910d8deb6cb
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/14/2020
ms.locfileid: "79399796"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="06c48-102">Omezení parametrů typu (Průvodce programováním jazyka C#)</span><span class="sxs-lookup"><span data-stu-id="06c48-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="06c48-103">Omezení informují kompilátor o schopnostech, které musí mít argument typu.</span><span class="sxs-lookup"><span data-stu-id="06c48-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="06c48-104">Bez omezení může být argument typu libovolný typ.</span><span class="sxs-lookup"><span data-stu-id="06c48-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="06c48-105">Kompilátor může převzít <xref:System.Object?displayProperty=nameWithType>pouze členy , což je konečná základní třída pro libovolný typ .NET.</span><span class="sxs-lookup"><span data-stu-id="06c48-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="06c48-106">Další informace naleznete v tématu [Why use constraints](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="06c48-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="06c48-107">Pokud se klientský kód pokusí vytvořit instanci vaší třídy pomocí typu, který není povolen omezením, výsledkem je chyba v době kompilace.</span><span class="sxs-lookup"><span data-stu-id="06c48-107">If client code tries to instantiate your class by using a type that is not allowed by a constraint, the result is a compile-time error.</span></span> <span data-ttu-id="06c48-108">Omezení jsou určena pomocí `where` kontextového klíčového slova.</span><span class="sxs-lookup"><span data-stu-id="06c48-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="06c48-109">V následující tabulce je uvedeno sedm typů omezení:</span><span class="sxs-lookup"><span data-stu-id="06c48-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="06c48-110">Omezení</span><span class="sxs-lookup"><span data-stu-id="06c48-110">Constraint</span></span>|<span data-ttu-id="06c48-111">Popis</span><span class="sxs-lookup"><span data-stu-id="06c48-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="06c48-112">Argument typu musí být typ hodnoty s hodnotou, kterou nelze utnout.</span><span class="sxs-lookup"><span data-stu-id="06c48-112">The type argument must be a non-nullable value type.</span></span> <span data-ttu-id="06c48-113">Informace o typech hodnot s možnou hodnotou s možnou hodnotou null naleznete v tématu [Nullable typy hodnot](../../language-reference/builtin-types/nullable-value-types.md).</span><span class="sxs-lookup"><span data-stu-id="06c48-113">For information about nullable value types, see [Nullable value types](../../language-reference/builtin-types/nullable-value-types.md).</span></span> <span data-ttu-id="06c48-114">Vzhledem k tomu, že všechny typy `struct` hodnot `new()` mají přístupný konstruktor bez parametrů, omezení znamená omezení a nelze je kombinovat s omezením. `new()`</span><span class="sxs-lookup"><span data-stu-id="06c48-114">Because all value types have an accessible parameterless constructor, the `struct` constraint implies the `new()` constraint and can't be combined with the `new()` constraint.</span></span> <span data-ttu-id="06c48-115">Omezení také nelze `struct` kombinovat `unmanaged` s omezením.</span><span class="sxs-lookup"><span data-stu-id="06c48-115">You also cannot combine the `struct` constraint with the `unmanaged` constraint.</span></span>|
|`where T : class`|<span data-ttu-id="06c48-116">Argument typu musí být typ odkazu.</span><span class="sxs-lookup"><span data-stu-id="06c48-116">The type argument must be a reference type.</span></span> <span data-ttu-id="06c48-117">Toto omezení platí také pro všechny třídy, rozhraní, delegáta nebo typu pole.</span><span class="sxs-lookup"><span data-stu-id="06c48-117">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="06c48-118">Argument typu musí být typ, který nelze utnout.</span><span class="sxs-lookup"><span data-stu-id="06c48-118">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="06c48-119">Argument může být typ odkazu s možnou hodnotou neshodnou v c# 8.0 nebo novějším nebo typ hodnoty, který nelze s nulovou hodnotou.</span><span class="sxs-lookup"><span data-stu-id="06c48-119">The argument can be a non-nullable reference type in C# 8.0 or later, or a not nullable value type.</span></span> <span data-ttu-id="06c48-120">Toto omezení platí také pro všechny třídy, rozhraní, delegáta nebo typu pole.</span><span class="sxs-lookup"><span data-stu-id="06c48-120">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : unmanaged`|<span data-ttu-id="06c48-121">Argument typu musí být [nespravovaný typ,](../../language-reference/builtin-types/unmanaged-types.md)který nelze utnout.</span><span class="sxs-lookup"><span data-stu-id="06c48-121">The type argument must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="06c48-122">Omezení `unmanaged` znamená `struct` omezení a nelze kombinovat s `struct` omezeními `new()` nebo.</span><span class="sxs-lookup"><span data-stu-id="06c48-122">The `unmanaged` constraint implies the `struct` constraint and can't be combined with either the `struct` or `new()` constraints.</span></span>|
|`where T : new()`|<span data-ttu-id="06c48-123">Argument typu musí mít veřejný konstruktor bez parametrů.</span><span class="sxs-lookup"><span data-stu-id="06c48-123">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="06c48-124">Při použití společně s jinými `new()` omezeními musí být omezení zadáno jako poslední.</span><span class="sxs-lookup"><span data-stu-id="06c48-124">When used together with other constraints, the `new()` constraint must be specified last.</span></span> <span data-ttu-id="06c48-125">Omezení `new()` nelze kombinovat s `struct` a `unmanaged` omezení.</span><span class="sxs-lookup"><span data-stu-id="06c48-125">The `new()` constraint can't be combined with the `struct` and `unmanaged` constraints.</span></span>|
|<span data-ttu-id="06c48-126">`where T :`*název základní třídy>\<*</span><span class="sxs-lookup"><span data-stu-id="06c48-126">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="06c48-127">Argument typu musí být nebo odvozen ze zadané základní třídy.</span><span class="sxs-lookup"><span data-stu-id="06c48-127">The type argument must be or derive from the specified base class.</span></span>|
|<span data-ttu-id="06c48-128">`where T :`>názvu rozhraní \* \<\*</span><span class="sxs-lookup"><span data-stu-id="06c48-128">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="06c48-129">Argument typu musí být nebo implementovat zadané rozhraní.</span><span class="sxs-lookup"><span data-stu-id="06c48-129">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="06c48-130">Lze zadat více omezení rozhraní.</span><span class="sxs-lookup"><span data-stu-id="06c48-130">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="06c48-131">Omezující rozhraní může být také obecné.</span><span class="sxs-lookup"><span data-stu-id="06c48-131">The constraining interface can also be generic.</span></span>|
|`where T : U`|<span data-ttu-id="06c48-132">Argument typu zadaný pro T musí být nebo odvozen z argumentu poskytnutého pro U.</span><span class="sxs-lookup"><span data-stu-id="06c48-132">The type argument supplied for T must be or derive from the argument supplied for U.</span></span>|

## <a name="why-use-constraints"></a><span data-ttu-id="06c48-133">Proč používat omezení</span><span class="sxs-lookup"><span data-stu-id="06c48-133">Why use constraints</span></span>

<span data-ttu-id="06c48-134">Omezením parametru typu zvýšíte počet povolených operací a volání metod na ty, které jsou podporovány omezujícím typem a všemi typy v hierarchii dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="06c48-134">By constraining the type parameter, you increase the number of allowable operations and method calls to those supported by the constraining type and all types in its inheritance hierarchy.</span></span> <span data-ttu-id="06c48-135">Při návrhu obecných tříd nebo metod, pokud budete provádět jakoukoli operaci na obecné členy <xref:System.Object?displayProperty=nameWithType>mimo jednoduché přiřazení nebo volání všechny metody, které nejsou podporovány , budete muset použít omezení parametru typu.</span><span class="sxs-lookup"><span data-stu-id="06c48-135">When you design generic classes or methods, if you'll be performing any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="06c48-136">Například omezení základní třídy říká kompilátoru, že jako argumenty typu budou použity pouze objekty tohoto typu nebo odvozené z tohoto typu.</span><span class="sxs-lookup"><span data-stu-id="06c48-136">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="06c48-137">Jakmile kompilátor má tuto záruku, může povolit metody tohoto typu, které mají být volány v obecné třídě.</span><span class="sxs-lookup"><span data-stu-id="06c48-137">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="06c48-138">Následující příklad kódu ukazuje funkce, které můžete `GenericList<T>` přidat do třídy (v [úvodu k obecným typům](../../../standard/generics/index.md)) použitím omezení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="06c48-138">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](../../../standard/generics/index.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="06c48-139">Omezení umožňuje obecné třídy `Employee.Name` používat vlastnost.</span><span class="sxs-lookup"><span data-stu-id="06c48-139">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="06c48-140">Omezení určuje, že všechny `T` položky typu jsou `Employee` zaručeně objekt nebo `Employee`objekt, který dědí z .</span><span class="sxs-lookup"><span data-stu-id="06c48-140">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="06c48-141">Více omezení lze použít na stejný parametr typu a omezení sami mohou být obecné typy, takto:</span><span class="sxs-lookup"><span data-stu-id="06c48-141">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="06c48-142">Při použití `where T : class` omezení, `==` vyhnout `!=` a operátory na parametr typu, protože tyto operátory budou testovat pouze referenční identitu, nikoli pro rovnost hodnot.</span><span class="sxs-lookup"><span data-stu-id="06c48-142">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="06c48-143">K tomuto chování dochází i v případě, že tyto operátory jsou přetíženy v typu, který se používá jako argument.</span><span class="sxs-lookup"><span data-stu-id="06c48-143">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="06c48-144">Následující kód ilustruje tento bod; výstup je false i <xref:System.String> v případě, že třída přetížení `==` operátor.</span><span class="sxs-lookup"><span data-stu-id="06c48-144">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="06c48-145">Kompilátor pouze `T` ví, že je typ odkazu v době kompilace a musí používat výchozí operátory, které jsou platné pro všechny typy odkazů.</span><span class="sxs-lookup"><span data-stu-id="06c48-145">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="06c48-146">Pokud je nutné otestovat rovnost hodnot, doporučeným `where T : IEquatable<T>` způsobem `where T : IComparable<T>` je také použít nebo omezení a implementovat rozhraní v libovolné třídě, která bude použita k vytvoření obecné třídy.</span><span class="sxs-lookup"><span data-stu-id="06c48-146">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="06c48-147">Omezení více parametrů</span><span class="sxs-lookup"><span data-stu-id="06c48-147">Constraining multiple parameters</span></span>

<span data-ttu-id="06c48-148">Omezení můžete použít na více parametrů a více omezení na jeden parametr, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="06c48-148">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="06c48-149">Parametry neohraničeného typu</span><span class="sxs-lookup"><span data-stu-id="06c48-149">Unbounded type parameters</span></span>

 <span data-ttu-id="06c48-150">Parametry typu, které nemají žádná omezení, `SampleClass<T>{}`například T ve veřejné třídě , se nazývají parametry neohraničeného typu.</span><span class="sxs-lookup"><span data-stu-id="06c48-150">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="06c48-151">Parametry neohraničeného typu mají následující pravidla:</span><span class="sxs-lookup"><span data-stu-id="06c48-151">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="06c48-152">A `!=` `==` operátory nelze použít, protože neexistuje žádná záruka, že konkrétní argument typu bude podporovat tyto operátory.</span><span class="sxs-lookup"><span data-stu-id="06c48-152">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="06c48-153">Mohou být převedeny do `System.Object` a z nebo explicitně převedeny na libovolný typ rozhraní.</span><span class="sxs-lookup"><span data-stu-id="06c48-153">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="06c48-154">Můžete je porovnat s [hodnotou null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="06c48-154">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="06c48-155">Pokud je porovnán s neohraničeným parametrem `null`, porovnání vždy vrátí false, pokud je argumentem typu typ typ.</span><span class="sxs-lookup"><span data-stu-id="06c48-155">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="06c48-156">Parametry typu jako omezení</span><span class="sxs-lookup"><span data-stu-id="06c48-156">Type parameters as constraints</span></span>

<span data-ttu-id="06c48-157">Použití parametru obecného typu jako omezení je užitečné, pokud členská funkce s vlastním parametrem typu musí tento parametr omezit na parametr typu obsahujícího typu, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="06c48-157">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="06c48-158">V předchozím příkladu `T` je omezení typu v `Add` kontextu metody a neomezený parametr typu v `List` kontextu třídy.</span><span class="sxs-lookup"><span data-stu-id="06c48-158">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="06c48-159">Parametry typu lze také použít jako omezení v definicích obecných tříd.</span><span class="sxs-lookup"><span data-stu-id="06c48-159">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="06c48-160">Parametr typu musí být deklarován v rámci úhlových závorek spolu s dalšími parametry typu:</span><span class="sxs-lookup"><span data-stu-id="06c48-160">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="06c48-161">Užitečnost parametrů typu jako omezení s obecnými třídami je omezena, protože kompilátor nemůže `System.Object`předpokládat nic o parametru typu s tím rozdílem, že je odvozen z .</span><span class="sxs-lookup"><span data-stu-id="06c48-161">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="06c48-162">Parametry typu použijte jako omezení obecných tříd ve scénářích, ve kterých chcete vynutit vztah dědičnosti mezi dvěma parametry typu.</span><span class="sxs-lookup"><span data-stu-id="06c48-162">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="06c48-163">Omezení notnull</span><span class="sxs-lookup"><span data-stu-id="06c48-163">NotNull constraint</span></span>

<span data-ttu-id="06c48-164">Počínaje C# 8.0, můžete `notnull` použít omezení určit, že argument typu musí být typ nenulové hodnoty typu nebo non-nullable typ odkazu.</span><span class="sxs-lookup"><span data-stu-id="06c48-164">Beginning with C# 8.0, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="06c48-165">Omezení `notnull` lze použít pouze `nullable enable` v kontextu.</span><span class="sxs-lookup"><span data-stu-id="06c48-165">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="06c48-166">Kompilátor vygeneruje upozornění, `notnull` pokud přidáte omezení v kontextu, který je nenutelný, s hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="06c48-166">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span>

<span data-ttu-id="06c48-167">Na rozdíl od jiných omezení, když `notnull` argument typu porušuje omezení, kompilátor generuje upozornění, když je tento kód kompilován v `nullable enable` kontextu.</span><span class="sxs-lookup"><span data-stu-id="06c48-167">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="06c48-168">Pokud je kód zkompilován v kontextu, který nelze upustit od nuly, kompilátor negeneruje žádná upozornění nebo chyby.</span><span class="sxs-lookup"><span data-stu-id="06c48-168">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="06c48-169">Nespravované omezení</span><span class="sxs-lookup"><span data-stu-id="06c48-169">Unmanaged constraint</span></span>

<span data-ttu-id="06c48-170">Počínaje C# 7.3, můžete `unmanaged` použít omezení určit, že parametr typu musí být [nenulelný nespravovaný typ](../../language-reference/builtin-types/unmanaged-types.md).</span><span class="sxs-lookup"><span data-stu-id="06c48-170">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="06c48-171">Omezení `unmanaged` umožňuje psát opakovaně použitelné rutiny pro práci s typy, které lze manipulovat jako bloky paměti, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="06c48-171">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="06c48-172">Předchozí metoda musí být zkompilován v `unsafe` `sizeof` kontextu, protože používá operátor na typ není známo, že je vestavěný typ.</span><span class="sxs-lookup"><span data-stu-id="06c48-172">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="06c48-173">Bez `unmanaged` omezení `sizeof` operátor není k dispozici.</span><span class="sxs-lookup"><span data-stu-id="06c48-173">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

<span data-ttu-id="06c48-174">Omezení `unmanaged` znamená `struct` omezení a nelze s ním kombinovat.</span><span class="sxs-lookup"><span data-stu-id="06c48-174">The `unmanaged` constraint implies the `struct` constraint and can't be combined with it.</span></span> <span data-ttu-id="06c48-175">Vzhledem `struct` k `new()` tomu, `unmanaged` že omezení znamená omezení, `new()` omezení nelze kombinovat s omezením také.</span><span class="sxs-lookup"><span data-stu-id="06c48-175">Because the `struct` constraint implies the `new()` constraint, the `unmanaged` constraint can't be combined with the `new()` constraint as well.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="06c48-176">Omezení delegáta</span><span class="sxs-lookup"><span data-stu-id="06c48-176">Delegate constraints</span></span>

<span data-ttu-id="06c48-177">Také počínaje C# 7.3, <xref:System.Delegate?displayProperty=nameWithType> můžete <xref:System.MulticastDelegate?displayProperty=nameWithType> použít nebo jako omezení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="06c48-177">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="06c48-178">CLR vždy povoleno toto omezení, ale jazyk Jazyka C# zakázáno.</span><span class="sxs-lookup"><span data-stu-id="06c48-178">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="06c48-179">Omezení `System.Delegate` umožňuje psát kód, který pracuje s delegáty způsobem bezpečné ho typu.</span><span class="sxs-lookup"><span data-stu-id="06c48-179">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="06c48-180">Následující kód definuje metodu rozšíření, která kombinuje dva delegáty za předpokladu, že jsou stejného typu:</span><span class="sxs-lookup"><span data-stu-id="06c48-180">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="06c48-181">Výše uvedenou metodu můžete použít ke sloučení delegátů, které jsou stejného typu:</span><span class="sxs-lookup"><span data-stu-id="06c48-181">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="06c48-182">Pokud odkomentujete poslední řádek, nebude kompilovat.</span><span class="sxs-lookup"><span data-stu-id="06c48-182">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="06c48-183">Oba `first` `test` a jsou typy delegátů, ale jsou různé typy delegátů.</span><span class="sxs-lookup"><span data-stu-id="06c48-183">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="06c48-184">Omezení výčtu</span><span class="sxs-lookup"><span data-stu-id="06c48-184">Enum constraints</span></span>

<span data-ttu-id="06c48-185">Počínaje C# 7.3, můžete také <xref:System.Enum?displayProperty=nameWithType> zadat typ jako omezení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="06c48-185">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="06c48-186">CLR vždy povoleno toto omezení, ale jazyk Jazyka C# zakázáno.</span><span class="sxs-lookup"><span data-stu-id="06c48-186">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="06c48-187">Obecné typy `System.Enum` používající poskytují programování bezpečné pro typ `System.Enum`pro ukládání výsledků do mezipaměti pomocí statických metod v .</span><span class="sxs-lookup"><span data-stu-id="06c48-187">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="06c48-188">Následující ukázka vyhledá všechny platné hodnoty pro typ výčtu a potom vytvoří slovník, který tyto hodnoty mapuje na jeho řetězcovou reprezentaci.</span><span class="sxs-lookup"><span data-stu-id="06c48-188">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the enum constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="06c48-189">Metody používané k použití reflexe, která má vliv na výkon.</span><span class="sxs-lookup"><span data-stu-id="06c48-189">The methods used to make use of reflection, which has performance implications.</span></span> <span data-ttu-id="06c48-190">Tuto metodu můžete volat k vytvoření kolekce, která je uložena do mezipaměti a znovu použita, nikoli opakování volání, která vyžadují reflexi.</span><span class="sxs-lookup"><span data-stu-id="06c48-190">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="06c48-191">Můžete jej použít, jak je znázorněno v následující ukázce k vytvoření výčtu a vytvoření slovníku jeho hodnot a názvů:</span><span class="sxs-lookup"><span data-stu-id="06c48-191">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[enum definition](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the enum constrained method](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="06c48-192">Viz také</span><span class="sxs-lookup"><span data-stu-id="06c48-192">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="06c48-193">Programovací příručka jazyka C#</span><span class="sxs-lookup"><span data-stu-id="06c48-193">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="06c48-194">Úvod do obecných typů</span><span class="sxs-lookup"><span data-stu-id="06c48-194">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="06c48-195">Obecné třídy</span><span class="sxs-lookup"><span data-stu-id="06c48-195">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="06c48-196">nové omezení</span><span class="sxs-lookup"><span data-stu-id="06c48-196">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
