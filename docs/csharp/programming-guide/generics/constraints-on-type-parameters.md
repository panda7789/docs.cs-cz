---
title: Omezení parametrů typu – Průvodce programováním v C#
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 4c4554c808ab15776f3217c257e0a60119ea2338
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/04/2020
ms.locfileid: "84368358"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="8a60f-102">Omezení parametrů typu (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="8a60f-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="8a60f-103">Omezení informují kompilátor o možnostech, které musí mít argument typu.</span><span class="sxs-lookup"><span data-stu-id="8a60f-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="8a60f-104">Bez omezení může být argumentem typu libovolný typ.</span><span class="sxs-lookup"><span data-stu-id="8a60f-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="8a60f-105">Kompilátor může předpokládat pouze členy <xref:System.Object?displayProperty=nameWithType> , což je maximální základní třída pro libovolný typ rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="8a60f-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="8a60f-106">Další informace najdete v tématu [Proč použít omezení](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="8a60f-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="8a60f-107">Pokud klientský kód používá typ, který nesplňuje omezení, kompilátor vyvolá chybu.</span><span class="sxs-lookup"><span data-stu-id="8a60f-107">If client code uses a type that doesn't satisfy a constraint, the compiler issues an error.</span></span> <span data-ttu-id="8a60f-108">Omezení jsou určena pomocí `where` klíčového slova kontextové.</span><span class="sxs-lookup"><span data-stu-id="8a60f-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="8a60f-109">V následující tabulce jsou uvedeny sedm typů omezení:</span><span class="sxs-lookup"><span data-stu-id="8a60f-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="8a60f-110">Jedinečn</span><span class="sxs-lookup"><span data-stu-id="8a60f-110">Constraint</span></span>|<span data-ttu-id="8a60f-111">Description</span><span class="sxs-lookup"><span data-stu-id="8a60f-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="8a60f-112">Argument typu musí být typ hodnoty, která není null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-112">The type argument must be a non-nullable value type.</span></span> <span data-ttu-id="8a60f-113">Informace o typech hodnot s možnou hodnotou null naleznete v tématu [hodnoty s možnou hodnotou null](../../language-reference/builtin-types/nullable-value-types.md).</span><span class="sxs-lookup"><span data-stu-id="8a60f-113">For information about nullable value types, see [Nullable value types](../../language-reference/builtin-types/nullable-value-types.md).</span></span> <span data-ttu-id="8a60f-114">Vzhledem k tomu, že všechny typy hodnot mají přístupný konstruktor bez parametrů, `struct` omezení implikuje `new()` omezení a nelze je kombinovat s `new()` omezením.</span><span class="sxs-lookup"><span data-stu-id="8a60f-114">Because all value types have an accessible parameterless constructor, the `struct` constraint implies the `new()` constraint and can't be combined with the `new()` constraint.</span></span> <span data-ttu-id="8a60f-115">Omezení nelze kombinovat `struct` s `unmanaged` omezením.</span><span class="sxs-lookup"><span data-stu-id="8a60f-115">You can't combine the `struct` constraint with the `unmanaged` constraint.</span></span>|
|`where T : class`|<span data-ttu-id="8a60f-116">Argument typu musí být typ odkazu.</span><span class="sxs-lookup"><span data-stu-id="8a60f-116">The type argument must be a reference type.</span></span> <span data-ttu-id="8a60f-117">Toto omezení platí také pro libovolnou třídu, rozhraní, delegáta nebo typ pole.</span><span class="sxs-lookup"><span data-stu-id="8a60f-117">This constraint applies also to any class, interface, delegate, or array type.</span></span> <span data-ttu-id="8a60f-118">V kontextu s možnou hodnotou null v jazyce C# 8,0 nebo vyšší `T` musí být odkazový typ, který není null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-118">In a nullable context in C# 8.0 or later, `T` must be a non-nullable reference type.</span></span> |
|`where T : class?`|<span data-ttu-id="8a60f-119">Argument typu musí být typ odkazu, který může mít hodnotu null nebo nemůže mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-119">The type argument must be a reference type, either nullable or non-nullable.</span></span> <span data-ttu-id="8a60f-120">Toto omezení platí také pro libovolnou třídu, rozhraní, delegáta nebo typ pole.</span><span class="sxs-lookup"><span data-stu-id="8a60f-120">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="8a60f-121">Argument typu musí být typ, který nemůže mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-121">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="8a60f-122">Argument může být odkazový typ, který neumožňuje hodnotu null v C# 8,0 nebo novější, nebo typ hodnoty, která není null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-122">The argument can be a non-nullable reference type in C# 8.0 or later, or a non-nullable value type.</span></span> |
|`where T : unmanaged`|<span data-ttu-id="8a60f-123">Argument typu musí být [nespravovaný typ](../../language-reference/builtin-types/unmanaged-types.md), který nemůže mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-123">The type argument must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="8a60f-124">`unmanaged`Omezení implikuje `struct` omezení a nelze ho kombinovat s `struct` `new()` omezeními or.</span><span class="sxs-lookup"><span data-stu-id="8a60f-124">The `unmanaged` constraint implies the `struct` constraint and can't be combined with either the `struct` or `new()` constraints.</span></span>|
|`where T : new()`|<span data-ttu-id="8a60f-125">Argument typu musí mít veřejný konstruktor bez parametrů.</span><span class="sxs-lookup"><span data-stu-id="8a60f-125">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="8a60f-126">Při použití společně s jinými omezeními je `new()` třeba omezení zadat jako poslední.</span><span class="sxs-lookup"><span data-stu-id="8a60f-126">When used together with other constraints, the `new()` constraint must be specified last.</span></span> <span data-ttu-id="8a60f-127">`new()`Omezení nelze kombinovat s `struct` `unmanaged` omezeními a.</span><span class="sxs-lookup"><span data-stu-id="8a60f-127">The `new()` constraint can't be combined with the `struct` and `unmanaged` constraints.</span></span>|
|<span data-ttu-id="8a60f-128">`where T :` *\<base class name>*</span><span class="sxs-lookup"><span data-stu-id="8a60f-128">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="8a60f-129">Argument typu musí být nebo odvozen ze zadané základní třídy.</span><span class="sxs-lookup"><span data-stu-id="8a60f-129">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="8a60f-130">V kontextu s možnou hodnotou null v C# 8,0 a novějším `T` musí být odkazový typ, který je odvozen od zadané základní třídy, nepovoluje hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-130">In a nullable context in C# 8.0 and later, `T` must be a non-nullable reference type derived from the specified base class.</span></span> |
|<span data-ttu-id="8a60f-131">`where T :` *\<base class name>?*</span><span class="sxs-lookup"><span data-stu-id="8a60f-131">`where T :` *\<base class name>?*</span></span>|<span data-ttu-id="8a60f-132">Argument typu musí být nebo odvozen ze zadané základní třídy.</span><span class="sxs-lookup"><span data-stu-id="8a60f-132">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="8a60f-133">V kontextu s možnou hodnotou null v C# 8,0 a novější `T` může být buď typ s možnou hodnotou null, nebo typ, který nemůže mít hodnotu null odvozený ze zadané základní třídy.</span><span class="sxs-lookup"><span data-stu-id="8a60f-133">In a nullable context in C# 8.0 and later, `T` may be either a nullable or non-nullable type derived from the specified base class.</span></span> |
|<span data-ttu-id="8a60f-134">`where T :` *\<interface name>*</span><span class="sxs-lookup"><span data-stu-id="8a60f-134">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="8a60f-135">Argument typu musí být nebo implementovat zadané rozhraní.</span><span class="sxs-lookup"><span data-stu-id="8a60f-135">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="8a60f-136">Je možné zadat více omezení rozhraní.</span><span class="sxs-lookup"><span data-stu-id="8a60f-136">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="8a60f-137">Omezení rozhraní může být také obecné.</span><span class="sxs-lookup"><span data-stu-id="8a60f-137">The constraining interface can also be generic.</span></span> <span data-ttu-id="8a60f-138">V kontextu s možnou hodnotou null v jazyce C# 8,0 a novějším `T` musí být typ bez hodnoty null, který implementuje zadané rozhraní.</span><span class="sxs-lookup"><span data-stu-id="8a60f-138">In a nullable context in C# 8.0 and later, `T` must be a non-nullable type that implements the specified interface.</span></span>|
|<span data-ttu-id="8a60f-139">`where T :` *\<interface name>?*</span><span class="sxs-lookup"><span data-stu-id="8a60f-139">`where T :` *\<interface name>?*</span></span>|<span data-ttu-id="8a60f-140">Argument typu musí být nebo implementovat zadané rozhraní.</span><span class="sxs-lookup"><span data-stu-id="8a60f-140">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="8a60f-141">Je možné zadat více omezení rozhraní.</span><span class="sxs-lookup"><span data-stu-id="8a60f-141">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="8a60f-142">Omezení rozhraní může být také obecné.</span><span class="sxs-lookup"><span data-stu-id="8a60f-142">The constraining interface can also be generic.</span></span> <span data-ttu-id="8a60f-143">V kontextu s možnou hodnotou null v jazyce C# 8,0 `T` může být odkazový typ s možnou hodnotou null, odkazem, který nepovoluje hodnotu null nebo typ hodnoty.</span><span class="sxs-lookup"><span data-stu-id="8a60f-143">In a nullable context in C# 8.0, `T` may be a nullable reference type, a non-nullable reference type, or a value type.</span></span> <span data-ttu-id="8a60f-144">`T`nesmí se jednat o typ hodnoty s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-144">`T` may not be a nullable value type.</span></span>|
|`where T : U`|<span data-ttu-id="8a60f-145">Argument typu zadaný pro `T` musí být nebo odvozen od argumentu zadaného pro `U` .</span><span class="sxs-lookup"><span data-stu-id="8a60f-145">The type argument supplied for `T` must be or derive from the argument supplied for `U`.</span></span> <span data-ttu-id="8a60f-146">V kontextu s možnou hodnotou null, pokud `U` je odkazový typ, který nepovoluje hodnotu null, `T` musí být odkazový typ, který není null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-146">In a nullable context, if `U` is a non-nullable reference type, `T` must be non-nullable reference type.</span></span> <span data-ttu-id="8a60f-147">Pokud `U` je odkazový typ s možnou hodnotou null, `T` může mít buď hodnotu null, nebo nemůže být null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-147">If `U` is a nullable reference type, `T` may be either nullable or non-nullable.</span></span> |

## <a name="why-use-constraints"></a><span data-ttu-id="8a60f-148">Proč použít omezení</span><span class="sxs-lookup"><span data-stu-id="8a60f-148">Why use constraints</span></span>

<span data-ttu-id="8a60f-149">Omezení určují schopnosti a očekávání parametru typu.</span><span class="sxs-lookup"><span data-stu-id="8a60f-149">Constraints specify the capabilities and expectations of a type parameter.</span></span> <span data-ttu-id="8a60f-150">Deklarace těchto omezení znamená, že můžete použít volání operací a metody omezení typu.</span><span class="sxs-lookup"><span data-stu-id="8a60f-150">Declaring those constraints means you can use the operations and method calls of the constraining type.</span></span> <span data-ttu-id="8a60f-151">Pokud vaše obecná třída nebo metoda používá jakoukoli operaci u obecných členů nad rámec jednoduchého přiřazení nebo volání jakékoli metody, kterou nepodporuje <xref:System.Object?displayProperty=nameWithType> , budete muset použít omezení na parametr typu.</span><span class="sxs-lookup"><span data-stu-id="8a60f-151">If your generic class or method uses any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="8a60f-152">Například omezení základní třídy říká kompilátoru, že jako argumenty typu budou použity pouze objekty tohoto typu nebo odvozené z tohoto typu.</span><span class="sxs-lookup"><span data-stu-id="8a60f-152">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="8a60f-153">Jakmile má kompilátor tuto záruku, může dovolit volání metod tohoto typu v obecné třídě.</span><span class="sxs-lookup"><span data-stu-id="8a60f-153">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="8a60f-154">Následující příklad kódu ukazuje funkce, které lze přidat do `GenericList<T>` třídy (v části [Úvod do obecných](../../../standard/generics/index.md)) pomocí omezení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="8a60f-154">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](../../../standard/generics/index.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#9)]

<span data-ttu-id="8a60f-155">Omezení umožňuje, aby obecná třída používala `Employee.Name` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="8a60f-155">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="8a60f-156">Omezení určuje, že všechny položky typu `T` jsou zaručeny buď `Employee` objekt, nebo objekt, který dědí z `Employee` .</span><span class="sxs-lookup"><span data-stu-id="8a60f-156">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="8a60f-157">U stejného parametru typu lze použít více omezení a samotné omezení mohou být obecné typy, a to takto:</span><span class="sxs-lookup"><span data-stu-id="8a60f-157">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#10)]

<span data-ttu-id="8a60f-158">Při použití `where T : class` omezení se vyhněte `==` `!=` operátorům a v parametru typu, protože tyto operátory budou testovat pouze referenční identitu, nikoli pro rovnost hodnot.</span><span class="sxs-lookup"><span data-stu-id="8a60f-158">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="8a60f-159">K tomuto chování dochází, i když jsou tyto operátory přetíženy v typu, který se používá jako argument.</span><span class="sxs-lookup"><span data-stu-id="8a60f-159">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="8a60f-160">Následující kód ilustruje tento bod; výstup je false, i když <xref:System.String> Třída přetěžuje `==` operátor.</span><span class="sxs-lookup"><span data-stu-id="8a60f-160">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#11)]

<span data-ttu-id="8a60f-161">Kompilátor zná pouze `T` odkazový typ v době kompilace a musí používat výchozí operátory, které jsou platné pro všechny typy odkazů.</span><span class="sxs-lookup"><span data-stu-id="8a60f-161">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="8a60f-162">Pokud je nutné testovat rovnost hodnot, doporučuje se také použít `where T : IEquatable<T>` `where T : IComparable<T>` omezení nebo a implementovat rozhraní v jakékoli třídě, která bude použita k vytvoření obecné třídy.</span><span class="sxs-lookup"><span data-stu-id="8a60f-162">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="8a60f-163">Omezení více parametrů</span><span class="sxs-lookup"><span data-stu-id="8a60f-163">Constraining multiple parameters</span></span>

<span data-ttu-id="8a60f-164">Můžete použít omezení na více parametrů a více omezení na jeden parametr, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="8a60f-164">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="8a60f-165">Neohraničené parametry typu</span><span class="sxs-lookup"><span data-stu-id="8a60f-165">Unbounded type parameters</span></span>

 <span data-ttu-id="8a60f-166">Parametry typu, které nemají žádná omezení, jako je například T ve veřejné třídě `SampleClass<T>{}` , se nazývají neohraničené parametry typu.</span><span class="sxs-lookup"><span data-stu-id="8a60f-166">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="8a60f-167">Neohraničené parametry typu mají následující pravidla:</span><span class="sxs-lookup"><span data-stu-id="8a60f-167">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="8a60f-168">`!=`Operátory a `==` nelze použít, protože není nijak zaručeno, že konkrétní argument typu bude podporovat tyto operátory.</span><span class="sxs-lookup"><span data-stu-id="8a60f-168">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="8a60f-169">Lze je převést na `System.Object` nebo z nebo explicitně převést na libovolný typ rozhraní.</span><span class="sxs-lookup"><span data-stu-id="8a60f-169">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="8a60f-170">Můžete je porovnat s [hodnotou null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="8a60f-170">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="8a60f-171">Pokud je neohraničený parametr porovnán `null` , porovnávání vždy vrátí hodnotu false, pokud je argumentem typu hodnotový typ.</span><span class="sxs-lookup"><span data-stu-id="8a60f-171">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="8a60f-172">Parametry typu jako omezení</span><span class="sxs-lookup"><span data-stu-id="8a60f-172">Type parameters as constraints</span></span>

<span data-ttu-id="8a60f-173">Použití parametru obecného typu jako omezení je užitečné, pokud členská funkce s vlastním parametrem typu musí omezit tento parametr na parametr typu nadřazeného typu, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="8a60f-173">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#13)]

<span data-ttu-id="8a60f-174">V předchozím příkladu `T` je omezení typu v kontextu `Add` metody a parametr nevázaného typu v kontextu `List` třídy.</span><span class="sxs-lookup"><span data-stu-id="8a60f-174">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="8a60f-175">Parametry typu lze také použít jako omezení v definicích obecných tříd.</span><span class="sxs-lookup"><span data-stu-id="8a60f-175">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="8a60f-176">Parametr typu musí být deklarovaný v lomených závorkách spolu s jinými parametry typu:</span><span class="sxs-lookup"><span data-stu-id="8a60f-176">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](snippets/GenericWhereConstraints.cs#14)]

<span data-ttu-id="8a60f-177">Užitečnost parametrů typu jako omezení s obecnými třídami je omezená, protože kompilátor může předpokládat nic o parametru typu s výjimkou toho, že je odvozen z `System.Object` .</span><span class="sxs-lookup"><span data-stu-id="8a60f-177">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="8a60f-178">Použijte parametry typu jako omezení pro obecné třídy ve scénářích, ve kterých chcete vynutit vztah dědičnosti mezi dvěma parametry typu.</span><span class="sxs-lookup"><span data-stu-id="8a60f-178">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="8a60f-179">Omezení NotNull</span><span class="sxs-lookup"><span data-stu-id="8a60f-179">NotNull constraint</span></span>

<span data-ttu-id="8a60f-180">Počínaje jazykem C# 8,0 v kontextu s možnou hodnotou null můžete použít `notnull` omezení, chcete-li určit, že argument typu musí být typ hodnoty, která není null, nebo odkaz na typ, který nepovoluje hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-180">Beginning with C# 8.0 in a nullable context, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="8a60f-181">`notnull`Omezení lze použít pouze v `nullable enable` kontextu.</span><span class="sxs-lookup"><span data-stu-id="8a60f-181">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="8a60f-182">Kompilátor vygeneruje upozornění, pokud přidáte `notnull` omezení do oblivious kontextu s možnou hodnotou null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-182">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span>

<span data-ttu-id="8a60f-183">Na rozdíl od jiných omezení, pokud argument typu narušuje `notnull` omezení, kompilátor vygeneruje upozornění, když je tento kód zkompilován v `nullable enable` kontextu.</span><span class="sxs-lookup"><span data-stu-id="8a60f-183">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="8a60f-184">Pokud je kód zkompilován v oblivious kontextu s možnou hodnotou null, kompilátor negeneruje žádná upozornění ani chyby.</span><span class="sxs-lookup"><span data-stu-id="8a60f-184">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

<span data-ttu-id="8a60f-185">Počínaje jazykem C# 8,0 v kontextu s možnou hodnotou null `class` Určuje omezení, že argument typu musí být odkazový typ, který není null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-185">Beginning with C# 8.0 in a nullable context, the `class` constraint specifies that the type argument must be a non-nullable reference type.</span></span> <span data-ttu-id="8a60f-186">V kontextu s možnou hodnotou null, pokud je parametr typu odkazem s možnou hodnotou null, kompilátor vygeneruje upozornění.</span><span class="sxs-lookup"><span data-stu-id="8a60f-186">In a nullable context, when a type parameter is a nullable reference type, the compiler generates a warning.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="8a60f-187">Nespravované omezení</span><span class="sxs-lookup"><span data-stu-id="8a60f-187">Unmanaged constraint</span></span>

<span data-ttu-id="8a60f-188">Počínaje jazykem C# 7,3 můžete použít `unmanaged` omezení, chcete-li určit, že parametr typu musí být [nespravovaný typ](../../language-reference/builtin-types/unmanaged-types.md), který nemůže mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="8a60f-188">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="8a60f-189">`unmanaged`Omezení umožňuje napsat opakovaně použitelné rutiny pro práci s typy, které mohou být manipulovány jako bloky paměti, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="8a60f-189">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](snippets/GenericWhereConstraints.cs#15)]

<span data-ttu-id="8a60f-190">Předchozí metoda musí být zkompilována v `unsafe` kontextu, protože používá `sizeof` operátor u typu, který není známý jako vestavěný typ.</span><span class="sxs-lookup"><span data-stu-id="8a60f-190">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="8a60f-191">Bez `unmanaged` omezení `sizeof` není operátor k dispozici.</span><span class="sxs-lookup"><span data-stu-id="8a60f-191">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

<span data-ttu-id="8a60f-192">`unmanaged`Omezení implikuje `struct` omezení a nelze je kombinovat s ním.</span><span class="sxs-lookup"><span data-stu-id="8a60f-192">The `unmanaged` constraint implies the `struct` constraint and can't be combined with it.</span></span> <span data-ttu-id="8a60f-193">Vzhledem k tomu `struct` , že omezení implikuje omezení `new()` , `unmanaged` nelze omezení kombinovat `new()` i s omezením.</span><span class="sxs-lookup"><span data-stu-id="8a60f-193">Because the `struct` constraint implies the `new()` constraint, the `unmanaged` constraint can't be combined with the `new()` constraint as well.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="8a60f-194">Omezení delegování</span><span class="sxs-lookup"><span data-stu-id="8a60f-194">Delegate constraints</span></span>

<span data-ttu-id="8a60f-195">Počínaje jazykem C# 7,3 můžete také použít <xref:System.Delegate?displayProperty=nameWithType> <xref:System.MulticastDelegate?displayProperty=nameWithType> omezení základní třídy nebo.</span><span class="sxs-lookup"><span data-stu-id="8a60f-195">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="8a60f-196">CLR vždy povoluje toto omezení, ale jazyk C# ho nepovolil.</span><span class="sxs-lookup"><span data-stu-id="8a60f-196">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="8a60f-197">`System.Delegate`Omezení umožňuje napsat kód, který pracuje s delegáty způsobem bezpečným pro typ.</span><span class="sxs-lookup"><span data-stu-id="8a60f-197">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="8a60f-198">Následující kód definuje metodu rozšíření, která kombinuje dva delegáty, pokud jsou stejného typu:</span><span class="sxs-lookup"><span data-stu-id="8a60f-198">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](snippets/GenericWhereConstraints.cs#16)]

<span data-ttu-id="8a60f-199">K kombinování delegátů, kteří mají stejný typ, můžete použít výše uvedenou metodu:</span><span class="sxs-lookup"><span data-stu-id="8a60f-199">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](snippets/GenericWhereConstraints.cs#17)]

<span data-ttu-id="8a60f-200">Pokud Odkomentujete poslední řádek, nebude zkompilován.</span><span class="sxs-lookup"><span data-stu-id="8a60f-200">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="8a60f-201">`first`A `test` jsou typy delegátů, ale jsou různými typy delegátů.</span><span class="sxs-lookup"><span data-stu-id="8a60f-201">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="8a60f-202">Omezení výčtu</span><span class="sxs-lookup"><span data-stu-id="8a60f-202">Enum constraints</span></span>

<span data-ttu-id="8a60f-203">Počínaje jazykem C# 7,3 můžete také zadat <xref:System.Enum?displayProperty=nameWithType> typ jako omezení základní třídy.</span><span class="sxs-lookup"><span data-stu-id="8a60f-203">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="8a60f-204">CLR vždy povoluje toto omezení, ale jazyk C# ho nepovolil.</span><span class="sxs-lookup"><span data-stu-id="8a60f-204">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="8a60f-205">Obecné typy pomocí `System.Enum` poskytují programování zajišťující bezpečnost typů pro ukládání výsledků z použití statických metod v `System.Enum` .</span><span class="sxs-lookup"><span data-stu-id="8a60f-205">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="8a60f-206">Následující ukázka vyhledá všechny platné hodnoty pro typ výčtu a potom vytvoří slovník, který tyto hodnoty mapuje na jeho řetězcovou reprezentaci.</span><span class="sxs-lookup"><span data-stu-id="8a60f-206">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the enum constraint](snippets/GenericWhereConstraints.cs#18)]

<span data-ttu-id="8a60f-207">`Enum.GetValues`a `Enum.GetName` používejte reflexi, který má dopad na výkon.</span><span class="sxs-lookup"><span data-stu-id="8a60f-207">`Enum.GetValues` and `Enum.GetName` use reflection, which has performance implications.</span></span> <span data-ttu-id="8a60f-208">Můžete zavolat `EnumNamedValues` k sestavení kolekce, která je uložena v mezipaměti a znovu použita, nikoli volání, která vyžadují reflexi.</span><span class="sxs-lookup"><span data-stu-id="8a60f-208">You can call `EnumNamedValues` to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="8a60f-209">Můžete ji použít, jak je znázorněno v následujícím příkladu, pro vytvoření výčtu a sestavení slovníku jeho hodnot a názvů:</span><span class="sxs-lookup"><span data-stu-id="8a60f-209">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[enum definition](snippets/GenericWhereConstraints.cs#19)]

[!code-csharp[using the enum constrained method](snippets/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="8a60f-210">Viz také</span><span class="sxs-lookup"><span data-stu-id="8a60f-210">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="8a60f-211">Průvodce programováním v C#</span><span class="sxs-lookup"><span data-stu-id="8a60f-211">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="8a60f-212">Úvod do obecných typů</span><span class="sxs-lookup"><span data-stu-id="8a60f-212">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="8a60f-213">Obecné třídy</span><span class="sxs-lookup"><span data-stu-id="8a60f-213">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="8a60f-214">nové omezení</span><span class="sxs-lookup"><span data-stu-id="8a60f-214">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
