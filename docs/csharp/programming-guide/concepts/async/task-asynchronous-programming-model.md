---
title: Úloha asynchronního programování (klepnutím) s modifikátorem Async a operátoru Await (C#)
description: Naučte se, kdy a jak používat asynchronní programování založené na úlohách, zjednodušený přístup k asynchronnímu programování v jazyce C#.
ms.date: 08/19/2020
ms.assetid: 9bcf896a-5826-4189-8c1a-3e35fa08243a
ms.openlocfilehash: 5e85b99025b31e205c66468d4bd886701cbaea17
ms.sourcegitcommit: 9c45035b781caebc63ec8ecf912dc83fb6723b1f
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/25/2020
ms.locfileid: "88812082"
---
# <a name="task-asynchronous-programming-model"></a><span data-ttu-id="f7510-103">Model asynchronního programování úloh</span><span class="sxs-lookup"><span data-stu-id="f7510-103">Task asynchronous programming model</span></span>

<span data-ttu-id="f7510-104">Pomocí asynchronního programování se můžete vyhnout kritickým bodům a zlepšit celkovou rychlost reakce aplikace.</span><span class="sxs-lookup"><span data-stu-id="f7510-104">You can avoid performance bottlenecks and enhance the overall responsiveness of your application by using asynchronous programming.</span></span> <span data-ttu-id="f7510-105">Tradiční techniky pro psaní asynchronních aplikací však mohou být složité, takže je obtížné je napsat, ladit a udržovat.</span><span class="sxs-lookup"><span data-stu-id="f7510-105">However, traditional techniques for writing asynchronous applications can be complicated, making them difficult to write, debug, and maintain.</span></span>

<span data-ttu-id="f7510-106">[Jazyk C# 5](../../../whats-new/csharp-version-history.md#c-version-50) představil zjednodušený přístup, asynchronní programování, které využívá asynchronní podporu v .NET Framework 4,5 a vyšších, .NET Core a prostředí Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="f7510-106">[C# 5](../../../whats-new/csharp-version-history.md#c-version-50) introduced a simplified approach, async programming, that leverages asynchronous support in the .NET Framework 4.5 and higher, .NET Core, and the Windows Runtime.</span></span> <span data-ttu-id="f7510-107">Kompilátor na sebe přejímá náročnou práci, kterou vykonával vývojář, a vaše aplikace si zachovává logickou strukturu, která se podobá synchronnímu kódu.</span><span class="sxs-lookup"><span data-stu-id="f7510-107">The compiler does the difficult work that the developer used to do, and your application retains a logical structure that resembles synchronous code.</span></span> <span data-ttu-id="f7510-108">Výsledkem je, že získáte všechny výhody asynchronního programování při pouhém zlomku úsilí.</span><span class="sxs-lookup"><span data-stu-id="f7510-108">As a result, you get all the advantages of asynchronous programming with a fraction of the effort.</span></span>

<span data-ttu-id="f7510-109">Toto téma obsahuje přehled kdy a jak použít asynchronní programování a obsahuje odkazy na témata podpory, která obsahují podrobné informace a příklady.</span><span class="sxs-lookup"><span data-stu-id="f7510-109">This topic provides an overview of when and how to use async programming and includes links to support topics that contain details and examples.</span></span>

## <a name="async-improves-responsiveness"></a><a name="BKMK_WhentoUseAsynchrony"></a> <span data-ttu-id="f7510-110">Asynchronní funkce vylepšuje rychlost odezvy.</span><span class="sxs-lookup"><span data-stu-id="f7510-110">Async improves responsiveness</span></span>

<span data-ttu-id="f7510-111">Asynchronii je nezbytné pro aktivity, které jsou potenciálně blokovány, například webový přístup.</span><span class="sxs-lookup"><span data-stu-id="f7510-111">Asynchrony is essential for activities that are potentially blocking, such as web access.</span></span> <span data-ttu-id="f7510-112">Přístup k webovému prostředku je někdy pomalý nebo zpožděný.</span><span class="sxs-lookup"><span data-stu-id="f7510-112">Access to a web resource sometimes is slow or delayed.</span></span> <span data-ttu-id="f7510-113">Pokud je taková aktivita zablokovaná v synchronním procesu, musí počkat celá aplikace.</span><span class="sxs-lookup"><span data-stu-id="f7510-113">If such an activity is blocked in a synchronous process, the entire application must wait.</span></span> <span data-ttu-id="f7510-114">U asynchronního procesu může aplikace pokračovat v další práci, která nezávisí na webovém prostředku, dokud neskončí potenciálně blokující úloha.</span><span class="sxs-lookup"><span data-stu-id="f7510-114">In an asynchronous process, the application can continue with other work that doesn't depend on the web resource until the potentially blocking task finishes.</span></span>

<span data-ttu-id="f7510-115">Následující tabulka ukazuje typické oblasti, kde asynchronní programování zlepšuje rychlost reakce.</span><span class="sxs-lookup"><span data-stu-id="f7510-115">The following table shows typical areas where asynchronous programming improves responsiveness.</span></span> <span data-ttu-id="f7510-116">Uvedená rozhraní API z rozhraní .NET a prostředí Windows Runtime obsahují metody, které podporují asynchronní programování.</span><span class="sxs-lookup"><span data-stu-id="f7510-116">The listed APIs from .NET and the Windows Runtime contain methods that support async programming.</span></span>

| <span data-ttu-id="f7510-117">Oblast aplikace</span><span class="sxs-lookup"><span data-stu-id="f7510-117">Application area</span></span> | <span data-ttu-id="f7510-118">Typy .NET s asynchronními metodami</span><span class="sxs-lookup"><span data-stu-id="f7510-118">.NET types with async methods</span></span> | <span data-ttu-id="f7510-119">Typy prostředí Windows Runtime s asynchronními metodami</span><span class="sxs-lookup"><span data-stu-id="f7510-119">Windows Runtime types with async methods</span></span> |
|--|--|--|
| <span data-ttu-id="f7510-120">Webový přístup</span><span class="sxs-lookup"><span data-stu-id="f7510-120">Web access</span></span> | <xref:System.Net.Http.HttpClient> | <xref:Windows.Web.Http.HttpClient?displayProperty=nameWithType> <br> <xref:Windows.Web.Syndication.SyndicationClient> |
| <span data-ttu-id="f7510-121">Práce se soubory</span><span class="sxs-lookup"><span data-stu-id="f7510-121">Working with files</span></span> | <xref:System.Text.Json.JsonSerializer> <br> <xref:System.IO.StreamReader> <br> <xref:System.IO.StreamWriter> <br> <xref:System.Xml.XmlReader> <br> <xref:System.Xml.XmlWriter> | <xref:Windows.Storage.StorageFile> |
| <span data-ttu-id="f7510-122">Práce s obrázky</span><span class="sxs-lookup"><span data-stu-id="f7510-122">Working with images</span></span> |  | <xref:Windows.Media.Capture.MediaCapture> <br> <xref:Windows.Graphics.Imaging.BitmapEncoder> <br> <xref:Windows.Graphics.Imaging.BitmapDecoder> |
| <span data-ttu-id="f7510-123">Programování WCF</span><span class="sxs-lookup"><span data-stu-id="f7510-123">WCF programming</span></span> | [<span data-ttu-id="f7510-124">Synchronní a asynchronní operace</span><span class="sxs-lookup"><span data-stu-id="f7510-124">Synchronous and Asynchronous Operations</span></span>](../../../../framework/wcf/synchronous-and-asynchronous-operations.md) |  |

<span data-ttu-id="f7510-125">Asynchronie je obzvláště užitečná pro aplikace, které přistupují k vláknu UI, protože všechny aktivity související s uživatelským rozhraním obvykle sdílí jedno vlákno.</span><span class="sxs-lookup"><span data-stu-id="f7510-125">Asynchrony proves especially valuable for applications that access the UI thread because all UI-related activity usually shares one thread.</span></span> <span data-ttu-id="f7510-126">Pokud je jakýkoli proces blokován v synchronní aplikaci, jsou blokovány všechny.</span><span class="sxs-lookup"><span data-stu-id="f7510-126">If any process is blocked in a synchronous application, all are blocked.</span></span> <span data-ttu-id="f7510-127">Vaše aplikace přestane reagovat a můžete dojít k závěru, že selhala, i když místo toho čeká.</span><span class="sxs-lookup"><span data-stu-id="f7510-127">Your application stops responding, and you might conclude that it has failed when instead it's just waiting.</span></span>

<span data-ttu-id="f7510-128">Při použití asynchronních metod bude aplikace i nadále odpovídat na uživatelské rozhraní.</span><span class="sxs-lookup"><span data-stu-id="f7510-128">When you use asynchronous methods, the application continues to respond to the UI.</span></span> <span data-ttu-id="f7510-129">Pokud nechcete čekat na dokončení, můžete změnit velikost nebo minimalizovat okno, například můžete zavřít aplikaci.</span><span class="sxs-lookup"><span data-stu-id="f7510-129">You can resize or minimize a window, for example, or you can close the application if you don't want to wait for it to finish.</span></span>

<span data-ttu-id="f7510-130">Asynchronní přístup přidává ekvivalent automatického přenosu do seznamu možností, z nichž můžete vybírat při vytváření asynchronní operace.</span><span class="sxs-lookup"><span data-stu-id="f7510-130">The async-based approach adds the equivalent of an automatic transmission to the list of options that you can choose from when designing asynchronous operations.</span></span> <span data-ttu-id="f7510-131">To znamená, že získáte všechny výhody tradičního asynchronního programování, ale s mnohem menším úsilím ze strany vývojáře.</span><span class="sxs-lookup"><span data-stu-id="f7510-131">That is, you get all the benefits of traditional asynchronous programming but with much less effort from the developer.</span></span>

## <a name="async-methods-are-easy-to-write"></a><a name="BKMK_HowtoWriteanAsyncMethod"></a> <span data-ttu-id="f7510-132">Asynchronní metody se snadno zapisují</span><span class="sxs-lookup"><span data-stu-id="f7510-132">Async methods are easy to write</span></span>

<span data-ttu-id="f7510-133">Klíčová slova [Async](../../../language-reference/keywords/async.md) a [await](../../../language-reference/operators/await.md) v jazyce C# představují srdce asynchronního programování.</span><span class="sxs-lookup"><span data-stu-id="f7510-133">The [async](../../../language-reference/keywords/async.md) and [await](../../../language-reference/operators/await.md) keywords in C# are the heart of async programming.</span></span> <span data-ttu-id="f7510-134">Pomocí těchto dvou klíčových slov můžete použít prostředky v .NET Framework, .NET Core nebo prostředí Windows Runtime k vytvoření asynchronní metody téměř stejně snadno, jako vytváříte synchronní metodu.</span><span class="sxs-lookup"><span data-stu-id="f7510-134">By using those two keywords, you can use resources in .NET Framework, .NET Core, or the Windows Runtime to create an asynchronous method almost as easily as you create a synchronous method.</span></span> <span data-ttu-id="f7510-135">Asynchronní metody, které definujete pomocí `async` klíčového slova, jsou označovány jako *asynchronní metody*.</span><span class="sxs-lookup"><span data-stu-id="f7510-135">Asynchronous methods that you define by using the `async` keyword are referred to as *async methods*.</span></span>

<span data-ttu-id="f7510-136">Následující příklad ukazuje asynchronní metodu.</span><span class="sxs-lookup"><span data-stu-id="f7510-136">The following example shows an async method.</span></span> <span data-ttu-id="f7510-137">Téměř vše v kódu by mělo vypadat dobře.</span><span class="sxs-lookup"><span data-stu-id="f7510-137">Almost everything in the code should look familiar to you.</span></span>

<span data-ttu-id="f7510-138">Můžete najít kompletní příklad Windows Presentation Foundation (WPF), který je k dispozici ke stažení z [asynchronního programování s modifikátorem Async a await v jazyce C#](https://docs.microsoft.com/samples/dotnet/samples/async-and-await-cs).</span><span class="sxs-lookup"><span data-stu-id="f7510-138">You can find a complete Windows Presentation Foundation (WPF) example available for download from [Asynchronous programming with async and await in C#](https://docs.microsoft.com/samples/dotnet/samples/async-and-await-cs).</span></span>

:::code language="csharp" source="snippets/access-web/Program.cs" id="ControlFlow":::

<span data-ttu-id="f7510-139">V předchozím příkladu se můžete seznámit s několika postupy.</span><span class="sxs-lookup"><span data-stu-id="f7510-139">You can learn several practices from the preceding sample.</span></span> <span data-ttu-id="f7510-140">Začněte s podpisem metody.</span><span class="sxs-lookup"><span data-stu-id="f7510-140">Start with the method signature.</span></span> <span data-ttu-id="f7510-141">Obsahuje `async` modifikátor.</span><span class="sxs-lookup"><span data-stu-id="f7510-141">It includes the `async` modifier.</span></span> <span data-ttu-id="f7510-142">Návratový typ je `Task<int>` (další možnosti najdete v části "návratové typy").</span><span class="sxs-lookup"><span data-stu-id="f7510-142">The return type is `Task<int>` (See "Return Types" section for more options).</span></span> <span data-ttu-id="f7510-143">Název metody končí `Async` .</span><span class="sxs-lookup"><span data-stu-id="f7510-143">The method name ends in `Async`.</span></span> <span data-ttu-id="f7510-144">V těle metody `GetStringAsync` vrátí `Task<string>` .</span><span class="sxs-lookup"><span data-stu-id="f7510-144">In the body of the method, `GetStringAsync` returns a `Task<string>`.</span></span> <span data-ttu-id="f7510-145">To znamená, že po `await` obdržení úlohy získáte `string` ( `contents` ).</span><span class="sxs-lookup"><span data-stu-id="f7510-145">That means that when you `await` the task you'll get a `string` (`contents`).</span></span> <span data-ttu-id="f7510-146">Před čekáním na úlohu můžete provádět práci, která nespoléhá na `string` z `GetStringAsync` .</span><span class="sxs-lookup"><span data-stu-id="f7510-146">Before awaiting the task, you can do work that doesn't rely on the `string` from `GetStringAsync`.</span></span>

<span data-ttu-id="f7510-147">Věnujte velkou pozornost `await` operátoru.</span><span class="sxs-lookup"><span data-stu-id="f7510-147">Pay close attention to the `await` operator.</span></span> <span data-ttu-id="f7510-148">Pozastavuje se `GetUrlContentLengthAsync` :</span><span class="sxs-lookup"><span data-stu-id="f7510-148">It suspends `GetUrlContentLengthAsync`:</span></span>

- <span data-ttu-id="f7510-149">`GetUrlContentLengthAsync` nedá se pokračovat `getStringTask` , dokud se nedokončí.</span><span class="sxs-lookup"><span data-stu-id="f7510-149">`GetUrlContentLengthAsync` can't continue until `getStringTask` is complete.</span></span>
- <span data-ttu-id="f7510-150">Mezitím se ovládací prvek vrátí volajícímu `GetUrlContentLengthAsync` .</span><span class="sxs-lookup"><span data-stu-id="f7510-150">Meanwhile, control returns to the caller of `GetUrlContentLengthAsync`.</span></span>
- <span data-ttu-id="f7510-151">Po dokončení se ovládací prvek obnoví `getStringTask` .</span><span class="sxs-lookup"><span data-stu-id="f7510-151">Control resumes here when `getStringTask` is complete.</span></span>
- <span data-ttu-id="f7510-152">`await`Operátor následně načte `string` výsledek z `getStringTask` .</span><span class="sxs-lookup"><span data-stu-id="f7510-152">The `await` operator then retrieves the `string` result from `getStringTask`.</span></span>

<span data-ttu-id="f7510-153">Příkaz return určuje celočíselný výsledek.</span><span class="sxs-lookup"><span data-stu-id="f7510-153">The return statement specifies an integer result.</span></span> <span data-ttu-id="f7510-154">Všechny metody, které čekají `GetUrlContentLengthAsync` na načtení hodnoty Length.</span><span class="sxs-lookup"><span data-stu-id="f7510-154">Any methods that are awaiting `GetUrlContentLengthAsync` retrieve the length value.</span></span>

<span data-ttu-id="f7510-155">Pokud nemá `GetUrlContentLengthAsync` žádnou práci, kterou může provést mezi voláním `GetStringAsync` a čekáním na jeho dokončení, můžete zjednodušit kód voláním a čekáním v následujícím jediném příkazu.</span><span class="sxs-lookup"><span data-stu-id="f7510-155">If `GetUrlContentLengthAsync` doesn't have any work that it can do between calling `GetStringAsync` and awaiting its completion, you can simplify your code by calling and awaiting in the following single statement.</span></span>

```csharp
string contents = await client.GetStringAsync("https://docs.microsoft.com/dotnet");
```

<span data-ttu-id="f7510-156">Následující charakteristiky shrnují, co dělá předchozí příklad asynchronní metodou:</span><span class="sxs-lookup"><span data-stu-id="f7510-156">The following characteristics summarize what makes the previous example an async method:</span></span>

- <span data-ttu-id="f7510-157">Signatura metody obsahuje `async` modifikátor.</span><span class="sxs-lookup"><span data-stu-id="f7510-157">The method signature includes an `async` modifier.</span></span>
- <span data-ttu-id="f7510-158">Název asynchronní metody končí podle konvence příponou „Async“.</span><span class="sxs-lookup"><span data-stu-id="f7510-158">The name of an async method, by convention, ends with an "Async" suffix.</span></span>
- <span data-ttu-id="f7510-159">Návratový typ je jeden z následujících typů:</span><span class="sxs-lookup"><span data-stu-id="f7510-159">The return type is one of the following types:</span></span>

  - <span data-ttu-id="f7510-160"><xref:System.Threading.Tasks.Task%601> Pokud vaše metoda má návratový příkaz, ve kterém je operand typu `TResult` .</span><span class="sxs-lookup"><span data-stu-id="f7510-160"><xref:System.Threading.Tasks.Task%601> if your method has a return statement in which the operand has type `TResult`.</span></span>
  - <span data-ttu-id="f7510-161"><xref:System.Threading.Tasks.Task> Pokud vaše metoda nemá návratový příkaz nebo má návratový příkaz bez operandu.</span><span class="sxs-lookup"><span data-stu-id="f7510-161"><xref:System.Threading.Tasks.Task> if your method has no return statement or has a return statement with no operand.</span></span>
  - <span data-ttu-id="f7510-162">`void` Pokud píšete asynchronní obslužnou rutinu události.</span><span class="sxs-lookup"><span data-stu-id="f7510-162">`void` if you're writing an async event handler.</span></span>
  - <span data-ttu-id="f7510-163">Jakýkoli jiný typ, který má `GetAwaiter` metodu (počínaje jazykem C# 7,0).</span><span class="sxs-lookup"><span data-stu-id="f7510-163">Any other type that has a `GetAwaiter` method (starting with C# 7.0).</span></span>

  <span data-ttu-id="f7510-164">Další informace naleznete v části [návratové typy a parametry](#BKMK_ReturnTypesandParameters) .</span><span class="sxs-lookup"><span data-stu-id="f7510-164">For more information, see the [Return types and parameters](#BKMK_ReturnTypesandParameters) section.</span></span>

- <span data-ttu-id="f7510-165">Metoda obvykle zahrnuje nejméně jeden `await` výraz, který označuje bod, kde metoda nemůže pokračovat, dokud není dokončena očekávaná asynchronní operace.</span><span class="sxs-lookup"><span data-stu-id="f7510-165">The method usually includes at least one `await` expression, which marks a point where the method can't continue until the awaited asynchronous operation is complete.</span></span> <span data-ttu-id="f7510-166">Během této doby je metoda pozastavena a ovládací prvek se vrátí volajícímu metody.</span><span class="sxs-lookup"><span data-stu-id="f7510-166">In the meantime, the method is suspended, and control returns to the method's caller.</span></span> <span data-ttu-id="f7510-167">Další část tohoto tématu ukazuje, co se stane v okamžiku pozastavení.</span><span class="sxs-lookup"><span data-stu-id="f7510-167">The next section of this topic illustrates what happens at the suspension point.</span></span>

<span data-ttu-id="f7510-168">V asynchronních metodách používáte zadaná klíčová slova a typy pro označení, jakou akci chcete provést, a kompilátor udělá zbytek, včetně udržování přehledu o tom, co musí nastat, když se řízení vrátí do bodu „await“ pozastavené metody.</span><span class="sxs-lookup"><span data-stu-id="f7510-168">In async methods, you use the provided keywords and types to indicate what you want to do, and the compiler does the rest, including keeping track of what must happen when control returns to an await point in a suspended method.</span></span> <span data-ttu-id="f7510-169">Některé běžné procesy, jako je zpracování smyček a výjimek, může být v tradičním asynchronním kódu obtížné zpracovat.</span><span class="sxs-lookup"><span data-stu-id="f7510-169">Some routine processes, such as loops and exception handling, can be difficult to handle in traditional asynchronous code.</span></span> <span data-ttu-id="f7510-170">V asynchronní metodě zapisujete tyto prvky podobně, jako byste to udělali v synchronním řešení, a problém je vyřešen.</span><span class="sxs-lookup"><span data-stu-id="f7510-170">In an async method, you write these elements much as you would in a synchronous solution, and the problem is solved.</span></span>

<span data-ttu-id="f7510-171">Další informace o asynchronii v předchozích verzích .NET Framework naleznete v tématu [TPL a tradiční .NET Framework asynchronní programování](../../../../standard/parallel-programming/tpl-and-traditional-async-programming.md).</span><span class="sxs-lookup"><span data-stu-id="f7510-171">For more information about asynchrony in previous versions of .NET Framework, see [TPL and traditional .NET Framework asynchronous programming](../../../../standard/parallel-programming/tpl-and-traditional-async-programming.md).</span></span>

## <a name="what-happens-in-an-async-method"></a><a name="BKMK_WhatHappensUnderstandinganAsyncMethod"></a> <span data-ttu-id="f7510-172">Co se stane v asynchronní metodě</span><span class="sxs-lookup"><span data-stu-id="f7510-172">What happens in an async method</span></span>

<span data-ttu-id="f7510-173">Nejdůležitějším principem, který je třeba pochopit v asynchronním programování, je, jak ovládat přesuny toků od metody k metodě.</span><span class="sxs-lookup"><span data-stu-id="f7510-173">The most important thing to understand in asynchronous programming is how the control flow moves from method to method.</span></span> <span data-ttu-id="f7510-174">Následující diagram vás provede procesem:</span><span class="sxs-lookup"><span data-stu-id="f7510-174">The following diagram leads you through the process:</span></span>

:::image type="content" source="media/task-asynchronous-programming-model/navigation-trace-async-program.png" alt-text="Trasování navigace toku asynchronního řízení" lightbox="media/task-asynchronous-programming-model/navigation-trace-async-program.png":::

<span data-ttu-id="f7510-176">Čísla v diagramu odpovídají následujícím krokům, které jsou iniciovány, když volající metoda volá asynchronní metodu.</span><span class="sxs-lookup"><span data-stu-id="f7510-176">The numbers in the diagram correspond to the following steps, initiated when a calling method calls the async method.</span></span>

1. <span data-ttu-id="f7510-177">Volající metoda volá a očekává `GetUrlContentLengthAsync` asynchronní metodu.</span><span class="sxs-lookup"><span data-stu-id="f7510-177">A calling method calls and awaits the `GetUrlContentLengthAsync` async method.</span></span>

1. <span data-ttu-id="f7510-178">`GetUrlContentLengthAsync` vytvoří <xref:System.Net.Http.HttpClient> instanci a zavolá <xref:System.Net.Http.HttpClient.GetStringAsync%2A> asynchronní metodu pro stažení obsahu webu jako řetězce.</span><span class="sxs-lookup"><span data-stu-id="f7510-178">`GetUrlContentLengthAsync` creates an <xref:System.Net.Http.HttpClient> instance and calls the <xref:System.Net.Http.HttpClient.GetStringAsync%2A> asynchronous method to download the contents of a website as a string.</span></span>

1. <span data-ttu-id="f7510-179">Dojde k tomu `GetStringAsync` , že se něco pozastaví.</span><span class="sxs-lookup"><span data-stu-id="f7510-179">Something happens in `GetStringAsync` that suspends its progress.</span></span> <span data-ttu-id="f7510-180">Možná je třeba vyčkat na dokončení stahování nebo jiné blokující aktivity na webu.</span><span class="sxs-lookup"><span data-stu-id="f7510-180">Perhaps it must wait for a website to download or some other blocking activity.</span></span> <span data-ttu-id="f7510-181">Aby nedocházelo k blokování prostředků, `GetStringAsync` poskytne řízení volajícímu, `GetUrlContentLengthAsync` .</span><span class="sxs-lookup"><span data-stu-id="f7510-181">To avoid blocking resources, `GetStringAsync` yields control to its caller, `GetUrlContentLengthAsync`.</span></span>

     <span data-ttu-id="f7510-182">`GetStringAsync` Vrátí <xref:System.Threading.Tasks.Task%601> , kde `TResult` je řetězec, a `GetUrlContentLengthAsync` přiřadí úkol `getStringTask` proměnné.</span><span class="sxs-lookup"><span data-stu-id="f7510-182">`GetStringAsync` returns a <xref:System.Threading.Tasks.Task%601>, where `TResult` is a string, and `GetUrlContentLengthAsync` assigns the task to the `getStringTask` variable.</span></span> <span data-ttu-id="f7510-183">Úkol představuje pokračující proces pro volání `GetStringAsync` s závazkem vytvořit skutečnou řetězcovou hodnotu po dokončení práce.</span><span class="sxs-lookup"><span data-stu-id="f7510-183">The task represents the ongoing process for the call to `GetStringAsync`, with a commitment to produce an actual string value when the work is complete.</span></span>

1. <span data-ttu-id="f7510-184">Vzhledem k tomu `getStringTask` , že se ještě nečekalo, `GetUrlContentLengthAsync` může pokračovat v jiné práci, která nezávisí na konečném výsledku z `GetStringAsync` .</span><span class="sxs-lookup"><span data-stu-id="f7510-184">Because `getStringTask` hasn't been awaited yet, `GetUrlContentLengthAsync` can continue with other work that doesn't depend on the final result from `GetStringAsync`.</span></span> <span data-ttu-id="f7510-185">Tato práce je reprezentována voláním synchronní metody `DoIndependentWork` .</span><span class="sxs-lookup"><span data-stu-id="f7510-185">That work is represented by a call to the synchronous method `DoIndependentWork`.</span></span>

1. <span data-ttu-id="f7510-186">`DoIndependentWork` je synchronní metoda, která provede svou práci a vrátí jejímu volajícímu.</span><span class="sxs-lookup"><span data-stu-id="f7510-186">`DoIndependentWork` is a synchronous method that does its work and returns to its caller.</span></span>

1. <span data-ttu-id="f7510-187">`GetUrlContentLengthAsync` nemá dostatek práce, kterou může dělat bez výsledku z `getStringTask` .</span><span class="sxs-lookup"><span data-stu-id="f7510-187">`GetUrlContentLengthAsync` has run out of work that it can do without a result from `getStringTask`.</span></span> <span data-ttu-id="f7510-188">`GetUrlContentLengthAsync` Dále chce vypočítat a vrátit délku staženého řetězce, ale metoda nemůže tuto hodnotu vypočítat, dokud metoda nemá řetězec.</span><span class="sxs-lookup"><span data-stu-id="f7510-188">`GetUrlContentLengthAsync` next wants to calculate and return the length of the downloaded string, but the method can't calculate that value until the method has the string.</span></span>

    <span data-ttu-id="f7510-189">Proto `GetUrlContentLengthAsync` pomocí operátoru await pozastaví svůj průběh a vrátí řízení metodě, která je volána `GetUrlContentLengthAsync` .</span><span class="sxs-lookup"><span data-stu-id="f7510-189">Therefore, `GetUrlContentLengthAsync` uses an await operator to suspend its progress and to yield control to the method that called `GetUrlContentLengthAsync`.</span></span> <span data-ttu-id="f7510-190">`GetUrlContentLengthAsync` Vrátí hodnotu `Task<int>` volajícímu.</span><span class="sxs-lookup"><span data-stu-id="f7510-190">`GetUrlContentLengthAsync` returns a `Task<int>` to the caller.</span></span> <span data-ttu-id="f7510-191">Úloha představuje slib vyrábět celé číslo výsledku, který má délku staženého řetězce.</span><span class="sxs-lookup"><span data-stu-id="f7510-191">The task represents a promise to produce an integer result that's the length of the downloaded string.</span></span>

    > [!NOTE]
    > <span data-ttu-id="f7510-192">Pokud `GetStringAsync` (a tedy `getStringTask` ) se dokončí před tím `GetUrlContentLengthAsync` , než očekává, ovládací prvek zůstane v `GetUrlContentLengthAsync` .</span><span class="sxs-lookup"><span data-stu-id="f7510-192">If `GetStringAsync` (and therefore `getStringTask`) completes before `GetUrlContentLengthAsync` awaits it, control remains in `GetUrlContentLengthAsync`.</span></span> <span data-ttu-id="f7510-193">Náklady na pozastavení a návrat do `GetUrlContentLengthAsync` by byly nevyužité, pokud se pojmenovaný asynchronní proces `getStringTask` již dokončil a `GetUrlContentLengthAsync` nemusí čekat na konečný výsledek.</span><span class="sxs-lookup"><span data-stu-id="f7510-193">The expense of suspending and then returning to `GetUrlContentLengthAsync` would be wasted if the called asynchronous process `getStringTask` has already completed and `GetUrlContentLengthAsync` doesn't have to wait for the final result.</span></span>

    <span data-ttu-id="f7510-194">Uvnitř metody volání pokračuje vzor zpracování.</span><span class="sxs-lookup"><span data-stu-id="f7510-194">Inside the calling method the processing pattern continues.</span></span> <span data-ttu-id="f7510-195">Volající může provést další práci, která nezávisí na výsledku před čekáním na `GetUrlContentLengthAsync` výsledek, nebo volající může očekávat okamžitě.</span><span class="sxs-lookup"><span data-stu-id="f7510-195">The caller might do other work that doesn't depend on the result from `GetUrlContentLengthAsync` before awaiting that result, or the caller might await immediately.</span></span> <span data-ttu-id="f7510-196">Volající metoda čeká na `GetUrlContentLengthAsync` a `GetUrlContentLengthAsync` čeká na `GetStringAsync` .</span><span class="sxs-lookup"><span data-stu-id="f7510-196">The calling method is waiting for `GetUrlContentLengthAsync`, and `GetUrlContentLengthAsync` is waiting for `GetStringAsync`.</span></span>

1. <span data-ttu-id="f7510-197">`GetStringAsync` dokončí a vytvoří výsledek řetězce.</span><span class="sxs-lookup"><span data-stu-id="f7510-197">`GetStringAsync` completes and produces a string result.</span></span> <span data-ttu-id="f7510-198">Výsledek řetězce není vrácen voláním `GetStringAsync` způsobem, který by mohl očekávat.</span><span class="sxs-lookup"><span data-stu-id="f7510-198">The string result isn't returned by the call to `GetStringAsync` in the way that you might expect.</span></span> <span data-ttu-id="f7510-199">(Nezapomeňte, že metoda již vrátila úlohu v kroku 3.) Místo toho je výsledek řetězce uložen v úkolu, který představuje dokončení metody `getStringTask` .</span><span class="sxs-lookup"><span data-stu-id="f7510-199">(Remember that the method already returned a task in step 3.) Instead, the string result is stored in the task that represents the completion of the method, `getStringTask`.</span></span> <span data-ttu-id="f7510-200">Operátor await načte výsledek z `getStringTask` .</span><span class="sxs-lookup"><span data-stu-id="f7510-200">The await operator retrieves the result from `getStringTask`.</span></span> <span data-ttu-id="f7510-201">Příkaz přiřazení přiřadí načtený výsledek do `contents` .</span><span class="sxs-lookup"><span data-stu-id="f7510-201">The assignment statement assigns the retrieved result to `contents`.</span></span>

1. <span data-ttu-id="f7510-202">Když `GetUrlContentLengthAsync` má výsledek řetězce, metoda může vypočítat délku řetězce.</span><span class="sxs-lookup"><span data-stu-id="f7510-202">When `GetUrlContentLengthAsync` has the string result, the method can calculate the length of the string.</span></span> <span data-ttu-id="f7510-203">Pak `GetUrlContentLengthAsync` je práce také dokončena a může pokračovat obslužná rutina čekající události.</span><span class="sxs-lookup"><span data-stu-id="f7510-203">Then the work of `GetUrlContentLengthAsync` is also complete, and the waiting event handler can resume.</span></span> <span data-ttu-id="f7510-204">V úplném příkladu na konci tématu si můžete potvrdit, že obslužná rutina události načte a vytiskne hodnotu výsledné délky.</span><span class="sxs-lookup"><span data-stu-id="f7510-204">In the full example at the end of the topic, you can confirm that the event handler retrieves and prints the value of the length result.</span></span>
<span data-ttu-id="f7510-205">Pokud jste v oblasti asynchronního programování nováčky, zvažte rozdíl mezi synchronním a asynchronním chováním.</span><span class="sxs-lookup"><span data-stu-id="f7510-205">If you are new to asynchronous programming, take a minute to consider the difference between synchronous and asynchronous behavior.</span></span> <span data-ttu-id="f7510-206">Synchronní metoda je vrácena, jakmile je její práce dokončena (krok 5), ale asynchronní metoda vrátí hodnotu úlohy, když je její práce pozastavena (kroky 3 a 6).</span><span class="sxs-lookup"><span data-stu-id="f7510-206">A synchronous method returns when its work is complete (step 5), but an async method returns a task value when its work is suspended (steps 3 and 6).</span></span> <span data-ttu-id="f7510-207">Když asynchronní metoda nakonec dokončí svou práci, je úloha označena jako dokončená a výsledek, pokud existuje, je uložen v úloze.</span><span class="sxs-lookup"><span data-stu-id="f7510-207">When the async method eventually completes its work, the task is marked as completed and the result, if any, is stored in the task.</span></span>

## <a name="api-async-methods"></a><a name="BKMK_APIAsyncMethods"></a> <span data-ttu-id="f7510-208">Asynchronní metody rozhraní API</span><span class="sxs-lookup"><span data-stu-id="f7510-208">API async methods</span></span>

<span data-ttu-id="f7510-209">Možná vás zajímá, kde najít metody, jako je například `GetStringAsync` Podpora asynchronního programování.</span><span class="sxs-lookup"><span data-stu-id="f7510-209">You might be wondering where to find methods such as `GetStringAsync` that support async programming.</span></span> <span data-ttu-id="f7510-210">.NET Framework 4,5 nebo vyšší a .NET Core obsahují mnoho členů, kteří pracují s `async` a `await` .</span><span class="sxs-lookup"><span data-stu-id="f7510-210">.NET Framework 4.5 or higher and .NET Core contain many members that work with `async` and `await`.</span></span> <span data-ttu-id="f7510-211">Můžete je rozpoznat pomocí přípony "Async", která je připojena k názvu člena a podle jejich návratového typu <xref:System.Threading.Tasks.Task> nebo <xref:System.Threading.Tasks.Task%601> .</span><span class="sxs-lookup"><span data-stu-id="f7510-211">You can recognize them by the "Async" suffix that's appended to the member name, and by their return type of <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="f7510-212">Například `System.IO.Stream` Třída obsahuje metody, jako <xref:System.IO.Stream.CopyToAsync%2A> , a <xref:System.IO.Stream.ReadAsync%2A> <xref:System.IO.Stream.WriteAsync%2A> vedle synchronních metod <xref:System.IO.Stream.CopyTo%2A> , <xref:System.IO.Stream.Read%2A> a <xref:System.IO.Stream.Write%2A> .</span><span class="sxs-lookup"><span data-stu-id="f7510-212">For example, the `System.IO.Stream` class contains methods such as <xref:System.IO.Stream.CopyToAsync%2A>, <xref:System.IO.Stream.ReadAsync%2A>, and <xref:System.IO.Stream.WriteAsync%2A> alongside the synchronous methods <xref:System.IO.Stream.CopyTo%2A>, <xref:System.IO.Stream.Read%2A>, and <xref:System.IO.Stream.Write%2A>.</span></span>

<span data-ttu-id="f7510-213">Prostředí Windows Runtime taky obsahuje spoustu metod, které můžete používat s `async` `await` aplikacemi a v aplikacích pro Windows.</span><span class="sxs-lookup"><span data-stu-id="f7510-213">The Windows Runtime also contains many methods that you can use with `async` and `await` in Windows apps.</span></span> <span data-ttu-id="f7510-214">Další informace naleznete v tématech [dělení vlákna a asynchronní programování](/windows/uwp/threading-async/) pro vývoj UWP a [asynchronní programování (aplikace pro Windows Store)](https://docs.microsoft.com/previous-versions/windows/apps/hh464924(v=win.10)) a [rychlý Start: volání asynchronních rozhraní api v jazyce C# nebo Visual Basic](https://docs.microsoft.com/previous-versions/windows/apps/hh452713(v=win.10)) , pokud používáte starší verze prostředí Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="f7510-214">For more information, see [Threading and async programming](/windows/uwp/threading-async/) for UWP development, and [Asynchronous programming (Windows Store apps)](https://docs.microsoft.com/previous-versions/windows/apps/hh464924(v=win.10)) and [Quickstart: Calling asynchronous APIs in C# or Visual Basic](https://docs.microsoft.com/previous-versions/windows/apps/hh452713(v=win.10)) if you use earlier versions of the Windows Runtime.</span></span>

## <a name="threads"></a><a name="BKMK_Threads"></a> <span data-ttu-id="f7510-215">Vláken</span><span class="sxs-lookup"><span data-stu-id="f7510-215">Threads</span></span>

<span data-ttu-id="f7510-216">Asynchronní metody mají být neblokující operace.</span><span class="sxs-lookup"><span data-stu-id="f7510-216">Async methods are intended to be non-blocking operations.</span></span> <span data-ttu-id="f7510-217">`await`Výraz v asynchronní metodě neblokuje aktuální vlákno, zatímco je spuštěn očekávaný úkol.</span><span class="sxs-lookup"><span data-stu-id="f7510-217">An `await` expression in an async method doesn't block the current thread while the awaited task is running.</span></span> <span data-ttu-id="f7510-218">Namísto toho se výraz zaregistruje pro zbývající metody jako pokračování a vrátí řízení volajícímu asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="f7510-218">Instead, the expression signs up the rest of the method as a continuation and returns control to the caller of the async method.</span></span>

<span data-ttu-id="f7510-219">`async` `await` Klíčová slova a nezpůsobí vytvoření dalších vláken.</span><span class="sxs-lookup"><span data-stu-id="f7510-219">The `async` and `await` keywords don't cause additional threads to be created.</span></span> <span data-ttu-id="f7510-220">Asynchronní metody nevyžadují multithreading, protože asynchronní metoda není spuštěna ve vlastním vlákně.</span><span class="sxs-lookup"><span data-stu-id="f7510-220">Async methods don't require multithreading because an async method doesn't run on its own thread.</span></span> <span data-ttu-id="f7510-221">Metoda pracuje na aktuálním kontextu synchronizace a používá čas ve vlákně pouze v případě, že je metoda aktivní.</span><span class="sxs-lookup"><span data-stu-id="f7510-221">The method runs on the current synchronization context and uses time on the thread only when the method is active.</span></span> <span data-ttu-id="f7510-222">Můžete použít <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> pro přesun práce vázané na procesor do vlákna na pozadí, ale vlákno na pozadí nepomůže s procesem, který právě čeká na zpřístupnění výsledků.</span><span class="sxs-lookup"><span data-stu-id="f7510-222">You can use <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> to move CPU-bound work to a background thread, but a background thread doesn't help with a process that's just waiting for results to become available.</span></span>

<span data-ttu-id="f7510-223">Asynchronní přístup při asynchronním programování se doporučuje v téměř každém případě existujících přístupů.</span><span class="sxs-lookup"><span data-stu-id="f7510-223">The async-based approach to asynchronous programming is preferable to existing approaches in almost every case.</span></span> <span data-ttu-id="f7510-224">Konkrétně tento přístup je lepší než <xref:System.ComponentModel.BackgroundWorker> třída pro operace vázané na vstup/výstup, protože kód je jednodušší a nemusíte se chránit před konflikty časování.</span><span class="sxs-lookup"><span data-stu-id="f7510-224">In particular, this approach is better than the <xref:System.ComponentModel.BackgroundWorker> class for I/O-bound operations because the code is simpler and you don't have to guard against race conditions.</span></span> <span data-ttu-id="f7510-225">V kombinaci s <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> metodou je asynchronní programování lepší než <xref:System.ComponentModel.BackgroundWorker> pro operace vázané na procesor, protože asynchronní programování odděluje koordinační údaje o spuštění kódu z práce, která se `Task.Run` přenáší do fondu vláken.</span><span class="sxs-lookup"><span data-stu-id="f7510-225">In combination with the <xref:System.Threading.Tasks.Task.Run%2A?displayProperty=nameWithType> method, async programming is better than <xref:System.ComponentModel.BackgroundWorker> for CPU-bound operations because async programming separates the coordination details of running your code from the work that `Task.Run` transfers to the thread pool.</span></span>

## <a name="async-and-await"></a><a name="BKMK_AsyncandAwait"></a> <span data-ttu-id="f7510-226">Async a await</span><span class="sxs-lookup"><span data-stu-id="f7510-226">async and await</span></span>

<span data-ttu-id="f7510-227">Pokud určíte, že metoda je asynchronní metodou pomocí modifikátoru [Async](../../../language-reference/keywords/async.md) , povolíte následující dvě možnosti.</span><span class="sxs-lookup"><span data-stu-id="f7510-227">If you specify that a method is an async method by using the [async](../../../language-reference/keywords/async.md) modifier, you enable the following two capabilities.</span></span>

- <span data-ttu-id="f7510-228">Označená asynchronní metoda může použít [operátor await](../../../language-reference/operators/await.md) k určení bodů zavěšení.</span><span class="sxs-lookup"><span data-stu-id="f7510-228">The marked async method can use [await](../../../language-reference/operators/await.md) to designate suspension points.</span></span> <span data-ttu-id="f7510-229">`await`Operátor instruuje kompilátor, že asynchronní metoda nemůže pokračovat za tento bod, dokud není dokončen očekávaný asynchronní proces.</span><span class="sxs-lookup"><span data-stu-id="f7510-229">The `await` operator tells the compiler that the async method can't continue past that point until the awaited asynchronous process is complete.</span></span> <span data-ttu-id="f7510-230">Během této doby se ovládací prvek vrátí volajícímu asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="f7510-230">In the meantime, control returns to the caller of the async method.</span></span>

     <span data-ttu-id="f7510-231">Pozastavení asynchronní metody ve `await` výrazu nepředstavuje příkaz exit z metody a `finally` bloky nejsou spuštěny.</span><span class="sxs-lookup"><span data-stu-id="f7510-231">The suspension of an async method at an `await` expression doesn't constitute an exit from the method, and `finally` blocks don't run.</span></span>

- <span data-ttu-id="f7510-232">Samotná označená asynchronní metoda může být očekávána metodami, které ji volaly.</span><span class="sxs-lookup"><span data-stu-id="f7510-232">The marked async method can itself be awaited by methods that call it.</span></span>

<span data-ttu-id="f7510-233">Asynchronní metoda obvykle obsahuje jeden nebo více výskytů `await` operátoru, ale absence `await` výrazů nezpůsobí chybu kompilátoru.</span><span class="sxs-lookup"><span data-stu-id="f7510-233">An async method typically contains one or more occurrences of an `await` operator, but the absence of `await` expressions doesn't cause a compiler error.</span></span> <span data-ttu-id="f7510-234">Pokud asynchronní metoda nepoužívá `await` operátor k označení bodu pozastavení, metoda se spustí jako synchronní metoda bez ohledu na `async` modifikátor.</span><span class="sxs-lookup"><span data-stu-id="f7510-234">If an async method doesn't use an `await` operator to mark a suspension point, the method executes as a synchronous method does, despite the `async` modifier.</span></span> <span data-ttu-id="f7510-235">Kompilátor u takových metod zahlásí upozornění.</span><span class="sxs-lookup"><span data-stu-id="f7510-235">The compiler issues a warning for such methods.</span></span>

<span data-ttu-id="f7510-236">`async` a `await` jsou kontextová klíčová slova.</span><span class="sxs-lookup"><span data-stu-id="f7510-236">`async` and `await` are contextual keywords.</span></span> <span data-ttu-id="f7510-237">Další informace a příklady naleznete v následujících tématech:</span><span class="sxs-lookup"><span data-stu-id="f7510-237">For more information and examples, see the following topics:</span></span>

- [<span data-ttu-id="f7510-238">async</span><span class="sxs-lookup"><span data-stu-id="f7510-238">async</span></span>](../../../language-reference/keywords/async.md)
- [<span data-ttu-id="f7510-239">await</span><span class="sxs-lookup"><span data-stu-id="f7510-239">await</span></span>](../../../language-reference/operators/await.md)

## <a name="return-types-and-parameters"></a><a name="BKMK_ReturnTypesandParameters"></a> <span data-ttu-id="f7510-240">Návratové typy a parametry</span><span class="sxs-lookup"><span data-stu-id="f7510-240">Return types and parameters</span></span>

<span data-ttu-id="f7510-241">Asynchronní metoda obvykle vrací <xref:System.Threading.Tasks.Task> nebo <xref:System.Threading.Tasks.Task%601> .</span><span class="sxs-lookup"><span data-stu-id="f7510-241">An async method typically returns a <xref:System.Threading.Tasks.Task> or a <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="f7510-242">V rámci asynchronní metody `await` je operátor použit pro úkol, který je vrácen z volání jiné asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="f7510-242">Inside an async method, an `await` operator is applied to a task that's returned from a call to another async method.</span></span>

<span data-ttu-id="f7510-243">Zadáte <xref:System.Threading.Tasks.Task%601> jako návratový typ, pokud metoda obsahuje [`return`](../../../language-reference/keywords/return.md) příkaz, který určuje operand typu `TResult` .</span><span class="sxs-lookup"><span data-stu-id="f7510-243">You specify <xref:System.Threading.Tasks.Task%601> as the return type if the method contains a [`return`](../../../language-reference/keywords/return.md) statement that specifies an operand of type `TResult`.</span></span>

<span data-ttu-id="f7510-244">Použijete <xref:System.Threading.Tasks.Task>  jako návratový typ, pokud metoda nemá žádný návratový příkaz nebo má návratový příkaz, který nevrací operand.</span><span class="sxs-lookup"><span data-stu-id="f7510-244">You use <xref:System.Threading.Tasks.Task>  as the return type if the method has no return statement or has a return statement that doesn't return an operand.</span></span>

<span data-ttu-id="f7510-245">Počínaje jazykem C# 7,0 můžete také zadat jakýkoli jiný návratový typ za předpokladu, že typ obsahuje `GetAwaiter` metodu.</span><span class="sxs-lookup"><span data-stu-id="f7510-245">Starting with C# 7.0, you can also specify any other return type, provided that the type includes a `GetAwaiter` method.</span></span> <span data-ttu-id="f7510-246"><xref:System.Threading.Tasks.ValueTask%601> je příkladem takového typu.</span><span class="sxs-lookup"><span data-stu-id="f7510-246"><xref:System.Threading.Tasks.ValueTask%601> is an example of such a type.</span></span> <span data-ttu-id="f7510-247">Je k dispozici v balíčku NuGet [System. Threading. Tasks. Extension](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) .</span><span class="sxs-lookup"><span data-stu-id="f7510-247">It is available in the [System.Threading.Tasks.Extension](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) NuGet package.</span></span>

<span data-ttu-id="f7510-248">Následující příklad ukazuje, jak deklarovat a volat metodu, která vrací <xref:System.Threading.Tasks.Task%601> nebo a <xref:System.Threading.Tasks.Task> :</span><span class="sxs-lookup"><span data-stu-id="f7510-248">The following example shows how you declare and call a method that returns a <xref:System.Threading.Tasks.Task%601> or a <xref:System.Threading.Tasks.Task>:</span></span>

```csharp
async Task<int> GetTaskOfTResultAsync()
{
    int hours = 0;
    await Task.Delay(0);

    return hours;
}


Task<int> returnedTaskTResult = GetTaskOfTResultAsync();
int intResult = await returnedTaskTResult;
// Single line
// int intResult = await GetTaskOfTResultAsync();

async Task GetTaskAsync()
{
    await Task.Delay(0);
    // No return statement needed
}

Task returnedTask = GetTaskAsync();
await returnedTask;
// Single line
await GetTaskAsync();
```

<span data-ttu-id="f7510-249">Každá vrácená úloha představuje probíhající práci.</span><span class="sxs-lookup"><span data-stu-id="f7510-249">Each returned task represents ongoing work.</span></span> <span data-ttu-id="f7510-250">Úloha zapouzdřuje informace o stavu asynchronního procesu a posléze buď konečný výsledek z procesu, nebo výjimku, kterou proces vyvolá v případě neúspěchu.</span><span class="sxs-lookup"><span data-stu-id="f7510-250">A task encapsulates information about the state of the asynchronous process and, eventually, either the final result from the process or the exception that the process raises if it doesn't succeed.</span></span>

<span data-ttu-id="f7510-251">Asynchronní metoda může mít také `void` návratový typ.</span><span class="sxs-lookup"><span data-stu-id="f7510-251">An async method can also have a `void` return type.</span></span> <span data-ttu-id="f7510-252">Tento návratový typ slouží primárně k definování obslužných rutin událostí, kde `void` je požadován návratový typ.</span><span class="sxs-lookup"><span data-stu-id="f7510-252">This return type is used primarily to define event handlers, where a `void` return type is required.</span></span> <span data-ttu-id="f7510-253">Asynchronní obslužné rutiny událostí často slouží jako výchozí bod pro asynchronní programy.</span><span class="sxs-lookup"><span data-stu-id="f7510-253">Async event handlers often serve as the starting point for async programs.</span></span>

<span data-ttu-id="f7510-254">Asynchronní metoda, která má `void` návratový typ, nemůže být očekávána a volající metody vracející typ void nemůže zachytit žádné výjimky, které metoda vyvolá.</span><span class="sxs-lookup"><span data-stu-id="f7510-254">An async method that has a `void` return type can't be awaited, and the caller of a void-returning method can't catch any exceptions that the method throws.</span></span>

<span data-ttu-id="f7510-255">Asynchronní metoda [nemůže deklarovat parametry](../../../language-reference/keywords/in-parameter-modifier.md), [ref](../../../language-reference/keywords/ref.md) nebo [out](../../../language-reference/keywords/out-parameter-modifier.md) , ale metoda může volat metody, které mají tyto parametry.</span><span class="sxs-lookup"><span data-stu-id="f7510-255">An async method can't declare [in](../../../language-reference/keywords/in-parameter-modifier.md), [ref](../../../language-reference/keywords/ref.md) or [out](../../../language-reference/keywords/out-parameter-modifier.md) parameters, but the method can call methods that have such parameters.</span></span> <span data-ttu-id="f7510-256">Podobně asynchronní metoda nemůže vrátit hodnotu odkazem, přestože může volat metody s návratovými hodnotami ref.</span><span class="sxs-lookup"><span data-stu-id="f7510-256">Similarly, an async method can't return a value by reference, although it can call methods with ref return values.</span></span>

<span data-ttu-id="f7510-257">Další informace a příklady naleznete v tématu [Async Return Types (C#)](async-return-types.md).</span><span class="sxs-lookup"><span data-stu-id="f7510-257">For more information and examples, see [Async return types (C#)](async-return-types.md).</span></span> <span data-ttu-id="f7510-258">Další informace o tom, jak zachytit výjimky v asynchronních metodách, naleznete v tématu [try-catch](../../../language-reference/keywords/try-catch.md).</span><span class="sxs-lookup"><span data-stu-id="f7510-258">For more information about how to catch exceptions in async methods, see [try-catch](../../../language-reference/keywords/try-catch.md).</span></span>

<span data-ttu-id="f7510-259">Asynchronní rozhraní API v prostředí Windows Runtime programování mají jeden z následujících návratových typů, které jsou podobné úlohám:</span><span class="sxs-lookup"><span data-stu-id="f7510-259">Asynchronous APIs in Windows Runtime programming have one of the following return types, which are similar to tasks:</span></span>

- <span data-ttu-id="f7510-260"><xref:Windows.Foundation.IAsyncOperation%601>, který odpovídá <xref:System.Threading.Tasks.Task%601></span><span class="sxs-lookup"><span data-stu-id="f7510-260"><xref:Windows.Foundation.IAsyncOperation%601>, which corresponds to <xref:System.Threading.Tasks.Task%601></span></span>
- <span data-ttu-id="f7510-261"><xref:Windows.Foundation.IAsyncAction>, který odpovídá <xref:System.Threading.Tasks.Task></span><span class="sxs-lookup"><span data-stu-id="f7510-261"><xref:Windows.Foundation.IAsyncAction>, which corresponds to <xref:System.Threading.Tasks.Task></span></span>
- <xref:Windows.Foundation.IAsyncActionWithProgress%601>
- <xref:Windows.Foundation.IAsyncOperationWithProgress%602>

## <a name="naming-convention"></a><a name="BKMK_NamingConvention"></a> <span data-ttu-id="f7510-262">Konvence pojmenování</span><span class="sxs-lookup"><span data-stu-id="f7510-262">Naming convention</span></span>

<span data-ttu-id="f7510-263">Podle konvence metody, které vracejí běžně čekajíelné typy (například, `Task` `Task<T>` , `ValueTask` ,), `ValueTask<T>` by měly mít názvy končící na "Async".</span><span class="sxs-lookup"><span data-stu-id="f7510-263">By convention, methods that return commonly awaitable types (for example, `Task`, `Task<T>`, `ValueTask`, `ValueTask<T>`) should have names that end with "Async".</span></span> <span data-ttu-id="f7510-264">Metody, které spouštějí asynchronní operaci, ale nevracejí očekávaný typ, by neměly mít názvy, které končí na "Async", ale mohou začínat na "begin", "Start" nebo některé jiné operace, které tuto metodu navrhují, nevrátí ani nevyvolávají výsledek operace.</span><span class="sxs-lookup"><span data-stu-id="f7510-264">Methods that start an asynchronous operation but do not return an awaitable type should not have names that end with "Async", but may start with "Begin", "Start", or some other verb to suggest this method does not return or throw the result of the operation.</span></span>

<span data-ttu-id="f7510-265">Můžete ignorovat konvenci, kde událost, základní třída a rozhraní smlouvy navrhují odlišný název.</span><span class="sxs-lookup"><span data-stu-id="f7510-265">You can ignore the convention where an event, base class, or interface contract suggests a different name.</span></span> <span data-ttu-id="f7510-266">Například byste neměli přejmenovat běžné obslužné rutiny událostí, jako je například `OnButtonClick` .</span><span class="sxs-lookup"><span data-stu-id="f7510-266">For example, you shouldn't rename common event handlers, such as `OnButtonClick`.</span></span>

## <a name="related-topics-and-samples-visual-studio"></a><a name="BKMK_RelatedTopics"></a> <span data-ttu-id="f7510-267">Související témata a ukázky (Visual Studio)</span><span class="sxs-lookup"><span data-stu-id="f7510-267">Related topics and samples (Visual Studio)</span></span>

| <span data-ttu-id="f7510-268">Nadpis</span><span class="sxs-lookup"><span data-stu-id="f7510-268">Title</span></span> | <span data-ttu-id="f7510-269">Popis</span><span class="sxs-lookup"><span data-stu-id="f7510-269">Description</span></span> | <span data-ttu-id="f7510-270">Ukázka</span><span class="sxs-lookup"><span data-stu-id="f7510-270">Sample</span></span> |
|--|--|--|
| [<span data-ttu-id="f7510-271">Jak zajistit paralelní více webových požadavků pomocí modifikátoru Async a operátoru Await (C#)</span><span class="sxs-lookup"><span data-stu-id="f7510-271">How to make multiple web requests in parallel by using async and await (C#)</span></span>](how-to-make-multiple-web-requests-in-parallel-by-using-async-and-await.md) | <span data-ttu-id="f7510-272">Ukazuje, jak spustit několik úloh současně.</span><span class="sxs-lookup"><span data-stu-id="f7510-272">Demonstrates how to start several tasks at the same time.</span></span> | [<span data-ttu-id="f7510-273">Asynchronní vzorek: paralelní provádění více webových požadavků</span><span class="sxs-lookup"><span data-stu-id="f7510-273">Async Sample: Make Multiple Web Requests in Parallel</span></span>](https://code.msdn.microsoft.com/Async-Make-Multiple-Web-49adb82e) |
| [<span data-ttu-id="f7510-274">Asynchronní návratové typy (C#)</span><span class="sxs-lookup"><span data-stu-id="f7510-274">Async return types (C#)</span></span>](async-return-types.md) | <span data-ttu-id="f7510-275">Znázorňuje typy, které mohou vracet asynchronní metody, a vysvětluje, kdy je každý typ vhodný.</span><span class="sxs-lookup"><span data-stu-id="f7510-275">Illustrates the types that async methods can return, and explains when each type is appropriate.</span></span> |  |
| <span data-ttu-id="f7510-276">Zruší úkoly s tokenem zrušení jako mechanismus signalizace.</span><span class="sxs-lookup"><span data-stu-id="f7510-276">Cancel tasks with a cancellation token as a signaling mechanism.</span></span> | <span data-ttu-id="f7510-277">Ukazuje, jak přidat k asynchronnímu řešení následující funkce:</span><span class="sxs-lookup"><span data-stu-id="f7510-277">Shows how to add the following functionality to your async solution:</span></span><br><br> <span data-ttu-id="f7510-278">- [Zrušení seznamu úkolů (C#)](cancel-an-async-task-or-a-list-of-tasks.md)</span><span class="sxs-lookup"><span data-stu-id="f7510-278">- [Cancel a list of tasks (C#)](cancel-an-async-task-or-a-list-of-tasks.md)</span></span><br><span data-ttu-id="f7510-279">- [Zrušení úloh po určitém časovém intervalu (C#)](cancel-async-tasks-after-a-period-of-time.md)</span><span class="sxs-lookup"><span data-stu-id="f7510-279">- [Cancel tasks after a period of time (C#)](cancel-async-tasks-after-a-period-of-time.md)</span></span><br><span data-ttu-id="f7510-280">- [Zpracovat asynchronní úlohu po dokončení (C#)](start-multiple-async-tasks-and-process-them-as-they-complete.md)</span><span class="sxs-lookup"><span data-stu-id="f7510-280">- [Process asynchronous task as they complete (C#)](start-multiple-async-tasks-and-process-them-as-they-complete.md)</span></span> |  |
| [<span data-ttu-id="f7510-281">Použití Async pro přístup k souborům (C#)</span><span class="sxs-lookup"><span data-stu-id="f7510-281">Using async for file access (C#)</span></span>](using-async-for-file-access.md) | <span data-ttu-id="f7510-282">Seznam a ukázka výhod použití operátorů async a await při přístupu k souborům.</span><span class="sxs-lookup"><span data-stu-id="f7510-282">Lists and demonstrates the benefits of using async and await to access files.</span></span> |  |
| [<span data-ttu-id="f7510-283">Asynchronní vzor založený na úlohách (klepnutím)</span><span class="sxs-lookup"><span data-stu-id="f7510-283">Task-based asynchronous pattern (TAP)</span></span>](../../../../standard/asynchronous-programming-patterns/task-based-asynchronous-pattern-tap.md) | <span data-ttu-id="f7510-284">Popisuje asynchronní vzorek, je vzor založen na <xref:System.Threading.Tasks.Task> <xref:System.Threading.Tasks.Task%601> typech a.</span><span class="sxs-lookup"><span data-stu-id="f7510-284">Describes an asynchronous pattern, the pattern is based on the <xref:System.Threading.Tasks.Task> and <xref:System.Threading.Tasks.Task%601> types.</span></span> |  |
| [<span data-ttu-id="f7510-285">Asynchronní videa na Channel 9</span><span class="sxs-lookup"><span data-stu-id="f7510-285">Async Videos on Channel 9</span></span>](https://channel9.msdn.com/search?term=async%20&type=All#pubDate=year&ch9Search&lang-en=en) | <span data-ttu-id="f7510-286">Poskytuje odkazy na různá videa o asynchronním programování.</span><span class="sxs-lookup"><span data-stu-id="f7510-286">Provides links to a variety of videos about async programming.</span></span> |  |

## <a name="see-also"></a><span data-ttu-id="f7510-287">Viz také</span><span class="sxs-lookup"><span data-stu-id="f7510-287">See also</span></span>

- [<span data-ttu-id="f7510-288">async</span><span class="sxs-lookup"><span data-stu-id="f7510-288">async</span></span>](../../../language-reference/keywords/async.md)
- [<span data-ttu-id="f7510-289">await</span><span class="sxs-lookup"><span data-stu-id="f7510-289">await</span></span>](../../../language-reference/operators/await.md)
- [<span data-ttu-id="f7510-290">Asynchronní programování</span><span class="sxs-lookup"><span data-stu-id="f7510-290">Asynchronous programming</span></span>](../../../async.md)
- [<span data-ttu-id="f7510-291">Asynchronní přehled</span><span class="sxs-lookup"><span data-stu-id="f7510-291">Async overview</span></span>](../../../../standard/async.md)
