---
title: Asynchronní návratové typy (C#)
ms.date: 04/14/2020
ms.assetid: ddb2539c-c898-48c1-ad92-245e4a996df8
ms.openlocfilehash: 73a6e1924652c8635377547e2faddc864ac5540a
ms.sourcegitcommit: c91110ef6ee3fedb591f3d628dc17739c4a7071e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/15/2020
ms.locfileid: "81389136"
---
# <a name="async-return-types-c"></a><span data-ttu-id="a9d4f-102">Asynchronní návratové typy (C#)</span><span class="sxs-lookup"><span data-stu-id="a9d4f-102">Async Return Types (C#)</span></span>

<span data-ttu-id="a9d4f-103">Asynchronní metody mohou mít následující návratové typy:</span><span class="sxs-lookup"><span data-stu-id="a9d4f-103">Async methods can have the following return types:</span></span>

- <span data-ttu-id="a9d4f-104"><xref:System.Threading.Tasks.Task%601>, pro asynchronní metodu, která vrací hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-104"><xref:System.Threading.Tasks.Task%601>, for an async method that returns a value.</span></span>
- <span data-ttu-id="a9d4f-105"><xref:System.Threading.Tasks.Task>, pro asynchronní metodu, která provádí operaci, ale vrací žádnou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-105"><xref:System.Threading.Tasks.Task>, for an async method that performs an operation but returns no value.</span></span>
- <span data-ttu-id="a9d4f-106">`void`, pro obslužnou rutinu události.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-106">`void`, for an event handler.</span></span>
- <span data-ttu-id="a9d4f-107">Počínaje C# 7.0, libovolný typ, `GetAwaiter` který má přístupnou metodu.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-107">Starting with C# 7.0, any type that has an accessible `GetAwaiter` method.</span></span> <span data-ttu-id="a9d4f-108">Objekt vrácený `GetAwaiter` metodou musí <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> implementovat rozhraní.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-108">The object returned by the `GetAwaiter` method must implement the <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> interface.</span></span>
- <span data-ttu-id="a9d4f-109">Počínaje C# 8.0 <xref:System.Collections.Generic.IAsyncEnumerable%601>, pro asynchronní metodu, která vrací *asynchronní datový proud*.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-109">Starting with C# 8.0, <xref:System.Collections.Generic.IAsyncEnumerable%601>, for an async method that returns an *async stream*.</span></span>

<span data-ttu-id="a9d4f-110">Další informace o asynchronních metodách naleznete [v tématu Asynchronní programování s asynchronní a await (C#)](./index.md).</span><span class="sxs-lookup"><span data-stu-id="a9d4f-110">For more information about async methods, see [Asynchronous Programming with async and await (C#)](./index.md).</span></span>  
  
## <a name="tasktresult-return-type"></a><span data-ttu-id="a9d4f-111">Návratový\> typ tresult úkolu\<</span><span class="sxs-lookup"><span data-stu-id="a9d4f-111">Task\<TResult\> Return Type</span></span>  
<span data-ttu-id="a9d4f-112">Návratový <xref:System.Threading.Tasks.Task%601> typ se používá pro asynchronní metodu, která obsahuje příkaz `TResult` [return](../../../language-reference/keywords/return.md) (C#), ve kterém je operand .</span><span class="sxs-lookup"><span data-stu-id="a9d4f-112">The <xref:System.Threading.Tasks.Task%601> return type is used for an async method that contains a [return](../../../language-reference/keywords/return.md) (C#) statement in which the operand is `TResult`.</span></span>  
  
<span data-ttu-id="a9d4f-113">V následujícím příkladu `GetLeisureHours` asynchronní `return` metoda obsahuje příkaz, který vrací celé číslo.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-113">In the following example, the `GetLeisureHours` async method contains a `return` statement that returns an integer.</span></span> <span data-ttu-id="a9d4f-114">Proto deklarace metody musí určit `Task<int>`návratový typ .</span><span class="sxs-lookup"><span data-stu-id="a9d4f-114">Therefore, the method declaration must specify a return type of `Task<int>`.</span></span>  <span data-ttu-id="a9d4f-115">Asynchronní <xref:System.Threading.Tasks.Task.FromResult%2A> metoda je zástupný symbol pro operaci, která vrací řetězec.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-115">The <xref:System.Threading.Tasks.Task.FromResult%2A> async method is a placeholder for an operation that returns a string.</span></span>
  
:::code language="csharp" source="./snippets/async-returns1.cs" id="SnippetFirstExample":::

<span data-ttu-id="a9d4f-116">Když `GetLeisureHours` je volána z v `ShowTodaysInfo` rámci await výraz v metodě, await výraz `leisureHours`načte hodnotu celé číslo `GetLeisureHours` (hodnota ), která je uložena v úloze vrácené metodou.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-116">When `GetLeisureHours` is called from within an await expression in the `ShowTodaysInfo` method, the await expression retrieves the integer value (the value of `leisureHours`) that's stored in the task returned by the `GetLeisureHours` method.</span></span> <span data-ttu-id="a9d4f-117">Další informace o výrazy await naleznete v [tématu await](../../../language-reference/operators/await.md).</span><span class="sxs-lookup"><span data-stu-id="a9d4f-117">For more information about await expressions, see [await](../../../language-reference/operators/await.md).</span></span>  
  
<span data-ttu-id="a9d4f-118">Můžete lépe pochopit, `await` jak načte `Task<T>` výsledek z oddělením volání `GetLeisureHours` `await`od aplikace , jak ukazuje následující kód.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-118">You can better understand how `await` retrieves the result from a `Task<T>` by separating the call to `GetLeisureHours` from the application of `await`, as the following code shows.</span></span> <span data-ttu-id="a9d4f-119">Volání metody, `GetLeisureHours` která není okamžitě očekávána `Task<int>`vrátí , jak byste očekávali od deklarace metody.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-119">A call to method `GetLeisureHours` that isn't immediately awaited returns a `Task<int>`, as you would expect from the declaration of the method.</span></span> <span data-ttu-id="a9d4f-120">Úkol je přiřazen `integerTask` proměnné v příkladu.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-120">The task is assigned to the `integerTask` variable in the example.</span></span> <span data-ttu-id="a9d4f-121">Protože `integerTask` je <xref:System.Threading.Tasks.Task%601>, obsahuje <xref:System.Threading.Tasks.Task%601.Result> vlastnost `TResult`typu .</span><span class="sxs-lookup"><span data-stu-id="a9d4f-121">Because `integerTask` is a <xref:System.Threading.Tasks.Task%601>, it contains a <xref:System.Threading.Tasks.Task%601.Result> property of type `TResult`.</span></span> <span data-ttu-id="a9d4f-122">V tomto `TResult` případě představuje celý typ.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-122">In this case, `TResult` represents an integer type.</span></span> <span data-ttu-id="a9d4f-123">Při `await` použití `integerTask`na , await výraz vyhodnotí <xref:System.Threading.Tasks.Task%601.Result%2A> obsah `integerTask`vlastnosti .</span><span class="sxs-lookup"><span data-stu-id="a9d4f-123">When `await` is applied to `integerTask`, the await expression evaluates to the contents of the <xref:System.Threading.Tasks.Task%601.Result%2A> property of `integerTask`.</span></span> <span data-ttu-id="a9d4f-124">Hodnota je přiřazena `ret` proměnné.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-124">The value is assigned to the `ret` variable.</span></span>  
  
> [!IMPORTANT]
> <span data-ttu-id="a9d4f-125">Vlastnost <xref:System.Threading.Tasks.Task%601.Result%2A> je blokující vlastnost.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-125">The <xref:System.Threading.Tasks.Task%601.Result%2A> property is a blocking property.</span></span> <span data-ttu-id="a9d4f-126">Pokud se pokusíte získat přístup před dokončením úlohy, vlákno, které je aktuálně aktivní, je blokováno, dokud se úloha nedokončí a hodnota nebude k dispozici.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-126">If you try to access it before its task is finished, the thread that's currently active is blocked until the task completes and the value is available.</span></span> <span data-ttu-id="a9d4f-127">Ve většině případů byste měli přistupovat k hodnotě pomocí `await` namísto přístupu k vlastnosti přímo.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-127">In most cases, you should access the value by using `await` instead of accessing the property directly.</span></span> <br/> <span data-ttu-id="a9d4f-128">V předchozím příkladu načetl <xref:System.Threading.Tasks.Task%601.Result%2A> hodnotu vlastnosti blokovat `ShowTodaysInfo` hlavní vlákno tak, aby metoda mohla dokončit provádění před ukončením aplikace.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-128">The previous example retrieved the value of the <xref:System.Threading.Tasks.Task%601.Result%2A> property to block the main thread so that the `ShowTodaysInfo` method could finish execution before the application ended.</span></span>  

:::code language="csharp" source="./snippets/async-returns1a.cs" id="SnippetSecondVersion":::

## <a name="task-return-type"></a><span data-ttu-id="a9d4f-129">Typ vrácení úkolu</span><span class="sxs-lookup"><span data-stu-id="a9d4f-129">Task Return Type</span></span>  
<span data-ttu-id="a9d4f-130">Asynchronní metody, které `return` neobsahují příkaz `return` nebo které obsahují příkaz, který nevrací operand obvykle mají návratový typ <xref:System.Threading.Tasks.Task>.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-130">Async methods that don't contain a `return` statement or that contain a `return` statement that doesn't return an operand usually have a return type of <xref:System.Threading.Tasks.Task>.</span></span> <span data-ttu-id="a9d4f-131">Tyto metody `void` vrátit, pokud jsou spuštěny synchronně.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-131">Such methods return `void` if they run synchronously.</span></span> <span data-ttu-id="a9d4f-132">Pokud použijete <xref:System.Threading.Tasks.Task> návratový typ pro asynchronní metodu, volající metoda může použít `await` operátor k pozastavení dokončení volajícího, dokud nebude ukončena volaná asynchronní metoda.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-132">If you use a <xref:System.Threading.Tasks.Task> return type for an async method, a calling method can use an `await` operator to suspend the caller's completion until the called async method has finished.</span></span>  
  
<span data-ttu-id="a9d4f-133">V následujícím příkladu `WaitAndApologize` asynchronní metoda neobsahuje `return` příkaz, takže <xref:System.Threading.Tasks.Task> metoda vrátí objekt.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-133">In the following example, the `WaitAndApologize` async method doesn't contain a `return` statement, so the method returns a <xref:System.Threading.Tasks.Task> object.</span></span> <span data-ttu-id="a9d4f-134">Vrácení `Task` umožňuje `WaitAndApologize` být očekáván.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-134">Returning a `Task` enables `WaitAndApologize` to be awaited.</span></span> <span data-ttu-id="a9d4f-135">Typ <xref:System.Threading.Tasks.Task> neobsahuje vlastnost, `Result` protože nemá žádnou vrácenou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-135">The <xref:System.Threading.Tasks.Task> type doesn't include a `Result` property because it has no return value.</span></span>  

:::code language="csharp" source="./snippets/async-returns2.cs" id="SnippetTaskReturn":::

<span data-ttu-id="a9d4f-136">`WaitAndApologize`is waited by using await statement instead of await expression, similar to the calling statement for a synchronní void-returning method.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-136">`WaitAndApologize` is awaited by using an await statement instead of an await expression, similar to the calling statement for a synchronous void-returning method.</span></span> <span data-ttu-id="a9d4f-137">Použití operátoru await v tomto případě nevytváří hodnotu.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-137">The application of an await operator in this case doesn't produce a value.</span></span>  
  
<span data-ttu-id="a9d4f-138">Stejně jako <xref:System.Threading.Tasks.Task%601> v předchozím příkladu `WaitAndApologize` můžete oddělit volání od aplikace operátoru await, jak ukazuje následující kód.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-138">As in the previous <xref:System.Threading.Tasks.Task%601> example, you can separate the call to `WaitAndApologize` from the application of an await operator, as the following code shows.</span></span> <span data-ttu-id="a9d4f-139">Mějte však `Task` na paměti, `Result` že a nemá vlastnost a že žádná hodnota `Task`je vytvořena při čekání operátor je použita na .</span><span class="sxs-lookup"><span data-stu-id="a9d4f-139">However, remember that a `Task` doesn't have a `Result` property, and that no value is produced when an await operator is applied to a `Task`.</span></span>  
  
<span data-ttu-id="a9d4f-140">Následující kód odděluje volání `WaitAndApologize` metody od čekání na úkol, který metoda vrátí.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-140">The following code separates calling the `WaitAndApologize` method from awaiting the task that the method returns.</span></span>  

:::code language="csharp" source="./snippets/async-returns2a.cs" id="SnippetAwaitTask":::

## <a name="void-return-type"></a><span data-ttu-id="a9d4f-141">Typ vrácení void</span><span class="sxs-lookup"><span data-stu-id="a9d4f-141">Void return type</span></span>

<span data-ttu-id="a9d4f-142">Návratový `void` typ se používá v obslužných `void` rutinách asynchronních událostí, které vyžadují návratový typ.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-142">You use the `void` return type in asynchronous event handlers, which require a `void` return type.</span></span> <span data-ttu-id="a9d4f-143">Pro metody jiné než obslužné rutiny událostí, <xref:System.Threading.Tasks.Task> které nevracejí hodnotu, `void` byste měli vrátit místo, protože asynchronní metoda, která vrátí nelze čekat.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-143">For methods other than event handlers that don't return a value, you should return a <xref:System.Threading.Tasks.Task> instead, because an async method that returns `void` can't be awaited.</span></span> <span data-ttu-id="a9d4f-144">Každý volající takové metody musí pokračovat v dokončení bez čekání na dokončení volané asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-144">Any caller of such a method must continue to completion without waiting for the called async method to finish.</span></span> <span data-ttu-id="a9d4f-145">Volající musí být nezávislý na všech hodnotách nebo výjimkách, které generuje asynchronní metoda.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-145">The caller must be independent of any values or exceptions that the async method generates.</span></span>  
  
<span data-ttu-id="a9d4f-146">Volající asynchronní metody vracející se za neplatné nemůže zachytit výjimky vyvoláné z metody a takové neošetřené výjimky pravděpodobně způsobí selhání aplikace.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-146">The caller of a void-returning async method can't catch exceptions thrown from the method, and such unhandled exceptions are likely to cause your application to fail.</span></span> <span data-ttu-id="a9d4f-147">Pokud metoda, která <xref:System.Threading.Tasks.Task> <xref:System.Threading.Tasks.Task%601> vrátí nebo vyvolá výjimku, výjimka je uložena v vrácené úloze.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-147">If a method that returns a <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> throws an exception, the exception is stored in the returned task.</span></span> <span data-ttu-id="a9d4f-148">Výjimka je znovu vyvolána, když je úkol očekáván.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-148">The exception is rethrown when the task is awaited.</span></span> <span data-ttu-id="a9d4f-149">Proto se ujistěte, že všechny asynchronní metody, <xref:System.Threading.Tasks.Task> které <xref:System.Threading.Tasks.Task%601> mohou způsobit výjimku má návratový typ nebo, že volání metody jsou očekávány.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-149">Therefore, make sure that any async method that can produce an exception has a return type of <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> and that calls to the method are awaited.</span></span>  
  
<span data-ttu-id="a9d4f-150">Další informace o tom, jak zachytit výjimky v asynchronních metodách, naleznete v části [Výjimky v asynchronních metodách](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) v článku [try-catch.](../../../language-reference/keywords/try-catch.md)</span><span class="sxs-lookup"><span data-stu-id="a9d4f-150">For more information about how to catch exceptions in async methods, see the [Exceptions in Async Methods](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) section of the [try-catch](../../../language-reference/keywords/try-catch.md) article.</span></span>  
  
<span data-ttu-id="a9d4f-151">Následující příklad ukazuje chování obslužné rutiny události asynchronní.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-151">The following example shows the behavior of an async event handler.</span></span> <span data-ttu-id="a9d4f-152">V ukázkovém kódu musí obslužná rutina asynchronní události povědět hlavní vlákno, když skončí.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-152">In the example code, an async event handler must let the main thread know when it finishes.</span></span> <span data-ttu-id="a9d4f-153">Hlavní vlákno pak může čekat na dokončení obslužné rutiny asynchronní události před ukončením programu.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-153">Then the main thread can wait for an async event handler to complete before exiting the program.</span></span>

:::code language="csharp" source="./snippets/async-returns3.cs":::

## <a name="generalized-async-return-types-and-valuetasktresult"></a><span data-ttu-id="a9d4f-154">Generalizované asynchronní návratové\<typy a ValueTask TResult\></span><span class="sxs-lookup"><span data-stu-id="a9d4f-154">Generalized async return types and ValueTask\<TResult\></span></span>

<span data-ttu-id="a9d4f-155">Počínaje C# 7.0 asynchronní metoda může vrátit libovolný `GetAwaiter` typ, který má přístupnou metodu.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-155">Starting with C# 7.0, an async method can return any type that has an accessible `GetAwaiter` method.</span></span>

<span data-ttu-id="a9d4f-156">Vzhledem k tomu, <xref:System.Threading.Tasks.Task> a <xref:System.Threading.Tasks.Task%601> jsou typy odkazů, přidělení paměti v cestě kritické pro výkon, zejména v případě, že přidělení dojít v těsné smyčky, může nepříznivě ovlivnit výkon.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-156">Because <xref:System.Threading.Tasks.Task> and <xref:System.Threading.Tasks.Task%601> are reference types, memory allocation in performance-critical paths, particularly when allocations occur in tight loops, can adversely affect performance.</span></span> <span data-ttu-id="a9d4f-157">Podpora pro generalizované návratové typy znamená, že můžete vrátit zjednodušený typ hodnoty namísto typu odkazu, aby se zabránilo další přidělení paměti.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-157">Support for generalized return types means that you can return a lightweight value type instead of a reference type to avoid additional memory allocations.</span></span>

<span data-ttu-id="a9d4f-158">Rozhraní .NET <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> poskytuje strukturu jako zjednodušenou implementaci zobecněné hodnoty vrácení úloh.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-158">.NET provides the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> structure as a lightweight implementation of a generalized task-returning value.</span></span> <span data-ttu-id="a9d4f-159">Chcete-li <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> použít typ, `System.Threading.Tasks.Extensions` musíte přidat balíček NuGet do projektu.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-159">To use the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> type, you must add the `System.Threading.Tasks.Extensions` NuGet package to your project.</span></span> <span data-ttu-id="a9d4f-160">Následující příklad používá <xref:System.Threading.Tasks.ValueTask%601> strukturu k načtení hodnoty dvou kostky válců.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-160">The following example uses the <xref:System.Threading.Tasks.ValueTask%601> structure to retrieve the value of two dice rolls.</span></span>
  
:::code language="csharp" source="./snippets/async-valuetask.cs":::

## <a name="async-streams-with-iasyncenumerablet"></a><span data-ttu-id="a9d4f-161">Asynchronní datové proudy s\<iAsyncEsnumerable T\></span><span class="sxs-lookup"><span data-stu-id="a9d4f-161">Async streams with IAsyncEnumerable\<T\></span></span>

<span data-ttu-id="a9d4f-162">Počínaje C# 8.0 asynchronní metoda může vrátit *asynchronní datový proud*, reprezentované <xref:System.Collections.Generic.IAsyncEnumerable%601>.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-162">Starting with C# 8.0, an async method may return an *async stream*, represented by <xref:System.Collections.Generic.IAsyncEnumerable%601>.</span></span> <span data-ttu-id="a9d4f-163">Asynchronní datový proud poskytuje způsob, jak vytvořit výčet položek číst z datového proudu, když jsou prvky generovány v blocích s opakovanými asynchronními voláními.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-163">An async stream provides a way to enumerate items read from a stream when elements are generated in chunks with repeated asynchronous calls.</span></span> <span data-ttu-id="a9d4f-164">Následující příklad ukazuje asynchronní metodu, která generuje asynchronní datový proud:</span><span class="sxs-lookup"><span data-stu-id="a9d4f-164">The following example shows an async method that generates an async stream:</span></span>

:::code language="csharp" source="./snippets/AsyncStreams.cs" id="SnippetGenerateAsyncStream":::

<span data-ttu-id="a9d4f-165">Předchozí příklad čte řádky z řetězce asynchronně.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-165">The preceding example reads lines from a string asynchronously.</span></span> <span data-ttu-id="a9d4f-166">Po přečtení každého řádku kód vytvoří výčet každé slovo v řetězci.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-166">Once each line is read, the code enumerates each word in the string.</span></span> <span data-ttu-id="a9d4f-167">Volající by výčet každé slovo `await foreach` pomocí příkazu.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-167">Callers would enumerate each word using the `await foreach` statement.</span></span> <span data-ttu-id="a9d4f-168">Metoda čeká, když potřebuje asynchronně číst další řádek ze zdrojového řetězce.</span><span class="sxs-lookup"><span data-stu-id="a9d4f-168">The method awaits when it needs to asynchronously read the next line from the source string.</span></span>

## <a name="see-also"></a><span data-ttu-id="a9d4f-169">Viz také</span><span class="sxs-lookup"><span data-stu-id="a9d4f-169">See also</span></span>

- <xref:System.Threading.Tasks.Task.FromResult%2A>
- [<span data-ttu-id="a9d4f-170">Návod: Přístup k webu pomocí asynchronní a čeká (C#)</span><span class="sxs-lookup"><span data-stu-id="a9d4f-170">Walkthrough: Accessing the Web by Using async and await (C#)</span></span>](./walkthrough-accessing-the-web-by-using-async-and-await.md)
- [<span data-ttu-id="a9d4f-171">Tok řízení v asynchronních programech (C#)</span><span class="sxs-lookup"><span data-stu-id="a9d4f-171">Control Flow in Async Programs (C#)</span></span>](./control-flow-in-async-programs.md)
- [<span data-ttu-id="a9d4f-172">async</span><span class="sxs-lookup"><span data-stu-id="a9d4f-172">async</span></span>](../../../language-reference/keywords/async.md)
- [<span data-ttu-id="a9d4f-173">await</span><span class="sxs-lookup"><span data-stu-id="a9d4f-173">await</span></span>](../../../language-reference/operators/await.md)
