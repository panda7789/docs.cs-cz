---
title: Asynchronní návratové typy (C#)
ms.date: 05/29/2017
ms.assetid: ddb2539c-c898-48c1-ad92-245e4a996df8
ms.openlocfilehash: 9926fea5308f9088ad924bcc98d8deed319c6300
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/14/2020
ms.locfileid: "79170036"
---
# <a name="async-return-types-c"></a><span data-ttu-id="cbd3a-102">Asynchronní návratové typy (C#)</span><span class="sxs-lookup"><span data-stu-id="cbd3a-102">Async Return Types (C#)</span></span>
<span data-ttu-id="cbd3a-103">Asynchronní metody mohou mít následující návratové typy:</span><span class="sxs-lookup"><span data-stu-id="cbd3a-103">Async methods can have the following return types:</span></span>

- <span data-ttu-id="cbd3a-104"><xref:System.Threading.Tasks.Task%601>, pro asynchronní metodu, která vrací hodnotu.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-104"><xref:System.Threading.Tasks.Task%601>, for an async method that returns a value.</span></span>

- <span data-ttu-id="cbd3a-105"><xref:System.Threading.Tasks.Task>, pro asynchronní metodu, která provádí operaci, ale vrací žádnou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-105"><xref:System.Threading.Tasks.Task>, for an async method that performs an operation but returns no value.</span></span>

- <span data-ttu-id="cbd3a-106">`void`, pro obslužnou rutinu události.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-106">`void`, for an event handler.</span></span>

- <span data-ttu-id="cbd3a-107">Počínaje C# 7.0, libovolný typ, `GetAwaiter` který má přístupnou metodu.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-107">Starting with C# 7.0, any type that has an accessible `GetAwaiter` method.</span></span> <span data-ttu-id="cbd3a-108">Objekt vrácený `GetAwaiter` metodou musí <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> implementovat rozhraní.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-108">The object returned by the `GetAwaiter` method must implement the <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> interface.</span></span>
  
<span data-ttu-id="cbd3a-109">Další informace o asynchronních metodách naleznete [v tématu Asynchronní programování s asynchronní a await (C#)](./index.md).</span><span class="sxs-lookup"><span data-stu-id="cbd3a-109">For more information about async methods, see [Asynchronous Programming with async and await (C#)](./index.md).</span></span>  
  
<span data-ttu-id="cbd3a-110">Každý návratový typ je zkoumán v jedné z následujících částí a můžete najít úplný příklad, který používá všechny tři typy na konci tématu.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-110">Each return type is examined in one of the following sections, and you can find a full example that uses all three types at the end of the topic.</span></span>  
  
## <a name="BKMK_TaskTReturnType"></a><span data-ttu-id="cbd3a-111">Návratový\> typ tresult úkolu\<</span><span class="sxs-lookup"><span data-stu-id="cbd3a-111">Task\<TResult\> Return Type</span></span>  
<span data-ttu-id="cbd3a-112">Návratový <xref:System.Threading.Tasks.Task%601> typ se používá pro asynchronní metodu, která obsahuje příkaz [return](../../../language-reference/keywords/return.md) (C#), ve kterém má operand typ `TResult`.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-112">The <xref:System.Threading.Tasks.Task%601> return type is used for an async method that contains a [return](../../../language-reference/keywords/return.md) (C#) statement in which the operand has type `TResult`.</span></span>  
  
<span data-ttu-id="cbd3a-113">V následujícím příkladu `GetLeisureHours` asynchronní `return` metoda obsahuje příkaz, který vrací celé číslo.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-113">In the following example, the `GetLeisureHours` async method contains a `return` statement that returns an integer.</span></span> <span data-ttu-id="cbd3a-114">Proto deklarace metody musí určit `Task<int>`návratový typ .</span><span class="sxs-lookup"><span data-stu-id="cbd3a-114">Therefore, the method declaration must specify a return type of `Task<int>`.</span></span>  <span data-ttu-id="cbd3a-115">Asynchronní <xref:System.Threading.Tasks.Task.FromResult%2A> metoda je zástupný symbol pro operaci, která vrací řetězec.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-115">The <xref:System.Threading.Tasks.Task.FromResult%2A> async method is a placeholder for an operation that returns a string.</span></span>
  
[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns1.cs)]

<span data-ttu-id="cbd3a-116">Když `GetLeisureHours` je volána z v `ShowTodaysInfo` rámci await výraz v metodě, await výraz `leisureHours`načte hodnotu celé číslo `GetLeisureHours` (hodnota ), která je uložena v úloze vrácené metodou.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-116">When `GetLeisureHours` is called from within an await expression in the `ShowTodaysInfo` method, the await expression retrieves the integer value (the value of `leisureHours`) that's stored in the task returned by the `GetLeisureHours` method.</span></span> <span data-ttu-id="cbd3a-117">Další informace o výrazy await naleznete v [tématu await](../../../language-reference/operators/await.md).</span><span class="sxs-lookup"><span data-stu-id="cbd3a-117">For more information about await expressions, see [await](../../../language-reference/operators/await.md).</span></span>  
  
<span data-ttu-id="cbd3a-118">Můžete lépe pochopit, jak k tomu `GetLeisureHours` dochází oddělením `await`volání od aplikace , jak ukazuje následující kód.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-118">You can better understand how this happens by separating the call to `GetLeisureHours` from the application of `await`, as the following code shows.</span></span> <span data-ttu-id="cbd3a-119">Volání metody, `GetLeisureHours` která není okamžitě očekávána `Task<int>`vrátí , jak byste očekávali od deklarace metody.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-119">A call to method `GetLeisureHours` that isn't immediately awaited returns a `Task<int>`, as you would expect from the declaration of the method.</span></span> <span data-ttu-id="cbd3a-120">Úkol je přiřazen `integerTask` proměnné v příkladu.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-120">The task is assigned to the `integerTask` variable in the example.</span></span> <span data-ttu-id="cbd3a-121">Protože `integerTask` je <xref:System.Threading.Tasks.Task%601>, obsahuje <xref:System.Threading.Tasks.Task%601.Result> vlastnost `TResult`typu .</span><span class="sxs-lookup"><span data-stu-id="cbd3a-121">Because `integerTask` is a <xref:System.Threading.Tasks.Task%601>, it contains a <xref:System.Threading.Tasks.Task%601.Result> property of type `TResult`.</span></span> <span data-ttu-id="cbd3a-122">V tomto `TResult` případě představuje celý typ.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-122">In this case, `TResult` represents an integer type.</span></span> <span data-ttu-id="cbd3a-123">Při `await` použití `integerTask`na , await výraz vyhodnotí <xref:System.Threading.Tasks.Task%601.Result%2A> obsah `integerTask`vlastnosti .</span><span class="sxs-lookup"><span data-stu-id="cbd3a-123">When `await` is applied to `integerTask`, the await expression evaluates to the contents of the <xref:System.Threading.Tasks.Task%601.Result%2A> property of `integerTask`.</span></span> <span data-ttu-id="cbd3a-124">Hodnota je přiřazena `ret` proměnné.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-124">The value is assigned to the `ret` variable.</span></span>  
  
> [!IMPORTANT]
> <span data-ttu-id="cbd3a-125">Vlastnost <xref:System.Threading.Tasks.Task%601.Result%2A> je blokující vlastnost.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-125">The <xref:System.Threading.Tasks.Task%601.Result%2A> property is a blocking property.</span></span> <span data-ttu-id="cbd3a-126">Pokud se pokusíte získat přístup před dokončením úlohy, vlákno, které je aktuálně aktivní, je blokováno, dokud se úloha nedokončí a hodnota nebude k dispozici.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-126">If you try to access it before its task is finished, the thread that's currently active is blocked until the task completes and the value is available.</span></span> <span data-ttu-id="cbd3a-127">Ve většině případů byste měli přistupovat k hodnotě pomocí `await` namísto přístupu k vlastnosti přímo.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-127">In most cases, you should access the value by using `await` instead of accessing the property directly.</span></span> <br/> <span data-ttu-id="cbd3a-128">V předchozím příkladu načetl <xref:System.Threading.Tasks.Task%601.Result%2A> hodnotu vlastnosti blokovat `ShowTodaysInfo` hlavní vlákno tak, aby metoda mohla dokončit provádění před ukončením aplikace.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-128">The previous example retrieved the value of the <xref:System.Threading.Tasks.Task%601.Result%2A> property to block the main thread so that the `ShowTodaysInfo` method could finish execution before the application ended.</span></span>  

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns1a.cs#1)]
  
## <a name="BKMK_TaskReturnType"></a><span data-ttu-id="cbd3a-129">Typ vrácení úkolu</span><span class="sxs-lookup"><span data-stu-id="cbd3a-129">Task Return Type</span></span>  
<span data-ttu-id="cbd3a-130">Asynchronní metody, které `return` neobsahují příkaz `return` nebo které obsahují příkaz, který nevrací operand obvykle mají návratový typ <xref:System.Threading.Tasks.Task>.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-130">Async methods that don't contain a `return` statement or that contain a `return` statement that doesn't return an operand usually have a return type of <xref:System.Threading.Tasks.Task>.</span></span> <span data-ttu-id="cbd3a-131">Tyto metody `void` vrátit, pokud jsou spuštěny synchronně.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-131">Such methods return `void` if they run synchronously.</span></span> <span data-ttu-id="cbd3a-132">Pokud použijete <xref:System.Threading.Tasks.Task> návratový typ pro asynchronní metodu, volající metoda může použít `await` operátor k pozastavení dokončení volajícího, dokud nebude ukončena volaná asynchronní metoda.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-132">If you use a <xref:System.Threading.Tasks.Task> return type for an async method, a calling method can use an `await` operator to suspend the caller's completion until the called async method has finished.</span></span>  
  
<span data-ttu-id="cbd3a-133">V následujícím příkladu `WaitAndApologize` asynchronní metoda neobsahuje `return` příkaz, takže <xref:System.Threading.Tasks.Task> metoda vrátí objekt.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-133">In the following example, the `WaitAndApologize` async method doesn't contain a `return` statement, so the method returns a <xref:System.Threading.Tasks.Task> object.</span></span> <span data-ttu-id="cbd3a-134">To `WaitAndApologize` umožňuje čekat.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-134">This enables `WaitAndApologize` to be awaited.</span></span> <span data-ttu-id="cbd3a-135">Všimněte <xref:System.Threading.Tasks.Task> si, že typ `Result` neobsahuje vlastnost, protože nemá žádnou vrácenou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-135">Note that the <xref:System.Threading.Tasks.Task> type doesn't include a `Result` property because it has no return value.</span></span>  

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns2.cs)]  
  
<span data-ttu-id="cbd3a-136">`WaitAndApologize`is waited by using await statement instead of await expression, similar to the calling statement for a synchronní void-returning method.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-136">`WaitAndApologize` is awaited by using an await statement instead of an await expression, similar to the calling statement for a synchronous void-returning method.</span></span> <span data-ttu-id="cbd3a-137">Použití operátoru await v tomto případě nevytváří hodnotu.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-137">The application of an await operator in this case doesn't produce a value.</span></span>  
  
<span data-ttu-id="cbd3a-138">Stejně jako <xref:System.Threading.Tasks.Task%601> v předchozím příkladu `WaitAndApologize` můžete oddělit volání od aplikace operátoru await, jak ukazuje následující kód.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-138">As in the previous <xref:System.Threading.Tasks.Task%601> example, you can separate the call to `WaitAndApologize` from the application of an await operator, as the following code shows.</span></span> <span data-ttu-id="cbd3a-139">Mějte však `Task` na paměti, `Result` že a nemá vlastnost a že žádná hodnota `Task`je vytvořena při čekání operátor je použita na .</span><span class="sxs-lookup"><span data-stu-id="cbd3a-139">However, remember that a `Task` doesn't have a `Result` property, and that no value is produced when an await operator is applied to a `Task`.</span></span>  
  
<span data-ttu-id="cbd3a-140">Následující kód odděluje volání `WaitAndApologize` metody od čekání na úkol, který metoda vrátí.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-140">The following code separates calling the `WaitAndApologize` method from awaiting the task that the method returns.</span></span>  

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns2a.cs#1)]  

## <a name="BKMK_VoidReturnType"></a><span data-ttu-id="cbd3a-141">Typ vrácení void</span><span class="sxs-lookup"><span data-stu-id="cbd3a-141">Void return type</span></span>

<span data-ttu-id="cbd3a-142">Návratový `void` typ se používá v obslužných `void` rutinách asynchronních událostí, které vyžadují návratový typ.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-142">You use the `void` return type in asynchronous event handlers, which require a `void` return type.</span></span> <span data-ttu-id="cbd3a-143">Pro metody jiné než obslužné rutiny událostí, <xref:System.Threading.Tasks.Task> které nevracejí hodnotu, `void` byste měli vrátit místo, protože asynchronní metoda, která vrátí nelze čekat.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-143">For methods other than event handlers that don't return a value, you should return a <xref:System.Threading.Tasks.Task> instead, because an async method that returns `void` can't be awaited.</span></span> <span data-ttu-id="cbd3a-144">Každý volající takové metody musí být schopen pokračovat v dokončení bez čekání na dokončení volané asynchronní metody a volající musí být nezávislý na všech hodnotách nebo výjimkách, které generuje asynchronní metoda.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-144">Any caller of such a method must be able to continue to completion without waiting for the called async method to finish, and the caller must be independent of any values or exceptions that the async method generates.</span></span>  
  
<span data-ttu-id="cbd3a-145">Volající asynchronní metody vracející se za neplatné nemůže zachytit výjimky, které jsou vyvolány z metody, a takové neošetřené výjimky pravděpodobně způsobí selhání aplikace.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-145">The caller of a void-returning async method can't catch exceptions that are thrown from the method, and such unhandled exceptions are likely to cause your application to fail.</span></span> <span data-ttu-id="cbd3a-146">Pokud dojde k výjimce v asynchronní metodě, která vrátí <xref:System.Threading.Tasks.Task> a nebo <xref:System.Threading.Tasks.Task%601>, je výjimka uložena v vrácené úloze a je znovu vyvolána při čekání na úlohu.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-146">If an exception occurs in an async method that returns a <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601>, the exception is stored in the returned task and is rethrown when the task is awaited.</span></span> <span data-ttu-id="cbd3a-147">Proto se ujistěte, že všechny asynchronní metody, <xref:System.Threading.Tasks.Task> které <xref:System.Threading.Tasks.Task%601> mohou způsobit výjimku má návratový typ nebo, že volání metody jsou očekávány.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-147">Therefore, make sure that any async method that can produce an exception has a return type of <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> and that calls to the method are awaited.</span></span>  
  
<span data-ttu-id="cbd3a-148">Další informace o tom, jak zachytit výjimky v asynchronních metodách, naleznete v části [Výjimky v asynchronních metodách](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) tématu [try-catch.](../../../language-reference/keywords/try-catch.md)</span><span class="sxs-lookup"><span data-stu-id="cbd3a-148">For more information about how to catch exceptions in async methods, see the [Exceptions in Async Methods](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) section of the [try-catch](../../../language-reference/keywords/try-catch.md) topic.</span></span>  
  
<span data-ttu-id="cbd3a-149">Následující příklad ukazuje chování obslužné rutiny události asynchronní.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-149">The following example shows the behavior of an async event handler.</span></span> <span data-ttu-id="cbd3a-150">Všimněte si, že v ukázkovém kódu musí obslužná rutina asynchronní události povědět hlavní vlákno, když skončí.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-150">Note that in the example code, an async event handler must let the main thread know when it finishes.</span></span> <span data-ttu-id="cbd3a-151">Hlavní vlákno pak může čekat na dokončení obslužné rutiny asynchronní události před ukončením programu.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-151">Then the main thread can wait for an async event handler to complete before exiting the program.</span></span>

[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-returns3.cs)]  

## <a name="generalized-async-return-types-and-valuetasktresult"></a><span data-ttu-id="cbd3a-152">Generalizované asynchronní návratové\<typy a ValueTask TResult\></span><span class="sxs-lookup"><span data-stu-id="cbd3a-152">Generalized async return types and ValueTask\<TResult\></span></span>

<span data-ttu-id="cbd3a-153">Počínaje C# 7.0 asynchronní metoda může vrátit libovolný `GetAwaiter` typ, který má přístupnou metodu.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-153">Starting with C# 7.0, an async method can return any type that has an accessible `GetAwaiter` method.</span></span>

<span data-ttu-id="cbd3a-154">Vzhledem k tomu, <xref:System.Threading.Tasks.Task> a <xref:System.Threading.Tasks.Task%601> jsou typy odkazů, přidělení paměti v cestě kritické pro výkon, zejména v případě, že přidělení dojít v těsné smyčky, může nepříznivě ovlivnit výkon.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-154">Because <xref:System.Threading.Tasks.Task> and <xref:System.Threading.Tasks.Task%601> are reference types, memory allocation in performance-critical paths, particularly when allocations occur in tight loops, can adversely affect performance.</span></span> <span data-ttu-id="cbd3a-155">Podpora pro generalizované návratové typy znamená, že můžete vrátit zjednodušený typ hodnoty namísto typu odkazu, aby se zabránilo další přidělení paměti.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-155">Support for generalized return types means that you can return a lightweight value type instead of a reference type to avoid additional memory allocations.</span></span>

<span data-ttu-id="cbd3a-156">Rozhraní .NET <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> poskytuje strukturu jako zjednodušenou implementaci zobecněné hodnoty vrácení úloh.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-156">.NET provides the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> structure as a lightweight implementation of a generalized task-returning value.</span></span> <span data-ttu-id="cbd3a-157">Chcete-li <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> použít typ, `System.Threading.Tasks.Extensions` musíte přidat balíček NuGet do projektu.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-157">To use the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> type, you must add the `System.Threading.Tasks.Extensions` NuGet package to your project.</span></span> <span data-ttu-id="cbd3a-158">Následující příklad používá <xref:System.Threading.Tasks.ValueTask%601> strukturu k načtení hodnoty dvou kostky válců.</span><span class="sxs-lookup"><span data-stu-id="cbd3a-158">The following example uses the <xref:System.Threading.Tasks.ValueTask%601> structure to retrieve the value of two dice rolls.</span></span>
  
[!code-csharp[return-value](../../../../../samples/snippets/csharp/programming-guide/async/async-valuetask.cs)]

## <a name="see-also"></a><span data-ttu-id="cbd3a-159">Viz také</span><span class="sxs-lookup"><span data-stu-id="cbd3a-159">See also</span></span>

- <xref:System.Threading.Tasks.Task.FromResult%2A>
- [<span data-ttu-id="cbd3a-160">Návod: Přístup k webu pomocí asynchronní a čeká (C#)</span><span class="sxs-lookup"><span data-stu-id="cbd3a-160">Walkthrough: Accessing the Web by Using async and await (C#)</span></span>](./walkthrough-accessing-the-web-by-using-async-and-await.md)
- [<span data-ttu-id="cbd3a-161">Tok řízení v asynchronních programech (C#)</span><span class="sxs-lookup"><span data-stu-id="cbd3a-161">Control Flow in Async Programs (C#)</span></span>](./control-flow-in-async-programs.md)
- [<span data-ttu-id="cbd3a-162">async</span><span class="sxs-lookup"><span data-stu-id="cbd3a-162">async</span></span>](../../../language-reference/keywords/async.md)
- [<span data-ttu-id="cbd3a-163">await</span><span class="sxs-lookup"><span data-stu-id="cbd3a-163">await</span></span>](../../../language-reference/operators/await.md)
