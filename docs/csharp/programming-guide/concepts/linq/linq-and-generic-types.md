---
title: LINQ a obecné typy (C#)
ms.date: 07/20/2015
helpviewer_keywords:
- LINQ [C#], generic types
- generic types [LINQ]
- generics [LINQ]
ms.assetid: 660e3799-25ca-462c-8c4a-8bce04fbb031
ms.openlocfilehash: 8ec2a599a6762d62d101f7660892a6d85a100794
ms.sourcegitcommit: 986f836f72ef10876878bd6217174e41464c145a
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/19/2019
ms.locfileid: "69591937"
---
# <a name="linq-and-generic-types-c"></a><span data-ttu-id="84d69-102">LINQ a obecné typy (C#)</span><span class="sxs-lookup"><span data-stu-id="84d69-102">LINQ and Generic Types (C#)</span></span>
[!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)]<span data-ttu-id="84d69-103">dotazy jsou založeny na obecných typech, které byly představeny ve verzi 2,0 .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="84d69-103">queries are based on generic types, which were introduced in version 2.0 of the .NET Framework.</span></span> <span data-ttu-id="84d69-104">Než budete moct začít psát dotazy, nepotřebujete důkladné znalosti obecných typů.</span><span class="sxs-lookup"><span data-stu-id="84d69-104">You do not need an in-depth knowledge of generics before you can start writing queries.</span></span> <span data-ttu-id="84d69-105">Můžete ale chtít pochopit dvě základní koncepty:</span><span class="sxs-lookup"><span data-stu-id="84d69-105">However, you may want to understand two basic concepts:</span></span>  
  
1. <span data-ttu-id="84d69-106">Při vytváření instance třídy <xref:System.Collections.Generic.List%601>obecné kolekce, jako je, nahraďte "T" typem objektů, které bude seznam obsahovat.</span><span class="sxs-lookup"><span data-stu-id="84d69-106">When you create an instance of a generic collection class such as <xref:System.Collections.Generic.List%601>, you replace the "T" with the type of objects that the list will hold.</span></span> <span data-ttu-id="84d69-107">Například seznam řetězců je vyjádřen jako `List<string>`a `Customer` seznam objektů je vyjádřen jako `List<Customer>`.</span><span class="sxs-lookup"><span data-stu-id="84d69-107">For example, a list of strings is expressed as `List<string>`, and a list of `Customer` objects is expressed as `List<Customer>`.</span></span> <span data-ttu-id="84d69-108">Obecný seznam je silného typu a poskytuje mnoho výhod oproti kolekcím, které ukládají jejich <xref:System.Object>prvky jako.</span><span class="sxs-lookup"><span data-stu-id="84d69-108">A generic list is strongly typed and provides many benefits over collections that store their elements as <xref:System.Object>.</span></span> <span data-ttu-id="84d69-109">Pokud se pokusíte přidat `Customer` `List<string>`do, zobrazí se chyba v době kompilace.</span><span class="sxs-lookup"><span data-stu-id="84d69-109">If you try to add a `Customer` to a `List<string>`, you will get an error at compile time.</span></span> <span data-ttu-id="84d69-110">Je snadné použít obecné kolekce, protože není nutné provádět přetypování typu runtime.</span><span class="sxs-lookup"><span data-stu-id="84d69-110">It is easy to use generic collections because you do not have to perform run-time type-casting.</span></span>  
  
2. <span data-ttu-id="84d69-111"><xref:System.Collections.Generic.IEnumerable%601>je rozhraní, které umožňuje vytvořit výčet tříd obecných kolekcí pomocí `foreach` příkazu.</span><span class="sxs-lookup"><span data-stu-id="84d69-111"><xref:System.Collections.Generic.IEnumerable%601> is the interface that enables generic collection classes to be enumerated by using the `foreach` statement.</span></span> <span data-ttu-id="84d69-112">Třídy obecných kolekcí podporují <xref:System.Collections.Generic.IEnumerable%601> stejné jako neobecné třídy kolekcí, jako je <xref:System.Collections.ArrayList> například <xref:System.Collections.IEnumerable>podpora.</span><span class="sxs-lookup"><span data-stu-id="84d69-112">Generic collection classes support <xref:System.Collections.Generic.IEnumerable%601> just as non-generic collection classes such as <xref:System.Collections.ArrayList> support <xref:System.Collections.IEnumerable>.</span></span>  
  
 <span data-ttu-id="84d69-113">Další informace o obecných typech naleznete v tématu [generické typy](../../generics/index.md).</span><span class="sxs-lookup"><span data-stu-id="84d69-113">For more information about generics, see [Generics](../../generics/index.md).</span></span>  
  
## <a name="ienumerablet-variables-in-linq-queries"></a><span data-ttu-id="84d69-114">Proměnné IEnumerable <\> T v dotazech LINQ</span><span class="sxs-lookup"><span data-stu-id="84d69-114">IEnumerable<T\> variables in LINQ Queries</span></span>  
 [!INCLUDE[vbteclinq](~/includes/vbteclinq-md.md)]<span data-ttu-id="84d69-115">proměnné dotazu jsou zadány <xref:System.Collections.Generic.IEnumerable%601> jako nebo odvozený typ <xref:System.Linq.IQueryable%601>, například.</span><span class="sxs-lookup"><span data-stu-id="84d69-115">query variables are typed as <xref:System.Collections.Generic.IEnumerable%601> or a derived type such as <xref:System.Linq.IQueryable%601>.</span></span> <span data-ttu-id="84d69-116">Když vidíte proměnnou dotazu, která je zapsána `IEnumerable<Customer>`jako, znamená to pouze to, že dotaz, když je spuštěn, vytvoří posloupnost nula nebo více `Customer` objektů.</span><span class="sxs-lookup"><span data-stu-id="84d69-116">When you see a query variable that is typed as `IEnumerable<Customer>`, it just means that the query, when it is executed, will produce a sequence of zero or more `Customer` objects.</span></span>  
  
 [!code-csharp[csLINQGettingStarted#34](~/samples/snippets/csharp/VS_Snippets_VBCSharp/CsLINQGettingStarted/CS/Class1.cs#34)]  
  
 <span data-ttu-id="84d69-117">Další informace najdete v tématu [vztahy typů v operacích dotazu LINQ](./type-relationships-in-linq-query-operations.md).</span><span class="sxs-lookup"><span data-stu-id="84d69-117">For more information, see [Type Relationships in LINQ Query Operations](./type-relationships-in-linq-query-operations.md).</span></span>  
  
## <a name="letting-the-compiler-handle-generic-type-declarations"></a><span data-ttu-id="84d69-118">Umožnění kompilátoru zpracovat deklarace obecných typů</span><span class="sxs-lookup"><span data-stu-id="84d69-118">Letting the Compiler Handle Generic Type Declarations</span></span>  
 <span data-ttu-id="84d69-119">Pokud dáváte přednost, můžete se vyhnout obecné syntaxi pomocí klíčového slova [var](../../../language-reference/keywords/var.md) .</span><span class="sxs-lookup"><span data-stu-id="84d69-119">If you prefer, you can avoid generic syntax by using the [var](../../../language-reference/keywords/var.md) keyword.</span></span> <span data-ttu-id="84d69-120">Klíčové slovo instruuje kompilátor, aby odvodí typ proměnné dotazu tím, že prohlíží zdroj dat zadaný `from` v klauzuli. `var`</span><span class="sxs-lookup"><span data-stu-id="84d69-120">The `var` keyword instructs the compiler to infer the type of a query variable by looking at the data source specified in the `from` clause.</span></span> <span data-ttu-id="84d69-121">Následující příklad vytvoří stejný zkompilovaný kód jako předchozí příklad:</span><span class="sxs-lookup"><span data-stu-id="84d69-121">The following example produces the same compiled code as the previous example:</span></span>  
  
 [!code-csharp[csLINQGettingStarted#35](~/samples/snippets/csharp/VS_Snippets_VBCSharp/CsLINQGettingStarted/CS/Class1.cs#35)]  
  
 <span data-ttu-id="84d69-122">`var` Klíčové slovo je užitečné, pokud je typ proměnné zjevný nebo není-li to důležité pro explicitní určení vnořených obecných typů, například těch, které jsou vytvářeny pomocí skupinových dotazů.</span><span class="sxs-lookup"><span data-stu-id="84d69-122">The `var` keyword is useful when the type of the variable is obvious or when it is not that important to explicitly specify nested generic types such as those that are produced by group queries.</span></span> <span data-ttu-id="84d69-123">Obecně platí, že pokud použijete `var`, je vhodné si uvědomit, že pokud chcete, aby byl váš kód obtížnější číst ostatní.</span><span class="sxs-lookup"><span data-stu-id="84d69-123">In general, we recommend that if you use `var`, realize that it can make your code more difficult for others to read.</span></span> <span data-ttu-id="84d69-124">Další informace naleznete v tématu [implicitně typované lokální proměnné](../../classes-and-structs/implicitly-typed-local-variables.md).</span><span class="sxs-lookup"><span data-stu-id="84d69-124">For more information, see [Implicitly Typed Local Variables](../../classes-and-structs/implicitly-typed-local-variables.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="84d69-125">Viz také:</span><span class="sxs-lookup"><span data-stu-id="84d69-125">See also</span></span>

- [<span data-ttu-id="84d69-126">Obecné typy</span><span class="sxs-lookup"><span data-stu-id="84d69-126">Generics</span></span>](../../generics/index.md)
