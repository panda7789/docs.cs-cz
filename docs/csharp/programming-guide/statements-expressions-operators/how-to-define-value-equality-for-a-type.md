---
title: Definování rovnosti hodnoty pro typ – Průvodce programováním v C#
description: Naučte se definovat rovnost hodnot pro určitý typ. Podívejte se na příklady kódu a zobrazte další dostupné prostředky.
ms.date: 07/20/2015
helpviewer_keywords:
- overriding Equals method [C#]
- object equivalence [C#]
- Equals method [C#], overriding
- value equality [C#]
- equivalence [C#]
ms.assetid: 4084581e-b931-498b-9534-cf7ef5b68690
ms.openlocfilehash: cf4449618c2b57f21855354f2250d41a403b4d57
ms.sourcegitcommit: 552b4b60c094559db9d8178fa74f5bafaece0caf
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/29/2020
ms.locfileid: "87381642"
---
# <a name="how-to-define-value-equality-for-a-type-c-programming-guide"></a><span data-ttu-id="a08db-104">Definování rovnosti hodnoty pro typ (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="a08db-104">How to define value equality for a type (C# Programming Guide)</span></span>

<span data-ttu-id="a08db-105">Při definování třídy nebo struktury se rozhodujete, zda má smysl vytvořit vlastní definici hodnoty rovnosti (nebo ekvivalence) pro daný typ.</span><span class="sxs-lookup"><span data-stu-id="a08db-105">When you define a class or struct, you decide whether it makes sense to create a custom definition of value equality (or equivalence) for the type.</span></span> <span data-ttu-id="a08db-106">Obvykle implementujete rovnost hodnot, pokud se očekává, že se objekty typu přidají do kolekce nějakého řazení, nebo když jejich primární účel ukládá sadu polí nebo vlastností.</span><span class="sxs-lookup"><span data-stu-id="a08db-106">Typically, you implement value equality when objects of the type are expected to be added to a collection of some sort, or when their primary purpose is to store a set of fields or properties.</span></span> <span data-ttu-id="a08db-107">Definici rovnosti hodnot můžete založit na porovnání všech polí a vlastností v typu nebo můžete založit definici u podmnožiny.</span><span class="sxs-lookup"><span data-stu-id="a08db-107">You can base your definition of value equality on a comparison of all the fields and properties in the type, or you can base the definition on a subset.</span></span>

<span data-ttu-id="a08db-108">V obou případech a v obou třídách i strukturách by vaše implementace měla dodržovat pět záruk ekvivalence (pro následující pravidla předpokládají, že `x` `y` a `z` nejsou null):</span><span class="sxs-lookup"><span data-stu-id="a08db-108">In either case, and in both classes and structs, your implementation should follow the five guarantees of equivalence (For the following rules, assume that `x`, `y` and `z` are not null):</span></span>  
  
1. <span data-ttu-id="a08db-109">`x.Equals(x)`Vrátí `true` .</span><span class="sxs-lookup"><span data-stu-id="a08db-109">`x.Equals(x)` returns `true`.</span></span> <span data-ttu-id="a08db-110">Tato vlastnost se nazývá reflexivní.</span><span class="sxs-lookup"><span data-stu-id="a08db-110">This is called the reflexive property.</span></span>  
  
2. <span data-ttu-id="a08db-111">`x.Equals(y)`vrací stejnou hodnotu jako `y.Equals(x)` .</span><span class="sxs-lookup"><span data-stu-id="a08db-111">`x.Equals(y)` returns the same value as `y.Equals(x)`.</span></span> <span data-ttu-id="a08db-112">Tato vlastnost se nazývá symetrická vlastnost.</span><span class="sxs-lookup"><span data-stu-id="a08db-112">This is called the symmetric property.</span></span>  
  
3. <span data-ttu-id="a08db-113">Pokud `(x.Equals(y) && y.Equals(z))` vrátí `true` , vrátí `x.Equals(z)` `true` .</span><span class="sxs-lookup"><span data-stu-id="a08db-113">if `(x.Equals(y) && y.Equals(z))` returns `true`, then `x.Equals(z)` returns `true`.</span></span> <span data-ttu-id="a08db-114">Tato vlastnost se nazývá Transitive.</span><span class="sxs-lookup"><span data-stu-id="a08db-114">This is called the transitive property.</span></span>  
  
4. <span data-ttu-id="a08db-115">Po sobě jdoucí volání `x.Equals(y)` vracet stejnou hodnotu, pokud se objekty odkazované x a y nezmění.</span><span class="sxs-lookup"><span data-stu-id="a08db-115">Successive invocations of `x.Equals(y)` return the same value as long as the objects referenced by x and y are not modified.</span></span>  
  
5. <span data-ttu-id="a08db-116">Libovolná hodnota, která není null, se nerovná hodnotě null.</span><span class="sxs-lookup"><span data-stu-id="a08db-116">Any non-null value is not equal to null.</span></span> <span data-ttu-id="a08db-117">Modul CLR však kontroluje hodnotu null u všech volání metody a vyvolá výjimku, `NullReferenceException` Pokud `this` by měl odkaz hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="a08db-117">However, the CLR checks for null on all method calls and throws a `NullReferenceException` if the `this` reference would be null.</span></span> <span data-ttu-id="a08db-118">Proto `x.Equals(y)` vyvolá výjimku, pokud `x` má hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="a08db-118">Therefore, `x.Equals(y)` throws an exception when `x` is null.</span></span> <span data-ttu-id="a08db-119">Který přerušuje pravidla 1 nebo 2 v závislosti na argumentu `Equals` .</span><span class="sxs-lookup"><span data-stu-id="a08db-119">That breaks rules 1 or 2, depending on the argument to `Equals`.</span></span>

 <span data-ttu-id="a08db-120">Libovolná struktura, kterou definujete, již má výchozí implementaci rovnosti hodnot, kterou dědí z <xref:System.ValueType?displayProperty=nameWithType> přepsání <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> metody.</span><span class="sxs-lookup"><span data-stu-id="a08db-120">Any struct that you define already has a default implementation of value equality that it inherits from the <xref:System.ValueType?displayProperty=nameWithType> override of the <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="a08db-121">Tato implementace používá reflexi k prohlédnutí všech polí a vlastností v typu.</span><span class="sxs-lookup"><span data-stu-id="a08db-121">This implementation uses reflection to examine all the fields and properties in the type.</span></span> <span data-ttu-id="a08db-122">I když tato implementace přináší správné výsledky, je poměrně pomalé ve srovnání s vlastní implementací, kterou zapisujete konkrétně pro daný typ.</span><span class="sxs-lookup"><span data-stu-id="a08db-122">Although this implementation produces correct results, it is relatively slow compared to a custom implementation that you write specifically for the type.</span></span>  
  
 <span data-ttu-id="a08db-123">Podrobnosti implementace pro rovnost hodnot se liší pro třídy a struktury.</span><span class="sxs-lookup"><span data-stu-id="a08db-123">The implementation details for value equality are different for classes and structs.</span></span> <span data-ttu-id="a08db-124">Nicméně třídy a struktury vyžadují stejný základní postup pro implementaci rovnosti:</span><span class="sxs-lookup"><span data-stu-id="a08db-124">However, both classes and structs require the same basic steps for implementing equality:</span></span>  
  
1. <span data-ttu-id="a08db-125">Přepsat [virtuální](../../language-reference/keywords/virtual.md) <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> metodu.</span><span class="sxs-lookup"><span data-stu-id="a08db-125">Override the [virtual](../../language-reference/keywords/virtual.md) <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="a08db-126">Ve většině případů vaše implementace `bool Equals( object obj )` by měla volat pouze do metody specifické pro typ `Equals` , který je implementací <xref:System.IEquatable%601?displayProperty=nameWithType> rozhraní.</span><span class="sxs-lookup"><span data-stu-id="a08db-126">In most cases, your implementation of `bool Equals( object obj )` should just call into the type-specific `Equals` method that is the implementation of the <xref:System.IEquatable%601?displayProperty=nameWithType> interface.</span></span> <span data-ttu-id="a08db-127">(Viz krok 2.)</span><span class="sxs-lookup"><span data-stu-id="a08db-127">(See step 2.)</span></span>  
  
2. <span data-ttu-id="a08db-128">Implementujte <xref:System.IEquatable%601?displayProperty=nameWithType> rozhraní zadáním metody specifické pro typ `Equals` .</span><span class="sxs-lookup"><span data-stu-id="a08db-128">Implement the <xref:System.IEquatable%601?displayProperty=nameWithType> interface by providing a type-specific `Equals` method.</span></span> <span data-ttu-id="a08db-129">Je to místo, kde se provádí skutečné porovnání rovnosti.</span><span class="sxs-lookup"><span data-stu-id="a08db-129">This is where the actual equivalence comparison is performed.</span></span> <span data-ttu-id="a08db-130">Například se můžete rozhodnout definovat rovnost porovnáním pouze jednoho nebo dvou polí v typu.</span><span class="sxs-lookup"><span data-stu-id="a08db-130">For example, you might decide to define equality by comparing only one or two fields in your type.</span></span> <span data-ttu-id="a08db-131">Negenerovat výjimky z `Equals` .</span><span class="sxs-lookup"><span data-stu-id="a08db-131">Do not throw exceptions from `Equals`.</span></span> <span data-ttu-id="a08db-132">Pouze pro třídy: Tato metoda by měla prozkoumávat pouze pole, která jsou deklarována ve třídě.</span><span class="sxs-lookup"><span data-stu-id="a08db-132">For classes only: This method should examine only fields that are declared in the class.</span></span> <span data-ttu-id="a08db-133">Měla by volat `base.Equals` , aby prozkoumala pole, která jsou v základní třídě.</span><span class="sxs-lookup"><span data-stu-id="a08db-133">It should call `base.Equals` to examine fields that are in the base class.</span></span> <span data-ttu-id="a08db-134">(Tuto operaci neprovádějte, pokud typ dědí přímo z <xref:System.Object> , protože <xref:System.Object> implementace <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> provede kontrolu rovnosti referencí.)</span><span class="sxs-lookup"><span data-stu-id="a08db-134">(Do not do this if the type inherits directly from <xref:System.Object>, because the <xref:System.Object> implementation of <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> performs a reference equality check.)</span></span>  
  
3. <span data-ttu-id="a08db-135">Volitelné, ale doporučené: přetížení [==](../../language-reference/operators/equality-operators.md#equality-operator-) operátorů a [! =](../../language-reference/operators/equality-operators.md#inequality-operator-) .</span><span class="sxs-lookup"><span data-stu-id="a08db-135">Optional but recommended: Overload the [==](../../language-reference/operators/equality-operators.md#equality-operator-) and [!=](../../language-reference/operators/equality-operators.md#inequality-operator-) operators.</span></span>  
  
4. <span data-ttu-id="a08db-136">Přepsat <xref:System.Object.GetHashCode%2A?displayProperty=nameWithType> tak, aby dva objekty, které mají rovnost hodnot, vytvořily stejný kód hash.</span><span class="sxs-lookup"><span data-stu-id="a08db-136">Override <xref:System.Object.GetHashCode%2A?displayProperty=nameWithType> so that two objects that have value equality produce the same hash code.</span></span>  
  
5. <span data-ttu-id="a08db-137">Volitelné: pro podporu definic pro "větší než" nebo "menší než", implementujte <xref:System.IComparable%601> rozhraní pro váš typ a také [<=](../../language-reference/operators/comparison-operators.md#less-than-or-equal-operator-) [>=](../../language-reference/operators/comparison-operators.md#greater-than-or-equal-operator-) operátory a.</span><span class="sxs-lookup"><span data-stu-id="a08db-137">Optional: To support definitions for "greater than" or "less than," implement the <xref:System.IComparable%601> interface for your type, and also overload the [<=](../../language-reference/operators/comparison-operators.md#less-than-or-equal-operator-) and [>=](../../language-reference/operators/comparison-operators.md#greater-than-or-equal-operator-) operators.</span></span>  
  
 <span data-ttu-id="a08db-138">První příklad, který následuje, ukazuje implementaci třídy.</span><span class="sxs-lookup"><span data-stu-id="a08db-138">The first example that follows shows a class implementation.</span></span> <span data-ttu-id="a08db-139">Druhý příklad ukazuje implementaci struktury.</span><span class="sxs-lookup"><span data-stu-id="a08db-139">The second example shows a struct implementation.</span></span>  

## <a name="example"></a><span data-ttu-id="a08db-140">Příklad</span><span class="sxs-lookup"><span data-stu-id="a08db-140">Example</span></span>

 <span data-ttu-id="a08db-141">Následující příklad ukazuje, jak implementovat hodnotu rovnosti ve třídě (odkazový typ).</span><span class="sxs-lookup"><span data-stu-id="a08db-141">The following example shows how to implement value equality in a class (reference type).</span></span>  
  
 [!code-csharp[csProgGuideStatements#19](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideStatements/CS/Statements.cs#19)]  
  
 <span data-ttu-id="a08db-142">Ve třídách (odkazových typech) provede výchozí implementace obou <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> metod porovnání rovnosti referencí, nikoli kontrolu rovnosti hodnoty.</span><span class="sxs-lookup"><span data-stu-id="a08db-142">On classes (reference types), the default implementation of both <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> methods performs a reference equality comparison, not a value equality check.</span></span> <span data-ttu-id="a08db-143">Když implementátor přepíše virtuální metodu, účelem je poskytnout sémantiku rovnosti hodnoty.</span><span class="sxs-lookup"><span data-stu-id="a08db-143">When an implementer overrides the virtual method, the purpose is to give it value equality semantics.</span></span>  
  
 <span data-ttu-id="a08db-144">`==`Operátory a `!=` lze použít s třídami i v případě, že třída je přetěžují.</span><span class="sxs-lookup"><span data-stu-id="a08db-144">The `==` and `!=` operators can be used with classes even if the class does not overload them.</span></span> <span data-ttu-id="a08db-145">Výchozí chování je však provést kontrolu rovnosti referencí.</span><span class="sxs-lookup"><span data-stu-id="a08db-145">However, the default behavior is to perform a reference equality check.</span></span> <span data-ttu-id="a08db-146">V případě třídy, pokud jste převedli `Equals` metodu, byste měli přetížit `==` `!=` operátory a, ale není to vyžadováno.</span><span class="sxs-lookup"><span data-stu-id="a08db-146">In a class, if you overload the `Equals` method, you should overload the `==` and `!=` operators, but it is not required.</span></span>  

## <a name="example"></a><span data-ttu-id="a08db-147">Příklad</span><span class="sxs-lookup"><span data-stu-id="a08db-147">Example</span></span>

 <span data-ttu-id="a08db-148">Následující příklad ukazuje, jak implementovat hodnotu rovnosti ve struktuře (typ hodnoty):</span><span class="sxs-lookup"><span data-stu-id="a08db-148">The following example shows how to implement value equality in a struct (value type):</span></span>  
  
 [!code-csharp[csProgGuideStatements#20](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideStatements/CS/Statements.cs#20)]  
  
 <span data-ttu-id="a08db-149">Pro struktury, výchozí implementace <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> (která je přepsaná verze v <xref:System.ValueType?displayProperty=nameWithType> ) provede kontrolu rovnosti hodnoty pomocí reflexe pro porovnání hodnot každého pole v typu.</span><span class="sxs-lookup"><span data-stu-id="a08db-149">For structs, the default implementation of <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> (which is the overridden version in <xref:System.ValueType?displayProperty=nameWithType>) performs a value equality check by using reflection to compare the values of every field in the type.</span></span> <span data-ttu-id="a08db-150">Když implementátor přepíše virtuální `Equals` metodu ve struktuře, účelem je poskytnout efektivnější způsob provedení kontroly rovnosti hodnoty a volitelně založit porovnání na některé podmnožině pole nebo vlastností struktury.</span><span class="sxs-lookup"><span data-stu-id="a08db-150">When an implementer overrides the virtual `Equals` method in a struct, the purpose is to provide a more efficient means of performing the value equality check and optionally to base the comparison on some subset of the struct's field or properties.</span></span>  
  
 <span data-ttu-id="a08db-151">[==](../../language-reference/operators/equality-operators.md#equality-operator-)Operátory and [! =](../../language-reference/operators/equality-operators.md#inequality-operator-) nelze použít pro strukturu, pokud je struktura explicitně nepřetěžuje.</span><span class="sxs-lookup"><span data-stu-id="a08db-151">The [==](../../language-reference/operators/equality-operators.md#equality-operator-) and [!=](../../language-reference/operators/equality-operators.md#inequality-operator-) operators cannot operate on a struct unless the struct explicitly overloads them.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="a08db-152">Viz také:</span><span class="sxs-lookup"><span data-stu-id="a08db-152">See also</span></span>

- [<span data-ttu-id="a08db-153">Porovnání rovnosti</span><span class="sxs-lookup"><span data-stu-id="a08db-153">Equality comparisons</span></span>](equality-comparisons.md)
- [<span data-ttu-id="a08db-154">Průvodce programováním v C#</span><span class="sxs-lookup"><span data-stu-id="a08db-154">C# programming guide</span></span>](../index.md)
