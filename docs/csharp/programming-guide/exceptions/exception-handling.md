---
title: Zpracování výjimek – Průvodce programováním v C#
description: Přečtěte si o zpracování výjimek. Podívejte se na příklady příkazů try-catch, try-finally a try-catch-finally.
ms.date: 07/20/2015
helpviewer_keywords:
- exception handling [C#], about exception handling
- exceptions [C#], handling
ms.assetid: b4e4ecf2-b907-4e58-891f-2563762258e9
ms.openlocfilehash: 8e55b44573c40f594e567fc5a4501689e66c7af4
ms.sourcegitcommit: 6f58a5f75ceeb936f8ee5b786e9adb81a9a3bee9
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/28/2020
ms.locfileid: "87302032"
---
# <a name="exception-handling-c-programming-guide"></a><span data-ttu-id="b9828-104">Zpracování výjimek (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="b9828-104">Exception Handling (C# Programming Guide)</span></span>
<span data-ttu-id="b9828-105">Blok [Try](../../language-reference/keywords/try-catch.md) je používán programátory v jazyce C# k rozdělení kódu, který může být ovlivněn výjimkou.</span><span class="sxs-lookup"><span data-stu-id="b9828-105">A [try](../../language-reference/keywords/try-catch.md) block is used by C# programmers to partition code that might be affected by an exception.</span></span> <span data-ttu-id="b9828-106">Přidružené bloky [catch](../../language-reference/keywords/try-catch.md) se používají ke zpracování všech výsledných výjimek.</span><span class="sxs-lookup"><span data-stu-id="b9828-106">Associated [catch](../../language-reference/keywords/try-catch.md) blocks are used to handle any resulting exceptions.</span></span> <span data-ttu-id="b9828-107">Blok [finally](../../language-reference/keywords/try-finally.md) obsahuje kód, který je spuštěn bez ohledu na to, zda je výjimka vyvolána v `try` bloku, například uvolnění prostředků, které jsou přiděleny v `try` bloku.</span><span class="sxs-lookup"><span data-stu-id="b9828-107">A [finally](../../language-reference/keywords/try-finally.md) block contains code that is run regardless of whether or not an exception is thrown in the `try` block, such as releasing resources that are allocated in the `try` block.</span></span> <span data-ttu-id="b9828-108">`try`Blok vyžaduje jeden nebo více přidružených `catch` bloků, nebo `finally` blok nebo obojí.</span><span class="sxs-lookup"><span data-stu-id="b9828-108">A `try` block requires one or more associated `catch` blocks, or a `finally` block, or both.</span></span>  
  
 <span data-ttu-id="b9828-109">Následující příklady znázorňují `try-catch` příkaz, `try-finally` příkaz a `try-catch-finally` příkaz.</span><span class="sxs-lookup"><span data-stu-id="b9828-109">The following examples show a `try-catch` statement, a `try-finally` statement, and a `try-catch-finally` statement.</span></span>  
  
 [!code-csharp[csProgGuideExceptions#6](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#6)]  
  
 [!code-csharp[csProgGuideExceptions#7](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#7)]  
  
 [!code-csharp[csProgGuideExceptions#8](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#8)]  
  
 <span data-ttu-id="b9828-110">`try`Blok bez `catch` `finally` bloku nebo způsobí chybu kompilátoru.</span><span class="sxs-lookup"><span data-stu-id="b9828-110">A `try` block without a `catch` or `finally` block causes a compiler error.</span></span>  
  
## <a name="catch-blocks"></a><span data-ttu-id="b9828-111">Bloky catch</span><span class="sxs-lookup"><span data-stu-id="b9828-111">Catch Blocks</span></span>  
 <span data-ttu-id="b9828-112">`catch`Blok může určovat typ výjimky, která má být zachycena.</span><span class="sxs-lookup"><span data-stu-id="b9828-112">A `catch` block can specify the type of exception to catch.</span></span> <span data-ttu-id="b9828-113">Specifikace typu se nazývá *filtr výjimek*.</span><span class="sxs-lookup"><span data-stu-id="b9828-113">The type specification is called an *exception filter*.</span></span> <span data-ttu-id="b9828-114">Typ výjimky by měl být odvozen z <xref:System.Exception> .</span><span class="sxs-lookup"><span data-stu-id="b9828-114">The exception type should be derived from <xref:System.Exception>.</span></span> <span data-ttu-id="b9828-115">Obecně nedávejte <xref:System.Exception> jako filtr výjimek, Pokud nevíte, jak zpracovat všechny výjimky, které mohou být vyvolány v `try` bloku nebo jste na konci bloku zahrnuli příkaz [throw](../../language-reference/keywords/throw.md) `catch` .</span><span class="sxs-lookup"><span data-stu-id="b9828-115">In general, do not specify <xref:System.Exception> as the exception filter unless either you know how to handle all exceptions that might be thrown in the `try` block, or you have included a [throw](../../language-reference/keywords/throw.md) statement at the end of your `catch` block.</span></span>  
  
 <span data-ttu-id="b9828-116">Několik `catch` bloků s různými filtry výjimek lze zřetězit dohromady.</span><span class="sxs-lookup"><span data-stu-id="b9828-116">Multiple `catch` blocks with different exception filters can be chained together.</span></span> <span data-ttu-id="b9828-117">`catch`Bloky jsou vyhodnocovány shora dolů ve vašem kódu, ale `catch` pro každou vyvolanou výjimku je spuštěn pouze jeden blok.</span><span class="sxs-lookup"><span data-stu-id="b9828-117">The `catch` blocks are evaluated from top to bottom in your code, but only one `catch` block is executed for each exception that is thrown.</span></span> <span data-ttu-id="b9828-118">`catch`Je proveden první blok, který určuje přesný typ nebo základní třídu vyvolané výjimky.</span><span class="sxs-lookup"><span data-stu-id="b9828-118">The first `catch` block that specifies the exact type or a base class of the thrown exception is executed.</span></span> <span data-ttu-id="b9828-119">Pokud žádný `catch` blok neurčuje odpovídající filtr výjimek, `catch` je vybrán blok, který nemá filtr, pokud je přítomný v příkazu.</span><span class="sxs-lookup"><span data-stu-id="b9828-119">If no `catch` block specifies a matching exception filter, a `catch` block that does not have a filter is selected, if one is present in the statement.</span></span> <span data-ttu-id="b9828-120">Je důležité umístit bloky s nejpřesnější `catch` (tj. nejvíce odvozené) typy výjimek.</span><span class="sxs-lookup"><span data-stu-id="b9828-120">It is important to position `catch` blocks with the most specific (that is, the most derived) exception types first.</span></span>  
  
 <span data-ttu-id="b9828-121">Výjimky byste měli zachytit, pokud jsou splněné následující podmínky:</span><span class="sxs-lookup"><span data-stu-id="b9828-121">You should catch exceptions when the following conditions are true:</span></span>  
  
- <span data-ttu-id="b9828-122">Máte dobrý přehled o tom, proč může být výjimka vyvolána, a můžete implementovat konkrétní obnovení, jako je například zobrazení výzvy uživateli při zachycení objektu zadání nového názvu souboru <xref:System.IO.FileNotFoundException> .</span><span class="sxs-lookup"><span data-stu-id="b9828-122">You have a good understanding of why the exception might be thrown, and you can implement a specific recovery, such as prompting the user to enter a new file name when you catch a <xref:System.IO.FileNotFoundException> object.</span></span>  
  
- <span data-ttu-id="b9828-123">Můžete vytvořit a vyvolat novou, konkrétnější výjimku.</span><span class="sxs-lookup"><span data-stu-id="b9828-123">You can create and throw a new, more specific exception.</span></span>  
  
     [!code-csharp[csProgGuideExceptions#9](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#9)]  
  
- <span data-ttu-id="b9828-124">Chcete výjimku částečně zpracovat před tím, než ji předáte pro další zpracování.</span><span class="sxs-lookup"><span data-stu-id="b9828-124">You want to partially handle an exception before passing it on for additional handling.</span></span> <span data-ttu-id="b9828-125">V následujícím příkladu `catch` je blok použit k přidání položky do protokolu chyb před opakovanou vyvolání výjimky.</span><span class="sxs-lookup"><span data-stu-id="b9828-125">In the following example, a `catch` block is used to add an entry to an error log before re-throwing the exception.</span></span>  
  
     [!code-csharp[csProgGuideExceptions#10](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#10)]  
  
## <a name="finally-blocks"></a><span data-ttu-id="b9828-126">Bloky finally</span><span class="sxs-lookup"><span data-stu-id="b9828-126">Finally Blocks</span></span>  
 <span data-ttu-id="b9828-127">`finally`Blok umožňuje vyčistit akce, které se provádí v `try` bloku.</span><span class="sxs-lookup"><span data-stu-id="b9828-127">A `finally` block enables you to clean up actions that are performed in a `try` block.</span></span> <span data-ttu-id="b9828-128">Je-li k dispozici, `finally` blok se spustí jako poslední, za `try` blok a libovolným odpovídajícím `catch` blokem.</span><span class="sxs-lookup"><span data-stu-id="b9828-128">If present, the `finally` block executes last, after the `try` block and any matched `catch` block.</span></span> <span data-ttu-id="b9828-129">`finally`Vždy se spustí blok bez ohledu na to, zda je vyvolána výjimka, nebo `catch` je nalezen blok, který odpovídá typu výjimky.</span><span class="sxs-lookup"><span data-stu-id="b9828-129">A `finally` block always runs, regardless of whether an exception is thrown or a `catch` block matching the exception type is found.</span></span>  
  
 <span data-ttu-id="b9828-130">`finally`Blok lze použít k uvolnění prostředků, například datových proudů, připojení k databázi a grafických popisovačů, aniž byste čekali na uvolňování paměti v modulu runtime k finalizaci objektů.</span><span class="sxs-lookup"><span data-stu-id="b9828-130">The `finally` block can be used to release resources such as file streams, database connections, and graphics handles without waiting for the garbage collector in the runtime to finalize the objects.</span></span> <span data-ttu-id="b9828-131">Další informace najdete v tématu [použití příkazu Using](../../language-reference/keywords/using-statement.md) .</span><span class="sxs-lookup"><span data-stu-id="b9828-131">See [using Statement](../../language-reference/keywords/using-statement.md) for more information.</span></span>  
  
 <span data-ttu-id="b9828-132">V následujícím příkladu `finally` je blok použit k zavření souboru, který je otevřen v `try` bloku.</span><span class="sxs-lookup"><span data-stu-id="b9828-132">In the following example, the `finally` block is used to close a file that is opened in the `try` block.</span></span> <span data-ttu-id="b9828-133">Všimněte si, že před zavřením souboru je zkontrolován stav popisovače souboru.</span><span class="sxs-lookup"><span data-stu-id="b9828-133">Notice that the state of the file handle is checked before the file is closed.</span></span> <span data-ttu-id="b9828-134">Pokud `try` blok nemůže otevřít soubor, popisovač souboru má stále hodnotu `null` a `finally` blok se nepokusí zavřít.</span><span class="sxs-lookup"><span data-stu-id="b9828-134">If the `try` block cannot open the file, the file handle still has the value `null` and the `finally` block does not try to close it.</span></span> <span data-ttu-id="b9828-135">Případně, pokud je soubor v bloku otevřený úspěšně `try` , `finally` blok zavře otevřený soubor.</span><span class="sxs-lookup"><span data-stu-id="b9828-135">Alternatively, if the file is opened successfully in the `try` block, the `finally` block closes the open file.</span></span>  
  
 [!code-csharp[csProgGuideExceptions#11](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#11)]  
  
## <a name="c-language-specification"></a><span data-ttu-id="b9828-136">Specifikace jazyka C#</span><span class="sxs-lookup"><span data-stu-id="b9828-136">C# Language Specification</span></span>  

<span data-ttu-id="b9828-137">Další informace naleznete v tématu [výjimky](~/_csharplang/spec/exceptions.md) a [příkaz try](~/_csharplang/spec/statements.md#the-try-statement) ve [specifikaci jazyka C#](/dotnet/csharp/language-reference/language-specification/introduction).</span><span class="sxs-lookup"><span data-stu-id="b9828-137">For more information, see [Exceptions](~/_csharplang/spec/exceptions.md) and [The try statement](~/_csharplang/spec/statements.md#the-try-statement) in the [C# Language Specification](/dotnet/csharp/language-reference/language-specification/introduction).</span></span> <span data-ttu-id="b9828-138">Specifikace jazyka je úplným a rozhodujícím zdrojem pro syntaxi a použití jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="b9828-138">The language specification is the definitive source for C# syntax and usage.</span></span>
  
## <a name="see-also"></a><span data-ttu-id="b9828-139">Viz také:</span><span class="sxs-lookup"><span data-stu-id="b9828-139">See also</span></span>

- [<span data-ttu-id="b9828-140">Reference jazyka C#</span><span class="sxs-lookup"><span data-stu-id="b9828-140">C# Reference</span></span>](../../language-reference/index.md)
- [<span data-ttu-id="b9828-141">Průvodce programováním v C#</span><span class="sxs-lookup"><span data-stu-id="b9828-141">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="b9828-142">Výjimky a zpracování výjimek</span><span class="sxs-lookup"><span data-stu-id="b9828-142">Exceptions and Exception Handling</span></span>](./index.md)
- [<span data-ttu-id="b9828-143">try-catch</span><span class="sxs-lookup"><span data-stu-id="b9828-143">try-catch</span></span>](../../language-reference/keywords/try-catch.md)
- [<span data-ttu-id="b9828-144">try-finally</span><span class="sxs-lookup"><span data-stu-id="b9828-144">try-finally</span></span>](../../language-reference/keywords/try-finally.md)
- [<span data-ttu-id="b9828-145">try-catch-finally</span><span class="sxs-lookup"><span data-stu-id="b9828-145">try-catch-finally</span></span>](../../language-reference/keywords/try-catch-finally.md)
- [<span data-ttu-id="b9828-146">using – příkaz</span><span class="sxs-lookup"><span data-stu-id="b9828-146">using Statement</span></span>](../../language-reference/keywords/using-statement.md)
