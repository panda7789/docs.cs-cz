---
title: Použití výjimek – Průvodce programováním v C#
description: Naučte se používat výjimky. Výjimky jsou vyvolány kódem, který zaznamená chybu a zachycena kódem, který chybu opraví.
ms.date: 07/20/2015
helpviewer_keywords:
- exception handling [C#], about exception handling
- exceptions [C#], about exceptions
ms.assetid: 71472c62-320a-470a-97d2-67995180389d
ms.openlocfilehash: fb45381f1c6cfa2f27d036ead8e662b7a0d8d924
ms.sourcegitcommit: 6f58a5f75ceeb936f8ee5b786e9adb81a9a3bee9
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/28/2020
ms.locfileid: "87303371"
---
# <a name="use-exceptions-c-programming-guide"></a><span data-ttu-id="85e37-104">Použití výjimek (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="85e37-104">Use exceptions (C# programming guide)</span></span>

<span data-ttu-id="85e37-105">V jazyce C# jsou chyby v programu za běhu šířeny přes program pomocí mechanismu nazývaného výjimky.</span><span class="sxs-lookup"><span data-stu-id="85e37-105">In C#, errors in the program at run time are propagated through the program by using a mechanism called exceptions.</span></span> <span data-ttu-id="85e37-106">Výjimky jsou vyvolány kódem, který zaznamená chybu a zachycena kódem, který může chybu opravit.</span><span class="sxs-lookup"><span data-stu-id="85e37-106">Exceptions are thrown by code that encounters an error and caught by code that can correct the error.</span></span> <span data-ttu-id="85e37-107">Výjimky mohou být vyvolány modulem runtime .NET nebo kódem v programu.</span><span class="sxs-lookup"><span data-stu-id="85e37-107">Exceptions can be thrown by the .NET runtime or by code in a program.</span></span> <span data-ttu-id="85e37-108">Jakmile je vyvolána výjimka, rozšíří zásobník volání, dokud nebude `catch` nalezen příkaz pro výjimku.</span><span class="sxs-lookup"><span data-stu-id="85e37-108">Once an exception is thrown, it propagates up the call stack until a `catch` statement for the exception is found.</span></span> <span data-ttu-id="85e37-109">Nezachycené výjimky jsou zpracovávány pomocí obecné obslužné rutiny výjimky poskytované systémem, který zobrazuje dialogové okno.</span><span class="sxs-lookup"><span data-stu-id="85e37-109">Uncaught exceptions are handled by a generic exception handler provided by the system that displays a dialog box.</span></span>  
  
 <span data-ttu-id="85e37-110">Výjimky jsou reprezentovány třídami odvozenými z <xref:System.Exception> .</span><span class="sxs-lookup"><span data-stu-id="85e37-110">Exceptions are represented by classes derived from <xref:System.Exception>.</span></span> <span data-ttu-id="85e37-111">Tato třída identifikuje typ výjimky a obsahuje vlastnosti, které obsahují podrobnosti o výjimce.</span><span class="sxs-lookup"><span data-stu-id="85e37-111">This class identifies the type of exception and contains properties that have details about the exception.</span></span> <span data-ttu-id="85e37-112">Vyvolání výjimky zahrnuje vytvoření instance třídy odvozené od výjimky, volitelně konfiguraci vlastností výjimky a následné vyvolání objektu pomocí `throw` klíčového slova.</span><span class="sxs-lookup"><span data-stu-id="85e37-112">Throwing an exception involves creating an instance of an exception-derived class, optionally configuring properties of the exception, and then throwing the object by using the `throw` keyword.</span></span> <span data-ttu-id="85e37-113">Například:</span><span class="sxs-lookup"><span data-stu-id="85e37-113">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#1](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#1)]  
  
 <span data-ttu-id="85e37-114">Po vyvolání výjimky modul runtime zkontroluje aktuální příkaz a zjistí, zda je v rámci `try` bloku.</span><span class="sxs-lookup"><span data-stu-id="85e37-114">After an exception is thrown, the runtime checks the current statement to see whether it is within a `try` block.</span></span> <span data-ttu-id="85e37-115">Pokud je, `catch` jsou zkontrolovány všechny bloky přidružené k `try` bloku, aby bylo vidět, zda mohou zachytit výjimku.</span><span class="sxs-lookup"><span data-stu-id="85e37-115">If it is, any `catch` blocks associated with the `try` block are checked to see whether they can catch the exception.</span></span> <span data-ttu-id="85e37-116">`Catch`bloky obvykle určují typy výjimek; Pokud `catch` je typ bloku shodný s typem výjimky nebo základní třídou výjimky, `catch` může blok zpracovat metodu.</span><span class="sxs-lookup"><span data-stu-id="85e37-116">`Catch` blocks typically specify exception types; if the type of the `catch` block is the same type as the exception, or a base class of the exception, the `catch` block can handle the method.</span></span> <span data-ttu-id="85e37-117">Například:</span><span class="sxs-lookup"><span data-stu-id="85e37-117">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#2](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#2)]  
  
 <span data-ttu-id="85e37-118">Pokud příkaz, který vyvolá výjimku, není uvnitř `try` bloku nebo pokud `try` blok, který ho obklopuje, nemá odpovídající `catch` blok, modul runtime zkontroluje volající metodu pro `try` příkaz a `catch` bloky.</span><span class="sxs-lookup"><span data-stu-id="85e37-118">If the statement that throws an exception is not within a `try` block or if the `try` block that encloses it has no matching `catch` block, the runtime checks the calling method for a `try` statement and `catch` blocks.</span></span> <span data-ttu-id="85e37-119">Modul runtime pokračuje v zásobníku volání a hledá kompatibilní `catch` blok.</span><span class="sxs-lookup"><span data-stu-id="85e37-119">The runtime continues up the calling stack, searching for a compatible `catch` block.</span></span> <span data-ttu-id="85e37-120">Po `catch` nalezení a provedení bloku je ovládací prvek předán dalšímu příkazu po tomto `catch` bloku.</span><span class="sxs-lookup"><span data-stu-id="85e37-120">After the `catch` block is found and executed, control is passed to the next statement after that `catch` block.</span></span>  
  
 <span data-ttu-id="85e37-121">`try`Příkaz může obsahovat více než jeden `catch` blok.</span><span class="sxs-lookup"><span data-stu-id="85e37-121">A `try` statement can contain more than one `catch` block.</span></span> <span data-ttu-id="85e37-122">`catch`Je proveden první příkaz, který může zpracovat výjimku; všechny následující `catch` příkazy, i když jsou kompatibilní, jsou ignorovány.</span><span class="sxs-lookup"><span data-stu-id="85e37-122">The first `catch` statement that can handle the exception is executed; any following `catch` statements, even if they are compatible, are ignored.</span></span> <span data-ttu-id="85e37-123">Proto by bloky catch měly být vždy řazeny z nejvíce specifického (nebo nejvíce odvozeného) na nejméně konkrétní.</span><span class="sxs-lookup"><span data-stu-id="85e37-123">Therefore, catch blocks should always be ordered from most specific (or most-derived) to least specific.</span></span> <span data-ttu-id="85e37-124">Například:</span><span class="sxs-lookup"><span data-stu-id="85e37-124">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#3)]  
  
 <span data-ttu-id="85e37-125">Před `catch` provedením bloku modul runtime vyhledá `finally` bloky.</span><span class="sxs-lookup"><span data-stu-id="85e37-125">Before the `catch` block is executed, the runtime checks for `finally` blocks.</span></span> <span data-ttu-id="85e37-126">`Finally`bloky umožňují programátorovi vyčistit libovolný nejednoznačný stav, který by mohl zůstat z přerušeného `try` bloku, nebo uvolnit externí prostředky (jako jsou grafické popisovače, databázová připojení nebo datové proudy souborů), aniž by čekali, až uvolňování paměti v modulu runtime dokončí objekty.</span><span class="sxs-lookup"><span data-stu-id="85e37-126">`Finally` blocks enable the programmer to clean up any ambiguous state that could be left over from an aborted `try` block, or to release any external resources (such as graphics handles, database connections or file streams) without waiting for the garbage collector in the runtime to finalize the objects.</span></span> <span data-ttu-id="85e37-127">Například:</span><span class="sxs-lookup"><span data-stu-id="85e37-127">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#4)]  
  
 <span data-ttu-id="85e37-128">Pokud `WriteByte()` vyvolal výjimku, kód v druhém `try` bloku, který se pokusí znovu otevřít soubor `file.Close()` , selže, pokud není volán, a soubor by zůstal uzamčen.</span><span class="sxs-lookup"><span data-stu-id="85e37-128">If `WriteByte()` threw an exception, the code in the second `try` block that tries to reopen the file would fail if `file.Close()` is not called, and the file would remain locked.</span></span> <span data-ttu-id="85e37-129">Vzhledem k tomu `finally` , že jsou bloky spouštěny i v případě, že dojde k výjimce, `finally` blok v předchozím příkladu umožňuje, aby se soubor správně zavřel, a pomáhá vyhnout se chybě.</span><span class="sxs-lookup"><span data-stu-id="85e37-129">Because `finally` blocks are executed even if an exception is thrown, the `finally` block in the previous example allows for the file to be closed correctly and helps avoid an error.</span></span>  
  
 <span data-ttu-id="85e37-130">Pokud `catch` v zásobníku volání není po vyvolání výjimky nalezen žádný kompatibilní blok, nastane jedna ze tří akcí:</span><span class="sxs-lookup"><span data-stu-id="85e37-130">If no compatible `catch` block is found on the call stack after an exception is thrown, one of three things occurs:</span></span>  
  
- <span data-ttu-id="85e37-131">Pokud je výjimka v finalizační metodě, finalizační metoda je přerušena a základní finalizační metoda, pokud existuje, je volána.</span><span class="sxs-lookup"><span data-stu-id="85e37-131">If the exception is within a finalizer, the finalizer is aborted and the base finalizer, if any, is called.</span></span>  
  
- <span data-ttu-id="85e37-132">Pokud zásobník volání obsahuje statický konstruktor nebo inicializátor statických polí, <xref:System.TypeInitializationException> je vyvolána, s původní výjimkou přiřazenou <xref:System.Exception.InnerException%2A> Vlastnosti nové výjimky.</span><span class="sxs-lookup"><span data-stu-id="85e37-132">If the call stack contains a static constructor, or a static field initializer, a <xref:System.TypeInitializationException> is thrown, with the original exception assigned to the <xref:System.Exception.InnerException%2A> property of the new exception.</span></span>  
  
- <span data-ttu-id="85e37-133">Pokud je dosaženo začátku vlákna, vlákno je ukončeno.</span><span class="sxs-lookup"><span data-stu-id="85e37-133">If the start of the thread is reached, the thread is terminated.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="85e37-134">Viz také:</span><span class="sxs-lookup"><span data-stu-id="85e37-134">See also</span></span>

- [<span data-ttu-id="85e37-135">Průvodce programováním v C#</span><span class="sxs-lookup"><span data-stu-id="85e37-135">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="85e37-136">Výjimky a zpracování výjimek</span><span class="sxs-lookup"><span data-stu-id="85e37-136">Exceptions and Exception Handling</span></span>](./index.md)
