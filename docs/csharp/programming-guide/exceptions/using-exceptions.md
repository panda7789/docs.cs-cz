---
title: Použití výjimek – programovací příručka jazyka C#
ms.date: 07/20/2015
helpviewer_keywords:
- exception handling [C#], about exception handling
- exceptions [C#], about exceptions
ms.assetid: 71472c62-320a-470a-97d2-67995180389d
ms.openlocfilehash: 4012027dc1a9bd2543d0a4195360e5f7e0586fe1
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/14/2020
ms.locfileid: "75705259"
---
# <a name="using-exceptions-c-programming-guide"></a><span data-ttu-id="56c70-102">Použití výjimek (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="56c70-102">Using Exceptions (C# Programming Guide)</span></span>
<span data-ttu-id="56c70-103">V C# chyby v programu za běhu jsou šířeny prostřednictvím programu pomocí mechanismu nazývaného výjimky.</span><span class="sxs-lookup"><span data-stu-id="56c70-103">In C#, errors in the program at run time are propagated through the program by using a mechanism called exceptions.</span></span> <span data-ttu-id="56c70-104">Výjimky jsou vyvolány kódem, který narazí na chybu a zachycen kódem, který může chybu opravit.</span><span class="sxs-lookup"><span data-stu-id="56c70-104">Exceptions are thrown by code that encounters an error and caught by code that can correct the error.</span></span> <span data-ttu-id="56c70-105">Výjimky mohou být vyvolány za běhu množých jazyků .NET Framework common language (CLR) nebo kódem v programu.</span><span class="sxs-lookup"><span data-stu-id="56c70-105">Exceptions can be thrown by the .NET Framework common language runtime (CLR) or by code in a program.</span></span> <span data-ttu-id="56c70-106">Jakmile je vyvolána výjimka, rozšíří se zásobníkvolání, dokud není nalezen `catch` příkaz pro výjimku.</span><span class="sxs-lookup"><span data-stu-id="56c70-106">Once an exception is thrown, it propagates up the call stack until a `catch` statement for the exception is found.</span></span> <span data-ttu-id="56c70-107">Nezachycené výjimky jsou zpracovány obecnou obslužnou rutinou výjimek poskytovanou systémem, který zobrazuje dialogové okno.</span><span class="sxs-lookup"><span data-stu-id="56c70-107">Uncaught exceptions are handled by a generic exception handler provided by the system that displays a dialog box.</span></span>  
  
 <span data-ttu-id="56c70-108">Výjimky jsou reprezentovány třídami odvozenými z . <xref:System.Exception></span><span class="sxs-lookup"><span data-stu-id="56c70-108">Exceptions are represented by classes derived from <xref:System.Exception>.</span></span> <span data-ttu-id="56c70-109">Tato třída identifikuje typ výjimky a obsahuje vlastnosti, které mají podrobnosti o výjimce.</span><span class="sxs-lookup"><span data-stu-id="56c70-109">This class identifies the type of exception and contains properties that have details about the exception.</span></span> <span data-ttu-id="56c70-110">Vyvolání výjimky zahrnuje vytvoření instance třídy odvozené od výjimky, volitelně konfiguraci vlastností výjimky a následné vyvolání objektu pomocí klíčového `throw` slova.</span><span class="sxs-lookup"><span data-stu-id="56c70-110">Throwing an exception involves creating an instance of an exception-derived class, optionally configuring properties of the exception, and then throwing the object by using the `throw` keyword.</span></span> <span data-ttu-id="56c70-111">Například:</span><span class="sxs-lookup"><span data-stu-id="56c70-111">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#1](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#1)]  
  
 <span data-ttu-id="56c70-112">Po vyvolání výjimky za běhu zkontroluje aktuální příkaz, zda `try` je v rámci bloku.</span><span class="sxs-lookup"><span data-stu-id="56c70-112">After an exception is thrown, the runtime checks the current statement to see whether it is within a `try` block.</span></span> <span data-ttu-id="56c70-113">Pokud ano, `catch` všechny bloky `try` přidružené k bloku jsou kontrolovány, zda mohou zachytit výjimku.</span><span class="sxs-lookup"><span data-stu-id="56c70-113">If it is, any `catch` blocks associated with the `try` block are checked to see whether they can catch the exception.</span></span> <span data-ttu-id="56c70-114">`Catch`bloky obvykle určují typy výjimek; Pokud je typ `catch` bloku stejného typu jako výjimka nebo základní třída `catch` výjimky, blok může zpracovat metodu.</span><span class="sxs-lookup"><span data-stu-id="56c70-114">`Catch` blocks typically specify exception types; if the type of the `catch` block is the same type as the exception, or a base class of the exception, the `catch` block can handle the method.</span></span> <span data-ttu-id="56c70-115">Například:</span><span class="sxs-lookup"><span data-stu-id="56c70-115">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#2](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#2)]  
  
 <span data-ttu-id="56c70-116">Pokud příkaz, který vyvolá výjimku `try` není v `try` rámci bloku nebo pokud blok, který jej obklopuje nemá `catch` odpovídající `try` blok, runtime zkontroluje volající metodu pro příkaz a `catch` bloky.</span><span class="sxs-lookup"><span data-stu-id="56c70-116">If the statement that throws an exception is not within a `try` block or if the `try` block that encloses it has no matching `catch` block, the runtime checks the calling method for a `try` statement and `catch` blocks.</span></span> <span data-ttu-id="56c70-117">Runtime pokračuje do zásobníku volání a `catch` hledá kompatibilní blok.</span><span class="sxs-lookup"><span data-stu-id="56c70-117">The runtime continues up the calling stack, searching for a compatible `catch` block.</span></span> <span data-ttu-id="56c70-118">Po `catch` nalezení a spuštění bloku je ovládací prvek předán `catch` dalšímu příkazu za tímto blokem.</span><span class="sxs-lookup"><span data-stu-id="56c70-118">After the `catch` block is found and executed, control is passed to the next statement after that `catch` block.</span></span>  
  
 <span data-ttu-id="56c70-119">Příkaz `try` může obsahovat `catch` více než jeden blok.</span><span class="sxs-lookup"><span data-stu-id="56c70-119">A `try` statement can contain more than one `catch` block.</span></span> <span data-ttu-id="56c70-120">První `catch` příkaz, který může zpracovat výjimku je proveden; všechny `catch` následující příkazy, i když jsou kompatibilní, jsou ignorovány.</span><span class="sxs-lookup"><span data-stu-id="56c70-120">The first `catch` statement that can handle the exception is executed; any following `catch` statements, even if they are compatible, are ignored.</span></span> <span data-ttu-id="56c70-121">Proto catch bloky by měly být vždy seřazeny z nejvíce konkrétní (nebo nejvíce odvozené) na nejméně specifické.</span><span class="sxs-lookup"><span data-stu-id="56c70-121">Therefore, catch blocks should always be ordered from most specific (or most-derived) to least specific.</span></span> <span data-ttu-id="56c70-122">Například:</span><span class="sxs-lookup"><span data-stu-id="56c70-122">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#3)]  
  
 <span data-ttu-id="56c70-123">Před `catch` spuštěním bloku za běhu zkontroluje bloky. `finally`</span><span class="sxs-lookup"><span data-stu-id="56c70-123">Before the `catch` block is executed, the runtime checks for `finally` blocks.</span></span> <span data-ttu-id="56c70-124">`Finally`Bloky umožňují programátorovi vyčistit nejednoznačný stav, který by `try` mohl zůstat z přerušeného bloku, nebo uvolnit všechny externí prostředky (například grafické popisovače, připojení k databázi nebo datové proudy souborů) bez čekání na uvolňování paměti v době runtime k dokončení objektů.</span><span class="sxs-lookup"><span data-stu-id="56c70-124">`Finally` blocks enable the programmer to clean up any ambiguous state that could be left over from an aborted `try` block, or to release any external resources (such as graphics handles, database connections or file streams) without waiting for the garbage collector in the runtime to finalize the objects.</span></span> <span data-ttu-id="56c70-125">Například:</span><span class="sxs-lookup"><span data-stu-id="56c70-125">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#4)]  
  
 <span data-ttu-id="56c70-126">Pokud `WriteByte()` vyvolalvýjimku, kód v `try` druhém bloku, který se pokusí `file.Close()` znovu otevřít soubor selže, pokud není volána, a soubor zůstane uzamčen.</span><span class="sxs-lookup"><span data-stu-id="56c70-126">If `WriteByte()` threw an exception, the code in the second `try` block that tries to reopen the file would fail if `file.Close()` is not called, and the file would remain locked.</span></span> <span data-ttu-id="56c70-127">Vzhledem k tomu, že `finally` bloky jsou `finally` spouštěny i v případě, že je vyvolána výjimka, blok v předchozím příkladu umožňuje, aby byl soubor správně uzavřen a pomáhá vyhnout se chybě.</span><span class="sxs-lookup"><span data-stu-id="56c70-127">Because `finally` blocks are executed even if an exception is thrown, the `finally` block in the previous example allows for the file to be closed correctly and helps avoid an error.</span></span>  
  
 <span data-ttu-id="56c70-128">Pokud žádný `catch` kompatibilní blok je nalezen v zásobníku volání po vyvolání výjimky, dojde k jedné ze tří věcí:</span><span class="sxs-lookup"><span data-stu-id="56c70-128">If no compatible `catch` block is found on the call stack after an exception is thrown, one of three things occurs:</span></span>  
  
- <span data-ttu-id="56c70-129">Pokud je výjimka v rámci finalizační metody, finalizační metoda je přerušena a je volána základní finalizační metoda, pokud existuje.</span><span class="sxs-lookup"><span data-stu-id="56c70-129">If the exception is within a finalizer, the finalizer is aborted and the base finalizer, if any, is called.</span></span>  
  
- <span data-ttu-id="56c70-130">Pokud zásobník volání obsahuje statický konstruktor nebo statický inicializátor pole, je vyvolána, <xref:System.TypeInitializationException> s původní výjimku přiřazenou <xref:System.Exception.InnerException%2A> vlastnosti nové výjimky.</span><span class="sxs-lookup"><span data-stu-id="56c70-130">If the call stack contains a static constructor, or a static field initializer, a <xref:System.TypeInitializationException> is thrown, with the original exception assigned to the <xref:System.Exception.InnerException%2A> property of the new exception.</span></span>  
  
- <span data-ttu-id="56c70-131">Pokud je dosaženo začátku vlákna, vlákno je ukončeno.</span><span class="sxs-lookup"><span data-stu-id="56c70-131">If the start of the thread is reached, the thread is terminated.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="56c70-132">Viz také</span><span class="sxs-lookup"><span data-stu-id="56c70-132">See also</span></span>

- [<span data-ttu-id="56c70-133">Programovací příručka jazyka C#</span><span class="sxs-lookup"><span data-stu-id="56c70-133">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="56c70-134">Výjimky a zpracování výjimek</span><span class="sxs-lookup"><span data-stu-id="56c70-134">Exceptions and Exception Handling</span></span>](./index.md)
