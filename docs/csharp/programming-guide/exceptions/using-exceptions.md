---
title: Použití výjimek (Průvodce programováním v C#)
ms.date: 07/20/2015
helpviewer_keywords:
- exception handling [C#], about exception handling
- exceptions [C#], about exceptions
ms.assetid: 71472c62-320a-470a-97d2-67995180389d
ms.openlocfilehash: 43012ec1190117b1905b5e44010d5f57a1e543aa
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 05/04/2018
ms.locfileid: "33339823"
---
# <a name="using-exceptions-c-programming-guide"></a><span data-ttu-id="c1f54-102">Použití výjimek (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="c1f54-102">Using Exceptions (C# Programming Guide)</span></span>
<span data-ttu-id="c1f54-103">V jazyce C# chyby v programu v době běhu rozšířeny prostřednictvím programu pomocí mechanismus názvem výjimky.</span><span class="sxs-lookup"><span data-stu-id="c1f54-103">In C#, errors in the program at run time are propagated through the program by using a mechanism called exceptions.</span></span> <span data-ttu-id="c1f54-104">Výjimky vyvolané kód, který dojde k chybě a zachytila kód, který můžete chybu opravit.</span><span class="sxs-lookup"><span data-stu-id="c1f54-104">Exceptions are thrown by code that encounters an error and caught by code that can correct the error.</span></span> <span data-ttu-id="c1f54-105">Výjimky může být vyvolána pomocí rozhraní .NET Framework common language runtime (CLR) nebo pomocí kódu v programu.</span><span class="sxs-lookup"><span data-stu-id="c1f54-105">Exceptions can be thrown by the .NET Framework common language runtime (CLR) or by code in a program.</span></span> <span data-ttu-id="c1f54-106">Jakmile je vyvolána výjimka, rozšíří zásobníkem volání až `catch` se nachází příkaz pro výjimku.</span><span class="sxs-lookup"><span data-stu-id="c1f54-106">Once an exception is thrown, it propagates up the call stack until a `catch` statement for the exception is found.</span></span> <span data-ttu-id="c1f54-107">Nezachycená výjimky jsou zpracovávány obslužná rutina obecná výjimka poskytované systémem, který se zobrazí dialogové okno.</span><span class="sxs-lookup"><span data-stu-id="c1f54-107">Uncaught exceptions are handled by a generic exception handler provided by the system that displays a dialog box.</span></span>  
  
 <span data-ttu-id="c1f54-108">Výjimky jsou reprezentované pomocí třídy odvozené od třídy <xref:System.Exception>.</span><span class="sxs-lookup"><span data-stu-id="c1f54-108">Exceptions are represented by classes derived from <xref:System.Exception>.</span></span> <span data-ttu-id="c1f54-109">Tato třída identifikuje typ výjimky a obsahuje vlastnosti, které mají podrobnosti o výjimce.</span><span class="sxs-lookup"><span data-stu-id="c1f54-109">This class identifies the type of exception and contains properties that have details about the exception.</span></span> <span data-ttu-id="c1f54-110">Došlo k výjimce zahrnuje vytvoření instance třídy odvozené výjimky, volitelně konfigurace vlastností výjimky a pak vyvolání objekt pomocí `throw` – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="c1f54-110">Throwing an exception involves creating an instance of an exception-derived class, optionally configuring properties of the exception, and then throwing the object by using the `throw` keyword.</span></span> <span data-ttu-id="c1f54-111">Příklad:</span><span class="sxs-lookup"><span data-stu-id="c1f54-111">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#1](../../../csharp/programming-guide/exceptions/codesnippet/CSharp/using-exceptions_1.cs)]  
  
 <span data-ttu-id="c1f54-112">Jakmile je vyvolána výjimka, modul runtime zkontroluje aktuální příkaz a zkontrolujte, zda je v rámci `try` bloku.</span><span class="sxs-lookup"><span data-stu-id="c1f54-112">After an exception is thrown, the runtime checks the current statement to see whether it is within a `try` block.</span></span> <span data-ttu-id="c1f54-113">Pokud se jedná, všechny `catch` bloky přidružené `try` bloku se kontroluje, zda jejich zachycení výjimky.</span><span class="sxs-lookup"><span data-stu-id="c1f54-113">If it is, any `catch` blocks associated with the `try` block are checked to see whether they can catch the exception.</span></span> <span data-ttu-id="c1f54-114">`Catch` bloky obvykle zadat typy výjimek; Pokud typ `catch` blok je stejného typu jako výjimka nebo základní třída výjimky, `catch` bloku dokáže zpracovat metodu.</span><span class="sxs-lookup"><span data-stu-id="c1f54-114">`Catch` blocks typically specify exception types; if the type of the `catch` block is the same type as the exception, or a base class of the exception, the `catch` block can handle the method.</span></span> <span data-ttu-id="c1f54-115">Příklad:</span><span class="sxs-lookup"><span data-stu-id="c1f54-115">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#2](../../../csharp/programming-guide/exceptions/codesnippet/CSharp/using-exceptions_2.cs)]  
  
 <span data-ttu-id="c1f54-116">Pokud příkaz, který vyvolá výjimku, není v `try` bloku nebo, pokud `try` bloku, která obklopuje ho má neexistuje odpovídající `catch` bloku, modul runtime zkontroluje volání metody pro `try` příkaz a `catch` bloky.</span><span class="sxs-lookup"><span data-stu-id="c1f54-116">If the statement that throws an exception is not within a `try` block or if the `try` block that encloses it has no matching `catch` block, the runtime checks the calling method for a `try` statement and `catch` blocks.</span></span> <span data-ttu-id="c1f54-117">Modul runtime pokračuje v až zásobník volání hledání kompatibilní `catch` bloku.</span><span class="sxs-lookup"><span data-stu-id="c1f54-117">The runtime continues up the calling stack, searching for a compatible `catch` block.</span></span> <span data-ttu-id="c1f54-118">Po `catch` bloku nalezen a provést, ovládací prvek předává do další příkazu, po který `catch` bloku.</span><span class="sxs-lookup"><span data-stu-id="c1f54-118">After the `catch` block is found and executed, control is passed to the next statement after that `catch` block.</span></span>  
  
 <span data-ttu-id="c1f54-119">A `try` příkaz může obsahovat více než jednu `catch` bloku.</span><span class="sxs-lookup"><span data-stu-id="c1f54-119">A `try` statement can contain more than one `catch` block.</span></span> <span data-ttu-id="c1f54-120">První `catch` je spustit příkaz, který může zpracovat výjimku; všechny následující `catch` příkazy, i když jsou kompatibilní, jsou ignorovány.</span><span class="sxs-lookup"><span data-stu-id="c1f54-120">The first `catch` statement that can handle the exception is executed; any following `catch` statements, even if they are compatible, are ignored.</span></span> <span data-ttu-id="c1f54-121">Proto catch – bloky je vždy třeba seřadit od nejvíce konkrétní (nebo většinou odvozených) nejméně specifická.</span><span class="sxs-lookup"><span data-stu-id="c1f54-121">Therefore, catch blocks should always be ordered from most specific (or most-derived) to least specific.</span></span> <span data-ttu-id="c1f54-122">Příklad:</span><span class="sxs-lookup"><span data-stu-id="c1f54-122">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#3](../../../csharp/programming-guide/exceptions/codesnippet/CSharp/using-exceptions_3.cs)]  
  
 <span data-ttu-id="c1f54-123">Před `catch` bloku se spustí, zkontroluje modulu runtime pro `finally` bloky.</span><span class="sxs-lookup"><span data-stu-id="c1f54-123">Before the `catch` block is executed, the runtime checks for `finally` blocks.</span></span> <span data-ttu-id="c1f54-124">`Finally` bloky povolit programátorů vyčistit všechny nejednoznačný stavu, který může být zbyly ze přerušené `try` bloku, nebo chcete-li uvolnit všechny externí prostředky (například zpracovává grafiky, připojení databáze nebo souboru datové proudy) bez čekání uvolňování paměti kolekce v modulu runtime pro dokončení objekty.</span><span class="sxs-lookup"><span data-stu-id="c1f54-124">`Finally` blocks enable the programmer to clean up any ambiguous state that could be left over from an aborted `try` block, or to release any external resources (such as graphics handles, database connections or file streams) without waiting for the garbage collector in the runtime to finalize the objects.</span></span> <span data-ttu-id="c1f54-125">Příklad:</span><span class="sxs-lookup"><span data-stu-id="c1f54-125">For example:</span></span>  
  
 [!code-csharp[csProgGuideExceptions#4](../../../csharp/programming-guide/exceptions/codesnippet/CSharp/using-exceptions_4.cs)]  
  
 <span data-ttu-id="c1f54-126">Pokud `WriteByte()` došlo k výjimce, kód za sekundu `try` blok, který se pokusí znovu otevřete soubor selže, pokud `file.Close()` není volán, a soubor by zůstat zamčené.</span><span class="sxs-lookup"><span data-stu-id="c1f54-126">If `WriteByte()` threw an exception, the code in the second `try` block that tries to reopen the file would fail if `file.Close()` is not called, and the file would remain locked.</span></span> <span data-ttu-id="c1f54-127">Protože `finally` bloky provedení i v případě, že je vyvolána výjimka, `finally` bloku v předchozím příkladu umožňuje pro soubor, který má být správně uzavřený a pomáhá zabránit k chybě.</span><span class="sxs-lookup"><span data-stu-id="c1f54-127">Because `finally` blocks are executed even if an exception is thrown, the `finally` block in the previous example allows for the file to be closed correctly and helps avoid an error.</span></span>  
  
 <span data-ttu-id="c1f54-128">Pokud žádné kompatibilní `catch` bloku se nachází v zásobníku volání po je vyvolána výjimka, dojde k jedné z následujících způsobů:</span><span class="sxs-lookup"><span data-stu-id="c1f54-128">If no compatible `catch` block is found on the call stack after an exception is thrown, one of three things occurs:</span></span>  
  
-   <span data-ttu-id="c1f54-129">Pokud byla výjimka v rámci finalizační metody, finalizační metodu byla přerušena a základní finalizační metodu, pokud existuje, je volána.</span><span class="sxs-lookup"><span data-stu-id="c1f54-129">If the exception is within a finalizer, the finalizer is aborted and the base finalizer, if any, is called.</span></span>  
  
-   <span data-ttu-id="c1f54-130">Pokud obsahuje zásobník volání statického konstruktoru nebo inicializátoru statické pole <xref:System.TypeInitializationException> je vyvolána, s výjimkou původní přiřazené <xref:System.Exception.InnerException%2A> vlastnost novou výjimku.</span><span class="sxs-lookup"><span data-stu-id="c1f54-130">If the call stack contains a static constructor, or a static field initializer, a <xref:System.TypeInitializationException> is thrown, with the original exception assigned to the <xref:System.Exception.InnerException%2A> property of the new exception.</span></span>  
  
-   <span data-ttu-id="c1f54-131">Když je dosaženo začátku vlákno, je ukončen vlákno.</span><span class="sxs-lookup"><span data-stu-id="c1f54-131">If the start of the thread is reached, the thread is terminated.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="c1f54-132">Viz také</span><span class="sxs-lookup"><span data-stu-id="c1f54-132">See Also</span></span>  
 [<span data-ttu-id="c1f54-133">Průvodce programováním v jazyce C#</span><span class="sxs-lookup"><span data-stu-id="c1f54-133">C# Programming Guide</span></span>](../../../csharp/programming-guide/index.md)  
 [<span data-ttu-id="c1f54-134">Výjimky a jejich zpracování</span><span class="sxs-lookup"><span data-stu-id="c1f54-134">Exceptions and Exception Handling</span></span>](../../../csharp/programming-guide/exceptions/index.md)
