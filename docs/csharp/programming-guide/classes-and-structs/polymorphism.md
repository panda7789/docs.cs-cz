---
title: Polymorfismus – Průvodce programováním v C#
description: Seznamte se s polymorfismus, Klíčovým konceptem v objektově orientovaných programovacích jazycích, jako je C#, který popisuje vztah mezi základní a odvozenou třídou.
ms.date: 02/08/2020
helpviewer_keywords:
- C# language, polymorphism
- polymorphism [C#]
ms.assetid: 086af969-29a5-4ce8-a993-0b7d53839dab
ms.openlocfilehash: 2a1ca4c498c5885c7d34475405ac83c4cccecd6f
ms.sourcegitcommit: 3d84eac0818099c9949035feb96bbe0346358504
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/21/2020
ms.locfileid: "86864121"
---
# <a name="polymorphism-c-programming-guide"></a><span data-ttu-id="ab344-103">Polymorfismus (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="ab344-103">Polymorphism (C# Programming Guide)</span></span>

<span data-ttu-id="ab344-104">Polymorfismus se často označuje jako třetí pilíř objektově orientovaného programování, a to po zapouzdření a dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="ab344-104">Polymorphism is often referred to as the third pillar of object-oriented programming, after encapsulation and inheritance.</span></span> <span data-ttu-id="ab344-105">Polymorfismus je řecký Word, který znamená "velký tvar" a má dva odlišné aspekty:</span><span class="sxs-lookup"><span data-stu-id="ab344-105">Polymorphism is a Greek word that means "many-shaped" and it has two distinct aspects:</span></span>
  
- <span data-ttu-id="ab344-106">V době běhu lze objekty odvozené třídy považovat za objekty základní třídy v místech, jako jsou parametry metody a kolekce nebo pole.</span><span class="sxs-lookup"><span data-stu-id="ab344-106">At run time, objects of a derived class may be treated as objects of a base class in places such as method parameters and collections or arrays.</span></span> <span data-ttu-id="ab344-107">Pokud tato polymorfismua nastane, deklarovaný typ objektu již není totožný s jeho typem za běhu.</span><span class="sxs-lookup"><span data-stu-id="ab344-107">When this polymorphism occurs, the object's declared type is no longer identical to its run-time type.</span></span>
- <span data-ttu-id="ab344-108">Základní třídy mohou definovat a implementovat [virtuální](../../language-reference/keywords/virtual.md) *metody*a odvozené třídy je mohou [přepsat](../../language-reference/keywords/override.md) , což znamená, že poskytují svou vlastní definici a implementaci.</span><span class="sxs-lookup"><span data-stu-id="ab344-108">Base classes may define and implement [virtual](../../language-reference/keywords/virtual.md) *methods*, and derived classes can [override](../../language-reference/keywords/override.md) them, which means they provide their own definition and implementation.</span></span> <span data-ttu-id="ab344-109">V době běhu, když kód klienta volá metodu, modul CLR vyhledá typ za běhu objektu a vyvolá přepsání virtuální metody.</span><span class="sxs-lookup"><span data-stu-id="ab344-109">At run-time, when client code calls the method, the CLR looks up the run-time type of the object, and invokes that override of the virtual method.</span></span> <span data-ttu-id="ab344-110">Ve zdrojovém kódu můžete zavolat metodu pro základní třídu a způsobit spuštění odvozené třídy verze metody.</span><span class="sxs-lookup"><span data-stu-id="ab344-110">In your source code you can call a method on a base class, and cause a derived class's version of the method to be executed.</span></span>

<span data-ttu-id="ab344-111">Virtuální metody umožňují pracovat se skupinami souvisejících objektů jednotným způsobem.</span><span class="sxs-lookup"><span data-stu-id="ab344-111">Virtual methods enable you to work with groups of related objects in a uniform way.</span></span> <span data-ttu-id="ab344-112">Předpokládejme například, že máte aplikaci pro kreslení, která umožňuje uživateli vytvářet různé druhy tvarů na kreslicí ploše.</span><span class="sxs-lookup"><span data-stu-id="ab344-112">For example, suppose you have a drawing application that enables a user to create various kinds of shapes on a drawing surface.</span></span> <span data-ttu-id="ab344-113">V době kompilace neznáte, které konkrétní typy tvarů uživatel vytvoří.</span><span class="sxs-lookup"><span data-stu-id="ab344-113">You do not know at compile time which specific types of shapes the user will create.</span></span> <span data-ttu-id="ab344-114">Aplikace však musí sledovat všechny různé typy tvarů, které byly vytvořeny, a musí je aktualizovat v reakci na akce myši uživatele.</span><span class="sxs-lookup"><span data-stu-id="ab344-114">However, the application has to keep track of all the various types of shapes that are created, and it has to update them in response to user mouse actions.</span></span> <span data-ttu-id="ab344-115">K vyřešení tohoto problému můžete použít polymorfismus ve dvou základních krocích:</span><span class="sxs-lookup"><span data-stu-id="ab344-115">You can use polymorphism to solve this problem in two basic steps:</span></span>

1. <span data-ttu-id="ab344-116">Vytvořte hierarchii třídy, ve které jsou jednotlivé konkrétní třídy tvarů odvozeny ze společné základní třídy.</span><span class="sxs-lookup"><span data-stu-id="ab344-116">Create a class hierarchy in which each specific shape class derives from a common base class.</span></span>
1. <span data-ttu-id="ab344-117">Použijte virtuální metodu k vyvolání vhodné metody pro jakoukoli odvozenou třídu prostřednictvím jediného volání metody základní třídy.</span><span class="sxs-lookup"><span data-stu-id="ab344-117">Use a virtual method to invoke the appropriate method on any derived class through a single call to the base class method.</span></span>

<span data-ttu-id="ab344-118">Nejprve vytvořte základní třídu s názvem `Shape` a odvozené třídy, jako například `Rectangle` , `Circle` a `Triangle` .</span><span class="sxs-lookup"><span data-stu-id="ab344-118">First, create a base class called `Shape`, and derived classes such as `Rectangle`, `Circle`, and `Triangle`.</span></span> <span data-ttu-id="ab344-119">Poskytněte `Shape` třídě virtuální metodu s názvem `Draw` a přepište ji v každé odvozené třídě pro vykreslení konkrétního tvaru, který třída představuje.</span><span class="sxs-lookup"><span data-stu-id="ab344-119">Give the `Shape` class a virtual method called `Draw`, and override it in each derived class to draw the particular shape that the class represents.</span></span> <span data-ttu-id="ab344-120">Vytvořte `List<Shape>` objekt a přidejte `Circle` `Triangle` do něj objekt,, a `Rectangle` .</span><span class="sxs-lookup"><span data-stu-id="ab344-120">Create a `List<Shape>` object and add a `Circle`, `Triangle`, and `Rectangle` to it.</span></span>

[!code-csharp[Polymorphism overview](~/samples/snippets/csharp/objectoriented/Inheritance.cs#PolymorphismOverview)]

<span data-ttu-id="ab344-121">Chcete-li aktualizovat plochu pro kreslení, použijte smyčku [foreach](../../language-reference/keywords/foreach-in.md) k iterování seznamu a zavolejte `Draw` metodu pro každý `Shape` objekt v seznamu.</span><span class="sxs-lookup"><span data-stu-id="ab344-121">To update the drawing surface, use a [foreach](../../language-reference/keywords/foreach-in.md) loop to iterate through the list and call the `Draw` method on each `Shape` object in the list.</span></span> <span data-ttu-id="ab344-122">I když každý objekt v seznamu má deklarovaný typ `Shape` , je typ za běhu (přepsaná verze metody v každé odvozené třídě), která bude vyvolána.</span><span class="sxs-lookup"><span data-stu-id="ab344-122">Even though each object in the list has a declared type of `Shape`, it's the run-time type (the overridden version of the method in each derived class) that will be invoked.</span></span>

[!code-csharp[Polymorphism overview](~/samples/snippets/csharp/objectoriented/Inheritance.cs#UsePolymorphism)]

<span data-ttu-id="ab344-123">V jazyce C# je každý typ polymorfní, protože všechny typy, včetně uživatelsky definovaných typů, dědí z <xref:System.Object> .</span><span class="sxs-lookup"><span data-stu-id="ab344-123">In C#, every type is polymorphic because all types, including user-defined types, inherit from <xref:System.Object>.</span></span>  

## <a name="polymorphism-overview"></a><span data-ttu-id="ab344-124">Přehled polymorfismu</span><span class="sxs-lookup"><span data-stu-id="ab344-124">Polymorphism overview</span></span>

### <a name="virtual-members"></a><span data-ttu-id="ab344-125">Virtuální členové</span><span class="sxs-lookup"><span data-stu-id="ab344-125">Virtual members</span></span>

<span data-ttu-id="ab344-126">Když odvozená třída dědí ze základní třídy, získá všechny metody, pole, vlastnosti a události základní třídy.</span><span class="sxs-lookup"><span data-stu-id="ab344-126">When a derived class inherits from a base class, it gains all the methods, fields, properties, and events of the base class.</span></span> <span data-ttu-id="ab344-127">Návrhář odvozené třídy může lišit možnosti pro chování virtuálních metod:</span><span class="sxs-lookup"><span data-stu-id="ab344-127">The designer of the derived class can different choices for the behavior of virtual methods:</span></span>

- <span data-ttu-id="ab344-128">Odvozená třída může přepsat virtuální členy v základní třídě a definovat nové chování.</span><span class="sxs-lookup"><span data-stu-id="ab344-128">The derived class may override virtual members in the base class, defining new behavior.</span></span>
- <span data-ttu-id="ab344-129">Odvozená třída dědí nejbližší metodu základní třídy bez přepsání, zachovává stávající chování, ale umožňuje dalším odvozeným třídám přepsat metodu.</span><span class="sxs-lookup"><span data-stu-id="ab344-129">The derived class inherit the closest base class method without overriding it, preserving the existing behavior but enabling further derived classes to override the method.</span></span>
- <span data-ttu-id="ab344-130">Odvozená třída může definovat novou nevirtuální implementaci těchto členů, kteří skryjí implementace základní třídy.</span><span class="sxs-lookup"><span data-stu-id="ab344-130">The derived class may define new non-virtual implementation of those members that hide the base class implementations.</span></span>

<span data-ttu-id="ab344-131">Odvozená třída může přepsat člen základní třídy pouze v případě, že je člen základní třídy deklarován jako [virtuální](../../language-reference/keywords/virtual.md) nebo [abstraktní](../../language-reference/keywords/abstract.md).</span><span class="sxs-lookup"><span data-stu-id="ab344-131">A derived class can override a base class member only if the base class member is declared as [virtual](../../language-reference/keywords/virtual.md) or [abstract](../../language-reference/keywords/abstract.md).</span></span> <span data-ttu-id="ab344-132">Odvozený člen musí použít klíčové slovo [override](../../language-reference/keywords/override.md) k explicitnímu označení toho, že je metoda určena k účasti ve virtuálním vyvolání.</span><span class="sxs-lookup"><span data-stu-id="ab344-132">The derived member must use the [override](../../language-reference/keywords/override.md) keyword to explicitly indicate that the method is intended to participate in virtual invocation.</span></span> <span data-ttu-id="ab344-133">Následující kód poskytuje příklad:</span><span class="sxs-lookup"><span data-stu-id="ab344-133">The following code provides an example:</span></span>

[!code-csharp[Virtual overview](~/samples/snippets/csharp/objectoriented/Inheritance.cs#VirtualMethods)]

<span data-ttu-id="ab344-134">Pole nemůžou být virtuální. virtuální můžou být jenom metody, vlastnosti, události a indexery.</span><span class="sxs-lookup"><span data-stu-id="ab344-134">Fields cannot be virtual; only methods, properties, events, and indexers can be virtual.</span></span> <span data-ttu-id="ab344-135">Když odvozená třída přepíše virtuální člen, je tento člen volán i v případě, že instance této třídy je k dispozici jako instance základní třídy.</span><span class="sxs-lookup"><span data-stu-id="ab344-135">When a derived class overrides a virtual member, that member is called even when an instance of that class is being accessed as an instance of the base class.</span></span> <span data-ttu-id="ab344-136">Následující kód poskytuje příklad:</span><span class="sxs-lookup"><span data-stu-id="ab344-136">The following code provides an example:</span></span>

[!code-csharp[Virtual overview example](~/samples/snippets/csharp/objectoriented/Inheritance.cs#SnippetTestVirtualMethods)]

<span data-ttu-id="ab344-137">Virtuální metody a vlastnosti umožňují odvozeným třídám rozšiřuje základní třídu bez nutnosti použití implementace základní třídy metody.</span><span class="sxs-lookup"><span data-stu-id="ab344-137">Virtual methods and properties enable derived classes to extend a base class without needing to use the base class implementation of a method.</span></span> <span data-ttu-id="ab344-138">Další informace najdete v tématu [Správa verzí pomocí klíčových slov override a New](./versioning-with-the-override-and-new-keywords.md).</span><span class="sxs-lookup"><span data-stu-id="ab344-138">For more information, see [Versioning with the Override and New Keywords](./versioning-with-the-override-and-new-keywords.md).</span></span> <span data-ttu-id="ab344-139">Rozhraní poskytuje další způsob, jak definovat metodu nebo sadu metod, jejichž implementace je ponechána na odvozené třídy.</span><span class="sxs-lookup"><span data-stu-id="ab344-139">An interface provides another way to define a method or set of methods whose implementation is left to derived classes.</span></span> <span data-ttu-id="ab344-140">Další informace naleznete v tématu [rozhraní](../interfaces/index.md).</span><span class="sxs-lookup"><span data-stu-id="ab344-140">For more information, see [Interfaces](../interfaces/index.md).</span></span>

### <a name="hide-base-class-members-with-new-members"></a><span data-ttu-id="ab344-141">Skrýt členy základní třídy novými členy</span><span class="sxs-lookup"><span data-stu-id="ab344-141">Hide base class members with new members</span></span>

<span data-ttu-id="ab344-142">Chcete-li, aby vaše odvozená třída měla člena se stejným názvem jako členem v základní třídě, můžete použít klíčové slovo [New](../../language-reference/keywords/new-modifier.md) pro skrytí člena základní třídy.</span><span class="sxs-lookup"><span data-stu-id="ab344-142">If you want your derived class to have a member with the same name as a member in a base class, you can use the [new](../../language-reference/keywords/new-modifier.md) keyword to hide the base class member.</span></span> <span data-ttu-id="ab344-143">`new`Klíčové slovo je vloženo před návratový typ člena třídy, který se nahrazuje.</span><span class="sxs-lookup"><span data-stu-id="ab344-143">The `new` keyword is put before the return type of a class member that is being replaced.</span></span> <span data-ttu-id="ab344-144">Následující kód poskytuje příklad:</span><span class="sxs-lookup"><span data-stu-id="ab344-144">The following code provides an example:</span></span>

[!code-csharp[New method overview example](~/samples/snippets/csharp/objectoriented/Inheritance.cs#NewMethods)]

<span data-ttu-id="ab344-145">Ke skrytým členům základní třídy je možné přivodit z klientského kódu přetypování instance odvozené třídy na instanci základní třídy.</span><span class="sxs-lookup"><span data-stu-id="ab344-145">Hidden base class members may be accessed from client code by casting the instance of the derived class to an instance of the base class.</span></span> <span data-ttu-id="ab344-146">Příklad:</span><span class="sxs-lookup"><span data-stu-id="ab344-146">For example:</span></span>

[!code-csharp[New method overview usage](~/samples/snippets/csharp/objectoriented/Inheritance.cs#UseNewMethods)]

### <a name="prevent-derived-classes-from-overriding-virtual-members"></a><span data-ttu-id="ab344-147">Zabránit odvozeným třídám z přepsání virtuálních členů</span><span class="sxs-lookup"><span data-stu-id="ab344-147">Prevent derived classes from overriding virtual members</span></span>  

<span data-ttu-id="ab344-148">Virtuální členové zůstávají virtuální bez ohledu na to, kolik tříd bylo deklarováno mezi virtuálním členem a třídou, která ji původně deklarovala.</span><span class="sxs-lookup"><span data-stu-id="ab344-148">Virtual members remain virtual, regardless of how many classes have been declared between the virtual member and the class that originally declared it.</span></span> <span data-ttu-id="ab344-149">Pokud třída `A` deklaruje virtuální člen a třída je `B` odvozena z třídy `A` a třída je `C` odvozena z `B` třídy, třída `C` zdědí virtuálního člena a může jej přepsat bez ohledu na to, zda třída `B` deklarovala přepsání pro daného člena.</span><span class="sxs-lookup"><span data-stu-id="ab344-149">If class `A` declares a virtual member, and class `B` derives from `A`, and class `C` derives from `B`, class `C` inherits the virtual member, and may override it, regardless of whether class `B` declared an override for that member.</span></span> <span data-ttu-id="ab344-150">Následující kód poskytuje příklad:</span><span class="sxs-lookup"><span data-stu-id="ab344-150">The following code provides an example:</span></span>

[!code-csharp[Basic hierarchy](~/samples/snippets/csharp/objectoriented/Hierarchy.cs#FirstHierarchy)]

<span data-ttu-id="ab344-151">Odvozená třída může zastavit virtuální dědičnost deklarací override jako [sealed](../../language-reference/keywords/sealed.md).</span><span class="sxs-lookup"><span data-stu-id="ab344-151">A derived class can stop virtual inheritance by declaring an override as [sealed](../../language-reference/keywords/sealed.md).</span></span> <span data-ttu-id="ab344-152">Zastavování dědičnosti vyžaduje vložení `sealed` klíčového slova před `override` klíčové slovo v deklaraci člena třídy.</span><span class="sxs-lookup"><span data-stu-id="ab344-152">Stopping inheritance requires putting the `sealed` keyword before the `override` keyword in the class member declaration.</span></span> <span data-ttu-id="ab344-153">Následující kód poskytuje příklad:</span><span class="sxs-lookup"><span data-stu-id="ab344-153">The following code provides an example:</span></span>

[!code-csharp[A sealed overridden member](~/samples/snippets/csharp/objectoriented/Hierarchy.cs#SealedOverride)]

<span data-ttu-id="ab344-154">V předchozím příkladu již metoda není typu `DoWork` Virtual pro žádnou třídu odvozenou z `C` .</span><span class="sxs-lookup"><span data-stu-id="ab344-154">In the previous example, the method `DoWork` is no longer virtual to any class derived from `C`.</span></span> <span data-ttu-id="ab344-155">Je stále virtuální pro instance `C` , i když jsou přetypování na typ `B` nebo typ `A` .</span><span class="sxs-lookup"><span data-stu-id="ab344-155">It's still virtual for instances of `C`, even if they're cast to type `B` or type `A`.</span></span> <span data-ttu-id="ab344-156">Zapečetěné metody lze nahradit odvozenými třídami pomocí `new` klíčového slova, jak ukazuje následující příklad:</span><span class="sxs-lookup"><span data-stu-id="ab344-156">Sealed methods can be replaced by derived classes by using the `new` keyword, as the following example shows:</span></span>

[!code-csharp[New method declaration](~/samples/snippets/csharp/objectoriented/Hierarchy.cs#NewDeclaration)]

<span data-ttu-id="ab344-157">V tomto případě, pokud `DoWork` je volána při `D` použití proměnné typu `D` , `DoWork` je volána nová.</span><span class="sxs-lookup"><span data-stu-id="ab344-157">In this case, if `DoWork` is called on `D` using a variable of type `D`, the new `DoWork` is called.</span></span> <span data-ttu-id="ab344-158">Pokud proměnná typu `C` , `B` nebo `A` je použita pro přístup k instanci `D` , volání metody `DoWork` bude následovat pravidla virtuální dědičnosti, směrování těchto volání do implementace `DoWork` třídy na třídu `C` .</span><span class="sxs-lookup"><span data-stu-id="ab344-158">If a variable of type `C`, `B`, or `A` is used to access an instance of `D`, a call to `DoWork` will follow the rules of virtual inheritance, routing those calls to the implementation of `DoWork` on class `C`.</span></span>

### <a name="access-base-class-virtual-members-from-derived-classes"></a><span data-ttu-id="ab344-159">Přístup k virtuálním členům základní třídy z odvozených tříd</span><span class="sxs-lookup"><span data-stu-id="ab344-159">Access base class virtual members from derived classes</span></span>

<span data-ttu-id="ab344-160">Odvozená třída, která nahradila nebo přepsala metodu nebo vlastnost, může stále přistupovat k metodě nebo vlastnosti základní třídy pomocí `base` klíčového slova.</span><span class="sxs-lookup"><span data-stu-id="ab344-160">A derived class that has replaced or overridden a method or property can still access the method or property on the base class using the `base` keyword.</span></span> <span data-ttu-id="ab344-161">Následující kód poskytuje příklad:</span><span class="sxs-lookup"><span data-stu-id="ab344-161">The following code provides an example:</span></span>

```csharp
public class Base
{
    public virtual void DoWork() {/*...*/ }
}
public class Derived : Base
{
    public override void DoWork()
    {
        //Perform Derived's work here
        //...
        // Call DoWork on base class
        base.DoWork();
    }
}
```

<span data-ttu-id="ab344-162">Další informace naleznete v tématu [Base](../../language-reference/keywords/base.md).</span><span class="sxs-lookup"><span data-stu-id="ab344-162">For more information, see [base](../../language-reference/keywords/base.md).</span></span>

> [!NOTE]
> <span data-ttu-id="ab344-163">Doporučuje se, aby virtuální členové použili `base` k volání implementace základní třídy tohoto člena ve své vlastní implementaci.</span><span class="sxs-lookup"><span data-stu-id="ab344-163">It is recommended that virtual members use `base` to call the base class implementation of that member in their own implementation.</span></span> <span data-ttu-id="ab344-164">V případě, že dojde k chování základní třídy, umožňuje odvozeným třídám soustředit se na implementaci konkrétního chování, které je specifické pro odvozenou třídu.</span><span class="sxs-lookup"><span data-stu-id="ab344-164">Letting the base class behavior occur enables the derived class to concentrate on implementing behavior specific to the derived class.</span></span> <span data-ttu-id="ab344-165">Pokud není volána implementace základní třídy, je až do odvozené třídy, aby jejich chování bylo kompatibilní s chováním základní třídy.</span><span class="sxs-lookup"><span data-stu-id="ab344-165">If the base class implementation is not called, it is up to the derived class to make their behavior compatible with the behavior of the base class.</span></span>

## <a name="in-this-section"></a><span data-ttu-id="ab344-166">V této části</span><span class="sxs-lookup"><span data-stu-id="ab344-166">In this section</span></span>

- [<span data-ttu-id="ab344-167">Správa verzí pomocí klíčových slov override a new</span><span class="sxs-lookup"><span data-stu-id="ab344-167">Versioning with the Override and New Keywords</span></span>](./versioning-with-the-override-and-new-keywords.md)
- [<span data-ttu-id="ab344-168">Znalost, kdy použít klíčová slova override a new</span><span class="sxs-lookup"><span data-stu-id="ab344-168">Knowing When to Use Override and New Keywords</span></span>](./knowing-when-to-use-override-and-new-keywords.md)
- [<span data-ttu-id="ab344-169">Jak potlačit metodu ToString</span><span class="sxs-lookup"><span data-stu-id="ab344-169">How to override the ToString method</span></span>](./how-to-override-the-tostring-method.md)

## <a name="see-also"></a><span data-ttu-id="ab344-170">Viz také</span><span class="sxs-lookup"><span data-stu-id="ab344-170">See also</span></span>

- [<span data-ttu-id="ab344-171">Průvodce programováním v C#</span><span class="sxs-lookup"><span data-stu-id="ab344-171">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="ab344-172">Dědičnost</span><span class="sxs-lookup"><span data-stu-id="ab344-172">Inheritance</span></span>](./inheritance.md)
- [<span data-ttu-id="ab344-173">Abstraktní a uzavřené třídy a jejich členové</span><span class="sxs-lookup"><span data-stu-id="ab344-173">Abstract and Sealed Classes and Class Members</span></span>](./abstract-and-sealed-classes-and-class-members.md)
- [<span data-ttu-id="ab344-174">Metody</span><span class="sxs-lookup"><span data-stu-id="ab344-174">Methods</span></span>](./methods.md)
- [<span data-ttu-id="ab344-175">Události</span><span class="sxs-lookup"><span data-stu-id="ab344-175">Events</span></span>](../events/index.md)
- [<span data-ttu-id="ab344-176">Vlastnosti</span><span class="sxs-lookup"><span data-stu-id="ab344-176">Properties</span></span>](./properties.md)
- [<span data-ttu-id="ab344-177">Indexery</span><span class="sxs-lookup"><span data-stu-id="ab344-177">Indexers</span></span>](../indexers/index.md)
- [<span data-ttu-id="ab344-178">Typy</span><span class="sxs-lookup"><span data-stu-id="ab344-178">Types</span></span>](../types/index.md)
