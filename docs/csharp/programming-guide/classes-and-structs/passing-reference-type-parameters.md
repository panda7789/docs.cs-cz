---
title: Předávání parametrů typu odkazu – C# Průvodce programováním
ms.custom: seodec18
ms.date: 07/20/2015
helpviewer_keywords:
- method parameters [C#], reference types
- parameters [C#], reference
ms.assetid: 9e6eb65c-942e-48ab-920a-b7ba9df4ea20
ms.openlocfilehash: f4329c525995b8246427072d1f537d91d875ef95
ms.sourcegitcommit: 986f836f72ef10876878bd6217174e41464c145a
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/19/2019
ms.locfileid: "69596263"
---
# <a name="passing-reference-type-parameters-c-programming-guide"></a><span data-ttu-id="bc53d-102">Předávání parametrů typu odkazu (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="bc53d-102">Passing Reference-Type Parameters (C# Programming Guide)</span></span>
<span data-ttu-id="bc53d-103">Proměnná [typu odkazu](../../language-reference/keywords/reference-types.md) neobsahuje přímo data; obsahuje odkaz na jeho data.</span><span class="sxs-lookup"><span data-stu-id="bc53d-103">A variable of a [reference type](../../language-reference/keywords/reference-types.md) does not contain its data directly; it contains a reference to its data.</span></span> <span data-ttu-id="bc53d-104">Při předání parametru typu odkazu podle hodnoty je možné změnit data patřící do odkazovaného objektu, jako je například hodnota člena třídy.</span><span class="sxs-lookup"><span data-stu-id="bc53d-104">When you pass a reference-type parameter by value, it is possible to change the data belonging to the referenced object, such as the value of a class member.</span></span> <span data-ttu-id="bc53d-105">Nelze však změnit hodnotu samotného odkazu; Nemůžete například použít stejný odkaz pro přidělení paměti pro nový objekt a jeho zachování mimo metodu.</span><span class="sxs-lookup"><span data-stu-id="bc53d-105">However, you cannot change the value of the reference itself; for example, you cannot use the same reference to allocate memory for a new object and have it persist outside the method.</span></span> <span data-ttu-id="bc53d-106">Chcete-li to provést, předejte parametr pomocí klíčového slova [ref](../../language-reference/keywords/ref.md) nebo [out](../../language-reference/keywords/out-parameter-modifier.md) .</span><span class="sxs-lookup"><span data-stu-id="bc53d-106">To do that, pass the parameter using the [ref](../../language-reference/keywords/ref.md) or [out](../../language-reference/keywords/out-parameter-modifier.md) keyword.</span></span> <span data-ttu-id="bc53d-107">Pro zjednodušení používá `ref`následující příklady.</span><span class="sxs-lookup"><span data-stu-id="bc53d-107">For simplicity, the following examples use `ref`.</span></span>  
  
## <a name="passing-reference-types-by-value"></a><span data-ttu-id="bc53d-108">Předávání typů odkazů podle hodnoty</span><span class="sxs-lookup"><span data-stu-id="bc53d-108">Passing Reference Types by Value</span></span>  
 <span data-ttu-id="bc53d-109">Následující příklad ukazuje předání parametru typu odkazu, `arr`, podle hodnoty, metodě,. `Change`</span><span class="sxs-lookup"><span data-stu-id="bc53d-109">The following example demonstrates passing a reference-type parameter, `arr`, by value, to a method, `Change`.</span></span> <span data-ttu-id="bc53d-110">Vzhledem k tomu, že parametr je `arr`odkaz na, je možné změnit hodnoty prvků pole.</span><span class="sxs-lookup"><span data-stu-id="bc53d-110">Because the parameter is a reference to `arr`, it is possible to change the values of the array elements.</span></span> <span data-ttu-id="bc53d-111">Pokus o změnu přiřazení parametru do jiného umístění paměti však funguje pouze uvnitř metody a nemá vliv na původní proměnnou `arr`.</span><span class="sxs-lookup"><span data-stu-id="bc53d-111">However, the attempt to reassign the parameter to a different memory location only works inside the method and does not affect the original variable, `arr`.</span></span>  
  
 [!code-csharp[csProgGuideParameters#7](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#7)]  
  
 <span data-ttu-id="bc53d-112">V předchozím příkladu je pole, `arr`,, který je odkazový typ, předáno metodě `ref` bez parametru.</span><span class="sxs-lookup"><span data-stu-id="bc53d-112">In the preceding example, the array, `arr`, which is a reference type, is passed to the method without the `ref` parameter.</span></span> <span data-ttu-id="bc53d-113">V takovém případě je kopie odkazu, který odkazuje na `arr`, předána metodě.</span><span class="sxs-lookup"><span data-stu-id="bc53d-113">In such a case, a copy of the reference, which points to `arr`, is passed to the method.</span></span> <span data-ttu-id="bc53d-114">Výstup ukazuje, že je možné, aby metoda změnila obsah prvku pole v tomto případě z `1` na. `888`</span><span class="sxs-lookup"><span data-stu-id="bc53d-114">The output shows that it is possible for the method to change the contents of an array element, in this case from `1` to `888`.</span></span> <span data-ttu-id="bc53d-115">Avšak přidělení nové části paměti pomocí operátoru `Change` [New](../../language-reference/operators/new-operator.md) v metodě způsobí, že proměnná `pArray` odkazuje na nové pole.</span><span class="sxs-lookup"><span data-stu-id="bc53d-115">However, allocating a new portion of memory by using the [new](../../language-reference/operators/new-operator.md) operator inside the `Change` method makes the variable `pArray` reference a new array.</span></span> <span data-ttu-id="bc53d-116">Proto všechny změny, které nejsou ovlivněny, nebudou mít vliv na `arr`původní pole, které je `Main`vytvořeno v rámci.</span><span class="sxs-lookup"><span data-stu-id="bc53d-116">Thus, any changes after that will not affect the original array, `arr`, which is created inside `Main`.</span></span> <span data-ttu-id="bc53d-117">Ve skutečnosti jsou v tomto příkladu vytvořena dvě pole, jedna uvnitř `Main` a jedna `Change` uvnitř metody.</span><span class="sxs-lookup"><span data-stu-id="bc53d-117">In fact, two arrays are created in this example, one inside `Main` and one inside the `Change` method.</span></span>  
  
## <a name="passing-reference-types-by-reference"></a><span data-ttu-id="bc53d-118">Předávání typů odkazů odkazem</span><span class="sxs-lookup"><span data-stu-id="bc53d-118">Passing Reference Types by Reference</span></span>  
 <span data-ttu-id="bc53d-119">Následující příklad je stejný jako předchozí příklad s tím rozdílem, že `ref` klíčové slovo je přidáno do záhlaví metody a volání.</span><span class="sxs-lookup"><span data-stu-id="bc53d-119">The following example is the same as the previous example, except that the `ref` keyword is added to the method header and call.</span></span> <span data-ttu-id="bc53d-120">Všechny změny, které probíhají v metodě, ovlivňují původní proměnnou v volajícím programu.</span><span class="sxs-lookup"><span data-stu-id="bc53d-120">Any changes that take place in the method affect the original variable in the calling program.</span></span>  
  
 [!code-csharp[csProgGuideParameters#8](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#8)]  
  
 <span data-ttu-id="bc53d-121">Všechny změny, které probíhají v metodě, ovlivňují původní pole v `Main`.</span><span class="sxs-lookup"><span data-stu-id="bc53d-121">All of the changes that take place inside the method affect the original array in `Main`.</span></span> <span data-ttu-id="bc53d-122">Ve skutečnosti je původní pole znovu přiděleno pomocí `new` operátoru.</span><span class="sxs-lookup"><span data-stu-id="bc53d-122">In fact, the original array is reallocated using the `new` operator.</span></span> <span data-ttu-id="bc53d-123">Proto po volání `Change` metody všechny `arr` odkazy odkazují na pole s pěti prvky, `Change` které je vytvořeno v metodě.</span><span class="sxs-lookup"><span data-stu-id="bc53d-123">Thus, after calling the `Change` method, any reference to `arr` points to the five-element array, which is created in the `Change` method.</span></span>  
  
## <a name="swapping-two-strings"></a><span data-ttu-id="bc53d-124">Záměna dvou řetězců</span><span class="sxs-lookup"><span data-stu-id="bc53d-124">Swapping Two Strings</span></span>  
 <span data-ttu-id="bc53d-125">Výměna řetězců je dobrým příkladem předání parametrů typu odkazu odkazem.</span><span class="sxs-lookup"><span data-stu-id="bc53d-125">Swapping strings is a good example of passing reference-type parameters by reference.</span></span> <span data-ttu-id="bc53d-126">V příkladu jsou dva řetězce `str1` a `str2`, inicializovány `SwapStrings` v `Main` a předány metodě jako parametry upravené `ref` klíčovým slovem.</span><span class="sxs-lookup"><span data-stu-id="bc53d-126">In the example, two strings, `str1` and `str2`, are initialized in `Main` and passed to the `SwapStrings` method as parameters modified by the `ref` keyword.</span></span> <span data-ttu-id="bc53d-127">Tyto dva řetězce jsou v rámci metody prohozeny a `Main` uvnitř také.</span><span class="sxs-lookup"><span data-stu-id="bc53d-127">The two strings are swapped inside the method and inside `Main` as well.</span></span>  
  
 [!code-csharp[csProgGuideParameters#9](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#9)]  
  
 <span data-ttu-id="bc53d-128">V tomto příkladu musí být parametry předány odkazem na vliv na proměnné v volajícím programu.</span><span class="sxs-lookup"><span data-stu-id="bc53d-128">In this example, the parameters need to be passed by reference to affect the variables in the calling program.</span></span> <span data-ttu-id="bc53d-129">Odeberete-li `ref` klíčové slovo z hlavičky metody a volání metody, nebudou provedeny žádné změny v volajícím programu.</span><span class="sxs-lookup"><span data-stu-id="bc53d-129">If you remove the `ref` keyword from both the method header and the method call, no changes will take place in the calling program.</span></span>  
  
 <span data-ttu-id="bc53d-130">Další informace o řetězcích naleznete v tématu [String](../../language-reference/keywords/string.md).</span><span class="sxs-lookup"><span data-stu-id="bc53d-130">For more information about strings, see [string](../../language-reference/keywords/string.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="bc53d-131">Viz také:</span><span class="sxs-lookup"><span data-stu-id="bc53d-131">See also</span></span>

- [<span data-ttu-id="bc53d-132">Průvodce programováním v jazyce C#</span><span class="sxs-lookup"><span data-stu-id="bc53d-132">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="bc53d-133">Předávání parametrů</span><span class="sxs-lookup"><span data-stu-id="bc53d-133">Passing Parameters</span></span>](./passing-parameters.md)
- [<span data-ttu-id="bc53d-134">ref</span><span class="sxs-lookup"><span data-stu-id="bc53d-134">ref</span></span>](../../language-reference/keywords/ref.md)
- [<span data-ttu-id="bc53d-135">in</span><span class="sxs-lookup"><span data-stu-id="bc53d-135">in</span></span>](../../language-reference/keywords/in-parameter-modifier.md)
- [<span data-ttu-id="bc53d-136">out</span><span class="sxs-lookup"><span data-stu-id="bc53d-136">out</span></span>](../../language-reference/keywords/out.md)
- [<span data-ttu-id="bc53d-137">Odkazové typy</span><span class="sxs-lookup"><span data-stu-id="bc53d-137">Reference Types</span></span>](../../language-reference/keywords/reference-types.md)
