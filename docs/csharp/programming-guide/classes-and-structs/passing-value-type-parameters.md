---
title: Předání parametrů typu hodnoty – Průvodce programováním v C#
description: Pokud předáte proměnnou hodnotového typu metodě podle hodnoty v jazyce C#, žádné změny nebudou mít vliv na původní data. Chcete-li změnit hodnotu, předejte odkaz.
ms.date: 07/20/2015
helpviewer_keywords:
- method parameters [C#], value types
- parameters [C#], value
ms.assetid: 193ab86f-5f9b-4359-ac29-7cdf8afad3a6
ms.openlocfilehash: 84829722cfb0b7ce71aff36f27a7c97cfa07ad5e
ms.sourcegitcommit: 3d84eac0818099c9949035feb96bbe0346358504
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/21/2020
ms.locfileid: "86864693"
---
# <a name="passing-value-type-parameters-c-programming-guide"></a><span data-ttu-id="5a2d1-104">Předávání parametrů typu hodnoty (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="5a2d1-104">Passing Value-Type Parameters (C# Programming Guide)</span></span>
<span data-ttu-id="5a2d1-105">Proměnná [typu hodnoty](../../language-reference/builtin-types/value-types.md) obsahuje svá data přímo na rozdíl od proměnné [typu odkazu](../../language-reference/keywords/reference-types.md) , která obsahuje odkaz na její data.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-105">A [value-type](../../language-reference/builtin-types/value-types.md) variable contains its data directly as opposed to a [reference-type](../../language-reference/keywords/reference-types.md) variable, which contains a reference to its data.</span></span> <span data-ttu-id="5a2d1-106">Předání proměnné typu hodnoty metodě podle hodnoty znamená předání kopie proměnné metodě.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-106">Passing a value-type variable to a method by value means passing a copy of the variable to the method.</span></span> <span data-ttu-id="5a2d1-107">Všechny změny parametru, které probíhají v metodě, nemají žádný vliv na původní data uložená v proměnné argumentu.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-107">Any changes to the parameter that take place inside the method have no effect on the original data stored in the argument variable.</span></span> <span data-ttu-id="5a2d1-108">Chcete-li, aby volaná metoda změnila hodnotu argumentu, je nutné ji předat odkazem pomocí klíčového slova [ref](../../language-reference/keywords/ref.md) nebo [out](../../language-reference/keywords/out-parameter-modifier.md) .</span><span class="sxs-lookup"><span data-stu-id="5a2d1-108">If you want the called method to change the value of the argument, you must pass it by reference, using the [ref](../../language-reference/keywords/ref.md) or [out](../../language-reference/keywords/out-parameter-modifier.md) keyword.</span></span> <span data-ttu-id="5a2d1-109">Můžete také použít klíčové slovo [in](../../language-reference/keywords/in-parameter-modifier.md) k předání parametru hodnoty odkazem k tomu, aby se zabránilo kopírování a zároveň zaručeno, že hodnota nebude změněna.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-109">You may also use the [in](../../language-reference/keywords/in-parameter-modifier.md) keyword to pass a value parameter by reference to avoid the copy while guaranteeing that the value will not be changed.</span></span> <span data-ttu-id="5a2d1-110">Pro zjednodušení používá následující příklady `ref` .</span><span class="sxs-lookup"><span data-stu-id="5a2d1-110">For simplicity, the following examples use `ref`.</span></span>  
  
## <a name="passing-value-types-by-value"></a><span data-ttu-id="5a2d1-111">Předávání typů hodnot podle hodnoty</span><span class="sxs-lookup"><span data-stu-id="5a2d1-111">Passing Value Types by Value</span></span>  
 <span data-ttu-id="5a2d1-112">Následující příklad ukazuje předání parametrů typu hodnoty podle hodnoty.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-112">The following example demonstrates passing value-type parameters by value.</span></span> <span data-ttu-id="5a2d1-113">Proměnná `n` je předána hodnotou do metody `SquareIt` .</span><span class="sxs-lookup"><span data-stu-id="5a2d1-113">The variable `n` is passed by value to the method `SquareIt`.</span></span> <span data-ttu-id="5a2d1-114">Všechny změny, které probíhají v metodě, nemají žádný vliv na původní hodnotu proměnné.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-114">Any changes that take place inside the method have no effect on the original value of the variable.</span></span>  
  
 [!code-csharp[csProgGuideParameters#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#3)]  
  
 <span data-ttu-id="5a2d1-115">Proměnná `n` je typ hodnoty.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-115">The variable `n` is a value type.</span></span> <span data-ttu-id="5a2d1-116">Obsahuje data a hodnotu `5` .</span><span class="sxs-lookup"><span data-stu-id="5a2d1-116">It contains its data, the value `5`.</span></span> <span data-ttu-id="5a2d1-117">Při `SquareIt` vyvolání je obsah `n` zkopírován do parametru `x` , který je v rámci metody čtvercový.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-117">When `SquareIt` is invoked, the contents of `n` are copied into the parameter `x`, which is squared inside the method.</span></span> <span data-ttu-id="5a2d1-118">V `Main` , ale hodnota `n` je stejná po volání `SquareIt` metody jako předtím.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-118">In `Main`, however, the value of `n` is the same after calling the `SquareIt` method as it was before.</span></span> <span data-ttu-id="5a2d1-119">Změny, které probíhají v rámci metody, ovlivňují pouze místní proměnnou `x` .</span><span class="sxs-lookup"><span data-stu-id="5a2d1-119">The change that takes place inside the method only affects the local variable `x`.</span></span>  
  
## <a name="passing-value-types-by-reference"></a><span data-ttu-id="5a2d1-120">Předávání typů hodnot odkazem</span><span class="sxs-lookup"><span data-stu-id="5a2d1-120">Passing Value Types by Reference</span></span>  
 <span data-ttu-id="5a2d1-121">Následující příklad je stejný jako předchozí příklad s tím rozdílem, že argument je předán jako `ref` parametr.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-121">The following example is the same as the previous example, except that the argument is passed as a `ref` parameter.</span></span> <span data-ttu-id="5a2d1-122">Hodnota podkladového argumentu, `n` , je při `x` změně v metodě změněna.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-122">The value of the underlying argument, `n`, is changed when `x` is changed in the method.</span></span>  
  
 [!code-csharp[csProgGuideParameters#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#4)]  
  
 <span data-ttu-id="5a2d1-123">V tomto příkladu není hodnota `n` předána, ale odkaz na `n` je předán.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-123">In this example, it is not the value of `n` that is passed; rather, a reference to `n` is passed.</span></span> <span data-ttu-id="5a2d1-124">Parametr není `x` [int](../../language-reference/builtin-types/integral-numeric-types.md); jedná se o odkaz na výjimku `int` , v tomto případě odkaz na `n` .</span><span class="sxs-lookup"><span data-stu-id="5a2d1-124">The parameter `x` is not an [int](../../language-reference/builtin-types/integral-numeric-types.md); it is a reference to an `int`, in this case, a reference to `n`.</span></span> <span data-ttu-id="5a2d1-125">Proto, pokud `x` je uvnitř metody, co je ve skutečnosti na čtverci, je to `x` , co se týká, `n` .</span><span class="sxs-lookup"><span data-stu-id="5a2d1-125">Therefore, when `x` is squared inside the method, what actually is squared is what `x` refers to, `n`.</span></span>  
  
## <a name="swapping-value-types"></a><span data-ttu-id="5a2d1-126">Výměna hodnotových typů</span><span class="sxs-lookup"><span data-stu-id="5a2d1-126">Swapping Value Types</span></span>  
 <span data-ttu-id="5a2d1-127">Běžným příkladem změny hodnot argumentů je metoda swap, kde předáváte do metody dvě proměnné a metoda zahodí jejich obsah.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-127">A common example of changing the values of arguments is a swap method, where you pass two variables to the method, and the method swaps their contents.</span></span> <span data-ttu-id="5a2d1-128">Argumenty metody swap je nutné předat odkazem.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-128">You must pass the arguments to the swap method by reference.</span></span> <span data-ttu-id="5a2d1-129">V opačném případě provedete prohození místních kopií parametrů uvnitř metody a v metodě volání nedojde k žádným změnám.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-129">Otherwise, you swap local copies of the parameters inside the method, and no change occurs in the calling method.</span></span> <span data-ttu-id="5a2d1-130">Následující příklad odměňuje celočíselné hodnoty.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-130">The following example swaps integer values.</span></span>  
  
 [!code-csharp[csProgGuideParameters#5](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#5)]  
  
 <span data-ttu-id="5a2d1-131">Při volání `SwapByRef` metody použijte `ref` klíčové slovo v volání, jak je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="5a2d1-131">When you call the `SwapByRef` method, use the `ref` keyword in the call, as shown in the following example.</span></span>  
  
 [!code-csharp[csProgGuideParameters#6](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#6)]  
  
## <a name="see-also"></a><span data-ttu-id="5a2d1-132">Viz také</span><span class="sxs-lookup"><span data-stu-id="5a2d1-132">See also</span></span>

- [<span data-ttu-id="5a2d1-133">Průvodce programováním v C#</span><span class="sxs-lookup"><span data-stu-id="5a2d1-133">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="5a2d1-134">Předávání parametrů</span><span class="sxs-lookup"><span data-stu-id="5a2d1-134">Passing Parameters</span></span>](./passing-parameters.md)
- [<span data-ttu-id="5a2d1-135">Předávání parametrů typu odkazu</span><span class="sxs-lookup"><span data-stu-id="5a2d1-135">Passing Reference-Type Parameters</span></span>](./passing-reference-type-parameters.md)
