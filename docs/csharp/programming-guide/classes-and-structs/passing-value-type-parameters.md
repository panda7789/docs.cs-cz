---
title: Předávání parametrů typu hodnoty – programovací příručka Jazyka C#
ms.date: 07/20/2015
helpviewer_keywords:
- method parameters [C#], value types
- parameters [C#], value
ms.assetid: 193ab86f-5f9b-4359-ac29-7cdf8afad3a6
ms.openlocfilehash: 670af18d4b2b356aa33a0a03a29c05f5ba9bf78f
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/14/2020
ms.locfileid: "76744492"
---
# <a name="passing-value-type-parameters-c-programming-guide"></a><span data-ttu-id="952a1-102">Předávání parametrů typu hodnoty (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="952a1-102">Passing Value-Type Parameters (C# Programming Guide)</span></span>
<span data-ttu-id="952a1-103">Proměnná [typu value](../../language-reference/builtin-types/value-types.md) obsahuje svá data přímo na rozdíl od proměnné [typu odkazu,](../../language-reference/keywords/reference-types.md) která obsahuje odkaz na data.</span><span class="sxs-lookup"><span data-stu-id="952a1-103">A [value-type](../../language-reference/builtin-types/value-types.md) variable contains its data directly as opposed to a [reference-type](../../language-reference/keywords/reference-types.md) variable, which contains a reference to its data.</span></span> <span data-ttu-id="952a1-104">Předání proměnné typu hodnoty metodě pomocí hodnoty znamená předání kopie proměnné metodě.</span><span class="sxs-lookup"><span data-stu-id="952a1-104">Passing a value-type variable to a method by value means passing a copy of the variable to the method.</span></span> <span data-ttu-id="952a1-105">Všechny změny parametru, které probíhají uvnitř metody, nemají žádný vliv na původní data uložená v proměnné argumentu.</span><span class="sxs-lookup"><span data-stu-id="952a1-105">Any changes to the parameter that take place inside the method have no affect on the original data stored in the argument variable.</span></span> <span data-ttu-id="952a1-106">Pokud chcete, aby volaná metoda změnila hodnotu argumentu, musíte ji předat odkazem pomocí klíčového slova [ref](../../language-reference/keywords/ref.md) nebo [out.](../../language-reference/keywords/out-parameter-modifier.md)</span><span class="sxs-lookup"><span data-stu-id="952a1-106">If you want the called method to change the value of the argument, you must pass it by reference, using the [ref](../../language-reference/keywords/ref.md) or [out](../../language-reference/keywords/out-parameter-modifier.md) keyword.</span></span> <span data-ttu-id="952a1-107">Můžete také použít [v](../../language-reference/keywords/in-parameter-modifier.md) klíčové slovo předat parametr hodnoty odkazem, aby se zabránilo kopii a zároveň zaručit, že hodnota nebude změněna.</span><span class="sxs-lookup"><span data-stu-id="952a1-107">You may also use the [in](../../language-reference/keywords/in-parameter-modifier.md) keyword to pass a value parameter by reference to avoid the copy while guaranteeing that the value will not be changed.</span></span> <span data-ttu-id="952a1-108">Pro jednoduchost používají `ref`následující příklady .</span><span class="sxs-lookup"><span data-stu-id="952a1-108">For simplicity, the following examples use `ref`.</span></span>  
  
## <a name="passing-value-types-by-value"></a><span data-ttu-id="952a1-109">Předávání typů hodnot podle hodnoty</span><span class="sxs-lookup"><span data-stu-id="952a1-109">Passing Value Types by Value</span></span>  
 <span data-ttu-id="952a1-110">Následující příklad ukazuje předávání parametrů typu hodnoty podle hodnoty.</span><span class="sxs-lookup"><span data-stu-id="952a1-110">The following example demonstrates passing value-type parameters by value.</span></span> <span data-ttu-id="952a1-111">Proměnná `n` je předána hodnotou metodě `SquareIt`.</span><span class="sxs-lookup"><span data-stu-id="952a1-111">The variable `n` is passed by value to the method `SquareIt`.</span></span> <span data-ttu-id="952a1-112">Všechny změny, které probíhají uvnitř metody nemají žádný vliv na původní hodnotu proměnné.</span><span class="sxs-lookup"><span data-stu-id="952a1-112">Any changes that take place inside the method have no affect on the original value of the variable.</span></span>  
  
 [!code-csharp[csProgGuideParameters#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#3)]  
  
 <span data-ttu-id="952a1-113">Proměnná `n` je typ hodnoty.</span><span class="sxs-lookup"><span data-stu-id="952a1-113">The variable `n` is a value type.</span></span> <span data-ttu-id="952a1-114">Obsahuje data, hodnotu `5`.</span><span class="sxs-lookup"><span data-stu-id="952a1-114">It contains its data, the value `5`.</span></span> <span data-ttu-id="952a1-115">Při `SquareIt` vyvolání `n` je obsah zkopírovány `x`do parametru , který je ve své metodě na druhou.</span><span class="sxs-lookup"><span data-stu-id="952a1-115">When `SquareIt` is invoked, the contents of `n` are copied into the parameter `x`, which is squared inside the method.</span></span> <span data-ttu-id="952a1-116">V `Main`, však `n` hodnota je stejná `SquareIt` po volání metody, jak tomu bylo dříve.</span><span class="sxs-lookup"><span data-stu-id="952a1-116">In `Main`, however, the value of `n` is the same after calling the `SquareIt` method as it was before.</span></span> <span data-ttu-id="952a1-117">Změna, která probíhá uvnitř metody, `x`ovlivní pouze místní proměnnou .</span><span class="sxs-lookup"><span data-stu-id="952a1-117">The change that takes place inside the method only affects the local variable `x`.</span></span>  
  
## <a name="passing-value-types-by-reference"></a><span data-ttu-id="952a1-118">Předávání typů hodnot podle odkazu</span><span class="sxs-lookup"><span data-stu-id="952a1-118">Passing Value Types by Reference</span></span>  
 <span data-ttu-id="952a1-119">Následující příklad je stejný jako v předchozím příkladu s `ref` tím rozdílem, že argument je předán jako parametr.</span><span class="sxs-lookup"><span data-stu-id="952a1-119">The following example is the same as the previous example, except that the argument is passed as a `ref` parameter.</span></span> <span data-ttu-id="952a1-120">Hodnota podkladového argumentu `n`, se `x` změní při změně metody.</span><span class="sxs-lookup"><span data-stu-id="952a1-120">The value of the underlying argument, `n`, is changed when `x` is changed in the method.</span></span>  
  
 [!code-csharp[csProgGuideParameters#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#4)]  
  
 <span data-ttu-id="952a1-121">V tomto příkladu není hodnota, `n` která je předána; spíše je předán `n` odkaz na.</span><span class="sxs-lookup"><span data-stu-id="952a1-121">In this example, it is not the value of `n` that is passed; rather, a reference to `n` is passed.</span></span> <span data-ttu-id="952a1-122">Parametr `x` není [int](../../language-reference/builtin-types/integral-numeric-types.md); jedná se o `int`odkaz na , v `n`tomto případě odkaz na .</span><span class="sxs-lookup"><span data-stu-id="952a1-122">The parameter `x` is not an [int](../../language-reference/builtin-types/integral-numeric-types.md); it is a reference to an `int`, in this case, a reference to `n`.</span></span> <span data-ttu-id="952a1-123">Proto, `x` když je na druhou uvnitř metody, co `x` je ve `n`skutečnosti na druhou, je to, co odkazuje na , .</span><span class="sxs-lookup"><span data-stu-id="952a1-123">Therefore, when `x` is squared inside the method, what actually is squared is what `x` refers to, `n`.</span></span>  
  
## <a name="swapping-value-types"></a><span data-ttu-id="952a1-124">Zaměňování typů hodnot</span><span class="sxs-lookup"><span data-stu-id="952a1-124">Swapping Value Types</span></span>  
 <span data-ttu-id="952a1-125">Běžným příkladem změny hodnot argumentů je metoda odkládacího, kde předáte metodě dvě proměnné a metoda zamění jejich obsah.</span><span class="sxs-lookup"><span data-stu-id="952a1-125">A common example of changing the values of arguments is a swap method, where you pass two variables to the method, and the method swaps their contents.</span></span> <span data-ttu-id="952a1-126">Argumenty je nutné předat metodě odkládacího vztahu odkazem.</span><span class="sxs-lookup"><span data-stu-id="952a1-126">You must pass the arguments to the swap method by reference.</span></span> <span data-ttu-id="952a1-127">V opačném případě zaměníte místní kopie parametrů uvnitř metody a v metodě volání nedojde k žádné změně.</span><span class="sxs-lookup"><span data-stu-id="952a1-127">Otherwise, you swap local copies of the parameters inside the method, and no change occurs in the calling method.</span></span> <span data-ttu-id="952a1-128">Následující příklad zamění celé hodnoty.</span><span class="sxs-lookup"><span data-stu-id="952a1-128">The following example swaps integer values.</span></span>  
  
 [!code-csharp[csProgGuideParameters#5](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#5)]  
  
 <span data-ttu-id="952a1-129">Při volání `SwapByRef` metody použijte `ref` klíčové slovo v volání, jak je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="952a1-129">When you call the `SwapByRef` method, use the `ref` keyword in the call, as shown in the following example.</span></span>  
  
 [!code-csharp[csProgGuideParameters#6](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideParameters/CS/Parameters.cs#6)]  
  
## <a name="see-also"></a><span data-ttu-id="952a1-130">Viz také</span><span class="sxs-lookup"><span data-stu-id="952a1-130">See also</span></span>

- [<span data-ttu-id="952a1-131">Programovací příručka jazyka C#</span><span class="sxs-lookup"><span data-stu-id="952a1-131">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="952a1-132">Předávání parametrů</span><span class="sxs-lookup"><span data-stu-id="952a1-132">Passing Parameters</span></span>](./passing-parameters.md)
- [<span data-ttu-id="952a1-133">Předávání parametrů typu odkazu</span><span class="sxs-lookup"><span data-stu-id="952a1-133">Passing Reference-Type Parameters</span></span>](./passing-reference-type-parameters.md)
