---
title: Metody rozšíření – Průvodce programováním v C#
description: Metody rozšíření v jazyce C# umožňují přidávat metody do stávajících typů bez vytváření nového odvozeného typu, rekompilace nebo jiné změny původního typu.
ms.date: 03/19/2020
helpviewer_keywords:
- methods [C#], adding to existing types
- extension methods [C#]
- methods [C#], extension
ms.assetid: 175ce3ff-9bbf-4e64-8421-faeb81a0bb51
ms.openlocfilehash: 116087ac1aab57f2869b05f436801c7861c56eca
ms.sourcegitcommit: 7476c20d2f911a834a00b8a7f5e8926bae6804d9
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/11/2020
ms.locfileid: "88063702"
---
# <a name="extension-methods-c-programming-guide"></a><span data-ttu-id="16a8a-103">Metody rozšíření (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="16a8a-103">Extension Methods (C# Programming Guide)</span></span>

<span data-ttu-id="16a8a-104">Metody rozšíření umožňují „přidávat“ metody ke stávajícím typům bez vytváření nového odvozeného typu, rekompilace nebo jiné změny původního typu.</span><span class="sxs-lookup"><span data-stu-id="16a8a-104">Extension methods enable you to "add" methods to existing types without creating a new derived type, recompiling, or otherwise modifying the original type.</span></span> <span data-ttu-id="16a8a-105">Rozšiřující metody jsou statické metody, ale jsou volány, jako by šlo o metody instance rozšířeného typu.</span><span class="sxs-lookup"><span data-stu-id="16a8a-105">Extension methods are static methods, but they're called as if they were instance methods on the extended type.</span></span> <span data-ttu-id="16a8a-106">Pro klientský kód napsaný v jazyce C#, F # a Visual Basic neexistuje žádný zřejmý rozdíl mezi voláním metody rozšíření a metodami definovanými v typu.</span><span class="sxs-lookup"><span data-stu-id="16a8a-106">For client code written in C#, F# and Visual Basic, there's no apparent difference between calling an extension method and the methods defined in a type.</span></span>

<span data-ttu-id="16a8a-107">Nejběžnější metody rozšíření jsou operátory dotazů LINQ Standard, které přidávají funkce dotazů existujícím <xref:System.Collections.IEnumerable?displayProperty=nameWithType> <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType> typům a.</span><span class="sxs-lookup"><span data-stu-id="16a8a-107">The most common extension methods are the LINQ standard query operators that add query functionality to the existing <xref:System.Collections.IEnumerable?displayProperty=nameWithType> and <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType> types.</span></span> <span data-ttu-id="16a8a-108">Chcete-li použít standardní operátory dotazu, nejprve je přeneste do rozsahu s `using System.Linq` direktivou.</span><span class="sxs-lookup"><span data-stu-id="16a8a-108">To use the standard query operators, first bring them into scope with a `using System.Linq` directive.</span></span> <span data-ttu-id="16a8a-109">Pak jakýkoli typ, který implementuje, <xref:System.Collections.Generic.IEnumerable%601> má metody instance, jako například <xref:System.Linq.Enumerable.GroupBy%2A> , <xref:System.Linq.Enumerable.OrderBy%2A> , <xref:System.Linq.Enumerable.Average%2A> a tak dále.</span><span class="sxs-lookup"><span data-stu-id="16a8a-109">Then any type that implements <xref:System.Collections.Generic.IEnumerable%601> appears to have instance methods such as <xref:System.Linq.Enumerable.GroupBy%2A>, <xref:System.Linq.Enumerable.OrderBy%2A>, <xref:System.Linq.Enumerable.Average%2A>, and so on.</span></span> <span data-ttu-id="16a8a-110">Tyto další metody lze zobrazit v dokončování příkazů technologie IntelliSense, pokud zadáte "tečka" po instanci <xref:System.Collections.Generic.IEnumerable%601> typu, například <xref:System.Collections.Generic.List%601> nebo <xref:System.Array> .</span><span class="sxs-lookup"><span data-stu-id="16a8a-110">You can see these additional methods in IntelliSense statement completion when you type "dot" after an instance of an <xref:System.Collections.Generic.IEnumerable%601> type such as <xref:System.Collections.Generic.List%601> or <xref:System.Array>.</span></span>

### <a name="orderby-example"></a><span data-ttu-id="16a8a-111">Příklad OrderBy</span><span class="sxs-lookup"><span data-stu-id="16a8a-111">OrderBy Example</span></span>

<span data-ttu-id="16a8a-112">Následující příklad ukazuje, jak zavolat standardní metodu operátoru dotazu `OrderBy` na pole celých čísel.</span><span class="sxs-lookup"><span data-stu-id="16a8a-112">The following example shows how to call the standard query operator `OrderBy` method on an array of integers.</span></span> <span data-ttu-id="16a8a-113">Výraz v závorkách je výraz lambda.</span><span class="sxs-lookup"><span data-stu-id="16a8a-113">The expression in parentheses is a lambda expression.</span></span> <span data-ttu-id="16a8a-114">Mnoho standardních operátorů dotazů přijímá výrazy lambda jako parametry, ale to není vyžadováno pro metody rozšíření.</span><span class="sxs-lookup"><span data-stu-id="16a8a-114">Many standard query operators take lambda expressions as parameters, but this isn't a requirement for extension methods.</span></span> <span data-ttu-id="16a8a-115">Další informace naleznete v tématu [lambda výrazy](../../language-reference/operators/lambda-expressions.md).</span><span class="sxs-lookup"><span data-stu-id="16a8a-115">For more information, see [Lambda Expressions](../../language-reference/operators/lambda-expressions.md).</span></span>

[!code-csharp[csProgGuideExtensionMethods#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExtensionMethods/cs/extensionmethods.cs#3)]

<span data-ttu-id="16a8a-116">Metody rozšíření jsou definovány jako statické metody, ale jsou volány pomocí syntaxe metody instance.</span><span class="sxs-lookup"><span data-stu-id="16a8a-116">Extension methods are defined as static methods but are called by using instance method syntax.</span></span> <span data-ttu-id="16a8a-117">Jejich první parametr určuje, na který typ metoda pracuje.</span><span class="sxs-lookup"><span data-stu-id="16a8a-117">Their first parameter specifies which type the method operates on.</span></span> <span data-ttu-id="16a8a-118">Tomuto parametru předchází [Tento](../../language-reference/keywords/this.md) modifikátor.</span><span class="sxs-lookup"><span data-stu-id="16a8a-118">The parameter is preceded by the [this](../../language-reference/keywords/this.md) modifier.</span></span> <span data-ttu-id="16a8a-119">Metody rozšíření jsou v oboru pouze v případě, že explicitně importujete obor názvů do zdrojového kódu s `using` direktivou.</span><span class="sxs-lookup"><span data-stu-id="16a8a-119">Extension methods are only in scope when you explicitly import the namespace into your source code with a `using` directive.</span></span>

<span data-ttu-id="16a8a-120">Následující příklad ukazuje metodu rozšíření definovanou pro <xref:System.String?displayProperty=nameWithType> třídu.</span><span class="sxs-lookup"><span data-stu-id="16a8a-120">The following example shows an extension method defined for the <xref:System.String?displayProperty=nameWithType> class.</span></span> <span data-ttu-id="16a8a-121">Je definovaný uvnitř nevnořené, neobecné statické třídy:</span><span class="sxs-lookup"><span data-stu-id="16a8a-121">It's defined inside a non-nested, non-generic static class:</span></span>

[!code-csharp[csProgGuideExtensionMethods#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExtensionMethods/cs/extensionmethods.cs#4)]

<span data-ttu-id="16a8a-122">`WordCount`Metoda rozšíření může být přenesena do rozsahu pomocí této `using` direktivy:</span><span class="sxs-lookup"><span data-stu-id="16a8a-122">The `WordCount` extension method can be brought into scope with this `using` directive:</span></span>

```csharp
using ExtensionMethods;
```

<span data-ttu-id="16a8a-123">A může být volána z aplikace pomocí následující syntaxe:</span><span class="sxs-lookup"><span data-stu-id="16a8a-123">And it can be called from an application by using this syntax:</span></span>

```csharp
string s = "Hello Extension Methods";
int i = s.WordCount();
```

<span data-ttu-id="16a8a-124">Metodu rozšíření ve vašem kódu vyvoláte pomocí syntaxe metody instance.</span><span class="sxs-lookup"><span data-stu-id="16a8a-124">You invoke the extension method in your code with instance method syntax.</span></span> <span data-ttu-id="16a8a-125">Převodní jazyk (IL) generovaný kompilátorem překládá váš kód do volání statické metody.</span><span class="sxs-lookup"><span data-stu-id="16a8a-125">The intermediate language (IL) generated by the compiler translates your code into a call on the static method.</span></span> <span data-ttu-id="16a8a-126">Princip zapouzdření není opravdu vyrušován.</span><span class="sxs-lookup"><span data-stu-id="16a8a-126">The principle of encapsulation is not really being violated.</span></span> <span data-ttu-id="16a8a-127">Metody rozšíření nemůžou přistupovat k privátním proměnným v typu, který rozšiřují.</span><span class="sxs-lookup"><span data-stu-id="16a8a-127">Extension methods cannot access private variables in the type they are extending.</span></span>

<span data-ttu-id="16a8a-128">Další informace naleznete v tématu [implementace a volání vlastní metody rozšíření](./how-to-implement-and-call-a-custom-extension-method.md).</span><span class="sxs-lookup"><span data-stu-id="16a8a-128">For more information, see [How to implement and call a custom  extension method](./how-to-implement-and-call-a-custom-extension-method.md).</span></span>

<span data-ttu-id="16a8a-129">Obecně platí, že budete pravděpodobně volat metody rozšíření mnohem častěji než implementace vlastního.</span><span class="sxs-lookup"><span data-stu-id="16a8a-129">In general, you'll probably be calling extension methods far more often than implementing your own.</span></span> <span data-ttu-id="16a8a-130">Vzhledem k tomu, že metody rozšíření jsou volány pomocí syntaxe metody instance, není vyžadována žádná zvláštní znalost, abyste je mohli použít v klientském kódu.</span><span class="sxs-lookup"><span data-stu-id="16a8a-130">Because extension methods are called by using instance method syntax, no special knowledge is required to use them from client code.</span></span> <span data-ttu-id="16a8a-131">Chcete-li povolit metody rozšíření pro konkrétní typ, stačí přidat `using` direktivu pro obor názvů, ve kterém jsou metody definovány.</span><span class="sxs-lookup"><span data-stu-id="16a8a-131">To enable extension methods for a particular type, just add a `using` directive for the namespace in which the methods are defined.</span></span> <span data-ttu-id="16a8a-132">Například pro použití standardních operátorů dotazu přidejte tuto `using` direktivu do kódu:</span><span class="sxs-lookup"><span data-stu-id="16a8a-132">For example, to use the standard query operators, add this `using` directive to your code:</span></span>

```csharp
using System.Linq;
```

<span data-ttu-id="16a8a-133">(Je také možné, že budete muset přidat odkaz na System.Core.dll.) Všimněte si, že standardní operátory dotazu se teď zobrazují v IntelliSense jako další metody dostupné pro většinu <xref:System.Collections.Generic.IEnumerable%601> typů.</span><span class="sxs-lookup"><span data-stu-id="16a8a-133">(You may also have to add a reference to System.Core.dll.) You'll notice that the standard query operators now appear in IntelliSense as additional methods available for most <xref:System.Collections.Generic.IEnumerable%601> types.</span></span>

## <a name="binding-extension-methods-at-compile-time"></a><span data-ttu-id="16a8a-134">Vytváření vazeb na metody rozšíření v době kompilace</span><span class="sxs-lookup"><span data-stu-id="16a8a-134">Binding Extension Methods at Compile Time</span></span>

<span data-ttu-id="16a8a-135">Metody rozšíření můžete použít k rozšíření třídy nebo rozhraní, nikoli však k jejich přepsání.</span><span class="sxs-lookup"><span data-stu-id="16a8a-135">You can use extension methods to extend a class or interface, but not to override them.</span></span> <span data-ttu-id="16a8a-136">Metoda rozšíření se stejným názvem a signaturou, jako má rozhraní nebo metoda třídy, nebude nikdy volána.</span><span class="sxs-lookup"><span data-stu-id="16a8a-136">An extension method with the same name and signature as an interface or class method will never be called.</span></span> <span data-ttu-id="16a8a-137">V době kompilace mají metody rozšíření vždy nižší prioritu než metody instance definované v samotném typu.</span><span class="sxs-lookup"><span data-stu-id="16a8a-137">At compile time, extension methods always have lower priority than instance methods defined in the type itself.</span></span> <span data-ttu-id="16a8a-138">Jinými slovy, pokud typ obsahuje metodu s názvem a máte `Process(int i)` metodu rozšíření se stejnou signaturou, kompilátor bude vždy svázán s metodou instance.</span><span class="sxs-lookup"><span data-stu-id="16a8a-138">In other words, if a type has a method named `Process(int i)`, and you have an extension method with the same signature, the compiler will always bind to the instance method.</span></span> <span data-ttu-id="16a8a-139">Pokud kompilátor narazí na vyvolání metody, nejprve vyhledá shodu v metodách instance tohoto typu.</span><span class="sxs-lookup"><span data-stu-id="16a8a-139">When the compiler encounters a method invocation, it first looks for a match in the type's instance methods.</span></span> <span data-ttu-id="16a8a-140">Pokud není nalezena žádná shoda, budou vyhledány jakékoli metody rozšíření, které jsou definovány pro daný typ, a budou připojeny k první vyhledané metodě rozšíření.</span><span class="sxs-lookup"><span data-stu-id="16a8a-140">If no match is found, it will search for any extension methods that are defined for the type, and bind to the first extension method that it finds.</span></span> <span data-ttu-id="16a8a-141">Následující příklad znázorňuje, jakým způsobem kompilátor určuje, se kterou metodou rozšíření nebo metodou instance má vytvořit vazbu.</span><span class="sxs-lookup"><span data-stu-id="16a8a-141">The following example demonstrates how the compiler determines which extension method or instance method to bind to.</span></span>

## <a name="example"></a><span data-ttu-id="16a8a-142">Příklad</span><span class="sxs-lookup"><span data-stu-id="16a8a-142">Example</span></span>

<span data-ttu-id="16a8a-143">Následující příklad znázorňuje pravidla, které u kompilátoru jazyka C# určují, zda vytvořit vazbu volání metody s metodou instance v rámci typu, nebo s metodou rozšíření.</span><span class="sxs-lookup"><span data-stu-id="16a8a-143">The following example demonstrates the rules that the C# compiler follows in determining whether to bind a method call to an instance method on the type, or to an extension method.</span></span> <span data-ttu-id="16a8a-144">Statická třída `Extensions` obsahuje metody rozšíření definované pro jakýkoli typ, který implementuje `IMyInterface` .</span><span class="sxs-lookup"><span data-stu-id="16a8a-144">The static class `Extensions` contains extension methods defined for any type that implements `IMyInterface`.</span></span> <span data-ttu-id="16a8a-145">Třídy `A` , `B` a `C` implementují rozhraní.</span><span class="sxs-lookup"><span data-stu-id="16a8a-145">Classes `A`, `B`, and `C` all implement the interface.</span></span>

<span data-ttu-id="16a8a-146">`MethodB`Metoda rozšíření se nikdy nevolá, protože její název a signatura se přesně shodují s metodami, které už jsou implementované třídami.</span><span class="sxs-lookup"><span data-stu-id="16a8a-146">The `MethodB` extension method is never called because its name and signature exactly match methods already implemented by the classes.</span></span>

<span data-ttu-id="16a8a-147">Pokud kompilátor nemůže najít metodu instance s vyhovující signaturou, bude vytvořena vazba s vyhovující metodou rozšíření, pokud taková existuje.</span><span class="sxs-lookup"><span data-stu-id="16a8a-147">When the compiler can't find an instance method with a matching signature, it will bind to a matching extension method if one exists.</span></span>

[!code-csharp[csProgGuideExtensionMethods#5](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExtensionMethods/cs/extensionmethods.cs#5)]

## <a name="common-usage-patterns"></a><span data-ttu-id="16a8a-148">Běžné vzorce použití</span><span class="sxs-lookup"><span data-stu-id="16a8a-148">Common Usage Patterns</span></span>

### <a name="collection-functionality"></a><span data-ttu-id="16a8a-149">Funkce kolekce</span><span class="sxs-lookup"><span data-stu-id="16a8a-149">Collection Functionality</span></span>

<span data-ttu-id="16a8a-150">V minulosti bylo běžné vytvořit "třídy kolekcí", které implementovaly <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType> rozhraní pro daný typ a obsahovaly funkce, které se v kolekcích daného typu jednaly.</span><span class="sxs-lookup"><span data-stu-id="16a8a-150">In the past, it was common to create "Collection Classes" that implemented the <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType> interface for a given type and contained functionality that acted on collections of that type.</span></span> <span data-ttu-id="16a8a-151">Přestože při vytváření tohoto typu objektu kolekce není nic špatné, je možné dosáhnout stejné funkčnosti pomocí rozšíření na <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="16a8a-151">While there's nothing wrong with creating this type of collection object, the same functionality can be achieved by using an extension on the <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType>.</span></span> <span data-ttu-id="16a8a-152">Rozšíření mají výhodu povolit volání funkce z jakékoli kolekce, jako je například <xref:System.Array?displayProperty=nameWithType> nebo <xref:System.Collections.Generic.List%601?displayProperty=nameWithType> , která implementuje <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType> Tento typ.</span><span class="sxs-lookup"><span data-stu-id="16a8a-152">Extensions have the advantage of allowing the functionality to be called from any collection such as an <xref:System.Array?displayProperty=nameWithType> or <xref:System.Collections.Generic.List%601?displayProperty=nameWithType> that implements <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType> on that type.</span></span> <span data-ttu-id="16a8a-153">Příklad tohoto použití pole typu Int32 najdete [výše v tomto článku](#orderby-example).</span><span class="sxs-lookup"><span data-stu-id="16a8a-153">An example of this using an Array of Int32 can be found [earlier in this article](#orderby-example).</span></span>

### <a name="layer-specific-functionality"></a><span data-ttu-id="16a8a-154">Funkce specifické pro vrstvu</span><span class="sxs-lookup"><span data-stu-id="16a8a-154">Layer-Specific Functionality</span></span>

<span data-ttu-id="16a8a-155">Při použití průsvitek nebo jiného návrhu vrstvené aplikace je běžné mít sadu entit domény nebo Přenos dat objekty, které lze použít ke komunikaci napříč hranicemi aplikací.</span><span class="sxs-lookup"><span data-stu-id="16a8a-155">When using an Onion Architecture or other layered application design, it's common to have a set of Domain Entities or Data Transfer Objects that can be used to communicate across application boundaries.</span></span> <span data-ttu-id="16a8a-156">Tyto objekty obecně neobsahují žádné funkce nebo pouze minimální funkce, které se vztahují na všechny vrstvy aplikace.</span><span class="sxs-lookup"><span data-stu-id="16a8a-156">These objects generally contain no functionality, or only minimal functionality that applies to all layers of the application.</span></span> <span data-ttu-id="16a8a-157">Metody rozšíření lze použít k přidání funkcionality, která je specifická pro každou vrstvu aplikace bez načítání objektu mimo jiné, než je potřeba nebo žádoucí v jiných vrstvách.</span><span class="sxs-lookup"><span data-stu-id="16a8a-157">Extension methods can be used to add functionality that is specific to each application layer without loading the object down with methods not needed or wanted in other layers.</span></span>

```csharp
public class DomainEntity
{
    public int Id { get; set; }
    public string FirstName { get; set; }
    public string LastName { get; set; }
}

static class DomainEntityExtensions
{
    static string FullName(this DomainEntity value)
        => $"{value.FirstName} {value.LastName}";
}
```

### <a name="extending-predefined-types"></a><span data-ttu-id="16a8a-158">Rozšíření předdefinovaných typů</span><span class="sxs-lookup"><span data-stu-id="16a8a-158">Extending Predefined Types</span></span>

<span data-ttu-id="16a8a-159">Místo vytváření nových objektů, pokud je třeba vytvořit opakovaně použitelná funkčnost, můžeme často rozlišit existující typ, například typ .NET nebo CLR.</span><span class="sxs-lookup"><span data-stu-id="16a8a-159">Rather than creating new objects when reusable functionality needs to be created, we can often extend an existing type, such as a .NET or CLR type.</span></span> <span data-ttu-id="16a8a-160">Pokud například nepoužíváme rozšiřující metody, můžeme vytvořit `Engine` `Query` třídu nebo k provedení dotazu na SQL Server, která může být volána z více míst v našem kódu.</span><span class="sxs-lookup"><span data-stu-id="16a8a-160">As an example, if we don't use extension methods, we might create an `Engine` or `Query` class to do the work of executing a query on a SQL Server that may be called from multiple places in our code.</span></span> <span data-ttu-id="16a8a-161">Místo toho však můžeme rozšířit <xref:System.Data.SqlClient.SqlConnection?displayProperty=nameWithType> třídu pomocí rozšiřujících metod, aby bylo možné tento dotaz provést odkudkoli odkudkoli, máme připojení k SQL Server.</span><span class="sxs-lookup"><span data-stu-id="16a8a-161">However we can instead extend the <xref:System.Data.SqlClient.SqlConnection?displayProperty=nameWithType> class using extension methods to perform that query from anywhere we have a connection to a SQL Server.</span></span> <span data-ttu-id="16a8a-162">Další příklady mohou být přidány do třídy společné funkce <xref:System.String?displayProperty=nameWithType> , rozšiřuje možnosti zpracování dat <xref:System.IO.File?displayProperty=nameWithType> objektů a a <xref:System.IO.Stream?displayProperty=nameWithType> <xref:System.Exception?displayProperty=nameWithType> objekty pro konkrétní funkce zpracování chyb.</span><span class="sxs-lookup"><span data-stu-id="16a8a-162">Other examples might be to add common functionality to the <xref:System.String?displayProperty=nameWithType> class, extend the data processing capabilities of the <xref:System.IO.File?displayProperty=nameWithType> and <xref:System.IO.Stream?displayProperty=nameWithType> objects, and <xref:System.Exception?displayProperty=nameWithType> objects for specific error handling functionality.</span></span> <span data-ttu-id="16a8a-163">Tyto typy případů použití jsou omezené jenom představivost a dobrým smyslem.</span><span class="sxs-lookup"><span data-stu-id="16a8a-163">These types of use-cases are limited only by your imagination and good sense.</span></span>

<span data-ttu-id="16a8a-164">Rozšíření předdefinovaných typů může být obtížné s `struct` typy, protože jsou předány podle hodnoty metodám.</span><span class="sxs-lookup"><span data-stu-id="16a8a-164">Extending predefined types can be difficult with `struct` types because they're passed by value to methods.</span></span> <span data-ttu-id="16a8a-165">To znamená, že všechny změny struktury jsou provedeny na kopii struktury.</span><span class="sxs-lookup"><span data-stu-id="16a8a-165">That means any changes to the struct are made to a copy of the struct.</span></span> <span data-ttu-id="16a8a-166">Tyto změny nejsou po ukončení metody rozšíření viditelné.</span><span class="sxs-lookup"><span data-stu-id="16a8a-166">Those changes aren't visible once the extension method exits.</span></span> <span data-ttu-id="16a8a-167">Počínaje jazykem C# 7,2 můžete přidat `ref` Modifikátor k prvnímu argumentu metody rozšíření.</span><span class="sxs-lookup"><span data-stu-id="16a8a-167">Beginning with C# 7.2, you can add the `ref` modifier to the first argument of an extension method.</span></span> <span data-ttu-id="16a8a-168">Přidání `ref` modifikátoru znamená, že první argument je předán odkazem.</span><span class="sxs-lookup"><span data-stu-id="16a8a-168">Adding the `ref` modifier means the first argument is passed by reference.</span></span> <span data-ttu-id="16a8a-169">To umožňuje psát rozšiřující metody, které mění stav rozšířené struktury.</span><span class="sxs-lookup"><span data-stu-id="16a8a-169">This enables you to write extension methods that change the state of the struct being extended.</span></span>

## <a name="general-guidelines"></a><span data-ttu-id="16a8a-170">Obecné pokyny</span><span class="sxs-lookup"><span data-stu-id="16a8a-170">General Guidelines</span></span>

<span data-ttu-id="16a8a-171">I když je stále vhodnější přidat funkce úpravou kódu objektu nebo odvozením nového typu pokaždé, když je to přijatelné a možné, je nutné, aby se metody rozšíření staly zásadní volbou pro vytváření opakovaně použitelných funkcí v rámci ekosystému .NET.</span><span class="sxs-lookup"><span data-stu-id="16a8a-171">While it's still considered preferable to add functionality by modifying an object's code or deriving a new type whenever it's reasonable and possible to do so, extension methods have become a crucial option for creating reusable functionality throughout the .NET ecosystem.</span></span> <span data-ttu-id="16a8a-172">Pro případy, kdy původní zdroj není pod vaším ovládacím prvkem, když je odvozený objekt nevhodný nebo nedostupný nebo pokud by se funkce nemusely vystavit nad rámec příslušného rozsahu, jsou rozšiřující metody vynikající volbou.</span><span class="sxs-lookup"><span data-stu-id="16a8a-172">For those occasions when the original source isn't under your control, when a derived object is inappropriate or impossible, or when the functionality shouldn't be exposed beyond its applicable scope, Extension methods are an excellent choice.</span></span>

<span data-ttu-id="16a8a-173">Další informace o odvozených typech naleznete v tématu [Dědičnost](./inheritance.md).</span><span class="sxs-lookup"><span data-stu-id="16a8a-173">For more information on derived types, see [Inheritance](./inheritance.md).</span></span>

<span data-ttu-id="16a8a-174">Při použití rozšiřující metody k rozšíření typu, jehož zdrojový kód neovládáte, spustíte riziko, že změna implementace typu způsobí, že vaše metoda rozšíření bude mít za následek přerušení.</span><span class="sxs-lookup"><span data-stu-id="16a8a-174">When using an extension method to extend a type whose source code you aren't in control of, you run the risk that a change in the implementation of the type will cause your extension method to break.</span></span>

<span data-ttu-id="16a8a-175">Pokud implementujete metody rozšíření pro daný typ, mějte na paměti následující body:</span><span class="sxs-lookup"><span data-stu-id="16a8a-175">If you do implement extension methods for a given type, remember the following points:</span></span>

- <span data-ttu-id="16a8a-176">Metoda rozšíření nebude nikdy volána, pokud má stejnou signaturu jako metoda definovaná v typu.</span><span class="sxs-lookup"><span data-stu-id="16a8a-176">An extension method will never be called if it has the same signature as a method defined in the type.</span></span>
- <span data-ttu-id="16a8a-177">Dále jsou metody rozšíření přeneseny do rozsahu na úrovni oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="16a8a-177">Extension methods are brought into scope at the namespace level.</span></span> <span data-ttu-id="16a8a-178">Například pokud máte více statických tříd, které obsahují metody rozšíření v jednom oboru názvů s názvem `Extensions` , budou všechny převedeny do rozsahu podle `using Extensions;` direktivy.</span><span class="sxs-lookup"><span data-stu-id="16a8a-178">For example, if you have multiple static classes that contain extension methods in a single namespace named `Extensions`, they'll all be brought into scope by the `using Extensions;` directive.</span></span>

<span data-ttu-id="16a8a-179">Chcete-li zamezit zvýšení čísla verze sestavení, neměli byste pro implementovanou knihovnu metody rozšíření používat.</span><span class="sxs-lookup"><span data-stu-id="16a8a-179">For a class library that you implemented, you shouldn't use extension methods to avoid incrementing the version number of an assembly.</span></span> <span data-ttu-id="16a8a-180">Pokud chcete přidat významné funkce do knihovny, pro kterou vlastníte zdrojový kód, postupujte podle pokynů rozhraní .NET pro správu verzí sestavení.</span><span class="sxs-lookup"><span data-stu-id="16a8a-180">If you want to add significant functionality to a library for which you own the source code, follow the .NET guidelines for assembly versioning.</span></span> <span data-ttu-id="16a8a-181">Další informace naleznete v tématu [Správa verzí sestavení](../../../standard/assembly/versioning.md).</span><span class="sxs-lookup"><span data-stu-id="16a8a-181">For more information, see [Assembly Versioning](../../../standard/assembly/versioning.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="16a8a-182">Viz také</span><span class="sxs-lookup"><span data-stu-id="16a8a-182">See also</span></span>

- [<span data-ttu-id="16a8a-183">Průvodce programováním v C#</span><span class="sxs-lookup"><span data-stu-id="16a8a-183">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="16a8a-184">Ukázky paralelního programování (zahrnuje mnoho ukázkových metod rozšíření)</span><span class="sxs-lookup"><span data-stu-id="16a8a-184">Parallel Programming Samples (these include many example extension methods)</span></span>](/samples/browse/?products=dotnet-core%2Cdotnet-standard&term=parallel)
- [<span data-ttu-id="16a8a-185">Výrazy lambda</span><span class="sxs-lookup"><span data-stu-id="16a8a-185">Lambda Expressions</span></span>](../../language-reference/operators/lambda-expressions.md)
- [<span data-ttu-id="16a8a-186">Přehled standardních operátorů dotazu</span><span class="sxs-lookup"><span data-stu-id="16a8a-186">Standard Query Operators Overview</span></span>](../concepts/linq/standard-query-operators-overview.md)
- [<span data-ttu-id="16a8a-187">Pravidla převodu pro parametry instance a jejich dopad</span><span class="sxs-lookup"><span data-stu-id="16a8a-187">Conversion rules for Instance parameters and their impact</span></span>](https://docs.microsoft.com/archive/blogs/sreekarc/conversion-rules-for-instance-parameters-and-their-impact)
- [<span data-ttu-id="16a8a-188">Interoperabilita metod rozšíření mezi jazyky</span><span class="sxs-lookup"><span data-stu-id="16a8a-188">Extension methods Interoperability between languages</span></span>](https://docs.microsoft.com/archive/blogs/sreekarc/extension-methods-interoperability-between-languages)
- [<span data-ttu-id="16a8a-189">Metody rozšíření a Delegáti curryfikované</span><span class="sxs-lookup"><span data-stu-id="16a8a-189">Extension methods and Curried Delegates</span></span>](https://docs.microsoft.com/archive/blogs/sreekarc/extension-methods-and-curried-delegates)
- [<span data-ttu-id="16a8a-190">Vazba metody rozšíření a zasílání zpráv o chybách</span><span class="sxs-lookup"><span data-stu-id="16a8a-190">Extension method Binding and Error reporting</span></span>](https://docs.microsoft.com/archive/blogs/sreekarc/extension-method-binding-and-error-reporting)
