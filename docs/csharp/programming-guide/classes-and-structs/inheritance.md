---
title: Dědičnost – průvodce programováním jazyka C#
ms.date: 02/07/2020
helpviewer_keywords:
- abstract methods [C#]
- abstract classes [C#]
- inheritance [C#]
- derived classes [C#]
- virtual methods [C#]
- C# language, inheritance
ms.assetid: 81d64ee4-50f9-4d6c-a8dc-257c348d2eea
ms.openlocfilehash: 448b1695a4afc50f4afa20383e5fda280b9f12e9
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/14/2020
ms.locfileid: "77626656"
---
# <a name="inheritance-c-programming-guide"></a><span data-ttu-id="72093-102">Dědičnost (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="72093-102">Inheritance (C# Programming Guide)</span></span>

<span data-ttu-id="72093-103">Dědičnost, spolu s zapouzdření a polymorfismu, je jednou ze tří základních charakteristik objektově orientovaného programování.</span><span class="sxs-lookup"><span data-stu-id="72093-103">Inheritance, together with encapsulation and polymorphism, is one of the three primary characteristics of object-oriented programming.</span></span> <span data-ttu-id="72093-104">Dědičnost umožňuje vytvářet nové třídy, které znovu používají, rozšiřují a upravují chování definované v jiných třídách.</span><span class="sxs-lookup"><span data-stu-id="72093-104">Inheritance enables you to create new classes that reuse, extend, and modify the behavior defined in other classes.</span></span> <span data-ttu-id="72093-105">Třída, jejíž členové jsou zděděni, se nazývá *základní třída*a třída, která tyto členy dědí, se nazývá *odvozená třída*.</span><span class="sxs-lookup"><span data-stu-id="72093-105">The class whose members are inherited is called the *base class*, and the class that inherits those members is called the *derived class*.</span></span> <span data-ttu-id="72093-106">Odvozená třída může mít pouze jednu přímou základní třídu.</span><span class="sxs-lookup"><span data-stu-id="72093-106">A derived class can have only one direct base class.</span></span> <span data-ttu-id="72093-107">Dědičnost je však přenositá.</span><span class="sxs-lookup"><span data-stu-id="72093-107">However, inheritance is transitive.</span></span> <span data-ttu-id="72093-108">Pokud `ClassC` je odvozen `ClassB`od `ClassB` , a `ClassA` `ClassC` je odvozen od `ClassB` , `ClassA`dědí členy deklarované v a .</span><span class="sxs-lookup"><span data-stu-id="72093-108">If `ClassC` is derived from `ClassB`, and `ClassB` is derived from `ClassA`, `ClassC` inherits the members declared in `ClassB` and `ClassA`.</span></span>

> [!NOTE]
> <span data-ttu-id="72093-109">Struktury nepodporují dědičnost, ale mohou implementovat rozhraní.</span><span class="sxs-lookup"><span data-stu-id="72093-109">Structs do not support inheritance, but they can implement interfaces.</span></span> <span data-ttu-id="72093-110">Další informace naleznete v [tématu Rozhraní](../interfaces/index.md).</span><span class="sxs-lookup"><span data-stu-id="72093-110">For more information, see [Interfaces](../interfaces/index.md).</span></span>

<span data-ttu-id="72093-111">Koncepčně odvozené třídy je specializace základní třídy.</span><span class="sxs-lookup"><span data-stu-id="72093-111">Conceptually, a derived class is a specialization of the base class.</span></span> <span data-ttu-id="72093-112">Například pokud máte základní `Animal`třídu , můžete mít jednu `Mammal` odvozenou třídu, `Reptile`která je pojmenována a další odvozené třídy, která je pojmenována .</span><span class="sxs-lookup"><span data-stu-id="72093-112">For example, if you have a base class `Animal`, you might have one derived class that is named `Mammal` and another derived class that is named `Reptile`.</span></span> <span data-ttu-id="72093-113">A `Mammal` je `Animal`, `Reptile` a `Animal`a je , ale každá odvozená třída představuje různé specializace základní třídy.</span><span class="sxs-lookup"><span data-stu-id="72093-113">A `Mammal` is an `Animal`, and a `Reptile` is an `Animal`, but each derived class represents different specializations of the base class.</span></span>

<span data-ttu-id="72093-114">Deklarace rozhraní může definovat výchozí implementaci pro své členy.</span><span class="sxs-lookup"><span data-stu-id="72093-114">Interface declarations may define a default implementation for its members.</span></span> <span data-ttu-id="72093-115">Tyto implementace jsou zděděny odvozené rozhraní a třídy, které implementují tato rozhraní.</span><span class="sxs-lookup"><span data-stu-id="72093-115">These implementations are inherited by derived interfaces, and by classes that implement those interfaces.</span></span> <span data-ttu-id="72093-116">Další informace o výchozích metodách rozhraní naleznete v článku o [rozhraních](../../language-reference/keywords/interface.md) v části referenční jazyk.</span><span class="sxs-lookup"><span data-stu-id="72093-116">For more information on default interface methods, see the article on [interfaces](../../language-reference/keywords/interface.md) in the language reference section.</span></span>

<span data-ttu-id="72093-117">Když definujete třídu, která má být odvozena z jiné třídy, odvozená třída implicitně získá všechny členy základní třídy, s výjimkou jejích konstruktorů a finalizačních metod.</span><span class="sxs-lookup"><span data-stu-id="72093-117">When you define a class to derive from another class, the derived class implicitly gains all the members of the base class, except for its constructors and finalizers.</span></span> <span data-ttu-id="72093-118">Odvozené třídy znovu používá kód v základní třídě bez nutnosti znovu implementovat.</span><span class="sxs-lookup"><span data-stu-id="72093-118">The derived class reuses the code in the base class without having to reimplement it.</span></span> <span data-ttu-id="72093-119">Můžete přidat další členy v odvozené třídě.</span><span class="sxs-lookup"><span data-stu-id="72093-119">You can add more members in the derived class.</span></span> <span data-ttu-id="72093-120">Odvozená třída rozšiřuje funkce základní třídy.</span><span class="sxs-lookup"><span data-stu-id="72093-120">The derived class extends the functionality of the base class.</span></span>

<span data-ttu-id="72093-121">Následující obrázek znázorňuje třídu, `WorkItem` která představuje položku práce v některém obchodním procesu.</span><span class="sxs-lookup"><span data-stu-id="72093-121">The following illustration shows a class `WorkItem` that represents an item of work in some business process.</span></span> <span data-ttu-id="72093-122">Stejně jako všechny třídy, odvozuje a <xref:System.Object?displayProperty=nameWithType> dědí všechny své metody.</span><span class="sxs-lookup"><span data-stu-id="72093-122">Like all classes, it derives from <xref:System.Object?displayProperty=nameWithType> and inherits all its methods.</span></span> <span data-ttu-id="72093-123">`WorkItem`přidává pět vlastních členů.</span><span class="sxs-lookup"><span data-stu-id="72093-123">`WorkItem` adds five members of its own.</span></span> <span data-ttu-id="72093-124">Tyto členy zahrnují konstruktor, protože konstruktory nejsou zděděny.</span><span class="sxs-lookup"><span data-stu-id="72093-124">These members include a constructor, because constructors aren't inherited.</span></span> <span data-ttu-id="72093-125">Třída `ChangeRequest` dědí `WorkItem` z a představuje určitý druh pracovní položky.</span><span class="sxs-lookup"><span data-stu-id="72093-125">Class `ChangeRequest` inherits from `WorkItem` and represents a particular kind of work item.</span></span> <span data-ttu-id="72093-126">`ChangeRequest`přidá další dva členy k členům, které dědí od `WorkItem` a od <xref:System.Object>.</span><span class="sxs-lookup"><span data-stu-id="72093-126">`ChangeRequest` adds two more members to the members that it inherits from `WorkItem` and from <xref:System.Object>.</span></span> <span data-ttu-id="72093-127">Musí přidat vlastní konstruktor a také `originalItemID`přidává .</span><span class="sxs-lookup"><span data-stu-id="72093-127">It must add its own constructor, and it also adds `originalItemID`.</span></span> <span data-ttu-id="72093-128">Vlastnost `originalItemID` umožňuje `ChangeRequest` instance být přidruženy `WorkItem` k originálu, na které se vztahuje požadavek na změnu.</span><span class="sxs-lookup"><span data-stu-id="72093-128">Property `originalItemID` enables the `ChangeRequest` instance to be associated with the original `WorkItem` to which the change request applies.</span></span>

![Diagram, který zobrazuje dědičnost třídy](./media/inheritance/class-inheritance-diagram.png)

<span data-ttu-id="72093-130">Následující příklad ukazuje, jak jsou vyjádřeny vztahy třídy demonstrované na předchozím obrázku v c#.</span><span class="sxs-lookup"><span data-stu-id="72093-130">The following example shows how the class relationships demonstrated in the previous illustration are expressed in C#.</span></span> <span data-ttu-id="72093-131">Příklad také `WorkItem` ukazuje, jak přepíše virtuální metodu <xref:System.Object.ToString%2A?displayProperty=nameWithType>a jak `ChangeRequest` třída dědí `WorkItem` implementaci metody.</span><span class="sxs-lookup"><span data-stu-id="72093-131">The example also shows how `WorkItem` overrides the virtual method <xref:System.Object.ToString%2A?displayProperty=nameWithType>, and how the `ChangeRequest` class inherits the `WorkItem` implementation of the method.</span></span> <span data-ttu-id="72093-132">První blok definuje třídy:</span><span class="sxs-lookup"><span data-stu-id="72093-132">The first block defines the classes:</span></span>

[!code-csharp[DefineClasses](~/samples/snippets/csharp/objectoriented/inheritance.cs#Classes)]

<span data-ttu-id="72093-133">Tento další blok ukazuje, jak používat základní a odvozené třídy:</span><span class="sxs-lookup"><span data-stu-id="72093-133">This next block shows how to use the base and derived classes:</span></span>

[!code-csharp[UseClasses](~/samples/snippets/csharp/objectoriented/inheritance.cs#UseClasses)]

## <a name="abstract-and-virtual-methods"></a><span data-ttu-id="72093-134">Abstraktní a virtuální metody</span><span class="sxs-lookup"><span data-stu-id="72093-134">Abstract and virtual methods</span></span>

<span data-ttu-id="72093-135">Když základní třída deklaruje metodu [`virtual`](../../language-reference/keywords/virtual.md) [`override`](../../language-reference/keywords/override.md) jako , odvozená třída může metoda s vlastní implementací.</span><span class="sxs-lookup"><span data-stu-id="72093-135">When a base class declares a method as [`virtual`](../../language-reference/keywords/virtual.md), a derived class can [`override`](../../language-reference/keywords/override.md) the method with its own implementation.</span></span> <span data-ttu-id="72093-136">Pokud základní třída deklaruje člen jako [`abstract`](../../language-reference/keywords/abstract.md), tato metoda musí být přepsána v jakékoli neabstraktní třídě, která přímo dědí z této třídy.</span><span class="sxs-lookup"><span data-stu-id="72093-136">If a base class declares a member as [`abstract`](../../language-reference/keywords/abstract.md), that method must be overridden in any non-abstract class that directly inherits from that class.</span></span> <span data-ttu-id="72093-137">Pokud je odvozená třída sama abstraktní, dědí abstraktní členy bez jejich implementace.</span><span class="sxs-lookup"><span data-stu-id="72093-137">If a derived class is itself abstract, it inherits abstract members without implementing them.</span></span> <span data-ttu-id="72093-138">Abstraktní a virtuální členové jsou základem polymorfismu, který je druhou primární charakteristikou objektově orientovaného programování.</span><span class="sxs-lookup"><span data-stu-id="72093-138">Abstract and virtual members are the basis for polymorphism, which is the second primary characteristic of object-oriented programming.</span></span> <span data-ttu-id="72093-139">Další informace naleznete [v tématu Polymorfismus](./polymorphism.md).</span><span class="sxs-lookup"><span data-stu-id="72093-139">For more information, see [Polymorphism](./polymorphism.md).</span></span>

## <a name="abstract-base-classes"></a><span data-ttu-id="72093-140">Abstraktní základní třídy</span><span class="sxs-lookup"><span data-stu-id="72093-140">Abstract base classes</span></span>

<span data-ttu-id="72093-141">Můžete deklarovat třídu jako [abstraktní,](../../language-reference/keywords/abstract.md) pokud chcete zabránit přímé konkretizovat pomocí [nového](../../language-reference/operators/new-operator.md) operátoru.</span><span class="sxs-lookup"><span data-stu-id="72093-141">You can declare a class as [abstract](../../language-reference/keywords/abstract.md) if you want to prevent direct instantiation by using the [new](../../language-reference/operators/new-operator.md) operator.</span></span> <span data-ttu-id="72093-142">Abstraktní třídu lze použít pouze v případě, že je z ní odvozena nová třída.</span><span class="sxs-lookup"><span data-stu-id="72093-142">An abstract class can be used only if a new class is derived from it.</span></span> <span data-ttu-id="72093-143">Abstraktní třída může obsahovat jeden nebo více podpisů metody, které jsou samy deklarovány jako abstraktní.</span><span class="sxs-lookup"><span data-stu-id="72093-143">An abstract class can contain one or more method signatures that themselves are declared as abstract.</span></span> <span data-ttu-id="72093-144">Tyto podpisy určují parametry a vrácenou hodnotu, ale nemají žádnou implementaci (tělo metody).</span><span class="sxs-lookup"><span data-stu-id="72093-144">These signatures specify the parameters and return value but have no implementation (method body).</span></span> <span data-ttu-id="72093-145">Abstraktní třída nemusí obsahovat abstraktní členy; pokud však třída obsahuje abstraktní člen, musí být samotná třída deklarována jako abstraktní.</span><span class="sxs-lookup"><span data-stu-id="72093-145">An abstract class doesn't have to contain abstract members; however, if a class does contain an abstract member, the class itself must be declared as abstract.</span></span> <span data-ttu-id="72093-146">Odvozené třídy, které nejsou abstraktní samy o sobě musí poskytnout implementaci pro všechny abstraktní metody z abstraktní základní třídy.</span><span class="sxs-lookup"><span data-stu-id="72093-146">Derived classes that aren't abstract themselves must provide the implementation for any abstract methods from an abstract base class.</span></span> <span data-ttu-id="72093-147">Další informace naleznete [v tématu Abstract and Sealed Classes and Class Members](abstract-and-sealed-classes-and-class-members.md).</span><span class="sxs-lookup"><span data-stu-id="72093-147">For more information, see [Abstract and Sealed Classes and Class Members](abstract-and-sealed-classes-and-class-members.md).</span></span>

## <a name="interfaces"></a><span data-ttu-id="72093-148">Rozhraní</span><span class="sxs-lookup"><span data-stu-id="72093-148">Interfaces</span></span>

<span data-ttu-id="72093-149">*Rozhraní* je typ odkazu, který definuje sadu členů.</span><span class="sxs-lookup"><span data-stu-id="72093-149">An *interface* is a reference type that defines a set of members.</span></span> <span data-ttu-id="72093-150">Všechny třídy a struktury, které implementují toto rozhraní musí implementovat tuto sadu členů.</span><span class="sxs-lookup"><span data-stu-id="72093-150">All classes and structs that implement that interface must implement that set of members.</span></span> <span data-ttu-id="72093-151">Rozhraní může definovat výchozí implementaci pro některé nebo všechny tyto členy.</span><span class="sxs-lookup"><span data-stu-id="72093-151">An interface may define a default implementation for any or all of these members.</span></span> <span data-ttu-id="72093-152">Třída může implementovat více rozhraní, i když může být odvozena pouze z jedné přímé základní třídy.</span><span class="sxs-lookup"><span data-stu-id="72093-152">A class can implement multiple interfaces even though it can derive from only a single direct base class.</span></span>

<span data-ttu-id="72093-153">Rozhraní se používají k definování specifických možností pro třídy, které nemusí mít nutně vztah "je a".</span><span class="sxs-lookup"><span data-stu-id="72093-153">Interfaces are used to define specific capabilities for classes that don't necessarily have an "is a" relationship.</span></span> <span data-ttu-id="72093-154">Například <xref:System.IEquatable%601?displayProperty=nameWithType> rozhraní může být implementováno libovolnou třídou nebo strukturou k určení, zda jsou dva objekty typu ekvivalentní (ale typ definuje ekvivalenci).</span><span class="sxs-lookup"><span data-stu-id="72093-154">For example, the <xref:System.IEquatable%601?displayProperty=nameWithType> interface can be implemented by any class or struct to determine whether two objects of the type are equivalent (however the type defines equivalence).</span></span> <span data-ttu-id="72093-155"><xref:System.IEquatable%601>neznamená stejný druh vztahu "je a", který existuje mezi základní třídou a odvozenou `Mammal` třídou (například a `Animal`je).</span><span class="sxs-lookup"><span data-stu-id="72093-155"><xref:System.IEquatable%601> doesn't imply the same kind of "is a" relationship that exists between a base class and a derived class (for example, a `Mammal` is an `Animal`).</span></span> <span data-ttu-id="72093-156">Další informace naleznete v [tématu Rozhraní](../interfaces/index.md).</span><span class="sxs-lookup"><span data-stu-id="72093-156">For more information, see [Interfaces](../interfaces/index.md).</span></span>

## <a name="preventing-further-derivation"></a><span data-ttu-id="72093-157">Prevence dalšího odvození</span><span class="sxs-lookup"><span data-stu-id="72093-157">Preventing further derivation</span></span>  

<span data-ttu-id="72093-158">Třída může zabránit tomu, aby z ní dědili jiné třídy nebo od [`sealed`](../../language-reference/keywords/sealed.md)některého ze svých členů tím, že deklaruje sebe nebo člena jako .</span><span class="sxs-lookup"><span data-stu-id="72093-158">A class can prevent other classes from inheriting from it, or from any of its members, by declaring itself or the member as [`sealed`](../../language-reference/keywords/sealed.md).</span></span> <span data-ttu-id="72093-159">Další informace naleznete [v tématu Abstract and Sealed Classes and Class Members](./abstract-and-sealed-classes-and-class-members.md).</span><span class="sxs-lookup"><span data-stu-id="72093-159">For more information, see [Abstract and Sealed Classes and Class Members](./abstract-and-sealed-classes-and-class-members.md).</span></span>

## <a name="derived-class-hiding-of-base-class-members"></a><span data-ttu-id="72093-160">Odvozené třídy skrývá ní členy základní třídy</span><span class="sxs-lookup"><span data-stu-id="72093-160">Derived class hiding of base class members</span></span>  

<span data-ttu-id="72093-161">Odvozená třída může skrýt členy základní třídy deklarováním členů se stejným názvem a podpisem.</span><span class="sxs-lookup"><span data-stu-id="72093-161">A derived class can hide base class members by declaring members with the same name and signature.</span></span> <span data-ttu-id="72093-162">Modifikátor [`new`](../../language-reference/keywords/new-modifier.md) lze explicitně označit, že člen není určen k přepsání základního člena.</span><span class="sxs-lookup"><span data-stu-id="72093-162">The [`new`](../../language-reference/keywords/new-modifier.md) modifier can be used to explicitly indicate that the member isn't intended to be an override of the base member.</span></span> <span data-ttu-id="72093-163">Použití [`new`](../../language-reference/keywords/new-modifier.md) není nutné, ale upozornění kompilátoru bude [`new`](../../language-reference/keywords/new-modifier.md) generováno, pokud se nepoužívá.</span><span class="sxs-lookup"><span data-stu-id="72093-163">The use of [`new`](../../language-reference/keywords/new-modifier.md) isn't required, but a compiler warning will be generated if [`new`](../../language-reference/keywords/new-modifier.md) isn't used.</span></span> <span data-ttu-id="72093-164">Další informace naleznete [v tématu Správa verzí s přepsáním a novými klíčovými slovy](./versioning-with-the-override-and-new-keywords.md) a informace o [tom, kdy použít přepsání a nová klíčová slova](./knowing-when-to-use-override-and-new-keywords.md).</span><span class="sxs-lookup"><span data-stu-id="72093-164">For more information, see [Versioning with the Override and New Keywords](./versioning-with-the-override-and-new-keywords.md) and [Knowing When to Use Override and New Keywords](./knowing-when-to-use-override-and-new-keywords.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="72093-165">Viz také</span><span class="sxs-lookup"><span data-stu-id="72093-165">See also</span></span>

- [<span data-ttu-id="72093-166">Programovací příručka jazyka C#</span><span class="sxs-lookup"><span data-stu-id="72093-166">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="72093-167">Třídy a struky</span><span class="sxs-lookup"><span data-stu-id="72093-167">Classes and Structs</span></span>](./index.md)
- [<span data-ttu-id="72093-168">Třída</span><span class="sxs-lookup"><span data-stu-id="72093-168">class</span></span>](../../language-reference/keywords/class.md)
