---
title: Anonymní typy – Průvodce programováním v C#
description: Anonymní typy v jazyce C# zapouzdřují sadu vlastností jen pro čtení v objektu, aniž by bylo nutné explicitně definovat typ. Kompilátor vygeneruje název.
ms.date: 07/20/2015
helpviewer_keywords:
- anonymous types [C#]
- C# Language, anonymous types
ms.assetid: 59c9d7a4-3b0e-475e-b620-0ab86c088e9b
ms.openlocfilehash: ccdee1a86ab0c292721e69346cb7cedb03d4c28b
ms.sourcegitcommit: cf5a800a33de64d0aad6d115ffcc935f32375164
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/20/2020
ms.locfileid: "86474498"
---
# <a name="anonymous-types-c-programming-guide"></a><span data-ttu-id="f3c9f-104">Anonymní typy (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="f3c9f-104">Anonymous Types (C# Programming Guide)</span></span>

<span data-ttu-id="f3c9f-105">Anonymní typy poskytují pohodlný způsob, jak zapouzdřit sadu vlastností jen pro čtení do jednoho objektu bez nutnosti explicitně definovat typ jako první.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-105">Anonymous types provide a convenient way to encapsulate a set of read-only properties into a single object without having to explicitly define a type first.</span></span> <span data-ttu-id="f3c9f-106">Název typu je generován kompilátorem a není k dispozici na úrovni zdrojového kódu.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-106">The type name is generated by the compiler and is not available at the source code level.</span></span> <span data-ttu-id="f3c9f-107">Typ každé vlastnosti je odvozen kompilátorem.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-107">The type of each property is inferred by the compiler.</span></span>  
  
 <span data-ttu-id="f3c9f-108">Anonymní typy lze vytvořit pomocí operátoru [New](../../language-reference/operators/new-operator.md) spolu s inicializátorem objektu.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-108">You create anonymous types by using the [new](../../language-reference/operators/new-operator.md) operator together with an object initializer.</span></span> <span data-ttu-id="f3c9f-109">Další informace o inicializátorech objektů naleznete v tématu [Inicializátory objektů a kolekcí](./object-and-collection-initializers.md).</span><span class="sxs-lookup"><span data-stu-id="f3c9f-109">For more information about object initializers, see [Object and Collection Initializers](./object-and-collection-initializers.md).</span></span>  
  
 <span data-ttu-id="f3c9f-110">Následující příklad ukazuje anonymní typ, který je inicializován pomocí dvou vlastností s názvem `Amount` a `Message` .</span><span class="sxs-lookup"><span data-stu-id="f3c9f-110">The following example shows an anonymous type that is initialized with two properties named `Amount` and `Message`.</span></span>  
  
```csharp  
var v = new { Amount = 108, Message = "Hello" };  
  
// Rest the mouse pointer over v.Amount and v.Message in the following  
// statement to verify that their inferred types are int and string.  
Console.WriteLine(v.Amount + v.Message);  
```  
  
 <span data-ttu-id="f3c9f-111">Anonymní typy obvykle jsou používány v klauzuli [Select](../../language-reference/keywords/select-clause.md) výrazu dotazu pro vrácení podmnožiny vlastností z každého objektu ve zdrojové sekvenci.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-111">Anonymous types typically are used in the [select](../../language-reference/keywords/select-clause.md) clause of a query expression to return a subset of the properties from each object in the source sequence.</span></span> <span data-ttu-id="f3c9f-112">Další informace o dotazech naleznete [v tématu LINQ v jazyce C#](../../linq/index.md).</span><span class="sxs-lookup"><span data-stu-id="f3c9f-112">For more information about queries, see [LINQ in C#](../../linq/index.md).</span></span>  
  
 <span data-ttu-id="f3c9f-113">Anonymní typy obsahují jednu nebo více veřejných vlastností jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-113">Anonymous types contain one or more public read-only properties.</span></span> <span data-ttu-id="f3c9f-114">Žádné jiné druhy členů třídy, jako jsou metody nebo události, jsou platné.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-114">No other kinds of class members, such as methods or events, are valid.</span></span> <span data-ttu-id="f3c9f-115">Výraz použitý k inicializaci vlastnosti nemůže být `null` , anonymní funkce nebo typu ukazatele.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-115">The expression that is used to initialize a property cannot be `null`, an anonymous function, or a pointer type.</span></span>  
  
 <span data-ttu-id="f3c9f-116">Nejběžnějším scénářem je inicializace anonymního typu s vlastnostmi z jiného typu.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-116">The most common scenario is to initialize an anonymous type with properties from another type.</span></span> <span data-ttu-id="f3c9f-117">V následujícím příkladu Předpokládejme, že existuje třída s názvem `Product` .</span><span class="sxs-lookup"><span data-stu-id="f3c9f-117">In the following example, assume that a class exists that is named `Product`.</span></span> <span data-ttu-id="f3c9f-118">Třída `Product` zahrnuje `Color` a `Price` vlastnosti spolu s dalšími vlastnostmi, které vás zajímají.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-118">Class `Product` includes `Color` and `Price` properties, together with other properties that you are not interested in.</span></span> <span data-ttu-id="f3c9f-119">Proměnná `products` je kolekcí `Product` objektů.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-119">Variable `products` is a collection of `Product` objects.</span></span> <span data-ttu-id="f3c9f-120">Deklarace anonymního typu začíná `new` klíčovým slovem.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-120">The anonymous type declaration starts with the `new` keyword.</span></span> <span data-ttu-id="f3c9f-121">Deklarace inicializuje nový typ, který používá pouze dvě vlastnosti `Product` .</span><span class="sxs-lookup"><span data-stu-id="f3c9f-121">The declaration initializes a new type that uses only two properties from `Product`.</span></span> <span data-ttu-id="f3c9f-122">To způsobí, že se v dotazu vrátí menší množství dat.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-122">This causes a smaller amount of data to be returned in the query.</span></span>  
  
 <span data-ttu-id="f3c9f-123">Pokud v anonymním typu nezadáte názvy členů, kompilátor poskytne členům anonymního typu stejný název jako vlastnost, která je použita k jejich inicializaci.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-123">If you do not specify member names in the anonymous type, the compiler gives the anonymous type members the same name as the property being used to initialize them.</span></span> <span data-ttu-id="f3c9f-124">Je nutné zadat název vlastnosti, která je inicializována výrazem, jak je znázorněno v předchozím příkladu.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-124">You must provide a name for a property that is being initialized with an expression, as shown in the previous example.</span></span> <span data-ttu-id="f3c9f-125">V následujícím příkladu jsou názvy vlastností anonymního typu `Color` a `Price` .</span><span class="sxs-lookup"><span data-stu-id="f3c9f-125">In the following example, the names of the properties of the anonymous type are `Color` and `Price`.</span></span>  
  
 [!code-csharp[csRef30Features#81](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csRef30Features/CS/csref30.cs#81)]  
  
 <span data-ttu-id="f3c9f-126">Obvykle při použití anonymního typu k inicializaci proměnné, deklarujete proměnnou jako implicitně typovou místní proměnnou pomocí [var](../../language-reference/keywords/var.md).</span><span class="sxs-lookup"><span data-stu-id="f3c9f-126">Typically, when you use an anonymous type to initialize a variable, you declare the variable as an implicitly typed local variable by using [var](../../language-reference/keywords/var.md).</span></span> <span data-ttu-id="f3c9f-127">V deklaraci proměnné nelze zadat název typu, protože k podkladovému názvu anonymního typu má přístup pouze kompilátor.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-127">The type name cannot be specified in the variable declaration because only the compiler has access to the underlying name of the anonymous type.</span></span> <span data-ttu-id="f3c9f-128">Další informace o naleznete `var` v tématu [implicitně typované lokální proměnné](./implicitly-typed-local-variables.md).</span><span class="sxs-lookup"><span data-stu-id="f3c9f-128">For more information about `var`, see [Implicitly Typed Local Variables](./implicitly-typed-local-variables.md).</span></span>  
  
 <span data-ttu-id="f3c9f-129">Pole anonymně typovaného prvku lze vytvořit kombinací implicitní typové místní proměnné a implicitně typovaného pole, jak je znázorněno v následujícím příkladu.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-129">You can create an array of anonymously typed elements by combining an implicitly typed local variable and an implicitly typed array, as shown in the following example.</span></span>  
  
```csharp  
var anonArray = new[] { new { name = "apple", diam = 4 }, new { name = "grape", diam = 1 }};  
```  
  
## <a name="remarks"></a><span data-ttu-id="f3c9f-130">Poznámky</span><span class="sxs-lookup"><span data-stu-id="f3c9f-130">Remarks</span></span>  
 <span data-ttu-id="f3c9f-131">Anonymní typy jsou typy [třídy](../../language-reference/keywords/class.md) , které jsou odvozeny přímo z [objektu](../../language-reference/builtin-types/reference-types.md)a které nelze přetypovat na libovolný typ s výjimkou [Object](../../language-reference/builtin-types/reference-types.md).</span><span class="sxs-lookup"><span data-stu-id="f3c9f-131">Anonymous types are [class](../../language-reference/keywords/class.md) types that derive directly from [object](../../language-reference/builtin-types/reference-types.md), and that cannot be cast to any type except [object](../../language-reference/builtin-types/reference-types.md).</span></span> <span data-ttu-id="f3c9f-132">Kompilátor poskytuje název pro každý anonymní typ, přestože aplikace k němu nemá přístup.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-132">The compiler provides a name for each anonymous type, although your application cannot access it.</span></span> <span data-ttu-id="f3c9f-133">Z perspektivy modulu CLR (Common Language Runtime) se anonymní typ neliší od žádného jiného typu odkazu.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-133">From the perspective of the common language runtime, an anonymous type is no different from any other reference type.</span></span>  
  
 <span data-ttu-id="f3c9f-134">Pokud dva nebo více inicializátorů anonymních objektů v sestavení určují sekvenci vlastností, které jsou ve stejném pořadí a mají stejné názvy a typy, kompilátor považuje objekty za instance stejného typu.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-134">If two or more anonymous object initializers in an assembly specify a sequence of properties that are in the same order and that have the same names and types, the compiler treats the objects as instances of the same type.</span></span> <span data-ttu-id="f3c9f-135">Sdílejí stejné informace typu vygenerované kompilátorem.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-135">They share the same compiler-generated type information.</span></span>  
  
 <span data-ttu-id="f3c9f-136">Nemůžete deklarovat pole, vlastnost, událost ani návratový typ metody jako anonymní typ.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-136">You cannot declare a field, a property, an event, or the return type of a method as having an anonymous type.</span></span> <span data-ttu-id="f3c9f-137">Podobně nelze deklarovat formální parametr metody, vlastnosti, konstruktoru nebo indexeru jako typ, který je typu anonymní.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-137">Similarly, you cannot declare a formal parameter of a method, property, constructor, or indexer as having an anonymous type.</span></span> <span data-ttu-id="f3c9f-138">Pro předání anonymního typu nebo kolekce, která obsahuje anonymní typy jako argument metody, lze parametr deklarovat jako objekt typu.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-138">To pass an anonymous type, or a collection that contains anonymous types, as an argument to a method, you can declare the parameter as type object.</span></span> <span data-ttu-id="f3c9f-139">To však vede k přepsání účelu silného typování.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-139">However, doing this defeats the purpose of strong typing.</span></span> <span data-ttu-id="f3c9f-140">Pokud je nutné uložit výsledky dotazu nebo je předat mimo hranice metody, zvažte použití obyčejné pojmenované struktury nebo třídy namísto anonymního typu.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-140">If you must store query results or pass them outside the method boundary, consider using an ordinary named struct or class instead of an anonymous type.</span></span>  
  
 <span data-ttu-id="f3c9f-141">Vzhledem k <xref:System.Object.Equals%2A> tomu <xref:System.Object.GetHashCode%2A> , že metody a na anonymních typech jsou definovány s ohledem na `Equals` `GetHashCode` metody a vlastností, jsou dvě instance stejného anonymního typu stejné pouze v případě, že jsou všechny jejich vlastnosti stejné.</span><span class="sxs-lookup"><span data-stu-id="f3c9f-141">Because the <xref:System.Object.Equals%2A> and <xref:System.Object.GetHashCode%2A> methods on anonymous types are defined in terms of the `Equals` and `GetHashCode` methods of the properties, two instances of the same anonymous type are equal only if all their properties are equal.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="f3c9f-142">Viz také</span><span class="sxs-lookup"><span data-stu-id="f3c9f-142">See also</span></span>

- [<span data-ttu-id="f3c9f-143">Průvodce programováním v C#</span><span class="sxs-lookup"><span data-stu-id="f3c9f-143">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="f3c9f-144">Inicializátory objektu a kolekce</span><span class="sxs-lookup"><span data-stu-id="f3c9f-144">Object and Collection Initializers</span></span>](./object-and-collection-initializers.md)
- [<span data-ttu-id="f3c9f-145">Začínáme s dotazy LINQ v jazyce C#</span><span class="sxs-lookup"><span data-stu-id="f3c9f-145">Getting Started with LINQ in C#</span></span>](../concepts/linq/index.md)
- [<span data-ttu-id="f3c9f-146">LINQ v jazyku C#</span><span class="sxs-lookup"><span data-stu-id="f3c9f-146">LINQ in C#</span></span>](../../linq/index.md)
