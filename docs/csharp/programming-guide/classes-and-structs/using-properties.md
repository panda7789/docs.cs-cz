---
title: Použití vlastností – Průvodce programováním v C#
description: Tyto příklady ilustrují použití vlastností v jazyce C#. Podívejte se, jak přistupující objekty get a set implementují přístup pro čtení a zápis a přečtěte si informace o použití vlastností.
ms.date: 07/20/2015
helpviewer_keywords:
- set accessor [C#]
- get accessor [C#]
- properties [C#], about properties
ms.assetid: f7f67b05-0983-4cdb-96af-1855d24c967c
ms.openlocfilehash: 51ca0a37022c99bfbd9d61f2cc47f529d535e72a
ms.sourcegitcommit: 3d84eac0818099c9949035feb96bbe0346358504
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/21/2020
ms.locfileid: "86864654"
---
# <a name="using-properties-c-programming-guide"></a><span data-ttu-id="f838a-104">Použití vlastností (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="f838a-104">Using Properties (C# Programming Guide)</span></span>

<span data-ttu-id="f838a-105">Vlastnosti kombinují aspekty obou polí a metod.</span><span class="sxs-lookup"><span data-stu-id="f838a-105">Properties combine aspects of both fields and methods.</span></span> <span data-ttu-id="f838a-106">Pro uživatele objektu se zobrazí vlastnost, která má být pole a přístup k vlastnosti vyžaduje stejnou syntaxi.</span><span class="sxs-lookup"><span data-stu-id="f838a-106">To the user of an object, a property appears to be a field, accessing the property requires the same syntax.</span></span> <span data-ttu-id="f838a-107">Pro Implementátor třídy je vlastnost jedním nebo dvěma bloky kódu, které představují přistupující objekt [Get](../../language-reference/keywords/get.md) nebo přístupový objekt [set](../../language-reference/keywords/set.md) .</span><span class="sxs-lookup"><span data-stu-id="f838a-107">To the implementer of a class, a property is one or two code blocks, representing a [get](../../language-reference/keywords/get.md) accessor and/or a [set](../../language-reference/keywords/set.md) accessor.</span></span> <span data-ttu-id="f838a-108">Blok kódu pro `get` přistupující objekt je proveden při čtení vlastnosti. blok kódu pro `set` přistupující objekt je spuštěn, když je vlastnost přiřazena nová hodnota.</span><span class="sxs-lookup"><span data-stu-id="f838a-108">The code block for the `get` accessor is executed when the property is read; the code block for the `set` accessor is executed when the property is assigned a new value.</span></span> <span data-ttu-id="f838a-109">Vlastnost bez `set` přístupového objektu je považována za jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="f838a-109">A property without a `set` accessor is considered read-only.</span></span> <span data-ttu-id="f838a-110">Vlastnost bez `get` přístupového objektu je považována za jen pro zápis.</span><span class="sxs-lookup"><span data-stu-id="f838a-110">A property without a `get` accessor is considered write-only.</span></span> <span data-ttu-id="f838a-111">Vlastnost, která má oba přístupové objekty, je pro čtení i zápis.</span><span class="sxs-lookup"><span data-stu-id="f838a-111">A property that has both accessors is read-write.</span></span>

<span data-ttu-id="f838a-112">Na rozdíl od polí nejsou vlastnosti klasifikovány jako proměnné.</span><span class="sxs-lookup"><span data-stu-id="f838a-112">Unlike fields, properties are not classified as variables.</span></span> <span data-ttu-id="f838a-113">Proto nelze vlastnost předat jako parametr [ref](../../language-reference/keywords/ref.md) nebo [out](../../language-reference/keywords/out-parameter-modifier.md) .</span><span class="sxs-lookup"><span data-stu-id="f838a-113">Therefore, you cannot pass a property as a [ref](../../language-reference/keywords/ref.md) or [out](../../language-reference/keywords/out-parameter-modifier.md) parameter.</span></span>

<span data-ttu-id="f838a-114">Vlastnosti mají mnoho použití: může ověřit data před tím, než povolí změnu. mohou transparentně vystavovat data pro třídu, kde jsou data skutečně načtena z nějakého jiného zdroje, jako je například databáze. mohou provést akci při změně dat, jako je například vyvolání události nebo změna hodnoty jiných polí.</span><span class="sxs-lookup"><span data-stu-id="f838a-114">Properties have many uses: they can validate data before allowing a change; they can transparently expose data on a class where that data is actually retrieved from some other source, such as a database; they can take an action when data is changed, such as raising an event, or changing the value of other fields.</span></span>

<span data-ttu-id="f838a-115">Vlastnosti jsou deklarovány v bloku třídy zadáním úrovně přístupu pole, za nímž následuje typ vlastnosti následovaný názvem vlastnosti a následným blokem kódu, který deklaruje `get` přistupující objekt nebo `set` přistupující objekt.</span><span class="sxs-lookup"><span data-stu-id="f838a-115">Properties are declared in the class block by specifying the access level of the field, followed by the type of the property, followed by the name of the property, and followed by a code block that declares a `get`-accessor and/or a `set` accessor.</span></span> <span data-ttu-id="f838a-116">Příklad:</span><span class="sxs-lookup"><span data-stu-id="f838a-116">For example:</span></span>

[!code-csharp[csProgGuideProperties#7](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideProperties/CS/Properties.cs#7)]

<span data-ttu-id="f838a-117">V tomto příkladu `Month` je deklarován jako vlastnost, aby `set` přístupový objekt mohl zajistit, aby byla `Month` hodnota nastavena v rozmezí od 1 do 12.</span><span class="sxs-lookup"><span data-stu-id="f838a-117">In this example, `Month` is declared as a property so that the `set` accessor can make sure that the `Month` value is set between 1 and 12.</span></span> <span data-ttu-id="f838a-118">`Month`Vlastnost používá soukromé pole ke sledování skutečné hodnoty.</span><span class="sxs-lookup"><span data-stu-id="f838a-118">The `Month` property uses a private field to track the actual value.</span></span> <span data-ttu-id="f838a-119">Skutečné umístění dat vlastnosti se často označuje jako "záložní úložiště" vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="f838a-119">The real location of a property's data is often referred to as the property's "backing store."</span></span> <span data-ttu-id="f838a-120">Vlastnosti pro použití privátních polí jako záložního úložiště je běžné.</span><span class="sxs-lookup"><span data-stu-id="f838a-120">It is common for properties to use private fields as a backing store.</span></span> <span data-ttu-id="f838a-121">Pole je označeno jako soukromé, aby se zajistilo, že lze změnit pouze voláním vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="f838a-121">The field is marked private in order to make sure that it can only be changed by calling the property.</span></span> <span data-ttu-id="f838a-122">Další informace o omezeních veřejného a soukromého přístupu najdete v tématu [modifikátory přístupu](./access-modifiers.md).</span><span class="sxs-lookup"><span data-stu-id="f838a-122">For more information about public and private access restrictions, see [Access Modifiers](./access-modifiers.md).</span></span>

<span data-ttu-id="f838a-123">Automaticky implementované vlastnosti poskytují zjednodušenou syntaxi pro jednoduché deklarace vlastností.</span><span class="sxs-lookup"><span data-stu-id="f838a-123">Auto-implemented properties provide simplified syntax for simple property declarations.</span></span> <span data-ttu-id="f838a-124">Další informace najdete v tématu věnovaném [automaticky implementovaným vlastnostem](auto-implemented-properties.md).</span><span class="sxs-lookup"><span data-stu-id="f838a-124">For more information, see [Auto-Implemented Properties](auto-implemented-properties.md).</span></span>

## <a name="the-get-accessor"></a><span data-ttu-id="f838a-125">Přístupový objekt get</span><span class="sxs-lookup"><span data-stu-id="f838a-125">The get Accessor</span></span>

<span data-ttu-id="f838a-126">Tělo `get` přístupového objektu se podobá metodě.</span><span class="sxs-lookup"><span data-stu-id="f838a-126">The body of the `get` accessor resembles that of a method.</span></span> <span data-ttu-id="f838a-127">Musí vracet hodnotu typu vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="f838a-127">It must return a value of the property type.</span></span> <span data-ttu-id="f838a-128">Provedení `get` přístupového objektu je ekvivalentní k načtení hodnoty pole.</span><span class="sxs-lookup"><span data-stu-id="f838a-128">The execution of the `get` accessor is equivalent to reading the value of the field.</span></span> <span data-ttu-id="f838a-129">Například pokud vracíte soukromou proměnnou z `get` přístupového objektu a optimalizace jsou povoleny, volání `get` metody přistupujícího objektu je vloženo kompilátorem, takže neexistuje žádná režie volání metody.</span><span class="sxs-lookup"><span data-stu-id="f838a-129">For example, when you are returning the private variable from the `get` accessor and optimizations are enabled, the call to the `get` accessor method is inlined by the compiler so there is no method-call overhead.</span></span> <span data-ttu-id="f838a-130">`get`Metoda virtuálního přístupového objektu však nemůže být vložena, protože kompilátor neví v době kompilace, kterou lze skutečně volat v době běhu.</span><span class="sxs-lookup"><span data-stu-id="f838a-130">However, a virtual `get` accessor method cannot be inlined because the compiler does not know at compile-time which method may actually be called at run time.</span></span> <span data-ttu-id="f838a-131">Následuje `get` přístup, který vrací hodnotu soukromého pole `_name` :</span><span class="sxs-lookup"><span data-stu-id="f838a-131">The following is a `get` accessor that returns the value of a private field `_name`:</span></span>

[!code-csharp[csProgGuideProperties#8](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideProperties/CS/Properties.cs#8)]

<span data-ttu-id="f838a-132">Když odkazujete na vlastnost, s výjimkou cíle přiřazení, `get` je vyvolán přistupující objekt pro čtení hodnoty vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="f838a-132">When you reference the property, except as the target of an assignment, the `get` accessor is invoked to read the value of the property.</span></span> <span data-ttu-id="f838a-133">Příklad:</span><span class="sxs-lookup"><span data-stu-id="f838a-133">For example:</span></span>

[!code-csharp[csProgGuideProperties#9](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideProperties/CS/Properties.cs#9)]

<span data-ttu-id="f838a-134">`get`Přistupující objekt musí končit příkazem [return](../../language-reference/keywords/return.md) nebo [throw](../../language-reference/keywords/throw.md) a ovládací prvek nemůže přesměrovat tělo přistupujícího objektu.</span><span class="sxs-lookup"><span data-stu-id="f838a-134">The `get` accessor must end in a [return](../../language-reference/keywords/return.md) or [throw](../../language-reference/keywords/throw.md) statement, and control cannot flow off the accessor body.</span></span>

<span data-ttu-id="f838a-135">Je to špatný styl programování ke změně stavu objektu pomocí `get` přístupového objektu.</span><span class="sxs-lookup"><span data-stu-id="f838a-135">It is a bad programming style to change the state of the object by using the `get` accessor.</span></span> <span data-ttu-id="f838a-136">Například následující přistupující objekt vytváří vedlejší účinek změny stavu objektu při každém `_number` přístupu k poli.</span><span class="sxs-lookup"><span data-stu-id="f838a-136">For example, the following accessor produces the side effect of changing the state of the object every time that the `_number` field is accessed.</span></span>

[!code-csharp[csProgGuideProperties#10](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideProperties/CS/Properties.cs#10)]

<span data-ttu-id="f838a-137">`get`Přístupový objekt lze použít k vrácení hodnoty pole nebo k jejímu výpočtu a vrácení.</span><span class="sxs-lookup"><span data-stu-id="f838a-137">The `get` accessor can be used to return the field value or to compute it and return it.</span></span> <span data-ttu-id="f838a-138">Příklad:</span><span class="sxs-lookup"><span data-stu-id="f838a-138">For example:</span></span>

[!code-csharp[csProgGuideProperties#11](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideProperties/CS/Properties.cs#11)]

<span data-ttu-id="f838a-139">Pokud v předchozím segmentu kódu nepřiřazujete hodnotu `Name` vlastnosti, vrátí hodnotu `NA` .</span><span class="sxs-lookup"><span data-stu-id="f838a-139">In the previous code segment, if you do not assign a value to the `Name` property, it will return the value `NA`.</span></span>

## <a name="the-set-accessor"></a><span data-ttu-id="f838a-140">Přístupový objekt set</span><span class="sxs-lookup"><span data-stu-id="f838a-140">The set Accessor</span></span>

<span data-ttu-id="f838a-141">`set`Přistupující objekt se podobá metodě, jejíž návratový typ je [void](../../language-reference/builtin-types/void.md).</span><span class="sxs-lookup"><span data-stu-id="f838a-141">The `set` accessor resembles a method whose return type is [void](../../language-reference/builtin-types/void.md).</span></span> <span data-ttu-id="f838a-142">Používá implicitní parametr `value` s názvem, jehož typ je typ vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="f838a-142">It uses an implicit parameter called `value`, whose type is the type of the property.</span></span> <span data-ttu-id="f838a-143">V následujícím příkladu `set` je přistupující objekt přidán do `Name` vlastnosti:</span><span class="sxs-lookup"><span data-stu-id="f838a-143">In the following example, a `set` accessor is added to the `Name` property:</span></span>

[!code-csharp[csProgGuideProperties#12](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideProperties/CS/Properties.cs#12)]

<span data-ttu-id="f838a-144">Když přiřadíte hodnotu vlastnosti, `set` přistupující objekt je vyvolán pomocí argumentu, který poskytuje novou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="f838a-144">When you assign a value to the property, the `set` accessor is invoked by using an argument that provides the new value.</span></span> <span data-ttu-id="f838a-145">Příklad:</span><span class="sxs-lookup"><span data-stu-id="f838a-145">For example:</span></span>

[!code-csharp[csProgGuideProperties#13](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideProperties/CS/Properties.cs#13)]

<span data-ttu-id="f838a-146">Použití implicitního názvu parametru, `value` pro deklaraci lokální proměnné v `set` přístupovém objektu, je chybné.</span><span class="sxs-lookup"><span data-stu-id="f838a-146">It is an error to use the implicit parameter name, `value`, for a local variable declaration in a `set` accessor.</span></span>

## <a name="remarks"></a><span data-ttu-id="f838a-147">Poznámky</span><span class="sxs-lookup"><span data-stu-id="f838a-147">Remarks</span></span>

<span data-ttu-id="f838a-148">Vlastnosti mohou být označeny jako `public` , `private` , `protected` , `internal` `protected internal` nebo `private protected` .</span><span class="sxs-lookup"><span data-stu-id="f838a-148">Properties can be marked as `public`, `private`, `protected`, `internal`, `protected internal` or `private protected`.</span></span> <span data-ttu-id="f838a-149">Tyto modifikátory přístupu definují, jak budou uživatelé třídy mít přístup k vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="f838a-149">These access modifiers define how users of the class can access the property.</span></span> <span data-ttu-id="f838a-150">`get` `set` Přístupové objekty a pro stejnou vlastnost mohou mít různé modifikátory přístupu.</span><span class="sxs-lookup"><span data-stu-id="f838a-150">The `get` and `set` accessors for the same property may have different access modifiers.</span></span> <span data-ttu-id="f838a-151">Například `get` může být `public` Povolení přístupu jen pro čtení zvenčí mimo typ a `set` může být `private` nebo `protected` .</span><span class="sxs-lookup"><span data-stu-id="f838a-151">For example, the `get` may be `public` to allow read-only access from outside the type, and the `set` may be `private` or `protected`.</span></span> <span data-ttu-id="f838a-152">Další informace najdete v tématu [modifikátory přístupu](./access-modifiers.md).</span><span class="sxs-lookup"><span data-stu-id="f838a-152">For more information, see [Access Modifiers](./access-modifiers.md).</span></span>

<span data-ttu-id="f838a-153">Vlastnost může být deklarována jako statická vlastnost pomocí `static` klíčového slova.</span><span class="sxs-lookup"><span data-stu-id="f838a-153">A property may be declared as a static property by using the `static` keyword.</span></span> <span data-ttu-id="f838a-154">Tato vlastnost zpřístupňuje volajícím kdykoli, a to i v případě, že žádná instance třídy neexistuje.</span><span class="sxs-lookup"><span data-stu-id="f838a-154">This makes the property available to callers at any time, even if no instance of the class exists.</span></span> <span data-ttu-id="f838a-155">Další informace naleznete v tématu [statické třídy a statické členy třídy](./static-classes-and-static-class-members.md).</span><span class="sxs-lookup"><span data-stu-id="f838a-155">For more information, see [Static Classes and Static Class Members](./static-classes-and-static-class-members.md).</span></span>

<span data-ttu-id="f838a-156">Vlastnost může být označena jako virtuální vlastnost pomocí klíčového slova [Virtual](../../language-reference/keywords/virtual.md) .</span><span class="sxs-lookup"><span data-stu-id="f838a-156">A property may be marked as a virtual property by using the [virtual](../../language-reference/keywords/virtual.md) keyword.</span></span> <span data-ttu-id="f838a-157">To umožňuje odvozeným třídám přepsat chování vlastnosti pomocí klíčového slova [override](../../language-reference/keywords/override.md) .</span><span class="sxs-lookup"><span data-stu-id="f838a-157">This enables derived classes to override the property behavior by using the [override](../../language-reference/keywords/override.md) keyword.</span></span> <span data-ttu-id="f838a-158">Další informace o těchto možnostech naleznete v tématu [Dědičnost](inheritance.md).</span><span class="sxs-lookup"><span data-stu-id="f838a-158">For more information about these options, see [Inheritance](inheritance.md).</span></span>

<span data-ttu-id="f838a-159">Vlastnost přepisujecí virtuální vlastnost může být také [zapečetěná](../../language-reference/keywords/sealed.md)a specifikuje, že pro odvozené třídy už není virtuální.</span><span class="sxs-lookup"><span data-stu-id="f838a-159">A property overriding a virtual property can also be [sealed](../../language-reference/keywords/sealed.md), specifying that for derived classes it is no longer virtual.</span></span> <span data-ttu-id="f838a-160">Nakonec může být vlastnost deklarována jako [abstraktní](../../language-reference/keywords/abstract.md).</span><span class="sxs-lookup"><span data-stu-id="f838a-160">Lastly, a property can be declared [abstract](../../language-reference/keywords/abstract.md).</span></span> <span data-ttu-id="f838a-161">To znamená, že ve třídě není žádná implementace, a odvozené třídy musí napsat svou vlastní implementaci.</span><span class="sxs-lookup"><span data-stu-id="f838a-161">This means that there is no implementation in the class, and derived classes must write their own implementation.</span></span> <span data-ttu-id="f838a-162">Další informace o těchto možnostech naleznete v tématu [abstraktní a zapečetěné třídy a členy třídy](abstract-and-sealed-classes-and-class-members.md).</span><span class="sxs-lookup"><span data-stu-id="f838a-162">For more information about these options, see [Abstract and Sealed Classes and Class Members](abstract-and-sealed-classes-and-class-members.md).</span></span>
  
> [!NOTE]
> <span data-ttu-id="f838a-163">Použití modifikátoru [Virtual](../../language-reference/keywords/virtual.md), [abstract](../../language-reference/keywords/abstract.md)nebo [override](../../language-reference/keywords/override.md) u přístupového objektu [statické](../../language-reference/keywords/static.md) vlastnosti je chybné.</span><span class="sxs-lookup"><span data-stu-id="f838a-163">It is an error to use a [virtual](../../language-reference/keywords/virtual.md), [abstract](../../language-reference/keywords/abstract.md), or [override](../../language-reference/keywords/override.md) modifier on an accessor of a [static](../../language-reference/keywords/static.md) property.</span></span>

## <a name="example"></a><span data-ttu-id="f838a-164">Příklad</span><span class="sxs-lookup"><span data-stu-id="f838a-164">Example</span></span>

<span data-ttu-id="f838a-165">Tento příklad ukazuje vlastnosti instance, static a jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="f838a-165">This example demonstrates instance, static, and read-only properties.</span></span> <span data-ttu-id="f838a-166">Přijímá jméno zaměstnance z klávesnice, zvyšuje hodnotu `NumberOfEmployees` o 1 a zobrazuje jméno a číslo zaměstnance.</span><span class="sxs-lookup"><span data-stu-id="f838a-166">It accepts the name of the employee from the keyboard, increments `NumberOfEmployees` by 1, and displays the Employee name and number.</span></span>

[!code-csharp[csProgGuideProperties#2](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideProperties/CS/Properties.cs#2)]

## <a name="example"></a><span data-ttu-id="f838a-167">Příklad</span><span class="sxs-lookup"><span data-stu-id="f838a-167">Example</span></span>

<span data-ttu-id="f838a-168">Tento příklad ukazuje, jak získat přístup k vlastnosti v základní třídě, která je skryta jinou vlastností, která má stejný název v odvozené třídě:</span><span class="sxs-lookup"><span data-stu-id="f838a-168">This example demonstrates how to access a property in a base class that is hidden by another property that has the same name in a derived class:</span></span>

[!code-csharp[csProgGuideProperties#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideProperties/CS/Properties.cs#3)]

<span data-ttu-id="f838a-169">Níže jsou uvedené důležité body v předchozím příkladu:</span><span class="sxs-lookup"><span data-stu-id="f838a-169">The following are important points in the previous example:</span></span>

- <span data-ttu-id="f838a-170">Vlastnost `Name` v odvozené třídě skrývá vlastnost `Name` v základní třídě.</span><span class="sxs-lookup"><span data-stu-id="f838a-170">The property `Name` in the derived class hides the property `Name` in the base class.</span></span> <span data-ttu-id="f838a-171">V takovém případě se `new` Modifikátor používá v deklaraci vlastnosti v odvozené třídě:</span><span class="sxs-lookup"><span data-stu-id="f838a-171">In such a case, the `new` modifier is used in the declaration of the property in the derived class:</span></span>

     [!code-csharp[csProgGuideProperties#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideProperties/CS/Properties.cs#4)]  

- <span data-ttu-id="f838a-172">Přetypování `(Employee)` se používá pro přístup k skryté vlastnosti v základní třídě:</span><span class="sxs-lookup"><span data-stu-id="f838a-172">The cast `(Employee)` is used to access the hidden property in the base class:</span></span>

     [!code-csharp[csProgGuideProperties#5](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideProperties/CS/Properties.cs#5)]

     <span data-ttu-id="f838a-173">Další informace o skrývání členů naleznete v tématu [nový modifikátor](../../language-reference/keywords/new-modifier.md).</span><span class="sxs-lookup"><span data-stu-id="f838a-173">For more information about hiding members, see the [new Modifier](../../language-reference/keywords/new-modifier.md).</span></span>

## <a name="example"></a><span data-ttu-id="f838a-174">Příklad</span><span class="sxs-lookup"><span data-stu-id="f838a-174">Example</span></span>

<span data-ttu-id="f838a-175">V tomto příkladu dvě třídy, `Cube` `Square` implementujte abstraktní třídu, `Shape` a přepište její abstraktní `Area` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="f838a-175">In this example, two classes, `Cube` and `Square`, implement an abstract class, `Shape`, and override its abstract `Area` property.</span></span> <span data-ttu-id="f838a-176">Všimněte si použití modifikátoru [přepsání](../../language-reference/keywords/override.md) u vlastností.</span><span class="sxs-lookup"><span data-stu-id="f838a-176">Note the use of the [override](../../language-reference/keywords/override.md) modifier on the properties.</span></span> <span data-ttu-id="f838a-177">Program přijme stranu jako vstup a vypočítá oblasti pro čtvercovou a datovou krychli.</span><span class="sxs-lookup"><span data-stu-id="f838a-177">The program accepts the side as an input and calculates the areas for the square and cube.</span></span> <span data-ttu-id="f838a-178">Také přijme oblast jako vstup a vypočte odpovídající stranu pro čtvercovou a datovou krychli.</span><span class="sxs-lookup"><span data-stu-id="f838a-178">It also accepts the area as an input and calculates the corresponding side for the square and cube.</span></span>

[!code-csharp[csProgGuideProperties#6](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideProperties/CS/Properties.cs#6)]

## <a name="see-also"></a><span data-ttu-id="f838a-179">Viz také</span><span class="sxs-lookup"><span data-stu-id="f838a-179">See also</span></span>

- [<span data-ttu-id="f838a-180">Průvodce programováním v C#</span><span class="sxs-lookup"><span data-stu-id="f838a-180">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="f838a-181">Vlastnosti</span><span class="sxs-lookup"><span data-stu-id="f838a-181">Properties</span></span>](properties.md)
- [<span data-ttu-id="f838a-182">Vlastnosti rozhraní</span><span class="sxs-lookup"><span data-stu-id="f838a-182">Interface Properties</span></span>](interface-properties.md)
- [<span data-ttu-id="f838a-183">Automaticky implementované vlastnosti</span><span class="sxs-lookup"><span data-stu-id="f838a-183">Auto-Implemented Properties</span></span>](auto-implemented-properties.md)
