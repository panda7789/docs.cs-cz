---
title: Správa verzí pomocí přepsání a nových klíčových slov - C# Průvodce programováním
ms.custom: seodec18
ms.date: 07/20/2015
helpviewer_keywords:
- C# language, versioning
- C# language, override and new
ms.assetid: 88247d07-bd0d-49e9-a619-45ccbbfdf0c5
ms.openlocfilehash: 39aae39a761414947c14f0a78aedcdbf89ddfbda
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: HT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/23/2019
ms.locfileid: "61702968"
---
# <a name="versioning-with-the-override-and-new-keywords-c-programming-guide"></a><span data-ttu-id="bfee8-102">Správa verzí pomocí klíčových slov override a new (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="bfee8-102">Versioning with the Override and New Keywords (C# Programming Guide)</span></span>
<span data-ttu-id="bfee8-103">Jazyk C# je navržený tak, aby Správa verzí mezi [základní](../../../csharp/language-reference/keywords/base.md) a odvozené třídy v jiných knihovnách můžete vyvíjet a udržovat zpětnou kompatibilitu.</span><span class="sxs-lookup"><span data-stu-id="bfee8-103">The C# language is designed so that versioning between [base](../../../csharp/language-reference/keywords/base.md) and derived classes in different libraries can evolve and maintain backward compatibility.</span></span> <span data-ttu-id="bfee8-104">To znamená, například, že zavedení nového člena v základní třídě [třída](../../../csharp/language-reference/keywords/class.md) se stejným názvem jako člena v odvozené třídě je plně podporován v jazyce C# a nevede k neočekávanému chování.</span><span class="sxs-lookup"><span data-stu-id="bfee8-104">This means, for example, that the introduction of a new member in a base [class](../../../csharp/language-reference/keywords/class.md) with the same name as a member in a derived class is completely supported by C# and does not lead to unexpected behavior.</span></span> <span data-ttu-id="bfee8-105">Také znamená, že třída musí explicitně uvést, zda metoda je určena k přepsání zděděné metody nebo určuje, zda je metoda novou metodu, která skrývá podobně pojmenovaných zděděné metody.</span><span class="sxs-lookup"><span data-stu-id="bfee8-105">It also means that a class must explicitly state whether a method is intended to override an inherited method, or whether a method is a new method that hides a similarly named inherited method.</span></span>  
  
 <span data-ttu-id="bfee8-106">V jazyce C# odvozené třídy mohou obsahovat metody se stejným názvem jako metody základní třídy.</span><span class="sxs-lookup"><span data-stu-id="bfee8-106">In C#, derived classes can contain methods with the same name as base class methods.</span></span>  
  
- <span data-ttu-id="bfee8-107">Metoda základní třídy musí být definován [virtuální](../../../csharp/language-reference/keywords/virtual.md).</span><span class="sxs-lookup"><span data-stu-id="bfee8-107">The base class method must be defined [virtual](../../../csharp/language-reference/keywords/virtual.md).</span></span>  
  
- <span data-ttu-id="bfee8-108">Pokud metoda v odvozené třídě není předcházen [nové](../../../csharp/language-reference/keywords/new.md) nebo [přepsat](../../../csharp/language-reference/keywords/override.md) klíčová slova, kompilátor vygeneruje upozornění a metodu se bude chovat jako `new` – klíčové slovo nebyly nalezeny.</span><span class="sxs-lookup"><span data-stu-id="bfee8-108">If the method in the derived class is not preceded by [new](../../../csharp/language-reference/keywords/new.md) or [override](../../../csharp/language-reference/keywords/override.md) keywords, the compiler will issue a warning and the method will behave as if the `new` keyword were present.</span></span>  
  
- <span data-ttu-id="bfee8-109">Pokud je metoda v odvozené třídě začínající `new` – klíčové slovo, metoda je definována jako nezávislé na metodu v základní třídě.</span><span class="sxs-lookup"><span data-stu-id="bfee8-109">If the method in the derived class is preceded with the `new` keyword, the method is defined as being independent of the method in the base class.</span></span>  
  
- <span data-ttu-id="bfee8-110">Pokud je metoda v odvozené třídě začínající `override` – klíčové slovo, objekty odvozené třídy zavolá tato metoda namísto metody základní třídy.</span><span class="sxs-lookup"><span data-stu-id="bfee8-110">If the method in the derived class is preceded with the `override` keyword, objects of the derived class will call that method instead of the base class method.</span></span>  
  
- <span data-ttu-id="bfee8-111">Metoda základní třídy lze volat z v rámci odvozené třídy pomocí `base` – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="bfee8-111">The base class method can be called from within the derived class using the `base` keyword.</span></span>  
  
- <span data-ttu-id="bfee8-112">`override`, `virtual`, A `new` klíčová slova lze také použít u vlastnosti, indexery a události.</span><span class="sxs-lookup"><span data-stu-id="bfee8-112">The `override`, `virtual`, and `new` keywords can also be applied to properties, indexers, and events.</span></span>  
  
 <span data-ttu-id="bfee8-113">Ve výchozím nastavení nejsou virtuální metody jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="bfee8-113">By default, C# methods are not virtual.</span></span> <span data-ttu-id="bfee8-114">Pokud metoda deklarována jako virtuální, všechny třídu, která dědí metodu můžete implementovat vlastní verzi.</span><span class="sxs-lookup"><span data-stu-id="bfee8-114">If a method is declared as virtual, any class inheriting the method can implement its own version.</span></span> <span data-ttu-id="bfee8-115">Virtuální, aby metoda `virtual` modifikátor se používá v deklaraci metody základní třídy.</span><span class="sxs-lookup"><span data-stu-id="bfee8-115">To make a method virtual, the `virtual` modifier is used in the method declaration of the base class.</span></span> <span data-ttu-id="bfee8-116">Odvozená třída může přepsat základní metodu virtuální pak pomocí `override` – klíčové slovo nebo skrýt virtuální metodu v základní třídě pomocí `new` – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="bfee8-116">The derived class can then override the base virtual method by using the `override` keyword or hide the virtual method in the base class by using the `new` keyword.</span></span> <span data-ttu-id="bfee8-117">Pokud ani `override` – klíčové slovo ani `new` zadáno klíčové slovo, kompilátor vygeneruje upozornění a metodu v odvozené třídě budou skrývat metodu v základní třídě.</span><span class="sxs-lookup"><span data-stu-id="bfee8-117">If neither the `override` keyword nor the `new` keyword is specified, the compiler will issue a warning and the method in the derived class will hide the method in the base class.</span></span>  
  
 <span data-ttu-id="bfee8-118">Abychom si předvedli v praxi, předpokládají na chvíli zastavíme, firmy A vytvořil třídu s názvem `GraphicsClass`, který program používá.</span><span class="sxs-lookup"><span data-stu-id="bfee8-118">To demonstrate this in practice, assume for a moment that Company A has created a class named `GraphicsClass`, which your program uses.</span></span> <span data-ttu-id="bfee8-119">Tady je `GraphicsClass`:</span><span class="sxs-lookup"><span data-stu-id="bfee8-119">The following is `GraphicsClass`:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#27](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#27)]  
  
 <span data-ttu-id="bfee8-120">Vaše společnost používá ochranu této třídy a můžete ji použít k odvodit vlastní třídu, přidání nové metody:</span><span class="sxs-lookup"><span data-stu-id="bfee8-120">Your company uses this class, and you use it to derive your own class, adding a new method:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#28](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#28)]  
  
 <span data-ttu-id="bfee8-121">Vaše aplikace se používá bez problémů, dokud nové verze aktualizací společnosti A `GraphicsClass`, který vypadá podobně jako následující kód:</span><span class="sxs-lookup"><span data-stu-id="bfee8-121">Your application is used without problems, until Company A releases a new version of `GraphicsClass`, which resembles the following code:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#29](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#29)]  
  
 <span data-ttu-id="bfee8-122">Nová verze `GraphicsClass` nyní obsahuje metodu s názvem `DrawRectangle`.</span><span class="sxs-lookup"><span data-stu-id="bfee8-122">The new version of `GraphicsClass` now contains a method named `DrawRectangle`.</span></span> <span data-ttu-id="bfee8-123">Na začátku nedojde k žádné akci.</span><span class="sxs-lookup"><span data-stu-id="bfee8-123">Initially, nothing occurs.</span></span> <span data-ttu-id="bfee8-124">Nová verze je stále binární kompatibilní s předchozí verzi aplikace.</span><span class="sxs-lookup"><span data-stu-id="bfee8-124">The new version is still binary compatible with the old version.</span></span> <span data-ttu-id="bfee8-125">Veškerý software, který jste nasadili se budou nadále fungovat, i v případě nové třídy je nainstalována v těchto počítačích.</span><span class="sxs-lookup"><span data-stu-id="bfee8-125">Any software that you have deployed will continue to work, even if the new class is installed on those computer systems.</span></span> <span data-ttu-id="bfee8-126">Všechna existující volání do metody `DrawRectangle` budou i nadále odkazují na vaši verzi, a to v odvozené třídě.</span><span class="sxs-lookup"><span data-stu-id="bfee8-126">Any existing calls to the method `DrawRectangle` will continue to reference your version, in your derived class.</span></span>  
  
 <span data-ttu-id="bfee8-127">Ale co nejdříve aplikaci znovu zkompilujete pomocí nové verze `GraphicsClass`, zobrazí se upozornění z kompilátoru CS0108.</span><span class="sxs-lookup"><span data-stu-id="bfee8-127">However, as soon as you recompile your application by using the new version of `GraphicsClass`, you will receive a warning from the compiler, CS0108.</span></span> <span data-ttu-id="bfee8-128">Toto upozornění vás informuje, že budete muset zvážit, jak chcete vaši `DrawRectangle` metoda chovat ve vaší aplikaci.</span><span class="sxs-lookup"><span data-stu-id="bfee8-128">This warning informs you that you have to consider how you want your `DrawRectangle` method to behave in your application.</span></span>  
  
 <span data-ttu-id="bfee8-129">Pokud chcete metodu pro přepsání novou metodu základní třídy, použijte `override` – klíčové slovo:</span><span class="sxs-lookup"><span data-stu-id="bfee8-129">If you want your method to override the new base class method, use the `override` keyword:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#30](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#30)]  
  
 <span data-ttu-id="bfee8-130">`override` – Klíčové slovo zajišťuje, že všechny objekty odvozené z `YourDerivedGraphicsClass` budou používat verzi odvozené třídy `DrawRectangle`.</span><span class="sxs-lookup"><span data-stu-id="bfee8-130">The `override` keyword makes sure that any objects derived from `YourDerivedGraphicsClass` will use the derived class version of `DrawRectangle`.</span></span> <span data-ttu-id="bfee8-131">Objekty odvozené z `YourDerivedGraphicsClass` pořád přístup k verzi základní třídy `DrawRectangle` pomocí klíčového slova base:</span><span class="sxs-lookup"><span data-stu-id="bfee8-131">Objects derived from `YourDerivedGraphicsClass` can still access the base class version of `DrawRectangle` by using the base keyword:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#44](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#44)]  
  
 <span data-ttu-id="bfee8-132">Pokud nechcete, aby vaše metoda k přepsání novou metodu základní třídy, platí následující aspekty.</span><span class="sxs-lookup"><span data-stu-id="bfee8-132">If you do not want your method to override the new base class method, the following considerations apply.</span></span> <span data-ttu-id="bfee8-133">Aby nedocházelo k záměně mezi těmito dvěma metodami, můžete přejmenovat metodu.</span><span class="sxs-lookup"><span data-stu-id="bfee8-133">To avoid confusion between the two methods, you can rename your method.</span></span> <span data-ttu-id="bfee8-134">To může být časově náročné a náchylné k chybě a v některých případech stačí není praktické.</span><span class="sxs-lookup"><span data-stu-id="bfee8-134">This can be time-consuming and error-prone, and just not practical in some cases.</span></span> <span data-ttu-id="bfee8-135">Pokud váš projekt je poměrně malý, můžete přejmenovat metodu můžete použít možnosti aplikace Visual Studio refaktoringu.</span><span class="sxs-lookup"><span data-stu-id="bfee8-135">However, if your project is relatively small, you can use Visual Studio's Refactoring options to rename the method.</span></span> <span data-ttu-id="bfee8-136">Další informace najdete v tématu [refaktoring tříd a typů (návrhář tříd)](/visualstudio/ide/refactoring-classes-and-types-class-designer).</span><span class="sxs-lookup"><span data-stu-id="bfee8-136">For more information, see [Refactoring Classes and Types (Class Designer)](/visualstudio/ide/refactoring-classes-and-types-class-designer).</span></span>  
  
 <span data-ttu-id="bfee8-137">Alternativně můžete zabránit upozornění pomocí klíčového slova `new` v definici odvozené třídy:</span><span class="sxs-lookup"><span data-stu-id="bfee8-137">Alternatively, you can prevent the warning by using the keyword `new` in your derived class definition:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#31](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#31)]  
  
 <span data-ttu-id="bfee8-138">Použití `new` – klíčové slovo sděluje kompilátoru, že vaše definice skryje definici, která je obsažena v základní třídě.</span><span class="sxs-lookup"><span data-stu-id="bfee8-138">Using the `new` keyword tells the compiler that your definition hides the definition that is contained in the base class.</span></span> <span data-ttu-id="bfee8-139">Toto je výchozí chování.</span><span class="sxs-lookup"><span data-stu-id="bfee8-139">This is the default behavior.</span></span>  
  
## <a name="override-and-method-selection"></a><span data-ttu-id="bfee8-140">Přepsání a výběr metody</span><span class="sxs-lookup"><span data-stu-id="bfee8-140">Override and Method Selection</span></span>  
 <span data-ttu-id="bfee8-141">Při názvem metodu na třídu, kompilátor jazyka C# vybere nejlepší metody volat, pokud více než jedním ze způsobů je kompatibilní s voláním, například když existují dvě metody se stejným názvem a parametry, které jsou kompatibilní s parametrem předán.</span><span class="sxs-lookup"><span data-stu-id="bfee8-141">When a method is named on a class, the C# compiler selects the best method to call if more than one method is compatible with the call, such as when there are two methods with the same name, and parameters that are compatible with the parameter passed.</span></span> <span data-ttu-id="bfee8-142">Následující metody by měly kompatibilní:</span><span class="sxs-lookup"><span data-stu-id="bfee8-142">The following methods would be compatible:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#32](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#32)]  
  
 <span data-ttu-id="bfee8-143">Když `DoWork` je volán na instanci `Derived`, kompilátor jazyka C# se nejdřív pokusí použít, provede volání kompatibilní s verzemi `DoWork` původně deklarovaná na `Derived`.</span><span class="sxs-lookup"><span data-stu-id="bfee8-143">When `DoWork` is called on an instance of `Derived`, the C# compiler will first try to make the call compatible with the versions of `DoWork` declared originally on `Derived`.</span></span> <span data-ttu-id="bfee8-144">Přepsání metody nepovažují, protože deklaruje se na třídu, nové implementace metody deklarované pro základní třídu.</span><span class="sxs-lookup"><span data-stu-id="bfee8-144">Override methods are not considered as declared on a class, they are new implementations of a method declared on a base class.</span></span> <span data-ttu-id="bfee8-145">Pouze v případě, že kompilátor jazyka C# se nemůže shodovat volání metody na původní metodu na `Derived` se pokusit porovnat volání přepsané metody se stejným názvem a kompatibilní parametry.</span><span class="sxs-lookup"><span data-stu-id="bfee8-145">Only if the C# compiler cannot match the method call to an original method on `Derived` will it try to match the call to an overridden method with the same name and compatible parameters.</span></span> <span data-ttu-id="bfee8-146">Příklad:</span><span class="sxs-lookup"><span data-stu-id="bfee8-146">For example:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#33](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#33)]  
  
 <span data-ttu-id="bfee8-147">Protože proměnné `val` lze implicitně převést na dvojitou hodnotu volání kompilátor jazyka C# `DoWork(double)` místo `DoWork(int)`.</span><span class="sxs-lookup"><span data-stu-id="bfee8-147">Because the variable `val` can be converted to a double implicitly, the C# compiler calls `DoWork(double)` instead of `DoWork(int)`.</span></span> <span data-ttu-id="bfee8-148">Existují dva způsoby, abyste tomu předešli.</span><span class="sxs-lookup"><span data-stu-id="bfee8-148">There are two ways to avoid this.</span></span> <span data-ttu-id="bfee8-149">Vyhněte se nejprve deklaraci nových metod se stejným názvem jako virtuální metody.</span><span class="sxs-lookup"><span data-stu-id="bfee8-149">First, avoid declaring new methods with the same name as virtual methods.</span></span> <span data-ttu-id="bfee8-150">Za druhé, můžete dát pokyn kompilátoru C# pro volání virtuální metody tím, že metoda základní třídy v seznamu vyhledejte přetypováním instanci `Derived` k `Base`.</span><span class="sxs-lookup"><span data-stu-id="bfee8-150">Second, you can instruct the C# compiler to call the virtual method by making it search the base class method list by casting the instance of `Derived` to `Base`.</span></span> <span data-ttu-id="bfee8-151">Protože metoda je virtuální, provádění `DoWork(int)` na `Derived` , zavolá se.</span><span class="sxs-lookup"><span data-stu-id="bfee8-151">Because the method is virtual, the implementation of `DoWork(int)` on `Derived` will be called.</span></span> <span data-ttu-id="bfee8-152">Příklad:</span><span class="sxs-lookup"><span data-stu-id="bfee8-152">For example:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#34](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#34)]  
  
 <span data-ttu-id="bfee8-153">Další příklady `new` a `override`, naleznete v tématu [vědět, když pro použití přepsání a nových klíčových slov](../../../csharp/programming-guide/classes-and-structs/knowing-when-to-use-override-and-new-keywords.md).</span><span class="sxs-lookup"><span data-stu-id="bfee8-153">For more examples of `new` and `override`, see [Knowing When to Use Override and New Keywords](../../../csharp/programming-guide/classes-and-structs/knowing-when-to-use-override-and-new-keywords.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="bfee8-154">Viz také:</span><span class="sxs-lookup"><span data-stu-id="bfee8-154">See also</span></span>

- [<span data-ttu-id="bfee8-155">Průvodce programováním v jazyce C#</span><span class="sxs-lookup"><span data-stu-id="bfee8-155">C# Programming Guide</span></span>](../../../csharp/programming-guide/index.md)
- [<span data-ttu-id="bfee8-156">Třídy a struktury</span><span class="sxs-lookup"><span data-stu-id="bfee8-156">Classes and Structs</span></span>](../../../csharp/programming-guide/classes-and-structs/index.md)
- [<span data-ttu-id="bfee8-157">Metody</span><span class="sxs-lookup"><span data-stu-id="bfee8-157">Methods</span></span>](../../../csharp/programming-guide/classes-and-structs/methods.md)
- [<span data-ttu-id="bfee8-158">Dědičnost</span><span class="sxs-lookup"><span data-stu-id="bfee8-158">Inheritance</span></span>](../../../csharp/programming-guide/classes-and-structs/inheritance.md)
