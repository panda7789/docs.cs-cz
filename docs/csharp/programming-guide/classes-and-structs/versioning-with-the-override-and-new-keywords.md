---
title: Správa verzí pomocí klíčových slov override a New – Průvodce programováním v C#
ms.date: 07/20/2015
helpviewer_keywords:
- C# language, versioning
- C# language, override and new
ms.assetid: 88247d07-bd0d-49e9-a619-45ccbbfdf0c5
ms.openlocfilehash: 7bcc7e68810c97142cebca7595266a0e4a69ed51
ms.sourcegitcommit: 488aced39b5f374bc0a139a4993616a54d15baf0
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 05/12/2020
ms.locfileid: "83207941"
---
# <a name="versioning-with-the-override-and-new-keywords-c-programming-guide"></a><span data-ttu-id="e3139-102">Správa verzí pomocí klíčových slov override a new (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="e3139-102">Versioning with the Override and New Keywords (C# Programming Guide)</span></span>
<span data-ttu-id="e3139-103">Jazyk C# je navržen tak, aby bylo možné vyvíjet a udržovat zpětnou kompatibilitu verzí mezi [základními](../../language-reference/keywords/base.md) a odvozenými třídami v různých knihovnách.</span><span class="sxs-lookup"><span data-stu-id="e3139-103">The C# language is designed so that versioning between [base](../../language-reference/keywords/base.md) and derived classes in different libraries can evolve and maintain backward compatibility.</span></span> <span data-ttu-id="e3139-104">To například znamená, že zavedení nového člena v základní [třídě](../../language-reference/keywords/class.md) se stejným názvem jako člen v odvozené třídě je zcela podporováno jazykem C# a nevede k neočekávanému chování.</span><span class="sxs-lookup"><span data-stu-id="e3139-104">This means, for example, that the introduction of a new member in a base [class](../../language-reference/keywords/class.md) with the same name as a member in a derived class is completely supported by C# and does not lead to unexpected behavior.</span></span> <span data-ttu-id="e3139-105">Také to znamená, že třída musí explicitně určit, zda je metoda určena k přepsání zděděné metody, nebo zda je metoda novou metodou, která skrývá obdobně pojmenovanou zděděnou metodu.</span><span class="sxs-lookup"><span data-stu-id="e3139-105">It also means that a class must explicitly state whether a method is intended to override an inherited method, or whether a method is a new method that hides a similarly named inherited method.</span></span>  
  
 <span data-ttu-id="e3139-106">V jazyce C# odvozené třídy mohou obsahovat metody se stejným názvem jako metody základní třídy.</span><span class="sxs-lookup"><span data-stu-id="e3139-106">In C#, derived classes can contain methods with the same name as base class methods.</span></span>  

- <span data-ttu-id="e3139-107">Pokud metoda v odvozené třídě nepředchází klíčové slovo [New](../../language-reference/keywords/new-modifier.md) nebo [override](../../language-reference/keywords/override.md) , kompilátor vydá upozornění a metoda se bude chovat, jako kdyby `new` bylo klíčové slovo přítomno.</span><span class="sxs-lookup"><span data-stu-id="e3139-107">If the method in the derived class is not preceded by [new](../../language-reference/keywords/new-modifier.md) or [override](../../language-reference/keywords/override.md) keywords, the compiler will issue a warning and the method will behave as if the `new` keyword were present.</span></span>  
  
- <span data-ttu-id="e3139-108">Je-li metoda v odvozené třídě uvozena `new` klíčovým slovem, je metoda definována jako nezávislá na metodě v základní třídě.</span><span class="sxs-lookup"><span data-stu-id="e3139-108">If the method in the derived class is preceded with the `new` keyword, the method is defined as being independent of the method in the base class.</span></span>  
  
- <span data-ttu-id="e3139-109">Pokud metoda v odvozené třídě předchází `override` klíčové slovo, objekty odvozené třídy budou volat tuto metodu namísto metody základní třídy.</span><span class="sxs-lookup"><span data-stu-id="e3139-109">If the method in the derived class is preceded with the `override` keyword, objects of the derived class will call that method instead of the base class method.</span></span>  

- <span data-ttu-id="e3139-110">Aby bylo možné použít `override` klíčové slovo pro metodu v odvozené třídě, musí být metoda základní třídy definovaná jako [virtuální](../../language-reference/keywords/virtual.md).</span><span class="sxs-lookup"><span data-stu-id="e3139-110">In order to apply the `override` keyword to the method in the derived class, the base class method must be defined [virtual](../../language-reference/keywords/virtual.md).</span></span>
  
- <span data-ttu-id="e3139-111">Metoda základní třídy může být volána v rámci odvozené třídy pomocí `base` klíčového slova.</span><span class="sxs-lookup"><span data-stu-id="e3139-111">The base class method can be called from within the derived class using the `base` keyword.</span></span>  
  
- <span data-ttu-id="e3139-112">`override` `virtual` `new` Klíčová slova, a lze také použít pro vlastnosti, indexery a události.</span><span class="sxs-lookup"><span data-stu-id="e3139-112">The `override`, `virtual`, and `new` keywords can also be applied to properties, indexers, and events.</span></span>  
  
 <span data-ttu-id="e3139-113">Ve výchozím nastavení nejsou metody jazyka C# virtuální.</span><span class="sxs-lookup"><span data-stu-id="e3139-113">By default, C# methods are not virtual.</span></span> <span data-ttu-id="e3139-114">Pokud je metoda deklarována jako Virtual, jakákoliv třída, která dědí metodu, může implementovat svou vlastní verzi.</span><span class="sxs-lookup"><span data-stu-id="e3139-114">If a method is declared as virtual, any class inheriting the method can implement its own version.</span></span> <span data-ttu-id="e3139-115">Chcete-li vytvořit metodu Virtual, `virtual` je použit modifikátor v deklaraci metody základní třídy.</span><span class="sxs-lookup"><span data-stu-id="e3139-115">To make a method virtual, the `virtual` modifier is used in the method declaration of the base class.</span></span> <span data-ttu-id="e3139-116">Odvozená třída pak může přepsat základní virtuální metodu pomocí `override` klíčového slova nebo skrýt virtuální metodu v základní třídě pomocí `new` klíčového slova.</span><span class="sxs-lookup"><span data-stu-id="e3139-116">The derived class can then override the base virtual method by using the `override` keyword or hide the virtual method in the base class by using the `new` keyword.</span></span> <span data-ttu-id="e3139-117">Pokud `override` není zadáno klíčové slovo ani `new` klíčové slovo, kompilátor vydá upozornění a metoda v odvozené třídě skryje metodu v základní třídě.</span><span class="sxs-lookup"><span data-stu-id="e3139-117">If neither the `override` keyword nor the `new` keyword is specified, the compiler will issue a warning and the method in the derived class will hide the method in the base class.</span></span>  
  
 <span data-ttu-id="e3139-118">K tomu je potřeba předpokládat, že společnost A vytvořila třídu s názvem `GraphicsClass` , kterou program používá.</span><span class="sxs-lookup"><span data-stu-id="e3139-118">To demonstrate this in practice, assume for a moment that Company A has created a class named `GraphicsClass`, which your program uses.</span></span> <span data-ttu-id="e3139-119">Následující `GraphicsClass` :</span><span class="sxs-lookup"><span data-stu-id="e3139-119">The following is `GraphicsClass`:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#27](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#27)]  
  
 <span data-ttu-id="e3139-120">Vaše společnost tuto třídu používá a použijete ji k odvození vlastní třídy a přidání nové metody:</span><span class="sxs-lookup"><span data-stu-id="e3139-120">Your company uses this class, and you use it to derive your own class, adding a new method:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#28](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#28)]  
  
 <span data-ttu-id="e3139-121">Vaše aplikace se používá bez problémů, dokud společnost A neuvolní novou verzi nástroje `GraphicsClass` , která se podobá následujícímu kódu:</span><span class="sxs-lookup"><span data-stu-id="e3139-121">Your application is used without problems, until Company A releases a new version of `GraphicsClass`, which resembles the following code:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#29](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#29)]  
  
 <span data-ttu-id="e3139-122">Nová verze `GraphicsClass` nyní obsahuje metodu s názvem `DrawRectangle` .</span><span class="sxs-lookup"><span data-stu-id="e3139-122">The new version of `GraphicsClass` now contains a method named `DrawRectangle`.</span></span> <span data-ttu-id="e3139-123">Zpočátku nedojde k žádnému výskytu.</span><span class="sxs-lookup"><span data-stu-id="e3139-123">Initially, nothing occurs.</span></span> <span data-ttu-id="e3139-124">Nová verze je stále binární kompatibilní se starou verzí.</span><span class="sxs-lookup"><span data-stu-id="e3139-124">The new version is still binary compatible with the old version.</span></span> <span data-ttu-id="e3139-125">Veškerý software, který jste nasadili, bude fungovat i v případě, že je nová třída nainstalovaná na těchto počítačových systémech.</span><span class="sxs-lookup"><span data-stu-id="e3139-125">Any software that you have deployed will continue to work, even if the new class is installed on those computer systems.</span></span> <span data-ttu-id="e3139-126">Všechna existující volání metody `DrawRectangle` budou v odvozené třídě nadále odkazovat na vaši verzi.</span><span class="sxs-lookup"><span data-stu-id="e3139-126">Any existing calls to the method `DrawRectangle` will continue to reference your version, in your derived class.</span></span>  
  
 <span data-ttu-id="e3139-127">Jakmile však aplikaci znovu zkompilujete pomocí nové verze nástroje `GraphicsClass` , obdržíte upozornění od kompilátoru, CS0108.</span><span class="sxs-lookup"><span data-stu-id="e3139-127">However, as soon as you recompile your application by using the new version of `GraphicsClass`, you will receive a warning from the compiler, CS0108.</span></span> <span data-ttu-id="e3139-128">Toto upozornění vás informuje o tom, že je nutné vzít v úvahu, jak se má vaše `DrawRectangle` Metoda chovat ve vaší aplikaci.</span><span class="sxs-lookup"><span data-stu-id="e3139-128">This warning informs you that you have to consider how you want your `DrawRectangle` method to behave in your application.</span></span>  
  
 <span data-ttu-id="e3139-129">Chcete-li, aby metoda přepsala novou metodu základní třídy, použijte `override` klíčové slovo:</span><span class="sxs-lookup"><span data-stu-id="e3139-129">If you want your method to override the new base class method, use the `override` keyword:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#30](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#30)]  
  
 <span data-ttu-id="e3139-130">`override`Klíčové slovo zajistí, že všechny objekty odvozené z `YourDerivedGraphicsClass` budou používat odvozenou třídu verze `DrawRectangle` .</span><span class="sxs-lookup"><span data-stu-id="e3139-130">The `override` keyword makes sure that any objects derived from `YourDerivedGraphicsClass` will use the derived class version of `DrawRectangle`.</span></span> <span data-ttu-id="e3139-131">Objekty odvozené od `YourDerivedGraphicsClass` mají stále přístup k verzi základní třídy `DrawRectangle` pomocí klíčového slova Base:</span><span class="sxs-lookup"><span data-stu-id="e3139-131">Objects derived from `YourDerivedGraphicsClass` can still access the base class version of `DrawRectangle` by using the base keyword:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#44](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#44)]  
  
 <span data-ttu-id="e3139-132">Pokud nechcete, aby metoda přepsala novou metodu základní třídy, platí následující požadavky.</span><span class="sxs-lookup"><span data-stu-id="e3139-132">If you do not want your method to override the new base class method, the following considerations apply.</span></span> <span data-ttu-id="e3139-133">Chcete-li se vyhnout nejasnostem mezi těmito dvěma metodami, můžete přejmenovat metodu.</span><span class="sxs-lookup"><span data-stu-id="e3139-133">To avoid confusion between the two methods, you can rename your method.</span></span> <span data-ttu-id="e3139-134">To může být časově náročné a náchylné k chybám a v některých případech to není prakticky praktické.</span><span class="sxs-lookup"><span data-stu-id="e3139-134">This can be time-consuming and error-prone, and just not practical in some cases.</span></span> <span data-ttu-id="e3139-135">Pokud je však projekt relativně malý, můžete k přejmenování metody použít možnosti refaktoringu sady Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="e3139-135">However, if your project is relatively small, you can use Visual Studio's Refactoring options to rename the method.</span></span> <span data-ttu-id="e3139-136">Další informace naleznete v tématu [Refaktoring tříd a typů (návrhář tříd)](/visualstudio/ide/class-designer/refactoring-classes-and-types).</span><span class="sxs-lookup"><span data-stu-id="e3139-136">For more information, see [Refactoring Classes and Types (Class Designer)](/visualstudio/ide/class-designer/refactoring-classes-and-types).</span></span>  
  
 <span data-ttu-id="e3139-137">Alternativně můžete zabránit upozornění pomocí klíčového slova `new` v definici odvozené třídy:</span><span class="sxs-lookup"><span data-stu-id="e3139-137">Alternatively, you can prevent the warning by using the keyword `new` in your derived class definition:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#31](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#31)]  
  
 <span data-ttu-id="e3139-138">Použití `new` klíčového slova dává kompilátoru pokyn, že definice skrývá definici, která je obsažena v základní třídě.</span><span class="sxs-lookup"><span data-stu-id="e3139-138">Using the `new` keyword tells the compiler that your definition hides the definition that is contained in the base class.</span></span> <span data-ttu-id="e3139-139">Toto je výchozí chování.</span><span class="sxs-lookup"><span data-stu-id="e3139-139">This is the default behavior.</span></span>  
  
## <a name="override-and-method-selection"></a><span data-ttu-id="e3139-140">Přepsání a výběr metody</span><span class="sxs-lookup"><span data-stu-id="e3139-140">Override and Method Selection</span></span>  
 <span data-ttu-id="e3139-141">Pokud je metoda pojmenována na třídě, kompilátor jazyka C# vybere nejlepší metodu pro volání, pokud je více než jedna metoda kompatibilní se voláním, například pokud existují dvě metody se stejným názvem a parametry, které jsou kompatibilní s předaným parametrem.</span><span class="sxs-lookup"><span data-stu-id="e3139-141">When a method is named on a class, the C# compiler selects the best method to call if more than one method is compatible with the call, such as when there are two methods with the same name, and parameters that are compatible with the parameter passed.</span></span> <span data-ttu-id="e3139-142">Následující metody by byly kompatibilní:</span><span class="sxs-lookup"><span data-stu-id="e3139-142">The following methods would be compatible:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#32](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#32)]  
  
 <span data-ttu-id="e3139-143">Když `DoWork` je volána na instanci `Derived` , kompilátor jazyka C# se nejprve pokusí provést volání kompatibilní s verzemi `DoWork` deklarovanými původně na `Derived` .</span><span class="sxs-lookup"><span data-stu-id="e3139-143">When `DoWork` is called on an instance of `Derived`, the C# compiler will first try to make the call compatible with the versions of `DoWork` declared originally on `Derived`.</span></span> <span data-ttu-id="e3139-144">Metody přepsání nejsou považovány za deklarované u třídy, jsou to nové implementace metody deklarované u základní třídy.</span><span class="sxs-lookup"><span data-stu-id="e3139-144">Override methods are not considered as declared on a class, they are new implementations of a method declared on a base class.</span></span> <span data-ttu-id="e3139-145">Pouze v případě, že kompilátor jazyka C# nemůže souhlasit s voláním metody původní metody, se `Derived` pokusí porovnat volání přepsané metody se stejným názvem a kompatibilními parametry.</span><span class="sxs-lookup"><span data-stu-id="e3139-145">Only if the C# compiler cannot match the method call to an original method on `Derived` will it try to match the call to an overridden method with the same name and compatible parameters.</span></span> <span data-ttu-id="e3139-146">Příklad:</span><span class="sxs-lookup"><span data-stu-id="e3139-146">For example:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#33](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#33)]  
  
 <span data-ttu-id="e3139-147">Vzhledem k tomu `val` , že proměnnou lze převést na typ Double implicitně, kompilátor jazyka C# `DoWork(double)` místo `DoWork(int)` .</span><span class="sxs-lookup"><span data-stu-id="e3139-147">Because the variable `val` can be converted to a double implicitly, the C# compiler calls `DoWork(double)` instead of `DoWork(int)`.</span></span> <span data-ttu-id="e3139-148">Neexistují dva způsoby, jak se tomu vyhnout.</span><span class="sxs-lookup"><span data-stu-id="e3139-148">There are two ways to avoid this.</span></span> <span data-ttu-id="e3139-149">Nejprve Vyhněte deklaraci nových metod se stejným názvem jako virtuální metody.</span><span class="sxs-lookup"><span data-stu-id="e3139-149">First, avoid declaring new methods with the same name as virtual methods.</span></span> <span data-ttu-id="e3139-150">Za druhé můžete instruovat kompilátor jazyka C#, aby vyvolal virtuální metodu tím, že prohledá seznam metod základní třídy přetypováním instance `Derived` na `Base` .</span><span class="sxs-lookup"><span data-stu-id="e3139-150">Second, you can instruct the C# compiler to call the virtual method by making it search the base class method list by casting the instance of `Derived` to `Base`.</span></span> <span data-ttu-id="e3139-151">Vzhledem k tomu, že metoda je virtuální, `DoWork(int)` bude `Derived` volána implementace on.</span><span class="sxs-lookup"><span data-stu-id="e3139-151">Because the method is virtual, the implementation of `DoWork(int)` on `Derived` will be called.</span></span> <span data-ttu-id="e3139-152">Příklad:</span><span class="sxs-lookup"><span data-stu-id="e3139-152">For example:</span></span>  
  
 [!code-csharp[csProgGuideInheritance#34](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideInheritance/CS/Inheritance.cs#34)]  
  
 <span data-ttu-id="e3139-153">Další příklady a najdete `new` `override` v tématu [znalost, kdy použít klíčová slova override a New](./knowing-when-to-use-override-and-new-keywords.md).</span><span class="sxs-lookup"><span data-stu-id="e3139-153">For more examples of `new` and `override`, see [Knowing When to Use Override and New Keywords](./knowing-when-to-use-override-and-new-keywords.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="e3139-154">Viz také</span><span class="sxs-lookup"><span data-stu-id="e3139-154">See also</span></span>

- [<span data-ttu-id="e3139-155">Průvodce programováním v C#</span><span class="sxs-lookup"><span data-stu-id="e3139-155">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="e3139-156">Třídy a struktury</span><span class="sxs-lookup"><span data-stu-id="e3139-156">Classes and Structs</span></span>](./index.md)
- [<span data-ttu-id="e3139-157">Metody</span><span class="sxs-lookup"><span data-stu-id="e3139-157">Methods</span></span>](./methods.md)
- [<span data-ttu-id="e3139-158">Dědičnost</span><span class="sxs-lookup"><span data-stu-id="e3139-158">Inheritance</span></span>](./inheritance.md)
