---
title: Použití delegátů – Průvodce programováním v C#
description: Naučte se používat delegáty. Delegáti jsou objektově orientovaný, typově bezpečný a zabezpečený typ, který bezpečně zapouzdřuje metodu.
ms.date: 07/20/2015
helpviewer_keywords:
- delegates [C#], how to use
ms.assetid: 99a2fc27-a32e-4a34-921c-e65497520eec
ms.openlocfilehash: a9b625b8c0785ed2f446be27c11dc76108bc4bce
ms.sourcegitcommit: 6f58a5f75ceeb936f8ee5b786e9adb81a9a3bee9
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/28/2020
ms.locfileid: "87302149"
---
# <a name="using-delegates-c-programming-guide"></a><span data-ttu-id="d774e-104">Použití delegátů (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="d774e-104">Using Delegates (C# Programming Guide)</span></span>

<span data-ttu-id="d774e-105">[Delegát](../../language-reference/builtin-types/reference-types.md) je typ, který bezpečně zapouzdřuje metodu, podobně jako ukazatel na funkci v jazyce C a C++.</span><span class="sxs-lookup"><span data-stu-id="d774e-105">A [delegate](../../language-reference/builtin-types/reference-types.md) is a type that safely encapsulates a method, similar to a function pointer in C and C++.</span></span> <span data-ttu-id="d774e-106">Na rozdíl od ukazatelů funkcí jazyka C jsou delegáti objektově orientovaný, typově bezpečný a zabezpečený.</span><span class="sxs-lookup"><span data-stu-id="d774e-106">Unlike C function pointers, delegates are object-oriented, type safe, and secure.</span></span> <span data-ttu-id="d774e-107">Typ delegáta je definován názvem delegáta.</span><span class="sxs-lookup"><span data-stu-id="d774e-107">The type of a delegate is defined by the name of the delegate.</span></span> <span data-ttu-id="d774e-108">Následující příklad deklaruje delegáta s názvem `Del` , který může zapouzdřit metodu, která přijímá [řetězec](../../language-reference/builtin-types/reference-types.md) jako argument a vrací [typ void](../../language-reference/builtin-types/void.md):</span><span class="sxs-lookup"><span data-stu-id="d774e-108">The following example declares a delegate named `Del` that can encapsulate a method that takes a [string](../../language-reference/builtin-types/reference-types.md) as an argument and returns [void](../../language-reference/builtin-types/void.md):</span></span>

[!code-csharp[csProgGuideDelegates#21](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideDelegates/CS/Delegates.cs#21)]

<span data-ttu-id="d774e-109">Objekt delegáta je obvykle vytvořen tak, že poskytuje název metody, které bude delegát zalamovat, nebo s [anonymní funkcí](../statements-expressions-operators/anonymous-functions.md).</span><span class="sxs-lookup"><span data-stu-id="d774e-109">A delegate object is normally constructed by providing the name of the method the delegate will wrap, or with an [anonymous function](../statements-expressions-operators/anonymous-functions.md).</span></span> <span data-ttu-id="d774e-110">Po vytvoření instance delegáta bude do této metody předána volání metody provedené delegátem.</span><span class="sxs-lookup"><span data-stu-id="d774e-110">Once a delegate is instantiated, a method call made to the delegate will be passed by the delegate to that method.</span></span> <span data-ttu-id="d774e-111">Parametry předané delegátovi jsou předány metodě a návratová hodnota, pokud existuje, z metody se vrátí volajícímu delegátovi.</span><span class="sxs-lookup"><span data-stu-id="d774e-111">The parameters passed to the delegate by the caller are passed to the method, and the return value, if any, from the method is returned to the caller by the delegate.</span></span> <span data-ttu-id="d774e-112">Označuje se jako volání delegáta.</span><span class="sxs-lookup"><span data-stu-id="d774e-112">This is known as invoking the delegate.</span></span> <span data-ttu-id="d774e-113">Instanci delegáta lze vyvolat, jako by se jednalo o zabalenou metodu.</span><span class="sxs-lookup"><span data-stu-id="d774e-113">An instantiated delegate can be invoked as if it were the wrapped method itself.</span></span> <span data-ttu-id="d774e-114">Příklad:</span><span class="sxs-lookup"><span data-stu-id="d774e-114">For example:</span></span>

[!code-csharp[csProgGuideDelegates#22](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideDelegates/CS/Delegates.cs#22)]  

[!code-csharp[csProgGuideDelegates#23](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideDelegates/CS/Delegates.cs#23)]

<span data-ttu-id="d774e-115">Typy delegátů jsou odvozeny z <xref:System.Delegate> třídy v rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="d774e-115">Delegate types are derived from the <xref:System.Delegate> class in .NET.</span></span> <span data-ttu-id="d774e-116">Typy delegátů jsou [zapečetěné](../../language-reference/keywords/sealed.md)– nemůžou být odvozené od – a není možné odvodit vlastní třídy z <xref:System.Delegate> .</span><span class="sxs-lookup"><span data-stu-id="d774e-116">Delegate types are [sealed](../../language-reference/keywords/sealed.md)—they cannot be derived from— and it is not possible to derive custom classes from <xref:System.Delegate>.</span></span> <span data-ttu-id="d774e-117">Vzhledem k tomu, že konstruktor s vytvořeným instancemi je objekt, může být předán jako parametr nebo přiřazen vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="d774e-117">Because the instantiated delegate is an object, it can be passed as a parameter, or assigned to a property.</span></span> <span data-ttu-id="d774e-118">To umožňuje, aby metoda přijímala delegáta jako parametr a později volala delegáta.</span><span class="sxs-lookup"><span data-stu-id="d774e-118">This allows a method to accept a delegate as a parameter, and call the delegate at some later time.</span></span> <span data-ttu-id="d774e-119">Tento postup se označuje jako asynchronní zpětné volání a je běžnou metodou upozorňování volajícího na dokončení dlouhého procesu.</span><span class="sxs-lookup"><span data-stu-id="d774e-119">This is known as an asynchronous callback, and is a common method of notifying a caller when a long process has completed.</span></span> <span data-ttu-id="d774e-120">V případě použití delegáta tímto způsobem kód používající delegáta nepotřebuje žádné znalosti implementace používané metody.</span><span class="sxs-lookup"><span data-stu-id="d774e-120">When a delegate is used in this fashion, the code using the delegate does not need any knowledge of the implementation of the method being used.</span></span> <span data-ttu-id="d774e-121">Funkčnost je podobná rozhraním zapouzdření.</span><span class="sxs-lookup"><span data-stu-id="d774e-121">The functionality is similar to the encapsulation interfaces provide.</span></span>

<span data-ttu-id="d774e-122">Dalším běžným použitím zpětných volání je definování vlastní metody porovnání a předání tohoto delegáta metodě řazení.</span><span class="sxs-lookup"><span data-stu-id="d774e-122">Another common use of callbacks is defining a custom comparison method and passing that delegate to a sort method.</span></span> <span data-ttu-id="d774e-123">Umožňuje, aby se kód volajícího stal součástí algoritmu řazení.</span><span class="sxs-lookup"><span data-stu-id="d774e-123">It allows the caller's code to become part of the sort algorithm.</span></span> <span data-ttu-id="d774e-124">Následující příklad metody používá `Del` typ jako parametr:</span><span class="sxs-lookup"><span data-stu-id="d774e-124">The following example method uses the `Del` type as a parameter:</span></span>

[!code-csharp[csProgGuideDelegates#24](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideDelegates/CS/Delegates.cs#24)]

<span data-ttu-id="d774e-125">Potom můžete předat delegáta, který jste vytvořili výše, do této metody:</span><span class="sxs-lookup"><span data-stu-id="d774e-125">You can then pass the delegate created above to that method:</span></span>

[!code-csharp[csProgGuideDelegates#25](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideDelegates/CS/Delegates.cs#25)]

<span data-ttu-id="d774e-126">a dostanete následující výstup do konzoly:</span><span class="sxs-lookup"><span data-stu-id="d774e-126">and receive the following output to the console:</span></span>

```console
The number is: 3
```

<span data-ttu-id="d774e-127">Použití delegáta jako abstrakce `MethodWithCallback` není nutné volat konzolu přímo – nemusí být navržena s ohledem na konzolu.</span><span class="sxs-lookup"><span data-stu-id="d774e-127">Using the delegate as an abstraction, `MethodWithCallback` does not need to call the console directly—it does not have to be designed with a console in mind.</span></span> <span data-ttu-id="d774e-128">Co `MethodWithCallback` je jednoduše Příprava řetězce a předání řetězce do jiné metody.</span><span class="sxs-lookup"><span data-stu-id="d774e-128">What `MethodWithCallback` does is simply prepare a string and pass the string to another method.</span></span> <span data-ttu-id="d774e-129">To je obzvláště výkonné, protože delegovaná metoda může používat libovolný počet parametrů.</span><span class="sxs-lookup"><span data-stu-id="d774e-129">This is especially powerful since a delegated method can use any number of parameters.</span></span>

<span data-ttu-id="d774e-130">Když je delegát vytvořen k zabalení metody instance, delegát odkazuje jak na instanci, tak na metodu.</span><span class="sxs-lookup"><span data-stu-id="d774e-130">When a delegate is constructed to wrap an instance method, the delegate references both the instance and the method.</span></span> <span data-ttu-id="d774e-131">Delegát nemá žádné znalosti o typu instance z metody, kterou zabalí, takže delegát může odkazovat na libovolný typ objektu, pokud existuje metoda, která odpovídá signatuře delegáta.</span><span class="sxs-lookup"><span data-stu-id="d774e-131">A delegate has no knowledge of the instance type aside from the method it wraps, so a delegate can refer to any type of object as long as there is a method on that object that matches the delegate signature.</span></span> <span data-ttu-id="d774e-132">Když je delegát vytvořen pro zabalení statické metody, odkazuje pouze na metodu.</span><span class="sxs-lookup"><span data-stu-id="d774e-132">When a delegate is constructed to wrap a static method, it only references the method.</span></span> <span data-ttu-id="d774e-133">Vezměte v úvahu následující deklarace:</span><span class="sxs-lookup"><span data-stu-id="d774e-133">Consider the following declarations:</span></span>

[!code-csharp[csProgGuideDelegates#26](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideDelegates/CS/Delegates.cs#26)]

<span data-ttu-id="d774e-134">Společně se statickým `DelegateMethod` uvedeným dříve máme tři metody, které mohou být zabaleny `Del` instancí.</span><span class="sxs-lookup"><span data-stu-id="d774e-134">Along with the static `DelegateMethod` shown previously, we now have three methods that can be wrapped by a `Del` instance.</span></span>

<span data-ttu-id="d774e-135">Delegát může při vyvolání volat více než jednu metodu.</span><span class="sxs-lookup"><span data-stu-id="d774e-135">A delegate can call more than one method when invoked.</span></span> <span data-ttu-id="d774e-136">Tato funkce se označuje jako vícesměrové vysílání.</span><span class="sxs-lookup"><span data-stu-id="d774e-136">This is referred to as multicasting.</span></span> <span data-ttu-id="d774e-137">Chcete-li přidat další metodu do seznamu metod delegáta – seznam vyvolání – vyžaduje přidání dvou delegátů pomocí operátorů sčítání nebo sčítání (' + ' nebo ' + = ').</span><span class="sxs-lookup"><span data-stu-id="d774e-137">To add an extra method to the delegate's list of methods—the invocation list—simply requires adding two delegates using the addition or addition assignment operators ('+' or '+=').</span></span> <span data-ttu-id="d774e-138">Příklad:</span><span class="sxs-lookup"><span data-stu-id="d774e-138">For example:</span></span>

[!code-csharp[csProgGuideDelegates#27](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideDelegates/CS/Delegates.cs#27)]

<span data-ttu-id="d774e-139">V tomto okamžiku `allMethodsDelegate` obsahuje tři metody v seznamu jeho vyvolání – `Method1` , `Method2` a `DelegateMethod` .</span><span class="sxs-lookup"><span data-stu-id="d774e-139">At this point `allMethodsDelegate` contains three methods in its invocation list—`Method1`, `Method2`, and `DelegateMethod`.</span></span> <span data-ttu-id="d774e-140">Původní tři delegáti, `d1` , `d2` a `d3` , zůstávají beze změny.</span><span class="sxs-lookup"><span data-stu-id="d774e-140">The original three delegates, `d1`, `d2`, and `d3`, remain unchanged.</span></span> <span data-ttu-id="d774e-141">Když `allMethodsDelegate` je vyvolána, všechny tři metody jsou volány v pořadí.</span><span class="sxs-lookup"><span data-stu-id="d774e-141">When `allMethodsDelegate` is invoked, all three methods are called in order.</span></span> <span data-ttu-id="d774e-142">Pokud delegát používá referenční parametry, je odkaz předán postupně každé tři metody a všechny změny v jedné metodě jsou viditelné pro další metodu.</span><span class="sxs-lookup"><span data-stu-id="d774e-142">If the delegate uses reference parameters, the reference is passed sequentially to each of the three methods in turn, and any changes by one method are visible to the next method.</span></span> <span data-ttu-id="d774e-143">Když kterákoli z metod vyvolá výjimku, která není zachycena v rámci metody, je tato výjimka předána volajícímu delegáta a žádné následné metody v seznamu volání nejsou volány.</span><span class="sxs-lookup"><span data-stu-id="d774e-143">When any of the methods throws an exception that is not caught within the method, that exception is passed to the caller of the delegate and no subsequent methods in the invocation list are called.</span></span> <span data-ttu-id="d774e-144">Pokud má delegát návratovou hodnotu nebo výstupní parametry, vrátí návratovou hodnotu a parametry poslední vyvolané metody.</span><span class="sxs-lookup"><span data-stu-id="d774e-144">If the delegate has a return value and/or out parameters, it returns the return value and parameters of the last method invoked.</span></span> <span data-ttu-id="d774e-145">Chcete-li odebrat metodu ze seznamu vyvolání, použijte [operátory přiřazení odčítání nebo odčítání](../../language-reference/operators/subtraction-operator.md) ( `-` nebo `-=` ).</span><span class="sxs-lookup"><span data-stu-id="d774e-145">To remove a method from the invocation list, use the [subtraction or subtraction assignment operators](../../language-reference/operators/subtraction-operator.md) (`-` or `-=`).</span></span> <span data-ttu-id="d774e-146">Příklad:</span><span class="sxs-lookup"><span data-stu-id="d774e-146">For example:</span></span>

[!code-csharp[csProgGuideDelegates#28](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideDelegates/CS/Delegates.cs#28)]

<span data-ttu-id="d774e-147">Vzhledem k tomu, že typy delegátů jsou odvozeny z `System.Delegate` , metody a vlastnosti definované touto třídou mohou být volány na delegáta.</span><span class="sxs-lookup"><span data-stu-id="d774e-147">Because delegate types are derived from `System.Delegate`, the methods and properties defined by that class can be called on the delegate.</span></span> <span data-ttu-id="d774e-148">Pokud například chcete najít počet metod v seznamu volání delegáta, můžete napsat:</span><span class="sxs-lookup"><span data-stu-id="d774e-148">For example, to find the number of methods in a delegate's invocation list, you may write:</span></span>

[!code-csharp[csProgGuideDelegates#29](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideDelegates/CS/Delegates.cs#29)]

<span data-ttu-id="d774e-149">Delegáti s více než jednou metodou v seznamu volání jsou odvozeni z <xref:System.MulticastDelegate> , což je podtřídou třídy `System.Delegate` .</span><span class="sxs-lookup"><span data-stu-id="d774e-149">Delegates with more than one method in their invocation list derive from <xref:System.MulticastDelegate>, which is a subclass of `System.Delegate`.</span></span> <span data-ttu-id="d774e-150">Výše uvedený kód funguje v obou případech, protože obě třídy podporují `GetInvocationList` .</span><span class="sxs-lookup"><span data-stu-id="d774e-150">The above code works in either case because both classes support `GetInvocationList`.</span></span>

<span data-ttu-id="d774e-151">Delegáti vícesměrového vysílání se často používají při zpracování událostí.</span><span class="sxs-lookup"><span data-stu-id="d774e-151">Multicast delegates are used extensively in event handling.</span></span> <span data-ttu-id="d774e-152">Zdrojové objekty událostí odesílají oznámení o událostech do objektů příjemců, které jsou zaregistrované pro příjem této události.</span><span class="sxs-lookup"><span data-stu-id="d774e-152">Event source objects send event notifications to recipient objects that have registered to receive that event.</span></span> <span data-ttu-id="d774e-153">K registraci pro událost příjemce vytvoří metodu, která je navržena pro zpracování události, a pak vytvoří delegáta pro tuto metodu a předá delegáta zdroji události.</span><span class="sxs-lookup"><span data-stu-id="d774e-153">To register for an event, the recipient creates a method designed to handle the event, then creates a delegate for that method and passes the delegate to the event source.</span></span> <span data-ttu-id="d774e-154">Zdroj volá delegáta, když dojde k události.</span><span class="sxs-lookup"><span data-stu-id="d774e-154">The source calls the delegate when the event occurs.</span></span> <span data-ttu-id="d774e-155">Delegát pak zavolá metodu zpracování události na příjemce a doručí data události.</span><span class="sxs-lookup"><span data-stu-id="d774e-155">The delegate then calls the event handling method on the recipient, delivering the event data.</span></span> <span data-ttu-id="d774e-156">Typ delegáta pro danou událost je definován zdrojem události.</span><span class="sxs-lookup"><span data-stu-id="d774e-156">The delegate type for a given event is defined by the event source.</span></span> <span data-ttu-id="d774e-157">Další informace najdete v tématu [události](../events/index.md).</span><span class="sxs-lookup"><span data-stu-id="d774e-157">For more, see [Events](../events/index.md).</span></span>

<span data-ttu-id="d774e-158">Porovnání delegátů dvou různých typů přiřazených v době kompilace způsobí chybu kompilace.</span><span class="sxs-lookup"><span data-stu-id="d774e-158">Comparing delegates of two different types assigned at compile-time will result in a compilation error.</span></span> <span data-ttu-id="d774e-159">Pokud jsou instance delegátů staticky typu `System.Delegate` , je porovnávání povoleno, ale v době běhu vrátí hodnotu false.</span><span class="sxs-lookup"><span data-stu-id="d774e-159">If the delegate instances are statically of the type `System.Delegate`, then the comparison is allowed, but will return false at run time.</span></span> <span data-ttu-id="d774e-160">Příklad:</span><span class="sxs-lookup"><span data-stu-id="d774e-160">For example:</span></span>

[!code-csharp[csProgGuideDelegates#30](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideDelegates/CS/Delegates.cs#30)]

## <a name="see-also"></a><span data-ttu-id="d774e-161">Viz také:</span><span class="sxs-lookup"><span data-stu-id="d774e-161">See also</span></span>

- [<span data-ttu-id="d774e-162">Průvodce programováním v C#</span><span class="sxs-lookup"><span data-stu-id="d774e-162">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="d774e-163">Delegáti</span><span class="sxs-lookup"><span data-stu-id="d774e-163">Delegates</span></span>](./index.md)
- [<span data-ttu-id="d774e-164">Použití odchylek v delegátech</span><span class="sxs-lookup"><span data-stu-id="d774e-164">Using Variance in Delegates</span></span>](../concepts/covariance-contravariance/using-variance-in-delegates.md)
- [<span data-ttu-id="d774e-165">Odchylky v delegátech</span><span class="sxs-lookup"><span data-stu-id="d774e-165">Variance in Delegates</span></span>](../concepts/covariance-contravariance/variance-in-delegates.md)
- [<span data-ttu-id="d774e-166">Použití odchylek pro obecné delegáty Func a Action</span><span class="sxs-lookup"><span data-stu-id="d774e-166">Using Variance for Func and Action Generic Delegates</span></span>](../concepts/covariance-contravariance/using-variance-for-func-and-action-generic-delegates.md)
- [<span data-ttu-id="d774e-167">Události</span><span class="sxs-lookup"><span data-stu-id="d774e-167">Events</span></span>](../events/index.md)
