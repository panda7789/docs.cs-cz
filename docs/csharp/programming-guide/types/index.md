---
title: Typy – C# Průvodce programováním
ms.date: 07/20/2015
helpviewer_keywords:
- value types [C#]
- reference types [C#]
- types [C#]
- C# language, data types
- common type system [C#]
- data types [C#]
- C# language, types
- strong typing [C#]
ms.assetid: f782d7cc-035e-4500-b1b1-36a9881130ad
ms.openlocfilehash: be2af77f9680ab3285720525638bba8ef2041999
ms.sourcegitcommit: de17a7a0a37042f0d4406f5ae5393531caeb25ba
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 01/24/2020
ms.locfileid: "76745376"
---
# <a name="types-c-programming-guide"></a><span data-ttu-id="7fe52-102">Typy (Průvodce programováním v C#)</span><span class="sxs-lookup"><span data-stu-id="7fe52-102">Types (C# Programming Guide)</span></span>

## <a name="types-variables-and-values"></a><span data-ttu-id="7fe52-103">Typy, proměnné a hodnoty</span><span class="sxs-lookup"><span data-stu-id="7fe52-103">Types, variables, and values</span></span>

<span data-ttu-id="7fe52-104">C#je jazyk silného typu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-104">C# is a strongly-typed language.</span></span> <span data-ttu-id="7fe52-105">Každá proměnná a konstanta má typ, stejně jako každý výraz, který je vyhodnocen na hodnotu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-105">Every variable and constant has a type, as does every expression that evaluates to a value.</span></span> <span data-ttu-id="7fe52-106">Každý podpis metody určuje typ pro každý vstupní parametr a pro návratovou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-106">Every method signature specifies a type for each input parameter and for the return value.</span></span> <span data-ttu-id="7fe52-107">Knihovna tříd .NET definuje sadu předdefinovaných číselných typů i složitější typy, které reprezentují širokou škálu logických konstrukcí, jako je například systém souborů, síťová připojení, kolekce a pole objektů a data.</span><span class="sxs-lookup"><span data-stu-id="7fe52-107">The .NET class library defines a set of built-in numeric types as well as more complex types that represent a wide variety of logical constructs, such as the file system, network connections, collections and arrays of objects, and dates.</span></span> <span data-ttu-id="7fe52-108">Typický C# program používá typy z knihovny tříd a také uživatelsky definované typy, které modelují koncepty specifické pro doménu problému programu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-108">A typical C# program uses types from the class library as well as user-defined types that model the concepts that are specific to the program's problem domain.</span></span>

<span data-ttu-id="7fe52-109">Informace uložené v typu mohou zahrnovat následující:</span><span class="sxs-lookup"><span data-stu-id="7fe52-109">The information stored in a type can include the following:</span></span>

- <span data-ttu-id="7fe52-110">Prostor úložiště, který proměnná typu vyžaduje.</span><span class="sxs-lookup"><span data-stu-id="7fe52-110">The storage space that a variable of the type requires.</span></span>

- <span data-ttu-id="7fe52-111">Maximální a minimální hodnoty, které může představovat.</span><span class="sxs-lookup"><span data-stu-id="7fe52-111">The maximum and minimum values that it can represent.</span></span>

- <span data-ttu-id="7fe52-112">Členové (metody, pole, události atd.), které obsahuje.</span><span class="sxs-lookup"><span data-stu-id="7fe52-112">The members (methods, fields, events, and so on) that it contains.</span></span>

- <span data-ttu-id="7fe52-113">Základní typ, ze kterého dědí.</span><span class="sxs-lookup"><span data-stu-id="7fe52-113">The base type it inherits from.</span></span>

- <span data-ttu-id="7fe52-114">Místo, kde bude přidělena paměť pro proměnné v době běhu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-114">The location where the memory for variables will be allocated at run time.</span></span>

- <span data-ttu-id="7fe52-115">Druhy operací, které jsou povoleny.</span><span class="sxs-lookup"><span data-stu-id="7fe52-115">The kinds of operations that are permitted.</span></span>

<span data-ttu-id="7fe52-116">Kompilátor používá informace o typu k ujištění, že všechny operace, které jsou provedeny ve vašem kódu, jsou *typově bezpečné*.</span><span class="sxs-lookup"><span data-stu-id="7fe52-116">The compiler uses type information to make sure that all operations that are performed in your code are *type safe*.</span></span> <span data-ttu-id="7fe52-117">Například pokud deklarujete proměnnou typu [int](../../language-reference/builtin-types/integral-numeric-types.md), kompilátor vám umožní použít proměnnou v operaci sčítání a odčítání.</span><span class="sxs-lookup"><span data-stu-id="7fe52-117">For example, if you declare a variable of type [int](../../language-reference/builtin-types/integral-numeric-types.md), the compiler allows you to use the variable in addition and subtraction operations.</span></span> <span data-ttu-id="7fe52-118">Pokud se pokusíte provést stejné operace s proměnnou typu [bool](../../language-reference/builtin-types/bool.md), kompilátor vygeneruje chybu, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="7fe52-118">If you try to perform those same operations on a variable of type [bool](../../language-reference/builtin-types/bool.md), the compiler generates an error, as shown in the following example:</span></span>

[!code-csharp[csProgGuideTypes#42](~/samples/snippets/csharp/VS_Snippets_VBCSharp/CsProgGuideTypes/CS/Class1.cs#42)]

> [!NOTE]
> <span data-ttu-id="7fe52-119">C a C++ vývojáři Všimněte si, že C#v není hodnota [bool](../../language-reference/builtin-types/bool.md) převoditelná na [int](../../language-reference/builtin-types/integral-numeric-types.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-119">C and C++ developers, notice that in C#, [bool](../../language-reference/builtin-types/bool.md) is not convertible to [int](../../language-reference/builtin-types/integral-numeric-types.md).</span></span>

<span data-ttu-id="7fe52-120">Kompilátor vloží informace o typu do spustitelného souboru jako metadata.</span><span class="sxs-lookup"><span data-stu-id="7fe52-120">The compiler embeds the type information into the executable file as metadata.</span></span> <span data-ttu-id="7fe52-121">Modul CLR (Common Language Runtime) používá tato metadata v době běhu k dalšímu zajištění bezpečnosti typu při přidělování a uvolňování paměti.</span><span class="sxs-lookup"><span data-stu-id="7fe52-121">The common language runtime (CLR) uses that metadata at run time to further guarantee type safety when it allocates and reclaims memory.</span></span>

### <a name="specifying-types-in-variable-declarations"></a><span data-ttu-id="7fe52-122">Určení typů v deklaracích proměnných</span><span class="sxs-lookup"><span data-stu-id="7fe52-122">Specifying types in variable declarations</span></span>

<span data-ttu-id="7fe52-123">Pokud deklarujete proměnnou nebo konstantu v programu, je nutné buď zadat její typ, nebo použít klíčové slovo [var](../../language-reference/keywords/var.md) , aby kompilátor mohl daný typ odvodit.</span><span class="sxs-lookup"><span data-stu-id="7fe52-123">When you declare a variable or constant in a program, you must either specify its type or use the [var](../../language-reference/keywords/var.md) keyword to let the compiler infer the type.</span></span> <span data-ttu-id="7fe52-124">Následující příklad ukazuje některé deklarace proměnných, které používají předdefinované číselné typy a komplexní uživatelsky definované typy:</span><span class="sxs-lookup"><span data-stu-id="7fe52-124">The following example shows some variable declarations that use both built-in numeric types and complex user-defined types:</span></span>

[!code-csharp[csProgGuideTypes#36](~/samples/snippets/csharp/VS_Snippets_VBCSharp/CsProgGuideTypes/CS/Class1.cs#36)]

<span data-ttu-id="7fe52-125">Typy parametrů metody a návratové hodnoty jsou uvedeny v signatuře metody.</span><span class="sxs-lookup"><span data-stu-id="7fe52-125">The types of method parameters and return values are specified in the method signature.</span></span> <span data-ttu-id="7fe52-126">Následující signatura ukazuje metodu, která vyžaduje [int](../../language-reference/builtin-types/integral-numeric-types.md) jako vstupní argument a vrací řetězec:</span><span class="sxs-lookup"><span data-stu-id="7fe52-126">The following signature shows a method that requires an [int](../../language-reference/builtin-types/integral-numeric-types.md) as an input argument and returns a string:</span></span>

[!code-csharp[csProgGuideTypes#35](~/samples/snippets/csharp/VS_Snippets_VBCSharp/CsProgGuideTypes/CS/Class1.cs#35)]

<span data-ttu-id="7fe52-127">Po deklarování proměnné nemůže být znovu deklarována s novým typem a nelze jí přiřadit hodnotu, která není kompatibilní s deklarovaným typem.</span><span class="sxs-lookup"><span data-stu-id="7fe52-127">After a variable is declared, it cannot be re-declared with a new type, and it cannot be assigned a value that is not compatible with its declared type.</span></span> <span data-ttu-id="7fe52-128">Například nelze deklarovat [int](../../language-reference/builtin-types/integral-numeric-types.md) a přiřadit jí logickou hodnotu `true`.</span><span class="sxs-lookup"><span data-stu-id="7fe52-128">For example, you cannot declare an [int](../../language-reference/builtin-types/integral-numeric-types.md) and then assign it a Boolean value of `true`.</span></span> <span data-ttu-id="7fe52-129">Hodnoty lze však převést na jiné typy, například když jsou přiřazeny k novým proměnným nebo předány jako argumenty metody.</span><span class="sxs-lookup"><span data-stu-id="7fe52-129">However, values can be converted to other types, for example when they are assigned to new variables or passed as method arguments.</span></span> <span data-ttu-id="7fe52-130">*Konverze typu* , která nezpůsobí ztrátu dat, je automaticky prováděna kompilátorem.</span><span class="sxs-lookup"><span data-stu-id="7fe52-130">A *type conversion* that does not cause data loss is performed automatically by the compiler.</span></span> <span data-ttu-id="7fe52-131">Převod, který může způsobit ztrátu dat, vyžaduje *přetypování* ve zdrojovém kódu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-131">A conversion that might cause data loss requires a *cast* in the source code.</span></span>

<span data-ttu-id="7fe52-132">Další informace naleznete v tématu [přetypování a převody typu](./casting-and-type-conversions.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-132">For more information, see [Casting and Type Conversions](./casting-and-type-conversions.md).</span></span>

## <a name="built-in-types"></a><span data-ttu-id="7fe52-133">Předdefinované typy</span><span class="sxs-lookup"><span data-stu-id="7fe52-133">Built-in types</span></span>

<span data-ttu-id="7fe52-134">C#poskytuje standardní sadu předdefinovaných číselných typů k vyjádření celých čísel, hodnot s plovoucí desetinnou čárkou, logických výrazů, textových znaků, desetinných hodnot a dalších typů dat.</span><span class="sxs-lookup"><span data-stu-id="7fe52-134">C# provides a standard set of built-in numeric types to represent integers, floating point values, Boolean expressions, text characters, decimal values, and other types of data.</span></span> <span data-ttu-id="7fe52-135">K dispozici jsou také předdefinované `string` a `object` typy.</span><span class="sxs-lookup"><span data-stu-id="7fe52-135">There are also built-in `string` and `object` types.</span></span> <span data-ttu-id="7fe52-136">Jsou k dispozici pro použití v jakémkoli C# programu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-136">These are available for you to use in any C# program.</span></span> <span data-ttu-id="7fe52-137">Další informace o předdefinovaných typech naleznete v tématu [referenční tabulky pro předdefinované typy](../../language-reference/keywords/built-in-types-table.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-137">For more information about the built-in types, see [Reference Tables for built-in Types](../../language-reference/keywords/built-in-types-table.md).</span></span>

## <a name="custom-types"></a><span data-ttu-id="7fe52-138">Vlastní typy</span><span class="sxs-lookup"><span data-stu-id="7fe52-138">Custom types</span></span>

<span data-ttu-id="7fe52-139">Použijete konstrukce [struct](../../language-reference/keywords/struct.md), [Class](../../language-reference/keywords/class.md), [Interface](../../language-reference/keywords/interface.md)a [Enum](../../language-reference/builtin-types/enum.md) k vytvoření vlastních typů.</span><span class="sxs-lookup"><span data-stu-id="7fe52-139">You use the [struct](../../language-reference/keywords/struct.md), [class](../../language-reference/keywords/class.md), [interface](../../language-reference/keywords/interface.md), and [enum](../../language-reference/builtin-types/enum.md) constructs to create your own custom types.</span></span> <span data-ttu-id="7fe52-140">Samotná knihovna tříd .NET je kolekce vlastních typů poskytovaných Microsoftem, které můžete použít ve svých vlastních aplikacích.</span><span class="sxs-lookup"><span data-stu-id="7fe52-140">The .NET class library itself is a collection of custom types provided by Microsoft that you can use in your own applications.</span></span> <span data-ttu-id="7fe52-141">Ve výchozím nastavení jsou nejčastěji používané typy v knihovně tříd k dispozici v jakémkoli C# programu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-141">By default, the most frequently used types in the class library are available in any C# program.</span></span> <span data-ttu-id="7fe52-142">Ostatní budou k dispozici pouze v případě, že explicitně přidáte odkaz na projekt do sestavení, ve kterém jsou definovány.</span><span class="sxs-lookup"><span data-stu-id="7fe52-142">Others become available only when you explicitly add a project reference to the assembly in which they are defined.</span></span> <span data-ttu-id="7fe52-143">Poté, co má kompilátor odkaz na sestavení, můžete deklarovat proměnné (a konstanty) typů deklarovaných v tomto sestavení ve zdrojovém kódu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-143">After the compiler has a reference to the assembly, you can declare variables (and constants) of the types declared in that assembly in source code.</span></span> <span data-ttu-id="7fe52-144">Další informace naleznete v tématu [Knihovna tříd .NET](../../../standard/class-library-overview.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-144">For more information, see [.NET Class Library](../../../standard/class-library-overview.md).</span></span>

## <a name="the-common-type-system"></a><span data-ttu-id="7fe52-145">Obecný systém typů</span><span class="sxs-lookup"><span data-stu-id="7fe52-145">The common type system</span></span>

<span data-ttu-id="7fe52-146">Je důležité porozumět dvěma základním bodům o systému typů v .NET:</span><span class="sxs-lookup"><span data-stu-id="7fe52-146">It is important to understand two fundamental points about the type system in .NET:</span></span>

- <span data-ttu-id="7fe52-147">Podporuje princip dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="7fe52-147">It supports the principle of inheritance.</span></span> <span data-ttu-id="7fe52-148">Typy mohou být odvozeny od jiných typů, které se nazývají *základní typy*.</span><span class="sxs-lookup"><span data-stu-id="7fe52-148">Types can derive from other types, called *base types*.</span></span> <span data-ttu-id="7fe52-149">Odvozený typ dědí (s určitými omezeními) metody, vlastnosti a další členy základního typu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-149">The derived type inherits (with some restrictions) the methods, properties, and other members of the base type.</span></span> <span data-ttu-id="7fe52-150">Základní typ může být odvozen z jiného typu. v takovém případě odvozený typ dědí členy obou základních typů v rámci své Hierarchie dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="7fe52-150">The base type can in turn derive from some other type, in which case the derived type inherits the members of both base types in its inheritance hierarchy.</span></span> <span data-ttu-id="7fe52-151">Všechny typy, včetně předdefinovaných číselných typů, jako je napříkladC# <xref:System.Int32?displayProperty=nameWithType> (klíčové slovo: [int](../../language-reference/builtin-types/integral-numeric-types.md)), jsou odvozeny z jednoho základního typu, kterýC# je <xref:System.Object?displayProperty=nameWithType> (klíčové slovo: [Object](../../language-reference/builtin-types/reference-types.md)).</span><span class="sxs-lookup"><span data-stu-id="7fe52-151">All types, including built-in numeric types such as <xref:System.Int32?displayProperty=nameWithType> (C# keyword: [int](../../language-reference/builtin-types/integral-numeric-types.md)), derive ultimately from a single base type, which is <xref:System.Object?displayProperty=nameWithType> (C# keyword: [object](../../language-reference/builtin-types/reference-types.md)).</span></span> <span data-ttu-id="7fe52-152">Tato hierarchie sjednoceného typu se nazývá CTS ( [Common Type System](../../../standard/base-types/common-type-system.md) ).</span><span class="sxs-lookup"><span data-stu-id="7fe52-152">This unified type hierarchy is called the [Common Type System](../../../standard/base-types/common-type-system.md) (CTS).</span></span> <span data-ttu-id="7fe52-153">Další informace o dědičnosti v C#naleznete v tématu [Dědičnost](../classes-and-structs/inheritance.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-153">For more information about inheritance in C#, see [Inheritance](../classes-and-structs/inheritance.md).</span></span>

- <span data-ttu-id="7fe52-154">Každý typ v CTS je definován buď jako *typ hodnoty* , nebo jako *typ odkazu*.</span><span class="sxs-lookup"><span data-stu-id="7fe52-154">Each type in the CTS is defined as either a *value type* or a *reference type*.</span></span> <span data-ttu-id="7fe52-155">To zahrnuje všechny vlastní typy v knihovně tříd .NET a také vlastní uživatelsky definované typy.</span><span class="sxs-lookup"><span data-stu-id="7fe52-155">This includes all custom types in the .NET class library and also your own user-defined types.</span></span> <span data-ttu-id="7fe52-156">Typy, které definujete pomocí klíčového slova [struct](../../language-reference/keywords/struct.md) , jsou typy hodnot; všechny předdefinované číselné typy jsou `structs`.</span><span class="sxs-lookup"><span data-stu-id="7fe52-156">Types that you define by using the [struct](../../language-reference/keywords/struct.md) keyword are value types; all the built-in numeric types are `structs`.</span></span> <span data-ttu-id="7fe52-157">Typy, které definujete pomocí klíčového slova [Class](../../language-reference/keywords/class.md) , jsou odkazové typy.</span><span class="sxs-lookup"><span data-stu-id="7fe52-157">Types that you define by using the [class](../../language-reference/keywords/class.md) keyword are reference types.</span></span> <span data-ttu-id="7fe52-158">Typy odkazů a typy hodnot mají odlišná pravidla kompilace a jiné chování za běhu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-158">Reference types and value types have different compile-time rules, and different run-time behavior.</span></span>

<span data-ttu-id="7fe52-159">Následující ilustrace znázorňuje vztah mezi typy hodnot a typy odkazů v CTS.</span><span class="sxs-lookup"><span data-stu-id="7fe52-159">The following illustration shows the relationship between value types and reference types in the CTS.</span></span>

<span data-ttu-id="7fe52-160">Následující obrázek znázorňuje typy hodnot a typy odkazů v CTS:</span><span class="sxs-lookup"><span data-stu-id="7fe52-160">The following image shows value types and reference types in the CTS:</span></span>

![Snímek obrazovky zobrazující typy hodnot CTS a odkazové typy](./media/index/value-reference-types-common-type-system.png)

> [!NOTE]
> <span data-ttu-id="7fe52-162">Můžete vidět, že nejčastěji používané typy jsou uspořádány v oboru názvů <xref:System>.</span><span class="sxs-lookup"><span data-stu-id="7fe52-162">You can see that the most commonly used types are all organized in the <xref:System> namespace.</span></span> <span data-ttu-id="7fe52-163">Obor názvů, ve kterém je typ obsažen, ale nemá žádný vztah k tomu, zda se jedná o typ hodnoty nebo typ odkazu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-163">However, the namespace in which a type is contained has no relation to whether it is a value type or reference type.</span></span>

### <a name="value-types"></a><span data-ttu-id="7fe52-164">Typy hodnot</span><span class="sxs-lookup"><span data-stu-id="7fe52-164">Value types</span></span>

<span data-ttu-id="7fe52-165">Typy hodnot jsou odvozeny z <xref:System.ValueType?displayProperty=nameWithType>, které jsou odvozeny z <xref:System.Object?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="7fe52-165">Value types derive from <xref:System.ValueType?displayProperty=nameWithType>, which derives from <xref:System.Object?displayProperty=nameWithType>.</span></span> <span data-ttu-id="7fe52-166">Typy, které jsou odvozeny od <xref:System.ValueType?displayProperty=nameWithType> mají zvláštní chování v modulu CLR.</span><span class="sxs-lookup"><span data-stu-id="7fe52-166">Types that derive from <xref:System.ValueType?displayProperty=nameWithType> have special behavior in the CLR.</span></span> <span data-ttu-id="7fe52-167">Proměnné typu hodnoty přímo obsahují jejich hodnoty, což znamená, že je paměť přidělena vloženému v jakémkoli kontextu, kdy je proměnná deklarována.</span><span class="sxs-lookup"><span data-stu-id="7fe52-167">Value type variables directly contain their values, which means that the memory is allocated inline in whatever context the variable is declared.</span></span> <span data-ttu-id="7fe52-168">Neexistuje žádné samostatné přidělení haldy nebo režie uvolňování paměti pro proměnné typu hodnoty.</span><span class="sxs-lookup"><span data-stu-id="7fe52-168">There is no separate heap allocation or garbage collection overhead for value-type variables.</span></span>

<span data-ttu-id="7fe52-169">Existují dvě kategorie typů hodnot: [struct](../../language-reference/keywords/struct.md) a [Enum](../../language-reference/builtin-types/enum.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-169">There are two categories of value types: [struct](../../language-reference/keywords/struct.md) and [enum](../../language-reference/builtin-types/enum.md).</span></span>

<span data-ttu-id="7fe52-170">Předdefinované číselné typy jsou struktury a mají vlastnosti a metody, ke kterým máte přístup:</span><span class="sxs-lookup"><span data-stu-id="7fe52-170">The built-in numeric types are structs, and they have properties and methods that you can access:</span></span>

```csharp
// Static method on type byte.
byte b = byte.MaxValue;
```

<span data-ttu-id="7fe52-171">Ale deklarujete a přiřadíte jim hodnoty, jako kdyby byly jednoduché neagregované typy:</span><span class="sxs-lookup"><span data-stu-id="7fe52-171">But you declare and assign values to them as if they were simple non-aggregate types:</span></span>

```csharp
byte num = 0xA;
int i = 5;
char c = 'Z';
```

<span data-ttu-id="7fe52-172">Typy hodnot jsou *zapečetěné*, což znamená, že nemůžete odvodit typ z <xref:System.Int32?displayProperty=nameWithType>a nemůžete definovat strukturu, která by dědila z jakékoli uživatelsky definované třídy nebo struktury, protože struktura může dědit pouze z <xref:System.ValueType?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="7fe52-172">Value types are *sealed*, which means, for example, that you cannot derive a type from <xref:System.Int32?displayProperty=nameWithType>, and you cannot define a struct to inherit from any user-defined class or struct because a struct can only inherit from <xref:System.ValueType?displayProperty=nameWithType>.</span></span> <span data-ttu-id="7fe52-173">Struktura však může implementovat jedno nebo více rozhraní.</span><span class="sxs-lookup"><span data-stu-id="7fe52-173">However, a struct can implement one or more interfaces.</span></span> <span data-ttu-id="7fe52-174">Typ struktury můžete přetypovat na libovolný typ rozhraní, který implementuje; To způsobí, že operace *zabalení* zabalí strukturu uvnitř objektu typu reference na spravované haldě.</span><span class="sxs-lookup"><span data-stu-id="7fe52-174">You can cast a struct type to any interface type that it implements; this causes a *boxing* operation to wrap the struct inside a reference type object on the managed heap.</span></span> <span data-ttu-id="7fe52-175">K operacím zabalení dojde, když předáte typ hodnoty metodě, která přijímá <xref:System.Object?displayProperty=nameWithType> nebo libovolný typ rozhraní jako vstupní parametr.</span><span class="sxs-lookup"><span data-stu-id="7fe52-175">Boxing operations occur when you pass a value type to a method that takes a <xref:System.Object?displayProperty=nameWithType> or any interface type as an input parameter.</span></span> <span data-ttu-id="7fe52-176">Další informace naleznete v tématu [zabalení a rozbalení](./boxing-and-unboxing.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-176">For more information, see [Boxing and Unboxing](./boxing-and-unboxing.md).</span></span>

<span data-ttu-id="7fe52-177">Klíčové slovo [struct](../../language-reference/keywords/struct.md) můžete použít k vytvoření vlastních typů hodnot.</span><span class="sxs-lookup"><span data-stu-id="7fe52-177">You use the [struct](../../language-reference/keywords/struct.md) keyword to create your own custom value types.</span></span> <span data-ttu-id="7fe52-178">Struktura se obvykle používá jako kontejner pro malou sadu souvisejících proměnných, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="7fe52-178">Typically, a struct is used as a container for a small set of related variables, as shown in the following example:</span></span>

[!code-csharp[csProgGuideObjects#1](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideObjects/CS/Objects.cs#1)]

<span data-ttu-id="7fe52-179">Další informace o strukturách naleznete v tématu [structs](../classes-and-structs/structs.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-179">For more information about structs, see [Structs](../classes-and-structs/structs.md).</span></span> <span data-ttu-id="7fe52-180">Další informace o typech hodnot naleznete v tématu [typy hodnot](../../language-reference/builtin-types/value-types.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-180">For more information about value types, see [Value types](../../language-reference/builtin-types/value-types.md).</span></span>

<span data-ttu-id="7fe52-181">Druhá kategorie typů hodnot je [Enum](../../language-reference/builtin-types/enum.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-181">The other category of value types is [enum](../../language-reference/builtin-types/enum.md).</span></span> <span data-ttu-id="7fe52-182">Výčet definuje sadu pojmenovaných celočíselných konstant.</span><span class="sxs-lookup"><span data-stu-id="7fe52-182">An enum defines a set of named integral constants.</span></span> <span data-ttu-id="7fe52-183">Například výčet <xref:System.IO.FileMode?displayProperty=nameWithType> v knihovně tříd .NET obsahuje sadu pojmenovaných celých čísel, která určují, jak by měl být soubor otevřen.</span><span class="sxs-lookup"><span data-stu-id="7fe52-183">For example, the <xref:System.IO.FileMode?displayProperty=nameWithType> enumeration in the .NET class library contains a set of named constant integers that specify how a file should be opened.</span></span> <span data-ttu-id="7fe52-184">Je definován tak, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="7fe52-184">It is defined as shown in the following example:</span></span>

[!code-csharp[csProgGuideTypes#44](~/samples/snippets/csharp/VS_Snippets_VBCSharp/CsProgGuideTypes/CS/Class1.cs#44)]

<span data-ttu-id="7fe52-185">`System.IO.FileMode.Create` konstanta má hodnotu 2.</span><span class="sxs-lookup"><span data-stu-id="7fe52-185">The `System.IO.FileMode.Create` constant has a value of 2.</span></span> <span data-ttu-id="7fe52-186">Název je však mnohem smysluplnější pro lidi, kteří čtou zdrojový kód, a z tohoto důvodu je lepší používat výčty namísto konstantních literálových čísel.</span><span class="sxs-lookup"><span data-stu-id="7fe52-186">However, the name is much more meaningful for humans reading the source code, and for that reason it is better to use enumerations instead of constant literal numbers.</span></span> <span data-ttu-id="7fe52-187">Další informace naleznete v tématu <xref:System.IO.FileMode?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="7fe52-187">For more information, see <xref:System.IO.FileMode?displayProperty=nameWithType>.</span></span>

<span data-ttu-id="7fe52-188">Všechny výčty dědí z <xref:System.Enum?displayProperty=nameWithType>, které dědí z <xref:System.ValueType?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="7fe52-188">All enums inherit from <xref:System.Enum?displayProperty=nameWithType>, which inherits from <xref:System.ValueType?displayProperty=nameWithType>.</span></span> <span data-ttu-id="7fe52-189">Všechna pravidla, která platí pro struktury, platí také pro výčty.</span><span class="sxs-lookup"><span data-stu-id="7fe52-189">All the rules that apply to structs also apply to enums.</span></span> <span data-ttu-id="7fe52-190">Další informace o výčtech naleznete v tématu [výčtové typy](../../language-reference/builtin-types/enum.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-190">For more information about enums, see [Enumeration types](../../language-reference/builtin-types/enum.md).</span></span>

### <a name="reference-types"></a><span data-ttu-id="7fe52-191">Odkazové typy</span><span class="sxs-lookup"><span data-stu-id="7fe52-191">Reference types</span></span>

<span data-ttu-id="7fe52-192">Typ, který je definován jako [Třída](../../language-reference/keywords/class.md), [delegát](../../language-reference/builtin-types/reference-types.md), pole nebo [rozhraní](../../language-reference/keywords/interface.md) , je *odkazový typ*.</span><span class="sxs-lookup"><span data-stu-id="7fe52-192">A type that is defined as a [class](../../language-reference/keywords/class.md), [delegate](../../language-reference/builtin-types/reference-types.md), array, or [interface](../../language-reference/keywords/interface.md) is a *reference type*.</span></span> <span data-ttu-id="7fe52-193">V době běhu, pokud deklarujete proměnnou typu odkazu, proměnná obsahuje hodnotu [null](../../language-reference/keywords/null.md) , dokud explicitně nevytvoříte objekt pomocí operátoru [New](../../language-reference/operators/new-operator.md) , nebo přiřaďte objekt, který byl vytvořen jinde pomocí `new`, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="7fe52-193">At run time, when you declare a variable of a reference type, the variable contains the value [null](../../language-reference/keywords/null.md) until you explicitly create an object by using the [new](../../language-reference/operators/new-operator.md) operator, or assign it an object that has been created elsewhere by using `new`, as shown in the following example:</span></span>

```csharp
MyClass mc = new MyClass();
MyClass mc2 = mc;
```

<span data-ttu-id="7fe52-194">Rozhraní musí být inicializováno společně s objektem třídy, který jej implementuje.</span><span class="sxs-lookup"><span data-stu-id="7fe52-194">An interface must be initialized together with a class object that implements it.</span></span> <span data-ttu-id="7fe52-195">Pokud `MyClass` implementuje `IMyInterface`, vytvoříte instanci `IMyInterface`, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="7fe52-195">If `MyClass` implements `IMyInterface`, you create an instance of `IMyInterface` as shown in the following example:</span></span>

```csharp
IMyInterface iface = new MyClass();
```

<span data-ttu-id="7fe52-196">Při vytvoření objektu je paměť přidělena spravované haldě a proměnná obsahuje pouze odkaz na umístění objektu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-196">When the object is created, the memory is allocated on the managed heap, and the variable holds only a reference to the location of the object.</span></span> <span data-ttu-id="7fe52-197">Typy na spravované haldě vyžadují režii při jejich přidělování a v případě, že jsou uvolněny automatickou funkcí správy paměti modulu CLR, která se označuje jako *uvolňování*paměti.</span><span class="sxs-lookup"><span data-stu-id="7fe52-197">Types on the managed heap require overhead both when they are allocated and when they are reclaimed by the automatic memory management functionality of the CLR, which is known as *garbage collection*.</span></span> <span data-ttu-id="7fe52-198">Uvolňování paměti je ale také vysoce optimalizované a ve většině scénářů nevytváří problém s výkonem.</span><span class="sxs-lookup"><span data-stu-id="7fe52-198">However, garbage collection is also highly optimized, and in most scenarios it does not create a performance issue.</span></span> <span data-ttu-id="7fe52-199">Další informace o uvolňování paměti najdete v tématu [Automatická správa paměti](../../../standard/automatic-memory-management.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-199">For more information about garbage collection, see [Automatic Memory Management](../../../standard/automatic-memory-management.md).</span></span>

<span data-ttu-id="7fe52-200">Všechna pole jsou odkazové typy, a to i v případě, že jejich prvky jsou typy hodnot.</span><span class="sxs-lookup"><span data-stu-id="7fe52-200">All arrays are reference types, even if their elements are value types.</span></span> <span data-ttu-id="7fe52-201">Pole jsou implicitně odvozena od třídy <xref:System.Array?displayProperty=nameWithType>, ale deklarujete je a použijete s zjednodušenou syntaxí, C#která je poskytována, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="7fe52-201">Arrays implicitly derive from the <xref:System.Array?displayProperty=nameWithType> class, but you declare and use them with the simplified syntax that is provided by C#, as shown in the following example:</span></span>

[!code-csharp[csProgGuideTypes#45](~/samples/snippets/csharp/VS_Snippets_VBCSharp/CsProgGuideTypes/CS/Class1.cs#45)]

<span data-ttu-id="7fe52-202">Typy odkazů plně podporují dědičnost.</span><span class="sxs-lookup"><span data-stu-id="7fe52-202">Reference types fully support inheritance.</span></span> <span data-ttu-id="7fe52-203">Při vytváření třídy můžete dědit z jakéhokoli jiného rozhraní nebo třídy, která není definována jako [sealed](../../language-reference/keywords/sealed.md), a jiné třídy mohou dědit z vaší třídy a přepsat vaše virtuální metody.</span><span class="sxs-lookup"><span data-stu-id="7fe52-203">When you create a class, you can inherit from any other interface or class that is not defined as [sealed](../../language-reference/keywords/sealed.md), and other classes can inherit from your class and override your virtual methods.</span></span> <span data-ttu-id="7fe52-204">Další informace o tom, jak vytvořit vlastní třídy, naleznete v tématu [třídy a struktury](../classes-and-structs/index.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-204">For more information about how to create your own classes, see [Classes and Structs](../classes-and-structs/index.md).</span></span> <span data-ttu-id="7fe52-205">Další informace o dědičnosti a virtuálních metodách naleznete v tématu [Dědičnost](../classes-and-structs/inheritance.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-205">For more information about inheritance and virtual methods, see [Inheritance](../classes-and-structs/inheritance.md).</span></span>

## <a name="types-of-literal-values"></a><span data-ttu-id="7fe52-206">Typy hodnot literálů</span><span class="sxs-lookup"><span data-stu-id="7fe52-206">Types of literal values</span></span>

<span data-ttu-id="7fe52-207">V C#rozhraní hodnoty literálu obdrží typ z kompilátoru.</span><span class="sxs-lookup"><span data-stu-id="7fe52-207">In C#, literal values receive a type from the compiler.</span></span> <span data-ttu-id="7fe52-208">Můžete určit, jak se má číselný literál zadat připojením písmene ke konci čísla.</span><span class="sxs-lookup"><span data-stu-id="7fe52-208">You can specify how a numeric literal should be typed by appending a letter to the end of the number.</span></span> <span data-ttu-id="7fe52-209">Například chcete-li určit, že hodnota 4,56 by měla být považována za float, přidejte "f" nebo "F" za číslo: `4.56f`.</span><span class="sxs-lookup"><span data-stu-id="7fe52-209">For example, to specify that the value 4.56 should be treated as a float, append an "f" or "F" after the number: `4.56f`.</span></span> <span data-ttu-id="7fe52-210">Pokud není připojeno žádné písmeno, kompilátor odvodí typ literálu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-210">If no letter is appended, the compiler will infer a type for the literal.</span></span> <span data-ttu-id="7fe52-211">Další informace o tom, které typy lze zadat pomocí přípon písmen, naleznete v části [integrální číselné typy](../../language-reference/builtin-types/integral-numeric-types.md) a [číselné typy s plovoucí desetinnou](../../language-reference/builtin-types/floating-point-numeric-types.md)čárkou.</span><span class="sxs-lookup"><span data-stu-id="7fe52-211">For more information about which types can be specified with letter suffixes, see [Integral numeric types](../../language-reference/builtin-types/integral-numeric-types.md) and [Floating-point numeric types](../../language-reference/builtin-types/floating-point-numeric-types.md).</span></span>

<span data-ttu-id="7fe52-212">Vzhledem k tomu, že jsou zadány literály a všechny typy jsou odvozeny od <xref:System.Object?displayProperty=nameWithType>, můžete napsat a zkompilovat kód, například následující:</span><span class="sxs-lookup"><span data-stu-id="7fe52-212">Because literals are typed, and all types derive ultimately from <xref:System.Object?displayProperty=nameWithType>, you can write and compile code such as the following:</span></span>

[!code-csharp[csProgGuideTypes#37](~/samples/snippets/csharp/VS_Snippets_VBCSharp/CsProgGuideTypes/CS/Class1.cs#37)]

## <a name="generic-types"></a><span data-ttu-id="7fe52-213">Obecné typy</span><span class="sxs-lookup"><span data-stu-id="7fe52-213">Generic types</span></span>

<span data-ttu-id="7fe52-214">Typ lze deklarovat s jedním nebo více *parametry typu* , které slouží jako zástupný text pro skutečný typ ( *konkrétní typ*), který klientský kód poskytne při vytváření instance typu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-214">A type can be declared with one or more *type parameters* that serve as a placeholder for the actual type (the *concrete type*) that client code will provide when it creates an instance of the type.</span></span> <span data-ttu-id="7fe52-215">Tyto typy se nazývají *Obecné typy*.</span><span class="sxs-lookup"><span data-stu-id="7fe52-215">Such types are called *generic types*.</span></span> <span data-ttu-id="7fe52-216">Například typ .NET <xref:System.Collections.Generic.List%601?displayProperty=nameWithType> má jeden parametr typu, který je podle konvence dán názvem *t*. Při vytváření instance typu zadáte typ objektů, které bude seznam obsahovat, například řetězec:</span><span class="sxs-lookup"><span data-stu-id="7fe52-216">For example, the .NET type <xref:System.Collections.Generic.List%601?displayProperty=nameWithType> has one type parameter that by convention is given the name *T*. When you create an instance of the type, you specify the type of the objects that the list will contain, for example, string:</span></span>

```csharp
List<string> stringList = new List<string>();
stringList.Add("String example");
// compile time error adding a type other than a string:
stringList.Add(4);
```

<span data-ttu-id="7fe52-217">Použití parametru typu umožňuje znovu použít stejnou třídu pro uchování libovolného typu elementu, aniž by bylo nutné převést každý prvek na [Object](../../language-reference/builtin-types/reference-types.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-217">The use of the type parameter makes it possible to reuse the same class to hold any type of element, without having to convert each element to [object](../../language-reference/builtin-types/reference-types.md).</span></span> <span data-ttu-id="7fe52-218">Třídy obecné kolekce se nazývají *kolekce se silnými* typy, protože kompilátor zná konkrétní typ prvků kolekce a může vyvolat chybu v době kompilace, pokud se například pokusíte přidat celé číslo do objektu `stringList` v předchozím příkladu.</span><span class="sxs-lookup"><span data-stu-id="7fe52-218">Generic collection classes are called *strongly-typed collections* because the compiler knows the specific type of the collection's elements and can raise an error at compile-time if, for example, you try to add an integer to the `stringList` object in the previous example.</span></span> <span data-ttu-id="7fe52-219">Další informace najdete v tématu [Obecné typy](../generics/index.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-219">For more information, see [Generics](../generics/index.md).</span></span>

## <a name="implicit-types-anonymous-types-and-nullable-value-types"></a><span data-ttu-id="7fe52-220">Implicitní typy, anonymní typy a typy hodnot s možnou hodnotou null</span><span class="sxs-lookup"><span data-stu-id="7fe52-220">Implicit types, anonymous types, and nullable value types</span></span>

<span data-ttu-id="7fe52-221">Jak bylo uvedeno dříve, můžete implicitně napsat místní proměnnou (ale ne členy třídy) pomocí klíčového slova [var](../../language-reference/keywords/var.md) .</span><span class="sxs-lookup"><span data-stu-id="7fe52-221">As stated previously, you can implicitly type a local variable (but not class members) by using the [var](../../language-reference/keywords/var.md) keyword.</span></span> <span data-ttu-id="7fe52-222">Proměnná stále přijímá typ v době kompilace, ale typ je poskytován kompilátorem.</span><span class="sxs-lookup"><span data-stu-id="7fe52-222">The variable still receives a type at compile time, but the type is provided by the compiler.</span></span> <span data-ttu-id="7fe52-223">Další informace naleznete v tématu [implicitně typované lokální proměnné](../classes-and-structs/implicitly-typed-local-variables.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-223">For more information, see [Implicitly Typed Local Variables](../classes-and-structs/implicitly-typed-local-variables.md).</span></span>

<span data-ttu-id="7fe52-224">V některých případech je nevhodné vytvořit pojmenovaný typ pro jednoduché sady souvisejících hodnot, které nechcete ukládat nebo předávat mimo hranice metody.</span><span class="sxs-lookup"><span data-stu-id="7fe52-224">In some cases, it is inconvenient to create a named type for simple sets of related values that you do not intend to store or pass outside method boundaries.</span></span> <span data-ttu-id="7fe52-225">Pro tento účel můžete vytvořit *anonymní typy* .</span><span class="sxs-lookup"><span data-stu-id="7fe52-225">You can create *anonymous types* for this purpose.</span></span> <span data-ttu-id="7fe52-226">Další informace najdete v tématu [anonymní typy](../classes-and-structs/anonymous-types.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-226">For more information, see [Anonymous Types](../classes-and-structs/anonymous-types.md).</span></span>

<span data-ttu-id="7fe52-227">Typy běžných hodnot nemohou mít hodnotu [null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-227">Ordinary value types cannot have a value of [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="7fe52-228">Můžete však vytvořit typy hodnot s možnou hodnotou null, a to tak, že po typu napřipojíte `?`.</span><span class="sxs-lookup"><span data-stu-id="7fe52-228">However, you can create nullable value types by affixing a `?` after the type.</span></span> <span data-ttu-id="7fe52-229">Například `int?` je `int` typ, který může mít také hodnotu [null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-229">For example, `int?` is an `int` type that can also have the value [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="7fe52-230">Typy s možnou hodnotou null jsou instancemi obecného typu struktury <xref:System.Nullable%601?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="7fe52-230">Nullable value types are instances of the generic struct type <xref:System.Nullable%601?displayProperty=nameWithType>.</span></span> <span data-ttu-id="7fe52-231">Typy hodnot s možnou hodnotou null jsou obzvláště užitečné, Pokud předáváte data do a z databází, ve kterých mohou být číselné hodnoty null.</span><span class="sxs-lookup"><span data-stu-id="7fe52-231">Nullable value types are especially useful when you are passing data to and from databases in which numeric values might be null.</span></span> <span data-ttu-id="7fe52-232">Další informace naleznete v tématu [typy hodnot s možnou hodnotou null](../../language-reference/builtin-types/nullable-value-types.md).</span><span class="sxs-lookup"><span data-stu-id="7fe52-232">For more information, see [Nullable value types](../../language-reference/builtin-types/nullable-value-types.md).</span></span>

## <a name="related-sections"></a><span data-ttu-id="7fe52-233">Související oddíly</span><span class="sxs-lookup"><span data-stu-id="7fe52-233">Related sections</span></span>

<span data-ttu-id="7fe52-234">Další informace najdete v následujících tématech:</span><span class="sxs-lookup"><span data-stu-id="7fe52-234">For more information, see the following topics:</span></span>

- [<span data-ttu-id="7fe52-235">Přetypování a převody typů</span><span class="sxs-lookup"><span data-stu-id="7fe52-235">Casting and Type Conversions</span></span>](./casting-and-type-conversions.md)

- [<span data-ttu-id="7fe52-236">Zabalení a rozbalení</span><span class="sxs-lookup"><span data-stu-id="7fe52-236">Boxing and Unboxing</span></span>](./boxing-and-unboxing.md)

- [<span data-ttu-id="7fe52-237">Použití typu dynamic</span><span class="sxs-lookup"><span data-stu-id="7fe52-237">Using Type dynamic</span></span>](./using-type-dynamic.md)

- [<span data-ttu-id="7fe52-238">Typy hodnot</span><span class="sxs-lookup"><span data-stu-id="7fe52-238">Value Types</span></span>](../../language-reference/builtin-types/value-types.md)

- [<span data-ttu-id="7fe52-239">Odkazové typy</span><span class="sxs-lookup"><span data-stu-id="7fe52-239">Reference Types</span></span>](../../language-reference/keywords/reference-types.md)

- [<span data-ttu-id="7fe52-240">Třídy a struktury</span><span class="sxs-lookup"><span data-stu-id="7fe52-240">Classes and Structs</span></span>](../classes-and-structs/index.md)

- [<span data-ttu-id="7fe52-241">Anonymní typy</span><span class="sxs-lookup"><span data-stu-id="7fe52-241">Anonymous Types</span></span>](../classes-and-structs/anonymous-types.md)

- [<span data-ttu-id="7fe52-242">Obecné typy</span><span class="sxs-lookup"><span data-stu-id="7fe52-242">Generics</span></span>](../generics/index.md)

## <a name="c-language-specification"></a><span data-ttu-id="7fe52-243">specifikace jazyka C#</span><span class="sxs-lookup"><span data-stu-id="7fe52-243">C# language specification</span></span>

[!INCLUDE[CSharplangspec](~/includes/csharplangspec-md.md)]

## <a name="see-also"></a><span data-ttu-id="7fe52-244">Viz také</span><span class="sxs-lookup"><span data-stu-id="7fe52-244">See also</span></span>

- [<span data-ttu-id="7fe52-245">C#Odkaz</span><span class="sxs-lookup"><span data-stu-id="7fe52-245">C# Reference</span></span>](../../language-reference/index.md)
- [<span data-ttu-id="7fe52-246">Průvodce programováním v C#</span><span class="sxs-lookup"><span data-stu-id="7fe52-246">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="7fe52-247">Převod datových typů XML</span><span class="sxs-lookup"><span data-stu-id="7fe52-247">Conversion of XML Data Types</span></span>](../../../standard/data/xml/conversion-of-xml-data-types.md)
- [<span data-ttu-id="7fe52-248">Celočíselné typy</span><span class="sxs-lookup"><span data-stu-id="7fe52-248">Integral types</span></span>](../../language-reference/builtin-types/integral-numeric-types.md)
