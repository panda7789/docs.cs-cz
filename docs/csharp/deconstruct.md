---
title: Dekonstrukce řazených kolekcí členů a dalších typů
description: Naučte se dekonstruovat řazené kolekce členů a další typy.
ms.technology: csharp-fundamentals
ms.date: 07/18/2016
ms.assetid: 0b0c4b0f-4a47-4f66-9b8e-f5c63b195960
ms.openlocfilehash: 23d193faf9702628698fe558f6667aeb130e8916
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/14/2020
ms.locfileid: "73100665"
---
# <a name="deconstructing-tuples-and-other-types"></a><span data-ttu-id="4a9d5-103">Dekonstrukce řazených kolekcí členů a dalších typů</span><span class="sxs-lookup"><span data-stu-id="4a9d5-103">Deconstructing tuples and other types</span></span>

<span data-ttu-id="4a9d5-104">Řazená kolekce členů poskytuje lehký způsob, jak načíst více hodnot z volání metody.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-104">A tuple provides a lightweight way to retrieve multiple values from a method call.</span></span> <span data-ttu-id="4a9d5-105">Ale jakmile načtete řazenou kolekce členů, musíte zpracovat její jednotlivé prvky.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-105">But once you retrieve the tuple, you have to handle its individual elements.</span></span> <span data-ttu-id="4a9d5-106">To na základě element-by-element je těžkopádné, jak ukazuje následující příklad.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-106">Doing this on an element-by-element basis is cumbersome, as the following example shows.</span></span> <span data-ttu-id="4a9d5-107">Metoda `QueryCityData` vrátí 3 řazené kolekce členů a každý z jeho prvků je přiřazen proměnné v samostatné operaci.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-107">The `QueryCityData` method returns a 3-tuple, and each of its elements is assigned to a variable in a separate operation.</span></span>

[!code-csharp[WithoutDeconstruction](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/deconstruct-tuple1.cs)]

<span data-ttu-id="4a9d5-108">Načítání více hodnot polí a vlastností z objektu může být stejně těžkopádné: je třeba přiřadit hodnotu pole nebo vlastnosti proměnné na základě člena podle člena.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-108">Retrieving multiple field and property values from an object can be equally cumbersome: you have to assign a field or property value to a variable on a member-by-member basis.</span></span>

<span data-ttu-id="4a9d5-109">Počínaje C# 7.0, můžete načíst více prvků z řazené kolekce členů nebo načíst více polí, vlastností a vypočítaných hodnot z objektu v jedné operaci *dekonstrukce.*</span><span class="sxs-lookup"><span data-stu-id="4a9d5-109">Starting with C# 7.0, you can retrieve multiple elements from a tuple or retrieve multiple field, property, and computed values from an object in a single *deconstruct* operation.</span></span> <span data-ttu-id="4a9d5-110">Při dekonstrukci řazené kolekce členů přiřadíte její prvky k jednotlivým proměnným.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-110">When you deconstruct a tuple, you assign its elements to individual variables.</span></span> <span data-ttu-id="4a9d5-111">Když dekonstruujete objekt, přiřadíte vybrané hodnoty jednotlivým proměnným.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-111">When you deconstruct an object, you assign selected values to individual variables.</span></span>

## <a name="deconstructing-a-tuple"></a><span data-ttu-id="4a9d5-112">Dekonstrukce řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="4a9d5-112">Deconstructing a tuple</span></span>

<span data-ttu-id="4a9d5-113">C# obsahuje integrovanou podporu pro deconstructing řazené kolekce členů, který umožňuje zrušit balíček všech položek v řazené kolekce členů v jedné operaci.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-113">C# features built-in support for deconstructing tuples, which lets you unpackage all the items in a tuple in a single operation.</span></span> <span data-ttu-id="4a9d5-114">Obecná syntaxe pro deconstructing řazené kolekce členů je podobná syntaxi pro definování jedné: uzavřete proměnné, ke kterým má být každý prvek přiřazen v závorcích na levé straně příkazu přiřazení.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-114">The general syntax for deconstructing a tuple is similar to the syntax for defining one: you enclose the variables to which each element is to be assigned in parentheses in the left side of an assignment statement.</span></span> <span data-ttu-id="4a9d5-115">Například následující příkaz přiřadí prvky 4-n-tice čtyřma samostatným proměnným:</span><span class="sxs-lookup"><span data-stu-id="4a9d5-115">For example, the following statement assigns the elements of a 4-tuple to four separate variables:</span></span>

```csharp
var (name, address, city, zip) = contact.GetAddressInfo();
```

<span data-ttu-id="4a9d5-116">Existují tři způsoby, jak dekonstruovat řazenou n-tice:</span><span class="sxs-lookup"><span data-stu-id="4a9d5-116">There are three ways to deconstruct a tuple:</span></span>

- <span data-ttu-id="4a9d5-117">Můžete explicitně deklarovat typ každého pole uvnitř závorek.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-117">You can explicitly declare the type of each field inside parentheses.</span></span> <span data-ttu-id="4a9d5-118">Následující příklad používá tento přístup k deconstruct 3 `QueryCityData` řazené kolekce členů vrácené metodou.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-118">The following example uses this approach to deconstruct the 3-tuple returned by the `QueryCityData` method.</span></span>

    [!code-csharp[Deconstruction-Explicit](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/deconstruct-tuple2.cs#1)]

- <span data-ttu-id="4a9d5-119">Můžete použít `var` klíčové slovo tak, aby C# odvodí typ každé proměnné.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-119">You can use the `var` keyword so that C# infers the type of each variable.</span></span> <span data-ttu-id="4a9d5-120">`var` Klíčové slovo umístíte mimo závorky.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-120">You place the `var` keyword outside of the parentheses.</span></span> <span data-ttu-id="4a9d5-121">Následující příklad používá odvození typu při dekonstrukci 3-n-tice vrácené `QueryCityData` metodou.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-121">The following example uses type inference when deconstructing the 3-tuple returned by the `QueryCityData` method.</span></span>

    [!code-csharp[Deconstruction-Infer](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/deconstruct-tuple3.cs#1)]

    <span data-ttu-id="4a9d5-122">`var` Klíčové slovo můžete také použít jednotlivě s některou nebo všechny deklarace proměnných uvnitř závorek.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-122">You can also use the `var` keyword individually with any or all of the variable declarations inside the parentheses.</span></span>

    [!code-csharp[Deconstruction-Infer-Some](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/deconstruct-tuple4.cs#1)]

    <span data-ttu-id="4a9d5-123">To je těžkopádné a nedoporučuje se.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-123">This is cumbersome and is not recommended.</span></span>

- <span data-ttu-id="4a9d5-124">Nakonec můžete deconstruct řazené kolekce členů do proměnné, které již byly deklarovány.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-124">Lastly, you may deconstruct the tuple into variables that have already been declared.</span></span>

    [!code-csharp[Deconstruction-Declared](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/deconstruct-tuple5.cs#1)]

<span data-ttu-id="4a9d5-125">Všimněte si, že nelze zadat určitý typ mimo závorky i v případě, že každé pole v n-tice má stejný typ.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-125">Note that you cannot specify a specific type outside the parentheses even if every field in the tuple has the same type.</span></span> <span data-ttu-id="4a9d5-126">Tím se vygeneruje chyba kompilátoru CS8136, "Dekonstrukce 'var (...)' formulář zakazuje určitý typ pro 'var'.".</span><span class="sxs-lookup"><span data-stu-id="4a9d5-126">This generates compiler error CS8136, "Deconstruction 'var (...)' form disallows a specific type for 'var'.".</span></span>

<span data-ttu-id="4a9d5-127">Všimněte si, že je také nutné přiřadit každý prvek řazené kolekce členů proměnné.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-127">Note that you must also assign each element of the tuple to a variable.</span></span> <span data-ttu-id="4a9d5-128">Pokud vyneche všechny prvky, kompilátor generuje chybu CS8132, "Nelze dekonstruovat n-tice 'x' prvky do 'y' proměnné."</span><span class="sxs-lookup"><span data-stu-id="4a9d5-128">If you omit any elements, the compiler generates error CS8132, "Cannot deconstruct a tuple of 'x' elements into 'y' variables."</span></span>

<span data-ttu-id="4a9d5-129">Všimněte si, že nelze kombinovat deklarace a přiřazení k existující proměnné na levé straně deconstruction.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-129">Note that you cannot mix declarations and assignments to existing variables on the left-hand side of a deconstruction.</span></span> <span data-ttu-id="4a9d5-130">Kompilátor generuje chybu CS8184, "dekonstrukce nemůže kombinovat deklarace a výrazy na levé straně."</span><span class="sxs-lookup"><span data-stu-id="4a9d5-130">The compiler generates error CS8184, "a deconstruction cannot mix declarations and expressions on the left-hand-side."</span></span> <span data-ttu-id="4a9d5-131">pokud členové zahrnují nově deklarované a existující proměnné.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-131">when the members include newly declared and existing variables.</span></span>

## <a name="deconstructing-tuple-elements-with-discards"></a><span data-ttu-id="4a9d5-132">Dekonstrukce prvků n-tice s výměty</span><span class="sxs-lookup"><span data-stu-id="4a9d5-132">Deconstructing tuple elements with discards</span></span>

<span data-ttu-id="4a9d5-133">Často při dekonstrukci řazené kolekce členů, máte zájem o hodnoty pouze některé prvky.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-133">Often when deconstructing a tuple, you're interested in the values of only some elements.</span></span> <span data-ttu-id="4a9d5-134">Počínaje C# 7.0, můžete využít c#'s podporu pro *zahození*, které jsou pouze pro zápis proměnné, jejichž hodnoty, které jste se rozhodli ignorovat.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-134">Starting with C# 7.0, you can take advantage of C#'s support for *discards*, which are write-only variables whose values you've chosen to ignore.</span></span> <span data-ttu-id="4a9d5-135">Zahození je označeno znakem podtržítka ("\_") v přiřazení.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-135">A discard is designated by an underscore character ("\_") in an assignment.</span></span> <span data-ttu-id="4a9d5-136">Můžete zahodit tolik hodnot, kolik chcete; všechny jsou reprezentovány jediným `_`výmětem.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-136">You can discard as many values as you like; all are represented by the single discard, `_`.</span></span>

<span data-ttu-id="4a9d5-137">Následující příklad ilustruje použití řazené kolekce členů s výtažky.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-137">The following example illustrates the use of tuples with discards.</span></span> <span data-ttu-id="4a9d5-138">Metoda `QueryCityDataForYears` vrátí 6-n-tice s názvem města, jeho oblast, rok, město populace pro tento rok, druhý rok, a město populace pro tento druhý rok.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-138">The `QueryCityDataForYears` method returns a 6-tuple with the name of a city, its area, a year, the city's population for that year, a second year, and the city's population for that second year.</span></span> <span data-ttu-id="4a9d5-139">Příklad ukazuje změnu počtu obyvatel mezi těmito dvěma roky.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-139">The example shows the change in population between those two years.</span></span> <span data-ttu-id="4a9d5-140">Z údajů dostupných z řazené kolekce členů se nestaráme o městskou oblast a známe název města a dvě data v době návrhu.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-140">Of the data available from the tuple, we're unconcerned with the city area, and we know the city name and the two dates at design-time.</span></span> <span data-ttu-id="4a9d5-141">V důsledku toho nás zajímají pouze dvě hodnoty základního souboru uložené v řazené kolekce členů a mohou zpracovat jeho zbývající hodnoty jako zahození.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-141">As a result, we're only interested in the two population values stored in the tuple, and can handle its remaining values as discards.</span></span>  

[!code-csharp[Tuple-discard](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

## <a name="deconstructing-user-defined-types"></a><span data-ttu-id="4a9d5-142">Dekonstrukce uživatelem definovaných typů</span><span class="sxs-lookup"><span data-stu-id="4a9d5-142">Deconstructing user-defined types</span></span>

<span data-ttu-id="4a9d5-143">C# nenabízí integrovanou podporu pro deconstructing typy neřazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-143">C# does not offer built-in support for deconstructing non-tuple types.</span></span> <span data-ttu-id="4a9d5-144">Však jako autor třídy, struktury nebo rozhraní, můžete povolit instance typu deconstructed implementací jedné `Deconstruct` nebo více metod.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-144">However, as the author of a class, a struct, or an interface, you can allow instances of the type to be deconstructed by implementing one or more `Deconstruct` methods.</span></span> <span data-ttu-id="4a9d5-145">Metoda vrátí void a každá hodnota, která má být deconstructed je [označena out](language-reference/keywords/out-parameter-modifier.md) parametr v podpisu metody.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-145">The method returns void, and each value to be deconstructed is indicated by an [out](language-reference/keywords/out-parameter-modifier.md) parameter in the method signature.</span></span> <span data-ttu-id="4a9d5-146">Například následující `Deconstruct` metoda `Person` třídy vrátí první, střední a příjmení:</span><span class="sxs-lookup"><span data-stu-id="4a9d5-146">For example, the following `Deconstruct` method of a `Person` class returns the first, middle, and last name:</span></span>

[!code-csharp[Class-deconstruct](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/deconstruct-class1.cs#1)]

<span data-ttu-id="4a9d5-147">Potom můžete dekonstruovat `Person` instanci `p` třídy pojmenované s přiřazením takto:</span><span class="sxs-lookup"><span data-stu-id="4a9d5-147">You can then deconstruct an instance of the `Person` class named `p` with an assignment like the following:</span></span>

[!code-csharp[Class-deconstruct](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/deconstruct-class1.cs#2)]

<span data-ttu-id="4a9d5-148">Následující příklad přetíží metodu `Deconstruct` vrátit různé kombinace `Person` vlastností objektu.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-148">The following example overloads the `Deconstruct` method to return various combinations of properties of a `Person` object.</span></span> <span data-ttu-id="4a9d5-149">Individuální přetížení vrátí:</span><span class="sxs-lookup"><span data-stu-id="4a9d5-149">Individual overloads return:</span></span>

- <span data-ttu-id="4a9d5-150">Křestní jméno a příjmení.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-150">A first and last name.</span></span>
- <span data-ttu-id="4a9d5-151">První, poslední a prostřední jméno.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-151">A first, last, and middle name.</span></span>
- <span data-ttu-id="4a9d5-152">Křestní jméno, příjmení, název města a název státu.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-152">A first name, a last name, a city name, and a state name.</span></span>

[!code-csharp[Class-deconstruct](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/deconstruct-class2.cs)]

<span data-ttu-id="4a9d5-153">Vzhledem k `Deconstruct` tomu, že můžete přetížení metody tak, aby odrážely skupiny `Deconstruct` dat, které jsou běžně extrahovány z objektu, měli byste být opatrní definovat metody s podpisy, které jsou rozlišovací a jednoznačné.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-153">Because you can overload the `Deconstruct` method to reflect groups of data that are commonly extracted from an object, you should be careful to define `Deconstruct` methods with signatures that are distinctive and unambiguous.</span></span> <span data-ttu-id="4a9d5-154">Více `Deconstruct` metod, které mají `out` stejný počet parametrů nebo `out` stejný počet a typ parametrů v jiném pořadí může způsobit nejasnosti.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-154">Multiple `Deconstruct` methods that have the same number of `out` parameters or the same number and type of `out` parameters in a different order can cause confusion.</span></span>

<span data-ttu-id="4a9d5-155">Přetížená `Deconstruct` metoda v následujícím příkladu ilustruje jeden možný zdroj nejasností.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-155">The overloaded `Deconstruct` method in the following example illustrates one possible source of confusion.</span></span> <span data-ttu-id="4a9d5-156">První přetížení vrátí křestní jméno, druhé jméno, příjmení `Person` a stáří objektu v tomto pořadí.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-156">The first overload returns the first name, middle name, last name, and age of a `Person` object, in that order.</span></span> <span data-ttu-id="4a9d5-157">Druhé přetížení vrátí informace o názvu pouze spolu s roční příjem, ale první, střední a příjmení jsou v jiném pořadí.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-157">The second overload returns name information only along with annual income, but the first, middle, and last name are in a different order.</span></span> <span data-ttu-id="4a9d5-158">To usnadňuje zmást pořadí argumentů při dekonstrukci `Person` instance.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-158">This makes it easy to confuse the order of arguments when deconstructing a `Person` instance.</span></span>

[!code-csharp[Deconstruct-ambiguity](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/deconstruct-ambiguous.cs)]

## <a name="deconstructing-a-user-defined-type-with-discards"></a><span data-ttu-id="4a9d5-159">Odstranění uživatelem definovaného typu s výměty</span><span class="sxs-lookup"><span data-stu-id="4a9d5-159">Deconstructing a user-defined type with discards</span></span>

<span data-ttu-id="4a9d5-160">Stejně jako u [řazených kolekcí členů](#deconstructing-tuple-elements-with-discards)můžete použít `Deconstruct` zahozené položky k ignorování vybraných položek vrácených metodou.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-160">Just as you do with [tuples](#deconstructing-tuple-elements-with-discards), you can use discards to ignore selected items returned by a `Deconstruct` method.</span></span> <span data-ttu-id="4a9d5-161">Každý zahození je definovánproměnnou s názvem "\_" a jedna operace dekonstrukce může obsahovat více zahození.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-161">Each discard is defined by a variable named "\_", and a single deconstruction operation can include multiple discards.</span></span>

<span data-ttu-id="4a9d5-162">Následující příklad dekonstruuje `Person` objekt do čtyř řetězců (křestní jméno a příjmení, město a stát), ale zahodí příjmení a stav.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-162">The following example deconstructs a `Person` object into four strings (the first and last names, the city, and the state) but discards the last name and the state.</span></span>

[!code-csharp[Class-discard](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/class-discard1.cs#1)]

## <a name="deconstructing-a-user-defined-type-with-an-extension-method"></a><span data-ttu-id="4a9d5-163">Dekonstrukce uživatelem definovaného typu metodou rozšíření</span><span class="sxs-lookup"><span data-stu-id="4a9d5-163">Deconstructing a user-defined type with an extension method</span></span>

<span data-ttu-id="4a9d5-164">Pokud jste nevytvořili třídu, strukturu nebo rozhraní, můžete stále dekonstruovat objekty `Deconstruct` tohoto typu implementací jedné nebo více [metod rozšíření](programming-guide/classes-and-structs/extension-methods.md) a vrátit hodnoty, které vás zajímají.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-164">If you didn't author a class, struct, or interface, you can still deconstruct objects of that type by implementing one or more `Deconstruct` [extension methods](programming-guide/classes-and-structs/extension-methods.md) to return the values in which you're interested.</span></span>

<span data-ttu-id="4a9d5-165">Následující příklad definuje `Deconstruct` dvě metody <xref:System.Reflection.PropertyInfo?displayProperty=nameWithType> rozšíření pro třídu.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-165">The following example defines two `Deconstruct` extension methods for the <xref:System.Reflection.PropertyInfo?displayProperty=nameWithType> class.</span></span> <span data-ttu-id="4a9d5-166">První vrátí sadu hodnot, které označují charakteristiky vlastnosti, včetně jejího typu, zda je statické nebo instance, zda je jen pro čtení a zda je indexován.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-166">The first returns a set of values that indicate the characteristics of the property, including its type, whether it's static or instance, whether it's read-only, and whether it's indexed.</span></span> <span data-ttu-id="4a9d5-167">Druhý označuje dostupnost vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-167">The second indicates the property's accessibility.</span></span> <span data-ttu-id="4a9d5-168">Vzhledem k tomu, že přístupové objekty přístupů get a set se mohou lišit, logické hodnoty označují, zda má vlastnost samostatné přístupové objekty get a set, a pokud ano, zda mají stejnou přístupnost.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-168">Because the accessibility of get and set accessors can differ, Boolean values indicate whether the property has separate get and set accessors and, if it does, whether they have the same accessibility.</span></span> <span data-ttu-id="4a9d5-169">Pokud existuje pouze jeden přistupující objekt nebo oba get a `access` set přístupový objekt mají stejnou přístupnost, proměnná označuje usnadnění vlastnosti jako celku.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-169">If there is only one accessor or both the get and the set accessor have the same accessibility, the `access` variable indicates the accessibility of the property as a whole.</span></span> <span data-ttu-id="4a9d5-170">V opačném případě jsou přístupnosti přístupových částí get `getAccess` `setAccess` a set označeny proměnnými a.</span><span class="sxs-lookup"><span data-stu-id="4a9d5-170">Otherwise, the accessibility of the get and set accessors are indicated by the `getAccess` and `setAccess` variables.</span></span>

[!code-csharp[Extension-deconstruct](../../samples/snippets/csharp/programming-guide/deconstructing-tuples/deconstruct-extension1.cs)]

## <a name="see-also"></a><span data-ttu-id="4a9d5-171">Viz také</span><span class="sxs-lookup"><span data-stu-id="4a9d5-171">See also</span></span>

- [<span data-ttu-id="4a9d5-172">Zahození</span><span class="sxs-lookup"><span data-stu-id="4a9d5-172">Discards</span></span>](discards.md)
- [<span data-ttu-id="4a9d5-173">Záznamů</span><span class="sxs-lookup"><span data-stu-id="4a9d5-173">Tuples</span></span>](tuples.md)
