---
title: Typy řazené kolekce členů – průvodce v C#
description: Další informace o typech pojmenované a nepojmenované řazené kolekce členů v C#
ms.date: 05/15/2018
ms.assetid: ee8bf7c3-aa3e-4c9e-a5c6-e05cc6138baa
ms.openlocfilehash: b0c838791e640c9813005b8a32d009153a794c14
ms.sourcegitcommit: efff8f331fd9467f093f8ab8d23a203d6ecb5b60
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/01/2018
ms.locfileid: "43404311"
---
# <a name="c-tuple-types"></a><span data-ttu-id="6fe94-103">Typy řazené kolekce členů v C#</span><span class="sxs-lookup"><span data-stu-id="6fe94-103">C# tuple types</span></span> #

<span data-ttu-id="6fe94-104">C# řazené kolekce členů jsou typy, které definujete pomocí jednoduché syntaxe.</span><span class="sxs-lookup"><span data-stu-id="6fe94-104">C# tuples are types that you define using a lightweight syntax.</span></span> <span data-ttu-id="6fe94-105">Mezi výhody patří jednodušší syntaxí, pravidla pro převody na základě čísla (označované jako Kardinalita) a typy prvků a konzistentní pravidla pro kopie, testování rovnosti a přiřazení.</span><span class="sxs-lookup"><span data-stu-id="6fe94-105">The advantages include a simpler syntax, rules for conversions based on number (referred to as cardinality) and types of elements, and consistent rules for copies, equality tests, and assignments.</span></span> <span data-ttu-id="6fe94-106">Jako kompromis řazených kolekcí členů nepodporuje některé objektově orientované idiomy přidružené dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="6fe94-106">As a tradeoff, tuples do not support some of the object-oriented idioms associated with inheritance.</span></span> <span data-ttu-id="6fe94-107">V části můžete získat přehled [řazenými kolekcemi členů v co je nového v jazyce C# 7.0](whats-new/csharp-7.md#tuples) článku.</span><span class="sxs-lookup"><span data-stu-id="6fe94-107">You can get an overview in the section on [tuples in the What's new in C# 7.0](whats-new/csharp-7.md#tuples) article.</span></span>

<span data-ttu-id="6fe94-108">V tomto článku se dozvíte jazyka pravidla řazených kolekcí členů v C# 7.0 a novějších verzích, různé způsoby použití je a počáteční pokyny pro práci s řazenými kolekcemi členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-108">In this article, you'll learn the language rules governing tuples in C# 7.0 and later versions, different ways to use them, and initial guidance on working with tuples.</span></span>

> [!NOTE]
> <span data-ttu-id="6fe94-109">Vyžadovat nových funkcí řazených kolekcí členů <xref:System.ValueTuple> typy.</span><span class="sxs-lookup"><span data-stu-id="6fe94-109">The new tuples features require the <xref:System.ValueTuple> types.</span></span>
> <span data-ttu-id="6fe94-110">Je nutné přidat balíček NuGet [ `System.ValueTuple` ](https://www.nuget.org/packages/System.ValueTuple/) aby bylo možné používat na platformách, které neobsahují typy.</span><span class="sxs-lookup"><span data-stu-id="6fe94-110">You must add the NuGet package [`System.ValueTuple`](https://www.nuget.org/packages/System.ValueTuple/) in order to use it on platforms that do not include the types.</span></span>
>
> <span data-ttu-id="6fe94-111">To se podobá další jazykové funkce, které využívají typy v rozhraní framework.</span><span class="sxs-lookup"><span data-stu-id="6fe94-111">This is similar to other language features that rely on types delivered in the framework.</span></span> <span data-ttu-id="6fe94-112">Mezi příklady patří `async` a `await` spoléhat na `INotifyCompletion` rozhraní a LINQ spoléhat na `IEnumerable<T>`.</span><span class="sxs-lookup"><span data-stu-id="6fe94-112">Examples include `async` and `await` relying on the `INotifyCompletion` interface, and LINQ relying on `IEnumerable<T>`.</span></span> <span data-ttu-id="6fe94-113">Jak .NET se mění na další platformy, které jsou nezávislé, mechanismus doručování mění.</span><span class="sxs-lookup"><span data-stu-id="6fe94-113">However, the delivery mechanism is changing as .NET is becoming more platform independent.</span></span> <span data-ttu-id="6fe94-114">Rozhraní .NET Framework nemusí vždy odeslání na stejném tempo jako kompilátor jazyka.</span><span class="sxs-lookup"><span data-stu-id="6fe94-114">The .NET Framework may not always ship on the same cadence as the language compiler.</span></span> <span data-ttu-id="6fe94-115">Nové funkce jazyků využívají nové typy, typy nebudou mít k dispozici jako balíčků NuGet při dodávání funkcí jazyka.</span><span class="sxs-lookup"><span data-stu-id="6fe94-115">When new language features rely on new types, those types will be available as NuGet packages when the language features ship.</span></span> <span data-ttu-id="6fe94-116">Tyto nové typy získat přidána do rozhraní API .NET Standard a dodávána jako součást rozhraní framework, odeberou se požadavek na balíček NuGet.</span><span class="sxs-lookup"><span data-stu-id="6fe94-116">As these new types get added to the .NET Standard API and delivered as part of the framework, the NuGet package requirement will be removed.</span></span>

<span data-ttu-id="6fe94-117">Začněme s důvody pro přidání nové podpory řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-117">Let's start with the reasons for adding new tuple support.</span></span> <span data-ttu-id="6fe94-118">Metody vrátí jeden objekt.</span><span class="sxs-lookup"><span data-stu-id="6fe94-118">Methods return a single object.</span></span> <span data-ttu-id="6fe94-119">Řazené kolekce členů umožňují snadněji balíček více hodnot v tomto jednoho objektu.</span><span class="sxs-lookup"><span data-stu-id="6fe94-119">Tuples enable you to package multiple values in that single object more easily.</span></span>

<span data-ttu-id="6fe94-120">Rozhraní .NET Framework již má obecný `Tuple` třídy.</span><span class="sxs-lookup"><span data-stu-id="6fe94-120">The .NET Framework already has generic `Tuple` classes.</span></span> <span data-ttu-id="6fe94-121">Tyto třídy, ale má dvě hlavní omezení.</span><span class="sxs-lookup"><span data-stu-id="6fe94-121">These classes, however, had two major limitations.</span></span> <span data-ttu-id="6fe94-122">Za prvé `Tuple` třídy s názvem jejich vlastnosti `Item1`, `Item2`, a tak dále.</span><span class="sxs-lookup"><span data-stu-id="6fe94-122">For one, the `Tuple` classes named their properties `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="6fe94-123">Tyto názvy nesou žádné sémantické informace.</span><span class="sxs-lookup"><span data-stu-id="6fe94-123">Those names carry no semantic information.</span></span> <span data-ttu-id="6fe94-124">Použití těchto `Tuple` typy neumožňuje komunikaci význam jednotlivých vlastností.</span><span class="sxs-lookup"><span data-stu-id="6fe94-124">Using these `Tuple` types does not enable communicating the meaning of each of the properties.</span></span> <span data-ttu-id="6fe94-125">Nové funkce jazyků umožňují deklarování a použití sémanticky smysluplné názvy prvků v řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-125">The new language features enable you to declare and use semantically meaningful names for the elements in a tuple.</span></span>

<span data-ttu-id="6fe94-126">`Tuple` Třídy způsobit dopadům na výkon, protože jsou odkazové typy.</span><span class="sxs-lookup"><span data-stu-id="6fe94-126">The `Tuple` classes cause more performance concerns because they are reference types.</span></span> <span data-ttu-id="6fe94-127">Pomocí jednoho z `Tuple` typy znamená, že přidělování objektů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-127">Using one of the `Tuple` types means allocating objects.</span></span> <span data-ttu-id="6fe94-128">V horké cesty přidělování mnoha malých objektů může být měřitelný dopad na výkon vaší aplikace.</span><span class="sxs-lookup"><span data-stu-id="6fe94-128">On hot paths, allocating many small objects can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="6fe94-129">Proto se využívá novou jazykovou podporu pro řazené kolekce členů `ValueTuple` struktury.</span><span class="sxs-lookup"><span data-stu-id="6fe94-129">Therefore, the language support for tuples leverages the new `ValueTuple` structs.</span></span>

<span data-ttu-id="6fe94-130">Aby se zabránilo tyto nedostatky, můžete vytvořit `class` nebo `struct` provádět několik elementů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-130">To avoid those deficiencies, you could create a `class` or a `struct` to carry multiple elements.</span></span> <span data-ttu-id="6fe94-131">Bohužel to je další práci za vás a skryje máte v úmyslu návrhu.</span><span class="sxs-lookup"><span data-stu-id="6fe94-131">Unfortunately, that's more work for you, and it obscures your design intent.</span></span> <span data-ttu-id="6fe94-132">Vytváření `struct` nebo `class` znamená, že definujete typ s daty a chování.</span><span class="sxs-lookup"><span data-stu-id="6fe94-132">Making a `struct` or `class` implies that you are defining a type with both data and behavior.</span></span> <span data-ttu-id="6fe94-133">V mnoha případech chcete jednoduše uložení více hodnot do jednoho objektu.</span><span class="sxs-lookup"><span data-stu-id="6fe94-133">Many times, you simply want to store multiple values in a single object.</span></span>

<span data-ttu-id="6fe94-134">Funkce jazyka a `ValueTuple` obecné struktury vynutit pravidlo, že nemůžete přidat žádné chování (metody) pro tyto typy řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-134">The language features and the `ValueTuple` generic structs enforce the rule that you cannot add any behavior (methods) to these tuple types.</span></span>
<span data-ttu-id="6fe94-135">Všechny `ValueTuple` typy jsou *proměnlivé struktury*.</span><span class="sxs-lookup"><span data-stu-id="6fe94-135">All the `ValueTuple` types are *mutable structs*.</span></span> <span data-ttu-id="6fe94-136">Každý člen pole je veřejné pole.</span><span class="sxs-lookup"><span data-stu-id="6fe94-136">Each member field is a public field.</span></span> <span data-ttu-id="6fe94-137">Díky tomu je velmi jednoduché.</span><span class="sxs-lookup"><span data-stu-id="6fe94-137">That makes them very lightweight.</span></span> <span data-ttu-id="6fe94-138">Nicméně to znamená, že řazené kolekce členů by neměl být použít, kde je důležité neměnnosti.</span><span class="sxs-lookup"><span data-stu-id="6fe94-138">However, that means tuples should not be used where immutability is important.</span></span>

<span data-ttu-id="6fe94-139">Řazené kolekce členů jsou kontejnery obou data jednodušší a flexibilnější než `class` a `struct` typy.</span><span class="sxs-lookup"><span data-stu-id="6fe94-139">Tuples are both simpler and more flexible data containers than `class` and `struct` types.</span></span> <span data-ttu-id="6fe94-140">Podíváme se na tyto rozdíly.</span><span class="sxs-lookup"><span data-stu-id="6fe94-140">Let's explore those differences.</span></span>

## <a name="named-and-unnamed-tuples"></a><span data-ttu-id="6fe94-141">Pojmenované a nepojmenované řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="6fe94-141">Named and unnamed tuples</span></span>

<span data-ttu-id="6fe94-142">`ValueTuple` Struktura obsahuje pole s názvem `Item1`, `Item2`, `Item3`, a tak dále, podobně jako vlastnosti definované v existujícím `Tuple` typy.</span><span class="sxs-lookup"><span data-stu-id="6fe94-142">The `ValueTuple` struct has fields named `Item1`, `Item2`, `Item3`, and so on, similar to the properties defined in the existing `Tuple` types.</span></span>
<span data-ttu-id="6fe94-143">Tyto názvy jsou pouze názvy, můžete použít pro *nepojmenované řazených kolekcí členů*.</span><span class="sxs-lookup"><span data-stu-id="6fe94-143">These names are the only names you can use for *unnamed tuples*.</span></span> <span data-ttu-id="6fe94-144">Pokud nezadáte žádné pole alternativní názvy pro n-tice, co vytvoříte nepojmenované řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="6fe94-144">When you do not provide any alternative field names to a tuple, you've created an unnamed tuple:</span></span>

[!code-csharp[UnnamedTuple](../../samples/snippets/csharp/tuples/tuples/program.cs#01_UnNamedTuple "Unnamed tuple")]

<span data-ttu-id="6fe94-145">Řazené kolekce členů v předchozím příkladu byl inicializován pomocí literálu konstanty a nebude mít názvy elementů vytvořit pomocí *řazené kolekce členů pole Název projekce* v jazyce C# 7.1.</span><span class="sxs-lookup"><span data-stu-id="6fe94-145">The tuple in the previous example was initialized using literal constants and won't have element names created using *tuple field name projections* in C# 7.1.</span></span>

<span data-ttu-id="6fe94-146">Při inicializaci řazené kolekce členů, ale můžete použít nové funkce jazyků, které poskytnou lepší názvy k jednotlivým polím.</span><span class="sxs-lookup"><span data-stu-id="6fe94-146">However, when you initialize a tuple, you can use new language features that give better names to each field.</span></span> <span data-ttu-id="6fe94-147">Tím se vytvoří *s názvem řazené kolekce členů*.</span><span class="sxs-lookup"><span data-stu-id="6fe94-147">Doing so creates a *named tuple*.</span></span>
<span data-ttu-id="6fe94-148">Pojmenované řazených kolekcí členů, ještě elementů s názvem `Item1`, `Item2`, `Item3` a tak dále.</span><span class="sxs-lookup"><span data-stu-id="6fe94-148">Named tuples still have elements named `Item1`, `Item2`, `Item3` and so on.</span></span>
<span data-ttu-id="6fe94-149">Ale mají také synonyma pro některý z těchto prvků, které mají s názvem.</span><span class="sxs-lookup"><span data-stu-id="6fe94-149">But they also have synonyms for any of those elements that you have named.</span></span>
<span data-ttu-id="6fe94-150">Vytvořit pojmenovaný řazené kolekce členů tak, že zadáte názvy pro každý prvek.</span><span class="sxs-lookup"><span data-stu-id="6fe94-150">You create a named tuple by specifying the names for each element.</span></span> <span data-ttu-id="6fe94-151">První způsob je zadat názvy jako součást inicializace řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="6fe94-151">One way is to specify the names as part of the tuple initialization:</span></span>

[!code-csharp[NamedTuple](../../samples/snippets/csharp/tuples/tuples/program.cs#02_NamedTuple "Named tuple")]

<span data-ttu-id="6fe94-152">Tato synonyma jsou zpracovány v kompilátoru a jazyk tak, aby vám s názvem řazených kolekcí členů efektivně.</span><span class="sxs-lookup"><span data-stu-id="6fe94-152">These synonyms are handled by the compiler and the language so that you can use named tuples effectively.</span></span> <span data-ttu-id="6fe94-153">Editory a integrovanými vývojovými prostředími můžete číst tyto sémantické názvy pomocí rozhraní Roslyn API.</span><span class="sxs-lookup"><span data-stu-id="6fe94-153">IDEs and editors can read these semantic names using the Roslyn APIs.</span></span> <span data-ttu-id="6fe94-154">Prvky pojmenované řazené kolekce členů můžete odkazovat na tyto sémantické názvy kdekoli ve stejném sestavení.</span><span class="sxs-lookup"><span data-stu-id="6fe94-154">You can reference the elements of a named tuple by those semantic names anywhere in the same assembly.</span></span> <span data-ttu-id="6fe94-155">Kompilátor nahradí názvy, které jste definovali s `Item*` ekvivalenty při generování kompilovaný výstup.</span><span class="sxs-lookup"><span data-stu-id="6fe94-155">The compiler replaces the names you've defined with `Item*` equivalents when generating the compiled output.</span></span> <span data-ttu-id="6fe94-156">Kompilované Microsoft Intermediate Language (MSIL) nezahrnuje názvy jste dali tyto prvky.</span><span class="sxs-lookup"><span data-stu-id="6fe94-156">The compiled Microsoft Intermediate Language (MSIL) does not include the names you've given these elements.</span></span>

<span data-ttu-id="6fe94-157">Od verze C# 7.1, může být poskytnuta názvy polí pro řazené kolekce členů z proměnných, které slouží k inicializaci řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-157">Beginning with C# 7.1, the field names for a tuple may be provided from the variables used to initialize the tuple.</span></span> <span data-ttu-id="6fe94-158">To se označuje jako  **[řazené kolekce členů projekce inicializátory](#tuple-projection-initializers)**.</span><span class="sxs-lookup"><span data-stu-id="6fe94-158">This is referred to as **[tuple projection initializers](#tuple-projection-initializers)**.</span></span> <span data-ttu-id="6fe94-159">Následující kód vytvoří řazenou kolekci členů s názvem `accumulation` s prvky `count` (celé číslo), a `sum` (typ double).</span><span class="sxs-lookup"><span data-stu-id="6fe94-159">The following code creates a tuple named `accumulation` with elements `count` (an integer), and `sum` (a double).</span></span>

[!code-csharp[ProjectedTuple](../../samples/snippets/csharp/tuples/tuples/program.cs#ProjectedTupleNames "Named tuple")]

<span data-ttu-id="6fe94-160">Kompilátor musí komunikovat tyto názvy, které jste vytvořili pro řazené kolekce členů, které jsou vráceny z veřejné metody nebo vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="6fe94-160">The compiler must communicate those names you created for tuples that are returned from public methods or properties.</span></span> <span data-ttu-id="6fe94-161">V těchto případech kompilátor přidá <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute> atributu v metodě.</span><span class="sxs-lookup"><span data-stu-id="6fe94-161">In those cases, the compiler adds a <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute> attribute on the method.</span></span> <span data-ttu-id="6fe94-162">Tento atribut obsahuje <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute.TransformNames> seznamu vlastností, které obsahují názvy na každý prvek v řazené kolekci členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-162">This attribute contains a <xref:System.Runtime.CompilerServices.TupleElementNamesAttribute.TransformNames> list property that contains the names given to each of the elements in the tuple.</span></span>

> [!NOTE]
> <span data-ttu-id="6fe94-163">Vývojářské nástroje, jako je Visual Studio také čtení těchto metadat a poskytují technologie IntelliSense a dalších funkcí pomocí názvů polí metadat.</span><span class="sxs-lookup"><span data-stu-id="6fe94-163">Development Tools, such as Visual Studio, also read that metadata, and provide IntelliSense and other features using the metadata field names.</span></span>

<span data-ttu-id="6fe94-164">Je důležité pochopit tyto základní principy nové řazených kolekcí členů a `ValueTuple` typ, chcete-li pochopit pravidla pro přiřazení s názvem řazené kolekce členů k sobě navzájem.</span><span class="sxs-lookup"><span data-stu-id="6fe94-164">It is important to understand these underlying fundamentals of the new tuples and the `ValueTuple` type in order to understand the rules for assigning named tuples to each other.</span></span>

## <a name="tuple-projection-initializers"></a><span data-ttu-id="6fe94-165">Inicializátory projekce řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="6fe94-165">Tuple projection initializers</span></span>

<span data-ttu-id="6fe94-166">Inicializátory projekce řazené kolekce členů obecně fungovat pomocí názvů pole nebo proměnné z pravá strana výrazu inicializace řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-166">In general, tuple projection initializers work by using the variable or field names from the right-hand side of a tuple initialization statement.</span></span>
<span data-ttu-id="6fe94-167">-Li zadána explicitní název, který má přednost před jakékoli předpokládané název.</span><span class="sxs-lookup"><span data-stu-id="6fe94-167">If an explicit name is given, that takes precedence over any projected name.</span></span> <span data-ttu-id="6fe94-168">Například v následujícím inicializátoru, jsou prvky `explicitFieldOne` a `explicitFieldTwo`, nikoli `localVariableOne` a `localVariableTwo`:</span><span class="sxs-lookup"><span data-stu-id="6fe94-168">For example, in the following initializer, the elements are `explicitFieldOne` and `explicitFieldTwo`, not `localVariableOne` and `localVariableTwo`:</span></span>

[!code-csharp[ExplicitNamedTuple](../../samples/snippets/csharp/tuples/tuples/program.cs#ProjectionExample_Explicit "Explicitly named tuple")]

<span data-ttu-id="6fe94-169">Pro všechna pole, pokud není poskytnut jasný název je použít implicitní název očekávaná.</span><span class="sxs-lookup"><span data-stu-id="6fe94-169">For any field where an explicit name is not provided, an applicable implicit name is projected.</span></span> <span data-ttu-id="6fe94-170">Není nutné zadat sémantické názvy, explicitně nebo implicitně.</span><span class="sxs-lookup"><span data-stu-id="6fe94-170">There is no requirement to provide semantic names, either explicitly or implicitly.</span></span> <span data-ttu-id="6fe94-171">Následující inicializátor s názvy polí `Item1`, jehož hodnota je `42` a `stringContent`, jehož hodnota je "Odpověď na všechno, co":</span><span class="sxs-lookup"><span data-stu-id="6fe94-171">The following initializer has     field names `Item1`, whose value is `42` and `stringContent`, whose value is "The answer to everything":</span></span>

[!code-csharp[MixedTuple](../../samples/snippets/csharp/tuples/tuples/program.cs#MixedTuple "mixed tuple")]

<span data-ttu-id="6fe94-172">Existují dvě podmínky. Pokud nejsou názvy polí Release candidate plánovaných na pole řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="6fe94-172">There are two conditions where candidate field names are not projected onto the tuple field:</span></span>

1. <span data-ttu-id="6fe94-173">Pokud název Release candidate je název vyhrazené řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-173">When the candidate name is a reserved tuple name.</span></span> <span data-ttu-id="6fe94-174">Mezi příklady patří `Item3`, `ToString`.</span><span class="sxs-lookup"><span data-stu-id="6fe94-174">Examples include `Item3`, `ToString`.</span></span> <span data-ttu-id="6fe94-175">nebo `Rest`.</span><span class="sxs-lookup"><span data-stu-id="6fe94-175">or `Rest`.</span></span>
1. <span data-ttu-id="6fe94-176">Pokud název Release candidate je duplicitní jiného názvu pole řazené kolekce členů, explicitní nebo implicitní.</span><span class="sxs-lookup"><span data-stu-id="6fe94-176">When the candidate name is a duplicate of another tuple field name, either explicit or implicit.</span></span>

<span data-ttu-id="6fe94-177">Tyto podmínky-li předejít nejednoznačnosti.</span><span class="sxs-lookup"><span data-stu-id="6fe94-177">These conditions avoid ambiguity.</span></span> <span data-ttu-id="6fe94-178">Tyto názvy by způsobit nejednoznačnost, pokud byly použity jako názvy polí pro pole řazená kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-178">These names would cause an ambiguity if they were used as the field names for a field in a tuple.</span></span> <span data-ttu-id="6fe94-179">Žádná z těchto podmínek způsobit chyby kompilace.</span><span class="sxs-lookup"><span data-stu-id="6fe94-179">Neither of these conditions cause compile-time errors.</span></span> <span data-ttu-id="6fe94-180">Místo toho elementy bez předpokládané názvů nemají sémantické názvy plánovaných pro ně.</span><span class="sxs-lookup"><span data-stu-id="6fe94-180">Instead, the elements without projected names do not have semantic names projected for them.</span></span>  <span data-ttu-id="6fe94-181">Následující příklady ukazují tyto podmínky:</span><span class="sxs-lookup"><span data-stu-id="6fe94-181">The following examples demonstrate these conditions:</span></span>

[!code-csharp[Ambiguity](../../samples/snippets/csharp/tuples/tuples/program.cs#ProjectionAmbiguities "tuples where projections are not performed")]

<span data-ttu-id="6fe94-182">Těmito situacemi nezpůsobí chyby kompilátoru, protože, která by byla k zásadní změně kódu napsané v jazyce C# 7.0, když řazené kolekce členů pole Název projekce nebyly k dispozici.</span><span class="sxs-lookup"><span data-stu-id="6fe94-182">These situations do not cause compiler errors because that would be a breaking change for code written with C# 7.0, when tuple field name projections were not available.</span></span>

## <a name="equality-and-tuples"></a><span data-ttu-id="6fe94-183">Rovnost a řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="6fe94-183">Equality and tuples</span></span>

<span data-ttu-id="6fe94-184">Od verze C# 7.3, typy řazené kolekce členů podpory `==` a `!=` operátory.</span><span class="sxs-lookup"><span data-stu-id="6fe94-184">Beginning with C# 7.3, tuple types support the `==` and `!=` operators.</span></span> <span data-ttu-id="6fe94-185">Tyto operátory pracují na základě porovnání každý člen levý argument pro každého člena pravý argument v pořadí.</span><span class="sxs-lookup"><span data-stu-id="6fe94-185">These operators work by comparing each member of the left argument to each member of the right argument in order.</span></span> <span data-ttu-id="6fe94-186">Tato porovnání zkrácenou.</span><span class="sxs-lookup"><span data-stu-id="6fe94-186">These comparisons short-circuit.</span></span> <span data-ttu-id="6fe94-187">`==` Operátor zastaví jako jednu dvojici se nerovná vyhodnocování členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-187">The `==` operator stops evaluating members as soon as one pair is not equal.</span></span> <span data-ttu-id="6fe94-188">`!=` Operátor zastaví ihned poté, co se rovná jednu dvojici vyhodnocování členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-188">The `!=` operator stops evaluating members as soon as one pair is equal.</span></span> <span data-ttu-id="6fe94-189">Následující příklady kódu používají `==`, ale pravidla porovnání všech platí pro `!=`.</span><span class="sxs-lookup"><span data-stu-id="6fe94-189">The following code examples use `==`, but the comparison rules all apply to `!=`.</span></span> <span data-ttu-id="6fe94-190">Následující příklad kódu ukazuje porovnání rovnosti pro dvě dvojice celých čísel:</span><span class="sxs-lookup"><span data-stu-id="6fe94-190">The following code example shows an equality comparison for two pairs of integers:</span></span>

[!code-csharp[TupleEquality](../../samples/snippets/csharp/tuples/tuples/program.cs#Equality "Testing tuples for equality")]

<span data-ttu-id="6fe94-191">Existuje několik pravidel, která pohodlnější provádění testů rovnost řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-191">There are several rules that make tuple equality tests more convenient.</span></span> <span data-ttu-id="6fe94-192">Rovnost řazené kolekce členů provádí [zrušeno vs. převody](language-reference/language-specification/index.md) Pokud jeden ze záznamů je s možnou hodnotou Null řazené kolekce členů, jak je znázorněno v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="6fe94-192">Tuple equality performs [lifted conversions](language-reference/language-specification/index.md) if one of the tuples is a nullable tuple, as shown in the following code:</span></span>


[!code-csharp[NullableTupleEquality](../../samples/snippets/csharp/tuples/tuples/program.cs#NullableEquality "Comparing Tuples and nullable tuples")]

<span data-ttu-id="6fe94-193">Rovnost řazené kolekce členů také provádí implicitní převody v každém členovi obou řazených kolekcí členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-193">Tuple equality also performs implicit conversions on each member of both tuples.</span></span> <span data-ttu-id="6fe94-194">Patří mezi ně zdvižené převody rozšiřující převody a jiných implicitních převodů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-194">These include lifted conversions, widening conversions, or other implicit conversions.</span></span> <span data-ttu-id="6fe94-195">Následující příklady ukazují, že celé číslo 2-řazené kolekce členů je možné porovnat s dlouhou 2-řazené kolekce členů z důvodu implicitní převod z celého čísla na dlouhé:</span><span class="sxs-lookup"><span data-stu-id="6fe94-195">The following examples show that an integer 2-tuple can be compared to a long 2-tuple because of the implicit conversion from integer to long:</span></span>

[!code-csharp[SnippetMemberConversions](../../samples/snippets/csharp/tuples/tuples/program.cs#SnippetMemberConversions "converting tuples for equality tests")]

<span data-ttu-id="6fe94-196">Názvy členů řazené kolekce členů nejsou součástí testy pro rovnost.</span><span class="sxs-lookup"><span data-stu-id="6fe94-196">The names of the tuple members do not participate in tests for equality.</span></span> <span data-ttu-id="6fe94-197">Ale pokud jeden z operandů je literál s explicitní názvy řazené kolekce členů, kompilátor vygeneruje upozornění CS8383 Pokud tyto názvy se neshodují s názvy je druhý operand.</span><span class="sxs-lookup"><span data-stu-id="6fe94-197">However, if one of the operands is a tuple literal with explicit names, the compiler generates warning CS8383 if those names do not match the names of the other operand.</span></span>
<span data-ttu-id="6fe94-198">V případě, pokud jsou oba operandy literálech řazené kolekce členů se upozornění na pravý operand, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="6fe94-198">In the case where both operands are tuple literals, the warning is on the right operand as shown in the following example:</span></span>

[!code-csharp[MemberNames](../../samples/snippets/csharp/tuples/tuples/program.cs#SnippetMemberNames "Tuple member names do not participate in equality tests")]

<span data-ttu-id="6fe94-199">Nakonec řazené kolekce členů může obsahovat vnořené řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-199">Finally, tuples may contain nested tuples.</span></span> <span data-ttu-id="6fe94-200">Porovná rovnost řazené kolekce členů "tvar" operandem prostřednictvím vnořené řazených kolekcí členů, jako je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="6fe94-200">Tuple equality compares the "shape" of each operand through nested tuples as shown in the following example:</span></span>

[!code-csharp[NestedTuples](../../samples/snippets/csharp/tuples/tuples/program.cs#SnippetNestedTuples "Tuples may contain nested tuples that participate in tuple equality.")]

## <a name="assignment-and-tuples"></a><span data-ttu-id="6fe94-201">Přiřazení a řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="6fe94-201">Assignment and tuples</span></span>

<span data-ttu-id="6fe94-202">Jazyk podporuje mezi typy řazené kolekce členů, které mají stejný počet prvků, kde každý prvek na pravé straně lze implicitně převést na odpovídající prvek levá strana příkazu přiřazení.</span><span class="sxs-lookup"><span data-stu-id="6fe94-202">The language supports assignment between tuple types that have the same number of elements, where each right-hand side element can be implicitly converted to its corresponding left-hand side element.</span></span> <span data-ttu-id="6fe94-203">Ostatní převody, které nejsou považované za pro přiřazení.</span><span class="sxs-lookup"><span data-stu-id="6fe94-203">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="6fe94-204">Pojďme se podívat na typy přiřazení, které jsou povolené mezi typy řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-204">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="6fe94-205">Vezměte v úvahu tyto proměnné, používat v následujících příkladech:</span><span class="sxs-lookup"><span data-stu-id="6fe94-205">Consider these variables used in the following examples:</span></span>

[!code-csharp[VariableCreation](../../samples/snippets/csharp/tuples/tuples/program.cs#03_VariableCreation "Variable creation")]

<span data-ttu-id="6fe94-206">První dvě proměnné `unnamed` a `anonymous` nemají sémantické názvy, které jsou k dispozici pro elementy.</span><span class="sxs-lookup"><span data-stu-id="6fe94-206">The first two variables, `unnamed` and `anonymous` do not have semantic names provided for the elements.</span></span> <span data-ttu-id="6fe94-207">Názvy polí jsou `Item1` a `Item2`.</span><span class="sxs-lookup"><span data-stu-id="6fe94-207">The field names are `Item1` and `Item2`.</span></span>
<span data-ttu-id="6fe94-208">Poslední dvě proměnné, `named` a `differentName` sémantické názvy pro elementy.</span><span class="sxs-lookup"><span data-stu-id="6fe94-208">The last two variables, `named` and `differentName` have semantic names given for the elements.</span></span> <span data-ttu-id="6fe94-209">Tyto dvě řazené kolekce členů mají různé názvy prvků.</span><span class="sxs-lookup"><span data-stu-id="6fe94-209">These two tuples have different names for the elements.</span></span>

<span data-ttu-id="6fe94-210">Všechny čtyři tyto řazených kolekcí členů mají stejný počet prvků (dále jako "Kardinalita") a typy tyto prvky jsou identické.</span><span class="sxs-lookup"><span data-stu-id="6fe94-210">All four of these tuples have the same number of elements (referred to as 'cardinality') and the types of those elements are identical.</span></span> <span data-ttu-id="6fe94-211">Proto všechny tato přiřazení práce:</span><span class="sxs-lookup"><span data-stu-id="6fe94-211">Therefore, all of these assignments work:</span></span>

[!code-csharp[VariableAssignment](../../samples/snippets/csharp/tuples/tuples/program.cs#04_VariableAssignment "Variable assignment")]

<span data-ttu-id="6fe94-212">Všimněte si, že nejsou přiřazeny názvy řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-212">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="6fe94-213">Hodnoty elementů jsou přiřazeny následující pořadí prvků řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-213">The values of the elements are assigned following the order of the elements in the tuple.</span></span>

<span data-ttu-id="6fe94-214">N-tice různé typy nebo počtem elementů nejsou přiřadit:</span><span class="sxs-lookup"><span data-stu-id="6fe94-214">Tuples of different types or numbers of elements are not assignable:</span></span>

```csharp
// Does not compile.
// CS0029: Cannot assign Tuple(int,int,int) to Tuple(int, string)
var differentShape = (1, 2, 3);
named = differentShape;
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="6fe94-215">Řazené kolekce členů jako návratové hodnoty metod</span><span class="sxs-lookup"><span data-stu-id="6fe94-215">Tuples as method return values</span></span>

<span data-ttu-id="6fe94-216">Jedním z nejběžnějších používá pro řazené kolekce členů je jako návratovou hodnotu metody.</span><span class="sxs-lookup"><span data-stu-id="6fe94-216">One of the most common uses for tuples is as a method return value.</span></span> <span data-ttu-id="6fe94-217">Projděme si jedním z příkladů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-217">Let's walk through one example.</span></span> <span data-ttu-id="6fe94-218">Vezměte v úvahu tuto metodu, která vypočítá směrodatnou odchylku pro sekvenci čísel:</span><span class="sxs-lookup"><span data-stu-id="6fe94-218">Consider this method that computes the standard deviation for a sequence of numbers:</span></span>

[!code-csharp[StandardDeviation](../../samples/snippets/csharp/tuples/tuples/statistics.cs#05_StandardDeviation "Compute Standard Deviation")]

> [!NOTE]
> <span data-ttu-id="6fe94-219">Tyto příklady výpočetní směrodatná odchylka neopraveno vzorku.</span><span class="sxs-lookup"><span data-stu-id="6fe94-219">These examples compute the uncorrected sample standard deviation.</span></span>
> <span data-ttu-id="6fe94-220">Vzorec směrodatné odchylky opravený ukázka by rozdělit součet kvadratických odchylek od střední hodnoty podle (N-1) namísto N, jako `Average` metodu rozšíření.</span><span class="sxs-lookup"><span data-stu-id="6fe94-220">The corrected sample standard deviation formula would divide the sum of the squared differences from the mean by (N-1) instead of N, as the `Average` extension method does.</span></span> <span data-ttu-id="6fe94-221">Statistiky textu najdete podrobné informace o rozdílech mezi tyto vzorce pro směrodatnou odchylku.</span><span class="sxs-lookup"><span data-stu-id="6fe94-221">Consult a statistics text for more details on the differences between these formulas for standard deviation.</span></span>

<span data-ttu-id="6fe94-222">Předchozí kód se řídí učebnice vzorec směrodatné odchylky.</span><span class="sxs-lookup"><span data-stu-id="6fe94-222">The preceding code follows the textbook formula for the standard deviation.</span></span> <span data-ttu-id="6fe94-223">Vytvoří správnou odpověď, ale je neefektivní implementace.</span><span class="sxs-lookup"><span data-stu-id="6fe94-223">It produces the correct answer, but it's an inefficient implementation.</span></span> <span data-ttu-id="6fe94-224">Tato metoda vytvoří výčet sekvence dvakrát: jednou k vytvoření průměr a jednou pro vytvoření průměr druhou mocninu rozdíl průměr.</span><span class="sxs-lookup"><span data-stu-id="6fe94-224">This method enumerates the sequence twice: Once to produce the average, and once to produce the average of the square of the difference of the average.</span></span>
<span data-ttu-id="6fe94-225">(Mějte na paměti, že LINQ dotazů jsou vyhodnocovány laxně, tak výpočet rozdíl oproti průměr a průměr těchto rozdílů je pouze jeden výčet.)</span><span class="sxs-lookup"><span data-stu-id="6fe94-225">(Remember that LINQ queries are evaluated lazily, so the computation of the differences from the mean and the average of those differences makes only one enumeration.)</span></span>

<span data-ttu-id="6fe94-226">Existuje alternativní vzorec, který vypočítá směrodatnou odchylku pomocí pouze jeden výčet pořadí.</span><span class="sxs-lookup"><span data-stu-id="6fe94-226">There is an alternative formula that computes standard deviation using only one enumeration of the sequence.</span></span>  <span data-ttu-id="6fe94-227">Tento výpočet vytváří dvě hodnoty při výčtu sekvence: součet všech položek v sekvenci a součet jednotlivých hodnot spolehlivosti:</span><span class="sxs-lookup"><span data-stu-id="6fe94-227">This computation produces two values as it enumerates the sequence: the sum of all items in the sequence, and the sum of the each value squared:</span></span>

[!code-csharp[SumOfSquaresFormula](../../samples/snippets/csharp/tuples/tuples/statistics.cs#06_SumOfSquaresFormula "Compute Standard Deviation using the sum of squares")]

<span data-ttu-id="6fe94-228">Tato verze vytvoří výčet sekvence přesně jednou.</span><span class="sxs-lookup"><span data-stu-id="6fe94-228">This version enumerates the sequence exactly once.</span></span> <span data-ttu-id="6fe94-229">Ale to není opakovaně použitelný kód.</span><span class="sxs-lookup"><span data-stu-id="6fe94-229">But it's not reusable code.</span></span> <span data-ttu-id="6fe94-230">Jak budete pokračovat v práci, zjistíte, že mnoho různých statistické výpočty pomocí počet položek v sekvenci, součet pořadí a součet druhých mocnin pořadí.</span><span class="sxs-lookup"><span data-stu-id="6fe94-230">As you keep working, you'll find that many different statistical computations use the number of items in the sequence, the sum of the sequence, and the sum of the squares of the sequence.</span></span> <span data-ttu-id="6fe94-231">Pojďme Refaktorovat tuto metodu a napíše metoda nástroj, který vytváří všechny tři z těchto hodnot.</span><span class="sxs-lookup"><span data-stu-id="6fe94-231">Let's refactor this method and write a utility method that produces all three of those values.</span></span> <span data-ttu-id="6fe94-232">Všechny tři hodnoty mohou být vráceny jako řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-232">All three values can be returned as a tuple.</span></span>

<span data-ttu-id="6fe94-233">Umožňuje aktualizovat tuto metodu tak tři hodnoty vypočítané během výčtu jsou uloženy v řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-233">Let's update this method so the three values computed during the enumeration are stored in a tuple.</span></span> <span data-ttu-id="6fe94-234">Který vytváří tato verze:</span><span class="sxs-lookup"><span data-stu-id="6fe94-234">That creates this version:</span></span>

[!code-csharp[TupleVersion](../../samples/snippets/csharp/tuples/tuples/statistics.cs#07_TupleVersion "Refactor to use tuples")]

<span data-ttu-id="6fe94-235">Podpora Refactoring sady Visual Studio umožňuje snadno extrahovat funkci pro základní statistiky do privátní metodu.</span><span class="sxs-lookup"><span data-stu-id="6fe94-235">Visual Studio's Refactoring support makes it easy to extract the functionality for the core statistics into a private method.</span></span> <span data-ttu-id="6fe94-236">Který vám `private static` metodu, která vrací typ řazené kolekce členů s použitím tří hodnot `Sum`, `SumOfSquares`, a `Count`:</span><span class="sxs-lookup"><span data-stu-id="6fe94-236">That gives you a `private static` method that returns the tuple type with the three values of `Sum`, `SumOfSquares`, and `Count`:</span></span>

[!code-csharp[TupleMethodVersion](../../samples/snippets/csharp/tuples/tuples/statistics.cs#08_TupleMethodVersion "After extracting utility method")]
 
<span data-ttu-id="6fe94-237">Jazyk umožňuje několik další možnosti, které můžete použít, pokud chcete, aby několik rychlé úpravy ručně.</span><span class="sxs-lookup"><span data-stu-id="6fe94-237">The language enables a couple more options that you can use, if you want to make a few quick edits by hand.</span></span> <span data-ttu-id="6fe94-238">Nejprve, můžete použít `var` prohlášení k inicializaci výsledkem řazené kolekce členů `ComputeSumAndSumOfSquares` volání metody.</span><span class="sxs-lookup"><span data-stu-id="6fe94-238">First, you can use the `var` declaration to initialize the tuple result from the `ComputeSumAndSumOfSquares` method call.</span></span> <span data-ttu-id="6fe94-239">Můžete také vytvořit tři samostatná proměnných uvnitř `ComputeSumAndSumOfSquares` metody.</span><span class="sxs-lookup"><span data-stu-id="6fe94-239">You can also create three discrete variables inside the `ComputeSumAndSumOfSquares` method.</span></span> <span data-ttu-id="6fe94-240">Konečná verze můžete vidět v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="6fe94-240">The final version is shown in the following code:</span></span>

[!code-csharp[CleanedTupleVersion](../../samples/snippets/csharp/tuples/tuples/statistics.cs#09_CleanedTupleVersion "After final cleanup")]

<span data-ttu-id="6fe94-241">Pro jakoukoli metodu, která potřebuje tyto tři hodnoty nebo jakékoli některé z nich je možné tuto poslední verzi.</span><span class="sxs-lookup"><span data-stu-id="6fe94-241">This final version can be used for any method that needs those three values, or any subset of them.</span></span>

<span data-ttu-id="6fe94-242">Jazyk podporuje další možnosti při správě názvy prvků v těchto metodách, které vracejí řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-242">The language supports other options in managing the names of the elements in these tuple-returning methods.</span></span>

<span data-ttu-id="6fe94-243">Můžete odebrat názvy polí z deklarace návratovou hodnotu a vrátí nepojmenované řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="6fe94-243">You can remove the field names from the return value declaration and return an unnamed tuple:</span></span>

```csharp
private static (double, double, int) ComputeSumAndSumOfSquares(IEnumerable<double> sequence)
{
    double sum = 0;
    double sumOfSquares = 0;
    int count = 0;

    foreach (var item in sequence)
    {
        count++;
        sum += item;
        sumOfSquares += item * item;
    }

    return (sum, sumOfSquares, count);
}
```

<span data-ttu-id="6fe94-244">Pole této řazené kolekce členů jsou pojmenovány `Item1`, `Item2`, a `Item3`.</span><span class="sxs-lookup"><span data-stu-id="6fe94-244">The fields of this tuple are named `Item1`, `Item2`, and `Item3`.</span></span>
<span data-ttu-id="6fe94-245">Doporučuje se, že zadáte sémantické názvy elementů řazené kolekce členů vracené z metod.</span><span class="sxs-lookup"><span data-stu-id="6fe94-245">It's recommended that you provide semantic names to the elements of tuples returned from methods.</span></span>

<span data-ttu-id="6fe94-246">Jiné idiom, kde může být užitečné řazené kolekce členů je při vytváření dotazů LINQ.</span><span class="sxs-lookup"><span data-stu-id="6fe94-246">Another idiom where tuples can be useful is when you are authoring LINQ queries.</span></span> <span data-ttu-id="6fe94-247">Konečný výsledek předpokládané často obsahuje některé, ale ne všechny vlastnosti objektů výběru.</span><span class="sxs-lookup"><span data-stu-id="6fe94-247">The final projected result often contains some, but not all, of the properties of the objects being selected.</span></span>

<span data-ttu-id="6fe94-248">Výsledky dotazu by tradičně projektu na sekvenci objektů, které byly anonymního typu.</span><span class="sxs-lookup"><span data-stu-id="6fe94-248">You would traditionally project the results of the query into a sequence of objects that were an anonymous type.</span></span> <span data-ttu-id="6fe94-249">Který zobrazí mnoho omezení, především proto anonymní typy nelze jednoduše pojmenované v návratovém typu pro metodu.</span><span class="sxs-lookup"><span data-stu-id="6fe94-249">That presented many limitations, primarily because anonymous types could not conveniently be named in the return type for a method.</span></span> <span data-ttu-id="6fe94-250">Alternativy pomocí `object` nebo `dynamic` jako typ výsledku dodán jako součást výkonu náklady.</span><span class="sxs-lookup"><span data-stu-id="6fe94-250">Alternatives using `object` or `dynamic` as the type of the result came with significant performance costs.</span></span>

<span data-ttu-id="6fe94-251">Vrací posloupnost řazené kolekce členů typu je snadné a názvy a typy elementů jsou k dispozici, v době kompilace a integrovaného vývojového prostředí pomocí nástrojů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-251">Returning a sequence of a tuple type is easy, and the names and types of the elements are available at compile time and through IDE tools.</span></span>
<span data-ttu-id="6fe94-252">Zvažte například aplikaci seznamu úkolů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-252">For example, consider a ToDo application.</span></span> <span data-ttu-id="6fe94-253">Můžete třeba definovat třídu podobný následujícímu pro reprezentaci jedna položka v seznamu úkolů:</span><span class="sxs-lookup"><span data-stu-id="6fe94-253">You might define a class similar to the following to represent a single entry in the ToDo list:</span></span>

[!code-csharp[ToDoItem](../../samples/snippets/csharp/tuples/tuples/projectionsample.cs#14_ToDoItem "To Do Item")]

<span data-ttu-id="6fe94-254">Mobilní aplikace může podporovat kompaktní podobě aktuální položky seznamu úkolů, který zobrazuje pouze název.</span><span class="sxs-lookup"><span data-stu-id="6fe94-254">Your mobile applications may support a compact form of the current ToDo items that only displays the title.</span></span> <span data-ttu-id="6fe94-255">Že dotazů LINQ s žádným projekci, která obsahuje pouze ID a název.</span><span class="sxs-lookup"><span data-stu-id="6fe94-255">That LINQ query would make a projection that includes only the ID and the title.</span></span> <span data-ttu-id="6fe94-256">Metodu, která vrátí sekvenci řazených kolekcí členů vyjadřuje tento návrh kontejneru:</span><span class="sxs-lookup"><span data-stu-id="6fe94-256">A method that returns a sequence of tuples expresses that design well:</span></span>

[!code-csharp[QueryReturningTuple](../../samples/snippets/csharp/tuples/tuples/projectionsample.cs#15_QueryReturningTuple "Query returning a tuple")]

> [!NOTE]
> <span data-ttu-id="6fe94-257">V jazyce C# 7.1 řazené kolekce členů projekce umožňují vytvářet pojmenované řazených kolekcí členů, pomocí elementů, názvy vlastností anonymních typů podobným způsobem.</span><span class="sxs-lookup"><span data-stu-id="6fe94-257">In C# 7.1, tuple projections enable you to create named tuples using elements, in a manner similar to the property naming in anonymous types.</span></span> <span data-ttu-id="6fe94-258">Ve výše uvedeném kódu `select` příkaz v projekci dotaz vytvoří řazenou kolekci členů, který má prvky `ID` a `Title`.</span><span class="sxs-lookup"><span data-stu-id="6fe94-258">In the above code, the `select` statement in the query projection creates a tuple that has elements `ID` and `Title`.</span></span>

<span data-ttu-id="6fe94-259">Pojmenované řazené kolekce členů může být součást podpisu.</span><span class="sxs-lookup"><span data-stu-id="6fe94-259">The named tuple can be part of the signature.</span></span> <span data-ttu-id="6fe94-260">Umožňuje kompilátoru a integrovaného vývojového prostředí nástroje poskytují statické kontroluje se, že výsledek používáte správně.</span><span class="sxs-lookup"><span data-stu-id="6fe94-260">It lets the compiler and IDE tools provide static checking that you are using the result correctly.</span></span> <span data-ttu-id="6fe94-261">Pojmenované řazené kolekce členů také přenáší informace statického typu, takže není nutné používat drahé běhu funkcí, jako je odraz nebo dynamické vazby pro práci s výsledky.</span><span class="sxs-lookup"><span data-stu-id="6fe94-261">The named tuple also carries the static type information so there is no need to use expensive run time features like reflection or dynamic binding to work with the results.</span></span>

## <a name="deconstruction"></a><span data-ttu-id="6fe94-262">Dekonstrukce</span><span class="sxs-lookup"><span data-stu-id="6fe94-262">Deconstruction</span></span>

<span data-ttu-id="6fe94-263">Můžete rozbalit všechny položky v řazené kolekce členů podle *dekonstrukce* řazené kolekce členů, vrací metoda.</span><span class="sxs-lookup"><span data-stu-id="6fe94-263">You can unpackage all the items in a tuple by *deconstructing* the tuple returned by a method.</span></span> <span data-ttu-id="6fe94-264">Existují tři různé přístupy k dekonstrukce řazených kolekcí členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-264">There are three different approaches to deconstructing tuples.</span></span>  <span data-ttu-id="6fe94-265">Nejprve lze explicitně deklarovat typ každého pole uvnitř závorek vytváření diskrétního proměnných pro každý prvek v řazené kolekci členů:</span><span class="sxs-lookup"><span data-stu-id="6fe94-265">First, you can explicitly declare the type of each field inside parentheses to create discrete variables for each of the elements in the tuple:</span></span>

[!code-csharp[Deconstruct](../../samples/snippets/csharp/tuples/tuples/statistics.cs#10_Deconstruct "Deconstruct")]

<span data-ttu-id="6fe94-266">Implicitně typované proměnné pro každé pole v řazené kolekci členů můžete také deklarovat s použitím `var` – klíčové slovo mimo závorek:</span><span class="sxs-lookup"><span data-stu-id="6fe94-266">You can also declare implicitly typed variables for each field in a tuple by using the `var` keyword outside the parentheses:</span></span>

[!code-csharp[DeconstructToVar](../../samples/snippets/csharp/tuples/tuples/statistics.cs#11_DeconstructToVar "Deconstruct to Var")]

<span data-ttu-id="6fe94-267">Je také možné použít `var` – klíčové slovo se některé nebo všechny deklarace proměnných v závorkách.</span><span class="sxs-lookup"><span data-stu-id="6fe94-267">It is also legal to use the `var` keyword with any, or all of the variable declarations inside the parentheses.</span></span> 

```csharp
(double sum, var sumOfSquares, var count) = ComputeSumAndSumOfSquares(sequence);
```

<span data-ttu-id="6fe94-268">Konkrétní typ mimo závorky, nelze použít, i v případě každé pole v n-tice stejného typu.</span><span class="sxs-lookup"><span data-stu-id="6fe94-268">You cannot use a specific type outside the parentheses, even if every field in the tuple has the same type.</span></span>

<span data-ttu-id="6fe94-269">Možné dekonstruovat řazené kolekce členů s také existující deklarace:</span><span class="sxs-lookup"><span data-stu-id="6fe94-269">You can deconstruct tuples with existing declarations as well:</span></span>

```csharp
public class Point
{
    public int X { get; set; }
    public int Y { get; set; }

    public Point(int x, int y) => (X, Y) = (x, y);
}
```

> [!WARNING]
>  <span data-ttu-id="6fe94-270">Nejde kombinovat existující deklarace s deklaracemi v závorkách.</span><span class="sxs-lookup"><span data-stu-id="6fe94-270">You cannot mix existing declarations with declarations inside the parentheses.</span></span> <span data-ttu-id="6fe94-271">Například následující není povoleno: `(var x, y) = MyMethod();`.</span><span class="sxs-lookup"><span data-stu-id="6fe94-271">For instance, the following is not allowed: `(var x, y) = MyMethod();`.</span></span> <span data-ttu-id="6fe94-272">Výsledkem chyba CS8184, protože *x* je deklarována uvnitř závorek a *y* se dřív deklaroval jinde.</span><span class="sxs-lookup"><span data-stu-id="6fe94-272">This produces error CS8184 because *x* is declared inside the parentheses and *y* is previously declared elsewhere.</span></span>

### <a name="deconstructing-user-defined-types"></a><span data-ttu-id="6fe94-273">Dekonstrukce uživatelem definované typy</span><span class="sxs-lookup"><span data-stu-id="6fe94-273">Deconstructing user-defined types</span></span>

<span data-ttu-id="6fe94-274">Jakýkoli typ řazené kolekce členů je možné dekonstruovat. jak je znázorněno výše.</span><span class="sxs-lookup"><span data-stu-id="6fe94-274">Any tuple type can be deconstructed as shown above.</span></span> <span data-ttu-id="6fe94-275">Je také snadné povolení dekonstrukce na libovolný typ definovaný uživatelem (třídy, struktury nebo dokonce rozhraní).</span><span class="sxs-lookup"><span data-stu-id="6fe94-275">It's also easy to enable deconstruction on any user-defined type (classes, structs, or even interfaces).</span></span>

<span data-ttu-id="6fe94-276">Autor typu můžete definovat jeden nebo více `Deconstruct` metody, které přiřazují hodnoty do libovolného počtu `out` představující datové prvky, které tvoří typ proměnné.</span><span class="sxs-lookup"><span data-stu-id="6fe94-276">The type author can define one or more `Deconstruct` methods that assign values to any number of `out` variables representing the data elements that make up the type.</span></span> <span data-ttu-id="6fe94-277">Například následující `Person` definuje typ `Deconstruct` metodu, která deconstructs osoba do prvků, která představuje křestní jméno a příjmení:</span><span class="sxs-lookup"><span data-stu-id="6fe94-277">For example, the following `Person` type defines a `Deconstruct` method that deconstructs a person object into the elements representing the first name and last name:</span></span>

[!code-csharp[TypeWithDeconstructMethod](../../samples/snippets/csharp/tuples/tuples/person.cs#12_TypeWithDeconstructMethod "Type with a deconstruct method")]

<span data-ttu-id="6fe94-278">Metoda deconstruct umožňuje přiřazení `Person` na dva řetězce představující `FirstName` a `LastName` vlastnosti:</span><span class="sxs-lookup"><span data-stu-id="6fe94-278">The deconstruct method enables assignment from a `Person` to two strings, representing the `FirstName` and `LastName` properties:</span></span>

[!code-csharp[Deconstruct Type](../../samples/snippets/csharp/tuples/tuples/program.cs#12A_DeconstructType "Deconstruct a class type")]

<span data-ttu-id="6fe94-279">Můžete povolit dekonstrukce i pro typy, které nelze vytvářet.</span><span class="sxs-lookup"><span data-stu-id="6fe94-279">You can enable deconstruction even for types you did not author.</span></span>
<span data-ttu-id="6fe94-280">`Deconstruct` Metoda může být metodu rozšíření, který rozbalí dostupné datové členy objektu.</span><span class="sxs-lookup"><span data-stu-id="6fe94-280">The `Deconstruct` method can be an extension method that unpackages the accessible data members of an object.</span></span> <span data-ttu-id="6fe94-281">V příkladu níže ukazuje `Student` typu odvozeného z `Person` typ a metodu rozšíření, která deconstructs `Student` do tří proměnných, představující `FirstName`, `LastName`a `GPA`:</span><span class="sxs-lookup"><span data-stu-id="6fe94-281">The example below shows a `Student` type, derived from the `Person` type, and an extension method that deconstructs a `Student` into three variables, representing the `FirstName`, the `LastName`, and the `GPA`:</span></span>

[!code-csharp[ExtensionDeconstructMethod](../../samples/snippets/csharp/tuples/tuples/person.cs#13_ExtensionDeconstructMethod "Type with a deconstruct extension method")]

<span data-ttu-id="6fe94-282">A `Student` objektu má teď dostupné dvě `Deconstruct` metody: metodu rozšíření deklarovat pro `Student` typů a členů z `Person` typu.</span><span class="sxs-lookup"><span data-stu-id="6fe94-282">A `Student` object now has two accessible `Deconstruct` methods: the extension method declared for `Student` types, and the member of the `Person` type.</span></span> <span data-ttu-id="6fe94-283">Obě jsou v oboru a, která umožňuje `Student` k možné dekonstruovat na proměnné dvě nebo tři.</span><span class="sxs-lookup"><span data-stu-id="6fe94-283">Both are in scope, and that enables a `Student` to be deconstructed into either two variables or three.</span></span>
<span data-ttu-id="6fe94-284">Pokud přiřadíte student tří proměnných, křestní jméno, poslední název a GPA jsou všechny vrácené.</span><span class="sxs-lookup"><span data-stu-id="6fe94-284">If you assign a student to three variables, the first name, last name, and GPA are all returned.</span></span> <span data-ttu-id="6fe94-285">Pokud přiřadíte student dvě proměnné, budou vráceny pouze křestní jméno a příjmení.</span><span class="sxs-lookup"><span data-stu-id="6fe94-285">If you assign a student to two variables, only the first name and the last name are returned.</span></span>

[!code-csharp[Deconstruct extension method](../../samples/snippets/csharp/tuples/tuples/program.cs#13A_DeconstructExtension "Deconstruct a class type using an extension method")]

<span data-ttu-id="6fe94-286">Měli byste být opatrní definování více `Deconstruct` metody ve třídě nebo hierarchie tříd.</span><span class="sxs-lookup"><span data-stu-id="6fe94-286">You should be careful defining multiple `Deconstruct` methods in a class or a class hierarchy.</span></span> <span data-ttu-id="6fe94-287">Více `Deconstruct` metody, které mají stejný počet `out` parametry rychle mohou způsobit nejednoznačnost.</span><span class="sxs-lookup"><span data-stu-id="6fe94-287">Multiple `Deconstruct` methods that have the same number of `out` parameters can quickly cause ambiguities.</span></span> <span data-ttu-id="6fe94-288">Volající není možné snadno volání požadovaný `Deconstruct` metody.</span><span class="sxs-lookup"><span data-stu-id="6fe94-288">Callers may not be able to easily call the desired `Deconstruct` method.</span></span>

<span data-ttu-id="6fe94-289">V tomto příkladu, existuje minimální pravděpodobnost pro nejednoznačné volání protože `Deconstruct` metodu `Person` má dvě výstupních parametrů a `Deconstruct` metodu pro `Student` má tři.</span><span class="sxs-lookup"><span data-stu-id="6fe94-289">In this example, there is minimal chance for an ambiguous call because the `Deconstruct` method for `Person` has two output parameters, and the `Deconstruct` method for `Student` has three.</span></span>

<span data-ttu-id="6fe94-290">Operátory dekonstrukce nejsou součástí testování rovnosti.</span><span class="sxs-lookup"><span data-stu-id="6fe94-290">Deconstruction operators do not participate in testing equality.</span></span> <span data-ttu-id="6fe94-291">Následující příklad vygeneruje Chyba kompilátoru CS0019:</span><span class="sxs-lookup"><span data-stu-id="6fe94-291">The following example generates compiler error CS0019:</span></span>

```csharp
Person p = new Person("Althea", "Goodwin");
if (("Althea", "Goodwin") == p)
    Console.WriteLine(p);
```

<span data-ttu-id="6fe94-292">`Deconstruct` Metodu se nepovedlo převést `Person` objektu `p` pro n-tici obsahující dva řetězce, ale se nedá použít v kontextu rovnosti testy.</span><span class="sxs-lookup"><span data-stu-id="6fe94-292">The `Deconstruct` method could convert the `Person` object `p` to a tuple containing two strings, but it is not applicable in the context of equality tests.</span></span>

## <a name="conclusion"></a><span data-ttu-id="6fe94-293">Závěr</span><span class="sxs-lookup"><span data-stu-id="6fe94-293">Conclusion</span></span> 

<span data-ttu-id="6fe94-294">Novou podporu jazyka a knihovny pro pojmenované řazených kolekcí členů výrazně usnadňuje práci s návrhy, které používají datové struktury, ukládání více prvků, které nedefinují chování, stejně jako třídy a struktury.</span><span class="sxs-lookup"><span data-stu-id="6fe94-294">The new language and library support for named tuples makes it much easier to work with designs that use data structures that store multiple elements but do not define behavior, as classes and structs do.</span></span> <span data-ttu-id="6fe94-295">Je snadné a stručné použijte pro tyto typy řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-295">It's easy and concise to use tuples for those types.</span></span> <span data-ttu-id="6fe94-296">Získáte všechny výhody statický typ kontroly, aniž byste museli vytváření typů pomocí více podrobné `class` nebo `struct` syntaxe.</span><span class="sxs-lookup"><span data-stu-id="6fe94-296">You get all the benefits of static type checking, without needing to author types using the more verbose `class` or `struct` syntax.</span></span> <span data-ttu-id="6fe94-297">I tak jsou nejvhodnější pro pomocné metody, které jsou `private`, nebo `internal`.</span><span class="sxs-lookup"><span data-stu-id="6fe94-297">Even so, they are most useful for utility methods that are `private`, or `internal`.</span></span> <span data-ttu-id="6fe94-298">Vytvoření uživatelem definovaných typů, buď `class` nebo `struct` zadává při veřejné metody vrací hodnotu, která má více elementů.</span><span class="sxs-lookup"><span data-stu-id="6fe94-298">Create user-defined types, either `class` or `struct` types when your public methods return a value that has multiple elements.</span></span>
