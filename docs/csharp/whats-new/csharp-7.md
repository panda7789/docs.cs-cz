---
title: Co je nového v C# 7.0 - C# Průvodce
description: Získejte přehled o nových funkcích ve verzi 7.0 jazyka C#.
ms.date: 02/20/2019
ms.assetid: fd41596d-d0c2-4816-b94d-c4d00a5d0243
ms.openlocfilehash: a6ac5c00ceb2ce8e5e56e2a86a8cde937d5108e2
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/14/2020
ms.locfileid: "79399691"
---
# <a name="whats-new-in-c-70"></a><span data-ttu-id="4b2e3-103">Co je nového v jazyce C# 7.0</span><span class="sxs-lookup"><span data-stu-id="4b2e3-103">What's new in C# 7.0</span></span>

<span data-ttu-id="4b2e3-104">C# 7.0 přidá do jazyka C# řadu nových funkcí:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-104">C# 7.0 adds a number of new features to the C# language:</span></span>

- [<span data-ttu-id="4b2e3-105">`out`Proměnné</span><span class="sxs-lookup"><span data-stu-id="4b2e3-105">`out` variables</span></span>](#out-variables)
  - <span data-ttu-id="4b2e3-106">Hodnoty vuvedené jako argumenty můžete deklarovat `out` jako argumenty pro metodu, kde se používají.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-106">You can declare `out` values inline as arguments to the method where they're used.</span></span>
- [<span data-ttu-id="4b2e3-107">Záznamů</span><span class="sxs-lookup"><span data-stu-id="4b2e3-107">Tuples</span></span>](#tuples)
  - <span data-ttu-id="4b2e3-108">Můžete vytvořit zjednodušené, nepojmenované typy, které obsahují více veřejných polí.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-108">You can create lightweight, unnamed types that contain multiple public fields.</span></span> <span data-ttu-id="4b2e3-109">Kompilátory a nástroje IDE pochopit sémantiku těchto typů.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-109">Compilers and IDE tools understand the semantics of these types.</span></span>
- [<span data-ttu-id="4b2e3-110">Zahození</span><span class="sxs-lookup"><span data-stu-id="4b2e3-110">Discards</span></span>](#discards)
  - <span data-ttu-id="4b2e3-111">Zahození jsou dočasné proměnné pouze pro zápis používané v přiřazeních, pokud se nestaráte o přiřazenou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-111">Discards are temporary, write-only variables used in assignments when you don't care about the value assigned.</span></span> <span data-ttu-id="4b2e3-112">Jsou nejužitečnější při deconstructing řazené kolekce členů a uživatelem definované typy, stejně jako při volání metody s `out` parametry.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-112">They're most useful when deconstructing tuples and user-defined types, as well as when calling methods with `out` parameters.</span></span>
- [<span data-ttu-id="4b2e3-113">Porovnávání</span><span class="sxs-lookup"><span data-stu-id="4b2e3-113">Pattern Matching</span></span>](#pattern-matching)
  - <span data-ttu-id="4b2e3-114">Můžete vytvořit logiku větvení na základě libovolných typů a hodnot členů těchto typů.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-114">You can create branching logic based on arbitrary types and values of the members of those types.</span></span>
- [<span data-ttu-id="4b2e3-115">`ref`místní obyvatelé a vrací</span><span class="sxs-lookup"><span data-stu-id="4b2e3-115">`ref` locals and returns</span></span>](#ref-locals-and-returns)
  - <span data-ttu-id="4b2e3-116">Metoda místní proměnné a vrácené hodnoty mohou být odkazy na jiné úložiště.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-116">Method local variables and return values can be references to other storage.</span></span>
- [<span data-ttu-id="4b2e3-117">Místní funkce</span><span class="sxs-lookup"><span data-stu-id="4b2e3-117">Local Functions</span></span>](#local-functions)
  - <span data-ttu-id="4b2e3-118">Můžete vnořit funkce uvnitř jiných funkcí omezit jejich rozsah a viditelnost.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-118">You can nest functions inside other functions to limit their scope and visibility.</span></span>
- [<span data-ttu-id="4b2e3-119">Více členů s výrazovým tělem</span><span class="sxs-lookup"><span data-stu-id="4b2e3-119">More expression-bodied members</span></span>](#more-expression-bodied-members)
  - <span data-ttu-id="4b2e3-120">Seznam členů, které lze vytvářet pomocí výrazů, se rozrostl.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-120">The list of members that can be authored using expressions has grown.</span></span>
- [<span data-ttu-id="4b2e3-121">`throw`Výrazy</span><span class="sxs-lookup"><span data-stu-id="4b2e3-121">`throw` Expressions</span></span>](#throw-expressions)
  - <span data-ttu-id="4b2e3-122">Můžete vyvolat výjimky v konstrukcích kódu, které `throw` dříve nebyly povoleny, protože byl příkaz.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-122">You can throw exceptions in code constructs that previously weren't allowed because `throw` was a statement.</span></span>
- [<span data-ttu-id="4b2e3-123">Generalizované asynchronní návratové typy</span><span class="sxs-lookup"><span data-stu-id="4b2e3-123">Generalized async return types</span></span>](#generalized-async-return-types)
  - <span data-ttu-id="4b2e3-124">Metody deklarované pomocí modifikátoru `async` mohou kromě `Task` a `Task<T>`.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-124">Methods declared with the `async` modifier can return other types in addition to `Task` and `Task<T>`.</span></span>
- [<span data-ttu-id="4b2e3-125">Číselná vylepšení syntaxe literálu</span><span class="sxs-lookup"><span data-stu-id="4b2e3-125">Numeric literal syntax improvements</span></span>](#numeric-literal-syntax-improvements)
  - <span data-ttu-id="4b2e3-126">Nové tokeny zlepšují čitelnost pro číselné konstanty.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-126">New tokens improve readability for numeric constants.</span></span>

<span data-ttu-id="4b2e3-127">Zbývající část tohoto článku obsahuje přehled jednotlivých funkcí.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-127">The remainder of this article provides an overview of each feature.</span></span> <span data-ttu-id="4b2e3-128">U každé funkce se dozvíte důvody, které za ní stojí.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-128">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="4b2e3-129">Naučíte se syntaxi.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-129">You'll learn the syntax.</span></span> <span data-ttu-id="4b2e3-130">Tyto funkce můžete prozkoumat ve `dotnet try` vašem prostředí pomocí globálního nástroje:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-130">You can explore these features in your environment using the `dotnet try` global tool:</span></span>

1. <span data-ttu-id="4b2e3-131">Nainstalujte globální nástroj [dotnet-try.](https://github.com/dotnet/try/blob/master/README.md#setup)</span><span class="sxs-lookup"><span data-stu-id="4b2e3-131">Install the [dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) global tool.</span></span>
1. <span data-ttu-id="4b2e3-132">Klonovat úložiště [dotnet/try-samples.](https://github.com/dotnet/try-samples)</span><span class="sxs-lookup"><span data-stu-id="4b2e3-132">Clone the [dotnet/try-samples](https://github.com/dotnet/try-samples) repository.</span></span>
1. <span data-ttu-id="4b2e3-133">Nastavte aktuální adresář do podadresáře *csharp7* pro úložiště *try-samples.*</span><span class="sxs-lookup"><span data-stu-id="4b2e3-133">Set the current directory to the *csharp7* subdirectory for the *try-samples* repository.</span></span>
1. <span data-ttu-id="4b2e3-134">Spusťte `dotnet try`.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-134">Run `dotnet try`.</span></span>

## <a name="out-variables"></a><span data-ttu-id="4b2e3-135">`out`Proměnné</span><span class="sxs-lookup"><span data-stu-id="4b2e3-135">`out` variables</span></span>

<span data-ttu-id="4b2e3-136">V této verzi `out` byla vylepšena existující syntaxe, která podporuje parametry.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-136">The existing syntax that supports `out` parameters has been improved in this version.</span></span> <span data-ttu-id="4b2e3-137">Nyní můžete `out` deklarovat proměnné v seznamu argumentů volání metody, spíše než psát prohlášení samostatné deklarace:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-137">You can now declare `out` variables in the argument list of a method call, rather than writing a separate declaration statement:</span></span>

[!code-csharp[OutVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVariableDeclarations "Out variable declarations")]

<span data-ttu-id="4b2e3-138">Můžete chtít zadat typ `out` proměnné pro přehlednost, jak je znázorněno výše.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-138">You may want to specify the type of the `out` variable for clarity, as shown above.</span></span> <span data-ttu-id="4b2e3-139">Jazyk však podporuje použití implicitně zadané místní proměnné:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-139">However, the language does support using an implicitly typed local variable:</span></span>

[!code-csharp[OutVarVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVarVariableDeclarations "Implicitly typed Out variable")]

- <span data-ttu-id="4b2e3-140">Kód je čitelnější.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-140">The code is easier to read.</span></span>
  - <span data-ttu-id="4b2e3-141">Deklarujete out proměnnou tam, kde ji používáte, nikoli na jiném výše uvedeném řádku.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-141">You declare the out variable where you use it, not on another line above.</span></span>
- <span data-ttu-id="4b2e3-142">Není třeba přiřazovat počáteční hodnotu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-142">No need to assign an initial value.</span></span>
  - <span data-ttu-id="4b2e3-143">Deklarováním `out` proměnné, kde se používá ve volání metody, nelze omylem použít před přiřazením.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-143">By declaring the `out` variable where it's used in a method call, you can't accidentally use it before it is assigned.</span></span>

## <a name="tuples"></a><span data-ttu-id="4b2e3-144">N-tice</span><span class="sxs-lookup"><span data-stu-id="4b2e3-144">Tuples</span></span>

<span data-ttu-id="4b2e3-145">C# poskytuje bohatou syntaxi pro třídy a struktury, který se používá k vysvětlení záměru návrhu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-145">C# provides a rich syntax for classes and structs that is used to explain your design intent.</span></span> <span data-ttu-id="4b2e3-146">Ale někdy, že bohatá syntaxe vyžaduje další práci s minimálním přínosem.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-146">But sometimes that rich syntax requires extra work with minimal benefit.</span></span> <span data-ttu-id="4b2e3-147">Často může psát metody, které potřebují jednoduchou strukturu obsahující více než jeden datový prvek.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-147">You may often write methods that need a simple structure containing more than one data element.</span></span> <span data-ttu-id="4b2e3-148">Pro podporu těchto scénářů byly přidány *řazené kolekce členů* do jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-148">To support these scenarios *tuples* were added to C#.</span></span> <span data-ttu-id="4b2e3-149">Řazené kolekce členů jsou zjednodušené datové struktury, které obsahují více polí představujících datové členy.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-149">Tuples are lightweight data structures that contain multiple fields to represent the data members.</span></span>
<span data-ttu-id="4b2e3-150">Pole nejsou ověřena a nelze definovat vlastní metody.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-150">The fields aren't validated, and you can't define your own methods</span></span>

> [!NOTE]
> <span data-ttu-id="4b2e3-151">Řazené kolekce členů byly k dispozici před C# 7.0, ale byly neefektivní a neměl žádnou jazykovou podporu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-151">Tuples were available before C# 7.0, but they were inefficient and had no language support.</span></span>
> <span data-ttu-id="4b2e3-152">To znamenalo, že prvky řazené kolekce členů lze odkazovat pouze jako `Item1`, `Item2` a tak dále.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-152">This meant that tuple elements could only be referenced as `Item1`, `Item2` and so on.</span></span> <span data-ttu-id="4b2e3-153">C# 7.0 zavádí jazykovou podporu pro řazené kolekce členů, který umožňuje sémantické názvy pro pole řazené kolekce členů pomocí nové, efektivnější řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-153">C# 7.0 introduces language support for tuples, which enables semantic names for the fields of a tuple using new, more efficient tuple types.</span></span>

<span data-ttu-id="4b2e3-154">Řazenou skupinu členů můžete vytvořit přiřazením hodnoty každému členovi a volitelně poskytnutím sémantických názvů každému členovi řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-154">You can create a tuple by assigning a value to each member, and optionally providing semantic names to each of the members of the tuple:</span></span>

[!code-csharp[NamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#NamedTuple "Named tuple")]

<span data-ttu-id="4b2e3-155">Řazená `namedLetters` kolekce členů `Alpha` `Beta`obsahuje pole označovaná jako a .</span><span class="sxs-lookup"><span data-stu-id="4b2e3-155">The `namedLetters` tuple contains fields referred to as `Alpha` and `Beta`.</span></span> <span data-ttu-id="4b2e3-156">Tyto názvy existují pouze v době kompilace a nejsou zachovány, například při kontrole řazené kolekce členů pomocí reflexe za běhu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-156">Those names exist only at compile time and aren't preserved, for example when inspecting the tuple using reflection at runtime.</span></span>

<span data-ttu-id="4b2e3-157">V přiřazení řazené kolekce členů můžete také zadat názvy polí na pravé straně přiřazení:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-157">In a tuple assignment, you can also specify the names of the fields on the right-hand side of the assignment:</span></span>

[!code-csharp[ImplicitNamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#ImplicitNamedTuple "Implicitly named tuple")]

<span data-ttu-id="4b2e3-158">Může být časy, kdy chcete zrušit balíček členy řazené kolekce členů, které byly vráceny z metody.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-158">There may be times when you want to unpackage the members of a tuple that were returned from a method.</span></span>  <span data-ttu-id="4b2e3-159">Můžete to provést deklarováním samostatných proměnných pro každou z hodnot v řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-159">You can do that by declaring separate variables for each of the values in the tuple.</span></span> <span data-ttu-id="4b2e3-160">Toto odbalování se nazývá *deconstructing* řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-160">This unpackaging is called *deconstructing* the tuple:</span></span>

[!code-csharp[CallingWithDeconstructor](~/samples/snippets/csharp/new-in-7/program.cs#CallingWithDeconstructor "Deconstructing a tuple")]

<span data-ttu-id="4b2e3-161">Můžete také poskytnout podobnou dekonstrukci pro libovolný typ v rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-161">You can also provide a similar deconstruction for any type in .NET.</span></span> <span data-ttu-id="4b2e3-162">Napíšete `Deconstruct` metodu jako člen třídy.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-162">You write a `Deconstruct` method as a member of the class.</span></span> <span data-ttu-id="4b2e3-163">Tato `Deconstruct` metoda poskytuje `out` sadu argumentů pro každou z vlastností, které chcete extrahovat.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-163">That `Deconstruct` method provides a set of `out` arguments for each of the properties you want to extract.</span></span> <span data-ttu-id="4b2e3-164">Zvažte `Point` tuto třídu, která poskytuje deconstructor metoda, která extrahuje `X` a `Y` souřadnice:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-164">Consider this `Point` class that provides a deconstructor method that extracts the `X` and `Y` coordinates:</span></span>

[!code-csharp[PointWithDeconstruction](~/samples/snippets/csharp/new-in-7/point.cs#PointWithDeconstruction "Point with deconstruction method")]

<span data-ttu-id="4b2e3-165">Jednotlivá pole můžete extrahovat `Point` přiřazením řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-165">You can extract the individual fields by assigning a `Point` to a tuple:</span></span>

[!code-csharp[DeconstructPoint](~/samples/snippets/csharp/new-in-7/program.cs#DeconstructPoint "Deconstruct a point")]

<span data-ttu-id="4b2e3-166">Další informace o řazených kolekcích členů se dozvíte podrobněji v [článku n-tic](../tuples.md).</span><span class="sxs-lookup"><span data-stu-id="4b2e3-166">You can learn more in depth about tuples in the [tuples article](../tuples.md).</span></span>

## <a name="discards"></a><span data-ttu-id="4b2e3-167">Zahození</span><span class="sxs-lookup"><span data-stu-id="4b2e3-167">Discards</span></span>

<span data-ttu-id="4b2e3-168">Často při deconstructing řazené `out` kolekce členů nebo volání metody s parametry, jste nuceni definovat proměnnou, jejíž hodnotu nezajímáte a nemají v úmyslu použít.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-168">Often when deconstructing a tuple or calling a method with `out` parameters, you're forced to define a variable whose value you don't care about and don't intend to use.</span></span> <span data-ttu-id="4b2e3-169">C# přidá podporu pro *zahození* pro zpracování tohoto scénáře.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-169">C# adds support for *discards* to handle this scenario.</span></span> <span data-ttu-id="4b2e3-170">Zahození je proměnná pouze `_` pro zápis, jejíž název je (znak podtržítka); můžete přiřadit všechny hodnoty, které chcete zahodit k jedné proměnné.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-170">A discard is a write-only variable whose name is `_` (the underscore character); you can assign all of the values that you intend to discard to the single variable.</span></span> <span data-ttu-id="4b2e3-171">Výmět je jako nepřiřazená proměnná; kromě příkazu přiřazení nelze zahodit použít v kódu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-171">A discard is like an unassigned variable; apart from the assignment statement, the discard can't be used in code.</span></span>

<span data-ttu-id="4b2e3-172">Zahození jsou podporovány v následujících scénářích:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-172">Discards are supported in the following scenarios:</span></span>

- <span data-ttu-id="4b2e3-173">Při dekonstrukci řazené kolekce členů nebo uživatelem definované typy.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-173">When deconstructing tuples or user-defined types.</span></span>
- <span data-ttu-id="4b2e3-174">Při volání metod s [out](../language-reference/keywords/out-parameter-modifier.md) parametry.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-174">When calling methods with [out](../language-reference/keywords/out-parameter-modifier.md) parameters.</span></span>
- <span data-ttu-id="4b2e3-175">V operaci porovnávání vzorů s příkazy [is](../language-reference/keywords/is.md) a [switch.](../language-reference/keywords/switch.md)</span><span class="sxs-lookup"><span data-stu-id="4b2e3-175">In a pattern matching operation with the [is](../language-reference/keywords/is.md) and [switch](../language-reference/keywords/switch.md) statements.</span></span>
- <span data-ttu-id="4b2e3-176">Jako samostatný identifikátor, pokud chcete explicitně identifikovat hodnotu přiřazení jako zahození.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-176">As a standalone identifier when you want to explicitly identify the value of an assignment as a discard.</span></span>

<span data-ttu-id="4b2e3-177">Následující příklad definuje `QueryCityDataForYears` metodu, která vrací 6 řazené kolekce členů, která obsahuje data pro město pro dva různé roky.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-177">The following example defines a `QueryCityDataForYears` method that returns a 6-tuple that contains data for a city for two different years.</span></span> <span data-ttu-id="4b2e3-178">Volání metody v příkladu se týká pouze dvě hodnoty základního souboru vrácené metodou a tak zachází zbývající hodnoty v řazené kolekce členů jako zahodí při deconstructs řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-178">The method call in the example is concerned only with the two population values returned by the method and so treats the remaining values in the tuple as discards when it deconstructs the tuple.</span></span>

[!code-csharp[Tuple-discard](~/samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="4b2e3-179">Další informace naleznete [v tématu Discards](../discards.md).</span><span class="sxs-lookup"><span data-stu-id="4b2e3-179">For more information, see [Discards](../discards.md).</span></span>

## <a name="pattern-matching"></a><span data-ttu-id="4b2e3-180">Porovnávání vzorů</span><span class="sxs-lookup"><span data-stu-id="4b2e3-180">Pattern matching</span></span>

<span data-ttu-id="4b2e3-181">*Porovnávání vzorů* je funkce, která umožňuje implementovat odeslání metody na vlastnosti než typ objektu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-181">*Pattern matching* is a feature that allows you to implement method dispatch on properties other than the type of an object.</span></span> <span data-ttu-id="4b2e3-182">Pravděpodobně jste již obeznámeni s odesláním metody na základě typu objektu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-182">You're probably already familiar with method dispatch based on the type of an object.</span></span> <span data-ttu-id="4b2e3-183">V objektově orientovaném programování poskytují metody virtual a override syntaxi pro implementaci dispečerské metody založené na typu objektu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-183">In object-oriented programming, virtual and override methods provide language syntax to implement method dispatching based on an object's type.</span></span> <span data-ttu-id="4b2e3-184">Základní a odvozené třídy poskytují různé implementace.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-184">Base and Derived classes provide different implementations.</span></span>
<span data-ttu-id="4b2e3-185">Vzor odpovídající výrazy rozšířit tento koncept tak, aby můžete snadno implementovat podobné vzorky odeslání pro typy a datové prvky, které nesouvisejí prostřednictvím hierarchie dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-185">Pattern matching expressions extend this concept so that you can easily implement similar dispatch patterns for types and data elements that aren't related through an inheritance hierarchy.</span></span>

<span data-ttu-id="4b2e3-186">Porovnávání `is` vzorů podporuje `switch` výrazy a výrazy.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-186">Pattern matching supports `is` expressions and `switch` expressions.</span></span> <span data-ttu-id="4b2e3-187">Každý umožňuje kontrolu objektu a jeho vlastnosti k určení, pokud tento objekt splňuje požadovaný vzor.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-187">Each enables inspecting an object and its properties to determine if that object satisfies the sought pattern.</span></span> <span data-ttu-id="4b2e3-188">`when` Klíčové slovo slouží k určení dalších pravidel vzoru.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-188">You use the `when` keyword to specify additional rules to the pattern.</span></span>

<span data-ttu-id="4b2e3-189">Výraz `is` vzoru rozšiřuje známý [ `is` operátor](../language-reference/keywords/is.md#pattern-matching-with-is) k dotazování objektu o jeho typu a přiřazení výsledku v jedné instrukci.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-189">The `is` pattern expression extends the familiar [`is` operator](../language-reference/keywords/is.md#pattern-matching-with-is) to query an object about its type and assign the result in one instruction.</span></span> <span data-ttu-id="4b2e3-190">Následující kód zkontroluje, zda `int`je proměnná , a pokud ano, přidá ji k aktuálnímu součtu:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-190">The following code checks if a variable is an `int`, and if so, adds it to the current sum:</span></span>

```csharp
if (input is int count)
    sum += count;
```

<span data-ttu-id="4b2e3-191">Předchozí malý příklad ukazuje vylepšení výrazu. `is`</span><span class="sxs-lookup"><span data-stu-id="4b2e3-191">The preceding small example demonstrates the enhancements to the `is` expression.</span></span> <span data-ttu-id="4b2e3-192">Můžete testovat proti typy hodnot, jakož i typy odkazů a můžete přiřadit úspěšný výsledek nové proměnné správného typu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-192">You can test against value types as well as reference types, and you can assign the successful result to a new variable of the correct type.</span></span>

<span data-ttu-id="4b2e3-193">Výraz shody přepínače má známou `switch` syntaxi založenou na příkazu, který je již součástí jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-193">The switch match expression has a familiar syntax, based on the `switch` statement already part of the C# language.</span></span> <span data-ttu-id="4b2e3-194">Příkaz aktualizovaný přepínač má několik nových konstrukcí:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-194">The updated switch statement has several new constructs:</span></span>

- <span data-ttu-id="4b2e3-195">Řídící typ výrazu `switch` již není omezen na `Enum` integrální typy, `string`typy nebo typ s možnou hodnotou null odpovídající jednomu z těchto typů.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-195">The governing type of a `switch` expression is no longer restricted to integral types, `Enum` types, `string`, or a nullable type corresponding to one of those types.</span></span> <span data-ttu-id="4b2e3-196">Lze použít libovolný typ.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-196">Any type may be used.</span></span>
- <span data-ttu-id="4b2e3-197">Můžete otestovat typ `switch` výrazu `case` v každém popisku.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-197">You can test the type of the `switch` expression in each `case` label.</span></span> <span data-ttu-id="4b2e3-198">Stejně `is` jako u výrazu můžete tomuto typu přiřadit novou proměnnou.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-198">As with the `is` expression, you may assign a new variable to that type.</span></span>
- <span data-ttu-id="4b2e3-199">Můžete přidat `when` klauzuli k dalšímu testování podmínek na tuto proměnnou.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-199">You may add a `when` clause to further test conditions on that variable.</span></span>
- <span data-ttu-id="4b2e3-200">Pořadí štítků `case` je nyní důležité.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-200">The order of `case` labels is now important.</span></span> <span data-ttu-id="4b2e3-201">První větev, která má odpovídat, je spuštěna; ostatní jsou přeskočeny.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-201">The first branch to match is executed; others are skipped.</span></span>

<span data-ttu-id="4b2e3-202">Následující kód ukazuje tyto nové funkce:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-202">The following code demonstrates these new features:</span></span>

```csharp
public static int SumPositiveNumbers(IEnumerable<object> sequence)
{
    int sum = 0;
    foreach (var i in sequence)
    {
        switch (i)
        {
            case 0:
                break;
            case IEnumerable<int> childSequence:
            {
                foreach(var item in childSequence)
                    sum += (item > 0) ? item : 0;
                break;
            }
            case int n when n > 0:
                sum += n;
                break;
            case null:
                throw new NullReferenceException("Null found in sequence");
            default:
                throw new InvalidOperationException("Unrecognized type");
        }
    }
    return sum;
}
```

- <span data-ttu-id="4b2e3-203">`case 0:`je známý konstantní vzor.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-203">`case 0:` is the familiar constant pattern.</span></span>
- <span data-ttu-id="4b2e3-204">`case IEnumerable<int> childSequence:`je vzor typu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-204">`case IEnumerable<int> childSequence:` is a type pattern.</span></span>
- <span data-ttu-id="4b2e3-205">`case int n when n > 0:`je vzor typu s `when` další podmínkou.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-205">`case int n when n > 0:` is a type pattern with an additional `when` condition.</span></span>
- <span data-ttu-id="4b2e3-206">`case null:`je vzor null.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-206">`case null:` is the null pattern.</span></span>
- <span data-ttu-id="4b2e3-207">`default:`je známé výchozí případ.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-207">`default:` is the familiar default case.</span></span>

<span data-ttu-id="4b2e3-208">Další informace o porovnávání vzorů v [porovnání vzorů v c#](../pattern-matching.md).</span><span class="sxs-lookup"><span data-stu-id="4b2e3-208">You can learn more about pattern matching in [Pattern Matching in C#](../pattern-matching.md).</span></span>

## <a name="ref-locals-and-returns"></a><span data-ttu-id="4b2e3-209">Ref místní obyvatelé a vrací</span><span class="sxs-lookup"><span data-stu-id="4b2e3-209">Ref locals and returns</span></span>

<span data-ttu-id="4b2e3-210">Tato funkce umožňuje algoritmy, které používají a vracejí odkazy na proměnné definované jinde.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-210">This feature enables algorithms that use and return references to variables defined elsewhere.</span></span> <span data-ttu-id="4b2e3-211">Jedním z příkladů je práce s velkými maticemi a nalezení jednoho místa s určitými vlastnostmi.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-211">One example is working with large matrices, and finding a single location with certain characteristics.</span></span> <span data-ttu-id="4b2e3-212">Následující metoda vrátí **odkaz** na toto úložiště v matici:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-212">The following method returns a **reference** to that storage in the matrix:</span></span>

[!code-csharp[FindReturningRef](~/samples/snippets/csharp/new-in-7/MatrixSearch.cs#FindReturningRef "Find returning by reference")]

<span data-ttu-id="4b2e3-213">Můžete deklarovat vrácenou `ref` hodnotu jako a upravit tuto hodnotu v matici, jak je znázorněno v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-213">You can declare the return value as a `ref` and modify that value in the matrix, as shown in the following code:</span></span>

[!code-csharp[AssignRefReturn](~/samples/snippets/csharp/new-in-7/Program.cs#AssignRefReturn "Assign ref return")]

<span data-ttu-id="4b2e3-214">Jazyk C# má několik pravidel, která vás `ref` chrání před zneužitím místních obyvatel a vrátí:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-214">The C# language has several rules that protect you from misusing the `ref` locals and returns:</span></span>

- <span data-ttu-id="4b2e3-215">Klíčové `ref` slovo je nutné přidat k `return` podpisu metody a ke všem příkazům v metodě.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-215">You must add the `ref` keyword to the method signature and to all `return` statements in a method.</span></span>
  - <span data-ttu-id="4b2e3-216">To jasně ukazuje, metoda vrátí odkazem v celé metodě.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-216">That makes it clear the method returns by reference throughout the method.</span></span>
- <span data-ttu-id="4b2e3-217">A `ref return` může být přiřazena k `ref` proměnné hodnoty nebo proměnné.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-217">A `ref return` may be assigned to a value variable, or a `ref` variable.</span></span>
  - <span data-ttu-id="4b2e3-218">Volající určuje, zda je vrácená hodnota zkopírována nebo ne.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-218">The caller controls whether the return value is copied or not.</span></span> <span data-ttu-id="4b2e3-219">Vynechání modifikátor při `ref` přiřazování vrácená hodnota označuje, že volající chce kopii hodnoty, nikoli odkaz na úložiště.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-219">Omitting the `ref` modifier when assigning the return value indicates that the caller wants a copy of the value, not a reference to the storage.</span></span>
- <span data-ttu-id="4b2e3-220">Standardní metodu nelze přiřadit `ref` k místní proměnné.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-220">You can't assign a standard method return value to a `ref` local variable.</span></span>
  - <span data-ttu-id="4b2e3-221">Že zamítá prohlášení jako`ref int i = sequence.Count();`</span><span class="sxs-lookup"><span data-stu-id="4b2e3-221">That disallows statements like `ref int i = sequence.Count();`</span></span>
- <span data-ttu-id="4b2e3-222">Nelze vrátit `ref` proměnné, jejíž životnost nepřesahuje provádění metody.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-222">You can't return a `ref` to a variable whose lifetime doesn't extend beyond the execution of the method.</span></span>
  - <span data-ttu-id="4b2e3-223">To znamená, že nelze vrátit odkaz na místní proměnnou nebo proměnnou s podobným rozsahem.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-223">That means you can't return a reference to a local variable or a variable with a similar scope.</span></span>
- <span data-ttu-id="4b2e3-224">`ref`místní a vrácení nelze použít s asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-224">`ref` locals and returns can't be used with async methods.</span></span>
  - <span data-ttu-id="4b2e3-225">Kompilátor nemůže vědět, pokud byla odkazovaná proměnná nastavena na konečnou hodnotu, když se vrátí asynchronní metoda.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-225">The compiler can't know if the referenced variable has been set to its final value when the async method returns.</span></span>

<span data-ttu-id="4b2e3-226">Přidání ref locals a ref vrátí umožňuje algoritmy, které jsou efektivnější tím, že zabrání kopírování hodnot nebo provádění dereferencing operace vícekrát.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-226">The addition of ref locals and ref returns enables algorithms that are more efficient by avoiding copying values, or performing dereferencing operations multiple times.</span></span>

<span data-ttu-id="4b2e3-227">Přidání `ref` k vrácené hodnotě je [změna kompatibilní se zdrojem](version-update-considerations.md#source-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="4b2e3-227">Adding `ref` to the return value is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span> <span data-ttu-id="4b2e3-228">Existující kód se zkompiluje, ale vrácená hodnota ref je zkopírována při přiřazení.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-228">Existing code compiles, but the ref return value is copied when assigned.</span></span> <span data-ttu-id="4b2e3-229">Volající musí aktualizovat úložiště pro vrácenou `ref` hodnotu na místní proměnnou, aby bylo vráceno jako odkaz.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-229">Callers must update the storage for the return value to a `ref` local variable to store the return as a reference.</span></span>

<span data-ttu-id="4b2e3-230">Další informace naleznete v článku [klíčového slova ref.](../language-reference/keywords/ref.md)</span><span class="sxs-lookup"><span data-stu-id="4b2e3-230">For more information, see the [ref keyword](../language-reference/keywords/ref.md) article.</span></span>

## <a name="local-functions"></a><span data-ttu-id="4b2e3-231">Lokální funkce</span><span class="sxs-lookup"><span data-stu-id="4b2e3-231">Local functions</span></span>

<span data-ttu-id="4b2e3-232">Mnoho návrhů pro třídy obsahuje metody, které jsou volány pouze z jednoho umístění.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-232">Many designs for classes include methods that are called from only one location.</span></span> <span data-ttu-id="4b2e3-233">Tyto další soukromé metody udržují každou metodu malou a zaměřenou.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-233">These additional private methods keep each method small and focused.</span></span> <span data-ttu-id="4b2e3-234">*Místní funkce* umožňují deklarovat metody v kontextu jiné metody.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-234">*Local functions* enable you to declare methods inside the context of another method.</span></span> <span data-ttu-id="4b2e3-235">Místní funkce usnadňují čtenářům třídy vidět, že místní metoda je volána pouze z kontextu, ve kterém je deklarována.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-235">Local functions make it easier for readers of the class to see that the local method is only called from the context in which it is declared.</span></span>

<span data-ttu-id="4b2e3-236">Existují dva běžné případy použití pro místní funkce: metody veřejného iterátoru a veřejné asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-236">There are two common use cases for local functions: public iterator methods and public async methods.</span></span> <span data-ttu-id="4b2e3-237">Oba typy metod generují kód, který hlásí chyby později, než mohou programátoři očekávat.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-237">Both types of methods generate code that reports errors later than programmers might expect.</span></span> <span data-ttu-id="4b2e3-238">V metodách iterátoru jsou všechny výjimky pozorovány pouze při volání kódu, který vyjmenovává vrácenou sekvenci.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-238">In iterator methods, any exceptions are observed only when calling code that enumerates the returned sequence.</span></span> <span data-ttu-id="4b2e3-239">V asynchronních metodách jsou všechny výjimky pozorovány pouze v případě, že je očekáváno vrácené. `Task`</span><span class="sxs-lookup"><span data-stu-id="4b2e3-239">In async methods, any exceptions are only observed when the returned `Task` is awaited.</span></span> <span data-ttu-id="4b2e3-240">Následující příklad ukazuje oddělení ověření parametru od implementace iterátoru pomocí místní funkce:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-240">The following example demonstrates separating parameter validation from the iterator implementation using a local function:</span></span>

[!code-csharp[22_IteratorMethodLocal](~/samples/snippets/csharp/new-in-7/Iterator.cs#IteratorMethodLocal "Iterator method with local function")]

<span data-ttu-id="4b2e3-241">Stejnou techniku lze `async` použít s metodami, které zajišťují, že výjimky vyplývající z ověření argumentu jsou vyvolány před zahájením asynchronní práce:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-241">The same technique can be employed with `async` methods to ensure that exceptions arising from argument validation are thrown before the asynchronous work begins:</span></span>

[!code-csharp[TaskExample](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#TaskExample "Task returning method with local function")]

> [!NOTE]
> <span data-ttu-id="4b2e3-242">Některé návrhy, které jsou podporovány místními funkcemi, lze také provést pomocí *výrazů lambda*.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-242">Some of the designs that are supported by local functions could also be accomplished using *lambda expressions*.</span></span> <span data-ttu-id="4b2e3-243">Další informace naleznete [v tématu Místní funkce vs. lambda výrazy](../local-functions-vs-lambdas.md).</span><span class="sxs-lookup"><span data-stu-id="4b2e3-243">For more information, see [Local functions vs. lambda expressions](../local-functions-vs-lambdas.md).</span></span>

## <a name="more-expression-bodied-members"></a><span data-ttu-id="4b2e3-244">Více členů s výrazovým tělem</span><span class="sxs-lookup"><span data-stu-id="4b2e3-244">More expression-bodied members</span></span>

<span data-ttu-id="4b2e3-245">C# 6 představil [výraz tělo členy](csharp-6.md#expression-bodied-function-members) pro členské funkce a vlastnosti jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-245">C# 6 introduced [expression-bodied members](csharp-6.md#expression-bodied-function-members) for member functions, and read-only properties.</span></span> <span data-ttu-id="4b2e3-246">C# 7.0 rozbalí povolené členy, které mohou být implementovány jako výrazy.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-246">C# 7.0 expands the allowed members that can be implemented as expressions.</span></span> <span data-ttu-id="4b2e3-247">V c# 7.0 můžete implementovat *konstruktory*, `get` `set` *finalizační metody*a přístupové objekty na *vlastnosti* a *indexery*.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-247">In C# 7.0, you can implement *constructors*, *finalizers*, and `get` and `set` accessors on *properties* and *indexers*.</span></span> <span data-ttu-id="4b2e3-248">Následující kód ukazuje příklady každého z nich:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-248">The following code shows examples of each:</span></span>

[!code-csharp[ExpressionBodiedMembers](~/samples/snippets/csharp/new-in-7/expressionmembers.cs#ExpressionBodiedEverything "new expression-bodied members")]

> [!NOTE]
> <span data-ttu-id="4b2e3-249">Tento příklad nepotřebuje finalizační metodu, ale je zobrazen k předvedení syntaxe.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-249">This example does not need a finalizer, but it is shown to demonstrate the syntax.</span></span> <span data-ttu-id="4b2e3-250">Ve své třídě byste neměli implementovat finalizační metodu, pokud není nutné uvolnit nespravované prostředky.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-250">You should not implement a finalizer in your class unless it is necessary to  release unmanaged resources.</span></span> <span data-ttu-id="4b2e3-251">Měli byste také <xref:System.Runtime.InteropServices.SafeHandle> zvážit použití třídy namísto přímé správy nespravovaných prostředků.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-251">You should also consider using the <xref:System.Runtime.InteropServices.SafeHandle> class instead of managing unmanaged resources directly.</span></span>

<span data-ttu-id="4b2e3-252">Tato nová umístění pro členy s výrazem představují důležitý milník pro jazyk C#: Tyto funkce byly implementovány členy komunity pracujícími na projektu [Roslyn](https://github.com/dotnet/Roslyn) s otevřeným zdrojovým kódem.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-252">These new locations for expression-bodied members represent an important milestone for the C# language: These features were implemented by community members working on the open-source [Roslyn](https://github.com/dotnet/Roslyn) project.</span></span>

<span data-ttu-id="4b2e3-253">Změna metody na člen výrazu je [binární kompatibilní změna](version-update-considerations.md#binary-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="4b2e3-253">Changing a method to an expression bodied member is a [binary compatible change](version-update-considerations.md#binary-compatible-changes).</span></span>

## <a name="throw-expressions"></a><span data-ttu-id="4b2e3-254">Výrazy throw</span><span class="sxs-lookup"><span data-stu-id="4b2e3-254">Throw expressions</span></span>

<span data-ttu-id="4b2e3-255">V C#, `throw` byl vždy prohlášení.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-255">In C#, `throw` has always been a statement.</span></span> <span data-ttu-id="4b2e3-256">Protože `throw` je příkaz, nikoli výraz, byly c# konstrukce, kde nelze použít.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-256">Because `throw` is a statement, not an expression, there were C# constructs where you couldn't use it.</span></span> <span data-ttu-id="4b2e3-257">Mezi ně patřily podmíněné výrazy, nulové slučující výrazy a některé výrazy lambda.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-257">These included conditional expressions, null coalescing expressions, and some lambda expressions.</span></span> <span data-ttu-id="4b2e3-258">Přidání členů s výrazem přidá další `throw` umístění, kde výrazy by bylo užitečné.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-258">The addition of expression-bodied members adds more locations where `throw` expressions would be useful.</span></span> <span data-ttu-id="4b2e3-259">Tak, že můžete napsat některý z těchto konstrukcí, C# 7.0 zavádí [*vyvolat výrazy*](../language-reference/keywords/throw.md#the-throw-expression).</span><span class="sxs-lookup"><span data-stu-id="4b2e3-259">So that you can write any of these constructs, C# 7.0 introduces [*throw expressions*](../language-reference/keywords/throw.md#the-throw-expression).</span></span>

<span data-ttu-id="4b2e3-260">Toto přidání usnadňuje psaní kódu založeného na výrazu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-260">This addition makes it easier to write more expression-based code.</span></span> <span data-ttu-id="4b2e3-261">Nepotřebujete další příkazy pro kontrolu chyb.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-261">You don't need additional statements for error checking.</span></span>

## <a name="generalized-async-return-types"></a><span data-ttu-id="4b2e3-262">Generalizované asynchronní návratové typy</span><span class="sxs-lookup"><span data-stu-id="4b2e3-262">Generalized async return types</span></span>

<span data-ttu-id="4b2e3-263">Vrácení objektu `Task` z asynchronních metod může zavést kritické body výkonu v určitých cestách.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-263">Returning a `Task` object from async methods can introduce performance bottlenecks in certain paths.</span></span> <span data-ttu-id="4b2e3-264">`Task`je typ odkazu, takže jeho použití znamená přidělení objektu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-264">`Task` is a reference type, so using it means allocating an object.</span></span> <span data-ttu-id="4b2e3-265">V případech, kdy metoda `async` deklarovaná s modifikátorem vrátí výsledek v mezipaměti nebo dokončí synchronně, může se mimořádná přidělení stát významnými časovými náklady v částech kódu kritických pro výkon.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-265">In cases where a method declared with the `async` modifier returns a cached result, or completes synchronously, the extra allocations can become a significant time cost in performance critical sections of code.</span></span> <span data-ttu-id="4b2e3-266">Může být nákladné, pokud se tyto alokace vyskytují v těsných smyčkách.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-266">It can become costly if those allocations occur in tight loops.</span></span>

<span data-ttu-id="4b2e3-267">Nová funkce jazyka znamená, že návratové typy `Task`asynchronní metody nejsou omezeny na , `Task<T>`a `void`.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-267">The new language feature means that async method return types aren't limited to `Task`, `Task<T>`, and `void`.</span></span> <span data-ttu-id="4b2e3-268">Vrácený typ musí stále splňovat asynchronní vzorek, což znamená, že `GetAwaiter` metoda musí být přístupná.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-268">The returned type must still satisfy the async pattern, meaning a `GetAwaiter` method must be accessible.</span></span> <span data-ttu-id="4b2e3-269">Jako jeden konkrétní `ValueTask` příklad byl typ přidán do rozhraní .NET, aby bylo dosaženo této nové funkce jazyka:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-269">As one concrete example, the `ValueTask` type has been added to .NET to make use of this new language feature:</span></span>

[!code-csharp[UsingValueTask](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#UsingValueTask "Using ValueTask")]

> [!NOTE]
> <span data-ttu-id="4b2e3-270">Je třeba přidat Balíček [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) NuGet, <xref:System.Threading.Tasks.ValueTask%601> aby bylo možné použít typ.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-270">You need to add the NuGet package [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) in order to use the <xref:System.Threading.Tasks.ValueTask%601> type.</span></span>

<span data-ttu-id="4b2e3-271">Toto vylepšení je nejužitečnější pro autory `Task` knihovny, aby se zabránilo přidělení v kritickém výkonu kódu.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-271">This enhancement is most useful for library authors to avoid allocating a `Task` in performance critical code.</span></span>

## <a name="numeric-literal-syntax-improvements"></a><span data-ttu-id="4b2e3-272">Číselná vylepšení syntaxe literálu</span><span class="sxs-lookup"><span data-stu-id="4b2e3-272">Numeric literal syntax improvements</span></span>

<span data-ttu-id="4b2e3-273">Nesprávné čtení číselných konstant může ztížit pochopení kódu při prvním čtení.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-273">Misreading numeric constants can make it harder to understand code when reading it for the first time.</span></span> <span data-ttu-id="4b2e3-274">Bitové masky nebo jiné symbolické hodnoty jsou náchylné k nedorozumění.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-274">Bit masks or other symbolic values are prone to misunderstanding.</span></span> <span data-ttu-id="4b2e3-275">C# 7.0 obsahuje dvě nové funkce pro zápis čísel nejčitelnějším způsobem pro zamýšlené použití: *binární literály*a *oddělovače číslic*.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-275">C# 7.0 includes two new features to write numbers in the most readable fashion for the intended use: *binary literals*, and *digit separators*.</span></span>

<span data-ttu-id="4b2e3-276">Pro ty časy, kdy vytváříte bitové masky nebo kdykoli binární reprezentace čísla vytvoří nejčitelnější kód, napište toto číslo do binárního souboru:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-276">For those times when you're creating bit masks, or whenever a binary representation of a number makes the most readable code, write that number in binary:</span></span>

[!code-csharp[ThousandSeparators](~/samples/snippets/csharp/new-in-7/Program.cs#ThousandSeparators "Thousands separators")]

<span data-ttu-id="4b2e3-277">Na `0b` začátku konstanty označuje, že číslo je zapsáno jako binární číslo.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-277">The `0b` at the beginning of the constant indicates that the number is written as a binary number.</span></span> <span data-ttu-id="4b2e3-278">Binární čísla mohou být dlouhá, takže je často snazší vidět `_` bitové vzory zavedením oddělovače číslic, jak je znázorněno výše v binární konstantě.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-278">Binary numbers can get long, so it's often easier to see the bit patterns by introducing the `_` as a digit separator, as shown above in the binary constant.</span></span> <span data-ttu-id="4b2e3-279">Oddělovač číslic se může objevit kdekoli v konstantě.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-279">The digit separator can appear anywhere in the constant.</span></span> <span data-ttu-id="4b2e3-280">Pro základní 10 čísel, je běžné použít jako oddělovač tisíců:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-280">For base 10 numbers, it is common to use it as a thousands separator:</span></span>

[!code-csharp[LargeIntegers](~/samples/snippets/csharp/new-in-7/Program.cs#LargeIntegers "Large integer")]

<span data-ttu-id="4b2e3-281">Oddělovač číslic lze `decimal`použít `float`také `double` s , a typy:</span><span class="sxs-lookup"><span data-stu-id="4b2e3-281">The digit separator can be used with `decimal`, `float`, and `double` types as well:</span></span>

[!code-csharp[OtherConstants](~/samples/snippets/csharp/new-in-7/Program.cs#OtherConstants "non-integral constants")]

<span data-ttu-id="4b2e3-282">Dohromady můžete deklarovat číselné konstanty s mnohem větší čitelností.</span><span class="sxs-lookup"><span data-stu-id="4b2e3-282">Taken together, you can declare numeric constants with much more readability.</span></span>
