---
title: Co je nového v jazyce C# 7.0 – průvodce v C#
description: Přehled nových funkcí, bude v příštích verzí 7 jazyka C#.
ms.date: 12/21/2016
ms.assetid: fd41596d-d0c2-4816-b94d-c4d00a5d0243
ms.openlocfilehash: a78b30411d734d6dadc52b7dbd402763d4eb7f5e
ms.sourcegitcommit: 88f251b08bf0718ce119f3d7302f514b74895038
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 05/10/2018
---
# <a name="whats-new-in-c-70"></a><span data-ttu-id="aa45b-103">Co je nového v C# 7.0</span><span class="sxs-lookup"><span data-stu-id="aa45b-103">What's new in C# 7.0</span></span>

<span data-ttu-id="aa45b-104">C# 7.0 přidá řadu nových funkcí jazyka C#:</span><span class="sxs-lookup"><span data-stu-id="aa45b-104">C# 7.0 adds a number of new features to the C# language:</span></span>
* [<span data-ttu-id="aa45b-105">`out` proměnné</span><span class="sxs-lookup"><span data-stu-id="aa45b-105">`out` variables</span></span>](#out-variables)
    - <span data-ttu-id="aa45b-106">Je možné deklarovat `out` hodnoty vložené jako argumenty pro metodu, kdy se používá.</span><span class="sxs-lookup"><span data-stu-id="aa45b-106">You can declare `out` values inline as arguments to the method where they are used.</span></span>
* [<span data-ttu-id="aa45b-107">Řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="aa45b-107">Tuples</span></span>](#tuples)
    - <span data-ttu-id="aa45b-108">Můžete vytvořit jednoduché, Nepojmenované typy, které obsahují více veřejná pole.</span><span class="sxs-lookup"><span data-stu-id="aa45b-108">You can create lightweight, unnamed types that contain multiple public fields.</span></span> <span data-ttu-id="aa45b-109">Kompilátory a nástroje IDE pochopit sémantika tyto typy.</span><span class="sxs-lookup"><span data-stu-id="aa45b-109">Compilers and IDE tools understand the semantics of these types.</span></span>
* [<span data-ttu-id="aa45b-110">Zahození</span><span class="sxs-lookup"><span data-stu-id="aa45b-110">Discards</span></span>](#discards)
    - <span data-ttu-id="aa45b-111">Zahození jsou dočasné, jen pro zápis proměnné používané v přiřazení, pokud vám nezáleží hodnotu přiřazenou.</span><span class="sxs-lookup"><span data-stu-id="aa45b-111">Discards are temporary, write-only variables used in assignments when you don't care about the value assigned.</span></span> <span data-ttu-id="aa45b-112">Jsou užitečné, když deconstructing řazených kolekcí členů a uživatelem definované typy, jakož i při volání metody s `out` parametry.</span><span class="sxs-lookup"><span data-stu-id="aa45b-112">They are particularly useful when deconstructing tuples and user-defined types, as well as when calling methods with `out` parameters.</span></span>
* [<span data-ttu-id="aa45b-113">Porovnávání vzorů</span><span class="sxs-lookup"><span data-stu-id="aa45b-113">Pattern Matching</span></span>](#pattern-matching)
    - <span data-ttu-id="aa45b-114">Můžete vytvořit na základě náhodné typy a hodnoty členů tyto typy logiky větvení.</span><span class="sxs-lookup"><span data-stu-id="aa45b-114">You can create branching logic based on arbitrary types and values of the members of those types.</span></span>
* [<span data-ttu-id="aa45b-115">`ref` lokální a vrátí</span><span class="sxs-lookup"><span data-stu-id="aa45b-115">`ref` locals and returns</span></span>](#ref-locals-and-returns)
    - <span data-ttu-id="aa45b-116">Metoda argumentů a místní proměnné může být odkazy na další úložiště.</span><span class="sxs-lookup"><span data-stu-id="aa45b-116">Method arguments and local variables can be references to other storage.</span></span>
* [<span data-ttu-id="aa45b-117">Lokální funkce</span><span class="sxs-lookup"><span data-stu-id="aa45b-117">Local Functions</span></span>](#local-functions)
    - <span data-ttu-id="aa45b-118">Funkce uvnitř jiných funkcí omezit jejich rozsah a viditelnost lze vnořit.</span><span class="sxs-lookup"><span data-stu-id="aa45b-118">You can nest functions inside other functions to limit their scope and visibility.</span></span>
* [<span data-ttu-id="aa45b-119">Další výraz vozidlo členy</span><span class="sxs-lookup"><span data-stu-id="aa45b-119">More expression-bodied members</span></span>](#more-expression-bodied-members)
    - <span data-ttu-id="aa45b-120">Seznam členů, které mohou být vytvořeny pomocí výrazů se zvětšil.</span><span class="sxs-lookup"><span data-stu-id="aa45b-120">The list of members that can be authored using expressions has grown.</span></span>
* [<span data-ttu-id="aa45b-121">`throw` Výrazy</span><span class="sxs-lookup"><span data-stu-id="aa45b-121">`throw` Expressions</span></span>](#throw-expressions)
    - <span data-ttu-id="aa45b-122">Můžete vyvolat výjimky v konstrukce kódu, které předtím nebyly povoleny, protože `throw` byl příkaz.</span><span class="sxs-lookup"><span data-stu-id="aa45b-122">You can throw exceptions in code constructs that previously were not allowed because `throw` was a statement.</span></span> 
* [<span data-ttu-id="aa45b-123">Zobecněný asynchronní návratové typy</span><span class="sxs-lookup"><span data-stu-id="aa45b-123">Generalized async return types</span></span>](#generalized-async-return-types)
    - <span data-ttu-id="aa45b-124">Metody deklarovat s `async` modifikátoru může vrátit jiné typy kromě `Task` a `Task<T>`.</span><span class="sxs-lookup"><span data-stu-id="aa45b-124">Methods declared with the `async` modifier can return other types in addition to `Task` and `Task<T>`.</span></span>
* [<span data-ttu-id="aa45b-125">Vylepšení číselný literál syntaxe</span><span class="sxs-lookup"><span data-stu-id="aa45b-125">Numeric literal syntax improvements</span></span>](#numeric-literal-syntax-improvements)
    - <span data-ttu-id="aa45b-126">Nové tokeny zlepšení čitelnosti pro číselné konstanty.</span><span class="sxs-lookup"><span data-stu-id="aa45b-126">New tokens improve readability for numeric constants.</span></span>

<span data-ttu-id="aa45b-127">Zbývající část tohoto tématu popisuje každou funkci.</span><span class="sxs-lookup"><span data-stu-id="aa45b-127">The remainder of this topic discusses each of the features.</span></span> <span data-ttu-id="aa45b-128">Pro každou funkci dozvíte jejich zdůvodnění.</span><span class="sxs-lookup"><span data-stu-id="aa45b-128">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="aa45b-129">Dozvíte syntaxe.</span><span class="sxs-lookup"><span data-stu-id="aa45b-129">You'll learn the syntax.</span></span> <span data-ttu-id="aa45b-130">Zobrazí se několik ukázkových scénářů, kde pomocí nové funkce mohou zvýšit vaši produktivitu jako vývojář.</span><span class="sxs-lookup"><span data-stu-id="aa45b-130">You'll see some sample scenarios where using the new feature will make you more productive as a developer.</span></span> 

## <a name="out-variables"></a><span data-ttu-id="aa45b-131">`out` proměnné</span><span class="sxs-lookup"><span data-stu-id="aa45b-131">`out` variables</span></span>

<span data-ttu-id="aa45b-132">Existující syntaxi, která podporuje `out` parametry vylepšeno v této verzi.</span><span class="sxs-lookup"><span data-stu-id="aa45b-132">The existing syntax that supports `out` parameters has been improved in this version.</span></span>  

<span data-ttu-id="aa45b-133">Dříve museli byste oddělit deklaraci mimo proměnnou a její inicializace do dvou různých příkazů:</span><span class="sxs-lookup"><span data-stu-id="aa45b-133">Previously, you would need to separate the declaration of the out variable and its initialization into two different statements:</span></span>

[!code-csharp[OutVariableOldStyle](../../../samples/snippets/csharp/new-in-7/program.cs#03_OutVariableOldStyle "classic out variable declaration")]

<span data-ttu-id="aa45b-134">Nyní můžete deklarovat `out` proměnných v seznamu argumentů volání metody místo psaní příkazů samostatné prohlášení:</span><span class="sxs-lookup"><span data-stu-id="aa45b-134">You can now declare `out` variables in the argument list of a method call, rather than writing a separate declaration statement:</span></span>

[!code-csharp[OutVariableDeclarations](../../../samples/snippets/csharp/new-in-7/program.cs#01_OutVariableDeclarations "Out variable declarations")]

<span data-ttu-id="aa45b-135">Můžete chtít zadat typ `out` proměnná pro přehlednost, jak je uvedeno výše.</span><span class="sxs-lookup"><span data-stu-id="aa45b-135">You may want to specify the type of the `out` variable for clarity, as shown above.</span></span> <span data-ttu-id="aa45b-136">Však jazyk podporuje použití implicitně typované lokální proměnnou:</span><span class="sxs-lookup"><span data-stu-id="aa45b-136">However, the language does support using an implicitly typed local variable:</span></span>

[!code-csharp[OutVarVariableDeclarations](../../../samples/snippets/csharp/new-in-7/program.cs#02_OutVarVariableDeclarations "Implicitly typed Out variable")]

* <span data-ttu-id="aa45b-137">Kód je snadnější čtení.</span><span class="sxs-lookup"><span data-stu-id="aa45b-137">The code is easier to read.</span></span> 
    - <span data-ttu-id="aa45b-138">Je deklarovat proměnnou mimo, kde má být použito, není na dalším řádku výše.</span><span class="sxs-lookup"><span data-stu-id="aa45b-138">You declare the out variable where you use it, not on another line above.</span></span>
* <span data-ttu-id="aa45b-139">Není potřeba přiřadit počáteční hodnotu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-139">No need to assign an initial value.</span></span>
    - <span data-ttu-id="aa45b-140">Deklarováním `out` proměnné, kde se používá při volání metody, nemůžete je využít omylem předtím, než je přiřazen.</span><span class="sxs-lookup"><span data-stu-id="aa45b-140">By declaring the `out` variable where it is used in a method call, you can't accidentally use it before it is assigned.</span></span>

<span data-ttu-id="aa45b-141">Nejčastěji používá pro tuto funkci bude `Try` vzor.</span><span class="sxs-lookup"><span data-stu-id="aa45b-141">The most common use for this feature will be the `Try` pattern.</span></span> <span data-ttu-id="aa45b-142">V tomto vzoru, metoda vrátí `bool` označující úspěch nebo neúspěch a `out` proměnné, která poskytuje výsledek, pokud metoda bude úspěšná.</span><span class="sxs-lookup"><span data-stu-id="aa45b-142">In this pattern, a method returns a `bool` indicating success or failure and an `out` variable that provides the result if the method succeeds.</span></span>

<span data-ttu-id="aa45b-143">Při použití `out` deklarace proměnné deklarované proměnná "nevracení" do vnějšího oboru, pokud příkaz.</span><span class="sxs-lookup"><span data-stu-id="aa45b-143">When using the `out` variable declaration, the declared variable "leaks" into the outer scope of the if statement.</span></span> <span data-ttu-id="aa45b-144">To umožňuje používat proměnné později:</span><span class="sxs-lookup"><span data-stu-id="aa45b-144">This allows you to use the variable afterwards:</span></span>

```csharp
if (!int.TryParse(input, out int result))
{    
    return null;
}

return result;
```

## <a name="tuples"></a><span data-ttu-id="aa45b-145">Řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="aa45b-145">Tuples</span></span>

> [!NOTE]
> <span data-ttu-id="aa45b-146">Vyžadovat nové funkce řazených kolekcí členů <xref:System.ValueTuple> typy.</span><span class="sxs-lookup"><span data-stu-id="aa45b-146">The new tuples features require the <xref:System.ValueTuple> types.</span></span>
> <span data-ttu-id="aa45b-147">Je nutné přidat balíček NuGet [ `System.ValueTuple` ](https://www.nuget.org/packages/System.ValueTuple/) odborných na platformách, které neobsahují typy.</span><span class="sxs-lookup"><span data-stu-id="aa45b-147">You must add the NuGet package [`System.ValueTuple`](https://www.nuget.org/packages/System.ValueTuple/) in order to use it on platforms that do not include the types.</span></span>
>
> <span data-ttu-id="aa45b-148">Toto je podobná jiné jazykové funkce, které závisí na typech doručit v rámci.</span><span class="sxs-lookup"><span data-stu-id="aa45b-148">This is similar to other language features that rely on types delivered in the framework.</span></span> <span data-ttu-id="aa45b-149">Příklad zahrnují `async` a `await` spoléhat na `INotifyCompletion` rozhraní a LINQ spoléhat na `IEnumerable<T>`.</span><span class="sxs-lookup"><span data-stu-id="aa45b-149">Example include `async` and `await` relying on the `INotifyCompletion` interface, and LINQ relying on `IEnumerable<T>`.</span></span> <span data-ttu-id="aa45b-150">Jak .NET se stává stále další platformy, které jsou nezávislé, mechanismus doručování mění.</span><span class="sxs-lookup"><span data-stu-id="aa45b-150">However, the delivery mechanism is changing as .NET is becoming more platform independent.</span></span> <span data-ttu-id="aa45b-151">Rozhraní .NET Framework nemusí vždy dodávat na stejné cadence jako kompilátor jazyka.</span><span class="sxs-lookup"><span data-stu-id="aa45b-151">The .NET Framework may not always ship on the same cadence as the language compiler.</span></span> <span data-ttu-id="aa45b-152">Nové jazykové funkce využívají nové typy, tyto typy nebudou mít k dispozici jako balíčků NuGet při odeslání funkce jazyka.</span><span class="sxs-lookup"><span data-stu-id="aa45b-152">When new language features rely on new types, those types will be available as NuGet packages when the language features ship.</span></span> <span data-ttu-id="aa45b-153">Tyto nové typy získat přidávají do rozhraní API .NET standardní a dodávána jako součást rozhraní, požadavek na balíček NuGet se odeberou.</span><span class="sxs-lookup"><span data-stu-id="aa45b-153">As these new types get added to the .NET Standard API and delivered as part of the framework, the NuGet package requirement will be removed.</span></span>

<span data-ttu-id="aa45b-154">C# poskytuje bohaté syntaxe pro tříd a struktur, který používá k popisu vašich představ návrhu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-154">C# provides a rich syntax for classes and structs that is used to explain your design intent.</span></span> <span data-ttu-id="aa45b-155">Ale někdy této bohaté syntaxe vyžaduje další práci s minimálním výhody.</span><span class="sxs-lookup"><span data-stu-id="aa45b-155">But sometimes that rich syntax requires extra work with minimal benefit.</span></span> <span data-ttu-id="aa45b-156">Často může zapisovat metody, které je třeba jednoduchá struktura obsahující více než jeden element data.</span><span class="sxs-lookup"><span data-stu-id="aa45b-156">You may often write methods that need a simple structure containing more than one data element.</span></span> <span data-ttu-id="aa45b-157">Chcete-li tyto scénáře podporovat *řazených kolekcí členů* byly přidány do jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="aa45b-157">To support these scenarios *tuples* were added to C#.</span></span> <span data-ttu-id="aa45b-158">Řazené kolekce členů jsou jednoduché datové struktury, které obsahují několik polí k reprezentaci datových členů.</span><span class="sxs-lookup"><span data-stu-id="aa45b-158">Tuples are lightweight data structures that contain multiple fields to represent the data members.</span></span>
<span data-ttu-id="aa45b-159">Nelze ověřit pole a nelze definovat vlastní metody</span><span class="sxs-lookup"><span data-stu-id="aa45b-159">The fields are not validated, and you cannot define your own methods</span></span>

> [!NOTE]
> <span data-ttu-id="aa45b-160">Řazené kolekce členů byly k dispozici před C# 7.0, ale měla neefektivní a měl neexistuje jazyková podpora.</span><span class="sxs-lookup"><span data-stu-id="aa45b-160">Tuples were available before C# 7.0, but they were inefficient and had no language support.</span></span>
> <span data-ttu-id="aa45b-161">Vynutila si, že prvky n-tice může být odkazováno pouze jako `Item1`, `Item2` a tak dále.</span><span class="sxs-lookup"><span data-stu-id="aa45b-161">This meant that tuple elements could only be referenced as `Item1`, `Item2` and so on.</span></span> <span data-ttu-id="aa45b-162">C# 7.0 zavádí jazyková podpora pro řazené kolekce členů, která umožňuje sémantického názvy polí pomocí nové, efektivnější řazené kolekce členů typů řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="aa45b-162">C# 7.0 introduces language support for tuples, which enables semantic names for the fields of a tuple using new, more efficient tuple types.</span></span>

<span data-ttu-id="aa45b-163">Můžete vytvořit řazené kolekce členů přiřazením hodnoty pro každého člena:</span><span class="sxs-lookup"><span data-stu-id="aa45b-163">You can create a tuple by assigning a value to each member:</span></span>

[!code-csharp[UnnamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#04_UnnamedTuple "Unnamed tuple")]

<span data-ttu-id="aa45b-164">Aby přiřazení vytvoří řazenou kolekci členů, jejíž členové jsou `Item1` a `Item2`, který můžete použít stejným způsobem jako <xref:System.Tuple> syntaxe k vytvoření řazené kolekce členů, která poskytuje sémantického názvy na každý ze členů řazené kolekce členů, můžete změnit:</span><span class="sxs-lookup"><span data-stu-id="aa45b-164">That assignment creates a tuple whose members are `Item1` and `Item2`, which you can use in the same way as <xref:System.Tuple> You can change the syntax to create a tuple that provides semantic names to each of the members of the tuple:</span></span>

[!code-csharp[NamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#05_NamedTuple "Named tuple")]

<span data-ttu-id="aa45b-165">`namedLetters` Řazené kolekce členů obsahuje pole, které jsou označovány jako `Alpha` a `Beta`.</span><span class="sxs-lookup"><span data-stu-id="aa45b-165">The `namedLetters` tuple contains fields referred to as `Alpha` and `Beta`.</span></span> <span data-ttu-id="aa45b-166">Názvy v rámci existují pouze v době kompilace a nejsou zachována např. při kontrole řazené kolekce členů v době běhu pomocí reflexe.</span><span class="sxs-lookup"><span data-stu-id="aa45b-166">Those names exist only at compile time and are not preserved for example when inspecting the tuple using reflection at runtime.</span></span>

<span data-ttu-id="aa45b-167">V přiřazení řazené kolekce členů můžete také zadat názvy polí na pravé straně přiřazení:</span><span class="sxs-lookup"><span data-stu-id="aa45b-167">In a tuple assignment, you can also specify the names of the fields on the right-hand side of the assignment:</span></span>

[!code-csharp[ImplicitNamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#06_ImplicitNamedTuple "Implicitly named tuple")]

<span data-ttu-id="aa45b-168">Můžete zadat názvy polí na levé a pravé straně přiřazení:</span><span class="sxs-lookup"><span data-stu-id="aa45b-168">You can specify names for the fields on both the left and right-hand side of the assignment:</span></span>

[!code-csharp[NamedTupleConflict](../../../samples/snippets/csharp/new-in-7/program.cs#07_NamedTupleConflict "Named tuple conflict")]

<span data-ttu-id="aa45b-169">Výše uvedené řádku generuje upozornění, `CS8123`, o tom, že názvy na pravé straně přiřazení, `Alpha` a `Beta` jsou ignorovány, protože jsou v konfliktu s názvy na levé straně, `First` a `Second`.</span><span class="sxs-lookup"><span data-stu-id="aa45b-169">The line above generates a warning, `CS8123`, telling you that the names on the right side of the assignment, `Alpha` and `Beta` are ignored because they conflict with the names on the left side, `First` and `Second`.</span></span>

<span data-ttu-id="aa45b-170">Výše uvedených příkladech základní syntaxe deklarovat řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="aa45b-170">The examples above show the basic syntax to declare tuples.</span></span> <span data-ttu-id="aa45b-171">Řazené kolekce členů jsou velmi užitečné jako návratové typy pro `private` a `internal` metody.</span><span class="sxs-lookup"><span data-stu-id="aa45b-171">Tuples are most useful as return types for `private` and `internal` methods.</span></span> <span data-ttu-id="aa45b-172">Řazené kolekce členů poskytují jednoduché syntaxi pro tyto metody vrátit více diskrétních hodnot: uložit práci při vytváření `class` nebo `struct` , který definuje typ vrácený.</span><span class="sxs-lookup"><span data-stu-id="aa45b-172">Tuples provide a simple syntax for those methods to return multiple discrete values: You save the work of authoring a `class` or a `struct` that defines the type returned.</span></span> <span data-ttu-id="aa45b-173">Není potřeba pro vytvoření nového typu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-173">There is no need for creating a new type.</span></span>

<span data-ttu-id="aa45b-174">Vytvoření řazené kolekce členů je efektivnější a zvýšit produktivitu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-174">Creating a tuple is more efficient and more productive.</span></span>
<span data-ttu-id="aa45b-175">Je jednodušší, lightweight syntaxe definovat datová struktura, která představuje více než jednu hodnotu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-175">It is a simpler, lightweight syntax to define a data structure that carries more than one value.</span></span> <span data-ttu-id="aa45b-176">Následující příklad metoda vrátí na minimální a maximální hodnoty zjištěné v posloupnost celých čísel:</span><span class="sxs-lookup"><span data-stu-id="aa45b-176">The example method below returns the minimum and maximum values found in a sequence of integers:</span></span>

[!code-csharp[TupleReturningMethod](../../../samples/snippets/csharp/new-in-7/program.cs#08_TupleReturningMethod "Tuple returning method")]

<span data-ttu-id="aa45b-177">Použití řazených kolekcí členů tímto způsobem nabízí několik výhod:</span><span class="sxs-lookup"><span data-stu-id="aa45b-177">Using tuples in this way offers several advantages:</span></span>

* <span data-ttu-id="aa45b-178">Uložit práci při vytváření `class` nebo `struct` , který definuje typ vrácený.</span><span class="sxs-lookup"><span data-stu-id="aa45b-178">You save the work of authoring a `class` or a `struct` that defines the type returned.</span></span> 
* <span data-ttu-id="aa45b-179">Není nutné k vytvoření nového typu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-179">You do not need to create new type.</span></span>
* <span data-ttu-id="aa45b-180">Rozšíření pro jazyk eliminuje nutnost volání <xref:System.Tuple.Create``1(``0)> metody.</span><span class="sxs-lookup"><span data-stu-id="aa45b-180">The language enhancements removes the need to call the <xref:System.Tuple.Create``1(``0)> methods.</span></span>

<span data-ttu-id="aa45b-181">Deklarace pro metodu poskytuje názvy pro pole řazené kolekce členů, která je vrácena.</span><span class="sxs-lookup"><span data-stu-id="aa45b-181">The declaration for the method provides the names for the fields of the tuple that is returned.</span></span> <span data-ttu-id="aa45b-182">Při volání metody vrácená hodnota je řazené kolekce členů, jejichž pole jsou `Max` a `Min`:</span><span class="sxs-lookup"><span data-stu-id="aa45b-182">When you call the method, the return value is a tuple whose fields are `Max` and `Min`:</span></span>

[!code-csharp[CallingTupleMethod](../../../samples/snippets/csharp/new-in-7/program.cs#09_CallingTupleMethod "Calling a tuple returning method")]

<span data-ttu-id="aa45b-183">Může nastat situace, kdy budete chtít rozbalit členy řazené kolekce členů, kteří byly vráceny z metody.</span><span class="sxs-lookup"><span data-stu-id="aa45b-183">There may be times when you want to unpackage the members of a tuple that were returned from a method.</span></span>  <span data-ttu-id="aa45b-184">Můžete to udělat pomocí deklarování samostatné proměnných pro každou z hodnot v řazené kolekci členů.</span><span class="sxs-lookup"><span data-stu-id="aa45b-184">You can do that by declaring separate variables for each of the values in the tuple.</span></span> <span data-ttu-id="aa45b-185">To se označuje jako *deconstructing* řazenou kolekci členů:</span><span class="sxs-lookup"><span data-stu-id="aa45b-185">This is called *deconstructing* the tuple:</span></span>

[!code-csharp[CallingWithDeconstructor](../../../samples/snippets/csharp/new-in-7/program.cs#10_CallingWithDeconstructor "Deconstructing a tuple")]

<span data-ttu-id="aa45b-186">Můžete zadat taky podobné deconstruction pro libovolného typu v rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="aa45b-186">You can also provide a similar deconstruction for any type in .NET.</span></span> <span data-ttu-id="aa45b-187">To se provádí zápis `Deconstruct` jako člena třídy metodu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-187">This is done by writing a `Deconstruct` method as a member of the class.</span></span> <span data-ttu-id="aa45b-188">Aby `Deconstruct` metoda poskytuje sadu `out` argumenty pro každé z vlastností, které mají být extrahovány.</span><span class="sxs-lookup"><span data-stu-id="aa45b-188">That `Deconstruct` method provides a set of `out` arguments for each of the properties you want to extract.</span></span> <span data-ttu-id="aa45b-189">Zvažte proto `Point` třídu, která poskytuje deconstructor metodu, která extrahuje `X` a `Y` souřadnice:</span><span class="sxs-lookup"><span data-stu-id="aa45b-189">Consider this `Point` class that provides a deconstructor method that extracts the `X` and `Y` coordinates:</span></span>

[!code-csharp[PointWithDeconstruction](../../../samples/snippets/csharp/new-in-7/point.cs#11_PointWithDeconstruction "Point with deconstruction method")]
 
<span data-ttu-id="aa45b-190">Můžete rozbalit přiřazením jednotlivých polí `Point` k řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="aa45b-190">You can extract the individual fields by assigning a `Point` to a tuple:</span></span>

[!code-csharp[DeconstructPoint](../../../samples/snippets/csharp/new-in-7/program.cs#12_DeconstructPoint "Deconstruct a point")]

<span data-ttu-id="aa45b-191">Názvy definované v nejsou vázány `Deconstruct` metoda.</span><span class="sxs-lookup"><span data-stu-id="aa45b-191">You are not bound by the names defined in the `Deconstruct` method.</span></span> <span data-ttu-id="aa45b-192">Extrakce proměnné můžete přejmenovat jako součást přiřazení:</span><span class="sxs-lookup"><span data-stu-id="aa45b-192">You can rename the extract variables as part of the assignment:</span></span>  

[!code-csharp[DeconstructNames](../../../samples/snippets/csharp/new-in-7/program.cs#13_DeconstructNames "Deconstruct with new names")]

<span data-ttu-id="aa45b-193">Dozvíte se v podrobněji řazených kolekcí členů v [řazených kolekcí členů tématu](../tuples.md).</span><span class="sxs-lookup"><span data-stu-id="aa45b-193">You can learn more in depth about tuples in the [tuples topic](../tuples.md).</span></span>

## <a name="discards"></a><span data-ttu-id="aa45b-194">Zahození</span><span class="sxs-lookup"><span data-stu-id="aa45b-194">Discards</span></span>

<span data-ttu-id="aa45b-195">Často, když deconstructing řazené kolekce členů nebo volání metody s `out` parametry, se musí k definování proměnné jehož hodnota nezáleží a nemáte v úmyslu použít.</span><span class="sxs-lookup"><span data-stu-id="aa45b-195">Often when deconstructing a tuple or calling a method with `out` parameters, you're forced to define a variable whose value you don't care about and don't intend to use.</span></span> <span data-ttu-id="aa45b-196">C# přidává podporu pro *zahodí* tohoto scénáře.</span><span class="sxs-lookup"><span data-stu-id="aa45b-196">C# adds support for *discards* to handle this scenario.</span></span> <span data-ttu-id="aa45b-197">Zahození je proměnná jen pro zápis, jehož název je `_` (podtržítko); můžete přiřadit všechny hodnoty, které chcete zahodit do jedné proměnné.</span><span class="sxs-lookup"><span data-stu-id="aa45b-197">A discard is a write-only variable whose name is `_` (the underscore character); you can assign all of the values that you intend to discard to the single variable.</span></span> <span data-ttu-id="aa45b-198">Zahození je stejná jako nepřiřazené proměnné. Kromě příkaz přiřazení nelze použít zahození v kódu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-198">A discard is like an unassigned variable; apart from the assignment statement, the discard can't be used in code.</span></span>

<span data-ttu-id="aa45b-199">Zahození jsou podporovány v následujících scénářích:</span><span class="sxs-lookup"><span data-stu-id="aa45b-199">Discards are supported in the following scenarios:</span></span>

* <span data-ttu-id="aa45b-200">Když deconstructing řazené kolekce členů nebo uživatelem definované typy.</span><span class="sxs-lookup"><span data-stu-id="aa45b-200">When deconstructing tuples or user-defined types.</span></span>

* <span data-ttu-id="aa45b-201">Při volání metody s [out](../language-reference/keywords/out-parameter-modifier.md) parametry.</span><span class="sxs-lookup"><span data-stu-id="aa45b-201">When calling methods with [out](../language-reference/keywords/out-parameter-modifier.md) parameters.</span></span>

* <span data-ttu-id="aa45b-202">V porovnávání operaci s [je](../language-reference/keywords/is.md) a [přepínač](../language-reference/keywords/switch.md) příkazy.</span><span class="sxs-lookup"><span data-stu-id="aa45b-202">In a pattern matching operation with the [is](../language-reference/keywords/is.md) and [switch](../language-reference/keywords/switch.md) statements.</span></span>

* <span data-ttu-id="aa45b-203">Pokud chcete explicitně Identifikujte jako samostatné identifikátor hodnota přiřazení jako zahození.</span><span class="sxs-lookup"><span data-stu-id="aa45b-203">As a standalone identifier when you want to explicitly identify the value of an assignment as a discard.</span></span>

<span data-ttu-id="aa45b-204">V následujícím příkladu definuje `QueryCityDataForYears` metodu, která vrátí 6-n-tice obsahující data pro město pro dva různé roky.</span><span class="sxs-lookup"><span data-stu-id="aa45b-204">The following example defines a `QueryCityDataForYears` method that returns a 6-tuple that contains a data for a city for two different years.</span></span> <span data-ttu-id="aa45b-205">Volání metody v příkladu se týká pouze dva naplnění hodnoty vrácené metodou a proto považuje zbývající hodnoty v řazené kolekci členů jako zahodí, pokud ji deconstructs řazenou kolekci členů.</span><span class="sxs-lookup"><span data-stu-id="aa45b-205">The method call in the example is concerned only with the two population values returned by the method and so treats the remaining values in the tuple as discards when it deconstructs the tuple.</span></span>

[!code-csharp[Tuple-discard](../../../samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="aa45b-206">Další informace najdete v tématu [zahodí](../discards.md).</span><span class="sxs-lookup"><span data-stu-id="aa45b-206">For more information, see [Discards](../discards.md).</span></span>
 
## <a name="pattern-matching"></a><span data-ttu-id="aa45b-207">Shoda vzoru</span><span class="sxs-lookup"><span data-stu-id="aa45b-207">Pattern matching</span></span>

<span data-ttu-id="aa45b-208">*Shoda vzoru* je funkce, která umožňuje implementovat metodu odesílání na vlastnosti než typ objektu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-208">*Pattern matching* is a feature that allows you to implement method dispatch on properties other than the type of an object.</span></span> <span data-ttu-id="aa45b-209">Pravděpodobně už jste obeznámeni s odesílání metoda na základě typu objektu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-209">You're probably already familiar with method dispatch based on the type of an object.</span></span> <span data-ttu-id="aa45b-210">V objektově orientované programování virtuální a přepsání metody poskytují syntaxe jazyka pro implementaci metoda odeslání založený na typu objektu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-210">In Object Oriented programming, virtual and override methods provide language syntax to implement method dispatching based on an object's type.</span></span> <span data-ttu-id="aa45b-211">Základní a odvozené třídy poskytovat různé implementace.</span><span class="sxs-lookup"><span data-stu-id="aa45b-211">Base and Derived classes provide different implementations.</span></span> <span data-ttu-id="aa45b-212">Vzor odpovídající výrazy rozšířit tento koncept proto, že můžete snadno implementovat podobné odesílání vzory pro typy a data elementy, které nejsou v relaci prostřednictvím hierarchie dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="aa45b-212">Pattern matching expressions extend this concept so that you can easily implement similar dispatch patterns for types and data elements that are not related through an inheritance hierarchy.</span></span> 

<span data-ttu-id="aa45b-213">Porovnávání se podporuje `is` výrazy a `switch` výrazy.</span><span class="sxs-lookup"><span data-stu-id="aa45b-213">Pattern matching supports `is` expressions and `switch` expressions.</span></span> <span data-ttu-id="aa45b-214">Každý umožňuje zkontrolujete objektu a jeho vlastnosti k určení, pokud objekt splňuje hledané kombinaci vzoru.</span><span class="sxs-lookup"><span data-stu-id="aa45b-214">Each enables inspecting an object and its properties to determine if that object satisfies the sought pattern.</span></span> <span data-ttu-id="aa45b-215">Můžete použít `when` – klíčové slovo lze určit další pravidla, se vzorem.</span><span class="sxs-lookup"><span data-stu-id="aa45b-215">You use the `when` keyword to specify additional rules to the pattern.</span></span>

### <a name="is-expression"></a><span data-ttu-id="aa45b-216">`is` výraz</span><span class="sxs-lookup"><span data-stu-id="aa45b-216">`is` expression</span></span>

<span data-ttu-id="aa45b-217">`is` Vzor výrazu rozšiřuje známé `is` operátor dotazovat objekt nad rámec jeho typu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-217">The `is` pattern expression extends the familiar `is` operator to query an object beyond its type.</span></span>

<span data-ttu-id="aa45b-218">Začněme pomocí jednoduchého scénáře.</span><span class="sxs-lookup"><span data-stu-id="aa45b-218">Let's start with a simple scenario.</span></span> <span data-ttu-id="aa45b-219">Do tohoto scénáře, které ukazují, jak vytvořit vzor odpovídající výrazy algoritmy, které pracují s nesouvisejícími typy snadno přidáme možnosti.</span><span class="sxs-lookup"><span data-stu-id="aa45b-219">We'll add capabilities to this scenario that demonstrate how pattern matching expressions make algorithms that work with unrelated types easy.</span></span> <span data-ttu-id="aa45b-220">Začneme s metodu, která vypočítá součet počtu zobrazí souhrn die:</span><span class="sxs-lookup"><span data-stu-id="aa45b-220">We'll start with a method that computes the sum of a number of die rolls:</span></span>

[!code-csharp[SumDieRolls](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#14_SumDieRolls "Sum die rolls")]

<span data-ttu-id="aa45b-221">Můžete rychle zjistit, budete muset najít součet die zobrazí souhrn, kde některé zobrazí souhrn se provedou více rozčlenění (rozčlenění je množném čísle die).</span><span class="sxs-lookup"><span data-stu-id="aa45b-221">You might quickly find that you need to find the sum of die rolls where some of the rolls are made with multiple dice (dice is the plural of die).</span></span> <span data-ttu-id="aa45b-222">Součást vstupní pořadí může být více výsledků místo jedno číslo:</span><span class="sxs-lookup"><span data-stu-id="aa45b-222">Part of the input sequence may be multiple results instead of a single number:</span></span>

[!code-csharp[SumDieRollsWithGroups](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#15_SumDieRollsWithGroups "Sum die rolls with groups")]

<span data-ttu-id="aa45b-223">`is` Vzor výrazu velmi dobře funguje v tomto scénáři.</span><span class="sxs-lookup"><span data-stu-id="aa45b-223">The `is` pattern expression works quite well in this scenario.</span></span> <span data-ttu-id="aa45b-224">Jako součást kontrola typu zapisovat proměnné inicializace.</span><span class="sxs-lookup"><span data-stu-id="aa45b-224">As part of checking the type, you write a variable initialization.</span></span> <span data-ttu-id="aa45b-225">Tím se vytvoří novou proměnnou typu ověřené runtime.</span><span class="sxs-lookup"><span data-stu-id="aa45b-225">This creates a new variable of the validated runtime type.</span></span>

<span data-ttu-id="aa45b-226">Tyto scénáře zachovat rozšíření, můžete zjistit sestavení více `if` a `else if` příkazy.</span><span class="sxs-lookup"><span data-stu-id="aa45b-226">As you keep extending these scenarios, you may find that you build more `if` and `else if` statements.</span></span> <span data-ttu-id="aa45b-227">Po který bude nepraktické, budete pravděpodobně chtít přepnout na `switch` vzor výrazy.</span><span class="sxs-lookup"><span data-stu-id="aa45b-227">Once that becomes unwieldy, you'll likely want to switch to `switch` pattern expressions.</span></span>

### <a name="switch-statement-updates"></a><span data-ttu-id="aa45b-228">`switch` aktualizace – příkaz</span><span class="sxs-lookup"><span data-stu-id="aa45b-228">`switch` statement updates</span></span>

<span data-ttu-id="aa45b-229">*Odpovídat výrazu* má známé syntaxe, na základě `switch` příkaz již součástí jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="aa45b-229">The *match expression* has a familiar syntax, based on the `switch` statement already part of the C# language.</span></span> <span data-ttu-id="aa45b-230">Umožňuje překlad stávajícího kódu pro použití výrazu shody před přidáním nové případy:</span><span class="sxs-lookup"><span data-stu-id="aa45b-230">Let's translate the existing code to use a match expression before adding new cases:</span></span> 

[!code-csharp[SumUsingSwitch](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#16_SumUsingSwitch "Sum using switch")]

<span data-ttu-id="aa45b-231">Výrazy shody mají mírně odlišné syntaxi než `is` výrazy, kde deklarace typu a proměnné na začátku `case` výraz.</span><span class="sxs-lookup"><span data-stu-id="aa45b-231">The match expressions have a slightly different syntax than the `is` expressions, where you declare the type and variable at the beginning of the `case` expression.</span></span>

<span data-ttu-id="aa45b-232">Výrazy shody také podporují konstanty.</span><span class="sxs-lookup"><span data-stu-id="aa45b-232">The match expressions also support constants.</span></span> <span data-ttu-id="aa45b-233">Tím ušetřit čas tím řešení se jednoduchých případech:</span><span class="sxs-lookup"><span data-stu-id="aa45b-233">This can save time by factoring out simple cases:</span></span>

[!code-csharp[SwitchWithConstants](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#17_SwitchWithConstants "Switch with constants")]

<span data-ttu-id="aa45b-234">Výše uvedený kód přidá případů pro `0` jako zvláštní případ `int`, a `null` ve speciálním případě při žádný vstup.</span><span class="sxs-lookup"><span data-stu-id="aa45b-234">The code above adds cases for `0` as a special case of `int`, and `null` as a special case when there is no input.</span></span> <span data-ttu-id="aa45b-235">Tento příklad ukazuje jeden důležité nové funkce ve výrazech vzor přepínač: pořadí `case` výrazy nyní záleží.</span><span class="sxs-lookup"><span data-stu-id="aa45b-235">This demonstrates one important new feature in switch pattern expressions: the order of the `case` expressions now matters.</span></span> <span data-ttu-id="aa45b-236">`0` Případu musí být před Obecné `int` případu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-236">The `0` case must appear before the general `int` case.</span></span> <span data-ttu-id="aa45b-237">Jinak, by první vzor tak, aby odpovídala `int` případu, i když je hodnota `0`.</span><span class="sxs-lookup"><span data-stu-id="aa45b-237">Otherwise, the first pattern to match would be the `int` case, even when the value is `0`.</span></span> <span data-ttu-id="aa45b-238">Pokud omylem výrazy shody pořadí tak, že již byla zpracována novější případu, bude kompilátor příznak, který a generuje chybu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-238">If you accidentally order match expressions such that a later case has already been handled, the compiler will flag that and generate an error.</span></span>

<span data-ttu-id="aa45b-239">Toto stejné chování umožňuje zvláštní případ prázdnou sekvencí vstupní.</span><span class="sxs-lookup"><span data-stu-id="aa45b-239">This same behavior enables the special case for an empty input sequence.</span></span>
<span data-ttu-id="aa45b-240">Uvidíte, že případ `IEnumerable` položku, která obsahuje elementy musí být před Obecné `IEnumerable` případu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-240">You can see that the case for an `IEnumerable` item that has elements must appear before the general `IEnumerable` case.</span></span>

<span data-ttu-id="aa45b-241">Tato verze taky přidala `default` případu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-241">This version has also added a `default` case.</span></span> <span data-ttu-id="aa45b-242">`default` Případ vždy vyhodnocena poslední, bez ohledu na pořadí zobrazí se ve zdroji.</span><span class="sxs-lookup"><span data-stu-id="aa45b-242">The `default` case is always evaluated last, regardless of the order it appears in the source.</span></span> <span data-ttu-id="aa45b-243">Z tohoto důvodu je konvence uvést `default` případu poslední.</span><span class="sxs-lookup"><span data-stu-id="aa45b-243">For that reason, convention is to put the `default` case last.</span></span>

<span data-ttu-id="aa45b-244">Nakonec přidejme jednu poslední `case` nového stylu die.</span><span class="sxs-lookup"><span data-stu-id="aa45b-244">Finally, let's add one last `case` for a new style of die.</span></span> <span data-ttu-id="aa45b-245">Některé hry pomocí percentilu rozčlenění znázornit větší rozsah čísel.</span><span class="sxs-lookup"><span data-stu-id="aa45b-245">Some games use percentile dice to represent larger ranges of numbers.</span></span> 

> [!NOTE]
> <span data-ttu-id="aa45b-246">Dva rozčlenění 10 zachytávání percentilu může představovat každé číslo od 0 do 99.</span><span class="sxs-lookup"><span data-stu-id="aa45b-246">Two 10-sided percentile dice can represent every number from 0 through 99.</span></span> <span data-ttu-id="aa45b-247">Počátku má strany, které jsou označené jako `00`, `10`, `20`,... `90`.</span><span class="sxs-lookup"><span data-stu-id="aa45b-247">One die has sides labelled `00`, `10`, `20`, ... `90`.</span></span> <span data-ttu-id="aa45b-248">Další die má strany s názvem bez přípony `0`, `1`, `2`,... `9`.</span><span class="sxs-lookup"><span data-stu-id="aa45b-248">The other die has sides labeled `0`, `1`, `2`, ... `9`.</span></span> <span data-ttu-id="aa45b-249">Přidejte dva die hodnot společně a získáte každé číslo od 0 až 99.</span><span class="sxs-lookup"><span data-stu-id="aa45b-249">Add the two die values together and you can get every number from 0 through 99.</span></span>

<span data-ttu-id="aa45b-250">Pokud chcete tento druh die přidat do kolekce, nejprve definujte typ představují rozčlenění percentil.</span><span class="sxs-lookup"><span data-stu-id="aa45b-250">To add this kind of die to your collection, first define a type to represent the percentile dice.</span></span> <span data-ttu-id="aa45b-251">`TensDigit` Vlastnosti se ukládají hodnoty `0`, `10`, `20`, až `90`:</span><span class="sxs-lookup"><span data-stu-id="aa45b-251">The `TensDigit` property stores values `0`, `10`, `20`, up to `90`:</span></span>

[!code-csharp[18_PercentileDice](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#18_PercentileDice "Percentile Die type")]

<span data-ttu-id="aa45b-252">Potom přidat `case` odpovídat výrazu pro nový typ:</span><span class="sxs-lookup"><span data-stu-id="aa45b-252">Then, add a `case` match expression for the new type:</span></span>

[!code-csharp[SwitchWithNewTypes](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#19_SwitchWithNewTypes "Include Percentile Die type")]

<span data-ttu-id="aa45b-253">Nové syntaxe pro výrazy porovnávání vzorů usnadňuje vytvoření odesílání algoritmů na základě typ objektu, nebo jiné vlastnosti pomocí syntaxe jasným a přesným metrikám.</span><span class="sxs-lookup"><span data-stu-id="aa45b-253">The new syntax for pattern matching expressions makes it easier to create dispatch algorithms based on an object's type, or other properties, using a clear and concise syntax.</span></span> <span data-ttu-id="aa45b-254">Vzor odpovídající výrazy povolte tyto konstrukce pro datové typy, které jsou nesouvisející podle dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="aa45b-254">Pattern matching expressions enable these constructs on data types that are unrelated by inheritance.</span></span>

<span data-ttu-id="aa45b-255">Další informace o porovnávání se v tomto tématu vyhrazený pro [v jazyce C# porovnávání vzorů](../pattern-matching.md).</span><span class="sxs-lookup"><span data-stu-id="aa45b-255">You can learn more about pattern matching in the topic dedicated to [pattern matching in C#](../pattern-matching.md).</span></span>

## <a name="ref-locals-and-returns"></a><span data-ttu-id="aa45b-256">Místní hodnoty REF a vrátí</span><span class="sxs-lookup"><span data-stu-id="aa45b-256">Ref locals and returns</span></span>

<span data-ttu-id="aa45b-257">Tato funkce umožňuje algoritmy, které používají a vrátí odkazy na proměnné definované jinde.</span><span class="sxs-lookup"><span data-stu-id="aa45b-257">This feature enables algorithms that use and return references to variables defined elsewhere.</span></span> <span data-ttu-id="aa45b-258">Jedním z příkladů je práce s velké matic a hledání jedno umístění s určité charakteristické vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="aa45b-258">One example is working with large matrices, and finding a single location with certain characteristics.</span></span> <span data-ttu-id="aa45b-259">Jedna metoda vrátí dva indexy na jednom místě v matici:</span><span class="sxs-lookup"><span data-stu-id="aa45b-259">One method would return the two indices for a single location in the matrix:</span></span>

[!code-csharp[FindReturningIndices](../../../samples/snippets/csharp/new-in-7/MatrixSearch.cs#20_FindReturningIndices "Find returning indices")]

<span data-ttu-id="aa45b-260">Existuje mnoho problémů s tímto kódem.</span><span class="sxs-lookup"><span data-stu-id="aa45b-260">There are many issues with this code.</span></span> <span data-ttu-id="aa45b-261">První řadě je veřejná metoda, která vrací řazenou kolekci členů.</span><span class="sxs-lookup"><span data-stu-id="aa45b-261">First of all, it's a public method that's returning a tuple.</span></span> <span data-ttu-id="aa45b-262">Tento jazyk podporuje, ale uživatelem definované typy (třídy nebo struktury) jsou upřednostněny veřejná rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="aa45b-262">The language supports this, but user defined types (either classes or structs) are preferred for public APIs.</span></span>

<span data-ttu-id="aa45b-263">Druhý tato metoda vrací indexy na položku v matici.</span><span class="sxs-lookup"><span data-stu-id="aa45b-263">Second, this method is returning the indices to the item in the matrix.</span></span>
<span data-ttu-id="aa45b-264">Který vede volající napsat kód, který používá tyto indexy dereference matice a upravovat jeden element:</span><span class="sxs-lookup"><span data-stu-id="aa45b-264">That leads callers to write code that uses those indices to dereference the matrix and modify a single element:</span></span>

[!code-csharp[UpdateItemFromIndices](../../../samples/snippets/csharp/new-in-7/program.cs#21_UpdateItemFromIndices "Update Item From Indices")]

<span data-ttu-id="aa45b-265">By místo napíše metoda, která vrátí *odkaz* na prvek přehled, který chcete změnit.</span><span class="sxs-lookup"><span data-stu-id="aa45b-265">You'd rather write a method that returns a *reference* to the element of the matrix that you want to change.</span></span> <span data-ttu-id="aa45b-266">Může to provést pouze pomocí nezabezpečený kód a vrací ukazatel na `int` v předchozích verzích.</span><span class="sxs-lookup"><span data-stu-id="aa45b-266">You could only accomplish this by using unsafe code and returning a pointer to an `int` in previous versions.</span></span>

<span data-ttu-id="aa45b-267">Projděme řadu změny k předvedení funkcí místní ref a znázorňují postup vytvoření metody, která vrátí odkaz na interní úložiště.</span><span class="sxs-lookup"><span data-stu-id="aa45b-267">Let's walk through a series of changes to demonstrate the ref local feature and show how to create a method that returns a reference to internal storage.</span></span>
<span data-ttu-id="aa45b-268">Cestou se dozvíte pravidla ref vrátit a ref místní funkce, která chrání před omylem zneužití ho.</span><span class="sxs-lookup"><span data-stu-id="aa45b-268">Along the way, you'll learn the rules of the ref return and ref local feature that protects you from accidentally misusing it.</span></span>

<span data-ttu-id="aa45b-269">Spustit změnou `Find` deklarace metody, které se vrátí `ref int` místo řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="aa45b-269">Start by modifying the `Find` method declaration so that it returns a `ref int` instead of a tuple.</span></span> <span data-ttu-id="aa45b-270">Potom upravte příkaz return tak, aby vracel s hodnotou uloženou v matici místo dvou indexy:</span><span class="sxs-lookup"><span data-stu-id="aa45b-270">Then, modify the return statement so it returns the value stored in the matrix instead of the two indices:</span></span>

```csharp
// Note that this won't compile. 
// Method declaration indicates ref return,
// but return statement specifies a value return.
public static ref int Find2(int[,] matrix, Func<int, bool> predicate)
{
    for (int i = 0; i < matrix.GetLength(0); i++)
        for (int j = 0; j < matrix.GetLength(1); j++)
            if (predicate(matrix[i, j]))
                return matrix[i, j];
    throw new InvalidOperationException("Not found");
}
```

<span data-ttu-id="aa45b-271">Když je deklarovat, metoda vrátí `ref` proměnné, musíte taky přidat `ref` – klíčové slovo pro každý příkaz return.</span><span class="sxs-lookup"><span data-stu-id="aa45b-271">When you declare that a method returns a `ref` variable, you must also add the `ref` keyword to each return statement.</span></span> <span data-ttu-id="aa45b-272">Určující vrátit odkazem a pomáhá vývojářům čtení kódu později mějte na paměti, že metoda vrátí odkazem:</span><span class="sxs-lookup"><span data-stu-id="aa45b-272">That indicates return by reference, and helps developers reading the code later remember that the method returns by reference:</span></span>

[!code-csharp[FindReturningRef](../../../samples/snippets/csharp/new-in-7/MatrixSearch.cs#22_FindReturningRef "Find returning by reference")]

<span data-ttu-id="aa45b-273">Teď, když metoda vrátí odkaz na celé číslo v matici, budete muset upravit, kde je volán.</span><span class="sxs-lookup"><span data-stu-id="aa45b-273">Now that the method returns a reference to the integer value in the matrix, you need to modify where it's called.</span></span>  <span data-ttu-id="aa45b-274">`var` Prohlášení znamená, že `valItem` je nyní `int` místo řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="aa45b-274">The `var` declaration means that `valItem` is now an `int` rather than a tuple:</span></span>

[!code-csharp[AssignRefReturnToValue](../../../samples/snippets/csharp/new-in-7/program.cs#23_AssignRefReturnToValue "Assign ref return to value")]

<span data-ttu-id="aa45b-275">Druhý `WriteLine` příkaz v předchozím příkladu vytiskne hodnota `42`, nikoli `24`.</span><span class="sxs-lookup"><span data-stu-id="aa45b-275">The second `WriteLine` statement in the example above prints out the value `42`, not `24`.</span></span> <span data-ttu-id="aa45b-276">Proměnná `valItem` je `int`, nikoli `ref int`.</span><span class="sxs-lookup"><span data-stu-id="aa45b-276">The variable `valItem` is an `int`, not a `ref int`.</span></span> <span data-ttu-id="aa45b-277">`var` – Klíčové slovo umožňuje kompilátoru k určení typu, ale nepřidá implicitně `ref` modifikátor.</span><span class="sxs-lookup"><span data-stu-id="aa45b-277">The `var` keyword enables the compiler to specify the type, but will not implicitly add the `ref` modifier.</span></span> <span data-ttu-id="aa45b-278">Místo toho hodnota odkazuje `ref return` je *zkopírovat* do proměnné na levé straně přiřazení.</span><span class="sxs-lookup"><span data-stu-id="aa45b-278">Instead, the value referred to by the `ref return` is *copied* to the variable on the left-hand side of the assignment.</span></span> <span data-ttu-id="aa45b-279">Proměnná není `ref` místní.</span><span class="sxs-lookup"><span data-stu-id="aa45b-279">The variable is not a `ref` local.</span></span>

<span data-ttu-id="aa45b-280">Chcete-li získat výsledek, který chcete, je nutné přidat `ref` modifikátor na místní deklarace proměnné při vrácená hodnota je odkaz vytvořit odkaz na proměnnou:</span><span class="sxs-lookup"><span data-stu-id="aa45b-280">In order to get the result you want, you need to add the `ref` modifier to the local variable declaration to make the variable a reference when the return value is a reference:</span></span>

[!code-csharp[AssignRefReturn](../../../samples/snippets/csharp/new-in-7/program.cs#24_AssignRefReturn "Assign ref return")]

<span data-ttu-id="aa45b-281">Nyní, druhý `WriteLine` příkaz v předchozím příkladu bude vytisknout hodnotu `24`, indikující, že byl upraven úložiště v matici.</span><span class="sxs-lookup"><span data-stu-id="aa45b-281">Now, the second `WriteLine` statement in the example above will print out the value `24`, indicating that the storage in the matrix has been modified.</span></span> <span data-ttu-id="aa45b-282">Místní proměnná bylo deklarováno s `ref` modifikátor a bude trvat `ref` vrátit.</span><span class="sxs-lookup"><span data-stu-id="aa45b-282">The local variable has been declared with the `ref` modifier, and it will take a `ref` return.</span></span> <span data-ttu-id="aa45b-283">Je třeba inicializovat `ref` proměnná při deklaraci, nelze rozdělit deklarace a inicializace.</span><span class="sxs-lookup"><span data-stu-id="aa45b-283">You must initialize a `ref` variable when it is declared, you cannot split the declaration and the initialization.</span></span>

<span data-ttu-id="aa45b-284">Jazyk C# má tři další pravidla, která ochranu proti zneužití `ref` lokální a vrátí:</span><span class="sxs-lookup"><span data-stu-id="aa45b-284">The C# language has three other rules that protect you from misusing the `ref` locals and returns:</span></span>

* <span data-ttu-id="aa45b-285">Nelze přiřadit standardní metoda návratovou hodnotu pro `ref` místní proměnné.</span><span class="sxs-lookup"><span data-stu-id="aa45b-285">You cannot assign a standard method return value to a `ref` local variable.</span></span>
    - <span data-ttu-id="aa45b-286">Která zakáže příkazy jako `ref int i = sequence.Count();`</span><span class="sxs-lookup"><span data-stu-id="aa45b-286">That disallows statements like `ref int i = sequence.Count();`</span></span>
* <span data-ttu-id="aa45b-287">Nelze vrátit `ref` do proměnné, jejichž doba platnosti nepřesahuje provádění metody.</span><span class="sxs-lookup"><span data-stu-id="aa45b-287">You cannot return a `ref` to a variable whose lifetime does not extend beyond the execution of the method.</span></span>
    - <span data-ttu-id="aa45b-288">To znamená, že nemůže vrátit odkaz na místní proměnná nebo proměnná se podobně jako obor.</span><span class="sxs-lookup"><span data-stu-id="aa45b-288">That means you cannot return a reference to a local variable or a variable with a similar scope.</span></span>
* <span data-ttu-id="aa45b-289">`ref` lokální a vrátí nelze použít u asynchronních metod.</span><span class="sxs-lookup"><span data-stu-id="aa45b-289">`ref` locals and returns can't be used with async methods.</span></span>
    - <span data-ttu-id="aa45b-290">Kompilátor nemůže vědět, pokud odkazovaná proměnná byla nastavena na jeho konečná hodnota při návratu asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="aa45b-290">The compiler can't know if the referenced variable has been set to its final value when the async method returns.</span></span>

<span data-ttu-id="aa45b-291">Přidání místní hodnoty ref a ref vrátí povolit algoritmy, které jsou efektivnější vyhnout kopírování hodnot nebo provádění operací při přesměrování vícekrát.</span><span class="sxs-lookup"><span data-stu-id="aa45b-291">The addition of ref locals and ref returns enable algorithms that are more efficient by avoiding copying values, or performing dereferencing operations multiple times.</span></span> 

## <a name="local-functions"></a><span data-ttu-id="aa45b-292">Lokální funkce</span><span class="sxs-lookup"><span data-stu-id="aa45b-292">Local functions</span></span>

<span data-ttu-id="aa45b-293">Řada návrhů pro třídy zahrnují metody, které se nazývají z pouze jedno umístění.</span><span class="sxs-lookup"><span data-stu-id="aa45b-293">Many designs for classes include methods that are called from only one location.</span></span> <span data-ttu-id="aa45b-294">Tyto další metody privátní zachovat každá metoda malé a zaměřují se.</span><span class="sxs-lookup"><span data-stu-id="aa45b-294">These additional private methods keep each method small and focused.</span></span> <span data-ttu-id="aa45b-295">Však budou může ztížit pochopit třídu při čtení ji poprvé.</span><span class="sxs-lookup"><span data-stu-id="aa45b-295">However, they can make it harder to understand a class when reading it the first time.</span></span> <span data-ttu-id="aa45b-296">Tyto metody, je třeba chápat mimo kontext jednoho volání umístění.</span><span class="sxs-lookup"><span data-stu-id="aa45b-296">These methods must be understood outside of the context of the single calling location.</span></span>

<span data-ttu-id="aa45b-297">Pro tyto návrhy *lokální funkce* vám umožní deklarovat metody uvnitř kontextu jinou metodu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-297">For those designs, *local functions* enable you to declare methods inside the context of another method.</span></span> <span data-ttu-id="aa45b-298">To usnadňuje čtečky třídy zobrazíte, že místní metoda je volána pouze z kontextu, ve kterém je deklarovaná.</span><span class="sxs-lookup"><span data-stu-id="aa45b-298">This makes it easier for readers of the class to see that the local method is only called from the context in which is it declared.</span></span>

<span data-ttu-id="aa45b-299">Existují dvě velmi běžné případy použití pro místní funkce: metody veřejné iterator a veřejné asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="aa45b-299">There are two very common use cases for local functions: public iterator methods and public async methods.</span></span> <span data-ttu-id="aa45b-300">Oba typy metod generovat kód, který později, než by se dalo očekávat programátory sestavy chyb.</span><span class="sxs-lookup"><span data-stu-id="aa45b-300">Both types of methods generate code that reports errors later than programmers might expect.</span></span> <span data-ttu-id="aa45b-301">V případě metody iterator jakékoli výjimky jsou dodržovány pouze při volání metody kód, který vytvoří výčet vrácený pořadí.</span><span class="sxs-lookup"><span data-stu-id="aa45b-301">In the case of iterator methods, any exceptions are observed only when calling code that enumerates the returned sequence.</span></span> <span data-ttu-id="aa45b-302">V případě asynchronní metody jakékoli výjimky jsou pouze dodržovány při vrácený `Task` je očekáváno.</span><span class="sxs-lookup"><span data-stu-id="aa45b-302">In the case of async methods, any exceptions are only observed when the returned `Task` is awaited.</span></span>

<span data-ttu-id="aa45b-303">Začneme metodu iterator:</span><span class="sxs-lookup"><span data-stu-id="aa45b-303">Let's start with an iterator method:</span></span>

[!code-csharp[IteratorMethod](../../../samples/snippets/csharp/new-in-7/Iterator.cs#25_IteratorMethod "Iterator method")]

<span data-ttu-id="aa45b-304">Zkontrolujte následující kód, který volá metodu iterator nesprávně:</span><span class="sxs-lookup"><span data-stu-id="aa45b-304">Examine the code below that calls the iterator method incorrectly:</span></span>

[!code-csharp[CallIteratorMethod](../../../samples/snippets/csharp/new-in-7/program.cs#26_CallIteratorMethod "Call iterator method")]

<span data-ttu-id="aa45b-305">Když je vyvolána výjimka `resultSet` vstupní, nejsou při `resultSet` je vytvořena.</span><span class="sxs-lookup"><span data-stu-id="aa45b-305">The exception is thrown when `resultSet` is iterated, not when `resultSet` is created.</span></span>
<span data-ttu-id="aa45b-306">V tomto příkladu obsažené Většina vývojářů může rychle diagnostikovat problém.</span><span class="sxs-lookup"><span data-stu-id="aa45b-306">In this contained example, most developers could quickly diagnose the problem.</span></span> <span data-ttu-id="aa45b-307">V větší základy kódu, kód, který vytvoří iterovat často není co nejblíže kód, který zobrazí výsledek.</span><span class="sxs-lookup"><span data-stu-id="aa45b-307">However, in larger codebases, the code that creates an iterator often isn't as close to the code that enumerates the result.</span></span> <span data-ttu-id="aa45b-308">Kód můžete Refaktorovat tak, že veřejná metoda ověří všechny argumenty a soukromá metoda generuje výčtu:</span><span class="sxs-lookup"><span data-stu-id="aa45b-308">You can refactor the code so that the public method validates all arguments, and a private method generates the enumeration:</span></span>

[!code-csharp[IteratorMethodRefactored](../../../samples/snippets/csharp/new-in-7/Iterator.cs#27_IteratorMethodRefactored "Iterator method refactored")]

<span data-ttu-id="aa45b-309">Tato verze refactored vyvolá výjimku výjimky okamžitě, protože veřejná metoda není metodou iterator; pouze privátní metoda používá `yield return` syntaxe.</span><span class="sxs-lookup"><span data-stu-id="aa45b-309">This refactored version will throw exceptions immediately because the public method is not an iterator method; only the private method uses the `yield return` syntax.</span></span> <span data-ttu-id="aa45b-310">Existují však potenciální problémy s Tento refaktoring.</span><span class="sxs-lookup"><span data-stu-id="aa45b-310">However, there are potential problems with this refactoring.</span></span> <span data-ttu-id="aa45b-311">Soukromá metoda by měla být volána metoda veřejné rozhraní pouze, protože jinak se všemi ověřovacími argument přeskočí.</span><span class="sxs-lookup"><span data-stu-id="aa45b-311">The private method should only be called from the public interface method, because otherwise all argument validation is skipped.</span></span>
<span data-ttu-id="aa45b-312">Čtečky třídy musí tuto skutečnost zjistit čtení celou třídu a vyhledáním všechny odkazy na `alphabetSubsetImplementation` metoda.</span><span class="sxs-lookup"><span data-stu-id="aa45b-312">Readers of the class must discover this fact by reading the entire class and searching for any other references to the `alphabetSubsetImplementation` method.</span></span>

<span data-ttu-id="aa45b-313">Tento návrh záměr můžete vytvořit více zrušte pomocí deklarace `alphabetSubsetImplementation` jako místní funkce uvnitř metody veřejné rozhraní API:</span><span class="sxs-lookup"><span data-stu-id="aa45b-313">You can make that design intent more clear by declaring the `alphabetSubsetImplementation` as a local function inside the public API method:</span></span>

[!code-csharp[22_IteratorMethodLocal](../../../samples/snippets/csharp/new-in-7/Iterator.cs#28_IteratorMethodLocal "Iterator method with local function")]

<span data-ttu-id="aa45b-314">Výše uvedené verze umožňuje vymazat odkazovaný místní metoda pouze v kontextu vnější metody.</span><span class="sxs-lookup"><span data-stu-id="aa45b-314">The version above makes it clear that the local method is referenced only in the context of the outer method.</span></span> <span data-ttu-id="aa45b-315">Pravidla pro lokální funkce také zajistěte, aby vývojář nelze omylem volání místní funkce z jiného umístění v třídě a ověření argument obejít.</span><span class="sxs-lookup"><span data-stu-id="aa45b-315">The rules for local functions also ensure that a developer can't accidentally call the local function from another location in the class and bypass the argument validation.</span></span>

<span data-ttu-id="aa45b-316">Stejným způsobem je možné použít s `async` zajistit, že vzniklých argument ověření je vyvolána před zahájením práce asynchronní metody:</span><span class="sxs-lookup"><span data-stu-id="aa45b-316">The same technique can be employed with `async` methods to ensure that exceptions arising from argument validation are thrown before the asynchronous work begins:</span></span>

[!code-csharp[TaskExample](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#29_TaskExample "Task returning method with local function")]

> [!NOTE]
> <span data-ttu-id="aa45b-317">Některé návrhů, které jsou podporovány pomocí místní také je možné dosáhnout pomocí *výrazy lambda*.</span><span class="sxs-lookup"><span data-stu-id="aa45b-317">Some of the designs that are supported by local functions could also be accomplished using *lambda expressions*.</span></span> <span data-ttu-id="aa45b-318">Můžete tyto zúčastněným [Další informace o rozdílech](../local-functions-vs-lambdas.md)</span><span class="sxs-lookup"><span data-stu-id="aa45b-318">Those interested can [read more about the differences](../local-functions-vs-lambdas.md)</span></span>

## <a name="more-expression-bodied-members"></a><span data-ttu-id="aa45b-319">Další výraz vozidlo členy</span><span class="sxs-lookup"><span data-stu-id="aa45b-319">More expression-bodied members</span></span>

<span data-ttu-id="aa45b-320">C# 6 zavedená [výraz vozidlo členy](csharp-6.md#expression-bodied-function-members) pro členské funkce a vlastnosti jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="aa45b-320">C# 6 introduced [expression-bodied members](csharp-6.md#expression-bodied-function-members) for member functions, and read-only properties.</span></span> <span data-ttu-id="aa45b-321">C# 7.0 rozšíří povolené členy, u nichž může být implementováno jako výrazy.</span><span class="sxs-lookup"><span data-stu-id="aa45b-321">C# 7.0 expands the allowed members that can be implemented as expressions.</span></span> <span data-ttu-id="aa45b-322">V jazyce C# 7.0, můžete implementovat *konstruktory*, *finalizační metody*, a `get` a `set` přístupových objektů na *vlastnosti* a *indexery* .</span><span class="sxs-lookup"><span data-stu-id="aa45b-322">In C# 7.0, you can implement *constructors*, *finalizers*, and `get` and `set` accessors on *properties* and *indexers*.</span></span> <span data-ttu-id="aa45b-323">Následující kód ukazuje příklady jednotlivých:</span><span class="sxs-lookup"><span data-stu-id="aa45b-323">The following code shows examples of each:</span></span>

[!code-csharp[ExpressionBodiedMembers](../../../samples/snippets/csharp/new-in-7/expressionmembers.cs#36_ExpressionBodiedEverything "new expression-bodied members")]

> [!NOTE]
> <span data-ttu-id="aa45b-324">Tento příklad nemusí finalizační metody, ale je zobrazen k předvedení syntaxe.</span><span class="sxs-lookup"><span data-stu-id="aa45b-324">This example does not need a finalizer, but it is shown to demonstrate the syntax.</span></span> <span data-ttu-id="aa45b-325">Finalizační metody by neměla implementovat v třídě, pokud to není nutné k uvolnění nespravovaných prostředků.</span><span class="sxs-lookup"><span data-stu-id="aa45b-325">You should not implement a finalizer in your class unless it is necessary to  release unmanaged resources.</span></span> <span data-ttu-id="aa45b-326">Měli byste také zvážit použití <xref:System.Runtime.InteropServices.SafeHandle> třída místo přímo Správa nespravovaných prostředků.</span><span class="sxs-lookup"><span data-stu-id="aa45b-326">You should also consider using the <xref:System.Runtime.InteropServices.SafeHandle> class instead of managing unmanaged resources directly.</span></span>

<span data-ttu-id="aa45b-327">Tato nová umístění pro výraz vozidlo členy představují důležité milník pro jazyk C#: tyto funkce, které byly implementovány členové komunity pracující na open-source [Roslyn](https://github.com/dotnet/Roslyn) projektu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-327">These new locations for expression-bodied members represent an important milestone for the C# language: These features were implemented by community members working on the open-source [Roslyn](https://github.com/dotnet/Roslyn) project.</span></span>

## <a name="throw-expressions"></a><span data-ttu-id="aa45b-328">Throw – výrazy</span><span class="sxs-lookup"><span data-stu-id="aa45b-328">Throw expressions</span></span>

<span data-ttu-id="aa45b-329">V jazyce C# `throw` byla vždy příkaz.</span><span class="sxs-lookup"><span data-stu-id="aa45b-329">In C#, `throw` has always been a statement.</span></span> <span data-ttu-id="aa45b-330">Protože `throw` je prohlášení, není výrazu, že byly konstrukce jazyka C#, kde nelze použít.</span><span class="sxs-lookup"><span data-stu-id="aa45b-330">Because `throw` is a statement, not an expression, there were C# constructs where you could not use it.</span></span> <span data-ttu-id="aa45b-331">Tyto zahrnuty podmíněné výrazy, null slučování výrazy a některé výrazy lambda.</span><span class="sxs-lookup"><span data-stu-id="aa45b-331">These included conditional expressions, null coalescing expressions, and some lambda expressions.</span></span> <span data-ttu-id="aa45b-332">Přidání členů výraz vozidlo přidá další umístění kde `throw` výrazy mohla být užitečná.</span><span class="sxs-lookup"><span data-stu-id="aa45b-332">The addition of expression-bodied members adds more locations where `throw` expressions would be useful.</span></span> <span data-ttu-id="aa45b-333">Tak, aby bylo možné napsat některé z těchto konstrukce, C# 7.0 zavádí *throw výrazy*.</span><span class="sxs-lookup"><span data-stu-id="aa45b-333">So that you can write any of these constructs, C# 7.0 introduces *throw expressions*.</span></span>

<span data-ttu-id="aa45b-334">Syntaxe je stejný, jako jste používali vždy pro `throw` příkazy.</span><span class="sxs-lookup"><span data-stu-id="aa45b-334">The syntax is the same as you've always used for `throw` statements.</span></span> <span data-ttu-id="aa45b-335">Jediným rozdílem je, že teď můžete umístit je nová umístění, jako třeba podmíněného výrazu:</span><span class="sxs-lookup"><span data-stu-id="aa45b-335">The only difference is that now you can place them in new locations, such as in a conditional expression:</span></span>

[!code-csharp[Throw_ExpressionExample](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#37_Throw_ExpressionExample "conditional throw expressions")]

<span data-ttu-id="aa45b-336">Tato funkce umožňuje pomocí throw výrazů ve výrazech inicializace:</span><span class="sxs-lookup"><span data-stu-id="aa45b-336">This features enables using throw expressions in initialization expressions:</span></span>

[!code-csharp[ThrowInInitialization](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#38_ThrowInInitialization "conditional throw expressions")]

<span data-ttu-id="aa45b-337">Tyto inicializacích by dříve, musí být v konstruktoru, s příkazech throw v těle konstruktoru:</span><span class="sxs-lookup"><span data-stu-id="aa45b-337">Previously, those initializations would need to be in a constructor, with the throw statements in the body of the constructor:</span></span>


[!code-csharp[ThrowInConstructor](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#39_ThrowInConstructor "throw statements")]

> [!NOTE]
> <span data-ttu-id="aa45b-338">Obě předchozí konstrukce způsobí, že výjimky vyvolání během vytváření objektu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-338">Both of the preceding constructs will cause exceptions to be thrown during the construction of an object.</span></span> <span data-ttu-id="aa45b-339">Ty jsou často obtížné zotavit.</span><span class="sxs-lookup"><span data-stu-id="aa45b-339">Those are often difficult to recover from.</span></span>
> <span data-ttu-id="aa45b-340">Z tohoto důvodu se nedoporučuje návrhů generující výjimky během vytváření.</span><span class="sxs-lookup"><span data-stu-id="aa45b-340">For that reason, designs that throw exceptions during construction are discouraged.</span></span>

## <a name="generalized-async-return-types"></a><span data-ttu-id="aa45b-341">Zobecněný asynchronní návratové typy</span><span class="sxs-lookup"><span data-stu-id="aa45b-341">Generalized async return types</span></span>

<span data-ttu-id="aa45b-342">Vrácení `Task` objekt z asynchronní metody můžou představovat kritické body v určité cesty.</span><span class="sxs-lookup"><span data-stu-id="aa45b-342">Returning a `Task` object from async methods can introduce performance bottlenecks in certain paths.</span></span> <span data-ttu-id="aa45b-343">`Task` je typu odkazu, takže ho pomocí znamená přidělování objekt.</span><span class="sxs-lookup"><span data-stu-id="aa45b-343">`Task` is a reference type, so using it means allocating an object.</span></span> <span data-ttu-id="aa45b-344">V případech, kde metodu deklarovat s `async` modifikátor vrátí výsledky uložené v mezipaměti, nebo dokončí synchronně, navíc přidělení se může stát déle náklady v kritické oddíly výkon kódu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-344">In cases where a method declared with the `async` modifier returns a cached result, or completes synchronously, the extra allocations can become a significant time cost in performance critical sections of code.</span></span> <span data-ttu-id="aa45b-345">Může být velmi náročná, pokud tyto přidělení, ke kterým došlo v úzkou smyčky.</span><span class="sxs-lookup"><span data-stu-id="aa45b-345">It can become very costly if those allocations occur in tight loops.</span></span>

<span data-ttu-id="aa45b-346">Nová funkce jazyka znamená, že asynchronní metody může vrátit jiné typy kromě `Task`, `Task<T>` a `void`.</span><span class="sxs-lookup"><span data-stu-id="aa45b-346">The new language feature means that async methods may return other types in addition to `Task`, `Task<T>` and `void`.</span></span> <span data-ttu-id="aa45b-347">Vrácený typ stále musí vyhovovat vzoru async znamená `GetAwaiter` metoda musí být přístupné.</span><span class="sxs-lookup"><span data-stu-id="aa45b-347">The returned type must still satisfy the async pattern, meaning a `GetAwaiter` method must be accessible.</span></span> <span data-ttu-id="aa45b-348">Například jeden konkrétní `ValueTask` do rozhraní .NET framework, aby se přidal typ využívají tato nová funkce jazyka:</span><span class="sxs-lookup"><span data-stu-id="aa45b-348">As one concrete example, the `ValueTask` type has been added to the .NET framework to make use of this new language feature:</span></span> 

[!code-csharp[UsingValueTask](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#30_UsingValueTask "Using ValueTask")]

> [!NOTE]
> <span data-ttu-id="aa45b-349">Je nutné přidat balíček NuGet [ `System.Threading.Tasks.Extensions` ](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) Chcete-li použít <xref:System.Threading.Tasks.ValueTask%601> typu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-349">You need to add the NuGet package [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) in order to use the <xref:System.Threading.Tasks.ValueTask%601> type.</span></span>

<span data-ttu-id="aa45b-350">Může být jednoduchý optimalizace použití `ValueTask` v místech, kde `Task` se použije před.</span><span class="sxs-lookup"><span data-stu-id="aa45b-350">A simple optimization would be to use `ValueTask` in places where `Task` would be used before.</span></span> <span data-ttu-id="aa45b-351">Ale pokud chcete provést další optimalizace ručně, můžete mezipaměti výsledky z asynchronní pracovní a znovu použít výsledek v následných volání.</span><span class="sxs-lookup"><span data-stu-id="aa45b-351">However, if you want to perform extra optimizations by hand, you can cache results from async work and reuse the result in subsequent calls.</span></span> <span data-ttu-id="aa45b-352">`ValueTask` Struktura neobsahuje konstruktor s `Task` parametr tak, že můžete vytvořit `ValueTask` z návratová hodnota všechny existující asynchronní metody:</span><span class="sxs-lookup"><span data-stu-id="aa45b-352">The `ValueTask` struct has a constructor with a `Task` parameter so that you can construct a `ValueTask` from the return value of any existing async method:</span></span>

[!code-csharp[AsyncOptimizedValueTask](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#31_AsyncOptimizedValueTask "Return async result or cached value")]
 
<span data-ttu-id="aa45b-353">Jak se všechna doporučení výkonu by měla otestovat obě verze před provedením velké škálování změny kódu.</span><span class="sxs-lookup"><span data-stu-id="aa45b-353">As with all performance recommendations, you should benchmark both versions before making large scale changes to your code.</span></span>

## <a name="numeric-literal-syntax-improvements"></a><span data-ttu-id="aa45b-354">Vylepšení číselný literál syntaxe</span><span class="sxs-lookup"><span data-stu-id="aa45b-354">Numeric literal syntax improvements</span></span>

<span data-ttu-id="aa45b-355">Číselné konstanty misreading může ztížit zjistit kód při čtení poprvé.</span><span class="sxs-lookup"><span data-stu-id="aa45b-355">Misreading numeric constants can make it harder to understand code when reading it for the first time.</span></span> <span data-ttu-id="aa45b-356">To často dochází, pokud tato čísla se používají jako bitové masky nebo jiných symbolický místo číselné hodnoty.</span><span class="sxs-lookup"><span data-stu-id="aa45b-356">This often occurs when those numbers are used as bit masks or other symbolic rather than numeric values.</span></span> <span data-ttu-id="aa45b-357">C# 7.0 obsahuje dvě nové funkce, aby bylo snazší zápis čísla způsobem nejvíce čitelný pro zamýšlené použití: *binární literály*, a *číslice oddělovačů*.</span><span class="sxs-lookup"><span data-stu-id="aa45b-357">C# 7.0 includes two new features to make it easier to write numbers in the most readable fashion for the intended use: *binary literals*, and *digit separators*.</span></span>

<span data-ttu-id="aa45b-358">Při vytváření bitové masky nebo kdykoli binární reprezentace číslo neposkytuje pro tyto časy kód nejvíce čtení, zápisu toto číslo v binárním:</span><span class="sxs-lookup"><span data-stu-id="aa45b-358">For those times when you are creating bit masks, or whenever a binary representation of a number makes the most readable code, write that number in binary:</span></span>

[!code-csharp[BinaryConstants](../../../samples/snippets/csharp/new-in-7/Program.cs#32_BinaryConstants "Binary constants")]

<span data-ttu-id="aa45b-359">`0b` Na začátku konstanta označuje, zda číslo je zapsána jako binární číslo.</span><span class="sxs-lookup"><span data-stu-id="aa45b-359">The `0b` at the beginning of the constant indicates that the number is written as a binary number.</span></span>

<span data-ttu-id="aa45b-360">Binární čísla můžete získat tak často je snazší zjistit vzory bit zavedením velmi náročná `_` jako oddělovač číslic:</span><span class="sxs-lookup"><span data-stu-id="aa45b-360">Binary numbers can get very long, so it's often easier to see the bit patterns by introducing the `_` as a digit separator:</span></span>

[!code-csharp[ThousandSeparators](../../../samples/snippets/csharp/new-in-7/Program.cs#33_ThousandSeparators "Thousands separators")]

<span data-ttu-id="aa45b-361">Oddělovač číslice může vyskytovat kdekoli v konstanta.</span><span class="sxs-lookup"><span data-stu-id="aa45b-361">The digit separator can appear anywhere in the constant.</span></span> <span data-ttu-id="aa45b-362">Pro základní 10 čísel, by se běžné můžete použít jako tisíců oddělovače:</span><span class="sxs-lookup"><span data-stu-id="aa45b-362">For base 10 numbers, it would be common to use it as a thousands separator:</span></span>

[!code-csharp[LargeIntegers](../../../samples/snippets/csharp/new-in-7/Program.cs#34_LargeIntegers "Large integer")]

<span data-ttu-id="aa45b-363">Oddělovač číslice lze použít s `decimal`, `float` a `double` také typy:</span><span class="sxs-lookup"><span data-stu-id="aa45b-363">The digit separator can be used with `decimal`, `float` and `double` types as well:</span></span>

[!code-csharp[OtherConstants](../../../samples/snippets/csharp/new-in-7/Program.cs#35_OtherConstants "non-integral constants")]

<span data-ttu-id="aa45b-364">Dohromady, můžou deklarovat číselné konstanty s lepší čitelnost.</span><span class="sxs-lookup"><span data-stu-id="aa45b-364">Taken together, you can declare numeric constants with much more readability.</span></span>
