---
title: Co je nového v jazyce C# 7.0 – průvodce v C#
description: Získejte přehled o nové funkce ve verzi 7.0 C# jazyka.
ms.date: 12/21/2016
ms.assetid: fd41596d-d0c2-4816-b94d-c4d00a5d0243
ms.openlocfilehash: 0646eaa999579e5347007dd71defcc643c19c7f9
ms.sourcegitcommit: 2b986afe4ce9e13bbeec929c9737757eb61de60e
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 02/22/2019
ms.locfileid: "56665079"
---
# <a name="whats-new-in-c-70"></a><span data-ttu-id="8462c-103">Co je nového v jazyce C# 7.0</span><span class="sxs-lookup"><span data-stu-id="8462c-103">What's new in C# 7.0</span></span>

<span data-ttu-id="8462c-104">C# 7.0 přidá několik nových funkcí jazyka C#:</span><span class="sxs-lookup"><span data-stu-id="8462c-104">C# 7.0 adds a number of new features to the C# language:</span></span>
* [<span data-ttu-id="8462c-105">`out` Proměnné</span><span class="sxs-lookup"><span data-stu-id="8462c-105">`out` variables</span></span>](#out-variables)
    - <span data-ttu-id="8462c-106">Je možné deklarovat `out` hodnoty inline jako argumentů metody, kde jsou použity.</span><span class="sxs-lookup"><span data-stu-id="8462c-106">You can declare `out` values inline as arguments to the method where they are used.</span></span>
* [<span data-ttu-id="8462c-107">Řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="8462c-107">Tuples</span></span>](#tuples)
    - <span data-ttu-id="8462c-108">Můžete vytvořit jednoduchý, Nepojmenované typy, které obsahují více veřejná pole.</span><span class="sxs-lookup"><span data-stu-id="8462c-108">You can create lightweight, unnamed types that contain multiple public fields.</span></span> <span data-ttu-id="8462c-109">Kompilátory a nástroje pro prostředí IDE pochopit sémantiku z těchto typů.</span><span class="sxs-lookup"><span data-stu-id="8462c-109">Compilers and IDE tools understand the semantics of these types.</span></span>
* [<span data-ttu-id="8462c-110">Zahození</span><span class="sxs-lookup"><span data-stu-id="8462c-110">Discards</span></span>](#discards)
    - <span data-ttu-id="8462c-111">Zahození jsou dočasné, jen pro zápis proměnné při nezáleží hodnota přiřazená v přiřazeních.</span><span class="sxs-lookup"><span data-stu-id="8462c-111">Discards are temporary, write-only variables used in assignments when you don't care about the value assigned.</span></span> <span data-ttu-id="8462c-112">Jsou zvláště užitečné při dekonstrukce řazených kolekcí členů a uživatelem definovaných typů, stejně jako při volání metody s `out` parametry.</span><span class="sxs-lookup"><span data-stu-id="8462c-112">They are particularly useful when deconstructing tuples and user-defined types, as well as when calling methods with `out` parameters.</span></span>
* [<span data-ttu-id="8462c-113">Porovnávání vzorů</span><span class="sxs-lookup"><span data-stu-id="8462c-113">Pattern Matching</span></span>](#pattern-matching)
    - <span data-ttu-id="8462c-114">Můžete vytvořit na základě libovolné typy a hodnoty členů z těchto typů logiky větvení.</span><span class="sxs-lookup"><span data-stu-id="8462c-114">You can create branching logic based on arbitrary types and values of the members of those types.</span></span>
* [<span data-ttu-id="8462c-115">`ref` místní hodnoty a vrátí</span><span class="sxs-lookup"><span data-stu-id="8462c-115">`ref` locals and returns</span></span>](#ref-locals-and-returns)
    - <span data-ttu-id="8462c-116">Metoda lokálních proměnných a návratovými hodnotami může být odkazy na další úložiště.</span><span class="sxs-lookup"><span data-stu-id="8462c-116">Method local variables and return values can be references to other storage.</span></span>
* [<span data-ttu-id="8462c-117">Lokální funkce</span><span class="sxs-lookup"><span data-stu-id="8462c-117">Local Functions</span></span>](#local-functions)
    - <span data-ttu-id="8462c-118">Je možné vnořovat funkce do jiných funkcí a omezit jejich rozsah a viditelnost.</span><span class="sxs-lookup"><span data-stu-id="8462c-118">You can nest functions inside other functions to limit their scope and visibility.</span></span>
* [<span data-ttu-id="8462c-119">Více členů s výrazem v těle</span><span class="sxs-lookup"><span data-stu-id="8462c-119">More expression-bodied members</span></span>](#more-expression-bodied-members)
    - <span data-ttu-id="8462c-120">Seznam členů, které mohou být vytvořeny pomocí výrazů zvětšil na velikost.</span><span class="sxs-lookup"><span data-stu-id="8462c-120">The list of members that can be authored using expressions has grown.</span></span>
* [<span data-ttu-id="8462c-121">`throw` Výrazy</span><span class="sxs-lookup"><span data-stu-id="8462c-121">`throw` Expressions</span></span>](#throw-expressions)
    - <span data-ttu-id="8462c-122">Může vyvolat výjimky v konstruktorech kódu, které dříve nebyly povoleny, protože `throw` byl příkaz.</span><span class="sxs-lookup"><span data-stu-id="8462c-122">You can throw exceptions in code constructs that previously were not allowed because `throw` was a statement.</span></span> 
* [<span data-ttu-id="8462c-123">Zobecněný asynchronní návratové typy</span><span class="sxs-lookup"><span data-stu-id="8462c-123">Generalized async return types</span></span>](#generalized-async-return-types)
    - <span data-ttu-id="8462c-124">Metody deklarované s `async` modifikátor může vrátit jiné typy kromě `Task` a `Task<T>`.</span><span class="sxs-lookup"><span data-stu-id="8462c-124">Methods declared with the `async` modifier can return other types in addition to `Task` and `Task<T>`.</span></span>
* [<span data-ttu-id="8462c-125">Vylepšení číselný literál syntaxe</span><span class="sxs-lookup"><span data-stu-id="8462c-125">Numeric literal syntax improvements</span></span>](#numeric-literal-syntax-improvements)
    - <span data-ttu-id="8462c-126">Nové tokeny zlepšit čitelnost pro číselné konstanty.</span><span class="sxs-lookup"><span data-stu-id="8462c-126">New tokens improve readability for numeric constants.</span></span>

<span data-ttu-id="8462c-127">Zbývající část tohoto tématu popisuje všechny funkce.</span><span class="sxs-lookup"><span data-stu-id="8462c-127">The remainder of this topic discusses each of the features.</span></span> <span data-ttu-id="8462c-128">Pro jednotlivé funkce dozvíte zdůvodnění.</span><span class="sxs-lookup"><span data-stu-id="8462c-128">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="8462c-129">Dozvíte syntaxe.</span><span class="sxs-lookup"><span data-stu-id="8462c-129">You'll learn the syntax.</span></span> <span data-ttu-id="8462c-130">Zobrazí se vám několik ukázkových scénářů, ve kterém pomocí nové funkce se budete produktivnější jako vývojář.</span><span class="sxs-lookup"><span data-stu-id="8462c-130">You'll see some sample scenarios where using the new feature will make you more productive as a developer.</span></span> 

## <a name="out-variables"></a><span data-ttu-id="8462c-131">`out` Proměnné</span><span class="sxs-lookup"><span data-stu-id="8462c-131">`out` variables</span></span>

<span data-ttu-id="8462c-132">Existující syntaxi, která podporuje `out` v této verzi jsme vylepšili parametry.</span><span class="sxs-lookup"><span data-stu-id="8462c-132">The existing syntax that supports `out` parameters has been improved in this version.</span></span>  

<span data-ttu-id="8462c-133">Dříve je třeba oddělit deklarace proměnné out a jeho inicializaci na dva různé příkazy:</span><span class="sxs-lookup"><span data-stu-id="8462c-133">Previously, you would need to separate the declaration of the out variable and its initialization into two different statements:</span></span>

[!code-csharp[OutVariableOldStyle](../../../samples/snippets/csharp/new-in-7/program.cs#03_OutVariableOldStyle "classic out variable declaration")]

<span data-ttu-id="8462c-134">Nyní můžete deklarovat `out` proměnných v seznamu argumentů volání metody místo psaní příkazu samostatné deklarace:</span><span class="sxs-lookup"><span data-stu-id="8462c-134">You can now declare `out` variables in the argument list of a method call, rather than writing a separate declaration statement:</span></span>

[!code-csharp[OutVariableDeclarations](../../../samples/snippets/csharp/new-in-7/program.cs#01_OutVariableDeclarations "Out variable declarations")]

<span data-ttu-id="8462c-135">Možná budete chtít zadat typ `out` proměnné pro přehlednost, jak je znázorněno výše.</span><span class="sxs-lookup"><span data-stu-id="8462c-135">You may want to specify the type of the `out` variable for clarity, as shown above.</span></span> <span data-ttu-id="8462c-136">Však jazyk podporuje použití implicitně typovaná lokální proměnná:</span><span class="sxs-lookup"><span data-stu-id="8462c-136">However, the language does support using an implicitly typed local variable:</span></span>

[!code-csharp[OutVarVariableDeclarations](../../../samples/snippets/csharp/new-in-7/program.cs#02_OutVarVariableDeclarations "Implicitly typed Out variable")]

* <span data-ttu-id="8462c-137">Kód je lépe čitelný.</span><span class="sxs-lookup"><span data-stu-id="8462c-137">The code is easier to read.</span></span> 
    - <span data-ttu-id="8462c-138">Deklarujete proměnnou mimo, kde má být použito, ne na další řádek výše.</span><span class="sxs-lookup"><span data-stu-id="8462c-138">You declare the out variable where you use it, not on another line above.</span></span>
* <span data-ttu-id="8462c-139">Už není potřeba přiřadit počáteční hodnotu.</span><span class="sxs-lookup"><span data-stu-id="8462c-139">No need to assign an initial value.</span></span>
    - <span data-ttu-id="8462c-140">Deklarací `out` proměnné, kde se používá ve volání metody, nemůžete použít omylem ho dřív, než je přiřadí.</span><span class="sxs-lookup"><span data-stu-id="8462c-140">By declaring the `out` variable where it is used in a method call, you can't accidentally use it before it is assigned.</span></span>

<span data-ttu-id="8462c-141">Nejběžnějším využitím této funkce bude `Try` vzor.</span><span class="sxs-lookup"><span data-stu-id="8462c-141">The most common use for this feature will be the `Try` pattern.</span></span> <span data-ttu-id="8462c-142">V tomto vzoru, metoda vrátí `bool` udávající úspěch nebo neúspěch a `out` proměnné, která obsahuje výsledek, pokud metoda uspěje.</span><span class="sxs-lookup"><span data-stu-id="8462c-142">In this pattern, a method returns a `bool` indicating success or failure and an `out` variable that provides the result if the method succeeds.</span></span>

<span data-ttu-id="8462c-143">Při použití `out` deklarace proměnné deklarované proměnné "nevracení" do vnějšího rozsahu if – příkaz.</span><span class="sxs-lookup"><span data-stu-id="8462c-143">When using the `out` variable declaration, the declared variable "leaks" into the outer scope of the if statement.</span></span> <span data-ttu-id="8462c-144">Díky tomu můžete použít později:</span><span class="sxs-lookup"><span data-stu-id="8462c-144">This allows you to use the variable afterwards:</span></span>

```csharp
if (!int.TryParse(input, out int result))
{    
    return null;
}

return result;
```

## <a name="tuples"></a><span data-ttu-id="8462c-145">N-tice</span><span class="sxs-lookup"><span data-stu-id="8462c-145">Tuples</span></span>

> [!NOTE]
> <span data-ttu-id="8462c-146">Vyžadovat nových funkcí řazených kolekcí členů <xref:System.ValueTuple> typy.</span><span class="sxs-lookup"><span data-stu-id="8462c-146">The new tuples features require the <xref:System.ValueTuple> types.</span></span>
> <span data-ttu-id="8462c-147">Je nutné přidat balíček NuGet [ `System.ValueTuple` ](https://www.nuget.org/packages/System.ValueTuple/) aby bylo možné používat na platformách, které neobsahují typy.</span><span class="sxs-lookup"><span data-stu-id="8462c-147">You must add the NuGet package [`System.ValueTuple`](https://www.nuget.org/packages/System.ValueTuple/) in order to use it on platforms that do not include the types.</span></span>
>
> <span data-ttu-id="8462c-148">To se podobá další jazykové funkce, které využívají typy v rozhraní framework.</span><span class="sxs-lookup"><span data-stu-id="8462c-148">This is similar to other language features that rely on types delivered in the framework.</span></span> <span data-ttu-id="8462c-149">Příklad zahrnují `async` a `await` spoléhat na `INotifyCompletion` rozhraní a LINQ spoléhat na `IEnumerable<T>`.</span><span class="sxs-lookup"><span data-stu-id="8462c-149">Example include `async` and `await` relying on the `INotifyCompletion` interface, and LINQ relying on `IEnumerable<T>`.</span></span> <span data-ttu-id="8462c-150">Jak .NET se mění na další platformy, které jsou nezávislé, mechanismus doručování mění.</span><span class="sxs-lookup"><span data-stu-id="8462c-150">However, the delivery mechanism is changing as .NET is becoming more platform independent.</span></span> <span data-ttu-id="8462c-151">Rozhraní .NET Framework nemusí vždy odeslání na stejném tempo jako kompilátor jazyka.</span><span class="sxs-lookup"><span data-stu-id="8462c-151">The .NET Framework may not always ship on the same cadence as the language compiler.</span></span> <span data-ttu-id="8462c-152">Nové funkce jazyků využívají nové typy, typy nebudou mít k dispozici jako balíčků NuGet při dodávání funkcí jazyka.</span><span class="sxs-lookup"><span data-stu-id="8462c-152">When new language features rely on new types, those types will be available as NuGet packages when the language features ship.</span></span> <span data-ttu-id="8462c-153">Tyto nové typy získat přidána do rozhraní API .NET Standard a dodávána jako součást rozhraní framework, odeberou se požadavek na balíček NuGet.</span><span class="sxs-lookup"><span data-stu-id="8462c-153">As these new types get added to the .NET Standard API and delivered as part of the framework, the NuGet package requirement will be removed.</span></span>

<span data-ttu-id="8462c-154">C# poskytuje bohaté syntaxi pro třídy a struktury, které slouží k vysvětlení máte v úmyslu návrhu.</span><span class="sxs-lookup"><span data-stu-id="8462c-154">C# provides a rich syntax for classes and structs that is used to explain your design intent.</span></span> <span data-ttu-id="8462c-155">Ale někdy bohaté syntaxe vyžaduje další práci s minimálními výhodu.</span><span class="sxs-lookup"><span data-stu-id="8462c-155">But sometimes that rich syntax requires extra work with minimal benefit.</span></span> <span data-ttu-id="8462c-156">Často může zapisovat metody, které vyžadují jednoduchou strukturu obsahující více než jeden datový prvek.</span><span class="sxs-lookup"><span data-stu-id="8462c-156">You may often write methods that need a simple structure containing more than one data element.</span></span> <span data-ttu-id="8462c-157">Pro podporu těchto scénářů *řazených kolekcí členů* byly přidány do jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="8462c-157">To support these scenarios *tuples* were added to C#.</span></span> <span data-ttu-id="8462c-158">Řazené kolekce členů jsou jednoduché datové struktury, které obsahují více polí k reprezentaci datové členy.</span><span class="sxs-lookup"><span data-stu-id="8462c-158">Tuples are lightweight data structures that contain multiple fields to represent the data members.</span></span>
<span data-ttu-id="8462c-159">Pole pořadí úloh se neověřuje, a nelze definovat vlastní metody</span><span class="sxs-lookup"><span data-stu-id="8462c-159">The fields are not validated, and you cannot define your own methods</span></span>

> [!NOTE]
> <span data-ttu-id="8462c-160">Řazené kolekce členů byly k dispozici před C# 7.0, ale bylo neefektivní a měl neexistuje jazyková podpora.</span><span class="sxs-lookup"><span data-stu-id="8462c-160">Tuples were available before C# 7.0, but they were inefficient and had no language support.</span></span>
> <span data-ttu-id="8462c-161">To znamená, že elementů řazené kolekce členů mohou být odkazovány pouze jako `Item1`, `Item2` a tak dále.</span><span class="sxs-lookup"><span data-stu-id="8462c-161">This meant that tuple elements could only be referenced as `Item1`, `Item2` and so on.</span></span> <span data-ttu-id="8462c-162">C# 7.0 zavádí podporu jazyka pro řazené kolekce členů, umožňující sémantické názvy polí pomocí nové, efektivnější řazené kolekce členů typy řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="8462c-162">C# 7.0 introduces language support for tuples, which enables semantic names for the fields of a tuple using new, more efficient tuple types.</span></span>

<span data-ttu-id="8462c-163">Řazená kolekce členů můžete vytvořit tak, že přiřadíte hodnotu k jednotlivým členům:</span><span class="sxs-lookup"><span data-stu-id="8462c-163">You can create a tuple by assigning a value to each member:</span></span>

[!code-csharp[UnnamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#04_UnnamedTuple "Unnamed tuple")]

<span data-ttu-id="8462c-164">Který vytvoří přiřazení řazené kolekce členů obsahující `Item1` a `Item2`, který můžete použít stejným způsobem jako <xref:System.Tuple> změníte syntaxe pro vytvoření n-tice, která poskytuje sémantické názvy pro každého člena řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="8462c-164">That assignment creates a tuple whose members are `Item1` and `Item2`, which you can use in the same way as <xref:System.Tuple> You can change the syntax to create a tuple that provides semantic names to each of the members of the tuple:</span></span>

[!code-csharp[NamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#05_NamedTuple "Named tuple")]

<span data-ttu-id="8462c-165">`namedLetters` Řazené kolekce členů obsahuje pole, které jsou označovány jako `Alpha` a `Beta`.</span><span class="sxs-lookup"><span data-stu-id="8462c-165">The `namedLetters` tuple contains fields referred to as `Alpha` and `Beta`.</span></span> <span data-ttu-id="8462c-166">Tyto názvy existují pouze v době kompilace a není zachováno například při kontrole řazené kolekce členů v době běhu pomocí operace reflection.</span><span class="sxs-lookup"><span data-stu-id="8462c-166">Those names exist only at compile time and are not preserved for example when inspecting the tuple using reflection at runtime.</span></span>

<span data-ttu-id="8462c-167">V přiřazení řazené kolekce členů můžete také zadat názvy polí na pravé straně přiřazení:</span><span class="sxs-lookup"><span data-stu-id="8462c-167">In a tuple assignment, you can also specify the names of the fields on the right-hand side of the assignment:</span></span>

[!code-csharp[ImplicitNamedTuple](../../../samples/snippets/csharp/new-in-7/program.cs#06_ImplicitNamedTuple "Implicitly named tuple")]

<span data-ttu-id="8462c-168">Na levé a pravé straně přiřazení je možné zadat názvy pro pole:</span><span class="sxs-lookup"><span data-stu-id="8462c-168">You can specify names for the fields on both the left and right-hand side of the assignment:</span></span>

[!code-csharp[NamedTupleConflict](../../../samples/snippets/csharp/new-in-7/program.cs#07_NamedTupleConflict "Named tuple conflict")]

<span data-ttu-id="8462c-169">Řádek nad generuje upozornění, `CS8123`, o tom, že názvy na pravé straně přiřazení `Alpha` a `Beta` budou ignorováni, protože jsou v konfliktu s názvy na levé straně `First` a `Second`.</span><span class="sxs-lookup"><span data-stu-id="8462c-169">The line above generates a warning, `CS8123`, telling you that the names on the right side of the assignment, `Alpha` and `Beta` are ignored because they conflict with the names on the left side, `First` and `Second`.</span></span>

<span data-ttu-id="8462c-170">Výše uvedené příklady ukazují základní syntaxe pro deklaraci řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="8462c-170">The examples above show the basic syntax to declare tuples.</span></span> <span data-ttu-id="8462c-171">Řazené kolekce členů jsou nejužitečnější jako typy vracených hodnot pro `private` a `internal` metody.</span><span class="sxs-lookup"><span data-stu-id="8462c-171">Tuples are most useful as return types for `private` and `internal` methods.</span></span> <span data-ttu-id="8462c-172">Řazené kolekce členů poskytují jednoduché syntaxi pro tyto metody k vrácení více diskrétní hodnoty: Uložit práci při vytváření `class` nebo `struct` , který definuje typ vrácený.</span><span class="sxs-lookup"><span data-stu-id="8462c-172">Tuples provide a simple syntax for those methods to return multiple discrete values: You save the work of authoring a `class` or a `struct` that defines the type returned.</span></span> <span data-ttu-id="8462c-173">Není nutné pro vytvoření nového typu.</span><span class="sxs-lookup"><span data-stu-id="8462c-173">There is no need for creating a new type.</span></span>

<span data-ttu-id="8462c-174">Vytvoření řazené kolekce členů je efektivnější a zvýšit produktivitu práce.</span><span class="sxs-lookup"><span data-stu-id="8462c-174">Creating a tuple is more efficient and more productive.</span></span>
<span data-ttu-id="8462c-175">Je jednodušší a jednoduché syntaxe pro definování datová struktura, která provede více než jednu hodnotu.</span><span class="sxs-lookup"><span data-stu-id="8462c-175">It is a simpler, lightweight syntax to define a data structure that carries more than one value.</span></span> <span data-ttu-id="8462c-176">Následující příklad metoda vrátí minimální a maximální hodnoty nalezené v posloupnost celých čísel:</span><span class="sxs-lookup"><span data-stu-id="8462c-176">The example method below returns the minimum and maximum values found in a sequence of integers:</span></span>

[!code-csharp[TupleReturningMethod](../../../samples/snippets/csharp/new-in-7/program.cs#08_TupleReturningMethod "Tuple returning method")]

<span data-ttu-id="8462c-177">Použití řazených kolekcí členů tímto způsobem nabízí několik výhod:</span><span class="sxs-lookup"><span data-stu-id="8462c-177">Using tuples in this way offers several advantages:</span></span>

* <span data-ttu-id="8462c-178">Uložit práci při vytváření `class` nebo `struct` , který definuje typ vrácený.</span><span class="sxs-lookup"><span data-stu-id="8462c-178">You save the work of authoring a `class` or a `struct` that defines the type returned.</span></span> 
* <span data-ttu-id="8462c-179">Chcete-li vytvořit nový typ nepotřebujete.</span><span class="sxs-lookup"><span data-stu-id="8462c-179">You do not need to create new type.</span></span>
* <span data-ttu-id="8462c-180">Vylepšení jazyka eliminuje nutnost volání <xref:System.Tuple.Create``1(``0)> metody.</span><span class="sxs-lookup"><span data-stu-id="8462c-180">The language enhancements removes the need to call the <xref:System.Tuple.Create``1(``0)> methods.</span></span>

<span data-ttu-id="8462c-181">Deklarace metody poskytuje názvy pro pole řazená kolekce členů, která je vrácena.</span><span class="sxs-lookup"><span data-stu-id="8462c-181">The declaration for the method provides the names for the fields of the tuple that is returned.</span></span> <span data-ttu-id="8462c-182">Při volání metody, vrácená hodnota je řazené kolekce členů, jejichž pole jsou `Max` a `Min`:</span><span class="sxs-lookup"><span data-stu-id="8462c-182">When you call the method, the return value is a tuple whose fields are `Max` and `Min`:</span></span>

[!code-csharp[CallingTupleMethod](../../../samples/snippets/csharp/new-in-7/program.cs#09_CallingTupleMethod "Calling a tuple returning method")]

<span data-ttu-id="8462c-183">Může nastat situace, kdy budete chtít rozbalit členy řazené kolekce členů, které byly vráceny z metody.</span><span class="sxs-lookup"><span data-stu-id="8462c-183">There may be times when you want to unpackage the members of a tuple that were returned from a method.</span></span>  <span data-ttu-id="8462c-184">Můžete to udělat deklarací samostatné proměnných pro všechny hodnoty řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="8462c-184">You can do that by declaring separate variables for each of the values in the tuple.</span></span> <span data-ttu-id="8462c-185">Tento postup se nazývá *dekonstrukce* řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="8462c-185">This is called *deconstructing* the tuple:</span></span>

[!code-csharp[CallingWithDeconstructor](../../../samples/snippets/csharp/new-in-7/program.cs#10_CallingWithDeconstructor "Deconstructing a tuple")]

<span data-ttu-id="8462c-186">Můžete také zadat podobné dekonstrukce pro jakýkoli typ v rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="8462c-186">You can also provide a similar deconstruction for any type in .NET.</span></span> <span data-ttu-id="8462c-187">Je to realizuje zápisem `Deconstruct` metoda jako člen třídy.</span><span class="sxs-lookup"><span data-stu-id="8462c-187">This is done by writing a `Deconstruct` method as a member of the class.</span></span> <span data-ttu-id="8462c-188">Že `Deconstruct` metoda poskytuje sadu `out` argumenty pro každou z vlastností, které mají být extrahovány.</span><span class="sxs-lookup"><span data-stu-id="8462c-188">That `Deconstruct` method provides a set of `out` arguments for each of the properties you want to extract.</span></span> <span data-ttu-id="8462c-189">Zvažte proto `Point` třída, která poskytuje deconstructor metodu, která extrahuje `X` a `Y` souřadnice:</span><span class="sxs-lookup"><span data-stu-id="8462c-189">Consider this `Point` class that provides a deconstructor method that extracts the `X` and `Y` coordinates:</span></span>

[!code-csharp[PointWithDeconstruction](../../../samples/snippets/csharp/new-in-7/point.cs#11_PointWithDeconstruction "Point with deconstruction method")]
 
<span data-ttu-id="8462c-190">Můžete extrahovat jednotlivá pole pomocí přiřazení `Point` k řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="8462c-190">You can extract the individual fields by assigning a `Point` to a tuple:</span></span>

[!code-csharp[DeconstructPoint](../../../samples/snippets/csharp/new-in-7/program.cs#12_DeconstructPoint "Deconstruct a point")]

<span data-ttu-id="8462c-191">Názvy definované v nejsou vázány `Deconstruct` metody.</span><span class="sxs-lookup"><span data-stu-id="8462c-191">You are not bound by the names defined in the `Deconstruct` method.</span></span> <span data-ttu-id="8462c-192">Extrakce proměnné můžete přejmenovat jako součást přiřazení:</span><span class="sxs-lookup"><span data-stu-id="8462c-192">You can rename the extract variables as part of the assignment:</span></span>  

[!code-csharp[DeconstructNames](../../../samples/snippets/csharp/new-in-7/program.cs#13_DeconstructNames "Deconstruct with new names")]

<span data-ttu-id="8462c-193">Další podrobnosti v o řazenými kolekcemi členů v [řazených kolekcí členů tématu](../tuples.md).</span><span class="sxs-lookup"><span data-stu-id="8462c-193">You can learn more in depth about tuples in the [tuples topic](../tuples.md).</span></span>

## <a name="discards"></a><span data-ttu-id="8462c-194">Zahození</span><span class="sxs-lookup"><span data-stu-id="8462c-194">Discards</span></span>

<span data-ttu-id="8462c-195">Při často dekonstrukce řazené kolekce členů nebo volání metody s `out` parametry, budete muset definovat proměnnou jehož hodnota nezáleží a používat.</span><span class="sxs-lookup"><span data-stu-id="8462c-195">Often when deconstructing a tuple or calling a method with `out` parameters, you're forced to define a variable whose value you don't care about and don't intend to use.</span></span> <span data-ttu-id="8462c-196">C# přidává podporu pro *zahodí* tohoto scénáře.</span><span class="sxs-lookup"><span data-stu-id="8462c-196">C# adds support for *discards* to handle this scenario.</span></span> <span data-ttu-id="8462c-197">Zahození je proměnná jen pro zápis, jehož název je `_` (podtržítko); můžete přiřadit všechny hodnoty, které máte v úmyslu zahodit do jedné proměnné.</span><span class="sxs-lookup"><span data-stu-id="8462c-197">A discard is a write-only variable whose name is `_` (the underscore character); you can assign all of the values that you intend to discard to the single variable.</span></span> <span data-ttu-id="8462c-198">Zahození je stejná jako nepřiřazené proměnné. Kromě příkazu přiřazení nelze použít zahození v kódu.</span><span class="sxs-lookup"><span data-stu-id="8462c-198">A discard is like an unassigned variable; apart from the assignment statement, the discard can't be used in code.</span></span>

<span data-ttu-id="8462c-199">Zahození jsou podporovány v následujících scénářích:</span><span class="sxs-lookup"><span data-stu-id="8462c-199">Discards are supported in the following scenarios:</span></span>

* <span data-ttu-id="8462c-200">Když dekonstrukce řazených kolekcí členů nebo uživatelem definované typy.</span><span class="sxs-lookup"><span data-stu-id="8462c-200">When deconstructing tuples or user-defined types.</span></span>

* <span data-ttu-id="8462c-201">Při volání metody s [si](../language-reference/keywords/out-parameter-modifier.md) parametry.</span><span class="sxs-lookup"><span data-stu-id="8462c-201">When calling methods with [out](../language-reference/keywords/out-parameter-modifier.md) parameters.</span></span>

* <span data-ttu-id="8462c-202">Ve vzorku s odpovídajícím operaci s [je](../language-reference/keywords/is.md) a [přepnout](../language-reference/keywords/switch.md) příkazy.</span><span class="sxs-lookup"><span data-stu-id="8462c-202">In a pattern matching operation with the [is](../language-reference/keywords/is.md) and [switch](../language-reference/keywords/switch.md) statements.</span></span>

* <span data-ttu-id="8462c-203">Pokud chcete explicitně Identifikujte jako samostatné identifikátor hodnotu přiřazení jako zahození.</span><span class="sxs-lookup"><span data-stu-id="8462c-203">As a standalone identifier when you want to explicitly identify the value of an assignment as a discard.</span></span>

<span data-ttu-id="8462c-204">Následující příklad definuje `QueryCityDataForYears` metodu, která vrátí 6-n-tice, která obsahuje data pro město pro dvě různé roky.</span><span class="sxs-lookup"><span data-stu-id="8462c-204">The following example defines a `QueryCityDataForYears` method that returns a 6-tuple that contains a data for a city for two different years.</span></span> <span data-ttu-id="8462c-205">Volání metody v příkladu se týká pouze s hodnotami dva naplnění vrácený metodou a proto zpracuje zbývající hodnoty řazené kolekce členů jako zahodí při deconstructs řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="8462c-205">The method call in the example is concerned only with the two population values returned by the method and so treats the remaining values in the tuple as discards when it deconstructs the tuple.</span></span>

[!code-csharp[Tuple-discard](../../../samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="8462c-206">Další informace najdete v tématu [zahodí](../discards.md).</span><span class="sxs-lookup"><span data-stu-id="8462c-206">For more information, see [Discards](../discards.md).</span></span>

## <a name="pattern-matching"></a><span data-ttu-id="8462c-207">Porovnávání vzorů</span><span class="sxs-lookup"><span data-stu-id="8462c-207">Pattern matching</span></span>

<span data-ttu-id="8462c-208">*Porovnávání vzorů* je funkce, která umožňuje implementovat metodu odeslání na vlastnosti jiné než typu objektu.</span><span class="sxs-lookup"><span data-stu-id="8462c-208">*Pattern matching* is a feature that allows you to implement method dispatch on properties other than the type of an object.</span></span> <span data-ttu-id="8462c-209">Už pravděpodobně již znáte odesílání metodu na základě typu objektu.</span><span class="sxs-lookup"><span data-stu-id="8462c-209">You're probably already familiar with method dispatch based on the type of an object.</span></span> <span data-ttu-id="8462c-210">Objektově orientované programování, virtuální a přepsání metody poskytují syntaxe jazyka pro implementaci metody odesílání na základě typu objektu.</span><span class="sxs-lookup"><span data-stu-id="8462c-210">In Object Oriented programming, virtual and override methods provide language syntax to implement method dispatching based on an object's type.</span></span> <span data-ttu-id="8462c-211">Základní a odvozené třídy poskytují tak různé implementace.</span><span class="sxs-lookup"><span data-stu-id="8462c-211">Base and Derived classes provide different implementations.</span></span> <span data-ttu-id="8462c-212">Výrazy odpovídající vzor rozšiřte tento koncept tak, že je možné snadno implementovat podobné vzory odeslání pro typy a data prvky, které spolu nesouvisí prostřednictvím hierarchie dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="8462c-212">Pattern matching expressions extend this concept so that you can easily implement similar dispatch patterns for types and data elements that are not related through an inheritance hierarchy.</span></span> 

<span data-ttu-id="8462c-213">Porovnávání vzorů podporuje `is` výrazy a `switch` výrazy.</span><span class="sxs-lookup"><span data-stu-id="8462c-213">Pattern matching supports `is` expressions and `switch` expressions.</span></span> <span data-ttu-id="8462c-214">Každý umožňuje kontrolu objektu a vlastností určíte, pokud daný objekt splňuje hledané kombinaci vzoru.</span><span class="sxs-lookup"><span data-stu-id="8462c-214">Each enables inspecting an object and its properties to determine if that object satisfies the sought pattern.</span></span> <span data-ttu-id="8462c-215">Můžete použít `when` – klíčové slovo lze určit další pravidla se vzorem.</span><span class="sxs-lookup"><span data-stu-id="8462c-215">You use the `when` keyword to specify additional rules to the pattern.</span></span>

### <a name="is-expression"></a><span data-ttu-id="8462c-216">`is` Výraz</span><span class="sxs-lookup"><span data-stu-id="8462c-216">`is` expression</span></span>

<span data-ttu-id="8462c-217">`is` Výraz vzoru rozšiřuje známé [ `is` operátor](../language-reference/keywords/is.md#pattern-matching-with-is) k dotazování nad rámec jeho typ objektu.</span><span class="sxs-lookup"><span data-stu-id="8462c-217">The `is` pattern expression extends the familiar [`is` operator](../language-reference/keywords/is.md#pattern-matching-with-is) to query an object beyond its type.</span></span>

<span data-ttu-id="8462c-218">Začněme pomocí jednoduchého scénáře.</span><span class="sxs-lookup"><span data-stu-id="8462c-218">Let's start with a simple scenario.</span></span> <span data-ttu-id="8462c-219">Přidáme možnosti pro tento scénář, které ukazují, jak vytvořit odpovídající výrazy vzor algoritmy, které pracují s nesouvisejících typů jednoduché.</span><span class="sxs-lookup"><span data-stu-id="8462c-219">We'll add capabilities to this scenario that demonstrate how pattern matching expressions make algorithms that work with unrelated types easy.</span></span> <span data-ttu-id="8462c-220">Začneme s metodou, která vypočítá součet počtu kostka zobrazí:</span><span class="sxs-lookup"><span data-stu-id="8462c-220">We'll start with a method that computes the sum of a number of die rolls:</span></span>

[!code-csharp[SumDieRolls](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#14_SumDieRolls "Sum die rolls")]

<span data-ttu-id="8462c-221">Můžete rychle zjistit, že je potřeba najít součet kostka zobrazí část zobrazí po provedena s emulátorem dice více (dice je plural kostka).</span><span class="sxs-lookup"><span data-stu-id="8462c-221">You might quickly find that you need to find the sum of die rolls where some of the rolls are made with multiple dice (dice is the plural of die).</span></span> <span data-ttu-id="8462c-222">Část vstupní sekvence může být více výsledků namísto jedno číslo:</span><span class="sxs-lookup"><span data-stu-id="8462c-222">Part of the input sequence may be multiple results instead of a single number:</span></span>

[!code-csharp[SumDieRollsWithGroups](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#15_SumDieRollsWithGroups "Sum die rolls with groups")]

<span data-ttu-id="8462c-223">`is` Výraz vzoru poměrně dobře funguje v tomto scénáři.</span><span class="sxs-lookup"><span data-stu-id="8462c-223">The `is` pattern expression works quite well in this scenario.</span></span> <span data-ttu-id="8462c-224">Jako součást kontrolu typu můžete zapsat inicializaci proměnné.</span><span class="sxs-lookup"><span data-stu-id="8462c-224">As part of checking the type, you write a variable initialization.</span></span> <span data-ttu-id="8462c-225">Tím se vytvoří novou proměnnou typu ověřené modulu runtime.</span><span class="sxs-lookup"><span data-stu-id="8462c-225">This creates a new variable of the validated runtime type.</span></span>

<span data-ttu-id="8462c-226">Jak zachovat rozšíření scénářů, může být pro vás vytvořit více `if` a `else if` příkazy.</span><span class="sxs-lookup"><span data-stu-id="8462c-226">As you keep extending these scenarios, you may find that you build more `if` and `else if` statements.</span></span> <span data-ttu-id="8462c-227">Po, který nepraktický, budete pravděpodobně chtít přepnout na `switch` vzorku výrazy.</span><span class="sxs-lookup"><span data-stu-id="8462c-227">Once that becomes unwieldy, you'll likely want to switch to `switch` pattern expressions.</span></span>

### <a name="switch-statement-updates"></a><span data-ttu-id="8462c-228">`switch` aktualizace – příkaz</span><span class="sxs-lookup"><span data-stu-id="8462c-228">`switch` statement updates</span></span>

<span data-ttu-id="8462c-229">*Odpovídat výrazu* má známou syntaxi, na základě `switch` příkaz již součástí jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="8462c-229">The *match expression* has a familiar syntax, based on the `switch` statement already part of the C# language.</span></span> <span data-ttu-id="8462c-230">Umožňuje převést existující kód, který použije odpovídající výraz před přidáním nové případy:</span><span class="sxs-lookup"><span data-stu-id="8462c-230">Let's translate the existing code to use a match expression before adding new cases:</span></span> 

[!code-csharp[SumUsingSwitch](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#16_SumUsingSwitch "Sum using switch")]

<span data-ttu-id="8462c-231">Výrazy shody mají mírně odlišnou syntaxi než `is` výrazy, ve kterém je deklarovat typ a proměnné na začátku `case` výrazu.</span><span class="sxs-lookup"><span data-stu-id="8462c-231">The match expressions have a slightly different syntax than the `is` expressions, where you declare the type and variable at the beginning of the `case` expression.</span></span>

<span data-ttu-id="8462c-232">Výrazy shody také podporují konstanty.</span><span class="sxs-lookup"><span data-stu-id="8462c-232">The match expressions also support constants.</span></span> <span data-ttu-id="8462c-233">To ušetřit čas tím, které budou zohledňovat si jednoduchý případy:</span><span class="sxs-lookup"><span data-stu-id="8462c-233">This can save time by factoring out simple cases:</span></span>

[!code-csharp[SwitchWithConstants](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#17_SwitchWithConstants "Switch with constants")]

<span data-ttu-id="8462c-234">Výše uvedený kód přidá případy `0` jako speciální případ `int`, a `null` ve speciálním případě pokud neexistuje žádný vstup.</span><span class="sxs-lookup"><span data-stu-id="8462c-234">The code above adds cases for `0` as a special case of `int`, and `null` as a special case when there is no input.</span></span> <span data-ttu-id="8462c-235">Tento příklad ukazuje jednu důležitou novou funkci ve výrazech vzor přepínač: pořadí `case` výrazy nyní věcech.</span><span class="sxs-lookup"><span data-stu-id="8462c-235">This demonstrates one important new feature in switch pattern expressions: the order of the `case` expressions now matters.</span></span> <span data-ttu-id="8462c-236">`0` Případu musí být uvedena před Obecné `int` případ.</span><span class="sxs-lookup"><span data-stu-id="8462c-236">The `0` case must appear before the general `int` case.</span></span> <span data-ttu-id="8462c-237">V opačném případě by být první odpovídající vzor `int` případu, dokonce i když je hodnota `0`.</span><span class="sxs-lookup"><span data-stu-id="8462c-237">Otherwise, the first pattern to match would be the `int` case, even when the value is `0`.</span></span> <span data-ttu-id="8462c-238">Pokud omylem výrazech pořadí tak, aby novější případ již byla zpracována, kompilátor se příznak, který a vygenerují chybu.</span><span class="sxs-lookup"><span data-stu-id="8462c-238">If you accidentally order match expressions such that a later case has already been handled, the compiler will flag that and generate an error.</span></span>

<span data-ttu-id="8462c-239">Toto chování stejné umožňuje zvláštní případ pro prázdný vstupní sekvence.</span><span class="sxs-lookup"><span data-stu-id="8462c-239">This same behavior enables the special case for an empty input sequence.</span></span>
<span data-ttu-id="8462c-240">Vidíte, že v případě `IEnumerable` položku, která má elementy se musí nacházet před Obecné `IEnumerable` případ.</span><span class="sxs-lookup"><span data-stu-id="8462c-240">You can see that the case for an `IEnumerable` item that has elements must appear before the general `IEnumerable` case.</span></span>

<span data-ttu-id="8462c-241">Tato verze také přidal `default` případ.</span><span class="sxs-lookup"><span data-stu-id="8462c-241">This version has also added a `default` case.</span></span> <span data-ttu-id="8462c-242">`default` Případ je vyhodnocen vždy poslední, bez ohledu na to, pořadí se zobrazí ve zdroji.</span><span class="sxs-lookup"><span data-stu-id="8462c-242">The `default` case is always evaluated last, regardless of the order it appears in the source.</span></span> <span data-ttu-id="8462c-243">Z tohoto důvodu je konvence vložit `default` případu poslední.</span><span class="sxs-lookup"><span data-stu-id="8462c-243">For that reason, convention is to put the `default` case last.</span></span>

<span data-ttu-id="8462c-244">Nakonec přidáme jeden poslední `case` pro nový styl kostka.</span><span class="sxs-lookup"><span data-stu-id="8462c-244">Finally, let's add one last `case` for a new style of die.</span></span> <span data-ttu-id="8462c-245">Některé hry pomocí percentilu dice představovat větší rozsah čísel.</span><span class="sxs-lookup"><span data-stu-id="8462c-245">Some games use percentile dice to represent larger ranges of numbers.</span></span> 

> [!NOTE]
> <span data-ttu-id="8462c-246">Dva oboustranný 10 percentilu dice může představovat všechna čísla od 0 do 99.</span><span class="sxs-lookup"><span data-stu-id="8462c-246">Two 10-sided percentile dice can represent every number from 0 through 99.</span></span> <span data-ttu-id="8462c-247">Jedna kostka má strany označeny `00`, `10`, `20`,... `90`.</span><span class="sxs-lookup"><span data-stu-id="8462c-247">One die has sides labelled `00`, `10`, `20`, ... `90`.</span></span> <span data-ttu-id="8462c-248">Další kostka má strany s popiskem `0`, `1`, `2`,... `9`.</span><span class="sxs-lookup"><span data-stu-id="8462c-248">The other die has sides labeled `0`, `1`, `2`, ... `9`.</span></span> <span data-ttu-id="8462c-249">Přidat hodnoty dvou kostka společně a získáte všechna čísla od 0 až 99.</span><span class="sxs-lookup"><span data-stu-id="8462c-249">Add the two die values together and you can get every number from 0 through 99.</span></span>

<span data-ttu-id="8462c-250">Chcete-li přidat tento druh kostka do kolekce, nejprve definujte typů k vyjádření rozčlenění percentil.</span><span class="sxs-lookup"><span data-stu-id="8462c-250">To add this kind of die to your collection, first define a type to represent the percentile dice.</span></span> <span data-ttu-id="8462c-251">`TensDigit` Vlastnosti se ukládají hodnoty `0`, `10`, `20`, až `90`:</span><span class="sxs-lookup"><span data-stu-id="8462c-251">The `TensDigit` property stores values `0`, `10`, `20`, up to `90`:</span></span>

[!code-csharp[18_PercentileDice](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#18_PercentileDice "Percentile Die type")]

<span data-ttu-id="8462c-252">Pak přidejte `case` odpovídat výrazu pro nový typ:</span><span class="sxs-lookup"><span data-stu-id="8462c-252">Then, add a `case` match expression for the new type:</span></span>

[!code-csharp[SwitchWithNewTypes](../../../samples/snippets/csharp/new-in-7/patternmatch.cs#19_SwitchWithNewTypes "Include Percentile Die type")]

<span data-ttu-id="8462c-253">Nová syntaxe výrazů pro porovnávání usnadňuje vytvoření odeslání algoritmů na základě typu objektu nebo dalších vlastností, pomocí jasné a stručné syntaxe.</span><span class="sxs-lookup"><span data-stu-id="8462c-253">The new syntax for pattern matching expressions makes it easier to create dispatch algorithms based on an object's type, or other properties, using a clear and concise syntax.</span></span> <span data-ttu-id="8462c-254">Vzor odpovídající výrazy umožňují tyto konstrukce pro datové typy, které jsou nesouvisející prostřednictvím dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="8462c-254">Pattern matching expressions enable these constructs on data types that are unrelated by inheritance.</span></span>

<span data-ttu-id="8462c-255">Další informace o porovnávání vzorů v tématu vyhrazený pro [porovnávání vzorů v jazyce C#](../pattern-matching.md).</span><span class="sxs-lookup"><span data-stu-id="8462c-255">You can learn more about pattern matching in the topic dedicated to [pattern matching in C#](../pattern-matching.md).</span></span>

## <a name="ref-locals-and-returns"></a><span data-ttu-id="8462c-256">Místní referenční hodnoty a vrátí</span><span class="sxs-lookup"><span data-stu-id="8462c-256">Ref locals and returns</span></span>

<span data-ttu-id="8462c-257">Tato funkce umožňuje algoritmy, které používají a vrátit odkazy na proměnné definované jinde.</span><span class="sxs-lookup"><span data-stu-id="8462c-257">This feature enables algorithms that use and return references to variables defined elsewhere.</span></span> <span data-ttu-id="8462c-258">Jedním z příkladů je práce s velké matice a hledání na jednom místě s určité vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="8462c-258">One example is working with large matrices, and finding a single location with certain characteristics.</span></span> <span data-ttu-id="8462c-259">Jedna metoda vrátí dva indexy na jednom místě v matici:</span><span class="sxs-lookup"><span data-stu-id="8462c-259">One method would return the two indices for a single location in the matrix:</span></span>

[!code-csharp[FindReturningIndices](../../../samples/snippets/csharp/new-in-7/MatrixSearch.cs#20_FindReturningIndices "Find returning indices")]

<span data-ttu-id="8462c-260">Existuje mnoho problémů s tímto kódem.</span><span class="sxs-lookup"><span data-stu-id="8462c-260">There are many issues with this code.</span></span> <span data-ttu-id="8462c-261">Za prvé je veřejná metoda, která vrací řazenou kolekci členů.</span><span class="sxs-lookup"><span data-stu-id="8462c-261">First of all, it's a public method that's returning a tuple.</span></span> <span data-ttu-id="8462c-262">Jazyk podporuje, ale uživatelsky definované typy (třídy nebo struktury) jsou upřednostněny veřejných rozhraní API.</span><span class="sxs-lookup"><span data-stu-id="8462c-262">The language supports this, but user defined types (either classes or structs) are preferred for public APIs.</span></span>

<span data-ttu-id="8462c-263">Za druhé tato metoda vrací indexy na položku v matici.</span><span class="sxs-lookup"><span data-stu-id="8462c-263">Second, this method is returning the indices to the item in the matrix.</span></span>
<span data-ttu-id="8462c-264">Který vede volajícím napsat kód, který používá tyto indexy přistoupit přes ukazatel matice a upravovat jeden element:</span><span class="sxs-lookup"><span data-stu-id="8462c-264">That leads callers to write code that uses those indices to dereference the matrix and modify a single element:</span></span>

[!code-csharp[UpdateItemFromIndices](../../../samples/snippets/csharp/new-in-7/program.cs#21_UpdateItemFromIndices "Update Item From Indices")]

<span data-ttu-id="8462c-265">Měli byste spíše napsat metodu, která vrátí *odkaz* na prvek matrice, který chcete změnit.</span><span class="sxs-lookup"><span data-stu-id="8462c-265">You'd rather write a method that returns a *reference* to the element of the matrix that you want to change.</span></span> <span data-ttu-id="8462c-266">Může to provést pouze pomocí nezabezpečený kód a vrací ukazatel na `int` v předchozích verzích.</span><span class="sxs-lookup"><span data-stu-id="8462c-266">You could only accomplish this by using unsafe code and returning a pointer to an `int` in previous versions.</span></span>

<span data-ttu-id="8462c-267">Projděme si prostřednictvím řady změn k předvedení funkcí místní ref a ukazují, jak vytvořit metodu, která vrátí odkaz na interní úložiště.</span><span class="sxs-lookup"><span data-stu-id="8462c-267">Let's walk through a series of changes to demonstrate the ref local feature and show how to create a method that returns a reference to internal storage.</span></span>
<span data-ttu-id="8462c-268">Na cestě se dozvíte pravidla vrácených hodnot ref a ref místní funkce, která chrání proti náhodnému zneužití.</span><span class="sxs-lookup"><span data-stu-id="8462c-268">Along the way, you'll learn the rules of the ref return and ref local feature that protects you from accidentally misusing it.</span></span>

<span data-ttu-id="8462c-269">Začněte tím, že změna `Find` deklarace metody tak, aby se `ref int` místo řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="8462c-269">Start by modifying the `Find` method declaration so that it returns a `ref int` instead of a tuple.</span></span> <span data-ttu-id="8462c-270">Potom upravte příkaz return tak, aby vracel hodnotu uloženou v matici místo dvou indexů:</span><span class="sxs-lookup"><span data-stu-id="8462c-270">Then, modify the return statement so it returns the value stored in the matrix instead of the two indices:</span></span>

```csharp
// Note that this won't compile. 
// Method declaration indicates ref return,
// but return statement specifies a value return.
public static ref int Find2(int[,] matrix, Func<int, bool> predicate)
{
    for (int i = 0; i < matrix.GetLength(0); i++)
        for (int j = 0; j < matrix.GetLength(1); j++)
            if (predicate(matrix[i, j]))
                return matrix[i, j];
    throw new InvalidOperationException("Not found");
}
```

<span data-ttu-id="8462c-271">Pokud deklarujete, metoda vrátí `ref` proměnné, je nutné přidat také `ref` – klíčové slovo pro každý příkaz return.</span><span class="sxs-lookup"><span data-stu-id="8462c-271">When you declare that a method returns a `ref` variable, you must also add the `ref` keyword to each return statement.</span></span> <span data-ttu-id="8462c-272">Který označuje návrat podle odkazu a pomáhá vývojářům čtení kódu později mějte na paměti, že metoda vrací odkazem:</span><span class="sxs-lookup"><span data-stu-id="8462c-272">That indicates return by reference, and helps developers reading the code later remember that the method returns by reference:</span></span>

[!code-csharp[FindReturningRef](../../../samples/snippets/csharp/new-in-7/MatrixSearch.cs#22_FindReturningRef "Find returning by reference")]

<span data-ttu-id="8462c-273">Teď, když metoda vrátí odkaz na celočíselnou hodnotu v matici, budete muset upravit, kde je volán.</span><span class="sxs-lookup"><span data-stu-id="8462c-273">Now that the method returns a reference to the integer value in the matrix, you need to modify where it's called.</span></span>  <span data-ttu-id="8462c-274">`var` Prohlášení znamená, že `valItem` je teď `int` místo řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="8462c-274">The `var` declaration means that `valItem` is now an `int` rather than a tuple:</span></span>

[!code-csharp[AssignRefReturnToValue](../../../samples/snippets/csharp/new-in-7/program.cs#23_AssignRefReturnToValue "Assign ref return to value")]

<span data-ttu-id="8462c-275">Druhá `WriteLine` příkaz v předchozím příkladu vytiskne hodnotu `42`, nikoli `24`.</span><span class="sxs-lookup"><span data-stu-id="8462c-275">The second `WriteLine` statement in the example above prints out the value `42`, not `24`.</span></span> <span data-ttu-id="8462c-276">Proměnná `valItem` je `int`, nikoli `ref int`.</span><span class="sxs-lookup"><span data-stu-id="8462c-276">The variable `valItem` is an `int`, not a `ref int`.</span></span> <span data-ttu-id="8462c-277">`var` – Klíčové slovo umožňuje kompilátoru k určení typu, ale nepřidá implicitně `ref` modifikátor.</span><span class="sxs-lookup"><span data-stu-id="8462c-277">The `var` keyword enables the compiler to specify the type, but will not implicitly add the `ref` modifier.</span></span> <span data-ttu-id="8462c-278">Místo toho hodnota odkazuje `ref return` je *zkopírovat* do proměnné na levé straně přiřazení.</span><span class="sxs-lookup"><span data-stu-id="8462c-278">Instead, the value referred to by the `ref return` is *copied* to the variable on the left-hand side of the assignment.</span></span> <span data-ttu-id="8462c-279">Proměnná není `ref` místní.</span><span class="sxs-lookup"><span data-stu-id="8462c-279">The variable is not a `ref` local.</span></span>

<span data-ttu-id="8462c-280">Aby bylo možné získat výsledek má, budete muset přidat `ref` modifikátoru deklarace lokální proměnné při vrácená hodnota je odkaz vytvořit odkaz proměnnou:</span><span class="sxs-lookup"><span data-stu-id="8462c-280">In order to get the result you want, you need to add the `ref` modifier to the local variable declaration to make the variable a reference when the return value is a reference:</span></span>

[!code-csharp[AssignRefReturn](../../../samples/snippets/csharp/new-in-7/program.cs#24_AssignRefReturn "Assign ref return")]

<span data-ttu-id="8462c-281">Nyní, druhý `WriteLine` příkaz v předchozím příkladu se vytiskne hodnotu `24`, označující, že byla změněna úložiště v matici.</span><span class="sxs-lookup"><span data-stu-id="8462c-281">Now, the second `WriteLine` statement in the example above will print out the value `24`, indicating that the storage in the matrix has been modified.</span></span> <span data-ttu-id="8462c-282">Lokální proměnná jsou deklarované v rámci `ref` modifikátor který bude trvat `ref` vrátit.</span><span class="sxs-lookup"><span data-stu-id="8462c-282">The local variable has been declared with the `ref` modifier, and it will take a `ref` return.</span></span> <span data-ttu-id="8462c-283">Je nutné inicializovat `ref` proměnné při deklaraci, nelze rozdělit deklaraci a inicializaci.</span><span class="sxs-lookup"><span data-stu-id="8462c-283">You must initialize a `ref` variable when it is declared, you cannot split the declaration and the initialization.</span></span>

<span data-ttu-id="8462c-284">Jazyk C# má tři další pravidla, které chrání vás před zneužitím `ref` místní hodnoty a vrátí:</span><span class="sxs-lookup"><span data-stu-id="8462c-284">The C# language has three other rules that protect you from misusing the `ref` locals and returns:</span></span>

* <span data-ttu-id="8462c-285">Nelze přiřadit standardní metoda návratovou hodnotu pro `ref` místní proměnné.</span><span class="sxs-lookup"><span data-stu-id="8462c-285">You cannot assign a standard method return value to a `ref` local variable.</span></span>
    - <span data-ttu-id="8462c-286">Který zakazuje příkazy jako `ref int i = sequence.Count();`</span><span class="sxs-lookup"><span data-stu-id="8462c-286">That disallows statements like `ref int i = sequence.Count();`</span></span>
* <span data-ttu-id="8462c-287">Nelze vrátit `ref` do proměnné, jehož doba života nerozšiřuje nad rámec provádění metody.</span><span class="sxs-lookup"><span data-stu-id="8462c-287">You cannot return a `ref` to a variable whose lifetime does not extend beyond the execution of the method.</span></span>
    - <span data-ttu-id="8462c-288">To znamená, že nejde vrátit odkazem na místní proměnná nebo proměnná se podobně jako obor.</span><span class="sxs-lookup"><span data-stu-id="8462c-288">That means you cannot return a reference to a local variable or a variable with a similar scope.</span></span>
* <span data-ttu-id="8462c-289">`ref` místní hodnoty a vrátí nelze použít u asynchronních metod.</span><span class="sxs-lookup"><span data-stu-id="8462c-289">`ref` locals and returns can't be used with async methods.</span></span>
    - <span data-ttu-id="8462c-290">Kompilátor nemůže vědět, pokud odkazovaná proměnná je nastavená na jeho konečnou hodnotu po návratu asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="8462c-290">The compiler can't know if the referenced variable has been set to its final value when the async method returns.</span></span>

<span data-ttu-id="8462c-291">Přidání místní referenční hodnoty a ref vrátí povolit algoritmy, které jsou efektivnější se vyhnout kopírování hodnot nebo provádění operací přesměrování více než jednou.</span><span class="sxs-lookup"><span data-stu-id="8462c-291">The addition of ref locals and ref returns enable algorithms that are more efficient by avoiding copying values, or performing dereferencing operations multiple times.</span></span>

<span data-ttu-id="8462c-292">Přidání `ref` návratová hodnota je [zdroj kompatibilní změnu](version-update-considerations.md#source-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="8462c-292">Adding `ref` to the return value is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span> <span data-ttu-id="8462c-293">Stávající kód zkompiluje, ale ref návratové hodnoty je zkopírován při přiřazení.</span><span class="sxs-lookup"><span data-stu-id="8462c-293">Existing code compiles, but the ref return value is copied when assigned.</span></span> <span data-ttu-id="8462c-294">Volající musí aktualizovat úložiště pro návratovou hodnotu `ref` místní proměnnou pro uložení vrácení jako odkaz.</span><span class="sxs-lookup"><span data-stu-id="8462c-294">Callers must update the storage for the return value to a `ref` local variable to store the return as a reference.</span></span>

<span data-ttu-id="8462c-295">Další informace najdete v tématu [ref – klíčové slovo](../language-reference/keywords/ref.md) článku.</span><span class="sxs-lookup"><span data-stu-id="8462c-295">For more information, see the [ref keyword](../language-reference/keywords/ref.md) article.</span></span>

## <a name="local-functions"></a><span data-ttu-id="8462c-296">Lokální funkce</span><span class="sxs-lookup"><span data-stu-id="8462c-296">Local functions</span></span>

<span data-ttu-id="8462c-297">Řada návrhů pro třídy zahrnují metody, které se volají z jediného umístění.</span><span class="sxs-lookup"><span data-stu-id="8462c-297">Many designs for classes include methods that are called from only one location.</span></span> <span data-ttu-id="8462c-298">Tyto další metody privátní zachovat každá metoda malém rozsahu a zaměřeně.</span><span class="sxs-lookup"><span data-stu-id="8462c-298">These additional private methods keep each method small and focused.</span></span> <span data-ttu-id="8462c-299">Však budou mít je těžší porozumět přečtením první třídy.</span><span class="sxs-lookup"><span data-stu-id="8462c-299">However, they can make it harder to understand a class when reading it the first time.</span></span> <span data-ttu-id="8462c-300">Tyto metody musí být srozumitelný mimo kontext z jednoho místa volání.</span><span class="sxs-lookup"><span data-stu-id="8462c-300">These methods must be understood outside of the context of the single calling location.</span></span>

<span data-ttu-id="8462c-301">Pro tyto návrhy *lokální funkce* umožňují deklarovat metody v kontextu jinou metodu.</span><span class="sxs-lookup"><span data-stu-id="8462c-301">For those designs, *local functions* enable you to declare methods inside the context of another method.</span></span> <span data-ttu-id="8462c-302">To usnadňuje pro čtenáře třídy zobrazíte, že místní metoda je volána pouze z kontextu, ve kterém je deklarována.</span><span class="sxs-lookup"><span data-stu-id="8462c-302">This makes it easier for readers of the class to see that the local method is only called from the context in which is it declared.</span></span>

<span data-ttu-id="8462c-303">Existují dva velmi běžné případy použití pro lokální funkce: veřejné iterátory a veřejné asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="8462c-303">There are two very common use cases for local functions: public iterator methods and public async methods.</span></span> <span data-ttu-id="8462c-304">Oba typy metod generování kódu, který bude hlásit chyby později, než se dalo očekávat programátory.</span><span class="sxs-lookup"><span data-stu-id="8462c-304">Both types of methods generate code that reports errors later than programmers might expect.</span></span> <span data-ttu-id="8462c-305">V případě metody iterátoru, všechny výjimky, jsou dodržovány pouze při volání metody kód, který uvádí, že vrácená sekvence.</span><span class="sxs-lookup"><span data-stu-id="8462c-305">In the case of iterator methods, any exceptions are observed only when calling code that enumerates the returned sequence.</span></span> <span data-ttu-id="8462c-306">V případě asynchronní metody, všechny výjimky jsou pouze dodržovat při vráceného `Task` je očekáváno.</span><span class="sxs-lookup"><span data-stu-id="8462c-306">In the case of async methods, any exceptions are only observed when the returned `Task` is awaited.</span></span>

<span data-ttu-id="8462c-307">Začněme iterační metodu:</span><span class="sxs-lookup"><span data-stu-id="8462c-307">Let's start with an iterator method:</span></span>

[!code-csharp[IteratorMethod](../../../samples/snippets/csharp/new-in-7/Iterator.cs#25_IteratorMethod "Iterator method")]

<span data-ttu-id="8462c-308">Zkontrolujte následující kód, který zavolá metodu iterátoru nesprávně:</span><span class="sxs-lookup"><span data-stu-id="8462c-308">Examine the code below that calls the iterator method incorrectly:</span></span>

[!code-csharp[CallIteratorMethod](../../../samples/snippets/csharp/new-in-7/program.cs#26_CallIteratorMethod "Call iterator method")]

<span data-ttu-id="8462c-309">Výjimka je vyvolána, když `resultSet` provést iteraci, ne když `resultSet` se vytvoří.</span><span class="sxs-lookup"><span data-stu-id="8462c-309">The exception is thrown when `resultSet` is iterated, not when `resultSet` is created.</span></span>
<span data-ttu-id="8462c-310">V tomto příkladu omezením Většina vývojářů může rychle diagnostikovat problém.</span><span class="sxs-lookup"><span data-stu-id="8462c-310">In this contained example, most developers could quickly diagnose the problem.</span></span> <span data-ttu-id="8462c-311">V větší základů kódu, kód, který vytvoří iterátor, často se podobají kódu, který se zobrazí výsledek.</span><span class="sxs-lookup"><span data-stu-id="8462c-311">However, in larger codebases, the code that creates an iterator often isn't as close to the code that enumerates the result.</span></span> <span data-ttu-id="8462c-312">Možné Refaktorovat kód, tak, aby veřejné metody ověří všechny argumenty a generuje výčet privátní metodu:</span><span class="sxs-lookup"><span data-stu-id="8462c-312">You can refactor the code so that the public method validates all arguments, and a private method generates the enumeration:</span></span>

[!code-csharp[IteratorMethodRefactored](../../../samples/snippets/csharp/new-in-7/Iterator.cs#27_IteratorMethodRefactored "Iterator method refactored")]

<span data-ttu-id="8462c-313">Tato verze refaktorovaný vyvolají výjimky okamžitě, protože není veřejná metoda metodu iterátoru; jenom privátní metoda používá `yield return` syntaxe.</span><span class="sxs-lookup"><span data-stu-id="8462c-313">This refactored version will throw exceptions immediately because the public method is not an iterator method; only the private method uses the `yield return` syntax.</span></span> <span data-ttu-id="8462c-314">Existují však potenciální problémy s Tento refaktoring.</span><span class="sxs-lookup"><span data-stu-id="8462c-314">However, there are potential problems with this refactoring.</span></span> <span data-ttu-id="8462c-315">Privátní metoda pouze lze volat z metodu veřejného rozhraní, protože jinak se přeskočí všechny ověřování argumentu.</span><span class="sxs-lookup"><span data-stu-id="8462c-315">The private method should only be called from the public interface method, because otherwise all argument validation is skipped.</span></span>
<span data-ttu-id="8462c-316">Čtenáři třídy musí tuto skutečnost zjišťovat, že čtení celé třídy a vyhledáte všechny odkazy na `alphabetSubsetImplementation` metody.</span><span class="sxs-lookup"><span data-stu-id="8462c-316">Readers of the class must discover this fact by reading the entire class and searching for any other references to the `alphabetSubsetImplementation` method.</span></span>

<span data-ttu-id="8462c-317">Záměr tohoto návrhu můžete vytvořit více vymazat deklarací `alphabetSubsetImplementation` jako lokální funkce uvnitř metody veřejné rozhraní API:</span><span class="sxs-lookup"><span data-stu-id="8462c-317">You can make that design intent more clear by declaring the `alphabetSubsetImplementation` as a local function inside the public API method:</span></span>

[!code-csharp[22_IteratorMethodLocal](../../../samples/snippets/csharp/new-in-7/Iterator.cs#28_IteratorMethodLocal "Iterator method with local function")]

<span data-ttu-id="8462c-318">Výše uvedené verze je zřejmé, na který odkazuje místní metodu pouze v kontextu vnější metody.</span><span class="sxs-lookup"><span data-stu-id="8462c-318">The version above makes it clear that the local method is referenced only in the context of the outer method.</span></span> <span data-ttu-id="8462c-319">Pravidla pro lokální funkce také zajistěte, aby vývojář nesmí omylem z jiného umístění ve třídě zavolejte funkci Místní a obejít ověřování argumentu.</span><span class="sxs-lookup"><span data-stu-id="8462c-319">The rules for local functions also ensure that a developer can't accidentally call the local function from another location in the class and bypass the argument validation.</span></span>

<span data-ttu-id="8462c-320">Stejným způsobem mohou být použity s `async` metody k zajištění, že výjimky vyplývající z ověřování argumentu jsou vyvolány před zahájením asynchronní práce:</span><span class="sxs-lookup"><span data-stu-id="8462c-320">The same technique can be employed with `async` methods to ensure that exceptions arising from argument validation are thrown before the asynchronous work begins:</span></span>

[!code-csharp[TaskExample](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#29_TaskExample "Task returning method with local function")]

> [!NOTE]
> <span data-ttu-id="8462c-321">Některé návrhy, které jsou podporovány lokální funkce také je možné dosáhnout použitím *výrazy lambda*.</span><span class="sxs-lookup"><span data-stu-id="8462c-321">Some of the designs that are supported by local functions could also be accomplished using *lambda expressions*.</span></span> <span data-ttu-id="8462c-322">Tyto zúčastněné můžete [Další informace o rozdílech](../local-functions-vs-lambdas.md)</span><span class="sxs-lookup"><span data-stu-id="8462c-322">Those interested can [read more about the differences](../local-functions-vs-lambdas.md)</span></span>

## <a name="more-expression-bodied-members"></a><span data-ttu-id="8462c-323">Více členů s výrazem v těle</span><span class="sxs-lookup"><span data-stu-id="8462c-323">More expression-bodied members</span></span>

<span data-ttu-id="8462c-324">C# 6 zavedené [členové tvoření](csharp-6.md#expression-bodied-function-members) pro členské funkce a vlastnosti jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="8462c-324">C# 6 introduced [expression-bodied members](csharp-6.md#expression-bodied-function-members) for member functions, and read-only properties.</span></span> <span data-ttu-id="8462c-325">C# 7.0 rozšíří povolené členy, které je možné implementovat jako výrazy.</span><span class="sxs-lookup"><span data-stu-id="8462c-325">C# 7.0 expands the allowed members that can be implemented as expressions.</span></span> <span data-ttu-id="8462c-326">V jazyce C# 7.0, můžete implementovat *konstruktory*, *finalizační metody*, a `get` a `set` přistupující objekty na *vlastnosti* a *indexerů* .</span><span class="sxs-lookup"><span data-stu-id="8462c-326">In C# 7.0, you can implement *constructors*, *finalizers*, and `get` and `set` accessors on *properties* and *indexers*.</span></span> <span data-ttu-id="8462c-327">Následující kód ukazuje příklady každé:</span><span class="sxs-lookup"><span data-stu-id="8462c-327">The following code shows examples of each:</span></span>

[!code-csharp[ExpressionBodiedMembers](../../../samples/snippets/csharp/new-in-7/expressionmembers.cs#36_ExpressionBodiedEverything "new expression-bodied members")]

> [!NOTE]
> <span data-ttu-id="8462c-328">V tomto příkladu nemusí finalizační metodu, ale je zobrazena na ukazují syntaxi.</span><span class="sxs-lookup"><span data-stu-id="8462c-328">This example does not need a finalizer, but it is shown to demonstrate the syntax.</span></span> <span data-ttu-id="8462c-329">Finalizační metody by neměla implementovat ve své třídě, pokud to není nutné k uvolnění nespravovaných prostředků.</span><span class="sxs-lookup"><span data-stu-id="8462c-329">You should not implement a finalizer in your class unless it is necessary to  release unmanaged resources.</span></span> <span data-ttu-id="8462c-330">Měli byste také zvážit použití <xref:System.Runtime.InteropServices.SafeHandle> třídy místo správy nespravované prostředky přímo.</span><span class="sxs-lookup"><span data-stu-id="8462c-330">You should also consider using the <xref:System.Runtime.InteropServices.SafeHandle> class instead of managing unmanaged resources directly.</span></span>

<span data-ttu-id="8462c-331">Tato nová umístění pro členy s výrazem v těle představuje důležitý milník pro C# jazyka: Tyto funkce byly implementované členy komunity open source pracovali [Roslyn](https://github.com/dotnet/Roslyn) projektu.</span><span class="sxs-lookup"><span data-stu-id="8462c-331">These new locations for expression-bodied members represent an important milestone for the C# language: These features were implemented by community members working on the open-source [Roslyn](https://github.com/dotnet/Roslyn) project.</span></span>

<span data-ttu-id="8462c-332">Změna metody na výrazu vozidlo člena je [binární kompatibilní změnu](version-update-considerations.md#binary-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="8462c-332">Changing a method to an expression bodied member is a [binary compatible change](version-update-considerations.md#binary-compatible-changes).</span></span>

## <a name="throw-expressions"></a><span data-ttu-id="8462c-333">Vyvolání výrazů</span><span class="sxs-lookup"><span data-stu-id="8462c-333">Throw expressions</span></span>

<span data-ttu-id="8462c-334">V jazyce C# `throw` vždy bylo příkazu.</span><span class="sxs-lookup"><span data-stu-id="8462c-334">In C#, `throw` has always been a statement.</span></span> <span data-ttu-id="8462c-335">Protože `throw` je příkaz, není výraz, že byly konstrukce jazyka C#, ve kterém nelze použít.</span><span class="sxs-lookup"><span data-stu-id="8462c-335">Because `throw` is a statement, not an expression, there were C# constructs where you could not use it.</span></span> <span data-ttu-id="8462c-336">Tyto uvedeny podmíněné výrazy, null slučovací výrazy a některé výrazy lambda.</span><span class="sxs-lookup"><span data-stu-id="8462c-336">These included conditional expressions, null coalescing expressions, and some lambda expressions.</span></span> <span data-ttu-id="8462c-337">Přidání členů s výrazem v těle přidá více míst kde `throw` výrazů může být užitečné.</span><span class="sxs-lookup"><span data-stu-id="8462c-337">The addition of expression-bodied members adds more locations where `throw` expressions would be useful.</span></span> <span data-ttu-id="8462c-338">Aby mohla zapisovat některý z těchto konstruktorů, C# 7.0 zavádí *vyvolání výrazů*.</span><span class="sxs-lookup"><span data-stu-id="8462c-338">So that you can write any of these constructs, C# 7.0 introduces *throw expressions*.</span></span>

<span data-ttu-id="8462c-339">Syntaxe je stejná jako vždy jste použili pro `throw` příkazy.</span><span class="sxs-lookup"><span data-stu-id="8462c-339">The syntax is the same as you've always used for `throw` statements.</span></span> <span data-ttu-id="8462c-340">Jediným rozdílem je, že teď můžete umístit je do nového umístění, například jako podmíněný výraz:</span><span class="sxs-lookup"><span data-stu-id="8462c-340">The only difference is that now you can place them in new locations, such as in a conditional expression:</span></span>

[!code-csharp[Throw_ExpressionExample](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#37_Throw_ExpressionExample "conditional throw expressions")]

<span data-ttu-id="8462c-341">Tato funkce umožňuje pomocí výrazů throw v výrazy inicializace:</span><span class="sxs-lookup"><span data-stu-id="8462c-341">This features enables using throw expressions in initialization expressions:</span></span>

[!code-csharp[ThrowInInitialization](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#38_ThrowInInitialization "conditional throw expressions")]

<span data-ttu-id="8462c-342">Tyto inicializace by dříve, musí být v konstruktoru, s příkazy throw v těle konstruktoru:</span><span class="sxs-lookup"><span data-stu-id="8462c-342">Previously, those initializations would need to be in a constructor, with the throw statements in the body of the constructor:</span></span>


[!code-csharp[ThrowInConstructor](../../../samples/snippets/csharp/new-in-7/throwexpressions.cs#39_ThrowInConstructor "throw statements")]

> [!NOTE]
> <span data-ttu-id="8462c-343">Obě předchozí konstruktorů způsobí, že výjimky, která je vyvolána během konstrukce objektu.</span><span class="sxs-lookup"><span data-stu-id="8462c-343">Both of the preceding constructs will cause exceptions to be thrown during the construction of an object.</span></span> <span data-ttu-id="8462c-344">Ty je často obtížné provést obnovení.</span><span class="sxs-lookup"><span data-stu-id="8462c-344">Those are often difficult to recover from.</span></span>
> <span data-ttu-id="8462c-345">Z tohoto důvodu se nedoporučuje návrhy, které vyvolají výjimky během konstrukce.</span><span class="sxs-lookup"><span data-stu-id="8462c-345">For that reason, designs that throw exceptions during construction are discouraged.</span></span>

## <a name="generalized-async-return-types"></a><span data-ttu-id="8462c-346">Zobecněný asynchronní návratové typy</span><span class="sxs-lookup"><span data-stu-id="8462c-346">Generalized async return types</span></span>

<span data-ttu-id="8462c-347">Vrácení `Task` objekt z asynchronní metody můžete zavést problémových míst výkonu v určitých cestách.</span><span class="sxs-lookup"><span data-stu-id="8462c-347">Returning a `Task` object from async methods can introduce performance bottlenecks in certain paths.</span></span> <span data-ttu-id="8462c-348">`Task` je typem odkazu, takže ho pomocí znamená, že přidělování objektu.</span><span class="sxs-lookup"><span data-stu-id="8462c-348">`Task` is a reference type, so using it means allocating an object.</span></span> <span data-ttu-id="8462c-349">V případech, kde metoda deklarována s `async` modifikátor vrátí výsledky uložené v mezipaměti, nebo dokončí synchronně, navíc přidělení se může stát spoustu času náklady na kritické oddíly výkon kódu.</span><span class="sxs-lookup"><span data-stu-id="8462c-349">In cases where a method declared with the `async` modifier returns a cached result, or completes synchronously, the extra allocations can become a significant time cost in performance critical sections of code.</span></span> <span data-ttu-id="8462c-350">Může být velmi náročná, pokud dojde k těmto přidělení v těsné smyčky.</span><span class="sxs-lookup"><span data-stu-id="8462c-350">It can become very costly if those allocations occur in tight loops.</span></span>

<span data-ttu-id="8462c-351">Nové funkce jazyka znamená, že asynchronní metody může vrátit jiné typy kromě `Task`, `Task<T>` a `void`.</span><span class="sxs-lookup"><span data-stu-id="8462c-351">The new language feature means that async methods may return other types in addition to `Task`, `Task<T>` and `void`.</span></span> <span data-ttu-id="8462c-352">Vrácený typ musí splňovat stále asynchronní vzorek, což znamená `GetAwaiter` metoda musí být přístupná.</span><span class="sxs-lookup"><span data-stu-id="8462c-352">The returned type must still satisfy the async pattern, meaning a `GetAwaiter` method must be accessible.</span></span> <span data-ttu-id="8462c-353">Jako konkrétní příklad `ValueTask` typ byl přidán do rozhraní .NET framework, aby pomocí této nové funkce jazyka:</span><span class="sxs-lookup"><span data-stu-id="8462c-353">As one concrete example, the `ValueTask` type has been added to the .NET framework to make use of this new language feature:</span></span> 

[!code-csharp[UsingValueTask](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#30_UsingValueTask "Using ValueTask")]

> [!NOTE]
> <span data-ttu-id="8462c-354">Je potřeba přidat balíček NuGet [ `System.Threading.Tasks.Extensions` ](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) Chcete-li použít <xref:System.Threading.Tasks.ValueTask%601> typu.</span><span class="sxs-lookup"><span data-stu-id="8462c-354">You need to add the NuGet package [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) in order to use the <xref:System.Threading.Tasks.ValueTask%601> type.</span></span>

<span data-ttu-id="8462c-355">Jednoduché optimalizace může být použití `ValueTask` v místech, kde `Task` by se použily před.</span><span class="sxs-lookup"><span data-stu-id="8462c-355">A simple optimization would be to use `ValueTask` in places where `Task` would be used before.</span></span> <span data-ttu-id="8462c-356">Ale pokud chcete provést další optimalizace ručně, můžete výsledky z asynchronní práce do mezipaměti a znovu použít výsledek v následných voláních.</span><span class="sxs-lookup"><span data-stu-id="8462c-356">However, if you want to perform extra optimizations by hand, you can cache results from async work and reuse the result in subsequent calls.</span></span> <span data-ttu-id="8462c-357">`ValueTask` Struktura obsahuje konstruktor s `Task` parametru tak, že můžete vytvořit `ValueTask` z existující asynchronní metody na návratový typ:</span><span class="sxs-lookup"><span data-stu-id="8462c-357">The `ValueTask` struct has a constructor with a `Task` parameter so that you can construct a `ValueTask` from the return value of any existing async method:</span></span>

[!code-csharp[AsyncOptimizedValueTask](../../../samples/snippets/csharp/new-in-7/AsyncWork.cs#31_AsyncOptimizedValueTask "Return async result or cached value")]

<span data-ttu-id="8462c-358">Jak se všechna doporučení k výkonu, můžete by měl srovnávací testy obě verze před provedením velké škálování změny v kódu.</span><span class="sxs-lookup"><span data-stu-id="8462c-358">As with all performance recommendations, you should benchmark both versions before making large scale changes to your code.</span></span>

<span data-ttu-id="8462c-359">Pokud vrácená hodnota je cílem `await` příkaz změny rozhraní API z <xref:System.Threading.Tasks.Task%601> k <xref:System.Threading.Tasks.ValueTask%601> je [zdroj kompatibilní změnu](version-update-considerations.md#source-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="8462c-359">When the return value is the target of an `await` statement, changing an API from a <xref:System.Threading.Tasks.Task%601> to a <xref:System.Threading.Tasks.ValueTask%601> is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span> <span data-ttu-id="8462c-360">Obecně platí, změna `ValueTask` není.</span><span class="sxs-lookup"><span data-stu-id="8462c-360">In general, changing to `ValueTask` is not.</span></span>

## <a name="numeric-literal-syntax-improvements"></a><span data-ttu-id="8462c-361">Vylepšení číselný literál syntaxe</span><span class="sxs-lookup"><span data-stu-id="8462c-361">Numeric literal syntax improvements</span></span>

<span data-ttu-id="8462c-362">Číselné konstanty misreading může znesnadnit pochopení kódu při čtení poprvé.</span><span class="sxs-lookup"><span data-stu-id="8462c-362">Misreading numeric constants can make it harder to understand code when reading it for the first time.</span></span> <span data-ttu-id="8462c-363">Tato situace často nastává, když těchto čísel se používají jako bitové masky nebo jiných symbolické namísto číselných hodnot.</span><span class="sxs-lookup"><span data-stu-id="8462c-363">This often occurs when those numbers are used as bit masks or other symbolic rather than numeric values.</span></span> <span data-ttu-id="8462c-364">C# 7.0 obsahuje dvě nové funkce, které usnadňují zápis čísla způsobem nejvíce čitelné pro zamýšlené použití: *binární literály:*, a *oddělovače číslic:*.</span><span class="sxs-lookup"><span data-stu-id="8462c-364">C# 7.0 includes two new features to make it easier to write numbers in the most readable fashion for the intended use: *binary literals*, and *digit separators*.</span></span>

<span data-ttu-id="8462c-365">Pro situace, při vytváření bitové masky, nebo když se binární vyjádření čísla je nejvíce čitelné kód zápis tohoto čísla v binárním souboru:</span><span class="sxs-lookup"><span data-stu-id="8462c-365">For those times when you are creating bit masks, or whenever a binary representation of a number makes the most readable code, write that number in binary:</span></span>

[!code-csharp[BinaryConstants](../../../samples/snippets/csharp/new-in-7/Program.cs#32_BinaryConstants "Binary constants")]

<span data-ttu-id="8462c-366">`0b` Na začátku konstanty Určuje, že číslo je zapsán jako binární číslo.</span><span class="sxs-lookup"><span data-stu-id="8462c-366">The `0b` at the beginning of the constant indicates that the number is written as a binary number.</span></span>

<span data-ttu-id="8462c-367">Můžete získat tak, aby byl často snazší zjistit bitové vzory zavedením příliš dlouho, binárních čísel `_` jako oddělovač číslic:</span><span class="sxs-lookup"><span data-stu-id="8462c-367">Binary numbers can get very long, so it's often easier to see the bit patterns by introducing the `_` as a digit separator:</span></span>

[!code-csharp[ThousandSeparators](../../../samples/snippets/csharp/new-in-7/Program.cs#33_ThousandSeparators "Thousands separators")]

<span data-ttu-id="8462c-368">Oddělovač číslic může vyskytovat kdekoli v konstantě.</span><span class="sxs-lookup"><span data-stu-id="8462c-368">The digit separator can appear anywhere in the constant.</span></span> <span data-ttu-id="8462c-369">Pro základní 10 čísel, bylo by běžné použití jako tisíců oddělovač:</span><span class="sxs-lookup"><span data-stu-id="8462c-369">For base 10 numbers, it would be common to use it as a thousands separator:</span></span>

[!code-csharp[LargeIntegers](../../../samples/snippets/csharp/new-in-7/Program.cs#34_LargeIntegers "Large integer")]

<span data-ttu-id="8462c-370">Oddělovač číslic jde použít s `decimal`, `float` a `double` typy a také:</span><span class="sxs-lookup"><span data-stu-id="8462c-370">The digit separator can be used with `decimal`, `float` and `double` types as well:</span></span>

[!code-csharp[OtherConstants](../../../samples/snippets/csharp/new-in-7/Program.cs#35_OtherConstants "non-integral constants")]

<span data-ttu-id="8462c-371">Dohromady, je možné deklarovat číselné konstanty s mnohem více čitelnost.</span><span class="sxs-lookup"><span data-stu-id="8462c-371">Taken together, you can declare numeric constants with much more readability.</span></span>
