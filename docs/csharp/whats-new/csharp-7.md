---
title: Co je nového v C# 7,0 – příručka C#
description: Získejte přehled o nových funkcích verze 7,0 jazyka C#.
ms.date: 02/20/2019
ms.assetid: fd41596d-d0c2-4816-b94d-c4d00a5d0243
ms.openlocfilehash: e78d680e19709bf3dd854531d5d9f6b7d6464f49
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/04/2020
ms.locfileid: "84392246"
---
# <a name="whats-new-in-c-70"></a><span data-ttu-id="7f6fb-103">Co je nového v jazyce C# 7.0</span><span class="sxs-lookup"><span data-stu-id="7f6fb-103">What's new in C# 7.0</span></span>

<span data-ttu-id="7f6fb-104">C# 7,0 přidává do jazyka C# řadu nových funkcí:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-104">C# 7.0 adds a number of new features to the C# language:</span></span>

- [<span data-ttu-id="7f6fb-105">`out`proměnné</span><span class="sxs-lookup"><span data-stu-id="7f6fb-105">`out` variables</span></span>](#out-variables)
  - <span data-ttu-id="7f6fb-106">Můžete deklarovat `out` hodnoty vložené jako argumenty pro metodu, kde se používají.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-106">You can declare `out` values inline as arguments to the method where they're used.</span></span>
- [<span data-ttu-id="7f6fb-107">N-tice</span><span class="sxs-lookup"><span data-stu-id="7f6fb-107">Tuples</span></span>](#tuples)
  - <span data-ttu-id="7f6fb-108">Můžete vytvořit odlehčené nejmenované typy, které obsahují více veřejných polí.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-108">You can create lightweight, unnamed types that contain multiple public fields.</span></span> <span data-ttu-id="7f6fb-109">Kompilátory a nástroje IDE, které porozuměl sémantikě těchto typů.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-109">Compilers and IDE tools understand the semantics of these types.</span></span>
- [<span data-ttu-id="7f6fb-110">Zahození</span><span class="sxs-lookup"><span data-stu-id="7f6fb-110">Discards</span></span>](#discards)
  - <span data-ttu-id="7f6fb-111">Zahození jsou dočasné a proměnné jen pro zápis použité v přiřazeních, když se nemusíte starat o přiřazenou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-111">Discards are temporary, write-only variables used in assignments when you don't care about the value assigned.</span></span> <span data-ttu-id="7f6fb-112">Jsou nejužitečnější při dekonstrukci řazených kolekcí členů a uživatelsky definovaných typů a také při volání metod s `out` parametry.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-112">They're most useful when deconstructing tuples and user-defined types, as well as when calling methods with `out` parameters.</span></span>
- [<span data-ttu-id="7f6fb-113">Porovnávání vzorů</span><span class="sxs-lookup"><span data-stu-id="7f6fb-113">Pattern Matching</span></span>](#pattern-matching)
  - <span data-ttu-id="7f6fb-114">Můžete vytvořit logiku větvení na základě libovolných typů a hodnot členů těchto typů.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-114">You can create branching logic based on arbitrary types and values of the members of those types.</span></span>
- [<span data-ttu-id="7f6fb-115">`ref`místní hodnoty a vrátí</span><span class="sxs-lookup"><span data-stu-id="7f6fb-115">`ref` locals and returns</span></span>](#ref-locals-and-returns)
  - <span data-ttu-id="7f6fb-116">Místní proměnné metody a návratové hodnoty mohou být odkazy na jiné úložiště.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-116">Method local variables and return values can be references to other storage.</span></span>
- [<span data-ttu-id="7f6fb-117">Místní funkce</span><span class="sxs-lookup"><span data-stu-id="7f6fb-117">Local Functions</span></span>](#local-functions)
  - <span data-ttu-id="7f6fb-118">Funkce můžete vnořit do jiných funkcí a omezit tak jejich rozsah a viditelnost.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-118">You can nest functions inside other functions to limit their scope and visibility.</span></span>
- [<span data-ttu-id="7f6fb-119">Další členové Expression-těle</span><span class="sxs-lookup"><span data-stu-id="7f6fb-119">More expression-bodied members</span></span>](#more-expression-bodied-members)
  - <span data-ttu-id="7f6fb-120">Seznam členů, které mohou být vytvořeny pomocí výrazů, byl vypěstován.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-120">The list of members that can be authored using expressions has grown.</span></span>
- [<span data-ttu-id="7f6fb-121">`throw`Expression</span><span class="sxs-lookup"><span data-stu-id="7f6fb-121">`throw` Expressions</span></span>](#throw-expressions)
  - <span data-ttu-id="7f6fb-122">Můžete vyvolat výjimky v konstrukcích kódu, které nebyly dřív povoleny, protože `throw` se jednalo o příkaz.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-122">You can throw exceptions in code constructs that previously weren't allowed because `throw` was a statement.</span></span>
- [<span data-ttu-id="7f6fb-123">Generalizované asynchronní návratové typy</span><span class="sxs-lookup"><span data-stu-id="7f6fb-123">Generalized async return types</span></span>](#generalized-async-return-types)
  - <span data-ttu-id="7f6fb-124">Metody deklarované s `async` modifikátorem mohou vracet další typy kromě `Task` a `Task<T>` .</span><span class="sxs-lookup"><span data-stu-id="7f6fb-124">Methods declared with the `async` modifier can return other types in addition to `Task` and `Task<T>`.</span></span>
- [<span data-ttu-id="7f6fb-125">Vylepšení syntaxe numerického literálu</span><span class="sxs-lookup"><span data-stu-id="7f6fb-125">Numeric literal syntax improvements</span></span>](#numeric-literal-syntax-improvements)
  - <span data-ttu-id="7f6fb-126">Nové tokeny zlepšují čitelnost pro číselné konstanty.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-126">New tokens improve readability for numeric constants.</span></span>

<span data-ttu-id="7f6fb-127">Zbývající část tohoto článku poskytuje přehled jednotlivých funkcí.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-127">The remainder of this article provides an overview of each feature.</span></span> <span data-ttu-id="7f6fb-128">U každé funkce se dozvíte, co je důvod na pozadí.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-128">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="7f6fb-129">Naučíte se syntaxí.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-129">You'll learn the syntax.</span></span> <span data-ttu-id="7f6fb-130">Pomocí globálního nástroje můžete prozkoumat tyto funkce ve vašem prostředí `dotnet try` :</span><span class="sxs-lookup"><span data-stu-id="7f6fb-130">You can explore these features in your environment using the `dotnet try` global tool:</span></span>

1. <span data-ttu-id="7f6fb-131">Nainstalujte nástroj [dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) Global.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-131">Install the [dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) global tool.</span></span>
1. <span data-ttu-id="7f6fb-132">Naklonujte úložiště [dotnet/try-Samples](https://github.com/dotnet/try-samples) .</span><span class="sxs-lookup"><span data-stu-id="7f6fb-132">Clone the [dotnet/try-samples](https://github.com/dotnet/try-samples) repository.</span></span>
1. <span data-ttu-id="7f6fb-133">Nastavte aktuální adresář do podadresáře *csharp7* pro úložiště *Try-Samples* .</span><span class="sxs-lookup"><span data-stu-id="7f6fb-133">Set the current directory to the *csharp7* subdirectory for the *try-samples* repository.</span></span>
1. <span data-ttu-id="7f6fb-134">Spusťte `dotnet try`.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-134">Run `dotnet try`.</span></span>

## <a name="out-variables"></a><span data-ttu-id="7f6fb-135">`out`proměnné</span><span class="sxs-lookup"><span data-stu-id="7f6fb-135">`out` variables</span></span>

<span data-ttu-id="7f6fb-136">`out`V této verzi byla vylepšena existující syntaxe, která podporuje parametry.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-136">The existing syntax that supports `out` parameters has been improved in this version.</span></span> <span data-ttu-id="7f6fb-137">Nyní můžete deklarovat `out` proměnné v seznamu argumentů volání metody, namísto psaní samostatného příkazu deklarace:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-137">You can now declare `out` variables in the argument list of a method call, rather than writing a separate declaration statement:</span></span>

[!code-csharp[OutVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVariableDeclarations "Out variable declarations")]

<span data-ttu-id="7f6fb-138">Možná budete chtít zadat typ `out` proměnné pro přehlednost, jak je uvedeno výše.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-138">You may want to specify the type of the `out` variable for clarity, as shown above.</span></span> <span data-ttu-id="7f6fb-139">Jazyk však podporuje použití implicitně typované lokální proměnné:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-139">However, the language does support using an implicitly typed local variable:</span></span>

[!code-csharp[OutVarVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVarVariableDeclarations "Implicitly typed Out variable")]

- <span data-ttu-id="7f6fb-140">Čtení kódu je snazší.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-140">The code is easier to read.</span></span>
  - <span data-ttu-id="7f6fb-141">Deklarujete proměnnou, kde ji používáte, nikoli na jiném řádku výše.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-141">You declare the out variable where you use it, not on another line above.</span></span>
- <span data-ttu-id="7f6fb-142">Není nutné přiřazovat počáteční hodnotu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-142">No need to assign an initial value.</span></span>
  - <span data-ttu-id="7f6fb-143">Deklarováním `out` proměnné, kde se používá ve volání metody, nemůžete ji omylně použít před přiřazením.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-143">By declaring the `out` variable where it's used in a method call, you can't accidentally use it before it is assigned.</span></span>

## <a name="tuples"></a><span data-ttu-id="7f6fb-144">N-tice</span><span class="sxs-lookup"><span data-stu-id="7f6fb-144">Tuples</span></span>

<span data-ttu-id="7f6fb-145">Jazyk C# poskytuje bohatou syntaxi pro třídy a struktury, které slouží k vysvětlení záměru návrhu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-145">C# provides a rich syntax for classes and structs that is used to explain your design intent.</span></span> <span data-ttu-id="7f6fb-146">V některých případech však bohatá syntaxe vyžaduje dodatečnou práci s minimální výhodou.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-146">But sometimes that rich syntax requires extra work with minimal benefit.</span></span> <span data-ttu-id="7f6fb-147">Můžete často napsat metody, které vyžadují jednoduchou strukturu obsahující více než jeden datový prvek.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-147">You may often write methods that need a simple structure containing more than one data element.</span></span> <span data-ttu-id="7f6fb-148">Pro podporu těchto scénářů byly přidány *řazené kolekce členů* do jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-148">To support these scenarios *tuples* were added to C#.</span></span> <span data-ttu-id="7f6fb-149">Řazené kolekce členů jsou jednoduché datové struktury, které obsahují více polí představujících datové členy.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-149">Tuples are lightweight data structures that contain multiple fields to represent the data members.</span></span>
<span data-ttu-id="7f6fb-150">Pole se neověřují a nemůžete definovat vlastní metody.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-150">The fields aren't validated, and you can't define your own methods</span></span>

> [!NOTE]
> <span data-ttu-id="7f6fb-151">Řazené kolekce členů byly k dispozici před jazykem C# 7,0, ale byly neefektivní a nemají žádnou jazykovou podporu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-151">Tuples were available before C# 7.0, but they were inefficient and had no language support.</span></span>
> <span data-ttu-id="7f6fb-152">To znamenalo, že prvky řazené kolekce členů mohou být odkazovány pouze jako `Item1` `Item2` a tak dále.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-152">This meant that tuple elements could only be referenced as `Item1`, `Item2` and so on.</span></span> <span data-ttu-id="7f6fb-153">C# 7,0 zavádí jazykovou podporu pro řazené kolekce členů, která umožňuje sémantické názvy polí řazené kolekce členů pomocí nových, efektivnějších typů řazených kolekcí členů.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-153">C# 7.0 introduces language support for tuples, which enables semantic names for the fields of a tuple using new, more efficient tuple types.</span></span>

<span data-ttu-id="7f6fb-154">Můžete vytvořit řazenou kolekci členů tak, že každému členovi přiřadíte hodnotu a volitelně zadáte sémantické názvy každé z členů řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-154">You can create a tuple by assigning a value to each member, and optionally providing semantic names to each of the members of the tuple:</span></span>

[!code-csharp[NamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#NamedTuple "Named tuple")]

<span data-ttu-id="7f6fb-155">`namedLetters`Řazená kolekce členů obsahuje pole, která jsou uvedena jako `Alpha` a `Beta` .</span><span class="sxs-lookup"><span data-stu-id="7f6fb-155">The `namedLetters` tuple contains fields referred to as `Alpha` and `Beta`.</span></span> <span data-ttu-id="7f6fb-156">Tyto názvy existují pouze v době kompilace a nejsou zachovány, například při kontrole řazené kolekce členů pomocí reflexe v době běhu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-156">Those names exist only at compile time and aren't preserved, for example when inspecting the tuple using reflection at run time.</span></span>

<span data-ttu-id="7f6fb-157">V přiřazení řazené kolekce členů můžete také zadat názvy polí na pravé straně přiřazení:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-157">In a tuple assignment, you can also specify the names of the fields on the right-hand side of the assignment:</span></span>

[!code-csharp[ImplicitNamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#ImplicitNamedTuple "Implicitly named tuple")]

<span data-ttu-id="7f6fb-158">Mohou nastat situace, kdy budete chtít odbalit členy řazené kolekce členů, které byly vráceny metodou.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-158">There may be times when you want to unpackage the members of a tuple that were returned from a method.</span></span>  <span data-ttu-id="7f6fb-159">To můžete provést deklarováním samostatných proměnných pro každou z hodnot v řazené kolekci členů.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-159">You can do that by declaring separate variables for each of the values in the tuple.</span></span> <span data-ttu-id="7f6fb-160">Toto rozbalení se označuje jako *dekonstrukce* řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-160">This unpackaging is called *deconstructing* the tuple:</span></span>

[!code-csharp[CallingWithDeconstructor](~/samples/snippets/csharp/new-in-7/program.cs#CallingWithDeconstructor "Deconstructing a tuple")]

<span data-ttu-id="7f6fb-161">Můžete také poskytnout podobný dekonstrukci pro libovolný typ v rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-161">You can also provide a similar deconstruction for any type in .NET.</span></span> <span data-ttu-id="7f6fb-162">Metodu můžete napsat `Deconstruct` jako člena třídy.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-162">You write a `Deconstruct` method as a member of the class.</span></span> <span data-ttu-id="7f6fb-163">Tato `Deconstruct` Metoda poskytuje sadu `out` argumentů pro každou z vlastností, které chcete extrahovat.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-163">That `Deconstruct` method provides a set of `out` arguments for each of the properties you want to extract.</span></span> <span data-ttu-id="7f6fb-164">Zvažte tuto `Point` třídu, která poskytuje metodu dekonstruktoru, která extrahuje `X` `Y` souřadnice a:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-164">Consider this `Point` class that provides a deconstructor method that extracts the `X` and `Y` coordinates:</span></span>

[!code-csharp[PointWithDeconstruction](~/samples/snippets/csharp/new-in-7/point.cs#PointWithDeconstruction "Point with deconstruction method")]

<span data-ttu-id="7f6fb-165">Jednotlivá pole můžete extrahovat přiřazením `Point` k řazené kolekci členů:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-165">You can extract the individual fields by assigning a `Point` to a tuple:</span></span>

[!code-csharp[DeconstructPoint](~/samples/snippets/csharp/new-in-7/program.cs#DeconstructPoint "Deconstruct a point")]

<span data-ttu-id="7f6fb-166">Podrobnější informace o řazených kolekcích členů najdete v [článku o řazených kolekcích členů](../tuples.md).</span><span class="sxs-lookup"><span data-stu-id="7f6fb-166">You can learn more in depth about tuples in the [tuples article](../tuples.md).</span></span>

## <a name="discards"></a><span data-ttu-id="7f6fb-167">Zahození</span><span class="sxs-lookup"><span data-stu-id="7f6fb-167">Discards</span></span>

<span data-ttu-id="7f6fb-168">Často při dekonstrukci řazené kolekce členů nebo volání metody s `out` parametry jste nuceni definovat proměnnou, jejíž hodnotu nezáleží na a nehodláte ji používat.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-168">Often when deconstructing a tuple or calling a method with `out` parameters, you're forced to define a variable whose value you don't care about and don't intend to use.</span></span> <span data-ttu-id="7f6fb-169">Jazyk C# přidává podporu pro *zahození* pro zpracování tohoto scénáře.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-169">C# adds support for *discards* to handle this scenario.</span></span> <span data-ttu-id="7f6fb-170">Zrušení je proměnná pouze pro zápis, jejíž název je (podtržítko `_` ). můžete přiřadit všechny hodnoty, které chcete zahodit do jedné proměnné.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-170">A discard is a write-only variable whose name is `_` (the underscore character); you can assign all of the values that you intend to discard to the single variable.</span></span> <span data-ttu-id="7f6fb-171">Zahození je jako Nepřiřazená proměnná; Kromě příkazu přiřazení nelze zrušit použití v kódu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-171">A discard is like an unassigned variable; apart from the assignment statement, the discard can't be used in code.</span></span>

<span data-ttu-id="7f6fb-172">Zahození jsou podporovaná v následujících scénářích:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-172">Discards are supported in the following scenarios:</span></span>

- <span data-ttu-id="7f6fb-173">Při dekonstrukci řazených kolekcí členů nebo uživatelsky definovaných typů.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-173">When deconstructing tuples or user-defined types.</span></span>
- <span data-ttu-id="7f6fb-174">Při volání metod s [výstupními](../language-reference/keywords/out-parameter-modifier.md) parametry.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-174">When calling methods with [out](../language-reference/keywords/out-parameter-modifier.md) parameters.</span></span>
- <span data-ttu-id="7f6fb-175">V operaci porovnávání vzorů s příkazy [is](../language-reference/keywords/is.md) a [Switch](../language-reference/keywords/switch.md) .</span><span class="sxs-lookup"><span data-stu-id="7f6fb-175">In a pattern matching operation with the [is](../language-reference/keywords/is.md) and [switch](../language-reference/keywords/switch.md) statements.</span></span>
- <span data-ttu-id="7f6fb-176">Jako samostatný identifikátor, pokud chcete explicitně identifikovat hodnotu přiřazení jako zahození.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-176">As a standalone identifier when you want to explicitly identify the value of an assignment as a discard.</span></span>

<span data-ttu-id="7f6fb-177">Následující příklad definuje `QueryCityDataForYears` metodu, která vrátí 6 – řazenou kolekci členů, která obsahuje data pro město dvou různých roků.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-177">The following example defines a `QueryCityDataForYears` method that returns a 6-tuple that contains data for a city for two different years.</span></span> <span data-ttu-id="7f6fb-178">Volání metody v příkladu se týká pouze dvou hodnot naplnění vrácených metodou, takže poběží zbývající hodnoty v řazené kolekci členů jako zahození při dekonstrukci řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-178">The method call in the example is concerned only with the two population values returned by the method and so treats the remaining values in the tuple as discards when it deconstructs the tuple.</span></span>

[!code-csharp[Tuple-discard](~/samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="7f6fb-179">Další informace najdete v tématu [zahození](../discards.md).</span><span class="sxs-lookup"><span data-stu-id="7f6fb-179">For more information, see [Discards](../discards.md).</span></span>

## <a name="pattern-matching"></a><span data-ttu-id="7f6fb-180">Porovnávání vzorů</span><span class="sxs-lookup"><span data-stu-id="7f6fb-180">Pattern matching</span></span>

<span data-ttu-id="7f6fb-181">*Porovnávání vzorů* je funkce, která umožňuje implementovat odeslání metody pro jiné vlastnosti než typ objektu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-181">*Pattern matching* is a feature that allows you to implement method dispatch on properties other than the type of an object.</span></span> <span data-ttu-id="7f6fb-182">V závislosti na typu objektu již pravděpodobně znáte způsob odeslání metody.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-182">You're probably already familiar with method dispatch based on the type of an object.</span></span> <span data-ttu-id="7f6fb-183">V objektově orientovaném programování metody Virtual a override poskytují jazykovou syntaxi pro implementaci metody odeslání na základě typu objektu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-183">In object-oriented programming, virtual and override methods provide language syntax to implement method dispatching based on an object's type.</span></span> <span data-ttu-id="7f6fb-184">Základní a odvozené třídy poskytují různé implementace.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-184">Base and Derived classes provide different implementations.</span></span>
<span data-ttu-id="7f6fb-185">Výrazy pro porovnávání vzorů přesahují tento koncept, takže můžete snadno implementovat podobné vzory odeslání pro typy a datové prvky, které nesouvisí s hierarchií dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-185">Pattern matching expressions extend this concept so that you can easily implement similar dispatch patterns for types and data elements that aren't related through an inheritance hierarchy.</span></span>

<span data-ttu-id="7f6fb-186">Porovnávání vzorů podporuje `is` výrazy a `switch` výrazy.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-186">Pattern matching supports `is` expressions and `switch` expressions.</span></span> <span data-ttu-id="7f6fb-187">Každá z nich umožňuje kontrolu objektu a jeho vlastností, aby bylo možné určit, zda tento objekt splňuje požadovaný vzor.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-187">Each enables inspecting an object and its properties to determine if that object satisfies the sought pattern.</span></span> <span data-ttu-id="7f6fb-188">`when`Klíčové slovo můžete použít k určení dalších pravidel pro vzor.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-188">You use the `when` keyword to specify additional rules to the pattern.</span></span>

<span data-ttu-id="7f6fb-189">`is`Výraz Pattern rozšiřuje známý [ `is` operátor](../language-reference/keywords/is.md#pattern-matching-with-is) pro dotazování objektu o jeho typu a přiřazení výsledku do jedné instrukce.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-189">The `is` pattern expression extends the familiar [`is` operator](../language-reference/keywords/is.md#pattern-matching-with-is) to query an object about its type and assign the result in one instruction.</span></span> <span data-ttu-id="7f6fb-190">Následující kód zkontroluje, zda je proměnná `int` , a pokud ano, přidá ji do aktuálního součtu:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-190">The following code checks if a variable is an `int`, and if so, adds it to the current sum:</span></span>

```csharp
if (input is int count)
    sum += count;
```

<span data-ttu-id="7f6fb-191">Předchozí malý příklad ukazuje vylepšení `is` výrazu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-191">The preceding small example demonstrates the enhancements to the `is` expression.</span></span> <span data-ttu-id="7f6fb-192">Můžete testovat proti typům hodnot i odkazovým typům a k nové proměnné správného typu můžete přiřadit úspěšný výsledek.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-192">You can test against value types as well as reference types, and you can assign the successful result to a new variable of the correct type.</span></span>

<span data-ttu-id="7f6fb-193">Výraz porovnávání přepínačů má známou syntaxi na základě příkazu, který je `switch` již součástí jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-193">The switch match expression has a familiar syntax, based on the `switch` statement already part of the C# language.</span></span> <span data-ttu-id="7f6fb-194">Aktualizovaný příkaz switch má několik nových konstrukcí:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-194">The updated switch statement has several new constructs:</span></span>

- <span data-ttu-id="7f6fb-195">Typ řízení `switch` výrazu již není omezen na celočíselné typy, `Enum` typy, `string` ani typ s možnou hodnotou null, který odpovídá jednomu z těchto typů.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-195">The governing type of a `switch` expression is no longer restricted to integral types, `Enum` types, `string`, or a nullable type corresponding to one of those types.</span></span> <span data-ttu-id="7f6fb-196">Může být použit libovolný typ.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-196">Any type may be used.</span></span>
- <span data-ttu-id="7f6fb-197">Můžete testovat typ `switch` výrazu v každém `case` popisku.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-197">You can test the type of the `switch` expression in each `case` label.</span></span> <span data-ttu-id="7f6fb-198">Stejně jako u `is` výrazu můžete k tomuto typu přiřadit novou proměnnou.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-198">As with the `is` expression, you may assign a new variable to that type.</span></span>
- <span data-ttu-id="7f6fb-199">Můžete přidat `when` klauzuli pro další podmínky testování v této proměnné.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-199">You may add a `when` clause to further test conditions on that variable.</span></span>
- <span data-ttu-id="7f6fb-200">Pořadí `case` popisků je teď důležité.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-200">The order of `case` labels is now important.</span></span> <span data-ttu-id="7f6fb-201">První větev, která se má porovnat, se spustí; ostatní se přeskočí.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-201">The first branch to match is executed; others are skipped.</span></span>

<span data-ttu-id="7f6fb-202">Následující kód demonstruje tyto nové funkce:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-202">The following code demonstrates these new features:</span></span>

```csharp
public static int SumPositiveNumbers(IEnumerable<object> sequence)
{
    int sum = 0;
    foreach (var i in sequence)
    {
        switch (i)
        {
            case 0:
                break;
            case IEnumerable<int> childSequence:
            {
                foreach(var item in childSequence)
                    sum += (item > 0) ? item : 0;
                break;
            }
            case int n when n > 0:
                sum += n;
                break;
            case null:
                throw new NullReferenceException("Null found in sequence");
            default:
                throw new InvalidOperationException("Unrecognized type");
        }
    }
    return sum;
}
```

- <span data-ttu-id="7f6fb-203">`case 0:`je známý konstantní vzor.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-203">`case 0:` is the familiar constant pattern.</span></span>
- <span data-ttu-id="7f6fb-204">`case IEnumerable<int> childSequence:`je vzor typu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-204">`case IEnumerable<int> childSequence:` is a type pattern.</span></span>
- <span data-ttu-id="7f6fb-205">`case int n when n > 0:`je vzor typu s další `when` podmínkou.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-205">`case int n when n > 0:` is a type pattern with an additional `when` condition.</span></span>
- <span data-ttu-id="7f6fb-206">`case null:`je vzor null.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-206">`case null:` is the null pattern.</span></span>
- <span data-ttu-id="7f6fb-207">`default:`je známý výchozí případ.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-207">`default:` is the familiar default case.</span></span>

<span data-ttu-id="7f6fb-208">Další informace o porovnávání vzorů najdete v [porovnávání vzorů v jazyce C#](../pattern-matching.md).</span><span class="sxs-lookup"><span data-stu-id="7f6fb-208">You can learn more about pattern matching in [Pattern Matching in C#](../pattern-matching.md).</span></span>

## <a name="ref-locals-and-returns"></a><span data-ttu-id="7f6fb-209">Místní a návratové hodnoty REF</span><span class="sxs-lookup"><span data-stu-id="7f6fb-209">Ref locals and returns</span></span>

<span data-ttu-id="7f6fb-210">Tato funkce umožňuje algoritmy, které používají a vracejí odkazy na proměnné definované jinde.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-210">This feature enables algorithms that use and return references to variables defined elsewhere.</span></span> <span data-ttu-id="7f6fb-211">Jeden příklad pracuje s velkými maticemi a hledá jedno umístění s určitými charakteristikami.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-211">One example is working with large matrices, and finding a single location with certain characteristics.</span></span> <span data-ttu-id="7f6fb-212">Následující metoda vrátí **odkaz** na toto úložiště v matici:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-212">The following method returns a **reference** to that storage in the matrix:</span></span>

[!code-csharp[FindReturningRef](~/samples/snippets/csharp/new-in-7/MatrixSearch.cs#FindReturningRef "Find returning by reference")]

<span data-ttu-id="7f6fb-213">Můžete deklarovat návratovou hodnotu jako `ref` a upravit tuto hodnotu v matici, jak je znázorněno v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-213">You can declare the return value as a `ref` and modify that value in the matrix, as shown in the following code:</span></span>

[!code-csharp[AssignRefReturn](~/samples/snippets/csharp/new-in-7/Program.cs#AssignRefReturn "Assign ref return")]

<span data-ttu-id="7f6fb-214">Jazyk C# obsahuje několik pravidel, která vás chrání před nepoužitím `ref` místních hodnot a vrátí:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-214">The C# language has several rules that protect you from misusing the `ref` locals and returns:</span></span>

- <span data-ttu-id="7f6fb-215">Klíčové slovo je nutné přidat `ref` do podpisu metody a do všech `return` příkazů v metodě.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-215">You must add the `ref` keyword to the method signature and to all `return` statements in a method.</span></span>
  - <span data-ttu-id="7f6fb-216">Tím se vymaže metoda, kterou vrátí odkaz v rámci této metody.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-216">That makes it clear the method returns by reference throughout the method.</span></span>
- <span data-ttu-id="7f6fb-217">`ref return`Může být přiřazená k proměnné hodnoty nebo `ref` proměnné.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-217">A `ref return` may be assigned to a value variable, or a `ref` variable.</span></span>
  - <span data-ttu-id="7f6fb-218">Volající řídí, zda je vrácená hodnota zkopírována nebo ne.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-218">The caller controls whether the return value is copied or not.</span></span> <span data-ttu-id="7f6fb-219">Vynechání `ref` modifikátoru při přiřazování návratové hodnoty znamená, že volající chce kopii hodnoty, nikoli odkaz na úložiště.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-219">Omitting the `ref` modifier when assigning the return value indicates that the caller wants a copy of the value, not a reference to the storage.</span></span>
- <span data-ttu-id="7f6fb-220">Nemůžete přiřadit návratovou hodnotu standardní metody `ref` místní proměnné.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-220">You can't assign a standard method return value to a `ref` local variable.</span></span>
  - <span data-ttu-id="7f6fb-221">Která nepovoluje příkazy jako`ref int i = sequence.Count();`</span><span class="sxs-lookup"><span data-stu-id="7f6fb-221">That disallows statements like `ref int i = sequence.Count();`</span></span>
- <span data-ttu-id="7f6fb-222">Nemůžete vrátit `ref` do proměnné, jejíž doba života nepřesahuje po provedení metody.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-222">You can't return a `ref` to a variable whose lifetime doesn't extend beyond the execution of the method.</span></span>
  - <span data-ttu-id="7f6fb-223">To znamená, že nemůžete vrátit odkaz na místní proměnnou nebo proměnnou s podobným oborem.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-223">That means you can't return a reference to a local variable or a variable with a similar scope.</span></span>
- <span data-ttu-id="7f6fb-224">`ref`lokální hodnoty a vrácení nelze použít s asynchronními metodami.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-224">`ref` locals and returns can't be used with async methods.</span></span>
  - <span data-ttu-id="7f6fb-225">Kompilátor nemůže zjistit, zda se odkazovaná proměnná nastavila na konečnou hodnotu, když vrátí asynchronní metoda.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-225">The compiler can't know if the referenced variable has been set to its final value when the async method returns.</span></span>

<span data-ttu-id="7f6fb-226">Přidání místních a návratových odkazů umožňuje algoritmy, které jsou efektivnější, protože se vyhnete kopírování hodnot nebo provádění operací zrušení odkazování vícekrát.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-226">The addition of ref locals and ref returns enables algorithms that are more efficient by avoiding copying values, or performing dereferencing operations multiple times.</span></span>

<span data-ttu-id="7f6fb-227">Přidání `ref` do návratové hodnoty je [Změna kompatibilní se zdrojem](version-update-considerations.md#source-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="7f6fb-227">Adding `ref` to the return value is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span> <span data-ttu-id="7f6fb-228">Existující kód je zkompilován, ale návratová hodnota REF je při přiřazení zkopírována.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-228">Existing code compiles, but the ref return value is copied when assigned.</span></span> <span data-ttu-id="7f6fb-229">Volající musí aktualizovat úložiště pro návratovou hodnotu na `ref` lokální proměnnou, aby se vrácení vrátilo jako odkaz.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-229">Callers must update the storage for the return value to a `ref` local variable to store the return as a reference.</span></span>

<span data-ttu-id="7f6fb-230">Další informace najdete v článku věnovaném [klíčovému slovu ref](../language-reference/keywords/ref.md) .</span><span class="sxs-lookup"><span data-stu-id="7f6fb-230">For more information, see the [ref keyword](../language-reference/keywords/ref.md) article.</span></span>

## <a name="local-functions"></a><span data-ttu-id="7f6fb-231">Lokální funkce</span><span class="sxs-lookup"><span data-stu-id="7f6fb-231">Local functions</span></span>

<span data-ttu-id="7f6fb-232">Mnoho návrhů pro třídy obsahuje metody, které jsou volány pouze z jednoho umístění.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-232">Many designs for classes include methods that are called from only one location.</span></span> <span data-ttu-id="7f6fb-233">Tyto další privátní metody udržují jednotlivé metody malé a prioritní.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-233">These additional private methods keep each method small and focused.</span></span> <span data-ttu-id="7f6fb-234">*Místní funkce* umožňují deklarovat metody v kontextu jiné metody.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-234">*Local functions* enable you to declare methods inside the context of another method.</span></span> <span data-ttu-id="7f6fb-235">Místní funkce usnadňují čtenářům třídy, aby bylo vidět, že místní metoda je volána pouze z kontextu, ve kterém je deklarována.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-235">Local functions make it easier for readers of the class to see that the local method is only called from the context in which it is declared.</span></span>

<span data-ttu-id="7f6fb-236">Existují dva běžné případy použití pro místní funkce: veřejné metody iterátoru a veřejné asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-236">There are two common use cases for local functions: public iterator methods and public async methods.</span></span> <span data-ttu-id="7f6fb-237">Oba typy metod generují kód, který hlásí chyby později než programátoři mohou očekávat.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-237">Both types of methods generate code that reports errors later than programmers might expect.</span></span> <span data-ttu-id="7f6fb-238">V metodách iterátoru jsou všechny výjimky pozorovány pouze při volání kódu, který vytvoří výčet vrácené sekvence.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-238">In iterator methods, any exceptions are observed only when calling code that enumerates the returned sequence.</span></span> <span data-ttu-id="7f6fb-239">V asynchronních metodách jsou jakékoli výjimky pozorovány pouze v případě, že `Task` je očekáváno vrácení.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-239">In async methods, any exceptions are only observed when the returned `Task` is awaited.</span></span> <span data-ttu-id="7f6fb-240">Následující příklad ukazuje oddělení ověřování parametrů z implementace iterátoru pomocí místní funkce:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-240">The following example demonstrates separating parameter validation from the iterator implementation using a local function:</span></span>

[!code-csharp[22_IteratorMethodLocal](~/samples/snippets/csharp/new-in-7/Iterator.cs#IteratorMethodLocal "Iterator method with local function")]

<span data-ttu-id="7f6fb-241">Stejný postup lze použít s `async` metodami, aby bylo zajištěno, že výjimky vznikající z ověřování argumentu jsou vyvolány před zahájením asynchronní práce:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-241">The same technique can be employed with `async` methods to ensure that exceptions arising from argument validation are thrown before the asynchronous work begins:</span></span>

[!code-csharp[TaskExample](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#TaskExample "Task returning method with local function")]

> [!NOTE]
> <span data-ttu-id="7f6fb-242">Některé z návrhů, které jsou podporovány místními funkcemi, lze také provést pomocí *výrazů lambda*.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-242">Some of the designs that are supported by local functions can also be accomplished using *lambda expressions*.</span></span> <span data-ttu-id="7f6fb-243">Další informace naleznete v tématu [místní funkce vs. výrazy lambda](../programming-guide/classes-and-structs/local-functions.md#local-functions-vs-lambda-expressions).</span><span class="sxs-lookup"><span data-stu-id="7f6fb-243">For more information, see [Local functions vs. lambda expressions](../programming-guide/classes-and-structs/local-functions.md#local-functions-vs-lambda-expressions).</span></span>

## <a name="more-expression-bodied-members"></a><span data-ttu-id="7f6fb-244">Další členové Expression-těle</span><span class="sxs-lookup"><span data-stu-id="7f6fb-244">More expression-bodied members</span></span>

<span data-ttu-id="7f6fb-245">C# 6 zavedlo [členy Expression-těle](csharp-6.md#expression-bodied-function-members) pro členské funkce a vlastnosti jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-245">C# 6 introduced [expression-bodied members](csharp-6.md#expression-bodied-function-members) for member functions, and read-only properties.</span></span> <span data-ttu-id="7f6fb-246">C# 7,0 rozšiřuje povolené členy, které lze implementovat jako výrazy.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-246">C# 7.0 expands the allowed members that can be implemented as expressions.</span></span> <span data-ttu-id="7f6fb-247">V C# 7,0 můžete implementovat *konstruktory*, *finalizační metody*a `get` `set` přístupové objekty do *vlastností* a *indexerů*.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-247">In C# 7.0, you can implement *constructors*, *finalizers*, and `get` and `set` accessors on *properties* and *indexers*.</span></span> <span data-ttu-id="7f6fb-248">Následující kód ukazuje příklady jednotlivých:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-248">The following code shows examples of each:</span></span>

[!code-csharp[ExpressionBodiedMembers](~/samples/snippets/csharp/new-in-7/expressionmembers.cs#ExpressionBodiedEverything "new expression-bodied members")]

> [!NOTE]
> <span data-ttu-id="7f6fb-249">Tento příklad nepotřebuje finalizační metodu, ale je zobrazený k předvedení syntaxe.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-249">This example does not need a finalizer, but it is shown to demonstrate the syntax.</span></span> <span data-ttu-id="7f6fb-250">V třídě byste neměli implementovat finalizační metodu, pokud není nutné vydávat nespravované prostředky.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-250">You should not implement a finalizer in your class unless it is necessary to  release unmanaged resources.</span></span> <span data-ttu-id="7f6fb-251">Měli byste také zvážit použití <xref:System.Runtime.InteropServices.SafeHandle> třídy namísto přímého spravování nespravovaných prostředků.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-251">You should also consider using the <xref:System.Runtime.InteropServices.SafeHandle> class instead of managing unmanaged resources directly.</span></span>

<span data-ttu-id="7f6fb-252">Tato nová umístění pro členy Expression-těle reprezentují důležitý milník pro jazyk C#: tyto funkce byly implementovány členy komunity, kteří pracují na open source projektu [Roslyn](https://github.com/dotnet/Roslyn) .</span><span class="sxs-lookup"><span data-stu-id="7f6fb-252">These new locations for expression-bodied members represent an important milestone for the C# language: These features were implemented by community members working on the open-source [Roslyn](https://github.com/dotnet/Roslyn) project.</span></span>

<span data-ttu-id="7f6fb-253">Změna metody na člen výrazu těle je [binární kompatibilní změna](version-update-considerations.md#binary-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="7f6fb-253">Changing a method to an expression bodied member is a [binary compatible change](version-update-considerations.md#binary-compatible-changes).</span></span>

## <a name="throw-expressions"></a><span data-ttu-id="7f6fb-254">Výrazy throw</span><span class="sxs-lookup"><span data-stu-id="7f6fb-254">Throw expressions</span></span>

<span data-ttu-id="7f6fb-255">V jazyce C# `throw` byl vždy příkaz.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-255">In C#, `throw` has always been a statement.</span></span> <span data-ttu-id="7f6fb-256">Vzhledem k tomu `throw` , že je příkaz, nikoli výraz, byly zjištěny konstrukce jazyka C#, kde ji nelze použít.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-256">Because `throw` is a statement, not an expression, there were C# constructs where you couldn't use it.</span></span> <span data-ttu-id="7f6fb-257">Tyto zahrnuté podmíněné výrazy, null slučovací výrazy a některé lambda výrazy.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-257">These included conditional expressions, null coalescing expressions, and some lambda expressions.</span></span> <span data-ttu-id="7f6fb-258">Přidání členů Expression-těle přidá více míst, kde `throw` by bylo užitečné výrazy.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-258">The addition of expression-bodied members adds more locations where `throw` expressions would be useful.</span></span> <span data-ttu-id="7f6fb-259">Takže můžete napsat jakýkoliv z těchto konstrukcí, C# 7,0 zavádí [*výrazy throw*](../language-reference/keywords/throw.md#the-throw-expression).</span><span class="sxs-lookup"><span data-stu-id="7f6fb-259">So that you can write any of these constructs, C# 7.0 introduces [*throw expressions*](../language-reference/keywords/throw.md#the-throw-expression).</span></span>

<span data-ttu-id="7f6fb-260">Díky tomu je snazší psát další kód založený na výrazu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-260">This addition makes it easier to write more expression-based code.</span></span> <span data-ttu-id="7f6fb-261">Pro kontrolu chyb nepotřebujete další příkazy.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-261">You don't need additional statements for error checking.</span></span>

## <a name="generalized-async-return-types"></a><span data-ttu-id="7f6fb-262">Generalizované asynchronní návratové typy</span><span class="sxs-lookup"><span data-stu-id="7f6fb-262">Generalized async return types</span></span>

<span data-ttu-id="7f6fb-263">Vrácení `Task` objektu z asynchronních metod může způsobit kritické body výkonu v určitých cestách.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-263">Returning a `Task` object from async methods can introduce performance bottlenecks in certain paths.</span></span> <span data-ttu-id="7f6fb-264">`Task`je odkazový typ, takže jeho použití znamená přidělení objektu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-264">`Task` is a reference type, so using it means allocating an object.</span></span> <span data-ttu-id="7f6fb-265">V případech, kdy metoda deklarovaná s `async` modifikátorem vrací výsledek uložený v mezipaměti, nebo je dokončena synchronně, se další přidělení můžou stát značnými náklady v částech kritického výkonu v kódu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-265">In cases where a method declared with the `async` modifier returns a cached result, or completes synchronously, the extra allocations can become a significant time cost in performance critical sections of code.</span></span> <span data-ttu-id="7f6fb-266">Pokud dojde k přidělení v těsných smyčkách, může to být nákladné.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-266">It can become costly if those allocations occur in tight loops.</span></span>

<span data-ttu-id="7f6fb-267">Nová funkce jazyka znamená, že návratové typy asynchronní metody nejsou omezeny na `Task` , `Task<T>` a `void` .</span><span class="sxs-lookup"><span data-stu-id="7f6fb-267">The new language feature means that async method return types aren't limited to `Task`, `Task<T>`, and `void`.</span></span> <span data-ttu-id="7f6fb-268">Vrácený typ musí stále splňovat asynchronní vzorek, což znamená, že `GetAwaiter` Metoda musí být přístupná.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-268">The returned type must still satisfy the async pattern, meaning a `GetAwaiter` method must be accessible.</span></span> <span data-ttu-id="7f6fb-269">V rámci jednoho konkrétního příkladu byl `ValueTask` typ přidán do rozhraní .NET, aby bylo možné používat tuto novou jazykovou funkci:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-269">As one concrete example, the `ValueTask` type has been added to .NET to make use of this new language feature:</span></span>

[!code-csharp[UsingValueTask](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#UsingValueTask "Using ValueTask")]

> [!NOTE]
> <span data-ttu-id="7f6fb-270">Aby bylo možné typ použít, je nutné přidat balíček NuGet [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) <xref:System.Threading.Tasks.ValueTask%601> .</span><span class="sxs-lookup"><span data-stu-id="7f6fb-270">You need to add the NuGet package [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) in order to use the <xref:System.Threading.Tasks.ValueTask%601> type.</span></span>

<span data-ttu-id="7f6fb-271">Toto vylepšení je nejužitečnější pro autory knihovny, aby nedošlo k přidělení `Task` v kritickém kódu výkonu.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-271">This enhancement is most useful for library authors to avoid allocating a `Task` in performance critical code.</span></span>

## <a name="numeric-literal-syntax-improvements"></a><span data-ttu-id="7f6fb-272">Vylepšení syntaxe numerického literálu</span><span class="sxs-lookup"><span data-stu-id="7f6fb-272">Numeric literal syntax improvements</span></span>

<span data-ttu-id="7f6fb-273">Nepřečtené číselné konstanty mohou ztížit pochopení kódu při prvním čtení.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-273">Misreading numeric constants can make it harder to understand code when reading it for the first time.</span></span> <span data-ttu-id="7f6fb-274">Bitové masky nebo jiné symbolické hodnoty jsou náchylné k nepochopení.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-274">Bit masks or other symbolic values are prone to misunderstanding.</span></span> <span data-ttu-id="7f6fb-275">C# 7,0 obsahuje dvě nové funkce pro zápis čísel v nejčitelnějším způsobu pro zamýšlené použití: *binární literály*a *oddělovače číslic*.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-275">C# 7.0 includes two new features to write numbers in the most readable fashion for the intended use: *binary literals*, and *digit separators*.</span></span>

<span data-ttu-id="7f6fb-276">V případech, kdy vytváříte bitové masky, nebo pokaždé, když binární reprezentace čísla provede Nejčitelnější kód, zapište toto číslo v binárním formátu:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-276">For those times when you're creating bit masks, or whenever a binary representation of a number makes the most readable code, write that number in binary:</span></span>

[!code-csharp[ThousandSeparators](~/samples/snippets/csharp/new-in-7/Program.cs#ThousandSeparators "Thousands separators")]

<span data-ttu-id="7f6fb-277">Na `0b` začátku konstanty označuje, že číslo je zapsáno jako binární číslo.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-277">The `0b` at the beginning of the constant indicates that the number is written as a binary number.</span></span> <span data-ttu-id="7f6fb-278">Binární čísla mohou být dlouhá, takže je často snazší zobrazit bitové vzory tím, že zavedete `_` jako oddělovač číslic, jak je uvedeno výše v binární konstantě.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-278">Binary numbers can get long, so it's often easier to see the bit patterns by introducing the `_` as a digit separator, as shown above in the binary constant.</span></span> <span data-ttu-id="7f6fb-279">Oddělovač číslic se může objevit kdekoli v konstantě.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-279">The digit separator can appear anywhere in the constant.</span></span> <span data-ttu-id="7f6fb-280">U základních 10 čísel je běžné ji použít jako oddělovač tisíců:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-280">For base 10 numbers, it is common to use it as a thousands separator:</span></span>

[!code-csharp[LargeIntegers](~/samples/snippets/csharp/new-in-7/Program.cs#LargeIntegers "Large integer")]

<span data-ttu-id="7f6fb-281">Oddělovač číslic lze použít s `decimal` `float` typy, a `double` také:</span><span class="sxs-lookup"><span data-stu-id="7f6fb-281">The digit separator can be used with `decimal`, `float`, and `double` types as well:</span></span>

[!code-csharp[OtherConstants](~/samples/snippets/csharp/new-in-7/Program.cs#OtherConstants "non-integral constants")]

<span data-ttu-id="7f6fb-282">Společně můžete deklarovat číselné konstanty s mnohem větší čitelností.</span><span class="sxs-lookup"><span data-stu-id="7f6fb-282">Taken together, you can declare numeric constants with much more readability.</span></span>
