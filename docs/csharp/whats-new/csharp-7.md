---
title: Co je nového v jazyce C# 7.0 – průvodce v C#
description: Získejte přehled o nové funkce ve verzi 7.0 C# jazyka.
ms.date: 02/20/2019
ms.assetid: fd41596d-d0c2-4816-b94d-c4d00a5d0243
ms.openlocfilehash: 58d43167341b69e7e9ac67024e9993cf51c26c0b
ms.sourcegitcommit: 127343afce8422bfa944c8b0c4ecc8f79f653255
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/25/2019
ms.locfileid: "67347449"
---
# <a name="whats-new-in-c-70"></a><span data-ttu-id="788eb-103">Co je nového v jazyce C# 7.0</span><span class="sxs-lookup"><span data-stu-id="788eb-103">What's new in C# 7.0</span></span>

<span data-ttu-id="788eb-104">C# 7.0 přidá několik nových funkcí jazyka C#:</span><span class="sxs-lookup"><span data-stu-id="788eb-104">C# 7.0 adds a number of new features to the C# language:</span></span>
* [<span data-ttu-id="788eb-105">`out` Proměnné</span><span class="sxs-lookup"><span data-stu-id="788eb-105">`out` variables</span></span>](#out-variables)
  - <span data-ttu-id="788eb-106">Je možné deklarovat `out` hodnoty inline jako argumentů metody, které používají.</span><span class="sxs-lookup"><span data-stu-id="788eb-106">You can declare `out` values inline as arguments to the method where they're used.</span></span>
* [<span data-ttu-id="788eb-107">Řazené kolekce členů</span><span class="sxs-lookup"><span data-stu-id="788eb-107">Tuples</span></span>](#tuples)
  - <span data-ttu-id="788eb-108">Můžete vytvořit jednoduchý, Nepojmenované typy, které obsahují více veřejná pole.</span><span class="sxs-lookup"><span data-stu-id="788eb-108">You can create lightweight, unnamed types that contain multiple public fields.</span></span> <span data-ttu-id="788eb-109">Kompilátory a nástroje pro prostředí IDE pochopit sémantiku z těchto typů.</span><span class="sxs-lookup"><span data-stu-id="788eb-109">Compilers and IDE tools understand the semantics of these types.</span></span>
* [<span data-ttu-id="788eb-110">Zahození</span><span class="sxs-lookup"><span data-stu-id="788eb-110">Discards</span></span>](#discards)
  - <span data-ttu-id="788eb-111">Zahození jsou dočasné, jen pro zápis proměnné při nezáleží hodnota přiřazená v přiřazeních.</span><span class="sxs-lookup"><span data-stu-id="788eb-111">Discards are temporary, write-only variables used in assignments when you don't care about the value assigned.</span></span> <span data-ttu-id="788eb-112">Jsou nejužitečnější tehdy, když dekonstrukce řazených kolekcí členů a uživatelem definovaných typů, stejně jako při volání metody s `out` parametry.</span><span class="sxs-lookup"><span data-stu-id="788eb-112">They're most useful when deconstructing tuples and user-defined types, as well as when calling methods with `out` parameters.</span></span>
* [<span data-ttu-id="788eb-113">Porovnávání vzorů</span><span class="sxs-lookup"><span data-stu-id="788eb-113">Pattern Matching</span></span>](#pattern-matching)
  - <span data-ttu-id="788eb-114">Můžete vytvořit na základě libovolné typy a hodnoty členů z těchto typů logiky větvení.</span><span class="sxs-lookup"><span data-stu-id="788eb-114">You can create branching logic based on arbitrary types and values of the members of those types.</span></span>
* [<span data-ttu-id="788eb-115">`ref` místní hodnoty a vrátí</span><span class="sxs-lookup"><span data-stu-id="788eb-115">`ref` locals and returns</span></span>](#ref-locals-and-returns)
  - <span data-ttu-id="788eb-116">Metoda lokálních proměnných a návratovými hodnotami může být odkazy na další úložiště.</span><span class="sxs-lookup"><span data-stu-id="788eb-116">Method local variables and return values can be references to other storage.</span></span>
* [<span data-ttu-id="788eb-117">Lokální funkce</span><span class="sxs-lookup"><span data-stu-id="788eb-117">Local Functions</span></span>](#local-functions)
  - <span data-ttu-id="788eb-118">Je možné vnořovat funkce do jiných funkcí a omezit jejich rozsah a viditelnost.</span><span class="sxs-lookup"><span data-stu-id="788eb-118">You can nest functions inside other functions to limit their scope and visibility.</span></span>
* [<span data-ttu-id="788eb-119">Více členů s výrazem v těle</span><span class="sxs-lookup"><span data-stu-id="788eb-119">More expression-bodied members</span></span>](#more-expression-bodied-members)
  - <span data-ttu-id="788eb-120">Seznam členů, které mohou být vytvořeny pomocí výrazů zvětšil na velikost.</span><span class="sxs-lookup"><span data-stu-id="788eb-120">The list of members that can be authored using expressions has grown.</span></span>
* [<span data-ttu-id="788eb-121">`throw` Výrazy</span><span class="sxs-lookup"><span data-stu-id="788eb-121">`throw` Expressions</span></span>](#throw-expressions)
  - <span data-ttu-id="788eb-122">Může vyvolat výjimky v konstruktorech kódu, které dříve nebyly povoleny, protože `throw` byl příkaz.</span><span class="sxs-lookup"><span data-stu-id="788eb-122">You can throw exceptions in code constructs that previously weren't allowed because `throw` was a statement.</span></span>
* [<span data-ttu-id="788eb-123">Zobecněný asynchronní návratové typy</span><span class="sxs-lookup"><span data-stu-id="788eb-123">Generalized async return types</span></span>](#generalized-async-return-types)
  - <span data-ttu-id="788eb-124">Metody deklarované s `async` modifikátor může vrátit jiné typy kromě `Task` a `Task<T>`.</span><span class="sxs-lookup"><span data-stu-id="788eb-124">Methods declared with the `async` modifier can return other types in addition to `Task` and `Task<T>`.</span></span>
* [<span data-ttu-id="788eb-125">Vylepšení číselný literál syntaxe</span><span class="sxs-lookup"><span data-stu-id="788eb-125">Numeric literal syntax improvements</span></span>](#numeric-literal-syntax-improvements)
  - <span data-ttu-id="788eb-126">Nové tokeny zlepšit čitelnost pro číselné konstanty.</span><span class="sxs-lookup"><span data-stu-id="788eb-126">New tokens improve readability for numeric constants.</span></span>

<span data-ttu-id="788eb-127">Zbývající část tohoto článku poskytuje přehled o jednotlivých funkcí.</span><span class="sxs-lookup"><span data-stu-id="788eb-127">The remainder of this article provides an overview of each feature.</span></span> <span data-ttu-id="788eb-128">Pro jednotlivé funkce dozvíte zdůvodnění.</span><span class="sxs-lookup"><span data-stu-id="788eb-128">For each feature, you'll learn the reasoning behind it.</span></span> <span data-ttu-id="788eb-129">Dozvíte syntaxe.</span><span class="sxs-lookup"><span data-stu-id="788eb-129">You'll learn the syntax.</span></span> <span data-ttu-id="788eb-130">Můžete prozkoumat tyto funkce v prostředí pomocí `dotnet try` globální nástroje:</span><span class="sxs-lookup"><span data-stu-id="788eb-130">You can explore these features in your environment using the `dotnet try` global tool:</span></span>

1. <span data-ttu-id="788eb-131">Nainstalujte [dotnet – zkuste](https://github.com/dotnet/try/blob/master/README.md#setup) globální nástroj.</span><span class="sxs-lookup"><span data-stu-id="788eb-131">Install the [dotnet-try](https://github.com/dotnet/try/blob/master/README.md#setup) global tool.</span></span>
1. <span data-ttu-id="788eb-132">Klonování [dotnet/try-samples](https://github.com/dotnet/try-samples) úložiště.</span><span class="sxs-lookup"><span data-stu-id="788eb-132">Clone the [dotnet/try-samples](https://github.com/dotnet/try-samples) repository.</span></span>
1. <span data-ttu-id="788eb-133">Nastavit aktuální adresář *csharp7* podadresář pro *try-samples* úložiště.</span><span class="sxs-lookup"><span data-stu-id="788eb-133">Set the current directory to the *csharp7* subdirectory for the *try-samples* repository.</span></span>
1. <span data-ttu-id="788eb-134">Spusťte `dotnet try`.</span><span class="sxs-lookup"><span data-stu-id="788eb-134">Run `dotnet try`.</span></span>

## <a name="out-variables"></a><span data-ttu-id="788eb-135">`out` Proměnné</span><span class="sxs-lookup"><span data-stu-id="788eb-135">`out` variables</span></span>

<span data-ttu-id="788eb-136">Existující syntaxi, která podporuje `out` v této verzi jsme vylepšili parametry.</span><span class="sxs-lookup"><span data-stu-id="788eb-136">The existing syntax that supports `out` parameters has been improved in this version.</span></span> <span data-ttu-id="788eb-137">Nyní můžete deklarovat `out` proměnných v seznamu argumentů volání metody místo psaní příkazu samostatné deklarace:</span><span class="sxs-lookup"><span data-stu-id="788eb-137">You can now declare `out` variables in the argument list of a method call, rather than writing a separate declaration statement:</span></span>

[!code-csharp[OutVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVariableDeclarations "Out variable declarations")]

<span data-ttu-id="788eb-138">Možná budete chtít zadat typ `out` proměnné pro přehlednost, jak je znázorněno výše.</span><span class="sxs-lookup"><span data-stu-id="788eb-138">You may want to specify the type of the `out` variable for clarity, as shown above.</span></span> <span data-ttu-id="788eb-139">Však jazyk podporuje použití implicitně typovaná lokální proměnná:</span><span class="sxs-lookup"><span data-stu-id="788eb-139">However, the language does support using an implicitly typed local variable:</span></span>

[!code-csharp[OutVarVariableDeclarations](~/samples/snippets/csharp/new-in-7/program.cs#OutVarVariableDeclarations "Implicitly typed Out variable")]

* <span data-ttu-id="788eb-140">Kód je lépe čitelný.</span><span class="sxs-lookup"><span data-stu-id="788eb-140">The code is easier to read.</span></span>
  - <span data-ttu-id="788eb-141">Deklarujete proměnnou mimo, kde má být použito, ne na další řádek výše.</span><span class="sxs-lookup"><span data-stu-id="788eb-141">You declare the out variable where you use it, not on another line above.</span></span>
* <span data-ttu-id="788eb-142">Už není potřeba přiřadit počáteční hodnotu.</span><span class="sxs-lookup"><span data-stu-id="788eb-142">No need to assign an initial value.</span></span>
  - <span data-ttu-id="788eb-143">Deklarací `out` proměnné, kde se používá ve volání metody, nemůžete použít omylem ho dřív, než je přiřadí.</span><span class="sxs-lookup"><span data-stu-id="788eb-143">By declaring the `out` variable where it's used in a method call, you can't accidentally use it before it is assigned.</span></span>

## <a name="tuples"></a><span data-ttu-id="788eb-144">N-tice</span><span class="sxs-lookup"><span data-stu-id="788eb-144">Tuples</span></span>

<span data-ttu-id="788eb-145">C# poskytuje bohaté syntaxi pro třídy a struktury, které slouží k vysvětlení máte v úmyslu návrhu.</span><span class="sxs-lookup"><span data-stu-id="788eb-145">C# provides a rich syntax for classes and structs that is used to explain your design intent.</span></span> <span data-ttu-id="788eb-146">Ale někdy bohaté syntaxe vyžaduje další práci s minimálními výhodu.</span><span class="sxs-lookup"><span data-stu-id="788eb-146">But sometimes that rich syntax requires extra work with minimal benefit.</span></span> <span data-ttu-id="788eb-147">Často může zapisovat metody, které vyžadují jednoduchou strukturu obsahující více než jeden datový prvek.</span><span class="sxs-lookup"><span data-stu-id="788eb-147">You may often write methods that need a simple structure containing more than one data element.</span></span> <span data-ttu-id="788eb-148">Pro podporu těchto scénářů *řazených kolekcí členů* byly přidány do jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="788eb-148">To support these scenarios *tuples* were added to C#.</span></span> <span data-ttu-id="788eb-149">Řazené kolekce členů jsou jednoduché datové struktury, které obsahují více polí k reprezentaci datové členy.</span><span class="sxs-lookup"><span data-stu-id="788eb-149">Tuples are lightweight data structures that contain multiple fields to represent the data members.</span></span>
<span data-ttu-id="788eb-150">Pole pořadí úloh se neověřuje, a nelze definovat vlastní metody</span><span class="sxs-lookup"><span data-stu-id="788eb-150">The fields aren't validated, and you can't define your own methods</span></span>

> [!NOTE]
> <span data-ttu-id="788eb-151">Řazené kolekce členů byly k dispozici před C# 7.0, ale bylo neefektivní a měl neexistuje jazyková podpora.</span><span class="sxs-lookup"><span data-stu-id="788eb-151">Tuples were available before C# 7.0, but they were inefficient and had no language support.</span></span>
> <span data-ttu-id="788eb-152">To znamená, že elementů řazené kolekce členů mohou být odkazovány pouze jako `Item1`, `Item2` a tak dále.</span><span class="sxs-lookup"><span data-stu-id="788eb-152">This meant that tuple elements could only be referenced as `Item1`, `Item2` and so on.</span></span> <span data-ttu-id="788eb-153">C# 7.0 zavádí podporu jazyka pro řazené kolekce členů, umožňující sémantické názvy polí pomocí nové, efektivnější řazené kolekce členů typy řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="788eb-153">C# 7.0 introduces language support for tuples, which enables semantic names for the fields of a tuple using new, more efficient tuple types.</span></span>

<span data-ttu-id="788eb-154">Řazená kolekce členů můžete vytvořit přiřazení hodnoty k jednotlivým členům a volitelně poskytuje sémantické názvy pro každého člena řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="788eb-154">You can create a tuple by assigning a value to each member, and optionally providing semantic names to each of the members of the tuple:</span></span>

[!code-csharp[NamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#NamedTuple "Named tuple")]

<span data-ttu-id="788eb-155">`namedLetters` Řazené kolekce členů obsahuje pole, které jsou označovány jako `Alpha` a `Beta`.</span><span class="sxs-lookup"><span data-stu-id="788eb-155">The `namedLetters` tuple contains fields referred to as `Alpha` and `Beta`.</span></span> <span data-ttu-id="788eb-156">Tyto názvy existují pouze v době kompilace a nejsou zachovány, například při kontrole řazené kolekce členů v době běhu pomocí operace reflection.</span><span class="sxs-lookup"><span data-stu-id="788eb-156">Those names exist only at compile time and aren't preserved, for example when inspecting the tuple using reflection at runtime.</span></span>

<span data-ttu-id="788eb-157">V přiřazení řazené kolekce členů můžete také zadat názvy polí na pravé straně přiřazení:</span><span class="sxs-lookup"><span data-stu-id="788eb-157">In a tuple assignment, you can also specify the names of the fields on the right-hand side of the assignment:</span></span>

[!code-csharp[ImplicitNamedTuple](~/samples/snippets/csharp/new-in-7/program.cs#ImplicitNamedTuple "Implicitly named tuple")]

<span data-ttu-id="788eb-158">Může nastat situace, kdy budete chtít rozbalit členy řazené kolekce členů, které byly vráceny z metody.</span><span class="sxs-lookup"><span data-stu-id="788eb-158">There may be times when you want to unpackage the members of a tuple that were returned from a method.</span></span>  <span data-ttu-id="788eb-159">Můžete to udělat deklarací samostatné proměnných pro všechny hodnoty řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="788eb-159">You can do that by declaring separate variables for each of the values in the tuple.</span></span> <span data-ttu-id="788eb-160">Tato rozbalení se nazývá *dekonstrukce* řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="788eb-160">This unpackaging is called *deconstructing* the tuple:</span></span>

[!code-csharp[CallingWithDeconstructor](~/samples/snippets/csharp/new-in-7/program.cs#CallingWithDeconstructor "Deconstructing a tuple")]

<span data-ttu-id="788eb-161">Můžete také zadat podobné dekonstrukce pro jakýkoli typ v rozhraní .NET.</span><span class="sxs-lookup"><span data-stu-id="788eb-161">You can also provide a similar deconstruction for any type in .NET.</span></span> <span data-ttu-id="788eb-162">Při psaní `Deconstruct` metoda jako člen třídy.</span><span class="sxs-lookup"><span data-stu-id="788eb-162">You write a `Deconstruct` method as a member of the class.</span></span> <span data-ttu-id="788eb-163">Že `Deconstruct` metoda poskytuje sadu `out` argumenty pro každou z vlastností, které mají být extrahovány.</span><span class="sxs-lookup"><span data-stu-id="788eb-163">That `Deconstruct` method provides a set of `out` arguments for each of the properties you want to extract.</span></span> <span data-ttu-id="788eb-164">Zvažte proto `Point` třída, která poskytuje deconstructor metodu, která extrahuje `X` a `Y` souřadnice:</span><span class="sxs-lookup"><span data-stu-id="788eb-164">Consider this `Point` class that provides a deconstructor method that extracts the `X` and `Y` coordinates:</span></span>

[!code-csharp[PointWithDeconstruction](~/samples/snippets/csharp/new-in-7/point.cs#PointWithDeconstruction "Point with deconstruction method")]

<span data-ttu-id="788eb-165">Můžete extrahovat jednotlivá pole pomocí přiřazení `Point` k řazené kolekce členů:</span><span class="sxs-lookup"><span data-stu-id="788eb-165">You can extract the individual fields by assigning a `Point` to a tuple:</span></span>

[!code-csharp[DeconstructPoint](~/samples/snippets/csharp/new-in-7/program.cs#DeconstructPoint "Deconstruct a point")]

<span data-ttu-id="788eb-166">Další podrobnosti v o řazenými kolekcemi členů v [řazených kolekcí členů článku](../tuples.md).</span><span class="sxs-lookup"><span data-stu-id="788eb-166">You can learn more in depth about tuples in the [tuples article](../tuples.md).</span></span>

## <a name="discards"></a><span data-ttu-id="788eb-167">Zahození</span><span class="sxs-lookup"><span data-stu-id="788eb-167">Discards</span></span>

<span data-ttu-id="788eb-168">Při často dekonstrukce řazené kolekce členů nebo volání metody s `out` parametry, budete muset definovat proměnnou jehož hodnota nezáleží a používat.</span><span class="sxs-lookup"><span data-stu-id="788eb-168">Often when deconstructing a tuple or calling a method with `out` parameters, you're forced to define a variable whose value you don't care about and don't intend to use.</span></span> <span data-ttu-id="788eb-169">C# přidává podporu pro *zahodí* tohoto scénáře.</span><span class="sxs-lookup"><span data-stu-id="788eb-169">C# adds support for *discards* to handle this scenario.</span></span> <span data-ttu-id="788eb-170">Zahození je proměnná jen pro zápis, jehož název je `_` (podtržítko); můžete přiřadit všechny hodnoty, které máte v úmyslu zahodit do jedné proměnné.</span><span class="sxs-lookup"><span data-stu-id="788eb-170">A discard is a write-only variable whose name is `_` (the underscore character); you can assign all of the values that you intend to discard to the single variable.</span></span> <span data-ttu-id="788eb-171">Zahození je stejná jako nepřiřazené proměnné. Kromě příkazu přiřazení nelze použít zahození v kódu.</span><span class="sxs-lookup"><span data-stu-id="788eb-171">A discard is like an unassigned variable; apart from the assignment statement, the discard can't be used in code.</span></span>

<span data-ttu-id="788eb-172">Zahození jsou podporovány v následujících scénářích:</span><span class="sxs-lookup"><span data-stu-id="788eb-172">Discards are supported in the following scenarios:</span></span>

* <span data-ttu-id="788eb-173">Když dekonstrukce řazených kolekcí členů nebo uživatelem definované typy.</span><span class="sxs-lookup"><span data-stu-id="788eb-173">When deconstructing tuples or user-defined types.</span></span>
* <span data-ttu-id="788eb-174">Při volání metody s [si](../language-reference/keywords/out-parameter-modifier.md) parametry.</span><span class="sxs-lookup"><span data-stu-id="788eb-174">When calling methods with [out](../language-reference/keywords/out-parameter-modifier.md) parameters.</span></span>
* <span data-ttu-id="788eb-175">Ve vzorku s odpovídajícím operaci s [je](../language-reference/keywords/is.md) a [přepnout](../language-reference/keywords/switch.md) příkazy.</span><span class="sxs-lookup"><span data-stu-id="788eb-175">In a pattern matching operation with the [is](../language-reference/keywords/is.md) and [switch](../language-reference/keywords/switch.md) statements.</span></span>
* <span data-ttu-id="788eb-176">Pokud chcete explicitně Identifikujte jako samostatné identifikátor hodnotu přiřazení jako zahození.</span><span class="sxs-lookup"><span data-stu-id="788eb-176">As a standalone identifier when you want to explicitly identify the value of an assignment as a discard.</span></span>

<span data-ttu-id="788eb-177">Následující příklad definuje `QueryCityDataForYears` metodu, která vrátí 6-n-tice, která obsahuje data pro město pro dvě různé roky.</span><span class="sxs-lookup"><span data-stu-id="788eb-177">The following example defines a `QueryCityDataForYears` method that returns a 6-tuple that contains data for a city for two different years.</span></span> <span data-ttu-id="788eb-178">Volání metody v příkladu se týká pouze s hodnotami dva naplnění vrácený metodou a proto zpracuje zbývající hodnoty řazené kolekce členů jako zahodí při deconstructs řazené kolekce členů.</span><span class="sxs-lookup"><span data-stu-id="788eb-178">The method call in the example is concerned only with the two population values returned by the method and so treats the remaining values in the tuple as discards when it deconstructs the tuple.</span></span>

[!code-csharp[Tuple-discard](~/samples/snippets/csharp/programming-guide/deconstructing-tuples/discard-tuple1.cs)]

<span data-ttu-id="788eb-179">Další informace najdete v tématu [zahodí](../discards.md).</span><span class="sxs-lookup"><span data-stu-id="788eb-179">For more information, see [Discards](../discards.md).</span></span>

## <a name="pattern-matching"></a><span data-ttu-id="788eb-180">Porovnávání vzorů</span><span class="sxs-lookup"><span data-stu-id="788eb-180">Pattern matching</span></span>

<span data-ttu-id="788eb-181">*Porovnávání vzorů* je funkce, která umožňuje implementovat metodu odeslání na vlastnosti jiné než typu objektu.</span><span class="sxs-lookup"><span data-stu-id="788eb-181">*Pattern matching* is a feature that allows you to implement method dispatch on properties other than the type of an object.</span></span> <span data-ttu-id="788eb-182">Už pravděpodobně již znáte odesílání metodu na základě typu objektu.</span><span class="sxs-lookup"><span data-stu-id="788eb-182">You're probably already familiar with method dispatch based on the type of an object.</span></span> <span data-ttu-id="788eb-183">Objektově orientované programování, virtuální a přepsání metody poskytují syntaxe jazyka pro implementaci metody odesílání na základě typu objektu.</span><span class="sxs-lookup"><span data-stu-id="788eb-183">In object-oriented programming, virtual and override methods provide language syntax to implement method dispatching based on an object's type.</span></span> <span data-ttu-id="788eb-184">Základní a odvozené třídy poskytují tak různé implementace.</span><span class="sxs-lookup"><span data-stu-id="788eb-184">Base and Derived classes provide different implementations.</span></span>
<span data-ttu-id="788eb-185">Výrazy odpovídající vzor rozšiřte tento koncept tak, že je možné snadno implementovat podobné vzory odeslání pro typy a datové prvky, které spolu nesouvisí prostřednictvím hierarchie dědičnosti.</span><span class="sxs-lookup"><span data-stu-id="788eb-185">Pattern matching expressions extend this concept so that you can easily implement similar dispatch patterns for types and data elements that aren't related through an inheritance hierarchy.</span></span>

<span data-ttu-id="788eb-186">Porovnávání vzorů podporuje `is` výrazy a `switch` výrazy.</span><span class="sxs-lookup"><span data-stu-id="788eb-186">Pattern matching supports `is` expressions and `switch` expressions.</span></span> <span data-ttu-id="788eb-187">Každý umožňuje kontrolu objektu a vlastností určíte, pokud daný objekt splňuje hledané kombinaci vzoru.</span><span class="sxs-lookup"><span data-stu-id="788eb-187">Each enables inspecting an object and its properties to determine if that object satisfies the sought pattern.</span></span> <span data-ttu-id="788eb-188">Můžete použít `when` – klíčové slovo lze určit další pravidla se vzorem.</span><span class="sxs-lookup"><span data-stu-id="788eb-188">You use the `when` keyword to specify additional rules to the pattern.</span></span>

<span data-ttu-id="788eb-189">`is` Výraz vzoru rozšiřuje známé [ `is` operátor](../language-reference/keywords/is.md#pattern-matching-with-is) pro dotazování objekt o její typ a přiřadit výsledek v jedné instrukce.</span><span class="sxs-lookup"><span data-stu-id="788eb-189">The `is` pattern expression extends the familiar [`is` operator](../language-reference/keywords/is.md#pattern-matching-with-is) to query an object about its type and assign the result in one instruction.</span></span> <span data-ttu-id="788eb-190">Následující kód ověří, zda proměnná `int`a pokud ano, přidá jej do aktuálního součet:</span><span class="sxs-lookup"><span data-stu-id="788eb-190">The following code checks if a variable is an `int`, and if so, adds it to the current sum:</span></span>

```csharp
if (input is int count)
    sum += count;
```

<span data-ttu-id="788eb-191">Ukazuje předchozí příklad malá vylepšení `is` výrazu.</span><span class="sxs-lookup"><span data-stu-id="788eb-191">The preceding small example demonstrates the enhancements to the `is` expression.</span></span> <span data-ttu-id="788eb-192">Můžete otestovat s typy hodnot, jakož i typy odkazů a přiřadíte úspěšný výsledek do nové proměnné nesprávného typu.</span><span class="sxs-lookup"><span data-stu-id="788eb-192">You can test against value types as well as reference types, and you can assign the successful result to a new variable of the correct type.</span></span>

<span data-ttu-id="788eb-193">Má známou syntaxi, na základě shody výraz přepínače `switch` příkaz již součástí C# jazyka.</span><span class="sxs-lookup"><span data-stu-id="788eb-193">The switch match expression has a familiar syntax, based on the `switch` statement already part of the C# language.</span></span> <span data-ttu-id="788eb-194">Příkaz aktualizované switch má několik nové konstruktory:</span><span class="sxs-lookup"><span data-stu-id="788eb-194">The updated switch statement has several new constructs:</span></span>

- <span data-ttu-id="788eb-195">Celopodnikové typu `switch` výrazu už není omezené na integrální typy `Enum` typy, `string`, nebo typ připouštějící hodnotu Null odpovídá jedné z těchto typů.</span><span class="sxs-lookup"><span data-stu-id="788eb-195">The governing type of a `switch` expression is no longer restricted to integral types, `Enum` types, `string`, or a nullable type corresponding to one of those types.</span></span> <span data-ttu-id="788eb-196">Libovolný typ může použít.</span><span class="sxs-lookup"><span data-stu-id="788eb-196">Any type may be used.</span></span>
- <span data-ttu-id="788eb-197">Můžete otestovat typu `switch` výrazu v každém `case` popisek.</span><span class="sxs-lookup"><span data-stu-id="788eb-197">You can test the type of the `switch` expression in each `case` label.</span></span> <span data-ttu-id="788eb-198">Stejně jako u `is` výrazu, může přiřadit novou proměnnou typu.</span><span class="sxs-lookup"><span data-stu-id="788eb-198">As with the `is` expression, you may assign a new variable to that type.</span></span>
- <span data-ttu-id="788eb-199">Můžete přidat `when` klauzuli pro další testování podmínek na tuto proměnnou.</span><span class="sxs-lookup"><span data-stu-id="788eb-199">You may add a `when` clause to further test conditions on that variable.</span></span>
- <span data-ttu-id="788eb-200">Pořadí `case` popisků je důležité.</span><span class="sxs-lookup"><span data-stu-id="788eb-200">The order of `case` labels is now important.</span></span> <span data-ttu-id="788eb-201">První větev tak, aby odpovídaly je spuštěn; ostatní se přeskočí.</span><span class="sxs-lookup"><span data-stu-id="788eb-201">The first branch to match is executed; others are skipped.</span></span>

<span data-ttu-id="788eb-202">Následující kód demonstruje tyto nové funkce:</span><span class="sxs-lookup"><span data-stu-id="788eb-202">The following code demonstrates these new features:</span></span>

```csharp
public static int SumPositiveNumbers(IEnumerable<object> sequence)
{
    int sum = 0;
    foreach (var i in sequence)
    {
        switch (i)
        {
            case 0:
                break;
            case IEnumerable<int> childSequence:
            {
                foreach(var item in childSequence)
                    sum += (item > 0) ? item : 0;
                break;
            }
            case int n when n > 0:
                sum += n;
                break;
            case null:
                throw new NullReferenceException("Null found in sequence");
            default:
                throw new InvalidOperationException("Unrecognized type");
        }
    }
    return sum;
}
```

- <span data-ttu-id="788eb-203">`case 0:` je známé konstantní vzorek.</span><span class="sxs-lookup"><span data-stu-id="788eb-203">`case 0:` is the familiar constant pattern.</span></span>
- <span data-ttu-id="788eb-204">`case IEnumerable<int> childSequence:` je typ modelu.</span><span class="sxs-lookup"><span data-stu-id="788eb-204">`case IEnumerable<int> childSequence:` is a type pattern.</span></span>
- <span data-ttu-id="788eb-205">`case int n when n > 0:` je typu vzorec ještě `when` podmínku.</span><span class="sxs-lookup"><span data-stu-id="788eb-205">`case int n when n > 0:` is a type pattern with an additional `when` condition.</span></span>
- <span data-ttu-id="788eb-206">`case null:` je vzor null.</span><span class="sxs-lookup"><span data-stu-id="788eb-206">`case null:` is the null pattern.</span></span>
- <span data-ttu-id="788eb-207">`default:` je známé výchozí případ.</span><span class="sxs-lookup"><span data-stu-id="788eb-207">`default:` is the familiar default case.</span></span>

<span data-ttu-id="788eb-208">Další informace o porovnávání vzorů v [vzor odpovídající v C# ](../pattern-matching.md).</span><span class="sxs-lookup"><span data-stu-id="788eb-208">You can learn more about pattern matching in [Pattern Matching in C#](../pattern-matching.md).</span></span>

## <a name="ref-locals-and-returns"></a><span data-ttu-id="788eb-209">Místní referenční hodnoty a vrátí</span><span class="sxs-lookup"><span data-stu-id="788eb-209">Ref locals and returns</span></span>

<span data-ttu-id="788eb-210">Tato funkce umožňuje algoritmy, které používají a vrátit odkazy na proměnné definované jinde.</span><span class="sxs-lookup"><span data-stu-id="788eb-210">This feature enables algorithms that use and return references to variables defined elsewhere.</span></span> <span data-ttu-id="788eb-211">Jedním z příkladů je práce s velké matice a hledání na jednom místě s určité vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="788eb-211">One example is working with large matrices, and finding a single location with certain characteristics.</span></span> <span data-ttu-id="788eb-212">Vrátí následující metodu **odkaz** do tohoto úložiště v matici:</span><span class="sxs-lookup"><span data-stu-id="788eb-212">The following method returns a **reference** to that storage in the matrix:</span></span>

[!code-csharp[FindReturningRef](~/samples/snippets/csharp/new-in-7/MatrixSearch.cs#FindReturningRef "Find returning by reference")]

<span data-ttu-id="788eb-213">Je možné deklarovat jako návratovou hodnotu `ref` a upravte tuto hodnotu v matici, jak je znázorněno v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="788eb-213">You can declare the return value as a `ref` and modify that value in the matrix, as shown in the following code:</span></span>

[!code-csharp[AssignRefReturn](~/samples/snippets/csharp/new-in-7/Program.cs#AssignRefReturn "Assign ref return")]

<span data-ttu-id="788eb-214">C# Jazyk má několik pravidel, které chrání vás před zneužitím `ref` místní hodnoty a vrátí:</span><span class="sxs-lookup"><span data-stu-id="788eb-214">The C# language has several rules that protect you from misusing the `ref` locals and returns:</span></span>

* <span data-ttu-id="788eb-215">Je nutné přidat `ref` – klíčové slovo do podpisu metody a do všech `return` příkazy v metodě.</span><span class="sxs-lookup"><span data-stu-id="788eb-215">You must add the `ref` keyword to the method signature and to all `return` statements in a method.</span></span>
  - <span data-ttu-id="788eb-216">Díky tomu se vymazat metoda vrací odkazem v rámci metody.</span><span class="sxs-lookup"><span data-stu-id="788eb-216">That makes it clear the method returns by reference throughout the method.</span></span>
* <span data-ttu-id="788eb-217">A `ref return` možné přiřadit hodnotu proměnné nebo `ref` proměnné.</span><span class="sxs-lookup"><span data-stu-id="788eb-217">A `ref return` may be assigned to a value variable, or a `ref` variable.</span></span>
  - <span data-ttu-id="788eb-218">Volající Určuje, zda je návratová hodnota zkopírován nebo ne.</span><span class="sxs-lookup"><span data-stu-id="788eb-218">The caller controls whether the return value is copied or not.</span></span> <span data-ttu-id="788eb-219">Vynechání `ref` modifikátor při přiřazování návratová hodnota označuje, že volající vyžaduje kopii hodnoty, ne odkaz na úložiště.</span><span class="sxs-lookup"><span data-stu-id="788eb-219">Omitting the `ref` modifier when assigning the return value indicates that the caller wants a copy of the value, not a reference to the storage.</span></span>
* <span data-ttu-id="788eb-220">Nelze přiřadit standardní metoda návratovou hodnotu pro `ref` místní proměnné.</span><span class="sxs-lookup"><span data-stu-id="788eb-220">You can't assign a standard method return value to a `ref` local variable.</span></span>
  - <span data-ttu-id="788eb-221">Který zakazuje příkazy jako `ref int i = sequence.Count();`</span><span class="sxs-lookup"><span data-stu-id="788eb-221">That disallows statements like `ref int i = sequence.Count();`</span></span>
* <span data-ttu-id="788eb-222">Nelze vrátit `ref` do proměnné, jehož doba života nerozšiřuje nad rámec provádění metody.</span><span class="sxs-lookup"><span data-stu-id="788eb-222">You can't return a `ref` to a variable whose lifetime doesn't extend beyond the execution of the method.</span></span>
  - <span data-ttu-id="788eb-223">To znamená, že nejde vrátit odkazem na místní proměnná nebo proměnná se podobně jako obor.</span><span class="sxs-lookup"><span data-stu-id="788eb-223">That means you can't return a reference to a local variable or a variable with a similar scope.</span></span>
* <span data-ttu-id="788eb-224">`ref` místní hodnoty a vrátí nelze použít u asynchronních metod.</span><span class="sxs-lookup"><span data-stu-id="788eb-224">`ref` locals and returns can't be used with async methods.</span></span>
  - <span data-ttu-id="788eb-225">Kompilátor nemůže vědět, pokud odkazovaná proměnná je nastavená na jeho konečnou hodnotu po návratu asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="788eb-225">The compiler can't know if the referenced variable has been set to its final value when the async method returns.</span></span>

<span data-ttu-id="788eb-226">Přidání místní referenční hodnoty a ref vrátí umožňuje algoritmy, které jsou efektivnější se vyhnout kopírování hodnot nebo provádění operací přesměrování více než jednou.</span><span class="sxs-lookup"><span data-stu-id="788eb-226">The addition of ref locals and ref returns enables algorithms that are more efficient by avoiding copying values, or performing dereferencing operations multiple times.</span></span>

<span data-ttu-id="788eb-227">Přidání `ref` návratová hodnota je [zdroj kompatibilní změnu](version-update-considerations.md#source-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="788eb-227">Adding `ref` to the return value is a [source compatible change](version-update-considerations.md#source-compatible-changes).</span></span> <span data-ttu-id="788eb-228">Stávající kód zkompiluje, ale ref návratové hodnoty je zkopírován při přiřazení.</span><span class="sxs-lookup"><span data-stu-id="788eb-228">Existing code compiles, but the ref return value is copied when assigned.</span></span> <span data-ttu-id="788eb-229">Volající musí aktualizovat úložiště pro návratovou hodnotu `ref` místní proměnnou pro uložení vrácení jako odkaz.</span><span class="sxs-lookup"><span data-stu-id="788eb-229">Callers must update the storage for the return value to a `ref` local variable to store the return as a reference.</span></span>

<span data-ttu-id="788eb-230">Další informace najdete v tématu [ref – klíčové slovo](../language-reference/keywords/ref.md) článku.</span><span class="sxs-lookup"><span data-stu-id="788eb-230">For more information, see the [ref keyword](../language-reference/keywords/ref.md) article.</span></span>

## <a name="local-functions"></a><span data-ttu-id="788eb-231">Lokální funkce</span><span class="sxs-lookup"><span data-stu-id="788eb-231">Local functions</span></span>

<span data-ttu-id="788eb-232">Řada návrhů pro třídy zahrnují metody, které se volají z jediného umístění.</span><span class="sxs-lookup"><span data-stu-id="788eb-232">Many designs for classes include methods that are called from only one location.</span></span> <span data-ttu-id="788eb-233">Tyto další metody privátní zachovat každá metoda malém rozsahu a zaměřeně.</span><span class="sxs-lookup"><span data-stu-id="788eb-233">These additional private methods keep each method small and focused.</span></span> <span data-ttu-id="788eb-234">*Lokální funkce* umožňují deklarovat metody v kontextu jinou metodu.</span><span class="sxs-lookup"><span data-stu-id="788eb-234">*Local functions* enable you to declare methods inside the context of another method.</span></span> <span data-ttu-id="788eb-235">Lokální funkce usnadnění pro čtenáře třídy, pokud chcete zobrazit, že místní metoda je volána pouze z kontextu, ve kterém je deklarována.</span><span class="sxs-lookup"><span data-stu-id="788eb-235">Local functions make it easier for readers of the class to see that the local method is only called from the context in which it is declared.</span></span>

<span data-ttu-id="788eb-236">Existují dva běžné případy použití pro lokální funkce: veřejné iterátory a veřejné asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="788eb-236">There are two common use cases for local functions: public iterator methods and public async methods.</span></span> <span data-ttu-id="788eb-237">Oba typy metod generování kódu, který bude hlásit chyby později, než se dalo očekávat programátory.</span><span class="sxs-lookup"><span data-stu-id="788eb-237">Both types of methods generate code that reports errors later than programmers might expect.</span></span> <span data-ttu-id="788eb-238">V metodách iterátoru, všechny výjimky, jsou dodržovány pouze při volání metody kód, který uvádí, že vrácená sekvence.</span><span class="sxs-lookup"><span data-stu-id="788eb-238">In iterator methods, any exceptions are observed only when calling code that enumerates the returned sequence.</span></span> <span data-ttu-id="788eb-239">V asynchronních metodách, všechny výjimky jsou pouze dodržovat při vráceného `Task` je očekáváno.</span><span class="sxs-lookup"><span data-stu-id="788eb-239">In async methods, any exceptions are only observed when the returned `Task` is awaited.</span></span> <span data-ttu-id="788eb-240">Následující příklad ukazuje dělicí ověření parametru od implementace rozhraní iterátoru pomocí lokální funkce:</span><span class="sxs-lookup"><span data-stu-id="788eb-240">The following example demonstrates separating parameter validation from the iterator implementation using a local function:</span></span>

[!code-csharp[22_IteratorMethodLocal](~/samples/snippets/csharp/new-in-7/Iterator.cs#IteratorMethodLocal "Iterator method with local function")]

<span data-ttu-id="788eb-241">Stejným způsobem mohou být použity s `async` metody k zajištění, že výjimky vyplývající z ověřování argumentu jsou vyvolány před zahájením asynchronní práce:</span><span class="sxs-lookup"><span data-stu-id="788eb-241">The same technique can be employed with `async` methods to ensure that exceptions arising from argument validation are thrown before the asynchronous work begins:</span></span>

[!code-csharp[TaskExample](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#TaskExample "Task returning method with local function")]

> [!NOTE]
> <span data-ttu-id="788eb-242">Některé návrhy, které jsou podporovány lokální funkce také je možné dosáhnout použitím *výrazy lambda*.</span><span class="sxs-lookup"><span data-stu-id="788eb-242">Some of the designs that are supported by local functions could also be accomplished using *lambda expressions*.</span></span> <span data-ttu-id="788eb-243">Tyto zúčastněné můžete [Další informace o rozdílech](../local-functions-vs-lambdas.md)</span><span class="sxs-lookup"><span data-stu-id="788eb-243">Those interested can [read more about the differences](../local-functions-vs-lambdas.md)</span></span>

## <a name="more-expression-bodied-members"></a><span data-ttu-id="788eb-244">Více členů s výrazem v těle</span><span class="sxs-lookup"><span data-stu-id="788eb-244">More expression-bodied members</span></span>

<span data-ttu-id="788eb-245">C# 6 zavedené [členové tvoření](csharp-6.md#expression-bodied-function-members) pro členské funkce a vlastnosti jen pro čtení.</span><span class="sxs-lookup"><span data-stu-id="788eb-245">C# 6 introduced [expression-bodied members](csharp-6.md#expression-bodied-function-members) for member functions, and read-only properties.</span></span> <span data-ttu-id="788eb-246">C# 7.0 rozšíří povolené členy, které je možné implementovat jako výrazy.</span><span class="sxs-lookup"><span data-stu-id="788eb-246">C# 7.0 expands the allowed members that can be implemented as expressions.</span></span> <span data-ttu-id="788eb-247">V jazyce C# 7.0, můžete implementovat *konstruktory*, *finalizační metody*, a `get` a `set` přistupující objekty na *vlastnosti* a *indexerů* .</span><span class="sxs-lookup"><span data-stu-id="788eb-247">In C# 7.0, you can implement *constructors*, *finalizers*, and `get` and `set` accessors on *properties* and *indexers*.</span></span> <span data-ttu-id="788eb-248">Následující kód ukazuje příklady každé:</span><span class="sxs-lookup"><span data-stu-id="788eb-248">The following code shows examples of each:</span></span>

[!code-csharp[ExpressionBodiedMembers](~/samples/snippets/csharp/new-in-7/expressionmembers.cs#ExpressionBodiedEverything "new expression-bodied members")]

> [!NOTE]
> <span data-ttu-id="788eb-249">V tomto příkladu nemusí finalizační metodu, ale je zobrazena na ukazují syntaxi.</span><span class="sxs-lookup"><span data-stu-id="788eb-249">This example does not need a finalizer, but it is shown to demonstrate the syntax.</span></span> <span data-ttu-id="788eb-250">Finalizační metody by neměla implementovat ve své třídě, pokud to není nutné k uvolnění nespravovaných prostředků.</span><span class="sxs-lookup"><span data-stu-id="788eb-250">You should not implement a finalizer in your class unless it is necessary to  release unmanaged resources.</span></span> <span data-ttu-id="788eb-251">Měli byste také zvážit použití <xref:System.Runtime.InteropServices.SafeHandle> třídy místo správy nespravované prostředky přímo.</span><span class="sxs-lookup"><span data-stu-id="788eb-251">You should also consider using the <xref:System.Runtime.InteropServices.SafeHandle> class instead of managing unmanaged resources directly.</span></span>

<span data-ttu-id="788eb-252">Tato nová umístění pro členy s výrazem v těle představuje důležitý milník pro C# jazyka: Tyto funkce byly implementované členy komunity open source pracovali [Roslyn](https://github.com/dotnet/Roslyn) projektu.</span><span class="sxs-lookup"><span data-stu-id="788eb-252">These new locations for expression-bodied members represent an important milestone for the C# language: These features were implemented by community members working on the open-source [Roslyn](https://github.com/dotnet/Roslyn) project.</span></span>

<span data-ttu-id="788eb-253">Změna metody na výrazu vozidlo člena je [binární kompatibilní změnu](version-update-considerations.md#binary-compatible-changes).</span><span class="sxs-lookup"><span data-stu-id="788eb-253">Changing a method to an expression bodied member is a [binary compatible change](version-update-considerations.md#binary-compatible-changes).</span></span>

## <a name="throw-expressions"></a><span data-ttu-id="788eb-254">Výrazy throw</span><span class="sxs-lookup"><span data-stu-id="788eb-254">Throw expressions</span></span>

<span data-ttu-id="788eb-255">V jazyce C# `throw` vždy bylo příkazu.</span><span class="sxs-lookup"><span data-stu-id="788eb-255">In C#, `throw` has always been a statement.</span></span> <span data-ttu-id="788eb-256">Protože `throw` je příkaz, není výraz bylo C# konstrukce, které nelze použít.</span><span class="sxs-lookup"><span data-stu-id="788eb-256">Because `throw` is a statement, not an expression, there were C# constructs where you couldn't use it.</span></span> <span data-ttu-id="788eb-257">Tyto uvedeny podmíněné výrazy, null slučovací výrazy a některé výrazy lambda.</span><span class="sxs-lookup"><span data-stu-id="788eb-257">These included conditional expressions, null coalescing expressions, and some lambda expressions.</span></span> <span data-ttu-id="788eb-258">Přidání členů s výrazem v těle přidá více míst kde `throw` výrazů může být užitečné.</span><span class="sxs-lookup"><span data-stu-id="788eb-258">The addition of expression-bodied members adds more locations where `throw` expressions would be useful.</span></span> <span data-ttu-id="788eb-259">Aby mohla zapisovat některý z těchto konstruktorů C# 7.0 zavádí [ *vyvolání výrazů*](../language-reference/keywords/throw.md#the-throw-expression).</span><span class="sxs-lookup"><span data-stu-id="788eb-259">So that you can write any of these constructs, C# 7.0 introduces [*throw expressions*](../language-reference/keywords/throw.md#the-throw-expression).</span></span>

<span data-ttu-id="788eb-260">Toto přidání je snazší psát další kód založené na výrazu.</span><span class="sxs-lookup"><span data-stu-id="788eb-260">This addition makes it easier to write more expression-based code.</span></span> <span data-ttu-id="788eb-261">Není nutné další příkazy pro kontrolu chyb.</span><span class="sxs-lookup"><span data-stu-id="788eb-261">You don't need additional statements for error checking.</span></span>

## <a name="generalized-async-return-types"></a><span data-ttu-id="788eb-262">Zobecněný asynchronní návratové typy</span><span class="sxs-lookup"><span data-stu-id="788eb-262">Generalized async return types</span></span>

<span data-ttu-id="788eb-263">Vrácení `Task` objekt z asynchronní metody můžete zavést problémových míst výkonu v určitých cestách.</span><span class="sxs-lookup"><span data-stu-id="788eb-263">Returning a `Task` object from async methods can introduce performance bottlenecks in certain paths.</span></span> <span data-ttu-id="788eb-264">`Task` je typem odkazu, takže ho pomocí znamená, že přidělování objektu.</span><span class="sxs-lookup"><span data-stu-id="788eb-264">`Task` is a reference type, so using it means allocating an object.</span></span> <span data-ttu-id="788eb-265">V případech, kde metoda deklarována s `async` modifikátor vrátí výsledky uložené v mezipaměti, nebo dokončí synchronně, navíc přidělení se může stát spoustu času náklady na kritické oddíly výkon kódu.</span><span class="sxs-lookup"><span data-stu-id="788eb-265">In cases where a method declared with the `async` modifier returns a cached result, or completes synchronously, the extra allocations can become a significant time cost in performance critical sections of code.</span></span> <span data-ttu-id="788eb-266">Může být nákladná, pokud dojde k těmto přidělení v těsné smyčky.</span><span class="sxs-lookup"><span data-stu-id="788eb-266">It can become costly if those allocations occur in tight loops.</span></span>

<span data-ttu-id="788eb-267">Nové funkce jazyka znamená, že asynchronní metody nejsou omezeny na návratové typy `Task`, `Task<T>`, a `void`.</span><span class="sxs-lookup"><span data-stu-id="788eb-267">The new language feature means that async method return types aren't limited to `Task`, `Task<T>`, and `void`.</span></span> <span data-ttu-id="788eb-268">Vrácený typ musí splňovat stále asynchronní vzorek, což znamená `GetAwaiter` metoda musí být přístupná.</span><span class="sxs-lookup"><span data-stu-id="788eb-268">The returned type must still satisfy the async pattern, meaning a `GetAwaiter` method must be accessible.</span></span> <span data-ttu-id="788eb-269">Jako konkrétní příklad `ValueTask` typ byl přidán do rozhraní .NET framework, aby pomocí této nové funkce jazyka:</span><span class="sxs-lookup"><span data-stu-id="788eb-269">As one concrete example, the `ValueTask` type has been added to the .NET framework to make use of this new language feature:</span></span>

[!code-csharp[UsingValueTask](~/samples/snippets/csharp/new-in-7/AsyncWork.cs#UsingValueTask "Using ValueTask")]

> [!NOTE]
> <span data-ttu-id="788eb-270">Je potřeba přidat balíček NuGet [ `System.Threading.Tasks.Extensions` ](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) Chcete-li použít <xref:System.Threading.Tasks.ValueTask%601> typu.</span><span class="sxs-lookup"><span data-stu-id="788eb-270">You need to add the NuGet package [`System.Threading.Tasks.Extensions`](https://www.nuget.org/packages/System.Threading.Tasks.Extensions/) in order to use the <xref:System.Threading.Tasks.ValueTask%601> type.</span></span>

<span data-ttu-id="788eb-271">Toto vylepšení je zvláště užitečná pro autory knihoven, aby se vyhnul přidělování `Task` v výkonu kritický kód.</span><span class="sxs-lookup"><span data-stu-id="788eb-271">This enhancement is most useful for library authors to avoid allocating a `Task` in performance critical code.</span></span>

## <a name="numeric-literal-syntax-improvements"></a><span data-ttu-id="788eb-272">Vylepšení číselný literál syntaxe</span><span class="sxs-lookup"><span data-stu-id="788eb-272">Numeric literal syntax improvements</span></span>

<span data-ttu-id="788eb-273">Číselné konstanty misreading může znesnadnit pochopení kódu při čtení poprvé.</span><span class="sxs-lookup"><span data-stu-id="788eb-273">Misreading numeric constants can make it harder to understand code when reading it for the first time.</span></span> <span data-ttu-id="788eb-274">Bitové masky nebo jiné symbolické hodnoty jsou náchylné k neporozumění.</span><span class="sxs-lookup"><span data-stu-id="788eb-274">Bit masks or other symbolic values are prone to misunderstanding.</span></span> <span data-ttu-id="788eb-275">C#7.0 obsahuje dvě nové funkce pro zápis čísla způsobem nejvíce čitelné pro zamýšlené použití: *binární literály:* , a *oddělovače číslic:* .</span><span class="sxs-lookup"><span data-stu-id="788eb-275">C# 7.0 includes two new features to write numbers in the most readable fashion for the intended use: *binary literals*, and *digit separators*.</span></span>

<span data-ttu-id="788eb-276">Pro situace, při vytváření bitové masky, nebo když se binární vyjádření čísla je nejvíce čitelné kód zápis tohoto čísla v binárním souboru:</span><span class="sxs-lookup"><span data-stu-id="788eb-276">For those times when you're creating bit masks, or whenever a binary representation of a number makes the most readable code, write that number in binary:</span></span>

[!code-csharp[ThousandSeparators](~/samples/snippets/csharp/new-in-7/Program.cs#ThousandSeparators "Thousands separators")]

<span data-ttu-id="788eb-277">`0b` Na začátku konstanty Určuje, že číslo je zapsán jako binární číslo.</span><span class="sxs-lookup"><span data-stu-id="788eb-277">The `0b` at the beginning of the constant indicates that the number is written as a binary number.</span></span> <span data-ttu-id="788eb-278">Můžete získat tak, aby byl často snazší zjistit bitové vzory zavedením dlouhý, binárních čísel `_` jako oddělovač číslic, jak je znázorněno výše binární – konstanta.</span><span class="sxs-lookup"><span data-stu-id="788eb-278">Binary numbers can get long, so it's often easier to see the bit patterns by introducing the `_` as a digit separator, as shown above in the binary constant.</span></span> <span data-ttu-id="788eb-279">Oddělovač číslic může vyskytovat kdekoli v konstantě.</span><span class="sxs-lookup"><span data-stu-id="788eb-279">The digit separator can appear anywhere in the constant.</span></span> <span data-ttu-id="788eb-280">Pro základní 10 čísel, je běžné použití jako tisíců oddělovač:</span><span class="sxs-lookup"><span data-stu-id="788eb-280">For base 10 numbers, it is common to use it as a thousands separator:</span></span>

[!code-csharp[LargeIntegers](~/samples/snippets/csharp/new-in-7/Program.cs#LargeIntegers "Large integer")]

<span data-ttu-id="788eb-281">Oddělovač číslic jde použít s `decimal`, `float`, a `double` typy a také:</span><span class="sxs-lookup"><span data-stu-id="788eb-281">The digit separator can be used with `decimal`, `float`, and `double` types as well:</span></span>

[!code-csharp[OtherConstants](~/samples/snippets/csharp/new-in-7/Program.cs#OtherConstants "non-integral constants")]

<span data-ttu-id="788eb-282">Dohromady, je možné deklarovat číselné konstanty s mnohem více čitelnost.</span><span class="sxs-lookup"><span data-stu-id="788eb-282">Taken together, you can declare numeric constants with much more readability.</span></span>
