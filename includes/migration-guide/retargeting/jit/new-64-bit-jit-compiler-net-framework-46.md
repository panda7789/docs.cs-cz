---
ms.openlocfilehash: 0237c848d71150aaea1f9de4f4b16a0cbbc4ab3a
ms.sourcegitcommit: e02d17b2cf9c1258dadda4810a5e6072a0089aee
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/01/2020
ms.locfileid: "85615636"
---
### <a name="new-64-bit-jit-compiler-in-the-net-framework-46"></a><span data-ttu-id="62385-101">Nový 64 kompilátor JIT v .NET Framework 4,6</span><span class="sxs-lookup"><span data-stu-id="62385-101">New 64-bit JIT compiler in the .NET Framework 4.6</span></span>

#### <a name="details"></a><span data-ttu-id="62385-102">Podrobnosti</span><span class="sxs-lookup"><span data-stu-id="62385-102">Details</span></span>

<span data-ttu-id="62385-103">Počínaje .NET Framework 4,6 se k kompilaci za běhu používá nový 64 kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="62385-103">Starting with the .NET Framework 4.6, a new 64-bit JIT compiler is used for just-in-time compilation.</span></span> <span data-ttu-id="62385-104">V některých případech je vyvolána Neočekávaná výjimka nebo je zjištěno jiné chování než při spuštění aplikace pomocí kompilátoru 32 nebo staršího 64 kompilátoru JIT.</span><span class="sxs-lookup"><span data-stu-id="62385-104">In some cases, an unexpected exception is thrown or a different behavior is observed than if an app is run using the 32-bit compiler or the older 64-bit JIT compiler.</span></span> <span data-ttu-id="62385-105">Tato změna nemá vliv na 32 kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="62385-105">This change does not affect the 32-bit JIT compiler.</span></span> <span data-ttu-id="62385-106">Mezi známé rozdíly patří následující:</span><span class="sxs-lookup"><span data-stu-id="62385-106">The known differences include the following:</span></span>

- <span data-ttu-id="62385-107">Za určitých podmínek může rozbalení operace vyvolat v sestavení vydaných <xref:System.NullReferenceException> verzí s zapnutou optimalizací.</span><span class="sxs-lookup"><span data-stu-id="62385-107">Under certain conditions, an unboxing operation may throw a <xref:System.NullReferenceException> in Release builds with optimization turned on.</span></span>
- <span data-ttu-id="62385-108">V některých případech může provedení produkčního kódu v těle metody vyvolat výjimku <xref:System.StackOverflowException> .</span><span class="sxs-lookup"><span data-stu-id="62385-108">In some cases, execution of production code in a large method body may throw a <xref:System.StackOverflowException>.</span></span>
- <span data-ttu-id="62385-109">Za určitých podmínek jsou struktury předané metodě považovány za typy odkazů, nikoli jako typy hodnot v sestaveních vydaných verzí.</span><span class="sxs-lookup"><span data-stu-id="62385-109">Under certain conditions, structures passed to a method are treated as reference types rather than as value types in Release builds.</span></span> <span data-ttu-id="62385-110">Jedním z manifestů tohoto problému je, že jednotlivé položky v kolekci se zobrazí v neočekávaném pořadí.</span><span class="sxs-lookup"><span data-stu-id="62385-110">One of the manifestations of this issue is that the individual items in a collection appear in an unexpected order.</span></span>
- <span data-ttu-id="62385-111">Za určitých podmínek <xref:System.UInt16> je porovnání hodnot s jejich vysokou bitovou sadou nesprávné, pokud je povolena optimalizace.</span><span class="sxs-lookup"><span data-stu-id="62385-111">Under certain conditions, the comparison of <xref:System.UInt16> values with their high bit set is incorrect if optimization is enabled.</span></span>
- <span data-ttu-id="62385-112">Za určitých podmínek, zejména při inicializaci hodnot pole, může inicializace paměti pomocí <xref:System.Reflection.Emit.OpCodes.Initblk?displayProperty=nameWithType> instrukcí Il inicializovat paměť s nesprávnou hodnotou.</span><span class="sxs-lookup"><span data-stu-id="62385-112">Under certain conditions, particularly when initializing array values, memory initialization by the <xref:System.Reflection.Emit.OpCodes.Initblk?displayProperty=nameWithType> IL instruction may initialize memory with an incorrect value.</span></span> <span data-ttu-id="62385-113">Výsledkem může být Neošetřená výjimka nebo nesprávný výstup.</span><span class="sxs-lookup"><span data-stu-id="62385-113">This can result either in an unhandled exception or incorrect output.</span></span>
- <span data-ttu-id="62385-114">Za určitých vzácných podmínek může podmíněný bit test vracet nesprávnou <xref:System.Boolean> hodnotu nebo vyvolat výjimku, pokud jsou povoleny optimalizace kompilátoru.</span><span class="sxs-lookup"><span data-stu-id="62385-114">Under certain rare conditions, a conditional bit test can return the incorrect <xref:System.Boolean> value or throw an exception if compiler optimizations are enabled.</span></span>
- <span data-ttu-id="62385-115">Za určitých podmínek platí, že pokud `if` je příkaz použit k otestování podmínky před vstupem do `try` bloku a do ukončení z `try` bloku a stejná podmínka je vyhodnocena v `catch` bloku nebo `finally` , nový 64 kompilátor JIT `if` `catch` `finally` při optimalizaci kódu odstraní podmínku z bloku nebo.</span><span class="sxs-lookup"><span data-stu-id="62385-115">Under certain conditions, if an `if` statement is used to test for a condition before entering  a `try` block and in the exit from the `try` block, and the same condition is evaluated in the `catch` or `finally` block, the new 64-bit JIT compiler removes the `if` condition from the `catch` or `finally` block when it optimizes code.</span></span> <span data-ttu-id="62385-116">V důsledku toho je kód uvnitř `if` příkazu v `catch` `finally` bloku nebo proveden nepodmíněným způsobem.</span><span class="sxs-lookup"><span data-stu-id="62385-116">As a result, code inside the `if` statement in the `catch` or `finally` block is executed unconditionally.</span></span>

#### <a name="suggestion"></a><span data-ttu-id="62385-117">Návrh</span><span class="sxs-lookup"><span data-stu-id="62385-117">Suggestion</span></span>

<span data-ttu-id="62385-118">**Zmírnění známých problémů**</span><span class="sxs-lookup"><span data-stu-id="62385-118">**Mitigation of known issues**</span></span> <br/> <span data-ttu-id="62385-119">Pokud narazíte na výše uvedené problémy, můžete je vyřešit provedením následujících akcí:</span><span class="sxs-lookup"><span data-stu-id="62385-119">If you encounter the issues listed above, you can address them by doing any of the following:</span></span>

- <span data-ttu-id="62385-120">Upgradujte na .NET Framework 4.6.2.</span><span class="sxs-lookup"><span data-stu-id="62385-120">Upgrade to the .NET Framework 4.6.2.</span></span> <span data-ttu-id="62385-121">Nový 64 kompilátor zahrnutý do .NET Framework 4.6.2 řeší všechny tyto známé problémy.</span><span class="sxs-lookup"><span data-stu-id="62385-121">The new 64-bit compiler included with the .NET Framework 4.6.2 addresses each of these known issues.</span></span>
- <span data-ttu-id="62385-122">Zajistěte, aby byla verze Windows aktuální, a to spuštěním web Windows Update.</span><span class="sxs-lookup"><span data-stu-id="62385-122">Ensure that your version of Windows is up to date by running Windows Update.</span></span> <span data-ttu-id="62385-123">Aktualizace služby .NET Framework 4,6 a 4.6.1 řeší každý z těchto problémů s výjimkou <xref:System.NullReferenceException> operace rozbalení.</span><span class="sxs-lookup"><span data-stu-id="62385-123">Service updates to the .NET Framework 4.6 and 4.6.1 address each of these issues except the <xref:System.NullReferenceException> in an unboxing operation.</span></span>
- <span data-ttu-id="62385-124">Zkompiluje se starším 64 kompilátorem JIT.</span><span class="sxs-lookup"><span data-stu-id="62385-124">Compile with the older 64-bit JIT compiler.</span></span> <span data-ttu-id="62385-125">Další informace o tom, jak to udělat, najdete v části **zmírnění problémů s dalšími problémy** .</span><span class="sxs-lookup"><span data-stu-id="62385-125">See the **Mitigation of other issues** section for more information on how to do this.</span></span>
<span data-ttu-id="62385-126">**Zmírnění jiných problémů**</span><span class="sxs-lookup"><span data-stu-id="62385-126">**Mitigation of other issues**</span></span> <br/> <span data-ttu-id="62385-127">Pokud narazíte na jiné rozdíly v chování mezi kódem kompilovaným se starším 64 kompilátorem a novým 64 kompilátorem JIT, nebo mezi verzemi ladění a vydání vaší aplikace, které jsou zkompilovány pomocí nového 64-bitového kompilátoru JIT, můžete provést následující postup pro zkompilování aplikace se 64 starším 32bitovým kompilátorem JIT. :</span><span class="sxs-lookup"><span data-stu-id="62385-127">If you encounter any other difference in behavior between code compiled with the older 64-bit compiler and the new 64-bit JIT compiler, or between the debug and release versions of your app that are both compiled with the new 64-bit JIT compiler, you can do the following to compile your app with the older 64-bit JIT compiler:</span></span>

- <span data-ttu-id="62385-128">Na základě jednotlivých aplikací můžete přidat [<](~/docs/framework/configure-apps/file-schema/runtime/uselegacyjit-element.md) prvek do konfiguračního souboru aplikace.</span><span class="sxs-lookup"><span data-stu-id="62385-128">On a per-application basis, you can add the [<](~/docs/framework/configure-apps/file-schema/runtime/uselegacyjit-element.md) element to your application's configuration file.</span></span> <span data-ttu-id="62385-129">Následující zakáže kompilaci s novým 64 kompilátorem JIT a místo toho používá starší 64 kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="62385-129">The following disables compilation with the new 64-bit JIT compiler and instead uses the legacy 64-bit JIT compiler.</span></span>

    ```xml
    <?xml version ="1.0"?>
    <configuration>
      <runtime>
       <useLegacyJit enabled="1" />
      </runtime>
    </configuration>
    ```

- <span data-ttu-id="62385-130">Na základě jednotlivých uživatelů můžete do `REG_DWORD` klíče registru přidat hodnotu s názvem `useLegacyJit` `HKEY_CURRENT_USER\SOFTWARE\Microsoft\.NETFramework` .</span><span class="sxs-lookup"><span data-stu-id="62385-130">On a per-user basis, you can add a `REG_DWORD` value named `useLegacyJit` to the `HKEY_CURRENT_USER\SOFTWARE\Microsoft\.NETFramework` key of the registry.</span></span> <span data-ttu-id="62385-131">Hodnota 1 povolí starší 64 kompilátor JIT; hodnota 0 ji zakáže a povolí nový 64 kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="62385-131">A value of 1 enables the legacy 64-bit JIT compiler; a value of 0 disables it and enables the new 64-bit JIT compiler.</span></span>
- <span data-ttu-id="62385-132">Na základě jednotlivých počítačů můžete do `REG_DWORD` klíče registru přidat hodnotu s názvem `useLegacyJit` `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\.NETFramework` .</span><span class="sxs-lookup"><span data-stu-id="62385-132">On a per-machine basis, you can add a `REG_DWORD` value named `useLegacyJit` to the `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\.NETFramework` key of the registry.</span></span> <span data-ttu-id="62385-133">Hodnota `1` povoluje starší 64 kompilátor JIT; hodnota `0` ji zakáže a povolí nový 64 kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="62385-133">A value of `1` enables the legacy 64-bit JIT compiler; a value of `0` disables it and enables the new 64-bit JIT compiler.</span></span>
<span data-ttu-id="62385-134">Můžete nám taky informovat o problému tím, že nahlásíte chybu na [Microsoft Connect](https://connect.microsoft.com/VisualStudio).</span><span class="sxs-lookup"><span data-stu-id="62385-134">You can also let us know about the problem by reporting a bug on [Microsoft Connect](https://connect.microsoft.com/VisualStudio).</span></span>

| <span data-ttu-id="62385-135">Name</span><span class="sxs-lookup"><span data-stu-id="62385-135">Name</span></span>    | <span data-ttu-id="62385-136">Hodnota</span><span class="sxs-lookup"><span data-stu-id="62385-136">Value</span></span>       |
|:--------|:------------|
| <span data-ttu-id="62385-137">Rozsah</span><span class="sxs-lookup"><span data-stu-id="62385-137">Scope</span></span>   | <span data-ttu-id="62385-138">Edge</span><span class="sxs-lookup"><span data-stu-id="62385-138">Edge</span></span>        |
| <span data-ttu-id="62385-139">Verze</span><span class="sxs-lookup"><span data-stu-id="62385-139">Version</span></span> | <span data-ttu-id="62385-140">4.6</span><span class="sxs-lookup"><span data-stu-id="62385-140">4.6</span></span>         |
| <span data-ttu-id="62385-141">Typ</span><span class="sxs-lookup"><span data-stu-id="62385-141">Type</span></span>    | <span data-ttu-id="62385-142">Změna cílení</span><span class="sxs-lookup"><span data-stu-id="62385-142">Retargeting</span></span> |
