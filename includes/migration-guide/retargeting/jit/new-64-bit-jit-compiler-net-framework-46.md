---
ms.openlocfilehash: ddae1bfdd1a6f67bce111e0f97dd501c82022f36
ms.sourcegitcommit: 337bdc5a463875daf2cc6883e5a2da97d56f5000
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/24/2019
ms.locfileid: "72887753"
---
### <a name="new-64-bit-jit-compiler-in-the-net-framework-46"></a><span data-ttu-id="d844b-101">Nový 64 kompilátor JIT v .NET Framework 4,6</span><span class="sxs-lookup"><span data-stu-id="d844b-101">New 64-bit JIT compiler in the .NET Framework 4.6</span></span>

|   |   |
|---|---|
|<span data-ttu-id="d844b-102">Podrobnosti</span><span class="sxs-lookup"><span data-stu-id="d844b-102">Details</span></span>|<span data-ttu-id="d844b-103">Počínaje .NET Framework 4,6 se k kompilaci za běhu používá nový 64 kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="d844b-103">Starting with the .NET Framework 4.6, a new 64-bit JIT compiler is used for just-in-time compilation.</span></span> <span data-ttu-id="d844b-104">V některých případech je vyvolána Neočekávaná výjimka nebo je zjištěno jiné chování než při spuštění aplikace pomocí kompilátoru 32 nebo staršího 64 kompilátoru JIT.</span><span class="sxs-lookup"><span data-stu-id="d844b-104">In some cases, an unexpected exception is thrown or a different behavior is observed than if an app is run using the 32-bit compiler or the older 64-bit JIT compiler.</span></span> <span data-ttu-id="d844b-105">Tato změna nemá vliv na 32 kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="d844b-105">This change does not affect the 32-bit JIT compiler.</span></span> <span data-ttu-id="d844b-106">Mezi známé rozdíly patří následující:</span><span class="sxs-lookup"><span data-stu-id="d844b-106">The known differences include the following:</span></span><ul><li><span data-ttu-id="d844b-107">Za určitých podmínek může rozbalení operace vyvolat <xref:System.NullReferenceException> v sestavení vydaných verzí s zapnutou optimalizací.</span><span class="sxs-lookup"><span data-stu-id="d844b-107">Under certain conditions, an unboxing operation may throw a <xref:System.NullReferenceException> in Release builds with optimization turned on.</span></span></li><li><span data-ttu-id="d844b-108">V některých případech může provádění produkčního kódu v těle velké metody vyvolat <xref:System.StackOverflowException>.</span><span class="sxs-lookup"><span data-stu-id="d844b-108">In some cases, execution of production code in a large method body may throw a <xref:System.StackOverflowException>.</span></span></li><li><span data-ttu-id="d844b-109">Za určitých podmínek jsou struktury předané metodě považovány za typy odkazů, nikoli jako typy hodnot v sestaveních vydaných verzí.</span><span class="sxs-lookup"><span data-stu-id="d844b-109">Under certain conditions, structures passed to a method are treated as reference types rather than as value types in Release builds.</span></span> <span data-ttu-id="d844b-110">Jedním z manifestů tohoto problému je, že jednotlivé položky v kolekci se zobrazí v neočekávaném pořadí.</span><span class="sxs-lookup"><span data-stu-id="d844b-110">One of the manifestations of this issue is that the individual items in a collection appear in an unexpected order.</span></span></li><li><span data-ttu-id="d844b-111">Za určitých podmínek je porovnání hodnot <xref:System.UInt16> s jejich vysokou bitovou sadou nesprávné, pokud je povolena optimalizace.</span><span class="sxs-lookup"><span data-stu-id="d844b-111">Under certain conditions, the comparison of <xref:System.UInt16> values with their high bit set is incorrect if optimization is enabled.</span></span></li><li><span data-ttu-id="d844b-112">Za určitých podmínek, zejména při inicializaci hodnot pole, může inicializace paměti pomocí instrukcí <xref:System.Reflection.Emit.OpCodes.Initblk?displayProperty=nameWithType> IL inicializovat paměť s nesprávnou hodnotou.</span><span class="sxs-lookup"><span data-stu-id="d844b-112">Under certain conditions, particularly when initializing array values, memory initialization by the <xref:System.Reflection.Emit.OpCodes.Initblk?displayProperty=nameWithType> IL instruction may initialize memory with an incorrect value.</span></span> <span data-ttu-id="d844b-113">Výsledkem může být Neošetřená výjimka nebo nesprávný výstup.</span><span class="sxs-lookup"><span data-stu-id="d844b-113">This can result either in an unhandled exception or incorrect output.</span></span></li><li><span data-ttu-id="d844b-114">Za určitých vzácných podmínek může podmíněný bit test vracet nesprávnou <xref:System.Boolean> hodnotu nebo vyvolat výjimku, pokud jsou povoleny optimalizace kompilátoru.</span><span class="sxs-lookup"><span data-stu-id="d844b-114">Under certain rare conditions, a conditional bit test can return the incorrect <xref:System.Boolean> value or throw an exception if compiler optimizations are enabled.</span></span></li><li><span data-ttu-id="d844b-115">Za určitých podmínek platí, že pokud se k otestování podmínky používá příkaz <code>if</code>, před vstupem do bloku <code>try</code> a při ukončení z bloku <code>try</code> se tato podmínka vyhodnocuje v <code>catch</code> nebo <code>finally</code> bloku. , nový 64 kompilátor JIT při optimalizaci kódu odstraní podmínku <code>if</code> z bloku <code>catch</code> nebo <code>finally</code>.</span><span class="sxs-lookup"><span data-stu-id="d844b-115">Under certain conditions, if an <code>if</code> statement is used to test for a condition before entering  a <code>try</code> block and in the exit from the <code>try</code> block, and the same condition is evaluated in the <code>catch</code> or <code>finally</code> block, the new 64-bit JIT compiler removes the <code>if</code> condition from the <code>catch</code> or <code>finally</code> block when it optimizes code.</span></span> <span data-ttu-id="d844b-116">V důsledku toho je kód v příkazu <code>if</code> v bloku <code>catch</code> nebo <code>finally</code> prováděn nepodmíněně.</span><span class="sxs-lookup"><span data-stu-id="d844b-116">As a result, code inside the <code>if</code> statement in the <code>catch</code> or <code>finally</code> block is executed unconditionally.</span></span></li></ul>|
|<span data-ttu-id="d844b-117">Doporučení</span><span class="sxs-lookup"><span data-stu-id="d844b-117">Suggestion</span></span>|<span data-ttu-id="d844b-118">**Zmírnění známých problémů**</span><span class="sxs-lookup"><span data-stu-id="d844b-118">**Mitigation of known issues**</span></span> <br/> <span data-ttu-id="d844b-119">Pokud narazíte na výše uvedené problémy, můžete je vyřešit provedením následujících akcí:</span><span class="sxs-lookup"><span data-stu-id="d844b-119">If you encounter the issues listed above, you can address them by doing any of the following:</span></span><ul><li><span data-ttu-id="d844b-120">Upgradujte na .NET Framework 4.6.2.</span><span class="sxs-lookup"><span data-stu-id="d844b-120">Upgrade to the .NET Framework 4.6.2.</span></span> <span data-ttu-id="d844b-121">Nový 64 kompilátor zahrnutý do .NET Framework 4.6.2 řeší všechny tyto známé problémy.</span><span class="sxs-lookup"><span data-stu-id="d844b-121">The new 64-bit compiler included with the .NET Framework 4.6.2 addresses each of these known issues.</span></span></li><li><span data-ttu-id="d844b-122">Zajistěte, aby byla verze Windows aktuální, a to spuštěním web Windows Update.</span><span class="sxs-lookup"><span data-stu-id="d844b-122">Ensure that your version of Windows is up to date by running Windows Update.</span></span> <span data-ttu-id="d844b-123">Aktualizace služby .NET Framework 4,6 a 4.6.1 řeší každý z těchto problémů s výjimkou <xref:System.NullReferenceException> v operaci rozbalení.</span><span class="sxs-lookup"><span data-stu-id="d844b-123">Service updates to the .NET Framework 4.6 and 4.6.1 address each of these issues except the <xref:System.NullReferenceException> in an unboxing operation.</span></span></li><li><span data-ttu-id="d844b-124">Zkompiluje se starším 64 kompilátorem JIT.</span><span class="sxs-lookup"><span data-stu-id="d844b-124">Compile with the older 64-bit JIT compiler.</span></span> <span data-ttu-id="d844b-125">Další informace o tom, jak to udělat, najdete v části **zmírnění problémů s dalšími problémy** .</span><span class="sxs-lookup"><span data-stu-id="d844b-125">See the **Mitigation of other issues** section for more information on how to do this.</span></span></li></ul><span data-ttu-id="d844b-126">**Zmírnění jiných problémů**</span><span class="sxs-lookup"><span data-stu-id="d844b-126">**Mitigation of other issues**</span></span> <br/> <span data-ttu-id="d844b-127">Pokud narazíte na jiné rozdíly v chování mezi kódem kompilovaným se starším 64 kompilátorem a novým 64 kompilátorem JIT, nebo mezi verzemi ladění a vydání vaší aplikace, které jsou zkompilovány pomocí nového 64-bitového kompilátoru JIT, můžete provést následující akce: pro zkompilování aplikace se starším 64 kompilátorem JIT:</span><span class="sxs-lookup"><span data-stu-id="d844b-127">If you encounter any other difference in behavior between code compiled with the older 64-bit compiler and the new 64-bit JIT compiler, or between the debug and release versions of your app that are both compiled with the new 64-bit JIT compiler, you can do the following to compile your app with the older 64-bit JIT compiler:</span></span><ul><li><span data-ttu-id="d844b-128">Na základě jednotlivých aplikací můžete přidat [<](~/docs/framework/configure-apps/file-schema/runtime/uselegacyjit-element.md) element do konfiguračního souboru aplikace.</span><span class="sxs-lookup"><span data-stu-id="d844b-128">On a per-application basis, you can add the [<](~/docs/framework/configure-apps/file-schema/runtime/uselegacyjit-element.md) element to your application's configuration file.</span></span> <span data-ttu-id="d844b-129">Následující zakáže kompilaci s novým 64 kompilátorem JIT a místo toho používá starší 64 kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="d844b-129">The following disables compilation with the new 64-bit JIT compiler and instead uses the legacy 64-bit JIT compiler.</span></span></li></ul><pre><code class="lang-xml">&lt;?xml version =&quot;1.0&quot;?&gt;&#13;&#10;&lt;configuration&gt;&#13;&#10;&lt;runtime&gt;&#13;&#10;&lt;useLegacyJit enabled=&quot;1&quot; /&gt;&#13;&#10;&lt;/runtime&gt;&#13;&#10;&lt;/configuration&gt;&#13;&#10;</code></pre><ul><li><span data-ttu-id="d844b-130">Na základě jednotlivých uživatelů můžete do <code>HKEY_CURRENT_USER\SOFTWARE\Microsoft\.NETFramework</code> klíče registru přidat <code>REG_DWORD</code>ovou hodnotu s názvem <code>useLegacyJit</code>.</span><span class="sxs-lookup"><span data-stu-id="d844b-130">On a per-user basis, you can add a <code>REG_DWORD</code> value named <code>useLegacyJit</code> to the <code>HKEY_CURRENT_USER\SOFTWARE\Microsoft\.NETFramework</code> key of the registry.</span></span> <span data-ttu-id="d844b-131">Hodnota 1 povolí starší 64 kompilátor JIT; hodnota 0 ji zakáže a povolí nový 64 kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="d844b-131">A value of 1 enables the legacy 64-bit JIT compiler; a value of 0 disables it and enables the new 64-bit JIT compiler.</span></span></li><li><span data-ttu-id="d844b-132">Na základě jednotlivých počítačů můžete do <code>HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\.NETFramework</code> klíče registru přidat <code>REG_DWORD</code>ovou hodnotu s názvem <code>useLegacyJit</code>.</span><span class="sxs-lookup"><span data-stu-id="d844b-132">On a per-machine basis, you can add a <code>REG_DWORD</code> value named <code>useLegacyJit</code> to the <code>HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\.NETFramework</code> key of the registry.</span></span> <span data-ttu-id="d844b-133">Hodnota <code>1</code> povoluje starší 64 kompilátor JIT; hodnota <code>0</code> ji zakáže a povolí nový 64 kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="d844b-133">A value of <code>1</code> enables the legacy 64-bit JIT compiler; a value of <code>0</code> disables it and enables the new 64-bit JIT compiler.</span></span></li></ul><span data-ttu-id="d844b-134">Můžete nám taky informovat o problému tím, že nahlásíte chybu na [Microsoft Connect](https://connect.microsoft.com/VisualStudio).</span><span class="sxs-lookup"><span data-stu-id="d844b-134">You can also let us know about the problem by reporting a bug on [Microsoft Connect](https://connect.microsoft.com/VisualStudio).</span></span>|
|<span data-ttu-id="d844b-135">Rozsah</span><span class="sxs-lookup"><span data-stu-id="d844b-135">Scope</span></span>|<span data-ttu-id="d844b-136">Edge</span><span class="sxs-lookup"><span data-stu-id="d844b-136">Edge</span></span>|
|<span data-ttu-id="d844b-137">Version</span><span class="sxs-lookup"><span data-stu-id="d844b-137">Version</span></span>|<span data-ttu-id="d844b-138">4.6</span><span class="sxs-lookup"><span data-stu-id="d844b-138">4.6</span></span>|
|<span data-ttu-id="d844b-139">Typ</span><span class="sxs-lookup"><span data-stu-id="d844b-139">Type</span></span>|<span data-ttu-id="d844b-140">Změna cílení</span><span class="sxs-lookup"><span data-stu-id="d844b-140">Retargeting</span></span>|
