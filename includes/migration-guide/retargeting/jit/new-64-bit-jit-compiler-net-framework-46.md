---
ms.openlocfilehash: ddae1bfdd1a6f67bce111e0f97dd501c82022f36
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/15/2020
ms.locfileid: "72887753"
---
### <a name="new-64-bit-jit-compiler-in-the-net-framework-46"></a><span data-ttu-id="2ac14-101">Nový 64bitový kompilátor JIT v rozhraní .NET Framework 4.6</span><span class="sxs-lookup"><span data-stu-id="2ac14-101">New 64-bit JIT compiler in the .NET Framework 4.6</span></span>

|   |   |
|---|---|
|<span data-ttu-id="2ac14-102">Podrobnosti</span><span class="sxs-lookup"><span data-stu-id="2ac14-102">Details</span></span>|<span data-ttu-id="2ac14-103">Počínaje rozhraním .NET Framework 4.6 se pro kompilaci just-in-time používá nový 64bitový kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="2ac14-103">Starting with the .NET Framework 4.6, a new 64-bit JIT compiler is used for just-in-time compilation.</span></span> <span data-ttu-id="2ac14-104">V některých případech je vyvolána neočekávaná výjimka nebo je pozorováno jiné chování, než když je aplikace spuštěna pomocí 32bitového kompilátoru nebo staršího 64bitového kompilátoru JIT.</span><span class="sxs-lookup"><span data-stu-id="2ac14-104">In some cases, an unexpected exception is thrown or a different behavior is observed than if an app is run using the 32-bit compiler or the older 64-bit JIT compiler.</span></span> <span data-ttu-id="2ac14-105">Tato změna nemá vliv na 32bitový kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="2ac14-105">This change does not affect the 32-bit JIT compiler.</span></span> <span data-ttu-id="2ac14-106">Známé rozdíly zahrnují následující:</span><span class="sxs-lookup"><span data-stu-id="2ac14-106">The known differences include the following:</span></span><ul><li><span data-ttu-id="2ac14-107">Za určitých podmínek může operace <xref:System.NullReferenceException> rozbalení vyvolat sestavení v verzi se zapnutou optimalizací.</span><span class="sxs-lookup"><span data-stu-id="2ac14-107">Under certain conditions, an unboxing operation may throw a <xref:System.NullReferenceException> in Release builds with optimization turned on.</span></span></li><li><span data-ttu-id="2ac14-108">V některých případech provádění výrobního kódu v těle <xref:System.StackOverflowException>velké metody může vyvolat .</span><span class="sxs-lookup"><span data-stu-id="2ac14-108">In some cases, execution of production code in a large method body may throw a <xref:System.StackOverflowException>.</span></span></li><li><span data-ttu-id="2ac14-109">Za určitých podmínek struktury předané metodě jsou považovány za typy odkazů, nikoli jako typy hodnot v sestaveních release.</span><span class="sxs-lookup"><span data-stu-id="2ac14-109">Under certain conditions, structures passed to a method are treated as reference types rather than as value types in Release builds.</span></span> <span data-ttu-id="2ac14-110">Jedním z projevů tohoto problému je, že jednotlivé položky v kolekci se zobrazí v neočekávaném pořadí.</span><span class="sxs-lookup"><span data-stu-id="2ac14-110">One of the manifestations of this issue is that the individual items in a collection appear in an unexpected order.</span></span></li><li><span data-ttu-id="2ac14-111">Za určitých <xref:System.UInt16> podmínek porovnání hodnot s jejich vysokou bitovou sadou je nesprávné, pokud je povolena optimalizace.</span><span class="sxs-lookup"><span data-stu-id="2ac14-111">Under certain conditions, the comparison of <xref:System.UInt16> values with their high bit set is incorrect if optimization is enabled.</span></span></li><li><span data-ttu-id="2ac14-112">Za určitých podmínek, zejména při inicializaci hodnot pole, může inicializace paměti inicializací <xref:System.Reflection.Emit.OpCodes.Initblk?displayProperty=nameWithType> il inicializace s nesprávnou hodnotou.</span><span class="sxs-lookup"><span data-stu-id="2ac14-112">Under certain conditions, particularly when initializing array values, memory initialization by the <xref:System.Reflection.Emit.OpCodes.Initblk?displayProperty=nameWithType> IL instruction may initialize memory with an incorrect value.</span></span> <span data-ttu-id="2ac14-113">To může mít za následek neošetřené výjimky nebo nesprávný výstup.</span><span class="sxs-lookup"><span data-stu-id="2ac14-113">This can result either in an unhandled exception or incorrect output.</span></span></li><li><span data-ttu-id="2ac14-114">Za určitých výjimečných podmínek podmíněný <xref:System.Boolean> bitový test může vrátit nesprávnou hodnotu nebo vyvolat výjimku, pokud jsou povoleny optimalizace kompilátoru.</span><span class="sxs-lookup"><span data-stu-id="2ac14-114">Under certain rare conditions, a conditional bit test can return the incorrect <xref:System.Boolean> value or throw an exception if compiler optimizations are enabled.</span></span></li><li><span data-ttu-id="2ac14-115">Za určitých podmínek, <code>if</code> pokud příkaz se používá <code>try</code> k testování podmínky před <code>try</code> zadáním bloku a výstupu <code>catch</code> z <code>finally</code> bloku a stejné podmínky je vyhodnocena <code>if</code> v <code>catch</code> nebo <code>finally</code> bloku, nový 64bitový kompilátor JIT odebere podmínku z nebo bloku při optimalizaci kódu.</span><span class="sxs-lookup"><span data-stu-id="2ac14-115">Under certain conditions, if an <code>if</code> statement is used to test for a condition before entering  a <code>try</code> block and in the exit from the <code>try</code> block, and the same condition is evaluated in the <code>catch</code> or <code>finally</code> block, the new 64-bit JIT compiler removes the <code>if</code> condition from the <code>catch</code> or <code>finally</code> block when it optimizes code.</span></span> <span data-ttu-id="2ac14-116">V důsledku toho je <code>if</code> kód <code>catch</code> uvnitř <code>finally</code> příkazu v bloku nebo proveden bezpodmínečně.</span><span class="sxs-lookup"><span data-stu-id="2ac14-116">As a result, code inside the <code>if</code> statement in the <code>catch</code> or <code>finally</code> block is executed unconditionally.</span></span></li></ul>|
|<span data-ttu-id="2ac14-117">Návrh</span><span class="sxs-lookup"><span data-stu-id="2ac14-117">Suggestion</span></span>|<span data-ttu-id="2ac14-118">**Zmírnění známých problémů**</span><span class="sxs-lookup"><span data-stu-id="2ac14-118">**Mitigation of known issues**</span></span> <br/> <span data-ttu-id="2ac14-119">Pokud narazíte na výše uvedené problémy, můžete je řešit některou z následujících akcí:</span><span class="sxs-lookup"><span data-stu-id="2ac14-119">If you encounter the issues listed above, you can address them by doing any of the following:</span></span><ul><li><span data-ttu-id="2ac14-120">Upgrade na rozhraní .NET Framework 4.6.2.</span><span class="sxs-lookup"><span data-stu-id="2ac14-120">Upgrade to the .NET Framework 4.6.2.</span></span> <span data-ttu-id="2ac14-121">Nový 64bitový kompilátor, který je součástí rozhraní .NET Framework 4.6.2, řeší každý z těchto známých problémů.</span><span class="sxs-lookup"><span data-stu-id="2ac14-121">The new 64-bit compiler included with the .NET Framework 4.6.2 addresses each of these known issues.</span></span></li><li><span data-ttu-id="2ac14-122">Spuštěním služby Windows Update zajistíte aktuálnost vaší verze systému Windows.</span><span class="sxs-lookup"><span data-stu-id="2ac14-122">Ensure that your version of Windows is up to date by running Windows Update.</span></span> <span data-ttu-id="2ac14-123">Aktualizace služby rozhraní .NET Framework 4.6 a 4.6.1 <xref:System.NullReferenceException> řeší každý z těchto problémů s výjimkou operace rozbalení.</span><span class="sxs-lookup"><span data-stu-id="2ac14-123">Service updates to the .NET Framework 4.6 and 4.6.1 address each of these issues except the <xref:System.NullReferenceException> in an unboxing operation.</span></span></li><li><span data-ttu-id="2ac14-124">Kompilujte se starším 64bitovým kompilátorem JIT.</span><span class="sxs-lookup"><span data-stu-id="2ac14-124">Compile with the older 64-bit JIT compiler.</span></span> <span data-ttu-id="2ac14-125">Další informace o tom, jak to provést, naleznete v části **Zmírnění dalších problémů.**</span><span class="sxs-lookup"><span data-stu-id="2ac14-125">See the **Mitigation of other issues** section for more information on how to do this.</span></span></li></ul><span data-ttu-id="2ac14-126">**Zmírnění dalších problémů**</span><span class="sxs-lookup"><span data-stu-id="2ac14-126">**Mitigation of other issues**</span></span> <br/> <span data-ttu-id="2ac14-127">Pokud narazíte na jiný rozdíl v chování mezi kódem kompilovaným se starším 64bitovým kompilátorem a novým 64bitovým kompilátorem JIT nebo mezi ladicí mi a verzí aplikace, které jsou zkompilovány s novým 64bitovým kompilátorem JIT, můžete provést následující postup kompilace aplikace se starším 64bitovým kompilátorem JIT:</span><span class="sxs-lookup"><span data-stu-id="2ac14-127">If you encounter any other difference in behavior between code compiled with the older 64-bit compiler and the new 64-bit JIT compiler, or between the debug and release versions of your app that are both compiled with the new 64-bit JIT compiler, you can do the following to compile your app with the older 64-bit JIT compiler:</span></span><ul><li><span data-ttu-id="2ac14-128">V základě pro aplikaci můžete [<](~/docs/framework/configure-apps/file-schema/runtime/uselegacyjit-element.md) přidat prvek do konfiguračního souboru aplikace.</span><span class="sxs-lookup"><span data-stu-id="2ac14-128">On a per-application basis, you can add the [<](~/docs/framework/configure-apps/file-schema/runtime/uselegacyjit-element.md) element to your application's configuration file.</span></span> <span data-ttu-id="2ac14-129">Následující zakáže kompilaci s novým 64bitovým kompilátorem JIT a místo toho používá starší 64bitový kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="2ac14-129">The following disables compilation with the new 64-bit JIT compiler and instead uses the legacy 64-bit JIT compiler.</span></span></li></ul><pre><code class="lang-xml">&lt;?xml version =&quot;1.0&quot;?&gt;&#13;&#10;&lt;configuration&gt;&#13;&#10;&lt;runtime&gt;&#13;&#10;&lt;useLegacyJit enabled=&quot;1&quot; /&gt;&#13;&#10;&lt;/runtime&gt;&#13;&#10;&lt;/configuration&gt;&#13;&#10;</code></pre><ul><li><span data-ttu-id="2ac14-130">Na základě pro jednotlivé uživatele můžete <code>REG_DWORD</code> přidat <code>useLegacyJit</code> hodnotu pojmenovanou <code>HKEY_CURRENT_USER\SOFTWARE\Microsoft\.NETFramework</code> do klíče registru.</span><span class="sxs-lookup"><span data-stu-id="2ac14-130">On a per-user basis, you can add a <code>REG_DWORD</code> value named <code>useLegacyJit</code> to the <code>HKEY_CURRENT_USER\SOFTWARE\Microsoft\.NETFramework</code> key of the registry.</span></span> <span data-ttu-id="2ac14-131">Hodnota 1 umožňuje starší 64bitový kompilátor JIT; hodnota 0 zakáže a povolí nový 64bitový kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="2ac14-131">A value of 1 enables the legacy 64-bit JIT compiler; a value of 0 disables it and enables the new 64-bit JIT compiler.</span></span></li><li><span data-ttu-id="2ac14-132">Na základě pro počítač můžete přidat <code>REG_DWORD</code> hodnotu <code>HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\.NETFramework</code> pojmenovanou <code>useLegacyJit</code> do klíče registru.</span><span class="sxs-lookup"><span data-stu-id="2ac14-132">On a per-machine basis, you can add a <code>REG_DWORD</code> value named <code>useLegacyJit</code> to the <code>HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\.NETFramework</code> key of the registry.</span></span> <span data-ttu-id="2ac14-133">Hodnota <code>1</code> umožňuje starší 64bitový kompilátor JIT; hodnota zakáže <code>0</code> a povolí nový 64bitový kompilátor JIT.</span><span class="sxs-lookup"><span data-stu-id="2ac14-133">A value of <code>1</code> enables the legacy 64-bit JIT compiler; a value of <code>0</code> disables it and enables the new 64-bit JIT compiler.</span></span></li></ul><span data-ttu-id="2ac14-134">Můžete nám také dát vědět o problému nahlášením chyby na [Microsoft Connect](https://connect.microsoft.com/VisualStudio).</span><span class="sxs-lookup"><span data-stu-id="2ac14-134">You can also let us know about the problem by reporting a bug on [Microsoft Connect](https://connect.microsoft.com/VisualStudio).</span></span>|
|<span data-ttu-id="2ac14-135">Rozsah</span><span class="sxs-lookup"><span data-stu-id="2ac14-135">Scope</span></span>|<span data-ttu-id="2ac14-136">Edge</span><span class="sxs-lookup"><span data-stu-id="2ac14-136">Edge</span></span>|
|<span data-ttu-id="2ac14-137">Version</span><span class="sxs-lookup"><span data-stu-id="2ac14-137">Version</span></span>|<span data-ttu-id="2ac14-138">4.6</span><span class="sxs-lookup"><span data-stu-id="2ac14-138">4.6</span></span>|
|<span data-ttu-id="2ac14-139">Typ</span><span class="sxs-lookup"><span data-stu-id="2ac14-139">Type</span></span>|<span data-ttu-id="2ac14-140">Změna cílení</span><span class="sxs-lookup"><span data-stu-id="2ac14-140">Retargeting</span></span>|
