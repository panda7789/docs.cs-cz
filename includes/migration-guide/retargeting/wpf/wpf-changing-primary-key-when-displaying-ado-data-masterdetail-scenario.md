### <a name="wpf-changing-a-primary-key-when-displaying-ado-data-in-a-masterdetail-scenario"></a><span data-ttu-id="b6feb-101">Změna primárního klíče při zobrazování dat ADO v případě záznamů Master/Detail v WPF</span><span class="sxs-lookup"><span data-stu-id="b6feb-101">WPF Changing a primary key when displaying ADO data in a Master/Detail scenario</span></span>

|   |   |
|---|---|
|<span data-ttu-id="b6feb-102">Podrobnosti</span><span class="sxs-lookup"><span data-stu-id="b6feb-102">Details</span></span>|<span data-ttu-id="b6feb-103">Předpokládejme, že máte kolekci ADO položky typu <code>Order</code>, s relation s názvem &quot;OrderDetails&quot; týkající se kolekce položek typu <code>Detail</code> přes primární klíč &quot;OrderID&quot;.</span><span class="sxs-lookup"><span data-stu-id="b6feb-103">Suppose you have an ADO collection of items of type <code>Order</code>, with a relation named &quot;OrderDetails&quot; relating it to a collection of items of type <code>Detail</code> via the primary key &quot;OrderID&quot;.</span></span> <span data-ttu-id="b6feb-104">V aplikaci WPF může vytvoření vazby ovládacího prvku seznam v podrobnostech uvedeném pořadí:</span><span class="sxs-lookup"><span data-stu-id="b6feb-104">In your WPF app, you can bind a list control to the details for a given order:</span></span><pre><code class="lang-xml">&lt;ListBox ItemsSource=&quot;{Binding Path=OrderDetails}&quot; &gt;&#13;&#10;</code></pre><span data-ttu-id="b6feb-105">kde je DataContext <code>Order</code>.</span><span class="sxs-lookup"><span data-stu-id="b6feb-105">where the DataContext is an <code>Order</code>.</span></span> <span data-ttu-id="b6feb-106">WPF získá hodnotu <code>OrderDetails</code> vlastnost – shromažďování D všech <code>Detail</code> položky, jejichž <code>OrderID</code> odpovídá <code>OrderID</code> hlavní položky. Změna chování nastane při změně primárního klíče <code>OrderID</code> hlavní položky.</span><span class="sxs-lookup"><span data-stu-id="b6feb-106">WPF gets the value of the <code>OrderDetails</code> property - a collection D of all the <code>Detail</code> items whose <code>OrderID</code> matches the <code>OrderID</code> of the master item.The behavior change arises when you change the primary key <code>OrderID</code> of the master item.</span></span> <span data-ttu-id="b6feb-107">ADO automaticky změní <code>OrderID</code> každé příslušné záznamy v kolekci podrobností (konkrétně těm, které jsou zkopírovány do kolekce D).</span><span class="sxs-lookup"><span data-stu-id="b6feb-107">ADO automatically changes the <code>OrderID</code> of each of the affected records in the Details collection (namely the ones copied into collection D).</span></span>  <span data-ttu-id="b6feb-108">Ale co se stane D?</span><span class="sxs-lookup"><span data-stu-id="b6feb-108">But what happens to D?</span></span><ul><li><span data-ttu-id="b6feb-109">Staré chování: není zaškrtnuté shromažďování D.</span><span class="sxs-lookup"><span data-stu-id="b6feb-109">Old behavior:   Collection D is cleared.</span></span>   <span data-ttu-id="b6feb-110">Hlavní položky nemá <em>není</em> vyvolat oznámení o změně pro vlastnost <code>OrderDetails</code>.</span><span class="sxs-lookup"><span data-stu-id="b6feb-110">The master item does <em>not</em> raise a change notification for property <code>OrderDetails</code>.</span></span>  <span data-ttu-id="b6feb-111">Objekt ListBox i nadále používat shromažďování D, která je teď prázdný.</span><span class="sxs-lookup"><span data-stu-id="b6feb-111">The ListBox continues to use collection D, which is now empty.</span></span></li><li><span data-ttu-id="b6feb-112">Nové chování: kolekce D je beze změny.</span><span class="sxs-lookup"><span data-stu-id="b6feb-112">New behavior:  Collection D is unchanged.</span></span>   <span data-ttu-id="b6feb-113">Každá z jeho položek vyvolá oznámení o změně pro <code>OrderID</code> vlastnost.</span><span class="sxs-lookup"><span data-stu-id="b6feb-113">Each of its items raises a change notification for the <code>OrderID</code> property.</span></span>  <span data-ttu-id="b6feb-114">Objekt ListBox dál používat shromažďování D a zobrazí podrobnosti o s novými <code>OrderID</code>.</span><span class="sxs-lookup"><span data-stu-id="b6feb-114">The ListBox continues to use collection D, and displays the details with the new <code>OrderID</code>.</span></span></li></ul><span data-ttu-id="b6feb-115">WPF implementuje nové chování tak, že vytvoříte kolekci D jiným způsobem: zavoláním metody ADO <xref:System.Data.DataRowView.CreateChildView(System.Data.DataRelation,System.Boolean)?displayProperty=nameWithType> s <code>followParent</code> argument nastaven na <code>true</code>.</span><span class="sxs-lookup"><span data-stu-id="b6feb-115">WPF implements the new behavior by creating collection D in a different way:  by calling the ADO method <xref:System.Data.DataRowView.CreateChildView(System.Data.DataRelation,System.Boolean)?displayProperty=nameWithType> with the <code>followParent</code> argument set to <code>true</code>.</span></span>|
|<span data-ttu-id="b6feb-116">Návrh</span><span class="sxs-lookup"><span data-stu-id="b6feb-116">Suggestion</span></span>|<span data-ttu-id="b6feb-117">Aplikace získá pomocí následující přepínač AppContext nové chování.</span><span class="sxs-lookup"><span data-stu-id="b6feb-117">An app gets the new behavior by using the following AppContext switch.</span></span><pre><code class="lang-xml">&lt;configuration&gt;&#13;&#10;&lt;runtime&gt;&#13;&#10;&lt;AppContextSwitchOverrides value=&quot;Switch.System.Windows.Data.DoNotUseFollowParentWhenBindingToADODataRelation=false&quot;/&gt;&#13;&#10;&lt;/runtime&gt;&#13;&#10;&lt;/configuration&gt;&#13;&#10;&#13;&#10;</code></pre><span data-ttu-id="b6feb-118">Výchozí hodnota přepínače <code>true</code> (staré chování) pro aplikace, které cílí na .NET 4.7.1 nebo dolů a <code>false</code> (nové chování) pro aplikace, které cílí na .NET 4.7.2 nebo vyšší.</span><span class="sxs-lookup"><span data-stu-id="b6feb-118">The switch defaults to <code>true</code> (old behavior) for apps that target .NET 4.7.1 or below, and to <code>false</code> (new behavior) for apps that target .NET 4.7.2 or above.</span></span>|
|<span data-ttu-id="b6feb-119">Rozsah</span><span class="sxs-lookup"><span data-stu-id="b6feb-119">Scope</span></span>|<span data-ttu-id="b6feb-120">Vedlejší</span><span class="sxs-lookup"><span data-stu-id="b6feb-120">Minor</span></span>|
|<span data-ttu-id="b6feb-121">Version</span><span class="sxs-lookup"><span data-stu-id="b6feb-121">Version</span></span>|<span data-ttu-id="b6feb-122">4.7.2</span><span class="sxs-lookup"><span data-stu-id="b6feb-122">4.7.2</span></span>|
|<span data-ttu-id="b6feb-123">Typ</span><span class="sxs-lookup"><span data-stu-id="b6feb-123">Type</span></span>|<span data-ttu-id="b6feb-124">Mění se cílení</span><span class="sxs-lookup"><span data-stu-id="b6feb-124">Retargeting</span></span>|

